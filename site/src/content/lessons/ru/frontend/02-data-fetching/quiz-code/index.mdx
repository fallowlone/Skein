---
concepts:
- request-waterfall
- promise-all
- query-key
- rsc-suspense
- abort-controller
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://react.dev/reference/rsc/server-components
- https://tanstack.com/query/v5/docs/framework/react/guides/request-waterfalls
status: ready
summary: "Чтение реальных сниппетов fetching — серверный waterfall, queryKey, RSC-разметка с Suspense и гонка поиска — выбери поведение или фикс с наибольшим рычагом."
title: 'Data fetching: чтение кода'
track: frontend
unit: 02-data-fetching
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги data fetching читаются в коде и в панели Network. Прочитай каждый сниппет, предскажи форму round-trip'ов и выбери фикс, который senior-фронтендер делает первым.
</Hook>

<Goal lang="ru">
Потренируй цикл, который запускаешь на каждом ревью fetching: проследи, какие запросы последовательны vs параллельны, прочитай ключ кэша как идентичность, заметь RSC-waterfall, прячущийся в цикле, и убей гонку ответов в источнике.
</Goal>

## Сниппет 1 — цикл в серверном компоненте

```tsx
// Server Component — рендерит список из 50 заказов
async function OrderList({ ids }: { ids: string[] }) {
  const orders = [];
  for (const id of ids) {
    orders.push(await fetchOrder(id));   // один await на итерацию
  }
  return <ul>{orders.map(o => <Order key={o.id} data={o} />)}</ul>;
}
```

<Quiz
  id="02-data-fetching-code-1"
  lessonSlug="02-data-fetching"
  lang="ru"
  question="Каждый fetchOrder занимает ~40ms. Каково суммарное время fetch и каков фикс?"
  choices={[
    { label: "~2000ms — await внутри for-цикла сериализует все 50 fetch'ей, серверный waterfall, который блокирует стриминг Suspense-границы. Фикс: const orders = await Promise.all(ids.map(fetchOrder)), что ограничивает время до ~40ms.", correct: true },
    { label: "~40ms — Server Components фетчат параллельно автоматически.", misconception: "Параллельно фетчат только соседние Server Components. Явный await внутри цикла выполняется строго последовательно; параллелизм надо выразить через Promise.all." },
    { label: "~40ms — движок JS поднимает await'ы и выполняет их конкурентно.", misconception: "JavaScript никогда не переупорядочивает и не параллелит await'ы в цикле. Каждый await ставит функцию на паузу до разрешения промиса, прежде чем начнётся следующая итерация." },
    { label: "~2000ms, но это неизбежно, потому что данные должны быть упорядочены.", misconception: "Promise.all сохраняет порядок входа в массиве результата, так что упорядоченность бесплатна. Последовательную цену можно избежать; for-await цикл — это дефект." },
  ]}
/>

## Сниппет 2 — ключ запроса

```typescript
function useProduct(id: string, currency: string) {
  return useQuery({
    queryKey: ['product', id],                 // currency НЕ в ключе
    queryFn: () => fetchProduct(id, currency),
  });
}
```

<Quiz
  id="02-data-fetching-code-2"
  lessonSlug="02-data-fetching"
  lang="ru"
  question="Пользователь переключает валюту с USD на EUR на том же товаре. Что ломается и почему?"
  choices={[
    { label: "Ключ кэша опускает currency, поэтому ['product', id] трактуется как одна запись. После USD-fetch переключение на EUR попадает в кэшированный USD-результат и может не рефетчить — цены показываются в неправильной валюте. Фикс: queryKey: ['product', id, currency], чтобы каждая валюта была отдельной записью кэша.", correct: true },
    { label: "Ничего — queryFn замыкает currency, поэтому всегда фетчится правильное значение.", misconception: "queryFn действительно замыкает currency, но кэш ключуется только по ['product', id]. Если эта запись свежа, TanStack Query возвращает кэшированное значение вообще без вызова queryFn — новая валюта никогда не фетчится." },
    { label: "Происходит двойной fetch, потому что смена валюты инвалидирует ключ.", misconception: "Ключ не изменился — currency в нём нет — поэтому ничто не инвалидируется. Баг обратный: устаревшее попадание в кэш, а не лишний fetch." },
    { label: "Фикс — снизить staleTime до 0.", misconception: "staleTime=0 форсит рефетч при монтировании, но сначала всё равно возвращает устаревшее кэшированное USD-значение и смешивает две валюты в одной записи. Идентичность принадлежит ключу; каждый вход, меняющий ответ, должен быть частью queryKey." },
  ]}
/>

## Сниппет 3 — RSC-разметка

```tsx
// Server Component страница
async function ProductPage({ id }) {
  const product = await fetchProduct(id);        // быстро, ~50ms
  return (
    <main>
      <h1>{product.name}</h1>
      <Suspense fallback={<ReviewsSkeleton />}>
        <Reviews productId={id} />               {/* медленно, ~800ms */}
      </Suspense>
    </main>
  );
}
```

<Quiz
  id="02-data-fetching-code-3"
  lessonSlug="02-data-fetching"
  lang="ru"
  question="Reviews занимает 800ms. Когда пользователь впервые видит заголовок товара и почему?"
  choices={[
    { label: "На ~50ms. Оболочка и h1 рендерятся, как только fetchProduct разрешился, и стримятся сразу; Suspense-граница позволяет Reviews разрешиться независимо, так что медленный 800ms-fetch не блокирует заголовок. TTFB — это время рендера оболочки, а не самого медленного fetch'а.", correct: true },
    { label: "На ~850ms — сервер должен завершить все fetch'и, прежде чем отправить HTML.", misconception: "Это блокирующий SSR без Suspense. Здесь Suspense-граница вокруг Reviews позволяет серверу стримить заголовок сразу и отправить чанк отзывов позже." },
    { label: "На ~800ms — Suspense ждёт всех детей, прежде чем показать родителя.", misconception: "Suspense изолирует за fallback только своё поддерево. h1 находится вне границы и стримится с оболочкой; за скелетоном ждёт только Reviews." },
    { label: "Никогда на сервере — заголовку нужна клиентская гидрация, чтобы появиться.", misconception: "Заголовок — это серверно-отрендеренный HTML, видимый до запуска любого JS. Гидрация прикрепляет интерактивность позже; для отрисовки заголовка она не нужна." },
  ]}
/>

## Сниппет 4 — поле поиска

```typescript
function useSearch(query: string) {
  const [results, setResults] = useState([]);
  useEffect(() => {
    fetch(`/api/search?q=${query}`)
      .then(r => r.json())
      .then(setResults);                  // нет cleanup, нет отмены
  }, [query]);
  return results;
}
```

<Quiz
  id="02-data-fetching-code-4"
  lessonSlug="02-data-fetching"
  lang="ru"
  question="При быстром наборе это периодически показывает результаты более раннего запроса. Каков минимальный корректный фикс?"
  choices={[
    { label: "Добавить AbortController и отменять предыдущий запрос в cleanup эффекта: создать controller, передать controller.signal в fetch и вернуть () => controller.abort(). Медленный более ранний ответ отменяется, прежде чем сможет затереть новые результаты.", correct: true },
    { label: "Обернуть setResults в setTimeout, чтобы отложить обновление.", misconception: "Задержка не упорядочивает ответы — устаревший ответ 'reac' всё равно может прийти последним. Только отмена (или версионирование queryKey) гарантирует победу последнего запроса." },
    { label: "Мемоизировать строку запроса через useMemo.", misconception: "useMemo избегает пересчёта значения; он ничего не делает с ответами сети, пришедшими не по порядку. Гонка во времени fetch'а, а не в пересчёте." },
    { label: "Вынести fetch из useEffect в тело рендера.", misconception: "Fetch во время рендера — худший анти-паттерн: он срабатывает на каждый рендер и по-прежнему без отмены. Фикс — AbortController в cleanup эффекта или библиотека запросов, отменяющая устаревшие ключи." },
  ]}
/>

<Recap lang="ru">
Каждый баг fetching виден в коде: await внутри цикла — это серверный waterfall, Promise.all ограничивает его самым медленным вызовом; queryKey должен содержать каждый вход, меняющий ответ, иначе кэш отдаёт неправильную запись; Suspense-граница позволяет оболочке стримиться на TTFB, пока медленные дети разрешаются независимо; а неотменённый fetch в эффекте гоняется на быстром вводе — AbortController в cleanup или библиотека, отменяющая устаревшие ключи, дают победу последнему запросу. Читай код, проследи round-trip'ы, чини структуру прежде чем тянуться к настройке.
</Recap>
