---
concepts:
- fetch-location
- fetch-waterfall
- rsc-streaming
- swr-cache
- lcp-critical-path
- cache-invalidation
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://react.dev/reference/rsc/server-components
- https://tanstack.com/query/v5/docs/framework/react/guides/request-waterfalls
- https://web.dev/articles/optimize-lcp
status: ready
summary: "Синтез всего юнита data fetching в формате выбора — место fetch, waterfall'ы, RSC streaming, SWR-кэш, LCP и многослойная инвалидация."
title: 'Data fetching: тест с множественным выбором'
track: frontend
unit: 02-data-fetching
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов через весь юнит. Каждый отражает решение, которое ты принимаешь на реальном перф-ревью — не определение для пересказа, а компромисс, который надо взвесить против бюджета LCP и риска устаревших данных.
</Hook>

<Goal lang="ru">
Убедись, что связываешь место fetch, waterfall'ы, RSC streaming, клиентский кэш и многослойную инвалидацию в одно решение: какие round-trip'ы лежат на critical path и что на самом деле покупает каждый фикс.
</Goal>

<Quiz
  id="02-data-fetching-choice-1"
  lessonSlug="02-data-fetching"
  lang="ru"
  question="Страница товара рендерит заголовок (LCP-элемент) из клиентского useEffect-fetch. LCP равен 2.9s. Команда добавляет скелетон загрузки и поднимает TTL CDN-кэша. LCP почти не двигается. Какой фикс настоящий?"
  choices={[
    { label: "Перенести fetch заголовка на сервер (RSC или SSR), чтобы HTML приходил с уже встроенным заголовком — это убирает JS-download + mount + fetch из LCP critical path, единственной цепочки, которая здесь задаёт LCP.", correct: true },
    { label: "Добавить fetchpriority='high' на текстовый узел заголовка.", misconception: "fetchpriority — про обнаружение ресурсов (картинки, шрифты), а не про вызов API через JS. Заголовок заперт за загрузкой бандла и mount, до которых fetchpriority не дотягивается." },
    { label: "Поднять TTL CDN ещё выше, чтобы скелетон кэшировался.", misconception: "Кэширование HTML скелетона ничего не меняет — LCP-элемент это заголовок, и он по-прежнему ждёт клиентский fetch после запуска JS. TTL CDN на оболочке эту цепочку не схлопывает." },
    { label: "Перейти на HTTP/3, чтобы срезать накладные расходы соединения.", misconception: "HTTP/3 срезает немного латентности handshake, но оставляет последовательный порядок bundle-затем-mount-затем-fetch нетронутым. Выигрыш — серверный fetch, а не более быстрый транспорт." },
  ]}
/>

<Quiz
  id="02-data-fetching-choice-2"
  lessonSlug="02-data-fetching"
  lang="ru"
  question="Страница профиля грузит user (200ms), posts (300ms) и followers (150ms) — все независимые — через три вложенных useEffect-компонента. Суммарно ~650ms. Почему, и что ограничивает это до ~300ms?"
  choices={[
    { label: "React рендерит сверху вниз: каждый ребёнок монтируется только после прихода данных эффекта родителя, поэтому каскад рендера сериализует независимые fetch'и. Подъём их в родительский Promise.all (или соседние RSC-компоненты) ограничивает суммарное время самым медленным fetch'ем, ~300ms.", correct: true },
    { label: "Fetch'и делят пул соединений, который допускает только одно за раз.", misconception: "Браузер разрешает несколько одновременных соединений на origin; сериализация здесь — каскад рендера React, а не лимиты соединений. Promise.all запускает все три сразу независимо от этого." },
    { label: "Суммарное время уже max(200,300,150) = 300ms; 650ms — это шум измерений.", misconception: "Вложенные useEffect-fetch'и реально складываются, потому что каждый ребёнок не может смонтироваться, пока родитель не перерендерится с данными. 650ms — реальный waterfall, а не шум." },
    { label: "Добавить больший staleTime, чтобы кэш поглотил латентность.", misconception: "staleTime управляет временем рефетча на повторных визитах, а не waterfall'ом первой отрисовки. На холодной загрузке кэша нет; fetch'и всё равно сериализуются." },
  ]}
/>

<Quiz
  id="02-data-fetching-choice-3"
  lessonSlug="02-data-fetching"
  lang="ru"
  question="Команда помечает каждый компонент 'use client' в Next.js App Router, а потом жалуется, что RSC не дал выигрыша по бандлу. Что произошло?"
  choices={[
    { label: "Выгода zero-JS у Server Components применяется только к компонентам, которые остаются на сервере. Пометка всего 'use client' отгружает всё дерево в браузер — тот же бандл, что у обычной SPA, плюс сложность RSC. Фикс: Server Component по умолчанию, 'use client' только на интерактивных листьях.", correct: true },
    { label: "RSC никогда не уменьшает размер бандла; это маркетинговое заявление.", misconception: "RSC реально отгружает ноль JS для server components — реальное приложение может упасть с ~240KB до ~60KB. Команда победила это, опт-инув каждый компонент в клиент." },
    { label: "Нужно было ещё включить streaming, чтобы получить выигрыш по бандлу.", misconception: "Streaming снижает TTFB, а не размер бандла. Выигрыш по бандлу приходит чисто от удержания неинтерактивных компонентов на сервере, чему 'use client' везде мешает." },
    { label: "Сборщик не смог tree-shake'нуть клиентские компоненты.", misconception: "Tree-shaking удаляет мёртвый код, а не реально используемые интерактивные компоненты. Каждый лист 'use client' — реальный код, который браузер обязан выполнить; поэтому бандл остался большим." },
  ]}
/>

<Quiz
  id="02-data-fetching-choice-4"
  lessonSlug="02-data-fetching"
  lang="ru"
  question="RSC-страница грузит данные на сервере, затем клиентский компонент вызывает useQuery за теми же данными при монтировании — каждая загрузка страницы делает два round-trip'а за один датасет. Какой фикс правильный?"
  choices={[
    { label: "Дегидрировать серверный query-кэш и регидрировать на клиенте: prefetchQuery на сервере, встроить dehydrate(queryClient) в HTML, обернуть дерево в HydrationBoundary. Тогда useQuery находит данные в кэше и пропускает лишний fetch.", correct: true },
    { label: "Убрать useQuery и полагаться только на RSC-fetch.", misconception: "Клиентскому компоненту нужен query для интерактивности после монтирования (фоновая ревалидация, мутации). Удаление теряет это; правильный ход — засеять его кэш, а не удалять query." },
    { label: "Поставить staleTime в 0, чтобы клиент всегда использовал свежие данные.", misconception: "staleTime=0 заставляет клиент рефетчить сразу при монтировании — это гарантирует двойной fetch, а не предотвращает его. Дегидрация — то, что убирает второй trip." },
    { label: "Добавить CDN-кэш перед API, чтобы второй fetch был дешёвым.", misconception: "Дешёвый второй fetch — всё равно второй round-trip и всё равно запрос от клиента. Ловушка dual-fetch чинится разделением уже загруженных сервером данных через гидрацию, а не кэшированием дубликата." },
  ]}
/>

<Quiz
  id="02-data-fetching-choice-5"
  lessonSlug="02-data-fetching"
  lang="ru"
  question="Поле search-as-you-type шлёт fetch на каждое нажатие. Пользователь печатает 'react', но видит результаты для 'reac'. Ни один из запросов не падает. Первопричина и лучший фикс?"
  choices={[
    { label: "Ответы пришли не по порядку: запрос 'reac' разрешился после 'react' и перезаписал UI. Фикс — AbortController (отменять in-flight запрос на каждом нажатии) или TanStack Query, который автоматически отменяет запросы устаревшего ключа при смене queryKey.", correct: true },
    { label: "Сервер вернул кэшированные результаты для неправильного запроса.", misconception: "Сервер вернул корректные результаты на каждый полученный запрос; баг на клиенте — более медленный ответ 'reac' пришёл последним и затёр более новое состояние 'react'." },
    { label: "React сбатчил обновления состояния в неправильном порядке.", misconception: "Батчинг React влияет на планирование перерендера внутри тика, а не на порядок прихода независимых сетевых ответов. Гонка между fetch'ами, а не внутри батчера React." },
    { label: "Увеличить staleTime, чтобы старые результаты не показывались.", misconception: "staleTime управляет тем, когда кэшированные данные рефетчатся, а не тем, какой из двух in-flight ответов победит. Гонку решает только отмена или версионирование queryKey." },
  ]}
/>

<Quiz
  id="02-data-fetching-choice-6"
  lessonSlug="02-data-fetching"
  lang="ru"
  question="После мутации, обновляющей товар, сервер ревалидирует свой кэш, но пользователи несколько минут видят старую цену. Server Function корректно вызвала revalidateTag. Что упустили?"
  choices={[
    { label: "Не инвалидировали кэш клиентской библиотеки (TanStack Query / SWR). У приложения Next.js 15 несколько слоёв кэша — CDN, fetch-кэш, DB-кэш, HTTP-кэш, клиентский кэш — и мутация должна инвалидировать каждый релевантный. queryClient.invalidateQueries пропустили, поэтому клиент отдавал устаревшие данные до истечения gcTime/staleTime.", correct: true },
    { label: "revalidateTag не работает в Server Functions.", misconception: "revalidateTag работает в Server Functions и сделал своё дело на сервере. Устаревшие данные жили в нетронутом клиентском query-кэше — отдельном слое." },
    { label: "TTL у CDN был слишком короткий, поэтому он переотдавал старые данные.", misconception: "Короткий TTL обновил бы раньше, а не закрепил устаревшие данные на минуты. Стойкая устарелость — это клиентский кэш, держащий старый ответ в течение собственного времени жизни." },
    { label: "DB-кэш запросов нельзя инвалидировать из приложения.", misconception: "DB/Redis-кэш инвалидируется кодом приложения и является одним из слоёв здесь, но симптом — старые данные только в браузере — указывает на клиентский кэш библиотеки, до которого revalidateTag никогда не дотягивается." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна карта critical path. Место fetch решает, сколько round-trip'ов предшествует LCP: server fetch их схлопывает, client fetch после JS их складывает. Waterfall'ы пересериализуют независимые fetch'и через каскад рендера; Promise.all и соседние RSC-компоненты ограничивают их самым медленным. RSC отгружает ноль JS только для компонентов, остающихся на сервере, а Suspense streaming покупает низкий TTFB. Клиентский кэш (SWR-семантика, single-flight, оптимистичные снимки) управляет повторными визитами и взаимодействиями, а AbortController убивает гонки. И каждая мутация должна инвалидировать каждый затронутый слой кэша — одной серверной ревалидации мало, браузер останется устаревшим.
</Recap>
