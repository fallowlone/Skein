---
concepts:
- TanStack-Query
- SWR
- stale-while-revalidate
- optimistic-update
- cache-invalidation
- single-flight
- queryKey
deepensInto:
- frontend/02-data-fetching/05-lcp-and-critical-path
- frontend/02-data-fetching/06-senior-internals
estMin: 12
lang: ru
lessonType: topic
level: middle
order: 4
prereqs:
- frontend/02-data-fetching/03-rsc-and-streaming
slug: 04-client-cache-swr
sources:
- https://tanstack.com/query/v5/docs/framework/react/guides/request-waterfalls
- https://tanstack.com/query/v5/docs/react/guides/optimistic-updates
- https://datatracker.ietf.org/doc/html/rfc5861
spiral:
- statefulness
status: ready
summary: Как TanStack Query и SWR реализуют общие кэши с stale-while-revalidate семантикой,
  optimistic updates и стратегии инвалидации кэша для post-mount интерактивного fetching.
title: 'Клиентский кэш: TanStack Query, SWR и stale-while-revalidate'
track: frontend
unit: 02-data-fetching
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import invalidationStrategies from "./infographics/invalidation-strategies.svg?raw";

<Hook>
Пользователь открывает страницу товара, уходит и возвращается через 10 секунд. С обычным `useEffect` он снова ждёт полный fetch. С TanStack Query страница мгновенно показывает кэшированный результат, пока фоновое обновление тихо проверяет наличие изменений. Те же данные, радикально разный опыт.
</Hook>

<Crux>TanStack Query / SWR: кэш по queryKey. Stale-while-revalidate — мгновенно из кэша, refetch в фоне. Повторные визиты мгновенны.</Crux>

<Explanation>

## Паттерн stale-while-revalidate

Если ты управлял состоянием fetch'а через `useState + useEffect`, ты знаешь, сколько кода нужно для загрузки, ошибок, кэширования и дедупликации. Этот урок покажет, почему библиотека справляется со всеми четырьмя задачами корректно там, где самописный код обычно ломается хотя бы на одной.

**Stale-while-revalidate** — стратегия свежести кэша из RFC 5861: отдавать кэшированные данные мгновенно даже если они могут быть устаревшими, затем делать refetch в фоне. Пользователь видит контент без ожидания; UI обновляется когда приходят свежие данные.

Оба TanStack Query и SWR реализуют это:

```typescript
// TanStack Query
const { data, isLoading } = useQuery({
  queryKey: ['product', id],
  queryFn: () => fetchProduct(id),
  staleTime: 30_000,   // данные "свежие" 30s — фоновый refetch не делается
  gcTime: 300_000,     // неиспользуемые записи кэша живут 5 мин до GC
});
```

Первый визит: `isLoading=true`, fetch запускается, данные приходят, `isLoading=false`.  
Второй визит (в пределах gcTime): `data` возвращается мгновенно из кэша. Фоновый fetch обновляет если `staleTime` истёк.

queryKey — это ключ кэша. `['product', id]` — отдельная запись кэша от `['product', otherId]`.

<FlowDiagram
  label="Stale-while-revalidate: useQuery читает кэш и возвращает кэшированные, возможно устаревшие, данные в UI немедленно; если запись старше staleTime, он также запускает фоновый re-fetch на сервер, который записывает свежий результат в кэш и обновляет UI только если данные изменились."
  hue="var(--d-frontend)"
  nodes={[
    { id: "query", label: "useQuery", sub: "по queryKey", col: 0, row: 1 },
    { id: "cache", label: "Кэш", sub: "поиск", col: 1, row: 1 },
    { id: "ui1", label: "UI", sub: "мгновенно", col: 2, row: 0 },
    { id: "server", label: "Сервер", sub: "re-fetch", col: 2, row: 2 },
    { id: "cache2", label: "Кэш", sub: "запись свежих", col: 3, row: 2 },
    { id: "ui2", label: "UI", sub: "обновить если изменилось", col: 4, row: 2 },
  ]}
  edges={[
    { from: "query", to: "cache" },
    { from: "cache", to: "ui1", label: "устаревшие сейчас" },
    { from: "cache", to: "server", label: "если устарело" },
    { from: "server", to: "cache2" },
    { from: "cache2", to: "ui2" },
  ]}
  caption="Кэшированное значение отрисовывается мгновенно (верхний путь) — пользователь не ждёт. Параллельно, если запись устарела, библиотека ревалидирует в фоне (нижний путь) и патчит UI только когда свежие данные отличаются."
/>

## Single-flight дедупликация

Когда несколько компонентов монтируются одновременно и все вызывают `useQuery(['products'])`, TanStack Query делает только **один** сетевой запрос. Все компоненты разделяют результат. Это называется **single-flight дедупликация** — она сворачивает N одинаковых in-flight запросов в один, избегая как лишнего bandwidth, так и race conditions кэша.

```typescript
// 10 компонентов <ProductCard> на странице, каждый вызывает:
useQuery({ queryKey: ['products'], queryFn: fetchProducts });
// → только ОДИН сетевой запрос
```

SWR делает то же самое. Это делает library-based кэширование радикально безопаснее, чем собственный `useState + useEffect` в каждом компоненте.

## Optimistic updates

Для действий которые должны ощущаться мгновенными — Like, Bookmark, Отметить как прочитанное — применить изменение локально до подтверждения сервером, затем откатить при ошибке.

```typescript
const mutation = useMutation({
  mutationFn: (postId: string) => likePost(postId),
  onMutate: async (postId) => {
    // 1. Отменить любые исходящие refetch'и
    await queryClient.cancelQueries({ queryKey: ['post', postId] });
    // 2. Snapshot текущего значения
    const previous = queryClient.getQueryData(['post', postId]);
    // 3. Optimistically обновить
    queryClient.setQueryData(['post', postId], (old) => ({ ...old, liked: true, likeCount: old.likeCount + 1 }));
    return { previous };
  },
  onError: (err, postId, context) => {
    // Откатить к snapshot'у при ошибке
    queryClient.setQueryData(['post', postId], context.previous);
  },
  onSettled: (postId) => {
    // Инвалидировать для получения канонического состояния сервера
    queryClient.invalidateQueries({ queryKey: ['post', postId] });
  },
});
```

Паттерн: применить ожидаемый ответ сервера локально, позволить реальному ответу заменить его. Если формы совпадают — замена no-op, пользователь не видит мерцания.

<div data-lesson-visual class="overflow-x-auto my-6 rounded-lg border border-border bg-surface-1 p-4 text-sm font-mono">
  <div class="mb-2 text-ink-2 text-xs uppercase tracking-wider">Жизненный цикл optimistic update</div>
  <div class="flex flex-col gap-2">
    <div class="flex items-center gap-2">
      <div class="rounded bg-amber-100 dark:bg-amber-900/30 px-2 py-1 text-xs">T=0</div>
      <span>Пользователь кликает Like → onMutate: snapshot + setQueryData(liked:true) → UI обновляется мгновенно</span>
    </div>
    <div class="flex items-center gap-2">
      <div class="rounded bg-blue-100 dark:bg-blue-900/30 px-2 py-1 text-xs">T=200ms</div>
      <span>Сервер отвечает OK → onSettled: инвалидировать → фоновый refetch подтверждает</span>
    </div>
    <div class="flex items-center gap-2">
      <div class="rounded bg-red-100 dark:bg-red-900/30 px-2 py-1 text-xs">При ошибке</div>
      <span>Сервер возвращает 429 → onError: setQueryData(snapshot) → UI откатывается</span>
    </div>
  </div>
</div>

## Стратегии инвалидации кэша

Инвалидация кэша — одна из двух общеизвестно сложных задач в разработке. Перед мутацией спроси себя: какие ещё запросы в кэше содержат старую версию данных? Ответ определяет, какой из трёх подходов выбрать.

Когда мутация завершается успешно, устаревшие запросы должны обновиться. Три подхода:

**invalidateQueries** — пометить запросы устаревшими, запустить refetch при следующем observer'е. Самый безопасный; гарантирует канонические данные сервера.

```typescript
queryClient.invalidateQueries({ queryKey: ['posts', userId] });
```

**setQueryData** — записать ответ мутации прямо в кэш. Самый дешёвый; лишних сетевых вызовов нет. Верно только когда ответ мутации включает полное новое состояние.

```typescript
queryClient.setQueryData(['post', id], serverResponse);
```

**refetchQueries** — принудительный немедленный fetch. Используется когда нужны свежие данные прямо сейчас и нельзя доверять ответу мутации.

Большинство production кода использует оба: `setQueryData` для непосредственно изменённой сущности (если API её возвращает), `invalidateQueries` для listing запросов которые на неё ссылаются. Вместе эти два вызова означают, что пользователь видит мутацию мгновенно — и на детальной странице, и в списке, без лишнего round-trip'а.

<Infographic
  svg={invalidationStrategies}
  label="Вертикальный список трёх стратегий инвалидации кэша после мутации, от самой дешёвой к самой нетерпеливой: setQueryData пишет ответ прямо в кэш без лишнего сетевого запроса, но только если ответ полный; invalidateQueries помечает запросы устаревшими и делает refetch у следующего наблюдателя ради гарантированно канонических данных ценой одного фонового запроса; refetchQueries принудительно запрашивает заново, когда ответу мутации доверять нельзя."
  caption="Выбор по доверию к ответу мутации: записать напрямую (setQueryData), пометить-и-ревалидировать (invalidateQueries) или принудительно запросить заново (refetchQueries) — цена растёт по мере падения доверия."
  hue="var(--d-infra)"
/>

<Inset kind="why" lang="ru">
TanStack Query v5 переименовал `cacheTime` в `gcTime` чтобы прояснить что он контролирует — не как долго данные кэшируются (это `staleTime`), а как долго неиспользуемые записи живут до garbage collection. Если `staleTime` равен 0, а `gcTime` — 300s, запись кэша живёт 5 минут, но при каждом обращении делается refetch. Если `staleTime` — 60s, refetch не делается 60 секунд после последнего успешного fetch'а.
</Inset>

<NumbersCard
  id="client-cache-numbers"
  title="TanStack Query vs SWR"
  stats={[
    { label: "TanStack Query v5 размер", value: "~16 KB gzipped" },
    { label: "SWR размер", value: "~5 KB gzipped" },
    { label: "Default staleTime (оба)", value: "0ms (всегда устаревшие)" },
    { label: "Default gcTime (TanStack Query)", value: "5 минут" },
    { label: "RFC 5861 (stale-while-revalidate)", value: "HTTP аналог" },
  ]}
/>

<Quiz
  id="04-q1"
  pieceSlug="04-client-cache-swr"
  lang="ru"
  question="useQuery возвращает isLoading=false и data. Пользователь уходит со страницы и возвращается. Каков дефолтный UX?"
  choices={[
    { label: "Спиннер показывается, потом данные заново fetch'атся с нуля", misconception: "Спиннер появляется только когда кэш пуст. Повторный визит попадает в кэш." },
    { label: "Кэшированные данные показываются мгновенно; фоновый refetch может обновить если staleTime истёк", correct: true },
    { label: "Пустая страница — useQuery всегда делает refetch при монтировании", misconception: "Дефолтное поведение возвращает кэшированные данные немедленно с опциональным фоновым refetch'ем." },
    { label: "Запрос выдаёт ошибку потому что данные устарели", misconception: "Устаревшие данные отдаются; staleness запускает фоновый refetch, а не ошибку." },
  ]}
/>

<Quiz
  id="04-q2"
  pieceSlug="04-client-cache-swr"
  lang="ru"
  question="Почему TanStack Query реализует single-flight дедупликацию?"
  choices={[
    { label: "HTTP/2 требует этого", misconception: "HTTP/2 мультиплексирует стримы; он не дедуплицирует запросы одного ключа на уровне библиотеки." },
    { label: "Несколько компонентов монтирующихся одновременно часто хотят одни данные — single-flight сворачивает N запросов с одним queryKey в один сетевой вызов", correct: true },
    { label: "Для уменьшения использования памяти", misconception: "Экономия памяти незначительна; главная цель — эффективность сети и консистентность." },
    { label: "Браузеры ограничивают параллельные fetch'и", misconception: "Браузеры ограничивают соединения по origin; single-flight — семантическая дедупликация на уровне библиотеки." },
  ]}
/>

<Quiz
  id="04-q3"
  pieceSlug="04-client-cache-swr"
  lang="ru"
  question="Optimistic Like update работает, но like откатывается через мгновение. Наиболее вероятная причина?"
  choices={[
    { label: "Сервер отклонил запрос", misconception: "Отклонение запускает onError откат, который вернул бы к pre-liked состоянию, а не мерцание." },
    { label: "onMutate установил liked:true, но забыл инкрементировать likeCount — ответ сервера заменил частичное optimistic состояние корректно-сформированным объектом", correct: true },
    { label: "staleTime слишком низкий", misconception: "staleTime контролирует тайминг refetch'а, не корректность optimistic update'ов." },
    { label: "React группировал обновление состояния", misconception: "Группировка влияет на ре-рендеры, а не на порядок вызова mutation callback'ов." },
  ]}
/>

</Explanation>

<KeyTakeaway>TanStack Query и SWR кэшируют результаты запросов по ключу с stale-while-revalidate: мгновенный возврат из кэша, фоновое обновление. Single-flight дедупликация безопасна для вызова одного запроса из N компонентов. Optimistic updates требуют полных snapshot'ов состояния в onMutate для избежания багов отката.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-retrieval"
  lang="ru"
  questions={[
    { q: "Что контролирует staleTime в TanStack Query?", a: "Как долго fetched данные считаются свежими. Пока свежие — фоновый refetch при монтировании не делается. После истечения staleTime следующее монтирование компонента запускает фоновый refetch. По умолчанию 0 — данные мгновенно устаревают после fetch'а." },
    { q: "Каков канонический паттерн optimistic update в TanStack Query?", a: "onMutate: отменить исходящие запросы, сделать snapshot через getQueryData, применить полное optimistic состояние через setQueryData, вернуть snapshot. onError: восстановить snapshot. onSettled: инвалидировать для получения канонического состояния сервера. Optimistic форма должна совпадать с формой ответа сервера для избежания мерцания." },
    { q: "Когда использовать invalidateQueries vs setQueryData после мутации?", a: "setQueryData когда ответ мутации включает полное новое состояние сущности — записывает прямо в кэш без лишнего fetch'а. invalidateQueries для listing/collection запросов которые ссылаются на изменённую сущность — помечает их устаревшими и запускает refetch. Большинство production кода использует оба: setQueryData для сущности, invalidateQueries для её списков." },
  ]}
/>

<Recap lang="ru">
TanStack Query и SWR ведут общий кэш по массивам queryKey с stale-while-revalidate семантикой: staleTime контролирует свежесть (по умолчанию 0), gcTime (время жизни до сборки мусора, по умолчанию 5 мин) контролирует время жизни записи. Несколько компонентов вызывающих один query key получают один сетевой запрос через single-flight дедупликацию. Optimistic updates применяют ожидаемый результат мутации локально через setQueryData со snapshot для отката; snapshot в onMutate должен повторять полную форму ответа сервера или UI мерцает когда приходят реальные данные. После мутаций invalidateQueries обеспечивает актуальность listing кэшей; setQueryData обрабатывает прямые записи кэша сущностей когда ответ мутации доступен. Теперь, когда optimistic Like откатывается через секунду, — ты знаешь куда смотреть первым: snapshot в onMutate неполный.
</Recap>
