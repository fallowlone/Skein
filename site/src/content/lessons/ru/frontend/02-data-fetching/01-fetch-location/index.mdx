---
slug: 01-fetch-location
lang: ru
track: frontend
unit: 02-data-fetching
order: 1
title: "Где происходит data fetching — и почему это решает LCP"
summary: "Четыре места, где React-приложение может делать fetch, что каждое стоит в round-trip'ах, и как выбирать между ними."
estMin: 8
status: ready
lessonType: topic
level: junior
prereqs: []
deepensInto:
  - frontend/02-data-fetching/02-waterfalls-and-parallelism
  - frontend/02-data-fetching/03-rsc-and-streaming
spiral: ["statefulness", "encapsulation"]
concepts: ["data-fetching", "LCP", "SSR", "CSR", "SSG", "streaming", "round-trip"]
sources:
  - https://web.dev/articles/optimize-lcp
  - https://react.dev/reference/rsc/server-components
  - https://martinfowler.com/articles/data-fetch-spa.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";

<Hook>
Один блог загружается за 400ms, другой — за 2100ms. Одинаковый CDN, одинаковая база данных, одинаковая версия React. Единственное отличие: где происходит запрос данных.
</Hook>

<Crux>Server fetch + stream = LCP меньше 1s. Client fetch после JS = LCP больше 3s. Место fetch'а определяет количество round-trip'ов.</Crux>

<Explanation>

## Critical path от клика до контента

Каждая загрузка страницы имеет critical path — цепочку последовательных round-trip'ов между браузером и сервером, которые должны завершиться до того, как основной контент станет виден. **Largest Contentful Paint (LCP)** измеряет, когда этот контент появляется. Каждый лишний round-trip добавляет 50–200ms на быстром соединении, 500ms+ на мобильном.

С чистым SPA цепочка выглядит так:

1. Браузер запрашивает `index.html` с CDN (~50ms)
2. Скачивается и парсится JS bundle (~400–600ms)
3. React монтирует дерево компонентов
4. `useEffect` запускает fetch на `/api/data` (~200ms)
5. React перерисовывается с данными, LCP срабатывает

Итого: легко 800–2000ms на быстром соединении, 3–5s на медленном.

<div data-lesson-visual class="overflow-x-auto my-6 rounded-lg border border-border bg-surface-1 p-4 text-sm font-mono">
  <div class="mb-2 text-ink-2 text-xs uppercase tracking-wider">Client SPA — 4 последовательных trip'а до LCP</div>
  <div class="flex flex-col gap-1">
    <div class="flex items-center gap-2">
      <span class="w-6 text-center rounded bg-red-100 dark:bg-red-900/40 text-red-700 dark:text-red-300 text-xs font-bold">1</span>
      <span>GET /index.html → CDN → 50ms</span>
    </div>
    <div class="flex items-center gap-2">
      <span class="w-6 text-center rounded bg-red-100 dark:bg-red-900/40 text-red-700 dark:text-red-300 text-xs font-bold">2</span>
      <span>GET /bundle.js → CDN → 600ms (parse + eval)</span>
    </div>
    <div class="flex items-center gap-2">
      <span class="w-6 text-center rounded bg-red-100 dark:bg-red-900/40 text-red-700 dark:text-red-300 text-xs font-bold">3</span>
      <span>React.mount() → CPU → 100ms</span>
    </div>
    <div class="flex items-center gap-2">
      <span class="w-6 text-center rounded bg-red-100 dark:bg-red-900/40 text-red-700 dark:text-red-300 text-xs font-bold">4</span>
      <span>GET /api/data → origin server → 200ms</span>
    </div>
    <div class="mt-2 font-bold text-red-700 dark:text-red-300">LCP срабатывает на ~950ms (быстрая сеть). Мобильный: 3–5s.</div>
  </div>
</div>

## Четыре локации

Современные фреймворки поддерживают четыре места для fetch'а:

**Время сборки (SSG — static generation).** Данные запекаются в HTML при сборке. Нулевая стоимость fetch'а на запрос. Данные устаревают до следующей сборки. Подходит для контента, который меняется реже раза в час: посты блога, маркетинговые страницы, документация.

**Сервер во время запроса (SSR или Server Components).** Fetch происходит на сервере при генерации HTML-ответа. Браузер получает HTML с данными внутри. Один эффективный round-trip. Next.js RSC async components, Remix loaders и `getServerSideProps` — все используют этот паттерн.

**Стриминг на сервере.** Сервер отправляет HTML чанками до того, как все данные готовы. Шелл лэйаута прилетает за ~100ms; медленные секции стримятся по мере готовности данных. Каждый React Suspense boundary стримится независимо.

**Клиент после монтирования (CSR).** `useEffect`, `useQuery`, `useSWR`. Страница монтируется пустой; данные приходят после выполнения JS. Необходим для пользовательских интерактивных потоков — фильтры, real-time обновления, всё что зависит от состояния пользователя. Но неверный выбор для LCP-элемента.

## Метафора ресторана

Client fetch — это как прийти в ресторан к пустому столу, две минуты изучать меню, потом ждать еду. Server fetch — это позвонить заранее: хост принимает заказ, вы садитесь к уже накрытому столу. Стриминг — это когда закуска приносится мгновенно, пока основное блюдо ещё готовится. Общее время кухни похоже, но воспринимаемое ожидание радикально короче.

<Quiz
  id="01-q1"
  pieceSlug="01-fetch-location"
  lang="ru"
  question="Почему client-side data fetching медленнее при первой загрузке страницы, чем server-side?"
  choices={[
    { label: "Клиентские CPU медленнее серверных", misconception: "Разница в скорости — это про round-trip'ы, не про CPU: даже быстрые CPU не могут избежать сетевой задержки." },
    { label: "Client fetch может начаться только после скачивания, парсинга и выполнения JS bundle'ов — это 2–4 последовательных round-trip'а по сравнению с server-side рендерингом", correct: true },
    { label: "JavaScript медленнее, чем HTML", misconception: "Оба выполняются с современной скоростью; разница в порядке операций." },
    { label: "Браузеры ограничивают клиентские fetch'и при первой загрузке", misconception: "Браузеры не ограничивают data fetch'и при первой загрузке." },
  ]}
/>

<Quiz
  id="01-q2"
  pieceSlug="01-fetch-location"
  lang="ru"
  question="Блог публикует 5 новых постов в день. Какая локация fetch'а подходит лучше всего?"
  choices={[
    { label: "useEffect (client fetch) в каждом браузере посетителя", misconception: "Это добавляет полный client-fetch round-trip к каждому просмотру страницы." },
    { label: "SSG с Incremental Static Regeneration — пересборка статического HTML периодически, раздача с CDN", correct: true },
    { label: "SSR на каждый запрос", misconception: "SSR верен, но дорог для редко меняющегося контента — CDN-кешированная SSG страница быстрее и дешевле." },
    { label: "WebSockets", misconception: "WebSockets — для двунаправленных real-time стримов, не для статической раздачи контента." },
  ]}
/>

<DragOrder
  id="01-drag-1"
  pieceSlug="01-fetch-location"
  lang="ru"
  prompt="Упорядочи шаги сети для типичного client-rendered React приложения, загружающего страницу товара:"
  items={[
    "Пользователь кликает по ссылке",
    "Браузер запрашивает index.html с CDN",
    "Браузер скачивает и парсит JS bundle",
    "JS выполняется, React монтирует приложение",
    "useEffect срабатывает, вызывает fetch('/api/product/123')",
    "Сервер отвечает JSON с данными товара",
    "React перерисовывается с данными, LCP срабатывает",
  ]}
/>

</Explanation>

<KeyTakeaway>Место fetch'а определяет количество round-trip'ов: server fetch сводит critical path к одному эффективному trip'у; client fetch после JS добавляет 2–4 последовательных trip'а. LCP-элемент должен получать данные на сервере; интерактивное состояние — на клиенте.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-retrieval"
  lang="ru"
  questions={[
    { q: "Назови четыре локации data fetching от build-time до client-only.", a: "Build time (SSG), сервер во время запроса (SSR / Server Components), стриминг на сервере (Suspense), клиент после монтирования (useEffect / useQuery)." },
    { q: "Почему SSG имеет нулевую стоимость fetch'а на запрос?", a: "Данные запекаются в статический HTML при сборке. Запросы попадают на CDN и возвращают предварительно отрендеренный HTML без round-trip'а к origin серверу." },
    { q: "Что такое LCP и почему локация fetch'а влияет на него?", a: "Largest Contentful Paint — время до того, как основной контент-элемент страницы становится виден. Client fetching добавляет загрузку JS + монтирование + fetch к critical path; server fetching убирает эти шаги, LCP ограничен только временем прихода HTML." },
  ]}
/>

<Recap lang="ru">
Critical path страницы — это цепочка последовательных round-trip'ов между кликом и видимым контентом. Client-rendered SPA'ы накапливают 4+ trip'а до срабатывания LCP; server-side рендеринг сворачивает это до одного эффективного trip'а. Стриминг идёт ещё дальше: шелл лэйаута приходит за 100ms, каждая секция стримится независимо. Правило: данные LCP-элемента — на сервере, пользовательское интерактивное состояние — на клиенте. Для в основном статического контента (блоги, документация) SSG с CDN-кешированием даёт почти нулевой TTFB при фактически нулевой серверной нагрузке.
</Recap>
