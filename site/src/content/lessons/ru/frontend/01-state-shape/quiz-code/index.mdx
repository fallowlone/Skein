---
concepts:
- derived-state
- normalization
- state-colocation
- single-source-of-truth
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://react.dev/learn/choosing-the-state-structure
- https://redux.js.org/tutorials/essentials/part-6-performance-normalization
status: ready
summary: "Читай реальные сниппеты React/TS, предсказывай баг формы и выбирай фикс с наибольшим рычагом — derived state, normalized vs вложенно и colocation."
title: 'Форма состояния: чтение кода'
track: frontend
unit: 01-state-shape
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги формы прячутся в коде, который компилируется и рендерится нормально — пока не сработает путь обновления, который он забыл покрыть. Прочитай каждый сниппет, найди второй источник истины или неправильно размещённый state, затем выбери фикс, который сеньор делает первым.
</Hook>

<Goal lang="ru">
Отработай цикл, который ты прогоняешь, ревьюя код state: прочитай компонент, заметь значение, которое надо выводить, или данные с неправильной для их паттерна доступа формой, и тянись к структурному фиксу до любой библиотеки.
</Goal>

## Сниппет 1 — хранимое выводимое значение

```tsx
function Cart({ items }: { items: Item[] }) {
  const [total, setTotal] = useState(0);

  useEffect(() => {
    setTotal(items.reduce((s, i) => s + i.price * i.qty, 0));
  }, [items]);

  return <Badge>{total}</Badge>;
}
```

<Quiz
  id="01-state-shape-code-1"
  lessonSlug="01-state-shape"
  lang="ru"
  question="В чём здесь баг формы и фикс уровня сеньора?"
  choices={[
    { label: "total выводится из items, но хранится в state и синхронизируется через эффект — второй источник истины плюс лишний рендер. Вычисляй в рендере: const total = items.reduce(...). Ни state, ни эффекта, никогда не устаревает.", correct: true },
    { label: "reduce надо обернуть в useCallback, чтобы не пересоздавать его.", misconception: "useCallback мемоизирует идентичность функции; он не решает реальную проблему — хранение значения, которое можно вычислить. reduce место в рендере, а не за state." },
    { label: "items тоже надо положить в state, чтобы компонент владел своими данными.", misconception: "items — это проп; копирование его в локальный state добавит ещё один источник истины, который разойдётся с родителем. Фикс — меньше хранимых значений, а не больше." },
    { label: "Всё в порядке; синхронизация derived state через эффект — стандартный паттерн.", misconception: "Синхронизация derived state эффектом — задокументированный антипаттерн: рендер сначала с устаревшим значением, затем ещё раз после эффекта. Вывод в рендере убирает и окно устаревания, и лишний рендер." },
  ]}
/>

## Сниппет 2 — вложенно vs normalized

```ts
// state.users[].posts[] — user встроен и в поле author каждого post
function renameUser(state, id: string, name: string) {
  return {
    ...state,
    users: state.users.map(u => u.id === id ? { ...u, name } : u),
    // posts по-прежнему несут post.author = {...старый user...}
  };
}
```

<Quiz
  id="01-state-shape-code-2"
  lessonSlug="01-state-shape"
  lang="ru"
  question="Переименование обновляет массив users, но список постов всё ещё показывает старое имя. Какое изменение формы — настоящий фикс?"
  choices={[
    { label: "Каждый post встраивает копию своего автора, поэтому переименование в массиве users оставляет каждый post.author устаревшим. Нормализуй: храни users один раз в users.byId, а posts держат authorId, джойня byId[authorId] при чтении — тогда переименование это одна запись.", correct: true },
    { label: "Тоже пройтись по posts и пропатчить каждый post.author новым именем в renameUser.", misconception: "Это патчит симптом и сохраняет дублирование — каждому будущему меняющемуся полю нужен тот же fan-out, и один пропущенный путь снова разойдётся. Удаление встроенных копий — устойчивый фикс." },
    { label: "Сделать deep-freeze state, чтобы старые объекты author нельзя было прочитать.", misconception: "Заморозка предотвращает мутацию, а не устаревание — замороженный post.author всё ещё держит старое имя. Проблема в дублированных данных, что устраняет normalization." },
    { label: "Перенести users в React Query, чтобы переименование делало рефетч.", misconception: "Это мутируемые на клиенте сущности, а не server cache для рефетча. Библиотека кэша не чинит клиентскую форму, дублирующую одного user по многим постам; это делает byId-словарь." },
  ]}
/>

## Сниппет 3 — поднятый, слишком широкий state

```tsx
// store приложения
const useStore = create((set) => ({
  mouseX: 0, mouseY: 0,
  setMouse: (x, y) => set({ mouseX: x, mouseY: y }),
}));

function Dashboard() {
  const { mouseX, mouseY } = useStore();   // подписка на весь store
  return <ExpensiveCharts /* ререндер на каждом mousemove */ />;
}
```

<Quiz
  id="01-state-shape-code-3"
  lessonSlug="01-state-shape"
  lang="ru"
  question="Почему движение мыши роняет фреймрейт этого дашборда и какой фикс?"
  choices={[
    { label: "Высокочастотная позиция мыши поднята в глобальный store, а Dashboard подписан на весь store, поэтому каждый mousemove ререндерит ExpensiveCharts. Колоцируй state мыши в маленьком компоненте, которому он нужен (или используй селектор, подписанный только на его срез).", correct: true },
    { label: "Zustand медленный с числами; перейди на Redux для примитивного state.", misconception: "Библиотека store — не узкое место; узкое место — слишком широкая подписка. Компонент, читающий весь store, ререндерится на любое изменение; фикс — более узкая область, а не другая библиотека." },
    { label: "ExpensiveCharts нужен React.memo, и проблема уйдёт.", misconception: "memo помогает, только если пропсы стабильны, но сам Dashboard ререндерится на каждом mousemove, потому что подписан на меняющийся store. Сначала почини область подписки; memo — пластырь." },
    { label: "mouseX/mouseY надо положить в URL, чтобы они сохранялись.", misconception: "Позиция мыши — эфемерное, покадровое UI-состояние, никогда не шарящееся view-состояние. URL бы забил историю. Это надо колоцировать в одном компоненте, который его читает." },
  ]}
/>

## Сниппет 4 — серверные данные как клиентский state

```tsx
function Orders() {
  const [orders, setOrders] = useState<Order[]>([]);
  useEffect(() => { fetch('/api/orders').then(r => r.json()).then(setOrders); }, []);
  // также рендерится в соседнем <OrdersSummary/> со своим идентичным fetch
}
```

<Quiz
  id="01-state-shape-code-4"
  lessonSlug="01-state-shape"
  lang="ru"
  question="Два компонента прогоняют один и тот же паттерн fetch-в-useState. Какой набор проблем создаёт форма и какой фикс?"
  choices={[
    { label: "Полученные данные смоделированы как клиентский state, отсюда дублирующие запросы, нет дедупа, нет общего кэша, нет свежести/рефетча, и вью расходятся после мутации. Перенеси в библиотеку server cache с ключом по запросу, чтобы оба компонента делили один кэшированный, инвалидируемый результат.", correct: true },
    { label: "Единственная проблема — отсутствующий флаг загрузки; добавь isLoading в каждый.", misconception: "Флаг загрузки — наименьшая часть. Проблема формы в том, что server cache трактуется как клиентский state, что также стоит дедупа, общего кэша и инвалидации — всё это библиотека кэша даёт сразу." },
    { label: "Поднять orders в глобальный store, чтобы оба компонента читали одну копию.", misconception: "Глобальный store дедуплицирует копию в памяти, но fetch, свежесть, рефетч и инвалидацию ты всё равно пишешь руками. Данные — server cache; библиотека кэша с ключом по запросу делает всё это." },
    { label: "Всё нормально, пока два fetch бьют в один эндпоинт.", misconception: "Идентичные эндпоинты всё равно означают два сетевых запроса, две копии кэша и независимую свежесть — ровно тот drift и расход, что устраняет библиотека server cache." },
  ]}
/>

<Recap lang="ru">
Каждый баг формы выше читается прямо из кода: эффект, синхронизирующий значение, которое можно вывести (вычисляй в рендере), авторы, встроенные в посты, так что переименование устаревает (нормализуй в byId + join), высокочастотный state, поднятый в глобальный store, ререндерящий весь мир (колоцируй или возьми срез селектором), и fetch, затянутый в useState, который должен быть server cache (используй библиотеку кэша с ключом по запросу). Прочитай компонент, найди второй источник истины или state, размещённый не там, где его читатели, и почини форму до того, как тянуться к библиотеке.
</Recap>
