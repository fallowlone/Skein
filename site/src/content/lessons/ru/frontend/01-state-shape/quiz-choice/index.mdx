---
concepts:
- derived-state
- server-cache-state
- single-source-of-truth
- state-colocation
- normalization
- url-state
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://react.dev/learn/choosing-the-state-structure
- https://kentcdodds.com/blog/state-colocation-will-make-your-react-app-faster
- https://redux.js.org/tutorials/essentials/part-6-performance-normalization
status: ready
summary: "Тест с множественным выбором по всему юниту про форму state — derived state, server cache vs клиентский state, colocation, URL-состояние и трейдоффы normalized."
title: 'Форма состояния: тест с выбором ответа'
track: frontend
unit: 01-state-shape
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, проходящих через весь юнит. Каждый отражает решение, которое ты принимаешь до того, как тянешься к любой библиотеке state — не определение, чтобы пересказать, а форму, которую надо выбрать под реальные ограничения.
</Hook>

<Goal lang="ru">
Убедись, что связываешь стержень юнита: выводи выводимое, отделяй server cache от клиентского state, колоцируй у нижнего читателя, шарящееся view-состояние выноси в URL, а реляционные данные нормализуй только когда их мутируешь.
</Goal>

<Quiz
  id="01-state-shape-choice-1"
  lessonSlug="01-state-shape"
  lang="ru"
  question="Список товаров получают из API, и его читают три соседних компонента на одном экране. Какая форма правильная?"
  choices={[
    { label: "Server cache в React Query / SWR с ключом по запросу — один fetch, дедуплицированный для всех трёх читателей, со свежестью и инвалидацией из коробки.", correct: true },
    { label: "Отдельные useState + fetch в каждом из трёх компонентов.", misconception: "Три копии означают три запроса и три источника истины, которые разойдутся, как только один сделает рефетч или прилетит мутация. Данные — это одна сущность, принадлежащая серверу." },
    { label: "Глобальный слайс Redux, который ты заполняешь через setProducts после fetch.", misconception: "Обычный слайс моделирует полученные данные как клиентский state, так что свежесть, дедуп, рефетч и инвалидацию ты пишешь руками — обычно с багами. Именно это устраняет библиотека server cache." },
    { label: "URL, чтобы все три компонента читали его из query-строки.", misconception: "URL — для шарящегося view-состояния (фильтры, id, пагинация), а не для кэша полученного payload. Список объектов-товаров не место в адресной строке." },
  ]}
/>

<Quiz
  id="01-state-shape-choice-2"
  lessonSlug="01-state-shape"
  lang="ru"
  question="Бейдж-счётчик показывает '127', а в списке 9 строк; count лежит в useState рядом с массивом results. В чём корневая причина и фикс уровня сеньора?"
  choices={[
    { label: "count — второй источник истины для results.length. Какой-то путь обновления (обработчик ошибки, оптимистичное обновление, патч по websocket) изменил results, не изменив count, и они разошлись. Фикс: не хранить count — выводить results.length в рендере.", correct: true },
    { label: "Устаревший рендер — бейджу нужен проп key, чтобы форсировать ререндер.", misconception: "Ререндер не поможет: само хранимое значение count неверно. Дефект в том, что два значения, означающие один факт, хранятся раздельно, и один путь обновления пропустил копию." },
    { label: "Обернуть count в useMemo, чтобы пересчитывался при изменении results.", misconception: "useMemo — инструмент производительности, а не корректности. Мемоизированная копия остаётся вторым источником истины — drift убирает именно вывод в рендере." },
    { label: "Перенести count в глобальный store, чтобы все компоненты были согласованы.", misconception: "Глобализация выводимого значения не чинит drift — она его размазывает и добавляет сверху радиус ререндеров. Единственный фикс — не хранить то, что можно вычислить." },
  ]}
/>

<Quiz
  id="01-state-shape-choice-3"
  lessonSlug="01-state-shape"
  lang="ru"
  question="Булево open/closed для тултипа лежит в корневом store приложения «чтобы было доступно везде». Почему это неверная форма?"
  choices={[
    { label: "open/closed читает только поддерево тултипа, значит оно должно жить у нижнего общего предка своих читателей. Подъём в корень подписывает всё приложение и ререндерит несвязанные компоненты на каждом переключении.", correct: true },
    { label: "Булевы слишком малы, чтобы занимать запись в store; в глобальном state место только объектам.", misconception: "Критерий не размер, а кто читает. Большой объект, читаемый одним поддеревом, всё равно колоцируется; крошечное булево, читаемое всем приложением, может оправдать глобальный state. У флага тултипа ровно один читатель." },
    { label: "Глобальные store не умеют хранить булевы без кастомного редьюсера.", misconception: "Любой store отлично хранит булевы. Проблема не в типе, а в размещении state далеко над единственным читателем, что раздувает радиус ререндеров." },
    { label: "Всё нормально — централизация state всегда упрощает рассуждения о приложении.", misconception: "«Lift state up» применяют слишком широко. Подъём в корень — это как флаг тултипа начинает ререндерить дашборд. Colocation означает спуск state вниз к нижнему читателю." },
  ]}
/>

<Quiz
  id="01-state-shape-choice-4"
  lessonSlug="01-state-shape"
  lang="ru"
  question="Фильтры и id открытой детали лежат только в useState. Скопированная ссылка открывает у коллеги пустой дефолтный вид, а рефреш тоже всё сбрасывает. Какую форму упустили?"
  choices={[
    { label: "Это шарящееся, кладущееся в закладки view-состояние, поэтому его источник истины — URL (query-строка / параметры маршрута). Хранение только в useState означает: рефреш теряет состояние, а скопированная ссылка мертва.", correct: true },
    { label: "Сохранить в localStorage, чтобы пережить рефреш.", misconception: "localStorage переживает рефреш, но он на устройство — скопированная ссылка всё равно откроет дефолт у коллеги. Именно URL-состояние делает вид шарящимся и восстановимым между машинами." },
    { label: "Положить в библиотеку server cache, чтобы их получали при загрузке.", misconception: "Фильтры и открытый id — клиентское view-состояние, а не серверные данные. Библиотека кэша с ключом по запросу не делает вид шарящимся; это делает URL." },
    { label: "Поднять в глобальный store, чтобы их читал каждый маршрут.", misconception: "Глобальный store всё равно в памяти и теряется на рефреше, и в скопированной ссылке его нет. Определяющее свойство здесь — шарящесть, которую даёт только URL." },
  ]}
/>

<Quiz
  id="01-state-shape-choice-5"
  lessonSlug="01-state-shape"
  lang="ru"
  question="Клиентский редактор держит threads, posts и users вложенно, как вернул API, и пользователей переименовывают постоянно. После переименования часть вью показывает старое имя. Почему и какая форма это чинит?"
  choices={[
    { label: "Вложенная форма копирует каждого user в каждый post, которого он касается, поэтому переименование должно переписать каждую встроенную копию — пропусти одну и вью разойдутся. Нормализуй в плоский byId-словарь на сущность, чтобы переименование было одной O(1)-записью, которую видит каждый join.", correct: true },
    { label: "Мутация переименования забыла вызвать ререндер; добавь forceUpdate.", misconception: "Вью ререндерились — они читали устаревшую встроенную копию, которую так и не обновили. Дефект в том, что форма дублирует одного user по всему дереву, а не в пропущенном рендере." },
    { label: "Нормализуй всё, включая read-mostly серверные данные, в byId-словари.", misconception: "Normalization нужна для часто мутируемого реляционного клиентского state. Read-mostly серверные данные должны остаться сырым ответом в библиотеке кэша; нормализовать их — это лишняя церемония без выгоды." },
    { label: "Перейти на библиотеку глубокого merge, чтобы spread-обновления доходили до каждой копии.", misconception: "Глубокий merge каждой встроенной копии плохо переизобретает normalization и остаётся хрупким. Хранение каждой сущности один раз в byId-словаре убирает дублирование, вызывающее drift." },
  ]}
/>

<Quiz
  id="01-state-shape-choice-6"
  lessonSlug="01-state-shape"
  lang="ru"
  question="Команда хранит selectedUser как целый объект user в state. После правки в другом месте, обновившей этого user, выбор всё ещё показывает старые поля. В чём урок про форму?"
  choices={[
    { label: "Объект выводится из selectedId плюс кэша, поэтому хранение целого объекта создало устаревший снимок. Храни id как настоящий state и делай join byId[selectedId] при чтении, чтобы выбор всегда отражал актуальные данные.", correct: true },
    { label: "Хранить объект нормально; обработчику правки просто нужно ещё обновлять selectedUser.", misconception: "Это снова ловушка derived state: теперь каждый путь правки обязан помнить про перезапись снимка. Хранение id и join при чтении убирают этот инвариант целиком." },
    { label: "selectedId — слишком мало информации; объект нужно держать, чтобы UI отрисовался.", misconception: "UI делает join id по кэшу (byId[selectedId]), чтобы получить актуальные поля. id — минимальный настоящий state; объект — выводимый вид от него." },
    { label: "Делать deep-clone объекта при выборе, чтобы его не мутировали под тобой.", misconception: "Замороженный клон — это ровно проблема устаревшего снимка: он никогда не отразит поздние правки. Фикс — хранить id и переджойнивать, а не захватывать копию." },
  ]}
/>

<Recap lang="ru">
Стержень юнита — одно дерево решений: если значение выводимо (count, выбранный объект), не храни его — вычисляй в рендере или джойни по id. Если это state, спроси, чьё оно — полученные с сервера данные это cache (React Query/SWR), шарящееся клиентское view-состояние идёт в URL, а остальное колоцируется у нижнего общего предка читателей, чтобы радиус ререндеров был мал. Реляционные клиентские данные нормализуй в byId-словарь только когда их мутируешь. Теперь, когда встретишь любой из этих дистракторов в реальном PR — выводимое значение рядом со своим источником, полученные данные в Redux-слайсе, флаг с одним читателем в корне, снимок объекта вместо join по id — ты знаешь имя ошибки формы, сбой, который она вызовет, и структурный фикс, который её убирает.
</Recap>
