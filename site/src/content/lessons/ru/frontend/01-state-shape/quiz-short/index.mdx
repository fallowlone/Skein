---
concepts:
- derived-state
- server-cache-state
- single-source-of-truth
- state-colocation
- url-state
- normalization
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://react.dev/learn/choosing-the-state-structure
- https://kentcdodds.com/blog/state-colocation-will-make-your-react-app-faster
- https://redux.js.org/tutorials/essentials/part-6-performance-normalization
status: ready
summary: "Промпты на свободное припоминание по всему юниту про форму state. Сначала ответь своими словами, затем раскрой модельный ответ и сравни."
title: 'Форма состояния: свободное припоминание'
track: frontend
unit: 01-state-shape
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт скажи или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет решения про форму.
</Hook>

<Goal lang="ru">
Восстанови ключевые решения юнита — выводимое vs хранимое, server cache vs клиентский state, colocation, URL как источник истины и normalization — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:visible
  id="01-state-shape-short-retrieval"
  lang="ru"
  questions={[
    { q: "Какой тест определяет, является значение state или выводимым (derived), и почему хранение выводимого значения вызывает баги?", a: "Тест: если значение можно вычислить из существующего state в рендере — это не state, это derived. count — это results.length; выбранный объект — byId[selectedId]; итог корзины — items.reduce(...). Хранение выводимого создаёт второй источник истины, и теперь каждый путь кода, меняющий источник, обязан переписать и копию: happy path, обработчик ошибки, оптимистичное обновление, патч по websocket. Фреймворк не обеспечит этот инвариант за тебя, поэтому один пропущенный путь и значения расходятся — бейдж говорит 127, а список показывает 9. Фикс — выводить в рендере, где значение не может разойтись само с собой. useMemo — лишь оптимизация производительности дорогого вычисления; мемоизированная копия всё ещё второй источник истины." },
    { q: "Почему вопрос «server cache vs клиентский state» — самый важный про форму и как он меняет твой инструментарий?", a: "У полученных данных есть свойства, которых у клиентского state не бывает: они устаревают, их можно рефетчить, их надо дедуплицировать, когда много компонентов хотят один ресурс, у них есть состояния загрузки и ошибки, и мутация в другом месте может их инвалидировать. Загрузишь их в useState или обычный слайс Redux — и пишешь всё это руками, обычно отгружая двойные fetch, устаревшие вью после мутации и отсутствие дедупа. Распознать их как server cache значит тянуться к React Query / SWR / RTK Query — это библиотеки кэша с ключом по запросу, а не библиотеки state. Тогда твой настоящий клиентский state остаётся маленьким — open/closed UI, черновик ввода, выбор — плюс URL для всего шарящегося." },
    { q: "Что такое colocation, почему она делает приложение быстрее и чем отличается от «lift state up»?", a: "Colocation означает, что кусок state живёт у нижнего общего предка всех, кто его читает — и не выше. Это делает приложение быстрее, потому что React ререндерит поддерево, владеющее state: держи флаг open тултипа в тултипе — и переключение ререндерит тултип, а не дашборд. «Lift state up» верно, но применяется слишком широко — люди поднимают в корень «чтобы было доступно», что подписывает всё приложение и создаёт штормы ререндеров. Зеркальное правило «push state down» — это и есть colocation на практике: поднимай только к нижнему общему читателю, а не на верх." },
    { q: "Какие виды state принадлежат URL и что ломается, если держать их только в useState?", a: "Всё шарящееся или кладущееся в закладки принадлежит URL как источнику истины: фильтры, поисковый запрос, сортировка, пагинация, id открытой детали, активная вкладка. URL — единственное место, переживающее рефреш и едущее в скопированной ссылке. Держи их только в useState — и ломаются две вещи: рефреш сбрасывает их в дефолты (пользователь теряет место), а ссылка, которую пользователь копирует, открывает дефолтный пустой вид у коллеги, потому что view-состояние не покинуло память того пользователя. localStorage чинит рефреш, но он на устройство, так что ссылку он шарящейся не делает — только URL делает оба." },
    { q: "Когда клиентский state надо нормализовать и в чём именно трейдофф против вложенной формы?", a: "Нормализуй реляционные клиентские данные, которые часто мутируешь: храни каждую сущность один раз в плоском byId-словаре на тип (users.byId, posts.byId) с allIds для порядка, а ссылки держат id (post.authorId), джойнящиеся при чтении. Трейдофф: вложенная форма (users, встроенные в posts) эргономична для чтения (post.author.name), но ловушка под мутацией, потому что одна сущность скопирована по дереву, и переименование должно переписать каждую копию, а один промах вызывает drift в масштабе. Normalized-форма делает обновления O(1) — одна запись в byId[id], которую видит каждый join — ценой join при чтении и небольшой церемонии, которую убирает createEntityAdapter из Redux Toolkit. Для read-mostly серверных данных не нормализуй: пусть библиотека кэша держит сырой ответ." },
    { q: "Пройди по порядку вопросов, которые сеньор задаёт, решая, где должно жить любое значение.", a: "(1) Выводимо ли оно из существующего state? Если да — не храни, вычисляй в рендере. (2) Если это действительно state, чьё оно — серверное (полученное) или клиентское (UI/черновик/выбор)? (3) Серверное → библиотека кэша с ключом по запросу (React Query/SWR/RTK Query), которая делает свежесть, дедуп, рефетч и инвалидацию. (4) Клиентское и шарящееся/кладущееся в закладки → URL как источник истины. (5) Клиентское и приватное → нижний общий предок читателей (colocate; не поднимай в корень). (6) Если это реляционные клиентские данные, которые часто мутируешь, нормализуй в byId-словарь. Эта лестница убирает большинство «проблем управления state» до выбора любой библиотеки." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь стержень юнита: выводи выводимое, чтобы оно не расходилось, распознавай полученные данные как server cache и отдавай их библиотеке кэша, колоцируй приватный клиентский state у нижнего читателя вместо подъёма, шарящееся view-состояние клади в URL, а реляционные данные нормализуй только когда их мутируешь. Теперь, когда откроешь PR и увидишь значение, хранящееся рядом со своим источником, или полученный payload в useState, или фильтры, слетающие при рефреше, — ты первым делом тянешься к дереву решений, а не к документации по библиотеке.
</Recap>
