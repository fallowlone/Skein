---
concepts:
- route-based-splitting
- request-waterfall
- vendor-chunk-invalidation
- preload-prefetch
- over-splitting
- bundle-budget
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://web.dev/articles/code-splitting-suspense
- https://web.dev/articles/granular-chunking-nextjs
- https://nextjs.org/docs/pages/guides/lazy-loading
status: ready
summary: "Синтез по всему юниту code splitting в формате множественного выбора — латентностный размен, разбивка по маршрутам vs по компонентам, request waterfall, кэширование vendor-чанка и resource hints."
title: 'Code splitting: тест с множественным выбором'
track: frontend
unit: 06-code-splitting
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Ни один не просит определение — каждый отражает решение по разбивке, которое ты принимаешь под реальным бюджетом латентности, где неверный выбор деградирует LCP или заставляет заново скачивать React на каждом деплое.
</Hook>

<Goal lang="ru">
Убедись, что связываешь латентностный размен, правило «маршрут vs компонент», request waterfall, кэширование vendor-чанка и resource hints — синтез, к которому вёл обзор.
</Goal>

<Quiz
  id="06-code-splitting-choice-1"
  lessonSlug="06-code-splitting"
  lang="ru"
  question="Команда оборачивает каждую панель дашборда в React.lazy. Анализатор показывает 40 чанков, ни один больше 12 KB, но полевой LCP на 4G деградирует с 2.3s до 4.1s. Что доминирующая причина?"
  choices={[
    { label: "Разбивка превращает одну загрузку во много запросов; вложенные lazy-границы обнаруживаются последовательно, поэтому зависимые round trip'ы складываются на критический путь на канале с высоким RTT, а крошечные чанки хуже сжимаются.", correct: true },
    { label: "Суммарный объём байт вырос, поэтому страница просто скачивает больше кода.", misconception: "Разбивка не добавила кода — те же модули теперь в большем числе файлов. Регрессия — это латентность round trip'ов от последовательного waterfall плюс потеря сжатия, а не лишние байты." },
    { label: "40 мелких файлов парсятся медленнее одного большого вне зависимости от сети.", misconception: "Стоимость парсинга масштабируется с суммарным объёмом байт, а не числом файлов. Доминирующая стоимость на 4G — последовательная латентность round trip, поэтому тот же билд нормален на десктопе." },
    { label: "Анализатор бандла ошибается; 40 крошечных чанков всегда быстрее.", misconception: "Анализатор прав по размеру. Меньше-на-диске не равно быстрее-по-сети, когда каждый чанк — это запрос, а запросы зависимы — это вся посылка юнита." },
  ]}
/>

<Quiz
  id="06-code-splitting-choice-2"
  lessonSlug="06-code-splitting"
  lang="ru"
  question="Можно разбить ровно одну вещь на странице. Какой кандидат — самый высокорычажный и низкорисковый split?"
  choices={[
    { label: "Отдельный маршрут / страница — route-based splitting отгружает только код текущего маршрута, остальное приходит при навигации, а waterfall ограничен осознанным действием пользователя с естественным моментом загрузки.", correct: true },
    { label: "Карточка 12 KB, рендерящаяся над сгибом при загрузке.", misconception: "Разбивка компонента над сгибом покупает round trip и спиннер, экономя почти ничего, бьёт по LCP и рискует CLS. Мелкий контент над сгибом отгружай в чанке маршрута." },
    { label: "Каждый листовой компонент, чтобы все были как можно мельче.", misconception: "Over-splitting дробит граф в последовательный waterfall и убивает сжатие. Правило — разбивать тяжёлое и не-на-первой-отрисовке, а не всё подряд." },
    { label: "Общий runtime React, чтобы он загрузился первым.", misconception: "React нужен для первой отрисовки, поэтому его отсрочка лишь добавляет round trip на критический путь. React место в долго-кэшируемом vendor-чанке, а не за lazy-границей." },
  ]}
/>

<Quiz
  id="06-code-splitting-choice-3"
  lessonSlug="06-code-splitting"
  lang="ru"
  question="Ревьюер заявляет: «HTTP/2 multiplexing делает бандлинг устаревшим, так что дроби как угодно гранулярно». В чём он неправ?"
  choices={[
    { label: "Multiplexing убирает штраф соединения для множества параллельных запросов, но не может распараллелить последовательный lazy-waterfall (чанк B неизвестен, пока чанк A не распарсен) и не возвращает сжатие, потерянное при дроблении одного ресурса на слайвера.", correct: true },
    { label: "Он прав — с HTTP/2 число чанков вообще больше не имеет значения.", misconception: "Multiplexing помогает только запросам, о которых браузер знает одновременно. Waterfall последователен по построению, а мелкие файлы хуже сжимаются — обе стоимости переживают HTTP/2." },
    { label: "HTTP/2 не может скачивать больше одного чанка на соединение.", misconception: "HTTP/2 как раз мультиплексирует много параллельных запросов по одному соединению — эта часть верна. Изъян — в последовательном обнаружении и потере сжатия, а не в параллелизме." },
    { label: "Бандлинг всегда быстрее разбивки при любом транспорте.", misconception: "Не всегда — route-based splitting всё равно выигрывает, держа начальный маршрут лёгким. Суть в том, чтобы найти середину: n чанков, где n много меньше числа модулей, а не дробить и не отгружать один гигантский файл." },
  ]}
/>

<Quiz
  id="06-code-splitting-choice-4"
  lessonSlug="06-code-splitting"
  lang="ru"
  question="Вернувшиеся пользователи заново скачивают React на каждом деплое, хотя зависимости не менялись. Самая вероятная причина?"
  choices={[
    { label: "webpack runtime / module-id map зашит внутрь vendor-чанка, поэтому одно изменение в приложении сдвигает map, меняет content hash и URL vendor-чанка и сбрасывает кэш у всех.", correct: true },
    { label: "React обязан перекачиваться при любом изменении кода приложения, by design.", misconception: "Это симптом плохого чанкинга, а не правило дизайна. Корректно изолированный content-hashed vendor-чанк остаётся в кэше между деплоями, пока его байты не меняются." },
    { label: "Кэш браузера автоматически истекает для vendor-файлов на каждом деплое.", misconception: "Кэш ключуется по URL. С content-hashed именами неизменный vendor-чанк сохраняет тот же URL и остаётся в кэше; hash меняется, потому что в него протекло что-то волатильное." },
    { label: "Vendor-чанк слишком большой, поэтому браузер отказывается его кэшировать.", misconception: "Размер тут не блокирует кэширование. Дефект — волатильность хэша: вынеси runtime в отдельный крошечный файл, чтобы байты vendor оставались стабильными между деплоями." },
  ]}
/>

<Quiz
  id="06-code-splitting-choice-5"
  lessonSlug="06-code-splitting"
  lang="ru"
  question="Интерактивный viewer на 180 KB лежит под сгибом и открывается, только когда пользователь до него прокрутит. Лучшая стратегия загрузки для аудитории на 4G?"
  choices={[
    { label: "React.lazy для viewer и prefetch его чанка низким приоритетом на idle или когда он приближается к viewport, чтобы байты обычно уже были в кэше к моменту монтирования — без стоимости на критическом пути и без спиннера при прокрутке.", correct: true },
    { label: "Отгрузить его в основном чанке маршрута, чтобы он всегда был готов.", misconception: "Это кладёт 180 KB, до которых большинство не прокрутит, на критический путь LCP, раздувая парсинг и TTI для всех ради одного спиннера. Тяжёлый, под-сгибом, гейтнутый взаимодействием виджет — канонический кандидат на отсрочку." },
    { label: "React.lazy для каждой секции страницы, включая шапку, для единообразия.", misconception: "Разбивка шапки над сгибом добавляет round trip и спиннер к контенту первой отрисовки и строит последовательный waterfall. Разбивай тяжёлое отсроченное; не дроби страницу." },
    { label: "Загрузить его в iframe, чтобы браузер обрабатывал его отдельно.", misconception: "iframe — это отдельный документ со своим JS-контекстом и стоимостью загрузки, а не инструмент стратегии чанков. Viewer — компонент: разбей его как компонент и упреди waterfall через prefetch." },
  ]}
/>

<Quiz
  id="06-code-splitting-choice-6"
  lessonSlug="06-code-splitting"
  lang="ru"
  question="В чём практическая разница между rel='preload' (или modulepreload) и rel='prefetch' для чанков?"
  choices={[
    { label: "preload загружает чанк, который текущей странице точно понадобится, прямо сейчас, параллельно документу, выравнивая waterfall до его образования; prefetch загружает низким приоритетом для вероятной будущей навигации, например по hover, чтобы следующий клик ощущался мгновенным.", correct: true },
    { label: "Они взаимозаменяемы — оба просто скачивают чанк раньше.", misconception: "Приоритет и намерение различаются. preload — высокий приоритет для чанка, нужного текущей странице; prefetch — idle-приоритет, спекуляция о будущей навигации. Prefetch для критического чанка оставит его обнаруженным слишком поздно." },
    { label: "preload для картинок, а prefetch для скриптов.", misconception: "Оба применимы к script/module-чанкам (modulepreload для ES-модулей). Разделение — критично-для-текущей-страницы vs вероятно-будущее, а не по типу ресурса." },
    { label: "prefetch блокирует рендеринг до прихода чанка; preload — нет.", misconception: "Ни один не блокирует рендеринг — оба это hints. prefetch — низкоприоритетная спекуляция; preload — высокоприоритетная текущая нужда. Перевёрнуто относительно утверждения." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одно дерево решений: разбивка это латентностный размен, а не бесплатное сокращение размера, поэтому вопрос всегда в том, сколько последовательных round trip'ов ты добавил на критический путь. Разбивай по маршрутам по умолчанию (ограниченный waterfall, дефолт фреймворка); разбивай компонент только если он тяжёлый и не на первой отрисовке; никогда не разбивай мелкий контент над сгибом. Именно waterfall, а не число байт, проигрывает на 4G, и HTTP/2 его не спасает. Защити кэширование content-hashed vendor-чанком с вынесенным runtime, и используй preload для критичных сейчас чанков, prefetch — для вероятно-следующих навигаций. Теперь, когда видишь в полевых данных деградацию LCP после «оптимизации» бандла, — смотри сначала в network waterfall: считай последовательные запросы чанков на критическом пути, а не общее число чанков.
</Recap>
