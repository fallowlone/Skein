---
slug: 01-overview
lang: ru
track: frontend
unit: 06-code-splitting
order: 1
level: junior
lessonType: topic
status: ready
title: "Разбивка кода: граф чанков — это бюджет латентности, а не выигрыш в размере"
summary: "Разбивка меняет одну большую загрузку на много мелких. По маршрутам она окупается; по компонентам на телефоне с высокой латентностью она строит request waterfall и проигрывает тому самому бандлу, который ты пытался победить."
prereqs: []
deepensInto: []
spiral: []
concepts: ["route-based-splitting", "dynamic-import", "request-waterfall", "bundle-budget", "preload-prefetch", "vendor-chunk-invalidation"]
estMin: 16
sources:
  - https://web.dev/articles/code-splitting-suspense
  - https://web.dev/articles/granular-chunking-nextjs
  - https://medium.com/webpack/webpack-http-2-7083ec3f3ce6
  - https://nextjs.org/docs/pages/guides/lazy-loading
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
Команда «оптимизирует» дашборд: каждая панель получает свой `React.lazy`. Анализатор бандла выглядит шикарно — 40 крошечных чанков, ни одного больше 12 KB. Потом приходят полевые данные. LCP на 4G деградировал с 2.3s до 4.1s, CLS подскочил до 0.31, и спиннеры мигают на каждой панели. На телефоне с RTT 150 ms страница теперь делает цепочку round trip'ов, которой раньше не было: загрузить маршрут, распарсить его, *затем* обнаружить, что нужна панель A, загрузить её, обнаружить панель B, загрузить её. «Меньший» билд стал медленнее, потому что чанки — это запросы, а запросы на мобиле дорогие.
</Hook>

<Crux>Разбивка меняет одну загрузку на много. Переборщи — и request waterfall на телефоне с высокой латентностью проиграет бандлу, что ты чинил.</Crux>

<Explanation>

## Разбивка — это трейдофф латентности, а не бесплатное уменьшение размера

Собрать всё в один файл — значит заставить пользователя скачать код страниц, которые он, возможно, никогда не посетит. Разбивка кода чинит это, разрезая граф на чанки, загружаемые по требованию: webpack/Rollup/Vite видят `dynamic import()` и выпускают отдельный файл. Меньше начального JavaScript — быстрее парсинг, быстрее Time-to-Interactive, быстрее Largest Contentful Paint.

Но ты не удалил код; ты превратил одну загрузку в несколько. Каждый чанк — это отдельный HTTP-запрос, а запрос не бесплатен: планирование соединения, латентность round trip, декомпрессия и парсинг, который может начаться только после прихода байтов. На быстром десктопе эти накладные расходы тонут в шуме. На 4G-телефоне с round trip 100–200 ms цепочка зависимых запросов — это доминирующая стоимость. Вопрос сеньора никогда не «стал ли бандл меньше?» — он «сколько *последовательных* round trip'ов разбивка добавила в критический путь?».

## Разбивка по маршрутам — безопасный дефолт; по компонентам — скальпель

Разбивка по маршрутам — самый рычажный и наименее рискованный сплит: один чанк на страницу, грузится при навигации. Начальный маршрут отгружает только свой код; остальное приходит по мере движения пользователя. Фреймворки делают это бесплатно — Next.js, Remix и React Router режут по маршрутам автоматически. Выигрыш большой, а waterfall ограничен, потому что каждая навигация — это намеренное действие пользователя с естественным моментом загрузки.

Разбивка по компонентам (`React.lazy` вокруг тяжёлого виджета) — это скальпель, и правило таково: режь то, что **тяжёлое и не на первой отрисовке**. Богатый текстовый редактор на 200 KB, открывающийся за кнопкой — режь. Библиотека графиков на одной вкладке — режь. Карточка на 12 KB, рендерящаяся над сгибом при загрузке — *никогда* не режь; ты добавил round trip и спиннер ради почти ничего. Эвристика сообщества — резать только компоненты примерно от 30–50 KB; ниже накладные расходы на запрос стоят дороже сэкономленных байтов.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Кандидат</th>
        <th class="border border-border px-3 py-2 text-left">Резать?</th>
        <th class="border border-border px-3 py-2 text-left">Почему</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Другой маршрут / страница</td>
        <td class="border border-border px-3 py-2">Да — по маршрутам</td>
        <td class="border border-border px-3 py-2">Самый высокий рычаг; дефолт фреймворка; waterfall ограничен навигацией</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Редактор на 200 KB за кнопкой</td>
        <td class="border border-border px-3 py-2">Да — по компонентам</td>
        <td class="border border-border px-3 py-2">Тяжёлый, под сгибом, не на первой отрисовке → отложи до взаимодействия</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Карточка на 12 KB над сгибом</td>
        <td class="border border-border px-3 py-2">Нет</td>
        <td class="border border-border px-3 py-2">Round trip + спиннер дороже байтов; отгрузи в чанке маршрута</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Общий <code>node_modules</code> (React, date-либа)</td>
        <td class="border border-border px-3 py-2">Vendor-чанк</td>
        <td class="border border-border px-3 py-2">Стабилен между деплоями → долго кэшируется отдельно от кода приложения</td>
      </tr>
    </tbody>
  </table>
</div>

## Request waterfall: почему переразбивка проигрывает на мобиле

Failure mode, который кусается в проде, — это *waterfall*. С `React.lazy` родительский маршрут рендерится, React упирается в `lazy`-границу и только *тогда* запрашивает дочерний чанк — последовательно. Вложи две-три lazy-границы — и получишь зависимую цепочку: чанк A должен прийти и распарситься, прежде чем браузер вообще узнает, что чанк B существует. На канале с RTT 150 ms три последовательных чанка добавляют ~450 ms чистой латентности до того, как хоть один исполнится, поверх загрузки и парсинга.

HTTP/2 помогает, но не спасает. Мультиплексирование позволяет *независимым* запросам делить одно соединение параллельно, поэтому много мелких чанков, выпущенных разом, скачиваются без head-of-line штрафа HTTP/1.1. Это реально — гранулярное чанкирование в Next.js использовало `maxInitialRequests: 25`, чтобы безопасно отгружать больше мелких чанков. Но мультиплексирование параллелит только запросы, о которых браузер знает *одновременно*. Lazy-waterfall последователен по построению — браузер не может загрузить чанк B параллельно, потому что ещё не распарсил чанк A. И крошечные чанки хуже сжимаются: gzip/brotli нужно окно повторяющихся байтов, поэтому разрезание ассета на 115 KB на много кусочков может потерять столько сжатия, что даже мультиплексирование не отыграет (классический тест спрайта: 10 KB вместе против 115 KB по отдельности).

<Inset kind="why" lang="ru">
«HTTP/2 делает бандлинг ненужным» — миф, стоивший командам реальной производительности. Мультиплексирование убирает штраф *соединения* за много запросов, но не штраф *последовательного обнаружения* у waterfall и не штраф *сжатия* у мелких файлов. Вывод самой команды webpack: всё равно собирай в n чанков, где n больше 1 и сильно меньше числа модулей — найди середину, не дроби граф в пыль.
</Inset>

## Бюджеты бандла и ловушка кэша при деплое

Сеньор задаёт бюджет и проверяет его в CI, чтобы регрессию ловил билд, а не полевые данные. Частый старт — примерно 200 KB JS и 50 KB CSS (сжатых) на маршрут, причём начальный маршрут держат строже всего, потому что он на критическом пути LCP. Над линией разбивки идёт всё нужное для первой отрисовки и интерактивности; под ней — всё за кликом, маршрутом или скроллом.

Тоньше ловушка — **vendor-чанк и инвалидация кэша**. Ты изолируешь `node_modules` в vendor-чанк именно потому, что он редко меняется — чтобы возвращающиеся пользователи держали его в кэше между деплоями. Но если бандлер хэширует vendor и код приложения вместе или инлайнит webpack runtime / мапу module-id в vendor-файл, то *каждый* деплой меняет хэш vendor, и каждый пользователь перекачивает React на каждый релиз. Фикс — гранулярное чанкирование: стабильный vendor-чанк, runtime в своём крошечном файле и имена с content-hash, чтобы новый URL получали только реально изменившиеся чанки — гранулярное чанкирование Next.js завезло ровно это, чтобы одно изменение приложения не инвалидировало весь vendor.

## Preload критичного чанка; prefetch вероятного следующего

Если ты обязан разрезать что-то на критическом пути, не дай браузеру обнаружить это поздно. Два resource hint закрывают разрыв. `<link rel="preload">` (или `modulepreload` для ES-модулей) говорит браузеру загрузить чанк *сейчас*, параллельно документу, выравнивая waterfall до того, как он сформируется — используй для чанка, который текущая страница точно потребует. `<link rel="prefetch">` загружает с низким приоритетом для *будущей* навигации — например, prefetch чанка дашборда при наведении на его ссылку, чтобы клик ощущался мгновенно. Фреймворки автоматизируют это: Next.js делает prefetch целей `<Link>` в вьюпорте, а `import()` внутри `next/dynamic` позволяет ему внедрить preload до рендера.

Производственная авария, которую это предотвращает, — lazy-загрузка над сгибом. Сделай lazy герой или главную панель — и пользователь сначала видит Suspense-фолбэк (спиннер или пустой блок), потом реальный компонент впрыгивает, сдвигая всё под ним. Это удар по Cumulative Layout Shift; один реальный кейс ушёл с CLS 0.85 до 0.1 просто за счёт отказа от lazy над сгибом и резервирования места под то, что грузится. Пороги, которые ты защищаешь: LCP меньше 2.5s, CLS меньше 0.1, INP меньше 200 ms. Спиннер над сгибом атакует все три разом.

<TradeoffMatrix
  id="06-code-splitting-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Страница товара должна показать интерактивный 3D-просмотрщик на 180 KB, который сидит под сгибом и открывается, только когда пользователь доскроллит до него. Выбери стратегию загрузки для 4G-аудитории."
  options={[
    { name: "React.lazy просмотрщик и prefetch его чанка на idle / когда он приближается к вьюпорту", summary: "Сплит + prefetch.", correct: true, justification: "Просмотрщик тяжёлый (180 KB) и под сгибом, поэтому не должен блокировать начальный маршрут или LCP — ровно для этого и нужна разбивка по компонентам. Но наивный lazy сделал бы waterfall спиннера, когда пользователь доскроллит. Prefetch чанка с низким приоритетом на idle или при приближении к вьюпорту означает, что байты обычно уже в кэше к моменту монтирования: ни стоимости на критическом пути, ни спиннера при скролле. Это учебный паттерн «режь тяжёлое отложенное» с waterfall, упреждённым подсказкой." },
    { name: "Отгрузить просмотрщик в главном чанке маршрута, чтобы он всегда был готов", summary: "Не резать.", justification: "Кладёт 180 KB кода, до которого большинство посетителей не доскроллит, на критический путь LCP, раздувая начальный парсинг и TTI всем ради избавления от одного спиннера. Весь смысл разбивки — держать первую отрисовку лёгкой; тяжёлый, под сгибом, за взаимодействием виджет — это каноничное «отложить», а не «инлайнить»." },
    { name: "React.lazy каждую секцию страницы, включая хедер над сгибом", summary: "Резать всё.", justification: "Переразбивка. Резать хедер над сгибом — значит добавить round trip и спиннер контенту, нужному для первой отрисовки, бьёт по LCP и рискует CLS, а цепочка lazy-границ строит последовательный waterfall, который проигрывает на 4G round trip. Режь тяжёлое отложенное; не дроби страницу." },
    { name: "Грузить в iframe, чтобы браузер обрабатывал его отдельно", summary: "В iframe.", justification: "iframe — это отдельный документ со своим JS-контекстом и накладными расходами, а не инструмент разбивки кода. Он не решает стратегию чанков, усложняет состояние и стилизацию и добавляет свою стоимость загрузки. Просмотрщик — это компонент; режь его как компонент." },
  ]}
/>

<Quiz
  id="06-code-splitting-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="На 4G-телефоне с RTT 150 ms почему билд из 40 крошечных lazy-чанков может быть медленнее одного бандла?"
  choices={[
    { label: "Вложенные lazy-границы загружают чанки последовательно, поэтому зависимые round trip'ы стакаются на критическом пути, а крошечные файлы хуже сжимаются", correct: true },
    { label: "Крошечные чанки всегда парсятся медленнее одного большого файла независимо от сети", misconception: "Парсинг масштабируется по суммарным байтам, а не по числу файлов; доминирующая стоимость здесь — последовательная латентность round trip на канале с высоким RTT плюс потеря сжатия на кусочках." },
    { label: "HTTP/2 не может загружать больше одного чанка за раз", misconception: "HTTP/2 нормально мультиплексирует параллельные запросы; проблема в том, что waterfall последователен по построению, поэтому нечего параллелить, пока не распарсится каждый родительский чанк." },
    { label: "Браузеры ограничивают JavaScript одним запросом на страницу", misconception: "Такого лимита нет. Проблема в зависимом обнаружении: чанк B неизвестен, пока чанк A не загружен и не распарсен." },
  ]}
/>

<Quiz
  id="06-code-splitting-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Возвращающиеся пользователи перекачивают React на каждый деплой, хотя зависимости не менялись. Вероятная причина?"
  choices={[
    { label: "webpack runtime / мапа module-id инлайнятся в vendor-чанк, поэтому его content-hash меняется каждый билд", correct: true },
    { label: "React обязан перекачиваться при любом изменении кода приложения, by design", misconception: "Не by design — это симптом плохого чанкирования. Правильно изолированный vendor-чанк с content-hash остаётся в кэше между деплоями, пока его байты не меняются." },
    { label: "Кэш браузера автоматически истекает у vendor-файлов после каждого деплоя", misconception: "Кэш ключуется по URL; с именами на content-hash неизменившийся vendor-чанк держит тот же URL и остаётся в кэше. Хэш меняется, потому что в него протекло что-то волатильное." },
    { label: "HTTP/2 инвалидирует кэшированные чанки на новых соединениях", misconception: "HTTP/2 не имеет отношения к инвалидации кэша; кэширование управляется URL'ами и заголовками кэша, а не транспортом." },
  ]}
/>

<DragOrder
  id="06-code-splitting-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь решения, которые сеньор принимает, добавляя разбивку кода на маршрут:"
  items={[
    { id: "a", label: "Сначала режь по маршрутам — крупнейший выигрыш, дефолт фреймворка, waterfall ограничен навигацией" },
    { id: "b", label: "Найди тяжёлые (~30–50 KB+) компоненты, что под сгибом или за взаимодействием" },
    { id: "c", label: "React.lazy только их; контент над сгибом оставь в чанке маршрута" },
    { id: "d", label: "Preload критичных отложенных чанков; prefetch вероятных следующих маршрутов на hover/idle" },
    { id: "e", label: "Изолируй vendor-чанк с content-hash, чтобы деплои не перекачивали зависимости" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Режь по маршрутам по умолчанию; компонент — только если он тяжёлый и не на первой отрисовке. Чанки — это round trip'ы, переразбивка делает waterfall на мобиле. Preload критичных чанков, изолируй хэшированный vendor-чанк.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-code-splitting-retrieval"
  lang="ru"
  questions={[
    { q: "Коллега хочет обернуть каждый компонент на странице в React.lazy, «чтобы бандл стал меньше». Объясни, почему это может сделать мобильную страницу медленнее, а не быстрее.", a: "Разбивка не удаляет код — она превращает одну загрузку в много отдельных запросов, и каждый запрос несёт латентность round trip, декомпрессию и парсинг, который может начаться только после прихода байтов. На 4G-телефоне с RTT 100–200 ms эти расходы — доминирующая стоимость, а не число байтов. Хуже того, lazy-границы React обнаруживаются последовательно: родительский маршрут должен отрендериться и упереться в lazy-границу, прежде чем запросит дочерний чанк, поэтому вложенные lazy-компоненты образуют request waterfall — чанк A должен прийти и распарситься, прежде чем браузер вообще узнает о чанке B. Три последовательных чанка могут добавить ~450 ms чистой латентности на критический путь. Крошечные чанки ещё и хуже сжимаются, потому что gzip/brotli нужно окно повторяющихся байтов. Мультиплексирование HTTP/2 помогает независимым параллельным запросам, но не может распараллелить waterfall и не вернёт потерянное сжатие. Фикс — резать по маршрутам, затем резать только тяжёлые (~30–50 KB+) компоненты, что не на первой отрисовке, и делать preload всего критичного, чтобы оно грузилось параллельно документу, а не обнаруживалось поздно." },
    { q: "Почему изоляция зависимостей в vendor-чанк помогает кэшированию, и как она случайно бьёт по тебе на каждом деплое?", a: "Код node_modules (React, date-либа, либа графиков) меняется куда реже кода приложения, поэтому помещение его в отдельный vendor-чанк позволяет возвращающимся пользователям держать его в кэше между деплоями, а перекачивать только app-чанки. С именами на content-hash неизменившийся vendor-чанк держит тот же URL и остаётся в кэше. Бьёт по тебе, когда в байты этого чанка протекает что-то волатильное — чаще всего webpack runtime и мапа module-id, которые могут сдвигаться при добавлении или переупорядочивании модулей. Если эта мапа инлайнится в vendor-файл, хэш vendor меняется на каждый билд, URL меняется, и каждый пользователь перекачивает React на каждый релиз, хотя зависимость не менялась. Фикс — гранулярное чанкирование: вынеси runtime в свой крошечный файл, дай vendor стабильную границу и делай content-hash имён, чтобы новый URL получали только чанки, чьи байты реально изменились. Гранулярное чанкирование Next.js завезло ровно это, чтобы одно изменение приложения больше не инвалидировало весь vendor-бандл." },
  ]}
/>

<Recap lang="ru">
Разбивка кода — это трейдофф латентности, а не бесплатное уменьшение размера: она превращает одну загрузку в много запросов, и каждый запрос стоит round trip, который сильнее всего чувствуешь на мобильном канале с высокой латентностью. Разбивка по маршрутам — безопасный, высокорычажный дефолт: один чанк на страницу, waterfall ограничен навигацией, и фреймворки делают это за тебя. Разбивка по компонентам — скальпель для того, что и тяжёлое (примерно от 30–50 KB), и не на первой отрисовке; резать маленький компонент над сгибом — просто купить round trip и спиннер. Классический производственный провал — переразбивка в последовательный waterfall, проигрывающий бандлу на 4G, или lazy над сгибом со сдвигом макета — следи за LCP меньше 2.5s, CLS меньше 0.1, INP меньше 200 ms. Задай бюджет бандла (примерно 200 KB JS на маршрут) и проверяй его в CI, делай preload критичных отложенных чанков и prefetch вероятных следующих маршрутов, изолируй vendor-чанк с content-hash и вынесенным runtime, чтобы одно изменение приложения не заставляло каждого пользователя перекачивать React. Граф чанков — это бюджет, который ты проектируешь, а не число, которое минимизируешь.
</Recap>
