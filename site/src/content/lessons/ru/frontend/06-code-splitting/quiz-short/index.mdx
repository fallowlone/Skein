---
concepts:
- latency-trade
- route-based-splitting
- request-waterfall
- vendor-chunk-invalidation
- preload-prefetch
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://web.dev/articles/code-splitting-suspense
- https://web.dev/articles/granular-chunking-nextjs
- https://medium.com/webpack/webpack-http-2-7083ec3f3ce6
status: ready
summary: "Промпты на свободное припоминание по юниту code splitting. Сначала ответь своими словами, затем раскрой модельный ответ и сравни."
title: 'Code splitting: свободное припоминание'
track: frontend
unit: 06-code-splitting
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт проговори или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет дерево решений по разбивке.
</Hook>

<Goal lang="ru">
Восстанови спину юнита — почему разбивка это латентностный размен, правило «маршрут vs компонент», request waterfall, ловушку кэша vendor-чанка и разделение preload/prefetch — не подглядывая в обзор.
</Goal>

<RetrievalDrawer
  client:load
  id="06-code-splitting-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему code splitting — это латентностный размен, а не бесплатное сокращение размера?", a: "Разбивка не удаляет код — она режет граф модулей на чанки, превращая одну загрузку в несколько. Каждый чанк — отдельный HTTP-запрос, а запрос несёт планирование соединения, латентность round trip, декомпрессию и парсинг, который может начаться только после прихода байт. На быстром десктоп-канале эти накладные расходы растворяются в шуме, но на 4G-телефоне с RTT 100–200 ms они становятся доминирующей стоимостью. Поэтому senior-вопрос — никогда не «меньше ли бандл?», а «сколько последовательных round trip'ов разбивка добавила на критический путь?». Меньше начального JavaScript помогает парсингу, TTI и LCP — но только если ты не заменил байты цепочкой зависимых запросов." },
    { q: "Почему route-based splitting — безопасный дефолт, а разбивка по компонентам — скальпель?", a: "Route-based splitting отгружает один чанк на страницу: начальный маршрут несёт только свой код, остальное приходит по мере навигации. Это самый высокорычажный и низкорисковый split, потому что каждая навигация — осознанное действие пользователя с естественным моментом загрузки, так что waterfall ограничен — и фреймворки (Next.js, Remix, React Router) делают это бесплатно. Разбивка по компонентам через React.lazy — скальпель со строгим правилом: разбивай только то, что одновременно тяжёлое (примерно 30–50 KB и выше) и не на первой отрисовке — редактор на 200 KB за кнопкой, график на одной вкладке. Мелкий или над-сгибом компонент разбивать нельзя никогда, потому что round trip и спиннер стоят дороже сэкономленных байт." },
    { q: "Объясни request waterfall и почему HTTP/2 multiplexing от него не спасает.", a: "С React.lazy родительский маршрут рендерится, React доходит до lazy-границы и только тогда запрашивает дочерний чанк — последовательно. Вложи две-три lazy-границы и получишь зависимую цепочку: чанк A должен прийти и распарситься, прежде чем браузер вообще узнает о существовании чанка B, так что на канале с RTT 150 ms три последовательных чанка добавляют ~450 ms чистой латентности до того, как хоть один выполнится. HTTP/2 multiplexing позволяет независимым запросам делить одно соединение параллельно, убирая штраф head-of-line из HTTP/1.1 — но он может распараллелить только запросы, о которых браузер знает одновременно. Waterfall последователен по построению, поэтому распараллеливать нечего, пока каждый родитель не распарсится. Multiplexing также не возвращает сжатие, потерянное при дроблении одного ресурса на слайвера, потому что gzip/brotli нужно окно повторяющихся байт." },
    { q: "Как изоляция vendor-чанка помогает кэшированию и как она случайно даёт обратный эффект на каждом деплое?", a: "Код node_modules (React, библиотека дат или графиков) меняется куда реже кода приложения, поэтому отдельный content-hashed vendor-чанк позволяет вернувшимся пользователям держать его в кэше между деплоями, пока перекачиваются лишь app-чанки. Обратный эффект случается, когда в байты этого чанка протекает что-то волатильное — чаще всего webpack runtime и module-id map, которые сдвигаются при добавлении или переупорядочивании модулей. Если этот map зашит в vendor-файл, hash и URL vendor меняются на каждом билде, и каждый пользователь заново скачивает React на каждом релизе, хотя зависимость не менялась. Фикс — granular chunking: вынеси runtime в отдельный крошечный файл, дай vendor стабильную границу и content-hash имена файлов, чтобы новый URL получали лишь чанки, чьи байты действительно изменились." },
    { q: "Когда использовать rel=preload (modulepreload), а когда rel=prefetch для чанка?", a: "Используй preload для чанка, который текущей странице точно понадобится, но который иначе обнаружился бы поздно: он говорит браузеру загрузить сейчас, параллельно документу, выравнивая waterfall до его образования. Используй prefetch для чанка, который пользователю вероятно понадобится при будущей навигации: он загружает низким приоритетом, чтобы не конкурировать с текущей страницей — например, prefetch чанка дашборда по hover на ссылку, чтобы клик ощущался мгновенным. Фреймворки автоматизируют оба: Next.js делает prefetch целей Link во viewport и инжектит preload для чанков next/dynamic до рендера. Разделение — критично-сейчас (preload) против вероятно-будущее (prefetch)." },
    { q: "Почему lazy-загрузка компонента над сгибом — классическая продакшен-авария и какие Core Web Vitals она атакует?", a: "Если обернуть hero или основную панель в React.lazy, пользователь сначала видит fallback Suspense (спиннер или пустой блок), затем реальный компонент впрыгивает и сдвигает всё ниже. Это удар по Cumulative Layout Shift; один задокументированный случай прошёл с CLS 0.85 до 0.1, просто перестав lazy-грузить над сгибом и зарезервировав место под то, что грузится. Это атакует все три порога сразу: отсроченная загрузка задерживает Largest Contentful Paint (цель ниже 2.5s), позднее монтирование бьёт по отзывчивости (INP ниже 200 ms), а впрыгивание сдвигает раскладку (CLS ниже 0.1). Контент над сгибом, для первой отрисовки, место в чанке маршрута, а не за lazy-границей." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь спину юнита: разбивка меняет одну загрузку на много запросов, поэтому метрика — добавленные последовательные round trip'ы, а не сэкономленные байты. Route-based splitting — ограниченный, высокорычажный дефолт; разбивка по компонентам — скальпель только для тяжёлых, не-на-первой-отрисовке виджетов. Именно waterfall проигрывает на мобиле, и HTTP/2 его не спасает; content-hashed vendor-чанк с вынесенным runtime держит кэши тёплыми между деплоями; а preload защищает критичные сейчас чанки, prefetch прогревает вероятно-следующие — и никогда не lazy-грузи над сгибом.
</Recap>
