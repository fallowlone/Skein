---
concepts:
- dependency-confusion
- lockfile-integrity
- slsa-provenance
- typosquatting
- ci-token-scope
- ignore-scripts
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://medium.com/@alex.birsan/dependency-confusion-how-i-hacked-into-apple-microsoft-and-dozens-of-other-companies-4a5d60fec610
- https://slsa.dev/spec/v1.0/levels
- https://en.wikipedia.org/wiki/XZ_Utils_backdoor
status: ready
summary: "Синтез с множественным выбором по всему модулю supply chain — dependency confusion, typosquatting, целостность lockfile, provenance, подписание и радиус поражения CI."
title: 'Supply-chain безопасность: тест с множественным выбором'
track: security
unit: 07-supply-chain
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, охватывающих весь модуль. Каждый отражает решение, которое вы принимаете в реальном инциденте — не определение для заучивания, а компромисс, который нужно взвесить, когда поверхностью атаки становится шаг установки.
</Hook>

<Goal lang="ru">
Убедитесь, что умеете связывать задокументированные паттерны атак с защитой, которая реально останавливает каждую из них — целостность lockfile, приоритет реестра, provenance и подписание — и определять, какой слой защищает данный контроль.
</Goal>

<Quiz
  id="07-supply-chain-choice-1"
  lessonSlug="07-supply-chain"
  lang="ru"
  question="Команда фиксирует точные версии в package.json (без caret-диапазонов) и считает себя защищённой от supply-chain атак. Что эта фиксация на самом деле НЕ гарантирует?"
  choices={[
    { label: "Что байты за этой версией не изменились. Версия — это метка, которой управляет издатель; если релиз перевыпущен с новым содержимым или реестр скомпрометирован, фиксация только по версии установит его молча. Лишь закоммиченный lockfile с integrity-хэшами, проверяемый через `npm ci`, ловит подмену на уровне байтов.", correct: true },
    { label: "Ничего — точная фиксация это полная защита.", misconception: "Точная фиксация лишь не даёт caret-диапазону автоматически прыгнуть на новый релиз. Она ничего не говорит о реальных байтах tarball, о provenance сборки или о компрометации реестра — всё равно нужны integrity-хэши и `npm ci`." },
    { label: "Что транзитивные зависимости тоже зафиксированы.", misconception: "Транзитивная фиксация — реальный пробел, но lockfile уже фиксирует всё транзитивное дерево по версиям и хэшам. Более глубокий пробел фиксации версии — целостность байтов, а не покрытие транзитивных." },
    { label: "Что применены последние патчи безопасности.", misconception: "Свежесть патчей — отдельный вопрос. Здесь речь о целостности: даже точно зафиксированная версия может оказаться другим артефактом, чем вы аудитировали, если форсируется только версия, а не хэш." },
  ]}
/>

<Quiz
  id="07-supply-chain-choice-2"
  lessonSlug="07-supply-chain"
  lang="ru"
  question="Сборка в крупной компании молча разрешила публичный пакет, названный как внутренний модуль, версии 9.9.9, и выполнила код атакующего. Никаких учётных данных не украдено. Что это позволило, и каков основной фикс?"
  choices={[
    { label: "Dependency confusion: пакетный менеджер обращался и к публичному, и к приватному реестру и предпочёл бóльшую версию. Фикс: владеть scoped-неймспейсом в публичном реестре и дать приватному реестру строгий приоритет для этого scope.", correct: true },
    { label: "Утёкший токен реестра позволил атакующему опубликовать в приватный реестр.", misconception: "Токен не нужен. Атакующий опубликовал в ПУБЛИЧНЫЙ реестр под внутренним именем; порядок разрешения — предпочтение бóльшей версии между реестрами — сделал остальное." },
    { label: "Typosquatting на опечатке в имени пакета.", misconception: "Typosquatting рассчитан на опечатку разработчика. Confusion опечатки не требует — настоящее внутреннее имя разрешается в публичного самозванца с более высокой версией." },
    { label: "Скомпрометированный хэш lockfile совпал с пакетом атакующего.", misconception: "Принудительный lockfile здесь — защита, а не причина. Confusion бьёт по сборкам, которые разрешают заново по нескольким реестрам без строгого приоритета." },
  ]}
/>

<Quiz
  id="07-supply-chain-choice-3"
  lessonSlug="07-supply-chain"
  lang="ru"
  question="У бэкдора xz-utils (CVE-2024-3094) исходник в Git был чистым, а выпущенный tarball — вредоносным. Какой контроль поймал бы это, и почему lockfile и audit — нет?"
  choices={[
    { label: "Воспроизводимые сборки плюс SLSA provenance: provenance связывает артефакт с точным исходником и сборкой, поэтому tarball, не совпадающий с закоммиченным исходником, не проходит проверку. Lockfile лишь сверяет, что полученные байты совпадают с записанным хэшем — уже подменённого артефакта; audit знает лишь опубликованные advisory, а это был zero-day.", correct: true },
    { label: "Lockfile с integrity-хэшами поймал бы это.", misconception: "Хэш фиксирует полученные байты, но эти байты И БЫЛИ бэкдоренным tarball. Хэширование подменённого артефакта лишь закрепляет подмену — оно никогда не сравнивает с чистым исходником." },
    { label: "Запуск audit пометил бы advisory до установки.", misconception: "Это был zero-day-бэкдор без advisory, а xz — C-библиотека, поставляемая через дистрибутивы, не npm. Audit ловит лишь известные, раскрытые CVE постфактум." },
    { label: "Фиксация точной версии остановила бы это.", misconception: "Все, кто устанавливал, получали одну и ту же зафиксированную бэкдоренную версию. Фиксация вредоносного артефакта лишь гарантирует, что вы продолжите получать вредоносный." },
  ]}
/>

<Quiz
  id="07-supply-chain-choice-4"
  lessonSlug="07-supply-chain"
  lang="ru"
  question="Почему важно добавлять `--ignore-scripts` к установкам, и от чего конкретно это защищает?"
  choices={[
    { label: "Это запрещает lifecycle-скриптам пакета (preinstall/postinstall) выполнять произвольный код во время загрузки — в момент, когда есть ваши токены реестра, секреты CI и доступ к сети. Большинство install-time-нагрузок выполняется именно там, так что блокировка скриптов сужает радиус поражения вредоносного или confusion-пакета.", correct: true },
    { label: "Это не даёт мутировать lockfile во время установки.", misconception: "Это делает `npm ci`. `--ignore-scripts` ортогонален: он блокирует выполнение произвольного кода из lifecycle-хуков, а не дрейф lockfile." },
    { label: "Это строже проверяет integrity-хэши.", misconception: "Проверка хэшей не зависит от скриптов. `--ignore-scripts` ни усиливает, ни ослабляет проверки целостности; он убирает вектор выполнения кода во время установки." },
    { label: "Это блокирует пакету доступ к публичному реестру.", misconception: "Приоритет реестра и scoping управляют тем, откуда разрешаются пакеты. `--ignore-scripts` управляет лишь тем, может ли полученный пакет выполнять код при установке." },
  ]}
/>

<Quiz
  id="07-supply-chain-choice-5"
  lessonSlug="07-supply-chain"
  lang="ru"
  question="CI-воркфлоу выдаёт job сборки широкий токен со scope на запись и запускается на pull_request из форков. В чём supply-chain риск и каков первый фикс?"
  choices={[
    { label: "Недоверенный код PR выполняется в job, который может читать секреты и пушить артефакты — одна вредоносная зависимость или PR может выкрасть токен и опубликовать отравленные релизы. Первый фикс: least-privilege, токен по умолчанию только на чтение, и не выдавать секреты публикации запускам из форков.", correct: true },
    { label: "Риска нет — CI изолирован от продакшена.", misconception: "CI напрямую в supply chain: он собирает и подписывает артефакты, доходящие до продакшена. Утёкший там токен может опубликовать отравленные релизы, которым доверяют ниже по цепочке." },
    { label: "Фикс — добавить шаг audit в воркфлоу.", misconception: "Audit ловит известные advisory; он ничего не делает с переразрешённым токеном, который недоверенный код может украсть. Фикс — least-privilege scoping и изоляция секретов от запусков из форков." },
    { label: "Переключить раннер на машину побольше.", misconception: "Ресурсы не связаны с радиусом поражения. Проблема в том, что недоверенный код выполняется с учётными данными, которых он не должен видеть — вопрос прав и изоляции триггеров." },
  ]}
/>

<Quiz
  id="07-supply-chain-choice-6"
  lessonSlug="07-supply-chain"
  lang="ru"
  question="Выходит CVE в глубокой транзитивной зависимости. Руководство спрашивает «затронуты ли мы и где?». Какой артефакт отвечает за секунды, а не через лихорадочный grep по репозиториям?"
  choices={[
    { label: "SBOM (CycloneDX/SPDX), сгенерированный при сборке: машиночитаемая опись каждого компонента и версии, так что вы запрашиваете имя компонента и получаете точный список затронутых сборок и сервисов.", correct: true },
    { label: "Достаточно одного lockfile.", misconception: "Lockfile перечисляет разрешённое дерево одного репозитория, но по многим сервисам и образам нет консолидированной, запрашиваемой описи. SBOM — стандартизированный, агрегируемый артефакт ровно для этого вопроса." },
    { label: "Отвечает SLSA provenance.", misconception: "Provenance удостоверяет, КАК артефакт собран и что он не подменён — а не ЧТО внутри. Вопрос состава — это задача SBOM." },
    { label: "Отвечает подписанный артефакт.", misconception: "Подпись доказывает подлинность и целостность артефакта, а не список ингредиентов. Чтобы узнать, присутствует ли уязвимый компонент, читают SBOM." },
  ]}
/>

<Recap lang="ru">
Сквозная линия модуля — одна слоистая модель: шаг установки и есть поверхность атаки, и каждый контроль отвечает на свой вопрос. Lockfile + `npm ci` + integrity-хэши отвечают «получил ли я именно те байты, что ожидал»; точные пины и `--ignore-scripts` сужают окна авто-обновления и выполнения кода; scoped-имена с приоритетом реестра убивают dependency confusion; SBOM отвечает «что внутри»; а SLSA provenance плюс подписание отвечают «как это собрано и не подменено ли» — тот пробел, что использовал xz. Сам CI в цепочке, поэтому least-privilege токены важны не меньше, чем зависимости, которые они обрабатывают.
</Recap>
