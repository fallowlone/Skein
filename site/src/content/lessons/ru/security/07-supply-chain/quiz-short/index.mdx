---
concepts:
- install-step-surface
- lockfile-integrity
- dependency-confusion
- sbom
- slsa-provenance
- sigstore-signing
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://slsa.dev/spec/v1.0/levels
- https://medium.com/@alex.birsan/dependency-confusion-how-i-hacked-into-apple-microsoft-and-dozens-of-other-companies-4a5d60fec610
- https://www.cisa.gov/resources-tools/resources/securing-software-supply-chain-recommended-practices
status: ready
summary: "Подсказки на свободное припоминание по всему модулю supply chain. Сначала ответьте своими словами, затем откройте эталонный ответ и сравните."
title: 'Supply-chain безопасность: тест с краткими ответами'
track: security
unit: 07-supply-chain
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. Для каждой подсказки проговорите или запишите полный ответ по памяти, прежде чем открыть эталон — именно усилие извлечения закрепляет материал.
</Hook>

<Goal lang="ru">
Восстановите ключевые механизмы модуля — почему поверхностью атаки является шаг установки, что lockfile и хэши делают и упускают, как работает confusion, и на что отвечают SBOM, provenance и подписание — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:load
  id="07-supply-chain-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему поверхностью атаки является шаг установки, а не код вашего приложения?", a: "Современное приложение — тонкий слой вашей логики на глубоком стеке зависимостей, большинство из которых транзитивные: подтянуты вашими зависимостями, не названы в package.json, не проверены никем в команде. Так что бóльшую часть байтов, что вы поставляете, написали незнакомцы, и обновляются они автоматически. XSS и SQL-инъекция целятся в ваш код; supply-chain атаки обходят его и целятся в момент, когда ваша машина или CI получает пакет и запускает его install-скрипты — с вашими токенами реестра, секретами CI и доступом к сети. Можно написать безупречный код приложения и всё равно быть скомпрометированным на `npm install`. Поэтому усиление фокусируется на границе fetch-and-install." },
    { q: "От чего защищает lockfile с integrity-хэшами, и от чего — НЕТ?", a: "Lockfile фиксирует каждую зависимость — прямую и транзитивную — к точной версии и криптографическому integrity-хэшу (sha512). Версия не даёт молча прыгнуть на вредоносный релиз; хэш означает, что пакет, чьи байты изменились под тем же номером версии, громко падает на установке, а не выполняется. Но защищает он лишь если форсируется: `npm install` мутирует lockfile, чтобы удовлетворить ослабленный диапазон, тогда как `npm ci` ставит ровно lockfile и падает, если package.json расходится — отсюда правило сеньора: `npm ci` в CI, никогда не голый install. Чего lockfile НЕ говорит — как эти артефакты собраны и не подменены ли после публикации. Это пробел, который использовал xz: исходник был чист, поставленный tarball — нет, и хэш подменённого tarball лишь закрепляет подмену." },
    { q: "Разберите атаку dependency confusion и слоистую защиту от неё.", a: "Многие пакетные менеджеры при разрешении имени обращаются и к приватному, и к публичному реестру и предпочитают наибольшую версию. Алекс Бирсан собрал внутренние имена пакетов, утёкшие в публичных JS-бандлах, опубликовал пакеты с этими именами в npm/PyPI/RubyGems версии 9.9.9 и подождал. Сборки в Apple, Microsoft, Tesla, Uber и 30+ других разрешили его публичный пакет поверх внутреннего и выполнили его код, принеся ему более $130 000 баунти. Защита — приоритет, а не везение: владеть scoped-неймспейсом вроде @acme/ в публичном реестре, чтобы никто другой не мог под ним публиковать; настроить менеджер так, чтобы приватный реестр имел строгий приоритет для вашего scope, или заблокировать публичный реестр для внутренних scope; зеркалировать публичные пакеты через один прокси; и как подстраховку — самому опубликовать заглушки под внутренними именами, чтобы имя было просто занято." },
    { q: "Что такое SBOM и на какой вопрос он отвечает, на который не ответят подпись или provenance?", a: "SBOM (Software Bill of Materials, в формате CycloneDX или SPDX) — это список ингредиентов: машиночитаемая опись каждого компонента и версии в сборке. Его задача — вопрос состава: когда выходит очередной CVE в глубокой транзитивной зависимости, вы запрашиваете имя компонента и за секунды получаете точный список затронутых сборок и сервисов вместо лихорадочного grep по репозиториям. Подпись доказывает, что артефакт подлинный и неподменённый, а SLSA provenance удостоверяет, как и из какого исходника он собран — но ни то ни другое не перечисляет, что внутри. SBOM — единственный артефакт, созданный отвечать на «затронуты ли мы и где»." },
    { q: "Объясните SLSA provenance и подписанные артефакты, и почему xz-utils нужен был именно этот слой.", a: "SLSA (Supply-chain Levels for Software Artifacts) удостоверяет, как собран артефакт; его прогрессивные уровни идут от «provenance существует» (L1) до укреплённой, изолированной, нефальсифицируемой сборки (L3), и большинство команд целится в L2–L3 для продакшена. Provenance — подписанная, проверяемая запись, связывающая артефакт с точным коммитом исходника и сборкой, что его произвели, так что подменённый бинарник не может выдать себя за официальный. Подписанные коммиты и подписанные артефакты — через Sigstore / cosign и in-toto attestations — замыкают петлю: вы проверяете подпись прежде, чем довериться байтам, вместо доверия имени. xz нуждался в этом, потому что бэкдор жил лишь в выпущенном tarball, не в читаемом исходнике Git; lockfile и audit ничего не видели. Provenance плюс воспроизводимые сборки поймали бы tarball, не совпадающий с закоммиченным исходником." },
    { q: "Расположите supply-chain защиты от самой дешёвой/немедленной до самой организационной и обоснуйте порядок.", a: "(1) Закоммитить lockfile и переключить CI на `npm ci` с integrity-хэшами — одно изменение конфига, немедленная гарантия целостности байтов. (2) Зафиксировать точные версии и добавить `--ignore-scripts` — убирает окно авто-обновления и вектор выполнения кода при установке. (3) Включить автоматическую проверку зависимостей (Dependabot / audit / Renovate) на каждом PR — ловит известно-плохие версии до попадания в lockfile. (4) Использовать scoped-имена плюс приоритет приватного реестра и резервировать внутренние имена публично — убивает dependency confusion, но требует конфигурации реестра и владения. (5) Генерировать SBOM и требовать SLSA provenance плюс подписанные артефакты для релизов — самое организационное, ведь затрагивает пайплайн сборки и процесс релиза. Порядок отражает соотношение усилий и сокращения радиуса поражения: дешевейшие контроли сначала закрывают самые частые дыры, а тяжёлые покрывают то, что хэши и пины структурно не видят." },
  ]}
/>

<Recap lang="ru">
Если вы смогли восстановить каждый ответ по памяти, вы держите хребет модуля: шаг установки и есть поверхность атаки; lockfile с хэшами и `npm ci` гарантируют, что вы получили именно те байты, но ничего не говорят о том, как они собраны; dependency confusion — баг порядка разрешения, исправляемый приоритетом, а не везением; SBOM отвечает, что внутри; а SLSA provenance плюс подписание Sigstore отвечают, как это собрано и не подменено ли — слой, нужный xz и упущенный lockfile. Защиты складываются от одной строки конфига до укреплённого пайплайна релиза.
</Recap>
