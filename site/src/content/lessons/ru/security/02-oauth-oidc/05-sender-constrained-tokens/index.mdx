---
concepts:
- dpop
- mtls
- sender-constrained
- fapi
- par
- jar
- cnf
- jkt
deepensInto:
- security/02-oauth-oidc/06-oauth-production
estMin: 11
lang: ru
lessonType: topic
level: senior
order: 5
prereqs:
- security/02-oauth-oidc/04-refresh-tokens-and-scopes
slug: 05-sender-constrained-tokens
sources:
- https://datatracker.ietf.org/doc/html/rfc9449
- https://datatracker.ietf.org/doc/html/rfc8705
- https://openid.net/specs/fapi-security-profile-2_0-final.html
spiral:
- statefulness
- encapsulation
status: ready
summary: Почему bearer-токены уязвимы под XSS или supply-chain атакой, как DPoP привязывает
  токен к client-held ключу, и когда вместо этого использовать mTLS-bound токены —
  с требованиями FAPI 2.0.
title: 'Sender-constrained токены: DPoP и mTLS'
track: security
unit: 02-oauth-oidc
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Supply-chain атака инжектирует одну строчку JavaScript в твой SPA. Она читает access-токен из памяти и exfiltrate-ит его на сервер атакующего. Токен — bearer токен: кто держит, тот и использует. Атакующий теперь имеет минуты или часы для вызова твоего API как жертва. DPoP делает украденный токен бесполезным: без соответствующего приватного ключа, который никогда не покидал клиента, атакующий не может сгенерировать валидный proof.
</Hook>

<Crux>Bearer-токен: любой держатель использует его. DPoP привязывает к приватному ключу клиента — кража без ключа бесполезна.</Crux>

<Explanation>

## Проблема bearer-токена

Почему короткий TTL и ротация не решают проблему кражи токенов полностью? Потому что окно всё равно существует — и для high-value нагрузок даже 15 минут несанкционированного доступа неприемлемы. Это фундаментальная брешь, которую bearer-токены оставляют открытой. Bearer access-токен (токен-носитель: кто держит, тот и использует) — это ровно то, что в названии: **любой держатель может его использовать**. Resource server не проверяет кто представляет токен — только что токен валиден. Это дизайн.

Следствие: кража = полная компрометация на оставшееся TTL токена. Короткие TTL (5–15 мин) ограничивают window, но не устраняют его. Для банкинга, healthcare и других high-value нагрузок "украденный токен работает 15 минут" — неприемлемый риск.

## DPoP — Demonstrating Proof of Possession (RFC 9449)

DPoP привязывает access-токен к key pair под контролем клиента. Клиент генерирует non-extractable key pair (WebCrypto в браузерах, OS keychain в native apps). Access-токен несёт claim `cnf` (confirmation) с JWK thumbprint (`jkt`) публичного ключа.

**На каждый API-вызов** клиент вычисляет DPoP proof JWT в HTTP заголовке `DPoP`. Payload proof содержит:
- `jti` — unique ID (сервер кеширует для replay detection, типичное окно 5–10 мин)
- `htm` — HTTP method (например `POST`)
- `htu` — HTTP target URI (например `https://api.bank.example/v1/transfer`)
- `iat` — issued-at timestamp (сервер отклоняет если вне ~60с окна)
- `ath` — SHA-256 access-токена (привязывает этот proof к этому конкретному токену)

Header proof включает публичный ключ (`jwk`). Resource server валидирует:
1. Подпись валидна (используя embedded `jwk`)
2. `ath` = SHA-256 incoming bearer токена
3. `htm` и `htu` совпадают с реальным запросом
4. `iat` в допустимом окне
5. `jti` не виден недавно (anti-replay)
6. `jwk` хешируется в `jkt` в `cnf` claim токена

Украденный токен без приватного ключа не может пройти шаги 1 или 5. API-вызов отклонён.

<div data-lesson-visual class="overflow-x-auto my-6 rounded-lg border border-border bg-surface-1 p-4 text-sm">
  <div class="mb-3 text-ink-2 text-xs uppercase tracking-wider">Структура DPoP proof на каждый API-вызов</div>
  <div class="space-y-2 text-xs font-mono">
    <div class="rounded bg-surface-2 border border-border p-2">
      <div class="text-ink-2 mb-1">DPoP header (JWT подписан приватным ключом клиента)</div>
      <div>header: &#123; alg: ES256, jwk: &#123;публичный ключ&#125; &#125;</div>
      <div>payload: &#123; jti: uuid, htm: "POST", htu: "https://api.bank.../transfer", iat: 1716230400, ath: sha256(access_token) &#125;</div>
    </div>
    <div class="rounded bg-surface-2 border border-border p-2">
      <div class="text-ink-2 mb-1">cnf claim access-токена</div>
      <div>cnf: &#123; jkt: "base64url(SHA-256(public_key))" &#125;</div>
    </div>
    <div class="text-ink-2">Resource server: верифицировать подпись proof, проверить ath, htm+htu, jki = jkt в токене. Украденный токен без приватного ключа проваливает шаг 1.</div>
  </div>
</div>

<SequenceDiagram
  label="DPoP sender-constraining на каждый API-вызов: access-токен уже несёт cnf.jkt, привязанный к ключу клиента; клиент подписывает свежий DPoP proof приватным ключом и отправляет токен плюс proof; resource server верифицирует подпись proof и что ключ proof хешируется в jkt токена, поэтому украденный bearer-токен без ключа отклоняется."
  hue="var(--d-crypto)"
  actors={["Client", "Resource API"]}
  messages={[
    { from: "Client", to: "Client", label: "1. подписать DPoP proof (jti, htm, htu, iat, ath) приватным ключом" },
    { from: "Client", to: "Resource API", label: "2. запрос + Authorization: token + DPoP: proof" },
    { from: "Resource API", to: "Resource API", label: "3. проверить подпись proof, ath, htm/htu, jti; hash jwk == cnf.jkt токена" },
    { from: "Resource API", to: "Client", label: "4. binding OK → защищённые данные" },
  ]}
  caption="Приватный ключ никогда не покидает клиента, поэтому токен, exfiltrate-нутый через XSS или supply-chain скрипт, нельзя сопоставить с валидным proof — шаг 3 проваливается и вызов отклоняется."
/>

## mTLS-bound токены (RFC 8705)

mTLS (mutual TLS) привязывает access-токен к X.509 сертификату клиента. TLS handshake сам представляет сертификат; resource server верифицирует, что TLS-presented сертификат совпадает с `cnf.x5t#S256` claim в токене.

**DPoP vs mTLS — практическое отличие для браузерных SPA:**
- Браузеры не раскрывают TLS client сертификаты JavaScript. mTLS требует выбора сертификата на уровне OS / браузера — непрактично для SPA, где ключ должен жить в JS.
- DPoP использует WebCrypto API для хранения non-extractable key pair в browser JS контексте. Это работает; ключ никогда не покидает браузер, но управляется SPA кодом.
- **FAPI 2.0** (Open Banking профиль) требует sender-constrained токены через mTLS или DPoP. Для нового browser-based финтека в 2026 DPoP — дефолтный выбор. mTLS — выбор для backend service-to-service, где client сертификаты управляемы.

## PAR и JAR: укрепление authorize-запроса

**Pushed Authorization Requests (PAR, RFC 9126):** Вместо того чтобы все параметры были в browser redirect URL, клиент POST-ит их на `/par` endpoint через TLS-authenticated back channel сначала. Сервер возвращает short-lived `request_uri`. Клиент редиректит браузер на `/authorize?request_uri=...` — никаких параметров в URL.

Преимущества: параметры не в browser history, referrer header или screenshots. Authorization server валидирует запрос до того, как браузер вовлечён.

**JAR (JWT Authorization Requests, RFC 9101):** Клиент упаковывает authorize параметры как signed JWT. Auth server валидирует подпись до обработки. В сочетании с PAR весь запрос off-URL и integrity-protected.

Оба PAR + JAR требуются FAPI 2.0. Consumer OAuth обычно скипает их; банкинг и healthcare требуют.

<Quiz
  id="05-q1"
  pieceSlug="05-sender-constrained-tokens"
  lang="ru"
  question="Почему DPoP предпочтительнее mTLS для браузерных SPA, хотя оба FAPI 2.0 compliant?"
  choices={[
    { label: "DPoP значительно быстрее mTLS", misconception: "Throughput сравним; выбор о том, где живёт ключ." },
    { label: "Браузеры не дают JavaScript доступ к TLS client cert, но WebCrypto позволяет JavaScript держать non-extractable ключ — DPoP работает в браузере, mTLS нет", correct: true },
    { label: "mTLS deprecated в FAPI 2.0", misconception: "mTLS-bound токены (RFC 8705) актуальны и требуются FAPI 2.0 наряду с DPoP." },
    { label: "DPoP требуется GDPR", misconception: "Ни GDPR ни FAPI не указывают DPoP над mTLS; deployability драйвит выбор." },
  ]}
/>

<Quiz
  id="05-q2"
  pieceSlug="05-sender-constrained-tokens"
  lang="ru"
  question="DPoP proof валиден, но jti был виден сервером 2 минуты назад. Что должен делать сервер?"
  choices={[
    { label: "Принять — jti replay важен только после 10 минут", misconception: "Replay-cache окно обычно 5–10 минут; jti, виденный в этом окне — replay попытка независимо от timestamp." },
    { label: "Отклонить — jti replay detected, атакующий может replay-ить захваченный proof", correct: true },
    { label: "Принять и выдать новый access-токен", misconception: "Replayed DPoP proof должен быть отклонён независимо от статуса токена." },
    { label: "Залогировать warning, но продолжить", misconception: "Логирование с продолжением допускает replay атаки — спецификация требует отклонения." },
  ]}
/>

<DragOrder
  id="05-drag-1"
  pieceSlug="05-sender-constrained-tokens"
  lang="ru"
  prompt="Поставь шаги DPoP валидации resource server-а в порядке:"
  items={[
    "Распарсить DPoP JWT header и извлечь embedded публичный ключ (jwk)",
    "Верифицировать подпись DPoP proof используя embedded публичный ключ",
    "Верифицировать ath claim = SHA-256 incoming access-токена",
    "Верифицировать htm и htu совпадают с реальным HTTP method и URI этого запроса",
    "Верифицировать iat в допустимом time window (~60 секунд)",
    "Проверить jti не виден в replay cache (5–10 минутное окно)",
    "Извлечь cnf.jkt из access-токена и верифицировать jwk proof хешируется в это значение",
  ]}
/>

</Explanation>

<KeyTakeaway>Bearer-токены — украдены и использованы. DPoP привязывает токен к приватному ключу, который никогда не покидает клиента — украденный токен без него бесполезен. FAPI 2.0 требует sender-constrained токены для high-value нагрузок; DPoP — выбор для браузера, mTLS — для backend сервисов.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-sender-constrained-tokens-retrieval"
  lang="ru"
  questions={[
    { q: "Что делает DPoP proof бесполезным для украденного access-токена?", a: "Proof должен быть подписан приватным ключом клиента, который никогда не покидает клиента. Proof содержит ath = SHA-256(access_token), привязывая его к конкретному токену. Resource server проверяет и подпись (требуя приватный ключ), и ath (связывая proof с токеном). Без приватного ключа атакующий не может сгенерировать новый валидный proof для украденного токена." },
    { q: "Что PAR (Pushed Authorization Requests) добавляет к OAuth flow?", a: "Вместо параметров /authorize в redirect URL, клиент POST-ит их на /par endpoint через authenticated TLS сначала. Сервер возвращает request_uri. Browser redirect несёт только этот short-lived request_uri. Преимущества: никаких параметров в browser history/referrers/screenshots, сервер валидирует запрос до вовлечения браузера. Требуется FAPI 2.0." },
    { q: "Когда выбирать mTLS вместо DPoP для sender-constraining токенов?", a: "mTLS — лучший выбор для backend service-to-service сценариев, где TLS client сертификаты управляемы (mutual TLS между сервисами). DPoP — выбор для browser SPA и mobile apps, где client ключ должен жить в JavaScript (WebCrypto) или mobile OS keychain, не в TLS certificate stack, который браузеры не раскрывают JS." },
  ]}
/>

<Recap lang="ru">
Bearer-токены — legacy дефолт: любой держатель может их использовать. DPoP (RFC 9449) привязывает токен к client-held приватному ключу — каждый API-вызов требует fresh proof JWT, подписанного этим ключом, содержащего хеш access-токена, request method и target URI. Украденные токены без ключа отклоняются. mTLS-bound токены (RFC 8705) достигают той же привязки через X.509 client сертификаты. FAPI 2.0 требует одно или другое для банкинга, healthcare и регулируемых AI нагрузок. DPoP — browser выбор, потому что WebCrypto предоставляет non-extractable ключи в JavaScript; mTLS — выбор backend сервисов. PAR и JAR укрепляют сам /authorize запрос, перемещая параметры с URL. Теперь, когда встретишь отклонение DPoP proof с `invalid_dpop_proof`, будешь знать какой из шести шагов валидации проверять первым — и почему токен на сервере атакующего без приватного ключа навсегда бесполезен.
</Recap>
