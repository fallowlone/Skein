---
concepts:
- pkce
- redirect-uri
- id-token-validation
- refresh-rotation
- sender-constrained
- audience-confusion
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://datatracker.ietf.org/doc/html/rfc9700
- https://openid.net/specs/openid-connect-core-1_0.html
- https://datatracker.ietf.org/doc/html/rfc9449
status: ready
summary: "Тест с выбором на синтез по юниту OAuth/OIDC — PKCE, точное совпадение redirect_uri, проверки id_token, ротация refresh token, sender-constrained-токены и атаки на аудиторию."
title: 'OAuth/OIDC: тест с выбором ответа'
track: security
unit: 02-oauth-oidc
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает решение в реальном инциденте или ревью дизайна — не определение для заучивания, а обязательную проверку, которую надо защитить. Тезис юнита: пропусти любую одну — и ты выкатил CVE.
</Hook>

<Goal lang="ru">
Убедись, что связываешь PKCE, точное совпадение redirect_uri, восемь проверок id_token, ротацию refresh token, sender-constrained-токены и валидацию аудитории в одну модель безопасности «полного набора».
</Goal>

<Quiz
  id="02-oauth-oidc-choice-1"
  lessonSlug="02-oauth-oidc"
  lang="ru"
  question="Твой OAuth-клиент — конфиденциальное серверное приложение с client_secret. Ревьюер спрашивает, зачем ты всё равно шлёшь PKCE. Какой ответ верный?"
  choices={[
    { label: "OAuth 2.1 требует PKCE для каждого клиента. Он защищает от перехвата authorization code даже когда client_secret отсутствует, утёк или обойдён — это эшелонированная защита поверх секрета, а не его замена.", correct: true },
    { label: "PKCE заменяет client_secret, так что после внедрения PKCE секрет можно убрать.", misconception: "Для конфиденциальных клиентов PKCE идёт в дополнение к client_secret, а не вместо него. Оба работают вместе." },
    { label: "PKCE нужен только нативным и SPA-клиентам, которые не могут хранить секрет.", misconception: "OAuth 2.1 требует PKCE для ВСЕХ клиентов, включая конфиденциальные, как раз чтобы утёкший секрет сам по себе не позволял обменять перехваченный code." },
    { label: "Он делает ответ с токенами короче и быстрее.", misconception: "PKCE добавляет около 50 байт на запрос и проверку хэша; это мера безопасности, а не оптимизация размера или скорости." },
  ]}
/>

<Quiz
  id="02-oauth-oidc-choice-2"
  lessonSlug="02-oauth-oidc"
  lang="ru"
  question="Команда регистрирует redirect_uri как префиксное совпадение (https://app.example/callback*), чтобы поддержать динамические пути возврата. Почему это уязвимость?"
  choices={[
    { label: "OAuth 2.1 требует точного совпадения redirect_uri. Префикс или wildcard позволяет атакующему завести поток в любой open-redirect или неожиданный путь под доменом и вывести authorization code на свой сервер.", correct: true },
    { label: "Префиксное совпадение нормально, пока хост верный — значим только хост.", misconception: "Хоста недостаточно. Open-redirect на разрешённом пути под этим хостом уводит code за домен; закрывает это только посимвольное точное совпадение." },
    { label: "Wildcard ломает PKCE, потому что challenge привязан к пути.", misconception: "PKCE привязывает code к verifier, а не к пути. Правило redirect_uri — независимая обязательная проверка, которую префикс обходит." },
    { label: "Это важно только нативным приложениям с кастомными URI-схемами.", misconception: "Точное совпадение требуется всем клиентам. Нативные приложения дополнительно используют loopback или зарегистрированные private-use-схемы, но и веб-приложения обязаны точно совпадать." },
  ]}
/>

<Quiz
  id="02-oauth-oidc-choice-3"
  lessonSlug="02-oauth-oidc"
  lang="ru"
  question="OIDC-клиент проверяет подпись id_token, iss, exp и nonce — но берёт алгоритм проверки из собственного заголовка alg токена. В чём изъян?"
  choices={[
    { label: "Доверие к alg токена открывает algorithm-confusion: атакующий ставит alg в none или в HS256, используя публичный ключ как HMAC-секрет, и уводит валидатор на путь проверки, который контролирует. Закрепи ожидаемый алгоритм (RS256/ES256) на сервере.", correct: true },
    { label: "Изъяна нет — заголовок alg для того и есть, чтобы валидатор знал, какой алгоритм применить.", misconception: "Заголовок alg — это управляемый атакующим ввод. Валидатор обязан закрепить ожидаемый алгоритм из конфигурации и игнорировать claim токена." },
    { label: "Настоящий пробел в том, что не указан aud, а alg безвреден.", misconception: "aud действительно обязателен, но доверие к alg эксплуатируется независимо — через alg=none и подмену RS256 на HS256." },
    { label: "alg важен только для access token, а не для id_token.", misconception: "Algorithm-confusion применима к любому JWT, который ты проверяешь, включая id_token. Проверка подписи надёжна ровно настолько, насколько закреплён алгоритм." },
  ]}
/>

<Quiz
  id="02-oauth-oidc-choice-4"
  lessonSlug="02-oauth-oidc"
  lang="ru"
  question="Refresh token утёк через access-лог. Ротация refresh token включена. Каково максимальное окно вреда?"
  choices={[
    { label: "До следующего обновления легитимным клиентом: он предъявляет уже использованный RT1, сервер видит replay, отзывает всю цепочку и форсит повторную аутентификацию. Окно равно интервалу ротации — минуты-часы, а не абсолютный срок жизни.", correct: true },
    { label: "До абсолютного истечения refresh token, обычно это недели или месяцы.", misconception: "Ротация помечает старый токен использованным при каждом обновлении, поэтому окно абсолютного срока жизни больше не действует — обнаружение replay заканчивает его гораздо раньше." },
    { label: "Навсегда — refresh token нельзя отозвать после выпуска.", misconception: "Refresh token отзываемы, а ротация автоматизирует обнаружение: replay схлопывает всю цепочку при следующем легитимном использовании." },
    { label: "Ровно пять минут, зафиксировано спецификацией OAuth 2.1.", misconception: "Фиксированного правила в пять минут нет. Окно зависит от того, когда легитимный клиент обновится в следующий раз и запустит обнаружение replay." },
  ]}
/>

<Quiz
  id="02-oauth-oidc-choice-5"
  lessonSlug="02-oauth-oidc"
  lang="ru"
  question="Supply-chain-атака внедряет JavaScript в твой SPA и выводит access token из памяти. Токен привязан через DPoP. Почему украденный токен не проходит на resource server?"
  choices={[
    { label: "Каждый вызов API требует свежий DPoP proof JWT, подписанный приватным ключом клиента, чей thumbprint закреплён в claim cnf.jkt токена. Ключ — неизвлекаемый материал WebCrypto: токен у атакующего есть, а валидный proof он создать не может.", correct: true },
    { label: "Resource server сверяет IP вызывающего с тем, что получил токен.", misconception: "DPoP не привязывается к IP. Он привязывается к удерживаемому клиентом приватному ключу через подпись proof и проверку thumbprint cnf.jkt." },
    { label: "DPoP шифрует access token, чтобы JavaScript не мог его прочитать.", misconception: "DPoP не шифрует токен; токен могут украсть. Он привязывает ИСПОЛЬЗОВАНИЕ токена к владению приватным ключом, которого у атакующего нет." },
    { label: "Короткий TTL сам по себе делает украденный токен бесполезным.", misconception: "Короткий TTL лишь ограничивает окно для bearer-токена. Именно DPoP делает украденный токен непригодным сразу, даже внутри его TTL." },
  ]}
/>

<Quiz
  id="02-oauth-oidc-choice-6"
  lessonSlug="02-oauth-oidc"
  lang="ru"
  question="Один authorization server выпускает токены для API-A и API-B. Валидатор API-B делает: если token.aud содержит любой известный client_id, принять. Почему это уязвимость audience-confusion?"
  choices={[
    { label: "Токен, выпущенный для API-A, предъявляется API-B и принимается, потому что проверка сверяет принадлежность к множеству известных id, а не равенство собственному захардкоженному идентификатору API-B. Фикс: aud должен содержать ожидаемое значение ЭТОГО resource server, с регрессионным тестом, отклоняющим aud равный other-api.", correct: true },
    { label: "Два API должны делить client_id, и это настоящий баг.", misconception: "client_id идентифицирует клиента, а не resource server. Дефект — в проверке aud по множеству вместо захардкоженной ожидаемой аудитории." },
    { label: "Общие токены всегда просрочены, так что их и так отклонят.", misconception: "Истечение тут ни при чём. Живой токен для API-A проходит сломанную проверку API-B и даёт межсервисный доступ на весь свой TTL." },
    { label: "Фикс — добавить introspection, что снимает необходимость проверять aud.", misconception: "Introspection сообщает об отзыве и активности, но не заменяет валидацию аудитории — ты всё равно обязан проверить, что aud равен идентификатору этого сервера." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одно правило полного набора: PKCE привязывает code к клиенту, точное совпадение redirect_uri удерживает code в пределах домена, восемь проверок id_token (с закреплённым на сервере алгоритмом) аутентифицируют пользователя, ротация refresh token превращает кражу в сигнал тревоги, sender-constrained-токены (DPoP/mTLS) обезвреживают кражу bearer-токена, а валидация аудитории по захардкоженному идентификатору блокирует межсервисную confusion. Каждый реальный взлом OAuth — Facebook, Slack, GitHub Enterprise, Azure AD — сводится ровно к одной из этих проверок, пропущенной или вывернутой.
</Recap>
