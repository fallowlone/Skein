---
concepts:
- pkce
- id-token-validation
- refresh-rotation
- dpop
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://datatracker.ietf.org/doc/html/rfc7636
- https://openid.net/specs/openid-connect-core-1_0.html
- https://datatracker.ietf.org/doc/html/rfc9449
status: ready
summary: "Читай реальный код OAuth/OIDC — обмен PKCE, валидацию id_token, ротацию refresh token и привязку DPoP — находи пропущенную или вывернутую обязательную проверку и выбирай фикс."
title: 'OAuth/OIDC: код-ревью'
track: security
unit: 02-oauth-oidc
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Каждая CVE в OAuth — это одна пропущенная или вывернутая проверка, спрятанная в коде, который выглядит верным. Читай каждый сниппет как security-ревьюер, находи пропущенный обязательный шаг и выбирай фикс, который закрывает реальную дыру.
</Hook>

<Goal lang="ru">
Отработай цикл ревью, который ловит баги OAuth до выката: читай путь обмена или валидации, определи, какая обязательная проверка отсутствует или неверна, и применяй точный фикс, а не размытое «усиление».
</Goal>

## Сниппет 1 — обмен токенов

```js
// обработчик callback: IdP перенаправляет назад с ?code=...&state=...
async function handleCallback(req, session) {
  const { code, state } = req.query;
  const res = await fetch(TOKEN_ENDPOINT, {
    method: "POST",
    body: new URLSearchParams({
      grant_type: "authorization_code",
      code,
      client_id: CLIENT_ID,
      redirect_uri: REDIRECT_URI,
      code_verifier: session.codeVerifier,
    }),
  });
  return res.json(); // токены
}
```

<Quiz
  id="02-oauth-oidc-code-1"
  lessonSlug="02-oauth-oidc"
  lang="ru"
  question="PKCE code_verifier передан верно. Какая обязательная проверка отсутствует в этом callback и что она позволяет атакующему?"
  choices={[
    { label: "Параметр state нигде не сравнивается со значением, сохранённым в сессии. Без этой проверки handler открыт для login CSRF: атакующий шлёт жертве сфабрикованный callback-URL со своим code и молча логинит жертву в аккаунт атакующего. Фикс: отклонять, если state !== session.state, затем удалить сохранённое значение.", correct: true },
    { label: "Ничего не пропущено — PKCE уже покрывает всё, что дал бы параметр state.", misconception: "PKCE привязывает code к этому клиенту; он не доказывает, что этот callback относится к потоку, который начал этот пользователь. State — независимая анти-CSRF-проверка, и её здесь нет." },
    { label: "Баг в отправке code_verifier — надо слать code_challenge на /token.", misconception: "Запрос на /token обязан нести исходный code_verifier, чтобы сервер захэшировал и сравнил его с сохранённым challenge. Отправка challenge тут полностью сломала бы PKCE." },
    { label: "redirect_uri надо убрать из запроса на токен.", misconception: "redirect_uri обязан присутствовать и точно совпадать с зарегистрированным значением, чтобы сервер мог его подтвердить; его удаление ослабляет поток, а не чинит пропущенную проверку state." },
  ]}
/>

## Сниппет 2 — валидация id_token

```js
import jwt from "jsonwebtoken";

function verifyIdToken(idToken, jwks) {
  const header = decodeHeader(idToken);
  const key = jwks.find((k) => k.kid === header.kid);
  // верифицируем тем алгоритмом, что указан в самом токене
  const claims = jwt.verify(idToken, key.pem, { algorithms: [header.alg] });
  if (claims.iss !== EXPECTED_ISS) throw new Error("bad iss");
  if (claims.exp < Date.now() / 1000) throw new Error("expired");
  return claims;
}
```

<Quiz
  id="02-oauth-oidc-code-2"
  lessonSlug="02-oauth-oidc"
  lang="ru"
  question="Этот валидатор проверяет iss и exp. Какие две обязательные проверки сломаны или отсутствуют и почему это важно?"
  choices={[
    { label: "Он доверяет собственному header.alg токена (algorithm-confusion — закрепи algorithms: [\"RS256\"] на сервере, не читай из токена) и никогда не проверяет aud или nonce (audience-confusion и replay id_token). Добавь закреплённый алгоритм, проверь, что aud содержит твой client_id, и что nonce равен отправленному на /authorize.", correct: true },
    { label: "Единственная проблема — exp в секундах; должно быть в миллисекундах.", misconception: "Деление Date.now() на 1000 уже переводит в секунды под exp. Реальные дефекты — доверие к header.alg и пропуск aud и nonce." },
    { label: "Не надо искать ключ по kid; надо пробовать каждый ключ из JWKS.", misconception: "Выбор ключа по kid верен и обязателен. Дефекты — доверие к алгоритму и пропущенные проверки aud и nonce, а не поиск ключа." },
    { label: "Всё в порядке, пока IdP подписывает только RS256.", misconception: "Токен фабрикует атакующий, а не IdP. Доверяя header.alg, валидатор можно увести на alg=none или HS256 с публичным ключом в роли секрета, независимо от того, чем обычно подписывает IdP." },
  ]}
/>

## Сниппет 3 — ротация refresh

```js
async function refresh(presentedRT) {
  const record = await db.refreshTokens.find(presentedRT);
  if (!record) throw new Error("unknown refresh token");
  // выпускаем свежую пару токенов
  const next = await mintTokens(record.userId);
  await db.refreshTokens.insert(next.refreshToken, { userId: record.userId });
  return next;
}
```

<Quiz
  id="02-oauth-oidc-code-3"
  lessonSlug="02-oauth-oidc"
  lang="ru"
  question="Это выпускает новый refresh token на каждый вызов. Почему оно не обнаруживает украденный refresh token и каков фикс?"
  choices={[
    { label: "Оно никогда не помечает предъявленный токен использованным и не обнаруживает повторное использование. Украденный RT1 и легитимный RT1 оба работают вечно — это не ротация, а просто выпуск. Фикс: пометить RT1 использованным и связать с преемником; если уже использованный токен предъявлен снова, считать это replay и отозвать всю цепочку.", correct: true },
    { label: "Баг в том, что оно вообще выпускает новый refresh token — надо переиспользовать RT1.", misconception: "Переиспользование одного долгоживущего refresh token — это ровно то, что ротация заменяет. Фикс — инвалидировать старый токен и обнаруживать его повторное использование, а не перестать выпускать новые." },
    { label: "Надо проверять подпись access token перед обновлением.", misconception: "Обновление использует refresh token, а не access token. Дефект — отсутствие пометки об использовании и обнаружения replay у самого refresh token." },
    { label: "Фикс — просто более короткий TTL refresh token.", misconception: "Короткий TTL ограничивает окно, но украденный токен всё равно работает до истечения незамеченным. Именно ротация с обнаружением replay вскрывает кражу." },
  ]}
/>

## Сниппет 4 — проверка DPoP proof

```js
function verifyDpop(proofJwt, accessToken, req) {
  const { header, payload } = parse(proofJwt);
  verifySignature(proofJwt, header.jwk); // подписан встроенным ключом
  if (payload.htm !== req.method) throw new Error("htm");
  if (payload.htu !== req.url) throw new Error("htu");
  if (Math.abs(now() - payload.iat) > 60) throw new Error("iat skew");
  return true;
}
```

<Quiz
  id="02-oauth-oidc-code-4"
  lessonSlug="02-oauth-oidc"
  lang="ru"
  question="Это проверяет подпись proof, htm, htu и iat. Каких двух привязок DPoP не хватает и какую атаку открывает каждый пробел?"
  choices={[
    { label: "Оно не проверяет ath (SHA-256 access token) и то, что header.jwk хэшируется в cnf.jkt токена, и не кэширует jti от replay. Без ath/jkt любой валидный с виду proof может прикрыть другой токен; без jti перехваченный proof можно повторить. Фикс: проверь, что ath равен SHA-256(accessToken), что thumbprint(header.jwk) равен token.cnf.jkt, и отклоняй jti, виденный в окне 5–10 мин.", correct: true },
    { label: "Ничего не пропущено — подпись, htm, htu и iat полностью привязывают proof.", misconception: "Они доказывают, что proof свежий и для этого запроса, но не то, что он относится к ЭТОМУ токену или ЭТОМУ ключу клиента. ath, совпадение thumbprint cnf.jkt и кэш jti от replay — недостающие привязки." },
    { label: "Баг в проверке встроенным header.jwk; надо использовать JWKS.", misconception: "DPoP proof подписан собственным ключом клиента, который несётся в header.jwk и привязан через cnf.jkt — не JWKS-ключом IdP. Дефект — отсутствие проверок ath, jkt и jti." },
    { label: "htu надо игнорировать, потому что URL меняются прокси.", misconception: "htu привязывает proof к целевому URI и должен проверяться (при необходимости с нормализацией). Его отбрасывание ослабляет proof, а не чинит отсутствующие проверки ath/jkt/jti." },
  ]}
/>

<Recap lang="ru">
Каждый сниппет ломался так же, как ломается реальный код OAuth — пропуском или выворачиванием одной обязательной проверки при верном виде: отсутствие сравнения state открывает login CSRF; доверие к header.alg и пропуск aud/nonce открывают algorithm-confusion, audience-confusion и replay; выпуск без пометки об использовании убивает ротацию; а DPoP-валидатор без ath, совпадения thumbprint cnf.jkt и кэша jti от replay на деле не sender-constrained. Проверяй полный набор: читай путь, назови отсутствующую проверку, примени точный фикс.
</Recap>
