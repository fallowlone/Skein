---
concepts:
- pkce
- authorization-code
- code-verifier
- code-challenge
- state
- nonce
- redirect-uri
deepensInto:
- security/02-oauth-oidc/03-id-token-validation
- security/02-oauth-oidc/04-refresh-tokens-and-scopes
estMin: 10
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- security/02-oauth-oidc/01-what-is-oauth
slug: 02-auth-code-pkce
sources:
- https://datatracker.ietf.org/doc/html/rfc7636
- https://datatracker.ietf.org/doc/html/rfc9700
- https://auth0.com/docs/get-started/authentication-and-authorization-flow/authorization-code-flow-with-pkce
spiral:
- statefulness
- encapsulation
status: ready
summary: Как обязательный PKCE в OAuth 2.1 предотвращает перехват кода, почему exact-match
  redirect URI блокирует open-redirect exfiltration, и от чего защищают state и nonce.
title: Authorization code flow с PKCE
track: security
unit: 02-oauth-oidc
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Атакующий мониторит исходящие запросы мобильного приложения и перехватывает one-time authorization code из redirect URL. Без PKCE он обменивает код на токены и захватывает аккаунт. PKCE — это замок, делающий украденный код бесполезным. За следующие десять минут ты увидишь точно, как этот замок работает, как выглядят open-redirect и CSRF векторы без него, и какие три параметра OAuth 2.1 делает обязательными.
</Hook>

<Crux>PKCE привязывает код к клиенту. Украденный код бесполезен — verifier никогда не покидает клиента.</Crux>

<Explanation>

## Как работает PKCE — шаг за шагом

PKCE (Proof Key for Code Exchange, RFC 7636) добавляет криптографический challenge к authorization code flow. OAuth 2.1 мандатит его для каждого клиента, включая confidential server-side приложения.

**Перед редиректом:**
1. Клиент генерирует `code_verifier` — случайную URL-safe строку 43–128 символов (криптографически случайную).
2. Клиент вычисляет `code_challenge = base64url(SHA-256(code_verifier))`.
3. Клиент отправляет `code_challenge` (но НЕ `code_verifier`) в authorization server в `/authorize` запросе.

**После согласия пользователя:**
4. Authorization server возвращает one-time `code` в редиректе. Сохраняет `code_challenge` рядом с кодом.

**Обмен на токены:**
5. Клиент POST-ит на `/token` с `code` И оригинальным `code_verifier`.
6. Authorization server хеширует `code_verifier`, сравнивает с сохранённым `code_challenge`. Совпадение → токены выданы. Несовпадение → запрос отклонён.

Атакующий, перехвативший код на шаге 4, не может его обменять — ему нужен `code_verifier`, который никогда не передавался по сети.

<div data-lesson-visual class="overflow-x-auto my-6 rounded-lg border border-border bg-surface-1 p-4 text-sm font-mono">
  <div class="mb-3 text-ink-2 text-xs uppercase tracking-wider">Привязка кода через PKCE</div>
  <div class="space-y-2">
    <div class="flex gap-2 items-start">
      <span class="shrink-0 w-6 h-6 rounded-full bg-blue-500 text-white text-xs flex items-center justify-center font-bold">1</span>
      <span>Клиент генерирует <code>code_verifier</code> (случайный 43–128 chars, остаётся локально)</span>
    </div>
    <div class="flex gap-2 items-start">
      <span class="shrink-0 w-6 h-6 rounded-full bg-blue-500 text-white text-xs flex items-center justify-center font-bold">2</span>
      <span>Клиент вычисляет <code>code_challenge = base64url(SHA-256(verifier))</code></span>
    </div>
    <div class="flex gap-2 items-start">
      <span class="shrink-0 w-6 h-6 rounded-full bg-blue-500 text-white text-xs flex items-center justify-center font-bold">3</span>
      <span><code>/authorize</code> несёт только <code>code_challenge</code> (хеш, verifier остаётся секретом)</span>
    </div>
    <div class="flex gap-2 items-start">
      <span class="shrink-0 w-6 h-6 rounded-full bg-green-500 text-white text-xs flex items-center justify-center font-bold">4</span>
      <span>Auth server сохраняет challenge → выдаёт code в redirect URL</span>
    </div>
    <div class="flex gap-2 items-start">
      <span class="shrink-0 w-6 h-6 rounded-full bg-orange-500 text-white text-xs flex items-center justify-center font-bold">5</span>
      <span><code>/token</code> несёт code + <code>code_verifier</code> — auth server хеширует, сравнивает, выдаёт токены</span>
    </div>
  </div>
  <div class="mt-3 text-ink-2 text-xs border-t border-border pt-2">Атакующий получает code на шаге 4, но не verifier → вызов /token отклонён.</div>
</div>

<SequenceDiagram
  label="Authorization code flow с PKCE: клиент генерирует verifier и его хешированный challenge, браузер несёт challenge на /authorize, пользователь аутентифицируется, authorization server возвращает one-time код через браузер, и клиент обменивает код плюс оригинальный verifier на /token, получая токены."
  hue="var(--d-crypto)"
  actors={["Browser", "Client", "Auth Server"]}
  messages={[
    { from: "Client", to: "Browser", label: "1. сделать verifier→challenge; redirect" },
    { from: "Browser", to: "Auth Server", label: "2. GET /authorize (code_challenge, state, nonce)" },
    { from: "Auth Server", to: "Browser", label: "3. login + consent prompt" },
    { from: "Browser", to: "Auth Server", label: "4. аутентификация + одобрить scope" },
    { from: "Auth Server", to: "Browser", label: "5. redirect: code + state" },
    { from: "Browser", to: "Client", label: "6. доставить code" },
    { from: "Client", to: "Auth Server", label: "7. POST /token (code + code_verifier)" },
    { from: "Auth Server", to: "Client", label: "8. проверить challenge → access + ID токены" },
  ]}
  caption="code_verifier не доходит до authorization server до финального вызова /token, и только по собственному back channel клиента. Код, украденный на шаге 5, нельзя обменять без него."
/>

## Правило exact-match redirect URI

OAuth 2.1 требует, чтобы `redirect_uri` в token request точно совпадал с pre-registered URI — никаких wildcard, префиксов пути, partial match.

Без этого правила: атакующий может триггернуть flow с `redirect_uri=https://yourapp.com/comments?next=https://attacker.com`. Если у твоего приложения есть open-redirect на `/comments`, authorization code оказывается на сервере атакующего. Exact-match закрывает это: URI должен совпадать character-for-character с зарегистрированным значением.

Для native apps, где нельзя надёжно зарегистрировать URL (deep links, custom URI schemes), OAuth 2.1 + RFC 8252 требуют `http://127.0.0.1:RANDOM_PORT` (loopback) или private-use URI scheme, зарегистрированный с OS.

## State и nonce: два отдельных anti-attack токена

**State** — anti-CSRF OAuth: клиент генерирует случайное значение на `/authorize`, сервер эхо-нит его в редиректе, клиент проверяет совпадение. Без state атакующий может отправить жертве crafted `/authorize` URL, указывающий на redirect атакующего — при клике браузер жертвы тихо завершает flow под аккаунтом атакующего.

**Nonce** — anti-replay OIDC: клиент отправляет случайное значение в `/authorize`; IdP embed-ит его внутрь `id_token`. Клиент проверяет, что nonce в токене совпадает с отправленным. Без nonce атакующий, захвативший `id_token` из другого канала, может replay-нуть его в другую сессию.

Оба per-request случайные. Оба валидируются server-side. Ни один не optional в современных flow.

<Quiz
  id="02-q1"
  pieceSlug="02-auth-code-pkce"
  lang="ru"
  question="Зачем OAuth 2.1 требует PKCE для ВСЕХ клиентов, включая confidential server-side?"
  choices={[
    { label: "PKCE заменяет client_secret", misconception: "PKCE добавляется к client_secret для confidential клиентов, не заменяет." },
    { label: "Защищает от перехвата authorization code даже если client_secret отсутствует или утёк", correct: true },
    { label: "Требование GDPR", misconception: "GDPR не требует PKCE. OAuth 2.1 мандатит его для defense in depth." },
    { label: "Делает токены короче", misconception: "PKCE добавляет ~50 байт на запрос; это не оптимизация размера." },
  ]}
/>

<Quiz
  id="02-q2"
  pieceSlug="02-auth-code-pkce"
  lang="ru"
  question="Клиент валидирует подпись id_token и iss, но скипает проверку nonce. Какая атака становится возможной?"
  choices={[
    { label: "Open redirect", misconception: "Open redirect — про redirect_uri, не про nonce." },
    { label: "ID-token replay: атакующий с захваченным id_token может использовать его в другой сессии, имперсонируя жертву", correct: true },
    { label: "PKCE downgrade", misconception: "PKCE downgrade — про challenge methods, не связан с nonce." },
    { label: "Bypass token introspection", misconception: "Introspection — server-side trust механизм, не связан с nonce." },
  ]}
/>

<DragOrder
  id="02-drag-1"
  pieceSlug="02-auth-code-pkce"
  lang="ru"
  prompt="Поставь full authorization code + PKCE flow в порядке:"
  items={[
    "Клиент генерирует code_verifier и code_challenge (SHA-256 от verifier)",
    "Клиент редиректит браузер на /authorize с code_challenge, state, nonce, client_id, redirect_uri",
    "Пользователь аутентифицируется на authorization server и одобряет scope",
    "Authorization server редиректит на redirect_uri с code и state",
    "Клиент верифицирует state совпадает; POST-ит на /token с code + code_verifier",
    "Authorization server хеширует verifier, сравнивает с stored challenge, выдаёт access + ID-токены",
    "Клиент валидирует ID-токен (подпись, iss, aud, exp, nonce) и читает identity пользователя",
  ]}
/>

</Explanation>

<KeyTakeaway>PKCE привязывает код к клиенту, сгенерировавшему его — перехват бесполезен без verifier. Exact-match redirect URI блокирует open-redirect code exfiltration. State предотвращает CSRF; nonce предотвращает replay ID-токена. OAuth 2.1 требует всех трёх.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-auth-code-pkce-retrieval"
  lang="ru"
  questions={[
    { q: "Что предотвращает PKCE и как?", a: "PKCE предотвращает атаки перехвата authorization code. Клиент генерирует секретный code_verifier, отправляет только его SHA-256 хеш (code_challenge) в authorization server, затем доказывает владение на /token, отправляя оригинальный verifier. Атакующий, укравший код, не может его обменять без verifier, который никогда не передавался по сети." },
    { q: "Почему OAuth 2.1 требует exact-match redirect URI?", a: "Без exact match атакующий может скрестить authorization flow с open-redirect багом на домене приложения — редиректируя код на URL под его контролем. Exact match гарантирует, что authorization server редиректит только на URI, который разработчик явно зарегистрировал, character-for-character." },
    { q: "В чём разница между параметром state и параметром nonce?", a: "State — anti-CSRF значение для OAuth flow: клиент генерирует, сервер эхо-нит в редиректе, клиент проверяет совпадение. Nonce — anti-replay значение OIDC: клиент генерирует, IdP embed-ит в id_token, клиент проверяет nonce в id_token совпадает с отправленным. State защищает redirect flow; nonce защищает id_token." },
  ]}
/>

<Recap lang="ru">
Authorization code flow с PKCE — единственный grant type, разрешённый для user-facing приложений в OAuth 2.1. PKCE предотвращает перехват кода: verifier никогда не покидает клиента, поэтому украденный код не может быть обменян. Exact-match redirect URI предотвращает open-redirect code exfiltration. Параметры state и nonce закрывают два независимых attack window — CSRF на flow и replay на ID-токене. Все три обязательны в OAuth 2.1; пропуск любого — документированный CVE вектор. Теперь, когда ревьюишь OAuth интеграцию, сразу проверяй три вещи: включён ли PKCE с S256, зарегистрирован ли redirect URI точно символ в символ, и валидируются ли state и nonce оба на callback?
</Recap>
