---
slug: 01-what-is-oauth
lang: ru
track: security
unit: 02-oauth-oidc
order: 1
title: "Что такое OAuth и почему пароли — не ответ"
summary: "OAuth делегирует доступ без передачи паролей. Три стороны, два токена, одна модель доверия — объяснено через метафору выгульщика собак."
estMin: 7
status: ready
lessonType: topic
level: junior
prereqs:
  - security/01-owasp-modern
deepensInto:
  - security/02-oauth-oidc/02-auth-code-pkce
spiral: ["statefulness", "encapsulation"]
concepts: ["oauth", "access-token", "id-token", "authorization-server", "resource-server", "scope"]
sources:
  - https://datatracker.ietf.org/doc/html/rfc9700
  - https://openid.net/specs/openid-connect-core-1_0.html
  - https://auth0.com/docs/get-started/authentication-and-authorization-flow/authorization-code-flow-with-pkce
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";

<Hook>
Приложение для печати фото просит тебя "подключить Google Drive". Ты вводишь свой пароль Google прямо в их форму. Теперь это приложение может читать каждый файл, письмо и фото в твоём аккаунте — навсегда. Одна утечка PrintPic — и все сервисы, подключённые через Google, скомпрометированы. OAuth существует потому, что это неправильный способ делиться доступом.
</Hook>

<Crux>OAuth позволяет одному приложению действовать от имени пользователя в другом сервисе без того, чтобы пароль покидал identity provider.</Crux>

<Explanation>

## Три стороны

Каждый OAuth flow вовлекает три стороны:

1. **Клиент** (твоё приложение, например PrintPic). Хочет ограниченного доступа к ресурсу от имени пользователя. Никогда не видит пароль пользователя.
2. **Authorization server** (identity provider, например Google). Аутентифицирует пользователя, выдаёт токены. Единственная сторона, хранящая пароль.
3. **Resource server** (API, например Google Drive). Принимает токены для обработки запросов. Валидирует токены через authorization server.

<div data-lesson-visual class="overflow-x-auto my-6 rounded-lg border border-border bg-surface-1 p-4 text-sm">
  <div class="mb-3 text-ink-2 text-xs uppercase tracking-wider">Модель доверия трёх сторон OAuth</div>
  <div class="grid grid-cols-3 gap-4 text-center">
    <div class="rounded border border-border bg-surface-2 p-3">
      <div class="font-bold text-ink-1 mb-1">Клиент (App)</div>
      <div class="text-ink-2 text-xs">PrintPic — хочет загружать фото. Никогда не видит пароль. Держит access-токен.</div>
    </div>
    <div class="rounded border border-blue-400 bg-blue-50 dark:bg-blue-950 p-3">
      <div class="font-bold text-blue-700 dark:text-blue-300 mb-1">Authorization Server</div>
      <div class="text-ink-2 text-xs">Google — держит пароль. Выдаёт access + ID-токены после согласия пользователя.</div>
    </div>
    <div class="rounded border border-border bg-surface-2 p-3">
      <div class="font-bold text-ink-1 mb-1">Resource Server</div>
      <div class="text-ink-2 text-xs">Google Drive — отдаёт файлы. Валидирует access-токены. Ничего не знает о пароле.</div>
    </div>
  </div>
  <div class="mt-3 text-center text-ink-2 text-xs">Пароль живёт только на authorization server. Токены текут к двум другим сторонам.</div>
</div>

## Метафора выгульщика собак

Ты нанимаешь выгульщика собак. Ты не даёшь ему ключ от дома. Вместо этого идёшь в key kiosk (authorization server) и говоришь "Фрэнк может выгуливать собаку по вторникам 9–10". Киоск печатает Фрэнку single-use карту, открывающую только заднюю калитку, только по вторникам, только на час. Фрэнк потерял карту — без проблем, отзываешь в киоске. Карта — это **access-токен**. Задняя калитка — **resource API**.

## Два токена, два назначения

Успешный OAuth + OpenID Connect flow выдаёт два разных токена:

- **Access token** — авторизует API-вызовы. Клиент отправляет его в заголовке `Authorization: Bearer`. Говорит "держатель вправе делать X". Resource server принимает его без знания identity пользователя.
- **ID token** — доказывает кто пользователь (слой OpenID Connect). Всегда JWT. Содержит claims вроде `sub` (стабильный user ID), `email`, `name`. Клиент читает его, чтобы знать кто только что вошёл. Никогда не отправляется в API.

Access-токен отвечает: "может ли это приложение делать X?" ID-токен отвечает: "кто пользователь?"

<Quiz
  id="01-q1"
  pieceSlug="01-what-is-oauth"
  lang="ru"
  question="Почему OAuth безопаснее, чем отдать приложению username и пароль?"
  choices={[
    { label: "OAuth шифрует пароль лучше", misconception: "OAuth не шифрует пароль — он не даёт приложению вообще его увидеть." },
    { label: "Приложение никогда не видит пароль; получает short-lived scoped токен, который можно отозвать", correct: true },
    { label: "OAuth быстрее password auth", misconception: "OAuth добавляет round-trip; выгода — безопасность, не скорость." },
    { label: "OAuth заменяет пароли биометрией", misconception: "OAuth не требует биометрии — это WebAuthn / passkeys, отдельный механизм." },
  ]}
/>

<Quiz
  id="01-q2"
  pieceSlug="01-what-is-oauth"
  lang="ru"
  question="В чём разница между access-токеном и OpenID Connect ID-токеном?"
  choices={[
    { label: "Это одно и то же под разными именами", misconception: "Они выполняют разные задачи и валидируются по-разному." },
    { label: "Access-токен авторизует API-вызовы; ID-токен доказывает кто пользователь", correct: true },
    { label: "Access-токены вечные; ID-токены истекают", misconception: "Оба short-lived. Access-токены обычно короче (минуты); ID-токены длятся примерно сессию." },
    { label: "Access-токены всегда JWT; ID-токены никогда", misconception: "Оба могут быть JWT. Отличие в назначении, не в формате." },
  ]}
/>

<DragOrder
  id="01-drag-1"
  pieceSlug="01-what-is-oauth"
  lang="ru"
  prompt="Поставь шаги 'Sign in with Google' в порядке:"
  items={[
    "Пользователь кликает 'Sign in with Google' в приложении",
    "Приложение редиректит браузер на /authorize эндпоинт Google",
    "Пользователь логинится в Google и одобряет запрошенные scope",
    "Google редиректит браузер обратно в приложение с one-time кодом",
    "Сервер приложения обменивает код на access-токен и ID-токен на /token эндпоинте Google",
    "Приложение верифицирует подпись ID-токена и читает identity claims пользователя",
  ]}
/>

</Explanation>

<KeyTakeaway>OAuth держит пароль у identity provider. Приложение получает short-lived scoped токен вместо него — токен, который можно ограничить конкретными действиями и отозвать мгновенно при компрометации.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-what-is-oauth-retrieval"
  lang="ru"
  questions={[
    { q: "Назови три стороны OAuth flow и по одному предложению о каждой.", a: "Клиент (приложение — держит токены, не пароль). Authorization server (identity provider — аутентифицирует пользователя, выдаёт токены). Resource server (API — принимает токены, валидирует их)." },
    { q: "В чём разница между access-токеном и ID-токеном?", a: "Access-токен авторизует API-вызовы — отправляется в Authorization: Bearer заголовке. ID-токен доказывает кто пользователь — читается клиентом для установления identity. Никогда не отправляй ID-токен в API." },
    { q: "Почему кража токена менее катастрофична, чем кража пароля?", a: "Токены имеют scope (ограниченные действия), short-lived (минуты-часы), и отзываемы (IdP может инвалидировать мгновенно). Украденный пароль открывает каждый аккаунт, где он переиспользуется; украденный токен открывает только scoped ресурс до expiry или отзыва." },
  ]}
/>

<Recap lang="ru">
OAuth решает проблему передачи учётных данных: вместо того чтобы отдавать пароль каждому приложению, ты авторизуешь identity provider выдать short-lived scoped токен. Три стороны — клиент, authorization server, resource server — делят доверие так, что ни одна не нуждается в доверии всем остальным. OpenID Connect добавляет identity-слой через подписанный ID-токен. Украденный токен имеет scope, short-lived и отзываем; украденный пароль — нет.
</Recap>
