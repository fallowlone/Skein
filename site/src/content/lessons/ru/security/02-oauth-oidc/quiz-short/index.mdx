---
concepts:
- pkce
- redirect-uri
- id-token-validation
- jwks-rotation
- refresh-rotation
- dpop
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://datatracker.ietf.org/doc/html/rfc9700
- https://openid.net/specs/openid-connect-core-1_0.html
- https://datatracker.ietf.org/doc/html/rfc9449
status: ready
summary: "Промпты на свободное воспроизведение по юниту OAuth/OIDC. Сначала ответь своими словами, потом открой эталонный ответ и сравни."
title: 'OAuth/OIDC: тест с краткими ответами'
track: security
unit: 02-oauth-oidc
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение по памяти бьёт перечитывание. На каждый промпт сначала скажи или напиши полный ответ из головы, и только потом открывай эталон — именно усилие припоминания закрепляет механизм. Это объяснения, которые ты дал бы на ревью дизайна.
</Hook>

<Goal lang="ru">
Восстанови ключевые механизмы юнита — привязку PKCE, точное совпадение redirect_uri, проверки id_token, обработку ротации JWKS, ротацию refresh token и sender-constrained-токены — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:visible
  id="02-oauth-oidc-short-retrieval"
  lang="ru"
  questions={[
    { q: "Что предотвращает PKCE и как именно работает привязка от начала до конца?", a: "PKCE предотвращает перехват authorization code. До редиректа клиент генерирует случайный code_verifier (43–128 символов) и отправляет на /authorize только его SHA-256-хэш — code_challenge. Authorization server хранит challenge рядом с одноразовым code. На /token клиент шлёт code плюс исходный code_verifier; сервер хэширует verifier и сравнивает с сохранённым challenge — совпало, выдаёт токены; нет, отклоняет. Атакующий, перехвативший code в редиректе, не может его обменять, потому что verifier никогда не шёл по сети. OAuth 2.1 требует PKCE для каждого клиента, включая конфиденциальные, чтобы утёкший client_secret сам по себе не обменял украденный code." },
    { q: "Почему OAuth 2.1 требует точного совпадения redirect_uri и какую атаку открывает префиксное совпадение?", a: "redirect_uri в запросе на токен должен посимвольно совпадать с заранее зарегистрированным значением — без wildcard и префиксов путей. При префиксном или wildcard-совпадении атакующий заводит поток, чей redirect_uri указывает на open-redirect или неожиданный путь под легитимным доменом; authorization code попадает на сервер атакующего, и он меняет его на токены. Точное совпадение означает, что authorization server редиректит только на URI, который разработчик явно зарегистрировал. Нативные приложения, которые не могут зарегистрировать фиксированный URL, используют loopback (http://127.0.0.1 на случайном порту) или зарегистрированную в ОС private-use-схему." },
    { q: "Перечисли обязательные проверки id_token и назови атаку, которую блокирует каждая.", a: "Распарсить JWT, затем: прочитать kid из заголовка, чтобы выбрать ключ; достать этот публичный ключ из JWKS; проверить подпись закреплённым на сервере алгоритмом (никогда не доверять alg токена — блокирует alg=none и подмену RS256 на HS256); проверить, что iss равен ожидаемому issuer (блокирует токены от чужого IdP); проверить, что aud содержит твой client_id (блокирует audience-confusion между приложениями); проверить, что exp в будущем, а iat в прошлом, с допуском около 300 с (блокирует replay просроченного токена); проверить, что nonce равен отправленному на /authorize (блокирует replay id_token в другую сессию). Пропуск любой — задокументированная категория CVE." },
    { q: "Объясни устаревание кэша JWKS при ротации ключей и паттерн обновления при промахе кэша.", a: "Клиент кэширует JWKS IdP (публичные ключи подписи), чтобы не ходить за ними на каждый запрос, с TTL 5–15 минут. Когда IdP ротирует ключи подписи, он публикует новый ключ, но в кэше всё ещё только старый. Новые токены подписаны новым ключом; валидатор читает kid, ищет в кэше, не находит и проваливает проверку подписи — шторм 401 на полностью валидных токенах, пока не истечёт TTL. Фикс: при промахе по kid немедленно дозагрузить свежий JWKS и повторить поиск, провалить только если kid всё ещё отсутствует. Короткий TTL плюс обновление при промахе — стандартная мера; зрелые IdP ещё и публикуют ключи за 24–48 часов до активации." },
    { q: "Как ротация refresh token превращает украденный refresh token из неограниченного вреда в окно обнаружения?", a: "При ротации каждое обновление выпускает новый refresh token и помечает старый использованным, а сервер отслеживает, какой токен сменил какой. Если атакующий крадёт RT1 и использует его, он получает RT2, а RT1 помечается использованным; когда легитимный клиент позже предъявит RT1, сервер увидит уже использованный токен — replay — и отзовёт всю цепочку (RT1, RT2, всех потомков), форся повторную аутентификацию. Доступ теряют обе стороны. Окно вреда сжимается с абсолютного срока жизни (дни-месяцы без ротации) до интервала ротации — минуты-часы до следующего легитимного обновления, которое сработает как тревога. refresh_replay_detected_total больше нуля — прямой сигнал компрометации." },
    { q: "Что делает DPoP-привязанный токен устойчивым к краже и когда вместо него выбрать mTLS?", a: "Bearer-токен может использовать любой, кто им владеет; DPoP привязывает токен к удерживаемой клиентом паре ключей. Токен несёт cnf.jkt — thumbprint публичного ключа клиента. На каждый вызов API клиент шлёт DPoP proof JWT, подписанный приватным ключом, с htm (метод), htu (целевой URI), iat, jti (кэшируется от replay) и ath (SHA-256 access token). Resource server проверяет подпись proof, сверяет ath с предъявленным токеном, подтверждает htm/htu, отклоняет устаревший iat или повторённый jti и проверяет, что jwk из proof хэшируется в cnf.jkt. Украденный токен без неизвлекаемого приватного ключа валидный proof создать не может. Выбирай DPoP для браузерных SPA и мобильных (ключ держат WebCrypto / keychain ОС, так как браузеры не отдают TLS-клиентские сертификаты в JS); выбирай mTLS для backend service-to-service, где X.509-сертификаты управляемы. FAPI 2.0 требует одно из двух." },
  ]}
/>

<Recap lang="ru">
Если каждый ответ восстановился по памяти, у тебя есть хребет юнита: PKCE привязывает code к клиенту, точное совпадение redirect_uri удерживает code в домене, проверки id_token (с закреплённым алгоритмом) аутентифицируют пользователя, обновление JWKS при промахе кэша переживает ротацию ключей, ротация refresh token превращает кражу в тревогу, а DPoP или mTLS делают украденный токен непригодным. Безопасность OAuth — задача полного набора: каждая проверка, каждый раз.
</Recap>
