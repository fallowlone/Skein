---
concepts:
- ambient-authority
- samesite-cookies
- get-side-effects
- double-submit-cookie
- origin-referer-check
- cors-not-csrf
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html
- https://web.dev/articles/samesite-cookies-explained
- https://portswigger.net/web-security/csrf/bypassing-samesite-restrictions
status: ready
summary: "Тест с выбором на синтез по всему юниту — ambient authority, бреши SameSite, побочные эффекты на GET, токен-паттерны, проверка Origin и почему CORS — не защита от CSRF."
title: 'CSRF: тест с выбором ответа'
track: security
unit: 04-csrf
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает решение, которое ты принимаешь, ревьюя реальный дизайн аутентификации — не определение для заучивания, а компромисс, который надо взвесить, когда эндпоинт меняет состояние.
</Hook>

<Goal lang="ru">
Убедись, что связываешь ambient authority, намеренные бреши SameSite, токен-паттерны и проверки Origin — тот синтез, к которому вёл урок, и ловушку, когда CORS принимают за защиту от CSRF.
</Goal>

<Quiz
  id="04-csrf-choice-1"
  lessonSlug="04-csrf"
  lang="ru"
  question={"Ревьюер заявляет: «наша сессионная кука HttpOnly и Secure, значит, мы защищены от CSRF». Почему это рассуждение неверно?"}
  choices={[
    { label: "CSRF никогда не читает куку — браузер сам прикрепляет её к любому запросу, адресованному твоему origin. HttpOnly лишь запрещает JavaScript читать значение (защита от XSS); Secure лишь требует HTTPS. Ни то, ни другое не мешает браузеру отправить куку в подделанной кросс-сайтовой записи.", correct: true },
    { label: "HttpOnly устарел и больше не даёт защиты.", misconception: "HttpOnly жив и полезен — но против кражи куки через XSS, это другой класс атак. Рассуждение неверно потому, что CSRF вообще не нужно читать куку." },
    { label: "Они правы — HttpOnly не даёт подделанному запросу нести куку.", misconception: "HttpOnly управляет доступом JavaScript к значению куки, а не тем, прикрепит ли её браузер к запросу. Браузер всё равно отправляет её кросс-сайтово; это и есть ambient authority, на которой едет CSRF." },
    { label: "Важен флаг Secure — по HTTPS куку нельзя подделать.", misconception: "Secure лишь заставляет куку идти по HTTPS. Подделанный запрос тоже идёт по HTTPS, с прикреплённой кукой. Шифрование транспорта ортогонально CSRF." },
  ]}
/>

<Quiz
  id="04-csrf-choice-2"
  lessonSlug="04-csrf"
  lang="ru"
  question={"Сессионные куки — SameSite=Lax. Скрытая форма на evil.com авто-сабмитит кросс-сайтовый POST /transfer. Что произойдёт и какой эндпоинт реально остаётся уязвим?"}
  choices={[
    { label: "POST придёт без сессионной куки — Lax не шлёт куки на кросс-сайтовые POST-подзапросы, так что классическая атака мертва. Живая дыра — любое изменение состояния, достижимое через GET (например, GET /account/close), потому что Lax шлёт куку на top-level GET-навигациях.", correct: true },
    { label: "POST пройдёт с прикреплённой кукой, значит SameSite=Lax ничего не дал.", misconception: "Lax как раз снимает куку с кросс-сайтовых POST-подзапросов; это его главный выигрыш. Остаётся уязвимым изменение состояния через GET на top-level навигации, а не POST." },
    { label: "И POST, и любой GET полностью заблокированы — Lax ведёт себя как Strict для кросс-сайта.", misconception: "Lax — это не Strict. Он намеренно шлёт куку на top-level навигациях безопасными методами, так что GET, меняющий состояние, всё ещё подделываем через переход по ссылке." },
    { label: "Ничего не уязвимо; SameSite=Lax — полная защита от CSRF сам по себе.", misconception: "Lax оставляет три бреши: побочные эффекты на GET, куки SameSite=None и окно default-Lax 120с Lax+POST. Это defense-in-depth, никогда не единственный замок." },
  ]}
/>

<Quiz
  id="04-csrf-choice-3"
  lessonSlug="04-csrf"
  lang="ru"
  question={"Почему «никогда не меняй состояние на GET-запросе» — реальное правило безопасности, а не просто вкусовщина REST?"}
  choices={[
    { label: "SameSite=Lax шлёт куку на top-level GET-навигациях, поэтому GET, мутирующий данные, подделываем через клик по ссылке или попадание на редиректящую страницу — без формы и скрипта. Держать все побочные эффекты на POST/PUT/PATCH/DELETE устраняет этот вектор целиком.", correct: true },
    { label: "GET-запросы медленнее, поэтому мутации на GET бьют по производительности.", misconception: "Производительность тут ни при чём. Суть в том, что Lax всё равно прикрепляет куку к top-level GET-навигациям, так что мутирующий GET напрямую подделываем через CSRF." },
    { label: "Браузеры кэшируют GET-ответы, поэтому мутация потеряется.", misconception: "Кэширование — про корректность, а не про безопасность. Реальная проблема в том, что побочный эффект на GET достижим подделанной top-level навигацией, несущей Lax-куку." },
    { label: "Это неважно — проверка токена защищает GET и POST одинаково, так что метод — свободный выбор.", misconception: "На практике GET-эндпоинты регулярно освобождают от проверки токена, и их триггерят обычные ссылки и теги изображений. Держать мутации вне GET закрывает самый дешёвый вектор CSRF до любой токен-логики." },
  ]}
/>

<Quiz
  id="04-csrf-choice-4"
  lessonSlug="04-csrf"
  lang="ru"
  question={"Stateless JSON API использует обычный double-submit cookie: случайное значение в куке, которое надо эхом вернуть в заголовке. Атакующий контролирует поддомен с XSS. Почему он обходит защиту и какой фикс?"}
  choices={[
    { label: "Куки расшарены вверх по дереву доменов, так что поддомен может выставить куку для родительского домена. Атакующий пишет значение CSRF-куки И эхом возвращает то же значение в заголовке — оба совпадают, проверка обойдена. Фикс: HMAC-подписанный вариант, привязывающий токен к сессии, чтобы подставленное значение не прошло верификацию.", correct: true },
    { label: "Double-submit невозможно развернуть stateless, значит, он был неверно настроен.", misconception: "Double-submit действительно stateless — в этом его прелесть. Слабость в том, что он предполагает, будто атакующий не может писать твои куки; XSS на поддомене или cookie injection ломают это допущение." },
    { label: "Заголовок можно прочитать кросс-сайтово, и атакующий просто его копирует.", misconception: "Same-origin policy запрещает кросс-сайтовое чтение твоих заголовков и значения куки. Здесь брешь — записываемость куки через поддомен, а не читаемость." },
    { label: "Фикса нет — double-submit фундаментально сломан, надо переходить на stateful.", misconception: "HMAC-подписанный double-submit привязывает токен к сессии, так что подставленное значение не пройдёт валидацию, сохраняя паттерн stateless. Synchronizer token (stateful) сильнее, но не единственный вариант." },
  ]}
/>

<Quiz
  id="04-csrf-choice-5"
  lessonSlug="04-csrf"
  lang="ru"
  question={"Команда добавляет строгую CORS-политику (никаких чужих origin) и делает вывод, что её эндпоинты с изменением состояния теперь защищены от CSRF. Где рассуждение ломается?"}
  choices={[
    { label: "CORS управляет тем, может ли JavaScript ПРОЧИТАТЬ кросс-ориджин ответ; он не мешает запросу быть ОТПРАВЛЕННЫМ с прикреплёнными куками. Top-level POST-форма или простой запрос доходят до сервера и выполняют побочный эффект независимо от CORS — атакующему и не нужно читать ответ.", correct: true },
    { label: "Они правы — строгая CORS-политика блокирует все кросс-ориджин запросы, включая подделанные.", misconception: "CORS не блокирует отправку или выполнение запросов; он контролирует выдачу ответа скрипту браузером. Подделанная запись всё равно проходит. CORS — не защита от CSRF." },
    { label: "CORS работает только на GET, так что на POST они защищены, а на GET нет.", misconception: "CORS применяется ко всем методам, но во всех случаях он гейтит читаемость ответа, а не выполнение запроса. Мутация происходит до того, как CORS начинает играть роль." },
    { label: "Поломка в том, что CORS требует preflight на каждый запрос, который они не включили.", misconception: "Preflight срабатывает только для непростых запросов. Простые запросы и top-level POST-формы пропускают его целиком и выполняют побочный эффект — именно поэтому CORS не может быть защитой от CSRF." },
  ]}
/>

<Quiz
  id="04-csrf-choice-6"
  lessonSlug="04-csrf"
  lang="ru"
  question={"Атакующий логинит браузер жертвы в АККАУНТ АТАКУЮЩЕГО (login CSRF), и дальнейшая активность жертвы пишется под личностью атакующего. Какая защита это закрывает и почему обычное рассуждение неполно?"}
  choices={[
    { label: "Защита нужна самой форме логина — pre-session (анонимный) synchronizer token, привязанный к посетителю и валидируемый на login POST. Команды забывают это, потому что у эндпоинта логина ещё нет аутентифицированной сессии, но login CSRF подделывает сам акт аутентификации, так что токен должен покрывать и неаутентифицированный поток.", correct: true },
    { label: "Login CSRF безвреден — атакующий лишь логинит жертву в собственный аккаунт атакующего.", misconception: "Он не безвреден: жертва затем вводит данные, платёжные реквизиты или историю, которые попадают в аккаунт атакующего, где он позже их прочтёт. Login CSRF — реальная именованная угроза." },
    { label: "SameSite=Strict на сессионной куке полностью предотвращает login CSRF.", misconception: "SameSite управляет сессионной кукой, но login CSRF подделывает login POST до того, как какая-либо сессионная кука существует. Нужен pre-session токен на форме логина, а не SameSite на пост-логин куке." },
    { label: "HTTPS предотвращает login CSRF, потому что креды шифруются в транзите.", misconception: "Шифрование защищает конфиденциальность в транзите; оно ничего не делает с подделанной top-level отправкой логина. Защита — CSRF-токен, покрывающий неаутентифицированный поток логина." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна цепочка: куки — это ambient authority, поэтому браузер шлёт твою сессию на любой запрос к твоему origin — включая подделанную запись. SameSite=Lax срезал поверхность кросс-сайтового POST в 2020, но оставил побочные эффекты на GET, SameSite=None и окно 120с Lax+POST, так что это defense-in-depth, а не замок. Реальная защита слоит неугадываемый токен (synchronizer для stateful-приложений, HMAC-подписанный double-submit для stateless API), проверку Origin/Referer и правило, что мутации не едут на GET. И две ловушки повторяются: HttpOnly/Secure и CORS оба ощущаются как защита от CSRF, но касаются чтения, а не подделанной записи, которая уже выполнилась. Теперь, когда слышишь «у нас HttpOnly и строгий CORS — мы защищены», ты знаешь точно, какой вопрос задать следующим: что мешает подделанной записи приземлиться?
</Recap>
