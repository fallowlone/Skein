---
concepts:
- ambient-authority
- samesite-cookies
- synchronizer-token
- double-submit-cookie
- origin-referer-check
- cors-not-csrf
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html
- https://web.dev/articles/samesite-cookies-explained
- https://portswigger.net/web-security/csrf/bypassing-samesite-restrictions
status: ready
summary: "Промпты на свободное воспроизведение по всему юниту CSRF. Сначала ответь своими словами, потом раскрой образцовый ответ и сравни."
title: 'CSRF: тест на свободное воспроизведение'
track: security
unit: 04-csrf
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение из памяти бьёт перечитывание. На каждый промпт скажи или запиши полный ответ по памяти, прежде чем открыть образцовый — именно усилие припоминания закрепляет материал.
</Hook>

<Goal lang="ru">
Восстанови спину юнита — ambient authority, намеренные бреши SameSite, токен-паттерны synchronizer и double-submit, проверки Origin/Referer и почему CORS — не защита от CSRF — не подсматривая в урок.
</Goal>

<RetrievalDrawer
  client:visible
  id="04-csrf-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему CSRF работает, хотя атакующий никогда не видит ответ и никогда не читает сессионную куку?", a: "CSRF эксплуатирует ambient authority: браузер автоматически прикрепляет куки сайта к каждому запросу, адресованному его origin, независимо от того, кто инициировал запрос. Скрытая форма или авто-сабмитящая страница на evil.com, постящая на bank.com, получает сессионную куку жертвы бесплатно — атакующему не нужно её читать или красть, поэтому HttpOnly нерелевантен. Same-origin policy мешает атакующему прочитать ответ bank.com, но это и не нужно: CSRF — атака записи, а не чтения. Вся цель — побочный эффект (перевод денег, смена email, удаление аккаунта), и он уже произошёл в момент, когда аутентифицированный запрос дошёл до сервера. Это проблема confused deputy: браузер действует с полномочиями пользователя, обманом использованными в пользу атакующего." },
    { q: "Chrome сделал SameSite=Lax дефолтом в 2020. Назови точно, что он блокирует, и три бреши, которые он оставляет.", a: "Lax не даёт кукам ехать на кросс-сайтовых POST-подзапросах, так что классическая атака скрытой авто-POST формой мертва по умолчанию — крупнейшая разовая митигация CSRF в истории веба. Но он намеренно оставляет три бреши. (1) Побочные эффекты на GET: Lax шлёт куку на top-level GET-навигациях, поэтому любое изменение состояния через GET подделываемо кликом по ссылке — отсюда правило «не мутируй на GET». (2) SameSite=None: куки, которые должны работать кросс-сайтово (сторонний SSO, встроенные виджеты, кросс-ориджин API), ставятся SameSite=None; Secure, что отключает всю защиту SameSite. (3) Окно Lax+POST: default-Lax Chrome (не явный) держит 120-секундное исключение совместимости, где свежая кука шлётся на top-level кросс-сайтовый POST. И SameSite навязывается браузером, так что старые клиенты и WebView ведут себя по-разному." },
    { q: "Сравни synchronizer token pattern и double-submit cookie pattern: как работает каждый и когда что выбрать.", a: "Synchronizer token: сервер генерирует криптографически случайный токен на сессию (или на запрос), хранит его на сервере и встраивает в каждую форму или ждёт в кастомном заголовке. Кросс-сайтовая страница атакующего не может его прочитать (same-origin policy) и не может угадать, поэтому подделанный запрос не несёт валидный. Stateful, сильнейший, дефолт OWASP для server-rendered приложений. Double-submit cookie: stateless — сервер ставит случайное значение в куку и ждёт то же значение эхом в заголовке/поле; атакующий не может прочитать куку, чтобы скопировать, так что два значения не совпадут. Дешевле (без серверного хранения), но слабее: если атакующий может записать куку для твоего домена (XSS на поддомене, cookie injection), он сделает оба значения совпадающими. Выбирай synchronizer для stateful server-rendered приложений; выбирай HMAC-подписанный double-submit (токен привязан к сессии) для stateless API, где нельзя держать серверное состояние." },
    { q: "Что добавляют проверки заголовков Origin и Referer и почему это слой defense-in-depth, а не единственная защита?", a: "Сервер проверяет, что Origin запроса (с откатом на Referer) совпадает с собственным origin, и отвергает чужие — дешёвый широкий фильтр, ловящий большинство кросс-сайтовых подделок до любой токен-логики. Это defense-in-depth, а не единственная защита, по двум причинам. Во-первых, оба заголовка могут отсутствовать или быть срезаны: настройки приватности, прокси или строгий referrer-policy убирают Referer, а часть запросов не несёт Origin, так что строгий reject ломает легитимный трафик, а мягкий откат ослабляет проверку. Во-вторых, это негативный фильтр (отвергнуть чужой origin) без позитивного доказательства намерения пользователя — его даёт токен. Используй Origin/Referer вместе с токеном, а не вместо него." },
    { q: "Коллега говорит: «у нас строгая CORS-политика, значит, мы защищены от CSRF». Объясни точно, почему CORS — не защита от CSRF.", a: "CORS управляет тем, выдаст ли браузер кросс-ориджин ОТВЕТ в JavaScript; он не предотвращает отправку или выполнение запроса. Top-level POST-форма и любой «простой» запрос доходят до сервера и выполняют побочный эффект независимо от CORS — а CSRF атака записи, так что атакующему и не нужно было читать ответ. CORS preflight срабатывает лишь для непростых запросов (кастомные заголовки, непростые content-type), и даже тогда гейтит читаемость ответа, а не выполнение. Одна смежная, но реальная митигация: требование кастомного заголовка плюс непростого content-type форсит preflight, который подделанный кросс-сайтовый fetch не удовлетворит — но это частичная мера, не токен-защита, ведь top-level POST-формы и простые запросы пропускают preflight целиком." },
    { q: "Что такое login CSRF, почему он подставляет команды и как защищаться?", a: "Login CSRF подделывает сам акт логина: атакующий заставляет браузер жертвы отправить форму ЛОГИНА с кредами атакующего, так что жертва молча аутентифицируется в аккаунт атакующего. Затем жертва вводит данные, платёжные реквизиты или активность, которые попадают в аккаунт атакующего, и он позже их читает. Команды подставляются, рассуждая «у эндпоинта логина ещё нет сессии, защищать нечего» — но подделываемое действие и есть сама аутентификация. Защита — pre-session (анонимный) CSRF-токен: выдай токен неаутентифицированному посетителю, встрой в форму логина и валидируй на login POST. SameSite на пост-логин сессионной куке не помогает, потому что подделка происходит до того, как эта кука существует." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь спину юнита: CSRF едет на ambient authority, так что подделанная запись выполняется с полной аутентификацией; SameSite=Lax срезал поверхность кросс-сайтового POST, но оставил побочные эффекты на GET, SameSite=None и окно 120с Lax+POST; synchronizer token (stateful) и HMAC-подписанный double-submit (stateless) — реальные защиты; Origin/Referer — дешёвый фильтр defense-in-depth, а не единственная защита; CORS гейтит чтение ответа, а не выполнение запроса, поэтому он не защита от CSRF; а login CSRF требует pre-session токена, потому что подделка предшествует сессионной куке. Теперь, встречая cookie-сессию на эндпоинте с изменением состояния, ты задашь три вопроса без подсказки: есть ли мутация на GET? какой токен доказывает намерение? и закрыта ли форма логина pre-session токеном?
</Recap>
