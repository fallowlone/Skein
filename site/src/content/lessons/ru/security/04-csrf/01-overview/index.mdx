---
concepts:
- ambient-authority
- samesite-cookies
- synchronizer-token
- double-submit-cookie
- origin-referer-check
- defense-in-depth
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Guides/Cookies
- https://web.dev/articles/samesite-cookies-explained
- https://portswigger.net/web-security/csrf/bypassing-samesite-restrictions
spiral: []
status: ready
summary: Браузер сам прикрепляет cookie к кросс-сайтовым запросам, поэтому вредоносная
  страница может запустить аутентифицированное изменение состояния. SameSite=Lax (дефолт
  Chrome с 2020) сжал поверхность — но GET-изменения, SameSite=None и брешь top-level
  навигации держат CSRF живым.
title: 'CSRF в эпоху SameSite: почему дефолт помог, но не убил атаку'
track: security
unit: 04-csrf
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";
import PacketDot from "~/components/diagram/PacketDot.astro";

<Hook>
Банк выкатывает эндпоинт «перевести деньги» и чувствует себя спокойно — сессии на cookie, HTTPS везде, логин надёжный. Через месяцы пользователь кликает по ссылке в посте на форуме. Открытая страница тихо авто-сабмитит скрытую форму на `POST /transfer` с `amount=5000&to=attacker`. Браузер делает то, что делают браузеры: прикрепляет cookie сессии пользователя к кросс-сайтовому запросу. Сервер видит валидную аутентифицированную сессию и переводит деньги. Пароль не украден, сессия не угнана. Атакующий даже не увидел ответ — ему и не нужно. Побочный эффект уже случился.
</Hook>

<Crux>CSRF едет на cookie, которые браузер прикрепляет сам; SameSite=Lax в 2020 сжал поверхность атаки, но не закрыл её.</Crux>

<Explanation>

## Механизм: ambient authority

За десять минут ты поймёшь, почему полностью аутентифицированный запрос может добраться до твоего сервера, не имея ничего общего с намерением пользователя — и какие защиты реально это останавливают.

CSRF существует из-за одного поведения браузера: cookie — это *ambient authority* (амбиентные, «фоновые» полномочия — браузер прикрепляет их автоматически, без ведома пользователя). Браузер прикрепляет cookie сайта к каждому запросу на origin этого сайта — неважно, кто инициировал запрос. Форма на `evil.com`, которая постит на `bank.com`, всё равно получит cookie сессии банка, потому что браузер ключует cookie по адресу назначения, а не по тому, кто начал навигацию. Атакующему не нужно читать или красть cookie (HttpOnly это блокирует и здесь нерелевантен). Ему нужно лишь заставить браузер жертвы *отправить* запрос. Аутентификация прилетает бесплатно.

В этом весь трюк, и поэтому CSRF иногда зовут «confused deputy» (запутанный заместитель): браузер — это заместитель, действующий с полномочиями пользователя, которого запутали так, что он использует эти полномочия на стороне атакующего. Важно: атакующий не может прочитать ответ — same-origin policy это блокирует. CSRF — это атака на *запись*, а не на чтение. Она работает на любом эндпоинте, где целью является сам побочный эффект: перевести деньги, сменить email, удалить аккаунт, повысить пользователя до админа.

<SequenceDiagram
  label="Атака CSRF в три шага: страница атакующего авто-сабмитит поддельный запрос через браузер жертвы, браузер автоматически прикрепляет сессионную куку жертвы, и целевой сервер выполняет изменение состояния от имени жертвы."
  hue="var(--d-crypto)"
  actors={["Attacker page", "Victim browser", "Target server"]}
  messages={[
    { from: "Attacker page", to: "Victim browser", label: "авто-сабмит скрытой формы" },
    { from: "Victim browser", to: "Target server", label: "POST /transfer + сессионная кука" },
    { from: "Target server", to: "Victim browser", label: "выполняет перевод как жертва" },
  ]}
  caption="Жертва лишь открыла страницу. Браузер сам прикрепляет куку банка к поддельному POST, поэтому сервер видит валидный аутентифицированный запрос и переводит деньги — атакующий не читает ответ, цель в самом побочном эффекте."
/>

Куку никто не крадёт — она просто едет вместе с запросом. Анимация ниже показывает, как сессионная кука жертвы авто-прикрепляется к поддельному запросу, путешествуя от страницы атакующего к целевому серверу:

<DiagramFrame
  label="Поддельный запрос движется от страницы атакующего через браузер жертвы к целевому серверу; движущаяся точка изображает сессионную куку, которую браузер прикрепляет сам и несёт до сервера."
  hue="var(--d-crypto)"
  caption="Браузер прикрепляет куку по адресу назначения, а не по тому, кто начал запрос — поэтому поддельный POST атакующего приходит полностью аутентифицированным."
>
  <svg viewBox="0 0 460 120" width="460" height="120" role="presentation" style="display:block;margin:0 auto;max-width:100%;height:auto;font-family:var(--font-body);">
    {/* path the cookie+request travels */}
    <path data-path d="M70 60 L230 60 L390 60" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" stroke-dasharray="4 4" />
    {/* attacker page */}
    <rect x="8" y="36" width="118" height="48" rx="7" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="67" y="58" text-anchor="middle" font-size="12" fill="var(--ink)">Страница атакующего</text>
    <text x="67" y="73" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--muted)">evil.com</text>
    {/* victim browser */}
    <rect x="171" y="36" width="118" height="48" rx="7" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="230" y="58" text-anchor="middle" font-size="12" fill="var(--ink)">Браузер жертвы</text>
    <text x="230" y="73" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--muted)">+ кука</text>
    {/* target server */}
    <rect x="334" y="36" width="118" height="48" rx="7" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="393" y="58" text-anchor="middle" font-size="12" fill="var(--ink)">Целевой сервер</text>
    <text x="393" y="73" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--muted)">bank.com</text>
    <text x="230" y="22" text-anchor="middle" font-size="10.5" font-family="var(--font-mono)" fill="var(--d)">поддельный POST + кука →</text>
    <PacketDot color="var(--d-crypto)" durationMs={2200} size={9} />
  </svg>
</DiagramFrame>

## SameSite=Lax: дефолт 2020, который изменил всё

В феврале 2020 Chrome 80 сделал `SameSite=Lax` дефолтом для любой cookie без явного атрибута. Firefox и Edge последовали. Это был самый сильный удар по классическому CSRF в истории веба. Cookie с `Lax` **не** отправляется на кросс-сайтовые сабзапросы — форма на `evil.com`, постящая на `bank.com`, больше не получает cookie, поэтому атака выше мертва по умолчанию для `POST`.

Но `Lax` — это не `Strict`, и брешь намеренная. `Lax` *всё же* отправляет cookie при **top-level навигации безопасными методами** — когда пользователь кликает по ссылке и в адресной строке появляется твой сайт, cookie едет с ним (иначе любая входящая ссылка разлогинивала бы тебя). Из этого напрямую следуют два вывода, и оба — живые векторы CSRF.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Запрос</th>
        <th class="border border-border px-3 py-2 text-left">SameSite=Lax шлёт cookie?</th>
        <th class="border border-border px-3 py-2 text-left">Риск CSRF</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Кросс-сайт <code>POST</code> форма / fetch</td>
        <td class="border border-border px-3 py-2">Нет</td>
        <td class="border border-border px-3 py-2">Заблокировано по умолчанию — большая победа</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Клик по ссылке (top-level) → кросс-сайт <code>GET</code></td>
        <td class="border border-border px-3 py-2">Да</td>
        <td class="border border-border px-3 py-2">Открыто, если изменение достижимо через <code>GET</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Cookie с <code>SameSite=None; Secure</code></td>
        <td class="border border-border px-3 py-2">Да, везде</td>
        <td class="border border-border px-3 py-2">Вся защита SameSite отключена by design</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>POST</code> в первые 120с после установки (Chrome Lax+POST)</td>
        <td class="border border-border px-3 py-2">Да (только дефолт-Lax)</td>
        <td class="border border-border px-3 py-2">Окно ~2 минуты после логина</td>
      </tr>
    </tbody>
  </table>
</div>

## Почему CSRF не мёртв

**Изменения состояния через GET.** Если `GET /account/delete` или `GET /transfer?to=x&amount=5000` реально меняет данные, `Lax` с радостью отправит cookie на top-level навигацию. Атакующему нужен лишь клик жертвы по ссылке (или попадание на страницу с редиректом). Поэтому «никогда не мутируй на `GET`» — это реальное правило безопасности, а не предпочтение в стиле REST.

**SameSite=None.** Любой сайт, которому легитимно нужны кросс-сайтовые cookie — встраиваемые виджеты, сторонний SSO, API, потребляемый с другого origin — обязан выставить `SameSite=None; Secure`, что отключает *всю* защиту SameSite для этой cookie. В момент, когда ты включаешь кросс-сайт, ты возвращаешься в эпоху до 2020 и тебе нужна настоящая токен-защита.

**Окно Lax+POST.** *Дефолт*-Lax в Chrome (не явный `SameSite=Lax`) держит исключение для совместимости: совсем новая cookie отправляется на top-level кросс-сайтовый `POST` первые 120 секунд, чтобы не сломать некоторые SSO-редиректы. Это окно CSRF ~2 минуты сразу после установки cookie. Явная установка `SameSite=Lax` это исключение убирает.

**Несогласованное применение.** Дефолт-Lax применяет *браузер*. Старые браузеры, встроенные WebView и некоторые не-Chromium клиенты ведут себя не одинаково, и нельзя считать сервер защищённым клиентским дефолтом. SameSite — слой defense-in-depth, никогда не единственный замок.

<Inset kind="why" lang="ru">
Есть тонкое различие, на котором горят: защита отличается между cookie **без** атрибута SameSite (браузер применяет *дефолт*-Lax, включающий исключение Lax+POST на 120с) и cookie с **явным** `SameSite=Lax` (без исключения, строже). Если ты вообще полагаешься на SameSite — выставляй его явно. Доверять свою безопасность браузерному дефолту значит наследовать его хаки совместимости.
</Inset>

## Защиты, которые всё ещё важны

Когда ты аудируешь приложение на сессионных куках, что ты реально ставишь перед эндпоинтом изменения состояния? SameSite сжимает поверхность; он не заменяет токен-защиту ни на одном важном эндпоинте. Два серверных паттерна остаются хребтом:

- **Synchronizer token pattern (синхронизатор-токен).** Сервер генерирует криптографически случайный токен на сессию (или на запрос), хранит его на сервере и встраивает в каждую форму / шлёт в кастомном заголовке. Кросс-сайтовая страница атакующего не может его прочитать (same-origin policy) и не может угадать. Сервер отклоняет любой меняющий состояние запрос, чей токен не совпал. Stateful, самый сильный, дефолт OWASP для server-rendered приложений.
- **Double-submit cookie (двойная отправка cookie).** Stateless: сервер кладёт случайное значение в cookie *и* ждёт то же значение в заголовке / поле формы. Атакующий не может прочитать твою cookie, чтобы скопировать её в заголовок, поэтому значения не совпадут. Дешевле (нет серверного хранения), но слабее — уязвим, если атакующий может писать cookie для твоего домена (например, XSS на сабдомене); поэтому OWASP теперь рекомендует **HMAC-подписанный** вариант, привязывающий токен к сессии.

Добавь слой **defense-in-depth** на чувствительные эндпоинты: проверяй, что заголовок `Origin` (с откатом на `Referer`) совпадает с твоим origin, отклоняя запросы с чужих origin. А для JSON-API требование кастомного заголовка (например `X-Requested-With`) плюс непростой content-type заставляют сделать CORS *preflight* — который кросс-сайтовый `fetch` атакующего не пройдёт, если твоя CORS-политика это не разрешит. Это частичная мера, а не полная токен-защита, потому что top-level отправки форм и некоторые content-type пропускают preflight.

Вместе эти слои означают, что ни один отдельный сбой не открывает дверь: SameSite режет поверхность, токен даёт позитивное доказательство намерения, Origin/Referer ловит то, что проскальзывает. Без токена SameSite оставляет открытыми GET-эффекты и куки с None; без SameSite каждый POST уязвим до того, как дойдёт до проверки токена.

<TradeoffMatrix
  id="04-csrf-overview-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Server-rendered банковское приложение с cookie-сессиями нужно защитить от CSRF на формах, меняющих состояние. Выбери основную защиту."
  options={[
    { name: "Synchronizer token pattern (случайный токен на сессию, хранится на сервере, встроен в каждую форму) плюс явный SameSite=Lax и проверка Origin", summary: "Токен + defense-in-depth.", correct: true, justification: "Это рекомендованный OWASP хребет для server-rendered приложений. Токен неугадываем и нечитаем кросс-сайт (same-origin policy), поэтому подделанный запрос не сможет включить валидный; SameSite=Lax режет поверхность ещё до проверки токена; проверка Origin ловит то, что проскочило. Слоисто, и ни один отдельный сбой не открывает эндпоинт." },
    { name: "Полагаться только на дефолт SameSite=Lax — Chrome сделал его дефолтом в 2020, так что кросс-сайт POST заблокированы", summary: "Только браузерный дефолт.", justification: "SameSite — это defense-in-depth, а не полная защита. Дефолт-Lax всё ещё шлёт cookie на top-level GET-навигации (так что любое GET-изменение открыто), держит окно Lax+POST на 120с и применяется только браузером — старые клиенты и WebView разнятся. Никогда не делай клиентский дефолт единственным замком." },
    { name: "Только double-submit cookie — он stateless и легко деплоится", summary: "Stateless токен.", justification: "Разумно для stateless API, но слабее как единственная защита: если атакующий может выставить cookie для твоего домена (например, XSS на сабдомене или cookie injection), он добьётся совпадения значений. OWASP теперь рекомендует HMAC-подписанный вариант, привязанный к сессии. Для stateful банка синхронизатор-токен сильнее." },
    { name: "Проверять только заголовок Referer и отклонять всё, где его нет", summary: "Только Referer.", justification: "Проверки Origin/Referer — хороший слой defense-in-depth, но плохая единственная защита: Referer вырезают приватные настройки, прокси или referrer-policy, поэтому легитимные запросы блокируются, а позитивного доказательства намерения ты не добавил. Используй вместе с токеном, а не вместо него." },
  ]}
/>

<Quiz
  id="04-csrf-overview-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Почему HttpOnly НЕ защищает от CSRF?"
  choices={[
    { label: "CSRF не нужно читать cookie — браузер сам прикрепляет её к запросу; HttpOnly лишь не даёт JavaScript её прочитать", correct: true },
    { label: "HttpOnly устарел в современных браузерах", misconception: "HttpOnly жив и полезен — но против XSS (не даёт скрипту украсть cookie), это другой класс атак, не CSRF." },
    { label: "HttpOnly работает только по HTTPS", misconception: "Это флаг Secure. HttpOnly управляет доступом JavaScript к cookie, а не транспортом, и ни то, ни другое не мешает браузеру слать cookie кросс-сайт." },
    { label: "Защищает — HttpOnly целиком останавливает подделанный запрос", misconception: "CSRF — это атака confused-deputy на запись: атакующий заставляет браузер отправить запрос с уже прикреплённой cookie. Читать cookie никогда не требовалось." },
  ]}
/>

<Quiz
  id="04-csrf-overview-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Твои cookie-сессии — SameSite=Lax. Какой эндпоинт всё ещё открыт для CSRF?"
  choices={[
    { label: "GET /account/close, который реально закрывает аккаунт — Lax шлёт cookie на top-level GET-навигации", correct: true },
    { label: "Кросс-сайт POST /transfer из скрытой формы на evil.com", misconception: "Именно это Lax и блокирует — cookie не шлётся на кросс-сайт POST-сабзапросы, поэтому подделанный POST приходит без сессии." },
    { label: "Кросс-сайт fetch() с JSON-телом", misconception: "Lax блокирует cookie на кросс-сайт fetch, а непростой JSON-запрос триггерит CORS preflight, который атакующий не пройдёт. Настоящая дыра — GET-изменение." },
    { label: "Тег image, указывающий на кросс-сайт POST-эндпоинт", misconception: "Загрузка картинки — это GET и сабзапрос, а не top-level навигация; Lax не прикрепит cookie. Открыт случай GET, мутирующего на top-level навигации." },
  ]}
/>

<DragOrder
  id="04-csrf-overview-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь слои современной защиты от CSRF — от широкого дешёвого фильтра до самого сильного позитивного доказательства:"
  items={[
    { id: "a", label: "Никогда не мутируй состояние на GET — держи все побочные эффекты на POST/PUT/PATCH/DELETE" },
    { id: "b", label: "Выставляй cookie явно SameSite=Lax (или Strict) — режет кросс-сайтовую поверхность" },
    { id: "c", label: "Проверяй заголовок Origin (с откатом на Referer) на совпадение с твоим origin" },
    { id: "d", label: "Требуй synchronizer или HMAC double-submit CSRF-токен на каждое изменение состояния" },
  ]}
  correctOrder={["a", "b", "c", "d"]}
/>

</Explanation>

<KeyTakeaway>SameSite=Lax (дефолт Chrome с 2020) блокирует кросс-сайт POST, но оставляет GET-изменения, SameSite=None и брешь Lax+POST на 120с. Слои поверх: явный CSRF-токен и проверка Origin.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-csrf-overview-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, почему CSRF работает, хотя атакующий никогда не видит ответ и не читает cookie сессии.", a: "CSRF эксплуатирует ambient authority: браузер автоматически прикрепляет cookie сайта к каждому запросу на origin этого сайта, неважно, кто инициировал запрос. Поэтому скрытая форма или авто-сабмитящая страница на evil.com, постящая на bank.com, бесплатно получает cookie сессии жертвы — атакующему не надо её читать или красть (HttpOnly нерелевантен). Same-origin policy не даёт атакующему прочитать ответ bank.com, но ему всё равно: CSRF — это атака на запись, а не на чтение. Цель — побочный эффект: деньги переведены, email сменён, аккаунт удалён. Это уже случилось в момент, когда аутентифицированный запрос дошёл до сервера. Это проблема confused deputy: браузер — заместитель, действующий с полномочиями пользователя, обманутый в их использовании на стороне атакующего." },
    { q: "Chrome с 2020 ставит cookie дефолтом в SameSite=Lax. Почему этого мало само по себе и что добавить?", a: "Lax был огромной победой — он не даёт cookie ехать на кросс-сайт POST-сабзапросы, убивая классическую атаку скрытой формой по умолчанию. Но он намеренно держит три бреши. Первая: Lax шлёт cookie на top-level GET-навигации, поэтому любое изменение, достижимое через GET, всё ещё подделываемо кликом жертвы по ссылке — поэтому «никогда не мутируй на GET» это правило безопасности. Вторая: любая cookie с SameSite=None (нужна для легитимных кросс-сайт сценариев вроде стороннего SSO или встраиваемых виджетов) отключает всю защиту SameSite. Третья: дефолт-Lax в Chrome держит окно совместимости Lax+POST на 120 секунд, где свежая cookie шлётся на top-level кросс-сайт POST. И SameSite применяется браузером, так что старые клиенты и WebView разнятся. Фикс — defense-in-depth: держи все мутации вне GET, выставляй SameSite явно, проверяй заголовок Origin/Referer и требуй неугадываемый CSRF-токен (синхронизатор-паттерн или HMAC-подписанный double-submit) на каждый меняющий состояние запрос — чтобы ни один отдельный сбой не открыл эндпоинт." },
  ]}
/>

<Recap lang="ru">
CSRF работает, потому что cookie — это ambient authority: браузер прикрепляет cookie твоей сессии к любому запросу на твой origin, даже к тому, что начала вредоносная страница, поэтому подделанная запись выполняется с полной аутентификацией, а атакующий ответ так и не читает. Переход Chrome в 2020 на SameSite=Lax дефолтом был крупнейшей одиночной мерой в истории веба — кросс-сайт POST больше не несут cookie — но это не полная защита. Lax всё ещё шлёт cookie на top-level GET-навигации (так что любое изменение на GET открыто), SameSite=None полностью отключает защиту для кросс-сайт сценариев, а дефолт-Lax держит окно Lax+POST ~120 секунд. Поэтому позиция сеньора — defense-in-depth: никогда не мутируй на GET, выставляй SameSite явно, проверяй заголовок Origin/Referer и подкрепляй настоящим CSRF-токеном — синхронизатор-паттерн для server-rendered приложений или HMAC-подписанный double-submit cookie для stateless API — чтобы сбой одного слоя не открыл дверь. Теперь, когда видишь cookie-сессию на эндпоинте с изменением состояния, первый вопрос не «выставлен ли Lax?», а «что произойдёт с запросом без CSRF-токена?»
</Recap>
