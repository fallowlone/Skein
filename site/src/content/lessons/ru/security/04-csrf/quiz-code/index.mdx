---
concepts:
- samesite-cookies
- double-submit-cookie
- get-side-effects
- origin-referer-check
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html
- https://web.dev/articles/samesite-cookies-explained
status: ready
summary: "Читай реальные конфиги кук и обработчики маршрутов, предсказывай поведение CSRF и выбирай фикс с наибольшим рычагом, который сделал бы senior-ревьюер."
title: 'CSRF: чтение кода и конфигов'
track: security
unit: 04-csrf
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги CSRF живут в атрибутах кук и обработчиках маршрутов, а не в прозе. Читай каждый сниппет, предсказывай, пройдёт ли подделанный запрос, и выбирай фикс, который senior-ревьюер пометит первым.
</Hook>

<Goal lang="ru">
Отработай цикл, который ты гоняешь в каждом security-ревью: читай конфиг кук и обработчик, предсказывай, где приземлится подделанная запись, и тянись к фиксу с наибольшим рычагом до рассуждений об экзотических обходах.
</Goal>

## Сниппет 1 — конфиг куки

```js
// сессионная кука Express
app.use(session({
  secret: process.env.SECRET,
  cookie: {
    httpOnly: true,
    secure: true,
    sameSite: 'none',   // <-- нужно, чтобы встроенный виджет на partner.com работал
  },
}));
```

<Quiz
  id="04-csrf-code-1"
  lessonSlug="04-csrf"
  lang="ru"
  question={"Команда поставила sameSite: 'none', чтобы сессия работала в iframe на partner.com. Какую CSRF-постуру это создаёт и что должно за этим последовать?"}
  choices={[
    { label: "SameSite=None отключает всю защиту SameSite для этой куки — она теперь шлётся на каждый кросс-сайтовый запрос, ровно поверхность до 2020. Браузерный дефолт больше ничего не охраняет, так что полная токен-защита (synchronizer или HMAC double-submit) плюс проверка Origin становятся обязательны на каждом маршруте с изменением состояния.", correct: true },
    { label: "httpOnly: true компенсирует, так что SameSite=None безопасен сам по себе.", misconception: "httpOnly запрещает JavaScript читать куку (про XSS); он ничего не делает, чтобы браузер не слал её кросс-сайтово. С SameSite=None не осталось защиты SameSite, на которую можно опереться." },
    { label: "secure: true делает SameSite=None безопасным, ведь кука идёт только по HTTPS.", misconception: "secure обязателен синтаксически с SameSite=None, но лишь форсит транспорт HTTPS. Подделанный кросс-сайтовый запрос тоже по HTTPS; SameSite=None всё равно шлёт куку. Шифрование ортогонально CSRF." },
    { label: "Всё нормально — iframe всё равно не может триггерить изменения состояния.", misconception: "Встроенный контекст может слать запросы, несущие куку SameSite=None кросс-сайтово, и весь смысл None — слать куку повсюду. Изменения состояния полностью достижимы; нужна токен-защита." },
  ]}
/>

## Сниппет 2 — проверка double-submit

```js
function verifyCsrf(req, res, next) {
  const cookieToken = req.cookies['csrf'];
  const headerToken = req.get('X-CSRF-Token');
  if (cookieToken && cookieToken === headerToken) return next();
  return res.status(403).send('CSRF check failed');
}
```

<Quiz
  id="04-csrf-code-2"
  lessonSlug="04-csrf"
  lang="ru"
  question={"Эта обычная double-submit проверка проходит, когда значение куки равно значению заголовка. Какую слабость пометит senior-ревьюер и какой фикс?"}
  choices={[
    { label: "Она доверяет, что куку csrf может выставить только твой origin. Если атакующий может записать куку для твоего домена — XSS на поддомене или cookie injection через незащищённый сиблинг — он ставит csrf=X и шлёт X в заголовке; оба совпадают, проверка проходит. Фикс: HMAC-подписанный вариант, привязывающий токен к сессии, чтобы подставленное значение не верифицировалось.", correct: true },
    { label: "Надо использовать === по обрезанным строкам; баг в пробелах.", misconception: "Сравнение строк — не дефект. Структурная слабость в том, что паттерн предполагает, будто атакующий не может записать твою куку — сломай это допущение (XSS на поддомене, injection), и совпадающие значения тривиально подделать." },
    { label: "Проверка нормальна; double-submit безусловно безопасен, ведь заголовок нельзя подделать кросс-сайтово.", misconception: "Заголовок нельзя ПРОЧИТАТЬ или выставить кросс-сайтово скриптом, верно — но атакующему не нужно читать; ему нужно контролировать обе половины, что даёт примитив записи куки. Обычный double-submit слабее, чем кажется." },
    { label: "Нужно хранить токен на сервере, так что любой stateless-подход небезопасен.", misconception: "Stateless может быть безопасным: HMAC-подписанный double-submit привязывает токен к секрету сессии, так что подставленное значение куки не пройдёт валидацию, без серверного хранения. Фикс — привязка, а не отказ от stateless." },
  ]}
/>

## Сниппет 3 — обработчик маршрута

```js
// «Отписаться в один клик из письма»
app.get('/account/email-prefs/unsubscribe', requireSession, (req, res) => {
  db.users.update(req.session.userId, { subscribed: false });  // мутирует!
  res.send('Вы отписаны.');
});
```

<Quiz
  id="04-csrf-code-3"
  lessonSlug="04-csrf"
  lang="ru"
  question={"Сессии здесь SameSite=Lax и есть CSRF-токен на всех POST-маршрутах. Этот GET-обработчик безопасен? Почему да или нет?"}
  choices={[
    { label: "Нет — он мутирует состояние на GET. SameSite=Lax шлёт сессионную куку на top-level GET-навигациях, а GET-маршруты — именно то, что команды освобождают от проверки токена. Атакующему достаточно, чтобы жертва открыла ссылку или тег изображения, указывающий сюда. Фикс: перенести мутацию на POST/PUT и потребовать CSRF-токен.", correct: true },
    { label: "Безопасен — requireSession означает, что сюда доходят только залогиненные.", misconception: "requireSession удовлетворяется собственной авто-прикреплённой Lax-кукой жертвы на подделанном top-level GET. Аутентификация есть; намерения нет. Мутирующий GET — это дыра." },
    { label: "Безопасен — Lax блокирует все кросс-сайтовые запросы, включая этот GET.", misconception: "Lax намеренно шлёт куку на top-level GET-навигациях. Это именно тот случай, под который уязвим обработчик; Lax его не блокирует." },
    { label: "Безопасен — он идемпотентен, так что повтор ничего не меняет.", misconception: "Идемпотентность нерелевантна CSRF: один подделанный запрос уже переключает subscribed в false против воли пользователя. Дефект — мутация на GET, где проверка токена не запускается." },
  ]}
/>

## Сниппет 4 — проверка Origin

```js
function checkOrigin(req, res, next) {
  const origin = req.get('Origin');
  if (origin === 'https://app.example.com') return next();
  return res.status(403).send('Bad origin');
}
```

<Quiz
  id="04-csrf-code-4"
  lessonSlug="04-csrf"
  lang="ru"
  question={"Если использовать это как ЕДИНСТВЕННУЮ защиту от CSRF на маршрутах с изменением состояния, что ломается и как этот слой надо применять?"}
  choices={[
    { label: "Часть легитимных запросов не несёт заголовок Origin (некоторые same-origin навигации, старые клиенты), так что строгий reject по равенству блокирует реальных пользователей; и как негативный фильтр он не добавляет позитивного доказательства намерения. Используй как слой defense-in-depth рядом с реальным токеном, откатываясь на Referer при отсутствии Origin — а не как единственный замок.", correct: true },
    { label: "Ничего не ломается — Origin всегда присутствует и неподделываем, так что это полная защита.", misconception: "Origin ставится браузером и не подделываем скриптом, верно, но он не всегда присутствует, а единственный негативный фильтр не доказывает намерения. Он рядом с токеном, с откатом на Referer, а не сам по себе." },
    { label: "Он слишком слаб, ведь атакующий может выставить заголовок Origin в твой домен.", misconception: "Origin ставится браузером и не спуфится скриптом страницы в обычном кросс-сайтовом запросе — это его сила. Реальные проблемы — отсутствие/строгость и отсутствие позитивного доказательства, так что это слой, а не вся защита." },
    { label: "Надо проверять Referer первым, ведь Referer надёжнее Origin.", misconception: "Origin — более надёжный сигнал (шлётся на кросс-сайтовые запросы с изменением состояния и минимален); Referer — откат и чаще срезается приватностью или referrer-policy. Порядок: сначала Origin, Referer как откат." },
  ]}
/>

<Recap lang="ru">
Каждое CSRF-ревью читается в атрибутах кук и обработчиках: SameSite=None возвращает тебя в полную поверхность до 2020 и обязывает к токен-защите; обычный double-submit доверяет, что куку можешь записать только ты, так что HMAC-подписанный вариант — фикс; мутация на GET подделываема через top-level Lax-навигацию и должна перейти на POST за токеном; а проверка Origin — дешёвый фильтр defense-in-depth, с откатом на Referer, никогда не единственный замок. Замечай структурный дефект, чини на слое с наибольшим рычагом, потом подтверждай, что подделанный запрос больше не приземляется.
</Recap>
