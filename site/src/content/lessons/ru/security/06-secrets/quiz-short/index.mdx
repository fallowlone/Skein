---
concepts:
- secret-rotation
- secrets-manager
- dynamic-secrets
- envelope-encryption
- least-privilege
- git-leak-remediation
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://cheatsheetseries.owasp.org/cheatsheets/Secrets_Management_Cheat_Sheet.html
- https://developer.hashicorp.com/vault/tutorials/get-started/understand-static-dynamic-secrets
status: ready
summary: "Подсказки на свободное вспоминание по всему юниту — сначала ответьте своими словами, затем откройте эталон и сравните: устранение утечки, лестница, динамические креды и envelope encryption."
title: 'Управление секретами: обзор на свободное вспоминание'
track: security
unit: 06-secrets
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Вспоминание побеждает перечитывание. На каждую подсказку проговорите или запишите полный ответ по памяти, прежде чем открыть эталон, — именно усилие припоминания закрепляет материал, когда вы посреди инцидента в два часа ночи.
</Hook>

<Goal lang="ru">
Восстановите ключевые механизмы юнита — почему закоммиченный секрет уже утёк, лестницу зрелости, динамические короткоживущие креды, envelope encryption (схема, где KMS оборачивает data key, а данные шифруются локально) и least privilege (принцип минимальных прав) — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:visible
  id="06-secrets-short-retrieval"
  lang="ru"
  questions={[
    { q: "Коллега закоммитил пароль БД, потом удалил строку и сделал push. Объясните, почему это не решает проблему, и каково реальное устранение.", a: "История git append-only, поэтому удаление строки новым коммитом не убирает секрет из прежнего коммита — он остаётся во всей истории и в каждом клоне и форке, который кто-либо сделал. Любой сканер или любой, кто запускает git log -p, читает его тривиально, а утёкшие ключи в публичных репозиториях используют примерно за минуту. Удаление косметично. Реальное устранение — ротация: отозвать пароль в БД, чтобы значение, которым владеет атакующий, перестало работать, затем проверить аудит и биллинг провайдера на предмет того, что было сделано, пока он был живым. В качестве гигиены можно переписать историю через git filter-repo и вычистить блоб, но это не защищает задним числом копии, которые уже клонировали, — именно поэтому ротация, а не удаление, и есть фикс. Наконец, добавьте pre-commit сканер секретов, чтобы следующий ловился до того, как покинет ноутбук." },
    { q: "Проведите коллегу вверх по лестнице зрелости секретов и объясните, почему каждая ступень лучше, закончив тем, почему цель — короткоживущие динамические секреты.", a: "Первая ступень — захардкоженный секрет, то есть просто утёкшая креда, лежащая в истории git навсегда. Вторая — .env вне git через .gitignore: лучше, потому что значение вне коммита и образа, но это всё ещё plaintext на каждом ноутбуке и CI-раннере, читаемый любым, у кого есть машина, без контроля доступа и аудита, и в одном плохом .gitignore от катастрофы. Третья — secret manager (Vault, AWS Secrets Manager или SOPS), который добавляет три вещи, недоступные .env: шифрование at rest, контроль доступа, чтобы только нужный сервис читал данный секрет, и аудит каждого чтения, который и подсказывает, чью креду ротировать после инцидента. Четвёртая — динамические короткоживущие секреты: вместо статического значения manager выдаёт уникальную креду на запрос с TTL (движок БД Vault обычно использует часовой TTL в проде) и автоматически её отзывает. Это делает TTL радиусом поражения любой утечки, даёт каждому потребителю уникальную, ограниченную по правам least-privilege идентичность, которую аудит-лог называет точно, и полностью убирает долгоживущие ключи из приложения. Эта ступень дороже всего в настройке, но для всего, что охраняет продовые данные, часовое окно экспозиции лучше бессрочного." },
    { q: "Объясните envelope encryption с KMS и почему она масштабируется лучше, чем вызов KMS на самом payload.", a: "В envelope encryption вы не отправляете данные в KMS. Вместо этого вы просите KMS сгенерировать data key; KMS возвращает его и в открытом виде, и обёрнутым (зашифрованным под ключ-шифрующий-ключ, который никогда не покидает KMS). Вы шифруете payload локально открытым data key, затем уничтожаете открытый data key и храните рядом с шифротекстом только обёрнутый data key. Чтобы расшифровать, вы отправляете обёрнутый data key обратно в KMS, получаете открытый data key и расшифровываете локально. Это масштабируется, потому что KMS обрабатывает только крошечные фиксированного размера ключи, а не ваши многомегабайтные payload, так что вы не упираетесь в лимиты размера и частоты запросов, а ваш plaintext не пересекает сеть к третьей стороне. Ротация тоже дешевеет: ротация ключа-шифрующего-ключа требует лишь перевёртывания data keys, а не перешифровки каждого payload." },
    { q: "Что значит least privilege применительно к секретам и как уникальные идентичности по сервисам меняют разбор инцидента?", a: "Least privilege означает, что каждая креда ограничена ровно теми правами, которые нужны её потребителю, и не больше — сервис отчётности получает read-only креду, которая не может удалить продовую БД, воркер очереди получает доступ только к своей очереди. Для секретов это сочетается с уникальными идентичностями: вместо одного общего токена на двенадцать сервисов каждый сервис аутентифицируется как он сам и получает собственную креду. Это меняет разбор инцидента двояко. Во-первых, радиус поражения: украденная креда отчётности не может навредить вне своих прав, поэтому компрометация ограничена тем, что эта роль и так могла. Во-вторых, атрибуция: поскольку креда уникальна, аудит-лог называет точный экземпляр сервиса, который действовал, так что вы знаете, какую именно креду ротировать и чего она касалась, вместо одного общего имени пользователя, используемого всем подряд." },
    { q: "Различите шифрование at rest и шифрование in transit для секретов и объясните, почему secret manager нужны оба плюс контроль доступа.", a: "Шифрование in transit (TLS) защищает секрет, пока он движется по сети — между приложением и manager или приложением и БД, — так что прослушка или вредоносный прокси не прочтут его на проводе. Шифрование at rest защищает хранимое значение на диске — в бэкенде manager, в бэкапе, в снапшоте, — так что укравший диск или файл бэкапа получает шифротекст, а не секрет. Они защищают разные угрозы, и нужны оба, потому что секрет уязвим в каждой точке, где лежит или движется. Но ни одно не отвечает на вопрос, кому вообще позволено читать секрет: это контроль доступа. Manager сочетает все три — TLS для транзита, шифрованное хранение at rest и политики доступа по идентичностям — плюс аудит-лог, фиксирующий каждое чтение, что и делает manager настоящим апгрейдом над plaintext .env, а не просто зашифрованным файлом." },
    { q: "Почему TTL динамического секрета называют «радиусом поражения» и что остаётся долгоживущим даже при использовании динамических секретов?", a: "Называть TTL радиусом поражения значит, что максимальное окно ущерба от утёкшей динамической креды равно её времени жизни: если часовая креда утекает, она автоматически отзывается в течение часа, заметил кто-то утечку или нет, так что используемое атакующим окно ограничено заранее, а не тянется, пока человек не вспомнит ротировать (что после утечки часто не случается никогда). Чем короче TTL, тем меньше радиус поражения — ценой более частого трафика fetch-and-renew. Долгоживущей остаётся сама конфигурация движка: root- или admin-креда, которой secret manager выпускает эти динамические листья (например, суперпользователь БД, которым Vault создаёт пользователей на запрос), сама по себе мощный, постоянный секрет. Её надо периодически ротировать и тщательно защищать, потому что её компрометация компрометирует всю выпускающую машинерию — динамические секреты спускают долгоживущий риск в одно хорошо охраняемое место, но не отменяют его." },
  ]}
/>

<Recap lang="ru">
Если вы смогли восстановить каждый ответ по памяти, вы держите стержень юнита: закоммиченный секрет уже утёк, и завершает это только ротация, потому что история append-only; лестница зрелости идёт от захардкоженного к .env, к manager и к динамическим короткоживущим кредам; envelope encryption позволяет KMS защищать данные на масштабе, оборачивая локальный data key; least privilege с уникальными идентичностями ограничивает и атрибутирует взлом; шифрование at rest и in transit защищают разные угрозы и оба идут в паре с контролем доступа; а TTL — это радиус поражения, тогда как root-креда движка остаётся тем долгоживущим риском, который надо охранять. Теперь, когда придёт реальный инцидент, вы потянетесь к ротации раньше, чем к переписыванию истории — и будете точно знать, почему.
</Recap>
