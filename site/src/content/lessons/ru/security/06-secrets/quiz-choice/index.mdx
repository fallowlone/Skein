---
concepts:
- secret-rotation
- secrets-manager
- dynamic-secrets
- least-privilege
- envelope-encryption
- secret-sprawl
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://cheatsheetseries.owasp.org/cheatsheets/Secrets_Management_Cheat_Sheet.html
- https://developer.hashicorp.com/vault/tutorials/get-started/understand-static-dynamic-secrets
status: ready
summary: "Синтез всего юнита в формате с выбором ответа — устранение утечки, лестница зрелости, короткоживущие динамические креды, envelope encryption и least privilege под давлением реального инцидента."
title: 'Управление секретами: обзор с выбором ответа'
track: security
unit: 06-secrets
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, проходящих через весь юнит. Каждый отражает решение, которое вы принимаете в реальном инциденте, — не определение для пересказа, а компромисс, который надо взвесить, пока ключ ещё живой и им, возможно, уже владеет атакующий.
</Hook>

<Goal lang="ru">
Убедитесь, что вы связываете устранение утечки, лестницу зрелости, динамические креды, envelope encryption и least privilege — синтез, к которому вели уроки.
</Goal>

<Quiz
  id="06-secrets-choice-1"
  lessonSlug="06-secrets"
  lang="ru"
  question="Живой AWS-ключ закоммитили в публичный репозиторий на прошлой неделе. Коллега удалил строку и сделал force-push. Какое действие реально завершает экспозицию?"
  choices={[
    { label: "Ротация (rotation): отозвать ключ в AWS, чтобы значение, которым владеет атакующий, перестало работать — удаление не достаёт до уже сделанных клонов.", correct: true },
    { label: "Запустить git filter-repo и вычистить блоб из каждого коммита; как только история чиста, секрет в безопасности.", misconception: "Переписывание истории — это гигиена, а не устранение. Оно не касается клонов, форков и кешей сканеров, у которых блоб уже есть, — ключ остаётся валидным, пока вы не отзовёте его у источника." },
    { label: "Сделать репозиторий приватным, чтобы посторонние больше не читали историю.", misconception: "Смена видимости урезает аудиторию, но не риск: ключ был публичным достаточно долго, чтобы сканеры его забрали (часто за минуту), и он всё ещё валиден. Экспозицию завершает только ротация." },
    { label: "Больше ничего — force-push переписал историю, поэтому строки нигде больше нет.", misconception: "Force-push лишь сдвигает верхушку вашего remote. У каждого существующего клона и форка остаётся старый коммит, и вы никогда не подтвердите, что копия не утекла. Удаление — не отзыв." },
  ]}
/>

<Quiz
  id="06-secrets-choice-2"
  lessonSlug="06-secrets"
  lang="ru"
  question="Команда хранит продовые пароли БД в .env, который раскатывается на каждый сервер и держится вне git через .gitignore. Почему secret manager — следующая ступень и что он даёт такого, чего .env не может?"
  choices={[
    { label: "Шифрование at rest, контроль доступа по сервисам и аудит каждого чтения — три вещи, которых plaintext .env на диске и в CI дать не может.", correct: true },
    { label: "Ничего существенного — оба держат значение вне git, значит для прода они эквивалентны.", misconception: "Вне git — это пол, а не цель. .env лежит в открытом виде на каждом ноутбуке и CI-раннере, читается любым, у кого есть доступ к машине, без контроля доступа и без аудита, кто что читал, — именно это и чинит manager." },
    { label: "Manager делает учётку нечитаемой, так что само приложение её не сможет утечь.", misconception: "Приложение всё равно получает и использует значение в открытом виде для аутентификации. Manager управляет тем, кто может читать, и фиксирует, что читали, — он не делает пригодный к использованию секрет нечитаемым." },
    { label: "Он убирает необходимость когда-либо менять пароль.", misconception: "Manager, хранящий статическое значение, всё ещё держит долгоживущий секрет, который надо ротировать; он лишь централизует и аудирует хранение. Ротация становится автоматической только при переходе к динамическим секретам." },
  ]}
/>

<Quiz
  id="06-secrets-choice-3"
  lessonSlug="06-secrets"
  lang="ru"
  question="Сервис берёт динамическую креду БД из Vault с TTL в один час вместо статического пароля. Что реально даёт TTL?"
  choices={[
    { label: "Он ограничивает радиус поражения: утёкшая креда сама истекает в течение часа, даже если никто не заметил, и каждый потребитель получает уникальную, ограниченную по правам, аудируемую идентичность.", correct: true },
    { label: "Он шифрует креду in transit, чтобы её нельзя было перехватить на проводе.", misconception: "Шифрование на проводе — это задача TLS, и оно отдельно от TTL. TTL задаёт, как долго утёкшая креда остаётся валидной, а не наблюдаема ли она в полёте." },
    { label: "Он гарантирует, что БД никогда не перегружается слишком большим числом соединений.", misconception: "Лимиты соединений — это пулинг и квоты. Короткий TTL ограничивает время жизни утёкшей креды; о числе соединений он ничего не говорит." },
    { label: "Он означает, что корневые креды, которые использует Vault, никогда не надо ротировать.", misconception: "Динамические секреты автоматизируют листовые креды, но root- и конфигурационные креды движка остаются долгоживущими и требуют периодической ротации — у Vault для этого есть отдельная операция rotate-root." },
  ]}
/>

<Quiz
  id="06-secrets-choice-4"
  lessonSlug="06-secrets"
  lang="ru"
  question="Команда шифрует конфиг-блоб на 2 МБ, отправляя весь payload в KMS на каждый запрос, и упирается в лимит частоты запросов KMS. Что меняет envelope encryption?"
  choices={[
    { label: "KMS генерирует data key; вы шифруете payload локально этим data key и храните рядом с шифротекстом только обёрнутый KMS data key. KMS видит крошечные ключи, а не ваши данные, поэтому всё масштабируется и ваш plaintext не покидает машину.", correct: true },
    { label: "Он отправляет данные в KMS меньшими кусками, чтобы каждый запрос укладывался в лимит размера.", misconception: "Envelope encryption вообще не прогоняет payload через KMS. KMS лишь оборачивает маленький data key; основной объём шифруется локально этим ключом, так что KMS никогда не видит payload." },
    { label: "Он заменяет KMS локальным ключом, так что KMS больше не участвует.", misconception: "KMS остаётся в цепочке — он защищает data key, а ключ-шифрующий-ключ никогда не покидает KMS. Вы лишь переносите массовое шифрование локально; data key всё равно должен распаковываться KMS для расшифровки." },
    { label: "Он убирает необходимость когда-либо ротировать ключ шифрования.", misconception: "Envelope encryption ортогонален ротации. Вы всё равно ротируете ключ-шифрующий-ключ в KMS; выигрыш в том, что вы перевёртываете data keys, а не перешифровываете каждый payload." },
  ]}
/>

<Quiz
  id="06-secrets-choice-5"
  lessonSlug="06-secrets"
  lang="ru"
  question="Аудитор находит один общий API-токен, используемый двенадцатью сервисами и дающий полные admin-права. Сервис отчётности скомпрометирован, атакующий удалил продовую таблицу. Какой принцип это сдержал бы?"
  choices={[
    { label: "Least privilege с идентичностями по сервисам: read-only креда отчётности, отличная от всех остальных, не может удалить таблицу, а аудит-лог называет точно, какой сервис действовал.", correct: true },
    { label: "Шифрование at rest хранилища токенов — будь токен зашифрован, атакующий не смог бы им воспользоваться.", misconception: "Шифрование at rest защищает хранимое значение, а не его использование. Сервис расшифровывает и использует токен легально; ущерб пришёл от слишком широких прав и общей идентичности, а не от способа хранения." },
    { label: "Более длинный, высокоэнтропийный токен, чтобы его нельзя было угадать.", misconception: "Стойкость токена останавливает подбор, а не злоупотребление украденной-но-валидной кредой. Взлом использовал настоящий токен с admin-правами, к тому же общий — энтропия не относится ни к одному из провалов." },
    { label: "Более частая ротация токена по фиксированному расписанию.", misconception: "Расписанная ротация сокращает экспозицию, но ничего не делает со слишком широкими правами и невозможностью понять, какой из двенадцати сервисов действовал. Least privilege плюс уникальные идентичности — вот что ограничивает и атрибутирует ущерб." },
  ]}
/>

<Quiz
  id="06-secrets-choice-6"
  lessonSlug="06-secrets"
  lang="ru"
  question="В шестилетней организации один и тот же ключ Stripe вставлен в четыре репозитория, три CI-системы, две вики и тред в Slack, и никто не знает, какую копию читает каждый сервис. Как это называется и какой устойчивый фикс?"
  choices={[
    { label: "Secret sprawl: одно и то же значение скопировано во множество неотслеживаемых мест. Фикс — единый источник истины: один secret manager, из которого каждый потребитель читает в рантайме, чтобы было одно место для ротации и один аудит-лог.", correct: true },
    { label: "Обычная избыточность — несколько копий означают высокую доступность секрета, и это хорошо.", misconception: "Копии — это не избыточность, а неконтролируемые дубликаты. Каждый — отдельная поверхность утечки, и ротация должна попасть в каждую копию, иначе устаревшее значение ломает сервис. Sprawl — это пассив, а не доступность." },
    { label: "Это проблема ротации — ротируйте ключ, и дубликаты разрешатся сами.", misconception: "Ротация делает sprawl хуже: надо найти и обновить каждую разбросанную копию одновременно, иначе что-то сломается. Корень — само дублирование; фикс — централизация к одному источнику, и тогда ротация — единая безопасная операция." },
    { label: "Это проблема шифрования — зашифруйте каждую копию там, где она лежит.", misconception: "Шифрование двенадцати разбросанных копий оставляет двенадцать копий и двенадцать мест для ротации и аудита. Дефект — дублирование по неотслеживаемым местам, а не то, что каждая копия в открытом виде." },
  ]}
/>

<Recap lang="ru">
Через весь юнит проходит одна цепочка решений: секрет, попавший в репозиторий, уже утёк, поэтому экспозицию завершает ротация, а не удаление; лестница зрелости идёт от захардкоженного к .env, к manager (шифрование at rest, контроль доступа, аудит) и к динамическим короткоживущим кредам, чей TTL и есть радиус поражения; envelope encryption позволяет KMS защищать данные на масштабе, оборачивая локальный data key вместо вашего payload; least privilege с уникальными идентичностями по сервисам сдерживает и атрибутирует взлом; а secret sprawl лечится единым источником истины, а не новыми копиями. Каждый ответ сводится к ограничению и атрибуции ущерба заранее. Теперь, когда коллега потянется к gitignore или force-push, чтобы «почистить» утёкший ключ, вы знаете, какой первый вопрос задать: а ротация была?
</Recap>
