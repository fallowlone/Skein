---
concepts:
- git-leak-remediation
- dynamic-secrets
- envelope-encryption
- secret-scanning
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://cheatsheetseries.owasp.org/cheatsheets/Secrets_Management_Cheat_Sheet.html
- https://developer.hashicorp.com/vault/tutorials/get-started/understand-static-dynamic-secrets
status: ready
summary: "Чтение реальных конфигов и кода — закоммиченный .env, выборка динамической креды из Vault, хелпер envelope encryption на KMS и утекающий лог — предскажите провал и выберите фикс с наибольшим рычагом."
title: 'Управление секретами: чтение кода и конфигов'
track: security
unit: 06-secrets
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Провалы с секретами диагностируются в диффах, конфигах и логах. Прочтите каждый сниппет, предскажите, что утекает или ломается, затем выберите фикс, который senior-инженер сделал бы первым, — до того как тянуться к флагу настройки.
</Hook>

<Goal lang="ru">
Отработайте цикл, который вы запускаете в каждом инциденте с секретами: прочитать артефакт, найти, где значение покидает свою границу доверия или где неверно время жизни, и взять фикс с наибольшим рычагом.
</Goal>

## Сниппет 1 — закоммиченный .env

```bash
# config/.env  — отслеживается в git, в прошлый вторник запушен в публичное зеркало
DATABASE_URL=postgres://app:Sup3rSecret@db.prod.internal:5432/main
STRIPE_SECRET_KEY=sk_live_51HxQ...redacted...9aZ
JWT_SIGNING_KEY=hs256-7f3e9c1b2a8d4e6f
```

<Quiz
  id="06-secrets-code-1"
  lessonSlug="06-secrets"
  lang="ru"
  question="Этот .env закоммитили и запушили в публичное зеркало. Вы добавляете его в .gitignore и удаляете. Какое утверждение верно?"
  choices={[
    { label: "Все три значения уже утекли и должны быть ротированы у источников (пароль БД, ключ Stripe, ключ подписи JWT); .gitignore и удаление ничего не делают с копиями в истории и на публичном зеркале.", correct: true },
    { label: "Добавление в .gitignore убирает его из репозитория, так что секреты теперь в безопасности.", misconception: ".gitignore лишь останавливает отслеживание будущих изменений неотслеживаемого файла; он ничего не делает с уже закоммиченным. Три секрета остаются в истории и на зеркале — их надо ротировать." },
    { label: "Важен только живой ключ Stripe; пароль БД внутренний, так что с ним всё в порядке.", misconception: "Внутренний хостнейм — не защита: креда валидна, и атакующий, добравшийся до сети (или через другую утёкшую точку входа), её использует. Каждый закоммиченный секрет ротируется, а не сортируется по предполагаемой чувствительности." },
    { label: "Удаление файла новым коммитом вычищает его из истории.", misconception: "История append-only; коммит-удаление добавляет удаление, но прежние коммиты (и публичное зеркало) всё ещё содержат значения. Нужно переписать историю, и даже тогда экспозицию завершает ротация." },
  ]}
/>

## Сниппет 2 — выборка динамической креды из Vault

```go
// Получить короткоживущую креду БД из движка database в Vault.
secret, err := client.Logical().Read("database/creds/reporting-ro")
if err != nil {
    return err
}
user := secret.Data["username"].(string)
pass := secret.Data["password"].(string)
// leaseDuration — секунды до авто-отзыва этой креды в Vault
lease := time.Duration(secret.LeaseDuration) * time.Second

db := openDB(user, pass)
defer db.Close()
// ... долгоживущий воркер использует db часами ...
```

<Quiz
  id="06-secrets-code-2"
  lessonSlug="06-secrets"
  lang="ru"
  question="Роль выпускает креду с часовым lease, но этот воркер работает часами. В чём дефект и фикс?"
  choices={[
    { label: "Код никогда не продлевает lease, поэтому Vault авто-отзывает креду после TTL, и обращения воркера к БД начинают падать посреди работы. Фикс: продлевать lease до истечения (lease lifetime watcher) или перезапрашивать свежую креду при приближении к истечению.", correct: true },
    { label: "Чтение из database/creds/reporting-ro неверно — динамические секреты надо читать один раз на старте и кешировать навсегда.", misconception: "Кеширование динамической креды навсегда сводит на нет её смысл: вся суть в коротком TTL, ограничивающем радиус поражения. Креду нужно продлевать или перезапрашивать, а не закреплять на время жизни процесса." },
    { label: "Баг — это read-only роль; долгоживущему воркеру нужна admin-креда.", misconception: "Права задаются нуждой, а не длительностью — воркер отчётности должен оставаться read-only (least privilege). Реальный дефект — управление временем жизни: lease никогда не продлевается и истекает под воркером." },
    { label: "Ничего страшного; Vault держит креду живой, пока открыто соединение.", misconception: "Vault отзывает по TTL lease независимо от открытых соединений; уже установленное соединение может выжить, но новая аутентификация падает. Надо продлевать lease или перезапрашивать — открытые сокеты lease не продлевают." },
  ]}
/>

## Сниппет 3 — хелпер envelope encryption на KMS

```python
def encrypt_blob(kms, key_id: str, plaintext: bytes) -> dict:
    # просим KMS data key: открытый для использования сейчас + обёрнутый для хранения
    resp = kms.generate_data_key(KeyId=key_id, KeySpec="AES_256")
    data_key = resp["Plaintext"]
    wrapped_key = resp["CiphertextBlob"]

    ciphertext = aes_gcm_encrypt(data_key, plaintext)   # шифруем ЛОКАЛЬНО

    return {
        "ciphertext": ciphertext,
        "data_key": data_key,        # хранится рядом с шифротекстом
        "wrapped_key": wrapped_key,
    }
```

<Quiz
  id="06-secrets-code-3"
  lessonSlug="06-secrets"
  lang="ru"
  question="Этот хелпер делает локальное AES-GCM шифрование data key, выданным KMS, — envelope encryption. Что разрушает всю схему?"
  choices={[
    { label: "Он хранит открытый data_key рядом с шифротекстом. Любой, кто прочтёт запись, расшифрует payload, ни разу не обратившись к KMS. Фикс: обнулить открытый data key после использования и хранить только wrapped_key, распаковывая через KMS для расшифровки.", correct: true },
    { label: "Локальное AES-GCM — это и есть изъян; всё шифрование должно происходить внутри KMS.", misconception: "Локальное массовое шифрование data key, выданным KMS, — это и есть смысл envelope encryption: оно масштабируется и держит plaintext вне провода. Дефект — хранение открытого data key, а не локальное шифрование." },
    { label: "generate_data_key надо вызвать один раз и переиспользовать data key для каждого блоба навсегда.", misconception: "Переиспользование одного data key для всего расширяет радиус поражения при компрометации одного ключа; предпочтительны data keys на контекст. В любом случае фатальный баг здесь — хранение открытого data key, а не частота его выпуска." },
    { label: "Возврат wrapped_key — это утечка, ведь он выведен из настоящего ключа.", misconception: "wrapped_key — это data key, зашифрованный под ключ KMS, который никогда не покидает KMS; его безопасно хранить, и именно его вы оставляете. Утечка — открытый data_key, лежащий рядом с шифротекстом." },
  ]}
/>

## Сниппет 4 — утекающий лог

```js
app.post("/charge", async (req, res) => {
  const cfg = { apiKey: process.env.STRIPE_SECRET_KEY, amount: req.body.amount };
  try {
    const charge = await stripe.charges.create(cfg);
    res.json({ id: charge.id });
  } catch (err) {
    // вывалим всё, что есть, чтобы помочь дебагу
    console.error("charge failed", { cfg, err, body: req.body });
    res.status(500).json({ error: "charge failed" });
  }
});
```

<Quiz
  id="06-secrets-code-4"
  lessonSlug="06-secrets"
  lang="ru"
  question="Сканер помечает этот хендлер. Где секрет покидает границу доверия и в чём фикс?"
  choices={[
    { label: "Ветка ошибки логирует cfg, содержащий apiKey — живой ключ Stripe теперь записан в логи (и отправлен в любой агрегатор логов). Фикс: никогда не логировать объекты с секретами; редактировать или опускать их и считать любой залогированный секрет утёкшим — ротировать.", correct: true },
    { label: "Утечка — это чтение process.env.STRIPE_SECRET_KEY; секреты надо хардкодить.", misconception: "Чтение из окружения нормально и стандартно; хардкод был бы хуже (снова в git). Утечка — логирование объекта, содержащего ключ, — чините лог, а не чтение." },
    { label: "Утечки нет — логи внутренние и под контролем доступа.", misconception: "Логи текут в агрегаторы, экспортируются и читаются множеством людей и инструментов; секрет в логах — известный путь утечки. Считайте любой секрет, попавший в лог, скомпрометированным." },
    { label: "Фикс — логировать на уровне debug, а не error, чтобы он попадал только в dev.", misconception: "Уровень лога не сдерживает секрет — debug-логи всё равно пишутся, отправляются и читаются, а конфиг часто включает их в проде во время инцидентов. Уберите секрет из лога полностью." },
  ]}
/>

<Recap lang="ru">
Каждый инцидент с секретами читается в артефактах: закоммиченный .env означает, что все три значения утекли и их надо ротировать, а не gitignore-ить; динамическая креда, чей lease никогда не продлевается, истекает посреди работы — продлевайте или перезапрашивайте; envelope encryption разрушается в момент, когда вы сохраняете открытый data key рядом с шифротекстом — храните только обёрнутый ключ и распаковывайте через KMS; а объект с секретом в строке лога отправляет ваш живой ключ в каждый агрегатор, поэтому редактируйте и ротируйте. Прочтите артефакт, найдите, где значение покидает границу или где неверно время жизни, и почините границу до того, как тронуть флаг.
</Recap>
