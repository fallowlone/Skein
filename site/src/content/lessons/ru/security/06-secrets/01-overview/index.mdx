---
concepts:
- hardcoded-credentials
- secret-rotation
- secrets-manager
- dynamic-secrets
- least-privilege
- secret-scanning
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://cheatsheetseries.owasp.org/cheatsheets/Secrets_Management_Cheat_Sheet.html
- https://www.helpnetsecurity.com/2024/12/02/revoke-exposed-aws-keys/
- https://developer.hashicorp.com/vault/tutorials/get-started/understand-static-dynamic-secrets
- https://snyk.io/articles/state-of-secrets/
spiral: []
status: ready
summary: Учётные данные в коде — это утёкший секрет. История git хранит его после
  удаления, сканеры находят публичные ключи примерно за минуту, и единственный реальный
  фикс — ротация, а не удаление. Лестница зрелости идёт от захардкоженных ключей к
  управляемому короткоживущему секрету.
title: 'Управление секретами: почему ключ в git — это уже взлом'
track: security
unit: 06-secrets
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
Джун пушит быстрый фикс в публичный репозиторий. В диффе спрятан AWS-ключ доступа, который был «просто для теста». За минуту его уже забрал автоматический сканер, следящий за публичным потоком событий GitHub; за одиннадцать минут кто-то поднимает флот GPU-инстансов для майнинга на счёте компании. Разработчик паникует и форс-пушит коммит, удаляющий строку. Ключ всё ещё работает — он по-прежнему живёт в истории git, и ротация, а не удаление, — единственное, что останавливает взлом.
</Hook>

<Crux>Секрет, закоммиченный в git, уже утёк: история хранит его после удаления, поэтому экспозицию завершает только ротация — не удаление.</Crux>

<Explanation>

## Захардкоженные учётные данные — это утёкшие учётные данные

Инстинкт — относиться к ключу в коде как к неряшливости, которую надо прибрать перед демо. Это не неряшливость, это экспозиция. В тот момент, когда учётные данные попадают в репозиторий, они есть у всех, у кого есть доступ на чтение, их несёт каждый клон, их может напечатать любой CI-лог, а если репозиторий когда-нибудь сделают публичным — они транслируются в интернет. GitHub зафиксировал более 39 миллионов утёкших секретов в 2024 году — рост на 67% за год, а GitGuardian насчитал 28,65 миллиона новых захардкоженных секретов, запушенных в публичные репозитории только за 2025 год. Это не редкая оплошность — это самый частый способ, которым реальные системы взламывают, поэтому OWASP относит управление секретами к первоклассным заботам.

Цифры по эксплуатации — та часть, что пугает людей и заставляет меняться. Исследователи, подбрасывавшие honeypot-ключи AWS, видели, как автоматические боты находят и используют их примерно за одну минуту, а security-фирмы измеряли, как сканеры забирают открытые ключи за одиннадцать минут и сразу запускают GPU-инстансы. Льготного периода нет. К моменту, когда ты замечаешь плохой коммит, атакующий уже использовал ключ.

## История git — append-only, поэтому удаление — это ложь

Ловушка, в которую попадает большинство инженеров: они делают `git rm` файла или удаляют строку, пушат и верят, что секрета больше нет. Его есть. Модель данных git — append-only: каждый коммит сохраняется, и секрет навсегда сидит в истории каждого клона и форка. Любой, кто запустит `git log -p` или любой сканер, проходящий полную историю (а они все так делают), читает его так же легко, как текущий код.

Поэтому очистка состоит из двух частей, и важна по-настоящему только одна. Историю можно переписать через `git filter-repo`, чтобы вычистить блоб из каждого коммита, — но это не помогает тем, кто уже сделал клон, и ты никогда не уверен, что копия не утекла. Шаг, который завершает взлом, — это **ротация**: отзови утёкшие учётные данные у источника, чтобы значение в руках атакующего стало бесполезным. Считай скомпрометированным каждый секрет, который хоть раз касался репозитория, и ротируй его. Переписывание истории — это гигиена; ротация — это фикс.

<Inset kind="why" lang="ru">
Хардкод — не единственный путь утечки. Тот же секрет может быть запечён в слой Docker-образа (любой, кто стянет образ, запускает `docker history` и читает его), напечатан в логи приложения или в подробную страницу ошибки, или собран в клиентский JavaScript-бандл и отгружен в каждый браузер. «Бэкендовый» секрет в Next.js-компоненте, который не server-only, — в одном неосторожном импорте от публичного бандла. Каждый из этих случаев — один и тот же провал: секрет покинул границу доверия.
</Inset>

## Лестница зрелости: хардкод → dotenv → управляемый

Есть чёткий прогресс в том, как команды обращаются с секретами, и большинство аварий случаются потому, что команда застряла на нижней ступени.

Визуал ниже — это лестница, на которую сеньор поднимается осознанно:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Ступень</th>
        <th class="border border-border px-3 py-2 text-left">Что даёт</th>
        <th class="border border-border px-3 py-2 text-left">Что всё ещё болит</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Хардкод в коде</td>
        <td class="border border-border px-3 py-2">Ничего — только удобство</td>
        <td class="border border-border px-3 py-2">Навсегда в истории git; утекает на каждом клоне, в логе, при публичном пуше</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>.env</code> + <code>.gitignore</code></td>
        <td class="border border-border px-3 py-2">Вне git, вне образа при правильной настройке</td>
        <td class="border border-border px-3 py-2">Plaintext на диске и в CI; живёт в env процесса; нет access control и аудита; один плохой <code>.gitignore</code> от катастрофы</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Менеджер секретов (Vault, AWS Secrets Manager, SOPS)</td>
        <td class="border border-border px-3 py-2">Шифрование at rest, access control, аудит-лог кто что читал</td>
        <td class="border border-border px-3 py-2">Операционная цена; всё ещё долгоживущий, если хранишь только статические значения</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Динамические, короткоживущие секреты</td>
        <td class="border border-border px-3 py-2">Учётка на запрос с TTL, авто-отзыв, аудит по каждому сервису</td>
        <td class="border border-border px-3 py-2">Больше всего настройки; нужна интеграция, которую понимает приложение</td>
      </tr>
    </tbody>
  </table>
</div>

Большой скачок — от `.env` к менеджеру секретов. Файл `.env` действительно лучше хардкода — он держит значение вне коммита — но это всё равно plaintext, лежащий на ноутбуке каждого разработчика и на CI-раннере, читаемый любым, у кого есть доступ к машине, и невидимый для любого аудита. Менеджер вроде HashiCorp Vault, AWS Secrets Manager или SOPS добавляет три вещи, которые `.env` дать не может: **шифрование at rest**, **access control** (этот сервис может читать этот секрет и никакой другой) и **аудит-след** каждого чтения. Когда случается инцидент, именно аудит-лог говорит, чью учётку ротировать и чего она касалась.

## Короткоживущий бьёт долгоживущий: TTL — это радиус взрыва

Верхняя ступень — то, к чему целятся сеньорские команды, и она меняет математику утечки. Статические долгоживущие учётные данные — самый частый вектор атаки именно потому, что они валидны, пока человек не вспомнит ротировать их, — что после утечки часто никогда. Динамический секрет генерируется по запросу со встроенным TTL и автоматически отзывается по истечении. Движок баз данных Vault, например, выпускает уникального пользователя БД на запрос с TTL в один час на проде; утёкшая учётка мертва за шестьдесят минут — заметил это кто-то или нет.

Из этого следуют ещё два свойства. Поскольку каждый потребитель получает **уникальную** учётку, аудит-лог может назвать точный экземпляр сервиса, который что-то сделал, — ты не пялишься на один общий username, используемый всем подряд. И динамические учётки делают **least privilege** применимым: каждый lease ограничен ровно теми правами, что нужны роли, поэтому украденный ключ read-only сервиса отчётов не может удалить продовую базу. Сеньорский трейдофф реален — динамические секреты дороже всего в настройке и требуют, чтобы приложение получало и продлевало lease, — но для всего, что защищает продовые данные, радиус взрыва в один час бьёт бесконечный.

<TradeoffMatrix
  id="06-secrets-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Бэкенд-сервису нужны учётные данные БД на проде. У команды есть спринт на это. Выбери подход, который защитит сеньор."
  options={[
    { name: "Динамические секреты из Vault: уникальный пользователь БД на запрос с TTL в 1 час, авто-отзыв", summary: "Короткоживущий, ограниченный.", correct: true, justification: "Радиус взрыва утечки — это TTL. Утёкшая учётка бесполезна в течение часа, даже если никто не заметил; каждый экземпляр сервиса получает уникальный username, поэтому аудит-лог называет ровно то, что действовало; lease ограничен least privilege. Цена — интеграция получения и продления lease, посильная за спринт, и это единственный вариант, который заранее ограничивает ущерб от утечки." },
    { name: "Долгоживущий пароль БД в AWS Secrets Manager с access control и аудитом", summary: "Управляемый, но статический.", justification: "Реальное улучшение над .env — шифрование at rest, access control и аудит чтений. Но значение всё ещё долгоживущее, поэтому утечка валидна, пока человек не ротирует, а после инцидента это часто никогда. Хорош как шаг; не сеньорская конечная точка, когда динамические секреты на столе." },
    { name: "Файл .env, задеплоенный на сервер, держится вне git через .gitignore", summary: "Plaintext на диске.", justification: "Лучше хардкода, потому что вне коммита, но это plaintext на машине и в CI, читаемый любым, у кого есть доступ, без access control и без аудита. Один криво отредактированный .gitignore — и он снова в git. Норм для хобби-проекта, не для продовых данных." },
    { name: "Захардкодить в конфиг-модуль и добавить файл в .gitignore, чтобы он не закоммитился", summary: "Всё ещё в коде.", justification: "Конфиг-модуль — это всё ещё исходный код на диске и почти наверняка уже отслеживаемый или вот-вот будет — .gitignore помогает только неотслеживаемым файлам, и один git add -f или неосторожный коммит кладёт секрет в историю навсегда. Это ровно та ступень, с которой и говорит уйти весь урок." },
  ]}
/>

<Quiz
  id="06-secrets-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Ты обнаружил, что API-ключ закоммитили в репозиторий месяц назад. Удаляешь строку и форс-пушишь. Секрет теперь в безопасности?"
  choices={[
    { label: "Нет — ключ всё ещё в истории git и на каждом клоне; нужно ротировать (отозвать) его у источника", correct: true },
    { label: "Да — форс-пуш переписал историю, поэтому строки больше нигде нет", misconception: "Форс-пуш меняет только текущую вершину твоего remote; каждый существующий клон и форк всё ещё держит старый коммит, и ты никогда не подтвердишь, что копия не утекла. Удаление — не отзыв." },
    { label: "Да, пока репозиторий приватный", misconception: "Приватность урезает аудиторию, но не риск — любой с доступом на чтение, любой CI-лог и любое будущее переключение видимости всё ещё раскрывают его. Фикс — ротация независимо от видимости." },
    { label: "Да — git rm удаляет файл из истории", misconception: "git rm удаляет файл из рабочего дерева и будущих коммитов, а не из прошлых. История git — append-only; блоб остаётся, пока не перепишешь историю, и даже тогда важна именно ротация." },
  ]}
/>

<Quiz
  id="06-secrets-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Почему динамическая короткоживущая учётка БД бьёт долгоживущую для прода?"
  choices={[
    { label: "Её TTL ограничивает радиус взрыва — утёкшая учётка авто-истекает (например, за час), и каждый потребитель получает уникальную, ограниченную, аудируемую личность", correct: true },
    { label: "Она шифрует учётку, чтобы её нельзя было прочитать", misconception: "Приложение всё равно читает учётку в plaintext, чтобы её использовать; выигрыш — в коротком времени жизни и scope на потребителя, а не в нечитаемости значения." },
    { label: "Она убирает необходимость в аудит-логе", misconception: "Наоборот: уникальные учётки на потребителя делают аудит-лог точнее, называя конкретный экземпляр, который действовал, вместо одного общего пользователя." },
    { label: "Это значит, что больше никогда ничего не надо ротировать", misconception: "Динамические секреты автоматизируют ротацию, а не отменяют — сам движок и корневые учётки всё равно нуждаются в периодической ротации; приложение просто перестаёт работать с долгоживущими ключами." },
  ]}
/>

<DragOrder
  id="06-secrets-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Ты только что нашёл живой облачный ключ, закоммиченный в публичный репозиторий. Расставь реакцию от первого к последнему:"
  items={[
    { id: "a", label: "Ротируй учётку сейчас — отзови утёкший ключ у источника, чтобы он перестал работать" },
    { id: "b", label: "Проверь аудит/биллинг-логи провайдера на то, что ключ уже сделал" },
    { id: "c", label: "Перепиши историю git (git filter-repo), чтобы вычистить блоб из каждого коммита" },
    { id: "d", label: "Добавь pre-commit сканер секретов (gitleaks/trufflehog), чтобы это не повторилось" },
    { id: "e", label: "Перенеси секрет в менеджер с access control и короткоживущими lease" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Секрет в git утёк, а не неряшлив — сканеры используют публичные ключи примерно за минуту. Удаление не чинит (история append-only); ротируй у источника. Поднимайся от хардкода к управляемому короткоживущему секрету.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-secrets-retrieval"
  lang="ru"
  questions={[
    { q: "Коллега закоммитил пароль БД, потом удалил строку и запушил. Объясни, почему это не решает проблему и какая реальная ремедиация.", a: "История git — append-only, поэтому удаление строки в новом коммите не убирает секрет из предыдущего коммита — он по-прежнему живёт в полной истории репозитория и в каждом клоне и форке, который кто-то сделал. Любой сканер или любой, кто запустит git log -p, читает его тривиально, поэтому с точки зрения безопасности пароль утёк в момент пуша, а утёкшие ключи на публичных репозиториях используют примерно за минуту. Удаление — косметика. Реальная ремедиация — это ротация: отозвать пароль на базе, чтобы значение в руках атакующего перестало работать, затем проверить аудит и биллинг-логи провайдера, чтобы увидеть, что было сделано, пока он был живым. Как гигиену можно ещё переписать историю через git filter-repo, чтобы вычистить блоб, но это не делает задним числом безопасными копии, которые люди уже склонировали, — именно поэтому фикс это ротация, а не удаление. Наконец, добавь pre-commit сканер секретов, чтобы следующий поймали ещё до того, как он покинет ноутбук." },
    { q: "Проведи коллегу вверх по лестнице зрелости секретов и объясни, почему каждая ступень лучше, закончив тем, почему короткоживущие динамические секреты — цель.", a: "Ступень один — хардкод, что является просто утёкшей учёткой, навсегда сидящей в истории git. Ступень два — файл .env вне git через .gitignore: лучше, потому что значение вне коммита и вне образа, но это всё ещё plaintext на каждом ноутбуке и CI-раннере, читаемый любым, у кого есть машина, без access control и без аудита, и в одном плохом .gitignore от катастрофы. Ступень три — менеджер секретов — HashiCorp Vault, AWS Secrets Manager или SOPS — который добавляет три вещи, которых .env не может: шифрование at rest, access control, чтобы только нужный сервис мог прочитать данный секрет, и аудит-след каждого чтения, который и говорит, чью учётку ротировать после инцидента. Ступень четыре — динамические короткоживущие секреты: вместо хранения статического значения менеджер выпускает уникальную учётку на запрос с TTL — движок БД Vault обычно использует TTL в один час на проде — и автоматически отзывает её по истечении. Это делает TTL радиусом взрыва любой утечки, даёт каждому потребителю уникальную, ограниченную least-privilege личность, которую аудит-лог называет точно, и полностью убирает долгоживущие ключи из приложения. Это дороже всего в настройке, но для всего, что охраняет продовые данные, окно экспозиции в один час бьёт бесконечное." },
  ]}
/>

<Recap lang="ru">
Учётные данные в исходном коде — это не вопрос аккуратности, а утёкший секрет, и утёкшие ключи на публичных репозиториях используют примерно за минуту автоматические сканеры, которые тут же поднимают ресурсы на твоём счёте. Определяющая ловушка в том, что история git — append-only: удаление строки или даже форс-пуш не убирает секрет из прошлых коммитов или из чьего-то клона, поэтому единственное, что завершает экспозицию, — это ротация, отзыв значения у источника, подкреплённый проверкой аудит и биллинг-логов на то, что уже произошло. Дальше сеньор поднимается по лестнице зрелости: прочь от хардкода, мимо файла .env (вне git, но всё ещё plaintext, неконтролируемый и неаудируемый), в менеджер секретов, добавляющий шифрование at rest, access control и аудит-след, и наконец к динамическим короткоживущим секретам, чей TTL ограничивает радиус взрыва любой утечки, обеспечивая least privilege на каждого потребителя. Страховочная сетка под всем этим — pre-commit сканер секретов, чтобы следующая ошибка была поймана ещё до того, как покинет машину.
</Recap>
