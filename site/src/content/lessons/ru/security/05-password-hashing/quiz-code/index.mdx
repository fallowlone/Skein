---
concepts:
- argon2id-params
- naive-fast-hash
- timing-safe-compare
- rehash-on-login
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html
- https://datatracker.ietf.org/doc/rfc9106/
status: ready
summary: "Читай реальные auth-сниппеты — параметры Argon2id, наивное хранение SHA-256, timing-unsafe сравнение и отсутствующий rehash-on-login — и выбирай фикс, который сделает senior первым."
title: 'Хеширование паролей: разбор кода'
track: security
unit: 05-password-hashing
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги в паролях прячутся в коде, который выглядит нормально и проходит happy-path тест. Прочитай каждый сниппет, найди дефект, который вскрыла бы утечка или нагрузочный тест, и выбери фикс, за которым senior тянется первым.
</Hook>

<Goal lang="ru">
Отработай цикл ревью: прочитать auth-путь, найти криптографический или тайминговый дефект и выбрать максимально рычажную правку до отправки в production.
</Goal>

## Сниппет 1 — наивное хранение

```js
import { createHash } from "node:crypto";

function storePassword(user, password) {
  const hash = createHash("sha256").update(password).digest("hex");
  db.users.update(user.id, { passwordHash: hash });
}
```

<Quiz
  id="05-password-hashing-code-1"
  lessonSlug="05-password-hashing"
  lang="ru"
  question="Что здесь не так и каков один максимально рычажный фикс?"
  choices={[
    { label: "Два дефекта: SHA-256 — быстрый хеш (~22 миллиарда попыток/с на одной GPU), и нет salt на пользователя (одинаковые пароли коллизируют, поддаются rainbow tables). Чини оба разом, перейдя на медленный, memory-hard KDF: argon2.hash(password) с Argon2id m=19 MiB, t=2, p=1, который сам генерит и встраивает salt.", correct: true },
    { label: "Всё нормально, если ещё добавить колонку salt.", misconception: "Salt убивает rainbow tables, но скорость SHA-256 не трогает — утёкшая колонка всё равно ломается за часы. Надо менять алгоритм на медленный KDF, а не просто прикручивать salt." },
    { label: "Просто перейди на SHA-512 ради более длинного, сильного дайджеста.", misconception: "SHA-512 тоже быстрый универсальный хеш; более длинный вывод не замедляет перебор. Фикс — намеренно медленный memory-hard KDF, а не больший быстрый хеш." },
    { label: "Закодируй хеш в base64 вместо hex для усиления.", misconception: "Кодировка косметична и ничего не меняет в взламываемости. Проблема — выбор алгоритма и отсутствующий salt." },
  ]}
/>

## Сниппет 2 — параметры Argon2id

```js
import argon2 from "argon2";

async function hashPassword(password) {
  return argon2.hash(password, {
    type: argon2.argon2id,
    memoryCost: 512,   // KiB
    timeCost: 1,
    parallelism: 1,
  });
}
```

<Quiz
  id="05-password-hashing-code-2"
  lessonSlug="05-password-hashing"
  lang="ru"
  question="Выбор алгоритма верный. Что не так с настройкой?"
  choices={[
    { label: "memoryCost равен 512 KiB — намного ниже минимума OWASP в 19456 KiB (19 MiB) — а timeCost равен 1, ниже минимума t=2. При 512 KiB исчезает memory-hardness, которая душит параллелизм GPU. Ставь memoryCost: 19456, timeCost: 2, parallelism: 1, затем поднимай под свой бюджет задержки.", correct: true },
    { label: "argon2id — неверный вариант; для хранения паролей используй argon2i.", misconception: "argon2id — ровно верный вариант: он смешивает data-independent и data-dependent проходы ради устойчивости и к GPU, и к side-channel. Баг — заниженные memory и time cost, а не вариант." },
    { label: "parallelism: 1 слишком мал и калечит безопасность.", misconception: "p=1 соответствует рекомендации OWASP; parallelism — ручка пропускной способности, а не рычаг безопасности. Реальный дефект — memory cost в 512 KiB." },
    { label: "Ничего — это консервативные, безопасные дефолты.", misconception: "512 KiB — это примерно 1/38 от минимума OWASP в 19 MiB, поэтому GPU снова гоняет много попыток параллельно. Эти параметры опасно слабые, а не консервативные." },
  ]}
/>

## Сниппет 3 — сравнение

```js
function verifyResetToken(provided, stored) {
  // оба — hex-кодированные HMAC-дайджесты
  if (provided === stored) {
    return true;
  }
  return false;
}
```

<Quiz
  id="05-password-hashing-code-3"
  lessonSlug="05-password-hashing"
  lang="ru"
  question="Здесь два секретных дайджеста сравниваются через ===. Почему это проблема и каков фикс?"
  choices={[
    { label: "=== замыкается на первом различающемся байте, поэтому возвращается тем быстрее, чем меньше ведущих байт совпало — тайминговый side-channel, которым атакующий восстанавливает секрет побайтно. Используй constant-time сравнение (crypto.timingSafeEqual на Buffer равной длины). Verify-функции KDF вроде bcrypt.compare / argon2.verify уже делают это внутри.", correct: true },
    { label: "=== нормально для строк; тайминг важен только для чисел.", misconception: "Строковый === в V8 замыкается на первом несовпавшем символе, поэтому его длительность зависит от числа совпавших ведущих символов — ровно та утечка. Используй constant-time сравнение." },
    { label: "Баг в том, что возвращается boolean; надо бросать исключение при несовпадении.", misconception: "Возврат boolean — корректный дизайн API. Дефект — тайминговая вариативность ===, а не тип возврата." },
    { label: "Просто перехешируй оба значения перед сравнением, и баг уйдёт.", misconception: "Перехеширование не делает финальное сравнение constant-time, а сравнение перехешей через === имеет ту же утечку. Используй timing-safe примитив равенства на байтах." },
  ]}
/>

## Сниппет 4 — вход без миграции

```js
async function login(email, password) {
  const user = await db.users.findByEmail(email);
  if (!user) return null;
  const ok = await argon2.verify(user.passwordHash, password);
  if (!ok) return null;
  return issueSession(user);
}
```

<Quiz
  id="05-password-hashing-code-4"
  lessonSlug="05-password-hashing"
  lang="ru"
  question="Verify верный и constant-time. Что этот вход упускает, что важно по мере старения параметров?"
  choices={[
    { label: "Он никогда не обновляет устаревшие хеши. После успешного verify проверь, использует ли user.passwordHash устаревшие параметры (argon2.needsRehash), и, пока ещё держишь plaintext для этого запроса, перехешируй по текущей стоимости и обнови строку. Это мигрирует всю таблицу за недели без принудительных сбросов.", correct: true },
    { label: "Ничего — раз хеш сохранён, его трогать нельзя.", misconception: "Железо ускоряется, поэтому параметры 2020 сегодня слабы. Вход — единственный момент, когда ты держишь plaintext и можешь прозрачно перехешировать, ровно тогда миграция и должна происходить." },
    { label: "Надо перехешировать при КАЖДОМ входе независимо от параметров.", misconception: "Перехеширование при каждом входе тратит CPU и бесполезно, когда параметры уже совпадают. Перехешируй только когда needsRehash сообщает, что хранимые параметры устарели." },
    { label: "Надо заставить пользователя сбросить пароль, чтобы обновить.", misconception: "Принудительный сброс не нужен и враждебен пользователю. Plaintext уже есть при успешном входе, поэтому можно перехешировать молча без сброса." },
  ]}
/>

<Recap lang="ru">
Это дефекты, которые находишь в реальных auth-ревью: быстрый хеш без salt должен стать медленным, memory-hard KDF; Argon2id с заниженным memory cost выбрасывает свойство, душащее GPU, поэтому соблюдай OWASP m=19 MiB/t=2/p=1; сравнения секретов должны быть constant-time (crypto.timingSafeEqual или собственный verify KDF), а не ===; а корректный verify всё равно нуждается в rehash-on-login через needsRehash, чтобы держать work factor актуальным без принудительных сбросов. Диагностируй алгоритм, параметры, тайминг и миграцию — именно в этом порядке. Теперь, открывая auth-PR, эти четыре вопроса срабатывают автоматически — и ты ловишь проблему дня утечки до того, как она уходит в продакшн.
</Recap>
