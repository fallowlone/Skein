---
concepts:
- offline-cracking
- per-user-salt
- pepper
- memory-hardness
- work-factor-migration
- timing-safe-compare
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html
- https://datatracker.ietf.org/doc/rfc9106/
status: ready
summary: "Промпты на свободное припоминание по юниту хеширования паролей. Сначала ответь своими словами, затем раскрой модельный ответ и сравни."
title: 'Хеширование паролей: свободное припоминание'
track: security
unit: 05-password-hashing
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт проговори или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет материал.
</Hook>

<Goal lang="ru">
Восстанови ключевые механизмы юнита — почему быстрые хеши проигрывают, что делают salt и pepper по отдельности, почему memory-hardness бьёт сырую медленность, как мигрирует work factor и почему нельзя писать своё сравнение — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:load
  id="05-password-hashing-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему быстрый универсальный хеш вроде SHA-256 — неверный инструмент для хранения паролей, даже с salt на пользователя?", a: "Модель угроз — офлайн-перебор: как только у атакующего есть твоя колонка хешей из утечки, его больше не ограничивает твой login-эндпоинт — он гоняет попытки локально на полной скорости железа. SHA-256 спроектирован быстрым (фингерпринтит файлы гигабайтами в секунду), и одна RTX 4090 даёт около 22 миллиардов попыток SHA-256 в секунду; восемь в облаке — почти 180 миллиардов. Против стандартных словарей плюс правил мутации все слабые и средние пароли падают за часы. Salt чинит другую проблему — делает одинаковые пароли разными хешами и убивает rainbow tables — но скорость не трогает. Фикс — сменить алгоритм на намеренно медленный, memory-hard KDF, чтобы одна проверка при входе стоила сотни миллисекунд, а миллиард офлайн-попыток стал экономически нерентабельным." },
    { q: "В чём разница между salt и pepper и где живёт каждый?", a: "Salt — случайное значение (минимум 16 байт из CSRNG), генерируемое на пользователя. Его единственная задача — уникальность: он делает так, что два пользователя с одинаковым паролем дают разные хеши, что убивает rainbow tables и вынуждает ломать каждую учётку отдельно. Salt НЕ секретный — он хранится прямо рядом с хешем, обычно встроен в закодированную строку вывода KDF, и это нормально. Pepper — единый секрет, подмешиваемый в каждый хеш (часто через HMAC), но хранимый ВНЕ базы — в KMS, HSM или секрете приложения. Это defense-in-depth для частого взлома, когда утекает только БД, а секрет приложения нет: без pepper утёкшие salted-хеши вообще не взломать. Итого: salt = на пользователя, открытый, в строке, убивает rainbow tables; pepper = глобальный, секретный, вне БД, защищает от утечки только БД. Можно использовать оба." },
    { q: "Почему memory-hardness калечит атакующего сильнее, чем простое повышение числа итераций по CPU?", a: "Структурное преимущество атакующего — параллелизм: у GPU тысячи ядер и она гоняет тысячи попыток разом, поэтому чисто медленный-по-CPU хеш (например, большой счётчик итераций PBKDF2) просто делится по этим ядрам, и атакующий всё равно выигрывает по пропускной способности. Memory-hardness меняет экономику, заставляя каждую попытку выделять блок быстрой RAM — минимум OWASP для Argon2id это m=19456 (19 MiB). У GPU ограниченная пропускная способность памяти, поэтому она тянет лишь горстку попыток по 19 MiB параллельно вместо тысяч. Ты бьёшь прямо по аппаратному преимуществу, а не только по тактовой частоте — поэтому Argon2id (и scrypt) сопротивляются GPU и ASIC куда лучше PBKDF2." },
    { q: "Назови три принятых современных алгоритма с параметрами OWASP и скажи, когда PBKDF2 — правильный выбор.", a: "Argon2id (предпочтителен): победил в Password Hashing Competition 2015, стандартизирован в RFC 9106, memory-hard; минимум OWASP m=19456 (19 MiB), t=2, p=1. Бери вариант id ради устойчивости и к GPU, и к side-channel, не i и не d. scrypt: тоже memory-hard, старше Argon2; OWASP N=2^17, r=8, p=1 — крепкий выбор там, где Argon2 недоступен. bcrypt (legacy, всё ещё приемлем): на основе Blowfish, медленный и с salt, но НЕ memory-hard; минимум cost factor 10, каждый +1 удваивает работу — годится для существующих систем, но следи за обрезкой 72 байт. PBKDF2 существует для FIPS-сред (600 000+ итераций с HMAC-SHA-256), но не memory-hard и слабейший из четырёх против GPU — тянись за ним только когда режим соответствия заставляет." },
    { q: "Что такое ловушка 72 байт у bcrypt, как она вызывала реальные обходы аутентификации и каков правильный фикс?", a: "bcrypt молча игнорирует всё после первых 72 байт ввода — следствие ограничения key-schedule у Blowfish. Поэтому два ввода с одинаковыми первыми 72 байтами хешируются в одно значение, что вносило вклад в реальные production-обходы аутентификации, где слишком длинные вводы схлопывались в совпадающие хеши. Наивный «фикс» через пред-хеширование быстрым хешем и base64-кодирование может выстрелить в ногу: дайджест SHA-256 — 32 байта, но его hex/base64 форма 44–64 байта, и приписывание других данных может снова выкинуть за 72, заново обрезая ту энтропию, что ты хотел сохранить. Правильный паттерн — пред-хешировать и передавать СЫРЫЕ байты в пределах 72, либо просто использовать Argon2id, у которого такого лимита нет. Как минимум проверяй длину ввода и отклоняй или пред-хешируй до того, как bcrypt его увидит." },
    { q: "Как держать work factor актуальным со временем и почему сравнение паролей должно быть constant-time?", a: "Параметры стоимости не «настроил-и-забыл»: железо ускоряется каждый год, поэтому стоимость, бравшая 250 мс в 2020, сегодня дешева. Считай work factor значением, которое поднимаешь по расписанию, и мигрируй при входе — когда пользователь успешно аутентифицируется, а его хеш на устаревших параметрах, прозрачно перехешируй plaintext, который держишь в этом запросе, по новой стоимости и обнови строку. За недели вся таблица мигрирует без принудительных сбросов. Для сравнения используй constant-time (timing-safe) проверку: наивный storedHash === computedHash может вернуться быстрее или медленнее в зависимости от числа совпавших ведущих байт, утекая информацию атакующему. Стандартные verify-функции (bcrypt.compare, Argon2 verify) уже сравнивают за постоянное время, поэтому вызывай их, а не пиши своё сравнение." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, у тебя есть хребет юнита: защищаемая атака — офлайн-перебор на полной скорости железа, что делает быстрые хеши неверным инструментом; salt на пользователя (открытый, в строке) убивает rainbow tables, а pepper (секретный, вне БД) защищает от утечки только БД; memory-hardness бьёт сырую медленность, атакуя параллелизм GPU; принятые алгоритмы — Argon2id (предпочтителен), scrypt и bcrypt (а PBKDF2 только под FIPS), каждый с именованными параметрами OWASP; обрезка 72 байт у bcrypt — реальная ловушка; а work factor поднимается по расписанию через rehash-on-login, со сравнением, выполняемым библиотекой за постоянное время, никогда руками.
</Recap>
