---
concepts:
- slow-kdf
- per-user-salt
- argon2id
- bcrypt-truncation
- work-factor
- timing-safe-compare
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html
- https://datatracker.ietf.org/doc/rfc9106/
status: ready
summary: "Тест с выбором на синтез по всему юниту — провал быстрого хеша, salt vs pepper, параметры Argon2id/bcrypt/scrypt, ловушка 72 байт, миграция work factor и timing-safe сравнение."
title: 'Хеширование паролей: тест с выбором ответа'
track: security
unit: 05-password-hashing
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает реальное решение — что отправить в код регистрации, как читать утечку, какую ручку покрутить — а не определение для заучивания.
</Hook>

<Goal lang="ru">
Убедись, что связываешь модель угроз (офлайн-перебор), защиты (медленный + с salt + memory-hard), именованные параметры и production-ловушки — тот синтез, к которому вёл урок.
</Goal>

<Quiz
  id="05-password-hashing-choice-1"
  lessonSlug="05-password-hashing"
  lang="ru"
  question="Таблица users утекла, пароли хранились как sha256(salt + password), salt уникален на пользователя. Насколько всё плохо?"
  choices={[
    { label: "Плохо. Salt блокирует rainbow tables и вынуждает ломать каждую учётку отдельно, но SHA-256 всё равно даёт ~22 миллиарда попыток/с на одной GPU — все слабые и средние пароли падают за часы. Salt у быстрого хеша чинит предвычисление, но не пропускную способность.", correct: true },
    { label: "Нормально — salt на пользователя означает, что хеши не взломать.", misconception: "Salt гарантирует только уникальность и убивает rainbow tables. Скорость он не трогает, а скорость — это вся проблема: офлайн-атакующий гоняет быстрый хеш на полной скорости железа по твоей колонке." },
    { label: "Плохо, потому что SHA-256 криптографически сломан и даёт коллизии на паролях.", misconception: "SHA-256 не сломан. Дефект чисто в том, что он быстрый — а быстрый это ровно то, что нужно офлайн-атакующему." },
    { label: "Нормально — 256 бит вывода слишком много, чтобы перебрать.", misconception: "Длина вывода тут ни при чём. Атакующий перебирает пароли, а не выводы хеша, поэтому важна метрика попыток/с — а она для SHA-256 катастрофически высока." },
  ]}
/>

<Quiz
  id="05-password-hashing-choice-2"
  lessonSlug="05-password-hashing"
  lang="ru"
  question="Коллега предлагает и salt, и pepper. Где живёт каждый и зачем оба?"
  choices={[
    { label: "Salt: на пользователя, случайный, НЕ секретный, хранится рядом с хешем — убивает rainbow tables и вынуждает ломать каждую учётку. Pepper: единый секрет, подмешиваемый в каждый хеш (часто через HMAC), хранится ВНЕ БД (KMS/HSM/env) — defense-in-depth для частого случая утечки только БД.", correct: true },
    { label: "Это одно и то же; использовать оба избыточно.", misconception: "Они решают разные задачи. Salt даёт уникальность в открытом виде; pepper даёт секрет, который атакующий не получает, когда утекла только база." },
    { label: "Оба надо хранить на пользователя в строке БД.", misconception: "Хранить pepper в той же БД, что утекает, перечёркивает весь его смысл. Его ценность в том, что он живёт там, куда взлом не дотянулся." },
    { label: "Pepper заменяет salt, ведь он секретный и сильнее.", misconception: "Pepper глобальный, поэтому не может сделать одинаковые пароли разными хешами. Salt на пользователя всё равно нужен для уникальности — они дополняют друг друга, а не заменяют." },
  ]}
/>

<Quiz
  id="05-password-hashing-choice-3"
  lessonSlug="05-password-hashing"
  lang="ru"
  question="Почему memory-hardness (Argon2id, scrypt) важнее против реального атакующего, чем просто сырая медленность по CPU (например, PBKDF2 с большим числом итераций)?"
  choices={[
    { label: "Преимущество атакующего — параллелизм. GPU гоняет тысячи попыток разом, поэтому медленный-по-CPU хеш просто делится по ядрам. Заставив каждую попытку выделять ~19 MiB быстрой RAM, ты душишь GPU, ограниченную пропускной способностью памяти — она тянет лишь несколько параллельно. Ты бьёшь прямо по аппаратному преимуществу.", correct: true },
    { label: "Memory-hard функции дают более длинные хеши, которые труднее обратить.", misconception: "Длина вывода не меняется. Memory-hardness поднимает стоимость RAM на одну попытку, что душит параллелизм — к размеру вывода это отношения не имеет." },
    { label: "PBKDF2 криптографически слабее, поэтому его хеши можно обратить.", misconception: "PBKDF2 не обратим; он просто не memory-hard, поэтому GPU дёшево его распараллеливает. Дело в экономике атакующего, а не во взломе." },
    { label: "Memory-hardness ускоряет вход для легитимных пользователей.", misconception: "Она делает одну проверку дороже по RAM, а не быстрее. Цель — сделать миллиард офлайн-попыток нерентабельным, оставив один вход приемлемым." },
  ]}
/>

<Quiz
  id="05-password-hashing-choice-4"
  lessonSlug="05-password-hashing"
  lang="ru"
  question="Нужно хранить пароли в новом коде без FIPS-ограничений. Какой выбор и параметры?"
  choices={[
    { label: "Argon2id с параметрами OWASP m=19456 (19 MiB), t=2, p=1, salt на пользователя, встроенный в вывод. Это стандарт RFC 9106: медленный, с salt и memory-hard — предпочтительный дефолт.", correct: true },
    { label: "bcrypt без проверки длины, cost 12.", misconception: "bcrypt при cost 10+ приемлем, но он не memory-hard и молча игнорирует ввод за пределами 72 байт — это надо охранять. Для нового кода Argon2id сильнее как дефолт." },
    { label: "PBKDF2-HMAC-SHA-256 с 600 000 итераций.", misconception: "PBKDF2 — слабейший из четырёх против GPU, потому что не memory-hard. Тянись за ним только когда режим соответствия FIPS заставляет." },
    { label: "SHA-512 с 1000 раундов перехеширования для медленности.", misconception: "Самопальный stretching — классическая ловушка: не memory-hard, слишком мало раундов, и ты отказываешься от рецензированного, устойчивого к side-channel дизайна. Используй проверенный KDF." },
  ]}
/>

<Quiz
  id="05-password-hashing-choice-5"
  lessonSlug="05-password-hashing"
  lang="ru"
  question="Отчёт об обходе аутентификации: две разные длинные парольные фразы пускают в один аккаунт, обе хранятся через bcrypt. Корневая причина?"
  choices={[
    { label: "bcrypt молча игнорирует всё после первых 72 байт (ограничение key-schedule у Blowfish). Два ввода с одинаковыми первыми 72 байтами хешируются одинаково. Фикс: проверять длину, либо пред-хешировать в сырые байты в пределах 72, либо использовать Argon2id, у которого такого лимита нет.", correct: true },
    { label: "salt у bcrypt совпал для двух паролей.", misconception: "Salt на пользователя на практике не коллизирует, и даже общий salt не сделал бы разные вводы совпадающими. Причина — обрезка по 72 байтам, а не salt." },
    { label: "Cost factor был слишком мал, поэтому хеши пересеклись.", misconception: "Cost factor управляет работой, а не коллизиями. Понижение ускоряет взлом, но никогда не делает два разных ввода одним хешем — это делает обрезка." },
    { label: "bcrypt сломан, его нельзя использовать.", misconception: "bcrypt приемлем по OWASP при cost 10+; обрезка — задокументированный край, от которого защищаются проверкой длины, а не фундаментальный взлом." },
  ]}
/>

<Quiz
  id="05-password-hashing-choice-6"
  lessonSlug="05-password-hashing"
  lang="ru"
  question="Твои параметры Argon2id настроены в 2020 на ~250 мс. Спустя годы ревью их помечает. Какова стандартная миграция и как сравнивать хеши при входе?"
  choices={[
    { label: "Поднимай work factor по расписанию и перехешируй прозрачно ПРИ ВХОДЕ: когда успешная аутентификация использует устаревшие параметры, перехешируй plaintext, который держишь в этом запросе, по новой стоимости и обнови строку — таблица мигрирует без принудительных сбросов. Сравнивай через constant-time функцию библиотеки; никогда не пиши storedHash === computed руками.", correct: true },
    { label: "Заставить всех сбросить пароль, чтобы обновить параметры.", misconception: "Принудительный сброс не нужен и враждебен пользователю. Plaintext есть только при входе — ровно тогда можно перехешировать прозрачно без сброса." },
    { label: "Оставить старые хеши как есть; сильные параметры только у новых регистраций.", misconception: "Это оставляет существующие учётки — основную массу таблицы — на слабом факторе навсегда. Rehash-on-login мигрирует их за недели без сбоев." },
    { label: "Сравнивать через storedHash === computedHash для чистой проверки равенства.", misconception: "Нативный === может по таймингу выдать, сколько ведущих байт совпало. Используй constant-time verify KDF (bcrypt.compare, Argon2 verify) — он timing-safe по дизайну." },
  ]}
/>

<Recap lang="ru">
Сквозная линия — одна модель угроз и один стек защиты. Атака — офлайн-перебор: утёкшая колонка хешей гоняется на полной скорости железа (~22 миллиарда SHA-256/с на GPU), что делает любой быстрый хеш неверным инструментом. Защита — медленный + с salt + memory-hard: salt на пользователя (не секретный, рядом с хешем) убивает rainbow tables, проверенный KDF с именованными параметрами (Argon2id m=19 MiB/t=2/p=1 предпочтителен, bcrypt cost 10+ или scrypt N=2^17 приемлемы, PBKDF2 только под FIPS), опциональный pepper вне БД, work factor, поднимаемый по расписанию через rehash-on-login, и constant-time verify библиотеки вместо самопального сравнения. Следи за ловушкой обрезки 72 байт в bcrypt.
</Recap>
