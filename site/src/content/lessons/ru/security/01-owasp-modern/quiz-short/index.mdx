---
concepts:
- broken-access-control
- deny-by-default
- cryptographic-failures
- insecure-design
- ssrf
- vulnerable-components
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://owasp.org/Top10/2021/
- https://owasp.org/Top10/2021/A01_2021-Broken_Access_Control/
- https://owasp.org/Top10/2021/A04_2021-Insecure_Design/
status: ready
summary: "Промпты на свободное припоминание по OWASP Top 10 (2021). Сначала ответь своими словами, потом открой модельный ответ и сравни."
title: 'OWASP Top 10: тест на припоминание'
track: security
unit: 01-owasp-modern
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание побеждает перечитывание. Для каждого промпта проговори или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет материал, когда ты ревьюишь реальный код.
</Hook>

<Goal lang="ru">
Реконструируй стержень юнита — почему Broken Access Control стал #1, что значит deny-by-default в коде, переосмысление причины вместо симптома, категории эпохи проектирования и как защищаются от SSRF — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:load
  id="01-owasp-modern-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему Broken Access Control поднялся до #1 в 2021 и что значит 'deny by default' в коде?", a: "Он поднялся с #5 до #1, потому что данные OWASP 2021 показали, что это одновременно самый распространённый класс — присутствует в 94% всех протестированных приложений, с наибольшим числом вхождений в датасете по 34 отображённым CWE — и среди самых вредоносных, ведь он позволяет пользователю читать или менять не свои данные. Две доминирующие production-формы: IDOR (сервер достаёт запись по присланному клиентом id без проверки владения) и отсутствие function-level authorization (привилегированный эндпоинт достижим лишь потому, что кнопку в UI скрыли). Deny by default значит, что любой доступ к ресурсу начинается с отказа: сервер на каждом запросе и на самой записи и действии вычисляет, авторизован ли аутентифицированный вызывающий — по серверному состоянию, а не по скрытому элементу UI или подделываемому claim клиента. В коде это ограничение запросов владельцем (WHERE id = ? AND owner_id = ?) и гейтинг привилегированных маршрутов явной проверкой роли/права." },
    { q: "Объясни переосмысление 'причина вместо симптома' в 2021 на примере Sensitive Data Exposure.", a: "Главное изменение 2021 было философским: список переорганизовали вокруг корневых причин вместо симптомов, переведя его примерно на 400 отображённых CWE (с около 30). 'Sensitive Data Exposure' описывал то, что ты видел после взлома — данные утекли. Его замена, 'Cryptographic Failures', называет то, что реально пошло не так: слабое или отсутствующее крипто, отсутствие TLS, захардкоженные ключи, сломанный выбор шифров. Выгода — выполнимость: категория, называющая режим отказа, против которого можно проектировать (включить TLS, ротировать ключи, убрать слабые шифры), полезнее той, что называет исход, замечаемый слишком поздно. Та же логика сформировала остальной список, поэтому он читается как карта того, как ломаются приложения, а не каталог симптомов взлома." },
    { q: "Что такое Insecure Design (A04), чем он отличается от бага в коде и почему его нельзя залатать?", a: "Insecure Design — это признание 2021 года, что некоторые уязвимости заложены до написания первой строки: флоу сброса пароля без rate limit, оформление заказа, доверяющее присланной клиентом цене, фича без продуманного abuse case. Это отсутствующее требование безопасности, а не дефект в иначе корректном коде: реализация добросовестно делает не то. Поэтому никакой фильтр ввода, санитайзер или патч его не закрывают — контроль вообще не был специфицирован. Фикс — shift-left: threat-modeling фичи, написание abuse cases рядом с user stories и встраивание контроля в архитектуру (серверная цена, rate limits, конечные автоматы, которые нельзя проскочить). Это знак взросления поля за пределы 'фильтруй ввод' к 'проектируй под наименьшие привилегии'." },
    { q: "Что такое SSRF (A10), почему он попал в рейтинг при скудных данных и как от него защищаться?", a: "SSRF — это когда сервер можно обманом заставить сделать запрос к выбранному атакующим URL — например, фичу 'загрузи это изображение' направить на http://169.254.169.254/, чтобы прочитать учётки cloud metadata. Он отображается примерно на один CWE и почти не имел исторических данных по частоте, но попал в рейтинг, потому что OWASP резервирует два из десяти слотов под риски, проголосованные опросом сообщества, и практики его выбрали. Защита слоями: валидируй и allowlist'и назначение (схему, хост, порт), а не blocklist; резолви имя хоста и отклоняй приватные, loopback и link-local диапазоны (в том числе после редиректов и DNS rebinding); отключай ненужные схемы URL; требуй IMDSv2 / убирай metadata-эндпоинт; и изолируй фетчер в сегменте сети, не достигающем внутренних сервисов." },
    { q: "Почему 'Vulnerable & Outdated Components' (A06) — твоя проблема и какой процесс не даёт категории дрейфовать?", a: "A06 вышел на видное место, потому что CVE зависимости становится твоей CVE в тот момент, когда она работает в твоём production — атакующему всё равно, что уязвимый код писал не ты. Современные приложения — в основном сторонний код, поэтому поверхность атаки доминируется транзитивными зависимостями, о которых ты можешь и не знать. Категория дрейфует (молча накапливает риск), когда нет дисциплины обновлений. Удерживающий процесс: вести SBOM, чтобы знать, что поставляешь, гонять сканирование зависимостей/CVE в CI (и постоянно, ведь новые CVE появляются против старых версий), удалять неиспользуемые зависимости, предпочитать поддерживаемые библиотеки и иметь быстрый путь патча, чтобы критическую CVE можно было выкатить в тот же день, а не ждать следующего релиза." },
    { q: "Как сеньор на самом деле должен читать Top 10 — как чек-лист или как что-то иное?", a: "Не как догму и не как pass/fail чек-лист, а как карту приоритизации, построенную на данных по 500 000+ реальных приложений. Сеньор читает её как 'где моё время потрачено лучше всего', и access control выигрывает по ожидаемой ценности: это самый распространённый класс и среди самых вредоносных, поэтому он заслуживает наибольшей строгости — серверные, deny-by-default проверки на каждом объекте и каждой функции. Остальное — карта покрытия: крипто в транзите и at rest, параметризованный доступ к данным, threat-modeled дизайны, процесс обновления зависимостей, hardened конфиги и структурированное security-логирование, чтобы обнаружить взлом, который ты не предотвратил. Рейтинг говорит, куда движется поле (SSRF заслужил слот голосованием), так что относись к нему как к ориентиру для взвешивания усилий, а не как к галочке." },
  ]}
/>

<Recap lang="ru">
Если ты смог реконструировать каждый ответ по памяти, ты держишь стержень юнита: Broken Access Control — #1, потому что он одновременно самый распространённый и среди самых вредоносных, чинится серверными deny-by-default проверками; список 2021 переорганизован вокруг корневых причин (Cryptographic Failures, не Sensitive Data Exposure); Insecure Design называет изъяны, которые не закрыть патчем; SSRF заслужил слот голосованием практиков и защищается allowlist'ингом и блокировкой metadata; уязвимые компоненты — это твои CVE и требуют SBOM плюс CI-сканирования; а весь список — карта приоритизации, а не чек-лист.
</Recap>
