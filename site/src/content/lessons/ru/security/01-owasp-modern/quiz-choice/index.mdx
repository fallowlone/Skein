---
concepts:
- broken-access-control
- cryptographic-failures
- injection
- insecure-design
- ssrf
- security-misconfiguration
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://owasp.org/Top10/2021/
- https://owasp.org/Top10/2021/A01_2021-Broken_Access_Control/
- https://owasp.org/Top10/2021/A10_2021-Server-Side_Request_Forgery_%28SSRF%29/
status: ready
summary: "Тест с выбором на синтез по OWASP Top 10 (2021) — access control, крипто, injection, insecure design, SSRF и misconfiguration как решения, которые ты принимаешь в реальном ревью."
title: 'OWASP Top 10: тест с выбором ответа'
track: security
unit: 01-owasp-modern
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего Top 10. Каждый отражает решение, которое ты принимаешь в реальном security-ревью — не определение для заучивания, а корневую причину, которую надо назвать, и фикс, к которому сеньор тянется первым.
</Hook>

<Goal lang="ru">
Убедись, что связываешь категории 2021 года с тем, как приложения реально ломаются: к какой корневой причине относится находка, почему сдвинулся рейтинг и какой устойчивый фикс вместо костыля.
</Goal>

<Quiz
  id="01-owasp-modern-choice-1"
  lessonSlug="01-owasp-modern"
  lang="ru"
  question="Эндпоинт выполняет SELECT * FROM invoices WHERE id = ?, где id берётся из URL и передаётся как параметр. Пользователь меняет id и читает чужой инвойс. Какая это категория и почему это НЕ injection?"
  choices={[
    { label: "Broken Access Control. Запрос параметризован, поэтому ничего не инъектируется — изъян в том, что сервер не проверяет принадлежность записи вызывающему. Фикс: ограничить по owner_id, вычисляемому на сервере из сессии.", correct: true },
    { label: "Injection — управляемый пользователем id попал в SQL-запрос.", misconception: "id привязан как параметр, то есть это данные, а не код; ничего не инъектируется. Дефект — отсутствие проверки авторизации на строку, это A01, а не A03." },
    { label: "Cryptographic Failures — инвойс должен был шифроваться at rest.", misconception: "Шифрование at rest защищает от украденного диска, а не от авторизованной сессии, читающей чужую строку. Недостаёт проверки владения, а не шифра." },
    { label: "Не уязвимость — id был валидным целым, а запрос безопасен.", misconception: "Синтаксически валидный запрос, возвращающий чужие данные, — хрестоматийная форма IDOR в Broken Access Control, категории #1 в 2021." },
  ]}
/>

<Quiz
  id="01-owasp-modern-choice-2"
  lessonSlug="01-owasp-modern"
  lang="ru"
  question="Почему список 2021 переименовал 'Sensitive Data Exposure' в 'Cryptographic Failures' и что это переосмысление меняет на практике?"
  choices={[
    { label: "Оно называет корневую причину (слабое/отсутствующее/неверно применённое крипто) вместо симптома (данные утекли), поэтому рекомендация указывает на действие — включить TLS, убрать слабые шифры, перестать хардкодить ключи — а не на исход, который замечаешь только после взлома.", correct: true },
    { label: "Потому что sensitive data exposure перестал быть проблемой после GDPR.", misconception: "Утечки никуда не делись; переименование — про категоризацию по причине, чтобы фикс был выполнимым, а не про исчезновение риска." },
    { label: "Потому что криптография теперь единственный способ утечки данных.", misconception: "Данные утекают через broken access control, misconfiguration и прочее. Переименование нацелено на крипто-подмножество по его причине, а не утверждает, что крипто — единственный путь." },
    { label: "Чтобы категория звучала строже для комплаенс-аудиторов.", misconception: "Изменение методологическое — весь список 2021 переорганизован вокруг корневых причин против симптомов — а не косметическое или ради аудита." },
  ]}
/>

<Quiz
  id="01-owasp-modern-choice-3"
  lessonSlug="01-owasp-modern"
  lang="ru"
  question="Флоу оформления заказа принимает цену товара от клиента и списывает её. Нет injection, нет auth-бага, TLS включён. Ревьюер всё равно помечает это. Какая категория и почему нельзя залатать?"
  choices={[
    { label: "Insecure Design (A04). Доверие присланной клиентом цене — это отсутствующее требование безопасности, а не баг в коде: сервер обязан пересчитать цену из своего каталога. Никакой фильтр ввода не чинит флоу, спроектированный доверять не тому источнику; фикс — threat-modeling фичи.", correct: true },
    { label: "Injection — санитизировать поле цены перед списанием.", misconception: "Цена не интерпретируется как код, поэтому санитизировать нечего. Изъян в том, что авторитетное значение берётся из недоверенного источника — дефект проектирования, A04." },
    { label: "Security Misconfiguration — флаг конфигурации разрешает клиентскую цену.", misconception: "Никакой неверной настройки нет; приложение построено так, чтобы доверять клиентской цене. Это архитектурное решение, то есть A04 Insecure Design." },
    { label: "Всё нормально — клиент это браузер самого пользователя, значит цена надёжна.", misconception: "Всё, что присылает клиент, управляемо атакующим через консоль или прокси. Авторитетные значения вроде цены должны выводиться на сервере." },
  ]}
/>

<Quiz
  id="01-owasp-modern-choice-4"
  lessonSlug="01-owasp-modern"
  lang="ru"
  question="SSRF (A10) отображается примерно на один CWE и почти не имел исторических данных по частоте, но всё равно попал в рейтинг 2021. Что его включение говорит о том, как читать Top 10?"
  choices={[
    { label: "Две из десяти категорий приходят из опроса сообщества, а не только из данных, поэтому список может выявлять зарождающиеся риски, которые инструменты ещё не измерили. SSRF проголосовали практики, считая его серьёзным — список отслеживает, куда движется поле, а не только где уже были старые сканеры.", correct: true },
    { label: "SSRF попал, потому что имел больше всего вхождений в датасете.", misconception: "Всё наоборот — у SSRF почти не было исторических данных и он отображается примерно на один CWE. Он попал через опрос практиков, и именно поэтому его включение примечательно." },
    { label: "Рейтинг чисто data-driven, значит SSRF — артефакт измерения.", misconception: "Методология намеренно резервирует два слота под проголосованные риски, чтобы список не ограничивался тем, что инструменты уже хорошо измеряют." },
    { label: "SSRF — просто частный случай injection и не заслуживает своего слота.", misconception: "SSRF — это когда сервер обманом заставляют запросить выбранный атакующим URL, что отличается от инъекции кода в интерпретатор. Его собственный слот отражает отдельную корневую причину." },
  ]}
/>

<Quiz
  id="01-owasp-modern-choice-5"
  lessonSlug="01-owasp-modern"
  lang="ru"
  question="Не-админ напрямую через curl шлёт POST /api/admin/users/7/promote, и это срабатывает — единственной 'проверкой' было то, что кнопка админки скрыта в его UI. Категория и фикс?"
  choices={[
    { label: "Broken Access Control — отсутствует function-level authorization. Скрытие UI не является контролем; эндпоинт обязан проверять роль/право на сервере, deny by default, на самом действии.", correct: true },
    { label: "Security Misconfiguration — админский маршрут оставили открытым.", misconception: "Маршрут и должен существовать; дефект в том, что он не проверяет роль вызывающего. Это отсутствующая проверка авторизации (A01), а не неверная настройка." },
    { label: "Cryptographic Failures — подписать запрос, чтобы его нельзя было подделать.", misconception: "Подпись доказывает, что сообщение не подменили, а не то, что этот вызывающий вправе повысить пользователя. Недостаёт серверной проверки роли." },
    { label: "Это не уязвимость, потому что легитимные админы используют тот же эндпоинт.", misconception: "Общие эндпоинты — норма; баг в том, что авторизация вообще не проверяется, поэтому не-админ, дотянувшись напрямую, добивается успеха." },
  ]}
/>

<Quiz
  id="01-owasp-modern-choice-6"
  lessonSlug="01-owasp-modern"
  lang="ru"
  question="Сервис поставляет известно-уязвимую версию библиотеки логирования (CVE класса Log4Shell) и возвращает клиентам стек-трейсы с полными версиями фреймворка. Какие две категории в игре и какой фикс выше по leverage?"
  choices={[
    { label: "Vulnerable & Outdated Components (A06) для CVE и Security Misconfiguration (A05) для многословных ошибок. Обновление зависимости выше по leverage — CVE удалённо эксплуатируема, а утечка версий лишь ускоряет атакующего; чини оба, но патч закрывает реальную дыру.", correct: true },
    { label: "Только Injection — библиотека логирования интерпретирует ввод атакующего.", misconception: "Баги класса Log4Shell действительно связаны с попаданием недоверенного ввода в интерпретатор, но OWASP относит проблему 'мы поставили известно-уязвимую зависимость' к A06; многословные стек-трейсы — отдельно A05." },
    { label: "Только Security Misconfiguration — многословные ошибки и есть вся проблема.", misconception: "Скрытие стек-трейсов само по себе оставляет эксплуатируемую CVE живой. Обновление зависимости — несущий фикс; подавление ошибок — hardening сверху." },
    { label: "Ни то, ни другое — CVE зависимости это проблема мейнтейнера, а не твоя.", misconception: "A06 существует именно потому, что CVE твоих зависимостей становятся твоими CVE в production; обновление и SBOM на тебе." },
  ]}
/>

<Recap lang="ru">
Сквозная линия — чтение находки обратно к её корневой причине: параметризованный запрос, всё равно отдающий чужие строки, — это access control, а не injection; переименованная категория указывает на причину, а не симптом; доверенная клиентская цена — insecure design, который не чинится фильтром; SSRF заслужил слот голосованием практиков, а не данными; скрытый UI никогда не авторизация; а уязвимая зависимость плюс многословные ошибки — две категории, где патч важнее hardening. Трать строгость там, где ожидаемый ущерб выше — access control в первую очередь — и всегда предпочитай фикс корневой причины костылю. Теперь, когда встречаешь незнакомую находку, первый вопрос: что именно пошло не так на уровне дизайна — и что нужно изменить, чтобы это больше не могло случиться?
</Recap>
