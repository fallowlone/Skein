---
concepts:
- broken-access-control
- owasp-top-10
- insecure-design
- ssrf
- deny-by-default
- shift-left-security
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://owasp.org/Top10/2021/
- https://owasp.org/Top10/2021/A01_2021-Broken_Access_Control/
- https://owasp.org/Top10/2021/A04_2021-Insecure_Design/
- https://owasp.org/Top10/2021/A10_2021-Server-Side_Request_Forgery_%28SSRF%29/
spiral: []
status: ready
summary: 'В версии 2021 Broken Access Control стал #1 (найден в 94% протестированных
  приложений), а вся область сместилась от «фильтруй плохой ввод» к «проектируй под
  least privilege и deny by default». Это карта того, как веб-приложения реально ломаются.'
title: 'OWASP Top 10 (2021): что изменилось и почему это важно'
track: security
unit: 01-owasp-modern
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Залогиненный пользователь открывает свой счёт по `GET /api/invoices/4815` и от скуки правит URL на `4816`. Он загружается — чужой счёт, с именем, адресом и последними четырьмя цифрами карты. Никакого эксплойта, ни SQL, ни payload. Просто число, которому сервер поверил, потому что в запросе была валидная сессионная кука. Это IDOR, хрестоматийное лицо Broken Access Control — и в данных OWASP 2021 он занял вершину списка, найденный в 94% всех протестированных приложений.
</Hook>

<Crux>В Top 10 2021 Broken Access Control стал #1, а область сместилась от «фильтруй плохой ввод» к «deny by default».</Crux>

<Explanation>

## Что такое Top 10 на самом деле

OWASP Top 10 — это не чек-лист багов, а основанный на данных рейтинг *категорий* риска, которые чаще всего встречаются в реальных веб-приложениях. Версия 2021 собрана из контрибьюченных данных по более чем 500 000 приложениям от дюжины организаций: восемь из десяти категорий пришли прямо из этих данных, а две добавлены по опросу сообщества, чтобы захватить риски, которые инструменты слишком молоды, чтобы измерить. Эта последняя деталь важнее, чем кажется: именно поэтому категория может попасть в рейтинг без больших исторических чисел за спиной.

Крупнейшее изменение 2021 года — философское. Список реорганизовали вокруг **корневых причин вместо симптомов**. «Sensitive Data Exposure» — старый #3 — описывал то, что ты *видишь* после взлома. Его замена, «Cryptographic Failures», называет то, что реально *пошло не так*: слабая криптография, отсутствие TLS, захардкоженные ключи. Весь список пересобрали против примерно 400 размеченных CWE (против примерно 30 раньше), так что категория теперь группирует реальный режим отказа, против которого можно проектировать, а не исход, который замечаешь слишком поздно.

## Broken Access Control: новый #1

Контроль доступа отвечает на один вопрос: *можно ли этому аутентифицированному пользователю сделать именно это действие с именно этим ресурсом?* Когда ответ вычислен неверно — или не вычислен вовсе — получаешь Broken Access Control. Он поднялся с #5 в 2017 до **#1 в 2021**, разметив 34 CWE, с наибольшим числом вхождений в датасете среди всех категорий. Заглавная цифра: 94% протестированных приложений показали ту или иную форму broken access control. Это одновременно самый частый и один из самых серьёзных классов.

В продакшене доминируют две формы отказа:

- **IDOR (Insecure Direct Object Reference):** сервер использует переданный клиентом id, чтобы достать запись, но никогда не проверяет, что запись принадлежит вызывающему. `GET /api/invoices/4816` возвращает чужой счёт, потому что запрос был `WHERE id = ?`, а не `WHERE id = ? AND owner_id = ?`.
- **Отсутствие авторизации на уровне функции:** UI прячет кнопку админа, поэтому endpoint никто не охраняет. Не-админ, который шлёт `POST /api/admin/users/{id}/promote` напрямую, получает повышение, потому что единственной «проверкой» была невидимость ссылки.

Рефлекс сеньора — **deny by default**: любой доступ к ресурсу начинается с «нет», а авторизация проверяется на стороне сервера на конкретной записи и действии — никогда не выводится из спрятанного UI-элемента, claim в JWT, который клиент мог подделать, или самого факта наличия сессии.

<FlowDiagram
  label="Путь атаки IDOR: атакующий меняет id в URL, запрос всё ещё несёт валидную сессионную куку, сервер достаёт запись по id без проверки владения, и возвращается чужой счёт; авторизация deny-by-default, ограниченная владельцем, разрывает цепочку."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "edit", label: "Атакующий правит id", sub: "4815 → 4816" },
    { id: "session", label: "Валидная сессия", sub: "кука приложена" },
    { id: "query", label: "Запрос сервера", sub: "WHERE id = ?" },
    { id: "leak", label: "Чужая запись", sub: "взлом" },
    { id: "fix", label: "Deny by default", sub: "AND owner_id = ?" },
  ]}
  edges={[
    { from: "edit", to: "session" },
    { from: "session", to: "query", label: "доверяет" },
    { from: "query", to: "leak", label: "нет проверки" },
    { from: "query", to: "fix", label: "фикс" },
  ]}
  caption="Broken Access Control (A01) в одном пути: сервер доверяет валидной сессии как авторизации и достаёт запись по одному id. Ограничение запроса владельцем — WHERE id = ? AND owner_id = ?, проверяемое на сервере — превращает утечку в отказ."
/>

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Ранг 2021</th>
        <th class="border border-border px-3 py-2 text-left">Категория</th>
        <th class="border border-border px-3 py-2 text-left">Изменение vs 2017</th>
        <th class="border border-border px-3 py-2 text-left">Что это реально значит</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">A01</td>
        <td class="border border-border px-3 py-2">Broken Access Control</td>
        <td class="border border-border px-3 py-2">Поднялся с #5</td>
        <td class="border border-border px-3 py-2">94% приложений; IDOR + пропуск authz</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">A02</td>
        <td class="border border-border px-3 py-2">Cryptographic Failures</td>
        <td class="border border-border px-3 py-2">Переименован из Sensitive Data Exposure</td>
        <td class="border border-border px-3 py-2">Причина, не симптом: слабая крипта, нет TLS</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">A03</td>
        <td class="border border-border px-3 py-2">Injection</td>
        <td class="border border-border px-3 py-2">Опустился с #1</td>
        <td class="border border-border px-3 py-2">Теперь включает XSS; параметризуй всё</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">A04</td>
        <td class="border border-border px-3 py-2">Insecure Design</td>
        <td class="border border-border px-3 py-2">НОВАЯ</td>
        <td class="border border-border px-3 py-2">Threat-model до кода; часть дефектов не патчится</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">A05</td>
        <td class="border border-border px-3 py-2">Security Misconfiguration</td>
        <td class="border border-border px-3 py-2">Поднялся с #6</td>
        <td class="border border-border px-3 py-2">Дефолтные креды, болтливые ошибки, открытые бакеты</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">A06</td>
        <td class="border border-border px-3 py-2">Vulnerable & Outdated Components</td>
        <td class="border border-border px-3 py-2">Поднялся с #9</td>
        <td class="border border-border px-3 py-2">CVE твоих зависимостей — это твои CVE</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">A10</td>
        <td class="border border-border px-3 py-2">SSRF</td>
        <td class="border border-border px-3 py-2">НОВАЯ (по голосованию)</td>
        <td class="border border-border px-3 py-2">Сервер запрашивает выбранный атакующим URL</td>
      </tr>
    </tbody>
  </table>
</div>

## Новые категории эпохи проектирования

Два добавления 2021 года отмечают взросление области за пределы «вычисти ввод». **Insecure Design (A04)** — это признание того, что часть уязвимостей зашита ещё до написания первой строки: флоу сброса пароля без rate limit, оформление заказа, доверяющее присланной клиентом цене. Из ошибочного дизайна не выпатчишься; фикс — **shift-left**: смоделируй угрозы для фичи, пиши abuse cases рядом с user stories и закладывай контроль в архитектуру. Это разница между ошибкой кода и отсутствующим требованием безопасности.

**SSRF (A10)** выделяется по другой причине: у него почти не было исторических данных — он размечает по сути единственный CWE — и всё же он попал в рейтинг, потому что практики *проголосовали* за него через опрос сообщества. SSRF — это когда твой сервер можно обманом заставить сделать запрос к URL, который контролирует атакующий: фича «загрузи это изображение», направленная на `http://169.254.169.254/`, чтобы прочитать креды из метаданных облака. Его присутствие в списке, впереди лучше задокументированных багов, — самый чёткий сигнал, что Top 10 отслеживает, куда движется область, а не только то, где старые сканеры уже побывали.

<Inset kind="why" lang="ru">
Почему Injection — многолетний #1 — падает до #3 в 2021? Не потому, что он стал реже, а потому, что инструменты стали хороши. ORM, параметризованные запросы и шаблонизаторы с авто-экранированием сделали *частый* путь безопасным по умолчанию, поэтому средняя частота упала, даже когда категория расширилась, поглотив XSS. Урок: риск падает в рейтинге, когда безопасный вариант становится лёгким вариантом — ровно тот исход, к которому стремится хороший дизайн.
</Inset>

## Как сеньор читает список

Рейтинг — это инструмент приоритизации, а не евангелие. Сеньор читает его как «где моё время потрачено лучше всего», и контроль доступа выигрывает по ожидаемой ценности: это самый частый класс *и* один из самых разрушительных, поэтому он заслуживает наибольшей строгости — серверные проверки на каждый объект и каждую функцию, написанные как deny-by-default. Остальное считай картой покрытия: крипта в транзите и в покое, параметризованный доступ к данным, процесс обновления зависимостей, чтобы A06 не накапливался молча, ужесточённые конфиги и структурное логирование безопасности, чтобы вообще обнаружить взлом, который не предотвратил.

<TradeoffMatrix
  id="01-owasp-overview-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Endpoint `GET /api/invoices/{id}` возвращает счёт по id. Пользователь меняет id в URL и видит чужой счёт. Выбери правильный фикс."
  options={[
    { name: "Ограничивай каждый запрос вызывающим: `WHERE id = ? AND owner_id = ?`, проверка на сервере на реальной записи", summary: "Deny by default.", correct: true, justification: "Это корневой фикс IDOR. Сервер обязан проверять, что аутентифицированный вызывающий владеет (или иначе авторизован на) конкретной записью, на каждый доступ, вычисляя это на сервере из сессии — никогда из присланного клиентом claim. Deny-by-default значит, что доступ начинается с «нет» и даётся только когда владение доказано, ровно как предписывает A01." },
    { name: "Заменить последовательный integer на случайный UUID, чтобы id были неугадываемы", summary: "Спрятать id.", justification: "Это безопасность через неясность. UUID труднее угадать, но id постоянно утекают — в логах, реферерах, расшаренных ссылках, прошлых ответах. Любой, кто получит валидный id, всё равно получит запись, потому что проверки авторизации по-прежнему нет. Это чуть повышает планку, оставляя саму дыру открытой." },
    { name: "Спрятать ссылку на счёт в UI для тех, кто им не владеет", summary: "Спрятать кнопку.", justification: "Скрытие на стороне клиента — не контроль доступа. Endpoint по-прежнему достижим напрямую через curl или консоль браузера. Это классическая ошибка «отсутствует авторизация на уровне функции» — путать то, что показывает UI, с тем, что проверяет сервер." },
    { name: "Добавить правило WAF, блокирующее запросы, где id не совпадает с недавними id пользователя", summary: "Накладной фильтр.", justification: "WAF — хрупкая, постфактумная догадка о намерении, которая не может надёжно знать, какими записями владеет пользователь. Он навешивает эвристику на периметр вместо фикса отсутствующей авторизации в источнике, и будет и пропускать реальные атаки, и блокировать легитимный доступ." },
  ]}
/>

<Quiz
  id="01-owasp-overview-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Почему «Sensitive Data Exposure» переименовали в «Cryptographic Failures» в 2021?"
  choices={[
    { label: "Чтобы назвать корневую причину (слабая/отсутствующая крипта) вместо симптома (данные утекли)", correct: true },
    { label: "Потому что утечки чувствительных данных прекратились после принятия GDPR", misconception: "Утечки данных никуда не делись. Переименование — про категоризацию по причине, чтобы рекомендации были применимы, а не про исчезновение риска." },
    { label: "Потому что криптография — единственный способ, которым данные вообще утекают", misconception: "Данные утекают многими путями (например, через broken access control). Смысл переименования — формулировка через корневую причину для крипто-подмножества, а не утверждение, что крипта — единственная причина." },
    { label: "Чтобы категория звучала технологичнее для комплаенс-аудитов", misconception: "Изменение методологическое — 2021 реорганизовал весь список вокруг причин vs симптомов, а не ради косметики или аудита." },
  ]}
/>

<Quiz
  id="01-owasp-overview-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Не-админ шлёт `POST /api/admin/users/7/promote` напрямую, и это срабатывает, хотя кнопка админа спрятана в его UI. Что это за категория и каков фикс?"
  choices={[
    { label: "Broken Access Control — проверяй роль/права на endpoint на стороне сервера, deny by default", correct: true },
    { label: "Cryptographic Failures — зашифровать тело запроса", misconception: "Шифрование защищает данные в транзите; оно ничего не делает с тем, авторизован ли этот вызывающий повышать пользователя. Не хватает серверной проверки авторизации." },
    { label: "Injection — санитизировать id пользователя перед использованием", misconception: "Здесь нет недоверенных данных, интерпретируемых как код. Проблема в том, что привилегированное действие выполняется без проверки роли вызывающего." },
    { label: "Это не уязвимость, потому что кнопка спрятана", misconception: "Скрытие UI — не контроль доступа. Endpoint достижим напрямую; это отсутствие авторизации на уровне функции, ключевая часть A01." },
  ]}
/>

<DragOrder
  id="01-owasp-overview-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь категории Top 10 2021 по рангу, от A01 до A05:"
  items={[
    { id: "a", label: "Broken Access Control (94% приложений; IDOR + пропуск authz)" },
    { id: "b", label: "Cryptographic Failures (переименован из Sensitive Data Exposure)" },
    { id: "c", label: "Injection (теперь включает XSS; опустился с #1)" },
    { id: "d", label: "Insecure Design (новая — threat-model до кода)" },
    { id: "e", label: "Security Misconfiguration (дефолтные креды, болтливые ошибки)" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Top 10 2021 ранжирует риск по корневой причине. Broken Access Control — #1 (94% приложений) — чини серверными deny-by-default проверками на каждый объект и функцию. Проектируй под least privilege, не фильтруй ввод.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-owasp-overview-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, почему Broken Access Control прыгнул на #1 в 2021 и что значит «deny by default» в коде.", a: "Broken Access Control поднялся с #5 на #1, потому что данные OWASP 2021 показали, что это и самый частый класс — присутствует в 94% всех протестированных приложений, с наибольшим числом вхождений в датасете среди 34 размеченных CWE — и один из самых разрушительных, поскольку позволяет пользователю читать или менять данные, которые ему не принадлежат. Две доминирующие продакшен-формы: IDOR (сервер достаёт запись по присланному клиентом id без проверки владения) и отсутствие авторизации на уровне функции (привилегированный endpoint достижим лишь потому, что кнопку UI спрятали). «Deny by default» значит, что любой доступ к ресурсу начинается с отказа: сервер обязан вычислять на каждый запрос и на конкретной записи и действии, авторизован ли аутентифицированный вызывающий — используя серверное состояние, а не спрятанный UI-элемент или подделываемый claim клиента. В коде это выглядит как ограничение запросов владельцем (WHERE id = ? AND owner_id = ?) и гейтинг привилегированных маршрутов явной проверкой роли/прав, а не предположение, что валидная сессия означает право." },
    { q: "Чем особенны Insecure Design и SSRF и почему их появление в 2021 сигнализирует о взрослении области?", a: "Оба новые в 2021 и оба указывают за пределы старого мышления «фильтруй плохой ввод». Insecure Design (A04) — это признание того, что часть уязвимостей архитектурна: отсутствие rate limit, оформление заказа, доверяющее присланной клиентом цене, — и их нельзя выпатчить, потому что требования безопасности там не было изначально. Его фикс — shift-left: моделируй угрозы для фичи, пиши abuse cases рядом с user stories и встраивай контроль в дизайн. SSRF (A10) поражает тем, что у него почти не было исторических данных (он размечает примерно один CWE), и всё же он попал в рейтинг — за него проголосовали через опрос сообщества OWASP, который даёт две из десяти категорий именно чтобы захватить нарождающиеся риски, которые инструменты ещё не измерили. SSRF — это когда сервер обманом заставляют запросить выбранный атакующим URL, например направить фичу «загрузи это изображение» на endpoint метаданных облака, чтобы украсть креды. Вместе они показывают, как Top 10 взрослеет из каталога багов ввода в карту риска на уровне дизайна, отслеживая, куда движется область, а не только то, где старые сканеры уже искали." },
  ]}
/>

<Recap lang="ru">
OWASP Top 10 (2021) — это основанный на данных рейтинг категорий риска, собранный из 500 000+ реальных приложений и реорганизованный вокруг корневых причин вместо симптомов — поэтому «Sensitive Data Exposure» стал «Cryptographic Failures». Broken Access Control — заглавная история: он поднялся с #5 на #1, встречается в 94% протестированных приложений и проявляется в продакшене как IDOR и отсутствие авторизации на уровне функции. Фикс — авторизация deny-by-default, проверяемая на сервере на каждом объекте и действии, никогда не выводимая из спрятанной кнопки или присланного клиентом claim. Две новые категории эпохи проектирования — Insecure Design и SSRF — отмечают взросление области от «фильтруй плохой ввод» к «проектируй под least privilege», причём SSRF заслужил своё место голосом практиков несмотря на тонкие исторические данные. Читай список как карту приоритетов: трать больше всего строгости там, где ожидаемый ущерб выше, а остальное держи как покрытие, которому не даёшь дрейфовать.
</Recap>
