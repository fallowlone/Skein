---
concepts:
- broken-access-control
- ssrf
- security-misconfiguration
- injection
- deny-by-default
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://owasp.org/Top10/2021/
- https://owasp.org/Top10/2021/A01_2021-Broken_Access_Control/
- https://owasp.org/Top10/2021/A10_2021-Server-Side_Request_Forgery_%28SSRF%29/
status: ready
summary: "Читай реальные сниппеты кода, конфига и лога — проверку access control, SSRF-фетч, конфиг CORS — называй категорию OWASP и выбирай фикс, который сеньор делает первым."
title: 'OWASP Top 10: чтение кода и конфигов'
track: security
unit: 01-owasp-modern
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Уязвимости живут в коде и конфиге, а не в абстракции. Читай каждый сниппет так, как читал бы в pull request, называй категорию OWASP и выбирай фикс, к которому сеньор тянется первым — корневую причину, а не костыль.
</Hook>

<Goal lang="ru">
Потренируй цикл ревью: прочитай хендлер или конфиг, определи, к какой категории Top 10 он относится, предскажи эксплойт и потянись за структурным фиксом до любого периметрового патча.
</Goal>

## Сниппет 1 — проверка владения

```js
// GET /api/invoices/:id
app.get("/api/invoices/:id", requireLogin, async (req, res) => {
  const invoice = await db.invoice.findUnique({
    where: { id: Number(req.params.id) },
  });
  if (!invoice) return res.status(404).end();
  res.json(invoice);   // returns whatever row matched the id
});
```

<Quiz
  id="01-owasp-modern-code-1"
  lessonSlug="01-owasp-modern"
  lang="ru"
  question="requireLogin выполняется, значит вызывающий аутентифицирован. В чём уязвимость и какой минимальный корректный фикс?"
  choices={[
    { label: "Broken Access Control (IDOR). Аутентификация — не авторизация: хендлер не проверяет, что invoice принадлежит req.user. Ограничь выборку: findFirst({ where: { id, ownerId: req.user.id } }) и 404 при промахе, вычисляемое на сервере.", correct: true },
    { label: "Безопасно — requireLogin уже гейтит маршрут.", misconception: "requireLogin доказывает, кто вызывающий, а не к чему он вправе обращаться. Любой залогиненный пользователь читает любой инвойс, меняя id; это хрестоматийный IDOR." },
    { label: "Injection — Number(req.params.id) можно подделать.", misconception: "id приводится к числу и идёт через ORM как параметр, поэтому injection нет. Недостаёт проверки владения на строку." },
    { label: "Cryptographic Failures — ответ должен шифроваться.", misconception: "Транспорт уже шифруется TLS; шифрование тела ничего не меняет, когда авторизованная сессия читает не свою строку. Фикс — авторизация." },
  ]}
/>

## Сниппет 2 — фетчер URL

```js
// POST /api/preview  { "url": "..." }
app.post("/api/preview", async (req, res) => {
  const r = await fetch(req.body.url);     // fetch whatever the client sent
  const html = await r.text();
  res.send(extractOpenGraph(html));        // server returns the fetched content
});
```

<Quiz
  id="01-owasp-modern-code-2"
  lessonSlug="01-owasp-modern"
  lang="ru"
  question="Какой это класс бага, какой эксплойт с наибольшим импактом и какая правильная защита?"
  choices={[
    { label: "SSRF (A10). Атакующий шлёт url=http://169.254.169.254/latest/meta-data/, чтобы прочитать учётки cloud IMDS, или бьёт по внутренним сервисам. Защита: allowlist схемы/хоста, резолв IP и отклонение приватных/loopback/link-local диапазонов (с перепроверкой после редиректов), блокировка IMDS (IMDSv2).", correct: true },
    { label: "Injection — санитизировать строку URL перед фетчем.", misconception: "Санитизация строки не останавливает идеально валидный URL, указывающий на внутренний адрес. SSRF — про то, куда серверу разрешено подключаться, защищается allowlist'ингом и проверками IP-диапазонов, а не экранированием." },
    { label: "Всё нормально — fetch достаёт только публичные веб-страницы.", misconception: "fetch охотно запросит внутренние хосты, link-local metadata-эндпоинты и другие схемы. Именно эту достижимость эксплуатирует SSRF." },
    { label: "Broken Access Control — потребовать логин на эндпоинте.", misconception: "Логин ограничивает, кто это триггерит, но аутентифицированный сервер всё равно делает опасный внутренний запрос. Корневая причина — неограниченное назначение серверного фетча, то есть SSRF." },
  ]}
/>

## Сниппет 3 — конфиг CORS

```js
app.use(cors({
  origin: req.headers.origin,   // reflect whatever Origin the request sent
  credentials: true,            // allow cookies to be sent cross-origin
}));
```

<Quiz
  id="01-owasp-modern-code-3"
  lessonSlug="01-owasp-modern"
  lang="ru"
  question="Отражение Origin запроса при разрешённых credentials — что это на самом деле допускает и как это чинить?"
  choices={[
    { label: "Security Misconfiguration. Отражение любого Origin с credentials:true фактически позволяет любому сайту делать аутентифицированные cross-origin запросы и читать ответы с куками жертвы. Фикс: allowlist фиксированного набора доверенных origin и отражать только их (или ни одного для публичных API).", correct: true },
    { label: "Это верно — отражение Origin и есть то, как CORS должен работать.", misconception: "Отражение Origin вызывающего с credentials — каноническая misconfiguration CORS; это эквивалент wildcard, который вдобавок утекает куки. CORS ожидает явный allowlist доверенных origin." },
    { label: "Cryptographic Failures — выставить Secure на куке вместо этого.", misconception: "Secure управляет передачей только по HTTPS, а не тем, какие origin вправе читать cross-origin ответы. Дефект — политика CORS с отражённым origin и credentials." },
    { label: "Broken Access Control — добавить серверные проверки авторизации.", misconception: "Серверная авторизация всё ещё работает, но браузер отдаст ответ вредоносному origin, потому что CORS сказал, что origin доверенный. Фикс — корректный allowlist CORS." },
  ]}
/>

## Сниппет 4 — строка лога

```
2026-05-29T11:42:08Z WARN auth login failed user=alice@corp.com
  pw=hunter2 ip=203.0.113.9 ua="Mozilla/5.0" attempt=5 within=30s
```

<Quiz
  id="01-owasp-modern-code-4"
  lessonSlug="01-owasp-modern"
  lang="ru"
  question="В этой одной строке лога видны две находки. Какая пара верна?"
  choices={[
    { label: "Cryptographic/sensitive-data failure (пароль в открытом виде pw=hunter2 никогда нельзя логировать) плюс сигнал brute-force (attempt=5 within=30s), на который A07 Identification & Authentication Failures и A09 logging велят ставить rate-limit и алерт, а не просто записывать.", correct: true },
    { label: "Только проблема форматирования — лог просто многословен.", misconception: "Логирование пароля в открытом виде — реальная утечка чувствительных данных в хранилища логов и SIEM, а быстрые повторные неудачи — несмягчённый сигнал credential-stuffing. Оба — security-находки, а не стиль." },
    { label: "SSRF — поле ip показывает внешний запрос.", misconception: "Поле ip в auth-логе — обычные метаданные запроса, а не серверный фетч. SSRF здесь ни при чём." },
    { label: "Injection — строка user-agent может содержать payload.", misconception: "UA логируется, а не исполняется против интерпретатора. Реальные проблемы — залогированный пароль в открытом виде и неограниченные повторные auth-неудачи." },
  ]}
/>

<Recap lang="ru">
Каждая находка читается обратно к категории и фиксу корневой причины: аутентифицированный хендлер без проверки владения — это IDOR/Broken Access Control (ограничь по owner_id); сервер, фетчащий присланный клиентом URL, — это SSRF (allowlist плюс отклонение приватных диапазонов плюс блокировка IMDS); отражение Origin запроса с credentials — это Security Misconfiguration CORS (allowlist доверенных origin); а строка лога с паролем в открытом виде и быстрыми повторными неудачами — утечка чувствительных данных плюс пробел в auth/logging. Читай код и конфиг как ревьюер — назови причину, потом чини причину.
</Recap>
