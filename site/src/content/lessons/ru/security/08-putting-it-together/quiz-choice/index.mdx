---
concepts:
- broken-access-control
- oauth-token-validation
- jwt-storage-xss
- csrf-samesite
- password-kdf
- supply-chain-install
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://owasp.org/Top10/2021/A01_2021-Broken_Access_Control/
- https://www.rfc-editor.org/rfc/rfc8725.html
- https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html
status: ready
summary: "Тест с выбором на синтез по всему треку security — контроль доступа, OAuth/OIDC, валидация JWT, CSRF, хеширование паролей, секреты и supply chain, каждый как решение в реальной модели угроз."
title: 'Security capstone: тест с выбором ответа'
track: security
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего трека security. Ни один не про заучивание определения — каждый это решение в реальной модели угроз безопасного веб-приложения, где брешь почти всегда живёт в шве между двумя контролями, каждый из которых по отдельности выглядел правильным.
</Hook>

<Goal lang="ru">
Убедись, что можешь собрать весь трек — контроль доступа, OAuth/OIDC, валидацию JWT, защиту от CSRF, хеширование паролей, секреты и supply chain — в одну модель угроз и сказать, какой слой реально останавливает данную атаку.
</Goal>

<Quiz
  id="08-putting-it-together-choice-1"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question={"Залогиненный пользователь шлёт GET /accounts/{id}/statements с валидным, корректно подписанным JWT, но с чужим id — и читает чужие данные. Какой слой отказал и что чинит?"}
  choices={[
    { label: "Авторизация. Аутентификация подтвердила, кто вызывающий; ничего не проверило, владеет ли этот субъект названным объектом. Фикс: проверяй owns(subject, object) на сервере, deny-by-default, на каждом запросе — OWASP A01.", correct: true },
    { label: "Аутентификация — усиль JWT более стойким алгоритмом и коротким сроком жизни.", misconception: "Токен настоящий и вызывающий реально залогинен; атака использует реальную сессию, а не подделку. Усиление AuthN ничего не даёт при отсутствующей проверке AuthZ. AuthN — это не AuthZ." },
    { label: "Транспорт — запрос не был отправлен по TLS с HSTS.", misconception: "TLS защищает байты на проводе; у него нет мнения о том, может ли залогиненный вызывающий читать запись, которой не владеет. Это пробел в объектной авторизации, а не в транспорте." },
    { label: "Хранение токена — JWT должен был быть в HttpOnly-куке, а не в localStorage.", misconception: "Хранение важно против кражи через XSS, но здесь атакующий использует свой собственный легитимный токен. Никакой выбор хранения не закрывает отсутствующую проверку владения." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-2"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Твой SPA получает OIDC ID token и access token. Джун подключает API так, что авторизует вызовы, читая claims из ID token. Почему это неправильно?"
  choices={[
    { label: "ID token доказывает, кто вошёл (он для клиента); access token авторизует вызовы API (его audience — resource server). Авторизация по ID token означает, что API доверяет токену, выпущенному не для него — вместо этого валидируй у access token подпись, iss, aud и exp.", correct: true },
    { label: "Ничего страшного — оба JWT, значит взаимозаменяемы.", misconception: "Формат не равен назначению. У двух токенов разные audience и задачи; resource server обязан проверять aud и отклонять токен, выпущенный для клиента, а не для API." },
    { label: "ID token не подписан, поэтому ему вообще нельзя доверять.", misconception: "OIDC ID token всегда подписанный JWT. Проблема не в отсутствии подписи — а в том, что его audience и назначение это клиент, а не авторизация API." },
    { label: "Access token никогда не истекает, поэтому проверять безопасно только ID token.", misconception: "Access token намеренно короткоживущий; это фича безопасности, а не повод авторизовать по ID token. API обязан валидировать access token, включая exp." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-3"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question={"Пентестер берёт валидный RS256 JWT, меняет header на alg:HS256, ставит role:admin и переподписывает его, используя твой публичный ключ из JWKS как HMAC-секрет — и сервер принимает. Корень причины?"}
  choices={[
    { label: "Algorithm confusion: сервер выбирает путь верификации из header-поля alg токена, поэтому HMAC-верифицирует публичным ключом (который публичен). Фикс: закрепи allowlist алгоритмов — algorithms равно ['RS256'] — и никогда не доверяй alg из header.", correct: true },
    { label: "Приватный RSA-ключ утёк, и атакующий подписал им.", misconception: "Приватный ключ не использовался. Атака злоупотребляет публичным ключом как HMAC-секретом именно потому, что сервер дал токену выбрать алгоритм. Ключ не утекал." },
    { label: "Срок жизни JWT был слишком длинным, что позволило переиспользовать токен.", misconception: "Срок жизни тут ни при чём — атакующий чеканит совершенно новый токен. Дефект в доверии к alg из header вместо закрепления ожидаемого." },
    { label: "Payload не был зашифрован, поэтому атакующий смог прочитать role.", misconception: "Payload JWT подписан, а не секретен — его может прочитать любой. Проблема не в конфиденциальности, а в сломанной проверке целостности (alg, выбираемый из header)." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-4"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Команда переносит сессионную аутентификацию с JWT в localStorage в HttpOnly-куку, чтобы остановить кражу через XSS. Какую новую брешь это открывает и как её закрыть?"
  choices={[
    { label: "CSRF: куки — это ambient authority, которую браузер автоматически прикрепляет cross-site. Закрой её через SameSite (Lax/Strict) плюс CSRF-токен или проверку origin, и убедись, что ни одно изменение состояния не идёт через GET. Переход на HttpOnly верный; он просто сдвигает шов.", correct: true },
    { label: "Ничего — HttpOnly-куки иммунны к любой веб-атаке.", misconception: "HttpOnly блокирует чтение из JS (кражу через XSS), но не автоотправку куки браузером cross-site. Это ровно та поверхность CSRF, которой у токена в localStorage не было." },
    { label: "Это снова открывает SQL-инъекцию в таблице сессий.", misconception: "Хранение куки никак не связано с параметризацией запросов. Новая релевантная поверхность — CSRF, который про ambient-авторитет куки, а не про инъекции." },
    { label: "Это ослабляет хеш пароля, поэтому нужно перехешировать все пароли.", misconception: "Транспорт токена не влияет на KDF, хранящий пароли. Внесённая брешь — CSRF, решается через SameSite плюс токен, а не перехешированием." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-5"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Утечка сдампила твою таблицу users. Какой выбор хранения означает «они получат пару тривиально слабых паролей и сдадутся», а не «всё распространённое взломано к понедельнику»?"
  choices={[
    { label: "Argon2id (или bcrypt cost 12+) с уникальной солью на пользователя, настроенный на несколько сотен мс на хеш. Медленный, memory-hard, солёный KDF делает офлайн-перебор экономически невозможным в масштабе.", correct: true },
    { label: "SHA-256 с солью на пользователя.", misconception: "Соль побеждает rainbow-таблицы, но SHA-256 всё ещё быстрый — одна GPU делает ~22 миллиарда/с, поэтому словарные и распространённые пароли падают за часы. Скорость для паролей — это баг." },
    { label: "SHA-256 с серверным pepper в env.", misconception: "Pepper помогает, только пока остаётся секретным, а SHA-256 всё равно GPU-быстрый. Если pepper утечёт (а это секрет, которым надо управлять), ты снова на быстром переборе." },
    { label: "Зашифровать пароли AES ключом из конфига приложения.", misconception: "Шифрование обратимо, и ключ обычно утекает вместе с брешью (часто в том же репо/конфиге). Пароли надо хешировать медленным KDF, а не шифровать." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-6"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Твой код приложения безупречен, но CI компрометируют на npm install: пакет разрешился из публичного реестра с версией 9.9.9 поверх твоего внутреннего. Какой это класс и основная защита?"
  choices={[
    { label: "Dependency confusion — резолвер предпочёл публичный пакет с большей версией твоему приватному. Защита: scope/namespace для внутренних пакетов, закрепление одного доверенного реестра/precedence и lockfile с integrity-хешами, чтобы установки были воспроизводимы.", correct: true },
    { label: "Cross-site scripting в выводе сборки.", misconception: "XSS целит в DOM работающего приложения, а не в шаг установки. Это supply-chain атака на разрешении зависимостей, которую код приложения предотвратить не может." },
    { label: "Слабый пароль на аккаунте CI.", misconception: "Никакой пароль не подбирали; резолвер просто выбрал вредоносный публичный пакет по precedence версии. Фикс — scoping реестра и integrity lockfile, а не более стойкий пароль." },
    { label: "Это безвредно, ведь твой код никогда не импортирует этот пакет.", misconception: "Install-скрипты выполняются с твоими CI-кредами и сетью в момент разрешения пакета — выполнение кода происходит до любого импорта. Транзитивные и confused-зависимости — твоя поверхность атаки." },
  ]}
/>

<Recap lang="ru">
Сквозная линия всего трека — одна модель угроз: один чувствительный запрос проходит через транспорт, аутентификацию, авторизацию, обработку ввода, секреты и supply chain — и брешь живёт в шве, а не внутри одного слоя. AuthN доказывает «кто»; AuthZ решает, к чему можно прикоснуться (A01). OIDC ID token идентифицирует пользователя, а access token авторизует API, валидируемый по подписи, iss, aud, exp. JWT заслуживает доверия, только когда ты закрепляешь алгоритм. Перенос токена в HttpOnly-куку меняет кражу через XSS на CSRF, закрываемый SameSite плюс токеном. Паролям нужен медленный, солёный, memory-hard KDF. А шаг установки — твоя настоящая поверхность атаки: scope пакетов, закрепи реестр, требуй integrity lockfile.
</Recap>
