---
concepts:
- defense-in-depth
- authn-vs-authz
- token-composition
- offline-cracking
- secret-rotation
- supply-chain-trust
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://owasp.org/Top10/2021/A01_2021-Broken_Access_Control/
- https://www.rfc-editor.org/rfc/rfc8725.html
- https://cheatsheetseries.owasp.org/cheatsheets/Secrets_Management_Cheat_Sheet.html
status: ready
summary: "Промпты на свободное воспроизведение по всему треку security. Сначала ответь своими словами, затем открой модельный ответ и сверь с полной моделью угроз."
title: 'Security capstone: тест на свободное воспроизведение'
track: security
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. На каждый промпт реконструируй полный ответ по памяти, прежде чем открыть модельный — и заметь, что каждый заставляет связать два или более юнита трека, потому что именно там ломаются реальные системы.
</Hook>

<Goal lang="ru">
Реконструируй хребет трека, не подглядывая: defense in depth, AuthN против AuthZ, как композируются контроли токена, почему быстрые хеши проигрывают, почему ротация (а не удаление) завершает утечку секрета и почему твои зависимости — это твоя поверхность атаки.
</Goal>

<RetrievalDrawer
  client:visible
  id="08-putting-it-together-short-retrieval"
  lang="ru"
  questions={[
    { q: "Сформулируй центральный тезис всего трека security своими словами на одном конкретном межслойном примере.", a: "Ни один контроль не достаточен сам по себе: defense in depth предполагает, что каждый контроль рано или поздно откажет, поэтому ты складываешь независимые слои — транспорт, аутентификацию, авторизацию, обработку ввода, секреты, supply chain — где один ловит то, что упустил другой. Опасные отказы это не отсутствующий слой (их ловят на ревью), а два правильных слоя, которые не композируются. Канонический пример: аутентификация безупречно доказывает, что пользователь залогинен, а эндпоинт вроде GET /accounts/{id}/statements доверяет валидному токену как означающему, что вызывающий может читать любой названный id. Аутентификация этого никогда не утверждала — она ответила «кто», а не «к чему можно прикоснуться». Брешь живёт в шве авторизации, поэтому OWASP ставит Broken Access Control на A01. Ты моделируешь угрозы всего потока, а не каждого контроля по отдельности, и применяешь least privilege, чтобы при отказе слоя радиус поражения был мал." },
    { q: "Объясни разницу между OIDC ID token и OAuth access token и валидацию, которую API обязан выполнить, прежде чем доверять любому из них.", a: "ID token (слой OIDC) доказывает, кто вошёл — он для клиента, чтобы установить идентичность, несёт claims вроде sub/email/name и никогда не отправляется в API. Access token авторизует вызовы API — клиент шлёт его в Authorization: Bearer, и его audience — resource server. Авторизовать вызов API чтением claims из ID token неправильно, потому что API доверял бы токену, выпущенному не для него. Прежде чем доверять access token, resource server обязан проверить подпись против опубликованного ключа издателя, закрепить алгоритм (allowlist, никогда alg из header, чтобы блокировать alg:none и RS256→HS256 confusion) и проверить iss, aud (этот API) и exp. Даже тогда токен лишь устанавливает идентичность и scopes — эндпоинт всё равно поверх него проверяет объектную авторизацию (owns(subject, object))." },
    { q: "Пройди шов JWT-в-localStorage: почему идеально сформированный токен всё равно крадут и как выглядит композиция контролей.", a: "Стойкость токена — про то, можно ли его подделать; кража — про то, где он лежит. В localStorage любой успешный XSS выполняет localStorage.getItem('token') и эксфильтрует его — подпись и короткий срок жизни нерелевантны, как только JavaScript может прочитать значение. Перенос в HttpOnly-куку это блокирует, потому что JS не может тронуть куку, но куки — это ambient authority, которую браузер автоматически шлёт cross-site, что снова открывает CSRF. CSRF закрывают через SameSite (Lax/Strict) плюс CSRF-токен или проверку origin и гарантируют, что ни одно изменение состояния не идёт через GET (SameSite=Lax всё ещё шлёт куку на top-level GET-навигациях). Каждый фикс решает один слой и открывает следующий шов — в этом и суть: композируй HttpOnly + Secure + SameSite + короткий TTL и всё равно убивай XSS у источника через output encoding и CSP — ведь атакующий, выполняющий скрипт в твоём origin, уже прошёл мимо вопроса хранения." },
    { q: "Утечка сдампила таблицу users. Объясни, почему выбор хранения, сделанный годы назад, решает, насколько плох понедельник.", a: "Как только у атакующего есть столбец с хешами, он больше не ограничен rate-лимитом твоего логина — он перебирает офлайн, так быстро, как позволяет железо. Быстрый универсальный хеш вроде SHA-256 — не тот инструмент: одна потребительская GPU делает ~22 миллиарда попыток/с, поэтому против стандартных словарей любое словарное слово, имя-плюс-год и распространённый паттерн падают за часы, даже с солью (соль лишь побеждает rainbow-таблицы и заставляет работать по каждому аккаунту). Фикс — намеренно медленный, memory-hard, солёный на пользователя KDF — Argon2id (или bcrypt cost 12+), настроенный на несколько сотен мс на хеш — так один логин дёшев, но миллиард офлайн-попыток экономически невозможен. Шифрование неверно, потому что обратимо и ключ обычно утекает вместе с брешью. Решение живёт в одной строке кода регистрации, написанной задолго до утечки." },
    { q: "Почему ротация утёкшего секрета — единственный реальный фикс и как это связано с least privilege и радиусом поражения по всему треку?", a: "История git append-only, поэтому удаление строки или git-rm файла не убирает секрет — он остаётся в истории каждого клона и форка навсегда, читаемый любым, кто запустит git log -p или сканер, обходящий историю. Honeypot-ключи злоупотребляют за ~1 минуту, поэтому к моменту, когда ты заметишь, значение уже используется. Переписывание истории (git filter-repo) — это гигиена; ротация — отзыв креда у источника, чтобы копия атакующего стала бесполезной — это фикс. Это та же логика радиуса поражения, что и в остальном треке: vault-кред должен давать только те строки и операции, что нужны эндпоинту, access token должен быть короткоживущим, чтобы украденный истёк за минуты, а зависимость выполняется внутри твоей границы доверия. Least privilege повсюду решает, как далеко расходится ущерб, когда (а не если) слой откажет." },
    { q: "Твой код приложения безупречен. Объясни, почему тебя всё равно могут полностью скомпрометировать, и защиты, которые это адресуют.", a: "Современное приложение — тонкий слой твоей логики на сотнях в основном транзитивных зависимостей, написанных незнакомцами и обновляемых автоматически — поэтому поверхность атаки сместилась на шаг установки. Supply-chain атаки минуют твой код: в момент, когда CI забирает пакет и запускает его install-скрипты, они выполняются с твоими кредами, registry-токенами и сетью. Документированные формы: dependency confusion (резолвер предпочитает публичный пакет с большей версией твоему внутреннему — Birsan пробил Apple, Microsoft, Tesla), typosquatting (lodahs рядом с lodash), компрометация легитимных пакетов (event-stream, ua-parser-js) и долгая игра с захватом мейнтейнера (бэкдор xz-utils, где доверие к мейнтейнеру не было доверием к сборке). Защиты: scope/namespace внутренних пакетов и закрепление одного доверенного precedence реестра, lockfile с integrity-хешами для воспроизводимых установок, отслеживание SBOM и provenance (SLSA) и gating обновлений зависимостей вместо авто-мержа." },
  ]}
/>

<Recap lang="ru">
Если ты реконструировал каждый ответ по памяти — ты держишь хребет трека: defense in depth (эшелонированная защита) предполагает отказ каждого контроля, поэтому брешь живёт в шве — AuthN доказывает «кто», AuthZ решает, к чему можно прикоснуться (A01). Токены композируются: валидируй access token (подпись, закреплённый alg, iss/aud/exp), держи его подальше от localStorage и меняй кражу через XSS на CSRF, закрытый SameSite плюс токеном. Паролям нужен медленный, солёный, memory-hard KDF, потому что офлайн-перебор GPU-быстрый. Утёкший секрет завершается только ротацией, никогда удалением. А твои зависимости — настоящая поверхность атаки: scope, pin, lock и проверяй provenance. Теперь, когда моделируешь угрозы нового потока, ты проходишь стек целиком — не останавливаясь, когда каждый слой проходит свой тест, а спрашивая, через какой шов между двумя правильными контролями пройдёт атакующий.
</Recap>
