---
concepts:
- jwt-verification
- object-level-authz
- csrf-cookie-config
- password-kdf
- secret-in-config
- input-handling
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://www.rfc-editor.org/rfc/rfc8725.html
- https://owasp.org/Top10/2021/A01_2021-Broken_Access_Control/
- https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html
status: ready
summary: "Читай реальные сниппеты auth/веб-приложения поперёк трека — валидация JWT, объектная авторизация, конфиг куки/CSRF, хранение паролей — и выбирай фикс с наибольшим рычагом, который security-сеньор делает первым."
title: 'Security capstone: чтение кода и конфигов'
track: security
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги безопасности читаются в коде и конфигах, а не в лозунгах. Каждый сниппет ниже — небольшой кусок реального стека безопасного приложения. Прочитай, найди шов и выбери фикс, который сеньор делает раньше всего — тот же цикл, что и на ревью или в инциденте.
</Hook>

<Goal lang="ru">
Отработай цикл ревью поперёк всего трека: заметь сломанную проверку токена, отсутствующую проверку владения, куку, которая снова открывает CSRF, и секрет, превращающий конфиг в брешь — затем выбери фикс с наибольшим рычагом.
</Goal>

## Сниппет 1 — верификация токена

```js
// Express-мидлварь, защищающая API
function authenticate(req, res, next) {
  const token = req.headers.authorization?.split(" ")[1];
  const claims = jwt.decode(token);          // только декод
  if (claims?.sub) {
    req.user = claims;                        // доверяем payload
    return next();
  }
  return res.status(401).end();
}
```

<Quiz
  id="08-putting-it-together-code-1"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Эта мидлварь аутентифицирует каждый запрос. В чём критический изъян и фикс?"
  choices={[
    { label: "Она вызывает jwt.decode, который никогда не проверяет подпись — любой атакующий может base64-нуть payload с любым sub/role и быть аутентифицированным. Фикс: jwt.verify с закреплённым ключом и allowlist алгоритмов (например, algorithms равно ['RS256']), затем проверь iss/aud/exp.", correct: true },
    { label: "Она читает токен из неверного header; используй куку.", misconception: "Парсинг header в порядке. Фатальный баг — декод без верификации подписи; кука не починит поддельные неподписанные claims." },
    { label: "Нужно ещё ловить исключения из split().", misconception: "Optional chaining уже защищает от отсутствующего header. Помимо устойчивости, дефект безопасности — доверие декодированным claims без верификации подписи вообще." },
    { label: "Всё в порядке, пока токен пришёл по HTTPS.", misconception: "TLS защищает транзит; он ничего не делает с поддельным payload. Атакующий пишет свой токен — это останавливает только верификация подписи с закреплённым alg." },
  ]}
/>

## Сниппет 2 — эндпоинт под OAuth

```js
// access token уже верифицирован выше; req.user = { sub, scope }
app.get("/accounts/:id/statements", requireScope("statements:read"),
  async (req, res) => {
    const rows = await db.query(
      "SELECT * FROM statements WHERE account_id = $1", [req.params.id]
    );
    res.json(rows);
  });
```

<Quiz
  id="08-putting-it-together-code-2"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Токен валидирован и scope проверен. Пользователь с валидным токеном всё равно читает statements чужого аккаунта. Почему и в чём фикс?"
  choices={[
    { label: "Запрос параметризован (нет SQLi) и scope проверен, но ничто не проверяет, что вызывающий владеет :id — классический IDOR / Broken Access Control. Фикс: ограничь запрос субъектом, например WHERE account_id = $1 AND owner_sub = $2 с req.user.sub, deny-by-default.", correct: true },
    { label: "Это SQL-инъекция — :id конкатенируется в запрос.", misconception: "Запрос использует параметр ($1), инъекции нет. Изъян — отсутствующая объектная авторизация: валидный токен и scope не доказывают владение этим id." },
    { label: "Неверная строка scope; должно быть statements:write.", misconception: "Read scope корректен для GET. Scope — грубое разрешение, а не владение конкретным объектом. Баг — отсутствие проверки owns(subject, object) на конкретном аккаунте." },
    { label: "Ничего — requireScope уже авторизует запрос.", misconception: "Scope авторизует класс действия (можно читать statements), а не конкретную запись. Без проверки владения это IDOR, A01 у OWASP." },
  ]}
/>

## Сниппет 3 — сессионная кука

```js
res.cookie("session", token, {
  httpOnly: true,
  secure: true,
  // sameSite не задан
});

// в другом месте, «удобный» маршрут
app.get("/account/delete", deleteAccountHandler);  // изменение состояния на GET
```

<Quiz
  id="08-putting-it-together-code-3"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="HttpOnly и Secure заданы, поэтому XSS не прочитает куку. Какая экспозиция CSRF остаётся и как её закрыть?"
  choices={[
    { label: "Две проблемы складываются: sameSite не задан (старые/краевые случаи шлют его cross-site) и изменяющее состояние действие живёт на GET, который SameSite=Lax всё равно шлёт на top-level навигации. Фикс: задай SameSite=Lax/Strict, добавь CSRF-токен или проверку origin и сделай разрушительные действия POST/DELETE — никогда GET.", correct: true },
    { label: "Никакая — HttpOnly уже предотвращает CSRF.", misconception: "HttpOnly останавливает чтение из JS, а не автоприкрепление куки браузером cross-site. Изменяющий состояние GET — учебная цель CSRF; HttpOnly к нему нерелевантен." },
    { label: "Токен надо зашифровать перед установкой как куку.", misconception: "Шифрование значения куки не мешает браузеру слать её на поддельный cross-site запрос. Фикс — SameSite плюс CSRF-токен и перенос действия с GET." },
    { label: "Установи secure:false, чтобы кука работала в разработке.", misconception: "Сброс Secure — регрессия, разрешающая куку по plaintext. Это ничего не даёт против CSRF и ослабляет транспортную безопасность." },
  ]}
/>

## Сниппет 4 — конфиг и регистрация

```js
// config.js (закоммичен в репо)
export const config = {
  dbPassword: "S3cr3t-prod-pw!",
  jwtSecret: "supersecret",
};

// регистрация
const hash = crypto.createHash("sha256").update(password).digest("hex");
await db.users.insert({ email, hash });
```

<Quiz
  id="08-putting-it-together-code-4"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="В этих нескольких строках сидят два отказа поперёк трека. Назови оба и фикс для каждого."
  choices={[
    { label: "(1) Живые секреты закоммичены — история git хранит их после любого удаления, поэтому ротируй пароль БД и JWT-секрет и загружай их из vault/env, никогда из репо. (2) Пароли хранятся быстрым несолёным SHA-256 — замени на Argon2id (или bcrypt 12+) с солью на пользователя.", correct: true },
    { label: "Неверно только хеширование; закоммиченные секреты в порядке, ведь репо приватное.", misconception: "Приватное репо всё равно утекает через клоны, CI-логи и случайную публикацию, а история хранит значение после удаления. Закоммиченный секрет уже скомпрометирован — ротируй его." },
    { label: "Неверны только секреты; SHA-256 в порядке, ведь это криптографический хеш.", misconception: "SHA-256 криптографичен, но быстр (~22 миллиарда/с на GPU) и здесь несолён — ровно неправильно для паролей. Нужен медленный, солёный, memory-hard KDF." },
    { label: "Добавь соль к SHA-256, и хеширование станет безопасным.", misconception: "Соль побеждает rainbow-таблицы, но оставляет GPU-скорость SHA-256, поэтому распространённые пароли всё равно быстро ломаются офлайн. Фикс — намеренно медленный KDF, а не просто соль." },
  ]}
/>

<Recap lang="ru">
Каждый сниппет был швом между двумя юнитами трека. jwt.decode ничего не аутентифицирует — верифицируй подпись и закрепляй алгоритм. Валидный токен и scope — это не владение; добавь объектную проверку owns(subject, object), чтобы убить IDOR. HttpOnly останавливает кражу через XSS, но не CSRF — задай SameSite, добавь токен и держи изменения состояния подальше от GET. А закоммиченный секрет уже взломан (ротируй, не удаляй), тогда как быстрый несолёный хеш отдаёт столбец паролей (используй Argon2id с солью на пользователя). Читай код, находи шов между правильными с виду слоями и чини структурный пробел.
</Recap>
