---
concepts:
- defense-in-depth
- broken-access-control
- threat-modeling
- least-privilege
- token-storage
- control-composition
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://owasp.org/Top10/2021/A01_2021-Broken_Access_Control/
- https://devguide.owasp.org/en/02-foundations/03-security-principles/
- https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html
- https://pages.nist.gov/800-63-4/sp800-63b.html
spiral: []
status: ready
summary: 'Ни один контроль безопасности не достаточен сам по себе. Каждый слой — TLS,
  аутентификация, авторизация, ввод, секреты, зависимости — может быть корректным
  по отдельности, а система всё равно падает: брешь живёт в шве, где встречаются два
  корректных контроля.'
title: 'Эшелонированная защита: брешь живёт в шве'
track: security
unit: 08-putting-it-together
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
Флоу входа — образцовый. Пароли хешируются Argon2id, сессия — короткоживущий JWT, TLS принуждается через HSTS, API-ключ лежит в вольте. А потом пользователь меняет `id` в `GET /accounts/{id}/statements` и читает чужие банковские выписки. Ни один контроль не сломан. Аутентификация проверила, *кто* вызывает, — безупречно — а эндпоинт выписок просто никогда не спросил, *имеет ли этот вызывающий право читать эту запись*. Каждый слой прошёл свой собственный тест. Брешь жила в зазоре между ними.
</Hook>

<Crux>Ни один контроль не достаточен: слои по отдельности выглядят верно, но брешь живёт в шве, где встречаются два корректных контроля.</Crux>

<Explanation>

## Один запрос, шесть слоёв

Проследи одно чувствительное действие — залогиненный пользователь переводит деньги — и ты пройдёшь через все контроли, что покрыл трек безопасности. Каждый — отдельный слой со своей задачей, и сеньор рассуждает о них как о *стеке*, а не как о чек-листе:

1. **Транспорт** — TLS плюс HSTS, чтобы запрос нельзя было прочитать или даунгрейднуть до открытого текста на проводе.
2. **Аутентификация** — доказать, *кто* вызывает. Короткоживущий access-токен (минуты, не дни), пароли хранятся как хеши Argon2id или bcrypt, никогда сам пароль.
3. **Авторизация** — решить, *имеет ли этот вызывающий право делать это с этим объектом*. Запрет-по-умолчанию, наименьшие привилегии. Это риск №1 у OWASP не просто так.
4. **Обработка ввода** — относись к каждому полю как к враждебному: валидируй, параметризуй запросы, кодируй вывод.
5. **Секреты** — ключи и креды БД живут в вольте или env, никогда в репозитории, никогда в JWT.
6. **Цепочка поставок** — зависимости, исполняющие всё вышеперечисленное, сами проверены и запинены.

Тезис всего трека приземляется здесь: **эшелонированная защита предполагает, что каждый контроль рано или поздно откажет.** Ты складываешь их так, чтобы, когда первый барьер падёт, второй замедлил атакующего, а третий поднял тревогу. Ошибка джунов — считать один сильный контроль ответом.

## Брешь живёт в шве

Опасные провалы — это не отсутствующий слой, такие ловят на ревью. Это два корректных слоя, которые не композируются. Хук — каноничный пример: аутентификация ответила «да, это валидный залогиненный пользователь», а эндпоинт доверил этому ответу значение «...значит, пусть читает запись», чего аутентификация никогда не утверждала. **AuthN — это не AuthZ.** Знание, кто человек, не говорит ничего о том, к чему он может прикасаться.

OWASP ставит Broken Access Control на A01 — самый частый серьёзный изъян — именно потому, что он живёт в этих швах. Фикс структурный, а не заплатка на один роут: принуждай авторизацию в доверенном серверном коде, запрещай по умолчанию для каждого непубличного ресурса и проверяй *владение* записью на объекте, который называет запрос, а не только на роуте. Атакующий, переключающий `{id}` в `GET /accounts/{id}/statements`, делает IDOR; останавливает это только сервер, спрашивающий «владеет ли *этот* субъект *этим* объектом?» на каждом запросе.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Слой</th>
        <th class="border border-border px-3 py-2 text-left">По отдельности выглядит верно</th>
        <th class="border border-border px-3 py-2 text-left">Шов, который всё равно его ломает</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Аутентификация (JWT)</td>
        <td class="border border-border px-3 py-2">Подписан, короткоживущий, проверен</td>
        <td class="border border-border px-3 py-2">Эндпоинт не проверяет <code>owns(subject, object)</code> → IDOR</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Хеширование пароля</td>
        <td class="border border-border px-3 py-2">Argon2id, с солью, ~250мс</td>
        <td class="border border-border px-3 py-2">У эндпоинта сброса нет рейт-лимита → захват аккаунта</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Хранение токена</td>
        <td class="border border-border px-3 py-2">Сильный, валидный JWT</td>
        <td class="border border-border px-3 py-2">Лежит в <code>localStorage</code> → один XSS читает всё</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Защита от CSRF</td>
        <td class="border border-border px-3 py-2">Кука <code>SameSite=Lax</code></td>
        <td class="border border-border px-3 py-2">Меняющий состояние роут на <code>GET</code> обходит её полностью</td>
      </tr>
    </tbody>
  </table>
</div>

## Шов JWT-в-localStorage

Решение о хранении токена — чистая проблема композиции, поэтому заслуживает близкого взгляда. Безупречно выпущенный JWT — подписанный, короткий срок, верные claims — побеждается тем, *куда ты его кладёшь*. Положи его в `localStorage`, и любой успешный XSS выполняет `localStorage.getItem('token')` и эксфильтрует его на сервер атакующего; собственная сила токена нерелевантна, как только JavaScript может его прочитать. Митигация — `HttpOnly`-кука, которую JavaScript не может тронуть, поэтому XSS её не украдёт — но куки шлются автоматически кросс-сайтово, что вновь открывает CSRF. *Это* ты закрываешь через `SameSite` плюс CSRF-токен. Заметь форму: каждый фикс решает один слой и обнажает шов со следующим. Нет единой настройки, которая побеждает; ты композируешь `HttpOnly` + `Secure` + `SameSite` + короткий TTL, и всё равно обязан убить XSS у источника кодированием вывода и CSP, потому что атакующий, способный исполнить скрипт в твоём origin, уже прошёл мимо вопроса хранения.

<Inset kind="why" lang="ru">
«Мы используем JWT, значит, мы stateless и в безопасности» смешивает две несвязанные вещи. JWT доказывает аутентификацию и может нести claims; он ничего не говорит об авторизации на конкретном объекте и ничего не делает для защиты собственного хранения. Statelessness — это выбор архитектуры. Устойчивость к IDOR и XSS — отдельные контроли, которые ты всё равно обязан построить: формат токена не даёт ни того, ни другого.
</Inset>

## Наименьшие привилегии везде, не только у двери

Эшелонированная защита — ещё и про радиус взрыва. Когда слой всё же откажет — а модель предполагает, что один откажет, — наименьшие привилегии решают, как далеко расползётся ущерб. Кред из вольта, что использует API, должен давать только те строки и операции, что нужны эндпоинту, чтобы скомпрометированный сервис не осушил всю базу. Access-токен должен быть короткоживущим, чтобы украденный истёк за минуты, а не недели. Зависимость, которую ты подтянул, должна работать под тем же надзором, потому что вредоносный транзитивный пакет исполняется внутри твоей границы доверия. NIST SP 800-63B и руководство OWASP по паролям задают пол для слоя аутентификации — Argon2id, настроенный примерно на 250–500мс на хеш, или bcrypt с cost factor 12+, с уникальной солью на пользователя, — но сильный хеш за эндпоинтом сброса без рейт-лимита всё равно падает. Число на одном слое никогда не оправдывает зазор в следующем.

<TradeoffMatrix
  id="08-security-overview-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Залогиненный пользователь вызывает `GET /accounts/{id}/statements` с валидным подписанным JWT, но с id, который не его. Что на самом деле останавливает брешь?"
  options={[
    { name: "Серверная авторизация, проверяющая, что субъект владеет названным объектом на каждом запросе (запрет-по-умолчанию)", summary: "Проверяй владение, а не только личность.", correct: true, justification: "Это шов из хука. Аутентификация уже подтвердила валидного пользователя; недостающий контроль — авторизация на конкретном объекте: владеет ли этот субъект account {id}? Принуждение owns(subject, object) в доверенном серверном коде, с запретом по умолчанию, — единственное, что останавливает IDOR. OWASP ставит Broken Access Control на A01 именно потому, что команды проверяют личность и забывают авторизацию на уровне объекта." },
    { name: "Более сильный алгоритм подписи и более короткий срок жизни JWT", summary: "Усилить токен.", justification: "Токен уже валиден, и вызывающий по-настоящему залогинен — атака использует реальную сессию, не подделанную. Усиление аутентификации ничего не делает для зазора авторизации. Это классическая ошибка — относиться к AuthN так, будто это AuthZ." },
    { name: "Хеширование пароля Argon2id и TLS с HSTS", summary: "Запереть нижние слои.", justification: "Оба корректны и необходимы, но сидят на разных слоях. Секретность транспорта и хранение пароля не решают, может ли залогиненный вызывающий читать запись, которой не владеет. Прохождение каждым слоем своего теста — ровно то, как случается брешь в шве." },
    { name: "Перенести токен из localStorage в HttpOnly-куку", summary: "Починить хранение.", justification: "Хорошая гигиена против кражи через XSS, но здесь нерелевантна: атакующий использует свой собственный легитимный токен, не украденный. Изъян — отсутствие авторизации на уровне объекта, чего не адресует никакой выбор хранения." },
  ]}
/>

<Quiz
  id="08-security-overview-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Сильный подписанный JWT хранится в localStorage. Каким реалистичным способом его крадут?"
  choices={[
    { label: "XSS-пейлоад выполняет localStorage.getItem('token') и отправляет его на сервер атакующего — JavaScript может читать localStorage", correct: true },
    { label: "Атакующий брутфорсит подпись JWT по сети", misconception: "Корректно подписанный JWT с сильным секретом на практике не брутфорсят; реалистичный путь кражи — прочитать его из хранилища внедрённым скриптом, а не сломать криптографию." },
    { label: "Перехват TLS на проводе", misconception: "TLS плюс HSTS защищают токен в транзите; экспозиция в localStorage — в браузере, где релевантный контроль — HttpOnly-куки, а не TLS." },
    { label: "CSRF заставляет браузер отправить токен", misconception: "CSRF едет на куках, шлющихся автоматически; токен в localStorage прикрепляет твой собственный JS, поэтому вектор не CSRF — а XSS, читающий значение." },
  ]}
/>

<Quiz
  id="08-security-overview-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Что фундаментально предполагает «эшелонированная защита» о твоих контролях?"
  choices={[
    { label: "Каждый контроль рано или поздно откажет, поэтому ты складываешь независимые слои, где один ловит то, что упустил другой", correct: true },
    { label: "Один достаточно сильный контроль (вроде идеального JWT) делает остальные необязательными", misconception: "Это заблуждение единственного контроля. Сильный токен не даёт авторизации, валидации ввода или безопасности хранения — глубина существует, потому что ни один слой не покрывает остальные." },
    { label: "Если каждый слой проходит свой тест, система безопасна", misconception: "Прохождение каждым слоем теста по отдельности — ровно то условие, при котором случается брешь в шве: зазор между корректными слоями, а не внутри одного из них." },
    { label: "Больше слоёв всегда замедляют приложение настолько, что это того не стоит", misconception: "Слои — про радиус взрыва и избыточность, а не про голую цену; наименьшие привилегии и короткие сроки токенов дёшевы и ограничивают ущерб, когда слой падает." },
  ]}
/>

<DragOrder
  id="08-security-overview-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь слои, через которые проходит один чувствительный запрос, от внешнего к внутреннему:"
  items={[
    { id: "a", label: "Транспорт: TLS + HSTS, чтобы запрос нельзя было прочитать или даунгрейднуть" },
    { id: "b", label: "Аутентификация: доказать, кто вызывает (короткоживущий токен, хешированный пароль)" },
    { id: "c", label: "Авторизация: запрет-по-умолчанию, проверить, что этот субъект владеет этим объектом" },
    { id: "d", label: "Обработка ввода: валидировать, параметризовать, кодировать каждое поле" },
    { id: "e", label: "Секреты + зависимости: ключи в вольте, пакеты проверены и запинены" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Безопасность слоиста: TLS, аутентификация, авторизация, ввод, секреты, зависимости. Каждый корректен сам, но система падает в шве между двумя — AuthN это не AuthZ. Считай, что каждый откажет; складывай их с запретом-по-умолчанию.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="08-security-overview-retrieval"
  lang="ru"
  questions={[
    { q: "Коллега говорит, что флоу входа безопасен, потому что пароли хешируются Argon2id и JWT сильный. Объясни, почему этого недостаточно и где прячется настоящая брешь.", a: "Хеширование Argon2id и сильный JWT — это два слоя, аутентификация и хранение учётных данных, и оба могут быть безупречны, пока система всё равно падает. Аутентификация отвечает только на 'кто вызывает', а JWT несёт личность, не разрешение на конкретном объекте. Брешь прячется в шве авторизации: эндпоинт вроде GET /accounts/{id}/statements, который доверяет валидному токену значение, что вызывающий может читать любой id, который запросит. Это IDOR, и OWASP ставит Broken Access Control на A01, потому что команды корректно проверяют личность, а потом забывают проверки на уровне объекта. Фикс структурный — принуждай owns(subject, object) в доверенном серверном коде, запрещай по умолчанию для непубличных ресурсов и никогда не позволяй пройденному нижнему слою оправдывать отсутствующий верхний. Эшелонированная защита предполагает, что каждый контроль рано или поздно откажет, поэтому сильный хеш за эндпоинтом сброса без рейт-лимита или сильный токен, прочитанный из localStorage через XSS, всё равно проигрывают." },
    { q: "Пройди по тому, почему безупречно валидный JWT всё равно крадут, и как выглядит композиция контролей на практике.", a: "Сила токена — про то, можно ли его подделать, а кража — про то, где он живёт. В localStorage любой успешный XSS выполняет localStorage.getItem('token') и эксфильтрует его — подпись и короткий срок нерелевантны, как только JavaScript может прочитать значение. Перенос в HttpOnly-куку это блокирует, потому что JS не может тронуть куку, но куки шлются автоматически кросс-сайтово, вновь открывая CSRF; ты закрываешь это через SameSite плюс CSRF-токен. Каждый фикс решает один слой и обнажает шов со следующим, и в этом весь смысл: нет единой выигрышной настройки. На практике ты композируешь HttpOnly + Secure + SameSite + короткий TTL и всё равно устраняешь XSS у источника кодированием вывода и Content Security Policy, потому что атакующий, способный исполнить скрипт в твоём origin, уже прошёл мимо вопроса хранения. Это и есть эшелонированная защита — независимые контроли, чтобы, когда один откажет, следующий всё ещё ограничивал радиус взрыва." },
  ]}
/>

<Recap lang="ru">
Один чувствительный запрос проходит через слои — транспорт, аутентификация, авторизация, обработка ввода, секреты, цепочка поставок — и трек безопасности покрыл каждый. Урок их сборки воедино в том, что ни один контроль не достаточен, а опасные провалы — это не отсутствующие слои, а два корректных слоя, которые не композируются. Аутентификация доказывает, кто вызывает, и ничего не говорит о том, может ли он трогать конкретный объект, поэтому Broken Access Control — риск №1 у OWASP: брешь живёт в шве, эндпоинт доверяет валидному токену значение владения. Сильный хеш Argon2id падает за эндпоинтом сброса без рейт-лимита; сильный JWT эксфильтруется из localStorage одним XSS. Эшелонированная защита предполагает, что каждый контроль рано или поздно откажет, поэтому ты складываешь независимые слои, запрещаешь по умолчанию, применяешь наименьшие привилегии к кредам и срокам жизни токенов и принуждаешь авторизацию на объекте — не только на личности — на каждом запросе. Моделируй угрозы всему флоу, а не каждому контролю по отдельности, потому что зазор между двумя корректными контролями — ровно там, где живут атакующие.
</Recap>
