---
concepts:
- alg-none
- algorithm-confusion
- token-revocation
- refresh-token-rotation
- token-storage-xss
- weak-hmac-secret
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://www.rfc-editor.org/rfc/rfc8725.html
- https://portswigger.net/web-security/jwt/algorithm-confusion
- https://auth0.com/blog/refresh-tokens-what-are-they-and-when-to-use-them/
- https://workos.com/blog/secure-jwt-storage
spiral: []
status: ready
summary: JWT настолько безопасен, насколько правильно ты его проверяешь. Классические
  провалы — приём alg:none, путаница RS256→HS256, отсутствие отзыва и токены в localStorage
  — растут из доверия токену вместо того, чтобы зафиксировать ожидаемое сервером.
title: 'Ловушки безопасности JWT: путаница alg, отсутствие отзыва и где утекают токены'
track: security
unit: 03-jwt-pitfalls
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Пентестер логинится как обычный пользователь, копирует JWT, меняет в заголовке `"alg":"RS256"` на `"alg":"HS256"`, правит payload на `"role":"admin"` и переподписывает токен, используя **публичный** RSA-ключ сервера как HMAC-секрет. Сервер читает заголовок, уходит в HMAC-ветку, проверяет тем же публичным ключом — и пропускает. Приватный ключ не понадобился. «Секрет» всё это время лежал в открытом эндпоинте JWKS.
</Hook>

<Crux>JWT можно доверять, только если сервер фиксирует ожидаемые алгоритм и ключ — а не те, что заявляет подконтрольный атакующему заголовок.</Crux>

<Explanation>

## Токен не значит ничего, пока ты его не проверил

JWT — это три base64url-части: заголовок, payload и подпись. Заголовок несёт `alg` (какой алгоритм), payload несёт claims (`sub`, `role`, `exp`), а подпись — то, что делает первые две части неподделываемыми, *если* ты проверяешь её правильно. Опасная ментальная модель — считать декодированный payload фактами. Это не факты. Любой может base64-декодировать и переписать payload в консоли браузера. Между атакующим и `"role":"admin"` стоит только проверка подписи, и каждая ловушка ниже — это способ, которым проверка тихо ничего не делает.

<DiagramFrame
  label="JWT — это три base64url-сегмента, соединённые точками: заголовок точка payload точка подпись. Заголовок называет алгоритм, payload хранит claims, а подпись вычисляется над заголовком и payload вместе — поэтому правка любой из первых двух частей делает подпись невалидной, если проверку не пропустили."
  hue="var(--d-crypto)"
  caption="Подпись вычисляется над заголовком + payload (первые два сегмента). Правка payload на role:admin ломает подпись — атака работает только когда сервер не проверяет её правильно."
>
  <svg viewBox="0 0 460 170" width="460" height="170" role="presentation" style="display:block;margin:0 auto;max-width:100%;height:auto;font-family:var(--font-body);">
    {/* header segment */}
    <rect x="8" y="28" width="120" height="44" rx="7" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="68" y="20" text-anchor="middle" font-size="11" fill="var(--muted)">Заголовок</text>
    <text x="68" y="50" text-anchor="middle" font-size="11.5" font-family="var(--font-mono)" fill="var(--ink)">{`{"alg",`}</text>
    <text x="68" y="64" text-anchor="middle" font-size="11.5" font-family="var(--font-mono)" fill="var(--ink)">{`"typ"}`}</text>
    {/* dot 1 */}
    <text x="142" y="58" text-anchor="middle" font-size="22" font-family="var(--font-mono)" fill="var(--ink)">.</text>
    {/* payload segment */}
    <rect x="156" y="28" width="120" height="44" rx="7" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="216" y="20" text-anchor="middle" font-size="11" fill="var(--muted)">Payload</text>
    <text x="216" y="50" text-anchor="middle" font-size="11.5" font-family="var(--font-mono)" fill="var(--ink)">{`{"sub",`}</text>
    <text x="216" y="64" text-anchor="middle" font-size="11.5" font-family="var(--font-mono)" fill="var(--ink)">{`"role"}`}</text>
    {/* dot 2 */}
    <text x="290" y="58" text-anchor="middle" font-size="22" font-family="var(--font-mono)" fill="var(--ink)">.</text>
    {/* signature segment */}
    <rect x="304" y="28" width="148" height="44" rx="7" fill="var(--card-2)" stroke="var(--d)" stroke-width="1.4" />
    <text x="378" y="20" text-anchor="middle" font-size="11" fill="var(--muted)">Подпись</text>
    <text x="378" y="54" text-anchor="middle" font-size="11.5" font-family="var(--font-mono)" fill="var(--ink)">3pBd...x9</text>
    {/* brace under header + payload */}
    <path d="M8 92 L8 100 L138 100 L138 108 L144 100 L138 100 L138 92" fill="none" stroke="var(--muted)" stroke-width="1.2" />
    <path d="M150 100 L156 108 L156 92 M156 100 L276 100 L276 92" fill="none" stroke="var(--muted)" stroke-width="1.2" />
    {/* arrow from brace tip to signature */}
    <path d="M144 108 L300 140 L370 78" fill="none" stroke="var(--d)" stroke-width="1.4" stroke-dasharray="4 3" />
    <text x="200" y="134" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--d)">sign(header + payload, key)</text>
  </svg>
</DiagramFrame>

RFC 8725 (JSON Web Token Best Current Practices, 2020) существует именно потому, что гибкость спеки — позволять *токену* самому называть свой алгоритм — это корневая причина большинства атак на JWT. Его прямое правило: приложение должно решать, какие алгоритмы и ключи допустимы, и проверять токен против *этого*, а не против того, что просит заголовок.

## alg:none — пустая подпись, которую некоторые библиотеки принимали

Спека JWT определяет значение `"alg":"none"`, означающее «этот токен не подписан». Оно существует для токенов, уже защищённых другим слоем. Катастрофа: ранние версии библиотек, получив токен с `"alg":"none"` и пустым сегментом подписи, возвращали «валиден». Атакующий просто ставит в заголовок `none`, удаляет подпись, пишет любой нужный payload — и аутентифицирован как кто угодно.

Фикс — одна строка дисциплины: верификатору надо сказать ожидаемый алгоритм и отвергать всё остальное. `jwt.verify(token, key, { algorithms: ["RS256"] })` — allowlist без `none` делает атаку невозможной. Никогда не вызывай функцию только-декодирования (`jwt.decode`, которая пропускает подпись) и не действуй по её claims.

## Путаница алгоритмов RS256 → HS256

Это более тонкий и более злой родственник — тот, что в начале урока. Асимметричный RS256 использует *пару* ключей: приватный подписывает, публичный проверяет, и публичный предназначен для шаринга — он лежит в твоём `/.well-known/jwks.json`. Симметричный HS256 использует *один* секрет и для подписи, и для проверки.

Атака эксплуатирует сервер, который выбирает путь проверки из заголовка `alg`:

1. Атакующий берёт публичный ключ (он публичный).
2. Атакующий подделывает токен с `"alg":"HS256"` и подписывает его HMAC-SHA256, используя **строку публичного ключа как HMAC-секрет**.
3. Сервер читает `HS256`, уходит в HMAC-ветку кода и выполняет HMAC-SHA256 со своим публичным ключом в роли секрета — ровно ту операцию, что только что сделал атакующий.
4. Подписи совпадают. Поддельный admin-токен принят.

Всё это работает, потому что сервер доверился `alg` из заголовка и использовал один ключ с двумя алгоритмами. RFC 8725 называет обе защиты напрямую: **зафиксируй ожидаемый алгоритм** (явный allowlist, переданный в verify) и **привяжи каждый ключ ровно к одному алгоритму**, чтобы публичный RS256-ключ никогда не попал в HMAC-верификатор. Вместе эти два правила делают поддельный заголовок атакующего нерелевантным — алгоритм решает сервер, не токен; без обоих правил шаг 2 атаки — это бесплатный пропуск.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Ловушка</th>
        <th class="border border-border px-3 py-2 text-left">Что делает атакующий</th>
        <th class="border border-border px-3 py-2 text-left">Фикс</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Принимается <code>alg: none</code></td>
        <td class="border border-border px-3 py-2">Ставит alg в none, опустошает подпись, переписывает payload</td>
        <td class="border border-border px-3 py-2">Передай allowlist алгоритмов в verify; никогда не принимай <code>none</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Путаница RS256 → HS256</td>
        <td class="border border-border px-3 py-2">Подписывает HS256, используя публичный RSA-ключ как HMAC-секрет</td>
        <td class="border border-border px-3 py-2">Зафиксируй алгоритм; привяжи каждый ключ к одному алгоритму</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Нет отзыва</td>
        <td class="border border-border px-3 py-2">Реплеит украденный токен, пока он не истечёт</td>
        <td class="border border-border px-3 py-2">Короткий TTL access (5–15 мин) + ротация refresh-токена</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Токен в <code>localStorage</code></td>
        <td class="border border-border px-3 py-2">XSS читает токен через JavaScript и выкачивает его</td>
        <td class="border border-border px-3 py-2"><code>httpOnly</code>-cookie или in-memory + короткий TTL</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Слабый HMAC-секрет</td>
        <td class="border border-border px-3 py-2">Брутфорсит секрет офлайн из перехваченного токена</td>
        <td class="border border-border px-3 py-2">≥32 случайных байта (256 бит) из CSPRNG</td>
      </tr>
    </tbody>
  </table>
</div>

## Нет отзыва: украденный JWT валиден до истечения

Определяющее свойство stateless-JWT — сервер его не хранит: он просто проверяет подпись и доверяет claim `exp`. Обратная сторона — производственная головная боль: **встроенного logout нет**. Если токен украден (XSS, утёкший лог, прокси), отозвать его нельзя. Вызов «logout»-эндпоинта, который удаляет клиентскую cookie, никак не влияет на токен, который атакующий уже скопировал. Он остаётся валидным до `exp`.

Митигация сеньора — держать радиус поражения малым через время. Выдавай **короткоживущие access-токены** — стандартный диапазон 5–15 минут — в паре с долгоживущим **refresh-токеном**, который меняется на новые access-токены. Теперь украденный access-токен умирает за минуты. Refresh-токен — самый ценный секрет, поэтому он получает **ротацию**: при каждом использовании сервер выдаёт новый refresh-токен и инвалидирует старый. Если старый refresh-токен когда-нибудь зареплеят (атакующий использовал тот, мимо которого ты уже проротировал), сервер замечает повторное использование и отзывает всё семейство токенов — превращая украденный refresh-токен в самоуничтожающуюся ловушку.

<Inset kind="why" lang="ru">
«Stateless» — это спектр, а не религия. В момент, когда ты добавляешь ротацию refresh-токенов или список отзыва, ты вернул серверное состояние — и это нормально. Выигрыш JWT никогда не был «ноль серверного состояния навсегда»; он был в том, чтобы держать *высокочастотную* проверку (каждый API-запрос валидирует подпись локально) дешёвой, пока *редкие* события (логин, refresh, отзыв) трогают стор. Команды, считающие statelessness абсолютом, в итоге не могут выкинуть скомпрометированную сессию.
</Inset>

## Где ты хранишь токен — решает, какая атака тебя достанет

Прежде чем выбрать хранилище, спроси себя: если на этой странице выполняется сторонний скрипт или проскользнул XSS — что именно может прочитать атакующий? Ответ определяет поверхность атаки до того, как написана хоть строка кода.

Токен — bearer-учётка: кто им владеет, тот и есть ты. Поэтому хранение — решение по безопасности, а не по удобству, и это настоящий трейдофф без бесплатного варианта.

- **`localStorage`** доступен любому JavaScript на странице. Один XSS-баг — уязвимая зависимость, несанитизированный рендер — и атакующий читает токен и выкачивает его. Аналога `httpOnly` для `localStorage` нет.
- **`httpOnly`-cookie** невидимы для JavaScript, так что XSS их не прочтёт — но cookie отправляются автоматически, что открывает CSRF. Это закрывают через `SameSite=Strict` (или `Lax`) плюс `Secure` и CSRF-токены для чувствительных действий.
- **In-memory** (JS-переменная) не переживает ни рефреш, ни новую вкладку, но никогда не касается диска и является наименьшей XSS-целью — частый выбор для access-токена в паре с `httpOnly`-cookie, держащей refresh-токен.

Хранилища, иммунного ко всему, нет; ты выбираешь, какой класс атаки тебе удобнее защищать, а короткие TTL токенов сжимают урон от того, что всё же просочится.

<TradeoffMatrix
  id="03-jwt-pitfalls-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="SPA нужно держать пользователя залогиненным между рефрешами страницы. Выбери стратегию хранения токенов, которую отгрузит думающий о безопасности сеньор."
  options={[
    { name: "Access-токен в памяти (короткий TTL), refresh-токен в httpOnly + Secure + SameSite cookie", summary: "Раздели по чувствительности.", correct: true, justification: "Access-токен — высокочастотная учётка и наименьшая XSS-цель, когда он живёт только в JS-переменной; TTL 5–15 мин ограничивает урон при утечке. Долгоживущий refresh-токен — настоящий приз — лежит в httpOnly-cookie, которую JavaScript не прочтёт никогда, а SameSite + Secure закрывают CSRF и транспортные дыры. Это слоёный раздел, к которому ведут и RFC 8725, и рекомендации Auth0." },
    { name: "Оба токена в localStorage, чтобы SPA легко их читала", summary: "Удобно.", justification: "localStorage читается любым скриптом на странице, так что один XSS-баг выкачивает оба токена — включая долгоживущий refresh-токен, потеря которого хуже всего. Удобство здесь обменивается на всю безопасность сессии." },
    { name: "Оба токена в cookie без httpOnly", summary: "Cookie, но читаемая.", justification: "Снять httpOnly — значит JavaScript всё ещё читает cookie, так что ты сохраняешь XSS-экспозицию и добавляешь CSRF-поверхность от автоотправки cookie — худшее из обеих моделей хранения." },
    { name: "Долгоживущий access-токен (24ч) в памяти, без refresh-токена", summary: "Без refresh.", justification: "Хранение в памяти хорошо, но у 24-часового access-токена нет отзыва: при утечке он валиден весь день, и ты убрал механизм ротации refresh, который ловит повторное использование. Короткий TTL + ротация — в этом и смысл." },
  ]}
/>

<Quiz
  id="03-jwt-pitfalls-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Сервер проверяет токены через jwt.verify(token, key) и читает алгоритм из заголовка токена. Какое усиление самое важное?"
  choices={[
    { label: "Передать явный allowlist алгоритмов, например { algorithms: ['RS256'] }, чтобы alg из заголовка не мог выбрать путь проверки", correct: true },
    { label: "Сделать RSA-ключ длиннее (4096 бит), чтобы публичный ключ было труднее угадать", misconception: "Длина ключа не помогает: в путанице алгоритмов публичный ключ не угадывают, его публикуют. Баг — в том, что заголовок выбирает HS256 и публичный ключ попадает в HMAC-верификатор." },
    { label: "Дважды base64-кодировать payload для лишней обфускации", misconception: "Кодирование — это не шифрование и не подпись; двойное кодирование ничего не даёт, payload по-прежнему переписывается атакующим до подписи." },
    { label: "Добавить собственный claim 'verified: true' и проверять его", misconception: "Любой claim внутри payload подконтролен атакующему, пока подпись не проверена правильно. Самопровозглашённый флаг ничего не подтверждает." },
  ]}
/>

<Quiz
  id="03-jwt-pitfalls-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Пользователь сообщил, что его сессию угнали. Со stateless-JWT почему нельзя просто «разлогинить» его на сервере и что ограничивает урон?"
  choices={[
    { label: "Stateless-JWT не хранится на сервере, поэтому валиден до exp; короткие TTL access (5–15 мин) плюс ротация refresh-токена ограничивают окно", correct: true },
    { label: "Можно мгновенно отозвать любой JWT, удалив его из базы данных", misconception: "Stateless-JWT никогда не был в твоей базе — в этом весь дизайн. Удалять нечего; токен валиден до exp, если не построить список отзыва." },
    { label: "Очистка клиентской cookie инвалидирует токен везде", misconception: "Удаление клиентской копии никак не влияет на токен, который атакующий уже выкачал; он валиден до истечения." },
    { label: "JWT автоистекают после одного использования", misconception: "JWT — не одноразовые токены; один и тот же JWT реплеится, пока не пройдёт его claim exp." },
  ]}
/>

<DragOrder
  id="03-jwt-pitfalls-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь проверки, которые сеньор прогоняет, чтобы усилить путь проверки JWT:"
  items={[
    { id: "a", label: "Зафиксируй явный allowlist алгоритмов в verify — никогда не читай alg из заголовка для выбора пути" },
    { id: "b", label: "Привяжи каждый ключ ровно к одному алгоритму, чтобы публичный ключ нельзя было использовать как HMAC-секрет" },
    { id: "c", label: "Используй HMAC-секрет из CSPRNG длиной не менее 256 бит (или асимметричную пару ключей)" },
    { id: "d", label: "Поставь короткие TTL access-токенов (5–15 мин), чтобы утёкший токен быстро истекал" },
    { id: "e", label: "Добавь ротацию refresh-токенов с детекцией повторного использования, чтобы отозвать скомпрометированное семейство токенов" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<SequenceDiagram
  actors={["Атакующий", "JWKS endpoint", "Сервер (jwt.verify)"]}
  messages={[
    { from: "Атакующий", to: "JWKS endpoint", label: "GET /.well-known/jwks.json" },
    { from: "JWKS endpoint", to: "Атакующий", label: "публичный RSA-ключ (опубликован)" },
    { from: "Атакующий", to: "Атакующий", label: "подделка: alg=HS256, role=admin, подпись публичным ключом как HMAC-секретом" },
    { from: "Атакующий", to: "Сервер (jwt.verify)", label: "поддельный JWT (alg:HS256)" },
    { from: "Сервер (jwt.verify)", to: "Сервер (jwt.verify)", label: "читает alg из заголовка → уходит в HMAC-ветку, проверяет публичным ключом" },
    { from: "Сервер (jwt.verify)", to: "Атакующий", label: "200 OK — доступ admin получен" },
  ]}
  label="Атака путаницы алгоритмов RS256→HS256: атакующий берёт опубликованный RSA-публичный ключ, подделывает токен с alg:HS256, подписывает его публичным ключом как HMAC-секретом — и сервер, доверяя заголовку alg, проверяет тем же ключом и принимает поддельный токен."
  caption="Фикс — одна строка: передать { algorithms: ['RS256'] } в verify, и заголовок alg из токена становится нерелевантным. Никогда не позволяй токену описывать, как он должен проверяться."
  hue="var(--d-crypto)"
/>

<KeyTakeaway>Не доверяй заголовку токена. Фиксируй алгоритм и ключ в verify, отвергай alg:none, привязывай каждый ключ к одному алгоритму, держи TTL access на 5–15 мин, ротируй refresh-токены и храни их там, где XSS не достанет.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-jwt-pitfalls-retrieval"
  lang="ru"
  questions={[
    { q: "Проведи коллегу по атаке путаницы RS256-в-HS256 и двум защитам RFC 8725, которые её останавливают.", a: "RS256 асимметричен: приватный ключ подписывает, публичный проверяет, и этот публичный ключ предназначен для шаринга — он в твоём эндпоинте JWKS. HS256 симметричен: один секрет и подписывает, и проверяет. Атака бьёт по серверу, который решает путь проверки из собственного заголовка alg токена. Атакующий берёт опубликованный публичный ключ, подделывает токен с alg равным HS256 и подписывает его HMAC-SHA256, используя строку публичного ключа как HMAC-секрет. Сервер читает HS256, уходит в HMAC-код и проверяет тем же публичным ключом в роли секрета — воспроизводя ровно то, что вычислил атакующий, так что подписи совпадают и поддельный admin-токен принят. RFC 8725 даёт две защиты: первая — зафиксировать ожидаемый алгоритм, передав явный allowlist в verify, чтобы заголовок никогда не выбрал HS256; вторая — привязать каждый ключ ровно к одному алгоритму, чтобы публичный RS256-ключ нельзя было отдать HMAC-верификатору. Любая из них в одиночку ломает атаку; делай обе." },
    { q: "Почему у stateless-JWT нет настоящего logout и как короткие TTL плюс ротация refresh-токена сдерживают украденный токен?", a: "Stateless-JWT не хранится на сервере — сервер просто проверяет подпись и доверяет claim exp. Поэтому при logout нечего удалять, а токен, который атакующий уже скопировал, остаётся валидным до истечения; очистка клиентской cookie на него не влияет. Сдерживаешь это временем и ротацией. Access-токены получают короткий TTL, обычно 5–15 минут, так что утёкший умирает почти сразу. Долгоживущий refresh-токен — настоящий секрет, поэтому он ротируется: каждое использование выдаёт новый refresh-токен и инвалидирует старый. Если когда-нибудь предъявят старый, уже проротированный refresh-токен, сервер видит повторное использование и отзывает всё семейство токенов, выкидывая и атакующего, и легитимного пользователя до повторной аутентификации. Это возвращает немного серверного состояния для редких событий refresh и отзыва, тогда как частый случай — проверка подписи на каждом запросе — остаётся stateless и дешёвым." },
  ]}
/>

<Recap lang="ru">
Каждая ловушка JWT сводится к одной ошибке: доверять токену в описании того, как его проверять. Баг alg:none принимает пустую подпись; путаница алгоритмов превращает опубликованный публичный RS256-ключ в HMAC-секрет — обе останавливаются фиксацией явного allowlist алгоритмов в verify и привязкой каждого ключа ровно к одному алгоритму, как предписывает RFC 8725. Поскольку stateless-токен не хранится, мгновенного logout нет, поэтому украденный токен сдерживают короткими TTL access (5–15 минут) и ротацией refresh-токенов с детекцией повторного использования. HMAC-секреты должны быть минимум 256 случайных бит, чтобы их нельзя было взломать офлайн. А хранение — это трейдофф без бесплатного варианта: localStorage читается XSS, httpOnly-cookie уворачиваются от XSS, но приглашают CSRF (закрой его SameSite + Secure), а in-memory — наименьшая цель; выбери атаку, которую тебе удобнее защищать, и пусть короткие TTL сжимают то, что просочится. Теперь, когда встретишь вызов jwt.verify в код-ревью, первый вопрос: передан ли явный allowlist алгоритмов и привязан ли каждый ключ ровно к одному алгоритму? Если хоть один ответ — нет, токен сам описывает свою проверку — а это и есть точка входа атакующего.
</Recap>
