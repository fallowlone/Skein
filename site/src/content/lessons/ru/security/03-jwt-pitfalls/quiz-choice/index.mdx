---
concepts:
- alg-none
- algorithm-confusion
- claim-validation
- token-revocation
- token-storage-xss
- weak-hmac-secret
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.rfc-editor.org/rfc/rfc8725.html
- https://portswigger.net/web-security/jwt/algorithm-confusion
- https://workos.com/blog/secure-jwt-storage
status: ready
summary: "Тест с выбором на синтез по всему юниту JWT-pitfalls — alg:none, путаница RS256→HS256, проверка claim, отзыв и компромиссы хранения токенов."
title: 'JWT pitfalls: тест с выбором ответа'
track: security
unit: 03-jwt-pitfalls
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает реальное решение о верификации — не определение для заучивания, а границу доверия, которую надо защитить против атакующего, контролирующего токен.
</Hook>

<Goal lang="ru">
Убедись, что связываешь сбои JWT — доверие к алгоритму, привязку ключа, проверки claim, отзыв и хранение — в одно правило: никогда не позволяй токену описывать, как его проверять.
</Goal>

<Quiz
  id="03-jwt-pitfalls-choice-1"
  lessonSlug="03-jwt-pitfalls"
  lang="ru"
  question="Версия библиотеки принимает токен с заголовком alg:none и пустым сегментом signature, возвращая valid. Почему это катастрофа и какой фикс в одну строку?"
  choices={[
    { label: "alg:none означает «без подписи», поэтому атакующий пишет любой payload (role:admin), опустошает signature и аутентифицируется как кто угодно. Фикс: передать verify явный allowlist алгоритмов, не содержащий none.", correct: true },
    { label: "Это важно, только если payload ещё и зашифрован; зашифруй claims и пустая signature безвредна.", misconception: "JWT по умолчанию подписываются, а не шифруются, и шифрование — отдельная задача. Брешь целостности — это принятие пустой signature; шифрование не остановит верификатор, доверяющий alg:none." },
    { label: "Всё нормально, ведь exp всё ещё ограничивает время жизни токена.", misconception: "exp — это просто ещё один контролируемый атакующим claim, как только signature не проверяется. С принятым alg:none атакующий ставит exp каким угодно вместе с role:admin." },
    { label: "Использовать длиннее ключ, чтобы пустую signature нельзя было угадать.", misconception: "Угадывать нечего — alg:none означает полное отсутствие подписи. Длина ключа нерелевантна; верификатор должен прямо отвергать алгоритм none." },
  ]}
/>

<Quiz
  id="03-jwt-pitfalls-choice-2"
  lessonSlug="03-jwt-pitfalls"
  lang="ru"
  question="В атаке путаницы RS256→HS256 каким секретом атакующий подписывает поддельный токен и почему сервер его принимает?"
  choices={[
    { label: "Опубликованной строкой публичного RSA-ключа сервера, используемой как HMAC-секрет. Сервер читает HS256, уходит в ветку HMAC и проверяет тем же публичным ключом — воспроизводя вычисление атакующего, так что подписи совпадают.", correct: true },
    { label: "Приватным RSA-ключом сервера, восстановленным факторизацией публичного.", misconception: "Никакого восстановления приватного ключа нет; RSA не взломан. Атака использует публичный ключ как симметричный секрет — он уже опубликован в JWKS-эндпоинте." },
    { label: "Брутфорснутым HMAC-секретом, угаданным из токена.", misconception: "Ничего не брутфорсится. HMAC-секрет — это публичный ключ, доступный свободно, поэтому атакующий вычисляет валидную HS256-подпись напрямую." },
    { label: "Украденной session-cookie, воспроизведённой как signature.", misconception: "Cookie не связаны с подписью JWT. Путаница работает чисто потому, что верификатор позволяет заголовку выбрать HS256 и скармливает HMAC-пути ключ, привязанный к RS256." },
  ]}
/>

<Quiz
  id="03-jwt-pitfalls-choice-3"
  lessonSlug="03-jwt-pitfalls"
  lang="ru"
  question="Сервис корректно пинит alg:RS256 и проверяет signature, но токен, выпущенный для другого микросервиса в том же домене доверия, принимается как свой. Какой проверки claim не хватает?"
  choices={[
    { label: "Проверки aud (audience) — и обычно iss тоже. Валидная signature лишь доказывает, что токен пришёл от доверенного issuer; aud доказывает, что он выпущен для ЭТОГО сервиса, а не для соседа, делящего ключ подписи.", correct: true },
    { label: "Проверки exp — токен должен был уже истечь.", misconception: "exp управляет временем жизни, а не предполагаемым получателем. Совершенно свежий токен для другого audience всё равно неверен для этого сервиса; только aud/iss привязывают его к нужному потребителю." },
    { label: "Ничего не не хватает — валидной signature достаточно.", misconception: "Валидность signature лишь доказывает аутентичность выпуска. Без aud любой сервис, делящий ключ, принимает токены, выпущенные для любого другого сервиса — confused-deputy через домен доверия." },
    { label: "Проверки kid — использован неверный id ключа.", misconception: "kid выбирает, какой ключ проверяет signature; здесь signature уже прошла. Дефект — принятие токена, чей audience — совсем другой сервис." },
  ]}
/>

<Quiz
  id="03-jwt-pitfalls-choice-4"
  lessonSlug="03-jwt-pitfalls"
  lang="ru"
  question="Заголовок токена несёт kid, указывающий на https://attacker.example/jwks.json, и сервер берёт ключ по этому URL для проверки. В чём сбой и фикс?"
  choices={[
    { label: "kid (и jku) — контролируемые атакующим поля заголовка. Доверие к ним даёт атакующему захостить свой ключ и проверить самоподписанный токен. Фикс: резолвить kid только по закреплённому allowlist доверенных ключей; никогда не тянуть с URL, названного заголовком.", correct: true },
    { label: "Это безопасно, ведь ключ берётся по HTTPS.", misconception: "HTTPS аутентифицирует собственный сервер атакующего, а не то, что ключ — твой. Заголовок выбрал локацию, поэтому безопасность транспорта не мешает атакующему отдать ключ, который он контролирует." },
    { label: "Фикс — кэшировать полученный ключ, чтобы тянуть его один раз.", misconception: "Кэширование переданного атакующим ключа лишь закрепляет неверный ключ. Дефект — вообще брать ключ из локации, названной заголовком; пинь доверенные ключи на стороне сервера." },
    { label: "kid влияет только на логирование, так что это косметика.", misconception: "kid выбирает ключ проверки. Если сервер тянет ключ, на который указывает заголовок, атакующий полностью контролирует верификацию — противоположность косметике." },
  ]}
/>

<Quiz
  id="03-jwt-pitfalls-choice-5"
  lessonSlug="03-jwt-pitfalls"
  lang="ru"
  question="Пользователь сообщает, что его сессию угнали. С stateless JWT почему нельзя разлогинить его на стороне сервера и что ограничивает ущерб?"
  choices={[
    { label: "Stateless JWT не хранится на сервере, поэтому он валиден до exp; удалять нечего. Короткие TTL access-токена (5–15 мин) плюс ротация refresh-токена с детектом повторного использования ограничивают окно.", correct: true },
    { label: "Ты мгновенно отзываешь его, удалив строку из таблицы sessions.", misconception: "Stateless JWT никогда не был в твоей базе — в этом весь смысл. Удалять нечего; токен валиден до exp, если не добавить список отзыва." },
    { label: "Очистка клиентской cookie аннулирует токен везде.", misconception: "Удаление копии клиента ничего не делает с токеном, который атакующий уже выкрал. Он валиден до exp независимо от состояния cookie клиента." },
    { label: "JWT одноразовые, так что угнанный токен уже мёртв.", misconception: "JWT не одноразовые; тот же JWT воспроизводим, пока не пройдёт его exp, — именно поэтому важны короткие TTL и ротация." },
  ]}
/>

<Quiz
  id="03-jwt-pitfalls-choice-6"
  lessonSlug="03-jwt-pitfalls"
  lang="ru"
  question="SPA должна переживать перезагрузку страницы. Какое разделение хранения отгружает security-минд senior и почему?"
  choices={[
    { label: "Access-токен в памяти (короткий TTL); refresh-токен в cookie httpOnly + Secure + SameSite. Высокоценный refresh-токен недоступен JavaScript, а короткоживущий access-токен — наименьшая цель для XSS.", correct: true },
    { label: "Оба токена в localStorage для лёгкого доступа.", misconception: "localStorage читается любым скриптом на странице, так что один XSS-баг выкрадывает оба токена — включая долгоживущий refresh-токен, потерять который хуже всего. Для localStorage нет аналога httpOnly." },
    { label: "Оба токена в cookie без httpOnly.", misconception: "Снятие httpOnly оставляет cookie читаемой JavaScript, так что ты сохраняешь экспозицию XSS И добавляешь поверхность CSRF от автоматической отправки cookie — худшее из обеих моделей." },
    { label: "Один долгоживущий (24ч) access-токен в памяти, без refresh-токена.", misconception: "Память — это хорошо, но у 24-часового access-токена нет быстрого отзыва, и ты убрал ротацию refresh, детектящую повтор. Короткий TTL плюс ротация — в этом весь смысл." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одно правило: никогда не доверяй токену описывать собственную верификацию. alg:none и путаница RS256→HS256 умирают, когда ты пинишь явный allowlist алгоритмов и привязываешь каждый ключ к одному алгоритму; контролируемые атакующим kid/jku умирают, когда ты резолвишь ключи только по закреплённому allowlist. Валидная signature необходима, но недостаточна — проверяй aud и iss, чтобы токен был привязан к ЭТОМУ сервису. А поскольку stateless-токен нельзя «отвыпустить», короткие TTL access, ротация refresh-токена с детектом повтора и XSS-осознанное хранение ограничивают то, что утечёт. Теперь, когда встретишь вызов verify в пул-реквесте, этот чеклист сработает автоматически — алгоритм запинен, ключ привязан, claim проверены, хранение разделено — до того, как нажмёшь «Approve».
</Recap>
