---
concepts:
- alg-none
- algorithm-confusion
- claim-validation
- kid-injection
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://www.rfc-editor.org/rfc/rfc8725.html
- https://portswigger.net/web-security/jwt/algorithm-confusion
status: ready
summary: "Читай реальные сниппеты верификации JWT и поддельный токен, находи уязвимость и выбирай фикс наибольшего рычага, который security-минд senior делает первым."
title: 'JWT pitfalls: чтение кода и токенов'
track: security
unit: 03-jwt-pitfalls
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги JWT прячутся в вызове verify и проверках claim после него. Читай каждый сниппет так, как делал бы это в security-ревью, найди, что эксплуатирует атакующий, и выбери фикс, прежде чем дописать ещё строку.
</Hook>

<Goal lang="ru">
Отработай цикл ревью, который запускаешь на каждом auth-пути: читай вызов verify, спроси, что контролирует атакующий, и тянись к фиксу «пинь-и-валидируй», закрывающему брешь доверия.
</Goal>

## Сниппет 1 — поддельный токен alg:none

```
header:  { "alg": "none", "typ": "JWT" }
payload: { "sub": "victim", "role": "admin" }
signature: (empty)

// код сервера
const claims = jwt.decode(token);   // только decode, без verify
if (claims.role === "admin") grantAdmin();
```

<Quiz
  id="03-jwt-pitfalls-code-1"
  lessonSlug="03-jwt-pitfalls"
  lang="ru"
  question="Что этот код даёт атакующему и какой фикс?"
  choices={[
    { label: "Полный admin: jwt.decode никогда не проверяет signature, поэтому самописный payload role:admin доверяется напрямую. Фикс: вызывать jwt.verify(token, key, { algorithms: ['RS256'] }) и действовать только по проверенным claim.", correct: true },
    { label: "Ничего — decode бросает исключение на пустой signature.", misconception: "decode парсит base64url и возвращает claims независимо от signature; он её никогда не валидирует. Токен с пустой signature декодируется нормально, и проверка role проходит." },
    { label: "Ничего — токены alg:none отвергаются каждой библиотекой.", misconception: "Опасность здесь даже не в обработке alg:none — код вызывает decode, который полностью пропускает верификацию. Даже корректно подписывающий верификатор обойдён, потому что verify не вызывается." },
    { label: "Только чтение, ведь admin требует второй фактор.", misconception: "Сниппет выдаёт admin чисто по непроверенному claim role. В этом пути кода нет второго фактора; поддельный claim используется напрямую." },
  ]}
/>

## Сниппет 2 — проверка публичным ключом

```js
const publicKey = fs.readFileSync("rsa-public.pem");
function verifyToken(token) {
  // опция algorithms не передана
  return jwt.verify(token, publicKey);
}
```

<Quiz
  id="03-jwt-pitfalls-code-2"
  lessonSlug="03-jwt-pitfalls"
  lang="ru"
  question="Атакующий подделывает токен с alg:HS256, подписанный содержимым rsa-public.pem как HMAC-секретом. Почему verifyToken его принимает?"
  choices={[
    { label: "Без allowlist algorithms verify доверяет alg заголовка. Он читает HS256 и запускает HMAC-SHA256, используя байты публичного ключа как секрет — ту же операцию, что и атакующий, — так что подписи совпадают. Фикс: передать { algorithms: ['RS256'] }.", correct: true },
    { label: "Не принимает — verify всегда предполагает RS256 для .pem-ключа.", misconception: "Без явного allowlist библиотека выбирает алгоритм из заголовка, а не из расширения файла ключа. Заголовок говорит HS256, поэтому запускается HMAC-путь с публичным ключом как секретом." },
    { label: "Только если публичный ключ короче 256 бит.", misconception: "Длина ключа нерелевантна путанице; публичный ключ используется дословно как HMAC-секрет. Баг — вообще позволять заголовку выбрать HS256." },
    { label: "Потому что fs.readFileSync портит PEM в симметричный ключ.", misconception: "Файл читается корректно. Атака воспроизводит точное HMAC-вычисление сервера, используя ту же строку публичного ключа как секрет — без всякой порчи." },
  ]}
/>

## Сниппет 3 — signature проверена, claim нет

```js
const claims = jwt.verify(token, key, { algorithms: ["RS256"] });
// подпись ОК — теперь доверяем всему
req.user = claims.sub;
req.roles = claims.roles;
// нет проверок exp / iss / aud сверх дефолта библиотеки
```

<Quiz
  id="03-jwt-pitfalls-code-3"
  lessonSlug="03-jwt-pitfalls"
  lang="ru"
  question="Алгоритм запинен и signature проверена. В микросервисной сети, делящей один ключ подписи, что всё ещё не так?"
  choices={[
    { label: "Нет проверки aud (или iss). Токен, выпущенный для соседнего сервиса, делит ключ подписи, поэтому проходит и здесь — баг confused-deputy. Фикс: передать { audience: 'this-service', issuer: 'trusted-iss' }, чтобы токен был привязан к этому потребителю.", correct: true },
    { label: "Ничего — проверенной RS256-signature достаточно самой по себе.", misconception: "Валидная signature лишь доказывает аутентичность выпуска. Без aud любой сервис, делящий ключ, принимает токены, выпущенные для любого другого сервиса в сети." },
    { label: "Баг — чтение claims.sub перед claims.roles.", misconception: "Порядок чтения полей нерелевантен. Дефект — принятие токена, чей audience — совсем другой сервис, потому что aud никогда не валидируется." },
    { label: "Нужен HS256 вместо RS256 для микросервисов.", misconception: "Выбор алгоритма не связан с отсутствующей проверкой audience. Переход на HS256 наоборот расширит риск путаницы, а не починит привязку audience." },
  ]}
/>

## Сниппет 4 — резолв ключа из заголовка

```js
function getKey(header) {
  // header.kid контролируется атакующим
  return fetch(header.jku)
    .then(r => r.json())
    .then(jwks => jwks.keys.find(k => k.kid === header.kid));
}
const claims = jwt.verify(token, await getKey(jwt.decodeHeader(token)));
```

<Quiz
  id="03-jwt-pitfalls-code-4"
  lessonSlug="03-jwt-pitfalls"
  lang="ru"
  question="Как атакующий полностью контролирует верификацию здесь и какой фикс?"
  choices={[
    { label: "header.jku и header.kid контролируются атакующим, поэтому он хостит свой JWKS, указывает на него jku, подписывает соответствующим приватным ключом, и сервер тянет и доверяет этому ключу. Фикс: игнорировать jku; резолвить kid только по закреплённому на сервере allowlist доверенных ключей.", correct: true },
    { label: "Это безопасно, ведь JWKS берётся по HTTPS.", misconception: "HTTPS аутентифицирует собственный сервер атакующего, а не то, что ключ принадлежит тебе. Заголовок выбрал URL, поэтому безопасность транспорта не остановит ключ, который контролирует атакующий." },
    { label: "Единственная проблема — отсутствие кэша, fetch на каждый запрос.", misconception: "Кэширование переданного атакующим ключа лишь закрепляет неверный ключ. Уязвимость — вообще брать ключ с названного заголовком URL, а не частота fetch." },
    { label: "Нормально, пока kid совпадает с ключом в полученном наборе.", misconception: "Собственный JWKS атакующего по построению содержит совпадающий kid. Сверка с набором, захостенным атакующим, ничего не доказывает; набор ключей должен браться из доверенного серверного источника." },
  ]}
/>

<Recap lang="ru">
Каждый дефект JWT читается в вызове verify и проверках вокруг него: decode-only код доверяет неподписанным claim; отсутствующий allowlist algorithms позволяет заголовку выбрать HS256 и превращает публичный ключ в HMAC-секрет; проверенная signature без проверки aud/iss — это ждущий своего часа confused-deputy в сети с общим ключом; а резолв kid/jku из контролируемых атакующим заголовков отдаёт выбор ключа атакующему. Фикс всегда одной формы — пинь алгоритм, привязывай и allowlist-и ключ, затем валидируй зарегистрированные claim против того, что ожидает этот сервис. Теперь, открывая ревью любого auth-пути, ты знаешь, куда смотреть первым делом: вызов verify, опция алгоритма, проверки claim — именно в таком порядке.
</Recap>
