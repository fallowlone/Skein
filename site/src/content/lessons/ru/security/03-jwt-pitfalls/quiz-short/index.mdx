---
concepts:
- algorithm-confusion
- claim-validation
- token-revocation
- refresh-token-rotation
- token-storage-xss
- weak-hmac-secret
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.rfc-editor.org/rfc/rfc8725.html
- https://portswigger.net/web-security/jwt/algorithm-confusion
- https://workos.com/blog/secure-jwt-storage
status: ready
summary: "Промпты на свободное припоминание по юниту JWT-pitfalls. Сначала ответь своими словами, затем раскрой модельный ответ и сравни."
title: 'JWT pitfalls: свободное припоминание'
track: security
unit: 03-jwt-pitfalls
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт проговори или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет защиту, когда ты ревьюишь реальный auth-код.
</Hook>

<Goal lang="ru">
Восстанови ключевые механизмы юнита — пиннинг алгоритма, привязку ключа, проверки claim, отзыв, ротацию и компромиссы хранения — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:load
  id="03-jwt-pitfalls-short-retrieval"
  lang="ru"
  questions={[
    { q: "Разбери атаку путаницы алгоритмов RS256→HS256 и две защиты RFC 8725, которые её останавливают.", a: "RS256 асимметричен: приватный ключ подписывает, публичный проверяет, и публичный предназначен для распространения — он лежит в твоём JWKS-эндпоинте. HS256 симметричен: один секрет и подписывает, и проверяет. Атака бьёт по серверу, выбирающему путь верификации из собственного заголовка alg токена. Атакующий берёт опубликованный публичный ключ, подделывает токен с alg равным HS256 и подписывает его HMAC-SHA256, используя строку публичного ключа как HMAC-секрет. Сервер читает HS256, уходит в HMAC-код и проверяет тем же публичным ключом как секретом — воспроизводя ровно то, что вычислил атакующий, так что подписи совпадают и поддельный admin-токен принят. RFC 8725 даёт две защиты: пинь ожидаемый алгоритм явным allowlist, передаваемым в verify, чтобы заголовок никогда не выбрал HS256; и привязывай каждый ключ ровно к одному алгоритму, чтобы публичный RS256-ключ нельзя было отдать HMAC-верификатору. Любая по отдельности ломает атаку; делай обе." },
    { q: "Signature проверена корректно. Почему этого необходимо, но недостаточно, и какие claim нужно проверить ещё?", a: "Валидная signature лишь доказывает, что токен выпущен держателем ключа подписи и не был подделан после. Она ничего не говорит о том, предназначен ли токен тебе, в окне ли он валидности и кто его выпустил. Нужно ещё проверить exp (и nbf, время not-before), чтобы соблюсти окно жизни с небольшим люфтом на рассинхрон часов; iss — чтобы подтвердить, что токен от issuer, которому ты доверяешь; и aud — чтобы подтвердить, что он выпущен для ЭТОГО сервиса, а не соседа, делящего тот же ключ подписи. Пропуск aud — классический баг confused-deputy в микросервисной сети: каждый сервис, делящий ключ, принимает токены, выпущенные для любого другого сервиса. Так что верификация — два шага: проверь signature, затем валидируй зарегистрированные claim против того, что ожидает твоё приложение." },
    { q: "Как заголовок kid (или jku/x5u) превращается в уязвимость инъекции ключа и как её нейтрализовать?", a: "kid называет, какой ключ должен проверять signature, а jku/x5u называют URL, откуда тянуть набор ключей. Всё это — поля заголовка, контролируемые атакующим. Если сервер резолвит kid, доверяя заголовку — тянет ключ с названного заголовком URL или трактует kid как path- или SQL-lookup — атакующий хостит свой ключ, указывает на него заголовком, подписывает токен соответствующим приватным ключом, и сервер проверяет полностью самоподписанный токен. Фикс — никогда не брать материал верификации из контролируемых заголовком локаций: резолвить kid только по закреплённому на сервере allowlist доверенных ключей, игнорировать jku/x5u, если хост не в allowlist, и санитизировать kid, чтобы его нельзя было использовать для path traversal или инъекции. Считай заголовок подсказкой для выбора среди ключей, которым ты уже доверяешь, но никогда — источником самого ключа." },
    { q: "Почему у stateless JWT нет настоящего logout и как короткие TTL плюс ротация refresh-токена сдерживают украденный токен?", a: "Stateless JWT не хранится на сервере — сервер просто проверяет signature и доверяет claim exp. Так что удалять на logout нечего, и токен, который атакующий уже скопировал, валиден до истечения; очистка клиентской cookie ничего с ним не делает. Это сдерживается временем и ротацией. Access-токены получают короткий TTL, обычно 5–15 минут, так что утёкший умирает почти сразу. Долгоживущий refresh-токен — настоящий секрет, поэтому он ротируется: каждое использование выпускает новый refresh-токен и аннулирует старый. Если старый, уже отротированный refresh-токен предъявят снова, сервер детектит повтор и отзывает всё семейство токенов, блокируя и атакующего, и пользователя до повторной аутентификации. Это вводит немного серверного состояния для редких событий refresh и revoke, тогда как частый случай — проверка signature на каждый запрос — остаётся stateless и дешёвым." },
    { q: "Сравни localStorage, httpOnly cookie и хранение в памяти для токенов. Какую атаку приглашает каждое и как митигировать?", a: "localStorage читается любым JavaScript на странице, так что один XSS-баг выкрадывает токен; аналога httpOnly нет. httpOnly cookie невидимы JavaScript, поэтому XSS не может их прочитать, но cookie отправляются автоматически, что открывает CSRF — это закрывается SameSite=Strict или Lax плюс Secure, и CSRF-токенами для изменяющих состояние действий. Память (JS-переменная) никогда не касается диска и наименьшая цель XSS, но не переживает перезагрузку или новую вкладку. Senior-паттерн делит по чувствительности: access-токен живёт в памяти с коротким TTL как наименьшая цель, а высокоценный refresh-токен сидит в cookie httpOnly + Secure + SameSite, которую JavaScript никогда не прочитает. Ни одно хранение не иммунно ко всему; ты выбираешь, какой класс атаки защищать, и короткие TTL уменьшают то, что просочится." },
    { q: "Почему HMAC-секрет должен быть минимум 256 случайных бит из CSPRNG и что идёт не так с выбранным человеком?", a: "Верификация HS256 надёжна ровно настолько, насколько энтропия секрета. Короткий или выбранный человеком секрет — слово, фраза, дефолт из конфига — уязвим к офлайн-брутфорсу или словарной атаке: атакующий захватывает любой валидный токен, затем перебирает кандидаты-секреты локально, пока один не воспроизведёт signature, без сетевых раундтрипов и без rate limiting, который бы его замедлил. Восстановив секрет, он может выпускать токены с любыми claim, включая role:admin. Секрет в 256 бит из криптографически стойкого генератора делает пространство перебора астрономически большим, так что офлайн-взлом неосуществим. Правила: минимум 32 случайных байта, из CSPRNG (не Math.random и не timestamp), никогда не в системе контроля версий, ротировать при любой утечке. Для асимметричных алгоритмов аналог — корректно сгенерированная пара RSA или EC и хранение приватного ключа подальше от любого проверяющего сервиса." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: пинь алгоритм и привязывай каждый ключ к одному алгоритму, чтобы alg:none и путаница RS256→HS256 не сработали; считай валидную signature необходимой, но недостаточной и валидируй exp/nbf/iss/aud; резолви kid/jku только по закреплённому allowlist, чтобы заголовок не инжектил ключ; сдерживай украденный токен короткими TTL access и ротацией refresh-токена с детектом повтора, раз настоящего logout нет; выбирай хранение по тому, какую атаку предпочитаешь защищать; и используй 256-битный секрет из CSPRNG, чтобы HMAC нельзя было взломать офлайн.
</Recap>
