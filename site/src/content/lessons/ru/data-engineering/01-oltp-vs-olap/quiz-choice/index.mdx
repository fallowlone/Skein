---
concepts:
- row-store
- column-store
- column-pruning
- vectorized-execution
- cdc-etl
- workload-split
estMin: 12
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://clickhouse.com/resources/engineering/why-columnar-databases-are-fast
- https://clickhouse.com/resources/engineering/unifying-oltp-and-olap
- https://aws.amazon.com/blogs/database/improve-postgresql-performance-diagnose-and-mitigate-lock-manager-contention/
status: ready
summary: "Тест на синтез по всему юниту OLTP vs OLAP — row store против column store, column pruning, сжатие, векторизация, авария от аналитики на реплике и когда разделять хранилища."
title: 'OLTP vs OLAP: тест с множественным выбором'
track: data-engineering
unit: 01-oltp-vs-olap
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, проходящих через весь юнит. Каждый отражает решение, которое вы принимаете на реальном архитектурном ревью, — не определение для пересказа, а компромисс, который надо взвесить, когда дашборд и путь оформления заказа делят одну базу.
</Hook>

<Goal lang="ru">
Убедитесь, что вы связываете физический layout, множители column store, режим отказа «аналитика на реплике» и решение о разделении хранилищ — тот синтез, к которому вёл обзорный урок.
</Goal>

<Quiz
  id="01-oltp-vs-olap-choice-1"
  lessonSlug="01-oltp-vs-olap"
  lang="ru"
  question="Запрос считает SUM(total) по 40M строкам orders, у каждой по 50 колонок. Почему column store громит row store именно на этом запросе?"
  choices={[
    { label: "Column pruning: column store читает только колонку total — примерно 6% байтов от того, что трогает row store, — затем сканирует её сжатой и векторизованно. Pruning, сжатие и пакетное выполнение складываются в разрыв 10×–1000×.", correct: true },
    { label: "У column store лучше индекс на total, превращающий скан в seek.", misconception: "Агрегату по всей таблице нужны почти все строки, поэтому искать нечего — планировщик всё равно делает sequential scan. Выигрыш в том, что читается меньше колонок, а не в индексе." },
    { label: "Column store держит всю таблицу в RAM, а row store читает с диска.", misconception: "Ни один движок не определяется кэшированием всего. Структурный выигрыш — в layout на диске (одна колонка непрерывно), что включает pruning, сжатие и векторизованные сканы даже с диска." },
    { label: "Column store быстрее пишет одиночные строки, поэтому и читает быстрее.", misconception: "Column store медленнее на одиночных записях, а не быстрее — запись трогает каждый файл колонки. Преимущество в сканах по колонкам, это совсем другая ось." },
  ]}
/>

<Quiz
  id="01-oltp-vs-olap-choice-2"
  lessonSlug="01-oltp-vs-olap"
  lang="ru"
  question="Сервис A делает point lookup по id со скоростью 5k req/s; сервис B гоняет агрегат по колонкам тех же данных. Почему один layout не может хорошо обслуживать обоих?"
  choices={[
    { label: "OLTP оптимизирует задержку записи и извлечение целой строки (row store, колонки строки лежат рядом); OLAP оптимизирует пропускную способность скана по нескольким колонкам (column store, каждая колонка лежит непрерывно). Layout, отличный в одном, структурно плох в другом — они тянут физический дизайн в противоположные стороны.", correct: true },
    { label: "Могут — поставь один флаг, переключающий таблицу между row и column режимом на каждый запрос.", misconception: "Бесплатного переключателя нет. Layout физический: row store хранит строки непрерывно, column store — колонки непрерывно. HTAP-системы пытаются это совместить, но платят реальную цену; один layout не может быть оптимален для обеих форм." },
    { label: "Разница лишь в том, что OLAP-запросы медленнее, так что просто дай OLTP-машине больше CPU.", misconception: "Это не ручка скорости. Несоответствие структурное — row store читает каждый байт каждой строки для агрегата, и больше CPU не меняет того, что он читает 50 колонок ради 1." },
    { label: "OLAP — это просто OLTP с большими таблицами; тот же движок масштабируется нормально.", misconception: "Размер таблиц — не различие. Различие в паттерне доступа: point lookup и мелкие записи против сканов всей таблицы по нескольким колонкам — противоположные цели оптимизации." },
  ]}
/>

<Quiz
  id="01-oltp-vs-olap-choice-3"
  lessonSlug="01-oltp-vs-olap"
  lang="ru"
  question="На ClickBench из 100M строк ClickHouse хранит данные примерно в 9 GiB там, где Postgres нужно около 100 GiB. Почему column store сжимает настолько лучше и почему это расширяет разрыв в пропускной способности скана, а не просто экономит диск?"
  choices={[
    { label: "Колонка хранит значения одного типа и часто низкой кардинальности, поэтому dictionary + run-length + LZ4/ZSTD дают 5–10× (30×+ на низкокардинальных колонках) против 1.5–3× для смешанных по типу страниц строки. Меньше данных на диске — меньше I/O, а I/O ограничивает сканы — поэтому разрыв в сжатии умножает разрыв в пропускной способности.", correct: true },
    { label: "ClickHouse выбрасывает индексы, поэтому он меньше; скорость только от меньшего обслуживания.", misconception: "Разница в размере от колоночного сжатия однотипных значений, а не от выброса индексов. Выигрыш в пропускной способности — от pruning плюс чтения куда меньшего объёма сжатых байтов, а не от обслуживания индексов." },
    { label: "Сжатие только экономит место на диске; на скорость запросов оно не влияет.", misconception: "Для аналитических запросов, упёртых в скан, узкое место — I/O. Меньше байтов на диске — меньше байтов читать, поэтому сжатие напрямую умножает пропускную способность скана; это не просто экономия хранения." },
    { label: "Row store мог бы сравняться, включив сжатие страниц; форматы эквивалентны.", misconception: "Сжатие страниц row store мешает int, string, timestamp и float на странице и даёт лишь ~1.5–3×. Колонка пакует один тип вместе, поэтому достигает 5–10× — layout-ы не эквивалентны." },
  ]}
/>

<Quiz
  id="01-oltp-vs-olap-choice-4"
  lessonSlug="01-oltp-vs-olap"
  lang="ru"
  question="Дашборд growth-команды гоняет 90-дневный агрегат выручки против прод-реплики Postgres. Checkout p99 прыгает с 40ms до 2s, а replica lag растёт до 90s. Какую первопричину senior назовёт первой?"
  choices={[
    { label: "Аналитический скан тянет холодные страницы через shared_buffers и вытесняет горячие OLTP-страницы, поэтому транзакционные чтения, бывшие попаданиями в память, начинают бить по диску; он же конкурирует за CPU и I/O и задерживает применение WAL, поднимая replica lag. Реплика — всё ещё row store, рассчитанный на транзакционный working set.", correct: true },
    { label: "На реплике не хватает композитного индекса, нужного запросу дашборда.", misconception: "Индекс не помогает 90-дневному агрегату по всей таблице — искать нечего. А симптом — замедление OLTP от вытеснения кэша и конкуренции, а не медленный запрос дашборда." },
    { label: "Реплики read-only, поэтому записи дашборда копятся и блокируют checkout.", misconception: "Дашборд только читает, а записи идут на primary, не на реплику. Урон — вытеснение buffer cache и конкуренция за ресурсы на реплике плюс replica lag, а не заблокированные записи." },
    { label: "Слишком маленький connection pool, поэтому запросы checkout ждут соединение.", misconception: "Размер пула — не механизм. Даже один аналитический скан отравляет buffer cache и конкурирует за I/O на row store, рассчитанном на транзакционный working set." },
  ]}
/>

<Quiz
  id="01-oltp-vs-olap-choice-5"
  lessonSlug="01-oltp-vs-olap"
  lang="ru"
  question="Продукт хочет 90-дневный дашборд выручки. Какую из четырёх архитектур senior выкатит и почему?"
  choices={[
    { label: "Поднять отдельный колоночный OLAP-store (warehouse или ClickHouse) с собственным хранилищем и кэшами, кормить его из Postgres через CDC или батчевый ETL и направить дашборд туда. Это физически изолирует нагрузки — агрегат делает pruning, сжимается и векторизуется, а checkout этого не чувствует.", correct: true },
    { label: "Добавить композитный индекс на (country, created_at), чтобы агрегат использовал индекс.", misconception: "Индексы чинят selectivity, а не объём скана. 90-дневному агрегату по всем странам нужны почти все строки, поэтому планировщик игнорирует индекс и всё равно сканирует — вы добавили стоимость записи без выигрыша на чтении." },
    { label: "Кэшировать результат дашборда в Redis и пересчитывать раз в час с реплики.", misconception: "Ежечасный пересчёт всё равно гоняет разрушительный полный скан на реплике — checkout деградирует раз в час, а кэш устаревает между прогонами. Это лечит проблему layout как проблему представления." },
    { label: "Гонять на реплике, но только в непиковые часы.", misconception: "Непиковые часы сужают радиус поражения, но не чинят: скан всё равно вытесняет buffer cache и поднимает replica lag в окне, а дашборды «за последние 90 дней» редко по-настоящему непиковые. Фикс — отдельное хранилище, а не расписание." },
  ]}
/>

<Quiz
  id="01-oltp-vs-olap-choice-6"
  lessonSlug="01-oltp-vs-olap"
  lang="ru"
  question="Команда предлагает HTAP-движок, чтобы обслуживать и транзакции, и аналитику на одной системе, утверждая, что это снимает нужду разделять хранилища. Какова senior-формулировка?"
  choices={[
    { label: "HTAP может помочь, но ни один промышленный HTAP не устраняет конкуренцию между скан-тяжёлой и запись-тяжёлой нагрузкой — он управляет ею (отдельные row и column реплики, resource groups), а не убирает. Разделение на два хранилища с CDC/ETL остаётся дефолтом, потому что даёт независимые кэши и failure domains.", correct: true },
    { label: "HTAP решает это полностью — один движок означает один layout, оптимальный для обеих нагрузок.", misconception: "Ни один физический layout не оптимален и для point-записей, и для сканов по колонкам. HTAP-системы держат внутри и row, и column представление и платят за их поддержку — они управляют напряжением, а не отменяют его." },
    { label: "HTAP всегда неправ; нужно держать две отдельные базы.", misconception: "У HTAP есть реальные применения, где задержка данных должна быть почти нулевой, а эксплуатация двух систем слишком дорога. Это компромисс, а не ошибка — но он не стирает конкуренцию, мотивирующую разделение." },
    { label: "HTAP снимает нужду в CDC, потому что всё в одном месте.", misconception: "Даже с HTAP вы часто всё равно кормите downstream-склады; а смысл CDC в дизайне с двумя хранилищами — изоляция. То, что HTAP меняет, где лежат данные, само по себе не устраняет конкуренцию, ради ухода от которой делалось разделение." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одно решение: паттерн доступа задаёт layout. Point lookup и мелкие записи хотят row store; сканы всей таблицы по нескольким колонкам хотят column store, где pruning (читать ~6% байтов), сжатие 5–10× и векторизованное пакетное выполнение складываются в разрыв 10×–1000×, который не закроет ни один индекс. Классический senior-провал — аналитика на прод-реплике OLTP, где один скан вытесняет горячий buffer cache, конкурирует за I/O и поднимает replica lag. Фикс архитектурный — отдельный колоночный store, кормимый через CDC или ETL, — с принятием небольшой задержки данных ради изолированных хранилища, кэшей и failure domains. HTAP может управлять конкуренцией, но никогда её не отменяет.
</Recap>
