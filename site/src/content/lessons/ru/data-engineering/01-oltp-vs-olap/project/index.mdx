---
concepts:
- workload-split
- column-pruning
- cdc-etl
- buffer-cache-eviction
- denormalization
estMin: 240
lang: ru
level: senior
order: 10
prereqs: []
slug: project
sources:
- https://clickhouse.com/resources/engineering/why-columnar-databases-are-fast
- https://clickhouse.com/resources/engineering/unifying-oltp-and-olap
- https://aws.amazon.com/blogs/database/improve-postgresql-performance-diagnose-and-mitigate-lock-manager-contention/
status: ready
summary: "Практический проект — воспроизведите аварию «аналитика на OLTP», затем разделите нагрузку в колоночный store, кормимый через CDC/ETL, и докажите фикс числами до/после."
title: 'OLTP vs OLAP: разделите нагрузку'
track: data-engineering
unit: 01-oltp-vs-olap
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Читать про аварию «аналитика на реплике» — не то же, что вызвать её, а потом починить. Поднимите row-store OLTP-базу, загоните в неё аналитический скан, пока checkout не деградирует, затем разделите нагрузку в колоночный store, кормимый через CDC/ETL, — и докажите каждый шаг измерениями.
</Hook>

<Goal lang="ru">
Превратите ментальную модель юнита в воспроизводимый инженерный цикл: воспроизвести конкуренцию, измерить её, поднять правильный OLAP-layout, подключить feed и проверить и что аналитика ускорилась, и что транзакционный путь перестал страдать.
</Goal>

<ProjectBrief
  id="01-oltp-vs-olap-project"
  lessonSlug="01-oltp-vs-olap"
  lang="ru"
  objective="Воспроизведите отказ «аналитика на OLTP» на row store, затем перенесите аналитику в отдельный колоночный store, кормимый через CDC или ETL — доказав, что аналитический запрос резко ускорился И что транзакционная нагрузка больше не страдает, числами до/после при одинаковой нагрузке."
  requirements={[
    "Загрузите row-store OLTP-базу (Postgres/MySQL) широкой таблицей orders — минимум 20M строк, 20+ колонок, нормализованная схема с FK на country/измерение. Гоните устойчивую транзакционную нагрузку: point lookup по id и мелкие записи с фиксированной скоростью.",
    "Прогоните аналитический запрос против того же инстанса (или его реплики чтения): многомесячный агрегат вроде SUM(total) GROUP BY country без селективного WHERE. Снимите EXPLAIN (ANALYZE, BUFFERS) — подтвердите sequential scan и запишите прочитанные байты/страницы и время выполнения.",
    "Измерьте конкуренцию: транзакционный p99, hit ratio buffer cache и (если используете реплику) replica lag — с запущенным аналитическим запросом и без. Покажите, что путь OLTP деградирует, когда идёт аналитика.",
    "Продемонстрируйте, что добавление индекса НЕ чинит: создайте правдоподобный композитный индекс, перезапустите агрегат и покажите, что планировщик всё равно выбирает sequential scan (или индекс лишь добавляет стоимость записи).",
    "Поднимите отдельный колоночный store (ClickHouse, DuckDB или облачный warehouse) с денормализованной широкой fact-таблицей; сверните измерение в low-cardinality/dictionary-encoded колонку и выберите разумный sort/order key.",
    "Кормите колоночный store из OLTP: CDC-стрим (Debezium → Kafka или логическая репликация) или батчевый ETL/ELT. Укажите окно задержки данных, которое вводит ваш feed.",
    "Перезапустите идентичный агрегат против колоночного store и одновременно прогоните транзакционную нагрузку на OLTP; запишите числа после рядом с до.",
  ]}
  acceptance={[
    "Таблица до/после: время аналитического запроса, просканированные байты/страницы, OLTP p99, hit ratio buffer cache и replica lag — всё измерено при одинаковой нагрузке, не оценено.",
    "План колоночного store показывает, что он читает только колонки, на которые ссылается (pruning), и аналитический запрос минимум в 10× быстрее скана на row store.",
    "С аналитикой, унесённой с OLTP-инстанса, транзакционный p99 и hit ratio кэша возвращаются к baseline, пока идёт аналитический запрос — конкуренция исчезла.",
    "Абзац разбора: почему индекс не помог (selectivity против объёма скана), какое свойство column store дало большую часть ускорения и цену задержки данных, которую вы приняли за изоляцию.",
  ]}
  stretch={[
    "Добавьте сравнение сжатия: сообщите размер таблицы на диске в row store против колоночного store и разложите, какие колонки сжались сильнее и почему (низкая кардинальность, один тип).",
    "Сделайте CDC-feed почти real-time и измерьте сквозной лаг от коммита в OLTP до видимости в колоночном store; постройте график лага под всплеском записи.",
    "Добавьте guardrail, не дающий аналитическим запросам попадать на OLTP-инстанс (отдельная роль/endpoint, statement timeout или query router) и покажите, что он блокирует разрушительный скан.",
    "Поднимите вариант HTAP (например column-реплику или HTAP-движок) для той же нагрузки и сравните его с разделением на два хранилища по аналитической задержке, OLTP p99 под нагрузкой, свежести данных и операционной стоимости.",
  ]}
/>

<Recap lang="ru">
Это цикл, который вы запускаете на каждом реальном решении по архитектуре данных: воспроизвести конкуренцию, чтобы понять её, доказать, что индекс не починит агрегат по всей таблице, поднять правильный колоночный layout (денормализованный, низкокардинальный, пруньящийся), накормить его через CDC или ETL и проверить числами до/после, что аналитика ускорилась, а транзакционный путь перестал страдать — приняв ограниченное окно задержки данных ради изолированных хранилища, кэшей и failure domains. Сделав это раз на игрушке, делаешь продакшен-версию мышечной памятью.
</Recap>
