---
concepts:
- row-store
- column-store
- column-pruning
- normalization
- cdc-etl
- htap
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://clickhouse.com/resources/engineering/why-columnar-databases-are-fast
- https://clickhouse.com/resources/engineering/database-compression
- https://clickhouse.com/resources/engineering/unifying-oltp-and-olap
status: ready
summary: "Промпты на свободное воспроизведение по юниту OLTP vs OLAP. Сначала ответьте своими словами, затем откройте модельный ответ и сравните."
title: 'OLTP vs OLAP: тест на свободное воспроизведение'
track: data-engineering
unit: 01-oltp-vs-olap
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. На каждый промпт скажите или запишите полный ответ по памяти, прежде чем открыть модельный, — усилие припоминания и закрепляет разделение нагрузок.
</Hook>

<Goal lang="ru">
Восстановите хребет юнита — почему layout следует за паттерном доступа, три множителя column store, почему индекс не спасёт агрегат, авария на реплике и фикс через разделение хранилищ — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:visible
  id="01-oltp-vs-olap-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему OLTP и OLAP нужны противоположные физические layout-ы и какой каждый?", a: "Паттерн доступа диктует layout. OLTP — это point lookup и мелкие записи целых строк, тысячи в секунду за единицы миллисекунд — ему нужен row store, где все колонки строки лежат на диске непрерывно, так что чтение или запись одного заказа трогает одну страницу. OLAP сканирует и агрегирует несколько колонок по всей таблице, и секунды на запрос — норма — ему нужен column store, где все значения одной колонки лежат непрерывно, так что запрос читает только те колонки, на которые ссылается. Это не две настройки одной ручки: layout, индекс или формат, отличный в одном, структурно плох в другом. Это напряжение — а не конкретный инструмент — и есть то, чем управляет вся остальная инженерия данных." },
    { q: "Назовите три множителя, делающих column store быстрее row store на аналитике, и примерно насколько велик совокупный разрыв.", a: "(1) Column pruning — запрос, трогающий 3 из 50 колонок, читает примерно 6% байтов от row store, потому что каждая колонка хранится отдельно. (2) Сжатие — одна колонка хранит значения одного типа и часто низкой кардинальности, так что dictionary + run-length + LZ4/ZSTD дают 5–10× (30×+ на низкокардинальных колонках) против 1.5–3× для смешанных по типу страниц строки, и меньше данных на диске — меньше I/O. (3) Векторизованное выполнение — движок обрабатывает колонку пакетами по тысячи непрерывных однотипных значений в плотных SIMD-дружественных циклах, а не строка-за-строкой с вызовом функции и ветвлением на строку. Три складываются, поэтому column store обгоняет row store примерно в 10×–1000× на аналитических запросах." },
    { q: "Коллега спрашивает, почему нельзя просто добавить индекс, чтобы 90-дневный агрегат выручки на Postgres стал быстрым. Объясните.", a: "Индекс ускоряет запросы, выбирающие малую долю строк, — он превращает скан в seek. 90-дневному SUM по всем странам нужны почти все строки, так что искать селективно нечего; планировщик справедливо выбирает sequential scan, а индекс лишь добавляет стоимость записи на каждом insert. Индексы чинят selectivity, а не объём скана. Агрегат медленный, потому что row store читает каждый байт каждой строки — все 50 колонок — ради одной колонки, которую суммирует. Настоящий фикс — другой физический layout: column store пруньит до этой одной колонки, читает её сжатой на 5–10× и сканирует векторизованно — на порядки меньше I/O, чего ни один индекс на row store не достигнет." },
    { q: "Почему запуск дашборда аналитики на прод-реплике — авария, ждущая своего часа, и какова правильная архитектура?", a: "Реплика для чтения — всё ещё row store с buffer cache, рассчитанным на транзакционный working set. Длинный аналитический скан делает три разрушительных вещи разом: тянет холодные страницы через shared_buffers и вытесняет горячие OLTP-страницы, так что транзакционные чтения, бывшие попаданиями в память, начинают бить по диску и замедляются в 10–100×; потребляет CPU, память под hash join и пропускную способность I/O, которые zero-sum с транзакционными потоками на том же движке; и, удерживая ресурсы, задерживает применение WAL, поднимая replica lag, так что чтения возвращают устаревшие данные. «Это же просто чтение» — неверно: чтение всей таблицы на аналитику — самый разрушительный вид на транзакционном движке. Фикс архитектурный: отдельный колоночный OLAP-store (Redshift/BigQuery/Snowflake или ClickHouse), непрерывно кормимый из OLTP через CDC (например Debezium, стримящий WAL в Kafka) или батчевый ETL/ELT, дающий независимые хранилище, кэши и failure domains. Цена — пайплайн в эксплуатации и небольшое окно задержки данных — почти всегда того стоит." },
    { q: "Почему OLTP-схема остаётся нормализованной, а OLAP-target денормализован, и почему это не противоречие?", a: "Normalization держит записи OLTP мелкими и консистентными: каждый факт живёт в одном месте, так что UPDATE трогает одну строку и нет аномалий обновления — идеально для запись-тяжёлой транзакционной таблицы. OLAP-target денормализует в широкие fact-таблицы, чтобы аналитические сканы не платили join на каждом запросе в миллионы строк, и сворачивает измерения в низкокардинальные колонки, которые dictionary-encoded и сжимаются почти бесплатно. Это не противоречие, потому что целостность обеспечивается выше, в OLTP, а повторяющиеся низкокардинальные значения column store стоят почти ничего хранить. Каждый layout соответствует своей нагрузке — OLTP не делают похожим на OLAP и наоборот." },
    { q: "Что такое HTAP и почему он не отменяет нужду думать о разделении OLTP/OLAP?", a: "HTAP (hybrid transactional/analytical processing) — единая система, пытающаяся обслуживать и транзакции, и аналитику, обычно держа внутри и row, и column представление и маршрутизируя каждый запрос к нужному. Он реально полезен, когда задержка данных должна быть почти нулевой, а эксплуатация двух систем слишком дорога. Но ни один промышленный HTAP не устраняет конкуренцию за ресурсы между скан-тяжёлой и запись-тяжёлой нагрузкой — он управляет ею (отдельные row и column реплики, resource groups, уровни изоляции), а не отменяет. Так что базовое напряжение то же; HTAP меняет, где вы им управляете, а не существует ли оно. Поэтому разделение на два хранилища, кормимых через CDC/ETL — с независимыми кэшами и failure domains — остаётся дефолтом." },
  ]}
/>

<Recap lang="ru">
Если вы смогли восстановить каждый ответ по памяти, вы держите хребет юнита: паттерн доступа диктует layout (row store для point-записей, column store для сканов); column store выигрывает за счёт pruning, сжатия 5–10× и векторизации, складывающихся в разрыв 10×–1000×, который не закроет ни один индекс; OLTP нормализует, а OLAP денормализует, потому что каждый соответствует своей нагрузке; аналитика на прод-реплике — авария, потому что один скан вытесняет buffer cache, конкурирует за I/O и поднимает lag; а фикс — отдельный колоночный store, кормимый через CDC/ETL, где HTAP управляет конкуренцией — но никогда её не отменяет.
</Recap>
