---
slug: 01-overview
lang: ru
track: data-engineering
unit: 01-oltp-vs-olap
order: 1
level: junior
lessonType: topic
status: ready
title: "OLTP против OLAP: разделение нагрузок, определяющее data engineering"
summary: "Row-store OLTP и column-store OLAP оптимизируют противоположные формы: крошечные индексированные записи против огромных сканов. Аналитика на prod-реплике — классический сбой; фикс — отдельное колоночное хранилище, питаемое ETL/CDC."
prereqs: []
deepensInto: []
spiral: []
concepts: ["row-store", "column-store", "column-pruning", "vectorized-execution", "cdc-etl", "htap"]
estMin: 16
sources:
  - https://clickhouse.com/resources/engineering/why-columnar-databases-are-fast
  - https://clickhouse.com/resources/engineering/database-compression
  - https://aws.amazon.com/blogs/database/improve-postgresql-performance-diagnose-and-mitigate-lock-manager-contention/
  - https://clickhouse.com/resources/engineering/unifying-oltp-and-olap
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
Команда роста выкатывает дашборд: «выручка по странам за последние 90 дней». Он смотрит в prod-реплику Postgres, потому что «это же просто чтение». В 9 утра запрос разворачивает `SUM` по 40M строк `orders`, вытесняет из `shared_buffers` горячие страницы B-дерева, нужные каждому оформлению заказа, и забивает I/O на 30 секунд. Checkout p99 прыгает с 40ms до 2s, лаг реплики растёт до 90s, а дежурного будят с «база лежит». Ничего не легло. Один аналитический скан отравил транзакционную систему. Индекс бы не помог — не было `WHERE`, чтобы сузить выборку; запросу нужна была каждая строка.
</Hook>

<Crux>OLTP хочет крошечные индексированные записи; OLAP — огромные сканы колонок. Один layout не выиграет оба — это и есть data engineering.</Crux>

<Explanation>

## Две нагрузки, две противоположные формы

OLTP — Online Transaction Processing — это база приложения: `INSERT` заказа, `UPDATE` баланса, выборка одного пользователя по id. Паттерн доступа — точечные чтения и мелкие записи по нескольким строкам, тысячи раз в секунду, каждое за единицы миллисекунд. OLAP — Online Analytical Processing — это машина вопросов: средний чек по региону по месяцам, когорты удержания, «какие 1% пользователей дают 40% выручки». Она сканирует миллионы строк, но касается лишь горстки колонок таблицы, и запрос, идущий секунды, — это нормально.

Это не две настройки одной ручки. Они тянут физический дизайн в противоположные стороны. OLTP оптимизирует latency записи и выборку одной строки; OLAP оптимизирует пропускную способность скана по нескольким колонкам всей таблицы. Схема, индекс, формат хранения, отличный в одном, структурно плох в другом. Именно это напряжение — а не какой-то конкретный инструмент — и есть то, чем занимается весь остальной data engineering.

## Row-store против column-store: дело в том, что лежит рядом с чем

Row-store (Postgres, MySQL, большинство OLTP-движков) держит колонки строки на диске подряд: `[id, user, country, total, created_at]` для строки 1, потом вся строка 2, и так далее. Это идеально для OLTP — чтение или запись одного целого заказа касается одной страницы. Но запроси `SUM(total)` по 40M строк, и движок обязан прочитать каждую страницу таблицы, протащив `id`, `user`, `country`, `created_at` через память лишь чтобы добраться до `total`. Ты платишь I/O за 50 колонок, чтобы использовать 1.

Column-store (ClickHouse, Redshift, BigQuery, Snowflake) переворачивает layout: все значения `total` лежат вместе, все значения `country` вместе. Теперь `SUM(total)` читает только колонку `total` — **column pruning**. Запрос, касающийся 3 из 50 колонок, читает примерно 6% байтов, которые прочитал бы row-store. Это одно свойство — большая часть разрыва.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Измерение</th>
        <th class="border border-border px-3 py-2 text-left">Row-store (OLTP)</th>
        <th class="border border-border px-3 py-2 text-left">Column-store (OLAP)</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Физический layout</td>
        <td class="border border-border px-3 py-2">Все колонки строки вместе</td>
        <td class="border border-border px-3 py-2">Все значения колонки вместе</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Хорош в</td>
        <td class="border border-border px-3 py-2">Точечное чтение/запись целых строк</td>
        <td class="border border-border px-3 py-2">Скан + агрегат нескольких колонок</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Цена большого агрегата</td>
        <td class="border border-border px-3 py-2">Читает каждый байт каждой строки</td>
        <td class="border border-border px-3 py-2">Читает только нужные колонки (~6%)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Сжатие</td>
        <td class="border border-border px-3 py-2">~1.5–3× (смешанные типы на странице)</td>
        <td class="border border-border px-3 py-2">~5–10× (один тип в колонке)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Запись одной строки</td>
        <td class="border border-border px-3 py-2">Дёшево — одна строка, одно место</td>
        <td class="border border-border px-3 py-2">Дорого — касается каждого файла колонки</td>
      </tr>
    </tbody>
  </table>
</div>

## Почему колоночное сжимается в 5–10× и работает векторизованно

Хранить одну колонку вместе — значит хранить рядом значения одного типа, часто низкой кардинальности, и это сжимается куда лучше, чем мешанина строки из int, string, timestamp, float. Колонка `country` — это несколько сотен различных строк, повторённых миллионы раз: dictionary-кодируешь их в маленькие int, затем run-length-кодируешь прогоны, затем LZ4/ZSTD сверху. Row-store обычно достигают 1.5–3×; column-store берут 5–10× на аналитических данных и 30×+ на колонках низкой кардинальности. На 100M-строчном ClickBench ClickHouse держит датасет в ~9 GiB там, где Postgres нужно ~100 GiB. Меньше данных на диске — меньше I/O, а I/O — это узкое место для сканов, поэтому разрыв в сжатии *расширяет* разрыв в пропускной способности до 50–100×.

Второй множитель — **vectorized execution**. Row-движок обрабатывает запрос строка-за-строкой: на каждую строку — вызов функции, проверка типа, ветвление. Column-движок обрабатывает колонку батчами по тысячи подряд идущих значений одного типа, поэтому CPU крутит плотные циклы по массивам, дружественные к SIMD, с предсказуемыми ветвлениями и тёплым кэшем. Та же арифметика, на порядок меньше накладных расходов на значение. Pruning, сжатие и векторизация складываются: column-store обгоняют row-store в 10×–1000× на аналитических запросах — и «добавь индекс» этого не закроет, потому что у агрегата по всей таблице нечего искать.

<Inset kind="why" lang="ru">
«Просто добавь индекс» — инстинкт, который тут проваливается, и стоит знать почему именно. Индекс превращает скан в seek, когда запрос выбирает *малую долю* строк. Аналитический агрегат — `SUM`, `AVG`, `COUNT(*)`, свёртка за 90 дней — нуждается в *большинстве или всех* строках, поэтому планировщик правильно игнорирует индекс и делает последовательный скан; индекс лишь добавил бы цену записи. Индексы чинят селективность, а не объём скана. Фикс объёма скана — layout, читающий меньше байтов на строку: column-store.
</Inset>

## Режим отказа: аналитика на prod OLTP-реплике

Hook — это режим отказа сеньорского уровня целиком. Read-реплика — всё ещё row-store с буферным кэшем, рассчитанным на транзакционный рабочий набор. Направь на неё долгий аналитический скан, и сразу пойдёт не так три вещи. Первое — **вытеснение буферного кэша**: скан тянет холодные страницы через `shared_buffers` и вытесняет горячие OLTP-страницы, поэтому транзакционные запросы, что были попаданиями в память, начинают бить в диск и тормозят в 10–100×. Второе — **конкуренция за блокировки и ресурсы**: устойчивый CPU, память под hash join и пропускная способность I/O — игра с нулевой суммой; аналитический запрос морит голодом транзакционные потоки на том же движке. Третье — **лаг реплики**: долгий запрос, держащий ресурсы, задерживает применение потока WAL, поэтому реплика отстаёт от primary, и всё, что её читает, отдаёт устаревшие данные.

Фикс архитектурный, а не флаг тюнинга: дай аналитике свой дом. Подними отдельное OLAP-хранилище (колоночный warehouse — Redshift/BigQuery/Snowflake — или ClickHouse) и питай его из OLTP непрерывно через **CDC** (change data capture, например Debezium, стримящий WAL в Kafka) или батчевый **ETL/ELT**. У двух систем теперь независимое хранилище, независимые кэши, независимые домены отказа. Дашборд бьёт в warehouse; checkout этого не чувствует. Цена, которую ты берёшь на себя, — это pipeline в эксплуатацию и от секунд до минут data latency, почти всегда верный обмен.

<TradeoffMatrix
  id="01-oltp-vs-olap-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Продукт хочет дашборд «выручка по странам за 90 дней» по таблице orders на 40M строк в prod Postgres. Выбери архитектуру."
  options={[
    { name: "Поднять колоночный warehouse/ClickHouse, питать его из Postgres через CDC или ETL, направить дашборд туда", summary: "Отдельное OLAP-хранилище.", correct: true, justification: "Это стандартный ответ, потому что он физически разделяет нагрузки. Warehouse колоночный, поэтому 90-дневный SUM прунит до нужных колонок, сканирует сжатые данные (в 5–10× меньше) и работает векторизованно — секунды, с нулевым влиянием на checkout. OLTP оставляет себе row-store и тёплый буферный кэш; OLAP получает свои кэши, CPU и домен отказа. Цена — CDC/ETL pipeline в эксплуатацию и лаг данных от секунд до минут, который дашборд легко терпит." },
    { name: "Гонять запрос дашборда против prod read-реплики — это же чтение, реплики и существуют для чтения", summary: "Использовать реплику.", justification: "Это ровно тот сбой из Hook. Реплика — всё ещё row-store: скан читает каждый байт 40M строк, вытесняет горячие OLTP-страницы из shared_buffers (поэтому checkout начинает бить в диск), конкурирует за CPU/I/O с применением репликации и гонит лаг реплики вверх. «Это же просто чтение» игнорирует, что аналитическое чтение всей таблицы — самый разрушительный вид на транзакционном движке." },
    { name: "Добавить составной индекс на (country, created_at), чтобы агрегат использовал индекс", summary: "Заиндексировать.", justification: "Индексы чинят селективность, а не объём скана. 90-дневный агрегат по всем странам нуждается в большинстве строк, поэтому планировщик всё равно игнорирует индекс и делает последовательный скан — ты добавил цену записи без выгоды для чтения. Нечего искать, когда надо прочитать большую часть таблицы." },
    { name: "Кэшировать результат дашборда в Redis и пересчитывать его раз в час с реплики", summary: "Кэшировать вывод.", justification: "Кэширование прячет симптом для зрителей, но почасовой пересчёт всё равно гоняет разрушительный полный скан на реплике — checkout всё равно деградирует раз в час, а кэш между прогонами устаревший. Это лечит проблему layout хранения как проблему отображения. Под капотом всё равно нужно настоящее OLAP-хранилище." },
  ]}
/>

<Quiz
  id="01-oltp-vs-olap-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Запрос считает SUM(total) по 40M строк, но читает только колонку total. Почему column-store здесь громит row-store?"
  choices={[
    { label: "Column pruning: читает только колонку total (~6% байтов), затем сканирует её сжатой и векторизованно", correct: true },
    { label: "У него лучше индекс на total, превращающий скан в seek", misconception: "Агрегат по всей таблице касается большинства строк, поэтому искать нечего — планировщик всё равно делает скан. Выигрыш — в чтении меньшего числа колонок, а не в индексе." },
    { label: "Column-store держат всю таблицу в RAM, а row-store нет", misconception: "Ни один не определяется кэшированием всего; структурный выигрыш — это layout на диске (одна колонка подряд), дающий pruning, сжатие и векторизованные сканы." },
    { label: "Он быстрее пишет одиночные строки, поэтому и чтения быстрее", misconception: "Column-store медленнее на записи одной строки, а не быстрее. Преимущество — на сканах колонок, совсем другая ось." },
  ]}
/>

<Quiz
  id="01-oltp-vs-olap-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Дашборд аналитики, направленный на prod OLTP read-реплику, вызывает скачок checkout p99. Какую первопричину сеньор называет первой?"
  choices={[
    { label: "Аналитический скан вытесняет горячие OLTP-страницы из shared_buffers и конкурирует за I/O, поэтому транзакционные чтения бьют в диск, а лаг реплики растёт", correct: true },
    { label: "Реплике не хватает индекса, нужного дашборду", misconception: "Добавление индекса не помогает агрегату по всей таблице, а симптом — замедление OLTP от вытеснения кэша и конкуренции за ресурсы, а не медленный запрос дашборда." },
    { label: "Реплики только для чтения, поэтому записи встают в очередь за дашбордом", misconception: "Дашборд работает на реплике, не на primary; записи идут в primary. Урон — это вытеснение буферного кэша и конкуренция за ресурсы на реплике плюс лаг репликации." },
    { label: "Пул соединений слишком мал", misconception: "Размер пула — не механизм: даже один аналитический скан отравляет буферный кэш и конкурирует за I/O на row-store, рассчитанном на транзакционные рабочие наборы." },
  ]}
/>

<DragOrder
  id="01-oltp-vs-olap-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь шаги, чтобы правильно убрать тяжёлую аналитику с prod OLTP-базы:"
  items={[
    { id: "a", label: "Распознать, что запрос — это OLAP: скан/агрегат по всей таблице, а не селективная выборка, которую чинит индекс" },
    { id: "b", label: "Поднять отдельное OLAP-хранилище (колоночный warehouse или ClickHouse) со своим хранилищем и кэшами" },
    { id: "c", label: "Питать его из OLTP непрерывно через CDC (например Debezium → Kafka) или батчевый ETL/ELT" },
    { id: "d", label: "Направить дашборды/аналитиков в OLAP-хранилище, оставив буферный кэш OLTP под транзакции" },
    { id: "e", label: "Принять лаг данных от секунд до минут как обмен на изолированный домен отказа" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Row-store OLTP выигрывают точечные чтения/записи; column-store OLAP — сканы через pruning, сжатие 5–10× и векторизацию. Не гоняй аналитику на prod-реплике — питай колоночное хранилище через CDC/ETL.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-oltp-vs-olap-retrieval"
  lang="ru"
  questions={[
    { q: "Коллега спрашивает, почему нельзя просто добавить индекс, чтобы 90-дневный агрегат выручки летал на Postgres. Объясни.", a: "Индекс ускоряет запросы, выбирающие малую долю строк — он превращает скан в seek. 90-дневный SUM по всем странам нуждается в большинстве или всех строках, поэтому искать нечего; планировщик правильно выбирает последовательный скан, а индекс просто лежит, добавляя цену записи на каждый insert/update. Индексы чинят селективность, а не объём скана. Агрегат медленный, потому что row-store обязан прочитать каждый байт каждой строки (все 50 колонок), чтобы добраться до одной колонки, которую он суммирует. Настоящий фикс — другой физический layout: column-store держит каждую колонку подряд, поэтому запрос прунит до одной колонки, читает её сжатой в 5–10× и сканирует векторизованно — на порядки меньше I/O и CPU, и ни один индекс не достигнет этого на row-store." },
    { q: "Почему гонять дашборд аналитики на prod read-реплике — это сбой, ждущий своего часа, и какая архитектура правильная?", a: "Read-реплика — всё ещё row-store с буферным кэшем, рассчитанным на транзакционный рабочий набор. Долгий аналитический скан делает три вредные вещи разом: тянет холодные страницы через shared_buffers и вытесняет горячие OLTP-страницы, поэтому транзакционные чтения, что были попаданиями в память, начинают бить в диск и тормозят в 10–100×; потребляет CPU, память под hash join и пропускную способность I/O — игра с нулевой суммой с транзакционными потоками на том же движке; и, держа ресурсы, задерживает применение WAL, гоня лаг реплики вверх, так что чтения возвращают устаревшие данные. «Это же просто чтение» неверно — аналитическое чтение всей таблицы самый разрушительный вид на транзакционном движке. Правильная архитектура — дать аналитике свой дом: отдельное колоночное OLAP-хранилище (Redshift/BigQuery/Snowflake или ClickHouse), питаемое из OLTP непрерывно через CDC (например Debezium, стримящий WAL в Kafka) или батчевый ETL/ELT. У двух систем тогда независимые хранилище, кэши и домены отказа; дашборд бьёт в warehouse, а checkout этого не чувствует. Цена — pipeline в эксплуатацию и небольшое окно лага данных, почти всегда оно того стоит." },
  ]}
/>

<Recap lang="ru">
OLTP и OLAP — две нагрузки с противоположными физическими нуждами: OLTP хочет крошечные индексированные чтения и записи целых строк, поэтому использует row-store, где колонки строки лежат подряд; OLAP хочет сканировать и агрегировать несколько колонок по всей таблице, поэтому использует column-store, где подряд лежит каждая колонка. Этот layout даёт column-store три множителя — column pruning (читать ~6% байтов для запроса, касающегося 3 из 50 колонок), сжатие 5–10×, потому что значения одного типа пакуются плотно, и векторизованное батчевое исполнение, — которые вместе делают аналитические запросы быстрее в 10×–1000×, и этот разрыв не закроет ни один индекс, потому что у агрегата по всей таблице нечего искать. Классический сеньорский сбой — гонять тяжёлую аналитику на prod OLTP-реплике, где один скан вытесняет горячий буферный кэш, конкурирует за I/O и CPU и гонит лаг реплики, превращая «это же просто чтение» в сбой checkout. Фикс архитектурный: отдельное колоночное OLAP-хранилище, питаемое из OLTP через CDC или ETL, с принятием небольшого лага данных в обмен на изолированные хранилище, кэши и домены отказа. HTAP-системы, пытающиеся обслужить обе на одном движке, существуют, но ни один production HTAP не устраняет конкуренцию за ресурсы — ею можно управлять, не избежать, поэтому раздел на два хранилища остаётся дефолтом.
</Recap>
