---
concepts:
- embeddings
- approximate-nearest-neighbor
- hnsw
- ivf-pq
- recall-latency-tradeoff
- hybrid-search
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://github.com/pgvector/pgvector
- https://www.crunchydata.com/blog/hnsw-indexes-with-postgres-and-pgvector
- https://dev.to/franckpachot/no-pre-filtering-in-pgvector-means-reduced-ann-recall-1aa1
- https://weaviate.io/blog/hybrid-search-explained
spiral: []
status: ready
summary: Семантический поиск ранжирует эмбеддинги по расстоянию, но точный kNN — это
  O(N·d) и умирает на масштабе. ANN-индексы вроде HNSW покупают скорость ценой recall
  — а неверный ef-параметр тихо роняет recall, хотя запрос всё равно возвращает десять
  строк.
title: 'Векторный поиск: треугольник recall–latency–память за RAG'
track: data-engineering
unit: 07-vectors
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
На демо RAG отвечал идеально. В проде поддержка говорит, что бот «не находит» документы, которые очевидно есть. Ничего не падает — каждый запрос возвращает свои десять чанков, ранжированных, быстро. Подвох: команда выкатила HNSW-индекс, но так и не подняла `hnsw.ef_search` с дефолтных 40, а потом прикрутила фильтр `WHERE tenant_id = ?`. Recall тихо упал до ~30%. Система уверенно возвращала не те десять строк, и ни один лог, ни метрика, ни исключение об этом не сказали.
</Hook>

<Crux>ANN-поиск тихо меняет recall на скорость — при неверном ef всё равно вернутся десять строк, просто не те десять.</Crux>

<Explanation>

За следующие пятнадцать минут ты поймёшь, почему падение recall невидимо, какие ручки индекса двигают какое ребро треугольника и что измерить до следующего деплоя RAG.

## Эмбеддинги — это точки; поиск — это расстояние

Модель эмбеддингов превращает текст (или картинку) в вектор фиксированной длины — 768 измерений у многих открытых моделей, 1536 у OpenAI `text-embedding-3-small`, 3072 у большой. Семантически близкие входы оказываются рядом в этом пространстве, поэтому «найти релевантные документы» превращается в «найти ближайшие векторы к вектору запроса». Близость — это метрика: косинусная схожесть (угол), скалярное/inner product или L2 (евклидово) расстояние. Для нормализованных эмбеддингов косинус и dot ранжируют одинаково, поэтому inner product используют как более дешёвый эквивалент.

Наивный ответ — точный k-nearest-neighbor: сравнить запрос с каждым хранимым вектором и взять top k. Это O(N·d) на запрос — при 10M векторов × 1536 измерений это ~15 миллиардов умножений-сложений на один поиск. Нормально для 50k строк, фатально для 50M. Поэтому brute-force `ORDER BY embedding <-> $1 LIMIT 10` без индекса корректен, но не выкатываем дальше пары сотен тысяч строк.

<DiagramFrame
  label="2-D набросок пространства эмбеддингов: вектор запроса находится среди векторов документов, и возвращаются top-k ближайших по косинусному или скалярному расстоянию; далёкие векторы семантически нерелевантны и исключаются."
  hue="var(--d-data)"
  caption="Поиск — это близость. Запрос эмбеддится в то же пространство, что и документы; возвращаются k ближайших векторов по косинусу или dot-расстоянию. Далёкие векторы нерелевантны. ANN приближает этот top-k, чтобы не сканировать каждую точку."
>
  <svg viewBox="0 0 520 300" font-family="var(--font-body)">
    <rect x="40" y="30" width="440" height="220" rx="8" fill="var(--card-2)" stroke="var(--hairline-2)" stroke-width="0.8" />
    <text x="52" y="24" font-size="11" font-family="var(--font-mono)" fill="var(--muted)">пространство эмбеддингов (768–3072 измерений, показано как 2-D)</text>

    <circle cx="250" cy="150" r="66" fill="none" stroke="var(--accent)" stroke-width="1" stroke-dasharray="4 4" opacity="0.7" />
    <text x="250" y="78" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--accent)">радиус top-k</text>

    <line x1="250" y1="150" x2="215" y2="120" stroke="var(--accent)" stroke-width="1.2" opacity="0.8" />
    <line x1="250" y1="150" x2="290" y2="135" stroke="var(--accent)" stroke-width="1.2" opacity="0.8" />
    <line x1="250" y1="150" x2="235" y2="195" stroke="var(--accent)" stroke-width="1.2" opacity="0.8" />
    <line x1="250" y1="150" x2="300" y2="185" stroke="var(--accent)" stroke-width="1.2" opacity="0.8" />
    <text x="320" y="128" font-size="9.5" font-family="var(--font-mono)" fill="var(--accent)">косинус / dot</text>

    <circle cx="215" cy="120" r="6" fill="var(--accent)" />
    <circle cx="290" cy="135" r="6" fill="var(--accent)" />
    <circle cx="235" cy="195" r="6" fill="var(--accent)" />
    <circle cx="300" cy="185" r="6" fill="var(--accent)" />
    <text x="250" y="240" text-anchor="middle" font-size="10.5" font-family="var(--font-mono)" fill="var(--accent)">top-k ближайших (возвращаются)</text>

    <circle cx="90" cy="60" r="5" fill="var(--muted)" opacity="0.55" />
    <circle cx="430" cy="70" r="5" fill="var(--muted)" opacity="0.55" />
    <circle cx="110" cy="230" r="5" fill="var(--muted)" opacity="0.55" />
    <circle cx="450" cy="225" r="5" fill="var(--muted)" opacity="0.55" />
    <circle cx="380" cy="250" r="5" fill="var(--muted)" opacity="0.55" />
    <text x="430" y="92" text-anchor="middle" font-size="9.5" font-family="var(--font-mono)" fill="var(--muted)">далёкие (нерелевантные)</text>

    <circle cx="250" cy="150" r="8" fill="var(--ink)" stroke="var(--card-2)" stroke-width="1.5" />
    <text x="250" y="170" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--ink)">вектор запроса</text>
  </svg>
</DiagramFrame>

## ANN: меняем recall на скорость

Зачем вообще думать о recall раньше любой другой настройки? Потому что когда recall падает, ни один монитор тебе об этом не скажет — ни ошибка, ни алерт, ни медленный запрос. Узнаешь только когда пользователь пожалуется, что бот «не находит» документ, который очевидно есть.

Approximate nearest neighbor (ANN, приближённый поиск ближайших соседей) индексы отказываются от гарантии найти *истинный* top-k в обмен на сублинейный поиск. Метрика качества — **recall@k**: из истинного top-10 сколько индекс реально вернул? Recall 1.0 — идеально; 0.9 — один из каждых десяти «лучших» результатов потерян. Инсайт сеньора: recall — это *регулятор настройки*, а не фиксированное свойство, и, что критично, **запрос на recall 0.3 выглядит идентично запросу на recall 1.0**: то же число строк, тот же профиль latency, то же отсутствие ошибок. Поймать это можно только измеряя recall против точного бейзлайна.

Доминирующий индекс — **HNSW** (Hierarchical Navigable Small World — иерархически навигируемый мир малых связей). Он строит слоистый граф: разреженные дальние связи наверху для быстрых прыжков по пространству, плотные короткие связи внизу для точности. Запрос входит сверху, жадно идёт к вектору запроса и спускается. Три ручки управляют треугольником:

- **`M`** — связей на узел (дефолт pgvector 16). Выше M → лучше recall и больше, плотнее граф. Сам граф HNSW занимает **2–5× размера сырых векторов** в RAM, и M — главный рычаг на это.
- **`ef_construction`** — размер пула кандидатов при построении (дефолт 64). Выше → лучше качество графа и recall, но время сборки растёт примерно линейно; 400 против 200 — примерно в 2× дольше.
- **`ef_search`** — размер пула кандидатов в момент запроса (дефолт **40** в pgvector). Это runtime-регулятор recall/latency. Подъём со 100 до 500 может сдвинуть recall с ~85% до ~98% ценой роста с ~1мс до ~5мс на запрос.

Вместе эти три ручки задают треугольник recall–latency–память: M устанавливает бюджет памяти, ef_construction фиксирует качество графа при сборке, а ef_search — единственный регулятор, который можно крутить в проде без пересборки. Когда видишь необъяснимую деградацию качества ответов, начинай именно с ef_search.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Индекс</th>
        <th class="border border-border px-3 py-2 text-left">Память</th>
        <th class="border border-border px-3 py-2 text-left">Recall</th>
        <th class="border border-border px-3 py-2 text-left">Сборка / записи</th>
        <th class="border border-border px-3 py-2 text-left">Выбирай, когда</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>HNSW</code></td>
        <td class="border border-border px-3 py-2">Высокая (2–5× сырых)</td>
        <td class="border border-border px-3 py-2">Наивысший, легче добиться</td>
        <td class="border border-border px-3 py-2">Медленная сборка, дешёвые инкрементальные вставки</td>
        <td class="border border-border px-3 py-2">Дефолт для RAG; данные меняются; recall важен</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>IVFFlat</code></td>
        <td class="border border-border px-3 py-2">~1× сырых</td>
        <td class="border border-border px-3 py-2">Хороший, дрейфует при изменениях</td>
        <td class="border border-border px-3 py-2">Быстрая сборка, нужны данные заранее</td>
        <td class="border border-border px-3 py-2">Большой статичный набор, RAM в обрез</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>IVF-PQ</code></td>
        <td class="border border-border px-3 py-2">Наименьшая (сжатие ~7×)</td>
        <td class="border border-border px-3 py-2">Ниже (~70%), восстановим</td>
        <td class="border border-border px-3 py-2">Сборка + обучение codebook</td>
        <td class="border border-border px-3 py-2">100M+ векторов не влезают в RAM</td>
      </tr>
    </tbody>
  </table>
</div>

## IVF и IVF-PQ: кластеризуем, потом квантуем

Другое семейство — **IVF** (inverted file). Он k-means-кластеризует все векторы в `lists` бакетов; запрос сканирует только `probes` ближайших бакетов, а не всё. В pgvector это `ivfflat` с `lists` при сборке и `ivfflat.probes` в запросе (дефолт **1** — слишком мало для хорошего recall). Память IVF близка к 1× сырых векторов, но есть острый край: кластеры фиксированы при сборке, поэтому по мере вставки новых данных центроиды устаревают и recall **дрейфует**, пока не переиндексируешь. У HNSW такого дрейфа нет — вставки в граф константной стоимости и держат качество.

**IVF-PQ** добавляет product quantization (векторное квантование по блокам): разбивает каждый вектор на под-векторы и заменяет каждый id ближайшего центроида из маленького codebook, сжимая 1536-мерный float32-вектор (6 КБ) до пары десятков байт. NVIDIA показала 360 ГиБ FP32-векторов, сжатых до 54 ГиБ, удерживая >95% recall на миллиарде векторов. Цена — потеря recall из-за ошибки квантования: IVF-PQ сам по себе часто оказывается около **70% от recall HNSW** и требует rerank-шага (пересчитать top-кандидатов по полноточным расстояниям), чтобы восстановить. PQ — это инструмент, когда 100M+ векторов просто не влезают в RAM, а не дефолт.

<Inset kind="why" lang="ru">
«Проклятие размерности» — вот почему это вообще сложно. В 1536 измерениях почти каждая пара случайных точек примерно равноудалена — расстояния концентрируются, и *контраст* между ближайшим соседом и сотым становится тонким. Именно это делает recall хрупким: чуть слишком жадный обход графа или слишком грубый квантователь не отличат настоящего соседа от near-miss, а приближённые методы отказываются от гарантии именно потому, что точное разделение стоит O(N·d).
</Inset>

## Фильтрация и hybrid search: где ломается прод

Две продовые ловушки. Первая — **фильтрация по метаданным плохо сочетается с ANN**. pgvector делает post-filtering: обходит граф HNSW на `ef_search` кандидатов, *потом* применяет твой `WHERE tenant_id = ?`. Если фильтр оставляет 1% строк, а `ef_search` равен 40, можно остаться почти ни с чем — граф навигировал по крошечному, почти разорванному подграфу, и recall рушится. pgvector 0.8.0 добавил `hnsw.iterative_scan` (`strict_order` / `relaxed_order`), чтобы продолжать вытягивать кандидатов, пока фильтр не удовлетворён, что возвращает latency ради потерянного recall.

Вторая — **чистый векторный поиск пропускает точные совпадения**. Запрос по коду ошибки, SKU или имени функции — это лексическая потребность, а эмбеддинги размазывают её в «семантически близкое» — поэтому `panic: nil pointer` может оказаться ниже смутно связанного абзаца. Фикс — **hybrid search** (гибридный поиск): запускать BM25 (keyword-поиск по обратному индексу слов) и вектор параллельно и сливать через Reciprocal Rank Fusion (RRF — взаимное ранговое слияние, `RRF = Σ 1/(k + rank)`, k≈60), который объединяет по позиции в ранге, так что две несовместимые шкалы оценок никогда не сравниваются напрямую. Команды сообщают о прыжке с ~65–78% до ~91% recall@10 за счёт добавления keyword-ноги.

<TradeoffMatrix
  id="07-vectors-overview-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="RAG-сервис над ~5M чанков получает непрерывную загрузку, требует высокого recall и имеет запас RAM. Выбери стратегию индекса."
  options={[
    { name: "HNSW с настроенным ef_search, измеряя против точного бейзлайна", summary: "Граф-индекс, регулируй recall.", correct: true, justification: "HNSW здесь правильный дефолт: даёт наивысший recall с наименьшей настройкой, а его вставки константной стоимости, поэтому непрерывная загрузка не деградирует граф так, как дрейфуют кластеры IVF. С запасом RAM можно позволить 2–5× overhead графа, а подъём hnsw.ef_search с дефолтных 40 даёт менять latency на recall по требованию. Несгибаемое правило — измерять recall@10 против точного ORDER BY скана, чтобы тихое падение было видно." },
    { name: "IVFFlat с большим значением lists, собранный один раз", summary: "Бакеты кластеров.", justification: "IVFFlat влезает в ~1× сырых векторов, что заманчиво, но в условии — непрерывная загрузка: центроиды IVF фиксированы при сборке, поэтому новые данные стабильно уводят recall вниз, пока не переиндексируешь. Ему также нужны репрезентативные данные до сборки. С доступной RAM и требованием высокого recall ты выбираешь индекс, который дрейфует и проигрывает HNSW без нужного тебе выигрыша по памяти." },
    { name: "IVF-PQ, чтобы сжать векторы как можно сильнее", summary: "Жёстко квантовать.", justification: "PQ — для случая, когда векторы вообще не влезают в RAM (100M+). Здесь набор ~5M и RAM есть, поэтому платить recall-потерей PQ — часто около 70% от recall HNSW и с нуждой в rerank-проходе — значит покупать ненужное сжатие, ухудшая recall, который явно требуется." },
    { name: "Brute-force точный kNN, без индекса", summary: "Сканировать всё.", justification: "Точный kNN даёт recall 1.0, но это O(N·d): при 5M чанков × 1536 измерений каждый запрос — миллиарды умножений-сложений, так что p99 latency не выкатываем. Точный скан — правильный инструмент только как офлайн-бейзлайн, против которого ты измеряешь recall ANN, а не как путь обслуживания." },
  ]}
/>

<Quiz
  id="07-vectors-overview-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Твой поиск на HNSW возвращает десять ранжированных строк за 2мс, без ошибок — но ответы слегка неверны. Что проверить первым?"
  choices={[
    { label: "Recall@k против точного ORDER BY бейзлайна — низкий ef_search тихо возвращает не те десять строк", correct: true },
    { label: "База упала", misconception: "Она возвращает десять строк за 2мс без ошибок — с БД всё в порядке. Провал ANN невидим: низкий recall выглядит ровно как высокий по числу строк и latency." },
    { label: "Latency API модели эмбеддингов", misconception: "Latency 2мс и запросы успешны; симптом — неверные результаты, а не медленные. Это указывает на recall, а не на вызов эмбеддинга." },
    { label: "Добавить реплик, чтобы масштабировать throughput", misconception: "Дело не в throughput — в корректности. Масштабирование реплицирует тот же индекс с низким recall, умножая неверные ответы." },
  ]}
/>

<Quiz
  id="07-vectors-overview-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Пользователи ищут точную строку ошибки 'ECONNREFUSED', и чистый векторный поиск хоронит совпадающий документ. Лучший фикс?"
  choices={[
    { label: "Hybrid search: BM25 по ключевым словам + вектор параллельно, слить через Reciprocal Rank Fusion", correct: true },
    { label: "Поднять ef_search до 1000", misconception: "Подъём ef_search улучшает векторный recall, но проблема лексическая — эмбеддинги размазывают точные токены вроде кода ошибки в «семантически близкое», так что больше кандидатов не того сорта не вытащат буквальное совпадение." },
    { label: "Переэмбеддить всё моделью побольше", misconception: "Большая модель эмбеддингов всё равно меряет семантическую схожесть; она не даёт точечной точности по токенам. SKU, коды ошибок и идентификаторы — это keyword-потребность, которую закрывает BM25." },
    { label: "Сменить метрику расстояния с косинуса на L2", misconception: "Выбор метрики чуть меняет ранжирование, но не фундаментальный разрыв: векторному поиску не хватает точности точного совпадения независимо от метрики. Hybrid добавляет keyword-ногу, которая это даёт." },
  ]}
/>

<DragOrder
  id="07-vectors-overview-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь, как запрос HNSW находит ближайших соседей и как ты бы его настраивал:"
  items={[
    { id: "a", label: "Эмбеддишь запрос в то же векторное пространство, что и хранимые документы" },
    { id: "b", label: "Входишь в граф HNSW на верхнем разреженном слое для быстрых дальних прыжков" },
    { id: "c", label: "Жадно идёшь к вектору запроса, спускаясь в более плотные слои" },
    { id: "d", label: "Собираешь ef_search кандидатов на нижнем слое и возвращаешь top k по расстоянию" },
    { id: "e", label: "Меряешь recall@k против точного бейзлайна; поднимаешь ef_search, если recall низкий" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>ANN меняет recall на скорость, и низкий recall невидим — то же число строк, та же latency. Настраивай ef_search и меряй recall@k. HNSW для меняющихся данных, IVF-PQ когда не влезает в RAM, hybrid для точных совпадений.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-vectors-overview-retrieval"
  lang="ru"
  questions={[
    { q: "Почему ANN-индекс с низким recall так опасен в проде и как его реально обнаружить?", a: "Потому что провал полностью тихий. Запрос на recall 0.3 возвращает те же десять ранжированных строк, за те же несколько миллисекунд, без ошибки, без предупреждения и без аномальной метрики — он просто возвращает не те десять. Классические провалы корректности кидают исключения или дают всплеск latency; этот выглядит идентично здоровому запросу, поэтому проходит staging и всплывает лишь как смутные жалобы «бот не находит» через недели. Триггер обычно — слишком низкий hnsw.ef_search (дефолт pgvector 40), часто усугублённый селективным WHERE-фильтром, который post-filtering разрывает. Обнаруживаешь, измеряя recall@k напрямую: берёшь выборку запросов, гоняешь их точным сканом ORDER BY embedding <-> $1 LIMIT k (без индекса) для ground truth, гоняешь те же запросы через ANN-индекс и считаешь пересечение. Это число recall — не latency, не число строк — метрика, на которую ты алертишь. Без этого бейзлайна летишь вслепую." },
    { q: "Когда тянуться к IVF-PQ вместо HNSW и какую цену принимаешь?", a: "К IVF-PQ тянешься, когда корпус просто не влезает в RAM как полноточные векторы — порядка 100M–1B+ векторов. Граф HNSW занимает 2–5× размера сырых векторов, так что миллиард 1536-мерных float32-векторов (уже ~6 КБ каждый, терабайты в сыром виде) безнадёжно держать резидентно. Product quantization разбивает каждый вектор на под-векторы и заменяет каждый id центроида из маленького codebook, сжимая драматически — NVIDIA сообщила о 360 ГиБ до 54 ГиБ на миллиарде векторов. Цена, которую принимаешь, — recall: квантование выбрасывает точность, поэтому IVF-PQ сам часто оказывается около 70% от recall HNSW. Большую часть отыгрываешь rerank-шагом — достаёшь больший набор кандидатов сжатым индексом, затем пересчитываешь их по полноточным расстояниям — и подъёмом probes для скана большего числа кластеров, ценой latency. Так что IVF-PQ — это выбор по памяти: меняешь recall и добавляешь rerank-стадию, чтобы сделать поиск на терабайтном масштабе доступным, а не дефолт для повседневного RAG." },
  ]}
/>

<Recap lang="ru">
Векторный поиск превращает «найти релевантный контент» в «найти ближайшие эмбеддинги», ранжированные по косинусу, dot или L2-расстоянию. Точный kNN корректен, но O(N·d), поэтому дальше пары сотен тысяч строк переходишь на approximate nearest neighbor и принимаешь обмен recall на скорость. HNSW — дефолт: слоистый граф, настраиваемый M и ef_construction при сборке и ef_search в запросе, стоящий 2–5× сырых векторов в RAM, но дающий наивысший recall с наименьшим дрейфом при изменениях данных. IVF кластеризует, а IVF-PQ сжимает — последний для корпусов, слишком больших для RAM, ценой recall (≈70% от HNSW), который восстанавливаешь reranking. Дисциплина сеньора — это треугольник recall, latency и памяти, и одна привычка важнее всех: падения recall тихие — то же число строк и latency — поэтому меряешь recall@k против точного бейзлайна, поднимаешь ef_search осознанно, следишь за селективными фильтрами по метаданным, которые post-filtering уничтожает (используй iterative scan), и добавляешь hybrid BM25 + вектор с rank fusion, когда точные совпадения важны. Теперь, когда встретишь RAG-систему с уверенными, но слегка неверными ответами, первый вопрос звучит не «достаточно ли хороша модель?» — а «какой у нас recall@k против точного бейзлайна?»
</Recap>
