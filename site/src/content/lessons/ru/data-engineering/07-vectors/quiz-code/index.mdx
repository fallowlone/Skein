---
concepts:
- hnsw-params
- pgvector-query
- distance-operators
- ivf-probes
- recall-measurement
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://github.com/pgvector/pgvector
- https://www.crunchydata.com/blog/hnsw-indexes-with-postgres-and-pgvector
status: ready
summary: "Читай реальный pgvector DDL и запросы — предсказывай поведение recall/latency и выбирай фикс с наибольшим рычагом для HNSW-параметров, операторов расстояния и фильтрованного поиска."
title: 'Vector search: чтение кода и запросов'
track: data-engineering
unit: 07-vectors
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги vector search живут в DDL и в запросе, а не в исключении. Читай SQL, предсказывай recall и latency, которые он даёт, затем выбирай фикс, который senior-инженер делает первым.
</Hook>

<Goal lang="ru">
Отработай цикл, который запускаешь в каждом recall-инциденте: читай определение индекса и запрос, предсказывай, где утекает recall или взлетает latency, и берись за фикс с наибольшим рычагом.
</Goal>

## Сниппет 1 — HNSW-индекс и ручка по умолчанию

```sql
CREATE INDEX ON docs USING hnsw (embedding vector_cosine_ops)
  WITH (m = 16, ef_construction = 64);

-- путь запроса, на каждый запрос
SELECT id, body FROM docs
ORDER BY embedding <=> $1
LIMIT 10;
```

<Quiz
  id="07-vectors-code-1"
  lessonSlug="07-vectors"
  lang="ru"
  question="Индекс строится нормально, запросы быстрые, но recall@10 измеряется лишь ~80%. Фикс с наибольшим рычагом?"
  choices={[
    { label: "Поднять hnsw.ef_search на запросе (SET hnsw.ef_search = 200) — по умолчанию 40, это рантайм-ручка recall. Больший пул кандидатов поднимает recall к 0.95+ ценой пары мс, без перестройки.", correct: true },
    { label: "Удалить и перестроить индекс с m = 64.", misconception: "Бо́льший m поднимает recall и память, но требует полной перестройки и всё равно оставляет ef_search на 40. Дешёвый немедленный рычаг — рантайм ef_search, а не дорогая перестройка." },
    { label: "Сменить оператор с <=> на <-> ради точности.", misconception: "<=> это cosine-расстояние, а <-> это L2; индекс построен с vector_cosine_ops, поэтому смена оператора на запросе рассогласует opclass. Разрыв recall от ef_search, а не от оператора." },
    { label: "Добавить строк, чтобы граф стал плотнее.", misconception: "Добавление данных не поднимает recall существующих запросов — оно расширяет пространство поиска. recall@10 задаётся ef_search и качеством графа, а не размером корпуса." },
  ]}
/>

## Сниппет 2 — оператор расстояния vs opclass

```sql
-- embedding сохранены от модели, обученной на cosine similarity
CREATE INDEX ON items USING hnsw (embedding vector_l2_ops);

SELECT id FROM items
ORDER BY embedding <=> $1   -- cosine-оператор
LIMIT 10;
```

<Quiz
  id="07-vectors-code-2"
  lessonSlug="07-vectors"
  lang="ru"
  question="Запрос медленный и recall плохой. Что здесь не так?"
  choices={[
    { label: "Оператор и opclass индекса не согласованы: индекс построен под L2 (vector_l2_ops), а запрос сортирует по cosine-оператору (<=>), поэтому планировщик не может использовать индекс и падает в полный скан. Построй индекс с vector_cosine_ops под cosine-запрос — и под то, как обучалась модель.", correct: true },
    { label: "Оператор <=> просто медленнее <->.", misconception: "Операторы сопоставимы по стоимости; медлительность — от того, что индекс непригоден для этого оператора, что вынуждает последовательный скан по каждой строке." },
    { label: "pgvector не поддерживает cosine-расстояние.", misconception: "pgvector поддерживает cosine (<=>), L2 (<->) и inner product (<#>). Дефект — рассогласование между оператором запроса и opclass индекса, а не отсутствие поддержки." },
    { label: "LIMIT 10 слишком мал, чтобы индекс включился.", misconception: "LIMIT не управляет использованием индекса. Индекс пропускается, потому что его opclass не совпадает с оператором расстояния запроса." },
  ]}
/>

## Сниппет 3 — IVFFlat probes

```sql
CREATE INDEX ON docs USING ivfflat (embedding vector_cosine_ops)
  WITH (lists = 1000);

-- запрос
SELECT id FROM docs ORDER BY embedding <=> $1 LIMIT 10;
-- ivfflat.probes оставлен по умолчанию
```

<Quiz
  id="07-vectors-code-3"
  lessonSlug="07-vectors"
  lang="ru"
  question="При lists = 1000 и probes по умолчанию recall сильно ниже ожиданий. Почему и первый ход?"
  choices={[
    { label: "ivfflat.probes по умолчанию 1, поэтому запрос сканирует лишь один кластер из 1000 — большинство истинных соседей живёт в других корзинах и никогда не видны. Поднять probes (например SET ivfflat.probes = 50), чтобы сканировать больше кластеров, меняя latency на recall.", correct: true },
    { label: "lists = 1000 слишком велик, надо уменьшить.", misconception: "Бо́льший lists даёт более мелкие кластеры и может помочь recall, когда probes поднят; при probes = 1 симптом — скан одной корзины, а не слишком многих." },
    { label: "IVFFlat не умеет cosine-расстояние.", misconception: "IVFFlat поддерживает cosine через vector_cosine_ops, что и используется. Потеря recall — от probes = 1, сканирующего единственный кластер." },
    { label: "Индексу нужен REINDEX после каждого запроса.", misconception: "Reindex — для дрейфа центроидов со временем, не на каждый запрос. Немедленный фикс — поднять probes, чтобы сканировалось больше кластеров." },
  ]}
/>

## Сниппет 4 — измерение recall против ground truth

```sql
-- точный ground truth (без индекса, брутфорс)
SELECT id FROM docs ORDER BY embedding <=> $1 LIMIT 10;  -- с отключённым индексом

-- ANN-результат
SET hnsw.ef_search = 100;
SELECT id FROM docs ORDER BY embedding <=> $1 LIMIT 10;   -- с HNSW
-- recall@10 = |exact_ids ∩ ann_ids| / 10
```

<Quiz
  id="07-vectors-code-4"
  lessonSlug="07-vectors"
  lang="ru"
  question="Коллега считает recall, сверяя среднее расстояние ANN-результата с фиксированным порогом, а не пересечение с точным топ-10. Почему это неверно?"
  choices={[
    { label: "recall@k определяется как пересечение наборов ID между ANN топ-k и точным топ-k, а не порог расстояния. У неверного соседа может быть малое расстояние, поэтому порог пропускает результаты, которых нет в истинном топ-10 — нужно сравнивать наборы ID против точного baseline.", correct: true },
    { label: "Пороги расстояния норм; они меряют то же, что recall.", misconception: "Порог расстояния меряет, насколько близки возвращённые элементы, а не верные ли они. Два разных вектора могут оба быть под порогом, но один истинный сосед, а другой нет." },
    { label: "Надо мерить latency, а не recall.", misconception: "Latency и recall — разные оси. Вся суть в том, что низкий recall невидим по latency, поэтому recall надо мерить напрямую через пересечение ID." },
    { label: "Recall можно мерить только в embedding-модели, не в SQL.", misconception: "Recall меряется сравнением набора ID от ANN с точным ORDER BY-сканом в той же базе; для ground-truth сравнения вызов модели не нужен." },
  ]}
/>

<Recap lang="ru">
Каждый recall-инцидент читается в DDL и запросе: ef_search по умолчанию 40 и это рантайм-ручка recall; оператор расстояния запроса должен совпадать с opclass индекса, иначе планировщик падает в полный скан; probes в IVFFlat по умолчанию 1 и сканирует один кластер, пока не поднимешь; а recall@k — это пересечение наборов ID против точного baseline, никогда не порог расстояния. Читай SQL, найди утечку, поверни самую дешёвую ручку первой, затем перемерь recall для подтверждения.
</Recap>
