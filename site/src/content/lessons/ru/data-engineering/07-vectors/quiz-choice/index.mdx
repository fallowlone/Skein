---
concepts:
- ann-recall-tradeoff
- hnsw-tuning
- ivf-pq
- distance-metric
- post-filtering
- hybrid-search
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://github.com/pgvector/pgvector
- https://www.crunchydata.com/blog/hnsw-indexes-with-postgres-and-pgvector
- https://weaviate.io/blog/hybrid-search-explained
status: ready
summary: "Тест с выбором на синтез по всему юниту vector search — recall vs latency, HNSW vs IVF-PQ, метрики расстояния, post-filtering и тихий провал recall."
title: 'Vector search: тест с выбором ответа'
track: data-engineering
unit: 07-vectors
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает решение, которое ты принимаешь, глядя на RAG-пайплайн, возвращающий десять ранжированных строк, быстро, без ошибок — и не те десять.
</Hook>

<Goal lang="ru">
Убедись, что связываешь embedding, выбор ANN-индекса, треугольник recall–latency–memory, метрики расстояния и production-ловушки — тот синтез, к которому вёл урок.
</Goal>

<Quiz
  id="07-vectors-choice-1"
  lessonSlug="07-vectors"
  lang="ru"
  question="RAG-поиск возвращает десять ранжированных строк за 2 мс без ошибок, но поддержка сообщает, что бот 'не находит' документы, которые явно есть. Самая вероятная причина и как её подтвердить?"
  choices={[
    { label: "Recall тихо обвалился — скорее всего слишком низкий hnsw.ef_search (по умолчанию 40), часто усугублённый селективным WHERE-фильтром. Подтверждается выборкой запросов, точным ORDER BY-сканом для ground truth и подсчётом пересечения recall@k.", correct: true },
    { label: "База перегружена и теряет строки.", misconception: "Она возвращает все десять строк за 2 мс без ошибок — БД здорова. Низкий recall невидим: по числу строк и latency он выглядит как высокий." },
    { label: "API embedding-модели уходит в таймаут.", misconception: "Запросы успешны за 2 мс; симптом — неверные результаты, а не медленные или упавшие. Это указывает на recall, а не на вызов embedding." },
    { label: "Нужно больше реплик под нагрузку.", misconception: "Проблема не в throughput, а в корректности. Реплицирование индекса с низким recall лишь умножает неверные ответы." },
  ]}
/>

<Quiz
  id="07-vectors-choice-2"
  lessonSlug="07-vectors"
  lang="ru"
  question="RAG-сервис на ~5M чанков с непрерывным приёмом данных, высокими требованиями к recall и запасом RAM. Какой индекс выбрать по умолчанию и почему?"
  choices={[
    { label: "HNSW с настроенным ef_search, измеряемым против точного baseline. Его вставки имеют константную стоимость, поэтому непрерывный приём не деградирует граф, а с запасом RAM накладные 2–5× оправданы ради наивысшего recall.", correct: true },
    { label: "IVFFlat, построенный один раз с большим lists.", misconception: "Центроиды IVF фиксируются при построении, поэтому непрерывный приём данных постепенно сносит recall, пока не сделаешь reindex. С запасом RAM и требованием высокого recall это выбор дрейфующего индекса ради экономии памяти, которая тебе не нужна." },
    { label: "IVF-PQ, чтобы сжать векторы максимально.", misconception: "PQ нужен, когда корпус вообще не влезает в RAM (100M+). На ~5M с запасом RAM ты платишь потерей recall от квантизации за сжатие, которое не требуется." },
    { label: "Брутфорс точный kNN без индекса.", misconception: "Точный kNN даёт recall 1.0, но это O(N·d); на 5M на 1536 измерений каждый запрос — миллиарды умножений-сложений, поэтому p99 неотгружаем. Это offline-baseline, а не путь обслуживания." },
  ]}
/>

<Quiz
  id="07-vectors-choice-3"
  lessonSlug="07-vectors"
  lang="ru"
  question="Поднятие hnsw.ef_search со 100 до 500 двигает recall с ~85% до ~98%, но latency с ~1 мс до ~5 мс. Как это читать senior-инженеру?"
  choices={[
    { label: "Ожидаемо — ef_search это рантайм-ручка recall/latency. Больший пул кандидатов находит больше истинных соседей ценой большей работы по графу на запрос. Выбирай точку, которая удовлетворяет recall-SLO при приемлемом p99.", correct: true },
    { label: "Индекс настроен неверно — recall и latency должны улучшаться вместе.", misconception: "Ни один ANN-индекс на этапе запроса не улучшает recall и latency одновременно; они торгуются вдоль ef_search. Эти числа — нормальная форма ручки." },
    { label: "Перестроить с бо́льшим M.", misconception: "M — это build-time ручка, влияющая на плотность графа и память; она не заменяет ef_search. Перестройка гораздо дороже, чем поворот рантайм-ручки до цели." },
    { label: "Перейти на IVFFlat, чтобы избежать компромисса.", misconception: "У IVF тот же компромисс recall/latency через probes, плюс дрейф центроидов. Смена индекса не выводит из треугольника — любой ANN торгует recall за скорость." },
  ]}
/>

<Quiz
  id="07-vectors-choice-4"
  lessonSlug="07-vectors"
  lang="ru"
  question="Команда хранит нормализованные embedding и спорит про cosine similarity vs внутреннее (dot) произведение. Что верно?"
  choices={[
    { label: "Для нормализованных векторов cosine и dot product ранжируют одинаково, поэтому inner product — более дешёвый эквивалент: он пропускает нормализацию длины. Метрику подбирают под то, как обучалась модель, и держат её согласованной между индексом и запросом.", correct: true },
    { label: "Cosine всегда точнее dot product.", misconception: "Cosine — это dot product, делённый на длины. На уже нормализованных векторах длины равны 1, поэтому оба дают идентичное ранжирование — cosine лишь добавляет лишнюю работу." },
    { label: "L2-расстояние всегда самый безопасный дефолт.", misconception: "L2 ранжирует иначе, чем cosine, если векторы не нормализованы, а метрика, под которую embedding не обучались, ухудшает ранжирование. Метрика должна совпадать с моделью, а не быть L2 по умолчанию." },
    { label: "Метрика не влияет на результаты, только на скорость.", misconception: "Метрика задаёт, что значит 'ближайший', и меняет ранжирование, если векторы не нормализованы. Неверный выбор под модель тихо ухудшает релевантность." },
  ]}
/>

<Quiz
  id="07-vectors-choice-5"
  lessonSlug="07-vectors"
  lang="ru"
  question="Поиск с областью видимости тенанта добавляет WHERE tenant_id = ? поверх HNSW-запроса, и для маленьких тенантов recall резко падает. Почему и как чинить?"
  choices={[
    { label: "pgvector делает post-filtering: он обходит граф ради ef_search кандидатов, а затем применяет фильтр. Если фильтр оставляет 1% строк, почти никто из ef_search кандидатов не выживает. Фикс: hnsw.iterative_scan (0.8.0+), чтобы добирать кандидатов, меняя latency на recall.", correct: true },
    { label: "Колонке tenant_id нужен B-tree индекс.", misconception: "B-tree по tenant_id ускоряет фильтр, но не меняет того, что ANN-кандидаты собираются до применения фильтра — обвал recall из-за порядка post-filtering, а не скорости фильтра." },
    { label: "У маленьких тенантов просто слишком мало документов для ранжирования.", misconception: "Даже при достатке подходящих документов post-filtering выбрасывает большинство ef_search кандидатов, потому что они принадлежат другим тенантам. Проблема в порядке, а не в размере корпуса." },
    { label: "Переключить cosine на L2 для фильтрованных запросов.", misconception: "Метрика тут ни при чём. Потеря recall — оттого, что фильтр применяется после того, как обход графа уже сузился до ef_search кандидатов." },
  ]}
/>

<Quiz
  id="07-vectors-choice-6"
  lessonSlug="07-vectors"
  lang="ru"
  question="Пользователи ищут точную строку ошибки 'ECONNREFUSED', а чистый vector search хоронит нужный документ под смутно-связанными абзацами. Лучший фикс?"
  choices={[
    { label: "Hybrid search: запускать BM25-поиск по ключевым словам и vector-поиск параллельно и сливать через Reciprocal Rank Fusion. Слияние по позиции ранга избегает сравнения двух несовместимых шкал оценок и возвращает точные совпадения токенов.", correct: true },
    { label: "Поднять ef_search до 1000.", misconception: "Больше кандидатов того же рода не помогает — потребность лексическая, а embedding размазывают точный токен вроде кода ошибки в 'семантически близкое'. ef_search настраивает vector recall, а не точность совпадения токена." },
    { label: "Переэмбеддить всё моделью побольше.", misconception: "Бо́льшая модель всё равно измеряет семантическую близость; она не даёт точности по токену. Коды ошибок, SKU и идентификаторы — это keyword-потребность, которую закрывает BM25." },
    { label: "Сменить метрику расстояния с cosine на dot product.", misconception: "Выбор метрики чуть двигает ранжирование, но никогда не даёт лексической точности. Vector search лишён точного совпадения независимо от метрики; hybrid добавляет keyword-ногу, у которой оно есть." },
  ]}
/>

<Recap lang="ru">
Сквозная линия — один треугольник recall, latency, memory — и одна привычка: провалы recall тихие. Пул кандидатов (ef_search / probes) задаёт recall vs latency, семейство индекса задаёт память и дрейф (HNSW для изменяющихся данных, IVFFlat для статики, IVF-PQ когда не влезает в RAM), метрика должна совпадать с тем, как обучались embedding, post-filtering ломает селективные фильтры (используй iterative scan), а потребность в точных токенах требует hybrid BM25 + vector с rank fusion. Прежде всего — измеряй recall@k против точного baseline, иначе летишь вслепую.
</Recap>
