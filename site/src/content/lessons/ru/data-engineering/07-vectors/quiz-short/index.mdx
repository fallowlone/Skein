---
concepts:
- recall-latency-tradeoff
- hnsw-vs-ivf
- ivf-pq
- distance-metric
- post-filtering
- hybrid-search
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://github.com/pgvector/pgvector
- https://www.crunchydata.com/blog/hnsw-indexes-with-postgres-and-pgvector
- https://weaviate.io/blog/hybrid-search-explained
status: ready
summary: "Промпты на свободное воспроизведение по юниту vector search — сначала ответь по памяти, затем раскрой модельный ответ и сравни."
title: 'Vector search: тест на свободное воспроизведение'
track: data-engineering
unit: 07-vectors
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. На каждый промпт скажи или запиши полный ответ по памяти, прежде чем открыть модельный — усилие припоминания и закрепляет материал.
</Hook>

<Goal lang="ru">
Восстанови позвоночник юнита — треугольник recall–latency–memory, почему recall падает тихо, HNSW vs IVF vs IVF-PQ, выбор метрики, post-filtering и hybrid search — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:load
  id="07-vectors-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему ANN-индекс с низким recall так опасен в проде и как его реально обнаружить?", a: "Потому что провал полностью тихий. Запрос на recall 0.3 возвращает те же десять ранжированных строк, за те же несколько миллисекунд, без ошибки, без предупреждения, без аномальной метрики — просто не те десять. Классические провалы корректности бросают исключения или взвинчивают latency; этот выглядит идентично здоровому запросу, поэтому проходит staging и всплывает лишь как смутные жалобы 'бот не находит' через недели. Триггер обычно — слишком низкий hnsw.ef_search (по умолчанию в pgvector 40), часто усугублённый селективным WHERE-фильтром, который рвёт post-filtering. Обнаруживается прямым измерением recall@k: взять выборку запросов, прогнать их точным ORDER BY-сканом без индекса для ground truth, прогнать те же через ANN-индекс и посчитать пересечение наборов ID. Это число recall — не latency, не число строк — то, на что вешаешь алерт." },
    { q: "Опиши треугольник recall–latency–memory и какая ручка двигает какую ось в HNSW.", a: "Эти три в напряжении, и ни один ANN-метод не оптимизирует всё сразу. Память задаётся в основном на build-time через M (связей на узел): больший M — плотнее граф, больше RAM (граф HNSW занимает в 2-5 раз больше сырых векторов) и выше достижимый recall. Качество построения задаёт ef_construction: больший пул кандидатов при построении даёт лучший граф, но время построения растёт примерно линейно. Recall vs latency на serving-time задаёт ef_search: подъём со 100 до 500 может поднять recall с ~85% до ~98%, пока latency идёт с ~1 мс до ~5 мс. Так что тратишь память и время построения, чтобы сделать высокий recall дёшево достижимым, затем крутишь recall против latency на каждый запрос через ef_search, чтобы попасть в SLO." },
    { q: "Когда выбираешь HNSW, IVFFlat или IVF-PQ и чего стоит каждый?", a: "HNSW — дефолт для RAG: наивысший recall с наименьшим тюнингом и константная стоимость вставок, поэтому он не дрейфует при изменении данных — ценой 2-5x сырых векторов в RAM и медленных построений. IVFFlat кластеризует векторы k-means в lists корзин и сканирует лишь ближайшие probes корзин; он влезает примерно в 1x сырых векторов и быстро строится, но его центроиды фиксируются на build-time, поэтому recall дрейфует по мере вставки новых данных, и ему нужны репрезентативные данные до построения. IVF-PQ добавляет product quantization — деля каждый вектор на под-векторы, заменяемые id центроидов из кодбука — сжимая радикально (NVIDIA сообщила о 360 GiB до 54 GiB на миллиарде векторов), ценой потери recall, часто оседающей около 70% от HNSW, восстанавливаемой шагом rerank. IVF-PQ — выбор по памяти для 100M+ векторов, не влезающих в RAM, а не повседневный дефолт." },
    { q: "Как связаны cosine, dot product и L2 и как выбрать метрику?", a: "Cosine similarity меряет угол между векторами, игнорируя длину; dot (inner) product вмешивает длину; L2 — евклидово расстояние. Для нормализованных embedding cosine и dot product ранжируют идентично, поэтому inner product используют как более дешёвый эквивалент — он пропускает нормализацию длины. Правило — подбирать метрику под то, как обучалась embedding-модель: модель, обученную на cosine, ищут cosine (или dot на нормализованных векторах), а opclass индекса (vector_cosine_ops / vector_ip_ops / vector_l2_ops) плюс оператор запроса (<=> / <#> / <->) должны совпадать, иначе планировщик не сможет использовать индекс. Несовпадающая метрика тихо ухудшает релевантность." },
    { q: "Почему фильтрация по метаданным плохо взаимодействует с ANN и как это решает pgvector?", a: "pgvector делает post-filtering: сначала обходит граф HNSW, собирая ef_search кандидатов, затем применяет твой WHERE. Если фильтр селективен — скажем tenant_id оставляет 1% строк — большинство этих ef_search кандидатов отбрасывается, и можно остаться почти ни с чем, потому что граф навигировал по крошечному, почти несвязному подграфу; recall обваливается для маленьких разделов. pgvector 0.8.0 добавил hnsw.iterative_scan (strict_order или relaxed_order), который добирает кандидатов из графа, пока достаточно не пройдёт фильтр, меняя latency обратно на потерянный recall. Senior-привычка — понимать, что селективный фильтр плюс дефолтный ef_search это recall-ловушка, и тестировать recall именно на фильтрованном пути." },
    { q: "Что такое hybrid search, когда он нужен и как Reciprocal Rank Fusion соединяет результаты?", a: "Hybrid search запускает лексический поиск (BM25 по ключевым словам) и vector-поиск параллельно и сливает два списка результатов. Он нужен, потому что чистый vector search промахивается на точных совпадениях: запрос на код ошибки, SKU или имя функции — лексическая потребность, а embedding размазывают его в 'семантически близкое', поэтому 'panic: nil pointer' может ранжироваться ниже смутно связанного абзаца. Reciprocal Rank Fusion соединяет по позиции ранга, а не по оценке: RRF = сумма по спискам 1/(k + rank) с k около 60, поэтому две несовместимые шкалы (BM25 term-оценки vs vector-расстояния) не приходится сравнивать напрямую. Команды сообщают о скачке с ~65-78% до ~91% recall@10 после добавления keyword-ноги." },
  ]}
/>

<Recap lang="ru">
Если смог восстановить каждый ответ по памяти — держишь позвоночник юнита: recall падает тихо, поэтому меряешь recall@k против точного baseline; треугольник recall–latency–memory крутится через M, ef_construction и ef_search; HNSW — дефолт, а IVF-PQ — запасной выход по памяти; метрика должна совпадать с моделью и opclass индекса; селективные фильтры обваливают post-filtered recall, пока не используешь iterative scan; а hybrid BM25 + vector с rank fusion — ответ всегда, когда важны точные токены.
</Recap>
