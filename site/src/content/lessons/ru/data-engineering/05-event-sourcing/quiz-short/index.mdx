---
concepts:
- event-log-truth
- left-fold-state
- cqrs-projections
- eventual-consistency
- snapshotting
- event-versioning
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://martinfowler.com/eaaDev/EventSourcing.html
- https://event-driven.io/en/gdpr_in_event_driven_architecture/
- https://www.confluent.io/blog/event-sourcing-using-apache-kafka/
status: ready
summary: "Промпты на свободное воспроизведение по event sourcing. Сначала ответь своими словами, потом раскрой модельный ответ и сверься с сеньорской планкой."
title: 'Event sourcing: тест на свободное воспроизведение'
track: data-engineering
unit: 05-event-sourcing
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. На каждый промпт скажи или напиши полный ответ по памяти, прежде чем открыть модельный — усилие припоминания и закрепляет паттерн в голове, особенно те части, что кусаются в продакшене.
</Hook>

<Goal lang="ru">
Восстанови хребет юнита — лог как истина, состояние как свёртка, projection CQRS, eventual consistency, snapshot и вечное версионирование — не подсматривая в урок.
</Goal>

<RetrievalDrawer
  client:visible
  id="05-event-sourcing-short-retrieval"
  lang="ru"
  questions={[
    { q: "Какая одна инверсия определяет event sourcing и почему именно она источник всей пользы и всей цены?", a: "Вместо хранения текущего состояния и уничтожения истории на каждом UPDATE ты хранишь неизменяемый append-only поток событий, меняющих состояние, как источник истины, и выводишь текущее состояние свёрткой лога слева (state = events.reduce(apply, initial)). Текущее состояние становится одноразовым кэшем, который можно пересчитать когда угодно. Вся польза выпадает из того, что данные никогда не выбрасываются: полный аудит-трейл (лог И ЕСТЬ аудит), темпоральные запросы (свернуть только события до timestamp и получить состояние на любой прошлый момент), детерминированная отладка через replay (скопировать продакшен-поток в staging и воспроизвести точный баг). И вся цена выпадает из того же ограничения: поскольку лог не переписывается, изменения схемы вечны (upcasting), стирание по GDPR сталкивается с неизменяемостью (crypto-shredding), стоимость replay растёт неограниченно (snapshot), а чтения отстают от write-стороны (eventual consistency)." },
    { q: "Коллега говорит «мы публикуем события в Kafka, значит мы event-sourced». Почему это может быть неверно и что реально сделало бы это event sourcing?", a: "Эмиссия событий это не event sourcing. Определяющее свойство — лог это авторитетный источник истины и состояние восстанавливается его свёрткой, а не то, что события эмитируются побочным каналом после того, как база уже закоммитила настоящую истину в другом месте. Если сервис пишет строку в Postgres, а потом публикует уведомление в Kafka, то строка это истина, а Kafka это лишь change feed; потеря топика не теряет ничего канонического. Чтобы быть event-sourced, добавление события должно БЫТЬ записью (нет отдельной авторитетной строки), и ты должен мочь сбросить всё производное состояние и пересобрать его чисто через replay лога. С Kafka конкретно: компакция лога оставляет только последнее значение на ключ и уничтожает историю, на которой держится паттерн, поэтому нужны топики на retention с бесконечным сроком, а не компактируемые; а оптимистичная конкурентность на агрегат (append только на ожидаемой версии N) не имеет чистого примитива в Kafka, поэтому специализированные хранилища вроде EventStoreDB делают expectedVersion first-class условием append." },
    { q: "Что такое CQRS в этом контексте, почему projection одноразовы и какие два свойства делают их безопасными в продакшене?", a: "Нельзя обслуживать каждый запрос свёрткой всего лога на каждый вызов, поэтому event sourcing сочетается с CQRS: write-сторона добавляет события; read-сторона запускает projection, которые потребляют поток и материализуют целевые read-модели (SQL-таблица, поисковый индекс, денормализованный кэш). Projection одноразовы, потому что они чистые производные — удали таблицу, проиграй лог, получи её обратно — а значит, новую вью можно добавить через месяцы, просто написав projection и проиграв историю. Два свойства делают это безопасным. Первое, идемпотентность: at-least-once доставка и ретраи могут доставить то же событие дважды, поэтому применить его дважды должно равняться применению один раз — стандартный механизм это version-checkpoint на поток, игнорирующий любое событие, чья версия меньше или равна последней виденной. Второе, eventual consistency: между append и тем, как projection догонит, есть реальный лаг, поэтому чтения не гарантированно актуальны в любой момент — они сходятся со временем." },
    { q: "Как обработать лаг read-модели в UI, не протекая архитектурой пользователю?", a: "Наивный фикс — опрашивать projection, пока изменение не появится — протекает лаг eventual consistency пользователю как мерцание или пропавшую строку. Реальный фикс это вообще не читать отстающую projection для этого одного экрана: вернуть новое состояние оптимистично из результата команды (ты уже знаешь, что добавил), или read-your-own-writes с write-стороны для немедленной вью после действия, а projection пусть успокаивается в фоне для всех остальных. Ментальная модель в том, что лаг это UX-проблема, проектируемая по экранам, а не инфра-баг, устраняемый принудительным синхронным обновлением projection (что выбросило бы развязку, ради которой существует CQRS)." },
    { q: "Почему стоимость replay растёт неограниченно, как snapshot это чинят и в чём footgun snapshot?", a: "Загрузка агрегата свёрткой всего потока становится медленнее по мере роста потока — система, проигрывающая миллионы событий (или терабайты тиков), может тратить минуты на пересборку, потому что стоимость масштабируется с длиной потока. Snapshot чинят это, периодически сохраняя свёрнутое состояние на версии N в отдельное хранилище; при загрузке читаешь snapshot и проигрываешь только события строго после N (исключающая нижняя граница — повторная свёртка версии N применяет её дважды). Snapshot это чистая оптимизация производительности, оставляющая лог нетронутым. Footgun: если snapshot дрейфует от событий, которые он претендует обобщать — изменилась логика apply или потерялась запись посреди потока — он молча отдаёт неверное состояние И маскирует баг, потому что replay никогда не пересчитывает с начала, чтобы вскрыть расхождение. Защищающиеся команды берут контрольную сумму snapshot и пересобирают при несовпадении, и считают snapshot пересобираемыми, а не авторитетными." },
    { q: "Почему версионирование схемы событий вечно, как его решает upcasting и как стирание по GDPR взаимодействует с неизменяемым логом?", a: "Поскольку лог append-only и никогда не переписывается, каждое когда-либо записанное событие остаётся в исходной форме навсегда и должно оставаться проигрываемым — так что при изменении схемы события (добавить поле, переименовать, разбить одно на два) все исторические события всё ещё существуют в старой форме, и их нельзя редактировать без нарушения append-only, аудит-трейла и конкурентности по версиям. Upcasting решает это, не трогая историю: цепочка маленьких чистых функций преобразования поднимает старую форму события к следующей версии при десериализации, пока та не достигнет текущей формы, так что доменный код всегда видит только последнюю версию. Upcaster накапливаются — это код, который несёшь бесконечно и должен тестировать на реальных исторических payload. Право на забвение по GDPR сталкивается с неизменяемостью в лоб: ты юридически обязан стереть PII пользователя, но не можешь переписать лог. Доминирующая техника это crypto-shredding — шифровать PII каждого пользователя ключом на пользователя, хранимым вне лога, и «забыть» его, уничтожив ключ, сделав шифротекст навсегда невосстановимым, пока структура событий цела. Честная оговорка: регуляторы могут всё ещё считать неудаляемую зашифрованную PII персональными данными, так что это сильная прагматичная мера, требующая юридической визы, а не автоматическая гарантия." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: одна инверсия (append-only лог это истина, состояние это свёртка) покупает аудит, темпоральные запросы и replay, а счёт выставляет в версионировании, GDPR, snapshot и eventual consistency. Event sourcing это не топик Kafka, эмитированный после записи в БД; projection CQRS одноразовы, идемпотентны и eventually consistent; лаг это UX-проблема по экранам; snapshot ограничивают replay, но молча дрейфуют; версионирование вечно, поэтому делаешь upcast; а GDPR решается crypto-shredding с юридической оговоркой. Одно правило под всем этим: никогда не мутируй лог. Теперь, когда встретишь решение, требующее правки прошлого события — GDPR-запрос, исправление схемы, «просто удали эту запись» — ты потянешься за компенсирующим событием, upcasting или crypto-shredding, и будешь знать, почему.
</Recap>
