---
concepts:
- projection-rebuild
- idempotent-projection
- snapshot-load
- event-upcasting
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://martinfowler.com/eaaDev/EventSourcing.html
- https://docs.eventsourcingdb.io/best-practices/read-model-consistency-and-lag/
status: ready
summary: "Чтение реальных сниппетов event sourcing — пересборка projection, загрузка через snapshot и upcaster — предскажи поведение и выбери сеньорский фикс."
title: 'Event sourcing: чтение кода'
track: data-engineering
unit: 05-event-sourcing
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Паттерн живёт или умирает в цикле свёртки, обработчике projection, загрузке через snapshot и upcaster. Прочитай каждый сниппет, предскажи, что он делает в реальных условиях, потом выбери фикс, который сеньор делает первым.
</Hook>

<Goal lang="ru">
Потренируйся читать четыре куска кода, что есть в любой event-sourced системе: пересборку read-модели через replay, идемпотентность projection, загрузку через snapshot плюс хвост, и upcasting старой формы события без переписывания истории.
</Goal>

## Сниппет 1 — пересборка projection

```ts
// пересборка "drop and replay" read-модели балансов
async function rebuildBalances(store: EventStore, db: ReadDb) {
  await db.exec("TRUNCATE balances");
  for await (const e of store.readAll({ from: 0 })) {  // все события, по порядку
    if (e.type === "MoneyDeposited") {
      await db.exec(
        "UPDATE balances SET cents = cents + $1 WHERE acct = $2",
        e.data.cents, e.data.acct,
      );
    }
    // обработчик MoneyWithdrawn опущен
  }
}
```

<Quiz
  id="05-event-sourcing-code-1"
  lessonSlug="05-event-sourcing"
  lang="ru"
  question="Таблица balances пуста после TRUNCATE. Для счёта, чьё первое событие MoneyDeposited, UPDATE затрагивает ноль строк и баланс не появляется. Каков правильный фикс?"
  choices={[
    { label: "Projection предполагает, что строка уже есть. Либо UPSERT (INSERT ... ON CONFLICT DO UPDATE), либо обработать событие создания счёта (AccountOpened), чтобы засеять строку до применения депозитов. Пересборка должна реконструировать строки из лога, а не предполагать их.", correct: true },
    { label: "Ошибка была в TRUNCATE; пересобирай инкрементально поверх существующей таблицы.", misconception: "Полная пересборка ДОЛЖНА начинаться с пустоты — именно это делает projection одноразовыми и воспроизводимыми. Баг в том, что обработчик никогда не вставляет строку, а не в том, что таблицу очистили." },
    { label: "readAll итерирует в неправильном порядке; разверни его, чтобы депозиты пришли последними.", misconception: "Порядок событий должен быть от старых к новым для корректной свёртки; разворот портит состояние. Пропавшая строка из UPDATE без предшествующего INSERT, а не из порядка." },
    { label: "Добавь уникальный индекс на acct, чтобы UPDATE создал строку.", misconception: "Уникальный индекс ограничивает дубли; он не заставляет UPDATE вставить отсутствующую строку. Нужен UPSERT или обработчик события создания." },
  ]}
/>

## Сниппет 2 — обработчик projection при ретраях

```ts
async function onEvent(e: Event, db: ReadDb) {
  // доставка at-least-once: может быть вызвана дважды для одного события
  await db.exec(
    "UPDATE counters SET total = total + 1 WHERE name = $1",
    e.aggregateId,
  );
}
```

<Quiz
  id="05-event-sourcing-code-2"
  lessonSlug="05-event-sourcing"
  lang="ru"
  question="Доставка at-least-once, так что обработчик может выполниться дважды для одного события. В чём баг и каков стандартный фикс?"
  choices={[
    { label: "Он не идемпотентен — переданное повторно событие считается дважды. Отслеживай последнюю обработанную версию события на поток в read-хранилище, применяй обновление и продвигай checkpoint в одной транзакции, и игнорируй любое событие, чья версия меньше или равна последней виденной.", correct: true },
    { label: "Бага нет; базы данных автоматически дедуплицируют одинаковые UPDATE.", misconception: "Базы не дедуплицируют по тексту запроса — выполнить тот же инкремент дважды добавит два. At-least-once означает, что идемпотентность обеспечиваешь ты сам." },
    { label: "Переключись на exactly-once доставку, и обработчик в порядке как есть.", misconception: "Настоящий exactly-once через сеть и базу в общем недостижим; системы дают at-least-once, а потребителя ты делаешь идемпотентным. Полагаться на exactly-once это ловушка." },
    { label: "Оберни UPDATE в цикл ретраев, чтобы повторить неудачное применение.", misconception: "Ретраи делают дублирующее применение БОЛЕЕ вероятным, а не менее. Фикс это version-checkpoint, делающий повторное применение no-op, а не больше ретраев." },
  ]}
/>

## Сниппет 3 — загрузка агрегата через snapshot

```ts
async function loadAccount(id: string, store: EventStore): Promise<Account> {
  const snap = await store.latestSnapshot(id);          // может быть null
  let state = snap ? snap.state : Account.empty(id);
  const from = snap ? snap.version : 0;
  for await (const e of store.read(id, { from })) {      // БАГ здесь
    state = apply(state, e);
  }
  return state;
}
```

<Quiz
  id="05-event-sourcing-code-3"
  lessonSlug="05-event-sourcing"
  lang="ru"
  question="Snapshot захватил состояние до версии N включительно (snap.version === N). Чтение использует from = N. Что идёт не так?"
  choices={[
    { label: "Ошибка на единицу: read(from = N) повторно применяет событие версии N, которое snapshot уже свернул, применяя его дважды. Хвост должен начинаться строго после snapshot — from = snap.version + 1 (исключающая нижняя граница).", correct: true },
    { label: "Ничего — повторное применение одного события безвредно, потому что apply идемпотентен.", misconception: "apply это шаг свёртки, не идемпотентный — применить MoneyDeposited дважды добавит сумму дважды. Snapshot уже включает версию N, поэтому её надо пропустить." },
    { label: "Snapshot надо вообще игнорировать; всегда проигрывай с 0 ради корректности.", misconception: "Это сводит на нет весь смысл snapshot, существующих, чтобы ограничить стоимость replay. Фикс — правильная исключающая граница, а не отказ от snapshot." },
    { label: "store.read возвращает события в обратном порядке, так что последнее применяется первым.", misconception: "read возвращает события от старых к новым с заданной версии; порядок в норме. Дефект во включающей нижней границе, повторно применяющей версию N." },
  ]}
/>

## Сниппет 4 — upcasting эволюционировавшего события

```ts
// v1: { type: "OrderPlaced", total: 1299 }            // центы, валюта подразумевается USD
// v2: { type: "OrderPlaced", amount: { cents, currency } }
function upcastOrderPlaced(raw: any): OrderPlacedV2 {
  if (raw.version === 2) return raw;                    // уже текущая
  // raw это v1
  return {
    type: "OrderPlaced",
    version: 2,
    amount: { cents: raw.total, currency: "USD" },
  };
}
```

<Quiz
  id="05-event-sourcing-code-4"
  lessonSlug="05-event-sourcing"
  lang="ru"
  question="Какова роль этой функции и какое свойство существенно, чтобы её было безопасно нести бесконечно?"
  choices={[
    { label: "Это upcaster: при десериализации он поднимает сохранённое событие v1 к текущей форме v2, чтобы доменный код всегда видел только v2 — без переписывания сохранённого события v1. Он должен быть чистым детерминированным отображением исторических payload, протестированным на реальных данных v1, потому что баг тут молча портит каждый replay старых событий.", correct: true },
    { label: "Это миграция, переписывающая сохранённые события v1 в v2 в логе.", misconception: "Она возвращает преобразованный объект в памяти и никогда не пишет обратно в хранилище; сохранённое событие v1 не тронуто. Переписывание лога нарушило бы append-only." },
    { label: "Это обработчик projection, обновляющий read-модель.", misconception: "Она производит нормализованное событие, а не строку read-модели. Upcasting происходит на пути чтения хранилища событий, до того как событие дойдёт до projection или агрегатов." },
    { label: "Это необязательный сахар; можно просто писать `currency ?? 'USD'` везде.", misconception: "Рассыпание дефолтов по каждому потребителю навсегда протекает историей схемы. Централизация в одном протестированном upcaster и есть смысл — доменный код остаётся на текущей форме." },
  ]}
/>

<Recap lang="ru">
Любая event-sourced система читается в этих четырёх кусках кода. Пересборка начинается с пустоты и должна реконструировать строки из лога, поэтому обработчики делают UPSERT или чтят события создания, а не предполагают строки. Projection сталкиваются с доставкой at-least-once, поэтому продвигают version-checkpoint на поток в той же транзакции, что и запись, и игнорируют уже виденные версии. Загрузка через snapshot использует исключающую нижнюю границу (версия + 1), чтобы заснапшоченное событие не свернулось повторно. А upcaster это чистые протестированные преобразования, поднимающие старые формы к текущей при чтении, никогда не переписывая сохранённый лог. Читай код, предскажи свёртку, потом чини размещение истины — но не сам лог.
</Recap>
