---
concepts:
- event-log-truth
- projections-cqrs
- event-replay
- snapshotting
- event-versioning
- crypto-shredding
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://martinfowler.com/eaaDev/EventSourcing.html
- https://event-driven.io/en/gdpr_in_event_driven_architecture/
- https://docs.eventsourcingdb.io/best-practices/read-model-consistency-and-lag/
status: ready
summary: "Синтез по всему юниту event sourcing с множественным выбором — лог как источник истины, projection и CQRS, replay, snapshot, версионирование событий и GDPR поверх неизменяемого лога."
title: 'Event sourcing: тест с множественным выбором'
track: data-engineering
unit: 05-event-sourcing
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов через весь юнит. Каждый отражает решение, которое ты принимаешь, проектируя или эксплуатируя event-sourced систему — не определение для пересказа, а компромисс, который надо взвесить, когда лог это истина, а всё остальное производно.
</Hook>

<Goal lang="ru">
Убедись, что можешь связать append-only лог, projection, replay, snapshot, версионирование и GDPR — синтез, к которому вёл урок, на той глубине, где решения реально кусаются.
</Goal>

<Quiz
  id="05-event-sourcing-choice-1"
  lessonSlug="05-event-sourcing"
  lang="ru"
  question="Коллега утверждает: «мы и так публикуем доменные события в Kafka после каждой записи в БД, значит мы event-sourced». Почему это, скорее всего, неверно?"
  choices={[
    { label: "Если строка в Postgres коммитится первой, а событие в Kafka эмитируется после, то строка это источник истины, а топик это лишь change feed. В event sourcing добавление события И ЕСТЬ запись — нет отдельной авторитетной строки — и всё состояние должно восстанавливаться чисто свёрткой лога.", correct: true },
    { label: "Это и есть event sourcing — эмитить событие на каждое изменение состояния это и есть определение.", misconception: "Эмиссия событий необходима, но недостаточна. Определяющее свойство — лог авторитетен и состояние выводится из него, а не то, что события публикуются побочным каналом после того, как база уже держит настоящую истину." },
    { label: "Неверно, потому что Kafka никогда не может быть хранилищем событий.", misconception: "Kafka может служить хранилищем событий с оговорками (бесконечный retention, без компакции). Изъян здесь в dual-write, где истину держит БД, а не в самой Kafka." },
    { label: "Неверно только если они забыли сделать топик партиционированным.", misconception: "Партиционирование — про порядок и пропускную способность, не про то, лог ли источник истины. Реальная проблема в том, что авторитетна закоммиченная строка, а не лог." },
  ]}
/>

<Quiz
  id="05-event-sourcing-choice-2"
  lessonSlug="05-event-sourcing"
  lang="ru"
  question="Почему projection (read-модели) безопасно считать одноразовыми и что это даёт операционно?"
  choices={[
    { label: "Потому что они чистые производные лога — удали таблицу, проиграй события, получи её обратно. Значит, новую вью можно ввести через месяцы, написав projection и проиграв через неё историю; данные всегда были в логе.", correct: true },
    { label: "Потому что read-модели бэкапятся ночью, так что удаление всегда восстанавливается из бэкапа.", misconception: "Одноразовость идёт от воспроизводимости через replay, а не от бэкапов. Бэкап восстанавливает старую форму; replay может построить совершенно новые формы, которых в бэкапе не было." },
    { label: "Потому что projection хранят единственную копию событий, так что их удаление обратимо.", misconception: "Projection хранят производное состояние, а не авторитетные события. События живут в append-only хранилище; projection это одноразовая материализация из них." },
    { label: "Потому что CQRS запрещает менять read-модель после её построения.", misconception: "CQRS делает обратное — он позволяет свободно добавлять, удалять и пересобирать read-модели именно потому, что они производны и одноразовы." },
  ]}
/>

<Quiz
  id="05-event-sourcing-choice-3"
  lessonSlug="05-event-sourcing"
  lang="ru"
  question="Пользователь жмёт «Сохранить», ты добавляешь событие, потом редиректишь на список из projection — и его изменения там нет. В чём корневая причина и правильный фикс?"
  choices={[
    { label: "Read-сторона eventually consistent: projection отстаёт от append. Правильный фикс — вернуть новое состояние из результата команды (или read-your-own-writes с write-стороны для этого одного экрана), а не опрашивать projection, пока он не догонит.", correct: true },
    { label: "Событие потерялось; добавь синхронную транзакцию БД, чтобы projection обновлялся атомарно с append.", misconception: "Заставлять projection обновляться синхронно с каждым append выбрасывает развязку и масштабируемость, ради которых существует CQRS, а событие не потеряно — оно просто ещё не спроецировано." },
    { label: "Projection должна быть идемпотентной, а эта нет, поэтому дублирующая запись сократилась.", misconception: "Идемпотентность про безопасную повторную доставку того же события, а не про пропавшее. Изменения нет, потому что projection просто не догнала — лаг replay, а не дубль." },
    { label: "Append молча провалился; переключись на at-most-once доставку, чтобы вскрыть ошибку.", misconception: "At-most-once меняет риск дублей на риск потери событий, что здесь хуже. Append, вероятно, прошёл; read-модель просто отстаёт от него на некоторый лаг." },
  ]}
/>

<Quiz
  id="05-event-sourcing-choice-4"
  lessonSlug="05-event-sourcing"
  lang="ru"
  question="Загрузка одного агрегата сворачивает 4 миллиона событий и слишком медленна. Ты добавляешь snapshot. От какого одного режима отказа защищается сеньор?"
  choices={[
    { label: "Дрейф snapshot: если snapshot расходится с событиями (изменилась логика, потерялась запись), он молча отдаёт неверное состояние и маскирует баг, потому что replay никогда не пересчитывает с нуля. Защита — контрольная сумма snapshot и пересборка при несовпадении.", correct: true },
    { label: "Snapshot ломает append-only инвариант, потому что пишется в поток событий.", misconception: "Snapshot хранятся отдельно (snapshot-хранилище или боковой поток), а не мутацией событий. Это чистая оптимизация чтения, лог остаётся нетронутым." },
    { label: "Snapshot делает replay невозможным, так что теряются темпоральные запросы.", misconception: "Snapshot — оптимизация поверх replay: читаешь snapshot на версии N, потом проигрываешь события после N. Полный replay с самого начала всегда доступен." },
    { label: "Snapshot надо делать после каждого события, иначе они бесполезны.", misconception: "Snapshot делаются периодически (каждые N событий или по расписанию); вся их ценность в амортизации стоимости свёртки, а не в захвате каждого события." },
  ]}
/>

<Quiz
  id="05-event-sourcing-choice-5"
  lessonSlug="05-event-sourcing"
  lang="ru"
  question="OrderPlaced получает обязательное поле currency. У миллионов исторических OrderPlaced его нет. Каков стандартный подход?"
  choices={[
    { label: "Upcasting: зарегистрировать преобразование, которое поднимает старую форму события к новой при десериализации (например, проставляет USD для старых событий), так что остальной код всегда видит только текущую версию. История никогда не переписывается.", correct: true },
    { label: "Запустить миграцию, которая обновит каждое историческое событие OrderPlaced на месте, добавив поле.", misconception: "Мутация прошлых событий нарушает append-only инвариант, ломает каждый downstream replay и гарантию аудита, обесценивает конкурентность по версиям. Лог не переписывают." },
    { label: "Удалить все события OrderPlaced старше изменения схемы и проигрывать только с новых.", misconception: "Удаление истории уничтожает аудит-трейл и темпоральные запросы, ради которых существует паттерн, и делает агрегаты несобираемыми. Upcasting сохраняет историю, показывая новую форму." },
    { label: "Добавить nullable-поле и рассыпать проверки на null по доменной логике навсегда.", misconception: "Проверки на null навсегда протекают историей схемы в каждого потребителя. Upcasting централизует фикс в одной цепочке преобразований, и доменный код остаётся чистым." },
  ]}
/>

<Quiz
  id="05-event-sourcing-choice-6"
  lessonSlug="05-event-sourcing"
  lang="ru"
  question="Пользователь реализует право на забвение по GDPR; его PII вшита в сотни неизменяемых событий. Что реально шипит сеньор?"
  choices={[
    { label: "Crypto-shredding: шифровать PII каждого пользователя ключом на пользователя, хранимым вне лога, и «забыть» его, уничтожив ключ — шифротекст становится навсегда невосстановимым, а структура событий и replay остаются целы. Оговорка: регуляторы могут всё ещё считать неудаляемую зашифрованную PII персональными данными, нужна юридическая виза.", correct: true },
    { label: "Найти и удалить из лога каждое событие, содержащее PII пользователя.", misconception: "Переписывание append-only лога ломает каждый replay, уничтожает аудит-трейл и обесценивает версии оптимистичной конкурентности. Стоит мутировать историю — и у тебя уже не event sourcing." },
    { label: "Положиться на полнодисковое шифрование — оно уже удовлетворяет праву на забвение.", misconception: "Дисковое шифрование защищает от физической кражи, а не от того, что система удерживает данные субъекта после запроса на удаление. Оно ничего не даёт для удаляемости данных одного человека." },
    { label: "Отказаться от event sourcing для любого агрегата с PII и перевести его на CRUD.", misconception: "Это выбрасывает аудит, темпоральные запросы и replay для всего агрегата ради проблемы, которую crypto-shredding решает хирургически. Чрезмерная перекоррекция." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна инверсия и её следствия: append-only лог это источник истины, текущее состояние это свёртка, всё прочее производно. Projection это одноразовые read-модели, пересобираемые через replay, поэтому чтения eventually consistent. Snapshot ограничивают стоимость replay, но молча дрейфуют без защиты. Версионирование вечно — старые формы живут всегда, поэтому делаешь upcast, а не переписывание. А GDPR поверх неизменяемого лога решается crypto-shredding с реальной юридической оговоркой. Каждый ответ сводится к одному правилу: никогда не мутируй лог; выводи, проигрывай и преобразуй вокруг него.
</Recap>
