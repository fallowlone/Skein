---
concepts:
- columnar-storage
- row-groups
- predicate-pushdown
- encoding-compression
- small-files-problem
- table-formats
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://parquet.apache.org/docs/concepts/
- https://arrow.apache.org/blog/2022/12/26/querying-parquet-with-millisecond-latency/
status: ready
summary: "Синтез всего юнита Parquet в формате с выбором ответа — колоночная раскладка, статистики футера и pushdown, encoding против compression, размер row group и ловушка small files."
title: 'Parquet: тест с выбором ответа'
track: data-engineering
unit: 03-parquet
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, проходящих сквозь весь юнит. Каждый — это решение, которое ты принимаешь, проектируя таблицу в озере данных: не определение для пересказа, а компромисс, который надо взвесить против реального запроса и реальной цены.
</Hook>

<Goal lang="ru">
Убедись, что умеешь связать колоночную раскладку, статистики футера, encoding против compression, размер row group и продакшен-ловушки — синтез, к которому вёл урок.
</Goal>

<Quiz
  id="03-parquet-choice-1"
  lessonSlug="03-parquet"
  lang="ru"
  question="Таблица events из 40 колонок запрашивается как SELECT user_id, country WHERE day = '2024-02-01'. Два независимых механизма Parquet делают это гораздо дешевле, чем CSV. Какие?"
  choices={[
    { label: "Column pruning читает только column chunk колонок user_id и country, а row-group skipping использует min/max по day из футера каждой row group, чтобы пропустить группы, диапазон которых не может содержать эту дату.", correct: true },
    { label: "Встроенный вторичный индекс отображает каждое значение day прямо в его байтовое смещение, и движок прыгает сразу на нужные строки.", misconception: "В Parquet нет внешнего индекса значение-в-смещение. Пропуск идёт от min/max/null-count статистик в собственном футере файла по каждой row group плюс опциональные page indexes — это грубый отсев по диапазону, а не точный seek." },
    { label: "Parquet распаковывает snappy сильно быстрее, чем CSV распаковывает gzip, так что тот же полный скан просто заканчивается раньше.", misconception: "Выигрыш в том, что читается меньше, а не в скорости распаковки. Column pruning и row-group skipping означают, что большинство байтов вообще не читается и не распаковывается; более быстрый кодек вторичен." },
    { label: "Parquet хранит всю таблицу глобально отсортированной, и бинарный поиск находит нужные строки за логарифм.", misconception: "Parquet не сортирует данные глобально за тебя; кластеризацию ты устраиваешь при записи. Pruning — это отсев row group и pages по диапазону через статистики, а не бинарный поиск." },
  ]}
/>

<Quiz
  id="03-parquet-choice-2"
  lessonSlug="03-parquet"
  lang="ru"
  question="В таблице есть статистики по day на каждую row group, но WHERE day = '2024-02-01' всё равно сканирует почти все row groups. Какая причина наиболее вероятна?"
  choices={[
    { label: "Значения day разбросаны по row groups, поэтому min/max каждой группы охватывает почти весь год и ничего нельзя пропустить — данные не кластеризованы по колонке фильтра.", correct: true },
    { label: "Статистики min/max работают только для числовых колонок, поэтому предикат по дате не может отсеивать.", misconception: "Parquet держит min/max для упорядоченных типов, включая даты и строки; pruning для них работает. Проблема здесь — перекрытие диапазонов из-за разбросанных значений, а не тип колонки." },
    { label: "Predicate pushdown выключен, пока ты дополнительно не построишь отдельный Bloom filter на колонку.", misconception: "Pushdown по диапазону из min/max футера работает без Bloom filter. Bloom filter помогает равенству на колонках высокой кардинальности; он не предпосылка для отсева по диапазону дат." },
    { label: "Файл использует snappy вместо zstd, а статистики раскрывают только zstd-файлы.", misconception: "Статистики живут в футере независимо от кодека сжатия. Выбор кодека влияет на размер и скорость чтения, но никогда на то, существуют ли статистики и могут ли они отсеивать." },
  ]}
/>

<Quiz
  id="03-parquet-choice-3"
  lessonSlug="03-parquet"
  lang="ru"
  question="Коллега говорит: 'Parquet сжимает данные, значит encoding и compression — это одно и то же.' Какая точная поправка?"
  choices={[
    { label: "Это два наложенных, отдельных слоя. Encoding учитывает тип и структурен (dictionary, RLE, bit-packing, delta); затем универсальный кодек (snappy/zstd/gzip) сжимает уже закодированные байты. Encoding сам по себе уменьшает данные ещё до запуска кодека.", correct: true },
    { label: "Верно — 'encoding' это просто маркетинговое название шага сжатия snappy/zstd.", misconception: "Encoding и compression различны. Dictionary/RLE/delta encoding переструктурирует значения по типу до того, как байтовый кодек вообще запустится, и сам по себе даёт значительную часть выигрыша по размеру." },
    { label: "Encoding происходит только когда ты выбираешь gzip; snappy-файлы хранятся без всякого encoding.", misconception: "Encoding независим от выбора кодека. Snappy-файл всё равно использует dictionary/RLE/bit-packing на своих pages; кодек накладывается поверх того encoding, что был применён." },
    { label: "Это одно и то же, потому что оба lossless, так что различать их — академизм.", misconception: "Оба lossless, но это разные механизмы с разными ручками и разными режимами отказа — dictionary blowup это проблема encoding, а CPU-цена кодека это проблема compression. Смешение скрывает реальные решения по тюнингу." },
  ]}
/>

<Quiz
  id="03-parquet-choice-4"
  lessonSlug="03-parquet"
  lang="ru"
  question="Ты включаешь dictionary encoding на колонке случайных UUID. Какой вероятный исход и почему?"
  choices={[
    { label: "Словарь вырастает за свой page-size бюджет, потому что значения почти не повторяются, Parquet откатывается на plain encoding, и файл может оказаться не меньше — иногда больше — чем при одном plain.", correct: true },
    { label: "UUID сжимаются идеально, потому что каждое значение заменяется коротким целым кодом.", misconception: "Dictionary encoding окупается только когда значения повторяются. С почти уникальными UUID словарь становится таким же большим, как сама колонка, переполняет бюджет, и Parquet его бросает." },
    { label: "Колонка тихо повреждается, потому что UUID нельзя dictionary-кодировать.", misconception: "Повреждения нет — Parquet просто откатывается на plain encoding для этой колонки. Цена это потраченный CPU и, возможно, больший файл, но никогда не корректность." },
    { label: "Ничего не меняется; dictionary encoding всегда бесплатно включать.", misconception: "На данных высокой кардинальности это не бесплатно: ты платишь за построение словаря, который ничего не уменьшает, а затем его выбрасываешь, что может дать файл больше, чем дал бы plain encoding." },
  ]}
/>

<Quiz
  id="03-parquet-choice-5"
  lessonSlug="03-parquet"
  lang="ru"
  question="Kafka-консьюмер сбрасывает крошечный Parquet-файл в S3 каждые 10 секунд. Дашборды над таблицей стали мучительно медленными. Какое решение даёт наибольший рычаг?"
  choices={[
    { label: "Запустить периодический compaction-джоб, который перезаписывает рой крошечных файлов в файлы размером 128 МБ — 1 ГБ — именно листинг и чтение футеров миллионов объектов доминирует в планировании запроса.", correct: true },
    { label: "Переключить файлы с snappy на zstd, чтобы они были на ~15-20% меньше.", misconception: "zstd это реальный выигрыш по размеру за крошечную плату на чтении, но он ничего не делает с количеством файлов. Узкое место — листинг и чтение миллионов футеров при планировании, а не байты на файл: верная идея, не та проблема." },
    { label: "Поднять размер row group у писателя до 1 ГБ, чтобы каждый файл держал одну большую row group.", misconception: "Сброс раз в 10 секунд имеет лишь пару КБ для записи, так что он не заполнит row group в 1 ГБ — ты по-прежнему получаешь много крошечных файлов, теперь с крошечными row groups. Размер row group важен для больших батч-записей, а не для высокочастотного стримингового писателя." },
    { label: "Перевести таблицу в CSV, чтобы не было оверхеда на чтение футера в каждом файле.", misconception: "Убрав футер, ты убираешь column pruning и predicate pushdown, так что каждый запрос полностью сканирует и парсит — гораздо хуже. И у тебя всё равно останутся миллионы мелких файлов для листинга. Это меняет вопрос тюнинга на фундаментальный." },
  ]}
/>

<Quiz
  id="03-parquet-choice-6"
  lessonSlug="03-parquet"
  lang="ru"
  question="Сырые Parquet-файлы в директории дают тебе колоночное хранение и pushdown. Что добавляют сверху Iceberg, Delta Lake и Hudi и почему это важно?"
  choices={[
    { label: "Слой метаданных/манифестов над Parquet-файлами: ACID-коммиты, снапшоты и time travel, schema evolution только на уровне метаданных, и pruning на уровне манифеста, который отсеивает целые файлы по их статистикам ещё до открытия какого-либо футера.", correct: true },
    { label: "Более быстрый колоночный формат хранения, который полностью заменяет Parquet.", misconception: "Они не заменяют Parquet — они держат данные в Parquet и добавляют слой таблицы сверху. Хранение остаётся колоночным Parquet; формат добавляет транзакции, снапшоты и метаданные для планирования." },
    { label: "Встроенное сжатие, которое делает нижележащие Parquet-файлы меньше.", misconception: "Сжатие по-прежнему работа Parquet (snappy/zstd/gzip). Table formats добавляют корректность и планирование — манифест текущего набора файлов, ACID, time travel — а не новый кодек." },
    { label: "Ничего функционального — это просто соглашение об именовании директорий для партиций.", misconception: "Это гораздо больше, чем именование: манифест отслеживает, какие именно файлы принадлежат таблице прямо сейчас, с их статистиками, что даёт ACID-коммиты, time travel и отсев на уровне файла, недоступный простому листингу директории." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — один цикл проектирования: колоночная раскладка даёт column pruning, min/max в футере дают пропуск row group и pages (predicate pushdown) — но только когда данные кластеризованы по колонкам фильтра. Encoding и compression — отдельные наложенные выигрыши, и dictionary encoding бьёт по рукам на колонках высокой кардинальности. Повторяющиеся продакшен-ловушки — это small-files problem (лечится compaction, а не кодеком получше) и размер row group. А поскольку у сырого Parquet нет транзакций, table formats — Iceberg, Delta Lake, Hudi — оборачивают его слоем манифеста ради ACID, time travel, безопасной schema evolution и pruning на уровне манифеста. Теперь, когда увидишь медленную таблицу озера, у тебя есть чеклист: кластеризованы ли данные, заданы ли row groups по байтам, предикат в читателе, кодек соответствует паттерну доступа?
</Recap>
