---
concepts:
- columnar-storage
- predicate-pushdown
- encoding-compression
- row-group-sizing
- schema-evolution
- table-formats
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://parquet.apache.org/docs/concepts/
- https://arrow.apache.org/blog/2022/12/26/querying-parquet-with-millisecond-latency/
status: ready
summary: "Промпты на свободное припоминание по всему юниту Parquet — колоночная раскладка, pushdown через футер, encoding против compression, размер row group, schema evolution и table formats."
title: 'Parquet: тест на припоминание'
track: data-engineering
unit: 03-parquet
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание бьёт перечитывание. На каждый промпт скажи или запиши полный ответ по памяти, прежде чем открыть модельный ответ — именно усилие припоминания закрепляет решения по раскладке.
</Hook>

<Goal lang="ru">
Восстанови ключевые механизмы юнита — колоночную раскладку, pushdown через футер, разделение encoding/compression, размер row group, schema evolution и то, что добавляют table formats — не заглядывая в урок.
</Goal>

<RetrievalDrawer
  client:visible
  id="03-parquet-short-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни от начала до конца, почему фильтрованный запрос с проекцией на Parquet читает гораздо меньше, чем тот же запрос на CSV.", a: "Два независимых механизма складываются. Первый, column pruning: поскольку Parquet колоночный, все значения одной колонки лежат вместе в column chunk, так что запрос, которому нужны две из сорока колонок, читает только эти два chunk и не касается остального — CSV строчный, и чтение любой колонки означает проход каждого байта каждой строки с парсингом разделителей. Второй, row-group skipping через predicate pushdown: футер хранит min/max/null-count статистики по колонке на каждую row group, так что для WHERE day = '2024-02-01' движок проверяет диапазон day каждой группы и пропускает любую, чей диапазон не может содержать значение, не читая ни байта данных; опциональные page-level статистики толкают это на уровень глубже. У CSV нет статистик и встроенных типов, так что единственный способ узнать, подходит ли строка, — прочитать и распарсить её, то есть вынужденный полный скан. Оговорка: пропуск окупается только когда данные отсортированы или партиционированы по колонкам фильтра, чтобы диапазоны были узкими и непересекающимися." },
    { q: "Опиши физическую вложенность внутри Parquet-файла, от файла вниз до закодированных значений.", a: "Parquet-файл вложен на четыре уровня и заканчивается футером. Файл разбит на row groups — горизонтальные срезы строк с целевым размером по умолчанию около 128 МБ. Внутри каждой row group каждая колонка хранится как непрерывный column chunk, так что все значения одной колонки физически вместе. Каждый column chunk разбит на pages, единицу encoding и compression, около 1 МБ по умолчанию. Значения page сначала кодируются по типу (dictionary, RLE, bit-packing, delta), а затем сжимаются универсальным кодеком (snappy, zstd, gzip). Футер в конце держит схему плюс метаданные по row group и колонке, включая min/max/null-count статистики, которые движут pushdown — поэтому читатель открывает футер первым." },
    { q: "Чем отличаются encoding и compression в Parquet и почему держать их в голове раздельно?", a: "Это два наложенных, различных слоя. Encoding учитывает тип и структурен: dictionary encoding заменяет повторяющиеся значения короткими целыми кодами (отлично для строк низкой кардинальности), RLE схлопывает прогоны одинаковых значений, bit-packing использует ровно столько бит, сколько нужно диапазону, а delta encoding хранит разности для отсортированных целых и таймстемпов. Compression — это универсальный байтовый кодек — snappy, zstd или gzip — прогоняемый поверх уже закодированных байтов. Держать их раздельно важно, потому что у них разные ручки и разные режимы отказа: dictionary blowup на колонке высокой кардинальности — проблема encoding, решаемая выключением словаря, а медленное чтение из gzip — проблема кодека compression, решаемая переходом на snappy или zstd. Encoding сам по себе часто даёт значительную часть выигрыша по размеру ещё до запуска кодека." },
    { q: "Что такое small-files problem, почему она калечит планирование запросов и как помогают table formats?", a: "Это случается, когда джоб — обычно стриминговый писатель, сбрасывающий данные по короткому интервалу, — производит огромное число крошечных Parquet-файлов вместо нескольких больших. Каждый файл несёт свой футер и стоит одного I/O только чтобы открыться. Прежде чем движок спланирует запрос, он должен перечислить файлы и прочитать каждый футер ради статистик; на облачных объектных хранилищах рекурсивный листинг миллионов объектов медленный и дорогой, а чтение миллионов футеров доминирует в планировании, так что запрос медленный ещё до скана данных. Решение — compaction: периодический фоновый джоб переписывает рой в файлы по 128 МБ — 1 ГБ. Table formats — Iceberg, Delta Lake, Hudi — решают это напрямую: они держат данные в Parquet, но добавляют слой манифеста, отслеживающий текущий набор файлов с их статистиками, так что движок отсеивает целые файлы до открытия любого футера, и они везут compaction плюс ACID-коммиты, снапшоты и schema evolution на уровне метаданных." },
    { q: "Как выбирать размер row group и что идёт не так на каждом краю?", a: "Задавай размер row group в байтах, а не в числе строк, целясь примерно в диапазон 128 МБ — 1 ГБ для батч-записей. Слишком велик — теряешь гранулярность пропуска (фильтр может пропустить только целые группы, так что гигантскую группу редко удаётся пропустить) и нужна большая память, чтобы буферизовать одну группу при записи. Слишком мал — оверхед метаданных и футеров раздувается: движок читает больше футеров, чем данных, цена планирования доминирует, и ты дрейфуешь к small-files problem даже внутри одного файла. Правильный размер зависит от ширины колонок и типичной селективности фильтра — более узкие, более селективные фильтры выигрывают от меньших групп ради более тонкого пропуска, а широкие сканы предпочитают большие группы ради меньшего числа чтений футеров." },
    { q: "Почему schema evolution это ловушка с сырым Parquet и как table formats делают её безопасной?", a: "Сырой Parquet — это просто файлы в директории, каждый несёт свою схему. Добавление, переименование или перестановка колонок по тысячам файлов оставляет читателей примирять несовпадающие схемы во время запроса — добавленная позже колонка отсутствует (null) в старых файлах, а переименованное-и-переиспользованное имя колонки может тихо смешать два разных смысла. Нет единого источника правды о текущей схеме таблицы или о том, какие файлы ей принадлежат. Table formats чинят это слоем метаданных, отслеживающим схему по стабильным ID колонок, а не по именам, так что переименование становится операцией только над метаданными вместо полной перезаписи данных, а манифест фиксирует, какие именно файлы и какая версия схемы текущие. Это даёт безопасную атомарную schema evolution плюс time travel для чтения таблицы на момент более ранней схемы." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти — ты держишь хребет юнита: Parquet колоночный и самоописывающий, так что pruning и pushdown читают только то, что нужно запросу — но лишь когда данные кластеризованы по колонкам фильтра. Файл вложен файл — row group — column chunk — page, и каждая page сначала кодируется (структурный, типозависимый слой), затем сжимается (байтовый кодек) — два отдельных выигрыша с отдельными режимами отказа. Размер row group — реальная ручка с плохими краями в обе стороны, small-files problem лечится compaction, а поскольку у сырого Parquet нет транзакций и стабильной идентичности схемы, table formats оборачивают его манифестом ради ACID, безопасной schema evolution, time travel и отсева на уровне файла. Теперь, когда откроешь медленную таблицу озера в продакшене, потянешься к статистикам футера, кластеризации и пути доступа первым делом — а не к железу.
</Recap>
