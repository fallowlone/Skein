---
concepts:
- oltp-olap-split
- elt-transform
- parquet-pruning
- materialized-view-staleness
- dual-write-outbox
- vector-recall
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.confluent.io/blog/dual-write-problem/
- https://docs.getdbt.com/docs/deploy/source-freshness
- https://www.e6data.com/blog/apache-iceberg-snapshots-time-travel
status: ready
summary: "Сквозной синтез: выбери правильное хранилище, формат и индекс под нагрузку — через OLTP/OLAP, ELT, Parquet, MV, event sourcing, поиск и векторы."
title: 'Дата-платформа: тест с выбором ответа'
track: data-engineering
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, прошивающих весь трек. Каждый — проектное решение, которое ты принимаешь, когда один факт обязан жить сразу в нескольких хранилищах: не определение, а выбор хранилища, формата или контракта под реальную нагрузку.
</Hook>

<Goal lang="ru">
Убедись, что умеешь направить нагрузку в подходящее хранилище и раскладку и рассуждать о стыках между ними — к этому синтезу вели юниты про OLTP/OLAP, ELT, Parquet, MV, event sourcing, поиск и векторы.
</Goal>

<Quiz
  id="08-putting-it-together-choice-1"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Один факт о товаре должен обслуживать точечные lookups на пути оформления заказа И полный скан таблицы для подсчёта выручки в аналитике. Какая архитектура верна для senior?"
  choices={[
    { label: "Два хранилища по нагрузке: строковое OLTP как система записи для индексированных точечных записей/чтений и колоночный склад (Parquet/Iceberg), питаемый через CDC, для сканов. Одна раскладка не выигрывает оба паттерна доступа.", correct: true },
    { label: "Один Postgres с колоночным расширением, обслуживающий оба запроса напрямую.", misconception: "Колоночное расширение помогает сканам, но ты по-прежнему платишь write amplification строкового стора на OLTP-пути и привязываешь аналитическую нагрузку к транзакционной БД. Первый юнит трека ровно об этом: одна раскладка не оптимизирует и крошечные индексированные записи, и огромные колоночные сканы." },
    { label: "Запускать аналитику на read-репликах OLTP, чтобы раскладка осталась прежней.", misconception: "Реплика копирует ту же строковую раскладку, так что скан выручки всё равно читает каждую колонку каждой строки с диска. Реплики изолируют нагрузку, а не паттерн доступа — скан остаётся медленным, потому что раскладка под него неверная." },
    { label: "Держать всё в складе и пусть он же обслуживает точечные lookups оформления.", misconception: "Колоночные сторы отвечают на одно-строчный lookup, трогая множество row groups и реконструируя колонки — высокая задержка на строку и отсутствие настоящего точечного индекса. Путь оформления требует OLTP; склад — для сканов." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-2"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Команда выбирает между ETL (трансформация в отдельном движке до загрузки) и ELT (загрузка сырого, трансформация внутри склада через dbt). Данные грязные, а бизнес постоянно меняет определение «активного пользователя». Что подходит и почему?"
  choices={[
    { label: "ELT: приземляешь сырой bronze, трансформируешь в silver/gold внутри склада. Можно переиграть трансформацию по сохранённым сырым данным каждый раз при смене определения, а тяжёлую работу дёшево делает вычислитель склада.", correct: true },
    { label: "ETL: трансформировать заранее, чтобы приземлялись только чистые строки, экономя место в складе.", misconception: "Трансформация до загрузки выбрасывает сырые данные, поэтому при смене определения «активного пользователя» переиграть нельзя — придётся переподтягивать из источника, если он ещё хранит данные. ELT держит сырой bronze именно ради того, чтобы переопределение было re-run, а не re-ingest." },
    { label: "ETL, потому что трансформации внутри склада не выражают сложную логику очистки.", misconception: "Современные склады + dbt выражают сколь угодно сложные SQL/UDF-трансформации. Индустрия перешла на ELT, потому что вычисления склада стали дёшевы и эластичны; где исполняется T — это решение про cost/replay/fidelity, а не потолок возможностей." },
    { label: "Ни то ни другое — написать трансформацию один раз в приложении, до попадания в БД.", misconception: "Перенос логики бизнес-определений в приложение привязывает её к деплоям и полностью теряет сырой аудит-след. Смысл разбиения bronze/silver/gold в том, что определения живут в версионируемых, переигрываемых трансформациях поверх сохранённых сырых данных." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-3"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Ночной запрос дашборда фильтрует event_date = '2026-05-01' AND country = 'US' по таблице Parquet/Iceberg на 2 ТБ и всё равно сканирует почти все данные. Что даёт наибольший рычаг?"
  choices={[
    { label: "Партиционировать/сортировать данные так, чтобы event_date и country были prunable, и опираться на min/max-статистику в футере Parquet по row group, чтобы движок пропускал файлы и row groups, которые не могут совпасть с предикатом.", correct: true },
    { label: "Добавить вторичный B-tree индекс на country, как сделал бы в OLTP.", misconception: "У Parquet нет построчного B-tree индекса; pruning делается раскладкой партиций плюс статистикой футера по row group. Инстинкт индексирования из OLTP не переносится — ты делаешь предикаты prunable, а не индексируешь." },
    { label: "Конвертировать таблицу обратно в строковый формат, чтобы фильтры были быстрее.", misconception: "Строковый формат делает колоночный скан хуже: читаешь каждую колонку каждой строки. Колоночный плюс статистика футера — это и есть то, что позволяет движку читать только event_date/country и пропускать несовпадающие row groups." },
    { label: "Закэшировать всю таблицу в память, чтобы сканы шли на скорости RAM.", misconception: "2 ТБ редко влезают в память, и ты всё равно просканируешь всё; кэширование маскирует настоящую проблему. Pruning означает вообще не читать ненужные 99%, что и дают статистика футера плюс партиционирование." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-4"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Gold materialized view, обслуживающая дашборд, обновляется каждые 6 часов. Финансовый лид жалуется, что число «неверное» против живого SQL-подсчёта. Оба внутренне корректны. Что в дизайне сделано не так?"
  choices={[
    { label: "Нет объявленного freshness SLA. MV меняет задержку чтения на устаревание; число корректно по состоянию на последний refresh и устарело относительно «сейчас». Объяви окно свежести и вали пайплайн, когда данные старше SLA.", correct: true },
    { label: "В определении MV баг в агрегирующем SQL.", misconception: "Если оба числа внутренне корректны, разрыв — во времени, а не в логике. MV права для своего 6-часовой давности снимка, а живой подсчёт прав для «сейчас» — проверь и объяви свежесть прежде, чем подозревать трансформацию." },
    { label: "Materialized views вообще нельзя использовать для финансовых чисел.", misconception: "MV нормальны для финансов, когда устаревание подходит под вопрос. Провал — оставить устаревание необъявленным и без мониторинга, а не сам факт использования MV: управляемая цена — это refresh и устаревание, которое он протекает." },
    { label: "Переключить MV на refresh при каждой записи, чтобы она всегда была актуальна.", misconception: "Refresh на каждую запись убивает смысл MV и может стоить дороже исходного запроса. Senior-ход — объявить SLA под нужную вопросу свежесть, а не гнаться за нулевым устареванием." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-5"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Сервис пишет заказ в Postgres, потом публикует 'OrderPlaced' в Kafka, чтобы среагировали поиск и аналитика. Иногда поисковый индекс так и не узнаёт о заказе. В чём корневая причина и фикс?"
  choices={[
    { label: "Проблема dual-write: две записи поверх БД и брокера без общей транзакции, так что падение между коммитом и публикацией теряет событие. Фикс: transactional outbox (строка + событие в одной транзакции), доставляемый через CDC, с идемпотентными консьюмерами и периодическим джобом реконсиляции.", correct: true },
    { label: "Kafka теряет сообщения; поднять replication factor у брокера.", misconception: "Событие теряется до того, как достигнет Kafka — процесс умер после коммита в Postgres, но до публикации. Durability брокера не спасёт событие, которое никогда не было отправлено; нужно сделать запись и событие атомарными через outbox." },
    { label: "Публиковать в Kafka первым, потом писать в Postgres, чтобы событие не терялось.", misconception: "Это лишь инвертирует dual-write: теперь падение после публикации, но до коммита, испускает событие для заказа, которого нет. Перестановка двух не-транзакционных записей никогда не закрывает разрыв — только одна атомарная запись (outbox) плюс реконсиляция." },
    { label: "Пусть поисковый индекс опрашивает Postgres раз в минуту вместо потребления событий.", misconception: "Поллинг вручную пересобирает CDC, ограничивает свежесть интервалом опроса и пропускает удаления, если не опрашивать ещё и tombstones. Фикс — сделать путь событий надёжным (outbox плюс идемпотентный консьюмер), а реконсиляцию оставить как страховку." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-6"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Поиск по каталогу должен находить опечатанные названия товаров, А RAG-ассистент — отвечать на «ноутбук, хороший для видеомонтажа». Одна команда предлагает использовать векторный индекс для обоих. Какое разделение верно?"
  choices={[
    { label: "Два индекса под две задачи: полнотекстовый стор на inverted index (ранжирование в стиле BM25) для лексического/ключевого совпадения и векторный ANN-индекс для семантического сходства. Лексический поиск требует совпадения термов; семантический retrieval — эмбеддингов, и многие команды гибридно ранжируют оба.", correct: true },
    { label: "Использовать только векторный индекс — эмбеддинги поглощают поиск по ключевым словам.", misconception: "Эмбеддинги ловят семантику, но размывают точные токены, SKU и редкие термы, так что точный запрос по номеру модели деградирует. Inverted index силён в точном/лексическом совпадении и ранжировании BM25; векторы — в смысле. Они решают разные половины релевантности." },
    { label: "Использовать только inverted index — BM25 умеет ранжировать и семантическое сходство.", misconception: "BM25 ранжирует по перекрытию термов, так что «хороший для видеомонтажа» не совпадёт с описанием «высокопроизводительный GPU и 32 ГБ RAM» без общих слов. Семантический retrieval требует векторного сходства, а не частоты термов." },
    { label: "Пропустить оба и гонять SQL LIKE по подстроке против Postgres.", misconception: "Скан по подстроке не ранжирует, не терпит опечаток, не совпадает семантически и полностью сканирует OLTP-стор. Поиск — это совпадение плюс ранжирование, а семантический retrieval — это сходство; ни то ни другое не SQL LIKE." },
  ]}
/>

<Recap lang="ru">
Сквозная нить трека — одна привычка: направь каждую нагрузку в подходящие хранилище и раскладку, а потом спроектируй контракт на каждом стыке. Строковое OLTP для точечных записей, колоночный Parquet для сканов (с pruning по статистике футера), ELT поверх сохранённого сырого ради переигрываемых определений, MV ради задержки чтения с объявленным freshness SLA, outbox против dual-write, inverted index для лексического поиска и векторный ANN для семантического retrieval. Каждое хранилище корректно для своей задачи; система остаётся корректной, только когда ты владеешь схемой, гарантией доставки, freshness SLA и реконсиляцией между ними.
</Recap>
