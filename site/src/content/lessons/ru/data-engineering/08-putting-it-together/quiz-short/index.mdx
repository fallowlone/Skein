---
concepts:
- oltp-olap-split
- elt-replay
- parquet-pruning
- materialized-view-staleness
- event-sourcing
- vector-recall
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.confluent.io/blog/dual-write-problem/
- https://docs.getdbt.com/docs/deploy/source-freshness
- https://www.e6data.com/blog/apache-iceberg-snapshots-time-travel
status: ready
summary: Промпты на свободное припоминание сквозь весь трек data engineering. Сначала ответь своими словами, потом раскрой образцовый ответ и сравни.
title: 'Дата-платформа: свободное припоминание'
track: data-engineering
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. Для каждого промпта восстанови полный ответ по памяти, прежде чем открыть образцовый — усилие собрать весь трек воедино и есть то, что сплавляет отдельные хранилища в одну ментальную модель.
</Hook>

<Goal lang="ru">
Восстанови хребет трека, не подглядывая: почему OLTP и OLAP разделяются, где исполняется трансформация, как Parquet прунит, чем жертвует MV, почему лог — источник истины и как поиск и векторы делят релевантность — и как проектируются стыки между ними.
</Goal>

<RetrievalDrawer
  client:load
  id="08-putting-it-together-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему одна раскладка хранения не может обслуживать и OLTP-путь оформления, и OLAP-сканы выручки, и каков стандартный ответ из двух хранилищ?", a: "OLTP хочет крошечные индексированные чтения и записи целых строк — найти один заказ, обновить его статус — что строковая раскладка обслуживает дёшево, потому что колонки строки лежат рядом на странице. OLAP хочет сканировать одну-две колонки по миллиардам строк — sum amount за 90 дней — что строковая раскладка обслуживает ужасно, потому что тянет с диска и все неиспользуемые колонки. Колоночная раскладка хранит каждую колонку непрерывно, так что скан читает только затронутые колонки и хорошо их жмёт, но плохо отвечает на одно-строчный lookup. Ни одна раскладка не выигрывает оба, поэтому стандартная архитектура — два хранилища: строковое OLTP как система записи для транзакционного пути и колоночный склад (Parquet/Iceberg), питаемый из него через CDC, для аналитики. Это разделение — фундамент всего трека." },
    { q: "Что реально даёт выбор ELT вместо ETL и какова цена?", a: "ETL трансформирует в отдельном движке до загрузки, так что приземляются только очищенные, приведённые данные; ELT сначала грузит сырое, а трансформирует внутри склада (обычно слоями bronze/silver/gold в dbt). Выигрыш ELT — переигрываемость и точность: поскольку сырой bronze сохранён, при смене бизнес-определения — что считать «активным пользователем», как атрибутировать выручку — ты переигрываешь трансформацию по истории, а не переподтягиваешь из источника, у которого данных может уже не быть. Он также едет на дешёвом эластичном вычислителе склада, поэтому индустрия перешла на ELT. Цена — хранение (держишь сырое) и governance (сырые данные грязные и требуют контроля доступа). Где исполняется T — это по сути решение cost-vs-replay-vs-fidelity, а не вопрос возможностей." },
    { q: "Как Parquet пропускает данные и какая ошибка в коде тихо это ломает?", a: "Parquet колоночный и самоописывающийся: каждый файл разбит на row groups, а футер файла хранит статистику по каждой row group, включая min/max каждой колонки. Когда у запроса есть предикат вроде country = 'US' или диапазон по event_date, движок сначала читает футер и пропускает любую row group, чей диапазон min/max не может содержать совпадения — predicate pushdown / pruning — и читает только колонки, которые запрос проецирует. Партиционирование таблицы по колонке (например, event_date) прунит на уровне файла/директории сверху. Классическая ошибка, убивающая pruning, — обернуть колонку в функцию в предикате — upper(country) = 'US' — потому что статистика футера на сырой колонке, а не на upper(column), так что движок больше не может её использовать и сваливается в скан. Держи предикаты sargable: сравнивай сырую колонку или нормализуй на записи." },
    { q: "Чем жертвует materialized view и как держать этот компромисс честным в продакшене?", a: "Materialized view хранит предвычисленный результат запроса, так что чтения быстрые и дешёвые — тяжёлую агрегацию платишь один раз на refresh, а не на каждом чтении. То, чем ты жертвуешь и чем обязан активно управлять, — устаревание: между refresh-ами MV отдаёт число, корректное по состоянию на последний refresh и устаревшее относительно движущегося источника. Так что у вопроса «дашборд корректен?» нет ответа без «по состоянию на когда?». Держишь честным так: объявляешь freshness SLA на каждый источник (warn_after / error_after у dbt), гоняешь freshness-проверку как минимум вдвое чаще самого жёсткого SLA и валишь пайплайн, когда данные старше допустимого — чтобы заглохший фид ловился, а не тихо отдавал устаревшие числа. Вторая управляемая цена — хранение и вычисления refresh." },
    { q: "Почему append-only лог событий — источник истины в event sourcing и как текущее состояние связано с ним?", a: "Вместо хранения текущего состояния и его перезаписи на каждом изменении event sourcing хранит неизменяемый append-only поток событий, породивших состояние — OrderPlaced, ItemShipped, OrderRefunded. Текущее состояние — это left-fold (свёртка) над логом: проиграй события по порядку и реконструируешь состояние любой сущности на любой момент. Это даёт полный аудит-след для комплаенса, возможность пересобрать производные read-модели (поисковые индексы, MV, проекции) переигрыванием и time-travel отладку. Дисциплина, которую он требует, — порядок и идемпотентность: события должны нести id и версию/последовательность, чтобы консьюмеры дедуплицировали и упорядочивали по последовательности, а не по времени прибытия — иначе события не по порядку или повторно сыгранные удваивают счёт, ровно так выручка считается дважды на стыке." },
    { q: "Как полнотекстовый поиск и векторный поиск делят задачу релевантности и почему зрелые системы гоняют оба?", a: "Полнотекстовый поиск использует inverted index: токенизирует и анализирует текст, отображает каждый терм на документы, его содержащие, для быстрого подбора кандидатов, потом ранжирует схемой вроде BM25 — так он силён в точном и лексическом совпадении, редких токенах, SKU и опечатко-устойчивых терм-запросах. Векторный поиск встраивает текст в высокоразмерное пространство и делает приближённый поиск ближайших соседей (ANN), так что ловит семантическое сходство — «хороший для видеомонтажа» совпадает с «высокопроизводительный GPU» без общих слов — что питает семантический поиск и RAG. ANN тихо меняет recall на задержку через параметры вроде ef_search, так что неверные параметры всё равно возвращают ранжированные строки, просто не те. Они решают разные половины релевантности: лексическую точность против семантического смысла. Зрелые системы гибридно ранжируют оба и, что критично, фильтруют векторные hits против живых OLTP-id, чтобы устаревший эмбеддинг не вытащил удалённую сущность." },
  ]}
/>

<Recap lang="ru">
Если ты восстановил каждый ответ, ты держишь хребет трека: OLTP и OLAP разделяются, потому что ни одна раскладка не выигрывает оба паттерна доступа; ELT покупает переигрываемость, сохраняя сырое; Parquet прунит через статистику футера, пока функция не спрячет колонку; MV меняет устаревание на скорость чтения и требует объявленного freshness SLA; лог событий — источник истины, а состояние — свёртка над ним; поиск и векторы делят лексическую и семантическую релевантность. И главное — каждое хранилище корректно для своей задачи; система остаётся корректной, только когда ты проектируешь контракт, гарантию доставки, свежесть и реконсиляцию на каждом стыке между ними.
</Recap>
