---
slug: 01-overview
lang: ru
track: data-engineering
unit: 08-putting-it-together
order: 1
level: junior
lessonType: topic
status: ready
title: "Собираем вместе: система ломается на стыках, а не в хранилищах"
summary: "Проследи один продукт через OLTP, склад, dbt, MV, лог событий, поиск и vector index. Каждое хранилище корректно; система расходится там, где встречаются два корректных хранилища — поэтому консистентность, свежесть и lineage это сквозные свойства."
prereqs: []
deepensInto: []
spiral: []
concepts: ["data-contract", "dual-write", "cdc-reconciliation", "freshness-sla", "end-to-end-lineage", "medallion-architecture"]
estMin: 17
sources:
  - https://www.confluent.io/blog/dual-write-problem/
  - https://docs.getdbt.com/docs/deploy/source-freshness
  - https://www.e6data.com/blog/apache-iceberg-snapshots-time-travel
  - https://www.ml4devs.com/what-is/medallion-architecture/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
Поддержка эскалирует тикет: клиент купил ноутбук, который поиск в каталоге всё ещё показывает «в наличии», AI-ассистент всё ещё его рекомендует, а дашборд аналитики считает его в выручке этой недели дважды. Четыре команды открывают четыре расследования. Postgres корректен — заказ есть, продукт soft-deleted. Склад корректен — для снимка, который он последний раз обновил. Поисковый индекс корректен — для последнего CDC-события, которое до него дошло. Vector index корректен — для эмбеддингов, которые он последний раз пересобрал. Каждое хранилище проходит свои unit-тесты. Баг живёт в зазоре между ними, и им не владеет никто.
</Hook>

<Crux>Каждый слой корректен по отдельности; система ломается на стыке, где два корректных хранилища расходятся об одном и том же факте.</Crux>

<Explanation>

## Один факт, семь копий

К моменту, когда продукт существует в зрелой системе, одна и та же строка живёт в семи местах. Postgres — система записи (OLTP): транзакция, которая взяла деньги, и есть истина. CDC стримит эту истину в колоночный склад на Parquet/Iceberg. dbt прогоняет её через bronze (сырой landing), silver (очищенный, согласованный) и gold (бизнес-агрегаты). Materialized views и роллапы обслуживают дашборды без пере-сканирования миллиардов строк. Event-sourced поток аудита фиксирует каждое изменение состояния для комплаенса. Полнотекстовый индекс (Elasticsearch/OpenSearch) питает поиск по каталогу. Vector index питает семантический поиск и RAG. Семь хранилищ, один факт: «доступен ли этот продукт и по какой цене?»

Каждый юнит, который ты изучал раньше в этом треке, оптимизировал своё хранилище под одну задачу — и именно эта оптимизация заставляет копии расходиться. Склад построен под throughput, поэтому батчит; батчинг означает lag. Поисковый индекс построен под latency запросов, поэтому денормализует; денормализация означает, что он не видит изменение foreign-key, пока ему кто-то о нём не скажет. Vector index дорого пересобирать, поэтому эмбеддинги пересчитывают по расписанию, а не на каждую запись. Ничего из этого не баг. Это корректный дизайн каждого хранилища. Системный баг в том, что никто не спроектировал контракт *между* ними.

## Стык — это замаскированная проблема dual-write

Посмотри на самый частый источник drift: твой сервис пишет заказ в Postgres, потом публикует «OrderPlaced» в Kafka, чтобы поиск и аналитика среагировали. Две записи, две системы, общей транзакции нет. Если процесс умрёт после коммита в Postgres, но до публикации, в Postgres заказ есть, а остальная система о нём так и не услышит. Это **проблема dual-write**, и это каноничный отказ стыка — в разборе Confluent прямо сказано, что «нет способа иметь одну атомарную транзакцию» поверх базы и брокера сообщений.

Фикс сеньора — **transactional outbox**: в той же транзакции БД, что пишет заказ, вставляешь строку в таблицу `outbox`. Коммит атомарен — заказ и событие живут или умирают вместе. Отдельный relay (или CDC-коннектор вроде Debezium, читающий WAL) отправляет строки outbox в Kafka с at-least-once доставкой. Ты сменил dual-write на одну запись плюс идемпотентного консьюмера, и идемпотентность не обсуждается: at-least-once означает, что то же событие придёт дважды, поэтому каждое хранилище ниже по потоку обязано дедуплицировать по event id, иначе оно дважды посчитает выручку ровно как в Hook.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Стык (хранилище → хранилище)</th>
        <th class="border border-border px-3 py-2 text-left">Как расходится</th>
        <th class="border border-border px-3 py-2 text-left">Контракт, который чинит</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">OLTP → Kafka → поисковый индекс</td>
        <td class="border border-border px-3 py-2">CDC-событие потеряно; удалённый продукт всё ещё ищется</td>
        <td class="border border-border px-3 py-2">Transactional outbox + идемпотентный консьюмер + периодическая полная сверка</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">OLTP → склад → MV → дашборд</td>
        <td class="border border-border px-3 py-2">Refresh lag: MV отдаёт число, которое источник уже поменял</td>
        <td class="border border-border px-3 py-2">Объявленный freshness SLA + freshness-проверки, валящие build</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">OLTP → vector index (RAG)</td>
        <td class="border border-border px-3 py-2">Эмбеддинги пересобираются ночью; возвращает удалённый час назад продукт</td>
        <td class="border border-border px-3 py-2">Фильтровать выдачу по живым id из OLTP; tombstone при удалении</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">поток событий → gold-агрегат</td>
        <td class="border border-border px-3 py-2">События не по порядку / переигранные считаются дважды</td>
        <td class="border border-border px-3 py-2">Event id + версия; дедуп и сортировка по sequence, а не по приходу</td>
      </tr>
    </tbody>
  </table>
</div>

## Свежесть и lineage — свойства пути, а не хранилища

Спроси «дашборд корректен?» — и единственный честный ответ: «по состоянию на когда?» Gold-MV может быть идеальной для снимка, который она обновила в 02:00, но если возврат прошёл в 09:00, число корректно *и* неверно — корректно для своего снимка, неверно для вопроса, который реально задаёт руководитель. dbt формализует это через **source freshness**: ты объявляешь окно `warn_after` и `error_after` на каждый источник, и freshness-джоб валит пайплайн, когда данные старше твоего SLA. Собственное правило dbt — запускать freshness-проверку как минимум вдвое чаще самого жёсткого SLA, чтобы обнаружить заглохший фид до того, как дашборд проврёт целый час.

Lineage — другое сквозное свойство. Когда число неверно, ты дебажишь не дашборд — ты идёшь по пути назад: gold → silver → bronze → CDC offset → OLTP. Поэтому важны и разделение medallion (bronze/silver/gold), и неизменяемый формат таблиц. Снимки Iceberg делают каждое изменение таблицы полной версионированной точкой во времени, поэтому можно выполнить **time-travel запрос** на ровно том снимке, который прочитал дашборд, и доказать, склад ли был устаревшим или трансформ был неверным. Задержка в bronze каскадит в silver и gold, поэтому один заглохший батч проявляется как «вчерашняя выручка» тремя слоями ниже — и только lineage даёт найти, какой слой заглох.

<Inset kind="why" lang="ru">
Reconciliation-джоб — неэффектная часть, которую никто не закладывает в бюджет и которая нужна всем. CDC рано или поздно потеряет или продублирует событие — рестарт коннектора, заполнившийся и сброшенный WAL-слот, пропущенное poison-сообщение. Фикс не в том, чтобы «сделать CDC идеальным»; это периодическая полная пере-синхронизация, которая считает чексумму (количество строк + хеш ключевых колонок) на партицию в OLTP и в индексе, а потом чинит разницу. Считай живой CDC быстрым путём, а reconciliation — страховочной сеткой; и то, и другое, всегда.
</Inset>

## Ты проектируешь контракт, а не только хранилища

Сдвиг от мидла к сеньору здесь — в том, чья проблема этот стык. Мидл владеет хранилищем и делает его корректным. Сеньор владеет **контрактом данных**: схемой, event id и версией, гарантией доставки, freshness SLA и reconciliation, которая замыкает петлю. Контракт — это то, что позволяет семи независимо-корректным хранилищам сложиться в одну систему, которая согласуется сама с собой.

Конкретно контракт отвечает на четыре вопроса для каждого стыка: какова каноничная схема и кто может её менять (ломающее переименование колонки в OLTP молча занулит колонку ниже по потоку)? Какова гарантия доставки и, значит, должны ли консьюмеры быть идемпотентными (at-least-once → да)? Каков freshness SLA и что валится при его нарушении? И что сверяет drift, как часто и кого зовут по пейджеру? Пропусти любой — и получишь Hook: четыре корректных хранилища, одна некорректная система, владельца нет.

<TradeoffMatrix
  id="08-putting-it-together-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Сервис должен обновить Postgres, а затем сделать изменение видимым в поисковом индексе и на складе. Выбери дизайн интеграции."
  options={[
    { name: "Transactional outbox: пишешь строку + outbox-событие в одной транзакции БД; CDC-relay отправляет события at-least-once консьюмерам, которые дедупят по event id", summary: "Одна атомарная запись.", correct: true, justification: "Это схлопывает dual-write в один атомарный коммит — заказ и его событие живут или умирают вместе — и принимает at-least-once доставку, делая каждого консьюмера идемпотентным. Это стандартный ответ, потому что он переживает падение процесса между записями, ровно тот отказ, что теряет события. Дополни периодическим reconciliation-джобом, чтобы чинить редкое потерянное/задвоенное событие, и стык закрыт от начала до конца." },
    { name: "Dual write: коммитишь в Postgres, потом сразу публикуешь в Kafka из кода приложения", summary: "Две записи подряд.", justification: "Классический отказ стыка. Нет общей транзакции поверх базы и брокера, поэтому падение после коммита, но до публикации оставляет Postgres корректным, а каждое хранилище ниже — в неведении. Работает в демо и молча теряет события в проде под нагрузкой и при рестартах." },
    { name: "Пусть каждое хранилище ниже опрашивает Postgres по расписанию и пересчитывает свой view", summary: "Pull, без событий.", justification: "Рабочее для низких требований к свежести, но не масштабируется: каждый поллер фулл-сканит или ведёт свой high-water mark, свежесть ограничена интервалом опроса, а удаления невидимы, пока не опрашиваешь и tombstone. В итоге переизобретаешь CDC руками, только хуже." },
    { name: "Без событий; ночью полностью пересобирать поиск и склад из дампа Postgres", summary: "Только батч.", justification: "Норм как страховочная сетка reconciliation, неверно как единственный путь. Ночная пересборка означает до 24ч drift — каталог весь день показывает распроданные товары. Реальные системы гоняют CDC ради свежести и периодическую полную пересборку ради корректности; этот дизайн оставляет только медленную половину." },
  ]}
/>

<Quiz
  id="08-putting-it-together-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Продукт soft-deleted в Postgres в 14:00. В 14:30 RAG-ассистент всё ещё его рекомендует. Vector index пересобирает эмбеддинги ночью. Какой фикс сеньора?"
  choices={[
    { label: "Фильтровать полученных кандидатов по живым id из OLTP (или по множеству tombstone) во время запроса, чтобы устаревший эмбеддинг не мог выдать удалённый продукт", correct: true },
    { label: "Пересобирать весь vector index каждую минуту, чтобы он никогда не был устаревшим", misconception: "Пере-эмбеддить весь каталог каждую минуту дико дорого и всё равно оставляет окно; индекс — неправильное место для проверки живости, на которую дёшево отвечает OLTP." },
    { label: "Смириться — векторный поиск приближённый, поэтому какая-то устарелость ожидаема", misconception: "Approximate nearest-neighbour — про ранжирование, а не про возврат удалённых сущностей. Рекомендовать продукт, который клиент не может купить, — баг корректности, а не приемлемое приближение." },
    { label: "Удалить строку hard-delete в Postgres, чтобы CDC распространялся быстрее", misconception: "Hard-delete вообще не меняет latency распространения и уничтожает аудит-трейл; drift на стыке выдачи, чинится фильтрацией, а не сменой удаления в OLTP." },
  ]}
/>

<Quiz
  id="08-putting-it-together-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Дашборд выручки расходится с ручным SQL-подсчётом по Postgres. Оба «корректны». Какова самая вероятная причина?"
  choices={[
    { label: "Refresh lag: gold-MV отдаёт число по состоянию на свой последний снимок обновления, а OLTP-источник с тех пор изменился", correct: true },
    { label: "Баг в dbt-трансформе, который неправильно считает сумму", misconception: "Возможно, но если оба числа внутренне корректны, вероятнее всего зазор во времени, а не в логике — MV верна для своего снимка и устарела для сейчас. Проверь свежесть до переписывания трансформа." },
    { label: "Postgres вернул неверный count", misconception: "OLTP как система записи здесь истина по определению; подозревать её первой — неверный инстинкт, когда замешана копия ниже по потоку." },
    { label: "Инструмент дашборда кеширует страницу", misconception: "Кеш браузера/инструмента может маскировать свежесть, но структурная причина в этом стеке — refresh lag MV против движущегося OLTP-источника; туда смотреть первым." },
  ]}
/>

<DragOrder
  id="08-putting-it-together-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Число на дашборде неверно. Расставь обход lineage, который делает сеньор, чтобы локализовать drift:"
  items={[
    { id: "a", label: "Спроси «по состоянию на когда?» — установи время снимка/обновления, которое прочитал дашборд" },
    { id: "b", label: "Проверь историю обновления gold-MV / агрегата против freshness SLA" },
    { id: "c", label: "Поднимись по medallion: silver устарел, потому что bronze приземлился поздно?" },
    { id: "d", label: "Проверь CDC offset / лаг outbox между OLTP и bronze" },
    { id: "e", label: "Сравни чексумму (количество строк + хеш ключей) OLTP против партиции склада, чтобы подтвердить, где они расходятся" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Каждое хранилище может быть корректным по отдельности, а система — неверной. Консистентность, свежесть и lineage — сквозные свойства: проектируй контракт данных и reconciliation на каждом стыке, а не только хранилища.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="08-putting-it-together-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни коллеге, почему поисковый индекс показывает продукт, который Postgres удалил, хотя оба хранилища проходят свои тесты, и как ты закроешь стык.", a: "Postgres — система записи, и он корректно сделал soft-delete продукта. Поисковый индекс корректно отражает последнее CDC-событие, которое до него дошло. Баг в том, что событие удаления так и не пришло — вероятно, dual-write, который закоммитился в Postgres, но упал до публикации в Kafka, или CDC-коннектор, который рестартнулся и пропустил сегмент WAL. Ни одно хранилище не неверно; стык между ними потерял факт. Фикс из двух половин. Первая — сделать быстрый путь надёжным: используй transactional outbox, чтобы удаление и его событие коммитились в одной атомарной транзакции, и сделай консьюмер индекса идемпотентным, чтобы at-least-once передоставка не применилась дважды. Вторая — прими, что события всё равно иногда исчезают, и добавь reconciliation-джоб: периодически считай чексумму — количество строк плюс хеш ключевых колонок — на партицию в OLTP и в индексе, сравни их и почини недостающие или лишние строки. CDC даёт свежесть; reconciliation даёт корректность. Нужно и то, и другое, потому что прод ломается на стыке, а не в хранилище." },
    { q: "Объясни, почему «дашборд корректен?» — неверный вопрос, и при чём тут свежесть и lineage.", a: "Верный вопрос — «корректен по состоянию на когда?» Gold materialized view может быть идеально посчитана для снимка, который она последний раз обновила, и всё равно расходиться с живым подсчётом по OLTP-источнику, потому что возврат или заказ прошли после этого снимка. Число одновременно корректно (для своего снимка) и неверно (для сейчас) — refresh lag, а не баг логики. Свежесть делает это измеримым: ты объявляешь SLA на источник (warn_after / error_after в dbt) и гоняешь freshness-проверку как минимум вдвое чаще самого жёсткого SLA, чтобы заглохший фид валил пайплайн, а не тихо отдавал устаревшие числа. Lineage — это как ты локализуешь drift, когда он помечен: ты идёшь по пути назад — gold к silver к bronze к CDC offset к OLTP — и поскольку слои medallion версионированы (снимки Iceberg дают time-travel запросы на точку во времени), можно прочитать каждый слой на ровно том снимке, который видел дашборд, и доказать, какой слой заглох. Задержка в bronze каскадит в silver и gold, поэтому без lineage один опоздавший батч выглядит как загадочное падение выручки тремя слоями ниже." },
  ]}
/>

<Recap lang="ru">
К моменту, когда продукт в проде, один факт живёт в семи хранилищах: OLTP как система записи, колоночный склад, питаемый CDC, слои bronze/silver/gold в dbt, materialized views для дашбордов, event-sourced поток аудита, полнотекстовый поисковый индекс и vector index для RAG. Каждое хранилище корректно оптимизировано под свою задачу — и именно эта оптимизация заставляет копии расходиться. Система ломается не внутри хранилища, а на стыке между двумя корректными: потерянное CDC-событие оставляет удалённый продукт в поиске, refresh lag заставляет MV расходиться с OLTP-источником, ночью пересобранный vector index рекомендует удалённый час назад продукт. Ход сеньора — перестать владеть хранилищами и начать владеть контрактами: transactional outbox, чтобы убить dual-write, идемпотентные консьюмеры для at-least-once доставки, объявленный freshness SLA, валящий build, lineage через версионированные слои medallion, чтобы идти по drift назад, и периодический reconciliation, который сверяет OLTP с каждой копией и чинит разницу. Консистентность, свежесть и lineage — сквозные свойства: ты проектируешь их на стыках, иначе система расходится сама с собой, пока каждая её часть проходит тесты.
</Recap>
