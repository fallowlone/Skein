---
concepts:
- inverted-index
- text-analysis-pipeline
- bm25-ranking
- tsvector-gin
- analyzer-mismatch
- near-real-time-indexing
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://www.elastic.co/blog/practical-bm25-part-3-considerations-for-picking-b-and-k1-in-elasticsearch
- https://www.postgresql.org/docs/current/textsearch-indexes.html
- https://www.elastic.co/docs/manage-data/data-store/text-analysis/index-search-analysis
- https://www.elastic.co/blog/changing-mapping-with-zero-downtime
spiral: []
status: ready
summary: 'LIKE ''''''''%term%'''''''' сканирует каждую строку и игнорирует релевантность.
  Полнотекстовый поиск переворачивает задачу: инвертированный индекс находит кандидатов
  за миллисекунды, конвейер анализа нормализует текст, а BM25 ранжирует по полезности,
  а не по факту наличия.'
title: 'Полнотекстовый поиск: инвертированный индекс, анализ и почему ранжирование
  важнее совпадения'
track: data-engineering
unit: 06-search
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Поиск товаров выкатили с `WHERE title LIKE '%running shoes%'`. В демо работает. На 50k строк это full table scan на 400ms, потому что ведущий `%` убивает любой индекс. Потом покупатель ищет "running shoe" — в единственном числе — и получает ноль результатов, а "Running Shoes" не находит ничего, потому что совпадение чувствительно к регистру. Запрос делает сравнение строк, тогда как задачей был поиск. Две разные проблемы в одном операторе `=`.
</Hook>

<Crux>Поиск — это совпадение плюс ранжирование: инвертированный индекс быстро находит кандидатов, а полезными их делает скоринг BM25.</Crux>

<Explanation>

## Почему `LIKE '%term%'` никогда не станет поиском

У `LIKE '%running%'` два фатальных свойства. Первое: ведущий wildcard означает, что ни один B-tree индекс не поможет — база сканирует каждую строку и прогоняет на каждой поиск подстроки, поэтому стоимость растёт линейно с размером таблицы. На паре тысяч строк никто не замечает; на паре миллионов это таймаут. Второе: он матчит *подстроки байтов*, а не *слова языка*. "running" не совпадёт с "run", "Run", "ran" или "runs". Зато радостно найдёт "scunthorpe" внутри длинного слова и промахнётся мимо "shoe", когда в тексте "shoes". И главное: у `LIKE` нет понятия релевантности — строка, где термин встречается один раз в подвале, ранжируется так же, как та, где он трижды в заголовке. Совпадение бинарно; поиск упорядочен.

Полнотекстовый поиск заменяет обе половины. Инвертированный индекс решает *нахождение* — превращает линейный скан в lookup по хешу. Скоринг релевантности решает *упорядочивание* — какой из 10 000 матчей человек должен увидеть первым. Нужны оба. Система, которая находит всё, но плохо ранжирует, так же бесполезна, как та, что ранжирует идеально, но слишком медленна.

## Инвертированный индекс: термин → posting list

Обычный (прямой) индекс отображает документ на его слова. *Инвертированный* индекс — наоборот: каждый термин указывает на **posting list** из id документов, которые его содержат, обычно с позициями и частотами термина. Поиск "running shoes" превращается в: взять posting list для `running`, взять posting list для `shoes`, пересечь их. Это два lookup по словарю и merge двух отсортированных списков id — миллисекунды независимо от размера корпуса, потому что ты никогда не трогаешь документы без этих терминов.

```
Документы:                  Инвертированный индекс:
  doc1: "running shoes"       running -> [doc1, doc3]
  doc2: "blue dress"          shoes   -> [doc1]
  doc3: "trail running gear"  blue    -> [doc2]
                              dress   -> [doc2]
                              trail   -> [doc3]
                              gear    -> [doc3]
Запрос "running shoes" -> пересечение [doc1,doc3] & [doc1] = [doc1]
```

Это структура данных внутри Postgres `tsvector`/GIN и внутри Lucene (движок под Elasticsearch и OpenSearch). Трудность не в lookup — в решении, что считается "термином", а это и есть конвейер анализа.

## Анализ: конвейер, который решает, что совпадёт

Сырой текст никогда не попадает в индекс напрямую. Он проходит через **анализатор**: tokenize → lowercase → удалить стоп-слова → stemming или лемматизация. "The Runners Were Running" становится примерно `[run, run]` после разбиения по пробелам, понижения регистра, выброса стоп-слов "the"/"were" и стемминга "runners"/"running" до корня "run". Теперь запрос на "run", "running" или "runners" попадает в один и тот же индексированный токен. *Вот* почему полнотекстовый поиск матчит единственное и множественное число там, где `LIKE` не может — совпадение происходит на нормализованных токенах, а не на сырых строках.

Ловушка сеньора прячется на виду: **один и тот же анализатор должен работать на индексировании и на запросе.** Если ты стеммишь документы до "run" при индексировании, но забываешь стеммить запрос, то поиск "running" даёт токен "running", которого нет в индексе, и ты получаешь ноль результатов — молча, без ошибки. Это самый частый инцидент "поиск сломан, и я не знаю почему". Фикс и диагностика — один и тот же инструмент: API `_analyze` в Elasticsearch (или `to_tsvector` в Postgres) показывает точные токены обеих сторон, и ты видишь рассогласование.

<Inset kind="why" lang="ru">
Stemming и лемматизация — не одно и то же. Стемминг (Porter, Snowball) рубит суффиксы грубыми правилами — быстро, но "university" и "universe" оба стеммятся в "univers", а "ran" не сводится к "run". Лемматизация использует словарь, чтобы найти настоящую базовую форму (обрабатывая "ran" → "run") — точнее, дороже, зависит от языка. Большинство продакшен-поиска использует стемминг, потому что его достаточно и он дешёвый; к лемматизации тянутся, когда точность важнее CPU.
</Inset>

## Релевантность: TF-IDF, затем BM25

Когда у тебя есть кандидаты, ты их скоришь. Интуиция — TF-IDF: термин важнее в документе, чем чаще он там встречается (term frequency), но менее важен, если он частый по всему корпусу (inverse document frequency) — "the" 50 раз не говорит ни о чём. Современные движки используют **BM25** — уточнение, которое чинит главный недостаток TF-IDF: сырая частота термина растёт без предела, поэтому спамный документ, повторяющий "shoes" 200 раз, доминировал бы. BM25 *насыщает* частоту термина — 10-е вхождение добавляет куда меньше, чем 2-е — и нормализует на длину документа, чтобы длинная статья не получала несправедливый буст просто за длину.

У BM25 две ручки, которые сеньор должен узнавать. `k1` (по умолчанию `1.2` в Elasticsearch и Lucene) задаёт, как быстро насыщается частота термина — ниже значит насыщается раньше. `b` (по умолчанию `0.75`) задаёт, насколько сильно нормализуется длина документа: `0` игнорирует длину полностью, `1` нормализует полностью. Дефолты хороши для прозы; ты тюнишь их, когда документы необычно короткие (заголовки товаров) или длинные (мануалы), и тюнишь, измеряя релевантность на размеченных запросах — никогда на глаз, потому что изменение тюнинга, помогающее одному классу запросов, молча регрессирует другой.

## Postgres против выделенного движка: реальное решение

Postgres `tsvector` + GIN — это по-настоящему хороший поиск и правильный дефолт, пока не перестанет быть им. Решение, которое сеньор действительно взвешивает:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Аспект</th>
        <th class="border border-border px-3 py-2 text-left">Postgres tsvector + GIN</th>
        <th class="border border-border px-3 py-2 text-left">Elasticsearch / OpenSearch</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Настройка и эксплуатация</td>
        <td class="border border-border px-3 py-2">Уже работает; один индекс, без отдельной системы</td>
        <td class="border border-border px-3 py-2">Отдельный кластер: деплой, безопасность, мониторинг, синхронизация</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Релевантность</td>
        <td class="border border-border px-3 py-2"><code>ts_rank</code> базовый; BM25 только через расширения</td>
        <td class="border border-border px-3 py-2">BM25 по умолчанию, тюнинг <code>k1</code>/<code>b</code> на поле</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Опечатки / fuzzy</td>
        <td class="border border-border px-3 py-2">Прикручивается (<code>pg_trgm</code>); не встроено в FTS</td>
        <td class="border border-border px-3 py-2">Fuzziness и edit distance из коробки</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Фасеты / агрегации</td>
        <td class="border border-border px-3 py-2">Ручной <code>GROUP BY</code></td>
        <td class="border border-border px-3 py-2">Нативные агрегации, заточены под фасеты</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Масштабирование</td>
        <td class="border border-border px-3 py-2">Вертикальное; одна нода</td>
        <td class="border border-border px-3 py-2">Шардинг + реплики по нодам</td>
      </tr>
    </tbody>
  </table>
</div>

Внутри Postgres есть второй выбор: **GIN против GiST**. GIN ищет примерно в 3 раза быстрее, но строится в ~3 раза дольше, в 2–3 раза больше на диске и медленнее обновляется — а с `fastupdate=on` он батчит записи в pending list, который раздувает индекс и может застопорить запрос при сбросе. GiST меньше и быстрее обновляется, но lossy, поэтому перепроверяет таблицу на матчах. Правило большого пальца: GIN для read-heavy, в основном статичного текста; GiST когда записи постоянны или размер индекса больно бьёт. Режим отказа на масштабе — bloat GIN под высокой записью: твой "быстрый" поисковый индекс тихо деградирует, пока `REINDEX` или тюнинг autovacuum не спасут.

## Near-real-time и налог на reindex-the-world

Выделенные движки не мгновенно консистентны — они *near-real-time*. `refresh_interval` в Elasticsearch по умолчанию `1s`, то есть свежеиндексированный документ не ищется до секунды. Это фича: refresh дорогой, поэтому под тяжёлыми bulk-загрузками сеньоры поднимают его до `30s` или ставят `-1` на время большого импорта и делают refresh один раз в конце — меняя задержку видимости на throughput. Ловушка — предполагать write-then-read консистентность, которой у тебя нет.

Самый большой эксплуатационный налог — миграция **reindex-the-world**. Lucene не даст сменить тип или анализатор существующего поля — токены уже зафиксированы. Хочешь добавить фильтр синонимов или починить баг стемминга по 200M документам? Ты строишь *новый* индекс с новым mapping, реиндексируешь всё в него (часы), затем атомарно переключаешь read-алиас со старого на новый, чтобы клиенты не увидели даунтайма. Забыть спроектировать всё за алиасом в первый день — та ошибка, которая превращает однострочный фикс анализатора в многодневную миграцию всем составом.

<TradeoffMatrix
  id="06-search-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="SaaS-приложение уже на Postgres нуждается в поиске по ~2M тикетов поддержки: совпадение по ключевым словам, базовая релевантность, низкий бюджет эксплуатации, записи умеренные. Выбери подход."
  options={[
    { name: "Добавить колонку tsvector с GIN-индексом и ранжировать через ts_rank", summary: "Остаться в Postgres.", correct: true, justification: "Правильный выбор под эти ограничения. Данные уже живут в Postgres, поэтому нет второй системы для деплоя, безопасности, мониторинга и синхронизации — и нет проблемы консистентности dual-write. GIN даёт быстрые lookup на read-heavy тексте, а 2M строк с умеренными записями — вполне в его комфортной зоне (слабость GIN — высокий churn записи, которого тут нет). Релевантность ts_rank базовая, но достаточна для поиска по тикетам по ключевым словам. К Elasticsearch переходишь только когда нужен реальный тюнинг BM25, фасеты, fuzzy или горизонтальное масштабирование — ничего из чего в условии нет." },
    { name: "Поднять кластер Elasticsearch и dual-write тикеты в него", summary: "Выделенный движок.", justification: "Оверинжиниринг под эти ограничения. ES — верный ответ, когда нужны фасеты, fuzzy/опечатки, тюнингуемый BM25 или scale-out — ничего из этого тут не требуется. Цена реальна: кластер в эксплуатации плюс пайплайн dual-write, который вносит свои проблемы консистентности и лага. Тянись к нему, когда Postgres FTS реально упрётся, а не превентивно." },
    { name: "Продолжать LIKE '%term%', но добавить B-tree индекс на колонку", summary: "Индексировать LIKE.", justification: "Не работает. LIKE с ведущим wildcard не может использовать B-tree индекс, поэтому ты всё равно full-scan'ишь 2M строк на каждый запрос, и у тебя по-прежнему нет стемминга, нет приведения регистра и нет ранжирования по релевантности. Это ровно та проблема, которую полнотекстовый поиск и существует заменить." },
    { name: "Использовать GiST вместо GIN для tsvector-индекса", summary: "Индекс GiST.", justification: "Защитимо, но не лучший фит. GiST блистает, когда записи постоянны или размер индекса — жёсткий лимит; здесь записи умеренные и доминирует чтение, что ровно sweet spot GIN — GIN ищет в ~3 раза быстрее. Lossiness у GiST к тому же вынуждает перепроверки таблицы, замедляющие запросы. Выбирай GiST, когда churn записи или размер диска вынуждают компромисс, а не по умолчанию для read-heavy поиска." },
  ]}
/>

<Quiz
  id="06-search-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Документы индексируют со стемминг-анализатором, но запрос отправляют без анализа. Пользователь ищет 'running' и получает ноль результатов. Почему?"
  choices={[
    { label: "На индексировании сохранён токен 'run', а непроанализированный запрос ищет 'running', которого в индексе нет — рассогласование анализаторов", correct: true },
    { label: "GIN-индекс раздут и нуждается в REINDEX", misconception: "Bloat замедляет запросы; он не делает так, чтобы корректный токен возвращал ничего. Токена просто нет, потому что индекс и запрос произвели разные токены." },
    { label: "BM25 k1 выставлен слишком низко, и скор округляется до нуля", misconception: "k1 влияет на ранжирование совпавших документов; он не может превратить match в non-match. Ноль результатов значит, что ничто не совпало — это проблема токенизации, а не скоринга." },
    { label: "refresh_interval ещё не истёк, поэтому документы пока не ищутся", misconception: "Это даёт краткую задержку для новых записей, а не постоянный ноль для уже проиндексированных документов, которые нашлись бы при другой формулировке запроса." },
  ]}
/>

<Quiz
  id="06-search-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Почему поисковые движки перешли от сырого TF-IDF к BM25 для скоринга по частоте термина?"
  choices={[
    { label: "BM25 насыщает частоту термина и нормализует на длину документа, поэтому документ, спамящий термин 200 раз, не может доминировать", correct: true },
    { label: "BM25 быстрее вычисляется, чем TF-IDF, на запросе", misconception: "Дело не в скорости; BM25 — уточнение релевантности. Оба дёшевы в вычислении, как только posting list'ы найдены." },
    { label: "BM25 убирает необходимость в инвертированном индексе", misconception: "BM25 — функция скоринга, которая работает на кандидатах, уже произведённых инвертированным индексом. Она не заменяет индекс — она ранжирует его вывод." },
    { label: "BM25 делает стемминг ненужным", misconception: "Стемминг происходит в конвейере анализа до скоринга. BM25 скорит те токены, что произвёл анализ; это разные стадии." },
  ]}
/>

<DragOrder
  id="06-search-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь стадии, через которые проходит текст, от сырого ввода до ранжированного результата:"
  items={[
    { id: "a", label: "Анализ: tokenize, lowercase, выброс стоп-слов, stemming/лемматизация в нормализованные термины" },
    { id: "b", label: "Индексирование: записать каждый термин в его posting list в инвертированном индексе" },
    { id: "c", label: "Запрос: прогнать ТОТ ЖЕ анализатор по строке поиска, чтобы получить термины запроса" },
    { id: "d", label: "Совпадение: найти и пересечь posting list'ы терминов запроса" },
    { id: "e", label: "Ранжирование: оценить кандидатов через BM25 и вернуть упорядоченными" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="Конвейер полнотекстового поиска"
  perRow={4}
  hue="var(--d-data)"
  nodes={[
    { id: "docs", label: "Документы", sub: "сырой текст", col: 0, row: 0 },
    { id: "analyze1", label: "Анализ", sub: "токены/стем/стоп", col: 1, row: 0 },
    { id: "index", label: "Инверт. индекс", sub: "термин -> postings", col: 2, row: 0 },
    { id: "query", label: "Запрос", sub: "сырая строка", col: 0, row: 1 },
    { id: "analyze2", label: "Анализ", sub: "ТОТ ЖЕ анализатор", col: 1, row: 1 },
    { id: "match", label: "Совпадение", sub: "пересечь postings", col: 2, row: 1 },
    { id: "ranked", label: "Ранжированный список", sub: "BM25 / TF-IDF", col: 3, row: 1 },
  ]}
  edges={[
    { from: "docs", to: "analyze1" },
    { from: "analyze1", to: "index", label: "build" },
    { from: "query", to: "analyze2" },
    { from: "analyze2", to: "match" },
    { from: "index", to: "match", label: "lookup" },
    { from: "match", to: "ranked", label: "score" },
  ]}
  caption="Индексирование (сверху) и запрос (снизу) гоняют ОДИН анализатор, поэтому токены совпадают. Инвертированный индекс отображает термин в posting list; запрос пересекает эти списки, затем BM25/TF-IDF ранжирует выживших."
/>

<KeyTakeaway>Бери инвертированный индекс и анализатор вместо LIKE, гоняй один анализатор на индексе и запросе, ранжируй через BM25. Начни на Postgres GIN; иди в Elasticsearch ради фасетов, fuzzy или масштаба.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-search-retrieval"
  lang="ru"
  questions={[
    { q: "Коллега говорит, что полнотекстовый поиск 'просто ничего не возвращает' для части запросов, и ошибок в логах нет. Пройди по тому, как ты будешь это диагностировать.", a: "Ноль результатов без ошибки почти всегда значит, что токены на индексировании и на запросе не совпадают — рассогласование анализаторов. Совпадение происходит на нормализованных токенах, а не на сырых строках, поэтому если документы стеммились до 'run' при индексировании, но запрос на 'running' не анализируется (или использует другой анализатор), он ищет токен 'running', который никогда не записывался, и не находит ничего. Диагностика — прогнать обе стороны через анализатор и сравнить: в Elasticsearch API _analyze показывает точные токены для текста и анализатора; в Postgres сравниваешь to_tsvector(doc) с plainto_tsquery(query). В момент, когда видишь, что одна сторона дала 'run', а другая 'running', — ты нашёл проблему. Фикс — заставить запрос использовать тот же анализ, что и индекс; именно поэтому один анализатор на индексе и запросе — это правило, а его нарушение — самый частый молчаливый отказ поиска." },
    { q: "Когда Postgres tsvector/GIN — правильный выбор, и что конкретно толкает к выделенному движку вроде Elasticsearch?", a: "Полнотекстовый поиск в Postgres — правильный дефолт, когда данные уже живут в Postgres, корпус умеренный, записи не экстремальны и нужно совпадение по ключевым словам с базовой релевантностью — потому что нет второй системы для деплоя, безопасности, мониторинга и поддержания консистентности через dual-write. Переходишь на Elasticsearch/OpenSearch, когда упираешься в то, что Postgres FTS делает плохо: реальный тюнинг релевантности BM25 на поле (k1, b), фасеты и агрегации над результатами, встроенная fuzzy/устойчивость к опечаткам, near-real-time индексирование на высоком объёме записи и горизонтальный scale-out через шарды и реплики. Цена перехода реальна — кластер в эксплуатации плюс пайплайн синхронизации — поэтому делаешь это, когда Postgres реально упёрся, а не превентивно. И какой бы движок ты ни выбрал, проектируй за read-алиасом с первого дня, потому что смена анализатора или типа поля позже вынуждает полный reindex, а алиас — это то, что позволяет переключиться на пересобранный индекс с нулевым даунтаймом вместо миграции всем составом." },
  ]}
/>

<Recap lang="ru">
Полнотекстовый поиск существует, потому что `LIKE '%term%'` — это два провала сразу: full scan, который не может использовать индекс, и сравнение байтовых подстрок без понятия релевантности. Инвертированный индекс чинит нахождение — каждый термин указывает на posting list из id документов, поэтому запрос — это lookup по словарю и merge, быстро независимо от размера корпуса. Конвейер анализа (tokenize, lowercase, стоп-слова, stemming/лемматизация) решает, что считается термином, и его железное правило — один и тот же анализатор должен работать на индексе и на запросе, иначе получишь молчаливый ноль результатов. Ранжирование — вторая половина: BM25 со своими ручками k1 и b насыщает частоту термина и нормализует на длину, чтобы всплывали самые полезные документы, а не любые со словом. Postgres tsvector с GIN-индексом — правильный дефолт (быстрые lookup, без отдельной системы), и внутри ты взвешиваешь bloat GIN под записью против lossiness GiST. К Elasticsearch или OpenSearch переходишь, когда нужны фасеты, fuzzy, тюнинг BM25 или масштаб, принимая near-real-time refresh и проектируя за read-алиасом, чтобы неизбежная миграция reindex-the-world осталась переключением алиаса с нулевым даунтаймом.
</Recap>
