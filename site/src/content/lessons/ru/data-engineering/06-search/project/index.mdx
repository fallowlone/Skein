---
concepts:
- inverted-index
- analyzer-parity
- bm25-ranking
- tsvector-gin
- reindex-alias
estMin: 240
lang: ru
level: senior
order: 10
prereqs: []
slug: project
sources:
- https://www.postgresql.org/docs/current/textsearch-indexes.html
- https://www.elastic.co/docs/manage-data/data-store/text-analysis/index-search-analysis
- https://www.elastic.co/blog/changing-mapping-with-zero-downtime
status: ready
summary: "Практический проект — построй настоящий full-text search по корпусу документов, докажи релевантность на judged queries и проведи смену анализатора за read-алиасом без даунтайма."
title: 'Полнотекстовый поиск: построй и мигрируй поисковый индекс'
track: data-engineering
unit: 06-search
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Читать про паритет анализаторов и reindex-миграции — не то же самое, что выкатить поисковую фичу, которая переживает фикс бага stemming на масштабе. Построй поиск по реальному корпусу, докажи его релевантность числами, затем проведи миграцию, которая рано или поздно нужна каждой поисковой системе — так, чтобы никто не заметил.
</Hook>

<Goal lang="ru">
Преврати модель юнита в работающую систему: проиндексируй корпус в inverted index, обеспечь один анализатор при индексировании и при запросе, оттюнь BM25 на judged queries и отрепетируй zero-downtime reindex-за-алиасом, который навязывает движок с неизменяемыми токенами.
</Goal>

<ProjectBrief
  id="06-search-project"
  lessonSlug="06-search"
  lang="ru"
  objective="Построй full-text search по реальному корпусу документов (≥50k документов) — сначала на Postgres tsvector/GIN, затем опционально на Elasticsearch/OpenSearch — который возвращает релевантные ранжированные результаты за миллисекунды, и докажи это измеренной релевантностью и zero-downtime миграцией анализатора."
  requirements={[
    "Загрузи реальный корпус минимум 50k документов (например публичный дамп статей, товаров или тикетов поддержки). Реализуй поиск через колонку tsvector плюс GIN-индекс в Postgres; ранжируй через ts_rank (или настоящий BM25 через расширение, если хочешь).",
    "Определи один анализатор (tokenize, lowercase, stopword filter, stemming) и примени его идентично при индексировании и при запросе. Добавь тест, прогоняющий известный терм через оба пути и утверждающий, что токены совпадают — это твоя защита от молчаливых нулей.",
    "Собери небольшой judged-query набор: 15-20 реальных запросов, у каждого помечено несколько релевантных документов. Измерь базовую релевантность (precision@10 или nDCG@10), чтобы было число для улучшения, а не ощущение.",
    "Продемонстрируй failure mode намеренно: сломай паритет анализаторов (stem только одну сторону), покажи, что запрос возвращает ноль результатов, затем почини и покажи, что результаты вернулись. Зафиксируй оба состояния.",
    "Оттюнь релевантность: подкрути ранжирование (BM25 k1/b, если движок на BM25, или веса/нормализацию в Postgres) и перемерь на judged-наборе. Запиши, какое изменение помогло какому классу запросов, и подтверди, что не ухудшил остальные.",
    "Проведи reindex-за-алиасом миграцию: поменяй анализатор (например добавь synonym или почини правило stemming), построй НОВЫЙ индекс/таблицу с новым mapping, переиндексируй корпус в него, затем атомарно переключи read-алиас (ES alias или view/search-функция в Postgres) со старого на новый, чтобы живые запросы не ломались.",
    "Покажи, что латентность поиска держится в единицах-низких десятках миллисекунд на размере корпуса, и проверь, что GIN-индекс реально используется (EXPLAIN показывает index scan, а не sequential scan).",
  ]}
  acceptance={[
    "Таблица релевантности: precision@10 (или nDCG@10) до и после тюнинга на том же judged-query наборе, измеренные, а не оценённые, без класса запросов, ухудшенного ниже базы.",
    "Доказательство защиты паритета анализаторов: прогон намеренного mismatch, возвращающий ноль результатов, и тест паритета, который теперь это предотвращает — оба зафиксированы.",
    "Вывод EXPLAIN (ANALYZE), доказывающий, что запрос поиска использует GIN-индекс (bitmap/index scan), а не sequential scan по корпусу.",
    "Лог миграции, показывающий построение и переиндексацию нового индекса, атомарный swap read-алиаса и корректно обслуженные запросы на протяжении всего процесса — с абзацем, почему ограничение неизменяемых токенов форсирует rebuild-and-swap, а не правку на месте.",
  ]}
  stretch={[
    "Добавь второй бэкенд (Elasticsearch или OpenSearch) по тому же корпусу и сравни релевантность, латентность и операционную стоимость с версией на Postgres на идентичном judged-query наборе.",
    "Добавь fuzzy/typo-толерантность (pg_trgm в Postgres или fuzziness в ES) и измерь, насколько она улучшает recall на подмножестве запросов с опечатками, не разрушая precision.",
    "Добавь faceting/aggregations (счётчики по категориям рядом с результатами) и измерь стоимость латентности вычисления facets на запрос.",
    "Добавь near-real-time обработку записи: принимай поток новых документов, подними refresh_interval (или батчи обновлений tsvector) под bulk-загрузкой и покажи, что пропускная способность записи растёт, задокументировав принятый компромисс по латентности видимости.",
  ]}
/>

<Recap lang="ru">
Это цикл, который ты прогоняешь, когда поиск — твоя ответственность: индексируй в inverted-структуру, обеспечь один анализатор на обеих сторонах и защити его тестом паритета, тюнь ранжирование на judged queries реальными числами и отрепетируй reindex-за-алиасом миграцию, которую движок с неизменяемыми токенами рано или поздно потребует. Сделав это один раз на реальном корпусе, ты делаешь mismatch анализаторов и reindex-миграции очевидными, а не пугающими, когда они всплывут в production.
</Recap>
