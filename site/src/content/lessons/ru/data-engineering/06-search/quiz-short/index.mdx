---
concepts:
- inverted-index
- analyzer-parity
- bm25-ranking
- tsvector-gin
- near-real-time-indexing
- reindex-alias
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.postgresql.org/docs/current/textsearch-indexes.html
- https://www.elastic.co/docs/manage-data/data-store/text-analysis/index-search-analysis
- https://www.elastic.co/blog/changing-mapping-with-zero-downtime
status: ready
summary: "Промпты на свободное припоминание по всему юниту поиска. Сначала ответь своими словами, затем открой модельный ответ и сравни со стержнем юнита."
title: 'Полнотекстовый поиск: тест на свободное припоминание'
track: data-engineering
unit: 06-search
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание бьёт перечитывание. На каждый промпт скажи или запиши полный ответ по памяти до того, как откроешь модельный — именно усилие припоминания превращает идеи юнита в то, что можно применить под давлением.
</Hook>

<Goal lang="ru">
Реконструируй ключевые механизмы юнита — inverted index (инвертированный индекс: каждый термин указывает на список id содержащих его документов), конвейер анализа и его правило паритета, BM25 и его ручки, выбор Postgres-vs-движок и операционные ловушки — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:load
  id="06-search-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему LIKE '%term%' никогда не может быть поиском и какие две разные проблемы full-text search решает вместо него?", a: "LIKE '%term%' проваливается сразу по двум осям. Во-первых, ведущий wildcard означает, что никакой B-tree индекс не поможет, поэтому БД сканирует каждую строку и делает substring-матч — стоимость растёт линейно с размером таблицы и таймаутит на масштабе. Во-вторых, он матчит подстроки байтов, а не слова языка: 'running' не совпадёт с 'run', 'Run' или 'runs', и у него нет понятия релевантности, поэтому упоминание в футере ранжируется так же, как заголовок, повторённый трижды. Full-text search заменяет обе половины. Inverted index решает поиск — превращая линейный скан в dictionary lookup. Скоринг релевантности решает упорядочивание — определяя, какой из тысяч матчей человек должен увидеть первым. Нужны обе: найти всё, но плохо ранжировать, так же бесполезно, как идеально ранжировать, но быть слишком медленным." },
    { q: "Опиши inverted index и что делает запрос быстрым на нём независимо от размера корпуса.", a: "Forward index маппит документ на его слова; inverted index маппит наоборот — каждый терм указывает на posting list из id документов, которые его содержат, обычно с позициями и term frequency. Запрос вроде 'running shoes' становится: взять posting list для 'running', взять posting list для 'shoes' и пересечь два отсортированных списка id. Это два dictionary lookup и merge — миллисекунды независимо от размера корпуса, потому что ты никогда не трогаешь документы без этих термов. Это структура внутри Postgres tsvector/GIN и внутри Lucene (под Elasticsearch и OpenSearch). Сложная часть не lookup, а решение, что считать термом — это работа конвейера анализа." },
    { q: "Что делает анализатор и почему один и тот же анализатор должен работать при индексировании и при запросе?", a: "Анализатор превращает сырой текст в нормализованные токены, которые идут в индекс: tokenize, lowercase, drop stopwords, затем stem или lemmatize. 'The Runners Were Running' становится примерно [run, run] после разбиения, lowercase, отбрасывания 'the'/'were' и стемминга к корню. Вот почему full-text search матчит единственное и множественное там, где LIKE не может — совпадение идёт по нормализованным токенам, а не сырым строкам. Железное правило: один и тот же анализатор должен работать на обеих сторонах: если документы stemmed к 'run' при индексировании, а запрос по 'running' не stemmed, lookup идёт по токену, который никогда не записывался, и ты молча получаешь ноль результатов без ошибок. Диагностика — тот же инструмент, что и предотвращает это: _analyze API в Elasticsearch или to_tsvector в Postgres, который показывает точные токены каждой стороны, чтобы заметить mismatch." },
    { q: "Объясни, почему поиск перешёл от TF-IDF к BM25 и что управляют ручки k1 и b.", a: "Интуиция TF-IDF верна — терм важнее, чем чаще он встречается в документе (term frequency), но менее важен, если он часто встречается по всему корпусу (inverse document frequency). Его изъян в том, что сырой term frequency растёт без границ, поэтому документ, повторяющий 'shoes' 200 раз, доминировал бы. BM25 чинит это, насыщая term frequency — 10-е вхождение добавляет гораздо меньше, чем 2-е — и нормализуя по длине документа, чтобы длинная статья не бустилась просто за длину. У него две ручки: k1 (по умолчанию 1.2 в Elasticsearch/Lucene) управляет тем, как быстро насыщается term frequency, ниже — насыщается раньше; b (по умолчанию 0.75) управляет тем, насколько сильно нормализуется длина, где 0 игнорирует длину полностью, а 1 нормализует полностью. Их тюнят, измеряя релевантность на judged queries — никогда по ощущениям, потому что изменение, помогающее одному классу запросов, может молча ухудшить другой." },
    { q: "Когда Postgres tsvector/GIN — правильный выбор по умолчанию, что толкает к выделенному движку и как выбрать GIN vs GiST внутри Postgres?", a: "Postgres FTS — правильный выбор по умолчанию, когда данные уже в Postgres, корпус умеренный, записи не экстремальны и нужен keyword-матч с базовой релевантностью — потому что нет второй системы для деплоя, защиты, мониторинга или поддержания консистентности через dual writes. Ты переходишь на Elasticsearch или OpenSearch, когда нужны реальный тюнинг BM25 по полям, faceting и aggregations, встроенная fuzzy/typo-толерантность, near-real-time индексирование при высоком объёме записи или горизонтальный scale-out — и принимаешь кластер в эксплуатации плюс sync-конвейер как цену. Внутри Postgres GIN делает lookup примерно в 3x быстрее, но медленнее строится, больше на диске и медленнее обновляется — а под высокой записью с fastupdate=on его pending list раздувает индекс. GiST меньше и быстрее обновляется, но lossy, поэтому делает recheck по таблице на матчах. Эмпирика: GIN для read-heavy, почти-статичного текста; GiST, когда записи постоянны или размер индекса болит." },
    { q: "Что значит 'near-real-time' для выделенного движка и почему надо проектировать за read-алиасом с первого дня?", a: "Выделенные движки не мгновенно консистентны — они near-real-time. refresh_interval в Elasticsearch по умолчанию 1s, поэтому свежепроиндексированный документ не доступен для поиска до секунды; под тяжёлыми bulk-загрузками ты поднимаешь его до 30s или ставишь -1 и делаешь refresh один раз в конце, обменивая латентность видимости на пропускную способность. Ловушка — предполагать write-then-read консистентность, которой у тебя нет. Больший операционный налог — это reindex-the-world миграция: Lucene не даст поменять тип или анализатор существующего поля, потому что токены уже закоммичены, поэтому фикс бага stemming или добавление synonym filter на сотни миллионов документов означает построить новый индекс, переиндексировать всё в него (часы) и переключить read-алиас со старого на новый. Проектирование за этим алиасом с первого дня — вот что превращает однострочный фикс анализатора в zero-downtime swap вместо многодневной all-hands миграции." },
  ]}
/>

<Recap lang="ru">
Если ты смог реконструировать каждый ответ по памяти, ты держишь стержень юнита: LIKE проваливается и в поиске, и в ранжировании, inverted index делает поиск dictionary lookup, конвейер анализа решает, что такое терм, и его правило паритета не подлежит обсуждению, BM25 насыщает term frequency и нормализует длину, чтобы всплывали полезные документы, Postgres GIN — правильный выбор по умолчанию, пока facets/fuzziness/масштаб не толкнут к выделенному движку, а near-real-time refresh плюс reindex из-за неизменяемых токенов — это причина строить за read-алиасом с самого начала. Теперь, когда в production сломается поисковая фича, ты можешь отследить сбой до конкретного слоя — токенизация, структура индекса, скоринг или консистентность — не гадая наугад.
</Recap>
