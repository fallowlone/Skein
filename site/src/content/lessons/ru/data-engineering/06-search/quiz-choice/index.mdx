---
concepts:
- inverted-index
- analyzer-mismatch
- bm25-ranking
- tsvector-gin
- near-real-time-indexing
- reindex-alias
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.postgresql.org/docs/current/textsearch-indexes.html
- https://www.elastic.co/blog/practical-bm25-part-3-considerations-for-picking-b-and-k1-in-elasticsearch
- https://www.elastic.co/blog/changing-mapping-with-zero-downtime
status: ready
summary: "Тест с выбором на синтез по всему юниту — inverted index, паритет анализаторов, тюнинг BM25, GIN vs GiST, near-real-time refresh и reindex без даунтайма."
title: 'Полнотекстовый поиск: тест с выбором ответа'
track: data-engineering
unit: 06-search
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает решение, которое ты принимаешь, проектируя или отлаживая реальный поиск — не определение для заучивания, а компромисс, который надо взвесить относительно корпуса, скорости записи и SLO.
</Hook>

<Goal lang="ru">
Убедись, что связываешь inverted index (инвертированный индекс, где каждый термин указывает на список id документов), конвейер анализа, релевантность BM25, выбор Postgres-vs-движок и операционные ловушки (задержка refresh, reindex-миграции) в единую модель того, как поиск на самом деле себя ведёт.
</Goal>

<Quiz
  id="06-search-choice-1"
  lessonSlug="06-search"
  lang="ru"
  question="Запрос по 'running' возвращает ноль результатов без ошибок в логах. Документы проиндексированы со stemming-анализатором; путь запроса анализирует терм иначе. Какова наиболее вероятная корневая причина?"
  choices={[
    { label: "Analyzer mismatch: при индексировании сохранён stemmed-токен 'run', а путь запроса произвёл 'running', которого нет ни в одном posting list. Совпадение идёт по нормализованным токенам, поэтому разный анализ на сторонах молча не находит ничего.", correct: true },
    { label: "GIN-индекс распух, и REINDEX это починит.", misconception: "Bloat замедляет lookup, но никогда не превращает корректно записанный токен в не-совпадение. Ноль результатов значит, что ничего не совпало — это проблема токенизации, а не здоровья индекса." },
    { label: "BM25 k1 слишком низкий, поэтому все score округляются в ноль.", misconception: "k1 лишь меняет форму ранжирования уже совпавших документов. Он не может превратить совпадение в не-совпадение — ноль результатов это сбой матчинга, а не скоринга." },
    { label: "refresh_interval ещё не истёк, поэтому документы пока не доступны для поиска.", misconception: "Задержка refresh даёт краткую паузу для свежезаписанных документов, а не постоянный ноль для уже проиндексированных, которые другой формулировкой запроса нашлись бы." },
  ]}
/>

<Quiz
  id="06-search-choice-2"
  lessonSlug="06-search"
  lang="ru"
  question="Почему современные движки перешли от чистого TF-IDF к BM25 для скоринга по частоте терма?"
  choices={[
    { label: "BM25 насыщает term frequency — 10-е вхождение добавляет гораздо меньше, чем 2-е — и нормализует по длине документа, поэтому документ, спамящий термом 200 раз, или длинная статья не могут доминировать по сырым счётчикам.", correct: true },
    { label: "BM25 гораздо быстрее считать на момент запроса, чем TF-IDF.", misconception: "Скорость не мотив; оба дёшевы после слияния posting lists. BM25 — это улучшение релевантности, а не производительности." },
    { label: "BM25 убирает необходимость в inverted index.", misconception: "BM25 скорит кандидатов, которых уже произвёл inverted index. Он ранжирует вывод индекса, но не заменяет сам индекс." },
    { label: "BM25 делает конвейер анализа ненужным.", misconception: "Stemming и tokenization происходят до скоринга. BM25 скорит те токены, что произвёл анализ — две стадии независимы." },
  ]}
/>

<Quiz
  id="06-search-choice-3"
  lessonSlug="06-search"
  lang="ru"
  question="SaaS-приложению на Postgres нужен поиск по ~2M тикетам поддержки: keyword-матч, базовая релевантность, умеренные записи, малый ops-бюджет. Какой выбор по умолчанию правильный?"
  choices={[
    { label: "Добавить колонку tsvector с GIN-индексом и ранжировать через ts_rank. Данные уже в Postgres, поэтому нет второй системы для деплоя и синхронизации, а 2M строк с умеренными записями — внутри комфортной зоны GIN.", correct: true },
    { label: "Поднять кластер Elasticsearch и dual-write каждого тикета в него.", misconception: "Оверинжиниринг под эти ограничения. ES оправдан на faceting, fuzzy-матче, тюнинге BM25 или scale-out — ничего из этого тут не нужно — ценой кластера в эксплуатации и проблемы консистентности dual-write." },
    { label: "Оставить LIKE '%term%', но добавить B-tree индекс на колонку.", misconception: "LIKE с ведущим wildcard не может использовать B-tree, поэтому ты всё равно сканируешь все 2M строк, без stemming, без case-folding и без релевантности. Это ровно та проблема, которую заменяет full-text search." },
    { label: "Использовать GiST вместо GIN для tsvector-индекса.", misconception: "GiST блещет при постоянных записях или жёстком лимите диска; здесь доминируют чтения, а записи умеренные — это вотчина GIN: lookup примерно в 3x быстрее, а lossiness GiST форсирует rechecks по таблице." },
  ]}
/>

<Quiz
  id="06-search-choice-4"
  lessonSlug="06-search"
  lang="ru"
  question="Ты выбрал GIN для tsvector-индекса. Спустя месяцы, на теперь write-heavy таблице, латентность поиска деградирует с перебоями без изменения запроса. Каков наиболее вероятный механизм?"
  choices={[
    { label: "GIN bloat под высокой записью: при fastupdate=on вставки батчатся в pending list, который периодически сбрасывается, и индекс растёт и деградирует, пока REINDEX или тюнинг autovacuum не спасут. Запрос может застопориться в момент сброса pending list.", correct: true },
    { label: "BM25 k1/b сдвинулись, потому что корпус вырос.", misconception: "k1 и b — статическая конфигурация; они не дрейфуют с размером корпуса, и Postgres ts_rank по умолчанию вообще не использует BM25. Симптом — здоровье индекса под записью, а не скоринг." },
    { label: "ts_rank стал медленнее, потому что совпадающих документов больше.", misconception: "Больше кандидатов добавляет немного стоимости ранжирования, но перебойная деградация, привязанная к объёму записи, прямо указывает на flush pending list и bloat GIN, а не на арифметику ранжирования." },
    { label: "Поменялся анализатор, поэтому токены больше не совпадают.", misconception: "Смена анализатора дала бы неверные или пустые результаты, а не перебойную латентность, которая трекает объём записи. Это failure mode сопровождения индекса, а не токенизации." },
  ]}
/>

<Quiz
  id="06-search-choice-5"
  lessonSlug="06-search"
  lang="ru"
  question="Сервис на Elasticsearch пишет документ и сразу читает его обратно поиском, периодически получая промах. Команда предполагала write-then-read консистентность. Что происходит?"
  choices={[
    { label: "Elasticsearch near-real-time, а не мгновенно консистентен: свежепроиндексированный документ не доступен для поиска до следующего refresh (refresh_interval по умолчанию 1s). Промах — это окно refresh, а не потерянная запись.", correct: true },
    { label: "Документ не проиндексировался и был молча отброшен.", misconception: "Перебойный промах, который разрешается за ~1s, — это окно refresh, а не отброс. Отброшенная запись никогда не стала бы доступной для поиска; эта становится — мгновением позже." },
    { label: "BM25 отранжировал документ ниже отсечки результатов.", misconception: "Позиция ранжирования не может сделать документ кратко невидимым, а затем видимым без изменения данных. Тайминг видим-потом-нет — сигнатура refresh interval." },
    { label: "GIN-индекс ещё не построен.", misconception: "GIN — структура Postgres; Elasticsearch использует сегменты Lucene. Описанное поведение — это refresh-to-searchable латентность Lucene, by design." },
  ]}
/>

<Quiz
  id="06-search-choice-6"
  lessonSlug="06-search"
  lang="ru"
  question="Нужно починить баг stemming, поменяв анализатор существующего поля на 200M документах в Elasticsearch. Какова правильная миграция и почему?"
  choices={[
    { label: "Построить новый индекс с исправленным mapping, переиндексировать всё в него, затем атомарно переключить read-алиас со старого на новый. Lucene не даст поменять анализатор существующего поля, потому что токены уже закоммичены, поэтому rebuild плюс alias swap — единственный путь без даунтайма.", correct: true },
    { label: "Обновить mapping поля на месте и дать Elasticsearch ре-токенизировать лениво.", misconception: "Lucene не может поменять анализатор или тип существующего поля — закоммиченные токены неизменяемы. Ленивой ре-токенизации нет; нужно строить новый индекс." },
    { label: "Удалить и пересоздать индекс, приняв краткое окно даунтайма.", misconception: "Удаление живого индекса означает, что клиенты видят пустой или падающий поиск, пока reindex не завершится — часы на 200M документов. Проектирование за read-алиасом полностью избегает простоя." },
    { label: "Поднять refresh_interval, чтобы смена анализатора распространилась быстрее.", misconception: "refresh_interval управляет латентностью write-to-searchable, а не сменой анализатора. Он не имеет отношения к ре-токенизации уже закоммиченных документов." },
  ]}
/>

<Recap lang="ru">
Сквозная линия: совпадение — это токены, а не строки, поэтому один и тот же анализатор должен работать при индексировании и при запросе, иначе получишь молчаливые нули; BM25 ранжирует кандидатов, которых произвёл inverted index, насыщая term frequency и нормализуя длину; Postgres GIN — правильный выбор по умолчанию, пока не понадобятся facets, fuzziness, тюнинг BM25 или масштаб, и тогда внутренний выбор — write-bloat GIN против lossiness GiST; а операционные ловушки — near-real-time задержка refresh и reindex из-за неизменяемых токенов — это причина проектировать за read-алиасом с первого дня. Теперь, когда встретишь mismatch анализаторов или алерт о bloat GIN, ты знаешь, за какой рычаг тянуться раньше, чем всё остальное.
</Recap>
