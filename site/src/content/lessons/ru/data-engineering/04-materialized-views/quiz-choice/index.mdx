---
concepts:
- materialized-view
- refresh-strategy
- refresh-concurrently
- incremental-view-maintenance
- staleness-window
- streaming-materialized-view
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.postgresql.org/docs/current/sql-refreshmaterializedview.html
- https://github.com/sraoss/pg_ivm
- https://clickhouse.com/docs/materialized-view/incremental-materialized-view
status: ready
summary: "Синтез всего юнита в формате выбора — стратегия refresh, скрытые издержки CONCURRENTLY, компромиссы incremental maintenance, ответственность за staleness и streaming MV."
title: 'Materialized views: тест с выбором ответа'
track: data-engineering
unit: 04-materialized-views
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов через весь юнит. Каждый — это решение, которое вы принимаете, когда кто-то предлагает «давай просто заматериализуем» — не определение для пересказа, а компромисс, который нужно взвесить против реальной нагрузки.
</Hook>

<Goal lang="ru">
Убедитесь, что вы связываете стоимость чтения, стратегию refresh, бюджет staleness, частоту записи и операционную ответственность — синтез, к которому вёл урок.
</Goal>

<Quiz
  id="04-materialized-views-choice-1"
  lessonSlug="04-materialized-views"
  lang="ru"
  question="Обычное представление (view) никогда не устаревает, но команды всё равно берут materialized view. Что именно даёт materialized view и какой ценой?"
  choices={[
    { label: "Он отдаёт чтения из сохранённого набора результата (дешёвый scan или поиск по индексу) вместо повторного выполнения запроса на каждом чтении; взамен вы берёте на себя staleness и обязанность делать refresh.", correct: true },
    { label: "Он ускоряет сам определяющий запрос к базовым таблицам.", misconception: "Базовый запрос не меняется — он по-прежнему выполняется целиком во время refresh. Меняется то, что чтения больше его не запускают; они идут в сохранённые строки. Стоимость переезжает с чтения на refresh, а не исчезает." },
    { label: "Он гарантирует, что данные всегда актуальны, без повторного выполнения запроса.", misconception: "Это обычное view, которое перевыполняется на каждом чтении и потому всегда свежее. Materialized view устаревает в момент изменения базовой строки после последнего refresh." },
    { label: "Он избавляет от необходимости индексов на базовых таблицах.", misconception: "Индексация базовых таблиц ортогональна; MV — это сохранённая копия результата, а не замена ей, и refresh CONCURRENTLY как раз требует уникальный индекс на самом представлении." },
  ]}
/>

<Quiz
  id="04-materialized-views-choice-2"
  lessonSlug="04-materialized-views"
  lang="ru"
  question="Дашборд аналитиков постоянно читает materialized view. Ops по ночам запускает обычный REFRESH MATERIALIZED VIEW (без CONCURRENTLY) на агрегате длиной 90 секунд. Пользователи жалуются, что дашборд каждую ночь зависает. Почему и как это чинить?"
  choices={[
    { label: "Обычный refresh держит блокировку ACCESS EXCLUSIVE всё время своего выполнения, поэтому каждое чтение view блокируется на ~90с. Добавьте UNIQUE-индекс на view и перейдите на REFRESH ... CONCURRENTLY, чтобы читатели продолжали видеть старую версию во время пересборки.", correct: true },
    { label: "Refresh сначала очищает view, поэтому чтения возвращают пусто, пока он не наполнится заново.", misconception: "Refresh не показывает пустого окна; читатели блокируются на блокировке, а не видят частичные или пустые данные. Именно CONCURRENTLY оставляет старые строки читаемыми во время swap." },
    { label: "Перенесите refresh на read-реплику, чтобы primary не блокировался.", misconception: "Materialized view обновляется там, где он живёт; нельзя переложить refresh MV основного сервера на реплику. Проблема — блокировка, а CONCURRENTLY убирает блокировку чтения, а не маршрутизация на реплику." },
    { label: "Поднимите work_mem, чтобы агрегат считался быстрее и блокировка была короче.", misconception: "Тюнинг может сэкономить секунды, но блокирующая чтения блокировка структурна для обычного refresh. Даже быстрый refresh блокирует чтения; CONCURRENTLY меняет поведение блокировки, а не только длительность." },
  ]}
/>

<Quiz
  id="04-materialized-views-choice-3"
  lessonSlug="04-materialized-views"
  lang="ru"
  question="Команда перешла на REFRESH MATERIALIZED VIEW CONCURRENTLY, чтобы убрать блокировку чтений. Теперь refresh иногда не завершается, и накапливается отставание. Какова наиболее вероятная причина?"
  choices={[
    { label: "CONCURRENTLY ждёт, пока завершатся открытые транзакции, прежде чем стартовать, и против одного view одновременно идёт только один refresh. Постоянный поток долгих аналитических запросов раз за разом тормозит refresh, поэтому refresh-и встают в очередь друг за другом и не догоняют.", correct: true },
    { label: "CONCURRENTLY на самом деле не требует уникальный индекс, поэтому молча уходит в медленный путь.", misconception: "Он жёстко требует UNIQUE-индекс (обычные колонки, без выражений, без WHERE) и падает без него — никакого молчаливого отката. Стоп возникает из-за ожидания старых транзакций и самосериализации, а не из-за отсутствия индекса." },
    { label: "CONCURRENTLY разрешает несколько refresh параллельно, и они deadlock-ают друг с другом.", misconception: "Наоборот: против данного view одновременно идёт только один refresh. Никакого parallel-refresh deadlock нет; второй refresh просто встаёт в очередь за первым." },
    { label: "Concurrent refresh пересчитывает быстрее обычного, поэтому переназначается слишком часто.", misconception: "CONCURRENTLY обычно медленнее обычного refresh, потому что вычисляет дельту против существующих строк. Именно его медлительность плюс ожидание долгих транзакций и создают отставание." },
  ]}
/>

<Quiz
  id="04-materialized-views-choice-4"
  lessonSlug="04-materialized-views"
  lang="ru"
  question="Fact-таблица принимает ~5000 inserts/сек; аналитики весь день читают агрегатный MV и терпят ~5 минут staleness. Кто-то предлагает incremental maintenance через pg_ivm, чтобы view был всегда свежим. Почему это неверное решение?"
  choices={[
    { label: "pg_ivm ставит AFTER-триггеры, которые вычисляют и применяют дельту внутри каждой транзакции записи. При 5000 inserts/сек каждая запись платит эти издержки и конкурирует за блокировки view, душа ingest — ради нулевого staleness, когда потребитель уже согласен на 5 минут.", correct: true },
    { label: "pg_ivm вообще не умеет поддерживать агрегатные view, поэтому упадёт на запросе дашборда.", misconception: "pg_ivm поддерживает существенное подмножество, включая агрегаты (с SQL-ограничениями). Проблема не в возможностях, а в стоимости на каждую запись при 5000/сек — это и есть доминирующий путь стоимости." },
    { label: "Incremental maintenance не подходит для read-heavy таблиц по форме.", misconception: "Read-heavy — как раз там, где IVM блистает: чтения остаются дешёвыми и свежими. Здесь дисквалификатор — высокая частота записи, а не паттерн чтения." },
    { label: "pg_ivm работает только с одной базовой таблицей, а join-агрегат использует несколько.", misconception: "pg_ivm ставит триггеры на все базовые таблицы запроса и поддерживает многотабличные view. Здесь неподходящим его делает высокая частота записи, а не число таблиц." },
  ]}
/>

<Quiz
  id="04-materialized-views-choice-5"
  lessonSlug="04-materialized-views"
  lang="ru"
  question="Materialized view всё утро показывал вчерашние цифры. Ночной refresh-крон упал на lock timeout шесть дней назад, и ничего не алертило. В чём настоящий урок?"
  choices={[
    { label: "Неотслеживаемый refresh падает «открыто»: чтения продолжают успешно идти по последнему хорошему снапшоту, поэтому система выглядит зелёной, а данные молча неверны. Владейте refresh — алертите на успех/длительность и показывайте last-refreshed timestamp, чтобы staleness был обнаружим.", correct: true },
    { label: "Materialized view по природе ненадёжны; используйте обычное view, чтобы избежать этого режима отказа.", misconception: "Обычное view перевыполняет дорогой агрегат на каждом чтении, возвращая ту самую задержку, ради которой MV и существовал. Решение — владеть и мониторить refresh, а не отказываться от материализации." },
    { label: "Refresh должен был выбросить ошибку в дашборд, чтобы пользователи её увидели.", misconception: "Сбои refresh не доходят до читателей — чтения успешно идут по устаревшим сохранённым строкам. Именно эта развязка и требует внешнего алертинга на refresh-задачу, а не пользовательской ошибки." },
    { label: "Lock timeout означает, что база повреждена и нуждается в восстановлении.", misconception: "Lock timeout лишь значит, что refresh не смог вовремя взять блокировку; сохранённые данные целы, просто заморожены. Дефект — отсутствие алерта на упавшую задачу." },
  ]}
/>

<Quiz
  id="04-materialized-views-choice-6"
  lessonSlug="04-materialized-views"
  lang="ru"
  question="Incremental materialized view в ClickHouse (MV-on-insert) показывает неверные суммы: строк, существовавших до создания MV, нет, а некоторые группы разбиты по разным строкам. Что объясняет оба симптома?"
  choices={[
    { label: "MV в ClickHouse ведёт себя как AFTER INSERT-триггер на источнике: он видит только вставляемый блок. Он игнорирует уже существовавшие данные, пока вы не сделаете POPULATE, и агрегирует только внутри одного блока вставки — группы, разбитые между блоками, сводятся, только если движок целевой таблицы (например AggregatingMergeTree) их слияет.", correct: true },
    { label: "MV устарел и ему нужен ручной REFRESH, чтобы догнать.", misconception: "Incremental MV в ClickHouse поддерживаются на вставке, а не через refresh; шага REFRESH нет. Поведение — by design: он преобразует каждый вставляемый блок, а не исторические или межблоковые данные." },
    { label: "ClickHouse не умеет агрегацию в materialized view.", misconception: "Умеет — но агрегация идёт по каждому блоку вставки. Сведение между блоками — задача движка целевой таблицы, для чего и существует AggregatingMergeTree." },
    { label: "Базовой таблице нужен уникальный индекс, как для refresh CONCURRENTLY в Postgres.", misconception: "Это требование Postgres для другого механизма. Симптомы ClickHouse идут из поблоковой обработки и отсутствия POPULATE, а не из индекса." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна последовательность решений: убедиться, что чтение действительно дорогое и повторяемое, ограничить staleness, который терпит потребитель, затем подобрать refresh под частоту записи — плановый REFRESH CONCURRENTLY для staleness в минуты, incremental (pg_ivm) только для read-heavy таблиц с малой записью, streaming MV, чтобы вовсе убрать refresh-задачу. У каждой стратегии своя скрытая цена: CONCURRENTLY требует уникальный индекс и ждёт старые транзакции, incremental облагает каждую запись, MV в ClickHouse видят только вставляемый блок. А режим отказа везде один — неотслеживаемый refresh отдаёт устаревшие данные, пока система выглядит зелёной, поэтому владейте им и алертите на него.
</Recap>
