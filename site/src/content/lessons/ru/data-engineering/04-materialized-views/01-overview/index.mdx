---
slug: 01-overview
lang: ru
track: data-engineering
unit: 04-materialized-views
order: 1
level: junior
lessonType: topic
status: ready
title: "Материализованные представления: меняем устаревание и место на диске на скорость чтения"
summary: "Материализованное представление хранит результат дорогого запроса вместо пересчёта при каждом чтении. Главное решение — refresh: полный пересчёт блокирует читателей, инкрементальный нужен расширению, а упавший refresh молча отдаёт неверные агрегаты."
prereqs: []
deepensInto: []
spiral: []
concepts: ["materialized-view", "refresh-strategy", "incremental-view-maintenance", "staleness-window", "refresh-concurrently", "streaming-materialized-view"]
estMin: 16
sources:
  - https://www.postgresql.org/docs/current/sql-refreshmaterializedview.html
  - https://github.com/sraoss/pg_ivm
  - https://clickhouse.com/docs/materialized-view/incremental-materialized-view
  - https://materialize.com/blog/self-correcting-materialized-views/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
Дашборд по выручке отваливается по таймауту. 14-секундный агрегат по таблице фактов на 400M строк выполняется на каждой загрузке страницы, а совет директоров через час. Кто-то оборачивает это в материализованное представление: загрузка падает до 40мс, все радуются. Через три недели финансы эскалируют — дашборд всё утро показывает вчерашние цифры. Ночной cron с `REFRESH MATERIALIZED VIEW` молча упал по таймауту блокировки шесть дней назад, и никто не оповестил. Представление устарело не потому, что было неверным, — оно было верным; оно устарело, потому что refresh никому не принадлежал.
</Hook>

<Crux>Материализованное представление хранит результат запроса ради быстрого чтения; реально управляешь ты refresh-ем и устареванием.</Crux>

<Explanation>

## Обычное представление vs материализованное

Обычное представление (view) — это сохранённый запрос, имя для `SELECT`. Каждое чтение заново выполняет полный запрос по живым базовым таблицам, поэтому оно всегда свежее и ничего не хранит. Материализованное представление выполняет запрос один раз, пишет результат на диск как настоящее отношение и отдаёт чтения из этой хранимой копии. Чтение становится дешёвым сканом таблицы или поиском по индексу вместо 14-секундного join-а с агрегацией. В этом вся ценность: ты платишь за место и вычисления *заранее, один раз*, чтобы *многие* чтения стали дешёвыми.

Цена — корректность. Обычное представление не может устареть. Материализованное устаревает в тот же миг, как меняется базовая строка после последнего refresh, и остаётся неверным, пока не сделаешь refresh снова. Поэтому вопрос перестаёт быть «view или materialized view» и становится единственным, который важен: **как и как часто ты делаешь refresh — и какое окно устаревания терпит потребитель?**

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Свойство</th>
        <th class="border border-border px-3 py-2 text-left">Обычное представление</th>
        <th class="border border-border px-3 py-2 text-left">Материализованное</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Стоимость чтения</td>
        <td class="border border-border px-3 py-2">Полный запрос на каждое чтение</td>
        <td class="border border-border px-3 py-2">Скан/поиск по хранимым строкам</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Свежесть</td>
        <td class="border border-border px-3 py-2">Всегда актуально</td>
        <td class="border border-border px-3 py-2">На момент refresh — может устареть</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Место на диске</td>
        <td class="border border-border px-3 py-2">Нет</td>
        <td class="border border-border px-3 py-2">Весь результат на диске</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Влияние на запись</td>
        <td class="border border-border px-3 py-2">Нет</td>
        <td class="border border-border px-3 py-2">Стоимость refresh (полный) или дельта на запись (инкрементальный)</td>
      </tr>
    </tbody>
  </table>
</div>

## Полный refresh: просто, корректно — и блокировка, которую ты почувствуешь

Дефолт в Postgres — `REFRESH MATERIALIZED VIEW mv`. Он заново выполняет определяющий запрос с нуля, пересчитывая каждую строку. Это предельно просто и всегда сходится к верному ответу — и берёт блокировку `ACCESS EXCLUSIVE` на всё время. Пока эта блокировка держится, каждое чтение представления *блокируется*. Сделай refresh тяжёлого агрегата, который идёт 90 секунд, и ты только что сделал дашборд недоступным на 90 секунд — ровно тогда, когда кто-то его загружает.

Запасной выход — `REFRESH MATERIALIZED VIEW CONCURRENTLY`. Он строит новый результат в фоне и подменяет его построчными диффами, поэтому читатели продолжают видеть *старую* версию до завершения подмены — без блокирующей чтения блокировки. Но у него жёсткие требования и цена, которую сеньор обязан знать: ему нужен хотя бы один `UNIQUE`-индекс на представлении (только имена столбцов, без выражений, без `WHERE`), он обычно **медленнее** обычного refresh, потому что вычисляет дельту относительно существующих строк, и одновременно может идти только один refresh на представление. Он также ждёт, пока завершатся текущие транзакции, прежде чем стартовать, поэтому долгий отчётный запрос может бесконечно стопорить refresh.

<Inset kind="why" lang="ru">
«`CONCURRENTLY` не блокирует чтения» — правда, но ей легко переверить. Он блокирует *сам себя* — второй refresh встаёт в очередь за первым — и ждёт завершения старых транзакций перед стартом. При устойчивом потоке долгих аналитических запросов refresh может ждать так долго, что накапливается бэклог refresh-ей, которые никогда не догоняют: refresh, который, по сути, никогда не заканчивается.
</Inset>

## Инкрементальный refresh: платишь за запись, а не за чтение

Полный refresh пересчитывает всё, даже если изменилась одна строка. Инкрементальное сопровождение представления (IVM) вместо этого вычисляет только *дельту* — строки, затронутые недавними записями, — и применяет её к хранимому представлению. Стоимость переезжает из «дорогой периодический пересчёт» в «небольшая стоимость на каждую запись в базовую таблицу».

В чистом Postgres **нет нативного инкрементального refresh** — это каждый раз удивляет людей. Пробел закрывает расширение `pg_ivm`: `create_immv()` наполняет настоящую таблицу и ставит `AFTER`-триггеры на каждую базовую таблицу запроса. С этого момента каждый `INSERT/UPDATE/DELETE` запускает триггер, который вычисляет и применяет дельту *внутри той же транзакции*. Выигрыш — почти нулевое устаревание на выбранных представлениях. Цена ровно зеркальна полному refresh: каждая запись в базовую таблицу теперь чуть медленнее, сопровождение держит блокировки, которые душат пропускную способность записи, и есть ограничения SQL на то, что может содержать представление. Правило большого пальца от самого проекта: IVM окупается, когда базовая таблица читается много и меняется редко; при высоком темпе записи накладные расходы на каждую запись доминируют, и лучше делать запланированный полный refresh.

<TradeoffMatrix
  id="04-materialized-views-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="MV-дашборд в Postgres агрегирует таблицу фактов, в которую весь день идёт ~5000 вставок/сек; аналитики читают постоянно и терпят ~5 минут устаревания. Выбери стратегию refresh."
  options={[
    { name: "Запланированный REFRESH ... CONCURRENTLY раз в несколько минут, с уникальным индексом на представлении", summary: "Периодический неблокирующий пересчёт.", correct: true, justification: "Подходит под ограничения: бюджет устаревания в 5 минут означает, что свежесть на каждую запись не нужна, а CONCURRENTLY держит читателей на предыдущей версии во время пересборки, а не блокирует их. Уникальный индекс — плата за вход. Ты также не нагружаешь путь записи в 5000 вставок/сек, который тут доминирующая стоимость. Добавь мониторинг и алерты на задачу refresh, чтобы молчаливый сбой не отдавал устаревшие данные незаметно." },
    { name: "Инкрементальное сопровождение pg_ivm, чтобы представление всегда было свежим", summary: "Дельта на запись.", justification: "Верная форма для таблиц с малой записью и большим чтением, но тут ровно неверная: 5000 вставок/сек означает, что каждая из этих записей платит за триггер + сопровождение дельты и конкурирует за блокировки представления, душа ингест. Нагрузка терпит 5 минут устаревания, так что платить за нулевое устаревание — это покупать корректность, которую никто не просил, ценой пропускной способности, которую чувствуют все." },
    { name: "Обычный REFRESH MATERIALIZED VIEW (не concurrent) по cron", summary: "Пересчёт с блокировкой.", justification: "Просто и корректно, но не-concurrent refresh берёт блокировку ACCESS EXCLUSIVE, поэтому каждое чтение аналитика блокируется на всё время пересчёта. На тяжёлом агрегате это повторяющийся многосекундный простой дашборда — именно тогда, когда люди читают. CONCURRENTLY убирает блокировку чтений ценой уникального индекса." },
    { name: "Убрать MV и взять обычное представление, чтобы всегда было свежо", summary: "Заново на каждое чтение.", justification: "Свежесть бесплатна, но ты возвращаешь исходную проблему: дорогой агрегат выполняется на каждой загрузке страницы по таблице на 400M строк. MV существовало именно чтобы этого избежать. Ограничение — задержка чтения, а не свежесть; обычное представление оптимизирует измерение, которое тебе не важно." },
  ]}
/>

## Потоковые материализованные представления: сопровождаются, а не обновляются

Вне Postgres другая модель убирает решение о refresh целиком: представление сопровождается непрерывно по мере прихода данных. Инкрементальное MV в ClickHouse ведёт себя как `AFTER INSERT`-триггер на самой левой исходной таблице — каждый вставленный *блок* строк трансформируется и пишется в целевую таблицу в момент вставки, читая только новые строки из RAM, а не с диска. Подвох, который сеньор обязан усвоить: оно видит только вставляемый блок. Оно **не** обрабатывает данные, существовавшие до создания представления (для этого нужен `POPULATE`), а агрегация группирует строки только *внутри одного блока вставки* — раскинь группу по двум блокам, и они не сольются, если только целевой движок (например, `AggregatingMergeTree`) не сведёт их при слиянии.

Специализированные потоковые базы идут дальше. Materialize компилирует SQL в *differential dataflow* (из Naiad от Microsoft Research), который работает с изменениями как с кортежами `(data, time, diff)` и делает работу пропорционально размеру изменения, а не размеру набора данных, — сопровождая сложные join-ы и агрегации с миллисекундной задержкой и строгой сериализуемостью. Концептуальный сдвиг: нет задачи «refresh», которую надо планировать, мониторить или которая может молча упасть. Представление по построению всегда согласовано со своими входами. Ты меняешь операционную нагрузку refresh на новую систему в эксплуатации и другую модель согласованности — eventually-consistent чтения в некоторых конфигурациях могут удивить пользователей, ждущих read-your-writes.

<Quiz
  id="04-materialized-views-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Почему обычный REFRESH MATERIALIZED VIEW (без CONCURRENTLY) делает дашборд недоступным во время refresh?"
  choices={[
    { label: "Он берёт блокировку ACCESS EXCLUSIVE на весь refresh, поэтому чтения представления блокируются до завершения", correct: true },
    { label: "Он сначала удаляет строки представления, поэтому чтения возвращают пусто до повторного наполнения", misconception: "Это блокировка, а не окно пустоты; читатели блокируются, а не видят частичные данные. Старую версию во время пересборки оставляет читаемой именно CONCURRENTLY." },
    { label: "Refresh всегда падает на нагруженной таблице, и дашборд показывает ошибку", misconception: "Refresh не падает только из-за того, что таблица нагружена; проблема доступности чтения — это удерживаемая блокировка, а не ошибка." },
    { label: "Материализованные представления пересчитываются на каждое чтение, как обычные", misconception: "Это описание обычного представления. Материализованное отдаёт хранимые строки; стоимость возникает во время refresh, а не во время чтения." },
  ]}
/>

<Quiz
  id="04-materialized-views-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Когда pg_ivm (инкрементальное сопровождение) — неверный выбор?"
  choices={[
    { label: "Когда у базовых таблиц очень высокий темп записи — накладные расходы на триггеры и блокировки на каждую запись душат пропускную способность", correct: true },
    { label: "Когда представление часто читают", misconception: "Большое чтение — это ровно там, где IVM блистает: чтения остаются дешёвыми, а представление держится свежим. Цена на стороне записи, а не чтения." },
    { label: "Когда можешь терпеть нулевое устаревание", misconception: "Нулевое устаревание — это то, что даёт IVM; это его суть, а не причина его избегать." },
    { label: "Когда базовую таблицу меняют редко", misconception: "Редкие изменения — идеальный случай: мало накладных расходов на сопровождение при свежем представлении. Проблема — высокая частота записи, а не низкая." },
  ]}
/>

<DragOrder
  id="04-materialized-views-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь решения, которые принимает сеньор, когда тянется к материализованному представлению:"
  items={[
    { id: "a", label: "Убедись, что чтение действительно дорогое и повторяющееся — иначе хватит обычного представления или индекса" },
    { id: "b", label: "Реши окно устаревания, которое потребитель реально терпит (секунды? минуты? сутки?)" },
    { id: "c", label: "Выбери refresh: терпит минуты → запланированный REFRESH CONCURRENTLY; нужна свежесть-сейчас + мало записей → инкрементальный (pg_ivm)" },
    { id: "d", label: "Если CONCURRENTLY — добавь требуемый UNIQUE-индекс на представлении" },
    { id: "e", label: "Добавь мониторинг и алерты на задачу refresh, чтобы молчаливый сбой не отдавал устаревшие данные незаметно" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Материализуй, только когда чтения дорогие и повторяются. Дальше владей refresh: полный пересчёт блокирует читателей (бери CONCURRENTLY), инкрементальный платит за каждую запись, немониторимый refresh молча врёт.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-materialized-views-retrieval"
  lang="ru"
  questions={[
    { q: "Коллега предлагает материализованное представление, чтобы ускорить медленный отчёт, и планирует делать REFRESH ночью. Какие три риска ты поднимешь до одобрения?", a: "Первый — устаревание: ночной refresh означает, что отчёт отстаёт до 24 часов, поэтому подтверди, что потребитель терпит это окно — финансовый или комплаенс-дашборд часто не может. Второй — блокировка: обычный REFRESH MATERIALIZED VIEW берёт блокировку ACCESS EXCLUSIVE на всё время, блокируя каждое чтение представления; если refresh идёт, пока кто-то читает, отчёт становится недоступным. Переключись на REFRESH ... CONCURRENTLY, который держит старую версию читаемой во время пересборки, и добавь требуемый ему UNIQUE-индекс. Третий, и тот, что бьёт сильнее всего в проде: владение и мониторинг. Задача refresh, которая падает молча — таймаут блокировки, OOM, деплой, снёсший cron, — оставляет представление замороженным в последнем хорошем состоянии, пока чтения продолжают проходить, поэтому ничего не выглядит сломанным. Данные неверны, но система зелёная. Нужны алерты на успех/длительность refresh и видимая метка времени последнего обновления, чтобы устаревание было обнаружимым, а не вскрытым через недели разъярённым стейкхолдером." },
    { q: "Объясни разницу между полным refresh, инкрементальным сопровождением (pg_ivm) и потоковым материализованным представлением, и что каждое оптимизирует.", a: "Полный refresh пересчитывает весь результат из базовых таблиц по расписанию; это просто и всегда корректно, но платит полную стоимость запроса каждый раз и (не-concurrently) блокирует читателей — он оптимизирует простоту и терпит окно устаревания, равное интервалу refresh. Инкрементальное сопровождение через pg_ivm в Postgres ставит триггеры на базовые таблицы и применяет только дельту от каждой записи внутри той же транзакции, давая почти нулевое устаревание; оно оптимизирует свежесть на таблицах с большим чтением и малой записью, а его цена платится на каждой записи плюс ограничения SQL — при высоком темпе записи эти накладные на запись доминируют. Потоковые материализованные представления (MV-на-вставку в ClickHouse, differential dataflow у Materialize) сопровождают представление непрерывно по мере прихода данных, делая работу пропорционально изменению, а не набору данных; они оптимизируют real-time корректность без задачи refresh, которую надо планировать или которая может упасть, ценой эксплуатации другой системы и рассуждений о её модели согласованности. ClickHouse именно видит только вставленный блок — игнорирует уже существующие данные без POPULATE и агрегирует только внутри блока." },
  ]}
/>

<Recap lang="ru">
Материализованное представление хранит результат дорогого запроса, чтобы многие чтения стали дешёвыми сканами вместо повторного пересчёта — ты платишь за место и вычисления один раз, заранее, чтобы амортизировать задержку чтения. В отличие от обычного представления, которое заново выполняется на каждое чтение и потому всегда свежее, материализованное устаревает в момент изменения базовой строки, поэтому единственное важное решение — refresh. Полный refresh прост и корректен, но берёт блокировку ACCESS EXCLUSIVE, которая блокирует чтения; REFRESH ... CONCURRENTLY избегает блокировки чтений ценой требуемого уникального индекса, меньшей скорости и ожидания старых транзакций. В Postgres нет нативного инкрементального refresh, поэтому pg_ivm применяет дельты на запись через триггеры — отлично для таблиц с большим чтением и малой записью, губительно при высоком темпе записи. Потоковые материализованные представления (ClickHouse на вставку, differential dataflow у Materialize) обходят решение о refresh, сопровождая представление непрерывно, меняя задачу refresh на новую систему и модель согласованности. Повторяющийся для всех них режим отказа: немониторимый refresh, упавший молча, отдаёт устаревшие данные, пока всё выглядит зелёным, — поэтому владей refresh, ограничивай окно устаревания и ставь на него алерты.
</Recap>
