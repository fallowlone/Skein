---
concepts:
- materialized-view
- refresh-strategy
- refresh-concurrently
- incremental-view-maintenance
- staleness-window
- streaming-materialized-view
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.postgresql.org/docs/current/sql-refreshmaterializedview.html
- https://github.com/sraoss/pg_ivm
- https://materialize.com/blog/self-correcting-materialized-views/
status: ready
summary: "Промпты на свободное припоминание по всему юниту. Сначала ответьте по памяти, затем раскройте модельный ответ и сравните."
title: 'Materialized views: тест на припоминание'
track: data-engineering
unit: 04-materialized-views
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. Для каждого промпта проговорите или запишите полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет компромиссы к моменту, когда вы окажетесь на дизайн-ревью.
</Hook>

<Goal lang="ru">
Восстановите ключевые механизмы юнита — что даёт materialized view, лестницу refresh, скрытые издержки CONCURRENTLY, incremental maintenance, streaming MV и режим отказа со staleness — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:visible
  id="04-materialized-views-short-retrieval"
  lang="ru"
  questions={[
    { q: "Что на самом деле даёт materialized view по сравнению с обычным view и какое одно решение становится главным, когда он у вас есть?", a: "Обычное view — это просто сохранённый запрос: каждое чтение перевыполняет полный SELECT к живым базовым таблицам, поэтому оно всегда свежее, но платит полную стоимость запроса на каждом чтении. Materialized view выполняет запрос один раз, сохраняет строки результата как настоящее отношение на диске и отдаёт чтения как дешёвый scan или поиск по индексу. Вы платите за хранение и вычисление заранее, один раз, чтобы множество чтений были дешёвыми — в этом вся ценность. Цена, которую вы берёте на себя, — корректность во времени: view устаревает в момент изменения базовой строки после последнего refresh. Поэтому единственным значимым решением становится то, как и как часто делать refresh и какое окно staleness терпит потребитель." },
    { q: "Сравните обычный REFRESH MATERIALIZED VIEW и REFRESH ... CONCURRENTLY, включая требования и издержки CONCURRENTLY.", a: "Обычный REFRESH перевыполняет определяющий запрос с нуля и берёт блокировку ACCESS EXCLUSIVE на всё своё время, поэтому каждое чтение view блокируется до завершения — обновите 90-секундный агрегат, и дашборд недоступен 90 секунд. REFRESH ... CONCURRENTLY строит новый результат в фоне и подменяет его построчными diff-ами, поэтому читатели продолжают видеть старую версию без блокирующей чтения блокировки. Издержки: ему нужен минимум один UNIQUE-индекс на view (обычные колонки, без выражений, без WHERE); он обычно медленнее обычного refresh, потому что считает дельту против существующих строк; против данного view одновременно идёт только один refresh; и он ждёт завершения открытых транзакций перед стартом, поэтому долгие аналитические запросы могут его тормозить и копить отставание refresh, которое не догоняет." },
    { q: "Что такое incremental view maintenance, как pg_ivm реализует его в Postgres и когда он окупается, а когда вредит?", a: "Incremental view maintenance (IVM) считает только дельту — строки, затронутые недавними записями — и применяет её к сохранённому view вместо полного пересчёта. В ванильном Postgres нет нативного incremental refresh; pg_ivm закрывает пробел. create_immv() наполняет настоящую таблицу и ставит AFTER-триггеры на все базовые таблицы запроса, поэтому каждый INSERT/UPDATE/DELETE вычисляет и применяет дельту внутри той же транзакции, давая почти нулевой staleness. Он окупается, когда базовая таблица read-heavy и меняется редко: чтения остаются дешёвыми и свежими при малой стоимости поддержки. Он вредит при высокой частоте записи — каждая запись теперь платит издержки триггера и поддержки дельты и конкурирует за блокировки view, душа throughput — поэтому там лучше плановый полный refresh. Он также накладывает SQL-ограничения на содержимое view." },
    { q: "Как streaming materialized views (ClickHouse, Materialize) меняют модель и какой подвох должен усвоить senior для каждого?", a: "Streaming MV убирают решение о refresh: view поддерживается непрерывно по мере поступления данных, поэтому нет refresh-задачи, которую надо планировать, мониторить или которая молча падает. Incremental MV в ClickHouse ведёт себя как AFTER INSERT-триггер на источнике — каждый вставляемый блок преобразуется и пишется в целевую таблицу в момент вставки, читая только новые строки. Подвох: он видит только вставляемый блок, поэтому игнорирует данные, существовавшие до создания MV, пока вы не сделаете POPULATE, и агрегирует только внутри одного блока вставки — группы, разбитые между блоками, сводятся, только если движок целевой таблицы (например AggregatingMergeTree) их слияет. Materialize компилирует SQL в differential dataflow, который делает работу пропорционально размеру изменения, поддерживая сложные join-ы и агрегации с миллисекундной задержкой при строгой сериализуемости. Его подвох операционный и концептуальный: это отдельная система для эксплуатации, и нужно рассуждать о её модели согласованности — eventually-consistent чтения в некоторых конфигурациях удивляют тех, кто ждёт read-your-writes." },
    { q: "Коллега предлагает materialized view с ночным refresh, чтобы ускорить медленный отчёт. Какие три риска вы поднимаете до одобрения?", a: "Первый — staleness: ночной refresh означает, что отчёт может отставать до 24 часов, поэтому подтвердите, что потребитель терпит это окно — финансовые и комплаенс-дашборды часто не могут. Второй — блокировка: обычный REFRESH берёт блокировку ACCESS EXCLUSIVE на всё своё время и блокирует каждое чтение view, поэтому если он идёт во время чьего-то чтения, отчёт становится недоступен; перейдите на REFRESH ... CONCURRENTLY и добавьте требуемый им уникальный индекс. Третий, и самый болезненный: ответственность и мониторинг. Refresh, падающий молча — lock timeout, OOM, потерянный крон — оставляет view замороженным на последнем хорошем состоянии, пока чтения успешно идут, поэтому ничего не выглядит сломанным; данные неверны, а система зелёная. Нужны алерты на успех и длительность refresh плюс видимый last-refreshed timestamp, чтобы устаревшие данные были обнаружимы, а не найдены через недели разгневанным стейкхолдером." },
    { q: "Упорядочьте решения, которые senior принимает, берясь за materialized view, и скажите, почему ответственность идёт последней, но важнее всего.", a: "(1) Подтвердить, что чтение действительно дорогое и повторяемое — иначе достаточно обычного view или индекса на базовой таблице, и вы вовсе избегаете проблемы staleness. (2) Решить, какое окно staleness реально терпит потребитель: секунды, минуты или сутки. (3) Подобрать refresh под частоту записи: терпимость в минуты при высокой записи указывает на плановый REFRESH CONCURRENTLY; свежесть-сейчас на read-heavy таблице с малой записью — на incremental (pg_ivm); real-time на потоке — на streaming MV. (4) Если CONCURRENTLY, добавить требуемый UNIQUE-индекс на view. (5) Добавить мониторинг и алертинг на refresh-задачу. Ответственность идёт последней по порядку, но важнее всего, потому что любой другой выбор деградирует мягко — в задержку или стоимость, тогда как неотслеживаемый refresh деградирует в молчаливую отдачу неверных данных — отказ, который повторяется во всех стратегиях юнита." },
  ]}
/>

<Recap lang="ru">
Если вы смогли восстановить каждый ответ по памяти, вы держите хребет юнита: materialized view обменивает свежесть на скорость чтения, поэтому refresh — единственное значимое решение; обычный refresh блокирует чтения блокировкой ACCESS EXCLUSIVE, тогда как CONCURRENTLY избегает этого ценой уникального индекса, более медленных diff-ов и ожидания старых транзакций; pg_ivm даёт почти нулевой staleness, но облагает каждую запись, поэтому подходит только для read-heavy таблиц с малой записью; streaming MV убирают refresh-задачу, но добавляют систему и модель согласованности; а сквозной режим отказа везде один — неотслеживаемый refresh отдаёт устаревшие данные, пока всё выглядит зелёным — поэтому владейте refresh, ограничивайте окно staleness и алертите на него.
</Recap>
