---
concepts:
- elt-vs-etl
- storage-compute-separation
- medallion-architecture
- idempotent-loads
- incremental-models
- warehouse-cost
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.getdbt.com/blog/elt-best-practices-snowflake
- https://docs.getdbt.com/docs/build/incremental-models
status: ready
summary: "Синтез всего юнита ELT vs ETL в формате выбора: где идёт Transform, реплейабельность, стоимость warehouse, идемпотентность и контракт medallion."
title: 'ELT vs ETL: тест с выбором ответа'
track: data-engineering
unit: 02-elt-vs-etl
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов сквозь весь юнит. Каждый — решение, которое ты реально принимаешь при проектировании пайплайна: не определение для зубрёжки, а компромисс, который надо взвесить против цены, реплея и комплаенса.
</Hook>

<Goal lang="ru">
Убедись, что связываешь, где идёт Transform, с его последствиями вниз по потоку: реплейабельность, счёт warehouse, дисциплина схемы и идемпотентность, которая не даёт повтору удвоить данные.
</Goal>

<Quiz
  id="02-elt-vs-etl-choice-1"
  lessonSlug="02-elt-vs-etl"
  lang="ru"
  question="Какое одно архитектурное изменение в облачных warehouse (Snowflake, BigQuery) — настоящая причина, по которой индустрия перешла от ETL к ELT?"
  choices={[
    { label: "Storage и compute разделили. Storage стал дешёвым объектным хранилищем, поэтому посадить всё сырьё почти ничего не стоит; compute стал эластичным, поэтому платишь только когда идёт трансформ. Это сняло ограничение, заставлявшее трансформировать до загрузки.", correct: true },
    { label: "SQL-движки стали быстрее Spark на любой нагрузке.", misconception: "Чистая скорость запросов — не драйвер: Spark всё ещё выигрывает многие тяжёлые джобы. Переход был экономическим и архитектурным: дешёвый разделённый storage сделал посадку сырья доступной, чего ETL никогда не мог." },
    { label: "Вышел dbt и дал командам инструмент для трансформов в warehouse.", misconception: "dbt оседлал волну, а не создал её. Включающим изменением было разделение storage/compute. dbt — лишь доминирующий способ писать in-warehouse T после того, как это стало возможным." },
    { label: "Объёмы данных уменьшились, поэтому трансформировать в warehouse стало дёшево.", misconception: "Объёмы выросли, а не упали. ELT стал жизнеспособен вопреки бо́льшим объёмам именно потому, что разделённый storage сделал хранение всего этого дешёвым." },
  ]}
/>

<Quiz
  id="02-elt-vs-etl-choice-2"
  lessonSlug="02-elt-vs-etl"
  lang="ru"
  question="Ты находишь баг таймзоны в трансформации, которая полгода выдавала неверные числа. При ELT с medallion-архитектурой какой быстрый и правильный фикс?"
  choices={[
    { label: "Чинишь SQL в silver-модели и перезапускаешь её по сырым bronze-данным, которые у тебя уже есть, — без re-extract из источника.", correct: true },
    { label: "Переэкстрагировать полгода истории из source API и пересобрать всё.", misconception: "Это режим провала ETL — re-extract нужен только когда сырьё выброшено. ELT держит сырьё в bronze постоянно ровно для того, чтобы переигрывать трансформы, не трогая источник, который мог измениться или заблокировать тебя по rate-limit." },
    { label: "Пропатчить выходную gold-таблицу на месте через UPDATE.", misconception: "Ручная мутация gold ломает контракт пересобираемости и невоспроизводима — следующий полный запуск тихо откатит твой патч. Чини трансформацию, а не вывод." },
    { label: "Ничего нельзя сделать; трансформированная история неизменна после записи.", misconception: "Это верно только в ETL, где сырьё выбросили. Вся суть ELT в том, что bronze сохранён, поэтому трансформированная история всегда переигрываема." },
  ]}
/>

<Quiz
  id="02-elt-vs-etl-choice-3"
  lessonSlug="02-elt-vs-etl"
  lang="ru"
  question="dbt-модель поставили на full-refresh по умолчанию и расписание ежечасно; она пересобирает фактовую таблицу на 2 ТБ с нуля каждый запуск, и счёт Snowflake вырос на 40%. Вывод корректен. Где баг и каков фикс?"
  choices={[
    { label: "Баг в материализации, а не в логике. ELT переносит compute трансформа на тарифицируемый warehouse, поэтому полная пересборка, сканируемая ежечасно, тарифицирует каждый байт. Сделай модель инкрементальной — обрабатывай только строки новее текущего максимума — а full-refresh оставь на случай смены логики.", correct: true },
    { label: "SQL неверен; корректный вывод не может быть ещё и багом стоимости.", misconception: "Корректность и стоимость — независимые оси в ELT. Идеально корректный трансформ, материализованный как ежечасная полная пересборка, — это четырёхзначный пункт счёта; счёт и есть баг, даже когда числа верны." },
    { label: "Snowflake неверно тарифицирует нагрузку; открой тикет в поддержку.", misconception: "Тарификация работает как задумано — ты платишь за warehouse-секунду за compute, который запросил. Фикс на твоей стороне: перестань пересканировать 2 ТБ ежечасно, перейдя на инкрементальность." },
    { label: "Увеличь размер warehouse, чтобы каждый запуск завершался быстрее и стоил меньше.", misconception: "Бо́льший warehouse завершает быстрее, но тарифицирует больше кредитов в секунду; суммарная стоимость того же полного скана почти не меняется. Рычаг — сканировать меньше данных (инкрементально), а не сканировать те же данные быстрее." },
  ]}
/>

<Quiz
  id="02-elt-vs-etl-choice-4"
  lessonSlug="02-elt-vs-etl"
  lang="ru"
  question="Регулируемый финтех принимает платёжные события с PAN-ами карт, и комплаенс запрещает сырым данным держателя карты когда-либо лежать в аналитическом warehouse. Какой паттерн подходит и почему современный дефолт ELT здесь неверен?"
  choices={[
    { label: "ETL: маскируй/токенизируй PAN-ы в контролируемом движке до загрузки и грузи только compliant-результат. Warehouse никогда не хранит сырую PII, сужая аудит-поверхность — принятая цена в потере реплейабельности сырья для этих полей.", correct: true },
    { label: "ELT: посадить сырые события в bronze, потом маскировать в silver-трансформе.", misconception: "Это правильный дефолт для большинства данных, но проигрывает по жёсткому ограничению: сырые PAN физически садятся в bronze до любой маскировки, поэтому данные держателя карты лежат в warehouse — ровно то, что комплаенс запрещает." },
    { label: "ELT с column-level динамической маскировкой на момент запроса.", misconception: "Динамическая маскировка прячет PII от читателей, но сырые значения всё равно физически хранятся в bronze. Маскировка — контроль на момент view, а не контроль хранения; хранимое-но-скрытое всё равно нарушает «никогда не лежит в warehouse»." },
    { label: "ELT теперь всегда верен; удовлетвори комплаенс политиками row access.", misconception: "Политики доступа управляют тем, кто читает данные, а не тем, хранятся ли они. Правило здесь о хранении, поэтому ни один in-warehouse контроль его не удовлетворит — маскировка должна идти до загрузки." },
  ]}
/>

<Quiz
  id="02-elt-vs-etl-choice-5"
  lessonSlug="02-elt-vs-etl"
  lang="ru"
  question="EL-инструмент повторил частично-успешную загрузку, и твоя фактовая таблица выручки теперь показывает завышенные итоги. Какого свойства не хватало и каков надёжный дизайн-фикс?"
  choices={[
    { label: "Загрузка была не идемпотентной — перезапуск частичного батча вставил те же строки снова. Сделай загрузки и инкрементальные трансформы идемпотентными через merge/upsert по unique_key, чтобы повтор обновлял строки на месте, а не аппендил дубли.", correct: true },
    { label: "EL-инструмент глючный; отключи повторы, чтобы загрузка никогда не шла дважды.", misconception: "Повторы при временном сбое — корректное поведение, и нельзя полагаться на то, что повтора не будет. Фикс — сделать загрузку безопасной к перезапуску (идемпотентной), а не запретить перезапуск." },
    { label: "Warehouse должен был сам отклонить дублирующиеся строки.", misconception: "У append-загрузки нет ключа для дедупликации, поэтому warehouse спокойно хранит дубли. Дедупликация — твой дизайн-выбор (unique_key с merge), а не автоматическая гарантия." },
    { label: "Добавь ночной джоб, удаляющий дубли постфактум.", misconception: "Джоб-чистильщик — пластырь, который гонится за каждой новой загрузкой и может удалить легитимные строки. Идемпотентные загрузки не дают дублю записаться вообще — это и есть настоящий фикс." },
  ]}
/>

<Quiz
  id="02-elt-vs-etl-choice-6"
  lessonSlug="02-elt-vs-etl"
  lang="ru"
  question="Кто-то называет schema-on-read «чистой свободой — никакой схемы, с которой надо драться на загрузке». Что, по версии юнита, он упускает?"
  choices={[
    { label: "Schema-on-read — это отложенный счёт, а не бесплатность. Schema-on-write в ETL отвергает кривую строку на загрузке, и ты узнаёшь сразу; ELT садит в bronze что угодно, и нарушение контракта всплывает позже в silver как тихий NULL или неверный джойн. Работа со схемой уезжает вниз по потоку и становится твоей задачей — обеспечивать её в тестах.", correct: true },
    { label: "Ничего — schema-on-read действительно убирает работу со схемой.", misconception: "Он убирает работу со схемой только на момент загрузки. Работа возвращается ниже по потоку: ты обязан обеспечить структуру в silver тестами, иначе плохие данные тихо испортят gold." },
    { label: "Schema-on-read медленнее в запросах, чем schema-on-write.", misconception: "Дело не в скорости запросов, а в том, когда происходит валидация. Цена пропуска валидации на загрузке — тихие баги качества данных, всплывающие поздно, а не медленные чтения." },
    { label: "Schema-on-read применим только к JSON, а не к табличным данным.", misconception: "Он применим к любому формату, посаженному сырьём. Суть в отсрочке обеспечения до момента чтения/трансформа независимо от формы источника." },
  ]}
/>

<Recap lang="ru">
Сквозная нить: где идёт Transform — решает всё ниже по потоку. Разделённые storage/compute сделали посадку сырья дешёвой, что покупает реплейабельность через контракт medallion (неизменный bronze, очищенный silver, готовый к бизнесу gold). Но T теперь тарифицируется на счёте warehouse, поэтому идёшь инкрементально по умолчанию. А поскольку загрузчики повторяют, каждая загрузка обязана быть идемпотентной — merge по unique_key — иначе повтор удвоит данные. ELT — дефолт; ETL всё ещё побеждает, когда жёсткое правило говорит, что сырая PII не должна касаться warehouse. Теперь, когда ревьюишь дизайн пайплайна или dbt-модель, задаёшь себе три вопроса: где идёт трансформ, инкрементальна ли модель и есть ли у неё unique_key — эти три ответа предсказывают счёт, качество данных и безопасность повтора.
</Recap>
