---
concepts:
- elt-vs-etl
- replayability
- medallion-architecture
- idempotent-loads
- incremental-models
- schema-on-read
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.getdbt.com/blog/elt-best-practices-snowflake
- https://docs.getdbt.com/docs/build/incremental-models
status: ready
summary: "Промпты на свободное припоминание по юниту ELT vs ETL. Сначала ответь своими словами, потом раскрой модельный ответ и сравни."
title: 'ELT vs ETL: тест на свободное припоминание'
track: data-engineering
unit: 02-elt-vs-etl
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт скажи или напиши полный ответ по памяти, прежде чем открыть модельный, — именно усилие припоминания закрепляет рассуждения о пайплайне.
</Hook>

<Goal lang="ru">
Восстанови хребет юнита, не подглядывая: почему индустрия перешла, что покупает воспроизводимость данных, контракт medallion, куда уехала стоимость, и два свойства — инкрементальность и идемпотентность, — которые держат пайплайн warehouse дешёвым и корректным.
</Goal>

<RetrievalDrawer
  client:load
  id="02-elt-vs-etl-short-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, почему индустрия перешла от ETL к ELT и что ты отдал в обмен.", a: "Переход стал возможен благодаря облачным warehouse (Snowflake, BigQuery), разделившим storage и compute. Storage стал дешёвым объектным хранилищем, поэтому посадить всё сырьё почти ничего не стоит; compute стал эластичным, поэтому платишь только когда трансформ реально идёт. Это сняло старое ограничение, заставлявшее трансформировать до загрузки. Что отдаёшь: стоимость трансформа теперь падает на тарифицируемый счёт warehouse вместо кластера, которым ты владел; сырая PII физически садится до того, как ты её замаскируешь; обеспечение схемы уезжает с момента загрузки (schema-on-write) в твои же тесты ниже по потоку (schema-on-read). Для большинства данных обмен стоит того; для жёстких комплаенс-линий по PII трансформ-до-загрузки в ETL всё ещё побеждает." },

    { q: "Что такое воспроизводимость данных в ELT и почему это самая глубокая причина, по которой ELT стал дефолтом, — глубже цены?", a: "Воспроизводимость — способность заново вывести любую таблицу ниже по потоку из сохранённого сырого ввода. В ETL сырые исходные данные выбрасываются после трансформации; warehouse держит только трансформированный вывод. Поэтому когда ты находишь баг трансформа — неверный ключ джойна, ошибку таймзоны, — ты не можешь починить историю; приходится переэкстрагировать из источника, который мог измениться, заблокировать тебя по rate-limit или сбросить старые строки. В ELT сырьё живёт в warehouse постоянно, поэтому баг трансформа — это однострочный SQL-фикс плюс перезапуск по данным, которые у тебя уже есть. Экономия реальна, но вторична; воспроизводимость делает ELT-пайплайны долговечными и исправимыми — поэтому это решающее преимущество." },
    { q: "Опиши medallion-архитектуру и тот единственный контракт, что держит её здоровой.", a: "Medallion — трёхслойная структура ELT. Bronze — сырые, append-only загруженные данные, никогда не мутируются. Silver — очищенные, приведённые, дедуплицированные таблицы, где схема реально обеспечивается. Gold — готовые к бизнесу агрегаты и витрины, которые читает BI. Каждый слой пересобираем из нижнего, а bronze пересобираем из источника только в крайнем случае. Контракт, держащий её здоровой: никогда не мутируй bronze, всегда трансформируй вперёд. Пока bronze неизменен, а каждый слой ниже по потоку — чистая функция от слоя под ним, ты можешь пересобрать silver и gold когда угодно после любого фикса трансформа — ровно та воспроизводимость, что обещает ELT." },
    { q: "Куда уехала стоимость, когда индустрия перешла на ELT, и какая одна самая дорогая ошибка?", a: "ELT не делает трансформацию бесплатной — он переносит стоимость с CapEx-кластера, которым ты владел, на тарифицируемую OpEx-строку счёта warehouse (за warehouse-секунду в Snowflake, за просканированные байты в BigQuery). Эта строка зверски чувствительна к тому, как ты пишешь SQL. Самая дорогая ошибка — модель, материализованная как полная пересборка таблицы, поставленная часто по расписанию, сканирующая всё на каждом запуске — например, SELECT * по непартиционированной фактовой таблице на 2 ТБ ежечасно, что само по себе четырёхзначный месячный пункт. Фикс — инкрементальная материализация плюс отдельные warehouse на команду и auto-suspend на простое, чтобы тяжёлые трансформы не голодили BI и не сливали кредиты впустую." },
    { q: "Что значит, что загрузка идемпотентна, почему загрузки в warehouse обязаны быть идемпотентными и как этого добиться в dbt?", a: "Идемпотентно — значит запуск загрузки дважды даёт тот же результат, что запуск один раз. Загрузки в warehouse обязаны быть идемпотентными, потому что EL-инструменты (Fivetran, Airbyte) и твои загрузчики повторяют при временном сбое по дизайну — это корректно — поэтому любая неидемпотентная загрузка рано или поздно перезапустит частично-успешный батч и вставит те же строки дважды, завышая итоги и убивая доверие к дашборду. В dbt этого добиваешься инкрементальной моделью со стратегией incremental_strategy='merge' и unique_key (например, event_id): merge обновляет строку, если ключ уже есть, и вставляет, только если нет, поэтому повтор делает upsert на месте вместо аппенда. Классическая ловушка — инкрементальная модель без unique_key и без фильтра is_incremental() — dbt тихо аппендит весь вывод на каждом запуске, дублируя строки и пересканируя всё." },
    { q: "Как безопасно прогнать 90-дневный backfill после фикса бага трансформа и почему microbatch-модели — правильный инструмент?", a: "Backfill заново выводит историческую выдачу из сохранённого сырья — что ELT делает возможным, потому что bronze всё ещё его держит. Наивный подход, один full-refresh на 90 дней, идёт одной долгой транзакцией: может ловить таймауты, а любое падение отбрасывает весь прогресс. Microbatch-модели разбивают диапазон на независимые временны́е батчи (например, по дню), каждый атомарно заменяет своё окно. Упавший день переигрывается изолированно, не трогая соседей, батчи могут идти параллельно, а повтор того же окна идемпотентен. Поэтому backfill становится перезапускаемым и ограниченным на батч — ты возобновляешься с упавшего дня вместо перезапуска всех 90 дней." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти — ты держишь хребет юнита: разделённые storage/compute включили переход; воспроизводимость данных (а не цена) — его самый глубокий выигрыш; контракт medallion — неизменный bronze, трансформируй вперёд — делает реплей надёжным; стоимость уехала на тарифицируемый warehouse, поэтому идёшь инкрементально по умолчанию; каждая загрузка обязана быть идемпотентной (merge по unique_key), потому что загрузчики повторяют; а microbatch превращает долгий backfill в перезапускаемые, независимые, идемпотентные единицы. Теперь, когда видишь сбой пайплайна в продакшене, ты автоматически спрашиваешь: была ли загрузка идемпотентной, была ли модель инкрементальной и структурирован ли backfill как ограниченные на батч единицы — эти три вопроса покрывают корневую причину большинства ELT-инцидентов.
</Recap>
