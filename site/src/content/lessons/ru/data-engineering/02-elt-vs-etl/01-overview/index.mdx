---
concepts:
- elt-vs-etl
- schema-on-read
- medallion-architecture
- idempotent-loads
- incremental-models
- storage-compute-separation
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://www.getdbt.com/blog/elt-best-practices-snowflake
- https://docs.getdbt.com/docs/build/incremental-models
- https://docs.getdbt.com/docs/build/incremental-microbatch
- https://coalesce.io/data-insights/snowflake-cost-optimization-a-practical-engineering-guide-to-reducing-snowflake-costs/
spiral: []
status: ready
summary: ETL трансформирует до загрузки в отдельном движке; ELT грузит сырьё в склад
  и трансформирует в SQL. Дешёвый колоночный compute и раздельные storage/compute
  перевернули дефолт — и перенесли стоимость на счёт склада.
title: 'ELT против ETL: где выполняется Transform и почему индустрия перевернулась'
track: data-engineering
unit: 02-elt-vs-etl
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
В понедельник тебе пишет финансовый аналитик: «Почему наш счёт за Snowflake на прошлой неделе подскочил на 40%?» Ничего не выкатывали — кроме изменения в пайплайне. Кто-то выставил dbt-модели `full-refresh` по умолчанию и поставил их по расписанию ежечасно. Каждый запуск пересканирует всю фактовую таблицу на 2 ТБ и пересобирает её с нуля. Трансформация корректна. Баг — это счёт. Это та самая «налоговая ставка ELT», о которой никто не предупредил: когда T делает склад, склад тарифицирует каждый байт, к которому ты прикоснулся.
</Hook>

<Crux>ETL трансформирует до загрузки в отдельном движке; ELT грузит сырьё и трансформирует в складе. Где идёт T — решает цену, реплей и точность.</Crux>

<Explanation>

## Развилка пайплайна: где живёт T

Оба паттерна перемещают данные из источников в склад. Единственная реальная разница — порядок средней буквы, и эта одна перестановка меняет всё, что идёт дальше.

**ETL** (extract, transform, load) родился, когда storage и compute были одной дорогой коробкой. Ты не мог позволить себе сгружать всё в склад, поэтому трансформировал сначала — в отдельном движке (Informatica, кластер Spark/Hadoop, Python-джоб) — чистил, джойнил, фильтровал и агрегировал, а потом грузил только отполированный результат. Склад видел только готовую таблицу.

**ELT** (extract, load, transform) переставляет два последних шага. Ты грузишь сырые исходные данные в склад первыми, нетронутыми, а потом трансформируешь их *на месте* в SQL. Трансформация больше не отдельная система — это запросы, выполняющиеся на том же движке, что хранит данные. dbt — доминирующий инструмент для «T»: версионируемые, протестированные, документированные SQL-модели вместо чёрного ящика-пайплайна.

Причина переворота индустрии архитектурная, а не модная. Snowflake и BigQuery разделяют storage и compute. Storage — это дешёвое объектное хранилище; compute — отдельный эластичный ресурс, который ты поднимаешь только когда выполняется запрос. Это полностью ломает старое ограничение — ты *можешь* позволить себе посадить сырьё, потому что хранить его почти ничего не стоит, а за compute платишь только когда реально трансформируешь.

## Почему ELT победил: сырьё реплейабельно

Самая глубокая причина, по которой ELT стал дефолтом, — не цена, а **реплейабельность** (возможность переиграть). В ETL сырые исходные данные выбрасываются после трансформации; склад держит только трансформированный вывод. Поэтому когда (не если) ты находишь баг в трансформации — неверный ключ джойна, ошибку таймзоны, валюту, которую надо было конвертировать, — ты не можешь починить историю. Сырой ввод исчез. Приходится переэкстрагировать из источника, а источник мог измениться, заблокировать тебя по rate-limit или больше не держать старые строки.

В ELT сырьё живёт в складе постоянно. Баг трансформации — это однострочный SQL-фикс плюс перезапуск по данным, которые у тебя уже есть. Никакого re-extract. Это сердце **medallion-архитектуры**: слой **bronze** из сырых, append-only загруженных данных; слой **silver** из очищенных, приведённых, дедуплицированных таблиц; слой **gold** из готовых к бизнесу агрегатов и витрин. Каждый слой пересобираем из нижнего, а bronze пересобираем из источника, только если уж совсем нужно. Контракт такой: никогда не мутируй bronze, всегда трансформируй вперёд.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Измерение</th>
        <th class="border border-border px-3 py-2 text-left">ETL (трансформ до загрузки)</th>
        <th class="border border-border px-3 py-2 text-left">ELT (трансформ в складе)</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Точность сырья</td>
        <td class="border border-border px-3 py-2">Потеряна — хранится только трансформированный вывод</td>
        <td class="border border-border px-3 py-2">Сохранена — слой bronze реплейабелен</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Починка бага трансформации</td>
        <td class="border border-border px-3 py-2">Re-extract из источника (может исчезнуть)</td>
        <td class="border border-border px-3 py-2">Правишь SQL, перезапуск по имеющемуся сырью</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Где тарифицируется compute</td>
        <td class="border border-border px-3 py-2">Отдельный движок (твой кластер)</td>
        <td class="border border-border px-3 py-2">Склад — каждый трансформ в счёте</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Контроль PII / цены до загрузки</td>
        <td class="border border-border px-3 py-2">Сильный — снять/замаскировать до посадки</td>
        <td class="border border-border px-3 py-2">Слабый — сырьё (вкл. PII) садится первым</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Дисциплина схемы</td>
        <td class="border border-border px-3 py-2">Schema-on-write (на загрузке)</td>
        <td class="border border-border px-3 py-2">Schema-on-read (ты обеспечиваешь в silver)</td>
      </tr>
    </tbody>
  </table>
</div>

<FlowDiagram
  label="Два пайплайна в сравнении. ETL извлекает, затем трансформирует в отдельном движке, затем грузит готовый результат в склад. ELT извлекает, грузит сырые данные в склад первыми, затем трансформирует их на месте внутри склада."
  hue="var(--d-data)"
  perRow={4}
  nodes={[
    { id: "etl-e", label: "Extract", sub: "из источника", col: 0, row: 0 },
    { id: "etl-t", label: "Transform", sub: "отдельный движок", col: 1, row: 0 },
    { id: "etl-l", label: "Load", sub: "готовый результат", col: 2, row: 0 },
    { id: "etl-w", label: "Склад", sub: "готовая таблица", col: 3, row: 0 },
    { id: "elt-e", label: "Extract", sub: "из источника", col: 0, row: 1 },
    { id: "elt-l", label: "Load", sub: "сырьё, нетронутое", col: 1, row: 1 },
    { id: "elt-w", label: "Склад", sub: "сырой bronze", col: 2, row: 1 },
    { id: "elt-t", label: "Transform", sub: "SQL в складе", col: 3, row: 1 },
  ]}
  edges={[
    { from: "etl-e", to: "etl-t" },
    { from: "etl-t", to: "etl-l" },
    { from: "etl-l", to: "etl-w" },
    { from: "elt-e", to: "elt-l" },
    { from: "elt-l", to: "elt-w" },
    { from: "elt-w", to: "elt-t", label: "на месте" },
  ]}
  caption="ETL (сверху) трансформирует до загрузки, поэтому склад видит только готовую таблицу. ELT (снизу) грузит сырьё первым, затем трансформирует на месте внутри склада — блок Transform смещается правее блока Load."
/>

## Стоимость переезжает на счёт склада

ELT не делает трансформацию бесплатной — он переносит стоимость с CapEx-кластера, которым ты владел, на тарифицируемую OpEx-строку в счёте склада, и эта строка зверски чувствительна к тому, как ты пишешь SQL. Hook — каноничный провал: модель, материализованная как полная пересборка таблицы, поставленная часто по расписанию, сканирующая всё на каждом запуске. В Snowflake ты платишь за warehouse-секунду; в BigQuery — за просканированные байты. `SELECT *` по непартиционированной таблице на 2 ТБ, ежечасно, — это уже сам по себе четырёхзначный пункт месячного счёта.

Фикс — **инкрементальные модели**. Вместо пересборки всей таблицы ты обрабатываешь только новые или изменённые строки с прошлого запуска. В dbt это макрос `is_incremental()`, оборачивающий фильтр:

```sql
{% if is_incremental() %}
  where event_time >= (select max(event_time) from {{ this }})
{% endif %}
```

На первом запуске таблица строится полностью; на каждом следующем трогаются только строки новее текущего максимума. Ночной джоб, сканировавший 2 ТБ, теперь сканирует дельту за день — часто несколько ГБ — срезая и время выполнения, и тарифицируемые байты на один-два порядка. Дисциплина сеньора: инкрементально по умолчанию, full refresh только когда меняется логика (`dbt run --full-refresh`), и отдельный warehouse на команду, чтобы тяжёлый трансформ никогда не голодил BI-дашборды на общем compute. Команды, забывающие auto-suspend на простаивающих warehouse, сливают кредиты за compute, который ничего не делает.

<Inset kind="why" lang="ru">
«Schema-on-read» звучит как свобода, но это отложенный счёт. Schema-on-write в ETL отвергает кривую строку на загрузке — ты узнаёшь сразу. ELT с радостью садит что угодно в bronze; нарушение контракта всплывает позже, в silver, часто как тихий NULL или неверный джойн. ELT не убирает работу со схемой — он сдвигает её ниже по потоку и делает *твоей* задачей обеспечить её в тестах, а не задачей загрузчика.
</Inset>

## Идемпотентность: повтор, который удваивает твои данные

Провал, от которого люди просыпаются ночью, — дублирование данных. EL-инструменты вроде Fivetran и Airbyte, и твои собственные загрузчики, повторяют при временном сбое — это корректное поведение. Но если загрузка *не идемпотентна*, повтор, перезапускающий частично-успешный батч, вставляет те же строки дважды. Теперь твой итог по выручке завышен, и дашборду никто не верит.

Лекарство — сделать загрузки и инкрементальные трансформации **идемпотентными**: запустить дважды даёт тот же результат, что запустить один раз. В инкрементальных моделях dbt это стратегия `merge` с `unique_key`:

```sql
{{ config(materialized='incremental', incremental_strategy='merge', unique_key='event_id') }}
```

На каждой строке `merge` обновляет, если `unique_key` уже существует, и вставляет, если нет — это upsert. Повтор того же батча обновляет строки на месте вместо дублирования. Ловушка: инкрементальная модель **без** `unique_key` и **без** фильтра `is_incremental()` тихо *аппендит* весь свой вывод на каждом запуске, поэтому она и дублирует, и пересканирует всё — худшее из двух миров. Microbatch-модели идут дальше, трактуя каждый временно́й батч как атомарную, независимо заменяемую единицу, поэтому упавший батч переигрывается чисто, не трогая соседей.

<TradeoffMatrix
  id="01-elt-vs-etl-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Регулируемый финтех загружает платёжные события с номерами карт (PAN) и PII. Комплаенс запрещает сырым данным держателя карты вообще оказываться в аналитическом складе. Выбери паттерн."
  options={[
    { name: "ETL: маскировать/токенизировать PAN и убрать запрещённую PII в шаге до загрузки, потом грузить только комплаентный результат", summary: "Трансформ до загрузки.", correct: true, justification: "Когда жёсткая линия комплаенса говорит, что сырая PII никогда не должна оказаться в складе, действовать надо до загрузки — ровно это и даёт трансформ-до-загрузки в ETL. Ты снимаешь, маскируешь или токенизируешь в контролируемом движке, и склад хранит только комплаентные данные, сжимая аудит-поверхность. Ты теряешь реплейабельность сырья по этим полям — это принятая цена регуляции." },
    { name: "ELT: посадить сырые платёжные события в bronze, потом маскировать в silver-трансформе", summary: "Маскировать в складе.", justification: "Это современный дефолт для большинства данных, но здесь он проигрывает по явному ограничению: сырые PAN физически садятся в bronze до любой маскировки, поэтому данные держателя карты лежат в складе — ровно то, что комплаенс запрещает. Schema-on-read не может задним числом «разсадить» данные, уже коснувшиеся диска." },
    { name: "ELT с column-level masking-политиками, применяемыми на момент запроса", summary: "Маскировать на чтении.", justification: "Динамическая маскировка прячет PII от читателей, но сырые значения всё равно физически хранятся в bronze — маскировка это контроль на момент view, а не контроль хранения. Если правило «никогда не лежит в складе», то хранимое-но-скрытое всё равно его нарушает." },
    { name: "Обойти склад для PII-полей и отправить CSV комплаенсу на почту", summary: "Боковой канал.", justification: "Внеполосный CSV — это экспозиция PII хуже, чем склад, без контроля доступа, без аудит-следа и без жизненного цикла. Это меняет управляемую систему на неуправляемую." },
  ]}
/>

<Quiz
  id="01-elt-vs-etl-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Ты находишь баг с таймзоной в трансформации, которая работала полгода. При ELT с medallion-архитектурой какой быстрый фикс?"
  choices={[
    { label: "Починить SQL в silver-модели и перезапустить её по сырым bronze-данным, которые уже есть — без re-extract", correct: true },
    { label: "Переэкстрагировать полгода истории из source API и пересобрать всё", misconception: "Это провальный сценарий ETL — re-extract нужен только когда сырьё исчезло. ELT держит сырьё в bronze именно чтобы можно было переиграть трансформацию без обращения к источнику." },
    { label: "Пропатчить выходную таблицу склада на месте через UPDATE", misconception: "Ручная мутация gold ломает контракт пересобираемости и невоспроизводима — следующий полный запуск тихо откатит твой патч. Чини трансформацию, а не вывод." },
    { label: "Ничего нельзя сделать; трансформированная история неизменна", misconception: "Это верно только в ETL, где сырьё выбросили. Весь смысл ELT в том, что bronze сохранён, поэтому история реплейабельна." },
  ]}
/>

<Quiz
  id="01-elt-vs-etl-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Инкрементальная dbt-модель настроена без unique_key и без фильтра is_incremental(). Что происходит на каждом запуске по расписанию?"
  choices={[
    { label: "Она аппендит весь свой вывод снова — дублируя строки и пересканируя всё, худшее из двух миров", correct: true },
    { label: "Она безопасно делает upsert по первичному ключу", misconception: "Upsert требует unique_key со стратегией merge. Без него dbt не может сопоставить строки, поэтому аппендит." },
    { label: "Она автоматически обрабатывает только новые строки", misconception: "Инкрементальная фильтрация происходит только внутри блока is_incremental(). Без него у dbt нет WHERE, ограничивающего скан." },
    { label: "dbt отказывается запускаться и падает с ошибкой", misconception: "dbt с радостью запускается — в этом и опасность. Он тихо аппендит, поэтому баг проявляется как дублирующиеся данные и растущий счёт, а не как ошибка." },
  ]}
/>

<DragOrder
  id="01-elt-vs-etl-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь стадии современного ELT-пайплайна от источника до дашборда:"
  items={[
    { id: "a", label: "Extract + Load: EL-инструмент (Fivetran/Airbyte) копирует сырые исходные данные в склад" },
    { id: "b", label: "Bronze: сырые, append-only посаженные данные, никогда не мутируются" },
    { id: "c", label: "Silver: очищенные, дедуплицированные, приведённые таблицы (dbt-модели, схема обеспечивается здесь)" },
    { id: "d", label: "Gold: готовые к бизнесу агрегаты и витрины" },
    { id: "e", label: "BI / дашборды читают из gold" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>ELT садит сырьё первым ради реплейабельности, но T идёт на тарифицируемом складе — иди инкрементально и идемпотентно (merge по unique_key) или получишь взрыв трат и дубли. ETL побеждает, когда PII нельзя садить.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-elt-vs-etl-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, почему индустрия перешла от ETL к ELT и что ты отдал в этом обмене.", a: "Переход стал возможен благодаря облачным складам (Snowflake, BigQuery), разделившим storage и compute. Storage стал дешёвым объектным хранилищем, поэтому посадить всё сырьё стоит почти ничего; compute стал эластичным, поэтому платишь только когда трансформ реально выполняется. Это убрало старое ограничение, заставлявшее трансформировать до загрузки. Решающий выигрыш — реплейабельность: ELT держит сырьё постоянно в слое bronze, поэтому когда находишь баг трансформации — неверный джойн, ошибку таймзоны — ты правишь SQL и перезапускаешь по данным, которые уже есть, вместо re-extract из источника, который мог измениться или исчезнуть (провальный сценарий ETL). Что отдаёшь: стоимость трансформа теперь падает на тарифицируемый счёт склада вместо кластера, которым ты владел; сырая PII физически садится до того, как ты её замаскируешь; а обеспечение схемы переезжает с момента загрузки (schema-on-write) на твои собственные тесты ниже по потоку (schema-on-read). Для большинства данных обмен стоит того; для жёстких линий комплаенса по PII трансформ-до-загрузки в ETL всё ещё побеждает." },
    { q: "Повтор продублировал строки в фактовой таблице и завысил выручку. Какой дизайн это бы предотвратил и почему?", a: "Корень проблемы — неидемпотентная загрузка: перезапуск частично-успешного батча вставил те же строки снова. EL-инструменты и загрузчики повторяют при временном сбое по дизайну, поэтому любая загрузка, не идемпотентная, рано или поздно удвоит свои данные. Фикс — сделать загрузки и инкрементальные трансформы идемпотентными: запуск дважды даёт тот же результат, что запуск один раз. В dbt это инкрементальная модель со стратегией incremental_strategy='merge' и unique_key (например, event_id): merge обновляет строку, если ключ уже есть, и вставляет, только если нет, поэтому повтор делает upsert на месте вместо аппенда. Классическая ловушка — инкрементальная модель без unique_key и без фильтра is_incremental() — dbt тихо аппендит весь вывод на каждом запуске, дублируя строки и пересканируя всё. Microbatch-модели усиливают это дальше, делая каждый временно́й батч атомарной, независимо заменяемой единицей, поэтому упавший батч переигрывается, не тревожа соседей." },
  ]}
/>

<Recap lang="ru">
ETL и ELT отличаются только тем, где выполняется Transform, и эта одна перестановка решает цену, реплейабельность и точность данных. ETL трансформирует в отдельном движке до загрузки и держит только отполированный вывод — сильно для снятия PII и контроля цены до посадки данных, но сырьё выброшено, поэтому баг трансформации означает re-extract из источника, которого может уже не быть. ELT грузит сырьё в склад первым и трансформирует в SQL (dbt), что облачное разделение storage и compute сделало дешёвым. Его решающее преимущество — реплейабельность через medallion-паттерн: неизменный сырой bronze, очищенный silver, готовый к бизнесу gold, каждый пересобираем из нижнего. Цена в том, что compute трансформа теперь тарифицируется на счёте склада — поэтому идёшь инкрементально (обрабатываешь только дельту, а не полную пересборку) и идемпотентно (merge по unique_key) или получаешь взрыв трат и дублирующиеся строки на повторе. Выбирай ELT по умолчанию; тянись к ETL, когда жёсткое правило говорит, что сырая PII не должна касаться склада.
</Recap>
