---
concepts:
- incremental-models
- idempotent-loads
- merge-upsert
- backfill
- warehouse-cost
- microbatch
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://docs.getdbt.com/docs/build/incremental-models
- https://docs.getdbt.com/docs/build/incremental-microbatch
status: ready
summary: "Читай реальные конфиги dbt-моделей и сигнатуру счёта Snowflake, предсказывай поведение пайплайна — идемпотентность, инкрементальную загрузку, backfill — и выбирай фикс наибольшего рычага."
title: 'ELT vs ETL: чтение моделей и конфигов'
track: data-engineering
unit: 02-elt-vs-etl
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги пайплайна живут в конфиге модели и в SQL, а не на диаграмме. Прочитай каждую dbt-модель и трейс, предскажи, что она делает на втором и третьем запуске по расписанию, и выбери фикс, к которому сеньор тянется первым.
</Hook>

<Goal lang="ru">
Отработай цикл, который ты гоняешь на каждом ревью пайплайна: прочитай конфиг материализации и инкрементальный фильтр, предскажи, дублирует ли повтор, пересканирует или чисто делает upsert, и тянись к фиксу наибольшего рычага.
</Goal>

## Сниппет 1 — тихий аппенд

```sql
-- models/marts/fct_events.sql
{{ config(materialized='incremental') }}

select
  event_id,
  user_id,
  event_time,
  amount
from {{ ref('stg_events') }}
```

<Quiz
  id="02-elt-vs-etl-code-1"
  lessonSlug="02-elt-vs-etl"
  lang="ru"
  question="Модель материализована как 'incremental', но без unique_key и без блока is_incremental(). Что происходит на каждом запуске по расписанию после первого и каков фикс?"
  choices={[
    { label: "Она снова аппендит весь свой вывод — дублируя каждую строку и пересканируя весь stg_events, худшее из двух миров. Фикс: добавить фильтр is_incremental() по event_time И unique_key='event_id' со стратегией incremental_strategy='merge'.", correct: true },
    { label: "Она безопасно делает upsert по первичному ключу.", misconception: "Upsert требует unique_key со стратегией merge. Без обоих у dbt нет ключа для сопоставления и нет фильтра для ограничения скана, поэтому она просто аппендит весь результат." },
    { label: "Она автоматически обрабатывает только новые строки, ведь она incremental.", misconception: "Сама по себе инкрементальная материализация ничего не делает — фильтр, ограничивающий строки, должен быть внутри блока is_incremental(). Без него нет WHERE, поэтому dbt снова выбирает всё." },
    { label: "dbt отказывается запускаться и падает с ошибкой, защищая тебя.", misconception: "dbt спокойно запускается — в этом и опасность. Он тихо аппендит, поэтому баг ты обнаруживаешь как удвоенную выручку и растущий счёт, а не как ошибку." },
  ]}
/>

## Сниппет 2 — идемпотентный merge

```sql
-- models/marts/fct_orders.sql
{{ config(
    materialized='incremental',
    incremental_strategy='merge',
    unique_key='order_id'
) }}

select order_id, customer_id, status, updated_at, total
from {{ ref('stg_orders') }}
{% if is_incremental() %}
  where updated_at > (select max(updated_at) from {{ this }})
{% endif %}
```

<Quiz
  id="02-elt-vs-etl-code-2"
  lessonSlug="02-elt-vs-etl"
  lang="ru"
  question="Статус заказа меняется с 'pending' на 'shipped' (тот же order_id, более новый updated_at). Затем EL-инструмент падает посреди запуска, и оркестратор повторяет весь батч. Какое финальное состояние fct_orders?"
  choices={[
    { label: "Ровно одна строка на этот order_id со статусом 'shipped'. Merge делает upsert по order_id, поэтому изменённая строка обновляется на месте, а повтор повторно применяет тот же merge к тем же строкам — идемпотентно, без дубля.", correct: true },
    { label: "Две строки на этот order_id — одна 'pending', одна 'shipped', — потому что повтор вставил заново.", misconception: "Так сделал бы append. Merge сопоставляет по unique_key='order_id', поэтому обновляет существующую строку, а не вставляет вторую. Повтор безопасен." },
    { label: "Повтор задвоит заказ в любом агрегате SUM(total) ниже по потоку.", misconception: "Дубль-строка не создаётся, поэтому SUM ниже видит заказ один раз. Merge плюс unique_key — ровно то, что делает загрузку идемпотентной, а итоги корректными." },
    { label: "Строка сохраняет старый статус 'pending', потому что merge только вставляет.", misconception: "Merge обновляет, когда ключ есть, и вставляет, когда нет, — это upsert. Строка с более новым updated_at совпадает по order_id и перезаписывает статус на 'shipped'." },
  ]}
/>

## Сниппет 3 — счёт за full-refresh

```text
# История запросов Snowflake (одна модель, расписание ежечасно)
fct_pageviews   MEDIUM_WH   bytes_scanned=2.1 TB   elapsed=11m   24 runs/day
```

```sql
{{ config(materialized='table') }}   -- не incremental
select * from {{ ref('stg_pageviews') }}   -- 2 ТБ, непартиционированная, ежечасно
```

<Quiz
  id="02-elt-vs-etl-code-3"
  lessonSlug="02-elt-vs-etl"
  lang="ru"
  question="Эта одна модель сканирует 2.1 ТБ двадцать четыре раза в день. Каков корневой драйвер стоимости и каков фикс наибольшего рычага — не самый соблазнительный?"
  choices={[
    { label: "Она материализована как полная пересборка таблицы (SELECT * без фильтра), поэтому каждый ежечасный запуск пересканирует все 2 ТБ. Фикс — сделать её инкрементальной по event_time, чтобы каждый запуск сканировал только дельту за час, срезая просканированные байты на один-два порядка.", correct: true },
    { label: "Warehouse слишком мал; перейди на LARGE_WH, чтобы запуски завершались быстрее.", misconception: "Бо́льший warehouse завершит тот же скан 2 ТБ быстрее, но тарифицирует больше кредитов в секунду — суммарная стоимость почти не меняется. Рычаг — сканировать меньше данных, а не те же данные быстрее." },
    { label: "Запускай раз в день вместо ежечасно, чтобы из 24 сканов сделать 1.", misconception: "Это срежет частоту, но каждый запуск всё равно пересканирует 2 ТБ, и ты жертвуешь свежестью. Инкрементальность держит ежечасную свежесть, сканируя только дельту, — строго лучше, чем душить расписание." },
    { label: "Добавь больше партиций, чтобы таблица читалась быстрее.", misconception: "Партиционирование помогает, только если запрос фильтрует по ключу партиции. SELECT * без WHERE читает каждую партицию всё равно. Сначала нужен инкрементальный фильтр, чтобы партиционирование заработало." },
  ]}
/>

## Сниппет 4 — backfill

```sql
-- Нужен backfill на 90 дней после фикса бага трансформации.
-- Модель использует microbatch:
{{ config(
    materialized='incremental',
    incremental_strategy='microbatch',
    event_time='event_time',
    batch_size='day',
    begin='2024-01-01'
) }}
```

```bash
dbt run --select fct_events --event-time-start "2024-01-01" --event-time-end "2024-04-01"
```

<Quiz
  id="02-elt-vs-etl-code-4"
  lessonSlug="02-elt-vs-etl"
  lang="ru"
  question="Почему microbatch-модель — правильный инструмент для этого 90-дневного backfill по сравнению с одним full-refresh по тому же диапазону?"
  choices={[
    { label: "Microbatch разбивает диапазон на независимые дневные батчи, каждый атомарно заменяет свой день. Упавший день переигрывается один, не трогая соседей, батчи могут идти параллельно, а повтор того же окна идемпотентен — поэтому backfill перезапускаем и ограничен на батч.", correct: true },
    { label: "Microbatch — просто псевдоним full-refresh с диапазоном дат; поведение идентично.", misconception: "Они различаются фундаментально: full-refresh пересобирает всю таблицу одной транзакцией, поэтому падение на 80-м дне теряет всю предыдущую работу. Microbatch делает каждый день независимой заменяемой единицей, которую можно перезапустить изолированно." },
    { label: "Microbatch вообще обходит warehouse, поэтому backfill бесплатен.", misconception: "Microbatch всё равно гоняет SQL на тарифицируемом warehouse — он не обходит стоимость compute. Его ценность в перезапускаемости и ограниченном радиусе поражения на батч, а не в бесплатном compute." },
    { label: "Full-refresh был бы безопаснее, потому что это одна атомарная транзакция.", misconception: "Одна гигантская транзакция на 90 дней — противоположность безопасности на масштабе: она долгая, может ловить таймауты, а любое падение отбрасывает весь прогресс. Атомарные батчи по дню локализуют падение и дают возобновиться с упавшего дня." },
  ]}
/>

<Recap lang="ru">
Каждое ревью пайплайна читает конфиг и фильтр: инкрементальная модель без unique_key и без is_incremental() тихо аппендит и пересканирует всё; merge по unique_key делает повтор идемпотентным (upsert, не дубль); полный SELECT * по таблице, сканируемый ежечасно, — это баг стоимости, чинимый инкрементальностью, а не бо́льшим warehouse; а microbatch превращает долгий backfill в независимые, перезапускаемые, идемпотентные дневные единицы. Сначала читай материализацию, потом фильтр, потом решай.
</Recap>
