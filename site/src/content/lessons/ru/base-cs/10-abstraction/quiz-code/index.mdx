---
concepts:
- interface-vs-implementation
- encapsulation
- method-bundle
- module-export
- leaky-abstraction
estMin: 14
lang: ru
level: middle
order: 9
prereqs: []
slug: quiz-code
sources:
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Classes
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Modules
- https://www.joelonsoftware.com/2002/11/11/the-law-of-leaky-abstractions/
status: ready
summary: "Читай небольшие сниппеты — объект-счётчик, exports модуля, пробой через устаревшее состояние и протечку stack overflow — и выбирай, что abstraction реально открывает, прячет или не может спрятать."
title: 'Абстракция: чтение кода'
track: base-cs
unit: 10-abstraction
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Abstraction оценивают по коду, а не по определению. Читай каждый сниппет, реши, где кончается interface и начинается скрытая implementation, и замечай момент, когда граница держит — или ломается.
</Hook>

<Goal lang="ru">
Потренируй чтение, которое работающий программист запускает постоянно: найди interface, от которого зависит вызывающий, найди implementation за ним и определи, держит ли сокрытие под кодом перед тобой.
</Goal>

## Сниппет 1 — связка-счётчик

```ts
const counter = {
  count: 0,                       // data field — состояние
  increment() {                   // method field — операция
    this.count = this.count + 1;
  },
  value(): number {               // method field — ещё операция
    return this.count;
  },
};

counter.increment();
counter.increment();
let n = counter.value();          // n становится 2
```

<Quiz
  id="10-abstraction-code-1"
  lessonSlug="10-abstraction"
  lang="ru"
  question="Какие строки — единственные, что называют поле данных, и что это говорит об interface связки?"
  choices={[
    { label: "Только строки внутри increment() и value() называют count через this; вызывающий ниже использует лишь методы, поэтому interface — два метода, а count — скрытая implementation.", correct: true },
    { label: "Строка вызывающего counter.value() называет поле count напрямую.", misconception: "counter.value() вызывает метод — он никогда не пишет count. Метод читает count внутри через this; вызывающий видит только метод, а это и есть смысл связки." },
    { label: "Каждая строка называет count, ведь весь объект про count.", misconception: "Объект про count, но трогают его через this только тела методов. Три строки вызывающего называют только методы — это разделение и даёт encapsulation." },
    { label: "Ни одна строка не называет count, потому что поля данных нельзя называть в коде.", misconception: "Тела методов называют его как this.count. Поля данных называемы внутри собственных методов объекта; encapsulation прячет именно называние их снаружи." },
  ]}
/>

## Сниппет 2 — модуль

```ts
// module: text-format
const TABLE = { /* ... */ };               // таблица поиска
function pad(s: string): string { /* ... */ }
function trim(s: string): string { /* ... */ }
export function format(s: string): string {
  return pad(trim(s));                     // использует оба хелпера
}
```

```ts
// другой модуль, импортирующий тот, что выше
import { format } from "./text-format";
format("  hi  ");        // ок
pad("x");                // ??? — pad никогда не экспортировали
```

<Quiz
  id="10-abstraction-code-2"
  lessonSlug="10-abstraction"
  lang="ru"
  question="Импортирующий модуль вызывает pad('x') напрямую. Что происходит и каков public interface модуля?"
  choices={[
    { label: "pad('x') падает — pad приватен, никогда не экспортировался, поэтому невидим через границу. Public interface — ровно одно имя: format. TABLE, pad и trim — скрытая implementation.", correct: true },
    { label: "pad('x') работает, потому что pad определён в том же файле, что и экспортированный format.", misconception: "Нахождение в одном файле с export не экспортирует имя. Через границу проходят только имена с пометкой export; у pad её нет, поэтому импортирующий модуль его не видит." },
    { label: "pad('x') работает, потому что format вызывает pad, косвенно его реэкспортируя.", misconception: "Вызов приватного хелпера внутри не экспортирует его. То, что format использует pad, — деталь implementation; pad остаётся приватным и недостижимым снаружи." },
    { label: "Весь модуль не загружается, потому что pad вызван снаружи.", misconception: "Модуль грузится нормально; ошибка — только вызов pad('x'). Граница блокирует эту одну ссылку, а не ломает модуль, легально экспортировавший format." },
  ]}
/>

## Сниппет 3 — пробой

```ts
// module: bank
let balance = 100;                  // приватно — без export
export function deposit(n: number) { balance = balance + n; }
export function getBalance() { return balance; }
```

```ts
// вызывающий — что он может и не может
import { deposit, getBalance } from "./bank";
deposit(50);                        // ок — через interface
balance = -999;                     // ??? — мимо interface
```

<Quiz
  id="10-abstraction-code-3"
  lessonSlug="10-abstraction"
  lang="ru"
  question="Вызывающий присваивает balance = -999, чтобы сделать баланс отрицательным в обход deposit. Почему дизайн модуля не даёт этому стать реальным риском?"
  choices={[
    { label: "balance приватен — не экспортирован — поэтому balance у вызывающего ссылается на его собственное необъявленное имя, а не на модульное. Модульный balance достижим только через deposit/getBalance, поэтому инвариант balance >= 0 остаётся обеспечиваемым внутри модуля.", correct: true },
    { label: "Это реальный риск — присваивание balance = -999 дотягивается до модуля и портит его состояние.", misconception: "Вызывающий не может дотянуться до модульного balance: его не экспортировали. Присваивание трогает отдельное имя в вызывающем, а не приватное поле модуля, так что состояние модуля в безопасности." },
    { label: "Оно работает на модульном balance, но только потому, что deposit тоже экспортирован.", misconception: "Экспорт deposit экспортирует функцию, а не переменную balance. Приватный balance остаётся приватным независимо от того, какие функции экспортированы рядом." },
    { label: "Модуль небезопасен, потому что любое приватное имя можно перезаписать снаружи.", misconception: "Это ровно то, что граница предотвращает. Приватное имя нельзя достать или перезаписать снаружи; именно это принудительное сокрытие позволяет модулю гарантировать инвариант." },
  ]}
/>

## Сниппет 4 — протечка

```ts
function depth(n: number): number {
  return depth(n + 1);   // не останавливается — нет базового случая
}

depth(0);   // RangeError: Maximum call stack size exceeded
```

<Quiz
  id="10-abstraction-code-4"
  lessonSlug="10-abstraction"
  lang="ru"
  question="Abstraction вызова функции обещает «вызови, она выполнится, вернёт». Этот код падает, называя стек вызовов. Как юнит это назвал и в чём урок?"
  choices={[
    { label: "Протекающая abstraction: конечный стек вызовов — деталь нижнего слоя — проступил, когда безудержная рекурсия его заполнила. Abstraction вызова реально полезна и всё равно протекает на пределе, поэтому ты учил стек под ней.", correct: true },
    { label: "Abstraction вызова функции сломана; рекурсии следует избегать вовсе.", misconception: "Abstraction в порядке — баг в отсутствии базового случая. Суть в том, что даже хорошая abstraction протекает нижним слоем (стеком), когда тот упирается в реальный предел, а не в том, что рекурсия запрещена." },
    { label: "Это синтаксическая ошибка в depth, не связанная с протечкой abstraction.", misconception: "Синтаксис валиден; код работает, пока стек не переполнится. Ошибка — рантайм-предел скрытого слоя, проступивший наружу (определение протечки), а не проблема разбора." },
    { label: "Добавление памяти сделало бы эту abstraction идеально непротекающей.", misconception: "Больше памяти лишь отсрочит переполнение; неограниченная рекурсия исчерпает любой конечный стек. Никакой объём памяти не делает abstraction над реальной машиной непротекающей." },
  ]}
/>

<Recap lang="ru">
Каждый сниппет — одно и то же чтение на разном масштабе. В счётчике interface — методы, а count скрыт за this. В модуле через границу проходит только экспортированный format, а pad, trim и TABLE остаются приватными. В банке приватный balance нельзя достать снаружи, поэтому модуль может гарантировать инвариант — граница превращает надежду в правило. А в безудержной рекурсии конечный стек вызовов протекает сквозь abstraction вызова в момент, когда нижний слой упирается в предел. Найди interface, найди скрытую implementation и проверь, держит ли сокрытие.
</Recap>
