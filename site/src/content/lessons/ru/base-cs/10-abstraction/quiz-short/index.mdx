---
concepts:
- interface-vs-implementation
- encapsulation
- module-boundary
- namespacing
- leaky-abstraction
- layer-stack
estMin: 13
lang: ru
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.joelonsoftware.com/2002/11/11/the-law-of-leaky-abstractions/
- https://developer.mozilla.org/en-US/docs/Glossary/Abstraction
- https://sicp.sourceacademy.org/chapters/2.1.2.html
status: ready
summary: "Вопросы на свободное припоминание по всему юниту. Сначала ответь своими словами, затем открой модельный ответ и сравни."
title: 'Абстракция: тест на припоминание'
track: base-cs
unit: 10-abstraction
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый вопрос проговори или запиши полный ответ по памяти, прежде чем открыть модельный — усилие припоминания и закрепляет юнит.
</Hook>

<Goal lang="ru">
Восстанови по памяти стержень юнита — interface против implementation, что дают связка-метод и encapsulation, что обеспечивает граница module, зачем нужен namespacing и почему любая нетривиальная abstraction протекает — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:load
  id="10-abstraction-short-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое abstraction и каковы её две стороны?", a: "Abstraction — это сокрытие детали за именем, чтобы пользоваться вещью, не зная, как она устроена внутри. У неё две стороны. Interface — видимая поверхность, от которой зависит пользователь: для функции это имя, входы и возвращаемое значение; это контракт, обещание. Implementation — скрытая машинерия, исполняющая обещание: тело функции и инструкции, которые оно запускает. Смысл разделения в том, что implementation можно свободно переписывать, пока interface держит обещание, поэтому вызывающие не меняются. Ты пользуешься abstraction с ранних юнитов: имя переменной прячет адрес в памяти, вызов функции прячет тело." },
    { q: "Что такое метод и что даёт encapsulation?", a: "Метод — это функция, хранимая как поле объекта, работающая над данными того же объекта и достающая их через this. У объекта тогда есть поля данных с состоянием и поля-методы с операциями над ним — вместе это связка: один именованный блок с данными и операциями. Interface связки — набор методов, которые можно вызвать; раскладка полей — скрытая implementation. Связывание данных с операциями при сокрытии внутренних данных от внешнего кода — это encapsulation. Выигрыш: ровно одно место — методы — трогает данные, поэтому можно переименовать или перестроить поля, и каждый вызывающий останется без правок, ведь interface сохранился." },
    { q: "Что такое module и что на самом деле обеспечивает его граница?", a: "Module — это блок кода, группирующий связанные определения за границей. По умолчанию каждое имя внутри приватно — видно только коду в том же module, так же как переменная видна в своей функции. Module помечает выбранные имена как exports; набор экспортированных имён — его public interface, всё остальное — скрытая implementation. Граница принудительна на уровне языка и делает две конкретные вещи: внешний код не может прочитать или перезаписать приватное состояние module, и изменение приватного имени не может сломать код снаружи. Поэтому автор свободно переписывает все внутренности — граница превращает договорённость «не трогай мои внутренности» в гарантию." },
    { q: "Что такое namespacing и зачем он нужен большой программе?", a: "Namespacing — это группировка имён под module так, чтобы одинаковые короткие имена не сталкивались. Два module могут определить функцию format — одну для дат, другую для валюты — и они не сталкиваются, потому что к каждой обращаются через свою ручку module: dateModule.format и currencyModule.format. Имя module впереди даёт каждому имени отдельный дом. Большой программе это нужно, потому что в едином плоском пространстве каждое имя конкурировало бы с каждым; второй format перезаписал бы первый, и хорошие имена кончились бы. То, что каждый module — свежее пространство, и делает большие программы записываемыми." },
    { q: "Что такое протекающая abstraction и почему любая нетривиальная abstraction протекает?", a: "Abstraction протекает, когда не может полностью спрятать нижний слой — когда деталь снизу проступает и тебя вынуждают думать об уровне, который обещали игнорировать. Протечки чаще всего проступают, когда нижний слой падает или тормозит: пока всё работает, скрытый слой скрыт, но как только он ломается или вязнет — проступает. Закон протекающих абстракций Джоэла Спольски гласит: любая нетривиальная abstraction в какой-то мере протекает. Они протекают, потому что нижний слой всё ещё делает реальную работу, способную пойти не так способами, которых interface не описывал — время, пределы памяти, конечный стек, аппаратные сбои. Interface описывает результат; он не может описать прочь физику, и этот зазор — источник любой протечки." },
    { q: "Если abstraction протекают, почему практический урок — НЕ перестать их использовать и НЕ перестать учить нижние слои?", a: "Обе крайности неверны. Abstraction не опциональна — без неё нельзя написать программу длиннее пары строк, потому что человек держит в уме лишь несколько вещей, а реальная программа — миллиарды переключений гейтов в секунду. И ни одна abstraction над реальной машиной не является непротекающей. Зрелый взгляд держит оба факта сразу: используй abstraction ради рассуждения, что она даёт, и всё равно понимай нижний слой на тот день, когда она протечёт. Abstraction экономит время работы, но не время обучения. Ровно поэтому трек прошёл весь стек слоёв — гейты, машинный код, язык, затем конструкции — обучая каждому слою под теми, в которых будешь работать чаще всего." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, у тебя есть стержень юнита: abstraction — это фиксированный interface над скрытой, заменяемой implementation; связка-метод плюс encapsulation применяют это к данным и операциям; module обеспечивает границу, так что приватное состояние недосягаемо, и даёт именам дом через namespacing; а укладка слоёв управляет сложностью ценой протечки — поэтому используешь interface и всё равно учишь слой под ним.
</Recap>
