---
concepts:
- interface-vs-implementation
- encapsulation
- module-boundary
- namespacing
- leaky-abstraction
- layer-stack
estMin: 13
lang: ru
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.joelonsoftware.com/2002/11/11/the-law-of-leaky-abstractions/
- https://developer.mozilla.org/en-US/docs/Glossary/Abstraction
- https://sicp.sourceacademy.org/chapters/2.1.2.html
status: ready
summary: "Тест с выбором на синтез по всему юниту — interface против implementation, encapsulation, границы module, namespacing и почему любая нетривиальная abstraction протекает."
title: 'Абстракция: тест с выбором ответа'
track: base-cs
unit: 10-abstraction
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый — решение, которое принимает работающий программист: не определение для заучивания, а граница между тем, что abstraction обещает, и тем, что она реально прячет.
</Hook>

<Goal lang="ru">
Убедись, что связываешь четыре идеи, к которым вёл юнит — interface против implementation, encapsulation внутри связки, границу module и namespacing, а также компромисс протекающей abstraction — и применяешь их к коду, который ещё не видел.
</Goal>

<Quiz
  id="10-abstraction-choice-1"
  lessonSlug="10-abstraction"
  lang="ru"
  question="Команда переписывает тело add(a, b), чтобы считать сумму быстрее. Имя, два входа и возвращаемая сумма остаются те же. Сколько из сотен мест вызова надо изменить и почему?"
  choices={[
    { label: "Ноль. Вызывающие зависят только от interface — имени, входов, возвращаемой суммы. Контракт сохранился, поэтому implementation можно менять под каждым вызовом без правок.", correct: true },
    { label: "Все — любое изменение тела функции заставляет обновлять её вызывающих.", misconception: "Вызывающие зависят от interface, а не от тела. Пока имя, входы и результат те же, тело можно переписать целиком, и ни один вызов не меняется ни на символ." },
    { label: "Только те вызывающие, которые читают локальные переменные add.", misconception: "Вызывающие вообще не могут читать локальные переменные функции — это часть скрытой implementation. Снаружи от них ничто не зависит, обновлять нечего." },
    { label: "Ни одного, потому что переписать тело невозможно, раз уже есть вызывающие.", misconception: "Переписывать тело — именно то, что abstraction делает безопасным. Ноль правок — из-за фиксированного interface, а не из-за запрета трогать тело." },
  ]}
/>

<Quiz
  id="10-abstraction-choice-2"
  lessonSlug="10-abstraction"
  lang="ru"
  question="Объект-счётчик связывает поле данных count с методами increment() и value(). Что на самом деле даёт здесь encapsulation?"
  choices={[
    { label: "Методы — это interface, а раскладка полей — скрытая implementation, поэтому единственное место, которое трогает count, — методы: можно переименовать count или разбить надвое, и каждый вызывающий останется без правок.", correct: true },
    { label: "Это ускоряет объект, потому что методы компилируются иначе, чем обычные функции.", misconception: "Encapsulation — про то, что открыто, а что скрыто, а не про скорость. Метод — это просто функция, хранимая как поле; связка меняет видимость и сопровождаемость, а не скорость выполнения." },
    { label: "Это позволяет вызывающим читать и писать count напрямую, минуя метод.", misconception: "Это противоположность encapsulation. Весь смысл в том, что вызывающие используют методы и никогда не называют поле данных, так что раскладка остаётся скрытой и заменяемой." },
    { label: "Это удаляет поле count из памяти, оставляя только методы.", misconception: "count всё ещё существует и держит состояние — методы читают и пишут его через this. Encapsulation прячет поле от внешнего кода, но не удаляет его." },
  ]}
/>

<Quiz
  id="10-abstraction-choice-3"
  lessonSlug="10-abstraction"
  lang="ru"
  question="Module определяет шесть имён и помечает два как exports. Внешний код пытается вызвать одно из четырёх неэкспортированных имён напрямую. Что происходит и почему границу стоило сделать принудительной?"
  choices={[
    { label: "Падает — четыре приватных имени не видны через границу. Принудительность, а не доверие к договорённости, гарантирует, что снаружи ничто не зависит от приватной детали, поэтому автор может свободно переписать все четыре.", correct: true },
    { label: "Работает, потому что любое имя, которое определяет module, можно вызвать, зная его имя.", misconception: "По умолчанию имена module приватны — видны только внутри него. Через границу проходят только два экспортированных имени; остальные четыре снаружи назвать нельзя вовсе." },
    { label: "Работает, только если приватное имя хранит функцию, а не значение.", misconception: "Приватность не зависит от того, хранит ли имя функцию или значение. Любое имя без пометки export невидимо через границу — функция оно или нет." },
    { label: "Падает, но только потому, что имена случайно совпадают с именами в вызывающем module.", misconception: "Падает потому, что имена приватны, а не из-за совпадения. Даже без единого столкновения имён неэкспортированное имя нельзя достать снаружи." },
  ]}
/>

<Quiz
  id="10-abstraction-choice-4"
  lessonSlug="10-abstraction"
  lang="ru"
  question="Большой программе нужны две функции, обе разумно названные format — одна для дат, другая для валюты. Почему помещение каждой в свой module не даёт им столкнуться?"
  choices={[
    { label: "Каждый module — собственный namespace, поэтому к ним обращаются как dateModule.format и currencyModule.format — это разные имена, потому что ручка module впереди их разводит.", correct: true },
    { label: "Язык автоматически переименовывает второй format в format2 за кулисами.", misconception: "Ничто молча не переименовывается. Имена остаются format в каждом module; они не сталкиваются, потому что к каждому обращаются через имя своего module, дающее отдельный дом." },
    { label: "Существовать может только одна из двух функций format; module заставляет удалить вторую.", misconception: "Обе могут существовать одновременно. Namespacing — ровно то, что позволяет одинаковым коротким именам сосуществовать: каждый module — свежее пространство, удалять ничего не надо." },
    { label: "Module сливает две функции в одну, обрабатывающую и даты, и валюту.", misconception: "Module держит две функции полностью раздельно; код не сливается. Граница плюс ручка module — вот что разводит два имени format." },
  ]}
/>

<Quiz
  id="10-abstraction-choice-5"
  lessonSlug="10-abstraction"
  lang="ru"
  question="Abstraction вызова функции отлично работает тысячи обычных вызовов, затем безудержная рекурсия падает с ошибкой stack overflow, называющей кадры стека. Как прочитать это через юнит?"
  choices={[
    { label: "Abstraction протекла: деталь нижнего слоя — конечный стек вызовов — проступила, когда этот слой упёрся в предел. Она была реально полезна для обычных вызовов и протекла на пределе; верно и то, и другое.", correct: true },
    { label: "Abstraction была сломана с самого начала и её вообще не стоило использовать.", misconception: "Она верно работала тысячи вызовов — это реальная польза. Закон протекающих абстракций гласит: любая нетривиальная abstraction в какой-то мере протекает; протечка не делает её бесполезной." },
    { label: "Достаточно хорошая abstraction вызова функции никогда бы так не протекла.", misconception: "Ни одна abstraction над реальной машиной не является непротекающей. Время, память и конечный стек — реальные свойства нижнего слоя, которые ни один верхний interface не спрячет полностью." },
    { label: "Падение доказывает, что вызов функции никогда по-настоящему не прятал стек.", misconception: "Он прятал стек для каждого обычного вызова — ты писал f() тысячи раз, не думая о кадрах. Он проступил только когда нижний слой упал, а это и есть протечка." },
  ]}
/>

<Quiz
  id="10-abstraction-choice-6"
  lessonSlug="10-abstraction"
  lang="ru"
  question="Раз любая нетривиальная abstraction протекает, коллега заключает: можно перестать учить слои под теми, в которых работаешь. Что не так с этим выводом?"
  choices={[
    { label: "Abstraction экономит время работы, но не время обучения. Любой слой может протечь, поэтому каждый надо понимать на тот день, когда это случится — ровно поэтому трек учил слоям под теми, что используешь чаще всего.", correct: true },
    { label: "Ничего не так — раз abstraction существует, нижний слой действительно больше никогда не важен.", misconception: "Нижний слой всё ещё работает и может протечь. Abstraction убирает деталь из внимания, пока всё работает; она не освобождает от понимания слоя на случай поломки." },
    { label: "Вывод нормален, ведь протечки так редки, что на практике их можно игнорировать.", misconception: "Протечки не экзотика — они проступают всякий раз, когда нижний слой падает или тормозит, а это в реальных системах рутина. Именно поэтому нижние слои надо учить." },
    { label: "Он неверен, потому что abstraction на самом деле замедляет программы, а слои снизу их ускоряют.", misconception: "Abstraction — про управление сложностью, тем, что держишь в голове, а не про скорость. Изъян вывода в том, что любой слой может протечь, поэтому ни один нельзя безопасно игнорировать навсегда." },
  ]}
/>

<Recap lang="ru">
Сквозная нить юнита — одна цепочка: abstraction — это фиксированный interface над скрытой, заменяемой implementation; связка применяет это к данным плюс операциям над ними и зовёт сокрытие encapsulation; module применяет это к целой группе кода с принудительной границей и namespacing, так что приватная деталь остаётся недосягаемой, а короткие имена перестают сталкиваться; а укладка этих слоёв — то, как управляют сложностью. Цена — протечка: любая нетривиальная abstraction проступает нижним слоем, когда тот падает или тормозит, поэтому interface используют ради рассуждения, что он даёт, и всё равно учат машинерию под ним.
</Recap>
