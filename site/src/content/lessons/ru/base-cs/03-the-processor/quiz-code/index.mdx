---
concepts:
- instruction-trace
- register-state
- program-counter
- machine-code-decode
- clock-cycle
estMin: 14
lang: ru
level: middle
order: 9
prereqs: []
slug: quiz-code
sources:
- https://www.nand2tetris.org/
- https://en.wikipedia.org/wiki/Instruction_cycle
- https://en.wikipedia.org/wiki/Machine_code
status: ready
summary: Трассируйте небольшие программы в игрушечном наборе инструкций, предсказывайте состояние регистров и счётчика команд, декодируйте слово машинного кода и считайте такты.
title: 'Процессор: трассировка и декодирование'
track: base-cs
unit: 03-the-processor
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Понять процессор — значит уметь прогонять его в голове. Каждый фрагмент ниже — это крошечная программа или одно слово машинного кода; прочитайте его, прошагайте цикл и предскажите состояние так, как его произвёл бы CPU.
</Hook>

<Goal lang="ru">
Отработайте ключевой навык юнита: взять инструкции и начальное состояние, вручную прошагать цикл fetch-decode-execute и считать получившиеся значения регистров, значение счётчика команд (program counter), смысл машинного кода и число тактов.

Эти фрагменты используют игрушечный набор инструкций из юнита: каждая инструкция — 2 байта, счётчик команд сдвигается на 2 после каждой инструкции, кроме JUMP, а регистры — R0 и R1.
</Goal>

## Фрагмент 1 — трассировка состояния регистров

```
; Начальное состояние: R0 = 0, R1 = 0, PC = 0
; Память: mem[200] = 9, mem[201] = 4

0: LOAD  R0, 201
2: LOAD  R1, 200
4: ADD   R0, R1     ; R0 <- R0 + R1
6: STORE 202, R0
```

<Quiz
  id="03-the-processor-code-1"
  lessonSlug="03-the-processor"
  lang="ru"
  question="После выполнения всех четырёх инструкций какое значение хранится по адресу 202?"
  choices={[
    { label: "13 — LOAD R0,201 кладёт 4 в R0; LOAD R1,200 кладёт 9 в R1; ADD даёт R0 = 4 + 9 = 13; STORE пишет R0 (13) в mem[202].", correct: true },
    { label: "9 — STORE пишет то, что лежит по адресу 200.", misconception: "STORE 202, R0 пишет значение из регистра R0, а не содержимое какого-то адреса памяти. К этому моменту R0 хранит сумму 13, а не 9." },
    { label: "5 — программа вычитает два входа.", misconception: "Единственная арифметическая инструкция здесь — ADD, которая складывает R0 и R1. Вычитания нет; 4 + 9 = 13." },
    { label: "Нельзя сказать — R0 не получил стартового значения.", misconception: "R0 загружается из mem[201] = 4 в первой инструкции, так что его начальный 0 перезаписан до ADD. Результат полностью определён: 13." },
  ]}
/>

## Фрагмент 2 — счётчик команд и переход

```
; Начальное состояние: PC = 0, R0 = 0
; Каждая инструкция — 2 байта.

0: LOAD R0, 200
2: JUMP 6
4: ADD  R0, R1      ; никогда не достигается
6: STORE 202, R0
```

<Quiz
  id="03-the-processor-code-2"
  lessonSlug="03-the-processor"
  lang="ru"
  question="Какая инструкция по адресу 4 НЕ выполнится, и каким будет значение счётчика команд сразу после выполнения JUMP по адресу 2?"
  choices={[
    { label: "ADD по адресу 4 пропущена; сразу после JUMP PC = 6. JUMP устанавливает PC в свою цель вместо сдвига на 2, поэтому следующая выборка идёт с адреса 6, а не 4.", correct: true },
    { label: "ADD по адресу 4 всё равно выполнится; после JUMP PC = 4, потому что PC всегда сдвигается на 2.", misconception: "JUMP переопределяет обычный сдвиг на +2. Он записывает свою цель (6) в PC, поэтому адрес 4 перешагивается целиком." },
    { label: "Ничего не пропущено; после JUMP PC = 8.", misconception: "PC устанавливается в цель перехода 6, а не сдвигается дальше неё. Адрес 4 пропущен, и следующая выборка — по адресу 6." },
    { label: "STORE по адресу 6 пропущена; после JUMP PC = 6.", misconception: "PC = 6 верно, но это значит, что выборка по адресу 6 (STORE) как раз выполнится. Перешагивается инструкция ADD по адресу 4." },
  ]}
/>

## Фрагмент 3 — декодирование слова машинного кода

```
; Кодировка 16-битной инструкции:
;   биты 15-12 (4 бита) = опкод
;   биты 11-8  (4 бита) = номер регистра-приёмника
;   биты 7-0   (8 бит)  = непосредственное значение (immediate)
; опкод 0001 = LOAD-IMMEDIATE (положить константу в регистр)

Слово в памяти:  0001 0011 00010010
```

<Quiz
  id="03-the-processor-code-3"
  lessonSlug="03-the-processor"
  lang="ru"
  question="Декодируя это 16-битное слово, какую инструкцию оно представляет?"
  choices={[
    { label: "LOAD-IMMEDIATE в регистр 3 константы 18 — опкод 0001 = LOAD-IMMEDIATE, поле регистра 0011 = 3, непосредственное значение 00010010 = 16 + 2 = 18.", correct: true },
    { label: "LOAD-IMMEDIATE в регистр 3 константы 12 — значение прочитано как шестнадцатеричное 12.", misconception: "Поле immediate — это 8-битное двоичное значение, а не шестнадцатеричное. Двоичное 00010010 = 16 + 2 = 18, а не 12." },
    { label: "ADD в регистр 1 — опкод равен 0011.", misconception: "Опкод — это старшие 4 бита, 0001 (LOAD-IMMEDIATE). 0011 — это поле регистра-приёмника, которое называет регистр 3, а не опкод." },
    { label: "Декодировать нельзя — слово это просто данные, а не инструкция.", misconception: "То, инструкция ли байты, задаётся счётчиком команд, а не самими байтами. Раз PC указывает сюда, поля декодируются однозначно: LOAD-IMMEDIATE, регистр 3, значение 18." },
  ]}
/>

## Фрагмент 4 — подсчёт тактов

```
; Программа выполняет 6 инструкций в теле плотного цикла.
; На этом CPU каждая инструкция занимает ровно 1 такт (clock cycle).
; Тело цикла выполняется 100 раз. Тактовая частота CPU — 2 ГГц
; (2 000 000 000 тактов в секунду).
```

<Quiz
  id="03-the-processor-code-4"
  lessonSlug="03-the-processor"
  lang="ru"
  question="Сколько всего тактов потребляет тело цикла и сколько примерно времени это занимает на частоте 2 ГГц?"
  choices={[
    { label: "Всего 600 тактов (6 инструкций x 100 итераций), что при 2 миллиардах тактов/сек составляет около 300 наносекунд.", correct: true },
    { label: "Всего 6 тактов, потому что 6 инструкций считаются один раз независимо от итераций.", misconception: "Каждая итерация заново выполняет тело цикла, поэтому такты умножаются на число итераций: 6 x 100 = 600, а не 6." },
    { label: "Всего 600 тактов, что при 2 ГГц занимает около 300 миллисекунд.", misconception: "Число тактов верное, но время ошибочно в миллион раз. 600 тактов / 2e9 тактов в секунду = 3e-7 с = 300 наносекунд, а не миллисекунд." },
    { label: "Всего 100 тактов, по одному на итерацию.", misconception: "Каждая итерация выполняет все 6 инструкций, и каждая занимает 1 такт, так что итерация стоит 6 тактов. 100 итераций x 6 = 600." },
  ]}
/>

<Recap lang="ru">
На каждый вопрос здесь отвечали, прошагав один и тот же цикл: выбрать инструкцию, на которую указывает PC, декодировать опкод и операнды, исполнить (LOAD и STORE двигают данные между памятью и регистрами; ADD задействует ALU над значениями регистров; JUMP перезаписывает PC), затем сдвинуть PC на размер инструкции — если только JUMP не задал его напрямую. Машинный код декодируется разбиением слова на поля опкода и операнда. А время работы — это просто инструкции x итерации x такты-на-инструкцию, делённые на тактовую частоту. Прошагайте вручную — и процессор перестанет быть чёрным ящиком.
</Recap>
