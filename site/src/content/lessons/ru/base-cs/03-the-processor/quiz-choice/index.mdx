---
concepts:
- fetch-decode-execute
- program-counter
- register
- machine-code
- instruction-set
- stored-program
estMin: 13
lang: ru
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.nand2tetris.org/
- https://en.wikipedia.org/wiki/Instruction_cycle
- https://en.wikipedia.org/wiki/Von_Neumann_architecture
status: ready
summary: "Тест с множественным выбором по всему юниту о процессоре: инструкции, цикл fetch-decode-execute, счётчик команд, регистры, машинный код и идея хранимой программы."
title: 'Процессор: тест с множественным выбором'
track: base-cs
unit: 03-the-processor
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, проходящих через весь юнит. Ни один не требует пересказать определение — каждый заставляет собрать инструкцию, цикл, регистры и байты в памяти в одну работающую машину.
</Hook>

<Goal lang="ru">
Убедитесь, что вы можете связать части, которые уроки строили по отдельности: инструкция — это операция плюс операнды, CPU выполняет их в цикле fetch-decode-execute под управлением счётчика команд (program counter), регистры хранят рабочие значения, а вся программа — это просто байты машинного кода в памяти.
</Goal>

<Quiz
  id="03-the-processor-choice-1"
  lessonSlug="03-the-processor"
  lang="ru"
  question="Программа складывает миллион чисел в цикле. Почему CPU держит текущую сумму в регистре, а не в основной памяти?"
  choices={[
    { label: "Регистр находится на том же кристалле, что и ALU, и читается за 1-2 такта (clock cycles), тогда как обращение к RAM стоит примерно 100-300 тактов; хранение суммы в регистре избавляет от этой задержки миллион раз подряд.", correct: true },
    { label: "Основная память может хранить только инструкции, а не числа, поэтому сумме приходится жить в регистре.", misconception: "Основная память хранит и инструкции, и данные без различий в самом носителе. Регистры существуют ради скорости, а не потому что RAM не может хранить число." },
    { label: "Регистры вмещают большие числа, чем ячейки памяти, поэтому сумма не переполнится.", misconception: "Регистр и ячейка памяти хранят одинаковую фиксированную ширину битов. Дело не в переполнении, а в том, что доступ к регистру намного быстрее доступа к RAM." },
    { label: "Разницы по сути нет; компилятор выбирает наугад.", misconception: "В этом и весь смысл регистров: принести значение в окрестность CPU, обработать его на скорости регистра и записать обратно в RAM один раз в конце." },
  ]}
/>

<Quiz
  id="03-the-processor-choice-2"
  lessonSlug="03-the-processor"
  lang="ru"
  question="CPU только что выполнил обычную инструкцию ADD (не переход). Что происходит со счётчиком команд (program counter) и почему?"
  choices={[
    { label: "Он сдвигается на размер этой инструкции, и следующая выборка читает инструкцию, идущую сразу за ней в памяти. Последовательное выполнение — поведение по умолчанию; переход (JUMP) — исключение, которое его переопределяет.", correct: true },
    { label: "Он увеличивается ровно на 1, потому что счётчик команд считает выполненные инструкции.", misconception: "Несмотря на название, PC хранит адрес, а не счётчик. Он сдвигается на размер инструкции в байтах — 2 у игрушечного CPU, 4 на ARM, 1-15 на x86-64 — а не всегда на 1." },
    { label: "Он устанавливается в результат ADD, и выполнение продолжается с этого адреса.", misconception: "Результаты арифметики идут в регистр-приёмник. PC перезаписывается только переходом или ветвлением, но никогда значением, которое вычислил ADD." },
    { label: "Он остаётся прежним, пока его не сдвинет переход.", misconception: "Если бы PC не сдвигался сам, CPU вечно выбирал бы одну и ту же инструкцию. Именно последовательный сдвиг после каждой инструкции без перехода и движет цикл вперёд." },
  ]}
/>

<Quiz
  id="03-the-processor-choice-3"
  lessonSlug="03-the-processor"
  lang="ru"
  question="Джуниор говорит: 'Мой код написан на Python, значит, CPU выполняет Python напрямую.' Что здесь неверно?"
  choices={[
    { label: "CPU понимает только битовые паттерны своего набора инструкций (instruction set). Python должен быть переведён — скомпилирован или интерпретирован — в эти инструкции, прежде чем что-либо из него сможет выполниться на чипе.", correct: true },
    { label: "Всё верно; у современных CPU есть встроенный набор инструкций для Python.", misconception: "Ни один массовый CPU не декодирует Python. x86-64, ARM и RISC-V понимают только собственную кодировку машинного кода; разрыв закрывает рантайм языка." },
    { label: "Python слишком медленный для компиляции, поэтому он вообще не доходит до CPU.", misconception: "Python безусловно доходит до CPU — через интерпретатор, собственный машинный код которого выполняется от имени каждого оператора Python. CPU выполняет инструкции интерпретатора, а не текст Python." },
    { label: "CPU выполняет Python только после переписывания на C.", misconception: "Переписывать на C не требуется. Интерпретатор выполняет Python без перевода в нативный код; в любом случае CPU выполняет только машинный код своего набора инструкций." },
  ]}
/>

<Quiz
  id="03-the-processor-choice-4"
  lessonSlug="03-the-processor"
  lang="ru"
  question="На шаге декодирования (Decode) цикла какая часть CPU выполняет работу и что она производит?"
  choices={[
    { label: "Устройство управления (control unit) читает опкод и выдаёт сигналы управления, которые маршрутизируют операнды и выбирают нужный функциональный блок (ALU, шину памяти или обновление PC) для шага исполнения.", correct: true },
    { label: "ALU выполняет арифметику, потому что декодирование означает вычислить результат заранее.", misconception: "ALU работает на шаге исполнения (Execute), а не декодирования. Decode лишь распознаёт инструкцию и готовит сигналы управления; ничего ещё не вычисляется." },
    { label: "Основная память декодирует байты и присылает смысл инструкции.", misconception: "Память на шаге выборки лишь возвращает сырые байты. Интерпретация этих байтов — задача устройства управления внутри CPU, на шаге декодирования." },
    { label: "Счётчик команд декодирует инструкцию, чтобы знать, насколько сдвинуться.", misconception: "PC хранит адрес и сдвигается после исполнения. Декодирование опкода — задача устройства управления, а не PC." },
  ]}
/>

<Quiz
  id="03-the-processor-choice-5"
  lessonSlug="03-the-processor"
  lang="ru"
  question="Две ячейки памяти по адресам 100 и 101 хранят байты, кодирующие инструкцию. Те же значения байтов есть и по адресам 200 и 201, где программа хранит данные. Что делает одну пару инструкцией, а другую — данными?"
  choices={[
    { label: "Сами байты ничем не отличаются. Они инструкция только тогда, когда счётчик команд (program counter) указывает на них во время выборки, и данные — когда до них доходит LOAD или STORE. Это и есть идея хранимой программы (архитектура фон Неймана).", correct: true },
    { label: "Байты инструкций хранятся в отдельной микросхеме только для инструкций, куда данные никогда не попадают.", misconception: "Это описывает строгую гарвардскую архитектуру. CPU общего назначения используют модель фон Неймана: инструкции и данные делят одну память и различаются только по контексту." },
    { label: "Байты инструкций несут скрытый бит-метку, помечающий их как исполняемые.", misconception: "В основной памяти нет побайтовой метки инструкции. Исполняемое-против-данных обеспечивает защита памяти ОС по целым областям, а не флаг внутри каждого байта." },
    { label: "CPU перечитывает байты и распознаёт паттерн опкода как 'инструкцию'.", misconception: "Многие паттерны опкодов — это и допустимые значения данных. CPU не классифицирует байт по его значению — он считает его инструкцией просто потому, что на него указал PC." },
  ]}
/>

<Quiz
  id="03-the-processor-choice-6"
  lessonSlug="03-the-processor"
  lang="ru"
  question="Программа, скомпилированная для Mac на Apple Silicon (ARM), не запустится как есть на ноутбуке Intel (x86-64), хотя оба процессора 64-битные. Почему?"
  choices={[
    { label: "ARM и x86-64 — разные наборы инструкций (instruction sets) с разной кодировкой опкодов. Набор инструкций — это контракт между ПО и железом, и эти два чипа его не разделяют.", correct: true },
    { label: "x86-64 быстрее, поэтому он отказывается запускать бинарник, собранный для более медленного ARM.", misconception: "Скорость ни при чём. Бинарник — это поток байтов машинного кода ARM, которые декодер x86-64 просто не может истолковать как свои инструкции." },
    { label: "Бинарник ARM зашифрован, и ключ есть только у железа Apple.", misconception: "Дело не в шифровании. Байты прекрасно читаются; они просто кодируют инструкции ARM, для которых у чипа x86-64 нет декодирования." },
    { label: "Раз оба 64-битные, они разделяют набор инструкций, так что он на самом деле запустится без изменений.", misconception: "Разрядность (64 бита) — не то же самое, что набор инструкций. Одинаковая ширина, но разная кодировка — именно поэтому нужен слой трансляции или эмулятор." },
  ]}
/>

<Recap lang="ru">
Сквозная нить юнита — одна машина. Инструкция — это операция плюс операнды; CPU выполняет инструкции в цикле fetch-decode-execute, где счётчик команд называет следующую и сдвигается на размер инструкции, если только переход не переопределит это. Регистры хранят рабочие значения на скорости CPU, потому что RAM в 100-300 раз медленнее. Decode — это устройство управления, превращающее опкод в сигналы управления. А вся программа — это просто байты машинного кода в общей памяти, инструкции лишь потому, что счётчик команд проходит по ним. Набор инструкций — это контракт, решающий, какой чип может выполнить какие байты.
</Recap>
