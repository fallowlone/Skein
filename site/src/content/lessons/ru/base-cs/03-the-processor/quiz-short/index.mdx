---
concepts:
- fetch-decode-execute
- program-counter
- register
- machine-code
- stored-program
- instruction-set
estMin: 13
lang: ru
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.nand2tetris.org/
- https://en.wikipedia.org/wiki/Instruction_cycle
- https://en.wikipedia.org/wiki/Processor_register
status: ready
summary: Вопросы на свободное припоминание по юниту о процессоре. Сначала ответьте по памяти, затем откройте модельный ответ и сравните.
title: 'Процессор: тест на свободное припоминание'
track: base-cs
unit: 03-the-processor
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый вопрос произнесите или запишите полный ответ по памяти, прежде чем открыть модельный ответ — именно усилие вытащить механизм из головы и закрепляет его.
</Hook>

<Goal lang="ru">
Восстановите по памяти ключевые механизмы юнита: что такое инструкция, как работает цикл fetch-decode-execute, что делают счётчик команд и регистры и почему программа — это просто байты в памяти.
</Goal>

<RetrievalDrawer
  client:load
  id="03-the-processor-short-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое инструкция CPU и из каких двух частей состоит почти любая инструкция?", a: "Инструкция CPU — это наименьшая, неделимая команда, которую процессор может выполнить за один шаг; её нельзя разбить на меньшие части. Почти любая инструкция состоит из двух частей: операции (опкода), которая говорит, что делать, например ADD, LOAD, STORE или JUMP; и операндов, которые говорят, над чем это делать — входов, например над какими регистрами, по какому адресу памяти или с какой непосредственной константой. ADD нужны два регистровых входа; LOAD нужен адрес для чтения; JUMP нужен адрес цели. Несколько инструкций, например HALT, операндов не имеют." },
    { q: "Опишите цикл fetch-decode-execute и роль счётчика команд (program counter) в нём.", a: "CPU вечно повторяет один цикл. Выборка (Fetch): он читает инструкцию по адресу из счётчика команд (PC) в регистр инструкций. Декодирование (Decode): устройство управления читает опкод и производит сигналы управления, которые маршрутизируют операнды и выбирают функциональный блок. Исполнение (Execute): выбранный блок выполняет действие — ALU для арифметики, шина памяти для LOAD/STORE или перезапись PC для JUMP. Затем PC сдвигается на размер инструкции в байтах (так что следующая выборка читает идущую далее инструкцию), если только JUMP не установил его в цель. PC — это курсор, который сообщает CPU, какая инструкция следующая; без него цикл не мог бы двигаться вперёд." },
    { q: "Почему CPU держит рабочие значения в регистрах, а не читает и пишет основную память на каждом шаге?", a: "Регистры — это крошечные слоты хранения, встроенные прямо в CPU, на том же кристалле, что и ALU, поэтому чтение одного занимает примерно 1-2 такта (clock cycles). Основная память (RAM) — это отдельная микросхема, доступная по шине, и одно обращение стоит примерно 100-300 тактов — в 100-300 раз медленнее. Если бы CPU ходил в RAM за каждым промежуточным значением, простые циклы тратили бы почти всё время на ожидание памяти. Вместо этого принцип такой: принести данные в окрестность CPU (регистр), сделать работу там на полной скорости и записать результат обратно в RAM один раз в конце. Регистры — и есть эта окрестность." },
    { q: "В чём разница между регистром общего назначения и регистром специального назначения? Приведите пример каждого.", a: "Регистр общего назначения (general-purpose register, GPR) не имеет фиксированного смысла — любая инструкция может использовать его для любого значения, и компилятор решает, что где лежит. Примеры: R0/R1 в игрушечном CPU или RAX/RBX на x86-64 (у которого 16 GPR; у ARM — 31). Регистр специального назначения имеет фиксированную аппаратную роль. Счётчик команд (program counter) хранит адрес следующей инструкции; указатель стека (stack pointer) отслеживает вершину стека вызовов; регистр флагов (статуса) фиксирует исходы вроде нуля, переноса или отрицательности, которые проверяют условные переходы; регистр инструкций хранит исполняемую сейчас инструкцию. Счётчик команд — это сам по себе регистр; его сдвиг — это обычное обновление PC, происходящее каждый цикл." },
    { q: "Сформулируйте принцип хранимой программы (фон Неймана) и объясните, почему он важен на практике.", a: "Принцип хранимой программы гласит, что инструкции программы и её данные живут в одной памяти, оба как обычные байты; между байтом-инструкцией и байтом-данными нет аппаратной разницы. Байты считаются инструкцией только тогда, когда на них указывает счётчик команд во время выборки, и данными — когда до них доходит LOAD или STORE. Это важно, потому что значит, что одна машина может выполнить любую программу: вы меняете то, что делает компьютер, просто загружая в память другие байты — без перепайки. Поэтому вы ставите ПО, не вскрывая железо; поэтому один CPU телефона запускает браузер и игру; и поэтому JIT-компиляторы могут генерировать машинный код в память во время работы. (ОС использует защиту памяти, чтобы программа не исполнила данные, которые исполнять не должна.)" },
    { q: "Что такое машинный код и что такое набор инструкций (ISA)? Почему бинарник ARM не запускается напрямую на чипе x86-64?", a: "Машинный код — это сырые битовые паттерны, прямо кодирующие инструкции для конкретного CPU; единственный язык, который железо действительно понимает. Каждая инструкция — это определённая последовательность байтов: поле опкода плюс поля операндов (номера регистров, непосредственные значения или адреса). Набор инструкций, или архитектура набора инструкций (ISA), — это полный словарь инструкций, которые понимает модель CPU, вместе с их кодировками; это контракт между ПО и железом. x86-64, ARM и RISC-V — разные ISA с разной кодировкой опкодов, поэтому бинарник, собранный для ARM, — это поток байтов, который декодер x86-64 не может истолковать как свои инструкции. Чтобы его запустить, нужна перекомпиляция под целевую ISA либо эмулятор/слой трансляции." },
  ]}
/>

<Recap lang="ru">
Если вы смогли восстановить каждый ответ по памяти, вы держите стержень юнита: инструкция — это операция плюс операнды; CPU выполняет их в цикле fetch-decode-execute под управлением счётчика команд; регистры хранят рабочие значения, потому что они в 100-300 раз быстрее RAM; некоторые регистры (PC, указатель стека, флаги, регистр инструкций) имеют фиксированные аппаратные роли; а вся программа — это просто байты машинного кода в общей памяти — идея хранимой программы — поэтому одна машина может выполнить любую программу, и поэтому каждая ISA определяет, какие байты может выполнить данный чип.
</Recap>
