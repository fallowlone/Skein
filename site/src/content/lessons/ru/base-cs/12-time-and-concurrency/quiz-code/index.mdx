---
concepts:
- non-blocking-call
- callback-ordering
- interleaving
- data-race
- lost-update
- lock
estMin: 14
lang: ru
level: middle
order: 9
prereqs: []
slug: quiz-code
sources:
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Execution_model
- https://html.spec.whatwg.org/multipage/webappapis.html#event-loops
- https://www.nand2tetris.org/
status: ready
summary: "Читай небольшие сниппеты — головоломку порядка event loop, переплетение thread'ов, lost-update data race и фикс через lock — и предсказывай поведение или выбирай правильный фикс."
title: 'Время и конкурентность: чтение кода'
track: base-cs
unit: 12-time-and-concurrency
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги конкурентности читаются в коде и в трассах того, что когда выполнилось. Прочти каждый сниппет, выясни порядок, в котором всё реально происходит, и выбери, что внимательный инженер заключит — или починит — первым.
</Hook>

<Goal lang="ru">
Отработай ключевой навык этого юнита: трассировать, что когда выполняется. Предсказывай порядок вывода event loop, замечай, как два thread'а переплетаются над общим состоянием, распознавай lost-update data race и определяй lock, который его чинит, не перебарщивая.
</Goal>

## Сниппет 1 — порядок event loop

```js
console.log("start");

setTimeout(() => {
  console.log("timeout");   // callback
}, 0);

console.log("end");
```

<Quiz
  id="12-time-and-concurrency-code-1"
  lessonSlug="12-time-and-concurrency"
  lang="ru"
  question="Что это печатает по порядку?"
  choices={[
    { label: "start, end, timeout. Две строки console.log синхронны и выполняются первыми; setTimeout лишь ставит свой callback в очередь, а event loop запускает его после опустошения стека — даже при задержке 0.", correct: true },
    { label: "start, timeout, end — callback выполняется сразу, как только вызван setTimeout.", misconception: "setTimeout не выполняет callback; он ставит его в очередь. Синхронный 'end' выполняется раньше callback'а из очереди, потому что callback запускается только когда call stack пуст. 0 — это задержка постановки в очередь, а не немедленный запуск." },
    { label: "start, end (callback таймаута не выполняется, потому что задержка 0).", misconception: "Задержка 0 всё равно ставит callback в очередь; он выполняется после завершения синхронного кода. 'timeout' печатается — он просто последний, когда стек опустеет." },
    { label: "timeout, start, end — таймер срабатывает раньше всего.", misconception: "Синхронный код всегда выполняется первым. Таймер лишь делает callback пригодным для постановки в очередь; event loop не запустит его, пока 'start' и 'end' не напечатаны и стек не пуст." },
  ]}
/>

## Сниппет 2 — два thread'а, одна общая переменная

```text
shared: total = 0

Thread A:                  Thread B:
  r = read(total)            r = read(total)   // оба читают 0
  r = r + 1                  r = r + 1
  write(total, r)            write(total, r)   // оба пишут 1
```

<Quiz
  id="12-time-and-concurrency-code-2"
  lessonSlug="12-time-and-concurrency"
  lang="ru"
  question="Оба thread'а выполняют это по разу, параллельно, на двух ядрах. После того как оба закончат, чем может быть total — и как это называется?"
  choices={[
    { label: "Это может быть 1 или 2. Если шаги переплетутся так, что оба прочитают 0 прежде, чем любой запишет, оба запишут 1 и один инкремент потеряется (итог 1). Только непересекающийся порядок даёт 2. Плохой исход — это race condition, а именно lost update.", correct: true },
    { label: "Всегда 2, потому что каждый thread прибавляет 1, а thread'ов два.", misconception: "Это верно только если thread'ы не пересекаются. Поскольку read-add-write — это три отдельных шага, оба thread'а могут сначала прочитать 0, затем оба записать 1 — потеряв один инкремент. Результат зависит от переплетения, поэтому не всегда 2." },
    { label: "Всегда 1, потому что две записи сталкиваются и взаимно гасятся.", misconception: "Записи не гасятся; они перезаписывают. Если thread'ы случайно не пересекутся, ты корректно получишь 2. Значение равно 1 лишь в переплетении, где оба читают одно старое значение. Исход недетерминирован, а не всегда 1." },
    { label: "Это undefined behaviour, и программа падает.", misconception: "Data race на простом счётчике здесь не падает; он молча выдаёт неверное число (часто 1 вместо 2). Опасность именно в том, что выглядит нормально и просто теряет обновления — race condition, а не крах." },
  ]}
/>

## Сниппет 3 — тот же код под нагрузкой

```text
total = 0
spawn 1000 threads, each does:  total = total + 1   // без synchronization
join all threads
print(total)
```

<Quiz
  id="12-time-and-concurrency-code-3"
  lessonSlug="12-time-and-concurrency"
  lang="ru"
  question="На многоядерной машине каким будет вероятное напечатанное значение и почему?"
  choices={[
    { label: "Часто меньше 1000, и оно меняется от запуска к запуску. total = total + 1 — это read-add-write; без synchronization thread'ы переплетаются и перезаписывают обновления друг друга, поэтому инкременты теряются. Число потерь зависит от тайминга.", correct: true },
    { label: "Ровно 1000 каждый запуск — 1000 thread'ов, каждый прибавляет 1.", misconception: "Только если инкременты никогда не пересекаются, что без synchronization не гарантировано. На практике многие thread'ы читают одно значение перед записью, поэтому обновления теряются и итог выходит ниже 1000, по-разному каждый запуск." },
    { label: "Ровно 0, потому что thread'ы перезаписывают total обратно к старту.", misconception: "Thread'ы не сбрасывают total в 0; каждый пишет (старое значение, что он прочитал) + 1. Часть инкрементов всё же доходит. Результат — некоторое число ниже 1000, а не 0: потерянные обновления уменьшают его, а не стирают." },
    { label: "Ровно 1000, потому что порождение thread'ов заставляет их идти по очереди.", misconception: "Порождение thread'ов не сериализует их — на нескольких ядрах они идут параллельно и могут свободно переплетаться. Прохождение по очереди через общее обновление — это именно то, чего здесь не хватает; именно это добавил бы lock." },
  ]}
/>

## Сниппет 4 — добавляем lock

```text
total = 0
lock = new Lock()

each of 1000 threads does:
  lock.acquire()
  total = total + 1     // защищено: только один thread внутри за раз
  lock.release()
```

<Quiz
  id="12-time-and-concurrency-code-4"
  lessonSlug="12-time-and-concurrency"
  lang="ru"
  question="Что меняет lock и какова цена?"
  choices={[
    { label: "Он делает read-add-write одним неделимым шагом: thread должен держать lock, чтобы войти, поэтому два thread'а не пересекаются на total. Результат теперь ровно 1000 каждый запуск. Цена в том, что эта секция выполняется последовательно — thread'ы стоят в очереди за lock, а не идут параллельно.", correct: true },
    { label: "Он ускоряет thread'ы, позволяя им всем обновлять total сразу.", misconception: "Lock делает противоположность 'всем сразу' — он проводит thread'ы через защищённую секцию по одному за раз. Так он убирает race, но также убирает parallelism для этой секции, поэтому это цена, а не ускорение." },
    { label: "Он не даёт эффекта; race всё ещё может случиться внутри lock.", misconception: "Пока один thread держит lock, никакой другой не может войти в защищённую секцию, поэтому read-add-write не может переплестись. Race исчез — результат ровно 1000. Lock — это именно то, что сериализует общее обновление." },
    { label: "Он убирает необходимость в thread'ах вообще.", misconception: "Thread'ы всё ещё выполняются и делают работу; lock лишь контролирует, когда они могут войти в общую секцию. Он координирует thread'ы, а не заменяет их." },
  ]}
/>

<Recap lang="ru">
Читать конкурентность — значит читать, *когда что выполняется*. Event loop ставит синхронный код первым, а callback'и последними (start, end, timeout). Когда работа разбита на **thread'ы**, делящие состояние, обычный `total = total + 1` на деле read-add-write, поэтому параллельные thread'ы могут переплестись, прочитать одно значение и перезаписать друг друга — **data race**, теряющий обновления и дающий неверный недетерминированный итог. **Lock** чинит это, делая общую секцию неделимой: один thread за раз, точный результат восстановлен. Цена в том, что заблокированная секция выполняется последовательно. Зрелый ход — сохранить lock для корректности и сделать заблокированную область как можно меньше.
</Recap>
