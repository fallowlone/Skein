---
concepts:
- concurrency-vs-parallelism
- thread
- interleaving
- race-condition
- shared-state
- run-to-completion
estMin: 13
lang: ru
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Execution_model
- https://www.nand2tetris.org/
- https://www.freecodecamp.org/news/concurrency-vs-parallelism-whats-the-difference-and-why-should-you-care/
status: ready
summary: "Тест с выбором на синтез по всему юниту — async, blocking vs non-blocking, event loop, concurrency vs parallelism и race conditions, появляющиеся, когда задачи делят общее состояние."
title: 'Время и конкурентность: тест с выбором ответа'
track: base-cs
unit: 12-time-and-concurrency
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый — это предсказание или решение о реально работающей программе: не определение для заучивания, а рассуждение, к которому вели уроки, доведённое на шаг дальше — туда, где задачи делят общее состояние.
</Hook>

<Goal lang="ru">
Убедись, что связываешь спину юнита — зачем существует async, blocking vs non-blocking, единственное правило event loop и concurrency vs parallelism — и рассуждаешь о том, что происходит, когда переплетённые или параллельные задачи трогают одни и те же данные.
</Goal>

<Quiz
  id="12-time-and-concurrency-choice-1"
  lessonSlug="12-time-and-concurrency"
  lang="ru"
  question="Веб-сервер читает файл с диска на каждый запрос. Переход с blocking-чтения на non-blocking делает само чтение с диска насколько долгим и что реально улучшается?"
  choices={[
    { label: "Диск по-прежнему тратит своё время — около 10 мс. Улучшается то, что CPU больше не заморожен на время ожидания: он выполняет другую работу (обслуживает другие запросы), а не простаивает на застрявшем кадре стека.", correct: true },
    { label: "Чтение с диска завершается быстрее, потому что non-blocking-вызовы пропускают медленную часть устройства.", misconception: "Non-blocking ничего не меняет в устройстве. Диск всё равно тратит свои 10 мс. Меняется лишь то, заморожен ли CPU на время ожидания (blocking) или свободен делать другую работу (non-blocking)." },
    { label: "Оба чтения занимают одинаковое время и ведут себя одинаково; non-blocking — просто другое написание.", misconception: "Они ведут себя очень по-разному. Blocking-вызов держит свой кадр стека застрявшим, пока работа не закончится, замораживая программу; non-blocking-вызов возвращается сразу и доставляет результат позже через callback." },
    { label: "Non-blocking делает так, что сам запрос падает, если файл ещё не готов.", misconception: "Он не падает. Non-blocking-вызов передаёт запрос устройству и сразу возвращается; результат доставляется позже через callback, когда устройство закончит." },
  ]}
/>

<Quiz
  id="12-time-and-concurrency-choice-2"
  lessonSlug="12-time-and-concurrency"
  lang="ru"
  question="Программа выполняет: console.log('A'); setTimeout(cb, 0), где cb печатает 'B'; затем console.log('C'). Что она печатает и какое правило за этим стоит?"
  choices={[
    { label: "A, C, B. Event loop запускает callback из очереди только когда call stack пуст, поэтому синхронные строки (A и C) завершаются первыми; callback с задержкой 0 (B) лишь быстро попадает в очередь, а не быстро запускается.", correct: true },
    { label: "A, B, C — задержка 0 мс значит, что B выполнится сразу после планирования.", misconception: "0 задаёт задержку постановки в очередь, а не время запуска. B кладётся в callback queue, но event loop не запустит его, пока call stack не опустеет — а это происходит только после того, как синхронные A и C напечатаны." },
    { label: "B, A, C — у setTimeout выше приоритет, поэтому его callback идёт вперёд.", misconception: "Callback'и никогда не вытесняют синхронный код. Callback в очереди ждёт опустошения стека; синхронный код всегда завершается первым. Нет приоритета, который позволил бы callback прервать выполняющуюся строку." },
    { label: "A, C, B потому что B выполняется на втором ядре параллельно.", misconception: "Event loop однопоточный — одно ядро, одно дело за раз. B поздний из-за правила пустого стека, а не потому что выполнялся на другом ядре. Здесь ничто не идёт параллельно." },
  ]}
/>

<Quiz
  id="12-time-and-concurrency-choice-3"
  lessonSlug="12-time-and-concurrency"
  lang="ru"
  question="Программа устроена как одна задача с одним thread. Ты запускаешь её на 16-ядерной машине. Насколько она быстра и почему?"
  choices={[
    { label: "Не быстрее, чем на одном ядре. Один thread — это один поток инструкций и использует ровно одно ядро; остальные 15 простаивают. Parallelism помогает только если программу сначала разбили на несколько задач.", correct: true },
    { label: "Примерно в 16x быстрее, потому что ОС распределит её инструкции по всем 16 ядрам.", misconception: "Один thread — это один упорядоченный поток инструкций, его нельзя разбить по ядрам. Лишние ядра простаивают, пока программа не структурирована в несколько thread'ов или задач. Структура должна предшествовать parallelism." },
    { label: "Быстрее, потому что больше ядер автоматически делают любую программу параллельной.", misconception: "Больше ядер сами по себе никогда не делают программу параллельной. Parallelism требует, чтобы работа была сначала разбита на несколько задач; программа с одной задачей и одним thread выполняется на одном ядре, сколько бы их ни было." },
    { label: "Медленнее, потому что координация 16 ядер добавляет накладные расходы даже для одной задачи.", misconception: "Одна задача использует одно ядро; остальные 15 просто не используются, а не координируются. Накладных расходов нет, потому что нечего координировать — программа ровно так же быстра, как на одноядерной машине." },
  ]}
/>

<Quiz
  id="12-time-and-concurrency-choice-4"
  lessonSlug="12-time-and-concurrency"
  lang="ru"
  question="Одноядерная машина переплетает 50 задач через event loop. Сколько выполняется в один и тот же момент и это concurrency или parallelism?"
  choices={[
    { label: "Ровно одна в любой момент. Это concurrency — много задач переплетены на одном ядре, по очереди. Это не parallelism, который требовал бы нескольких ядер, выполняющих задачи в один и тот же момент.", correct: true },
    { label: "Все 50 выполняются в один и тот же момент; именно это значит 'concurrent'.", misconception: "Concurrency значит чередование по очереди, а не настоящую одновременность. Одно ядро — это один fetch-decode-execute движок, и выполняет ровно одну инструкцию за раз. В любой момент реально выполняется лишь одна из 50 задач." },
    { label: "До 50 сразу, потому что event loop — это форма parallelism.", misconception: "Event loop — это concurrency, а не parallelism. Он выполняет один callback до конца, прежде чем начать следующий, всё на одном ядре. Parallelism требует нескольких ядер; event loop на одном ядре никогда не имеет двух задач, выполняющихся в один момент." },
    { label: "Ноль — переплетение значит, что ни одна по-настоящему не выполняется, лишь запланирована.", misconception: "Переплетение значит, что одна выполняется, пока остальные ждут своей очереди. В каждый момент ровно одна задача исполняется на ядре; остальные приостановлены, а не все остановлены." },
  ]}
/>

<Quiz
  id="12-time-and-concurrency-choice-5"
  lessonSlug="12-time-and-concurrency"
  lang="ru"
  question="Два thread'а на двух ядрах каждый выполняет counter = counter + 1 миллион раз над одним общим counter, без synchronization. Итоговое значение часто гораздо ниже двух миллионов. Почему?"
  choices={[
    { label: "counter = counter + 1 — это не один шаг: чтение, сложение, затем запись. С двумя thread'ами, идущими параллельно, оба могут прочитать одно старое значение прежде, чем любой запишет, поэтому одно обновление перезаписывает другое. Это race condition (потерянное обновление, lost update).", correct: true },
    { label: "Инкремент атомарен, поэтому единственный возможный результат — ровно два миллиона; низкое число — ошибка измерения.", misconception: "counter = counter + 1 не атомарен — это чтение, сложение и запись. Два thread'а могут переплестись между этими подшагами, оба прочитать одно значение, и одна запись затирает другую. Потерянные обновления реальны, а не ошибка измерения." },
    { label: "Два ядра не могут обращаться к одной памяти, поэтому половина записей просто отвергается.", misconception: "Оба ядра могут читать и писать одну общую память — именно в этом проблема. Ничего не отвергается; обновления теряются, потому что два чтения одного старого значения ведут к двум записям одного нового значения, тогда как должно быть два разных инкремента." },
    { label: "Event loop пропускает лишь один инкремент за раз, поэтому он не может терять обновления.", misconception: "Event loop — это однопоточная concurrency. Здесь сценарий — два реальных thread'а на двух ядрах, идущих параллельно: нет единого event loop, сериализующего их. Именно настоящий параллельный доступ к общему состоянию делает race возможным." },
  ]}
/>

<Quiz
  id="12-time-and-concurrency-choice-6"
  lessonSlug="12-time-and-concurrency"
  lang="ru"
  question="Команда добавила lock, чтобы только один thread мог выполнять обновление counter за раз. Race исчез, но throughput упал на 8-ядерной машине. Как это читать?"
  choices={[
    { label: "Ожидаемо. Lock сериализует защищённую секцию — только один thread выполняет её за раз — что убирает race, но также убирает parallelism для этой секции. Фикс — сократить заблокированную область или перепроектировать так, чтобы thread'ы делили меньше, а не убирать lock.", correct: true },
    { label: "Lock сломан — правильный lock должен сохранять полный parallelism и убирать race.", misconception: "Ни один lock не может и то, и другое. Lock работает именно тем, что заставляет thread'ы проходить через защищённую секцию по очереди, что по определению последовательно. Цена throughput — это плата за корректность; снижают её, блокируя меньше, а не ожидая, что lock пойдёт параллельно." },
    { label: "У lock нет стоимости по производительности; замедление приходит откуда-то совсем ещё.", misconception: "У lock есть реальная стоимость: он превращает параллельную секцию в последовательную, поэтому thread'ы стоят в очереди за ним. Чем больше времени внутри lock, тем меньше parallelism остаётся. Замедление — прямое и ожидаемое следствие блокировки." },
    { label: "Убери lock и добавь больше ядер — это вернёт и скорость, и корректность.", misconception: "Убрать lock — значит сразу вернуть race; больше ядер делают race хуже, а не лучше, потому что больше thread'ов борются за одни незащищённые данные. Правильный рычаг — минимизировать общую заблокированную область, сохраняя lock для корректности." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна идея, доведённая до края: CPU быстр, а устройства медленны, поэтому программы используют **async** — **non-blocking**-вызовы плюс **callback'и** — и **event loop**, выполняющий один callback за раз, только когда call stack пуст. Это **concurrency**: много задач переплетены на одном ядре, по очереди. **Parallelism** — иное: задачи по-настоящему выполняются в один и тот же момент на нескольких ядрах — и помогает только когда работа структурирована в задачи. Как только эти задачи делят состояние, становится возможен **race condition**: шаг вроде `counter = counter + 1` на деле чтение-сложение-запись, и параллельные thread'ы могут переплестись и потерять обновления. **Lock** восстанавливает корректность, сериализуя общую секцию — ценой того самого parallelism, который он защищает. Сначала корректность, затем минимизируй то, что должно быть последовательным.
</Recap>
