---
concepts:
- async-motivation
- blocking-vs-non-blocking
- event-loop
- concurrency-vs-parallelism
- race-condition
- synchronization
estMin: 14
lang: ru
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Execution_model
- https://nodejs.org/learn/asynchronous-work/overview-of-blocking-vs-non-blocking
- https://www.freecodecamp.org/news/concurrency-vs-parallelism-whats-the-difference-and-why-should-you-care/
status: ready
summary: "Промпты на свободное воспроизведение по всему юниту — зачем существует async, blocking vs non-blocking, правило event loop, concurrency vs parallelism и как общее состояние рождает race conditions, которые чинит lock."
title: 'Время и конкурентность: свободное воспроизведение'
track: base-cs
unit: 12-time-and-concurrency
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение по памяти бьёт перечитывание. Для каждого промпта скажи или запиши полный ответ по памяти, прежде чем открыть образцовый ответ — именно усилие припоминания закрепляет идею. Эти промпты охватывают весь юнит, заканчивая там, куда указывали уроки: что происходит, когда конкурентные задачи делят данные.
</Hook>

<Goal lang="ru">
Восстанови спину юнита по памяти — зачем существует async, разделение blocking/non-blocking, единственное правило event loop, различие concurrency vs parallelism и почему общее состояние вынуждает synchronization — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:load
  id="12-time-and-concurrency-short-retrieval"
  lang="ru"
  questions={[
    { q: "Зачем существует async? Используй в ответе разрыв в скорости CPU и устройства.", a: "CPU выполняет примерно одну инструкцию за наносекунду, но медленное устройство — диск около 10 мс, сеть десятки–сотни мс — тратит миллионы циклов CPU на ответ. Если программа велит CPU остановиться и ждать, CPU простаивает: включён, но не делает полезной работы, выбрасывая каждый цикл ожидания. Async существует, чтобы CPU делал другую полезную работу в этот разрыв, а не простаивал. Он не делает устройство быстрее — диск всё равно тратит свои 10 мс — он лишь не даёт быстрому CPU пропадать впустую, пока работает медленное устройство." },
    { q: "В чём разница между blocking- и non-blocking-вызовом и для чего нужен callback?", a: "Blocking-вызов не возвращается, пока работа не сделана; когда работа медленная, его кадр стека застревает на стеке на всё ожидание, и следующая строка не может выполниться — программа заморожена. Non-blocking-вызов передаёт запрос устройству и возвращается сразу, поэтому программа продолжает работать, пока устройство трудится в фоне. Поскольку вызов возвращается до того, как результат существует, он не может вернуть результат обычным способом. Поэтому программа передаёт callback: функцию, отданную с инструкцией 'выполни это, когда результат будет готов'. Когда устройство закончит, результат доставляется вызовом этого callback с результатом как аргументом." },
    { q: "Что такое event loop и какое единственное правило определяет, когда выполняется callback?", a: "Event loop — это две вещи: callback queue (очередь FIFO из callback'ов, чья медленная работа завершилась) и цикл, повторяющийся вечно. Его единственное правило: если call stack пуст, взять передний callback из очереди и выполнить его; иначе ждать. Из этого следуют два факта. Синхронный код всегда завершается первым, потому что пока он выполняется, стек непуст и цикл не запустит callback. И каждый callback выполняется до конца без прерывания, потому что цикл не берёт следующий, пока кадры текущего не сняты. Поэтому console.log(1); setTimeout(cb, 0); console.log(3) печатает 1, 3, 2 — callback с задержкой 0 лишь быстро поставлен в очередь, а не быстро запущен." },
    { q: "В чём разница между concurrency и parallelism и почему одноядерная машина может быть concurrent, но никогда parallel?", a: "Ядро — это один fetch-decode-execute движок и выполняет ровно одну инструкцию за раз; thread — это один упорядоченный поток инструкций. Concurrency — это переплетение многих задач на одном ядре так, что они идут по очереди: в любой момент выполняется лишь одна, но все продвигаются со временем (event loop — это concurrency). Parallelism — это много задач, по-настоящему выполняющихся в один и тот же момент, что требует нескольких ядер, по одной инструкции на ядро одновременно. Одноядерная машина может переплетать задачи (concurrency), но физически выполняет одну инструкцию за раз, поэтому никогда не имеет двух задач в один момент (parallelism). Слоган: concurrency про структуру, parallelism про исполнение." },
    { q: "Что такое race condition и почему counter = counter + 1 в двух thread'ах его обнажает?", a: "Race condition — это когда результат программы зависит от непредсказуемого порядка (переплетения), в котором выполняются конкурентные задачи, и некоторые порядки дают неверный ответ. counter = counter + 1 выглядит как один шаг, но на деле это три: прочитать counter, прибавить один, записать counter обратно. Если два thread'а выполняют это параллельно над общим состоянием, оба могут прочитать одно старое значение прежде, чем любой запишет, затем оба записать одно новое значение — так один инкремент теряется (lost update). Без synchronization итог выходит ниже истинного и меняется от запуска к запуску, потому что исход зависит от того, как чтения и записи случайно переплетутся." },
    { q: "Что делает lock, чтобы починить race, и какова цена его использования?", a: "Lock делает секцию кода неделимой: thread должен захватить lock, чтобы войти в защищённую секцию, и пока он держит lock, никакой другой thread войти не может. Поэтому read-add-write больше не может переплестись — thread'ы идут по очереди через общее обновление, и результат становится корректным и детерминированным. Цена в том, что защищённая секция выполняется последовательно: thread'ы стоят в очереди за lock вместо параллельного выполнения, что убирает parallelism именно для этой секции. Зрелое правило — сначала корректность, затем снижение цены: держать заблокированную область как можно меньше и заставлять thread'ы делить как можно меньше состояния — а не убирать lock." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь спину юнита: **async** существует, потому что CPU не должен простаивать через огромный разрыв в скорости CPU и устройства; **non-blocking**-вызов возвращается сразу и доставляет результат позже через **callback**; **event loop** выполняет callback'и по одному за раз, только когда call stack пуст, поэтому синхронный код всегда завершается первым. Это **concurrency** — переплетение на одном ядре, по очереди — в отличие от **parallelism**, настоящей одновременности на нескольких ядрах. И как только конкурентные задачи делят состояние, порядок начинает иметь значение: **race condition** может потерять обновления, потому что read-add-write не атомарен, а **lock** восстанавливает корректность, сериализуя общую секцию ценой её parallelism.
</Recap>
