---
concepts:
- memory-simulation
- address-vs-value
- pointer-dereference
- stack-vs-heap
- byte-layout
estMin: 200
lang: ru
level: middle
order: 8
prereqs: []
slug: project
sources:
- https://www.nand2tetris.org/
- https://en.wikipedia.org/wiki/Pointer_(computer_programming)
- https://en.wikipedia.org/wiki/Stack-based_memory_allocation
status: ready
summary: "Построй крошечный byte-адресуемый симулятор памяти с read, write и dereference, затем смоделируй цепочку pointer'ов и сценарий stack vs heap."
title: 'Память: построй крошечный симулятор памяти'
track: base-cs
unit: 02-memory
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Ты понимаешь address, value, pointer и разделение stack/heap, когда можешь построить модель, подчиняющуюся этим правилам, и увидеть, что она ведёт себя ровно так, как предсказывали уроки. Построй небольшой симулятор памяти от модели ряда ячеек, затем прогони через него цепочку pointer'ов и сценарий stack vs heap.
</Hook>

<Goal lang="ru">
Преврати ментальную модель юнита в работающий код: фиксированный ряд пронумерованных byte-ячеек, из которых можно читать и в которые можно писать по address, операцию dereference, идущую за сохранённым address, и демонстрацию, отличающую value от address и время жизни stack от времени жизни heap.
</Goal>

<ProjectBrief
  id="02-memory-project"
  lessonSlug="02-memory"
  lang="ru"
  objective="Построй крошечный симулятор памяти — фиксированный массив пронумерованных ячеек с read, write и dereference — на любом языке, затем используй его, чтобы смоделировать цепочку pointer'ов и сценарий времени жизни stack vs heap, доказывая каждое поведение распечатанным состоянием до/после."
  requirements={[
    "Представь память как массив фиксированного размера из N ячеек (например 256), каждая хранит одно целое value в диапазоне 0–255. Индекс массива — это address; хранимое целое — value.",
    "Реализуй read(address), возвращающий value по этому address, и write(address, value), сохраняющий value с перезаписью прежнего. Отклоняй любой address вне диапазона 0..N-1 ясной ошибкой выхода за границы.",
    "Реализуй dereference(address): прочитай value по address, трактуй это value как второй address и верни value, лежащее там (всего два чтения). Отклони dereference, если промежуточный address вне диапазона.",
    "Напечатай состояние памяти (address и value для занятых ячеек) и продемонстрируй перезапись value записью: покажи ячейку до и после и покажи, что ни одна другая ячейка не изменилась.",
    "Смоделируй цепочку pointer'ов: ячейка A хранит address ячейки B, а ячейка B хранит настоящие данные. Сделай dereference A и покажи, что доходишь до value B за два чтения. Затем измени value A так, чтобы он указывал на другую ячейку, и покажи, что тот же dereference теперь возвращает value новой цели.",
    "Смоделируй в симуляторе сценарий времени жизни stack vs heap: зарезервируй нижний диапазон address как heap и верхний как stack. Сымитируй вызов функции, зарезервировав stack-фрейм (несколько ячеек), heap-аллокацию, на которую фрейм хранит pointer, затем возврат функции освобождением фрейма. Покажи, что данные на heap всё ещё читаемы после исчезновения фрейма, а освобождённые stack-ячейки трактуются как свободные.",
    "Напиши короткий абзац для каждого сценария, называя, какую идею юнита он демонстрирует (address vs value, dereference, время жизни stack vs heap).",
  ]}
  acceptance={[
    "read, write и dereference ведут себя ровно по спецификации, а address вне диапазона отклоняются, а не молча оборачиваются или роняют программу.",
    "Демонстрация перезаписи показывает изменение целевой ячейки и неизменность всех остальных, распечатанные до и после.",
    "Демонстрация цепочки pointer'ов доходит до данных за два чтения, а перенаправление ячейки A меняет результат dereference, не трогая ячейку B.",
    "Демонстрация stack vs heap показывает, что данные на heap переживают освобождение stack-фрейма, а освобождённые stack-ячейки помечены свободными.",
    "Короткое описание называет идею юнита, которую доказывает каждый сценарий.",
  ]}
  stretch={[
    "Добавь многобайтовые значения: храни 32-битное целое в четырёх последовательных ячейках и добавь read32/write32, собирающие и разбивающие четыре byte, чтобы увидеть, почему одно value растягивается на четыре address.",
    "Добавь простой аллокатор для региона heap: allocate(size), находящий свободные ячейки и возвращающий их стартовый address, и free(address), освобождающий их. Покажи фрагментацию, появляющуюся после нескольких циклов allocate/free.",
    "Добавь защиту от stack overflow: если смоделированные фреймы вырастают за зарезервированный размер региона stack, поднимай ошибку stack overflow вместо порчи региона heap.",
    "Визуализируй память как печатную сетку, цветом кодирующую свободные, stack- и heap-ячейки, и анимируй её по сценарию вызова шаг за шагом.",
  ]}
/>

<Recap lang="ru">
Построение симулятора делает модель конкретной: address — это просто индекс массива, value — просто целое, лежащее там, а dereference — два чтения, ровно как описывали уроки. Резервирование нижнего диапазона под heap и верхнего под stack и наблюдение, как данные на heap переживают освобождённый фрейм, превращает различие stack vs heap из прочитанного факта в поведение, которое ты сам произвёл. Как только правила работают в коде, весь юнит перестаёт быть абстрактным.
</Recap>
