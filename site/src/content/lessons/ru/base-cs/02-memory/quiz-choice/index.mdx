---
concepts:
- address-vs-value
- byte-addressing
- pointer-indirection
- stack-lifo
- heap-lifetime
- dereference
estMin: 13
lang: ru
level: middle
order: 5
prereqs: []
slug: quiz-choice
sources:
- https://en.wikipedia.org/wiki/Pointer_(computer_programming)
- https://en.wikipedia.org/wiki/Stack-based_memory_allocation
- https://en.wikipedia.org/wiki/Byte
status: ready
summary: "Синтез по юниту в формате выбора: address vs value, байтовая адресация, pointer и dereference, время жизни stack vs heap."
title: 'Память: тест с выбором'
track: base-cs
unit: 02-memory
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, связывающих весь юнит воедино. Ни один не про заучивание определения — каждый требует совместить две-три идеи (address vs value, byte, pointer, stack vs heap) и рассудить, что машина делает на самом деле.
</Hook>

<Goal lang="ru">
Подтверди, что умеешь связать ключевые идеи юнита: у ячейки есть и address, и value, byte — единица адресации, value сам может быть address (pointer), а stack и heap — два региона одной и той же памяти с разным временем жизни.
</Goal>

<Quiz
  id="02-memory-choice-1"
  lessonSlug="02-memory"
  lang="ru"
  question="Ячейка по address 7 хранит value 7. Какое утверждение верно?"
  choices={[
    { label: "Address (7) — фиксированная позиция ячейки в ряду; value (7) — данные, лежащие там сейчас. Это одно и то же число по совпадению, а не потому что это одна и та же сущность.", correct: true },
    { label: "Ячейка особенная, потому что её address равен её value.", misconception: "В ячейке, у которой address случайно равен value, нет ничего особенного. Address и value независимы — одно называет местоположение, другое есть данные. Их равенство здесь — чистое совпадение." },
    { label: "Запись нового value также изменит address.", misconception: "Запись меняет только value в ячейке. Address — постоянная позиция ячейки в ряду, и он не меняется при перезаписи содержимого." },
    { label: "Раз числа совпадают, эта ячейка — pointer на саму себя.", misconception: "Ячейка становится pointer только когда программа намеренно использует её value как address. Value, равный собственному address ячейки, не делает её self-pointer; ничто здесь не трактует 7 как address." },
  ]}
/>

<Quiz
  id="02-memory-choice-2"
  lessonSlug="02-memory"
  lang="ru"
  question="Программа хранит 32-битное целое в byte-адресуемой памяти начиная с address 100. Какие address оно занимает и почему?"
  choices={[
    { label: "Address 100, 101, 102, 103 — четыре последовательных byte. Память называет один byte на address, поэтому 32-битное (4-байтовое) значение растягивается на четыре address; CPU читает их вместе как одно слово.", correct: true },
    { label: "Только address 100 — один address хранит всё 32-битное значение.", misconception: "Каждый address называет ровно один byte (8 бит), а не 32 бита. 32-битному значению нужно четыре byte, значит оно занимает четыре последовательных address; CPU грузит их вместе." },
    { label: "Address с 100 по 131 — по одному address на бит.", misconception: "Память byte-адресуема, а не bit-адресуема. Address называет целый byte, поэтому 32-битному значению нужно 32 / 8 = 4 address, а не 32." },
    { label: "Address 100 и 101 — двух byte хватит на любое целое.", misconception: "Два byte хранят лишь 16 бит. 32-битному целому нужно четыре byte (четыре address). Число byte получается делением битов на 8." },
  ]}
/>

<Quiz
  id="02-memory-choice-3"
  lessonSlug="02-memory"
  lang="ru"
  question="Ячейка по address 5 хранит value 12. Ячейка по address 12 хранит value 99. Программа делает dereference pointer'а по address 5. Сколько чтений памяти произойдёт и каков итоговый результат?"
  choices={[
    { label: "Два чтения: читаем address 5 и получаем 12 (это address), затем читаем address 12 и получаем 99. Итог — 99: dereference следует по сохранённому address на один шаг.", correct: true },
    { label: "Одно чтение: dereference address 5 возвращает 12 — value из ячейки-pointer.", misconception: "Dereference не останавливается на ячейке-pointer. Он использует найденный value (12) как второй address и читает и эту ячейку. Результат — value по address 12, то есть 99." },
    { label: "Два чтения, итог 5 — возвращается к исходному address.", misconception: "Dereference не зацикливается назад. Он читает pointer (5 даёт 12), затем читает address 12 и получает 99. Результат — 99, а не стартовый address." },
    { label: "Ноль чтений — value pointer'а 12 и есть ответ напрямую.", misconception: "Value pointer'а 12 — это address, а не данные. Нужно второе чтение по address 12, чтобы добраться до настоящего value 99. Оба чтения обязательны." },
  ]}
/>

<Quiz
  id="02-memory-choice-4"
  lessonSlug="02-memory"
  lang="ru"
  question="Почему indirection через pointer встречается почти в каждой нетривиальной программе, хотя стоит лишнего чтения памяти?"
  choices={[
    { label: "Она даёт sharing без копирования (много ячеек могут хранить один address) и обновление в одном месте (перемести данные, поменяй только pointer — и каждая ссылка идёт за ним). Эта гибкость стоит одного лишнего чтения.", correct: true },
    { label: "Потому что pointer делает доступ к памяти быстрее, чем чтение value напрямую.", misconception: "Indirection медленнее для одного доступа — она добавляет чтение. Pointer используют ради sharing и гибких обновлений, а не ради скорости одной выборки." },
    { label: "Потому что железо требует, чтобы каждое value доставалось через pointer.", misconception: "Железо может прочитать любую ячейку напрямую по её address. Pointer — программная конвенция поверх, нужная при sharing или indirection, а не требование железа." },
    { label: "Потому что pointer уменьшает число byte, которые занимает value.", misconception: "Pointer добавляет хранение (сам address-pointer плюс данные, на которые он указывает). Выгода — sharing и косвенные обновления, а не меньший объём хранения." },
  ]}
/>

<Quiz
  id="02-memory-choice-5"
  lessonSlug="02-memory"
  lang="ru"
  question="Функция main вызывает a, та вызывает b. Пока b работает, a создаёт большой объект на heap и держит на него pointer. b возвращается, затем возвращается a. Что верно про память после этого?"
  choices={[
    { label: "Stack-фрейм b отбрасывается при возврате b, а фрейм a — при возврате a (LIFO). Объект на heap может пережить оба вызова — он существует, пока не освобождён явно или garbage collector'ом, независимо от stack.", correct: true },
    { label: "Объект на heap уничтожается автоматически при возврате a, как stack-фрейм.", misconception: "Данные на heap не привязаны ко времени жизни функции. В отличие от stack-фрейма, они живут, пока не освобождены явно или garbage collector'ом — эта независимость от времени жизни вызова и есть смысл heap." },
    { label: "Фрейм b живёт до возврата main, потому что создан глубже всех.", misconception: "Stack работает по LIFO: самый поздно добавленный фрейм (b) снимается первым — при возврате b. Фрейм никогда не переживает свою функцию." },
    { label: "Объект на heap лежит на отдельном чипе памяти, поэтому возвраты со stack его не затрагивают.", misconception: "Stack и heap — регионы одной и той же физической RAM. Объект выживает из-за правил времени жизни heap, а не потому что лежит на другом железе." },
  ]}
/>

<Quiz
  id="02-memory-choice-6"
  lessonSlug="02-memory"
  lang="ru"
  question="Рекурсивная функция без базового случая бесконечно вызывает себя, и программа падает со stack overflow. Что произошло на самом деле в терминах модели юнита?"
  choices={[
    { label: "Каждый вызов клал новый stack-фрейм, и ни один не снимался, поэтому регион stack вырос за свой фиксированный лимит (обычно 1–8 МБ) и упёрся в границу. Это лимит региона памяти, а не арифметическая ошибка.", correct: true },
    { label: "Закончилось место на heap, потому что каждый вызов аллоцировал объект на heap.", misconception: "Stack overflow — про регион stack, а не heap. Каждый вызов кладёт фрейм на stack; без базового случая фреймы копятся, пока не превышен фиксированный размер stack." },
    { label: "У CPU кончились address для byte-значений.", misconception: "Падение — это превышение зарезервированного размера региона stack, а не нехватка address у машины. Address-пространство куда больше маленького фиксированного лимита stack." },
    { label: "Сохраняемые value стали слишком велики для одного byte каждый.", misconception: "Stack overflow — про число фреймов (глубину вложенности), а не размер отдельного value. Безграничная рекурсия растит счётчик фреймов без предела." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна модель: память есть ряд byte-адресуемых ячеек; у каждой ячейки фиксированный address и изменяемый value; value сам может быть address — это и делает pointer, а следование за ним (dereference) стоит одного лишнего чтения; и одна и та же физическая память разделена на LIFO-stack, чьи фреймы живут ровно столько, сколько их функция, и heap, чьи объекты могут пережить любой вызов. Каждый вопрос выше сводится к различению address и value и времени жизни от местоположения.
</Recap>
