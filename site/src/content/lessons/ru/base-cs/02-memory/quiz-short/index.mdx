---
concepts:
- address-vs-value
- byte-addressing
- pointer
- dereference
- stack-vs-heap
- random-access
estMin: 13
lang: ru
level: middle
order: 6
prereqs: []
slug: quiz-short
sources:
- https://en.wikipedia.org/wiki/Random-access_memory
- https://en.wikipedia.org/wiki/Pointer_(computer_programming)
- https://en.wikipedia.org/wiki/Stack-based_memory_allocation
status: ready
summary: "Промпты на свободное припоминание по юниту памяти. Сначала ответь по памяти, затем раскрой модельный ответ и сравни."
title: 'Память: свободное припоминание'
track: base-cs
unit: 02-memory
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт проговори или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие вытащить его и закрепляет идею.
</Hook>

<Goal lang="ru">
Восстанови ключевые идеи юнита — address vs value, byte как единицу адресации, random access, pointer и dereference, stack vs heap — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:load
  id="02-memory-short-retrieval"
  lang="ru"
  questions={[
    { q: "В чём разница между address и value и почему их легко спутать?", a: "Address — это фиксированный номер позиции ячейки в длинном ряду ячеек памяти, начиная с 0; это то, как ты находишь ячейку. Value — данные, лежащие в ячейке сейчас, которые меняются в любой момент записью. Их легко спутать, потому что оба — просто числа: ячейка по address 42 может хранить value 42, но равенство — совпадение. Address говорит машине, где смотреть; value — то, что она находит. Запись меняет только value; address постоянен." },
    { q: "Почему память byte-адресуема, а не bit-адресуема, и что такое byte?", a: "Byte — это 8 бит, сгруппированных и трактуемых как одна единица; он хранит 256 различных значений (0–255). Память byte-адресуема: каждый address называет целый byte, а не один бит. Bit-адресация была бы непрактична — нужно в восемь раз больше address, и каждая полезная операция требовала бы пересборки нескольких однобитовых чтений в символ или число. Byte — правильная гранулярность: достаточно большой, чтобы хранить символ или малое число, достаточно малый, чтобы не тратить много места. Многобайтовое значение (например 32-битное целое) занимает несколько последовательных byte-address." },
    { q: "Что на самом деле значит random access (RA в RAM)?", a: "Random access значит, что CPU достигает любой ячейки напрямую, называя её address, примерно за одно и то же время, где бы эта ячейка ни была в ряду. Он не сканирует ячейки одну за другой в поисках данных. Чтобы прочитать, он кладёт address на адресную шину и получает обратно value ячейки; чтобы записать, шлёт address плюс новое value. Нет затрат на обход, чтобы добраться до дальней ячейки против ближней — этот прямой, не зависящий от позиции доступ и есть random access." },
    { q: "Что такое pointer и как работает его dereference?", a: "Pointer — это ячейка, чей value намеренно используется как address: он хранит местоположение другой ячейки, а не обычные данные. Поскольку address и value оба — просто числа, ничто в железе не помечает value как pointer; программа сама решает трактовать его как address. Dereference значит следовать за pointer: прочитать ячейку-pointer, чтобы получить хранимый address, затем сделать второе чтение по этому address, чтобы получить настоящие данные. Так dereference стоит двух чтений памяти вместо одного — лишнее чтение есть плата за indirection." },
    { q: "Почему indirection через pointer оправдывает свою лишнюю цену?", a: "Indirection стоит одного лишнего чтения памяти на доступ, но даёт две вещи, нужные почти каждой программе. Первое — sharing без копирования: много ячеек могут хранить один address, поэтому разные части программы ссылаются на одни данные, не дублируя их. Второе — обновление в одном месте: если данные переезжают, ты меняешь только value pointer'а на новый address, и каждая ссылка через него идёт за ним автоматически. Без indirection пришлось бы находить и обновлять каждую копию. Эти две выгоды и есть причина, почему pointer и reference встречаются почти везде." },
    { q: "Чем различаются stack и heap по управлению и времени жизни, раз это одна и та же физическая память?", a: "Оба — регионы одной и той же плоской byte-адресуемой RAM; разница в управлении и времени жизни. Stack управляется автоматически: каждый вызов функции кладёт фрейм с её локальными переменными и параметрами, и фрейм отбрасывается при возврате функции, в порядке last-in-first-out — фрейм самой поздно вызванной функции снимается первым. Данные на stack живут ровно столько, сколько их функция. Heap держит данные, которые должны пережить один вызов; он аллоцируется и освобождается явно (или garbage collector'ом), без фиксированного порядка, поэтому его объекты живут до освобождения независимо от того, какая функция их создала." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: address называет ячейку, а value — то, что в ней лежит; byte — единица адресации, и память byte-адресуема; random access достигает любой ячейки напрямую по address; pointer — это value, используемый как address, и его dereference стоит второго чтения в обмен на sharing и обновление в одном месте; а stack и heap — одна и та же RAM, разделённая управлением и временем жизни: автоматические LIFO-фреймы против явно управляемых, дольше живущих объектов.
</Recap>
