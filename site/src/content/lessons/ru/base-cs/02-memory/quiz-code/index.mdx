---
concepts:
- memory-diagram
- address-arithmetic
- pointer-dereference
- stack-vs-heap-placement
- byte-layout
estMin: 14
lang: ru
level: middle
order: 7
prereqs: []
slug: quiz-code
sources:
- https://en.wikipedia.org/wiki/Pointer_(computer_programming)
- https://www.cs.cmu.edu/afs/cs/academic/class/15213-f15/www/lectures/09-machine-advanced.pdf
- https://en.wikipedia.org/wiki/Stack-based_memory_allocation
status: ready
summary: "Читай диаграммы памяти и небольшие сниппеты, считай адресную арифметику, трассируй dereference и решай stack vs heap."
title: 'Память: чтение диаграмм и трассировок'
track: base-cs
unit: 02-memory
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Вопросы про память — на деле вопросы про диаграммы. Прочитай каждую раскладку или сниппет, трассируй, что машина делает ячейка за ячейкой, и выбери ответ, совпадающий с моделью — address vs value, byte-смещения, dereference и где живут данные.
</Hook>

<Goal lang="ru">
Отработай движения, которые будешь повторять всегда: прочитать диаграмму памяти, посчитать byte-address по смещению, пройти по pointer на один шаг и рассудить, лежит value на stack или на heap.
</Goal>

## Сниппет 1 — диаграмма памяти

```
addr:   0    1    2    3    4    5
value: 25   30    3    0   99   12
```

<Quiz
  id="02-memory-code-1"
  lessonSlug="02-memory"
  lang="ru"
  question="Ячейка по address 2 хранит value 3, и программа трактует address 2 как pointer. Сделай dereference: какое value вернётся?"
  choices={[
    { label: "0 — читаем address 2 и получаем value 3, затем читаем address 3 (только что найденный address) и получаем 0.", correct: true },
    { label: "3 — value из ячейки-pointer и есть ответ.", misconception: "Value 3 в ячейке-pointer — это address, а не итоговые данные. Dereference дальше читает address 3, где лежит 0. Ответ — 0." },
    { label: "25 — dereference всегда стартует с address 0.", misconception: "Dereference стартует с ячейки-pointer (address 2), а не с address 0. Читаем address 2 и получаем 3, затем address 3 и получаем 0." },
    { label: "99 — возвращается наибольшее value в ряду.", misconception: "Dereference следует по сохранённому address, а не ищет максимум. Address 2 хранит 3, а address 3 хранит 0, поэтому результат — 0." },
  ]}
/>

## Сниппет 2 — адресная арифметика

```
Массив 32-битных целых начинается с address 200.
Каждый элемент шириной 4 byte (32 бита).
Элементы лежат вплотную без зазоров.
```

<Quiz
  id="02-memory-code-2"
  lessonSlug="02-memory"
  lang="ru"
  question="По какому byte-address начинается элемент с индексом 3 (четвёртый элемент)?"
  choices={[
    { label: "212 — элемент i начинается с base + i * elementSize = 200 + 3 * 4 = 212. Индекс 3 — четвёртый элемент, потому что индексы стартуют с 0.", correct: true },
    { label: "203 — прибавить индекс 3 к base-address 200.", misconception: "Каждый элемент шириной 4 byte, поэтому продвигаешься на index * 4, а не на index * 1. Смещение = 3 * 4 = 12, итого 200 + 12 = 212." },
    { label: "216 — элемент с индексом 3 — третий элемент, по 200 + 4 * 4.", misconception: "Индекс 3 — четвёртый элемент (индексы с 0), и его смещение = 3 * 4 = 12. Это даёт 212, а не 216. 4 * 4 был бы индекс 4." },
    { label: "200 — массив всегда начинается с base-address независимо от индекса.", misconception: "На base лежит только индекс 0. Индекс 3 — на три элемента дальше: 200 + 3 * 4 = 212." },
  ]}
/>

## Сниппет 3 — размещение stack vs heap

```c
int* make_counter() {
    int local = 0;        // строка A
    int* shared = malloc(sizeof(int));  // строка B
    *shared = 0;
    return shared;        // возвращает heap-pointer
}
```

<Quiz
  id="02-memory-code-3"
  lessonSlug="02-memory"
  lang="ru"
  question="После возврата make_counter что из local (строка A) и int от malloc (строка B) безопасно существует и почему?"
  choices={[
    { label: "Выживает только int от malloc (строка B): он живёт на heap, который переживает вызов. local (строка A) живёт в stack-фрейме этой функции и отбрасывается, когда фрейм снимается при возврате.", correct: true },
    { label: "Выживают оба, потому что оба — целые, созданные внутри функции.", misconception: "Быть целым к времени жизни отношения не имеет; важно размещение. local лежит в stack-фрейме и умирает с ним. Только heap-аллокация от malloc переживает вызов." },
    { label: "Выживает только local, потому что объявлен первым.", misconception: "Порядок объявления не задаёт время жизни. local — в stack-фрейме и исчезает после возврата; переживает heap-аллокация (строка B)." },
    { label: "Не выживает ничего — возврат из функции освобождает всю память, которой она касалась.", misconception: "Возврат освобождает stack-фрейм, но heap-память от malloc независима от вызова и живёт, пока не освобождена явно. Аллокация строки B выживает." },
  ]}
/>

## Сниппет 4 — трассировка записей

```
Старт:  addr 10 -> 7,  addr 11 -> 0
Op 1: записать value 4 по address 11
Op 2: записать value 9 по address 10
Op 3: записать value 0 по address 10
```

<Quiz
  id="02-memory-code-4"
  lessonSlug="02-memory"
  lang="ru"
  question="После всех трёх записей какие value лежат по address 10 и 11?"
  choices={[
    { label: "По address 10 лежит 0, по address 11 лежит 4. Каждая запись перезаписывает прежнее value (по ячейке побеждает последняя запись), а запись в один address никогда не трогает другой.", correct: true },
    { label: "По address 10 лежит 9, по address 11 лежит 4 — Op 3 лишь добавляет к ячейке.", misconception: "Запись перезаписывает, а не складывает и не наслаивает value. Op 3 заменяет 9 на 0, поэтому address 10 заканчивается на 0, а не на 9." },
    { label: "По address 10 лежит 0, по address 11 лежит 0 — последняя запись очищает всё.", misconception: "Запись затрагивает только свой целевой address. Op 3 ставит address 10 в 0, но оставляет address 11 равным 4 — value, записанному Op 1." },
    { label: "По address 10 лежит 7, по address 11 лежит 0 — исходные value, ведь записи временны.", misconception: "Записи деструктивны и постоянны до следующей записи в ту ячейку. Исходных нет: address 10 заканчивается на 0, а address 11 — на 4." },
  ]}
/>

<Recap lang="ru">
Каждая задача здесь решается буквальным чтением диаграммы: value может быть address, поэтому dereference — это второе чтение по тому address; byte-address элемента массива — это base + index * elementSize; выживание value после возврата функции зависит от того, лежал он в stack-фрейме (исчез) или на heap (сохранился); а запись перезаписывает ровно одну ячейку, побеждает последняя запись, и ни один другой address не трогается. Трассируй ячейка за ячейкой — и ответ предопределён.
</Recap>
