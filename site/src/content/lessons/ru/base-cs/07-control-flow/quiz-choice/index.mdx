---
concepts:
- program-counter
- conditional-jump
- flags-register
- fall-through
- backward-jump
- infinite-loop
estMin: 13
lang: ru
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://en.wikipedia.org/wiki/Control_flow
- https://www.cs.cmu.edu/afs/cs/academic/class/15213-f15/www/lectures/05-machine-basics.pdf
- https://www.nand2tetris.org/
status: ready
summary: "Синтез всего unit по control flow с множественным выбором: program counter, conditional jump и флаги, fall-through, backward jump и почему loop повторяется или работает вечно."
title: 'Control flow: тест с множественным выбором'
track: base-cs
unit: 07-control-flow
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, проходящих через весь unit. Ни один не просит определение. Каждый просит рассуждать о том, что program counter сделает дальше — а это и есть единственный вопрос, к которому сводятся любой branch и любой loop.
</Hook>

<Goal lang="ru">
Убедитесь, что вы связываете четыре урока в одну модель: program counter управляет flow, инструкция сравнения выставляет флаги, conditional jump читает один бит флага, backward jump повторяет тело, а if/else — это просто два адреса назначения для PC.
</Goal>

<Quiz
  id="07-control-flow-choice-1"
  lessonSlug="07-control-flow"
  lang="ru"
  question="Две работающие машины хранят одинаковые байты инструкций по одинаковым адресам, но исполняют совершенно разные последовательности инструкций. Как такое возможно?"
  choices={[
    { label: "Execution flow задаётся значением, которое program counter держит при каждой выборке, а не байтами. Поведите PC иначе — будут взяты другие цели jump — и те же байты выполнятся в другом порядке.", correct: true },
    { label: "Невозможно: одинаковые байты по одинаковым адресам всегда исполняются в одном порядке.", misconception: "Байты фиксированы и неподвижны; flow динамичен. PC решает, какой байт выбирается следующим, поэтому одна программа может идти разными flow в зависимости от того, какие jump взяты." },
    { label: "CPU перечитывает исходный код, а у двух машин разные исходники.", misconception: "CPU никогда не видит исходный код — только байты в памяти. Обе машины исполняют одни и те же байты; разница в том, какие адреса посещает PC." },
    { label: "Регистры общего назначения R0 и R1 хранят адрес следующей инструкции.", misconception: "Регистры общего назначения хранят вычисляемые данные. Только program counter хранит адрес следующей инструкции для выборки." },
  ]}
/>

<Quiz
  id="07-control-flow-choice-2"
  lessonSlug="07-control-flow"
  lang="ru"
  question="Выполнился CMP R0, R1, затем достигнут JNE (jump if not equal). Какую единственную часть состояния JNE реально проверяет, решая jump или fall-through?"
  choices={[
    { label: "Флаг Z (zero), выставленный CMP. JNE прыгает при Z=0 (вычитание дало не ноль, значит значения различаются) и делает fall-through при Z=1.", correct: true },
    { label: "Он сам заново вычитает R0 и R1, чтобы пересчитать, равны ли они.", misconception: "Conditional jump не делает арифметики. Сравнение уже произошло в CMP; флаг — его остаток. JNE только читает бит флага." },
    { label: "Текущие значения R0 и R1 напрямую.", misconception: "К моменту выполнения JNE он смотрит на flags register, а не на операнды. CMP записал отношение в флаг Z; значения операндов заново не читаются." },
    { label: "Младший бит program counter.", misconception: "PC хранит адрес для выборки, а не результат сравнения. Решение исходит из флага (здесь Z), который выставил CMP." },
  ]}
/>

<Quiz
  id="07-control-flow-choice-3"
  lessonSlug="07-control-flow"
  lang="ru"
  question="if/else компилируется в: CMP; conditional jump на ELSE; блок if-true; безусловный JUMP на END; ELSE: блок if-false; END. Зачем нужен безусловный JUMP после блока if-true?"
  choices={[
    { label: "Без него после блока if-true PC просто двинулся бы дальше в блок else и выполнил оба. JUMP перепрыгивает блок else на END.", correct: true },
    { label: "Он декоративен; удаление ничего не меняет, ведь блок else исполняется только когда условие ложно.", misconception: "Fall-through механичен: PC продолжает идти на следующие байты в памяти, а это блок else. Без перепрыгивания путь if-true тоже выполнит блок else." },
    { label: "Он сбрасывает program counter в 0, чтобы перезапустить функцию.", misconception: "Безусловный JUMP здесь целится в END, инструкцию после блока else, а не в адрес 0. Его задача — перепрыгнуть блок else, а не перезапустить." },
    { label: "Он перевыполняет CMP, чтобы блок else мог заново проверить условие.", misconception: "Блок else достигается только через conditional jump, когда условие ложно. Завершающий JUMP существует, чтобы перепрыгнуть блок else на истинном пути, а не для повторной проверки." },
  ]}
/>

<Quiz
  id="07-control-flow-choice-4"
  lessonSlug="07-control-flow"
  lang="ru"
  question="На машинном уровне в чём единственное структурное различие между скомпилированным while loop и for loop?"
  choices={[
    { label: "Никакого, которое CPU мог бы обнаружить. for loop добавляет блок init перед верхом loop и блок update прямо перед backward jump, но шаблон «тест — тело — backward jump» идентичен.", correct: true },
    { label: "for loop использует отдельный аппаратный регистр-счётчик loop, а while loop — нет.", misconception: "У CPU общего назначения нет отдельного счётчика loop в цикле fetch-decode-execute. Оба loop используют обычный регистр, уменьшаемый или увеличиваемый обычной инструкцией." },
    { label: "while loop проверяет условие внизу, for loop — наверху.", misconception: "Оба проверяют наверху, поэтому тело может выполниться ноль раз. Проверка внизу (тело исполняется хотя бы раз) — это do-while, а не for loop." },
    { label: "for loop компилируется в один специальный опкод FOR.", misconception: "Опкода FOR не существует. for — синтаксический сахар, компилирующийся в ту же backward-jump структуру, что и while." },
  ]}
/>

<Quiz
  id="07-control-flow-choice-5"
  lessonSlug="07-control-flow"
  lang="ru"
  question="Loop обратного отсчёта уменьшает счётчик и должен выйти при достижении 0, но никогда не выходит и держит CPU на 100%. Какое объяснение согласуется с тем, как машина реально исполняет loop?"
  choices={[
    { label: "Тело loop никогда не меняет значение, которое сравнивает тест, поэтому флаг выхода не переключается. Backward jump срабатывает на каждом проходе, и PC снова возвращается наверх.", correct: true },
    { label: "CPU потерял счёт, сколько раз он уже прошёл loop.", misconception: "CPU ничего не считает. Он заново проверяет условие каждый проход и делает одно бинарное решение jump/fall-through; число итераций — эмерджентное свойство изменения условия, а не то, что CPU отслеживает." },
    { label: "Байты тела loop копировались обратно наверх памяти на каждом проходе.", misconception: "Инструкции никогда не двигаются. Байты остаются по фиксированным адресам; backward jump лишь отматывает PC, поэтому те же фиксированные байты исполняются заново." },
    { label: "Infinite loop — это аппаратный сбой, который CPU должен был обнаружить.", misconception: "Infinite loop — это CPU, делающий ровно то, что говорит программа. Он не может отличить намеренный while(true) от бага; остановить может только ОС или прерывание." },
  ]}
/>

<Quiz
  id="07-control-flow-choice-6"
  lessonSlug="07-control-flow"
  lang="ru"
  question="В sumPositives по [3, -1, 4] внутренний if пропускает отрицательный элемент. Когда nums[i] не положителен, что делает машина и завершается ли loop досрочно?"
  choices={[
    { label: "Conditional jump перепрыгивает sum += nums[i]; i++ всё равно выполняется, потому что стоит вне if; затем безусловный backward jump возвращает к while-тесту. Loop не завершается досрочно — его завершает только достижение i длины массива.", correct: true },
    { label: "Loop завершается, как только найден неположительный элемент.", misconception: "While-тест сверяет i с длиной массива, а не со знаком элемента. Отрицательное значение лишь пропускает тело внутреннего if; loop продолжается." },
    { label: "i++ пропускается, поэтому тот же элемент перепроверяется вечно.", misconception: "i++ стоит вне блока if и выполняется всегда. Его пропуск перепроверял бы тот же элемент и зациклил бы вечно — но код делает не это." },
    { label: "Для отрицательного элемента исполняются оба пути: if-true и if-false.", misconception: "Conditional jump выбирает ровно один путь по одному биту флага. Отрицательный элемент идёт только по пути пропуска." },
  ]}
/>

<Recap lang="ru">
Весь unit — одна сквозная линия: program counter решает, что выполнится дальше, и только jump может его изменить. Сравнение выставляет флаг; conditional jump читает этот один флаг и либо прыгает, либо делает fall-through; if/else — это два адреса назначения для PC с защитным JUMP на границе; loop — это backward jump, чей conditional-тест наверху решает, упасть ли снова в тело; а loop работает вечно, когда проверяемое значение никогда не меняется. Любые branch и loop, которые вы прочтёте, сводятся к одному вопросу: куда PC указывает дальше и почему.
</Recap>
