---
slug: 04-tracing-a-program
lang: ru
track: base-cs
unit: 07-control-flow
order: 4
title: "Трассировка программы"
summary: "Пройди шаг за шагом TypeScript-программу, объединяющую условие и цикл. Следи за счётчиком команд и состоянием переменных на каждой итерации, чтобы увидеть точно, как if и while работают вместе на уровне инструкций."
estMin: 25
status: ready
lessonType: coding
prereqs:
  - "02-fetch-decode-execute"
  - "01-what-flow-means"
  - "02-conditionals-as-branches"
  - "03-loops-as-repeated-jumps"
  - "01-a-variable-is-a-named-cell"
mathPrereqs: []
concepts: ["program-trace", "combined-if-while", "loop-variable-state", "step-by-step-execution"]
sources:
  - https://www.nand2tetris.org/
  - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/while
  - https://www.cs.cmu.edu/afs/cs/academic/class/15213-f15/www/lectures/05-machine-basics.pdf
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import PracticeSet from "~/components/math/PracticeSet.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Ты теперь знаешь, что `if` — это условный переход, а `while` — обратный переход. Но
читать о них по отдельности — совсем не то же самое, что наблюдать, как они работают
вместе в реальной программе, шаг за шагом, с меняющимся прямо перед тобой состоянием
переменных.

Именно это и делает этот урок. Ты прочитаешь короткую TypeScript-функцию, которая
обходит небольшой массив и суммирует только положительные числа. Затем проследишь её
кадр за кадром — следя за счётчиком команд и значением каждой переменной на каждой
строке — пока функция не вернёт результат.

После этой трассировки ты будешь знать не только то, что код *говорит*; ты будешь знать,
что CPU *делает*, по одной инструкции за раз.
</Hook>

<Goal lang="ru">
После этого урока ты сможешь прочитать программу, объединяющую цикл и условие, предсказать
каждый шаг выполнения, отслеживая счётчик команд и значения переменных, объяснить, как
взаимодействуют условие цикла и условие if, и определить точный кадр, в котором цикл
выходит.
</Goal>

<Idea lang="ru">
Программа для трассировки — функция суммирования положительных чисел по фиксированному
массиву. В ней три момента управления потоком:

1. **Тест while** — вычислить `i < nums.length` в начале каждой итерации. Счётчик команд
   либо проваливается в тело цикла (условие истинно), либо прыгает вперёд за цикл
   (условие ложно, выход).
2. **Тест if** — вычислить `nums[i] > 0` внутри тела. Счётчик команд либо выполняет
   `sum += nums[i]` (условие истинно, fall-through), либо пропускает его (условие ложно,
   прыжок вперёд).
3. **Инкремент и обратный переход** — `i++` выполняется на каждой итерации (положительное
   или нет), затем безусловный обратный переход возвращается к тесту while.

Массив: `[3, -1, 4]`. Начальное состояние: `sum = 0`, `i = 0`. Ожидаемый результат:
`3 + 4 = 7` (значение `-1` пропускается внутренним if).

Каждый кадр в трассировке показывает:
- Строку кода, которая вот-вот выполнится.
- Текущие значения `i`, `sum` и `nums[i]`.

Настоятельно рекомендуется сначала самостоятельно пройти трассировку вручную, а потом
смотреть кадры — это закрепляет ментальную модель движения счётчика команд по коду.
</Idea>

<Code lang="ru">

<AnnotatedCode
  code={`function sumPositives(nums: number[]): number {
  let sum = 0;   // аккумулятор начинается с нуля
  let i = 0;     // счётчик цикла начинается с нуля

  while (i < nums.length) {   // цикл с обратным переходом
    if (nums[i] > 0) {        // условие: пропустить отрицательные
      sum += nums[i];
    }
    i++;                      // всегда инкремент, положительное или нет
  }

  return sum;
}`}
  annotations={[
    { line: 2, text: "sum: аккумулятор — собирает текущую сумму" },
    { line: 3, text: "i: индекс в nums — продвигается на каждой итерации" },
    { line: 5, text: "тест while: CMP i, nums.length → условный переход выходит при i >= length" },
    { line: 6, text: "тест if: CMP nums[i], 0 → условный переход пропускает sum+= когда nums[i] <= 0" },
    { line: 7, text: "sum += nums[i]: добавляет текущий элемент только когда условие if истинно" },
    { line: 9, text: "i++: безусловный; выполняется даже когда if был пропущен" },
    { line: 10, text: "закрывающая скобка: безусловный обратный переход к тесту while на строке 5" },
  ]}
  caption="sumPositives: цикл while с внутренним условием. Вход: [3, -1, 4]. Ожидаемый результат: 7."
/>
</Code>

<Trace lang="ru">
Пройди `sumPositives([3, -1, 4])` кадр за кадром. Каждая ячейка показывает текущее
значение одной переменной. Активная строка подсвечена в панели кода выше.

<AlgoTrace
  id="trace-sum-positives"
  lang="ru"
  code={[
    "function sumPositives(nums: number[]): number {",
    "  let sum = 0;",
    "  let i = 0;",
    "",
    "  while (i < nums.length) {",
    "    if (nums[i] > 0) {",
    "    sum += nums[i];",
    "    }",
    "    i++;",
    "  }",
    "",
    "  return sum;",
    "}"
  ]}
  frames={[
    {
      caption: "Начало: функция вызвана с nums=[3,-1,4]. Локальных переменных ещё нет.",
      line: 1,
      cells: []
    },
    {
      caption: "Строка 2: sum = 0. Аккумулятор инициализирован нулём.",
      line: 2,
      cells: [
        { value: "sum=0", mark: "active" }
      ]
    },
    {
      caption: "Строка 3: i = 0. Счётчик цикла инициализирован нулём.",
      line: 3,
      cells: [
        { value: "sum=0", mark: "done" },
        { value: "i=0", mark: "active" }
      ]
    },
    {
      caption: "Строка 5 (тест while, итер 1): i=0 < nums.length=3 → true. Fall-through в тело.",
      line: 5,
      cells: [
        { value: "sum=0", mark: "done" },
        { value: "i=0", mark: "active" },
        { value: "nums[0]=3", mark: "target" }
      ]
    },
    {
      caption: "Строка 6 (тест if, итер 1): nums[0]=3 > 0 → true. Fall-through к sum+=.",
      line: 6,
      cells: [
        { value: "sum=0", mark: "done" },
        { value: "i=0", mark: "done" },
        { value: "nums[0]=3", mark: "active" }
      ]
    },
    {
      caption: "Строка 7: sum += nums[0] = 0 + 3 = 3. sum теперь равен 3.",
      line: 7,
      cells: [
        { value: "sum=3", mark: "active" },
        { value: "i=0", mark: "done" },
        { value: "nums[0]=3", mark: "done" }
      ]
    },
    {
      caption: "Строка 9: i++ → i = 1. Инкремент выполняется безусловно.",
      line: 9,
      cells: [
        { value: "sum=3", mark: "done" },
        { value: "i=1", mark: "active" }
      ]
    },
    {
      caption: "Строка 5 (тест while, итер 2): i=1 < nums.length=3 → true. Fall-through в тело.",
      line: 5,
      cells: [
        { value: "sum=3", mark: "done" },
        { value: "i=1", mark: "active" },
        { value: "nums[1]=-1", mark: "target" }
      ]
    },
    {
      caption: "Строка 6 (тест if, итер 2): nums[1]=-1 > 0 → false. Условный переход — пропустить sum+=.",
      line: 6,
      cells: [
        { value: "sum=3", mark: "done" },
        { value: "i=1", mark: "done" },
        { value: "nums[1]=-1", mark: "active" }
      ]
    },
    {
      caption: "Строка 9: i++ → i = 2. Тело if было пропущено; sum остаётся 3.",
      line: 9,
      cells: [
        { value: "sum=3", mark: "done" },
        { value: "i=2", mark: "active" }
      ]
    },
    {
      caption: "Строка 5 (тест while, итер 3): i=2 < nums.length=3 → true. Fall-through в тело.",
      line: 5,
      cells: [
        { value: "sum=3", mark: "done" },
        { value: "i=2", mark: "active" },
        { value: "nums[2]=4", mark: "target" }
      ]
    },
    {
      caption: "Строка 6 (тест if, итер 3): nums[2]=4 > 0 → true. Fall-through к sum+=.",
      line: 6,
      cells: [
        { value: "sum=3", mark: "done" },
        { value: "i=2", mark: "done" },
        { value: "nums[2]=4", mark: "active" }
      ]
    },
    {
      caption: "Строка 7: sum += nums[2] = 3 + 4 = 7. sum теперь равен 7.",
      line: 7,
      cells: [
        { value: "sum=7", mark: "active" },
        { value: "i=2", mark: "done" },
        { value: "nums[2]=4", mark: "done" }
      ]
    },
    {
      caption: "Строка 9: i++ → i = 3.",
      line: 9,
      cells: [
        { value: "sum=7", mark: "done" },
        { value: "i=3", mark: "active" }
      ]
    },
    {
      caption: "Строка 5 (тест while, выход): i=3 < nums.length=3 → false. Условный переход выходит из цикла.",
      line: 5,
      cells: [
        { value: "sum=7", mark: "done" },
        { value: "i=3", mark: "active" }
      ]
    },
    {
      caption: "Строка 12: return sum = 7. Функция возвращает 7. Только 3 и 4 были добавлены; -1 был пропущен.",
      line: 12,
      cells: [
        { value: "sum=7", mark: "target" },
        { value: "i=3", mark: "done" }
      ]
    }
  ]}
/>
</Trace>

<Inset kind="edgecase" lang="ru">
**Что если массив пустой?** Если `nums = []`, то `nums.length = 0`, и тест while при
самой первой проверке вычисляет `i=0 < 0` → false. Тело цикла не выполняется ни разу.
Функция сразу возвращает `sum = 0`. Это корректный результат для пустых входных данных
и является естественным следствием проверки условия перед телом — тот же аппаратный
механизм, который делает тест `while` в начале.
</Inset>

<PracticeSet
  id="tracing-a-program-practice"
  lessonSlug="04-tracing-a-program"
  lang="ru"
  problems={[
    {
      prompt: "После первой итерации sumPositives([3,-1,4]) переменная i была проинкрементирована с 0. Какое значение имеет i в начале второго теста while?",
      answer: 1,
      hint: "i начинается с 0. i++ выполняется в конце первой итерации: i = 0 + 1 = 1.",
    },
    {
      prompt: "Во время итерации 2 (nums[1] = -1) условие if nums[1] > 0 ложно. Какое значение имеет sum после выполнения i++ в конце этой итерации?",
      answer: 3,
      hint: "Тело if пропускается когда nums[i] <= 0. sum не изменяется во время итерации 2. sum остаётся равным 3.",
    },
    {
      prompt: "Сколько раз выполняется строка 'sum += nums[i]' при вызове с [3,-1,4]?",
      answer: 2,
      hint: "sum+= выполняется только когда nums[i] > 0. nums[0]=3 (да) и nums[2]=4 (да) подходят; nums[1]=-1 — нет. Так что выполняется 2 раза.",
    },
    {
      prompt: "При каком значении i цикл while выходит (т. е. когда условие while становится ложным)?",
      answer: 3,
      hint: "nums.length равен 3. Условие while i < 3 становится ложным когда i = 3. В этот момент обратный переход не срабатывает и выполнение переходит за пределы цикла.",
    },
    {
      prompt: "sumPositives([2, -5, -3, 6]) возвращает какое значение?",
      answer: 8,
      hint: "Положительные значения — 2 и 6. 2 + 6 = 8. Значения -5 и -3 пропускаются if-ом.",
    },
  ]}
/>

<Check lang="ru">
  <Quiz
    id="tracing-a-program-check"
    pieceSlug="04-tracing-a-program"
    lang="ru"
    question="В sumPositives: что происходит когда nums[i] отрицательный или нулевой?"
    choices={[
      {
        label: "Условие if ложно; условный переход пропускает 'sum += nums[i]'; i++ всё равно выполняется; затем обратный переход возвращается к тесту while",
        correct: true,
      },
      {
        label: "Цикл немедленно завершается, когда обнаруживается неположительное значение",
        misconception: "Цикл не выходит при обнаружении отрицательного значения. Условие while проверяет i < nums.length, а не знак текущего элемента. Отрицательный элемент вызывает пропуск тела if, но цикл продолжается: i++ выполняется, срабатывает обратный переход и условие while проверяется снова.",
      },
      {
        label: "i++ пропускается когда условие if ложно, поэтому цикл повторно проверяет тот же элемент",
        misconception: "i++ находится за пределами блока if и всегда выполняется, независимо от того, было ли условие if истинным или ложным. Посмотри на отступы в коде: i++ находится на том же уровне, что и оператор if, а не внутри него. Пропуск i++ вызвал бы бесконечный цикл для отрицательных значений.",
      },
    ]}
  />
</Check>

<Recap lang="ru">
Трассировка программы шаг за шагом раскрывает конкретный механизм за высокоуровневым
синтаксисом. В `sumPositives` цикл `while` — это структура с тестом в начале и обратным
переходом: на каждой итерации условие `i < nums.length` вычисляется (сравнение обновляет
флаг Z или N), условный переход либо выходит из цикла, либо проваливается в тело, а
безусловный обратный переход в конце сбрасывает счётчик команд обратно к тесту.
Внутренний `if` — это второй CMP + условный переход: когда `nums[i] > 0` ложно, тело
`sum += nums[i]` пропускается условным прямым переходом; когда истинно — fall-through.
Инкремент `i++` безусловен — он стоит за пределами блока if и выполняется в любом случае.
Цикл выходит, когда `i` достигает `nums.length`: в этот момент условие while ложно,
условный переход в начале срабатывает (прямой переход за цикл), и счётчик команд
перемещается к `return sum`. Итоговое значение `sum` — сумма всех положительных
элементов, встреченных в ходе итерации.
</Recap>
