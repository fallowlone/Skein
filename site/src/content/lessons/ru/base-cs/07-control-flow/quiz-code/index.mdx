---
concepts:
- loop-trace
- off-by-one
- short-circuit
- branch-prediction-by-hand
- fall-through
estMin: 14
lang: ru
level: middle
order: 9
prereqs: []
slug: quiz-code
sources:
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/while
- https://en.wikipedia.org/wiki/Control_flow
- https://www.cs.cmu.edu/afs/cs/academic/class/15213-f15/www/lectures/05-machine-basics.pdf
status: ready
summary: "Прочитайте четыре маленькие программы и протрассируйте их вручную: предскажите число итераций loop и вывод, найдите off-by-one на границе и разберите short-circuit как цепочку conditional jump."
title: 'Control flow: трассировка кода'
track: base-cs
unit: 07-control-flow
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Единственный надёжный способ узнать, что делает branch или loop, — протрассировать его так, как это делает CPU: следить за program counter и переменными, по одному шагу. Прочитайте каждый сниппет, предскажите результат вручную, затем проверьте себя.
</Hook>

<Goal lang="ru">
Отработайте главный навык unit: проходить conditional и loop вручную, точно считать итерации, находить off-by-one, переворачивающий границу, и рассуждать о short-circuit как о цепочке conditional jump, в которую он компилируется.
</Goal>

## Сниппет 1 — посчитайте итерации

```ts
let count = 0;
let i = 1;
while (i <= 5) {   // обратите внимание: <= , не <
  count++;
  i++;
}
// чему равно count?
```

<Quiz
  id="07-control-flow-code-1"
  lessonSlug="07-control-flow"
  lang="ru"
  question="Каково итоговое значение count?"
  choices={[
    { label: "5 — тело исполняется для i = 1, 2, 3, 4, 5; на шестой проверке i = 6 не проходит i <= 5 и loop выходит.", correct: true },
    { label: "4 — loop идёт от 1 до 5, но не включая 5.", misconception: "Проверка — это i <= 5, что включает 5, поэтому тело исполняется для i = 1..5 (пять раз). Четыре раза было бы только если бы проверка была i < 5." },
    { label: "6 — тело исполняется ещё и для i = 6.", misconception: "Когда i становится 6, while-проверка i <= 5 ложна, поэтому тело для 6 не исполняется. Backward jump не берётся, и loop выходит, не увеличив count снова." },
    { label: "Бесконечно — i никогда не перестаёт расти.", misconception: "i++ выполняется каждую итерацию, поэтому сравниваемое значение меняется и проверка в итоге проваливается при i = 6. Loop завершается." },
  ]}
/>

## Сниппет 2 — граница off-by-one

```ts
const a = [10, 20, 30];   // длина 3, валидные индексы 0,1,2
let i = 0;
while (i <= a.length) {   // <= length, не < length
  console.log(a[i]);
  i++;
}
```

<Quiz
  id="07-control-flow-code-2"
  lessonSlug="07-control-flow"
  lang="ru"
  question="Сколько раз исполняется тело loop и в чём баг?"
  choices={[
    { label: "Четыре раза (i = 0,1,2,3). Проверка i <= a.length позволяет i дойти до 3, но индекс 3 за последним валидным индексом 2, поэтому a[3] — undefined. Классический off-by-one. Исправление: i < a.length.", correct: true },
    { label: "Три раза, по одному на элемент — бага нет.", misconception: "i <= a.length истинно для i = 0,1,2 и ещё для i = 3 (ведь 3 <= 3). Тело исполняется четыре раза и читает a[3], которого не существует." },
    { label: "Loop работает вечно, потому что i никогда не достигает a.length.", misconception: "i++ выполняется каждый проход, поэтому i доходит до 3, затем до 4; при i = 4 проверка 4 <= 3 ложна и loop выходит. Не бесконечный — просто на один проход больше." },
    { label: "Ноль раз, потому что a.length — не валидный индекс.", misconception: "Проверка сравнивает i с a.length, а не индексирует им. Первая проверка — 0 <= 3 (истина), поэтому тело исполняется; ошибка границы в конце, а не в начале." },
  ]}
/>

## Сниппет 3 — short-circuit как цепочка jump

```ts
function check(user) {
  // && останавливается на первом ложном операнде
  if (user && user.active && user.age >= 18) {
    return "allowed";
  }
  return "denied";
}
check(null);   // user равен null
```

<Quiz
  id="07-control-flow-code-3"
  lessonSlug="07-control-flow"
  lang="ru"
  question="check(null) возвращает 'denied'. Почему user.active никогда не вычисляется, в терминах машинного уровня?"
  choices={[
    { label: "&& компилируется в цепочку conditional jump: первый операнд (user) проверяется, и поскольку он ложный, conditional jump идёт прямо на путь denied, пропуская инструкции, которые читали бы user.active. Последующие операнды не достигаются.", correct: true },
    { label: "Все три операнда сначала вычисляются, затем объединяются; user.active просто вернёт false.", misconception: "&& делает short-circuit: останавливается на первом ложном операнде. Чтение user.active у null-user на самом деле бросило бы ошибку — short-circuit именно это и предотвращает, перепрыгивая эти чтения." },
    { label: "CPU вычисляет справа налево, поэтому user.age проверяется первым и проваливается.", misconception: "Операнды вычисляются слева направо. user проверяется первым; будучи null (ложным), conditional jump пропускает остальное до того, как user.active или user.age вообще прочитаны." },
    { label: "user.active вычисляется, но его результат отбрасывается.", misconception: "Он никогда не вычисляется. Conditional jump после проверки user полностью переносит управление за эти чтения, поэтому на null-user ошибка не бросается." },
  ]}
/>

## Сниппет 4 — вложенный loop и conditional

```ts
let sum = 0;
for (let i = 0; i < 3; i++) {     // внешний: i = 0,1,2
  for (let j = 0; j < 3; j++) {   // внутренний: j = 0,1,2
    if (i === j) {                // только диагональ
      sum += 1;
    }
  }
}
// чему равно sum?
```

<Quiz
  id="07-control-flow-code-4"
  lessonSlug="07-control-flow"
  lang="ru"
  question="Каково итоговое значение sum?"
  choices={[
    { label: "3 — внутренний if срабатывает только при i === j, что бывает раз на каждый внешний проход: (0,0), (1,1), (2,2). Остальные шесть пар (i, j) берут conditional jump, пропускающий sum += 1.", correct: true },
    { label: "9 — sum растёт на каждой внутренней итерации.", misconception: "Внутреннее тело исполняется 9 раз, но sum += 1 защищён if (i === j). Только 3 диагональные пары проходят проверку; остальные 6 берут jump пропуска." },
    { label: "6 — считается каждая пара, кроме диагонали.", misconception: "Всё наоборот: sum увеличивают только диагональные пары (где i === j). 6 внедиагональных пар пропускаются conditional jump." },
    { label: "1 — i === j истинно лишь однажды за весь прогон.", misconception: "i === j истинно раз на каждую внешнюю итерацию, а их 3, поэтому это истинно 3 раза: (0,0), (1,1), (2,2)." },
  ]}
/>

<Recap lang="ru">
Трассировка вручную — это весь навык. Число итераций loop — ровно столько, сколько раз его проверка остаётся истинной, а оператор границы (`&lt;` против `&lt;=`) — то место, где живут баги off-by-one: здесь чтение на один элемент за концом массива из 3 элементов. Short-circuit && компилируется в цепочку conditional jump, уходящих на ложный путь в момент, когда операнд становится ложным, — поэтому последующие операнды даже не читаются (и поэтому проверка на null защищает чтения после неё). Вложенные loop перемножают число итераций, но защищающий if всё равно срабатывает только при своём условии. Если сомневаетесь — проводите program counter и переменные по одному шагу.
</Recap>
