---
concepts:
- execution-flow
- program-counter
- conditional-jump
- backward-jump
- fall-through
- infinite-loop
estMin: 13
lang: ru
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://en.wikipedia.org/wiki/Control_flow
- https://www.cs.cmu.edu/afs/cs/academic/class/15213-f15/www/lectures/05-machine-basics.pdf
- https://www.nand2tetris.org/
status: ready
summary: "Вопросы на свободное припоминание по всему unit control flow. Сначала отвечайте по памяти, затем открывайте модельный ответ и сравнивайте."
title: 'Control flow: тест на свободное припоминание'
track: base-cs
unit: 07-control-flow
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый вопрос произнесите или запишите полный ответ по памяти, прежде чем открыть модельный, — именно усилие вытащить его и закрепляет механизм.
</Hook>

<Goal lang="ru">
Восстановите хребет unit по памяти: что такое execution flow, как program counter его порождает, как сравнение плюс conditional jump делают branch, как backward jump делает loop и почему loop работает вечно.
</Goal>

<RetrievalDrawer
  client:load
  id="07-control-flow-short-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое execution flow и что единственное его определяет?", a: "Execution flow — это последовательность, в которой CPU реально исполняет инструкции; не порядок, в котором байты лежат в памяти, а порядок их выборки и исполнения. Единственное, что его определяет, — значение, которое program counter (PC) держит при каждой выборке: какой адрес в PC, ту инструкцию CPU и выполнит следующей. Байты фиксированы и неподвижны; flow динамичен. Те же байты могут давать разный flow в зависимости от того, какие jump взяты, потому что каждый jump переписывает PC." },
    { q: "Почему sequential (straight-line) исполнение — это default и как PC его порождает?", a: "После любой инструкции, не являющейся jump, PC обновляется прибавлением размера текущей инструкции (например +4 на 4-байтовой архитектуре), поэтому он переходит к следующей инструкции в памяти. Для этого нужен лишь сумматор — простейшая схема, — поэтому «перейти к следующей инструкции» и есть особый-default-ничего, эмерджентно возникающий из железа. CPU тем самым проходит память сверху вниз, исполняя каждую инструкцию один раз по порядку, пока инструкция не перезапишет PC другим адресом. Sequential исполнение — это базис; любой branch и loop — отклонение от него." },
    { q: "Объясните, как if становится сравнением плюс conditional jump на машинном уровне.", a: "У CPU нет схемы, понимающей '>' как идею. Вместо этого инструкция сравнения (CMP) вычитает один операнд из другого, отбрасывает числовой результат и записывает отношение в виде отдельных битов в flags register — важнее всего флаг Z (zero, выставляется при равенстве значений) и флаг N (negative, выставляется при отрицательном результате). Затем conditional jump (например JZ, JNE, JL) читает один из этих битов: если условие выполнено, он прыгает на адрес-цель; если нет — делает fall-through. Так что if — это ровно: выставить флаг, затем повести PC по этому одному биту." },
    { q: "Что значит fall-through и зачем if/else нужен безусловный jump после блока if-true?", a: "Fall-through значит, что conditional jump ничего не сделал с PC, поэтому PC нормально переходит на непосредственно следующую инструкцию — это отсутствие jump, а не отдельная операция. В if/else блок if-true стоит сразу после conditional jump, а блок else — после него. Когда блок if-true заканчивается, PC продолжил бы идти прямо в блок else, выполнив оба. Поэтому компилятор ставит безусловный JUMP в конце блока if-true, перепрыгивающий блок else к точке слияния. Блок else достигается только когда условие было ложным и conditional jump взял ветвь в него." },
    { q: "Что делает loop и почему while и for для CPU — одно и то же?", a: "Loop — это backward jump: jump, чья цель-адрес раньше в памяти, чем сам jump, поэтому CPU заново выбирает и выполняет уже исполненные инструкции. В паре с conditional-тестом наверху он становится управляемым повторением: проверить условие, при истине упасть в тело, выполнить тело, затем безусловно прыгнуть назад к тесту. While loop — это ровно оно. For loop добавляет блок init перед верхом loop и блок update прямо перед backward jump, но шаблон «тест — тело — backjump» идентичен, поэтому скомпилированный код для CPU неотличим. Специальной инструкции loop нет — только jump и проверка флага." },
    { q: "Почему возникает infinite loop и может ли CPU его обнаружить?", a: "Loop повторяется, пока тест перед его backward jump остаётся истинным. Он не заканчивается в двух случаях: backward jump безусловен (while(true) без теста, частый в намеренных server/event loop) или тест есть, но тело никогда не меняет сравниваемое значение, поэтому флаг не переключается (часто баг, как забытое уменьшение счётчика). CPU не может обнаружить ни один случай: каждый проход он просто заново проверяет условие и делает одно решение jump/fall-through, без памяти о числе проходов. Он гоняет backward jump бесконечно, пока ОС или прерывание не остановят. Infinite loop — это машина, честно подчиняющаяся программе, а не аппаратный сбой." },
  ]}
/>

<Recap lang="ru">
Если вы смогли восстановить каждый ответ по памяти, у вас есть хребет unit: execution flow — это порядок реального исполнения инструкций, целиком задаваемый program counter; PC по умолчанию продвигается вперёд, давая straight-line код; сравнение выставляет флаг, а conditional jump читает его, чтобы сделать branch; if/else — это два адреса назначения для PC с jump-пропуском на границе; backward jump плюс тест наверху loop делают loop, одинаковый для while и for; а loop работает вечно, когда проверяемое значение никогда не меняется — чего CPU не может ни сосчитать, ни обнаружить.
</Recap>
