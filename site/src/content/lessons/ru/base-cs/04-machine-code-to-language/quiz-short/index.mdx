---
concepts:
- one-to-one-translation
- abstraction
- compilation-vs-interpretation
- jit
- runtime-system
- source-to-running-program
estMin: 13
lang: ru
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://en.wikipedia.org/wiki/Assembly_language
- https://en.wikipedia.org/wiki/Compiler
- https://en.wikipedia.org/wiki/Runtime_system
status: ready
summary: Промпты на свободное припоминание по юниту. Сначала ответь своими словами, затем раскрой модельный ответ и сравни.
title: 'От машинного кода к языку: свободное припоминание'
track: base-cs
unit: 04-machine-code-to-language
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт проговори или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет материал.
</Hook>

<Goal lang="ru">
Восстанови костяк юнита, не подглядывая: правило один-к-одному у assembler, зачем нужны высокоуровневые языки, compile vs interpret vs JIT, что даёт runtime и полный путь от исходника до исполнения.
</Goal>

<RetrievalDrawer
  client:load
  id="04-machine-code-to-language-short-retrieval"
  lang="ru"
  questions={[
    { q: "Что делает assembler и в чём правило один-к-одному?", a: "Assembler — это программа, которая переводит текст исходника assembly в двоичный machine code, исполняемый CPU. Для каждой строки она ищет mnemonic (LOAD, ADD, JUMP) в таблице, чтобы получить биты опкода, переводит имена регистров и числовые константы в их битовые формы, разрешает метки в числовые адреса через таблицу символов и пишет байты в выходной файл. Правило один-к-одному — определяющее свойство assembly: каждая инструкция assembly отображается ровно в одну машинную инструкцию и наоборот — без сворачивания нескольких инструкций в одну и без разворачивания одной во многие. Именно это отличает assembly от любого более высокоуровневого языка, где один оператор может стать многими инструкциями." },
    { q: "Assembly уже даёт machine code читаемые имена. Зачем поверх него изобретать высокоуровневые языки?", a: "Assembly исправляет читаемость, но оставляет две проблемы. Во-первых, он специфичен для CPU: mnemonic'и x86-64 называют опкоды x86-64, так что программу надо переписывать под каждое новое семейство CPU. Во-вторых, он многословен — каждая операция CPU это отдельная строка, поэтому одно математическое выражение может занять много инструкций. Высокоуровневые языки решают обе: один оператор может обозначать много машинных инструкций (продуктивность — IBM сообщала о выигрыше примерно 20 к 1 над assembly с FORTRAN), и один исходник можно транслировать разными compiler или interpreter под каждый CPU (переносимость). В основе лежит абстракция: спрятать регистры, опкоды и адреса, чтобы выражать намерение, а не механизм." },
    { q: "Сравни compiler и interpreter по тому, что они производят и когда транслируют.", a: "Compiler переводит всю программу-исходник в machine code заранее, до запуска, производя самостоятельный платформенно-зависимый бинарник. Во время работы CPU исполняет этот бинарник напрямую без накладных расходов на трансляцию, поэтому скомпилированный код обычно быстрее, но его надо перекомпилировать под каждый CPU. Interpreter читает исходник во время работы и исполняет каждый оператор по мере встречи — прочитать, понять смысл, исполнить, перейти дальше — и никогда не производит machine code бинарник для твоей программы. Интерпретируемый код переносимее (один исходник работает везде, где есть interpreter) и не требует шага компиляции, но медленнее, потому что накладные расходы на трансляцию платятся на каждом операторе. Сам interpreter — это скомпилированный бинарник, исполняющий свой machine code в ответ на твои операторы." },
    { q: "Что такое JIT-компиляция и какую проблему отражает период прогрева?", a: "Just-in-time компиляция — гибрид, используемый V8, JVM и PyPy. Программа стартует интерпретируемой (или исполняя bytecode через быструю VM); runtime следит, какие функции исполняются чаще всего — hot spot'ы — и компилирует именно их в нативный machine code, пока программа уже работает. Получаешь быстрый старт и переносимость interpreter плюс скорость, близкую к скомпилированной, на важных путях, причём JIT может использовать сведения, известные лишь во время работы (например реальные типы), чтобы оптимизировать лучше, чем compiler заранее. Период прогрева — это цена: первые вызовы функции интерпретируются (медленно), пока она не отработает достаточно раз, чтобы быть скомпилированной, поэтому пиковая производительность наступает только после прогрева — поэтому сервис на Node разгоняется до полной скорости." },
    { q: "Что такое runtime и какие четыре сервиса он обычно предоставляет?", a: "Runtime (runtime system) — это поддерживающий код и машинерия, которые реализация языка поставляет и запускает рядом с твоей программой, наводя мост между общими сервисами ОС и семантикой языка. Он предоставляет: (1) управление памятью — heap для новых объектов и, в большинстве современных языков, garbage collector, который автоматически освобождает недостижимые объекты, чтобы ты не вызывал malloc/free; (2) машинерию call stack — кадры стека, адреса возврата и соглашения о вызовах, заставляющие вызовы и возвраты функций работать, исчерпание которых это stack overflow; (3) стандартную библиотеку — встроенные функции и структуры данных (I/O, коллекции, математика, строки), чей код живёт в runtime, а не в твоём файле; и (4) interpreter или virtual machine для интерпретируемых или bytecode-языков (CPython, JVM, V8). Даже у скомпилированного C есть тонкий runtime: libc, стартовый код, настраивающий стек и вызывающий main, и очистка при выходе. Runtime — это не ОС: у двух языков на одной ОС совершенно разные runtime." },
    { q: "Проследи шесть стадий от текста исходника до исполнения программы на CPU.", a: "(1) Исходный текст — ты пишешь читаемый человеком код, который CPU не может исполнить. (2) Трансляция — compiler или assembler переводит его в machine code в объектных файлах, либо для интерпретируемого языка interpreter обрабатывает его во время работы. (3) Линковка — linker объединяет объектные файлы и архивы библиотек в один исполняемый файл, разрешая внешние ссылки на символы (например находит printf в libc и патчит место вызова); интерпретируемые языки этот шаг пропускают. (4) Загрузка — loader ОС читает исполняемый файл с диска, создаёт процесс с собственным адресным пространством, копирует байты machine code в память, отображает разделяемые библиотеки, настраивает call stack и направляет program counter на первую инструкцию. (5) Старт runtime — runtime языка инициализируется (настройка стека, GC, VM, стандартная библиотека) и вызывает точку входа. (6) Fetch-decode-execute — CPU крутит цикл: достать инструкцию по program counter, декодировать опкод и операнды, исполнить, продвинуть program counter, повторить. К этому моменту текст исходника исчез; CPU исполняет битовые паттерны и не знает, с какого языка они пришли." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь костяк юнита: assembler переводит assembly один-к-одному в machine code; высокоуровневые языки добавляют продуктивность и переносимость через абстракцию; compiler транслирует всё заранее, а interpreter — оператор за оператором во время работы, JIT же компилирует hot spot'ы на лету после прогрева; runtime даёт управление памятью, call stack, стандартную библиотеку и VM под твоим кодом; а шестистадийный конвейер доносит каждую программу от текста исходника до цикла CPU fetch-decode-execute.
</Recap>
