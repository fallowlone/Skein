---
concepts:
- one-to-one-translation
- many-to-one-expansion
- compilation-vs-interpretation
- bytecode
- jit
estMin: 14
lang: ru
level: middle
order: 9
prereqs: []
slug: quiz-code
sources:
- https://en.wikipedia.org/wiki/Assembly_language
- https://developer.mozilla.org/en-US/docs/Glossary/Just_In_Time_Compilation
status: ready
summary: Читай сниппеты assembly и высокоуровневого кода, прослеживай отображение исходник → machine code и отличай compilation от interpretation и JIT по тому, что и когда производит каждый.
title: 'От машинного кода к языку: чтение кода и отображений'
track: base-cs
unit: 04-machine-code-to-language
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Яснее всего различить слои — посмотреть на реальный код и спросить: сколько машинных инструкций из этого получится и когда происходит трансляция? Прочитай каждый сниппет и выбери ответ, который дал бы внимательный инженер.
</Hook>

<Goal lang="ru">
Потренируй чтение отображения между слоями: assembly в machine code (один-к-одному), высокоуровневый оператор во много инструкций и разницу, которую вносят compile, interpret и JIT в то, что и когда производится.
</Goal>

## Сниппет 1 — assembly в machine code

```
        LOAD  R0, 200    ; опкод 00, R0=0, операнд 200
        LOAD  R1, 201    ; опкод 00, R1=1, операнд 201
        ADD   R0, R1     ; опкод 10
        STORE 202, R0    ; опкод 01, операнд 202
```

<Quiz
  id="04-machine-code-to-language-code-1"
  lessonSlug="04-machine-code-to-language"
  lang="ru"
  question="На игрушечном CPU каждая инструкция занимает 2 байта. Сколько машинных инструкций и сколько байт assembler выпустит для этих четырёх строк и почему?"
  choices={[
    { label: "Четыре машинные инструкции, 8 байт. Assembly — один-к-одному: каждая строка-mnemonic становится ровно одной машинной инструкцией, поэтому четыре строки дают четыре инструкции по два байта.", correct: true },
    { label: "Больше четырёх инструкций, потому что ADD должен ещё перезагрузить свои операнды.", misconception: "Assembler не добавляет скрытых инструкций. Один-к-одному означает, что каждая строка — ровно одна машинная инструкция; строка ADD — это одна инструкция ADD, а операнды уже в R0 и R1 после LOAD'ов." },
    { label: "Меньше четырёх, потому что assembler оптимизирует LOAD'ы вместе.", misconception: "Assembler не оптимизирует и не сворачивает инструкции — это работа compiler. Assembler подставляет mnemonic в опкод один-к-одному, поэтому четыре строки остаются четырьмя инструкциями." },
    { label: "Четыре инструкции, но всего 4 байта, потому что mnemonic'и короче machine code.", misconception: "Число байт задаётся кодировкой инструкции, а не длиной текста mnemonic. Каждая инструкция здесь 2 байта, поэтому четыре инструкции — 8 байт." },
  ]}
/>

## Сниппет 2 — один высокоуровневый оператор

```ts
const total = price * quantity + shipping;
```

<Quiz
  id="04-machine-code-to-language-code-2"
  lessonSlug="04-machine-code-to-language"
  lang="ru"
  question="Три переменные уже в памяти. Примерно во сколько машинных инструкций превращается эта одна строка TypeScript и кто решает, в какие?"
  choices={[
    { label: "В несколько — загрузить price, загрузить quantity, умножить, загрузить shipping, сложить, сохранить результат — и compiler решает, какие инструкции, регистры и адреса использовать. Один высокоуровневый оператор отображается во много машинных инструкций; это расширение многие-к-одному и есть выигрыш в продуктивности.", correct: true },
    { label: "Ровно в одну, потому что это одна строка исходника.", misconception: "Строки исходника — это не машинные инструкции. Высокоуровневые операторы — многие-к-одному: это выражение становится загрузками, умножением, сложением и сохранением. Только assembly — один-к-одному." },
    { label: "В несколько, но программист должен выбрать регистры и адреса.", misconception: "Программист не выбирал ни одного из них — в этом и абстракция. Compiler выбирает инструкции, распределяет регистры и разрешает адреса; ты написал лишь намерение." },
    { label: "Ни в одну — CPU вычисляет всё выражение за один шаг.", misconception: "У CPU нет инструкции для произвольного выражения. Он исполняет отдельные загрузки, умножение, сложение и сохранение, которые выпустил compiler." },
  ]}
/>

## Сниппет 3 — три способа запустить одну программу

```
A)  gcc add.c -o add   &&   ./add          # создаёт нативный бинарник, затем запускает
B)  python3 add.py                          # interpreter читает и исполняет исходник
C)  node add.js                             # сперва интерпретация, затем JIT горячих функций
```

<Quiz
  id="04-machine-code-to-language-code-3"
  lessonSlug="04-machine-code-to-language"
  lang="ru"
  question="Какое утверждение верно различает A, B и C по тому, что производится и когда?"
  choices={[
    { label: "A заранее компилирует всю программу в нативный machine code бинарник, затем запускает его без пооператорной трансляции. B никогда не производит machine code бинарник для твоей программы — interpreter исполняет каждый оператор во время работы. C начинает с интерпретации и JIT-компилирует горячие функции в нативный код прямо во время работы.", correct: true },
    { label: "Все три производят нативный бинарник на диске перед запуском.", misconception: "Только A пишет нативный бинарник заранее. Interpreter в B не производит machine code бинарник для твоей программы; C производит нативный код лишь во время работы, в памяти, для горячих функций." },
    { label: "B и C идентичны — обе просто интерпретируют исходник.", misconception: "C — не чистый interpreter. Он сперва интерпретирует, затем JIT-компилирует часто исполняемые функции в нативный machine code во время работы, достигая скорости, близкой к скомпилированной, после прогрева." },
    { label: "A самый медленный, потому что должен компилировать каждый раз при запуске.", misconception: "A компилирует один раз; ./add затем запускает готовый бинарник без накладных расходов на трансляцию. Это интерпретируемый путь B платит за трансляцию при каждом запуске и на каждом операторе." },
  ]}
/>

## Сниппет 4 — bytecode посередине

```
javac App.java     ->  App.class   (bytecode, не нативный machine code)
java App           ->  JVM загружает App.class и исполняет bytecode
```

<Quiz
  id="04-machine-code-to-language-code-4"
  lessonSlug="04-machine-code-to-language"
  lang="ru"
  question="Что такое App.class и зачем доставлять bytecode вместо текста исходника или нативного бинарника?"
  choices={[
    { label: "Bytecode — компактный переносимый промежуточный формат для JVM, а не нативный machine code. Он разделяет переносимость (один и тот же .class запускается на любой машине с JVM) и производительность (JVM интерпретирует его или JIT-компилирует в нативный код во время работы), так что он и переносим, и быстро стартует.", correct: true },
    { label: "Нативный machine code x86-64, готовый к прямому исполнению CPU.", misconception: "Bytecode — не нативный machine code, CPU не может исполнить его напрямую. JVM читает bytecode и интерпретирует или JIT-компилирует его в нативный код. Эта косвенность и делает один .class переносимым между CPU." },
    { label: "Текст исходника Java в сжатом файле.", misconception: "Это не текст исходника; javac уже перевёл исходник в bytecode — компактный двоичный формат для JVM. Исходный текст Java — не то, что загружает JVM." },
    { label: "Бинарник, который запустится, только если пользователь установит тот же CPU, на котором ты компилировал.", misconception: "Наоборот — bytecode не зависит от CPU. Любая машина с JVM запустит один и тот же .class; JVM наводит мост к нативному коду этой машины." },
  ]}
/>

<Recap lang="ru">
Чтение слоёв сводится к двум вопросам. Сколько машинных инструкций? Assembly — один-к-одному; высокоуровневый оператор — многие-к-одному, и инструкции выбирает compiler. Когда происходит трансляция и что производится? Compiler выпускает нативный бинарник заранее; чистый interpreter не выпускает ничего для твоей программы и транслирует каждый оператор во время работы; JIT сперва интерпретирует и компилирует горячие функции в нативный код на лету; а bytecode — переносимый промежуточный слой, который VM интерпретирует или JIT-компилирует. Сопоставь сниппет с механизмом — и остальное следует само.
</Recap>
