---
concepts:
- one-to-one-translation
- abstraction
- compilation-vs-interpretation
- jit
- runtime-system
- source-to-running-program
estMin: 13
lang: ru
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://en.wikipedia.org/wiki/Assembly_language
- https://developer.mozilla.org/en-US/docs/Glossary/Just_In_Time_Compilation
- https://en.wikipedia.org/wiki/Runtime_system
status: ready
summary: "Синтез с множественным выбором по всему юниту: правило один-к-одному у assembler, зачем нужны высокоуровневые языки, компромиссы compile vs interpret vs JIT, runtime и полный путь от исходника до исполнения."
title: 'От машинного кода к языку: тест с множественным выбором'
track: base-cs
unit: 04-machine-code-to-language
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, проходящих сквозь весь юнит. Каждый просит связать идеи из разных уроков — правило один-к-одному у assembler, что дают высокоуровневые языки, чем различаются стратегии трансляции и что делает runtime — а не воспроизвести одно определение.
</Hook>

<Goal lang="ru">
Убедись, что можешь соединить ступени лестницы: сырой machine code, assembly, высокоуровневые языки, стратегии трансляции между ними и runtime с конвейером, доносящие твой код до CPU.
</Goal>

<Quiz
  id="04-machine-code-to-language-choice-1"
  lessonSlug="04-machine-code-to-language"
  lang="ru"
  question="Ты пишешь одну строку assembly, ADD R0 R1, и одну строку TypeScript, total = price * qty + shipping. Сколько машинных инструкций даёт каждая и что это раскрывает?"
  choices={[
    { label: "Строка assembly даёт ровно одну машинную инструкцию (один-к-одному — определяющее свойство assembly); строка TypeScript даёт много (загрузки, умножение, сложение, сохранение). Высокоуровневые языки меняют прозрачность один-к-одному на продуктивность: один оператор обозначает много инструкций.", correct: true },
    { label: "Обе дают по одной машинной инструкции — каждая строка исходника становится одной инструкцией.", misconception: "Только assembly — один-к-одному. Высокоуровневый оператор обычно разворачивается в несколько машинных инструкций; их выбирает compiler. Это расширение многие-к-одному и есть весь аргумент продуктивности высокоуровневых языков." },
    { label: "Обе дают много инструкций; assembly просто прячет остальные.", misconception: "Assembly ничего не прячет — один mnemonic отображается ровно в одну машинную инструкцию. Нет ни сворачивания, ни разворачивания. Это высокоуровневая строка разворачивается во многие." },
    { label: "Строка TypeScript даёт одну инструкцию, потому что CPU понимает всё выражение сразу.", misconception: "CPU никогда не понимает высокоуровневое выражение. Он исполняет лишь отдельные машинные инструкции, которые выпустил compiler; одно выражение становится несколькими из них." },
  ]}
/>

<Quiz
  id="04-machine-code-to-language-choice-2"
  lessonSlug="04-machine-code-to-language"
  lang="ru"
  question="Команда хочет доставить одну и ту же программу на серверы x86-64 Linux и на ноутбуки ARM. Они думают распространять её как написанный вручную assembly. Что пойдёт не так и что это исправит?"
  choices={[
    { label: "Assembly специфичен для CPU: mnemonic'и x86-64 кодируют опкоды x86-64 и не соберутся и не запустятся на ARM. Исправит это высокоуровневый язык — один и тот же исходник транслируется отдельным compiler или interpreter под каждый CPU, так что переносим исходник, хотя каждый бинарник — нет.", correct: true },
    { label: "Ничего — assembly запускается на любом CPU, ведь это читаемый человеком текст.", misconception: "Читаемый текст — это не переносимое исполнение. У каждого семейства CPU свой набор инструкций и кодировки; assembly x86-64 надо переписать на assembly ARM, чтобы запустить на ARM." },
    { label: "Сработает, если сначала скомпилировать assembly в bytecode.", misconception: "Assembly — это уже специфичный для CPU текст машинного уровня; в чистом assembly нет переносимого шага bytecode. Переносимость даёт высокоуровневый язык, чей транслятор нацелен на каждый CPU." },
    { label: "Assembly подойдёт; транслировать надо лишь текст исходника, а не инструкции.", misconception: "Именно инструкции и есть проблема — mnemonic'и assembly называют опкоды одного CPU. Переносимый слой — это высокоуровневый язык, а не assembly." },
  ]}
/>

<Quiz
  id="04-machine-code-to-language-choice-3"
  lessonSlug="04-machine-code-to-language"
  lang="ru"
  question="Пакетная задача с упором в CPU работает часами. Её можно доставить как скомпилированный C или как интерпретируемый скрипт Python. Не считая трудозатрат на разработку, что считает быстрее во время исполнения и почему?"
  choices={[
    { label: "Версия на скомпилированном C. Compiler заранее перевёл и оптимизировал всю программу, поэтому CPU исполняет готовый machine code без пооператорных накладных расходов на трансляцию. Interpreter перечитывает, решает и исполняет каждый оператор во время исполнения, платя эти накладные расходы на каждой операции.", correct: true },
    { label: "Версия на Python, потому что interpreter пропускает медленный шаг компиляции.", misconception: "Пропуск шага компиляции помогает скорости разработки, а не скорости исполнения. Во время работы interpreter платит за трансляцию на каждом операторе, поэтому работа с упором в CPU идёт медленнее заранее скомпилированного machine code." },
    { label: "Они работают с одинаковой скоростью, ведь обе в итоге становятся machine code на одном CPU.", misconception: "Только программа на C переведена заранее в оптимизированный machine code. Чистый interpreter никогда не выпускает machine code для твоей программы — он исполняет каждый оператор своим кодом во время работы, что медленнее для циклов с упором в CPU." },
    { label: "Python, потому что это высокоуровневый язык, а высокоуровневые языки всегда быстрее.", misconception: "Уровень языка не задаёт скорость исполнения; её задаёт стратегия трансляции. Оба высокоуровневые относительно assembly, но заранее скомпилированный C проходит цикл с упором в CPU быстрее, чем interpreter." },
  ]}
/>

<Quiz
  id="04-machine-code-to-language-choice-4"
  lessonSlug="04-machine-code-to-language"
  lang="ru"
  question="Сервис на Node.js первые несколько секунд под нагрузкой медленный, затем ускоряется и остаётся быстрым. Какой механизм это объясняет и баг ли это?"
  choices={[
    { label: "Прогрев JIT. V8 начинает с интерпретации; как только функция вызвана достаточно часто, чтобы стать hot spot, JIT компилирует её в нативный machine code. Пиковая скорость наступает после прогрева. Это ожидаемое поведение, а не баг — JIT меняет короткий прогрев на скорость, близкую к скомпилированной, в установившемся режиме.", correct: true },
    { label: "Утечка памяти, которую garbage collector в конце концов вычищает, возвращая скорость.", misconception: "Утечка со временем замедляет сервис, а не ускоряет. Форма «разогнался и держится» — это кривая прогрева JIT: горячие функции компилируются в нативный код после достаточного числа вызовов." },
    { label: "Interpreter кеширует твой исходный файл на диск после первого запуска.", misconception: "Кеширование текста исходника не изменило бы скорость исполнения вычисления. Меняется то, что JIT компилирует горячие функции в нативный machine code во время работы после прогрева." },
    { label: "Loader ОС всё ещё копирует бинарник в память в течение медленного периода.", misconception: "Загрузка происходит один раз при старте процесса заметно быстрее секунды. Многосекундный разгон — это прогрев JIT: горячие пути кода компилируются в нативный код, пока программа работает." },
  ]}
/>

<Quiz
  id="04-machine-code-to-language-choice-5"
  lessonSlug="04-machine-code-to-language"
  lang="ru"
  question="Джуниор говорит: «runtime — это просто другое название операционной системы». Почему это неверно?"
  choices={[
    { label: "ОС предоставляет общие сервисы — процессы, страницы памяти, файловые дескрипторы, системные вызовы — и ничего не знает о типах языка или его объектной модели. Runtime — это специфичный для языка слой поверх: garbage collector, машинерия call stack и соглашения о вызовах, стандартная библиотека и interpreter или VM. Runtime у Python и у Java различаются даже на одной ОС.", correct: true },
    { label: "Это верно — runtime любого языка и есть ядро ОС.", misconception: "Runtime располагается между твоим кодом и ОС; он использует сервисы ОС, но добавляет специфичные для языка сервисы (GC, стандартную библиотеку, VM), о которых ядро ничего не знает. У двух языков на одной ОС совершенно разные runtime." },
    { label: "Неверно, потому что у скомпилированных языков нет runtime, он есть только у интерпретируемых.", misconception: "Даже у скомпилированного C есть runtime: libc, стартовый код, который настраивает стек и вызывает main, и очистка при выходе. В C runtime тоньше, чем в Python, но он всегда есть." },
    { label: "Неверно, потому что runtime — это код, который пишешь ты, а не инфраструктура.", misconception: "Твой код работает во время исполнения, но «runtime» как существительное означает поддерживающую инфраструктуру, поставляемую реализацией языка — GC, стандартную библиотеку, VM — а не твой исходник." },
  ]}
/>

<Quiz
  id="04-machine-code-to-language-choice-6"
  lessonSlug="04-machine-code-to-language"
  lang="ru"
  question="Ты нажимаешь Run на программе на C. Какой порядок стадий конвейера верный?"
  choices={[
    { label: "Исходный текст → трансляция (компиляция в объектные файлы) → линковка (разрешение символов библиотек в один исполняемый файл) → загрузка (ОС создаёт процесс, копирует байты в память, устанавливает program counter) → старт runtime → цикл CPU fetch-decode-execute.", correct: true },
    { label: "Загрузка → трансляция → линковка → fetch-decode-execute → старт runtime.", misconception: "Загрузка не может быть первой — нечего загружать, пока исходник не скомпилирован и не слинкован в исполняемый файл. Трансляция и линковка предшествуют загрузке; старт runtime предшествует твоему коду; fetch-decode-execute — это уже исполнение CPU." },
    { label: "Трансляция → загрузка → линковка → старт runtime → fetch-decode-execute.", misconception: "Линковка происходит до загрузки, а не после. Linker объединяет объектные файлы в один исполняемый; только потом loader ОС может прочитать его с диска и поместить в память." },
    { label: "Линковка → трансляция → загрузка → fetch-decode-execute → старт runtime.", misconception: "Нельзя линковать до трансляции — linker объединяет объектные файлы, которые выпустил compiler, так что трансляция должна быть первой. И старт runtime идёт до твоего кода, а не после цикла CPU." },
  ]}
/>

<Recap lang="ru">
Весь юнит — одна лестница. Assembly называет machine code по одной инструкции; высокоуровневые языки позволяют одному оператору обозначать много инструкций и оставаться переносимыми между CPU; compiler транслирует всю программу заранее, а interpreter — оператор за оператором во время исполнения, JIT же смешивает оба, компилируя hot spot'ы на лету; runtime (GC, call stack, стандартная библиотека, VM) поддерживает твой код всё время; а полный конвейер — исходник → трансляция → линковка → загрузка → старт runtime → fetch-decode-execute — доносит каждую программу до CPU, который никогда не знает, с какого языка она пришла.
</Recap>
