---
concepts:
- error-vs-exception
- stack-unwinding
- reading-a-trace
- undefined-behaviour
- debugging-as-reasoning
- throw-site-vs-cause
estMin: 14
lang: ru
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Control_flow_and_error_handling
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Error/stack
- https://cacm.acm.org/research/modern-debugging/
status: ready
summary: Вопросы на свободное припоминание по всему юниту. Сначала ответь своими словами, затем открой модельный ответ и сравни.
title: 'Когда программа падает: тест на припоминание'
track: base-cs
unit: 11-when-a-program-fails
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. Для каждого вопроса скажи или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет различения: условие против механизма, громкое против тихого, рассуждение против угадывания.
</Hook>

<Goal lang="ru">
Восстанови костяк юнита, не подглядывая: error против exception, как раскручивается стек, что такое trace и как его читать, почему undefined behaviour — опасный случай, и что делает debugging рассуждением, а не угадыванием.
</Goal>

<RetrievalDrawer
  client:load
  id="11-when-a-program-fails-short-retrieval"
  lang="ru"
  questions={[
    { q: "В чём разница между error и exception?", a: "Error — это условие: факт о состоянии программы, через который она не может пройти нормальной логикой, например отсутствующий файл, деление на ноль или плохое приведение типа. Exception — механизм, которым рантайм отвечает: в момент поднятия error (ключевое слово throw в JavaScript) нормальный поток управления останавливается, следующая строка не выполняется, и рантайм начинает раскручивать стек вызовов в поиске обработчика. Эти двое меняются независимо — то же условие можно поднять как exception в одной программе и обработать обычной проверкой в другой, а тот же механизм раскрутки отвечает на тысячи разных условий одинаково. Exception не есть доказательство поломки: программа может его поднять, поймать и продолжить нормально." },
    { q: "Пройди по тому, что делает рантайм, когда exception поднят и обработчик не найден.", a: "Нормальный поток управления останавливается мгновенно — остаток бросившей функции не выполняется. Затем рантайм раскручивает стек вызовов: начиная с фрейма, поднявшего exception, он проверяет каждый фрейм на обработчик (блок catch). Если у фрейма его нет, он снимает (pop) этот фрейм — бросая функцию и отбрасывая её локальные — и идёт к фрейму ниже. Он шагает вниз к точке входа. Если он доходит до нижнего фрейма и всё ещё не находит обработчика, искать больше негде: программа падает, завершаясь и печатая необработанный exception вместе со списком фреймов, через которые раскрутилась, — это и есть stack trace." },
    { q: "Что такое stack trace, где в нём throw site и как его читать?", a: "Stack trace — снимок стека вызовов, сделанный в момент поднятия exception: список фреймов, по строке на каждый, с именем функции и строкой, которую она выполняла. Он захватывается на throw, а не на падении, потому что раскрутка разрушает стек, снимая каждый фрейм; если бы рантайм ждал до падения, описывать было бы нечего. Печатается верхним фреймом вперёд: верхняя строка — throw site, функция и строка, где exception всплыл, а каждая строка ниже — вызывающий строки над ней, вплоть до точки входа внизу. Читай сверху вниз — начинай с верхней строки, она говорит, что именно бросило и где; строки ниже дают цепочку вызовов, которая туда привела. Нижняя строка — точка входа и почти никогда не виновник." },
    { q: "Что такое undefined behaviour и почему оно опаснее defined error?", a: "Defined error — сбой, для которого язык специфицирует отрепортованный ответ: машина обнаруживает условие и поднимает exception, поток останавливается и trace наводит на строку — ты узнаёшь сразу. Undefined behaviour — операция, для которой язык не гарантирует никакого результата вовсе: чтение за концом массива в низкоуровневом языке без проверки, signed integer overflow, расползание значения undefined или NaN по вычислению. Определяющая опасность в том, что оно тихое: exception не поднимается, программа не останавливается, trace не печатается. Она несёт мусорное значение дальше как верное. Громкий, определённый сбой находят и чинят быстро; тихий может пережить тесты, уехать к пользователям и испортить реальные данные далеко от строки-причины, без trace, ведущего назад." },
    { q: "Почему детерминизм машины делает debugging рассуждением, а не угадыванием?", a: "Цикл fetch-decode-execute детерминирован: одна и та же инструкция на одном и том же состоянии машины всегда даёт ровно тот же результат, каждый раз, без всякой удачи. Значит баг не случаен — это фиксированное, повторяемое следствие кода и входа, дающее тот же неверный результат на каждом запуске с тем же входом. Поскольку баг сидит на месте, его можно найти рассуждением, а не удачей. Угадывание — менять строки и перезапускать, пока симптом не исчезнет — относится к багу как к тайне и может скрыть дефект, оставив его на месте. Рассуждение относится к багу как к определённому следствию и находит точный шаг, где реальное поведение программы впервые разошлось с тем, что она должна делать." },
    { q: "Назови три приёма проверки гипотезы при debugging и объясни, почему throw site не всегда есть причина.", a: "Гипотеза — конкретное, проверяемое утверждение о том, какой шаг разошёлся с ожидаемым, достаточно острое, чтобы одна проверка его подтвердила или опровергла. Три приёма: сузить диапазон — разбей программу, проверь состояние в средней точке и отбрось половину, которая ещё верна, вдвое сокращая подозреваемую область каждый раз; прочитать trace — когда сбой это поднятый exception, верхняя строка бесплатно выдаёт throw site, точную отправную точку вместо чистого листа; и проверить состояние — иди и посмотри на реальные значения переменных в тот самый момент, о котором гипотеза делает предсказание, убивая или заостряя гипотезу. Throw site — где error всплыл, не всегда где живёт причина: функция может бросить, потому что вызывающий передал плохие данные, поэтому рассуждение идёт по цепочке за throw site, пока не найдёт шаг, где состояние впервые пошло не так. Только тогда меняешь строку." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь костяк юнита: error — условие, exception — механизм, который останавливает поток и раскручивает стек в поиске обработчика; stack trace — этот стек, замороженный на throw, читаемый сверху вниз, throw site наверху; undefined behaviour — опасный тихий сбой без поднятия error; а поскольку машина детерминирована, debugging — это рассуждение: сформулируй гипотезу, сузь диапазон, прочитай trace, проверь состояние и иди по цепочке за throw site к реальной причине, прежде чем менять строку.
</Recap>
