---
concepts:
- error-vs-exception
- stack-unwinding
- reading-a-trace
- undefined-behaviour
- silent-failure
- debugging-as-reasoning
estMin: 13
lang: ru
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Control_flow_and_error_handling
- https://en.wikipedia.org/wiki/Undefined_behavior
- https://cacm.acm.org/research/modern-debugging/
status: ready
summary: "Тест с выбором на синтез по всему юниту: error против exception, раскрутка стека, чтение stack trace, undefined behaviour и debugging как рассуждение."
title: 'Когда программа падает: тест с выбором ответа'
track: base-cs
unit: 11-when-a-program-fails
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый — небольшое решение, которое ты принимаешь перед реальным сбоем: не определение для заучивания, а различение, которое надо держать твёрдо — условие против механизма, громкий сбой против тихого, рассуждение против угадывания.
</Hook>

<Goal lang="ru">
Убедись, что связываешь четыре урока в одну картину: error — это условие, exception — механизм, раскручивающий стек, stack trace — снимок этой раскрутки, undefined behaviour — опасный тихий случай, а debugging — рассуждение над детерминированной машиной.
</Goal>

<Quiz
  id="11-when-a-program-fails-choice-1"
  lessonSlug="11-when-a-program-fails"
  lang="ru"
  question="Программа пытается открыть несуществующий файл. Что здесь error, а что exception?"
  choices={[
    { label: "Error — это условие: запрошенного файла нет, факт о состоянии программы. Exception — механизм ответа рантайма: остановить нормальный поток и раскручивать стек в поиске обработчика.", correct: true },
    { label: "Error и exception — одно и то же: оба означают, что программа сломана и должна остановиться.", misconception: "Они меняются независимо. То же условие отсутствующего файла можно поднять как exception в одной программе и обработать обычной проверкой в другой. Программа может поднять exception, поймать его и продолжить совершенно нормально — ничего не сломано навсегда." },
    { label: "Error — то, что обнаруживает код программиста; exception — то, что обнаруживает CPU в железе.", misconception: "Оба живут на уровне рантайма/программы. Разделение — условие против механизма, а не софт против железа: error — плохая ситуация, exception — ответ рантайма в виде раскрутки стека." },
    { label: "Exception — это условие, а error — ответ рантайма на него.", misconception: "Всё наоборот. Error — это условие (факт); exception — ответ (процесс раскрутки стека в поиске обработчика)." },
  ]}
/>

<Quiz
  id="11-when-a-program-fails-choice-2"
  lessonSlug="11-when-a-program-fails"
  lang="ru"
  question="main вызывает a, a вызывает b, b поднимает exception. Обработчик есть только у main. Сколько фреймов снимет (pop) рантайм и где возобновится нормальный поток?"
  choices={[
    { label: "Снимаются два фрейма — b (нет обработчика), затем a (нет обработчика) — раскрутка останавливается на main, его обработчик срабатывает, и нормальный поток возобновляется внутри main.", correct: true },
    { label: "Снимаются три фрейма, и программа падает.", misconception: "Crash наступает только когда раскрутка доходит до дна, и обработчика нет нигде. У main обработчик есть, поэтому раскрутка останавливается там и фрейм main не снимается — снимаются лишь b и a." },
    { label: "Снимается ноль фреймов — обработчик main ловит exception до всякой раскрутки.", misconception: "Обработчик в main, но exception поднят в b. Рантайм должен сначала раскрутить b и a, чтобы дойти до фрейма, у которого реально есть обработчик." },
    { label: "Снимается один фрейм — b брошен, а main, вызвавший a, обрабатывает напрямую.", misconception: "a стоит между b и main и не имеет обработчика, поэтому фрейм a тоже снимается. Перед тем как раскрутка дойдёт до main, снимаются два фрейма (b, затем a)." },
  ]}
/>

<Quiz
  id="11-when-a-program-fails-choice-3"
  lessonSlug="11-when-a-program-fails"
  lang="ru"
  question="Stack trace из четырёх строк (сверху вниз): divide, getAge, loadUser, main. Новичок первым делом правит main. Почему это неверный инстинкт?"
  choices={[
    { label: "Нижняя строка — точка входа программы: она есть почти в каждом trace и почти никогда не виновата. Throw site — это верхняя строка (divide); чтение начинается там, с этой функции и номера строки.", correct: true },
    { label: "main править правильно, ведь это самый внешний вызов, значит баг возникает там.", misconception: "Самый внешний вызов — лишь место, откуда программа стартовала. Exception всплыл на верхней строке, divide. main в trace только потому, что он на цепочке вызовов, а не из-за вины." },
    { label: "Trace надо читать снизу вверх, так что main внизу означает, что он бросил последним.", misconception: "Trace читается сверху вниз. Верхняя строка — throw site, где поднят exception, а каждая строка ниже — вызывающий строки над ней, вплоть до точки входа внизу." },
    { label: "Ни одна строка не важна — trace просто перечисляет все функции файла.", misconception: "Trace — не список функций программы. В нём лишь фреймы, активные на стеке в момент throw, — точная цепочка вызовов, приведшая к exception." },
  ]}
/>

<Quiz
  id="11-when-a-program-fails-choice-4"
  lessonSlug="11-when-a-program-fails"
  lang="ru"
  question="Одна программа читает индекс 5 массива из 3 элементов и поднимает exception 'index out of range'. Другая читает ту же ячейку за границей и возвращает лежащие там биты, без всякой ошибки. Какой сбой опаснее и почему?"
  choices={[
    { label: "Второй опаснее. Это undefined behaviour: ни exception, ни trace, ни остановки — программа несёт мусорное значение дальше, поэтому сбой может пережить тесты, уехать в прод и испортить реальные данные далеко от своей причины.", correct: true },
    { label: "Первый опаснее, ведь поднятый exception роняет программу, а падения — худший исход.", misconception: "Громкий, определённый сбой — это хороший случай: он останавливает поток и наводит trace на строку, так что ты находишь и чинишь быстро. Опасен именно тихий сбой, потому что его ничто не сигналит." },
    { label: "Они одинаково опасны — оба читают неверную ячейку, значит исход тот же.", misconception: "Чтение неверной ячейки — тот же баг, но исходы расходятся полностью: определённая ошибка поймана и отрепортована; чтение с undefined behaviour молчит и распространяет неверное значение без trace." },
    { label: "Второй безопаснее, ведь он не падает, и программа продолжает обслуживать пользователей.", misconception: "Продолжать работу с мусорным значением — не безопасность, а опасность. Программа выдаёт неверные результаты, выглядящие правильными, что хуже громкой остановки." },
  ]}
/>

<Quiz
  id="11-when-a-program-fails-choice-5"
  lessonSlug="11-when-a-program-fails"
  lang="ru"
  question="В JavaScript чтение индекса 9 массива из 3 элементов даёт undefined (не падение), и затем код вычисляет undefined + 1. Что происходит и почему это напоминает undefined behaviour, хотя каждый шаг определён?"
  choices={[
    { label: "undefined + 1 равно NaN, и NaN тихо расползается по всем последующим вычислениям без поднятия exception. Каждый шаг определён, но неверное значение распространяется молча — тот же паттерн тихого сбоя, что у настоящего undefined behaviour.", correct: true },
    { label: "undefined + 1 поднимает exception, поэтому рантайм останавливается и печатает trace на этой строке.", misconception: "Exception не поднимается. undefined + 1 вычисляется в NaN, и программа продолжает работать. Опасность как раз в том, что она не останавливается." },
    { label: "undefined + 1 равно 1, ведь undefined считается нулём в арифметике.", misconception: "undefined не приводится к 0 в сложении; undefined + 1 — это NaN. Этот NaN затем заражает каждое вычисление, которого касается." },
    { label: "Это настоящий undefined behaviour, ведь JavaScript не проверяет границы массива.", misconception: "JavaScript проверяет границы — чтение за пределами возвращает undefined, что есть определённое поведение. Опасность в стиле undefined behaviour идёт от использования этого undefined незаметно для себя, а не от самого чтения." },
  ]}
/>

<Quiz
  id="11-when-a-program-fails-choice-6"
  lessonSlug="11-when-a-program-fails"
  lang="ru"
  question="Программа возвращает неверную сумму. Инженер начинает переворачивать сравнения и перезапускать, пока число не станет похожим на верное. В чём принципиальное возражение, опирающееся на работу машины?"
  choices={[
    { label: "Машина детерминирована — один и тот же шаг на одном и том же состоянии всегда даёт тот же результат — значит у бага фиксированная, находимая причина. Угадывание может скрыть симптом, оставив дефект; рассуждение (гипотеза, сузить диапазон, проверить состояние) находит реальный виновный шаг.", correct: true },
    { label: "Ничего плохого — случайные изменения до исчезновения симптома и есть самый быстрый надёжный способ убрать баг.", misconception: "Заставить видимый симптом исчезнуть — не значит убрать баг. Угадывание может замаскировать дефект, который всплывёт на другом входе. Детерминизм означает, что причина фиксирована и рассуждение может её найти." },
    { label: "Возражение в том, что компьютеры ведут себя случайно, поэтому надо пробовать много фиксов, пока один не сработает.", misconception: "Компьютеры не ведут себя случайно — fetch-decode-execute детерминирован. То же состояние всегда даёт тот же результат, и именно это делает баг находимым рассуждением, а не удачей." },
    { label: "Переворачивать сравнения нормально, пока сохраняешь изменение, на котором тест проходит.", misconception: "Прохождение теста после слепого изменения не доказывает, что причина найдена; оно может лишь скрыть один путь к симптому. Менять строку стоит только после того, как рассуждение указало точный расходящийся шаг." },
  ]}
/>

<Recap lang="ru">
Юнит — одна цепочка: error — условие, exception — механизм, который останавливает нормальный поток и раскручивает стек фрейм за фреймом в поиске обработчика, а stack trace — этот стек, замороженный в момент throw, читаемый сверху вниз, throw site первым. Undefined behaviour — опасный случай, где error не поднимается вовсе и мусорное значение едет молча. А поскольку машина детерминирована, ответ на любой сбой — рассуждение, а не угадывание: сформулируй проверяемую гипотезу, сузь диапазон, прочитай trace, проверь состояние, затем чини шаг, виновность которого доказал.
</Recap>
