---
concepts:
- reading-a-trace
- classify-the-error
- out-of-bounds-read
- defined-vs-undefined
- throw-site-vs-cause
estMin: 14
lang: ru
level: middle
order: 9
prereqs: []
slug: quiz-code
sources:
- https://v8.dev/docs/stack-trace-api
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Error/stack
- https://en.wikipedia.org/wiki/Undefined_behavior
status: ready
summary: "Прочитай stack trace и короткие сниппеты, затем классифицируй каждый сбой: какая строка бросила, defined error это или undefined behaviour, и где живёт настоящая причина."
title: 'Когда программа падает: чтение кода и trace'
track: base-cs
unit: 11-when-a-program-fails
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Сбой читается в двух местах: код, который выполнился, и trace, который напечатал рантайм. Для каждого сниппета ниже делай то, что инженер делает первым — найди throw site, назови вид сбоя и реши, где живёт настоящая причина.
</Hook>

<Goal lang="ru">
Потренируй цикл чтения, к которому вёл юнит: разбирай stack trace сверху вниз, чтобы найти throw site, классифицируй сбой как defined error или undefined behaviour и отделяй, где exception всплыл, от того, где реально сидит его причина.
</Goal>

## Сниппет 1 — прочитай trace

```
Error: Cannot read properties of undefined (reading 'name')
    at formatLabel (render.ts:14)
    at renderRow (render.ts:31)
    at renderTable (render.ts:48)
    at main (app.ts:9)
```

<Quiz
  id="11-when-a-program-fails-code-1"
  lessonSlug="11-when-a-program-fails"
  lang="ru"
  question="Читая этот trace сверху вниз, какое утверждение верно?"
  choices={[
    { label: "Throw site — formatLabel в render.ts:14, верхняя строка, и он всплыл потому, что ему передали значение, равное undefined. renderRow, renderTable и main — цепочка вызовов, приведшая туда; main внизу — лишь точка входа.", correct: true },
    { label: "main в app.ts:9 виновен, потому что это нижняя строка trace.", misconception: "Нижняя строка — точка входа: она почти в каждом trace и почти никогда не причина. Throw site — верхняя строка, formatLabel в render.ts:14." },
    { label: "Trace перечисляет все функции render.ts в порядке их объявления.", misconception: "Trace — не список функций файла. Это лишь фреймы, активные на стеке в момент throw — цепочка main → renderTable → renderRow → formatLabel." },
    { label: "Бросил renderRow, потому что он в середине цепочки.", misconception: "Throw site — всегда верхняя строка. renderRow стоит ниже formatLabel, то есть renderRow ждал на строке 31 на своём вызове formatLabel — он не бросал." },
  ]}
/>

## Сниппет 2 — классифицируй сбой

```js
function loadUser(record) {
  return record.profile.name.toUpperCase();
}
loadUser({ profile: null });  // profile равен null, а не объект
```

<Quiz
  id="11-when-a-program-fails-code-2"
  lessonSlug="11-when-a-program-fails"
  lang="ru"
  question="record.profile равен null, поэтому код читает .name у null. Какой это вид сбоя в JavaScript и как ты о нём узнаешь?"
  choices={[
    { label: "Defined error. Чтение свойства у null специфицировано как поднятие exception ('Cannot read properties of null'), поэтому поток останавливается и stack trace наводит на эту строку. Громко, найдено, починено.", correct: true },
    { label: "Undefined behaviour — чтение возвращает мусорные биты, и программа молча продолжает работать.", misconception: "Чтение свойства у null — defined error в JavaScript: оно поднимает exception. Undefined behaviour — тихий случай без поднятия ошибки, а это громкий, отрепортованный случай." },
    { label: "Ничего — JavaScript возвращает undefined для null.name, и программа продолжает нормально.", misconception: "null.name не возвращает undefined; чтение свойства у null поднимает TypeError. (Чтение отсутствующего свойства у объекта дало бы undefined, но null — не объект.)" },
    { label: "Зависит от рантайма — иногда падает, иногда возвращает мусор.", misconception: "Это детерминировано и определено: то же чтение у null всегда поднимает тот же exception. В языке, который проверяет этот случай, исхода с мусорными битами нет." },
  ]}
/>

## Сниппет 3 — выход за границы, два языка

```
prices = [10, 20, 30]   // допустимые индексы 0, 1, 2
read prices[5]          // индекс 5 — на два за концом
```

<Quiz
  id="11-when-a-program-fails-code-3"
  lessonSlug="11-when-a-program-fails"
  lang="ru"
  question="Одно и то же чтение за границей выполняется в языке с проверкой границ и в низкоуровневом языке без проверки. Чем различаются исходы?"
  choices={[
    { label: "Язык с проверкой границ поднимает defined exception 'index out of range' — громко, с trace. Язык без проверки вычисляет адрес за массивом и читает лежащие там биты — undefined behaviour, молча, возвращая бессмысленное значение как настоящее.", correct: true },
    { label: "Оба языка поднимают exception, просто с разными сообщениями.", misconception: "Только язык с проверкой границ поднимает exception. Низкоуровневый язык без проверки не делает её — он читает ячейку за границей и возвращает мусор вообще без ошибки." },
    { label: "Оба возвращают undefined, ведь индекс за концом всегда даёт undefined.", misconception: "undefined — это конкретный определённый результат JavaScript. У низкоуровневого языка без проверки такой гарантии нет — он читает любые биты по вычисленному адресу." },
    { label: "Ни один ничего не делает, ведь индекс 5 просто не существует.", misconception: "То, что индекс невалиден, — в этом вся суть. Язык с проверкой границ обнаруживает это и бросает; язык без проверки игнорирует и читает память, которую не должен." },
  ]}
/>

## Сниппет 4 — throw site не есть причина

```js
function parseAge(text) {
  const n = Number(text);
  if (Number.isNaN(n)) throw new Error("not a number: " + text);
  return n;
}

function loadProfile(raw) {
  return { age: parseAge(raw.age) };   // raw.age пришёл из вышестоящего слоя
}
// raw.age равен "thirty" — ни разу не провалидирован до этого места
```

<Quiz
  id="11-when-a-program-fails-code-4"
  lessonSlug="11-when-a-program-fails"
  lang="ru"
  question="Trace упирается в parseAge ('not a number: thirty'). Гипотеза при debugging должна отделить, где exception всплыл, от того, где живёт причина. Какое прочтение верно?"
  choices={[
    { label: "parseAge — throw site, но он ведёт себя правильно: проверил вход и отрепортовал плохие данные. Причина выше по цепочке: raw.age пришёл как 'thirty', потому что его не провалидировали до loadProfile. Иди по цепочке за throw site, чтобы найти реальный дефект.", correct: true },
    { label: "parseAge — баг; удали throw, чтобы он перестал падать.", misconception: "parseAge делает свою работу: обнаруживает плохой вход и громко его репортует. Удаление throw превратило бы громкий, определённый сбой в тихий — худший исход. Дефект — непровалидированное 'thirty' выше." },
    { label: "Причина всегда на верхней строке trace, так что фикс — в parseAge.", misconception: "Верхняя строка — где error всплыл, не всегда где живёт причина. Здесь вызывающий передал плохие данные; рассуждение идёт по цепочке к шагу выше, где состояние впервые пошло не так." },
    { label: "Бага нет — программа бросила, значит сработала как задумано.", misconception: "Бросать на плохом входе — корректное поведение, но сам плохой вход — дефект. Суть юнита — продолжать рассуждение за throw site до шага, который первым разошёлся." },
  ]}
/>

<Recap lang="ru">
Любой сбой читается в коде и trace: trace разбирается сверху вниз, верхняя строка — throw site, нижняя — лишь точка входа; чтение свойства у null — defined error, который громко поднимается, а непроверенное чтение за границей — undefined behaviour, молча возвращающее мусор; и throw site — где error всплыл, не всегда где живёт причина. Классифицируй сбой, найди, где он всплыл, затем рассуждай вниз по цепочке к шагу, который первым пошёл не так.
</Recap>
