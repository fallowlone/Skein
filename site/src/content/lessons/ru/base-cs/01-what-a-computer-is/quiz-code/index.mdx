---
concepts:
- place-value
- ascii
- truth-table
- half-adder
estMin: 14
lang: ru
level: middle
order: 9
prereqs: []
slug: quiz-code
sources:
- https://www.nand2tetris.org/project01
- https://en.wikipedia.org/wiki/UTF-8
status: ready
summary: "Читай битовые паттерны, ASCII-байты, булево выражение и трассу полусумматора, затем предсказывай результат так, как учил юнит."
title: 'Что такое компьютер: чтение паттернов и схем'
track: base-cs
unit: 01-what-a-computer-is
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Юнит был про извлечение смысла из битов. Здесь ты делаешь это напрямую: декодируешь двоичное число, декодируешь ASCII-байт, вычисляешь булево выражение по приоритету и трассируешь полусумматор. Реши каждое сам, прежде чем выбрать.
</Hook>

<Goal lang="ru">
Потренируй ядро всего юнита — взять сырой паттерн или маленькую схему, применить правило (позиционное значение, кодировку, булев приоритет или таблицу истинности гейта) и считать правильный результат.
</Goal>

## Сниппет 1 — прочитай двоичное число

```
bit:    1  0  1  1  0  1  0  0
weight: 128 64 32 16 8  4  2  1
```

<Quiz
  id="01-what-a-computer-is-code-1"
  lessonSlug="01-what-a-computer-is"
  lang="ru"
  question="Читая этот 8-битный паттерн по позиционному значению, какое это десятичное число?"
  choices={[
    { label: "180 — сложи веса позиций, где стоит 1: 128 + 32 + 16 + 4 = 180.", correct: true },
    { label: "53 — читай биты слева направо как веса 1, 2, 4, 8, ...", misconception: "Позиция веса 1 — это самый правый бит, а не самый левый. Закрепи вес 1 справа и удваивай влево, затем сложи позиции с 1: 128 + 32 + 16 + 4 = 180." },
    { label: "11010100 — этот паттерн уже и есть число.", misconception: "11010100 — двоичное представление; вопрос про десятичное значение. Сумма весов 1-битов даёт 180." },
    { label: "8 — здесь восемь битов, поэтому значение равно 8.", misconception: "Число битов — это ширина, а не значение. Значение — позиционная сумма 1-битов: 128 + 32 + 16 + 4 = 180." },
  ]}
/>

## Сниппет 2 — декодируй ASCII-байты

```
byte 1: 01001000   (decimal 72)
byte 2: 01101001   (decimal 105)
ASCII reference: 'A'=65  'H'=72  'a'=97  'i'=105
```

<Quiz
  id="01-what-a-computer-is-code-2"
  lessonSlug="01-what-a-computer-is"
  lang="ru"
  question="Декодированные как ASCII-текст, что складывают эти два байта — и чем были бы ТЕ ЖЕ два байта при простом числовом чтении?"
  choices={[
    { label: "ASCII: 'Hi' (72='H', 105='i'). Числа: целые 72 и 105. Те же биты, два валидных смысла — решает кодировка.", correct: true },
    { label: "ASCII: 'Hi', и численно они тоже обязаны быть 'Hi' — у байтов один истинный смысл.", misconception: "Байты не несут собственного смысла. Численно эти байты — 72 и 105; только кодировка ASCII превращает их в 'Hi'." },
    { label: "ASCII: 'HI' (оба заглавные), потому что в ASCII нет строчных.", misconception: "В ASCII есть строчные: 'a'=97, 'i'=105. Байт 105 — строчная 'i', поэтому текст 'Hi', а не 'HI'." },
    { label: "Декодировать нельзя — 8-битные байты не являются валидным ASCII.", misconception: "ASCII — 7-битный код, хранимый в 8-битных байтах со старшим битом 0. Оба байта в диапазоне 0–127, поэтому декодируются нормально: 'Hi'." },
  ]}
/>

## Сниппет 3 — вычисли булево выражение

```
expr = NOT A OR (B AND C)
inputs: A = 1, B = 1, C = 0
precedence: NOT first, then AND, then OR
```

<Quiz
  id="01-what-a-computer-is-code-3"
  lessonSlug="01-what-a-computer-is"
  lang="ru"
  question="Вычисляя по указанному приоритету, каков результат?"
  choices={[
    { label: "0 — NOT A = NOT 1 = 0; B AND C = 1 AND 0 = 0; затем 0 OR 0 = 0.", correct: true },
    { label: "1 — вычисляй слева направо: NOT A OR B это 0 OR 1 = 1, потом AND C.", misconception: "Приоритет — NOT, затем AND, затем OR, а не строго слева направо. AND в скобках связывает B и C первым: NOT A (=0) OR (B AND C = 0) = 0." },
    { label: "1 — NOT применяется ко всему выражению A OR (B AND C).", misconception: "NOT связывает сильнее всего и применяется только к A, давая NOT A = 0. Он не отрицает всё выражение. Результат — 0." },
    { label: "Не определено — нельзя смешивать AND и OR в одном выражении.", misconception: "Смешивать можно; приоритет это разрешает. NOT, затем AND, затем OR даёт одно значение: здесь 0." },
  ]}
/>

## Сниппет 4 — трассируй полусумматор

```
half-adder:
  sum   = A XOR B   (1 only when inputs differ)
  carry = A AND B   (1 only when both are 1)
inputs: A = 1, B = 1
```

<Quiz
  id="01-what-a-computer-is-code-4"
  lessonSlug="01-what-a-computer-is"
  lang="ru"
  question="Какие сумму и перенос даёт полусумматор для A=1, B=1, и почему это совпадает со сложением битов?"
  choices={[
    { label: "sum = 0, carry = 1. XOR двух равных входов — 0; AND двух единиц — 1. Это ровно 1 + 1 = двоичное 10 (бит суммы 0, бит переноса 1).", correct: true },
    { label: "sum = 1, carry = 1, потому что 1 + 1 = 2, и оба выхода идут в высокое.", misconception: "Бит суммы — это XOR, который равен 0, когда входы равны. 1 + 1 — двоичное 10: бит суммы 0, бит переноса 1, а не сумма 1." },
    { label: "sum = 1, carry = 0, так как XOR от 1 и 1 равен 1.", misconception: "XOR равен 1 только когда входы РАЗЛИЧАЮТСЯ. Для двух единиц XOR равен 0. Значит sum = 0, а перенос (AND) равен 1." },
    { label: "sum = 0, carry = 0 — оба гейта дают 0, когда входы совпадают.", misconception: "AND двух единиц равен 1, поэтому перенос равен 1, а не 0. Здесь нулём оказывается только XOR-сумма." },
  ]}
/>

<Recap lang="ru">
Каждый сниппет был миниатюрой читающего цикла юнита: двоичное число — позиционная сумма его 1-битов; ASCII-байт — это число, пока кодировка не назовёт его буквой; булево выражение раскрывается по правилу NOT-затем-AND-затем-OR; а XOR-сумма и AND-перенос полусумматора в точности воспроизводят сложение одного бита. Биты плюс правило дают смысл — это весь юнит в четырёх чтениях.
</Recap>
