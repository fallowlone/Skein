---
concepts:
- bit
- place-value
- encoding
- truth-table
- functional-completeness
- half-adder
estMin: 13
lang: ru
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.nand2tetris.org/
- https://en.wikipedia.org/wiki/Positional_notation
status: ready
summary: "Промпты на свободное припоминание по юниту — проговори полный ответ по памяти, затем раскрой модельный ответ и сравни."
title: 'Что такое компьютер: свободное припоминание'
track: base-cs
unit: 01-what-a-computer-is
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт проговори или запиши полный ответ по памяти, прежде чем открыть модельный. Усилие реконструкции идеи — а не узнавания — закрепляет её.
</Hook>

<Goal lang="ru">
Восстанови хребет юнита, не подглядывая: почему два состояния, как позиционное значение превращает биты в числа, почему битам нужна кодировка, почему трёх булевых операций достаточно и как гейты поднимаются от логики к арифметике.
</Goal>

<RetrievalDrawer
  client:load
  id="01-what-a-computer-is-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему железо использует ровно два состояния, а не десять, и что такое бит?", a: "Бит (binary digit) — наименьшая единица информации: один выбор между двумя состояниями — 0 или 1, выкл или вкл, низкое или высокое напряжение. Железо использует два состояния, а не десять, потому что напряжение никогда не идеально стабильно: температура, соседние сигналы и колебания питания вызывают небольшие флуктуации. Два уровня (скажем, 0–1 В для низкого и 3–5 В для высокого) оставляют широкий зазор, который шум почти никогда не пересекает, поэтому чтение надёжно восстанавливает задуманное значение. Десять тесных уровней оставили бы зазоры настолько малыми, что крошечная флуктуация переключала бы цифру. Чем больше зазор относительно шума, тем надёжнее чтение — поэтому два состояния максимизируют запас, и двоичная победила." },
    { q: "Объясни позиционное значение в двоичной системе и почему каждый добавленный бит удваивает диапазон.", a: "Двоичная система использует ту же позиционную идею, что и десятичная, но с основанием 2: самая правая позиция стоит 2⁰ = 1, затем 2, 4, 8, 16 и так далее — каждая позиция вдвое больше соседней справа. Число — это сумма веса каждого 1-бита. Каждый лишний бит удваивает счёт паттернов, потому что новый бит может независимо быть 0 или 1, так что каждый существующий паттерн порождает два: 1 бит даёт 2 паттерна, 2 бита — 4, 3 бита — 8. Поэтому переход с 8 на 16 бит умножает диапазон на 2⁸ = 256, а не на 2." },
    { q: "Что такое кодировка и почему её нужно согласовать заранее? Возьми байт 01000001 как пример.", a: "Кодировка — это общее соглашение, присваивающее смысл битовому паттерну. Биты не несут собственного смысла — тот же байт 01000001 — это число 65 при числовом чтении, буква 'A' в ASCII и один канал тёмного цвета в RGB. Поскольку у битового паттерна нет самоописывающей метки, отправитель и получатель должны согласовать кодировку заранее — через формат файла, заголовок, MIME-тип, расширение. Без этого предварительного соглашения получатель гадает, а неверная догадка даёт mojibake, испорченные изображения или искажённый звук. ASCII задаёт 'A'=65, 'a'=97, '0'=48; Unicode расширяет это на любую письменность, а UTF-8 хранит кодовые точки в 1–4 байтах, обратно совместимо с ASCII." },
    { q: "Что такое таблица истинности и почему это определение булевой операции, а не просто сводка?", a: "Таблица истинности перечисляет каждую возможную комбинацию входов и выход, который операция даёт для каждой — две строки на один вход, четыре на два входа (2 × 2). Это определение, а не сводка, потому что булевы входы исчерпывающе равны 0 или 1, и таблица полностью задаёт поведение операции, не оставляя ничего неуказанным. Две операции одинаковы тогда и только тогда, когда их таблицы истинности тождественны — поэтому таблица и есть главный инструмент доказательства эквивалентности булевых выражений: показать, что они совпадают в каждой строке." },
    { q: "Сформулируй, что значит 'функционально полны' для AND, OR, NOT, и набросай, почему это так.", a: "Функционально полны означает, что любую возможную булеву функцию — любую таблицу истинности, сколько бы входов в ней ни было — можно выразить только через AND, OR и NOT. Набросок — дизъюнктивная нормальная форма: для каждой строки, где желаемый выход равен 1, пишем одно AND-выражение, истинное ровно в этой строке (используя NOT, чтобы отрицать любой вход, равный 0 в этой строке), затем соединяем все эти построчные выражения через OR. Результат истинен ровно в 1-строках и ложен во всех прочих, совпадая с целевой таблицей. Сильнее того: (AND, NOT), (OR, NOT) и даже один NAND каждый полны — так что целый компьютер можно построить из одного типа гейта." },
    { q: "Как полусумматор превращает гейты в арифметику и какой гейт даёт сумму, а какой перенос?", a: "Логический гейт — это железо, выполняющее одну булеву операцию; его выход — бит, того же типа, что и входы, поэтому гейты свободно собираются в схемы. Полусумматор складывает два одиночных бита и имеет два выхода. При сложении A и B: бит суммы равен 1 ровно когда входы различаются (0+1 или 1+0) — это XOR; бит переноса равен 1 ровно когда оба равны 1 (1+1 = двоичное 10) — это AND. Значит полусумматор — это схема XOR для суммы и гейт AND для переноса, подключённые к одним входам. Полный сумматор добавляет перенос-вход (два полусумматора плюс OR), а цепочка полных сумматоров складывает многобитные числа — арифметико-логическое устройство внутри CPU состоит из гейтов до самого низа." },
  ]}
/>

<Recap lang="ru">
Если ты восстановил каждый ответ по памяти, ты держишь хребет юнита: два состояния побеждают по запасу на шум; позиционное значение превращает биты в числа, и каждый бит удваивает диапазон; согласованная кодировка — это то, что придаёт битам смысл; таблица истинности полностью определяет булеву операцию; AND, OR, NOT (или один NAND) функционально полны; а гейты, собранные в полусумматор, воспроизводят сложение — первая ступенька от логики вверх к работающему CPU.
</Recap>
