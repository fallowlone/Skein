---
concepts:
- parameter
- argument
- return-value
- frame-boundary
- pass-by-value
estMin: 20
lang: ru
lessonType: coding
mathPrereqs: []
order: 3
prereqs:
- 01-what-a-function-is
- 02-the-call-stack
- 04-stack-and-heap
- 01-a-variable-is-a-named-cell
slug: 03-parameters-and-return
sources:
- https://www.nand2tetris.org/
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions
- https://www.cs.cmu.edu/afs/cs/academic/class/15213-f15/www/lectures/09-machine-advanced.pdf
status: ready
summary: Параметры — значения, скопированные в новый кадр до начала выполнения тела
  функции. Возвращаемое значение передаётся вызывающей стороне при срабатывании RET.
  Оба механизма передают данные через границу кадра стека.
title: Параметры и возвращаемые значения
track: base-cs
unit: 08-functions-and-the-call-stack
---



import Hook from "~/components/lesson/Hook.astro";
import PracticeSet from "~/components/math/PracticeSet.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
Ты уже умеешь трассировать стек вызовов по мере вызова и возврата функций. Следующий
вопрос: как данные попадают в функцию и как результат возвращается обратно?

Функция, которая всегда работает с одними и теми же фиксированными значениями, почти
бесполезна. Настоящие функции принимают **параметры** — значения, предоставленные
вызывающей стороной в момент вызова — и производят **возвращаемое значение**, которое
вызывающая сторона может использовать. Эти два механизма — интерфейс между функцией и
кодом, который её вызывает, и оба работают через кадр стека.
</Hook>

<Goal lang="ru">
После этого урока ты сможешь объяснить, что такое параметр (значение, скопированное в
новый кадр в момент вызова), что такое возвращаемое значение (значение, произведённое
функцией и переданное вызывающей стороне), и трассировать вызов функции с параметрами
и возвращаемым значением, показывая содержимое кадра на каждом шаге.
</Goal>

<Idea lang="ru">
**Параметры** объявляются в сигнатуре функции. Когда вызывающая сторона выдаёт `CALL`,
значения, которые она передаёт (называемые **аргументами**), копируются в новый кадр до
начала выполнения тела функции. Внутри функции параметры выглядят и ведут себя точно как
локальные переменные — это именованные ячейки внутри кадра. Разница в том, что они
заранее заполнены значениями вызывающей стороны, а не устанавливаются присваиванием.

В синтаксисе TypeScript `function add(x: number, y: number): number` объявляет два
параметра `x` и `y`. В точке вызова `add(3, 5)` значения `3` и `5` — аргументы,
которые копируются в ячейки `x` и `y` нового кадра.

**Возвращаемые значения** работают в обратном направлении. Когда тело функции достигает
оператора `return`, оно вычисляет возвращаемое значение и делает его доступным для
вызывающей стороны. Аппаратно это обычно делается путём помещения возвращаемого значения
в регистр CPU (быстрый слот хранения внутри самого CPU) до срабатывания `RET`. Вызывающая
сторона затем читает этот регистр после того, как `RET` восстанавливает счётчик команд.

Ключевой момент: параметры передают данные *в* кадр; возвращаемое значение передаёт
данные *из* кадра. Оба пересечения происходят на границе кадра — в момент `CALL` (вход)
и `RET` (выход).

Функция для трассировки: `add(x, y)` принимает два числа и возвращает их сумму.
Трассировка показывает `add(3, 5)` с состоянием стека до вызова, во время выполнения
и после возврата.
</Idea>

<DiagramFrame
  label="Поток данных для add(3, 5): точка вызова передаёт аргументы 3 и 5; каждый копируется в одну из ячеек-параметров add (x = 3, y = 5); тело вычисляет x + y = 8; затем return передаёт это одно значение обратно в точку вызова, где total становится 8."
  hue="var(--d-hardware)"
  caption="Аргументы в точке вызова связываются с параметрами функции (копии — для примитивов). Тело вычисляет, и return передаёт ровно одно значение обратно вызывающей стороне."
>
  <svg viewBox="0 0 460 250" width="460" height="250" role="presentation" style="display:block;margin:0 auto;max-width:100%;height:auto;font-family:var(--font-body);">
    <defs>
      <marker id="par-arrow-ru" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
        <path d="M0 0 L10 5 L0 10 z" fill="var(--muted)" />
      </marker>
    </defs>
    {/* CALL SITE box */}
    <rect x="20" y="44" width="150" height="96" rx="7" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="95" y="36" text-anchor="middle" font-size="12" fill="var(--ink)">точка вызова (main)</text>
    <text x="95" y="74" text-anchor="middle" font-size="13" font-family="var(--font-mono)" fill="var(--ink)">add(3, 5)</text>
    <text x="95" y="98" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--faint)">аргументы</text>
    <text x="95" y="120" text-anchor="middle" font-size="13" font-family="var(--font-mono)" fill="var(--ink)">total = 8</text>
    {/* FUNCTION frame box */}
    <rect x="290" y="30" width="150" height="124" rx="7" fill="none" stroke="var(--hairline-strong)" stroke-width="0.8" stroke-dasharray="4 3" />
    <text x="365" y="22" text-anchor="middle" font-size="12" fill="var(--ink)">кадр add</text>
    {/* parameter cells */}
    <rect x="304" y="40" width="122" height="26" rx="4" fill="var(--card-2)" stroke="var(--muted)" stroke-width="0.8" />
    <text x="365" y="57" text-anchor="middle" font-size="12" font-family="var(--font-mono)" fill="var(--ink)">x = 3</text>
    <rect x="304" y="72" width="122" height="26" rx="4" fill="var(--card-2)" stroke="var(--muted)" stroke-width="0.8" />
    <text x="365" y="89" text-anchor="middle" font-size="12" font-family="var(--font-mono)" fill="var(--ink)">y = 5</text>
    {/* body */}
    <text x="365" y="120" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--faint)">тело</text>
    <text x="365" y="140" text-anchor="middle" font-size="12" font-family="var(--font-mono)" fill="var(--ink)">x + y = 8</text>
    {/* args -> params arrow (copied in) */}
    <path d="M170 80 L296 60" fill="none" stroke="var(--muted)" stroke-width="1.6" marker-end="url(#par-arrow-ru)" />
    <text x="232" y="58" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--ink)">копия внутрь</text>
    {/* return -> caller arrow (one value out) */}
    <path d="M296 140 L170 118" fill="none" stroke="var(--muted)" stroke-width="1.6" stroke-dasharray="4 3" marker-end="url(#par-arrow-ru)" />
    <text x="232" y="146" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--ink)">return 8</text>
  </svg>
</DiagramFrame>

<Code lang="ru">

<AnnotatedCode
  code={`function add(x: number, y: number): number {
  let result = x + y;   // result — локальная переменная в кадре add
  return result;         // передаёт result обратно вызывающей стороне
}

function main(): void {
  let total = add(3, 5); // вызывающая сторона: аргументы 3 и 5 переданы в add
  // total теперь хранит 8
}`}
  annotations={[
    { line: 1, text: "x и y — параметры: живут в кадре add, заранее заполнены аргументами вызывающей стороны" },
    { line: 2, text: "result — локальная переменная: кадр add теперь содержит x, y и result" },
    { line: 3, text: "return: помещает значение result в регистр, затем срабатывает RET" },
    { line: 7, text: "add(3,5): аргументы 3 и 5 копируются в новый кадр add как x и y" },
    { line: 8, text: "после RET: регистр с 8 прочитан; total присваивается 8 в кадре main" },
  ]}
  caption="add(3, 5): параметры входят в кадр add при вызове; возвращаемое значение выходит через регистр при RET."
/>
</Code>

<Trace lang="ru">
Трассировка `main()`, которая вызывает `add(3, 5)`. Каждая ячейка показывает содержимое
одного кадра.

<AlgoTrace
  id="trace-params-return"
  lang="ru"
  code={[
    "function add(x: number, y: number): number {",
    "  let result = x + y;",
    "  return result;",
    "}",
    "",
    "function main(): void {",
    "  let total = add(3, 5);",
    "}"
  ]}
  frames={[
    {
      caption: "main() вызвана. Кадр main положен. total ещё не присвоена.",
      line: 6,
      cells: [
        { value: "main\ntotal=?", mark: "active" }
      ]
    },
    {
      caption: "Строка 7: add(3,5) вызвана. Кадр add положен с x=3, y=5 заранее заполненными.",
      line: 7,
      cells: [
        { value: "main\ntotal=?", mark: "done" },
        { value: "add\nx=3,y=5", mark: "active" }
      ]
    },
    {
      caption: "Строка 2: result = x + y = 3 + 5 = 8. result сохранена в кадре add.",
      line: 2,
      cells: [
        { value: "main\ntotal=?", mark: "done" },
        { value: "add\nx=3,y=5\nres=8", mark: "active" }
      ]
    },
    {
      caption: "Строка 3: return result. Значение 8 помещено в регистр возврата. Срабатывает RET.",
      line: 3,
      cells: [
        { value: "main\ntotal=?", mark: "done" },
        { value: "add\nret=8", mark: "target" }
      ]
    },
    {
      caption: "Кадр add снят. Регистр возврата хранит 8. main возобновляется на строке 7.",
      line: 7,
      cells: [
        { value: "main\ntotal=?", mark: "active" }
      ]
    },
    {
      caption: "Строка 7 завершена: total = 8 (прочитана из регистра возврата). Кадр main обновлён.",
      line: 7,
      cells: [
        { value: "main\ntotal=8", mark: "active" }
      ]
    },
    {
      caption: "Строка 8: main возвращается. Кадр main снят. Стек пуст.",
      line: 8,
      cells: []
    }
  ]}
/>
</Trace>

<Inset kind="why" lang="ru">
Почему параметры копируются в кадр, а не используются совместно с вызывающей стороной?
Потому что каждый вызов функции нуждается в независимом рабочем пространстве. Если бы
`add` могла изменять `3` и `5` вызывающей стороны, вызвать `add` дважды с разными
аргументами было бы невозможно осмыслить. Копирование значений означает, что кадр
вызываемой функции полностью самодостаточен — она может свободно изменять ячейки `x`
и `y`, не затрагивая ничего в кадре вызывающей стороны. Это поведение «копировать при
входе» называется **передачей по значению** и является стандартом в большинстве языков
для примитивных значений, таких как числа.
</Inset>

<PracticeSet
  id="parameters-and-return-practice"
  lessonSlug="03-parameters-and-return"
  lang="ru"
  problems={[
    {
      prompt: "function multiply(a: number, b: number): number { return a * b; } — Сколько параметров у multiply?",
      answer: 2,
      hint: "Параметры объявлены в сигнатуре функции: a и b. Посчитай: 2.",
    },
    {
      prompt: "Вызывается multiply(4, 6). Какое значение возвращает multiply?",
      answer: 24,
      hint: "return a * b = 4 * 6 = 24.",
    },
    {
      prompt: "После возврата multiply(4, 6) сколько кадров стека на стеке (при условии, что она была вызвана из main и ничего больше не активно)?",
      answer: 1,
      hint: "Кадр multiply снимается при возврате. Остаётся только кадр main. 1 кадр.",
    },
    {
      prompt: "function square(n: number): number { return n * n; } — Вызывается square(7). Какое значение помещается в регистр возврата?",
      answer: 49,
      hint: "return n * n = 7 * 7 = 49.",
    },
    {
      prompt: "function add(x: number, y: number): number { let r = x + y; return r; } — Кадр add содержит x, y и r. Сколько именованных ячеек содержит кадр add?",
      answer: 3,
      hint: "x (параметр), y (параметр) и r (локальная переменная) — 3 именованные ячейки в кадре add.",
    },
  ]}
/>

<Check lang="ru">
  <Quiz
    id="parameters-and-return-check"
    pieceSlug="03-parameters-and-return"
    lang="ru"
    question="При вызове add(3, 5), где живут значения 3 и 5 во время выполнения add?"
    choices={[
      {
        label: "Они копируются в кадр стека add как параметры x и y, где живут как именованные ячейки в кадре",
        correct: true,
      },
      {
        label: "Они остаются в кадре main; add читает их из кадра main при необходимости",
        misconception: "Параметры копируются в собственный кадр вызываемой функции, а не читаются из кадра вызывающей. Это копирование делает каждый вызов независимым — у вызываемой функции есть своё рабочее пространство, и она не может случайно изменить переменные вызывающей стороны.",
      },
      {
        label: "Они помещаются в глобальную память, доступную любой функции",
        misconception: "Аргументы не помещаются в глобальную память. Они копируются в новый кадр стека, созданный для вызова. Кадр принадлежит только этому вызову — он снимается и уничтожается, когда функция возвращается.",
      },
    ]}
  />
</Check>

<Recap lang="ru">
**Параметры** — именованные ячейки в кадре стека функции, заранее заполненные
**аргументами** (значениями), которые вызывающая сторона передала в момент вызова.
Внутри функции параметры ведут себя точно как локальные переменные. Значения
**копируются** в кадр (передача по значению для примитивов), поэтому у вызываемой
функции есть независимое рабочее пространство. **Возвращаемое значение** производится
оператором `return`: значение помещается в регистр CPU, срабатывает `RET`, кадр
снимается, и вызывающая сторона читает возвращаемое значение из регистра. Параметры
несут данные *в* кадр при `CALL`; возвращаемое значение несёт данные *из* кадра при
`RET`.
</Recap>
