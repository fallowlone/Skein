---
concepts:
- call-stack
- stack-frame
- push-frame
- pop-frame
- return-address
- lifo-calls
estMin: 22
lang: ru
lessonType: coding
mathPrereqs: []
order: 2
prereqs:
- 01-what-a-function-is
- 04-stack-and-heap
- 02-fetch-decode-execute
- 01-a-variable-is-a-named-cell
slug: 02-the-call-stack
sources:
- https://www.nand2tetris.org/
- https://www.cs.cmu.edu/afs/cs/academic/class/15213-f15/www/lectures/09-machine-advanced.pdf
- https://developer.mozilla.org/en-US/docs/Glossary/Call_stack
status: ready
summary: Каждый вызов функции кладёт кадр стека (адрес возврата + локальные переменные)
  на стек. Возврат снимает его. В любой момент стек хранит ровно кадры всех активных
  вызовов.
title: Стек вызовов
track: base-cs
unit: 08-functions-and-the-call-stack
---



import Hook from "~/components/lesson/Hook.astro";
import PracticeSet from "~/components/math/PracticeSet.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
Ты знаешь, что инструкция `CALL` сохраняет адрес возврата и переходит к функции. Но
куда она его сохраняет? И что происходит, когда эта функция сама вызывает другую
функцию — адреса возврата где-то накапливаются? Где живут локальные переменные, пока
функция выполняется?

Ответ — **стек вызовов**: область памяти, которую рантайм использует как структуру
«последним вошёл — первым вышел». Каждый вызов **кладёт** блок данных, называемый
**кадром стека**, на стек; каждый возврат **снимает** верхний кадр обратно. Кадры
накапливаются по мере вложения вызовов и разматываются в обратном порядке по мере
возврата функций. После этого урока ты сможешь держать точное состояние стека в голове
в любой момент выполнения программы.
</Hook>

<Goal lang="ru">
После этого урока ты сможешь описать, что содержит кадр стека (адрес возврата и
локальные переменные), объяснить, что значит «положить кадр» и «снять кадр», трассировать
состояние стека через последовательность вложенных вызовов и возвратов, и объяснить,
почему стек вызовов — LIFO.
</Goal>

<Idea lang="ru">
**Стек вызовов** — это область стека в адресном пространстве (введена в Блоке 02,
уроке 04). Напомним: область стека растёт и сжимается автоматически по мере выполнения
функций. Каждый раз, когда вызывается функция, рантайм резервирует блок ячеек для этой
функции — этот блок называется **кадром стека** (или записью активации). Кадр хранит:

1. **Адрес возврата** — куда должен перейти счётчик команд, когда эта функция вернётся.
2. **Локальные переменные функции** — каждая переменная, объявленная внутри тела функции.

«Положить кадр» означает, что рантайм помещает новый кадр на вершину стека (указатель
стека сдвигается, резервируя новое место). «Снять кадр» означает, что рантайм удаляет
верхний кадр (указатель стека возвращается назад), освобождая эти ячейки для повторного
использования.

Стек вызовов — LIFO: функция, вызванная последней, всегда завершается первой. Её кадр
лежит сверху и снимается первым. Кадр под ним принадлежит вызывающей стороне, которая
возобновляет выполнение после снятия. Этот порядок LIFO обусловлен тем, как функции
вкладываются: последняя вызванная функция должна завершиться, прежде чем её вызывающая
сторона сможет продолжить.

Программа для трассировки ниже содержит три функции: `main` вызывает `outer`, которая
вызывает `inner`. Все три кадра появятся на стеке одновременно, прежде чем начнётся
разматывание.
</Idea>

<StackDiagram
  label="Стек вызовов с тремя вложенными кадрами: main внизу, outer над ним и inner сверху. Каждый вызов кладёт кадр сверху; каждый возврат снимает верхний кадр."
  hue="var(--d-hardware)"
  layers={[
    { label: "inner  (вершина — текущий вызов)", note: "c=3 · положен последним, снят первым" },
    { label: "outer", note: "b=2 · возврат → main" },
    { label: "main  (низ — первый вызов)", note: "a=1 · возврат → конец программы" },
  ]}
  caption="Стек в самой глубокой точке: main вызвал outer, outer вызвал inner. Новейший вызов (inner) лежит сверху. Каждый вызов КЛАДЁТ кадр наверх; каждый возврат СНИМАЕТ верхний кадр, поэтому стек разматывается inner → outer → main — последним вошёл, первым вышел."
/>

<Code lang="ru">

<AnnotatedCode
  code={`function inner(): void {
  let c = 3;           // единственная локальная переменная inner
}

function outer(): void {
  let b = 2;           // единственная локальная переменная outer
  inner();             // вызов inner — кладёт кадр inner
}

function main(): void {
  let a = 1;           // единственная локальная переменная main
  outer();             // вызов outer — кладёт кадр outer
}

main();                // точка входа — кладёт кадр main`}
  annotations={[
    { line: 2, text: "c живёт в кадре стека inner — не существует до вызова inner" },
    { line: 6, text: "b живёт в кадре стека outer" },
    { line: 7, text: "CALL inner: сохраняет адрес возврата (строка 8), кладёт кадр inner" },
    { line: 11, text: "a живёт в кадре стека main" },
    { line: 12, text: "CALL outer: сохраняет адрес возврата (строка 13), кладёт кадр outer" },
    { line: 15, text: "CALL main: запускает всю цепочку — кладёт кадр main" },
  ]}
  caption="Три вложенные функции. Каждый вызов кладёт кадр; каждый возврат снимает его. Трассировка ниже показывает точное состояние стека на каждом шаге."
/>
</Code>

<Trace lang="ru">
Пройди вызов `main()` шаг за шагом. Каждая ячейка представляет один кадр стека. Кадры
показаны снизу вверх (старейший слева, новейший справа).

<AlgoTrace
  id="trace-call-stack"
  lang="ru"
  code={[
    "function inner(): void {",
    "  let c = 3;",
    "}",
    "",
    "function outer(): void {",
    "  let b = 2;",
    "  inner();",
    "}",
    "",
    "function main(): void {",
    "  let a = 1;",
    "  outer();",
    "}",
    "",
    "main();"
  ]}
  frames={[
    {
      caption: "Стек пуст. Ни одна функция ещё не активна.",
      line: 15,
      cells: []
    },
    {
      caption: "main() вызвана. Кадр main положен: хранит адрес возврата + a (ещё не задана).",
      line: 10,
      cells: [
        { value: "main", mark: "active" }
      ]
    },
    {
      caption: "Строка 11: a = 1. Локальная переменная a сохранена в кадре main.",
      line: 11,
      cells: [
        { value: "main\na=1", mark: "active" }
      ]
    },
    {
      caption: "Строка 12: outer() вызвана. Кадр outer положен поверх кадра main.",
      line: 12,
      cells: [
        { value: "main\na=1", mark: "done" },
        { value: "outer", mark: "active" }
      ]
    },
    {
      caption: "Строка 6: b = 2. Локальная переменная b сохранена в кадре outer.",
      line: 6,
      cells: [
        { value: "main\na=1", mark: "done" },
        { value: "outer\nb=2", mark: "active" }
      ]
    },
    {
      caption: "Строка 7: inner() вызвана. Кадр inner положен сверху. На стеке три кадра.",
      line: 7,
      cells: [
        { value: "main\na=1", mark: "done" },
        { value: "outer\nb=2", mark: "done" },
        { value: "inner", mark: "active" }
      ]
    },
    {
      caption: "Строка 2: c = 3. Локальная переменная c сохранена в кадре inner.",
      line: 2,
      cells: [
        { value: "main\na=1", mark: "done" },
        { value: "outer\nb=2", mark: "done" },
        { value: "inner\nc=3", mark: "active" }
      ]
    },
    {
      caption: "Строка 3: inner возвращается. Кадр inner снят. На стеке снова два кадра.",
      line: 3,
      cells: [
        { value: "main\na=1", mark: "done" },
        { value: "outer\nb=2", mark: "active" }
      ]
    },
    {
      caption: "outer возобновляется на строке 8 (после вызова inner). Кадр outer теперь на вершине.",
      line: 8,
      cells: [
        { value: "main\na=1", mark: "done" },
        { value: "outer\nb=2", mark: "active" }
      ]
    },
    {
      caption: "Строка 8: outer возвращается. Кадр outer снят. На стеке только кадр main.",
      line: 8,
      cells: [
        { value: "main\na=1", mark: "active" }
      ]
    },
    {
      caption: "main возобновляется на строке 13 (после вызова outer). Кадр main снова на вершине.",
      line: 13,
      cells: [
        { value: "main\na=1", mark: "active" }
      ]
    },
    {
      caption: "Строка 13: main возвращается. Кадр main снят. Стек пуст. Программа завершена.",
      line: 13,
      cells: []
    }
  ]}
/>
</Trace>

<Inset kind="edgecase" lang="ru">
**Переполнение стека.** Область стека имеет конечный размер (обычно 1–8 МБ в современных
системах). Если вызовы вложены достаточно глубоко — чаще всего из-за безграничной
рекурсии — стек заканчивается. Когда указатель стека должен выйти за пределы области
стека, ОС посылает сигнал, завершающий программу с ошибкой «переполнение стека» (stack
overflow). Это не логическая ошибка в алгоритме; это физическое ограничение
зарезервированного диапазона адресов. В уроке 05 ты увидишь, как это происходит при
рекурсии.
</Inset>

<PracticeSet
  id="the-call-stack-practice"
  lessonSlug="02-the-call-stack"
  lang="ru"
  problems={[
    {
      prompt: "main вызывает outer; outer вызывает inner. Пока inner выполняется, сколько кадров стека находится на стеке?",
      answer: 3,
      hint: "Кадр main, кадр outer и кадр inner одновременно на стеке. По одному кадру на каждый активный вызов функции.",
    },
    {
      prompt: "inner возвращается. Сколько кадров стека теперь на стеке?",
      answer: 2,
      hint: "Кадр inner снимается при возврате inner. main и outer всё ещё активны. 3 - 1 = 2.",
    },
    {
      prompt: "Кадр стека хранит адрес возврата и локальные переменные функции. Функция f объявляет 3 локальные переменные. Сколько минимум единиц данных хранит кадр f (адрес возврата + локальные)?",
      answer: 4,
      hint: "1 адрес возврата + 3 локальные переменные = 4 единицы данных.",
    },
    {
      prompt: "Стек вызовов — LIFO. Если main вызвал outer, который вызвал inner, чей кадр удаляется первым при возврате функций?",
      answer: 1,
      hint: "LIFO = последним вошёл, первым вышел. Кадр inner был добавлен последним, поэтому удаляется первым. Введи 1 за кадр inner.",
    },
    {
      prompt: "outer вызывает inner. Адрес возврата inner указывает на инструкцию сразу после вызова inner() в outer. Если эта инструкция на строке 8, на какую строку переходит счётчик команд при возврате inner?",
      answer: 8,
      hint: "RET устанавливает счётчик команд на сохранённый адрес возврата — инструкцию сразу после CALL, строку 8.",
    },
  ]}
/>

<Check lang="ru">
  <Quiz
    id="the-call-stack-check"
    pieceSlug="02-the-call-stack"
    lang="ru"
    question="Что содержит кадр стека и когда он кладётся и снимается?"
    choices={[
      {
        label: "Кадр хранит адрес возврата и локальные переменные функции; кладётся при вызове функции и снимается при её возврате",
        correct: true,
      },
      {
        label: "Кадр хранит только адрес возврата; локальные переменные хранятся в отдельной области памяти",
        misconception: "Локальные переменные живут внутри кадра стека, а не в отдельной области. Кадр — единица выделения памяти для вызова функции: он хранит и адрес возврата, и все локальные переменные, объявленные в теле этой функции.",
      },
      {
        label: "Кадр кладётся при запуске программы и снимается только при её завершении",
        misconception: "Каждый отдельный вызов функции кладёт ровно один кадр, и каждый возврат снимает ровно один кадр. Стек динамически растёт и сжимается по мере вложения и разматывания вызовов — он не остаётся фиксированным от начала до конца программы.",
      },
    ]}
  />
</Check>

<Recap lang="ru">
**Стек вызовов** — это область стека в адресном пространстве, управляемая в порядке
LIFO. Когда вызывается функция, рантайм **кладёт** **кадр стека** на стек: блок ячеек,
хранящий **адрес возврата** и все **локальные переменные** функции. Когда функция
возвращается, рантайм **снимает** этот кадр — эти ячейки немедленно освобождаются. В
любой момент выполнения стек хранит ровно кадры всех текущих активных функций: от
самого внешнего вызывающего внизу до последней вызванной функции вверху. Кадры
добавляются и удаляются в строгом порядке «последним вошёл — первым вышел», потому что
последняя вызванная функция должна завершиться, прежде чем её вызывающая сторона сможет
продолжить. Если вызовы вложены слишком глубоко, область стека заканчивается, и программа
аварийно завершается с ошибкой переполнения стека.
</Recap>
