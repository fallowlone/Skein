---
concepts:
- recursion
- recursive-call
- base-case
- recursive-case
- stack-overflow
estMin: 22
lang: ru
lessonType: coding
mathPrereqs: []
order: 5
prereqs:
- 01-what-a-function-is
- 02-the-call-stack
- 03-parameters-and-return
- 04-scope
- 04-stack-and-heap
slug: 05-recursion-preview
sources:
- https://www.nand2tetris.org/
- https://developer.mozilla.org/en-US/docs/Glossary/Recursion
- https://www.cs.cmu.edu/afs/cs/academic/class/15213-f15/www/lectures/09-machine-advanced.pdf
status: ready
summary: Рекурсивная функция вызывает саму себя. Каждый вызов — просто ещё один кадр
  на стеке. Базовый случай останавливает спуск. Без базового случая стек переполняется.
title: Предварительный взгляд на рекурсию
track: base-cs
unit: 08-functions-and-the-call-stack
---



import Hook from "~/components/lesson/Hook.astro";
import PracticeSet from "~/components/math/PracticeSet.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Ты трассировал стеки вызовов, где `main` вызывает `outer`, который вызывает `inner`.
Каждый вызов кладёт новый кадр. Но в правилах нет ничего, что запрещало бы функции
вызывать *саму себя*. Что тогда происходит со стеком?

Ответ: ровно то же самое. Функция, вызывающая себя, — это просто ещё одна инструкция
`CALL`, указывающая на тот же адрес функции. Новый кадр кладётся с собственной копией
параметров и локальных переменных. Функция не «перезапускается» — создаётся второй,
независимый экземпляр кадра. Это **рекурсия**.

Критическое ограничение: что-то должно в конечном итоге остановить спуск, иначе стек
растёт до переполнения. Это условие остановки — **базовый случай**.
</Hook>

<Goal lang="ru">
После этого урока ты сможешь объяснить, что такое рекурсивная функция (функция, которая
вызывает саму себя), описать каждый рекурсивный вызов как новый независимый кадр на
стеке, определить базовый случай и рекурсивный случай в функции, и объяснить, что
вызывает переполнение стека в рекурсивной программе.
</Goal>

<Idea lang="ru">
Рекурсия имеет ровно две части:

1. **Базовый случай** — условие, при котором функция *не* вызывает себя и вместо этого
   возвращает прямое значение. Базовый случай — дно спуска. Без него рекурсия никогда
   не останавливается.

2. **Рекурсивный случай** — условие, при котором функция вызывает себя с «меньшим»
   входным значением, двигаясь к базовому случаю. «Меньший» означает ближе к базовому
   случаю, не обязательно численно меньше.

Каждый рекурсивный вызов создаёт новый кадр на стеке. Цепочка вызовов растёт вниз
(больше кадров), пока не достигнет базового случая. Затем цепочка разматывается вверх
(кадры снимаются), по мере того как каждый вызов возвращает результат вызывающей стороне
выше.

Функция для трассировки: `countdown(n)` концептуально выводит n, n-1, ..., 1, 0, и
затем ещё один вызов, который попадает в базовый случай. Базовый случай: `n < 0`
(остановка ниже нуля — срабатывает, когда `n` становится равным -1). Рекурсивный
случай вызывает `countdown(n - 1)`. Трассируется с `countdown(3)`.
</Idea>

<Code lang="ru">

<AnnotatedCode
  code={`function countdown(n: number): void {
  if (n < 0) return;          // базовый случай: остановиться ниже нуля
  // (в реальной программе здесь мы бы вывели n)
  countdown(n - 1);           // рекурсивный вызов: n уменьшается на 1
}`}
  annotations={[
    { line: 1, text: "n — параметр: каждый вызов получает свой n в своём кадре" },
    { line: 2, text: "базовый случай: когда n < 0, вернуться без следующего вызова — останавливает спуск" },
    { line: 4, text: "рекурсивный случай: CALL countdown с n-1; кладёт новый кадр для следующего уровня" },
  ]}
  caption="countdown(n): базовый случай n < 0 останавливает рекурсию. Каждый вызов кладёт один новый кадр. countdown(3) создаёт 5 кадров всего (n=3,2,1,0,-1 при базовом случае)."
/>
</Code>

<Trace lang="ru">
Трассировка `countdown(3)`. Каждая ячейка — один кадр. Кадры накапливаются по мере
уменьшения n, затем разматываются по мере возврата каждого вызова.

<AlgoTrace
  id="trace-recursion"
  lang="ru"
  code={[
    "function countdown(n: number): void {",
    "  if (n < 0) return;",
    "  countdown(n - 1);",
    "}"
  ]}
  frames={[
    {
      caption: "countdown(3) вызвана. Кадр для n=3 положен.",
      line: 1,
      cells: [
        { value: "n=3", mark: "active" }
      ]
    },
    {
      caption: "Строка 2: n=3 >= 0, базовый случай не сработал. Выполнение продолжается.",
      line: 2,
      cells: [
        { value: "n=3", mark: "active" }
      ]
    },
    {
      caption: "Строка 3: countdown(2) вызвана. Новый кадр для n=2 положен.",
      line: 3,
      cells: [
        { value: "n=3", mark: "done" },
        { value: "n=2", mark: "active" }
      ]
    },
    {
      caption: "Строка 2: n=2 >= 0, базовый случай не сработал.",
      line: 2,
      cells: [
        { value: "n=3", mark: "done" },
        { value: "n=2", mark: "active" }
      ]
    },
    {
      caption: "Строка 3: countdown(1) вызвана. Новый кадр для n=1 положен.",
      line: 3,
      cells: [
        { value: "n=3", mark: "done" },
        { value: "n=2", mark: "done" },
        { value: "n=1", mark: "active" }
      ]
    },
    {
      caption: "Строка 2: n=1 >= 0, базовый случай не сработал.",
      line: 2,
      cells: [
        { value: "n=3", mark: "done" },
        { value: "n=2", mark: "done" },
        { value: "n=1", mark: "active" }
      ]
    },
    {
      caption: "Строка 3: countdown(0) вызвана. Новый кадр для n=0 положен.",
      line: 3,
      cells: [
        { value: "n=3", mark: "done" },
        { value: "n=2", mark: "done" },
        { value: "n=1", mark: "done" },
        { value: "n=0", mark: "active" }
      ]
    },
    {
      caption: "Строка 2: n=0 >= 0, базовый случай не сработал.",
      line: 2,
      cells: [
        { value: "n=3", mark: "done" },
        { value: "n=2", mark: "done" },
        { value: "n=1", mark: "done" },
        { value: "n=0", mark: "active" }
      ]
    },
    {
      caption: "Строка 3: countdown(-1) вызвана. Новый кадр для n=-1 положен.",
      line: 3,
      cells: [
        { value: "n=3", mark: "done" },
        { value: "n=2", mark: "done" },
        { value: "n=1", mark: "done" },
        { value: "n=0", mark: "done" },
        { value: "n=-1", mark: "active" }
      ]
    },
    {
      caption: "Строка 2: n=-1 < 0 — БАЗОВЫЙ СЛУЧАЙ. Срабатывает return. Кадр n=-1 снят.",
      line: 2,
      cells: [
        { value: "n=3", mark: "done" },
        { value: "n=2", mark: "done" },
        { value: "n=1", mark: "done" },
        { value: "n=0", mark: "active" }
      ]
    },
    {
      caption: "Вызов n=0 возобновляется после вызова countdown(-1). Он возвращается. Кадр n=0 снят.",
      line: 4,
      cells: [
        { value: "n=3", mark: "done" },
        { value: "n=2", mark: "done" },
        { value: "n=1", mark: "active" }
      ]
    },
    {
      caption: "Вызов n=1 возобновляется. Он возвращается. Кадр n=1 снят.",
      line: 4,
      cells: [
        { value: "n=3", mark: "done" },
        { value: "n=2", mark: "active" }
      ]
    },
    {
      caption: "Вызов n=2 возобновляется. Он возвращается. Кадр n=2 снят.",
      line: 4,
      cells: [
        { value: "n=3", mark: "active" }
      ]
    },
    {
      caption: "Вызов n=3 возобновляется. Он возвращается. Кадр n=3 снят. Стек пуст.",
      line: 4,
      cells: []
    }
  ]}
/>
</Trace>

<Inset kind="edgecase" lang="ru">
**Что если базового случая нет?** Удали строку `if (n < 0) return;`. Теперь
`countdown(-1)` вызывает `countdown(-2)`, который вызывает `countdown(-3)`, и так
далее бесконечно. Каждый вызов кладёт новый кадр. Область стека (обычно 1–8 МБ)
заполняется. Когда указатель стека должен выйти за пределы зарезервированной области,
ОС завершает программу с ошибкой «переполнение стека» (stack overflow). Это не медленная
ошибка — она крашит программу за миллисекунды, потому что каждый вызов занимает
наносекунды и миллионы могут произойти до того, как стек исчерпается. Базовый случай
в рекурсивных функциях не опционален.
</Inset>

<PracticeSet
  id="recursion-preview-practice"
  lessonSlug="05-recursion-preview"
  lang="ru"
  problems={[
    {
      prompt: "Вызывается countdown(3). Включая вызов базового случая (n=-1), сколько всего кадров кладётся на стек?",
      answer: 5,
      hint: "Кадры для n=3, n=2, n=1, n=0 и n=-1 (базовый случай). Это 5 положенных кадров.",
    },
    {
      prompt: "При каком значении n срабатывает базовый случай в countdown?",
      answer: -1,
      hint: "Условие базового случая — n < 0. Первое значение, при котором n < 0 истинно, — n = -1.",
    },
    {
      prompt: "countdown(3) вызывает countdown(2), который вызывает countdown(1), ..., до базового случая. Максимальное количество кадров на стеке одновременно равно 5 (для n=3,2,1,0,-1). Если бы был вызван countdown(10), каков максимум кадров на стеке одновременно?",
      answer: 12,
      hint: "Для countdown(n) максимальная глубина — n+2 кадра: n=10, n=9, ..., n=0, n=-1 (базовый случай). 10+2 = 12.",
    },
    {
      prompt: "У рекурсивной функции нет базового случая. Что происходит при её вызове? Введи 1 за 'переполнение стека', 0 за 'возвращается нормально'.",
      answer: 1,
      hint: "Без базового случая функция вызывает себя бесконечно, кладя кадры до исчерпания области стека. ОС завершает программу с ошибкой переполнения стека. Введи 1.",
    },
    {
      prompt: "function factorial(n: number): number { if (n === 0) return 1; return n * factorial(n - 1); } — Каково значение n в базовом случае?",
      answer: 0,
      hint: "Базовый случай — когда n === 0, при этом функция возвращает 1 без рекурсивного вызова.",
    },
  ]}
/>

<Check lang="ru">
  <Quiz
    id="recursion-preview-check"
    pieceSlug="05-recursion-preview"
    lang="ru"
    question="Что делает рекурсию безопасной (т. е. что предотвращает переполнение стека)?"
    choices={[
      {
        label: "Базовый случай: условие, останавливающее рекурсию, возвращая значение без следующего рекурсивного вызова",
        correct: true,
      },
      {
        label: "Рантайм языка автоматически ограничивает рекурсивные вызовы до безопасной глубины",
        misconception: "Большинство рантаймов языков не ограничивают глубину рекурсии автоматически — они позволяют стеку расти до переполнения. Базовый случай в логике функции — ответственность программиста, а не рантайма. Некоторые языки предлагают отдельную функцию оптимизации хвостовых вызовов, но это другая тема.",
      },
      {
        label: "Каждый рекурсивный вызов использует меньший кадр стека, поэтому стек никогда не заканчивается",
        misconception: "Каждый рекурсивный вызов кладёт полноразмерный новый кадр, а не меньший. Размер кадра зависит от параметров и локальных переменных функции, которые не уменьшаются между рекурсивными вызовами. Кадры накапливаются в полном размере, пока базовый случай не остановит спуск.",
      },
    ]}
  />
</Check>

<Recap lang="ru">
**Рекурсивная функция** вызывает саму себя. Каждый вызов — просто `CALL` по тому же
адресу функции, который кладёт **новый независимый кадр** с собственной копией
параметров. Рекурсивные вызовы вкладываются на стеке точно так же, как любая другая
цепочка вызовов. Спуск продолжается, пока не достигнет **базового случая** — условия,
возвращающего значение без следующего вызова. Как только базовый случай сработал, стек
разматывается: кадры снимаются один за другим по мере возврата каждого вызова к
вызывающей стороне выше. Без базового случая кадры накапливаются без ограничений, пока
область стека не исчерпается и программа не аварийно завершится с ошибкой
**переполнения стека**. Каждая безопасная рекурсивная функция должна иметь базовый
случай, достижимый для всех допустимых входных данных.
</Recap>
