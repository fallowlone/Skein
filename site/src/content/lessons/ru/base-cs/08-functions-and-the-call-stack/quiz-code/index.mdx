---
concepts:
- stack-frame-trace
- recursion-depth
- pass-by-value
- return-value
- stack-overflow
estMin: 14
lang: ru
level: middle
order: 9
prereqs: []
slug: quiz-code
sources:
- https://developer.mozilla.org/en-US/docs/Glossary/Call_stack
- https://developer.mozilla.org/en-US/docs/Glossary/Recursion
- https://www.cs.cmu.edu/afs/cs/academic/class/15213-f15/www/lectures/09-machine-advanced.pdf
status: ready
summary: "Прочитайте четыре маленьких сниппета на TypeScript, проследите стек кадр за кадром, посчитайте глубину рекурсии и предскажите, что реально делают передача параметров и возврат."
title: 'Функции и стек вызовов: чтение кода и трассировка'
track: base-cs
unit: 08-functions-and-the-call-stack
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Чтение стека вызовов по коду — навык, к которому ведёт весь юнит. Для каждого сниппета прогоните его в голове: кладите кадр на каждый вызов, снимайте один на каждый возврат и следите, куда текут значения.
</Hook>

<Goal lang="ru">
Потренируйтесь трассировать реальный код так, как его выполняет машина, — считая активные кадры, предсказывая глубину рекурсии и рассуждая о том, что пересекает границу кадра, когда аргументы входят, а значение выходит.
</Goal>

## Сниппет 1 — вложенные вызовы и глубина стека

```typescript
function inner(): void {
  let c = 3;          // единственная локальная переменная inner
}

function outer(): void {
  let b = 2;          // единственная локальная переменная outer
  inner();            // кладёт кадр inner
}

function main(): void {
  let a = 1;          // единственная локальная переменная main
  outer();            // кладёт кадр outer
}

main();
```

<Quiz
  id="08-functions-and-the-call-stack-code-1"
  lessonSlug="08-functions-and-the-call-stack"
  lang="ru"
  question="В момент, когда inner выполняет let c = 3, сколько кадров на стеке и какой из них хранит a = 1?"
  choices={[
    { label: "Три кадра — main (хранит a = 1) внизу, outer (b = 2) в середине, inner (c = 3) сверху. Кадр main хранит a; это нижний кадр, потому что main была вызвана первой.", correct: true },
    { label: "Один кадр, потому что сейчас выполняется только inner.", misconception: "outer и main ещё не вернулись — они приостановлены ниже inner, ожидая его завершения. Их кадры остаются на стеке, поэтому сосуществуют три кадра." },
    { label: "Три кадра, причём a = 1 в кадре inner сверху.", misconception: "a — локальная переменная main, поэтому она живёт в кадре main внизу, а не в inner. Локальные переменные каждой функции живут только в её собственном кадре." },
    { label: "Два кадра, так как у inner нет собственных вызываемых функций.", misconception: "Число кадров — это число активных вызовов, а не вызываемых функций. main, outer и inner все активны, пока выполняется inner, поэтому кадров три." },
  ]}
/>

## Сниппет 2 — передача параметров (по значению)

```typescript
function bump(x: number): void {
  x = x + 100;        // переприсваивает собственную копию x в bump
}

function main(): void {
  let v = 1;
  bump(v);            // аргумент 1 копируется в x функции bump
  // чему равно v здесь?
}
```

<Quiz
  id="08-functions-and-the-call-stack-code-2"
  lessonSlug="08-functions-and-the-call-stack"
  lang="ru"
  question="После возврата bump(v) чему равно v в main и почему?"
  choices={[
    { label: "v по-прежнему 1. bump(v) скопировал значение 1 в параметр x функции bump; переприсвоение x изменило только кадр bump. v в main — отдельная ячейка, не затронутая передачей по значению.", correct: true },
    { label: "v равно 101, потому что bump прибавил к нему 100.", misconception: "bump прибавил 100 к своей копии x, а не к v. При передаче по значению аргумент копируется в кадр вызываемой функции, поэтому переменная вызывающей стороны никогда не меняется от переприсвоения параметра." },
    { label: "v равно undefined, потому что bump его израсходовал.", misconception: "Передача значения как аргумента не расходует и не удаляет переменную вызывающей стороны. v в main сохраняет своё исходное значение 1 всё время." },
    { label: "v равно 100, потому что x сбрасывается в 0 в новом кадре перед прибавлением 100.", misconception: "x инициализируется копией аргумента (1), а не 0, поэтому x становится 101 внутри bump — но это живёт в кадре bump и никогда не утекает обратно. v в main остаётся 1." },
  ]}
/>

## Сниппет 3 — глубина рекурсии

```typescript
function sumTo(n: number): number {
  if (n === 0) return 0;        // базовый случай
  return n + sumTo(n - 1);      // рекурсивный случай
}

sumTo(4);
```

<Quiz
  id="08-functions-and-the-call-stack-code-3"
  lessonSlug="08-functions-and-the-call-stack"
  lang="ru"
  question="Для sumTo(4) каково максимальное число кадров, активных одновременно, и какое значение возвращается?"
  choices={[
    { label: "Пять кадров на пике — sumTo(4), sumTo(3), sumTo(2), sumTo(1), sumTo(0) — все активны до того, как базовый случай начнёт разматывание; вызов возвращает 4 + 3 + 2 + 1 + 0 = 10.", correct: true },
    { label: "Один кадр, переиспользуемый для каждого рекурсивного вызова; возвращает 10.", misconception: "Каждый рекурсивный вызов кладёт новый независимый кадр со своим n; кадр n = 4 приостановлен на 4 + sumTo(3), пока вся цепочка не вернётся. В самой глубокой точке сосуществуют пять кадров." },
    { label: "Четыре кадра; возвращает 10.", misconception: "Вызов базового случая sumTo(0) тоже кладёт кадр перед возвратом 0, поэтому пик — пять кадров, а не четыре, даже если он ничего не добавляет к сумме." },
    { label: "Пять кадров; возвращает 0, потому что базовый случай возвращает 0.", misconception: "Базовый случай даёт 0, но каждый приостановленный кадр добавляет свой n при разматывании: 0 + 1 + 2 + 3 + 4 = 10. Базовый случай лишь останавливает спуск." },
  ]}
/>

## Сниппет 4 — отсутствующий базовый случай

```typescript
function blow(n: number): number {
  return n + blow(n + 1);   // нет базового случая: n только растёт
}

blow(0);
```

<Quiz
  id="08-functions-and-the-call-stack-code-4"
  lessonSlug="08-functions-and-the-call-stack"
  lang="ru"
  question="Что происходит при запуске blow(0) и в чём первопричина?"
  choices={[
    { label: "Программа аварийно падает с переполнением стека. blow всегда рекурсирует с бо́льшим n, поэтому ни один вызов не удовлетворяет условию остановки; кадры громоздятся, пока фиксированная область стека (1-8 МБ) не исчерпается и ОС не завершит программу.", correct: true },
    { label: "Возвращает очень большое число, как только n станет достаточно большим.", misconception: "Базового случая нет, а n только растёт, поэтому ни один вызов никогда не возвращает обычное значение, чтобы начать разматывание. Ничего не суммируется — программа падает раньше, чем что-либо вернёт." },
    { label: "Зацикливается навсегда без падения, как while(true).", misconception: "В отличие от цикла, каждый рекурсивный вызов потребляет новый кадр стека. Стек конечен, поэтому он переполняется и падает за миллисекунды, а не крутится бесконечно." },
    { label: "Компилятор отказывается собирать это, потому что рекурсия бесконечна.", misconception: "Компиляторы обычно не доказывают достижимость базового случая; это собирается нормально. Сбой — это переполнение стека во время выполнения, а не ошибка компиляции." },
  ]}
/>

<Recap lang="ru">
Каждый сниппет читается прогоном стека в голове: вложенные вызовы держат кадры всех вызывающих функций активными ниже текущего; скопированный аргумент означает, что переприсвоение параметра никогда не трогает переменную вызывающей стороны; рекурсивный вызов кладёт новый кадр на уровень, поэтому пиковая глубина — это число вложенных активных вызовов (включая базовый случай); а рекурсия без достижимого базового случая растит стек без ограничений, пока он не переполнится. Трассируйте укладку-при-вызове и снятие-при-возврате — и ответы выпадают сами.
</Recap>
