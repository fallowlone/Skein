---
concepts:
- function-call
- stack-frame
- return-address
- pass-by-value
- scope-lifetime
- stack-overflow
estMin: 13
lang: ru
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://developer.mozilla.org/en-US/docs/Glossary/Call_stack
- https://www.cs.cmu.edu/afs/cs/academic/class/15213-f15/www/lectures/09-machine-advanced.pdf
- https://www.nand2tetris.org/
status: ready
summary: "Синтез всего юнита в вопросах с выбором: вызовы, кадры стека, адреса возврата, передача параметров, область видимости и время жизни, переполнение стека."
title: 'Функции и стек вызовов: обзор с выбором ответа'
track: base-cs
unit: 08-functions-and-the-call-stack
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, охватывающих весь юнит. Каждый просит рассуждать о том, что реально делает машина при вызове функции, — не воспроизвести определение, а предсказать состояние стека, путь управления и где живут данные.
</Hook>

<Goal lang="ru">
Убедитесь, что вы связываете механизм вызова, кадр стека, передачу параметров, область видимости и время жизни, а также переполнение стека в одну целостную модель того, как работает вызов функции на уровне машины.
</Goal>

<Quiz
  id="08-functions-and-the-call-stack-choice-1"
  lessonSlug="08-functions-and-the-call-stack"
  lang="ru"
  question="Тело функции живёт по одному адресу. Одна и та же функция вызывается из трёх разных мест программы. Как каждый вызов возвращается к нужной вызывающей стороне?"
  choices={[
    { label: "Каждый CALL сохраняет свой адрес возврата — инструкцию сразу после этого конкретного места вызова — и RET ставит счётчик команд обратно на тот адрес, который сохранил именно этот вызов. Одно тело, три места вызова, три независимо сохранённых адреса возврата.", correct: true },
    { label: "В три места вызова копируются три копии тела функции, поэтому каждое возвращается локально.", misconception: "CALL — это переход по одному фиксированному адресу, а не копирование. Тело существует ровно один раз. Между местами вызова различается адрес возврата, сохранённый при вызове, а не тело." },
    { label: "Функция всегда возвращается в то место, откуда её вызвали первый раз.", misconception: "Фиксированная цель возврата обслуживала бы только одну вызывающую сторону. Весь смысл сохранения адреса возврата на каждый вызов в том, чтобы одно тело обслуживало вызовы откуда угодно и каждый раз возвращалось в нужное место." },
    { label: "Компилятор переименовывает функцию для каждого места вызова, чтобы они не конфликтовали.", misconception: "Есть одно тело по одному адресу независимо от того, сколько имён или мест вызова на него ссылаются. Корректный возврат обеспечивается во время выполнения сохранённым адресом возврата, а не переименованием при компиляции." },
  ]}
/>

<Quiz
  id="08-functions-and-the-call-stack-choice-2"
  lessonSlug="08-functions-and-the-call-stack"
  lang="ru"
  question="main вызывает outer, outer вызывает inner. Пока выполняется inner, что лежит на стеке и в каком порядке?"
  choices={[
    { label: "Три кадра: main внизу, outer над ним, inner сверху. Самая недавно вызванная функция лежит сверху и завершится (снимется) первой — стек работает по принципу LIFO.", correct: true },
    { label: "Только кадр inner — более ранние кадры были сняты, когда начались более глубокие вызовы.", misconception: "Кадр вызывающей функции не снимается, когда она вызывает другую функцию; он ждёт ниже. main и outer ещё активны и всё ещё на стеке, пока выполняется inner. Все три сосуществуют." },
    { label: "Три кадра, причём inner внизу, а main сверху, так как main была положена последней.", misconception: "main была положена первой и лежит внизу; inner была положена последней и лежит сверху. Самый новый кадр всегда сверху и снимается первым." },
    { label: "Один объединённый кадр, хранящий переменные всех трёх функций.", misconception: "Каждый вызов получает собственный отдельный кадр со своим адресом возврата и локальными переменными. Это отдельные блоки, уложенные друг на друга, а не слитые воедино." },
  ]}
/>

<Quiz
  id="08-functions-and-the-call-stack-choice-3"
  lessonSlug="08-functions-and-the-call-stack"
  lang="ru"
  question="Функция add(x, y) вызывается как add(3, 5). Где живут значения 3 и 5, пока выполняется add, и что это гарантирует?"
  choices={[
    { label: "Они копируются в собственный кадр add как параметры x и y. Копия даёт add приватное рабочее пространство — она может переприсвоить x или y, не затрагивая ничего в кадре вызывающей стороны (передача по значению для чисел).", correct: true },
    { label: "Они остаются в кадре вызывающей стороны, и add читает их через границу кадра по имени.", misconception: "Вызываемая функция не может назвать локальные переменные вызывающей — область видимости это запрещает. Аргументы копируются в кадр вызываемой функции при вызове, поэтому add работает со своими ячейками, а не вызывающей стороны." },
    { label: "Они записываются в общую глобальную область, которую может читать любая функция.", misconception: "Аргументы не являются глобальными. Они копируются в новый кадр, приватный для этого вызова и выбрасываемый при возврате. Ничего глобального здесь нет." },
    { label: "Хранятся только их адреса, поэтому изменение x внутри add меняет значение вызывающей стороны.", misconception: "Для примитивных чисел копируются сами значения (передача по значению). Переприсвоение x внутри add меняет только ячейку add, оставляя аргумент вызывающей стороны нетронутым." },
  ]}
/>

<Quiz
  id="08-functions-and-the-call-stack-choice-4"
  lessonSlug="08-functions-and-the-call-stack"
  lang="ru"
  question="Функция f объявляет локальную переменную result. После возврата f код в main пытается использовать result. Что верно?"
  choices={[
    { label: "result была ячейкой в кадре f. Когда f вернулась, этот кадр был снят, а ячейка освобождена, поэтому result больше не существует как именованное значение. main не может на неё сослаться — её область видимости была телом f, а время жизни закончилось вместе с кадром f.", correct: true },
    { label: "result автоматически перемещается в кадр main, чтобы main могла продолжать её использовать.", misconception: "Локальные переменные никуда не мигрируют при возврате. Кадр снимается, а его ячейки освобождаются для повторного использования. Автоматической передачи локальных переменных вызываемой функции вызывающей нет." },
    { label: "result сохраняется в памяти до конца программы; main лишь нужно правильное имя.", misconception: "Ячейки повторно используются следующим вызовом почти сразу. Даже до перезаписи имя выходит за пределы области видимости — обращение к этим ячейкам было бы ошибкой использования после освобождения, а не корректным доступом." },
    { label: "main может прочитать result, потому что кадр f всё ещё ниже кадра main на стеке.", misconception: "После возврата f её кадр исчезает, а не лежит ниже main. И даже пока кадр вызывающей стороны на стеке, правила области видимости запрещают называть локальные переменные другой функции." },
  ]}
/>

<Quiz
  id="08-functions-and-the-call-stack-choice-5"
  lessonSlug="08-functions-and-the-call-stack"
  lang="ru"
  question="countdown(n) возвращается, когда n &lt; 0, иначе вызывает countdown(n - 1). Каково максимальное число кадров на стеке одновременно для countdown(3)?"
  choices={[
    { label: "Пять — по одному кадру для n = 3, 2, 1, 0 и -1. Самый глубокий вызов (базовый случай при n = -1) достигается до того, как снимется хоть один кадр, поэтому все пять сосуществуют на мгновение. В общем случае countdown(n) достигает глубины n + 2.", correct: true },
    { label: "Один — кадр повторно используется для каждого рекурсивного вызова, так как это та же функция.", misconception: "Рекурсивный вызов — это новый CALL по тому же адресу; он кладёт совершенно новый независимый кадр со своим n. Кадры не переиспользуются — они накапливаются, пока базовый случай не начнёт их разматывать." },
    { label: "Четыре — n = 3, 2, 1, 0; базовый случай не кладёт кадр.", misconception: "Вызов базового случая countdown(-1) — это тоже реальный вызов, кладущий кадр; он просто сразу возвращается без рекурсии. Этот кадр считается, давая пять на пике глубины." },
    { label: "Три — рекурсия считает только вызовы, которые рекурсируют дальше.", misconception: "Каждый вызов, включая тот, что попадает в базовый случай, кладёт кадр. Глубина на пике — это число вложенных активных вызовов, здесь n + 2, а не число рекурсивных шагов." },
  ]}
/>

<Quiz
  id="08-functions-and-the-call-stack-choice-6"
  lessonSlug="08-functions-and-the-call-stack"
  lang="ru"
  question="Рекурсивная функция выпущена без достижимого базового случая. Что происходит во время выполнения и почему так быстро?"
  choices={[
    { label: "Каждый вызов кладёт кадр, и ни один никогда не снимается, поэтому область стека (обычно 1-8 МБ) заполняется, и программа аварийно завершается с переполнением стека — за миллисекунды, потому что каждый вызов стоит лишь наносекунды и миллионы успевают произойти до исчерпания области.", correct: true },
    { label: "Программа работает медленно, но в конце концов завершается, когда закончатся входные данные.", misconception: "Без достижимого базового случая входные данные никогда не удовлетворяют условию остановки; спуск неограничен. Программа не завершается — она аварийно падает, когда область стека исчерпана." },
    { label: "Рантайм обнаруживает цикл и молча возвращает значение по умолчанию.", misconception: "Большинство рантаймов не обнаруживают бесконечную рекурсию и не подставляют значение. Они позволяют стеку расти, пока ОС не подаст сигнал переполнения стека и не завершит процесс." },
    { label: "Происходит утечка памяти кучи, пока на машине не закончится ОЗУ.", misconception: "Кадры стека потребляют фиксированную область стека, а не кучу. Сбой — это переполнение стека относительно небольшого зарезервированного диапазона, поэтому он происходит почти мгновенно, а не после исчерпания всего ОЗУ." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — это один механизм, увиденный с разных сторон: CALL сохраняет адрес возврата и переходит к фиксированному телу; каждый вызов кладёт LIFO-кадр стека, хранящий этот адрес возврата плюс локальные переменные функции; параметры копируются внутрь при CALL, а результат отдаётся обратно при RET; область видимости и время жизни локальной переменной ограничены её кадром, поэтому при возврате она исчезает; а рекурсия — это та же укладка кадра, повторённая снова, безопасная только при достижимом базовом случае — без него кадры громоздятся, пока стек не переполнится.
</Recap>
