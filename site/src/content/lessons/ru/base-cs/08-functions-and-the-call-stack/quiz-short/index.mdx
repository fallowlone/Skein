---
concepts:
- function-call
- stack-frame
- pass-by-value
- scope-lifetime
- recursion
- stack-overflow
estMin: 13
lang: ru
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://developer.mozilla.org/en-US/docs/Glossary/Call_stack
- https://developer.mozilla.org/en-US/docs/Glossary/Recursion
- https://www.nand2tetris.org/
status: ready
summary: "Подсказки на свободное припоминание по всему юниту. Сначала ответьте по памяти, затем откройте образцовый ответ и сравните — именно припоминание закрепляет механизм."
title: 'Функции и стек вызовов: обзор на припоминание'
track: base-cs
unit: 08-functions-and-the-call-stack
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. Для каждой подсказки произнесите или запишите полный ответ по памяти, прежде чем открыть образцовый, — именно усилие вытащить механизм из головы закрепляет его.
</Hook>

<Goal lang="ru">
Восстановите спину юнита по памяти — что такое функция на уровне машины, что хранит кадр стека, как параметры и значения возврата пересекают границу, почему область видимости и время жизни заканчиваются вместе с кадром, и что рекурсия делает со стеком.
</Goal>

<RetrievalDrawer
  client:load
  id="08-functions-and-the-call-stack-short-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое функция на уровне машины и что именно делает инструкция CALL?", a: "Функция — это именованный блок последовательных инструкций, начинающийся по фиксированному адресу в памяти; имя — удобство, которое компилятор разрешает в этот адрес. CALL — это особый переход: он сохраняет адрес возврата (адрес инструкции сразу после места вызова) и затем ставит счётчик команд на первую инструкцию функции. Когда функция завершается, RET читает этот сохранённый адрес возврата и переходит обратно, поэтому управление возобновляется ровно там, где вызывающая сторона остановилась. Поскольку адрес возврата сохраняется на каждый вызов, одно тело по одному адресу может обслуживать вызовы откуда угодно и корректно возвращаться к каждому." },
    { q: "Что содержит кадр стека и когда он кладётся и снимается?", a: "Кадр стека (запись активации) — это блок ячеек, который рантайм резервирует для одного вызова функции. Он хранит адрес возврата и все локальные переменные функции (и её параметры). Он кладётся при вызове функции — указатель стека сдвигается, резервируя место, — и снимается при возврате функции — указатель стека сдвигается обратно, освобождая эти ячейки для повторного использования. В любой момент стек хранит ровно по одному кадру на каждый активный вызов." },
    { q: "Почему стек вызовов работает по принципу LIFO и что это значит для того, какой кадр завершается первым?", a: "Стек вызовов работает по принципу «последним пришёл — первым ушёл» из-за того, как вкладываются вызовы: самая недавно вызванная функция должна завершиться раньше, чем продолжит её вызывающая сторона. Если main вызывает outer, который вызывает inner, кадр inner лежит сверху и снимается первым; затем возобновляется outer и снимается следующим; main возобновляется последней. Кадр вызывающей функции никогда не снимается, пока над ним есть активный вызываемый — он приостановлен ниже и ждёт. Поэтому самый новый кадр всегда снимается следующим." },
    { q: "Как параметры попадают в функцию и как значение возврата выходит, и почему передача по значению безопасна?", a: "При CALL аргументы вызывающей стороны копируются в новый кадр как параметры, где ведут себя как предзаполненные локальные переменные. Когда тело доходит до return, значение помещается в регистр процессора; срабатывает RET, кадр снимается, и вызывающая сторона читает значение из этого регистра. Передача по значению (по умолчанию для примитивных чисел) копирует аргумент, поэтому у вызываемой функции своё приватное рабочее пространство — переприсвоение параметра меняет только ячейку вызываемого и никогда переменную вызывающего. Параметры несут данные внутрь при CALL; значение возврата несёт одно значение наружу при RET." },
    { q: "В чём разница между областью видимости локальной переменной и её временем жизни, и что с ней происходит при возврате функции?", a: "Область видимости — это где в коде имя видно; область видимости локальной переменной — её собственное тело функции, поэтому ни одна другая функция не может её назвать. Время жизни — это как долго она существует в памяти; локальная переменная создаётся при укладке её кадра и уничтожается при снятии её кадра. При возврате кадр снимается, а ячейки освобождаются для повторного использования, поэтому локальная переменная перестаёт существовать как именованное значение; обращение к этим ячейкам после было бы ошибкой использования после освобождения. Поэтому две функции могут обе объявить x без конфликта: каждый x — отдельная ячейка в отдельном кадре." },
    { q: "Что такое рекурсия в терминах стека, каковы её две части и что вызывает переполнение стека?", a: "Рекурсивная функция вызывает саму себя; каждый самовызов — это просто ещё один CALL по тому же адресу, кладущий новый независимый кадр со своими параметрами. У неё две части: базовый случай (условие, возвращающее значение без рекурсии — дно спуска) и рекурсивный случай (самовызов на входе, ближе к базовому случаю). Цепочка растёт вниз, пока не сработает базовый случай, затем разматывается по мере снятия кадров. Если базовый случай никогда не достигается, кадры накапливаются без ограничений, пока фиксированная область стека (около 1-8 МБ) не исчерпается и ОС не завершит программу с переполнением стека — за миллисекунды, так как каждый вызов стоит наносекунды." },
  ]}
/>

<Recap lang="ru">
Если вы смогли восстановить каждый ответ по памяти, вы держите спину юнита: функция — это именованный адрес, CALL сохраняет адрес возврата и переходит, каждый вызов кладёт LIFO-кадр из адреса-возврата-плюс-локальные-переменные, параметры копируются внутрь, а значение возврата приходит наружу через эту границу, область видимости и время жизни локальной переменной заканчиваются при снятии её кадра, а рекурсия — это та же укладка кадра, повторённая снова, ограниченная только достижимым базовым случаем до переполнения стека.
</Recap>
