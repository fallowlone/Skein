---
slug: 02-assignment
lang: ru
track: base-cs
unit: 06-variables-and-state
order: 2
title: "Присваивание"
summary: "Присваивание — операция записи: среда выполнения вычисляет правую часть, затем записывает результат в ячейку, привязанную к переменной слева. Предыдущее значение исчезает. Имя остаётся; меняется только содержимое ячейки."
estMin: 22
status: ready
lessonType: coding
prereqs: ["01-a-variable-is-a-named-cell"]
mathPrereqs: []
concepts: ["assignment", "store-operation", "let-declaration", "const-declaration", "overwrite"]
sources:
  - https://tc39.es/ecma262/multipage/ecmascript-language-statements-and-declarations.html
  - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/Assignment
  - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/let
---

import Hook from "../../../../../../components/lesson/Hook.astro";
import Goal from "../../../../../../components/lesson/Goal.astro";
import Idea from "../../../../../../components/lesson/Idea.astro";
import Code from "../../../../../../components/lesson/Code.astro";
import Trace from "../../../../../../components/lesson/Trace.astro";
import Check from "../../../../../../components/lesson/Check.astro";
import Recap from "../../../../../../components/lesson/Recap.astro";
import Inset from "../../../../../../components/lesson/Inset.astro";
import AnnotatedCode from "../../../../../../components/algo/AnnotatedCode.astro";
import AlgoTrace from "../../../../../../components/algo/AlgoTrace.astro";
import PracticeSet from "../../../../../../components/math/PracticeSet.astro";
import Quiz from "../../../../../../components/pedagogy/Quiz.astro";

<Hook>
Ты объявил переменную. Ты привязал имя к ячейке и записал в неё начальное значение.
Что дальше? Программы должны менять значения со временем: счётчики растут, суммы
накапливаются, флаги состояний переключаются. Механизм, который меняет значение в
ячейке, называется **присваиванием**.

Присваивание выглядит обманчиво просто: `x = 5`. Но за этой строкой скрывается
точная машинная операция. Среда выполнения вычисляет правую часть, берёт результат и
записывает его в ячейку, привязанную к имени слева. Старое значение ячейки исчезает.
Всё остальное остаётся неизменным — привязка, имя, адрес — меняется только содержимое
ячейки.

Осознание того, что `=` в программировании — не знак математического равенства, а
**команда записи** — это одна из важнейших ранних перестроек мышления при обучении
программированию.
</Hook>

<Goal lang="ru">
После этого урока ты сможешь объяснить, что делает присваивание на уровне ячейки
памяти, проследить последовательность присваиваний и определить, какое значение
содержит переменная на каждом шаге, а также отличить `let` (допускает переприсваивание)
от `const` (одно присваивание).
</Goal>

<Idea lang="ru">
**Присваивание как операция записи.** Когда среда выполнения выполняет `x = expr`,
она производит два шага по порядку:

1. **Вычисляет** выражение в правой части `expr`. Это даёт значение — паттерн битов.
   Выражение может быть литералом (`5`), именем переменной (`y`), арифметикой
   (`x + 1`) или любым другим вычислимым выражением.
2. **Записывает** это значение в ячейку памяти, адрес которой привязан к имени `x`.
   Предыдущее содержимое ячейки перезаписывается и теряется.

Имя в левой части `=` не вычисляется — оно не читается. Оно преобразуется в адрес, и
этот адрес используется как назначение записи. Вот почему `=` в коде произносится как
«получает» или «присваивается», а не «равно»: `x = 5` означает «x *получает* 5», а не
«x *равно* 5».

**Первое присваивание и переприсваивание.** В жизни переменной есть два различных
момента:

- **Объявление с начальным значением** — `let x = 0;` — резервирует ячейку,
  устанавливает привязку и записывает начальное значение `0`. Это первое присваивание.
- **Переприсваивание** — `x = 42;` — ищет существующую привязку, находит адрес ячейки
  и записывает новое значение `42`, перезаписывая `0`. Привязка не изменяется.

**`const` запрещает переприсваивание.** TypeScript и JavaScript также предоставляют
`const`:

```ts
const y = 10;
y = 20; // TypeError: Assignment to constant variable.
```

`const` означает, что привязка запечатана после первого присваивания — дальнейшие
записи в эту привязку не разрешены. Ячейка записывается ровно один раз. Речь идёт не
о том, что значение «заморожено» в каком-то глубоком смысле (объекты, объявленные как
`const`, всё ещё могут иметь изменяемые свойства — это рассматривается в уроке 04), а
о том, что привязка имя→ячейка принимает ровно одну запись.

**Порядок вычисления важен.** Рассмотрим `x = x + 1`. Правая часть вычисляется
первой: среда выполнения читает текущее значение `x`, добавляет 1, затем записывает
результат обратно в ту же ячейку. Шаги: (1) читаем `x` → получаем текущее значение,
(2) добавляем 1, (3) записываем результат в `x`. Ячейка оказывается на единицу больше.
Старое значение исчезло.
</Idea>

<Code lang="ru">

<AnnotatedCode
  code={`// Объявление — резервирует ячейку и записывает начальное значение.
let score = 0;          // ячейка (допустим, addr 1042) → содержит 0

// Переприсваивание — перезаписывает содержимое ячейки.
score = 10;             // addr 1042 → теперь содержит 10
score = score + 5;      // addr 1042 → читаем 10, добавляем 5, пишем 15
score = score * 2;      // addr 1042 → читаем 15, умножаем на 2, пишем 30

// Имя 'score' не изменилось; изменялось только значение в ячейке.
console.log(score);     // 30

// const запечатывает привязку после первой записи.
const MAX = 100;
// MAX = 200;           // выбросит TypeError — привязка запечатана`}
  annotations={[
    { line: 2,  text: "let: объявляет переменную, резервирует ячейку, записывает начальное значение 0" },
    { line: 5,  text: "Переприсваивание: перезаписывает ячейку. Старое значение 0 потеряно." },
    { line: 6,  text: "Сначала вычисляется правая часть: читаем score (10), прибавляем 5, результат 15. Затем пишем 15 в ячейку." },
    { line: 7,  text: "Сначала вычисляется правая часть: читаем score (15), умножаем на 2, результат 30. Затем пишем 30." },
    { line: 10, text: "Чтение 'score' возвращает то, что сейчас в ячейке: 30." },
    { line: 13, text: "const: первая (и единственная) запись — при объявлении. Привязка теперь запечатана." },
    { line: 14, text: "Вторая запись в const-привязку вызывает ошибку во время выполнения." },
  ]}
/>
</Code>

<Trace lang="ru">
Проследим каждый шаг: как `score` меняется от объявления через три переприсваивания.

<AlgoTrace
  id="trace-assignment"
  lang="ru"
  code={[
    "let score = 0;",
    "score = 10;",
    "score = score + 5;",
    "score = score * 2;",
    "console.log(score);"
  ]}
  frames={[
    {
      caption: "До выполнения. Ячейки не выделены.",
      line: 1,
      cells: []
    },
    {
      caption: "Строка 1: let score = 0. Ячейка выделена (addr 1042). Привязка установлена. Записано значение 0.",
      line: 1,
      cells: [
        { value: "0", mark: "active" }
      ]
    },
    {
      caption: "Строка 2: score = 10. Правая часть — литерал 10. Записываем 10 в ячейку 1042. Старое значение 0 потеряно.",
      line: 2,
      cells: [
        { value: "10", mark: "active" }
      ]
    },
    {
      caption: "Строка 3: score = score + 5. Правая часть: читаем score (10), добавляем 5 → 15. Записываем 15 в ячейку 1042.",
      line: 3,
      cells: [
        { value: "15", mark: "active" }
      ]
    },
    {
      caption: "Строка 4: score = score * 2. Правая часть: читаем score (15), умножаем на 2 → 30. Записываем 30 в ячейку 1042.",
      line: 4,
      cells: [
        { value: "30", mark: "active" }
      ]
    },
    {
      caption: "Строка 5: console.log(score). Читаем ячейку 1042 → 30. Вывод: 30.",
      line: 5,
      cells: [
        { value: "30", mark: "done" }
      ]
    }
  ]}
/>
</Trace>

<Inset kind="mistake" lang="ru">
`x = x + 1` выглядит как замкнутый круг — «x это x плюс один?» — но это не уравнение.
Это двухшаговая команда: сначала вычисли `x + 1` (читая старое значение), затем
сохрани результат обратно в `x`. Правая часть вычисляется полностью до того, как
что-либо записывается. После выполнения строки `x` содержит на единицу больше, чем
раньше. Читать и писать одну и ту же ячейку в одном операторе — совершенно допустимо
и встречается крайне часто.
</Inset>

<Inset kind="edgecase" lang="ru">
Что происходит при обмене значений двух переменных? Наивный подход:

```ts
let a = 1;
let b = 2;
a = b;   // a → 2, b всё ещё 2
b = a;   // b → 2 (! хотели 1)
```

Проблема: как только выполнилось `a = b`, исходное значение `a` (1) потеряно. Решение:
сохранить исходное значение заранее:

```ts
let temp = a;  // temp → 1, a → 1, b → 2
a = b;         // a → 2
b = temp;      // b → 1
```

Теперь обе ячейки содержат поменянные значения. Этот классический трёхшаговый обмен —
прямое следствие того, что присваивание является деструктивной перезаписью.
</Inset>

<PracticeSet
  id="assignment-practice"
  lessonSlug="02-assignment"
  lang="ru"
  problems={[
    {
      prompt: "let x = 3; x = 7; Какое значение хранит x после второй строки?",
      answer: 7,
      hint: "Переприсваивание x = 7 записывает 7 в ячейку x, перезаписывая 3. x теперь равно 7.",
    },
    {
      prompt: "let n = 10; n = n + 4; Какое значение хранит n?",
      answer: 14,
      hint: "Сначала вычисляется правая часть: читаем n (10), добавляем 4 → 14. Затем 14 записывается в ячейку n.",
    },
    {
      prompt: "let a = 5; let b = a; a = 99; Какое значение хранит b?",
      answer: 5,
      hint: "При выполнении 'let b = a' значение 5 было скопировано в ячейку b. Последующее изменение a не влияет на b.",
    },
    {
      prompt: "let c = 2; c = c * c; c = c * c; Какое значение хранит c?",
      answer: 16,
      hint: "Шаг 1: c = 2*2 = 4. Шаг 2: c = 4*4 = 16.",
    },
    {
      prompt: "let x = 1; x = x + x; x = x + x; Какое значение хранит x?",
      answer: 4,
      hint: "Шаг 1: x = 1+1 = 2. Шаг 2: x = 2+2 = 4.",
    },
  ]}
/>

<Check lang="ru">
  <Quiz
    id="assignment-check"
    pieceSlug="02-assignment"
    lang="ru"
    question="Что именно происходит в памяти при выполнении 'x = x + 1'?"
    choices={[
      {
        label: "Среда выполнения читает текущее значение x, прибавляет 1 и записывает результат обратно в ту же ячейку памяти, перезаписывая старое значение.",
        correct: true,
      },
      {
        label: "Для обновлённого значения выделяется новая ячейка памяти, и x привязывается к новой ячейке.",
        misconception: "Переприсваивание не выделяет новую ячейку. Существующая ячейка по адресу x перезаписывается на месте. Привязка (имя→адрес) остаётся неизменной; меняется только содержимое ячейки.",
      },
      {
        label: "Выражение x + 1 сохраняется в ячейке невычисленным; значение x вычисляется только при следующем чтении.",
        misconception: "JavaScript и TypeScript используют жадное вычисление. При выполнении 'x = x + 1' правая часть немедленно вычисляется до конкретного значения, и это значение записывается в ячейку. Отложенного вычисления здесь нет.",
      },
    ]}
  />
</Check>

<Recap lang="ru">
**Присваивание** — операция записи, состоящая из двух шагов: вычислить выражение
правой части, получив значение, затем записать это значение в ячейку памяти,
привязанную к имени слева. Предыдущее содержимое ячейки **перезаписывается и теряется**.
Привязка имя→адрес не изменяется. Когда в выражении TypeScript встречается `=`,
читай его как «получает», а не «равно» — это команда записи, а не математическое
утверждение. `let` объявляет переменную и допускает последующее переприсваивание.
`const` выполняет ровно одно присваивание при объявлении и запечатывает привязку от
дальнейших записей. Выражения в правой части, использующие ту же переменную
(`x = x + 1`), безопасны: правая часть полностью вычисляется до выполнения любой
записи.
</Recap>
