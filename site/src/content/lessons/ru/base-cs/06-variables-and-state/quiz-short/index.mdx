---
concepts:
- binding
- assignment
- mutation
- state
- reference-vs-value
- aliasing
estMin: 13
lang: ru
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Data_structures#primitive_values
- https://tc39.es/ecma262/multipage/ecmascript-language-statements-and-declarations.html
status: ready
summary: "Промпты на свободное припоминание по юниту — сначала ответь по памяти, затем раскрой модельный ответ и сравни."
title: 'Variables and state: свободное припоминание'
track: base-cs
unit: 06-variables-and-state
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт проговори или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие восстановить модель памяти закрепляет её.
</Hook>

<Goal lang="ru">
Восстанови хребет юнита по памяти: чем на самом деле является variable, что assignment и mutation делают с ячейкой, что такое состояние и почему копирование примитива отличается от копирования reference.
</Goal>

<RetrievalDrawer
  client:visible
  id="06-variables-and-state-short-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое variable в терминах имени, binding и ячейки?", a: "Variable — это человекочитаемое имя, привязанное (binding) к ячейке памяти. Имя (например, 'score') — метка в исходном коде; оно не несёт значения и никогда не хранится в ячейке. Ячейка — физическое место в памяти, держащее значение. Binding — связь между ними, хранимая в symbol table рантайма: когда код упоминает имя, рантайм находит привязанный адрес и читает или пишет туда. Имя и значение — разного рода вещи: имя фиксировано после объявления, а значение в ячейке может меняться много раз. Объявление переменной резервирует ячейку и записывает binding имя→адрес." },
    { q: "Объясни assignment как машинную операцию и почему '=' стоит читать как «получает», а не «равно».", a: "Assignment — операция записи из двух упорядоченных шагов: сначала вычислить правое выражение до конкретного значения, затем записать это значение в ячейку, привязанную к имени слева, перезаписав и уничтожив то, что там было. Левое имя никогда не читается как значение — оно разрешается в адрес-приёмник. Читать '=' как «равно» неверно, потому что 'x = x + 1' — не уравнение (оно было бы невозможно); это команда «x получает x + 1» — прочитать текущий x, прибавить 1, записать результат обратно в ту же ячейку. Правая часть полностью вычисляется до любой записи, поэтому самоссылающиеся присваивания безопасны." },
    { q: "Что такое mutation и чем состояние отличается от истории мутаций?", a: "Mutation — изменение ячейки на месте: assignment перезаписывает битовый шаблон по существующему адресу, оставляя адрес прежним, а прежнее значение — потерянным. Состояние — полный снимок текущих значений всех живых переменных в один момент, фотография каждой ячейки прямо сейчас. Состояние — кумулятивный итог всех прошлых мутаций, а не список этих мутаций: счётчик может дойти до 10 одним большим шагом или десятью маленькими, давая разные истории, но идентичное состояние. Каждый assignment меняет ровно одну ячейку, поэтому остальной снимок не затронут." },
    { q: "Сопоставь семантику копирования (по значению) и reference-семантику. Что лежит в ячейке в каждом случае?", a: "Примитивы — number, string, boolean, null, undefined — достаточно малы, чтобы лежать прямо в ячейке как биты своего значения. Присваивание одного другому копирует эти биты, поэтому каждая переменная держит независимое значение, и изменение одной никогда не затрагивает другую. Объекты и массивы — составные значения, живущие на куче; ячейка переменной держит лишь reference — адрес объекта на куче. Присваивание объекта другой переменной копирует этот адрес, а не объект, поэтому обе ячейки указывают на единственный объект. Решающий вопрос при любом баге — держит ли эта ячейка значение или reference?" },
    { q: "Что такое aliasing, почему он вызывает неожиданные баги и как его избежать?", a: "Aliasing — это когда два (или больше) имени держат один reference, так что указывают на один разделяемый объект на куче. Он кусается, потому что mutation через одно имя (obj2.field = x) сразу видна через каждый другой алиас (obj1.field читает то же) — код, «который не трогал obj1», как будто его изменил. Фикс — делать явную копию, когда нужна независимость: shallow-копия вроде { ...obj } или [...arr] даёт новый верхнеуровневый объект, хотя вложенные объекты внутри всё ещё разделяются (для этого нужна deep-копия). Понимание, что assignment разделяет, а не копирует объекты, превращает это из загадок в ожидаемое поведение." },
    { q: "Что именно защищает const и что он НЕ защищает?", a: "const запечатывает binding после единственного начального присваивания: ячейку нельзя перенаправить, поэтому 'x = somethingElse' падает. Это всё, что он делает. const не замораживает значение за binding. Для примитива менять больше нечего. Но для объекта const лишь фиксирует, какой reference держит ячейка — объект на куче полностью изменяем, поэтому 'obj.field = 1' и добавление свойств разрешены. Чтобы сделать содержимое неизменным, нужен отдельный механизм, например Object.freeze; один const охраняет binding, а не объект." },
  ]}
/>

<Recap lang="ru">
Если ты восстановил каждый ответ по памяти, ты держишь хребет юнита: variable — это имя, привязанное (binding) к ячейке, и имя и значение различны; assignment — разрушающая запись, читаемая как «получает»; mutation меняет ячейку на месте, а состояние — снимок всех текущих ячеек; примитивы копируются по значению, а объекты — по reference, что и делает aliasing — и порождаемые им баги — предсказуемыми; а const запечатывает binding, но никогда — содержимое за ним.
</Recap>
