---
concepts:
- binding
- assignment
- mutation
- aliasing
- reference-vs-value
- const
estMin: 13
lang: ru
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Data_structures#primitive_values
- https://tc39.es/ecma262/multipage/ecmascript-language-statements-and-declarations.html
status: ready
summary: "Тест с выбором на синтез по всему юниту — binding vs значение, assignment как запись, mutation и состояние, копирование vs reference, ловушки aliasing."
title: 'Variables and state: тест с выбором ответа'
track: base-cs
unit: 06-variables-and-state
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый — это решение о том, что реально лежит в ячейке: значение или reference, и что меняется при assignment или mutation. Разберись с моделью памяти — и баги aliasing перестанут быть сюрпризом.
</Hook>

<Goal lang="ru">
Убедись, что связываешь четыре идеи юнита: variable — это имя, привязанное (binding) к ячейке; assignment перезаписывает эту ячейку; mutation меняет состояние на месте; примитивы копируются, а объекты разделяются по reference.
</Goal>

<Quiz
  id="06-variables-and-state-choice-1"
  lessonSlug="06-variables-and-state"
  lang="ru"
  question="После 'let score = 0' программа выполняет 'score = 42'. Что изменилось в памяти?"
  choices={[
    { label: "Изменилось только содержимое ячейки, привязанной к 'score' — 0 перезаписан на 42. Имя и binding имя→адрес остались ровно теми же.", correct: true },
    { label: "Само имя 'score' стало равно 42.", misconception: "Имя — это метка в исходном коде; оно никогда не хранит значение и не «становится» числом. Имя 'score' остаётся 'score', а привязанная к нему ячейка теперь содержит 42." },
    { label: "Под 42 выделена новая ячейка, и 'score' перепривязан к ней.", misconception: "Re-assignment пишет на месте в существующую ячейку. Новая ячейка не выделяется и binding не меняется — меняется только содержимое ячейки." },
    { label: "Теперь хранятся и 0, и 42, причём 42 помечено как текущее.", misconception: "Assignment — это разрушающая перезапись. Прежнее значение 0 пропало; ячейка хранит только 42. Память не ведёт историю прошлых значений." },
  ]}
/>

<Quiz
  id="06-variables-and-state-choice-2"
  lessonSlug="06-variables-and-state"
  lang="ru"
  question="Почему читать '=' как «равно» — ловушка, и как читать вместо этого?"
  choices={[
    { label: "Читай как «получает». 'x = x + 1' — не утверждение, что x равен x+1, а команда записи: сначала вычисли правую часть, потом запиши результат в ячейку x. Старое значение перезаписывается.", correct: true },
    { label: "Читать как «равно» нормально — 'x = x + 1' просто утверждает истинное уравнение.", misconception: "Как уравнение 'x = x + 1' невозможно. Это вообще не уравнение, а команда из двух шагов — вычислить x+1, затем записать обратно — поэтому читается «x получает x+1»." },
    { label: "Читать как «сравнить» — assignment проверяет, равны ли стороны.", misconception: "Сравнение — другой оператор (== или ===). Одиночный '=' выполняет запись, а не проверку. Он всегда сохраняет правое значение в левую ячейку." },
    { label: "Правая часть сохраняется невычисленной и считается лишь при следующем чтении x.", misconception: "TypeScript вычисляет жадно. Правая часть немедленно сводится к конкретному значению, и оно записывается в ячейку сейчас — отложенного вычисления нет." },
  ]}
/>

<Quiz
  id="06-variables-and-state-choice-3"
  lessonSlug="06-variables-and-state"
  lang="ru"
  question="Баг-репорт: «Я изменил obj2, а obj1 мутировал сам по себе.» В коде было 'let obj2 = obj1'. Что произошло на самом деле?"
  choices={[
    { label: "'let obj2 = obj1' скопировал reference, а не объект. obj1 и obj2 — алиасы (aliasing) одного объекта на куче, поэтому mutation через любое имя видна через другое. Чтобы получить независимую копию, копируй объект явно (например, {...obj1}).", correct: true },
    { label: "Assignment установил живую связь, синхронизирующую два объекта.", misconception: "Никакой постоянной синхронизации нет. Assignment однократно скопировал адрес; дальше обе переменные независимо держат один адрес, указывающий на единственный объект. Двух объектов никогда не было." },
    { label: "obj1 мутировал, потому что объекты всегда передаются глубокой копией.", misconception: "Объекты не копируются глубоко при assignment — копируется только reference. Единый объект разделяется, поэтому mutation и проявилась на обоих именах." },
    { label: "Это невозможно; mutation obj2 не может затронуть obj1.", misconception: "Это и есть определяющее поведение aliasing. Оба имени держат один reference на один объект, поэтому 'obj2.field = x' — та же запись, которую читает 'obj1.field'." },
  ]}
/>

<Quiz
  id="06-variables-and-state-choice-4"
  lessonSlug="06-variables-and-state"
  lang="ru"
  question={"'const user = { name: \"A\" }; user.name = \"B\";' выполняется без ошибки. Но 'const x = 1; x = 2;' падает. Почему?"}
  choices={[
    { label: "const запечатывает binding — ячейку нельзя перенаправить — но не замораживает содержимое по этому reference. Перепривязка user (user = ...) отклоняется, а mutation объекта, на который он указывает (user.name = ...), разрешена.", correct: true },
    { label: "const на объекте молча игнорируется, поэтому разрешено всё.", misconception: "const действует и для объектов — перепривязка 'user = somethingElse' всё равно падает. Он лишь не запрещает mutation свойств объекта; он запечатывает только то, какой reference держит ячейка." },
    { label: "Объекты освобождены от const, потому что они не значения.", misconception: "Объекты — это значения; ячейка переменной держит значение-reference. const запечатывает этот reference от замены, но объект на куче за ним не заморожен, поэтому mutation свойства допустима." },
    { label: "Первая строка работает только потому, что свойство уже существовало.", misconception: "Существование свойства тут ни при чём. const охраняет binding, а не содержимое; добавление нового свойства (user.age = 1) тоже было бы разрешено." },
  ]}
/>

<Quiz
  id="06-variables-and-state-choice-5"
  lessonSlug="06-variables-and-state"
  lang="ru"
  question="'let a = [1,2]; let b = a; b.push(3);' — затем код в другом месте читает a.length, ожидая 2. Почему он видит 3?"
  choices={[
    { label: "Массивы — это объекты, поэтому действует reference-семантика. b держит тот же reference, что и a; push мутирует один разделяемый массив на месте, так что a.length равно 3. Независимая длина требует явной копии (например, let b = [...a]).", correct: true },
    { label: "push создаёт новый массив и перепривязывает b, оставляя a длиной 2.", misconception: "push мутирует существующий массив на месте; он не создаёт новый массив и ничего не перепривязывает. Так как a и b — алиасы этого массива, новая длина видна через a." },
    { label: "a.length равно 3 только потому, что оба объявлены через let.", misconception: "let vs const не влияет на разделение. Массивы разделяются, потому что reference скопирован при 'let b = a'; ключевое слово объявления этого не меняет." },
    { label: "Массивы — примитивы, поэтому b получил независимую копию.", misconception: "Массивы в JavaScript — объекты, не примитивы. Присваивание копирует их reference, поэтому оба имени указывают на единственный массив — по значению копируются только примитивы (number, string, boolean)." },
  ]}
/>

<Quiz
  id="06-variables-and-state-choice-6"
  lessonSlug="06-variables-and-state"
  lang="ru"
  question="В чём разница между «состоянием» программы и «мутациями» (mutation), которые его породили?"
  choices={[
    { label: "Состояние — это полный снимок текущих значений всех живых переменных в один момент. Mutation — это одно изменение одной ячейки на месте. Состояние — кумулятивный итог всех прошлых мутаций, а не список самих мутаций.", correct: true },
    { label: "Состояние — это полная история каждого assignment с момента запуска.", misconception: "Состояние — текущий снимок, а не история. Дойти до score=10 одним большим инкрементом или десятью маленькими — это разные истории, но идентичное состояние: важны только нынешние значения." },
    { label: "Состояние — это только переменные, изменённые последним assignment.", misconception: "Состояние охватывает каждую живую переменную, включая давно нетронутые. Их текущие значения по-прежнему часть условия программы прямо сейчас." },
    { label: "Mutation и состояние — одно и то же, описанное двумя способами.", misconception: "Они различны: состояние — это значения, которые держатся сейчас; mutation — акт изменения одного из этих значений на месте. Может быть неизменное состояние (только const) вообще без mutation." },
  ]}
/>

<Recap lang="ru">
Сквозная линия: variable — это имя, привязанное (binding) к ячейке, и имя и значение — разные вещи. Assignment — разрушающая запись, «получает», а не «равно». Mutation меняет ячейку на месте, а состояние — это снимок всех текущих содержимых ячеек. Решающее различие — что лежит в ячейке: примитивное значение (копируется при assignment, поэтому изменения не распространяются) или reference на объект кучи (копируется как адрес, создавая алиасы, где одна mutation видна через каждое имя). const запечатывает, какой reference держит ячейка, но никогда — содержимое за ним.
</Recap>
