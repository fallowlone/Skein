---
concepts:
- aliasing
- mutation
- reference-vs-value
- scope-shadowing
- binding
estMin: 14
lang: ru
level: middle
order: 9
prereqs: []
slug: quiz-code
sources:
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Data_structures#primitive_values
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/let
status: ready
summary: "Читай короткие сниппеты TypeScript — aliasing, копирование vs reference, scope shadowing и swap — и предсказывай, какое значение держит каждая переменная и почему."
title: 'Variables and state: чтение кода'
track: base-cs
unit: 06-variables-and-state
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Быстрее всего проверить, уложилась ли модель памяти, — протрассировать реальный код вручную. Для каждого сниппета следи за ячейками: где значение, где reference, к какому binding разрешается имя и какие записи — это mutation общего объекта. Предскажи вывод, прежде чем смотреть варианты.
</Hook>

<Goal lang="ru">
Потренируйся трассировать, что держит каждая переменная после каждой строки — отличая скопированный примитив от разделяемого reference, внутренний binding от внешнего и mutation на месте от свежего выделения памяти.
</Goal>

## Сниппет 1 — общий объект

```ts
const original = { count: 0 };
const copy = original;       // присваивает reference, а не объект
copy.count = 5;              // mutation через copy
console.log(original.count); // ?
```

<Quiz
  id="06-variables-and-state-code-1"
  lessonSlug="06-variables-and-state"
  lang="ru"
  question="Что выведет последняя строка и почему?"
  choices={[
    { label: "5 — 'const copy = original' копирует reference, поэтому оба имени указывают на один объект на куче. 'copy.count = 5' мутирует этот единственный объект на месте, и original читает то же поле.", correct: true },
    { label: "0 — copy является независимым дубликатом original, поэтому его mutation не трогает original.", misconception: "Assignment копирует reference, а не объект. Объект один; оба имени — его алиасы. Чтобы получить независимый дубликат, надо копировать явно, например { ...original }." },
    { label: "Падает — оба const, поэтому 'copy.count = 5' недопустимо.", misconception: "const запечатывает binding (нельзя copy = что-то другое), но не замораживает объект. Mutation свойства объекта по reference при const разрешена." },
    { label: "undefined — присваивание original в copy очищает поля original.", misconception: "Assignment никогда не очищает источник. Он копирует значение-reference в ячейку copy; original сохраняет свой reference и поле, которое затем mutation меняет на 5." },
  ]}
/>

## Сниппет 2 — примитив и объект рядом

```ts
let n = 10;
let m = n;          // копирует значение 10
m = m + 1;          // меняет собственную ячейку m

let p = { v: 10 };
let q = p;          // копирует reference
q.v = q.v + 1;      // мутирует общий объект

console.log(n, q === p, p.v); // ?
```

<Quiz
  id="06-variables-and-state-code-2"
  lessonSlug="06-variables-and-state"
  lang="ru"
  question="Что будет выведено?"
  choices={[
    { label: "10 true 11 — n примитив, поэтому m получил независимую копию, и изменение m оставило n равным 10. p и q разделяют один объект (q === p — true), поэтому mutation через q делает p.v равным 11.", correct: true },
    { label: "11 true 11 — инкремент m заодно инкрементировал n, потому что они начинали равными.", misconception: "Примитивы копируются по значению; m и n — отдельные ячейки. Изменение m никогда не трогает n. Разделяется только объектная сторона, поэтому меняется p.v, а не n." },
    { label: "10 false 10 — p и q разные объекты, поэтому q === p — false и p.v остаётся 10.", misconception: "'let q = p' копирует reference, поэтому q и p указывают на один объект: q === p — true, а mutation через q видна как p.v === 11." },
    { label: "10 true 10 — q === p — true, но mutation q не может изменить p.", misconception: "Если q === p, за обоими именами ровно один объект. Mutation через любое имя — та же запись, поэтому p.v становится 11." },
  ]}
/>

## Сниппет 3 — scope shadowing

```ts
let x = 1;
{
  let x = 2;        // новый binding во внутреннем блоке
  x = x + 10;       // пишет в ячейку внутреннего x
  console.log(x);   // внутренний
}
console.log(x);     // внешний
```

<Quiz
  id="06-variables-and-state-code-3"
  lessonSlug="06-variables-and-state"
  lang="ru"
  question="Что выведут две строки console.log, по порядку?"
  choices={[
    { label: "12, затем 1 — внутренний 'let x' объявляет отдельную переменную (свою ячейку), которая затеняет (shadowing) внешний x внутри блока. Все записи там попадают во внутреннюю ячейку; внешний x не тронут и остаётся 1.", correct: true },
    { label: "12, затем 12 — x только один, поэтому внутреннее переприсваивание выносится во внешний.", misconception: "'let x' внутри блока создаёт новый binding на новую ячейку. Внутри блока имя x разрешается в эту внутреннюю ячейку; внешний x сохраняет своё значение 1." },
    { label: "2, затем 1 — внутренний блок не может переприсвоить x, поэтому он остаётся 2 внутри.", misconception: "Внутренний x — обычный let; 'x = x + 10' читает 2 и пишет 12 во внутреннюю ячейку. Блок свободно мутирует свой binding." },
    { label: "Падает — нельзя объявить x дважды.", misconception: "Повторное объявление x во вложенном блоке легально: у каждого scope свой binding. Внутреннее объявление затеняет внешнее, а не конфликтует с ним." },
  ]}
/>

## Сниппет 4 — swap

```ts
let a = 1;
let b = 2;
a = b;
b = a;
console.log(a, b);  // ?
```

<Quiz
  id="06-variables-and-state-code-4"
  lessonSlug="06-variables-and-state"
  lang="ru"
  question="Что будет выведено и какой фикс, если задумывался обмен a и b?"
  choices={[
    { label: "2 2 — 'a = b' перезаписывает a на 2, уничтожая исходную 1, поэтому 'b = a' лишь копирует 2 обратно. Так как assignment — разрушающая перезапись, для обмена нужен temp: let t = a; a = b; b = t.", correct: true },
    { label: "2 1 — два присваивания корректно меняют значения местами.", misconception: "После 'a = b' старое значение a (1) пропало. 'b = a' читает новое a (2), поэтому оба заканчиваются на 2. Корректный swap сначала сохраняет исходное во временную." },
    { label: "1 2 — присваивания взаимно гасятся и оставляют исходные значения.", misconception: "Они не гасятся. 'a = b' перезаписывает a на месте на 2; исходная 1 потеряна до выполнения 'b = a', поэтому b становится 2, а не 1." },
    { label: "1 1 — assignment копирует левое значение в правое.", misconception: "Assignment пишет правое значение в левую ячейку, а не наоборот. 'a = b' делает a равным 2; оба заканчиваются на 2." },
  ]}
/>

<Recap lang="ru">
Трассировка вручную — это и есть навык: скопированный примитив (m, n) живёт в своей ячейке и игнорирует изменения другого; скопированный reference (p, q) делает оба имени алиасами одного объекта, поэтому mutation распространяется и === — true; внутренний 'let' создаёт новый binding, который затеняет внешнее имя, не трогая его ячейку; а наивный 'a = b; b = a' проваливается, потому что каждый assignment — разрушающая перезапись, поэтому реальный swap требует временную. Следи за ячейками — и ответ никогда не будет сюрпризом.
</Recap>
