---
concepts:
- contiguous-memory
- index-and-offset
- constant-time-access
- object-key-value
- references-and-indirection
- graph-of-cells
estMin: 13
lang: ru
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://see.stanford.edu/materials/icsppcs107/07-Arrays-The-Full-Story.pdf
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Data_structures
status: ready
summary: "Подсказки на свободное вспоминание по всему юниту. Сначала ответь своими словами, затем открой модельный ответ и сравни."
title: 'Данные в памяти: тест на свободное вспоминание'
track: base-cs
unit: 09-data-in-memory
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Вспоминание сильнее перечитывания. На каждую подсказку скажи или запиши полный ответ по памяти, прежде чем открыть модельный ответ — именно усилие вспоминания закрепляет раскладку.
</Hook>

<Goal lang="ru">
Восстанови костяк юнита, не подглядывая: почему array contiguous и из равных ячеек, как индекс становится адресом, почему доступ константный, чем отличается объект и почему reference превращают вложенные данные в graph of cells.
</Goal>

<RetrievalDrawer
  client:load
  id="09-data-in-memory-short-retrieval"
  lang="ru"
  questions={[
    { q: "Какова форма array в памяти и почему все ячейки должны быть одного размера?", a: "Array — это один неразрывный ряд ячеек, лежащих вплотную друг к другу — contiguous-память, без зазоров. Все элементы имеют один тип, поэтому каждому нужно одинаковое число байтов; это фиксированное число — element size, и оно делает каждую ячейку одинаковой по размеру. Свойства связаны: равные ячейки, упакованные contiguous, образуют идеально регулярную сетку, поэтому позиция любой ячейки предсказуема. Array из N элементов по S байтов — это один цельный блок в N x S байтов, а имя array ссылается на начало этого блока." },
    { q: "Дай формулу адреса элемента i и назови каждую часть.", a: "address элемента i = base_address + (index x element_size). Base address — это байтовый адрес первой ячейки array, фиксированный якорь, на который ссылается имя array. Index — позиция элемента, отсчитанная от 0. Element size — байты, которые занимает один элемент. Произведение index x element_size — это offset: на сколько байтов после base начинается ячейка. Так что берёшь base, умножаешь индекс на element size, чтобы получить offset, прибавляешь к base и читаешь там. Длина и другие элементы в формуле не появляются." },
    { q: "Почему первый элемент имеет index 0, а не index 1?", a: "Index — это offset, измеренный в элементах от начала, а не бытовой счёт «какой элемент». Первый элемент лежит ровно на base address — он ноль элементов после начала — поэтому его offset 0 x element_size = 0, а адрес base + 0 = base. Единственный индекс, дающий нулевой offset, это 0, и первый элемент — единственный с нулевым offset. Нумерация навязана арифметикой адреса, а не выбрана соглашением." },
    { q: "Почему доступ к любому элементу — константное время и что это сломало бы?", a: "Адрес любого элемента находится той же фиксированной работой — одно умножение, одно сложение, одно чтение — независимо от индекса и длины array. Поэтому доступ к arr[0] стоит ровно столько же, сколько к arr[9999]; это константное время. Это работает только потому, что array contiguous и из равных ячеек: единый element size позволяет одним умножением найти любой offset. Если бы элементы были разного размера, пришлось бы суммировать размеры всех предыдущих по одному (уже не константа), а будь зазоры — умножение попало бы на неверный адрес." },
    { q: "Чем объект отличается от array и почему порядок полей объекта не важен?", a: "Оба группируют несколько значений под одним именем, но различаются способом доступа к значению внутри. Array достаёт значение по числовой позиции — arr[2] вычисляет адрес из индекса. Объект достаёт значение по именованному key — user.age находит слот с меткой age и читает его. Каждое поле — это key-value pair: key — имя, value — данные. Так как доступ по имени, написанный порядок полей не несёт смысла: { a: 1, b: 2 } и { b: 2, a: 1 } это один и тот же объект. Используй array для упорядоченной последовательности однотипных значений; объект — для именованного набора разнозначных частей." },
    { q: "Как array может хранить сотню объектов разного размера и сохранять сетку из равных ячеек?", a: "Малое value фиксированного размера (например число) вмещается в ячейку напрямую, но объект переменного размера и может расти, поэтому не вмещается. Вместо этого каждая ячейка хранит reference — адрес объекта в другом месте. Так как каждый reference одного малого фиксированного размера, array остаётся contiguous-сеткой из равных ячеек и base + i x element_size по-прежнему работает; изменилось лишь содержимое ячейки — с value на адрес. Чтение поля тогда два прыжка: арифметика в array, чтобы прочитать reference, затем проход по reference к объекту. Доведённые до конца, вложенные данные — это graph of cells: одни хранят value, другие хранят адреса; держать вложенную структуру значит держать один reference в этот граф." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, у тебя есть костяк юнита: array — contiguous-ряд из равных ячеек; индекс становится адресом через base + i x element_size, поэтому index 0 первый и поэтому доступ константный; объект достаёт значения по имени, так что порядок полей не важен; а reference позволяют сетке фиксированного размера указывать на объекты переменного размера, превращая любую вложенную структуру в graph of cells, связанный адресами.
</Recap>
