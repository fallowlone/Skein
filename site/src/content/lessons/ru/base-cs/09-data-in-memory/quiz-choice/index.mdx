---
concepts:
- contiguous-memory
- array-indexing
- key-value-object
- references-and-indirection
- equal-size-cells
- graph-of-cells
estMin: 13
lang: ru
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://see.stanford.edu/materials/icsppcs107/07-Arrays-The-Full-Story.pdf
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Data_structures
status: ready
summary: "Тест с выбором на синтез по всему юниту: contiguous-раскладка, арифметика индекса, объекты с именованными полями и как reference позволяет контейнеру фиксированной формы хранить данные переменного размера."
title: 'Данные в памяти: тест с выбором ответа'
track: base-cs
unit: 09-data-in-memory
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Ни один не просит пересказать определение — каждый просит рассуждать о том, как данные реально лежат в памяти и почему раскладка ведёт себя именно так.
</Hook>

<Goal lang="ru">
Убедись, что связываешь четыре идеи юнита: ячейки одного размера, лежащие contiguous; формулу «индекс → адрес»; объекты с именованными полями; и reference, которые позволяют фиксированной сетке хранить вещи переменного размера.
</Goal>

<Quiz
  id="09-data-in-memory-choice-1"
  lessonSlug="09-data-in-memory"
  lang="ru"
  question="Array из 8 чисел занимает один contiguous-блок в 32 байта. Какое утверждение о его раскладке верно?"
  choices={[
    { label: "Каждый элемент — 4 байта, ячейки идут вплотную без зазоров, и блок — это один неразрывный ряд адресов: всего 8 x 4 = 32 байта.", correct: true },
    { label: "8 элементов лежат по 8 несвязанным адресам, а имя array хранит отдельное местоположение для каждого.", misconception: "Array contiguous: элементы занимают один непрерывный блок последовательных адресов, а не разбросаны. Имя array ссылается на единый блок, а именно на его начало." },
    { label: "Ячейки могут быть разного размера, лишь бы в сумме давали 32 байта.", misconception: "Все элементы простого array имеют один тип, поэтому все ячейки одного размера. Сетка из равных ячеек и есть то, что определяет array; ячейки разного размера сломали бы регулярную раскладку." },
    { label: "В 32 байта входят зазоры между элементами, чтобы их разделять.", misconception: "Зазоров нет. Contiguous значит, что ячейки идут вплотную; именно регулярность безразрывной сетки и позволяет арифметике индекса попадать в нужную ячейку." },
  ]}
/>

<Quiz
  id="09-data-in-memory-choice-2"
  lessonSlug="09-data-in-memory"
  lang="ru"
  question="Доступ к arr[3000] занимает столько же времени, сколько к arr[3]. Почему доступ к array — константное время?"
  choices={[
    { label: "Адрес вычисляется напрямую как base_address + index x element_size — одно умножение и одно сложение, так что работа не растёт ни с индексом, ни с длиной array.", correct: true },
    { label: "Программа сканирует от элемента 0, считая ячейки до индекса, но современные CPU делают сканирование быстрым.", misconception: "Доступ к array никогда не сканирует. Сканирование сделало бы большой индекс медленнее малого. Адрес вычисляется за один арифметический шаг — одинаковая фиксированная работа для любого индекса." },
    { label: "Среда хранит таблицу, отображающую каждый индекс в его адрес, поэтому любой — это чтение из таблицы.", misconception: "Никакой поэлементной таблицы нет. Раскладка contiguous и одного размера позволяет вычислить адрес арифметикой из base и индекса — таблица не нужна." },
    { label: "Длина array достаточно мала, чтобы любой индекс был на практике быстрым.", misconception: "Длина ни при чём: формула base + i x element_size не использует длину и не трогает другие элементы, так что доступ остаётся константным даже на десяти миллионах элементов." },
  ]}
/>

<Quiz
  id="09-data-in-memory-choice-3"
  lessonSlug="09-data-in-memory"
  lang="ru"
  question="Почему первый элемент array имеет index 0, а не index 1?"
  choices={[
    { label: "Index — это offset, измеренный в элементах от начала. Первая ячейка лежит ровно на base address, поэтому её offset — 0 x element_size = 0, и 0 — единственный индекс с нулевым offset.", correct: true },
    { label: "Это произвольное соглашение, выбранное разработчиками языков, которое просто запоминают.", misconception: "Это навязано арифметикой, а не произвольно. Формула base + index x element_size делает индекс первой ячейки именно тем, который даёт нулевой offset — то есть 0." },
    { label: "Index 0 — зарезервированный слот под длину array, поэтому реальные данные начинаются с адреса после него.", misconception: "Index 0 хранит первый реальный элемент, а не метаданные. Его offset нулевой, что ставит его ровно на base address; именно поэтому он первый." },
    { label: "Счёт с 0 делает array на один элемент короче и экономит память.", misconception: "Счёт с 0 не меняет размеров. Он отражает, что index — это offset в элементах от начала: первый элемент — ноль элементов после base." },
  ]}
/>

<Quiz
  id="09-data-in-memory-choice-4"
  lessonSlug="09-data-in-memory"
  lang="ru"
  question="Array достаёт значение через arr[2]; объект достаёт значение через user.age. В чём ключевое различие и что из него следует?"
  choices={[
    { label: "Array достаёт значение по числовой позиции, и порядок и есть структура; объект достаёт значение по именованному key, и порядок полей не имеет смысла — поэтому { a: 1, b: 2 } и { b: 2, a: 1 } это один и тот же объект.", correct: true },
    { label: "Объект — это просто array, чьи элементы случайно помечены, а метки ищутся по числовому индексу, как элементы array.", misconception: "Поля объекта достаются по их key (имени), а не по числовому индексу. Доступ по имени — весь смысл объекта; доступ по позиции — свойство array." },
    { label: "Объект хранит поля в строгом порядке, и читать их надо первое-поле-первым.", misconception: "У полей объекта нет осмысленного порядка. Поле достаётся по его key, а не по тому, какое идёт первым. Упорядоченный доступ — свойство array; если порядок важен, нужен array." },
    { label: "Реального различия нет — и то и другое просто последовательности значений под одним именем.", misconception: "Они группируют значения по-разному: array — по позиции, объект — по имени. Этот выбор решает, несёт ли порядок смысл и индексируешь ты или именуешь, чтобы прочитать значение." },
  ]}
/>

<Quiz
  id="09-data-in-memory-choice-5"
  lessonSlug="09-data-in-memory"
  lang="ru"
  question="У тебя array из 100 объектов user, каждый разного размера. Как он лежит в памяти, сохраняя contiguous-сетку array из равных ячеек?"
  choices={[
    { label: "Каждая ячейка array хранит reference — адрес фиксированного размера — указывающий на объект user, лежащий в другом месте. Блок остаётся contiguous-сеткой из равных reference-ячеек; объекты разбросаны по памяти.", correct: true },
    { label: "100 целых объектов упакованы встык внутри одного огромного contiguous-блока array.", misconception: "Объекты разного размера и могут расти, поэтому не заполняют сетку из равных ячеек. Ячейки хранят reference; сами объекты лежат отдельно, где нашлось место." },
    { label: "Array автоматически меняет размер каждой ячейки под объект, поэтому ячейки разного размера.", misconception: "Ячейки array одного размера — именно это делает base + i x element_size верной. Ячейки хранят reference одного размера; разного размера только сами объекты по ссылкам." },
    { label: "Array вообще не может хранить объекты; они должны лежать в другой структуре.", misconception: "Array может хранить объекты, храня reference на них. Каждая ячейка хранит адрес объекта, а array остаётся обычным contiguous array из равных ячеек." },
  ]}
/>

<Quiz
  id="09-data-in-memory-choice-6"
  lessonSlug="09-data-in-memory"
  lang="ru"
  question="Чтение users[1].name в array объектов занимает два прыжка (hop). Какие они и почему два?"
  choices={[
    { label: "Прыжок 1 — арифметика: вычислить base + 1 x element_size, чтобы дойти до ячейки и прочитать там reference. Прыжок 2 идёт по этому reference к объекту, затем ищет поле name. Два прыжка, потому что ячейка хранит адрес, а не объект.", correct: true },
    { label: "Один прыжок: ячейка array содержит весь объект user, поэтому чтение ячейки уже даёт поле name.", misconception: "Ячейка array хранит reference, а не объект. Надо пройти по адресу к объекту, прежде чем читать поле — это второй прыжок." },
    { label: "Два прыжка, потому что любой доступ к array сканирует array один раз, а затем объект один раз.", misconception: "Ни один шаг не сканирование. Первый прыжок — одно арифметическое вычисление в contiguous array; второй — один проход по reference к объекту. Оба прямые." },
    { label: "Три прыжка: один за array, один за reference, один чтобы отсчитать позицию поля.", misconception: "Поле достаётся по его key, а не отсчётом позиций. Это два прыжка: арифметика в array, затем один проход по reference к объекту, где key находит поле." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна слоистая картина. Array — это contiguous-ряд из равных ячеек, поэтому адрес любого элемента — одно умножение и одно сложение (base + i x element_size): константное время, а index 0 навязан нулевым offset. Объект группирует значения по имени, а не по позиции, поэтому порядок полей не несёт смысла. А так как ячейка фиксированного размера не вмещает объект переменного размера, ячейки хранят reference — адреса одного размера — позволяя единообразной сетке указывать на данные переменной формы. Доведённые до конца, вложенные данные — это graph of cells: одни хранят значения, другие хранят адреса, связанные вместе.
</Recap>
