---
concepts:
- array
- contiguous-memory
- array-element
- equal-size-cells
- array-block
estMin: 20
lang: ru
lessonType: coding
mathPrereqs: []
order: 1
prereqs:
- 01-addressable-cells
- 02-the-byte
- 01-what-a-value-is
- 03-primitive-types
slug: 01-arrays-as-contiguous-cells
sources:
- https://www.nand2tetris.org/
- https://see.stanford.edu/materials/icsppcs107/07-Arrays-The-Full-Story.pdf
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array
status: ready
summary: Массив — это череда ячеек одинакового размера, размещённых вплотную друг
  к другу в памяти. Поскольку каждый элемент одного типа, каждая ячейка одного размера,
  и весь массив занимает один непрерывный блок адресов.
title: Массивы как непрерывные ячейки
track: base-cs
unit: 09-data-in-memory
---



import Hook from "~/components/lesson/Hook.astro";
import PracticeSet from "~/components/math/PracticeSet.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
До сих пор каждая переменная, которую ты видел, хранила одно значение: одно число, один
булев, одну строку. Но программе почти всегда нужно хранить *много* значений одного вида —
очки каждого игрока, байты файла, пиксели изображения.

Можно было бы объявить сотню отдельных переменных, но это нерабочий путь. Вместо этого
языки дают тебе **массив**: одно имя, которое ссылается на целую последовательность
значений, расположенных по порядку. Первое, что нужно понять о массиве, — это не его
синтаксис, а его форма в памяти. Массив не разбросан по памяти случайно. Это единая
непрерывная череда ячеек, каждая одного размера, размещённых прямо вплотную друг к другу.

После этого урока ты сможешь представить, где именно в памяти лежит каждый элемент
массива и почему эта картина — основа всего, что массивы умеют делать.
</Hook>

<Goal lang="ru">
После этого урока ты сможешь определить массив как непрерывную череду ячеек одинакового
размера, объяснить, почему каждая ячейка массива одного размера, определить, какие адреса
занимает небольшой массив, и трассировать раскладку массива-литерала в памяти элемент за
элементом.
</Goal>

<Idea lang="ru">
Вспомни два факта из ранних блоков. Из Блока 02: память — это длинный ряд **адресуемых
ячеек**, у каждой свой числовой адрес, а **байт** (8 битов) — стандартная единица,
которую хранит ячейка. Из Блока 05: у каждого значения есть **тип**, и тип задаёт,
сколько байтов нужно значению — число заданного типа всегда занимает одно и то же
фиксированное число байтов.

**Массив** построен прямо на этих двух фактах. Массив — это последовательность значений,
все **одного типа**, хранимая в памяти как череда ячеек, которые:

1. **Одинаковы по размеру.** Поскольку каждый элемент одного типа, каждому элементу нужно
   одинаковое число байтов. Значит, каждая ячейка массива одного размера. Назовём этот
   размер **размером элемента**.
2. **Непрерывны.** Ячейки лежат прямо вплотную, без разрывов между ними. Массив занимает
   один непрерывный блок адресов. Это свойство называется **непрерывной памятью**.

Каждое значение, хранимое в массиве, называется **элементом**. Если у массива 5 элементов
и каждый элемент 4 байта, весь массив — один цельный блок из 5 × 4 = 20 байтов.

Эти два свойства связаны. *Одинаковый размер* — это то, что делает *непрерывность*
полезной: когда одинаковые ячейки упакованы вплотную, положение любой ячейки совершенно
регулярно и предсказуемо. Эту регулярность ты используешь в следующем уроке, чтобы найти
любой элемент мгновенно. Пока цель — просто увидеть форму: массив — это аккуратная полоса
одинаковых ячеек без разрывов.

Программа ниже строит небольшой массив из 5 чисел. Трассировка показывает, как массив
обретает форму ячейка за ячейкой.
</Idea>

<Code lang="ru">

<AnnotatedCode
  code={`// Массив-литерал: 5 чисел одного типа.
let scores: number[] = [90, 80, 70, 60, 50];

// Все пять значений живут в одном непрерывном блоке памяти.
// Элемент 0 находится в начале блока; остальные следуют
// прямо за ним, каждый в ячейке одного размера.

// У scores 5 элементов.
// Если каждое число занимает 4 байта, весь массив — 20 байтов.`}
  annotations={[
    { line: 2, text: "scores — массив из 5 элементов, каждый элемент число, поэтому каждая ячейка одного размера" },
    { line: 5, text: "Элемент 0 (значение 90) лежит первым; элемент 1 (значение 80) лежит прямо за ним, и так далее" },
    { line: 6, text: "Без разрывов: ячейки вплотную. Это непрерывная память." },
    { line: 9, text: "5 элементов x 4 байта каждый = один цельный блок в 20 байтов" },
  ]}
  caption="Массив-литерал из 5 чисел. Трассировка ниже размещает каждый элемент в его ячейке памяти."
/>
</Code>

<Trace lang="ru">
Пройди построение `scores` шаг за шагом. Каждый прямоугольник — одна ячейка массива; адрес
под каждым прямоугольником — байтовый адрес, с которого эта ячейка начинается. Считаем,
что блок массива начинается с адреса 1000 и каждое число занимает 4 байта, поэтому ячейки
начинаются с 1000, 1004, 1008, 1012, 1016.

<AlgoTrace
  id="trace-arrays-contiguous"
  lang="ru"
  code={[
    "let scores: number[] =",
    "  [90, 80, 70, 60, 50];"
  ]}
  frames={[
    {
      caption: "До: блок массива зарезервирован, но значения элементов ещё не размещены.",
      line: 1,
      cells: [
        { value: "?" },
        { value: "?" },
        { value: "?" },
        { value: "?" },
        { value: "?" }
      ]
    },
    {
      caption: "Элемент 0 размещён: значение 90 сохранено в первой ячейке, по адресу 1000.",
      line: 2,
      cells: [
        { value: "90", mark: "active" },
        { value: "?" },
        { value: "?" },
        { value: "?" },
        { value: "?" }
      ]
    },
    {
      caption: "Элемент 1 размещён: значение 80 сохранено в следующей ячейке, по адресу 1004 — прямо за ячейкой 0.",
      line: 2,
      cells: [
        { value: "90", mark: "done" },
        { value: "80", mark: "active" },
        { value: "?" },
        { value: "?" },
        { value: "?" }
      ]
    },
    {
      caption: "Элемент 2 размещён: значение 70 сохранено по адресу 1008. Ячейка того же размера, без разрыва.",
      line: 2,
      cells: [
        { value: "90", mark: "done" },
        { value: "80", mark: "done" },
        { value: "70", mark: "active" },
        { value: "?" },
        { value: "?" }
      ]
    },
    {
      caption: "Элемент 3 размещён: значение 60 сохранено по адресу 1012.",
      line: 2,
      cells: [
        { value: "90", mark: "done" },
        { value: "80", mark: "done" },
        { value: "70", mark: "done" },
        { value: "60", mark: "active" },
        { value: "?" }
      ]
    },
    {
      caption: "Элемент 4 размещён: значение 50 сохранено по адресу 1016. Массив завершён: один цельный блок в 20 байтов, адреса с 1000 по 1019.",
      line: 2,
      cells: [
        { value: "90", mark: "done" },
        { value: "80", mark: "done" },
        { value: "70", mark: "done" },
        { value: "60", mark: "done" },
        { value: "50", mark: "active" }
      ]
    }
  ]}
/>
</Trace>

<Inset kind="why" lang="ru">
**Почему массиву нужно, чтобы каждый элемент был одного размера?** Если бы элементы могли
быть разного размера, ячейки не были бы одинаковыми, и не было бы способа узнать, где
кончается один элемент и начинается следующий, не осмотрев их все. Заставляя один тип — а
значит, один фиксированный размер — язык гарантирует, что ячейки образуют совершенно
регулярную сетку. Именно эта регулярность позволяет программе перепрыгнуть прямо к любому
элементу. Последовательность смешанного размера возможна, но это уже не простая сетка
вплотную; ей нужен дополнительный учёт. Простой массив меняет гибкость на чистую,
предсказуемую форму.
</Inset>

<Inset kind="mistake" lang="ru">
Распространённое заблуждение — что элементы массива хранятся «где-то в памяти», а имя
массива служит списком отдельных мест. Это не модель массива. Элементы занимают *один*
блок последовательных адресов. Имя массива ссылается на этот блок — а именно на то, где
он начинается. Элементы не разбросаны; это единая непрерывная череда.
</Inset>

<PracticeSet
  id="arrays-contiguous-practice"
  lessonSlug="01-arrays-as-contiguous-cells"
  lang="ru"
  problems={[
    {
      prompt: "У массива 6 элементов, и каждый элемент занимает 4 байта. Сколько байтов занимает весь блок массива?",
      answer: 24,
      hint: "Массив — один непрерывный блок: число элементов x размер элемента. 6 x 4 = 24.",
    },
    {
      prompt: "Массив из 8 элементов занимает непрерывный блок в 16 байтов всего. Сколько байтов каждый элемент (размер элемента)?",
      answer: 2,
      hint: "Общий размер блока, делённый на число элементов: 16 / 8 = 2 байта на элемент.",
    },
    {
      prompt: "Блок массива начинается с адреса 1000, и каждый элемент 4 байта. С какого адреса начинается элемент 1 (второй элемент)?",
      answer: 1004,
      hint: "Элемент 0 начинается с 1000. Элемент 1 лежит прямо за ним, на 4 байта дальше: 1000 + 4 = 1004.",
    },
    {
      prompt: "В простом массиве каждый элемент должен быть одного типа, чтобы каждая ячейка была одного размера. Если массив хранит числа, может ли одна ячейка хранить число, а другая — строку из 1 символа другого байтового размера? Введи 1 за да, 0 за нет.",
      answer: 0,
      hint: "Простой массив требует один тип для всех элементов, поэтому все ячейки одного размера. Смешение типов разного размера ломает однородную сетку. Ответ — 0 (нет).",
    },
    {
      prompt: "Массив из 5 чисел, каждое 4 байта, начинается с адреса 2000. С какого адреса начинается последний элемент (элемент 4)?",
      answer: 2016,
      hint: "Элемент 4 — на 4 ячейки за началом: 2000 + 4 элемента x 4 байта = 2000 + 16 = 2016.",
    },
  ]}
/>

<Check lang="ru">
  <Quiz
    id="arrays-contiguous-check"
    pieceSlug="01-arrays-as-contiguous-cells"
    lang="ru"
    question="Какова форма массива в памяти?"
    choices={[
      {
        label: "Один непрерывный блок ячеек одинакового размера, размещённых прямо вплотную, без разрывов между элементами",
        correct: true,
      },
      {
        label: "Набор ячеек, разбросанных по памяти по несвязанным адресам, где имя массива хранит отдельное место для каждого элемента",
        misconception: "Массив непрерывен: его элементы занимают один сплошной блок последовательных адресов. Они не разбросаны. Имя массива ссылается на единый блок, а не на список независимых мест.",
      },
      {
        label: "Блок ячеек, где каждая ячейка может быть разного размера в зависимости от хранимого в ней значения",
        misconception: "Каждый элемент простого массива одного типа, поэтому каждая ячейка одного размера. Эта однородная сетка одинаковых ячеек и определяет массив. Ячейки разного размера сломали бы регулярную раскладку.",
      },
    ]}
  />
</Check>

<Recap lang="ru">
**Массив** — это последовательность значений одного типа, хранимая в памяти как череда
**ячеек одинакового размера**, размещённых прямо вплотную друг к другу. Поскольку каждый
элемент одного типа, каждому элементу нужно одинаковое число байтов — **размер элемента** —
поэтому каждая ячейка одинакова по размеру. Поскольку ячейки лежат без разрывов между
ними, массив занимает один непрерывный блок адресов; это **непрерывная память**. Массив из
N элементов, каждый размером S байтов, — это один цельный блок из N × S байтов. Каждое
значение в массиве — это **элемент**, а имя массива ссылается на начало блока. Эта
аккуратная сетка одинаковых ячеек без разрывов — основа всего, что массивы умеют делать,
начиная с мгновенного поиска элемента, который ты увидишь в следующем уроке.
</Recap>
