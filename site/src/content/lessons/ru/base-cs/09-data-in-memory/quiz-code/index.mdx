---
concepts:
- index-arithmetic
- element-size
- base-address
- reference-cells
- two-hop-access
estMin: 14
lang: ru
level: middle
order: 9
prereqs: []
slug: quiz-code
sources:
- https://see.stanford.edu/materials/icsppcs107/07-Arrays-The-Full-Story.pdf
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array
status: ready
summary: "Читай небольшие сниппеты и раскладки в памяти, вычисляй адреса элементов через base + i x element_size и рассуждай, что реально хранит ячейка — value или reference."
title: 'Данные в памяти: чтение кода и раскладки'
track: base-cs
unit: 09-data-in-memory
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Раскладка в памяти — это где идеи становятся конкретными. Читай каждый сниппет и его раскладку, считай арифметику адреса вручную и решай, что реально хранит каждая ячейка, прежде чем выбрать ответ.
</Hook>

<Goal lang="ru">
Потренируй движение, которое делаешь каждый раз, когда рассуждаешь о данных в памяти: зафиксируй base address и element size, вычисли целевой адрес и определи, хранит ли ячейка value напрямую или reference на нечто в другом месте.
</Goal>

## Сниппет 1 — формула адреса

```js
// scores — один непрерывный блок.
// базовый адрес = 1000, каждый элемент 4 байта.
let scores = [90, 80, 70, 60, 50];
let x = scores[3];   // какой адрес читает это?
```

<Quiz
  id="09-data-in-memory-code-1"
  lessonSlug="09-data-in-memory"
  lang="ru"
  question="При base address 1000 и 4-байтовом элементе какой адрес читает scores[3] и как он вычисляется?"
  choices={[
    { label: "1012 — address = base + index x element_size = 1000 + 3 x 4 = 1012, одно умножение и одно сложение.", correct: true },
    { label: "1003 — индекс прибавляют напрямую к base address: 1000 + 3.", misconception: "Index считает элементы, а не байты. Сначала надо умножить на element size: 1000 + 3 x 4 = 1012. Прибавление сырого индекса игнорирует, сколько байтов занимает элемент." },
    { label: "1016 — третий элемент начинается после отсчёта четырёх 4-байтовых ячеек.", misconception: "scores[3] — это элемент 3, на три элемента после base, а не на четыре. 1000 + 3 x 4 = 1012. Index 3 — четвёртый элемент, но его offset 3 x element_size." },
    { label: "Зависит от длины array, которая здесь не дана.", misconception: "Формула никогда не использует длину: base + i x element_size трогает только base, индекс и element size. Длина не нужна для адреса элемента 3." },
  ]}
/>

## Сниппет 2 — element size меняет offset

```js
// Тот же индекс, другой element size.
// big имеет базовый адрес 2000, каждый элемент 8 байтов.
let big = [1.5, 2.5, 3.5, 4.5];
let y = big[2];   // адрес?
```

<Quiz
  id="09-data-in-memory-code-2"
  lessonSlug="09-data-in-memory"
  lang="ru"
  question="При base address 2000 и 8-байтовом элементе какой адрес читает big[2]?"
  choices={[
    { label: "2016 — base + index x element_size = 2000 + 2 x 8 = 2016. Тот же индекс попадает дальше, потому что каждый элемент крупнее.", correct: true },
    { label: "2008 — index 2 всегда значит 8 байтов после base.", misconception: "Offset — это index x element_size, а не фиксированные 8. При 8-байтовом элементе index 2 — это 2 x 8 = 16 байтов после base: 2000 + 16 = 2016. Element size масштабирует offset." },
    { label: "2002 — прибавить индекс к base.", misconception: "Index считает элементы; надо умножать на element size. 2000 + 2 x 8 = 2016. Прибавление сырого индекса игнорирует, что каждый элемент шириной 8 байтов." },
    { label: "То же, что дал бы 4-байтовый array, потому что индекс тот же.", misconception: "Element size — часть формулы. 4-байтовый элемент даёт 2000 + 2 x 4 = 2008; 8-байтовый даёт 2016. Тот же индекс, другой offset." },
  ]}
/>

## Сниппет 3 — value-ячейки против reference-ячеек

```js
let nums  = [10, 20, 30];                 // array чисел
let users = [{ name: "A" }, { name: "B" }]; // array объектов
```

<Quiz
  id="09-data-in-memory-code-3"
  lessonSlug="09-data-in-memory"
  lang="ru"
  question="Что хранит каждая ячейка nums против каждой ячейки users?"
  choices={[
    { label: "Ячейки nums хранят числа напрямую (малые value фиксированного размера); ячейки users хранят reference — адреса, указывающие на объекты в другом месте — потому что объект не вмещается в малую ячейку фиксированного размера.", correct: true },
    { label: "Обе хранят содержимое прямо в ячейке; объекты в users упакованы в блок array.", misconception: "Объект переменного размера и может расти, поэтому не вмещается в ячейку array фиксированного размера. Ячейки users хранят reference на объекты в другом месте; только nums хранит свои value напрямую." },
    { label: "nums хранит reference на числа, а users хранит объекты напрямую.", misconception: "Всё наоборот. Число — это малое value фиксированного размера, оно вмещается в ячейку напрямую. Именно объекты не вмещаются, поэтому users хранит reference на них." },
    { label: "Ни один array не contiguous, потому что их ячейки хранят разные вещи.", misconception: "Оба array — contiguous-сетки из равных ячеек. Разница только в содержимом: nums хранит value, users хранит reference одного размера. Оба сохраняют единообразную раскладку." },
  ]}
/>

## Сниппет 4 — доступ в два прыжка в array объектов

```js
// users имеет базовый адрес 1000; каждая ячейка-ссылка 4 байта.
// объект user 0 лежит на 7000, объект 1 на 7100, объект 2 на 7200.
let users = [u0, u1, u2];
let a = users[1].age;   // как это достаётся?
```

<Quiz
  id="09-data-in-memory-code-4"
  lessonSlug="09-data-in-memory"
  lang="ru"
  question="При чтении users[1].age какая последовательность верна?"
  choices={[
    { label: "Вычислить base + 1 x 4 = 1004, чтобы дойти до ячейки; прочитать там reference (7100); пройти по нему к объекту на 7100; затем по key age найти поле внутри. Два прыжка.", correct: true },
    { label: "Вычислить 1004, затем прочитать значение age прямо из ячейки array на 1004.", misconception: "Ячейка на 1004 хранит reference (7100), а не объект. Надо пройти по этому адресу к объекту, прежде чем читать любое поле — это второй прыжок." },
    { label: "Сразу идти на 7100, потому что users[1] — это объект 1, минуя арифметику array.", misconception: "Ты не знаешь, что объект 1 лежит на 7100, пока не прочитаешь reference из ячейки array. Первый прыжок — арифметика в array, чтобы достать этот адрес." },
    { label: "Вычислить 1004, затем сканировать поля объекта по одному, пока age не окажется следующей позицией.", misconception: "Поле достаётся по key, а не сканированием позиций. После прохода по reference к объекту key age находит поле напрямую." },
  ]}
/>

<Recap lang="ru">
Любой вопрос о раскладке сводится к тем же двум движениям. Сначала арифметика: адрес элемента — это base + index x element_size — индекс считает элементы, element size превращает его в байты, а длина никогда не появляется. Меняешь element size — тот же индекс попадает в другое место. Затем спроси, что хранит ячейка: малое value лежит в ячейке напрямую, но объект не вмещается, поэтому ячейка хранит reference, и чтение поля — два прыжка: арифметика в contiguous array, затем один проход к объекту.
</Recap>
