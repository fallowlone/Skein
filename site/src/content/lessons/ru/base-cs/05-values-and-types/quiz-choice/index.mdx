---
concepts:
- bits-have-no-meaning
- type-as-interpretation
- same-bits-different-types
- primitive-types
- static-vs-dynamic
- type-error
estMin: 13
lang: ru
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://craftinginterpreters.com/values-and-types.html
- https://tc39.es/ecma262/#sec-ecmascript-language-types
- https://en.wikipedia.org/wiki/Data_type
status: ready
summary: "Синтез всего юнита в формате выбора: биты не несут смысла, type — это правило чтения, одни и те же биты декодируются по-разному, и что такое type error на самом деле."
title: 'Значения и типы: тест с выбором'
track: base-cs
unit: 05-values-and-types
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, проходящих сквозь весь юнит. Ни один не требует пересказать определение. Каждый просит применить единственную идею юнита — биты ничего не значат, пока type не скажет, как их читать — к конкретной ситуации.
</Hook>

<Goal lang="ru">
Убедись, что можешь связать все нити юнита: value — это сырые биты, type — правило их интерпретации, одни и те же биты декодируются по-разному под разными правилами, а система типов существует, чтобы не дать применить неверное правило.
</Goal>

<Quiz
  id="05-values-and-types-choice-1"
  lessonSlug="05-values-and-types"
  lang="ru"
  question="Ячейка памяти хранит 8-битный паттерн 01000001. Две программы читают её: одна печатает 65, другая — букву 'A'. Какое утверждение верно?"
  choices={[
    { label: "Обе правы. Биты одинаковы; каждая программа применила своё правило типа — place-value целого u8 даёт 65, таблица ASCII сопоставляет коду 65 букву 'A'. Значение определяет чтение, а не биты.", correct: true },
    { label: "Байт на самом деле 65; программа, напечатавшая 'A', содержит баг.", misconception: "Ни одно значение не является 'истинным' смыслом байта. Паттерн 01000001 не несёт встроенного значения. И 65, и 'A' — корректные прочтения одних и тех же битов под двумя разными правилами типа." },
    { label: "Чтение как символа изменило биты, значит вторая программа повредила ячейку.", misconception: "Применение типа — это read-only декодирование. Оно никогда не меняет хранимые биты. Обе программы прочитали один неизменный паттерн; различалась лишь интерпретация." },
    { label: "Ячейка может хранить только один тип, значит она тайно либо integer, либо символ.", misconception: "Биты в ячейке не несут постоянной метки типа. Аппаратура хранит восемь состояний вкл/выкл. Какое правило типа применить — выбор программы, отслеживаемый вне ячейки." },
  ]}
/>

<Quiz
  id="05-values-and-types-choice-2"
  lessonSlug="05-values-and-types"
  lang="ru"
  question="Нужно сохранить value и позже корректно прочитать его обратно. Одного стартового адреса недостаточно. Что ещё должен знать читатель?"
  choices={[
    { label: "Размер (сколько байт занимает значение) и интерпретацию (как декодировать эти байты) — вместе это ровно то, что даёт type.", correct: true },
    { label: "Ничего — как только знаешь адрес, аппаратура памяти вернёт значение с прикреплённым смыслом.", misconception: "Память возвращает сырые биты, никогда не смысл. Аппаратура знает адреса, а не типы. Размер и интерпретация живут в программе, а не в ячейке." },
    { label: "Только размер; узнав, что значение занимает 4 байта, его уже можно прочитать.", misconception: "Размер говорит, какие байты загрузить, но не что они означают. Четыре байта могут быть знаковым integer, float или четырьмя символами. Интерпретация — отдельная и столь же необходимая часть." },
    { label: "Имя переменной, чтобы рантайм нашёл её смысл.", misconception: "Имя — метка для программиста; машина его отбросила. На уровне битов нужны размер плюс правило интерпретации — две половины типа." },
  ]}
/>

<Quiz
  id="05-values-and-types-choice-3"
  lessonSlug="05-values-and-types"
  lang="ru"
  question="В JavaScript и 42, и 36.6 дают typeof === 'number'. Почему нет отдельного целочисленного типа?"
  choices={[
    { label: "Каждый number в JS хранится как единый 64-битный IEEE 754 double. Целые и дробные делят один 8-байтовый формат, поэтому 42 и 36.6 — один тип; отдельной целочисленной категории в повседневном JS нет.", correct: true },
    { label: "JS округляет 36.6 до 37 внутри, так что на деле оба — целые.", misconception: "JS не округляет 36.6 до целого. Он хранит дробь в IEEE 754. Оба значения — один тип, потому что делят формат 64-битного double, а не потому что одно округлили." },
    { label: "typeof слишком грубый, чтобы отличить целые от float, но внутри это разные типы.", misconception: "На деле тип number один. typeof здесь отражает реальность: рантайм хранит и 42, и 36.6 как double. Различие integer-vs-float вообще не входит в набор примитивов JS." },
    { label: "42 — целочисленный тип, а 36.6 — float; typeof объединяет их лишь для отображения.", misconception: "В JS один тип number. Он не ведёт под капотом отдельные целочисленный и float типы, сливая их при выводе — это один тип, один формат хранения." },
  ]}
/>

<Quiz
  id="05-values-and-types-choice-4"
  lessonSlug="05-values-and-types"
  lang="ru"
  question="Коллега пишет: 'null и undefined — одно и то же, оба значат пусто.' Какова точная поправка?"
  choices={[
    { label: "Это два различных примитивных значения отсутствия. null — намеренное отсутствие (программист написал его, чтобы сказать 'здесь ничего, специально'); undefined — автоматическое отсутствие (объявленная, но не присвоенная переменная или отсутствующее свойство).", correct: true },
    { label: "Они идентичны; язык держит два имени лишь по историческим причинам.", misconception: "Это действительно различные значения с различными смыслами. null — намеренное 'ничего'; undefined — несозданное/отсутствующее состояние, которое рантайм даёт автоматически. Strict mode TypeScript даже отслеживает их как отдельные типы." },
    { label: "undefined — для чисел, а null — для объектов; различие лишь в том, к какому типу они относятся.", misconception: "Ни одно не привязано к конкретному типу значения. Любая переменная может быть undefined (не присвоена), и любой слот может быть намеренно null. Различие — намеренное vs автоматическое отсутствие, а не number-vs-object." },
    { label: "null — это undefined после прохода сборщика мусора.", misconception: "Сборка мусора не превращает одно в другое. Это с самого начала отдельные примитивные значения: null пишут намеренно, undefined возникает из отсутствующего присваивания или свойства." },
  ]}
/>

<Quiz
  id="05-values-and-types-choice-5"
  lessonSlug="05-values-and-types"
  lang="ru"
  question="Какую единственную глубинную проблему создана ловить любая система типов — статическая или динамическая?"
  choices={[
    { label: "Применение неверного правила интерпретации к паттерну битов: чтение битов, сохранённых под одним типом, как будто это другой тип — что даёт неверные значения, падения или повреждение памяти.", correct: true },
    { label: "Программы, потребляющие слишком много памяти, потому что значения занимают слишком много байт.", misconception: "Объём памяти — отдельная забота. Система типов отслеживает, какое правило чтения принадлежит какому значению; она существует не для того, чтобы ограничивать число байт значения." },
    { label: "Код, который трудно читать или плохо организован.", misconception: "Читаемость — цель дизайна и стиля. Системы типов нацелены на проблему корректности: не дать декодировать биты неверным правилом. Чистый код и type safety — разные слои." },
    { label: "Медленные программы, которым нужна оптимизация.", misconception: "Производительность не связана с основной целью. Задача системы типов — предотвратить неверную интерпретацию битов, а не ускорить код." },
  ]}
/>

<Quiz
  id="05-values-and-types-choice-6"
  lessonSlug="05-values-and-types"
  lang="ru"
  question="Один и тот же баг несоответствия типов можно поймать на этапе компиляции или во время выполнения. В чём практическая разница между статической (TypeScript) и динамической (JavaScript) проверкой?"
  choices={[
    { label: "Статическая типизация ловит несоответствие до запуска программы, устраняя весь класс ошибок для типокорректного кода; динамическая ловит его лишь когда плохая строка реально выполнится — поэтому редко исполняемый путь может долго прятать баг.", correct: true },
    { label: "Статическая типизация ловит все баги; динамическая — никакие.", misconception: "Статическая типизация ловит лишь один класс — несоответствия типов — и то лишь до выполнения. Баги логики, алгоритма и бизнес-правил проходят мимо обеих. Динамическая тоже ловит type errors, просто позже." },
    { label: "Они ловят совершенно разные категории багов; пересечения нет.", misconception: "Обе нацелены на одну глубинную проблему — биты, которые вот-вот прочтут неверным правилом. Различие в моменте: этап компиляции против времени выполнения, а не категория ошибки." },
    { label: "Динамическая типизация всегда безопаснее, ведь проверяет во время выполнения с полной информацией.", misconception: "Обнаружение во время выполнения — позднее обнаружение: ошибка всплывёт лишь если и когда строка выполнится, возможно в продакшене. Раннее (статическое) обнаружение убирает класс до релиза — потому TypeScript и существует поверх JS." },
  ]}
/>

<Recap lang="ru">
Хребет юнита в одной цепочке решений: value — фиксированный паттерн битов без встроенного смысла; type даёт две недостающие части — размер и интерпретацию — превращающие биты в значение; одни и те же биты декодируются в разные значения под разными правилами; JS открывает небольшой набор примитивов (number как единый 64-битный IEEE 754 double, string, boolean и два различных отсутствия null и undefined); а система типов существует, чтобы не дать применить неверное правило — статически до выполнения или динамически в момент исполнения.
</Recap>
