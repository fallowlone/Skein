---
concepts:
- ieee-754-rounding
- safe-integer-limit
- type-coercion
- typeof-quirks
estMin: 14
lang: ru
level: middle
order: 9
prereqs: []
slug: quiz-code
sources:
- https://tc39.es/ecma262/#sec-ecmascript-language-types
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Data_structures
- https://262.ecma-international.org/13.0/#sec-typeof-operator
status: ready
summary: "Прочитай четыре коротких JS-сниппета и предскажи, что рантайм делает на самом деле: округление float, предел safe integer, тихий coercion и quirk typeof null."
title: 'Значения и типы: чтение кода'
track: base-cs
unit: 05-values-and-types
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Все сюрпризы повседневного кода восходят к основной идее юнита: биты сохранены под одним правилом типа, а позже прочитаны под другим. Прочитай каждый сниппет, предскажи точный вывод и назови правило, которое его породило.
</Hook>

<Goal lang="ru">
Отработай цикл, который запускаешь всякий раз, когда программа печатает неожиданное: определи, какое правило типа рантайм применил к битам, и объясни, почему вывод именно такой, а не тот, что ты сперва предположил.
</Goal>

## Сниппет 1 — дробь, которая не сходится

```js
const sum = 0.1 + 0.2;
console.log(sum);            // ?
console.log(sum === 0.3);    // ?
```

<Quiz
  id="05-values-and-types-code-1"
  lessonSlug="05-values-and-types"
  lang="ru"
  question="Что это печатает и почему?"
  choices={[
    { label: "0.30000000000000004, и sum === 0.3 — false. number в JS — это 64-битный IEEE 754 double; 0.1 и 0.2 не имеют точного двоичного представления, поэтому каждое хранится округлённым, и ошибки округления переживают сложение.", correct: true },
    { label: "0.3, и sum === 0.3 — true; десятичные складываются точно, как на бумаге.", misconception: "Двоичный float не может хранить 0.1 или 0.2 точно, как десятичная система не хранит 1/3 точно. Хранимые значения уже слегка смещены, поэтому сумма — 0.30000000000000004, а равенство — false." },
    { label: "Ошибку рантайма, потому что float нельзя складывать напрямую.", misconception: "Float складывается нормально; операция не падает. Загвоздка в точности: результат IEEE 754 — 0.30000000000000004, а не 0.3, так что удивляет лишь напечатанное значение." },
    { label: "0.3, потому что JS округляет вывод, но хранит точную десятичную.", misconception: "JS хранит IEEE 754 double, который сам по себе неточен для 0.1 и 0.2 — он не держит точную десятичную за кулисами. Хранимое значение действительно 0.30000000000000004." },
  ]}
/>

## Сниппет 2 — целое, которое перестаёт считать

```js
const big = 9007199254740991;   // Number.MAX_SAFE_INTEGER
console.log(big + 1);           // ?
console.log(big + 1 === big + 2); // ?
```

<Quiz
  id="05-values-and-types-code-2"
  lessonSlug="05-values-and-types"
  lang="ru"
  question="Что это печатает и в чём глубинная причина?"
  choices={[
    { label: "9007199254740992, затем true. Выше 2^53 у 64-битного double нет свободных бит мантиссы для соседних целых, поэтому big + 1 и big + 2 округляются к одному и тому же представимому значению — целочисленная точность теряется.", correct: true },
    { label: "9007199254740992, затем false — каждое целое всё ещё различимо.", misconception: "За пределами MAX_SAFE_INTEGER 52 бита мантиссы double не могут разделить соседние целые, поэтому big + 1 и big + 2 схлопываются в одно значение и сравнение даёт true, а не false." },
    { label: "Происходит overflow и оборачивается в отрицательное число, как целое фиксированной ширины в C.", misconception: "number в JS не оборачивается; у единого типа double нет фиксированной целочисленной ширины, чтобы переполниться. Вместо этого значение теряет точность — большие целые округляются к ближайшему представимому double." },
    { label: "RangeError, потому что число слишком велико для хранения.", misconception: "Значение хранится нормально — double достигает ~1.8e308. Выше 2^53 ломается целочисленная точность: результат молча округляется, а не отвергается. Для точных больших целых используй BigInt." },
  ]}
/>

## Сниппет 3 — плюс, который меняет решение

```js
console.log(5 + "5");   // ?
console.log(5 - "5");   // ?
console.log("5" * 2);   // ?
```

<Quiz
  id="05-values-and-types-code-3"
  lessonSlug="05-values-and-types"
  lang="ru"
  question="Что печатают три строки и какое правило объясняет различие?"
  choices={[
    { label: "'55', затем 0, затем 10. С +, если один из операндов — строка, JS приводит другой к строке и конкатенирует; с - и *, у которых нет строкового смысла, JS вместо этого приводит строку к числу. Одни значения, разный coercion на каждый оператор.", correct: true },
    { label: "10, 0, 10 — JS всегда конвертирует строки в числа для арифметики.", misconception: "Оператор + перегружен: если одна из сторон — строка, он конкатенирует, поэтому 5 + '5' — это '55', а не 10. Лишь - и * (без строкового смысла) форсируют числовой coercion." },
    { label: "Все три бросают TypeError, потому что нельзя смешивать число и строку.", misconception: "JS динамически типизирован и здесь приводит, а не бросает. Он молча конвертирует операнды, чтобы каждый оператор сработал — именно поэтому неверный тип может дать неверное значение без ошибки." },
    { label: "'55', '5', '52' — строки всегда побеждают и всё становится текстом.", misconception: "Строки побеждают лишь для +. Для - и *, у которых нет строкового определения, JS приводит строку к числу, давая 0 и 10. Направление coercion решает оператор." },
  ]}
/>

## Сниппет 4 — тип, который лжёт

```js
console.log(typeof null);        // ?
console.log(typeof undefined);   // ?
console.log(null === undefined); // ?
```

<Quiz
  id="05-values-and-types-code-4"
  lessonSlug="05-values-and-types"
  lang="ru"
  question="Что печатает каждая строка и какой результат — исторический quirk?"
  choices={[
    { label: "'object', затем 'undefined', затем false. typeof null, возвращающий 'object', — давний баг, сохранённый ради обратной совместимости; null на самом деле примитив. Проверяй его через x === null, а не typeof.", correct: true },
    { label: "'null', 'undefined', false — typeof сообщает каждый примитив его собственным именем.", misconception: "typeof null не возвращает 'null'. Из-за бага в исходной реализации он возвращает 'object', и это нельзя исправить, не сломав веб. Надёжная проверка — x === null." },
    { label: "'object', 'undefined', true — null и undefined равны под ===.", misconception: "null === undefined — это false: строгое равенство сравнивает значение и тип, а это два различных примитива. Они равны лишь нестрого под ==." },
    { label: "'undefined', 'undefined', true — null и undefined — один и тот же примитив.", misconception: "Это различные примитивы с различными результатами typeof: 'object' для null (quirk) и 'undefined' для undefined. Под === они не равны." },
  ]}
/>

<Recap lang="ru">
Каждый сюрприз здесь — та же идея юнита в костюме JS. Округление float (0.1 + 0.2) и предел safe integer (выше 2^53) оба идут от единого 64-битного IEEE 754 double с конечной точностью — отдельного целочисленного типа, к которому можно отступить, нет. Оператор + конкатенирует со строками, но приводит к числам для - и *, так что динамическая типизация молча выбирает правило типа за тебя. А typeof null возвращает 'object' — замороженный исторический баг. Прочитай оператор и формат хранения — и вывод перестаёт быть загадкой.
</Recap>
