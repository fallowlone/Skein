---
slug: 03-primitive-types
lang: ru
track: base-cs
unit: 05-values-and-types
order: 3
title: "Примитивные типы"
summary: "В JavaScript/TypeScript есть пять категорий примитивных значений: number, string, boolean, null и undefined. Оператор typeof сообщает, какое правило интерпретации среда выполнения применяет к значению."
estMin: 22
status: ready
lessonType: coding
prereqs: ["02-types-interpret-bits"]
mathPrereqs: []
concepts: ["primitive-type", "js-number", "js-string", "js-boolean", "js-null", "js-undefined", "typeof"]
sources:
  - https://tc39.es/ecma262/#sec-ecmascript-language-types
  - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Data_structures
  - https://www.typescriptlang.org/docs/handbook/2/everyday-types.html
  - https://262.ecma-international.org/13.0/#sec-typeof-operator
---

import Hook from "../../../../../../components/lesson/Hook.astro";
import Goal from "../../../../../../components/lesson/Goal.astro";
import Idea from "../../../../../../components/lesson/Idea.astro";
import Code from "../../../../../../components/lesson/Code.astro";
import Trace from "../../../../../../components/lesson/Trace.astro";
import Check from "../../../../../../components/lesson/Check.astro";
import Recap from "../../../../../../components/lesson/Recap.astro";
import Inset from "../../../../../../components/lesson/Inset.astro";
import AnnotatedCode from "../../../../../../components/algo/AnnotatedCode.astro";
import AlgoTrace from "../../../../../../components/algo/AlgoTrace.astro";
import PracticeSet from "../../../../../../components/math/PracticeSet.astro";
import Quiz from "../../../../../../components/pedagogy/Quiz.astro";

<Hook>
Ты теперь знаешь, что тип — это правило интерпретации битов. Следующий логичный вопрос:
какие типы реально доступны в языке, который ты изучаешь?

В JavaScript и TypeScript каждое значение принадлежит одной из небольшого набора
встроенных категорий. Простейшие члены категорий — те, что хранят один неделимый кусок
данных, — называются **примитивными типами**. Они образуют дно иерархии типов: числа,
текстовые символы, решения истина/ложь, «здесь ничего», «ещё не задано». Примитивное
значение нельзя разложить на более мелкие типизированные части.

Этот урок проходит через каждый примитивный тип в JS/TS, привязывает каждый к его
представлению на уровне битов и вводит `typeof` — способ, которым среда выполнения
сообщает тебе, какое правило интерпретации она применяет к значению.
</Hook>

<Goal lang="ru">
После этого урока ты сможешь назвать все пять категорий примитивных типов в JS/TS,
описать, что каждая из них представляет на уровне битов, предсказать результат выражения
`typeof` для любого примитивного значения, и объяснить, что означают null и undefined как
два разных «отсутствия».
</Goal>

<Idea lang="ru">
JavaScript и TypeScript определяют пять категорий примитивных значений:

1. **`number`** — любое числовое значение: целые числа, дроби и специальные значения
   IEEE 754: `Infinity`, `-Infinity` и `NaN`. Внутри хранится как 64-битное число с
   двойной точностью по стандарту IEEE 754 (8 байт). Даже целые числа вроде `42`
   хранятся именно так — у JS нет отдельного целочисленного типа.

2. **`string`** — последовательность кодовых точек Unicode. В движке V8 короткая строка
   может храниться как последовательность 16-битных кодовых единиц UTF-16 в смежной
   памяти. Строковое значение — не один байт; это структурированное значение с длиной и
   блоком символьных данных.

3. **`boolean`** — ровно два значения: `true` и `false`. На аппаратном уровне это
   соответствует концепции 1 бита (хотя движки обычно хранят его как тегированное слово,
   а не отдельный бит, из соображений выравнивания). Семантика: одно из двух взаимно
   исключающих состояний.

4. **`null`** — намеренное отсутствие какого-либо значения. Программист пишет `null`,
   чтобы сказать: «этот слот намеренно ничего не содержит». Существует ровно одно
   значение null.

5. **`undefined`** — непреднамеренное или незаданное отсутствие значения. Объявленная, но
   ещё не присвоенная переменная содержит `undefined`. Существует ровно одно значение
   undefined.

Оператор **`typeof`** — это способ, которым среда выполнения сообщает тип значения. Для
любого выражения `typeof expr` возвращает строку с именем типа. Для примитивов:

| Значение | Результат `typeof` |
|---|---|
| `42` | `"number"` |
| `"hello"` | `"string"` |
| `true` | `"boolean"` |
| `undefined` | `"undefined"` |
| `null` | `"object"` ← историческая особенность |

Случай с `null` — широко известная языковая особенность: `typeof null` возвращает
`"object"` из-за ошибки в оригинальной реализации JS, которую не удалось исправить из
соображений обратной совместимости. На практике: `null` является примитивом, а не
объектом.
</Idea>

<Code lang="ru">

<AnnotatedCode
  code={`// 1. number — 64-битный IEEE 754 double в памяти
const temperature = 36.6;
const count = 100;
const big = 9007199254740991;   // Number.MAX_SAFE_INTEGER

console.log(typeof temperature); // "number"
console.log(typeof count);       // "number"
console.log(typeof NaN);         // "number" — NaN это числовой паттерн битов

// 2. string — последовательность кодовых точек Unicode
const greeting = "hello";
const empty = "";

console.log(typeof greeting);    // "string"
console.log(greeting.length);    // 5 — длина является частью структурированного значения

// 3. boolean — только true или false
const isReady = true;
const isOver = false;

console.log(typeof isReady);     // "boolean"

// 4. null — намеренное отсутствие
const slot = null;
console.log(typeof slot);        // "object" — историческая особенность, null ЯВЛЯЕТСЯ примитивом
console.log(slot === null);      // true — проверяй на null через ===, а не typeof

// 5. undefined — незаданное / ещё-не-присвоенное
let future;                      // объявлена, но не присвоена
console.log(typeof future);      // "undefined"
console.log(future === undefined);// true`}
  annotations={[
    { line: 2, text: "number: хранит 36.6 как 64-битный IEEE 754 double (8 байт)" },
    { line: 7, text: "typeof возвращает строку с именем типа" },
    { line: 8, text: "NaN — специальный паттерн битов IEEE 754, тип по-прежнему 'number'" },
    { line: 12, text: "string: свойство length сообщает количество кодовых точек" },
    { line: 17, text: "boolean: ровно два значения, true и false" },
    { line: 21, text: "null: typeof возвращает 'object' — известная ошибка JS, не исправляемая для совместимости" },
    { line: 22, text: "Используй === null для определения null, а не typeof" },
    { line: 26, text: "undefined: переменная объявлена, но никогда не присваивалась" },
  ]}
/>
</Code>

<Trace lang="ru">
Наблюдай, как `typeof` реагирует на каждый вид значения. Мы трассируем небольшую
программу, которая присваивает пять переменных (по одной на примитивный тип) и
выводит результат их `typeof`.

<AlgoTrace
  id="trace-primitive-types"
  lang="ru"
  code={[
    "const n = 42;",
    "const s = 'A';",
    "const b = true;",
    "const x = null;",
    "let u;",
    "console.log(typeof n, typeof s, typeof b, typeof x, typeof u);"
  ]}
  frames={[
    {
      caption: "Начало: переменных ещё нет.",
      line: 1,
      cells: []
    },
    {
      caption: "Строка 1: n = 42. Среда выполнения хранит 42 как 64-битный IEEE 754 double. typeof n = 'number'.",
      line: 1,
      cells: [
        { value: "42", mark: "active" },
      ]
    },
    {
      caption: "Строка 2: s = 'A'. Среда выполнения хранит строку 'A'. typeof s = 'string'.",
      line: 2,
      cells: [
        { value: "42", mark: "done" },
        { value: "'A'", mark: "active" },
      ]
    },
    {
      caption: "Строка 3: b = true. typeof b = 'boolean'.",
      line: 3,
      cells: [
        { value: "42", mark: "done" },
        { value: "'A'", mark: "done" },
        { value: "true", mark: "active" },
      ]
    },
    {
      caption: "Строка 4: x = null. typeof x = 'object' (историческая особенность). Значение — null, намеренное отсутствие.",
      line: 4,
      cells: [
        { value: "42", mark: "done" },
        { value: "'A'", mark: "done" },
        { value: "true", mark: "done" },
        { value: "null", mark: "active" },
      ]
    },
    {
      caption: "Строка 5: u объявлена, но не присвоена. u = undefined. typeof u = 'undefined'.",
      line: 5,
      cells: [
        { value: "42", mark: "done" },
        { value: "'A'", mark: "done" },
        { value: "true", mark: "done" },
        { value: "null", mark: "done" },
        { value: "undef", mark: "active" },
      ]
    },
    {
      caption: "Строка 6: console.log печатает: 'number string boolean object undefined'. Все пять примитивов.",
      line: 6,
      cells: [
        { value: "42", mark: "done" },
        { value: "'A'", mark: "done" },
        { value: "true", mark: "done" },
        { value: "null", mark: "done" },
        { value: "undef", mark: "done" },
      ]
    }
  ]}
/>
</Trace>

<Inset kind="mistake" lang="ru">
`null` и `undefined` выглядят похоже, но означают разное. `null` — намеренный выбор
программиста: «Я явно говорю, что этот слот ничего не содержит». `undefined` —
автоматическое: «эта переменная объявлена, но никогда не получала значения, или это
свойство не существует». Рассматривай их как два разных состояния отсутствия. В строгом
режиме TypeScript компилятор отслеживает их отдельно — функция, которая может вернуть
`null`, отличается от функции, которая может вернуть `undefined`.
</Inset>

<PracticeSet
  id="primitive-types-practice"
  lessonSlug="03-primitive-types"
  lang="ru"
  problems={[
    {
      prompt: "Сколько байт занимает число JS (IEEE 754 double) в памяти?",
      answer: 8,
      hint: "IEEE 754 с двойной точностью — это 64-битный формат. 64 бита ÷ 8 = 8 байт.",
    },
    {
      prompt: "Что возвращает typeof 42? Введи ASCII-код первого символа строки-результата 'number', которая начинается с 'n'. ASCII 'n' = 110.",
      answer: 110,
      hint: "typeof 42 возвращает строку 'number'. Её первый символ — 'n', ASCII-код 110.",
    },
    {
      prompt: "Переменная объявлена через let x; и никогда не присваивалась. Сколько символов в строке 'undefined', которую возвращает typeof x?",
      answer: 9,
      hint: "Строка 'undefined' содержит 9 символов: u-n-d-e-f-i-n-e-d.",
    },
    {
      prompt: "Сколько различных булевых значений существует в JS? (true и false.)",
      answer: 2,
      hint: "Boolean имеет ровно два значения: true и false.",
    },
    {
      prompt: "Что возвращает typeof null? Введи длину строки-результата. Строка — 'object'. Сколько в ней символов?",
      answer: 6,
      hint: "Строка 'object' содержит 6 символов: o-b-j-e-c-t.",
    },
  ]}
/>

<Check lang="ru">
  <Quiz
    id="primitive-types-check"
    pieceSlug="03-primitive-types"
    lang="ru"
    question="Что корректно описывает, что возвращает typeof null, и почему?"
    choices={[
      {
        label: "'object' — историческая ошибка в оригинальной реализации JS, которую нельзя исправить из-за обратной совместимости",
        correct: true,
      },
      {
        label: "'null' — среда выполнения корректно идентифицирует null как свой собственный примитивный тип",
        misconception: "typeof null не возвращает 'null'. Он возвращает 'object' — это известная языковая особенность. Для надёжной проверки на null используй строгое равенство: x === null.",
      },
      {
        label: "'undefined' — null и undefined это одно и то же значение отсутствия",
        misconception: "null и undefined — это разные значения отсутствия. null означает намеренное отсутствие (программист написал null). undefined означает неинициализированное или незаданное. Это отдельные примитивы с разными результатами typeof.",
      },
    ]}
  />
</Check>

<Recap lang="ru">
JavaScript и TypeScript определяют пять **примитивных типов**: `number` (64-битный IEEE
754 double), `string` (последовательность Unicode), `boolean` (true или false), `null`
(намеренное отсутствие) и `undefined` (неинициализированное или отсутствующее). Каждый
представляет конкретное правило интерпретации, применяемое к битам, которые хранит среда
выполнения. Оператор **`typeof`** возвращает имя этого правила в виде строки — с одной
исторической особенностью: `typeof null` возвращает `"object"`, а не `"null"`.
Примитивные значения неделимы: из `42` нельзя прочитать подполе так, как из объекта
читают свойство. Они — атомарные строительные блоки, из которых состоят все значения в
JS/TS.
</Recap>
