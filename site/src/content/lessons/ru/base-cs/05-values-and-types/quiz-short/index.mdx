---
concepts:
- value-as-bits
- type-as-rule
- same-bits-different-types
- primitive-types
- null-vs-undefined
- static-vs-dynamic
estMin: 13
lang: ru
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://craftinginterpreters.com/values-and-types.html
- https://tc39.es/ecma262/#sec-ecmascript-language-types
- https://en.wikipedia.org/wiki/Type_system
status: ready
summary: "Промпты на свободное припоминание по юниту. Сначала ответь по памяти, затем открой модельный ответ и сравни, что упустил."
title: 'Значения и типы: свободное припоминание'
track: base-cs
unit: 05-values-and-types
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт проговори или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие вытащить его и закрепляет идею.
</Hook>

<Goal lang="ru">
Восстанови хребет юнита, не подглядывая: что такое value, что делает type, почему одни и те же биты декодируются по-разному, повседневные примитивы JS, различие null/undefined и зачем вообще существуют системы типов.
</Goal>

<RetrievalDrawer
  client:load
  id="05-values-and-types-short-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое value на уровне машины и откуда берётся его смысл?", a: "Value — фиксированный паттерн битов, занимающий одну или несколько подряд идущих ячеек памяти, хранящийся по конкретному адресу и занимающий конкретное число байт. Биты пассивны: аппаратура хранит и извлекает их, не заботясь о том, что они представляют. Байт 01000001 — это просто восемь состояний вкл/выкл. Смысл не в битах — он возникает лишь когда программа читает их с выбранной интерпретацией. Две программы, читающие один байт под разными правилами, законно увидят разные значения." },
    { q: "Что такое type и какие две части информации он даёт?", a: "Type — правило интерпретации, которое программа применяет к паттерну битов, чтобы превратить его в пригодное значение. Он даёт две вещи: размер — сколько байт занимает значение — и интерпретацию — как декодировать эти байты (place-value для integer, поиск по таблице для символа, разбивка sign/exponent/mantissa для IEEE 754 float). Знание адреса говорит, где биты; лишь type говорит, сколько их читать и что они значат. Важно: type живёт в программе, компиляторе или рантайме — но не как метка внутри ячейки." },
    { q: "Покажи, как один паттерн битов даёт разные значения под разными типами.", a: "Возьми единственный байт 01000001. Как беззнаковое 8-битное integer, двоичный place-value даёт 64 + 1 = 65. Как символ ASCII, код 65 сопоставляется букве 'A'. Как красный канал пикселя RGB, это интенсивность красного 65 из 255. Физические биты не меняются между этими прочтениями — меняется лишь применяемое к ним правило. Это не баг; это механизм, позволяющий одной физической памяти представлять числа, текст, цвета и всё остальное." },
    { q: "Назови повседневные примитивы JS/TS и один факт о том, как хранятся числа.", a: "Пять повседневных примитивов — number, string, boolean, null и undefined. (Полная спека добавляет symbol и bigint, итого семь.) Ключевой факт о хранении: каждый number в JS — будь то 42 или 36.6 — это единый 64-битный IEEE 754 double. Отдельного целочисленного типа нет, поэтому 0.1 + 0.2 не равно точно 0.3, а целые выше 2^53 теряют точность. String — структурированная последовательность code points; boolean — одно из ровно двух состояний." },
    { q: "Различи null и undefined и скажи, что возвращает typeof null.", a: "Оба — значения отсутствия, но вид отсутствия различается. null — намеренное отсутствие: программист пишет его, чтобы сказать 'здесь ничего, специально'. undefined — автоматическое отсутствие: объявленная, но не присвоенная переменная или отсутствующее свойство, которое даёт рантайм. Это различные примитивы; strict mode TypeScript отслеживает их как отдельные типы. Знаменитый quirk: typeof null возвращает 'object' — замороженный баг исходной реализации, поэтому null проверяют через x === null, а не typeof." },
    { q: "Зачем существуют системы типов и чем различаются статическая и динамическая проверка?", a: "Системы типов существуют, чтобы ловить одну глубинную проблему: применение неверного правила интерпретации к паттерну битов — чтение битов, сохранённых под одним типом, как будто это другой — что даёт неверные значения, падения или (в низкоуровневых языках) повреждение памяти. Type error — это обнаруженное несоответствие между хранимым типом и применённым. Статическая типизация (TypeScript) обнаруживает его до запуска, устраняя весь класс для типокорректного кода; динамическая (JavaScript) обнаруживает лишь когда плохая строка выполнится — или, при разрешающем coercion, может не пометить вовсе. Одна проблема, разный момент обнаружения." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: value — это сырые биты, type — правило размер-плюс-интерпретация, дающее им смысл, одни и те же биты декодируются в разные значения под разными правилами, JS открывает горстку примитивов (каждый number — 64-битный IEEE 754 double, а null/undefined — два различных отсутствия), а система типов существует, чтобы ловить применение неверного правила — рано на этапе компиляции или поздно во время выполнения.
</Recap>
