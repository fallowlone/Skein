---
slug: 01-three-guarantees
lang: ru
track: queues
unit: 01-delivery-guarantees
order: 1
title: "At-most-once, at-least-once, exactly-once: три контракта доставки"
summary: "Что означают три гарантии доставки, почему exactly-once delivery невозможна и почему at-least-once — production-стандарт."
estMin: 8
status: ready
lessonType: topic
level: junior
prereqs: []
deepensInto:
  - queues/01-delivery-guarantees/02-failure-modes
  - queues/01-delivery-guarantees/03-consumer-dedup
spiral: ["statefulness", "encapsulation"]
concepts: ["at-most-once", "at-least-once", "exactly-once", "delivery-guarantee", "idempotency"]
sources:
  - https://docs.confluent.io/kafka/design/delivery-semantics.html
  - https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-visibility-timeout.html
  - https://blog.bulloak.io/post/20200917-the-impossibility-of-exactly-once/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";

<Hook>
Payment worker обрабатывает сообщение «списать $50» и падает до того, как успевает сообщить очереди о завершении. Очередь передоставляет. Клиент списан дважды. Это не баг в коде — это поведение по умолчанию каждой production-очереди в мире.
</Hook>

<Crux>Exactly-once delivery невозможна. Production-фикс — at-least-once delivery плюс идемпотентный consumer, игнорирующий дубликаты.</Crux>

<Explanation>

## Три контракта

Каждая message queue предлагает delivery-контракт — гарантию о том, сколько раз сообщение дойдёт до consumer-а при сбоях.

**At-most-once.** Брокер отправляет сообщение один раз и никогда не ретраит. Если сеть роняет сообщение — оно потеряно. Дубликатов нет, но возможна потеря. Use case: debug-логи, некритичная телеметрия. Никогда — для денег, заказов или событий, потеря которых имеет бизнес-последствия.

**At-least-once.** Брокер ретраит, пока consumer не отправит явный ack. Если ack потерян — брокер передоставляет. Сообщение доходит минимум один раз, но возможно и больше. At-least-once — дефолт в SQS, RabbitMQ (with ack) и Kafka с дефолтными настройками.

**Exactly-once.** Каждое сообщение обрабатывается ровно один раз независимо от retry. Это то, что нужно. Проблема: чистая exactly-once delivery поверх ненадёжной сети математически невозможна — Two Generals' Problem доказывает, что две стороны поверх ненадёжного канала никогда не могут быть уверены, что обе знают одно и то же. Нельзя гарантировать, что обе стороны знают: сообщение доставлено ровно один раз.

Что *можно* достичь — **exactly-once processing**: side effect consumer-а (списание карты, отправка email, обновление строки) происходит ровно один раз, даже если брокер доставит сообщение более чем один раз. Трюк — идемпотентный consumer.

<div data-lesson-visual class="overflow-x-auto my-6 rounded-lg border border-border bg-surface-1 p-4 text-sm font-mono">
  <div class="mb-3 text-ink-2 text-xs uppercase tracking-wider">Три контракта доставки</div>
  <div class="flex flex-col gap-3">
    <div class="flex items-start gap-3">
      <span class="shrink-0 w-24 rounded bg-yellow-100 dark:bg-yellow-900/40 text-yellow-800 dark:text-yellow-200 text-xs font-bold px-2 py-1 text-center">at-most-once</span>
      <span>Отправить один раз, без retry. Может терять. Подходит для метрик, логов.</span>
    </div>
    <div class="flex items-start gap-3">
      <span class="shrink-0 w-24 rounded bg-blue-100 dark:bg-blue-900/40 text-blue-800 dark:text-blue-200 text-xs font-bold px-2 py-1 text-center">at-least-once</span>
      <span>Retry до ack. Может доставить 2+ раз. Production-дефолт. Требует идемпотентного consumer.</span>
    </div>
    <div class="flex items-start gap-3">
      <span class="shrink-0 w-24 rounded bg-green-100 dark:bg-green-900/40 text-green-800 dark:text-green-200 text-xs font-bold px-2 py-1 text-center">exactly-once</span>
      <span>Delivery невозможна. Processing достижимо через consumer dedup. 20–30% overhead в Kafka.</span>
    </div>
  </div>
</div>

## Почему at-least-once — безопасный дефолт

At-most-once принимает потери. Для платёжной системы потерянное «списать клиента» — пропущенная транзакция, недопустимо. At-least-once принимает дубликаты. Дублирующее «списать клиента» — двойное списание, тоже плохо, но *исправимо* на стороне consumer. Exactly-once delivery недостижима без дорогой координации.

Конвергенция индустрии: at-least-once delivery + идемпотентный consumer. Consumer проверяет «обработал ли я этот msg ID уже?» перед действием. Дубликаты игнорируются. Очередь может ретраить без вреда.

<Quiz
  id="01-q1"
  pieceSlug="01-three-guarantees"
  lang="ru"
  question="Какая гарантия доставки — дефолт для SQS и Kafka?"
  choices={[
    { label: "Exactly-once — брокер сам всё дедупит", misconception: "Exactly-once требует opt-in механизмов (транзакции, idempotent producer); дефолт — at-least-once." },
    { label: "At-least-once — retry до ack consumer-а, принимая дубликаты как цену durability", correct: true },
    { label: "At-most-once — никогда не ретраит для низкой латентности", misconception: "At-most-once — opt-in для latency-critical use-кейсов; дефолт — at-least-once." },
    { label: "Случайно варьируется", misconception: "Delivery-семантика — детерминированный контракт; дефолты документированы." },
  ]}
/>

<Quiz
  id="01-q2"
  pieceSlug="01-three-guarantees"
  lang="ru"
  question="Почему exactly-once processing достижима, а exactly-once delivery — нет?"
  choices={[
    { label: "Delivery требует, чтобы и producer, и consumer согласились на одном факте поверх ненадёжного канала — математически невозможно (Two Generals). Processing требует только одну сторону (consumer) с dedup-логом — это достижимо.", correct: true },
    { label: "Железо ограничивает сеть", misconception: "Дело в impossibility-доказательстве ненадёжного канала, не в железе." },
    { label: "Брокеры гарантируют one delivery при оплате premium tier", misconception: "Никакой paid tier не решает базовую невозможность; фикс — consumer-side dedup." },
    { label: "Отключение retry даёт exactly-once", misconception: "Отключение retry даёт at-most-once, не exactly-once — сообщения могут теряться." },
  ]}
/>

<DragOrder
  id="01-drag-1"
  pieceSlug="01-three-guarantees"
  lang="ru"
  prompt="Поставь шаги at-least-once платёжного потока с consumer-side dedup:"
  items={[
    "Producer генерирует уникальный msg ID и шлёт 'списать $50' в брокер",
    "Брокер надёжно сохраняет сообщение и ack-ит producer",
    "Consumer получает сообщение и читает ID 'msg-7a3f'",
    "Consumer проверяет dedup-стор: 'обрабатывал ли msg-7a3f раньше?'",
    "Dedup-стор отвечает 'нет' — consumer вызывает payment API и записывает списание",
    "Consumer пишет 'msg-7a3f обработан' в dedup-стор",
    "Consumer ack-ит брокер — сообщение удалено из очереди",
  ]}
/>

</Explanation>

<KeyTakeaway>At-most-once теряет; at-least-once дублирует; exactly-once delivery невозможна. Production-стандарт: at-least-once + идемпотентный consumer = effectively exactly-once processing.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-three-guarantees-retrieval"
  lang="ru"
  questions={[
    { q: "Назови три гарантии доставки и по одному use case для каждой.", a: "At-most-once: метрики/логи (потеря допустима). At-least-once: платёжные события, обработка заказов (потеря недопустима, дубликаты исправимы). Exactly-once: stream-to-stream внутри Kafka через транзакции (дорого, ~20–30% overhead)." },
    { q: "Почему at-least-once — production-дефолт, а не exactly-once?", a: "Чистая exactly-once delivery математически невозможна (Two Generals' Problem). Kafka-style транзакционная exactly-once стоит 20–30% throughput и привязывает систему к одной экосистеме брокера. At-least-once + idempotent consumer достигает той же корректности через любой брокер с намного меньшей ценой." },
    { q: "Какое свойство должен иметь consumer, чтобы at-least-once delivery была безопасной?", a: "Идемпотентность — обработка одного и того же сообщения несколько раз даёт тот же результат, что и один раз. Реализуется через dedup-стор по msg ID, который consumer проверяет перед действием." },
  ]}
/>

<Recap lang="ru">
Каждая очередь предлагает delivery-контракт: at-most-once принимает потери, at-least-once принимает дубликаты, exactly-once delivery невозможна — Two Generals' Problem доказывает, что никакой конечный протокол поверх ненадёжного канала не даст обеим сторонам уверенности в согласии. Exactly-once *processing* достижима: consumer держит dedup-лог по msg ID и пропускает уже обработанные. Это конвертирует at-least-once delivery в exactly-once processing ценой dedup-стора — вот почему at-least-once — production-дефолт в SQS, Kafka и RabbitMQ.
</Recap>
