---
concepts:
- two-generals
- effectively-once
- transactional-dedup
- visibility-timeout
- kafka-transactions
- outbox-pattern
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://en.wikipedia.org/wiki/Two_Generals%27_Problem
- https://docs.confluent.io/kafka/design/delivery-semantics.html
- https://stripe.com/docs/api/idempotent_requests
status: ready
summary: "Промпты на свободное припоминание по всему юниту. Сначала ответь по памяти, потом открой эталонный ответ и сравни."
title: 'Гарантии доставки: тест на свободное припоминание'
track: queues
unit: 01-delivery-guarantees
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт скажи или запиши полный ответ по памяти, прежде чем открыть эталон — именно усилие припоминания связывает доказательство невозможности с production-паттернами.
</Hook>

<Goal lang="ru">
Реконструируй позвоночник юнита, не подсматривая в уроки: почему exactly-once delivery невозможна, откуда берутся дубликаты, как консьюмер делает их безвредными и какой инструмент брокера какой пробел закрывает.
</Goal>

<RetrievalDrawer
  client:load
  id="01-delivery-guarantees-short-retrieval"
  lang="ru"
  questions={[
    { q: "Сформулируй Two Generals' Problem и объясни асимметрию, делающую exactly-once processing достижимым, хотя exactly-once delivery — нет.", a: "Две стороны, общающиеся над ненадёжным каналом, никогда не достигнут взаимной уверенности, что разделяют один согласованный факт. Доказательство от противного: возьми последнее сообщение, которое при доставке гарантирует согласие; убери его — протокол всё ещё должен работать, ведь отправитель уже имел уверенность — индуктивно сводя к нулю сообщений, что абсурдно. Exactly-once delivery требует, чтобы и producer, и брокер знали, что сообщение доставлено ровно один раз: двусторонняя уверенность, невозможно. Exactly-once processing требует лишь одной стороны — консьюмера — держать dedup-лог и пропускать уже обработанные сообщения. Односторонняя уверенность достижима, поэтому production-цель — effectively-once: доставка at-least-once плюс идемпотентный консьюмер." },
    { q: "Назови три плеча сбоев и скажи, какое порождает больше всего production-дубликатов и почему.", a: "Плечо 1, producer→broker: потерянный ack заставляет продюсера ретраить, и брокер видит сообщение дважды (idempotent producer Kafka чинит это за ~3%). Плечо 2, durability брокера: краш до репликации теряет сообщение — риск потери при неправильной конфигурации, не источник дубликатов. Плечо 3, broker→consumer: консьюмер обрабатывает сообщение (списывает карту, пишет в БД), затем падает до ack, поэтому после visibility timeout брокер передоставляет и side effect выполняется снова. Плечо 3 доминирует, потому что side effect уже виден, и только консьюмер может записать, что он случился — поэтому надёжный фикс это идемпотентность консьюмера." },
    { q: "Почему dedup INSERT и side effect должны коммититься в одной транзакции и почему INSERT первым?", a: "Если dedup INSERT коммитится отдельно, а side effect затем падает, сообщение помечено обработанным, но работа не выполнена — и передоставка пропустит его навсегда, тихая потеря. Если side effect выполнился, а потом INSERT упал, работа сделана, но ничего не записано, поэтому передоставка её повторит. Обёртка обоих в одну транзакцию делает их успешными или провальными вместе. INSERT-first с UNIQUE-ограничением также побеждает гонку одновременной доставки: второй консьюмер на то же сообщение упирается в нарушение UNIQUE, вся транзакция откатывается, side effect не выполняется, и он просто делает ack. SELECT-then-act не атомарен и снова открывает гонку при каждом ребалансе." },
    { q: "Какое правило большого пальца для SQS visibility timeout и что сбрасывается при передоставке?", a: "Ставь дефолт очереди примерно в 6x средней обработки, чтобы сообщение не появлялось снова у других консьюмеров, пока первый ещё законно работает — этот зазор и есть окно одновременных дубликатов. Для сильно переменной обработки используй heartbeat: вызывай ChangeMessageVisibility каждые timeout/3 из фонового потока, тогда краш даёт таймауту истечь естественно, а застрявший консьюмер восстанавливается. Критично: при передоставке таймаут сбрасывается к дефолту очереди — расширенное значение через ChangeMessageVisibility было привязано к прежнему receipt handle, поэтому надо поднимать сам дефолт очереди или заново слать heartbeat на каждой доставке." },
    { q: "Что покрывают idempotent producer Kafka и транзакции по отдельности, и что они НЕ покрывают?", a: "Idempotent producer (enable.idempotence=true, ~3%) назначает producer-ID и per-partition sequence numbers, поэтому брокер тихо отбрасывает дубликаты от ретраев продюсера — только плечо 1. Транзакции (transactional.id + read_committed консьюмеры, ~20–30%) оборачивают записи в несколько партиций плюс коммит оффсета в один атомарный блок через координатора, делая цикл read-process-write внутри Kafka exactly-once. Ни то ни другое не покрывает кросс-системные записи: как только пишешь в Postgres или вызываешь Stripe, выходишь за границу транзакции Kafka, поэтому краш между коммитом Kafka и внешней записью её повторяет. Кросс-системной корректности нужен дедуп консьюмера (ON CONFLICT DO NOTHING по offset/msg_id) плюс Idempotency-Key для внешних API." },
    { q: "Что такое проблема dual-write и как паттерн outbox её решает?", a: "Dual-write: приложение обновляет БД и публикует событие двумя независимыми операциями. Если БД закоммитилась, а публикация упала (брокер недоступен, краш процесса), downstream-консьюмеры никогда не увидят событие — тихая потеря данных; обратный порядок оставляет событие без закоммиченного состояния. Паттерн outbox делает INSERT payload события в таблицу outbox в ТОЙ ЖЕ транзакции, что и бизнес-обновление, поэтому строка события существует тогда и только тогда, когда обновление закоммичено. Отдельный sender (поллинг или CDC через Debezium, читающий transaction log) публикует pending-строки и помечает их sent; краш после публикации до пометки оставляет строку pending, поэтому она переопубликуется — дубликат, который поглощают idempotent producer или дедуп консьюмера." },
  ]}
/>

<Recap lang="ru">
Если смог реконструировать каждый ответ по памяти — ты держишь позвоночник юнита: Two Generals делает exactly-once delivery невозможной, поэтому прод работает по at-least-once и обеспечивает effectively-once в консьюмере. Дубликаты концентрируются на плече 3; INSERT-first транзакционный дедуп плюс Idempotency-Key для внешних вызовов делают их безвредными. Idempotent producer Kafka и транзакции укрепляют только путь внутри Kafka, SQS visibility timeout надо ставить в ~6x обработки, а паттерн outbox закрывает dual-write на стороне продюсера. Повторяющийся урок: гарантии брокера необходимы, но никогда не достаточны — корректность живёт в консьюмере и в операционной дисциплине.
</Recap>
