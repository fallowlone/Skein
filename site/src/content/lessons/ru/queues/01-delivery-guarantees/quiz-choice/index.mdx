---
concepts:
- at-least-once
- idempotent-consumer
- visibility-timeout
- kafka-transactions
- outbox-pattern
- two-generals
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://docs.confluent.io/kafka/design/delivery-semantics.html
- https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-visibility-timeout.html
- https://en.wikipedia.org/wiki/Two_Generals%27_Problem
status: ready
summary: "Тест с выбором на синтез по всему юниту — доказательство невозможности, три плеча сбоев, idempotent-консьюмеры, Kafka EOS, SQS-таймауты и outbox."
title: 'Гарантии доставки: тест с выбором ответа'
track: queues
unit: 01-delivery-guarantees
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает решение, которое ты принимаешь в реальном инциденте — не определение для заучивания, а компромисс, который надо взвесить, пока клиентов списывают дважды.
</Hook>

<Goal lang="ru">
Убедись, что связываешь доказательство невозможности, плечи сбоев, идемпотентность консьюмера, транзакции Kafka, механику SQS и outbox в одну связную историю end-to-end корректности.
</Goal>

<Quiz
  id="01-delivery-guarantees-choice-1"
  lessonSlug="01-delivery-guarantees"
  lang="ru"
  question="Вендор рекламирует у очереди 'exactly-once delivery'. Senior-ревьюер возражает. Какое возражение точное и корректное?"
  choices={[
    { label: "Exactly-once delivery невозможна над ненадёжным каналом (Two Generals): стороны никогда не достигнут взаимной уверенности об одной доставке. Достижимо exactly-once processing — односторонняя уверенность через dedup-лог консьюмера. Читай этот ярлык как at-least-once плюс дедуп где-то на стороне.", correct: true },
    { label: "Возражение неверно — современные брокеры решили это усиленной репликацией.", misconception: "Больше реплик улучшают durability, а не двустороннее согласие. Никакая репликация не даёт producer и consumer общей уверенности над теряющим каналом; невозможность информационно-теоретическая, а не аппаратное ограничение." },
    { label: "Всё нормально, если ещё включить TCP, который дедуплицирует на уровне пакетов.", misconception: "TCP дедуплицирует пакеты внутри соединения; он ничего не говорит о повторной доставке на уровне приложения после краша консьюмера или реконнекта. Дубликаты гарантий доставки живут выше транспортного уровня." },
    { label: "Exactly-once достижима, но только синхронным two-phase commit на каждом сообщении.", misconception: "Two-phase commit между producer, брокером и consumer непрактичен на пропускной способности очереди и всё равно не побеждает асимметрию Two Generals. Production-ответ — односторонний дедуп консьюмера, а не распределённое согласие." },
  ]}
/>

<Quiz
  id="01-delivery-guarantees-choice-2"
  lessonSlug="01-delivery-guarantees"
  lang="ru"
  question="Из трёх плеч сбоев какое порождает дубликаты, с которыми реально борешься в проде, и почему фикс ложится на консьюмера?"
  choices={[
    { label: "Плечо 3, broker→consumer: консьюмер выполняет side effect, затем падает до ack, поэтому после таймаута брокер передоставляет и side effect выполняется снова. Брокер не может знать, что side effect уже случился — записать это может только консьюмер, поэтому надёжный фикс — идемпотентность консьюмера.", correct: true },
    { label: "Плечо 1, producer→broker — потерянный ack продюсера это главный источник дубликатов.", misconception: "Ретраи плеча 1 при потере ack действительно дают дубликаты, но idempotent producer Kafka нейтрализует их ценой ~3%. Постоянная боль прода — плечо 3, где обработка уже дала видимый side effect до краша." },
    { label: "Плечо 2, durability брокера — лаг репликации дублирует сообщения на failover.", misconception: "Плечо 2 — риск потери при неправильной конфигурации (acks не all), а не источник дубликатов. Корректно настроенная durability дубликатов не порождает." },
    { label: "Никакое — дубликаты это миф, если поставить таймаут достаточно большим.", misconception: "Большой таймаут уменьшает окна одновременных дубликатов, но никогда не убирает краш после обработки до ack. At-least-once означает, что дубликаты — структурная неизбежность, которую проектируют, а не вытюнивают." },
  ]}
/>

<Quiz
  id="01-delivery-guarantees-choice-3"
  lessonSlug="01-delivery-guarantees"
  lang="ru"
  question="Команда реализует дедуп консьюмера так: SELECT по msg_id; если нет — вызвать Stripe и сделать INSERT. При ребалансе consumer-group всё равно видят двойные списания. В чём структурный дефект?"
  choices={[
    { label: "SELECT-then-act не атомарен. Два консьюмера могут оба увидеть 'нет' для одного сообщения при ребалансе, оба вызвать Stripe, затем оба сделать INSERT. Фикс: INSERT-first внутри одной транзакции — нарушение UNIQUE откатывает весь блок, и side effect не выполнится дважды.", correct: true },
    { label: "Забыли поднять visibility timeout; сам паттерн SELECT корректен.", misconception: "Больший таймаут сужает окно, но гонка SELECT/INSERT — баг корректности независимо от тайминга; ребаланс или любая одновременная доставка её снова открывает. Дедуп надо сделать атомарным, а не просто медленнее в гонке." },
    { label: "Stripe списывает дважды, потому что Idempotency-Key работает только 24 часа.", misconception: "TTL здесь ни при чём; очереди передоставляют за секунды. Двойное списание — от неатомарного локального дедупа, который пропускает два вызова side effect раньше, чем приземлится любой INSERT." },
    { label: "Таблице processed нужен индекс, а не транзакция.", misconception: "Индекс ускоряет поиск, но не сериализует две одновременные транзакции; только попадание в UNIQUE-ограничение внутри той же транзакции, что и side effect, предотвращает дубликат." },
  ]}
/>

<Quiz
  id="01-delivery-guarantees-choice-4"
  lessonSlug="01-delivery-guarantees"
  lang="ru"
  question="У Kafka Streams-джобы стоит enable.idempotence=true и transactional.id, она пишет в output-топик И в таблицу Postgres. После рестарта брокера в Postgres всё равно дубли строк. Почему?"
  choices={[
    { label: "Транзакции Kafka атомарны только внутри Kafka — запись в output-топик плюс коммит оффсета. Запись в Postgres вне этой границы, поэтому краш между коммитом Kafka и записью в БД повторяет запись в БД. Postgres нужна своя идемпотентность (ON CONFLICT DO NOTHING по offset/msg_id).", correct: true },
    { label: "transactional.id не был уникален, поэтому координатор смешал потоки.", misconception: "Неуникальный transactional.id вызывает проблемы fencing, но даже с идеальной конфигурацией транзакции Kafka никогда не охватывают Postgres. Дубликат структурный: запись в БД вне границы транзакции Kafka." },
    { label: "Консьюмеру не хватало isolation.level=read_committed, что дедуплицировало бы строки в БД.", misconception: "read_committed лишь скрывает прерванные записи Kafka от консьюмера; на запись, которую твой код делает в Postgres, он не влияет. БД всё равно нужен свой дедуп." },
    { label: "Idempotent producer на ~3% недостаточно; нужны транзакции на ~20–30%, чтобы покрыть БД.", misconception: "Транзакции у них уже есть. Ни один слой Kafka не дотягивается до Postgres — добавление стоимости Kafka EOS не чинит кросс-системную запись. Чинит дедуп на стороне консьюмера в БД." },
  ]}
/>

<Quiz
  id="01-delivery-guarantees-choice-5"
  lessonSlug="01-delivery-guarantees"
  lang="ru"
  question="У SQS-консьюмера средняя обработка 30с; дефолтный visibility timeout очереди 30с. Инженеры сообщают о спорадической одновременной двойной обработке. Первый фикс и что он даёт?"
  choices={[
    { label: "Поднять дефолт очереди до ~6x средней обработки (около 180с) или использовать heartbeat ChangeMessageVisibility каждые timeout/3 для переменной работы. Таймаут, равный времени обработки, означает, что сообщение снова видно другим консьюмерам, пока первый ещё законно работает — это и есть окно одновременных дубликатов.", correct: true },
    { label: "Снизить maxReceiveCount до 1, чтобы дубликаты сразу шли в DLQ.", misconception: "maxReceiveCount=1 отправляет каждый transient-сбой в DLQ на первом промахе; он ничего не делает с окном слишком короткого таймаута и превращает обычные ретраи в расследования poison-pill." },
    { label: "Переключить SQS в режим exactly-once delivery.", misconception: "Стандартные очереди SQS — at-least-once, и нет переключателя 'exactly-once delivery', побеждающего Two Generals. Фикс корректности — размер таймаута плюс идемпотентный консьюмер." },
    { label: "Ставить таймаут на каждой доставке через ChangeMessageVisibility и полагаться, что он переживёт передоставки.", misconception: "Расширенное значение привязано к этому receipt handle; при передоставке таймаут сбрасывается к дефолту очереди. Надо поднимать сам дефолт очереди или заново слать heartbeat на каждой доставке." },
  ]}
/>

<Quiz
  id="01-delivery-guarantees-choice-6"
  lessonSlug="01-delivery-guarantees"
  lang="ru"
  question="Сервис обновляет Postgres и публикует событие в Kafka двумя отдельными шагами. Под нагрузкой downstream-консьюмеры иногда вообще не видят событие, чья строка в БД явно закоммичена. Какой паттерн чинит первопричину?"
  choices={[
    { label: "Паттерн outbox: INSERT события в таблицу outbox в ТОЙ ЖЕ транзакции, что и бизнес-обновление, затем отдельный sender публикует pending-строки. Это проблема dual-write — коммит БД и публикация независимы, поэтому сбой публикации тихо теряет событие. Outbox делает событие durable тогда и только тогда, когда обновление закоммичено.", correct: true },
    { label: "Добавить ретраи продюсера с enable.idempotence=true, и разрыв закроется.", misconception: "Idempotent producer предотвращает дубль публикаций; он не может опубликовать событие, которое никогда не было отправлено, потому что процесс умер после коммита БД. Потеря — это разрыв dual-write, который закрывает только транзакционный outbox (или CDC)." },
    { label: "Обернуть публикацию в Kafka и коммит Postgres в распределённый two-phase commit.", misconception: "XA/2PC между Postgres и Kafka хрупок, медленный и редко поддерживается на практике. Outbox достигает той же атомарности, используя только локальную транзакцию БД плюс асинхронный sender." },
    { label: "Увеличить visibility timeout SQS, чтобы у события было время опубликоваться.", misconception: "Это dual-write на стороне продюсера Kafka, а не проблема тайминга консьюмера SQS. Visibility timeout не имеет отношения к пропавшей публикации на стороне продюсера." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна цепочка: exactly-once delivery невозможна (Two Generals), поэтому работаешь по at-least-once и переносишь корректность на консьюмера. Дубликаты концентрируются на плече 3; самая дешёвая защита — INSERT-first транзакционный дедуп, расширенный Idempotency-Key в стиле Stripe для внешних вызовов. Idempotent producer Kafka (~3%) и транзакции (~20–30%) укрепляют только путь внутри Kafka — кросс-системным записям всё равно нужен дедуп консьюмера. SQS visibility timeout должен быть ~6x обработки (или с heartbeat), а паттерн outbox закрывает разрыв dual-write на стороне продюсера. Гарантии брокера необходимы, но никогда не достаточны.
</Recap>
