---
concepts:
- total-order
- per-partition-fifo
- partition-key
- producer-reorder
- repartition-hazard
- idempotency
estMin: 12
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://kafka.apache.org/41/configuration/producer-configs/
- https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/FIFO-queues-understanding-logic.html
- https://cwiki.apache.org/confluence/display/KAFKA/KIP-185:+Make+exactly+once+in+order+delivery+per+partition+the+default+producer+setting
status: ready
summary: "Тест с выбором на синтез по всему юниту — цена total order по пропускной способности, per-partition FIFO, выбор ordering key, переупорядочивание на продюсере и где порядок тихо ломается в проде."
title: 'Порядок сообщений: тест с выбором ответа'
track: queues
unit: 04-ordering
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает решение, которое ты принимаешь в реальном инциденте — не определение для заучивания, а компромисс, который надо взвесить, когда порядок и пропускная способность тянут в разные стороны.
</Hook>

<Goal lang="ru">
Убедись, что связываешь цену total order, per-partition FIFO, выбор ordering key, гарантии продюсера и production-сбои — тот синтез, к которому вёл урок.
</Goal>

<Quiz
  id="04-ordering-choice-1"
  lessonSlug="04-ordering"
  lang="ru"
  question="Почему глобальный total order по всем сообщениям — это фундаментально налог на пропускную способность, а не флаг конфига, который можно просто включить?"
  choices={[
    { label: "Единая глобальная последовательность требует единой точки сериализации — одна partition, которую по одному сообщению вычитывает один consumer. Как только два consumer'а работают параллельно, единого таймлайна больше нет: total order и параллелизм физически противоположны — нужный порядок это очередь из одного.", correct: true },
    { label: "Total order бесплатен; он лишь требует больше памяти, чтобы буферизовать сообщения по порядку.", misconception: "Это не стоимость буферизации — это стоимость конкуренции. Упорядочить всё значит сериализовать всё, что упирает тебя в пропускную способность одного consumer'а, сколько памяти ни добавь." },
    { label: "Total order'у просто нужен брокер побыстрее; пропускная способность масштабируется с железом.", misconception: "Более быстрое железо ускоряет единую точку сериализации, но не распараллеливает её. Потолок структурный — одна partition, один consumer — а не упор в железо." },
    { label: "Total order и partial order имеют одинаковую пропускную способность; разница только в надёжности.", misconception: "Они различаются именно пропускной способностью. Partial order параллелит по ключам; total order сериализует всё через одну точку. Надёжность — ортогональный вопрос." },
  ]}
/>

<Quiz
  id="04-ordering-choice-2"
  lessonSlug="04-ordering"
  lang="ru"
  question={"Сервис кошелька шлёт credit, затем debit на аккаунт. В staging с одним consumer'ом работало; в проде с восемью consumer'ами debit иногда приходит первым. Что на самом деле изменилось?"}
  choices={[
    { label: "Единственный consumer в staging случайно сериализовал всё. С восемью consumer'ами события на разных partition обрабатываются параллельно, и единого глобального порядка нет — очередь никогда не обещала порядок между consumer'ами, только внутри partition.", correct: true },
    { label: "Восемь consumer'ов каждый читают весь топик, поэтому каждое сообщение обрабатывается восемь раз в случайном порядке.", misconception: "В consumer group каждая partition назначается ровно одному consumer'у; не все читают все сообщения. Проблема — параллелизм по partition, а не дублирующее чтение." },
    { label: "Kafka тихо потеряла событие credit во время масштабирования.", misconception: "Ничего не потеряно. Оба события доставлены; они обработались не по порядку, потому что попали на partition, которые обслуживают разные consumer'ы параллельно." },
    { label: "Добавление consumer'ов увеличило сетевую задержку, что перемешало порядок прихода.", misconception: "Переупорядочивание структурное — параллельная обработка независимых partition — а не артефакт задержки. Корневая причина в снятии случайной однопоточности." },
  ]}
/>

<Quiz
  id="04-ordering-choice-3"
  lessonSlug="04-ordering"
  lang="ru"
  question="Нужен порядок по аккаунту при высокой пропускной способности в Kafka. Какой выбор ordering key верный и почему?"
  choices={[
    { label: "Использовать accountId как partition key. Kafka хеширует его, поэтому все события одного аккаунта попадают на одну partition по порядку, а разные аккаунты разносятся по partition и consumer'ам — порядок там, где он важен, параллелизм везде остальное.", correct: true },
    { label: "Использовать случайный ключ на сообщение, чтобы нагрузка равномерно растеклась по partition.", misconception: "Случайный ключ максимизирует параллелизм, но рушит порядок по аккаунту — события одного аккаунта разлетаются по partition и гонятся. Равномерная нагрузка бесполезна, если она ломает нужный порядок." },
    { label: "Использовать единый фиксированный ключ, чтобы всё было totally ordered и безопасно.", misconception: "Один ключ направляет каждое сообщение на одну partition: однопоточный конвейер. Это гарантирует total order, но выбрасывает весь параллелизм ради решения задачи на уровне аккаунта." },
    { label: "Использовать timestamp как ключ, чтобы сообщения сортировались хронологически.", misconception: "Хеширование timestamp разбрасывает события аккаунта по partition по времени, а не по сущности. Партиционирование — по хешу ключа, не по сортировке; ключ должен быть границей консистентности." },
  ]}
/>

<Quiz
  id="04-ordering-choice-4"
  lessonSlug="04-ordering"
  lang="ru"
  question="Какая конфигурация продюсера Kafka может тихо переупорядочить два сообщения на одной partition?"
  choices={[
    { label: "enable.idempotence=false и max.in.flight.requests.per.connection больше 1 — упавший батч может быть переотправлен и приземлиться после более позднего батча, который уже успешно записан, поменяв два сообщения местами на partition.", correct: true },
    { label: "enable.idempotence=true и до 5 in-flight запросов — брокер отвергает батчи не по порядку.", misconception: "Это безопасный дефолт со времён KIP-185: брокер отслеживает per-producer, per-partition sequence number (offset продюсера) и отвергает батчи не по порядку, сохраняя порядок даже при 5 in-flight запросах." },
    { label: "acks=all с одним in-flight запросом.", misconception: "Один in-flight запрос означает, что более поздний батч не может обогнать переотправляемый, поэтому порядок держится независимо от idempotence." },
    { label: "Любая конфигурация продюсера — Kafka вообще не гарантирует порядок внутри partition.", misconception: "Порядок внутри partition — базовая гарантия Kafka. Он ломается только в конкретном случае неидемпотентного продюсера с несколькими in-flight." },
  ]}
/>

<Quiz
  id="04-ordering-choice-5"
  lessonSlug="04-ordering"
  lang="ru"
  question="Команда растит топик Kafka с 6 до 12 partition ради пропускной способности и сразу видит порчу порядка по ключу. Почему и как это штатно избегают?"
  choices={[
    { label: "Kafka отображает ключ на partition хешированием по модулю числа partition, поэтому смена числа переотображает ключи. На время окна in-flight события дренируются со старой partition, а новые сыплются в новую — две partition, два consumer'а, один ключ. Команды заранее переавансируют число partition, чтобы не репартиционировать.", correct: true },
    { label: "Добавление partition удаляет старые, теряя in-flight сообщения.", misconception: "Существующие partition не удаляются; их данные остаются. Порча в том, что ключ теперь хешируется на другую partition, чем его уже стоящие в очереди события, поэтому две partition ненадолго держат один ключ." },
    { label: "Больше partition всегда снижают гарантии порядка навсегда.", misconception: "Порядок внутри partition держится после того, как пыль осядет; порча — переходное окно во время переотображения. Устойчивый фикс — избегать частого репартиционирования, а не вечно держать мало partition." },
    { label: "Consumer'ы не справляются с 12 partition, поэтому перемешивают сообщения.", misconception: "Consumer'ы отлично справляются с многими partition. Переупорядочивание идёт от переотображения ключ-в-partition при ресайзе, а не от ёмкости consumer'а." },
  ]}
/>

<Quiz
  id="04-ordering-choice-6"
  lessonSlug="04-ordering"
  lang="ru"
  question="Сервис профилей потребляет user.updated на at-least-once очереди с конкурентными consumer'ами; обновления одного пользователя иногда приходят не по порядку или дважды. Какой самый сильный дизайн?"
  choices={[
    { label: "Партиционировать по userId ради per-key FIFO И штамповать каждое событие монотонной версией, отбрасывая любую версию меньше или равную последней применённой. Партиционирование закрывает частый случай; version-guard ловит redelivery и поздний DLQ replay, которые partial order не закрывает.", correct: true },
    { label: "Сделать обработчик идемпотентным по message id и считать, что порядок теперь в норме.", misconception: "Идемпотентность не даёт применить одно и то же сообщение дважды, но не упорядочивает два разных сообщения. Два разных обновления одного пользователя всё ещё могут примениться не по порядку; нужна ещё проверка версии." },
    { label: "Переключить очередь на exactly-once доставку и положиться на неё ради порядка.", misconception: "Exactly-once решает дубликаты, а не межсообщенческий порядок между конкурентными consumer'ами. Трактовка её как гарантии порядка — ровно то непонимание, которое отгружает баг." },
    { label: "Форсировать один consumer и одну partition, чтобы всё было totally ordered.", misconception: "Это гарантирует порядок, сериализуя несвязанных пользователей друг против друга — упёртая пропускная способность ради задачи на уровне пользователя. Верно только для подлинно глобального леджера, которым сервис профилей не является." },
  ]}
/>

<Recap lang="ru">
Сквозная линия — одно дерево решений: total order требует единой точки сериализации и упирает пропускную способность, поэтому почти всегда нужен partial order — per-key FIFO. Выбирай partition key как сущность, которая должна оставаться консистентной (accountId, userId). Дальше порядок ломается в трёх предсказуемых местах: неидемпотентный продюсер с несколькими in-flight, репартиционирование, переотображающее ключи, и at-least-once redelivery или DLQ replay. Устойчивый дизайн партиционирует по ключу и version-guard'ит остаточный беспорядок.
</Recap>
