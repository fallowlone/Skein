---
concepts:
- total-order
- per-partition-fifo
- partition-key
- at-least-once
- version-guard
- repartition-hazard
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://kafka.apache.org/41/configuration/producer-configs/
- https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/FIFO-queues-understanding-logic.html
- https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/using-messagegroupid-property.html
status: ready
summary: "Промпты на свободное воспроизведение по всему юниту. Ответь своими словами сначала, потом раскрой модельный ответ и сравни."
title: 'Порядок сообщений: тест на свободное воспроизведение'
track: queues
unit: 04-ordering
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. Для каждого промпта скажи или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет материал.
</Hook>

<Goal lang="ru">
Восстанови ключевые механизмы юнита — total order против partial order, per-partition FIFO, выбор ordering key, опасность переупорядочивания на продюсере и паттерны выживаемости — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:visible
  id="04-ordering-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему total order по всем сообщениям — налог на пропускную способность, а не бесплатная опция конфига?", a: "Единая глобальная последовательность может появиться лишь из единой точки сериализации: одна partition, пишется под одним порядком, вычитывается одним consumer'ом по одному сообщению. В тот миг, когда два consumer'а работают параллельно, единого таймлайна нет — сообщение 5 и 6 гонятся, и побеждает тот обработчик, что закончил первым. Значит total order и параллелизм физически противоположны: запрошенный порядок — буквально очередь из одного. Конкретно, одна message group SQS FIFO упирается примерно в 300 сообщений/с (3000 с батчингом по 10), потому что следующее сообщение не выдаётся, пока предыдущее не удалено; одна partition Kafka ограничена пропускной способностью этой partition и одним consumer'ом в группе. Ты покупаешь total order своей масштабируемостью, поэтому почти никогда он реально не нужен." },
    { q: "Что такое partial order и как его выражают Kafka и SQS FIFO?", a: "Partial order значит, что сообщения с одним ключом FIFO друг относительно друга, а разные ключи свободно параллелятся — порядок внутри сущности, а не по всему. Кошельку всё равно, упорядочен ли credit аккаунта A против debit аккаунта B; ему критично, чтобы события самого A шли по порядку. Kafka выражает это через partition key: выбираешь accountId, Kafka хеширует его в partition, все события аккаунта приземляются туда по порядку, а разные ключи разносятся по partition и consumer'ам на полной per-partition пропускной способности. SQS FIFO выражает это через MessageGroupId: порядок строго в пределах группы, никогда между группами, поэтому совокупная пропускная способность растёт с числом групп (десятки тысяч сообщений/с) только за счёт растекания по множеству групп и батчинга, а не ускорения одной группы." },
    { q: "Как выбирать ordering key и что идёт не так с типичными плохими выборами?", a: "Бери ключ как границу консистентности — сущность, чьи события должны оставаться упорядоченными друг относительно друга: accountId, id агрегата, userId. Это даёт строгий порядок ровно там, где он важен, и параллелизм везде остальное. Случайный ключ на сообщение максимизирует растекание, но разбрасывает события одной сущности по partition, так что они гонятся и нужный порядок исчезает. Единый фиксированный ключ схлопывает всё на одну partition — total order и однопоточный конвейер, снова налог на пропускную способность. Ключ-timestamp сортирует по времени, а не по сущности, что всё равно разбрасывает события сущности. Ключ — про то, какие сообщения должны сериализоваться вместе, а не про равную нагрузку или хронологическую сортировку." },
    { q: "Как продюсер Kafka может переупорядочить два сообщения на одной partition и как это починили?", a: "Если enable.idempotence=false и max.in.flight.requests.per.connection больше 1, продюсер может держать несколько батчей в полёте одновременно. Если ранний батч падает и переотправляется, он может приземлиться после более позднего батча, который уже успешно записан — тихо поменяв два сообщения местами на одной partition, у источника, до того как любой consumer их увидит. KIP-185 сделал идемпотентный продюсер дефолтом, чтобы это закрыть: брокер отслеживает per-producer, per-partition sequence number (offset продюсера), отбрасывает дубликаты и отвергает батчи не по порядку, поэтому порядок сохраняется даже при до 5 in-flight запросах. Урок в том, что порядок внутри partition может ломаться выше consumer'а, поэтому проверяешь конфиг продюсера, а не только партиционирование." },
    { q: "Почему корректно партиционированный per-key FIFO поток всё равно может показать события не по порядку и что от этого защищает?", a: "Потому что at-least-once доставка и упорядоченная доставка — независимые свойства, а большинство очередей дают at-least-once. Сообщение может быть доставлено дважды, и redelivery может прийти после более новых сообщений, поэтому даже идеально партиционированный поток может показать [5, 6, 5] после redelivery 5. Retry и DLQ replay переинсертят сообщения вне очереди: обработчик падает на 5, 5 уходит в dead-letter queue, 6/7/8 коммитятся, потом 5 переигрывается и приземляется после своих преемников. Лестница защиты: партиционируй по сущности, которой нужен порядок, делай обработчики идемпотентными по message id, чтобы redelivery был no-op, предпочитай коммутативные или last-write-wins эффекты, чтобы остаточный беспорядок был безвреден, и штампуй per-entity монотонную версию, чтобы consumer отбрасывал любое обновление старше последнего применённого." },
    { q: "Почему добавление partition в топик Kafka рискует испортить порядок по ключу и как этого избегают?", a: "Kafka решает partition сообщения хешированием ключа по модулю числа partition, и этот модуль — ловушка: меняешь число, и тот же ключ отображается на другую partition. Во время и после репартиционирования есть окно, где in-flight события ключа ещё дренируются со старой partition, а новые события этого ключа производятся в новую — две partition держат события ключа, два consumer'а обрабатывают их параллельно, и per-key FIFO для этого окна исчезает. Это классический источник порчи порядка в проде. Штатное избегание — заранее переавансировать число partition, чтобы редко приходилось их растить, потому что добавление partition позже — операция, ломающая порядок, а не прозрачная ручка масштабирования. Если репартиционировать всё же надо, ты дренируешь или принимаешь временно ослабленный порядок и опираешься на идемпотентных, version-guard'нутых consumer'ов, чтобы поглотить беспорядок." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: total order требует одной точки сериализации и упирает пропускную способность, поэтому тянешься к partial order — per-key FIFO через partition key Kafka или MessageGroupId SQS FIFO. Выбирай ключ как границу консистентности, проверь, что продюсер идемпотентен и не может переупорядочить у источника, и помни, что at-least-once redelivery, DLQ replay и репартиционирование ломают порядок даже при корректном партиционировании. Устойчивый consumer идемпотентен, предпочитает коммутативные эффекты и version-guard'ит устаревшие обновления. Теперь, когда столкнёшься с порчей порядка на инциденте, — знаешь, куда смотреть в первую очередь: partition key, потом конфиг продюсера, потом проверка версии в consumer'е.
</Recap>
