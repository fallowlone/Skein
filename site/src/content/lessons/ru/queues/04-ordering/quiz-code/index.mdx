---
concepts:
- partition-key
- producer-reorder
- idempotency
- version-guard
- per-partition-fifo
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://kafka.apache.org/41/configuration/producer-configs/
- https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/using-messagegroupid-property.html
status: ready
summary: "Читай реальные сниппеты продюсера и consumer'а, предскажи поведение порядка и выбери фикс с наибольшим рычагом, который senior сделает первым."
title: 'Порядок сообщений: чтение кода и конфига'
track: queues
unit: 04-ordering
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги порядка живут в конфиге продюсера и обработчике consumer'а, а не в брокере. Читай код, предсказывай, где порядок ломается, и выбирай фикс, к которому senior потянется первым.
</Hook>

<Goal lang="ru">
Отработай цикл, который запускаешь на каждом инциденте порядка: читай partition key и конфиг продюсера, найди, где два сообщения могут погнаться или поменяться местами, и тянись к структурному фиксу — выбор ключа, идемпотентный продюсер, version guard — прежде чем винить брокер.
</Goal>

## Сниппет 1 — partition key

```java
// События одного аккаунта должны оставаться упорядоченными друг относительно друга.
ProducerRecord<String, Event> record =
    new ProducerRecord<>("account-events", UUID.randomUUID().toString(), event);
producer.send(record);
```

<Quiz
  id="04-ordering-code-1"
  lessonSlug="04-ordering"
  lang="ru"
  question="Требование — порядок по аккаунту. Что на самом деле делает этот выбор ключа и каков фикс?"
  choices={[
    { label: "Свежий случайный UUID на запись разбрасывает события одного аккаунта по partition, так что они попадают на разные partition и гонятся — порядок по аккаунту разрушен. Фикс: использовать event.getAccountId() как ключ, чтобы все события аккаунта хешировались на одну partition по порядку.", correct: true },
    { label: "Всё нормально — Kafka сохраняет порядок независимо от ключа.", misconception: "Kafka сохраняет порядок только внутри partition. Случайный ключ растаскивает события аккаунта по partition, поэтому нет partition, держащей их по порядку. Ключ должен быть границей консистентности." },
    { label: "Баг в имени топика; порядку нужен отдельный топик на аккаунт.", misconception: "Топик на аккаунт не масштабируется и не нужен. Один топик, партиционированный по accountId, даёт порядок по аккаунту и параллелизм между аккаунтами. Дефект — случайный ключ." },
    { label: "Поставь acks=all, и случайный ключ станет упорядоченным.", misconception: "acks управляет надёжностью, а не тем, на какую partition попадёт ключ. Случайный ключ всё равно растаскивает события аккаунта; порядок чинит только ключевание по accountId." },
  ]}
/>

## Сниппет 2 — конфиг продюсера

```properties
enable.idempotence=false
max.in.flight.requests.per.connection=5
retries=10
acks=all
```

<Quiz
  id="04-ordering-code-2"
  lessonSlug="04-ordering"
  lang="ru"
  question="Ключ верный, и всё на одной partition. Почему два сообщения всё равно могут поменяться местами и каков фикс в одну строку?"
  choices={[
    { label: "При выключенной idempotence и 5 in-flight запросах упавший батч, переотправленный вне очереди, может приземлиться после более позднего батча, который уже успешно записан — переупорядочив два сообщения на одной partition у источника. Фикс: enable.idempotence=true, тогда брокер отвергает батчи не по порядку даже при 5 in-flight.", correct: true },
    { label: "retries=10 — это баг; поставь retries=0, чтобы ничего никогда не переотправлялось не по порядку.", misconception: "retries=0 меняет переупорядочивание на потерю сообщений при временных сбоях — хуже. Реальный фикс — идемпотентный продюсер, сохраняющий порядок через retry за счёт per-partition sequence number." },
    { label: "acks=all форсирует переупорядочивание; снизь до acks=1.", misconception: "acks=all — надёжная, верная настройка и не вызывает переупорядочивания. Переупорядочивание идёт от неидемпотентных retry с несколькими in-flight." },
    { label: "На одной partition ничто не может переупорядочиться независимо от конфига.", misconception: "Одна partition гарантирует порядок, только если продюсер не может дать переотправленному батчу обогнать более поздний — а именно это и позволяют неидемпотентные multi-in-flight retry." },
  ]}
/>

## Сниппет 3 — обработчик consumer'а

```python
def handle(msg):
    user = db.get(msg.user_id)
    user.name = msg.new_name      # last write wins на том, что пришло последним
    db.save(user)
```

<Quiz
  id="04-ordering-code-3"
  lessonSlug="04-ordering"
  lang="ru"
  question="Consumer'ы конкурентны на at-least-once очереди. Два обновления одного пользователя могут прийти не по порядку или дважды. Каков самый сильный фикс?"
  choices={[
    { label: "Добавить per-user монотонную версию к каждому событию; хранить последнюю применённую версию и пропускать любой msg, чья версия меньше или равна ей. Вместе с партиционированием по user_id это отбрасывает устаревшие и переигранные обновления вместо перезаписи хороших данных.", correct: true },
    { label: "Обернуть read-modify-write в транзакцию БД.", misconception: "Транзакция делает одно обновление атомарным, но не упорядочивает два независимых сообщения. Более старое обновление всё равно может закоммититься после более нового и перезаписать его; нужен version guard, а не только атомарность." },
    { label: "Дедуплицировать по message id и считать, что порядок тогда верный.", misconception: "Дедуп не даёт применить одно сообщение дважды, но два разных обновления одного пользователя всё равно могут примениться не по порядку. Нужны дедуп плюс проверка версии." },
    { label: "Обрабатывать всех пользователей одним consumer'ом, чтобы ничего не было конкурентным.", misconception: "Это сериализует несвязанных пользователей ради задачи на уровне пользователя, схлопывая пропускную способность. Партиционируй по user_id и version-guard'ь." },
  ]}
/>

## Сниппет 4 — DLQ replay

```python
# Ночная задача: дренировать dead-letter queue обратно в основной топик
for failed in dead_letter_queue.drain_all():
    main_topic.produce(failed.key, failed.value)   # на полной скорости, без троттлинга
```

<Quiz
  id="04-ordering-code-4"
  lessonSlug="04-ordering"
  lang="ru"
  question="Consumer идемпотентен и version-guard'нут. Что всё равно делает этот replay с живым порядком и как запускать его безопасно?"
  choices={[
    { label: "Replay на полной скорости переинсертит старые сообщения позади всего, что произведено с момента их падения, переупорядочивая целый батч против живого трафика. Version guard делает это безопасным для применения, но replay всё равно делают с rate-limit, чтобы всплеск не уморил и не захлестнул живых consumer'ов.", correct: true },
    { label: "Replay всегда безопасен на полной скорости, потому что сообщения сохраняют свои исходные ключи.", misconception: "Тот же ключ сохраняет, на какую partition они попадут, но они всё равно приземляются после более новых событий — переупорядочивание. Безопасно применять только из-за version guard, и replay всё равно троттлят ради живой пропускной способности." },
    { label: "Replay портит порядок навсегда; никогда не переигрывай DLQ.", misconception: "DLQ replay — нормальный инструмент восстановления. С идемпотентными, version-guard'нутыми consumer'ами беспорядок безвреден при применении; фикс — rate-limit на replay, а не запрет." },
    { label: "Producing без ключа сделал бы replay упорядоченным.", misconception: "Сброс ключа разбрасывает переигранные сообщения по partition, делая хуже. Сохраняй ключ, опирайся на version guard и троттли replay." },
  ]}
/>

<Recap lang="ru">
Порядок читается в продюсере и consumer'е, а не в брокере: случайный partition key разбрасывает события сущности и должен быть границей консистентности; неидемпотентный продюсер с несколькими in-flight переупорядочивает у источника, чинится через enable.idempotence=true; конкурентным consumer'ам на at-least-once очереди нужен per-entity version guard, а не только транзакция или дедуп; DLQ replay переупорядочивает против живого трафика, безопасен при version guard, но только с rate-limit. Сначала чини структуру, потом проверяй, что порядок держится под нагрузкой.
</Recap>
