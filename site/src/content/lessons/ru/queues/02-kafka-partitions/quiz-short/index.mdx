---
concepts:
- partition-key-hashing
- per-key-ordering
- consumer-group-assignment
- partition-count-one-way-door
- consumer-rebalancing
- hot-partition-skew
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.confluent.io/blog/how-choose-number-topics-partitions-kafka-cluster/
- https://www.confluent.io/blog/incremental-cooperative-rebalancing-in-kafka/
- https://cwiki.apache.org/confluence/display/KAFKA/KIP-345:+Introduce+static+membership+protocol+to+reduce+consumer+rebalances
status: ready
summary: "Промпты на свободное воспроизведение по всему юниту про partition в Kafka. Сначала отвечайте своими словами, затем открывайте образцовый ответ и сравнивайте."
title: 'Partition в Kafka: тест на свободное воспроизведение'
track: queues
unit: 02-kafka-partitions
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение по памяти эффективнее перечитывания. Для каждого промпта проговорите или запишите полный ответ из памяти, прежде чем открывать образцовый, — именно усилие припоминания закрепляет механизм.
</Hook>

<Goal lang="ru">
Восстановите ключевые механизмы юнита — partitioner, порядок по key, назначение в consumer group, одностороннюю дверь и rebalance — не заглядывая обратно в урок.
</Goal>

<RetrievalDrawer
  client:visible
  id="02-kafka-partitions-short-retrieval"
  lang="ru"
  questions={[
    { q: "Какие три задачи hash(key) % N в дефолтном partitioner решает одновременно и почему их смешение порождает баги дизайна?", a: "Одна формула служит трём целям сразу. Распределение: она раскидывает нагрузку по partition, broker и дискам. Порядок: поскольку один и тот же key всегда хешируется в один partition, все записи этого key лежат в одном упорядоченном логе — а Kafka гарантирует порядок только внутри partition, никогда на уровне топика. Co-location: один consumer, читающий этот partition, видит всю историю key по порядку. Ловушка в том, что нельзя настроить одно, не затронув остальное. Выбор key ради равномерного распределения может разбросать связанные события; выбор key ради порядка (скажем, customerId) может свалить кита на один partition. Так что выбор key — это на деле выбор того, что остаётся упорядоченным и co-located, а смена N ради перераспределения нагрузки тихо ломает порядок, который key вам покупал." },
    { q: "Почему порядок по key в Kafka бесплатен и какова точная область действия гарантии порядка?", a: "Kafka гарантирует порядок только внутри одного partition: записи дописываются в лог partition с монотонно растущими offset и читаются обратно ровно в этом порядке. Нет глобального порядка — ни между partition, ни между топиками, ни по wall-clock времени. Порядок по key бесплатен только потому, что дефолтный partitioner отправляет каждую запись с одинаковым key в один partition (hash(key) % N), поэтому события key ложатся в один лог в порядке отправки. Как только записи key могут попасть более чем на один partition — пустой key, который рассеивается, или смена количества partition, которая перехеширует key, — этот порядок исчезает, потому что между двумя независимыми partition нет определённого порядка." },
    { q: "Объясните, почему параллелизм consumer жёстко ограничен количеством partition.", a: "Consumer group делит partition топика между участниками так, что каждый partition в каждый момент принадлежит ровно одному consumer в группе — никогда двум. Это правило одного consumer на partition делает потребление безопасным и упорядоченным, но также означает, что количество partition — это максимальное число consumer, способных делать полезную работу. С 6 partition седьмой consumer в группе не получает назначения и простаивает. Поэтому нельзя масштабировать throughput consumer сверх количества partition, добавляя consumer; рост параллелизма требует больше partition, а это и есть односторонняя дверь. Вот почему количество partition — решение о ёмкости, принимаемое заранее, а не runtime-настройка." },
    { q: "Почему увеличение количества partition в топике — это односторонняя дверь и какова безопасная альтернатива?", a: "Partitioner — это hash(key) % N, функция от N. Увеличьте N с 6 до 12, и результат modulo меняется для большинства key, поэтому key, всегда уходивший в partition 2, теперь маршрутизируется в другой partition. Существующие записи никогда не перемещаются — они остаются там, где были записаны, — поэтому история key теперь расколота между старым и новым partition без определённого порядка между ними. Любой consumer, построивший состояние по key (текущий баланс, машина состояний, changelog Kafka Streams, keyed по partition), читает сломанную, неупорядоченную историю. И вернуть количество назад нельзя никогда, потому что два упорядоченных лога нельзя корректно слить. Безопасный путь — не менять размер на месте: создайте новый топик с большим количеством, делайте dual-write в старый и новый, выкачайте старый, затем переключите consumer — это миграция, а не правка конфига. А ещё лучше заложить запас partition заранее под ожидаемый рост." },
    { q: "Что такое stop-the-world rebalance, почему он болезненный и какие два механизма его уменьшают?", a: "Когда consumer присоединяется, уходит или считается мёртвым (пропущенный heartbeat, долгая пауза GC, медленный деплой), группа переназначает partition между участниками. По классическому eager-протоколу каждый consumer отзывает все свои partition, и потребление останавливается по всей группе, пока не вычислено и принято новое назначение, — пауза в секунды, хуже при rebalance-шторме, где участники постоянно мигают. Это болезненно, потому что превращает сбой одного consumer в простой для каждого partition. Incremental cooperative rebalancing (Kafka 2.4+) отзывает только те partition, что действительно должны переехать, поэтому большинство consumer продолжают работу сквозь rebalance. Static membership (KIP-345) даёт каждому consumer стабильный group.instance.id, поэтому быстрый перезапуск в пределах session timeout распознаётся как тот же участник и вообще не вызывает rebalance. Вместе они превращают rebalance из stop-the-world события в почти фоновую бухгалтерию." },
    { q: "Что такое hot-partition skew, почему больше partition и больше consumer его не исправляют и где настоящее исправление?", a: "Hot-partition skew — это когда один key доминирует в трафике (крупный клиент, виральный товар), поэтому все его записи хешируются в один partition. Единственный владеющий consumer этого partition упёрт в 100%, пока остальные простаивают. Больше partition не помогает, потому что горячий key всё равно хешируется ровно в один из них; больше consumer не помогает, потому что параллелизм consumer ограничен количеством partition, а у одного partition всё равно один владелец. Узкое место структурно: один key на одном partition. Исправление на уровне key, а не partition — добавить соль к key (приписать ограниченный суффикс, чтобы горячая сущность размазалась по нескольким partition, приняв, что строгий порядок по сущности ослабляется) или разбить горячую сущность на подключи. Вы меняете то, что видит partitioner, а не сколько partition существует." },
  ]}
/>

<Recap lang="ru">
Если вы смогли восстановить каждый ответ по памяти, у вас есть стержень юнита: одна формула partitioner делает распределение, порядок и co-location сразу; порядок гарантирован только внутри partition; параллелизм consumer жёстко ограничен количеством partition; рост этого количества перехеширует key и ломает порядок по key, поэтому это односторонняя дверь, под которую закладывают запас или которую обходят миграцией; а два продакшен-сбоя — rebalance и hot-partition skew — укрощаются cooperative rebalancing плюс static membership и исправлением key, а не количества. Теперь, когда коллега предложит поднять количество partition на месте, вы первым делом спросите: что произойдёт с hash(key) % N для каждого живого ключа?
</Recap>
