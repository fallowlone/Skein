---
concepts:
- partition-key-hashing
- partition-ordering
- consumer-group-assignment
- partition-count-one-way-door
- hot-partition-skew
- consumer-rebalancing
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://www.confluent.io/blog/how-choose-number-topics-partitions-kafka-cluster/
- https://www.confluent.io/blog/incremental-cooperative-rebalancing-in-kafka/
- https://cwiki.apache.org/confluence/display/KAFKA/KIP-345:+Introduce+static+membership+protocol+to+reduce+consumer+rebalances
- https://www.instaclustr.com/blog/apache-kafka-kraft-abandons-the-zookeeper-part-3-maximum-partitions-and-conclusions/
spiral: []
status: ready
summary: Topic разбит на N partitions; хеш ключа выбирает одну; порядок держится только
  внутри partition; один consumer на partition ограничивает параллелизм. Количество
  partitions — решение, которое не отыграть назад.
title: 'Kafka partitions: единица параллелизма, порядка и дверь в одну сторону'
track: queues
unit: 02-kafka-partitions
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";
import PacketDot from "~/components/diagram/PacketDot.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import metadataPlane from "./infographics/metadata-plane.svg?raw";

<Hook>
Topic order-events отставал, и кто-то поднял его с 6 partitions до 12, чтобы «добавить пропускную способность». Десять минут всё работало. Потом повалил саппорт: cancel для заказа обработался *раньше* create. Один `orderId`, два события, теперь летят в разные partitions, потому что `hash(orderId) % 12` больше не равен `hash(orderId) % 6`. Порядок по ключу — единственная гарантия, на которую опирался весь пайплайн — тихо испарился ровно в момент, когда изменилось количество partitions. Отката, который вернёт старые ключи на места, не существует.
</Hook>

<Crux>Topic — это N partitions; хеш ключа выбирает одну; порядок только внутри partition; один consumer на partition ограничивает параллелизм.</Crux>

<Explanation>

## Topic — это N независимых логов, и ключ выбирает лог

Почему неправильный ключ тихо уничтожает гарантию порядка, пока всё остальное выглядит здоровым? Потому что одна формула — `hash(key) % N` — делает сразу три разные работы, и большинство инженеров замечают только одну из них.

Topic в Kafka — это не один поток, а N partitions, каждая — независимый append-only лог со своими offsets. Когда producer шлёт запись с ключом, дефолтный partitioner вычисляет `murmur2(key) % N`, чтобы выбрать, в какую именно partition запись попадёт. Один ключ — одна partition, всегда — пока N не меняется. Записи без ключа размазываются по partitions (round-robin или sticky-батчинг), потому что им нечем закрепиться.

Этот единственный механизм делает сразу три работы, и их смешение — там, где дизайны ломаются:

- **Распределение** — раскладка нагрузки по brokers и дискам.
- **Порядок** — Kafka гарантирует порядок *только внутри одной partition*, никогда не по всему topic. Глобального порядка нет.
- **Co-location** — все записи одного ключа в одном месте, поэтому consumer видит всю историю этого ключа последовательно.

Значит «порядок» в Kafka на деле означает *порядок по ключу*, и достаётся он бесплатно лишь потому, что все записи ключа хешируются в одну partition. Выбор ключа — это и есть выбор того, что останется упорядоченным: ключ по `orderId` — события заказа выстроены; ключ по `customerId` — ты сериализуешь всё для одного клиента (и рискуешь, что один крупный клиент перегрузит partition — об этом ниже). Когда ты встречаешь баг порядка в унаследованном Kafka-пайплайне, первое, что нужно проверить, — не brokers и не consumer-код, а ключ, который использует producer.

## Consumer groups: одна partition — один consumer

На стороне чтения consumer group делит partitions между своими членами так, что каждая partition в любой момент принадлежит **ровно одному consumer в группе**. Это назначение — источник параллелизма Kafka и его самый жёсткий потолок. Если у topic 6 partitions, полезную работу делают максимум 6 consumers в группе. Запусти 7-й — он простаивает без назначения. Максимальный параллелизм потребления навсегда ограничен количеством partitions.

Поэтому количество partitions — решение по планированию ёмкости, а не ручка тюнинга. Ты выбираешь N заранее под параллелизм, который понадобится на пике, потому что поднять его потом — тот самый опасный ход, к которому этот урок всё время возвращается.

<FlowDiagram
  label="Ключи хешируются в partitions, partitions назначаются consumers"
  hue="var(--d-network)"
  nodes={[
    { id: "kA1", label: "orderId=A", sub: "событие 1", col: 0, row: 0 },
    { id: "kB", label: "orderId=B", sub: "событие 1", col: 0, row: 2 },
    { id: "kA2", label: "orderId=A", sub: "событие 2", col: 0, row: 4 },
    { id: "p0", label: "Partition 0", sub: "hash%3", col: 1, row: 1 },
    { id: "p1", label: "Partition 1", sub: "hash%3", col: 1, row: 2 },
    { id: "p2", label: "Partition 2", sub: "hash%3", col: 1, row: 3 },
    { id: "c1", label: "Consumer 1", sub: "владеет P0,P2", col: 2, row: 1 },
    { id: "c2", label: "Consumer 2", sub: "владеет P1", col: 2, row: 3 },
  ]}
  edges={[
    { from: "kA1", to: "p1", label: "A" },
    { from: "kA2", to: "p1", label: "A" },
    { from: "kB", to: "p0", label: "B" },
    { from: "p0", to: "c1" },
    { from: "p1", to: "c2" },
    { from: "p2", to: "c1" },
  ]}
  caption="Оба события orderId=A хешируются в одну partition (P1), поэтому история ключа остаётся упорядоченной. Каждая partition принадлежит ровно одному consumer в группе; при 3 partitions и 2 consumers один владеет двумя — а 3-й consumer простаивал бы, ведь параллелизм ограничен количеством partitions."
/>

<DiagramFrame label="Запись с фиксированным ключом летит от producer в свою одну partition; hash(key) по модулю N всегда выбирает ту же partition, поэтому один и тот же ключ каждый раз попадает в одно место" hue="var(--d-systems)" caption="Фиксированный ключ детерминирован: hash(key) % N выбирает одну partition, и producer маршрутизирует в неё каждую запись этого ключа — именно поэтому порядок по ключу держится, и поэтому смена N его перенаправляет.">
  <svg viewBox="0 0 360 120" width="360" height="120" style="display:block;margin:0 auto;max-width:100%;height:auto;">
    <rect x="6" y="46" width="82" height="28" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="47" y="64" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--ink)">Producer</text>
    <text x="47" y="38" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--muted)">key=A</text>
    <rect x="262" y="10" width="92" height="26" rx="6" fill="var(--card-2)" stroke="var(--hairline-2)" stroke-width="0.8" />
    <text x="308" y="27" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--muted)">Partition 0</text>
    <rect x="262" y="47" width="92" height="26" rx="6" fill="var(--card-2)" stroke="var(--d-systems)" stroke-width="1.2" />
    <text x="308" y="64" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--ink)">Partition 1</text>
    <text x="308" y="92" text-anchor="middle" font-size="9" font-family="var(--font-body)" fill="var(--muted)">hash(A) % 3</text>
    <rect x="262" y="84" width="92" height="26" rx="6" fill="var(--card-2)" stroke="var(--hairline-2)" stroke-width="0.8" />
    <text x="308" y="101" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--muted)">Partition 2</text>
    <path data-path d="M88 60 L262 60" fill="none" stroke="var(--hairline-2)" stroke-width="1.4" stroke-dasharray="4 4" />
    <PacketDot color="var(--d-systems)" durationMs={1400} size={9} />
  </svg>
</DiagramFrame>

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Если у тебя…</th>
        <th class="border border-border px-3 py-2 text-left">…то</th>
        <th class="border border-border px-3 py-2 text-left">Следствие</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">6 partitions, 4 consumers</td>
        <td class="border border-border px-3 py-2">Часть consumers держит по 2 partitions</td>
        <td class="border border-border px-3 py-2">Нормально — есть запас расти до 6</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">6 partitions, 6 consumers</td>
        <td class="border border-border px-3 py-2">Маппинг 1:1</td>
        <td class="border border-border px-3 py-2">Максимум параллелизма достигнут</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">6 partitions, 8 consumers</td>
        <td class="border border-border px-3 py-2">2 consumers не получают ничего</td>
        <td class="border border-border px-3 py-2">Простаивающая ёмкость — partitions ограничивают</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Горячий ключ на 1 partition</td>
        <td class="border border-border px-3 py-2">1 consumer перегружен, остальные простаивают</td>
        <td class="border border-border px-3 py-2">Перекос — добавление consumers не поможет</td>
      </tr>
    </tbody>
  </table>
</div>

Все эти строки говорят об одном: количество partitions — это ручка, которая определяет, сколько параллелизма вообще теоретически возможно. Добавишь consumers без добавления partitions — ничего не купишь. Проигноришь перекос — даже идеально сбалансированное назначение всё равно упрётся в один перегруженный ключ.

## Дверь в одну сторону: смена N перехеширует всё

Количество partitions у topic можно увеличить. Уменьшить нельзя, и увеличение редко обходится так дёшево, как кажется. Проблема в partitioner: `hash(key) % N` — функция от N. Поменяй N с 6 на 12, и результат по модулю меняется для большинства ключей, поэтому ключ, который всегда шёл в partition 2, теперь идёт в partition 8. Будущие записи ключа маршрутизируются в новую partition, а его история лежит в старой — два потока больше не упорядочены друг относительно друга, и любой consumer, построивший состояние по ключу (текущий баланс, позиция стейт-машины), теперь читает разорванную, неупорядоченную историю.

Для stateful-приложений Kafka Streams это хуже, чем икота: changelog/state store (журнал изменений/хранилище состояния) ключуется по partition, поэтому смена количества partitions фактически способна повредить локальное состояние. Когда видишь, что Kafka Streams выдаёт неверные результаты после «рутинного» бампа partitions — именно это расщепление истории нужно подозревать в первую очередь. Поэтому стандартный ход сеньора — *не* ресайзить на месте. Ты заранее закладываешь partitions с запасом под ожидаемый рост или — когда действительно надо масштабироваться — создаёшь новый topic с большим количеством и мигрируешь (dual-write в старый и новый, дренаж старого, перевод consumers). Это миграция, а не правка конфига.

<Inset kind="why" lang="ru">
Почему нельзя уменьшить partitions? Partition — это упорядоченный лог с зафиксированными offsets и, возможно, компактным состоянием. Слияние двух логов должно было бы вплести их записи в одну временную линию, но корректного вплетения нет — их offsets и порядки независимы. Kafka отказывает, вместо того чтобы тихо разрушить порядок, поэтому счётчик только растёт. Считай это необратимым.
</Inset>

## Горячие partitions, ребалансы и цена «слишком много»

Доминируют два прод-сценария отказа. Первый — **перекос (skew)**: если один ключ (кит-клиент, вирусный товар) забирает основной трафик, весь он хешируется в одну partition, поэтому один consumer упёрт в 100%, а остальные простаивают. Больше partitions и больше consumers не дают ничего — узкое место это один ключ на одной partition. Фикс на уровне ключа (солить ключ, разбить горячую сущность), а не на уровне partition.

Второй — **ребаланс**. Когда consumer входит, уходит или считается мёртвым (пропущенный heartbeat, долгая пауза GC, медленный деплой), группа переназначает partitions. Классический «eager»-протокол — **stop-the-world**: каждый consumer отзывает все свои partitions, и потребление встаёт по всей группе на время процесса — часто на секунды, иногда дольше в шторме ребалансов. Важны два смягчения: **incremental cooperative rebalancing** (Kafka 2.4+) переназначает только partitions, которые обязаны переехать, поэтому большинство consumers продолжают работать, и **static membership** (KIP-345, Kafka Improvement Proposal — официальный процесс изменений в Kafka) даёт каждому consumer стабильный `group.instance.id`, чтобы быстрый рестарт вообще не запускал ребаланс.

Цифры заземляют трейдофф. Одна partition тянет порядка десятков MB/s, поэтому пропускная способность растёт с количеством partitions — до предела. Перевали за ~1000 partitions на broker, и пропускная способность producer и p99 latency резко деградируют (собственные тесты Confluent показывают обвал throughput по мере роста от сотен к 10K partitions). При легаси-плоскости метаданных ZooKeeper отказ broker означал выборы лидеров, масштабирующиеся с количеством partitions, что загоняло controller failover в диапазон 5–7 секунд при тысячах partitions; KRaft (встроенный Raft-консенсус в Kafka, заменяющий ZooKeeper) срезал это до менее секунды и поднимает практический потолок в сотни тысяч partitions на cluster. Баланс сеньора: достаточно partitions для параллелизма и запаса, но не так много, чтобы failover latency и накладные расходы на partition стали доминировать.

<Infographic
  svg={metadataPlane}
  label="Двустороннее сравнение плоскости метаданных Kafka: у легаси ZooKeeper controller failover 5–7 секунд и практический потолок около 1000 partitions на broker, а у текущего KRaft failover менее секунды и потолок в сотни тысяч partitions на cluster."
  caption="Плоскость метаданных — а не brokers — реально ограничивает число partitions: KRaft превращает лимит в 5–7 с и ~1000 partitions в потолок менее секунды и сотни тысяч."
  hue="var(--d-infra)"
/>

<TradeoffMatrix
  id="02-kafka-partitions-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Topic order-events (ключ по orderId, 6 partitions) отстаёт на пике, и нужна большая пропускная способность consumers. Порядок по заказу должен держаться. Выбери ход."
  options={[
    { name: "Создать новый topic с бо́льшим числом partitions, dual-write, дренаж старого, затем перевод consumers", summary: "Мигрируй, не ресайзь.", correct: true, justification: "Увеличение partitions на месте меняет hash(key) % N, поэтому существующие orderId перехешируются в новые partitions, и порядок по заказу ломается — ровно тот сбой, с которого начинается урок. Миграция в свежий, больший topic сохраняет историю каждого ключа в одном месте во время перехода и позволяет переводить consumers осознанно. Это больше работы, но единственный путь, сохраняющий гарантию порядка целой." },
    { name: "Увеличить живой topic с 6 до 12 partitions, чтобы удвоить параллелизм consumers", summary: "Просто бамп N.", justification: "Это ловушка. Подъём счётчика перехеширует большинство ключей, поэтому create и cancel заказа могут попасть в разные partitions и обработаться не по порядку. Для stateful-consumers это способно повредить состояние по ключу. Throughput может улучшиться, пока порядок тихо ломается." },
    { name: "Добавить consumers в существующую группу сверх 6", summary: "Масштабировать читателей.", justification: "Здесь бесполезно: при 6 partitions consumers 7+ не получают назначения и простаивают. Параллелизм consumers жёстко ограничен количеством partitions, поэтому добавление читателей без добавления partitions не даёт ничего." },
    { name: "Проверить, не является ли один orderId / клиент горячим ключом, перекашивающим одну partition", summary: "Сначала проверь перекос.", justification: "Разумная диагностика — если лаг это одна горячая partition, никакое перепартиционирование не поможет, и фикс на уровне ключа. Но в условии спрашивают ход для безопасного роста throughput; если это настоящий объём, нужна именно миграция, а не ресайз на месте." },
  ]}
/>

<Quiz
  id="02-kafka-partitions-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="У topic 8 partitions. Consumer group с 12 consumers всё равно отстаёт. В чём настоящее ограничение?"
  choices={[
    { label: "Количество partitions ограничивает параллелизм — работу могут получить только 8 consumers; остальные 4 простаивают", correct: true },
    { label: "12 consumers дерутся за partitions, и им нужен лок", misconception: "Конкуренции нет — каждая partition принадлежит ровно одному consumer в группе. Лишним просто ничего не назначается." },
    { label: "Увеличить размер poll у consumers, и лаг рассосётся", misconception: "Тюнинг poll помогает занятому consumer, но не создаёт параллелизм, который запрещён количеством partitions; у 4 consumers всё ещё нет partitions." },
    { label: "Перейти на round-robin назначение, чтобы все 12 делили 8 partitions", misconception: "Никакой assignor не даёт двум consumers в одной группе совместно владеть partition; правило «один consumer на partition» фундаментально, а не выбор assignor." },
  ]}
/>

<Quiz
  id="02-kafka-partitions-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Почему подъём keyed-topic с 6 до 12 partitions рискует сломать порядок?"
  choices={[
    { label: "Partitioner — это hash(key) % N, поэтому смена N перенаправляет большинство ключей; новые записи ключа идут в другую partition, чем его история", correct: true },
    { label: "Kafka перетасовывает все существующие сообщения по новым partitions", misconception: "Существующие записи не перемещаются — они остаются там, где были записаны. Слом в том, что *будущие* записи ключа теперь хешируются в другое место, разрывая ключ по двум partitions." },
    { label: "Больше partitions значит больше brokers, а brokers обрабатывают не по порядку", misconception: "Brokers сохраняют порядок внутри каждой partition независимо от количества; проблема в смене маппинга ключ-на-partition, а не в поведении broker." },
    { label: "Не сломает — Kafka автоматически ребалансит порядок при добавлении partitions", misconception: "Автоматического переупорядочивания нет. Порядок по ключу между старой и новой partition просто теряется; поэтому количество partitions считают дверью в одну сторону." },
  ]}
/>

<DragOrder
  id="02-kafka-partitions-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь по порядку, что происходит, когда producer шлёт запись с ключом, а consumer group её читает:"
  items={[
    { id: "a", label: "Producer вычисляет hash(key) % N, чтобы выбрать одну partition" },
    { id: "b", label: "Запись добавляется в лог этой partition на следующий offset" },
    { id: "c", label: "Группа назначает эту partition ровно одному consumer" },
    { id: "d", label: "Этот consumer читает partition в порядке offset, видя историю ключа последовательно" },
    { id: "e", label: "Если consumer умирает, ребаланс переназначает его partitions другому члену" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Partitions — единица параллелизма и порядка в Kafka: ключ хешируется в одну partition, порядок только там, consumers — максимум один на partition. Подъём числа перехеширует ключи и ломает порядок — дверь в одну сторону.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-kafka-partitions-retrieval"
  lang="ru"
  questions={[
    { q: "Коллега хочет поднять живой keyed-topic с 6 до 24 partitions, чтобы починить лаг consumers. Объясни риск и более безопасный путь.", a: "Дефолтный partitioner маршрутизирует записи по hash(key) % N, поэтому partition для ключа зависит от количества partitions. Переход с 6 на 24 меняет модуль для большинства ключей: ключ, всегда попадавший в partition 2, теперь попадает куда-то ещё. Его старые записи остаются в partition 2, а новые идут в новую partition, поэтому два потока больше не упорядочены друг относительно друга — и любая гарантия по ключу, на которую опирался пайплайн (последовательность create-затем-cancel заказа, текущий агрегат, state store в Kafka Streams), тихо ломается в момент изменения. К тому же уменьшить количество назад нельзя никогда. Безопасный путь — миграция, а не ресайз на месте: создать новый topic с 24 partitions, dual-write в старый и новый, дать consumers дренировать старый topic, затем перевести consumer group на новый. Если же реальная проблема — один горячий ключ, прижимающий одну partition, перепартиционирование не поможет вообще — фикси на уровне ключа, соля или разбивая горячую сущность." },
    { q: "Что такое stop-the-world ребаланс, почему он больно бьёт и что его снижает?", a: "Когда consumer входит, уходит или считается мёртвым (пропущенный heartbeat, долгая пауза GC, медленный деплой), группа должна переназначить partitions между членами. При классическом eager-протоколе каждый consumer отзывает все свои partitions, и потребление встаёт по всей группе, пока новое назначение не вычислено и не принято — часто пауза в секунды, и хуже в шторме ребалансов, где consumers постоянно мигают. Больно потому, что это превращает сбой одного consumer в простой по всем partitions. Снижают это две вещи. Incremental cooperative rebalancing (Kafka 2.4+) отзывает только partitions, которым реально надо переехать, поэтому большинство consumers продолжают работать сквозь ребаланс. Static membership (KIP-345) даёт каждому consumer стабильный group.instance.id, поэтому быстрый рестарт в пределах session timeout распознаётся как тот же член и вообще не запускает ребаланс. Вместе они превращают ребаланс из stop-the-world события в нечто гораздо ближе к фоновой бухгалтерии." },
  ]}
/>

<Recap lang="ru">
Topic в Kafka — это N независимых partitions, и дефолтный partitioner отправляет каждую запись с ключом в hash(key) % N — поэтому ключ всегда попадает в одну и ту же partition, и поэтому порядок гарантирован только внутри partition и никогда по всему topic. На стороне чтения consumer group отдаёт каждую partition ровно одному consumer, поэтому количество partitions — жёсткий потолок параллелизма потребления: лишние consumers просто простаивают. Это делает количество решением по ёмкости, и почти необратимым — поднять можно, опустить нельзя, а подъём перехеширует большинство ключей, разрывая историю каждого ключа между старой и новой partition и тихо ломая порядок по ключу (и повреждая состояние Kafka Streams). Поэтому закладывают запас заранее или мигрируют в новый topic, а не ресайзят на месте. В проде боль проявляется как перекос горячих partitions (один ключ упирает один consumer, остальные простаивают — фикс на уровне ключа) и ребалансы (stop-the-world паузы в секунды, укрощаемые incremental cooperative rebalancing и static membership). Partition тянет десятки MB/s, но после ~1000 partitions на broker throughput и p99 деградируют, а на легаси ZooKeeper controller failover шёл 5–7 секунд при тысячах partitions — KRaft срезает это до менее секунды. Вся игра в том, чтобы выбрать N, дающее параллелизм и порядок, с которыми можно жить долго. Теперь, когда на keyed-топике растёт lag, ты будешь проверять количество partitions, количество consumers, распределение ключей и логи rebalance — именно в этом порядке — прежде чем трогать конфиг.
</Recap>
