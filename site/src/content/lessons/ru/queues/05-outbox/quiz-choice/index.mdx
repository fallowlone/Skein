---
concepts:
- dual-write-problem
- transactional-outbox
- polling-relay
- change-data-capture
- at-least-once-delivery
- idempotent-consumer
estMin: 12
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://microservices.io/patterns/data/transactional-outbox.html
- https://debezium.io/documentation/reference/stable/transformations/outbox-event-router.html
status: ready
summary: "Тест с выбором на синтез по всему юниту outbox — разрыв dual-write, фикс одной транзакцией, polling vs CDC relay, доставка at-least-once и операционный налог."
title: 'Outbox: тест с выбором ответа'
track: queues
unit: 05-outbox
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает решение, которое ты принимаешь в реальном инциденте — не определение для заучивания, а компромисс, который надо взвесить, когда запись и публикация обязаны произойти обе.
</Hook>

<Goal lang="ru">
Убедись, что связываешь разрыв dual-write, фикс одной транзакцией, выбор polling vs CDC relay, гарантию at-least-once и операционный налог — тот синтез, к которому вёл урок.
</Goal>

<Quiz
  id="05-outbox-choice-1"
  lessonSlug="05-outbox"
  lang="ru"
  question={"Хендлер делает INSERT заказа, затем kafka.publish('OrderPlaced'). Под нагрузкой pod убивается по OOM после коммита, но до возврата publish. Какой сбой получается и почему try/catch не спасёт?"}
  choices={[
    { label: "Потерянное событие: заказ долговечен, но OrderPlaced не дошёл до брокера. try/catch не поможет, потому что OOM-kill не бросает ничего, что можно поймать — процесс просто умирает в разрыве между двумя системами, у которых нет общей transaction.", correct: true },
    { label: "Фантомное событие — консьюмеры реагируют на заказ, который не закоммитился.", misconception: "Фантомные события возникают при публикации ДО коммита с последующим откатом. Здесь заказ закоммитился первым, поэтому опасность обратная: реальный заказ, чьё событие потеряно." },
    { label: "Ничего — ретраи продюсера Kafka в итоге доставят сообщение после рестарта.", misconception: "Вызов publish не завершился, и его in-memory состояние умерло вместе с pod. Нет долговечной записи, что публикация была должна, поэтому ничего не ретраится. Именно эту долговечную запись добавляет outbox." },
    { label: "INSERT заказа автоматически откатывается, когда publish падает.", misconception: "Transaction заказа закоммичена и независима от вызова брокера; более поздний сбой в отдельной системе не может откоммиченное отменить. У двух хранилищ нет общей transaction — это и есть корневая проблема." },
  ]}
/>

<Quiz
  id="05-outbox-choice-2"
  lessonSlug="05-outbox"
  lang="ru"
  question={"Почему запись бизнес-строки и outbox-строки в одной локальной transaction даёт атомарность «бесплатно», а 2PC между Postgres и Kafka — нет?"}
  choices={[
    { label: "Обе записи нацелены на одну базу, поэтому одна ACID transaction коммитит или откатывает обе без координатора. 2PC охватывает два хранилища и требует блокирующего координатора — а Kafka к тому же не практичный XA-участник.", correct: true },
    { label: "Поскольку outbox-таблица append-only, она не может конфликтовать с бизнес-записью.", misconception: "Атомарность даёт не append-only. Она следует из того, что оба INSERT делят одну локальную transaction против одной базы; outbox был бы так же атомарен, даже если бы допускал обновления." },
    { label: "Поздняя публикация relay входит в ту же transaction, расширяя атомарность на Kafka.", misconception: "Relay работает намного позже в отдельном процессе; он намеренно НЕ в transaction. Атомарность заканчивается на двух локальных INSERT — публикация это отдельный, ретраибельный шаг." },
    { label: "2PC прекрасно сработал бы; outbox просто проще кодить.", misconception: "2PC между БД и Kafka не просто сложнее кодить — блокирующий координатор 2PC становится худшей проблемой доступности, чем dual-write, а Kafka не даёт XA-участия, которое хотелось бы в production." },
  ]}
/>

<Quiz
  id="05-outbox-choice-3"
  lessonSlug="05-outbox"
  lang="ru"
  question="Команда гоняет polling relay с интервалом 1с и хочет, чтобы события прилетали быстрее. Они думают снизить до 25мс. Как это читает senior?"
  choices={[
    { label: "Ужимание интервала умножает нагрузку запросами на первичную OLTP-базу — каждый poll выполняется, есть работа или нет, а при многих репликах relay это становится постоянной нагрузкой. Если реально нужна задержка в единицы мс — переходи на CDC (tailing WAL), а не полли чаще.", correct: true },
    { label: "Poll в 25мс бесплатен — запрос по индексу, поэтому частота не важна.", misconception: "Индексный запрос всё равно стоит round trip, планирования и захвата блокировки на первичной каждый тик. Сорок poll в секунду по репликам — реальная, постоянная нагрузка, даже когда таблица пуста." },
    { label: "Polling никогда не побьёт ~1с задержки, так что идея невозможна.", misconception: "Задержка polling ограничена интервалом, поэтому более тесный интервал её снижает — проблема в стоимости нагрузки, а не в невозможности. CDC — лучший инструмент, когда реально нужно меньше 100мс." },
    { label: "Переход на CDC поднимет задержку, потому что WAL медленнее прямого запроса.", misconception: "CDC tailing WAL и эмитит в момент коммита INSERT, давая задержку в единицы мс с почти нулевой нагрузкой на таблицу — меньше задержки, чем polling, а не больше." },
  ]}
/>

<Quiz
  id="05-outbox-choice-4"
  lessonSlug="05-outbox"
  lang="ru"
  question="Почему outbox даёт at-least-once, а не exactly-once, и что это навязывает каждому консьюмеру?"
  choices={[
    { label: "Relay публикует, затем помечает строку sent двумя неатомарными шагами; крах после приёма брокером, но до коммита UPDATE переигрывает событие на рестарте. Поэтому консьюмеры должны быть idempotent — дедупить по стабильному event id.", correct: true },
    { label: "Это exactly-once, пока у брокера включены idempotent-продюсеры.", misconception: "Idempotent-продюсеры дедупят ретраи в рамках одной сессии продюсера, а не через крах-и-рестарт relay, который перечитывает неотправленную строку. Гарантия outbox — at-least-once сквозная; дедуп — работа консьюмера." },
    { label: "Пометка строки sent ДО публикации сделала бы это exactly-once.", misconception: "Это лишь сдвигает разрыв и превращает восстановимый дубликат в невосстановимое потерянное событие — крах после UPDATE и до publish, и строка выглядит готовой, хотя ничего не отправлено. Publish-then-mark строго безопаснее." },
    { label: "Это at-least-once, но значит дубликаты приемлемы и обработки не требуют.", misconception: "At-least-once терпит дубликаты только потому, что консьюмеры их дедупят. Обработка OrderPlaced дважды должна списать карту один раз; идемпотентность — несущая половина паттерна, а не опция." },
  ]}
/>

<Quiz
  id="05-outbox-choice-5"
  lessonSlug="05-outbox"
  lang="ru"
  question="Ты масштабируешь polling relay с одной реплики до трёх ради пропускной способности и сразу видишь, что каждое событие публикуется примерно трижды. Какой фикс?"
  choices={[
    { label: "Захватывай строки через SELECT ... FOR UPDATE SKIP LOCKED, чтобы каждая реплика блокировала непересекающийся батч и пропускала строки, уже удерживаемые другим воркером. Тогда реплики масштабируются, не перечитывая одни и те же неотправленные строки.", correct: true },
    { label: "Сократить до одной реплики навсегда — outbox нельзя масштабировать горизонтально.", misconception: "Outbox прекрасно масштабируется по репликам; нужна лишь координация захвата строк. SKIP LOCKED позволяет нескольким воркерам делить таблицу без пересечений." },
    { label: "Дать каждой реплике свой интервал polling, чтобы тики не совпадали.", misconception: "Смещённые интервалы не мешают двум воркерам выбрать одни и те же WHERE sent=false строки на перекрывающихся тиках. Только захват блокировкой делает батчи непересекающимися." },
    { label: "Принять это — at-least-once значит тройная доставка в пределах спеки.", misconception: "At-least-once терпит редкий дубликат от краха, а не намеренное утроение каждого события; это жжёт пропускную способность и долбит консьюмеров. Координируй relay через SKIP LOCKED." },
  ]}
/>

<Quiz
  id="05-outbox-choice-6"
  lessonSlug="05-outbox"
  lang="ru"
  question="Outbox-таблица выросла до нескольких сотен тысяч строк, polling-запросы замедляются, а ночной DELETE старых sent-строк вызывает конкуренцию блокировок с вставками. Какой дизайн очистки лучше?"
  choices={[
    { label: "Партиционируй outbox по дням, чтобы reaping был метаданным-только DROP PARTITION, либо удаляй sent-строки маленькими батчами. Один огромный DELETE дерётся с вставками за блокировки и треплет индексы.", correct: true },
    { label: "Перестать помечать строки sent, чтобы relay самоограничился и таблица перестала расти.", misconception: "Не помечать строки sent заставит relay переотправлять всё вечно — бесконечный шторм дубликатов, а не очистку. Sent-строки надо reaping, просто не одним гигантским DELETE." },
    { label: "Добавить больше индексов на outbox, чтобы DELETE шёл быстрее.", misconception: "Больше индексов делают записи — включая горячий путь INSERT и сам DELETE — медленнее, а не быстрее, и не решают конкуренцию блокировок. Батчируй удаления или партиционируй." },
    { label: "Перенести очистку в бизнес-transaction, чтобы коммитилась атомарно.", misconception: "Привязка очистки к бизнес-записи добавляет задержку и давление блокировок на горячий путь без выгоды. Reaping — фоновое обслуживание, батчируй его или дропай партиции вне основного потока." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна цепочка: dual-write поперёк двух хранилищ не имеет крах-безопасного порядка, поэтому ты схлопываешь его в одну локальную transaction, которая пишет бизнес-строку и outbox-строку вместе. Отдельный relay шлёт эти строки — polling (просто, но задержка ограничена интервалом и постоянная нагрузка на БД) или CDC (единицы мс, почти нулевая нагрузка на таблицу, ценой операций). Поскольку relay публикует, затем помечает sent двумя шагами, доставка at-least-once, поэтому консьюмеры дедупят по стабильному event id. Операционный налог — bloat таблицы, порядок при параллельных relay, конкурирующие реплики — оплачивается партиционированием, батчевым reaping, keying по aggregate и FOR UPDATE SKIP LOCKED.
</Recap>
