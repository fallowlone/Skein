---
concepts:
- dual-write-problem
- transactional-outbox
- polling-relay
- change-data-capture
- at-least-once-delivery
- idempotent-consumer
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://microservices.io/patterns/data/transactional-outbox.html
- https://debezium.io/documentation/reference/stable/transformations/outbox-event-router.html
status: ready
summary: "Промпты на свободное припоминание по юниту outbox. Сначала ответь своими словами, затем раскрой модельный ответ и сравни."
title: 'Outbox: свободное припоминание'
track: queues
unit: 05-outbox
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт проговори или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет материал.
</Hook>

<Goal lang="ru">
Восстанови ключевые механизмы юнита — разрыв dual-write, фикс одной транзакцией, polling vs CDC, гарантию at-least-once, idempotent-консьюмеров и операционный налог — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:load
  id="05-outbox-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему у хендлера, который пишет в базу и публикует в брокер, нет крах-безопасного порядка этих двух вызовов?", a: "База и брокер — две отдельные системы без общей transaction, поэтому хендлер делает dual-write. Запись-строки-затем-публикация теряет событие, если процесс падает после коммита и до того, как публикация приземлилась: состояние изменилось, но downstream никто не знает. Публикация-затем-запись-строки эмитит фантомное событие, если крах падает после публикации и до коммита: консьюмеры реагируют на заказ, который откатился и не существовал. try/catch не может компенсировать, потому что OOM-kill в середине вызова не бросает ничего, что можно поймать, а логика компенсации сама — крошечный координатор распределённой транзакции, который тоже может упасть. Просто нет порядка двух неатомарных вызовов поперёк двух хранилищ, который переживёт крах в разрыве." },
    { q: "Как transactional outbox превращает dual-write в нечто атомарное, и что именно долговечно после COMMIT?", a: "В той же локальной transaction базы, что меняет бизнес-состояние, ты также делаешь INSERT строки в outbox-таблицу, описывающей событие. Поскольку обе записи в одной ACID transaction против одной базы, они коммитятся вместе или откатываются вместе — атомарно, без координатора и без 2PC. После COMMIT долговечна не только строка заказа, но и намерение, что событие OrderPlaced должно быть доставлено, записанное в той же базе. Публикация в брокер теперь отдельный, ретраибельный шаг, который хендлер никогда не выполняет; он возвращается в момент коммита transaction. Ты обменял невозможный атомарный dual-write на тривиально атомарную одну запись плюс асинхронный, крах-безопасный насос." },
    { q: "Сравни polling relay и CDC relay по задержке, нагрузке на первичную и операционной стоимости.", a: "Polling relay гоняет SELECT ... WHERE sent = false на интервале, публикует каждую строку, затем помечает её sent. Это предельно просто и работает на любой базе, но сквозная задержка ограничена интервалом (обычно 100мс–1с), и каждый poll грузит первичную OLTP-базу, есть работа или нет — реальное бремя при многих репликах relay. CDC relay (Debezium) вместо этого tailing write-ahead log базы и эмитит событие в момент коммита outbox INSERT: задержка в единицы мс, почти нулевая нагрузка на таблицу и естественный порядок коммитов из лога. Цена операционная — ты гоняешь и мониторишь коннектор, управляешь слотами репликации и рассуждаешь о ретенции WAL. Polling подходит скромному объёму без новой инфры; CDC — высокой пропускной способности, низкой задержке и требованиям к порядку." },
    { q: "Почему доставка outbox at-least-once, а не exactly-once, и что это навязывает консьюмерам?", a: "Relay делает две вещи, которые нельзя сделать атомарными поперёк систем: публикует в брокер, затем делает UPDATE строки в sent. Если он падает после того, как брокер принял сообщение, но до коммита UPDATE, строка всё ещё выглядит неотправленной, поэтому на рестарте relay публикует то же событие снова — дубликат. Обратный порядок (сначала пометить sent, затем публиковать) лишь превращает дубликат в потерянное событие, если крах падает между UPDATE и публикацией. Поэтому единственный безопасный порядок — publish-then-mark, и это at-least-once. Это навязывает каждому консьюмеру быть idempotent: каждая outbox-строка несёт стабильный event id, и консьюмеры дедупят по нему — обычно inbox-таблица обработанных id, хранимых часы-дни, или уникальное ограничение на побочный эффект — так что обработка OrderPlaced дважды всё равно списывает карту ровно один раз." },
    { q: "Ты масштабируешь polling relay до нескольких реплик. Какие две проблемы появляются и как решить каждую?", a: "Первая — конкурирующие relay: запусти несколько реплик наивно, и все они делают SELECT одних и тех же WHERE sent=false строк и дважды публикуют их. Фикс — SELECT ... FOR UPDATE SKIP LOCKED (Postgres / MySQL 8.0+), так что каждый воркер захватывает и блокирует непересекающийся батч, пропуская строки, уже удерживаемые другим, и реплики масштабируются, не наступая друг на друга. Вторая — порядок: один relay сохраняет порядок created_at, но два воркера, публикуя конкурентно, могут доставить события не по порядку. Если консьюмеру нужен порядок по aggregate, ключуй по aggregate id, чтобы все события одного заказа шли в одну партицию и одному воркеру, сохраняя порядок там, где он важен, и всё ещё распараллеливая по разным aggregate." },
    { q: "Что вызывает bloat outbox-таблицы и как её reaping без вреда горячему пути записи?", a: "Outbox растёт с каждым событием, и sent-строки накапливаются. Без reaping таблица и её индексы раздуваются до сотен тысяч строк, где polling-запросы деградируют. Неверный фикс — один гигантский DELETE старых sent-строк: он дерётся с вставками за блокировки и треплет индексы, вредя горячему пути. Верные фиксы — удалять sent-строки маленькими батчами вне основного потока, либо партиционировать outbox по дням, чтобы очистка стала метаданным-только DROP PARTITION вместо индекс-треплющих удалений строк. В любом случае reaping — фоновое обслуживание, держится вне бизнес-transaction, которая должна делать только два INSERT." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: dual-write поперёк двух хранилищ не имеет крах-безопасного порядка; outbox схлопывает его в одну локальную transaction, чей COMMIT долговечно фиксирует намерение публиковать; relay шлёт строки polling (просто, ограничено интервалом) или CDC (быстро, операционно тяжелее); разрыв publish-then-mark делает доставку at-least-once, поэтому консьюмеры дедупят по стабильному event id; а операционный налог — bloat, порядок, конкурирующие relay — оплачивается партиционированием или батчевым reaping, keying по aggregate и FOR UPDATE SKIP LOCKED.
</Recap>
