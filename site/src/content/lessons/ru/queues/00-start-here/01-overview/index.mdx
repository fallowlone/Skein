---
concepts:
- message
- producer
- consumer
- queue-vs-topic
- broker
- asynchronous-decoupling
- acknowledgement
- backlog-lag
deepensInto: []
estMin: 10
lang: ru
lessonType: topic
level: zero
order: 1
prereqs: []
slug: 01-overview
sources:
- https://kafka.apache.org/documentation/
- https://www.rabbitmq.com/tutorials/tutorial-one-javascript
- https://aws.amazon.com/message-queue/
- https://www.cloudamqp.com/blog/part1-rabbitmq-for-beginners-what-is-rabbitmq.html
spiral: []
status: ready
summary: Очередь сообщений — буфер, позволяющий одному сервису передать работу другому
  без ожидания. Это карта «с нуля» и восемь слов, которые юнит о гарантиях доставки
  считает уже знакомыми.
title: 'С нуля: что такое очередь сообщений на самом деле'
track: queues
unit: 00-start-here
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import queueVsTopic from "./infographics/queue-vs-topic.svg?raw";

<Hook>
Твоё веб-приложение неожиданно завирусилось. При каждой регистрации ты отправляешь приветственное письмо — а почтовый провайдер медленный. Теперь каждый запрос на регистрацию висит три секунды, пока письмо не уйдёт. Трафик удвоился — сервер встал. Решение не в более быстром провайдере. Решение — перестать ждать. Очередь сообщений позволяет обработчику регистрации сказать «кто-нибудь отправьте это письмо» и сразу продолжить работу, пока отдельный процесс подберёт задачу и выполнит её в своём темпе. Этот урок — карта, после которой такое решение станет понятным.
</Hook>

<Crux>Очередь сообщений — буфер, который позволяет одному сервису передать работу другому без ожидания, разделяя производителя и потребителя.</Crux>

<Explanation>

## Единственная проблема, которую решают очереди сообщений

Два сервиса, общающихся напрямую, связаны в момент вызова: если Сервис Б медленный — Сервис А ждёт; если Сервис Б упал — Сервис А падает тоже. Очереди сообщений разрывают эту связь. Сервис А кладёт сообщение в общий буфер и сразу продолжает работу. Сервис Б читает из буфера когда готов. Оба сервиса никогда не обязаны быть живыми одновременно, а всплеск нагрузки просто увеличивает буфер, а не роняет отправителя.

Всё в треке queues — гарантии доставки, партиционирование, порядок, паттерн outbox — это детали поверх одной идеи: **отдели отправителя от получателя, дав сообщению место, где оно может подождать.**

## Восемь слов, которые остальной трек считает знакомыми

Senior-юниты дальше используют эти термины, не останавливаясь на определениях. Вот они, по одному предложению — что это и зачем оно.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Слово</th>
        <th class="border border-border px-3 py-2 text-left">Что это</th>
        <th class="border border-border px-3 py-2 text-left">Зачем оно</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Сообщение</strong> (message)</td>
        <td class="border border-border px-3 py-2">Единица данных, которую один сервис отправляет, чтобы другой её обработал.</td>
        <td class="border border-border px-3 py-2">Чтобы работу можно было описать один раз и обработать где угодно и когда угодно.</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Производитель</strong> (producer)</td>
        <td class="border border-border px-3 py-2">Сервис, который создаёт и публикует сообщение.</td>
        <td class="border border-border px-3 py-2">Чтобы отправитель отвечал только за публикацию, а не за то, что будет дальше.</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Потребитель</strong> (consumer)</td>
        <td class="border border-border px-3 py-2">Сервис, который читает и обрабатывает сообщение.</td>
        <td class="border border-border px-3 py-2">Чтобы обработку можно было масштабировать, заменять или приостанавливать независимо от производителя.</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Очередь vs топик</strong></td>
        <td class="border border-border px-3 py-2">Очередь доставляет каждое сообщение одному потребителю; топик — всем подписчикам.</td>
        <td class="border border-border px-3 py-2">Чтобы выбирать: работу забирает один воркер или оповещаются все слушатели.</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Брокер</strong> (broker)</td>
        <td class="border border-border px-3 py-2">Сервер (например, Kafka, RabbitMQ), который хранит и маршрутизирует сообщения.</td>
        <td class="border border-border px-3 py-2">Чтобы производителю и потребителю не нужно было знать адрес и расписание друг друга.</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Асинхронное разделение</strong></td>
        <td class="border border-border px-3 py-2">Производитель продолжает работу сразу после публикации; обработка происходит позже.</td>
        <td class="border border-border px-3 py-2">Чтобы медленный или упавший потребитель не делал медленным или упавшим производителя.</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Подтверждение</strong> (ack)</td>
        <td class="border border-border px-3 py-2">Сигнал, который потребитель отправляет брокеру после успешной обработки сообщения.</td>
        <td class="border border-border px-3 py-2">Чтобы брокер знал, что сообщение обработано, и мог удалить его, а не переотправить.</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Бэклог / лаг</strong> (backlog)</td>
        <td class="border border-border px-3 py-2">Количество сообщений в очереди, ещё не обработанных потребителем.</td>
        <td class="border border-border px-3 py-2">Чтобы видеть, успевают ли потребители, и масштабировать их, когда отстают.</td>
      </tr>
    </tbody>
  </table>
</div>

## Как они складываются вместе

Прочитанные по порядку, слова рассказывают одну историю: **производитель** публикует **сообщение** в **брокер**; брокер хранит его в **очереди** (или рассылает всем подписчикам через **топик**). **Потребитель** читает сообщение в своём темпе и отправляет **ack** брокеру, как только работа выполнена. Пока ack не пришёл — брокер держит сообщение готовым к переотправке, так что ничего не теряется незаметно. Разница между опубликованными и подтверждёнными сообщениями в каждый момент — это **бэклог**, живой сигнал здоровья системы. Вся конструкция — **асинхронное разделение**: производитель свободен в момент публикации, а потребитель может работать на любой машине, в любом темпе, и производителю это неважно.

<Infographic
  svg={queueVsTopic}
  label="Сравнение бок о бок: очередь доставляет каждое сообщение ровно одному потребителю, который забирает работу и удаляет сообщение, разделяя нагрузку между добавленными потребителями; топик доставляет копию каждому подписчику, рассылая один и тот же поток, чтобы все реагировали параллельно."
  caption="Один брокер, две модели доставки. Очередь раздаёт задачу одному воркеру; топик рассылает событие всем подписчикам — эта развилка решает, забирает ли работу один или её получают многие."
  hue="var(--d-infra)"
/>

Когда в следующих юнитах ты встретишь гарантии, порядок или сбои — каждый из них будет описываться через эти восемь ролей; если термин покажется незнакомым, эта таблица — твоя точка возврата.

<Inset kind="why" lang="ru">
Почему бы просто не вызвать другой сервис напрямую через HTTP? Потому что такой вызов синхронный: оба сервиса должны быть живы в одно и то же мгновение, и вызывающий висит, пока вызываемый не ответит. В длинной цепочке сервисов один медленный нижний делает медленными всех выше. Очередь поглощает всплеск: производитель публикует и возвращается за микросекунды; потребитель разгребает бэклог в своём темпе. Очередь работает как амортизатор — резкий всплеск трафика увеличивает бэклог, а не частоту ошибок.
</Inset>

<Quiz
  id="00-start-here-queues-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Что потребитель отправляет брокеру после успешной обработки сообщения и почему это важно?"
  choices={[
    { label: "Подтверждение (ack) — оно говорит брокеру, что сообщение обработано, и брокер удаляет его вместо переотправки", correct: true },
    { label: "Новое сообщение в очередь ответа — так потребитель возвращает результат производителю", misconception: "Очередь ответа — один из опциональных паттернов, но это не то, что такое ack. Ack — сигнал брокеру о конкретном сообщении: его цель — управлять удалением и переотправкой, а не передавать результат." },
    { label: "Heartbeat-пинг, чтобы брокер знал, что потребитель жив", misconception: "Heartbeat — отдельный механизм в некоторых брокерах, но ack относится к конкретному сообщению. Он говорит брокеру «это конкретное сообщение обработано» — без ack брокер считает его незавершённым и переотправит." },
    { label: "Ничего — брокер автоматически удаляет сообщение после доставки", misconception: "Большинство брокеров хранят сообщение до получения явного ack. Автоматическое удаление при доставке означало бы, что падение потребителя до обработки приведёт к тихой потере сообщения — именно для этого и нужны ack." },
  ]}
/>

<DragOrder
  id="00-start-here-queues-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь путь сообщения от создания до завершения:"
  items={[
    { id: "a", label: "Производитель публикует сообщение в брокер" },
    { id: "b", label: "Брокер хранит сообщение в очереди" },
    { id: "c", label: "Потребитель читает сообщение и обрабатывает его" },
    { id: "d", label: "Потребитель отправляет ack; брокер удаляет сообщение" },
  ]}
  correctOrder={["a", "b", "c", "d"]}
/>

</Explanation>

<FlowDiagram
  nodes={[
    { id: "producer", label: "Производитель", sub: "публикует и продолжает", col: 0, row: 0 },
    { id: "broker", label: "Брокер", sub: "Kafka / RabbitMQ", col: 1, row: 0 },
    { id: "queue", label: "Queue / Topic", sub: "хранит до ack", col: 2, row: 0 },
    { id: "consumer", label: "Потребитель", sub: "читает в своём темпе", col: 3, row: 0 },
    { id: "ack", label: "Ack", sub: "сигнал завершения", col: 3, row: 1 },
    { id: "backlog", label: "Бэклог", sub: "опубликовано − подтверждено", col: 2, row: 1 },
  ]}
  edges={[
    { from: "producer", to: "broker", label: "publish" },
    { from: "broker", to: "queue", label: "store" },
    { from: "queue", to: "consumer", label: "deliver" },
    { from: "consumer", to: "ack", label: "при успехе" },
    { from: "ack", to: "broker", label: "удалить msg" },
    { from: "queue", to: "backlog", label: "измеряет" },
  ]}
  label="Производитель публикует сообщение в брокер, который хранит его в queue; потребитель читает и обрабатывает в своём темпе, затем отправляет ack — брокер удаляет сообщение; неподтверждённые сообщения накапливаются как бэклог."
  caption="Ack — ключевой контракт: без него брокер считает обработку незавершённой и переотправляет сообщение. Именно поэтому падение потребителя до ack приводит к повторной обработке, а не к тихой потере. Размер бэклога — живой сигнал того, что потребители отстают."
  hue="var(--d-systems)"
  perRow={4}
/>

<KeyTakeaway>Брокер хранит сообщения, пока потребитель не будет готов; производитель не ждёт. Эта идея — асинхронное разделение — лежит в основе каждого последующего юнита трека queues.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="00-start-here-queues-retrieval"
  lang="ru"
  questions={[
    { q: "В одном дыхании: какую проблему решают очереди сообщений и в чём ядро механизма?", a: "Когда два сервиса общаются напрямую, они связаны в момент вызова: если получатель медленный — отправитель ждёт, если падает — отправитель тоже. В длинной цепочке один медленный нижний делает медленными всех выше. Очереди решают это, давая сообщению место между отправителем и получателем: брокер хранит сообщение в очереди, производитель публикует и продолжает сразу, потребитель читает в своём темпе. Ядро механизма — ack: потребитель сигнализирует брокеру об успешной обработке, после чего брокер удаляет сообщение. До получения ack сообщение сохраняется для переотправки — ничего не теряется незаметно. Бэклог — необработанные сообщения — показывает в реальном времени, успевают ли потребители." },
    { q: "Проследи путь одного сообщения от публикации до удаления, называя каждого участника и его роль.", a: "Производитель создаёт сообщение — единицу данных с описанием работы — и публикует его в брокер (Kafka, RabbitMQ и т.д.). Брокер хранит сообщение в очереди (или рассылает через топик всем подписчикам). Работа производителя заканчивается в момент публикации — он не ждёт. Позже — через миллисекунды или часы, в зависимости от бэклога — потребитель читает сообщение из брокера и обрабатывает его. Как только обработка успешна, потребитель отправляет ack брокеру. Брокер получает ack и удаляет сообщение из очереди. Если потребитель упал до отправки ack — брокер считает сообщение незавершённым и переотправляет его другому экземпляру потребителя. Количество сообщений между «опубликованы» и «подтверждены» в каждый момент — бэклог; его рост означает, что потребители отстают и, возможно, нужно масштабирование." },
  ]}
/>

<Recap lang="ru">
Очереди сообщений решают одну проблему: два сервиса, вызывающие друг друга напрямую, связаны во времени — когда один медленный или упавший, другой страдает. Очереди разрывают эту связь, давая сообщениям место для хранения. Производитель публикует сообщение в брокер и сразу продолжает работу; потребитель читает из брокера когда готов и подтверждает каждое сообщение ack-ом по завершении; брокер держит сообщение до получения ack, чтобы ничего не потерялось незаметно. Бэклог — неподтверждённые сообщения — это живой сигнал здоровья потребителей. Очередь доставляет одному; топик рассылает всем подписчикам. Этот словарь — сообщение, производитель, потребитель, брокер, ack, бэклог, асинхронное разделение — будет использоваться во всех юнитах без дополнительных определений. Теперь, когда встретишь сообщение, у которого «пропал ack», ты сразу поймёшь: брокер по-прежнему держит его и готов переотправить — именно так система защищает работу от молчаливой потери.
</Recap>
