---
concepts:
- optimistic-ui
- read-your-own-writes
- pending-states
- idempotency-key
- conflict-resolution
- reconciliation
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://www.nngroup.com/articles/response-times-3-important-limits/
- https://tanstack.com/query/latest/docs/framework/react/guides/optimistic-updates
- https://blog.logrocket.com/solving-eventual-consistency-frontend/
- https://dzone.com/articles/conflict-resolution-using-last-write-wins-vs-crdts
spiral: []
status: ready
summary: Когда запись идёт через очередь, результат не читается мгновенно. Наивный
  UI либо врёт («готово!» до того, как пайплайн закончил), либо показывает устаревшие
  данные. Это фронтенд-половина event-driven систем.
title: 'UX поверх async-бэкендов: optimistic UI, состояния ожидания, read-your-writes'
track: queues
unit: 07-eventual-ux
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Пользователь жмёт «Опубликовать», спиннер останавливается, тост говорит «Опубликовано!» — а поста нигде нет. Тикеты в саппорт растут. Баг: POST вернул `202 Accepted` и сбросил работу в Kafka-топик; консьюмер пишет пост через ~800мс. Фронтенд воспринял `202` как `200`, рефетчнул список до того, как консьюмер отработал, получил пустой результат и поверил ему. UI был не медленным. Он был уверенно неправ насчёт записи, которой ещё не было. За следующие десять минут ты поймёшь, почему это происходит детерминированно, и какие паттерны это исправляют навсегда.
</Hook>

<Crux>Когда запись идёт через очередь, результат не читается мгновенно — UI, который показывает «готово» или сразу рефетчит, соврёт.</Crux>

<Explanation>

## Окно консистентности теперь твоя проблема

В тот момент, когда запись перестаёт быть синхронной — уходит в очередь, на шину событий, к воркеру — появляется разрыв между «сервер принял» и «сервер вернёт это при чтении». Этот разрыв — **окно консистентности**. При нормальной нагрузке это может быть 50–300мс. При backpressure, отстающем консьюмере или ребалансе partition оно растягивается до секунд. Команда бэкенда видит в этом выигрыш по пропускной способности. Пользователь переживает это как то, что приложение забыло, что он только что сделал.

Первое, что это ломает, — **read-your-own-writes**: гарантия, что *я* вижу *своё* изменение немедленно, даже если другие пользователи ещё нет. Наивный сценарий — POST, потом рефетч списка — нарушает её постоянно, потому что рефетч гонится с консьюмером и обычно выигрывает. Собственная правка пользователя исчезает с его же экрана. Это самый частый «баг-призрак» на async-бэкендах, и он не случайный: это детерминированный итог доверия чтению, которое произошло внутри окна консистентности.

## Optimistic UI: применяй локально, согласовывай потом

Рефлекс сеньора — **optimistic UI**: применить изменение к локальному состоянию в момент действия пользователя, отправить запрос в фоне и согласовать, когда придёт истина. Обоснование не эстетическое — это пороги восприятия, которые Нильсен задокументировал в 1993 и которые до сих пор держатся: **~100мс** ощущается мгновенным (прямое манипулирование), **~1с** удерживает пользователя в потоке, но он замечает ожидание, а **~10с** — предел, за которым внимание рвётся и он считает, что всё упало. Async-пайплайн легко пробивает 1с по полному кругу, так что *реальный* round-trip быстрым не сделать — ты делаешь *воспринимаемый* ~0мс, рендеря предсказанный результат сразу.

У контракта три части, и третью джуны пропускают:

1. **Применить** предсказанное изменение к локальному состоянию и показать его.
2. **Отправить** запрос; сохранить достаточно контекста, чтобы откатить.
3. **Согласовать** — при успехе заменить предсказание серверной истиной (она может отличаться); при ошибке **откатить** к снимку, который ты взял перед применением.

Вместе эти три шага означают, что UI никогда не застревает с устаревшим предсказанием: либо сервер подтверждает и ты заменяешь на правду, либо отклоняет и ты откатываешь, как будто ничего не было. Пропусти шаг 3 — и ты отгружаешь ложь: оптимистичное значение висит на экране без пути коррекции.

TanStack Query кодирует ровно это в `useMutation`: `onMutate` отменяет рефетчи в полёте, делает снимок кэша и пишет оптимистичное значение (возвращая снимок как контекст); `onError` восстанавливает этот снимок; `onSettled` вызывает `invalidateQueries`, чтобы рефетчнуть реальное состояние. React 19 поставляет `useOptimistic` для облегчённой, привязанной к transition версии. Форма везде одна: снимок → предсказание → согласование-или-откат.

<Inset kind="why" lang="ru">
Optimistic UI молча предполагает, что клиент может *предсказать* результат сервера. Это работает для «добавить эту задачу», но ломается для «сервер назначает номер заказа», «сервер применяет скидку» или «другой пользователь уже это изменил». Когда предсказание и согласованная истина расходятся, ты обязан показать исправленное значение — а не держать оптимистичное. Оптимистичное обновление, которое никогда не согласуется, — это просто ложь с лучшей задержкой.
</Inset>

<SequenceDiagram
  label="Оптимистичное обновление сейчас, согласование потом"
  hue="var(--d-systems)"
  actors={["User", "UI", "Queue/Worker"]}
  messages={[
    { from: "User", to: "UI", label: "действие (click)" },
    { from: "UI", to: "User", label: "показать pending СЕЙЧАС (optimistic)" },
    { from: "UI", to: "Queue/Worker", label: "POST -> 202, в очередь" },
    { from: "Queue/Worker", to: "UI", label: "...обработка... событие done (позже)" },
    { from: "UI", to: "User", label: "согласовать: confirm ИЛИ откат + show error" },
  ]}
  caption="UI рендерит предсказанный результат в момент действия пользователя (~0мс воспринимаемо); воркер обрабатывает асинхронно, и UI согласует только когда результат пришёл — подтверждая при успехе или откатывая и вынося ошибку наружу."
/>

## Состояния ожидания лучше фейкового успеха

Когда ты *не можешь* безопасно предсказать исход (платёж списан? видео перекодировано? экспорт готов?), честный ход — явное **состояние ожидания/обработки**, а не фейковое «готово». Покажи, что работа в полёте, дай ей аффорданс — шаг прогресса, ETA, «мы пришлём письмо» — и дай UI сойтись, когда событие придёт. Кардинальный грех — схлопнуть `202 Accepted` в тост успеха: ты говоришь пользователю, что что-то завершилось, хотя оно ещё лежит в очереди.

Здесь живут два провала. Первый — **бесконечный спиннер**: UI ждёт событие, которое никогда не придёт (консьюмер упал, сообщение ушло в dead-letter — очередь недоставляемых сообщений), и крутится вечно. Каждому состоянию ожидания нужен путь **timeout + согласование** — через N секунд опросить авторитетный эндпоинт или показать «всё ещё обрабатывается, зайдите позже». Второй — **двойная отправка при повторе**: пользователь не видит обратной связи, кликает снова, и теперь в очереди два сообщения. Без **idempotency key** (ключ идемпотентности — уникальный токен, привязанный к намерению, который сервер использует для дедупликации повторных запросов) это два списания, два заказа, два письма.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Ситуация</th>
        <th class="border border-border px-3 py-2 text-left">Правильный UX-паттерн</th>
        <th class="border border-border px-3 py-2 text-left">Почему</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Результат предсказуем (добавить задачу, лайк, переименовать)</td>
        <td class="border border-border px-3 py-2">Optimistic UI + откат</td>
        <td class="border border-border px-3 py-2">Воспринимаемая задержка ~0мс; согласование на settle</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Результат непредсказуем (платёж, перекодирование, экспорт)</td>
        <td class="border border-border px-3 py-2">Явное состояние ожидания + timeout</td>
        <td class="border border-border px-3 py-2">Нельзя сфабриковать значение, которого не знаешь; не показывай «готово» рано</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Рефетч сразу после записи</td>
        <td class="border border-border px-3 py-2">Локальное эхо / sticky-чтение</td>
        <td class="border border-border px-3 py-2">Рефетч гонится с консьюмером и выигрывает → нарушение read-your-writes</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Пользователь повторяет / двойной клик</td>
        <td class="border border-border px-3 py-2">Idempotency key на намерение</td>
        <td class="border border-border px-3 py-2">Один ключ = эффект at-most-once; нет двойного списания</td>
      </tr>
    </tbody>
  </table>
</div>

## Когда сталкиваются два async-обновления

Eventual consistency означает, что две записи могут быть в полёте одновременно — твоя и коллеги, или с твоего телефона и ноутбука. Согласование обязано выбрать победителя, и стратегия — это реальный трейдофф. **Last-write-wins (LWW)** штампует каждую запись timestamp и оставляет последнюю; это тривиально и без состояния, но молча отбрасывает данные проигравшего и зависит от синхронных часов — clock skew между нодами может дать победу *более старой* правке. **Merge**-логика (по полям или три-сторонняя) сохраняет обе стороны, когда изменения не пересекаются, но требует доменных правил. **CRDT** (conflict-free replicated data types) позволяют репликам слиться в любом порядке к одному результату без координации — основа real-time коллаб-редакторов — но накапливают метаданные и могут расти неограниченно без сборки мусора. Для «переименовать» LWW годится; для общего документа LWW уничтожает текст, поэтому тянешься к CRDT.

Работа фронтенда — сделать выбранную стратегию *читаемой*: если LWW отбросил изменение пользователя — скажи об этом и предложи применить заново; если произошёл merge — покажи, что слилось; вынеси `409 Conflict` как реальный выбор («их версия / твоя версия / merge»), а не молчаливую перезапись.

<TradeoffMatrix
  id="07-eventual-ux-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Пользователь жмёт «Отметить выполненным» на задаче, которая идёт через очередь (консьюмер пишет через ~500мс). Выбери UX."
  options={[
    { name: "Optimistic UI: переключить чекбокс сейчас, отправить в фоне, откатить к снимку при ошибке запроса, согласовать на settle", summary: "Предсказать + согласовать.", correct: true, justification: "Исход предсказуем (задача станет выполненной), поэтому можно отрендерить результат сразу и попасть в ~0мс воспринимаемой задержки, а не ждать всё окно консистентности ~500мс+. Снимок перед переключением позволяет чисто откатиться при ошибке, а инвалидация на settle заменяет предсказание серверной истиной. Это ровно контракт onMutate → onError → onSettled." },
    { name: "Показать спиннер на чекбоксе, пока консьюмер не подтвердит, потом переключить", summary: "Блокироваться на истине.", justification: "Честно, но бессмысленно медленно для предсказуемого действия: ты заставляешь пользователя высидеть полный async round-trip (часто >1с, пробивая порог потока) ради результата, который мог предсказать. Блокирующие состояния ожидания оставляй для исходов, которые реально не предсказать, например списание платежа." },
    { name: "Переключить чекбокс и сразу рефетчнуть список задач для подтверждения", summary: "Рефетч для проверки.", justification: "Классическое нарушение read-your-writes: рефетч гонится с консьюмером ~500мс и обычно выигрывает, возвращая задачу всё ещё невыполненной — так твой собственный UI снимает галку, которую ты только что поставил. Согласуй инвалидацией на settle (с задержкой или по push), а не сразу гоночным рефетчем." },
    { name: "Показать тост «Выполнено!», как только POST вернул 202", summary: "Доверять 202.", justification: "202 Accepted значит «в очереди», а не «готово». Объявить успех до того, как консьюмер отработал, — это провал фейкового успеха: если консьюмер уйдёт в dead-letter, ты сказал пользователю, что что-то завершилось, хотя этого не будет." },
  ]}
/>

<Quiz
  id="07-eventual-ux-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="POST, сбрасывающий работу в очередь, возвращает 202 Accepted за 40мс; консьюмер пишет строку через ~700мс. UI делает POST и сразу рефетчит список. Что увидит пользователь?"
  choices={[
    { label: "Его новый элемент отсутствует — рефетч обогнал консьюмера и выиграл, прочитав внутри окна консистентности (нарушение read-your-writes)", correct: true },
    { label: "Новый элемент появляется мгновенно, потому что 202 значит, что запись закоммичена", misconception: "202 Accepted значит, что запрос поставлен в очередь, а не что строка существует. Данные не читаемы, пока не отработает консьюмер." },
    { label: "409 Conflict, потому что строки ещё нет", misconception: "Конфликта нет — чтение просто происходит до того, как запись приземлилась, поэтому возвращает устаревшие (элемент-отсутствует) данные, и UI им верит." },
    { label: "Ничего не меняется; рефетчи кэшированы, поэтому список никогда не обновится", misconception: "Рефетч как раз возвращает данные — просто данные до того, как консьюмер записал, в чём вся проблема." },
  ]}
/>

<Quiz
  id="07-eventual-ux-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Кнопка «Оплатить» показывает спиннер. Событие, подтверждающее списание, никогда не приходит (консьюмер упал). Без дополнительной обработки что произойдёт?"
  choices={[
    { label: "Бесконечный спиннер — UI ждёт вечно событие, которое не придёт; нужен timeout + согласование (опросить авторитетный эндпоинт или показать «всё ещё обрабатывается»)", correct: true },
    { label: "Спиннер сам разрешится после того, как истечёт таймаут HTTP-запроса", misconception: "HTTP POST уже вернулся (202); спиннер ждёт отдельное async-событие, которым никакой HTTP-таймаут не управляет." },
    { label: "TanStack Query откатит его автоматически через 30с", misconception: "Никакая клиентская библиотека не выдумывает исход сервера. Согласование/таймаут — это то, что проектируешь ты; ничто не откатит событие, которое просто не сработало." },
    { label: "Пользователь может безопасно кликнуть снова без риска", misconception: "Повторный клик без idempotency key рискует двойным списанием — первый запрос может всё ещё обрабатываться в очереди." },
  ]}
/>

<DragOrder
  id="07-eventual-ux-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь оптимистичную мутацию против async-бэкенда (контракт TanStack Query):"
  items={[
    { id: "a", label: "onMutate: отменить рефетчи в полёте, чтобы они не затёрли оптимистичное значение" },
    { id: "b", label: "onMutate: сделать снимок текущего кэша (чтобы можно было откатить)" },
    { id: "c", label: "onMutate: записать предсказанное значение в кэш и вернуть снимок как контекст" },
    { id: "d", label: "onError: восстановить снимок из контекста — откатить предсказание" },
    { id: "e", label: "onSettled: invalidateQueries, чтобы согласоваться с реальным состоянием сервера" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Async-записи не читаются мгновенно. Предсказывай + согласовывай (optimistic UI), когда результат известен; покажи честное состояние ожидания с таймаутом, когда нет. Не считай 202 готовым; ключуй повторы.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-eventual-ux-retrieval"
  lang="ru"
  questions={[
    { q: "Почему «POST, потом сразу рефетч» ломает read-your-own-writes на async-бэкенде, и как это починить, не отказываясь от обновления?", a: "POST возвращается быстро (часто 202 Accepted), потому что только ставит работу в очередь; реальная запись происходит, когда консьюмер обработает сообщение, а это может быть 50–300мс при нормальной нагрузке и секунды при backpressure или отстающей partition. Эта задержка — окно консистентности. Немедленный рефетч гонится с консьюмером и почти всегда выигрывает, поэтому читает состояние до того, как запись приземлилась, и возвращает собственное изменение пользователя как отсутствующее — а затем UI верит этому устаревшему чтению и стирает то, что пользователь только что сделал. Фикс — не зависеть от гоночного чтения ради read-your-writes: применить изменение оптимистично к локальному состоянию (локальное эхо), чтобы пользователь увидел его сразу, и согласовать с сервером позже — либо инвалидацией после короткой задержки, либо ожиданием push (WebSocket/SSE), сигналящего, что консьюмер закончил, либо sticky-чтением с ноды, гарантированно имеющей запись. Ты сохраняешь итоговое обновление; просто перестаёшь давать ему перезаписывать истину пользователя до того, как бэкенд догнал." },
    { q: "Когда optimistic UI — неправильный инструмент, и что делать вместо него?", a: "Optimistic UI предполагает, что клиент может предсказать результат сервера. Это безопасно для действий, чей исход полностью определён на клиенте — переключить чекбокс, добавить задачу, лайкнуть пост. Это неверно всякий раз, когда исход решает сервер, а не клиент: платёж, который могут отклонить, номер заказа, который назначает сервер, скидка, которую сервер вычисляет, перекодирование, которое может упасть, или запись, которая может проиграть конфликт другому пользователю. Подделка такого даёт оптимистичное значение, расходящееся с серверной истиной, и если ты никогда не согласуешь — ты отгрузил уверенную ложь. Для непредсказуемых исходов используй вместо этого явное состояние ожидания/обработки: покажи, что работа в полёте, дай ей аффорданс (прогресс, ETA, «мы пришлём письмо»), защити таймаутом, чтобы не приходящее событие не крутилось вечно, и дай UI сойтись, когда придёт реальный результат — заменяя состояние ожидания фактическим серверным значением, включая вынос 409-конфликта как реального выбора, а не молчаливую перезапись." },
  ]}
/>

<Recap lang="ru">
Async-бэкенды переносят окно консистентности — разрыв между «принято» и «читаемо» — на фронтенд, и наивный UI либо врёт, либо показывает устаревшие данные. Два честных инструмента делятся по предсказуемости: когда клиент может предсказать результат, используй optimistic UI — примени локально, сделай снимок, отправь, затем согласуй с серверной истиной на settle или откати при ошибке — что покупает ~0мс воспринимаемой задержки против порогов восприятия 100мс/1с/10с. Когда исход решает сервер, покажи явное состояние ожидания с таймаутом и согласованием, чтобы не приходящее событие не крутилось вечно, и никогда не схлопывай 202 Accepted в тост успеха. Read-your-own-writes ломается, когда рефетч гонится с консьюмером, поэтому эхай изменение локально вместо доверия немедленному гоночному чтению. Делай повторы идемпотентными через ключ, чтобы двойной клик не списал дважды. А когда сталкиваются две async-записи, выбирай согласование осознанно — last-write-wins для простых полей (принимая, что он отбрасывает проигравшего и доверяет часам), CRDT для общих документов — и затем делай исход читаемым для пользователя вместо молчаливой перезаписи его работы. Теперь, когда встретишь тост «Опубликовано!» на 202 — ты знаешь: это ложь в процессе, и знаешь ровно те контракты, которые её исправляют.
</Recap>
