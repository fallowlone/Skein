---
concepts:
- optimistic-ui
- read-your-own-writes
- pending-states
- idempotency-key
- conflict-resolution
- consistency-window
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.nngroup.com/articles/response-times-3-important-limits/
- https://tanstack.com/query/latest/docs/framework/react/guides/optimistic-updates
- https://dzone.com/articles/conflict-resolution-using-last-write-wins-vs-crdts
status: ready
summary: "Тест с выбором на синтез по всему юниту — optimistic UI, read-your-own-writes, pending-состояния, идемпотентные retry и conflict resolution над асинхронным бэкендом."
title: 'UX при eventual consistency: тест с выбором ответа'
track: queues
unit: 07-eventual-ux
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает UX-решение, которое ты принимаешь над реальным асинхронным бэкендом — не определение для заучивания, а компромисс, который надо взвесить, пока consistency-окно растягивается под нагрузкой.
</Hook>

<Goal lang="ru">
Убедись, что связываешь consistency-окно, optimistic UI, read-your-own-writes, честные pending-состояния, идемпотентные retry и conflict resolution — тот синтез, к которому вёл урок.
</Goal>

<Quiz
  id="07-eventual-ux-choice-1"
  lessonSlug="07-eventual-ux"
  lang="ru"
  question="POST кладёт работу в очередь и возвращает 202 Accepted за 40мс; consumer пишет строку ~700мс спустя. UI делает POST, а затем сразу рефетчит список. Что увидит пользователь и почему?"
  choices={[
    { label: "Его нового элемента нет — рефетч обогнал consumer и выиграл, прочитав внутри consistency-окна. Это нарушение read-your-own-writes: UI доверяет чтению, случившемуся до того, как запись приземлилась.", correct: true },
    { label: "Его новый элемент появляется мгновенно, потому что 202 означает, что запись закоммичена.", misconception: "202 Accepted означает, что запрос поставлен в очередь, а не что строка существует. Данные нечитаемы, пока consumer не обработает сообщение ~700мс спустя." },
    { label: "409 Conflict, потому что строки ещё нет.", misconception: "Конфликта нет — для конфликта нужны две конкурирующие записи. Чтение просто случается до того, как запись приземлилась, и возвращает устаревшие данные без элемента." },
    { label: "Ничего не меняется, потому что рефетчи закэшированы и не обновляются.", misconception: "Рефетч действительно возвращает свежие данные — просто данные до того, как consumer записал. Проблема в чтении внутри окна, а не в кэшировании." },
  ]}
/>

<Quiz
  id="07-eventual-ux-choice-2"
  lessonSlug="07-eventual-ux"
  lang="ru"
  question="Ты подключаешь optimistic-обновление в TanStack Query useMutation. Какой шаг чаще всего пропускают джуны и что ломается без него?"
  choices={[
    { label: "Снапшот кэша в onMutate и его восстановление в onError. Без пути rollback упавшая мутация навсегда оставляет предсказанное значение на экране — уверенная ложь, которая никогда не сверяется с серверной правдой.", correct: true },
    { label: "Вызов invalidateQueries только в onSuccess, а не в onSettled.", misconception: "Инвалидация на settle (успех ИЛИ ошибка) надёжнее, но реально пропускают именно снятие снапшота, чтобы onError мог откатить. Пропуск reconciliation плох; пропуск rollback портит состояние." },
    { label: "Отмена in-flight рефетчей в onMutate.", misconception: "Отмена рефетчей важна, чтобы они не затёрли optimistic-значение, но самое пропускаемое и самое разрушительное упущение — пара снапшот-и-rollback; без неё сбой невидим." },
    { label: "Ожидание (await) мутации до любого рендера.", misconception: "Await до рендера губит весь смысл optimistic UI — он возвращает задержку, которую ты прятал. Недостающая часть — rollback, а не блокировка." },
  ]}
/>

<Quiz
  id="07-eventual-ux-choice-3"
  lessonSlug="07-eventual-ux"
  lang="ru"
  question="Кнопка 'Оплатить' показывает спиннер. Событие, подтверждающее списание, не приходит, потому что consumer упал. Без дополнительной обработки что произойдёт?"
  choices={[
    { label: "Бесконечный спиннер — UI вечно ждёт событие, которое не придёт. Каждому pending-состоянию нужен timeout плюс путь reconciliation: через N секунд опросить авторитетный эндпоинт или показать 'всё ещё в обработке, загляни позже.'", correct: true },
    { label: "Спиннер сам разрешится, как только истечёт таймаут HTTP-запроса.", misconception: "HTTP POST уже вернул 202; спиннер ждёт отдельное асинхронное событие, которым не управляет никакой HTTP-таймаут. Запрос успешен — работа нет." },
    { label: "TanStack Query откатит его автоматически через 30 секунд.", misconception: "Ни одна клиентская библиотека не выдумывает серверный исход. Timeout и reconciliation ты проектируешь сам; ничто не откатывает событие, которое просто не сработало." },
    { label: "Пользователь может безопасно кликнуть снова без риска.", misconception: "Повторный клик без idempotency key рискует двойным списанием — первый запрос может всё ещё лежать в очереди, готовясь обработаться." },
  ]}
/>

<Quiz
  id="07-eventual-ux-choice-4"
  lessonSlug="07-eventual-ux"
  lang="ru"
  question="Предсказуемое действие (переключение чекбокса, которое идёт через очередь, consumer пишет ~500мс спустя) показывается блокирующим спиннером до подтверждения consumer'ом. Что не так с этим UX-выбором?"
  choices={[
    { label: "Он без нужды блокирует на правде ради результата, который ты мог предсказать. Ты заставляешь пользователя высидеть полный асинхронный round-trip (часто более 1с, за порогом потока), когда optimistic UI дал бы ~0мс воспринимаемой задержки. Блокировку оставляй для исходов, которые реально нельзя предсказать.", correct: true },
    { label: "Ничего — блокировка на реальном результате всегда самый корректный выбор.", misconception: "Корректность и хороший UX здесь расходятся: исход полностью предсказуем на клиенте, поэтому блокировка меняет воспринимаемую задержку на гарантию, которая была не нужна. Предсказывай и сверяй." },
    { label: "Спиннер должен быть прогресс-баром с ETA.", misconception: "Более богатый индикатор не чинит основную ошибку — блокировку на предсказуемом результате. Фикс — применить оптимистично, а не украсить ожидание." },
    { label: "Переключатели-чекбоксы вообще нельзя делать optimistic.", misconception: "Переключатель — каноничное optimistic-действие: его исход полностью определён на клиенте. Переключи сейчас, отправь в фоне, откати при сбое." },
  ]}
/>

<Quiz
  id="07-eventual-ux-choice-5"
  lessonSlug="07-eventual-ux"
  lang="ru"
  question="Два пользователя одновременно правят тело одного общего документа; обе записи идут через очередь. Бэкенд разрешает конфликт через last-write-wins (LWW). В чём senior-критика?"
  choices={[
    { label: "LWW молча выбрасывает правку проигравшего и доверяет синхронизированным часам — clock skew может дать выиграть более старой правке. Для свободного общего текста это уничтожает данные; тянись за CRDT, который позволяет репликам сливаться в любом порядке к одному результату без координации.", correct: true },
    { label: "LWW тут подходит, потому что последняя правка всегда та, что хотел пользователь.", misconception: "'Последняя' зависит от меток часов, которые могут разъезжаться между узлами, и даже корректная метка отбрасывает реальную работу другого пользователя. Для общего текста это потеря данных, а не разрешение." },
    { label: "Всегда нужно использовать CRDT и нигде не использовать LWW.", misconception: "LWW отлично подходит для простого поля вроде переименования, где выбросить проигравшего допустимо. CRDT несут накладные метаданных и могут расти неограниченно без GC — подбирай стратегию под данные." },
    { label: "Conflict resolution — забота бэкенда; у фронтенда нет роли.", misconception: "Задача фронтенда — сделать выбранную стратегию читаемой: если LWW выбросил изменение, скажи об этом и предложи переприменить; покажи 409 как реальный выбор, а не как тихую перезапись." },
  ]}
/>

<Quiz
  id="07-eventual-ux-choice-6"
  lessonSlug="07-eventual-ux"
  lang="ru"
  question="Пользователь жмёт 'Отправить', не видит фидбэка, потому что pending-состояние не построили, и жмёт снова. В очередь падают два сообщения. Какой устойчивый корневой фикс?"
  choices={[
    { label: "Прикрепить idempotency key на каждое намерение, чтобы один ключ давал эффект at-most-once — второе сообщение дедуплицируется на границе. Отключение кнопки — полезный guard, но не переживает refresh, retry или вторую вкладку; ключ — устойчивый фикс.", correct: true },
    { label: "Просто отключить кнопку после первого клика.", misconception: "Отключение помогает, но недостаточно — refresh страницы, сетевой retry или вторая вкладка все вернут путь. Только idempotency key делает эффект at-most-once во всех случаях." },
    { label: "Добавить более долгий спиннер, чтобы пользователь не кликал снова.", misconception: "Спиннер снижает шанс двойного клика, но ничего не делает для автоматических retry или refresh. Тебе всё равно нужен idempotency key, чтобы дедуплицировать на границе." },
    { label: "Повторять POST на клиенте, пока не получится.", misconception: "Слепые клиентские retry без idempotency key — ровно то, как одно намерение становится многими эффектами. Retry безопасны, только когда ключ делает их идемпотентными." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одно правило: асинхронная запись не читается мгновенно, поэтому consistency-окно становится проблемой фронтенда. Когда клиент может предсказать результат, используй optimistic UI — снапшот, предсказание, reconcile или rollback — чтобы выиграть ~0мс воспринимаемой задержки против порогов 100мс/1с/10с. Когда исходом владеет сервер, покажи честное pending-состояние с timeout, чтобы неприходящее событие не крутилось вечно, и никогда не сворачивай 202 в success-тост. Эхай записи локально вместо доверия гоночному рефетчу, ключуй retry, чтобы двойной клик не списал дважды, и разрешай конфликты осознанно — LWW для простых полей, CRDT для общего текста — делая исход читаемым, а не тихо перезаписывая пользователя.
</Recap>
