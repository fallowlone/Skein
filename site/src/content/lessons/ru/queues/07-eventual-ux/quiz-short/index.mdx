---
concepts:
- consistency-window
- optimistic-ui
- pending-states
- idempotency-key
- conflict-resolution
- read-your-own-writes
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.nngroup.com/articles/response-times-3-important-limits/
- https://tanstack.com/query/latest/docs/framework/react/guides/optimistic-updates
- https://dzone.com/articles/conflict-resolution-using-last-write-wins-vs-crdts
status: ready
summary: "Промпты на свободное припоминание по юниту. Сначала ответь своими словами, затем раскрой модельный ответ и сравни."
title: 'UX при eventual consistency: свободное припоминание'
track: queues
unit: 07-eventual-ux
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт проговори или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет материал.
</Hook>

<Goal lang="ru">
Восстанови ключевые идеи юнита — consistency-окно, трёхчастный контракт optimistic UI, когда показывать pending-состояние, идемпотентные retry и осознанный conflict resolution — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:load
  id="07-eventual-ux-short-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое consistency-окно и почему асинхронный бэкенд делает его проблемой фронтенда?", a: "Consistency-окно — это зазор между «сервер принял запись» и «сервер вернёт эту запись на чтении». В тот момент, когда запись перестаёт быть синхронной — уходит в очередь, на event bus или к воркеру — этот зазор открывается, потому что POST лишь ставит работу в очередь, а consumer применяет её позже. Под нормальной нагрузкой это может быть 50–300мс; под backpressure, при отстающем consumer'е или ребалансе партиций растягивается до секунд. Бэкенд-команда видит в async выигрыш пропускной способности, но окно не исчезает — оно переезжает на клиент, которому теперь надо отрисовать что-то честное в этот зазор. Наивный UI либо врёт (показывает «готово» до того, как consumer отработал), либо показывает устаревшие данные (рефетчит внутри окна). Так что фронтенд владеет окном, признаёт он это или нет." },
    { q: "Сформулируй трёхчастный контракт optimistic UI и какую часть пропускают джуны.", a: "Apply, send, reconcile. (1) Применить предсказанное изменение к локальному состоянию и сразу показать его, чтобы воспринимаемая задержка была ~0мс. (2) Отправить запрос в фоне, сохранив достаточно контекста — снапшот — чтобы откатить. (3) Reconcile: при успехе заменить предсказание серверной правдой, которая может отличаться; при сбое откатиться к снапшоту, снятому до применения. Часть, которую пропускают джуны, — шаг reconcile/rollback. Optimistic-обновление, которое никогда не сверяется, — это просто ложь с лучшей задержкой: если запрос упал, а ты не восстановил снапшот, предсказанное значение остаётся на экране навсегда, молча расходясь с тем, что реально держит сервер. TanStack Query кодирует контракт как onMutate (отменить рефетчи, снапшот, записать предсказание) → onError (восстановить снапшот) → onSettled (инвалидация для reconcile)." },
    { q: "Когда optimistic UI — неверный инструмент и что показывать вместо него?", a: "Optimistic UI предполагает, что клиент может предсказать результат сервера. Это держится для действий, чей исход определён на клиенте — переключить чекбокс, добавить todo, лайкнуть пост. Это неверно всякий раз, когда исход решает сервер: платёж, который могут отклонить, номер заказа, который назначает сервер, скидка, которую он считает, transcode, который может упасть, или запись, которая может проиграть конфликт другому пользователю. Подделка таких исходов даёт предсказание, расходящееся с правдой, и без reconciliation это уверенная ложь. Вместо этого покажи явное pending/processing-состояние: просигналь, что работа в полёте, дай ему affordance (шаг прогресса, ETA, «мы напишем на почту») и сведи UI, когда придёт реальное событие — заменив pending-состояние фактическим значением, в том числе показав 409-конфликт как реальный выбор, а не тихую перезапись." },
    { q: "Почему каждому pending-состоянию нужен timeout и какие два failure mode тут таятся?", a: "Потому что событие, которого ты ждёшь, может никогда не прийти. Первый failure mode — бесконечный спиннер: consumer упал или сообщение ушло в dead-letter, подтверждающее событие не сработало, и UI крутится вечно — никакой HTTP-таймаут им не управляет, потому что исходный POST уже вернул 202. Поэтому каждому pending-состоянию нужен timeout плюс путь reconciliation: через N секунд опросить авторитетный эндпоинт или показать «всё ещё в обработке, загляни позже». Второй failure mode — double-submit на retry: без фидбэка пользователь жмёт снова, и теперь в очереди два сообщения. Без idempotency key это два списания, два заказа, два письма. Pending-состояние и idempotency key дополняют друг друга — состояние мешает пользователю кликнуть снова от растерянности, ключ делает это безопасным, даже если он всё-таки кликнул." },
    { q: "Объясни компромисс LWW vs merge vs CRDT для разрешения двух конкурентных асинхронных записей и роль фронтенда в каждом случае.", a: "Eventual consistency означает, что две записи могут быть в полёте одновременно — твоя и коллеги, или с телефона и с ноутбука — и reconciliation должен выбрать победителя. Last-write-wins (LWW) штампует каждую запись меткой времени и держит последнюю; это тривиально и stateless, но молча выбрасывает данные проигравшего и зависит от синхронизированных часов, так что clock skew может дать выиграть более старой правке. Merge-логика (по полям или three-way) сохраняет обе стороны, когда изменения не пересекаются, но требует доменных правил. CRDT (conflict-free replicated data types) позволяют репликам сливаться в любом порядке к одному результату без координации — основа real-time коллаб-редакторов — но накапливают метаданные и могут расти неограниченно без garbage collection. Для переименования LWW годится; для общего документа LWW уничтожает текст, поэтому тянешься за CRDT. Роль фронтенда во всех случаях — сделать выбранную стратегию читаемой: если LWW выбросил изменение, скажи и предложи переприменить; если случился merge, покажи, что слилось; покажи 409 как реальный выбор, никогда — как тихую перезапись." },
    { q: "Почему «POST, а затем сразу рефетч» ломает read-your-own-writes и как сохранить итоговый refresh без нарушения?", a: "POST возвращается быстро (часто 202), потому что лишь ставит работу в очередь; фактическая запись случается, когда consumer обработает сообщение, где-то от 50–300мс до секунд спустя. Немедленный рефетч обгоняет этого consumer'а и почти всегда выигрывает, поэтому читает состояние до того, как запись приземлилась, и возвращает собственное изменение пользователя как отсутствующее — затем UI доверяет этому устаревшему чтению и стирает то, что он только что сделал. Фикс — перестать зависеть от гоночного чтения: применить изменение оптимистично как local echo, чтобы пользователь видел его сразу, и сверяться с сервером позже — инвалидировать после короткой задержки, ждать push (WebSocket/SSE), сигнализирующий, что consumer закончил, или sticky-читать с узла, гарантированно имеющего запись. Итоговый refresh ты сохраняешь; просто перестаёшь давать ему перезаписывать правду пользователя до того, как бэкенд догнал." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: асинхронная запись переносит consistency-окно на фронтенд; контракт apply-send-reconcile у optimistic UI покупает ~0мс воспринимаемой задержки, но без шага rollback это ложь; когда исходом владеет сервер, ты показываешь честное pending-состояние под защитой timeout; double-submit делается безопасным через idempotency key; конкурентные записи разрешаются осознанно (LWW для простых полей, CRDT для общего текста); а read-your-own-writes выживает за счёт локального эха вместо доверия гоночному рефетчу. Теперь, когда проектируешь следующую фичу над асинхронным бэкендом, ты можешь назвать consistency-окно, выбрать верный контракт для каждого действия и объяснить это любому, кто считает, что 202 означает «готово».
</Recap>
