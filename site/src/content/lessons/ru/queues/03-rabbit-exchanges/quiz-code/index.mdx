---
concepts:
- topic-pattern-matching
- binding-config
- prefetch-qos
- dead-letter-exchange
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://www.rabbitmq.com/docs/exchanges
status: ready
summary: "Читай реальные конфиги binding'ов RabbitMQ и сниппеты маршрутизации, предскажи, какие queue получат копию, и выбери фикс, который senior сделает первым."
title: 'RabbitMQ exchanges: чтение конфигов и маршрутизации'
track: queues
unit: 03-rabbit-exchanges
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баги маршрутизации читаются в таблице binding'ов и в вызове publish, а не в стектрейсе — его обычно нет. Читай каждый конфиг и сниппет, затем выбери поведение или фикс, который senior сделает первым.
</Hook>

<Goal lang="ru">
Отработай цикл, который ты запускаешь в каждом инциденте маршрутизации: прочитай тип exchange и binding'и, предскажи, какие queue получат копию, и потянись к верному фиксу прежде, чем гадать на продюсера.
</Goal>

## Сниппет 1 — topic-binding'и

```text
exchange: events  (type: topic)

bindings:
  Q_audit   <- "order.#"
  Q_eu      <- "order.*.eu"
  Q_created <- "order.created.*"

publish routing key: "order.created.eu"
```

<Quiz
  id="03-rabbit-exchanges-code-1"
  lessonSlug="03-rabbit-exchanges"
  lang="ru"
  question="С routing key order.created.eu, опубликованным в topic exchange events, какие queue получат копию?"
  choices={[
    { label: "Все три — order.# матчит любую глубину order.*, order.*.eu матчит трёхсловный ключ с eu в конце, а order.created.* матчит трёхсловный ключ, начинающийся order.created. Каждое совпадение даёт независимую копию.", correct: true },
    { label: "Только Q_audit, потому что # — самый широкий паттерн и выигрывает.", misconception: "Topic-binding'и не ранжируются и не исключают друг друга — нет правила «самый специфичный выигрывает». Каждый binding, чей паттерн совпал, независимо доставляет копию, поэтому все три queue получают сообщение." },
    { label: "Только Q_eu и Q_created, потому что order.# нужно больше слов, чем совпало.", misconception: "# матчит ноль или более слов, поэтому order.# полностью матчит order.created.eu. Q_audit тоже получает копию." },
    { label: "Никакие — routing key может удовлетворить только один паттерн binding'а.", misconception: "Routing key может удовлетворить сколько угодно binding'ов разом; именно этот fan-out в несколько queue и есть смысл topic exchange." },
  ]}
/>

## Сниппет 2 — конфиг prefetch

```python
channel.basic_qos(prefetch_count=1000)   # один канал, много consumer'ов
channel.basic_consume(queue="jobs", on_message_callback=handle)
# handle() делает медленную CPU-bound работу, ~200ms на сообщение
```

<Quiz
  id="03-rabbit-exchanges-code-2"
  lessonSlug="03-rabbit-exchanges"
  lang="ru"
  question="Несколько worker'ов потребляют jobs с этим конфигом, работа распределена неравномерно, а память брокера растёт. В чём дефект и фикс?"
  choices={[
    { label: "prefetch_count=1000 позволяет одному worker'у зарезервировать батч из 1000 неподтверждённых сообщений, голодая простаивающих соседей и удерживая эту память в брокере. Выставь низкий prefetch (начни с 1) и поднимай по метрикам.", correct: true },
    { label: "basic_consume надо заменить на basic_get, чтобы тянуть по требованию.", misconception: "Опрос через basic_get медленнее и всё равно не чинит распределение; push-модель в порядке. Дефект — раздутый prefetch, резервирующий большой неподтверждённый батч на одном consumer'е." },
    { label: "Queue надо объявить durable.", misconception: "Durability управляет тем, переживут ли сообщения рестарт брокера, а не тем, как in-flight сообщения распределяются между consumer'ами. Перекос и рост памяти идут от высокого prefetch." },
    { label: "Ничего — высокий prefetch всегда улучшает throughput.", misconception: "Высокий prefetch помогает, только когда consumer'ы быстры и однородны; с CPU-bound работой по 200ms он вызывает head-of-line голодание и безграничную неподтверждённую память. Низкий prefetch — senior-дефолт." },
  ]}
/>

## Сниппет 3 — конфиг dead-letter

```text
declare queue "work" with arguments:
  x-dead-letter-exchange: "dlx"
  x-message-ttl: 30000          # 30s

exchange "dlx" (type: fanout)  ->  queue "work.dead"

consumer on "work": on failure -> channel.basic_nack(requeue=True)
```

<Quiz
  id="03-rabbit-exchanges-code-3"
  lessonSlug="03-rabbit-exchanges"
  lang="ru"
  question="Poison message бесконечно зацикливается на queue work и никогда не доходит до work.dead, несмотря на конфиг dead-letter. Почему?"
  choices={[
    { label: "basic_nack(requeue=True) кладёт сообщение прямо обратно на work, поэтому оно никогда не отклоняется и не просрочивается так, чтобы запустить dead-lettering. Делай nack с requeue=False (или полагайся на TTL/лимит повторов), чтобы отклонённые сообщения шли в dlx.", correct: true },
    { label: "dlx должен быть topic exchange, а не fanout, чтобы принимать dead letters.", misconception: "Любой тип exchange может быть целью dead-letter; fanout в одну карантинную queue в порядке. Причина, почему ничего не приходит, в том, что requeue=True никогда не даёт сообщению стать dead letter." },
    { label: "x-message-ttl слишком большой, поэтому сообщение никогда не просрочивается.", misconception: "TTL в итоге dead-letter'нул бы нетронутое сообщение на 30s, но здесь consumer сразу переочередняет его, сбрасывая на активную queue до срабатывания TTL. Реальный дефект — флаг requeue." },
    { label: "Dead-lettering работает только на quorum queue.", misconception: "Dead-lettering работает на classic и quorum queue одинаково. Сообщение зацикливается, потому что requeue=True переотправляет его, вместо того чтобы отклонить в dlx." },
  ]}
/>

## Сниппет 4 — default exchange

```python
# exchange не объявлен; публикуем с пустым именем exchange
channel.basic_publish(exchange="", routing_key="reports", body=payload)
# queue с именем "reports" существует, но ТАКЖЕ привязана к topic exchange "events"
```

<Quiz
  id="03-rabbit-exchanges-code-4"
  lessonSlug="03-rabbit-exchanges"
  lang="ru"
  question="Куда уйдёт это сообщение при пустом имени exchange и routing key reports?"
  choices={[
    { label: "Прямо в queue с именем reports. Пустая строка-exchange — это default exchange, direct exchange, где каждая queue неявно привязана своим именем — поэтому routing_key=reports доставляет в queue reports.", correct: true },
    { label: "В topic exchange events, потому что это единственный объявленный exchange.", misconception: "Пустое имя exchange выбирает default (безымянный) direct exchange, а не events. На этой публикации сообщение не касается events." },
    { label: "Никуда — пустое имя exchange недопустимо.", misconception: "Пустая строка — валидный, всегда присутствующий exchange: default direct exchange. Это единственный случай, когда routing key ведёт себя как имя queue." },
    { label: "Во все queue, потому что пустое имя означает рассылку.", misconception: "Пустое имя — это default direct exchange, а не fanout. Он доставляет только в queue, чьё имя равно routing key — здесь reports." },
  ]}
/>

<Recap lang="ru">
Маршрутизация читается в таблице binding'ов, а не в стектрейсе: topic-паттерны матчатся независимо, и каждое совпадение даёт копию (* — ровно одно слово, # — ноль или более); слишком высокий prefetch резервирует большой неподтверждённый батч на одном consumer'е и голодает остальных; dead-lettering срабатывает только на reject/expire, поэтому nack-with-requeue зацикливает poison message навсегда; а пустая строка-exchange — это default direct exchange, где routing key и есть имя queue. Сначала прочитай тип и binding'и, предскажи копии, потом чини.
</Recap>
