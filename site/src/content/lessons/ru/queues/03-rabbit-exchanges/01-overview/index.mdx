---
concepts:
- exchange-types
- routing-keys-bindings
- smart-broker-push
- prefetch-qos
- dead-letter-exchange
- rabbitmq-vs-kafka
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://www.rabbitmq.com/docs/exchanges
- https://www.rabbitmq.com/docs/consumer-prefetch
- https://www.rabbitmq.com/docs/quorum-queues
- https://aws.amazon.com/compare/the-difference-between-rabbitmq-and-kafka/
spiral: []
status: ready
summary: Producer никогда не публикует в очередь — он публикует в exchange, а bindings
  и routing keys решают, какие очереди получат копию. Выберешь не тот тип exchange
  — сообщения либо разлетятся всюду, либо тихо исчезнут.
title: 'RabbitMQ exchanges: слой маршрутизации, который решает, кому достанется копия'
track: queues
unit: 03-rabbit-exchanges
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Платёжный сервис выходит в прод. Заказы идут, но очередь команды антифрода пуста — ноль сообщений, ни ошибок, ни исключений в логах. Producer спокойно публикует, брокер принимает. Через три дня замечают: новую очередь антифрода так и не *забиндили* к exchange. RabbitMQ маршрутизировал каждое сообщение в никуда и отбрасывал его беззвучно. Вызов publish всё это время возвращал успех, потому что по умолчанию exchange без подходящего binding тихо отбрасывает сообщение.
</Hook>

<Crux>В RabbitMQ producer публикует в exchange, а не в очередь; bindings и routing keys решают, какие очереди получат копию — или никакие.</Crux>

<Explanation>

## Ты публикуешь в exchange, а не в очередь

Первый сюрприз для всех, кто пришёл с моделью «очередь задач»: producer в RabbitMQ не может отправить сообщение в очередь. Он отправляет в *exchange*, и exchange решает — по своему типу, **routing key** сообщения и **bindings**, которые связывают его с очередями, — какие очереди (если вообще) получат копию. Binding — это правило: «exchange X, доставь в очередь Q, когда routing key совпадает с паттерном P». Одно сообщение может попасть в ноль очередей, в одну или в несколько; каждая совпавшая очередь получает свою независимую копию.

В этой непрямоте — весь смысл. Producer ничего не знает о консьюмерах, очередях и их количестве. Можно добавить нового консьюмера — ту самую очередь антифрода — создав очередь и забиндив её, без единого изменения в producer. Эта развязка и есть причина, почему RabbitMQ называют *smart broker*: логика маршрутизации живёт в брокере, а не в producer или консьюмере.

## Четыре типа exchange — это четыре стратегии маршрутизации

У каждого exchange есть тип, и тип — это алгоритм маршрутизации. Реально используешь четыре:

<div data-lesson-visual class="overflow-x-auto my-6">
| Тип | Маршрутизирует, когда… | Применять для |
| :--- | :--- | :--- |
| `direct` | routing key **в точности** равен binding key | point-to-point маршрутизация задач (`payments.refund` → один пул воркеров) |
| `fanout` | всегда — routing key игнорируется | broadcast: каждая забинженная очередь получает копию (инвалидация кэша, pub/sub) |
| `topic` | routing key совпадает с точечным паттерном с `*` / `#` | селективная подписка (`order.*.eu`, `logs.#`) |
| `headers` | атрибуты заголовков сообщения совпадают с аргументами binding | мульти-атрибутная маршрутизация, где плоского ключа мало |
</div>

**Direct** — точное совпадение: забиндь очередь `Q` с ключом `payments.refund`, и до неё дойдут только сообщения, чей routing key — ровно `payments.refund`. **Fanout** полностью игнорирует routing key и копирует каждое сообщение в каждую забинженную очередь — самый дешёвый и быстрый путь, идеально, когда N независимых консьюмеров хотят одно и то же событие. **Topic** — рабочая лошадка: routing keys — это слова через точку (`order.created.eu`), а паттерн binding использует `*` для совпадения ровно с одним словом и `#` для нуля или более. Так `order.*.eu` ловит `order.created.eu`, но не `order.created.us`; `logs.#` ловит всё под `logs`. **Headers** игнорирует routing key и матчит по мапе атрибутов заголовков, с `x-match` равным `all` или `any` — мощно, но медленнее и редко того стоит; topic key обычно выражает то же намерение дешевле.

<Inset kind="why" lang="ru">
Topic exchange с binding key `#` ведёт себя ровно как fanout (матчит всё), а topic без wildcards — как direct. Иногда «просто используют topic для всего». Работает, но fanout и direct проще для рассуждения и чуть быстрее, потому что пропускают сопоставление паттернов, — бери конкретный тип, когда намерение конкретно.
</Inset>

<FlowDiagram
  label="Один exchange веером раздаёт во многие очереди через bindings"
  hue="var(--d-network)"
  nodes={[
    { id: "pub", label: "Producer", sub: "routing key", col: 0, row: 1 },
    { id: "ex", label: "Exchange", sub: "тип + ключ", col: 1, row: 1 },
    { id: "bdirect", label: "Binding", sub: "direct =", col: 2, row: 0 },
    { id: "btopic", label: "Binding", sub: "topic *.#", col: 2, row: 1 },
    { id: "bfanout", label: "Binding", sub: "fanout *", col: 2, row: 2 },
    { id: "qpay", label: "Queue", sub: "payments", col: 3, row: 0 },
    { id: "qorders", label: "Queue", sub: "orders.eu", col: 3, row: 1 },
    { id: "qcache", label: "Queue", sub: "cache", col: 3, row: 2 },
    { id: "cpay", label: "Consumer", col: 4, row: 0 },
    { id: "corders", label: "Consumer", col: 4, row: 1 },
    { id: "ccache", label: "Consumer", col: 4, row: 2 },
  ]}
  edges={[
    { from: "pub", to: "ex", label: "publish" },
    { from: "ex", to: "bdirect", label: "payments.refund" },
    { from: "ex", to: "btopic", label: "order.*.eu" },
    { from: "ex", to: "bfanout", label: "(любой ключ)" },
    { from: "bdirect", to: "qpay" },
    { from: "btopic", to: "qorders" },
    { from: "bfanout", to: "qcache" },
    { from: "qpay", to: "cpay" },
    { from: "qorders", to: "corders" },
    { from: "qcache", to: "ccache" },
  ]}
  caption="Producer всегда отправляет в exchange, никогда в очередь. Exchange сверяет каждый binding с routing key — direct (точное совпадение), topic (паттерн), fanout (всегда) — и кладёт независимую копию в каждую совпавшую очередь. Один publish может раздаться веером многим консьюмерам; несвязанный ключ не дойдёт ни до одной."
/>

## Smart broker, dumb consumer — и почему это противоположность Kafka

Это и есть сеньорная рамка, которая решает, подходит ли RabbitMQ под твою задачу вообще. RabbitMQ **пушит** сообщения консьюмерам и удаляет каждое, как только его подтвердили (ack). Брокер умный (он маршрутизирует, отслеживает per-message ack, переотправляет при сбое); консьюмер относительно глупый (обрабатывает то, что ему дали, и подтверждает). Подтверждённое сообщение *исчезло* — нет лога, чтобы перемотать назад.

Kafka — зеркальное отражение: pull-модель **dumb broker, smart consumer**. Kafka — это append-only лог; брокер просто хранит упорядоченные записи, а консьюмеры сами отслеживают свой offset и тянут в своём темпе. При consume ничего не удаляется — сообщение лежит весь retention window, поэтому можно его replay-нуть, добавить новую consumer group, читающую с начала, или переобработать после фикса бага. Per-message ack-and-delete в RabbitMQ даёт тонкий контроль над workflow и сложную маршрутизацию; лог Kafka даёт replay и сырой throughput.

Разрыв в throughput большой и структурный. Классические очереди RabbitMQ достигают **десятков тысяч сообщений/сек на очередь** только в лёгкой, транзиентной конфигурации (без durability, без publisher confirms); включи durability, персистентность или quorum-очереди — и это падает существенно, часто до нескольких тысяч. Одна партиция Kafka держит порядка **1 миллиона сообщений/сек**, потому что батчит последовательные записи в лог. Обратная сторона: RabbitMQ доставляет с **субмиллисекундной** p50-latency (пушит по мере поступления), тогда как батчинг Kafka задаёт структурный пол примерно в **5–15 мс** p50. Так что выбор не «что лучше» — это *сложная per-message маршрутизация и низкая latency* (RabbitMQ) против *replayable высокопроизводительных стримов* (Kafka).

<TradeoffMatrix
  id="03-rabbit-exchanges-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Нужно доставлять события заказов в 3 сервиса сегодня, ожидаешь добавить ещё позже, и часть заботится только о EU-заказах. Выбери топологию exchange."
  options={[
    { name: "Topic exchange; producers публикуют с ключами вроде order.created.eu, консьюмеры биндят паттерны (order.#, order.*.eu)", summary: "Маршрутизация по паттерну.", correct: true, justification: "Topic создан ровно для этого: стабильный описательный routing key на событие и binding-паттерны на консьюмера. EU-сервис биндит order.*.eu; будущий аналитический сервис биндит order.# без единого изменения в producer. Producer остаётся в неведении, кто потребляет, — новые подписчики это ops-изменение (объявить очередь + binding), а не деплой кода. Это и есть развязка, ради которой существуют exchanges." },
    { name: "Fanout exchange; каждый сервис получает каждое сообщение и фильтрует в коде", summary: "Broadcast + фильтр.", justification: "Работает и это самый быстрый путь, но он толкает маршрутизацию в каждого консьюмера: EU-сервис получает все US-заказы только чтобы их выбросить, тратя bandwidth и CPU на масштабе, а любой баг фильтрации означает тихую потерю данных внутри консьюмера. Применяй fanout, когда консьюмеры действительно все хотят всё, а не когда им нужны селективные срезы." },
    { name: "Direct exchange с одним binding key на сервис", summary: "Точное совпадение.", justification: "Direct — точное совпадение, поэтому «все события заказов для EU» не выразить одним binding — пришлось бы перечислять каждый конкретный ключ (order.created.eu, order.cancelled.eu, …) и пере-биндить при появлении нового типа события. Нормально для фиксированной point-to-point маршрутизации; хрупко для развивающейся таксономии событий." },
    { name: "Публиковать прямо в очередь каждого сервиса из producer", summary: "Без exchange.", justification: "В RabbitMQ невозможно — producers всегда публикуют в exchange. Даже «default exchange» — это direct exchange, где routing key равен имени очереди. Такая модель снова сцепляет producer с полным списком очередей консьюмеров, убивая непрямоту." },
  ]}
/>

## Failure modes, которые поднимут тебя в 3 ночи

Три ошибки exchange-и-очередей вызывают большинство прод-инцидентов:

**Unbound exchange → тихие drop'ы.** Это и есть Hook. По умолчанию `basic.publish` не сообщает producer, была ли сообщение смаршрутизирована. Если ни один binding не совпал, брокер отбрасывает его, а publish всё равно возвращает успех. Фикс — флаг **mandatory**: выстави его, и несмаршрутизируемое сообщение вернётся producer через `basic.return`, а не исчезнет, — либо заведи catch-all очередь с `#`, чтобы ничего не терялось незаметно. Всегда алертуй на счётчик unroutable.

**Prefetch (QoS) starvation и взрыв памяти.** RabbitMQ пушит сообщения, а prefetch count (`basic.qos`) ограничивает, сколько *неподтверждённых* сообщений консьюмер может держать одновременно. Поставь слишком высоко — скажем, 1000 — и один медленный консьюмер хватает огромный backlog, пока быстрые простаивают, голодая; хуже того, эти unacked-сообщения остаются резидентно в памяти брокера, и если обработка застопорится, счётчик unacked растёт безгранично, пока узел не упрётся в memory watermark и не заблокирует всех publisher'ов. Сеньорный дефолт — *низкий* prefetch (начни с 1, тюнь вверх по метрикам), чтобы работа распределялась честно, а память оставалась ограниченной.

**Poison message без DLX → бесконечный цикл переотправки.** Консьюмер, который делает nack-with-requeue на сообщении, которое он никогда не сможет обработать (битый payload, баг, на котором он всегда падает), получает то же сообщение обратно, навсегда, пиня воркер. Фикс — **dead-letter exchange (DLX)**: настрой очередь так, чтобы отклонённые или истёкшие сообщения dead-letter'ились, маршрутизируясь в отдельный exchange и карантинную очередь, которую можно осмотреть и переобработать. Без DLX одно poison-сообщение может застопорить весь пайплайн.

## Durability и HA: classic, mirrored, quorum

Сообщение настолько в безопасности, насколько надёжна держащая его очередь. Классические очереди живут на одном узле; умрёт он — уйдут и сообщения очереди. Старым ответом были **classic mirrored queues** (реплики на других узлах), но их задепрекейтили и **удалили в RabbitMQ 4.0** — они могли подтвердить сообщение до того, как оно безопасно реплицировано, рискуя потерей при failover. Современный ответ — **quorum queues**: реплицируемая очередь на Raft, которая подтверждает только когда большинство узлов имеет сообщение. Трейдофф конкретен — репликация на 3 узла держит примерно **30K сообщений/сек** для сообщений в 1KB, ниже, чем нереплицируемая классическая очередь, в обмен на отсутствие потери данных, когда узел падает. Для всего, что нельзя позволить себе потерять, это та сделка, на которую идёт сеньор.

<Quiz
  id="03-rabbit-exchanges-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Producer публикует в topic exchange, но очередь только что добавленного консьюмера не получает ничего, при этом нигде нет ошибок. Какова наиболее вероятная причина?"
  choices={[
    { label: "Очередь не забинжена к exchange с совпадающим паттерном, поэтому брокер маршрутизирует сообщение в никуда и тихо его отбрасывает", correct: true },
    { label: "Producer публикует в очередь, а не в exchange", misconception: "Producers в RabbitMQ могут публиковать только в exchanges; нельзя опубликовать прямо в очередь, так что причина не в этом." },
    { label: "Retention в стиле Kafka истёк, и сообщения пропали до того, как консьюмер прочитал", misconception: "У RabbitMQ нет replayable-лога; сообщения не хранятся для поздних читателей. Маршрутизация решается во время publish биндингами, а не retention window." },
    { label: "Prefetch count консьюмера слишком низкий", misconception: "Низкий prefetch замедляет throughput, но всё равно доставляет сообщения; пустая очередь без ошибок указывает на отсутствующий или несовпадающий binding, а не на QoS." },
  ]}
/>

<Quiz
  id="03-rabbit-exchanges-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Нужно, чтобы поздно подключившиеся консьюмеры могли replay-нуть события последней недели с начала. Какая система подходит и почему?"
  choices={[
    { label: "Kafka — это append-only лог с retention, поэтому новая consumer group может читать с offset ноль и делать replay; при consume ничего не удаляется", correct: true },
    { label: "RabbitMQ с fanout exchange", misconception: "Fanout копирует живые сообщения в текущие забинженные очереди, но RabbitMQ удаляет каждое сообщение по ack — нет лога для перемотки, поэтому добавленный позже консьюмер не может replay-нуть прошлые события." },
    { label: "RabbitMQ с высоким prefetch count", misconception: "Prefetch управляет in-flight неподтверждённой доставкой, а не retention или replay. Подтверждённые сообщения в RabbitMQ исчезают независимо от prefetch." },
    { label: "RabbitMQ quorum queues", misconception: "Quorum queues добавляют репликацию ради durability при отказах узлов, а не replay — подтверждённое сообщение всё равно удаляется и не может быть перечитано новым консьюмером." },
  ]}
/>

<DragOrder
  id="03-rabbit-exchanges-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь путь, который сообщение проходит от producer до консьюмера в RabbitMQ:"
  items={[
    { id: "a", label: "Producer публикует сообщение с routing key в именованный exchange" },
    { id: "b", label: "Exchange применяет правило своего типа к routing key против своих bindings" },
    { id: "c", label: "Каждый совпавший binding доставляет независимую копию в свою очередь" },
    { id: "d", label: "Брокер пушит сообщение консьюмеру, в пределах лимита prefetch (QoS)" },
    { id: "e", label: "Консьюмер подтверждает (ack); брокер удаляет эту копию из очереди" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Producer публикует в exchange; тип + routing key + bindings решают, какие очереди получат копию. Smart-broker push с ack-and-delete, в отличие от replayable-лога Kafka. Берегись тихих drop'ов и prefetch starvation.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-rabbit-exchanges-retrieval"
  lang="ru"
  questions={[
    { q: "Коллега уверен, что RabbitMQ и Kafka — взаимозаменяемые очереди сообщений. Объясни архитектурное различие и когда что подходит.", a: "Они стоят на противоположных концах одного спектра. RabbitMQ — это smart broker с dumb consumers: producers публикуют в exchange, брокер маршрутизирует через bindings и routing keys, пушит каждое сообщение консьюмеру, отслеживает per-message ack и удаляет сообщение по подтверждению. Лога для перемотки нет — потреблённое сообщение исчезло. Это делает его отличным в сложной маршрутизации (direct/fanout/topic/headers), per-message нагрузках и низкой субмиллисекундной latency, но он упирается примерно в десятки тысяч сообщений/сек на очередь. Kafka — обратное: dumb broker со smart consumers. Это append-only лог; брокер просто хранит упорядоченные записи, а консьюмеры тянут на своём offset. При consume ничего не удаляется, поэтому можно делать replay, добавить новую consumer group, читающую с начала, или переобработать после фикса бага, а одна партиция держит около миллиона сообщений/сек — ценой пола latency в ~5-15 мс из-за батчинга и отсутствия встроенной маршрутизации по содержимому. Бери RabbitMQ, когда нужны богатая маршрутизация, per-message ack и распределение задач; бери Kafka, когда нужны replayable высокопроизводительные стримы событий." },
    { q: "Почему высокий prefetch count вызывает и несправедливость, и аварии, и какой безопасный дефолт?", a: "Prefetch (basic.qos) ограничивает, сколько неподтверждённых сообщений брокер запушит одному консьюмеру до того, как тот обязан сделать ack. RabbitMQ push-based, поэтому при высоком prefetch вроде 1000 брокер выдаёт медленному консьюмеру огромный батч заранее; эти сообщения теперь зарезервированы и in-flight, так что простаивающие быстрые консьюмеры не могут их взять — это и есть несправедливость, один медленный воркер захапал backlog, пока другие голодают. Аварийный угол — память: каждое unacked-сообщение остаётся резидентно в памяти брокера. Если обработка застопорится или консьюмер медленный, счётчик unacked растёт безгранично, пока узел не упрётся в memory high-watermark, и тогда RabbitMQ применяет back-pressure и блокирует всех publisher'ов — полный стоп пайплайна. Безопасный дефолт — низкий prefetch, часто 1, повышаемый только с метриками в руках: он честно распределяет работу между консьюмерами и держит unacked-память ограниченной, разменивая немного throughput на справедливость и стабильность." },
  ]}
/>

<Recap lang="ru">
Определяющий ход RabbitMQ в том, что producers публикуют в exchange, а не в очередь, и тип exchange плюс routing key сообщения плюс bindings решают, какие очереди получат независимую копию — возможно, никакие. Direct маршрутизирует по точному совпадению ключа, fanout вещает в каждую забинженную очередь, topic матчит точечные паттерны с `*` и `#`, а headers матчит атрибуты заголовков. Это модель smart-broker, dumb-consumer с push: брокер маршрутизирует, пушит, отслеживает per-message ack и удаляет по consume — противоположность pull-логу Kafka с dumb-broker, smart-consumer, который хранит сообщения для replay. Размен — богатство маршрутизации и субмиллисекундная latency при десятках тысяч сообщений/сек на очередь (RabbitMQ) против replay и примерно миллиона сообщений/сек на партицию (Kafka). Прод-сбои предсказуемы: unbound exchange тихо отбрасывает сообщения, пока ты не выставишь флаг mandatory или catch-all binding; слишком высокий prefetch голодит быстрых консьюмеров и растит unacked-память, пока publisher'ы не заблокируются; poison-сообщение без dead-letter exchange циклится вечно. А для durability classic mirrored queues исчезли начиная с RabbitMQ 4.0 — quorum queues реплицируют через Raft и подтверждают только по большинству, стоя throughput (около 30K сообщений/сек реплицировано на три узла), чтобы не терять данные при failover. Сперва выбери правильный тип exchange и bindings; всё ниже по потоку зависит от того, что маршрутизация верна.
</Recap>
