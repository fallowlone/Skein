---
concepts:
- exchange-types
- routing-key-binding
- smart-broker-push
- prefetch-qos
- dead-letter-exchange
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.rabbitmq.com/docs/exchanges
status: ready
summary: "Промпты на свободное припоминание по всему юниту. Сначала ответь своими словами, потом раскрой модельный ответ и сравни."
title: 'RabbitMQ exchanges: тест на припоминание'
track: queues
unit: 03-rabbit-exchanges
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание лучше перечитывания. На каждый промпт скажи или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет модель маршрутизации.
</Hook>

<Goal lang="ru">
Реконструируй ключевые механизмы юнита — четыре типа exchange, как binding и routing key решают доставку, контракт smart-broker push и режимы сбоев — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:load
  id="03-rabbit-exchanges-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему продюсер RabbitMQ никогда не публикует напрямую в queue и что даёт эта косвенность?", a: "Продюсер всегда публикует в exchange, никогда в queue. Затем exchange по своему типу, routing key сообщения и binding'ам, связывающим его с queue, решает, какие queue — если вообще какие — получат независимую копию. В этой косвенности весь смысл: продюсер ничего не знает о consumer'ах, queue или их количестве. Можно добавить нового consumer'а, объявив queue и привязав её, без единого изменения продюсера. Эта развязка и есть причина, почему RabbitMQ называют smart broker — логика маршрутизации живёт в брокере, а не в продюсере или consumer'е. Даже так называемый default exchange — это direct exchange, где routing key равен имени queue, поэтому публикация в queue на деле просто публикация в этот exchange." },
    { q: "Опиши четыре типа exchange и правило маршрутизации каждого.", a: "Тип — это алгоритм маршрутизации. Direct маршрутизирует, когда routing key точно равен binding key — point-to-point маршрутизация работы. Fanout игнорирует routing key и копирует каждое сообщение в каждую привязанную queue — самый дешёвый и быстрый путь рассылки для инвалидации кэша или pub/sub. Topic трактует routing key как слова через точку и матчит точечные паттерны, где * матчит ровно одно слово, а # — ноль или более, поэтому order.*.eu ловит order.created.eu, но не order.created.us, а logs.# ловит всё под logs — это рабочая лошадка для выборочной подписки. Headers игнорирует routing key и матчит по карте атрибутов-заголовков с x-match all или any — мощно, но медленнее и редко того стоит, когда topic-ключ выражает тот же смысл дешевле." },
    { q: "Объясни smart-broker push-модель и чем она отличается от pull-лога Kafka.", a: "RabbitMQ пушит сообщения consumer'ам и удаляет каждое, как только оно подтверждено. Брокер умный — он маршрутизирует, отслеживает per-message ack и переотправляет при сбое — а consumer относительно глуп, обрабатывает то, что ему передали, и подтверждает. Потреблённое и подтверждённое сообщение исчезло; лога для перемотки нет. Kafka — зеркальная противоположность: dumb broker со smart consumer'ами, append-only лог, где consumer'ы отслеживают свой offset и тянут в своём темпе, и ничего не удаляется при потреблении. Это позволяет consumer group в Kafka воспроизвести с начала или переобработать после фикса бага. Per-message ack-and-delete в RabbitMQ даёт мелкозернистый контроль воркфлоу и богатую маршрутизацию; лог Kafka даёт replay и сырой throughput." },
    { q: "Почему слишком высокий prefetch count вызывает и несправедливость, и аварию, и какой безопасный дефолт?", a: "Prefetch (basic.qos) ограничивает, сколько неподтверждённых сообщений брокер пушит одному consumer'у, прежде чем тот обязан подтвердить. RabbitMQ push-based, поэтому высокий prefetch вроде 1000 выдаёт медленному consumer'у огромный зарезервированный батч сразу; эти сообщения теперь in-flight, поэтому быстрые простаивающие consumer'ы не могут их взять — это несправедливость. Аварийный угол — память: каждое неподтверждённое сообщение остаётся резидентным в памяти брокера, и если обработка застопорится, счётчик неподтверждённых растёт безгранично, пока узел не достигнет memory high-watermark, и тогда RabbitMQ применяет back-pressure и блокирует всех продюсеров — полная остановка конвейера. Безопасный дефолт — низкий prefetch, часто 1, поднимаемый только с метриками на руках: он справедливо распределяет работу и держит неподтверждённую память ограниченной." },
    { q: "Что такое dead-letter exchange и какой режим сбоя он решает?", a: "Dead-letter exchange (DLX) — отдельный exchange, в который queue маршрутизирует сообщения, когда они отклонены (nack без requeue), просрочены по TTL или превышают лимит длины. Он решает режим poison message: consumer, делающий nack-with-requeue на payload, который никогда не сможет обработать, получает то же сообщение обратно навсегда, застряв одним worker'ом в бесконечном цикле переотправки и потенциально остановив весь конвейер. Настройка queue на dead-letter при отклонении маршрутизирует это сообщение наружу в карантинную queue, которую можно разобрать, поднять алерт и переобработать, чтобы одно плохое сообщение покинуло горячий путь вместо зацикливания. Без DLX одно poison message может остановить весь конвейер." },
    { q: "Как защититься от того, что exchange тихо отбрасывает немаршрутизируемые сообщения?", a: "По умолчанию basic.publish не сообщает продюсеру, было ли сообщение реально смаршрутизировано; если ни один binding не совпал, брокер отбрасывает его, а publish всё равно возвращает успех — классический тихий сбой. Две защиты: выставить флаг mandatory, чтобы немаршрутизируемое сообщение вернулось продюсеру через basic.return вместо исчезновения, давая обработать его или поднять алерт; или привязать catch-all queue (topic # binding или alternate-exchange на основном), чтобы ничего не терялось незаметно. В любом случае стоит алертить на счётчик немаршрутизируемых сообщений в мониторинге, потому что отсутствующий или опечатанный binding иначе невидим — продюсер продолжает рапортовать успех, пока сообщения уходят в никуда." },
  ]}
/>

<Recap lang="ru">
Если ты смог реконструировать каждый ответ по памяти, ты держишь хребет юнита: продюсеры публикуют в exchange, никогда в queue, а тип плюс routing key плюс binding решают, какие queue получат независимую копию. Direct — точное совпадение, fanout рассылает, topic матчит точечные * / # паттерны, а headers матчит атрибуты. Smart-broker push-модель ack-and-delete — противоположность воспроизводимому pull-логу Kafka. И production-сбои предсказуемы: непривязанный exchange тихо отбрасывает без флага mandatory, слишком высокий prefetch голодает consumer'ов и растит неподтверждённую память, а poison message без dead-letter exchange зацикливается навсегда.
</Recap>
