---
concepts:
- exchange-types
- routing-key-binding
- smart-broker-push
- prefetch-qos
- dead-letter-exchange
- rabbitmq-vs-kafka
estMin: 12
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.rabbitmq.com/docs/exchanges
status: ready
summary: "Тест с выбором на синтез по всему юниту — маршрутизация direct/fanout/topic/headers, binding key, smart-broker push, prefetch и сбои dead-letter."
title: 'RabbitMQ exchanges: тест с выбором ответа'
track: queues
unit: 03-rabbit-exchanges
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает решение по топологии, которое ты принимаешь на реальном ревью дизайна — не определение для заучивания, а компромисс маршрутизации, который надо взвесить под production-нагрузкой.
</Hook>

<Goal lang="ru">
Убедись, что связываешь тип exchange, binding key, smart-broker push-модель и production-сбои — тот синтез, к которому вёл урок.
</Goal>

<Quiz
  id="03-rabbit-exchanges-choice-1"
  lessonSlug="03-rabbit-exchanges"
  lang="ru"
  question="Надо доставлять события заказов трём сервисам сегодня, ожидаешь новых позже, и один сервис интересуют только EU-заказы. Какой тип exchange подходит и почему?"
  choices={[
    { label: "topic — продюсеры публикуют ключи вида order.created.eu; каждый consumer привязывает паттерн (order.#, order.*.eu), а новый подписчик — это ops-изменение, а не деплой продюсера.", correct: true },
    { label: "fanout — рассылать всё и фильтровать в коде каждого сервиса.", misconception: "Fanout копирует каждое сообщение в каждую привязанную queue, поэтому EU-сервис получает все US-заказы лишь чтобы их отбросить — лишний трафик и риск тихой потери данных при баге фильтра. Используй fanout, только когда всем consumer'ам действительно нужно всё." },
    { label: "direct — один binding key на сервис.", misconception: "Direct — точное совпадение, поэтому «все EU-события заказов» не выражаются одним binding; пришлось бы перечислять каждый конкретный ключ и пере-биндить при каждом новом типе события. Хорошо для фиксированной point-to-point маршрутизации, хрупко для развивающейся таксономии событий." },
    { label: "headers — матчить по заголовку region с x-match=any.", misconception: "Headers это выразит, но медленнее (матчит карту атрибутов вместо routing key) и редко того стоит, когда точечный topic-ключ вроде order.created.eu несёт тот же смысл дешевле." },
  ]}
/>

<Quiz
  id="03-rabbit-exchanges-choice-2"
  lessonSlug="03-rabbit-exchanges"
  lang="ru"
  question="Queue нового consumer'а ничего не получает — ни ошибок, ни исключений, а вызовы publish продюсера возвращают успех. Самая вероятная причина?"
  choices={[
    { label: "Queue не привязана к exchange совпадающим ключом, поэтому брокер маршрутизирует сообщение в никуда и тихо отбрасывает; по умолчанию basic.publish не сообщает о немаршрутизируемости.", correct: true },
    { label: "Kafka-style retention истёк до того, как consumer прочитал сообщения.", misconception: "У RabbitMQ нет воспроизводимого лога — маршрутизация решается во время публикации через binding, а не окном retention. Поздний consumer не может «пропустить» удержанные сообщения, потому что их не существует." },
    { label: "Prefetch count consumer'а слишком низкий, поэтому доставка застряла.", misconception: "Низкий prefetch замедляет throughput, но всё равно доставляет; пустая queue без ошибок указывает на отсутствующий или несовпадающий binding, а не на QoS." },
    { label: "Продюсер опубликовал в queue вместо exchange.", misconception: "Продюсеры RabbitMQ могут публиковать только в exchange — нельзя опубликовать в queue напрямую — поэтому это не выразимый режим сбоя." },
  ]}
/>

<Quiz
  id="03-rabbit-exchanges-choice-3"
  lessonSlug="03-rabbit-exchanges"
  lang="ru"
  question="Topic-binding использует ключ order.*.eu. Какой routing key совпадёт?"
  choices={[
    { label: "order.created.eu — * матчит ровно одно слово, поэтому трёхсловный ключ с eu в конце совпадает.", correct: true },
    { label: "order.eu — * всё равно совпадает, раз eu присутствует.", misconception: "* матчит ровно одно слово и обязателен; в order.eu всего два слова, поэтому средняя позиция, которую требует *, отсутствует, и совпадения нет. Используй # для матча нуля-или-более слов." },
    { label: "order.created.payment.eu — eu финальное слово.", misconception: "* матчит ровно одно слово, а не несколько; order.*.eu требует ровно трёх слов, поэтому четырёхсловный ключ не совпадает. Понадобился бы order.#.eu." },
    { label: "order.created.us — префикс order.created совпадает.", misconception: "Topic-binding должен совпасть в каждой позиции; финальное слово eu не равно us, поэтому весь ключ не совпадает независимо от совпадающего префикса." },
  ]}
/>

<Quiz
  id="03-rabbit-exchanges-choice-4"
  lessonSlug="03-rabbit-exchanges"
  lang="ru"
  question="Коллега говорит, что RabbitMQ и Kafka — взаимозаменяемые очереди, поэтому поздно подключившийся consumer может воспроизвести события за неделю из любой. Где ошибка?"
  choices={[
    { label: "RabbitMQ — smart broker с ack-and-delete: потреблённое и подтверждённое сообщение исчезло, лога для перемотки нет. Только append-only лог Kafka удерживает записи, чтобы новая consumer group читала с offset ноль.", correct: true },
    { label: "RabbitMQ может воспроизвести, если использовать fanout exchange.", misconception: "Fanout копирует живые сообщения в текущие привязанные queue во время публикации; он ничего не удерживает. Consumer, добавленный позже, всё равно не увидит прошлые события, так как RabbitMQ удалил каждое по ack." },
    { label: "RabbitMQ может воспроизвести, если поднять prefetch count.", misconception: "Prefetch управляет in-flight неподтверждённой доставкой, а не retention или replay. Подтверждённые сообщения удаляются независимо от prefetch." },
    { label: "RabbitMQ может воспроизвести с quorum queue.", misconception: "Quorum queue добавляют Raft-репликацию для долговечности при отказах узлов, а не replay — подтверждённое сообщение всё равно удаляется и не может быть перечитано новым consumer'ом." },
  ]}
/>

<Quiz
  id="03-rabbit-exchanges-choice-5"
  lessonSlug="03-rabbit-exchanges"
  lang="ru"
  question="Один consumer держит огромный бэклог неподтверждённых сообщений, пока быстрые consumer'ы простаивают, а память брокера ползёт к high-watermark. Что неверно настроено и какой фикс?"
  choices={[
    { label: "Prefetch count (basic.qos) слишком высокий, поэтому брокер выдаёт одному медленному consumer'у большой зарезервированный батч — голодая остальных и удерживая неподтверждённые сообщения в памяти брокера. Снизь prefetch (начни с 1) и поднимай по метрикам.", correct: true },
    { label: "Тип exchange надо сменить с topic на direct.", misconception: "Тип exchange управляет тем, какие queue получают копию, а не сколько in-flight сообщений держит один consumer. Голодание плюс рост неподтверждённой памяти — проблема QoS/prefetch, а не маршрутизации." },
    { label: "Queue нужен dead-letter exchange.", misconception: "Dead-letter exchange карантинит отклонённые или просроченные сообщения; он ничего не делает с медленным consumer'ом, захватившим большой prefetch-батч. Фикс — снизить prefetch, а не dead-letter." },
    { label: "Publisher confirms отключены, поэтому сообщения накапливаются.", misconception: "Publisher confirms сообщают продюсеру, что сообщение сохранено; они не управляют in-flight лимитами на стороне consumer'а. Бэклог и рост памяти идут от слишком высокого prefetch." },
  ]}
/>

<Quiz
  id="03-rabbit-exchanges-choice-6"
  lessonSlug="03-rabbit-exchanges"
  lang="ru"
  question="Consumer делает nack-with-requeue на некорректном payload, который никогда не сможет обработать, и один worker теперь застрял, бесконечно переполучая то же сообщение. Какой устойчивый фикс?"
  choices={[
    { label: "Настроить dead-letter exchange: queue dead-letter'ит отклонённые или просроченные сообщения, маршрутизируя их в карантинную queue для разбора и переобработки, чтобы poison message покинул горячий путь вместо зацикливания.", correct: true },
    { label: "Переключить queue на fanout exchange, чтобы сообщение распределилось.", misconception: "Fanout копирует то же poison message в больше queue — он множит проблему, а не убирает сообщение из цикла переотправки. Фикс — dead-letter наружу, а не рассылка." },
    { label: "Поднять prefetch count, чтобы worker мог пройти дальше.", misconception: "Больший prefetch позволяет worker'у держать больше сообщений, но poison message всё равно переочередняется и переотправляется; он не уходит. Только dead-letter убирает его из цикла." },
    { label: "Включить флаг mandatory на публикации.", misconception: "Флаг mandatory возвращает немаршрутизируемые сообщения продюсеру во время публикации; он ничего не делает с сообщением, которое смаршрутизировалось верно, но повторно падает в consumer'е. Этому случаю нужен dead-letter exchange." },
  ]}
/>

<Recap lang="ru">
Сквозная линия — одно дерево решений маршрутизации: тип exchange это алгоритм (direct — точное совпадение, fanout — рассылка, topic — точечный паттерн с * и #, headers — матч атрибутов), binding плюс routing key решают, какие queue получат независимую копию, а smart-broker push-модель означает, что подтверждённое сообщение удаляется — никакого replay, в отличие от Kafka. Production-сбои на слой ниже: непривязанный exchange тихо отбрасывает без флага mandatory, слишком высокий prefetch голодает быстрых consumer'ов и растит неподтверждённую память, а poison message без dead-letter exchange зацикливается навсегда. Сначала верно выбери тип exchange и binding; всё ниже зависит от корректной маршрутизации.
</Recap>
