---
concepts:
- transactional-outbox
- change-data-capture
- partition-key-ordering
- idempotent-consumer
- dead-letter-queue
- eventual-consistency-ux
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://medium.com/airbnb-engineering/avoiding-double-payments-in-a-distributed-payments-system-2981f6b070bb
- https://www.uber.com/us/en/blog/reliable-reprocessing/
- https://docs.stripe.com/api/idempotent_requests
- https://stripe.com/blog/idempotency
spiral: []
status: ready
summary: Проводим одну запись заказа через outbox, CDC, партиционированные Kafka-топики,
  идемпотентные consumer groups, dead-letter queue и eventually-consistent UI — и
  видим, почему идемпотентность — единственный инвариант, который держит всё вместе.
title: 'Сквозной pipeline заказов: где живёт каждая гарантия доставки'
track: queues
unit: 08-putting-it-together
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import guaranteeLadder from "./infographics/guarantee-ladder.svg?raw";

<Hook>
Клиента списали дважды за один заказ. Трейс рассказывает историю: payment-консьюмер обработал событие `order.placed`, вызвал шлюз, успешно — а потом под убили посреди коммита, до того как сдвинулся Kafka-офсет. Группа сделала ребаланс, новый консьюмер взял тот же офсет и переиграл событие. Два списания, один заказ. У вызова шлюза не было idempotency-ключа, так что он не мог понять, что это то же самое списание. Pipeline был корректен везде, кроме одного места, которое и было важным.
</Hook>

<Crux>Выбери гарантию доставки на каждый хоп, затем сделай каждый консьюмер идемпотентным — этот инвариант и делает весь pipeline безопасным.</Crux>

<Explanation>

## Одна запись, шесть хопов и одна гарантия, что их связывает

За следующие десять минут ты увидишь, где именно произошло двойное списание, почему никакой глобальный переключатель «exactly-once» не мог его предотвратить и какой один инвариант на самом деле закрывает брешь.

Проследи один заказ через event-driven pipeline — и пересечёшь шесть границ, у каждой свой режим отказа. Запись происходит один раз. Всё после — это эстафета через сети и процессы, каждый из которых может ретраить, падать, переупорядочивать или застревать. Идея капстоуна в том, что у тебя нет одной глобальной ручки «exactly-once». Ты выбираешь гарантию на каждый хоп, а потом защищаешь швы одним инвариантом — идемпотентностью — потому что каждый честный хоп в этой цепочке — это *at-least-once*.

<Infographic
  svg={guaranteeLadder}
  label="Лестница из шести шагов pipeline заказов: хоп 1, запись заказа, атомарен exactly-once через outbox-транзакцию; хопы 2–6 — публикация, маршрутизация, обработка, карантин и отображение — каждый at-least-once или eventual, поэтому идемпотентность у консьюмера и есть инвариант, связывающий их."
  caption="Только запись — exactly-once. Каждый хоп после неё — at-least-once, поэтому именно идемпотентный консьюмер делает переигрыши безопасными."
  hue="var(--d-infra)"
/>

Поток:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Хоп</th>
        <th class="border border-border px-3 py-2 text-left">Механизм</th>
        <th class="border border-border px-3 py-2 text-left">Какую гарантию даёт</th>
        <th class="border border-border px-3 py-2 text-left">Что её защищает</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">1. Запись заказа</td>
        <td class="border border-border px-3 py-2">Строка в БД + строка outbox в одной txn</td>
        <td class="border border-border px-3 py-2">Атомарность: событие есть тогда и только тогда, когда есть заказ</td>
        <td class="border border-border px-3 py-2">ACID-транзакция</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">2. Публикация</td>
        <td class="border border-border px-3 py-2">CDC-relay читает WAL → Kafka</td>
        <td class="border border-border px-3 py-2">At-least-once в топик</td>
        <td class="border border-border px-3 py-2">Возобновление с log-офсета</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">3. Маршрутизация</td>
        <td class="border border-border px-3 py-2">Топик партиционирован по id заказа</td>
        <td class="border border-border px-3 py-2">Порядок в пределах ключа</td>
        <td class="border border-border px-3 py-2">Стабильное назначение партиций</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">4. Обработка</td>
        <td class="border border-border px-3 py-2">Consumer group: payment / inventory / notify</td>
        <td class="border border-border px-3 py-2">Доставка at-least-once</td>
        <td class="border border-border px-3 py-2">Идемпотентный handler + дедуп</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">5. Карантин</td>
        <td class="border border-border px-3 py-2">Ретраи с backoff → DLQ</td>
        <td class="border border-border px-3 py-2">Ядовитые сообщения не блокируют</td>
        <td class="border border-border px-3 py-2">Бюджет ретраев + алерт на DLQ</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">6. Отображение</td>
        <td class="border border-border px-3 py-2">UI показывает оптимистично + pending</td>
        <td class="border border-border px-3 py-2">Eventual consistency, честно</td>
        <td class="border border-border px-3 py-2">Состояние pending + сверка</td>
      </tr>
    </tbody>
  </table>
</div>

## Хоп 1–2: запись атомарна, публикация — нет

Первое решение — знаменитая проблема двойной записи (dual-write): надо сохранить заказ *и* отправить `order.placed`. Если пишешь в БД, а потом публикуешь в Kafka двумя отдельными вызовами, падение между ними либо теряет событие (заказ есть, никто не знает), либо отправляет фантом (событие ушло, заказ откатился). Транзакционный **outbox** убирает зазор: в одной локальной транзакции БД ты пишешь и строку заказа, *и* строку `outbox`. Либо коммитятся обе, либо ни одной. Теперь событие существует тогда и только тогда, когда существует заказ.

Публикация — отдельная забота, и она честно at-least-once. **CDC** (Change Data Capture — захват изменений данных)-relay (Debezium, читающий WAL (Write-Ahead Log — журнал предзаписи) Postgres — каноничная схема) читает закоммиченные строки outbox из лога и пушит их в Kafka. Его суперсила — восстанавливаемость: он отслеживает свой log-офсет, поэтому при рестарте возобновляется с точной позиции — ноль поллинга, почти ноль нагрузки на БД, потому что он читает лог, а не таблицу. Но «возобновиться с офсета» означает, что он может переотправить строку, которую опубликовал прямо перед падением. Это нормально, и в этом весь смысл: ты не гонишься за exactly-once на проводе, ты перекладываешь ответственность за дедуп на консьюмер, где ей и место.

<Inset kind="why" lang="ru">
Сеньорская команда начинает с *поллинг-публикатора* (воркер, который `SELECT`-ит неотправленные строки outbox и помечает их отправленными), прежде чем тянуться к CDC. Его тривиально собрать, отладить и эксплуатировать, и он корректен. На log-tailing CDC переходят только когда задержка поллинга или нагрузка от записи маркера становятся реальной проблемой — а не по умолчанию. Паттерн тот же; меняется только relay.
</Inset>

## Хоп 3: партиционируй по ключу, который владеет заказом

Спроси себя: что пойдёт не так, если `payment.authorized` придёт раньше `order.placed` для одного и того же заказа? Ответ на этот вопрос — и есть причина, по которой партиционирование по ключу — это не трюк для производительности, а гарантия корректности.

Kafka гарантирует порядок только *внутри партиции*, а партицию выбирает ключ записи. Ключуй события заказа по `order_id` (или `customer_id`, когда нужно сериализовать события клиента) — и каждое событие этого заказа попадёт в ту же партицию, обработается в порядке коммита одним консьюмером. `payment.authorized` никогда не обгонит `order.placed` для одного заказа. Порядок между заказами не гарантируется и не нужен — именно это и позволяет масштабироваться добавлением партиций.

Ловушка операционная: дефолтный партиционер — это `hash(key) % partitionCount`. В тот момент, когда ты *добавляешь* партиции для масштабирования, модуль меняется, тот же ключ маппится в другую партицию, и порядок для этого ключа ломается на шве. Сеньорские команды переоверпровиживают партиции заранее или мигрируют ключи осознанно, а не бампают число партиций в горячую пятницу. Число партиций — дверь в одну сторону для порядка по ключу.

## Хоп 4–5: at-least-once — это обещание доставить дубликаты

Consumer group — место, где живёт несущее правило. Чтобы получить at-least-once, ты **коммитишь офсет только после успеха handler** — обработай, потом коммить. Цена — дубликаты: если под падает после обработки, но до коммита (ровно как в Hook), следующий консьюмер переигрывает этот офсет. At-least-once — не баг, который надо чинить; это контракт, который говорит «я доставлю твоё сообщение, возможно, не один раз». Единственный корректный ответ — **идемпотентный** handler.

Идемпотентность означает, что обработка одного события дважды даёт тот же эффект, что и один раз. Механизм — дедуп-ключ (id события или бизнесовый idempotency-ключ), записанный в дедуп-таблицу с уникальным ограничением, в той же транзакции, что и побочный эффект. Переотправка попадает в ограничение и становится no-op. Для внешних вызовов прокидывай ключ дальше: заголовок `Idempotency-Key` у Stripe дедупит 24 часа, поэтому, передав ключ события в вызов списания, ты получишь на ретрае *исходное* списание, а не второе. Этот единственный заголовок — разница между тем, был ли Hook почти-промахом или двойным списанием.

Часть отказов не транзиентна — кривой payload, навсегда отклонённая карта. Ретраить такое вечно — значит блокировать партицию (head-of-line blocking) и давать лагу расти без границ. Поэтому каждому сообщению даёшь **бюджет ретраев** с экспоненциальным backoff для транзиентных ошибок, а при исчерпании маршрутизируешь его в **dead-letter queue**. Дизайн надёжного репроцессинга у Uber использует ярусные retry-топики, питающие DLQ, именно чтобы ядовитое сообщение было в карантине, а не стопорило живой поток. DLQ — не кладбище; это инбокс, который надо вычерпывать.

<TradeoffMatrix
  id="08-putting-it-together-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Payment-консьюмер иногда списывает дважды на ретрае после падения. Куда поставить фикс?"
  options={[
    { name: "Идемпотентный handler: сохрани дедуп-ключ (id события) под уникальным ограничением в той же txn и прокинь Idempotency-Key в шлюз", summary: "Сделать переигрыш no-op.", correct: true, justification: "Это несущий фикс. At-least-once означает, что то же событие будет переотправлено после падения-до-коммита, поэтому единственный безопасный handler — тот, где обработка дважды равна обработке один раз. Уникальный дедуп-ключ внутри транзакции побочного эффекта делает вторую доставку no-op, а прокидывание Idempotency-Key в шлюз означает, что шлюз сам вернёт исходное списание, а не создаст второе. Защита живёт у консьюмера и у внешнего вызова — ровно там, где появляется дубликат." },
    { name: "Переключить консьюмер на коммит офсета ДО обработки, чтобы падение не могло переиграть", summary: "Commit-first.", justification: "Это меняет дубликаты на потерянные сообщения — at-most-once. Если закоммитить до обработки и затем упасть, офсет сдвинулся и событие пропало навсегда, так что заказ молча никогда не списывается. Для платежей это хуже дубликата. Commit-after-process плюс идемпотентность — корректная комбинация." },
    { name: "Добавить партиций, чтобы у каждого консьюмера было меньше нагрузки и он реже падал", summary: "Масштабироваться вширь.", justification: "Больше партиций снижает лаг, а не дубликаты — падение-до-коммита переигрывает независимо от того, насколько легко нагружен консьюмер. Хуже того, бамп числа партиций перехеширует ключи и может сломать порядок по заказу. Это лечит баг корректности как проблему ёмкости." },
    { name: "Маршрутизировать каждое неудачное списание прямо в DLQ и сверять руками", summary: "DLQ всё подряд.", justification: "DLQ — для ядовитых сообщений, исчерпавших бюджет ретраев, а не для транзиентных падений, которые идемпотентность уже обрабатывает. Отправка обычных ретраев в DLQ затапливает его, прячет реальный дефект и превращает автоматическое восстановление в ручной труд. Сначала почини идемпотентность; оставь DLQ для по-настоящему необрабатываемых событий." },
  ]}
/>

## Хоп 6: UI обязан говорить правду о лаге

Поскольку каждый хоп асинхронный, в момент клика «Оформить заказ» ответ *ещё не известен*. Inventory и notification могут устаканиться сотнями миллисекунд позже; под нагрузкой — когда сольётся лаг консьюмеров. Сеньорский UI про это не врёт. Он применяет **оптимистичное** обновление, чтобы заказ появился мгновенно, помечает его `pending`, пока не придёт событие подтверждения, и сверяет до `confirmed` или `failed` по реальному состоянию. Окно eventual-consistency — это продуктовая поверхность, а не дефект, который надо прятать.

Здесь же отрабатывает observability. Метрики, ловящие тихих убийц, — это **лаг консьюмеров** (зазор между последним офсетом и закоммиченным — Burrow смотрит на его *тренд* по скользящему окну, помечая OK / WARNING / ERR, а не сырое число), **глубина DLQ** (растущий DLQ означает, что handler-ы падают быстрее, чем ты их вычерпываешь) и **сквозная задержка** с **correlation id**, проставленным на событие на хопе 1 и пронесённым через каждый асинхронный хоп, чтобы один трейс охватил всю дорогу. Двойное списание из Hook невидимо без этого трейса. Лаг, растущий до переполнения DLQ, невидим без этих двух датчиков и алерта на каждый.

<Quiz
  id="08-putting-it-together-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Твой CDC-relay перезапустился и переотправил несколько строк outbox, которые уже публиковал. Pipeline сломан?"
  choices={[
    { label: "Нет — переотправка ожидаема при at-least-once; идемпотентные консьюмеры дедупят переигрыши", correct: true },
    { label: "Да — relay обязан гарантировать exactly-once, иначе события обработаются дважды", misconception: "Ни один хоп на проводе не даёт настоящего exactly-once; relay, возобновляясь с log-офсета, может переиграть by design. Корректность даёт идемпотентность консьюмеров, а не идеальный relay." },
    { label: "Да — надо отключить WAL-tailing и перейти на синхронную публикацию в запросе", misconception: "Синхронная публикация возвращает проблему dual-write, ради которой и существует outbox: падение между коммитом БД и публикацией теряет или фантомит события." },
    { label: "Нет — но только если у топика одна партиция, чтобы ничего не переупорядочилось", misconception: "Одна партиция убивает throughput и не объясняет, почему переигрыши безопасны. Партиционирование по ключу держит порядок; идемпотентность обрабатывает переигрыши." },
  ]}
/>

<Quiz
  id="08-putting-it-together-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Лаг консьюмеров растёт уже час, и DLQ наполняется. Что эта комбинация вероятнее всего означает?"
  choices={[
    { label: "Handler-ы падают или слишком медленны, исчерпывают бюджеты ретраев и маршрутизируют ядовитые сообщения в DLQ быстрее, чем кто-либо его вычерпывает", correct: true },
    { label: "Продюсеры отправляют слишком мало событий", misconception: "Лаг — это консьюмеры, отстающие от лога, а не продюсеры, замедляющиеся — меньше событий уменьшило бы лаг, а не растило." },
    { label: "Оптимистичные обновления UI рассинхронизированы", misconception: "Оптимистичный UI — забота отображения ниже по потоку от обработки; он не может вызвать бэкенд-лаг или рост DLQ." },
    { label: "Число партиций слишком высокое", misconception: "Больше партиций обычно снижает лаг за счёт параллелизма; растущий лаг плюс наполняющийся DLQ указывают на падающие или медленные handler-ы, а не на оверпартиционирование." },
  ]}
/>

<DragOrder
  id="08-putting-it-together-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь путь одного события заказа по pipeline:"
  items={[
    { id: "a", label: "Записать строку заказа и строку outbox в одной транзакции БД" },
    { id: "b", label: "CDC-relay читает WAL и публикует закоммиченную строку в Kafka" },
    { id: "c", label: "Топик партиционирован по id заказа, сохраняя порядок в пределах заказа" },
    { id: "d", label: "Consumer group обрабатывает событие; идемпотентный handler дедупит любой переигрыш" },
    { id: "e", label: "При исчерпанных ретраях ядовитое сообщение уходит в dead-letter queue" },
    { id: "f", label: "UI сверяет свою оптимистичную pending-строку до confirmed или failed" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f"]}
/>

</Explanation>

<FlowDiagram
  label="Одно событие заказа от начала до конца: продюсер атомарно пишет заказ и строку outbox, CDC публикует at-least-once в топик, партиционированный и упорядоченный по ключу, идемпотентная consumer group дедупит переигрыши, ядовитые сообщения уходят в dead-letter queue, а выжившие события расходятся по побочным эффектам — каждая гарантия подписана на шве, где она применяется."
  hue="var(--d-network)"
  nodes={[
    { id: "producer", label: "Продюсер", sub: "запись заказа", col: 0, row: 0 },
    { id: "outbox", label: "Outbox + CDC", sub: "читает WAL", col: 1, row: 0 },
    { id: "broker", label: "Брокер", sub: "партиции", col: 2, row: 0 },
    { id: "consumer", label: "Consumer group", sub: "дедуп", col: 3, row: 0 },
    { id: "effects", label: "Побочные эффекты", sub: "списать / отгрузить", col: 4, row: 0 },
    { id: "dlq", label: "DLQ", sub: "ядовитые сообщ.", col: 3, row: 1 },
  ]}
  edges={[
    { from: "producer", to: "outbox", label: "атомарно (outbox)" },
    { from: "outbox", to: "broker", label: "at-least-once" },
    { from: "broker", to: "consumer", label: "порядок по ключу" },
    { from: "consumer", to: "effects", label: "идемпотентно" },
    { from: "consumer", to: "dlq", label: "ретраи исчерпаны" },
  ]}
  caption="Запись атомарна ровно один раз (outbox); каждый хоп после неё — at-least-once, поэтому именно идемпотентный консьюмер делает переигрыши безопасными. Сообщения, исчерпавшие бюджет ретраев, уходят в DLQ, а не блокируют партицию."
/>

<KeyTakeaway>Нет одной ручки exactly-once. Выбери гарантию на хоп — атомарный outbox, at-least-once публикация, порядок по ключу — затем сделай каждый консьюмер идемпотентным. Этот инвариант и делает весь pipeline безопасным.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="08-putting-it-together-retrieval"
  lang="ru"
  questions={[
    { q: "Пройди по тому, почему именно идемпотентность, а не exactly-once доставка, — инвариант, держащий event-driven pipeline заказов вместе.", a: "Каждый хоп на проводе честно at-least-once. CDC-relay возобновляется с последнего log-офсета, поэтому может переотправить строку, опубликованную прямо перед падением. Консьюмер коммитит свой Kafka-офсет только после успеха handler, поэтому падение между обработкой и коммитом переигрывает то же событие следующему консьюмеру. Нет глобальной ручки exactly-once, которую можно повернуть — гнаться за ней значит бороться с физикой. Вместо этого ты принимаешь дубликаты как контракт и нейтрализуешь их у консьюмера: записываешь дедуп-ключ (id события или бизнесовый idempotency-ключ) под уникальным ограничением в той же транзакции, что и побочный эффект, чтобы переотправка попала в ограничение и стала no-op. Для внешних эффектов прокидываешь ключ дальше — заголовок Idempotency-Key у Stripe, например, возвращает исходное списание 24 часа вместо создания второго. Поскольку этот инвариант живёт у каждого консьюмера, атомарный outbox, at-least-once публикация и порядок по ключу могут оставаться простыми. Идемпотентность — это то, что позволяет остальному pipeline быть честным о своих гарантиях, а не притворяться идеальным." },
    { q: "Две метрики вместе — растущий лаг консьюмеров и наполняющийся dead-letter queue — описывают конкретный отказ. Объясни его и где должна жить observability.", a: "Лаг консьюмеров — это зазор между последним офсетом на партиции и офсетом, который закоммитила группа; он растёт, когда консьюмеры отстают от продюсеров. Наполняющийся DLQ означает, что сообщения исчерпывают бюджет ретраев и уходят в карантин. Вместе они описывают handler-ы, которые падают или слишком медленны: каждое ядовитое сообщение жжёт свои ретраи (растя лаг, пока блокирует партицию), а потом приземляется в DLQ, и если никто его не вычерпывает, он переполняется, пока живой поток молча отстаёт всё сильнее. Опасность в том, что оба растут тихо — в пути запроса нет исключения, потому что весь pipeline асинхронный. Поэтому observability не может быть запоздалой мыслью: ты ставишь correlation id на событие при записи (хоп 1) и несёшь его через каждый асинхронный хоп, чтобы один трейс охватил дорогу и двойное списание реально находилось; ты меришь лаг консьюмеров (Burrow смотрит на тренд по скользящему окну и помечает OK/WARNING/ERR, а не сырой счётчик); и ставишь алерт на глубину DLQ, чтобы растущий бэклог поднял кого-то по пейджеру до переполнения. Метрики живут на швах между хопами, потому что именно там прячутся тихие отказы." },
  ]}
/>

<Recap lang="ru">
Event-driven pipeline заказов — это шесть хопов, и на каждом ты выбираешь гарантию: запись заказа атомарна, потому что строка и её outbox-событие коммитятся в одной транзакции; CDC-relay публикует at-least-once, возобновляясь с log-офсета; топик сохраняет порядок по ключу, партиционируя по id заказа, и поэтому же бамп числа партиций — дверь в одну сторону, которая может сломать порядок по ключу; consumer group доставляет at-least-once, коммитя офсеты только после обработки; ядовитые сообщения, исчерпавшие бюджет ретраев, уходят в dead-letter queue вместо блокировки партиции; а UI говорит правду об окне eventual-consistency состоянием optimistic-then-pending. Нить через всё это — идемпотентность: дедуп-ключ под уникальным ограничением плюс Idempotency-Key, прокинутый во внешние вызовы, — потому что каждый честный хоп at-least-once и дубликаты — это контракт, а не баг. Observability живёт на швах: correlation id через каждый асинхронный хоп, лаг консьюмеров, отслеживаемый по тренду, и алерт на глубину DLQ. Теперь, когда ты увидишь двойное списание в production, ты сразу знаешь, на каком хопе искать, — а когда лаг растёт вместе с наполняющимся DLQ, знаешь: падает handler, а не брокер.
</Recap>
