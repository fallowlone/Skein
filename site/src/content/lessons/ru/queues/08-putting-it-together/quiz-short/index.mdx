---
concepts:
- transactional-outbox
- change-data-capture
- partition-key-ordering
- idempotent-consumer
- dead-letter-queue
- eventual-consistency-ux
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://microservices.io/patterns/data/transactional-outbox.html
- https://www.morling.dev/blog/mastering-postgres-replication-slots/
- https://www.uber.com/us/en/blog/reliable-reprocessing/
status: ready
summary: "Промпты на свободное припоминание по всему треку очередей. Сначала ответь своими словами по памяти, затем открой эталонный ответ и сравни."
title: 'Капстоун очередей: тест на свободное припоминание'
track: queues
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. Каждый промпт просит реконструировать один шов pipeline заказов по памяти — скажи или запиши полный ответ до того, как откроешь эталон. Усилие припоминания и сваривает уроки трека в один дизайн, который ты сможешь защитить на ревью.
</Hook>

<Goal lang="ru">
Реконструируй хребет pipeline, не подглядывая: атомарность outbox, риск slot у CDC, порядок по ключу, контракт at-least-once-плюс-идемпотентность, retry-бюджеты и DLQ, а также UI поверх eventual consistency.
</Goal>

<RetrievalDrawer
  client:visible
  id="08-putting-it-together-short-retrieval"
  lang="ru"
  questions={[
    { q: "Проведи событие заказа через все шесть хопов pipeline и назови гарантию и защиту на каждом.", a: "Хоп 1, запись: строка заказа и строка outbox коммитятся в одной локальной ACID-транзакции, так что событие существует тогда и только тогда, когда существует заказ — защищено самой транзакцией. Хоп 2, публикация: relay на CDC читает WAL и пушит закоммиченные outbox-строки в Kafka, at-least-once, защита — возобновление со своего подтверждённого LSN. Хоп 3, маршрутизация: топик ключуется по order_id, так что все события заказа попадают в одну партицию в порядке коммита — защита стабильным назначением партиции, поэтому количество партиций — дверь в одну сторону. Хоп 4, обработка: consumer group доставляет at-least-once, коммитя оффсеты только после успеха хендлера — защита идемпотентным хендлером с dedup-ключом. Хоп 5, карантин: транзиентный сбой ретраится с backoff против бюджета, и при исчерпании poison-сообщение идёт в dead-letter queue, а не блокирует партицию — защита retry-бюджетом и алертом на глубину DLQ. Хоп 6, отображение: UI показывает заказ оптимистично как pending и сверяется до confirmed или failed, когда приходит событие — защита честным pending-состоянием плюс сверкой. Одна запись, шесть хопов, и идемпотентность сшивает их вместе." },
    { q: "Почему именно идемпотентность, а не exactly-once доставка, — инвариант, который держит весь pipeline?", a: "Потому что каждый честный хоп на проводе — at-least-once, и нет глобальной ручки exactly-once, которую можно повернуть. Relay на CDC возобновляется со своего последнего подтверждённого LSN, так что при рестарте может переотправить строку, опубликованную прямо перед крахом. Консьюмер коммитит Kafka-оффсет только после успеха хендлера, так что краш между обработкой и коммитом делает replay того же события следующему консьюмеру после ребаланса. Гнаться за exactly-once доставкой — значит бороться с невозможностью из Two Generals. Вместо этого ты принимаешь дубли как контракт и нейтрализуешь их в консьюмере: записываешь dedup-ключ (event id или бизнесовый idempotency key) под уникальным ограничением внутри той же транзакции, что и side effect, так что повторная доставка натыкается на ограничение и становится no-op. Для внешних эффектов пробрасываешь ключ дальше — Idempotency-Key у Stripe возвращает исходное списание на 24 часа вместо создания второго. Поскольку этот один инвариант живёт в каждом консьюмере, атомарный outbox, at-least-once публикация и порядок по ключу могут оставаться простыми и честными о своих гарантиях." },
    { q: "И outbox, и CDC дают at-least-once доставку. Объясни, как они композируются и почему replication slot у CDC — самая опасная часть всей системы.", a: "Они композируются, а не конкурируют: outbox решает атомарность — пишешь бизнес-строку и строку-событие в одной транзакции, чтобы они закоммитились вместе — а CDC это один из способов отправить эти outbox-строки наружу, направив Debezium на таблицу outbox вместо polling-relay. CDC читает WAL через логический replication slot, доставляя изменения в пределах миллисекунд от коммита с нулевой нагрузкой запросами на таблицу. Slot опасен тем, что его обещание — Postgres не переиспользует WAL после подтверждённого LSN slot — безусловно и держится даже когда консьюмер мёртв. Если коннектор падает, получает OOM или лагает (или долгая транзакция замораживает restart_lsn, пока коннектор выглядит здоровым), WAL перестаёт переиспользоваться и копится. На нагруженном OLTP-ящике, делающем 20–50 ГБ WAL в час, здоровый диск заполняется меньше чем за два часа, а полный диск заставляет Postgres отвергать все записи — полный простой primary. Guardrail двухслойный: алертить на slot lag, следя за расстоянием в байтах между confirmed_flush_lsn и текущей позицией WAL, и capить через max_slot_wal_keep_size, чтобы Postgres инвалидировал убежавший slot, а не умирал. Вынужденный re-snapshot лучше простоя." },
    { q: "Где в pipeline живёт порядок по ключу и назови три способа, которыми он тихо ломается в production.", a: "Порядок живёт на партиции: Kafka гарантирует порядок только внутри партиции, выбранной через hash(key) % N, так что ключевание топика на order_id кладёт всю историю заказа в одну партицию, обрабатываемую одним консьюмером — cancel никогда не обгонит свой create для этого заказа. Порядок между заказами не гарантирован и не нужен, что и позволяет масштабироваться добавлением партиций. Ломается тремя способами. Первый: поднятие количества партиций рехеширует большинство ключей, так что новые записи ключа идут в другую партицию, чем его история, и два потока больше не упорядочены — репартиционируй миграцией на новый топик, никогда не меняй размер на месте. Второй: ретраи и DLQ-replay вставляют сообщение вне очереди: сообщение 5 падает, 6/7/8 коммитятся, потом 5 переигрывается и приземляется после своих преемников — поэтому replay делают rate-limited и опираются на version guard. Третий: неидемпотентный продюсер с max.in.flight больше 1 даёт переотправленному батчу приземлиться после более позднего, который успел — именно поэтому KIP-185 сделал идемпотентный продюсер дефолтом. Устойчивый дизайн партиционирует по сущности, которой нужен порядок, делает хендлеры идемпотентными, предпочитает коммутативные эффекты и штампует версию на сущность, чтобы отбрасывать устаревшие апдейты." },
    { q: "Транзиентный сбой и poison-сообщение оба роняют консьюмер оплаты. Объясни, почему им нужна разная обработка и как retry-бюджет плюс DLQ разделяют их.", a: "Транзиентный сбой — 500 от шлюза, мгновенный сетевой сбой, кратковременная контенция БД — скорее всего пройдёт при повторе, так что правильный ход — ретрай с экспоненциальным backoff. Poison-сообщение — кривой payload, навсегда отклонённая карта, баг, на котором хендлер всегда спотыкается — будет падать вечно, так что ретрай не просто бесполезен, он вреден: пинит партицию (head-of-line blocking), растит лаг консьюмера без предела и никогда не продвигается. Заранее их не различить, поэтому ты даёшь каждому сообщению retry-бюджет с backoff для транзиентного случая, а при исчерпании маршрутизируешь сообщение в dead-letter queue. Дизайн надёжной переобработки у Uber использует ярусные retry-топики, ведущие в DLQ, именно для этого: poison-сообщение выводится из живого потока в карантин после исчерпания бюджета, и остаток партиции продолжает течь. DLQ — не кладбище, а входящий ящик, который надо разгребать и на который надо алертить, потому что растущий DLQ означает, что хендлеры падают быстрее, чем кто-то чинит." },
    { q: "Две метрики вместе — растущий consumer lag и заполняющийся dead-letter queue — описывают один сбой. Объясни его и где должна жить observability по всему pipeline.", a: "Consumer lag — это разрыв между последним оффсетом на партиции и оффсетом, который закоммитила группа; он растёт, когда консьюмеры отстают от продюсеров. Заполняющийся DLQ означает, что сообщения исчерпывают retry-бюджет и уходят в карантин. Вместе они описывают хендлеры, которые падают или слишком медленны: каждое poison-сообщение сжигает свои ретраи (растя лаг, пока блокирует партицию) и затем приземляется в DLQ, и если никто не разгребает DLQ, он переполняется, пока живой поток тихо отстаёт всё сильнее. Опасность в том, что оба растут тихо — весь pipeline асинхронный, так что нет исключения ни в одном request-пути. Поэтому observability живёт на швах между хопами, а не внутри одного сервиса: ты штампуешь correlation id на событие при записи (хоп 1) и несёшь его через каждый асинхронный хоп, чтобы один трейс охватывал весь путь и двойное списание было реально находимо; меришь consumer lag по тренду на скользящем окне (Burrow флагает OK/WARNING/ERR, а не сырое число); алертишь на глубину DLQ, чтобы растущий бэклог поднял кого-то до переполнения; и следишь за slot lag CDC, чтобы тихий сбой relay не заполнил диск primary." },
  ]}
/>

<Recap lang="ru">
Если ты реконструировал каждый ответ по памяти, у тебя есть хребет трека: outbox делает запись и её событие атомарными в одной локальной транзакции; CDC отправляет эти строки at-least-once, и его replication slot — заряженное ружьё, на которое надо алертить и которое надо capить; топик хранит порядок по ключу через ключевание на order_id, и этот порядок ломается при репартиционировании, при вставке вне очереди ретраями и DLQ-replay или при переупорядочивании неидемпотентным продюсером; consumer group доставляет at-least-once, коммитя после обработки, так что хендлеры должны быть идемпотентны; транзиентные сбои ретраятся против бюджета, а poison-сообщения идут в DLQ; и UI говорит правду об окне согласованности. Идемпотентность — это нить, а observability живёт на швах: correlation id end-to-end, лаг по тренду и алерты на глубину DLQ и slot lag. Теперь, когда ты входишь на design review любой event-driven системы, знаешь шесть вопросов, которые надо задать, — и один инвариант, отсутствие которого делает все шесть ответов бессмысленными.
</Recap>
