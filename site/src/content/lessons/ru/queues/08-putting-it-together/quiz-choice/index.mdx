---
concepts:
- transactional-outbox
- partition-key-ordering
- idempotent-consumer
- dead-letter-queue
- change-data-capture
- eventual-consistency-ux
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://microservices.io/patterns/data/transactional-outbox.html
- https://debezium.io/documentation/reference/stable/connectors/postgresql.html
- https://docs.confluent.io/kafka/design/delivery-semantics.html
status: ready
summary: "Тест с выбором на синтез по всему треку очередей — outbox, CDC, упорядочивание по партициям, идемпотентные консьюмеры, dead-letter queue и UX поверх eventual consistency как единый дизайн."
title: 'Капстоун очередей: тест с выбором ответа'
track: queues
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего трека очередей. Ни один не про определение для заучивания — каждый отражает решение, которое ты принимаешь, проектируя или отлаживая один pipeline обработки заказов, где outbox, Kafka, CDC, DLQ и UI обязаны договориться между собой.
</Hook>

<Goal lang="ru">
Убедись, что держишь весь pipeline в голове целиком: где живёт каждая гарантия доставки, почему упорядочивание по ключу хрупкое и почему один инвариант — идемпотентность — делает безопасным каждый хоп.
</Goal>

<Quiz
  id="08-putting-it-together-choice-1"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="По всему pipeline заказа — запись в outbox, публикация через CDC, доставка Kafka, обработка консьюмером — какой единственный инвариант делает систему корректной end-to-end и почему?"
  choices={[
    { label: "Идемпотентный консьюмер по стабильному event id. Каждый хоп честно at-least-once (CDC возобновляется со своего LSN, консьюмер коммитит после обработки), так что дубли — это контракт; запись event id под уникальным ограничением внутри транзакции с side effect делает любой replay no-op.", correct: true },
    { label: "Exactly-once доставка, настроенная end-to-end на брокере.", misconception: "Ни один хоп на проводе не даёт настоящего exactly-once: CDC при рестарте переотправляет данные после своего подтверждённого LSN, а консьюмер делает replay при крахе до коммита. Корректность даёт идемпотентность, а не флаг доставки." },
    { label: "Единый глобальный порядок всех событий, чтобы ничто не обработалось не по очереди.", misconception: "Глобальный total order — это одна партиция и один консьюмер: он режет пропускную способность и всё равно не предотвращает дубли. Нужен порядок по ключу плюс идемпотентность, а не глобальная точка сериализации." },
    { label: "Two-phase commit поверх Postgres и Kafka, чтобы запись и публикация были атомарны.", misconception: "Kafka не практичный участник XA, а блокирующий координатор 2PC — это худшая проблема доступности. Outbox получает атомарность из одной локальной транзакции; идемпотентность закрывает at-least-once публикацию." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-2"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Хендлер заказа должен и сохранить заказ, и эмитить order.placed. Почему transactional outbox — правильная форма, а не INSERT-затем-kafka.publish в одном хендлере?"
  choices={[
    { label: "БД и брокер не делят транзакцию, поэтому любой порядок двух отдельных вызовов теряет событие или эмитит фантом при крахе в промежутке. Outbox пишет строку заказа и строку outbox в одной локальной ACID-транзакции, а relay отправляет событие отдельным retry-шагом.", correct: true },
    { label: "kafka.publish медленнее, чем INSERT в БД, так что outbox — это оптимизация задержки.", misconception: "Outbox про атомарность, а не про скорость — он даже добавляет хоп relay и задержку. Его задача — убрать промежуток dual-write, где краш теряет или фантомит событие." },
    { label: "Обернуть оба вызова в try/catch и компенсировать при сбое — эквивалентно и проще.", misconception: "OOM-kill посреди вызова не бросает ничего, что можно поймать, а самописная компенсация — это багованный координатор распределённой транзакции, который сам может упасть между шагами. Безопасного порядка dual-write нет." },
    { label: "Сначала публикация, потом INSERT строки заказа — так события не теряются.", misconception: "Publish-first меняет потерю событий на фантомные события: краш после публикации и до коммита — и консьюмеры реагируют на заказ, который откатился и никогда не существовал." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-3"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Топик заказов ключуется по order_id, 6 партиций. В горячую пятницу лаг скачет, и кто-то поднимает его до 12 партиций. Что ломается и почему?"
  choices={[
    { label: "Ломается порядок по заказу: партиционер — это hash(key) % N, так что смена N перемаршрутизирует большинство order_id. Новое событие заказа может попасть в другую партицию, чем его история, и cancel обгонит create. Мигрируй на новый топик, а не меняй размер на месте.", correct: true },
    { label: "Ничего — Kafka переразложит существующие записи по новым партициям и сохранит порядок.", misconception: "Существующие записи не двигаются, они остаются там, где были записаны. Ломается то, что будущие записи ключа теперь хешируются в другое место, расщепляя ключ на две партиции без порядка между ними." },
    { label: "Падает пропускная способность, потому что больше партиций добавляют накладные расходы координации.", misconception: "Больше партиций обычно повышают пропускную способность до определённого предела; настоящий урон здесь — тихая порча порядка из-за рехеширования ключей, а не регресс пропускной способности." },
    { label: "Consumer group автоматически перебалансирует порядок, так что смена безопасна.", misconception: "Никакого автоматического переупорядочивания между старыми и новыми партициями нет. Порядок по ключу просто теряется для in-flight ключей — поэтому количество партиций считают дверью в одну сторону." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-4"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Консьюмер оплаты тянет order.placed, вызывает Stripe, успешно, затем под убит до коммита Kafka-оффсета. Группа перебалансируется и делает replay оффсета. Как не допустить двойного списания?"
  choices={[
    { label: "Сделать хендлер идемпотентным: записать event id под уникальным ограничением в той же транзакции, что и списание, и пробросить Idempotency-Key в Stripe — тогда retry вернёт исходное списание на 24 часа вместо создания второго.", correct: true },
    { label: "Коммитить Kafka-оффсет до вызова Stripe, чтобы краш не делал replay.", misconception: "Commit-before-process — это at-most-once: краш после коммита, и списание тихо не происходит. Для платежей потерянное списание хуже дубля. Commit-after-process плюс идемпотентность — это правильно." },
    { label: "Отправлять переигранное событие сразу в DLQ и сверять руками.", misconception: "DLQ — для poison-сообщений, исчерпавших retry-бюджет, а не для обычных replay из-за краха, которые идемпотентность уже нейтрализует. Слать всё в DLQ — затопить его и спрятать настоящий дефект." },
    { label: "Добавить партиций, чтобы каждый консьюмер был легче и падал реже.", misconception: "Краш до коммита делает replay независимо от нагрузки, а поднятие количества партиций рехеширует ключи и ломает порядок. Это лечение бага корректности как проблемы ёмкости." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-5"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Ты используешь CDC (Debezium, читающий WAL) как relay для outbox. Воркер Kafka Connect получает OOM и лежит часами. В чём production-риск и какой guardrail?"
  choices={[
    { label: "Replication slot не даёт переиспользовать WAL после своего подтверждённого LSN, поэтому WAL копится, пока диск primary не заполнится и Postgres не перестанет принимать записи. Алертить на slot lag и ставить max_slot_wal_keep_size, чтобы убежавший slot был инвалидирован, а не уронил primary.", correct: true },
    { label: "События outbox теряются, пока коннектор лежит, так что их надо переотправить руками.", misconception: "События не теряются — slot держит WAL именно для того, чтобы коннектор возобновился со своего LSN. Опасность обратная: удержанный WAL растёт, пока не заполнит диск." },
    { label: "Postgres автоматически дропает slot, как только консьюмер отключается, так что риска нет.", misconception: "Логический slot переживает отключения by design — именно эта persistence и делает его опасным. Он инвалидируется, только если задан max_slot_wal_keep_size и порог превышен." },
    { label: "Postgres паузит все записи, пока коннектор не вернётся, вызывая краткий простой.", misconception: "Postgres продолжает принимать записи — именно это и растит WAL. Он останавливается, только когда диск реально полон, что и есть тот сбой, от которого ты защищаешься." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-6"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="POST на checkout возвращает 202 Accepted за 40мс; консьюмер пишет заказ примерно через 700мс. UI показывает тост об успехе и сразу рефетчит список заказов. Что увидит пользователь и какой честный дизайн?"
  choices={[
    { label: "Рефетч обгоняет консьюмера и выигрывает, поэтому нового заказа нет — нарушение read-your-writes — а тост соврал про запись, которая всё ещё в очереди. Покажи заказ оптимистично как pending, затем сверь по событию-подтверждению или по отложенному invalidate.", correct: true },
    { label: "Всё нормально — 202 значит, что запись закоммичена, так что рефетч покажет заказ.", misconception: "202 Accepted значит «поставлено в очередь», а не «готово». Строки нет, пока консьюмер не отработал, так что немедленный рефетч читает внутри окна согласованности и возвращает заказ отсутствующим." },
    { label: "Рефетч закеширован, так что список просто не обновляется — и это единственная проблема.", misconception: "Рефетч возвращает данные — просто данные до того, как консьюмер записал. Доверие этому устаревшему чтению и стирает собственную запись пользователя." },
    { label: "Блокировать спиннером, пока консьюмер не подтвердит; это всегда правильный ход.", misconception: "Блокировка на полный асинхронный round-trip для предсказуемого действия тратит впустую ~0мс воспринимаемой задержки, которую даёт оптимистичный UI. Блокирующие pending-состояния оставь для непредсказуемых исходов, вроде capture платежа." },
  ]}
/>

<Recap lang="ru">
Весь трек схлопывается в одно дерево решений: запись заказа атомарна, потому что строка и её outbox-событие коммитятся в одной локальной транзакции; CDC (или polling-relay) публикует at-least-once, возобновляясь со своего log offset — а slot это заряженное ружьё, на которое надо алертить и которое надо capить; топик хранит порядок по ключу через ключевание на order_id, поэтому поднятие количества партиций — дверь в одну сторону, рехеширующая ключи; consumer group доставляет at-least-once, коммитя оффсеты только после обработки; poison-сообщения, исчерпавшие retry-бюджет, идут в DLQ, а не блокируют партицию; и UI говорит правду об окне согласованности через optimistic-then-pending состояние. Нить через всё это — идемпотентность: dedup-ключ под уникальным ограничением плюс Idempotency-Key, проброшенный во внешние вызовы, потому что каждый честный хоп — at-least-once, и дубли это контракт, а не баг.
</Recap>
