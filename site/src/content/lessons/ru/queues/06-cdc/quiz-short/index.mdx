---
concepts:
- logical-replication-slot
- wal-retention
- snapshot-then-stream
- replica-identity
- at-least-once-delivery
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.morling.dev/blog/mastering-postgres-replication-slots/
- https://debezium.io/documentation/reference/stable/connectors/postgresql.html
status: ready
summary: "Промпты на свободное воспроизведение по всему юниту CDC. Сначала ответь своими словами, потом открой модельный ответ и сравни."
title: 'Change data capture: тест на воспроизведение'
track: queues
unit: 06-cdc
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. Для каждого промпта проговори или запиши полный ответ по памяти прежде, чем открыть модельный — именно усилие припоминания закрепляет механизм.
</Hook>

<Goal lang="ru">
Восстанови ключевые механизмы юнита — log-based capture vs polling, slot как заряженное ружьё, snapshot-then-stream, захват delete и at-least-once доставку — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:load
  id="06-cdc-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему log-based CDC бьёт polling таблицы по курсору на каждой важной оси?", a: "Polling спрашивает таблицу «есть строки новее моего курсора?» с интервалом, поэтому несёт до одного интервала задержки, пропускает любую строку, вставленную и удалённую между двумя опросами, и добавляет повторяющуюся query-нагрузку на таблицу, которая может уже быть горячей. Log-based CDC вместо этого читает собственный write-ahead log базы — долговечную упорядоченную запись изменений, которую база и так ведёт ради crash recovery и репликации. Он эмитит одно событие на закоммиченное изменение строки в миллисекундах от коммита, ловит каждое промежуточное состояние, включая hard delete, и добавляет нулевую query-нагрузку, потому что читает лог, а не строки. Ему не нужно никакого изменения приложения: приложение продолжает делать обычные INSERT/UPDATE/DELETE. Цена — операционная, а не в задержке: теперь ты владеешь replication slot." },
    { q: "Объясни, почему logical replication slot может уронить primary, и что ты ставишь перед запуском CDC.", a: "Logical replication slot — это серверная закладка: она записывает LSN, который consumer подтвердил, и обещает, что Postgres не переработает WAL дальше этой точки, чтобы consumer всегда мог продолжить. Это обещание безусловно — оно держится даже когда consumer мёртв. Поэтому если коннектор Debezium или его воркер Kafka Connect падает, ловит OOM или отстаёт, WAL перестаёт перерабатываться и накапливается; на нагруженной OLTP-машине с 20–50 ГБ WAL в час здоровый диск может заполниться меньше чем за два часа, а когда Postgres исчерпывает диск, он перестаёт принимать записи — полный отказ primary. Долгая транзакция делает то же тихо, замораживая restart_lsn. Перед запуском я ставлю два слоя: алерт на slot lag (байтовая дистанция между confirmed_flush_lsn и текущим WAL в pg_replication_slots), чтобы действовать до давления на диск, и ограничиваю радиус поражения через max_slot_wal_keep_size, чтобы Postgres инвалидировал сбежавший slot, а не умирал. Cap заставляет пере-снапшотить, что лучше отказа." },
    { q: "Что такое старт snapshot-then-stream и почему snapshot — опасная часть?", a: "Коннектор, стартующий на существующей базе, имеет проблему холодного старта: WAL содержит только недавние изменения, а downstream нужно текущее состояние каждой строки. Поэтому Debezium сначала снапшотит — читает всю таблицу (или отфильтрованный набор), чтобы засеять начальное состояние — затем переключается на чтение лога с LSN, зафиксированного в начале snapshot, чтобы ничего не пропустить на стыке. Snapshot — где кусают нагрузка и блокировки: классический блокирующий snapshot на MySQL берёт глобальный read lock (FLUSH TABLES WITH READ LOCK), который может заморозить записи на большой таблице на неприятное окно. Современный ответ — incremental snapshot (на сигналах/watermark): он снапшотит чанками, пока стриминг продолжается, его можно ставить на паузу и продолжать, и он никогда не берёт долгий lock — ценой бо́льшего числа движущихся частей. На многотерабайтной таблице это разница между многочасовой заморозкой записей и фоновым ручейком." },
    { q: "Почему захват полных DELETE требует REPLICA IDENTITY FULL и сколько это стоит? Что такое tombstone?", a: "DELETE в Postgres по умолчанию логирует только primary key, поэтому before-образ события — лишь ключ; нормально, если нужно лишь знать, какая строка ушла, бесполезно, если downstream нужна полная строка до удаления. REPLICA IDENTITY FULL на таблице заставляет WAL логировать всю старую строку для delete, давая полный before-образ — но и каждый UPDATE теперь логирует всю старую строку, повышая объём WAL (что замыкается обратно в давление slot/диска). Отдельно tombstone — это запись с null-значением, которую Debezium эмитит после удаления; данных строки в ней нет, но она позволяет log-compacted топикам Kafka реально дропнуть ключ, а не держать призрак вечно. Забудь семантику tombstone — и compacted-топик хранит удалённые ключи." },
    { q: "Почему доставка CDC по факту at-least-once, и как это меняет написание consumer? Как помогает порядок?", a: "Механизм slot/LSN даёт точный resume, а не exactly-once. При падении коннектор стартует с последнего подтверждённого LSN, поэтому любые события, которые он эмитил, но не подтвердил, пере-эмитятся — downstream может увидеть одно изменение дважды. Debezium 2.x умеет exactly-once в Kafka через транзакции Kafka, но эта гарантия кончается на Kafka: как только consumer читает запись и делает побочный эффект, повтор означает, что эффект случится снова. Поэтому end-to-end ты проектируешь под at-least-once. Фикс — идемпотентность: дедуп по стабильному ключу плюс LSN/позиция события, или естественно идемпотентная операция — upsert по primary key, set-to-this-value вместо increment, no-op, когда цель уже отражает изменение. Порядок помогает: Debezium держит порядок по ключу, маршрутизируя ключ в одну партицию, поэтому upsert-by-key consumer сходится даже с дубликатами — но нельзя предполагать глобальный порядок коммитов между таблицами." },
    { q: "Когда ты выберешь outbox-паттерн вместо прямого захвата доменных таблиц и как он соотносится с CDC?", a: "CDC и outbox-паттерн не соперники — они композируются. Outbox решает атомарность: в одной транзакции ты пишешь бизнес-строку и строку события, так что они коммитятся вместе и ты никогда не получаешь dual-write split-brain, где запись в БД успешна, а публикация сообщения провалилась. Но тебе всё равно нужно чем-то вывозить строки outbox: polling relay — простой вариант; направить Debezium на outbox-таблицу — log-based (у Debezium даже есть outbox event router). Так что «CDC vs outbox» — неверная ось. Реальный выбор — захватывать доменные таблицы напрямую (ноль изменений приложения, но ты связан с физической схемой и эмитишь сырые формы строк) или захватывать специально построенную outbox-таблицу, форму события которой ты контролируешь, ценой записи в неё в каждой транзакции. Бери outbox, когда хочешь стабильный публичный контракт событий, отвязанный от раскладки таблиц." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: log-based CDC бьёт polling по задержке, полноте, query-нагрузке и вторжению в приложение; безусловное обещание slot по WAL — и почему CDC возобновляем, и почему может заполнить диск, поэтому ты алертишь и ставишь cap; старт — snapshot-then-stream, где incremental snapshot избегает долгого lock; полные delete стоят WAL через REPLICA IDENTITY FULL и требуют tombstone для compaction; доставка — at-least-once, поэтому consumers должны быть идемпотентны; а outbox композируется с CDC, давая атомарный, отвязанный от схемы контракт событий.
</Recap>
