---
concepts:
- logical-replication-slot
- wal-retention
- snapshot-then-stream
- replica-identity
- at-least-once-delivery
- per-key-ordering
estMin: 12
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.morling.dev/blog/mastering-postgres-replication-slots/
- https://debezium.io/documentation/reference/stable/connectors/postgresql.html
status: ready
summary: "Тест с выбором на синтез по всему юниту CDC — риск replication slot, удержание WAL, стратегия snapshot, захват delete, семантика доставки и порядок."
title: 'Change data capture: тест с выбором ответа'
track: queues
unit: 06-cdc
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает решение, которое ты принимаешь, эксплуатируя CDC в production — не определение для заучивания, а сбой, который надо разобрать прежде, чем он тебя разбудит.
</Hook>

<Goal lang="ru">
Убедись, что связываешь механизм slot, удержание WAL, стратегию snapshot, захват delete и семантику доставки — тот синтез, к которому вёл урок.
</Goal>

<Quiz
  id="06-cdc-choice-1"
  lessonSlug="06-cdc"
  lang="ru"
  question="Воркер Kafka Connect коннектора Debezium ловит OOM и лежит шесть часов на нагруженной OLTP-машине. Какой главный риск для исходного Postgres и почему?"
  choices={[
    { label: "Slot пинит WAL на своём restart_lsn, и сервер держит каждый сегмент с тех пор; на машине с десятками ГБ WAL в час том данных переполняется и Postgres перестаёт принимать записи — полный отказ primary.", correct: true },
    { label: "События между падением и восстановлением теряются, но сама база не затронута.", misconception: "События не теряются — slot держит WAL именно чтобы коннектор мог продолжить со своего LSN. Опасность обратная: удерживаемый WAL растёт, пока не переполнится диск." },
    { label: "Postgres автоматически дропает slot после отключения consumer, так что ничего не накапливается.", misconception: "Logical slot по дизайну переживает отключения — в этой устойчивости весь смысл и именно поэтому он опасен. Postgres инвалидирует его только если задан max_slot_wal_keep_size и порог превышен." },
    { label: "Postgres приостанавливает новые записи, пока consumer не вернётся, чтобы держать slot консистентным.", misconception: "Postgres продолжает принимать записи — именно это растит WAL. Он останавливается, только когда диск реально полон." },
  ]}
/>

<Quiz
  id="06-cdc-choice-2"
  lessonSlug="06-cdc"
  lang="ru"
  question="Твой CDC-коннектор выглядит полностью здоровым — потребляет, lag около нуля — но WAL на primary продолжает расти и диск заполняется. Самая вероятная причина?"
  choices={[
    { label: "Открыта долгоживущая транзакция. Logical decoding не может продвинуться за незавершённую транзакцию, поэтому restart_lsn замораживается и WAL накапливается, хотя коннектор в порядке.", correct: true },
    { label: "Коннектор тихо потерял offset и пере-снапшотит в фоне.", misconception: "Re-snapshot читает существующие строки; сам по себе он не пинит WAL вверх. Замороженный restart_lsn из-за открытой транзакции — классическая причина роста при здоровом на вид коннекторе." },
    { label: "Postgres пишет лишний WAL уже потому, что slot существует.", misconception: "Slot не увеличивает генерацию WAL — он лишь управляет тем, как долго сегменты удерживаются. Удержание растит диск, только когда что-то мешает confirmed-позиции продвигаться." },
    { label: "Back-pressure от Kafka заставляет Postgres логировать каждую строку дважды.", misconception: "Back-pressure от Kafka может застопорить consumer (это видно как lag), но не меняет то, как Postgres пишет WAL. Подсказка здесь — lag около нуля, что указывает на замороженный restart_lsn." },
  ]}
/>

<Quiz
  id="06-cdc-choice-3"
  lessonSlug="06-cdc"
  lang="ru"
  question="Нужно забутстрапить CDC на многотерабайтной таблице MySQL под постоянными записями, и нельзя замораживать записи на часы. Какая стратегия snapshot подходит и в чём компромисс?"
  choices={[
    { label: "Incremental (на сигналах/watermark) snapshot: читает таблицу чанками, пока стриминг продолжается, можно ставить на паузу и продолжать, и он никогда не берёт долгий lock — ценой бо́льшего числа движущихся частей.", correct: true },
    { label: "Классический блокирующий snapshot с глобальным read lock ради гарантированно консистентной точки во времени.", misconception: "Блокирующий snapshot (FLUSH TABLES WITH READ LOCK) — именно то, что замораживает записи на большой таблице на неприятное окно, чего бриф на многотерабайтной таблице запрещает." },
    { label: "Пропустить snapshot и стримить только с этого момента; WAL/binlog уже содержит полное состояние таблицы.", misconception: "Лог содержит только недавние изменения, а не текущее состояние каждой существующей строки. Пропуск snapshot оставит downstream без всех строк, не менявшихся после старта." },
    { label: "Сделать дамп через mysqldump, загрузить downstream, затем стримить с временной метки дампа.", misconception: "Временная метка — не позиция репликации; нельзя надёжно совместить логический дамп с точным offset binlog, поэтому рискуешь пропуском или нахлёстом на стыке. Incremental snapshot существует, чтобы чисто совместить snapshot и stream." },
  ]}
/>

<Quiz
  id="06-cdc-choice-4"
  lessonSlug="06-cdc"
  lang="ru"
  question="Событие DELETE из Postgres приходит с before-образом, содержащим только primary key, а downstream нужна полная строка до удаления. Что происходит и какова цена фикса?"
  choices={[
    { label: "Таблица на дефолтном REPLICA IDENTITY, который логирует для delete только ключ. REPLICA IDENTITY FULL логирует всю старую строку — но теперь и каждый update логирует всю старую строку, повышая объём WAL.", correct: true },
    { label: "Debezium отбрасывает before-образ для экономии места; подними лимит размера payload коннектора.", misconception: "Debezium эмитит ровно то, что содержит WAL для этой таблицы. Before-образ только из ключа из-за REPLICA IDENTITY, а не из-за лимита размера у коннектора." },
    { label: "Строку уже вычистил vacuum, поэтому остался только ключ.", misconception: "Vacuum освобождает мёртвые кортежи, но не управляет тем, что WAL логирует для DELETE. Образ до удаления задаётся REPLICA IDENTITY на уровне таблицы." },
    { label: "Нужна tombstone-запись, чтобы восстановить полную строку.", misconception: "Tombstone — это запись с null-значением после удаления, позволяющая compacted-топикам Kafka дропнуть ключ — данных строки в ней нет. Полный before-образ даёт REPLICA IDENTITY FULL." },
  ]}
/>

<Quiz
  id="06-cdc-choice-5"
  lessonSlug="06-cdc"
  lang="ru"
  question="Команда говорит: «Debezium 2.x даёт exactly-once, так что consumer может пропустить дедуп». Где ломается это рассуждение?"
  choices={[
    { label: "Exactly-once относится только к доставке в Kafka через транзакции. Как только consumer читает запись и делает побочный эффект, crash/resume может повторить её — поэтому end-to-end это at-least-once и consumers должны быть идемпотентными.", correct: true },
    { label: "Это верно — exactly-once в Kafka означает, что consumer никогда не увидит дубликат.", misconception: "Гарантия заканчивается на Kafka. При resume с последнего подтверждённого LSN коннектор может пере-эмитить события, а падение consumer между чтением и commit переобработает их — дубликаты downstream всё ещё возможны." },
    { label: "Ломается, потому что Debezium не может точно отслеживать offset.", misconception: "Debezium точно отслеживает offset через slot/LSN — так он и продолжает. Именно точный resume и может вызвать повтор событий после подтверждённой точки." },
    { label: "Ломается только когда нарушается порядок между партициями.", misconception: "Порядок между партициями — отдельная забота. Причина, по которой нужна идемпотентность — дублирующая доставка при resume, независимо от порядка." },
  ]}
/>

<Quiz
  id="06-cdc-choice-6"
  lessonSlug="06-cdc"
  lang="ru"
  question="Consumer джойнит изменения из таблицы orders и таблицы payments и предполагает, что видит их в порядке коммитов. Почему при Debezium-to-Kafka это предположение небезопасно?"
  choices={[
    { label: "Debezium сохраняет порядок только по table/key, маршрутизируя каждый ключ в одну партицию. Между партициями (и между таблицами) глобального порядка нет, поэтому джойн может увидеть payment раньше его order.", correct: true },
    { label: "Kafka случайно переупорядочивает записи внутри партиции под нагрузкой.", misconception: "Kafka гарантирует порядок внутри партиции. Проблема в том, что две таблицы/ключа попадают в разные партиции, где порядок между партициями не обещан." },
    { label: "Предположение безопасно — Debezium эмитит единый глобальный поток в порядке коммитов.", misconception: "Глобального потока в порядке коммитов между партициями нет. Порядок только по ключу; джойн между таблицами не может полагаться на порядок коммитов." },
    { label: "Небезопасно только если не задан REPLICA IDENTITY FULL.", misconception: "REPLICA IDENTITY управляет тем, что логирует delete, а не порядком. Порядок между таблицами — свойство партиционирования, не связанное с identity." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна цепочка следствий: безусловное обещание slot по WAL делает CDC возобновляемым и оно же переполняет диск, когда consumer застрял или долгая транзакция заморозила restart_lsn; бутстрап — это snapshot-then-stream, где incremental snapshot меняет простоту на старт без lock; захват полных delete стоит объёма WAL через REPLICA IDENTITY FULL; а поскольку resume — at-least-once с порядком только по ключу, consumers должны быть идемпотентными и не предполагать глобальный порядок коммитов.
</Recap>
