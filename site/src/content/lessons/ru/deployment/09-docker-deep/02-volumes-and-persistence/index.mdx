---
concepts:
- writable-layer-ephemerality
- named-volumes
- bind-mounts
- tmpfs-mounts
- volume-lifecycle
- volumes-are-host-local
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-volumes-and-persistence
sources:
- https://docs.docker.com/storage/volumes/
- https://docs.docker.com/storage/
spiral: []
status: ready
summary: 'Writable-слой контейнера умирает вместе с контейнером, поэтому персистентным
  данным нужен mount: Docker-управляемый named volume для баз, bind mount для dev-исходников,
  tmpfs для секретов в памяти — у каждого свой жизненный цикл и ловушки.'
title: 'Тома и персистентность'
track: deployment
unit: 09-docker-deep
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
`docker compose down && docker compose up` на staging-машине стёр базу. Две недели засеянных тестовых данных — нет; ни `rm`, ни `DROP`, ни человеческой ошибки в привычном смысле. Postgres всё это время писал в собственный writable-слой контейнера, потому что никто не объявил volume. Удаление контейнера удалило слой, а слой *и был* данными. Фикс — одна строка в compose-файле. Урок — что storage контейнера эфемерен по умолчанию, и персистентность ты включаешь намеренно.
</Hook>

<Crux>Writable-слой контейнера умирает вместе с ним. Чтобы сохранить данные, монтируй внешний storage: named volume, bind mount или tmpfs.</Crux>

<Explanation>

## Writable-слой — это черновик, а не хранилище

Прежде чем добавить базу данных в любой Docker-сетап, спроси себя: что станет с данными после рестарта контейнера? Ответ на этот вопрос определит, найдёшь ли ты в понедельник пустую базу или живые данные.

Запущенный контейнер — это его read-only слои образа плюс один тонкий **writable-слой** (writable layer, записываемый слой) сверху, сшитые union-файловой системой (объединяющей ФС). Каждый файл, который твой процесс создаёт или меняет в рантайме — каталог данных Postgres, загруженная аватарка, лог-файл — попадает в этот writable-слой. Он ощущается как обычный диск, и в этом-то и ловушка.

Этот слой привязан к жизненному циклу контейнера. Удали контейнер (`docker rm`, `compose down`, передеплой, который пересоздаёт его) — и writable-слой удаляется вместе с ним. Данные не «где-то на хосте, откуда их можно выкопать» — они жили внутри собственного diff'а контейнера и исчезли. Хуже того, пока контейнер *жив*, всё, что ты туда пишешь, раздувает размер контейнера и идёт через union-абстракцию storage-драйвера, которая медленнее записи прямо на хост. Writable-слой — для эфемерного черновика: временных файлов, рантайм-состояния процесса, — но никогда для того, что было бы жаль потерять.

Чтобы персистить данные, ты монтируешь storage, который живёт **вне** writable-слоя. Docker даёт три типа mount'ов, и выбор правильного — это и есть весь навык.

## Named volumes: дефолт для данных, которые хранишь

**Named volume** — это storage, который Docker создаёт и сам им управляет; он живёт на хосте под `/var/lib/docker/volumes/<name>/_data`. Ты никогда не трогаешь этот путь напрямую — ты обращаешься к volume по имени, а Docker делает остальное. Это рекомендуемый механизм для всего stateful: файлов баз данных, данных очередей, загруженного пользователями контента.

```bash
# Создать по требованию и смонтировать; -v <name>:<путь-в-контейнере>
docker run -d --name db \
  -v pgdata:/var/lib/postgresql/data \
  postgres:16

# Явная, самодокументирующаяся форма
docker run -d --name db \
  --mount type=volume,src=pgdata,dst=/var/lib/postgresql/data \
  postgres:16
```

```yaml
# docker-compose.yml — одна строка, которая спасает staging-машину
services:
  db:
    image: postgres:16
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
```

Named volumes выигрывают для stateful-данных по трём пунктам. Они **отвязаны от раскладки хоста** — ты ссылаешься на `pgdata`, а не на `/home/deploy/что-то`, поэтому один и тот же compose-файл бежит и на ноутбуке, и на сервере. Они **портируемы и бэкапятся** — Docker управляет ими, инспектирует и бэкапит как объекты первого класса, и они переживают удаление контейнера by design. И они **пишут прямо в файловую систему хоста**, а не через union-слой, поэтому быстрее для write-heavy нагрузок, которые генерируют базы данных.

## Bind mounts: путь хоста, проброшенный напрямую

**Bind mount** мапит конкретный каталог или файл с машины-хоста внутрь контейнера. В отличие от volume, *ты* владеешь путём на хосте и его раскладкой; Docker просто прокидывает его, и хост с контейнером видят одни и те же файлы вживую.

```bash
# Смонтировать исходники текущего проекта в контейнер для live reload
docker run -it \
  --mount type=bind,src="$(pwd)",dst=/app \
  node:22 bash

# Короткая форма (-v с абсолютным путём хоста — это bind mount, а не volume)
docker run -it -v "$(pwd)":/app node:22 bash
```

Убойный сценарий — разработка: bind-монтируешь дерево исходников в контейнер, и dev-сервер делает hot-reload на каждом сохранении, без пересборки. Но bind mounts привязывают тебя к раскладке каталогов хоста (путь должен существовать и выглядеть одинаково везде, где бежит контейнер) и несут острый край — **bind mount поверх пути, который образ уже наполнил, скрывает исходное содержимое этого пути** на всё время mount'а. Прокинь пустую папку хоста на `/app` после того, как `node_modules` поставился туда при сборке, — и контейнер теперь видит пустой `/app`: установка затенена, а не слита. Это одна из самых частых загадок «в образе работало, а при запуске нет».

<Inset kind="why" lang="ru">
Почему bind mount *затеняет*, а не *сливает*? Mount заменяет то, что контейнер видит по этому пути, видом хоста — ровно как монтирование USB-флешки поверх папки на ноутбуке: исходное содержимое папки всё ещё на диске под ней, но невидимо, пока mount активен. Named volumes ведут себя иначе в одном полезном случае — когда ты монтируешь *пустой* named volume на путь, который наполнил образ, Docker сначала копирует существующие файлы образа в свежий volume, поэтому данные засеиваются, а не прячутся. Bind mount никогда не делает этого copy-up.
</Inset>

## tmpfs: в памяти, никогда не персистится

**tmpfs mount** живёт только в RAM хоста. На диск ничего не пишется, и в момент, когда контейнер останавливается, данные испаряются. В этом и смысл: это для данных, которые ты специально *не* хочешь персистить — расшифрованных секретов, сессионного черновика, промежуточных кэшей, которые лучше не оставлять на диске.

```bash
# Смонтировать in-memory файловую систему на /run/secrets (только Linux)
docker run -it --tmpfs /run/secrets myapp

# Явная форма с опциями
docker run -it \
  --mount type=tmpfs,dst=/run/secrets,tmpfs-size=64m \
  myapp
```

Тянись к tmpfs, когда секрет должен существовать как файл, чтобы один процесс его прочитал, но никогда не должен касаться долговечного storage, — или для горячего черновика, где важна скорость RAM, а не долговечность. Никогда не используй его для того, что нужно после рестарта: by design восстанавливать нечего.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Свойство</th>
        <th class="border border-border px-3 py-2 text-left">Named volume</th>
        <th class="border border-border px-3 py-2 text-left">Bind mount</th>
        <th class="border border-border px-3 py-2 text-left">tmpfs</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Управляется</td>
        <td class="border border-border px-3 py-2">Docker (под <code>/var/lib/docker/volumes</code>)</td>
        <td class="border border-border px-3 py-2">Тобой (любой путь хоста)</td>
        <td class="border border-border px-3 py-2">Ядром (RAM хоста)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Персистится?</td>
        <td class="border border-border px-3 py-2">Да — переживает удаление контейнера</td>
        <td class="border border-border px-3 py-2">Да — живёт на хосте</td>
        <td class="border border-border px-3 py-2">Нет — исчезает при stop</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Сценарий</td>
        <td class="border border-border px-3 py-2">База / stateful-данные</td>
        <td class="border border-border px-3 py-2">Live-reload dev-исходников</td>
        <td class="border border-border px-3 py-2">Секреты / черновик в памяти</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Портируемость</td>
        <td class="border border-border px-3 py-2">Высокая — имя, не путь</td>
        <td class="border border-border px-3 py-2">Низкая — привязка к раскладке хоста</td>
        <td class="border border-border px-3 py-2">Н/Д — ничего не хранится</td>
      </tr>
    </tbody>
  </table>
</div>

## Жизненный цикл volume: ловушка «куда делся диск»

У volumes жизненный цикл, намеренно *независимый* от контейнеров, и эта независимость режет в обе стороны. Volume создаётся по требованию при первой ссылке на него и **переживает удаление контейнера** — это и есть нужная долговечность. Но это же значит, что volume никогда не чистится автоматически. Удали контейнер — и named volume остаётся, занимая диск, пока ты явно его не удалишь.

```bash
docker volume ls                 # список томов
docker volume inspect pgdata     # посмотреть Mountpoint на хосте
docker volume rm pgdata          # удалить один том (должен быть не используемым)
docker volume prune              # удалить ВСЕ тома, не используемые контейнером
```

Сеньорская ловушка живёт в `docker volume prune`. Он удаляет каждый volume, не прикреплённый сейчас к контейнеру, — а volume, чей контейнер ты удалил на прошлой неделе, по этому определению «не используется». Запусти небрежный prune, чтобы освободить диск, — и можешь стереть базу, которую полностью собирался оставить. Обратная сторона, **протухшие volumes молча съедают диск**, — более частый повседневный сюрприз: месяцы мёртвых анонимных томов от удалённых контейнеров копятся под `/var/lib/docker/volumes`, пока на хосте не кончается место.

<TradeoffMatrix
  id="09-docker-deep-volumes-tradeoff-1"
  lessonSlug="02-volumes-and-persistence"
  lang="ru"
  prompt="Контейнер Postgres должен сохранять данные через передеплои, которые пересоздают контейнер. Где должен жить каталог данных?"
  options={[
    { name: "Named volume, смонтированный на /var/lib/postgresql/data", summary: "Docker-управляемый, долговечный.", correct: true, justification: "Это стандартный сеньорский выбор для данных базы. Named volume живёт вне writable-слоя контейнера, поэтому пересоздание или удаление контейнера оставляет данные целыми, и он пишет прямо в файловую систему хоста ради производительности, нужной write-heavy базе. Он отвязан от путей хоста (compose-файл портируем между машинами) и является объектом первого класса, который Docker может инспектировать и бэкапить. Единственная дисциплина, которую он требует, — помнить, что его надо удалять явно: сам он не исчезнет." },
    { name: "Writable-слой контейнера (mount не объявлен)", summary: "Дефолт, эфемерный.", justification: "Это ровно баг из хука. Writable-слой привязан к жизненному циклу контейнера, поэтому любой передеплой, пересоздающий контейнер, удаляет данные вместе со слоем. Он также раздувает размер контейнера и гонит записи через более медленную union-файловую систему. Writable-слой — это черновик, никогда не долговечное хранилище." },
    { name: "Bind mount на фиксированный путь хоста вроде /opt/pgdata", summary: "Работает, но привязывает.", justification: "Он персистится, и некоторые команды делают так намеренно ради прямого доступа к файлам с хоста. Но он привязывает деплой к конкретной раскладке каталогов хоста, обходит инструментарий томов Docker (inspect/backup как объектов первого класса) и его легко испортить правами и ловушкой затенения. Для чистой персистентности базы named volume — более чистый и портируемый дефолт." },
    { name: "tmpfs mount на каталоге данных", summary: "В памяти, теряется при stop.", justification: "tmpfs живёт только в RAM и стирается в момент остановки контейнера — противоположность тому, что нужно базе. Это для секретов и черновика в памяти, которые ты явно не хочешь персистить, никогда для долговечных данных." },
  ]}
/>

## Сеньорские случаи: host-local — это не сеть

Три правила отделяют того, кто умеет смонтировать volume, от того, кто умеет гонять stateful-нагрузки. Первое: **данные базы ДОЛЖНЫ жить на named volume, а не на слое контейнера** — хук и есть это правило, выученное на горьком опыте. Второе: **никогда не bind-монтируй поверх пути, который наполнил образ**, если только ты специально не хочешь его затенить; именно так `node_modules` и другое build-time содержимое молча исчезает в рантайме. Третье, и оно кусает на масштабе: **volumes — host-local.** Named volume живёт на диске одной машины. В кластере из нескольких узлов (Swarm, Kubernetes, несколько VM за балансировщиком) volume на узле A невидим контейнеру, запланированному на узел B. Для многоузловой персистентности нужен сетевой или объектный storage — управляемая база, S3-совместимое объектное хранилище или CSI/network-volume драйвер, — а не простой локальный volume. Считать локальный volume общим хранилищем — классическая ошибка, когда одноузловой compose-сетап дорастает до кластера. Вместе эти три правила означают: решения о хранилище, принятые при написании compose-файла, определяют устойчивость всей системы — пропустить второе правило значит потерять данные молча, пропустить третье — потерять данные всего узла на масштабе кластера.

<Quiz
  id="09-docker-deep-volumes-quiz-1"
  lessonSlug="02-volumes-and-persistence"
  lang="ru"
  question="Контейнер Postgres без объявленного volume удалён и пересоздан при передеплое. Что станет с данными?"
  choices={[
    { label: "Их нет — данные жили в writable-слое контейнера, который удаляется вместе с контейнером; объяви named volume, чтобы их персистить", correct: true },
    { label: "Они в безопасности в /var/lib/docker/volumes, пока ты не сделаешь prune", misconception: "Ничего не писалось в volume — volume не был объявлен. Данные ушли в writable-слой контейнера, который является частью контейнера и удаляется при его удалении. В /var/lib/docker/volumes лежат только named/анонимные тома." },
    { label: "Docker автоматически снапшотит writable-слой перед удалением", misconception: "Docker не снапшотит и не сохраняет writable-слой при удалении. Слой — это собственный diff контейнера и уничтожается вместе с ним. Персистентность включается через mount, а не автоматически." },
    { label: "Они переживут, потому что образ не менялся", misconception: "Образ read-only и в любом случае никогда не держал твои рантайм-данные. Рантайм-записи идут в writable-слой поверх образа, и этот слой умирает вместе с контейнером независимо от образа." },
  ]}
/>

<Quiz
  id="09-docker-deep-volumes-quiz-2"
  lessonSlug="02-volumes-and-persistence"
  lang="ru"
  question="Твой образ ставит node_modules на /app при сборке. Затем ты bind-монтируешь пустую папку хоста на /app в рантайме. Что увидит контейнер?"
  choices={[
    { label: "Пустой /app — bind mount затеняет содержимое образа по этому пути, скрывая установленные node_modules на всё время mount'а", correct: true },
    { label: "node_modules образа, слитые с пустой папкой хоста", misconception: "Bind mounts не сливают. Mount заменяет вид контейнера по этому пути видом хоста, поэтому файлы образа скрыты, а не объединены. Только пустой NAMED volume копирует существующие файлы образа вверх в том." },
    { label: "Ошибку, потому что по пути уже есть файлы", misconception: "Ошибки нет — mount молча проходит и заслоняет существующие файлы. Эта тишина и есть причина, почему этот баг так сложно диагностировать." },
    { label: "node_modules, потому что слой образа имеет приоритет", misconception: "Mount сидит поверх вида образа по этому пути и выигрывает; файлы слоя образа под ним и невидимы, пока bind mount активен." },
  ]}
/>

</Explanation>

<StackDiagram
  label="Storage контейнера: от read-only образа внизу вверх через эфемерный writable-слой, с тремя внешними типами mount'ов, подключёнными для персистентности"
  hue="var(--d-systems)"
  layers={[
    { label: "tmpfs mount", note: "в RAM — никогда не персистится (секреты/черновик)" },
    { label: "Bind mount", note: "путь хоста проброшен — dev-исходники" },
    { label: "Named volume", note: "/var/lib/docker/volumes — данные БД, переживают" },
    { label: "Writable-слой", note: "эфемерный — умирает с контейнером" },
    { label: "Слои образа", note: "read-only — не держат рантайм-данные" },
  ]}
  caption="Образ и writable-слой едут вместе с контейнером; writable-слой удаляется при удалении. Персистентность приходит от mount'ов, подключённых снаружи: named volume для долговечных данных, bind mount для файлов хоста, tmpfs для черновика в памяти."
/>

<KeyTakeaway>Writable-слой умирает с контейнером, персистентность намеренна: данные базы — на named volume, dev-исходники — на bind mount, секреты — на tmpfs. Volumes долговечны, но host-local, а не сетевой storage.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="09-docker-deep-volumes-retrieval"
  lang="ru"
  questions={[
    { q: "Почему данные, записанные внутри контейнера, эфемерны по умолчанию, и какие три типа mount'ов ты используешь, чтобы персистить их (или намеренно не персистить)?", a: "Контейнер — это его read-only слои образа плюс один тонкий writable-слой сверху, соединённые union-файловой системой. Каждый файл, который процесс создаёт или меняет в рантайме — каталог базы, загрузка, лог, — попадает в этот writable-слой, привязанный к жизненному циклу контейнера. Удали контейнер (docker rm, compose down, передеплой, который пересоздаёт его) — и writable-слой удаляется вместе с ним, поэтому данные действительно исчезли, а не припрятаны на хосте. Он также раздувает контейнер и пишет через более медленную union-абстракцию, поэтому это черновик, а не хранилище. Чтобы персистить данные, ты монтируешь storage вне writable-слоя, и типов три. Named volume — Docker-управляемый storage под /var/lib/docker/volumes, к которому обращаются по имени, — рекомендуемый выбор для баз и stateful-данных, потому что он отвязан от путей хоста, портируем, бэкапится, пишет прямо в файловую систему хоста и переживает удаление контейнера. Bind mount мапит конкретный путь хоста прямо в контейнер — идеален для live-reload исходников в dev, но привязывается к раскладке хоста и затеняет любое содержимое, которое образ уже положил по этому пути. tmpfs mount живёт только в RAM и стирается при остановке контейнера — для секретов и черновика в памяти, которые ты никогда не хочешь на диске." },
    { q: "Почему `docker volume prune` опасен и почему локальных volumes недостаточно для многоузлового деплоя?", a: "У volumes жизненный цикл, независимый от контейнеров: volume создаётся по требованию, переживает удаление контейнера by design и никогда не чистится автоматически — он сидит на диске, пока ты явно не удалишь его через docker volume rm или docker volume prune. Эта независимость — нужная долговечность, но она делает prune опасным: docker volume prune удаляет каждый volume, не прикреплённый сейчас к запущенному контейнеру, а volume, чей контейнер ты удалил на прошлой неделе, считается неиспользуемым. Поэтому небрежный prune ради освобождения диска может стереть базу, которую ты полностью собирался оставить. Дополнительный сюрприз — протухшие volumes молча заполняют диск под /var/lib/docker/volumes. Отдельно: volumes host-local — named volume живёт на файловой системе одной машины, поэтому в кластере из нескольких узлов (Swarm, Kubernetes, несколько VM) volume на узле A невидим контейнеру, запланированному на узел B. Настоящая многоузловая персистентность требует сетевого или объектного storage — управляемой базы, S3-совместимого объектного хранилища или CSI/network-volume драйвера, — а не простого локального volume. Считать локальный volume общим хранилищем — классический провал, когда одноузловой сетап дорастает до кластера." },
  ]}
/>

<Recap lang="ru">
Запущенный контейнер — это его read-only слои образа плюс один тонкий writable-слой, и этот writable-слой привязан к жизненному циклу контейнера: удали контейнер — и всё записанное там удаляется вместе с ним, поэтому Postgres без volume теряет данные на следующем передеплое. Персистентность включается намеренно, через один из трёх mount'ов, подключённых вне writable-слоя. Named volume — Docker-управляемый storage под /var/lib/docker/volumes, к которому обращаются по имени; он отвязан от путей хоста, портируем, бэкапится, пишет прямо в файловую систему хоста и переживает удаление контейнера, что делает его дефолтом для баз и любых stateful-данных. Bind mount мапит конкретный путь хоста прямо внутрь — идеален для live-reload в dev, но привязывается к раскладке хоста и молча затеняет всё, что образ уже наполнил по этому пути, поэтому никогда не bind-монтируй поверх каталога вроде build-time node_modules, если не хочешь его спрятать. tmpfs mount живёт только в RAM и исчезает при stop — для секретов и черновика, которые ты никогда не хочешь на диске. У volumes независимый жизненный цикл: создаются по требованию, переживают удаление контейнера и удаляются только явно — поэтому `docker volume prune` может стереть базу, которую ты собирался оставить, и поэтому мёртвые тома тихо съедают диск. Наконец, volumes host-local: в многоузловом кластере тебе нужен сетевой или объектный storage, а не простой локальный volume. Теперь, когда открываешь чужой compose-файл, первым делом смотри — объявлен ли named volume у каждого stateful-контейнера: одна пропущенная строка — и следующий передеплой превращается в потерю данных.
</Recap>
