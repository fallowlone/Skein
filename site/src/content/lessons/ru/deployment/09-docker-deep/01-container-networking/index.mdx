---
concepts:
- network-drivers
- bridge-network
- user-defined-bridge-dns
- port-publishing
- localhost-trap
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-container-networking
sources:
- https://docs.docker.com/network/
- https://docs.docker.com/network/drivers/bridge/
- https://docs.docker.com/reference/cli/docker/container/run/
spiral: []
status: ready
summary: 'Контейнеры общаются через сетевые драйверы — по умолчанию bridge. Пользовательский bridge добавляет DNS по имени контейнера; дефолтный — нет. Публикуй порты только для доступа снаружи и не верь localhost внутри контейнера.'
title: 'Сеть контейнеров: bridge, DNS по имени и ловушка localhost'
track: deployment
unit: 09-docker-deep
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Два контейнера, `api` и `db`, оба запущены, оба здоровы. На каждый запрос `api` пишет в лог `ECONNREFUSED 127.0.0.1:5432`. Разработчик клянётся, что база поднята — `docker ps` это подтверждает — и сжигает полдня, проверяя конфиг Postgres, файрволы и креды. Баг не в этом. В строке подключения стоит `localhost`, а внутри контейнера `api` `localhost` означает сам контейнер `api`, где на 5432 никто не слушает. База — в одном сетевом хопе, доступна по имени `db` — но только если оба контейнера в правильном типе network.
</Hook>

<Crux>Контейнеры общаются через драйвер — по умолчанию bridge. Пользовательский bridge резолвит имена контейнеров в IP; дефолтный — нет.</Crux>

<Explanation>

За десять минут ты поймёшь, почему `localhost` врёт внутри контейнера, какой драйвер выбрать и как связать сервисы так, чтобы они не теряли друг друга после рестарта.

## Драйверы: как контейнер вообще получает сеть

Контейнер стартует в своём network namespace — со своими интерфейсами, таблицей маршрутизации и `localhost`. **Сетевой драйвер** решает, как этот namespace соединяется со всем остальным. Драйвер ты выбираешь, создавая network или запуская контейнер.

Драйвер **bridge** — это умолчание. Docker создаёт на хосте приватный виртуальный коммутатор (`docker0` для дефолтной сети) и подключает к нему каждый контейнер виртуальной ethernet-парой. Контейнеры получают внутренние IP в приватной подсети (часто `172.17.0.0/16`), общаются друг с другом через этот bridge и достигают внешнего мира через NAT — хост маскирует их трафик за своим IP. Снаружи хоста эти IP контейнеров невидимы, пока ты не опубликуешь порт.

Драйвер **host** убирает изоляцию полностью: контейнер делит network namespace хоста. Нет отдельного IP контейнера, нет NAT и нет проброса портов — если приложение биндится на `:8080`, оно на `:8080` хоста напрямую. Это самый быстрый вариант (нет bridge, нет хопа через NAT) и причина использовать его для чувствительных к задержке или высоконагруженных ворклоадов, но ты теряешь изоляцию и не можешь запустить два контейнера, которым нужен один порт.

Драйвер **none** даёт контейнеру namespace только с loopback — вообще без внешней связности, для полностью изолированной работы. **overlay** охватывает несколько хостов (сервисы Swarm, мультихостовые кластеры), туннелируя трафик контейнеров между демонами. **macvlan** выдаёт контейнеру собственный MAC-адрес, так что он выглядит физическим устройством в LAN, — для легаси-систем, которые этого ждут. Повседневная рабочая лошадка — bridge; остальное ситуативно.

```bash
docker run -d --name web nginx                 # дефолтный bridge
docker run -d --network host nginx             # делит стек хоста, -p не нужен
docker run -d --network none alpine sleep 1d   # только loopback, изолирован
docker network ls                              # bridge, host, none + созданные тобой
```

## Дефолтный bridge против пользовательского bridge

Вот ключевой сеньорский момент, на котором держится хук. Существуют два вида bridge-сети, и они не равнозначны.

**Дефолтный bridge** (тот, что зовётся `bridge`, к которому подключается каждый контейнер, если не сказано иное) не имеет встроенного DNS. Контейнеры на нём достают друг друга только по сырому IP-адресу — что хрупко, потому что IP назначаются динамически и меняются между перезапусками. Старым обходом был устаревший флаг `--link`.

**Пользовательский bridge** — любая сеть, которую ты создаёшь сам — добавляет встроенный DNS-сервер. Контейнеры на нём резолвят друг друга **по имени контейнера** автоматически. Запусти `db` и `api` в одной пользовательской network — и `api` сможет подключиться к `db:5432` без IP, без link, без конфига. Это service discovery, и это единственная причина всегда помещать связанные контейнеры в созданную тобой network, а не в дефолтный bridge.

```bash
docker network create appnet                              # пользовательский bridge
docker run -d --name db  --network appnet postgres:16
docker run -d --name api --network appnet my/api          # api достаёт db по имени "db"
```

Именно это делает за тебя `docker compose`: каждый проект Compose получает собственный пользовательский bridge, так что сервисы достают друг друга по имени сервиса из коробки. Руками network ты создаёшь редко, потому что Compose уже это сделал.

<Inset kind="why" lang="ru">
На пользовательском bridge резолвинг имён динамичен: если `db` перезапустится и получит новый IP, встроенный DNS вернёт новый адрес при следующем запросе, так что `api` продолжит работать без перезапуска. У дефолтного bridge такого DNS нет — поэтому захардкоженный там IP контейнера ломается на следующем перезапуске. DNS Docker'а ещё и форвардит неизвестные ему имена резолверу хоста, так что внешние хостнеймы из контейнера резолвятся нормально.
</Inset>

## Публикация портов против общения внутри сети

Два контейнера в одной пользовательской network общаются **напрямую на собственном порту контейнера** — `api` бьёт в `db:5432` независимо от того, опубликован ли 5432. Публикация — только про доступ к контейнеру **снаружи Docker-сети**, обычно с хоста или из интернета.

`-p host:container` (`--publish`) маппит порт хоста на порт контейнера через NAT bridge'а. `-p 8080:80` значит «отправь хостовый :8080 в этот контейнерный :80». Ты публикуешь сервис, обращённый наружу (веб-сервер, API-шлюз), а внутренние сервисы вроде базы оставляешь без публикации — им не нужен хостовый порт, потому что их клиенты в той же network.

Частая путаница: **`EXPOSE` в Dockerfile ничего не публикует.** Это документация — метаданные, объявляющие, какой порт слушает приложение. Он не открывает ни одного хостового порта и не меняет файрвол. Только `-p` (или `ports:` в Compose) реально публикует; `EXPOSE` лишь сообщает читателям и инструментам намерение.

```bash
docker run -d --network appnet -p 8080:80 --name web my/web   # host:8080 → container:80
# у db НЕТ -p: с хоста недоступен, но web достаёт его на db:5432 внутри
```

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Драйвер</th>
        <th class="border border-border px-3 py-2 text-left">Изоляция</th>
        <th class="border border-border px-3 py-2 text-left">DNS по имени</th>
        <th class="border border-border px-3 py-2 text-left">Проброс портов</th>
        <th class="border border-border px-3 py-2 text-left">Для чего</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>bridge</code> (пользовательский)</td>
        <td class="border border-border px-3 py-2">Свой namespace + IP</td>
        <td class="border border-border px-3 py-2">Да (по имени контейнера)</td>
        <td class="border border-border px-3 py-2"><code>-p</code> для доступа снаружи</td>
        <td class="border border-border px-3 py-2">Умолчание для многоконтейнерных приложений</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>bridge</code> (дефолтный)</td>
        <td class="border border-border px-3 py-2">Свой namespace + IP</td>
        <td class="border border-border px-3 py-2">Нет (только IP)</td>
        <td class="border border-border px-3 py-2"><code>-p</code> для доступа снаружи</td>
        <td class="border border-border px-3 py-2">Быстрые разовые запуски; не для связки приложения</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>host</code></td>
        <td class="border border-border px-3 py-2">Нет (делит стек хоста)</td>
        <td class="border border-border px-3 py-2">Н/Д (резолвер хоста)</td>
        <td class="border border-border px-3 py-2">Нет &#123;без <code>-p</code>&#125;</td>
        <td class="border border-border px-3 py-2">Минимум задержки, один владелец порта</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>none</code></td>
        <td class="border border-border px-3 py-2">Полная (только loopback)</td>
        <td class="border border-border px-3 py-2">Нет</td>
        <td class="border border-border px-3 py-2">Нет связности</td>
        <td class="border border-border px-3 py-2">Полностью изолированные задачи</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>overlay</code></td>
        <td class="border border-border px-3 py-2">Свой namespace + IP</td>
        <td class="border border-border px-3 py-2">Да (сервисы Swarm)</td>
        <td class="border border-border px-3 py-2">Публикуется на сервис</td>
        <td class="border border-border px-3 py-2">Мультихостовые / Swarm-кластеры</td>
      </tr>
    </tbody>
  </table>
</div>

## Ловушка localhost

Теперь хук разрешается. Внутри контейнера `localhost` (`127.0.0.1`) — это **собственный loopback контейнера**, не хост и не какой-либо другой контейнер. У каждого контейнера свой loopback-интерфейс в своём namespace. Поэтому `api`, подключаясь к `localhost:5432`, спрашивает Postgres *у самого себя*, ничего слушающего не находит и получает `ECONNREFUSED`.

Фиксы следуют из всего сказанного выше:

- **Чтобы достать другой контейнер**, используй его имя в общей пользовательской network: `db:5432`. Никогда `localhost`, никогда захардкоженный IP.
- **Чтобы достать сервис на самой хост-машине** изнутри контейнера (например, базу, запущенную нативно на твоём ноутбуке), используй `host.docker.internal` — специальное DNS-имя, которое Docker резолвит в IP хост-машины. (`host`-сеть — другой путь: тогда `localhost` *и есть* хост, потому что отдельного namespace нет.) (`host`-сеть — другой путь: тогда `localhost` *и есть* хост, потому что отдельного namespace нет.)
- **Чтобы достать контейнер с хоста**, опубликуй порт через `-p` и подключайся к `localhost:<hostPort>` на хосте.

Вместе эти три правила означают одно: `localhost` — только «этот же контейнер», и без явного имени первый же рестарт в проде тихо ломает связку. В тот миг, когда ты пересекаешь границу контейнера, тебе нужно имя — `db`, `api` или `host.docker.internal`.

<Quiz
  id="01-container-networking-quiz-1"
  lessonSlug="01-container-networking"
  lang="ru"
  question="Контейнер api подключается к localhost:5432, чтобы достать контейнер db, оба в одной пользовательской network, и получает ECONNREFUSED. Что не так?"
  choices={[
    { label: "Внутри контейнера api localhost — это собственный loopback api, не db; нужно подключаться к db:5432, имени контейнера, которое резолвит DNS пользовательской network", correct: true },
    { label: "Контейнеру db нужен опубликованный -p 5432:5432, чтобы api его достал", misconception: "Публикация — только для доступа снаружи Docker-сети. Контейнеры в одной пользовательской network достают друг друга на порту контейнера напрямую — db:5432 работает вообще без -p." },
    { label: "Пользовательские bridge не поддерживают трафик между контейнерами", misconception: "Это и есть рекомендованный способ связывать контейнеры — они добавляют автоматический DNS по имени поверх обычной bridge-связности. Проблема в адресе localhost, а не в network." },
    { label: "Двум контейнерам нужно делить драйвер host", misconception: "Host-сеть сделала бы localhost хостом, что всё равно не там, где слушает db, если db не на host тоже. Чистый фикс — резолвинг имён на общем пользовательском bridge: db:5432." },
  ]}
/>

<Quiz
  id="01-container-networking-quiz-2"
  lessonSlug="01-container-networking"
  lang="ru"
  question="В Dockerfile есть EXPOSE 80, но контейнер запущен без флага -p. Достанешь ли ты приложение из браузера на хосте?"
  choices={[
    { label: "Нет — EXPOSE это только документация; без -p (или ports: в Compose) ни один хостовый порт не опубликован, так что ничего на хосте не маршрутизирует в контейнер", correct: true },
    { label: "Да, EXPOSE открывает порт на хосте автоматически", misconception: "EXPOSE ничего не публикует. Это метаданные, объявляющие порт прослушивания для людей и инструментов; только -p / --publish или ports: в Compose реально маппят хостовый порт на контейнер." },
    { label: "Да, но только на localhost внутри контейнера", misconception: "Приложение действительно может слушать внутри контейнера, но без опубликованного порта у хоста нет маршрута внутрь. EXPOSE этого не меняет — тебе всё равно нужен -p." },
    { label: "Нет, потому что EXPOSE блокирует внешний доступ ради безопасности", misconception: "EXPOSE ничего не открывает и не блокирует — это инертная документация. Причина, по которой ты его не достаёшь, — просто отсутствие опубликованного порта." },
  ]}
/>

<TradeoffMatrix
  id="01-container-networking-tradeoff-1"
  lessonSlug="01-container-networking"
  lang="ru"
  prompt="Многоконтейнерное приложение (web + api + db) работает на одном хосте, и web-уровень должен быть доступен из интернета. Выбери сетевую схему."
  options={[
    { name: "Пользовательский bridge для всех трёх; публикуй только web-порт через -p; сервисы общаются по имени (api, db)", summary: "DNS по имени, минимальная экспозиция.", correct: true, justification: "Это стандартный выбор сеньора. Пользовательский bridge даёт автоматический DNS, так что web достаёт api, а api достаёт db по имени, без захардкоженных IP и без хрупкости при перезапусках. Публикация только web-порта держит базу и внутренний api вне хоста и вне интернета — они доступны исключительно изнутри network. Это ровно та топология, которую строит за тебя docker compose." },
    { name: "Дефолтный bridge для всех трёх; соединяться по IP контейнера, найденному через docker inspect", summary: "Работает до перезапуска.", justification: "У дефолтного bridge нет DNS, поэтому ты откатываешься к сырым IP, которые Docker назначает динамически и перетасовывает на перезапуске — твоя связка ломается в следующий раз, когда контейнер перезапустится. Ему также не хватает удобств изоляции пользовательской network. Создай свою network и используй имена." },
    { name: "Host-сеть для всех трёх, чтобы они делили стек хоста и достали друг друга на localhost", summary: "Нет изоляции, конфликт портов.", justification: "Host-сеть снимает изоляцию и убирает порты на контейнер, так что три сервиса, дерущиеся за портовое пространство хоста, сталкиваются, а каждый слушающий порт оголён на хосте без шлюза -p. Это может подойти одному критичному к задержке сервису, но это неверный выбор для многосервисного приложения, которому нужны только-внутренние уровни." },
    { name: "Помести каждый контейнер в свою отдельную пользовательскую network", summary: "Изолировано до поломки.", justification: "DNS по имени и прямая связность работают только между контейнерами в ОДНОЙ network. Изолируя каждый, ты делаешь так, что web вообще не может резолвить или достать api или db. Сегментация сетей полезна для границ безопасности, но уровни, которые должны общаться, обязаны делить network." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Два контейнера на пользовательском bridge достают друг друга по имени, а bridge NAT-ит исходящий трафик, и опубликованный порт впускает хост"
  hue="var(--d-systems)"
  perRow="3"
  nodes={[
    { id: "host", label: "Хост", sub: ":8080 опубл.", col: 0, row: 0 },
    { id: "bridge", label: "Польз. bridge", sub: "appnet + DNS", col: 1, row: 0 },
    { id: "web", label: "web", sub: ":80", col: 2, row: 0 },
    { id: "db", label: "db", sub: ":5432", col: 2, row: 1 },
    { id: "net", label: "Интернет", sub: "через NAT", col: 0, row: 1 },
  ]}
  edges={[
    { from: "host", to: "bridge", label: "-p 8080:80" },
    { from: "bridge", to: "web", label: "" },
    { from: "web", to: "db", label: "db:5432 по имени" },
    { from: "bridge", to: "net", label: "NAT out" },
  ]}
  caption="Контейнеры на одном пользовательском bridge достают друг друга по имени (web → db:5432) без опубликованного порта. -p маппит порт хоста внутрь; исходящий трафик уходит через NAT bridge'а. Внутри web localhost означал бы сам web — никогда db."
/>

<KeyTakeaway>Пользовательский bridge даёт DNS, так что контейнеры достают друг друга по имени — всегда связывай их на нём. Публикуй (`-p`) лишь для доступа снаружи; `EXPOSE` не публикует. А `localhost` — это сам контейнер, не сосед.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-container-networking-retrieval"
  lang="ru"
  questions={[
    { q: "Почему пользовательский bridge даёт контейнерам доставать друг друга по имени, а дефолтный — нет, и что это меняет на практике?", a: "Каждая Docker-сеть использует драйвер; bridge — это умолчание, он строит на хосте приватный виртуальный коммутатор, подключающий контейнеры виртуальными ethernet-парами, выдаёт каждому внутренний IP в приватной подсети и NAT наружу. Решающая разница — DNS. У дефолтной сети с именем bridge нет встроенного DNS, поэтому контейнеры на ней могут адресовать друг друга только по сырому IP — который Docker назначает динамически и меняет между перезапусками, делая связку хрупкой (старым фиксом был устаревший --link). Любая сеть, созданная тобой самим — пользовательский bridge — запускает встроенный DNS-сервер, резолвящий имена контейнеров в их текущие IP автоматически. Так что если db и api делят пользовательскую network, api подключается к db:5432 без IP, без link и без конфига, и это продолжает работать, даже если db перезапустится с новым IP, потому что следующий DNS-запрос вернёт новый адрес. На практике это service discovery, и поэтому ты всегда помещаешь связанные контейнеры в созданную тобой network — что ровно и делает docker compose: каждый проект получает собственный пользовательский bridge, так что сервисы резолвят друг друга по имени сервиса из коробки." },
    { q: "Что означает localhost внутри контейнера и как достать (а) другой контейнер, (б) сервис на хост-машине и (в) контейнер с хоста?", a: "У каждого контейнера свой network namespace со своим loopback, поэтому внутри контейнера localhost (127.0.0.1) — это сам контейнер, не хост и не сосед. Подключение к localhost:5432 из контейнера api спрашивает Postgres у api, ничего не находит и возвращает ECONNREFUSED; это и есть классический баг. Чтобы достать (а) другой контейнер, используй его имя в общей пользовательской network — db:5432 — никогда localhost или захардкоженный IP, поскольку встроенный DNS резолвит имя в живой адрес. Чтобы достать (б) сервис, запущенный нативно на хост-машине, изнутри контейнера, используй host.docker.internal, который Docker резолвит в адрес хоста; либо запусти контейнер с host-сетью — тогда отдельного namespace нет и localhost действительно означает хост. Чтобы достать (в) контейнер с хоста, опубликуй порт через -p hostPort:containerPort и подключайся к localhost:hostPort на хосте — заметь, что публикация нужна только для входа в Docker-сеть снаружи; контейнеры в одной network уже общаются на порту контейнера напрямую, а один EXPOSE не публикует ничего." },
  ]}
/>

<Recap lang="ru">
Контейнер стартует в своём network namespace, и сетевой драйвер решает, как он соединяется. Bridge — умолчание: приватный виртуальный коммутатор на хосте даёт каждому контейнеру внутренний IP и NAT-ит его исходящий трафик, тогда как host-сеть делит стек хоста без изоляции и без проброса портов, none даёт только loopback, а overlay и macvlan покрывают мультихостовые случаи и присутствие в LAN. Сеньорское различие — между дефолтным bridge, у которого нет DNS и который вынуждает к хрупкой адресации по IP, и пользовательским bridge, чей встроенный DNS позволяет контейнерам резолвить друг друга по имени контейнера — автоматический service discovery, и причина, по которой docker compose создаёт network на проект, чтобы сервисы доставали друг друга по имени. Публикация через -p маппит порт хоста на порт контейнера и нужна только для доступа снаружи Docker-сети; контейнеры в одной пользовательской network уже общаются на порту контейнера напрямую, а EXPOSE — лишь документация, не публикующая ничего. Наконец, localhost внутри контейнера — это собственный loopback этого контейнера, поэтому достань соседа по имени (db:5432), хост через host.docker.internal, а контейнер с хоста — через опубликованный порт; никогда не считай, что localhost пересекает границу контейнера. Теперь, когда видишь ECONNREFUSED на 127.0.0.1, первый вопрос — в каком network namespace на самом деле работает приложение?
</Recap>
