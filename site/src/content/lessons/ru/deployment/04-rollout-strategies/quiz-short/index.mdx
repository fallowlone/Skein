---
concepts:
- rolling-update
- blue-green
- canary
- readiness-probe
- progressive-delivery
- expand-contract
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://kubernetes.io/docs/concepts/workloads/controllers/deployment/
- https://argo-rollouts.readthedocs.io/en/stable/features/canary/
- https://spring.io/blog/2016/05/31/zero-downtime-deployment-with-a-database/
status: ready
summary: "Промпты на свободное воспроизведение по всему разделу. Сначала ответьте по памяти, затем раскройте образцовый ответ и сравните."
title: 'Rollout strategies: обзор на воспроизведение'
track: deployment
unit: 04-rollout-strategies
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение по памяти сильнее перечитывания. На каждый промпт скажите или запишите полный ответ из памяти до того, как откроете образец — именно усилие припоминания закрепляет материал.
</Hook>

<Goal lang="ru">
Восстановите ключевые механизмы раздела — три оси компромисса, maxSurge/maxUnavailable, почему readiness-гейтинг несущий, метрические gate канарейки и безопасный для схемы rollback — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:load
  id="04-rollout-strategies-short-retrieval"
  lang="ru"
  questions={[
    { q: "Назовите четыре стратегии выката и три оси, между которыми вы выбираете.", a: "Четыре — это recreate, rolling update, blue-green и canary. Три оси: blast radius (сколько пользователей заденет плохой релиз, пока вы его не поймали — canary минимальный, recreate тотальный), стоимость ресурсов (blue-green примерно удваивает, запуская два полных окружения; canary и rolling переиспользуют один пул; recreate самый дешёвый) и скорость rollback (blue-green и canary почти мгновенны через переключение роутера или сдвиг веса в ноль; rolling катится назад под за подом; recreate означает второй простой). Универсально лучшего выбора нет — выбираете по тому, какая ось важнее для сервиса, а решающий фактор обычно — насколько хороша ваша observability." },
    { q: "Чем управляют maxSurge и maxUnavailable, каковы их дефолты и какова настройка zero-downtime?", a: "Оба — knobs rolling update в Kubernetes. maxSurge — сколько подов сверх желаемого числа реплик может существовать во время выката; maxUnavailable — сколько из желаемых подов может отсутствовать разом. Оба по умолчанию 25%. Для строгого zero-downtime ставят maxSurge: 1, maxUnavailable: 0 — никогда не падать ниже полной ёмкости (ни один старый под не уходит, пока его замена не готова) и добавлять максимум один surge-под для продвижения. Цена — более медленный выкат, по одному поду, плюс ёмкость на surge-под. Одних knobs мало: они гарантируют ёмкость, но всё равно нужен readiness probe, чтобы трафик удерживался, пока новый под не сможет реально обслуживать." },
    { q: "Почему readiness probe несущий во время rolling update и чем он отличается от liveness probe?", a: "Kubernetes направляет трафик в новый под в момент пометки ready, а по умолчанию под готов, как только стартует процесс контейнера — до того, как приложение открыло соединения к БД или прогрело кэши. Без реального readiness probe трафик льётся в недозагруженный процесс, и часть запросов падает с 502, пока Deployment по-прежнему сообщает 'available'. Readiness probe возвращает успех только когда приложение может обслужить запрос, поэтому Kubernetes удерживает трафик (плюс minReadySeconds для стабилизации) до этого момента. Readiness лишь убирает под из ротации балансировщика; liveness перезапускает под, который считает мёртвым. Путать их опасно — liveness probe, нацеленный на медленную зависимость, превращает икоту бэкенда в шторм рестартов." },
    { q: "Что делает canary менее рискованной, чем blue-green, и что единственное должно быть на месте, чтобы эта выгода была реальной?", a: "Canary направляет малый срез трафика — обычно 5%, затем 25%, затем 50% — в новую версию и наращивает только если метрики остаются здоровыми, так что плохой релиз бьёт по 5% пользователей вместо 100%. Blue-green переключает весь роутер разом, открывая весь трафик в момент cutover. Выгода реальна только с автоматическим метрическим gate: шагом анализа (Argo Rollouts или Flagger, читающие SLO-метрики вроде error-rate и latency), который автоматически приостанавливает или откатывает без человека в цикле. Это progressive delivery. Canary без gate — просто медленный ручной выкат: уставший инженер, глядящий на дашборд, всё равно промоутит сбойный релиз до 100%. Крошечный blast radius бесполезен, если вы не читаете срез канарейки." },
    { q: "Почему 'мгновенный rollback' blue-green проваливается на изменении схемы и как это чинит expand-contract?", a: "Blue-green переключает приложение атомарно, но blue и green делят одну БД — схема не переключается вместе с роутером. Если деплой green дропает или переименовывает колонку, живая схема имеет форму только под green; blue, по-прежнему ваша цель rollback, ей больше не соответствует, поэтому откат назад падает на схеме, которую не может прочитать. Expand-contract (parallel change) делает каждое изменение схемы многошаговым: сначала expand (добавить новую колонку или таблицу, оставить старую, dual-write, чтобы обе формы были заполнены) — аддитивно и обратносовместимо, так что и старый, и новый код работают; затем, только после полного вывода старой версии и когда rollback уже невозможен, contract в отдельном более позднем деплое дропает старую колонку. Ни в один момент нет схемы, которую читает лишь одна версия, поэтому rollback остаётся безопасным. Это касается и rolling, и canary — любой стратегии с двумя живыми версиями нужна схема, которую читают обе." },
    { q: "Когда recreate — правильный выбор несмотря на простой, и почему другие стратегии его не заменяют?", a: "Recreate правилен, когда две версии действительно не могут сосуществовать — in-place изменение stateful-синглтона или несовместимое вперёд изменение без совместимой промежуточной схемы — и можно вытерпеть окно обслуживания. Он гасит все старые экземпляры, затем поднимает новые, гарантируя отсутствие пересечения версий, и самый дешёвый по ресурсам. Rolling и canary держат обе версии одновременно, чтобы обслуживать пересекающийся трафик — именно то, чего этот ворклоад не терпит. У blue-green оба окружения тоже живы во время cutover и он удваивает стоимость ресурсов; expand-contract не спасёт, когда совместимой промежуточной схемы нет. Поэтому когда несосуществование — жёсткое ограничение, чистая замена версий у recreate подходит, и вы просто принимаете простой, который покрывает окно обслуживания." },
  ]}
/>

<Recap lang="ru">
Если вы смогли восстановить каждый ответ из памяти, у вас в руках хребет раздела: четыре формы, взвешенные по blast radius, стоимости ресурсов и скорости rollback; maxSurge/maxUnavailable плюс readiness probe для zero-downtime rolling; автоматический метрический gate (progressive delivery) для canary; expand-contract, чтобы rollback пережил изменение схемы; и recreate, зарезервированный для ворклоадов, где версии не могут сосуществовать. Observability — рычаг, решающий, какой стратегии вы реально можете доверять.
</Recap>
