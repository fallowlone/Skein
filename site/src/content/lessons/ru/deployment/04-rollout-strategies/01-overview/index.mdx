---
concepts:
- rolling-update
- blue-green
- canary
- readiness-probe
- expand-contract-migration
- blast-radius
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://kubernetes.io/docs/concepts/workloads/controllers/deployment/
- https://argo-rollouts.readthedocs.io/en/stable/features/canary/
- https://docs.aws.amazon.com/whitepapers/latest/blue-green-deployments/best-practices-for-managing-data-synchronization-and-schema-changes.html
- https://spring.io/blog/2016/05/31/zero-downtime-deployment-with-a-database/
spiral: []
status: ready
summary: Recreate, rolling, blue-green, canary — четыре способа выкатить новую версию.
  Выбор — это размен между радиусом поражения, стоимостью ресурсов и скоростью отката,
  и все четыре несущие, только если держатся health-чеки, план отката и совместимость
  схемы.
title: 'Стратегии выката: размен радиуса поражения на стоимость ресурсов и скорость
  отката'
track: deployment
unit: 04-rollout-strategies
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Rolling update выкатывается в 14:00. Дашборды зелёные — Deployment рапортует «available», все реплики подняты. И тут 30% запросов начинают отдавать 502. Новые поды загрузились, Kubernetes пометил их ready в тот же миг, как стартанул контейнер, и трафик пошёл туда раньше, чем приложение подключилось к базе. Readiness-пробы не было. Выкат «успешно» зашёл прямо в тихий частичный отказ, и понадобилось 20 нервных минут, чтобы понять — причина в деплое.
</Hook>

<Crux>Recreate, rolling, blue-green, canary разменивают радиус поражения на стоимость ресурсов и скорость отката — все опираются на health-чеки.</Crux>

<Explanation>

## Четыре стратегии и чего стоит каждая

Релиз может принять ровно четыре формы, и выбор одной — это инженерный размен, а не дефолт. Когда понимаешь, чего стоит каждая форма, перестаёшь выбирать «rolling, потому что Kubernetes так делает» и начинаешь принимать осознанное решение.

**Recreate** (воссоздание с нуля) убивает все старые инстансы, потом стартует новые. Просто, без пересечения версий — что важно, когда две версии действительно не могут сосуществовать (несовместимое in-place изменение схемы, singleton-джоба). Цена — downtime: окно, когда ничего не обслуживает. Допустимо только для не-HA сервисов или окон обслуживания.

**Rolling update** (постепенный выкат) — дефолт Kubernetes. Заменяет поды постепенно — подними несколько новых, слей несколько старых, повтори — так сервис никогда не падает полностью. Настраивается двумя ручками: `maxSurge` (сколько лишних подов сверх желаемого количества может существовать в процессе выката) и `maxUnavailable` (сколько может отсутствовать). Оба по умолчанию **25%**.

**Blue-green** держит два полных окружения. «Blue» обслуживает прод, пока «green» получает новую версию, полностью развёрнутую и прогретую; затем ты атомарно переключаешь роутер. Откат мгновенный — переключи обратно. Цена — примерно **двойные ресурсы** во время переключения, плюс проблема совместимости с базой (ниже).

**Canary** (канарейка — стратегия с постепенным наращиванием трафика) направляет небольшой срез трафика — обычно **5%, потом 25%, потом 50%** — на новую версию, смотрит метрики на каждом шаге и наращивает только если error rate и latency остаются здоровыми. Самый маленький радиус поражения, но нужны traffic-shaping и реальная observability. Инструменты вроде Argo Rollouts и Flagger автоматизируют шаги и метрические гейты (progressive delivery — прогрессивная доставка с автоматическим контролем метрик).

Вместе четыре стратегии покрывают весь спектр компромиссов: от «выкатить быстро, приняв downtime» (recreate) до «поймать плохой релиз до того, как он дойдёт до большинства пользователей» (canary). Форма, которую ты выбираешь, фиксирует конкретную стоимость — поэтому изучи её до того, как понадобится откат.

## Rolling update: readiness-проба не опциональна

Отказ из Hook — каноничный провал rolling update. Kubernetes отправляет трафик в новый под в тот момент, когда он **ready** — а по умолчанию под считается ready, как только стартует процесс контейнера. Если приложению нужны три секунды, чтобы открыть соединения с БД и прогреть кэш, это трёхсекундное окно на каждый под, в котором ты направляешь живой трафик в процесс, который не может обслуживать.

Корректная **readiness-проба** закрывает это окно: Kubernetes придерживает трафик, пока проба не пройдёт (и не истёк `minReadySeconds`). Без неё `maxUnavailable: 25%` тебя не защищает — четверть мощности может быть одновременно «ready» и мёртвой. Безопасный для сеньора конфиг для zero-downtime: `maxSurge: 1, maxUnavailable: 0` плюс настоящая readiness-проба — никогда не падать ниже полной мощности и никогда не слать трафик в под, который не доказал, что может отвечать.

<Inset kind="why" lang="ru">
Readiness-проба и liveness-проба — не одно и то же, и путаница вызывает отказы. Liveness перезапускает под, который считает мёртвым — наведи её на медленную зависимость, и икота бэкенда вызовет шторм рестартов. Readiness лишь убирает под из ротации балансировщика. Во время выката трафик гейтит именно *readiness*; liveness-проба, делающая двойную работу, радостно поубивает поды, которые были просто заняты.
</Inset>

## Blue-green: мгновенный откат, но база не переключается

Главный козырь blue-green — атомарное переключение и мгновенный откат, который оно даёт. Ловушка в том, что ты переключаешь *приложение* атомарно, но *база общая* — она не переключается. В день, когда green выкатывает миграцию, дропающую или переименовывающую колонку, blue (всё ещё твоя цель отката) теперь сломан против живой схемы. Переключишь обратно — упадёт. Твой «мгновенный откат» испарился ровно тогда, когда он нужен.

Фикс — паттерн **expand-contract** (parallel change): никогда не делай ломающее изменение схемы за один шаг.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Фаза</th>
        <th class="border border-border px-3 py-2 text-left">Действие со схемой</th>
        <th class="border border-border px-3 py-2 text-left">Почему обе версии выживают</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Expand</strong></td>
        <td class="border border-border px-3 py-2">Добавь новую колонку/таблицу; старую оставь</td>
        <td class="border border-border px-3 py-2">Старый код игнорирует новое поле; новый читает оба — аддитивное изменение обратносовместимо</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Migrate</strong></td>
        <td class="border border-border px-3 py-2">Бэкфилл данных; dual-write старое + новое</td>
        <td class="border border-border px-3 py-2">Обе формы заполнены, поэтому откат к старому коду всё ещё находит свои данные</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Contract</strong></td>
        <td class="border border-border px-3 py-2">Дропни старую колонку — в <em>более позднем</em> деплое</td>
        <td class="border border-border px-3 py-2">Только после того, как старая версия полностью выведена и отката к ней не будет</td>
      </tr>
    </tbody>
  </table>
</div>

Это и значит «обратносовместимые изменения схемы» на практике, и это применимо к rolling и canary тоже — любая стратегия с двумя живыми версиями приложения требует схему, которую читают обе.

## Выбор: радиус поражения vs стоимость ресурсов vs скорость отката

Универсально лучшей стратегии нет; ты взвешиваешь три оси. **Радиус поражения** — скольких пользователей задевает плохой релиз до того, как ты его поймал: у canary самый маленький, у recreate — тотальный. **Стоимость ресурсов** — blue-green платит за два полных окружения; canary и rolling переиспользуют один пул; recreate дешевле всех. **Скорость отката** — blue-green и canary почти мгновенны (переключи роутер / уведи вес в ноль); rolling приходится катить *назад* под за подом; recreate — это второй downtime.

Решающий фактор — обычно качество твоей observability. Когда тянешься к canary, сначала спроси себя: могу ли я на самом деле прочитать 5%-й срез? Крошечный радиус поражения canary бесполезен, если ты не можешь по метрикам понять, что 5%-й срез падает — ты накатишь его до 100% вслепую. Без крепких дашбордов и алертов на основе SLO хорошо настроенный rolling update с readiness-пробой бьёт canary, который ты не можешь прочитать.

<TradeoffMatrix
  id="04-rollout-overview-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Платёжный API на Kubernetes выкатывает рискованный рефакторинг. У тебя есть дашборды Prometheus и SLO-алерты, достаточная ёмкость кластера и нужно минимальное возможное влияние на пользователей, если что-то пойдёт не так. Выбери стратегию выката."
  options={[
    { name: "Canary: сдвигай 5% → 25% → 50% → 100%, с гейтом по error-rate и latency на каждом шаге", summary: "Progressive delivery.", correct: true, justification: "Самый маленький радиус поражения: плохой релиз задевает 5% трафика, а метрический гейт (Argo Rollouts / Flagger, читающий Prometheus) автопаузит или откатывает до того, как ты нарастишь. Это ровно тот случай, под который сделан canary — рискованное изменение, хорошая observability, чтобы прочитать canary-срез, ёмкость, чтобы крутить обе версии. Откат — вернуть вес в ноль, почти мгновенно." },
    { name: "Recreate: положи старую версию, потом подними новую", summary: "Стоп-старт.", justification: "Максимальный радиус поражения и гарантированный downtime — все пользователи попадают на новую версию разом, с разрывом обслуживания между. Неверно для высокодоступного платёжного API; recreate — для не-HA сервисов или окон обслуживания, где версии правда не пересекаются." },
    { name: "Blue-green: разверни green полностью, атомарно переключи роутер", summary: "Атомарное переключение.", justification: "Мгновенный откат и нулевое пересечение версий, но переключение выставляет 100% трафика на новую версию в тот же миг — больший радиус поражения, чем у canary, и удвоенная стоимость ресурсов. Хорошо, когда нельзя крутить смешанные версии; здесь гейтированный накат canary ловит провал на 5%, а не на 100%." },
    { name: "Rolling update с maxSurge: 1, maxUnavailable: 0 и readiness-пробой", summary: "Дефолт K8s, настроенный.", justification: "Крепкий zero-downtime дефолт и правильный ответ, когда observability слабая — но он сдвигает трафик на новые поды по мере прохождения readiness, поэтому плохой релиз достаёт растущую долю пользователей без метрического гейта, который остановит накат. С доступными хорошими дашбордами автоматический анализ canary здесь — менее рискованный выбор." },
  ]}
/>

<Quiz
  id="04-rollout-overview-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Rolling update рапортует «available», но ~30% запросов отдают 502 сразу после деплоя. Какая самая вероятная корневая причина?"
  choices={[
    { label: "Нет readiness-пробы, поэтому Kubernetes направил трафик на поды, которые стартанули, но ещё не готовы обслуживать", correct: true },
    { label: "maxSurge выставлен слишком высоко, создалось слишком много подов", misconception: "maxSurge контролирует лишнюю мощность во время выката — больше подов не вызывает 502. Симптом (трафик в неготовые поды) указывает на отсутствующий readiness-гейт, а не на размер surge." },
    { label: "Deployment'у нужен был Recreate вместо RollingUpdate", misconception: "Recreate вызвал бы полный downtime, а не частичный поток 502. Проблема в том, что трафик доходит до полузагруженных подов, что readiness-проба предотвращает во время rolling update." },
    { label: "maxUnavailable был 0, поэтому выкат не мог продвигаться", misconception: "maxUnavailable: 0 — это безопасная настройка и не вызвала бы 502 — она лишь значит, что под не убирают до того, как замена готова. Ошибки идут от трафика в неготовые новые поды." },
  ]}
/>

<Quiz
  id="04-rollout-overview-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Ты используешь blue-green и хочешь, чтобы откат оставался безопасным. Релиз green переименовывает колонку. Что делать?"
  choices={[
    { label: "Использовать expand-contract: добавь новую колонку, dual-write, и дропни старую только в более позднем деплое после вывода blue", correct: true },
    { label: "Просто переименуй колонку в миграции green — blue-green же даёт мгновенный откат", misconception: "Приложение переключается атомарно, но база общая и не переключается. Переименование ломает blue против живой схемы, поэтому переключение обратно падает — твой мгновенный откат исчезает ровно тогда, когда он нужен." },
    { label: "Сделать бэкап перед переключением, чтобы восстановить при нужде", misconception: "Восстановление медленное и теряет записи, сделанные после переключения. Обратносовместимые (expand-contract) изменения схемы держат обе версии рабочими без деструктивного восстановления." },
    { label: "Отказаться от blue-green и использовать Recreate, чтобы версия всегда была одна", misconception: "Recreate меняет проблему миграции на гарантированный downtime, а форвард-несовместимая схема всё равно блокирует откат. Expand-contract решает реальное ограничение: схема, которую читают обе версии." },
  ]}
/>

<DragOrder
  id="04-rollout-overview-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь шаги безопасного canary-выката рискованного изменения:"
  items={[
    { id: "a", label: "Выкати обратносовместимое (expand) изменение схемы, чтобы старая и новая версии обе работали" },
    { id: "b", label: "Направь 5% трафика на новую версию" },
    { id: "c", label: "Следи за error-rate и latency против SLO на этом шаге" },
    { id: "d", label: "Если здорово — наращивай 25% → 50% → 100%; если нет — верни вес в 0 (откат)" },
    { id: "e", label: "Когда полностью на новой версии и стабильно — contract: дропни старую схему в более позднем деплое" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="Rolling update поднимает новый под, придерживает трафик, пока не пройдёт его readiness-проба, затем сливает старый под — постепенно повторяя, так что сервис никогда не падает ниже мощности"
  hue="var(--d-systems)"
  perRow={5}
  nodes={[
    { id: "old", label: "Старые поды", sub: "v1 обслуживает" },
    { id: "new", label: "Новый под", sub: "v2 грузится" },
    { id: "probe", label: "Readiness", sub: "гейт трафика" },
    { id: "route", label: "Service", sub: "шлёт на v2" },
    { id: "drain", label: "Слить старый", sub: "v1 убран" },
  ]}
  edges={[
    { from: "old", to: "new", label: "surge +1" },
    { from: "new", to: "probe", label: "ждёт" },
    { from: "probe", to: "route", label: "прошла" },
    { from: "route", to: "drain", label: "потом" },
  ]}
  caption="Трафик переходит на новый под только после прохождения readiness-пробы; без этого гейта Service шлёт на под, который стартанул, но ещё не может обслуживать — тихий частичный отказ. Повторяй на каждый под, пока v2 полностью не заменит v1."
/>

<KeyTakeaway>Выбирай выкат, разменивая радиус поражения (canary меньший) vs стоимость (blue-green удваивает) vs скорость отката. Всем нужны readiness-проба, план отката и expand-contract миграции.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-rollout-overview-retrieval"
  lang="ru"
  questions={[
    { q: "Rolling update рапортует успех, но пользователи видят периодические 502. Объясни почему и какое одно изменение это чинит.", a: "Kubernetes сдвигает трафик на новый под в тот момент, когда он помечен ready, а по умолчанию под считается ready, как только стартует процесс контейнера — до того, как приложение открыло соединения с базой, прогрело кэши, установило downstream-клиентов. В это окно старта под уже в ротации балансировщика, но реально обслуживать не может, поэтому часть запросов попадает в полузагруженный процесс и падает с 502. Deployment всё равно рапортует «available», потому что доступность считает ready-поды, а поды технически прошли (несуществующую) планку readiness. Фикс — настоящая readiness-проба, которая возвращает успех только когда приложение может обслужить запрос; Kubernetes тогда придерживает трафик, пока она не пройдёт, а minReadySeconds добавляет буфер стабилизации. В сочетании с maxUnavailable: 0 ты никогда не падаешь ниже полной здоровой мощности во время выката." },
    { q: "Почему «мгновенный откат» blue-green ломается, когда релиз меняет схему, и как expand-contract его восстанавливает?", a: "Blue-green переключает приложение атомарно, но blue и green делят одну базу — схема не переключается с роутером. Если деплой green дропает или переименовывает колонку, живая схема теперь под форму green. Blue, всё ещё твоя цель отката, ей больше не соответствует, поэтому переключение обратно на blue падает против схемы, которую он не может прочитать — мгновенный откат испаряется ровно тогда, когда прод горит. Expand-contract делает каждое изменение схемы многошаговым parallel change: сначала expand (добавь новую колонку/таблицу, оставь старую, dual-write, чтобы обе формы были заполнены) — это аддитивно и обратносовместимо, поэтому старый и новый код оба работают. Только после того, как старая версия полностью выведена и откат уже невозможен, делаешь contract в отдельном более позднем деплое, дропая старую колонку. Ни в один момент нет схемы, которую читает только одна версия, поэтому откат остаётся безопасным на всём переключении." },
  ]}
/>

<Recap lang="ru">
Четыре формы выката, один размен. Recreate прост, но берёт downtime — годится только для не-HA сервисов. Rolling update — дефолт Kubernetes, настраивается через maxSurge и maxUnavailable (оба по умолчанию 25%), и он безопасен только с корректной readiness-пробой: без неё трафик идёт в поды, которые стартанули, но обслуживать не могут, давая «успешный» деплой прямо в тихий частичный отказ. Blue-green даёт атомарное переключение и мгновенный откат ценой двойных ресурсов — но общая база не переключается, поэтому ломающая миграция убивает твою цель отката, если не использовать expand-contract (добавь, dual-write, дропни позже). У canary самый маленький радиус поражения — 5% → 25% → 50% с гейтом по метрикам, автоматизируется через Argo Rollouts или Flagger — но он хорош ровно настолько, насколько хороша observability, читающая canary-срез. Выбирай, взвешивая радиус поражения против стоимости ресурсов против скорости отката, и помни несущий пререквизит под всеми четырьмя: health-чеки, план отката и обратносовместимые изменения схемы. Теперь, когда встретишь Deployment с отчётом «available», пока пользователи получают 502, твой первый вопрос — есть ли настоящая readiness-проба; а увидишь rename колонки, летящий в blue-green выкат, — знаешь, что нужен expand-contract до переключения роутера.
</Recap>
