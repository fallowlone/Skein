---
concepts:
- rolling-update
- blue-green
- canary
- maxsurge-maxunavailable
- readiness-probe
- expand-contract
estMin: 14
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://kubernetes.io/docs/concepts/workloads/controllers/deployment/
- https://argo-rollouts.readthedocs.io/en/stable/features/canary/
- https://docs.aws.amazon.com/whitepapers/latest/blue-green-deployments/best-practices-for-managing-data-synchronization-and-schema-changes.html
status: ready
summary: "Синтез всего раздела в формате выбора — компромиссы стратегий, maxSurge/maxUnavailable, readiness probe, gate канарейки и безопасный для схемы rollback."
title: 'Rollout strategies: обзор с выбором ответа'
track: deployment
unit: 04-rollout-strategies
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, проходящих сквозь весь раздел. Каждый — это решение, которое вы принимаете во время реального релиза: не определение для пересказа, а компромисс, который надо взвесить, пока на сервис идёт трафик.
</Hook>

<Goal lang="ru">
Убедитесь, что умеете связать выбор стратегии, knobs rolling update (maxSurge и maxUnavailable — параметры, управляющие скоростью и ёмкостью выката), readiness-гейтинг, метрические gate канарейки и безопасный для схемы rollback — тот синтез, к которому вёл обзорный урок.
</Goal>

<Quiz
  id="04-rollout-strategies-choice-1"
  lessonSlug="04-rollout-strategies"
  lang="ru"
  question="Платёжный API на Kubernetes выкатывает рискованный рефакторинг. У вас есть дашборды Prometheus, SLO-алерты и запас ёмкости кластера, и вы хотите минимально возможное влияние на пользователей, если что-то пойдёт не так. Какая стратегия и почему?"
  choices={[
    { label: "Canary — переключайте 5%, затем 25%, затем 50% с gate по error-rate и latency на каждом шаге, так что плохой релиз заденет лишь срез в 5%, а метрический gate автоматически приостановит или откатит до того, как вы нарастите долю.", correct: true },
    { label: "Recreate — погасить старую версию, затем поднять новую, чтобы в каждый момент жила только одна версия.", misconception: "У recreate максимальный blast radius и гарантированный простой: все пользователи бьют в новую версию одновременно, с разрывом обслуживания посередине. Это для не-HA сервисов или окон обслуживания, а не для высокодоступного платёжного API." },
    { label: "Blue-green — полностью развернуть green, затем атомарно переключить роутер.", misconception: "Blue-green даёт мгновенный rollback, но переключение в момент cutover открывает новую версию сразу всему трафику — blast radius больше, чем у canary, при удвоении ресурсов. С хорошей observability gate канарейки ловит сбой на 5%, а не на 100%." },
    { label: "Rolling update с дефолтами — observability не меняет того, какая стратегия правильна.", misconception: "Именно observability и делает canary возможной: метрический gate бесполезен, если вы не читаете срез канарейки. Хорошие дашборды делают canary менее рискованным выбором здесь, а не несущественной деталью." },
  ]}
/>

<Quiz
  id="04-rollout-strategies-choice-2"
  lessonSlug="04-rollout-strategies"
  lang="ru"
  question="Rolling update сообщает Deployment как 'available', все реплики подняты, однако ~30% запросов возвращают 502 в первые секунды после того, как каждый под входит в ротацию. В чём корневая причина?"
  choices={[
    { label: "Нет readiness probe, поэтому Kubernetes направляет трафик в поды в момент старта процесса контейнера — до того, как приложение открыло соединения к БД или прогрело кэши — и часть запросов бьёт в недозагруженный процесс.", correct: true },
    { label: "maxSurge выставлен слишком высоко, поэтому сразу создалось слишком много новых подов.", misconception: "maxSurge управляет только дополнительной ёмкостью во время выката; лишние поды не вызывают 502. Симптом — трафик в неготовые поды — указывает на отсутствующий readiness-гейт, а не на размер surge." },
    { label: "Deployment должен был использовать Recreate вместо RollingUpdate.", misconception: "Recreate дал бы полный простой, а не частичный поток 502. Проблема в том, что трафик доходит до недозагруженных подов, что предотвращает readiness probe во время rolling update." },
    { label: "maxUnavailable был 0, что заблокировало продвижение выката.", misconception: "maxUnavailable: 0 — безопасная настройка и не вызывает 502: она лишь означает, что ни один старый под не удаляется, пока его замена не готова. Ошибки идут от трафика в неготовые новые поды." },
  ]}
/>

<Quiz
  id="04-rollout-strategies-choice-3"
  lessonSlug="04-rollout-strategies"
  lang="ru"
  question={"Команда хочет строгий zero-downtime на Deployment из 4 реплик, который никогда не должен падать ниже полной ёмкости. Какая конфигурация rolling update верна и чего она стоит?\n\n```yaml\nstrategy:\n  type: RollingUpdate\n  rollingUpdate:\n    maxSurge: 1\n    maxUnavailable: 0\n```"}
  choices={[
    { label: "Верно: maxUnavailable: 0 означает, что ни один старый под не выходит из ротации, пока его замена не готова, поэтому ёмкость не проседает; maxSurge: 1 добавляет один временный под, чтобы выкат двигался. Цена — более медленный выкат (по одному поду) плюс ёмкость на один surge-под.", correct: true },
    { label: "Неверно: при maxUnavailable: 0 выкат зависнет, потому что ни один под нельзя удалить.", misconception: "Зависания нет — maxSurge: 1 сначала создаёт новый под, и только когда он готов, завершается старый. Эта пара — каноничная конфигурация zero-downtime; она просто катит по одному поду за раз." },
    { label: "Верно, и это само по себе гарантирует ноль 502.", misconception: "Knobs гарантируют, что ёмкость не проседает, но без реального readiness probe Kubernetes всё равно направляет трафик в поды в момент старта. Zero-downtime требует maxUnavailable: 0 И readiness probe, а не одни knobs." },
    { label: "Неверно: дефолты K8s 25%/25% всегда безопаснее, потому что катят быстрее.", misconception: "Дефолты 25% позволяют четверти ёмкости быть недоступной во время выката — это не zero-downtime. Быстрее не значит безопаснее: требование команды — никогда не падать ниже полной ёмкости." },
  ]}
/>

<Quiz
  id="04-rollout-strategies-choice-4"
  lessonSlug="04-rollout-strategies"
  lang="ru"
  question="Вы используете blue-green и хотите, чтобы rollback оставался безопасным. Релиз green переименовывает колонку в БД. Что вы делаете?"
  choices={[
    { label: "Применяете expand-contract: добавляете новую колонку, пишете dual-write в обе, и дропаете старую колонку только в более позднем деплое, после вывода blue из эксплуатации — так обе версии всегда читают схему.", correct: true },
    { label: "Просто переименовать колонку в миграции green; blue-green ведь даёт мгновенный rollback.", misconception: "Приложение переключается атомарно, но база общая и не переключается. Переименование ломает blue относительно живой схемы, поэтому откат назад падает — мгновенный rollback исчезает ровно тогда, когда нужен." },
    { label: "Сделать бэкап БД перед переключением, чтобы при необходимости восстановиться.", misconception: "Восстановление медленное и теряет каждую запись, сделанную после переключения. Обратносовместимые изменения expand-contract сохраняют обе версии рабочими без разрушительного restore." },
    { label: "Перейти на Recreate, чтобы жила только одна версия и переименование было безопасным.", misconception: "Recreate меняет проблему миграции на гарантированный простой, а несовместимая вперёд схема всё равно блокирует rollback. Expand-contract решает реальное ограничение: схему, которую читают обе версии." },
  ]}
/>

<Quiz
  id="04-rollout-strategies-choice-5"
  lessonSlug="04-rollout-strategies"
  lang="ru"
  question="Canary-деплой наращивает 5%, затем 25%, затем 50% без метрического gate — инженер на глаз смотрит дашборд и вручную промоутит на каждом шаге. В 3 часа ночи релиз с 4% ошибок доезжает до 100%. В чём настоящий провал?"
  choices={[
    { label: "Canary без автоматического метрического gate — это просто медленный ручной выкат; малый blast radius реализуется только тогда, когда шаг анализа (Argo Rollouts / Flagger, читающие SLO-метрики) автоматически приостанавливает или откатывает без человека в цикле.", correct: true },
    { label: "Проценты канарейки были неверны; старт с 1% поймал бы это.", misconception: "Меньший первый шаг уменьшает начальный blast radius, но всё равно доезжает до 100%, если метрики никто не читает. Недостающее звено — автоматический gate, а не размеры шагов." },
    { label: "Canary — неправильная стратегия для рискованного изменения; blue-green был бы безопаснее.", misconception: "Blue-green открывает 100% трафика на переключении — больший blast radius. Canary здесь правильна; провалилось то, что у неё не было метрического gate — именно того, что делает canary менее рискованной." },
    { label: "Уровень ошибок приемлем, ведь 96% запросов прошли успешно.", misconception: "4% ошибок на продакшен-сервисе обычно сильно за SLO — именно это и должен ловить gate. Смысл canary в том, что решает о промоушене gate, а не уставший человек." },
  ]}
/>

<Quiz
  id="04-rollout-strategies-choice-6"
  lessonSlug="04-rollout-strategies"
  lang="ru"
  question="Нужно выбрать выкат для сервиса, где две версии действительно не могут сосуществовать — in-place изменение stateful-синглтона без совместимой промежуточной схемы. Ёмкость в обрез, короткое окно обслуживания допустимо. Какая стратегия и какой компромисс вы принимаете?"
  choices={[
    { label: "Recreate — он гарантирует отсутствие пересечения версий, что и есть жёсткое ограничение здесь, и самый дешёвый по ресурсам. Принимаемый компромисс — простой в момент разрыва, который покрывает окно обслуживания.", correct: true },
    { label: "Rolling update — это дефолт Kubernetes, поэтому всегда безопасный выбор.", misconception: "Rolling update держит обе версии живыми одновременно — именно то, чего этот ворклоад не терпит. Дефолт не универсален; ограничение по несосуществованию его исключает." },
    { label: "Canary — малый blast radius делает её безопаснейшей для рискованных изменений.", misconception: "Canary тоже держит обе версии разом, чтобы обслуживать разделённый трафик, нарушая ограничение по несосуществованию. Выгода blast radius неуместна, когда смешанные версии портят состояние." },
    { label: "Blue-green — атомарное переключение означает, что в каждый момент обслуживает одна версия.", misconception: "Во время cutover существуют оба окружения, а blue-green удваивает стоимость ресурсов, что исключает ограниченная ёмкость; expand-contract не поможет, когда совместимой промежуточной схемы нет. Чистая замена версий у recreate подходит под ограничение." },
  ]}
/>

<Recap lang="ru">
Сквозная линия — одно решение: взвесить blast radius (радиус поражения: canary минимальный, recreate тотальный) против стоимости ресурсов (blue-green удваивает) против скорости rollback (blue-green и canary почти мгновенны, rolling катится назад, recreate — второй простой). Затем предпосылки, которые делают любой выбор реальным: maxSurge/maxUnavailable плюс readiness probe для zero-downtime rolling, автоматический метрический gate для canary и миграции expand-contract, чтобы rollback пережил изменение схемы. Observability решает, какой стратегии вы реально можете доверять. Теперь, когда столкнётесь со следующим решением о релизе, прогоните три оси до того, как потянетесь к дефолту — и убедитесь, что все предпосылки на месте до того, как деплой выходит в прод.
</Recap>
