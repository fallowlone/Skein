---
slug: 01-overview
lang: ru
track: deployment
unit: 06-lb-levels
order: 1
level: junior
lessonType: topic
status: ready
title: "Балансировка L4 vs L7: что каждый уровень видит и чего не видит"
summary: "L4-балансировщик маршрутизирует по IP и порту, не читая payload — быстро, но слепо к URL. L7-балансировщик терминирует соединение и читает HTTP, давая маршрутизацию по пути и retry ценой CPU. Выберешь не тот уровень — и маршрутизация по пути просто невозможна."
prereqs: []
deepensInto: []
spiral: []
concepts: ["l4-load-balancing", "l7-load-balancing", "tls-termination", "health-checks", "connection-draining", "session-affinity"]
estMin: 16
sources:
  - https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-target-groups.html
  - https://aws.amazon.com/compare/the-difference-between-the-difference-between-application-network-and-gateway-load-balancing/
  - https://docs.aws.amazon.com/elasticloadbalancing/latest/application/edit-target-group-attributes.html
  - https://www.nginx.com/resources/glossary/layer-4-load-balancing/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
Команда пилит монолит: `/api/*` должен идти в новый сервис, всё остальное — в старый. У них уже крутится AWS NLB «ради производительности», поэтому они добавляют правило маршрутизации по пути — и обнаруживают, что такого правила нет. NLB не видит путь. Он форвардит сырой TCP; URL появляется только после парсинга HTTP, а L4-балансировщик HTTP никогда не парсит. Миграция встаёт, пока кто-то не поставит ALB. Урок стоил дня: выбранный уровень решает, какая маршрутизация вообще *возможна*.
</Hook>

<Crux>L4-балансировщик маршрутизирует по IP/порту, не читая payload; L7 читает HTTP и маршрутизирует по пути, хосту, заголовку или cookie.</Crux>

<Explanation>

## L4 форвардит байты; L7 читает запросы

Балансировщик уровня 4 работает на транспортном уровне. Он видит TCP/UDP-соединение — source IP, destination IP, порты — и форвардит пакеты на бэкенд, ни разу не декодируя содержимое. По сути это быстрый, соединение-знающий NAT. Так как payload он не парсит, он протокол-агностичен (балансирует любой TCP- или UDP-сервис, а не только HTTP) и крайне дёшев на пакет — AWS NLB держит миллионы запросов в секунду с накладными в микросекунды на пакет и выдаёт статические IP.

Балансировщик уровня 7 работает на прикладном уровне. Он *терминирует* клиентское соединение, читает весь HTTP-запрос — метод, хост, путь, заголовки, cookie — и затем открывает собственное соединение к выбранному бэкенду. Это «терминировать и переоткрыть» — весь источник его мощи и его цены. Раз балансировщик держит распарсенный запрос, он может маршрутизировать на `/api/*` против `/`, по заголовку `Host:`, по cookie или по кастомному заголовку для канареек. Цена: он обязан буферизировать и парсить каждый запрос, а если в игре TLS — расшифровывать и шифровать заново. Реальный CPU на запрос, и он говорит только на тех протоколах, которые понимает (HTTP/1.1, HTTP/2, gRPC).

Решение не в том, «что лучше», а в том, «что мне нужно видеть». Если нужно лишь раскидать сырой TCP по одинаковым бэкендам — L4 быстрее и проще. В момент, когда маршрутизация зависит от чего-то *внутри* запроса, нужен L7 — и никакой тюнинг L4 не наколдует URL, который тот не декодировал.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Возможность</th>
        <th class="border border-border px-3 py-2 text-left">L4 (транспортный)</th>
        <th class="border border-border px-3 py-2 text-left">L7 (прикладной)</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Маршрутизирует по</td>
        <td class="border border-border px-3 py-2">IP + порт</td>
        <td class="border border-border px-3 py-2">пути, хосту, заголовку, cookie</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Читает payload?</td>
        <td class="border border-border px-3 py-2">Нет — форвардит байты</td>
        <td class="border border-border px-3 py-2">Да — парсит HTTP</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Протоколы</td>
        <td class="border border-border px-3 py-2">любой TCP/UDP</td>
        <td class="border border-border px-3 py-2">HTTP/1.1, HTTP/2, gRPC</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Терминация TLS</td>
        <td class="border border-border px-3 py-2">passthrough (обычно)</td>
        <td class="border border-border px-3 py-2">терминирует + может шифровать заново</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">HTTP-aware retry</td>
        <td class="border border-border px-3 py-2">Нет</td>
        <td class="border border-border px-3 py-2">Да (идемпотентные запросы)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Цена на запрос</td>
        <td class="border border-border px-3 py-2">очень низкая</td>
        <td class="border border-border px-3 py-2">парсинг + буфер + крипто</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Пример в AWS</td>
        <td class="border border-border px-3 py-2">NLB</td>
        <td class="border border-border px-3 py-2">ALB</td>
      </tr>
    </tbody>
  </table>
</div>

## Где терминируется TLS — меняет всё

Терминация TLS — самая чистая призма на раздел L4/L7. L7-балансировщик завершает зашифрованную сессию на краю: он держит сертификат, расшифровывает запрос, читает открытый HTTP, принимает решение о маршрутизации и форвардит — либо открытым текстом внутри доверенной сети, либо заново зашифрованным к бэкенду. Именно эта расшифровка и позволяет маршрутизировать по пути или заголовку. Она же централизует управление сертификатами и снимает крипто с серверов приложения.

L4-балансировщик обычно не может терминировать TLS, потому что терминировать — значит расшифровывать, а расшифровывать — значит стать HTTP-aware. Поэтому он делает TLS *passthrough*: зашифрованные байты идут прямо на бэкенд, который держит сертификат и терминирует. Это держит балансировщик слепым по дизайну — отлично для end-to-end шифрования и для не-HTTP протоколов, бесполезно, если хотелось маршрутизации по пути. Это и есть конкретная причина, почему «просто возьми быстрый» бьёт в обратку: быстрый буквально не может прочитать то, по чему ты хочешь маршрутизировать.

<Inset kind="why" lang="ru">
«L4 всегда быстрее» — полуправда и обман. На пакет — да, нет парсинга, нет крипто. Но L7-балансировщик, терминирующий TLS один раз на краю, может быть *дешевле в сумме*, чем прокидывание шифрования на каждый бэкенд, потому что каждый сервер приложения больше не платит за handshake и расшифровку. Правильный вопрос — где работу лучше сделать, а не какой уровень выигрывает микробенчмарк.
</Inset>

## Алгоритмы, health-чеки и вынос мёртвых бэкендов

Оба уровня всё равно должны *выбрать* бэкенд. Round-robin раскидывает соединения поровну и нормален, когда запросы стоят примерно одинаково. Least-connections отправляет следующий запрос на бэкенд с наименьшим числом активных соединений — куда лучше, когда длительности запросов сильно разнятся, потому что один медленный эндпоинт не будет получать всё больше нагрузки. Хеширование (по IP клиента или consistent hashing) прикрепляет данный ключ к данному бэкенду — так L4-балансировщик имитирует stickiness без cookie.

Health-чеки держат пул честным. Активные чеки опрашивают каждый бэкенд по интервалу (скажем, раз в несколько секунд) и выносят его из ротации после N подряд неудач; пассивные помечают бэкенд мёртвым после того, как он вернул ошибки реальному трафику. Open-source nginx опирается на пассивные чеки; ALB/NLB и nginx Plus делают активный опрос. То, что не доходит до интуиции: упавший бэкенд выносят только *после* того, как health-чек это заметит — поэтому слишком медленный интервал даёт окно, когда живой трафик продолжает бить в мёртвую коробку и 502 утекают пользователям.

<TradeoffMatrix
  id="06-lb-levels-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Нужно маршрутизировать /api/* в новый сервис, / в старый, терминировать TLS на краю и катить канарейку по заголовку запроса. Выбери балансировщик."
  options={[
    { name: "L7 прикладной балансировщик (ALB / nginx HTTP / Envoy)", summary: "Читает HTTP.", correct: true, justification: "Каждое требование живёт внутри HTTP-запроса: маршрутизация по пути (/api/* против /), терминация TLS (которая требует расшифровки) и канарейка по заголовку — всё требует, чтобы балансировщик распарсил запрос. Только L7-балансировщик терминирует соединение и читает метод, хост, путь, заголовки и cookie, поэтому он единственный уровень, способный удовлетворить хоть что-то из этого. Цена — парсинг и крипто на запрос — это плата за возможность маршрутизировать по тому, что говорит запрос." },
    { name: "L4 сетевой балансировщик (NLB) с доп. конфигом", summary: "Быстро форвардит TCP.", justification: "Никакой конфиг не заставит L4-балансировщик увидеть URL или заголовок — он форвардит TCP/UDP по IP и порту и никогда не декодирует payload. Маршрутизация по пути, канарейка по заголовку и терминация TLS структурно невозможны на L4. Это и есть классическая ловушка: выбрать L4 ради сырой скорости, а потом обнаружить, что нужной маршрутизации на этом уровне просто нет." },
    { name: "L4-балансировщик с TLS passthrough к бэкендам, которые маршрутизируют внутри", summary: "Опустить маршрутизацию вниз.", justification: "Технически возможно, но это лишь переносит L7-работу в каждый бэкенд: каждый сервер приложения теперь терминирует TLS и переизобретает маршрутизацию по пути/заголовку, умножая управление сертификатами и крипто-цену и размазывая логику маршрутизации. Ты пересобрал L7-балансировщик плохо по N коробкам вместо одного на краю." },
    { name: "DNS round-robin между двумя хостнеймами", summary: "Резолвит в IP.", justification: "DNS раздаёт IP; он не может проинспектировать путь или заголовок, не имеет health-aware фейловера в пределах TTL записи, а клиенты кэшируют результаты. Это грубый механизм распределения, а не запрос-знающий роутер, и не решает ни одно из заявленных требований." },
  ]}
/>

## Sticky-сессии и connection draining: два способа сломать деплой

Sticky-сессии (session affinity) прикрепляют клиента к одному бэкенду — через L4 IP-хеш или L7-cookie — чтобы in-memory состояние сессии оставалось на месте. Это работает и тихо вредит: нагрузка перестаёт распределяться ровно (один «кит»-клиент молотит одну коробку), и этот бэкенд нельзя чисто слить, потому что он владеет сессиями, которых нет ни у кого. Рефлекс сеньора — делать бэкенды stateless (сессии в Redis или JWT), чтобы любой бэкенд обслуживал любой запрос; тянуться к stickiness только когда состояние реально нельзя вынести.

Убийца деплоя — отсутствие connection draining. Когда ты убираешь бэкенд во время выката, на нём всё ещё обслуживаются in-flight запросы. Если балансировщик вырывает его мгновенно, эти запросы умирают — пользователи видят сброс на середине оформления заказа. Connection draining (AWS зовёт это *deregistration delay*) велит балансировщику перестать слать на бэкенд *новые* соединения, но дать существующим доработать в течение grace-окна. В AWS дефолт — **300 секунд**, настраиваемо от 0 до 3600. Поставишь короче самого долгого запроса — всё равно режешь живые запросы; забудешь включить — каждый деплой это маленький простой. Сочетай с health-чеками: слить, подождать, потом гасить.

<Quiz
  id="06-lb-levels-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Твоему L4 (NLB) балансировщику нужно слать /admin/* в отдельный пул бэкендов. Как настроить правило по пути?"
  choices={[
    { label: "Никак — L4-балансировщик никогда не парсит HTTP, поэтому пути на этом уровне не существует; нужен L7-балансировщик", correct: true },
    { label: "Добавить листенер-правило с матчингом по пути на NLB", misconception: "L4-листенеры матчат по порту, а не по пути. URL появляется только после парсинга HTTP, а L4-балансировщик форвардит сырой TCP без парсинга — добавлять правило по пути некуда." },
    { label: "Включить deep packet inspection на NLB", misconception: "Инспекция HTTP-payload, чтобы прочитать путь — это ровно то, что делает балансировщик L7. Сетевой балансировщик, делающий это, перестал бы работать на уровне 4." },
    { label: "Использовать IP-хеш, чтобы клиенты /admin/* липли к нужному пулу", misconception: "IP-хеш прикрепляет клиента к бэкенду независимо от пути; он не может отделить /admin/* от /, потому что пути он не видит. Affinity — это не маршрутизация." },
  ]}
/>

<Quiz
  id="06-lb-levels-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Во время rolling-деплоя пользователи жалуются на сброс соединения на середине запроса каждый раз, когда убирают старый инстанс. Какой фикс?"
  choices={[
    { label: "Включить connection draining / deregistration delay, чтобы in-flight запросы доработали до гашения бэкенда", correct: true },
    { label: "Переключиться с least-connections на round-robin", misconception: "Алгоритм выбирает среди здоровых бэкендов; он ничего не делает с убийством in-flight запросов на бэкенде, который ты убираешь. Сбросы — от отсутствия мягкого слива." },
    { label: "Добавить больше бэкендов в пул", misconception: "Больше ёмкости не меняет того, что происходит с запросами, уже работающими на убираемом инстансе — без draining их всё равно режут в момент дерегистрации." },
    { label: "Понизить интервал health-чека", misconception: "Более быстрые health-чеки раньше обнаружат мёртвый бэкенд, но проблема в том, что здоровый убирают на середине запроса, не дав соединениям доработать — это draining, а не обнаружение." },
  ]}
/>

<DragOrder
  id="06-lb-levels-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь шаги, чтобы мягко убрать бэкенд во время деплоя:"
  items={[
    { id: "a", label: "Пометить бэкенд на дерегистрацию, чтобы балансировщик перестал слать ему новые соединения" },
    { id: "b", label: "Дать connection draining отработать: существующие in-flight запросы продолжают обслуживаться" },
    { id: "c", label: "Выждать окно слива (например, AWS deregistration delay, дефолт 300с)" },
    { id: "d", label: "Как соединения доработают или окно истечёт — погасить инстанс" },
    { id: "e", label: "Health-чеки подтверждают, что оставшийся пул обслуживает трафик" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>L4 маршрутизирует по IP/порту и форвардит байты — быстро, но слепо к URL. L7 читает HTTP и маршрутизирует по пути/хосту/заголовку, ценой CPU. Всегда включай connection draining до удаления бэкенда.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-lb-levels-retrieval"
  lang="ru"
  questions={[
    { q: "Коллега хочет маршрутизацию по пути, но команда уже крутит L4 (NLB) балансировщик «потому что быстрее». Объясни точно, почему маршрутизация по пути там невозможна и что изменится при переходе на L7.", a: "Балансировщик уровня 4 работает на транспортном уровне: он видит TCP/UDP-соединение — source и destination IP и порт — и форвардит сырые байты на бэкенд, ни разу не декодируя payload. Путь URL — часть HTTP-запроса, который появляется только когда кто-то распарсит протокол прикладного уровня, а L4-балансировщик по определению этого никогда не делает. Поэтому нет правила по пути, которое можно добавить, и нет настройки, которую можно переключить; информация, по которой ты бы маршрутизировал, никогда не была декодирована. Переход на L7 меняет модель полностью: прикладной балансировщик терминирует клиентское соединение, парсит весь HTTP-запрос (метод, хост, путь, заголовки, cookie), принимает решение по любому из этих полей, затем открывает собственное соединение к выбранному бэкенду. Это «терминировать и прочитать» и даёт маршрутизацию /api/* против /, по хосту, канарейку по заголовку и HTTP-aware retry. Трейдофф, который ты принимаешь, — CPU на запрос: парсинг, буферизация и — если TLS терминируется на краю — расшифровка и повторное шифрование. Уровень выбирают по тому, что нужно видеть, а не по тому, что выигрывает микробенчмарк." },
    { q: "Пройди по тому, что делает connection draining во время деплоя и что ломается без него. Включи дефолт AWS.", a: "Во время rolling-деплоя ты убираешь старые бэкенды из пула, но в момент удаления один из них может всё ещё обслуживать in-flight запросы. Без connection draining балансировщик дерегистрирует его мгновенно, и эти открытые соединения рвутся — пользователи видят сброс или ошибку на середине запроса, например на середине оформления заказа. Connection draining (AWS зовёт это deregistration delay) это чинит: когда бэкенд помечен на удаление, балансировщик перестаёт слать ему новые соединения, но даёт существующим доработать до конца в течение grace-окна. В AWS дефолтный deregistration delay — 300 секунд, настраиваемо от 0 до 3600. Окно должно быть не короче самого долгого легитимного запроса, иначе ты всё равно режешь живой трафик; поставишь слишком длинным — деплои тянутся. Правильная последовательность: пометить на дерегистрацию, дать in-flight запросам слиться, выждать окно (или пока соединения доработают), затем погасить — с health-чеками, подтверждающими здоровье оставшегося пула. Забыть включить draining — превратить каждый деплой в маленький, самонанесённый простой." },
  ]}
/>

<Recap lang="ru">
Выбранный уровень решает, по чему балансировщик может маршрутизировать, потому что решает, что он может видеть. L4-балансировщик работает на транспортном уровне — IP, порт, сырой TCP/UDP — и форвардит байты без парсинга, что делает его быстрым, протокол-агностичным и способным на TLS passthrough, но структурно слепым к URL, заголовкам и cookie. L7-балансировщик терминирует соединение, парсит HTTP и маршрутизирует по пути, хосту, заголовку или cookie — это и даёт маршрутизацию по пути, терминацию TLS на краю, канарейки по заголовку и HTTP-aware retry — оплаченные CPU на запрос. Поверх выбора уровня лежат операционные детали, решающие надёжность: round-robin, least-connections и хеширование балансируют по-разному при неровной нагрузке; активные и пассивные health-чеки выносят мёртвые бэкенды, но лишь так быстро, как их интервал; sticky-сессии держат состояние на месте ценой ровного распределения и чистого слива; а connection draining (AWS deregistration delay, дефолт 300 секунд) даёт in-flight запросам доработать до удаления бэкенда. Два продакшен-провала падают прямо отсюда: потребовать маршрутизацию по пути на L4-балансировщике, что невозможно, и убрать бэкенд без draining, что убивает живые запросы на каждом деплое.
</Recap>
