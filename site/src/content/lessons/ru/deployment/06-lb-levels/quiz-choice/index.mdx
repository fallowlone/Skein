---
concepts:
- l4-vs-l7
- tls-termination
- health-checks
- connection-draining
- session-affinity
- balancing-algorithms
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-target-groups.html
- https://docs.aws.amazon.com/elasticloadbalancing/latest/application/edit-target-group-attributes.html
- https://www.nginx.com/resources/glossary/layer-4-load-balancing/
status: ready
summary: "Синтез юнита балансировки в формате выбора — видимость L4 vs L7, TLS termination, выбор алгоритма, health checks, sticky sessions и connection draining."
title: 'Уровни балансировки: тест с выбором'
track: deployment
unit: 06-lb-levels
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов сквозь весь юнит. Каждый отражает решение, которое ты принимаешь, поднимая или эксплуатируя load balancer — не определение для зубрёжки, а выбор уровня или режим отказа, который надо верно прочитать под нагрузкой.
</Hook>

<Goal lang="ru">
Убедись, что связываешь то, что каждый уровень видит, с тем, какую маршрутизацию он включает, и сводишь операционные ручки — алгоритм, health checks, affinity, draining — к отказам, которые они предотвращают.
</Goal>

<Quiz
  id="06-lb-levels-choice-1"
  lessonSlug="06-lb-levels"
  lang="ru"
  question="Перед сервисом стоит NLB (L4). Продукт просит направить /api/v2/* в новый пул бэкендов, оставив /api/v1/* на старом. Какой честный ответ и почему?"
  choices={[
    { label: "На L4 это невозможно — путь живёт внутри HTTP-запроса, который L4-балансировщик никогда не парсит; перенеси эту маршрутизацию на L7-балансировщик (ALB / nginx / Envoy), который терминирует соединение и читает путь.", correct: true },
    { label: "Добавь listener-правило по префиксу пути в target group NLB.", misconception: "L4-listener матчится только по протоколу и порту. Путь появляется лишь после парсинга HTTP, а L4-балансировщик форвардит сырой TCP без парсинга — правила по пути на этом уровне просто нет." },
    { label: "Включи deep packet inspection на NLB, чтобы он читал путь.", misconception: "Инспекция HTTP-payload ради чтения пути — это ровно то, что делает балансировщик L7. L4-балансировщик, декодирующий запросы, уже не работает на транспортном уровне — ты только что описал переход на L7." },
    { label: "Используй consistent hashing, чтобы запросы /api/v2/* попадали в новый пул.", misconception: "Хеширование пинит ключ (например, client IP) к бэкенду независимо от пути; оно не отделит v2 от v1, потому что путь оно не видит. Affinity — это не маршрутизация." },
  ]}
/>

<Quiz
  id="06-lb-levels-choice-2"
  lessonSlug="06-lb-levels"
  lang="ru"
  question="Длительность запросов на бэкендах сильно разнится: большинство отвечают за 5 мс, некоторые — за 2 с. При round-robin p99 плохой и один бокс всегда горячий. Какая смена алгоритма помогает сильнее всего и почему?"
  choices={[
    { label: "Least-connections — он шлёт следующий запрос на бэкенд с наименьшим числом активных соединений, так что бокс, застрявший на медленных запросах, перестаёт набирать новые вместо того, чтобы получать свою долю 1-из-N независимо от нагрузки.", correct: true },
    { label: "Остаться на round-robin, но добавить бэкендов.", misconception: "Round-robin всё равно раздаёт каждому бэкенду равную долю, слепо к его занятости, так что бокс посреди медленного запроса продолжают заваливать. Больше мощности размывает, но не чинит перекос, который создаёт сам алгоритм." },
    { label: "Перейти на IP-хеширование ради равномерного распределения.", misconception: "IP-хеширование пинит клиентов к бэкендам ради affinity; оно не балансирует по текущей нагрузке и может усилить перекос, если тяжёлый клиент захешируется на один бокс. Оно решает stickiness, а не разброс длительностей." },
    { label: "Снизить интервал health check, чтобы медленные бэкенды выкидывались.", misconception: "Медленный, но здоровый бэкенд проходит health check; выкидывать его по одной латентности — флапать пул. Фикс — рулить нагрузкой по текущему числу соединений, а не объявлять рабочий бокс мёртвым." },
  ]}
/>

<Quiz
  id="06-lb-levels-choice-3"
  lessonSlug="06-lb-levels"
  lang="ru"
  question="Нужны одновременно end-to-end шифрование (посередине ничего не расшифровывается) И маршрутизация по пути на краю из одного балансировщика. Что верно?"
  choices={[
    { label: "Сразу обоих на одном балансировщике не получить: маршрутизация по пути требует расшифровки для чтения HTTP (L7 termination), а значит это не passthrough. Выбирай — терминировать TLS на краю ради маршрутизации по пути, или пускать TLS насквозь ради настоящего end-to-end и маршрутизировать на L4 только по IP/порту.", correct: true },
    { label: "Взять L4-балансировщик с TLS passthrough и навесить сверху правила по пути.", misconception: "Passthrough означает, что байты идут через балансировщик зашифрованными, так что путь он прочитать не может — в этом весь смысл passthrough. Нельзя добавить правила по пути к трафику, который отказываешься расшифровывать." },
    { label: "Взять L7-балансировщик в режиме passthrough, чтобы держать шифрование end-to-end.", misconception: "L7-балансировщик маршрутизирует по пути именно потому, что терминирует TLS и читает plaintext HTTP. «L7 passthrough» — противоречие: если он не расшифровывает, он не может маршрутизировать на L7." },
    { label: "Включить маршрутизацию по SNI, чтобы балансировщик роутил по пути без расшифровки.", misconception: "SNI открывает hostname в TLS-хендшейке, так что L4-балансировщик может роутить по хосту без расшифровки — но путь в зашифрованном теле. SNI даёт маршрутизацию по хосту, никогда по пути." },
  ]}
/>

<Quiz
  id="06-lb-levels-choice-4"
  lessonSlug="06-lb-levels"
  lang="ru"
  question="Во время rolling deploy пользователи ловят сбросы соединений посреди запроса всякий раз, когда удаляют старый инстанс. Пул здоров и достаточен по размеру. Какой фикс?"
  choices={[
    { label: "Включить connection draining (в AWS — deregistration delay), чтобы балансировщик перестал слать инстансу новые соединения, но дал текущим запросам завершиться в окне грации перед терминацией.", correct: true },
    { label: "Переключиться с round-robin на least-connections.", misconception: "Алгоритм лишь выбирает среди здоровых бэкендов; он ничего не делает с запросами, уже идущими на инстансе, который ты намеренно удаляешь. Без draining их обрубает в момент дерегистрации." },
    { label: "Снизить интервал health check, чтобы мёртвый инстанс детектился быстрее.", misconception: "Инстанс не валит health checks — ты удаляешь здоровый посреди запроса. Быстрая детекция помогает при крашах, а не при аккуратном выводе рабочего бэкенда." },
    { label: "Добавить бэкендов, чтобы нагрузка ушла с инстанса.", misconception: "Лишняя мощность не меняет судьбу соединений, уже идущих на удаляемом инстансе. Без окна draining они всё равно умрут в момент дерегистрации." },
  ]}
/>

<Quiz
  id="06-lb-levels-choice-5"
  lessonSlug="06-lb-levels"
  lang="ru"
  question="Сервис использует cookie-based sticky sessions, чтобы держать in-memory состояние пользователя на одном бэкенде. Нагрузка неравномерна (один бокс горячий) и его нельзя чисто слить во время деплоя. Какой senior-фикс?"
  choices={[
    { label: "Вынести состояние сессии — в Redis или подписанный JWT, чтобы любой бэкенд мог обслужить любой запрос — и выключить stickiness. Stateless-бэкенды распределяются равномерно и сливаются чисто; affinity — это костыль для состояния, которое не стоило пинить к боксу.", correct: true },
    { label: "Сменить sticky sessions с cookie-based (L7) на IP-hash (L4).", misconception: "Смена механизма affinity не убирает affinity — нагрузка всё так же концентрируется, и бокс по-прежнему владеет сессиями, которых нет у других. Корень — per-backend состояние, а не способ пиннинга клиента." },
    { label: "Увеличить окно draining, чтобы у sticky-бокса было больше времени завершить.", misconception: "Длинное окно даёт активным сессиям догореть, но новые запросы по этим сессиям продолжают приходить, и бокс не уйдёт, пока они не истекут. Ты лечишь симптом, а не пиннинг состояния." },
    { label: "Включить least-connections, чтобы выровнять нагрузку несмотря на stickiness.", misconception: "Stickiness по построению перебивает алгоритм — раз клиент закреплён, его запросы обходят least-connections и возвращаются на тот же бокс. Нельзя сбалансировать affinity, который ты намеренно включил." },
  ]}
/>

<Quiz
  id="06-lb-levels-choice-6"
  lessonSlug="06-lb-levels"
  lang="ru"
  question="Команда использует DNS round-robin (две A-записи) как load balancer. Бэкенд умирает, и часть пользователей продолжают получать ошибки минутами. Почему, и что даёт настоящий балансировщик?"
  choices={[
    { label: "DNS раздаёт IP, и клиенты кешируют их на TTL записи, так что failover не быстрее TTL и кешей, которые ты не контролируешь — health-aware удаления нет. Настоящий балансировщик делает health check бэкендов и выкидывает мёртвый из ротации за секунды, прозрачно для клиентов.", correct: true },
    { label: "TTL был просто слишком большим; снизь до 1 секунды — и DNS round-robin станет нормальным балансировщиком.", misconception: "TTL в 1 с долбит резолверы, массово зажимается или игнорируется клиентами и OS-кешами и всё равно ничего не знает о здоровье бэкенда. Низкий TTL — пластырь, а не health-aware failover." },
    { label: "DNS round-robin делает health checks, просто интервал проверки был слишком медленным.", misconception: "Чистый DNS round-robin вообще не делает health checks — он слепо ротирует выданные записи. Health-aware DNS существует, но это другой продукт; базовый механизм мёртвый бэкенд не детектит." },
    { label: "Клиенты должны автоматически ретраить другой IP, так что это баг клиента.", misconception: "Клиенты дико разнятся в поведении ретраев, и многие не делают failover между A-записями оперативно. Надежда, что каждый клиент сам себя вылечит, — ровно тот разрыв, который закрывает балансировщик с health check." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одно правило: выбранный уровень решает, что балансировщик видит, а что он видит — решает, что он умеет. L4 форвардит байты по IP и порту — быстро, протокол-агностично, способен на TLS passthrough, но слеп к пути, заголовку и cookie; L7 терминирует соединение, читает HTTP и маршрутизирует по любому из них ценой CPU и крипто на запрос. Поверх уровня лежат операционные ручки, решающие надёжность: least-connections бьёт round-robin при неравномерной длительности запросов, health checks выкидывают мёртвые бэкенды, но не быстрее своего интервала, sticky sessions меняют равномерность и чистый draining на пиннинг состояния (так что сперва выноси состояние), а connection draining даёт текущим запросам завершиться перед выводом бэкенда. И DNS round-robin — не load balancer: ни health-aware, failover ограничен неконтролируемыми TTL и кешами. Теперь, когда встретишь требование к маршрутизации по пути или деплой со сбросами запросов, ты будешь точно знать, какой уровень взять и какую ручку крутить первой.
</Recap>
