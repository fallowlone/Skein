---
concepts:
- l4-vs-l7
- tls-termination
- health-checks
- connection-draining
- session-affinity
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-target-groups.html
- https://docs.aws.amazon.com/elasticloadbalancing/latest/application/edit-target-group-attributes.html
- https://www.nginx.com/resources/glossary/layer-4-load-balancing/
status: ready
summary: "Промпты на свободное припоминание по юниту балансировки. Сначала ответь своими словами, затем раскрой модельный ответ и сравни."
title: 'Уровни балансировки: тест на припоминание'
track: deployment
unit: 06-lb-levels
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание бьёт перечитывание. На каждый промпт скажи или напиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет модель уровней и её операционные ручки.
</Hook>

<Goal lang="ru">
Восстанови хребет юнита — что видит каждый уровень, почему TLS termination это чистейшая линза на раскол, и как алгоритмы, health checks, affinity и draining решают надёжность — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:load
  id="06-lb-levels-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему L4-балансировщик может маршрутизировать по IP и порту, но никогда по пути URL или HTTP-заголовку, и что меняется на L7?", a: "L4-балансировщик работает на транспортном уровне: он видит TCP/UDP-соединение — source и destination IP и порт — и форвардит сырые байты на бэкенд, не декодируя payload. Путь URL, хост, заголовки и cookie — часть HTTP-запроса, который появляется только после парсинга прикладного протокола, а L4-балансировщик по определению его не парсит. Поэтому правила по пути или заголовку добавить не к чему — эта информация никогда не декодировалась. L7-балансировщик терминирует клиентское соединение, парсит весь HTTP-запрос, принимает решение по method/host/path/header/cookie, затем открывает собственное соединение к выбранному бэкенду. Это «терминировать и прочитать» включает маршрутизацию по пути, по хосту, header-канарейки и HTTP-aware retry — ценой CPU на парсинг, буферизацию и крипто на каждый запрос. Уровень выбирают по тому, что нужно видеть, а не по тому, кто выигрывает микробенчмарк." },
    { q: "Объясни, почему TLS termination — чистейшая линза на раскол L4/L7, включая то, что L4-балансировщик делает с TLS вместо этого.", a: "Терминировать TLS значит расшифровывать, а расшифровывать значит стать HTTP-aware — так что termination структурно акт L7. L7-балансировщик завершает зашифрованную сессию на краю: держит сертификат, расшифровывает, читает plaintext HTTP, маршрутизирует по пути или заголовку, затем форвардит как plaintext по доверенной сети или переш­ифровывает на бэкенд. Та же расшифровка — ровно то, что позволяет ему маршрутизировать по содержимому запроса, и она централизует управление сертификатами и снимает крипто с app-серверов. L4-балансировщик обычно не может терминировать TLS, потому что ему пришлось бы декодировать запрос; вместо этого он делает TLS passthrough — зашифрованные байты идут прямо на бэкенд, который держит сертификат и терминирует. Passthrough держит балансировщик слепым по построению: отлично для end-to-end шифрования и не-HTTP протоколов, бесполезно, если нужна маршрутизация по пути." },
    { q: "Round-robin против least-connections против хеширования — когда выигрывает каждый, и что хеширование даёт L4-балансировщику?", a: "Round-robin раскидывает соединения равномерно и хорош, когда запросы стоят примерно одинаково — равная доля, без per-request состояния. Least-connections шлёт на бэкенд с наименьшим числом активных соединений, что заметно лучше при сильно разной длительности запросов, потому что бокс, застрявший на медленных запросах, перестаёт набирать новые вместо слепой доли 1-из-N. Хеширование (по client IP или consistent hashing) пинит данный ключ к данному бэкенду; так L4-балансировщик имитирует stickiness без cookie, а consistent hashing ограничивает, сколько ключей переезжает при изменении пула. Режим отказа перекоса: при разной длительности round-robin продолжает наваливать на занятый бокс и рушит p99, а хеширование может сконцентрировать тяжёлого клиента на одном бэкенде. Подбирай алгоритм под профиль запросов." },
    { q: "Что делают активные и пассивные health checks, и почему интервал проверки ограничивает скорость удаления мёртвого бэкенда?", a: "Health checks держат пул честным. Активные проверки зондируют каждый бэкенд по интервалу (например, каждые несколько секунд) и выкидывают его из ротации после N подряд неудач; пассивные помечают бэкенд мёртвым после того, как он вернул ошибки реальному трафику. Open-source nginx опирается на пассивные проверки, а ALB/NLB и nginx Plus делают активное зондирование. Интервал ограничивает скорость удаления, потому что упавший бэкенд выкидывают лишь после того, как проверка заметит — так что слишком медленный интервал оставляет окно, где живой трафик долбит мёртвый бокс и 502 утекают к пользователям. Ты меняешь скорость детекции на нагрузку от зондов и флаппинг: слишком агрессивно — кратко медленный бэкенд выкидывается и пул осциллирует; слишком лениво — отказы утекают. Draining и health checks работают в паре — слить, подождать, терминировать, при этом проверки подтверждают, что остаток пула здоров." },
    { q: "Что такое connection draining, что ломается без него и каков дефолт в AWS?", a: "Во время rolling deploy ты удаляешь старые бэкенды, но в момент удаления один из них может ещё обслуживать запросы в полёте. Без connection draining балансировщик дерегистрирует его сразу, и эти открытые соединения обрубаются — пользователи видят сбросы или ошибки посреди запроса, например посреди checkout, превращая каждый деплой в маленький само­индуцированный сбой. Connection draining (в AWS — deregistration delay) это чинит: когда бэкенд помечен на удаление, балансировщик перестаёт слать ему новые соединения, но даёт существующим завершиться в окне грации. В AWS дефолт deregistration delay — 300 секунд, настраивается от 0 до 3600. Окно должно быть не короче самого долгого легитимного запроса, иначе всё равно режешь живой трафик; слишком длинное — деплои тянутся. Последовательность: пометить на дерегистрацию, слить запросы в полёте, выждать окно (или пока соединения завершатся), затем терминировать — health checks подтверждают здоровье остального пула." },
    { q: "Почему sticky sessions вредят, даже когда работают, и в чём senior-рефлекс?", a: "Sticky sessions (session affinity) пинят клиента к одному бэкенду — через L4 IP hash или L7 cookie — чтобы in-memory состояние сессии оставалось на месте. Это работает, но тихо вредит двояко: нагрузка перестаёт распределяться равномерно, потому что один тяжёлый клиент долбит один бокс, и этот бэкенд нельзя чисто слить во время деплоя, потому что он владеет сессиями, которых нет у других. Senior-рефлекс — делать бэкенды stateless: вынести состояние сессии в Redis или подписанный JWT, чтобы любой бэкенд мог обслужить любой запрос, что возвращает равномерное распределение и чистый draining. Браться за stickiness стоит только тогда, когда вынести состояние действительно нельзя. Affinity — костыль для состояния, прибитого к боксу, а не фича маршрутизации, которую хочешь по умолчанию." },
  ]}
/>

<Recap lang="ru">
Если смог восстановить каждый ответ по памяти — у тебя есть хребет юнита: уровень решает, что балансировщик видит (L4 = IP/порт и байты, L7 = распарсенный HTTP), TLS termination — чистейшая линза на этот раскол (расшифровать ради маршрутизации по пути или пустить насквозь, чтобы остаться слепым и end-to-end), алгоритмы подбираются под профиль запросов (least-connections при разной длительности, хеширование для L4-affinity), health checks выкидывают мёртвые бэкенды не быстрее своего интервала, connection draining (дефолт AWS 300 с) даёт запросам в полёте завершиться перед выводом бэкенда, а sticky sessions меняют равномерность и чистый draining на пиннинг состояния — так что сперва выноси состояние.
</Recap>
