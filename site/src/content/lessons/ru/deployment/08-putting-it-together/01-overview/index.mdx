---
slug: 01-overview
lang: ru
track: deployment
unit: 08-putting-it-together
order: 1
level: junior
lessonType: topic
status: ready
title: "Capstone: деплой — это цепочка, а авария живёт в стыке"
summary: "Каждая стадия деплоя верна по отдельности, а релиз падает там, где две собираются неправильно: rolling update без readiness-пробы, blue-green-флип поверх несовместимой миграции, L4 без draining. Клей — health-чеки, expand-contract и метрики раскатки."
prereqs: []
deepensInto: []
spiral: []
concepts: ["deployment-pipeline", "readiness-probe", "rollout-strategy", "expand-contract-migration", "connection-draining", "infrastructure-as-code"]
estMin: 17
sources:
  - https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/
  - https://www.bytebase.com/blog/database-blue-green-deployment/
  - https://docs.cloud.google.com/kubernetes-engine/docs/troubleshooting/load-balancing
  - https://github.com/Caesarsage/bluegreen-deployment-ecs
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
Все блоки в диаграмме релиза были зелёными. Образ собрался и запушился. K8s-`Deployment` применился без ошибок. Rolling update отрапортовал успех за 40 секунд. И тут запищал пейджер: 30% запросов отдают 502 две минуты. Никто не выкатил баг. Образ был в порядке, манифест в порядке, миграция в порядке — *по отдельности*. У Deployment не было `readinessProbe`, поэтому k8s объявлял каждый новый под «готовым» в тот миг, когда стартовал процесс контейнера, ещё до того, как приложение открыло пул к БД. Rolling update послушно перенаправил трафик на поды, которые были живыми, но ещё не обслуживали. Авария не жила ни в одной стадии. Она жила в стыке между двумя верными.
</Hook>

<Crux>Деплой — это цепочка верных стадий; авария живёт в стыке, где две из них собираются вместе неправильно.</Crux>

<Explanation>

## Пайплайн — это один собранный объект, а не семь шагов

Весь трек деплоя шёл по одной стадии за раз: собрать лёгкий multi-stage образ, запушить в реестр, объявить k8s-объекты, выбрать стратегию раскатки, поставить перед сервисом балансировщик, инжектить секреты, закодировать всё как infrastructure-as-code. Озарение capstone в том, что ни одна из этих стадий сама по себе не выкатывает софт. *Релиз* — это композиция всех их, а у композиции есть эмерджентные режимы отказа, которые не ловят тесты ни одной отдельной стадии.

Думай об этом как о цепочке, где каждое звено передаёт артефакт следующему:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Стадия</th>
        <th class="border border-border px-3 py-2 text-left">Производит</th>
        <th class="border border-border px-3 py-2 text-left">Стык, который она может сломать</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Multi-stage сборка</td>
        <td class="border border-border px-3 py-2">Неизменяемый образ + digest</td>
        <td class="border border-border px-3 py-2">Build-зависимости утекают в runtime → раздутость, CVE</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Push в реестр</td>
        <td class="border border-border px-3 py-2">Тегнутый артефакт, готовый к pull</td>
        <td class="border border-border px-3 py-2">Изменяемый тег <code>:latest</code> → какая сборка реально live?</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">k8s-объекты</td>
        <td class="border border-border px-3 py-2">Deployment + Service + Ingress + Config/Secret</td>
        <td class="border border-border px-3 py-2">Нет пробы → «готов» врёт раскатке</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Стратегия раскатки</td>
        <td class="border border-border px-3 py-2">Сдвиг трафика старое → новое</td>
        <td class="border border-border px-3 py-2">Флип поверх несовместимой схемы → старый код 500-ит</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Балансировщик</td>
        <td class="border border-border px-3 py-2">Трафик клиента → здоровые бэкенды</td>
        <td class="border border-border px-3 py-2">L4 не умеет draining → запросы в полёте обрываются</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Секреты при деплое</td>
        <td class="border border-border px-3 py-2">Конфиг инжектится в runtime</td>
        <td class="border border-border px-3 py-2">Секрет вшит в образ → утёк + не ротируется</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">IaC</td>
        <td class="border border-border px-3 py-2">Воспроизводимое состояние кластера</td>
        <td class="border border-border px-3 py-2">Drift → окружение, что тестил ≠ что выкатил</td>
      </tr>
    </tbody>
  </table>
</div>

Каждая колонка «стык» — это реальная авария, которую кто-то выкатил. Каждая предполагает, что предыдущая стадия отработала — и всё равно сломалась, потому что контракт *между* стадиями никто не проверял.

## Health-чеки — это клей между «запущен» и «обслуживает»

Самый несущий кусок клея — readiness-проба, потому что она задаёт контракт, на который опирается раскатка. Вся работа rolling update такая: поднять новый под, дождаться, пока он *готов*, затем погасить старый, по `maxUnavailable` за раз. Слово «готов» делает тут гигантскую работу. Без `readinessProbe` k8s рапортует под готовым в тот миг, когда стартовал главный процесс контейнера, — а это до того, как прогрелась JVM, открылся пул соединений, прогрелись кэши. Rolling update видит зелёное, добавляет под в эндпоинты `Service`, и трафик льётся в процесс, который отдаёт 502 / connection-refused.

Фикс — проба, которая реально дёргает зависимости, плюс surge-арифметика, которая никогда не падает ниже ёмкости:

```yaml
readinessProbe:
  httpGet: { path: /healthz/ready, port: 8080 }
  initialDelaySeconds: 5
  periodSeconds: 5
strategy:
  rollingUpdate:
    maxUnavailable: 0   # никогда ниже желаемого числа реплик
    maxSurge: 1         # добавить новый под до удаления старого
```

Две пробы делают две разные работы, и их смешение — отдельная авария. Проба **readiness** управляет *трафиком* — провалил её, и под убирают из `Service`, но оставляют работать. Проба **liveness** управляет *рестартами* — провалил её, и kubelet убивает и пересоздаёт контейнер. Заведи медленную зависимость (флапающую БД) в liveness-пробу — и получишь шторм рестартов: каждый под, который не достучался до БД, убивают, что делает проблему БД хуже, а не лучше. Liveness должна проверять «я в дедлоке?», а не «весь ли мой мир здоров?».

<Inset kind="why" lang="ru">
Тонкая ловушка: проба на `path: /healthz`, которая отдаёт 200, если поднят *веб-сервер*, не говорит раскатке ничего полезного, потому что веб-сервер поднимается почти мгновенно. Проба обязана падать, пока то, что реально нужно запросу — пул к БД, соединение с кэшем, downstream-клиент — ещё инициализируется. Health-чек, который не может покраснеть во время старта, — это украшение, а не клей.
</Inset>

## Стратегия раскатки и миграция — это одно решение

Самый дорогой стык во всей цепочке — между стратегией раскатки и базой данных, потому что флип балансировщика обратим, а изменение схемы обычно нет. Представь blue-green деплой: blue live, green — новая версия, ты флипаешь LB и мгновенно откатываешься, если green ведёт себя плохо. Теперь допустим, релиз green «прибрал» схему — удалил колонку, которую blue всё ещё читает. Флип проходит, green обслуживает нормально. Потом green кидает ошибку, и ты откатываешься на blue — и blue немедленно 500-ит на каждом запросе, потому что нужной колонки больше нет. Твой «мгновенный откат» теперь односторонняя авария.

Дисциплина, которая делает раскатку и миграцию композируемыми, — **expand-contract** (он же «parallel change»): никогда не выкатывай изменение схемы, несовместимое с кодом, который сейчас работает. Ты разбиваешь одно логическое изменение на отдельные деплои, каждый держит **N-1 совместимость** (новая схема работает со старым кодом и наоборот):

1. **Expand** — добавь новую колонку/таблицу; оставь старую. Обе версии работают.
2. **Migrate** — перелей данные в новую форму; пиши из приложения в обе (dual-write).
3. **Contract** — только после того, как *весь* трафик на новом коде, удали старую колонку. Отдельный, более поздний деплой.

Цена реальна: переименование колонки, которое «должно» быть одной миграцией, становится тремя деплоями, размазанными по релизам, плюс dual-write код, который надо не забыть удалить. Выигрыш в том, что любое промежуточное состояние безопасно для отката, поэтому твоя стратегия раскатки сохраняет обратимость, которую обещала.

<TradeoffMatrix
  id="08-deploy-capstone-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Нужно переименовать горячую колонку в таблице на 200M строк, сохранив раскатки zero-downtime обратимыми. Выбери подход."
  options={[
    { name: "Expand-contract на три деплоя: добавить новую колонку, dual-write + backfill, затем удалить старую колонку после того, как весь код перешёл на новый путь", summary: "Parallel change.", correct: true, justification: "Любая промежуточная схема совместима и со старой, и с новой версией приложения (N-1), поэтому раскатка любой версии — и откат к ней — остаётся безопасной. Флип никогда не собирается со схемой, которую откаченный код не может прочесть. Цена — три деплоя плюс временный dual-write код, и это ровно плата за обратимость раскатки." },
    { name: "Одна миграция, переименовывающая колонку, выкатанная вместе с новой версией приложения одним blue-green флипом", summary: "Один заход.", justification: "Работает, только если ты никогда не откатываешься. В тот миг, когда ты флипнул на green, а потом вернулся на blue, blue запрашивает старое имя, которого больше нет, и 500-ит на каждом запросе — флип LB был обратим, а изменение схемы нет, и вместе они собрались в невосстановимое состояние." },
    { name: "Rolling update, где миграция-переименование запускается как initContainer до старта новых подов", summary: "Сначала миграция.", justification: "Во время rolling update старые и новые поды по дизайну работают одновременно. Если миграция переименует колонку заранее, ещё работающие старые поды ломаются немедленно — ты создал несовместимость посреди раскатки, до того как новый код вообще полностью выкатился." },
    { name: "Взять окно обслуживания, остановить трафик, мигрировать, перезапустить всё", summary: "Big-bang даунтайм.", justification: "Технически безопасно, но выбрасывает свойство zero-downtime, ради которого и существует весь пайплайн. Иногда приемлемо для крошечного внутреннего инструмента; для пользовательского сервиса это превращает решённую проблему (expand-contract) в плановый даунтайм." },
  ]}
/>

## Draining и observability замыкают цепочку

Даже с пробами и безопасными миграциями cutover может убить запросы, которые уже были в полёте. Когда под завершается, k8s убирает его из эндпоинтов `Service` *и* шлёт `SIGTERM` — но эти два события гоняются, и LB может ещё пару мгновений роутить запросы на умирающий под. L7-балансировщик умеет draining: перестаёт слать новые запросы и ждёт настраиваемый таймаут (хороший дефолт — 1.5–2× твоего p99 времени запроса), пока завершатся те, что в полёте. Наивная L4-схема, которая просто рвёт на уровне соединения, может оборвать запрос посреди ответа. Сторона приложения обязана сотрудничать: ловить `SIGTERM`, перестать принимать новую работу, доделать запросы в полёте, затем выйти — всё внутри `terminationGracePeriodSeconds` (дефолт 30s), с `preStop`-паузой, чтобы покрыть гонку с удалением эндпоинта.

И единственная причина, по которой ты *узнал*, что авария readiness-пробы в Hook была про 502, а не про «успех», — это observability. Exit-код раскатки говорит, что манифест применился; он не говорит про error rate, p99 latency или saturation на новых подах. Несущий сигнал — сравнение golden-метрик новой версии с baseline старой *во время* раскатки — ровно то, что canary делает автоматически: сдвинуть 5% трафика, понаблюдать error rate и latency пару минут, продвинуть или авто-откатить по метрике, а не по возвращаемому значению команды деплоя.

<Quiz
  id="08-deploy-capstone-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Rolling update рапортует успех за 40 секунд, но потом 30% запросов 502-ят две минуты. Образ, манифест и миграция по отдельности в порядке. Какая причина наиболее вероятна?"
  choices={[
    { label: "Нет readinessProbe, поэтому k8s пометил поды готовыми на старте процесса и пустил трафик до того, как приложение смогло обслуживать", correct: true },
    { label: "Образ был слишком большой, и поды всё ещё его тянули", misconception: "Медленный pull задерживает старт пода вообще; он не даёт «успех» с последующими 502 от подов, которые k8s уже считает готовыми и в Service." },
    { label: "maxSurge выставлен слишком высоко", misconception: "maxSurge управляет тем, сколько лишних подов поднимается во время раската; он не заставляет трафик бить в ещё не обслуживающие поды. Это делает отсутствующий readiness-гейт." },
    { label: "Истёк TLS-сертификат на Ingress", misconception: "Истёкший серт стабильно валит handshake, а не даёт временный 30% всплеск, который сам рассасывается по мере прогрева подов." },
  ]}
/>

<Quiz
  id="08-deploy-capstone-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Почему blue-green флип опасен в паре с миграцией, которая удаляет колонку?"
  choices={[
    { label: "Флип LB обратим, а удалённая колонка нет, поэтому откат на blue 500-ит на колонке, которой теперь нет", correct: true },
    { label: "Blue-green удваивает стоимость инфраструктуры во время флипа", misconception: "Правда, но к корректности отношения не имеет — опасность тут в том, что откат больше не безопасен, а не во временной стоимости двух окружений." },
    { label: "Green не может подключиться к той же базе, что и blue", misconception: "Общая база — норма для blue-green; проблема в том, что общая схема несовместима с версией, на которую ты можешь откатиться." },
    { label: "Миграции всегда требуют даунтайма", misconception: "Не требуют — expand-contract миграции это именно то, чем ты избегаешь даунтайма; проблема в деструктивном изменении, которое ломает N-1 совместимость." },
  ]}
/>

<DragOrder
  id="08-deploy-capstone-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь безопасный релиз переименования колонки так, чтобы каждое промежуточное состояние было безопасным для отката:"
  items={[
    { id: "a", label: "Деплой 1 — Expand: добавь новую колонку, оставь старую на месте" },
    { id: "b", label: "Деплой 2 — выкати код, который dual-write пишет в обе колонки и backfill-ит существующие строки" },
    { id: "c", label: "Проверь, что все работающие поды на новом коде и читают новую колонку" },
    { id: "d", label: "Деплой 3 — Contract: удали старую колонку, раз её никто не читает" },
    { id: "e", label: "Убери теперь мёртвый dual-write код отдельным деплоем" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Релиз — цепочка. Инженерь стыки: readiness-проба, чтобы «готов» значило «обслуживает», expand-contract для N-1 совместимости схемы, L7-draining для запросов в полёте, раскатка по метрикам.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="08-deploy-capstone-retrieval"
  lang="ru"
  questions={[
    { q: "Проведи коллегу через то, почему идеально верный rolling update всё равно может вызвать аварию, и какой один кусок конфигурации это предотвращает.", a: "Контракт rolling update такой: подними новый под, дождись, пока он готов, добавь его в Service, затем выведи старый. Слово «готов» — несущая часть. Без readinessProbe Kubernetes считает под готовым в тот миг, когда стартовал главный процесс контейнера — а это до того, как приложение открыло пул к базе, прогрело кэши или подключилось к downstream-зависимостям. Поэтому rolling update, ведя себя ровно как задумано, добавляет ещё не обслуживающий под в эндпоинты Service, и трафик льётся в процесс, который отдаёт 502 или connection-refused. Деплой рапортует успех, потому что манифест применился и поды «running»; пользователи видят ошибки, потому что running — это не serving. Единственный фикс — readinessProbe, которая реально дёргает настоящую зависимость (httpGet на эндпоинт /healthz/ready, который проверяет пул к БД), в сочетании с maxUnavailable: 0 и maxSurge: 1, чтобы ёмкость никогда не падала ниже желаемой во время раската. Проба превращает «процесс поднят» в «приложение может обслужить запрос» — контракт, который раскатке и был нужен всё это время." },
    { q: "Объясни expand-contract и почему именно он делает стратегию раскатки и миграцию базы безопасно композируемыми.", a: "Expand-contract (он же parallel change) разбивает одно изменение схемы на отдельные деплои так, что каждое промежуточное состояние совместимо и с работающим сейчас кодом, и со следующей версией — правило N-1 совместимости. Чтобы переименовать колонку, сначала Expand: добавь новую колонку, сохранив старую, чтобы и старый, и новый код работали. Затем Migrate: перелей данные в новую колонку и заставь приложение писать в обе (dual-write). Только после того, как каждый под подтверждённо работает на новом коде, читающем новую колонку, делаешь Contract: отдельный, более поздний деплой, который удаляет старую колонку. Это важно, потому что стратегия раскатки обещает обратимость — blue-green даёт флипнуть назад, rolling update гоняет старые и новые поды бок о бок — но деструктивная миграция ломает это обещание. Если удалить колонку в том же релизе, что флипает трафик, откат приземляется на код, который запрашивает колонку, которой больше нет, и он 500-ит на каждом запросе. Флип LB был обратим; изменение схемы нет; собранные вместе они дают невосстановимое состояние. Expand-contract держит схему обратимой на каждом шаге, поэтому стратегия раскатки сохраняет обещанную безопасность. Цена — три деплоя и временный dual-write код вместо одной миграции, и это явная плата за обратимость zero-downtime." },
  ]}
/>

<Recap lang="ru">
Деплой — это не семь независимых шагов; это один собранный объект, и аварии живут в стыках, где две по отдельности верные стадии встречаются под контрактом, который никто не проверял. Readiness-проба — это контракт между «процесс работает» и «приложение может обслуживать», и без неё безупречный rolling update роутит трафик на мёртвые поды. Стратегия раскатки и миграция базы — одно решение: обратимый флип LB в композиции с деструктивным, необратимым изменением схемы даёт состояние, на которое нельзя откатиться, и поэтому expand-contract — держать каждую схему N-1 совместимой через отдельные деплои expand, migrate и contract — это клей, который позволяет им сосуществовать. L7-балансировщик, который умеет draining, и приложение, которое обрабатывает SIGTERM внутри terminationGracePeriodSeconds, держат запросы в полёте живыми сквозь cutover, а метрики раскатки (error rate и p99 против baseline, как автоматически проверяет canary) — это то, что делает «успех» здоровьем, а не просто «применилось». Собери образ лёгким, запушь неизменяемый digest, объяви объекты, инжекть секреты в runtime и закодируй всё как IaC, чтобы окружение, которое ты тестил, было тем, что ты выкатил — тогда цепочка держится, потому что ты заинженерил стыки, а не только звенья.
</Recap>
