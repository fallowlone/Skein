---
concepts:
- deployment-chain
- readiness-probe
- expand-contract-migration
- l7-draining
- secret-injection
- iac-drift
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/
- https://kubernetes.io/docs/concepts/configuration/secret/
- https://www.bytebase.com/blog/database-blue-green-deployment/
status: ready
summary: "Вопросы на свободное воспроизведение по всему треку deployment — восстановите, как образ, probes, rollout, миграция, LB, секреты и IaC складываются в один релиз, затем откройте эталонный ответ."
title: 'Капстоун deployment: вопросы на воспроизведение'
track: deployment
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение по памяти эффективнее перечитывания. Каждый вопрос просит восстановить контракт, связывающий две и более стадии цепочки деплоя. Сначала проговорите или запишите полный ответ по памяти, потом откройте эталонный и сравните.
</Hook>

<Goal lang="ru">
Восстановите хребет всего трека, не подглядывая: почему релиз — это один составной объект, и что обеспечивает контракт на каждом шве между стадиями.
</Goal>

<RetrievalDrawer
  client:load
  id="08-putting-it-together-short-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните, почему релиз — это один составной объект, а не семь независимых шагов, и где живут его отказы.", a: "Трек deployment учит каждую стадию по отдельности: собрать лёгкий многостадийный образ, запушить неизменяемый digest в registry, объявить объекты k8s, выбрать стратегию rollout, поставить впереди балансировщик, инжектить секреты в runtime и закодифицировать всё как IaC. Но ни одна из них не выкатывает софт в одиночку — релиз есть композиция всех, а у композиции есть эмерджентные режимы отказа, которые тесты отдельной стадии не ловят. Каждая стадия передаёт артефакт следующей по неявному контракту: сборка даёт образ, rollout считает, что «готов» значит «обслуживает», LB считает, что может draining, миграция считает, что работающий код соответствует схеме. Авария живёт не внутри стадии — каждая может быть корректной по отдельности — она живёт в шве, где две из них встречаются по контракту, который никто не обеспечил. Инженерия швов (probes, expand-contract, draining, закрепление digest, IaC) — это то, что держит цепочку." },
    { q: "Почему идеально корректный rolling update всё равно может вызвать аварию и какая одна настройка это предотвращает? Заодно противопоставьте readiness и liveness.", a: "Контракт rolling update: запустить новый pod, дождаться готовности, добавить в Service, затем вывести старый (maxUnavailable: 0, maxSurge: 1 не дают ёмкости просесть). Несущее слово — «готов». Без readinessProbe k8s помечает pod готовым в момент старта процесса — до открытия пула к БД или прогрева кэшей — поэтому rolling update, действуя ровно по проекту, направляет трафик в запущенный, но не обслуживающий pod, и клиенты ловят 502. Деплой рапортует успех, потому что манифест применился; пользователи видят ошибки, потому что запущен — не значит обслуживает. Фикс — readinessProbe, проверяющий реальную зависимость (httpGet /healthz/ready, который проверяет пул к БД). Readiness управляет трафиком — провал убирает pod из Service, но оставляет живым; liveness управляет рестартами — провал заставляет kubelet убить и пересоздать контейнер. Никогда не подключайте медленную внешнюю зависимость в liveness, иначе временный сбой БД вызывает шторм рестартов на весь кластер, который делает только хуже." },
    { q: "Дайте определение expand-contract и объясните, почему именно это делает стратегию rollout и миграцию БД безопасно компонуемыми.", a: "Expand-contract (parallel change) разбивает одно изменение схемы на отдельные деплои так, чтобы каждое промежуточное состояние было совместимо и с работающим кодом, и со следующей версией — правило N-1. Чтобы переименовать колонку: (1) Expand — добавить новую колонку, сохранить старую; работают обе версии. (2) Migrate — забэкфилить данные в новую форму и dual-write из приложения. (3) Contract — только после того как каждый pod подтверждён на новом коде, читающем новую колонку, дропнуть старую отдельным поздним деплоем. Это важно, потому что стратегия rollout обещает обратимость: blue-green переключает LB обратно мгновенно, rolling update гоняет старые и новые pod бок о бок. Деструктивная миграция ломает обещание — дропни колонку в том же релизе, что переключает трафик, и откат попадёт на код, запрашивающий несуществующую колонку, давая 500 на каждом запросе. Переключение LB было обратимо; изменение схемы — нет; вместе они дают невосстановимое состояние. Expand-contract держит схему обратимой на каждом шаге. Цена — три деплоя плюс временный dual-write код вместо одной миграции — явная плата за обратимость без простоя." },
    { q: "Даже с probes и безопасными миграциями переключение может оборвать запросы в полёте. Объясните гонку и двусторонний фикс с L7 и SIGTERM.", a: "Когда pod завершается, k8s делает две гонящиеся вещи: убирает pod из endpoints Service и шлёт SIGTERM. Короткое окно LB может всё ещё направлять запросы в умирающий pod. L4-балансировщик маршрутизирует по IP/порту и не умеет draining на уровне запроса, поэтому может оборвать соединения на середине ответа; L7-балансировщик читает HTTP, перестаёт слать новые запросы и ждёт настраиваемый таймаут drain (хороший дефолт — 1.5–2× p99 времени запроса), пока запросы в полёте завершатся. Приложение должно сотрудничать: поймать SIGTERM, перестать принимать новую работу, завершить запросы в полёте, затем выйти — всё в пределах terminationGracePeriodSeconds (дефолт 30с) — с preStop sleep для покрытия гонки удаления из endpoints, чтобы LB перестал маршрутизировать до того, как процесс начнёт завершаться. L7-draining держит фронт; обработка SIGTERM держит тыл; вместе они сохраняют запросы живыми на переключении." },
    { q: "Почему секреты надо инжектить при деплое/в runtime, а не запекать в образ, и в чём подвох с Kubernetes Secret?", a: "Секрет, запечённый в Dockerfile (через ENV, ARG или скопированный файл), становится частью неизменяемого слоя образа. Любой, кто может стянуть образ, его прочитает; он сохраняется в истории слоёв, даже если поздняя инструкция его сбросит; и не ротируется без пересборки и передеплоя. Build-time и runtime — разные границы доверия: образ — широко распространяемый артефакт, поэтому креды принадлежат runtime-границе, инжектятся как переменные окружения или примонтированные файлы, которые оркестратор подаёт при деплое. Подвох: Kubernetes Secret лишь base64-кодирован, не зашифрован — base64 тривиально обратим. Поэтому ещё включают шифрование at-rest для etcd (или используют внешний менеджер вроде Vault / облачных секретов), сужают RBAC, чтобы Secret могли читать немногие, и предпочитают примонтированные файлы переменным окружения, так как окружение чаще утекает в логи и crash-дампы." },
    { q: "Что такое configuration drift, почему он опасен даже когда сервис работает, и как IaC замыкает контур с остальной цепочкой?", a: "Drift — это расхождение между фактическим работающим состоянием и желаемым, объявленным в IaC — обычно из-за внеполосной правки kubectl или изменения в консоли во время пожара. Он опасен даже пока сервис работает, потому что конфигурация, протестированная в коде, больше не та, что работает: следующий terraform/IaC apply, замена узла или событие масштабирования вернут реальность к объявленному состоянию и тихо отменят недокументированное изменение, часто прямо во время инцидента. IaC работает, сравнивая желаемое объявленное состояние с записанным state-файлом для вычисления плана — этот файл одновременно источник истины и опасность. Замкнуть контур — значит сделать IaC авторитетным: каждое изменение идёт через систему контроля версий, CI запускает plan по расписанию и алертит на любой diff, а внеполосные правки запрещены. Тогда среда, которую вы протестировали, и есть та, что вы выкатили — контракт, который позволяет гарантиям всех остальных стадий реально держаться в production." },
  ]}
/>

<Recap lang="ru">
Если вы смогли восстановить каждый ответ по памяти, вы держите хребет трека: релиз — это один составной объект, и вы инженерите швы — неизменяемый digest фиксирует артефакт, readinessProbe делает «готов» равным «обслуживает», expand-contract держит rollout и миграцию N-1 совместимыми, L7-draining плюс обработка SIGTERM защищают запросы в полёте, runtime-инжект секретов держит креды ротируемыми и вне образа, а IaC как единственный источник истины убивает drift. Звенья никогда не были сложной частью; сложны контракты между ними. Теперь, когда будете выкатывать следующий релиз, у каждого шва есть имя — и вы будете знать, какой проверить первым, когда придёт авария.
</Recap>
