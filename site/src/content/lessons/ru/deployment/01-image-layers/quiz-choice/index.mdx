---
concepts:
- build-cache
- layer-ordering
- multi-stage-builds
- image-size
- dockerignore
- layer-history-secrets
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://docs.docker.com/build/cache/invalidation/
- https://docs.docker.com/build/building/multi-stage/
- https://docs.docker.com/build/building/best-practices/
status: ready
summary: "Тест с выбором на синтез по всему юниту — поведение кэша как префикса, порядок инструкций, multi-stage build, компромиссы базовых образов, .dockerignore и секрет, который навсегда остаётся в истории."
title: 'Image layers: тест с выбором ответа'
track: deployment
unit: 01-image-layers
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает решение, которое ты принимаешь, когда пишешь или ревьюишь реальный Dockerfile — не определение для заучивания, а компромисс по кэшу и размеру образа, который надо продумать.
</Hook>

<Goal lang="ru">
Убедись, что связываешь модель кэша-как-префикса, порядок инструкций, multi-stage build, выбор базового образа, гигиену build-контекста и ловушку секрета в неизменяемом слое — тот синтез, к которому вёл урок.
</Goal>

<Quiz
  id="01-image-layers-choice-1"
  lessonSlug="01-image-layers"
  lang="ru"
  question={"Коллега переносит RUN npm ci прямо перед финальный CMD, рассуждая: 'установка дорогая, значит должна идти последней'. Что на самом деле произойдёт с кэшем при обычной правке исходника?"}
  choices={[
    { label: "Станет хуже: установка теперь ниже COPY . ., поэтому любая правка исходника бьёт слой COPY и install перезапускается. Дорогие шаги место в начале, глубоко в префиксе кэша — а не последними.", correct: true },
    { label: "Станет лучше, ведь самый дорогой шаг выполняется в самом конце.", misconception: "Кэш — строгий префиксный матч: первый изменившийся слой пересобирается вместе со всеми после него. Позиция в файле не важна; важно, стоит ли слой до или после часто меняющихся. Install должен идти перед COPY . ." },
    { label: "Без разницы — npm ci кэшируется сам по себе независимо от позиции.", misconception: "RUN npm ci кэшируется по своим входам и по всем предыдущим слоям. Если вышестоящий слой COPY . . инвалидируется, npm ci ниже промаха и перезапустится, хотя текст его команды не менялся." },
    { label: "Кэш-хиты улучшатся, но финальный образ станет больше.", misconception: "Перестановка инструкций не меняет размер образа — те же слои существуют. Меняется лишь то, какие слои остаются в кэше. Здесь перестановка вредит кэшу, а размер не затронут в любом случае." },
  ]}
/>

<Quiz
  id="01-image-layers-choice-2"
  lessonSlug="01-image-layers"
  lang="ru"
  question={"В Dockerfile эти две строки — отдельные инструкции. Сборки продолжают ставить версии пакетов многомесячной давности даже после обновления upstream-репозитория. Почему?\n\nRUN apt-get update\nRUN apt-get install -y curl"}
  choices={[
    { label: "RUN кэшируется по тексту команды, а не по эффекту. Неизменный 'apt-get update' — кэш-хит, поэтому переиспользуется слой со старым индексом пакетов, и install резолвится по нему. Объедини их в один RUN, чтобы они делили ключ и инвалидировались вместе.", correct: true },
    { label: "apt-get недетерминирован и не кэшируется надёжно.", misconception: "Кэш работает ровно как задумано — слишком хорошо. Docker переиспользует слой update, потому что текст его инструкции не менялся. Проблема в разбиении update и install на два ключа кэша, а не в недетерминизме." },
    { label: "Базовый образ пинит старое apt-зеркало, которое надо сменить.", misconception: "Зеркало не причина; причина — закэшированный слой 'apt-get update'. Даже со свежим зеркалом слой-хит update отдаёт старый индекс. Объедини update и install в одном RUN." },
    { label: "Надо передавать --no-cache в каждой сборке, чтобы пакеты были свежими.", misconception: "--no-cache отбрасывает весь кэш и обесценивает смысл layer cache. Точечный фикс — слить update и install, чтобы обновление индекса и установка инвалидировались как единое целое." },
  ]}
/>

<Quiz
  id="01-image-layers-choice-3"
  lessonSlug="01-image-layers"
  lang="ru"
  question={"Single-stage Go-сборка отдаёт образ ~180 МБ. Коллега предлагает дописать в конец 'RUN apt-get remove -y build-essential && rm -rf /usr/local/go', чтобы ужать. Образ уменьшится?"}
  choices={[
    { label: "Нет. Удаление файлов в более позднем слое добавляет diff-удаление сверху — ранние слои физически всё ещё содержат байты, образ остаётся большим, а toolchain восстановим. Используй multi-stage build и COPY только бинарь в финальный stage на scratch/distroless.", correct: true },
    { label: "Да — apt-get remove и rm возвращают место, и образ уменьшается соответственно.", misconception: "Слои — неизменяемые append-only diff-ы. Удаление в позднем слое скрывает файлы в объединённом виде, но никогда не правит ранние слои, поэтому байты — и размер образа — сохраняются. Только свежий финальный stage реально их убирает." },
    { label: "Да, если удаление выполнить в том же RUN, что и установку.", misconception: "Очистка в одном RUN помогает только для файлов, созданных и удалённых внутри той же инструкции. Здесь toolchain пришёл из базового образа и ранних слоёв, так что RUN-удаление не ужмёт его. Правильный инструмент — multi-stage." },
    { label: "Нет, но переход на больший базовый образ ubuntu это исправит.", misconception: "Бо́льший базовый образ делает образ больше, а не меньше. Фикс в обратном направлении: собирай в жирном stage, затем COPY артефакт в минимальный финальный stage вроде scratch или distroless/static." },
  ]}
/>

<Quiz
  id="01-image-layers-choice-4"
  lessonSlug="01-image-layers"
  lang="ru"
  question={"Нужен самый маленький, самый закалённый production-образ для самодостаточного скомпилированного сервиса, и шум CVE-сканера должен быть близок к нулю. Какой базовый образ финального stage — senior-выбор по умолчанию, и какую цену ты принимаешь?"}
  choices={[
    { label: "Distroless (gcr.io/distroless/*): только приложение и runtime-зависимости, без shell, без пакетного менеджера — минимальная attack surface, и сканеры почти ничего не находят, потому что почти ничего не установлено. Цена — отлаживаемость: нет shell для docker exec, поэтому опираешься на логи, ephemeral debug-контейнеры и COPY каждой runtime-зависимости.", correct: true },
    { label: "Alpine, потому что это самый маленький базовый образ и apk позволяет доставить инструменты позже.", misconception: "Alpine мал и удобен, но несёт shell и пакетный менеджер (больше attack surface, чем у distroless), а его musl libc может ломать нативные модули и давать сюрпризы с DNS/производительностью. Хорош, когда реально нужен shell, но не самый закалённый выбор." },
    { label: "Полный базовый образ сборки, переиспользованный как runtime, чтобы избежать второго FROM.", misconception: "Это ровно тот анти-паттерн, ради которого существует multi-stage build — ты тащишь компиляторы, dev-зависимости и apt-кэши в production: сотни МБ bloat и attack surface, которые никогда не обслуживают запрос." },
    { label: "ubuntu:22.04 с удалёнными после установки инструментами сборки через apt-get remove.", misconception: "Удаление инструментов в позднем слое не ужимает образ (байты остаются в ранних слоях) и сохраняет большой glibc-базис с shell и пакетным менеджером. Свежий финальный stage на distroless или scratch и меньше, и закалённее." },
  ]}
/>

<Quiz
  id="01-image-layers-choice-5"
  lessonSlug="01-image-layers"
  lang="ru"
  question={"В CI сборке нужен токен приватного registry для скачивания зависимостей. Инженер пишет COPY token.txt /tmp/token, использует его в RUN, затем RUN rm /tmp/token в следующей инструкции. Финальный образ опубликован. В чём экспозиция?"}
  choices={[
    { label: "Токен восстановим навсегда. COPY сделал его слоем; последующий rm — лишь diff, скрывающий его в объединённом виде. Любой извлечёт ранний слой через docker history или распаковав образ. Используй RUN --mount=type=secret, чтобы токен не попал ни в один слой, или держи его только в отбрасываемом builder stage.", correct: true },
    { label: "Безопасно — rm в следующей инструкции удаляет токен из образа.", misconception: "Слои — стек неизменяемых diff-ов. Rm добавляет новый diff, помечающий файл удалённым в объединённом дереве, но не может стереть ранний слой, который физически держит токен. Он восстановим в любом случае." },
    { label: "Безопасно, пока образ хранится только в приватном registry.", misconception: "Приватный registry управляет тем, кто может pull, а не тем, есть ли секрет в слоях. Любой, кто может pull (или имеет локальный образ), извлечёт его. Секрет вообще не должен был стать слоем." },
    { label: "Безопасно, потому что токен был только в /tmp, который контейнеры считают эфемерным.", misconception: "Эфемерная-в-рантайме семантика /tmp нерелевантна сборке: COPY записал файл в постоянный слой образа на этапе сборки. Путь не освобождает его от истории слоёв." },
  ]}
/>

<Quiz
  id="01-image-layers-choice-6"
  lessonSlug="01-image-layers"
  lang="ru"
  question={"docker build медленно даже стартует — несколько секунд проходят до первой инструкции — и COPY . . иногда тащит локальный .env разработчика в образ. В репозитории нет .dockerignore. Какое одно изменение решает оба?"}
  choices={[
    { label: "Добавить .dockerignore со списком node_modules, .git, *.log и .env. Весь build-контекст сначала тарится и отправляется демону; исключение объёмных и чувствительных путей сокращает upload (быстрый старт) и не даёт COPY . . грести секреты и мусор в слой.", correct: true },
    { label: "Заменить COPY . . на ADD . ., чтобы Docker стримил файлы эффективнее.", misconception: "ADD добавляет поведение URL/авто-распаковки, а не меньший контекст. Медленный старт — это тар контекста, отправляемый демону, и ADD . . всё равно скопирует .env. .dockerignore — фикс обоих симптомов." },
    { label: "Запинить меньший базовый образ, чтобы ускорить сборку.", misconception: "Размер базового образа влияет на pull и размер финального образа, а не на pre-build upload контекста или на то, что включает COPY . .. И медленный старт, и утёкший .env идут из нефильтрованного build-контекста." },
    { label: "Запускать сборку с --no-cache, чтобы контекст перечитывался начисто каждый раз.", misconception: "--no-cache форсит полные пересборки и пере-отправляет тот же раздутый контекст — он делает старт ещё медленнее и ничего не делает с утечкой .env. Правильный рычаг — фильтрация контекста через .dockerignore." },
  ]}
/>

<Recap lang="ru">
Сквозная линия — одна модель: образ это неизменяемые сложенные слои, кэш — строгий префикс, а RUN кэшируется по тексту команды, а не по эффекту. Упорядочивай от редко- к часто-меняющемуся, чтобы дорогой install оставался кэш-хитом; сливай update-and-install, чтобы они инвалидировались вместе; multi-stage, чтобы компилировать жирно и отгружать тонко вместо удаления байтов, которые никогда не покидают историю; выбирай distroless, когда нужна минимальная attack surface и ты принимаешь потерю shell; и держи build-контекст тонким через .dockerignore. Сложнейшая ловушка — секрет, добавленный и затем rm'нутый — следует из того же правила неизменяемости: его нельзя удалить обратно, поэтому он не должен становиться слоем вообще. Теперь, когда ты видишь любой из этих шести паттернов на ревью Dockerfile, ты знаешь, какой рычаг дёрнуть и почему.
</Recap>
