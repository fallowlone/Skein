---
concepts:
- build-cache
- layer-ordering
- multi-stage-builds
- image-size
- layer-history-secrets
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://docs.docker.com/build/cache/invalidation/
- https://docs.docker.com/build/building/multi-stage/
- https://docs.docker.com/build/building/best-practices/
status: ready
summary: "Вопросы на свободное припоминание по всему юниту. Сначала ответь своими словами, затем раскрой модельный ответ и сравни."
title: 'Image layers: тест на припоминание'
track: deployment
unit: 01-image-layers
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый вопрос проговори или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет механизм, когда ты ревьюишь реальный Dockerfile.
</Hook>

<Goal lang="ru">
Реконструируй ключевые механизмы юнита — кэш как префикс по содержимому, правило порядка, ужатие через multi-stage, компромиссы базовых образов и ловушку секрета в неизменяемом слое — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:visible
  id="01-image-layers-short-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое слой Docker-образа и по каким двум входам build cache ключует каждый слой?", a: "Образ — не один блоб, а стек read-only файловых diff-ов, представленных через union filesystem (overlay2 на Linux), так что контейнер видит одно объединённое дерево, а хранилище — цепочку diff-ов. Каждый RUN, COPY и ADD создаёт ровно один слой — файловые изменения этого шага. Build cache ключует слой по двум вещам: самому тексту инструкции и содержимому, от которого он зависит. Для COPY и ADD Docker считает чек-сумму копируемых файлов, поэтому один изменённый байт меняет ключ и даёт промах. Для RUN ключ — буквальный текст команды; Docker не смотрит, что команда реально делает, и это отдельная ловушка." },
    { q: "Сформулируй кардинальное правило порядка и объясни, почему COPY . . перед RUN npm ci рушит время сборки.", a: "Кэш — строгий префиксный матч: первый слой, чей ключ изменился, пересобирается вместе со всеми после него. Поэтому кардинальное правило — упорядочивать инструкции от редко- к часто-меняющимся: редкие шаги глубоко в префиксе, исходники-каждый-коммит последними. COPY . . считает чек-сумму каждого файла контекста, так что правка любого исходника меняет эту чек-сумму и инвалидирует слой COPY. Если RUN npm ci стоит после, install ниже промаха и перезапускается, таща все зависимости по сети на каждый push. Фикс — разбить копирование по частоте изменений: сначала COPY package.json package-lock.json ./, затем RUN npm ci, затем COPY . . для исходников — install остаётся кэш-хитом на подавляющем большинстве коммитов. Команды часто сообщают о падении времени CI-сборок примерно на 70% от одной этой перестановки." },
    { q: "Почему 'RUN apt-get update' и отдельный 'RUN apt-get install' отдают устаревшие пакеты, и как это исправить?", a: "Потому что RUN кэшируется по тексту команды, а не по эффекту. Строка 'apt-get update' не меняется, поэтому Docker считает этот слой кэш-хитом и переиспользует индекс пакетов многомесячной давности; отдельный install затем резолвится по этому старому индексу. Фикс — объединить их в одну инструкцию: RUN apt-get update && apt-get install -y <pkg> — чтобы они делили один ключ кэша и инвалидировались вместе. Та же логика объясняет, почему важно расположение ARG: build-arg меняет нижестоящие ключи кэша, поэтому объявляй его так поздно, как позволяет сборка." },
    { q: "Как multi-stage build ужимает образ и какие примерно дельты размера ожидать?", a: "Ты пишешь несколько FROM-stage в одном Dockerfile: жирный builder stage делает тяжёлую работу (компиляторы, dev-зависимости, кэши сборки), затем тонкий runtime stage делает COPY --from=builder только готовых артефактов. Лишь последний stage становится опубликованным образом; builder полностью отбрасывается, так что его toolchain не отгружается. Дельты большие: single-stage Node-сборка около 380 МБ+ падает примерно до 60 МБ на slim или distroless базе; Go-бинарь около 180 МБ схлопывается до нескольких МБ на scratch или distroless/static, потому что скомпилированному бинарю не нужен рантайм. Один широко цитируемый пример ужал 843 МБ до 12.1 МБ — сокращение на 98.6%." },
    { q: "Сравни distroless и alpine как базовые образы финального stage — когда тянуться за каждым?", a: "Alpine крошечный, потому что использует musl libc и BusyBox, и несёт apk, что удобно, когда нужно доставить инструмент. Подвох в том, что musl отличается от glibc достаточно, чтобы ломать нативные модули и давать редкие сюрпризы с DNS или производительностью, а наличие shell и пакетного менеджера расширяет attack surface. Distroless несёт только приложение и его прямые runtime-зависимости — без shell, без пакетного менеджера — поэтому attack surface и шум CVE-сканера минимальны, но нельзя docker exec shell для отладки, и каждую runtime-зависимость надо доставить через COPY. Тянись за distroless для закалённого самодостаточного скомпилированного сервиса; за alpine — когда реально нужен shell или установка runtime-пакетов." },
    { q: "Почему удаление секрета в более позднем слое его не убирает, и что делать вместо этого?", a: "Образ — стек неизменяемых append-only diff-ов. COPY ключа в образ создаёт слой, содержащий файл; последующий RUN rm — лишь ещё один diff, помечающий его удалённым в объединённом виде — он никогда не правит и не стирает ранний слой, который физически держит байты. Любой запустит docker history или распакует слои и восстановит файл, запушенный или нет. Правильные подходы держат секрет вне любого постоянного слоя: BuildKit secret mount, RUN --mount=type=secret,id=token, который открывает секрет только для одной инструкции и пишет его в ноль слоёв; или multi-stage build, где секрет живёт только в отбрасываемом builder stage. Принцип в том, что секрет не должен становиться слоем вообще — его нельзя удалить обратно." },
  ]}
/>

<Recap lang="ru">
Если ты смог реконструировать каждый ответ по памяти, ты держишь хребет юнита: слой — это diff с ключом по содержимому, кэш — строгий префикс, RUN ключуется по тексту, а не по эффекту, порядок от редко- к часто-меняющемуся держит дорогой install в кэше, multi-stage build компилирует жирно и отгружает тонко, distroless меняет shell на минимальную attack surface, а секрет, добавленный и затем rm'нутый, навсегда живёт в раннем слое — поэтому он не должен становиться слоем вообще. Теперь, когда ты встретишь Dockerfile в реальном коде, эти шесть ответов — твой чек-лист до апрува или мёржа.
</Recap>
