---
concepts:
- image-layers
- build-cache
- layer-ordering
- multi-stage-builds
- dockerignore
- layer-history-secrets
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-overview
sources:
- https://docs.docker.com/build/cache/invalidation/
- https://docs.docker.com/build/building/multi-stage/
- https://docs.docker.com/build/building/best-practices/
- https://xygeni.io/blog/dockerfile-secrets-why-layers-keep-your-sensitive-data-forever/
spiral: []
status: ready
summary: Образ — стек read-only слоёв, каждый шаг Dockerfile кэшируется по инструкции
  и хэшу входа. Расставь шаги от редко- к часто-меняющимся, иначе каждая правка переустанавливает
  всё — а удалённый секрет остаётся в истории.
title: 'Слои образа и кэш сборки: порядок решает всё'
track: deployment
unit: 01-image-layers
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
Однострочная правка CSS запускает четырёхминутную CI-сборку. Каждый пуш — то же самое: `npm install` бежит с нуля, тянет 1200 пакетов по сети, хотя `package.json` не менялся. Кто-то написал `COPY . .` перед `RUN npm install`, поэтому правка любого файла инвалидирует слой установки. Команда платила ~3 минуты за пуш месяцами — кэш, который ни разу не попадал, потому что Dockerfile задавал ему неправильный вопрос первым. За следующие несколько минут ты увидишь, почему так происходит, и единственную перестановку, которая превращает это в попадание за двадцать секунд.
</Hook>

<Crux>Образ — стек read-only слоёв, кэшируемых по инструкции + хэшу входа: первый изменённый шаг пересобирается со всем, что после.</Crux>

<Explanation>

## Слой — это diff, а ключ кэша — содержимое

Docker-образ — это не один blob. Это стек read-only слоёв, показанный через union-файловую систему (overlay2 на Linux — драйвер хранилища, накладывающий слои друг на друга без копирования): запущенный контейнер видит одно слитое дерево, но хранилище — цепочка diff'ов. Каждый `RUN`, `COPY` и `ADD` в Dockerfile порождает один слой — изменения файловой системы, которые сделал этот шаг, и не более.

Кэш сборки ключует каждый слой по двум вещам: по **самой строке инструкции** и по **содержимому, от которого она зависит**. Для `COPY` и `ADD` Docker считает чек-сумму копируемых файлов; меняется байт — меняется чек-сумма, и кэш промахивается. Для `RUN` ключ кэша — это буквальный текст команды; Docker *не* смотрит, что команда реально делает, — отдельная ловушка (ниже). Правило беспощадно: **в тот момент, когда ключ одного слоя меняется, этот слой и каждый слой после него пересобираются.** Кэш — это совпадение по префиксу. Ты держишь его только до первого промаха.

Этот единственный факт двигает каждую оптимизацию в уроке. Твоя задача как автора Dockerfile — расставить инструкции так, чтобы шаги, которые редко меняются, шли в начале (глубоко в префиксе кэша), а шаги, которые меняются на каждый коммит — твой исходный код — шли в конце.

## Кардинальное правило: от редко- к часто-меняющимся

Вот баг из хука и его фикс.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Шаг</th>
        <th class="border border-border px-3 py-2 text-left">Плохой порядок (кэш бьётся на каждой правке)</th>
        <th class="border border-border px-3 py-2 text-left">Хороший порядок (установка остаётся в кэше)</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">1</td>
        <td class="border border-border px-3 py-2"><code>COPY . .</code></td>
        <td class="border border-border px-3 py-2"><code>COPY package.json package-lock.json ./</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">2</td>
        <td class="border border-border px-3 py-2"><code>RUN npm ci</code></td>
        <td class="border border-border px-3 py-2"><code>RUN npm ci</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">3</td>
        <td class="border border-border px-3 py-2">—</td>
        <td class="border border-border px-3 py-2"><code>COPY . .</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Эффект правки одного файла</td>
        <td class="border border-border px-3 py-2">Чек-сумма шага 1 меняется → установка бежит заново (минуты)</td>
        <td class="border border-border px-3 py-2">Промахивается только шаг 3 → установка попадает в кэш (секунды)</td>
      </tr>
    </tbody>
  </table>
</div>

В хорошем порядке `COPY package.json package-lock.json ./` меняет свою чек-сумму, только когда меняется манифест зависимостей. Правишь компонент — шаги 1 и 2 остаются в кэше; пересобирается лишь финальный `COPY . .` и то, что следует. Это самое высокорычажное изменение, которое ты можешь внести в Dockerfile: команды регулярно сообщают о падении времени CI-сборки на 70% от одного только кэширования слоёв, потому что многоминутная установка зависимостей превращается в попадание в кэш за доли секунды на подавляющем большинстве коммитов.

Тот же принцип обобщается. Пинни базовый образ и ставь OS-пакеты рано; они меняются максимум раз в месяц. Дальше копируй lock-файлы и ставь зависимости. Исходники и сборку — последними. Градиент частоты — редко-меняющееся внизу, каждый-коммит наверху — это вся игра.

<Inset kind="why" lang="ru">
`RUN` кэшируется по *тексту* команды, а не по её результату. `RUN apt-get update` с радостью переиспользует кэшированный слой многомесячной давности, потому что строка не менялась, — и ты ставишь устаревшие индексы пакетов, а потом `apt-get install` против них. Фикс — соединить их в одну инструкцию: `RUN apt-get update && apt-get install -y curl`. Теперь у них общий ключ кэша, и инвалидируются они вместе. Поэтому же важно размещение `ARG`: build-аргумент меняет ключи кэша ниже по тексту, поэтому объявляй его так поздно, как позволяет сборка.
</Inset>

## Multi-stage: компилируй жирно, отгружай тонко

Твоему тулчейну сборки — компиляторам, dev-зависимостям, `node_modules`, набитому build-time пакетами, кэшам `apt` — нечего делать в образе, который ты гоняешь в проде. Он раздувает образ, расширяет поверхность атаки и замедляет каждый pull и деплой. Multi-stage сборки решают это: ты пишешь несколько `FROM`-стейджей в одном Dockerfile, делаешь тяжёлую работу в жирном builder-стейдже, потом через `COPY --from=builder` переносишь только готовые артефакты в тонкий runtime-стейдж. Опубликованным образом становится только последний стейдж; builder отбрасывается.

```dockerfile
# syntax=docker/dockerfile:1
FROM node:22 AS builder
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM gcr.io/distroless/nodejs22-debian12
WORKDIR /app
COPY --from=builder /app/dist ./dist
COPY --from=builder /app/node_modules ./node_modules
CMD ["dist/server.js"]
```

Числа драматичны. Single-stage Node-сборка обычно весит около 380MB+ (полная база `node:22` — ~1.1GB до твоего приложения); multi-stage версия на slim- или distroless-базе падает примерно до 60MB. Для Go-бинарника контраст резче — single-stage сборка около 180MB схлопывается до образа ~3–12MB на `scratch` или `distroless/static`, потому что скомпилированному бинарнику вообще не нужен runtime. Один широко цитируемый пример ужал образ с 843MB до 12.1MB — сокращение на 98.6%.

## Distroless vs alpine: трейдофф runtime-базы

Когда у тебя есть тонкий runtime-стейдж, выбор базы — реальное решение. Alpine крошечный, потому что использует musl libc и BusyBox, и поставляет пакетный менеджер — удобно, когда надо добавить инструмент. Подвох: musl отличается от glibc достаточно тонко, чтобы ломать нативные модули и иногда удивлять DNS или производительностью. Distroless поставляет только твоё приложение и его прямые runtime-зависимости — ни шелла, ни пакетного менеджера, ни `apt` — что отлично для поверхности атаки, но означает, что ты не можешь `docker exec` шелл внутрь для отладки, и обязан занести каждую runtime-зависимость через `COPY`.

<TradeoffMatrix
  id="01-image-layers-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Скомпилированный сервис должен отгружаться как маленький, защищённый прод-образ. Выбери базу финального стейджа."
  options={[
    { name: "Distroless (gcr.io/distroless/*): приложение + runtime-зависимости, без шелла, без пакетного менеджера", summary: "Минимальная поверхность атаки.", correct: true, justification: "Подходит для защищённого прод-runtime: отсутствие шелла и пакетного менеджера — это куда меньшая поверхность атаки и образ, а CVE-сканеры почти ничего не находят, потому что почти ничего не установлено. Цена, которую ты принимаешь, — отлаживаемость: нет шелла, чтобы docker exec, — поэтому опираешься на логи, эфемерные debug-контейнеры и занос каждой runtime-зависимости через COPY --from. Для скомпилированного или самодостаточного приложения это стандартный выбор сеньора." },
    { name: "Alpine: musl libc + BusyBox, с доступным apk", summary: "Крошечный, но musl.", justification: "Очень маленький и удобный, потому что apk позволяет добавлять инструменты, — отлично для утилитных образов. Но musl libc отличается от glibc достаточно, чтобы ломать нативные аддоны и иногда удивлять DNS/производительностью, а поставка пакетного менеджера и шелла расширяет поверхность атаки по сравнению с distroless. Разумный выбор, когда тебе реально нужен шелл или установка runtime-пакетов, но не самый защищённый вариант." },
    { name: "Полная builder-база (node:22, golang), переиспользованная как runtime", summary: "Без второго стейджа.", justification: "Это и есть антипаттерн, ради которого существуют multi-stage сборки. Ты отгружаешь в прод компиляторы, dev-зависимости и кэши apt — сотни MB раздувания и поверхности атаки, которые никогда не обслуживают запрос. Используй её только как builder-стейдж, потом COPY артефакты в тонкий финальный стейдж." },
    { name: "ubuntu:22.04 с скопированным приложением и инструментами сборки, удалёнными потом через apt-get remove", summary: "Почистим позже.", justification: "Удаление инструментов в более позднем слое не уменьшает образ — байты по-прежнему живут в раннем слое, образ остаётся большим, и всё чувствительное остаётся в истории. Чистка постфактум — та же ошибка, что удалить секрет в более позднем RUN. Используй свежий тонкий финальный стейдж." },
  ]}
/>

## .dockerignore и секрет, который никогда не умирает

Картину завершают два провала. Первый — **build-контекст**: когда ты запускаешь `docker build .`, вся директория упаковывается в tar и шлётся демону. Без `.dockerignore` туда попадают `.git`, `node_modules`, вывод сборки и `.env`-файлы — замедляя загрузку и рискуя тем, что `COPY . .` сгребёт секреты и мусор в слой. `.dockerignore`, перечисляющий `node_modules`, `.git`, `*.log` и `.env`, держит контекст лёгким, а копирование — чистым.

Второй — ловушка, которая бьёт команды сильнее всего: **секрет, добавленный в одном слое и удалённый в более позднем, всё равно живёт в истории образа.** Слои — это неизменяемые diff'ы. Если ты `COPY id_rsa` (или впишешь токен в файл), а потом `RUN rm id_rsa` через две строки, удаление — это просто новый diff сверху; исходный файл всё ещё восстановим из раннего слоя через `docker history` или извлечением образа. Удаление — театр. Правильные инструменты — BuildKit secret mounts: `RUN --mount=type=secret,id=token ...` делает секрет доступным для одной инструкции и пишет его в *ноль* слоёв, — или, если иначе никак, multi-stage сборка, где секрет живёт только в отбрасываемом builder-стейдже. Никогда не делай `rm` секрета в надежде, что он исчез.

<Quiz
  id="01-image-layers-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Ты правишь один файл исходника и пересобираешь. npm ci бежит каждый раз. Самая вероятная причина?"
  choices={[
    { label: "COPY . . идёт перед RUN npm ci, поэтому любая правка файла меняет чек-сумму COPY и инвалидирует слой установки и всё после него", correct: true },
    { label: "Базовый образ слишком большой", misconception: "Размер базового образа влияет на время pull/push и диск, а не на то, попадает ли слой установки в кэш. Промах кэша идёт от порядка инструкций, а не от размера базы." },
    { label: "npm ci недетерминирован и его нельзя кэшировать", misconception: "npm ci детерминирован; слой кэшируется нормально. Проблема в том, что бьётся более ранний слой (COPY . .), что заставляет каждый слой ниже пересобираться." },
    { label: "Нужно передать --no-cache", misconception: "--no-cache полностью отключает кэширование — противоположность тому, что нужно. Фикс — копировать lock-файл и ставить зависимости до копирования исходников." },
  ]}
/>

<Quiz
  id="01-image-layers-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Токен был COPY-нут в слой, потом удалён через RUN rm в следующей инструкции. Это безопасно?"
  choices={[
    { label: "Нет — токен всё ещё есть в diff'е более раннего слоя и восстановим из истории образа; используй BuildKit secret mount или отбрасываемый builder-стейдж", correct: true },
    { label: "Да, rm в следующем слое удаляет его из образа", misconception: "Слои — это стек неизменяемых diff'ов. rm добавляет новый diff, который прячет файл в слитом виде, но исходные байты остаются в раннем слое и восстановимы через docker history." },
    { label: "Да, пока ты не пушишь образ", misconception: "Даже без пуша любой с локальным образом может его извлечь. И смысл сборки обычно в том, чтобы его отгрузить — секрет едет вместе с ним." },
    { label: "Да, если rm в той же RUN-команде", misconception: "Чистка в той же RUN помогает с файлами, записанными и удалёнными внутри одной инструкции, но токен, COPY-нутый в отдельной инструкции, уже сам по себе слой. Используй --mount=type=secret, который не пишет ни в один слой." },
  ]}
/>

<DragOrder
  id="01-image-layers-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь шаги Node-Dockerfile для максимального переиспользования кэша (от редко- к часто-меняющимся):"
  items={[
    { id: "a", label: "FROM node:22 AS builder — пинни базовый образ (меняется редко)" },
    { id: "b", label: "RUN apt-get update && apt-get install -y <os-deps> — OS-пакеты (максимум раз в месяц)" },
    { id: "c", label: "COPY package.json package-lock.json ./ — манифест зависимостей (меняется при смене зависимостей)" },
    { id: "d", label: "RUN npm ci — установка зависимостей (в кэше, пока не сменится манифест)" },
    { id: "e", label: "COPY . . затем RUN npm run build — исходный код (меняется каждый коммит)" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<StackDiagram
  label="Слои образа контейнера, сложенные от базовой ОС внизу до кода приложения наверху, упорядоченные от редко- к часто-меняющимся"
  hue="var(--d-systems)"
  layers={[
    { label: "Код приложения", note: "COPY . . — меняется каждый коммит" },
    { label: "Зависимости", note: "RUN npm ci — в кэше, пока не сменится lock-файл" },
    { label: "Lock-файл", note: "COPY package*.json" },
    { label: "OS-пакеты", note: "RUN apt-get … — максимум раз в месяц" },
    { label: "Базовый образ", note: "FROM node:22 — запинён, меняется редко" },
  ]}
  caption="Кэш — совпадение по префиксу: первый слой, чей ключ меняется, пересобирается со всеми слоями выше. Ставь редко-меняющиеся шаги внизу, чтобы повседневная правка кода держала установку в кэше."
/>

<KeyTakeaway>Кэш — это префикс: первый изменённый слой пересобирается со всеми после. Копируй lock-файлы и ставь зависимости до исходников, multi-stage для тонкого образа, .dockerignore мусор, и не делай `rm` секрета — он в истории.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-image-layers-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, почему COPY . . перед RUN npm ci убивает время сборки, и точный реордер, который это чинит.", a: "Кэш сборки ключует каждый слой по инструкции плюс содержимому, от которого она зависит, и это строгое совпадение по префиксу: первый слой, чей ключ меняется, пересобирается, и каждый слой после него тоже. COPY . . берёт чек-сумму каждого файла в контексте, поэтому правка любого исходника — даже однострочный твик CSS — меняет эту чек-сумму и инвалидирует слой COPY. Поскольку RUN npm ci идёт после, слой установки находится ниже промаха и тоже пересобирается, таща все зависимости по сети на каждый пуш. Фикс — разбить копирование по частоте изменений: сначала COPY package.json package-lock.json ./, потом RUN npm ci, потом COPY . . для исходников. Теперь входы слоя установки меняются, только когда меняется lock-файл, поэтому он остаётся попаданием в кэш на подавляющем большинстве коммитов, а заново бегут лишь финальное копирование исходников и сборка. Команды обычно видят падение времени CI-сборки примерно на 70% от одного этого реордера." },
    { q: "Почему удаление секрета в более позднем слое не убирает его, и что делать вместо этого?", a: "Docker-образ — это стек неизменяемых, append-only слоёв, каждый из которых — diff файловой системы. Когда ты COPY-нул ключ в образ, это слой, содержащий файл. Более поздний RUN rm этого файла — просто ещё один diff сверху, который помечает его удалённым в слитом виде; он не редактирует и не стирает ранний слой, который физически по-прежнему содержит байты. Любой может запустить docker history или распаковать слои образа и восстановить исходный файл, запушенный или нет. Правильные подходы все держат секрет вне любого сохранённого слоя: используй BuildKit secret mount, RUN --mount=type=secret,id=token, который открывает секрет только для одной инструкции и не пишет ни в один слой; или multi-stage сборку, где секрет присутствует только в builder-стейдже, который отбрасывается и не публикуется; или, в крайнем случае, squash. Принцип в том, что секрет вообще не должен становиться слоем — удалить его обратно нельзя." },
  ]}
/>

<Recap lang="ru">
Образ — это стек read-only слоёв над union-файловой системой, и каждая инструкция Dockerfile порождает один слой, ключёванный по тексту инструкции плюс содержимому, которого она касается. Кэш — это совпадение по префиксу, поэтому первая инструкция, чей ключ меняется, пересобирает себя и всё ниже — что делает порядок инструкций самым большим рычагом, который у тебя есть. Ставь редко-меняющиеся шаги первыми: пинни базу, ставь OS-пакеты, копируй lock-файл и ставь зависимости, и только потом копируй исходники и собирай, чтобы повседневная правка кода держала дорогую установку как попадание в кэш. Используй multi-stage сборки, чтобы компилировать в жирном builder'е и через COPY переносить только артефакты в тонкий distroless- или alpine-runtime, роняя образы с сотен MB до десятков. Держи `.git`, `node_modules` и `.env` вне build-контекста через `.dockerignore`. И помни, что секрет, добавленный и потом `rm`-нутый, всё ещё живёт в diff'е раннего слоя — тянись к BuildKit secret mounts или отбрасываемому builder-стейджу, потому что в неизменяемых слоях удалить его обратно нельзя. Теперь, когда ты откроешь Dockerfile на ревью, первый вопрос — совпадает ли порядок слоёв с частотой изменений, и не касается ли что-то секретное инструкции COPY?
</Recap>
