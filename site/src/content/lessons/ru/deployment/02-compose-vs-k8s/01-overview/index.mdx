---
concepts:
- container-orchestration
- single-host-vs-multi-node
- reconciliation-loop
- self-healing
- rolling-update
- complexity-tax
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://docs.docker.com/compose/intro/features-uses/
- https://kubernetes.io/docs/concepts/overview/
- https://encore.cloud/resources/do-you-need-kubernetes
- https://thenewstack.io/kubernetes-vs-docker-compose/
spiral: []
status: ready
summary: 'Compose оркестрирует контейнеры на одном хосте крошечным YAML; Kubernetes
  — распределённый самовосстанавливающийся control plane: планирует по нодам и катит
  релизы с проверкой здоровья за реальную операционную цену. Ошибка — купить эту цену
  до масштаба, который её оправдывает.'
title: 'Compose против Kubernetes: выбор правильного веса оркестрации'
track: deployment
unit: 02-compose-vs-k8s
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
Команда из трёх человек выкатывает API и в первый же день берёт managed Kubernetes-кластер — «чтобы быть готовыми масштабироваться». Через полгода у них одна нода, одиннадцать сервисов, в которых никто не разбирается, и директория с YAML больше самого приложения. Кривой drain ноды роняет весь продукт на сорок минут, потому что никто не понял pod disruption budgets. Проблемы масштаба у них не было — они импортировали будущую и платили налог дежурства без трафика, который бы это оправдывал.
</Hook>

<Crux>Compose гоняет контейнеры на одном хосте; Kubernetes — самовосстанавливающийся распределённый control plane. Покупай вес по масштабу.</Crux>

<Explanation>

## Два инструмента, две задачи

Docker Compose и Kubernetes оба «запускают контейнеры», и этот общий глагол прячет, насколько они разные. Compose — менеджер процессов на одном хосте: один `docker-compose.yml` объявляет сервисы, сети и тома, а `docker compose up` запускает их все на *этой машине*. Его быстро освоить, быстро запустить и тривиально дебажить — весь стек это один файл и `docker compose logs`. Накладные расходы крошечные, порядка ~50MB, потому что нет кластера, который надо держать.

Kubernetes — зверь другого вида: распределённый control plane, работающий *поверх* машин. Ты не запускаешь контейнеры; ты объявляешь *желаемое состояние* (3 реплики этого образа, за этим сервисом, с этими проверками здоровья), а control plane непрерывно работает, чтобы реальность совпала с ним. Это непрерывное совпадение и есть весь продукт, и оно стоит тебе настоящего control plane — примерно ~2GB резидентных накладных расходов до того, как твоё приложение поднимет хоть один контейнер.

## Цикл согласования — это то, за что ты платишь

Единственный механизм, отделяющий Kubernetes от Compose, — это **цикл согласования** (reconciliation loop). Контроллеры наблюдают за кластером, сравнивают фактическое состояние с желаемым, хранящимся в `etcd`, и закрывают разрыв — непрерывно, по событиям, вечно. Единственная задача контроллера `ReplicaSet` — «держать N подов запущенными»: убей под, и он это заметит и заменит; потеряй целую ноду, и планировщик перепланирует её поды на здоровые ноды. Это **самовосстановление** (self-healing), и у Compose его попросту нет. `restart: always` у Compose перезапускает *контейнер на том же хосте*; если хост умирает, всё на нём умирает вместе с ним.

Тот же цикл питает **rolling updates с проверкой здоровья**. `Deployment` в Kubernetes поднимает новые поды, ждёт, пока пройдёт их readiness-проба, переводит трафик на них и только потом гасит старые — так кривой релиз никогда не обслуживает трафик, и ты получаешь zero-downtime выкатку и автоматический откат бесплатно. Compose перезапускает сервис на месте; в окне между остановкой и здоровым стартом этот сервис лежит. Нет ни проверки здоровья, ни автоматического отката.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Возможность</th>
        <th class="border border-border px-3 py-2 text-left">Docker Compose</th>
        <th class="border border-border px-3 py-2 text-left">Kubernetes</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Область планирования</td>
        <td class="border border-border px-3 py-2">Только один хост</td>
        <td class="border border-border px-3 py-2">Много нод, поды размещает планировщик</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Отказ ноды</td>
        <td class="border border-border px-3 py-2">Всё на этом хосте умирает</td>
        <td class="border border-border px-3 py-2">Самовосстановление — поды переезжают</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Деплои</td>
        <td class="border border-border px-3 py-2">Перезапуск на месте; короткий простой</td>
        <td class="border border-border px-3 py-2">Rolling update с проверкой readiness</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Автомасштабирование</td>
        <td class="border border-border px-3 py-2">Нет (вручную, только вертикально)</td>
        <td class="border border-border px-3 py-2">Горизонтальное по метрикам</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Накладные расходы</td>
        <td class="border border-border px-3 py-2">~50MB, без control plane</td>
        <td class="border border-border px-3 py-2">~2GB control plane до приложения</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Кривая обучения</td>
        <td class="border border-border px-3 py-2">Минуты; один YAML-файл</td>
        <td class="border border-border px-3 py-2">Недели; поды, сервисы, CNI, RBAC, YAML</td>
      </tr>
    </tbody>
  </table>
</div>

## Налог на сложность и кто его реально платит

Всё, что Kubernetes даёт, приходит в комплекте с налогом: control plane, который надо держать живым, сеть кластера (CNI), которую надо понять, RBAC, ingress, управление секретами и знаменитый разрастающийся YAML. Опросы стабильно ставят операционную сложность на первое место среди болей Kubernetes — около 70% пользователей называют её, — а кластеры рутинно работают на 30–50% утилизации, так что большая доля выделенного — это просто потери. Для маленькой команды цифра в деньгах жестокая: правдоподобный продакшен-сетап выходит в нижние пятизначные суммы в месяц, и реальная цена — это человеко-часы инженеров, уходящие на поддержание кластера вместо выпуска фич.

Поэтому трейдофф сеньора — не «что лучше», а **возможности против налога на сложность, взвешенные относительно твоего реального масштаба**. Возможности Kubernetes окупаются, только когда тебе действительно нужны планирование по нескольким нодам, zero-downtime выкатка под настоящим трафиком или автомасштабирование. Ниже этого ты платишь за самовосстановление поверх машин, которых у тебя нет, за планировщик, размещающий поды на единственной ноде. Каноничный провал — команда из трёх человек из Hook — это покупка налога до того, как появился масштаб, чтобы его оправдать.

<Inset kind="why" lang="ru">
«Нам это понадобится для масштаба когда-нибудь» — фраза, которая покупает Kubernetes слишком рано. Честный контраргумент: один мощный хост с Compose может обслужить удивительно много трафика, а миграция *на* Kubernetes позже — известный, ограниченный проект. Импортировать его сложность сейчас — это постоянный налог с неопределённой датой окупаемости. Начинай с веса, который заработал твой трафик; апгрейдись, когда виден реальный потолок, а не гипотетический.
</Inset>

## Где реально проходит граница — и что посередине

Есть честные сигналы, что ты перерос Compose. Первый — **потолок одного хоста**: Compose масштабируется вертикально (коробка побольше) или репликами на том же хосте, а у одной машины есть жёсткий предел — когда нужно размазать нагрузку по *машинам*, Compose не может. Второй — **доступность**: когда падение единственного хоста больше неприемлемо, нужно планирование, переживающее потерю ноды. Третий — **операционные нужды, которых у Compose нет**: zero-downtime rolling-деплои с проверкой здоровья и горизонтальное автомасштабирование под нагрузкой. Попади в одну из них по-настоящему — и налог начинает себя окупать.

Важно: это не бинарный выбор. Между «один хост с Compose» и «самоуправляемый Kubernetes» лежат варианты, дающие многонодовую устойчивость без полного операционного веса: **Docker Swarm** (многохостовый, синтаксис в духе Compose, куда проще k8s), **HashiCorp Nomad** и **managed PaaS / serverless-контейнеры** вроде Cloud Run, AWS App Runner, ECS или Azure Container Apps, которые гоняют твои контейнеры и берут масштабирование на себя без владения control plane. Managed Kubernetes (EKS/GKE/AKS) убирает *эксплуатацию* control plane, но не концептуальную сложность — ты по-прежнему владеешь подами, сетью и YAML.

<TradeoffMatrix
  id="02-compose-vs-k8s-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Стартап из 4 человек гоняет API + Postgres + Redis + воркер, всё на одном арендованном сервере, скромный трафик. Выбери вес оркестрации."
  options={[
    { name: "Docker Compose на одном хосте, план мигрировать, когда появится реальный потолок", summary: "Подгони под масштаб.", correct: true, justification: "Это ровно та нагрузка, под которую сделан Compose: горстка сервисов на одном хосте, описанных в одном YAML, дебажится через docker compose logs, ~50MB накладных. Команда — 4 человека со скромным трафиком, многонодовой потребности и автомасштабирования сегодня нет. Миграция на более тяжёлый оркестратор позже — известный, ограниченный проект; импортировать его сложность сейчас — постоянный налог без масштаба, который бы его оправдал." },
    { name: "Самоуправляемый Kubernetes-кластер сейчас, чтобы «быть готовыми масштабироваться»", summary: "На будущее.", justification: "Это каноничная ошибка «слишком рано». Команда из 4 человек владела бы control plane, CNI, RBAC, ingress и разрастающимся YAML — операционную сложность опросы ставят #1 болью k8s (~70% пользователей) — чтобы самовосстанавливаться поверх машин, которых нет. Реальные пятизначные расходы в месяц и потерянное время на фичи ради возможностей, которые трафик не задействует. Мигрируй, когда виден реальный потолок, а не до того." },
    { name: "Managed PaaS / serverless-контейнеры (Cloud Run, App Runner, Container Apps)", summary: "Середина.", justification: "Разумная альтернатива, берущая масштабирование и zero-downtime деплои без владения control plane. Часто это больше, чем нужно крошечной нагрузке, и добавляет вендор-привязку и стоимость, но легитимный выбор, если команда хочет управляемую выкатку/масштабирование без изучения k8s. Не неверно — просто тяжелее Compose ровно для этого условия." },
    { name: "Без оркестратора: docker run каждый контейнер руками на коробке", summary: "Голый Docker.", justification: "Технически работает, но ты теряешь единый декларативный файл Compose, именованные сети, порядок зависимостей и one-command up/down — без всякой выгоды. Compose — это пол для многоконтейнерных приложений; падение ниже него — это просто ручная возня, расходящаяся с любым источником истины." },
  ]}
/>

<Quiz
  id="02-compose-vs-k8s-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Какая возможность — настоящая причина переходить с Compose на Kubernetes — то, чего Compose принципиально не может?"
  choices={[
    { label: "Самовосстановление и планирование по нескольким нодам, чтобы потеря хоста не роняла приложение", correct: true },
    { label: "Описание сервисов, сетей и томов в YAML-файле", misconception: "Compose делает это одним docker-compose.yml — это базовая возможность, общая для обоих инструментов, а не причина переходить." },
    { label: "Запуск контейнеров из образов", misconception: "Оба запускают контейнеры; этот общий глагол прячет реальную разницу — распределённое планирование и согласование." },
    { label: "Чтение логов одной командой", misconception: "docker compose logs у Compose тут как раз проще; не наблюдаемость заставляет переходить на k8s." },
  ]}
/>

<Quiz
  id="02-compose-vs-k8s-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Что цикл согласования Kubernetes даёт такого, чего не даёт restart: always у Compose?"
  choices={[
    { label: "Он восстанавливает желаемое состояние по всему кластеру — перепланирует поды на здоровые ноды, когда умирает целый хост", correct: true },
    { label: "Он быстрее перезапускает упавший контейнер на том же хосте", misconception: "Дело не в скорости. restart: always справляется с перезапуском на том же хосте; ценность цикла — пережить потерю ноды, чего Compose не может." },
    { label: "Он делает YAML короче", misconception: "YAML в Kubernetes знаменито больше, а не короче — это разрастание и есть часть налога на сложность." },
    { label: "Он убирает необходимость в проверках здоровья", misconception: "Согласование опирается на readiness/liveness-пробы, чтобы знать, что значит «здоров»; оно добавляет проверку здоровья, а не убирает." },
  ]}
/>

<DragOrder
  id="02-compose-vs-k8s-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь вопросы, которые сеньор задаёт, прежде чем тянуться к Kubernetes:"
  items={[
    { id: "a", label: "Влезает ли нагрузка на один хост с запасом на рост? Если да — Compose достаточно" },
    { id: "b", label: "Падение единственного хоста неприемлемо, или нужно размазать нагрузку по машинам?" },
    { id: "c", label: "Нужны ли zero-downtime rolling-деплои с проверкой здоровья или горизонтальное автомасштабирование?" },
    { id: "d", label: "Если да — может ли середина (Swarm, Nomad, managed PaaS) закрыть нужду с меньшим налогом?" },
    { id: "e", label: "Только если масштаб и ops-нужды реальны и середина не подходит → Kubernetes" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Compose = один хост, нет self-healing поверх машин. Kubernetes = распределённое согласование, self-healing, выкатки с проверкой здоровья, автомасштабирование — за реальный налог на сложность. Покупай вес по масштабу.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-compose-vs-k8s-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни коллеге, почему команде из 3 человек брать самоуправляемый Kubernetes в первый же день — обычно ошибка, и что делать вместо этого.", a: "Ценность Kubernetes — распределённое согласование: самовосстановление по нодам, планирование по машинам, rolling updates с проверкой здоровья и автомасштабирование. Команда из 3 человек с одной нодой и скромным трафиком не задействует ничего из этого — они самовосстанавливаются поверх машин, которых нет, и планируют на единственную ноду. При этом они платят полный налог на сложность: control plane, который надо держать живым, сеть кластера (CNI), RBAC, ingress, секреты и разрастающийся YAML. Операционная сложность — боль k8s #1 в опросе за опросом (~70% пользователей), реальные продакшен-сетапы стоят нижние пятизначные суммы в месяц, а кластеры рутинно работают на 30–50% утилизации, так что многое — потери. Бóльшая цена — человеко-часы на нянченье кластера вместо выпуска фич. Лучший ход — подогнать вес под масштаб: гонять Compose (или managed PaaS) на одном хосте сейчас и мигрировать на более тяжёлый оркестратор позже — это известный, ограниченный проект — когда реальный потолок действительно виден, а не гипотетический." },
    { q: "Какие конкретные сигналы говорят, что ты по-настоящему перерос Docker Compose?", a: "Три честных сигнала. Первый — потолок одного хоста: Compose масштабируется вертикально (коробка побольше) или репликами на том же хосте, но у одной машины жёсткий предел — когда нужно размазать нагрузку по машинам, Compose не может, потому что у него нет многонодового планирования. Второй — доступность: когда падение одного хоста больше неприемлемо, нужно планирование, переживающее потерю ноды, чего restart: always у Compose не даёт (он перезапускает контейнер на том же хосте; если хост умирает, всё на нём умирает). Третий — операционные требования, которых у Compose нет: zero-downtime rolling-деплои с проверкой readiness и горизонтальное автомасштабирование по нагрузке. Попади в одно из этого по-настоящему — и налог на оркестрацию начинает себя окупать. До этого — просто стоимость. И помни, это не бинарно — Docker Swarm, Nomad или managed PaaS/serverless-платформа дают многонодовую устойчивость задолго до того, как оправдан полный самоуправляемый Kubernetes." },
  ]}
/>

<Recap lang="ru">
Compose и Kubernetes оба запускают контейнеры, но отвечают на разные вопросы. Compose — менеджер процессов на одном хосте: один YAML, минуты на освоение, ~50MB накладных, идеален для локальной разработки и небольших однонодовых деплоев, но без многонодового планирования, без самовосстановления поверх машин, без выкаток с проверкой здоровья и без автомасштабирования. Kubernetes — распределённый control plane, чей цикл согласования непрерывно гонит реальность к объявленному желаемому состоянию, давая самовосстановление, планирование по нодам, zero-downtime rolling updates и горизонтальное автомасштабирование — ценой реального налога на сложность: control plane, CNI, RBAC, разрастающийся YAML, дежурства и пятизначные счета в месяц. Решение сеньора — возможности против сложности, взвешенные относительно реального масштаба. Каноничный провал — купить налог слишком рано; обратный провал — слишком долго игнорировать потолок одного хоста у Compose. Между ними лежат Swarm, Nomad и managed PaaS. Выбирай вес, который заработал твой трафик, и мигрируй, когда виден реальный потолок, а не гипотетический.
</Recap>
