---
concepts:
- orchestration-scope
- reconciliation-loop
- self-healing
- rolling-update
- complexity-tax
- middle-ground
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://docs.docker.com/compose/intro/features-uses/
- https://kubernetes.io/docs/concepts/overview/
- https://encore.cloud/resources/do-you-need-kubernetes
status: ready
summary: "Тест с выбором на синтез по всему юниту Compose vs Kubernetes: scope оркестрации, reconciliation, self-healing, gated-выкатки и налог на сложность относительно реального масштаба."
title: 'Compose vs Kubernetes: тест с выбором ответа'
track: deployment
unit: 02-compose-vs-k8s
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает реальное решение об оркестрации — не определение для заучивания, а компромисс, который надо взвесить относительно масштаба, который у тебя реально есть.
</Hook>

<Goal lang="ru">
Убедись, что связываешь scope оркестрации, reconciliation loop, self-healing, gated-выкатки и налог на сложность — и можешь решить, какой вес заслуживает конкретная нагрузка.
</Goal>

<Quiz
  id="02-compose-vs-k8s-choice-1"
  lessonSlug="02-compose-vs-k8s"
  lang="ru"
  question="Команда держит API, Postgres, Redis и worker на одной арендованной машине при скромном трафике. Нужна устойчивость к падению контейнера и один декларативный файл. Какой вес оркестрации подходит и почему?"
  choices={[
    { label: "Docker Compose на одном хосте. Один docker-compose.yml декларирует все четыре сервиса, сети и тома; restart: always покрывает упавшие контейнеры; ~50МБ overhead, отлаживается через docker compose logs. Нагрузка не задействует multi-node — поэтому более тяжёлый оркестратор это чистый налог.", correct: true },
    { label: "Self-managed Kubernetes сразу, чтобы быть готовыми к масштабированию.", misconception: "Это классическая ошибка «слишком рано». Маленькая команда будет владеть control plane, CNI, RBAC, ingress и YAML-разрастанием, чтобы self-heal по машинам, которых у неё нет. Мигрируй, когда реальный потолок виден — а не гипотетический." },
    { label: "Без оркестратора — docker run каждого контейнера руками.", misconception: "Так теряются единый декларативный файл Compose, именованные сети, порядок зависимостей и one-command up/down — ради нуля выгоды. Compose это пол для multi-container приложений; опускаться ниже — это ручной дрейф." },
    { label: "Управляемый Kubernetes (EKS/GKE/AKS), ведь он снимает операционную нагрузку.", misconception: "Managed k8s снимает операции с control plane, но не концептуальную сложность — ты всё равно владеешь pods, сетью, RBAC и YAML. Это намного тяжелее, чем нужно этой однохостовой нагрузке." },
  ]}
/>

<Quiz
  id="02-compose-vs-k8s-choice-2"
  lessonSlug="02-compose-vs-k8s"
  lang="ru"
  question="Что reconciliation loop в Kubernetes даёт такого, чего restart: always в Compose принципиально не может?"
  choices={[
    { label: "Он непрерывно приводит фактическое состояние к декларированному desired state по всему кластеру — когда умирает целый узел, контроллеры переносят его pods на здоровые узлы. Это self-healing по машинам; restart: always лишь перезапускает контейнер на том же хосте.", correct: true },
    { label: "Он быстрее перезапускает упавший контейнер на том же хосте.", misconception: "Дело не в скорости. restart: always и так делает перезапуск на том же хосте; ценность loop в том, чтобы пережить потерю узла, чего Compose не умеет — у него нет multi-node scheduling." },
    { label: "Он делает YAML короче и проще.", misconception: "YAML в Kubernetes известно больше, а не меньше — pods, services, deployments, ingress. Это разрастание — часть налога на сложность за reconciliation, а не выгода." },
    { label: "Он убирает необходимость в health-проверках.", misconception: "Reconciliation опирается на readiness- и liveness-probes, чтобы понимать, что значит «здоровый» — он добавляет health gating, а не убирает его." },
  ]}
/>

<Quiz
  id="02-compose-vs-k8s-choice-3"
  lessonSlug="02-compose-vs-k8s"
  lang="ru"
  question="Сервису нужно выкатить новый релиз с нулевым downtime и автоматическим откатом, если новая версия нездорова. Чем тут отличаются Compose и Kubernetes Deployment?"
  choices={[
    { label: "Deployment поднимает новые pods, ждёт прохождения их readiness probe, переключает трафик и только потом гасит старые — поэтому плохой релиз никогда не отдаёт трафик, а откат автоматический. Compose перезапускает сервис на месте; в окне между остановкой и здоровым стартом он просто лежит, без health-гейта.", correct: true },
    { label: "Оба гейтят выкатку на readiness probe; разница лишь в скорости.", misconception: "У Compose вообще нет gated-выкатки по readiness. Он перезапускает на месте с окном downtime — health-гейт и автоматический откат это именно то, что даёт только reconciliation loop." },
    { label: "Compose делает zero-downtime выкатки нативно; Kubernetes нужен service mesh для этого.", misconception: "Всё наоборот. Rolling updates с gating по readiness встроены в Deployment; Compose перезапускает на месте. Никакой mesh для базовых rolling updates не нужен." },
    { label: "Ни один не умеет откатываться автоматически; обоим нужен ручной redeploy.", misconception: "Kubernetes Deployment хранит историю выкаток и может откатиться автоматически, когда новые pods не проходят readiness. У Compose такого механизма нет." },
  ]}
/>

<Quiz
  id="02-compose-vs-k8s-choice-4"
  lessonSlug="02-compose-vs-k8s"
  lang="ru"
  question="Какие честные сигналы говорят, что нагрузка реально переросла Docker Compose?"
  choices={[
    { label: "Потолок одного хоста (надо размазать нагрузку по машинам, а не просто взять коробку побольше), доступность (потеря хоста больше неприемлема) и операционные нужды, которых нет у Compose (health-gated zero-downtime выкатка, horizontal autoscaling). Попади в одно по-настоящему — и налог на оркестрацию начинает окупаться.", correct: true },
    { label: "docker-compose.yml вырос больше десятка сервисов.", misconception: "Размер файла не сигнал. Compose нормально держит много сервисов на одном хосте; к переходу вынуждает потребность в возможностях, которых у Compose нет — multi-node scheduling, self-healing по машинам — а не число строк." },
    { label: "Хочется использовать образы контейнеров вместо голых процессов.", misconception: "И Compose, и Kubernetes запускают образы контейнеров; этот общий глагол скрывает настоящую разницу. Использование образов — не повод менять оркестратор." },
    { label: "Команде нужны дашборды и метрики покрасивее.", misconception: "Инструменты наблюдаемости ортогональны весу оркестрации — дашборды можно поставить и на один Compose-хост. Переход на k8s диктуют нужды scheduling и доступности, а не дашборды." },
  ]}
/>

<Quiz
  id="02-compose-vs-k8s-choice-5"
  lessonSlug="02-compose-vs-k8s"
  lang="ru"
  question="Опросы стабильно ставят операционную сложность на первое место среди болей Kubernetes (~70% пользователей), а кластеры обычно держат 30–50% утилизации. Как это читает senior?"
  choices={[
    { label: "Возможности Kubernetes идут в комплекте с постоянным налогом на сложность — control plane, CNI, RBAC, ingress, YAML-разрастание, on-call, счета в нижние пять цифр в месяц — и значительная часть выделенной ёмкости пропадает. Налог окупается только при реальной нужде в multi-node scheduling, gated-выкатке под трафиком или autoscaling.", correct: true },
    { label: "Это значит, что большинство команд настраивают Kubernetes неправильно; верная настройка убирает сложность.", misconception: "Сложность встроена в работу распределённого control plane, а не вызвана мисконфигом. Даже хорошо настроенные кластеры несут концептуальный и операционный вес; вопрос в том, оправдан ли он твоим масштабом." },
    { label: "Низкая утилизация доказывает, что Kubernetes тратит память в рантайме, как текущий процесс.", misconception: "30–50% утилизации — это про выделенную против используемой ёмкости кластера и запас, а не про утечку памяти в рантайме. Это отражает over-provisioning относительно реальной нагрузки." },
    { label: "Эти числа неважны, как только используешь managed-сервис.", misconception: "Managed k8s снимает операции с control plane, но не концептуальную сложность и не экономику утилизации — ты всё равно владеешь pods, сетью и right-sizing кластера." },
  ]}
/>

<Quiz
  id="02-compose-vs-k8s-choice-6"
  lessonSlug="02-compose-vs-k8s"
  lang="ru"
  question="Команде нужна multi-node устойчивость и zero-downtime выкатки, но она не хочет владеть control plane или учить весь Kubernetes. Как senior формулирует их варианты?"
  choices={[
    { label: "Это не бинарный выбор. Между одним Compose-хостом и self-managed Kubernetes лежат Docker Swarm, HashiCorp Nomad и managed PaaS / serverless-контейнеры (Cloud Run, App Runner, ECS, Container Apps), которые берут на себя scaling и выкатку без владения control plane. Бери самый лёгкий вариант, закрывающий потребность.", correct: true },
    { label: "Их единственный реальный выбор — Compose или полный self-managed Kubernetes.", misconception: "Эта ложная бинарность как раз и гонит к преждевременному k8s. Swarm, Nomad и managed PaaS дают multi-node устойчивость и gated-выкатку задолго до того, как оправдан self-managed Kubernetes." },
    { label: "Managed Kubernetes всегда самый лёгкий middle ground.", misconception: "Managed k8s снимает операции с control plane, но сохраняет всю концептуальную поверхность — pods, сеть, RBAC, YAML. Managed PaaS или Swarm часто легче для команд, которым нужны лишь scaling и выкатка." },
    { label: "Остаться на Compose и вручную поставить балансировщик перед несколькими хостами.", misconception: "Самодельный multi-host Compose за балансировщиком не даёт ни self-healing, ни переноса при потере узла, ни gated-выкатки — ты вручную собираешь хрупкий оркестратор. Специализированный middle-ground инструмент — лучший ответ." },
  ]}
/>

<Recap lang="ru">
Сквозная линия — одно решение: возможности против налога на сложность, взвешенные относительно твоего реального масштаба. Scope оркестрации (один хост против многих узлов) и reconciliation loop — вот что разделяет инструменты; self-healing, gated-выкатки и autoscaling — то, что покупает loop, ценой control plane, CNI, RBAC и YAML-разрастания. Ты уходишь с Compose при реальном потолке одного хоста, реальной нужде в доступности или реальной нужде в выкатке/autoscaling — и даже тогда ответ часто middle ground (Swarm, Nomad, managed PaaS), а не self-managed Kubernetes. Покупай вес, который заслужил твой трафик.
</Recap>
