---
concepts:
- orchestration-scope
- reconciliation-loop
- self-healing
- rolling-update
- complexity-tax
- middle-ground
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://docs.docker.com/compose/intro/features-uses/
- https://kubernetes.io/docs/concepts/overview/
- https://encore.cloud/resources/do-you-need-kubernetes
status: ready
summary: "Промпты на свободное воспроизведение по юниту Compose vs Kubernetes. Сначала ответь своими словами, потом открой модельный ответ и сравни."
title: 'Compose vs Kubernetes: тест на воспроизведение'
track: deployment
unit: 02-compose-vs-k8s
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение по памяти бьёт перечитывание. На каждый промпт скажи или запиши полный ответ из головы прежде, чем открыть модельный — именно усилие припоминания закрепляет дерево решений об оркестрации.
</Hook>

<Goal lang="ru">
Восстанови хребет юнита — scope оркестрации, reconciliation loop, self-healing, gated-выкатки, налог на сложность и middle ground — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:visible
  id="02-compose-vs-k8s-short-retrieval"
  lang="ru"
  questions={[
    { q: "И Docker Compose, и Kubernetes «запускают контейнеры». В чём фундаментальная разница в том, чем они оркестрируют?", a: "В scope оркестрации. Docker Compose — это менеджер процессов одного хоста: один docker-compose.yml декларирует сервисы, сети и тома, а docker compose up запускает их все на этой машине. Кластера нет, поэтому overhead крошечный (~50МБ). Kubernetes — это распределённый control plane, работающий поперёк машин: ты не запускаешь контейнеры, а декларируешь desired state (N реплик, за этим service, с этими probes), и control plane непрерывно приводит реальность к нему — по узлам. Общий глагол «запускают контейнеры» это скрывает: Compose управляет процессами на одном хосте; Kubernetes планирует и реконсилит нагрузки по всему флоту, из-за чего несёт около 2ГБ overhead control plane ещё до того, как твоё приложение что-либо запустит." },
    { q: "Что такое reconciliation loop и почему это единственный механизм, отделяющий Kubernetes от Compose?", a: "Reconciliation loop — это движок Kubernetes. Контроллеры непрерывно наблюдают за кластером, сравнивают фактическое состояние с desired state, хранящимся в etcd, и закрывают разрыв — событийно, бесконечно. Единственная задача контроллера ReplicaSet — «держать N pods запущенными»: убей pod, и он заменит его; потеряй целый узел, и scheduler перенесёт его pods на здоровые узлы. У Compose такого loop нет. Его restart: always перезапускает контейнер на том же хосте, но если хост умирает, всё на нём умирает вместе с ним. Это непрерывное приведение и есть весь продукт, за который ты платишь, а self-healing, gated rolling updates и autoscaling — все выражения этого loop." },
    { q: "Как работает zero-downtime rolling update с автоматическим откатом в Kubernetes и что вместо этого делает Compose?", a: "Kubernetes Deployment, управляемый reconciliation loop, поднимает новые pods, ждёт прохождения их readiness probe, переключает на них трафик и только потом гасит старые. Плохой релиз никогда не отдаёт трафик, потому что нездоровые pods не проходят гейт, а Deployment хранит историю выкаток, поэтому может откатиться автоматически. У Compose ничего этого нет: он перезапускает сервис на месте, и в окне между остановкой и здоровым стартом сервис просто лежит. Нет ни readiness-гейта, ни автоматического отката — именно health gating это та возможность, которую добавляет loop." },
    { q: "Что такое «налог на сложность» Kubernetes и какими конкретными числами он описывается?", a: "Каждая возможность Kubernetes идёт в комплекте с операционным весом: control plane, который надо держать живым, сеть кластера (CNI), которую надо понимать, RBAC, ingress, управление секретами и знаменитое YAML-разрастание. Операционная сложность стабильно боль №1 в Kubernetes — её называют около 70% пользователей. Кластеры обычно работают на 30–50% утилизации, поэтому многое из выделенного — это потери. Достоверная продакшн-установка стоит в нижние пять цифр в месяц, а бо́льшая скрытая цена — инженеро-часы на поддержание кластера здоровым вместо выпуска фич. Один только control plane добавляет около 2ГБ overhead ещё до того, как приложение запустит хоть один контейнер." },
    { q: "Какие конкретные сигналы говорят, что нагрузка реально переросла Docker Compose?", a: "Три честных сигнала. Первый — потолок одного хоста: Compose масштабируется вертикально (коробка побольше) или репликами на том же хосте, но у одной машины жёсткий предел — когда надо размазать нагрузку по машинам, Compose не может, у него нет multi-node scheduling. Второй — доступность: когда падение одного хоста больше неприемлемо, нужен scheduling, переживающий потерю узла, чего restart: always дать не может. Третий — операционные нужды, которых нет у Compose: health-gated zero-downtime выкатки и horizontal autoscaling по нагрузке. Попади в одно по-настоящему — и налог на оркестрацию начинает окупаться. До этого он просто издержка — и даже тогда это не автоматически полный Kubernetes." },
    { q: "Почему выбор между Compose и Kubernetes не бинарный и что лежит посередине?", a: "Потому что между «одним Compose-хостом» и «self-managed Kubernetes» лежит спектр, дающий multi-node устойчивость без полного операционного веса. Docker Swarm предлагает multi-host, Compose-подобный синтаксис, гораздо проще k8s. HashiCorp Nomad — более лёгкий scheduler. Managed PaaS и serverless-платформы контейнеров — Cloud Run, AWS App Runner, ECS, Azure Container Apps — запускают твои контейнеры и берут на себя scaling и выкатку без владения control plane. Управляемый Kubernetes (EKS/GKE/AKS) снимает операции с control plane, но не концептуальную сложность: ты всё равно владеешь pods, сетью, RBAC и YAML. Senior-ход — взять самый лёгкий вариант, закрывающий реальную нужду, а не прыгать сразу в self-managed k8s." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: scope оркестрации отделяет менеджер процессов одного хоста от распределённого control plane; reconciliation loop — механизм, покупающий self-healing, gated-выкатки и autoscaling; налог на сложность (control plane, CNI, RBAC, YAML-разрастание, ~70% называют его болью №1, 30–50% утилизации, пятизначные счета) — это цена; сигналы уйти с Compose — реальный потолок одного хоста, реальная нужда в доступности или реальная нужда в выкатке/autoscaling; а выбор никогда не бинарный — Swarm, Nomad и managed PaaS лежат посередине. Покупай вес, который заслужил твой масштаб.
</Recap>
