---
concepts:
- reconciliation-loop
- pod-replicaset-deployment
- service-label-selector
- readiness-vs-liveness
- configmap-secret
- resource-requests-limits
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://kubernetes.io/docs/concepts/architecture/controller/
- https://kubernetes.io/docs/concepts/workloads/controllers/deployment/
- https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/
- https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
status: ready
summary: "Синтез всего юнита K8s objects в формате выбора — reconciliation, иерархия объектов, селекторы Service, probes, ConfigMap/Secret и requests против limits."
title: 'K8s objects: тест с выбором ответа'
track: deployment
unit: 03-k8s-objects
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, проходящих через весь юнит. Каждый — это решение, которое вы принимаете при написании манифеста или разборе сломанного раската: не определение для заучивания, а сценарий отказа, который нужно разобрать в продакшен-условиях.
</Hook>

<Goal lang="ru">
Проверьте, что вы связываете воедино reconciliation loop, иерархию Pod→ReplicaSet→Deployment, селекторы Service, семантику probes, поведение ConfigMap/Secret и requests против limits — синтез, к которому вёл урок.
</Goal>

<Quiz
  id="03-k8s-objects-choice-1"
  lessonSlug="03-k8s-objects"
  lang="ru"
  question="Узел перезагружается и уносит с собой 3 из 5 подов Deployment. Никто не запускает никаких команд. Что восстанавливает количество и каков механизм?"
  choices={[
    { label: "Контроллер ReplicaSet видит actual=2 против desired=5 и создаёт 3 новых пода — reconciliation loop непрерывно закрывает разрыв между объявленным и наблюдаемым состоянием.", correct: true },
    { label: "Deployment пересоздаётся с нуля, включая новый ReplicaSet.", misconception: "Reconciliation инкрементна: контроллер создаёт только 3 недостающих пода до desired count. Он не сносит и не перестраивает Deployment или его ReplicaSet." },
    { label: "Ничего не происходит, пока кто-то снова не запустит kubectl apply.", misconception: "Это императивное мышление. Desired state (replicas: 5) уже хранится в кластере; контроллер навязывает его вечно без участия человека — в этом весь смысл декларативной оркестрации." },
    { label: "Поды возвращаются только если настроен liveness probe.", misconception: "Liveness перезапускает зависший контейнер на месте на живом узле. Замена подов, потерянных вместе с мёртвым узлом, — это reconciliation количества в ReplicaSet, что не зависит от probes." },
  ]}
/>

<Quiz
  id="03-k8s-objects-choice-2"
  lessonSlug="03-k8s-objects"
  lang="ru"
  question="Джуниор запускает kubectl run debug --image=busybox для разового пода, и тот работает днями. Почему это всё равно неверно для всего, что должно оставаться поднятым?"
  choices={[
    { label: "Голый Pod ничей — ни один контроллер не держит его как цель desired-state — поэтому при перезагрузке или drain узла его никто не пересоздаёт, и он исчезает навсегда.", correct: true },
    { label: "Голым Pod нельзя назначить IP или подключить к сети.", misconception: "Голые Pod получают обычный Pod IP и полный доступ к сети. Проблема чисто в жизненном цикле: их никто не reconcile, поэтому они не переживают потерю узла или вытеснение." },
    { label: "kubectl run по умолчанию отключён в продакшен-кластерах.", misconception: "kubectl run по умолчанию не отключён; admission policy может его ограничить, но фундаментальная проблема в том, что у получившегося Pod нет владеющего контроллера для пересоздания." },
    { label: "Голые Pod исключаются из endpoints Service независимо от меток.", misconception: "Голый Pod с подходящими метками попадает в endpoints Service как любой другой. Изъян — в долговечности, а не в выбираемости." },
  ]}
/>

<Quiz
  id="03-k8s-objects-choice-3"
  lessonSlug="03-k8s-objects"
  lang="ru"
  question="Deployment задаёт selector app=web, и Service тоже выбирает app=web. Позже создаётся второй Deployment, чьи поды тоже несут app=web. Что происходит с трафиком?"
  choices={[
    { label: "Service маршрутизирует на поды ОБОИХ Deployment — он выбирает поды по метке напрямую, без понятия о том, какой Deployment ими владеет, поэтому пересекающаяся метка молча смешивает оба ворклоада за одним набором endpoints.", correct: true },
    { label: "Service отвергает неоднозначность и не маршрутизирует ни на один Deployment.", misconception: "Селекторы никогда не ошибаются при пересечении; Kubernetes просто перечисляет каждый подходящий Pod IP в объекте Endpoints. Пересекающиеся метки — частый источник случайного смешения трафика, а не жёсткий отказ." },
    { label: "Service маршрутизирует только на первый Deployment, так как он создан раньше.", misconception: "Порядок создания не важен. Селектор вычисляется по текущим меткам подов на каждом reconcile; любой под с app=web — endpoint независимо от того, какой Deployment его породил." },
    { label: "Трафик получают только поды, явно перечисленные в спецификации Service.", misconception: "Такой Service не перечисляет поды или IP — он выбирает динамически по метке. Жёстко прописанные Pod IP ломались бы при каждом перезапуске, ради чего и существуют селекторы." },
  ]}
/>

<Quiz
  id="03-k8s-objects-choice-4"
  lessonSlug="03-k8s-objects"
  lang="ru"
  question={"Приложению нужно ~25с на прогрев (загрузка config + прогрев пула) до готовности обслуживать, и спустя часы оно иногда зависает. Команда добавляет только это:\n```yaml\nlivenessProbe:\n  httpGet: { path: /healthz, port: 8080 }\n  initialDelaySeconds: 30\n```\nРаскаты всё ещё дают всплески 500. Почему и чего не хватает?"}
  choices={[
    { label: "Liveness управляет только тем, ПЕРЕЗАПУСКАЕТСЯ ли под, а не тем, маршрутизирует ли на него Service. Без readiness probe под попадает в endpoints в момент старта процесса, и трафик летит на холодное приложение. Добавьте readiness probe (гейтит членство в endpoints) и startupProbe (покрывает медленный старт).", correct: true },
    { label: "initialDelaySeconds: 30 мало для прогрева в 25с, поэтому liveness убивает под во время загрузки.", misconception: "30с покрывают прогрев в 25с, а всплески 500 на раскате происходят всё равно. Реальный пробел в том, что liveness вообще не гейтит трафик — для удержания пода вне Service до готовности нужен readiness probe." },
    { label: "Путь httpGet неверный; он должен бить в корень приложения, а не в /healthz.", misconception: "Путь в порядке. Даже корректный liveness-путь не чинит всплески 500 на раскате, потому что liveness перезапускает поды — он не решает членство в endpoints. Это делает readiness." },
    { label: "Liveness и readiness — это одна и та же проба; хватит одной.", misconception: "Они различны: liveness перезапускает зависший под, readiness выводит ещё-не-обслуживающий под из ротации Service. Использование liveness там, где нужен readiness, — классический отказ раската." },
  ]}
/>

<Quiz
  id="03-k8s-objects-choice-5"
  lessonSlug="03-k8s-objects"
  lang="ru"
  question={"Вы обновляете ConfigMap, потребляемый как env-переменные работающим Deployment:\n```yaml\nenvFrom:\n  - configMapRef: { name: app-config }\n```\nНовое значение не доходит до подов даже спустя часы. Корневая причина и стандартное исправление?"}
  choices={[
    { label: "Изменение ConfigMap не запускает раскат; значения env инжектируются при старте пода и заморожены на его жизнь. Стандартное исправление — хешировать содержимое ConfigMap в аннотацию pod-template, чтобы любое изменение катило новый ReplicaSet и перезапускало поды.", correct: true },
    { label: "ConfigMap иммутабельны, поэтому обновление молча не применилось.", misconception: "Объект ConfigMap обновился. Проблема в распространении: env читаются один раз при старте контейнера и не обновляются, поэтому работающие поды держат старые значения до перезапуска." },
    { label: "Deployment нужен kubectl rollout restart, потому что env hot-reload только у Secret.", misconception: "Ни у ConfigMap, ни у Secret env не делают hot-reload; оба заморожены при старте. Rollout restart работает, но команды автоматизируют это, хешируя config в аннотацию, чтобы сам apply триггерил раскат." },
    { label: "envFrom устарел; чтобы получать обновления, нужно монтировать ConfigMap как том.", misconception: "envFrom полностью поддерживается. Смонтированные томом ConfigMap со временем обновляют проецируемые файлы, но env никогда не обновляются — и даже файлы не перезапускают процесс, так что приложения должны их перечитывать." },
  ]}
/>

<Quiz
  id="03-k8s-objects-choice-6"
  lessonSlug="03-k8s-objects"
  lang="ru"
  question={"Чувствительный к задержкам сервис задаёт:\n```yaml\nresources:\n  requests: { cpu: 250m, memory: 256Mi }\n  limits:   { cpu: 500m, memory: 256Mi }\n```\nПод нагрузкой видны периодические всплески задержек и редкий OOMKill. Каковы два разных механизма и какая ручка опаснее?"}
  choices={[
    { label: "CPU сверх limit ТРОТТЛИТСЯ (квота CFS тормозит процесс, давая всплески задержек); память сверх limit вызывает OOMKill (память несжимаема, поэтому ядро убивает контейнер). Limit памяти — острее, потому что он завершает под, а не замедляет его.", correct: true },
    { label: "И CPU, и memory limit троттлят процесс; OOMKill не связан с limits.", misconception: "Память несжимаема — её нельзя троттлить. Превышение memory limit вызывает OOMKill, а не замедление. Троттлится только CPU через квоту CFS." },
    { label: "Троттлинг вызывают requests, а OOMKill — limits.", misconception: "Requests — для планирования и гарантированной доли, они не троттлят. Лимит CPU задаёт потолок троттлинга; лимит памяти задаёт потолок OOM. Requests напрямую не влияют ни на один потолок." },
    { label: "OOMKill вызывает именно равенство requests и limits для памяти.", misconception: "Равные request/limit для памяти дают класс QoS Guaranteed — обычно желательный. OOMKill приходит от фактического потребления сверх limit, а не от совпадения значений; исправление — больший limit или меньшее потребление памяти." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна модель: вы объявляете desired state, и контроллеры вечно reconcile actual к нему — поэтому Deployment самовосстанавливается, а голый Pod нет. Иерархия добавляет по одной задаче на слой (Pod гоняет контейнеры, ReplicaSet держит count, Deployment катит и откатывает), Service выбирают поды по метке в набор Endpoints без понятия о владении, probes делятся на readiness (гейтит трафик) и liveness (перезапускает), изменения ConfigMap/Secret никогда не катят автоматически, а ресурсы делятся на CPU (троттлится, сжимаем) и память (OOMKill, несжимаема). Теперь, когда вы читаете манифест, первым делом ищите readiness probe, проверяете, что selector достаточно специфичен, и спрашиваете, нужно ли изменение ConfigMap форсировать раскат.
</Recap>
