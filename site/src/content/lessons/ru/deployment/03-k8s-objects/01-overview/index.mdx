---
concepts:
- reconciliation-loop
- declarative-desired-state
- pod-replicaset-deployment
- service-label-selector
- readiness-probe
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://kubernetes.io/docs/concepts/architecture/controller/
- https://kubernetes.io/docs/concepts/workloads/controllers/deployment/
- https://kubernetes.io/docs/concepts/services-networking/service/
- https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/
spiral: []
status: ready
summary: Kubernetes декларативен — ты задаёшь желаемое состояние, а контроллеры вечно
  сводят к нему фактическое. Иерархия объектов (Pod → ReplicaSet → Deployment) плюс
  Services и пробы — вот почему rollout самоисцеляется или даёт 500, когда забыл readiness-пробу.
title: 'Объекты Kubernetes: цикл сверки за каждым Pod, Service и rollout'
track: deployment
unit: 03-k8s-objects
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
Команда выкатывает рутинное обновление версии. Rollout выглядит зелёным в `kubectl get pods` — новые поды `Running`. Но дашборд дежурного загорается: на каждом деплое error rate подскакивает до 4% на девяносто секунд, потом успокаивается. И так каждый rollout. Причина — одно отсутствующее поле: у Deployment нет readiness-пробы. Как только процесс контейнера в поде стартует, Kubernetes помечает его `Ready`, и Service начинает слать на него живой трафик — пока приложение ещё грузит конфиг, прогревает пул соединений и JIT-компилируется. Запросы приходят на процесс, который ещё не обслуживает, и получают 500.
</Hook>

<Crux>Kubernetes декларативен: задаёшь желаемое состояние, контроллеры вечно сводят к нему фактическое — никогда не запускай голый Pod.</Crux>

<Explanation>

## Декларативно, а не императивно: цикл сверки

Единственная идея, которая делает Kubernetes цельным, такова: ты не говоришь кластеру, *что делать*, ты говоришь, *что должно быть истиной*. Ты задаёшь желаемое состояние — «хочу 5 реплик этого образа» — а контроллеры кластера крутят цикл вечно, сравнивая фактическое состояние с желаемым и предпринимая корректирующее действие, чтобы закрыть разрыв. Это **цикл сверки** (reconciliation loop), и именно поэтому Kubernetes самоисцеляется. Узел умер и три пода исчезли? Контроллер ReplicaSet наблюдает `actual=2, desired=5` и создаёт три новых. Ты не писал «перезапускать при отказе» — ты задал инвариант, а контроллер его держит.

Это и есть самая глубокая причина, почему в проде никогда не делают `kubectl run` голого Pod. Голый Pod императивен: им никто не владеет, его никто не сверяет. Узел, на котором он живёт, перезагрузился — и Pod исчез навсегда, ни один контроллер его не пересоздаст. Тот же цикл, что исцеляет твой Deployment, игнорирует сиротский Pod, потому что ни один контроллер не держит его как цель желаемого состояния.

<Inset kind="why" lang="ru">
Цикл обязан быть идемпотентным: запустить его дважды с тем же желаемым состоянием не должно ничего удваивать. Поэтому контроллеры сравнивают-потом-действуют, а не просто действуют. Контроллер, который «создаёт под, когда видит Deployment», создавал бы бесконечные поды на каждом тике; контроллер, который «обеспечивает, что число подов равно replicas», сходится и затем ничего не делает. Идемпотентность — это то, что позволяет циклу крутиться непрерывно без дрожания.
</Inset>

## Иерархия рабочей нагрузки: Pod → ReplicaSet → Deployment

Зачем три отдельных объекта вместо одного? Потому что каждый сценарий отказа требует разного уровня владения — и понимание того, какой слой за что отвечает, сразу показывает, куда смотреть при сломанном раскате.

Три вложенных объекта, каждый добавляет одну способность:

- **Pod** — наименьшая разворачиваемая единица. Один или несколько контейнеров, которые делят сетевой namespace (один IP, общаются через `localhost`) и могут делить тома. Поды *эфемерны*: они получают новый IP при каждом перезапуске и спроектированы, чтобы их выбрасывали и заменяли, а не чинили.
- **ReplicaSet** — держит ровно N идентичных подов живыми. Вся его работа — это число: он следит за своими подами и сводит к `replicas: N`. Напрямую его почти никогда не создают.
- **Deployment** — управляет ReplicaSet-ами, чтобы дать rollouts и rollback. Когда ты меняешь образ, Deployment создаёт *новый* ReplicaSet и масштабирует его вверх, масштабируя старый вниз, под управлением `maxSurge` и `maxUnavailable` (оба по умолчанию 25%). Старый ReplicaSet остаётся в масштабе 0 — именно так работает `kubectl rollout undo`: он масштабирует предыдущий ReplicaSet обратно вверх.

Эти три слоя вместе означают, что тебе не нужно нигде писать «перезапускать при отказе» — ты объявляешь count, и иерархия его держит. Пропусти любой слой и потеряешь гарантию: у голого Pod нет родителя-ReplicaSet, поэтому никто не следит за count, а у ReplicaSet без Deployment нет механизма отката.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Объект</th>
        <th class="border border-border px-3 py-2 text-left">Владеет</th>
        <th class="border border-border px-3 py-2 text-left">Какую одну способность добавляет</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>Pod</code></td>
        <td class="border border-border px-3 py-2">Контейнерами</td>
        <td class="border border-border px-3 py-2">Запуск контейнеров вместе (общая сеть + тома); эфемерен</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>ReplicaSet</code></td>
        <td class="border border-border px-3 py-2">Подами</td>
        <td class="border border-border px-3 py-2">Держать ровно N копий живыми (сверка числа)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>Deployment</code></td>
        <td class="border border-border px-3 py-2">ReplicaSet-ами</td>
        <td class="border border-border px-3 py-2">Rollouts + rollback (постепенная замена ReplicaSet-ов)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>Service</code></td>
        <td class="border border-border px-3 py-2">(выбирает поды)</td>
        <td class="border border-border px-3 py-2">Стабильный IP + DNS поверх меняющегося набора IP подов</td>
      </tr>
    </tbody>
  </table>
</div>

## Services и клей из label-селекторов

Поды эфемерны и их IP постоянно меняются, поэтому ничто никогда не должно обращаться к IP пода напрямую. **Service** даёт тебе стабильный виртуальный IP и DNS-имя (`my-svc.my-namespace.svc.cluster.local`), которое остаётся фиксированным, пока поды за ним сменяются. Магия, которая привязывает Service к его подам, — это **метки и селекторы** (labels & selectors): Service объявляет `selector: { app: web }`, и Kubernetes поддерживает объект Endpoints (или EndpointSlice — масштабируемая замена Endpoints, разбивающая список по подмножествам), перечисляющий IP каждого пода, чьи метки совпадают. `kube-proxy` затем балансирует трафик по этим endpoint-ам. Метки — универсальный клей по всему Kubernetes: Deployment использует тот же механизм, чтобы знать, какими подами он владеет.

Три основных типа Service — это многослойная эскалация доступа:

- **ClusterIP** (по умолчанию) — виртуальный IP, доступный только внутри кластера. Внутренний трафик между сервисами.
- **NodePort** — открывает статический порт (диапазон по умолчанию `30000–32767`) на IP каждого узла, проброс на ClusterIP. Грубый внешний доступ; для прод-HTTP редко правильный ответ.
- **LoadBalancer** — провижинит облачный балансировщик, указывающий на NodePort. Цепочка трафика: внешний клиент → облачный LB → node:NodePort → ClusterIP → Pod. Один LB на каждый Service, что быстро становится дорого.
- **Ingress** — не тип Service, а L7 HTTP-роутер *перед* Services. Один LoadBalancer питает Ingress-контроллер (NGINX, Traefik), который маршрутизирует по host и path на множество ClusterIP-сервисов. Так ты выставляешь десятки приложений за одним внешним IP и TLS-сертификатом.

## ConfigMap, Secret и прод-провал

Конфиг и креды живут в своих объектах, чтобы ты не запекал их в образ: **ConfigMap** для нечувствительного конфига, **Secret** для кредов (закодированы в base64 и *не* зашифрованы at rest, пока не включишь шифрование). Оба монтируются как env-переменные или файлы. Один острый угол: изменение ConfigMap **не** триггерит rollout — поды держат старые значения до перезапуска, поэтому команды хешируют конфиг в аннотацию пода, чтобы форсировать новый ReplicaSet при изменении.

Теперь провал из Hook, по механике. Service маршрутизирует на под в тот момент, когда под становится `Ready`. Без **readiness-пробы** «ready» значит лишь «процесс контейнера стартовал» — а не «приложение может обслуживать запросы». Поэтому на каждом rollout Service добавляет новый под в свои endpoints, пока приложение ещё прогревается, и часть трафика даёт 500, пока оно не закончит. Фикс — readiness-проба (HTTP `GET /healthz`, TCP-проверка или exec), которая возвращает успех, только когда приложение по-настоящему обслуживает. Падающая readiness-проба вытаскивает под *из* endpoints Service — никакого трафика, пока она не пройдёт. Это отличается от **liveness-пробы**, которая *перезапускает* зависший под. Классический инцидент: использовать liveness там, где нужна была readiness, и медленно стартующее приложение убивают и перезапускают в цикле вместо того, чтобы просто держать вне ротации. Дефолты проб агрессивны — `periodSeconds: 10`, `timeoutSeconds: 1`, `failureThreshold: 3` — и медленный `/healthz` под нагрузкой будет флапать; для медленного старта используй `startupProbe`, чтобы придержать остальные.

<TradeoffMatrix
  id="03-k8s-objects-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Stateless HTTP API прогревается ~20с (загрузка конфига + пул соединений + прогрев кэша), прежде чем может обслуживать. Нужны rollout-ы без ошибок. Что настроишь?"
  options={[
    { name: "readiness-проба (HTTP GET /healthz), возвращающая 200 только после прогрева; плюс startupProbe, чтобы покрыть 20с старта", summary: "Держать трафик до настоящей готовности.", correct: true, justification: "Readiness управляет членством в endpoints Service: под остаётся вне ротации, пока /healthz не сообщит, что пул и кэш прогреты, поэтому ни один запрос не попадёт на холодный процесс. startupProbe даёт медленному старту щедрое окно, прежде чем liveness/readiness начнут свои тесные циклы, предотвращая убийство во время прогрева. Это каноничная схема rollout без простоя." },
    { name: "Только liveness-проба с длинным initialDelaySeconds, чтобы покрыть прогрев", summary: "Перезапуск при отказе.", justification: "Liveness перезапускает нездоровые поды — она НЕ управляет тем, шлёт ли Service на них трафик. Под может быть «live» и получать трафик, всё ещё прогреваясь, так что ты всё равно даёшь 500 на rollout. Хуже того, слишком короткое окно liveness убивает медленно стартующие поды в цикле." },
    { name: "Без проб; поднять maxUnavailable до 0, чтобы старые поды держались, пока новые не Running", summary: "Опереться на темп rollout.", justification: "maxUnavailable управляет тем, сколько старых подов сливается, но «Running» без readiness-пробы по-прежнему значит «процесс стартовал, не обслуживает». Service маршрутизирует на новый под в тот же миг, как он Running, так что трафик всё равно попадает на холодное приложение. Темп rollout не заменяет сигнал readiness." },
    { name: "Добавить 30с sleep в entrypoint контейнера перед стартом сервера", summary: "Задержать старт.", justification: "Фиксированный sleep — это догадка, а не сигнал: слишком коротко — и ты всё равно обслуживаешь холодным, слишком долго — и ты тратишь время rollout и ёмкость. Время прогрева меняется с нагрузкой и зависимостями. readiness-проба измеряет реальное условие вместо угадывания константы." },
  ]}
/>

<Quiz
  id="03-k8s-objects-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="Узел перезагрузился и забрал с собой три твоих пода. Твой Deployment запросил 5 реплик. Что произойдёт и почему?"
  choices={[
    { label: "Контроллер ReplicaSet наблюдает actual=2 против desired=5 и создаёт три новых пода — цикл сверки закрывает разрыв", correct: true },
    { label: "Ничего — поды исчезли, пока кто-то снова не запустит kubectl apply", misconception: "Это императивное мышление. Deployment задал желаемое число; контроллер сводит к нему непрерывно, без действий человека." },
    { label: "Весь Deployment пересоздаётся с нуля, включая новый ReplicaSet", misconception: "Сверка инкрементальна — она создаёт только недостающие поды до желаемого числа, а не свежий ReplicaSet." },
    { label: "Поды вернутся, только если настроена liveness-проба", misconception: "Liveness перезапускает зависший контейнер на месте; пересоздание подов, потерянных с узлом, — это сверка числа в ReplicaSet, независимая от проб." },
  ]}
/>

<Quiz
  id="03-k8s-objects-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Как Service узнаёт, на какие поды слать трафик?"
  choices={[
    { label: "По своему label-селектору — Kubernetes поддерживает список Endpoints из каждого пода, чьи метки совпадают с селектором", correct: true },
    { label: "По IP подов, которые ты перечислил в spec Service", misconception: "IP подов меняются при каждом перезапуске; хардкод сломался бы мгновенно. Селектор отслеживает поды динамически по метке." },
    { label: "По имени Deployment, на который ссылается Service", misconception: "Services вообще не ссылаются на Deployment — они выбирают поды напрямую по метке, поэтому Service может фронтить поды из нескольких Deployment-ов." },
    { label: "По имени узла — маршрутизирует на тот узел, где работают поды", misconception: "Маршрутизация по метке пода, а не по узлу. kube-proxy балансирует по совпадающим endpoint-ам подов, независимо от того, на каком узле они сидят." },
  ]}
/>

<DragOrder
  id="03-k8s-objects-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь, что происходит при смене образа Deployment (rolling update):"
  items={[
    { id: "a", label: "Ты применяешь новый образ; Deployment записывает новое желаемое состояние" },
    { id: "b", label: "Deployment создаёт новый ReplicaSet для нового образа" },
    { id: "c", label: "Новый RS масштабируется вверх, старый — вниз, в границах maxSurge / maxUnavailable" },
    { id: "d", label: "Каждый новый под проходит readiness-пробу, прежде чем Service шлёт на него трафик" },
    { id: "e", label: "Старый ReplicaSet достигает масштаба 0, но сохраняется, чтобы rollout undo мог поднять его обратно" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<StackDiagram
  label="Иерархия владения рабочей нагрузкой Kubernetes от Deployment наверху вниз до контейнеров, где каждый слой владеет нижним и сверяет его"
  hue="var(--d-systems)"
  layers={[
    { label: "Deployment", note: "владеет ReplicaSet-ами — rollout + rollback" },
    { label: "ReplicaSet", note: "владеет Pod-ами — держит ровно N живыми" },
    { label: "Pod", note: "владеет контейнерами — эфемерен, новый IP при рестарте" },
    { label: "Контейнеры", note: "общая сеть + тома" },
  ]}
  caption="Каждый слой добавляет одну задачу и сверяет нижний: Deployment меняет ReplicaSet-ы для выката, ReplicaSet держит число Pod-ов равным N, Pod запускает контейнеры. У голого, ничейного Pod нет контроллера сверху, поэтому никто не пересоздаёт его, когда умирает узел."
/>

<KeyTakeaway>Задавай желаемое состояние; контроллеры сводят к нему фактическое. Знай цепочку Pod→ReplicaSet→Deployment и Services, иначе rollout даёт 500.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-k8s-objects-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, почему Deployment самоисцеляется после отказа узла, а голый Pod — нет.", a: "Deployment владеет ReplicaSet-ом, а контроллер ReplicaSet крутит цикл сверки: он непрерывно сравнивает фактическое число работающих подов с желаемым числом реплик и создаёт или удаляет поды, чтобы закрыть разрыв. Когда узел умирает и забирает с собой поды, контроллер видит фактическое ниже желаемого и создаёт замены — ты не писал «перезапускать при отказе», ты задал инвариант (replicas: N), который контроллер держит вечно. Голый Pod, созданный через kubectl run, императивен: ни один контроллер не держит его как цель желаемого состояния. Его никто не сверяет, поэтому, когда его узел перезагружается, Pod исчезает навсегда. Это владение со стороны контроллера — вся причина, почему прод-нагрузки работают под Deployment-ами, а не голыми Pod-ами." },
    { q: "Пройди по шагам, как именно отсутствующая readiness-проба вызывает 500 во время rollout и как проба это чинит.", a: "Service маршрутизирует трафик на под в тот момент, когда под сообщает Ready. Без readiness-пробы «Ready» вырождается в просто «процесс контейнера стартовал» — у Kubernetes нет сигнала уровня приложения, поэтому он предполагает, что стартовавший процесс обслуживает. Во время rolling update Deployment поднимает новые поды; в тот же миг, как каждый Running, Service добавляет его IP в список Endpoints, и kube-proxy балансирует на него живой трафик. Но приложение ещё грузит конфиг, прогревает пул соединений и JIT-компилируется, поэтому эти ранние запросы попадают на процесс, который ещё не может обслуживать, и возвращают 500 — подскок error rate на каждом деплое, пока прогрев не закончится. readiness-проба (например HTTP GET /healthz) чинит это, возвращая успех, только когда приложение реально может обслуживать; до тех пор под держится вне endpoints Service и не получает трафика. Для медленного старта спарь её с startupProbe, чтобы тесные циклы readiness/liveness не срабатывали в окне прогрева." },
  ]}
/>

<Recap lang="ru">
Kubernetes декларативен: ты задаёшь желаемое состояние, а контроллеры вечно крутят цикл сверки, сравнивая фактическое с желаемым и действуя, чтобы закрыть разрыв — именно это делает кластер самоисцеляющимся и поэтому в проде никогда не запускают голый, ничейный Pod. Иерархия рабочей нагрузки складывает по одной способности на слой: Pod запускает контейнеры с общей сетью и томами, но эфемерен; ReplicaSet держит ровно N подов живыми, сверяя число; Deployment управляет ReplicaSet-ами, чтобы выкатывать и откатывать, постепенно меняя их под maxSurge и maxUnavailable. Services дают стабильный виртуальный IP и DNS поверх сменяющихся IP подов, привязанные label-селекторами в список Endpoints, эскалируя от ClusterIP к NodePort и LoadBalancer, а Ingress делает L7-маршрутизацию впереди. ConfigMap и Secret выносят конфиг и креды наружу. И прод-урок, который всё связывает: Service маршрутизирует на под в тот же миг, как он Ready, поэтому без readiness-пробы каждый rollout шлёт трафик на приложения, которые стартовали, но ещё не могут обслуживать — и они дают 500, пока не прогреются. Теперь, когда ты видишь всплеск ошибок на каждом деплое, первый вопрос звучит так: есть ли в этом Deployment readiness-проба — и действительно ли она гейтит трафик, или её попросту нет?
</Recap>
