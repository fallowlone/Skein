---
concepts:
- reconciliation-loop
- pod-replicaset-deployment
- service-label-selector
- readiness-vs-liveness
- configmap-secret
- resource-requests-limits
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://kubernetes.io/docs/concepts/architecture/controller/
- https://kubernetes.io/docs/concepts/workloads/controllers/deployment/
- https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/
- https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
status: ready
summary: "Промпты на свободное воспроизведение по всему юниту K8s objects. Сначала ответьте своими словами, затем откройте модельный ответ и сравните."
title: 'K8s objects: тест на воспроизведение'
track: deployment
unit: 03-k8s-objects
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. На каждый промпт скажите или запишите полный ответ по памяти, прежде чем открыть модельный — реконструкция механизма закрепляет его так, что он всплывёт, когда вы будете смотреть на сломанный раскат в 2 часа ночи.
</Hook>

<Goal lang="ru">
Восстановите ключевые механизмы юнита — reconciliation loop, иерархию объектов, селекторы Service, readiness против liveness, распространение ConfigMap/Secret и requests против limits — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:load
  id="03-k8s-objects-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему Deployment самовосстанавливается после отказа узла, а голый Pod — нет?", a: "Deployment владеет ReplicaSet, и контроллер ReplicaSet гоняет reconciliation loop: он непрерывно сравнивает фактическое число работающих подов с desired count реплик и создаёт или удаляет поды, закрывая разрыв. Когда узел умирает и уносит поды, контроллер видит actual ниже desired и создаёт замену — вы не писали 'перезапускать при сбое', вы объявили инвариант (replicas: N), который контроллер навязывает вечно. Голый Pod через kubectl run императивен: ни один контроллер не держит его как цель desired-state, поэтому его никто не reconcile. При перезагрузке или drain узла такой Pod исчезает навсегда. Это владение-контроллером — вся причина, по которой продакшен-ворклоады живут под Deployment, а не голыми Pod." },
    { q: "Опишите иерархию Pod → ReplicaSet → Deployment — какую единственную задачу добавляет каждый слой?", a: "Каждый слой добавляет ровно одну способность. Pod — наименьшая разворачиваемая единица: один или несколько контейнеров, делящих сетевой namespace (один IP, общение через localhost) и опционально тома; он эфемерен и получает новый IP при каждом перезапуске. ReplicaSet добавляет удержание count: он следит за своими подами и reconcile к replicas: N, пересоздавая исчезнувшие — напрямую его почти не создают. Deployment добавляет раскаты и откат: при смене образа он создаёт новый ReplicaSet и масштабирует его вверх, масштабируя старый вниз под maxSurge/maxUnavailable (оба по умолчанию 25%), и держит старый ReplicaSet на scale 0, чтобы rollout undo мог поднять его обратно. Итог: Pod гоняет контейнеры, ReplicaSet держит count, Deployment управляет версионными переходами." },
    { q: "Как Service узнаёт, на какие поды маршрутизировать, и каков режим отказа при пересекающихся метках?", a: "Service объявляет label selector, например selector: app=web, и Kubernetes поддерживает объект Endpoints (или EndpointSlice) со списком IP каждого Pod, чьи метки подходят; kube-proxy затем балансирует по этим endpoints. Он отслеживает поды динамически по метке, а не по жёстко прописанному IP — это критично, потому что Pod IP меняются при каждом перезапуске. У Service нет понятия о том, какой Deployment владеет подом. Поэтому если два разных Deployment создают поды с app=web, Service молча маршрутизирует на поды обоих, смешивая ворклоады за одним набором endpoints. Пересекающиеся селекторы никогда не ошибаются; они просто накапливают подходящие IP — частый источник случайного смешения трафика." },
    { q: "Сравните readiness, liveness и startup probes — какой гейтит трафик и каков классический отказ?", a: "Readiness probe гейтит членство в endpoints Service: пока он падает, под выведен из endpoints Service и не получает трафик — именно это делает раскаты безошибочными, ведь новые поды остаются вне ротации, пока реально не смогут обслуживать. Liveness probe перезапускает под, чей контейнер завис; он не управляет маршрутизацией. Startup probe удерживает циклы liveness и readiness во время медленного старта, чтобы медленно стартующее приложение не убивали до конца прогрева. Классический отказ — использование liveness там, где нужен readiness: медленно стартующее приложение убивают и перезапускают в цикле вместо того, чтобы просто держать вне ротации, или — без readiness probe вовсе — Service маршрутизирует на под в момент старта процесса, и трафик 500-ит на холодном приложении при каждом раскате." },
    { q: "Почему обновление ConfigMap или Secret не катит потребляющие их поды и как команды форсируют обновление?", a: "Значения, потребляемые как переменные окружения, инжектируются один раз при старте контейнера и заморожены на жизнь пода; изменение ConfigMap или Secret не триггерит никакого раската, поэтому работающие поды держат старые значения до перезапуска. Смонтированные томом ConfigMap/Secret со временем обновляют проецируемые файлы, но даже тогда процесс должен их перечитать, а под не перезапускается. Стандартное исправление — хешировать содержимое ConfigMap/Secret в аннотацию pod-template: при изменении config меняется хеш, меняется pod-template, и Deployment катит новый ReplicaSet — так обычный kubectl apply распространяет новый config. Учтите: Secret лишь base64-кодированы, а не зашифрованы at rest, если не включено шифрование." },
    { q: "Объясните разницу между resource requests и limits и почему CPU и память ведут себя по-разному на лимите.", a: "Requests — это то, что планировщик резервирует для размещения пода и что гарантировано под конкуренцией; limits — жёсткий потолок фактического потребления. Два ресурса ведут себя по-разному, потому что CPU сжимаем, а память — нет. Когда контейнер превышает лимит CPU, его троттлят — квота CFS тормозит процесс, давая всплески задержек, но без убийства. Когда он превышает лимит памяти, происходит OOMKill, потому что память нельзя вернуть, замедлив процесс; ядро завершает контейнер. Поэтому лимит памяти острее. Равенство request и limit даёт класс QoS Guaranteed (последним вытесняется под давлением узла); requests заметно ниже limits дают Burstable. Right-sizing — задать requests по реальному устойчивому потреблению и limits с запасом, чтобы избежать ложного троттлинга или OOMKill." },
  ]}
/>

<Recap lang="ru">
Если вы смогли восстановить каждый ответ по памяти, вы держите хребет юнита: контроллеры вечно reconcile объявленное состояние (поэтому Deployment самовосстанавливается, а голый Pod нет), иерархия добавляет по задаче на слой (Pod гоняет, ReplicaSet считает, Deployment катит), Service привязываются к подам по метке без знания о владении, readiness гейтит трафик, liveness перезапускает, а startup покрывает медленный старт, изменения ConfigMap/Secret никогда не катят автоматически — потому их хешируют в аннотацию, а ресурсы делятся на троттлящийся CPU и OOMKill-память. Теперь, когда вы столкнётесь с продакшен-инцидентом, вы сначала сведёте его к одному из этих механизмов — и лишь потом начнёте что-то менять.
</Recap>
