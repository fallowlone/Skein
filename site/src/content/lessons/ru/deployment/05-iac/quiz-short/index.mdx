---
concepts:
- declarative-desired-state
- terraform-plan-apply
- remote-state-backend
- state-locking
- infrastructure-drift
- immutable-infrastructure
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://developer.hashicorp.com/terraform/language/state/locking
- https://developer.hashicorp.com/terraform/tutorials/state/resource-drift
- https://www.hashicorp.com/en/blog/detecting-and-managing-drift-with-terraform
status: ready
summary: "Промпты на свободное припоминание по всему модулю IaC. Сначала ответь своими словами, затем открой эталонный ответ и сравни."
title: 'Infrastructure as Code: тест на припоминание'
track: deployment
unit: 05-iac
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт скажи или напиши полный ответ по памяти до того, как откроешь эталон — именно усилие припоминания закрепляет механизм, когда у терминала окажешься ты.
</Hook>

<Goal lang="ru">
Восстанови ключевые механизмы модуля — декларативное желаемое состояние и idempotency, движок diff в plan/apply, файл состояния как карту идентичности и опасность, locking при конкурентности и обработку drift — не подглядывая в урок.
</Goal>

<RetrievalDrawer
  client:visible
  id="05-iac-short-retrieval"
  lang="ru"
  questions={[
    { q: "Что значит, что IaC декларативен, и почему idempotency вытекает из этого?", a: "Декларативность значит, что ты описываешь желаемое конечное состояние — эти ресурсы существуют с такими свойствами — а не императивные шаги к нему. Инструмент читает декларацию, считает граф зависимостей и сам определяет упорядоченные действия. Idempotency — необходимое следствие: раз ты объявил конечное состояние, а не последовательность мутаций, повторный apply должен сходиться к тому же результату, а не накапливать дубликаты. Инструмент может гарантировать это лишь потому, что помнит, что уже построил, в файле состояния; на холостом apply он делает diff конфига против состояния, видит совпадение и ничего не предлагает. Выигрыш декларативной модели в том, что желаемое состояние — это текст в git: окружения становятся воспроизводимыми, ревьюабельными в PR и аудируемыми через историю blame, чего клик в консоли не даёт." },
    { q: "Пройди по тому, что на самом деле делает terraform plan, и назови три его входа.", a: "plan — это движок diff. Он читает три входа: конфиг (твоё желаемое состояние в коде .tf или Pulumi), записанный файл состояния (что инструмент построил в последний раз, включая карту от имён конфига к реальным ID ресурсов) и реальность (он делает refresh, опрашивая живого провайдера). Сперва он примиряет состояние с живым провайдером, затем печатает diff — что создаст, изменит или уничтожит — ничего не трогая. apply затем исполняет этот diff и пишет новую реальность обратно в файл состояния. Ключевая деталь: и plan, и apply сначала делают refresh — именно там проявляется drift: если реальность разошлась с записанным состоянием, refresh это ловит и plan показывает неожиданное изменение." },
    { q: "Почему файл состояния — одновременно источник истины и главная опасность?", a: "Это карта идентичности от символьных имён в конфиге (aws_lb.web) к реальным ID ресурсов провайдера, которые создал инструмент. Без неё инструмент буквально не знает, каким живым ресурсом управляет блок конфига, поэтому пересоздаст или осиротит ресурсы — это сторона источника истины. Сторона опасности тройная. Первое: он сериализует атрибуты ресурсов, включая секреты, в открытом виде по умолчанию, поэтому любой с доступом на чтение к бэкенду имеет твои пароли и ключи — исправление в том, чтобы никогда не прогонять секреты через state как outputs, а писать их прямо в secrets manager. Второе: он подвержен порче, поэтому должен жить в версионируемом удалённом бэкенде, который можно откатить. Третье: записи небезопасны при конкурентности, поэтому он должен быть залочен. Относись к нему как к продовой БД: залочен, забэкаплен, с контролем доступа и никогда не место для прогона секретов." },
    { q: "Почему конкурентные apply портят состояние и что именно предотвращает лок?", a: "Запись в файл состояния не атомарна на уровне команды. Если два apply идут одновременно, оба читают старое состояние, оба считают планы против той же базы и оба пишут обратно — второй затирает первый, и итоговый файл не описывает ни реальность, ни один из планов. Это порча, а не чистый last-writer-wins. Лок предотвращает это, заставляя бэкенд взять эксклюзивный лок перед любой записью; второй apply тогда ждёт (по -lock-timeout) или быстро падает с 'Error acquiring the state lock' вместо гонки. CI-дисциплина вокруг: concurrency group, чтобы джобы не пересекались, разумный lock-timeout, чтобы легитимные прогоны в полёте ожидались, а не падали, и force-unlock, зарезервированный только для действительно несвежих локов от упавшего прогона — никогда во время активной записи другого apply." },
    { q: "Дай определение drift, безопасный способ его обнаружить и ловушку тихого отката.", a: "Drift — это когда реальная инфраструктура расходится с тем, что говорят файл состояния и конфиг, — почти всегда из-за изменения вне инструмента: клика в консоли или аварийного патча через CLI во время инцидента. Обнаруживаешь его через terraform plan, который делает refresh против провайдера и сообщает различия; безопасный осознанный способ — plan -refresh-only, который показывает расхождение, не перезаписывая состояние, как голый refresh, в идеале по расписанию. Ловушка тихого отката в том, что IaC всегда примиряет реальность обратно к заявленному желаемому состоянию, поэтому рутинный apply тихо удалит ручное исправление, которого нет в конфиге, — повторно вызвав тот самый инцидент, что исправление решило. Поэтому, когда plan показывает drift, ты сперва принимаешь решение: закодифицировать ручное изменение в конфиг, если оно должно остаться, или дать apply восстановить декларацию, если это была ошибка. Ты решаешь намерение до apply, а не даёшь apply решать за тебя." },
    { q: "Что такое иммутабельная инфраструктура и как она связана с drift?", a: "Иммутабельная инфраструктура значит, что ты перестаёшь вручную мутировать работающие серверы и вместо этого заменяешь их: печёшь новый образ, выкатываешь его и уничтожаешь старый, а не патчишь на месте. Это напрямую связано с drift, потому что drift приходит из мутаций на месте вне инструмента — а когда ничего не мутируется на месте, у реальности гораздо меньше поверхности, чтобы уйти от декларации. Это также делает откат тривиальным: вместо того чтобы помнить и отменять каждую ручную правку, ты заново выкатываешь предыдущий известно-хороший образ. Так иммутабельность — глубокое лечение, тогда как обнаружение drift лишь снимает симптомы: detect-and-reconcile обрабатывает drift после факта, а иммутабельность убирает большую часть возможностей ему случиться вообще." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, у тебя есть хребет модуля: декларативное желаемое состояние делает apply идемпотентным; plan — движок diff на трёх входах (конфиг, состояние, обновлённая реальность); файл состояния — карта идентичности, которая одновременно опасность, поэтому живёт залоченным и версионируемым и никогда не несёт секретов; конкурентные записи портят состояние — поэтому существует locking; а drift проявляется в plan, но примиряется тихо, поэтому обнаруживай его через -refresh-only и решай намерение до apply — опираясь на иммутабельную инфраструктуру, чтобы дрейфить было нечему. Теперь, когда откроешь plan и увидишь неожиданный destroy или изменение после ночного инцидента, первым шагом будет -refresh-only, а не apply — и вопрос: кто сделал это и должно ли оно остаться.
</Recap>
