---
concepts:
- runtime-injection
- base64-not-encryption
- etcd-encryption-at-rest
- env-vs-file-mounts
- sealed-secrets
- secret-rotation
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://kubernetes.io/docs/concepts/configuration/secret/
- https://kubernetes.io/docs/tasks/administer-cluster/encrypt-data/
- https://external-secrets.io/latest/introduction/overview/
status: ready
summary: "Синтез по всему юниту secrets-at-deploy в формате выбора: гигиена образа, base64 против шифрования, etcd at rest, способ инъекции, sealed secrets и rotation."
title: 'Secrets at deploy: тест с выбором ответа'
track: deployment
unit: 07-secrets-at-deploy
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов через весь юнит. Каждый отражает реальное решение на этапе деплоя — это не определение для заучивания, а модель угроз, которую нужно продумать под аудитом.
</Hook>

<Goal lang="ru">
Убедитесь, что можете связать гигиену образа, ловушку base64, шифрование etcd, способ инъекции, sealed secrets и rotation в единую позицию: где secret попадает внутрь и где утекает.
</Goal>

<Quiz
  id="07-secrets-at-deploy-choice-1"
  lessonSlug="07-secrets-at-deploy"
  lang="ru"
  question="Dockerfile задаёт ENV API_KEY=sk-live-abc в раннем слое, затем поздний слой выполняет `unset API_KEY`. Безопасен ли ключ в запушенном образе?"
  choices={[
    { label: "Нет — слои образа контент-адресуемые и неизменяемые; ранний слой по-прежнему хранит значение, а `docker history` / `docker save` его воспроизводят. Удаление в позднем слое его не убирает.", correct: true },
    { label: "Да — побеждает финальный слой, поэтому в рантайм-окружении ключа больше нет.", misconception: "Состояние env в рантайме и слои на диске — разные вещи. Build-time ENV вшит в слой, который остаётся в реестре независимо от того, что делает поздний слой в рантайме." },
    { label: "Да — `unset` вычищает переменную из всех слоёв при squash.", misconception: "Автоматического squash нет, и даже squash-образ утечёт, если вытянуть build-кэш или промежуточные слои. Правило: вообще не класть secret в сборку." },
    { label: "Небезопасно только если реестр публичный.", misconception: "Любой с доступом на чтение реестра вытягивает и воспроизводит слои. Приватные реестры всё равно утекают на каждый аккаунт, CI-джоб и закешировавший узел, который может вытянуть образ." },
  ]}
/>

<Quiz
  id="07-secrets-at-deploy-choice-2"
  lessonSlug="07-secrets-at-deploy"
  lang="ru"
  question="Коллега уверяет, что кластер безопасен, потому что каждое значение Kubernetes Secret закодировано в base64. В чём точная поправка?"
  choices={[
    { label: "base64 — это обратимая транспортная кодировка без ключа: `base64 -d` восстанавливает плейнтекст примерно за секунду. Утёкший манифест, бэкап или история GitOps — это утёкший пароль; защита требует настоящего шифрования.", correct: true },
    { label: "Всё в порядке — base64 это одностороннее преобразование, оригинал не восстановить.", misconception: "base64 полностью обратим, это не хеш. Любой, у кого есть закодированная строка, тривиально получает плейнтекст обратно." },
    { label: "Всё в порядке — API-сервер шифрует значение base64 ключом кластера перед хранением.", misconception: "Кодирование и шифрование — разные вещи. Значение base64 по умолчанию лежит в etcd незашифрованным; никакой ключ кластера не применяется, пока не настроишь encryption at rest." },
    { label: "Частично безопасно — base64 останавливает случайные просмотры, но не целенаправленных атакующих.", misconception: "base64 не останавливает ничего. Нет ключа и нет work factor; разворот — одна команда, так что конфиденциальности ноль даже против случайного доступа." },
  ]}
/>

<Quiz
  id="07-secrets-at-deploy-choice-3"
  lessonSlug="07-secrets-at-deploy"
  lang="ru"
  question="Вы настроили EncryptionConfiguration, чтобы API-сервер шифровал Secrets at rest в etcd. Аудитор всё равно находит старый Secret в плейнтексте в бэкапе etcd. Почему и что упущено?"
  choices={[
    { label: "Включение encryption at rest шифрует только записи с этого момента — оно не перешифровывает существующие Secrets. После включения нужно переписать каждый Secret (например, `kubectl get secrets -A -o json | kubectl replace -f -`).", correct: true },
    { label: "EncryptionConfiguration тихо упала, и ничего никогда не шифровалось.", misconception: "Новые Secrets, записанные после изменения, зашифрованы; пробел именно в уже существующих значениях, которые не трогаются, пока их не перепишут." },
    { label: "Бэкапы etcd всегда хранятся в плейнтексте независимо от encryption at rest.", misconception: "Encryption at rest означает, что API-сервер шифрует перед записью в etcd, поэтому бэкап зашифрованных Secrets — это шифртекст. Плейнтекст здесь — остаток непреобразованных данных." },
    { label: "Кодирование base64 перекрыло слой шифрования.", misconception: "base64 — это лишь кодировка поля value; оно не взаимодействует со слоем encryption at rest в etcd и не перекрывает его." },
  ]}
/>

<Quiz
  id="07-secrets-at-deploy-choice-4"
  lessonSlug="07-secrets-at-deploy"
  lang="ru"
  question="Error-трекер показывает живой пароль БД в плейнтексте внутри стектрейса. Secret инъецировался как переменная окружения. Корневая причина и структурное исправление?"
  choices={[
    { label: "Crash dump сериализует всё окружение процесса, поэтому SDK отправил env в сторонний store. Инъецируйте secret как смонтированный файл — не в окружении, не в /proc/.../environ, не наследуется дочерними процессами.", correct: true },
    { label: "В SDK баг с логированием env-переменных; обновите его — и утечка прекратится.", misconception: "Захват состояния процесса при ошибке — это задуманное поведение SDK, а не баг. Пока secret живёт в окружении, любой захват crash-state может его утечь." },
    { label: "Пароль был слишком длинным для безопасного хранения в env и обрезался в лог.", misconception: "Длина ни при чём. Утечка из-за того, что env глобален, наследуется и сбрасывается при крахе — файл, который читаешь один раз и не реэкспортируешь, избегает всех трёх." },
    { label: "Исправление — вычистить имя переменной в deny-list SDK.", misconception: "Deny-list — хрупкая заплатка, пропускающая переименованные или вложенные ключи. Структурное исправление — вообще убрать secret из окружения через file mount." },
  ]}
/>

<Quiz
  id="07-secrets-at-deploy-choice-5"
  lessonSlug="07-secrets-at-deploy"
  lang="ru"
  question="Небольшой GitOps-команде нужно закоммитить секретную конфигурацию в приватный репозиторий, чтобы Argo CD её применил, при минимуме внешних зависимостей. Какой подход верный и почему его действительно безопасно коммитить?"
  choices={[
    { label: "Sealed Secrets — публичный ключ шифрует значение, и только приватный ключ in-cluster контроллера может расшифровать, так что закоммиченный блоб — это настоящий шифртекст. Утёкший репозиторий, форк или вся история git не раскрывают ничего полезного, и внешний vault не требуется.", correct: true },
    { label: "Закоммитить обычный Kubernetes Secret со значениями в base64 — Argo CD читает его напрямую.", misconception: "base64 — это кодирование, не шифрование. Любой с доступом на чтение репозитория выполняет base64 -d и читает плейнтекст; форки и история делают утечку необратимой. Это ровно та ловушка, с которой начинается юнит." },
    { label: "Положить значения в ConfigMap, чтобы они не помечались как Secrets.", misconception: "ConfigMap по замыслу в плейнтексте и предназначен для несекретной конфигурации. Коммитить туда секреты строго хуже base64 Secret — нет кодирования, шифрования и разделения доступа." },
    { label: "External Secrets Operator, тянущий из Vault — закоммитить ссылку ExternalSecret.", misconception: "ESO отлично работает на масштабе, но добавляет внешний менеджер, который команда не просила, и не кладёт самодостаточный коммитируемый артефакт в репозиторий. Тяжелее, чем нужно здесь." },
  ]}
/>

<Quiz
  id="07-secrets-at-deploy-choice-6"
  lessonSlug="07-secrets-at-deploy"
  lang="ru"
  question="Безопасность требует rotation секретов каждые 30 дней и хочет, чтобы украденный credential быстро становился бесполезным. Вы на Kubernetes, доступен Vault. Какая связка лучше всего достигает обеих целей?"
  choices={[
    { label: "Vault dynamic secrets (короткоживущие per-app credentials с авто-отзывом), инъецируемые как смонтированные файлы с обновлением на месте — нет долгоживущего секрета для rotation, а утёкший credential истекает раньше, чем им можно воспользоваться.", correct: true },
    { label: "Инъецировать статический 30-дневный credential как переменную окружения и полагаться на ежемесячный редеплой для rotation.", misconception: "Env-переменные заморожены на старте контейнера, поэтому rotation требует рестарта, а статический 30-дневный credential при краже валиден все 30 дней. Ни одна цель толком не достигнута." },
    { label: "Оставить статический secret, но укоротить cron rotation до ежедневного.", misconception: "Более частая rotation статического долгоживущего секрета сужает окно, но украденный credential всё равно валиден до следующей rotation. Dynamic короткоживущие creds устраняют долгоживущий secret полностью." },
    { label: "Включить etcd encryption at rest и считать rotation решённой.", misconception: "Encryption at rest защищает хранимые Secrets от кражи etcd; оно ничего не делает с частотой rotation или ограничением времени жизни утёкшего credential." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — один путь решений: secret попадает внутрь на деплое или в рантайме (никогда в образе), его нужно по-настоящему шифровать, а не кодировать в base64, для etcd нужно настроить encryption at rest и переписать существующие Secrets, file mounts превосходят env-переменные по поверхности утечки и rotation на месте, Sealed Secrets дают коммитируемый GitOps-артефакт, а dynamic короткоживущие credentials делают украденный secret бесполезным до истечения срока. Каждый неверный ответ здесь — реальный инцидент, который кто-то уже пережил.
</Recap>
