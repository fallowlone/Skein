---
concepts:
- runtime-injection
- base64-not-encryption
- etcd-encryption-at-rest
- env-vs-file-mounts
- external-secrets-operator
- secret-rotation
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://kubernetes.io/docs/concepts/configuration/secret/
- https://kubernetes.io/docs/tasks/administer-cluster/encrypt-data/
- https://external-secrets.io/latest/introduction/overview/
- https://developer.cyberark.com/blog/environment-variables-dont-keep-secrets-best-practices-for-plugging-application-credential-leaks/
spiral: []
status: ready
summary: Секреты должны входить при деплое/рантайме, а не запекаться в образ. Kubernetes
  Secret закодирован в base64, а не зашифрован — любой с доступом на чтение или утёкшим
  манифестом читает его за секунду.
title: 'Секреты при деплое: где они входят и где утекают'
track: deployment
unit: 07-secrets-at-deploy
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Коллега коммитит `secret.yaml` в GitOps-репозиторий. Выглядит безопасно — поле пароля это `cGFzc3dvcmQxMjM=`, непонятный блоб. Через полгода аудит безопасности запускает `base64 -d` и читает `password123` меньше чем за секунду. Репозиторий приватный, но в нём 40 контрибьюторов, три форка и полная git-история, которую не вычистит никакой force-push. «Зашифрованный» секрет никогда не был зашифрован. base64 — это кодирование, а не замок.
</Hook>

<Crux>Секреты входят при деплое/рантайме, а не запекаются в образ — и Kubernetes Secret закодирован в base64, а не зашифрован.</Crux>

Через несколько минут ты увидишь, где именно происходит каждая утечка и что опытный инженер делает иначе на каждом шаге.

<Explanation>

## Главное правило: никогда не запекай секреты в образ

Первый провал случается ещё до деплоя. `Dockerfile` с `ENV API_KEY=sk-live-...` или `COPY .env .` запекает секрет в слой образа. Этот слой адресуется по содержимому, кэшируется и пушится в реестр — а `docker history` плюс `docker save` воспроизведут содержимое каждого слоя. Удаление секрета в более позднем слое его не убирает; ранний слой всё ещё существует и доступен на pull любому с доступом на чтение реестра. То же с build-аргументами: `--build-arg TOKEN=...` оседает в метаданных образа.

Правило, которое сеньор не нарушает: **секреты входят при деплое или рантайме, а не на этапе сборки.** Образ — это почти публичный артефакт, относись к нему так, будто он утечёт. Конфигурация, меняющаяся от окружения к окружению (dev, staging, prod), и всё секретное инъектируется при старте контейнера, снаружи образа. Поэтому же один и тот же образ чисто продвигается из staging в prod: он не несёт окружение внутри себя.

## Ловушка Kubernetes Secret: base64 — это не шифрование

Прежде чем доверять какой-либо защите на уровне кластера, задай себе вопрос: если коллега получит сырой бэкап etcd, что он там увидит?

Kubernetes `Secret` хранит значения, закодированные в base64. Новые инженеры читают непонятную строку и думают, что она защищена. Это не так. base64 — обратимое транспортное кодирование: `echo cGFzcw== | base64 -d` разворачивает его за секунду без всякого ключа. Любой, кто может прочитать объект Secret или получит копию манифеста, читает открытый текст. Модель угроз широкая: утёкший YAML в тикете, бэкап, неправильно настроенная RBAC-роль или git-история GitOps-репозитория.

Хуже того, по умолчанию значение лежит **нешифрованным в etcd**, key-value хранилище кластера. Любой с доступом к etcd — компрометация ноды, украденный бэкап — читает каждый секрет в кластере как открытый текст. Шифрование at rest **по умолчанию выключено**; нужно явно настроить `EncryptionConfiguration`, чтобы API-сервер шифровал перед записью в etcd. И даже тогда есть ловушка: включение не перешифровывает существующие секреты. Надо переписать каждый Secret (например, `kubectl get secrets -A -o json | kubectl replace -f -`) после включения, иначе старые значения остаются открытыми. На проде шифрование подкрепляют KMS-провайдером (AWS KMS, GCP KMS), чтобы ключ жил вне кластера.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Убеждение</th>
        <th class="border border-border px-3 py-2 text-left">Реальность</th>
        <th class="border border-border px-3 py-2 text-left">Последствие</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">«base64 прячет значение»</td>
        <td class="border border-border px-3 py-2">Обратимо за &lt;1с, без ключа</td>
        <td class="border border-border px-3 py-2">Утёкший манифест = утёкший пароль</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">«etcd шифрует мои Secret'ы»</td>
        <td class="border border-border px-3 py-2">По умолчанию открытый текст</td>
        <td class="border border-border px-3 py-2">Кража бэкапа etcd = полный дамп секретов</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">«Я включил шифрование»</td>
        <td class="border border-border px-3 py-2">Старые Secret'ы не перешифрованы</td>
        <td class="border border-border px-3 py-2">Уже существующие значения остаются открытыми</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">«Env-переменные нормально»</td>
        <td class="border border-border px-3 py-2">Утечка через crash dump и <code>/proc</code></td>
        <td class="border border-border px-3 py-2">Секреты индексируются в трекерах ошибок</td>
      </tr>
    </tbody>
  </table>
</div>

## Env-переменные vs файловые маунты: стиль инъекции важен

Когда секрет уже снаружи образа, инъектировать его можно двумя способами, и выбор имеет реальный вес для безопасности. **Переменные окружения** просты — `process.env.DB_PASSWORD` — но они утекают. Их наследует каждый дочерний процесс, который ты порождаешь, их можно прочитать в `/proc/<pid>/environ` всем с нужным доступом, и убийственное: crash dump захватывает всё окружение целиком. Observability-SDK (Sentry, Datadog) рутинно сериализуют состояние процесса при ошибке, так что стектрейс оказывается с твоим живым API-ключом, проиндексированным открытым текстом в стороннем лог-хранилище. Многие фреймворки ещё и печатают окружение на странице фатальной ошибки.

**Файловые маунты** — безопаснее по умолчанию. Секрет ложится файлом (например, `/run/secrets/db-password`, режим `0400`, на tmpfs, который никогда не попадает на диск). Его нет в окружении, его не наследуют дочерние процессы, его нет в `/proc/.../environ`. Решающий операционный выигрыш: **файловые маунты обновляются на месте при ротации секрета**, тогда как env-переменные заморожены на старте контейнера и для нового значения нужен рестарт пода. Для креденшела, который ротируешь каждые 30–90 дней, эта разница — между rolling restart и тихой подменой на месте.

<Inset kind="why" lang="ru">
Env-переменные кажутся безопасными, потому что невидимы в коде. Но «невидимо мне» — не «невидимо атакующему». Ровно те свойства, что делают env-переменные удобными — глобальность для процесса, наследование детьми, дамп при краше — это и есть свойства, которые их сливают. Файл, который ты читаешь один раз и больше не экспортируешь, имеет куда меньший радиус взрыва.
</Inset>

## Реальный инструментарий: тяни из менеджера, шифруй до коммита

Ручное управление Secret'ами не масштабируется. Продакшен-ответ — держать источник истины в выделенном менеджере секретов (HashiCorp Vault, AWS/GCP Secrets Manager) и тянуть из него при деплое/рантайме:

- **External Secrets Operator (ESO)** — контроллер, который следит за внешним менеджером и синхронизирует значения в нативные Kubernetes Secret по `refreshInterval`. Манифесты ссылаются на `ExternalSecret`, а не на сырое значение.
- **Secrets Store CSI Driver** — монтирует секреты из менеджера прямо в под файлами, минуя etcd целиком; значение живёт только в tmpfs пода.
- **Sealed Secrets** — для GitOps, где надо что-то закоммитить: он асимметрично *шифрует* секрет так, что расшифровать может только контроллер внутри кластера. Теперь блоб в git реально безопасно коммитить, в отличие от base64-Secret.
- **Vault dynamic secrets** — Vault генерирует короткоживущий per-app креденшел по запросу (юзер БД, который существует час, потом авто-отзывается). Нечему долгоживущему утекать; украденный креденшел истекает раньше, чем станет полезен.

Вместе эти инструменты смещают поверхность угроз от «надеемся, что никто не прочитает наш YAML» к «утёкшее значение истечёт раньше, чем принесёт пользу». Без хотя бы одного из них ты полагаешься только на контроль доступа — а он ломается в момент компрометации любого аккаунта контрибьютора, CI-джоба или хранилища бэкапов.

<TradeoffMatrix
  id="07-secrets-deploy-tradeoff-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="GitOps-команде надо закоммитить секретный конфиг в приватный репозиторий, чтобы Argo CD его применил. Выбери подход."
  options={[
    { name: "Sealed Secrets: асимметрично зашифровать значение; расшифровать может только контроллер внутри кластера", summary: "Шифруй до коммита.", correct: true, justification: "Это GitOps-нативный ответ. Закоммиченный SealedSecret — это настоящий шифротекст: публичный ключ шифрует, а расшифровывает только приватный ключ контроллера в кластере. Утёкший репозиторий, форк или полная git-история не раскрывают ничего полезного. Не нужен внешний vault, что подходит небольшой команде с минимумом зависимостей. Трейдофф: он статичный — ротация значит пере-sealing и пере-коммит." },
    { name: "Закоммитить обычный Kubernetes Secret с base64-значениями", summary: "base64 его.", justification: "Классическая ловушка. base64 — кодирование, а не шифрование: любой с доступом на чтение репозитория запускает base64 -d и читает открытый текст за секунду. Форки и git-история делают это необратимым, как только утекло. Это ровно тот провал, с которого начинается урок." },
    { name: "External Secrets Operator, тянущий из Vault", summary: "Синк из менеджера.", justification: "Отлично для динамических, внешне управляемых секретов и правильный выбор на масштабе — но добавляет зависимость от внешнего менеджера, которую GitOps-команда из условия не просила, и не кладёт коммитимый артефакт в репозиторий. Сильно, но тяжелее, чем нужно здесь." },
    { name: "Положить значения открытым текстом в ConfigMap", summary: "ConfigMap.", justification: "ConfigMap — для несекретного конфига и по дизайну открытый текст: ни кодирования, ни шифрования, ни разделения доступа от обычного конфига. Коммитить секреты туда строго хуже, чем даже base64-Secret." },
  ]}
/>

<Quiz
  id="07-secrets-deploy-quiz-1"
  lessonSlug="01-overview"
  lang="ru"
  question="В поле data у Kubernetes Secret видно cGFzc3dvcmQ=. Насколько защищено значение?"
  choices={[
    { label: "Не защищено — base64 это обратимое кодирование; base64 -d раскрывает его мгновенно", correct: true },
    { label: "Зашифровано ключом кластера; читать может только API-сервер", misconception: "base64 — это не шифрование, тут вообще нет ключа. Любой, кто может прочитать Secret или его манифест, декодирует его за секунду." },
    { label: "Захешировано, поэтому оригинал не восстановить", misconception: "base64 полностью обратим, это не хеш. Открытый текст восстанавливается тривиально." },
    { label: "Зашифровано at rest в etcd по умолчанию", misconception: "Шифрование at rest по умолчанию выключено; нужно явно настроить EncryptionConfiguration, и даже тогда перешифровать существующие секреты." },
  ]}
/>

<Quiz
  id="07-secrets-deploy-quiz-2"
  lessonSlug="01-overview"
  lang="ru"
  question="Почему файловые маунты секретов часто предпочитают переменным окружения на проде?"
  choices={[
    { label: "Файлы не наследуются дочерними процессами, их нет в /proc/.../environ, и они обновляются на месте при ротации", correct: true },
    { label: "Файлы зашифрованы на диске, а env-переменные открытым текстом", misconception: "Маунты секретов обычно лежат на tmpfs и не «зашифрованы на диске» — выигрыш в уменьшении поверхности утечки и ротации на месте, а не в шифровании диска." },
    { label: "Env-переменные могут держать только короткие строки", misconception: "Дело не в длине; дело в том, что env-переменные утекают через crash dump, дочерние процессы и /proc и заморожены до рестарта." },
    { label: "Файлы загружаются быстрее переменных окружения", misconception: "Производительность не причина — причина в поверхности утечки и поведении ротации." },
  ]}
/>

<DragOrder
  id="07-secrets-deploy-drag-1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь решения по работе с секретами, которые принимает сеньор, от самого безопасного:"
  items={[
    { id: "a", label: "Никогда не запекай секрет в образ — ни ENV, ни COPY .env, ни --build-arg" },
    { id: "b", label: "Держи источник истины в менеджере секретов (Vault / облачный Secrets Manager)" },
    { id: "c", label: "Тяни при деплое/рантайме через ESO или CSI-драйвер (или sealed для GitOps)" },
    { id: "d", label: "Предпочитай файловые маунты env-переменным, чтобы сжать поверхность утечки" },
    { id: "e", label: "Ротируй по расписанию; динамические короткоживущие креды убирают долгоживущие секреты вовсе" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="Поток инъекции секрета: менеджер секретов держит источник истины, значение тянется при деплое и инъектируется в работающий под как файловый маунт или env-переменная — а не запекается в образ"
  hue="var(--d-systems)"
  perRow={4}
  nodes={[
    { id: "manager", label: "Хранилище секретов", sub: "Vault / KMS" },
    { id: "deploy", label: "Pull при деплое", sub: "ESO / CSI driver" },
    { id: "pod", label: "Под", sub: "файл-маунт / env" },
    { id: "image", label: "Образ", sub: "без секрета внутри" },
  ]}
  edges={[
    { from: "manager", to: "deploy" },
    { from: "deploy", to: "pod" },
    { from: "image", to: "pod", label: "только код" },
  ]}
  caption="Источник истины остаётся в менеджере секретов; значение тянется при деплое/рантайме и инъектируется в под файлом или env-переменной. Образ несёт только код — секрет, запечённый в слой, утекает и не ротируется."
/>

<KeyTakeaway>Секреты входят при деплое/рантайме, не в образ. k8s Secret закодирован в base64, не зашифрован, и etcd по умолчанию открытый текст — включи шифрование at rest, предпочитай файловые маунты, тяни из менеджера.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="07-secrets-deploy-retrieval"
  lang="ru"
  questions={[
    { q: "Коллега говорит: «наши Kubernetes Secret безопасны, потому что значения закодированы в base64». Поправь его точно.", a: "base64 — обратимое транспортное кодирование, а не шифрование: ключа нет, и base64 -d восстанавливает открытый текст примерно за секунду. Так что base64-Secret даёт ноль конфиденциальности от любого, кто может прочитать объект Secret или получить копию манифеста: утёкший YAML в тикете, бэкап, неправильно настроенная RBAC-роль или git-история GitOps-репозитория. Вдобавок по умолчанию значение лежит нешифрованным в etcd, так что кража бэкапа etcd или компрометация ноды дампит каждый секрет в кластере открытым текстом. Реальные защиты: включить шифрование at rest через EncryptionConfiguration (в идеале на KMS, чтобы ключ жил вне кластера) — и помнить, что оно не перешифровывает существующие Secret'ы, их надо переписать после включения. Для коммита в git используй Sealed Secrets, который реально шифрует так, что расшифровать может только контроллер внутри кластера." },
    { q: "Почему многие команды инъектируют секреты файловыми маунтами, а не переменными окружения, и что это даёт?", a: "Переменные окружения удобны, но текучи. Они глобальны для процесса и наследуются каждым дочерним процессом, их можно прочитать в /proc/<pid>/environ, а crash dump захватывает всё окружение — так что observability-SDK вроде Sentry или Datadog могут сериализовать живой API-ключ в стороннее лог-хранилище открытым текстом, а многие фреймворки печатают окружение на странице фатальной ошибки. Файловые маунты избегают всего этого: секрет ложится файлом (часто режим 0400 на tmpfs, который никогда не попадает на диск), его нет в окружении, его не наследуют дети, его нет в /proc/.../environ. Решающий операционный плюс — ротация: файловый маунт обновляется на месте при ротации секрета, тогда как env-переменная заморожена на старте контейнера и для нового значения нужен рестарт пода. Для креденшелов с ротацией каждые 30–90 дней это разница между rolling restart и тихой подменой на месте." },
  ]}
/>

<Recap lang="ru">
Безопасно завести секреты в задеплоенное приложение начинается с одного правила: они входят при деплое или рантайме, а не запекаются в образ, потому что слои образа кэшируются, пушатся в реестр и воспроизводятся через docker history — секрет, удалённый в позднем слое, всё ещё живёт в раннем. Ловушка Kubernetes Secret в том, что base64 — это кодирование, а не шифрование: утёкший манифест это утёкший пароль, и по умолчанию значение лежит в etcd открытым текстом, так что надо явно включить шифрование at rest (на KMS) и потом перешифровать существующие Secret'ы. Стиль инъекции тоже важен — переменные окружения утекают через дочерние процессы, /proc и crash dump, которые индексируются трекерами ошибок, тогда как файловые маунты сжимают эту поверхность и, что критично, ротируются на месте без рестарта. На масштабе ответ — реальный инструментарий: держи источник истины в Vault или облачном Secrets Manager, тяни в рантайме через External Secrets Operator или CSI-драйвер, шифруй-до-коммита через Sealed Secrets для GitOps и предпочитай короткоживущие динамические креденшелы, чтобы украденный секрет истёк раньше, чем станет полезен. Теперь, открывая Dockerfile или Kubernetes-манифест на ревью, ты сразу знаешь, какую строку проверять первой — и почему «репозиторий приватный» не является защитой.
</Recap>
