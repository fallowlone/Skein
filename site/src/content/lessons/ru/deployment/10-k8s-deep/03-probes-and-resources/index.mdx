---
concepts:
- readiness-probe
- liveness-probe
- startup-probe
- requests-vs-limits
- oomkilled-vs-throttling
- qos-classes
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: middle
order: 3
prereqs: []
slug: 03-probes-and-resources
sources:
- https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/
- https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
spiral: []
status: ready
summary: 'Readiness гейтит трафик, liveness рестартит, startup щитит медленный старт;
  requests планируют, limits ограничивают — превышение CPU даёт throttling, памяти —
  OOMKilled. Настрой неверно — здоровое приложение уйдёт в CrashLoop или будет вытеснено.'
title: 'Probes и лимиты ресурсов'
track: deployment
unit: 10-k8s-deep
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Сервис отгрузился нормально, а в 3 ночи поды начинают мигать. На дашборде `CrashLoopBackOff` растёт по всем репликам сразу. Никто не пушил код. Причина: `livenessProbe` с `initialDelaySeconds: 5` на приложении, которому нужно 25 секунд, чтобы прогреть JIT и подключиться к базе. Каждый запуск проба падала три раза до того, как приложение было готово, Kubernetes считал контейнер мёртвым и рестартил его — бесконечно. Совершенно здоровое приложение казнила его собственная проверка здоровья. Фикс — четыре строки YAML. Урок занял ночь.
</Hook>

<Crux>Readiness гейтит трафик, liveness рестартит, startup щитит старт; requests планируют, limits — потолок: CPU троттлит, память OOMKilled.</Crux>

<Explanation>

## Три probe, отвечающие на три разных вопроса

Kubernetes не считает запущенный контейнер работающим. Он спрашивает, раз за разом, через probes — и тип probe решает, что произойдёт, когда ответ «нет». Путаница между ними — главный источник самонанесённых аварий в этом уроке.

**Readiness** спрашивает *может ли этот под обслуживать трафик прямо сейчас?* Когда она падает, Kubernetes убирает под из endpoints'ов Service — трафик к нему прекращается, — но контейнер **не** рестартит. Под остаётся живым, просто вне ротации, и возвращается в момент, когда readiness снова проходит. Это гейт для роллаутов (новый под не получает трафик, пока реально не готов) и для временной перегрузки (под под давлением GC может сбросить трафик, не умирая). Сделай readiness отражающей *реальную* готовность: приложение забиндило порт, прогнало миграции и подтвердило, что критичные зависимости достижимы.

**Liveness** спрашивает *жив ли ещё этот контейнер, или он завис?* Когда она падает (за порогом), Kubernetes **рестартит контейнер**. Она существует ради одной задачи: восстановление из неустранимых состояний — дедлок, event loop, который перестал крутиться, утечка памяти, подвесившая процесс. Liveness — это кувалда. Если медленное-но-здоровое приложение или временный сбой зависимости её триггерит, ты получаешь тот самый restart loop из хука.

**Startup** спрашивает *закончил ли этот контейнер загрузку?* Она бежит первой; пока она падает, liveness и readiness probes **полностью придержаны**. Как только startup прошла один раз, она больше не запускается, и две другие берут управление. Это правильный щит для медленно стартующих приложений — старых JVM, всего с долгим прогревом, — чтобы держать liveness агрессивной для *рантайм*-сбоев, не давая ей убивать приложение во время старта.

```yaml
containers:
- name: api
  image: registry.example.com/api:1.4.0
  startupProbe:            # щит старта: до 30 × 10s = 5 мин на оживление
    httpGet: { path: /healthz, port: 8080 }
    periodSeconds: 10
    failureThreshold: 30
  readinessProbe:          # гейт трафика; падение убирает из Service, без рестарта
    httpGet: { path: /ready, port: 8080 }
    periodSeconds: 5
    failureThreshold: 3
  livenessProbe:           # рестарт, если завис; падение убивает + рестартит контейнер
    httpGet: { path: /healthz, port: 8080 }
    periodSeconds: 10
    failureThreshold: 3
```

<Inset kind="why" lang="ru">
Две ловушки probe бьют сеньоров. Первая — **liveness, указывающая на зависимость**: если твой `/healthz` проверяет базу, а у базы 30-секундная икота, liveness *каждого* пода падает разом, и Kubernetes рестартит весь твой флот — превращая короткий сбой зависимости в самонанесённую каскадную аварию. Liveness должна тестировать только сам процесс; пусть readiness отражает зависимости. Вторая — **переиспользование одного эндпоинта для обоих**: если `/healthz` делает тяжёлые проверки зависимостей и ты вешаешь на него liveness, ты тихо поставил liveness в зависимость от внешнего мира. Держи дешёвый локальный liveness-эндпоинт и более богатый readiness.
</Inset>

## requests и limits: планирование против жёсткого потолка

У ресурсов два числа на контейнер, и они делают совершенно разную работу.

**`requests`** — это то, что поду *гарантировано*, и то, что **планировщик** использует для размещения: Kubernetes поставит под только на ноду, у которой свободно как минимум `requests.cpu` и `requests.memory` (суммарно по ноде). Это резервирование, а не потолок. Ставь requests на типичное, устойчивое потребление контейнера.

**`limits`** — это **жёсткий потолок**, который контейнер не может превысить в рантайме, — и то, что происходит при попытке, люди понимают неправильно, потому что CPU и память ведут себя противоположно:

- **CPU сверх лимита → throttling, не убийство.** CPU сжимаемый. Превысишь `limits.cpu` — и CFS-планировщик ядра просто даёт контейнеру меньше тайм-слотов; приложение продолжает работать, но становится медленным. Заниженный CPU-лимит проявляется как загадочная латентность, а не как падения.
- **Память сверх лимита → OOMKilled.** Память несжимаема — нельзя дать процессу «меньше» RAM, чем он уже выделил. Превысишь `limits.memory` — и OOM-киллер ядра завершает контейнер с кодом выхода 137; ты видишь `OOMKilled` и рестарт. Заниженный лимит памяти проявляется как падения, а не как медлительность.

```yaml
resources:
  requests:        # гарантия + планирование — ставь ≈ типичное потребление
    cpu: "250m"
    memory: "256Mi"
  limits:          # жёсткий потолок — защищает ноду от убежавшего контейнера
    cpu: "1"       # сверху → throttling CFS (медленно, не убит)
    memory: "512Mi"  # сверху → OOMKilled (выход 137, контейнер рестартит)
```

Правило сеньора: ставь **requests ≈ типичное**, чтобы планировщик честно паковал ноду, и ставь **limits достаточно высоко, чтобы поглотить всплески, но достаточно низко, чтобы защитить ноду**. Дальше следи за двумя сигналами сбоя — повторяющийся `OOMKilled` означает, что лимит памяти слишком низок (или есть утечка); устойчивый CPU throttling означает, что CPU-лимит слишком низок. Настраивай по реальным метрикам, а не по догадкам.

## QoS-классы решают, кто умрёт первым

Соотношение requests и limits тихо назначает каждому поду **класс качества обслуживания (QoS)**, и этот класс задаёт порядок вытеснения, когда у ноды кончается память:

- **Guaranteed** — у каждого контейнера `requests == limits` и по CPU, и по памяти. Вытесняется последним. Используй для критичных по латентности, stateful-нагрузок.
- **Burstable** — задан хотя бы один request, но не равный limits (частый случай). Вытесняется после BestEffort, в порядке того, насколько он вышел за свои requests.
- **BestEffort** — никаких requests и limits вообще. Первый на вытеснение, первый на OOM-kill под давлением ноды. Годится только для одноразовой batch-работы.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Параметр</th>
        <th class="border border-border px-3 py-2 text-left">requests</th>
        <th class="border border-border px-3 py-2 text-left">limits</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Роль</td>
        <td class="border border-border px-3 py-2">Гарантированное резервирование; драйвит планирование</td>
        <td class="border border-border px-3 py-2">Жёсткий рантайм-потолок на контейнер</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Использует планировщик?</td>
        <td class="border border-border px-3 py-2">Да — под попадёт только на ноду со столькими свободными</td>
        <td class="border border-border px-3 py-2">Нет — применяется в рантайме ядром</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Сверх CPU</td>
        <td class="border border-border px-3 py-2">н/д (request — это пол)</td>
        <td class="border border-border px-3 py-2"><strong>throttling</strong> CFS — приложение тормозит, остаётся живым</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Сверх памяти</td>
        <td class="border border-border px-3 py-2">н/д</td>
        <td class="border border-border px-3 py-2"><strong>OOMKilled</strong> (выход 137) — контейнер рестартит</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Ставь в</td>
        <td class="border border-border px-3 py-2">≈ типичное устойчивое потребление</td>
        <td class="border border-border px-3 py-2">Высоко для всплесков, низко для защиты ноды</td>
      </tr>
    </tbody>
  </table>
</div>

<TradeoffMatrix
  id="03-probes-and-resources-tradeoff-1"
  lessonSlug="03-probes-and-resources"
  lang="ru"
  prompt="Медленно стартующему JVM-сервису (прогрев 25с) нужно восстанавливаться из редких дедлоков, не мигая на каждом рестарте. Как настроить его probes?"
  options={[
    { name: "Добавить startupProbe (failureThreshold × periodSeconds ≥ время старта), затем обычную livenessProbe и readinessProbe", summary: "Защитить старт, потом охранять рантайм.", correct: true, justification: "Это учебниковый фикс. startupProbe придерживает liveness и readiness, пока приложение не закончит 25-секундный прогрев, поэтому медленный старт никогда не триггерит liveness. Как только она прошла один раз, liveness берёт управление, чтобы ловить настоящие дедлоки в рантайме, а readiness гейтит трафик. Ты получаешь восстановление из дедлоков без restart loop на старте — две задачи чисто разделены." },
    { name: "Просто поднять livenessProbe.initialDelaySeconds до 30 и держать остальное агрессивным", summary: "Отложить liveness вместо этого.", justification: "Работает, но это более грубая, хрупкая версия. initialDelaySeconds — фиксированная догадка: слишком мало — и ты возвращаешь crash loop, слишком много — и контейнер, умерший сразу после задержки, лежит мёртвым всё окно до того, как liveness вообще начнёт проверять. startupProbe адаптивна — останавливается, как только приложение поднялось, — и держит liveness отзывчивой после. Для медленных стартов предпочитай startupProbe." },
    { name: "Убрать livenessProbe, чтобы она никогда не могла рестартить приложение", summary: "Вообще без liveness.", justification: "Это меняет один сбой на другой. Да, ты останавливаешь restart loop на старте, но и теряешь единственный механизм, восстанавливающий зависший процесс — дедлок, который тебя просили обработать, теперь висит вечно без авто-восстановления. Правильный ход — корректно ограничить liveness через startupProbe, а не удалять её." },
    { name: "Направить livenessProbe на /healthz, который проверяет соединение с базой", summary: "Liveness проверяет зависимости.", justification: "Активно опасно. Liveness-проба, зависящая от базы, означает, что 30-секундная икота БД роняет liveness всех подов одновременно и рестартит весь флот — каскадная самонанесённая авария. Liveness должна тестировать только процесс; здоровье зависимостей — дело readiness." },
  ]}
/>

<Quiz
  id="03-probes-and-resources-quiz-1"
  lessonSlug="03-probes-and-resources"
  lang="ru"
  question="Контейнер под нагрузкой раз за разом завершается с кодом выхода 137, затем рестартит. О чём это говорит?"
  choices={[
    { label: "Он OOMKilled — превысил лимит памяти, поэтому подними limits.memory по наблюдаемому пику (или чини утечку); память несжимаема, поэтому ядро убивает, а не замедляет", correct: true },
    { label: "CPU-лимит слишком низок, и его троттлит", misconception: "Превышение CPU даёт throttling, а не убийство — контейнер тормозит, но продолжает работать. Код выхода 137 — это SIGKILL от OOM-киллера, симптом лимита памяти, а не CPU." },
    { label: "Падает liveness-проба и рестартит его", misconception: "Рестарт по liveness виден как событие падения пробы, а не как код выхода 137 с причиной OOMKilled. 137 конкретно означает, что OOM-киллер завершил контейнер за превышение лимита памяти." },
    { label: "На ноде кончился диск", misconception: "Давление диска вызывает вытеснение с другой причиной, а не OOMKill с выходом 137. Код 137 — сигнатура лимита памяти: SIGKILL от OOM-киллера ядра." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Liveness-проба, падающая во время медленного старта, убивает контейнер, который рестартит и снова медленно стартует, опять триггеря liveness — бесконечный CrashLoopBackOff на здоровом приложении"
  hue="var(--d-systems)"
  perRow={5}
  nodes={[
    { id: "boot", label: "Под стартует", sub: "прогрев 25с" },
    { id: "probe", label: "Liveness", sub: "задержка 5с" },
    { id: "fail", label: "Падает ×3", sub: "не готов" },
    { id: "kill", label: "Рестарт", sub: "контейнер убит" },
    { id: "loop", label: "CrashLoop", sub: "BackOff" },
  ]}
  edges={[
    { from: "boot", to: "probe", label: "5с спустя" },
    { from: "probe", to: "fail", label: "слишком рано" },
    { from: "fail", to: "kill", label: "признан мёртвым" },
    { from: "kill", to: "loop", label: "повтор" },
  ]}
  caption="Агрессивная liveness на медленно стартующем приложении: проба срабатывает до конца прогрева, падает за порогом и рестартит контейнер — который медленно стартует и снова её триггерит. startupProbe (или initialDelay размером со старт) разрывает цикл, придерживая liveness до конца загрузки."
/>

<KeyTakeaway>Readiness гейтит трафик (без рестарта); liveness рестартит (только дедлоки — не направляй на зависимость); startup щитит старт. requests планируют, limits — потолок: CPU троттлит, память OOMKilled (137).</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-probes-and-resources-retrieval"
  lang="ru"
  questions={[
    { q: "Здоровое приложение застряло в CrashLoopBackOff сразу после деплоя. Пройдись по misconfiguration probe, который это вызывает, и по точному фиксу.", a: "Классическая причина — слишком агрессивная livenessProbe на медленно стартующем приложении. Liveness спрашивает «жив ли контейнер?» и при падении рестартит контейнер. Если приложению нужно, скажем, 25 секунд на прогрев (JIT, пулы соединений, миграции), но у liveness-пробы короткий initialDelaySeconds и низкий failureThreshold, проба срабатывает, пока приложение ещё стартует, падает за порогом, и Kubernetes признаёт контейнер мёртвым и рестартит его. Рестартнутый контейнер снова медленно стартует, проба снова падает — и ты получаешь бесконечный CrashLoopBackOff на совершенно здоровом приложении, казнённом его же проверкой здоровья. Фикс — не дать liveness работать во время старта. Добавь startupProbe, у которого failureThreshold × periodSeconds комфортно превышает реальное время старта (например, 30 × 10с = 5 минут); пока она падает, и liveness, и readiness полностью придержаны. Как только startup прошла один раз, она больше не запускается, и liveness берёт управление, чтобы ловить настоящие рантайм-дедлоки. Более грубая альтернатива — поднять liveness initialDelaySeconds под время старта, но startupProbe предпочтительнее, потому что останавливается сразу, как приложение поднялось, а не ждёт фиксированную догадку." },
    { q: "В чём разница между OOMKilled и throttling контейнера и что каждое говорит изменить?", a: "Это два поведения применения лимитов ресурсов, и они различаются потому, что память несжимаема, а CPU сжимаем. Когда контейнер превышает лимит памяти, OOM-киллер ядра завершает его с кодом выхода 137 — ты видишь статус OOMKilled, и контейнер рестартит. Нельзя дать процессу «меньше» RAM, чем он уже выделил, поэтому единственный ответ — убить его. Это говорит, что лимит памяти задан слишком низко для реального следа нагрузки, или есть утечка памяти — либо подними limits.memory по наблюдаемому потреблению, либо чини утечку. Когда контейнер превышает CPU-лимит, ничто не убивается: CFS-планировщик ядра просто выдаёт ему меньше тайм-слотов, и приложение продолжает работать, но становится медленнее. Это проявляется как загадочная латентность, а не падения, и говорит, что CPU-лимит слишком низок для нагрузки — подними limits.cpu (и проверь, что request отражает типичное потребление, чтобы планировщик поставил под туда, где есть запас). Правило большого пальца: повторяющийся OOMKilled — заниженная память; устойчивый throttling — заниженный CPU. Всегда настраивай по реальным метрикам, а не по догадкам." },
  ]}
/>

<Recap lang="ru">
Kubernetes держит поды здоровыми и правильно размеренными двумя независимыми инструментами, и сбои идут от путаницы случаев. Есть три probes, отвечающие на три вопроса: readiness («может ли обслуживать?» — падение убирает под из endpoints'ов Service, но не рестартит, гейт для роллаутов и сброса временной нагрузки), liveness («завис ли он?» — падение рестартит контейнер, только для восстановления из дедлоков) и startup («загрузился ли он?» — бежит первой и придерживает liveness и readiness до конца старта, щит для медленно стартующих приложений). Две ловушки, кладущие здоровые флоты, — это слишком агрессивная liveness-проба, гоняющая в CrashLoop медленное-но-здоровое приложение, и liveness, направленная на зависимость, которая рестартит все поды разом при кратком сбое зависимости — держи liveness дешёвой и локальной, пусть readiness отражает зависимости. По ресурсам: requests — это гарантированное резервирование, которое планировщик использует для размещения (ставь их около типичного потребления), а limits — жёсткий рантайм-потолок, но два ресурса применяются противоположно: превысишь CPU-лимит — тебя троттлит CFS (медленно, не убит), превысишь лимит памяти — тебя OOMKilled с кодом выхода 137 (убит и рестартнут). Соотношение requests к limits назначает QoS-класс — Guaranteed (requests == limits, вытесняется последним), Burstable или BestEffort (без limits, вытесняется первым), — который задаёт порядок гибели подов под давлением ноды. Следи за повторяющимися OOMKill (лимит памяти слишком низок) и устойчивым throttling (CPU-лимит слишком низок) и настраивай по метрикам, а не по догадкам.
</Recap>
