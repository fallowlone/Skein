---
concepts:
- config-from-image-separation
- configmap-env-vs-volume
- secret-base64-not-encryption
- encryption-at-rest-rbac
- immutable-and-rotation
- external-secret-managers
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-config-and-secrets
sources:
- https://kubernetes.io/docs/concepts/configuration/configmap/
- https://kubernetes.io/docs/concepts/configuration/secret/
spiral: []
status: ready
summary: 'Внедряй конфиг и секреты в рантайме, не запекай их в образ. ConfigMap и Secret
  питают env-переменные или примонтированные файлы — но base64 это кодирование, не
  шифрование, поэтому включай encryption-at-rest и RBAC.'
title: 'ConfigMap и секреты'
track: deployment
unit: 10-k8s-deep
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Staging-сборку промоутят в продакшен, и она тут же начинает писать в staging-базу. Образ собрали с `DATABASE_URL=postgres://staging...`, запечённым в строку `ENV`, поэтому «промоут образа» молча протащил staging-конфиг вместе с ним. Хуже того, `git log` показывает, что в прошлом релизе так же был вшит API-токен — он теперь в каждом реестре, в каждом `docker pull` разработчика и в истории слоёв навсегда. У обоих багов одна корневая причина: конфигурация, принадлежащая окружению, была заморожена в артефакте.
</Hook>

<Crux>Внедряй конфиг в рантайме через ConfigMap и Secret — не запекай его в образ. Base64 это кодирование, не шифрование.</Crux>

<Explanation>

## Один образ, много окружений: разделение по 12-factor

Спроси себя: если образ собирается в CI и затем промоутится в продакшен, что меняется между окружениями? Всё, что меняется, принадлежит *снаружи* образа — а не заморожено внутри него.

Образ контейнера задуман так, чтобы собираться один раз и запускаться где угодно — dev, staging, prod — одинаково. Как только ты запекаешь в него значение, специфичное для окружения (URL базы, фича-флаг, API-ключ), ты ломаешь это обещание: один образ больше не может работать в двух окружениях, а всё секретное, что ты заморозил, теперь навсегда в истории слоёв. Правило 12-factor — держать конфиг в окружении, а не в коде, и внедрять его в рантайме.

Kubernetes даёт для этого два объекта. **ConfigMap** хранит несекретную конфигурацию в формате ключ/значение — «API-объект для хранения неконфиденциальных данных в парах ключ-значение». **Secret** хранит те же формы для чувствительных данных. Оба развязаны с Pod: можно менять конфиг без пересборки образа и запускать один и тот же образ в staging и prod, направив его на разные ConfigMap и Secret.

```yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  LOG_LEVEL: "info"
  FEATURE_NEW_CHECKOUT: "true"
  app.properties: |
    cache.ttl=300
    cache.size=2000
---
apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
type: Opaque
data:
  # base64 от "postgres://app:s3cr3t@db:5432/app" — кодирование, НЕ шифрование
  DATABASE_URL: cG9zdGdyZXM6Ly9hcHA6czNjcjN0QGRiOjU0MzIvYXBw
```

## Два способа доставки: env-переменные vs примонтированные файлы

Pod потребляет ConfigMap или Secret в одной из двух форм, и выбор имеет последствие в рантайме, которое большинство узнаёт на собственных шишках. Можно проецировать значения как **env-переменные** (`envFrom` для всей карты, или `valueFrom.configMapKeyRef` / `secretKeyRef` для одного ключа), либо монтировать их как **файлы в volume**, где каждый ключ становится файлом. Env-переменные удобны для плоских скаляров; volume — правильный выбор для целых файлов конфигурации (TLS-сертификат, `application.yaml`) и для всего, что хочешь обновлять без редеплоя.

Последствие: **примонтированные файлы обновляются вживую; env-переменные — нет.** Когда ConfigMap, потребляемый в volume, обновляется, «проецируемые ключи в итоге тоже обновляются» — kubelet освежает их на своей периодической синхронизации. Но «ConfigMap, потребляемые как env-переменные, не обновляются автоматически и требуют рестарта пода». То же разделение справедливо для Secret. Поэтому если ты меняешь значение, которое приложение читает из env-переменной, ничего не происходит до рестарта Pod; меняешь значение, примонтированное как файл, — файл обновляется на месте, хотя приложению всё равно надо перечитать его.

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app
spec:
  replicas: 3
  selector:
    matchLabels: { app: app }
  template:
    metadata:
      labels: { app: app }
    spec:
      containers:
        - name: app
          image: registry.example.com/app:1.4.2   # один образ, каждое окружение
          envFrom:
            - configMapRef:
                name: app-config        # весь ConfigMap как env-переменные
          env:
            - name: DATABASE_URL        # один ключ секрета как env-переменная
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: DATABASE_URL
          volumeMounts:
            - name: config-files
              mountPath: /etc/app        # app.properties появляется как файл; обновляется вживую
              readOnly: true
      volumes:
        - name: config-files
          configMap:
            name: app-config
```

<Inset kind="why" lang="ru">
«Обновляется вживую» — не вся история. Kubelet освежает примонтированные volume только на своей периодической синхронизации, так что задержка может быть «вплоть до периода синхронизации kubelet + задержки распространения кэша» — секунды до минуты, не мгновенно. И смена файла на диске ничего не даёт, пока приложение не *следит* за файлом и не перечитывает его; большинство приложений читают конфиг один раз на старте. Так что монтирование как volume необходимо, но недостаточно для живой ротации — приложение должно участвовать.
</Inset>

## Сеньорская оговорка: base64 это не безопасность

Это провал, на котором команды получают breach. В поле `data` секрета значения **закодированы в base64, а не зашифрованы** — а base64 тривиально обратим (`echo ... | base64 -d`). По умолчанию «секреты Kubernetes по умолчанию хранятся незашифрованными в нижележащем хранилище API-сервера (etcd)». Так что объект Secret сам по себе не секретен: «любой с доступом к API может получить или изменить Secret, как и любой с доступом к etcd». Secret отличается от ConfigMap в основном намерением и инструментарием (он типизирован, держится вне некоторых логов, может быть ограничен RBAC) — но не какой-либо криптографией.

Чтобы Secret стали реально безопасны, Kubernetes говорит «предпринять как минимум следующие шаги»: **включить Encryption at Rest**, чтобы байты в etcd были зашифрованы, и **настроить RBAC с наименьшими привилегиями**, чтобы только те рабочие нагрузки и люди, которым нужен Secret, могли сделать `get`. Сверху — ограничение доступа к Secret конкретными контейнерами, а для настоящего управления секретами тянись к **внешним хранилищам секретов** — HashiCorp Vault, External Secrets Operator или облачному secret-менеджеру (AWS/GCP/Azure) — которые держат источник истины вне кластера, поддерживают аудит и ротацию и синхронизируют в Kubernetes по запросу. Кардинальный грех, которого избегать: считать base64-манифест Secret безопасным и коммитить его в git. base64 в репозитории — это plaintext для любого, кто его клонирует.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Свойство</th>
        <th class="border border-border px-3 py-2 text-left">ConfigMap</th>
        <th class="border border-border px-3 py-2 text-left">Secret</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Назначение</td>
        <td class="border border-border px-3 py-2">Неконфиденциальный конфиг</td>
        <td class="border border-border px-3 py-2">Чувствительные данные (токены, пароли, ключи)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Кодирование на проводе</td>
        <td class="border border-border px-3 py-2">Чистый UTF-8 в <code>data</code></td>
        <td class="border border-border px-3 py-2">base64 в <code>data</code> (кодирование, не шифрование)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Зашифровано в etcd at rest?</td>
        <td class="border border-border px-3 py-2">Нет</td>
        <td class="border border-border px-3 py-2">Не по умолчанию — нужно включить encryption-at-rest</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Кто может прочитать</td>
        <td class="border border-border px-3 py-2">Любой с доступом к API/etcd</td>
        <td class="border border-border px-3 py-2">Любой с RBAC <code>get secret</code> — поэтому ограничивай жёстко</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Потребляется как</td>
        <td class="border border-border px-3 py-2">env-переменные или файлы volume</td>
        <td class="border border-border px-3 py-2">env-переменные или файлы volume (те же две формы)</td>
      </tr>
    </tbody>
  </table>
</div>

## Иммутабельность и ротация

Когда ты меняешь значение конфига в проде, что должно происходить с уже запущенными pod-ами? Ответ зависит от того, за какой из двух рычагов ты берёшься — и у каждого свой сценарий отказа при неправильном применении.

Два операционных рычага завершают картину. Помечай ConfigMap или Secret **immutable** (добавь верхнеуровневое `immutable: true`), когда его значение не должно меняться на месте. Это даёт два эффекта: защиту от случайных правок, которые молча сломали бы работающие Pod, и выигрыш в производительности — kubelet перестаёт следить за иммутабельными объектами, что заметно снижает нагрузку на API-сервер в больших кластерах. Трейдофф в том, что иммутабельный объект нельзя редактировать; чтобы изменить его, создаёшь новый (часто с суффиксом-хэшем) и катишь Deployment, чтобы он указывал на него.

Это естественно ведёт к **ротации**. Поскольку значения env-переменных заморожены на старте Pod, ротация Secret, потребляемого как env-переменная, ничего не делает, пока ты не перезапустишь Pod — и осознанный `kubectl rollout restart` честный способ это применить. Secret, примонтированный как файл, *может* ротироваться без рестарта, но только если приложение следит за файлом и перезагружается. Поэтому многие команды стандартизируются либо на «монтирование + приложение следит» для настоящей ротации без даунтайма, либо на «immutable + имена с хэш-суффиксом + rollout», чтобы смена конфига была обычным, аудируемым, откатываемым деплоем.

<FlowDiagram
  label="ConfigMap и Secret питают Pod либо как env-переменные, либо как файлы в примонтированном volume"
  hue="var(--d-systems)"
  perRow="3"
  nodes={[
    { id: "cm", label: "ConfigMap", sub: "несекрет kv" },
    { id: "pod", label: "Pod", sub: "контейнер" },
    { id: "files", label: "Файлы volume", sub: "вживую" },
    { id: "sec", label: "Secret", sub: "base64 kv" },
    { id: "env", label: "Env-перем.", sub: "заморож. на старте" },
  ]}
  edges={[
    { from: "cm", to: "env", label: "envFrom" },
    { from: "cm", to: "files", label: "volume" },
    { from: "sec", to: "env", label: "secretKeyRef" },
    { from: "sec", to: "files", label: "volume" },
    { from: "env", to: "pod", label: "рестарт для смены" },
    { from: "files", to: "pod", label: "вживую, app перечит." },
  ]}
  caption="Оба объекта дают те же две формы. Форма решает поведение ротации: env-переменным нужен рестарт Pod; примонтированные файлы обновляются на синхронизации kubelet, но вступают в силу, только если приложение их перечитает."
/>

<TradeoffMatrix
  id="02-config-and-secrets-tradeoff-1"
  lessonSlug="02-config-and-secrets"
  lang="ru"
  prompt="Прод-приложение должно ротировать пароль базы без даунтайма. Как доставить credential в Pod?"
  options={[
    { name: "Secret, примонтированный как файл volume, с приложением, следящим за файлом и перезагружающим пул соединений при изменении", summary: "Живая ротация.", correct: true, justification: "Примонтированные файлы Secret в итоге освежаются kubelet на его периодической синхронизации, так что новый пароль ложится на диск без рестарта. Подвох в том, что смена файла ничего не даёт, пока приложение его не перечитает, поэтому требование без даунтайма вынуждает приложение следить за файлом и пересобирать пул. Эта комбинация — volume-монтирование плюс участвующее приложение — единственный путь к ротации без рестарта, что и требуется." },
    { name: "Secret как env-переменная через secretKeyRef", summary: "Заморожен на старте.", justification: "Env-переменные читаются один раз на старте контейнера и не обновляются на месте — «потребляемые как env-переменные не обновляются автоматически и требуют рестарта пода». Ротация Secret оставляет каждый работающий Pod на старом пароле, пока ты не перезапустишь их, так что сам по себе это не без даунтайма; нужен rollout, что противоречит требованию." },
    { name: "Запечь пароль в образ строкой ENV и пересобирать при ротации", summary: "В артефакте.", justification: "Это тот самый антипаттерн, ради убийства которого существует весь урок. Credential оказывается навсегда в истории слоёв и реестре, один образ не может работать в разных окружениях, а ротация означает полную пересборку-и-редеплой — медленно, протекает и противоположно нулевому даунтайму." },
    { name: "Закоммитить base64-манифест Secret в git и применять при каждой ротации", summary: "base64 в репозитории.", justification: "base64 это кодирование, не шифрование, так что коммит манифеста публикует plaintext-пароль всем, кто может клонировать репозиторий, и в его истории навсегда. Доставка всё равно через env или файл с теми же трейдофами ротации. Настоящей ротации место во внешнем secret-менеджере, не в git." },
  ]}
/>

<Quiz
  id="02-config-and-secrets-quiz-1"
  lessonSlug="02-config-and-secrets"
  lang="ru"
  question="Коллега говорит, что Secret безопасен, потому что значения закодированы в base64. Какая корректная поправка?"
  choices={[
    { label: "base64 это обратимое кодирование, не шифрование — Secret по умолчанию незашифрованы в etcd, и любой с RBAC get-secret или доступом к etcd может их декодировать; включи encryption-at-rest и RBAC с наименьшими привилегиями", correct: true },
    { label: "base64 — стойкий шифр, так что норм, пока кластер приватный", misconception: "base64 — транспортное кодирование без ключа и без секретности; echo … | base64 -d обращает его мгновенно. Приватная сеть не меняет того, что значение — plaintext для любого, кто может прочитать объект." },
    { label: "Secret автоматически шифруются at rest, так что больше ничего не нужно", misconception: "По умолчанию секреты Kubernetes хранятся незашифрованными в etcd. Encryption at rest — это опт-ин, который надо настроить; из коробки он не включён." },
    { label: "Только kube-apiserver может читать Secret, так что RBAC ни при чём", misconception: "Любой субъект с get на Secret может получить и декодировать его, как и любой с доступом к etcd. RBAC с наименьшими привилегиями — это ровно то, как ограничить, кто это." },
  ]}
/>

<Quiz
  id="02-config-and-secrets-quiz-2"
  lessonSlug="02-config-and-secrets"
  lang="ru"
  question="Ты правишь значение ConfigMap, которое приложение читает из env-переменной. Работающие Pod не подхватывают изменение. Почему?"
  choices={[
    { label: "Значения env-переменных инжектятся один раз на старте контейнера и не обновляются автоматически — нужен рестарт Pod; монтирование ключа как файла volume — это форма, обновляющаяся вживую", correct: true },
    { label: "Правка ConfigMap не сохранилась; env-переменные всегда отражают свежее значение", misconception: "Объект обновился нормально. Env-переменные просто не перечитываются после старта: «потребляемые как env-переменные не обновляются автоматически и требуют рестарта пода»." },
    { label: "ConfigMap нельзя менять после создания", misconception: "Обычный ConfigMap изменяем; его можно свободно править. (Явно иммутабельный нельзя, но это опт-ин.) Проблема здесь в форме потребления через env, а не в изменяемости." },
    { label: "Нужно удалить и пересоздать ConfigMap, чтобы любое изменение применилось", misconception: "Правка на месте работает; потребители через файлы даже подхватывают её на следующей синхронизации kubelet. Только потребителям через env нужен рестарт Pod." },
  ]}
/>

</Explanation>

<KeyTakeaway>Внедряй конфиг в рантайме как ConfigMap и Secret, не запекай в образ. Оба питают env (нужен рестарт) или файлы volume (вживую). base64 это кодирование, не шифрование: включи encryption-at-rest, RBAC, не коммить в git.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-config-and-secrets-retrieval"
  lang="ru"
  questions={[
    { q: "Почему base64 в Kubernetes Secret — это не безопасность, и что реально надо сделать, чтобы защитить Secret?", a: "base64 — обратимое транспортное кодирование без ключа — `echo <значение> | base64 -d` декодирует его мгновенно — так что значения в поле data секрета фактически plaintext для любого, кто может прочитать объект. По умолчанию секреты Kubernetes хранятся незашифрованными в etcd, и любой с доступом к API (get на Secret через RBAC) или прямым доступом к etcd может их получить и декодировать. Secret отличается от ConfigMap намерением и инструментарием, не криптографией. Чтобы реально защитить, предпринимают как минимум: включить Encryption at Rest, чтобы байты в etcd были зашифрованы; настроить RBAC с наименьшими привилегиями, чтобы только нужные рабочие нагрузки и люди могли сделать get; ограничить доступ к Secret конкретными контейнерами; а для настоящего управления секретами использовать внешнее хранилище вроде Vault, External Secrets Operator или облачного secret-менеджера, который держит источник истины вне кластера с аудитом и ротацией. Классический breach — коммит base64-манифеста Secret в git: это просто публикация plaintext всем, кто может клонировать репозиторий." },
    { q: "Когда смена значения ConfigMap или Secret реально вступает в силу в работающем Pod, и как форма потребления это решает?", a: "Это полностью зависит от того, потребляется ли значение как env-переменная или как примонтированный файл. Env-переменные (envFrom, valueFrom.configMapKeyRef / secretKeyRef) инжектятся один раз при старте контейнера и не обновляются на месте — они не обновляются автоматически и требуют рестарта Pod, так что осознанный kubectl rollout restart — это как применить новое значение. Значения, примонтированные как файлы в volume, обновляются вживую: когда нижележащий ConfigMap или Secret меняется, kubelet в итоге освежает проецируемые файлы на своей периодической синхронизации, с задержкой вплоть до периода синхронизации плюс задержки распространения кэша. Но смена файла на диске важна, только если приложение следит за файлом и перезагружается — большинство приложений читают конфиг один раз на старте, так что volume-монтирование необходимо, но недостаточно для живой ротации; приложение должно участвовать. Поэтому ротации без даунтайма нужно «монтирование как файл + приложение перечитывает», тогда как иммутабельные ConfigMap/Secret вынуждают обратный паттерн: создать новый объект с хэш-суффиксом и катнуть Deployment на него." },
  ]}
/>

<Recap lang="ru">
Конфигурацию, принадлежащую окружению, надо внедрять в рантайме, а не запекать в образ — запечёшь, и тот же артефакт уже не сможет перемещаться между окружениями, а любой секрет, что ты заморозил, живёт в истории слоёв навсегда. Теперь, когда встретишь `secretKeyRef`, ссылающийся на Secret, задай себе вопрос: зашифрован ли этот Secret at rest (зашифровано ли содержимое в хранилище etcd), ограничен ли доступ к нему RBAC с наименьшими привилегиями и хранится ли источник истины вне git? Если нет — у тебя строка в base64 с ложным ощущением безопасности. Kubernetes даёт ConfigMap для неконфиденциального конфига ключ/значение и Secret для чувствительных данных, оба развязаны с Pod, так что один образ работает везде, указывая на разные объекты. Каждый потребляется в одной из двух форм — env-переменные или файлы в примонтированном volume — и форма определяет ротацию: env-переменные заморожены на старте контейнера и требуют рестарта Pod, тогда как примонтированные файлы освежаются на синхронизации kubelet, но вступают в силу, только если приложение их перечитает. Сеньорская ловушка в том, что base64 в Secret — это кодирование, не шифрование: Secret по умолчанию лежат незашифрованными в etcd, и любой с RBAC get-secret или доступом к etcd может их декодировать, поэтому надо включить encryption-at-rest, ограничить доступ RBAC с наименьшими привилегиями, никогда не коммитить манифест Secret в git и тянуться к внешнему secret-менеджеру для всего, что реально важно. Наконец, помечай конфиг иммутабельным, чтобы предотвратить случайные правки и срезать нагрузку слежения kubelet, принимая, что ротация тогда означает новый объект с хэш-именем плюс rollout.
</Recap>
