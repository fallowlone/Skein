---
concepts:
- helm-package-manager
- chart-structure
- values-override
- releases-and-rollback
- chart-repositories-and-subcharts
- diff-and-dry-run
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: middle
order: 4
prereqs: []
slug: 04-helm-intro
sources:
- https://helm.sh/docs/topics/charts/
- https://helm.sh/docs/chart_template_guide/values_files/
spiral: []
status: ready
summary: 'Helm — пакетный менеджер Kubernetes. Chart шаблонизирует манифесты плейсхолдерами
  Go-template; values переопределяют их под окружение; а install/upgrade/rollback ведут
  именованный версионированный release, которого голый kubectl apply не даёт.'
title: 'Helm: чарты и values'
track: deployment
unit: 10-k8s-deep
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
У тебя один сервис в трёх окружениях. Dev хочет 1 реплику и образ `:dev`; staging — 2 и `:rc`; prod — 6, другой бюджет ресурсов и запиненный тег `:1.8.3`. Всё остальное в манифесте идентично. Поэтому команда копирует `deployment.yaml` в три папки, и теперь изменение readiness-пробы надо вносить руками в трёх местах — и кто-то всегда один пропускает. Хуже: когда прод-раскатка падает в 2 ночи, у `kubectl apply` нет «отмены». Нет записи, каким был предыдущий рабочий манифест, нет одной команды, чтобы его вернуть. Ты диффишь YAML против истории своего шелла.
</Hook>

<Crux>Helm шаблонизирует манифесты из values под окружение и ведёт каждый деплой как именованный версионированный release с upgrade и rollback.</Crux>

<Explanation>

## Проблема: голый YAML дублируется, а у `kubectl apply` нет отмены

У обычного YAML Kubernetes две структурные дыры, как только ты гоняешь одно приложение больше чем в одном месте. Первая — **дублирование**: окружения отличаются горсткой значений — число реплик, тег образа, лимиты ресурсов, hostname, — но у YAML нет параметров, поэтому ты копируешь весь набор манифестов на окружение и держишь их в синхроне руками. Дрейф неизбежен. Вторая — **нет единицы релиза**: `kubectl apply -f` отправляет текущие файлы в кластер и забывает. Он не записывает «это версия 4 my-api», не умеет связать Deployment + Service + ConfigMap + Ingress в один атомарный объект и не знает понятия отката к предыдущей версии. Хочешь отменить — реконструируй старый YAML сам.

Helm — де-факто **пакетный менеджер Kubernetes** — закрывает обе дыры. Он вводит единицу упаковки (chart), механизм параметризации (values) и stateful версионированную единицу деплоя (release).

## Chart — это пакет: templates + значения по умолчанию + метаданные

**Chart** — это директория (или упакованный `.tgz`) с фиксированной раскладкой. Три части, важные с первого дня:

```text
my-api/
  Chart.yaml          # метаданные: name, version, appVersion, dependencies
  values.yaml         # значения по умолчанию (публичный API чарта)
  templates/
    deployment.yaml   # манифесты K8s, но с плейсхолдерами Go-template
    service.yaml
    _helpers.tpl      # переиспользуемые сниппеты шаблонов
```

`templates/` держит обычные манифесты Kubernetes с **плейсхолдерами Go-template**, вбитыми в места, которые меняются. Helm рендерит эти шаблоны против дерева values, чтобы получить финальный плоский YAML, и шлёт его в кластер.

```yaml
# templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-api
spec:
  replicas: {{ .Values.replicaCount }}
  template:
    spec:
      containers:
        - name: api
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          resources:
            limits:
              memory: {{ .Values.resources.limits.memory }}
```

`values.yaml` поставляет **значения по умолчанию** — и заодно служит документированным публичным интерфейсом чарта:

```yaml
# values.yaml (значения по умолчанию)
replicaCount: 1
image:
  repository: registry.example.com/my-api
  tag: dev
resources:
  limits:
    memory: 256Mi
```

## Values переопределяют шаблоны под окружение

Один и тот же chart рендерит разные манифесты, потому что ты кормишь его разными **values**. Держишь по одному небольшому файлу на окружение и передаёшь его при install/upgrade через `-f` или переопределяешь отдельные ключи через `--set`. Приоритет слоистый: `values.yaml` чарта — база, каждый файл `-f` переопределяет её слева направо, а флаги `--set` бьют всё.

```yaml
# values-prod.yaml — только то, что отличается от значений по умолчанию
replicaCount: 6
image:
  tag: "1.8.3"
resources:
  limits:
    memory: 1Gi
```

```bash
# один chart, три окружения — единственная разница в values
helm upgrade --install my-api ./my-api -f values-prod.yaml
helm upgrade --install my-api ./my-api -f values-staging.yaml --set image.tag=rc-42
```

Это несущая идея: **values — это граница окружения.** Шаблон кодирует *форму* твоего деплоя один раз; values несут *факты*, специфичные для окружения. Определение readiness-пробы ровно одно на сопровождение, и dev/staging/prod больше не могут разъехаться в частях, которые должны быть идентичны.

<Inset kind="why" lang="ru">
Держи файлы values под окружение тонкими — только ключи, которые реально отличаются от `values.yaml`. Если ты копируешь весь файл значений по умолчанию на окружение, ты только что заново изобрёл проблему дублирования, ради решения которой Helm и существует, — просто теперь она спрятана на уровень ниже. Дисциплина такая: `values.yaml` чарта — единственный источник формы и разумных значений по умолчанию; каждый файл values под окружение — короткий diff против него.
</Inset>

## Releases: install, upgrade, rollback как одна версионированная единица

Когда ты `helm install` (или `helm upgrade --install`) chart, Helm создаёт именованный **release** и записывает пронумерованную **ревизию** ровно того, что задеплоил, храня её как Secret в кластере. Каждый `helm upgrade` поднимает номер ревизии. Поскольку Helm помнит каждую ревизию, `helm rollback my-api 4` — это одна команда, которая заново применяет известно-хорошее прошлое состояние, — «отмена», которой у `kubectl apply` никогда не было. `helm uninstall` удаляет ресурсы всего release разом. Это превращает деплой из «кучи YAML, которую я впихнул в API-сервер» в атомарную, версионированную, обратимую транзакцию над именованным набором.

```bash
helm upgrade --install my-api ./my-api -f values-prod.yaml  # -> ревизия 5
helm history my-api                                         # все ревизии
helm rollback my-api 4                                      # атомарная отмена к рабочему состоянию
```

<FlowDiagram
  label="Пайплайн рендера и release Helm: шаблоны чарта плюс values рендерятся в плоские манифесты, применяются к кластеру и записываются как пронумерованная ревизия release"
  hue="var(--d-systems)"
  perRow={5}
  nodes={[
    { id: "chart", label: "Chart", sub: "templates/" },
    { id: "values", label: "Values", sub: "-f / --set" },
    { id: "render", label: "Рендер", sub: "Go-template" },
    { id: "manifests", label: "Манифесты", sub: "плоский YAML" },
    { id: "release", label: "Release", sub: "рев N" },
  ]}
  edges={[
    { from: "chart", to: "render", label: "шаблоны" },
    { from: "values", to: "render", label: "заполнить" },
    { from: "render", to: "manifests", label: "вывод" },
    { from: "manifests", to: "release", label: "apply + запись" },
  ]}
  caption="Chart + values рендерятся в плоские манифесты, которые Helm применяет и записывает как пронумерованную обратимую ревизию. Подмени файл values — и тот же chart отрендерит другое окружение; именно история ревизий делает возможным helm rollback."
/>

## kubectl apply vs Helm, кратко

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Возможность</th>
        <th class="border border-border px-3 py-2 text-left">Голый <code>kubectl apply -f</code></th>
        <th class="border border-border px-3 py-2 text-left">Helm</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Шаблонизация / параметры</td>
        <td class="border border-border px-3 py-2">Нет — статический YAML, копия на окружение</td>
        <td class="border border-border px-3 py-2">Плейсхолдеры Go-template, заполняемые из values</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Много окружений</td>
        <td class="border border-border px-3 py-2">Дубли манифестов, ручной синхрон, дрейф</td>
        <td class="border border-border px-3 py-2">Один chart, тонкий файл values на окружение</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Release / rollback</td>
        <td class="border border-border px-3 py-2">Нет единицы release, нет встроенной отмены</td>
        <td class="border border-border px-3 py-2">Именованный release, пронумерованные ревизии, <code>helm rollback</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Упаковка / шаринг</td>
        <td class="border border-border px-3 py-2">Разрозненные файлы в репозитории</td>
        <td class="border border-border px-3 py-2">Chart <code>.tgz</code>, версионированный, в chart-репозитории</td>
      </tr>
    </tbody>
  </table>
</div>

## Шаринг и переиспользование: репозитории и subcharts

Чарты — распространяемые артефакты. Публичные **chart-репозитории** — самый известный Bitnami — публикуют production-grade чарты для готовой инфраструктуры (Postgres, Redis, nginx), так что тебе не надо писать руками stateful-set под базу. Можно и композировать: chart может объявить **зависимости** (subcharts) в `Chart.yaml`, а values родителя могут переопределить values сабчарта, так что зонтичный chart связывает твоё приложение с зависимостями как один release.

<Inset kind="warn" lang="ru">
Флаг Helm `--reuse-values` переносит values предыдущего release во время upgrade, что звучит удобно, но молча маскирует изменения, которые ты внёс в `values.yaml` и в значения по умолчанию новой версии чарта. Лучше передавать свои файлы `-f` явно на каждый upgrade, чтобы отрендеренный вывод был чистой функцией от chart + видимых тебе values, а не от накопленного скрытого состояния прошлых ревизий.
</Inset>

## Заметки сеньора: никогда не апгрейдь вслепую

Две практики отделяют безопасный Helm-воркфлоу от опасного. **Предпросмотр до применения.** `helm upgrade --dry-run` рендерит манифесты, не трогая кластер; широко используемый плагин `helm diff upgrade` показывает точное изменение против *живого* release, так что ты видишь «это удалит PVC» до того, как это случится, а не после. **Версионируй всё.** Chart несёт две версии в `Chart.yaml`: `version` (собственная версия чарта, поднимай при любом изменении чарта) и `appVersion` (приложение, которое он поставляет). Относись к чартам как к коду — пинни версию чарта, которую используют твои окружения, и поднимай её осознанно. И помни про **ловушку upgrade-vs-recreate**: правка неизменяемого поля (spec у Job, `clusterIP` у Service, некоторые поля volume) не патчится на месте; upgrade падает или, в зависимости от ресурса, форсит пересоздание, которое может означать даунтайм. Читай diff, знай, какие поля неизменяемы, и планируй раскатку — `helm rollback` это твоя страховка, а не стратегия.

<TradeoffMatrix
  id="04-helm-intro-tradeoff-1"
  lessonSlug="04-helm-intro"
  lang="ru"
  prompt="Ты гоняешь один сервис в dev, staging и prod, отличающихся только числом реплик, тегом образа и лимитом памяти. Нужны безболезненная конфигурация под окружения и реальный путь отката. Выбери подход."
  options={[
    { name: "Один chart Helm; тонкий файл values на окружение; деплой через helm upgrade --install", summary: "Шаблон один раз, параметризуй.", correct: true, justification: "Это ровно та задача, под которую построен Helm. Шаблон кодирует форму деплоя один раз, поэтому изменение пробы или лейбла вносится в одном месте; каждое окружение несёт только горстку реально отличающихся values, поэтому dev/staging/prod не могут разъехаться в частях, которые должны совпадать. И каждый upgrade создаёт пронумерованную ревизию, давая helm rollback как отмену одной командой и helm history как аудит-трейл — семантику release, которой голый kubectl никогда не даёт." },
    { name: "Три скопированных папки плоского YAML, по одной на окружение, применяемые через kubectl apply -f", summary: "Копия на окружение.", justification: "Это и есть исходная боль. Манифесты дублируют всё, что идентично, поэтому одно изменение пробы надо править в трёх файлах, и один в итоге пропустят — дрейф по построению. А у kubectl apply нет понятия release или rollback, так что отмена означает реконструировать предыдущий YAML руками из git или памяти. Работает для одного тривиального приложения и рушится в момент, когда появляются окружения, которые надо держать в синхроне." },
    { name: "Единый набор YAML с выбором окружения через ветвление kubectl-логики в shell-скрипте", summary: "Скриптуй различия.", justification: "Ты в итоге плохо переизобретаешь шаблонизацию и журнал релизов на bash. Нет приоритета values, нет предпросмотра отрендеренного вывода, нет истории ревизий и нет атомарного отката — просто скрипт, который сам по себе непротестированная инфраструктура. Helm уже решает это протестированным движком; самописный скрипт меняет известный инструмент на самодельную хрупкость." },
    { name: "--set каждый отличающийся ключ в командной строке kubectl/Helm, без файлов values", summary: "Все переопределения инлайн.", justification: "--set ок для разового переопределения, но кодирование каждого различия окружения как инлайн-флагов делает деплои длинными, error-prone и непроверяемыми — нет файла, который продиффить в PR, и нет записи, какова на самом деле конфигурация prod. Файлы values на окружение — проверяемый, версионируемый дом этих фактов; держи --set для одной разовой правки поверх." },
  ]}
/>

<Quiz
  id="04-helm-intro-quiz-1"
  lessonSlug="04-helm-intro"
  lang="ru"
  question="Прод-helm upgrade выкатил битый конфиг, и сервис начал отдавать ошибки. Какое самое быстрое корректное восстановление и почему оно возможно?"
  choices={[
    { label: "helm rollback my-api <предыдущая-ревизия> — Helm записал каждый upgrade как пронумерованную ревизию, поэтому может заново применить известно-хорошее прошлое состояние одной атомарной командой", correct: true },
    { label: "kubectl apply старого YAML, который ты реконструируешь из истории git", misconception: "Может сработать, но медленнее и error-prone: надо найти и пересобрать точные предыдущие манифесты руками. Смысл Helm в том, что он уже сохранил предыдущую ревизию, поэтому helm rollback восстанавливает её напрямую без всякой реконструкции." },
    { label: "Ничего — раз upgrade применён, его нельзя отменить", misconception: "Неверно именно для Helm. В отличие от голого kubectl apply, каждый helm upgrade сохраняет пронумерованную ревизию, а helm rollback заново применяет любую предыдущую. Этот журнал релизов и есть вся причина брать Helm вместо голого apply." },
    { label: "helm uninstall my-api и переустановить старый chart", misconception: "uninstall удаляет ресурсы всего release целиком — это даунтайм и риск потери состояния (PVC, в зависимости от политики). Rollback восстанавливает предыдущую ревизию на месте, не разрушая release." },
  ]}
/>

<Quiz
  id="04-helm-intro-quiz-2"
  lessonSlug="04-helm-intro"
  lang="ru"
  question="Где в чарте должна жить разница между dev и prod под окружение (реплики, тег образа)?"
  choices={[
    { label: "В тонком файле values на окружение, переданном через -f при upgrade; шаблоны остаются идентичными и читают эти ключи через .Values", correct: true },
    { label: "Продублированной в отдельные файлы шаблонов на окружение", misconception: "Это воссоздаёт дублирование, ради устранения которого Helm и существует — шаблоны разъедутся ровно как скопированный голый YAML. Шаблоны кодируют форму один раз; на окружение меняются только values." },
    { label: "Захардкоженной в метаданные Chart.yaml", misconception: "Chart.yaml — это метаданные (name, version, appVersion, dependencies), а не runtime-конфигурация. Values под окружение идут в файлы values, а не в метаданные." },
    { label: "Отредактированной прямо в отрендеренных манифестах после helm template", misconception: "Правка отрендеренного вывода полностью обходит chart, поэтому изменение теряется при следующем рендере, и нет источника истины. Весь смысл в том, что values кормят шаблон; ты меняешь values, а не вывод." },
  ]}
/>

</Explanation>

<KeyTakeaway>Helm упаковывает манифесты как chart, параметризует их через values (граница окружения) и ведёт каждый деплой как именованный пронумерованный release. Это даёт один источник формы, отсутствие дрейфа между окружениями и `helm rollback` — реальную отмену, которой нет у `kubectl apply`. Предпросматривай через `--dry-run`/`helm diff` и версионируй чарты.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-helm-intro-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, как один chart Helm выдаёт разные манифесты для dev, staging и prod, назвав три части чарта и как работает приоритет values.", a: "Chart — это пакет с тремя важными здесь частями: Chart.yaml (метаданные — name, version, appVersion, dependencies), templates/ (манифесты Kubernetes с плейсхолдерами Go-template вроде числа реплик или тега образа, вбитыми в места, которые меняются) и values.yaml (значения по умолчанию и документированный публичный интерфейс чарта). Один chart рендерит разные манифесты, потому что ты кормишь его разными values. База — собственный values.yaml чарта; затем ты накладываешь файлы окружений через -f (values-prod.yaml, values-staging.yaml), которые переопределяют значения по умолчанию слева направо; а флаги --set переопределяют всё поверх. Так шаблон кодирует форму деплоя ровно один раз, и каждое окружение несёт только тонкий файл из горстки реально отличающихся ключей — реплики, тег образа, лимит памяти. Helm рендерит шаблон + смерженные values в плоский YAML и применяет его. Итог — одно определение readiness-пробы на сопровождение и отсутствие дрейфа между окружениями в частях, которые должны быть идентичны; values — это граница окружения." },
    { q: "Почему Helm может откатить битый деплой, а kubectl apply нет, и что прогнать перед upgrade, чтобы откат не понадобился?", a: "kubectl apply -f отправляет текущие файлы в кластер и забывает — нет объекта release, нет связывания родственных ресурсов и нет записи предыдущего манифеста, поэтому отмена означает реконструировать старый YAML руками. Helm же создаёт именованный release и хранит пронумерованную ревизию ровно того, что задеплоил (как Secret в кластере), на каждый install и upgrade. Поскольку каждая ревизия записана, helm rollback my-api <ревизия> заново применяет известно-хорошее прошлое состояние одной атомарной командой, а helm history показывает весь журнал. Чтобы откат не понадобился, предпросматривай до применения: helm upgrade --dry-run рендерит манифесты, не трогая кластер, а плагин helm diff upgrade показывает точное изменение против живого release — так ты ловишь разрушительное изменение или правку неизменяемого поля (clusterIP у Service, spec у Job, некоторые поля volume, форсящие пересоздание и даунтайм) до того, как оно случится. Читай diff, знай, какие поля неизменяемы, и держи rollback как страховку, а не как план." },
  ]}
/>

<Recap lang="ru">
У голого YAML Kubernetes две дыры, как только приложение живёт больше чем в одном месте: он дублирует всё идентичное между окружениями, а у kubectl apply нет единицы release или отката. Helm — пакетный менеджер Kubernetes — закрывает обе. Chart — это пакет: метаданные Chart.yaml, templates/ с манифестами с плейсхолдерами Go-template и values.yaml со значениями по умолчанию и публичным интерфейсом чарта. Один и тот же chart рендерит разные манифесты, потому что values переопределяют шаблоны под окружение — тонкий файл values на окружение, переданный через -f, или отдельные ключи через --set, наслаиваемые база-переопределение-флаг. Values — это граница окружения: форма живёт в шаблоне один раз, факты окружения — в маленьких файлах values, поэтому dev/staging/prod не могут разъехаться. Каждый деплой — именованный пронумерованный release, поэтому helm upgrade поднимает ревизию, которую Helm хранит, а helm rollback заново применяет известно-хорошую атомарно — отмену, которой не было у apply. Переиспользуй готовую инфраструктуру из публичных chart-репозиториев вроде Bitnami и композируй через зависимости-subcharts. И никогда не апгрейдь вслепую: helm --dry-run и helm diff предпросматривают изменение, версионируй чарты через version и appVersion в Chart.yaml и следи за ловушкой upgrade-vs-recreate, где неизменяемые поля форсят разрушительное пересоздание.
</Recap>
