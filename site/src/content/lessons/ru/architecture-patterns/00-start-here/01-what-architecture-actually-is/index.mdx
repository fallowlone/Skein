---
slug: "01-what-architecture-actually-is"
lang: ru
track: "architecture-patterns"
unit: "00-start-here"
order: 1
title: "Что такое архитектура на самом деле"
summary: "Архитектура — это решения, которые дорого менять. Не диаграммы и не паттерны. Дизайн и архитектура работают на разных горизонтах стоимости. «Best practice» — это категориальная ошибка: контекст определяет пригодность, а не название паттерна."
estMin: 18
status: ready
lessonType: topic
level: middle
concepts:
  - architecture-definition
  - design-vs-architecture
  - fitness-for-context
  - reversibility
  - structural-decision
  - best-practice-fallacy
prereqs: []
sources:
  - https://www.oreilly.com/library/view/fundamentals-of-software/9781492043447/
  - https://martinfowler.com/ieeeSoftware/whoNeedsArchitect.pdf
  - https://www.infoq.com/articles/no-best-practices/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import decisionMatrix from "./infographics/decision-matrix.svg?raw";

<Hook>
Стартап выпускает монолит. Он работает. Через два года команда выросла до тридцати инженеров, и каждая фича занимает шесть недель, потому что всё завязано на модуль payments. CTO называет это «проблемой архитектуры» — но никто никогда не принимал решение связать payments со всем остальным. Просто никто никогда не принимал решения этого не делать. Связность накапливалась по одному маленькому срезу пути, каждый разумный сам по себе, пока отмотать её назад не потребовало бы переписать половину системы. Команда всё это время принимала архитектурные решения — просто не называла их так.
</Hook>

<Crux>Архитектура — это решения, которые сложно отменить: те, чья стоимость возврата растёт с каждой строкой кода, написанной поверх них.</Crux>

<Explanation>

## Определение через стоимость изменения

Самое полезное определение программной архитектуры — не про диаграммы со стрелочками и не про именованные паттерны. Ральф Джонсон сформулировал просто: архитектура — это решения, которые сложно изменить. Мартин Фаулер уточнил: решения, которые стоят всё дороже и дороже менять по мере того, как система растёт вокруг них.

Эта формулировка сразу отделяет архитектуру от обычного дизайна. Если сегодня выбрал имя переменной, а завтра захотел лучше — rename-рефактор занимает двадцать секунд. Это дизайн — низкая стоимость возврата. Если вложил логику billing прямо в HTTP-обработчик, а через двенадцать месяцев хочешь вызывать тот же billing из фонового задания, мобильного приложения и партнёрского API — это структурная проблема, которая потребует недель рискованной хирургии. Это архитектура — решение с высокой стоимостью возврата.

Вывод неудобный: **ты принимаешь архитектурные решения постоянно, независимо от того, называешь ли ты их так.** Вопрос не в том, принимать ли их, а в том, принимать ли их намеренно.

<Infographic
  svg={decisionMatrix}
  label="Матрица 2x2, размещающая решения по двум осям — цена отката (дёшево или дорого) и намеренность (намеренно или случайно): дёшево-и-намеренно — это обычный дизайн, дорого-и-намеренно — настоящая архитектура, дёшево-и-случайно — пустяк, а опасный квадрант — это дорогое для отката решение, принятое случайно."
  caption="Ловушка — это дорогое для отката решение, которое никто не принимал намеренно: связность накапливается именно потому, что её не назвали архитектурной."
  hue="var(--d-infra)"
/>

<FlowDiagram
  label="Дизайн-решения vs архитектурные решения: стоимость возврата со временем"
  nodes={[
    { id: "day1", label: "День 1", sub: "решение принято" },
    { id: "design", label: "Дизайн-решение", sub: "переименовать переменную" },
    { id: "arch", label: "Архитектурное решение", sub: "coupling billing к HTTP-слою" },
    { id: "rev-design", label: "Стоимость возврата: минуты", sub: "IDE rename refactor" },
    { id: "rev-arch", label: "Стоимость возврата: недели", sub: "извлечь, перемаршрутировать, перетестировать" },
  ]}
  edges={[
    { from: "day1", to: "design", label: "слабая связность" },
    { from: "day1", to: "arch", label: "сильная связность" },
    { from: "design", to: "rev-design", label: "в любой момент" },
    { from: "arch", to: "rev-arch", label: "через 12 месяцев" },
  ]}
  caption="Один момент нерешительности расходится на очень разные стоимости возврата в зависимости от того, насколько глубоко остальная система выросла вокруг исходного выбора."
/>

## Дизайн vs архитектура: два разных горизонта

Дизайн и архитектура — не два уровня иерархии; они работают на разных временных горизонтах и с разными конвертами стоимости.

**Дизайн** — это форма решения внутри уже установленного структурного контекста. Какой класс содержит этот метод? Как назвать эту функцию? Как должен выглядеть API этого модуля? Эти выборы важны для читаемости и корректности, но их можно пересмотреть без структурной хирургии. Дизайн-решения живут внутри границ, которые нарисовала архитектура.

**Архитектура** рисует эти границы. Где проходит граница между доменом billing и доменом shipping? Владеет ли каждый домен своей схемой БД, или они используют общую? Может ли сервис заказов обращаться к сервису биллинга синхронно, или это должно идти через событие? Эти решения распространяются структурно — они ограничивают каждое дизайн-решение, принятое после них.

Путаница возникает потому, что одна и та же технология может быть и тем, и другим. Выбор PostgreSQL для прототипа — это дизайн-решение: можно сменить. Выбор PostgreSQL как единственной общей БД, схему которой напрямую запрашивают восемь сервисов, каждый из которых предполагает конкретные структуры таблиц, — это архитектурное решение. Ты встроил допущение настолько глубоко, что БД нельзя заменить, не заменив все восемь сервисов сразу.

<Inset kind="why" lang="ru">
Почему это различие важно на практике? Потому что команды относятся к двум видам решений по-разному. Дизайн-решения пересматривают на код-ревью. Архитектурные решения принимаются в разговорах, которые могут нигде не фиксироваться. Эта асимметрия — то, как связность тихо накапливается: никто не говорит «сегодня мы принимаем архитектурное решение подцепить billing к HTTP-слою». Говорят «давай пока просто вызовем функцию billing прямо из обработчика» — фраза, звучащая как дизайн-решение, но в контексте являющаяся архитектурным.
</Inset>

## Категориальная ошибка «best practice»

«Используй микросервисы» — это best practice. «Используй hexagonal architecture» — best practice. «Отдели бизнес-логику от инфраструктуры» — best practice. Эти предписания появляются в книгах, докладах и инженерных блогах, и они не ошибочны — они описывают паттерны, решавшие реальные проблемы в конкретных командах в конкретных контекстах. Ошибка — трактовать предписание как контекстно-независимое.

Best practices — это решения для конкретных сил. Паттерн hexagonal architecture существует потому, что силы «мне нужно тестировать бизнес-логику без базы данных» и «мне нужно запускать одно и то же приложение из HTTP-сервера, CLI и очереди сообщений» оказывают реальное давление. Если эти силы присутствуют в твоей системе, hexagonal architecture хорошо подходит. Если твоя система — это CRUD API, на которую эти силы никогда не будут давить, hexagonal architecture добавляет слои, интерфейсы и indirection без соответствующей пользы. Это не «best» для тебя — это overkill.

Правильный вопрос никогда не «что является best practice?». Он — «каким силам подвержена моя система, и какие структуры отвечают этим силам?». Это рамка, которую использует весь остальной трек. Каждый рассматриваемый паттерн будет представлен как ответ на поимённый набор сил — и компромиссы, которые он при этом делает.

<FlowDiagram
  label="Паттерн — это совпадение между силами и структурой, а не универсальное предписание"
  nodes={[
    { id: "forces", label: "Силы", sub: "тестируемость, несколько точек входа, без БД в тестах" },
    { id: "hex", label: "Hexagonal architecture", sub: "ports + adapters" },
    { id: "fit", label: "Хорошо подходит", sub: "силы присутствуют" },
    { id: "overfit", label: "Overkill", sub: "силы отсутствуют" },
    { id: "crud", label: "Простой CRUD API", sub: "нет давления тестируемости" },
  ]}
  edges={[
    { from: "forces", to: "hex", label: "отвечает" },
    { from: "hex", to: "fit", label: "система с этими силами" },
    { from: "hex", to: "overfit", label: "применяется к" },
    { from: "crud", to: "overfit", label: "другие силы" },
  ]}
  caption="Структурный паттерн отвечает конкретным силам. Применённый без этих сил, тот же паттерн становится излишней сложностью — именно поэтому hexagonal architecture блестяща в одной системе и бюрократична в другой."
/>

## Чем архитектура не является

Две вещи часто маскируются под архитектуру:

**Диаграммы.** Диаграмма описывает архитектуру — она не является архитектурой. Команды часто путают артефакт с реальностью. Архитектура — это фактическая структура связанности в кодовой базе; диаграмма — модель этой структуры. Когда диаграмма говорит «billing — отдельный сервис», а код разделяет схему БД с ordering, диаграмма неверна, а связность — реальность.

**Upfront-дизайн.** Архитектура не требует фазы проектирования перед написанием кода. Многие из самых устойчивых систем проектировались инкрементально — каждое решение принималось тогда, когда силы были достаточно понятны, чтобы принять хорошее решение. Что архитектура требует — так это намеренности: понимания, какие решения дорого менять, принятия их обдуманно и фиксации причин. Это может происходить в любой точке проекта.

<Inset kind="note" lang="ru">
Практический тест — является ли решение архитектурным: спроси «если мне нужно отменить это через двенадцать месяцев, сколько кодовой базы придётся трогать?». Если ответ — «большую часть», решение архитектурное, независимо от того, было ли оно помечено как таковое. Этот тест также раскрывает, почему кажущиеся незначительными решения — например, выставляет ли сервис свою внутреннюю модель данных как публичный API — становятся архитектурными по мере роста потребителей системы.
</Inset>

<Quiz
  id="arch-00-01-quiz-1"
  lessonSlug="01-what-architecture-actually-is"
  lang="ru"
  question="Команда кладёт логику billing прямо в HTTP route handlers. Через полгода им нужен billing из фонового задания и партнёрского webhook. Что делает это архитектурным, а не дизайн-решением?"
  choices={[
    { label: "Стоимость его отмены растёт с каждой фичей, построенной поверх связанной структуры — отмена теперь требует изменений во многих call sites и тестах, а не одного рефактора.", correct: true },
    { label: "Оно затронуло большой файл, поэтому по определению архитектурное.", misconception: "Размер файла не является критерием. Тест стоимости возврата: сколько кодовой базы должно измениться, чтобы отменить это? Привязка billing к HTTP-обработчикам распространяет это допущение повсюду, где затрагивается billing." },
    { label: "Это было плохое решение, а архитектурные решения — это плохие.", misconception: "Архитектурные решения не плохи по определению — они дорогостоящие для изменения. Хорошее архитектурное решение может быть превосходным; плохое может иметь низкую стоимость, если система ещё не выросла вокруг него." },
    { label: "Оно затронуло более одного файла, что пересекает порог в архитектуру.", misconception: "Охват в файлах не является тестом. Однофайловое решение, встраивающее сквозное структурное допущение (например, 'billing происходит внутри HTTP'), является архитектурным; многофайловый рефактор имён переменных — нет." },
  ]}
/>

<Quiz
  id="arch-00-01-quiz-2"
  lessonSlug="01-what-architecture-actually-is"
  lang="ru"
  question="Команда строит CRUD API для простого внутреннего инструмента, у которого никогда не будет нескольких точек входа и нет давления на тестирование бизнес-логики. Senior-инженер предлагает принять hexagonal architecture. Что является наиболее точным ответом?"
  choices={[
    { label: "Hexagonal architecture отвечает силам (тестируемость, несколько точек входа, изоляция БД), которые здесь отсутствуют — принятие её добавит indirection без соответствующей пользы, что делает её overkill для этого контекста.", correct: true },
    { label: "Best practices всегда следует соблюдать независимо от контекста.", misconception: "Best practices — это решения для сил. Применённые без этих сил, они не являются 'best' — они являются накладными расходами. Вопрос всегда в том, присутствуют ли в системе силы, которые паттерн адресует." },
    { label: "Hexagonal architecture всегда лучше layered-подхода.", misconception: "Ни один паттерн не является универсально лучшим. Hexagonal architecture отвечает конкретным силам; если эти силы отсутствуют, простой layered-подход имеет меньше indirection и часто является лучшим выбором." },
    { label: "Команда должна использовать её для подготовки к будущим требованиям.", misconception: "YAGNI применяется и структурно. Добавление слоёв под гипотетические будущие силы — это спекулятивная архитектура: силы могут никогда не материализоваться, а накладные расходы реальны с первого дня. Проектируй, когда силы понятны, а не когда они воображаемые." },
  ]}
/>

<Quiz
  id="arch-00-01-quiz-3"
  lessonSlug="01-what-architecture-actually-is"
  lang="ru"
  question="Актуальная диаграмма архитектуры показывает billing как изолированный сервис. Реальный код вызывает логику billing напрямую из слоя репозитория сервиса заказов через общую внутреннюю библиотеку. Какая из них является реальной архитектурой?"
  choices={[
    { label: "Код — фактическая связанность в кодовой базе является реальной архитектурой. Диаграмма описывает модель, которая не соответствует реальности.", correct: true },
    { label: "Диаграмма, потому что она представляет предполагаемую архитектуру.", misconception: "Намерение — не архитектура. Архитектура — это фактическая структура связанности работающей системы. Диаграмма, расходящаяся с кодом, — устаревшая модель, а не конкурирующая архитектура." },
    { label: "Ни одна из них — архитектура существует только в проектных документах, утверждённых архитектором.", misconception: "Архитектура — это эмерджентное свойство кода, а не документ. Связанность между модулями существует независимо от того, назвал ли её кто-то. Практический тест: что должно измениться при изменении требований?" },
    { label: "Диаграмма, потому что диаграммы — это авторитетный артефакт в архитектуре.", misconception: "Диаграммы описывают архитектуру; они не являются архитектурой. Источник истины — фактическая структура зависимостей кода: что что импортирует, какие данные разделяются, что можно изменить независимо." },
  ]}
/>

</Explanation>

<KeyTakeaway>Архитектура — это решения, которые дорого отменять, а не диаграммы и не паттерны. Дизайн работает внутри структуры, которую рисует архитектура. Каждый именованный паттерн — решение для конкретных сил; без этих сил тот же паттерн — накладные расходы. Спрашивай не «что является best practice?», а «каким силам подвержена моя система, и какая структура отвечает на них?»</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-00-01-retrieval"
  lang="ru"
  questions={[
    { q: "Как отличить архитектурное решение от дизайн-решения?", a: "По стоимости возврата со временем. Дизайн-решение можно отменить быстро (переименование, извлечение метода) без структурной хирургии. Архитектурное решение становится дороже отменять с каждой фичей, построенной поверх него — потому что остальная система вырастает вокруг закодированного допущения. Практический тест: сколько кодовой базы придётся трогать, чтобы отменить это через двенадцать месяцев?" },
    { q: "Почему 'best practice' является категориальной ошибкой применительно к архитектурным паттернам?", a: "Потому что паттерны — это решения для сил, а не универсальные предписания. Такой паттерн, как hexagonal architecture, отвечает конкретным силам: тестируемость без инфраструктуры, несколько точек входа, изоляция домена. Применённый к системе, где эти силы отсутствуют, тот же паттерн добавляет слои и indirection без пользы. Правильный вопрос всегда: каким силам подвержена эта система, и какая структура отвечает на них?" },
    { q: "Если диаграмма архитектуры и код расходятся, какая из них является реальной архитектурой?", a: "Код. Архитектура — это фактическая структура связанности системы: что от чего зависит, что можно изменять независимо, что должно меняться вместе. Диаграмма, расходящаяся с кодом, — это устаревшая модель. Связанность в кодовой базе — это реальность, независимо от того, что говорит диаграмма." },
  ]}
/>

<Recap lang="ru">
Архитектура — это набор решений, которые становятся всё дороже отменять по мере того, как система созревает вокруг них. Это отделяет её от дизайна — который работает внутри уже установленного структурного контекста и остаётся дешёвым для пересмотра. Различие не об уровне диаграммы или количестве файлов: однострочное решение, связывающее billing с HTTP-слоем, является архитектурным; многофайловый rename-рефактор — нет.

Именованные паттерны — hexagonal, clean, DDD, CQRS — это ответы на конкретные силы. Это не best practices для универсального следования; это решения, стоящие своей сложности только тогда, когда присутствуют силы, которые они адресуют. Принятие их без этих сил — это спекулятивная архитектура, создающая накладные расходы без соответствующей пользы.

Диаграмма — это не архитектура; архитектура — это код. Намерения, проектные документы и диаграммы описывают архитектуру, которую хотели. Фактическая связанность в кодовой базе — это существующая архитектура. На протяжении всего трека мы будем называть силы первыми, а структурные ответы — вторыми: это единственный способ рассуждать о том, подходит ли данная структура для данной системы.
</Recap>
