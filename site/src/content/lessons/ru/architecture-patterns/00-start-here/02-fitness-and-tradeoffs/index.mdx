---
slug: "02-fitness-and-tradeoffs"
lang: ru
track: "architecture-patterns"
unit: "00-start-here"
order: 2
title: "Пригодность и компромиссы"
summary: "Не существует лучшей архитектуры — только структуры, пригодные или непригодные для конкретных сил. «Ilities» конкурируют между собой: оптимизация одной стоит другой. Урок вводит B2B-пример заказов и биллинга, используемый на протяжении всего трека."
estMin: 20
status: ready
lessonType: topic
level: middle
concepts:
  - architectural-fitness
  - quality-attributes
  - ilities
  - tradeoff-analysis
  - architectural-drivers
  - b2b-order-billing-example
prereqs: []
sources:
  - https://www.oreilly.com/library/view/building-evolutionary-architectures/9781491986356/
  - https://martinfowler.com/articles/microservices.html
  - https://www.infoq.com/presentations/ATAM/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Две инженерные команды, одна проблема: растущая B2B-платформа, где заказы поступают в биллинг. Команда A реструктурировала кодовую базу в пятнадцать микросервисов. Деплои стали быстрее, но отладка расхождения в биллинге теперь требует трассировки через шесть сервисов, три очереди сообщений и распределённый трейс, который читается сорок минут. Команда B сохранила монолит, но добавила чистые внутренние модульные границы. Они не могут масштабировать движок биллинга независимо, но отладка занимает десять минут с одним стектрейсом. Ни одна из команд не ошиблась. Они оптимизировали под разные силы — и заплатили за это разную цену.
</Hook>

<Crux>Не существует лучшей архитектуры — только структуры, пригодные или непригодные для фактических сил, воздействующих на конкретную систему в данный момент.</Crux>

<Explanation>

## Рамка пригодности

Предыдущий урок установил, что архитектура — это набор решений, которые дорого менять. Этот урок устанавливает, что делает данный набор решений *хорошим*: пригодность под силы, которым подвержена система.

Структура пригодна, если она делает наиболее важные для этой системы качества легкодостижимыми и если качества, которыми она жертвует, для этой системы не важны. Та же структура, которая превосходна под одним набором сил, вредна под другим. Это не релятивизм — это точность. Невозможно оценить архитектуру, не зная сил, на которые она должна отвечать.

Силы приходят с двух сторон: **функциональные требования** (что система должна делать) и **атрибуты качества** (насколько хорошо она должна это делать и при каких ограничениях). Функциональные требования — более лёгкая часть для рассуждений: их можно специфицировать, тестировать и отслеживать. Атрибуты качества — там, где живут структурные решения.

<FlowDiagram
  label="Пригодность: структура оценивается относительно сил, воздействующих на конкретную систему"
  nodes={[
    { id: "forces", label: "Силы", sub: "функциональные + качественные + ограничения" },
    { id: "structure", label: "Архитектурная структура", sub: "уже принятые решения" },
    { id: "fit", label: "Пригодна", sub: "структура отвечает доминирующим силам" },
    { id: "unfit", label: "Непригодна", sub: "структура оптимизирует неверные качества" },
    { id: "eval", label: "Оценка", sub: "не паттерн против паттерна — силы против структуры" },
  ]}
  edges={[
    { from: "forces", to: "eval", label: "определяет" },
    { from: "structure", to: "eval", label: "измеряется относительно" },
    { from: "eval", to: "fit", label: "совпадение" },
    { from: "eval", to: "unfit", label: "несовпадение" },
  ]}
  caption="Пригодность — это отношение между силами и структурой, а не внутреннее свойство ни того ни другого. Один и тот же паттерн может быть пригоден для одной системы и катастрофически непригоден для другой."
/>

## «Ilities»: конкурирующие атрибуты качества

Атрибуты качества часто называют «-ilities» (от английского суффикса), потому что многие их имена оканчиваются на это сочетание: maintainability, deployability, testability, scalability, reliability, security, observability. Каждый описывает измерение, по которому можно оценить систему. Проблема в том, что они тянут в противоположных направлениях.

**Maintainability** — насколько легко можно изменить кодовую базу без внесения ошибок? Высокомодульная, хорошо абстрагированная база набирает здесь очки, но абстракция добавляет indirection и когнитивные накладные расходы.

**Deployability** — насколько быстро и безопасно изменение попадает в production? Микросервисы максимизируют её (каждый сервис деплоится независимо), но переносят связанность из кода в инфраструктуру: сервисные контракты, управление версиями и распределённые режимы отказов.

**Testability** — насколько легко можно верифицировать поведение в изоляции? Dependency injection и паттерны ports/adapters делают unit-тестирование тривиальным, но добавляют интерфейсные слои, которые могут скрывать, а не раскрывать замысел в простых случаях.

**Scalability** — может ли система обрабатывать растущую нагрузку? Stateless, секционированные сервисы масштабируются горизонтально, но требуют, чтобы домен был сформирован вокруг схемы разбиения, что конфликтует с едиными доменными моделями.

**Observability** — можно ли понять, что делает система, когда что-то идёт не так? Монолит с хорошим логированием часто более наблюдаем, чем распределённая система, где один запрос трассируется через двенадцать сервисов.

Каждое архитектурное решение одновременно двигает несколько «ilities». Вынос billing в отдельный сервис улучшает deployability и scalability компонента billing; ухудшает observability и увеличивает поверхность для распределённых сбоев. Выбор — не «что лучше», а «какой регулятор наиболее важен для сил этой системы прямо сейчас».

<Inset kind="why" lang="ru">
Почему «ilities» конкурируют, а не дополняют друг друга? Потому что они не свойства кода в изоляции — они свойства системы в контексте её организации разработки, инфраструктуры деплоя и операционной среды. Изменение, улучшающее deployability (развязанные сервисы), ухудшает debuggability (распределённые трейсы). Изменение, улучшающее testability (dependency inversion), ухудшает простоту (больше интерфейсов). Напряжение структурно: оптимизация одного измерения часто требует жертвы другим, потому что лежащие в основе силы тянут в разные стороны.
</Inset>

## Бегущий пример: B2B-платформа заказов и биллинга

На протяжении всего трека повторяется одна система-пример: B2B-платформа заказов и биллинга для SaaS-компании. Заказы поступают через клиентский API. Они проходят ценообразование, расчёт налогов и рабочие процессы согласования. Они поступают в биллинг, который генерирует инвойсы, применяет скидки и инициирует сбор платежей. Отчёты биллинга питают финансовые дашборды. Разрешение споров требует просмотра полной истории заказа.

Это реальный домен с реальными силами:
- **Несколько точек входа**: заказы поступают через API, webhook и внутренние admin-инструменты.
- **Разные темпы изменений**: правила ценообразования меняются ежемесячно; логика сбора платежей меняется редко.
- **Требования соответствия**: записи биллинга — это неизменяемые аудит-трейлы; GDPR применяется к данным клиентов.
- **Давление интеграции**: платформа интегрируется с тремя платёжными процессорами, двумя ERP-системами и порталом клиента.
- **Структура команды**: продуктовая команда владеет потоком заказов; команда finance-tech владеет биллингом.

<FlowDiagram
  label="B2B-платформа заказов и биллинга: бегущий пример, который трек реструктурирует по единицам"
  nodes={[
    { id: "api", label: "Customer API", sub: "ввод заказов" },
    { id: "admin", label: "Admin-инструмент", sub: "внутреннее создание заказов" },
    { id: "webhook", label: "Webhook receiver", sub: "заказы от партнёров" },
    { id: "order", label: "Движок заказов", sub: "ценообразование, согласование, налоги" },
    { id: "billing", label: "Движок биллинга", sub: "инвойс, скидка, сбор" },
    { id: "finance", label: "Финансовый дашборд", sub: "отчётность" },
    { id: "erp", label: "ERP-интеграция", sub: "внешняя система" },
    { id: "payment", label: "Платёжные процессоры", sub: "Stripe, Adyen, Braintree" },
  ]}
  edges={[
    { from: "api", to: "order", label: "отправить" },
    { from: "admin", to: "order", label: "отправить" },
    { from: "webhook", to: "order", label: "отправить" },
    { from: "order", to: "billing", label: "подтверждённый заказ" },
    { from: "billing", to: "finance", label: "данные инвойса" },
    { from: "billing", to: "erp", label: "бухгалтерская запись" },
    { from: "billing", to: "payment", label: "сбор платежа" },
  ]}
  caption="B2B-платформа заказов и биллинга как плоская карта зависимостей. Каждая последующая единица трека реструктурирует эти отношения по-разному — применяя layered architecture, hexagonal ports, DDD bounded contexts, CQRS и стратегии декомпозиции — показывая, как разные структуры отвечают разным силам в том же домене."
/>

Эта платформа — не игрушка. Она вернётся, когда будем рассматривать layered architecture (единица 03), hexagonal architecture (единица 04), DDD bounded contexts (единица 06), CQRS (единица 07), event sourcing (единица 08) и декомпозицию (единица 10). Домен остаётся прежним; структура меняется каждый раз, когда мы применяем другой набор сил.

## Честное чтение компромисса

Стандартный инженерный провал при рассуждении об архитектуре — асимметричный компромисс: перечислить все преимущества выбранного подхода и ни одного из его издержек, затем перечислить все издержки отвергнутого подхода и ни одного из его преимуществ. Именно так команды приходят к микросервисам для решения проблемы скорости деплоя, а потом два года борются с распределёнными транзакциями и query fan-out.

Честно прочитанный компромисс имеет такую форму:

- **Каким силам отвечает эта структура?** (Назови их конкретно, не расплывчато.)
- **Чего это стоит?** (Назови «ilities», которые ухудшаются, и операционные накладные расходы, которые добавляются.)
- **Каким силам эта структура не отвечает?** (Будь явным в том, что ты принимаешь.)
- **Что должно измениться, чтобы другая структура стала правильной?** (Это эволюционный выход — если система столкнётся с силой, которую текущая структура не может обработать, что является триггером?)

Эта рамка появляется на протяжении всего трека. Это не чеклист для механического следования — это привычка явно называть силы перед оценкой структуры.

<Inset kind="note" lang="ru">
Метод анализа архитектурных компромиссов (ATAM) формализует этот паттерн в структурированную технику оценки архитектурных решений по атрибутам качества. ATAM выявляет «точки чувствительности» — решения, сильно влияющие на конкретный атрибут качества, — и «точки компромисса» — решения, влияющие на несколько атрибутов качества в противоположных направлениях. Неформальная версия того же мышления — то, что делает каждый опытный инженер при ревью структурного предложения: «да, это улучшает X, но посмотри, что это делает с Y». Единица 11 рассматривает это глубоко.
</Inset>

<Quiz
  id="arch-00-02-quiz-1"
  lessonSlug="02-fitness-and-tradeoffs"
  lang="ru"
  question="Команда переходит от монолита к микросервисам для улучшения скорости деплоя. Через шесть месяцев они сообщают, что деплои быстрее, но отладка production-инцидентов теперь занимает втрое больше времени. Какая характеристика наиболее точна?"
  choices={[
    { label: "Они приняли фитнес-решение, оптимизирующее deployability за счёт observability — настоящий компромисс, а не ошибка, при условии что deployability была доминирующей силой.", correct: true },
    { label: "Микросервисы — всегда неверный выбор, потому что они всегда ухудшают observability.", misconception: "Микросервисы не являются ни всегда правильными, ни всегда неправильными. Утверждение «всегда ухудшают observability» слишком абсолютно — при хорошей распределённой трассировке и хорошо спроектированных границах сервисов observability может быть приемлемой. Проблема в том, понимала ли команда компромисс до его принятия." },
    { label: "Им следует вернуться к монолиту, потому что observability важнее deployability.", misconception: "Нет универсального ранжирования «ilities». Важнее ли observability, чем deployability, зависит от контекста системы: как часто происходят инциденты, насколько они серьёзны и как часто нужны деплои. Правильный ответ требует знания фактических сил." },
    { label: "Они оптимизировали верно — deployability всегда является наиболее критичным атрибутом качества.", misconception: "Ни один «ility» не является универсально наиболее критичным. Финансовая система, где корректность данных важнее скорости деплоя, будет правильно расставлять приоритеты иначе. Доминирующий атрибут качества зависит от фактических сил, которым подвержена система." },
  ]}
/>

<Quiz
  id="arch-00-02-quiz-2"
  lessonSlug="02-fitness-and-tradeoffs"
  lang="ru"
  question="B2B-платформа заказов и биллинга нуждается в соответствии GDPR для данных клиентов, но финансовой команде также нужны неизменяемые аудит-трейлы каждого события биллинга. Эти два требования, кажется, конфликтуют: GDPR предписывает удаление, но неизменяемость запрещает его. Как это называется в рамке пригодности?"
  choices={[
    { label: "Точка компромисса — структурное решение, влияющее на несколько атрибутов качества в противоположных направлениях, требующее явного выбора о том, какой силе отдать предпочтение.", correct: true },
    { label: "Баг в требованиях — одно из них должно быть неверным.", misconception: "Оба являются законными требованиями, с которыми сталкиваются реальные системы. Соответствие GDPR и аудит неизменяемости — обе реальные силы. Задача — найти структурный ответ (например, персональные данные, отделённые от аудит-событий, с удалением полей персональных данных, оставляющим скелет события нетронутым), — а не отвергать одну силу." },
    { label: "Архитектурная невозможность — ни одна система не может удовлетворить обоим.", misconception: "Реальные системы справляются с этим (event sourcing с GDPR tombstoning, или разделение PII на удаляемое хранилище от неизменяемого журнала событий, содержащего только псевдонимизированные идентификаторы). Это сложная структурная задача, а не невозможность." },
    { label: "Точка чувствительности — решение, сильно влияющее только на один атрибут качества.", misconception: "Точка чувствительности влияет на один атрибут качества. Этот сценарий влияет на два (соответствие и аудируемость) в противоположных направлениях, что является определением точки компромисса. Различие важно, потому что точки компромисса требуют явного выбора, а не просто настройки." },
  ]}
/>

<Quiz
  id="arch-00-02-quiz-3"
  lessonSlug="02-fitness-and-tradeoffs"
  lang="ru"
  question="Senior-инженер на B2B-платформе отстаивает layered architecture, потому что 'она проста и все знают, как она работает'. Другой отстаивает hexagonal architecture, потому что 'это best practice для testability'. Как команде решить?"
  choices={[
    { label: "Определи доминирующие силы: если давление тестируемости высокое (сложная доменная логика, которую нужно тестировать без БД) и у команды несколько точек входа, hexagonal лучше подходит. Если домен — простой CRUD со стандартным инструментарием, layered скорее всего достаточна.", correct: true },
    { label: "По умолчанию выбрать hexagonal, потому что она современнее и гибче.", misconception: "Современность и теоретическая гибкость не являются правильными критериями. Стоимость hexagonal architecture (больше интерфейсов, больше indirection, больше шаблонного кода) реальна с первого дня; её стоит платить только если силы, которые она адресует — testability без инфраструктуры, несколько точек входа — действительно присутствуют." },
    { label: "По умолчанию выбрать layered, потому что знакомость снижает риск.", misconception: "Знакомость снижает трение при onboarding, что является реальной силой — но не единственной. Если домен имеет сложные инварианты, требующие тестирования без БД, layered-подход создаст давление тестирования, которое знакомый выбор не решает." },
    { label: "Использовать оба, поскольку больше паттернов означает больше гибкости.", misconception: "Смешивание структурных паттернов без чёткой силы для каждого добавляет сложность без пользы. Кодовая база, которая частично hexagonal и частично layered без чёткого правила границы, сложнее в рассуждении, чем та, которая намеренно придерживается одного из подходов." },
  ]}
/>

</Explanation>

<KeyTakeaway>Не существует лучшей архитектуры — только структуры, пригодные или непригодные для фактических сил, воздействующих на систему. Каждое структурное решение одновременно двигает несколько «ilities». Дисциплина — называть силы первыми, затем оценивать, какая структура отвечает им при приемлемой стоимости.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-00-02-retrieval"
  lang="ru"
  questions={[
    { q: "Что означает 'архитектурная пригодность' и почему для неё нужно знать силы?", a: "Пригодность — это отношение между структурой и силами, воздействующими на систему, а не свойство структуры само по себе. Структура пригодна, если она делает доминирующие атрибуты качества легкодостижимыми и не жертвует качествами, которые важны. Без знания сил (какие атрибуты качества доминируют, какие ограничения существуют, как выглядят команда и организация) нет основы для оценки пригодности — один и тот же паттерн может быть пригодным для одной системы и вредным для другой." },
    { q: "Назови три пары «ilities», которые типично конкурируют, и объясни почему.", a: "Deployability vs observability: микросервисы максимизируют независимый деплой, но распределяют трейсы по сервисам, затрудняя отладку. Testability vs simplicity: dependency inversion и ports/adapters делают unit-тестирование тривиальным, но добавляют интерфейсные слои, увеличивающие indirection. Scalability vs consistency: stateless горизонтальное масштабирование требует разбиения домена, что конфликтует с едиными транзакционными границами. Каждый конфликт структурен: оптимизация одного измерения требует структуры, ухудшающей другое." },
    { q: "Каковы четыре вопроса, составляющие честное чтение компромисса?", a: "1. Каким силам отвечает эта структура? (названным конкретно) 2. Чего это стоит? (какие «ilities» ухудшаются, какие операционные накладные расходы добавляются) 3. Каким силам она не отвечает? (что ты явно принимаешь) 4. Что должно измениться, чтобы другая структура стала правильной? (эволюционный триггер — условие, при котором ты пересмотришь решение)" },
  ]}
/>

<Recap lang="ru">
Не существует универсально лучшей архитектуры — только структуры, пригодные или непригодные для сил, воздействующих на конкретную систему в данное время. Эти силы делятся на функциональные требования (что система должна делать) и атрибуты качества (насколько хорошо она должна это делать при каких ограничениях). Атрибуты качества — maintainability, deployability, testability, scalability, observability, reliability — это структурные рычаги; каждое архитектурное решение двигает несколько из них одновременно и в противоположных направлениях.

B2B-платформа заказов и биллинга, введённая здесь, — заказы, поступающие через ценообразование и согласование в биллинг, с финансовой отчётностью, ERP-интеграцией и тремя платёжными процессорами — это бегущий пример, который этот трек реструктурирует. Домен остаётся постоянным; структура меняется по мере применения разных сил: layered architecture, hexagonal ports, DDD bounded contexts, CQRS, event sourcing и стратегии декомпозиции.

Честная оценка архитектурного выбора требует четырёх явных шагов: назови силы, которым она отвечает; назови стоимость, которую она извлекает; назови то, чему она не отвечает; назови триггер, который сделает другую структуру правильной. Эта рамка компромисса, а не каталог паттернов, — инструмент, который ты несёшь в каждое структурное решение.
</Recap>
