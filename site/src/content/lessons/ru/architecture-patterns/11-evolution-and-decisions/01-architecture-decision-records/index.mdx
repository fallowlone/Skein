---
slug: "01-architecture-decision-records"
lang: ru
track: "architecture-patterns"
unit: "11-evolution-and-decisions"
order: 1
title: "Architecture Decision Records"
summary: "ADR фиксирует одно архитектурное решение — контекст, силы, выбор и последствия — чтобы будущие инженеры знали ПОЧЕМУ, а не только что было решено. Легковесный формат, версионируемый рядом с кодом, неизменяемый после принятия: superseded, но не удалён."
estMin: 20
status: ready
lessonType: topic
level: senior
concepts:
  - architecture-decision-record
  - decision-log
  - architectural-fitness
  - status-lifecycle
  - decision-rationale
  - version-controlled-docs
prereqs: []
sources:
  - https://adr.github.io/
  - https://cognitect.com/blog/2011/11/15/documenting-architecture-decisions
  - https://martinfowler.com/articles/architecturally-evident-coding-style.html
  - https://www.thoughtworks.com/radar/techniques/lightweight-architecture-decision-records
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Через шесть месяцев после того, как B2B биллинговая платформа переключилась с REST-взаимодействия между модулями на событийную модель, новый инженер спросил — почему. Команда три дня просматривала Slack, Confluence и pull request'ы и нашла фрагменты: тред «обсуждали на мартовском митинге», описание PR «переходим на события», Jira-тикет «технический долг». Никто не смог восстановить силы, которые обусловили решение: болтливые REST-вызовы, вызывавшие каскадные отказы при биллинговых запусках; три рассмотренные альтернативы; причину, по которой отклонили очередь сообщений в пользу доменных событий; трейдоффы, которые сознательно приняли. Когда проявился режим отказа, с которым событийная модель не справлялась, команда снова начала спорить об исходном решении — без контекста. Двое хотели откатиться, двое — двигаться вперёд. Записи о том, что уже рассматривалось, не было.

Написать Architecture Decision Record занял бы тридцать минут. Вместо этого потребовалось три дня раскопок и неразрешённый архитектурный спор.
</Hook>

<Crux>ADR фиксирует одно решение — его контекст, силы и последствия — чтобы будущие читатели знали ПОЧЕМУ, а не только что было выбрано.</Crux>

<Explanation>

## Что такое ADR и чем он не является

Architecture Decision Record — короткий документ, как правило одна-две страницы, фиксирующий одно значимое архитектурное решение. Формат предложил Майкл Найгард в 2011 году. Ключевой инсайт: само решение менее важно, чем обоснование — контекст, сделавший решение необходимым, силы, тянувшие в разные стороны, рассмотренные альтернативы и принятые последствия.

ADR — это не:
- Проектный документ (он описывает, как что-то построено)
- Документ требований (он описывает, что должно делаться)
- Постмортем (он описывает, что пошло не так)
- Живой документ (ADR неизменяем после принятия: он superseded, но не редактируется)

Формат Найгарда состоит из пяти разделов:

**Заголовок.** Короткая фраза в повелительном наклонении: «Использовать доменные события для межмодульного взаимодействия», а не «Решение о событийно-ориентированной архитектуре».

**Статус.** Proposed → Accepted → Superseded (by ADR-NNN) | Deprecated. Статус никогда не бывает «rejected» для принятых решений — они superseded, когда более новое решение их заменяет. Отклонённые предложения остаются Proposed ADR'ами, которые так и не были приняты.

**Контекст.** Силы и обстоятельства, сделавшие это решение необходимым. В чём была проблема? Какие ограничения существовали? Какова была текущая ситуация?

**Решение.** Сделанный выбор, изложенный прямо: «Мы будем использовать доменные события, публикуемые на внутрипроцессной шине, для всей кросс-модульной коммуникации».

**Последствия.** Принятые трейдоффы. Что стало проще? Что стало сложнее? Какие новые риски введены?

<FlowDiagram
  label="Жизненный цикл статуса ADR"
  caption="ADR переходит из Proposed в Accepted при согласии команды. Принятые ADR'ы никогда не редактируются — когда более новое решение их заменяет, старый ADR отмечается как Superseded и ссылается на новый. Это создаёт журнал решений, в котором сохраняется полная история обоснований."
  nodes={[
    { id: "proposed", label: "Proposed", sub: "написан, обсуждается" },
    { id: "accepted", label: "Accepted", sub: "согласован, становится артефактом" },
    { id: "superseded", label: "Superseded", sub: "заменён более поздним ADR" },
    { id: "deprecated", label: "Deprecated", sub: "контекст больше не актуален" },
    { id: "new-adr", label: "Новый ADR", sub: "написан для замены старого решения" },
  ]}
  edges={[
    { from: "proposed", to: "accepted", label: "команда соглашается" },
    { from: "accepted", to: "superseded", label: "новое решение заменяет" },
    { from: "new-adr", to: "superseded", label: "ADR-NNN supersedes ADR-MMM" },
    { from: "accepted", to: "deprecated", label: "контекст исчез" },
  ]}
/>

## Почему ПОЧЕМУ — это ценность

Рассмотрим две записи об одном решении:

**Запись A (коммит-сообщение):** «Переключить кросс-модульное взаимодействие на доменные события».

**Запись B (ADR):** «Контекст: модуль billing вызывает REST API модуля ordering синхронно во время генерации инвойсов. При всплесках объёма заказов это вызывает каскадные таймауты. Мы оценили три варианта: (1) добавить очередь между REST-вызывающими, (2) переключиться на доменные события на внутрипроцессной шине, (3) переработать billing так, чтобы он владел read-моделью данных заказов. Мы отклонили (1), потому что он добавляет операционную сложность (брокер для управления) без устранения зацепления. Мы отклонили (3), потому что ownership данных заказов в billing нарушает принципы владения данными (ADR-004). Решение: принимаем доменные события на внутрипроцессной шине (без брокера). Последствия: кросс-модульные вызовы становятся асинхронными; логика billing должна обрабатывать события, приходящие не по порядку; мы принимаем, что billing не может немедленно подтвердить текущий статус заказа».

Запись B позволяет будущему инженеру понять: (a) почему проблема существовала, (b) что рассматривалось и отклонялось и почему, (c) какие трейдоффы были сознательно приняты.

<Inset kind="why">
Почему ADR должен фиксировать отклонённые альтернативы? Потому что именно они предотвращают повторное открытие вопроса. Без записи о том, почему вариант (1) был отклонён, следующий инженер, столкнувшись с проблемой, самостоятельно предложит вариант (1). Имея запись, обсуждение начинается с «мы отклонили это по причине X — изменилось ли X?», а не с нуля. Отклонённый путь столь же информативен, что и выбранный.
</Inset>

<Quiz
  id="arch-11-01-quiz-1"
  lessonSlug="01-architecture-decision-records"
  lang="ru"
  question="У команды биллинговой платформы есть принятый ADR (ADR-007) «использовать доменные события для кросс-модульного взаимодействия». Год спустя профилирование производительности показывает, что внутрипроцессная шина создаёт узкое место при 50 000 конкурентных заказов. Старший инженер предлагает заменить её Kafka-брокером. Как правильно поступить с ADR'ами?"
  choices={[
    { label: "Написать новый ADR (ADR-012: «Использовать Kafka-брокер для кросс-модульных событий»), который ссылается на ADR-007 как superseded, документирует новый контекст (узкое место производительности, порог масштаба, трейдофф операционных накладных расходов Kafka), формулирует новое решение и отмечает ADR-007 как «Superseded by ADR-012». ADR-007 никогда не удаляется и не редактируется — он остаётся в журнале решений, чтобы будущие инженеры видели полную историю: что было решено когда, при каких силах и почему это было изменено.", correct: true },
    { label: "Отредактировать ADR-007, добавив новое решение. ADR'ы должны быть живыми документами, эволюционирующими вместе с системой.", misconception: "Редактирование принятого ADR уничтожает историческую запись. ADR фиксирует, что было решено в конкретный момент времени при конкретных силах. Если его отредактировать, будущие инженеры не смогут узнать, каким было исходное решение, какие силы существовали тогда и почему система изменилась. Неизменяемость ADR — это именно то, что делает его полезным как институциональную память. Правильное действие — написать новый ADR, superseding старый." },
    { label: "Удалить ADR-007, поскольку он больше не актуален, и написать ADR-012 с нуля без ссылки на старое решение.", misconception: "Удаление ADR'ов убирает исторический контекст, который нужен будущим инженерам. Последовательность решений — и причины каждого — именно то, что журнал решений призван сохранять. Новый инженер после ADR-012 должен иметь возможность прочитать оба ADR'а и понять: система начала с внутрипроцессной шины (по этим причинам), столкнулась с конкретным узким местом при этой нагрузке, затем перешла на Kafka (с этими новыми трейдоффами)." },
    { label: "Оставить ADR-007 как есть и сделать изменение на Kafka без нового ADR, поскольку решение теперь в коде, а код — лучшая документация.", misconception: "Код показывает, что было выбрано; он не может показать, почему Kafka предпочтена альтернативам (RabbitMQ, Redis Streams, SNS/SQS), какие трейдоффы приняты или каков был порог производительности, ставший причиной изменения. Без ADR следующая проблема с производительностью вызовет те же дебаты заново." },
  ]}
/>

## ADR как артефакт первого класса: система контроля версий и расположение

Легковесный формат ADR специально разработан для хранения рядом с кодом в системе контроля версий. Исходное предложение Найгарда помещает их в `doc/arch/adr-NNN-title.md`. ThoughtWorks Radar (2016) включает Lightweight Architecture Decision Records в список техник, рекомендованных к внедрению, именно потому, что они дёшевы и версионируемы.

Преимущества совместного хранения ADR'ов с кодом:
- **История изменений:** когда меняется код, история ADR показывает, когда и почему было принято решение, коррелируемое с коммитом реализации.
- **Обнаруживаемость:** инженеры, изучающие кодовую базу, находят ADR'ы там, где работают (в репозитории), а не в вики, которая может устареть.
- **Workflow pull request'ов:** ADR можно предложить в pull request'е, рассмотреть вместе с изменением реализации и слить при принятии решения.
- **Долговечность:** вики дрейфуют, страницы Confluence устаревают, Slack-треды архивируются. ADR в репозитории живёт столько, сколько существует кодовая база.

<Inset kind="note">
Формат ADR намеренно минимален. Некоторые команды добавляют разделы (таблицы за/против, оценки рисков, ссылки на RFC-обсуждения). Это нормально, пока присутствует основная информация: контекст, решение, последствия. Формат Найгарда — минимум, а не максимум. Провальный режим — усложнить формат до такой степени, что написание ADR'ов становится настолько обременительным, что никто их не пишет. Двухабзацный ADR бесконечно лучше отсутствия ADR.
</Inset>

<Quiz
  id="arch-11-01-quiz-2"
  lessonSlug="01-architecture-decision-records"
  lang="ru"
  question="Команда хранит архитектурные решения в Confluence. Инженер утверждает, что Confluence лучше ADR'ов в репозитории, поскольку предлагает богатое форматирование, поиск и комментарии. Старший инженер не согласен. Каков наиболее важный структурный аргумент против Confluence для записей решений?"
  choices={[
    { label: "Важнейший аргумент — долговечность и корреляция с кодом. Страницы Confluence могут редактироваться без следа (нет неизменяемой истории), могут стать недоступными (смена прав, миграция организации) и не могут коррелироваться с конкретным коммитом, реализовавшим решение. ADR в репозитории версионируется: можно выполнить `git log doc/adr/`, чтобы увидеть все решения в хронологическом порядке, и `git blame` для корреляции решения с PR реализации. Когда экземпляр Confluence мигрирует или команда теряет доступ, история решений исчезает. Код переживает вики.", correct: true },
    { label: "Confluence на самом деле лучше для ADR'ов, поскольку предлагает более богатый инструментарий. Инженер, аргументирующий в пользу Confluence, прав.", misconception: "Богатое форматирование — второстепенная забота. Критические свойства ADR — неизменяемость (решения не редактируются без следа), долговечность (запись переживает инструмент) и корреляция с кодом. Confluence не справляется с долговечностью и корреляцией. Простой Markdown-файл в репозитории долговечен, версионируем и коррелирован с коммитами реализации — ни одно из этих свойств Confluence не предоставляет нативно." },
    { label: "Аргумент против Confluence в том, что он медленнее для написания, чем Markdown. Скорость — главный фактор, определяющий, пишут ли ADR'ы вообще.", misconception: "Скорость написания — второстепенная забота. Основная структурная проблема Confluence в том, что он не предоставляет свойств контроля версий, которые делают ADR'ы полезными: неизменяемая история, обнаруживаемость рядом с кодом и корреляция с коммитами реализации." },
    { label: "Оба формата равнозначны для записей решений. Команда должна выбрать тот, который предпочитают инженеры.", misconception: "Они не равнозначны. Версионируемые ADR'ы в репозитории обеспечивают неизменяемость, долговечность и корреляцию с кодом. Confluence не предоставляет ни одного из этих свойств. Выбор не эстетический — он определяет, будет ли запись решения доступна и точна через пять лет и можно ли её коррелировать с реализацией." },
  ]}
/>

## Какие решения требуют ADR

Не каждое решение нуждается в ADR. Сигнал: потребовалось бы опытному инженеру, присоединившемуся к команде через два года, знать, почему это решение было принято — и смог бы он это понять из кода?

Решения, требующие ADR:
- Выбор архитектурного стиля (событийно-ориентированный vs request-response, modular monolith vs микросервисы)
- Значимые технологические выборы (конкретное хранилище событий, брокер сообщений, ORM)
- Отклонённые альтернативы, которые скорее всего будут предложены снова («мы рассматривали X и отклонили, потому что…»)
- Решения с неочевидными последствиями или принятыми трейдоффами
- Решения, ограничивающие будущие опции («мы не сможем перейти на X, не переделав Y»)

Решения, не нуждающиеся в ADR:
- Рутинные решения реализации (соглашения об именовании, структура файлов внутри модуля)
- Идиоматические паттерны фреймворка (использование DI, потому что фреймворк его использует)
- Решения, очевидные из кода и домена

Пример для биллинговой платформы: «Использовать PostgreSQL» вероятно не требует ADR, если команда — Rails-шоп с PostgreSQL по умолчанию. «Использовать PostgreSQL event sourcing вместо специализированного хранилища событий типа EventStoreDB» — точно требует ADR, поскольку есть неочевидные силы (операционная привычность к PG, стоимость эксплуатации EventStoreDB, проблема удаления при GDPR), которые будущие инженеры не смогут вывести из кода.

<Quiz
  id="arch-11-01-quiz-3"
  lessonSlug="01-architecture-decision-records"
  lang="ru"
  question="Технический лид биллинговой платформы просматривает список кандидатов на ADR и находит четыре варианта. Какой из них наиболее явно требует ADR?"
  choices={[
    { label: "«Мы используем TypeScript path aliases для принудительного соблюдения границ модулей вместо отдельной библиотеки архитектурных тестов». Это решение имеет неочевидные силы (ArchUnit только для Java, ts-arch добавляет зависимость, path aliases уже есть в инструментарии TypeScript), отклонённые альтернативы (ts-arch, только правила ESLint, отсутствие принудительного соблюдения) и принятые трейдоффы (path aliases предотвращают резолюцию, но не утверждают направление зависимостей — циклические импорты между модулями всё ещё возможны). Будущий инженер, видящий секцию paths в tsconfig.json, не сможет вывести ничего из этого из кода.", correct: true },
    { label: "«Мы используем async/await вместо коллбэков в наших Node.js-сервисах». Это широко принятая идиома Node.js без значимых архитектурных трейдоффов.", misconception: "Это рутинное решение реализации. Async/await — идиоматический стиль Node.js; силы и последствия хорошо понятны любому Node.js-инженеру. Это не ограничивает будущие опции неочевидным образом, и будущие инженеры не нуждаются в объяснении, почему использовался async/await." },
    { label: "«Мы называем таблицы базы данных в snake_case». Это соглашение об именовании с минимальным архитектурным последствием.", misconception: "Соглашения об именовании — рутинные решения реализации. snake_case — конвенция PostgreSQL; выбор идиоматичен и не требует ADR. Нет неочевидных трейдоффов или отклонённых альтернатив, о которых будущий инженер должен знать." },
    { label: "«Мы используем Prettier для форматирования кода». Это инструментальное решение, влияющее на рабочий процесс разработчика.", misconception: "Выборы инструментов, такие как форматтер, — рутинные и обратимые. Силы (согласованность команды, принудительное соблюдение в CI) очевидны из конфигурационных файлов. Это не влияет на архитектуру таким образом, что будущий инженер должен это понимать для структурных решений." },
  ]}
/>

</Explanation>

<KeyTakeaway>ADR неизменяем после принятия — никогда не редактируется, только superseded. Его ценность — ПОЧЕМУ: контекст, отклонённые альтернативы и принятые последствия. Храни ADR'ы с версионированием в репозитории рядом с кодом, которым они управляют. Отклонённые пути не менее важны, чем выбранный: они предотвращают повторное открытие уже рассмотренных решений.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-11-01-retrieval"
  lang="ru"
  questions={[
    { q: "Каковы пять разделов формата ADR по Найгарду и почему раздел «Контекст» наиболее важен?", a: "Заголовок (короткая фраза в повелительном наклонении), Статус (Proposed/Accepted/Superseded/Deprecated), Контекст (силы и обстоятельства, обусловившие решение), Решение (сделанный выбор), Последствия (принятые трейдоффы). Контекст наиболее важен, поскольку объясняет, почему решение было необходимо — проблему, которую оно решало, существовавшие ограничения, силы, тянувшие в разные стороны. Без контекста решение выглядит произвольным. С контекстом будущие инженеры могут судить, применимы ли исходные силы до сих пор и стоит ли пересматривать решение." },
    { q: "Каков жизненный цикл статуса ADR и почему принятые ADR'ы никогда не удаляются и не редактируются?", a: "Proposed → Accepted → Superseded (by ADR-NNN) или Deprecated. Принятые ADR'ы никогда не редактируются, поскольку редактирование уничтожает историческую запись — будущим инженерам нужно видеть, что было решено в конкретный момент времени при конкретных силах. При изменении решения новый ADR supersedes старый и ссылается на него. Старый ADR остаётся в журнале. Это создаёт цепочку: ADR-007 принят (когда и почему), ADR-012 supersedes ADR-007 (новый контекст, новые силы, новые трейдоффы). Удаление или редактирование разрывает цепочку и вынуждает восстанавливать историю из разрозненных источников." },
    { q: "Почему ADR'ы должны храниться в репозитории кода, а не в вики или системе управления проектами?", a: "Три причины: неизменяемость (версионируемые файлы имеют историю изменений; вики можно редактировать без следа), долговечность (репозиторий переживает экземпляры Confluence, миграции организаций и смену инструментов) и корреляция (git log и git blame позволяют видеть, когда было принято решение и коррелировать его с коммитом реализации). Страница Confluence, верная в 2022 году, могла быть неверно отредактирована в 2024 году — оригинал не восстановить. ADR в git сохраняет оригинал с полным авторством и датой." },
  ]}
/>

<Recap lang="ru">
Трёхдневные «раскопки» команды биллинговой платформы — восстановление решения, принятие которого заняло час — это режим отказа, который ADR'ы предотвращают. Журнал решений — не бюрократия; это институциональная память, сделанная явной и долговечной.

Формат намеренно минимален: контекст, решение, последствия. Неизменяемость намеренная: принятые ADR'ы superseded, а не редактируются. Расположение намеренное: рядом с кодом в системе контроля версий, там, где работают инженеры и где запись переживёт любую вики.

Важнейшая часть ADR — отклонённые альтернативы. Без них каждый инженер, столкнувшийся с той же проблемой, заново предложит те же альтернативы. С ними команда может начать с существующего анализа и спросить: «Достаточно ли изменился контекст, чтобы пересмотреть это?»

ADR'ы — мост между архитектурой в её нынешнем виде и архитектурой в её становлении. Без них каждое структурное решение выглядит неизбежным. С ними инженеры понимают, что каждое решение было трейдоффом — и могут оценить, применимы ли силы, его обусловившие, по сей день.
</Recap>
