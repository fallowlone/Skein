---
slug: "02-fitness-functions-and-evolutionary-architecture"
lang: ru
track: "architecture-patterns"
unit: "11-evolution-and-decisions"
order: 2
title: "Fitness Functions и эволюционная архитектура"
summary: "Fitness function — автоматизированный тест того, что архитектурное свойство по-прежнему выполняется: wrong-way detector, который ломает сборку при нарушении, не позволяя архитектуре дрейфовать под давлением дедлайнов."
estMin: 22
status: ready
lessonType: topic
level: senior
concepts:
  - fitness-function
  - evolutionary-architecture
  - architectural-characteristic
  - cyclic-dependency-check
  - latency-budget
  - continuous-vs-triggered
prereqs: []
sources:
  - https://www.oreilly.com/library/view/building-evolutionary-architectures/9781492097532/
  - https://www.thoughtworks.com/radar/techniques/evolutionary-architecture
  - https://www.archunit.org/
  - https://martinfowler.com/articles/building-evolutionary-architectures.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import continuousVsTriggered from "./infographics/continuous-vs-triggered.svg?raw";

<Hook>
Через восемнадцать месяцев после того, как B2B биллинговая платформа выстроила modular monolith с принудительными границами, команда наняла двенадцать новых инженеров. Правила модулей были в ADR и в онбординговой документации. Правила ESLint для ограничения импортов существовали — но никто не проверял их четыре месяца, поскольку шаг линтинга был случайно удалён из CI-пайплайна при рефакторинге Dockerfile. Никто не заметил, потому что сборка продолжала выдавать зелёный результат.

Когда кто-то запустил архитектурные тесты вручную, обнаружилось 23 нарушения границ в шести модулях. Billing импортировал из внутренностей ordering. Модуль notifications напрямую вызывал слой репозитория модуля inventory. Хуже того: нарушения были задеплоены в продакшн. Архитектурная структура, которую команда строила месяцами, была разрушена — молча, инкрементально, одним ярлыком за другим — пока CI-пайплайн сообщал о зелёном статусе.

Проблема была не в правилах. Проблема была в том, что ничто не измеряло непрерывно, выполняются ли правила по-прежнему.
</Hook>

<Crux>Fitness function — автоматизированный тест архитектурного свойства: wrong-way detector, ломающий сборку при его нарушении.</Crux>

<Explanation>

## Что такое fitness function

Термин пришёл из книги Нила Форда, Ребекки Парсонс и Патрика Куа «Building Evolutionary Architectures» (O'Reilly, 2017). Они позаимствовали его из эволюционных вычислений, где fitness function оценивает, насколько хорошо кандидат-решение удовлетворяет требованиям задачи.

В архитектуре **fitness function** — любой механизм, обеспечивающий объективную, автоматизированную оценку того, выполняется ли архитектурная характеристика по-прежнему. Это не документация. Это не чеклист для code review. Это запускается в CI и выдаёт pass или fail.

Архитектурные характеристики — иногда называемые «-ilities» — это нефункциональные свойства, которые архитектура должна сохранять:
- **Модульность:** ни один модуль не импортирует из внутренностей другого
- **Задержка:** p99 времени ответа billing API не превышает 200 мс
- **Отсутствие циклических зависимостей:** нет циклов между модулями на уровне пакетов
- **Безопасность:** чувствительные данные не попадают в логи
- **Масштабируемость:** система может обработать 10 000 генераций инвойсов в минуту

Fitness function превращает эти характеристики из пожеланий в гейты сборки.

<FlowDiagram
  label="Fitness function как CI-гейт на архитектуру"
  caption="Fitness function запускается внутри CI. Когда изменение кода нарушает архитектурное свойство — нарушение границы, цикл, регрессия задержки — fitness function ломает сборку до того, как нарушение достигнет продакшна. Разработчик видит сбой немедленно, пока изменение ещё локальное и дешёвое для исправления."
  nodes={[
    { id: "code-change", label: "Изменение кода", sub: "PR или push" },
    { id: "ci", label: "CI-пайплайн", sub: "сборка, тесты, линтинг" },
    { id: "fitness-fn", label: "Fitness function", sub: "проверка архитектурного свойства" },
    { id: "pass", label: "Pass", sub: "свойство выполняется" },
    { id: "fail", label: "Fail", sub: "нарушение обнаружено — сборка заблокирована" },
    { id: "production", label: "Продакшн", sub: "получает только соответствующий код" },
  ]}
  edges={[
    { from: "code-change", to: "ci", label: "запускает" },
    { from: "ci", to: "fitness-fn", label: "выполняет" },
    { from: "fitness-fn", to: "pass", label: "свойство выполняется" },
    { from: "fitness-fn", to: "fail", label: "нарушение обнаружено" },
    { from: "pass", to: "production", label: "деплой продолжается" },
  ]}
/>

## Atomic vs holistic fitness functions

Форд, Парсонс и Куа выделяют два измерения fitness functions: область охвата и частота.

**Atomic fitness functions** тестируют одну архитектурную характеристику изолированно:
- «Нет циклических зависимостей между модулями» — одно правило ArchUnit
- «Нет прямых импортов из внутреннего пакета другого модуля» — правило ts-arch или ESLint
- «Все методы публичного API возвращают ответ в пределах 50 мс в unit-тестах» — assertion производительности в тест-сьюте

**Holistic fitness functions** тестируют комбинацию характеристик, проявляющихся только совместно:
- «При 10 000 RPM система поддерживает p99 < 200 мс И частоту ошибок < 0,1% И отсутствие OOM» — нагрузочный тест в CI
- «Задеплоенная система проходит сканирование безопасности И аудит зависимостей И отсутствие кросс-граничного доступа в продакшн-трейсах» — составной гейт

Большинство команд начинают с atomic fitness functions, потому что они дёшевы в написании и быстры в выполнении. Holistic functions дороже, но выявляют эмерджентные режимы отказа, которые атомарные тесты пропускают.

<Inset kind="why">
Почему это называется «evolutionary architecture»? Потому что fitness functions обеспечивают *управляемое инкрементальное изменение*. Без fitness functions архитектура деградирует через накопленные ярлыки — каждое отдельное изменение мало и объяснимо, но суммарный эффект — разрушение. С fitness functions каждое изменение оценивается против архитектурных свойств, которые команда взяла на себя обязательство поддерживать. Архитектура может эволюционировать — добавляются новые паттерны, убираются старые — но только в направлениях, сохраняющих важные свойства. Fitness function делает изменение безопасным. Это разница между «мы эволюционировали архитектуру намеренно» и «архитектура дрейфовала, пока кто-то не заметил».
</Inset>

<Quiz
  id="arch-11-02-quiz-1"
  lessonSlug="02-fitness-functions-and-evolutionary-architecture"
  lang="ru"
  question="Архитектурная команда биллинговой платформы хочет добавить fitness function для предотвращения нарушений границ. Есть два предложения: (A) Добавить тест ArchUnit, утверждающий «ни один класс в billing.internal не может импортировать из ordering.internal» — запускается в unit-тест-сьюте при каждом коммите. (B) Добавить архитектурный чеклист, который старшие инженеры заполняют в каждом PR. Что из этого является fitness function и почему это важно?"
  choices={[
    { label: "Вариант A является fitness function. Fitness function должна быть автоматизированной и объективной — она выдаёт pass/fail без человеческого суждения. Вариант B (чеклист) зависит от человеческого ревью, которое непоследовательно, пропускаемо и не непрерывно. Нарушения на биллинговой платформе накопились именно потому, что человеческое ревью не является непрерывным. Тест ArchUnit запускается при каждом коммите, ловит нарушения немедленно в момент их введения и не может быть пропущен без отключения теста — нарушение ломает сборку до того, как его можно слить.", correct: true },
    { label: "Вариант B лучше, потому что старшие инженеры могут поймать нюансированные нарушения, которые автоматизированные тесты пропускают.", misconception: "Обнаружение нюансированных нарушений старшими инженерами ценно, но это code review, а не fitness function. Code review не является непрерывным, объективным и автоматизированным. Fitness function ловит объективные, измеримые нарушения, не требующие суждения. Нарушения на биллинговой платформе не были нюансированными — это были прямые импорты из внутренних пакетов, именно тот вид объективной проверки, с которой автоматизированный инструмент справляется надёжно." },
    { label: "Оба являются fitness functions. Любой механизм, оценивающий архитектурное качество, подходит.", misconception: "Чеклист, заполняемый людьми, не является fitness function по определению Форда/Парсонс/Куа. Критическое свойство — автоматизация: fitness function запускается без вмешательства человека и выдаёт детерминированный результат. Ручной чеклист даёт результаты, варьирующиеся в зависимости от ревьюера, может быть пропущен и не запускается непрерывно. Только вариант A удовлетворяет требованию автоматизации." },
    { label: "Ни один не является fitness function. Fitness functions применимы только к характеристикам производительности, таким как задержка и пропускная способность.", misconception: "Fitness functions применимы к любой архитектурной характеристике, не только к производительности. Модульность (нарушения границ), структура зависимостей (циклы), безопасность (чувствительные данные в логах) и ограничения зацепления — все являются допустимыми целями для fitness functions. ArchUnit, применяющий правила зависимостей пакетов, — классический пример fitness function для модульности." },
  ]}
/>

## Triggered vs continuous fitness functions

Второе измерение по Форду/Парсонс/Куа — частота:

**Triggered fitness functions** запускаются по запросу — в CI, при слиянии, по расписанию. Подходят для дорогостоящих проверок (нагрузочные тесты, полное сканирование безопасности, интеграционные тесты против стейджингового окружения) или для проверок свойств, которые можно верифицировать только против собранного артефакта.

**Continuous fitness functions** запускаются при каждом изменении, в цикле разработки, максимально быстро. Подходят для структурных проверок (правила зависимостей, циклические зависимости, ограничения импортов), которые можно вычислить по исходному коду без сборки или деплоя.

Для проблемы нарушений границ биллинговой платформы правильны **continuous** fitness functions: проверка ArchUnit или ts-arch запускается в unit-тест-сьюте при каждом коммите, обнаруживая нарушение в момент его введения. Нет задержки между нарушением и обнаружением.

Нагрузочный тест, верифицирующий p99 < 200 мс при 10 000 RPM, по необходимости **triggered** — он требует живого окружения и занимает минуты. Запускается ежесуточно или перед релизом, а не при каждом коммите.

<Infographic
  svg={continuousVsTriggered}
  label="Сравнение continuous и triggered fitness functions по пяти измерениям: когда запускаются, стоимость, нужна ли сборка, тип проверяемого свойства и задержка обнаружения. Continuous запускаются при каждом коммите примерно за 2 секунды по исходному коду, мгновенно ловя структурные нарушения; triggered запускаются по расписанию и занимают минуты против собранного окружения, верифицируя эмерджентные свойства вроде нагрузочного теста."
  caption="Стоимость проверки определяет её частоту: структурная проверка за ~2 секунды стоит на гейте каждого коммита, а 8-минутный нагрузочный тест запускается ежесуточно — запуск при каждом коммите убил бы скорость разработки."
  hue="var(--d-infra)"
/>

<Quiz
  id="arch-11-02-quiz-2"
  lessonSlug="02-fitness-functions-and-evolutionary-architecture"
  lang="ru"
  question="У команды биллинга три предложенные fitness functions: (1) Правило ArchUnit, утверждающее отсутствие циклических зависимостей между модулями — занимает 2 секунды в тест-сьюте. (2) Нагрузочный тест, утверждающий p99 < 150 мс при 5 000 конкурентных запросов инвойсов — занимает 8 минут против стейджингового окружения. (3) Сканирование уязвимостей зависимостей — занимает 30 секунд. Как их классифицировать как triggered vs continuous и с какой частотой запускать каждую?"
  choices={[
    { label: "(1) Continuous — запускается при каждом коммите как часть unit-тест-сьюта. Быстрая (2 с), не требует внешнего окружения, ловит нарушения немедленно. (2) Triggered — запускается ежесуточно или перед релизом. Требует живого стейджингового окружения и занимает 8 минут; запуск при каждом коммите замедлил бы сборку до непригодности. (3) Triggered — запускается ежесуточно или по расписанию, не при каждом коммите. 30 секунд приемлемы в CI, но база данных уязвимостей меняется только при появлении новых CVE, что делает непрерывное выполнение расточительным. Запускать ежесуточно или при изменениях файлов зависимостей.", correct: true },
    { label: "Все три должны быть continuous — каждая fitness function должна запускаться при каждом коммите для немедленного выявления нарушений.", misconception: "Fitness functions имеют реальную стоимость. 8-минутный нагрузочный тест при каждом коммите означает 10-минутный цикл обратной связи для каждого инженерного изменения. Continuous fitness functions должны быть быстрыми (секунды, не минуты) и не требовать внешней инфраструктуры. Более медленные проверки запускаются по расписанию или на конкретных событиях. Цель — максимум сигнала при минимуме трения, а не максимум покрытия независимо от стоимости." },
    { label: "Все три должны быть triggered по еженедельному расписанию для баланса между покрытием и скоростью разработки.", misconception: "Еженедельное расписание для проверки циклических зависимостей означает, что нарушение, введённое в понедельник, не будет обнаружено до следующего понедельника — к этому моменту поверх него могут быть шесть других изменений. Структурные проверки (1) должны быть continuous, потому что они дёшевы и их ценность — в немедленной обратной связи." },
    { label: "(1) и (3) должны быть triggered при слиянии PR; (2) — запускаться ежесуточно. Все проверки требуют CI-окружения.", misconception: "Тесты ArchUnit (1) не требуют CI-окружения — они запускаются как unit-тесты на локальной машине и в CI. Отделение их от обычного тест-сьюта (делая их только при слиянии PR) создаёт лаг: нарушения, введённые между последним полным запуском и слиянием, могут быть использованы другими инженерами. Continuous-проверки, запускающиеся локально и в CI, ловят нарушения в самый ранний возможный момент." },
  ]}
/>

## Реализация fitness functions: практические примеры

**ArchUnit (Java) — направление зависимостей:**
```java
// Тест падает, если любой класс в billing импортирует из ordering.internal
@AnalyzeClasses(packages = "com.platform")
class ModuleBoundaryTest {
  @ArchTest
  static final ArchRule billingMayNotImportOrderingInternals =
    noClasses().that().resideInAPackage("..billing..")
      .should().dependOnClassesThat()
      .resideInAPackage("..ordering.internal..");
}
```

**ts-arch (TypeScript) — отсутствие циклов:**
```typescript
// Тест падает, если существует циклическая зависимость между модулями
describe("architecture", () => {
  it("has no cyclic module dependencies", async () => {
    const result = await tsarch.FileSystem
      .fromConfig("tsconfig.json")
      .onlyFiles(["src/**/*.ts"])
      .noCycles();
    expect(result).toEqual([]);
  });
});
```

**Бюджет задержки (assertion в unit-тесте):**
```typescript
it("invoice generation completes within 100ms", async () => {
  const start = performance.now();
  await billingService.generateInvoice(testOrderId);
  const elapsed = performance.now() - start;
  expect(elapsed).toBeLessThan(100);
});
```

Общее: каждый — тест, выдающий pass/fail. Каждый запускается в CI. Каждый ловит конкретное архитектурное свойство. Свойство ранее поддерживалось только человеческой конвенцией; fitness function превращает его в машинно-применяемый инвариант.

<Inset kind="note">
Нарушения границ на биллинговой платформе были обнаружены в конечном счёте — но только после месяцев нахождения в продакшне. Стоимость исправления была пропорциональна тому, как долго они существовали: шесть модулей имели нарушения, некоторые несущие нагрузку. Запускайся fitness function непрерывно, каждое нарушение было бы обнаружено в течение часов после введения, когда задействовано изменение только одного инженера. Стоимость исправления нарушения — O(1) при немедленном обнаружении; она растёт примерно линейно с количеством изменений, наложенных поверх него.
</Inset>

<Quiz
  id="arch-11-02-quiz-3"
  lessonSlug="02-fitness-functions-and-evolutionary-architecture"
  lang="ru"
  question="После исправления 23 нарушений границ команда рассматривает добавление fitness functions. Джуниор-инженер предлагает: «Можно добавить fitness functions прямо сейчас, без рефакторинга нарушений? Тогда они будут падать, и мы будем исправлять по ходу». Почему это рассуждение неверно?"
  choices={[
    { label: "Рассуждение неверно, потому что падающая fitness function в CI блокирует все слияния — не только те, что вводят новые нарушения. Если fitness function добавлена в кодовую базу с 23 нарушениями, CI будет падать при каждом PR каждого инженера, пока все 23 нарушения не исправлены. Это парализует команду: никакая фича не может быть слита, пока существуют архитектурные нарушения. Правильный подход: сначала исправить нарушения, добавить fitness function в чистую кодовую базу — тогда она ловит будущие нарушения, не блокируя текущую работу.", correct: true },
    { label: "Рассуждение верно. Добавление fitness function первым создаёт видимое давление для исправления нарушений. Падающая сборка мотивирует команду приоритизировать исправления.", misconception: "Видимое давление — плохая замена приоритизированной работе. Если сборка падает уже три недели из-за известных нарушений, инженеры будут локально пропускать падающий тест или добавлять его в список исключений — fitness functions теряют доверие при длительном игнорировании. Правильный подход: исправить нарушения перед добавлением гейта, чтобы гейт начинал чисто и оставался чистым. «Сборка всё равно всегда падает» — самый быстрый способ уничтожить доверие к CI." },
    { label: "Оба подхода равнозначны. Важно, что fitness function существует, а не когда она добавлена.", misconception: "Время добавления существенно. Fitness function, добавленная в сломанную кодовую базу, создаёт нездоровое состояние CI с первого дня. Инженеры учатся, что сбой CI не значим (он был до их изменения). Fitness function теряет ценность сигнала. Начало с чистой базы — исправить нарушения, добавить гейт, поддерживать зелёный — сохраняет сигнал: падающая fitness function означает, что именно это изменение ввело нарушение, а не что нарушения существуют в каком-то историческом бэклоге." },
    { label: "Рассуждение неверно, потому что fitness functions не могут обнаруживать уже существующие нарушения — они обнаруживают только новые, введённые после добавления функции.", misconception: "Fitness functions обнаруживают все нарушения в текущей кодовой базе, а не только новые. ArchUnit отметит каждое существующее нарушение границ при первом запуске. Именно поэтому порядок важен: запустить fitness function на текущей кодовой базе для инвентаризации нарушений, исправить их все, затем добавить функцию в CI, чтобы она начинала с чистого базиса." },
  ]}
/>

</Explanation>

<KeyTakeaway>Fitness function — автоматизированный, объективный тест того, что архитектурное свойство по-прежнему выполняется: запускается в CI и ломает сборку при нарушении. Atomic функции проверяют одно свойство (правило зависимостей, бюджет задержки, отсутствие циклов); holistic — эмерджентные комбинации. Continuous запускаются при каждом коммите; triggered — по расписанию или событию. Вместе они превращают «архитектуру» из конвенции, разрушающейся под давлением, в машинно-применяемый инвариант.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-11-02-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое fitness function и чем она отличается от чеклиста code review или архитектурного документа?", a: "Fitness function — автоматизированный, объективный механизм, тестирующий, выполняется ли архитектурная характеристика по-прежнему. Она выдаёт детерминированный pass/fail без человеческого суждения. Чеклист code review зависит от человеческого ревью — непоследовательного, пропускаемого и не непрерывного. Архитектурный документ описывает намерение, но не измеряет, выполняется ли оно. Только fitness function запускается при каждом коммите, ловит нарушения немедленно и не может быть обойдена без отключения проверки. Ключевые свойства: автоматизирована (нет человека в цикле), объективна (pass/fail, не суждение) и непрерывна (запускается с каждым изменением)." },
    { q: "В чём разница между atomic и holistic fitness functions? Приведи пример каждой.", a: "Atomic fitness function тестирует одну архитектурную характеристику изолированно — быстрая, дешёвая, сфокусированная. Пример: правило ArchUnit, утверждающее, что ни один класс в billing.internal не импортирует из ordering.internal. Holistic fitness function тестирует комбинацию характеристик, проявляющихся только совместно — медленнее, но ловит системные отказы. Пример: нагрузочный тест, утверждающий, что при 10 000 RPM система поддерживает p99 < 200 мс И частоту ошибок < 0,1% И отсутствие OOM. Эти свойства нельзя верифицировать независимо — они проявляются из взаимодействия кеширования, пула соединений с БД и поведения GC под реальной нагрузкой." },
    { q: "Почему «evolutionary architecture» требует fitness functions, а не просто хорошей документации и ревью старших инженеров?", a: "Потому что архитектура разрушается инкрементально — один ярлык за раз, каждый индивидуально объяснимый, суммарно деструктивный. Хорошая документация и ревью старших инженеров ломаются под давлением дедлайнов: инженеры пропускают чеклист, ревьюеры одобряют «небольшое исключение», шаг линтинга CI удаляется при рефакторинге. Fitness functions не имеют давления дедлайнов. Они запускаются при каждом коммите и не делают исключений. Evolutionary architecture — это архитектура, которая изменяется намеренно: fitness functions определяют конверт, в котором изменение безопасно. Без них «эволюционная» означает «дрейфует, пока кто-то не заметит». С ними — «управляемое инкрементальное изменение, сохраняющее важные свойства»." },
  ]}
/>

<Recap lang="ru">
Молчаливая деградация биллинговой платформы — 23 нарушения границ, накопленные за месяцы при зелёном CI — это режим отказа, который fitness functions предотвращают. Нарушения вводились не небрежными инженерами; их вводили инженеры, не получавшие немедленной обратной связи о пересечении границы.

Fitness function превращает архитектурное намерение в машинно-применяемый инвариант. Тест ArchUnit не доверяет инженерам помнить правила; он ловит нарушения в момент их введения — до любого code review, до любого деплоя, пока изменение ещё локальное и дешёвое для исправления.

Evolutionary architecture — не архитектура, которая дрейфует. Это архитектура, изменяющаяся намеренно, под руководством fitness functions, определяющих свойства, достойные сохранения. Fitness function дополняет ADR: ADR фиксирует, почему свойство было выбрано; fitness function применяет, что свойство по-прежнему поддерживается.

Практическая последовательность: написать ADR для фиксации решения, реализовать fitness function для его применения, запускать fitness function непрерывно для обнаружения дрейфа. Когда fitness function начинает падать по принципиальной причине — силы изменились, новый паттерн заменяет старый — написать новый ADR для superseding исходного, обновить fitness function и продолжить.
</Recap>
