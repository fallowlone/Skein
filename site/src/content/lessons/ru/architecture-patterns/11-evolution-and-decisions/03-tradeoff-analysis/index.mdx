---
slug: "03-tradeoff-analysis"
lang: ru
track: "architecture-patterns"
unit: "11-evolution-and-decisions"
order: 3
title: "Анализ трейдоффов"
summary: "ATAM-lite: sensitivity points, tradeoff points, риски — словарь для рассуждений о конкурирующих -ilities под реальными ограничениями. Закрывающий синтез: каждый паттерн — это ответ на трейдофф, нет лучшей архитектуры, есть только fit-for-forces."
estMin: 25
status: ready
lessonType: topic
level: senior
concepts:
  - tradeoff-point
  - sensitivity-point
  - atam
  - architectural-risk
  - quality-attribute
  - fitness-for-forces
prereqs: []
sources:
  - https://resources.sei.cmu.edu/library/asset-view.cfm?assetid=513908
  - https://www.sei.cmu.edu/our-work/projects/display.cfm?customel_datapageid_4050=21328
  - https://www.oreilly.com/library/view/fundamentals-of-software/9781492043447/
  - https://martinfowler.com/architecture/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import atamVocabulary from "./infographics/atam-vocabulary.svg?raw";

<Hook>
B2B биллинговая платформа работала на event-sourced агрегате заказов два года. Архитектурный ревью-борд получил запрос: «Нам нужно право на удаление по GDPR для данных клиентов. Клиент может запросить удаление всей своей персональной информации в течение 30 дней».

Инженер, построивший event store, сказал: «Это невозможно. Весь смысл event sourcing в том, что журнал неизменяем. События заказов содержат имя клиента, адрес и платёжные данные. Мы не можем их удалить, не переписывая историю».

Юрист ответил: «Невозможно — не вариант. Статья 17 GDPR не опциональна».

Комната зашла в тупик. Оба были правы. Команда приняла ряд индивидуально разумных архитектурных решений — event sourcing для аудируемости, неизменяемый журнал для надёжности, богатые события для воспроизведения — и совокупность этих решений сделала обязательное юридическое требование структурно невыполнимым без дорогостоящей миграции.

Проблема была не в том, что event sourcing был неправильным выбором. Проблема была в том, что команда никогда не анализировала, какие quality attributes решение об event sourcing улучшает, какие ухудшает и какие риски вводит — включая риск применимости регулирования «права на удаление». Они приняли решение, исходя из его преимуществ, и обнаружили его стоимость в продакшне, под юридическим давлением, два года спустя.

Это режим отказа, для предотвращения которого существует анализ трейдоффов.
</Hook>

<Crux>Каждое архитектурное решение — это tradeoff point: оно улучшает одни -ilities и ухудшает другие. ATAM называет структуру.</Crux>

<Explanation>

## Словарь ATAM: sensitivity points, tradeoff points, риски

Architecture Tradeoff Analysis Method (ATAM) разработан в Software Engineering Institute (SEI) Университета Карнеги-Меллон. Полный процесс ATAM тяжеловесен — несколько дней структурированных воркшопов. Но вводимый им словарь легковесен и немедленно полезен для повседневных архитектурных рассуждений.

Три концепции важны:

**Sensitivity point (точка чувствительности):** проектное решение, оказывающее сильный эффект на *одну* quality attribute. Его изменение значительно улучшает или ухудшает эту характеристику, тогда как другие атрибуты относительно не затрагиваются. Пример: партиционирование таблицы событий заказов по customer ID значительно улучшает задержку запросов на отчёты по клиентам, с минимальным эффектом на пропускную способность записи или согласованность данных. Решение чувствительно к задержке.

**Tradeoff point (точка трейдоффа):** проектное решение, влияющее на *несколько* quality attributes в противоположных направлениях. Улучшение одной атрибуты ухудшает другую. Пример: event sourcing улучшает аудируемость и возможности темпоральных запросов — но ухудшает модифицируемость (неизменяемый журнал усложняет удаление по GDPR) и операционную простоту (перестройка проекций, управление снэпшотами). Решение — это tradeoff между аудируемостью и модифицируемостью.

**Риск:** архитектурное решение, которое может привести к тому, что quality attribute не достигнет требуемого уровня в каком-то будущем, правдоподобном, но ещё не реализованном сценарии. Пример: использование журнала событий как источника истины для PII клиентов — это риск, если к системе применяется регулирование права на удаление.

Словарь делает рассуждения точными: вместо «эта архитектура хороша» или «эта архитектура рискована» можно сказать «это решение — tradeoff point между аудируемостью и модифицируемостью — мы принимаем ухудшенную модифицируемость в обмен на аудируемость и должны явно миtigировать риск GDPR».

<Infographic
  svg={atamVocabulary}
  label="Таксономия одного архитектурного решения как трёх концепций ATAM-lite: sensitivity point двигает одну quality attribute, tradeoff point улучшает одни атрибуты, ухудшая другие, а риск — правдоподобный будущий сценарий, в котором деградированный атрибут не достигает требуемого уровня."
  caption="Три слова ATAM-lite о любом решении: sensitivity point (один атрибут), tradeoff point (несколько, в противоположных направлениях), риск (правдоподобный, но ещё не наступивший отказ). Назовите нужное до релиза."
  hue="var(--d-infra)"
/>

<FlowDiagram
  label="Карта tradeoff point для решения об event sourcing"
  caption="Одно архитектурное решение — 'использовать event sourcing для агрегата заказов' — одновременно улучшает аудируемость и темпоральные запросы, ухудшая модифицируемость (неизменяемый журнал) и операционную простоту. Требование удаления по GDPR — это риск: будущий сценарий, где ухудшенная модифицируемость становится блокирующим ограничением."
  nodes={[
    { id: "decision", label: "Использовать event sourcing", sub: "для агрегата заказов" },
    { id: "auditability", label: "Аудируемость +", sub: "полная история сохранена" },
    { id: "temporal", label: "Темпоральные запросы +", sub: "воспроизведение до любого момента" },
    { id: "modifiability", label: "Модифицируемость -", sub: "неизменяемый журнал; удаление PII заблокировано" },
    { id: "ops", label: "Операционная простота -", sub: "перестройка проекций, снэпшоты" },
    { id: "risk", label: "Риск удаления GDPR", sub: "право на удаление может быть юридически обязательным" },
  ]}
  edges={[
    { from: "decision", to: "auditability", label: "улучшает" },
    { from: "decision", to: "temporal", label: "улучшает" },
    { from: "decision", to: "modifiability", label: "ухудшает" },
    { from: "decision", to: "ops", label: "ухудшает" },
    { from: "modifiability", to: "risk", label: "открывает риск" },
  ]}
/>

## Применение ATAM-lite к биллинговой платформе

Команда биллинговой платформы, применяя словарь ATAM ретроспективно к решению об event sourcing, получила бы:

**Quality attributes в области:** аудируемость, задержка, модифицируемость, операционная простота, соответствие регулированию.

**Решение:** хранить агрегат заказов как журнал событий; состояние заказа восстанавливается воспроизведением событий.

**Sensitivity point:** производительность воспроизведения чувствительна к объёму журнала событий. По мере роста журнала время холодного старта воспроизведения растёт пропорционально.

**Tradeoff point:** неизменяемость журнала событий обменивает модифицируемость на аудируемость. Каждое событие сохраняется постоянно — это актив для аудиторских следов и обязательство для требований удаления данных. То же свойство, которое делает систему отличным аудитовым журналом, делает её плохо подходящей для систем, подпадающих под регулирование права на удаление.

**Риск:** если к этой системе применяется регулирование права на удаление, неизменяемый журнал событий сделает соответствие структурно сложным. Риск правдоподобен (GDPR вступил в силу в 2018 году; платформа работает в ЕС) и не смягчён на момент принятия решения.

Документирование этого на момент принятия решения — в ADR для event sourcing — не предотвратило бы проблему GDPR. Но сделало бы трейдофф явным, вынудило бы команду рассмотреть варианты смягчения на этапе проектирования, когда они дешёвы, и дало бы инженеру и юристу общий фрейм для разговора два года спустя.

<Inset kind="why">
Почему важно называть трейдофф, если команда всё равно сталкивается с той же проблемой? Потому что называние меняет временную шкалу и опции. Tradeoff point, выявленный на этапе проектирования, имеет варианты смягчения: шифровать PII-поля и удалять ключ (crypto-shredding), хранить PII в отдельном изменяемом хранилище и ссылаться на него по ID в событиях, проектировать события содержащими только неличные данные заказа. Tradeoff point, обнаруженный в продакшне под юридическим давлением, имеет один вариант: дорогостоящая миграция в условиях дедлайна. Ценность анализа трейдоффов — не в предотвращении трейдоффов; это в обнаружении их на этапе проектирования, когда стоимость их устранения минимальна.
</Inset>

<Quiz
  id="arch-11-03-quiz-1"
  lessonSlug="03-tradeoff-analysis"
  lang="ru"
  question="Команда биллинговой платформы оценивает два варианта кросс-модульного взаимодействия: (A) синхронные REST-вызовы между модулями, (B) доменные события на внутрипроцессной шине. Инженер говорит: «Вариант A проще, вариант B более decoupled». Старший инженер отвечает: «Этот фрейм скрывает tradeoff point — позвольте быть точнее». Как выглядит точный фрейм ATAM?"
  choices={[
    { label: "Вариант A (REST) — это tradeoff point: улучшает операционную простоту (проще трейсировать, отлаживать) и согласованность (синхронные вызовы позволяют легко возвращать ошибки вызывающему) — ухудшая временну́ю развязку (медленный ordering каскадирует задержку в billing) и изоляцию отказов (billing падает, когда ordering недоступен). Вариант B (события) инвертирует это: лучшая временна́я развязка и изоляция отказов, ценой сложности согласованности (billing обрабатывает события асинхронно, может получать их не по порядку) и наблюдаемости (трейсирование запроса через обработчики событий сложнее синхронного стека вызовов). Решение — это tradeoff point между согласованностью/простотой и развязкой/устойчивостью.", correct: true },
    { label: "Старший инженер прав, требуя точности, но фрейм ATAM просто добавляет формальные имена к тому же наблюдению: REST прост, события более decoupled. Анализ не меняет решение.", misconception: "Фрейм ATAM не просто переименовывает наблюдение — он открывает измерения, которые бинарный фрейм скрывает. «Проще» упускает, что простота REST достигается ценой временно́го зацепления (режим каскадного отказа). «Более decoupled» упускает, что событийная развязка вводит сложность согласованности, требующую явной обработки. Точный фрейм определяет конкретные quality attributes, которыми обмениваются, и сценарии, в которых каждый выбор терпит неудачу." },
    { label: "Вариант B (события) всегда правильный архитектурный выбор для кросс-модульного взаимодействия в modular monolith, поскольку избегает проблемы временно́го зацепления.", misconception: "Нет универсально правильного выбора — принцип урока 00. Вариант A может быть правильным, если модули разделяют транзакционную границу (billing должен атомарно упасть вместе с ordering), если у команды слабый инструментарий наблюдаемости для асинхронных потоков, или если требования согласованности делают синхронное подтверждение обязательным. Фрейм ATAM определяет трейдоффы; силы конкретной системы определяют, какие трейдоффы приемлемы." },
    { label: "Оба варианта эквивалентны для modular monolith, поскольку взаимодействие внутрипроцессное и сетевые отказы невозможны.", misconception: "Внутрипроцессное взаимодействие устраняет риск сетевого разделения, но не другие трейдоффы. Временно́е зацепление (медленный путь вызова блокирует вызывающего) применимо к внутрипроцессным вызовам. Изоляция отказов (паника в стеке вызова ordering может обрушить контекст вызова billing) применима внутрипроцессно. Семантика согласованности (синхронная vs асинхронная) применима независимо от сетевой границы." },
  ]}
/>

## Трек как инструментарий трейдоффов

Каждый раздел этого трека вводил структурный паттерн. Каждый паттерн — ответ на трейдофф. Оглядываясь назад:

**Раздел 01 — Coupling и cohesion:** фундаментальные силы. Каждое структурное решение обменивает зацепление (насколько модули знают друг о друге) на связность (насколько сфокусирован каждый модуль). Высокое зацепление обеспечивает разделяемое состояние и низкую задержку ценой независимой эволюции.

**Раздел 02 — Направление зависимостей:** стабильные абстракции (интерфейсы, порты) улучшают модифицируемость, позволяя конкретным реализациям меняться, не затрагивая вызывающих — ценой косвенности и абстракционных накладных расходов. DIP — это tradeoff point между модифицируемостью и простотой.

**Раздел 03 — Слоевая архитектура:** N-tier слоение улучшает разделение ответственностей и тестируемость — но строгая граница слоёв создаёт ловушку transaction-script и fat service layer. Анемичные доменные модели обменивают богатство на простоту.

**Раздел 04 — Hexagonal:** ports and adapters улучшают тестируемость (домен тестируем без инфраструктуры) и модифицируемость (инфраструктура заменяема) ценой структурных накладных расходов.

**Раздел 05 — Clean/Onion:** концентрическое правило зависимостей максимизирует изоляцию домена — ценой шаблонного кода use cases и «налога чистой архитектуры».

**Раздел 06 — DDD:** bounded contexts применяют языковые и модельные границы — улучшая точность модели и автономию команды ценой интеграционных накладных расходов (context maps, ACL, слои трансляции).

**Раздел 07 — CQRS:** разделение моделей чтения и записи улучшает масштабируемость чтения и ясность записи ценой сложности синхронизации. Модель чтения может быть устаревшей.

**Раздел 08 — Event sourcing:** неизменяемый журнал событий улучшает аудируемость и обеспечивает темпоральные запросы — ценой модифицируемости (GDPR), операционной сложности (проекции, снэпшоты) и eventual consistency. Как узнала биллинговая платформа.

**Раздел 09 — Event-driven:** хореография улучшает развязку и устойчивость ценой распределённой трейсируемости и согласованности. Оркестрация улучшает наблюдаемость и согласованность ценой зацепления центрального координатора. Саги обменивают согласованность на доступность.

**Раздел 10 — Decomposition:** modular monolith обменивает независимость деплоя на операционную простоту. Микросервисы обменивают операционную простоту на независимость деплоя и автономию команды. Distributed monolith — режим отказа при неправильном сочетании обоих.

**Раздел 11 — Evolution:** ADR'ы делают ПОЧЕМУ явным; fitness functions применяют, что структурные свойства выполняются. Оба — инвестиции в долгосрочную модифицируемость ценой краткосрочных накладных расходов.

Ни один из этих паттернов не является универсально правильным. Каждый — ответ на конкретный набор сил. Силы — это ограничение: размер команды, сложность домена, регуляторная среда, требования к производительности, организационная структура, скорость изменений. Паттерн — это ответ.

<Quiz
  id="arch-11-03-quiz-2"
  lessonSlug="03-tradeoff-analysis"
  lang="ru"
  question="Стартап строит начальную версию B2B биллинговой платформы. CTO предлагает: «Нам стоит использовать полный DDD с bounded contexts, CQRS для агрегатов заказов и биллинга, event sourcing для аудируемости и modular monolith с принудительными границами с первого дня». Старший инженер отвечает: «Это слишком много архитектуры для наших текущих сил». Используя словарь трейдоффов из этого урока, в чём конкретный аргумент?"
  choices={[
    { label: "Предложение CTO одновременно вводит несколько tradeoff points — DDD bounded contexts добавляют интеграционные накладные расходы (ACL, слои трансляции), CQRS добавляет сложность синхронизации, event sourcing добавляет управление проекциями и риск GDPR — в обмен на quality attributes (аудируемость, масштабируемость, автономия команды), которые стартапу с одной командой, малым объёмом заказов и без требований аудита ещё не нужны. Каждый паттерн стоит модифицируемости и краткосрочной продуктивности в обмен на долгосрочные свойства. Когда силы не требуют этих свойств, стоимость — чистые накладные расходы. Аргумент старшего инженера: «Наши текущие силы — простота и скорость. Эти паттерны отвечают силам, которых у нас ещё нет. Начать с layered monolith, добавить bounded contexts при понимании домена, добавить CQRS при наличии узкого места read/write, добавить event sourcing только если аудируемость становится жёстким требованием — и зафиксировать это решение в ADR с явным указанием tradeoff GDPR».", correct: true },
    { label: "CTO прав. Начать с полной архитектурой всегда лучше, чем добавлять её ретроспективно. Стоимость добавления архитектуры постфактум превышает накладные расходы добавления заранее.", misconception: "Это игнорирует YAGNI применительно к архитектуре. Каждый паттерн в списке CTO имеет реальную стоимость: DDD требует инвестиций в доменное моделирование; CQRS требует логики синхронизации; event sourcing требует инфраструктуры проекций. Эти стоимости несутся немедленно. Преимущества (аудируемость, масштабируемость, автономия команды) материализуются только при возникновении сил, их мотивирующих. Стартап, никогда не достигший масштаба, при котором эти свойства важны, заплатил полную стоимость без выгоды." },
    { label: "Старший инженер прав, но по неверной причине. Проблема в том, что event sourcing и CQRS слишком сложны для реализации любой командой.", misconception: "Утверждение, что event sourcing и CQRS слишком сложны для любой команды, слишком сильное. Команды успешно используют оба паттерна, когда силы их обосновывают. Конкретный аргумент старшего инженера не в том, что паттерны слишком сложные, — а в том, что они отвечают силам, которых у стартапа сейчас нет. Если бы силы присутствовали (регуляторные требования аудита, высокая асимметрия чтения/записи, несколько команд, владеющих разными агрегатами), те же паттерны были бы уместны." },
    { label: "Два инженера спорят о мнениях, а не о фактах. Архитектурные решения нельзя оценить объективно.", misconception: "Архитектурные решения нельзя оценить относительно универсального оптимума, но можно оценить относительно сил конкретного контекста. Фрейм ATAM делает этот анализ конкретным: определить quality attributes, которые архитектура должна поддерживать, определить tradeoff points каждого решения, оценить, соответствуют ли трейдоффы текущим силам. Это не мнение — это структурированные рассуждения. Аргумент старшего инженера можно сформулировать точно и проверить относительно реальных требований стартапа." },
  ]}
/>

## Мета-принцип: fitness for forces, а не универсальные best practices

Раздел 00 ввёл фрейм: нет лучшей архитектуры, есть только fit-for-forces. Это закрывающий тезис трека.

Каждый паттерн в этом треке имеет контекст, в котором он — правильный ответ, и контекст, в котором он — неправильный. Силы, делающие hexagonal architecture правильной для domain-heavy приложения (тестируемость, независимость от инфраструктуры, долгосрочная модифицируемость), отсутствуют в простом CRUD-сервисе. Силы, делающие event sourcing правильным для аудит-критичной финансовой системы (неизменяемая история, темпоральные запросы, соответствие регулированию), — это в точности силы, делающие его неправильным для потребительского приложения, подпадающего под право на удаление, где модифицируемость персональных данных — жёсткое требование.

Задача архитектора — не применить наиболее сложный доступный паттерн. Это — определить силы и выбрать структурный ответ, соответствующий этим силам с наименьшим количеством лишних трейдоффов.

ADR'ы и fitness functions — инструменты, делающие этот процесс долговечным:
- ADR фиксирует, какие силы присутствовали, какие трейдоффы были приняты и почему — чтобы будущие инженеры могли оценить, применимы ли силы по-прежнему.
- Fitness function применяет, что структурные свойства, взятые на себя командой, по-прежнему поддерживаются — чтобы архитектура не дрейфовала от намерения.

Архитектура — это глагол. Это непрерывный процесс оценки сил, выбора структурных ответов, фиксации обоснования и поддержания важных свойств. Вот что означает «архитектура — это глагол».

<Inset kind="note">
Полный процесс ATAM включает несколько воркшопов, выявление бизнес- и архитектурных драйверов, прохождение сценариев и план смягчения рисков. Фрейм «ATAM-lite» здесь — это словарь: sensitivity points, tradeoff points, риски — применяемый неформально в проектных обсуждениях и ADR'ах. Не нужно проводить формальный воркшоп ATAM, чтобы извлечь пользу из словаря. Точное использование терминов в повседневных архитектурных разговорах даёт большую часть пользы при малой доле стоимости.
</Inset>

<Quiz
  id="arch-11-03-quiz-3"
  lessonSlug="03-tradeoff-analysis"
  lang="ru"
  question="Биллинговая платформа выросла до 15 команд. Архитектурная команда предлагает декомпозировать modular monolith на микросервисы. Старший инженер хочет провести ATAM-lite анализ перед решением. Используя sensitivity points, tradeoff points и риски, что выявляет строгий анализ?"
  choices={[
    { label: "Sensitivity points: независимость деплоя высоко чувствительна к этому решению — микросервисы обеспечивают независимые каденции деплоя на команду, что является сильной силой при 15 командах. Tradeoff points: (1) независимость деплоя vs операционная сложность — микросервисы обеспечивают независимые деплои, но требуют service discovery, distributed tracing и независимых CI/CD пайплайнов; (2) автономия команды vs распределённая согласованность данных — каждый сервис, владеющий своими данными, улучшает автономию команды, но вводит eventual consistency и challenges распределённых транзакций; (3) устойчивость vs задержка — сетевые вызовы вводят задержку и режимы частичного отказа, отсутствующие в монолите. Риски: (1) если границы модулей в текущем монолите не чистые (зацепление по данным, зависимости в тестах), преждевременное извлечение создаёт distributed monolith; (2) если у команд нет опыта эксплуатации микросервисов, операционная сложность ухудшает доступность, а не улучшает её. Анализ не отвечает «стоит ли декомпозировать?» — он даёт команде словарь для решения.", correct: true },
    { label: "Анализ излишен. При 15 командах микросервисы всегда правильный ответ, поскольку Закон Конвея предсказывает, что архитектура будет отражать организацию.", misconception: "Закон Конвея описывает тенденцию, а не предписание. Закон Конвея говорит, что архитектура будет стремиться отражать коммуникационную структуру — но правильным ответом может быть изменение коммуникационной структуры (топология команд), а не построение микросервисов. Modular monolith с 15 командами и чёткими границами модулей может работать, если модули хорошо ограничены и пайплайн деплоя быстр. Анализ ATAM — именно инструмент для определения, оправдывают ли организационные силы архитектурные трейдоффы декомпозиции." },
    { label: "ATAM-анализ должен заключить, что монолит следует сохранить. Операционная сложность микросервисов всегда превышает пользу при любом размере команды.", misconception: "Это зеркальная ошибка предыдущей: универсальное предписание в противоположном направлении. Операционная сложность микросервисов может или не может превышать пользу — это зависит от конкретных сил. При 15 командах, всех деплоящих один монолит, накладные расходы на координацию синхронизированных деплоев могут реально превышать операционные накладные расходы независимых сервисов. Анализ не заключает заранее; он открывает трейдоффы, чтобы решение было принято с явными силами." },
    { label: "ATAM-lite применим только к начальному решению об архитектуре. После того как система в продакшне, архитектурные решения принимаются через эволюционные изменения, а не формальный анализ.", misconception: "Словарь ATAM-lite применим к любому значимому архитектурному решению в любой момент жизненного цикла системы. Предложение декомпозировать 2-летний монолит на микросервисы — крупное архитектурное решение с tradeoff points quality attributes. Evolutionary architecture не означает отказ от анализа; это означает инкрементальные изменения под руководством fitness functions и задокументированные в ADR'ах, что требует понимания трейдоффов каждого изменения." },
  ]}
/>

</Explanation>

<KeyTakeaway>Sensitivity point — решение, сильно влияющее на одну quality attribute; tradeoff point — влияющее на несколько в противоположных направлениях. Каждый паттерн в этом треке — tradeoff point. Задача архитектора — не применить наиболее сложный паттерн, а определить силы и выбрать ответ, соответствующий им. ADR'ы фиксируют, какие трейдоффы были приняты и почему; fitness functions применяют, что взятые на себя свойства по-прежнему поддерживаются.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-11-03-retrieval"
  lang="ru"
  questions={[
    { q: "В чём разница между sensitivity point и tradeoff point в словаре ATAM?", a: "Sensitivity point — проектное решение, сильно влияющее на одну quality attribute: изменение значительно улучшает или ухудшает эту атрибуту, тогда как другие относительно не затрагиваются. Пример: партиционирование журнала событий по customer ID чувствительно к задержке запросов по клиентам. Tradeoff point — решение, влияющее на несколько quality attributes в противоположных направлениях: улучшение одной ухудшает другую. Пример: event sourcing улучшает аудируемость и темпоральные запросы, ухудшая модифицируемость (неизменяемый журнал) и операционную простоту. Различие важно: sensitivity points оптимизируемы (можно настраивать в одном направлении без системных последствий), тогда как tradeoff points требуют сознательного принятия худшей стороны." },
    { q: "Как словарь трейдоффов связан с форматом ADR из предыдущего урока?", a: "Раздел «Последствия» ADR — это именно то место, где принадлежат tradeoff points и риски. Хорошо написанный ADR называет улучшаемые quality attributes, ухудшаемые quality attributes и введённые риски. «Мы принимаем ухудшенную модифицируемость (проблема удаления GDPR) в обмен на улучшенную аудируемость» — это tradeoff point, задокументированный в ADR. «Если к этой системе применяется право на удаление, неизменяемый журнал будет структурно проблематичен» — это риск. Словарь трейдоффов делает раздел «Последствия» точным: вместо расплывчатых «за и против» ADR фиксирует конкретные трейдоффы quality attributes и именованные риски, которые будущие инженеры могут оценить относительно текущих сил." },
    { q: "Каков мета-принцип, закрывающий трек, и как ADR'ы и fitness functions ему служат?", a: "Мета-принцип: нет лучшей архитектуры, есть только fit-for-forces. Каждый структурный паттерн — ответ на конкретные силы (quality attributes, ограничения, организационный контекст). Задача архитектора — определить силы и выбрать ответ, соответствующий им с наименьшим количеством лишних трейдоффов. ADR'ы служат этому принципу, фиксируя, какие силы присутствовали при принятии решения, какие трейдоффы были приняты и почему — чтобы будущие инженеры могли оценить, применимы ли силы по-прежнему. Fitness functions служат ему, применяя, что структурные свойства, взятые на себя командой, по-прежнему поддерживаются по мере эволюции системы — предотвращая молчаливый дрейф, когда архитектура больше не соответствует силам, для которых была спроектирована, но никто этого не заметил." },
  ]}
/>

<Recap lang="ru">
Кризис GDPR на биллинговой платформе был вызван не плохой архитектурой. Он был вызван архитектурой, трейдоффы которой никогда не были сделаны явными. Event sourcing был правильным выбором для аудируемости. Он также был tradeoff point: та же неизменяемость, которая делала аудитовый журнал ценным, делала удаление данных клиентов структурно сложным. Этот трейдофф был обнаруживаем на этапе проектирования. Он не был обнаружен до двух лет спустя, под юридическим давлением, когда исправление стало дорогостоящим.

ATAM-lite даёт словарь для явного формулирования этих трейдоффов до того, как они становятся кризисами: sensitivity points (одна атрибута, одно решение), tradeoff points (несколько атрибутов, противоположные эффекты), риски (правдоподобные будущие сценарии, где деградированная атрибута становится блокирующим ограничением). Словарь не предотвращает трейдоффы — архитектура и есть трейдоффы. Он открывает их на этапе проектирования, когда смягчение дёшево.

Каждый раздел этого трека был конкретным трейдоффом: зацепление vs связность, стабильность vs гибкость, согласованность vs автономия, аудируемость vs модифицируемость, простота vs устойчивость. Паттерны — hexagonal, DDD, CQRS, event sourcing, modular monolith, saga — не ответы для универсального применения. Это ответы на силы. Задача архитектора — точно определить силы, выбрать соответствующий ответ, зафиксировать обоснование в ADR и применять результирующие свойства fitness functions.

Архитектура — это глагол. Это непрерывный процесс оценки сил, сознательного принятия трейдоффов и поддержания важных свойств. Нет архитектуры, правильной навсегда — есть только архитектуры, соответствующие силам, присутствовавшим при их проектировании, и дисциплина пересматривать их при изменении сил.
</Recap>
