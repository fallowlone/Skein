---
slug: "04-context-mapping"
lang: ru
track: "architecture-patterns"
unit: "06-domain-driven-design"
order: 4
title: "Context mapping"
summary: "Карта контекстов документирует интеграцию bounded contexts. Паттерны — Shared Kernel, Customer/Supplier, Conformist, ACL, Open Host Service — описывают командные отношения и стратегии трансляции, а не только форму API."
estMin: 26
status: ready
lessonType: topic
level: senior
concepts:
  - context-map
  - anticorruption-layer
  - shared-kernel
  - open-host-service
  - conformist
  - context-integration
prereqs: []
sources:
  - https://martinfowler.com/bliki/BoundedContext.html
  - https://www.domainlanguage.com/ddd/reference/
  - https://www.informit.com/store/implementing-domain-driven-design-9780321834577
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Sales-контекст B2B-платформы вызывал сторонний сервис расчёта налогов. У сервиса была своя модель «продуктов» — он называл их «taxable items» с полями `commodityCode`, `taxJurisdiction` и `lineNetAmount`. Sales-контекст имел собственную модель `LineItem` с `NegotiatedPrice` и `productId`. Разработчик, интегрирующий два контекста, пошёл путём наименьшего сопротивления: он использовал объектную модель налогового сервиса напрямую в Sales-контексте. Класс `LineItem` обзавёлся полем `commodityCode`. Сервисный метод `Quote`, вызывавший налоговый сервис, стал возвращать `TaxCalculationResult` налогового сервиса напрямую в остальной Sales-контекст. Через три месяца весь Sales-код был усеян полями `commodityCode` и `taxJurisdiction`, которые не значили ничего ни для одного доменного эксперта Sales. Когда провайдер налогового сервиса изменил API — заменив `taxJurisdiction` на enum `jurisdictionCode` — изменение распространилось в основную доменную модель Sales-контекста. Изменение Sales-фичи требовало понимания объектной модели налогового сервиса. Sales-контекст был заражён языком налогового сервиса. Именно это Эванс называет «коррупцией модели через интеграцию». Когда два bounded context интегрируются без явного слоя трансляции, upstream-модель просачивается в downstream-контекст. Ubiquitous language downstream-контекста постепенно замещается языком upstream. Граница контекста растворяется. Карта контекстов — стратегический инструмент, предотвращающий это: диаграмма и набор именованных паттернов, описывающих не только то, как контексты коммуницируют, но какие властные отношения у них есть и какую стратегию трансляции они используют.
</Hook>

<Crux>Карта контекстов именует паттерн интеграции каждой пары контекстов — командные отношения и стратегию трансляции, а не только топологию API.</Crux>

<Explanation>

## Что такое карта контекстов

Карта контекстов — стратегический обзор bounded contexts системы и отношений между ними. Она отвечает на вопросы:

- Какие контексты существуют?
- Как каждый контекст интегрируется с другими?
- Каковы командные отношения в каждой точке интеграции?
- Какая стратегия трансляции предотвращает коррупцию модели?

Карта контекстов — не архитектурная диаграмма сервисов или API. Это **диаграмма команд и моделей**. Стрелки на карте контекстов несут метки, описывающие организационные и модельные отношения — Shared Kernel, Customer/Supplier, Conformist, Anticorruption Layer — а не просто «вызывает» или «отправляет событие в».

Карта контекстов B2B-платформы включает: Sales, Billing, Shipping, Product Catalog, Tax Calculation (внешний). Каждая пара контекстов, которые интегрируются, имеет метку отношения. Метка определяет взаимодействие команд и метод трансляции моделей.

## Паттерны интеграции

### Shared Kernel

Два контекста разделяют небольшое, явно согласованное подмножество доменной модели. Обе команды владеют этой общей частью — изменения в ней требуют координации между обеими командами. Это самая тесная связанность и наибольшие накладные расходы на координацию.

Используйте, когда: два контекста подлинно разделяют ключевой концепт, который дорого дублировать или транслировать. На B2B-платформе value object `Money` (сумма + валюта) может быть элементом Shared Kernel — он нужен и Sales, и Billing, он стабилен, и наличие двух отдельных реализаций `Money`, которые могут тихо расходиться, опасно.

Избегайте для: всего, что эволюционирует с разной скоростью в каждом контексте. Shared Kernel, который одна команда хочет менять, а другая нет, создаёт узкое место координации.

### Customer/Supplier

Один контекст (Supplier) производит данные или сервисы, которые потребляет другой (Customer). Модель Supplier определяет интерфейс. Customer адаптируется к ней. Но критично то, что отношения согласовываются: Customer может запрашивать изменения, Supplier их рассматривает, и интерфейс эволюционирует при явной координации.

Это наиболее распространённый паттерн между внутренними командами. На B2B-платформе Product Catalog — Supplier для Sales: Sales потребляет данные каталога. Команда Product Catalog поддерживает API; команда Sales может запрашивать дополнения или изменения; команда Catalog расставляет приоритеты и планирует их.

Властные отношения важны. Если команда Supplier игнорирует нужды команды Customer, команда Customer вынуждена обходиться вокруг модели Supplier — что нередко приводит к коррупции модели в Customer-контексте.

### Conformist

Customer-контекст не имеет рычагов влияния на Supplier. Supplier не будет менять интерфейс под нужды Customer. Customer просто соответствует: он принимает модель Supplier как есть, включая её язык, формы объектов и жизненный цикл. Ubiquitous language Customer-контекста подчиняется языку upstream.

Это правильный выбор, когда: Supplier — крупная внешняя система, не заинтересованная в ваших потребностях (SaaS-платформа, сторонний поток данных), И модель Supplier достаточно разумна для прямого использования, И стоимость построения слоя трансляции превышает выгоду от сохранения собственной модели.

B2B-платформа может быть Conformist по отношению к платёжному процессору — если API процессора стабилен и его модель «payment», «charge» и «refund» достаточно чисто отображается на концепты Billing, подчинение может быть прагматичным.

Опасность: как только вы становитесь Conformist, вы импортировали ubiquitous language upstream в свой контекст. Ваша модель теперь эволюционирует в темпе Supplier. Дрейф модели в upstream распространяется напрямую в ваш контекст.

### Anticorruption Layer (ACL)

Customer-контекст транслирует между моделью Supplier и своей собственной моделью, защищая свой ubiquitous language. ACL — явный компонент трансляции: он живёт на интеграционной границе, говорит на языке Supplier снаружи и на языке Customer внутри.

Именно этот паттерн должен был использовать Sales-контекст B2B-платформы с налоговым сервисом. ACL транслировал бы: `LineItem` → `TaxableItem` (при вызове налогового сервиса), `TaxCalculationResult` → `QuoteTaxSummary` (при возврате результата в Sales). Модель Sales-контекста никогда не видела бы `commodityCode` или `taxJurisdiction`. ACL поглощает трансляцию.

ACL — выбор по умолчанию при интеграции с внешними системами, модель которых вы не контролируете и чей язык существенно отличается от вашего.

<Inset kind="why" lang="ru">
Почему «anti-corruption»? Потому что альтернатива — позволить upstream-модели просочиться в ваш контекст — Эванс описывает как «коррупцию» вашей доменной модели. Паттерн Conformist — осознанный выбор принять эту коррупцию, потому что стоимость построения слоя трансляции не оправдана. ACL — явный отказ от этой коррупции: мы потратим инженерные усилия на поддержку собственной модели, а не позволим языку upstream заменить наш. Стоимость ACL реальна (код трансляции для написания, поддержки и тестирования). Выгода — ваш контекст сохраняет свою модель: она эволюционирует в ответ на ваших доменных экспертов, а не на изменения API вашего upstream.
</Inset>

### Open Host Service (OHS) и Published Language (PL)

Когда контекст является Supplier для многих downstream-контекстов, он может формализовать свой интеграционный интерфейс как **Open Host Service**: чётко определённый протокол, с которым может интегрироваться любой downstream без индивидуальных переговоров. OHS стабилен, версионирован и спроектирован для потребления несколькими потребителями.

**Published Language** — формальный, хорошо задокументированный формат данных, используемый OHS: обычно версионированная схема событий, публичный API-контракт или стандартный формат. На B2B-платформе контекст Product Catalog может предоставлять Published Language из событий товаров (`ProductCreated`, `ProductPriceChanged`, `ProductDiscontinued`) со стабильной схемой, которую потребляют Sales, Billing и Shipping. Схема версионирована; о критических изменениях сообщается заранее.

Паттерн OHS + PL снижает накладные расходы на координацию в отношениях Customer/Supplier при масштабировании — Supplier не договаривается с каждым потребителем индивидуально; он поддерживает опубликованный контракт.

<FlowDiagram
  label="Карта контекстов B2B-платформы: паттерны интеграции между всеми контекстами"
  caption="Каждая стрелка представляет именованное отношение контекстного маппинга. ACL = Anticorruption Layer (Sales защищает свою модель от Tax-сервиса). OHS/PL = Open Host Service / Published Language (Product Catalog публикует стабильные события). C/S = Customer/Supplier (согласованный интерфейс). Conformist = Billing принимает модель платёжного процессора как есть."
  nodes={[
    { id: "sales", label: "Sales BC", sub: "upstream: Product Catalog (C/S), Tax Service (ACL)" },
    { id: "billing", label: "Billing BC", sub: "upstream: Sales (C/S), Payment Processor (Conformist)" },
    { id: "shipping", label: "Shipping BC", sub: "upstream: Sales (C/S), Product Catalog (OHS)" },
    { id: "catalog", label: "Product Catalog BC", sub: "OHS+PL: публикует схему ProductEvents" },
    { id: "tax", label: "Tax Service (внешний)", sub: "переговоры невозможны" },
    { id: "payment", label: "Payment Processor (внешний)", sub: "стабильная модель, нет рычагов влияния" },
  ]}
  edges={[
    { from: "catalog", to: "sales", label: "C/S (согласованный)" },
    { from: "catalog", to: "shipping", label: "OHS/PL (опубликованные события)" },
    { from: "sales", to: "billing", label: "C/S (событие OrderConfirmed)" },
    { from: "sales", to: "shipping", label: "C/S (событие OrderConfirmed)" },
    { from: "tax", to: "sales", label: "ACL (Sales транслирует)" },
    { from: "payment", to: "billing", label: "Conformist (Billing адаптируется)" },
  ]}
/>

## Чтение карты контекстов: власть и трансляция

Самое важное, что коммуницирует карта контекстов, — не топология API, а **властные отношения** между командами и **стратегия трансляции** на каждой границе.

Властные отношения: в Customer/Supplier Supplier имеет рычаги влияния. Если Supplier отзывчив и команды хорошо коммуницируют, Customer может эволюционировать. Если Supplier не отвечает (крупная платформенная команда со многими клиентами), Customer в конечном итоге становится Conformist по умолчанию — он не может получить нужные изменения, поэтому адаптирует свою модель под ограничения Supplier.

Стратегия трансляции: ACL означает «мы транслируем на границе, наша модель защищена». Conformist означает «мы приняли upstream-модель». Shared Kernel означает «мы совместно владеем этой частью». Эти выборы имеют накапливающиеся последствия на протяжении лет. Команда, ставшая Conformist десять лет назад, сейчас имеет внутреннюю модель, глубоко связанную с дизайном API upstream-сервиса образца 2015 года.

<Inset kind="note" lang="ru">
Карта контекстов — не единоразовая диаграмма. Её следует обновлять при добавлении новой интеграции, изменении отношения (Customer/Supplier становится Conformist, потому что Supplier перестаёт отвечать) или разделении/слиянии контекста. Карта — живой документ организационных и архитектурных решений команды. Устаревшая карта контекстов так же вредна, как устаревшие комментарии в коде — она описывает систему, которой больше нет.
</Inset>

<Quiz
  id="arch-06-04-quiz-1"
  lessonSlug="04-context-mapping"
  lang="ru"
  question="Sales-контекст интегрируется с внутренним Inventory-контекстом. Sales-команда хочет проверять наличие товара перед подтверждением Quote. Inventory-команда говорит: «Используйте наш API напрямую — просто вызовите наш эндпоинт InventoryItem и используйте возвращаемый нами enum InventoryStatus». Тимлид Sales говорит: «Нам нужен ACL». Inventory-команда возражает: «Зачем добавлять накладные расходы трансляции для внутреннего сервиса? Это расточительно». Как принять решение?"
  choices={[
    { label: "Решение зависит от того, принадлежат ли модель Inventory-контекста — InventoryItem, InventoryStatus — ubiquitous language Sales-контекста. Если доменные эксперты Sales говорят о «наличии» и «остатках», а не об «inventory items» и «inventory status» — прямое использование модели Inventory импортирует чужой язык в Sales. Каждый Sales-разработчик должен понимать концепты Inventory, чтобы читать Sales-код. Когда Inventory переименует значения InventoryStatus или изменит поля InventoryItem — Sales должен меняться тоже. ACL транслирует: InventoryStatus.IN_STOCK Inventory становится ProductAvailability.AVAILABLE Sales-контекста — Sales говорит на своём языке внутренне. Накладные расходы реальны, но ограничены, и они защищают модель Sales от эволюции в темпе Inventory.", correct: true },
    { label: "ACL нужен только для внешних сторонних сервисов, а не для внутренних сервисов между командами.", misconception: "Необходимость в ACL определяется тем, совпадает ли модель upstream с ubiquitous language downstream, а не тем, является ли сервис внутренним или внешним. Внутренние сервисы, построенные разными командами с разными доменными экспертами, могут иметь такое же расхождение моделей, как и внешние. Многие из наиболее разрушительных случаев коррупции модели происходят из внутренних сервисов, где команды предполагали, что «можно просто разделить модель», потому что работают в одной организации." },
    { label: "Sales-команде следует стать Conformist и использовать модель Inventory напрямую — это сокращает код, а внутренние сервисы должны разделять модели.", misconception: "Conformist — осознанный стратегический выбор, а не умолчание для внутренних сервисов. Выбор Conformist означает принятие того, что модель Sales-контекста будет эволюционировать в темпе Inventory-команды, что ubiquitous language Sales-контекста будет включать концепты Inventory и что изменения API Inventory распространятся напрямую в Sales. Описывать это как «сокращение кода» скрывает реальную стоимость: Sales-контекст теряет автономию модели." },
    { label: "Используйте Shared Kernel: обе команды соглашаются на общий тип AvailabilityStatus, который обе используют, избегая накладных расходов трансляции.", misconception: "Shared Kernel для статуса наличия означает, что обе команды должны координироваться при каждом изменении общего типа. Если Inventory нужно добавить новое значение статуса (например, RESERVED_FOR_RECALL), им нужно договориться с Sales перед внесением изменения. Shared Kernel уместен, когда две команды подлинно совместно владеют концептом. Наличие — это концепт Inventory, который потребляет Sales. Ответственность за трансляцию чётко на потребителе (Sales) — это именно то место, где она должна быть." },
  ]}
/>

<Quiz
  id="arch-06-04-quiz-2"
  lessonSlug="04-context-mapping"
  lang="ru"
  question="Product Catalog-контекст публикует событие `ProductPriceChanged` со схемой версии v1. Через шесть месяцев команда Catalog хочет добавить поле `previousPrice` к событию. Sales и Shipping — оба потребители. Что требует от команды Catalog паттерн Open Host Service / Published Language?"
  choices={[
    { label: "Добавление поля к схеме события — некритическое изменение (потребители могут игнорировать новое поле): команда Catalog может задеплоить его без координации с командами потребителей, но должна уведомить их и обновить документацию Published Language. Если бы изменение было критическим (переименование поля, изменение типа, удаление поля) — паттерн OHS/PL требует версионирования: опубликовать v2 схемы, поддерживать v1 в течение периода deprecation, координировать с потребителями миграцию до вывода v1. Ценность Published Language в том, что потребители могут полагаться на стабильность схемы — критические изменения без версионирования разрушают это доверие.", correct: true },
    { label: "Команда Catalog должна получить явное одобрение от Sales и Shipping перед любым изменением схемы, даже аддитивным.", misconception: "Паттерн OHS/PL снижает накладные расходы на координацию для аддитивных изменений именно тем, что делает контракт публичным и стабильным. Аддитивные изменения (новые необязательные поля) не ломают существующих потребителей — их можно задеплоить в одностороннем порядке с уведомлением в changelog. Требование явного одобрения для каждого аддитивного изменения нивелирует смысл Published Language: оно откатывает к переговорам Customer/Supplier для каждого изменения." },
    { label: "Команда Catalog не должна версионировать события — версионирование создаёт дублирующий код, и потребители должны всегда использовать последнюю схему.", misconception: "Невезионированные схемы событий с критическими изменениями вынуждают всех потребителей обновляться одновременно или ломаются. В системе с несколькими независимыми командами синхронизированные деплои по всем потребителям операционно дороги и чреваты ошибками. Версионирование схемы позволяет каждому потребителю мигрировать в своём темпе, сохраняя обратную совместимость в переходный период. Стоимость «дублирующего кода» версионирования реальна, но значительно меньше стоимости координации принудительных синхронизированных деплоев." },
    { label: "Поскольку Sales и Shipping являются Conformist по отношению к Catalog — они должны немедленно обновить свой код при изменении схемы.", misconception: "Потребление Sales и Shipping событий Product Catalog не делает их Conformist — скорее всего, это отношение Customer в Customer/Supplier или OHS. Conformist означает принятие upstream-модели в собственную доменную модель downstream без слоя трансляции. Если Sales и Shipping имеют собственную модель ценообразования товаров и транслируют события Catalog в свои собственные доменные события через ACL — они не являются Conformist и защищены от изменений схемы upstream на своей границе." },
  ]}
/>

<Quiz
  id="arch-06-04-quiz-3"
  lessonSlug="04-context-mapping"
  lang="ru"
  question="Два года назад Billing-контекст стал Conformist по отношению к внутреннему Payments-контексту, импортировав типы `PaymentMethod`, `ChargeResult` и `RefundPolicy` Payments-команды напрямую в доменную модель Billing. Payments-команда теперь куплена другой компанией и их сервис заменяется сторонним процессором. Billing-контексту нужно мигрировать. Почему эта миграция значительно сложнее, чем если бы Billing использовал ACL?"
  choices={[
    { label: "Поскольку Billing является Conformist, модель Payments встроена по всей его доменной модели — сигнатуры методов, типы событий, столбцы базы данных и тестовые фикстуры — все ссылаются на `PaymentMethod`, `ChargeResult` и `RefundPolicy`. Миграция на новый процессор означает изменение каждой из этих ссылок на эквивалентные типы нового процессора. Если бы у Billing был ACL, модель Payments существовала бы только внутри компонента трансляции ACL. Миграция была бы: переписать ACL для трансляции между доменной моделью Billing и API нового процессора. Доменная модель Billing сама — её агрегаты, доменные события, ubiquitous language — осталась бы нетронутой. Изолированные ACL-миграции ограничены одним компонентом; Conformist-миграции неограниченно распространяются по всей кодовой базе.", correct: true },
    { label: "Миграция была бы одинаково сложной с ACL, потому что ACL тоже нужно было бы переписать для нового процессора.", misconception: "ACL действительно нужно переписать — это его задача. Но ACL — один ограниченный компонент. Переписывание одного компонента категорически отличается от обновления ссылок, разбросанных по всей доменной модели. Ценность ACL в том, что радиус взрыва от изменения upstream ограничен одним компонентом трансляции. При Conformist радиус взрыва распространяется так далеко, как проникла upstream-модель в downstream-кодовую базу." },
    { label: "Подход Conformist был верным, потому что тогда сократил код — повышенная стоимость миграции является приемлемым trade-off.", misconception: "Это аргумент trade-off, который правомерен только если он был явно оценён в то время. «Сократил код тогда» — краткосрочный выигрыш; «стоимость миграции два года спустя» — долгосрочная стоимость. DDD-фреймворк требует осознанного trade-off: Conformist верен когда upstream-модель стабильна и стоимость трансляции действительно высока. Использование Conformist по умолчанию как «сокращение кода» без оценки риска миграции — не trade-off, а отложенная стоимость." },
    { label: "Проблема не в Conformist vs ACL — проблема в том, что внутренние сервисы никогда не должны заменяться внешними.", misconception: "Внутренние сервисы регулярно заменяются внешними — замена кастомной реализации платежей на Stripe или Adyen является обычным бизнес-решением. Архитектурный вопрос не в том, должно ли это происходить, а в том, насколько хорошо система структурирована для поглощения этого. Изоляция ACL делает систему устойчивой к изменениям upstream вне зависимости от того, является ли upstream внутренним или внешним." },
  ]}
/>

</Explanation>

<KeyTakeaway>Карта контекстов именует паттерн интеграции на каждой контекстной границе — Shared Kernel, Customer/Supplier, Conformist, ACL, Open Host Service. Каждый паттерн описывает как властные отношения команд, так и стратегию трансляции. ACL — защита по умолчанию от коррупции модели upstream-контекстами; Conformist — осознанное решение принять эту коррупцию, когда стоимость трансляции перевешивает выгоду независимости модели.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-06-04-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое карта контекстов и что она коммуницирует помимо топологии API?", a: "Карта контекстов — стратегическая диаграмма всех bounded contexts системы и именованных отношений между ними. Помимо топологии API (какой сервис что вызывает), она коммуницирует: властные отношения команд на каждой границе (имеет ли downstream-команда рычаги влияния для запроса изменений от upstream?), стратегию трансляции (защищает ли downstream свою модель с ACL, или соответствует upstream-модели?), и организационные последствия каждого выбора интеграции. Customer/Supplier означает, что команды договариваются. Conformist означает, что downstream принимает модель и темп эволюции upstream. ACL означает, что downstream инвестирует в трансляцию для защиты собственного языка. Это архитектурные решения с накапливающимися последствиями — выбор Conformist в первый год формирует стоимость миграции на третий год." },
    { q: "В чём разница между Conformist и Anticorruption Layer и когда уместен каждый?", a: "Оба описывают интеграцию downstream-контекста с upstream, модель которого он не может изменить. Разница в стратегии трансляции. Conformist: downstream принимает модель upstream напрямую в свою доменную модель — нет слоя трансляции. Язык upstream становится частью ubiquitous language downstream. Уместен, когда модель upstream стабильна, достаточно чисто отображается на концепты downstream и стоимость построения слоя трансляции не оправдана. ACL: downstream поддерживает собственную модель и транслирует на границе. Язык upstream существует только в компоненте ACL; доменная модель downstream изолирована. Уместен, когда модель upstream существенно расходится с языком downstream, или когда upstream вероятно изменит свою модель способами, которые распространились бы в домен downstream. ACL — более дорогой выбор заранее; Conformist — более дорогой выбор долгосрочно при изменении upstream." },
    { q: "Что такое паттерн Open Host Service и как он снижает накладные расходы координации по сравнению с Customer/Supplier?", a: "В Customer/Supplier-отношении Supplier договаривается об интерфейсе с каждым Customer индивидуально. По мере роста числа Customers это становится узким местом координации — каждый Customer хочет чего-то немного другого, и Supplier должен управлять очередью запросов. Паттерн Open Host Service формализует интерфейс Supplier как публичный, стабильный, версионированный протокол, с которым любой Consumer может интегрироваться без прямых переговоров. Published Language определяет схему явно (версионированные события, OpenAPI-контракт). Аддитивные изменения обратно совместимы и деплоятся без координации с потребителями. Критические изменения версионируются — v2 публикуется наряду с v1, потребители мигрируют в своём темпе, v1 deprecates после сообщённого дедлайна. OHS/PL обменивает гибкость индивидуальных переговоров на масштабируемость опубликованного контракта." },
  ]}
/>

<Recap lang="ru">
Коррупция модели из вступительной истории — Sales-контекст, заполненный `commodityCode` и `taxJurisdiction` от внешнего налогового сервиса — была не случайностью. Это было предсказуемым результатом интеграции без именованной стратегии. Разработчик пошёл путём наименьшего сопротивления: использовать upstream-модель напрямую. Граница bounded context растворилась.

Карта контекстов именует стратегию в каждой точке интеграции. Shared Kernel: обе команды совместно владеют небольшой частью. Customer/Supplier: согласованный интерфейс, downstream имеет рычаги. Conformist: downstream осознанно принимает upstream-модель. ACL: downstream транслирует на границе и защищает свою модель. Open Host Service: upstream публикует стабильный версионированный контракт для нескольких потребителей.

Каждый паттерн описывает командные отношения, а не только технический облик. Ценность ACL не в избегании кода — а в том, что радиус взрыва изменения upstream — один компонент трансляции, а не вся доменная модель. Опасность Conformist не теоретическая — она материализуется когда модель upstream меняется спустя годы.

Карта контекстов рисуется один раз и непрерывно обновляется. Это стратегическая картина, говорящая каждой команде: вот от кого вы зависите, сколько у вас власти и какова ваша стратегия защиты модели.
</Recap>
