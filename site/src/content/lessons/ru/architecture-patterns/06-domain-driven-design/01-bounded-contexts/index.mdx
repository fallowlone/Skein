---
slug: "01-bounded-contexts"
lang: ru
track: "architecture-patterns"
unit: "06-domain-driven-design"
order: 1
title: "Bounded contexts"
summary: "Bounded context — явная граница, внутри которой живёт одна согласованная доменная модель. Одно и то же слово — Order, Customer, Product — означает разную модель в каждом контексте. Bounded context — основная единица декомпозиции в DDD."
estMin: 24
status: ready
lessonType: topic
level: senior
concepts:
  - bounded-context
  - subdomain
  - ubiquitous-language
  - model-consistency
  - strategic-ddd
  - domain-decomposition
prereqs: []
sources:
  - https://martinfowler.com/bliki/BoundedContext.html
  - https://www.domainlanguage.com/ddd/reference/
  - https://www.informit.com/store/implementing-domain-driven-design-9780321834577
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
На B2B-платформе заказов была одна сущность «Order», разделённая на весь бэкенд. У неё было 47 полей. Команда продаж нуждалась в Order для фиксации согласованных позиций, уровней скидок и закреплённого менеджера. Команда биллинга нуждалась в Order для отслеживания сумм счетов, условий оплаты и налоговой юрисдикции. Команда доставки нуждалась в Order для адреса доставки, размеров упаковки и предпочтений перевозчика. Каждая команда добавляла поля в один и тот же класс. Класс рос. Поля для Sales ничего не значили для Billing. Поля для Billing запутывали сервис Shipping. Правила валидации конфликтовали: Sales говорил, что Order валиден с момента подписания соглашения; Billing — только когда подтверждён способ оплаты; Shipping — только когда склад взял товар в работу. Три команды, три определения «валидного Order», все борются за один класс. Старший архитектор, пришедший в команду, задал один вопрос: «Вы правда думаете, что Sales, Billing и Shipping говорят об одном и том же, когда произносят "Order"?» Нет. Сущность имела три разных идентичности, слитых в одну. Починка состояла не в добавлении флагов валидации. Она состояла в проведении трёх явных границ и выдаче каждой команде собственной модели.
</Hook>

<Crux>Bounded context — явная граница одной согласованной доменной модели: одно и то же слово означает разное в каждом контексте, и это намеренно.</Crux>

<Explanation>

## Центральная проблема: одно слово — много моделей

Domain-driven design начинается с наблюдения, на котором спотыкается почти каждая большая система: естественный язык неоднозначен. Слово «Customer» в CRM означает контакт с историей продаж, владельца отношений и лид-скор. В биллинговой системе «Customer» — это аккаунт со способом оплаты, кредитным лимитом и непогашенными счетами. В системе доставки «Customer» — это адрес доставки и контактный телефон.

Если в вашем коде один класс `Customer`, разделяемый всеми тремя системами, произойдёт одно из двух. Либо класс вырастет и вберёт в себя все три смысла (God-объект на 60 полей, который ни одна команда полностью не понимает), либо класс несёт смысл той команды, которая написала его первой, и каждая другая команда постоянно адаптируется вокруг полей, которые к ней не относятся.

Прозрение Эрика Эванса в *Domain-Driven Design* (2003): попытка объединить это в одну модель — не цель, а ошибка. У каждого поддомена свой язык, и этот язык порождает свою модель. Цель — не единая унифицированная модель, а набор моделей, каждая из которых внутренне согласована в своих границах.

**Bounded context** — это граница. Явное объявление: «Внутри этого контекста слово "Customer" означает именно вот это, слово "Order" означает именно вот это, и эта модель внутренне согласована». За пределами контекста то же слово может означать что-то другое — и это нормально, потому что граница сделана явной.

<FlowDiagram
  label="Одно слово — три модели: Order в контекстах Sales, Billing и Shipping"
  caption="Слово «Order» появляется в трёх bounded contexts. У каждого контекста своя модель с разными полями, правилами валидации и жизненным циклом. Стрелки показывают интеграционные точки между контекстами — они не разделяют один класс Order. Каждая стрелка — точка кросс-контекстной интеграции, а не общий импорт."
  nodes={[
    { id: "sales", label: "Sales Context", sub: "Order: позиции, скидка, менеджер, статус соглашения" },
    { id: "billing", label: "Billing Context", sub: "Order: сумма счёта, условия оплаты, налоговая юрисдикция" },
    { id: "shipping", label: "Shipping Context", sub: "Order: адрес доставки, размеры, перевозчик, статус сборки" },
    { id: "integration", label: "Интеграционный слой", sub: "переводит между моделями контекстов" },
  ]}
  edges={[
    { from: "sales", to: "integration", label: "событие OrderPlaced" },
    { from: "integration", to: "billing", label: "BillingOrderCreated" },
    { from: "integration", to: "shipping", label: "ShipmentOrderCreated" },
  ]}
/>

## Субдомены vs bounded contexts: пространство проблем vs пространство решений

Эти два термина часто используют как синонимы — напрасно. Разница — между тем, чем занимается бизнес, и тем, как ваш код это организует.

**Субдомен** — часть пространства бизнес-проблем: он обнаруживается в разговорах с доменными экспертами и существует вне зависимости от того, как вы строите ПО. У вашего бизнеса есть субдомен Sales, субдомен Billing, субдомен Shipping. Субдомены бывают ключевыми (core — дифференцируют бизнес), вспомогательными (supporting — необходимы, но не дифференцируют) и обобщёнными (generic — решённые проблемы, покупайте готовое).

**Bounded context** — решение в пространстве решений: оно проектируется вашей командой. Один субдомен может обслуживаться одним bounded context (типичный случай) или несколькими (когда большой ключевой домен требует отдельных команд). Один bounded context может охватывать несколько субдоменов (иногда делается для простых вспомогательных субдоменов).

Эвристика: сначала определите субдомены (в разговорах с бизнесом), затем решите, сколько bounded contexts создать для каждого — исходя из топологии команд, сложности модели и потребностей деплоя.

<Inset kind="why" lang="ru">
Почему разница важна на практике? Потому что команды часто путают их и получают границу bounded context, не совпадающую ни с какой реальной бизнес-границей. Разделение контекстов, продиктованное технологией («у нас будет микросервис для аутентификации») без соответствующего разделения моделей, просто создаёт вызов по RPC там, где раньше был вызов функции — та же логическая модель, теперь распределённая. Граница bounded context значима только когда модели по обе стороны подлинно различаются.
</Inset>

## Что на самом деле содержит bounded context

Bounded context — это не только граница сервиса или базы данных. Это **языковая и модельная граница**, содержащая:

- **Ubiquitous language** — точный словарь, разделяемый разработчиками и доменными экспертами в этом контексте. «Order» в Sales-контексте имеет одно специфическое определение, которое использует каждый разработчик и каждый продакт-менеджер этой команды.
- **Доменная модель** — классы, агрегаты, value objects и правила, реализующие этот словарь. Модель валидна и согласована внутри границы. Ничто снаружи не должно напрямую обращаться к её внутренностям.
- **Явный интерфейс интеграции** — граница явна. Другие контексты интегрируются через события, API или слои трансляции — не импортируя внутренние классы.

Граница применяется в коде: в монолите — границы пакетов, проверяемые архитектурными тестами; в сервисной архитектуре — граница сервиса и есть граница контекста.

## Основная единица декомпозиции

В стратегическом DDD bounded contexts — основная единица декомпозиции. Прежде чем спросить «сколько у нас должно быть микросервисов?», вы спрашиваете: «Сколько bounded contexts имеет этот домен, и какова модель в каждом?»

Каждый bounded context соответствует: одной команде с чётким владением, одному ubiquitous language, разделяемому разработчиками и доменными экспертами, одной кодовой базе (или модулю с принудительными границами), одной единице деплоя (в сервисной архитектуре).

<FlowDiagram
  label="Bounded contexts как единица декомпозиции B2B-платформы"
  caption="Три bounded context, каждый со своей доменной моделью. Пунктирные рамки — границы контекстов: ни один класс их не пересекает. Коммуникация только через явные интеграционные точки. Стрелки — потоки данных на интеграционных границах, а не source-code импорты."
  nodes={[
    { id: "sales-ctx", label: "Sales BC", sub: "владеет: SalesOrder, Quote, AccountExec, Discount" },
    { id: "billing-ctx", label: "Billing BC", sub: "владеет: Invoice, PaymentMethod, TaxRule, BillingOrder" },
    { id: "shipping-ctx", label: "Shipping BC", sub: "владеет: Shipment, Carrier, WarehousePick, ShippingOrder" },
    { id: "catalog-ctx", label: "Product Catalog BC", sub: "владеет: Product, SKU, Price — справочные данные" },
  ]}
  edges={[
    { from: "sales-ctx", to: "billing-ctx", label: "событие OrderConfirmed" },
    { from: "sales-ctx", to: "shipping-ctx", label: "событие OrderConfirmed" },
    { from: "catalog-ctx", to: "sales-ctx", label: "справка Product (чтение)" },
    { from: "catalog-ctx", to: "shipping-ctx", label: "справка Product (чтение)" },
  ]}
/>

<Quiz
  id="arch-06-01-quiz-1"
  lessonSlug="01-bounded-contexts"
  lang="ru"
  question="На B2B-платформе есть один общий класс `Order`, используемый Sales, Billing и Shipping. Класс вырос до 47 полей. Разработчик предлагает: «Разделим на три подкласса — SalesOrder, BillingOrder, ShippingOrder, все наследуют от базового Order». Почему это не решает проблему bounded context?"
  choices={[
    { label: "Потому что наследование удерживает модели связанными на уровне базового класса. Любое изменение базового Order вынуждает перекомпилировать все три подкласса. Важнее то, что все три команды по-прежнему разделяют одну иерархию классов. Изменения Billing-команды в BillingOrder рискуют сломать SalesOrder или ShippingOrder через базовый класс. Bounded context требует, чтобы модель каждого контекста владелась независимо, деплоилась независимо и не имела source-code зависимостей от моделей соседних контекстов. Наследование внутри общего пакета нарушает все три требования.", correct: true },
    { label: "Потому что наследование работает только для value objects, но не для сущностей.", misconception: "Наследование работает как для value objects, так и для сущностей в объектно-ориентированном коде. Проблема не в ограничении на то, что можно наследовать — а в том, что наследование удерживает модели связанными через иерархию классов. Базовый класс — общая зависимость; любое его изменение распространяется на все подклассы всех контекстов." },
    { label: "Потому что bounded contexts требуют микросервисов, а наследование работает только в монолите.", misconception: "Bounded contexts — концепция уровня модели, а не уровня деплоя. Они применимы как в монолите (с принудительными границами пакетов и архитектурными тестами), так и в сервисной архитектуре. Предложение с наследованием было бы одинаково проблематичным в обоих случаях." },
    { label: "Потому что у всех трёх подклассов всё равно осталось бы по 47 полей.", misconception: "Проблема 47 полей — симптом слияния трёх моделей, а не корневая причина. Разделение на подклассы может сократить число полей, но не создаёт независимые модели. Корневая причина — совместное владение и связанность. Правильное разделение по bounded contexts означает три независимые модели с собственными классами, правилами валидации и жизненным циклом — не три подкласса общей базы." },
  ]}
/>

<Quiz
  id="arch-06-01-quiz-2"
  lessonSlug="01-bounded-contexts"
  lang="ru"
  question="Команда выявила три субдомена: управление заказами (ключевой), расчёт налогов (вспомогательный) и email-уведомления (обобщённый). Джуниор предлагает по одному bounded context на субдомен. Старший архитектор говорит: «Для Tax и Email нужно использовать готовое ПО — значит, они вообще не наши bounded contexts». В чём логика архитектора?"
  choices={[
    { label: "Обобщённые и вспомогательные субдомены, решаемые внешним ПО или SaaS, имеют модель, определённую этим ПО, а не вашей командой. Вы интегрируетесь с ними — вы не владеете их моделью. Граница bounded context между ВАШИМ кодом и ИХ системой представлена в вашей кодовой базе Anticorruption Layer, переводящим их модель на ваш язык. Создавать bounded context для них означало бы проектировать собственную модель того, чем вы решили не владеть — что лишает покупку смысла.", correct: true },
    { label: "Вспомогательные и обобщённые субдомены всегда слишком просты, чтобы нуждаться в bounded context.", misconception: "Сложность — не критерий. Вспомогательный субдомен, реализованный собственным кодом, МОЖЕТ и ДОЛЖЕН иметь свой bounded context с собственной моделью и ubiquitous language — особенно если у него есть свой доменный эксперт. Логика архитектора касается именно ВНЕШНИХ субдоменов, модель которых принадлежит вендору." },
    { label: "Bounded contexts применяются только к ключевым субдоменам, потому что только у них есть доменные эксперты.", misconception: "Доменные эксперты есть и у вспомогательных субдоменов — бухгалтеры являются доменными экспертами для расчёта налогов. Разница не в том, есть ли эксперты, а в том, владеет ли ваша команда моделью. Когда вы используете внешнее ПО для субдомена, вы интегрируетесь с его моделью, а не проектируете свою." },
    { label: "Нельзя иметь больше одного bounded context на команду, поэтому архитектор сокращает их число.", misconception: "Нет жёсткого правила об одном bounded context на команду (хотя это распространённая эвристика). Логика архитектора — о владении моделью: если субдомен обрабатывается внешним ПО, команда не владеет и не поддерживает доменную модель для него." },
  ]}
/>

<Quiz
  id="arch-06-01-quiz-3"
  lessonSlug="01-bounded-contexts"
  lang="ru"
  question="Два инженера обсуждают разбивку bounded contexts. Инженер А говорит: «Customer означает одно и то же в Sales и в Customer Support — оба заботятся об одном человеке». Инженер Б говорит: «Но Sales заботится об истории покупок, лид-скоре и менеджере; Support — об открытых тикетах, уровне сервиса и эскалациях — это действительно разные модели». Как определить: один bounded context или два?»"
  choices={[
    { label: "Тест: могут ли модели эволюционировать независимо без координации между командами? Если модели Sales и Support для «Customer» должны меняться в ответ на разные силы (Sales реагирует на требования CRM; Support — на требования тикет-системы), и каждое изменение нужно согласовывать с другой командой — граница неверна. Если Sales может обогащать свою модель Customer, не трогая модель Support, и наоборот — они принадлежат разным контекстам с явным интеграционным контрактом. Доказательства инженера Б — разные поля, разные заботы, разные события жизненного цикла — указывают на две отдельные модели.", correct: true },
    { label: "Если они разделяют customer ID, они должны быть в одном bounded context.", misconception: "Разделение идентификатора — это именно то, как bounded contexts интегрируются, а не причина их объединять. Контексты ссылаются на агрегаты друг друга по ID, не импортируя классы (следующий урок разберёт это подробнее). Общий customer ID — указатель, пересекающий границу контекста: это механизм интеграции, а не свидетельство того, что модели нужно объединить." },
    { label: "Два контекста всегда лучше одного, потому что это снижает связность.", misconception: "Чрезмерное разделение — реальный антипаттерн. Если два «контекста» разделяют одну модель, одну команду, один релизный цикл и должны согласовывать каждое изменение схемы — их разделение создаёт связанность распределённого монолита с накладными расходами на сетевые вызовы. Правильное число контекстов определяется тем, подлинно ли различаются модели и могут ли они эволюционировать независимо." },
    { label: "Используйте один контекст, если обе команды работают с одной таблицей базы данных для customers.", misconception: "Разделение таблиц БД — деталь реализации, не граница модели. Два контекста могут читать из одной таблицы через отдельные слои доступа. Граница контекста — о владении моделью и ubiquitous language, а не о физической компоновке базы данных." },
  ]}
/>

</Explanation>

<KeyTakeaway>Bounded context — явная языковая и модельная граница, внутри которой живёт одна согласованная доменная модель. Одно и то же слово означает разное в каждом контексте — и это правильно. Bounded contexts — основная единица декомпозиции в DDD; их размещают, выявив субдомены (пространство проблем) и решив, сколько границ контекстов (пространство решений) лучше всего соответствует топологии команд и потребностям модели.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-06-01-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое bounded context и почему одно и то же слово намеренно означает разное в разных контекстах?", a: "Bounded context — явная граница, внутри которой одна доменная модель внутренне согласована. Ubiquitous language внутри контекста точен — каждый термин имеет одно значение, разделяемое всеми разработчиками и доменными экспертами этой команды. За пределами границы то же слово может означать что-то иное, потому что другая команда с другими доменными экспертами решает другую часть бизнес-проблемы. Это не провал в переговорах — это признание того, что «Customer» в Sales-контексте (лид-скор, менеджер, этап сделки) и «Customer» в Billing-контексте (способ оплаты, кредитный лимит, счета) — подлинно разные модели, эволюционирующие в разных направлениях. Объединение их в одну модель означает, что каждое изменение Sales должно согласовываться с Billing, и наоборот. Разделение позволяет каждому контексту эволюционировать в своём темпе за явной интеграционной границей." },
    { q: "В чём разница между субдоменом и bounded context?", a: "Субдомен — часть пространства бизнес-проблем: он обнаруживается в разговорах с доменными экспертами и существует вне зависимости от того, как вы строите ПО. Субдомены категоризируются как ключевые (core — дифференцирующие, конкурентное преимущество), вспомогательные (supporting — необходимые, но не дифференцирующие) и обобщённые (generic — решённые проблемы, покупайте или используйте готовое). Bounded context — решение в пространстве решений: граница контекста, которую вы проводите вокруг модели в коде. В идеале один ключевой субдомен соответствует одному bounded context с одной командой. Но большой ключевой субдомен может потребовать нескольких bounded contexts (разные команды для разных аспектов модели), а простой вспомогательный субдомен может не оправдывать собственного контекста вовсе. Субдомен говорит, что нужно бизнесу; bounded context говорит, как вы организовали код." },
    { q: "Что означает «основная единица декомпозиции» применительно к DDD?", a: "В стратегическом DDD вы отвечаете на вопрос «как декомпозировать эту систему?» не подсчётом микросервисов или API, а выявлением bounded contexts. Каждый bounded context представляет модель с согласованным языком, владеющей командой, границей деплоя (в сервисной архитектуре) или принудительной границей пакетов (в монолите) и явным интеграционным интерфейсом с каждым другим контекстом. Декомпозиция по bounded contexts гарантирует, что у каждой части системы есть чёткая модель владения, согласованный внутренний язык и реальное обоснование в бизнес-домене — а не только организационное или техническое удобство." },
  ]}
/>

<Recap lang="ru">
Сущность Order на 47 полей из вступительной истории была не проблемой плохого объектно-ориентированного дизайна. Это была проблема трёх различных моделей — Sales, Billing, Shipping — принудительно втиснутых в один класс, потому что никто не провёл явной границы.

Bounded context решает это, объявляя: внутри этой границы данное слово означает именно вот это, модель согласована, и изменения здесь не требуют координации с той другой командой. Граница языковая (ubiquitous language), модельная (согласованный набор классов и правил) и структурная (закреплена границами пакетов или сервисов в коде).

Bounded contexts — это не то же самое, что субдомены. Субдомены — пространство проблем бизнеса: обнаруживаемое, не проектируемое. Bounded contexts — ваше пространство решений: проектируемое вашей командой на основе субдоменов, топологии команд и сложности модели.

Следующий урок рассматривает ubiquitous language внутри bounded context — почему это инструмент проектирования, а не просто соглашение об именовании, и как дрейф имён является первым сигналом о том, что граница стоит не там.
</Recap>
