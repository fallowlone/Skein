---
slug: "03-aggregates-and-invariants"
lang: ru
track: "architecture-patterns"
unit: "06-domain-driven-design"
order: 3
title: "Aggregates и инварианты"
summary: "Aggregate — граница транзакционной согласованности. Aggregate root — единственная точка входа извне. Одна транзакция изменяет один aggregate. Размер aggregate определяется инвариантом, который он защищает, а не графом данных."
estMin: 24
status: ready
lessonType: topic
level: senior
concepts:
  - aggregate
  - aggregate-root
  - invariant
  - transactional-consistency
  - aggregate-by-id
  - aggregate-size
prereqs: []
sources:
  - https://martinfowler.com/bliki/DDD_Aggregate.html
  - https://www.domainlanguage.com/ddd/reference/
  - https://www.informit.com/store/implementing-domain-driven-design-9780321834577
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import aggregateSizing from "./infographics/aggregate-sizing.svg?raw";

<Hook>
В Sales-контексте B2B-платформы был `Quote`, содержащий объекты `LineItem`. У `LineItem` был `NegotiatedPrice`. У `Quote` было бизнес-правило: суммарная стоимость всех позиций не должна превышать кредитный лимит клиента в момент подачи. Правило казалось простым. Потом команда столкнулась с проблемой конкурентности. Два менеджера одновременно редактировали один Quote — один добавлял дорогостоящую позицию, другой удалял другую. Оба изменения прошли проверку кредитного лимита по отдельности, потому что каждое читало Quote до того, как запись другого завершилась. После коммита обоих транзакций суммарная стоимость превысила лимит. Инвариант — правило кредитного лимита — был нарушен, несмотря на то что оба изменения по отдельности были валидны. Первым решением команды стала оптимистичная блокировка на строку `Quote`. Это помогло для конфликтов на уровне Quote, но потом всплыла другая проблема: разработчик написал batch-задание, которое обновляло цены `LineItem` напрямую, минуя `Quote`. Задание не знало о правиле кредитного лимита, потому что проверяло только `LineItem`, а не владеющий им `Quote`. Две отдельные транзакции, две отдельные проверки инвариантов — одно и то же повреждение. Команда случайно разделила то, что должно было быть одной единицей согласованности, на два. Паттерн aggregate — ответ на этот класс проблем. Aggregate определяет границу транзакционной согласованности: кластер доменных объектов, рассматриваемый как одно целое для целей изменений данных. Aggregate root — единственная точка входа. Каждая мутация проходит через него. Инварианты, которые он применяет, гарантированно остаются верными после каждой транзакции.
</Hook>

<Crux>Aggregate — граница транзакционной согласованности: один root, одна транзакция, кластер инвариантов; снаружи — ссылка по ID.</Crux>

<Explanation>

## Что aggregate применяет

Aggregate — это не группировка связанных данных. Это группировка данных вокруг **инварианта согласованности** — бизнес-правила, которое должно оставаться верным для нескольких объектов после каждой операции.

Aggregate `Quote` применяет правило: «суммарная согласованная стоимость всех позиций не должна превышать кредитный лимит клиента в момент подачи». Это правило охватывает `Quote` и все его объекты `LineItem`. `Quote` не может делегировать проверку в `LineItem`, потому что `LineItem` не знает о суммарной стоимости. Ни один отдельный `LineItem` не может нарушить правило — только коллекция в целом.

Это определяющий критерий для aggregate: **какой наименьший набор объектов необходимо загрузить и заблокировать вместе, чтобы применить конкретный бизнес-инвариант?** Этот набор и есть aggregate. Объект в корне этого набора, тот, что применяет инвариант, — **aggregate root**.

Aggregate root — единственная точка входа для внешнего кода. Ни один объект снаружи aggregate не держит прямую ссылку на внутренний `LineItem`. Внешний код вызывает `quote.addLineItem(...)` или `quote.removeLineItem(...)`. Root `Quote` запускает проверку инварианта. Объекты `LineItem` — детали реализации: к ним нельзя обратиться, изменить или удалить, минуя root.

<FlowDiagram
  label="Aggregate Quote: root применяет инвариант над внутренними сущностями"
  caption="Aggregate root Quote — единственная точка входа извне. LineItem и NegotiatedPrice — внутренние сущности и value objects: внешний код не держит прямых ссылок на них. Инвариант кредитного лимита применяется root'ом по всем позициям. Стрелки показывают направление владения и доступа: внешний код обращается только к root'у."
  nodes={[
    { id: "ext", label: "Внешний код", sub: "вызывает quote.addLineItem(), quote.submit()" },
    { id: "root", label: "Quote (aggregate root)", sub: "применяет: total <= creditLimit при подаче" },
    { id: "li1", label: "LineItem (внутренний)", sub: "productId, quantity, NegotiatedPrice" },
    { id: "li2", label: "LineItem (внутренний)", sub: "productId, quantity, NegotiatedPrice" },
    { id: "price", label: "NegotiatedPrice (value object)", sub: "amount, currency — иммутабельный" },
  ]}
  edges={[
    { from: "ext", to: "root", label: "только точка входа" },
    { from: "root", to: "li1", label: "владеет" },
    { from: "root", to: "li2", label: "владеет" },
    { from: "li1", to: "price", label: "содержит" },
    { from: "li2", to: "price", label: "содержит" },
  ]}
/>

## Одна транзакция изменяет один aggregate

Это правило — структурное следствие границы согласованности. Если одна транзакция изменяет два aggregate'а, то либо:

1. Вы поместили неправильные объекты в один aggregate (инвариант на самом деле охватывает оба — они должны быть одним aggregate'ом), либо
2. Вы приняли eventual consistency между двумя aggregate'ами (инвариант не должен применяться синхронно сразу для обоих — они коммуницируют через события).

Правило «одна транзакция — один aggregate» объясняет, почему batch-задание из вступительной истории породило проблему. Оно изменяло `LineItem` в одной транзакции, не проходя через `Quote`. Проверка инварианта `Quote` никогда не запускалась. Решение: убрать прямые записи `LineItem` полностью — все мутации идут через `Quote.addLineItem()` или `Quote.updateLineItemPrice()`, которые применяют инвариант после каждого изменения.

<Inset kind="why" lang="ru">
Почему это правило такое строгое? Потому что «одна транзакция — один aggregate» — это то, что делает границу aggregate применимой. Если два aggregate'а можно изменить в одной транзакции, то любой инвариант, охватывающий оба, должен проверяться в каждой транзакции, затрагивающей любой из них. Нельзя определить, каким транзакциям нужна проверка, не анализируя все сразу. Граница aggregate становится бессмысленной. Как только вы принимаете одну транзакцию на aggregate, aggregate root становится единственным местом, где должна жить логика инварианта — и вы можете рассуждать о ней изолированно.
</Inset>

## Ссылки на другие aggregate'ы по ID

Когда одному aggregate'у нужно ссылаться на другой, он делает это **только через идентификатор** — не через ссылку на объект. Aggregate `Quote` в Sales может ссылаться на Customer, которому принадлежит. Но `Customer` — отдельный aggregate (со своими инвариантами вокруг кредитной истории и статуса аккаунта). Aggregate `Quote` хранит `customerId` — value object, оборачивающий идентификатор клиента, — а не прямую ссылку на объект `Customer`.

Это имеет три следствия:

1. **Нет каскадной загрузки**: загрузка `Quote` не загружает автоматически граф `Customer`. Каждый aggregate загружается независимо.
2. **Нет каскадной мутации**: aggregate root может применять инварианты только над объектами, которыми владеет. Он не может мутировать `Customer` через ссылку — для этого нужно обращаться к `Customer` через его собственный root.
3. **Явная кросс-aggregate согласованность**: если операция должна изменить и `Quote`, и `Customer` — эти изменения происходят в отдельных транзакциях, координируемых через доменные события (событие `QuoteSubmitted` может запустить процесс, обновляющий `Customer.openQuoteCount`). Это eventual consistency по замыслу.

<FlowDiagram
  label="Кросс-aggregate ссылка по ID в Sales-контексте"
  caption="Quote хранит customerId (типизированный value object) для ссылки на aggregate Customer. Нет прямой ссылки на объект. Каждый aggregate загружается и транзактируется независимо. Стрелка представляет ссылку по ID, а не source-code импорт или владение объектом."
  nodes={[
    { id: "quote", label: "Aggregate Quote", sub: "id, customerId (ссылка), lineItems, status" },
    { id: "customer", label: "Aggregate Customer", sub: "id, creditLimit, accountStatus, openQuoteCount" },
    { id: "quote-repo", label: "QuoteRepository", sub: "load(quoteId): Quote" },
    { id: "customer-repo", label: "CustomerRepository", sub: "load(customerId): Customer" },
  ]}
  edges={[
    { from: "quote", to: "customer", label: "customerId (только ID-ссылка)" },
    { from: "quote-repo", to: "quote", label: "гидратирует" },
    { from: "customer-repo", to: "customer", label: "гидратирует" },
  ]}
/>

## Размер aggregate'а: по инварианту, а не по графу данных

Самая распространённая ошибка в дизайне aggregate'ов — делать их слишком большими. Команды смотрят на граф объектов — Quote содержит LineItems, LineItems содержат Products, Products содержат PricingRules, PricingRules содержат Tiers — и объединяют всё в один большой aggregate, потому что «всё это связано».

Правильный вопрос размера: **каков инвариант, который должен выполняться транзакционно?** Если инвариант — «суммарная стоимость позиций не должна превышать кредитный лимит при подаче», то aggregate — это Quote + LineItems. Всё. `Product` — отдельный aggregate: его собственные правила (название, описание, статус в каталоге) никак не связаны с правилом кредитного лимита Quote. `PricingRule` — вероятно, тоже отдельный aggregate. Quote хранит ссылку `productId` и value object `NegotiatedPrice` — он не владеет Product.

Малые aggregate'ы имеют два критических преимущества:

**Конкуренция за блокировки**: большой aggregate блокируется на всё время транзакции. Если aggregate `Order` содержит каждую позицию, каждую отгрузку, каждый платёж и весь журнал взаимодействий с клиентом — тогда каждая операция над заказом блокирует весь заказ. В нагруженной системе это создаёт узкие места сериализации. Малые aggregate'ы блокируют только то, что нужно заблокировать для конкретного инварианта.

**Концептуальная ясность**: большой aggregate, объединяющий несвязанные вещи, — сигнал о том, что инварианты не были чётко определены. «Всё про заказ» — не инвариант. «Согласованная стоимость Quote не должна превышать кредитный лимит клиента» — это инвариант. Aggregate'ы, размер которых определяется инвариантом, делают бизнес-правила видимыми в коде.

Умолчание для размера aggregate'а: **настолько мал, насколько позволяет инвариант**. Начните с наименьшего aggregate'а, применяющего инвариант. Расширяйте только когда реальный инвариант этого требует — не когда граф объектов это предполагает.

<Infographic
  svg={aggregateSizing}
  label="Сравнение двух стратегий определения размера aggregate'а. Размер по графу данных даёт большой aggregate: складывает всё связанное, блокирует весь граф на каждую запись, каскадно грузит данные, не нужные операции, и скрывает охватывающий инвариант. Размер по инварианту даёт малый aggregate: держит только набор, нужный правилу, блокирует лишь данные инварианта, так что независимые aggregate'ы коммитятся параллельно, грузит каждый aggregate отдельно со ссылками на другие по ID и делает каждое правило видимым в коде с согласованностью между aggregate'ами через доменные события."
  caption="Размер — по инварианту, а не по графу данных: малый aggregate меняет синхронную кросс-aggregate согласованность на меньшую конкуренцию за блокировки, независимую загрузку и более ясные правила."
  hue="var(--d-infra)"
/>

<Quiz
  id="arch-06-03-quiz-1"
  lessonSlug="03-aggregates-and-invariants"
  lang="ru"
  question="Разработчик предлагает: «Aggregate Order должен содержать OrderLineItems, Shipments и Payments, потому что все они — часть заказа». Опытный в DDD ревьюер возражает. В чём его основной аргумент?"
  choices={[
    { label: "Аргумент ревьюера: какой инвариант охватывает все три? «OrderLineItems, Shipments и Payments — все часть заказа» описывает отношение данных, а не инвариант согласованности. Если инвариант — «суммарное отгруженное количество не должно превышать суммарное заказанное» — он охватывает Order и OrderLineItems, но не Payments. Если инвариант — «платёж не может превышать сумму заказа» — он охватывает Order и Payments, но не Shipments. Это разные инварианты, которые могут применяться в разных aggregate'ах. Объединение всех трёх создаёт большой aggregate, сериализующий все операции с заказом и загружающий весь граф объектов для операций, которым нужен только один срез.", correct: true },
    { label: "Аргумент ревьюера: aggregate'ы должны отображаться один к одному на таблицы базы данных, а это три таблицы.", misconception: "Границы aggregate'а — забота доменной модели, а не схемы базы данных. Aggregate может охватывать несколько таблиц (корень Order с дочерней таблицей order_line_items). Таблицы базы данных не определяют границы aggregate'а — это делают бизнес-инварианты." },
    { label: "Аргумент ревьюера: aggregate'ы не должны превышать 100 строк кода.", misconception: "Для aggregate'ов нет ограничения по числу строк. Критерий размера — инвариант. Простой инвариант может породить aggregate на 20 строк. Сложный — на 300 строк. Возражение ревьюера касается ясности инварианта и конкуренции за блокировки, а не размера кода." },
    { label: "Аргумент ревьюера: Shipment и Payment принадлежат отдельным bounded contexts, поэтому не могут быть в одном aggregate'е.", misconception: "Это может быть верным (Shipment и Payment вполне могут принадлежать отдельным bounded contexts — Shipping и Billing). Но это отдельный вопрос от аргумента о размере aggregate'а. Даже внутри одного контекста вопрос размера таков: какой инвариант применяет aggregate? Объединение «связанных» вещей без определения охватывающего инварианта неверно вне зависимости от размещения контекста." },
  ]}
/>

<Quiz
  id="arch-06-03-quiz-2"
  lessonSlug="03-aggregates-and-invariants"
  lang="ru"
  question="Aggregate Quote применяет правило: «суммарная стоимость позиций не должна превышать кредитный лимит клиента при подаче». Команда хочет обновлять кредитный лимит клиента при подаче Quote (резервировать кредит). Они предлагают: внутри `quote.submit()` вызвать `customer.reserveCredit(quote.total())` напрямую. Что не так с этим дизайном?"
  choices={[
    { label: "Вызов `customer.reserveCredit()` внутри `quote.submit()` изменяет два aggregate'а в одной транзакции — Quote (меняет статус на Submitted) и Customer (меняет зарезервированный кредит). Это нарушает правило «одна транзакция — один aggregate». Кроме того, aggregate Quote держит прямую ссылку на объект Customer, а не только customerId, связывая загрузку и блокировку двух aggregate'ов. Правильный дизайн: `quote.submit()` меняет статус Quote и генерирует доменное событие `QuoteSubmitted`. Отдельный процесс (сага или обработчик события) обрабатывает событие и вызывает `customer.reserveCredit()` в своей транзакции. Резервирование кредита становится eventually consistent — что приемлемо, поскольку краткое окно между подачей Quote и резервированием кредита обычно допустимо.", correct: true },
    { label: "Вызов нормален, поскольку он идёт через aggregate root Customer (`customer.reserveCredit()`), а не мутирует поля Customer напрямую.", misconception: "Обращение через aggregate root необходимо, но недостаточно. Проблема не в том, как мутируется Customer — а в том, что одна транзакция мутирует два aggregate'а. Правило aggregate root гарантирует применение инвариантов внутри aggregate'а. Оно не делает приемлемой кросс-aggregate мутацию в одной транзакции." },
    { label: "Проблема в том, что `quote.submit()` не должен знать о кредитных лимитах — эта логика принадлежит доменному сервису.", misconception: "Проверка кредитного лимита при подаче И ЕСТЬ инвариант aggregate'а Quote — для aggregate root вполне уместно его применять. Проблема не в том, где живёт проверка инварианта, а в том, что её применение через мутацию отдельного aggregate'а в одной транзакции нарушает границу aggregate'а. Доменный сервис мог бы оркестрировать кросс-aggregate взаимодействие, но через отдельные транзакции и доменные события — не через прямой вызов внутри `submit()`." },
    { label: "Этот дизайн корректен. Подача Quote и резервирование кредита — часть одной бизнес-операции и должны происходить атомарно.", misconception: "Допущение о том, что «одна бизнес-операция» требует «одной транзакции», является корневой причиной многих чрезмерно сложных aggregate'ов. Большинство требований кросс-aggregate согласованности могут допускать краткое окно eventual consistency. Альтернатива — блокировка двух aggregate'ов в одной транзакции — создаёт конкуренцию за распределённые блокировки и нивелирует преимущество малых aggregate'ов. Проектируйте для eventual consistency между aggregate'ами, если только жёсткое транзакционное требование не было явно подтверждено с бизнесом." },
  ]}
/>

<Quiz
  id="arch-06-03-quiz-3"
  lessonSlug="03-aggregates-and-invariants"
  lang="ru"
  question="У команды aggregate `Order`, содержащий каждый `LineItem`, и они испытывают конкуренцию за блокировки: обработка заказов медленная, потому что две операции, затрагивающие разные позиции одного заказа, не могут выполняться параллельно. Разработчик предлагает: «Сделать каждый LineItem собственным aggregate'ом». Какой trade-off команда должна оценить перед этим?"
  choices={[
    { label: "Разделение LineItem на собственный aggregate убирает конкуренцию за блокировки — операции над разными позициями теперь могут выполняться в параллельных транзакциях. Но это также убирает возможность применять любой инвариант, охватывающий заказ и его позиции, в одной транзакции. Если есть инвариант вроде «суммарная стоимость заказа не должна превышать X» — он больше не может проверяться атомарно, он становится eventually consistent, применяемым процессом, читающим все LineItems после транзакции. Команда должна подтвердить с доменными экспертами, существует ли охватывающий инвариант. Если нет — разделение верно. Если инвариант существует — команда должна либо принять eventual enforcement, либо переструктурировать инвариант.", correct: true },
    { label: "Разделение LineItem на собственный aggregate всегда безопасно, потому что aggregate'ы должны быть как можно меньше.", misconception: "Малые aggregate'ы — умолчание, но «как можно меньше» означает «настолько мал, насколько позволяет инвариант», а не «настолько мал, насколько технически возможно». Если бизнес-инвариант охватывает заказ и его позиции — их разделение ломает возможность атомарно применять этот инвариант. Размер определяется инвариантом, а не общим предпочтением к малому." },
    { label: "Правильное решение для конкуренции за блокировки — использовать оптимистичную блокировку на aggregate Order, а не разделять его.", misconception: "Оптимистичная блокировка снижает стоимость конкуренции, но не устраняет её — конкурирующие операции над одним aggregate'ом всё равно провалятся и будут повторяться. Если конкуренция структурная (две операции без логического конфликта сериализуются, потому что затрагивают один aggregate) — корневая причина в том, что aggregate слишком велик. Оптимистичная блокировка — инструмент для обработки реальных конфликтов; она не устраняет искусственную конкуренцию из-за oversized aggregate'ов." },
    { label: "LineItems никогда не могут быть собственным aggregate'ом, потому что у них нет естественной идентичности без Order.", misconception: "Сущности внутри aggregate'а имеют идентичность (у LineItem есть ID в пределах Order). При повышении до собственного aggregate'а они получают глобально уникальную идентичность и собственный жизненный цикл. Имеет ли доменный смысл существование и независимое изменение LineItem без его Order — это именно вопрос инварианта: ответ зависит от бизнес-правил, а не от наличия идентичности у объекта." },
  ]}
/>

</Explanation>

<KeyTakeaway>Aggregate — граница транзакционной согласованности: кластер объектов с одним root'ом, применяющим конкретный бизнес-инвариант. Все мутации идут через root. Одна транзакция изменяет один aggregate. Другие aggregate'ы ссылаются только по ID. Размер aggregate'а определяется инвариантом, который он должен применять, а не графом данных — малый aggregate'ы по умолчанию правильны, и напряжение между малыми aggregate'ами и кросс-aggregate согласованностью разрешается через доменные события и eventual consistency.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="arch-06-03-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое aggregate root и почему он — единственная внешняя точка входа?", a: "Aggregate root — сущность в вершине графа объектов aggregate'а: та, что владеет всеми внутренними объектами и применяет инварианты aggregate'а. Он — единственная внешняя точка входа, потому что инвариант может быть проверен только над полным состоянием aggregate'а. Если внешний код мог бы мутировать внутреннюю сущность напрямую (минуя root), он мог бы изменять состояние aggregate'а без запуска проверки инварианта. Batch-задание из вступительной истории — именно этот провал: оно обновляло LineItem напрямую, минуя Quote, и проверка правила кредитного лимита никогда не запускалась. Все мутации входят через root, root загружает всё необходимое внутреннее состояние, применяет изменение, проверяет инвариант и либо коммитит согласованное состояние, либо отклоняет операцию." },
    { q: "Почему «одна транзакция изменяет один aggregate» следует из границы aggregate'а?", a: "Граница aggregate'а говорит: этот кластер объектов имеет одно согласованное состояние, применяемое одним root'ом. Если одна транзакция изменяет два aggregate'а — нет единственного root'а, применяющего согласованность для обоих: каждый root применяет только свои собственные инварианты. Вы можете проверять инварианты каждого aggregate'а по отдельности, но если есть кросс-aggregate инвариант — ни один root не знает о нём, и он остаётся непроверенным. Правило «одна транзакция — один aggregate» гарантирует, что в каждой транзакции есть ровно один root для применения, и проверка инварианта полна. Кросс-aggregate согласованность, требующая синхронного применения, — сигнал того, что два объекта принадлежат одному aggregate'у. Кросс-aggregate согласованность, допускающая краткое окно, обрабатывается через доменные события в отдельных транзакциях." },
    { q: "Каков правильный критерий размера aggregate'а и почему «размер по графу данных» провалится?", a: "Правильный критерий: какой наименьший набор объектов необходимо загрузить и заблокировать вместе, чтобы применить конкретный бизнес-инвариант? Этот набор — aggregate. Размер по графу данных («всё связанное с Order») провалится, потому что связанность — не требование согласованности. LineItems связаны с Orders в графе данных, но Shipments, Payments и журналы взаимодействий с клиентом тоже связаны с Orders. Если объединить всё это — получится aggregate, держащий каждую операцию над заказом под одной блокировкой. Конкуренция за блокировки серьёзная, и ни один реальный инвариант не охватывает всё это. «Размер по инварианту» по умолчанию производит малые aggregate'ы, потому что большинство инвариантов охватывают лишь несколько объектов. Расширяйте только когда конкретный инвариант этого требует." },
  ]}
/>

<Recap lang="ru">
Баг конкурентности из вступительной истории произошёл из-за разделения одной единицы согласованности — Quote с его инвариантом кредитного лимита — на два отдельно изменяемых объекта. Решением была не новая блокировка; это было признание того, что Quote и его LineItems образуют один aggregate, с Quote как root'ом, применяющим правило кредитного лимита по всем позициям в каждой транзакции.

Aggregate — граница транзакционной согласованности. Root — единственная точка входа. Все мутации проходят через него, чтобы проверка инварианта всегда запускалась. Одна транзакция изменяет один aggregate. Другие aggregate'ы ссылаются по ID — нет прямых объектных ссылок через границы aggregate'ов.

Размер — критически важный навык. Вопрос не «что связано?», а «какой инвариант должен выполняться транзакционно?». Начните с наименьшего aggregate'а, применяющего инвариант. Противостойте соблазну включить всё связанное — большие aggregate'ы сериализуют несвязанные операции и загружают ненужные данные. Кросс-aggregate согласованность разрешается через доменные события и eventual consistency — что почти всегда приемлемо, потому что очень мало бизнес-правил требуют одновременных атомарных изменений нескольких доменных объектов.

Следующий урок рассматривает, как множество bounded contexts соотносятся друг с другом — стратегические паттерны интеграции, позволяющие независимым контекстам коммуницировать без утечки их моделей друг в друга.
</Recap>
