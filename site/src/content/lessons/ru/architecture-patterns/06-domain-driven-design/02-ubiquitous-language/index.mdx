---
slug: "02-ubiquitous-language"
lang: ru
track: "architecture-patterns"
unit: "06-domain-driven-design"
order: 2
title: "Ubiquitous language"
summary: "Ubiquitous language — общий словарь разработчиков и доменных экспертов внутри одного bounded context. Модель И ЕСТЬ язык: дрейф имён сигнализирует о неверной границе или отсутствующем концепте."
estMin: 20
status: ready
lessonType: topic
level: senior
concepts:
  - ubiquitous-language
  - domain-model
  - naming-drift
  - context-boundary
  - domain-expert
  - model-language
prereqs: []
sources:
  - https://martinfowler.com/bliki/UbiquitousLanguage.html
  - https://www.domainlanguage.com/ddd/reference/
  - https://www.informit.com/store/domain-driven-design-tackling-complexity-in-the-heart-9780321125217
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Sales-команда B2B-платформы называла это «Quote». В коде это было `ProposalDraft`. Таблица в базе данных называлась `pending_orders`. В продуктовой спецификации — «Pre-Order». CRM-плагин генерировал события `DealCreated`. Пять имён для одного бизнес-концепта — и каждый перевод открывал возможность для недопонимания. Разработчик, читающий баг-репорт о «квоте, которую не отправили», искал в коде `Quote`, ничего не находил, в конце концов обнаруживал `ProposalDraft`, пытался понять, не одно ли это с «Quote», читал спецификацию — там было «Pre-Order», находил таблицу `pending_orders` и в итоге спрашивал коллегу. Тридцать минут на поиск концепта. На той же неделе продакт-менеджер изменил правило: «Quote истекает через 30 дней». Разработчик поправил логику истечения в `ProposalDraft`. Но CRM-интеграция слушала событие `DealCreated` со своим автоматом состояний и не знала, что у `ProposalDraft` есть срок. Когда сделка истекала в коде, CRM по-прежнему показывала её как открытую. На ревью никто не заметил: ревьюер думал в терминах «Quote», а код был в терминах `ProposalDraft`. Термин Эванса для починки обманчиво прост: «ubiquitous language». Но простота вводит в заблуждение. Это не соглашение об именовании. Это обязательство: язык, на котором говорят на встречах с доменными экспертами, — это в точности тот язык, который записан в коде, базе данных, событиях, тестах и документации — без слоя перевода нигде.
</Hook>

<Crux>Ubiquitous language — общий словарь разработчиков и доменных экспертов внутри одного bounded context: модель И ЕСТЬ язык, без перевода.</Crux>

<Explanation>

## Что ubiquitous language означает на самом деле

«Ubiquitous» означает «встречающийся повсюду». Суть позиции Эванса: язык должен присутствовать — без перевода — в:

- Разговорах между разработчиками и доменными экспертами
- Коде: именах классов, методов, переменных, событий
- Схеме базы данных: именах таблиц, столбцов
- Тестах: описаниях тестов, именах тестовых данных
- Документации и спецификациях
- Метках пользовательского интерфейса (там, где они отражают доменные концепты)

Когда слово из разговора с доменным экспертом отличается от слова в коде, на границе каждого разговора происходит перевод. Переводы порождают ошибки. Они также маскируют проблемы модели: разработчик, использующий `ProposalDraft`, перестаёт задаваться вопросом, применимы ли правила «Quote» доменного эксперта, — потому что имена не совпадают.

Ubiquitous language не изобретается разработчиками и не объясняется потом доменным экспертам. Он вырабатывается совместно. Доменные эксперты используют естественный язык неточно: «заказ подан» может означать многое. Процесс построения ubiquitous language заставляет обе стороны быть точными. «Заказ подан» становится: «Sales Order переходит в состояние SubmittedForApproval, когда менеджер нажимает Confirm и все обязательные позиции имеют согласованную цену». Эта точность входит в код как имя метода (`submitForApproval()`), значение enum (`OrderStatus.SUBMITTED_FOR_APPROVAL`) и доменное событие (`SalesOrderSubmittedForApproval`).

<Inset kind="why" lang="ru">
Почему «модель И ЕСТЬ язык», а не «модель реализует язык»? Потому что если между ними есть разрыв — язык говорит «Quote», а модель говорит `ProposalDraft` — у вас два артефакта для поддержки. Каждый раз, когда язык эволюционирует (доменные эксперты открывают новый концепт или уточняют существующий), нужно обновлять и модель, и языковой артефакт. Когда они одно и то же, открытие концепта немедленно становится переименованием класса, новым методом или новым доменным событием. Именно поэтому Эванс настаивает на «ubiquitous» — «вездесущем», а не «согласованном» или «задокументированном»: он должен быть везде, а не в глоссарии, живущем отдельно от кода.
</Inset>

## Модель как язык: как это выглядит в коде

В B2B-платформе Sales-контекст вырабатывает ubiquitous language вокруг концептов: Quote, LineItem, NegotiatedPrice, AccountExecutive, QuoteExpiry, QuoteSubmission. Код отражает это буквально:

- Класс `Quote` с методами `addLineItem()`, `negotiate()`, `submitForApproval()`, `expire()`
- Value object `NegotiatedPrice` (не `BigDecimal` с комментарием)
- Доменное событие `QuoteSubmittedForApproval` (не `StatusUpdated` с полем в payload)
- Репозиторий `QuoteRepository` с методом `findExpiringBefore(date: LocalDate)`

Ни одно из этих имён не требует словаря. Доменный эксперт, читающий тест `quote_expires_when_no_approval_within_30_days`, понимает его немедленно. Когда доменный эксперт меняет правило на «Quote от enterprise-клиентов истекает через 60 дней», разработчик точно знает, какой класс и какой метод менять.

<FlowDiagram
  label="Ubiquitous language проходит от доменного разговора до каждого артефакта"
  caption="Один и тот же словарь — Quote, LineItem, NegotiatedPrice, QuoteExpiry — присутствует в разговорах с доменными экспертами, коде, событиях, тестах и базе данных. Нет перевода ни на одной границе. Стрелки показывают один и тот же термин в разном артефакте, а не поток данных."
  nodes={[
    { id: "conversation", label: "Доменный разговор", sub: "Quote, LineItem, NegotiatedPrice, истечение" },
    { id: "code", label: "Исходный код", sub: "class Quote, method expire(), NegotiatedPrice VO" },
    { id: "events", label: "Доменные события", sub: "QuoteSubmittedForApproval, QuoteExpired" },
    { id: "tests", label: "Тесты", sub: "quote_expires_after_30_days_without_approval" },
    { id: "schema", label: "Схема базы данных", sub: "таблица quotes, таблица line_items, столбец negotiated_price" },
  ]}
  edges={[
    { from: "conversation", to: "code", label: "тот же термин" },
    { from: "conversation", to: "events", label: "тот же термин" },
    { from: "conversation", to: "tests", label: "тот же термин" },
    { from: "conversation", to: "schema", label: "тот же термин" },
  ]}
/>

## Дрейф имён как сигнал

Самое ценное в ubiquitous language — не то, что он говорит, когда работает, а то, что он говорит, когда ломается. Когда язык начинает дрейфовать, с моделью что-то не так.

**Сигнал 1: слову начинают требоваться уточнения.** Когда разработчики начинают говорить «Sales Order» и «Billing Order», чтобы различать две вещи, которые оба называют «Order» — они обнаружили, что «Order» означает разное в двух местах. Это сигнал о границе bounded context, а не проблема именования. Решение — не переименовать оба варианта, а поместить каждый в свой контекст, где «Order» однозначно обозначает концепт этого контекста.

**Сигнал 2: один концепт получает разные имена в разных файлах.** `ProposalDraft` в доменной модели, `Quote` в сервисном слое, `pending_order` в базе данных. Это провал ubiquitous language: модель не совпадает с языком, на котором реально говорит команда.

**Сигнал 3: доменные эксперты используют слово, которого нет в коде.** Если доменный эксперт говорит об «approval escalation», а в коде нет класса `ApprovalEscalation`, нет метода `escalate()`, нет события `QuoteEscalated` — этот концепт живёт только в головах экспертов и в куче условий, разбросанных по сервисным методам. Модель упускает концепт, который реально существует в домене.

<Inset kind="note" lang="ru">
Ubiquitous language ограничен одним bounded context. Язык Billing-контекста отделён от языка Sales-контекста — намеренно. «Customer» в Sales означает нечто иное, чем в Billing, — и это нормально. Каждый контекст имеет свой точный язык. Ошибка — пытаться построить один унифицированный язык для всех контекстов: это порождает ту же проблему God-объекта на 47 полей, но на уровне словаря. Язык внутри контекста точен; перевод между контекстами явен.
</Inset>

## Рефакторинг к языку

Команда, у которой есть разрыв ubiquitous language, имеет задачу в бэклоге: переименовать `ProposalDraft` в `Quote`, переименовать `pending_orders` в `quotes`, переименовать события `StatusUpdated` в `QuoteSubmittedForApproval` и `QuoteExpired`. Это не косметика. Каждое переименование — исправление модели, снижающее стоимость перевода для каждого будущего разговора с доменным экспертом.

Дисциплина такова: когда доменный эксперт использует новый термин в разговоре — следующий шаг добавить его в модель, не в глоссарий, не в комментарий, а в код. Если термин раскрывает отсутствующий концепт — создать класс. Если раскрывает отсутствующий переход состояния — добавить метод. Если раскрывает отсутствующее событие — добавить тип события. Модель и язык эволюционируют вместе.

<Quiz
  id="arch-06-02-quiz-1"
  lessonSlug="02-ubiquitous-language"
  lang="ru"
  question="Разработчик переименовывает класс `ProposalDraft` в `Quote` после разговора с доменным экспертом. Тимлид возражает: «Это просто косметическое переименование — оно не добавляет бизнес-ценности и рискует внести баги». Что не так с этой формулировкой?"
  choices={[
    { label: "Переименование `ProposalDraft` в `Quote` — не косметика: оно устраняет перевод между тем, что говорит доменный эксперт, и тем, что написано в коде. Каждый будущий разговор о Quote больше не требует мысленного маппинга на `ProposalDraft`. Каждый баг-репорт, упоминающий «Quote», теперь напрямую соответствует конструкции кода. Риск неверно понять намерение доменного эксперта снижается. Тимлид воспринимает имена кода как детали реализации; ubiquitous language рассматривает их как часть доменной модели. Неверное имя — ошибка модели с реальными издержками, накапливающимися со временем.", correct: true },
    { label: "Переименование не имеет ценности, если не сопровождается изменением схемы базы данных.", misconception: "Переименование в коде создаёт немедленную ценность даже до переименования в базе данных. Каждый разработчик, читающий код, сразу получает пользу от точного имени. Переименование базы данных — отдельная задача (с риском миграции), которую можно сделать позже. Откладывать переименование в коде до готовности базы означает откладывать пользу от ясности модели без причины." },
    { label: "Тимлид прав — именование это вопрос стиля, а не архитектуры. Важно только поведение.", misconception: "В DDD именование И ЕСТЬ архитектура. Класс с именем `ProposalDraft`, когда домен называет его `Quote`, создаёт постоянные накладные расходы на перевод для каждого разработчика, читающего код, и для каждого разговора с доменным экспертом. Имена — не стиль, а видимая поверхность модели." },
    { label: "Переименование имеет ценность только если доменный эксперт тоже меняет свой словарь в соответствии с кодом.", misconception: "Причинно-следственная связь обратная: КОД должен принять словарь доменного эксперта, а не наоборот. Доменный эксперт владеет языком бизнес-домена; задача разработчика — отобразить этот язык на код. Просить экспертов менять словарь под имена реализации — это инверсия смысла DDD." },
  ]}
/>

<Quiz
  id="arch-06-02-quiz-2"
  lessonSlug="02-ubiquitous-language"
  lang="ru"
  question="На обзоре спринта доменный эксперт говорит: «Когда крупный enterprise-клиент размещает заказ, мы сначала делаем кредитную проверку, а для мелких клиентов просто одобряем автоматически». Разработчик смотрит в код и находит `if (customer.tier == 'ENTERPRISE') { creditService.check(order); }`, разбросанный по трём сервисным методам. Что это говорит о модели?"
  choices={[
    { label: "У доменного эксперта есть концепт — «путь одобрения с кредитной проверкой» vs «путь автоматического одобрения» — который модель не представляет. Условный оператор означает, что концепт живёт только как условие, а не как именованный элемент модели. Рефакторинг к ubiquitous language ввёл бы явный концепт: возможно, `ApprovalPolicy` с двумя реализациями (`CreditCheckPolicy` и `AutoApprovalPolicy`), или `OrderApprovalFlow`, фиксирующий условие как именованный доменный процесс. Разбросанные условные операторы будут расти по мере накопления enterprise-специфичных правил, потому что модели некуда их положить.", correct: true },
    { label: "Это стандартное бизнес-правило, и условный оператор — правильная реализация. Не каждый доменный концепт требует собственного класса.", misconception: "Проблема не в том, что правило существует, а в том, что оно растёт. «Кредитная проверка для enterprise» — сегодняшнее правило, но доменный эксперт, объяснивший его, почти наверняка имеет больше нюансов: что считается крупным enterprise? Что происходит при провале кредитной проверки? Есть ли эскалация? Эти последующие правила не имеют места в разбросанном условном операторе. Дисциплина ubiquitous language раскрывает концепт сейчас, чтобы у него было место для роста." },
    { label: "Модель в порядке — проблема в том, что сервисные методы нужно отрефакторить для вызова общего хелпера вместо дублирования условного оператора.", misconception: "Извлечение общего хелпера снижает дублирование кода, но не решает проблему модели. Хелпер `checkCreditIfEnterprise(order, customer)` по-прежнему не является именованным доменным концептом — это детали реализации. Рефакторинг скрывает дублирование, не раскрывая концепт. Разработчик, читающий хелпер, всё равно понимает бизнес-правило из кода; разработчик, читающий класс `CreditCheckPolicy`, читает концепт из имени." },
    { label: "Это сигнализирует о том, что Sales-контекст и Credit-контекст нужно объединить в один bounded context.", misconception: "Кредитная проверка — отдельная субдоменная забота, вероятно принадлежащая отдельному bounded context (или использующая внешний кредитный сервис). Проблема не в границе между Sales и Credit; проблема в том, что модель Sales-контекста не представляет решение о кредитной проверке как именованный концепт в своём собственном потоке. Sales-контекст мог бы обращаться к CreditRating bounded context, но он должен представлять точку принятия решения — «требует ли этот заказ кредитного одобрения?» — как явный элемент модели." },
  ]}
/>

<Quiz
  id="arch-06-02-quiz-3"
  lessonSlug="02-ubiquitous-language"
  lang="ru"
  question="У команды два bounded context: Sales и Billing. В Sales концепт называется «Quote» с жизненным циклом Draft → Submitted → Approved → Expired. В Billing тот же документ называется «Invoice» с жизненным циклом Pending → Sent → Paid → Overdue. Новый разработчик говорит: «Это явно один и тот же документ — нужно объединить в одну модель, чтобы не дублировать». Почему это неверно?"
  choices={[
    { label: "Quote и Invoice — подлинно разные модели, возникающие из одной бизнес-транзакции. Жизненный цикл Quote управляется процессом переговоров о продаже (черновик, подача, одобрение, истечение). Жизненный цикл Invoice — платёжным процессом (выставлен, отправлен, получен, просрочен). Триггеры переходов состояний разные, действующие лица разные (менеджеры управляют Quote; бухгалтерия управляет Invoice), допустимые операции разные (Quote можно переоговорить; Invoice — нельзя). Объединение создаёт God-объект, которым ни одна команда полностью не владеет.", correct: true },
    { label: "Их нужно объединить, потому что они разделяют одни данные — позиции, цены, ссылку на клиента.", misconception: "Разделение полей данных — не повод объединять модели. Две модели могут ссылаться на один и тот же customer ID, нести схожие ценовые поля и представлять позиции из одного каталога — и при этом быть подлинно различными. Идентичность модели исходит из её поведения, автомата состояний, бизнес-правил и забот её владеющей команды — не из структуры данных." },
    { label: "Решением является создание базового класса 'CommercialDocument', от которого наследуются и Quote, и Invoice.", misconception: "Это заблуждение о наследовании как решении проблемы bounded context из предыдущего урока. Общий базовый класс связывает обе команды с одной иерархией классов. Любое изменение CommercialDocument требует координации между Sales и Billing. Разделение bounded contexts существует именно для избежания этой связанности; наследование её воссоздаёт, сохраняя видимость разделения." },
    { label: "Их нужно объединить в одну модель, потому что доменные эксперты в обеих командах называют это 'документом заказа'.", misconception: "Естественный язык неоднозначен — 'документ заказа' является разговорным зонтичным термином, а не доменной моделью. Упражнение по ubiquitous language требует точности: когда вы просите Sales-эксперта описать все состояния Quote и что их вызывает, а затем спрашиваете Billing-эксперта о его документе — ответы расходятся достаточно, чтобы раскрыть две отдельные модели. Разговорное сходство не означает, что модели нужно объединять." },
  ]}
/>

</Explanation>

<KeyTakeaway>Ubiquitous language — общий словарь разработчиков и доменных экспертов внутри одного bounded context, используемый одинаково в разговорах, коде, событиях, тестах и базе данных. Модель И ЕСТЬ язык — нет никакого перевода. Дрейф имён (словам нужны уточнения, один концепт получает разные имена, термины существуют в разговорах, но не в коде) — дефект модели, а не вопрос стиля.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-06-02-retrieval"
  lang="ru"
  questions={[
    { q: "Что означает «ubiquitous» в «ubiquitous language» и почему важен именно этот выбор слова?", a: "Ubiquitous означает «присутствующий везде, встречающийся во всех местах». Эванс выбирает «ubiquitous», а не «согласованный» или «разделяемый», намеренно: язык должен появляться без перевода в разговорах, коде, схемах баз данных, именах событий, именах тестов и документации. Если он только в глоссарии — он не ubiquitous, а задокументированный. Если он только в коде, но не в разговорах с доменными экспертами — он не ubiquitous, а технический словарь. Требование: точно тот же термин, который использует доменный эксперт в разговоре, появляется как имя класса, метода или события в коде — без адаптера между ними. Любое место, где происходит перевод — точка, где может войти непонимание и начаться дрейф модели." },
    { q: "Каковы три сигнала дрейфа ubiquitous language и что каждый из них означает?", a: "Сигнал 1: слову начинают требоваться уточнения («Sales Order» vs «Billing Order»). Это означает, что концепт используется через границу контекста — слово имеет два разных смысла в двух местах. Решение — разделение bounded context, а не переименование. Сигнал 2: один концепт получает разные имена в разных артефактах (ProposalDraft в коде, Quote в разговоре, pending_order в базе данных). Это означает, что модель не совпадает с реальным языком команды, или у команды нет общего языка. Решение — рефакторинг-переименование к языку. Сигнал 3: доменные эксперты используют термин, которого нет в коде. Это означает, что в модели отсутствует концепт, реально существующий в бизнес-домене. Решение — добавить концепт: новый класс, метод, событие или состояние, а не комментарий." },
    { q: "Почему ubiquitous language должен быть ограничен одним bounded context?", a: "Потому что разные bounded contexts имеют доменных экспертов с разным словарем для одних и тех же реальных объектов. «Customer» в Sales (лид-скор, этап воронки, менеджер) — это другая модель от «Customer» в Billing (способ оплаты, кредитный лимит, задолженность). Попытка унифицировать их в один кросс-контекстный словарь порождает ту же проблему, что и попытка объединить их в один класс: перегруженный термин, означающий разное для разных команд, требующий уточнений. Язык каждого контекста точен внутри этого контекста. Перевод между контекстами обрабатывается на интеграционной границе — явным маппингом, а не общим словарем." },
  ]}
/>

<Recap lang="ru">
Пять имён для одного концепта из вступительной истории — Quote, ProposalDraft, pending_order, Pre-Order, DealCreated — были не провалом соглашения об именовании. Это был провал модели: общий язык не был согласован, поэтому каждая команда изобрела свой термин, и каждая точка интеграции требовала мысленного перевода.

Ubiquitous language решает это, делая словарь доменного эксперта словарём кода — без перевода нигде. Имена классов, методов, событий, описания тестов и столбцы баз данных используют те же термины, что появляются в разговорах с доменными экспертами. Когда доменное правило меняется, разработчик немедленно находит нужный код, потому что имена совпадают.

Язык — мощный диагностический инструмент. Когда слово требует уточнений — граница bounded context стоит не там. Когда один концепт имеет разные имена в разных файлах — модель не совпадает с пониманием команды. Когда термин живёт в разговорах с экспертами, но не в коде — в модели отсутствует концепт.

Следующий урок рассматривает, как применять согласованность модели внутри bounded context — в частности, паттерн агрегата, определяющего транзакционную границу вокруг кластера связанных объектов.
</Recap>
