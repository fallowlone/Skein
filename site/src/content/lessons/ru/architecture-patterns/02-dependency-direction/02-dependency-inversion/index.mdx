---
slug: "02-dependency-inversion"
lang: ru
track: "architecture-patterns"
unit: "02-dependency-direction"
order: 2
title: "Инверсия зависимостей"
summary: "DIP: высокоуровневые модули не зависят от низкоуровневых — оба зависят от абстракций. Интерфейс принадлежит потребителю, а не поставщику. Этот механизм лежит в основе hexagonal и clean architecture."
estMin: 22
status: ready
lessonType: topic
level: middle
concepts:
  - dependency-inversion-principle
  - interface-ownership
  - high-level-module
  - low-level-module
  - compile-time-boundary
  - inversion-mechanism
prereqs: []
sources:
  - https://www.informit.com/articles/article.aspx?p=1372233&seqNum=2
  - https://www.informit.com/store/clean-architecture-a-craftsmans-guide-to-software-structure-9780134494166
  - https://martinfowler.com/articles/dipInTheWild.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Команда billing на B2B-платформе спроектировала чистый интерфейс для репозитория заказов. Разместили его в пакете модуля order: `order/interfaces/IOrderRepository.ts`. Billing импортировал его оттуда. Выглядело аккуратно — интерфейс прямо рядом с реализацией. Но через шесть месяцев команда order решила реструктурировать пакет. Они переместили интерфейс, разбили его на части и поменяли сигнатуры некоторых методов. Billing сломался. Payment gateway сломался. Audit-сервис сломался. Команда order была в недоумении: они опубликовали интерфейс, значит потребители должны быть защищены. Не были — потому что интерфейс жил в пакете модуля order. Модуль order всё ещё им владел. Каждый раз, когда команда order трогала свой пакет, они могли сломать каждого потребителя — интерфейс есть, а защиты нет. У команды была верная идея (интерфейс как граница), но неверная модель владения. DIP — это не просто «заведи интерфейс». Это про то, кому он принадлежит.
</Hook>

<Crux>DIP: потребитель владеет абстракцией — задаёт интерфейс; поставщик реализует его; стрелка зависимости указывает от поставщика к потребителю.</Crux>

<Explanation>

## Две половины DIP

Принцип инверсии зависимостей Роберта Мартина состоит из двух утверждений, и оба должны выполняться:

1. **Высокоуровневые модули не должны зависеть от низкоуровневых. Оба должны зависеть от абстракций.**
2. **Абстракции не должны зависеть от деталей. Детали должны зависеть от абстракций.**

Первое утверждение привычно — это правило зависимостей из предыдущего урока в форме модулей. Высокоуровневые модули (billing на B2B-платформе) содержат политику: бизнес-логику, определяющую, что должно произойти. Низкоуровневые модули (репозиторий заказов, платёжный шлюз) содержат механизм: техническую сантехнику, которая делает это возможным. Политика не должна быть связана с механизмом.

Второе утверждение тоньше — и именно его нарушила вводная история. Размещение интерфейса в пакете низкоуровневого модуля делает абстракцию деталью низкоуровневого модуля. Высокоуровневый потребитель импортирует интерфейс *из* низкоуровневого модуля. Низкоуровневый модуль по-прежнему может сломать потребителя, реструктурировав пакет, переименовав интерфейс или разбив его на несколько. Абстракция технически присутствует, но владение не инвертировано.

**DIP требует, чтобы высокоуровневый модуль владел абстракцией, от которой зависит.**

## Кому принадлежит интерфейс?

Владение интерфейсом — это суть DIP. Интерфейс принадлежит тому, кто его определяет и контролирует его эволюцию. Когда спрашиваешь «должен ли этот интерфейс измениться?», ответ должен исходить из потребностей потребителя, а не из удобства реализации поставщика.

На B2B-платформе модуль billing нуждается в данных заказов. Интерфейс, который ему нужен — `IOrderProvider` или `IOrderPort` — должен жить в пакете billing (или в выделенном пакете границы, который billing контролирует). Он объявляет ровно то, что нужно billing: `getConfirmedOrder(id: string): Promise<ConfirmedOrderDTO>`. Не больше. Модуль order затем реализует этот интерфейс, написав класс, удовлетворяющий контракту billing.

Стрелки зависимостей теперь:

```
BillingService   ──imports──→   IOrderPort   (принадлежит billing)
                                      ↑
                              implements
                                      │
                         PostgresOrderRepository   (в модуле order)
```

Репозиторий модуля order зависит от интерфейса billing — а не наоборот. Если нужды billing изменятся, интерфейс меняется и репозиторий должен обновиться. Если команда order меняет внутренний способ получения данных заказов, она обновляет реализацию репозитория; интерфейс billing не трогается.

<FlowDiagram
  label="DIP: до и после инверсии владения интерфейсом"
  nodes={[
    { id: "billing-before", label: "BillingService", sub: "ДО: импортирует конкретный репозиторий" },
    { id: "repo-before", label: "PostgresOrderRepository", sub: "ДО: принадлежит модулю order" },
    { id: "billing-after", label: "BillingService", sub: "ПОСЛЕ: импортирует собственный интерфейс" },
    { id: "iport", label: "IOrderPort", sub: "принадлежит модулю billing" },
    { id: "repo-after", label: "PostgresOrderRepository", sub: "ПОСЛЕ: реализует интерфейс billing" },
  ]}
  edges={[
    { from: "billing-before", to: "repo-before", label: "ДО: concrete dep" },
    { from: "billing-after", to: "iport", label: "imports (source dep)" },
    { from: "repo-after", to: "iport", label: "implements (source dep)" },
  ]}
  caption="До DIP: BillingService импортирует PostgresOrderRepository напрямую — зависимость конкретного от конкретного. После DIP: BillingService импортирует IOrderPort (которым владеет), а PostgresOrderRepository реализует IOrderPort. Стрелка зависимости в исходном коде от репозитория теперь указывает в сторону границы billing, а не от неё."
/>

## Compile-time граница vs runtime граница

DIP по сути — о проведении границы, которую может соблюдать компилятор. На одной стороне: высокоуровневый модуль и принадлежащий ему интерфейс. На другой: низкоуровневый модуль, реализующий его. Ни одной стороне не нужно знать о конкретных деталях другой.

Различие между тем, что известно на этапе компиляции, и тем, что разрешается в runtime, — ключ к модульности:

**На этапе компиляции** BillingService знает только об `IOrderPort`. Он знает сигнатуры методов, типы параметров, возвращаемые типы. Он знает контракт. Он не знает, что `PostgresOrderRepository` существует. Компилятор не видит `PostgresOrderRepository` из контекста billing.

**В runtime** что-то должно связать `IOrderPort` с `PostgresOrderRepository`. Это работа dependency injection: composition root или контейнер разрешает, какая конкретная реализация удовлетворяет какому интерфейсу, и передаёт её в billing. В этот момент управление течёт от billing через интерфейс к реализации репозитория.

Это разделение — суть того, что делает hexagonal architecture (unit 04) и clean architecture (unit 05) структурно соблюдаемыми. Гексагон в hexagonal architecture — высокоуровневый домен, окружённый принадлежащими ему определениями интерфейсов (портами). Адаптеры — конкретные реализации — живут вне гексагона и зависят от портов внутрь. Концентрические круги в clean architecture — слои, где каждый внутренний круг владеет интерфейсами, которые реализуют внешние. DIP — механизм, делающий эти паттерны структурно соблюдаемыми.

<Inset kind="why" lang="ru">
Почему владение интерфейсом так важно на практике? Потому что без него абстракция — фикция. Интерфейс может существовать как TypeScript interface или Java abstract class, но если он живёт в пакете низкоуровневого модуля, низкоуровневая команда может его изменить. Они могут добавить обязательные методы, изменить сигнатуры, устаревшие удалить — и высокоуровневый потребитель ломается. Слово «инверсия» в DIP означает именно это: в наивном дизайне высокоуровневый модуль зависит от низкоуровневого. После DIP низкоуровневый модуль зависит от интерфейса высокоуровневого. Стрелка зависимости буквально инвертирована. Это происходит только когда интерфейс живёт в домене потребителя.
</Inset>

## DIP на B2B-платформе: до и после

**До DIP** платформа имела такую структуру:

- `billing/` импортирует `order/infra/postgres-order-repo.ts` → конкретное к конкретному
- `billing/` импортирует `payment/stripe-gateway.ts` → конкретное к конкретному
- `order/` импортирует `db/postgres-driver.ts` → конкретное к конкретному

Каждый модуль мог сломать каждый другой через изменения реализации. Добавление нового платёжного провайдера требовало изменений в billing. Замена БД требовала затронуть всё.

**После DIP**:

- `billing/ports/IOrderPort.ts` — принадлежит billing; определяет `getConfirmedOrder`
- `billing/ports/IPaymentPort.ts` — принадлежит billing; определяет `chargeCustomer`
- `order/adapters/BillingOrderAdapter.ts` — реализует `IOrderPort`, живёт в модуле order, но зависит от интерфейса billing
- `payment/adapters/StripePaymentAdapter.ts` — реализует `IPaymentPort`, живёт в модуле payment, но зависит от интерфейса billing

Billing теперь не имеет compile-time знания о внутренностях модуля order или модуля payment. Модули order и payment зависят от определений границы billing. Добавление нового платёжного провайдера означает написание нового адаптера, реализующего `IPaymentPort` — сам billing не меняется.

<Inset kind="note" lang="ru">
Распространённое заблуждение: DIP не означает «всегда используй интерфейс для всего». Он означает, что на значимых архитектурных границах — где высокоуровневый модуль политики должен пересекать в территорию низкоуровневого механизма — интерфейс должен принадлежать высокоуровневой стороне. Для внутренних деталей внутри модуля или для стабильных сторонних инфраструктурных библиотек накладные расходы на инверсию интерфейса могут не окупаться. DIP — инструмент для архитектурных границ, а не универсальное стилевое правило.
</Inset>

<Quiz
  id="arch-02-02-quiz-1"
  lessonSlug="02-dependency-inversion"
  lang="ru"
  question="Модуль order публикует интерфейс `IOrderRepository` в собственном пакете: `order/interfaces/IOrderRepository.ts`. Модуль billing импортирует и использует его. Команда order позже проводит рефакторинг, разбивая интерфейс на `IOrderReader` и `IOrderWriter`. Billing ломается. Что пошло не так с точки зрения DIP?"
  choices={[
    { label: "Интерфейс принадлежал модулю order (поставщику), а не billing (потребителю). Команда order могла изменить интерфейс, потому что он находился в их пакете. DIP требует, чтобы потребитель владел интерфейсом — billing должен был определить IOrderPort в собственном пакете, а модуль order реализовал бы этот интерфейс.", correct: true },
    { label: "Команда order не должна была менять опубликованный интерфейс — обратная совместимость всегда обязательна.", misconception: "Соглашения об обратной совместимости — это социальный договор, а не структурное принуждение. Структурная проблема в том, что интерфейс жил в пакете модуля order, поэтому команда order вообще могла его изменить. Владение определяет, кто может менять что. DIP перемещает интерфейс в домен потребителя именно для того, чтобы потребитель контролировал его изменения." },
    { label: "Billing должен был использовать конкретный PostgresOrderRepository вместо интерфейса — это было бы стабильнее.", misconception: "Использование конкретного класса напрямую — это до-DIP-проблема, а не решение. Конкретные реализации меняются ещё чаще, чем интерфейсы. Проблема в истории — не в использовании интерфейса, а в его владении." },
    { label: "Команда order разбила один интерфейс на два — интерфейсы никогда не должны разбиваться.", misconception: "Разбиение большого интерфейса на меньшие сфокусированные — часто хороший дизайн (Interface Segregation Principle). Проблема не в самом разбиении, а в том, что оно произошло без участия billing, потому что интерфейс принадлежал модулю order." },
  ]}
/>

<Quiz
  id="arch-02-02-quiz-2"
  lessonSlug="02-dependency-inversion"
  lang="ru"
  question="Модуль billing определяет IOrderPort в собственном пакете. На этапе компиляции BillingService.ts импортирует только IOrderPort. В runtime контейнер dependency injection связывает IOrderPort с PostgresOrderRepository. Новый разработчик говорит: «BillingService всё равно вызывает PostgreSQL в runtime — DIP не устранил зависимость». Как ты ответишь?"
  choices={[
    { label: "DIP разделяет compile-time знание от runtime-поведения. BillingService не имеет compile-time зависимости на PostgresOrderRepository — компилятор не видит его из контекста billing. В runtime DI-контейнер выполняет связывание. Зависимость, которую устраняет DIP — импорт в исходном коде, который и создаёт жёсткость и каскадную перекомпиляцию. Runtime-вызов остаётся; это нормально — так выполняется код.", correct: true },
    { label: "Разработчик прав — DIP не снижает реальных зависимостей, только создаёт иллюзию развязки.", misconception: "DIP снижает compile-time coupling — источник жёсткости. Compile-time coupling вынуждает перекомпиляцию, делает вынесение невозможным и создаёт давление каскадных изменений. Runtime coupling (один модуль вызывает другой) нельзя устранить — это просто выполняющийся код. DIP специально про compile-time зависимость, а не про runtime-вызов." },
    { label: "Чтобы устранить runtime-зависимость, следует использовать очереди сообщений вместо прямых вызовов.", misconception: "Очереди сообщений решают runtime coupling в распределённых системах. DIP касается направления зависимостей в исходном коде внутри кодовой базы. Это разные проблемы. Модуль billing может использовать прямой runtime-вызов через интерфейс (DIP применён верно) или очередь сообщений — оба ортогональны DIP." },
    { label: "Следует добавить anti-corruption layer между billing и DI-контейнером для полного разделения.", misconception: "Anti-corruption layer — это DDD-паттерн для трансляции между bounded contexts. Описанная структура (BillingService импортирует IOrderPort, контейнер связывает с PostgresOrderRepository) уже правильно применяет DIP. Никакого недостающего структурного слоя нет." },
  ]}
/>

<Quiz
  id="arch-02-02-quiz-3"
  lessonSlug="02-dependency-inversion"
  lang="ru"
  question="В hexagonal architecture (unit 04) домен определяет «порты» — интерфейсы для всех исходящих зависимостей. Модуль домена владеет этими портами. Инфраструктурные адаптеры реализуют их. Какое утверждение объясняет, почему hexagonal architecture структурно является DIP, применённым на архитектурном уровне?"
  choices={[
    { label: "Каждый порт — интерфейс, принадлежащий высокоуровневому модулю домена (потребителю внешних сервисов). Каждый адаптер — низкоуровневый конкретный модуль, реализующий порт. Оба — домен и адаптер — зависят от порта-интерфейса: точно структура DIP: высокоуровневый владеет абстракцией, низкоуровневый зависит от неё для подключения.", correct: true },
    { label: "Hexagonal architecture не связана с DIP — она про тестируемость, а не направление зависимостей.", misconception: "Тестируемость — одно из преимуществ hexagonal architecture, но структурный механизм, делающий её возможной, — DIP. Домен тестируется без инфраструктуры именно потому, что зависит только от интерфейсов (портов), которыми владеет, а не от конкретной инфраструктуры. Преимущество тестируемости следует из инверсии зависимостей." },
    { label: "В hexagonal architecture инфраструктурный слой владеет портами, а не домен — это делает адаптеры подключаемыми.", misconception: "Это задом наперёд. Если инфраструктура владела бы портами, домен зависел бы от определений инфраструктуры — это до-инверсионная проблема. В hexagonal architecture домен владеет портами. Инфраструктурные адаптеры зависят от определений портов домена для их реализации." },
    { label: "DIP применяется только внутри одного модуля; hexagonal architecture применяет его через всю системную границу.", misconception: "DIP применяется и на архитектурном уровне — он не ограничен одним модулем. Clean Architecture Роберта Мартина применяет DIP через всю многоуровневую систему. Hexagonal architecture применяет его на границе домена. Оба — DIP; масштаб разный, механизм одинаковый." },
  ]}
/>

</Explanation>

<KeyTakeaway>DIP — не просто «заведи интерфейс». Интерфейс должен принадлежать потребителю — высокоуровневому модулю политики, а не поставщику. Когда потребитель владеет интерфейсом, стрелка зависимости в исходном коде инвертируется: низкоуровневый поставщик теперь зависит от определения границы высокоуровневого потребителя. Это compile-time граница, на которой построены порты hexagonal, use-case-интерфейсы clean architecture и каждый другой паттерн на основе инверсии.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-02-02-retrieval"
  lang="ru"
  questions={[
    { q: "Каковы два утверждения DIP и почему второе (абстракции не должны зависеть от деталей) так же важно, как первое?", a: "Первое утверждение: высокоуровневые модули не должны зависеть от низкоуровневых — оба должны зависеть от абстракций. Второе утверждение: абстракции не должны зависеть от деталей — детали должны зависеть от абстракций. Второе важно, потому что размещение интерфейса в пакете низкоуровневого модуля технически удовлетворяет первому утверждению (billing импортирует интерфейс, а не класс), но нарушает второе. Интерфейс всё ещё определяется и контролируется низкоуровневым модулем (деталью). Когда низкоуровневый модуль реструктурирует пакет, переименовывает или разбивает интерфейс, потребители по-прежнему ломаются. Второе утверждение требует, чтобы интерфейс был определён на высокоуровневой стороне." },
    { q: "На B2B-платформе где именно должен жить IOrderPort и почему расположение пакета определяет, кто может его изменить?", a: "IOrderPort должен жить в пакете модуля billing (или в выделенном пакете границы, который billing контролирует). Расположение пакета определяет владение, потому что в большинстве модульных систем пакет модуля — его исключительное пространство имён. Если IOrderPort живёт в `billing/ports/`, только команда billing его модифицирует — они меняют его когда меняются нужды billing. Если он живёт в `order/interfaces/`, команда order может модифицировать его в любое время по своим причинам, и billing зависит от этих решений. Владение интерфейсом = владение пакетом = кто контролирует эволюцию." },
    { q: "Что такое 'compile-time граница', которую проводит DIP, и как DI-контейнер соотносится с ней?", a: "Compile-time граница — это линия, где заканчивается знание исходного кода BillingService: BillingService.ts импортирует IOrderPort (интерфейс) и ничего другого о модуле order. На этапе компиляции компилятор видит код billing и интерфейс billing — ему не нужно разрешать PostgresOrderRepository для компиляции BillingService.ts. Этот класс невидим billing на этапе компиляции. Работа DI-контейнера — выполнить runtime-разрешение: он знает, что IOrderPort должен быть удовлетворён PostgresOrderRepository, и передаёт построенный экземпляр в BillingService при запуске. Контейнер работает на другой стороне compile-time границы. Это точное разделение между тем, что контролирует архитектор (направление compile-time зависимости), и тем, что контролирует операционный слой (runtime-связывание)." },
  ]}
/>

<Recap lang="ru">
Принцип инверсии зависимостей состоит из двух половин. Первая: высокоуровневые модули (бизнес-политика) не должны зависеть от низкоуровневых (технический механизм) — оба должны зависеть от абстракций. Вторая: эти абстракции должны принадлежать высокоуровневой стороне, а не низкоуровневой.

Владение интерфейсом — операциональное следствие. Интерфейс, определённый в пакете низкоуровневого модуля, всё ещё принадлежит низкоуровневому модулю — поставщик может изменить его, разбить, переместить и сломать каждого потребителя. Интерфейс, определённый в пакете высокоуровневого модуля, принадлежит потребителю — он меняется только когда меняются требования потребителя, и поставщик должен соответствовать контракту потребителя.

Структурный результат: после DIP стрелка зависимости в исходном коде от низкоуровневого модуля указывает в сторону интерфейса высокоуровневого, а не от него. Поставщик зависит от определения границы потребителя. Это и есть настоящая инверсия.

Этот механизм делает hexagonal architecture (unit 04) и clean architecture (unit 05) структурно соблюдаемыми. В hexagonal architecture домен владеет своими портами — каждый исходящий интерфейс определён доменом. Инфраструктурные адаптеры реализуют эти порты и поэтому зависят внутрь. В clean architecture правило зависимостей ведёт к центру — внутренние круги определяют интерфейсы, внешние реализуют их. DIP — не архитектурный паттерн сам по себе; это базовый механизм, на который опираются все эти паттерны для соблюдения границ зависимостей на этапе компиляции.
</Recap>
