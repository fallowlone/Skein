---
slug: "01-the-dependency-rule"
lang: ru
track: "architecture-patterns"
unit: "02-dependency-direction"
order: 1
title: "Правило зависимостей"
summary: "Зависимости в исходном коде должны указывать в сторону стабильных абстракций. Конкретное зависит от абстрактного — не наоборот. Правило разделяет flow-of-control и flow-of-source-dependency и разрывает цепочки принудительных изменений."
estMin: 20
status: ready
lessonType: topic
level: middle
concepts:
  - dependency-rule
  - source-code-dependency
  - stability
  - flow-of-control
  - flow-of-dependency
  - abstraction-direction
prereqs: []
sources:
  - https://blog.cleancoder.com/uncle-bob/2012/08/13/the-clean-architecture.html
  - https://www.informit.com/store/clean-architecture-a-craftsmans-guide-to-software-structure-9780134494166
  - https://martinfowler.com/articles/dipInTheWild.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import concreteVsAbstract from "./infographics/concrete-vs-abstract.svg?raw";

<Hook>
На B2B-платформе заказов был модуль billing и модуль order. Billing зависел от order, order зависел от слоя доступа к данным, тот — от драйвера PostgreSQL. Всё логично: каждый слой зависит от нижележащего. Затем бизнес попросил новый платёжный воркфлоу: billing должен был напрямую вызывать платёжный шлюз, а шлюз — знать о статусе заказа, чтобы предотвратить двойное списание. Добавили модуль payment gateway, который тоже стал зависеть от order. Через два месяца команда захотела вынести billing в отдельный сервис. Не получилось: billing импортировал order, order импортировал слой БД, payment gateway тоже импортировал order, а billing успел импортировать хелперы из payment gateway напрямую. Зависимости образовали паутину. Каждый модуль знал о каждом другом конкретном модуле. Вынести billing означало тащить с собой всю платформу. Команда так и не задала вопрос, который предотвратил бы это: в каком направлении должны указывать зависимости в исходном коде?
</Hook>

<Crux>Зависимости в коде указывают к стабильным абстракциям: конкретное зависит от абстрактного, не наоборот — control flow не влияет.</Crux>

<Explanation>

## Два разных потока: поток управления и зависимость в исходном коде

Путаница, порождающая большинство проблем с зависимостями, — это смешение двух различных вещей, происходящих в коде одновременно: **flow of control** и **flow of source-code dependency**.

**Flow of control** — это то, что выполняется в runtime. Когда модуль billing вызывает `orderService.getOrder(id)`, управление переходит от billing в order service. Это путь выполнения — кто вызывает кого.

**Flow of source-code dependency** — это то, что видит компилятор. Когда billing содержит `import { OrderService } from '../order/orderService'`, billing имеет зависимость в исходном коде на модуль order. На этапе компиляции, если `orderService.ts` изменит свой экспортируемый интерфейс, billing потребует перекомпиляции. Это путь зависимости — кто знает о ком.

Эти два потока независимы. Они могут указывать в одну сторону, а могут — в противоположные. Правило зависимостей касается второго потока, а не первого. Управление всегда будет течь туда, куда диктуют runtime-вызовы. Но ты выбираешь, в каком направлении движется знание — импорт, `requires`, ссылка на тип — между модулями.

Критический вопрос: **если модуль изменится, какие другие модули вынуждены знать об этом?**

<FlowDiagram
  label="Flow of control vs flow of source-code dependency"
  nodes={[
    { id: "billing", label: "BillingModule", sub: "concrete — вызывает OrderService" },
    { id: "order", label: "OrderService", sub: "concrete — реализует логику" },
    { id: "iorder", label: "IOrderPort", sub: "abstract — интерфейс принадлежит billing" },
  ]}
  edges={[
    { from: "billing", to: "order", label: "control flows →" },
    { from: "billing", to: "iorder", label: "import (source dep)" },
    { from: "order", to: "iorder", label: "implements (source dep)" },
  ]}
  caption="Управление по-прежнему течёт от BillingModule к OrderService в runtime. Но зависимость в исходном коде инвертирована: и BillingModule, и OrderService зависят от IOrderPort. BillingModule больше не импортирует OrderService напрямую — он знает только об интерфейсе. Изменения в OrderService не вынуждают перекомпилировать BillingModule."
/>

## Правило зависимостей: указывай в сторону стабильности

Правило зависимостей Роберта Мартина, впервые сформулированное в контексте Clean Architecture, — это принцип, формализующий направление зависимостей в исходном коде:

> **Зависимости в исходном коде должны указывать в направлении стабильности.**

Стабильность здесь имеет конкретное значение: модуль стабилен, если он маловероятно изменится. Две силы делают модуль стабильным:
- **Высокий afferent coupling (Ca)**: на него зависит много модулей, поэтому он испытывает сильное давление оставаться на месте — его изменение ломает слишком много вызывающих.
- **Абстрактность, а не конкретность**: он определяет интерфейс или контракт, а не реализацию. Интерфейсы не меняются из-за деталей реализации; они меняются только тогда, когда сам контракт должен эволюционировать.

Правило зависимостей следует из этого: если модуль A зависит от модуля B, то при каждом изменении B модуль A может быть вынужден измениться тоже. Чтобы минимизировать принудительные изменения, A должен зависеть от вещей, которые меняются реже всего — от стабильных, абстрактных — а не от вещей, которые меняются чаще всего — от volatile, конкретных.

На B2B-платформе модуль billing должен зависеть от концепции «что-то, предоставляющее данные о заказе» — интерфейса — а не от конкретного `PostgresOrderRepository`, который реализует это сегодня. Если реализация переключится на другую БД или команда order проведёт рефакторинг внутренностей репозитория, billing останется нетронутым. Зависимость billing — на контракт, который стабилен, а не на реализацию, которая volatile.

<Inset kind="why" lang="ru">
Почему указание в сторону стабильности снижает жёсткость? Потому что жёсткость — невозможность изменить один модуль без каскадных изменений — вызвана зависимостями, идущими от стабильных модулей к volatile. Если billing (относительно стабилен, много вещей его вызывает) зависит напрямую от PostgreSQL-репозитория (volatile, детали реализации меняются), то каждое внутреннее изменение репозитория вынуждает перекомпилировать и проверять billing и каждый модуль, который billing экспортирует. Разворот этого направления — billing зависит только от интерфейса, репозиторий реализует его — означает, что репозиторий может свободно меняться, пока контракт держится. Стабильность становится свойством того, куда указывает стрелка зависимости, а не только того, как часто меняется код.
</Inset>

## Конкретное зависит от абстрактного, но не наоборот

Практическая форма правила зависимостей: **конкретные вещи зависят от абстрактных, никогда наоборот**.

Абстрактный модуль (интерфейс, протокол, порт в терминологии hexagonal) — это контракт. Он определяет, что что-то *делает*, не уточняя как. Он меняется только когда контракт эволюционирует — что происходит редко и намеренно.

Конкретный модуль (класс, сервис, реализация репозитория) — это единица выполнения. Он реализует контракт с использованием конкретной технологии, алгоритма или внешней зависимости. Он меняется каждый раз, когда нужно изменить реализацию — что происходит часто.

Если конкретный модуль A зависит от другого конкретного модуля B, A должен меняться при каждом изменении реализации B. Это создаёт цепочки принудительных изменений, распространяющихся по кодовой базе. Разворот направления — конкретное зависит от абстрактного — разрывает цепочку.

Зависимость billing от `IOrderRepository` вместо `PostgresOrderRepository` — пример: `PostgresOrderRepository` реализует `IOrderRepository`. Цепочка зависимостей:

```
BillingService → IOrderRepository ← PostgresOrderRepository
```

Оба — billing и реализация Postgres — зависят от интерфейса. Ни один не зависит от другого. Реализацию можно подменить тест-дублером, другой БД или REST API клиентом — и billing не изменится.

<Infographic
  svg={concreteVsAbstract}
  label="Сравнение двух направлений зависимости для одного и того же модуля billing: зависимость от конкретного модуля (PostgresOrderRepository) каскадом распространяет изменения вверх и заставляет вынесение тащить всю платформу, тогда как зависимость от абстрактного интерфейса (IOrderRepository) изолирует изменения и позволяет вынести billing вместе только с его интерфейсами."
  caption="Те же модули, противоположные последствия: направить зависимость конкретного модуля на абстракцию — а не на другой конкретный модуль — это и выводит billing из радиуса поражения и делает его извлекаемым."
  hue="var(--d-infra)"
/>

<Inset kind="note" lang="ru">
Слова «стабильный» и «абстрактный» коррелируют, но не тождественны. Очень широко используемый, очень конкретный класс (например, утилитарная библиотека) может быть стабильным на практике, потому что никто его не меняет. Но стабильность по соглашению хрупка — она зависит от социального договора, а не от структурного принуждения. Привязка зависимости к интерфейсу делает стабильность структурной: интерфейс меняется только когда контракт меняется, а не когда меняется реализация. Структура надёжнее соглашения.
</Inset>

<Quiz
  id="arch-02-01-quiz-1"
  lessonSlug="01-the-dependency-rule"
  lang="ru"
  question="Модуль billing содержит строку `import { PostgresOrderRepository } from '../order/infra/postgres-order-repository'`. Что именно неправильно в этом импорте и какое структурное исправление верно?"
  choices={[
    { label: "Billing имеет зависимость в исходном коде на конкретную деталь реализации модуля order. Исправление — сделать billing зависимым от интерфейса (IOrderRepository), с PostgresOrderRepository, реализующим этот интерфейс — чтобы billing был изолирован от изменений реализации Postgres.", correct: true },
    { label: "Billing не должен ничего импортировать из модуля order — все модули должны быть независимы.", misconception: "Модулям нужно взаимодействовать — нулевые кросс-модульные импорты не являются целью. Проблема не в том, что billing импортирует из order; проблема в том, что billing импортирует конкретный класс реализации, а не стабильную абстракцию. Исправление — ввести интерфейс и инвертировать зависимость, а не устранять её полностью." },
    { label: "Путь импорта использует относительные `..` сегменты — должен быть абсолютный импорт.", misconception: "Формат пути импорта — не структурная проблема здесь. Проблема в том, что billing импортирует конкретный класс репозитория, создавая зависимость в исходном коде на volatile-модуль. Исправление — введение интерфейса, независимо от стиля пути импорта." },
    { label: "PostgresOrderRepository находится в подкаталоге `infra` — его следует переместить в общее место.", misconception: "Перемещение файла не изменит отношение зависимости. Billing по-прежнему будет импортировать конкретный класс. Структурная проблема — в направлении зависимости (конкретное к конкретному), а не в расположении файла." },
  ]}
/>

<Quiz
  id="arch-02-01-quiz-2"
  lessonSlug="01-the-dependency-rule"
  lang="ru"
  question="В runtime BillingService вызывает orderRepository.getOrder(id), который выполняет логику БД и возвращает заказ. На этапе компиляции BillingService импортирует IOrderRepository (интерфейс), а PostgresOrderRepository реализует IOrderRepository. Какое утверждение верно описывает отношение между flow of control и зависимостью в исходном коде?"
  choices={[
    { label: "Управление течёт от BillingService через IOrderRepository к PostgresOrderRepository в runtime. Зависимость в исходном коде течёт от BillingService к IOrderRepository и от PostgresOrderRepository к IOrderRepository — оба указывают на абстракцию. Управление и зависимость указывают в разные стороны.", correct: true },
    { label: "Управление и зависимость в исходном коде оба течут в одном направлении: BillingService → IOrderRepository → PostgresOrderRepository.", misconception: "Это неверно относительно зависимостей в исходном коде. PostgresOrderRepository не зависит от IOrderRepository в том смысле, что IOrderRepository нуждается в PostgresOrderRepository для компиляции — всё наоборот. PostgresOrderRepository реализует (зависит от) IOrderRepository: он должен соответствовать форме интерфейса. Интерфейс не знает о PostgresOrderRepository." },
    { label: "Зависимость в исходном коде и flow of control всегда указывают в одном направлении — их нельзя разделить.", misconception: "Это именно то заблуждение, которое правило зависимостей исправляет. Через интерфейс или абстрактный тип можно заставить управление течь в одном направлении (вызывающий → реализация), пока зависимость в исходном коде указывает в противоположном (реализация → интерфейс)." },
    { label: "Интерфейс IOrderRepository имеет зависимость в исходном коде на PostgresOrderRepository, потому что определяет, что PostgresOrderRepository должен реализовывать.", misconception: "Это задом наперёд. Интерфейс определяет контракт изолированно — у него нет знаний о том, какие классы его реализуют. PostgresOrderRepository должен импортировать и реализовывать IOrderRepository; у IOrderRepository нет импорта PostgresOrderRepository." },
  ]}
/>

<Quiz
  id="arch-02-01-quiz-3"
  lessonSlug="01-the-dependency-rule"
  lang="ru"
  question="Команда хочет вынести модуль billing в отдельный сервис. После применения правила зависимостей — сделав все кросс-модульные зависимости указывающими на интерфейсы — вынесение заняло два дня. До применения правила та же операция была заброшена через неделю из-за каскадных цепочек импортов. Какое структурное свойство установило правило зависимостей, сделав вынесение реальным?"
  choices={[
    { label: "Зависимости в исходном коде каждого модуля теперь заканчиваются на стабильных интерфейсах, а не на других конкретных модулях. Граница compile-time знания billing — это ровно набор интерфейсов, от которых он зависит — у него нет compile-time знания о реализациях других модулей. Вынесение требует перемещения billing и его интерфейсов; ничего другого менять не нужно.", correct: true },
    { label: "Правило зависимостей уменьшило количество модулей в системе.", misconception: "Правило зависимостей не удаляет модули — оно перенаправляет стрелки между ними. До и после существуют те же модули; меняется лишь то, зависят ли они от конкретных реализаций друг друга или от общих интерфейсов. Меньше модулей — не механизм; более чистые границы зависимостей — да." },
    { label: "Правило зависимостей сделало каждый модуль stateless, что позволило развёртывать их независимо.", misconception: "Правило зависимостей касается направления импортов в исходном коде, а не состояния. Модуль может идеально следовать правилу зависимостей и при этом быть stateful. Возможность вынесения возникает из чётко определённых compile-time границ (интерфейсов), а не из наличия или отсутствия состояния." },
    { label: "Правило зависимостей обеспечило, чтобы ни один модуль не вызывал другой более одного раза.", misconception: "Правило зависимостей ничего не говорит о частоте вызовов. Оно управляет направлением стрелки зависимости в исходном коде. Модуль, следующий правилу, может вызывать интерфейс другого модуля сколько угодно раз — он просто не может импортировать конкретную реализацию." },
  ]}
/>

</Explanation>

<KeyTakeaway>Правило зависимостей разделяет два потока: управление (какой модуль вызывает какой в runtime) и зависимость в исходном коде (какой модуль импортирует какой на этапе компиляции). Правило гласит: зависимости в исходном коде должны указывать в сторону стабильных абстракций — конкретные модули зависят от интерфейсов, интерфейсы никогда не зависят от конкретных модулей. Нарушение создаёт жёсткие системы, где изменение любого конкретного модуля каскадом распространяется вверх.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-02-01-retrieval"
  lang="ru"
  questions={[
    { q: "В чём разница между flow of control и flow of source-code dependency? Почему правило зависимостей управляет только вторым?", a: "Flow of control — это то, что выполняется в runtime: какой модуль вызывает какой метод на каком объекте. Flow of source-code dependency — это то, что видит компилятор: какие модули ссылаются (импортируют) на какие другие модули. Правило зависимостей управляет зависимостью в исходном коде, потому что именно она определяет жёсткость: если модуль A импортирует конкретный класс модуля B, то любое изменение B потенциально вынуждает A перекомпилироваться и меняться. Управление может течь куда угодно по runtime-надобности — это нормально. Проблема в том, что когда flow of control используется как доверенное лицо для структуры импортов (A вызывает B, значит A импортирует конкретный класс B), volatile-характер распространяется вверх по стеку вызовов." },
    { q: "Что означает 'стабильный' в контексте правила зависимостей и почему указание в сторону стабильности снижает жёсткость?", a: "В правиле зависимостей стабильный модуль — тот, который маловероятно изменится. Два структурных свойства коррелируют со стабильностью: высокий afferent coupling (на него зависит много модулей, создавая давление оставаться обратно совместимым) и абстрактность (он определяет контракт, а не реализацию — контракт меняется только когда бизнес-контракт меняется). Указание в сторону стабильности снижает жёсткость, потому что жёсткость вызвана volatile конкретными модулями в цепочке импортов многих других модулей. Если billing импортирует PostgresOrderRepository и тот меняет внутреннюю структуру, billing попадает в зону взрыва — не потому что использует изменённую функциональность, а потому что импортировал модуль." },
    { q: "На B2B-платформе, до применения правила зависимостей, почему вынесение модуля billing требовало тащить с собой всю платформу?", a: "Потому что импорты billing образовывали транзитивное замыкание над всей платформой. Billing импортировал PostgresOrderRepository (конкретный). PostgresOrderRepository импортировал драйвер БД и общие утилиты. Payment gateway импортировал модуль order (конкретный). Billing также импортировал хелперы из payment gateway. Компилятор разрешает все транзитивные импорты: чтобы скомпилировать billing как отдельный сервис, нужно всё, что импортирует billing, и всё, что импортируют эти модули — рекурсивно. При отсутствии слоя абстракции это транзитивное замыкание было всей платформой." },
  ]}
/>

<Recap lang="ru">
Правило зависимостей — это дисциплина о том, в каком направлении указывают импорты в исходном коде, а не о том, в каком направлении идут вызовы в runtime. Flow of control (кто вызывает кого) и flow of source-code dependency (кто импортирует кого) независимы. Правило гласит: зависимости в исходном коде должны указывать в сторону стабильных абстракций.

Стабильность означает маловероятность изменений. Абстрактный модуль — интерфейс, порт, контракт — структурно стабилен, потому что меняется только когда контракт эволюционирует. Конкретный модуль — реализация репозитория, адаптер шлюза, класс фреймворка — volatile, потому что меняется при каждом изменении реализации. Правило зависимостей гласит: volatile конкретные модули зависят от стабильных абстрактных, но не наоборот.

Структурное следствие: конкретное зависит от абстрактного, абстрактное никогда не зависит от конкретного. На B2B-платформе BillingService импортирует IOrderRepository (абстрактный, стабильный). PostgresOrderRepository реализует IOrderRepository. Ни один не зависит от другого напрямую. Управление по-прежнему течёт от billing к репозиторию в runtime; зависимость в исходном коде от обоих идёт к интерфейсу.

Нарушение этого правила — конкретное импортирует конкретное — создаёт цепочки принудительных изменений. Исправление требует введения абстракции на каждой границе, где volatile-модуль импортируется модулем, который должен быть от него изолирован. Следующий урок рассматривает механизм, выполняющий это инвертирование: принцип инверсии зависимостей.
</Recap>
