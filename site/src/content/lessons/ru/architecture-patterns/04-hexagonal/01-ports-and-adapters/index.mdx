---
slug: "01-ports-and-adapters"
lang: ru
track: "architecture-patterns"
unit: "04-hexagonal"
order: 1
title: "Порты и адаптеры"
summary: "В hexagonal architecture порт — это интерфейс, которым владеет domain. Адаптер — любой компонент, реализующий или вызывающий его. Эта симметрия делает HTTP, CLI и базу данных просто адаптерами на границе domain."
estMin: 22
status: ready
lessonType: topic
level: middle
concepts:
  - port
  - adapter
  - domain-owned-interface
  - application-boundary
  - hexagonal-architecture
  - symmetry
prereqs: []
sources:
  - https://alistair.cockburn.us/hexagonal-architecture/
  - https://martinfowler.com/bliki/HexagonalArchitecture.html
  - https://www.informit.com/store/clean-architecture-a-craftsmans-guide-to-software-structure-9780134494166
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
На B2B-платформе заказов при запуске было три канала доставки: HTTP REST API для веб-фронтенда, CLI-инструмент для команды эксплуатации и ночной batch-job, подтягивавший заказы из SFTP-дропа партнёра. Каждый канал содержал собственную копию логики подтверждения заказа. Когда продуктовая команда добавила шаг согласования с отделом биллинга, три инженера обновили три разных кодовых пути. Двое справились. Третий пропустил граничный случай для заказов свыше $10 000. Баг прожил в продакшене шесть недель, потому что никто не знал, что CLI имеет собственную логику подтверждения. Проблема была не в трёх каналах — у приложения не было границы. Не было единственного определения «подтвердить заказ», через которое вызывала бы каждая точка входа. Именно эту проблему Alistair Cockburn описывал в 2005 году, представляя hexagonal architecture: приложению нужна чёткая граница, через которую каждый вызывающий — человек, автоматика или тест — должен проходить через одинаковый интерфейс.
</Hook>

<Crux>Порт — это интерфейс, которым владеет domain и который определяет, как внешний мир взаимодействует с приложением; адаптер — любой компонент, реализующий или вызывающий его — UI и база данных оба просто адаптеры.</Crux>

<Explanation>

## Что такое порт на самом деле

Слово «port» в hexagonal architecture заимствовано из аппаратного обеспечения: порт — это разъём, определённый протокол подключения. В программировании порт — это интерфейс, который **определяет само приложение**. Это владение и отличает hexagonal architecture от слоистой.

На B2B-платформе заказов приложение могло бы определить порт так:

```typescript
// Определён внутри приложения — в собственном пакете domain
interface OrderConfirmationPort {
  confirmOrder(orderId: string, approverId: string): Promise<ConfirmationResult>;
}
```

Этот интерфейс принадлежит приложению. Он выражает то, что умеет делать приложение, — на языке domain, а не HTTP или CLI. Приложение определяет контракт; внешний мир должен ему соответствовать.

В этом инверсия, отличающая hexagonal от слоистой архитектуры. В стандартном слоистом стеке application layer вызывает `OrderRepository` — инфраструктурный класс: приложение зависит от infrastructure. В hexagonal приложение определяет `IOrderRepository` как порт — infrastructure зависит от определения интерфейса приложения. Стрелка зависимости идёт от infrastructure к domain, никогда наоборот.

## Что такое адаптер на самом деле

Адаптер — это любой компонент, переводящий между внешним миром и портом. Адаптер либо:

- **Вызывает порт** (HTTP-контроллер вызывает `OrderConfirmationPort.confirmOrder`, когда приходит POST-запрос)
- **Реализует порт** (`PostgresOrderRepository` реализует `IOrderRepository`, удовлетворяя потребности domain в данных)

Ключевое наблюдение: эти две категории **симметричны**. HTTP-контроллер и PostgreSQL-репозиторий — оба адаптеры: один на левой стороне гексагона (вызывающий), один на правой (вызываемый). Структурно они эквивалентны: каждый — слой трансляции между внешней технологией и интерфейсом порта.

Эта симметрия и делает название архитектуры точным. У гексагона есть грани, и каждая может иметь порт. HTTP-адаптер подключается к одной грани; gRPC-адаптер мог бы подключиться к той же грани (через тот же порт). PostgreSQL-адаптер подключается к другой грани; in-memory-адаптер мог бы подключиться к той же грани. Application core не знает и не заботится, какие адаптеры подключены.

<FlowDiagram
  label="Hexagonal architecture: ядро приложения окружено портами и адаптерами"
  nodes={[
    { id: "http", label: "HTTP Controller", sub: "driving adapter (левый)" },
    { id: "cli", label: "CLI Adapter", sub: "driving adapter (левый)" },
    { id: "port-in", label: "OrderConfirmationPort", sub: "primary port — принадлежит domain" },
    { id: "core", label: "Application Core", sub: "domain + use cases" },
    { id: "port-out", label: "IOrderRepository", sub: "secondary port — принадлежит domain" },
    { id: "postgres", label: "PostgresOrderRepository", sub: "driven adapter (правый)" },
    { id: "inmem", label: "InMemoryOrderRepository", sub: "driven adapter (правый — тест)" },
  ]}
  edges={[
    { from: "http", to: "port-in", label: "вызывает" },
    { from: "cli", to: "port-in", label: "вызывает" },
    { from: "port-in", to: "core", label: "управляет" },
    { from: "core", to: "port-out", label: "вызывает (source dep)" },
    { from: "postgres", to: "port-out", label: "реализует (source dep)" },
    { from: "inmem", to: "port-out", label: "реализует (source dep)" },
  ]}
  caption="Application core определяет оба порта. Driving adapters (HTTP, CLI) вызывают primary port, активируя приложение. Driven adapters (Postgres, InMemory) реализуют secondary port, удовлетворяя инфраструктурные потребности domain. Каждая стрелка на границе порта указывает к приложению — domain владеет контрактом."
/>

## Симметрия: UI и база данных — оба просто адаптеры

Это наиболее практически полезное следствие hexagonal architecture. HTTP-слой не привилегирован — это просто адаптер, переводящий HTTP-запросы в вызовы порта. База данных не привилегирована — это просто адаптер, реализующий порт репозитория. Оба **внешние** по отношению к application core.

Из этого следует немедленное структурное следствие: можно независимо менять любую сторону. Заменить HTTP-адаптер на gRPC — application core не изменится. Заменить PostgreSQL на DynamoDB — application core не изменится. Добавить CLI-адаптер рядом с HTTP — application core не изменится. Именно этого не хватало B2B-платформе: если бы логика подтверждения заказа жила за единственным `OrderConfirmationPort`, HTTP-контроллер, CLI-адаптер и batch-адаптер вызывали бы один и тот же порт. Одно исправление бага в одном месте.

<Inset kind="why" lang="ru">
Почему Cockburn назвал архитектуру «hexagonal», а не круговой или квадратной? Гексагон не несёт особого значения — шесть граней не обязательное требование. Cockburn выбрал гексагон, чтобы на диаграммах было место для нескольких адаптеров на каждой стороне без сужения формы. Значимая форма в том, что приложение — это **замкнутая область** с явными точками входа/выхода (портами), а не открытый слоистый стек, где что угодно может импортировать что угодно. Количество граней — удобство для рисования, а не архитектурное ограничение.
</Inset>

## Порты как API и SPI приложения

Существуют две категории портов, напрямую соответствующие двум сторонам гексагона:

**Primary ports (API — Application Programming Interface)** определяют, как внешний мир управляет приложением. Это use case'ы, которые приложение предоставляет. На B2B-платформе: `OrderConfirmationPort`, `InvoiceGenerationPort`, `BillingApprovalPort`. Driving adapters вызывают эти порты.

**Secondary ports (SPI — Service Provider Interface)** определяют, что приложению нужно от infrastructure. Приложение вызывает эти порты; infrastructure реализует их. На B2B-платформе: `IOrderRepository`, `IPaymentGateway`, `IEmailNotifier`, `IEventPublisher`. Это в точности dependency-inverted интерфейсы из unit 02 — DIP, применённый на архитектурной границе.

Названия API и SPI пришли из экосистемы Java, но концепция универсальна. Приложение определяет обе стороны: что оно предлагает (API/primary ports) и что ему требуется (SPI/secondary ports). Внешний мир должен соответствовать обеим.

<Inset kind="note" lang="ru">
Распространённая ошибка — думать, что «порт — это HTTP-эндпоинт» или «порт — это таблица базы данных». Ни то ни другое. Порты определяются на языке domain. `OrderConfirmationPort.confirmOrder(orderId, approverId)` говорит на языке domain заказов — он ничего не знает о HTTP-глаголах, JSON-телах, SQL-таблицах или топиках очередей сообщений. Перевод между HTTP-языком и domain-языком — это именно работа адаптера. Такое разделение — domain-язык на порту, технологический язык в адаптере — делает приложение тестируемым без HTTP-инфраструктуры и заменяемым без переписывания domain.
</Inset>

<Quiz
  id="arch-04-01-quiz-1"
  lessonSlug="01-ports-and-adapters"
  lang="ru"
  question="Команда определяет IOrderRepository в пакете infrastructure рядом с PostgresOrderRepository. OrderService domain импортирует его оттуда. Является ли это hexagonal architecture и почему?"
  choices={[
    { label: "Нет. В hexagonal architecture порты определяются приложением/domain — потребитель владеет интерфейсом. Если IOrderRepository живёт в пакете infrastructure, команда infrastructure контролирует его и может менять без согласия domain. Domain теперь зависит от интерфейса, принадлежащего infrastructure — стрелка зависимости всё ещё указывает наружу (domain → пакет infrastructure). Структурная инверсия не произошла.", correct: true },
    { label: "Да. Между domain и infrastructure присутствует интерфейс — это и есть требование hexagonal architecture.", misconception: "Наличие интерфейса необходимо, но недостаточно для hexagonal architecture. Ключевое структурное требование — владение портом: domain должен владеть определением порта. Интерфейс в пакете infrastructure принадлежит infrastructure — domain всё ещё зависит от определений infrastructure. Это та же структурная проблема, что и в истории с billing-модулем, импортирующим интерфейс команды order." },
    { label: "Да, потому что IOrderRepository — это порт: любой интерфейс между слоями квалифицируется как hexagonal port.", misconception: "Не каждый интерфейс является портом в смысле hexagonal. Порт — это именно интерфейс, принадлежащий domain, определяющий, что приложение требует от внешнего мира или предлагает ему, на языке domain. Интерфейс, живущий в пакете infrastructure, определённый для удобства реализации и говорящий на языке infrastructure, не является hexagonal port — это интерфейс-образная деталь infrastructure." },
    { label: "Это зависит от того, используют ли методы интерфейса доменные типы или инфраструктурные.", misconception: "Типы методов важны для хорошего дизайна, но первичное структурное требование — владение: где определён интерфейс и кто контролирует его эволюцию. Интерфейс с domain-типизированными методами, но принадлежащий пакету infrastructure, всё равно не является hexagonal port, потому что infrastructure контролирует, когда он меняется. Владение предшествует именованию." },
  ]}
/>

<Quiz
  id="arch-04-01-quiz-2"
  lessonSlug="01-ports-and-adapters"
  lang="ru"
  question="На B2B-платформе есть HTTP-контроллер, CLI-адаптер и batch-адаптер — все трое активируют use case подтверждения заказа. Как эти адаптеры должны соотноситься с OrderConfirmationPort в hexagonal architecture?"
  choices={[
    { label: "Все три адаптера вызывают один и тот же OrderConfirmationPort. Каждый адаптер переводит собственный формат входных данных (тело HTTP-запроса, аргументы CLI, строка SFTP-файла) в доменные параметры, требуемые методом порта. Application core реализует confirmOrder один раз. Исправления багов и новая логика в подтверждении применяются один раз и достигают всех трёх точек входа.", correct: true },
    { label: "Каждый адаптер должен иметь собственный вариант порта под свой формат: HttpOrderConfirmationPort, CliOrderConfirmationPort, BatchOrderConfirmationPort — чтобы приложение не было вынуждено обрабатывать специфику адаптеров.", misconception: "Разделение портов по адаптерам уничтожает смысл. Порт — это определение приложением того, что оно умеет делать, на доменном языке. HttpOrderConfirmationPort, CliOrderConfirmationPort и BatchOrderConfirmationPort определяли бы один и тот же use case (подтвердить заказ) в трёх разных местах. Перевод из HTTP/CLI/batch-входных данных в доменные параметры — это задача адаптера, а не порта. Один порт; несколько адаптеров." },
    { label: "HTTP-контроллер должен владеть primary port, поскольку HTTP является доминирующей точкой входа; CLI и batch-адаптеры вызывают контроллер изнутри.", misconception: "Маршрутизация вызовов CLI и batch через HTTP-контроллер для доступа к логике приложения — форма той же исходной проблемы: теперь CLI и batch зависят от HTTP-адаптера. Граница приложения — это порт, а не какой-либо конкретный адаптер. Все точки входа вызывают порт напрямую; ни одна не должна вызывать другой адаптер для доступа к приложению." },
    { label: "Batch-адаптер следует считать secondary (driven) адаптером, поскольку работает асинхронно, тогда как HTTP-контроллер и CLI — primary (driving) адаптеры.", misconception: "Различие между driving и driven адаптерами не определяется синхронностью — оно определяется тем, кто инициирует взаимодействие. Driving adapters вызывают приложение (они инициируют). Batch-адаптер инициирует обработку заказов, читая SFTP-дроп и вызывая порт подтверждения — он управляет приложением, а не управляется им. Это primary adapter, а не secondary. Driven/secondary adapters реализуют порты, которые приложение вызывает наружу (репозитории, платёжные шлюзы, системы уведомлений)." },
  ]}
/>

<Quiz
  id="arch-04-01-quiz-3"
  lessonSlug="01-ports-and-adapters"
  lang="ru"
  question="После применения hexagonal architecture к B2B-платформе команда хочет заменить PostgreSQL на document store для коллекции заказов. Какие компоненты изменятся, а какие останутся прежними?"
  choices={[
    { label: "Изменится только driven adapter: новый DocumentOrderRepository реализует существующий порт IOrderRepository. Определение порта, application core (domain + use cases) и все driving adapters (HTTP, CLI, batch) не затрагиваются. Ни один primary port не меняется, потому что предлагаемые приложением возможности не изменились.", correct: true },
    { label: "Порт IOrderRepository должен измениться, поскольку document stores обладают иными возможностями для запросов, чем реляционные базы данных.", misconception: "Порт определяется на языке domain — что нужно приложению, — а не на языке возможностей базы данных. Если document store может выполнять те же domain-запросы (findById, findByCustomer, save), интерфейс порта не меняется. Если document store не может выполнить domain-запрос, требуемый текущим портом, — это проблема возможностей, решаемая в адаптере, а не причина менять API порта. Порт должен меняться только при изменении требований domain." },
    { label: "Application core изменится, потому что его нужно обновить для использования специфичных возможностей document store.", misconception: "Изменение application core ради использования специфичных для базы данных функций нарушает hexagonal-границу. Application core может знать только о своих портах. Функции, специфичные для реализации базы данных, принадлежат driven adapter. Если при замене баз данных application core меняется — значит, детали реализации базы данных просачивались через порт: признак того, что порт не был чисто доменным языком." },
    { label: "Все компоненты изменятся, потому что любая миграция модели данных требует обновления всего стека.", misconception: "Миграция модели данных меняет то, как данные хранятся и извлекаются — это инфраструктурное знание, живущее в driven adapter. Порт определяет domain-представление данных (findOrder возвращает доменный объект Order). Адаптер переводит между domain-представлением и форматом хранения. Миграция на document store требует нового адаптера и, возможно, скрипта миграции данных; application core и driving adapters затрагивать не нужно." },
  ]}
/>

</Explanation>

<KeyTakeaway>Порт — это интерфейс, определённый и принадлежащий приложению: domain задаёт, что ему нужно и что оно предлагает, на domain-языке. Адаптер — любой компонент, пересекающий границу порта: либо вызывающий primary port (driving adapter), либо реализующий secondary port (driven adapter). Симметрия — UI и база данных оба адаптеры — означает, что любую сторону можно заменить, не трогая core.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-04-01-retrieval"
  lang="ru"
  questions={[
    { q: "Что делает порт «портом» в hexagonal architecture — чем он отличается от любого другого интерфейса?", a: "Три свойства вместе определяют hexagonal port: (1) он принадлежит приложению (domain или application layer), а не infrastructure или presentation layer; (2) он говорит на языке domain — имена методов, типы параметров и возвращаемые типы выражены в domain-концепциях, а не HTTP-глаголах, SQL-параметрах или топиках очередей; (3) он определяет либо то, что приложение предлагает внешнему миру (primary port), либо то, что приложение требует от infrastructure (secondary port). Интерфейс, живущий в пакете infrastructure, сформированный соображениями реализации infrastructure или говорящий на языке базы данных/HTTP, не является портом — это интерфейс-образная деталь infrastructure с теми же рисками связанности, что и импорт конкретного класса." },
    { q: "На B2B-платформе было три адаптера (HTTP, CLI, batch), каждый со своей копией логики подтверждения. Как hexagonal architecture исправляет это структурно?", a: "Hexagonal architecture вводит единственный primary port — OrderConfirmationPort — принадлежащий приложению. Все три driving adapters вызывают этот порт. Логика подтверждения существует один раз, внутри application core. Каждый адаптер отвечает только за перевод своего формата входных данных в доменные параметры, требуемые методом порта (парсинг HTTP-тела, аргументов CLI, строки SFTP-файла), и за форматирование результата порта в соответствующий формат вывода (HTTP-ответ, вывод в терминал, запись в лог). Добавление четвёртой точки входа (например, получатель webhook) добавляет один адаптер и ноль изменений в логику подтверждения. Исправления багов применяются в одном месте и достигают всех точек входа." },
    { q: "Что такое primary ports и secondary ports, и на какой стороне гексагона находится каждый?", a: "Primary ports (API) находятся на левой/driving стороне. Они определяют, что предлагает приложение — его use case'ы на языке domain. Driving adapters (HTTP-контроллеры, CLI-раннеры, потребители очередей, тест-драйверы) вызывают primary ports для активации поведения приложения. Secondary ports (SPI) находятся на правой/driven стороне. Они определяют, что приложению требуется от внешнего мира для работы — интерфейсы репозиториев, платёжных шлюзов, уведомлений, публикаций событий. Приложение вызывает secondary ports; driven adapters реализуют их. Приложение владеет обеими: определяет, что предлагает, и что требует. Адаптеры с обеих сторон соответствуют определениям приложения." },
  ]}
/>

<Recap lang="ru">
Hexagonal architecture решает проблему отсутствия чёткой границы приложения — когда каждая точка входа содержит собственную копию бизнес-логики и одно исправление бага нужно воспроизвести в нескольких кодовых базах.

Порт — это интерфейс, принадлежащий приложению. Он говорит на языке domain и определяет либо то, что предлагает приложение (primary port), либо то, что ему требуется от infrastructure (secondary port). Domain пишет контракт; внешний мир должен ему соответствовать.

Адаптер — слой трансляции между внешней технологией и портом. Driving adapters (HTTP-контроллеры, CLI-раннеры, batch-задания, тест-драйверы) вызывают primary ports для активации приложения. Driven adapters (PostgreSQL-репозитории, Stripe-клиенты, SendGrid-отправители) реализуют secondary ports для удовлетворения инфраструктурных потребностей domain.

Симметрия — HTTP и PostgreSQL оба просто адаптеры на разных сторонах — это практическая выгода. Заменить HTTP-адаптер на gRPC — application core не меняется. Заменить PostgreSQL на document store — application core не меняется. Добавить новую точку входа — логика подтверждения не дублируется. Application core — замкнутая область; порты — её единственные легальные точки входа и выхода; адаптеры — это соответствующие разъёмы, предоставляемые внешним миром.

Эта архитектура — структурный ответ на утечки слоистости из unit 03. Transaction-script trap предотвращается, потому что порт обязывает к определённой границе use case. Layer-skipping предотвращается, потому что infrastructure может достичь core только через реализации портов — infrastructure зависит от интерфейсов domain, а не наоборот. Fat service предотвращается, потому что каждый primary port соответствует ограниченному use case, а не открытому списку методов.
</Recap>
