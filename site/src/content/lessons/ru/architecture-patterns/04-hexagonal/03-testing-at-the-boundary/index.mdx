---
slug: "03-testing-at-the-boundary"
lang: ru
track: "architecture-patterns"
unit: "04-hexagonal"
order: 3
title: "Тестирование на границе"
summary: "Hexagonal architecture делает domain тривиально тестируемым: вызови через primary port с тест-драйвером, подставь in-memory fake на каждый secondary port — и весь domain работает без БД, HTTP или платёжного SDK. Это и есть пирамида тестов в hexagonal-командах."
estMin: 24
status: ready
lessonType: topic
level: middle
concepts:
  - test-driver
  - in-memory-fake
  - port-substitution
  - test-pyramid
  - adapter-test
  - domain-test
prereqs: []
sources:
  - https://alistair.cockburn.us/hexagonal-architecture/
  - https://martinfowler.com/bliki/TestPyramid.html
  - https://martinfowler.com/articles/practical-test-pyramid.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
До hexagonal architecture тестовый набор `OrderService` B2B-платформы страдал проблемой, с которой команда научилась мириться: каждый тест, касающийся подтверждения заказа, требовал работающего PostgreSQL-инстанса, активного тестового аккаунта Stripe и настроенного SendGrid-sandbox. CI-пайплайн поднимал Docker Compose-стек при каждом пуше. Тесты выполнялись четыре минуты. Нестабильность была постоянной: sandbox Stripe периодически возвращал ошибки rate-limit; контейнер с базой данных иногда не успевал инициализироваться; у sandbox SendGrid был суточный лимит писем, который тестовый набор достигал к середине дня. Инженеры запускали полный набор локально примерно раз в неделю. Покрытие номинально составляло 72%, но большая его часть была интеграционной поверхностью, а не логической. Когда изменилась валидация суммы заказа — чисто доменное правило без инфраструктурной зависимости — для его тестирования всё равно требовалась полная интеграционная среда, потому что логика валидации находилась внутри `OrderService`, который импортировал `PostgresOrderRepository`, `StripeClient` и `SendGridClient`. Нетестируемость была не проблемой тестирования. Это была архитектурная проблема: логика domain была запутана с инфраструктурными зависимостями. Hexagonal architecture решает это структурно — не делая мокирование проще, а делая инфраструктурные зависимости структурно отсутствующими в domain.
</Hook>

<Crux>Hexagonal делает domain тестируемым без инфраструктуры: подставь fake на каждый secondary port — и весь use case выполняется в памяти.</Crux>

<Explanation>

## Почему старая архитектура была нетестируемой

Нетестируемость из вступительной истории была структурной. `OrderService` имел compile-time зависимости от `PostgresOrderRepository`, `StripeClient` и `SendGridClient`. Чтобы создать `OrderService` в тесте, нужно было либо предоставить реальную инфраструктуру, либо замокировать эти классы с помощью мок-фреймворка. Оба варианта несут издержки:

**Реальная инфраструктура в тестах** требует настройки окружения (Docker Compose, внешние аккаунты, доступ в сеть), медленная (секунды на тест, минуты на набор) и нестабильная (внешние сервисы падают, контейнеры таймаутят, rate limits срабатывают).

**Мок-фреймворки** требуют от теста знания внутренних деталей реализации `OrderService` — какие методы он вызывает у каких зависимостей, в каком порядке, с какими аргументами. Когда `OrderService` рефакторится (например, извлекается доменный метод), моки ломаются, даже если поведение не изменилось. Тесты становятся хрупким бременем сопровождения вместо надёжных страховочных сеток.

Обе проблемы разделяют одну первопричину: `OrderService` имеет прямые source-code зависимости от инфраструктурных классов. Тест не может изолировать доменную логику от этих зависимостей без предоставления их или замены структурно идентичными копиями (моками).

Hexagonal architecture устраняет первопричину. Domain не имеет source-code зависимостей от инфраструктурных классов — он зависит только от собственных интерфейсов портов. Случайно создать экземпляр `PostgresOrderRepository` внутри domain невозможно, потому что domain не импортирует его. Единственный способ удовлетворить secondary port — предоставить реализацию, которой может быть быстрый in-memory fake.

## Вызов через primary port с тест-драйвером

Тест-драйвер является driving adapter (урок 02). Он вызывает primary port напрямую, как это делает HTTP-контроллер. HTTP не задействован — ни сервер, ни парсинг запросов, ни форматирование ответов.

```typescript
// domain/ports/OrderConfirmationPort.ts
interface OrderConfirmationPort {
  confirmOrder(orderId: string, approverId: string): Promise<ConfirmationResult>;
}

// Тест: вызов через primary port
describe('Order confirmation use case', () => {
  let useCase: OrderConfirmationPort;
  let orderRepo: InMemoryOrderRepository;
  let paymentGateway: FakePaymentGateway;
  let emailNotifier: FakeEmailNotifier;

  beforeEach(() => {
    orderRepo = new InMemoryOrderRepository();
    paymentGateway = new FakePaymentGateway();
    emailNotifier = new FakeEmailNotifier();

    // Собираем use case с fake driven adapters
    useCase = new OrderConfirmationUseCase(orderRepo, paymentGateway, emailNotifier);
  });

  it('подтверждает pending-заказ и снимает оплату с клиента', async () => {
    // Arrange: помещаем pending-заказ в fake-репозиторий
    const order = Order.pending({ id: 'order-1', customerId: 'cust-42', amount: Money.of(500, 'USD') });
    await orderRepo.save(order);

    // Act: вызываем через primary port
    const result = await useCase.confirmOrder('order-1', 'approver-99');

    // Assert: доменный результат
    expect(result.success).toBe(true);
    expect(paymentGateway.chargesIssued).toHaveLength(1);
    expect(paymentGateway.chargesIssued[0].amount).toEqual(Money.of(500, 'USD'));
    expect(emailNotifier.sentMessages).toHaveLength(1);
  });

  it('отклоняет подтверждение при наличии просроченных счетов', async () => {
    const order = Order.pending({ id: 'order-2', customerId: 'cust-42', amount: Money.of(200, 'USD') });
    order.markInvoiceOverdue('inv-7');
    await orderRepo.save(order);

    const result = await useCase.confirmOrder('order-2', 'approver-99');

    expect(result.success).toBe(false);
    expect(result.reason).toBe('overdue-invoice');
    expect(paymentGateway.chargesIssued).toHaveLength(0);
  });
});
```

Никакого Docker Compose. Никакого Stripe-аккаунта. Никакого SendGrid-sandbox. Каждый тест выполняется за миллисекунды. Тестовый набор может содержать сотни тестов и завершаться менее чем за две секунды.

## In-memory fakes на secondary ports

Ключевой элемент — in-memory fake: простая, быстрая реализация secondary port, хранящая состояние в памяти. Это не mock (который записывает ожидания вызовов); это реальная реализация, просто использующая память вместо сети или диска.

```typescript
// Реализует secondary port: IOrderRepository
class InMemoryOrderRepository implements IOrderRepository {
  private store = new Map<string, Order>();

  async findById(id: string): Promise<Order | null> {
    return this.store.get(id) ?? null;
  }

  async save(order: Order): Promise<void> {
    this.store.set(order.id, order);
  }

  async findByCustomerId(customerId: string): Promise<Order[]> {
    return [...this.store.values()].filter(o => o.customerId === customerId);
  }
}

// Реализует secondary port: IPaymentGateway
class FakePaymentGateway implements IPaymentGateway {
  public chargesIssued: Array<{ customerId: string; amount: Money }> = [];
  public shouldFail = false;

  async charge(customerId: CustomerId, amount: Money): Promise<ChargeResult> {
    if (this.shouldFail) return ChargeResult.failure('card-declined');
    this.chargesIssued.push({ customerId: customerId.value, amount });
    return ChargeResult.success(`charge-${Date.now()}`);
  }

  async refund(chargeId: ChargeId, amount: Money): Promise<RefundResult> {
    return RefundResult.success();
  }
}
```

Fakes реализуют интерфейс secondary port в точности — тот же интерфейс, что реализуют production-адаптеры Postgres и Stripe. Domain use case не может отличить их во время компиляции. В runtime тест подставляет fake; production подключает реальный адаптер. Один и тот же use case выполняется в обоих контекстах без модификации.

<FlowDiagram
  label="Тест подставляет fake-адаптеры на secondary ports"
  nodes={[
    { id: "testdriver", label: "Test Driver", sub: "driving adapter — вызывает primary port" },
    { id: "pport", label: "OrderConfirmationPort", sub: "primary port" },
    { id: "usecase", label: "OrderConfirmationUseCase", sub: "application core" },
    { id: "sport-repo", label: "IOrderRepository", sub: "secondary port" },
    { id: "sport-pay", label: "IPaymentGateway", sub: "secondary port" },
    { id: "fake-repo", label: "InMemoryOrderRepository", sub: "fake driven adapter — в тесте" },
    { id: "fake-pay", label: "FakePaymentGateway", sub: "fake driven adapter — в тесте" },
  ]}
  edges={[
    { from: "testdriver", to: "pport", label: "вызывает (тест)" },
    { from: "pport", to: "usecase", label: "реализует" },
    { from: "usecase", to: "sport-repo", label: "вызывает (runtime)" },
    { from: "usecase", to: "sport-pay", label: "вызывает (runtime)" },
    { from: "fake-repo", to: "sport-repo", label: "реализует (тестовая подстановка)" },
    { from: "fake-pay", to: "sport-pay", label: "реализует (тестовая подстановка)" },
  ]}
  caption="В domain-тесте тест-драйвер заменяет HTTP-контроллер как driving adapter. Fake in-memory реализации заменяют production Postgres и Stripe driven adapters. Application core (OrderConfirmationUseCase) выполняется идентично — он видит только интерфейсы портов, которыми владеет. Инфраструктура не нужна."
/>

<Inset kind="why" lang="ru">
Почему это структурное свойство, а не техника тестирования? Потому что тестируемость возникает из структуры зависимостей, а не из дисциплины настройки тестов. Команда, использующая слоистую архитектуру, тоже может писать in-memory fakes — но ей нужно мокировать конкретные классы, которые импортирует её сервис, что означает зависимость настройки теста от деталей реализации. При рефакторинге сервиса моки ломаются. В hexagonal architecture domain импортирует только собственные интерфейсы. In-memory fake — это легитимная реализация этого интерфейса, а не мок конкретного класса. Рефакторинг use case не ломает fakes (fakes реализуют порт, который меняется только при изменении требований domain). Тестируемость встроена по построению.
</Inset>

## Пирамида тестов, которую используют hexagonal-команды

Структурное разделение domain от адаптеров создаёт естественную трёхуровневую пирамиду тестов:

**Уровень 1 — Domain-тесты (много, миллисекунды):** вызов через primary port с тест-драйвером и fake driven adapters. Тестирует каждое бизнес-правило, каждый инвариант, каждый граничный случай в domain. Эти тесты не имеют инфраструктурных зависимостей — они выполняются везде, мгновенно. Это основная часть тестового набора: каждая комбинация состояния заказа, логики согласования, правила биллинга и условия ошибки. На B2B-платформе это сотни тестов, покрывающих полную логику подтверждения, выставления счетов и согласования биллинга.

**Уровень 2 — Тесты адаптеров (меньше, секунды):** тестирование каждого адаптера изолированно против контракта его порта. Тест PostgreSQL-репозитория запускает реальную базу данных, сохраняет и извлекает доменные объекты, проверяет правильность маппинга. Тест Stripe-адаптера (против тестового API Stripe) проверяет маппинг charge/refund. Тест HTTP-контроллера проверяет, что поля HTTP-запроса корректно парсятся в параметры порта и результаты порта корректно сериализуются в HTTP-ответы. Эти тесты проверяют, что адаптер корректно реализует порт — они не пере-тестируют доменную логику.

**Уровень 3 — End-to-end тесты (очень мало, секунды-минуты):** запуск полного приложения со всеми реальными адаптерами. Отправка реальных HTTP-запросов. Проверка интеграции всего стека. Эти тесты дороги — оставляй их только для критических happy path.

```
Уровень 1: Domain-тесты    ████████████████████████████████████  (сотни)
Уровень 2: Тесты адаптеров ██████████                            (десятки)
Уровень 3: E2E-тесты       ██                                    (единицы)
```

Исходная проблема B2B-платформы — 72% номинального покрытия через медленные интеграционные тесты — была симптомом полного отсутствия тестов Уровня 1. Всё было Уровнем 3. Hexagonal architecture инвертирует это: тесты Уровня 1 дёшевы в написании, потому что domain полностью доступен через primary port без инфраструктуры.

<Inset kind="note" lang="ru">
Существует нюансированный антипаттерн «ice cream cone»: много E2E-тестов, мало тестов адаптеров, почти никаких domain-тестов. Команды в слоистых архитектурах естественно дрейфуют к нему, потому что unit-тестирование domain требует мокирования многих конкретных инфраструктурных зависимостей — что утомительно и хрупко. Hexagonal architecture делает domain-тесты дёшевыми в написании (никакого мок-фреймворка, просто fakes, реализующие порты), поэтому пирамида инвертируется естественно. Если тестовый набор hexagonal-команды всё ещё выглядит как «ice cream cone», это обычно означает, что secondary ports не были корректно инвертированы — domain где-то всё ещё импортирует конкретные инфраструктурные классы.
</Inset>

## Тесты адаптеров: тестирование границы снаружи

Тесты адаптеров структурно независимы от domain-тестов. Они тестируют другой вопрос: «Правильно ли этот адаптер реализует контракт порта?»

```typescript
// Тест адаптера: PostgresOrderRepository реализует IOrderRepository
describe('PostgresOrderRepository', () => {
  let db: Pool; // Реальное PostgreSQL-соединение
  let repo: PostgresOrderRepository;

  beforeAll(async () => {
    db = await createTestDatabase();
    repo = new PostgresOrderRepository(db);
  });

  it('сохраняет и извлекает заказ по id', async () => {
    const order = Order.pending({ id: 'test-1', customerId: 'c-1', amount: Money.of(100, 'USD') });
    await repo.save(order);
    const retrieved = await repo.findById('test-1');
    expect(retrieved).toEqual(order);
  });

  it('возвращает null для несуществующего заказа', async () => {
    const result = await repo.findById('no-such-id');
    expect(result).toBeNull();
  });
});
```

Эти тесты запускают реальную базу данных. Они медленнее domain-тестов. Но покрывают одно, чего domain-тесты не могут: корректный маппинг между доменными объектами и строками базы данных (имена столбцов, типы, обработка NULL, сериализация). Domain-тест с fake-репозиторием никогда не обнаружит баг маппинга столбца. Тест адаптера поймает его точно, не тестируя при этом доменную логику.

<Quiz
  id="arch-04-03-quiz-1"
  lessonSlug="03-testing-at-the-boundary"
  lang="ru"
  question="Команда конвертирует слоистую архитектуру в hexagonal. OrderConfirmationUseCase теперь получает IOrderRepository и IPaymentGateway через конструктор (подключаются при запуске). Разработчик говорит: «У нас уже был DI раньше — мы могли мокировать OrderRepository и StripeClient в тестах. Что нам даёт hexagonal, чего у нас не было?» Как ответить?"
  choices={[
    { label: "Раньше: моки были конкретных классов (PostgresOrderRepository, StripeClient), принадлежавших infrastructure. Когда те классы менялись внутри — новый метод, рефакторинг структуры — моки ломались, даже если доменная логика не изменилась. Fakes должны были отражать внутренние детали реализации. После hexagonal: fakes реализуют интерфейсы, принадлежащие domain (IOrderRepository, IPaymentGateway). Domain определяет эти интерфейсы; они меняются только при изменении требований domain. Infrastructure может быть полностью переписана без затрагивания fakes или domain-тестов. Compile-time граница теперь находится на собственных интерфейсах domain, а не на формах конкретных классов infrastructure.", correct: true },
    { label: "Hexagonal ничего нового не даёт — DI с интерфейсами всегда был достаточен для тестируемости.", misconception: "DI с интерфейсами — предпосылка тестируемости, но ключевой вопрос — кто владеет интерфейсами. Если IOrderRepository был определён в пакете infrastructure до hexagonal, команда infrastructure его контролировала. Моки/fakes, написанные под него, зависели от инфраструктурных решений. После hexagonal IOrderRepository находится в пакете domain — команда domain контролирует его. Это инверсия владения из unit 02, применённая к тестовой инфраструктуре." },
    { label: "Hexagonal даёт in-memory fakes вместо mock-объектов, которые быстрее.", misconception: "Прирост производительности реален, но вторичен. Основное преимущество — владение интерфейсом. In-memory fakes, реализующие интерфейсы, принадлежащие infrastructure, всё равно ломаются, когда infrastructure реструктурирует эти интерфейсы. Структурное изменение, которое вносит hexagonal, — перенос владения интерфейсом в domain, так что ни fakes, ни production-код никогда не нужно отслеживать внутренние изменения infrastructure." },
    { label: "Hexagonal позволяет запускать тесты без DI-контейнера, что было невозможно раньше.", misconception: "DI-контейнеры не требуются hexagonal architecture — use case можно собрать вручную в тесте (new OrderConfirmationUseCase(fakeRepo, fakePayment, fakeEmail)). Но это справедливо и для любой DI-слоистой архитектуры. Разница, которую вносит hexagonal, не в DI-контейнере, а во владении интерфейсом и структурной принудимости этого владения." },
  ]}
/>

<Quiz
  id="arch-04-03-quiz-2"
  lessonSlug="03-testing-at-the-boundary"
  lang="ru"
  question="Команда пишет domain-тест с InMemoryOrderRepository и FakePaymentGateway. Тест подтверждения заказа проходит. Позже Postgres-адаптер деплоится в production и заказ падает: столбец amount хранит значения как целые числа (центы), но domain использует Money value object, хранящийся как десятичная строка. Какой уровень тестов обнаружил бы этот баг, и почему domain-тест его не поймал?"
  choices={[
    { label: "Тест адаптера (Уровень 2) его обнаруживает. Domain-тест не поймал его, потому что fake-репозиторий хранит доменный Money object напрямую в памяти — сериализация не происходит. Баг в маппинге между доменными типами и типами столбцов базы данных — это логика адаптера, а не доменная логика. Тесты адаптеров запускают реальный Postgres-адаптер против реальной базы данных, проходя путь сериализации/десериализации, который domain-тест обходит.", correct: true },
    { label: "E2E-тест (Уровень 3) его обнаруживает. Тесты адаптеров тестируют адаптер изолированно и не могут обнаружить баги маппинга типов.", misconception: "Тесты адаптеров специально тестируют, корректно ли адаптер отображает доменные объекты в/из формата хранения. Тест адаптера PostgresOrderRepository, сохраняющий Order с Money value и затем извлекающий его, немедленно поймает несоответствие integer vs decimal. E2E-тесты тоже могут его поймать, но тесты адаптеров нацелены именно на этот тип сбоя и более быстрые и лёгкие в диагностике." },
    { label: "Domain-тест должен был его поймать — fake-репозиторий должен сериализовывать в/из формата базы данных для имитации production.", misconception: "Если fake-репозиторий имитирует сериализацию базы данных, он становится частичной переимплементацией Postgres-адаптера — что лишает смысла быстрый, простой fake. Fake существует для тестирования доменной логики изолированно; он хранит доменные объекты напрямую. Логика сериализации — забота адаптера и принадлежит тесту адаптера с реальной базой данных. Смешивание имитации сериализации в fakes создаёт бремя сопровождения и размывает границу ответственности." },
    { label: "Ни один тест это не поймает — это несоответствие типов, проявляющееся только в runtime и не тестируемое статически.", misconception: "Это несоответствие типов — именно то, для обнаружения чего предназначены тесты адаптеров. Тест адаптера PostgresOrderRepository, вставляющий заказ (с Money value object) и извлекающий его, упадёт на шаге сравнения, если round-trip сериализация повреждает amount. Это полностью тестируемо — просто требует тестирования адаптера с реальной базой данных, а не fake." },
  ]}
/>

<Quiz
  id="arch-04-03-quiz-3"
  lessonSlug="03-testing-at-the-boundary"
  lang="ru"
  question="У команды 400 domain-тестов (Уровень 1), 30 тестов адаптеров (Уровень 2) и 5 E2E-тестов (Уровень 3). CI занимает 45 секунд. Новый разработчик говорит: «Нужно добавить больше E2E-тестов для уверенности — они тестируют реальную систему». Как оценить этот аргумент?"
  choices={[
    { label: "Аргумент смешивает «тестирует реальную систему» с «большей уверенностью». E2E-тесты проверяют проводку интеграции — что адаптеры корректно соединяются end-to-end. 400 domain-тестов уже исчерпывающе проверяют все бизнес-правила с точной диагностикой сбоев. Добавление E2E-тестов дублирует покрытие доменной логики при значительно большей стоимости (медленно, нестабильно, сложно диагностировать). Правильный ответ: определить, что не покрывают 5 E2E-тестов (конкретный интеграционный путь, не охваченный тестами адаптеров), добавить целевые тесты адаптеров и сохранить минимальный набор E2E-тестов, доказывающих корректность проводки.", correct: true },
    { label: "Разработчик прав — только E2E-тесты обеспечивают реальную уверенность, поскольку тестируют фактический деплоимый стек.", misconception: "E2E-тесты тестируют проводку интеграции: правильно ли HTTP-контроллер соединён с use case, который правильно соединён с Postgres-адаптером? Domain-тесты тестируют бизнес-логику: правильно ли реализовано правило подтверждения? Оба необходимы, но ни один не заменяет другой. E2E-тест, прошедший успешно, не говорит, какое бизнес-правило выполнено корректно. Падение domain-теста указывает именно на то, какое правило не работает. Уверенность в бизнес-логике идёт от domain-тестов; уверенность в проводке — от E2E и тестов адаптеров." },
    { label: "Больше E2E-тестов нужно, если domain-тесты используют fakes — fakes не реальны, поэтому domain-тестам нельзя доверять.", misconception: "Fakes — реальные реализации интерфейсов secondary ports. Они реализуют тот же интерфейс, что Postgres и Stripe — domain не может их различить. Падение domain-теста означает, что бизнес-правило в use case не работает, независимо от того, какой адаптер использовался. Надёжность domain-тестов обеспечивается контрактом порта: если fake корректно реализует интерфейс порта и use case проходит с fake, он также пройдёт с любым корректным Postgres или Stripe адаптером. Тесты адаптеров проверяют, что production-адаптеры корректно реализуют порт." },
    { label: "45 секунд CI — свидетельство того, что нужно больше тестов: быстрый CI означает недостаточное покрытие.", misconception: "45 секунд при 400 domain-тестах — хороший результат: domain-тесты выполняются быстро. Добавление E2E-тестов значительно увеличит время CI (каждый E2E-тест может добавить 5-30 секунд). Более быстрый CI — особенность hexagonal пирамиды, а не признак недостаточного покрытия. Правильная метрика: покрывают ли domain-тесты каждое бизнес-правило, и покрывают ли тесты адаптеров каждую реализацию порта? Не: достаточно ли медленен CI?" },
  ]}
/>

</Explanation>

<KeyTakeaway>Hexagonal architecture делает domain-тесты структурно дешёвыми: domain импортирует только собственные интерфейсы портов, поэтому любой in-memory fake, реализующий эти интерфейсы, идеально заменяет production-инфраструктуру. Пирамида тестов — много быстрых domain-тестов, меньше тестов адаптеров, очень мало E2E — это структурный результат владения портом, а не тестовое соглашение, наложенное поверх архитектуры.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-04-03-retrieval"
  lang="ru"
  questions={[
    { q: "Какова структурная причина того, что domain-тесты в hexagonal architecture не требуют мок-фреймворка?", a: "В слоистой архитектуре тесты domain-сервиса требуют мокирования, потому что сервис имеет compile-time зависимости от конкретных инфраструктурных классов (PostgresOrderRepository, StripeClient). Моки должны отражать интерфейс конкретного класса — имена методов, типы возвращаемых значений, ожидания вызовов. Когда эти классы изменяются, моки ломаются. В hexagonal architecture use case имеет compile-time зависимости только от интерфейсов secondary ports, которыми он владеет (IOrderRepository, IPaymentGateway). In-memory fake реализует эти интерфейсы законно — это реальная реализация, а не мок конкретного класса. Никакой рефлексии, никакого мок-фреймворка, никакой записи ожидаемых вызовов. Fake компилируется против того же интерфейса, что и production-адаптер. Рефакторинг Postgres-адаптера не ломает fake, потому что fake реализует определение интерфейса domain, а не интерфейс класса Postgres." },
    { q: "В чём разница между in-memory fake и mock-объектом, и почему это различие важно для сопровождения тестов?", a: "Mock-объект обычно генерируется мок-фреймворком и конфигурируется на каждый тест для ожидания конкретных последовательностей вызовов. Он проверяет, что тестируемая система вызывала методы мока в конкретном порядке с конкретными аргументами. Это связывает тест с деталями реализации тестируемой системы — при рефакторинге use case (извлечение доменного метода, переупорядочивание операций) ожидания мока ломаются, даже если наблюдаемое поведение не изменилось. In-memory fake — полная реализация интерфейса secondary port, хранящая состояние в памяти. Ему всё равно, какие методы вызывались в каком порядке — он ведёт себя корректно для любой допустимой последовательности вызовов порта. При рефакторинге use case fake не ломается (пока интерфейс порта и контракт неизменны). Различие важно для сопровождения тестов: тестовые наборы с большим количеством моков становятся бременем при рефакторинге; тестовые наборы на fakes остаются стабильными." },
    { q: "У команды B2B-платформы 400 domain-тестов с fakes. Обнаружен production-баг: заказы на суммы свыше $10 000 в определённых валютах молча не проходят списание. FakePaymentGateway этого не выявил. Что это говорит о fake и как команде реагировать?", a: "Это говорит о том, что FakePaymentGateway не корректно симулирует поведение реального платёжного шлюза для дорогостоящих мультивалютных списаний — конкретно, он не возвращает ошибку там, где реальный шлюз её возвращает (или молча успешен). Это пробел в fake, а не недостаток hexagonal-структуры. Команде следует: (1) добавить тест-кейс в набор тестов Stripe-адаптера (Уровень 2), проверяющий конкретную комбинацию валюты/суммы, вызывающую сбой, против тестового API Stripe; (2) обновить FakePaymentGateway для репликации того же поведения (вернуть ошибку для этого сценария), чтобы domain-тестовый набор предотвращал регрессии; (3) проверить, корректно ли domain use case обрабатывает ответ ChargeResult.failure — это может выявить пропущенную ветвь обработки ошибок в доменной логике, которую нужно покрыть domain-тестом. Исправление — обогатить поведение fake и покрытие тестами адаптеров, а не отказываться от пирамидальной структуры." },
  ]}
/>

<Recap lang="ru">
Нетестируемость исходной B2B-платформы была не проблемой написания тестов. Это была структурная проблема: сервисный слой имел compile-time зависимости от инфраструктурных классов, поэтому каждый тест бизнес-логики требовал полного инфраструктурного стека.

Hexagonal architecture устраняет compile-time зависимости. Domain импортирует только собственные интерфейсы портов. Secondary ports удовлетворяются в runtime тем адаптером, который подставлен — production или fake. Domain не может случайно импортировать конкретный инфраструктурный класс.

In-memory fakes реализуют интерфейсы secondary ports полностью и корректно, без использования какой-либо внешней инфраструктуры. Domain use case выполняется идентично против fakes и production-адаптеров, потому что видит только интерфейсы портов, которыми владеет.

Пирамида тестов, которую это создаёт, имеет три уровня. Domain-тесты (много, миллисекунды) вызывают через primary port с fake driven adapters — они тестируют каждое бизнес-правило без инфраструктуры. Тесты адаптеров (меньше, секунды) тестируют каждый адаптер с реальной инфраструктурой — они проверяют маппинг между доменными объектами и строками БД, API-ответами или форматами сообщений. E2E-тесты (очень мало) проверяют проводку интеграции.

Антипаттерн «ice cream cone» — много E2E, почти никаких domain-тестов — симптом недостаточного владения портами. Когда domain-тесты становятся дёшевыми по построению, пирамида инвертируется естественно.

Эта структурная тестируемость — практическая выгода, делающая hexagonal architecture стоящей своей стоимости. Границы, предотвращающие layer-skipping (проблема unit 03) и владение портом, инвертирующее инфраструктурную зависимость (DIP из unit 02), также делают весь domain тестируемым без базы данных, без платёжного SDK и без работающего сервера.
</Recap>
