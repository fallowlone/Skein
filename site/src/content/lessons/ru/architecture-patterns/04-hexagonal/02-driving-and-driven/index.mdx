---
slug: "02-driving-and-driven"
lang: ru
track: "architecture-patterns"
unit: "04-hexagonal"
order: 2
title: "Driving и driven стороны"
summary: "У гексагона две стороны: driving (левая) — акторы инициируют через HTTP, CLI, тесты; driven (правая) — приложение обращается наружу к БД, оплате, почте. Обе стороны говорят через порты, принадлежащие domain. Domain изолирован в центре."
estMin: 22
status: ready
lessonType: topic
level: middle
concepts:
  - driving-adapter
  - driven-adapter
  - primary-port
  - secondary-port
  - hexagon-sides
  - actor
prereqs: []
sources:
  - https://alistair.cockburn.us/hexagonal-architecture/
  - https://martinfowler.com/bliki/HexagonalArchitecture.html
  - https://www.informit.com/store/clean-architecture-a-craftsmans-guide-to-software-structure-9780134494166
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import drivingVsDriven from "./infographics/driving-vs-driven.svg?raw";

<Hook>
На B2B-платформе заказов была рабочая hexagonal-структура для HTTP-канала: контроллер вызывал порт подтверждения, порт принадлежал domain, PostgreSQL-адаптер реализовывал порт репозитория с другой стороны. Затем команда добавила Stripe для обработки платежей. В первой реализации вызов Stripe SDK был помещён прямо внутри use case подтверждения заказа: `await stripe.paymentIntents.create(...)`. Три месяца спустя переход на другой платёжный процессор для европейских клиентов потребовал изменения самого use case. Команда перенесла вызов Stripe в класс сервиса, но тот класс сервиса импортировал `stripe` на верхнем уровне — и domain-модуль приобрёл транзитивную зависимость от npm-пакета Stripe. Тест, импортирующий domain для проверки логики подтверждения заказа, подтягивал Stripe и требовал реального API-ключа или ручного мока. Команда правильно определила, что Stripe — внешняя забота. Но она неправильно расставила границы: domain всё ещё тянулся к infrastructure. Driven-сторона — правая сторона гексагона — не была инвертирована. Понять, на какой стороне находится платёжный шлюз и почему — это структурный урок данного раздела.
</Hook>

<Crux>Driving-адаптеры вызывают primary ports; driven-адаптеры отвечают на secondary ports приложения — domain изолирован в центре.</Crux>

<Explanation>

## Две стороны гексагона

Оригинальное описание Cockburn размещало адаптеры на двух различных сторонах, и асимметрия имеет значение:

**Driving-сторона (левая / primary)** — акторы, инициирующие взаимодействие с приложением. Driving-адаптер вызывает primary port, чтобы заставить приложение что-то сделать. HTTP-контроллер получает POST и вызывает `confirmOrder`. CLI получает команду и вызывает `confirmOrder`. Автоматизированный тест вызывает `confirmOrder`. Каждый driving-адаптер переводит собственный формат входных данных в вызов метода порта.

**Driven-сторона (правая / secondary)** — инфраструктура, которую приложение активирует для выполнения своей работы. Driven-адаптер реализует secondary port. Приложение вызывает `IOrderRepository.save(order)` — Postgres-адаптер отвечает. Приложение вызывает `IPaymentGateway.charge(amount)` — Stripe-адаптер отвечает. Приложение вызывает `IEmailNotifier.send(message)` — SendGrid-адаптер отвечает.

Критическая асимметрия: **driving-сторона вызывает приложение; driven-сторона вызывается приложением.** Обе стороны опосредованы портами. Но направление инициирования противоположно.

<Infographic
  svg={drivingVsDriven}
  label="Сравнение двух сторон гексагона. Driving-сторона (слева): инициирует актор, точки входа — HTTP/CLI/Kafka/тест, адаптер вызывает primary port и переводит формат входа в вызов порта. Driven-сторона (справа): инициирует приложение, внешние зависимости — Postgres/Stripe/SendGrid/S3, адаптер реализует secondary port и переводит вызов порта в вызов SDK или SQL. С обеих сторон source-code зависимость указывает внутрь, к domain."
  caption="Две стороны зеркальны: driving-адаптеры вызывают приложение, driven-адаптеры вызываются им — но обе зависят внутрь от портов, которыми владеет domain."
  hue="var(--d-infra)"
/>

## Domain в центре

Domain находится в центре гексагона, окружённый с обеих сторон портами, которыми он владеет. Это структурное следствие применения Dependency Inversion Principle (unit 02) на архитектурном уровне: высокоуровневая политика (domain) владеет абстракциями; низкоуровневые механизмы (адаптеры) зависят от этих абстракций, чтобы подключиться.

На B2B-платформе domain определяет:

```typescript
// Primary ports (что domain предлагает)
interface OrderConfirmationPort {
  confirmOrder(orderId: string, approverId: string): Promise<ConfirmationResult>;
}
interface InvoiceGenerationPort {
  generateInvoice(orderId: string): Promise<InvoiceId>;
}

// Secondary ports (что domain требует)
interface IOrderRepository {
  findById(id: string): Promise<Order | null>;
  save(order: Order): Promise<void>;
}
interface IPaymentGateway {
  charge(customerId: CustomerId, amount: Money): Promise<ChargeResult>;
}
interface IEmailNotifier {
  send(recipient: Email, subject: string, body: string): Promise<void>;
}
```

Все эти интерфейсы живут в пакете domain. Ни один из них не импортирует HTTP, Stripe, Postgres или SendGrid. Domain не имеет никаких знаний о технологических выборах внешнего мира. Он знает только, что ему нужно (secondary ports) и что он предоставляет (primary ports).

<FlowDiagram
  label="Полный гексагон: driving-сторона слева, domain в центре, driven-сторона справа"
  nodes={[
    { id: "http", label: "HTTP Controller", sub: "driving adapter" },
    { id: "cli", label: "CLI Runner", sub: "driving adapter" },
    { id: "test", label: "Test Driver", sub: "driving adapter" },
    { id: "pport", label: "OrderConfirmationPort", sub: "primary port — source dep внутрь" },
    { id: "domain", label: "Domain + Use Cases", sub: "ядро гексагона" },
    { id: "sport", label: "IPaymentGateway / IOrderRepository", sub: "secondary ports — source dep внутрь" },
    { id: "stripe", label: "StripePaymentAdapter", sub: "driven adapter" },
    { id: "pg", label: "PostgresOrderRepository", sub: "driven adapter" },
  ]}
  edges={[
    { from: "http", to: "pport", label: "вызывает (runtime)" },
    { from: "cli", to: "pport", label: "вызывает (runtime)" },
    { from: "test", to: "pport", label: "вызывает (runtime)" },
    { from: "pport", to: "domain", label: "source dep внутрь" },
    { from: "domain", to: "sport", label: "вызывает (runtime)" },
    { from: "stripe", to: "sport", label: "реализует (source dep внутрь)" },
    { from: "pg", to: "sport", label: "реализует (source dep внутрь)" },
  ]}
  caption="Все стрелки source-code зависимостей указывают к domain. Driving adapters зависят от интерфейсов primary ports (принадлежат domain). Driven adapters зависят от интерфейсов secondary ports (тоже принадлежат domain). Domain ни от чего вне себя не зависит. Runtime-поток идёт слева направо; стрелки source-code зависимостей все указывают внутрь."
/>

## HTTP, CLI и тест-драйвер — все вызывают один и тот же primary port

Одно из наиболее ценных свойств driving-стороны: она нормализует все точки входа к одному и тому же интерфейсу порта. Use case подтверждения определён один раз на primary port. Любая технология, способная сформировать необходимые доменные параметры, может его вызвать.

```typescript
// HTTP driving adapter
class HttpOrderController {
  constructor(private port: OrderConfirmationPort) {}

  async post(req: Request): Promise<Response> {
    const result = await this.port.confirmOrder(
      req.params.orderId,
      req.body.approverId
    );
    return result.success
      ? Response.json({ status: 'confirmed' }, { status: 200 })
      : Response.json({ error: result.reason }, { status: 422 });
  }
}

// CLI driving adapter
class CliOrderConfirmCommand {
  constructor(private port: OrderConfirmationPort) {}

  async run(args: string[]): Promise<void> {
    const [orderId, approverId] = args;
    const result = await this.port.confirmOrder(orderId, approverId);
    console.log(result.success ? 'Confirmed' : `Failed: ${result.reason}`);
  }
}

// Test driving adapter — фреймворк не нужен, только порт
async function confirmOrderViaPort(
  port: OrderConfirmationPort,
  orderId: string,
  approverId: string
): Promise<ConfirmationResult> {
  return port.confirmOrder(orderId, approverId);
}
```

Логика подтверждения выполняется идентично независимо от того, является ли вызывающим HTTP-запрос, CLI-команда или тест. Тест-драйвер максимально прост: прямой вызов. Никакого HTTP-клиента, номера порта или запуска сервера. Это доступно именно потому, что primary port принадлежит domain и говорит на языке domain.

<Inset kind="why" lang="ru">
Почему driving-сторона важна структурно, а не только как «несколько точек входа»? Потому что primary port — это формальное определение того, что умеет делать приложение — его публичный контракт. Всё, что предлагает приложение, должно быть выразимо через метод primary port. Это имеет два следствия: (1) предотвращает размещение логики только в адаптерах (HTTP-only функция без метода порта архитектурно невидима); (2) делает возможности приложения перечислимыми — можно посмотреть на определения primary port и увидеть каждый use case, не читая код адаптеров. Вот почему тесты, написанные против primary port, являются наиболее значимыми регрессионными тестами: они тестируют фактический контракт приложения, а не интерпретацию адаптером этого контракта.
</Inset>

## Driven-сторона: secondary ports инвертируют зависимость от infrastructure

Driven-сторона — это место, где DIP из unit 02 становится конкретным. Domain определяет, что ему нужно — `IPaymentGateway`, `IOrderRepository` — в собственном пакете, на языке domain. Stripe-адаптер реализует `IPaymentGateway`; он зависит от определения интерфейса domain. Postgres-адаптер реализует `IOrderRepository`; он зависит от определения интерфейса domain.

Направление source-code зависимости на driven-стороне то же, что и на driving-стороне: **всегда внутрь, к domain**.

```typescript
// Secondary port — определён в пакете domain
// domain/ports/IPaymentGateway.ts
interface IPaymentGateway {
  charge(customerId: CustomerId, amount: Money): Promise<ChargeResult>;
  refund(chargeId: ChargeId, amount: Money): Promise<RefundResult>;
}

// Driven adapter — определён в пакете infrastructure
// infra/payment/StripePaymentAdapter.ts
import { IPaymentGateway, ChargeResult, RefundResult } from '../../domain/ports/IPaymentGateway';
import Stripe from 'stripe';

class StripePaymentAdapter implements IPaymentGateway {
  private client = new Stripe(process.env.STRIPE_KEY!);

  async charge(customerId: CustomerId, amount: Money): Promise<ChargeResult> {
    const intent = await this.client.paymentIntents.create({
      amount: amount.inCents(),
      currency: amount.currency,
      customer: customerId.value,
    });
    return ChargeResult.fromStripeIntent(intent);
  }

  async refund(chargeId: ChargeId, amount: Money): Promise<RefundResult> {
    const refund = await this.client.refunds.create({
      payment_intent: chargeId.value,
      amount: amount.inCents(),
    });
    return RefundResult.fromStripeRefund(refund);
  }
}
```

`StripePaymentAdapter` импортирует из domain (`IPaymentGateway`). Domain не импортирует из `StripePaymentAdapter`. npm-пакет `stripe` — деталь адаптера: domain не имеет транзитивной зависимости от него. Это исправление истории из вступления: перемещение вызова Stripe за secondary port означает, что domain можно тестировать без Stripe, а смена процессора платежей сводится к написанию нового адаптера без изменения domain.

<Inset kind="note" lang="ru">
Терминология «driving/driven» напрямую соответствует «primary/secondary»: driving adapters вызывают primary ports, driven adapters реализуют secondary ports. Некоторые источники используют «левая/правая сторона», потому что в стандартной диаграмме driving adapters размещены слева, а driven adapters — справа. Все три пары (driving/driven, primary/secondary, левый/правый) описывают одно и то же структурное различие: какая сторона инициирует взаимодействие, а какая отвечает. Используйте ту терминологию, которую закрепит ваша команда — структурное отношение — это то, что важно.
</Inset>

<Quiz
  id="arch-04-02-quiz-1"
  lessonSlug="02-driving-and-driven"
  lang="ru"
  question="Платформа заказов добавляет Kafka-потребителя, читающего события OrderPlaced из топика и инициирующего генерацию счёта. На какой стороне гексагона находится Kafka-потребитель и почему?"
  choices={[
    { label: "Driving-сторона (левая/primary). Kafka-потребитель получает сообщение и вызывает primary port для активации приложения — он инициирует взаимодействие. Это driving adapter, в точности как HTTP-контроллер и CLI-раннер. Направление инициирования: сообщение приходит → потребитель вызывает primary port → приложение действует. Kafka-потребитель переводит формат сообщения в вызов порта — так же, как HTTP-контроллер переводит формат HTTP-запроса.", correct: true },
    { label: "Driven-сторона (правая/secondary). Kafka — это infrastructure, поэтому любой Kafka-компонент является driven adapter.", misconception: "Driven/secondary-сторона определяется направлением инициирования, а не тем, является ли технология «инфраструктурой». Kafka-потребитель — инициатор: он получает событие и вызывает приложение. Это делает его driving adapter. Kafka-продюсер, которому приложение публикует события, был бы driven adapter (приложение вызывает IEventPublisher port; Kafka-продюсер реализует его). Инфраструктурная технология присутствует на обеих сторонах гексагона; сторона определяется тем, кто кого вызывает." },
    { label: "Ни то ни другое — потребители очередей сообщений являются middleware и принадлежат к отдельной архитектурной категории вне гексагона.", misconception: "Каждый компонент, взаимодействующий с приложением, пересекает границу порта и является либо driving, либо driven adapter. Категории 'middleware' вне гексагона не существует. Kafka-потребитель, вызывающий приложение, — driving adapter. Считать его middleware с прямым доступом к внутренностям приложения — это именно layer-skipping, который hexagonal architecture предотвращает." },
    { label: "Это зависит от того, синхронен или асинхронен Kafka-потребитель.", misconception: "Синхронность не определяет сторону адаптера. Синхронный HTTP-запрос и асинхронное Kafka-сообщение оба могут быть driving adapters — оба инициируют через вызов primary port. Различие driven/driving касается направления инициирования: этот компонент вызывает приложение (driving) или приложение вызывает этот компонент (driven)?" },
  ]}
/>

<Quiz
  id="arch-04-02-quiz-2"
  lessonSlug="02-driving-and-driven"
  lang="ru"
  question="Разработчик предлагает: «Давайте определим IPaymentGateway в общем пакете infra/interfaces/, чтобы и domain, и Stripe-адаптер могли импортировать его, не завися друг от друга». Правильно ли это применяет DIP? Почему?"
  choices={[
    { label: "Нет. Интерфейс должен жить в пакете domain, а не в общем инфраструктурном пакете. Если IPaymentGateway находится в infra/interfaces/, domain импортирует из пакета, смежного с infrastructure — infrastructure всё ещё контролирует эволюцию интерфейса. DIP требует, чтобы потребитель (domain) владел абстракцией. Domain определяет, что ему нужно от платёжного шлюза, на языке domain; это определение должно жить там, где domain его контролирует.", correct: true },
    { label: "Да. Общий пакет интерфейсов — распространённый индустриальный паттерн для избежания циклических зависимостей — он правильно развязывает обе стороны.", misconception: "Общий пакет интерфейсов — распространённый паттерн, но его расположение имеет значение. Если общий пакет контролируется командой infrastructure (потому что находится под infra/), domain не владеет интерфейсом — infrastructure владеет им. Правильное общее расположение, если нужно избежать нахождения интерфейса внутри core domain-пакета, — выделенный boundary/ports-пакет, контролируемый командой domain, от которого зависят инфраструктурные адаптеры. Ключ: кто решает, когда интерфейс меняется? Это должен быть потребитель (domain), а не поставщик (infrastructure)." },
    { label: "Да, потому что ни одна сторона не импортирует другую — общий пакет достигает структурной инверсии.", misconception: "То, что ни одна сторона не импортирует другую — свойство организации реализации, а не корректности DIP. DIP касается владения: модуль высокоуровневой политики (domain) должен владеть абстракцией. Если общий пакет действительно принадлежит команде domain и эволюционирует на основе требований domain — это приемлемо. Если это компромиссный пакет, не принадлежащий никому (или принадлежащий infrastructure) — это иллюзия разделения: интерфейс может меняться из соображений удобства реализации, ломая domain-потребителей." },
    { label: "Это зависит от того, используют ли методы IPaymentGateway доменные типы или инфраструктурные в сигнатурах.", misconception: "Выбор типов методов важен для качества интерфейса, но не определяет корректность DIP. DIP касается владения и направления стрелки source-code зависимости. Интерфейс с идеально domain-типизированными методами, но принадлежащий команде infrastructure, всё равно будет ломать domain-потребителей при реструктуризации со стороны infrastructure." },
  ]}
/>

<Quiz
  id="arch-04-02-quiz-3"
  lessonSlug="02-driving-and-driven"
  lang="ru"
  question="Команда пишет интеграционный тест, запускающий реальный HTTP-сервер, отправляющий реальный HTTP-запрос и проверяющий HTTP-ответ. Используется реальная PostgreSQL база данных. Является ли это тестом приложения через primary port? Что он тестирует, чего не может тест уровня primary port?"
  choices={[
    { label: "Это не тест через primary port — он тестирует HTTP-адаптер и Postgres-адаптер, а не application core. Что он тестирует сверх того, чего не может primary-port-тест: что HTTP-контроллер правильно переводит формат запроса в параметры порта и формат ответа из результатов порта; что Postgres-адаптер правильно отображает доменные объекты на строки БД и обратно; что весь стек интегрируется без ошибок проводки. Это заботы адаптеров, а не domain. Оба уровня тестирования ценны для разных сбоев.", correct: true },
    { label: "Это primary-port-тест, потому что он тестирует use case подтверждения end-to-end — именно то, что определяет primary port.", misconception: "Primary-port-тест вызывает интерфейс порта напрямую — без HTTP, без сервера, без TCP-соединения. Интеграционный тест через HTTP тестирует корректность HTTP-адаптера (правильно ли он парсит поля? правильные ли возвращает коды статуса?) и корректность Postgres-адаптера, в дополнение к логике приложения. Логику приложения тестирует primary-port-тест; корректность HTTP и Postgres требует теста уровня адаптера." },
    { label: "Это лучший тест, чем primary-port-тест, потому что покрывает больше кодовых путей.", misconception: "Покрытие — не то же самое, что целевая верификация. Интеграционный тест медленнее, требует больше инфраструктуры и производит труднодиагностируемые сбои (проблема в парсинге HTTP, доменной логике или SQL-маппинге?). Primary-port-тест быстр, не требует инфраструктуры и точно изолирует сбои доменной логики. Пирамидальная структура: primary-port domain-тесты (много, быстрые) + интеграционные тесты адаптеров (меньше, медленнее) + полноценные E2E-тесты (совсем мало, самые медленные)." },
    { label: "Это primary-port-тест, потому что HTTP-эндпоинт — просто реализация primary port.", misconception: "HTTP-эндпоинт — не primary port, а адаптер, вызывающий primary port. Primary port — это интерфейс, принадлежащий domain (OrderConfirmationPort). Тестирование через HTTP-адаптер тестирует слой адаптера плюс domain. Тестирование напрямую через primary port тестирует только domain. Различие важно для диагностики сбоев: когда HTTP-интеграционный тест падает, нужно определить, в адаптерном коде сбой или в доменном. Primary-port-тест, упавший, всегда указывает на доменный код." },
  ]}
/>

</Explanation>

<KeyTakeaway>Driving adapters (HTTP, CLI, тесты) находятся слева и вызывают primary ports; driven adapters (база данных, оплата, почта) находятся справа и реализуют secondary ports. Все source-code зависимости указывают внутрь к domain, который владеет обеими сторонами портов. Тест-драйвер структурно идентичен HTTP-контроллеру — оба driving adapters, вызывающие один и тот же primary port.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="arch-04-02-retrieval"
  lang="ru"
  questions={[
    { q: "Что отличает driving adapter от driven adapter — и как это соотносится с направлением runtime-вызовов против направления source-code зависимостей?", a: "Driving adapter инициирует взаимодействие с приложением: вызывает метод primary port, чтобы приложение что-то сделало (HTTP-контроллер отправляет запрос → вызывает confirmOrder; CLI-раннер выполняется → вызывает confirmOrder; тест-драйвер → вызывает confirmOrder). Driven adapter отвечает, когда приложение его вызывает: приложение вызывает метод secondary port, и driven adapter реализует этот метод (приложение вызывает IOrderRepository.save → PostgresOrderRepository выполняет SQL; приложение вызывает IPaymentGateway.charge → StripePaymentAdapter обращается к Stripe API). Направление runtime-вызова: driving adapter → primary port → domain → secondary port → driven adapter. Слева направо. Направление source-code зависимости: все адаптеры зависят от интерфейсов портов, принадлежащих domain. И driving adapters (импортируют интерфейс primary port), и driven adapters (реализуют интерфейс secondary port) имеют source-code зависимости, указывающие внутрь. Runtime-поток идёт слева направо; source-code зависимости все указывают к центру." },
    { q: "Почему тест-драйвер структурно является driving adapter, и какую возможность тестирования это даёт, недоступную HTTP-уровневым тестам?", a: "Тест-драйвер вызывает primary port напрямую — это driving adapter, переводящий тестовые входные данные в вызовы методов порта и читающий результаты порта, в точности как HTTP-контроллер переводит HTTP-запросы и форматирует HTTP-ответы. Структурно это один и тот же тип компонента. Возможность, которую это даёт: можно тестировать весь domain приложения — каждый use case, каждое бизнес-правило, каждый инвариант — без запуска HTTP-сервера, без открытия сетевого сокета, без парсинга HTTP-заголовков или сериализации/десериализации JSON. Тесты выполняются за миллисекунды, а не секунды. Сбои всегда являются domain-сбоями — а не «отвечает ли HTTP-сервер?». Фейки можно подставлять на secondary ports (InMemoryOrderRepository вместо Postgres) без какого-либо мок-фреймворка или настройки окружения. Это быстрый набор domain-тестов, составляющий основание пирамиды тестов. HTTP-уровневые тесты нужны для верификации корректности адаптера (HTTP-парсинг, коды ответов) — но не должны быть основным средством тестирования доменной логики." },
    { q: "На B2B-платформе Stripe SDK изначально вызывался напрямую внутри use case подтверждения заказа. Какое структурное правило это нарушало, и как перемещение за secondary port исправляет это?", a: "Прямой вызов Stripe SDK внутри use case нарушает правило: domain владеет secondary ports — domain не должен импортировать infrastructure. Stripe SDK — infrastructure: конкретная деталь, меняющаяся независимо от domain. Его импорт внутри use case означает: (1) domain имеет compile-time зависимость от конкретного платёжного процессора; (2) тесты domain должны мокировать или предоставлять Stripe-клиент; (3) смена процессора требует изменения use case. Перемещение за IPaymentGateway (secondary port, принадлежащий и определённый domain) исправляет все три: (1) domain импортирует только собственный интерфейс, а не Stripe SDK; (2) тесты подставляют InMemoryPaymentGateway или FakePaymentGateway, реализующий IPaymentGateway — Stripe SDK не нужен; (3) смена процессора означает написание нового класса адаптера, реализующего IPaymentGateway — use case не затрагивается. Это DIP, применённый к driven-стороне: domain (высокоуровневая политика) определяет интерфейс; Stripe-адаптер (низкоуровневый механизм) зависит от определения domain." },
  ]}
/>

<Recap lang="ru">
У гексагона две стороны, и асимметрия структурна.

Driving-сторона (левая) содержит адаптеры, инициирующие взаимодействие с приложением. Каждая точка входа — HTTP-контроллер, CLI-раннер, Kafka-потребитель, автоматизированный тест — является driving adapter. Каждый переводит собственный формат входных данных в вызов primary port, принадлежащего domain. Приложение определяет, что умеет делать; driving adapters соответствуют этому определению.

Driven-сторона (правая) содержит адаптеры, отвечающие когда приложению нужны внешние возможности. Каждая исходящая зависимость — PostgreSQL, Stripe, SendGrid, S3 — находится за driven adapter. Domain определяет, что ему нужно, через secondary ports, которыми он владеет; driven adapters реализуют эти порты и поэтому зависят внутрь к domain.

Domain находится в центре, окружённый портами, которыми владеет с обеих сторон. Он ничего не знает об HTTP, базах данных, платёжных SDK или очередях сообщений. Он знает только собственные интерфейсы. Это DIP из unit 02, применённый архитектурно: domain — высокоуровневая политика, владеющая абстракциями; адаптеры — низкоуровневые механизмы, зависящие от этих абстракций.

Тест-драйвер является driving adapter — структурно идентичным HTTP-контроллеру. Он вызывает primary port напрямую, минуя HTTP полностью. Это то, что делает возможными быстрые, не требующие инфраструктуры domain-тесты: domain доступен через тот же интерфейс порта, который используют production-адаптеры, с фейковыми driven adapters, подставленными вместо реальной infrastructure с другой стороны.
</Recap>
