---
slug: "02-projections-and-replay"
lang: ru
track: "architecture-patterns"
unit: "08-event-sourcing"
order: 2
title: "Проекции, replay и снапшоты"
summary: "Read models в event sourcing строятся replay журнала событий через функцию projection. Replay позволяет перестраивать модели по требованию. Снапшоты ограничивают стоимость replay. Версионирование событий и upcasting сохраняют читаемость старых событий при эволюции схемы."
estMin: 28
status: ready
lessonType: topic
level: senior
concepts:
  - projection-replay
  - snapshot
  - temporal-query
  - event-versioning
  - upcasting
  - read-model-rebuild
prereqs: []
sources:
  - https://martinfowler.com/eaaDev/EventSourcing.html
  - https://cqrs.files.wordpress.com/2010/11/cqrs_documents.pdf
  - https://verraes.net/2019/08/eventsourcing-state-from-events-vs-events-as-state/
  - https://microservices.io/patterns/data/event-sourcing.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Новый журнал событий биллинговой команды работал. Каждое событие заказа добавлялось корректно. Но менеджер по продукту спросил: «Можете дать мне дашборд со всеми открытыми заказами, отсортированными по аккаунт-менеджерам, с выделенными просроченными?» Команда посмотрела на журнал событий. Он содержал полную историю каждого заказа — но ответить на этот запрос означало прочитать полную историю каждого из 200 000 заказов, свернуть каждый для получения текущего статуса, отфильтровать ожидающие, объединить с данными аккаунт-менеджера и отсортировать. На 200 000 заказов такой запрос занял бы минуты. Журнал событий был источником истины, но не был спроектирован для паттернов доступа к данным при чтении. Команде нужна была отдельная структура — read model — сформированная для запроса дашборда. И нужен был механизм для её построения и поддержки. Этот механизм — projection: процесс, читающий журнал событий и преобразующий его в денормализованную read model. Но projection сразу поставил новый вопрос: что происходит, когда формат журнала событий изменится через шесть месяцев? Команда только что добавила поле `approval_threshold` в события `OrderApproved`. А как быть с 40 000 событиями `OrderApproved`, уже записанными в журнал до появления этого поля?
</Hook>

<Crux>Read models в event sourcing строятся replay журнала событий через функцию projection. Replay позволяет перестраивать по требованию; снапшоты ограничивают его стоимость; версионирование событий с upcasting сохраняет читаемость старых событий при эволюции схемы.</Crux>

<Explanation>

## Проекции: read models из журнала событий

Журнал событий — источник истины для write-стороны. Он не предназначен для паттернов доступа при чтении. Отвечать на запрос «все просроченные заказы аккаунт-менеджера Чена» сканированием и свёрткой 200 000-записного журнала при каждой загрузке страницы — структурно неправильно.

**Projection** — процесс, читающий события из журнала и строящий денормализованную read model, сформированную для конкретного запроса. Концепция та же, что в CQRS (юнит 07) — ключевое отличие в том, что источником проецирования является журнал событий, а не нормализованная реляционная таблица.

Каждая projection подписывается на поток событий и обрабатывает каждый интересующий тип:

```
on OrderPlaced:          вставить строку в OrderDashboard со status "pending"
on OrderApproved:        обновить строку, установить status "approved", approved_by, approved_at
on InvoiceGenerated:     обновить строку, установить status "invoiced", invoice_id
```

Результат — таблица `OrderDashboard` с одной строкой на заказ, денормализованной и сформированной для запроса дашборда. Запросы обращаются напрямую к read model — без свёртки событий во время запроса.

<FlowDiagram
  label="Журнал событий → read model через projection"
  caption="Журнал событий — источник истины. Projection читает события по порядку (направление replay: от старых к новым) и преобразует их в read model. Команды создают новые события, добавляемые в журнал; projection наблюдает их и обновляет read model. Стрелки показывают направление потока данных: команда к агрегату к журналу событий, журнал событий к projection к read model, запрос к read model."
  nodes={[
    { id: "cmd", label: "Команда", sub: "PlaceOrder, ApproveOrder" },
    { id: "agg", label: "Агрегат", sub: "fold журнала → состояние → валидация → событие" },
    { id: "log", label: "Журнал событий", sub: "append-only, неизменяемый источник истины" },
    { id: "proj", label: "Projection", sub: "replay событий → строит read model" },
    { id: "rm", label: "Read model", sub: "OrderDashboard, денормализованная" },
    { id: "qry", label: "Запрос", sub: "GetOverdueOrders" },
  ]}
  edges={[
    { from: "cmd", to: "agg", label: "инициирует" },
    { from: "agg", to: "log", label: "добавляет событие" },
    { from: "log", to: "proj", label: "поток событий" },
    { from: "proj", to: "rm", label: "записывает" },
    { from: "qry", to: "rm", label: "читает" },
  ]}
/>

## Rebuild by replay: структурная суперсила

Поскольку журнал событий — источник истины, а read model — производный артефакт, read model можно **перестроить с нуля в любое время, воспроизводя полный журнал событий через функцию projection**. Это структурно отличается от CRUD-системы, где read model и write model — одни и те же данные, и «воспроизводить» нечего.

Rebuild-by-replay ценен в нескольких сценариях:

**Изменение схемы:** Дашборду нужен новый столбец `days_to_first_payment`. Добавить столбец в таблицу read model, обновить логику projection для его вычисления, затем воспроизвести все события. Каждая историческая строка получает корректное значение, вычисленное из исходных данных события. Никакой write-side миграции.

**Исправление бага:** В projection был баг — она неправильно вычисляла признак просрочки, помечая заказы просроченными на два дня раньше. Исправить логику projection, воспроизвести журнал, и read model теперь корректна для всех исторических данных.

**Новая read model:** Финансовой команде нужен новый view: все заказы, сгруппированные по расчётному периоду. Создать новую projection, строящую таблицу `BillingPeriodSummary`. Воспроизвести существующий журнал событий для построения из истории. Не нужно ждать накопления будущих событий.

<Inset kind="why">
Rebuild-by-replay возможен только потому, что журнал событий никогда не изменяется. Если бы события можно было редактировать или удалять, replay не воспроизводил бы исходную историю. Неизменяемость — не просто предпочтение дизайна в event sourcing: это структурная предпосылка для того, чтобы replay имел смысл.
</Inset>

## Снапшоты: ограничение стоимости replay

Загрузка агрегата свёрткой его полного журнала событий становится дорогой по мере роста журнала. Заказ с 4000 событий требует 4000 вызовов applyEvent, прежде чем команда вообще начнёт обрабатываться. Это проблема производительности свёртки из урока 1.

**Снапшот** — кешированный промежуточный результат свёртки. На каком-то порядковом номере N приложение сохраняет производное состояние агрегата как снапшот рядом с журналом событий:

```
{
  aggregate_id: "order-48291",
  snapshot_seq: 3800,
  state: { status: "invoiced", total: 48200, line_items: [...], ... },
  created_at: "2024-06-01T12:00"
}
```

При следующей загрузке вместо начала с события 1 и применения всех 4000 событий приложение:
1. Загружает последний снапшот (состояние на seq 3800)
2. Загружает только события после seq 3800 (события 3801–4000)
3. Сворачивает только эти 200 событий поверх состояния снапшота

Стоимость свёртки падает с 4000 до 200 шагов. По мере добавления новых событий новый снапшот берётся периодически.

<FlowDiagram
  label="Загрузка агрегата со снапшотом: replay начинается от снапшота, а не от события 1"
  caption="Снапшот хранит производное состояние агрегата на конкретном порядковом номере. При загрузке приложение стартует со снапшота и сворачивает только события после его порядкового номера. Полный журнал событий присутствует и остаётся источником истины — снапшот является только оптимизацией производительности. Стрелки показывают путь загрузки: снапшот загружается первым, затем применяются хвостовые события."
  nodes={[
    { id: "log-head", label: "События 1–3800", sub: "не пересворачиваются при наличии снапшота" },
    { id: "snap", label: "Снапшот на seq 3800", sub: "кешированное состояние агрегата — не источник истины" },
    { id: "log-tail", label: "События 3801–4000", sub: "сворачиваются поверх состояния снапшота" },
    { id: "state", label: "Текущее состояние агрегата", sub: "результат снапшота + свёртки хвоста" },
  ]}
  edges={[
    { from: "snap", to: "state", label: "загружается первым" },
    { from: "log-tail", to: "state", label: "сворачивается поверх" },
    { from: "log-head", to: "snap", label: "был источником снапшота" },
  ]}
/>

Критический инвариант: снапшот — **не** источник истины. Источник истины — журнал событий. Снапшот можно удалить в любое время — состояние агрегата всегда можно восстановить свёрткой полного журнала с события 1. Снапшоты существуют исключительно для производительности свёртки; они не заменяют события.

## Временны́е запросы: replay префикса журнала

Урок 1 ввёл временно́й запрос: чтобы получить состояние заказа №48291 третьего марта в 14:00, отфильтровать журнал до временно́й метки и свернуть префикс. Это структурное следствие неизменяемого append-only журнала.

Временны́е запросы влекут следствие для производительности: они не могут использовать снапшот, взятый после целевой временно́й метки (этот снапшот включает события, которые нужно исключить). Нужно складывать с начала журнала до целевой временно́й метки. Для агрегатов с очень длинной историей временны́е запросы к старым временны́м меткам дороги. Это известная структурная стоимость event sourcing — управляема для разовых комплаенс-запросов, но не должна быть на горячем пути real-time запросов.

## Версионирование событий и upcasting

Журнал событий неизменяем — уже хранящиеся события нельзя изменить. Но понимание приложением того, что означает событие, эволюционирует. Через шесть месяцев после запуска системы `OrderApproved` получает новое поле `approval_threshold`. В журнале есть 40 000 событий `OrderApproved`, записанных до этого поля. Когда projection воспроизводит их, она встречает события без нового поля.

Два структурных ответа:

**Weak schema / tolerant reader:** Projection обрабатывает `OrderApproved` с политикой толерантности: если `approval_threshold` отсутствует, считать его null или значением по умолчанию. Код projection обрабатывает как старую, так и новую версии события. Работает для аддитивных изменений (новые необязательные поля) и является самым простым подходом.

**Upcasting:** При загрузке (или во время чтения) старые версии событий преобразуются в текущий формат до передачи в функцию `applyEvent` агрегата. Функция upcast для v1 → v2 `OrderApproved` может устанавливать `approval_threshold: null` для всех v1 событий. Логика свёртки агрегата видит только текущий формат. Журнал событий сохраняет исходные байты v1 — upcasting применяется при чтении, а не записывается обратно в журнал.

<Quiz
  id="arch-08-02-quiz-1"
  lessonSlug="02-projections-and-replay"
  lang="ru"
  question="Журнал событий биллинговой команды вырос до 12 миллионов событий для агрегата заказов. Команда замечает, что перестройка read model OrderDashboard с нуля (при изменениях схемы) теперь занимает 4 часа. Разработчик предлагает: «Нужно делать снапшот read model через регулярные интервалы и использовать его как отправную точку для будущих перестроек вместо replay с события 1». Это обоснованное предложение?"
  choices={[
    { label: "Это валидная оптимизация времени перестройки read model, но это не то же самое, что снапшот агрегата. 'Чекпоинт read model' (сохранение состояния read model на определённой позиции в журнале событий) позволяет возобновлять перестройку с чекпоинта вместо события 1, пропорционально сокращая время перестройки. Структурное ограничение: чекпоинт должен хранить позицию в журнале событий (порядковый номер), на которой он был сохранён, чтобы перестройки возобновлялись именно с этой позиции. Если логика projection изменилась (изменение схемы, исправление бага) до взятия чекпоинта, чекпоинт недействителен для этой перестройки и должен быть отброшен. Снапшоты агрегатов и чекпоинты проекций похожи по структуре, но служат разным целям — не путайте их.", correct: true },
    { label: "Предложение несостоятельно. Чекпоинты read model нарушают принцип неизменяемости event sourcing. Read model всегда должна перестраиваться с события 1 для обеспечения корректности.", misconception: "Неизменяемость применяется к журналу событий — события нельзя изменять. Она не запрещает кешировать промежуточные состояния производных артефактов (read models или агрегатов). Чекпоинт projection — оптимизация производительности производного артефакта, а не модификация исходных событий. Журнал событий остаётся неизменным и авторитетным; чекпоинт просто фиксирует, где предыдущая перестройка остановилась, чтобы будущие могли возобновить с этого места." },
    { label: "4-часовое время перестройки указывает на то, что журнал событий слишком велик. Команде следует архивировать события старше 6 месяцев в холодное хранилище и воспроизводить только недавние события.", misconception: "Архивирование событий из активного журнала означает, что они недоступны для полных перестроек. Это нарушает гарантию rebuild-by-replay — read model нельзя было бы восстановить для полной истории, что было структурным свойством, делавшим event sourcing ценным для аудита и временны́х запросов. Если старые события нужны только изредка, их можно перенести на более дешёвое хранилище, но они должны оставаться доступными для replay." },
    { label: "Предложение путает снапшоты агрегатов с чекпоинтами проекций. Для производительности загрузки агрегата уместны снапшоты. Для производительности перестройки read model правильное решение — увеличить скорость обработки воркера проекции (параллелизм, батчинг), а не чекпоинтировать read model.", misconception: "Параллелизм и батчинг могут улучшить пропускную способность projection, но чекпоинт также является валидным подходом для производительности перестройки read model. Ответ выше (первый вариант) полнее: чекпоинты валидны, но должны инвалидироваться при изменении логики projection. Обе техники можно комбинировать. Ключевой момент: чекпоинт projection — валидная оптимизация, отличная от снапшотов агрегатов, а не их путаница." },
  ]}
/>

<Quiz
  id="arch-08-02-quiz-2"
  lessonSlug="02-projections-and-replay"
  lang="ru"
  question="Биллинговая команда хочет добавить новое поле `freight_zone` во все будущие события OrderPlaced. Они также хотят заполнить это поле для существующих событий OrderPlaced в журнале. Разработчик предлагает: «Можно UPDATE существующие строки событий OrderPlaced в базе данных, добавив поле freight_zone для исторических событий». В чём структурная проблема этого предложения?"
  choices={[
    { label: "Обновление существующих строк событий нарушает append-only, неизменяемый контракт журнала событий. События — факты о том, что произошло: их нельзя ретроактивно изменить. Мутирование исторических событий OrderPlaced означает, что журнал больше не точно отражает историю: исходное событие OrderPlaced фиксировало, что заказ был размещён без freight_zone (потому что концепция не существовала в домене в то время). Изменение его для включения заполненного freight_zone создаёт ложный исторический протокол. Правильный подход для эволюции схемы — upcasting: когда историческое событие OrderPlaced читается, функция upcast добавляет freight_zone со значением по умолчанию (null или 'unknown') во время чтения. Журнал событий сохраняет исходные байты. Доменная логика видит согласованную схему. История сохраняется.", correct: true },
    { label: "Предложение нормально, если сделано в транзакции. Обновление исторических событий — валидная стратегия миграции в event sourcing, эквивалентная миграциям схемы в CRUD-системах.", misconception: "Ценность event sourcing для аудита, временны́х запросов и rebuild-by-replay зависит от того, что журнал событий является точной записью того, что произошло. Миграция схемы CRUD изменяет форму хранилища текущего состояния — она не ретроактивно изменяет историю произошедшего. Обновление исторических событий категорически иное и фундаментально нарушает контракт аудита. Заказ, размещённый в 2022 году, не имел freight_zone в момент размещения; обновление события с утверждением, что имел, — фальсификация записи." },
    { label: "Предложение допустимо только для некомплаенс-чувствительных полей, таких как freight_zone. Для финансовых полей события должны быть неизменяемы, но операционные метаданные можно обновлять.", misconception: "Неизменяемость событий не зависит от домена. Трейл аудита, неизменяемый за исключением 'неважных' полей, — не надёжный трейл аудита, а выборочно редактируемая запись. Структурная гарантия того, что replay всегда даёт один и тот же результат, нарушается в момент мутации любого события. Правильный подход — upcasting для всей эволюции схемы, независимо от чувствительности поля." },
    { label: "Предложение корректно, но нужно делать батчами. Обновление всех исторических событий OrderPlaced в одной транзакции может заблокировать таблицу.", misconception: "Проблема не в размере транзакции — в фундаментальном нарушении неизменяемости. Батчинг UPDATE не устраняет структурную проблему: журнал событий после заполнения всё равно содержал бы исторически неточные записи. Правильное решение — upcasting, работающий во время чтения без изменения хранящихся событий." },
  ]}
/>

<Quiz
  id="arch-08-02-quiz-3"
  lessonSlug="02-projections-and-replay"
  lang="ru"
  question="Команда проектирует event-sourced систему для агрегата заказов. Планируют делать снапшот каждые 100 событий. Новый член команды спрашивает: «Если у нас есть снапшот, можно ли удалять события до него для экономии места?» Каков правильный ответ и от чего он зависит?"
  choices={[
    { label: "Удаление событий до снапшота безопасно только если вы уверены, что эти события больше никогда не понадобятся. Случаи, когда они могут понадобиться: (1) временны́е запросы до временно́й метки снапшота — чтобы восстановить состояние на любой момент до взятия снапшота, нужны исходные события; (2) перестройка read model из полной истории — чекпоинты проекций, взятые до снапшота, становятся недействительными; (3) отладка или аудит конкретных прошлых решений. Если системе действительно не нужны временны́е запросы, полноисторические перестройки или глубокие аудиты, то архивирование событий до снапшота в холодное хранилище (не удаление) допустимо. Их постоянное удаление — это односторонняя дверь: после исчезновения исторические гарантии теряются навсегда. Большинство практиков event sourcing хранят полный журнал бессрочно или архивируют в холодное хранилище, а не удаляют.", correct: true },
    { label: "Да, события до снапшота можно удалить. Снапшот содержит полное состояние агрегата на тот момент, так что события избыточны. Их удаление экономит место без потери информации.", misconception: "Снапшот содержит производное состояние агрегата — результат свёртки этих событий. Он не сохраняет отдельные события или их payload. Временны́е запросы до снапшота требуют исходных событий. Projection, которой нужно обрабатывать payload события OrderPlaced (не просто состояние агрегата), не может использовать снапшот в качестве замены. Удаление событий лишает возможности ответить на вопрос «какие позиции были включены в заказ №48291 при его первоначальном размещении?»" },
    { label: "События нельзя удалять в event sourcing никогда. Система должна хранить полный журнал событий бессрочно без исключений.", misconception: "Хотя неизменяемость событий (никаких модификаций) — жёсткое структурное требование, постоянное хранение — это политическое решение, пересекающееся со стоимостью хранения, требованиями комплаенса и GDPR (рассматривается в уроке 3 юнита 08). Некоторые юрисдикции требуют удаления персональных данных, что создаёт реальное противоречие с неизменяемостью. Ответ не 'никогда не удалять' как абсолют — это 'понимать последствия и иметь осознанную политику'." },
    { label: "Удаление событий до снапшота нормально, если снапшот был взят в течение последних 24 часов. События старше суток не имеют операционной ценности.", misconception: "Операционная ценность исторических событий не ограничена давностью. Финансовые аудиты, требования регуляторного комплаенса и временны́е запросы могут охватывать годы. 24-часовой порог — произвольная политика, которая лишила бы систему возможности отвечать именно на те комплаенс-запросы, для которых был принят event sourcing." },
  ]}
/>

</Explanation>

<KeyTakeaway>Read models в event sourcing строятся replay журнала событий через функции projection, что делает их полностью перестраиваемыми из истории. Снапшоты кешируют промежуточные результаты свёртки для ограничения стоимости загрузки — они оптимизация производительности, не источник истины. Временны́е запросы работают свёрткой префикса журнала. Эволюция схемы событий обрабатывается upcasting (преобразование старых событий при чтении) или tolerant readers — никогда изменением хранящихся событий.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-08-02-retrieval"
  lang="ru"
  questions={[
    { q: "Почему read model в event sourcing можно перестроить с нуля в любое время и почему это ценно?", a: "Read model в event sourcing — производный артефакт: построена целиком replay журнала событий через функцию projection. Поскольку журнал событий неизменяем и полон, в read model нет информации, которую нельзя пересчитать из журнала. Read model можно удалить и перестроить в любое время: применить функцию projection ко всем историческим событиям — и та же read model будет восстановлена. Это ценно когда: (1) схема read model изменяется — перестроить с новой логикой projection и все исторические строки заполнятся корректно; (2) в projection был баг — исправить логику, перестроить, и read model корректна для всех исторических данных; (3) нужна новая read model — построить из существующей истории без ожидания будущих событий. Ничего из этого невозможно в CRUD-системе, где текущая таблица состояния — единственная запись." },
    { q: "Что такое снапшот в event sourcing и какой инвариант он должен соблюдать?", a: "Снапшот — кешированный промежуточный результат свёртки: производное состояние агрегата, сохранённое на конкретном порядковом номере события. Вместо свёртки всех N событий при каждой загрузке агрегата система загружает последний снапшот (состояние на seq K) и сворачивает только события после K. Это снижает стоимость свёртки с N шагов до N-K. Критический инвариант: снапшот — не источник истины. Журнал событий остаётся авторитетным. Снапшот можно удалить в любое время — состояние агрегата всегда можно восстановить свёрткой полного журнала с события 1. Единственное назначение снапшота — оптимизация производительности свёртки." },
    { q: "Что такое upcasting и почему он существует в event-sourced системах?", a: "Upcasting — преобразование, применяемое к хранящимся событиям при чтении для конвертации старых версий событий в текущую схему. События в журнале неизменяемы — они хранят байты, записанные в момент возникновения события. Когда схема события эволюционирует (поле добавлено, переименовано или реструктурировано), старые события в журнале не соответствуют новой схеме. Вместо изменения хранящихся событий (что нарушало бы неизменяемость) функция upcast применяется при чтении: принимает хранящееся событие и возвращает версию в текущей схеме, заполняя значениями по умолчанию новые поля или преобразуя переименованные. Журнал событий сохраняет исходные байты. Функция applyEvent агрегата видит только событие в текущем формате. Upcasting — слой совместимости между неизменяемой историей и эволюционирующей доменной моделью." },
  ]}
/>

<Recap lang="ru">
Журнал событий структурно решает проблему аудита и временны́х запросов. Но запрос менеджера по продукту к дашборду — все открытые заказы, отсортированные по аккаунт-менеджеру — нельзя ответить сканированием журнала событий во время запроса. Журнал событий оптимизирован для согласованности write-стороны и сохранения истории, а не для произвольных read-запросов.

Проекции закрывают этот разрыв: функция projection воспроизводит события из журнала и материализует read model, сформированную для конкретного запроса. Поскольку read model производна из неизменяемого журнала событий, её можно перестроить в любое время — изменения схемы, исправления багов, новые требования к запросам обрабатываются обновлением логики projection и replay. Никаких write-side миграций.

По мере роста журнала событий производительность свёртки деградирует. Снапшоты решают это: кешируют производное состояние агрегата на порядковом номере, ограничивая количество событий, которые нужно свернуть при загрузке. Снапшоты — чистая оптимизация производительности: журнал событий остаётся источником истины, снапшоты можно удалять и перестраивать.

Журнал событий неизменяем, но доменная модель не статична. Поля добавляются, переименовываются, реструктурируются. Upcasting справляется с этим: старые события преобразуются в текущую схему при чтении, сохраняя хранящиеся байты нетронутыми. История сохраняется; доменная модель может эволюционировать.

Следующий урок рассматривает ловушки: операционные и структурные издержки, которые event sourcing вводит в продакшне — eventual consistency, противоречие с GDPR, компенсирующие события и честное руководство о том, когда не применять event sourcing.
</Recap>
