---
slug: "03-the-traps"
lang: ru
track: "architecture-patterns"
unit: "08-event-sourcing"
order: 3
title: "Ловушки"
summary: "Неизменяемость и eventual consistency event sourcing — возможности на бумаге и ограничения в продакшне: эволюция схемы, GDPR-удаление, плохие события, лаг проекций — всё требует осознанных структурных ответов. Знать, когда не применять ES, так же важно, как знать, как."
estMin: 28
status: ready
lessonType: topic
level: senior
concepts:
  - eventual-consistency
  - schema-evolution
  - crypto-shredding
  - compensating-event
  - gdpr-immutability
  - event-sourcing-when-not
prereqs: []
sources:
  - https://martinfowler.com/eaaDev/EventSourcing.html
  - https://verraes.net/2019/08/eventsourcing-state-from-events-vs-events-as-state/
  - https://microservices.io/patterns/data/event-sourcing.html
  - https://gdpr-info.eu/art-17-gdpr/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import whenToEventSource from "./infographics/when-to-event-source.svg?raw";

<Hook>
Через восемнадцать месяцев после запуска event-sourced биллинговой платформы одновременно горели три пожара. Первый: клиент подал запрос на удаление данных по GDPR. Юристы сказали, что имя, email и ИНН клиента нужно очистить в течение 30 дней. Юридический отдел предполагал, что инженеры смогут просто удалить нужные строки. Инженеры объяснили: персональные данные клиента встроены в сотни событий — OrderPlaced, InvoiceGenerated, PaymentReceived — разбросанных по неизменяемому журналу событий. Их удаление повредит журнал. Второй: инженер биллинга случайно добавил событие OrderApproved для неправильного ID заказа — ошибка из-за опечатки во время ручной операции поддержки. «Просто удалите его», — сказал менеджер поддержки. Инженеры объяснили снова: события в журнале неизменяемы. Удаление повредит replay для всех read models. Третий: projection финансов показывала устаревшие итоги примерно две секунды после каждого обновления заказа — обычно нормально — но в период высокой нагрузки pipeline projection переполнился, и лаг вырос до сорока секунд. Финансовый директор увидел дашборд с неправильными суммами просрочки и позвонил CEO. Три пожара, одна причина: команда приняла event sourcing без полного понимания его структурных ограничений.
</Hook>

<Crux>Неизменяемость и eventual consistency ES — на бумаге плюсы, в продакшне ловушки; схема и GDPR требуют структурных ответов.</Crux>

<Explanation>

## Ловушка 1: eventual consistency между журналом и проекциями

В event-sourced системе журнал событий обновляется синхронно с каждой командой. Read models (проекции) обновляются асинхронно — они потребляют события из журнала и обновляют свои материализованные представления в отдельном процессе. Это создаёт **окно устаревания**: между добавлением события и моментом, когда projection его обработала и обновила read model, запросы возвращают предыдущее состояние.

При нормальной работе этот лаг — миллисекунды до единиц секунд. При высокой нагрузке, переполнении projection, или после перезапуска он может растянуться. Дашборд с неправильными суммами просрочки не был багом — это лаг projection, проявившийся под нагрузкой.

<FlowDiagram
  label="Лаг projection: событие добавлено до обновления read model"
  caption="Команда добавляет событие в журнал в T=0. Воркер projection забирает событие и обновляет read model в T=Δ. Запросы между T=0 и T=Δ видят устаревшие данные. Лаг Δ зависит от пропускной способности pipeline projection. Стрелки показывают временну́ю последовательность: событие добавлено, событие потреблено projection, read model обновлена."
  nodes={[
    { id: "append", label: "T=0: событие добавлено", sub: "OrderApproved записан в журнал" },
    { id: "pipeline", label: "Pipeline projection", sub: "переполнение растягивает лаг" },
    { id: "update", label: "T=Δ: read model обновлена", sub: "дашборд отражает одобрение" },
    { id: "stale-query", label: "Запрос в T=0..Δ", sub: "видит состояние до одобрения (устаревшее)" },
  ]}
  edges={[
    { from: "append", to: "pipeline", label: "событие поступает в" },
    { from: "pipeline", to: "update", label: "projection записывает" },
    { from: "stale-query", to: "append", label: "внутри окна устаревания" },
  ]}
/>

Управление лагом projection требует осознанного проектирования:

- **Мониторить лаг как метрику первого класса**: предупреждать, когда смещение projection-журнал превышает порог (секунды, а не минуты).
- **Масштабировать воркеры projection** на пиковую пропускную способность событий, а не среднюю.
- **Проектировать read models с учётом устаревания**: дашборд с пометкой «по состоянию на N секунд назад» лучше дашборда, молча показывающего неверные данные.
- **Не использовать async projections для решений на write-пути**: если write-сторона должна прочитать значение перед добавлением события, запрашивать состояние агрегата write-стороны, а не потенциально устаревшую read model.

Ловушка eventual consistency не уникальна для event sourcing — она появляется в любой CQRS-системе с async projection (юнит 07). В event sourcing она усилена, потому что read model — это *единственный* путь запросов; нормализованной таблицы текущего состояния, к которой можно обратиться, нет.

## Ловушка 2: эволюция схемы — неизменяемое прошлое, меняющееся будущее

События — неизменяемые факты о прошлом. Доменная модель не статична. Эти две истины создают постоянное противоречие: структура события, корректная в 2023 году, может не соответствовать тому, что ожидает доменная модель в 2025-м.

Урок 2 ввёл upcasting как механизм. Ловушка — в **операционной дисциплине**, необходимой для работы в масштабе:

- Каждый тип события нуждается в идентификаторе версии (явном или встроенном в имя типа: `OrderApproved.v1`, `OrderApproved.v2`).
- Каждая функция upcast должна поддерживаться бессрочно — пока v1 события существуют в журнале, upcast v1 → v2 должен быть задеплоен.
- **Аддитивные изменения** (новые необязательные поля) безопасны: tolerant readers обрабатывают их значениями по умолчанию.
- **Деструктивные изменения** (переименование или удаление поля, изменение значения поля) требуют новой версии и стратегии миграции.
- Переименованное поле, присутствующее в тысячах проекций, создаёт широкий радиус поражения — каждую projection, читающую старое имя поля, нужно обновить.

Ловушка: команды, не enforcing версионирование событий с первого дня, накапливают неявные v1 события повсюду, а затем сталкиваются с болезненным распутыванием при первом breaking change. Дисциплина в именовании (`OrderApproved.v1`) и поддержке реестра функций upcast — это операционные накладные расходы, которые нужно планировать.

<Inset kind="why">
Структурный урок: схема события — это публичный API с наихудшей политикой депрекации: нельзя удалять старых потребителей (журнал событий содержит их навсегда). К изменениям схемы событий нужно относиться с большей осторожностью, чем к изменениям REST API. Журнал событий — единственная зависимость, которую нельзя сломать.
</Inset>

## Ловушка 3: GDPR и право на забвение

Статья 17 GDPR требует удаления персональных данных по запросу. Неизменяемый журнал событий — структурное препятствие: персональные данные, встроенные в payload событий, нельзя удалить, не повредив журнал.

Стандартный структурный ответ — **crypto-shredding** (также называется crypto-erasure):

1. Персональные данные в payload событий **шифруются** ключом шифрования, специфичным для субъекта (один ключ на клиента или на границу субъекта данных).
2. Журнал событий хранит **зашифрованные** персональные данные — шифротекст, а не открытый текст.
3. При поступлении запроса на удаление ключ шифрования **уничтожается**. Payload событий в журнале по-прежнему существует, но персональные данные теперь вычислительно невосстановимы — фактически удалены.

<FlowDiagram
  label="Crypto-shredding: персональные данные зашифрованы per-subject, ключ уничтожается при удалении"
  caption="При записи персональные данные в payload события шифруются ключом субъекта перед добавлением в журнал. Ключ хранится отдельно в key store. При GDPR-удалении ключ удаляется из key store. Существующие события в журнале содержат только шифротекст — персональные данные вычислительно невосстановимы. Стрелки показывают поток шифрования при записи и путь удаления ключа при erasure."
  nodes={[
    { id: "event", label: "Payload события", sub: "содержит открытый текст персональных данных" },
    { id: "encrypt", label: "Шифрование ключом субъекта", sub: "перед добавлением в журнал" },
    { id: "log", label: "Журнал событий", sub: "хранит только шифротекст" },
    { id: "keystore", label: "Key store", sub: "ключ субъекта — отдельно от журнала" },
    { id: "erasure", label: "GDPR-запрос на удаление", sub: "удалить ключ субъекта из key store" },
  ]}
  edges={[
    { from: "event", to: "encrypt", label: "открытый текст" },
    { from: "encrypt", to: "log", label: "шифротекст добавлен" },
    { from: "keystore", to: "encrypt", label: "ключ используется при записи" },
    { from: "erasure", to: "keystore", label: "ключ удалён" },
  ]}
/>

Crypto-shredding имеет структурные предпосылки:

- Персональные данные должны быть **идентифицированы заранее** в каждом типе событий. Поля, добавленные спустя месяцы, могут быть пропущены.
- Key store должен быть отдельно от журнала событий. Потеря key store до обработки запроса на удаление означает, что данные никогда нельзя будет удалить.
- **Проекции, материализовавшие открытый текст**, тоже нужно обновить — crypto-shredding журнала событий автоматически не очищает read model, уже извлёкшую и сохранившую персональные данные в открытом тексте.

Команды, не проектирующие crypto-shredding с самого начала, сталкиваются с болезненным рефакторингом: идентификация всех персональных данных в сотнях типов событий, ретроактивное добавление шифрования (невозможно для уже хранящихся событий в открытом тексте) и потенциальное признание, что pre-retrofit персональные данные нельзя удалить из журнала.

## Ловушка 4: плохое событие вырублено в камне

Во время ручной операции поддержки инженер добавил событие `OrderApproved` для неправильного ID заказа. В CRUD-системе исправление — `UPDATE`. В event-sourced системе `UPDATE` не существует.

Правильный ответ на плохое событие — **compensating event** (компенсирующее событие): новое событие, добавленное в журнал, отменяющее бизнес-эффект ошибочного. Для неправильного одобрения заказа добавляется событие `OrderApprovalReverted` с причиной и объяснением ошибки. Логика свёртки агрегата обрабатывает `OrderApprovalReverted`, возвращая заказ в предыдущее состояние.

Это не обходное решение — это предусмотренный механизм. Он сохраняет полный трейл аудита: одобрение, ошибка и исправление — всё видно в журнале. Аудитор комплаенса может видеть точно, что произошло, когда и почему.

<Inset kind="note">
Compensating events требуют предусмотрительности: доменная модель должна определять события коррекции для каждой записи, которая может пойти не так операционно. Команды, обнаруживающие это только после того, как плохое событие уже добавлено, оказываются в худший возможный момент для проектирования схемы compensating event.
</Inset>

Структурное следствие: **инструментарий операторов, пишущий напрямую в журнал событий**, должен обрабатываться с той же дисциплиной, что и продакшн-код приложения. Добавление неправильного события через скрипт технического обслуживания вызывает ровно ту же проблему, что и баг в коде. Журналы событий в продакшне должны быть защищены контролем доступа с той же строгостью, что и продакшн-базы данных.

<Quiz
  id="arch-08-03-quiz-1"
  lessonSlug="03-the-traps"
  lang="ru"
  question="Биллинговая платформа получает запрос на удаление данных по GDPR для клиента #C-8821. Данные клиента (имя, email, ИНН) присутствуют в 340 событиях типов OrderPlaced, InvoiceGenerated и PaymentReceived. Система НЕ была спроектирована с crypto-shredding. Каковы реалистичные варианты и какова цена каждого?"
  choices={[
    { label: "Без crypto-shredding реалистичные варианты: (1) Редактирование на месте — перезаписать персональные поля в payload событий null или заглушкой. Это нарушает append-only контракт и повреждает трейл аудита (replay отредактированных событий даёт другой результат, чем оригинальных). Технически может удовлетворить GDPR, если данные исчезли, но нарушает структурную целостность журнала. (2) Удаление событий с персональными данными — аналогичное структурное нарушение, и вероятно приведёт к несогласованному fold при последующем replay. (3) Принять нарушение комплаенса и заплатить регуляторные последствия. (4) Мигрировать систему на crypto-shredding ретроактивно — не может защитить события, уже хранящиеся в открытом тексте; требует полного архитектурного рефакторинга. Все варианты несут серьёзные издержки. Это структурное следствие непроектирования под GDPR изначально в event-sourced системе.", correct: true },
    { label: "Команда может удалить только поля персональных данных из payload событий, сохранив сами события, удовлетворив GDPR без повреждения replay.", misconception: "Удаление полей из хранящихся событий нарушает неизменяемость журнала событий. Инвариант 'состояние как fold' зависит от того, что каждое событие даёт ровно тот же результат при каждом replay. Частично отредактированное событие OrderPlaced без поля customer_name может вынудить projection давать другой результат, чем оригинальный — особенно если функция applyEvent projection ожидала это поле. Кроме того, это всё равно модификация исторической записи, что является структурным ограничением, на котором построен event sourcing." },
    { label: "GDPR-запросы на удаление не применяются к журналам событий, потому что данные там хранились не 'намеренно как персональные данные' — это было побочным эффектом бизнес-операций.", misconception: "GDPR применяется к любым персональным данным, хранящимся в любой форме, независимо от механизма хранения или намерения. Журнал событий — это хранилище данных. Персональные данные в нём подпадают под Статью 17 GDPR, как и любые другие хранящиеся персональные данные. Неизменяемость журнала событий — инженерное ограничение, а не юридическая защита." },
    { label: "Команда может удовлетворить запрос, очистив только read models (проекции), содержащие данные клиента. Журнал событий — внутренняя техническая деталь, которую не нужно удалять.", misconception: "Статья 17 GDPR требует удаления персональных данных из всех систем, где они хранятся, а не только из поверхностей, видимых пользователю. Журнал событий хранит персональные данные. Его нужно включить. Очистка только проекций при сохранении персональных данных в журнале событий не удовлетворяет запрос на удаление." },
  ]}
/>

## Ловушка 5: операционная и сложностная стоимость — когда не применять event sourcing

Event sourcing — не дефолтный архитектурный выбор. Он несёт издержки, которые должны быть оправданы требованиями:

- **Сложность write-стороны**: вместо `INSERT`/`UPDATE` каждая запись требует загрузки агрегата (fold или снапшот + хвост), валидации команды, создания события и его атомарного добавления. Это значительно больше кода, чем CRUD.
- **Сложность read-стороны**: нет нормализованной таблицы текущего состояния для запросов. Каждый запрос требует read model, которая требует projection, которая требует pipeline. Для системы с 20 типами сущностей это означает 20+ проекций для построения и поддержки.
- **Операционная сложность**: event store, воркеры projection, хранилище снапшотов и key store — всё это продакшн-инфраструктура. Каждый компонент нуждается в мониторинге, планировании ёмкости и восстановлении после сбоев. Лаг projection нужно мониторить как метрику первого класса.
- **Сложность тестирования**: event-sourced агрегаты тестируются утверждением об испускаемых событиях (дано эти прошлые события → когда эта команда → ожидаем эти новые события). Это освоимо, но незнакомо большинству backend-команд.

<Quiz
  id="arch-08-03-quiz-2"
  lessonSlug="03-the-traps"
  lang="ru"
  question="Инженер предлагает применить event sourcing ко всем сущностям в B2B биллинговой платформе: заказам, позициям, счетам, платёжным записям, пользовательским предпочтениям, настройкам уведомлений UI и сессионным токенам. Каков правильный ответ и как на самом деле должно приниматься это решение?"
  choices={[
    { label: "Event sourcing всего набора сущностей накладывает сложность записи, поддержку read models и операционные накладные расходы на каждую сущность — включая те, для которых издержки совершенно не оправданы. Пользовательские предпочтения и сессионные токены не имеют требований к аудиту, нет потребности во временны́х запросах и никакой ценности фиксации бизнес-решений. Применение к ним event sourcing добавляет инфраструктурные издержки без структурной пользы. Решение должно приниматься пообъектно по трём вопросам: (1) является ли история переходов состояния бизнес-требованием первого класса (аудит, комплаенс, урегулирование споров)? (2) нужны ли временны́е запросы ('состояние на дату X')? (3) требует ли домен фиксации намерения/контекста за каждым изменением состояния? Если ответ на все три 'нет', CRUD-модель почти наверняка уместна. Заказы и счета, вероятно, проходят все три. Настройки уведомлений и сессионные токены — почти наверняка нет.", correct: true },
    { label: "Event sourcing нужно применять равномерно по всей системе для единообразия. Смешанная архитектура (одни сущности CRUD, другие event-sourced) сложнее в понимании и поддержке.", misconception: "Архитектурное единообразие — средство, а не цель. Принудительное прогонение всех сущностей через модель event sourcing во избежание смешанной архитектуры накладывает издержки без пользы для сущностей, которым не нужны аудит или временны́е запросы. Инженерная стоимость поддержки проекций и схем событий для сессионных токенов и UI-предпочтений реальна и постоянна. Система, применяющая event sourcing только к доменным сущностям, которые того заслуживают (заказы, счета, платежи), и использующая CRUD для остального — не непоследовательна, а правильно адаптирована под требования каждой сущности." },
    { label: "Инженер прав — единообразие важно, а накладные расходы event sourcing невелики после начальной настройки.", misconception: "Накладные расходы event sourcing не невелики. Каждый event-sourced тип сущности требует: определений типов событий, логики applyEvent, стратегии снапшотов, хотя бы одной projection, инфраструктуры воркеров projection, мониторинга лага и функций upcast для будущих изменений схемы. Умноженное на 20+ типов сущностей, включая те, которым не нужен аудит, — это существенные и постоянные инженерные издержки. «Невысокие накладные расходы после настройки» — это формулировка, используемая для обоснования решений, о которых позже сожалеют при поддержке." },
    { label: "Предложение нормально, если команда хранит все события в едином общем event store. Накладные расходы масштабируются сублинейно с числом сущностей.", misconception: "Общий event store снижает стоимость инфраструктуры хранения, но не снижает per-entity издержки проекций, функций applyEvent, логики upcasting и мониторинга. Структурные издержки на поддержку масштабируются с числом типов сущностей независимо от того, делят ли события физическое хранилище." },
  ]}
/>

Честное руководство о том, когда применять event sourcing:

**Event sourcing хорошо подходит когда:**
- История переходов состояния сущности — требование комплаенса или законодательства (финансовые транзакции, медицинские записи, регулируемые решения)
- Временны́е запросы к прошлым состояниям нужны в продакшне, а не только для отладки
- Домен требует различать разные бизнес-причины для одного и того же результирующего состояния (OrderApproved рецензентом vs AutoApproved правилом)
- У команды есть реальный опыт с паттерном — event sourcing, принятый командой, никогда не поддерживавшей pipeline projection, несёт высокий риск

**Event sourcing плохо подходит когда:**
- Сущность активно изменяется в CRUD-стиле без значимой истории (предпочтения, настройки, данные сессии, записи кеша)
- Основное требование — быстрые, гибкие запросы текущего состояния; требований к аудиту нет
- Команда ещё не имеет опыта с инфраструктурой проекций и управлением лагом
- Систему нужно сдать в сжатые сроки — event sourcing существенно увеличивает начальное время разработки

<Infographic
  svg={whenToEventSource}
  label="Двухколоночное сравнение: когда event sourcing хорошо подходит, а когда плохо. Хорошо подходит (бирюзовый): история — требование комплаенса или закона; нужны временны́е запросы к прошлому состоянию в проде; домену важен умысел за каждым изменением; у команды есть опыт с пайплайном проекций. Плохо подходит (розовый): CRUD-сущность без значимой истории; нужен только быстрый запрос текущего состояния; команда впервые работает с инфраструктурой проекций; сжатые сроки поставки."
  caption="Решение принимается пообъектно, а не на всю систему. Слева (бирюзовый) — event sourcing оправдывает цену; справа (розовый) — правильнее CRUD."
  hue="var(--d-infra)"
/>

<Quiz
  id="arch-08-03-quiz-3"
  lessonSlug="03-the-traps"
  lang="ru"
  question="Разработчик утверждает: «Event sourcing и CQRS — один паттерн, нельзя использовать один без другого». Какова правильная позиция и почему это различие важно?"
  choices={[
    { label: "Event sourcing и CQRS — независимые паттерны, хорошо компонующиеся, но не одно и то же. CQRS (юнит 07) разделяет write model от read model — он ничего не говорит о том, как write-сторона хранит состояние. Можно применять CQRS с нормализованной write-side базой данных и без event sourcing. Event sourcing касается того, как write-сторона хранит состояние — как append-only журнал событий, а не текущее состояние. Можно использовать event sourcing без CQRS: загрузить агрегат из событий, обработать команду, добавить событие, запрашивать состояние агрегата напрямую из fold. Путаница возникает, потому что они естественно компонуются: event sourcing создаёт полный журнал событий, который проекции (CQRS read models) могут воспроизводить. Но это выбор, а не требование. Путаница ведёт команды к принятию обоих, когда нужен только один, существенно увеличивая сложность.", correct: true },
    { label: "CQRS и event sourcing фактически один паттерн на практике. Каждая event-sourced система использует CQRS, и каждой CQRS-системе в конечном счёте нужен event sourcing для полных возможностей replay.", misconception: "Ни одно из этих направлений утверждения не верно. Event sourcing можно использовать без CQRS: агрегат загружается через fold, команды обрабатываются, события добавляются, текущее состояние запрашивается из производного свёрткой состояния без отдельной read model. И наоборот, CQRS можно реализовать с нормализованной write-side таблицей и sync или async projection — event sourcing не нужен. Паттерны хорошо компонуются, но независимы." },
    { label: "Различие теоретическое. В продакшн-системе два паттерна всегда используются вместе, поэтому считать их отдельными — излишняя академичность.", misconception: "Различие практически важно, потому что стоимость принятия каждого паттерна значительна. Принятие CQRS без event sourcing — умеренное архитектурное решение: добавляет read model и pipeline projection. Принятие event sourcing без CQRS редко, но валидно для простых доменов с немногими паттернами запросов. Принятие обоих добавляет существенную сложность. Команда, путающая их и принимающая оба, когда оправдан только один, платит лишние издержки сложности." },
    { label: "CQRS — предпосылка для event sourcing. Нужно разделить write и read модели, прежде чем хранить состояние как события.", misconception: "CQRS не является предпосылкой для event sourcing. Event sourcing касается только модели хранения write-стороны — журнал событий как источник истины, состояние производно через fold. Система может event-sourced хранить write-сторону и обслуживать read-запросы напрямую из сведённого состояния агрегата без какой-либо отдельной инфраструктуры read model. Добавление CQRS проекций — оптимизация масштабирования и форм запросов, а не структурное требование event sourcing." },
  ]}
/>

</Explanation>

<KeyTakeaway>Продакшн-ловушки event sourcing структурны, а не случайны: лаг projection требует явного мониторинга и бюджетов устаревания; эволюция схемы требует версионированных типов событий и функций upcast, поддерживаемых бессрочно; GDPR-удаление требует crypto-shredding, спроектированного с первого дня; плохие события требуют compensating events, а не редактирования. Event sourcing — правильный выбор для агрегатов с реальными требованиями аудита, временны́х запросов или фиксации намерений — и неправильный для всего остального.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="arch-08-03-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое crypto-shredding и почему он является стандартным GDPR-ответом для event-sourced систем?", a: "Crypto-shredding — техника шифрования персональных данных в payload событий с ключом шифрования, специфичным для субъекта, перед добавлением события в журнал. Ключ хранится отдельно. При поступлении GDPR-запроса на удаление ключ субъекта уничтожается. Payload событий в журнале по-прежнему существует, но содержит только шифротекст — персональные данные вычислительно невосстановимы. Это удовлетворяет требованию удаления без изменения журнала событий, сохраняя его неизменяемость и append-only структуру. Это стандартный ответ, потому что альтернативные подходы — удаление или редактирование событий — повреждают журнал и нарушают replay. Crypto-shredding должен быть спроектирован с самого начала; его нельзя ретроактивно применить к событиям, уже хранящимся в открытом тексте." },
    { q: "Плохое событие было добавлено в журнал из-за ошибки инженера поддержки. Почему его нельзя просто удалить и каков правильный ответ?", a: "Целостность журнала событий зависит от его append-only, неизменяемой природы. Удаление события из журнала означает, что каждая projection и каждый агрегат, воспроизводящий журнал, даст другие результаты после удаления, чем до. Трейл аудита повреждён — исторический протокол больше не точно отражает произошедшее. Правильный ответ — compensating event: новое событие, добавленное в журнал, отменяющее бизнес-эффект ошибочного. Для неправильного OrderApproved добавляется событие OrderApprovalReverted с причиной и отменяющим актором. Логика свёртки агрегата обрабатывает оба события и приходит к корректному состоянию. Трейл аудита сохраняется: ошибка и её исправление видны в журнале. Compensating events должны быть определены в доменной модели заблаговременно — их нельзя надёжно импровизировать во время инцидента." },
    { q: "Назовите три категории сущностей, для которых event sourcing плохо подходит, и объясните почему.", a: "Три категории: (1) Данные сессий и аутентификации (сессионные токены, OAuth-токены, refresh-токены) — нет требований к аудиту, нет потребности во временны́х запросах, есть естественный TTL. Хранение их как событий добавляет инфраструктуру проекций без структурной пользы. (2) Пользовательские предпочтения и настройки (конфигурация UI, настройки уведомлений, тема оформления) — меняются часто, нет требований комплаенса для хранения истории, намерение за каждым изменением не является доменным фактом, заслуживающим фиксации. (3) Записи кеша и производные данные — они уже являются производными артефактами; event-sourcing их создаёт журналы событий для данных, которые никогда не были источником истины. Общая нить: сущности плохо подходят для event sourcing, когда история их переходов состояния не имеет бизнес-ценности, нет требований комплаенса и нет потребности во временны́х запросах." },
  ]}
/>

<Recap lang="ru">
Три пожара, одна причина: команда приняла event sourcing без понимания его структурных ограничений.

GDPR-пожар был предсказуем. Персональные данные в неизменяемом журнале событий нельзя удалить. Crypto-shredding — шифрование персональных данных per-subject, уничтожение ключа при удалении — это структурное решение, но его нужно проектировать с первого дня. Ретрофит болезненен или невозможен для событий, уже хранящихся в открытом тексте.

Пожар с плохим событием тоже был предсказуем. Журнал событий неизменяем. Инструментарий операторов, пишущий напрямую в него, должен обрабатываться с продакшн-дисциплиной. Когда плохое событие попадает в журнал, ответ — compensating event: новый факт, отменяющий бизнес-эффект. История сохраняется; ошибка и её исправление видны оба.

Пожар с лагом projection был провалом мониторинга. Eventual consistency между журналом событий и проекциями — структурная особенность. Под нагрузкой лаг растёт. Лаг projection нужно мониторить как метрику первого класса, масштабировать на пиковую пропускную способность и показывать в UI там, где это важно.

Event sourcing и CQRS — независимые паттерны, хорошо компонующиеся, но не одно и то же. Event sourcing оправдывает свои издержки для агрегатов с реальными требованиями аудита, временны́х запросов или фиксации намерений. Для предпочтений, данных сессий и простых CRUD-сущностей издержки реальны, а пользы нет.

Структурный трек заканчивается здесь. Следующий юнит (09) переходит от event sourcing — как хранится состояние — к событийно-ориентированной архитектуре: как модули общаются через события и как это формирует связанность.
</Recap>
