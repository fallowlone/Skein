---
slug: "01-events-as-truth"
lang: ru
track: "architecture-patterns"
unit: "08-event-sourcing"
order: 1
title: "События как источник истины"
summary: "Event sourcing хранит состояние как append-only журнал неизменяемых фактов. Текущее состояние не хранится напрямую — оно вычисляется свёрткой (fold) журнала событий. События фиксируют, что произошло и почему, а не только итоговый снимок."
estMin: 26
status: ready
lessonType: topic
level: senior
concepts:
  - event-log
  - append-only
  - left-fold
  - immutable-events
  - temporal-query
  - audit-trail
prereqs: []
sources:
  - https://martinfowler.com/eaaDev/EventSourcing.html
  - https://cqrs.files.wordpress.com/2010/11/cqrs_documents.pdf
  - https://verraes.net/2014/07/log-all-the-things/
  - https://microservices.io/patterns/data/event-sourcing.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Биллинговая команда получила звонок от финансового директора в 21:00: «Скажите точно, в каком состоянии был заказ №48291 третьего марта в 14:00 и почему он изменился с "ожидает" на "одобрен" тем днём». Команда открыла базу данных. В таблице orders была одна строка для заказа №48291 с текущим статусом: «выставлен счёт». История исчезла. Таблица хранила только последнее состояние. Никакой записи о том, когда заказ был одобрен, кто это сделал, какими были позиции в момент одобрения и почему статус изменился, — не было. Историю восстанавливали по логам приложения — ненадёжным, неполным, не синхронизированным с деплоями. Чтобы дать частичный ответ, ушло шесть часов. Финансовый директор остался недоволен. Эта команда построила абсолютно нормальную CRUD-систему. Каждый `UPDATE orders SET status = 'approved'` уничтожал предыдущее состояние. «Почему» — бизнес-событие — нигде в схеме не было. Им нужен был не лучший запрос и не лучший индекс. Им нужна была другая модель того, что хранить в принципе.
</Hook>

<Crux>В event sourcing журнал — источник истины: состояние вычисляется свёрткой append-only событий, а не хранится напрямую.</Crux>

<Explanation>

## CRUD-модель уничтожает историю

Обычная CRUD-система хранит **текущее состояние**. Когда заказ одобряется, приложение выполняет `UPDATE orders SET status = 'approved' WHERE id = 48291`. Предыдущее состояние — «ожидает» — исчезло. В базе данных нет никакой записи о переходе: ни когда он произошёл, ни что его вызвало, ни каким был payload в момент принятия решения.

Это не баг приложения. Это следствие модели данных. Реляционные таблицы спроектированы для хранения последнего снимка сущности. Такая модель эффективна, привычна и уместна для большого класса задач. Но у неё есть структурное следствие: **история того, как сущность пришла к текущему состоянию, по умолчанию отбрасывается**.

Команда, восстанавливавшая историю для финансового директора, делала вручную то, что должна была автоматически сохранить модель данных. Они обратно инженерили историю из логов, временных меток коммитов и памяти.

## Модель event sourcing: только добавление, никогда перезапись

Event sourcing инвертирует это. Вместо хранения текущего состояния заказа вы храните **каждое бизнес-событие, которое когда-либо произошло с этим заказом** — в append-only журнале. Строки в таблице событий для заказа №48291 выглядят так:

```
seq  event_type           payload                          occurred_at
1    OrderPlaced          {customer_id, line_items}        2024-03-01 09:12
2    OrderReviewStarted   {reviewer_id}                    2024-03-02 14:33
3    OrderApproved        {approved_by, reason}            2024-03-03 14:07
4    InvoiceGenerated     {invoice_id, amount}             2024-03-04 11:55
```

Эти строки **никогда не обновляются, никогда не удаляются**. Событие, которое произошло, — это факт в прошедшем времени. `OrderPlaced` — это не описание текущего состояния, это запись о том, что размещение *произошло*. Event store — структурное выражение этой неизменяемости.

<FlowDiagram
  label="Журнал событий как источник истины: append-only события"
  caption="События — это факты, добавляемые по порядку в неизменяемый журнал. Ни одно событие не изменяется и не удаляется. Журнал растёт только в одном направлении — вперёд во времени. Стрелки показывают направление добавления: каждое новое событие добавляется после предыдущего."
  nodes={[
    { id: "e1", label: "OrderPlaced", sub: "seq 1 — 2024-03-01" },
    { id: "e2", label: "OrderReviewStarted", sub: "seq 2 — 2024-03-02" },
    { id: "e3", label: "OrderApproved", sub: "seq 3 — 2024-03-03" },
    { id: "e4", label: "InvoiceGenerated", sub: "seq 4 — 2024-03-04" },
    { id: "new", label: "Следующее событие", sub: "добавляется, никогда не перезаписывает" },
  ]}
  edges={[
    { from: "e1", to: "e2", label: "добавление" },
    { from: "e2", to: "e3", label: "добавление" },
    { from: "e3", to: "e4", label: "добавление" },
    { from: "e4", to: "new", label: "добавление" },
  ]}
/>

## Текущее состояние — это left-fold журнала событий

Если журнал событий — источник истины, как получить текущее состояние заказа? Нужно **свернуть** (reduce) события слева направо, применяя каждое событие к аккумулятору, начинающемуся с пустого состояния:

```
currentState = events.reduce(applyEvent, initialState)
```

`applyEvent` — чистая функция: принимает текущее накопленное состояние и событие, возвращает следующее состояние. Никаких побочных эффектов. Для каждого типа события она знает, как продвинуть состояние:

```
applyEvent(state, OrderPlaced)         → state со status: "pending", line_items: [...]
applyEvent(state, OrderReviewStarted)  → state со status: "under_review", reviewer_id: ...
applyEvent(state, OrderApproved)       → state со status: "approved", approved_by: ...
applyEvent(state, InvoiceGenerated)    → state со status: "invoiced", invoice_id: ...
```

Результат свёртки — текущее состояние агрегата. Оно **производно**, а не хранится. Когда приложению нужно обработать команду для заказа №48291, оно загружает журнал событий этого заказа и сворачивает его, восстанавливая текущее состояние. Состояние никогда не записывается в нормализованный столбец.

<Inset kind="note">
Это структурная связь между event sourcing и CQRS (юнит 07). Event sourcing касается write-стороны — как хранится состояние агрегата. CQRS — отдельный паттерн, разделяющий read и write модели. Они хорошо компонуются, но независимы: можно использовать event sourcing без CQRS и CQRS без event sourcing. Урок по проекциям CQRS (юнит 07) упоминал replay из журнала событий — это работа event sourcing.
</Inset>

## События фиксируют намерение, а не только итоговый снимок

Критическое свойство хорошо названных событий: они фиксируют **что произошло** на уровне бизнеса, а не только результирующее изменение данных. `OrderApproved` — это не то же самое, что «столбец status изменился с under_review на approved». Название несёт намерение: было принято бизнес-решение одобрить заказ. Payload несёт контекст: кто одобрил, код причины, действующий порог одобрения на тот момент.

Это различие важно, когда доменная логика сложна. Рассмотрим изменение количества в позиции:

- CRUD: `UPDATE order_items SET quantity = 5 WHERE id = 77` — только итоговый снимок
- Event sourcing: `LineItemQuantityAdjusted { item_id: 77, previous_qty: 3, new_qty: 5, adjusted_by: "ops-team", reason: "supplier_substitution" }` — намерение + контекст

Через шесть месяцев, когда возникнет спор о том, почему изменилось количество, event-sourced система ответит точно. CRUD-система предложит только цифру 5.

<Quiz
  id="arch-08-01-quiz-1"
  lessonSlug="01-events-as-truth"
  lang="ru"
  question="Команда обсуждает, применять ли event sourcing для агрегата заказов биллинговой платформы. Разработчик аргументирует: «Историю аудита можно получить, включив отслеживание изменений на уровне базы данных (CDC) или ведя отдельную таблицу аудита. Нам не нужно менять всю модель данных». В чём структурное различие между CDC/audit-log подходами и настоящим event sourcing?"
  choices={[
    { label: "CDC и таблицы аудита фиксируют after-image (изменённые значения строк) как побочный эффект операций записи. Это механические снимки изменений данных, а не бизнес-значимые факты. Event sourcing инвертирует связь: бизнес-события — основное хранилище; текущее состояние производно из них. Название события и payload фиксируют бизнес-намерение (OrderApproved с причиной и одобряющим), а не просто мутацию данных. CDC не может различить одобрение, инициированное старшим менеджером в обход правил, и одобрение, сработавшее по кредитному лимиту автоматически — оба дают одно и то же изменение строки. Event sourcing фиксирует оба как отдельные типы событий с разными payload, делая намерение данными первого класса.", correct: true },
    { label: "Структурного различия нет. CDC захватывает каждое изменение строки, так что он эквивалентен журналу событий. Команда должна использовать CDC, раз это не требует изменений модели данных.", misconception: "CDC захватывает мутации данных на уровне хранилища — какие байты изменились. Он не фиксирует бизнес-причину изменения, контекст решения или семантическое различие между двумя операциями, дающими одинаковое состояние строки разными бизнес-путями. Event-sourced модель фиксирует именованные бизнес-факты с payload, несущим намерение. Это структурно разные вещи: CDC — технический аудит мутаций хранилища; event sourcing — доменная модель, где события первичны." },
    { label: "Event sourcing лучше только если системе нужны временны́е запросы. Для простых требований аудита таблица аудита эквивалентна.", misconception: "Временны́е запросы — одно из преимуществ, но структурное различие шире. Таблица аудита обычно хранит старые/новые значения столбцов с временной меткой — это история мутаций строк, а не история бизнес-фактов. Она не может ответить на вопрос «каким было бизнес-намерение этого изменения?» или «воспроизведи точную последовательность бизнес-решений, приведших к этому состоянию». Event sourcing делает журнал событий основным источником истины, что одновременно даёт rebuild-by-replay, временны́е запросы и фиксацию намерений." },
    { label: "Команде следует добавить CDC-поток и проецировать его в журнал событий, создав гибридный подход с лучшим из обоих миров.", misconception: "Проецирование CDC-потока в события даёт технический журнал событий мутаций строк, а не доменный журнал бизнес-фактов. Проблема именования сохраняется: OrderApproved и status_column_changed_to_approved — не одно и то же. Если системе нужны настоящие бизнес-события, их нужно авторить как доменные события в прикладном слое — CDC не может вывести семантическое намерение мутации." },
  ]}
/>

## Суперсила аудита и временны́х запросов

Структурное следствие хранения журнала событий как источника истины: вопрос финансового директора — «в каком состоянии был заказ №48291 третьего марта в 14:00?» — становится тривиальным:

```
events.filter(e => e.occurred_at <= "2024-03-03T14:00").reduce(applyEvent, initialState)
```

Отфильтровать журнал событий до целевой временно́й метки, затем свернуть. Результат — точное состояние в тот момент. Никакой реконструкции из логов, никаких частичных ответов. Это **временно́й запрос** (temporal query): получение состояния сущности в любой момент её прошлого.

Временны́е запросы — не болт-он фича в event-sourced системах. Это структурное следствие модели данных. Журнал событий сохраняет каждый переход состояния; свёртка его префикса даёт любое прошлое состояние. Это делает event sourcing естественным выбором для доменов, где аудит и временны́е запросы — требования первого класса: финансовые системы, регулируемые домены, логистика, медицинские записи.

<Quiz
  id="arch-08-01-quiz-2"
  lessonSlug="01-events-as-truth"
  lang="ru"
  question="Команда проектирует агрегат заказов для event-sourced системы. Разработчик предлагает назвать событие 'OrderStatusUpdated' с payload { new_status: 'approved' }. Старший инженер возражает. В чём возражение и почему оно важно?"
  choices={[
    { label: "Возражение в том, что 'OrderStatusUpdated' описывает мутацию данных (поле status изменилось), а не бизнес-факт (было принято решение об одобрении). Это вынуждает читателя выводить намерение из данных. Два события, дающих одинаковое изменение статуса, — одобрение рецензентом-человеком и автоматическое одобрение по правилу кредитного лимита — неотличимы при таком именовании. Доменные события должны называться в прошедшем времени на бизнес-уровне: OrderApproved (с approved_by и reason), AutoApprovalGranted (с rule_id и threshold), CreditLimitApprovalOverridden (с override_by). Это важно, потому что намерение, вложенное в название события, — это данные, которые делают event sourcing ценнее простых журналов аудита. Запрос «все заказы, одобренные правилом кредитного лимита», невозможен, если все одобрения хранятся как OrderStatusUpdated.", correct: true },
    { label: "Возражение чисто стилистическое. 'OrderStatusUpdated' — корректное название события; payload содержит все необходимые данные. Старший инженер чрезмерно усложняет.", misconception: "Именование событий — доменное решение, а не вопрос стиля. Событие, фиксирующее изменение столбца status, — технический факт; событие, фиксирующее бизнес-решение, — доменный факт. Это различие определяет, на какие вопросы система может отвечать, запрашивая журнал событий. 'OrderStatusUpdated' не может различить разные бизнес-причины одного и того же перехода статуса, что сводит на нет большую часть ценности event sourcing." },
    { label: "Возражение в том, что события не должны включать поля статуса вообще. События должны фиксировать только триггер перехода (кто одобрил, какое правило сработало), а не результирующее состояние.", misconception: "События часто включают в payload и триггер, и соответствующее результирующее состояние. Проблема 'OrderStatusUpdated' — в названии (которое предполагает мутацию данных, а не бизнес-факт) и в потере намерения, а не в наличии данных о статусе в payload. Хорошо названное событие 'OrderApproved' может включать новый статус, одобряющего и код причины в payload, не теряя семантического намерения." },
    { label: "Возражение об обратной совместимости: 'OrderStatusUpdated' слишком общее и потребует версионирования схемы при добавлении новых значений статуса. Нужно более конкретное название, чтобы избежать проблем upcasting.", misconception: "Хотя эволюция схемы — реальная проблема в event sourcing (рассматривается в уроке 3 юнита 08), возражение старшего инженера здесь о доменной семантике, а не о совместимости схемы. Название 'OrderStatusUpdated' теряет бизнес-намерение вне зависимости от количества значений статуса. Проблема версионирования/upcasting — следствие плохого именования событий, а не причина его избегать." },
  ]}
/>

## Чего event sourcing не является: замена CRUD повсюду

Event sourcing влечёт структурные издержки. Журнал событий для активно мутирующего агрегата накапливается бесконечно. Загрузка агрегата с тысячами событий требует их свёртки — производительность деградирует без митигации (снапшоты, рассматриваются в уроке 2 юнита 08). Запросы к текущему состоянию множества агрегатов одновременно требуют read model (проекции), построенной из журнала событий, — то есть запуска и поддержки pipeline проекций. Write-сторона и read-сторона теперь отдельные системы.

Это не теоретические издержки. Команды, применяющие event sourcing ко всем сущностям в системе — включая те, у которых нет требований к аудиту или временны́м запросам, — накапливают эти издержки без выгоды. Event sourcing подходит для агрегатов, где:

- История переходов состояния — бизнес-требование первого класса (аудит, комплаенс, урегулирование споров)
- Нужны временны́е запросы («на дату X»)
- Намерение за каждым изменением состояния нужно сохранять как структурированные данные
- Доменная логика достаточно сложна, чтобы журнал событий служил полным, инспектируемым протоколом бизнес-решений

Для сущности `UserPreference`, хранящей состояние переключателя UI, event sourcing — почти наверняка оверкилл. Знание того, что пользователь двадцать раз переключил тёмную тему, не является бизнес-требованием.

<Quiz
  id="arch-08-01-quiz-3"
  lessonSlug="01-events-as-truth"
  lang="ru"
  question="Биллинговая команда загружает агрегат заказа для обработки команды. У заказа 4200 событий в журнале. Текущая реализация сворачивает все 4200 событий при каждой команде. В чём структурная проблема и каково стандартное её решение в event sourcing?"
  choices={[
    { label: "Структурная проблема в том, что неограниченный рост журнала событий делает свёртку всё дороже — загрузка и применение 4200 событий при каждой команде медленно и будет только хуже. Стандартное решение — снапшот (snapshot): на каком-то порядковом номере N текущее производное состояние сохраняется как снапшот рядом с журналом событий. При следующей загрузке вместо начала с события 1 стартуют с последнего снапшота, затем сворачивают только события после него. Снапшот — не источник истины: журнал событий остаётся авторитетным. Снапшот можно удалить и перестроить из журнала в любое время без потери данных.", correct: true },
    { label: "Структурная проблема в том, что 4200 событий означает слишком большой агрегат. Решение — разбить агрегат на меньшие, у каждого будет меньше событий.", misconception: "Размер агрегата определяется границей согласованности (какие инварианты должны соблюдаться атомарно), а не количеством событий. Долгоживущий заказ в сложной B2B-системе может законно накапливать тысячи событий. Разбиение агрегата для снижения числа событий связывает проектирование агрегата с проблемой производительности, что даёт неправильные границы агрегата. Правильное решение — снапшот, а не реструктуризация агрегата." },
    { label: "Структурная проблема в том, что event sourcing неуместен для этого агрегата. Команде следует перейти на CRUD и использовать event sourcing только для журнала аудита.", misconception: "Проблема производительности свёртки решается без отказа от event sourcing. Снапшоты — стандартное решение для длинных журналов событий. Переход на CRUD теряет структурные преимущества (временны́е запросы, rebuild-by-replay, фиксация намерений), которые оправдывали event sourcing для агрегата заказа." },
    { label: "Структурная проблема в схеме журнала событий. Все события нужно хранить в едином сжатом бинарном blob на агрегат для снижения I/O при свёртке.", misconception: "Хранение событий как бинарного blob устраняет возможность запрашивать отдельные события, фильтровать по типу и воспроизводить с конкретного порядкового номера. Это торгует структурной гибкостью ради незначительной экономии I/O. Снапшоты решают проблему производительности свёртки без ущерба для возможностей журнала." },
  ]}
/>

</Explanation>

<KeyTakeaway>Event sourcing хранит append-only, неизменяемый журнал бизнес-фактов в прошедшем времени. Текущее состояние никогда не хранится напрямую — оно вычисляется свёрткой (fold) журнала событий слева направо. Это делает временны́е запросы и полные трейлы аудита структурными следствиями модели данных. События фиксируют бизнес-намерение и контекст, а не только результирующее изменение данных. Цена — сложность свёртки и необходимость слоя проекций для read-запросов.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-08-01-retrieval"
  lang="ru"
  questions={[
    { q: "Что значит, что текущее состояние — это 'left-fold журнала событий'?", a: "Fold (свёртка, reduce) — функция, обрабатывающая последовательность элементов по одному, накапливая результат. Left-fold начинает с самого левого элемента и применяет функцию: accumulator = f(accumulator, element). В event sourcing fold стартует с пустого/начального состояния агрегата и применяет каждое событие по порядку через функцию applyEvent. После обработки всех событий из журнала аккумулятор содержит текущее состояние. 'Left' означает, что события обрабатываются в порядке добавления — сначала самые старые. Fold детерминирован: одна и та же последовательность событий всегда даёт одно и то же состояние. Именно поэтому журнал событий можно воспроизвести для перестройки любой read model или восстановления любого прошлого состояния." },
    { q: "Почему события в event sourcing используют названия в прошедшем времени (OrderApproved), а не в повелительном (ApproveOrder)?", a: "Названия в прошедшем времени — OrderApproved, LineItemShipped, PaymentReceived — фиксируют, что бизнес-факт произошёл: это неизменяемые исторические записи. Повелительные названия — ApproveOrder, ShipLineItem — описывают команды или намерения: то, что может произойти. События — не команды. События — сохранённый результат успешной обработки команды. Использование прошедшего времени — не просто соглашение: оно подчёркивает, что события нельзя отменить или изъять. Если плохое событие было записано, исправление — это новое компенсирующее событие (OrderApprovalReverted), а не редактирование или удаление существующего." },
    { q: "В чём ключевое структурное различие между event sourcing и добавлением таблицы аудита к CRUD-системе?", a: "Таблица аудита фиксирует мутации данных — какие значения столбцов изменились, с временно́й меткой. Она захватывает after-image каждой операции записи. Event sourcing фиксирует доменные события — бизнес-факты с семантическими названиями и payload, несущим намерение. Структурное различие в том, что находится в центре модели: в CRUD+audit-log нормализованная таблица текущего состояния — источник истины, а журнал аудита — побочный эффект; в event sourcing журнал событий — источник истины, а текущее состояние производно. Это означает: (1) в event sourcing текущее состояние можно удалить и пересчитать в любой момент; (2) названия событий несут бизнес-смысл, которого нет в мутациях таблицы аудита; (3) event sourcing позволяет rebuild-by-replay любой производной модели, а не только исторические запросы." },
  ]}
/>

<Recap lang="ru">
Звонок финансового директора в 21:00 обнажил структурную цену CRUD-модели: каждый UPDATE уничтожал предыдущее состояние. История того, как заказ пришёл к текущему статусу, исчезла. Её реконструкция из логов заняла шесть часов и дала частичный ответ.

Event sourcing решает это структурно, а не как запоздалую доработку. Журнал событий — append-only последовательность неизменяемых бизнес-фактов в прошедшем времени — является источником истины. Текущее состояние вычисляется свёрткой журнала, а не хранится напрямую. Свёртка детерминирована и воспроизводима. Временны́е запросы бесплатны: отфильтруй журнал до нужной временно́й метки и сверни префикс.

Хорошо названные события несут намерение. `OrderApproved` с одобряющим и кодом причины — это не то же самое, что изменение столбца status. Это разница между мутацией данных и бизнес-фактом. Именно это делает журнал событий полным, проверяемым протоколом бизнес-решений, а не просто историей записей строк.

Структурная цена реальна: производительность свёртки деградирует с длинными журналами (решается снапшотами); read-запросы требуют отдельного слоя проекций (рассматривается в уроке 2); эволюция схемы вводит задачи версионирования (рассматриваются в уроке 3). Event sourcing — не правильная модель для каждого агрегата. Это правильная модель, когда история бизнес-решений — требование первого класса.
</Recap>
