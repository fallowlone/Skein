---
slug: "02-read-models-and-projections"
lang: ru
track: "architecture-patterns"
unit: "07-cqrs"
order: 2
title: "Read models и проекции"
summary: "Read model — денормализованный артефакт, построенный проецированием изменений write-стороны. Projection создаёт окно рассинхронизации: read model может устареть. Граница допустимого устаревания — осознанное проектное решение."
estMin: 24
status: ready
lessonType: topic
level: senior
concepts:
  - read-model
  - projection
  - staleness
  - sync-projection
  - async-projection
  - replay
prereqs: []
sources:
  - https://cqrs.files.wordpress.com/2010/11/cqrs_documents.pdf
  - https://martinfowler.com/bliki/CQRS.html
  - https://learn.microsoft.com/en-us/azure/architecture/patterns/cqrs
  - https://udidahan.com/2009/12/09/clarified-cqrs/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import syncVsAsyncProjection from "./infographics/sync-vs-async-projection.svg?raw";

<Hook>
Команда биллинговой платформы разделила write и read модели. Write-сторона — нормализованный aggregate `Order` — была чистой. Read-сторона представляла собой отдельную таблицу `OrderDashboardRow`: одна денормализованная строка на заказ, предварительно объединённая с именем аккаунт-менеджера, последним платёжным событием и признаком просрочки. Запросы дашборда стали быстрыми. Но read model должна была откуда-то браться. Первая попытка команды была простой: после того как каждый command handler коммитил запись, он также напрямую обновлял таблицу `OrderDashboardRow` — в той же транзакции базы данных. Это работало. Согласованность была идеальной. Затем команда заметила, что каждая запись в `Order` — даже та, которая не имела ничего общего с дашбордом — стала занимать больше времени из-за дополнительного обновления `OrderDashboardRow`. Хуже того: когда продуктовая команда добавила вторую read model для отчётности аккаунт-менеджеров, command handlers снова разрослись. А когда добавили третью read model, старший инженер указал, что command handlers теперь несут три ответственности: применение инвариантов заказа, обновление view дашборда и обновление view отчёта по менеджерам. Command handlers запутались в построении read models. Команда сделала шаг назад и спросила: откуда должны браться read models? Ответ — projection: read model строится путём наблюдения за изменениями состояния на write-стороне и их преобразования в форму read model. Механизм projection, а не command handler, несёт ответственность за актуальность read model. Но projection вводит вопрос, которого команда не ожидала: read model может быть ещё не актуальной, когда придёт query. Насколько большое устаревание допустимо и как удержать это окно ограниченным?
</Hook>

<Crux>Read model — проекция write-стороны для queries; projection создаёт окно рассинхронизации — допустимое устаревание задаётся проектом.</Crux>

<Explanation>

## Что такое read model

Read model — это предварительно построенная, денормализованная структура данных, сформированная для конкретного query. Это не write model, просмотренная под другим углом — это **другой артефакт**, построенный из изменений состояния write-стороны и структурированный под конкретную потребность в чтении.

Для дашборда заказов read model может быть таблицей `OrderDashboardRow` с колонками: `order_id`, `customer_name`, `account_manager_name`, `submitted_at`, `total_amount`, `status`, `last_payment_event`, `is_overdue`. Эта строка существует исключительно для обслуживания запроса дашборда. Никакой инвариант в ней не применяется. Никакое бизнес-правило в ней не живёт. Это материализованный ответ на вопрос «что нужно отобразить на дашборде?»

Разные read models обслуживают разные queries. Read model для view пайплайна аккаунт-менеджера имеет другие колонки, чем дашборд. Read model для отчёта о просроченных заказах финансовой команды имеет другую группировку и фильтрацию. Каждая read model формируется для своего конкретного потребителя. Это структурная свобода, которую предоставляет CQRS: чтения больше не вынуждены использовать форму write model.

## Откуда берутся read models: projection

**Projection** — это процесс, транслирующий изменения состояния write-стороны в read model. Когда `Order` подаётся, write-сторона коммитит изменение в нормализованную таблицу `orders`. Projection наблюдает это изменение и обновляет `OrderDashboardRow`: получает имя аккаунт-менеджера, вычисляет итог, устанавливает статус. Теперь read model актуальна.

Projection — это механизм, которому принадлежит трансформация: изменение состояния или событие на write-стороне → обновление read model. Command handlers не обновляют read models. Эта ответственность целиком принадлежит projection.

<FlowDiagram
  label="Write-сторона → read model через projection"
  caption="Command handler мутирует только write model. Projection наблюдает изменение на write-стороне (через доменное событие или database trigger/outbox) и обновляет read model. Query handler читает из read model напрямую. Стрелки показывают направление потока данных: command во write model, изменение состояния наблюдается projection, projection пишет в read model, query читает из read model."
  nodes={[
    { id: "cmd", label: "Command handler", sub: "SubmitOrder, ApproveOrder" },
    { id: "write", label: "Write model", sub: "нормализованная таблица orders, aggregate" },
    { id: "proj", label: "Projection", sub: "наблюдает изменения write, преобразует в форму read" },
    { id: "read", label: "Read model", sub: "OrderDashboardRow, денормализованная" },
    { id: "qry", label: "Query handler", sub: "GetOrderDashboard" },
  ]}
  edges={[
    { from: "cmd", to: "write", label: "мутирует" },
    { from: "write", to: "proj", label: "изменение состояния / событие" },
    { from: "proj", to: "read", label: "обновляет" },
    { from: "qry", to: "read", label: "читает" },
  ]}
/>

Projection может наблюдать изменения write-стороны через разные механизмы:
- **Доменные события**: command handler генерирует событие `OrderSubmitted`; projection подписывается и обрабатывает его.
- **Database triggers или change-data capture**: projection наблюдает за таблицей `orders` напрямую на уровне базы данных.
- **Синхронный in-process вызов**: projection вызывается напрямую после коммита записи, ещё в том же потоке (но в отдельной заботе от command handler).

Выбор механизма определяет характеристики устаревания read model.

## Проблема синхронизации: устаревание

Когда projection работает **асинхронно** — через message bus или фоновый процесс, наблюдающий change-data capture — read model согласована с write-стороной в конечном счёте. Между моментом коммита записи и моментом запуска projection и обновления read model есть окно. В течение этого окна queries видят старые данные.

Это не случайность и не сбой. Это осознанный компромисс. Асинхронная projection отделяет write-путь от построения read model: записи коммитятся быстро; projection работает в фоне без блокировки command handler. Цена — устаревание.

Окно устаревания в здоровой системе обычно составляет миллисекунды — единицы секунд. Но команда должна решить: приемлемо ли это для каждой конкретной read model?

Для дашборда заказов несколько секунд устаревания обычно нормально. Менеджеру по продажам, подавшему заказ, не нужно видеть обновлённый статус на дашборде в том же запросе. Eventual consistency приемлема.

Для read model `CurrentCreditAvailability`, используемой при принятии решения о кредитном лимите во время подачи заказа, устаревание опасно. Если read model отстаёт, второй заказ может пройти проверку кредита по устаревшей цифре доступного кредита. Эта read model, вероятно, вообще не должна быть денормализованной проекцией — она должна быть синхронным запросом к write-стороне.

<Inset kind="why">
Правильный вопрос об устаревании не «как его устранить?», а «какое устаревание допустимо для каждой конкретной read model?» Некоторые read models терпят секунды; некоторые — минуты (еженедельный отчёт); некоторые не терпят никакого лага и не должны быть асинхронными проекциями. Проектирование read models означает явное определение бюджета устаревания для каждой из них, а не повсеместный выбор eventual consistency по умолчанию.
</Inset>

## Синхронная projection: согласованность ценой связанности

Синхронная projection выполняется в рамках той же транзакции, что и запись. Когда `SubmitOrder` коммитит, projection обновляет `OrderDashboardRow` в той же транзакции базы данных. Read model всегда согласована с write-стороной.

Цена: транзакция записи теперь длиннее (она также должна завершить обновление read model), а граница транзакции command handler теперь охватывает заботу read model. Если projection провалится, провалится весь command. Добавление ещё одной read model снова удлиняет транзакцию.

Синхронная projection уместна когда:
- Read model проста и невелика (одна строка, одно быстрое обновление).
- Устаревание read model недопустимо даже на миллисекунды.
- Накладные расходы на дополнительную запись приемлемы.

Она становится проблематичной при наличии многих read models, когда построение read model затратно, или когда требуется независимое развитие write и read сторон.

## Асинхронная projection: отвязанность ценой eventual consistency

Асинхронная projection выполняется после коммита записи — через доменное событие на message bus или через change-data capture. Command handler коммитит только изменение write-стороны. Projection выполняется в отдельном процессе или потоке.

<FlowDiagram
  label="Временна́я шкала async projection: запись коммитится раньше обновления read model"
  caption="Command handler коммитит запись в T=0. Async projection наблюдает событие и обновляет read model в T=Δ, где Δ — лаг projection. Queries между T=0 и T=Δ видят устаревшие данные. Окно устаревания Δ ограничено пропускной способностью и задержкой pipeline projection. Стрелки показывают временну́ю последовательность: коммит записи, распространение события, обновление projection."
  nodes={[
    { id: "t0", label: "T=0: запись коммитится", sub: "событие OrderSubmitted сгенерировано" },
    { id: "bus", label: "Event bus / CDC", sub: "распространяет событие" },
    { id: "tdelta", label: "T=Δ: projection выполняется", sub: "OrderDashboardRow обновлена" },
    { id: "query-stale", label: "Query в T=0..Δ", sub: "видит старую read model (устаревшую)" },
    { id: "query-fresh", label: "Query после T=Δ", sub: "видит обновлённую read model" },
  ]}
  edges={[
    { from: "t0", to: "bus", label: "событие" },
    { from: "bus", to: "tdelta", label: "потреблено" },
    { from: "query-stale", to: "t0", label: "внутри окна" },
    { from: "query-fresh", to: "tdelta", label: "после обновления" },
  ]}
/>

Преимущества: транзакция command handler минимальна и быстра; забота projection отвязана от заботы записи; несколько read models могут обновляться независимыми projection workers без влияния на write-путь; write и read модели могут разворачиваться и масштабироваться независимо.

<Infographic
  svg={syncVsAsyncProjection}
  label="Сравнение синхронной и асинхронной projection по пяти измерениям: синхронная выполняется в транзакции записи, всегда согласована, имеет более медленный write-путь, валит command при сбое projection и удлиняет транзакцию на каждую read model; асинхронная выполняется после коммита записи, eventually consistent с окном устаревания от миллисекунд до единиц секунд, имеет лёгкий быстрый write-путь, отвязана от записи и использует независимые projection workers."
  caption="Одна projection, два механизма: синхронная покупает согласованность, удлиняя транзакцию записи; асинхронная покупает лёгкий, отвязанный write-путь ценой ограниченного окна устаревания."
  hue="var(--d-infra)"
/>

## Перестройка через replay

Одно из самых мощных свойств read model на основе projection: её можно **перестроить с нуля путём replay write-стороны**. Если схема read model изменилась — добавлена колонка `is_overdue`, которой раньше не было — можно удалить read model, воспроизвести всю историческую write-стороны (или перепроецировать из исходной write-таблицы) и перестроить read model с новой колонкой. Никакой миграции данных на write-стороне. Никакой миграции схемы в write-таблице.

Это работает, потому что read model — **производный артефакт**. Она не хранит данных, которых нет на write-стороне. Write-сторона — источник истины; read model — материализованный view над ней. Удаление и перестройка read model ничего не теряет, что нельзя восстановить.

Это свойство становится ещё мощнее, когда write-сторона использует журнал событий как источник истины (event sourcing, рассматривается в юните 08). С журналом событий можно воспроизвести точную последовательность событий, породивших каждый заказ в истории, и построить новую read model с нуля в фоновом задании без простоя.

<Quiz
  id="arch-07-02-quiz-1"
  lessonSlug="02-read-models-and-projections"
  lang="ru"
  question="У команды есть async projection, обновляющая read model дашборда заказов. Менеджер по продукту жалуется: «После того как я одобряю заказ, дашборд ещё несколько секунд показывает его как ожидающий». Инженерный лид отвечает: «Это ожидаемо — read model eventually consistent». Приемлемо ли это, и как должно было приниматься решение?"
  choices={[
    { label: "Зависит от бюджета устаревания конкретной read model. Правильный ответ не «всегда приемлемо» или «никогда не приемлемо» — это проектное решение, которое должно приниматься явно при разработке read model. Для дашборда, просматриваемого менеджером по продукту, проверяющим заказы, несколько секунд eventual consistency почти наверняка нормально: одобрение надёжно записано на write-стороне, а дашборд догонит. Ответ инженерного лида оправдан, если этот компромисс был обдуман и задокументирован. Проблема возникает, если команда никогда не рассматривала вопрос устаревания и просто применила async projection повсюду по умолчанию.", correct: true },
    { label: "Неприемлемо. Пользователь всегда должен видеть немедленный эффект своего действия. Команда должна перейти к синхронной projection.", misconception: "Read-your-own-writes согласованность — это специфическое требование, которое нужно проектировать — не универсальное требование для всех read models. Для многих сценариев с дашбордами несколько секунд лага вполне приемлемы. Перевод всех projections на синхронные убирает преимущество отвязанности CQRS и делает каждую транзакцию записи длиннее. Правильный подход — решать для каждой read model отдельно, приемлемо ли устаревание, а не устранять eventual consistency повсюду из-за того, что один пользователь удивлён." },
    { label: "Приемлемо, потому что eventual consistency — фундаментальное свойство CQRS, и все read models должны быть eventually consistent.", misconception: "CQRS не требует eventual consistency. Синхронная projection — обновление read model в той же транзакции что и запись — это валидный подход CQRS. Eventual consistency возникает из выбора async projection, который является одной из стратегий реализации, а не требованием паттерна CQRS как такового." },
    { label: "Проблему следует решить добавлением механизма инвалидации кеша, немедленно инвалидирующего дашборд после завершения command одобрения.", misconception: "Инвалидация кеша на клиенте может скрыть устаревание для конкретного пользователя, но не решает структурный вопрос о том, каким должен быть бюджет устаревания read model. Это также добавляет сложность, не решая корневого вопроса проектирования. Правильный подход — решить, уместна ли sync или async projection для этой read model, исходя из её требований к устареванию." },
  ]}
/>

<Quiz
  id="arch-07-02-quiz-2"
  lessonSlug="02-read-models-and-projections"
  lang="ru"
  question="Команде нужно добавить новую колонку `days_since_last_contact` в read model дашборда заказов. В традиционной нормализованной схеме это потребовало бы миграции базы данных. Как это обрабатывается иначе в CQRS-системе на основе projection?"
  choices={[
    { label: "В CQRS-системе на основе projection read model — производный артефакт: всё в ней можно пересчитать из write-стороны. Для добавления новой колонки: обновить логику projection для вычисления и заполнения `days_since_last_contact` из данных write-стороны; перестроить read model путём replay write-стороны (или перепроецирования из write-таблицы). Схема write-стороны не изменяется. Никакой write-side миграции не требуется. Новая колонка в read model — это исключительно забота projection: меняется вывод, а не источник истины.", correct: true },
    { label: "Обрабатывается так же. Новая колонка в read model всё равно требует миграции базы данных на таблице read model, и логику projection нужно обновить.", misconception: "DDL-шаг на таблице read model (добавление колонки к определению таблицы) может потребоваться, но это отличается от write-side миграции. Ключевое отличие: read model можно перестроить перепроецированием — все данные для новой колонки могут быть получены из существующей write-стороны без необходимости модифицировать write-схему или выполнять миграции данных в таблице источника истины." },
    { label: "Колонки read model нельзя добавлять после начального развёртывания, потому что у всех существующих строк будут null-значения для новой колонки.", misconception: "У существующих строк будут null-значения только если просто добавить колонку без заполнения. Поскольку read model — это проекция, её можно полностью перестроить путём replay write-стороны, что заполнит все исторические строки вычисленными значениями для новой колонки. Это ключевое преимущество read models на основе projection перед традиционными схемами." },
    { label: "Новую колонку нужно сначала добавить в write model, затем спроецировать в read model.", misconception: "Добавление read-concern колонки в write model связывает обе стороны, именно структурную проблему, которую призван решить CQRS. Write model должна хранить только то, что нужно для применения write-side инвариантов. Данные для read-concern принадлежат read model, производимой projection. Write model не нуждается в изменении." },
  ]}
/>

<Quiz
  id="arch-07-02-quiz-3"
  lessonSlug="02-read-models-and-projections"
  lang="ru"
  question="Старший инженер предлагает: «Мы должны иметь одну read model, которую используют все наши queries. Наличие нескольких read models для разных views — бесполезное дублирование». Каков контраргумент?"
  choices={[
    { label: "Одна read model, оптимизированная для нескольких разных queries, — это исходная проблема, которую решает CQRS. У каждого query разные потребности в данных: дашборд нуждается в имени аккаунт-менеджера и признаке просрочки; финансовый отчёт — в группировке по расчётному периоду; view пайплайна менеджера — в открытых quote по репу. Одна read model, обслуживающая все три, должна нести колонки, нужные одному view, но не нужные другим, использовать форму, являющуюся компромиссом между тремя, и поддерживать join'ы или агрегации, нужные лишь некоторым queries. Результат — read model правильной формы ни для одного из них. Несколько read models, каждая сформированная для своего потребителя, позволяет обслуживать каждый query с оптимальной скоростью без лишних вычислений.", correct: true },
    { label: "Инженер прав. Наличие нескольких read models увеличивает стоимость хранения и количество projections для поддержки. Одна read model проще.", misconception: "Хранилище дёшево. Структурная стоимость одной read model, обслуживающей несколько queries с разными формами, значительно выше: компромисс производительности для каждого query, колонки, существующие для одних views но не для других, и изменения read model, затрагивающие всех потребителей. Стоимость поддержки нескольких projections реальна, но ограничена; структурная деградация общей read model нарастает со временем по мере умножения queries." },
    { label: "Несколько read models оправданы только когда queries выполняются на разных базах данных. Если они используют одну базу данных, достаточно одной read model с views.", misconception: "Несколько read models оправданы структурным различием форм queries, а не физическим размещением базы данных. Database view в той же базе может предоставить другую проекцию, но ограничена SQL-трансформациями — она не может агрегировать события, не может вычислять производные поля с использованием логики приложения и не может быть перестроена путём replay доменных событий. Аргумент за несколько read models структурный, а не топологический." },
    { label: "Несколько read models имеют смысл только если система использует event sourcing, потому что для их построения нужен event replay.", misconception: "Несколько read models можно строить путём projection из любого write-side механизма — реляционной write-таблицы, доменных событий на message bus или журнала событий. Event sourcing (юнит 08) делает перестройку через replay особенно мощной, но несколько read models — это валидный паттерн CQRS вне зависимости от того, использует ли write-сторона event sourcing." },
  ]}
/>

</Explanation>

<KeyTakeaway>Read model — производный денормализованный артефакт, сформированный для конкретного query. Она строится projection, наблюдающей изменения состояния write-стороны. Async projection отвязывает write-путь от построения read model, но вводит окно устаревания; sync projection согласована, но связывает транзакцию записи с обновлениями read model. Каждая read model требует явного бюджета устаревания. Read models можно перестраивать путём replay write-стороны — они материализованные view над write-стороной, не источник истины.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-07-02-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое projection и почему она существует как отдельная забота от command handler?", a: "Projection — это механизм, транслирующий изменения состояния write-стороны в read model. Когда write-сторона коммитит изменение (заказ подан, одобрен, оплачен), projection наблюдает это изменение и обновляет read model для отражения нового состояния. Она существует как отдельная забота от command handler, потому что работа command handler — применять write-side инварианты и коммитить изменения состояния, а не строить read views. Смешение построения read model в command handlers связывает две разные заботы, делая обе сложнее для независимого изменения. По мере роста числа read models command handlers, строящие read models inline, запутываются во всё большем числе view-забот. Projection отделяет эту ответственность." },
    { q: "Что такое окно устаревания в async projection и как им управлять?", a: "Окно устаревания — это время между коммитом изменения на write-стороне и моментом, когда async projection наблюдает это изменение и обновляет read model. В течение этого окна queries видят предыдущую версию read model — старые данные. Управление им означает: (1) явно решать, какое устаревание приемлемо для каждой read model, а не использовать eventual consistency по умолчанию повсюду; (2) для read models, где устаревание недопустимо (например, проверка доступности кредита, используемая в write-пути), использовать синхронную projection или вообще не использовать read model; (3) для read models, где несколько секунд лага приемлемы (дашборд, отчёт), выбирать async projection и сообщать о поведении eventual consistency пользователям. Окно устаревания ограничено пропускной способностью и задержкой pipeline projection — обычно миллисекунды — единицы секунд в здоровой системе." },
    { q: "Почему read model можно перестроить путём replay write-стороны, и когда это ценно?", a: "Read model — производный артефакт: все её данные вычисляются из изменений состояния write-стороны. Она не хранит ничего, чего нет на write-стороне. Поскольку read model полностью производна, удаление и её перестройка из write-стороны не теряет данных, которые нельзя восстановить. Перестройка через replay ценна когда: (1) схема read model изменяется (добавить колонку, изменить группировку) — вместо миграции перестройте read model путём перепроецирования из write-стороны; (2) логика projection содержала ошибку — исправьте логику, перестройте, и read model теперь корректна для всех исторических данных; (3) для нового query нужна новая read model — постройте её из существующей write-стороны без ожидания накопления будущих событий. Когда write-сторона использует журнал событий (event sourcing, юнит 08), перестройка через replay становится особенно мощной, поскольку сохраняется полная последовательность событий и её можно воспроизвести точно." },
  ]}
/>

<Recap lang="ru">
Запутавшиеся command handlers из вступительной истории — обновляющие три read models inline — были не плохой реализацией CQRS. Это была правильная интуиция (держать read models отдельно от write model), воплощённая неправильным механизмом (построение read models в command handlers). Решение — projection: отдельная забота, наблюдающая изменения состояния write-стороны и преобразующая их в обновления read model. Транзакция command handler покрывает только write-сторону.

Projection вводит устаревание. Async projection быстра и отвязана — write-путь лёгкий — но read model отстаёт от write-стороны. Этот лаг почти всегда приемлем для views вроде дашбордов. Он неприемлем для read models, участвующих в write-side принятии решений. Каждая read model требует явного бюджета устаревания.

Read models — производные артефакты. Их можно удалить и перестроить из write-стороны без потери чего-либо. Новые колонки, исправления ошибок, новые view-требования — всё может обрабатываться обновлением логики projection и replay истории. Write-сторона не нуждается в изменении.

Следующий урок рассматривает, когда CQRS избыточен — сложность, которую он вводит, какие случаи его оправдывают, и распространённую ошибку путаницы CQRS с event sourcing.
</Recap>
