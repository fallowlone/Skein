---
slug: "01-command-query-split"
lang: ru
track: "architecture-patterns"
unit: "07-cqrs"
order: 1
title: "Command-query split"
summary: "Модель записи и модель чтения — разные артефакты. CQS на уровне методов, CQRS на уровне архитектуры. Commands выражают намерение и могут быть отклонены; queries не имеют побочных эффектов. Разделение начинается с двух объектных моделей, а не двух баз данных."
estMin: 22
status: ready
lessonType: topic
level: senior
concepts:
  - cqs-principle
  - cqrs
  - command
  - query
  - write-model
  - read-model
prereqs: []
sources:
  - https://cqrs.files.wordpress.com/2010/11/cqrs_documents.pdf
  - https://martinfowler.com/bliki/CQRS.html
  - https://learn.microsoft.com/en-us/azure/architecture/patterns/cqrs
  - https://udidahan.com/2009/12/09/clarified-cqrs/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import modelShapeSplit from "./infographics/model-shape-split.svg?raw";

<Hook>
В доменной области заказов B2B-платформы была единственная модель `Order` — нормализованный, тщательно охраняемый aggregate. Она применяла три инварианта: суммарная стоимость позиций поданного заказа не должна превышать кредитный лимит клиента; заказ нельзя изменить после одобрения; условия оплаты должны соответствовать уровню контракта клиента. Команда гордилась моделью. Каждая запись шла через aggregate root. Инварианты держались. Потом продуктовая команда запросила новый дашборд заказов — экран для клиента, показывающий все заказы компании со статусом, суммой, именем аккаунт-менеджера, последним платёжным событием и признаком просрочки. Запрос затрагивал семь таблиц: `orders`, `line_items`, `customers`, `contracts`, `account_managers`, `payments` и `billing_events`. ORM загружал полный граф aggregate'а для каждого заказа на экране, затем выбрасывал 80% данных при рендере. Время ответа дашборда составило 3,4 секунды. Команда пробовала добавлять индексы, денормализовывать колонки, кешировать ответ. Каждое исправление делало модель записи немного хуже: денормализованные колонки нужно было синхронизировать; логика инвалидации кеша просачивалась в command handlers. Настоящая проблема была структурной: одну модель просили служить двум несовместимым целям. Для записей модель должна быть нормализованной, охраняющей инварианты, aggregate-образной. Для чтений — денормализованной, view-образной, объединённой по многим сущностям. Ни одна модель не может хорошо делать и то, и другое. Это противоречие — одна модель, два несовместимых назначения — и разрешает CQRS.
</Hook>

<Crux>Разделяй write и read модели: commands изменяют состояние через aggregate; queries отвечают из оптимизированной под чтение проекции.</Crux>

<Explanation>

## CQS: принцип за паттерном

CQRS строится на более простой идее Бертрана Мейера — Command-Query Separation (CQS). Принцип гласит: метод должен либо изменять состояние (command), либо возвращать значение (query), но не делать оба одновременно. Метод, который возвращает значение и при этом имеет побочный эффект, нарушает CQS — вызывающий код не может безопасно вызвать его несколько раз, и возвращаемое значение больше нельзя считать чистым.

На уровне объекта:
- `order.submit()` — command, изменяет состояние, ничего не возвращает (или доменное событие)
- `order.getTotal()` — query, читает состояние, возвращает значение, ничего не изменяет

Ценность CQS на уровне объекта — ясность: query-метод можно вызвать без страха побочных эффектов. Вы знаете, что command не вернёт данные, от которых зависит читающий код.

CQRS берёт то же разделение и масштабирует его до **архитектурного уровня**: вместо одного объекта с command-методами и query-методами — две отдельные **модели**: одна для записей, другая для чтений.

<FlowDiagram
  label="CQS на уровне методов vs CQRS на уровне моделей"
  caption="CQS разделяет command- и query-методы на одном объекте. CQRS разделяет их на две отдельные модели. Write model нормализована и охраняет инварианты; read model денормализована и имеет форму view. Стрелки показывают направление потока данных: command через write model, query через read model."
  nodes={[
    { id: "client", label: "Клиент", sub: "выдаёт commands или queries" },
    { id: "cmd", label: "Command handler", sub: "PlaceOrder, SubmitOrder, ApproveOrder" },
    { id: "write", label: "Write model (aggregate)", sub: "нормализованная, охраняет инварианты, транзакционная" },
    { id: "qry", label: "Query handler", sub: "GetOrderDashboard, GetOrderSummary" },
    { id: "read", label: "Read model (projection)", sub: "денормализованная, view-образная, без инвариантов" },
  ]}
  edges={[
    { from: "client", to: "cmd", label: "command" },
    { from: "cmd", to: "write", label: "мутирует" },
    { from: "client", to: "qry", label: "query" },
    { from: "qry", to: "read", label: "читает" },
  ]}
/>

## Почему одна модель не может служить двум целям

Write model формируется инвариантами. Aggregate `Order` нормализован, потому что инварианты должны проверяться над согласованным минимальным набором данных. Если денормализовать — хранить имя аккаунт-менеджера в строке заказа — то каждое изменение имени в `account_managers` должно распространяться на каждый заказ. Aggregate теперь должен защищать два инварианта: бизнес-инварианты о заказах и инвариант ссылочной целостности денормализованных данных. Эти инварианты конфликтуют друг с другом.

Read model формируется view. Запрос дашборда нуждается в имени аккаунт-менеджера, последнем платёжном событии и признаке просрочки в одном быстром обращении. Никакой инвариант эти поля не охраняет — они просто должны быть корректными и быстрыми. Правильная структура — предварительно объединённая, денормализованная строка, которую query может вернуть напрямую без загрузки полного графа aggregate'а.

Структурная несовместимость реальна: нормализация служит записям; денормализация служит чтениям. Когда их заставляют сосуществовать в одной модели, каждая деградирует другую.

<Infographic
  svg={modelShapeSplit}
  label="Сравнение модели записи и модели чтения. Модель записи нормализована, охраняет инварианты, имеет форму агрегата и транзакционна, обрабатывает команды. Модель чтения денормализована, имеет форму представления, без инвариантов, отвечает на запросы."
  caption="У двух задач противоположные структурные требования — нормализация для записи, денормализация для чтения — именно поэтому CQRS даёт каждой свою модель."
  hue="var(--d-infra)"
/>

<Inset kind="why">
Это прежде всего не оптимизация производительности. Это наблюдение о моделировании. Нормализованный aggregate — правильная форма для транзакционного применения инвариантов. Денормализованный view — правильная форма для эффективных ответов на вопросы. Попытка заставить одну структуру служить обоим назначениям означает, что она не имеет правильной формы ни для одного из них. CQRS — это не «добавь read-реплику»: это решение о моделировании, которое говорит, что забота о записях и забота о чтениях имеют разные структурные требования и должны моделироваться отдельно.
</Inset>

## Commands: намерение, а не CRUD

Command в CQRS — это не обновление записи. Это выражение **намерения** внешнего мира по отношению к домену. `SubmitOrder` — это command. `ApproveOrderForShipping` — это command. `RejectLineItemNegotiation` — это command. Эти имена берутся из языка домена (см. юнит 06 по DDD и ubiquitous language).

Commands могут быть **отклонены**. Когда приходит command `SubmitOrder` и сумма заказа превышает кредитный лимит клиента, write model отклоняет его. Command не провалился молча — он был оценён по инварианту и явно отказан. Command handler возвращает доменное событие при успехе или доменную ошибку при отклонении.

Это отличается от `PATCH /orders/:id`. PATCH ничего не говорит о намерении. Command говорит точно, что актор намерен сделать, и почему домен может отказать.

Commands также делают ответственность write model яснее. Write model обрабатывает commands. Она не отвечает на queries. Её работа — получить выражение намерения, проверить инварианты, мутировать состояние если инварианты выполнены, и сгенерировать события. Это вся работа.

## Queries: без побочных эффектов, view-образные

Query в CQRS — это вопрос. `GetOrderDashboard(customerId)`, `GetOrderSummaryForApproval(orderId)`, `ListOverdueOrdersForAccountManager(managerId)` — queries. Они возвращают данные. Они не изменяют состояние. Их можно безопасно вызывать несколько раз.

Поскольку queries не имеют побочных эффектов, им не нужно проходить через инвариантный механизм write model. Они читают из read model напрямую — предварительно построенной, view-образной структуры данных, оптимизированной для конкретного задаваемого вопроса.

Разные queries могут иметь разные read models. Запрос дашборда и запрос сводки для одобрения обслуживают разные view с разными потребностями в данных. CQRS позволяет каждому query опираться на наиболее эффективную read model для его конкретной формы.

<Inset kind="note">
Отдельные модели не означают отдельные базы данных — по крайней мере, не изначально. В простейшей реализации CQRS write model и read model — это два разных в-процессных графа объектов, читающих из одной базы данных. Write model читает нормализованные строки и применяет инварианты; read model выполняет другой, оптимизированный под view запрос к той же базе данных, возможно используя database view или запрос, возвращающий плоский DTO. Отдельные базы данных — это **необязательный инфраструктурный шаг**, актуальный когда требования к масштабированию или задержке оправдывают его. Многие команды применяют CQRS на уровне модели и никогда не доходят до точки, где нужны отдельные хранилища.
</Inset>

<Quiz
  id="arch-07-01-quiz-1"
  lessonSlug="01-command-query-split"
  lang="ru"
  question="Разработчик говорит: «Мы реализовали CQRS, добавив read-реплику базы данных. Commands идут на primary, queries — на реплику». Применён ли CQRS корректно?"
  choices={[
    { label: "Нет. Добавление read-реплики — это решение о топологии репликации, а не разделение модели CQRS. CQRS означает, что забота о записях и забота о чтениях смоделированы отдельно — два различных графа объектов с разными формами. Если read model на реплике — тот же нормализованный aggregate, что и на write-стороне, только на другом сервере, структурная проблема не изменилась: чтения по-прежнему загружают полный граф aggregate'а и выбрасывают большую часть данных. CQRS начинается с моделирования read-стороны иначе — как денормализованных, view-образных проекций — независимо от того, на одном или разных серверах физическое хранилище.", correct: true },
    { label: "Да. Маршрутизация commands на primary, а queries на реплику — это именно то, что предписывает CQRS.", misconception: "CQRS — это паттерн моделирования, а не инфраструктурная топология. Маршрутизация на read-реплику не меняет модель. Write model и read model в CQRS — это два разных по форме графа объектов с разными структурными требованиями, а не одна и та же модель на двух физических серверах." },
    { label: "Частично. Инфраструктура правильная, но команде также нужно добавить event bus между primary и репликой.", misconception: "Инфраструктурное разделение не имеет значения для того, применён ли CQRS. CQRS — это иметь две отдельные модели: одну для записей, другую для чтений. Event bus — один из механизмов синхронизации отдельно хранящейся read model с write-стороной, но он нужен только если вы выбираете хранить read model отдельно. Основная проблема здесь в том, что форма модели не была разделена." },
    { label: "Да, потому что записи и чтения теперь идут на разные серверы, что удовлетворяет разделению command/query.", misconception: "Разделение серверов не то же самое, что разделение моделей. Command и query могут идти на разные серверы, используя при этом одну и ту же форму модели на обоих. CQRS требует разных форм моделей, а не разных серверов." },
  ]}
/>

<Quiz
  id="arch-07-01-quiz-2"
  lessonSlug="01-command-query-split"
  lang="ru"
  question="В принципе CQS метод, возвращающий значение И изменяющий состояние, нарушает CQS. Почему это структурная проблема, а не просто стилистическое предпочтение?"
  choices={[
    { label: "Метод, возвращающий значение и изменяющий состояние, нельзя безопасно вызывать несколько раз. Второй вызов вернёт другое значение (первый вызов уже мутировал состояние) или уже исчерпал побочный эффект (например, извлёк элемент из очереди). Вызывающий код не может рассуждать о возвращаемом значении изолированно — он должен знать, в каком состоянии метод оставляет систему. Это создаёт скрытую временну́ю связь: возвращаемое значение корректно только при первом вызове в данной последовательности. Разделение commands и queries устраняет это: queries ссылочно прозрачны (безопасно вызывать любое количество раз), commands явно изменяют состояние (вызывающий знает, что значения ожидать не стоит). Ясность структурна, а не стилистична.", correct: true },
    { label: "Это нарушает CQS, потому что усложняет именование метода — нельзя назвать его одновременно как глагол (command) и как существительное (query).", misconception: "Сложность именования — симптом структурной проблемы, а не причина. Структурная проблема — временна́я связь и невозможность безопасно вызвать метод несколько раз. Сложность именования возникает потому, что метод делает две вещи; настоящий вопрос — что это делает с вызывающим кодом." },
    { label: "Это лишь стилистическое предпочтение. CQS — это руководство, и смешение command и query в одном методе допустимо, когда упрощает API.", misconception: "CQS — это принцип проектирования со структурными последствиями. Метод, и возвращающий значение, и мутирующий состояние, создаёт скрытую временну́ю связь. Вызывающий код не может безопасно вызвать его дважды, не может кешировать возвращаемое значение, не может рассуждать о нём без понимания побочного эффекта. Это структурные проблемы корректности, а не стилистические предпочтения." },
    { label: "Это проблема только в конкурентных системах, потому что конкурентные вызывающие могут наблюдать разные возвращаемые значения.", misconception: "Проблема существует и в однопоточном коде. Даже без конкурентности вызов метода, возвращающего значение и мутирующего состояние, дважды возвращает разные значения для одного и того же кажущегося входного состояния. Структурная проблема в том, что возвращаемое значение зависит от порядка вызовов — это форма временно́й связи вне зависимости от многопоточности." },
  ]}
/>

<Quiz
  id="arch-07-01-quiz-3"
  lessonSlug="01-command-query-split"
  lang="ru"
  question="Команда применяет CQRS к домену заказов. Младший инженер предлагает: «Command handler SubmitOrder должен возвращать полный Order DTO, чтобы UI мог сразу обновить экран». Каков CQRS-ориентированный ответ?"
  choices={[
    { label: "Возврат полного Order DTO из command handler размывает границу command/query. Работа command'а — изменить состояние и сгенерировать доменное событие; возврат view-образного DTO делает command handler также ответственным за построение результатов запроса, связывая его с формой read model. CQRS-ориентированный подход: command handler возвращает либо ничего (void), либо минимальное подтверждение (например, новый orderId или сигнал успеха/неудачи). UI затем выдаёт query к read model для получения обновлённого view. Это сохраняет write-сторону и read-сторону чисто разделёнными. Краткий дополнительный round-trip приемлем — read model обычно быстра, потому что денормализована и оптимизирована под view.", correct: true },
    { label: "Предложение нормально. Возврат DTO из command — это просто оптимизация, избегающая лишнего round-trip, и CQRS этого не запрещает.", misconception: "Хотя прагматичные компромиссы существуют в реальных системах, структурная стоимость реальна: command handler теперь связывает себя с формой read model. Изменения read model (добавление поля, изменение join) требуют изменений command handler'а. Command handler взял на себя вторую ответственность. В CQRS-системе, спроектированной для сопровождаемости, разделение стоит дополнительного round-trip." },
    { label: "Предложение неверно, потому что command handlers всегда должны возвращать void — возврат чего-либо из command нарушает CQS.", misconception: "Возврат минимального подтверждения (orderId, флага успеха/ошибки, доменного события) из command handler не является нарушением CQS — это не результаты запроса. Нарушения CQS происходят когда метод возвращает зависящие от состояния данные и одновременно изменяет состояние. Возврат свежесгенерированного идентификатора или токена подтверждения — распространённое прагматичное исключение, не связывающее command handler с формой read model." },
    { label: "Предложение верно, потому что в CQRS write model всегда должна возвращать текущее состояние после command, чтобы read model могла обновиться.", misconception: "Работа write model после command — сгенерировать доменные события, а не возвращать view-образные данные. Read model обновляется проецируя из этих доменных событий — не получая DTO от command handler. Возврат текущего состояния из command handler связывает write и read стороны в неправильном направлении." },
  ]}
/>

</Explanation>

<KeyTakeaway>CQRS разделяет write model (нормализованную, охраняющую инварианты, aggregate-образную) и read model (денормализованную, view-образную, оптимизированную под запросы). Commands выражают намерение по отношению к домену и могут быть отклонены; queries отвечают на вопросы без побочных эффектов. Разделение — это решение о моделировании, два графа объектов с разными структурами. Отдельные базы данных — необязательная инфраструктура, а не определение CQRS.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-07-01-retrieval"
  lang="ru"
  questions={[
    { q: "Какова структурная несовместимость, из-за которой одна модель плохо служит и записям, и чтениям?", a: "Write model должна быть нормализованной, чтобы инварианты можно было проверять над минимальным согласованным набором данных без поддержки денормализованной избыточности. Read model должна быть денормализованной, чтобы queries могли возвращать view-образные данные в одном быстром обращении без загрузки полного графа aggregate'а. Нормализация и денормализация структурно противоположны. Когда их заставляют сосуществовать в одной модели, каждая деградирует другую: денормализованные колонки должны синхронизироваться с записями (усложняя command handlers), нормализованные чтения вынуждены делать множество join'ов и загружать данные, которые выбрасываются (деградируя производительность запросов). CQRS разрешает несовместимость, позволяя каждой стороне иметь нужную ей форму." },
    { q: "В чём разница между command и query в CQRS, и почему commands могут быть отклонены?", a: "Command — это выражение намерения по отношению к домену: SubmitOrder, ApproveLineItem, RejectNegotiation. Он изменяет состояние и ничего не возвращает (или минимальное подтверждение). Commands могут быть отклонены, потому что write model проверяет инварианты перед применением изменения; если инвариант не выполняется (например, сумма заказа превышает кредитный лимит), command отклоняется. Query — это вопрос: GetOrderDashboard, ListOverdueOrders. Он читает из read model, возвращает данные и не имеет побочных эффектов. Queries нельзя «отклонить» в том же смысле — они либо возвращают данные, либо возвращают пустые результаты. Ключевое различие: commands оцениваются по бизнес-правилам и могут провалиться; queries просто читают." },
    { q: "Требует ли CQRS отдельных баз данных для write- и read-сторон?", a: "Нет. Отдельные базы данных — необязательная инфраструктура, а не определение CQRS. В простейшей реализации CQRS write model и read model — два разных в-процессных графа объектов, читающих из одной базы данных: write model загружает нормализованные строки и применяет инварианты; read model выполняет другой, оптимизированный под view запрос (возможно, database view или запрос с плоским DTO) к той же базе данных. Отдельные хранилища становятся актуальны когда требования к масштабированию или задержке оправдывают их. Многие команды применяют CQRS на уровне модели и никогда не нуждаются в отдельных хранилищах." },
  ]}
/>

<Recap lang="ru">
3,4-секундный дашборд из вступительной истории — это не проблема отсутствующего индекса. Это несоответствие моделирования: один aggregate, сформированный для охраны инвариантов, использовался для обслуживания view, которому нужны были денормализованные данные из семи таблиц. Структурное противоречие между нормализацией (записи нуждаются в ней) и денормализацией (чтения нуждаются в ней) — это то, что CQRS называет и разрешает.

CQS — принцип Мейера — разделяет command- и query-методы на одном объекте. CQRS масштабирует это разделение до архитектурного уровня: две отдельные модели, по одной для каждой заботы. Write model нормализована, охраняет инварианты, aggregate-образна. Read model денормализована, view-образна, оптимизирована под запросы. Commands выражают намерение и могут быть отклонены. Queries отвечают на вопросы и не имеют побочных эффектов.

Разделение — это решение о моделировании, а не развёртывание инфраструктуры. Отдельные базы данных необязательны. Команда может реализовать CQRS двумя графами объектов, читающими одну базу данных, и получить большую часть структурной выгоды.

Следующий урок рассматривает read-сторону подробно: как денормализованные read models строятся путём проецирования с write-стороны, какую проблему синхронизации это создаёт и как ограничить устаревание данных.
</Recap>
