---
slug: "03-when-cqrs-is-overkill"
lang: ru
track: "architecture-patterns"
unit: "07-cqrs"
order: 3
title: "Когда CQRS избыточен"
summary: "CQRS покупает структурное разделение за реальную цену: две модели, eventual consistency, инфраструктура projection. Оправдан только там, где асимметрия записи и чтения реальна. Большинство CRUD-экранов не подходит. CQRS и event sourcing — независимые паттерны."
estMin: 20
status: ready
lessonType: topic
level: senior
concepts:
  - cqrs-cost
  - partial-cqrs
  - crud
  - cqrs-vs-event-sourcing
  - read-write-asymmetry
  - eventual-consistency
prereqs: []
sources:
  - https://martinfowler.com/bliki/CQRS.html
  - https://cqrs.files.wordpress.com/2010/11/cqrs_documents.pdf
  - https://udidahan.com/2009/12/09/clarified-cqrs/
  - https://learn.microsoft.com/en-us/azure/architecture/patterns/cqrs
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
После того как команда биллинговой платформы внедрила CQRS для домена заказов, паттерн стал стандартным ответом команды. Модуль управления аккаунтами нуждался в экране редактирования данных компании — название, адрес, контакт по выставлению счетов. Разработчик предложил разбить на write model `CompanyAggregate` и read model `CompanyProfileView` с async projection. Технический лид возразил: «Какой инвариант охраняет write model? Какая асимметрия записи и чтения оправдывает projection?» Разработчик задумался. Экран редактирования компании не имел сложных инвариантов. У компании было название, адрес и контакт по выставлению счетов — три поля, одна таблица, одна форма. Записям и чтениям нужны были в точности одни и те же данные. Не было никаких проблем с производительностью при чтении нормализованной строки напрямую. Projection добавила бы асинхронный worker, доменное событие, отдельную таблицу read model и eventual consistency — для экрана, который пользователь редактирует раз в месяц. Вердикт технического лида: это CRUD. CQRS здесь — случайная сложность, а не структурная ясность. У той же команды была противоположная проблема в рабочем процессе одобрения заказов: там не было CQRS, была одна нормализованная модель `Order` на всё, и дашборд одобрения работал мучительно медленно из-за join по семи таблицам для каждой строки. Правильный ответ — не «всегда CQRS» и не «никогда CQRS». Это распознавание тех частей системы, где асимметрия записи и чтения реальна, и применение паттерна только там.
</Hook>

<Crux>Применяй CQRS там, где асимметрия записи и чтения реальна и инварианты оправдывают отдельную write model — не как умолчание. Большинство CRUD это не оправдывает.</Crux>

<Explanation>

## Реальная стоимость CQRS

CQRS не бесплатен. Прежде чем применять его, команда должна чётко понимать, что он вводит:

**Две модели для проектирования, построения и поддержки.** Каждое изменение write-стороны должно быть распространено на одну или несколько read models. Каждая новая read model — это новая projection для написания, тестирования и эксплуатации. Каждое изменение write-схемы должно оцениваться на предмет влияния на каждую projection.

**Eventual consistency там, где её не было.** Async projection означает, что read model может устареть. Это вопрос корректности, о котором нужно явно сообщать, управлять и проектировать. Пользователи видят не самые актуальные данные. Некоторые queries, которые раньше были строго согласованными, теперь eventually consistent.

**Инфраструктура projection.** Async projection требует механизма наблюдения за изменениями write-стороны: message bus с доменными событиями, change-data capture или паттерн outbox. Каждый из них имеет операционные накладные расходы — развёртывания, мониторинг, обработку сбоев, очереди недоставленных сообщений.

**Более сложная отладка.** Когда пользователь спрашивает «почему мой дашборд показывает неверный статус», ответ может затрагивать write model, pipeline projection, read model и окно eventual consistency. Цепочка причинности длиннее и сложнее для трассировки.

Эти затраты стоят того, когда структурная выгода реальна. Они — потери, когда применяются к проблемам, не имеющим подлинной асимметрии записи и чтения.

<FlowDiagram
  label="Стоимость vs выгода CQRS: где компромисс благоприятен для паттерна"
  caption="CQRS окупается когда сложность инвариантов write model и сложность queries read model тянут в структурно несовместимых направлениях. Там где обе просты и симметричны — затраты перевешивают выгоды. Стрелки показывают путь решения: сначала оцени асимметрию записи/чтения, затем структурную несовместимость, затем применяй или отклоняй CQRS."
  nodes={[
    { id: "assess", label: "Оцени домен", sub: "Есть ли в этой части реальная асимметрия записи/чтения?" },
    { id: "yes-asym", label: "Да: асимметрия реальна", sub: "сложные инварианты в записях, сложные join'ы в чтениях" },
    { id: "no-asym", label: "Нет: симметричный CRUD", sub: "одинаковые данные, одинаковая форма, простая форма" },
    { id: "apply", label: "Применяй CQRS локально", sub: "две модели, projection, eventual consistency" },
    { id: "skip", label: "Пропусти CQRS", sub: "одна модель, нормализованная, прямые чтения" },
  ]}
  edges={[
    { from: "assess", to: "yes-asym", label: "асимметрично" },
    { from: "assess", to: "no-asym", label: "симметрично" },
    { from: "yes-asym", to: "apply", label: "оправдано" },
    { from: "no-asym", to: "skip", label: "избыточно" },
  ]}
/>

## Большинство CRUD не требует CQRS

Значительная часть экранов приложений симметрична: те же поля, записанные в базу данных, отображаются обратно. Форма адреса записывает название, улицу, город и почтовый индекс. Экран редактирования читает те же четыре поля. Экран списка показывает те же поля с фильтром. Никакой инвариант поля адреса не охраняет. Никакой сложный join не нужен. Никакой структурной несовместимости записи и чтения не существует.

CQRS для этого экрана добавляет две модели, projection и eventual consistency за ноль структурной выгоды. Write model была бы `CompanyAddress`. Read model — `CompanyAddressView`. Projection копировала бы четыре поля из write-таблицы в read-таблицу. Это не архитектура — это церемония.

Эвристика: **если данные, которые экран записывает, и данные, которые он читает, имеют одинаковую форму и одинаковые поля, CQRS добавляет сложность без структурной выгоды.**

Для биллинговой платформы:
- Редактирование профиля компании (название, адрес, контакт) — CRUD, без CQRS
- Настройки аккаунта пользователя (email, часовой пояс, уведомления) — CRUD, без CQRS
- Дашборд одобрения заказов (join по семи таблицам, агрегация статусов, флаги просрочки) — реальная асимметрия, CQRS оправдан
- Подача заказа (инвариант кредитного лимита, инварианты позиций, переходы статусов) — сложные write-инварианты, CQRS оправдан

<Inset kind="why">
Вопрос, который нужно задать перед применением CQRS, не «можем ли мы разделить это на commands и queries?» Ответ на него всегда да — любой домен можно разделить. Вопрос: «имеют ли забота о записях и забота о чтениях структурно несовместимые требования, оправдывающие поддержку двух отдельных моделей?» Если ответ да — CQRS разрешает реальное структурное противоречие. Если нет — CQRS это архитектурное позолачение.
</Inset>

## Частичный и локальный CQRS

CQRS не обязан применяться ко всему приложению. Правильная область — **bounded context** или **доменная область**, где асимметрия записи и чтения действительно существует.

На биллинговой платформе домен управления заказами имеет реальную асимметрию: write-сторона имеет сложные инварианты (кредитный лимит, рабочий процесс одобрения, валидация позиций), а read-сторона имеет сложные queries (дашборд, пайплайн менеджера, финансовый отчёт). CQRS там оправдан.

Домен управления аккаунтами по большей части CRUD. Применение CQRS к управлению аккаунтами ради «согласованности с доменом заказов» — это карго-культ архитектуры: копирование паттерна без копирования сил, его оправдывающих.

Правильный ответ: применять CQRS локально к домену управления заказами. Дать домену управления аккаунтами использовать простую нормализованную модель с прямыми чтениями. Двум доменам не нужно быть архитектурно согласованными друг с другом — им нужно соответствовать своим собственным силам.

Это называется **локальный CQRS** или **частичный CQRS**: CQRS применяется избирательно там, где присутствует структурная выгода, а не равномерно по всей системе.

## CQRS — это не event sourcing

Самое распространённое заблуждение о CQRS — путать его с event sourcing. Это независимые паттерны, которые хорошо дополняют друг друга. Их путаница удваивает стоимость внедрения.

**CQRS** разделяет write model и read model. Write-сторона хранит текущее состояние в нормализованной реляционной таблице. Read-сторона хранит денормализованные views. Projection читает write-таблицу напрямую или наблюдает доменные события.

**Event sourcing** (рассматривается в юните 08) хранит состояние как append-only последовательность событий, а не как текущее состояние. Текущее состояние вычисляется воспроизведением журнала событий. Event sourcing — это другая стратегия хранения write-стороны, независимая от того, как обслуживаются чтения.

Можно иметь CQRS без event sourcing: write-сторона — нормализованная реляционная таблица, read-сторона — денормализованная проекция этой таблицы. Это наиболее распространённая форма CQRS в production.

Можно иметь event sourcing без CQRS: сервис использует журнал событий как write-хранилище, но обслуживает чтения напрямую из журнала воспроизведением по запросу — без отдельных read models.

Можно иметь оба: write-сторона — журнал событий; read models строятся путём проецирования журнала событий. Эта комбинация мощная, но несёт стоимость обоих паттернов.

<Inset kind="note">
Команды, впервые изучающие CQRS, часто встречают его в контексте event sourcing и предполагают, что они всегда идут вместе. Затем они решают, что не могут принять CQRS без принятия event sourcing, что поднимает стоимость внедрения до точки, где ни один не принимается. Реальность: CQRS с реляционной write model и projection, читающей write-таблицу, — это полностью валидная, малозатратная стартовая точка. Event sourcing — это необязательное и значительное дополнительное обязательство.
</Inset>

<Quiz
  id="arch-07-03-quiz-1"
  lessonSlug="03-when-cqrs-is-overkill"
  lang="ru"
  question="Команда строит систему с тремя доменами: Order (сложный рабочий процесс одобрения, инварианты кредитного лимита, multi-table дашборд), Product Catalog (название, описание, цена, изображение — простой CRUD, отображается как хранится), и Notifications (пользовательские предпочтения для email/SMS, переключаются на странице настроек). Для каких доменов следует использовать CQRS?"
  choices={[
    { label: "Только для Order. Домен Order имеет реальную асимметрию записи/чтения: сложные write-инварианты (кредитный лимит, рабочий процесс одобрения) и сложные чтения (multi-table дашборд). Product Catalog и Notifications — симметричный CRUD: те же данные, записанные, читаются обратно. Применение CQRS к ним добавляет две модели, projections и eventual consistency за ноль структурной выгоды.", correct: true },
    { label: "Ко всем трём. Согласованность по всей системе важна, и использование разных паттернов в разных доменах создаёт сложность поддержки.", misconception: "Равномерное применение CQRS ради согласованности — это карго-культ архитектуры. Паттерн должен следовать за силами — CQRS имеет смысл там, где асимметрия записи/чтения его оправдывает. Принуждение к нему симметричных CRUD-доменов добавляет случайную сложность. Сложность поддержки смешанных паттернов значительно ниже, чем сложность CQRS, применённого к доменам, которым он не нужен." },
    { label: "Order и Product Catalog. Product Catalog имеет read-heavy паттерн доступа (много просматривающих пользователей), поэтому отдельная read model улучшила бы производительность.", misconception: "Read-heavy доступ сам по себе недостаточное оправдание для CQRS. Если read model и write model имеют одинаковую форму — название продукта, описание, цена, изображение, читаемые именно как записаны — структурной выгоды от отдельной read model нет. Уровень кеша или CDN решит read-heavy нагрузку без структурной стоимости CQRS. CQRS оправдывается структурной несовместимостью между записями и чтениями, а не объёмом чтений." },
    { label: "Ни для одного. CQRS слишком сложен для любой production-системы без выделенной платформенной инженерии для его поддержки.", misconception: "CQRS широко используется в production-системах там, где асимметрия записи/чтения его оправдывает. Вопрос не в том, уместен ли CQRS вообще — а в том, уместен ли он для конкретного домена. Домен Order в этой системе имеет реальную асимметрию и оправдывает CQRS. Обеспокоенность сложностью поддержки реальна, но это аргумент за избирательное применение CQRS, а не за его полное отклонение." },
  ]}
/>

<Quiz
  id="arch-07-03-quiz-2"
  lessonSlug="03-when-cqrs-is-overkill"
  lang="ru"
  question="Команда хочет принять CQRS для домена заказов. Инженер говорит: «Мы не можем принять CQRS без event sourcing — они идут вместе». Верно ли это?"
  choices={[
    { label: "Нет. CQRS и event sourcing — независимые паттерны. CQRS разделяет write model и read model — write-сторона может быть нормализованной реляционной таблицей; read-сторона — денормализованной проекцией. Event sourcing заменяет стратегию хранения write-стороны на append-only журнал событий. CQRS можно применять с реляционной write model и никогда не использовать event sourcing. Два паттерна хорошо дополняют друг друга при совместном использовании, но их комбинирование удваивает стоимость внедрения и сложность. Начало с CQRS на реляционной write model — валидная, менее затратная стартовая точка.", correct: true },
    { label: "Да. CQRS требует журнал событий на write-стороне, чтобы projections могли воспроизводить историю для перестройки read models.", misconception: "Перестройка read model через replay возможна с event sourcing и без него. Projection может воспроизводить, повторно читая write-side реляционную таблицу с нуля. Event sourcing делает перестройку через replay мощнее (поскольку сохраняется полная история событий), но он не является требованием CQRS. Projections могут строиться из любого механизма, наблюдающего изменения write-стороны — реляционной таблицы, доменных событий на bus или журнала событий." },
    { label: "Да. Без event sourcing нет способа синхронизировать read model с write model.", misconception: "Синхронизация read model не требует event sourcing. Синхронная projection обновляет read model в той же транзакции что и запись. Async projection наблюдает изменения write-стороны через доменные события на message bus или change-data capture — ничего из этого не требует использования журнала событий на write-стороне. Event sourcing — один из источников событий изменений write-стороны; доменное событие, публикуемое command handler'ом, — другой." },
    { label: "Частично. CQRS можно принять без event sourcing, но теряется возможность перестройки read models.", misconception: "Read models можно перестраивать без event sourcing. Projection может перестраиваться повторным чтением write-side реляционной таблицы и повторным запуском логики трансформации по всем существующим строкам. Event sourcing делает перестройку богаче (он сохраняет последовательность и точное состояние каждого исторического изменения), но базовая перестройка перепроецированием из реляционного источника доступна без него." },
  ]}
/>

<Quiz
  id="arch-07-03-quiz-3"
  lessonSlug="03-when-cqrs-is-overkill"
  lang="ru"
  question="Спустя шесть месяцев после применения CQRS к домену заказов, команда обнаружила, что новый инженер потратил неделю на отладку бага, при котором дашборд заказов показывал неверный статус. Корневая причина: в projection был баг, и для некоторых заказов read model содержала устаревшие данные. Технический лид спрашивает: «Был ли CQRS неправильным выбором?» Как следует это оценивать?"
  choices={[
    { label: "Сложность отладки — известная стоимость CQRS с async projection, а не свидетельство неправильного выбора. Правильная оценка: стоила ли структурная выгода (чистая write model, быстрый дашборд, независимая развиваемость) операционных затрат (баги projection, отладка eventual consistency)? Если производительность дашборда и ясность write-стороны домена заказов были реальными приобретениями, и команда имеет инструментарий и дисциплину для надёжной эксплуатации projections — CQRS остаётся оправданным. Если команда стабильно борется с багами projection и исходная проблема производительности могла быть решена хорошо спроектированным SQL view — следует пересмотреть. Правильный вопрос не «встретили ли мы баг», а «правильный ли структурный компромисс для нашего контекста?»", correct: true },
    { label: "Да, CQRS был неправильным выбором. Баг, занимающий неделю отладки, указывает на то, что система слишком сложна. Следует вернуться к единой модели.", misconception: "Все архитектурные паттерны вводят новые режимы отказов. Подход с единой моделью имел бы другие баги (конкуренция за блокировки, производительность запросов, нарушения инвариантов), которые также могли занять неделю отладки. Вопрос не в том, ввёл ли CQRS новый режим отказа — он ввёл — а в том, приемлемы ли режимы отказов и операционные затраты с учётом структурных выгод. Откат к единой модели из-за одного инцидента отладки отбрасывает структурные выгоды без оценки полного компромисса." },
    { label: "CQRS был верным, и решение — добавить больше мониторинга pipeline projection для более раннего обнаружения устаревших данных.", misconception: "Улучшенный мониторинг — хорошее операционное улучшение, но оно не решает вопрос оценки. Команда должна оценить, оправдан ли структурный компромисс — а не просто патчить операционную сторону. Улучшенный мониторинг — часть ответственной эксплуатации CQRS, но вопрос правильности выбора CQRS для этого домена требует оценки обеих сторон компромисса, а не только улучшения операций." },
    { label: "CQRS был неправильным, потому что eventual consistency никогда не должна быть приемлемой в финансовой системе.", misconception: "Финансовые системы широко используют eventual consistency — сверка платежей, обновление балансов счетов, pipeline отчётности. Приемлемость eventual consistency зависит от конкретных данных и конкретной операции, а не от отрасли. View дашборда, показывающий немного устаревший статус заказа в течение нескольких секунд, — это совсем другое, чем решение о проведении платежа. Команда должна оценивать приемлемость устаревания для каждой read model, а не отвергать eventual consistency категорически." },
  ]}
/>

</Explanation>

<KeyTakeaway>CQRS вводит две модели, eventual consistency и инфраструктуру projection — реальные затраты, окупающиеся только там, где асимметрия записи и чтения реальна. Большинство CRUD-экранов симметричны и не оправдывают разделения. Применяй CQRS локально к доменным областям, где он оправдан; пусть другие домены остаются простыми. CQRS и event sourcing — независимые паттерны: можно иметь один без другого, и их путаница удваивает стоимость внедрения без необходимости.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-07-03-retrieval"
  lang="ru"
  questions={[
    { q: "Каковы основные затраты, которые вводит CQRS, и когда они окупаются?", a: "CQRS вводит: (1) две модели для проектирования, построения и поддержки — каждое изменение write-стороны должно отражаться в read models через projections; (2) eventual consistency там, где её не было — async projections означают, что read model может устареть, что нужно сообщать пользователям и управлять операционно; (3) инфраструктуру projection — message bus, change-data capture или паттерн outbox со своими операционными накладными расходами; (4) более длинные цепочки отладки — неверное значение на дашборде может трассироваться через write model, pipeline projection, read model и окно устаревания. Эти затраты окупаются когда: write-сторона имеет сложные инварианты, выигрывающие от чистого нормализованного aggregate; read-сторона имеет сложные queries, выигрывающие от денормализованных проекций; и две формы структурно несовместимы. Там где записям и чтениям нужны одни и те же данные в одной форме — CQRS это случайная сложность." },
    { q: "Что такое частичный/локальный CQRS и почему он предпочтительнее равномерного применения CQRS?", a: "Частичный или локальный CQRS — это применение паттерна только к доменным областям, где существует реальная асимметрия записи и чтения, при сохранении симметричных CRUD-доменов на простой нормализованной модели. Он предпочтительнее равномерного применения, потому что оправдание для CQRS специфично для структурных сил в каждом домене. Сложный домен заказов с рабочими процессами одобрения, инвариантами кредитного лимита и multi-table дашбордами имеет эти силы. Экран редактирования адреса компании — нет. Применение CQRS к экрану адреса ради «согласованности с доменом заказов» копирует паттерн без копирования сил, его оправдывающих — это карго-культ архитектуры. Локальный CQRS означает, что каждый домен структурирован соответствующим образом для своих собственных сил, а не архитектурно однороден ради однородности." },
    { q: "CQRS и event sourcing — это один паттерн? Можно ли иметь один без другого?", a: "Это разные паттерны. CQRS разделяет write model и read model — write-сторона может быть нормализованной реляционной таблицей; read-сторона — денормализованной проекцией. Event sourcing (юнит 08) заменяет стратегию хранения write-стороны на append-only журнал событий. CQRS без event sourcing: писать в реляционную таблицу, проецировать в read models из этой таблицы. Event sourcing без CQRS: использовать журнал событий как write-хранилище и обслуживать чтения напрямую из него без отдельных read models. Оба вместе: комбинация мощная (перестройка read models воспроизведением журнала событий), но несёт стоимость обоих паттернов. Их путаница — наиболее распространённая ошибка внедрения: команды предполагают, что не могут принять CQRS без event sourcing, что поднимает стоимость до точки, где ни один не принимается." },
  ]}
/>

<Recap lang="ru">
В вступительной истории была правильная интуиция — технический лид задал правильные вопросы: какой инвариант охраняет write model, и какая асимметрия оправдывает projection? Для экрана редактирования адреса компании ответами были «никакой» и «никакой». CQRS там был церемонией, а не архитектурой.

Затраты CQRS реальны: две модели, eventual consistency, инфраструктура projection и более длинные цепочки отладки. Эти затраты покупают реальную структурную выгоду — чистую write model, охраняющую инварианты, read model, сформированную именно для каждого query, независимую развиваемость обеих сторон. Компромисс оправдан там, где асимметрия реальна.

Применяй CQRS локально к доменным областям, где он оправдан. Пусть другие домены остаются на простых нормализованных моделях. Архитектурные паттерны должны следовать структурным силам, а не применяться ради согласованности.

CQRS и event sourcing независимы. Сегодня можно применить CQRS с реляционной write model без обязательства к журналу событий. Event sourcing (юнит 08) — это отдельное, значительное архитектурное решение о стратегии хранения write-стороны, дополняющее CQRS, но не требуемое им.
</Recap>
