---
slug: "02-cohesion"
lang: ru
track: "architecture-patterns"
unit: "01-coupling-and-cohesion"
order: 2
title: "Cohesion"
summary: "Cohesion измеряет фокус модуля: изменяются ли его части по одной причине? Functional cohesion — цель; coincidental — авария. LCOM сигнализирует о несвязных кластерах. Cohesion — противовес чрезмерному decoupling."
estMin: 20
status: ready
lessonType: topic
level: middle
concepts:
  - functional-cohesion
  - coincidental-cohesion
  - lcom-metric
  - cohesion-forms
  - single-responsibility
  - over-decoupling
prereqs: []
sources:
  - https://www.oreilly.com/library/view/software-metrics-a/9780470685013/
  - https://martinfowler.com/bliki/TwoHardThings.html
  - https://www.sciencedirect.com/science/article/pii/S0950584917302690
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Billing-инженер на B2B SaaS-платформе пытался разобраться в классе `BillingUtils`. Он содержал: `formatCurrency()`, `parseInvoiceXML()`, `sendEmailNotification()`, `retryWithBackoff()`, `calculateTax()`, `validateIBAN()` и `archiveOldInvoices()`. Класс вырастал два года, пока разработчики складывали в него «billing-связанные» утилиты в самый удобный контейнер. Каждая функция работала. Ни одна из них не принадлежала рядом с другими ни в каком осмысленном смысле — они разделяли папку, а не причину для изменения. Когда платёжный процессор сменил API и инженер искал все места форматирования валюты, пришлось прочитать весь класс, чтобы убедиться, что нашёл их все. У класса не было cohesion, и её отсутствие делало невозможным независимое рассуждение о нём.
</Hook>

<Crux>Cohesion — степень, в которой части модуля принадлежат вместе: служат ли они единой, объединённой цели и изменяются ли по одной причине.</Crux>

<Explanation>

## Спектр cohesion

Yourdon и Constantine определили cohesion по спектру от случайной (самая слабая) до функциональной (самая сильная), как они определили лестницу coupling. Уровни описывают, почему элементы модуля сгруппированы вместе.

**Coincidental cohesion** (самая слабая): элементы сгруппированы без структурной причины — они случайно оказались в одном файле или классе. `BillingUtils` из открытой истории — пример случайной cohesion. Единственное, что связывает её функции — разработчик решил там их разместить. Любой рефакторинг требует чтения всего модуля, чтобы понять, что в нём есть.

**Logical cohesion**: элементы сгруппированы, потому что логически похожи — все обработчики ошибок, все форматтеры строк, все функции валидации. Класс `StringUtils` с `trim`, `truncate`, `toSlug` и `escapeHtml` имеет логическую cohesion. Они разделяют общую область (строки), но не обязательно изменяются вместе или служат одной цели.

**Temporal cohesion**: элементы сгруппированы, потому что выполняются в одно время — вся логика инициализации при запуске, вся логика очистки при завершении. Функции выполняются вместе, но могут не иметь ничего общего в остальном.

**Communicational cohesion**: элементы сгруппированы, потому что работают с одними данными. Все функции, работающие с объектом `Order`, образуют communicationally cohesive группу — но единый класс `Order` с десятками методов для совершенно разных задач (pricing, shipping, fulfillment, billing, audit) по-прежнему слабо cohesive, если эти задачи изменяются по несвязанным причинам.

**Functional cohesion** (самая сильная): все элементы вносят вклад в единую, чётко определённую цель. Модуль делает одно дело, все его части служат этому одному делу, и изменение этого одного дела требует изменения только этого модуля. `InvoiceGenerator`, принимающий подтверждённый заказ и создающий отформатированный инвойс с корректным налогом и итогами — и больше ничего — имеет функциональную cohesion. Когда формат инвойса изменяется — изменяешь этот модуль. Когда меняется налоговое законодательство — изменяешь расчёт налогов в этом модуле. Когда меняется статус платежа — этот модуль не трогаешь.

<FlowDiagram
  label="Спектр cohesion: coincidental (слабейшая) до functional (сильнейшая)"
  nodes={[
    { id: "coincidental", label: "Coincidental", sub: "произвольная группировка" },
    { id: "logical", label: "Logical", sub: "похожая категория" },
    { id: "temporal", label: "Temporal", sub: "одно время выполнения" },
    { id: "communicational", label: "Communicational", sub: "одни данные" },
    { id: "functional", label: "Functional", sub: "единая, объединённая цель" },
  ]}
  edges={[
    { from: "coincidental", to: "logical", label: "сильнее" },
    { from: "logical", to: "temporal", label: "сильнее" },
    { from: "temporal", to: "communicational", label: "сильнее" },
    { from: "communicational", to: "functional", label: "сильнейшая" },
  ]}
  caption="Подъём по спектру cohesion означает, что элементы принадлежат вместе по более веской причине. Функциональная cohesion — цель: все элементы изменяются вместе, потому что служат одной единственной цели, и ничто больше не должно меняться при её эволюции."
/>

## LCOM: структурная интуиция для cohesion

Lack of Cohesion of Methods (LCOM) — статическая метрика, дающая структурное ощущение того, насколько хорошо методы класса принадлежат вместе. Интуиция проста: если класс имеет методы A, B и C, и методы A и B разделяют поля экземпляра, а метод C не разделяет ни одного поля ни с одним из них — класс, вероятно, делает два дела. Метод C, скорее всего, принадлежит в другое место.

Наиболее полезная форма (LCOM4 или вариант в обычных инструментах статического анализа) считает количество связных компонентов в графе, где узлы — методы, а рёбра соединяют методы, разделяющие хотя бы одно поле экземпляра. LCOM = 1 — идеал: каждый метод достижим от каждого другого через общие поля — весь класс одна тесно связная вещь. LCOM > 1 означает, что класс имеет несколько независимых кластеров, которые, вероятно, должны быть отдельными модулями.

В B2B-платформе представь класс `BillingProcessor` с такими связями метод/поле:

- `generateInvoice()` и `calculateTax()` оба используют `orderItems` и `customerId`
- `sendEmailNotification()` и `formatEmailBody()` оба используют `emailAddress` и `emailTemplate`
- Ни один метод первой группы не разделяет ни одного поля со второй группой

LCOM = 2. Класс делает две несвязанные вещи: генерацию инвойсов и отправку email. Это должны быть два класса.

<Inset kind="note" lang="ru">
LCOM — диагностика, а не приговор. Класс с LCOM = 2 может намеренно группировать две вещи, которые всегда изменяются вместе по бизнес-причинам — это осознанное решение, а не дефект. Используй LCOM для выявления кандидатов на ревью, а не для автоматического рефакторинга. Вопрос, который он задаёт: «принадлежат ли эти части действительно вместе?» — ответ требует суждения человека о границах изменений домена.
</Inset>

## Cohesion как противовес избыточному decoupling

Предыдущий урок установил, что coupling следует минимизировать — конкретно, степень, в которой один модуль зависит от внутренностей другого. Но в крайности «минимизировать coupling» производит модули настолько мелкозернистые, что ни один модуль не имеет собственного содержания. Пятьдесят классов, каждый с одним методом. Каждая операция требует компоновки пятнадцати классов. У кода data coupling везде и он полностью нечитаем.

Cohesion — противодействующая сила. Высоко cohesive модуль держит вместе то, что *изменяется вместе* — что точно и определяет хорошую структурную границу. Когда всё в модуле изменяется по одной причине, модуль — стабильная единица эволюции. Когда части модуля изменяются по разным причинам, модуль — неправильная граница.

Практическая связь: высокая cohesion естественно снижает coupling-поверхность. Высоко cohesive `InvoiceGenerator` предоставляет чистый, минимальный интерфейс (data coupling снаружи), потому что всё, что ему нужно внутренне, уже инкапсулировано. Тебе не нужно заглядывать в его внутренности, потому что все его внутренности скоординированы на одну цель. Низкая cohesion, напротив, вынуждает вызывающих знать о внутренней структуре модуля — потому что модулю не на что опереться.

<FlowDiagram
  label="Cohesion и coupling как структурные противодействующие силы"
  nodes={[
    { id: "high-cohesion", label: "Высокая cohesion", sub: "части изменяются вместе" },
    { id: "clean-boundary", label: "Чистый интерфейс", sub: "data coupling снаружи" },
    { id: "low-cohesion", label: "Низкая cohesion", sub: "части изменяются по разным причинам" },
    { id: "leaky-boundary", label: "Протекающий интерфейс", sub: "вызывающие проникают во внутренности" },
    { id: "over-decouple", label: "Чрезмерный decoupling", sub: "50 однометодных классов" },
    { id: "composition-hell", label: "Оверхед композиции", sub: "каждая операция требует 15 вызовов" },
  ]}
  edges={[
    { from: "high-cohesion", to: "clean-boundary", label: "обеспечивает" },
    { from: "low-cohesion", to: "leaky-boundary", label: "вынуждает к" },
    { from: "over-decouple", to: "composition-hell", label: "производит" },
    { from: "clean-boundary", to: "high-cohesion", label: "сигнализирует" },
  ]}
  caption="Высокая cohesion и чистые интерфейсы взаимно подкрепляют друг друга. Модуль, делающий одно дело хорошо, естественно предоставляет только то, что нужно вызывающим. Низкая cohesion вынуждает вызывающих знать больше о внутренностях — та же сила, что производит content и stamp coupling. Чрезмерный decoupling — разбиение за пределы естественных границ изменений — создаёт оверхед композиции без пользы cohesion."
/>

<Inset kind="why" lang="ru">
Почему cohesion важнее, чем кажется? Потому что стоимость низкой cohesion платится не в момент написания кода. Класс `BillingUtils` из открытой истории работал отлично. Каждая функция возвращала правильный результат. Стоимость появляется только при необходимости что-то изменить: нельзя определить, что есть в модуле, не прочитав всё; нельзя протестировать подмножество, не понимая остального; нельзя развивать одну задачу, не рискуя случайным столкновением с другой. Низкая cohesion — невидимый долг: он накапливается в момент изменения, а не в момент написания.
</Inset>

<Quiz
  id="arch-01-02-quiz-1"
  lessonSlug="02-cohesion"
  lang="ru"
  question="Модуль содержит три функции: `processPayment()`, `logPaymentAttempt()` и `notifyFinanceTeam()`. Каждая функция запускается при обработке платежа и каждая модифицирует одну запись платежа. Какая форма cohesion лучше всего описывает этот модуль?"
  choices={[
    { label: "Temporal и communicational cohesion — функции выполняются в одно время (обработка платежа) и работают с одними данными (записью платежа), но служат разным целям (платёж, логирование, уведомление) и могут изменяться по разным причинам.", correct: true },
    { label: "Functional cohesion — все они связаны с обработкой платежей.", misconception: "Связь с одним бизнес-событием — не functional cohesion. Функциональная cohesion требует, чтобы все части служили единой, объединённой цели и изменялись по одной причине. Здесь логика логирования изменяется при изменении формата логов; логика уведомлений — при изменении канала; логика платежей — при изменении платёжного процессора. У них три отдельные причины для изменения." },
    { label: "Coincidental cohesion — они не имеют никакой связи.", misconception: "Coincidental cohesion — самая слабая форма: произвольная группировка без структурной связи. Эти три функции имеют структурную связь: они выполняются вместе (temporal) и работают с одними данными (communicational). Это сильнее coincidental, но слабее functional." },
    { label: "Logical cohesion — все они принадлежат платёжному домену.", misconception: "Logical cohesion группирует вещи, потому что они логически похожи по категории. Эти три функции находятся в одной временной последовательности и работают с одними данными — это делает их temporal и communicational, а не просто logical." },
  ]}
/>

<Quiz
  id="arch-01-02-quiz-2"
  lessonSlug="02-cohesion"
  lang="ru"
  question="Статический анализ показывает, что класс BillingProcessor имеет LCOM = 3. Методы, генерирующие инвойсы, разделяют поля между собой; методы, отправляющие уведомления, разделяют поля между собой; методы, архивирующие старые записи, разделяют поля между собой — но никакие поля не разделяются между тремя группами. Что это означает и что следует делать?"
  choices={[
    { label: "Класс, вероятно, делает три вещи. Это кандидат на разбиение на три сфокусированных класса: InvoiceGenerator, NotificationSender и InvoiceArchiver — каждый с LCOM = 1 и единственной причиной для изменения.", correct: true },
    { label: "LCOM = 3 означает, что в классе слишком много методов и некоторые следует удалить.", misconception: "LCOM измеряет связные компоненты, а не количество методов. Класс с тремя методами может иметь LCOM = 3, если ни один не разделяет полей. LCOM = 3 означает три несвязанных кластера функциональности — правильный ответ — определить, должны ли эти кластеры быть отдельными модулями, а не удалять методы." },
    { label: "LCOM = 3 — идеальное значение для хорошо структурированного класса с тремя ответственностями.", misconception: "LCOM = 1 — идеальное значение: все методы связаны через общие поля, образуя один cohesive кластер. LCOM > 1 сигнализирует о несвязанных кластерах, которые могут принадлежать отдельным модулям. LCOM = 3 — предупреждение, а не цель." },
    { label: "LCOM значим только для классов с более чем десятью методами.", misconception: "LCOM значим для любого класса. Трёхметодный класс с LCOM = 3 — где каждый метод не разделяет полей ни с одним другим — явно делает три несвязанные вещи независимо от размера. Ценность метрики пропорциональна числу несвязанных кластеров, а не общему числу методов." },
  ]}
/>

<Quiz
  id="arch-01-02-quiz-3"
  lessonSlug="02-cohesion"
  lang="ru"
  question="Команда разбивает класс InvoiceGenerator на двенадцать однометодных классов для достижения максимального слабого coupling. Теперь генерация инвойса требует координации двенадцати вызовов в правильном порядке. Какую структурную проблему они создали?"
  choices={[
    { label: "Они пересекли естественную границу изменений. Двенадцать шагов генерации инвойса изменяются вместе — они единая cohesive задача. Разделение их на двенадцать классов распределило координационный coupling в каждый вызывающий, не получив стабильной единицы эволюции.", correct: true },
    { label: "Следовало разбить ровно на три класса, а не на двенадцать.", misconception: "Магического числа нет. Структурный принцип — то, что изменяется вместе, принадлежит вместе. Если все двенадцать шагов генерации инвойса изменяются при изменении формата инвойса, они одна cohesive вещь и принадлежат одному модулю. Разбиение на три не решает принципиальную проблему, если три группы по-прежнему изменяются по одной причине." },
    { label: "Двенадцать классов — слишком мало; следует использовать один класс на строку кода.", misconception: "Это доводит принцип до абсурда. Суть в том, что cohesion подсказывает правильную гранулярность. Произвольная мелкогранулярность увеличивает оверхед композиции, разрушая стабильную единицу эволюции, которую обеспечивает cohesive модуль." },
    { label: "Реальная проблема — недостаточное покрытие тестами, а не структура модулей.", misconception: "Тесты не исправляют структурный coupling. Проблема в том, что логика координации — последовательность двенадцати вызовов — теперь живёт в каждом вызывающем, то есть каждый вызывающий связан с жизненным циклом генерации инвойса. Добавление тестов документирует этот coupling; оно его не устраняет." },
  ]}
/>

</Explanation>

<KeyTakeaway>Cohesion — противовес coupling: она держит вместе то, что изменяется вместе. Функциональная cohesion — все части служат единой цели — это цель. LCOM предоставляет структурный сигнал, когда модуль делает несколько вещей. Правильная гранулярность декомпозиции — не «как можно мельче», а «как можно точнее совпадающая с границами изменений».</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-01-02-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое функциональная cohesion и чем она отличается от communicational cohesion?", a: "Функциональная cohesion означает, что все элементы модуля вносят вклад в единую, чётко определённую цель — и все они изменяются по одной причине. Communicational cohesion означает, что элементы сгруппированы, потому что работают с одними данными — но они могут служить разным целям и изменяться по разным причинам. Класс Order, обрабатывающий pricing, shipping, billing и fulfillment, имеет communicational cohesion (все работают с данными Order), но не функциональную (pricing изменяется по причинам pricing-правил, billing — по причинам billing-правил и т.д.). Функциональная cohesion добавляет ограничение единственной цели и единственной причины для изменения поверх общих данных." },
    { q: "Что измеряет LCOM и что означает LCOM > 1?", a: "LCOM (Lack of Cohesion of Methods) измеряет количество несвязанных кластеров в классе, где методы образуют кластер, если разделяют хотя бы одно поле экземпляра. LCOM = 1 означает, что все методы связаны через общие поля — одна cohesive вещь. LCOM > 1 означает, что класс имеет несколько независимых кластеров, не разделяющих полей — структурный сигнал того, что класс, вероятно, делает несколько вещей, которые можно разделить на отдельные, более сфокусированные модули. LCOM — диагностика, а не мандат; она выявляет кандидатов для проверки человеком, должны ли эти кластеры быть разделены." },
    { q: "Почему «максимизировать decoupling везде» — ловушка, и что её исправляет?", a: "Потому что coupling — не только между парами модулей; он существует и в оркестрации последовательностей. Когда ты разбиваешь cohesive задачу на множество мелкозернистых модулей для минимизации попарного coupling, вызывающий, который должен вызывать их в правильном порядке, берёт на себя координационный coupling. Ты переместил его, а не устранил. Исправление — cohesion: определи естественную границу изменений (что изменяется вместе по одной причине) и трактуй это как один модуль. Внутри этой границы слабый coupling между внутренними функциями хорош. Через границу цель — чистый интерфейс." },
  ]}
/>

<Recap lang="ru">
Cohesion измеряет сфокусированность модуля — принадлежат ли его части вместе, потому что служат единой цели и изменяются по единой причине. Спектр cohesion идёт от coincidental (произвольная группировка, самая слабая форма) через logical, temporal и communicational cohesion до functional cohesion (все части служат одной цели, все изменяются по одной причине — самая сильная форма и цель).

LCOM предоставляет структурный сигнал: класс с несколькими несвязанными кластерами методов (LCOM > 1) делает несколько вещей. Это диагностический инструмент, а не мандат — вопрос всегда в том, должны ли кластеры быть разделены на основе того, имеют ли они независимые причины для изменения.

Cohesion — противовес давлению decoupling из предыдущего урока. Минимизация попарного coupling без учёта cohesion производит чрезмерно декомпозированные модули, где координационная логика распределяется в каждый вызывающий. Правильная структурная граница — это естественная граница изменений: то, что изменяется вместе по одной причине, принадлежит вместе, и эта граница естественно производит чистый, минимальный интерфейс для внешнего мира (data coupling), потому что всё нужное внутри уже инкапсулировано.
</Recap>
