infographic hierarchy-tree-lr-curved-line-rounded-rect-node
data
  title BillingProcessor — LCOM = 2
  desc Два кластера методов без общих полей → разбить на два класса
  root
    label BillingProcessor (LCOM = 2)
    children
      - label Кластер инвойсов
        children
          - label generateInvoice()
          - label calculateTax()
          - label общие поля orderItems + customerId
      - label Кластер email
        children
          - label sendEmailNotification()
          - label formatEmailBody()
          - label общие поля emailAddress + emailTemplate
theme
  palette #6366f1 #14b8a6
