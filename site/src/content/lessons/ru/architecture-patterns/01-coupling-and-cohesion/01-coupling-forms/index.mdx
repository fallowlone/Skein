---
slug: "01-coupling-forms"
lang: ru
track: "architecture-patterns"
unit: "01-coupling-and-cohesion"
order: 1
title: "Формы coupling"
summary: "Afferent и efferent coupling измеряют направление зависимостей. Лестница coupling идёт от content до data. Более слабый coupling не всегда лучше — правильный уровень зависит от реальных границ изменений домена."
estMin: 22
status: ready
lessonType: topic
level: middle
concepts:
  - afferent-coupling
  - efferent-coupling
  - content-coupling
  - control-coupling
  - data-coupling
  - coupling-ladder
prereqs: []
sources:
  - https://martinfowler.com/ieeeSoftware/coupling.pdf
  - https://www.oreilly.com/library/view/software-metrics-a/9780470685013/
  - https://www.sciencedirect.com/science/article/pii/S0950584917302690
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Команда billing в B2B SaaS-компании решила изменить логику расчёта налогов. Простое изменение, думали они — один файл, пара часов. Вместо этого ушло три дня. Функция расчёта налогов напрямую читала внутренние строки БД модуля заказов, интерпретировала сырую раскладку колонок и ветвилась по внутреннему целочисленному статусу, который имел разный смысл в разных подсистемах. Когда нашли все места, зависящие от старого поведения, тронули одиннадцать файлов и внесли две регрессии. Никто не проектировал этот coupling намеренно. Он вырос по одной маленькой зависимости за раз, и никто его не измерял.
</Hook>

<Crux>Coupling — степень, в которой один модуль зависит от внутренностей другого: измеряется направлением (afferent/efferent) и тем, что именно разделяется (лестница от content до data).</Crux>

<Explanation>

## Afferent и efferent coupling

У каждого модуля системы есть два показателя coupling, которые важны по отдельности:

**Afferent coupling (Ca)** — количество модулей, которые зависят от данного: сколько других частей системы импортирует или вызывает его. Модуль с высоким afferent coupling широко используется. На него давит **стабильность**: если он изменится, сломается многое. Billing-движок в B2B-платформе имеет высокий Ca — от него зависят сервис заказов, финансовая панель, инструменты администрирования и ERP-интеграция.

**Efferent coupling (Ce)** — количество модулей, от которых зависит данный: сколько других частей системы он импортирует или вызывает. Модуль с высоким efferent coupling хрупок: он чувствителен к изменениям во многих других модулях. Модуль, импортирующий репозиторий заказов, tax-сервис, discount-движок и платёжный шлюз — высоко efferent. Любое изменение в любом из этих четырёх каскадирует в него.

Отношение Ce / (Ca + Ce) даёт грубую **оценку нестабильности** (метрика Роберта Мартина). Модуль с Ce = 8 и Ca = 2 имеет нестабильность ≈ 0.8 — он зависит от многих вещей, но зависит от него немного. Модуль с Ce = 0 и Ca = 20 максимально стабилен — ничто из его зависимостей не сломает его, но всё, что его использует, подвержено его изменениям.

<FlowDiagram
  label="Afferent vs efferent coupling в B2B-платформе"
  nodes={[
    { id: "billing", label: "BillingEngine", sub: "высокий Ca — зависят многие" },
    { id: "order", label: "OrderService", sub: "вызывает billing" },
    { id: "finance", label: "FinanceDashboard", sub: "читает billing" },
    { id: "admin", label: "AdminTool", sub: "запускает billing" },
    { id: "erp", label: "ERPIntegration", sub: "зависит от вывода billing" },
    { id: "tax", label: "TaxService", sub: "от него зависит billing" },
    { id: "payment", label: "PaymentGateway", sub: "от него зависит billing" },
  ]}
  edges={[
    { from: "order", to: "billing", label: "Ce для Order" },
    { from: "finance", to: "billing", label: "Ce для Finance" },
    { from: "admin", to: "billing", label: "Ce для Admin" },
    { from: "erp", to: "billing", label: "Ce для ERP" },
    { from: "billing", to: "tax", label: "Ce для Billing" },
    { from: "billing", to: "payment", label: "Ce для Billing" },
  ]}
  caption="BillingEngine имеет высокий afferent coupling (Ca = 4 показано): от него зависят четыре модуля. У него также есть efferent coupling (Ce = 2): он зависит от TaxService и PaymentGateway. Нестабильность: Ce/(Ca+Ce) = 2/6 ≈ 0.33 — относительно стабильно, как и должно быть для ядра домена."
/>

## Лестница coupling

Не весь coupling одинаков. Page-Jones (1988) и Yourdon & Constantine определили классическую таксономию, ранжирующую формы coupling от самой жёсткой до самой мягкой. Чем слабее coupling, тем независимее оба модуля могут эволюционировать.

**Content coupling** (самый жёсткий): модуль A напрямую обращается к внутренним структурам данных или деталям реализации модуля B. Функция расчёта налогов из открытой истории читала сырые строки БД модуля заказов — это content coupling. Любое изменение внутреннего устройства модуля заказов ломает функцию налогов. Coupling невидим на уровне интерфейса; он существует в коде, который полностью обходит интерфейс.

**Common coupling**: оба модуля разделяют глобальную изменяемую переменную или общее хранилище данных. Два сервиса, разделяющих таблицу БД и оба пишущих в неё напрямую — это common coupling. Любой из них может испортить общее состояние так, что это сломает другой, и ни один не контролирует контракт данных.

**Control coupling**: модуль A передаёт модулю B флаг или код, управляющий его поведением. `processOrder(order, mode: 'dry-run' | 'live')` — control coupling, если billing-движок ветвится по флагу mode. Вызывающий должен знать о внутренних путях выполнения вызываемого — coupling находится в потоке управления, а не только в данных.

**Stamp coupling** (или data-structure coupling): модуль A передаёт модулю B большую структуру данных, а модуль B использует только её часть. Передача полного объекта `Order`, когда billing нужны только `order.items` и `order.customerId` — stamp coupling. Модули связаны с формой общей структуры даже по полям, которые ни один из них не должен трогать.

**Data coupling** (самый мягкий): модули A и B общаются только через простые, чётко определённые параметры — ровно те данные, что нужны. Billing-движок принимает `(items: LineItem[], customerId: string)` и возвращает `(invoiceId: string, amount: Money)`. Ни один модуль ничего не знает о внутренностях другого. Это золотой стандарт.

<FlowDiagram
  label="Лестница coupling: content (самый жёсткий) до data (самый мягкий)"
  nodes={[
    { id: "content", label: "Content coupling", sub: "прямой доступ к внутренностям" },
    { id: "common", label: "Common coupling", sub: "общий global / общая БД" },
    { id: "control", label: "Control coupling", sub: "передача управляющего флага" },
    { id: "stamp", label: "Stamp coupling", sub: "избыточная структура данных" },
    { id: "data", label: "Data coupling", sub: "только нужные примитивы" },
  ]}
  edges={[
    { from: "content", to: "common", label: "слабее" },
    { from: "common", to: "control", label: "слабее" },
    { from: "control", to: "stamp", label: "слабее" },
    { from: "stamp", to: "data", label: "слабее" },
  ]}
  caption="Движение вниз по лестнице сокращает объём того, что разделяется между модулями. Каждый шаг ослабляет coupling и позволяет двум модулям эволюционировать независимее — но ценой необходимости определить точный минимальный интерфейс."
/>

<Inset kind="why" lang="ru">
Почему порядок лестницы важен? Потому что чем жёстче coupling, тем больше координации требуется при изменении любого из модулей. Content coupling означает, что любой внутренний рефакторинг в модуле B рискует сломать модуль A. Common coupling означает, что любая запись в общее состояние со стороны A может испортить чтение B невидимым способом, обнаруживаемым только в runtime. Control coupling означает, что A должен обновляться, когда B добавляет новые режимы выполнения. Stamp coupling означает, что оба модуля связаны с общей формой данных, которая может расти или меняться по причинам, которые ни один из них не контролирует. Каждый шаг вниз по лестнице сокращает радиус взрыва от изменения одного модуля на другой.
</Inset>

## Почему слабее — не всегда лучше

Вот структурная ловушка: команды, понявшие лестницу coupling, иногда заключают «data coupling везде, всегда». Это неверно, и типичный сбой — фрагментация.

Рассмотрим сервис заказов в B2B-платформе. Агрегат `Order` содержит `header`, `lineItems`, `pricing`, `taxDetails`, `approvalState` и `auditTrail`. Если каждая внутренняя функция, обращающаяся к данным заказа, рефакторится так, чтобы принимать только минимальные нужные примитивы, получается:

- `updateApprovalState(orderId, state, actorId, timestamp)` — data coupled, чисто.
- `recalcPricing(orderId, lineItems, customerId, discountCode)` — data coupled, чисто.
- `computeTax(lineItems, shippingAddress, taxClassification)` — data coupled, чисто.

Теперь координация изменения, затрагивающего approval state, pricing и tax одновременно, требует оркестратора, вызывающего все три функции, собирающего результаты и пересобирающего обновлённый заказ. Оркестратор теперь жёстко связан со всеми тремя — он знает полный жизненный цикл заказа. Coupling переехал вверх по стеку вызовов, а не исчез.

Вопрос не «слабо ли это связано?», а «находится ли coupling в правильном месте и отражает ли он реальные инварианты, которые должны изменяться вместе?» Высокая cohesion (рассматривается в следующем уроке) — это противодействующая сила.

<Inset kind="note" lang="ru">
Практическая эвристика: смотри на то, что изменяется вместе в продакшене. Если каждый раз, когда billing-команда деплоит изменение в генерацию инвойсов, приходится трогать структуры данных модуля заказов — coupling неправильный, независимо от позиции на лестнице. Coupling — это сила, а не цель. Цель — coupling, соответствующий реальным границам изменений домена.
</Inset>

<Quiz
  id="arch-01-01-quiz-1"
  lessonSlug="01-coupling-forms"
  lang="ru"
  question="Billing-движок читает записи заказов напрямую из таблицы БД, которой владеет сервис заказов, интерпретируя сырые значения колонок. После того как команда заказов переименовала колонку и добавила nullable-поле, billing молча начал генерировать неправильные инвойсы. Какая форма coupling стала причиной?"
  choices={[
    { label: "Content coupling — billing обошёл интерфейс модуля заказов и напрямую обратился к его внутренней структуре данных, поэтому любое внутреннее изменение в модуле заказов ломает billing.", correct: true },
    { label: "Stamp coupling — billing передали избыточный объект Order и он использовал только его часть.", misconception: "Stamp coupling — это получение большой структуры данных через параметр функции. Здесь billing полностью обходит интерфейс и читает БД напрямую — это content coupling, самая жёсткая форма на лестнице." },
    { label: "Common coupling — оба модуля разделяют БД.", misconception: "Common coupling — это взаимная запись в общее изменяемое состояние (например, оба пишут в одну таблицу). Здесь billing только читает из таблицы, которой владеет сервис заказов. Это content coupling: billing зависит от внутреннего представления данных модуля заказов." },
    { label: "Control coupling — поведение billing управлялось флагами модуля заказов.", misconception: "Control coupling предполагает передачу флага, управляющего путями выполнения. Этот сбой — о зависимости billing от внутреннего устройства схемы БД модуля заказов, что является content coupling." },
  ]}
/>

<Quiz
  id="arch-01-01-quiz-2"
  lessonSlug="01-coupling-forms"
  lang="ru"
  question="BillingEngine имеет Ca = 12 (двенадцать модулей зависят от него) и Ce = 1 (он зависит только от интерфейса PaymentGateway). Что это говорит о его стабильности и что это означает для подхода к его интерфейсу?"
  choices={[
    { label: "Он высокостабилен (нестабильность ≈ 0.08). Его интерфейс следует трактовать как высококостный к изменению контракт — любое изменение с потерей обратной совместимости распространится на двенадцать потребителей, поэтому интерфейс нужно проектировать консервативно и версионировать тщательно.", correct: true },
    { label: "Он высоконестабилен, потому что зависит от многих модулей.", misconception: "Нестабильность = Ce / (Ca + Ce) = 1 / 13 ≈ 0.08 — очень стабильно, а не нестабильно. Нестабильность измеряет, насколько окружение может принуждать модуль к изменениям. С только одной efferent-зависимостью очень мало что в окружении может напрямую заставить BillingEngine изменяться." },
    { label: "Ca и Ce должны быть равны для хорошо спроектированного модуля.", misconception: "Такого правила равенства не существует. Модули ядра домена правильно имеют высокий Ca (многие вещи зависят от них) и низкий Ce (они зависят от немногих вещей) — это подходящая структура для стабильного ядра. Паттерн, за которым нужно следить — высокий Ce с низким Ca: модуль поглощает изменения из многих направлений и от него зависит мало." },
    { label: "Низкий Ce означает, что billing плохо интегрирован и его следует подключить к большему числу модулей.", misconception: "Низкий Ce желателен для ядра домена — он означает, что billing нечувствителен к изменениям в остальной системе. Добавление ненужных efferent-зависимостей сделало бы его более хрупким без добавления ценности." },
  ]}
/>

<Quiz
  id="arch-01-01-quiz-3"
  lessonSlug="01-coupling-forms"
  lang="ru"
  question="Команда рефакторит billing-движок так, чтобы каждая функция принимала только минимальные нужные примитивы, достигая data coupling повсюду. Теперь billing-движок вызывает двенадцать отдельных функций для обработки заказа. Отладка расхождения в billing требует трассировки через все двенадцать. В чём реальная структурная проблема?"
  choices={[
    { label: "Переход на data coupling везде переместил координационный coupling вверх в оркестратор — coupling не был устранён, а перераспределён. Оркестратор теперь несёт весь вес жизненного цикла billing, а отлаживаемость пострадала как побочный эффект.", correct: true },
    { label: "Data coupling всегда неправильный и следует использовать stamp coupling.", misconception: "Data coupling — правильная цель на каждом попарном интерфейсе. Проблема здесь не в форме coupling — в том, что декомпозиция на двенадцать функций не отражает реальные границы изменений. Функции, всегда изменяющиеся вместе, принадлежат вместе; чрезмерная декомпозиция создаёт распределённый оркестрационный coupling, который data coupling на каждом интерфейсе не решает." },
    { label: "Проблема в том, что двенадцать — слишком много функций; лимит должен быть пять.", misconception: "Магического числа функций не существует. Структурная проблема в том, что все двенадцать функций вызываются в фиксированной последовательности для обработки одного заказа — эта последовательность является неявным модулем с высоким afferent coupling, которому место в явном месте. Число — симптом, а не причина." },
    { label: "Проблема в недостаточной документации.", misconception: "Документация не меняет структуру coupling. Проблема в том, что логика координации — знание, что что вызывает в каком порядке — распределена по вызывающим или неявна в оркестраторе, делая coupling невидимым, а не устраняя его." },
  ]}
/>

</Explanation>

<KeyTakeaway>Coupling измеряется в двух измерениях: направление (afferent внутрь, efferent наружу) и вид (от content до data на лестнице). Жёсткий coupling концентрирует радиус взрыва; слабый coupling перемещает ответственность за координацию вверх. Цель — не самая нижняя ступень лестницы везде, а coupling, соответствующий реальным границам изменений домена.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-01-01-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое afferent coupling и почему высокий afferent coupling делает модуль стабильным?", a: "Afferent coupling (Ca) — количество модулей, зависящих от данного: сколько вещей импортирует или вызывает его. Модуль с высоким Ca стабилен, потому что ничто из того, что делают его вызывающие, не может принудить его к изменению; принудить его могут только его собственные efferent-зависимости. Обратная сторона: поскольку от него зависят многие, любое его изменение распространяется широко — поэтому его интерфейс нужно проектировать консервативно. BillingEngine с Ca = 12 иллюстрирует это: стабилен к внешним изменениям, дорог в модификации." },
    { q: "Пройди по лестнице coupling от самого жёсткого до самого мягкого, одним предложением на каждую ступень.", a: "Content: A обращается к внутренней реализации или структуре данных B напрямую, обходя интерфейс B. Common: оба A и B разделяют глобальную переменную или изменяемое общее хранилище данных. Control: A передаёт B флаг, управляющий путём выполнения B, то есть A знает о логике ветвления B. Stamp: A передаёт B структуру данных большую, чем нужна B, связывая оба с формой общей структуры. Data: A передаёт B только минимальные нужные примитивы, а B возвращает только то, что нужно A — никаких внутренних знаний ни с одной стороны." },
    { q: "Почему достижение data coupling везде не устраняет coupling?", a: "Потому что coupling — это не только попарно; он существует и в оркестрации последовательностей. Когда ты декомпозируешь связную задачу на множество мелкозернистых data-coupled функций, вызывающий, который должен вызывать их в правильном порядке, берёт на себя координационный coupling. Попарный coupling между каждой функцией и её типами параметров слабый, но оркестратор жёстко связан со всеми ними и с последовательностью их вызова. Data coupling — правильная цель на каждом интерфейсе; высокая cohesion (сохранение вместе того, что изменяется вместе) — противодействующая сила, предотвращающая чрезмерную декомпозицию." },
  ]}
/>

<Recap lang="ru">
Coupling измеряется в двух измерениях. Направление: afferent coupling (Ca) считает, сколько модулей зависит от данного; efferent coupling (Ce) считает, сколько зависит от других этот. Модуль с высоким Ca и низким Ce стабилен — он поглощает мало изменений из окружения, но широко распространяет изменения, когда они происходят. Метрика нестабильности Ce/(Ca+Ce) это количественно выражает.

Вид: лестница coupling идёт от content coupling (прямой доступ к внутренностям другого модуля, самая жёсткая и хрупкая форма) через common coupling (общее изменяемое состояние), control coupling (передача управляющих флагов) и stamp coupling (избыточные структуры данных) до data coupling (минимальные, точные параметры — самая мягкая форма). Каждый шаг вниз по лестнице сокращает объём того, что изменяется вместе при эволюции одного модуля.

Ловушка — заключение, что data coupling везде всегда лучше. Слабый coupling на каждом попарном интерфейсе хорош; чрезмерная декомпозиция, распределяющая координационную логику по неявному оркестратору — нет. Правильный уровень coupling — тот, что соответствует реальным границам изменений домена: именно то, что помогает определить высокая cohesion, тема следующего урока.
</Recap>
