---
slug: "01-n-tier"
lang: ru
track: "architecture-patterns"
unit: "03-layered-architecture"
order: 1
title: "N-уровневая слоистая архитектура"
summary: "Слоистая архитектура делит код на горизонтальные уровни — presentation, application, domain, infrastructure. Каждый слой зависит только от нижележащего. Даёт замещаемость и тестируемость, но порождает sinkhole, когда слои просто проксируют данные."
estMin: 20
status: ready
lessonType: topic
level: middle
concepts:
  - layered-architecture
  - n-tier
  - strict-layering
  - relaxed-layering
  - architecture-sinkhole
  - layer-replaceability
prereqs: []
sources:
  - https://martinfowler.com/bliki/PresentationDomainDataLayering.html
  - https://www.informit.com/store/clean-architecture-a-craftsmans-guide-to-software-structure-9780134494166
  - https://www.oreilly.com/library/view/software-architecture-patterns/9781491971437/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import strictVsRelaxed from "./infographics/strict-vs-relaxed.svg?raw";

<Hook>
B2B-платформа заказов начиналась как один файл — обработчик запросов, который читал из базы данных, выполнял валидацию и возвращал ответ. За шесть месяцев файл разросся до 3000 строк. Команда разбила его на «слои»: пакет контроллеров, пакет сервисов, пакет репозиториев. Слоистость сделала код навигируемым. Можно было писать тесты против сервисов без живой базы данных. Когда команда перешла с REST-клиента к нативному драйверу — изменился только пакет репозиториев. Через два года платформа имела двенадцать классов-сервисов. Каждый метод сервиса следовал одному и тому же паттерну: валидировать вход, вызвать репозиторий, вернуть результат. Ни один метод ничего не делал, кроме делегирования — вызывал один метод репозитория и возвращал то, что тот возвращал, без изменений. Каждое изменение требовало затронуть контроллер, сервис и репозиторий в связке. Слоистость, которая должна была давать замещаемость, превратилась в церемонию: три файла на каждое однострочное бизнес-правило. Команда правильно назвала слои, но так и не спросила, что каждый слой должен добавлять.
</Hook>

<Crux>Слоистая архитектура делит код на уровни, каждый зависит только вниз; ценность — только там, где слой добавляет поведение.</Crux>

<Explanation>

## Четыре стандартных слоя

Каноническая n-tier-модель описывает четыре слоя, каждый со своей ответственностью:

**Presentation layer** — обрабатывает всё, что относится к пользовательскому интерфейсу: парсинг HTTP-запросов, десериализация входных данных, форматирование ответов, аутентификационные токены, согласование контента. Знает о веб-протоколе. Не знает о бизнес-правилах.

**Application layer** — оркестрирует use case'ы. Последовательно вызывает domain и infrastructure, управляет транзакциями, применяет политику авторизации, публикует события. Знает о потоке use case'а. Не содержит бизнес-логику — делегирует в domain.

**Domain layer** — содержит бизнес-модель: сущности, value objects, доменные сервисы и правила, управляющие ими. На B2B-платформе «заказ нельзя подтвердить, если у него есть просроченные счета» — это доменная логика. У этого слоя нет знания о базе данных, HTTP-фреймворке или системе очередей.

**Infrastructure layer** — реализует порты, которые определяют domain и application: репозитории баз данных, отправители email, публикаторы очередей, клиенты внешних API. Знает о технологиях. Не знает о бизнес-правилах.

<FlowDiagram
  label="Стек зависимостей четырёх слоёв"
  nodes={[
    { id: "pres", label: "Presentation", sub: "HTTP, сериализация, auth-токены" },
    { id: "app", label: "Application", sub: "оркестрация use case'ов, транзакции" },
    { id: "domain", label: "Domain", sub: "сущности, правила, инварианты" },
    { id: "infra", label: "Infrastructure", sub: "БД, email, очередь, внешние API" },
  ]}
  edges={[
    { from: "pres", to: "app", label: "зависит от (source-code)" },
    { from: "app", to: "domain", label: "зависит от (source-code)" },
    { from: "app", to: "infra", label: "зависит от (source-code)" },
    { from: "domain", to: "infra", label: "зависит от (source-code) — только relaxed" },
  ]}
  caption="При strict layering: Presentation зависит от Application, Application зависит от Domain, Domain не зависит ни от чего, Infrastructure реализует интерфейсы, определённые Domain или Application. При relaxed layering Application может также напрямую зависеть от Infrastructure. Стрелки зависимостей всегда указывают вниз — ни один слой не импортирует из слоя выше."
/>

## Strict vs relaxed layering

**Strict layering** требует, чтобы каждый слой зависел только от непосредственно нижележащего. Presentation вызывает Application; Application вызывает Domain; Domain определяет интерфейсы, которые реализует Infrastructure. Domain не имеет никакого знания о Infrastructure — не импортирует драйвер БД, ORM или классы фреймворка.

Strict layering даёт наиболее сильную изоляцию: domain можно тестировать без базы данных, очередей, внешних сервисов. Его можно понять без знания какой-либо технологии. Это фундамент, на котором строятся hexagonal и clean architecture (эти паттерны — strict layering, доведённый до логического завершения, о них — в unit'ах 04 и 05).

**Relaxed layering** (также «open layer» architecture) позволяет слою вызывать любой нижележащий слой, не только непосредственно смежный. Presentation может вызвать Domain напрямую, минуя Application. Application может импортировать классы Infrastructure напрямую, а не через интерфейс.

Relaxed layering прагматичен: для простых CRUD-операций application layer не добавляет ценности, и принудительный вызов через него — церемония. Компромисс: relaxed layering легче нарушить незаметно — как только разрешено пропускать слои для удобства, дисциплина размывается, и в итоге presentation импортирует классы репозиториев напрямую.

<Infographic
  svg={strictVsRelaxed}
  label="Сравнение strict и relaxed layering бок о бок: strict layering зависит только от непосредственно нижележащего слоя ради максимальной изоляции ценой boilerplate и дисциплины, а relaxed layering может пропускать слои ради прагматичного CRUD, но легче размывается в нарушения границ."
  caption="Strict layering максимизирует изоляцию и тестируемость ценой дисциплины; relaxed layering обменивает изоляцию на прагматизм и легче размывается — ключевое решение слоистости."
  hue="var(--d-infra)"
/>

<Inset kind="why" lang="ru">
Зачем вообще нужна слоистость? Исходная сила, которую она адресует, — замещаемость. Когда B2B-платформа переходила на другой драйвер базы данных, изменился только слой infrastructure — потому что никакая бизнес-логика не утекла в него. Когда команде потребовалось тестировать правила подтверждения заказа изолированно — это было возможно, потому что domain layer не зависел от базы данных. Оба преимущества требуют, чтобы границы слоёв держались: чтобы ни один вышележащий слой не импортировал детали реализации нижележащего. В момент, когда бизнес-логика утекает в репозиторий или логика presentation — в доменную сущность, эти преимущества исчезают.
</Inset>

## Что слоистость реально даёт — и какова её стоимость

Конкретные выгоды хорошо поддерживаемой слоистой архитектуры:

**Замещаемость** — infrastructure можно заменить (новая БД, новый message broker, новый email-провайдер) без изменения domain. Работает только тогда, когда domain layer определяет интерфейс, а infrastructure реализует его.

**Независимая тестируемость** — domain можно тестировать с чистыми in-memory двойниками. Application layer — с fake infrastructure. Для проверки бизнес-правила не нужен end-to-end тест.

**Навигируемость** — инженеры знают, где искать каждый тип изменений. Изменение URL-роута: Presentation. Изменение потока use case'а: Application. Изменение бизнес-правила: Domain. Изменение SQL: Infrastructure.

Стоимость: каждое сквозное изменение (добавить поле в сущность, выставить его в API, сохранить в БД) требует затронуть четыре файла в четырёх пакетах. Слоистая архитектура обменивает горизонтальную модульность на вертикальную жёсткость. Добавление функциональности — вертикальный разрез через все слои; архитектура делает этот разрез явным и тем самым несколько утомительным.

## Антипаттерн architecture sinkhole

Architecture sinkhole — это сбой, описанный во вступительной истории: слой, не добавляющий ценности и просто проксирующий вызовы в нижележащий слой.

```
// Сервис — чистый sinkhole — не добавляет ничего
class OrderService {
  async getOrder(id: string) {
    return this.orderRepository.findById(id);  // нулевое преобразование, нулевая логика
  }
  async createOrder(dto: CreateOrderDto) {
    return this.orderRepository.save(dto);  // нулевая валидация, нулевая доменная логика
  }
}
```

Sinkhole-сервис не просто бесполезен — он вреден. Он добавляет косвенность без ценности. Каждое изменение затрагивает лишний файл. Он приучает команду рефлекторно добавлять методы в сервис, даже когда тому нечего привнести.

Sinkhole — не признак того, что слоистость была ошибкой. Это признак того, что domain layer выхолощен. Если он не содержит реальной логики, application layer нечего оркестрировать, и сервисный слой становится прокси. Исправление — не убрать слои, а вернуть логику туда, где ей место: в domain (урок 03-02 разбирает это подробно как паттерн anemic-domain-model).

<Inset kind="note" lang="ru">
Марк Ричардс и Нил Форд рекомендуют «правило 80/20» для sinkhole: если более 20% запросов через слой просто проходят насквозь без преобразования, архитектура накапливает sinkhole-долг. Это не точная метрика, а диагностика — если вы регулярно пишете пустые методы делегирования, слоевая структура не окупается.
</Inset>

<Quiz
  id="arch-03-01-quiz-1"
  lessonSlug="01-n-tier"
  lang="ru"
  question="На B2B-платформе заказов добавляется новое правило: заказы стоимостью более $10 000 должны быть одобрены старшим менеджером перед подтверждением. Куда относится эта логика в четырёхслойной архитектуре и почему?"
  choices={[
    { label: "Domain layer. Правило «заказы выше $10 000 требуют одобрения» — это бизнес-инвариант: оно определяет, как выглядит валидный переход состояния заказа. Бизнес-инварианты принадлежат domain layer, а не application или presentation, чтобы они применялись независимо от того, какой интерфейс инициирует операцию.", correct: true },
    { label: "Application layer. Воркфлоу одобрения — это оркестрация use case'а и принадлежит application layer, а не domain.", misconception: "Оркестрация ТОГО, КАК запрашивается одобрение (уведомить менеджера, дождаться ответа, продолжить), принадлежит application layer. Но правило О ТОМ, ЧТО одобрение требуется для заказов выше $10 000 — это бизнес-инвариант, свойство того, что делает заказ валидным. Инварианты принадлежат domain layer, чтобы их нельзя было обойти ни одним use case'ом, пропустившим проверку в application layer." },
    { label: "Presentation layer. Правило должно применяться на уровне API через валидацию запроса.", misconception: "Валидация в presentation layer защищает от некорректного ввода (отсутствующие поля, неверные типы). Бизнес-правила — правила, управляющие переходами доменного состояния — должны жить в domain, иначе их можно обойти через любой код, напрямую вызывающий нижележащие слои. Правило, применяемое только в контроллере, может быть обойдено batch job или фоновым воркером, вызывающим репозиторий напрямую." },
    { label: "Infrastructure layer. Правило должно применяться через constraint базы данных.", misconception: "Ограничения БД применяют инварианты уровня данных (NOT NULL, уникальность, ссылочная целостность). Но условные правила воркфлоу — «если amount > $10 000, требуется запись об одобрении» — слишком сложны для constraint БД и слишком насыщены бизнес-семантикой, чтобы принадлежать infrastructure. Infrastructure реализует персистентность; бизнес-правилами она не владеет." },
  ]}
/>

<Quiz
  id="arch-03-01-quiz-2"
  lessonSlug="01-n-tier"
  lang="ru"
  question="Команда использует strict layering. Инженер предлагает, чтобы domain layer напрямую импортировал библиотеку логирования для записи нарушений инвариантов. Что это нарушает и каков правильный подход?"
  choices={[
    { label: "Это нарушает strict layering: импорт библиотеки логирования связывает domain с инфраструктурной задачей (транспорт лога, конфигурация, формат). В strict layering domain не имеет зависимостей на infrastructure. Правильный подход — определить интерфейс IDomainEventLogger в domain layer и внедрить реализацию из infrastructure layer.", correct: true },
    { label: "Логирование — сквозная задача, поэтому его импорт в любом месте допустим — это не считается инфраструктурной зависимостью.", misconception: "Библиотеки логирования — это infrastructure. Они пишут в файлы, потоки или внешние сервисы — всё это I/O-задачи. В strict layering любой I/O принадлежит infrastructure. Включая логирование. Domain layer остаётся чистым, определяя интерфейс для любых нужд наблюдаемости; infrastructure предоставляет реализацию." },
    { label: "Это нарушает слоистость только если библиотека логирования также импортирует типы domain.", misconception: "Направление нарушения — domain импортирует infrastructure, а не наоборот. Даже если библиотека логирования не знает о domain, наличие у domain compile-time зависимости на библиотеку означает, что замена библиотеки требует изменения domain. Именно это предотвращает strict layering." },
    { label: "Domain layer может импортировать стабильные, широко используемые инфраструктурные библиотеки без нарушения слоистости — правило применяется только к volatile-зависимостям.", misconception: "Правило слоистости касается архитектурной категории, а не стабильности библиотеки. Логирование — это infrastructure независимо от того, насколько оно стабильно. Если domain может импортировать «стабильную» infrastructure, граница становится переговорной и постепенно всё становится «достаточно стабильным» для импорта. Strict layering требует жёсткой границы." },
  ]}
/>

<Quiz
  id="arch-03-01-quiz-3"
  lessonSlug="01-n-tier"
  lang="ru"
  question="У команды есть OrderService с 50 методами, где 40 методов просто вызывают соответствующий метод OrderRepository и возвращают результат без изменений. Какую архитектурную проблему это описывает и какова её первопричина?"
  choices={[
    { label: "Антипаттерн architecture sinkhole: 80% application layer проходит насквозь без трансформации или оркестрации. Первопричина — у domain layer нет реальной логики: бизнес-правила, которые должны жить в domain, либо ушли в репозиторий (infrastructure), либо вообще не были написаны. Сервисному слою нечего оркестрировать, поэтому он становится прокси.", correct: true },
    { label: "Application layer слишком большой — его нужно разбить на меньшие сервисы. Первопричина — плохой дизайн сервисов.", misconception: "Проблема не в размере — в том, что методы не добавляют ценности. Разбить 50 пустых методов делегирования на два сервиса по 25 методов — всё равно 50 sinkhole. Первопричина — отсутствующий domain layer, а не структура application layer." },
    { label: "Repository layer делает слишком много работы. Исправление — добавить логику в методы сервиса.", misconception: "Добавление произвольной логики в методы сервиса не исправляет sinkhole — это лишь перемещает поведение из infrastructure в application, не помещая его в domain. Исправление — определить бизнес-правила, инварианты и преобразования, принадлежащие domain layer, и переместить их туда. Доменная логика должна жить в доменных сущностях и доменных сервисах, а не в application-сервисах." },
    { label: "Это приемлемо и ожидаемо в CRUD-приложении — слоистая архитектура не подходит для простых паттернов доступа к данным.", misconception: "Для по-настоящему простого CRUD без бизнес-правил более тонкая архитектура (без отдельного application или domain layer) — разумный выбор. Но sinkhole не «приемлем» — это предупреждение, что слои создают накладные расходы без выгоды. Честный ответ: либо добавить реальную доменную логику (если у domain есть правила), либо упростить архитектуру, убрав ненужные слои." },
  ]}
/>

</Explanation>

<KeyTakeaway>N-tier слоистая архитектура разделяет presentation, application, domain и infrastructure на горизонтальные слои, каждый из которых зависит только вниз. Даёт замещаемость и независимую тестируемость, когда границы соблюдаются. Architecture sinkhole — слои без добавленной ценности — возникает, когда domain layer выхолощен реальной логики.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="arch-03-01-retrieval"
  lang="ru"
  questions={[
    { q: "Каковы четыре стандартных слоя n-tier архитектуры и конкретная ответственность каждого?", a: "Presentation: обрабатывает HTTP, парсинг запросов, сериализацию, аутентификационные токены — всё, что связано с веб-протоколом. Application: оркестрирует use case'ы, управляет транзакциями, последовательно вызывает domain и infrastructure, применяет авторизацию. Domain: содержит бизнес-сущности, value objects и правила, которые ими управляют — инварианты, правила переходов состояний и политики бизнеса. Infrastructure: реализует технические порты, нужные domain и application — репозитории БД, отправители email, публикаторы очередей, клиенты внешних API. Ключевая дисциплина: каждый слой знает о нижележащих и ничего — о вышележащих. Domain ничего не знает об infrastructure; application ничего не знает о HTTP." },
    { q: "В чём разница между strict и relaxed layering и каков трейдофф?", a: "Strict layering: каждый слой может зависеть только от непосредственно нижележащего. Presentation вызывает только Application. Application вызывает Domain и (через интерфейсы) Infrastructure. Domain не имеет импортов из Infrastructure. Relaxed layering: каждый слой может зависеть от любого нижележащего, пропуская промежуточные. Presentation может напрямую вызвать Domain; Application может импортировать классы Infrastructure напрямую. Трейдофф: strict layering даёт наиболее сильную изоляцию — domain можно тестировать и понимать без знания инфраструктуры — но требует большего boilerplate интерфейсов и дисциплины. Relaxed layering прагматичен для простых случаев, но размывается легче; как только пропуск слоёв разрешён для удобства, паттерн деградирует." },
    { q: "Что такое антипаттерн architecture sinkhole и какова его первопричина?", a: "Sinkhole — это слой, не добавляющий трансформации, оркестрации или логики: он просто вызывает нижележащий слой и возвращает результат без изменений. Метод сервиса, который только вызывает один метод репозитория — sinkhole. Первопричина — выхолощенный domain layer: если он не содержит реальной бизнес-логики, application layer нечего оркестрировать, и он становится прокси. Sinkhole — не повод перестраивать слои, а сигнал наполнить domain layer реальными инвариантами, правилами переходов состояний и политиками бизнеса. Диагностика: если более ~20% запросов через слой проходят без трансформации, слой накапливает sinkhole-долг." },
  ]}
/>

<Recap lang="ru">
N-tier слоистая архитектура — стартовая структура для большинства приложений по прямой причине: она адресует наиболее распространённую силу — необходимость менять infrastructure (сменить БД, обновить фреймворк, добавить канал доставки) без изменения бизнес-логики, и тестировать бизнес-логику без реальной базы данных.

Четыре слоя — presentation, application, domain, infrastructure — каждый владеет отдельной ответственностью. Правило зависимостей внутри слоистой архитектуры: каждый слой зависит только вниз. Presentation вызывает Application, Application вызывает Domain, Domain определяет интерфейсы, которые Infrastructure реализует. Ни один слой не импортирует из вышележащего.

Strict layering обязывает каждый слой зависеть только от непосредственно нижележащего. Relaxed layering допускает пропуск слоёв в прагматичных случаях. Оба варианта валидны; strict layering даёт более сильную изоляцию ценой большей дисциплины.

Architecture sinkhole — это сбой: слой, проксирующий вызовы без добавленной ценности. Sinkhole'ы появляются, когда domain layer пуст — когда бизнес-правила ушли в репозиторий или просто не были зафиксированы в доменных объектах. Исправление — не убрать слои, а восстановить ответственность domain layer. Следующий урок разбирает, что это означает: разницу между anemic domain model (пакеты данных + логика в сервисах) и rich domain model (поведение вместе с данными, которые оно охраняет).
</Recap>
