---
slug: "02-anemic-vs-rich-domain"
lang: ru
track: "architecture-patterns"
unit: "03-layered-architecture"
order: 2
title: "Anemic vs rich domain model"
summary: "Анемичная доменная модель помещает всё поведение в сервисы, оставляя доменные объекты чистыми контейнерами данных. Это признанный антипаттерн для сложных доменов, уничтожающий инкапсуляцию. Для простого CRUD — нередко прагматичный выбор."
estMin: 22
status: ready
lessonType: topic
level: middle
concepts:
  - anemic-domain-model
  - rich-domain-model
  - domain-invariant
  - encapsulation
  - transaction-script
  - domain-service
prereqs: []
sources:
  - https://martinfowler.com/bliki/AnemicDomainModel.html
  - https://martinfowler.com/eaaCatalog/transactionScript.html
  - https://www.informit.com/store/domain-driven-design-tackling-complexity-in-the-heart-9780321125217
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
На B2B-платформе заказов был класс Order с двадцатью полями: id, status, customerId, lineItems, invoiceId, totalAmount, approvedBy, approvedAt, confirmedAt, cancelledAt и другие. У класса были геттеры и сеттеры для каждого поля — и ничего больше. Вся логика жила в OrderService. Там был метод `confirmOrder`, метод `cancelOrder`, метод `approveOrder` и метод `applyDiscount`. Через три месяца после запуска платформы в продакшене нашли баг: отменённый заказ был выставлен на счёт. Первопричина: `confirmOrder` и billing-задание оба изменяли поле status заказа напрямую — одно через `setStatus('confirmed')`, другое через ORM-обновление, — не зная друг о друге. Инвариант «отменённый заказ нельзя подтвердить или выставить на счёт» существовал в комментариях и в головах команды, но нигде в коде. Поскольку класс Order не владел никаким поведением, он не применял никаких правил. Любой код в системе мог установить любое поле в любое значение в любой момент. Модель была технически объектно-ориентированной — она использовала класс — но ничего реально не инкапсулировала.
</Hook>

<Crux>Anemic model разделяет данные и поведение — инварианты дрейфуют; rich model совмещает поведение с данными, которые оно защищает.</Crux>

<Explanation>

## Как выглядит anemic domain model

Мартин Фаулер ввёл термин «Anemic Domain Model» в 2003 году для описания паттерна, ставшего повсеместным: доменные объекты — не более чем именованные структуры данных (геттеры, сеттеры, никакого реального поведения) — в сочетании с классами-сервисами, содержащими всю логику.

```typescript
// Anemic: Order — пакет данных
class Order {
  id: string;
  status: string;
  lineItems: LineItem[];
  totalAmount: number;
  customerId: string;
  approvedBy?: string;
  approvedAt?: Date;

  // Только геттеры и сеттеры — никакого поведения, никакого применения правил
  setStatus(s: string) { this.status = s; }
  setApprovedBy(userId: string) { this.approvedBy = userId; }
}

// Вся логика в сервисе
class OrderService {
  async confirmOrder(orderId: string, userId: string) {
    const order = await this.repo.findById(orderId);
    if (order.status === 'pending') {
      order.setStatus('confirmed');
      order.setApprovedBy(userId);
      order.approvedAt = new Date();
      await this.repo.save(order);
    }
  }
}
```

Сервис знает правила. Объект Order ничего не применяет. Любой код в системе может вызвать `order.setStatus('confirmed')` напрямую, обойдя все проверки в `OrderService`. Инвариант «только pending-заказы можно подтвердить» существует только внутри одного метода одного класса-сервиса.

Вердикт Фаулера: это антипаттерн, потому что он лишает смысла объектно-ориентированный дизайн. Объект, не владеющий никаким поведением и не применяющий никаких инвариантов — это не доменная модель, а struct из C с синтаксисом Java.

## Почему это становится проблемой — при сложных инвариантах

Деградация anemic-модели появляется постепенно, и именно тогда, когда **инварианты становятся сложными или множественными**.

На B2B-платформе правила, управляющие жизненным циклом заказа, со временем умножились:
- Pending-заказ можно подтвердить только если у него есть хотя бы одна позиция.
- Заказ на сумму свыше $10 000 требует одобрения перед подтверждением.
- Подтверждённый заказ можно отменить только в течение 24 часов.
- Отменённый заказ нельзя выставить на счёт, подтвердить или переоткрыть.
- Итоговая сумма заказа всегда должна равняться сумме его позиций.

Каждое из этих правил было реализовано внутри отдельного метода сервиса. `confirmOrder` проверял два из них. `cancelOrder` — одно. Billing-задание — ни одного: оно было написано другой командой, не знавшей о правилах жизненного цикла заказа. Баг был не ошибкой программирования — это была структурная ошибка. Правила были разбросаны по методам сервиса без гарантии быть единственным путём мутации заказа.

<FlowDiagram
  label="Anemic model: поведение разбросано по сервисам"
  nodes={[
    { id: "order", label: "Order (anemic)", sub: "только данные — поля и сеттеры" },
    { id: "os", label: "OrderService", sub: "confirmOrder, cancelOrder, approve" },
    { id: "billing", label: "BillingJob", sub: "читает и мутирует Order напрямую" },
    { id: "report", label: "ReportingService", sub: "читает и обновляет поле status" },
  ]}
  edges={[
    { from: "os", to: "order", label: "мутирует через сеттеры" },
    { from: "billing", to: "order", label: "мутирует через сеттеры" },
    { from: "report", to: "order", label: "мутирует через сеттеры" },
  ]}
  caption="В anemic-модели каждый сервис может залезть в объект Order и мутировать любое поле. Нет единственного владельца инвариантов — правила там, где разработчик их написал. Новый вызывающий (BillingJob, ReportingService, фоновая задача) может обойти их все."
/>

**Инкапсуляция — это ключевое свойство, которое anemic-модель уничтожает.** Инкапсуляция — это не сокрытие полей за геттером/сеттером: это просто синтаксическая обёртка. Настоящая инкапсуляция означает, что внутреннее состояние объекта может изменяться только через операции, применяющие инварианты объекта. Если любой внешний код может вызвать `setStatus('confirmed')` напрямую, объект Order не даёт никакой инкапсуляции, независимо от того, является ли поле `status` приватным.

## Rich domain model как альтернатива

Rich domain model помещает поведение вместе с данными, которыми оно управляет. Объект Order владеет своими переходами состояний и применяет инварианты на границе каждой операции:

```typescript
// Rich: Order владеет своими инвариантами
class Order {
  private status: OrderStatus;
  private lineItems: LineItem[];
  private totalAmount: Money;
  private approvalRecord?: ApprovalRecord;

  confirm(approvedBy: UserId): void {
    if (this.status !== OrderStatus.Pending) {
      throw new InvalidOrderTransitionError(
        `Нельзя подтвердить заказ в статусе ${this.status}`
      );
    }
    if (this.lineItems.length === 0) {
      throw new OrderInvariantViolation('Нельзя подтвердить заказ без позиций');
    }
    if (this.totalAmount.greaterThan(Money.of(10_000, 'USD')) && !this.approvalRecord) {
      throw new OrderInvariantViolation('Заказы свыше $10 000 требуют предварительного одобрения');
    }
    this.status = OrderStatus.Confirmed;
    this.confirmedAt = new Date();
  }

  cancel(): void {
    if (this.status === OrderStatus.Cancelled) {
      throw new InvalidOrderTransitionError('Заказ уже отменён');
    }
    this.status = OrderStatus.Cancelled;
    this.cancelledAt = new Date();
  }
}
```

Теперь BillingJob не может установить `status = 'confirmed'` напрямую — он должен вызвать `order.confirm()`, которая применяет каждое соответствующее правило. Баг из вступительной истории становится невозможным: вызов `confirm()` на отменённом заказе выбросит ошибку. Ни один второй вызывающий не может обойти это.

<Inset kind="why" lang="ru">
Почему совмещение поведения с данными важнее, чем просто стилевое предпочтение? Потому что доменный объект становится единственной точкой истины о том, как выглядит валидное состояние. С anemic-моделью истина распределена по каждому методу каждого сервиса, когда-либо трогавшего объект — и по каждому разработчику, который когда-либо напишет новый метод. С rich-моделью истина в одном месте. Можно прочитать класс Order и знать каждое правило перехода состояния без чтения остального кода. Это свойство важнее всего при сложном домене и большой команде.
</Inset>

## Честный контраргумент: когда anemic подходит

Критика Фаулера верна для сложных доменов со значимыми инвариантами. Это НЕ универсальный закон. Anemic-модель вполне уместна когда:

**Домен — действительно CRUD.** Сервис управления профилями пользователей — создать, прочитать, обновить, удалить записи — не имеет state machine, многоэтапного жизненного цикла, инвариантов сложнее «email должен быть уникальным». Rich domain model здесь добавит косвенность и boilerplate без отдачи. Anemic-модель честна в том, что собой представляет домен.

**Домен прост и стабилен.** Таблицы конфигурации, справочники, референсные данные — это управление данными, а не доменное моделирование. ORM-сущности с геттерами и сеттерами — правильный инструмент.

**Приложение краткосрочное или одноразовое.** Скрипт миграции данных, одноразовый ETL-job, внутренняя admin-панель для трёх пользователей — стоимость создания rich domain model не оправдана отдачей.

Метка антипаттерна применяется, когда команда использует anemic-модель для сложного домена со значимыми инвариантами жизненного цикла — и затем испытывает последствия (нарушения инвариантов, разбросанная логика, невозможность рассуждать о состоянии), настаивая, что сделала правильный выбор. Проблема — не религиозный вопрос anemic vs rich. Проблема — использование неправильного инструмента для реальной сложности домена.

<Inset kind="note" lang="ru">
Transaction Script (тоже термин Фаулера) — это anemic-подход, доведённый до логического завершения: процедура, выполняющая серию шагов для завершения бизнес-транзакции, без персистентных доменных объектов вообще. Transaction Script хорошо работает для простых воркфлоу, где процедурная ясность перевешивает потерю инкапсуляции. Становится проблемой, когда процедуры разрастаются, пересекаются в мутациях состояния и начинают дублировать одни и те же бизнес-правила в разных местах. Процедурный подход масштабируется с простотой; rich domain model масштабируется со сложностью инвариантов.
</Inset>

<Quiz
  id="arch-03-02-quiz-1"
  lessonSlug="02-anemic-vs-rich-domain"
  lang="ru"
  question="На B2B-платформе есть инвариант: подтверждённый заказ можно отменить только в течение 24 часов с момента подтверждения. Это правило проверяется в `OrderService.cancelOrder()`. Новое фоновое задание читает заказы и вызывает `order.setStatus('cancelled')` напрямую для очистки устаревших заказов. Какое структурное свойство anemic-модели позволило появиться этому багу?"
  choices={[
    { label: "Anemic-объект Order не даёт никакой инкапсуляции: любой вызывающий может мутировать поле status напрямую через сеттеры, обходя проверку инварианта в OrderService. Поскольку поведение и данные разделены, инвариант существует только в одном методе сервиса — он невидим для любого вызывающего, не знающего об этом методе.", correct: true },
    { label: "Фоновое задание написано неверно — оно должно было вызвать OrderService.cancelOrder() вместо прямой установки поля. Исправление — поправить задание, а не менять архитектуру.", misconception: "Вызов правильного метода сервиса — одно исправление для одного вызывающего. Структурная проблема в том, что ничто не обязывает вызывающего проходить через сервис — API объекта Order допускает прямую мутацию. Следующий новый вызывающий (интеграция третьей стороны, скрипт миграции, новый член команды) столкнётся с тем же приглашением обойти проверку. Архитектура делает правильный путь лёгким для упущения, а неправильный — лёгким для выбора." },
    { label: "Инвариант — бизнес-правило и должен применяться через constraint базы данных, а не в коде.", misconception: "Ограничения БД могут применять простые правила данных (NOT NULL, уникальность, check constraints). Условное правило, основанное на времени — «отмена валидна только если now минус confirmedAt меньше 24 часов» — слишком динамично и насыщено бизнес-семантикой для constraint БД. Бизнес-правила такого уровня сложности принадлежат доменной модели." },
    { label: "Баг возник из-за того, что OrderService и фоновое задание не были в транзакции — исправление — добавить distributed locking.", misconception: "Distributed locking предотвращает конкурентные гонки — два вызывающих, мутирующих одну запись одновременно. Это другая проблема. Этот баг — не проблема конкурентности; это проблема обхода инварианта. Одиночный вызывающий, записывающий устаревшую отмену — вот проблема, и она возникла бы даже без конкурентности. Первопричина — проверка инварианта не на границе мутации." },
  ]}
/>

<Quiz
  id="arch-03-02-quiz-2"
  lessonSlug="02-anemic-vs-rich-domain"
  lang="ru"
  question="Команда строит billing-модуль B2B-платформы. Модуль создаёт счета, хранит записи платежей и связывает их с заказами. Нет сложных переходов состояний — счёт создаётся один раз, помечается оплаченным один раз и больше не меняет состояния. Стоит ли использовать rich или anemic-модель для Invoice?"
  choices={[
    { label: "Anemic-модель уместна. У Invoice тривиальный жизненный цикл (создан → оплачен) без ветвящихся инвариантов, без сложных условий, без необходимости предотвращать обход проверок разными вызывающими. Rich-модель добавит boilerplate-косвенность без структурной выгоды.", correct: true },
    { label: "Rich-модель всегда. Доменные объекты должны владеть своими переходами состояний независимо от того, насколько они просты.", misconception: "Rich-модель оправдывает свою стоимость, когда домен имеет значимую сложность — несколько вызывающих, которые могут обойти инварианты, сложные state machine, растущие правила. Для простой двухсостоянниой сущности с одним переходом rich-модель добавляет косвенность и церемонию, не предотвращая никакой реальной структурной проблемы. Прагматичный выбор — anemic-модель, с пониманием, что если сложность Invoice вырастет, модель нужно пересмотреть." },
    { label: "Использовать сервисный слой, применяющий переходы состояний Invoice, и оставить Invoice как запись данных — это индустриальный стандарт.", misconception: "Сервис, применяющий двухсостоянный переход на объекте-записи Invoice — это именно anemic-модель. Вопрос в том, подходит ли это для сложности Invoice, а не в том, есть ли у этого отраслевой ярлык. Для двухсостоянной сущности — разумный выбор, но осознанный, а не рефлекторный." },
    { label: "Ни то ни другое — для billing Transaction Script (процедурный код, без доменных объектов) — единственный правильный выбор.", misconception: "Transaction Script — допустимый вариант для простой billing-логики, но не единственный. Вопрос в том, какой уровень абстракции соответствует сложности. Для Invoice с двумя состояниями и без ветвлений anemic-модель (или даже Transaction Script) работает. Для Invoice со сложной логикой кредит-нот, частичных платежей, споров и audit trail более богатая модель оправдывает себя." },
  ]}
/>

<Quiz
  id="arch-03-02-quiz-3"
  lessonSlug="02-anemic-vs-rich-domain"
  lang="ru"
  question="При рефакторинге anemic-объекта Order в rich domain model команда обнаруживает, что нельзя просто добавить методы вроде `confirm()` в класс Order — существующий код вызывает `order.setStatus()` в 47 разных местах. Что говорит это распределение и что оно означает для рефакторинга?"
  choices={[
    { label: "Это говорит о том, что инварианты применялись непоследовательно в 47 местах — где-то с проверками, где-то без. Рефакторинг должен определить все 47 точек вызова, выяснить, какие проверки каждая из них выполняла (или не выполняла), объединить правильную логику инварианта в Order.confirm(), Order.cancel() и т.д., и заменить все вызовы сеттеров соответствующей доменной операцией. Распределение — не причина избегать рефакторинга, а свидетельство того, насколько он необходим.", correct: true },
    { label: "47 точек вызова означают, что доменная модель слишком велика и её нужно разбить на меньшие сервисы перед рефакторингом.", misconception: "Количество точек вызова — симптом anemic-модели (отсутствие применения на границе приглашает всех мутировать напрямую), а не свидетельство того, что домен слишком велик. Разбивка на меньшие сервисы распределит 47 точек дальше, не устранив пробел в применении инвариантов. Важный рефакторинг — введение доменного метода, применяющего инвариант, а затем замена разбросанных сеттеров на него." },
    { label: "Существующий код верен — 47 мест, вызывающих setStatus, означают, что логика хорошо распределена. Рефакторинг не нужен.", misconception: "Распределение логики мутации — противоположность хорошего дизайна: это означает, что инвариант повторяется (или пропускается) 47 раз. Согласованность в распределённой anemic-модели поддерживается соглашением и ревью, а не структурой. Одна пропущенная точка вызова порождает баг. Цель рефакторинга — сделать правильный путь единственным путём." },
    { label: "Решение — добавить триггер БД, применяющий state machine, устраняя необходимость рефакторинга кода приложения.", misconception: "Триггер БД может применять некоторые ограничения состояний, но не может получить доступ к контексту уровня приложения (объект approval record, уровень авторизации пользователя, бизнес-правила для перехода). Кроме того, триггеры БД переносят бизнес-логику в infrastructure, углубляя нарушение слоистости. Правильное исправление — доменный метод, инкапсулирующий инвариант на уровне приложения." },
  ]}
/>

</Explanation>

<KeyTakeaway>Anemic domain model разделяет данные и поведение, управляющее ими, разбрасывая применение инвариантов по методам сервисов. Это антипаттерн Фаулера для сложных доменов: любой вызывающий может обойти проверки. Rich domain model совмещает поведение с данными, применяя инварианты на границе каждого перехода состояния. Для по-настоящему простых CRUD-доменов anemic-модель прагматична и уместна — метка антипаттерна применяется, когда сложность реальна, а модель остаётся тонкой.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-03-02-retrieval"
  lang="ru"
  questions={[
    { q: "Что делает anemic domain model антипаттерном и какое свойство домена определяет, является ли это реальной проблемой?", a: "Критика Фаулера: anemic domain model использует классы как именованные структуры данных (поля + геттеры/сеттеры) со всем поведением в сервисах. Это уничтожает инкапсуляцию — любой код может мутировать любое поле через сеттеры, обходя любую проверку инварианта в сервисном слое. Новый вызывающий, новая интеграция, фоновое задание другой команды — все могут обойти сервис и мутировать состояние напрямую. Инварианты существуют только как неявные соглашения, поддерживаемые ревью и командной дисциплиной, а не структурным принуждением. Свойство, делающее это реальной проблемой — сложность домена: когда он имеет значимые инварианты жизненного цикла, несколько вызывающих и правила, которые можно нарушить, обходя сервис, anemic-модель не справляется со своей основной задачей. Для по-настоящему простых CRUD-доменов (нет значимых инвариантов, однопереходные сущности, референсные данные) anemic-модель уместна." },
    { q: "Что означает настоящая инкапсуляция в контексте доменного объекта и как rich domain model её обеспечивает?", a: "Настоящая инкапсуляция — это не сделать поля приватными или добавить методы геттер/сеттер. Это гарантия того, что состояние объекта может изменяться только через операции, применяющие его инварианты. Доменный объект с приватным полем `status` и публичным `setStatus(string)` имеет синтаксическую приватность, но не инкапсуляцию — любой вызывающий всё равно может установить любое значение статуса. Rich domain model обеспечивает инкапсуляцию, заменяя мутацию через сеттеры методами, выражающими намерение: `order.confirm()`, `order.cancel()`, `order.approve()`. Каждый метод содержит полную логику инварианта для данного перехода. Поле состояния напрямую не устанавливается — оно меняется только как следствие вызова метода, проверившего все предусловия. Любой вызывающий, желающий изменить состояние Order, должен пройти через метод, применяющий правила, делая нарушения инвариантов структурно невозможными, а не просто конвенционально нежелательными." },
    { q: "Когда anemic domain model — подходящий выбор и как понять, что домен из неё вырос?", a: "Anemic-модель уместна: когда домен по-настоящему CRUD (нет значимого жизненного цикла, нет инвариантов сложнее ограничений целостности данных); когда приложение простое и стабильное (таблицы конфигурации, референсные данные, одноразовые скрипты); когда команда понимает трейдофф и сделала осознанный выбор. Домен вырос из неё, когда: баги появляются из-за обхода сервиса вызывающими (прямой сеттер было проще вызвать); логика инвариантов дублируется в нескольких методах сервиса; новый член команды или интеграция пишут код, мутирующий состояние, не зная правил; ревью регулярно ловит прямые мутации полей. Признак — разбросанное применение: если поиск по кодовой базе прямых мутаций состояния доменного объекта даёт результаты в пяти разных файлах, инварианты поддерживаются только соглашением. Это момент для миграции на более богатую модель." },
  ]}
/>

<Recap lang="ru">
Anemic domain model — естественный результат построения слоистой архитектуры с последующим выхолащиванием domain layer. Доменные объекты становятся пакетами данных — поля, геттеры, сеттеры — а сервисы несут всю логику. Внешне это объектно-ориентированный дизайн, потому что используются классы, но ни одного из преимуществ инкапсуляции, ради которых существует ООП, он не даёт.

Критика Фаулера: anemic-модель — антипаттерн для сложных доменов, потому что разделяет поведение и данные, которые оно должно защищать. Любой вызывающий может обойти сервис с правилами и мутировать состояние доменного объекта напрямую. Инварианты дрейфуют в неявные соглашения, поддерживаемые ревью и командной дисциплиной, а не структурным принуждением. Когда домен усложняется, а количество вызывающих растёт — эти соглашения ломаются.

Rich domain model совмещает поведение с данными. `Order.confirm()` несёт полную логику инварианта перехода подтверждения. Ни один вызывающий не может обойти её. API доменного объекта — набор валидных операций, каждая из которых применяет свои предусловия.

Честный контраргумент: для по-настоящему простых CRUD-доменов — двухсостоянные сущности, референсные данные, краткосрочные скрипты — anemic-модель прагматична и уместна. Boilerplate и косвенность rich-модели стоят дороже, чем дают. Решение должно приниматься осознанно, исходя из реальной сложности инвариантов домена, а не как рефлекторный выбор паттерна.

Следующий урок разбирает, что происходит, когда слоистость применяется, но дисциплина не выдерживается: transaction-script trap, layer-skipping и fat service, который становится god object приложения.
</Recap>
