infographic compare-hierarchy-row-letter-card-rounded-rect-node
data
  title Слоистая архитектура: 3 утечки
  compares
    - label Transaction-script trap
      children
        - label Признак: domain пуст, сервис = app + domain
        - label Пробел: ничто не обязывает domain хранить логику
        - label Решение: rich domain core (hexagonal)
    - label Layer-skipping
      children
        - label Признак: контроллер импортирует репозиторий
        - label Пробел: компилятор не запрещает импорт вниз
        - label Решение: domain владеет портами (инверсия)
    - label Fat service / god object
      children
        - label Признак: 61 метод, 11 импортов infra за 18мес
        - label Пробел: граница use case не определена
        - label Решение: каждый use case = один primary port
theme
  palette #6366f1 #14b8a6 #f59e0b
