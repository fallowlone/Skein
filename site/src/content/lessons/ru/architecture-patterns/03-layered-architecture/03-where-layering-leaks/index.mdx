---
slug: "03-where-layering-leaks"
lang: ru
track: "architecture-patterns"
unit: "03-layered-architecture"
order: 3
title: "Где слоистость протекает"
summary: "Слоистая архитектура протекает через три сбоя: transaction-script trap (процедуры вместо слоёв), layer-skipping (presentation тянется в infrastructure) и fat service. Каждый — сила, мотивировавшая hexagonal и clean architecture."
estMin: 22
status: ready
lessonType: topic
level: middle
concepts:
  - transaction-script-trap
  - layer-skipping
  - leaky-abstraction
  - fat-service
  - god-object
  - architecture-erosion
prereqs: []
sources:
  - https://martinfowler.com/eaaCatalog/transactionScript.html
  - https://martinfowler.com/bliki/PresentationDomainDataLayering.html
  - https://alistair.cockburn.us/hexagonal-architecture/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Через восемнадцать месяцев после запуска B2B-платформы заказов старший инженер провёл аудит кодовой базы. Четырёхслойная структура была на месте — пакеты были названы правильно. Но `OrderController` разросся до 900 строк, потому что инженеры добавляли туда быструю логику, не рискуя трогать сервис. У `OrderService` было 61 метод, и он напрямую импортировал `PostgresOrderRepository`, `StripeClient`, `SendGridClient`, `PDFGenerator` и `S3Client` — сервисный слой стал слоем интеграции, а не оркестрации. И был метод `getBillingReportData()` в `OrderController`, напрямую вызывавший `OrderRepository.findRawSql()`, потому что разработчику, написавшему его, нужен был специфичный JOIN, который ни один метод сервиса не предоставлял. Слои существовали как названия пакетов. Дисциплина, делающая слои ценными, — сохранение чётких ответственностей и ограниченных импортов каждого слоя — была полностью размыта. Архитектура была слоистой по структуре и big ball of mud на практике.
</Hook>

<Crux>Слоистая архитектура размывается через три предсказуемых утечки: transaction-script trap, layer-skipping и fat service — каждая из них стала силой, которую dependency-inverted архитектуры были призваны устранить.</Crux>

<Explanation>

## Утечка 1: Transaction-script trap

Transaction-script trap — это то, что происходит, когда слоистость применяется структурно, но ментальная модель команды остаётся процедурной. Каждый use case становится длинной процедурой в методе сервиса — шаг 1, шаг 2, шаг 3, если то, то другое — с доменными объектами как пассивными контейнерами данных, которыми процедура манипулирует.

```typescript
// Transaction script, притворяющийся слоистой архитектурой
class OrderService {
  async confirmOrder(orderId: string, userId: string): Promise<void> {
    // Шаг 1: загрузка
    const order = await this.orderRepo.findById(orderId);
    const user = await this.userRepo.findById(userId);
    const invoices = await this.invoiceRepo.findByOrderId(orderId);

    // Шаг 2: валидация (вся логика встроена, ничего в domain)
    if (order.status !== 'pending') throw new Error('Не pending');
    if (!user.hasRole('manager')) throw new Error('Нет прав');
    if (invoices.some(i => i.status === 'overdue')) throw new Error('Просроченные счета');
    if (order.totalAmount > 10000 && !order.approvalId) throw new Error('Требуется одобрение');

    // Шаг 3: мутация (прямая установка полей)
    order.status = 'confirmed';
    order.confirmedAt = new Date();
    order.confirmedBy = userId;

    // Шаг 4: побочные эффекты (сервис напрямую вызывает infrastructure)
    await this.orderRepo.save(order);
    await this.emailClient.send(order.customerEmail, 'Заказ подтверждён', ...);
    await this.eventBus.publish({ type: 'OrderConfirmed', orderId });
  }
}
```

Слои существуют, но логика живёт в одной длинной процедуре внутри сервиса. Тестирование этого метода требует реальной базы данных (или мокирования 5 зависимостей). Добавление нового правила означает редактирование процедуры. Domain layer не имеет поведения — сервис одновременно является и application layer, и domain layer.

Это Transaction Script, маскирующийся под слоистую архитектуру. Пакет сервисов и пакет domain технически присутствуют — но пакет domain пуст от реальной логики, а пакет сервисов поглотил всё.

## Утечка 2: Layer-skipping и leaky abstractions

Layer-skipping происходит, когда вышестоящий слой импортирует из нижестоящего, минуя промежуточные. Канонический пример: контроллер, напрямую импортирующий класс репозитория.

```typescript
// Контроллер импортирует infrastructure напрямую
import { PostgresOrderRepository } from '../infra/PostgresOrderRepository';

class BillingReportController {
  private repo = new PostgresOrderRepository();

  async getBillingReport(req: Request): Promise<Response> {
    // Прямой SQL в контроллере
    const rows = await this.repo.findRawSql(`
      SELECT o.id, o.total_amount, i.invoice_number
      FROM orders o JOIN invoices i ON i.order_id = o.id
      WHERE o.status = 'confirmed' AND o.confirmed_at > $1
    `, [req.query.since]);
    return Response.json(rows);
  }
}
```

Это именно проблема `getBillingReportData` из вступительной истории. Разработчику нужен был специфичный SQL JOIN, которого не предоставлял ни один существующий метод сервиса или domain. Вместо того чтобы добавить метод в правильный слой, он напрямую обратился к infrastructure.

Утечка самоподкрепляется. Как только один контроллер импортирует репозиторий, другие разработчики видят паттерн и повторяют его. За несколько месяцев контроллеры по всему приложению обзаводятся прямыми зависимостями на репозиторий. Infrastructure layer теперь напрямую связан с presentation layer — замена базы данных требует изменения контроллеров.

<FlowDiagram
  label="Layer-skipping: presentation тянется в infrastructure"
  nodes={[
    { id: "ctrl", label: "BillingReportController", sub: "presentation layer" },
    { id: "svc", label: "OrderService", sub: "application layer — обход" },
    { id: "domain", label: "Order (domain)", sub: "domain layer — обход" },
    { id: "repo", label: "PostgresOrderRepository", sub: "infrastructure layer" },
  ]}
  edges={[
    { from: "ctrl", to: "repo", label: "импортирует напрямую — SKIP" },
    { from: "ctrl", to: "svc", label: "нормальный путь" },
    { from: "svc", to: "domain", label: "нормальный путь" },
    { from: "svc", to: "repo", label: "нормальный путь" },
  ]}
  caption="У контроллера два пути: нормальный — через application и domain layer, и ярлык напрямую к infrastructure. Ярлык обходит каждую абстракцию, которую должна была предоставить слоистая архитектура. Как только один пропуск разрешён — паттерн распространяется."
/>

**Leaky abstractions** — более тонкая форма того же сбоя: граница абстракции номинально существует, но детали реализации нижнего слоя просачиваются сквозь неё. Доменный сервис, возвращающий объект `Knex.QueryBuilder` вместо доменного типа, утечёт query builder в вышестоящий слой. Интерфейс репозитория, принимающий параметр `EntityManager`, утечёт ORM в application layer. Вызывающий теперь зависит от деталей реализации, хотя граница интерфейса внешне присутствует.

<Inset kind="why" lang="ru">
Почему происходит layer-skipping? Непосредственная причина — давление: дедлайн, разовая потребность в отчёте, сложный запрос, который быстрее написать в SQL, чем выразить через доменные методы. Глубинная причина: у слоистой архитектуры нет механизма принуждения. Нет ошибки компилятора, когда контроллер импортирует репозиторий. Единственное принуждение — дисциплина, ревью и командные соглашения, которые деградируют под давлением. Именно этот структурный пробел адресует hexagonal architecture: делая домен владельцем его портов (интерфейсов), она превращает правильное направление зависимости в путь наименьшего сопротивления, а не в опциональное соглашение.
</Inset>

## Утечка 3: Fat service и god object

Fat service — конечное состояние приложения, где вся бизнес-логика тяготеет к сервисному слою. Начинается с разумных методов — `createOrder`, `confirmOrder`, `cancelOrder` — и растёт по мере накопления функциональности. Через 18 месяцев у `OrderService` 61 метод и импорты из 11 различных зависимостей.

Fat service имеет структурные свойства god object:
- **Знает обо всём**: инфраструктурные клиенты, доменные сущности, внешние сервисы, запросы для отчётов, логика уведомлений, audit-логирование.
- **От него зависит всё**: presentation layer, фоновые задания, batch-процессоры, интеграционные адаптеры.
- **Не тестируется изолированно**: чтобы создать экземпляр `OrderService`, нужно замокировать 11 зависимостей. Тест для `confirmOrder` должен настроить полное дерево зависимостей.
- **Изменения распространяются повсюду**: изменение `OrderService` может затронуть любого из 61 вызывающего.

Fat service — результат правильного инстинкта слоистости (держать бизнес-логику вне контроллеров) в сочетании с отсутствием domain layer, способного поглотить эту логику. Без rich domain model, хранящего инварианты и сущности, и без чётких границ use case'ов в application layer, сервисный слой становится catch-all.

```
// Что импортирует раздутый OrderService через 18 месяцев:
import { OrderRepository } from '../infra/OrderRepository';
import { InvoiceRepository } from '../infra/InvoiceRepository';
import { UserRepository } from '../infra/UserRepository';
import { StripeClient } from '../infra/StripeClient';
import { SendGridClient } from '../infra/SendGridClient';
import { PDFGenerator } from '../infra/PDFGenerator';
import { S3Client } from '../infra/S3Client';
import { EventBus } from '../infra/EventBus';
import { AuditLogger } from '../infra/AuditLogger';
import { ReportingQueryRunner } from '../infra/ReportingQueryRunner';
import { FeatureFlagClient } from '../infra/FeatureFlagClient';
```

Сервисный слой стал интеграционным слоем. Он не оркестрирует доменную логику — он И ЕСТЬ доменная логика, смешанная с вызовами infrastructure. Вычленить один use case (например, подтверждение заказа) означает распутать его из 61 другого метода, разделяющих то же дерево зависимостей.

<Inset kind="note" lang="ru">
Три утечки не независимы — они усиливают друг друга. Transaction-script trap выхолащивает domain layer, поэтому fat service поглощает логику, которую domain должен был держать. Fat service накапливает всё больше зависимостей на infrastructure, соблазняя обойти его, чтобы добраться до этих зависимостей напрямую. Layer-skipping затем создаёт тесную связь между presentation и infrastructure. В итоге все три сбоя приходят вместе в зрелую кодовую базу, начинавшуюся с добрых архитектурных намерений.
</Inset>

## Почему эти утечки мотивировали hexagonal и clean architecture

Каждая из трёх утечек указывает на структурный пробел в плоской n-tier-слоистости:

**Transaction-script trap** указывает на пробел: нет механизма, обязывающего domain layer содержать реальную логику. Плоский четырёхслойный стек не предотвращает поглощение сервисом всего поведения.

**Layer-skipping** указывает на пробел: нет механизма, предотвращающего импорт вышестоящими слоями нижестоящих. Правило зависимостей — соглашение, а не структурное ограничение. В простой структуре пакетов ничто не мешает контроллеру импортировать репозиторий.

**Fat service** указывает на пробел: нет механизма, определяющего границу use case'а. Класс сервиса может расти без ограничений, поглощая задачи, которые должны быть в отдельных компонентах.

Hexagonal architecture (unit 04) адресует все три, инвертируя зависимость на границе домена. Domain владеет своими портами — интерфейсами, которые он требует от внешнего мира. Ни один класс infrastructure не может быть импортирован domain, потому что domain определяет интерфейс, а infrastructure реализует его. Сервис (application layer) ограничен своим первичным портом, а не классом, случайно находящимся в пакете «services». Внутренность гексагона (domain + application) тестируема без какой-либо infrastructure, потому что infrastructure всегда за интерфейсом, которым владеет domain.

Clean architecture (unit 05) обобщает это до концентрических слоёв с тем же правилом зависимостей: ни один внутренний слой не импортирует внешний. Ядро сущностей, кольцо use case'ов, адаптеры интерфейсов и фреймворки/драйверы разделены жёсткими ограничениями зависимостей — не соглашениями об именовании пакетов.

<Quiz
  id="arch-03-03-quiz-1"
  lessonSlug="03-where-layering-leaks"
  lang="ru"
  question="OrderService команды вырос до 80 методов и импортирует из 12 инфраструктурных классов. Новый инженер предлагает разбить его на OrderCommandService (записи) и OrderQueryService (чтения). Это исправляет проблему fat service и почему?"
  choices={[
    { label: "Это уменьшает размер, но не устраняет первопричину. Fat service вырос потому, что domain layer пуст (логика живёт в сервисе) и границы use case'ов не определены (любой вызывающий добавляет методы в сервис). Разбивка на command/query сервисы вдвое уменьшает количество методов, но не переносит логику в domain и не определяет границы use case'ов. Оба новых сервиса в течение года накопят те же методы снова.", correct: true },
    { label: "Да, разбивка по command и query (CQRS) — это правильное архитектурное исправление для fat service.", misconception: "CQRS — валидный паттерн для разделения моделей чтения и записи, но он не адресует первопричину fat service: отсутствие domain layer с реальной логикой и отсутствие границ use case'ов в application layer. Два fat service (один для команд, один для запросов) имеют те же структурные проблемы — просто с разными именами. CQRS полезен сам по себе, но не является средством от пустого domain." },
    { label: "Проблема fat service неотъемлема от слоистой архитектуры и не может быть исправлена в её рамках — команда должна мигрировать на микросервисы.", misconception: "Микросервисы адресуют задачи деплоя и масштабирования, а не внутреннюю структуру приложения. Микросервис может иметь точно те же проблемы fat service, что и монолит — границы сервисов в микросервисах между развёртываемыми единицами, а не между архитектурными слоями внутри одного сервиса. Проблема fat service исправляется обогащением domain layer и определением границ use case'ов в application layer — это задачи слоистой архитектуры, а не задачи деплоя." },
    { label: "Исправление — добавить больше слоёв: выделенный слой бизнес-правил между application и domain поглотит избыточную логику.", misconception: "Добавление слоёв без изменения места жительства логики просто добавляет больше пустого делегирования. Первопричина в том, что domain layer не имеет реальной логики — исправление в перемещении логики в domain (сущности, value objects, доменные сервисы), а не во введении ещё одного промежуточного слоя." },
  ]}
/>

<Quiz
  id="arch-03-03-quiz-2"
  lessonSlug="03-where-layering-leaks"
  lang="ru"
  question="Интерфейс репозитория IOrderRepository определён в domain layer. Единственный его метод — `findByCustomerAndDateRange(customerId: string, from: Date, to: Date): Promise<Order[]>`. PostgreSQL-реализация строит сложный запрос с несколькими JOIN. Это leaky abstraction?"
  choices={[
    { label: "Нет. Интерфейс раскрывает доменные концепции (customerId, диапазон дат, возвращаемый тип Order) — ни одна из них не специфична для infrastructure. Детали реализации (JOIN'ы, SQL) остаются внутри PostgreSQL-адаптера. Вызывающий знает только доменную концепцию; у него нет знания о том, как выполняется запрос.", correct: true },
    { label: "Да. Наличие сложных JOIN'ов в реализации означает, что абстракция утечёт детали infrastructure.", misconception: "Leaky abstraction — это то, что пересекает границу интерфейса, что вызывающий должен знать. Если интерфейс возвращает доменный тип и принимает доменные параметры, сложность лежащей в основе реализации не имеет значения для вызывающего. Сложный SQL за чистым интерфейсом — хорошая инкапсуляция. Абстракция протекает, когда механизм реализации просачивается в интерфейс: возврат QueryBuilder, принятие EntityManager, возврат объектов сырых SQL-строк." },
    { label: "Да. Сигнатура метода слишком специфична — для каждого варианта запроса потребуется новый метод интерфейса, поэтому детали реализации infrastructure утекут через пролиферацию методов.", misconception: "Пролиферация методов — архитектурный запах, но не leaky abstraction в техническом смысле. Leaky abstraction означает, что детали реализации видны через интерфейс вызывающим. Стабильный, основанный на доменных концепциях интерфейс с многими методами — всё равно чистая абстракция, если все методы говорят доменными терминами. Забота о пролиферации валидна с точки зрения сопровождаемости интерфейса, но это отдельно от вопроса о протечке деталей infrastructure." },
    { label: "Да. Интерфейс в domain layer, но использует инфраструктурные концепции (диапазоны дат — паттерн запросов к БД, а не доменная концепция).", misconception: "Диапазоны дат — не инфраструктурные концепции, а временные бизнес-параметры. «Заказы, оформленные в этот период» — бизнес-потребность, выраженная в доменных терминах. Интерфейс утечёт, если раскроет WHERE-клаузы SQL, query builder'ы или пагинацию через database-offset параметры. Использование DateRange как параметра — доменная концепция." },
  ]}
/>

<Quiz
  id="arch-03-03-quiz-3"
  lessonSlug="03-where-layering-leaks"
  lang="ru"
  question="Команда решает принять hexagonal architecture для исправления трёх утечек слоистости. Как hexagonal architecture структурно предотвращает каждую из трёх утечек, разобранных в этом уроке?"
  choices={[
    { label: "Transaction-script trap: гексагон — domain/application core — владеет своими портами (интерфейсами, которые ему нужны), обязывая реальную доменную логику существовать внутри core, а не как прокси к infrastructure. Layer-skipping: правило зависимостей структурно применяется через владение портами — infrastructure может достичь core только через первичные порты; core никогда не импортирует infrastructure. Fat service: use case'ы ограничены определением своего первичного порта, делая каждый use case явной точкой входа, а не открытым списком методов.", correct: true },
    { label: "Hexagonal architecture предотвращает все три утечки, применяя правила видимости на уровне пакетов — только публичные методы пересекают границы слоёв.", misconception: "Hexagonal architecture не опирается на правила видимости пакетов для своего принуждения. Её структурный механизм — владение портами: domain определяет интерфейсы (порты), которые ему нужны, и infrastructure реализует их. Это инвертирует зависимость так, что infrastructure не может быть импортирован domain — не из-за видимости пакетов, а потому что domain импортирует только собственные интерфейсы, а инфраструктурные адаптеры реализуют эти интерфейсы." },
    { label: "Hexagonal architecture предотвращает утечки, требуя прохождения всего кода через единственную точку входа (первичный порт), создавая бутылочное горлышко, препятствующее обходу.", misconception: "Hexagonal architecture определяет первичные порты (как внешний мир управляет приложением) и вторичные порты (как приложение обращается вовне). Первичных портов может быть несколько — HTTP-адаптер, CLI-адаптер, тест-драйвер — все первичные. Механизм принуждения — направление зависимостей (core владеет своими интерфейсами), а не единственная точка входа." },
    { label: "Hexagonal architecture предотвращает все три утечки, устраняя сервисный слой — вся логика переходит в domain и напрямую вызывается presentation layer.", misconception: "Hexagonal architecture сохраняет application layer — use case'ы или application services, оркестрирующие доменную логику. Она не устраняет оркестрацию; она перемещает её в ограниченный core и инвертирует инфраструктурную зависимость. Presentation layer вызывает через первичный порт в application layer, который делегирует в domain. Сервисный слой не устраняется — он ограничивается." },
  ]}
/>

</Explanation>

<KeyTakeaway>Три утечки слоистой архитектуры — transaction-script trap (domain пуст), layer-skipping (presentation импортирует infrastructure) и fat service (сервис поглощает всё) — это структурные силы, мотивировавшие hexagonal и clean architecture. Обе инвертируют зависимость на границе domain, делая правильное направление структурным, а не конвенциональным.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-03-03-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое transaction-script trap и чем он отличается от легитимного использования паттерна Transaction Script?", a: "Transaction-script trap — это когда команда применяет структурную форму слоистой архитектуры (пакеты controller, service, domain, repository), но пишет всю бизнес-логику как процедурные шаги внутри методов сервиса — с доменными объектами как пассивными держателями данных. Слои существуют как упаковка, но не как разделение ответственностей. Domain layer не имеет реальной логики; сервисный слой поглотил всё как длинные пошаговые процедуры. Отличие от легитимного Transaction Script: легитимное использование — осознанный выбор для действительно простых воркфлоу, где у домена нет значимых инвариантов и процедурная ясность перевешивает стоимость объектно-ориентированного моделирования. Trap — применение процедурной ментальной модели внутри номинально слоистой архитектуры: получаешь накладные расходы четырёх слоёв без выгоды изоляции доменной логики." },
    { q: "Что такое layer-skipping и почему он самоподкрепляется, как только появился?", a: "Layer-skipping — это когда вышестоящий слой минует промежуточные слои и импортирует напрямую из нижестоящего, например, контроллер импортирует репозиторий напрямую вместо прохождения через application и domain layer. Самоподкрепляется потому, что: (1) как только один пропуск виден в кодовой базе, он воспринимается как установившийся паттерн; (2) пропуск часто решает немедленную проблему быстрее, чем добавление метода в правильный слой; (3) нет компиляторного принуждения, останавливающего импорт — дисциплина социальная, а не структурная. Каждый пропуск нормализует следующий. За несколько месяцев контроллеры по всему приложению имеют прямые зависимости на репозиторий, и infrastructure layer теперь связан с presentation layer. Dependency-inverted архитектуры (hexagonal, clean) адресуют это, делая правильное направление структурным: domain владеет своими интерфейсами, поэтому импорт infrastructure из domain невозможен по построению." },
    { q: "Каковы три структурных пробела в плоской n-tier-слоистости, которые обнажают три утечки, и какой архитектурный ответ мотивирует каждый?", a: "Пробел 1 (transaction-script trap): нет механизма, обязывающего domain layer содержать реальную логику. Сервис может поглотить доменное поведение без структурной ошибки. Это мотивирует модель domain-core в hexagonal и clean архитектурах, где core — защищённый, тестируемый центр. Пробел 2 (layer-skipping): нет механизма, предотвращающего импорт вышестоящими слоями нижестоящих. Правило зависимостей — соглашение об именовании, а не структурное ограничение. Hexagonal architecture адресует это, инвертируя зависимость: domain определяет интерфейс порта; infrastructure реализует его. Domain не может импортировать infrastructure, потому что domain импортирует только собственные интерфейсы. Пробел 3 (fat service): нет механизма, определяющего границу use case'а. Границы use case'ов в hexagonal/clean архитектурах — это первичные порты: каждый use case — явная точка входа, а не открытый метод, добавляемый в общий класс сервиса." },
  ]}
/>

<Recap lang="ru">
Слоистая архитектура протекает через три предсказуемых сбоя, каждый из которых обнажает структурный пробел в плоской четырёхслойной модели.

Transaction-script trap: сервисный слой поглощает всю бизнес-логику как длинные процедуры, пока domain layer остаётся пустым. Слои существуют по названию, но не как разделение ответственностей. Доменные объекты — пассивные пакеты данных без применяемых инвариантов. Тестирование требует полного дерева зависимостей.

Layer-skipping: вышестоящие слои минуют промежуточные и импортируют из нижележащих напрямую. Контроллер импортирует репозиторий, потому что нужный запрос было быстрее написать в raw SQL. Как только один пропуск появился — он нормализуется. Infrastructure оказывается связан с presentation, лишая слоистость обещанной замещаемости. Leaky abstractions — более тонкая форма: границы интерфейсов существуют, но детали реализации (query builder'ы, ORM, типы строк SQL) просачиваются сквозь них.

Fat service: когда domain пуст и границы use case'ов не определены, сервисный слой становится catch-all. Через 18 месяцев — 60 методов и 11 импортов из infrastructure. Это god object, замаскированный под сервисный слой: не тестируемый изолированно, рискованный для изменений, из которого невозможно вычленить один use case.

Каждая утечка называет силу, мотивировавшую следующее поколение структурных паттернов. Hexagonal architecture (unit 04) инвертирует зависимость на границе domain: domain владеет своими портами, infrastructure реализует их, core тестируем без инфраструктуры. Clean architecture (unit 05) обобщает это до концентрических слоёв с жёстким правилом зависимостей, применяемым структурой, а не соглашением. Обе появились потому, что дисциплина плоской слоистости деградирует под давлением дедлайнов и растущих кодовых баз.
</Recap>
