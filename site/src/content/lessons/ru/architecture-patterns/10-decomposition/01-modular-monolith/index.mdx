---
slug: "01-modular-monolith"
lang: ru
track: "architecture-patterns"
unit: "10-decomposition"
order: 1
title: "Модульный монолит"
summary: "Модульный монолит устанавливает жёсткие швы между функциями внутри одного деплоя — пакеты по фиче, а не по слою — зарабатывая право на разделение позже."
estMin: 22
status: ready
lessonType: topic
level: senior
concepts:
  - modular-monolith
  - package-by-feature
  - module-boundary
  - monolith-first
  - coupling
  - seam
prereqs: []
sources:
  - https://martinfowler.com/bliki/MonolithFirst.html
  - https://samnewman.io/books/building_microservices_2nd_edition/
  - https://www.kamilgrzybek.com/design/modular-monolith-primer/
  - https://martinfowler.com/bliki/MicroservicePremium.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import boundaryCost from "./infographics/boundary-cost.svg?raw";

<Hook>
B2B биллинговая платформа начиналась как единое Rails-приложение, организованное по слоям: `app/models/`, `app/controllers/`, `app/services/`. Через восемнадцать месяцев в команде было 60 разработчиков, а деплой занимал 40 минут. Каждое изменение биллинговой логики требовало правок в `models/`, `services/`, и иногда `controllers/`. Никто не мог сказать, какие service-классы безопасно вызывать из биллинга, не заглянув в три директории. Технический лид предложил перейти на микросервисы. Руководство одобрило. Через шесть месяцев команда получила три сервиса, деплоящихся синхронно, разделяющих одну базу данных и ещё более сложных в изменении. Что пошло не так? Они разделили ком грязи и получили распределённый ком грязи. Нужная им модульная структура — это внутренняя дисциплина, а не сетевая граница.
</Hook>

<Crux>Modular monolith устанавливает швы внутри одного деплоя — пакеты по фиче, не по слою — зарабатывая право на разделение.</Crux>

<Explanation>

## Package-by-layer против package-by-feature

Классический скаффолд Rails/Spring/Django организует код по технической роли: `models/`, `controllers/`, `services/`, `repositories/`. Это package-by-layer. На старте проекта выглядит чисто. При масштабировании становится обузой.

**Package-by-layer** объединяет каждую модель из каждого домена. `OrderService` и `InvoiceService` и `CustomerService` — все живут в `services/`. Чтобы понять домен заказов, приходится читать четыре директории. Добавление фичи в заказы разбрасывает изменения по тем же четырём директориям. Каждое доменное изменение затрагивает каждый слой. Зацепление невидимо: ничто не мешает `InvoiceService` напрямую вызвать `OrderRepository`.

**Package-by-feature** (или package-by-domain) группирует всё для одной бизнес-возможности вместе. Модуль `ordering/` содержит свои модели, свои сервисы, свои репозитории, свою API-поверхность. Модуль `billing/` — соседний со своими внутренностями. Кросс-модульный доступ явный и контролируемый.

<FlowDiagram
  label="Package-by-layer против package-by-feature для B2B-платформы"
  caption="Package-by-layer рассыпает один домен по четырём директориям. Package-by-feature держит один домен целиком внутри одного модуля с контролируемой публичной границей. Кросс-модульные вызовы происходят только на границе, не сквозь внутренние слои."
  nodes={[
    { id: "layer-models", label: "models/", sub: "Order, Invoice, Customer (все вместе)" },
    { id: "layer-services", label: "services/", sub: "OrderSvc, InvoiceSvc, CustomerSvc (все вместе)" },
    { id: "layer-repos", label: "repositories/", sub: "OrderRepo, InvoiceRepo, CustomerRepo (все вместе)" },
    { id: "feat-ordering", label: "ordering/", sub: "Order, OrderService, OrderRepo — внутри модуля" },
    { id: "feat-billing", label: "billing/", sub: "Invoice, BillingService, InvoiceRepo — внутри модуля" },
    { id: "feat-boundary", label: "ordering/api", sub: "публичная поверхность — единственная точка входа" },
  ]}
  edges={[
    { from: "layer-services", to: "layer-models", label: "неограниченный доступ" },
    { from: "layer-services", to: "layer-repos", label: "неограниченный доступ" },
    { from: "feat-billing", to: "feat-boundary", label: "явный вызов через границу" },
    { from: "feat-boundary", to: "feat-ordering", label: "вход в модуль через публичный API" },
  ]}
/>

## Принудительное соблюдение границ модуля

Модульный монолит заслуживает своё название именно за принудительное соблюдение. Раскладывание файлов по поддиректориям — не граница модуля. Граница должна принудительно соблюдаться так, чтобы нарушение было ошибкой сборки, а не комментарием в code review.

Механизмы по языку/экосистеме:

- **Java-модули (JPMS):** `module-info.java` объявляет, что экспортируется и что требуется. Попытка доступа к неэкспортируемому пакету не компилируется.
- **TypeScript с `@typescript-eslint/no-restricted-imports`:** правила ESLint запрещают кросс-модульные импорты, кроме как через re-экспорты index. Нарушения ломают lint-гейт.
- **NestJS-модули:** `@Module({ exports: [...] })` явно определяет поверхность модуля. Injectable-сервисы не в `exports` невидимы снаружи модуля.
- **Go-пакеты с тулингом (`golang.org/x/tools/go/analysis`):** кастомные анализаторы могут assertить, что ни один пакет в `internal/ordering/` не импортируется напрямую из `internal/billing/`.
- **ArchUnit (Java) / `ts-arch` (TypeScript):** архитектурные тесты, assertящие правила зависимостей. Тест-сьют падает, если `billing` импортирует непубличный класс из `ordering`.

Ключевое: механизм должен быть автоматизированным. Если принудительное соблюдение границы требует ревью людьми, оно разрушится под давлением дедлайнов. Modular monolith — это обязательство перед автоматизацией.

<Inset kind="why">
Почему граница должна быть внутри одного деплоя, а не сетевой границей? Потому что сетевая граница добавляет задержку, распределённые режимы отказа и накладные расходы на сериализацию для каждого кросс-модульного вызова. Внутренняя граница модуля не стоит ничего в рантайме — это ограничение на этапе компиляции или тестирования. Форму границы можно дёшево менять. Если потом модуль извлекается в сервис, граница уже определена и протестирована — извлечение становится механической операцией, а не архитектурной переработкой.
</Inset>

<Infographic
  svg={boundaryCost}
  label="Сравнение внутренней границы модуля и сетевой границы. Внутренняя граница не стоит ничего в рантайме (ограничение на этапе компиляции или тестирования), дёшево меняет форму и делает последующее извлечение механическим разделением. Сетевая граница добавляет задержку и режимы отказа, фиксирует шов в контрактах API и команд и дорого меняет форму."
  caption="Один и тот же шов почти бесплатен внутри деплоя, но дорог, став сетевым — поэтому сначала дёшево экспериментируют с границей и лишь потом зарабатывают право на разделение."
  hue="var(--d-infra)"
/>

## Подход MonolithFirst по Фаулеру

Аргумент Мартина Фаулера «MonolithFirst» — не предпочтение монолитов. Это утверждение об информации: в начале проекта вы не знаете, где правильные границы сервисов. Доменные границы обнаруживаются через использование, а не проектируются заранее.

Паттерн отказа, который Фаулер наблюдал повторно: команды начинают с микросервисов, потому что «знают, что нужно масштабироваться», декомпозируют преждевременно по неправильным швам, и затем сталкиваются с двумя проблемами одновременно — строительством продукта и рефакторингом границ сервисов. Цена неправильной границы микросервиса высока: разделённые данные, API-версионирование, распределённые транзакции и две команды, которые должны согласовывать каждое изменение схемы.

Modular monolith — промежуточная позиция: обязаться на чистую внутреннюю структуру сейчас, отложить решение о распределении до понимания домена. Когда придёт время делить, деление происходит по швам, подтверждённым реальным использованием, а не предположенным заранее.

<Quiz
  id="arch-10-01-quiz-1"
  lessonSlug="01-modular-monolith"
  lang="ru"
  question="Архитектор команды предлагает: «Нужно разделиться на микросервисы сейчас, пока кодовая база мала. Потом, когда кода будет больше, делить будет сложнее». Старший инженер не согласен. В чём конкретный изъян рассуждений архитектора?"
  choices={[
    { label: "Изъян в том, что раннее разделение не снижает стоимость разделения — оно её увеличивает. Цена неправильной границы микросервиса высока: как только два сервиса общаются через сеть, изменение этой границы требует API-версионирования, миграции данных и межкомандной координации. Разделение при малом и плохо понятом домене гарантирует, что границы окажутся неправильными. Аргумент MonolithFirst именно в том, что границы обнаруживаются через использование, а не проектируются заранее. Modular monolith позволяет дёшево найти правильные швы, а потом разделить по ним при наличии доказательств.", correct: true },
    { label: "Архитектор прав. Раннее разделение всегда проще, потому что кода меньше.", misconception: "Это игнорирует стоимость неправильных границ. Ранее разделение при неизвестном домене не снижает стоимость миграции — оно фиксирует преждевременные решения. Объём кода — не главная стоимость; главное — правильность границы. Неправильная граница в микросервисе дорого исправляется, потому что разделение закодировано в сетевых контрактах, пайплайнах деплоя и владении командами." },
    { label: "Архитектор неправ, потому что микросервисы всегда добавляют больше сложности, чем убирают, для любой команды меньше 100 инженеров.", misconception: "Размер команды — один из факторов, но ключевой вопрос — знание домена, а не численность. Аргумент MonolithFirst — об информации: вы не знаете, где правильные границы, пока домен не понят через реальное использование. Небольшая команда с хорошо понятым доменом может разумно разделиться раньше; большая команда, исследующая новый домен, должна отложить разделение." },
    { label: "Архитектор прав для гринфилд-проектов. MonolithFirst применяется только к легаси-кодовым базам при миграции.", misconception: "MonolithFirst был сформулирован специально для гринфилд-проектов. Наблюдение Фаулера: команды, строящие новые системы, преждевременно оптимизируются под распределённость. Миграция легаси — другие силы, но оригинальный аргумент — о новых системах." },
  ]}
/>

## Что делает границу модуля хорошей

Не каждая поддиректория — это модуль. Хорошая граница модуля определяется:

**Явный публичный API.** Модуль выставляет поверхность — набор типов, функций или интерфейсов — и скрывает внутренности. Вызывающие зависят от публичного API, а не от классов реализации. При изменении внутренней реализации меняется только код самого модуля.

**Одна бизнес-возможность.** Модуль должен соответствовать одному bounded context (урок 06) или одной бизнес-возможности. `ordering/` управляет жизненным циклом заказа. `billing/` управляет инвойсингом и оплатой. `inventory/` управляет запасами. Модули со смешанными возможностями — `ordering-and-billing/` — сигнализируют о неправильном шве.

**Независимый тест-сьют.** Модуль может быть протестирован без запуска других модулей. Если тестирование `billing` требует импорта живого кода из `ordering`, зависимость неправильная.

**Низкое афферентное зацепление на внутренностях.** Внутренние классы модуля не импортируются ничем снаружи модуля. Весь внешний доступ идёт через публичный API. Если внутренний класс `Order` импортируется напрямую `BillingService`, граница уже нарушена.

<Quiz
  id="arch-10-01-quiz-2"
  lessonSlug="01-modular-monolith"
  lang="ru"
  question="В кодовой базе биллинговой платформы есть модуль `ordering/` и модуль `billing/`. Ревью кода обнаруживает: BillingService импортирует OrderRepository из `ordering/repositories/OrderRepository` напрямую, минуя публичную поверхность `ordering/api`. Инженер аргументирует: «Это только чтение — мы не модифицируем данные заказов, граница не нарушена». В чём структурная проблема такого рассуждения?"
  choices={[
    { label: "Структурная проблема в том, что нарушения границ — это не про чтение против записи, а про зацепление. BillingService теперь напрямую зависит от интерфейса OrderRepository, расположения пакета и внутренней реализации. Если команда ordering изменит сигнатуры методов OrderRepository, переименует класс или перестроит внутренности, BillingService сломается — даже несмотря на то, что биллинг формально отдельный модуль. Цель границы — позволить внутренностям каждого модуля меняться независимо. Прямой импорт только на чтение — всё равно прямой импорт: он создаёт то же зацепление, что и запись, просто на меньшем наборе операций.", correct: true },
    { label: "Инженер прав. Доступ только на чтение не нарушает границы модуля, потому что не влияет на состояние ordering.", misconception: "Нарушения границ — о зацеплении, а не о мутации состояния. Прямое чтение OrderRepository связывает BillingService с интерфейсом и расположением OrderRepository. Модуль ordering больше не может рефакторить свой слой репозиториев, не проверяя, что от него зависит billing. Граница существует для независимой эволюции, а не для предотвращения повреждения состояния." },
    { label: "Проблема в том, что BillingService не должен существовать в модуле billing — биллинговая логика должна быть в ordering, поскольку зависит от данных заказов.", misconception: "Это смешивает доступ к данным с владением модулем. Зависимость BillingService от данных заказов не означает, что биллинг принадлежит ordering. Правильное исправление: BillingService зависит от публичного API модуля ordering — метода вроде OrderApi.getOrderSummary() — который ordering контролирует и держит стабильным. Объединение модулей из-за того, что один читает из другого, воспроизвело бы монолитный антипаттерн." },
    { label: "Проблема — циклическая зависимость: если billing читает из ordering, ordering тоже должен читать из billing.", misconception: "Цикл не подразумевается. Однонаправленная зависимость (billing → публичный API ordering) правильна и не подразумевает обратной зависимости. Проблема в том, что billing обращается к внутренностям ordering, а не в направлении зависимости. Циклы — отдельная проблема, обнаруживаемая через ADP (урок 02)." },
  ]}
/>

## Когда делить: зарабатывание права

Modular monolith — не постоянная цель. Это состояние, в котором нужно быть перед разделением. Разделение зарабатывается, а не даруется.

Признаки того, что право на извлечение модуля в сервис заработано:

- Публичный API модуля стабилен и использовался несколькими вызывающими без изменения контракта.
- У модуля есть собственное хранилище данных (даже внутри общей БД его таблицы принадлежат исключительно ему).
- Тест-сьют модуля независим — может запускаться изолированно.
- Частота деплоя модуля явно отличается от других (нужно релизить несколько раз в неделю, другие — раз в месяц).
- Команда, владеющая модулем, достаточно велика, что накладные расходы на коммуникацию через границу модуля теперь дороже, чем граница деплоя.

Если эти условия не выполнены, разделение будет преждевременным и граница сервиса окажется неправильной. Самая дорогая ошибка в микросервисах — не слишком долгое оставание монолитом, а слишком раннее разделение по неправильным швам.

<Quiz
  id="arch-10-01-quiz-3"
  lessonSlug="01-modular-monolith"
  lang="ru"
  question="Модуль инвентаря биллинговой платформы работает в продакшне шесть месяцев. Команда рассматривает его извлечение в сервис. Инженер проверяет модуль и находит: (a) InventoryService.getAvailableStock() импортируется OrderService и BillingService через публичный API. (b) Таблицы базы данных модуля инвентаря напрямую запрашиваются BillingService через общее ORM-соединение. (c) Тест-сьют инвентаря требует запущенного OrderService для некоторых граничных случаев. Какие из этих находок должны блокировать извлечение и почему?"
  choices={[
    { label: "Находки (b) и (c) должны блокировать извлечение. (b) выявляет зацепление по данным: если inventory извлечь в сервис, BillingService больше не сможет напрямую запрашивать его таблицы — нужно вызывать API сервиса. Все запросы, минующие границу модуля, должны быть мигрированы до извлечения — это проблема владения данными, которую нужно решить внутри монолита. (c) выявляет зависимость в тестах, означающую, что модуль не тестируется независимо — обязательное условие для извлечения в сервис. (a) не является проблемой: несколько вызывающих через публичный API — это именно тот паттерн, который подтверждает правильность границы.", correct: true },
    { label: "Все три находки должны блокировать извлечение. (a) показывает слишком много зависимых от модуля, делая API-поверхность слишком большой для безопасного версионирования.", misconception: "Несколько вызывающих через публичный API — признак правильности и стабильности границы. Количество вызывающих не блокирует извлечение; важна стабильность API-контракта. Находки (b) и (c) — реальные блокеры." },
    { label: "Только находка (b) блокирует извлечение. Зависимость базы данных — единственная жёсткая зависимость; зависимости в тестах можно исправить после извлечения.", misconception: "Нетестируемый изолированно модуль становится нетестируемым изолированно сервисом. Зависимость в тестах выявляет реальное зацепление в рантайме. Исправление зависимостей в тестах после извлечения означает обнаружение зацепления через продакшн-сбои. Обе — (b) и (c) — должны быть решены до извлечения." },
    { label: "Ни одна из находок не должна блокировать извлечение. Эти проблемы можно решать параллельно с процессом извлечения.", misconception: "Решение зацепления по данным и изоляции тестов параллельно с извлечением означает одновременный рефакторинг и распределение — самую рискованную комбинацию в разработке ПО. Паттерн modular monolith специально требует устранения блокеров до перехода через сетевую границу, потому что после деплоя сервиса исправление требует согласованной миграции API и передачи владения данными." },
  ]}
/>

</Explanation>

<KeyTakeaway>Modular monolith устанавливает жёсткие, автоматизированные швы между бизнес-возможностями внутри одного деплоя. Package-by-feature, соблюдение границ на этапе сборки, независимое тестирование каждого модуля. Право на извлечение сервиса зарабатывается стабильным публичным API, собственными данными и изолированными тестами — не планированием будущего масштабирования.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-10-01-retrieval"
  lang="ru"
  questions={[
    { q: "В чём ключевое различие между package-by-layer и package-by-feature и почему это важно для границ модуля?", a: "Package-by-layer группирует весь код одной технической роли вместе (все модели, все сервисы, все репозитории) по всем бизнес-доменам. Это делает кросс-доменные зависимости невидимыми — OrderService может импортировать CustomerRepository без всякого трения. Package-by-feature группирует весь код для одной бизнес-возможности вместе. Граница между модулями явная: доступна только публичная API-поверхность. Это важно, потому что границы модуля могут принудительно соблюдаться только тогда, когда код организован по единице изоляции — бизнес-возможности, а не по технической роли. Слоевая организация структурно делает невозможным принудительное соблюдение доменной границы." },
    { q: "Что означает 'принудительное соблюдение границ модуля' на практике и почему оно должно быть автоматизировано?", a: "Принудительное соблюдение границы модуля означает превращение доступа к внутренностям модуля снаружи в ошибку сборки или провал теста. Механизмы: JPMS-дескрипторы в Java, правила ограничения импортов ESLint в TypeScript, экспорты NestJS-модулей, архитектурные тесты (ArchUnit, ts-arch), assertящие правила зависимостей. Автоматизация обязательна, потому что ревью людьми разрушается под давлением дедлайнов. Граница, нарушение которой не ломает CI, — это конвенция, а не контракт." },
    { q: "Каковы признаки того, что модуль 'заработал право' на извлечение в микросервис?", a: "Модуль имеет стабильный публичный API, использованный без изменений контракта. Модуль владеет своими данными — даже в общей базе его таблицы пишутся только его кодом. Тест-сьют модуля работает изолированно без других модулей. Частота деплоя модуля явно отличается от других модулей. Накладные расходы на командное владение теперь превышают накладные расходы сетевой границы. Все пять условий должны выполняться — выполнение только некоторых означает, что извлечение создаст распределённую проблему зацепления, сложнее исправляемую, чем сохранение модульности." },
  ]}
/>

<Recap lang="ru">
Провальная миграция на микросервисы на биллинговой платформе разбила ком грязи и получила распределённый ком грязи. Недостающим ингредиентом была не сетевая граница — а внутренняя дисциплина. Modular monolith обеспечивает эту дисциплину: жёсткие швы между бизнес-возможностями, принудительно соблюдаемые инструментарием сборки, организованные по фиче, а не по слою.

Паттерн MonolithFirst — не ностальгия по монолиту. Это утверждение об информации: вы не знаете правильных границ сервисов, пока домен не понят через продакшн-использование. Преждевременное разделение фиксирует неправильные границы по высокой цене. Разделение после периода дисциплины modular monolith означает деление по швам, подтверждённым реальными вызывающими, стабильными API и независимыми тест-сьютами.

Граница модуля внутри деплоя не стоит ничего в рантайме — это ограничение на этапе компиляции или тестирования. Её форму можно дёшево менять. Став сетевой границей, её изменение требует API-версионирования, миграции данных и межкомандной координации. Modular monolith — фаза дешёвых экспериментов, чтобы сетевая граница, когда придёт, была размещена правильно.
</Recap>
