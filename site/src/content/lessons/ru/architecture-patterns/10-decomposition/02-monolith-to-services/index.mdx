---
slug: "02-monolith-to-services"
lang: ru
track: "architecture-patterns"
unit: "10-decomposition"
order: 2
title: "От монолита к сервисам"
summary: "Декомпозиция по швам bounded context через strangler fig: инкрементальная маршрутизация фич к новым сервисам при одновременном уменьшении монолита. Каждый сервис владеет своими данными — без shared database."
estMin: 25
status: ready
lessonType: topic
level: senior
concepts:
  - strangler-fig
  - bounded-context-seam
  - database-per-service
  - decomposition-heuristic
  - data-ownership
  - incremental-extraction
prereqs: []
sources:
  - https://martinfowler.com/bliki/StranglerFigApplication.html
  - https://samnewman.io/books/monolith_to_microservices/
  - https://microservices.io/patterns/decompose/decompose-by-business-capability.html
  - https://martinfowler.com/articles/break-monolith-into-microservices.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import layerVsCapability from "./infographics/layer-vs-capability.svg?raw";

<Hook>
У B2B биллинговой платформы было 200 000 строк в одном Rails-монолите. CTO одобрил миграцию на сервисы. Команда выбрала прямой подход: взять самую сложную подсистему — управление заказами — и переписать её как микросервис. Шесть инженеров работали четыре месяца над новым OrderService. В день запуска запустили новый сервис рядом с монолитом и переключили трафик. Через час новый сервис и монолит отдавали противоречивые состояния заказов. Проблема: миграция перенесла API заказов, но данные заказов остались в общей базе. Billing всё ещё напрямую писал в таблицу `orders`. Inventory читал `orders` через свою ORM. OrderService имел API, но не владел данными. Через два месяца проект был заморожен. Урок: нельзя извлечь сервис, только переместив API. Нужно перемещать API и владение данными вместе, инкрементально, по швам, уже существующим в домене.
</Hook>

<Crux>Извлекай сервисы по швам bounded context через strangler fig; каждый сервис владеет своими данными — без shared database.</Crux>

<Explanation>

## Поиск швов: bounded context как единицы декомпозиции

Правильный шов для извлечения сервиса — это bounded context (урок 06): область модели, где применяется один согласованный набор концепций и языка. Bounded context уже имеет естественную API-поверхность — трансляции, которые он экспортирует в другие контексты — и естественную границу данных — агрегаты, которыми он владеет.

Извлечение по неправильному шву — самая распространённая и дорогая ошибка при миграции на микросервисы. Неправильные швы производят сервисы, тесно связанные на уровне данных, даже будучи разделёнными на уровне API. Признаки неправильного шва:

- Извлечённый сервис читает таблицы, которыми не владеет, требуя от монолита держать эти таблицы.
- Два сервиса должны координироваться при каждой записи для поддержания согласованности — по сути распределённая транзакция.
- Одна бизнес-операция требует последовательных вызовов API к трём-четырём сервисам — сервисы являются фрагментами воркфлоу, а не автономными возможностями.

Правильные швы соответствуют бизнес-возможностям, описываемым одним предложением без «и»: «управляет инвентарём продуктов», «обрабатывает жизненный цикл биллинга», «отслеживает статус заказа от размещения до доставки». Если описание требует «и» — возможно, это два шва, склеенных вместе.

<Inset kind="why">
Bounded context — единица декомпозиции, потому что это единица когерентности модели. Внутри bounded context каждый термин имеет одно значение. `Order` в bounded context заказов — не тот же `Order` в bounded context биллинга: один интересуется статусом выполнения, другой — статусом оплаты. Разделение по этой границе означает, что каждый сервис имеет свою модель и свои данные без разделяемого понимания, навязанного общей схемой.
</Inset>

## Паттерн strangler fig

Strangler fig (Фаулер, 2004) — инкрементальный паттерн миграции, названный в честь тропической лианы, растущей вокруг существующего дерева и в конце концов заменяющей его. Применительно к ПО:

1. **Поставь прокси перед монолитом.** Весь трафик проходит через прокси (API-шлюз, слой маршрутизации или обратный прокси). Монолит изначально обрабатывает всё.
2. **Извлеки одну возможность в новый сервис.** Новый сервис владеет частью домена — правильным bounded context.
3. **Маршрутизируй трафик этой возможности к новому сервису.** Прокси направляет запросы извлечённой возможности к новому сервису. Код монолита для этой возможности становится мёртвым.
4. **Удали мёртвый код из монолита.** Монолит уменьшается. Повторяй для следующей возможности.

В любой момент система работает. Монолит обрабатывает всё, что новые сервисы ещё не взяли. Миграция непрерывна и обратима на каждом шаге — при сбое нового сервиса прокси может вернуть трафик к пути монолита.

<FlowDiagram
  label="Миграция через strangler fig: billing извлечён из B2B-монолита"
  caption="Запросы входят через прокси. Биллинговый трафик маршрутизируется к новому BillingService, который владеет данными биллинга. Весь остальной трафик идёт к монолиту. Биллинговый код монолита теперь мёртв и может быть удалён. Прокси — плоскость управления миграцией."
  nodes={[
    { id: "client", label: "Клиент", sub: "фронтенд / API-потребители платформы" },
    { id: "proxy", label: "API-шлюз / Прокси", sub: "маршрутизирует по возможности" },
    { id: "monolith", label: "Монолит", sub: "заказы, инвентарь, уведомления — всё ещё живое" },
    { id: "billing-svc", label: "BillingService", sub: "извлечён — владеет данными биллинга" },
    { id: "billing-db", label: "БД биллинга", sub: "принадлежит исключительно BillingService" },
    { id: "monolith-db", label: "БД монолита", sub: "заказы, инвентарь, уведомления" },
  ]}
  edges={[
    { from: "client", to: "proxy", label: "все запросы" },
    { from: "proxy", to: "billing-svc", label: "маршруты billing/*" },
    { from: "proxy", to: "monolith", label: "все остальные маршруты" },
    { from: "billing-svc", to: "billing-db", label: "владеет и читает/пишет" },
    { from: "monolith", to: "monolith-db", label: "читает/пишет" },
  ]}
/>

## Владение данными и правило database-per-service

Самое нарушаемое правило при миграции на микросервисы — владение данными. Strangler fig может маршрутизировать API-трафик к новому сервису с первого дня, но миграция владения данными занимает больше времени. Пока владение данными не передано, сервис — фасад, а не автономная единица.

**Database-per-service** означает, что у каждого сервиса есть собственное хранилище без чтения или записи его таблиц другими сервисами. Это не технологический выбор (один инстанс PostgreSQL может обслуживать несколько сервисов на разных схемах) — это контракт владения:

- Только BillingService пишет в таблицы `invoices` и `charges`.
- Никакой другой сервис не держит ORM-модель для этих таблиц.
- Другие сервисы, которым нужны данные биллинга, вызывают API BillingService или потребляют его события.

Последовательность миграции для владения данными:

1. Определить весь код в монолите, читающий или пишущий мигрируемые таблицы.
2. Обернуть эти точки доступа за интерфейс в монолите, совпадающий с API-поверхностью нового сервиса.
3. Задеплоить новый сервис и направить реализацию интерфейса монолита на API нового сервиса.
4. Мигрировать данные в новую схему.
5. Удалить таблицы монолита (или заблокировать схему так, чтобы записи отклонялись).

<Quiz
  id="arch-10-02-quiz-1"
  lessonSlug="02-monolith-to-services"
  lang="ru"
  question="Команда использует strangler fig для извлечения InventoryService из B2B-монолита. Через два месяца InventoryService обрабатывает весь API-трафик инвентаря. Однако BillingService (всё ещё в монолите) продолжает читать таблицу `inventory_reservations` напрямую через общую ORM. Техлид говорит: «Извлечение завершено — весь API-трафик идёт к InventoryService». Старший инженер говорит, что извлечение не завершено. Кто прав и почему?"
  choices={[
    { label: "Старший инженер прав. Извлечение неполно, потому что владение данными не передано. Прямой ORM-доступ BillingService к `inventory_reservations` означает, что InventoryService не владеет своими данными эксклюзивно — он не может безопасно изменить схему, разделить таблицу или перейти на другую базу данных. Strangler fig маршрутизирует API-трафик, но сервис не автономен, пока не владеет своим хранилищем. Извлечение завершено только тогда, когда никакой код вне InventoryService не читает и не пишет таблицы инвентаря.", correct: true },
    { label: "Техлид прав. Маршрутизация API-трафика — это определение извлечения сервиса. Доступ к данным — вопрос производительности, а не владения.", misconception: "Маршрутизация API-трафика — необходимое, но недостаточное условие извлечения сервиса. Если BillingService читает таблицу `inventory_reservations` напрямую, InventoryService не может мигрировать схему, перейти на другую базу данных или оптимизировать модель данных без нарушения биллинга. Владение данными делает сервис автономным, а не маршрутизация API-трафика." },
    { label: "Оба частично правы. Извлечение API завершено; миграция данных — отдельный независимый проект, не влияющий на автономность сервиса.", misconception: "Владение данными и маршрутизация API не независимы. Сервис, не владеющий своими данными, не автономен. Другие сервисы, читающие его таблицы, создают зацепление на уровне данных, опаснее зацепления на уровне API: изменения схемы сложнее версионировать, и зацепление невидимо до момента, когда миграция что-то ломает." },
    { label: "Старший инженер прав, но исправление простое: ограничить ORM-доступ пользователем базы данных только для чтения для BillingService.", misconception: "Пользователь только для чтения ограничивает запись, но не схемное зацепление. ORM-модель BillingService для `inventory_reservations` всё равно связывает его с именами столбцов, типами и отношениями таблицы. Когда InventoryService нужно переименовать столбец, разделить таблицу или добавить NOT NULL столбец, нужна координация с биллингом — зацепление структурное, не только про записи." },
  ]}
/>

## Декомпозиция по возможности, а не по слою

Антипаттерн, производящий неправильные швы, — декомпозиция монолита по техническому слою: разделение слоя представления, слоя бизнес-логики и слоя данных в отдельные сервисы. Получаются три сервиса, реализующих одну бизнес-возможность — фрагменты воркфлоу, а не автономные возможности.

Сервис, извлечённый по слою:
- Не может быть независимо задеплоен (сервис логики зависит от сервиса данных для каждой операции).
- Не может быть независимо протестирован (сервис логики не имеет поведения без сервиса данных).
- Принуждает к синхронному зацеплению для каждого запроса (три сетевых хопа вместо нуля).

Сервис, извлечённый по бизнес-возможности, владеет своим полным вертикальным срезом: API, логика, хранилище. Это подход вертикальных срезов, применённый к декомпозиции сервисов.

<Infographic
  svg={layerVsCapability}
  label="Сравнение двух осей декомпозиции бок о бок. Декомпозиция по слою: 3 сетевых хопа на запрос, где нужен был ноль, нельзя деплоить независимо, нельзя тестировать независимо, распределённое зацепление без автономности. Декомпозиция по возможности: владеет полным вертикальным срезом из API, логики и хранилища, деплоится независимо, тестируется независимо, отказывает изолированно."
  caption="Обе оси платят полную операционную цену микросервисов, но автономность даёт только декомпозиция по возможности — разбиение по слою несёт цену и не получает выгоды."
  hue="var(--d-infra)"
/>

<Quiz
  id="arch-10-02-quiz-2"
  lessonSlug="02-monolith-to-services"
  lang="ru"
  question="Архитектор предлагает декомпозировать B2B биллинговый монолит на три сервиса: DataService (весь доступ к БД), BusinessLogicService (все доменные правила) и ApiService (все HTTP-эндпоинты). Каждый запрос от фронтенда идёт к ApiService, который вызывает BusinessLogicService, который вызывает DataService. Какие структурные проблемы создаёт такая декомпозиция?"
  choices={[
    { label: "Это декомпозиция по слою, а не по возможности. Каждый запрос требует трёх синхронных сетевых хопов вместо нуля. Ни один из трёх сервисов не может быть независимо задеплоен — деплой нового бизнес-правила требует согласованного деплоя всех трёх. Ни один не может быть независимо протестирован — BusinessLogicService не имеет поведения без DataService, ApiService — без обоих. Декомпозиция создала операционную сложность микросервисов (три деплойных юнита, сетевые сбои, версионирование) без какой-либо пользы автономности (каждый сервис всё равно связан с остальными для каждого запроса).", correct: true },
    { label: "Главная проблема в том, что трёх сервисов недостаточно для B2B-платформы. Для реальной независимости нужно больше сервисов.", misconception: "Проблема — ось декомпозиции, а не количество сервисов. Даже с десятью сервисами, декомпозированными по слою, те же структурные проблемы: каждый запрос пересекает несколько сервисов, все должны деплоиться вместе, ни один не тестируется независимо. Решение — декомпозиция по бизнес-возможности, а не добавление слоёв." },
    { label: "Декомпозиция верна. Так и работает сервис-ориентированная архитектура (SOA) — с отдельными уровнями для представления, логики и данных.", misconception: "Классические уровни SOA — паттерн деплоя и масштабирования, а не паттерн автономности. Трёхуровневая SOA всё равно связывает уровни для каждого запроса — улучшает горизонтальное масштабирование, но не автономность сервисов. Ценностное предложение микросервисов — автономная развёртываемость и независимая изоляция отказов, требующие декомпозиции по возможности, а не по слою." },
    { label: "DataService создаёт единственную точку отказа, но в остальном архитектура верна. Исправление — добавить реплику DataService.", misconception: "Реплика DataService решает доступность, но не структурные проблемы. Зацепление между сервисами (BusinessLogicService зависит от DataService для каждой операции) не решается репликацией. Реальная проблема: три сервиса реализуют одну возможность — им нужно действовать совместно для любого запроса, что делает ярлык 'микросервис' вводящим в заблуждение." },
  ]}
/>

## Последовательность: что извлекать первым

Не все модули одинаково хороши в качестве отправной точки для извлечения. Strangler fig работает лучше, когда первый извлекаемый сервис:

**Имеет низкое зацепление с другими внутренностями монолита.** Если биллинговая возможность обращается только к двум внутренним API (и эти вызовы можно обернуть за интерфейсы), извлечение относительно ограничено. Если она разделяет таблицы с каждым другим модулем, извлечение требует одновременной миграции каждой точки доступа.

**Имеет высокую частоту изменений.** Возможность, выпускающая изменения дважды в неделю при остальных раз в месяц, получает максимум от независимого деплоя.

**Уже модульна внутренне.** Если возможность заказов в монолите — связный модуль с чёткой внутренней структурой и собственными таблицами, извлечение — преимущественно сантехника. Если она запутана с общим кодом и таблицами, перед извлечением нужен рефакторинг.

**Имеет чёткое владение.** Команда, уже владеющая возможностью, может владеть её сервисом. Общий кросс-командный код сложнее всего извлекать.

<Quiz
  id="arch-10-02-quiz-3"
  lessonSlug="02-monolith-to-services"
  lang="ru"
  question="B2B биллинговая платформа планирует извлечения через strangler fig. Команда имеет двух кандидатов: (A) Модуль уведомлений — отправляет email и SMS, релизит редко, вызывается каждым другим модулем через общий класс NotificationService, не читает собственных таблиц (пишет в `notifications`), код чистый и самодостаточный. (B) Модуль отчётности — генерирует инвойсы и отчёты, релизит часто, читает таблицы `orders`, `invoices`, `customers` и `charges` напрямую через ORM, код сильно переплетён с биллинговой логикой. Какой модуль нужно извлекать первым и почему?"
  choices={[
    { label: "Модуль A (Уведомления) нужно извлекать первым. Несмотря на вызовы каждым модулем, зацепление входящее (другие модули вызывают его), а не исходящее (он не зависит от внутренностей других модулей). Извлечение требует добавления HTTP-клиента или подписки на события в каждом вызывающем, но сам модуль уведомлений ничего не читает, чем не владеет, и пишет только в свою таблицу. Извлечение ограничено одной таблицей данных и одним интерфейсом вызова. Модуль B имеет глубокое исходящее зацепление по данным — читает четыре таблицы, принадлежащие другим контекстам. Перед его извлечением все четыре прямых доступа к таблицам должны быть заменены API-вызовами к владеющим сервисам.", correct: true },
    { label: "Модуль B (Отчётность) нужно извлекать первым, потому что он релизит часто и независимый деплой даст больший бизнес-эффект.", misconception: "Частота релизов определяет приоритет среди извлечений схожей сложности, а не среди извлечений очень разной сложности. Глубокое зацепление модуля B по таблицам означает, что для извлечения нужно мигрировать четыре набора доступа к данным до маршрутизации трафика. Попытка сложного первого извлечения рискует провалом. Strangler fig советует начинать с менее связанных извлечений для накопления возможностей и уверенности." },
    { label: "Ни один из модулей не нужно извлекать первым. Монолит нужно сначала отрефакторить внутренне перед любым извлечением.", misconception: "Внутренний рефакторинг и извлечение через strangler fig не взаимоисключающие фазы. Strangler fig специально избегает требования полного внутреннего рефакторинга перед извлечением — он позволяет инкрементальную миграцию через маршрутизацию трафика. Извлечение модуля A требует только изменения конфигурации прокси и добавления HTTP-клиентов в вызывающих, без внутреннего рефакторинга монолита." },
    { label: "Оба модуля нужно извлекать одновременно для экономии времени.", misconception: "Параллельные извлечения умножают риск во время миграции. При проблемах в обоих одновременно команда отлаживает две сервисные интеграции при частично разобранном монолите. Strangler fig явно последовательный и обратимый: извлечь одну возможность, проверить в продакшне, затем продолжить. Параллелизм устраняет свойство снижения риска паттерна." },
  ]}
/>

</Explanation>

<KeyTakeaway>Декомпозируй по бизнес-возможности по швам bounded context, используя strangler fig для инкрементальной маршрутизации трафика. Каждый сервис должен владеть своими данными — без shared database. Мигрируй маршрутизацию API и владение данными вместе; маршрутизация одна даёт фасад, а не сервис.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="arch-10-02-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое паттерн strangler fig и почему он избегает проблемы 'большой перезаписи'?", a: "Strangler fig — инкрементальный паттерн миграции: поставить прокси перед монолитом, извлекать по одной возможности в новый сервис, маршрутизировать трафик этой возможности к новому сервису через прокси, удалять мёртвый код из монолита, повторять. Он избегает проблемы большой перезаписи, потому что система всегда работает — монолит обрабатывает всё, что новые сервисы ещё не взяли. Каждое извлечение независимо развёртывается и обратимо: при сбое нового сервиса прокси возвращает трафик к монолиту. Ошибки обнаруживаются в продакшне с ограниченным радиусом поражения, а не все сразу в день единого переключения." },
    { q: "Почему database-per-service — контракт владения, а не технологический выбор?", a: "Database-per-service — правило владения: только один сервис пишет в данный набор таблиц. Любой другой сервис, читающий или пишущий эти таблицы, создаёт схемное зацепление — владеющий сервис не может изменить схему без координации с каждым потребителем. Контракт не зависит от технологии: один инстанс PostgreSQL может обслуживать несколько сервисов на разных схемах, каждый со своим владением. Контракт нарушается, когда ORM-модель другого сервиса указывает на таблицу, принадлежащую иному сервису. Технологическая изоляция (отдельные инстансы) принудительно соблюдает контракт механически, но даже на общей инфраструктуре контракт должен соблюдаться." },
    { q: "Что не так с декомпозицией монолита по техническому слою (представление / логика / данные как три сервиса)?", a: "Декомпозиция по слою создаёт неавтономные сервисы: каждый запрос требует скоординированной цепочки вызовов через все три сервиса. Ни один не может быть независимо задеплоен (изменение бизнес-логики требует деплоя всех трёх), ни один не может быть независимо протестирован (сервис логики не имеет поведения без сервиса данных). Операционная сложность микросервисов — три деплойных юнита, сетевые сбои, версионирование — несётся без каких-либо преимуществ автономности. Правильная декомпозиция — по бизнес-возможности: каждый сервис владеет своим полным вертикальным срезом — API, логика, хранилище — и может независимо деплоиться, масштабироваться и отказывать." },
  ]}
/>

<Recap lang="ru">
Провальное извлечение на биллинговой платформе переместило API без перемещения данных. ORM монолита всё ещё владела таблицами; новый OrderService имел эндпоинт, но не данные. Каждая запись была гонкой между сервисом и оставшимся кодом монолита. Урок: сервис не автономен, пока не владеет своим хранилищем.

Strangler fig решает это инкрементально. Прокси перед монолитом маршрутизирует трафик срез за срезом к новым сервисам. Каждый срез извлекается с передачей владения данными — прямой доступ монолита к таблицам заменяется API-вызовами к новому сервису — до начала следующего среза. В любой момент система работает; монолит просто меньше.

Правильный шов для извлечения — bounded context: бизнес-возможность, называемая без «и». Декомпозиция по техническому слою — разделение представления, логики и данных в отдельные сервисы — производит наихудший исход: распределённое зацепление без автономности. Декомпозиция по возможности производит сервисы, развёртываемые, тестируемые и отказывающие изолированно.

Начинай с возможности с наименьшим исходящим зацеплением и наибольшей частотой изменений. Строй мышцу для извлечений через strangler fig на простом случае, затем применяй к сложным.
</Recap>
