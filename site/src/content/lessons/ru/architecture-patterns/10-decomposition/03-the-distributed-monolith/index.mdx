---
slug: "03-the-distributed-monolith"
lang: ru
track: "architecture-patterns"
unit: "10-decomposition"
order: 3
title: "Распределённый монолит"
summary: "Сервисы, деплоящиеся синхронно, вызывающие друг друга через N синхронных хопов и разделяющие базу данных — это distributed monolith: все операционные затраты распределённости без какой-либо автономности."
estMin: 22
status: ready
lessonType: topic
level: senior
concepts:
  - distributed-monolith
  - lockstep-deployment
  - temporal-coupling
  - shared-database
  - dual-write
  - chatty-services
prereqs: []
sources:
  - https://samnewman.io/books/building_microservices_2nd_edition/
  - https://martinfowler.com/bliki/MicroservicePremium.html
  - https://microservices.io/patterns/data/shared-database.html
  - https://www.infoq.com/articles/microservices-anti-patterns/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Через восемнадцать месяцев после разделения B2B биллинговой платформы на «микросервисы» дежурство стало невыносимым. Один запрос на размещение заказа проходил через пять сервисов последовательно: ApiGateway вызывал OrderService, который вызывал InventoryService, который вызывал BillingService, который вызывал NotificationService. Каждый хоп добавлял 20–40 мс задержки и новый режим отказа. Если NotificationService тормозил, размещение заказа получало таймаут. Задеплоить BillingService без координации с OrderService было невозможно: оба сервиса ожидали одну версию схемы `Order` — которая жила в общей базе данных, в которую оба писали. Каждый деплой требовал треда в Slack для подтверждения. Постмортем выявил первопричину: монолит разделили по техническому слою (API, логика, данные как отдельные сервисы), а не по бизнес-возможности. Получилось пять деплойных юнитов, пять дежурных ротаций, пять наборов пайплайнов деплоя и в пять раз больше сетевых точек отказа — при нулевой независимой развёртываемости. Это и был distributed monolith.
</Hook>

<Crux>Синхронные деплои и sync-цепочки означают distributed monolith: все затраты распределённости, ни одного из преимуществ.</Crux>

<Explanation>

## Что делает систему distributed monolith

Distributed monolith — набор сервисов, выглядящих независимыми, но тесно связанных в рантайме и во время деплоя. Он объединяет худшие свойства обеих архитектур:

- **От монолита:** изменение одного сервиса требует изменений и скоординированных деплоев в нескольких сервисах; кодовую базу сложно понять целиком, потому что зацепление неявное (сетевые вызовы и общая схема, а не видимые импорты).
- **От распределённых систем:** сетевая задержка на каждом межсервисном вызове, режимы частичного отказа сложнее для отладки, чем исключения уровня процесса, и операционные накладные расходы нескольких деплойных юнитов.

Диагностический вопрос — не «сколько у нас сервисов?» — а «может ли каждый сервис деплоиться и отказывать независимо?». Если нет — distributed monolith, сколько бы сервисов ни показывала архитектурная диаграмма.

Три структурных симптома определяют паттерн:

**1. Lockstep-деплой.** Сервисы должны деплоиться вместе или в определённой последовательности, потому что разделяют схему данных или API-контракт без версионирования. Изменение таблицы `orders` требует координации OrderService, BillingService и InventoryService в одном деплойном окне.

**2. Болтливые синхронные цепочки вызовов.** Один клиентский запрос разворачивается в N синхронных хопов через сервисы, не способные ответить, пока не ответят нижестоящие. Каждый хоп добавляет задержку и вероятность отказа. Под нагрузкой медленный сервис в конце цепочки производит таймауты, каскадирующиеся вверх.

**3. Shared database.** Несколько сервисов читают и пишут в одни и те же таблицы через общую ORM или общий пул соединений. Никакой сервис не владеет своими данными эксклюзивно; изменения схемы затрагивают каждый сервис, обращающийся к изменённой таблице.

<FlowDiagram
  label="Distributed monolith: один запрос на размещение заказа, пять синхронных хопов"
  caption="Один клиентский запрос разворачивается через пять сервисов последовательно. Каждый хоп добавляет задержку и точку отказа. NotificationService в конце цепочки может привести к таймауту всего размещения заказа. Ни один сервис не может деплоиться независимо, потому что все разделяют схему orders."
  nodes={[
    { id: "client", label: "Клиент", sub: "размещает заказ" },
    { id: "api", label: "ApiGateway", sub: "маршрутизирует запрос" },
    { id: "order", label: "OrderService", sub: "создаёт заказ" },
    { id: "inventory", label: "InventoryService", sub: "резервирует товар" },
    { id: "billing", label: "BillingService", sub: "списывает оплату" },
    { id: "notify", label: "NotificationService", sub: "отправляет подтверждение" },
    { id: "shared-db", label: "Shared DB", sub: "orders, charges, inventory — все таблицы" },
  ]}
  edges={[
    { from: "client", to: "api", label: "POST /orders" },
    { from: "api", to: "order", label: "sync-вызов" },
    { from: "order", to: "inventory", label: "sync-вызов" },
    { from: "inventory", to: "billing", label: "sync-вызов" },
    { from: "billing", to: "notify", label: "sync-вызов" },
    { from: "order", to: "shared-db", label: "читает/пишет таблицу orders" },
    { from: "billing", to: "shared-db", label: "читает/пишет таблицу charges" },
  ]}
/>

## Проблема dual-write

Shared database производит структурный дефект, проявляющийся как несогласованность данных: dual-write problem.

Рассмотрим создание BillingService записи о списании и обновление статуса заказа последовательно:

```
1. INSERT INTO charges (order_id, amount) VALUES (123, 450.00)
2. UPDATE orders SET status = 'charged' WHERE id = 123
```

Оба оператора успешно выполняются локально. Но в `orders` также пишет OrderService. Если OrderService конкурентно пишет `status = 'cancelled'` после шага 1, но до шага 2, заказ оказывается в состоянии `charged`, хотя был отменён. BillingService и OrderService каждый написал своё локальное представление истины в одну и ту же таблицу без владельца, принудительно соблюдающего согласованность.

Проблему dual-write нельзя полностью решить транзакцией, охватывающей записи обоих сервисов: такая транзакция является распределённой — она требует координатора для блокировки записей через соединения обоих сервисов одновременно. Это 2PC, который повторно вводит единственную точку отказа координатора, удержание блокировок между сервисами и зависимость доступности от всех участников (урок 09). Правильное исправление — дать каждому сервису эксклюзивное владение его таблицами, структурно исключив конкурентные записи от разных сервисов в одну запись.

<Inset kind="why">
Проблема dual-write — не баг в коде приложения, это структурное следствие разделённого владения данными. Никакая тщательная последовательность операций не устраняет её при двух сервисах, пишущих в одну таблицу, потому что гарантия последовательности требует распределённой блокировки — то есть 2PC под другим именем. Единственное структурное исправление — эксклюзивное владение данными на сервис.
</Inset>

<Quiz
  id="arch-10-03-quiz-1"
  lessonSlug="03-the-distributed-monolith"
  lang="ru"
  question="BillingService и OrderService биллинговой платформы оба пишут в общую таблицу `orders`. Разработчик предлагает исправить несогласованность dual-write, всегда выполняя записи в фиксированном порядке: сначала пишет OrderService, затем BillingService читает запись OrderService перед своей записью. В чём структурный изъян этого подхода?"
  choices={[
    { label: "Фиксированный порядок записи не предотвращает конкурентные записи — он только устанавливает конвенцию. При двух конкурентных запросах (одна отмена заказа от OrderService, одно списание от BillingService) оба потока следуют фиксированному порядку независимо, но их записи всё равно могут перемежаться. Поток A (OrderService): устанавливает status='cancelled'. Поток B (BillingService): читает статус (видит 'charged'), пишет status='charged'. Итоговое состояние: charged, хотя отмена закоммитилась первой. Принудительная упорядоченность между конкурентными потоками через разные сервисы требует распределённой блокировки — то есть 2PC. Структурное исправление — эксклюзивное владение: только один сервис пишет в любую данную таблицу.", correct: true },
    { label: "Подход верен. Фиксированный порядок записи исключает конфликты конкурентных записей, сериализуя все записи через определённую последовательность.", misconception: "Фиксированный порядок — конвенция среди разработчиков, не принудительная упорядоченность в базе данных. Два конкурентных запроса — одна отмена и одно списание — следуют конвенции независимо. Их перемежающееся выполнение всё равно производит гонки данных. Только распределённая блокировка (2PC) или эксклюзивное владение данными устраняет это; фиксированный порядок — ни то, ни другое." },
    { label: "Подход работает в большинстве случаев, но падает при перезапуске сервисов. Исправление — добавить логику ретраев.", misconception: "Перезапуски сервисов — не режим отказа. Гонка данных происходит между конкурентными нормальными запросами — без перезапуска. Ретраи не исправят гонку, где оба писателя успешно завершились, но произвели несогласованное итоговое состояние; повторный успешный write может усугубить несогласованность, применив устаревшее значение повторно." },
    { label: "Проблема только в том, что BillingService использует неправильный уровень изоляции. SERIALIZABLE-изоляция исправила бы конкурентный конфликт записи.", misconception: "SERIALIZABLE-изоляция на одном соединении с базой данных предотвращает конфликты внутри транзакции этого соединения, но два сервиса держат отдельные соединения и отдельные транзакции. Конфликт между двумя отдельными сериализуемыми транзакциями приводит к прерыванию одной из них с ошибкой сериализации — требуя ретрая — но не гарантирует корректное итоговое состояние. А если сервисы на разных инстансах базы, SERIALIZABLE-изоляция вообще не имеет кросс-сервисного эффекта." },
  ]}
/>

## Временно́е зацепление: скрытое зацепление синхронных цепочек

Temporal coupling (временно́е зацепление) — форма рантаймового зацепления, возникающего когда один сервис может завершить свою работу только при доступности другого сервиса в тот же момент. Каждый синхронный вызов сервиса создаёт temporal coupling.

В пятисервисной цепочке биллинговой платформы OrderService временно́ связан с InventoryService, BillingService и NotificationService. Если любой из них недоступен во время размещения заказа — даже ненадолго, во время деплоя или GC-паузы — вся операция размещения заказа завершается с ошибкой. Отказ сервиса уведомлений (низкий бизнес-приоритет) вызывает отказ размещения заказа (высокий бизнес-приоритет). Это инвертированный профиль риска temporal coupling: критически важная бизнес-операция падает при недоступности наименее критичного сервиса.

Temporal coupling также усложняет независимый деплой. Если OrderService должен вызывать InventoryService синхронно на каждом запросе, их нельзя тестировать независимо. Поведение OrderService при сбоях InventoryService нужно тестировать с живым InventoryService или тщательно созданными тест-дублями. На практике команды пропускают такое тестирование из-за сложности, производя характерную хрупкость distributed monolith.

<Quiz
  id="arch-10-03-quiz-2"
  lessonSlug="03-the-distributed-monolith"
  lang="ru"
  question="Дежурная команда биллинговой платформы наблюдает, что сбои NotificationService (email/SMS) вызывают сбои размещения заказов. Первопричина — синхронная цепочка вызовов. Инженер предлагает: «Нужно повысить SLA NotificationService до уровня OrderService — если уведомления станут надёжнее, цепочка станет надёжнее». В чём структурная проблема такого подхода?"
  choices={[
    { label: "Повышение надёжности NotificationService лечит симптом, а не причину. Причина — temporal coupling: OrderService не может завершить работу, пока NotificationService не завершит. Даже при улучшенном SLA NotificationService (система уведомлений) операционно связан с OrderService (транзакционная система) — всплеск уведомлений, лимит SendGrid или короткое деплойное окно NotificationService всё равно заблокируют размещение заказа. Структурное исправление — разорвать вызов: OrderService коммитит заказ и публикует событие; NotificationService потребляет событие асинхронно. Уведомления становятся best-effort и не могут заблокировать транзакцию.", correct: true },
    { label: "Подход верен. Улучшение надёжности NotificationService — правильный способ повысить общую надёжность системы при текущей архитектуре.", misconception: "Улучшение надёжности одного компонента не меняет структурное свойство, при котором его отказ каскадируется на все вышестоящие компоненты в синхронной цепочке. SLA 99,9% (8,7 ч простоя/год) для каждого из пяти связанных сервисов даёт совокупный SLA 99,5% (43 ч простоя/год) — с накоплением отказов. Структурное исправление — разорвать цепочку, а не укрепить её слабейшее звено." },
    { label: "Подход верен как краткосрочное исправление, но долгосрочное — заставить OrderService повторять упавшие вызовы к NotificationService.", misconception: "Повтор синхронного вызова к медленному NotificationService продлевает задержку размещения заказа на каждый ретрай. Если уведомления медленные из-за нагрузки, ретраи увеличивают нагрузку. Повтор синхронного вызова не развязывает системы; он добавляет обратное давление в уже связанную цепочку. Развязывающее исправление (событийное) необходимо независимо от поведения ретраев." },
    { label: "Подход работает, если NotificationService перенести так, чтобы он вызывался после записи в базу данных, а не до. Порядок вызовов в цепочке определяет, каскадируются ли сбои.", misconception: "В синхронной цепочке порядок вызовов определяет, на каком этапе обработки виден сбой, но все сервисы в цепочке всё равно временно́ связаны. Перенос NotificationService после записи в базу означает, что состояние заказа сохранено до отказа — но размещение заказа всё равно блокируется и возвращает ошибку клиенту при сбое уведомления. Перестановка вызовов в синхронной цепочке не снижает зацепление." },
  ]}
/>

## Диагностика distributed monolith

Контрольный список архитектурного ревью биллинговой платформы для distributed monolith:

**Изменил один сервис — нужно менять три.** Если добавление поля в доменный объект `Order` требует изменений кода в OrderService, BillingService и InventoryService — даже если поле вводит только один из них — сервисы структурно связаны. Настоящая граница сервиса означает, что изменение поля внутреннее для кодовой базы одного сервиса.

**Один запрос разворачивается в N sync-хопов.** Если диаграмма последовательностей для одного пользовательского действия показывает каскад синхронных вызовов через четыре сервиса, каждый из которых должен завершиться до ответа предыдущего, сервисы — деталь реализации одного воркфлоу, а не автономные возможности.

**Деплои требуют координационного треда.** Если деплой BillingService требует треда в Slack для подтверждения с командой OrderService совместимости миграции общей схемы, зацепление деплоев явное. Автономные сервисы деплоятся в любое время без межкомандной координации.

**Ни один сервис не может отказать изолированно.** Если отключение InventoryService на обслуживание приводит к сбою размещения заказов, размещение заказов не изолировано от инвентаря. Автономные сервисы деградируют gracefully при недоступности зависимостей — они не распространяют сбой вверх.

<Quiz
  id="arch-10-03-quiz-3"
  lessonSlug="03-the-distributed-monolith"
  lang="ru"
  question="Архитектурная команда биллинговой платформы проверяет пятисервисную систему. Находки: (1) Каждый запрос на размещение заказа делает четыре синхронных вызова последовательно. (2) Все пять сервисов разделяют один инстанс PostgreSQL с таблицами без чёткого владельца. (3) BillingService деплоится в продакшн три раза в неделю; OrderService — раз в месяц. (4) При деплое BillingService OrderService продолжает корректно работать. Инженер аргументирует: «Находки (3) и (4) доказывают независимость сервисов — разные частоты деплоев и деплои BillingService не ломают OrderService». Убедителен ли аргумент?"
  choices={[
    { label: "Аргумент неубедителен. Находки (3) и (4) показывают, что деплои BillingService не ломают OrderService — но ничего не говорят о том, ломают ли деплои OrderService BillingService, и ничего не говорят о рантаймовом зацеплении. Находки (1) и (2) выявляют distributed monolith: четыре синхронных хопа означают temporal coupling (OrderService может завершиться только когда ответят все нижестоящие сервисы), shared database означает схемное зацепление (любое изменение таблицы может сломать любой сервис). Частоты деплоев и безопасные деплои BillingService совместимы с distributed monolith — они доказывают только стабильность изолированной схемы BillingService, но не автономность сервисов системы.", correct: true },
    { label: "Аргумент убедителен. Разные частоты деплоев — сильнейшее доказательство автономности сервисов.", misconception: "Разные частоты деплоев показывают, что один сервис может деплоиться без немедленного ломания другого, но не показывают рантаймовую независимость или схемную независимость. В distributed monolith сервисы могут иметь разные частоты деплоев, при этом оставаясь временно́ связанными (синхронные цепочки) и схемно связанными (shared database). Сервис, вызываемый синхронно четыре раза на каждый запрос, не автономен, даже если деплоится независимо." },
    { label: "Аргумент убедителен для находки (3), но не для (4). Частота деплоев доказывает автономность; факт ломания другого сервиса при деплое не релевантен.", misconception: "Находка (4) — более операционально значимый тест независимости деплоя. Но обе находки вместе лишь устанавливают, что изменения BillingService совместимы с текущим бинарём OrderService. Они не тестируют, влияют ли изменения OrderService на BillingService, и не адресуют рантаймовое зацепление (синхронные цепочки) или схемное зацепление (shared database) — определяющие симптомы distributed monolith." },
    { label: "Аргумент неубедителен только из-за находки (2). Если shared database заменить отдельными базами, находки (3) и (4) доказали бы автономность сервисов.", misconception: "Отдельные базы решили бы схемное зацепление, но не temporal coupling. Даже с отдельными базами четыре синхронных хопа на запрос означают, что OrderService не может завершиться при медленности или недоступности любого нижестоящего сервиса. Сервис со своей базой, но синхронными зависимостями от четырёх других, — всё равно distributed monolith в плане рантаймового зацепления." },
  ]}
/>

## Как исправить distributed monolith

Distributed monolith исправляется тем же подходом, что его предотвращает: декомпозиция по бизнес-возможности по швам bounded context (урок 02), принудительное соблюдение владения данными, замена болтливых синхронных цепочек асинхронным событийным взаимодействием там, где бизнес допускает eventual consistency (урок 09).

Инкрементальное исправление:

1. **Определить болтливейшую синхронную цепочку.** Цепочка с наибольшим числом хопов — самое срочное temporal coupling для разрыва.
2. **Определить, какие вызовы блокирующие, а какие — уведомления.** Отправка уведомления-подтверждения не должна блокировать транзакцию заказа. Резервирование инвентаря должно — бизнес требует знать о наличии товара до фиксации заказа.
3. **Заменить уведомления событиями.** OrderService публикует событие `OrderPlaced`; NotificationService подписывается и отправляет email асинхронно. Транзакция размещения заказа завершается без ожидания уведомлений.
4. **Адресовать владение данными.** Определить, какими таблицами фактически владеет каждый сервис. Перенести эксклюзивный write-доступ к владеющему сервису. Заменить весь остальной доступ API-вызовами к владеющему сервису.
5. **Версионировать API, не схемы.** Когда двум сервисам нужно разделить концепцию (статус заказа), они делают это через API с версионированным контрактом — не через общую таблицу, в которую оба пишут.

</Explanation>

<KeyTakeaway>Distributed monolith — сервисы, деплоящиеся в lockstep, вызывающие друг друга через болтливые sync-цепочки и разделяющие базу данных: полные операционные затраты распределённости без автономности. Исправляется декомпозицией по швам возможностей, эксклюзивным владением данными и заменой sync-цепочек уведомлений событиями.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-10-03-retrieval"
  lang="ru"
  questions={[
    { q: "Каковы три структурных симптома, определяющих distributed monolith?", a: "Lockstep-деплой: сервисы должны деплоиться вместе или в фиксированной последовательности, потому что разделяют схему или API-контракт без версионирования. Изменение одной общей таблицы требует координации деплоев всех сервисов, обращающихся к ней. Болтливые синхронные цепочки вызовов: один клиентский запрос разворачивается через N сервисов последовательно, каждый ожидая следующего. Задержка и вероятность отказа накапливаются с каждым хопом. Shared database: несколько сервисов читают и пишут в одни и те же таблицы без эксклюзивного владения. Изменения схемы затрагивают каждый сервис; конкурентные записи от разных сервисов в одну запись производят dual-write-несогласованности." },
    { q: "Что такое проблема dual-write и почему её нельзя исправить транзакцией, охватывающей записи обоих сервисов?", a: "Dual-write — гонка данных, при которой два сервиса пишут в одну запись, производя несогласованное итоговое состояние, потому что каждый пишет своё локальное представление истины. Транзакция, охватывающая записи обоих сервисов, является распределённой транзакцией — требует координатора для блокировки записей через соединения обоих сервисов одновременно. Это 2PC, повторно вводящий единственную точку отказа координатора, удержание блокировок между сервисами и зависимость доступности от всех участников. Правильное исправление — эксклюзивное владение данными: только один сервис пишет в данную таблицу, структурно исключая конкурентные записи от разных сервисов." },
    { q: "Что такое temporal coupling и чем оно отличается от зацепления деплоев?", a: "Temporal coupling — рантаймовое зацепление: один сервис может завершить работу только при доступности другого в тот же момент. Каждый синхронный вызов сервиса создаёт temporal coupling. Зацепление деплоев — зацепление сборки/релиза: один сервис не может быть задеплоен без координации с другим, потому что они разделяют схему или API-контракт. Оба — симптомы distributed monolith, но проявляются в разное время. Система может иметь зацепление деплоев без temporal coupling (сервисы, разделяющие схему, но общающиеся асинхронно), и temporal coupling без зацепления деплоев (сервисы, деплоящиеся независимо, но вызывающие друг друга синхронно). Distributed monolith обычно имеет оба." },
  ]}
/>

<Recap lang="ru">
Пятисервисная система биллинговой платформы была distributed monolith во всех значимых смыслах: изменение домена заказов требовало изменений в трёх сервисах, размещение заказа блокировалось на отправке уведомлений, а shared database делала каждое изменение схемы скоординированным событием. Пять пайплайнов деплоя и пять дежурных ротаций — при нулевой независимой развёртываемости.

Distributed monolith появляется, когда команды разделяют по техническому слою вместо бизнес-возможности — производя фрагменты воркфлоу вместо автономных возможностей — или когда маршрутизируют API-трафик к новым сервисам без передачи владения данными. Сетевая граница косметическая, если зацепление по данным и temporal coupling остаются.

Диагностика проста: может ли каждый сервис деплоиться и отказывать независимо? Если деплой биллинга требует координационного треда с командой заказов — нет. Если простой сервиса уведомлений останавливает размещение заказов — нет. Количество сервисов на архитектурной диаграмме не имеет значения.

Исправление совпадает с предотвращением: декомпозиция по шву bounded context, эксклюзивное владение данными каждым сервисом, замена синхронных цепочек асинхронными событиями там, где бизнес принимает eventual consistency. С трудом усвоенный урок: распределённость — не цель. Цель — автономная развёртываемость.
</Recap>
