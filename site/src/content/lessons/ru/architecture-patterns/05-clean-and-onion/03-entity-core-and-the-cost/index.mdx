---
slug: "03-entity-core-and-the-cost"
lang: ru
track: "architecture-patterns"
unit: "05-clean-and-onion"
order: 3
title: "Entity core и его стоимость"
summary: "Кольцо Entities — наиболее стабильный код системы: чистые бизнес-правила без зависимостей. Реальная стоимость Clean Architecture — налог на маппинг и косвенное обращение на каждой границе кольца. Он оправдывается только в конкретных обстоятельствах."
estMin: 24
status: ready
lessonType: topic
level: middle
concepts:
  - entity-core
  - enterprise-business-rules
  - mapping-tax
  - boilerplate
  - architecture-overkill
  - clean-architecture-tradeoffs
prereqs: []
sources:
  - https://blog.cleancoder.com/uncle-bob/2012/08/13/the-clean-architecture.html
  - https://www.informit.com/store/clean-architecture-a-craftsmans-guide-to-software-structure-9780134494166
  - https://jeffreypalermo.com/2008/07/the-onion-architecture-part-1/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Спустя восемнадцать месяцев после принятия Clean Architecture для B2B-платформы заказов команда провела аудит. Хорошие новости: платформа была расширена четырьмя новыми delivery mechanisms — мобильное приложение, партнёрский API, батч-сервис сверки и ops-CLI — все подключились к существующим interactors без изменений бизнес-логики. Новые инженеры могли найти каждое бизнес-правило в одном месте. Плохие новости: кодовая база раздулась. Теперь для «заказа» существовало пять отдельных структур данных — entity Order в кольце Entities, ORM-класс `OrderRecord` в адаптере базы данных, `OrderResponseModel` в кольце Use Cases, `OrderDTO` в REST-адаптере и `OrderGraphQLType` в GraphQL-адаптере. Каждая граница кольца порождала код маппинга. Простое изменение «добавить поле в заказ» затрагивало все пять классов и три функции маппинга. Команда задала честный вопрос: окупается ли архитектура? Ответ: для этой платформы — да. Но они видели, как коллеги применяли тот же паттерн к двадцатитабличному CRUD-приложению для внутренней отчётности и производили тот же объём кода маппинга при нулевой структурной пользе. Clean Architecture не универсально правильна. У неё есть конкретная стоимость и конкретные условия, при которых эта стоимость оправдана.
</Hook>

<Crux>Entity core — стабильнейший код системы; граница кольца берёт налог на маппинг — оправданный только для сложных долгоживущих систем.</Crux>

<Explanation>

## Entity core — что там живёт и почему

Кольцо Entities — самое внутреннее кольцо в Clean Architecture. Оно содержит **общеприкладные бизнес-правила**: логику, которая была бы верна и валидна в любом приложении, построенном на этом бизнес-домене, независимо от delivery mechanism или используемой инфраструктуры.

На B2B-платформе заказов Entity core содержит:
- Класс `Order` с инвариантами жизненного цикла (pending-заказ должен иметь хотя бы одну позицию перед подтверждением; отменённый заказ не может перейти в confirmed; итог должен равняться сумме позиций)
- Класс `Invoice` со структурными инвариантами (позиции должны суммироваться в итог; оплаченный счёт не может быть изменён)
- Value object `Money` с правилами арифметики с учётом валюты
- Перечисления `OrderStatus`, `InvoiceStatus`, представляющие валидные состояния
- Domain events (`OrderConfirmed`, `InvoiceGenerated`) как чистые структуры данных

Что **отсутствует** в кольце Entities: всё, что касается инфраструктуры, прикладного потока или фреймворка. Никаких вызовов репозиториев. Никакого `async/await`-обращения к базе данных. Никаких ORM-аннотаций `@Column` или `@Entity`. Никаких HTTP-статус-кодов. Никаких импортов фреймворка логирования. Классы entity — чистые объекты, которые могут работать в полностью изолированном тесте без внешних зависимостей.

<FlowDiagram
  label="Кольцо Entities: что оно содержит vs что исключает"
  caption="Кольцо Entities ничего не импортирует из внешних колец. Все элементы внутри него — чистые in-memory объекты. Все элементы снаружи запрещены как source-code imports внутри любого Entity. Внешние элементы могут импортировать из кольца Entities, но не наоборот."
  nodes={[
    { id: "order", label: "Order (entity)", sub: "инварианты жизненного цикла, state machine" },
    { id: "invoice", label: "Invoice (entity)", sub: "структурные инварианты, статус" },
    { id: "money", label: "Money (value object)", sub: "арифметика с учётом валюты" },
    { id: "events", label: "Domain events", sub: "OrderConfirmed, InvoiceGenerated" },
    { id: "orm", label: "ORM annotations / DB types", sub: "запрещено в кольце Entities" },
    { id: "http", label: "HTTP types / framework", sub: "запрещено в кольце Entities" },
    { id: "repo", label: "Repository calls", sub: "запрещено в кольце Entities" },
  ]}
  edges={[
    { from: "order", to: "money", label: "imports (inner-ring ok)" },
    { from: "invoice", to: "money", label: "imports (inner-ring ok)" },
    { from: "orm", to: "order", label: "outer imports inner (allowed)" },
    { from: "http", to: "order", label: "outer imports inner (allowed)" },
  ]}
/>

## Аргумент стабильности

Аргумент в пользу сохранения Entity core чистым — это аргумент стабильности: entities — последний код, который нужно трогать при технологических изменениях. Базы данных мигрируют. Веб-фреймворки обновляются. Появляются мобильные клиенты. Ни одно из этих событий не должно изменить правило «Итог Invoice должен равняться сумме его позиций». Это правило постоянно, пока держится бизнес-модель.

Сохраняя entities свободными от инфраструктурных импортов, вы гарантируете: обновление ORM не может вызвать изменения entity. Переход с REST на GraphQL не может потребовать касания бизнес-логики Order. Новое требование облачного провайдера не может пройти в domain-правила. Самое внутреннее кольцо меняется только при изменении бизнес-модели — что и является единственным изменением, которое должно правомерно затрагивать его.

Эта стабильность также даёт выигрыш в тестировании. Unit-тесты entity — самые дешёвые тесты в системе: инстанцировать domain object, вызвать метод, проверить результат. Никакой базы данных, HTTP-сервера, mock-фреймворка. Entity инкапсулирует свои инварианты — для их тестирования нужен только класс entity и любые value types, которые он использует.

<Inset kind="why" lang="ru">
Почему именно «общеприкладные» правила для entities? Терминология Мартина различает правила, которые были бы валидны в нескольких приложениях одного предприятия (инварианты Order применимы к клиентскому порталу, операционной консоли и инструменту финансовой отчётности), и правила, специфичные для рабочего процесса одного приложения (конкретные шаги для «разместить заказ через партнёрский API»). Entities принадлежат самому внутреннему кольцу, потому что они — наиболее переиспользуемые, наиболее стабильные и наименее подверженные изменениям бизнес-знания системы.
</Inset>

## Честная стоимость — маппинг и косвенное обращение

Структурные преимущества Clean Architecture реальны. Стоимость не менее реальна, и её необходимо признать перед выбором паттерна.

**Налог на маппинг.** Каждая граница кольца требует кода трансляции. Поле entity `Order` не появляется автоматически в `OrderResponseModel` (кольцо Use Cases), `OrderDTO` (REST-адаптер) или ORM-классе `OrderRecord` (адаптер базы данных). Код маппинга на каждой границе пишется явно. Поле, начинающееся на entity, обычно появляется в четырёх-пяти отдельных классах и требует трёх-четырёх функций маппинга.

В аудите B2B-платформы: добавление `deliveryDeadline` к entity `Order` означало:
1. Добавить поле в `Order` (кольцо Entities) с инвариантом (deadline должен быть после даты создания)
2. Добавить в `GenerateInvoiceRequest` и `GenerateInvoiceResponse` там, где нужно (кольцо Use Cases)
3. Добавить в `OrderRecord` и функцию ORM-маппинга (кольцо Infrastructure)
4. Добавить в `OrderDTO` и код сериализации (REST-адаптер)
5. Добавить в `OrderGraphQLType` и resolver (GraphQL-адаптер)

Это налог на косвенное обращение. В layered architecture можно затронуть два файла. В Clean Architecture затрагиваешь пять — в обмен на гарантию, что каждое кольцо не знает форматов других.

**Церемония определений интерфейсов.** Каждый репозиторий, каждый сервис, каждый output port требует определения интерфейса в кольце Use Cases и конкретной реализации в другом месте. Для системы с тридцатью use cases это тридцать интерфейсов input port, тридцать классов interactor, потенциально тридцать интерфейсов output port, тридцать request models и тридцать response models. Прежде чем написать единственную строку бизнес-логики, вы строите значительный объём структуры.

**Когнитивная нагрузка.** Новые инженеры, приходящие в команду, должны понять кольцевую модель, прежде чем смогут найти любой кусок логики. Вопрос онбординга «где живёт проверка billing hold?» имеет точный ответ (кольцо Use Cases, внутри interactor) — но только если понимаешь архитектуру.

<FlowDiagram
  label="Налог на маппинг: одно поле пересекает четыре границы кольца"
  caption="Единственное domain-поле (deliveryDeadline) должно быть явно замаплено на каждой границе кольца. Для этого одного поля существуют четыре отдельных класса и три функции маппинга. Каждая стрелка — это поток МАППИНГА ДАННЫХ: она показывает, где явная функция маппинга копирует значение поля из структуры данных одного кольца в структуру другого. Это не стрелки зависимости: подписи в скобках (REST, GraphQL) указывают, какой адаптер владеет данным представлением в кольце, а не направление source-code импорта."
  nodes={[
    { id: "entity", label: "Order.deliveryDeadline", sub: "кольцо Entities — источник истины" },
    { id: "req", label: "GenerateInvoiceRequest.deliveryDeadline", sub: "кольцо Use Cases" },
    { id: "orm", label: "OrderRecord.delivery_deadline", sub: "кольцо Infrastructure (DB)" },
    { id: "dto", label: "OrderDTO.deliveryDeadline", sub: "кольцо Interface Adapters (REST)" },
    { id: "gql", label: "OrderGraphQLType.deliveryDeadline", sub: "кольцо Interface Adapters (GraphQL)" },
  ]}
  edges={[
    { from: "entity", to: "req", label: "mapped by" },
    { from: "entity", to: "orm", label: "mapped by" },
    { from: "entity", to: "dto", label: "mapped by" },
    { from: "entity", to: "gql", label: "mapped by" },
  ]}
/>

## Когда Clean Architecture избыточна

Clean Architecture окупается в конкретных обстоятельствах. За их пределами она генерирует церемонию без структурной отдачи.

**Избыточна для: CRUD-приложений.** Система, которая создаёт, читает, обновляет и удаляет записи без сложных инвариантов, state machines жизненного цикла или нескольких delivery mechanisms, не имеет бизнес-логики для защиты. Entities без реальных инвариантов (просто getters и setters над строкой базы данных) не выигрывают от нахождения в чистом самом внутреннем кольце.

**Избыточна для: небольших или краткосрочных приложений.** Внутренний дашборд отчётности для десяти человек, скрипт миграции данных, throwaway proof-of-concept — они будут заменены до того, как долгосрочные преимущества архитектуры начнут накапливаться.

**Избыточна для: команд без дисциплины поддержания границ.** Преимущества Clean Architecture исчезают в момент, когда разработчик из удобства добавляет импорт фреймворка в entity. Налог на маппинг постоянен; структурные гарантии постоянны только при соблюдении dependency rule. Если команда не может или не будет соблюдать границы колец, стоимость платится, но выгода не получается.

**Стоит того для: долгоживущих приложений со сложными domain-правилами.** Когда бизнес-логика действительно сложна — несколько состояний жизненного цикла, инварианты, требующие соблюдения, правила, растущие со временем, — entity core обеспечивает стабильный фундамент, переживающий годы инфраструктурных изменений.

**Стоит того для: нескольких delivery mechanisms над общим доменом.** Четыре delivery mechanisms B2B-платформы (веб, мобильный, партнёрский API, CLI) переиспользовали одни и те же interactors. Добавление четвёртого механизма обошлось команде только одним адаптером. Без Clean Architecture у них было бы четыре копии логики генерации счёта.

**Стоит того для: команд, которые будут расти и меняться.** Когда команда, строящая систему, не та, что её поддерживает, явные контракты (request models, port interfaces) служат документацией, которую компилятор принудительно соблюдает.

<Inset kind="note" lang="ru">
Практическая эвристика: посчитай количество delivery mechanisms, разделяющих одну бизнес-логику. Если ответ один — только HTTP API, и так будет всегда — симметричное преимущество Clean Architecture гипотетично. Если ответ три или больше — или есть достоверный roadmap к трём или более — паттерн interactor начинает окупаться. Вторая эвристика: посчитай domain-инварианты. Если можешь описать все бизнес-правила домена пятью предложениями, entity core мало что защищает. Если описание правил занимает сессию у доски, core стоит налога.
</Inset>

<Quiz
  id="arch-05-03-quiz-1"
  lessonSlug="03-entity-core-and-the-cost"
  lang="ru"
  question="Команда платформы добавляет новое поле `billingRegion` в entity Order. В полностью реализованной кодовой базе Clean Architecture сколько файлов минимально должно измениться, и почему?"
  choices={[
    { label: "Минимум четыре-пять файлов: entity Order (кольцо Entities), любые request/response models, несущие его (кольцо Use Cases), ORM record-класс (кольцо Infrastructure) и каждый delivery-mechanism DTO (кольцо Interface Adapters на адаптер). Каждая граница кольца требует явного добавления маппинга. Это налог на маппинг — честная стоимость разделения колец.", correct: true },
    { label: "Только entity Order — другие слои должны автоматически наследовать от entity.", misconception: "Никакого автоматического наследования между границами колец в Clean Architecture не существует. Каждое кольцо использует собственные структуры данных — в этом весь смысл: REST DTO может иметь другие имена полей и типы, чем entity, а ORM record может использовать snake_case-имена колонок. Автоматическое наследование повторно связало бы кольца. Маппинг намеренный, даже когда он затратен." },
    { label: "Два файла: entity Order и миграция базы данных — все остальные структуры генерируются из entity.", misconception: "Кодогенерация из entity может снизить ручную работу, но представляет ту же структурную связанность, которую Clean Architecture призвана предотвратить: если каждая структура данных генерируется из entity, форма entity диктует все кольцевые представления. Кодогенерация снижает церемонию; она не меняет количество существующих представлений." },
    { label: "Количество файлов зависит от того, какие use cases используют billingRegion — если ни один не использует, меняются только entity и ORM record.", misconception: "Это частично верно: только use cases, которые пробрасывают billingRegion через request/response models, нуждаются в обновлении. Но «только два файла» занижает — entity плюс ORM record плюс любые адаптерные представления для delivery mechanisms, открывающих billingRegion. Суть в том, что путь поля от entity к любому выводу требует маппинга на каждой пересечённой границе кольца." },
  ]}
/>

<Quiz
  id="arch-05-03-quiz-2"
  lessonSlug="03-entity-core-and-the-cost"
  lang="ru"
  question="Стартап строит B2B-систему управления заказами как монолит для одного клиента, ожидаемый срок работы — восемнадцать месяцев с последующей перестройкой. CTO предлагает Clean Architecture. Каков честный архитектурный совет?"
  choices={[
    { label: "Clean Architecture, вероятно, избыточна для этого сценария. Короткий жизненный цикл (восемнадцать месяцев), один клиент и уже запланированная перестройка означают, что долгосрочные выгоды архитектуры — переживание инфраструктурных изменений, поддержка нескольких delivery mechanisms, онбординг новых команд — не материализуются до замены системы. Более простой layered или transaction-script подход платит меньше накладных расходов за тот же функциональный результат.", correct: true },
    { label: "Clean Architecture всегда стоит того — это лучшая практика для любой production-системы вне зависимости от жизненного цикла.", misconception: "Лучшие практики зависят от контекста. Выгоды Clean Architecture долгосрочны (переживание технологических изменений, несколько delivery mechanisms, рост большой команды). Для восемнадцатимесячной системы с перестройкой эти выгоды не накапливаются. «Всегда используй Clean Architecture» — это мышление каталога паттернов, от которого трек начал отказываться: архитектура должна быть fit-for-forces, а не применяться рефлекторно." },
    { label: "Используй Clean Architecture, но пропусти output ports для снижения церемонии — это экономная версия.", misconception: "Пропуск output ports — валидное упрощение. Но налог на маппинг, кольцевая структура и церемония interactor остаются даже без output ports. Вопрос в том, оправдана ли полная концентрическая структура с учётом жизненного цикла и сложности системы. Частичная Clean Architecture всё равно несёт большую часть накладных расходов; фундаментальный вопрос fit-for-forces применяется ко всему подходу." },
    { label: "Clean Architecture актуальна только для микросервисов — монолит должен использовать layered architecture.", misconception: "Clean Architecture — внутриприкладная структура, а не топология развёртывания. Монолит может и часто должен иметь концентрическую dependency-структуру Clean Architecture внутри себя; архитектура описывает, как кодовые модули зависят друг от друга внутри одного deployable, а не сколько deployables существует. Микросервисы vs монолит — другая ось, чем layered vs clean." },
  ]}
/>

<Quiz
  id="arch-05-03-quiz-3"
  lessonSlug="03-entity-core-and-the-cost"
  lang="ru"
  question="Команда строит B2B-платформу заказов, сейчас имеющую один HTTP API. Product roadmap показывает мобильное приложение, партнёрскую EDI-интеграцию и ops-CLI, запланированные на следующие два года. CTO спрашивает: стоит ли принимать Clean Architecture сейчас или «подождать и посмотреть»? Каков структурный аргумент за принятие в начале?"
  choices={[
    { label: "Принятие концентрической структуры в начале означает, что interactors пишутся один раз без coupling с фреймворком. Каждый новый delivery mechanism (мобильный, EDI, CLI) добавляет адаптер и ничего больше — бизнес-логика уже абстрагирована. Ретрофит абстракции позже требует идентификации и распутывания логики, накопившейся в HTTP-специфичном коде, что дороже, чем начать со структурой.", correct: true },
    { label: "Подождать и посмотреть — не следует платить накладные расходы Clean Architecture, пока не знаешь, что нужны несколько delivery mechanisms.", misconception: "Стоимость налога на маппинг в начале относительно невелика — определяешь request/response models и port interfaces один раз при первоначальном написании use cases. Стоимость ретрофита растёт с накопившимся объёмом HTTP-специфичной логики. Если roadmap уже показывает три delivery mechanism, стратегия «подождать и посмотреть» платит налог дважды: при ретрофите и при поддержке рефакторированной структуры." },
    { label: "Clean Architecture только для больших команд — небольшая команда, строящая первую версию, не может позволить себе церемонию.", misconception: "Размер команды и накладные расходы Clean Architecture связаны лишь слабо. Налог на маппинг и церемония пропорциональны сложности домена, а не размеру команды. Небольшая команда, строящая систему со сложными инвариантами и несколькими delivery mechanisms, платит тот же налог, что и большая. Ключевой фактор — сложность и долговечность домена, не численность." },
    { label: "Используй Clean Architecture только если ORM не справляется со сложностью домена.", misconception: "Возможности ORM не имеют отношения к архитектурному решению. Clean Architecture касается направления source-code зависимостей — она говорит, что domain entity не должна импортировать ORM-типы. Насколько мощен или ограничен ORM, не меняет структурный аргумент за или против Clean Architecture." },
  ]}
/>

</Explanation>

<KeyTakeaway>Entity core — наиболее стабильный код системы: чистые бизнес-правила без зависимостей от инфраструктуры или прикладного потока. Реальная стоимость Clean Architecture — налог на маппинг на каждой границе кольца: несколько структур данных на domain object, явный код маппинга между ними. Эта стоимость окупается для долгоживущих систем со сложными инвариантами и несколькими delivery mechanisms; она избыточна для CRUD-приложений, краткосрочных систем и бэкендов с единственным delivery mechanism.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-05-03-retrieval"
  lang="ru"
  questions={[
    { q: "Что принадлежит Entity core, и каков аргумент стабильности за сохранение его чистым?", a: "Entity core содержит общеприкладные бизнес-правила: domain objects (Order, Invoice), value objects (Money), domain enumerations (OrderStatus), domain events — все как чистые in-memory объекты без внешних импортов. ORM-аннотации, вызовы репозиториев, HTTP-типы и импорты фреймворка отсутствуют. Аргумент стабильности: entities — последний код, который нужно трогать при технологических изменениях. Миграция базы данных, обновление веб-фреймворка, новый мобильный клиент — ни одно не должно требовать изменения правила 'отменённый заказ не может быть выставлен на счёт'. Сохраняя entities свободными от инфраструктурных импортов, гарантируешь, что технологические изменения не могут ripple в бизнес-логику. Entity меняется только при изменении бизнес-модели. Это также обеспечивает самые дешёвые тесты: entity unit-тесты требуют только domain objects, никакой инфраструктурной scaffolding." },
    { q: "Что такое налог на маппинг, и что делает его приемлемым?", a: "Налог на маппинг — явный код трансляции, требуемый на каждой границе кольца. Единственное domain-поле может появляться в четырёх-пяти отдельных структурах данных: entity, request/response models use case, ORM record и каждый delivery-mechanism DTO. Добавление или изменение поля затрагивает все они плюс их функции маппинга. Это конкретная стоимость разделения колец. Она приемлема когда: (1) система имеет несколько delivery mechanisms, разделяющих одну бизнес-логику — каждый механизм использует собственный DTO-формат, и форма entity не должна быть вынуждена соответствовать ни одному из них; (2) система долгоживущая — налог на маппинг — разовая стоимость на поле, а выгода (инфраструктурные изменения не затрагивают entities) накапливается годами; (3) домен действительно сложен — entities с реальными инвариантами оправдывают защищённый core. Не стоит платить для CRUD-систем, краткосрочных приложений или бэкендов с единственным delivery mechanism." },
    { q: "Назови три признака того, что Clean Architecture избыточна для данного проекта.", a: "Три честных индикатора: (1) Домен не имеет значимых инвариантов — entities это data bags, приложение CRUD, нет сложных state machines жизненного цикла. Entity core нечего защищать, поэтому налог на маппинг ничего не покупает. (2) Приложение имеет единственный delivery mechanism и roadmap не показывает планов на больше. Симметричное преимущество паттерна interactor — все delivery mechanisms переиспользуют одну бизнес-логику — требует нескольких delivery mechanisms. Один HTTP API навсегда делает разделение 'как вызывается' и 'что делает' гипотетическим. (3) Система краткосрочная или throwaway. Долгосрочные выгоды архитектуры (переживание инфраструктурных изменений, онбординг новых команд, поддержка нескольких клиентов) требуют времени для материализации. Восемнадцатимесячная система с перестройкой никогда не достигает точки, где эти выгоды приходят." },
  ]}
/>

<Recap lang="ru">
Entity core — самое внутреннее кольцо в Clean Architecture — наиболее стабильный, наиболее абстрактный, наиболее независимо тестируемый код системы. Он содержит общеприкладные бизнес-правила: инварианты, верные в любом приложении на этом бизнес-домене, независимо от delivery mechanism или инфраструктурной технологии. Что отсутствует: ORM-аннотации, типы базы данных, HTTP-концепции, импорты фреймворка. Entity меняется только при изменении бизнес-модели.

Честная стоимость Clean Architecture — налог на маппинг. Каждая граница кольца его берёт: domain-поле появляется в нескольких структурах данных, формат каждого кольца подходит для его цели, каждая граница требует явного кода маппинга. Это не артефакт плохой реализации — это прямое структурное следствие разделения колец. Аудит платформы обнаружил пять отдельных классов для «заказа» и три функции маппинга на поле. Вот счёт.

Счёт стоит оплатить при конкретных условиях: долгоживущие приложения, где инфраструктурные изменения опережают изменения бизнес-модели; системы с несколькими delivery mechanisms, переиспользующими одну бизнес-логику; сложные домены, где entity core действительно имеет инварианты для защиты; команды, которые будут расти и меняться. Для CRUD-приложений, throwaway-систем, бэкендов с единственным delivery mechanism и доменов с тривиальными инвариантами счёт приходит без соответствующей отдачи.

На этом завершается unit по Clean и Onion Architecture. Следующий unit вводит Domain-Driven Design — который идёт дальше концепции entity core и спрашивает, как моделировать бизнес-домен так, чтобы модель сама стала архитектурой.
</Recap>
