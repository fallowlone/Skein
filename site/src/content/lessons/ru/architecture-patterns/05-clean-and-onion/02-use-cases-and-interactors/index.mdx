---
slug: "02-use-cases-and-interactors"
lang: ru
track: "architecture-patterns"
unit: "05-clean-and-onion"
order: 2
title: "Use cases и interactors"
summary: "Кольцо Use Cases оркестрирует entities для достижения одной цели приложения. Interactor — класс, реализующий use case: получает request model, вызывает entities и возвращает response model — без типов фреймворка."
estMin: 22
status: ready
lessonType: topic
level: middle
concepts:
  - use-case
  - interactor
  - request-model
  - response-model
  - input-port
  - output-port
prereqs: []
sources:
  - https://blog.cleancoder.com/uncle-bob/2012/08/13/the-clean-architecture.html
  - https://www.informit.com/store/clean-architecture-a-craftsmans-guide-to-software-structure-9780134494166
  - https://jeffreypalermo.com/2008/07/the-onion-architecture-part-2/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
На B2B-платформе бэкенд вырос до более чем сорока методов сервисов. Когда пришёл новый инженер, его задачей при онбординге было найти, где происходит генерация счёта. Он искал `generateInvoice` и обнаружил вызовы в шести местах: HTTP-контроллер, фоновое задание, обработчик вебхука, скрипт батч-сверки, административный CLI и сервис обработки email. Каждое место передавало слегка разные аргументы. Два из них пропускали проверку «billing hold». Одно использовало объект подключения к базе данных напрямую. В кодовой базе не было единственного места, которое говорило бы: «вот что значит сгенерировать счёт — входные данные, шаги, результат». Логика была размазана по коду вызывающих. Когда Clean Architecture описывает кольцо Use Cases, это ответ именно на эту проблему: должен быть один класс на один application use case, и этот класс должен кодировать всё необходимое — форму входных данных, оркестрацию entities, форму выходных данных. Interactor — это тот самый класс.
</Hook>

<Crux>Interactor реализует один use case: получает request model, оркестрирует entities, возвращает response model — оба в domain-терминах.</Crux>

<Explanation>

## Кольцо Use Cases и что ему принадлежит

В кольцевой модели Clean Architecture кольцо Use Cases находится непосредственно снаружи кольца Entities. Оно содержит **бизнес-правила, специфичные для приложения** — правила, выражающие, что именно это приложение делает с entities.

Различие важно. Entity содержит **общеприкладные** бизнес-правила: у Order должна быть хотя бы одна позиция перед подтверждением; итог Invoice должен равняться сумме позиций. Эти правила верны в любом приложении, построенном на этом бизнесе. Use case содержит **прикладные** правила: чтобы сгенерировать счёт, сначала проверить, что заказ подтверждён, затем проверить billing holds, затем создать entity Invoice, затем опубликовать событие. Эти шаги специфичны для процесса данного приложения.

Кольцу Use Cases принадлежит:
- Один класс на use case (interactor)
- Интерфейс input port (что принимает use case)
- Интерфейс output port (что производит use case)
- Request model (структура данных, поступающая внутрь)
- Response model (структура данных, выходящая наружу)

## Паттерн interactor

Interactor — класс, реализующий один use case. Он содержит шаги прикладной логики этого use case и ничего больше. Он не знает об HTTP, SQL, JSON или любой концепции фреймворка.

```typescript
// Все типы здесь — в кольце Use Cases, без импортов фреймворка
interface GenerateInvoiceInputPort {
  execute(request: GenerateInvoiceRequest): Promise<void>;
}

interface GenerateInvoiceOutputPort {
  presentSuccess(response: GenerateInvoiceResponse): void;
  presentError(error: InvoiceGenerationError): void;
}

class GenerateInvoiceInteractor implements GenerateInvoiceInputPort {
  constructor(
    private readonly orders: IOrderRepository,
    private readonly invoices: IInvoiceRepository,
    private readonly billingHolds: IBillingHoldChecker,
    private readonly presenter: GenerateInvoiceOutputPort,
  ) {}

  async execute(request: GenerateInvoiceRequest): Promise<void> {
    const order = await this.orders.findById(request.orderId);
    if (!order) {
      this.presenter.presentError(new InvoiceGenerationError('ORDER_NOT_FOUND'));
      return;
    }
    if (order.status !== OrderStatus.Confirmed) {
      this.presenter.presentError(new InvoiceGenerationError('ORDER_NOT_CONFIRMED'));
      return;
    }
    const hold = await this.billingHolds.checkForHold(order.customerId);
    if (hold.active) {
      this.presenter.presentError(new InvoiceGenerationError('BILLING_HOLD_ACTIVE'));
      return;
    }
    const invoice = Invoice.createFor(order);
    await this.invoices.save(invoice);
    this.presenter.presentSuccess(new GenerateInvoiceResponse(invoice.id, invoice.total));
  }
}
```

`GenerateInvoiceInteractor` — единственное каноническое место, говорящее: «вот что значит сгенерировать счёт». Каждый вызывающий — HTTP-контроллер, batch-задание, CLI, обработчик вебхука — вызывает этот interactor. Ни один вызывающий не может пропустить проверку billing hold, потому что она внутри interactor, а не в вызывающем.

<FlowDiagram
  label="Оркестрация interactor: source-code зависимости на границе кольца Use Cases"
  caption="Interactor находится в кольце Use Cases. Он импортирует типы entities и интерфейсы репозиториев/сервисов (все определены во внутренних кольцах или самом кольце Use Cases). HTTP-контроллер и batch-задание — в кольце Interface Adapters; они импортируют интерфейс input port interactor. Все стрелки — source-code import зависимости, указывающие внутрь."
  nodes={[
    { id: "http", label: "HTTP Controller", sub: "кольцо Interface Adapters" },
    { id: "batch", label: "Batch Job Adapter", sub: "кольцо Interface Adapters" },
    { id: "input-port", label: "GenerateInvoiceInputPort", sub: "кольцо Use Cases — интерфейс" },
    { id: "interactor", label: "GenerateInvoiceInteractor", sub: "кольцо Use Cases — реализация" },
    { id: "output-port", label: "GenerateInvoiceOutputPort", sub: "кольцо Use Cases — интерфейс" },
    { id: "presenter", label: "HTTP Presenter", sub: "кольцо Interface Adapters — реализует output port" },
    { id: "entities", label: "Order / Invoice entities", sub: "кольцо Entities" },
  ]}
  edges={[
    { from: "http", to: "input-port", label: "imports" },
    { from: "batch", to: "input-port", label: "imports" },
    { from: "interactor", to: "input-port", label: "реализует" },
    { from: "interactor", to: "output-port", label: "вызывает" },
    { from: "presenter", to: "output-port", label: "реализует" },
    { from: "interactor", to: "entities", label: "imports" },
  ]}
/>

## Request models и response models — зачем они нужны

Распространённый ранний shortcut — передавать объект HTTP-запроса прямо в use case:

```typescript
// Неверно: кольцо Use Cases импортирует тип фреймворка
async execute(req: ExpressRequest): Promise<ExpressResponse>
```

Это создаёт source-code зависимость от HTTP-фреймворка в кольце Use Cases — зависимость, указывающую наружу, что нарушает концентрическое правило. Хуже того, use case становится нетестируемым без реального объекта HTTP-запроса.

Исправление — request models и response models: простые структуры данных, определённые в кольце Use Cases, несущие именно те данные, которые нужны use case, в domain-терминах.

```typescript
// Верно: кольцо Use Cases определяет собственные формы данных
class GenerateInvoiceRequest {
  constructor(
    public readonly orderId: OrderId,
    public readonly requestedBy: UserId,
  ) {}
}

class GenerateInvoiceResponse {
  constructor(
    public readonly invoiceId: InvoiceId,
    public readonly total: Money,
    public readonly generatedAt: Date,
  ) {}
}
```

HTTP-контроллер (в Interface Adapters) парсит HTTP-запрос и конструирует `GenerateInvoiceRequest`. Interactor никогда не видит `ExpressRequest`. Interactor производит `GenerateInvoiceResponse`. HTTP-presenter (тоже в Interface Adapters) конвертирует его в JSON HTTP-ответ. Перевод между типами фреймворка и типами use case — это полная работа кольца Interface Adapters.

<Inset kind="why" lang="ru">
Зачем определять отдельные request/response models вместо передачи plain object или generic dictionary? Потому что request model документирует именно то, что нужно use case. Это API-контракт use case — верифицируемый type checking, обнаруживаемый навигацией IDE, явный при code review. Plain object или generic dictionary скрывает этот контракт; любой вызывающий может передать любую форму и падает только в runtime. Request model — это документация, которую компилятор принудительно соблюдает.
</Inset>

## Input ports и output ports

Interactor взаимодействует через два интерфейса портов, оба определённых в кольце Use Cases:

**Input port** (`GenerateInvoiceInputPort`): интерфейс, который реализует interactor. Вызывающие (HTTP-контроллеры, CLI-адаптеры, batch-задания) импортируют этот интерфейс и вызывают `execute`. Они никогда не импортируют конкретный класс interactor. Это значит, что всю реализацию interactor можно заменить или замокировать в тестах без изменений в вызывающих.

**Output port** (`GenerateInvoiceOutputPort`): интерфейс, который interactor вызывает для доставки результатов. Interactor не возвращает значение — он вызывает output port, который реализует кольцо Interface Adapters (как presenter). Это инверсия зависимостей, применённая к стороне ответа: interactor не знает о HTTP-статус-кодах, JSON-сериализации или стриминге — он вызывает метод на domain-языке вроде `presentSuccess(response)` и позволяет presenter решить, как его отрендерить.

Эта двухпортовая структура отражает primary и secondary ports hexagonal architecture, теперь применённую конкретно на границе кольца Use Cases.

<Inset kind="note" lang="ru">
Многие реализации пропускают output port и просто возвращают response model из метода execute. Это проще и всё ещё валидно — упускается лишь возможность, чтобы interactor управлял несколькими каналами представления из одного вызова. Для большинства use cases прямой возврат response model — прагматичный выбор. Паттерн output port наиболее полезен, когда один use case должен одновременно обновить несколько выходных каналов (обновить UI presenter, опубликовать событие, записать лог) без знания о том, что это за каналы. Оба подхода сохраняют dependency rule.
</Inset>

<Quiz
  id="arch-05-02-quiz-1"
  lessonSlug="02-use-cases-and-interactors"
  lang="ru"
  question="GenerateInvoiceInteractor платформы принимает GenerateInvoiceRequest и вызывает GenerateInvoiceOutputPort presenter. Команда хочет добавить REST-эндпоинт И GraphQL-эндпоинт, оба запускающие генерацию счёта. Что изменится, а что останется прежним?"
  choices={[
    { label: "Interactor остаётся полностью неизменным. Добавляются два новых компонента Interface Adapters: REST-контроллер (парсит HTTP-запрос, создаёт GenerateInvoiceRequest, вызывает input port) и GraphQL-resolver (парсит GraphQL-аргументы, создаёт тот же GenerateInvoiceRequest, вызывает тот же input port). У каждого собственный presenter, реализующий GenerateInvoiceOutputPort для своего формата вывода. Бизнес-логика не дублируется.", correct: true },
    { label: "Interactor требует нового варианта метода execute (executeFromGraphQL), поскольку аргументы GraphQL имеют другую форму, чем тело REST-запроса.", misconception: "Interactor работает с GenerateInvoiceRequest — структурой данных кольца Use Cases без HTTP или GraphQL-осведомлённости. Оба контроллера REST и GraphQL переводят свои форматы в тот же GenerateInvoiceRequest перед вызовом input port. На interactor нет GraphQL-специфичного метода; работа по переводу принадлежит адаптеру (resolver), а не use case." },
    { label: "Для GraphQL-эндпоинта следует создать новый подкласс GenerateInvoiceInteractor, чтобы не менять уже протестированный код.", misconception: "Создание подклассов interactor для нового delivery mechanism нивелирует весь смысл архитектуры. Use case — что значит сгенерировать счёт — идентичен для REST и GraphQL. Interactor переиспользуется без изменений. Меняется только слой адаптера: новый resolver присоединяется к существующему контроллеру, оба вызывают тот же interactor через тот же input port." },
    { label: "Модель GenerateInvoiceRequest должна быть обновлена, чтобы нести поле 'source', чтобы interactor знал, вызван ли он из REST или GraphQL.", misconception: "Interactor не должен знать, какой delivery mechanism его вызвал — это инфраструктурное знание, живущее во внешних кольцах. Добавление поля 'source' в request model вносит осведомлённость о delivery mechanism в кольцо Use Cases, нарушая концентрическое правило. Если interactor когда-либо ветвится по 'source', он связал прикладную логику с инфраструктурой." },
  ]}
/>

<Quiz
  id="arch-05-02-quiz-2"
  lessonSlug="02-use-cases-and-interactors"
  lang="ru"
  question="Разработчик утверждает: «Паттерн output port добавляет лишнее косвенное обращение. Наш interactor должен просто возвращать GenerateInvoiceResponse и дать контроллеру его сериализовать». Это валидно, и что при этом теряется?"
  choices={[
    { label: "Валидно — прямой возврат response model распространён и разумен. Теряется возможность вызывать несколько выходных каналов изнутри interactor (например, одновременно обновить UI presenter и опубликовать audit-событие из одного вызова execute). Для use cases с одним выходным concern прямой возврат проще и всё равно сохраняет dependency rule.", correct: true },
    { label: "Не валидно — output port обязателен по Clean Architecture. Его удаление нарушает dependency rule.", misconception: "Dependency rule требует, чтобы source-code зависимости указывали внутрь — он не предписывает паттерн output port. Возврат response model из execute() и сериализация её контроллером всё равно держит interactor свободным от типов фреймворка. Output port — один способ достичь развязки, не единственный. Мартин описывает его как один вариант; многие команды находят прямой возврат response model проще для большинства use cases." },
    { label: "Не валидно — если interactor возвращает response model, контроллер должен импортировать из кольца Use Cases, создавая зависимость наружу.", misconception: "Контроллер находится в кольце Interface Adapters (внешнее) и импортирует из кольца Use Cases (внутреннее). Это зависимость, указывающая внутрь с точки зрения направления импорта: контроллер зависит от use case, а не наоборот. Dependency rule соблюдается — контроллер, импортирующий response model use case, корректен." },
    { label: "Валидно — но только если response model определена в кольце Interface Adapters, чтобы контроллер ею владел.", misconception: "Response model должна быть определена в кольце Use Cases — она часть выходного контракта use case. Если Interface Adapters определяет response model, interactor должен импортировать её оттуда, что является зависимостью наружу от Use Cases к Interface Adapters. Response models, как и request models, принадлежат use case." },
  ]}
/>

<Quiz
  id="arch-05-02-quiz-3"
  lessonSlug="02-use-cases-and-interactors"
  lang="ru"
  question="Команда обнаруживает, что PlaceOrderInteractor и GenerateInvoiceInteractor оба должны проверять billing holds на клиента. Они выносят CheckBillingHoldService с общей логикой. В каком кольце должен жить CheckBillingHoldService?"
  choices={[
    { label: "Кольцо Use Cases — потому что проверка billing hold является прикладной логикой, специфичной для этой платформы (правило о том, что должно быть проверено перед размещением заказа или генерацией счёта). Это не чистое entity-правило; это сквозная прикладная concern. Размещение в Use Cases позволяет обоим interactors использовать её, оставаясь свободной от инфраструктурных знаний.", correct: true },
    { label: "Кольцо Entities — потому что billing holds это domain-концепция и должны жить вместе с domain objects.", misconception: "Кольцо Entities содержит общеприкладные бизнес-правила, не зависящие ни от какого приложения. Блокирует ли billing hold операцию — это проверка уровня приложения, включающая консультацию с внешним сервисом или репозиторием (IBillingHoldChecker), что не является чистым in-memory entity-правилом. Сквозные прикладные сервисы принадлежат кольцу Use Cases." },
    { label: "Кольцо Interface Adapters — потому что это shared utility-код, адаптирующий между разными use cases.", misconception: "Кольцо Interface Adapters переводит между типами use case и типами фреймворка. Оно не содержит прикладную бизнес-логику. Проверка billing hold — прикладная логика, кодирующая правило о том, когда продолжать операцию. Логика, принадлежащая Use Cases, не должна выталкиваться в Interface Adapters." },
    { label: "Её следует продублировать в каждом interactor, чтобы избежать связанности между use cases.", misconception: "Дублирование бизнес-логики нарушает DRY и создаёт риск расхождения. Вынос общей прикладной логики в сервис кольца Use Cases — правильный шаг. «Связанность между use cases» — не правильная concern здесь: use cases одного кольца могут разделять сервисы. Правило касается направления зависимости между кольцами, а не intra-ring coupling." },
  ]}
/>

</Explanation>

<KeyTakeaway>Interactor — единственный авторитетный класс для одного use case: получает request model (без типов фреймворка), оркестрирует entities через интерфейсно-определённые репозитории и сервисы, доставляет результаты через output port или возвращает response model. Request model, response model, input port и output port — все определены в кольце Use Cases, никогда во внешнем кольце.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-05-02-retrieval"
  lang="ru"
  questions={[
    { q: "Что interactor владеет и что ему запрещено импортировать?", a: "Interactor владеет логикой ровно одного use case: шаги для оркестрации entities, проверки, последовательность операций. Импортирует из кольца Entities (классы entities и value objects) и из самого кольца Use Cases (интерфейсы репозиториев, интерфейсы сервисов, request/response models, интерфейсы портов — все определены в Use Cases или Entities). Запрещено импортировать что-либо из кольца Interface Adapters или Frameworks: никаких объектов HTTP-запросов, ORM-специфичных типов, аннотаций фреймворка, объектов подключения к базе. Если interactor импортирует из внешнего кольца, у него зависимость, указывающая наружу, что нарушает концентрическое правило и делает невозможным тестирование без инстанцирования этой внешней технологии." },
    { q: "Какова роль request model, и почему это не объект HTTP-запроса?", a: "Request model — простая структура данных, определённая в кольце Use Cases, несущая именно те данные, которые нужны use case, выраженные в domain-терминах. Её роль — быть входным контрактом use case: документирует, что требует use case, верифицируется type checking, независима от любого delivery mechanism. Объект HTTP-запроса — тип фреймворка из кольца Frameworks & Drivers: его импорт в Use Cases создаёт зависимость наружу. Кольцо Interface Adapters (HTTP-контроллер) парсит HTTP-запрос и конструирует request model, затем вызывает input port с ней. Этот перевод — работа контроллера. Interactor получает только domain-типизированные данные." },
    { q: "Что такое input ports и output ports на границе кольца Use Cases?", a: "Input port: интерфейс, определённый в кольце Use Cases, который реализует interactor. Вызывающие (в кольце Interface Adapters) импортируют этот интерфейс и вызывают его метод execute. Ни один вызывающий не импортирует конкретный класс interactor. Это позволяет заменить interactor (для тестирования или альтернативных реализаций) без изменения вызывающих. Output port: интерфейс, определённый в кольце Use Cases, который interactor вызывает для доставки результатов. Кольцо Interface Adapters (класс presenter) реализует этот интерфейс. Interactor вызывает методы на domain-языке вроде presentSuccess(response), не зная, будет ли это HTTP-ответ, JSON, вывод в CLI или публикация в очередь. Инверсия симметрична input port: внутреннее кольцо определяет интерфейс; внешнее реализует его." },
  ]}
/>

<Recap lang="ru">
Кольцо Use Cases решает проблему «размазанной логики»: когда одна и та же прикладная операция реализована по-разному в шести callsites, нет единственного места, кодирующего смысл операции. Паттерн interactor решает это напрямую — один класс, один use case, одно место для полной логики.

Interactor получает request model (простая структура данных, определённая в кольце Use Cases, с domain-типизированными полями без типов фреймворка), оркестрирует entities через интерфейсно-определённые репозитории и сервисы (также определённые в Use Cases), и доставляет результаты через output port или возвращаемый response model. Каждый вызывающий — HTTP-контроллер, CLI-адаптер, batch-задание, обработчик вебхука — конструирует request model и вызывает input port. Все они проходят одну логику, включая все одинаковые проверки.

Двухпортовая структура (input port и output port) применяет инверсию зависимостей симметрично: вызывающие зависят от интерфейса input port (зависимость внутрь от Interface Adapters к Use Cases); interactor вызывает output port (кольцо Interface Adapters реализует интерфейс output port, определённый в Use Cases). Ни один код внутреннего кольца не называет тип внешнего.

Следующий урок рассматривает Entity core — самое внутреннее кольцо, содержащее наиболее стабильный код, — и честную стоимость всей этой структуры: налог на маппинг, косвенное обращение и обстоятельства, при которых накладные расходы не оправдываются.
</Recap>
