---
slug: "01-the-concentric-rule"
lang: ru
track: "architecture-patterns"
unit: "05-clean-and-onion"
order: 1
title: "Концентрическое правило зависимостей"
summary: "Clean и Onion Architecture располагают слои концентрическими кольцами. Единственное жёсткое правило: source-code зависимости указывают только внутрь — к более высокоуровневой политике."
estMin: 22
status: ready
lessonType: topic
level: middle
concepts:
  - clean-architecture
  - onion-architecture
  - concentric-layers
  - dependency-rule
  - dependency-inversion
  - higher-level-policy
prereqs: []
sources:
  - https://blog.cleancoder.com/uncle-bob/2012/08/13/the-clean-architecture.html
  - https://jeffreypalermo.com/2008/07/the-onion-architecture-part-1/
  - https://www.informit.com/store/clean-architecture-a-craftsmans-guide-to-software-structure-9780134494166
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
На B2B-платформе заказов вырос четырёхслойный стек: presentation, application, domain, infrastructure. Команда изучила hexagonal architecture и тщательно расставила интерфейсы на каждой границе. Но спустя шесть месяцев обнаружилась проблема: domain layer импортировал из infrastructure layer утилиту форматирования денежных значений. Утилита жила в infrastructure, потому что изначально была помещена туда рядом с кодом конвертации валют для базы данных. Границы интерфейсов были целы — domain вызывал интерфейс, а не конкретный класс, — но сам интерфейс был определён внутри пакета infrastructure. Это значило, что domain зависел от пакета infrastructure ради определения интерфейса. Инверсия зависимостей была неполной. Когда в 2017 году вышла книга Clean Architecture, Robert Martin предлагал не что-то совершенно новое. Он брал ключевое наблюдение hexagonal architecture — порты, принадлежащие приложению — и систематизировал его в правило, применимое к каждой границе между слоями. Формулировка была точной: source-code зависимости могут указывать только внутрь — к более высокоуровневой политике.
</Hook>

<Crux>В Clean и Onion Architecture каждая source-code зависимость должна указывать к центру — внутренние кольца никогда не знают о внешних.</Crux>

<Explanation>

## Модель концентрических колец

Как Clean Architecture (Robert Martin, 2012 blog post, 2017 книга), так и Onion Architecture (Jeffrey Palermo, 2008) организуют систему в виде набора концентрических окружностей — колец, вложенных одно в другое. В центре — наиболее важный и стабильный код, на внешнем крае — наиболее изменчивый, специфичный для инфраструктуры.

Кольца немного различаются между двумя моделями, но логика одна. Канонические четыре кольца Мартина:

1. **Entities** (самое внутреннее) — общеприкладные бизнес-правила; наиболее стабильный код системы.
2. **Use Cases** — бизнес-правила конкретного приложения; оркестрируют entities для достижения целей.
3. **Interface Adapters** — переводят между форматом use case и внешним миром (controllers, presenters, gateways).
4. **Frameworks and Drivers** (самое внешнее) — веб-фреймворки, базы данных, внешние библиотеки; наиболее изменчивый код.

В Onion Палермо используются немного другие названия, но та же логика: Domain Model в центре, затем Domain Services, затем Application Services, затем внешнее кольцо Infrastructure. Обе модели приходят к одному структурному ограничению.

<FlowDiagram
  label="Концентрическое правило зависимостей: все source-code зависимости указывают внутрь"
  caption="Каждое кольцо может импортировать только из колец ближе к центру. Кольцо Entities ничего не импортирует внутри системы. Кольцо Frameworks может импортировать из любого внутреннего кольца. Ни одно внутреннее кольцо не знает о существовании внешних. Все стрелки — source-code import зависимости."
  nodes={[
    { id: "entities", label: "Entities", sub: "enterprise business rules (innermost)" },
    { id: "usecases", label: "Use Cases", sub: "application business rules" },
    { id: "adapters", label: "Interface Adapters", sub: "controllers, presenters, gateways" },
    { id: "frameworks", label: "Frameworks & Drivers", sub: "DB, web, UI (outermost)" },
  ]}
  edges={[
    { from: "usecases", to: "entities", label: "imports" },
    { from: "adapters", to: "usecases", label: "imports" },
    { from: "frameworks", to: "adapters", label: "imports" },
  ]}
/>

## Правило зависимостей — почему это правило, а не рекомендация

Единственное принудительно соблюдаемое ограничение: **source-code зависимость (import, reference, compilation dependency) может указывать только внутрь.** Ничто во внутреннем кольце не может ссылаться на что-либо во внешнем. Entity не может импортировать use case. Use case не может импортировать controller. Interface adapter не может импортировать веб-фреймворк — веб-фреймворк импортирует adapter.

Это не принцип стиля. Это структурное ограничение с конкретной отдачей: если ничто во внутренних кольцах не знает о внешних, внутренние кольца можно компилировать, тестировать и анализировать в полной изоляции от инфраструктуры. Все тесты entity и use case можно запустить без базы данных, веб-сервера или ORM.

Нарушение из вступительной истории — domain, импортирующий утилиту форматирования денег из пакета infrastructure — нарушало это правило. Даже при наличии интерфейса domain должен был импортировать его из пакета infrastructure. Путь импорта пересекал границу в неправильном направлении. Исправление то же, что и в hexagonal: перенести интерфейс во внутреннее кольцо (domain его определяет), а внешнее кольцо реализует его.

<Inset kind="why" lang="ru">
Почему стабильность возрастает к центру? Внутренние кольца содержат чистую бизнес-логику: правила, которые меняются только при изменении самой бизнес-модели. Внешние кольца содержат технологические решения: базы данных, фреймворки, протоколы — они меняются по технологическим причинам (новая версия ORM, смена облачного провайдера, deprecation стороннего API), не связанным с бизнес-логикой. Структурируя зависимости к стабильному центру, вы гарантируете, что технологические изменения не могут вызвать изменения бизнес-правил.
</Inset>

## Как Clean Architecture обобщает hexagonal

В unit 04 была представлена hexagonal architecture: порты, принадлежащие приложению, адаптеры с обеих сторон. Clean и Onion Architecture обобщают это с одной границы (domain/infrastructure) до N концентрических границ.

В hexagonal инверсия происходит один раз: domain определяет интерфейсы портов; infrastructure реализует их. Clean и Onion применяют ту же инверсию на каждой границе кольца. Кольцо Interface Adapters определяет интерфейсы, которые должны реализовывать Frameworks & Drivers. Кольцо Use Cases определяет интерфейсы (output ports, presenter interfaces), которые должно реализовывать кольцо Interface Adapters. Инверсия распространяется по всей цепочке внутрь.

<FlowDiagram
  label="Hexagonal vs Clean: одна инверсия против N инверсий"
  caption="Hexagonal инвертирует зависимость на одной границе domain/infrastructure. Clean Architecture применяет ту же инверсию на каждой границе кольца. Оба результата — source-code зависимости, указывающие внутрь повсеместно. Все стрелки — source-code import зависимости."
  nodes={[
    { id: "hex-domain", label: "Hexagonal: Domain", sub: "определяет интерфейсы портов" },
    { id: "hex-infra", label: "Hexagonal: Infrastructure", sub: "реализует интерфейсы портов" },
    { id: "clean-entities", label: "Clean: Entities", sub: "innermost; нет imports" },
    { id: "clean-uc", label: "Clean: Use Cases", sub: "импортирует entities; определяет output ports" },
    { id: "clean-adapters", label: "Clean: Interface Adapters", sub: "реализует output ports; импортирует use cases" },
    { id: "clean-fw", label: "Clean: Frameworks", sub: "реализует интерфейсы адаптеров; импортирует adapters" },
  ]}
  edges={[
    { from: "hex-infra", to: "hex-domain", label: "imports (source dep)" },
    { from: "clean-uc", to: "clean-entities", label: "imports" },
    { from: "clean-adapters", to: "clean-uc", label: "imports" },
    { from: "clean-fw", to: "clean-adapters", label: "imports" },
  ]}
/>

## Clean vs Onion — сходство и различие

Clean Architecture и Onion Architecture созданы независимо, но структурно сходятся. Обе пришли к одному правилу зависимостей. Практические различия:

**Onion Architecture** (Palermo, 2008) акцентирует **Domain Model** как абсолютный центр — чистые domain objects без каких-либо зависимостей. Domain Services в следующем кольце, Application Services далее, Infrastructure снаружи.

**Clean Architecture** (Martin, 2012/2017) вводит явное кольцо **Interface Adapters**, ответственное за перевод всех форматов данных между миром use case и внешним миром. Также разграничивает **Entities** (общеприкладные правила) и **Use Cases** (прикладные правила).

На практике команды часто смешивают оба подхода. Важно не то, какие названия колец вы используете — важно, соблюдается ли правило зависимостей.

<Inset kind="note" lang="ru">
Концентрическая модель — это не фиксированное число колец. Мартин прямо говорит: диаграмма с четырьмя кольцами иллюстративна; реальные системы могут иметь больше. Что не обсуждается — это направление зависимости: только внутрь. Можно добавить кольцо Anti-Corruption Layer, кольцо Reporting, кольцо External Services — если зависимости каждого нового кольца указывают внутрь, архитектура корректна.
</Inset>

<Quiz
  id="arch-05-01-quiz-1"
  lessonSlug="01-the-concentric-rule"
  lang="ru"
  question="Кольцо Use Cases платформы импортирует интерфейс CurrencyFormatter из кольца Infrastructure, где изначально была написана утилита форматирования денег. Интерфейс не содержит инфраструктурно-специфичных методов — он чистый. Является ли это нарушением правила зависимостей, и почему?"
  choices={[
    { label: "Да — нарушение. Путь импорта пересекает границу в неправильном направлении: кольцо Use Cases имеет source-code зависимость от пакета кольца Infrastructure. То, что интерфейс «чистый», не меняет того, где он определён. Use Cases теперь зависит от пакета Infrastructure ради определения интерфейса. Исправление: перенести определение интерфейса в кольцо Use Cases (где живёт его потребитель), а Infrastructure импортирует из Use Cases для реализации.", correct: true },
    { label: "Нет — не нарушение. Интерфейс не содержит деталей реализации, поэтому импорт интерфейса из кольца не создаёт реальной зависимости.", misconception: "Source-code зависимость определяется путём импорта, а не тем, является ли импортируемый артефакт интерфейсом или классом. Компилятор разрешает импорт из пакета Infrastructure; если этот пакет изменится, кольцо Use Cases перекомпилируется. Зависимость реальна — интерфейс находится в неправильном пакете, что и есть неправильное владение." },
    { label: "Нет — не нарушение, если кольцо Use Cases импортирует только интерфейс, а не какой-либо конкретный класс из Infrastructure.", misconception: "Это путает отсутствие coupling реализации с отсутствием source-code зависимости. Импорт интерфейса создаёт source-code зависимость на compilation unit пакета Infrastructure. Правило зависимостей касается source-code зависимостей — путей импорта, — а не конкретных vs абстрактных типов. Перенос интерфейса в кольцо Use Cases устраняет импорт; это структурное исправление." },
    { label: "Зависит от того, использует ли кольцо Use Cases Dependency Injection для получения реализации во время выполнения.", misconception: "Runtime Dependency Injection влияет на runtime coupling, но не на source-code зависимости. Если в исходнике кольца Use Cases есть `import { CurrencyFormatter } from '../infrastructure/formatters'`, source-code зависимость от Infrastructure существует независимо от того, как собираются экземпляры во время выполнения." },
  ]}
/>

<Quiz
  id="arch-05-01-quiz-2"
  lessonSlug="01-the-concentric-rule"
  lang="ru"
  question="Команда хочет добавить кольцо генерации PDF-отчётов снаружи кольца Interface Adapters, между Interface Adapters и Frameworks. Библиотека PDF — сторонний пакет. Где должны быть определены интерфейсы кольца PDF, и какое кольцо их реализует?"
  choices={[
    { label: "Интерфейсы кольца PDF (например, IReportRenderer) должны быть определены в кольце Interface Adapters — у потребителя. Кольцо PDF реализует эти интерфейсы с помощью сторонней библиотеки. Это сохраняет внутренние зависимости: Interface Adapters определяет контракт; внешнее кольцо PDF реализует его.", correct: true },
    { label: "Интерфейсы должны быть определены в самом кольце PDF, поскольку там живёт PDF-логика. Interface Adapters импортирует эти интерфейсы для использования.", misconception: "Определение интерфейса в кольце PDF и импорт его из Interface Adapters создаёт зависимость наружу: Interface Adapters (внутреннее) зависит от кольца PDF (внешнее). Это нарушает правило зависимостей. Интерфейс должен быть определён там, где он потребляется — во внутреннем кольце — чтобы внешнее кольцо соответствовало контракту внутреннего, а не наоборот." },
    { label: "Интерфейсы не нужны — Interface Adapters может импортировать стороннюю PDF-библиотеку напрямую, поскольку это просто инструмент.", misconception: "Прямой импорт сторонней PDF-библиотеки в Interface Adapters связывает внутреннее кольцо с технологией внешнего. При изменении API вендора или смене PDF-библиотеки Interface Adapters должен измениться. Инверсия — определить интерфейс в Interface Adapters, реализовать его в кольце PDF адаптера — сохраняет Interface Adapters независимым от конкретной PDF-технологии." },
    { label: "Новое кольцо нарушает Clean Architecture, поскольку каноническая модель имеет ровно четыре кольца.", misconception: "Мартин прямо указывает, что четыре кольца — иллюстрация, а не требование. Дополнительные кольца допустимы при соблюдении правила зависимостей. Кольцо рендеринга PDF между Interface Adapters и Frameworks архитектурно корректно, если Interface Adapters определяет интерфейсы, а кольцо PDF реализует их." },
  ]}
/>

<Quiz
  id="arch-05-01-quiz-3"
  lessonSlug="01-the-concentric-rule"
  lang="ru"
  question="Кодовая база Onion Architecture имеет структуру: Domain Model → Domain Services → Application Services → Infrastructure. Разработчик говорит: «Onion и hexagonal — это одно и то же: оба просто помещают интерфейс между domain и базой данных». Чего не хватает в этом описании?"
  choices={[
    { label: "Утверждение захватывает только инверсию innermost/outermost. Onion Architecture применяет правило зависимостей на каждой границе кольца — Domain Services не может импортировать Application Services, Application Services не может импортировать Infrastructure. Hexagonal инвертирует одну границу (domain/infrastructure). Onion и Clean обобщают до N концентрических границ с инверсией на каждой. Прочтение «просто интерфейс у базы данных» упускает правило «только внутрь» на внутренних границах колец.", correct: true },
    { label: "Утверждение точно. Обе архитектуры помещают интерфейс между domain и infrastructure, что и есть единственное значимое структурное отличие от layered architecture.", misconception: "Hexagonal architecture действительно имеет одну первичную инверсию. Onion Architecture добавляет ограничение: Application Services не может напрямую импортировать Infrastructure — инверсия применяется на каждой границе, а не только domain/infrastructure. Концентрическое правило строже, чем «поместить интерфейс у базы данных»." },
    { label: "Утверждение неверно, потому что hexagonal architecture не использует интерфейсы — она использует адаптеры.", misconception: "Hexagonal architecture использует интерфейсы (порты). Driving adapters вызывают primary port interface; driven adapters реализуют secondary port interfaces. Порт и есть интерфейс. Это не та часть описания, которой не хватает — не хватает многокольцевого обобщения." },
    { label: "Утверждение неверно, потому что в Onion Architecture нет кольца infrastructure — infrastructure просто деталь за пределами архитектуры.", misconception: "Onion Architecture явно называет кольцо Infrastructure на самом внешнем уровне. Infrastructure — часть модели; она помещена снаружи именно потому, что наиболее изменчива и специфична для деталей реализации. Кольцо существует; оно просто вынуждено зависеть от внутренних колец, а не наоборот." },
  ]}
/>

</Explanation>

<KeyTakeaway>Clean и Onion Architecture организуют код в концентрические кольца. Единственное принудительное правило: source-code зависимости указывают только внутрь, к более высокоуровневой политике. Внутренние кольца определяют интерфейсы; внешние реализуют их. Это обобщает единственную инверсию hexagonal на каждую границу кольца, делая всю внутреннюю систему независимо тестируемой и свободной от инфраструктуры.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-05-01-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое dependency rule в Clean Architecture, и почему оно обеспечивает независимую тестируемость внутренних колец?", a: "Dependency rule: source-code зависимости (импорты, compilation dependencies) могут указывать только внутрь — к более высокоуровневой политике. Внутреннее кольцо никогда не называет ничего из внешнего кольца. Следствие для тестируемости: если Entities и Use Cases не имеют импортов из Interface Adapters или Frameworks, они могут быть скомпилированы и протестированы при полном отсутствии инфраструктуры. Никакая база данных, веб-сервер или ORM не нужны для запуска теста use case. Это не случайность — это структурная отдача от соблюдения направления зависимости." },
    { q: "Как Clean Architecture обобщает hexagonal architecture?", a: "Hexagonal architecture применяет инверсию зависимостей на одной границе: domain определяет интерфейсы портов, infrastructure реализует secondary ports. Стрелка зависимости на этой границе указывает внутрь. Clean Architecture применяет ту же инверсию на каждой границе кольца. Кольцо Use Cases определяет output port interfaces; Interface Adapters реализует их. Кольцо Interface Adapters определяет собственные интерфейсы там, где необходимо; Frameworks & Drivers реализуют их. Инверсия распространяется от каждого внешнего кольца к каждому внутреннему. Hexagonal — частный случай Clean: двухкольцевая система, где domain — внутреннее кольцо, а всё остальное — внешнее." },
    { q: "В чём структурное различие между Onion Architecture и Clean Architecture?", a: "Обе модели соблюдают одно правило зависимостей: source-code зависимости указывают только внутрь. Различия в названиях колец и акцентах. Onion (Palermo, 2008): Domain Model (innermost), Domain Services, Application Services, Infrastructure. Акцент на том, что domain model полностью свободна от всех остальных concerns. Clean Architecture (Martin, 2012): Entities, Use Cases, Interface Adapters, Frameworks & Drivers. Добавляет явное кольцо Interface Adapters, ответственное за перевод форматов данных. На практике команды используют гибридные названия; различие в основном академическое. Инвариант обеих — направление стрелки зависимости." },
  ]}
/>

<Recap lang="ru">
Clean и Onion Architecture — концентрические кольцевые модели, обобщающие ключевое наблюдение hexagonal architecture на каждую границу слоёв. Центр архитектуры содержит наиболее стабильный, абстрактный код — чистые бизнес-правила, меняющиеся только при изменении самого бизнеса. Внешние кольца содержат технологические решения — базы данных, фреймворки, протоколы, — меняющиеся по технологическим причинам, не связанным с бизнес-логикой.

Dependency rule формулирует это точно: source-code зависимости могут указывать только внутрь. Внутреннее кольцо никогда не импортирует из внешнего. Внутреннее кольцо определяет интерфейс; внешнее реализует его. Каждая граница кольца применяет эту инверсию.

Нарушение из вступительной истории — кольцо Use Cases, импортирующее интерфейс из пакета Infrastructure — было технически тонким: интерфейс существовал, конкретный тип не импортировался. Но определение интерфейса жило в неправильном кольце, и у Use Cases был путь импорта, указывающий наружу. Исправление — владение: перенести интерфейс в кольцо, которое его потребляет.

Clean Architecture (Martin) и Onion Architecture (Palermo) различаются в основном названиями колец и акцентами. Обе приходят к одному структурному правилу. Число колец не фиксировано на четырёх. Что не обсуждается — направление: только внутрь.

Следующий урок рассматривает, что живёт во втором кольце — кольце Use Cases — и как паттерн interactor фиксирует «что делает приложение» как первоклассную структурную единицу.
</Recap>
