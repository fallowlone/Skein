---
slug: "01-choreography-vs-orchestration"
lang: ru
track: "architecture-patterns"
unit: "09-event-driven"
order: 1
title: "Хореография vs оркестрация"
summary: "Хореография — сервисы реагируют на события без центрального координатора, поток неявный. Оркестрация — центральный process-manager явно управляет шагами. Ключевой компромисс: связанность против видимости."
estMin: 25
status: ready
lessonType: topic
level: senior
concepts:
  - choreography
  - orchestration
  - process-manager
  - event-driven
  - saga-orchestrator
  - coupling-vs-visibility
prereqs: []
sources:
  - https://microservices.io/patterns/data/saga.html
  - https://www.enterpriseintegrationpatterns.com/patterns/messaging/ProcessManager.html
  - https://martinfowler.com/articles/201701-event-driven.html
  - https://microservices.io/patterns/decomposition/event-driven.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import couplingVsVisibility from "./infographics/coupling-vs-visibility.svg?raw";

<Hook>
Команда B2B биллинговой платформы начинала с четырёх сервисов, общающихся через события — хореография. Каждый сервис подписывался на события других и эмитировал свои. Общий поток обработки заказов существовал как поведение, возникающее из подписок. Восемнадцать месяцев спустя сервисов стало двенадцать. Когда появилось новое требование — «приостановить обработку заказа, если клиент превысил кредитный лимит» — никто не знал, какой сервис менять. Три дня ушло на трассировку логов, чтобы восстановить неявный поток по двенадцати подпискам. Затем команда рассмотрела переход на оркестрированную сагу, что сделало бы поток явным в одном месте. Но это означало центральный оркестратор, от которого зависели бы все двенадцать сервисов. Они поменяли одну проблему на другую.
</Hook>

<Crux>Хореография — неявный поток: слабо связан, невидим. Оркестрация — явный поток в одном месте: виден, но создаёт узел связанности.</Crux>

<Explanation>

## Хореография: неявный поток из подписок на события

В хореографированной системе каждый сервис знает только свою роль: «когда я вижу событие X, я делаю Y и эмитирую Z». Никакого центрального авторитета нет. Общий поток нигде не объявлен — он возникает из комбинации подписок всех сервисов.

Пример B2B биллинга с четырьмя сервисами:

- `OrderService` получает запрос клиента и эмитирует `OrderPlaced`
- `PaymentService` подписан на `OrderPlaced`, списывает средства, эмитирует `PaymentProcessed`
- `InventoryService` подписан на `PaymentProcessed`, резервирует товар, эмитирует `InventoryReserved`
- `ShippingService` подписан на `InventoryReserved`, планирует доставку, эмитирует `ShipmentScheduled`

Ни один сервис не знает полной картины. `PaymentService` не знает, что `ShippingService` существует. Поток слабо связан: добавить пятый сервис, реагирующий на `PaymentProcessed`, не требует изменений в `PaymentService`. Но поток невидим: единственный способ понять полный жизненный цикл заказа — прочесть подписки и каталог событий каждого сервиса.

<FlowDiagram
  label="Хореография: сервисы реагируют на события без центрального координатора"
  caption="Каждый сервис подписывается на события предыдущего шага и эмитирует свои. Общий поток обработки заказов неявный — он существует только как агрегированное поведение четырёх независимых подписок."
  nodes={[
    { id: "order", label: "OrderService", sub: "эмитирует OrderPlaced" },
    { id: "payment", label: "PaymentService", sub: "подписан: OrderPlaced, эмитирует PaymentProcessed" },
    { id: "inventory", label: "InventoryService", sub: "подписан: PaymentProcessed, эмитирует InventoryReserved" },
    { id: "shipping", label: "ShippingService", sub: "подписан: InventoryReserved, эмитирует ShipmentScheduled" },
    { id: "done", label: "Flow complete", sub: "no further events" },
  ]}
  edges={[
    { from: "order", to: "payment", label: "OrderPlaced" },
    { from: "payment", to: "inventory", label: "PaymentProcessed" },
    { from: "inventory", to: "shipping", label: "InventoryReserved" },
    { from: "shipping", to: "done", label: "ShipmentScheduled" },
  ]}
/>

## Оркестрация: явный поток в одном месте

В оркестрированной системе центральный process-manager — оркестратор — содержит полный бизнес-поток и направляет каждого участника шаг за шагом. Оркестратор знает: «сначала списать средства, потом зарезервировать товар, потом запланировать отправку». Участников вызывает оркестратор; они не подписываются на события друг друга.

Пример B2B биллинга с оркестратором `OrderSaga`:

- `OrderSaga` получает команду создания заказа
- Вызывает `PaymentService`: «спиши средства клиента X за заказ Y»
- При успехе вызывает `InventoryService`: «зарезервируй товар для заказа Y»
- При успехе вызывает `ShippingService`: «запланируй доставку заказа Y»
- При сбое любого шага `OrderSaga` точно знает, что уже сделано, и может скоординировать компенсацию

Поток теперь явный и виден в одном месте. Когда появляется требование «приостановить при превышении кредитного лимита», есть один сервис для изменения: `OrderSaga`. Компромисс — все участники теперь зависят от оркестратора, который стал узлом связанности.

<FlowDiagram
  label="Оркестрация: центральный OrderSaga управляет каждым сервисом пошагово"
  caption="Оркестратор OrderSaga содержит полный поток и явно направляет участников. Ни один сервис не подписывается на другой. Поток виден и изменяем в одном месте, но все участники зависят от оркестратора."
  nodes={[
    { id: "saga", label: "OrderSaga", sub: "центральный оркестратор, владеет потоком" },
    { id: "payment", label: "PaymentService", sub: "шаг 1: списать средства" },
    { id: "inventory", label: "InventoryService", sub: "шаг 2: зарезервировать товар" },
    { id: "shipping", label: "ShippingService", sub: "шаг 3: запланировать доставку" },
  ]}
  edges={[
    { from: "saga", to: "payment", label: "1. списать" },
    { from: "payment", to: "saga", label: "списано" },
    { from: "saga", to: "inventory", label: "2. зарезервировать" },
    { from: "inventory", to: "saga", label: "зарезервировано" },
    { from: "saga", to: "shipping", label: "3. запланировать" },
    { from: "shipping", to: "saga", label: "запланировано" },
  ]}
/>

<Quiz
  id="arch-09-01-quiz-1"
  lessonSlug="01-choreography-vs-orchestration"
  lang="ru"
  question="Команда B2B биллинговой платформы три дня трассировала хореографированную систему из 12 сервисов, пытаясь найти, где реализовать «приостановку при превышении кредитного лимита». Разработчик предлагает полностью перейти на оркестрацию. Старший инженер возражает: «Не нужно переводить всю систему — причина трудностей с трассировкой структурная, но она специфична для этого потока». Какое структурное свойство хореографии вызвало проблему трассируемости и что имеет в виду старший инженер?"
  choices={[
    { label: "Проблема трассируемости возникла из-за неявной, возникающей природы потока. В хореографии процесс существует только как совокупное поведение всех подписок — нигде не объявлен. По мере роста числа сервисов ни в одном месте нет описания порядка шагов, задействованных сервисов или того, что считается завершённой транзакцией. Старший инженер подразумевает, что решение — не отказываться от хореографии глобально, а признать: потоки со многими участниками, частыми изменениями бизнес-правил или сложной координацией плохо подходят для хореографии. Для простых, стабильных потоков с небольшим числом сервисов хореография остаётся уместной.", correct: true },
    { label: "Проблема трассируемости возникла из-за отсутствия документации. Если бы команда поддерживала диаграмму зависимостей сервисов, они нашли бы нужный сервис за несколько минут.", misconception: "Документация лечит симптом, а не структурную причину. В хореографии поток подлинно неявный — он существует только как агрегированное поведение подписок в рантайме. Документация хореографированной системы устаревает по мере эволюции сервисов. Проблема архитектурная: топология связанности распределена по определениям подписок в каждом сервисе, а не сосредоточена в одном месте." },
    { label: "Проблема трассируемости возникла из-за недостаточного логирования. Если каждый сервис логирует каждое обработанное событие, поток трассируем через распределённую трассировку.", misconception: "Распределённая трассировка помогает наблюдать произошедшее в работающей системе, но не делает предполагаемый поток видимым на этапе проектирования. Требование «приостановить при превышении кредитного лимита» — вопрос времени проектирования: какой сервис владеет этой логикой? Трассировка не ответит. Структурная проблема в том, что в хореографии ни один сервис не владеет потоком, поэтому нет очевидного владельца для новых сквозных бизнес-правил." },
    { label: "Проблема трассируемости возникла из-за использования событий вместо синхронных RPC. Если бы сервисы вызывали друг друга синхронно, граф вызовов был бы трассируем.", misconception: "Синхронные RPC имеют собственные проблемы с связанностью и доступностью, и трассируемость не гарантирована — глубоко вложенные синхронные цепочки вызовов могут быть такими же трудными для понимания, как подписки на события. Более того, компромисс урока — именно между хореографией и оркестрацией в событийно-ориентированных системах, а не между событийными и синхронными архитектурами." },
  ]}
/>

## Реальный компромисс: связанность против видимости

Бинарная формулировка — «хореография развязана, оркестрация связана» — скрывает реальный компромисс. Оба паттерна предполагают связанность; они просто связывают разные вещи по-разному.

**Связанность хореографии:** каждый сервис связан со схемой события. `PaymentService` зависит от формы `OrderPlaced`. Если `OrderPlaced` получает обязательное поле, `PaymentService` нужно обновить. Сервисы развязаны от существования друг друга, но связаны с контрактом событий.

**Связанность оркестрации:** каждый сервис связан с ожиданиями оркестратора. `PaymentService` должен отвечать на команду `ChargeCustomer` оркестратора в ожидаемом формате. Оркестратор связан со всеми участниками.

Измерение видимости/управляемости — там оркестрация явно выигрывает: поток аудитируем, тестируем и изменяем в одном месте. Бизнес-правила («приостановить при превышении кредитного лимита», «повторить платёж дважды перед отказом») живут в оркестраторе, а не рассеяны по логике подписчиков. Это важнее всего в потоках с частыми изменениями требований или сложной многошаговой координацией.

<Infographic
  svg={couplingVsVisibility}
  label="Сравнение хореографии (слева) и оркестрации (справа) по пяти измерениям: поток, связанность, видимость, владелец нового сквозного правила и когда подходит. Поток хореографии неявный и связан со схемой событий; поток оркестрации явный и связан с центральным оркестратором."
  caption="Связанность есть у обоих паттернов — у хореографии со схемой событий, у оркестрации с координатором. Реальный компромисс — видимость: оркестрация делает поток аудитируемым и изменяемым в одном месте, хореография оставляет его неявным, но невидимым."
  hue="var(--d-infra)"
/>

<Inset kind="note">
Ни один паттерн не является умолчанием. Хореография подходит для стабильных, простых потоков между небольшим числом сервисов, где выгода от развязки превышает стоимость потери видимости. Оркестрация подходит для сложных, часто меняющихся потоков, где бизнес-правила должны быть читаемыми, аудитируемыми и изменяемыми без изменения сервисов-участников. Многие реальные системы используют оба паттерна: хореографию для независимых доменных событий (сервис уведомлений, реагирующий на любое событие заказа), оркестрацию для сложных скоординированных транзакций (сага выполнения заказа).
</Inset>

<Quiz
  id="arch-09-01-quiz-2"
  lessonSlug="01-choreography-vs-orchestration"
  lang="ru"
  question="Команда строит новую систему уведомлений, отправляющую email при различных событиях на платформе: заказ размещён, платёж получен, отправка запланирована, тикет поддержки открыт. Они выбирают между хореографией и оркестрацией для потока уведомлений. Какой паттерн подходит лучше и почему?"
  choices={[
    { label: "Хореография подходит лучше. Сервис уведомлений может независимо подписаться на каждое релевантное событие без центрального координатора. Поток прост: событие пришло — отправить email. Каждое отображение событие-уведомление независимо от других — нет многошаговой координации, не нужна компенсация, нет сложной бизнес-логики, которая часто меняется. Выгода от развязки хореографии здесь высокая (логика уведомлений остаётся в сервисе уведомлений), а стоимость потери видимости низкая (поток достаточно прост, чтобы неявная структура не была трудна для понимания).", correct: true },
    { label: "Оркестрация подходит лучше, потому что даёт центральное место для управления всеми шаблонами уведомлений и правилами маршрутизации, упрощая будущие изменения.", misconception: "Логика маршрутизации уведомлений может жить внутри самого сервиса уведомлений — она не требует оркестратора. Ключевая причина выбора оркестрации — многошаговая координация с компенсацией, а не централизованная конфигурация. Сервис уведомлений, читающий правила маршрутизации из конфигурации, — это не проблема хореографии vs оркестрации." },
    { label: "Ни один паттерн не подходит — уведомления должны использовать выделенную очередь сообщений с point-to-point доставкой, а не publish/subscribe.", misconception: "Это смешивает механизм доставки (очередь vs pub/sub) с паттерном координации (хореография vs оркестрация). Описанная система уведомлений по своей природе событийно-ориентированная; вопрос в том, нужен ли центральный координатор. Нет, потому что нет многошаговой транзакции для координации — каждое уведомление является независимой реакцией на одно событие." },
    { label: "Оркестрация подходит лучше, потому что при нескольких типах событий нужен центральный сервис, знающий какой шаблон использовать для каждого события.", misconception: "Логика выбора шаблона по типу события является внутренней для сервиса уведомлений — она не требует внешнего оркестратора. Оркестрация — о координации нескольких внешних сервисов через последовательность шагов. Один сервис, выбирающий поведение на основе типа события, — это не оркестрация, это оператор switch." },
  ]}
/>

## Когда каждый паттерн ломается

**Хореография ломается, когда:**
- Поток охватывает много сервисов и бизнес-правила требуют логики координации между сервисами
- Требования часто меняются и нужный для изменения сервис неочевиден
- Отладка упавшей многосервисной транзакции требует трассировки событий по многим сервисам без единой точки истины
- Новое «глобальное» бизнес-правило должно влиять на поток, но не имеет естественного владельца в хореографированной топологии

**Оркестрация ломается, когда:**
- Оркестратор накапливает бизнес-логику помимо координации (становится god-object)
- Все изменения потока требуют изменения оркестратора, делая его узким местом деплоя
- Оркестратор становится единой точкой отказа для всех скоординированных транзакций
- Оркестратор технически корректен, но бизнес-команда владеет сервисами-участниками независимо, и связанность с общим оркестратором создаёт организационное трение

Проблема биллинговой платформы с 12 сервисами — это сценарий «хореография сломалась»: слишком много сервисов, слишком много сквозных бизнес-правил, неявный поток слишком сложен для осмысления. Но слепой переход на монолитный `OrderSaga`, оркестрирующий все 12 сервисов, создал бы хрупкий узел связанности. Правильная архитектура, вероятно, идентифицировала бы потоки, требующие оркестрации (критический путь выполнения заказа), и те, что могут остаться хореографированными (вспомогательные реакции: аудит-логирование, уведомления, метрики).

<Quiz
  id="arch-09-01-quiz-3"
  lessonSlug="01-choreography-vs-orchestration"
  lang="ru"
  question="Оркестратор OrderSaga команды вырос до 800 строк. Теперь в нём: логика маршрутизации (вызвать PaymentService), бизнес-правила («если клиент tier gold, пропустить проверку кредита»), политики повтора («повторить платёж до 3 раз»), и логика форматирования («форматировать итог заказа для этикетки доставки»). Старший инженер говорит, что оркестратор потерпел неудачу. Какой это режим отказа и какое структурное исправление?"
  choices={[
    { label: "Оркестратор стал god-object — накопил бизнес-логику, доменные правила, политики повтора и задачи представления, принадлежащие сервисам-участникам. Структурное исправление — вернуть оркестратору роль координации: он управляет последовательностью (вызвать A, потом B, потом C) и реагирует на результаты (успех/неудача), но делегирует доменную логику участникам. Логика проверки кредита принадлежит PaymentService. Политика повтора принадлежит инфраструктуре обмена сообщениями или декоратору повтора. Форматирование этикетки принадлежит ShippingService. Оркестратор должен читаться как описание бизнес-процесса на понятном языке, а не как насыщенный логикой сервис.", correct: true },
    { label: "Оркестратор вырос из-за того, что оркестрация — изначально плохой паттерн. Команда должна рефакторить на хореографию, чтобы убрать центральный координатор.", misconception: "Рост оркестратора — проблема организации кода, а не обвинение в адрес паттерна. Исправление — перенести некоординационные задачи обратно к участникам, а не отказываться от паттерна. Хореография заново введёт проблемы трассируемости, которые были раньше. Правильный шаг — более компактный оркестратор, а не его отсутствие." },
    { label: "800 строк не является проблемой само по себе. Если логика хорошо протестирована, оркестратор может быть таким большим, каким нужно для правильной координации потока.", misconception: "Размер — симптом, но основная проблема — нарушение границ. Бизнес-правила, политики повтора и логика представления не принадлежат компоненту координации независимо от покрытия тестами. Хорошо протестированный god-object всё равно остаётся god-object — он создаёт связанность между задачами, которые должны эволюционировать независимо, и делает общий поток труднее для чтения." },
    { label: "Исправление — разделить OrderSaga на несколько меньших саг, каждая отвечает за подмножество сервисов.", misconception: "Разделение на меньшие оркестраторы может быть правильным шагом, если поток реально декомпозируется на независимые подпотоки. Но это не исправляет основную проблему: бизнес-логика, политики повтора и код форматирования не принадлежат ни одному оркестратору независимо от размера. Разделение породит несколько god-object вместо одного." },
  ]}
/>

</Explanation>

<KeyTakeaway>Хореография = неявный, возникающий поток без центрального координатора — слабо связан, но трудно трассируем по мере роста сложности. Оркестрация = явный поток в одном месте — видим и управляем, но создаёт узел связанности. Выбирай исходя из сложности потока и частоты изменения бизнес-правил.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="arch-09-01-retrieval"
  lang="ru"
  questions={[
    { q: "Почему хореографию сложно отлаживать при сбое транзакции в середине потока?", a: "В хореографии поток неявный — ни один компонент не знает полной последовательности. При сбое единственный способ восстановить произошедшее — скоррелировать события по логам всех сервисов-участников с помощью общего correlation ID. Нет авторитетной записи о том, где в последовательности произошёл сбой, какие шаги завершились и в каком состоянии находится каждый сервис. При большом числе сервисов усилия по корреляции масштабируются плохо. В оркестрированной системе есть единственная запись саги, точно показывающая, какие шаги завершились, какие ожидают и какие упали." },
    { q: "В чём ключевое различие в связанности хореографии и оркестрации?", a: "В хореографии сервисы связаны со схемой события — каждый подписчик зависит от формы потребляемых событий. Сервисы не зависят от существования друг друга. В оркестрации сервисы-участники связаны с оркестратором — они должны отвечать на команды оркестратора ожидаемым образом. Оркестратор связан со всеми участниками. Хореография распределяет связанность по контрактам событий; оркестрация концентрирует её в координаторе." },
    { q: "Назови два признака того, что хореографированный поток стоит мигрировать на оркестрацию.", a: "Первый: поток охватывает много сервисов и реализация нового сквозного бизнес-правила (например, «приостановить при превышении кредитного лимита») требует знания, какой из многих сервисов менять — очевидного владельца нет. Второй: отладка упавшей многосервисной транзакции требует трассировки событий по многим сервисам, и усилия по восстановлению несоразмерны ценности — неявный поток стал слишком сложным для осмысления без централизованного представления о состоянии процесса." },
  ]}
/>

<Recap lang="ru">
Трёхдневная трассировка логов командой биллинговой платформы — это сбой хореографии. При двенадцати сервисах неявный поток — существующий только как агрегированное поведение двенадцати подписок на события — вырос за пределы возможности команды осмыслить его. Когда появилось новое сквозное правило, не было очевидного места для его размещения.

Сила хореографии — развязка: сервисы реагируют на события, не зная о существовании друг друга. Эта сила становится обязательством, когда поток сложный, когда бизнес-правила должны охватывать несколько участников, или когда вопрос «куда внести это изменение?» занимает дни.

Оркестрация делает поток явным в одном месте. `OrderSaga` знает полную последовательность, владеет бизнес-правилами и является единственным местом для добавления «приостановки при превышении кредитного лимита». Стоимость — все участники зависят от оркестратора, это узел связанности. И если оркестратор накапливает бизнес-логику помимо координации, он становится god-object.

Практический паттерн: использовать хореографию для простых, стабильных реакций (уведомления, аудит-логирование, метрики) и оркестрацию для сложных, часто меняющихся транзакций, где поток должен быть читаемым, аудитируемым и изменяемым. Многие системы используют оба паттерна одновременно.
</Recap>
