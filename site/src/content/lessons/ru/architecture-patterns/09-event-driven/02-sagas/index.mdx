---
slug: "02-sagas"
lang: ru
track: "architecture-patterns"
unit: "09-event-driven"
order: 2
title: "Саги"
summary: "Сага координирует бизнес-процесс между сервисами через локальные транзакции и компенсирующие действия без распределённых блокировок. Компенсация — не откат: это новое корректирующее действие. Саги допускают видимые промежуточные состояния."
estMin: 25
status: ready
lessonType: topic
level: senior
concepts:
  - saga
  - compensating-action
  - local-transaction
  - orchestrated-saga
  - choreographed-saga
  - saga-atomicity
prereqs: []
sources:
  - https://microservices.io/patterns/data/saga.html
  - https://www.cs.cornell.edu/andru/cs711/2002fa/reading/sagas.pdf
  - https://microservices.io/patterns/data/transactional-outbox.html
  - https://martinfowler.com/eaaDev/EventSourcing.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Биллинговой платформе нужно было обрабатывать размещение заказа как межсервисную операцию: зарезервировать товар, списать платёж, обновить статус заказа — всё или ничего. Команда попробовала распределённую транзакцию через двухфазный коммит (2PC). В стейджинге всё работало. В продакшне платёжный шлюз иногда отвечал восемь секунд. Распределённая транзакция удерживала блокировки на записи инвентаря все эти восемь секунд. Под нагрузкой таймауты блокировок каскадировались: блокировки инвентаря голодали другие заказы, таймауты триггерили ретраи, ретраи накапливали новые блокировки. Система встала. Они отказались от 2PC и реализовали сагу — последовательность локальных транзакций, каждая коммитится независимо, с явными компенсирующими действиями для отмены предыдущих шагов при сбое последующих.
</Hook>

<Crux>Сага — локальные транзакции между сервисами без распределённых блокировок. Каждый шаг коммитится; сбой запускает компенсацию — не откат.</Crux>

<Explanation>

## Почему 2PC не работает в распределённых системах

Двухфазный коммит (2PC) обеспечивает ACID-атомарность между несколькими участниками: либо все коммитятся, либо все откатываются. Координатор просит всех участников подготовиться (фаза 1), ждёт подтверждения от всех, затем отправляет commit или abort (фаза 2). Если кто-то из участников говорит «нет» в фазе 1, координатор откатывает всех.

Это звучит корректно. Проблема — операционная:

**Долгоудерживаемые блокировки.** В фазе 1 каждый участник удерживает свои локальные ресурсы заблокированными до получения сообщения commit или abort из фазы 2. Если платёжный шлюз отвечает 8 секунд, блокировка инвентаря удерживается 8 секунд. При высокой параллельности медленные участники блокируют несвязанные транзакции.

**Сбой координатора.** Если координатор падает между фазой 1 и фазой 2, участники зависают в «неопределённом» состоянии — они подготовились, но не знают, коммититься или откатываться. Они не могут снять блокировки до восстановления координатора. Восстановление может занять минуты.

**Недоступность участника.** 2PC требует одновременной доступности всех участников. Если сервис доставки недоступен в фазе 1, вся транзакция откатывается — даже если платёж и инвентарь полностью готовы.

Режим сбоя — не «транзакция иногда не проходит», а «вся система иногда зависает с удерживаемыми блокировками и неопределёнными участниками». Для высоконагруженного B2B биллинга это неприемлемо.

<Inset kind="why">
Саги заменяют координацию компенсацией. Вместо того чтобы требовать одновременной доступности всех участников под общей блокировкой, каждый участник коммитит свою локальную транзакцию независимо, как только успешно её выполнил. Компенсация обрабатывает сбои постфактум, добавляя корректирующие действия. Это смещает модель сбоя от «зависание» к «компенсация» — что восстанавливо и неблокирующе.
</Inset>

## Что такое сага

Сага — последовательность **локальных транзакций** — каждая коммитится внутри одного сервиса — вместе реализующих бизнес-процесс между несколькими сервисами. Каждая локальная транзакция обновляет состояние одного сервиса и публикует событие или отправляет сообщение для инициирования следующего шага.

Каждый шаг имеет соответствующее **компенсирующее действие**: бизнес-операцию, семантически отменяющую предыдущий шаг при сбое последующего. Компенсация — не откат: предыдущая транзакция уже закоммичена в базу данных. Компенсация добавляет новое корректирующее событие. Если инвентарь был зарезервирован, компенсация — освобождение резервации, новая запись, а не отмена.

Ключевое свойство, которое саги **не** обеспечивают, — **изоляция**: промежуточные состояния видимы другим транзакциям. Если сага резервирует инвентарь на шаге 2 и затем падает на шаге 3 (платёж отклонён), между шагом 2 и компенсацией инвентарь выглядит зарезервированным для других читателей. Это структурное следствие использования локальных транзакций без распределённой блокировки.

Контрмеры против потери изоляции:
- **Семантические блокировки:** помечать ресурсы как «в саге», чтобы другие транзакции считали их зарезервированными
- **Коммутативные обновления:** проектировать обновления так, чтобы конечное состояние было одинаковым независимо от порядка, делая наблюдаемость промежуточного состояния безвредной

<Quiz
  id="arch-09-02-quiz-1"
  lessonSlug="02-sagas"
  lang="ru"
  question="Команда мигрирует с 2PC на саги для потока выполнения заказов. Разработчик говорит: «Саги дают нам ту же гарантию «всё или ничего», что и 2PC, только реализованную иначе». Старший инженер исправляет это утверждение. В чём точное различие?"
  choices={[
    { label: "Саги обеспечивают атомарность-подобные результаты — либо весь бизнес-процесс завершается, либо компенсация выполняет отмену завершённых шагов — но они НЕ обеспечивают изоляцию. Между коммитом шага и выполнением компенсации промежуточные состояния видимы конкурентным транзакциям. 2PC обеспечивает и атомарность, И изоляцию: промежуточное состояние не наблюдаемо, потому что участники удерживают блокировки до финального решения координатора. Саги жертвуют изоляцией ради доступности и пропускной способности: нет узкого места координатора, нет межсервисных блокировок, нет каскадных таймаутов.", correct: true },
    { label: "Разницы нет. И саги, и 2PC дают полные ACID-гарантии; саги просто реализуют это на уровне приложения, а не базы данных.", misconception: "Саги не обеспечивают изоляцию — «I» в ACID. Между шагами саги промежуточные состояния закоммичены и видимы. 2PC обеспечивает изоляцию, удерживая участников в состоянии «подготовлен, но не закоммичен» до согласия всех. Это фундаментальное структурное различие, а не деталь реализации." },
    { label: "Саги используются только для операций чтения. Для операций записи между несколькими сервисами 2PC всё ещё необходим.", misconception: "Саги специально спроектированы для операций записи между несколькими сервисами. Они заменяют 2PC для координации межсервисных записей. Операции чтения не требуют распределённых транзакций." },
    { label: "Различие только в производительности. Саги и 2PC оба обеспечивают ACID-гарантии, но саги быстрее, потому что используют асинхронный обмен сообщениями.", misconception: "Разница в производительности реальна, но является следствием структурного различия: саги не обеспечивают изоляцию. Они — не 2PC с асинхронностью. Потеря изоляции — первоклассное свойство паттерна саги, а не побочный эффект реализации." },
  ]}
/>

## B2B сага заказа: шаги и компенсирующие действия

Сага размещения заказа биллинговой платформы:

1. **OrderCreated** — `OrderService` создаёт запись заказа локально, статус: `pending`. Компенсация: отменить заказ (пометить как `cancelled`).
2. **ReserveCredit** — `PaymentService` резервирует кредитный лимит клиента на сумму заказа. Компенсация: освободить резервацию кредита.
3. **ReserveInventory** — `InventoryService` резервирует необходимый товар. Компенсация: освободить резервацию инвентаря.
4. **ScheduleShipment** — `ShippingService` планирует доставку. Компенсация: отменить запланированную отправку.

Если `ScheduleShipment` падает (например, нет перевозчика для адреса), сага запускает компенсацию в обратном порядке: отменить запланированную отправку (уже не удалась), освободить резервацию инвентаря, освободить резервацию кредита, отменить заказ.

<FlowDiagram
  label="B2B сага заказа: шаги и компенсирующие действия при сбое"
  caption="Каждый шаг коммитится локально и инициирует следующий. При сбое ScheduleShipment компенсация выполняется в обратном порядке: освободить инвентарь, освободить кредит, отменить заказ. Компенсация — не откат: каждое компенсирующее действие является новым закоммиченным действием."
  nodes={[
    { id: "order", label: "OrderCreated", sub: "локальная транзакция — статус: pending" },
    { id: "credit", label: "ReserveCredit", sub: "локальная транзакция — кредит зарезервирован" },
    { id: "inventory", label: "ReserveInventory", sub: "локальная транзакция — товар зарезервирован" },
    { id: "ship", label: "ScheduleShipment", sub: "локальная транзакция — СБОЙ" },
    { id: "comp-inv", label: "ReleaseInventory", sub: "компенсация — товар освобождён" },
    { id: "comp-credit", label: "ReleaseCredit", sub: "компенсация — кредит освобождён" },
    { id: "comp-order", label: "CancelOrder", sub: "компенсация — заказ отменён" },
  ]}
  edges={[
    { from: "order", to: "credit", label: "успех" },
    { from: "credit", to: "inventory", label: "успех" },
    { from: "inventory", to: "ship", label: "успех" },
    { from: "ship", to: "comp-inv", label: "сбой — компенсация" },
    { from: "comp-inv", to: "comp-credit", label: "компенсация" },
    { from: "comp-credit", to: "comp-order", label: "компенсация" },
  ]}
/>

<Quiz
  id="arch-09-02-quiz-2"
  lessonSlug="02-sagas"
  lang="ru"
  question="Сага биллинговой платформы падает на шаге ScheduleShipment после успешного коммита ReserveCredit и ReserveInventory. Реализация компенсации команды помечает сагу как 'failed' в журнале саги, но не выполняет никаких компенсирующих действий, полагая, что резервации инвентаря и кредита истекут автоматически через 24 часа через scheduled job. Что не так с таким подходом?"
  choices={[
    { label: "24-часовое истечение означает, что инвентарь и кредит заблокированы до 24 часов, не давая другим заказам использовать эти ресурсы. Компенсация саги должна выполняться как можно скорее после обнаружения сбоя — а не в медленном цикле планового очищения. Ресурсы выглядят зарезервированными для конкурентных транзакций в течение всего окна истечения, что напрямую вызывает потерю доступности: чтение инвентаря показывает товар недоступным; проверки кредита показывают ёмкость исчерпанной. Компенсация существует именно для того, чтобы своевременно освобождать ресурсы, делая сбой саги прозрачным для системы за миллисекунды, а не за часы.", correct: true },
    { label: "Подход приемлем как временная мера. Автоматическое истечение — допустимая стратегия компенсации, пока окно истечения достаточно короткое для бизнеса.", misconception: "Компенсация на основе истечения — не компенсация саги, а резервный механизм очистки. Компенсация саги — намеренное, своевременное корректирующее действие, восстанавливающее состояние системы как можно скорее после сбоя. Полагаться на истечение значит считать загрязнение промежуточным состоянием приемлемым, что не так: инвентарь, удерживаемый 24 часа против упавшего заказа, — это потеря доступности для реальных заказов." },
    { label: "Подход неверен, потому что журнал саги должен использовать 2PC, чтобы запись статуса 'failed' была атомарной с компенсирующими действиями.", misconception: "Использование 2PC для записи в журнал саги отрицает смысл использования саг. Журнал саги обычно обновляется через локальную транзакцию в собственной базе оркестратора саги. Компенсирующие действия — локальные транзакции в сервисах-участниках. Саги намеренно избегают распределённых транзакций." },
    { label: "Подход корректен. Так как журнал саги фиксирует сбой, гарантия атомарности саги сохраняется — данные не теряются.", misconception: "Запись сбоя в журнал саги не восстанавливает ресурсы, закоммиченные предыдущими шагами. Атомарность-подобные результаты требуют либо завершения полной саги, либо выполнения компенсации для отмены эффектов завершённых шагов. Без компенсации ресурсы остаются в промежуточном состоянии, видимом и недоступном для других транзакций." },
  ]}
/>

## Оркестрированные vs хореографированные саги

Паттерн саги может быть реализован в любом из стилей координации из урока 01:

**Оркестрированная сага:** выделенный сервис-оркестратор саги содержит полную конечную машину состояний саги. Он направляет каждого участника пошагово, отслеживает завершённые шаги и координирует компенсацию при сбое шага. Поток явный и видимый. Оркестратор может эмитировать события состояния саги для наблюдаемости. Недостаток — узел связанности: все участники зависят от оркестратора.

**Хореографированная сага:** каждый участник реагирует на событие предыдущего шага и эмитирует собственное событие при успехе или сбое. Состояние саги распределено по событиям в журнале событий. Нет центрального координатора. Участники слабо связаны. Недостаток: отслеживание общего состояния саги — и понимание, завершена ли компенсация полностью — требует корреляции событий по всем участникам без единой авторитетной записи.

Для саги заказа биллинговой платформы с четырьмя-двенадцатью сервисами и частыми изменениями бизнес-правил оркестрированная сага лучше подходит: сложность потока оправдывает явную координацию, и бизнес должен добавлять правила вроде «приостановить при превышении кредитного лимита» в одном месте.

<Quiz
  id="arch-09-02-quiz-3"
  lessonSlug="02-sagas"
  lang="ru"
  question="Новый инженер приходит в биллинговую команду и спрашивает: «Почему мы не можем просто обернуть размещение заказа в транзакцию базы данных через OrderService, PaymentService и InventoryService? Мы уже везде используем PostgreSQL». Каков структурный ответ?"
  choices={[
    { label: "Транзакция базы данных обеспечивает ACID-гарантии только в рамках одного соединения с базой (или распределённой XA-транзакции). Каждый сервис владеет своей базой данных — это ключевое ограничение микросервисов. Транзакция, охватывающая несколько независимых баз данных, требует либо координатора распределённых транзакций (с проблемами блокировок и доступности, описанными выше), либо саги. Помимо технического ограничения: даже если бы все сервисы разделяли одну базу (что было бы моделью данных монолита), обернуть вызов внешнего платёжного шлюза в транзакцию базы данных невозможно — шлюз является внешним I/O, а не участником базы данных. Саги — правильный примитив координации для операций, пересекающих границы сервисов и включающих внешний I/O.", correct: true },
    { label: "Технически возможно, но медленно. Транзакция shared database сработала бы, но удерживала бы блокировки на всё время выполнения операций трёх сервисов, вызывая те же таймауты, что и 2PC.", misconception: "Это частично идентифицирует проблему блокировок, но упускает ограничение владения сервисами. В правильной микросервисной архитектуре каждый сервис владеет своей базой данных. Межбазовые транзакции требуют XA или 2PC с описанными режимами сбоев. Более фундаментально: внешние вызовы сервисов (платёжные шлюзы) вообще не могут участвовать в транзакции базы данных." },
    { label: "Возможно, если использовать один shared PostgreSQL инстанс. Реальная проблема в том, что команда преждевременно разбила свои сервисы.", misconception: "Разделение базы данных между сервисами разрушает границу сервиса — два сервиса, разделяющих базу, связаны на уровне модели данных, что отменяет автономность и независимую развёртываемость. Изоляция сервисов включает изоляцию баз данных. Саги существуют именно для координации через эти границы без их разрушения." },
    { label: "Межсервисные транзакции базы данных возможны через PostgreSQL foreign data wrappers (FDW). Команде следует настроить FDW между базами данных.", misconception: "FDW позволяют обращаться к удалённым таблицам как к локальным, но не обеспечивают распределённые ACID-транзакции через независимые инстансы PostgreSQL. Транзакция, читающая и пишущая через FDW, всё равно не выполняет требование I — удалённые записи через FDW не участвуют в гарантии атомарности локальной транзакции так, как требуется для межсервисной координации." },
  ]}
/>

</Explanation>

<KeyTakeaway>Саги координируют межсервисные бизнес-процессы через локальные транзакции и компенсирующие действия, избегая распределённых блокировок. Компенсация — не откат: это новое корректирующее действие постфактум. Саги допускают видимые промежуточные состояния; изоляция не гарантирована. Выбирай оркестрированные саги для сложных потоков; хореографированные — для простых.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-09-02-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое компенсирующее действие в саге и почему оно не то же самое, что откат базы данных?", a: "Компенсирующее действие — новая бизнес-операция, добавляемая для отмены эффекта предыдущего шага саги, который уже был закоммичен. Это не откат, потому что предыдущая транзакция уже долговечно закоммичена в базу данных — откатывать нечего. Например, если ReserveCredit успешно закоммитился и последующий шаг упал, компенсация — новая операция ReleaseCredit, записывающая новую строку или обновляющая столбец для освобождения зарезервированной суммы. Журнал событий пополняется ещё одной записью; он не усекается. Это то же самое, почему в event sourcing используются компенсирующие события (OrderApprovalReverted) вместо удалений." },
    { q: "Почему саги не обеспечивают изоляцию и каковы два основных контрмера?", a: "Саги используют локальные транзакции с независимыми коммитами. Между коммитом шага N и выполнением компенсации для шага N (если последующий шаг падает) промежуточное состояние шага N видимо конкурентным читателям и писателям — нет распределённой блокировки, скрывающей его. Два основных контрмера: (1) Семантические блокировки — помечать ресурс как «в саге» перед его изменением, чтобы конкурентные транзакции воспринимали его как в процессе и ждали или отказывали, а не работали с ним. (2) Коммутативные обновления — проектировать обновления так, чтобы конечный результат был одинаков независимо от порядка прихода шагов конкурентных саг, делая наблюдаемость промежуточного состояния безвредной." },
    { q: "Каково главное операционное преимущество хореографированной саги перед оркестрированной и каков её главный недостаток?", a: "Преимущество: нет центрального координатора — участники слабо связаны и могут разрабатываться, развёртываться и масштабироваться независимо. Добавление нового участника, реагирующего на шаг, не требует изменений ни в одном существующем сервисе. Недостаток: общее состояние саги распределено по журналу событий, без единой авторитетной записи о том, какие шаги завершились и какая компенсация выполнена. Отладка упавшей саги требует корреляции событий по логам всех сервисов-участников с использованием correlation ID. Неявное состояние саги затрудняет ответ на вопрос «полностью ли скомпенсирована эта сага?» без специального агрегирующего запроса." },
  ]}
/>

<Recap lang="ru">
Эксперимент биллинговой платформы с 2PC провалился не из-за неправильной реализации, а потому что 2PC структурно непригоден для высоконагруженных распределённых систем: медленные участники удерживают блокировки, каскадирующиеся в таймауты, а падение координатора оставляет участников в неопределённом состоянии.

Саги заменяют распределённую блокировку локальными транзакциями и компенсирующими действиями. Каждый шаг коммитится независимо. При сбое последующего шага компенсация выполняется в обратном порядке — не как откат, а как новое корректирующее действие, добавленное к записи. Зарезервированный инвентарь явно освобождается; списанный кредит явно возвращается.

Стоимость — изоляция: между коммитом шага N и выполнением его компенсации промежуточное состояние видимо. Это не баг — это структурное следствие замены распределённой блокировки локальными транзакциями. Контрмеры — семантические блокировки и коммутативные обновления — управляют экспозицией, но не устраняют её.

Координация саги может быть оркестрированной (явной, видимой, централизованной) или хореографированной (неявной, развязанной, с распределённым состоянием). Правильный выбор зависит от сложности потока, частоты изменения бизнес-правил и важности наличия единого авторитетного представления о состоянии саги в любой момент времени.
</Recap>
