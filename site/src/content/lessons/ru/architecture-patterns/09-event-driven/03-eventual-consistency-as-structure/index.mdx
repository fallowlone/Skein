---
slug: "03-eventual-consistency-as-structure"
lang: ru
track: "architecture-patterns"
unit: "09-event-driven"
order: 3
title: "Согласованность в конечном счёте как архитектурный выбор"
summary: "Проектирование для eventual consistency означает решение проблемы двойной записи через transactional outbox, идемпотентность консюмеров и явный дизайн read-your-writes. Согласованность — структурное решение, а не послесловие."
estMin: 25
status: ready
lessonType: topic
level: senior
concepts:
  - dual-write
  - transactional-outbox
  - idempotent-consumer
  - at-least-once-delivery
  - eventual-consistency
  - change-data-capture
prereqs: []
sources:
  - https://microservices.io/patterns/data/transactional-outbox.html
  - https://microservices.io/patterns/data/idempotent-consumer.html
  - https://www.confluent.io/blog/exactly-once-semantics-are-possible-heres-how-apache-kafka-does-it/
  - https://microservices.io/patterns/data/saga.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
OrderService биллинговой платформы сохранял состояние заказа в PostgreSQL, а затем публиковал событие OrderPlaced в Kafka. Простая схема. Но под нагрузкой сервис периодически падал после записи в базу данных и до публикации в Kafka. Событие терялось. Downstream-сервисы — инвентарь, платёж, доставка — никогда его не получали. Заказы зависали в промежуточном состоянии: подтверждённые в базе данных, невидимые для всех остальных. Команда добавила логику ретраев. Теперь после падения и перезапуска сервис иногда публиковал событие дважды. Downstream-сервисы обрабатывали один и тот же заказ дважды, двойным списанием клиентов. Проблема двойной записи — два независимых I/O-операции без гарантии атомарности — имела два режима сбоя, и исправление одного обнажало другой.
</Hook>

<Crux>Outbox фиксит двойную запись: событие и состояние в одной транзакции БД. Консюмеры идемпотентны — at-least-once доставка гарантирована.</Crux>

<Explanation>

## Проблема двойной записи

Каждый раз, когда сервис должен обновить собственное состояние И уведомить остальную систему, он сталкивается с двумя независимыми I/O-операциями:

1. Запись в базу данных (источник истины сервиса)
2. Публикация в брокер сообщений (сигнал для downstream-сервисов)

Эти две записи не имеют гарантии атомарности. Возможен любой из четырёх исходов:

| Запись в БД | Публикация в брокер | Эффект |
|-------------|---------------------|--------|
| успех       | успех               | корректно |
| успех       | сбой                | событие потеряно — downstream завис |
| сбой        | успех               | фантомное событие — downstream действует на несохранённые данные |
| сбой        | сбой                | чистый сбой — без вреда, ретрай безопасен |

Третий случай (фантомное событие) особенно опасен: downstream-сервисы обрабатывают событие для заказа, которого нет в базе данных источника. В биллинге это означает списание клиента за несуществующий заказ.

Первые два случая сбоя — потерянные события и фантомные события — нельзя устранить добавлением логики ретраев. Логика ретраев, повторно публикующая после падения, исправляет «событие потеряно при падении», но вводит «событие опубликовано дважды при ретрае». Проблема двойной записи структурная, а не операционная.

<FlowDiagram
  label="Проблема двойной записи: падение между записью в БД и публикацией в брокер"
  caption="OrderService пишет в PostgreSQL (успех), затем падает до публикации в Kafka. Событие теряется. Downstream-сервисы никогда не получают OrderPlaced. Заказ существует в базе, но невидим для инвентаря и доставки."
  nodes={[
    { id: "svc", label: "OrderService", sub: "записывает в БД, затем падает" },
    { id: "db", label: "PostgreSQL", sub: "строка заказа закоммичена" },
    { id: "kafka", label: "Kafka", sub: "событие никогда не приходит" },
    { id: "inv", label: "InventoryService", sub: "никогда не видит OrderPlaced" },
    { id: "pay", label: "PaymentService", sub: "никогда не видит OrderPlaced" },
  ]}
  edges={[
    { from: "svc", to: "db", label: "1. запись — успех" },
    { from: "svc", to: "kafka", label: "2. публикация — ПАДЕНИЕ" },
    { from: "kafka", to: "inv", label: "нет события" },
    { from: "kafka", to: "pay", label: "нет события" },
  ]}
/>

<Quiz
  id="arch-09-03-quiz-1"
  lessonSlug="03-eventual-consistency-as-structure"
  lang="ru"
  question="Разработчик предлагает решить проблему двойной записи, оборачивая запись в БД и публикацию в Kafka в try/catch: при сбое публикации откатывать транзакцию БД. Старший инженер говорит, что это не решает проблему. Почему?"
  choices={[
    { label: "Откат транзакции БД при сбое Kafka обрабатывает случай, когда Kafka недоступна и возвращает ошибку. Но он не обрабатывает случай, когда сервис падает после получения и подтверждения сообщения Kafka, но до получения подтверждения сервисом — событие опубликовано, БД откатана, и downstream-сервисы действуют на данные, которые так и не были закоммичены. Сетевые разделения и падения процессов могут породить этот сценарий. Единственное надёжное структурное исправление — сделать публикацию атомарной частью транзакции БД — что достигает transactional outbox.", correct: true },
    { label: "Подход корректен. Откат транзакции БД при сбое Kafka обеспечивает, что обе записи либо успешны, либо нет.", misconception: "Подход с откатом обрабатывает случай, когда Kafka недоступна и возвращает ошибку явно. Он не обрабатывает случай, когда Kafka получает сообщение, сеть обрывается до возврата подтверждения сервису, и сервис затем откатывает транзакцию БД. Это порождает опубликованное событие без соответствующего состояния в БД — фантомное событие." },
    { label: "Подход неверен только при конфигурации Kafka с acks=0. С acks=all публикация либо полностью успешна, либо возвращает чёткую ошибку, делая подход с try/catch надёжным.", misconception: "acks=all гарантирует репликацию сообщения до отправки подтверждения, но не решает окно сбоя между получением сообщения Kafka и получением подтверждения сервисом. Падение процесса в этом окне всё равно порождает фантомное событие при откате транзакции БД из-за неполучения ack." },
    { label: "Подход работает для однoinстансных сервисов. Проблема возникает только при нескольких инстансах, где два инстанса могут оба делать ретрай.", misconception: "Режим сбоя с фантомным событием возникает даже с одним инстансом сервиса. Одиночный инстанс может упасть после получения сообщения Kafka, но до получения ack — порождая опубликованное событие с откатанным состоянием БД. Несколько инстансов добавляют дополнительные проблемы конкурентности, но фундаментальная проблема двойной записи существует и с одним инстансом." },
  ]}
/>

## Transactional Outbox

Transactional outbox решает проблему двойной записи, убирая вторую запись из пути обработки запроса сервиса. Вместо прямой публикации в Kafka сервис записывает событие в **таблицу outbox** в той же транзакции базы данных, что и обновление состояния. Транзакция БД является единственным источником истины.

```sql
BEGIN;
INSERT INTO orders (id, customer_id, status) VALUES ($1, $2, 'pending');
INSERT INTO outbox (id, aggregate_type, aggregate_id, event_type, payload)
  VALUES (gen_random_uuid(), 'order', $1, 'OrderPlaced', $3);
COMMIT;
```

Если транзакция БД коммитится, и строка заказа, и строка outbox долговечно записаны. Если она падает, ни одна не записана. Атомарность гарантирована базой данных.

Отдельный **relay-процесс outbox** читает незаопубликованные строки outbox и публикует их в Kafka. После успешной публикации relay помечает строки как обработанные. Relay может быть:

- **На основе polling:** фоновый поток или сервис, запрашивающий `SELECT * FROM outbox WHERE published_at IS NULL LIMIT 100` с интервалом
- **На основе CDC (change-data-capture):** инструменты вроде Debezium читают WAL (write-ahead log) базы данных и стримят вставки в outbox напрямую в Kafka без polling

<FlowDiagram
  label="Transactional outbox: транзакция БД атомарно включает событие"
  caption="OrderService записывает состояние и строку outbox в одной транзакции БД. Relay outbox читает ожидающие строки outbox и публикует в Kafka. При падении relay после публикации, но до пометки как обработанного, он повторно публикует — идемпотентные консюмеры обрабатывают дубликат."
  nodes={[
    { id: "svc", label: "OrderService", sub: "единая транзакция БД" },
    { id: "db", label: "PostgreSQL", sub: "таблицы orders + outbox" },
    { id: "relay", label: "Relay outbox", sub: "polling или CDC" },
    { id: "kafka", label: "Kafka", sub: "at-least-once доставка" },
    { id: "consumer", label: "Консюмер", sub: "идемпотентная обработка" },
  ]}
  edges={[
    { from: "svc", to: "db", label: "1. атомарная транзакция: заказ + строка outbox" },
    { from: "db", to: "relay", label: "2. relay читает outbox" },
    { from: "relay", to: "kafka", label: "3. публикация события" },
    { from: "relay", to: "db", label: "4. пометить строку outbox как опубликованную" },
    { from: "kafka", to: "consumer", label: "5. обработка события" },
  ]}
/>

<Inset kind="note">
Паттерн outbox перекладывает проблему надёжности на relay. Relay гарантирует at-least-once доставку: читает строку outbox, публикует в Kafka и помечает строку как обработанную. Если relay падает после публикации, но до пометки, он повторно опубликует при перезапуске. Это намеренно — это источник гарантии at-least-once. Идемпотентные консюмеры обрабатывают дубликат. Вот почему идемпотентность структурна, а не опциональна: корректность relay outbox зависит от того, что консюмеры терпимы к дубликатам.
</Inset>

<Quiz
  id="arch-09-03-quiz-2"
  lessonSlug="03-eventual-consistency-as-structure"
  lang="ru"
  question="Биллинговая платформа реализует transactional outbox. Разработчик говорит: «Теперь, когда outbox гарантирует exactly-once доставку, нашим консюмерам не нужно обрабатывать дубликаты». Что не так с этим утверждением?"
  choices={[
    { label: "Outbox гарантирует at-least-once доставку, а не exactly-once. Relay помечает строку outbox как обработанную после публикации, но если relay падает после публикации в Kafka и до записи пометки, он повторно опубликует при перезапуске. Консюмер получит одно и то же событие дважды. Идемпотентность — ответственность консюмера: outbox не устраняет дубликаты, он только гарантирует, что события не теряются. Exactly-once доставка в распределённой системе требует сквозной координации, значительно более сложной (exactly-once семантика Kafka требует идемпотентности продюсера, транзакционных консюмеров и тщательного управления офсетами — и даже тогда это exactly-once на партицию, а не через системные границы).", correct: true },
    { label: "Утверждение корректно, если relay использует транзакционный API продюсера Kafka. С транзакциями публикация и пометка-как-обработанное могут быть атомарными.", misconception: "Транзакционный API продюсера Kafka обеспечивает exactly-once в кластере Kafka — сообщения не дублируются внутри брокера. Но запись пометки-как-обработанное — в PostgreSQL, а не в Kafka. Нет атомарной транзакции, охватывающей одновременно транзакцию продюсера Kafka и запись в PostgreSQL. Relay всё ещё сталкивается с окном между публикацией в Kafka и обновлением строки outbox." },
    { label: "Утверждение корректно. Таблица outbox выступает журналом дедупликации — если relay видит строку, которую уже обработал, он пропускает её.", misconception: "Таблица outbox помечается после публикации — она не предотвращает повторную публикацию в окне падения. Если relay падает после публикации в Kafka и до пометки строки, строка всё ещё выглядит необработанной при перезапуске. Relay повторно публикует. Консюмер получает дубликат." },
    { label: "Утверждение корректно для PostgreSQL outbox, но не для CDC-based outbox, которые могут производить дубликаты из-за лага репликации лога.", misconception: "Оба — polling-based и CDC-based relay outbox — имеют одинаковое свойство at-least-once. CDC relay читает из WAL и коммитит свой офсет после публикации — если relay падает после публикации, но до коммита офсета, он воспроизводит с последней закоммиченной позиции и повторно публикует. Источник at-least-once одинаков: публикация, затем подтверждение, с окном падения между ними." },
  ]}
/>

## Идемпотентные консюмеры

At-least-once доставка — не сбой доставки, а гарантия. Каждый консюмер в событийно-ориентированной системе должен быть спроектирован для обработки одного и того же события несколько раз с получением того же результата, что и при однократной обработке.

Стандартный структурный подход: поддерживать **таблицу дедупликации**, записывающую обработанные ID событий. Перед обработкой события проверить, есть ли его ID уже в таблице. Если да — пропустить. Если нет — обработать и вставить.

```sql
BEGIN;
-- Проверка дубликата
SELECT 1 FROM processed_events WHERE event_id = $1;
-- Если строки нет: обработать событие и записать его
INSERT INTO processed_events (event_id, processed_at) VALUES ($1, NOW());
UPDATE inventory SET reserved = reserved + $2 WHERE product_id = $3;
COMMIT;
```

Проверка дедупликации и бизнес-обновление должны быть в одной транзакции. Если они разделены, падение между бизнес-обновлением и вставкой записи дедупликации порождает обработанное событие без записи дедупликации — следующая доставка обработает его снова.

## Read-your-writes при eventual consistency

После отправки заказа пользователь ожидает увидеть его немедленно в дашборде. Если дашборд читает из асинхронной проекции, пополняемой событиями, заказ может не появиться несколько сотен миллисекунд. Пользователь отправил, получил успешный ответ, а UI показывает пусто.

Решения:

- **Read-after-write из стороны записи:** для собственных недавних записей пользователя — читать напрямую из сервиса, владеющего состоянием (модель записи), минуя асинхронную проекцию. Требует, чтобы UI знал, у какого сервиса запрашивать данные о собственных недавних данных пользователя.
- **Оптимистичное обновление UI:** обновить UI немедленно при отправке, до обработки события downstream. При отклонении сервером — откатить UI. Это не исправляет underlying eventual consistency, но скрывает от пользователя его задержку.
- **Sticky sessions к одной реплике:** для read-реплик модели записи маршрутизировать чтения пользователя после записи к той же реплике, получившей запись, пока не догонит репликация.

<Quiz
  id="arch-09-03-quiz-3"
  lessonSlug="03-eventual-consistency-as-structure"
  lang="ru"
  question="Команда строит дашборд платежей, читающий из read-модели, обновляемой событиями Kafka. Продакт-менеджер сообщает: «После подтверждения платежа пользователи иногда не видят его в дашборде 2-3 секунды». Лид команды говорит, что это «работает как спроектировано». Разработчик хочет это исправить. Какое исправление архитектурно корректно?"
  choices={[
    { label: "Для только что отправленного платежа пользователя — включить данные платежа непосредственно в ответ API (минуя read-модель) и немедленно вернуть их клиенту для оптимистичного отображения. Клиент может показать платёж немедленно из ответа API, пока read-модель догоняет в фоне. Это read-after-write consistency: для собственных недавних записей пользователя обойти путь eventual consistency. Для платежей других пользователей задержка 2-3 секунды является приемлемым поведением eventual consistency, не требующим исправления.", correct: true },
    { label: "Уменьшить consumer lag Kafka, добавив больше партиций. Задержка 2-3 секунды — проблема производительности, а не проектирования.", misconception: "Уменьшение consumer lag решает пропускную способность, а не фундаментальную задержку eventual consistency. Даже при нулевом consumer lag всё равно есть задержка между коммитом API и потреблением и проецированием события Kafka. Архитектурная проблема в том, что дашборд читает из асинхронной проекции — это свойство проектирования, а не проблема настройки." },
    { label: "Заменить Kafka-based read-модель синхронным запросом к таблице платежей. Eventual consistency неподходящa для финансовых данных.", misconception: "Eventual consistency — это архитектурный выбор с известными компромиссами — она не неприемлема для финансовых данных per se. Многие финансовые системы используют eventual consistency для read-моделей. Замена всей read-модели — большое архитектурное изменение для решения UX-проблемы, которая решается read-after-write consistency для собственных недавних записей пользователя." },
    { label: "Добавить клиентскую задержку 3 секунды перед переходом на дашборд после подтверждения платежа, давая read-модели время догнать.", misconception: "Фиксированная задержка хрупка (lag может превысить 3 секунды под нагрузкой) и ухудшает UX в большинстве случаев, когда read-модель догоняет менее чем за 500ms. Она также не исправляет underlying проблему — она скрывает её с помощью sleep. Read-after-write consistency является корректным структурным решением." },
  ]}
/>

</Explanation>

<KeyTakeaway>Проблема двойной записи — запись в БД и публикация в брокер без гарантии атомарности — требует transactional outbox: записать событие в таблицу outbox в той же транзакции БД, затем relay доставляет его в брокер. Relay гарантирует at-least-once доставку; консюмеры должны быть идемпотентны. Read-your-writes требует явного проектирования, а не надежды.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-09-03-retrieval"
  lang="ru"
  questions={[
    { q: "Каковы два опасных режима сбоя наивного паттерна двойной записи (запись в БД, затем публикация в брокер)?", a: "Первый: сервис падает или брокер недоступен после записи в БД, но до публикации — событие теряется, downstream-сервисы никогда его не видят, система расходится. Второй: сервис публикует успешно, брокер подтверждает, но сервис падает до того, как может записать факт публикации — при перезапуске он повторно публикует, downstream-сервисы получают одно и то же событие дважды и обрабатывают его снова. Добавление логики ретраев исправляет первый режим, но усиливает второй. Паттерн outbox исправляет оба, делая событие частью транзакции БД." },
    { q: "Опишите transactional outbox в одном абзаце: что записывается куда и кто отвечает за доставку?", a: "Сервис записывает и своё изменение состояния, и строку outbox-события в одной транзакции базы данных — атомарно. Если транзакция коммитится, и состояние, и строка outbox существуют долговечно. Отдельный relay-процесс (polling-based или CDC-based) читает незаопубликованные строки outbox и публикует их в брокер сообщений. После успешной публикации relay помечает строку outbox как обработанную. Relay гарантирует at-least-once доставку: при падении после публикации, но до пометки, он повторно публикует при перезапуске. Это означает, что консюмеры должны быть идемпотентны." },
    { q: "Почему проверка дедупликации и бизнес-обновление в идемпотентном консюмере должны быть в одной транзакции базы данных?", a: "Если проверка дедупликации и бизнес-обновление находятся в отдельных транзакциях, падение между ними оставляет систему в несогласованном состоянии. Если бизнес-обновление коммитится первым, а затем происходит падение до вставки записи дедупликации, следующая доставка того же события не найдёт записи дедупликации и обработает событие снова — результат двойного применения. Оборачивание обоих в одну транзакцию гарантирует, что либо и бизнес-обновление, и запись дедупликации существуют, либо ни одна из них." },
  ]}
/>

<Recap lang="ru">
Баг с двойной записью биллинговой платформы имел два режима: потерянные события при падении сервиса после записи в БД, и дублированные события, когда логика ретраев повторно публиковала при перезапуске. Ни один режим нельзя было исправить настройкой параметров ретраев — проблема была структурной.

Transactional outbox полностью убирает вторую запись из пути обработки запроса. Событие записывается как строка outbox в той же транзакции базы данных, что и обновление состояния. Relay обрабатывает доставку асинхронно. Атомарность гарантирована базой данных; в конечном счёте доставка гарантирована поведением повтора relay.

Поведение повтора relay является источником at-least-once доставки — а at-least-once означает, что консюмеры будут получать дубликаты. Идемпотентность — не nice-to-have; это контракт, делающий паттерн outbox рабочим. Консюмер, обрабатывающий одно и то же событие дважды и производящий двойное списание, не является идемпотентным. Таблица дедупликации, транзакционно привязанная к бизнес-обновлению, является структурным исправлением.

Eventual consistency при таком дизайне — осознанный выбор: система принимает, что downstream-состояние отстаёт от источника на миллисекунды-секунды, в обмен на доступность, пропускную способность и свободу эволюции сервисов независимо. Read-your-writes — наиболее частый видимый пользователю симптом этого отставания, и он требует своего структурного ответа — а не sleep и надежды.
</Recap>
