infographic chart-column-simple
data
  title Блокирующий I/O упирается в стену памяти
  desc Поток-на-соединение резервирует ~1–2 МБ стека на поток, поэтому память под стеки в ГБ растёт линейно с числом соединений — 10k ≈ 10+ ГБ
  values
    - label 1k соединений
      value 1
    - label 5k соединений
      value 5
    - label 10k соединений
      value 10
theme
  palette #14b8a6 #f59e0b #f43f5e
