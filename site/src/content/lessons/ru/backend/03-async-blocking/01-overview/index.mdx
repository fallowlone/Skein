---
concepts:
- blocking-io
- non-blocking-io
- thread-per-connection
- event-loop
- epoll
- c10k
- concurrency-vs-parallelism
deepensInto:
- backend/03-async-blocking/02-the-event-loop
estMin: 11
lang: ru
lessonType: topic
level: junior
order: 1
prereqs:
- backend/01-request-lifecycle/05-streaming-and-backpressure
slug: 01-overview
sources:
- https://nodejs.org/learn/asynchronous-work/event-loop-timers-and-nexttick
- https://www.systemoverflow.com/learn/os-systems-fundamentals/io-models/blocking-vs-non-blocking-io-memory-and-threading-trade-offs
- https://tuhuynh.com/en/posts/nio-under-the-hood/
spiral:
- latency
status: ready
summary: Сервер большую часть жизни ждёт I/O. Блокирующий I/O паркует целый поток
  на каждое ожидание, поэтому конкурентность стоит памяти; неблокирующий отдаёт ожидание
  ядру и даёт одному потоку жонглировать тысячами сокетов через event loop.
title: 'Блокирующий vs неблокирующий I/O: два способа ждать'
track: backend
unit: 03-async-blocking
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Замерь время типичного обработчика запроса — и удивишься, насколько мало в нём *твоего* кода. Он читает строку из Postgres, дёргает платёжный API, пишет строку в лог — и тратит 95% реального времени просто на ожидание ответа. Вся суть бэкенд-конкурентности сводится к одному: что делает программа, пока ждёт? На этот вопрос есть два ответа, делящие всю область пополам. Один паркует поток на каждое ожидание. Другой отказывается кого-либо парковать и просит ядро тронуть его за плечо, когда данные готовы.
</Hook>

<Crux>Блокирующий I/O паркует поток на каждое соединение; неблокирующий даёт одному потоку мультиплексировать тысячи сокетов через event loop.</Crux>

<Explanation>

## Ожидание — это и есть работа

Бэкенд — это в основном I/O-машина. Чтение с диска, запросы к базе, исходящий HTTP, запись в сокет — каждая операция *медленная* относительно CPU (микросекунды и миллисекунды, тогда как процессор выполняет миллиарды инструкций в секунду). Поэтому первый вопрос проектирования никогда не «как быстр мой код», а «как рантайм проводит ожидание». Две модели I/O дают противоположные ответы, и выбор определяет, как сервер масштабируется, сколько памяти ест и как падает под нагрузкой.

## Блокирующий I/O: поток на соединение

В **блокирующей** модели поток вызывает `read()`, и операционная система *приостанавливает этот поток* до прихода байтов. Поток запаркован — занимает свой стек и слот планировщика — и ничего полезного не делает. Чтобы обслужить второе соединение конкурентно, нужен второй поток, третьему — третий, и так далее: **поток-на-соединение** (thread-per-connection).

Это просто и легко рассуждать — код читается сверху вниз, каждая строка ждёт предыдущую — но масштабируется *добавлением потоков*, а потоки не бесплатны. Каждый поток ОС резервирует примерно **1–2 МБ** стека, поэтому 10 000 конкурентных соединений означают порядка **10+ ГБ** памяти только под стеки, плюс тысячи переключений контекста в секунду, пока планировщик тасует запаркованные потоки. Модель меняет память на простоту.

## Неблокирующий I/O: один поток, много сокетов

В **неблокирующей** модели сокет переводится в неблокирующий режим, и `read()` возвращается *немедленно* — либо с данными, либо с «пока не готово». Вместо того чтобы парковаться, поток регистрирует интерес к множеству сокетов через средство ядра — **epoll** на Linux, **kqueue** на BSD/macOS — и задаёт один вопрос: «кто из этих тысяч файловых дескрипторов готов прямо сейчас?» Ядро возвращает только готовые, примерно за **O(1)** независимо от того, сколько их под наблюдением. Поток обслуживает эти, затем спрашивает снова. Этот цикл и есть **event loop**.

Один поток поэтому способен тянуть десятки тысяч соединений, потому что трогает только те сокеты, у которых есть реальная работа. Цена — другая форма кода: нельзя читать сверху вниз и «ждать» — ты регистрируешь колбэк (или `await`), и цикл вызовет тебя позже. Логика, которая была прямой линией, превращается в набор продолжений (continuations).

<Inset kind="why" lang="ru">
Почему средство ядра так важно? Наивный способ следить за множеством сокетов — пройти по всем и спросить «готов? готов? готов?» — это `select`/`poll`, и он стоит O(n) за проход, поэтому слежка за 10 000 сокетов означает сканирование всех 10 000 каждый раз, даже если готов один. `epoll`/`kqueue` переворачивают это: ты регистрируешь набор один раз, а ядро отдаёт *только* те дескрипторы, что стали готовы, поэтому стоимость зависит от числа *активных* соединений, а не *всех*. Это и есть механизм, делающий «один поток, 50 000 простаивающих keep-alive соединений» по-настоящему дешёвым — простаивающие почти ничего не стоят, потому что цикл не заглядывает к ним, пока у них нет данных.
</Inset>

## Постановка C10k и реальный компромисс

Этот раскол назвали **проблемой C10k** (~1999): как обслужить 10 000 конкурентных клиентов на одной машине? Поток-на-соединение упёрся в стену памяти и переключений контекста; модель event loop — Nginx, Node.js, Netty, Redis — стала ответом. Честная сводка:

- **Блокирующий / поток-на-соединение** меняет *память и накладные расходы на переключение контекста* на *простоту*. Хорош, когда число соединений умеренное или работа CPU-тяжёлая; код остаётся линейным.
- **Неблокирующий / event loop** меняет *сложность кода* (колбэки, продолжения, никакой парковки) на *масштабируемость при множестве конкурентных, в основном простаивающих соединений*.

Ни одна не «быстрее» в принципе. Для I/O-нагрузок с высокой конкурентностью event loop решительно выигрывает по памяти и числу соединений. Для CPU-нагрузок один поток event loop не быстрее любого другого одного потока — предел, который следующие уроки сделают резким.

<div data-lesson-visual class="overflow-x-auto my-6">

| | Блокирующий (поток-на-соединение) | Неблокирующий (event loop) |
|---|---|---|
| Ожидание | Поток запаркован ОС | Ядро следит за FD, поток идёт дальше |
| 10k соединений | ~10+ ГБ стеков, много переключений | Один поток, память ~ активным соединениям |
| Форма кода | Линейная, сверху вниз | Колбэки / `await`, продолжения |
| Масштабируется через | Добавление потоков | Пропускную способность готовых событий |
| Лучше для | Умеренной конкурентности, CPU-тяжёлой | Высокой конкурентности, I/O-bound |

</div>

<Quiz
  id="async-overview-q1"
  lessonSlug="01-overview"
  lang="ru"
  question="Почему серверу с потоком-на-соединение тяжело держать 50 000 в основном простаивающих keep-alive соединений?"
  choices={[
    { label: "Каждое простаивающее соединение жжёт CPU, крутясь в цикле", misconception: "Простаивающие блокирующие потоки запаркованы ОС, а не крутятся; цена — память и планирование, а не загрузка CPU." },
    { label: "Каждому соединению нужен свой поток, а каждый поток резервирует ~1–2 МБ стека плюс слот планировщика, поэтому доминируют память и переключения контекста", correct: true },
    { label: "Ядро не может открыть больше нескольких тысяч сокетов", misconception: "Лимиты файловых дескрипторов настраиваемы; связывающее ограничение здесь — память на поток и накладные расходы переключений." },
    { label: "Чтобы принять любое соединение, нужны неблокирующие сокеты", misconception: "Блокирующие сокеты прекрасно принимают соединения; проблема в цене потока на соединение, а не в приёме." },
  ]}
/>

<Quiz
  id="async-overview-q2"
  lessonSlug="01-overview"
  lang="ru"
  question="Что `epoll`/`kqueue` дают event loop, чего не даёт наивное сканирование `select`/`poll`?"
  choices={[
    { label: "Делают чтение каждого сокета быстрее на уровне железа", misconception: "Скорость одного чтения не меняется; выигрыш в том, чтобы узнать, *какие* сокеты готовы, не сканируя все." },
    { label: "Возвращают только реально готовые дескрипторы, поэтому стоимость зависит от числа активных соединений, а не от общего числа наблюдаемых", correct: true },
    { label: "Запускают колбэки параллельно на нескольких ядрах CPU", misconception: "Event loop по-прежнему один поток; epoll лишь сообщает о готовности, он не распараллеливает колбэки." },
    { label: "Автоматически превращают блокирующие чтения в неблокирующие", misconception: "Сокеты нужно явно перевести в неблокирующий режим; epoll — это средство уведомления о готовности, а не переключатель режима." },
  ]}
/>

<DragOrder
  id="async-overview-drag1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь по порядку, что делает неблокирующий сервер, чтобы обслужить чтение на одном из множества сокетов:"
  items={[
    "Перевести сокет в неблокирующий режим и зарегистрировать в epoll/kqueue",
    "Спросить ядро, какие из наблюдаемых дескрипторов готовы",
    "Ядро возвращает только готовые дескрипторы",
    "Запустить колбэк для каждого готового сокета, читая доступные байты",
    "Вернуться к началу и снова спросить ядро",
  ]}
/>

</Explanation>

<FlowDiagram label="Блокирующий vs неблокирующий: что делает поток во время ожидания" hue="var(--d-backend)" perRow={4} nodes={[{id:"req",label:"Запрос",sub:"приходит"},{id:"block",label:"Поток запаркован",sub:"1–2 МБ стека"},{id:"nb",label:"epoll/kqueue",sub:"O(1) ready"},{id:"loop",label:"Event loop",sub:"обслуживает"}]} edges={[{from:"req",to:"block",label:"блокирующий"},{from:"req",to:"nb",label:"неблокирующий"},{from:"nb",to:"loop"}]} caption="Блокирующий паркует поток на каждое ожидание (~1–2 МБ, ~10 ГБ на 10k соединений); неблокирующий регистрирует в epoll/kqueue, возвращает только готовые дескрипторы, один поток тянет тысячи." />

<KeyTakeaway>Сервер большую часть времени ждёт I/O, и модель I/O решает, что происходит во время ожидания. Блокирующий I/O паркует поток на соединение — простой линейный код, но ~1–2 МБ на поток означают, что 10k соединений стоят 10+ ГБ и тяжёлые переключения контекста. Неблокирующий I/O делает сокеты неблокирующими и через epoll/kqueue даёт одному потоку обслуживать только готовые дескрипторы, масштабируясь до десятков тысяч соединений ценой кода в форме колбэков. Проблема C10k назвала этот раскол; event loop победил для высококонкурентной I/O-нагрузки.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="async-overview-retrieval"
  lang="ru"
  questions={[
    { q: "Почему «как рантайм проводит ожидание» — центральный вопрос для бэкенда, а не сырая скорость кода?", a: "Бэкенд — это в основном I/O-машина: чтение с диска, запросы к базе, исходящий HTTP и запись в сокет занимают микросекунды и миллисекунды, тогда как CPU выполняет миллиарды инструкций в секунду, поэтому обработчик запроса проводит подавляющую часть реального времени в ожидании, а не в вычислениях. Значит, доминирующее проектное решение — что рантайм делает во время этих ожиданий, а не как быстра маленькая доля твоего кода. Две модели I/O отвечают на это противоположно — одна паркует поток на каждое ожидание, другая отказывается парковать и даёт ядру сигналить о готовности — и этот единственный выбор определяет, как сервер масштабируется, сколько памяти использует и как ведёт себя под нагрузкой." },
    { q: "Как работает блокирующий поток-на-соединение и какова его цена масштабирования?", a: "В блокирующей модели поток вызывает read(), и операционная система приостанавливает этот поток до прихода байтов, поэтому поток запаркован и ничего не делает, но всё ещё держит стек и слот планировщика. Чтобы обслужить N конкурентных соединений, нужно примерно N потоков. Код прост и линеен — каждая строка ждёт предыдущую — но потоки дороги: каждый поток ОС резервирует около 1–2 МБ стека, поэтому 10 000 конкурентных соединений означают порядка 10+ ГБ только под стеки, плюс тысячи переключений контекста в секунду, пока планировщик тасует запаркованные потоки. Модель меняет память и накладные расходы планирования на простоту, что нормально при умеренной конкурентности или для CPU-тяжёлой работы, но упирается в стену под множеством соединений." },
    { q: "Как неблокирующий I/O с event loop обслуживает много соединений на одном потоке и что вносят epoll/kqueue?", a: "Сокеты переводятся в неблокирующий режим, поэтому read() возвращается немедленно — либо с данными, либо с «не готово». Вместо парковки единственный поток регистрирует интерес к множеству сокетов через средство ядра — epoll на Linux, kqueue на BSD/macOS — и многократно спрашивает «какие из этих дескрипторов готовы сейчас?» Ядро возвращает только готовые примерно за O(1) независимо от числа наблюдаемых, поток обслуживает их, затем спрашивает снова; этот цикл и есть event loop. Ключевой вклад epoll/kqueue по сравнению с наивным сканированием select/poll в том, что ты регистрируешь набор один раз и получаешь обратно только ставшие готовыми дескрипторы, поэтому стоимость зависит от числа активных соединений, а не от общего числа наблюдаемых — вот почему десятки тысяч простаивающих keep-alive соединений дёшевы. Цена — код в форме колбэков или await вместо прямой линии." },
  ]}
/>

<Recap lang="ru">
Бэкенд большую часть жизни ждёт I/O, поэтому модель того, *как он ждёт*, определяет всё ниже по течению. Блокирующий I/O паркует поток на каждое ожидание: линейный, лёгкий код, но каждый поток стоит примерно 1–2 МБ и слот планировщика, поэтому поток-на-соединение превращает 10 000 соединений в 10+ ГБ стеков и шторм переключений контекста — память в обмен на простоту. Неблокирующий I/O делает сокеты неблокирующими, возвращается немедленно и регистрирует их в epoll или kqueue, поэтому один поток спрашивает ядро, какие дескрипторы готовы, и обслуживает только их — масштабируясь до десятков тысяч соединений, потому что простаивающие почти ничего не стоят, ценой кода в форме колбэков или await. Проблема C10k назвала этот раздел, и event loop стал стандартным ответом для высококонкурентных I/O-bound серверов. Следующий урок раскрывает сам цикл: упорядоченные фазы, которые он выполняет, очередь микрозадач, которую он осушает между ними, и почему эта конкурентность кооперативная, а не параллельная. Теперь, когда видишь лимит потоков или конфиг epoll, ты точно знаешь, какую половину компромисса настраиваешь.
</Recap>
