---
concepts:
- backpressure
- high-water-mark
- bounded-concurrency
- unbounded-fan-out
- promise-all
- drain-event
- stream-pipeline
deepensInto:
- backend/03-async-blocking/06-throughput-under-load
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 5
prereqs:
- backend/03-async-blocking/04-offloading-cpu-work
slug: 05-backpressure-and-concurrency
sources:
- https://nodejs.org/learn/modules/backpressuring-in-streams
- https://github.com/sindresorhus/p-limit
- https://nodejs.org/api/stream.html
spiral:
- latency
status: ready
summary: Async легко запускает больше работы, чем система закончит, и цикл не остановит.
  Backpressure даёт продюсеру ощутить медленного консьюмера; ограниченная конкурентность
  ограничивает, сколько бежит разом. Без них неограниченный fan-out становится OOM
  и перегрузкой downstream.
title: Backpressure и ограниченная конкурентность
track: backend
unit: 03-async-blocking
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Скрипт миграции читает миллион ID пользователей и «для каждого достань профиль и сделай upsert». Написанный очевидным async-способом — `await Promise.all(ids.map(processUser))` — он запускает миллион конкурентных операций в один и тот же миг. База открывает соединения, пока не откажет, память лезет вверх, пока миллион ожидающих промисов и их замыканий копятся, и процесс убивают по OOM раньше, чем закончится первая тысяча. Код был корректен. *Конкурентность* была неограниченной, а event loop радостно запустит бесконечную работу, которую никогда не сможет закончить.
</Hook>

<Crux>Неограниченная конкурентность заливает память и downstream; backpressure и потолок подгоняют производство к скорости потребления.</Crux>

<Explanation>

## Цикл тебя не остановит

Дар event loop — запустить тысячи операций дёшево — это и заряженное ружьё. Ничто в `Promise.all(items.map(fn))` не ограничивает, сколько `fn` в полёте; он запускает их *все*, немедленно. Для десяти элементов это нормально. Для миллиона это самонанесённый отказ в обслуживании: каждая операция в полёте держит память (буферы, замыкания, ожидающий промис), и каждая молотит то, что вызывает. Две разные дисциплины укрощают это: **backpressure** (дать медленному консьюмеру толкнуть назад быстрого продюсера) и **ограниченная конкурентность** (ограничить, сколько операций бежит разом). Они лечат одну болезнь — производство быстрее, чем потребление — на разных слоях.

## Backpressure: консьюмер толкает назад

**Backpressure** — это сигнал обратной связи от медленного консьюмера быстрому продюсеру: «перестань слать, я полон». В Node-стримах он встроен. Когда ты `writable.write(chunk)` и внутренний буфер превышает **`highWaterMark`** (по умолчанию **16 КБ** для байтовых стримов, **16 объектов** в object mode), `write()` возвращает **`false`** — твой сигнал *перестать писать*, пока стрим не испустит событие **`drain`**, означающее, что буфер опустел. Уважь сигнал — и память остаётся ограниченной; проигнорируй — и Node продолжит буферить каждый chunk в памяти, пока процесс не словит OOM.

Причина, почему `pipe()` и `pipeline()` — рекомендуемый способ соединять стримы, в том, что они проводят это рукопожатие *автоматически* — паузя readable, когда writable полон, возобновляя на drain — поэтому файл в 50 ГБ копируется через несколько сотен КБ живого буфера вместо загрузки целиком. Ручные циклы `read`/`write`, пропускающие танец `false`/`drain`, — классический источник OOM при стриминге.

<Inset kind="why" lang="ru">
Почему `highWaterMark` — порог, а не жёсткий потолок? Это линия, на которой стрим *начинает говорить* «false» — backpressure совещательный, не принудительный. Один `write()` chunk-а в 10 МБ всё равно буферит все 10 МБ, хотя метка 16 КБ; метка лишь управляет тем, когда стрим сигналит о полноте, а не сколько данный write может добавить. Это важно, потому что backpressure защищает тебя, только если ты *проверяешь возвращаемое значение и ждёшь drain*. Код, зовущий `write()` в плотном цикле, игнорируя булево, побеждает весь механизм — буфер растёт без границ независимо от high-water mark. Метка — это сигнал вежливости между кооперирующими сторонами; она ничего не делает для кода, который не слушает.
</Inset>

## Ограниченная конкурентность: ограничь fan-out

Стримы решают случай байтового пайплайна. Другой случай — **N независимых async-задач** — достань эти 10 000 URL, обработай этот миллион строк — где фикс это **лимит конкурентности**: бежать максимум *k* разом, и как только каждая заканчивается, запускать следующую. Миграция выше становится безопасной заменой `Promise.all(ids.map(fn))` на лимитер (например `p-limit(20)`), чтобы в полёте было только 20 операций, или паттерном worker-pool/`for await`, который тянет из списка по мере освобождения ёмкости.

Числа доказывают: последовательный цикл `for...of await` и ограниченный `Promise.all` могут отличаться на порядки по реальному времени (один бенчмарк: ~30 с последовательно против ~340 мс конкурентно), но *неограниченная* конкурентность не обгоняет ограниченную — за пределом того, что downstream способен впитать, лишняя работа в полёте лишь добавляет очередь, память и отказы. Сладкая точка — наибольшее *k*, которое downstream терпит, а не бесконечность. Это *k* обычно задаётся собственным лимитом downstream: пул соединений на 20, лимит API, IOPS диска.

## Три режима, одно решение

Представь спектр. **Последовательный** (`await` в цикле) — по одному за раз: безопасно, медленно, ни на что не давит. **Неограниченный** (`Promise.all` по огромному списку) — всё сразу: быстро стартует, катастрофичен на масштабе. **Ограниченный** (лимит *k*) — продакшен-ответ: достаточно быстро, предсказуемая память, уважает downstream. Старший рефлекс при виде `Promise.all(bigArray.map(...))` мгновенный: *что это ограничивает?* Если размер массива контролируется атакующим или данными, неограниченный `Promise.all` — латентный сбой.

<div data-lesson-visual class="overflow-x-auto my-6">

| Паттерн | В полёте | Скорость | Риск |
|---|---|---|---|
| Последовательный `for await` | 1 | Самый медленный | Нет, но тратит простаивающую ёмкость |
| Неограниченный `Promise.all(map)` | Все N | Быстрый старт, затем коллапс | OOM, перегрузка downstream |
| Ограниченный (лимит *k*) | Максимум *k* | Быстро и стабильно | Настрой *k* под лимит downstream |
| Стрим `pipeline()` | ~`highWaterMark` | Ровно | Нельзя обходить backpressure |

</div>

<Quiz
  id="backpressure-q1"
  lessonSlug="05-backpressure-and-concurrency"
  lang="ru"
  question="`await Promise.all(millionIds.map(processUser))` убивает процесс по OOM. Что на самом деле пошло не так?"
  choices={[
    { label: "У Promise.all баг с большими массивами", misconception: "Promise.all ведёт себя по спецификации; он запускает каждый промис разом, что и есть задуманная (и здесь вредная) семантика." },
    { label: "Он запускает все миллион операций конкурентно, поэтому миллион ожидающих промисов, замыканий и downstream-соединений копятся в памяти разом — неограниченная конкурентность", correct: true },
    { label: "await внутри map блокирует event loop", misconception: "Колбэки map не блокируют цикл; проблема в том, сколько бежит разом, а не в том, что какой-то блокирует." },
    { label: "ID должны были быть числами, а не строками", misconception: "Типы данных нерелевантны; сбой — это число одновременных операций в полёте." },
  ]}
/>

<Quiz
  id="backpressure-q2"
  lessonSlug="05-backpressure-and-concurrency"
  lang="ru"
  question="В Node-стриме `writable.write(chunk)` возвращает `false`. Что это сигналит и что делать?"
  choices={[
    { label: "Запись провалилась и chunk потерян", misconception: "Chunk буферизован, не потерян; false — сигнал управления потоком, не ошибка." },
    { label: "Внутренний буфер прошёл highWaterMark — перестань писать и жди событие `drain` перед продолжением, иначе память растёт без границ", correct: true },
    { label: "Стрим закрыт и его надо переоткрыть", misconception: "Стрим открыт и принимает данные; false лишь значит, что буфер пока полон." },
    { label: "Backpressure отключён и записи теперь синхронны", misconception: "false — это ровно работающий backpressure; он просит паузу, не сигналит, что выключен." },
  ]}
/>

<Quiz
  id="backpressure-q3"
  lessonSlug="05-backpressure-and-concurrency"
  lang="ru"
  question="Что обычно задаёт правильный лимит конкурентности *k* для ограниченного fan-out async-задач?"
  choices={[
    { label: "Число элементов для обработки", misconception: "k про то, сколько бежит разом, независимо от общего числа; использовать N — это снова неограниченность." },
    { label: "Собственная ёмкость downstream — размер пула соединений, лимит API или IOPS диска — потому что лишняя работа в полёте за этим пределом лишь добавляет очередь и отказы", correct: true },
    { label: "Всегда число ядер CPU", misconception: "Ядра ограничивают CPU-работу; эти задачи I/O-bound, поэтому связывающий лимит — downstream-ресурс, а не ядра." },
    { label: "Как можно выше, потому что больше конкурентности всегда быстрее", misconception: "За пределом того, что впитывает downstream, больше конкурентности добавляет задержку и ошибки, а не пропускную способность; ограниченная обгоняет неограниченную." },
  ]}
/>

</Explanation>

<KeyTakeaway>Event loop запустит больше работы, чем способен закончить, поэтому ты должен наложить лимиты. Backpressure — это сигнал медленного консьюмера быстрому продюсеру: запись в стрим возвращает false, когда буфер проходит highWaterMark (16 КБ / 16 объектов), веля ждать drain — что pipe() и pipeline() проводят автоматически, поэтому огромные файлы текут через крошечные живые буферы. Ограниченная конкурентность ограничивает независимые задачи k в полёте (p-limit, пул воркеров) вместо неограниченного Promise.all(map), который OOM-ит и перегружает downstream. Последовательный безопасен-но-медленен, неограниченный быстр-затем-коллапс, ограниченный — продакшен-ответ, а k задаётся лимитом downstream, не бесконечностью.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="backpressure-retrieval"
  lang="ru"
  questions={[
    { q: "Почему Promise.all по огромному массиву вызывает сбой и какой фикс?", a: "Promise.all(items.map(fn)) запускает каждый fn немедленно и конкурентно; ничто не ограничивает, сколько в полёте. Для нескольких элементов это нормально, но для миллиона это самонанесённый отказ в обслуживании: каждая операция в полёте держит память в буферах, замыканиях и ожидающем промисе, и каждая молотит то, что вызывает, поэтому база открывает соединения, пока не откажет, и память лезет, пока процесс не убьют по OOM. Код корректен; конкурентность неограниченна. Фикс — ограниченная конкурентность: ограничь максимум k операций в полёте лимитером вроде p-limit(20) или паттерном worker-pool / for-await, тянущим новую работу по мере освобождения ёмкости, чтобы память и нагрузка downstream оставались предсказуемыми независимо от числа элементов." },
    { q: "Как работает backpressure Node-стримов и почему pipe/pipeline предпочтительнее ручных циклов?", a: "Backpressure — сигнал обратной связи от медленного консьюмера быстрому продюсеру, означающий «стоп, я полон». Когда ты зовёшь writable.write(chunk) и внутренний буфер превышает highWaterMark — по умолчанию 16 КБ для байтовых стримов или 16 объектов в object mode — write() возвращает false, что твой сигнал перестать писать, пока стрим не испустит событие drain, означающее, что буфер опустел. Уважь это — память остаётся ограниченной; проигнорируй — Node буферит каждый chunk, пока не словит OOM. pipe() и pipeline() предпочтительны, потому что проводят это рукопожатие автоматически, паузя readable, когда writable полон, и возобновляя на drain, поэтому файл в 50 ГБ копируется через несколько сотен КБ живого буфера. Ручные циклы read/write, пропускающие танец false/drain, — классический OOM при стриминге. Заметь, highWaterMark — лишь порог для того, когда стрим сигналит о полноте, а не жёсткий потолок — один огромный write всё равно буферит целиком — поэтому backpressure защищает, только если ты реально проверяешь возвращаемое значение и ждёшь drain." },
    { q: "Сравни последовательную, неограниченную и ограниченную конкурентность и объясни, как выбрать k.", a: "Последовательная (await в цикле for...of) бежит по одной операции за раз: безопасно и без давления, но медленно, и тратит простаивающую ёмкость, ожидая на I/O. Неограниченная (Promise.all по большому списку) запускает всё разом: быстро стартует, но коллапсирует на масштабе через OOM и перегрузку downstream. Ограниченная конкурентность бежит максимум k разом и запускает следующую, как каждая заканчивается: достаточно быстро, предсказуема в памяти и уважает downstream — продакшен-ответ. Бенчмарки показывают, что последовательная против конкурентной могут отличаться на порядки (около 30 с против 340 мс в одном случае), но неограниченная не обгоняет ограниченную, потому что за пределом того, что downstream способен впитать, лишняя работа в полёте лишь добавляет очередь, память и отказы. Поэтому k должно быть наибольшим значением, которое downstream терпит — обычно диктуемым размером пула соединений, лимитом API или IOPS диска — а не бесконечностью и уж точно не числом элементов." },
  ]}
/>

<Recap lang="ru">
Та же дешевизна, что даёт event loop запустить тысячи операций, даёт ему и запустить работу, которую он никогда не сможет закончить, и он тебя не остановит — Promise.all по массиву в миллион элементов запускает миллион операций разом и убивает процесс по OOM, молотя каждый downstream. Две дисциплины подгоняют скорость производства к скорости потребления. Backpressure — это консьюмер, толкающий назад продюсера: запись в стрим возвращает false за highWaterMark (16 КБ или 16 объектов), и ты должен ждать drain, что pipe и pipeline делают автоматически, поэтому огромные файлы текут через крошечные живые буферы; high-water mark — лишь сигнальный порог, поэтому код, игнорирующий булево, побеждает его целиком. Ограниченная конкурентность ограничивает независимые задачи k в полёте, заменяя неограниченный fan-out лимитером или пулом воркеров, где k задаётся тем, что downstream способен впитать — пул на 20, лимит API, IOPS диска — потому что за этим больше конкурентности покупает лишь очередь и отказ. Последовательный безопасен, но медлен, неограниченный быстр, затем катастрофичен, ограниченный — ответ. С работой, сброшенной с цикла, и притоком, придушенным до ёмкости, финальный урок отдаляется на всю систему под нагрузкой: как очередь заставляет хвостовую задержку взрываться у насыщения и почему один цикл — это одно ядро.
</Recap>
