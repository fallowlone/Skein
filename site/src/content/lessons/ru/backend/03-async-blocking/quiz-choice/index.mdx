---
concepts:
- io-models
- event-loop-schedule
- event-loop-blocking
- libuv-threadpool
- bounded-concurrency
- tail-latency
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://nodejs.org/learn/asynchronous-work/dont-block-the-event-loop
- https://nodejs.org/api/worker_threads.html
- https://nodejs.org/learn/modules/backpressuring-in-streams
status: ready
summary: "Синтез с множественным выбором по всему юниту async/blocking — модели I/O, расписание event loop, что его блокирует, вынос CPU-работы, ограниченная конкурентность и хвостовая задержка под нагрузкой."
title: 'Async vs blocking: тест с множественным выбором'
track: backend
unit: 03-async-blocking
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов через весь юнит. Каждый отражает решение, которое ты принимаешь в реальном инциденте — не определение для зубрёжки, а компромисс, который надо взвесить, пока loop горит, а хвост растёт.
</Hook>

<Goal lang="ru">
Убедись, что ты связываешь модель I/O, расписание event loop, что его замораживает, куда выносить CPU-работу, как ограничить fan-out и почему хвост взрывается у насыщения — синтез, к которому вели шесть уроков.
</Goal>

<Quiz
  id="03-async-blocking-choice-1"
  lessonSlug="03-async-blocking"
  lang="ru"
  question="Сервис A — thread-per-connection приложение, держащее 50 000 в основном простаивающих keep-alive сокетов; сервис B делает то же на event loop. Оба почти простаивают по CPU. Почему A падает, а B чувствует себя комфортно?"
  choices={[
    { label: "Каждое соединение A удерживает поток, который резервирует ~1–2 МБ стека плюс слот планировщика, так что 50k простаивающих соединений стоят десятки ГБ и тяжёлое переключение контекста; loop B трогает только готовые дескрипторы через epoll/kqueue, поэтому простаивающие почти ничего не стоят.", correct: true },
    { label: "A крутит CPU на каждом простаивающем соединении, а B их усыпляет.", misconception: "Простаивающие блокирующие потоки паркуются ОС, а не крутятся — CPU у A остаётся низким. Цена — это память под стеки и накладные расходы планирования, а не спин CPU." },
    { label: "B может открыть больше файловых дескрипторов, потому что event loop обходит лимит FD.", misconception: "Лимиты FD — настраиваемый per-process параметр, не зависящий от модели I/O; оба упираются в один лимит ядра. Реальная разница — в стоимости потока на соединение." },
    { label: "A медленнее на каждом чтении, потому что блокирующие чтения по своей природе медленнее неблокирующих.", misconception: "Задержка одного чтения по сути та же; epoll/kqueue меняет лишь то, как ты узнаёшь, какие сокеты готовы, а не скорость отдельного чтения." },
  ]}
/>

<Quiz
  id="03-async-blocking-choice-2"
  lessonSlug="03-async-blocking"
  lang="ru"
  question="Внутри I/O-колбэка ты планируешь setTimeout(a, 0), setImmediate(b) и Promise.resolve().then(c). Что выполнится первым и какой принцип это решает?"
  choices={[
    { label: "c, затем b, затем a — loop дренирует microtasks (promise-реакции) до пустоты после текущего колбэка перед любым macrotask; setImmediate выполняется в фазе check сразу после poll; таймер ждёт фазу timers на следующем тике.", correct: true },
    { label: "a, b, c — колбэки срабатывают в порядке исходника.", misconception: "Порядок исходника никогда не решает выполнение в event loop; решают фаза колбэка и дренаж microtasks. Promise-реакция обгоняет оба macrotask." },
    { label: "b, a, c — macrotasks всегда обгоняют microtasks.", misconception: "Наоборот: microtasks имеют более высокий приоритет и дренируются до пустоты после каждого колбэка, так что promise-реакция выполняется раньше любого таймера или setImmediate." },
    { label: "Все три сразу, на отдельных потоках libuv.", misconception: "Loop — один поток; эти колбэки в очереди и упорядочены, никогда не одновременны. Потоки libuv выполняют нативный I/O, а не твои JS-колбэки." },
  ]}
/>

<Quiz
  id="03-async-blocking-choice-3"
  lessonSlug="03-async-blocking"
  lang="ru"
  question="Двухстрочный эндпоинт /health начинает таймаутить, но только когда дёргают route отчётов. Этот route запускает JSON.parse на теле 40 МБ. Почему страдает health и какой сигнал диагностики правильный?"
  choices={[
    { label: "Парсинг — синхронная CPU-работа на единственном потоке loop, поэтому loop не может вернуться в poll, чтобы обслужить любой другой запрос — head-of-line blocking всего процесса. Event-loop lag (опоздавший таймер) измеряет заморозку лучше, чем загрузка CPU.", correct: true },
    { label: "Health и отчёты делят соединение к БД, которое парсинг держит открытым.", misconception: "Health не трогает БД, а JSON.parse не открывает соединений — это чистая CPU-работа в памяти. Монополизируется поток loop, а не соединение." },
    { label: "CPU достигает 100%, поэтому планировщик понижает приоритет дешёвого route health.", misconception: "Здесь нет per-route планирования ОС — это один поток. И высокий CPU может быть здоровым; вернее сигнал event-loop lag — разрыв между тем, когда колбэк должен был выполниться, и когда loop до него добрался." },
    { label: "Повышение UV_THREADPOOL_SIZE дало бы health выполниться на свободном потоке libuv.", misconception: "Пул libuv выполняет нативный I/O, а не твои JS-обработчики, а парсинг — это JS на loop. Расширение пула ничего не даёт для заблокированного loop." },
  ]}
/>

<Quiz
  id="03-async-blocking-choice-4"
  lessonSlug="03-async-blocking"
  lang="ru"
  question="CPU-тяжёлый ресайз картинки (чистый JS) замораживает loop. Один инженер поднимает UV_THREADPOOL_SIZE с 4 до 32; другой переносит ресайз в worker thread. Кто починил и почему?"
  choices={[
    { label: "Инженер с worker thread. Пул libuv выполняет нативные fs/dns/crypto/zlib, но никогда твой JavaScript, поэтому его расширение не помогает JS CPU-работе; worker thread — это отдельный V8-изолят с настоящим многоядерным параллелизмом для этого ресайза.", correct: true },
    { label: "Инженер с UV_THREADPOOL_SIZE — больший пул даёт ресайзу больше потоков.", misconception: "Пул libuv никогда не выполняет JavaScript, поэтому не может запустить ресайз при любом размере. Его расширение помогает, лишь когда упираешься в пропускную способность нативных fs/dns/crypto." },
    { label: "Оба фикса эквивалентны; выбирай тот, что проще.", misconception: "Они не эквивалентны — только worker thread выносит JS с loop. Изменение пула — no-op для этого бага и классическая ловушка на потерянную неделю." },
    { label: "Ни тот, ни другой — ресайз картинки I/O-bound, поэтому ему место на loop.", misconception: "Ресайз — это CPU-bound вычисление, именно поэтому он замораживает loop и должен быть вынесен в worker (или разбит на чанки)." },
  ]}
/>

<Quiz
  id="03-async-blocking-choice-5"
  lessonSlug="03-async-blocking"
  lang="ru"
  question="Миграция запускает await Promise.all(millionIds.map(processUser)) и её OOM-килит до того, как завершится первая тысяча, пока БД отказывает в новых соединениях. Корневая причина и фикс?"
  choices={[
    { label: "Неограниченная конкурентность: Promise.all стартует все миллион операций сразу, поэтому миллион ожидающих промисов, замыканий и соединений к downstream накапливаются. Фикс: лимит конкурентности (например p-limit с k под размер пула БД), чтобы в полёте было лишь k.", correct: true },
    { label: "У Promise.all баг с очень большими массивами.", misconception: "Promise.all ведёт себя точно по спецификации — стартует каждый промис немедленно. Вред — в неограниченном числе одновременных операций, а не в дефекте." },
    { label: "await внутри .map блокирует event loop.", misconception: "Колбэки map не блокируют loop; они I/O-bound и уступают управление. Проблема в том, сколько их выполняется одновременно, а не в том, что какой-то один блокирует." },
    { label: "Повышение лимита кучи дало бы всем миллиону выполниться безопасно.", misconception: "Больше кучи лишь отсрочит OOM и всё равно затопит БД сверх её лимита соединений. Надёжный фикс — ограничить конкурентность до того, что downstream способен принять." },
  ]}
/>

<Quiz
  id="03-async-blocking-choice-6"
  lessonSlug="03-async-blocking"
  lang="ru"
  question="Node-сервис сидит на 75% CPU со средним 20 мс; небольшой всплеск трафика поднимает CPU до 82%, а p99 прыгает с 80 мс до 1400 мс, пока среднее почти не двигается. Как это читать?"
  choices={[
    { label: "Время ожидания в очереди масштабируется как 1/(1−ρ), поэтому за коленом ~70–80% малые приросты нагрузки дают большие приросты задержки. Среднее смешивает медленные запросы с быстрыми и прячет это; хвост (p99) и event-loop utilization — настоящие сигналы. Работай с запасом.", correct: true },
    { label: "Утечка памяти сработала на 82% утилизации.", misconception: "Ничто не указывает на утечку; нелинейный скачок — это кривая очереди у насыщения, свойство нагрузки. Среднее, читающее 'всё ок', и есть ловушка." },
    { label: "p99 просто шумит на высокой нагрузке, его можно игнорировать.", misconception: "p99 здесь и есть значимый сигнал — он выявляет насыщение, которое среднее скрывает, а fan-out делает этот хвост 1-из-100 типичным опытом страницы с множеством вызовов." },
    { label: "Добавь ядра CPU, и loop линейно из этого выйдет.", misconception: "Один Node loop — это одно ядро JavaScript; лишним ядрам нужны лишние loops (cluster/инстансы). Глубже фикс — работа с запасом ниже колена, а не погоня за обрывом железом." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна цепочка решений: модель I/O задаёт, как ты тратишь ожидание (парковать поток vs мультиплексировать на loop), фазы loop и дренаж microtasks задают порядок колбэков, любой синхронный участок на loop замораживает все соединения сразу, CPU-bound JS живёт в worker thread (а не в большем пуле libuv), а длинная-но-делимая работа может разбиваться на чанки, fan-out надо ограничить до того, что downstream принимает, и у насыщения очередь взрывает хвост — поэтому смотришь p99 и event-loop utilization, а не среднее, и работаешь с запасом.
</Recap>
