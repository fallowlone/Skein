---
concepts:
- worker-threads
- libuv-threadpool
- uv-threadpool-size
- structured-clone
- transferable
- sharedarraybuffer
- task-partitioning
deepensInto:
- backend/03-async-blocking/05-backpressure-and-concurrency
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 4
prereqs:
- backend/03-async-blocking/03-what-blocks-the-loop
slug: 04-offloading-cpu-work
sources:
- https://nodejs.org/api/worker_threads.html
- https://shiftasia.com/community/understanding-the-thread-pool-and-libuv-in-node-js/
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/SharedArrayBuffer
spiral:
- latency
status: ready
summary: За единственным циклом прячутся два пула потоков, и их путаница стоит недель.
  Пул libuv выполняет нативный I/O вроде fs и crypto, но никогда твой JavaScript;
  CPU-bound JS требует worker threads или нарезки, у каждого своя цена — оверхед копирования
  сообщений, сложность разбиения.
title: 'Вынос CPU-работы: worker threads и пул libuv'
track: backend
unit: 03-async-blocking
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Команда профилирует медленный эндпоинт ресайза картинок, находит CPU-тяжёлую работу и «чинит» её, подняв `UV_THREADPOOL_SIZE` с 4 до 32. Ничего не улучшается. Ресайз — это JavaScript, бегущий на потоке цикла, а пул libuv, который они только что увеличили, вообще не выполняет JavaScript — он выполняет нативный I/O. Они тюнили не тот пул. Node прячет *два* ресурса исполнения за единственным event loop, и знание, какая работа куда идёт, — это разница между фиксом и потерянной неделей.
</Hook>

<Crux>Пул libuv обрабатывает нативный I/O, а не твой JS; CPU-bound JavaScript требует worker threads или нарезки, чтобы держать цикл свободным.</Crux>

<Explanation>

## Два пула, две задачи

Когда тянешься к ручке, чтобы починить CPU-проблему, нужно знать, какая ручка на самом деле влияет на какую работу — иначе настроишь не то и потеряешь неделю, точь-в-точь как команда из вводного примера. За единственным циклом — два разных источника параллелизма, и они служат противоположным видам работы:

- **Пул потоков libuv** — небольшой пул (по умолчанию **4 потока**, настраивается до **1024** через `UV_THREADPOOL_SIZE`), выполняющий *нативные* операции без асинхронного примитива ОС: вызовы файловой системы, DNS-резолвинг (`dns.lookup`) и асинхронные функции crypto/zlib. Он **не** выполняет твой JavaScript. Когда ты вызываешь `fs.promises.readFile`, libuv делает блокирующее чтение на потоке пула и постит результат обратно в цикл. Поэтому асинхронный `bcrypt` не замораживает цикл — хеширование бежит на потоке libuv.
- **Worker threads** (`worker_threads`) — реальные, отдельные изоляты V8 со своим event loop, дающие **истинный многоядерный параллелизм для твоего CPU-bound JavaScript**. Сюда относятся ресайз картинок, парсинг больших полезных нагрузок, сжатие и любые тяжёлые вычисления.

Баг с ресайзом теперь очевиден: ресайз — это JS, поэтому ему нужен *worker thread*, а не больший пул libuv. Увеличение libuv помогает, только когда упёрся в пропускную способность fs/dns/crypto — и даже тогда больше потоков пула, чем ядер CPU, в основном добавляет конкуренцию.

## Цена воркера: перемещение данных

Worker threads не бесплатны, и счёт в основном за **передачу данных**. По умолчанию всё, что ты `postMessage` воркеру, **глубоко копируется** алгоритмом structured-clone — для большого буфера эта копия — реальная работа (копия многомегабайтного `ArrayBuffer` замерялась около **268 мс** против примерно **29 мс** при передаче). Два обходных пути важны:

- **Transferables** — передай `ArrayBuffer` в `transferList`, и владение *перемещается* воркеру без копии (отправитель больше не может им пользоваться). Почти нулевая цена передачи.
- **`SharedArrayBuffer`** — разделяемая память, которую видят оба потока сразу, с `Atomics` для безопасной координации. Без копии, без передачи владения; правильный инструмент, когда обеим сторонам нужны одни байты.

Поэтому старший расчёт для выноса такой: *работа должна быть достаточно CPU-тяжёлой, чтобы затмить цену передачи сообщений.* Вынос вычисления на 2 мс воркеру может оказаться медленнее, чем просто выполнить его, как только заплатишь за клон и круговой рейс.

<Inset kind="why" lang="ru">
Почему не поднимать свежий воркер на каждый запрос? Создание потока и старт изолята дороги (десятки миллисекунд и реальная память на воркер), поэтому воркеры-на-запрос превращают CPU-проблему в проблему текучки потоков. Продакшен-паттерн — **пул воркеров**: создать фиксированный набор воркеров один раз (обычно ~один на ядро CPU), раздавать им задачи через очередь и переиспользовать. Это ограничивает параллелизм железом, которое реально есть — восемь ядер не могут по-настоящему выполнить девять CPU-bound задач разом — и избегает оплаты старта на каждом вызове. Это зеркалит пулинг соединений: ресурс дорог в создании, дёшев в переиспользовании и опасен в неограниченном создании. Библиотеки вроде Piscina существуют именно чтобы этим управлять, чтобы ты не писал очередь и жизненный цикл вручную.
</Inset>

## Когда не выносить: нарезай вместо этого

Не каждое долгое вычисление требует потока. Если работу можно **разбить** на мелкие куски, ты можешь выполнить кусок, затем `setImmediate` (или `await` разрешённого промиса), чтобы уступить цикл, затем выполнить следующий кусок — давая I/O-колбэкам вклиниваться между кусками. Это держит всё на потоке цикла без цены передачи, меняя общую пропускную способность (работа теперь конкурирует с запросами) на отзывчивый цикл. Нарезка подходит работе, которая *длинная, но прерываемая* (итерация большого массива, постраничное вычисление). Worker threads подходят работе *монолитной и тяжёлой* (один ресайз, операция crypto) или той, что ты искренне хочешь гонять параллельно на другом ядре.

И иногда правильный ответ — ни то ни другое: если CPU — узкое место по всему сервису, **горизонтальное масштабирование** — больше процессов (cluster) или больше машин — это рычаг, потому что один процесс Node маппится на один цикл, а CPU-bound пропускная способность фундаментально проблема числа ядер.

<div data-lesson-visual class="overflow-x-auto my-6">

| Подход | Что выполняет | Истинный параллелизм | Главная цена |
|---|---|---|---|
| Пул libuv (4, настраивается) | Нативный fs/dns/crypto/zlib | Да, для нативного I/O | Не тот инструмент для JS CPU-работы |
| Worker thread | Твой CPU-bound JS | Да, другое ядро | Копия / передача данных + старт |
| Нарезка + `setImmediate` | Твой JS, кусками | Нет (один цикл) | Ниже пропускная, ручное разбиение |
| Cluster / больше машин | Реплика всего процесса | Да, больше циклов | Сложность ops, координация состояния |

</div>

<Quiz
  id="offload-q1"
  lessonSlug="04-offloading-cpu-work"
  lang="ru"
  question="CPU-тяжёлый ресайз картинки (чистый JavaScript) блокирует цикл. Почему повышение `UV_THREADPOOL_SIZE` не помогает?"
  choices={[
    { label: "Размер пула не может превысить число ядер CPU", misconception: "Он может дойти до 1024; настоящая проблема в том, что пул выполняет, а не в его лимите размера." },
    { label: "Пул libuv выполняет нативные операции (fs, dns, crypto, zlib), а не твой JavaScript, поэтому JS CPU-работа остаётся на цикле; ей нужен worker thread", correct: true },
    { label: "Ресайз картинок — это I/O-bound, а не CPU-bound", misconception: "Ресайз — CPU-bound вычисление; именно поэтому он блокирует и почему фикс — worker thread." },
    { label: "UV_THREADPOOL_SIZE вступает в силу только после рестарта, который команда пропустила", misconception: "Даже применённый верно он не помог бы, потому что пул не исполняет JavaScript." },
  ]}
/>

<Quiz
  id="offload-q2"
  lessonSlug="04-offloading-cpu-work"
  lang="ru"
  question="Ты выносишь большой буфер воркеру и обнаруживаешь, что круговой рейс доминирует копированием. Какой самый прямой фикс?"
  choices={[
    { label: "Увеличить размер кучи воркера", misconception: "Размер кучи не меняет копию structured-clone; сама копия — это цена." },
    { label: "Передать ArrayBuffer в transferList, чтобы владение переместилось без копии, или использовать SharedArrayBuffer, если байты нужны обоим потокам", correct: true },
    { label: "Отправить данные как JSON-строку вместо буфера", misconception: "Сериализация в строку добавляет CPU и обычно больше байт; делает цену передачи хуже, не лучше." },
    { label: "Порождать свежий воркер на каждое сообщение, чтобы распараллелить копию", misconception: "Воркеры-на-сообщение добавляют цену старта и не убирают копию; её убирают передача или разделяемая память." },
  ]}
/>

<Quiz
  id="offload-q3"
  lessonSlug="04-offloading-cpu-work"
  lang="ru"
  question="Когда нарезка с `setImmediate` — лучший выбор, чем worker thread?"
  choices={[
    { label: "Когда работа монолитная и её нельзя разбить", misconception: "Монолитная тяжёлая работа — это случай worker thread; нарезке нужна разбиваемая работа." },
    { label: "Когда работа длинная, но прерываемая, поэтому выполнить кусок и уступить держит цикл отзывчивым без цены передачи сообщений", correct: true },
    { label: "Когда нужен истинный параллелизм по ядрам", misconception: "Нарезка остаётся на одном потоке цикла, поэтому не даёт параллелизма; это случай worker thread." },
    { label: "Когда вычисление всего пара миллисекунд", misconception: "Пара миллисекунд редко вообще требует выноса или нарезки; просто выполни." },
  ]}
/>

</Explanation>

<FlowDiagram label="Два пула за циклом: libuv для нативного I/O, worker thread для CPU JS" hue="var(--d-backend)" perRow={4} nodes={[{id:"loop",label:"Event loop",sub:"главный поток"},{id:"libuv",label:"Пул libuv",sub:"4 потока"},{id:"worker",label:"Worker thread",sub:"изолят V8"},{id:"cb",label:"Колбэк",sub:"результат"}]} edges={[{from:"loop",to:"libuv",label:"fs/dns/crypto"},{from:"loop",to:"worker",label:"CPU-bound JS"},{from:"libuv",to:"cb"},{from:"worker",to:"cb"},{from:"cb",to:"loop"}]} caption="Пул libuv обрабатывает нативный I/O (никогда не твой JS); CPU-bound JavaScript требует worker thread с собственным изолятом V8. Оба постят результаты обратно в цикл как колбэки." />

<KeyTakeaway>Node прячет два ресурса параллелизма за циклом. Пул libuv (4 потока, до 1024 через UV_THREADPOOL_SIZE) выполняет нативный I/O — fs, dns, crypto, zlib — но никогда твой JavaScript, поэтому асинхронный crypto не замораживает цикл. CPU-bound JS требует worker threads: отдельные изоляты V8 с истинным многоядерным параллелизмом, но ты платишь за передачу данных (глубокая копия structured-clone ~268 мс для большого буфера против ~29 мс при передаче), поэтому используй transferables или SharedArrayBuffer и переиспользуемый пул воркеров, а не воркеры-на-запрос. Для длинной-но-прерываемой работы нарезай и setImmediate; когда CPU — узкое место всего сервиса, масштабируйся горизонтально.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="offload-retrieval"
  lang="ru"
  questions={[
    { q: "Какие два пула потоков за event loop и что каждый выполняет?", a: "Пул потоков libuv — небольшой пул, по умолчанию 4 потока, настраиваемый до 1024 через UV_THREADPOOL_SIZE — выполняющий нативные операции без асинхронного примитива ОС: вызовы файловой системы, dns.lookup и асинхронные crypto и zlib. Критически он не выполняет твой JavaScript; когда ты вызываешь fs.promises.readFile, libuv делает блокирующее чтение на потоке пула и постит результат обратно в цикл, поэтому же асинхронное хеширование bcrypt не замораживает цикл. Worker threads совсем другие: реальные отдельные изоляты V8, каждый со своим event loop, дающие истинный многоядерный параллелизм для твоего CPU-bound JavaScript вроде ресайза картинок, большого парсинга или сжатия. Частая ошибка — увеличивать пул libuv ради фикса JS CPU-узкого места — он не может, потому что пул никогда не исполняет JavaScript; этой работе нужен worker thread." },
    { q: "Что стоит использовать worker thread и как снизить эту цену?", a: "Главная цена — передача данных. По умолчанию всё, что postMessage воркеру, глубоко копируется алгоритмом structured-clone, и для большого буфера эта копия значительна — копия многомегабайтного ArrayBuffer замерялась около 268 мс против примерно 29 мс при передаче. Два механизма срезают это: transferables, где ты передаёшь ArrayBuffer в transferList и владение перемещается воркеру без копии (отправитель больше не может им пользоваться), и SharedArrayBuffer, разделяемая память, видимая обоим потокам сразу, с Atomics для безопасной координации. Есть и цена старта — создание потока и изолята занимает десятки миллисекунд и реальную память — поэтому не порождай воркер на запрос; вместо этого создай фиксированный пул воркеров один раз (обычно около одного на ядро) и переиспользуй через очередь задач. Правило: выноси, только когда CPU-работа достаточно тяжёлая, чтобы затмить цену передачи сообщений и кругового рейса." },
    { q: "Когда нарезать работу на цикле вместо выноса и когда не подходит ни то ни другое?", a: "Нарезка подходит работе длинной, но прерываемой: разбей её на мелкие куски, выполни кусок, затем setImmediate или await разрешённого промиса, чтобы уступить цикл и дать I/O-колбэкам вклиниться, затем выполни следующий кусок. Это держит всё на потоке цикла без цены передачи, меняя часть общей пропускной способности (работа теперь конкурирует с запросами) на отзывчивый цикл — хорошо для итерации большого массива или постраничного вычисления. Worker threads вместо этого подходят монолитной тяжёлой работе вроде одного ресайза или операции crypto или всему, что искренне хочешь гонять параллельно на другом ядре. А когда CPU — узкое место по всему сервису, ни то ни другое не помогает достаточно: поскольку один процесс Node — это один цикл, CPU-bound пропускная способность это проблема числа ядер, поэтому рычаг — горизонтальное масштабирование, больше процессов через cluster или больше машин." },
  ]}
/>

<Recap lang="ru">
Два ресурса исполнения прячутся за единственным event loop, и хороший вынос значит отправить каждый вид работы к правильному. Пул потоков libuv — четыре потока по умолчанию, настраивается до 1024 — выполняет нативный I/O вроде fs, dns, crypto и zlib и никогда твой JavaScript, поэтому его увеличение ничего не даёт JS CPU-узкому месту (ловушка ресайза картинок). CPU-bound JavaScript принадлежит worker threads, отдельным изолятам V8, дающим истинный многоядерный параллелизм, но они выставляют счёт за перемещение данных: structured-clone глубоко копирует по умолчанию (около 268 мс для большого буфера против 29 мс при передаче), поэтому тянись к transferables или SharedArrayBuffer и переиспользуй фиксированный пул воркеров, а не порождай по одному на запрос, чтобы избежать текучки старта. Когда работа длинная, но разбиваемая, нарезай её и уступай через setImmediate, чтобы держать цикл отзывчивым ценой пропускной способности; когда CPU — потолок всего сервиса, масштабируйся горизонтально, потому что один процесс — это один цикл. С тяжёлой работой, убранной с критического пути, следующий урок берётся за управление I/O-работой, что остаётся: backpressure и ограниченная конкурентность, чтобы система соответствовала собственной скорости потребления, а не тонула в неограниченном fan-out. Теперь, когда встретишь UV_THREADPOOL_SIZE в обсуждении производительности, сначала проверяй: узкое место в нативном I/O или в JavaScript — потому что только от ответа зависит, релевантна ли эта ручка вообще.
</Recap>
