---
concepts:
- tail-latency
- littles-law
- saturation
- event-loop-utilization
- head-of-line-blocking
- throughput-vs-latency
- runtime-models
deepensInto:
- backend/04-pooling/01-overview
estMin: 17
lang: ru
lessonType: topic
level: senior
order: 6
prereqs:
- backend/03-async-blocking/05-backpressure-and-concurrency
slug: 06-throughput-under-load
sources:
- https://aerospike.com/blog/what-is-p99-latency/
- https://bliudze.me/notes/performance/queueing-basics-latency-budgets/
- https://nodejs.org/api/perf_hooks.html
spiral:
- latency
status: ready
summary: Под нагрузкой среднее лжёт. Теория очередей говорит, что задержка плоская
  до ~70–80% утилизации, затем взрывается нелинейно, и один медленный отрезок в голове
  задерживает всё за ним. Один цикл это одно ядро, поэтому следи за хвостом и утилизацией
  event loop, а не за средним.
title: 'Пропускная способность под нагрузкой: хвостовая задержка и насыщение'
track: backend
unit: 03-async-blocking
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
Сервис работает на 50% CPU со средним временем ответа 20 мс — комфортно. Трафик растёт на 40%, CPU лезет к 75%, и среднее едва двигается до 25 мс. Затем небольшой всплеск толкает его к 82%, и p99 прыгает с 80 мс до 1400 мс. Ничего не сломалось; код не менялся. Система пересекла колено кривой очередей, где задержка перестаёт быть линейной по нагрузке. Среднее прятало это всё время — а среднее это ровно тот не тот показатель, за которым надо следить.
</Hook>

<Crux>У насыщения очередь заставляет задержку взрываться нелинейно; один цикл — это одно ядро, поэтому хвост, а не среднее, — настоящий сигнал.</Crux>

<Explanation>

## Почему задержка взрывается у насыщения

Если ты когда-либо наблюдал, как сервис деградирует катастрофически от, казалось бы, небольшого роста нагрузки, — вот почему. Сервер — это система очередей: запросы приходят, ждут занятый ресурс, обслуживаются. Теория очередей даёт форму ожидания, и она не линейна. Пока утилизация (ρ) лезет вверх, время ожидания масштабируется примерно как **1 / (1 − ρ)** — плоско и дружелюбно до ~**70–80%**, затем обрыв. При ρ = 0,5 фактор 2; при ρ = 0,8 он 5; при ρ = 0,95 он 20. Поэтому сервер может впитывать нагрузку незаметно долго, а потом упасть со стены: **колено** кривой, где малые приросты темпа прихода рождают огромные приросты ожидания. Урок — *работать с запасом* — целиться в 60–70% утилизации связывающего ресурса — именно потому, что последние 20% ёмкости стоят нелинейной задержки и не оставляют ничего на всплески.

**Закон Литтла** (L = λ × W) связывает всё вместе: число запросов в системе равно темпу прихода, умноженному на время-в-системе. Когда W (задержка) взрывается у насыщения, L (конкурентные запросы в полёте) взрывается с ней — больше памяти, больше открытых соединений, больше давления — что та же спираль неограниченной конкурентности из прошлого урока, теперь движимая самой системой, а не твоим кодом.

## Среднее лжёт; следи за хвостом

Среднее складывает медленные запросы в быстрые и прячет их. Реальные пользователи живут в **хвосте** — p95, p99, p99.9 — и хвост это где насыщение, паузы GC и медленные зависимости проявляются первыми. p50 в 20 мс с p99 в 1400 мс означает, что 1 из 100 запросов в 70× медленнее типичного; для страницы, делающей 100 бэкенд-вызовов, это почти *гарантирует*, что каждая страница хотя бы раз попадёт в плохой хвост (fan-out усиливает хвосты). Старшие команды ставят SLO на перцентили, а не на средние, и алертят на тренды p99, потому что среднее будет читаться «нормально» прямо до сбоя.

## Head-of-line blocking, снова — на масштабе системы

Заморозка из раннего урока была внутри одного процесса; та же форма появляется поперёк очереди. **Head-of-line blocking** — это когда один медленный элемент впереди задерживает всё за ним: единственный медленный запрос, держащий ресурс, медленная upstream-зависимость, один жирный синхронный отрезок на цикле. Малая доля застрявшей работы каскадирует — задокументированный паттерн: ~**3% застряли** и задерживают ~**30%** запросов — потому что всё, что в очереди за застрявшим, наследует его ожидание. Поэтому один невынесенный CPU-отрезок (урок 3) или один неограниченный fan-out (урок 5) не просто вредит себе; он отравляет хвост несвязанному трафику.

## Один цикл — это одно ядро — измеряй ELU, выбирай модель

Хребет юнита, заявленный как факт ёмкости: **один event loop Node — это одно ядро JavaScript.** Он чудесно масштабируется по *конкурентному I/O*, но не по *CPU*. Поэтому сигнал насыщения для Node-сервиса — **утилизация event loop (ELU)** — доля времени, когда цикл занят против простоя — в паре с *задержкой* event loop. ELU около 1,0 означает, что цикл — узкое место, и единственные фиксы — делать меньше на запрос, выносить CPU или добавлять циклы (cluster / больше инстансов).

Отступив, модель рантайма — это *выбор*, подогнанный под нагрузку. Event loop блистает на высококонкурентном I/O при малой памяти, но не даёт параллелизма для CPU. Другие модели меняют по-другому: **горутины Go** (планировщик M:N, начальные стеки ~2 КБ, вытесняющий) и **виртуальные потоки Java** (~сотни байт оверхеда, смонтированы на потоках-носителях) дают писать блокирующий код, масштабирующийся до миллионов дешёвых «потоков» с реальным многоядерным параллелизмом. Ни одна не лучшая повсюду — старшее суждение в том, чтобы знать свою нагрузку (I/O-bound vs CPU-bound, уровень конкурентности, бюджет памяти) и выбрать модель, чьи компромиссы подходят, затем гонять её с запасом и следить за хвостом.

<Inset kind="why" lang="ru">
Почему целиться в ~70% утилизации, а не выжимать до 95% ради эффективности? Потому что цена последней доли утилизации платится в валюте, которую ощущают пользователи — хвостовой задержке — и она нелинейна. Переход с 70% на 95% утилизации примерно *учетверяет* ожидаемое ожидание в очереди (1/(1−0,7) ≈ 3,3 против 1/(1−0,95) = 20), поэтому ты меняешь скромную экономию железа на яростную регрессию задержки и нулевой запас на всплеск: всплеск трафика в 10% при 70% впитывается, тот же всплеск при 95% перекидывает тебя за 100% и очередь растёт без границ. «Эффективность», измеряемая как высокая средняя утилизация, — ловушка, оптимизирующая дешёвый ресурс (циклы CPU) за счёт дорогого (предсказуемая задержка и устойчивость к всплескам). Планирование ёмкости на самом деле планирование хвостовой задержки.
</Inset>

<div data-lesson-visual class="overflow-x-auto my-6">

| Утилизация ρ | Фактор очереди 1/(1−ρ) | Что наблюдаешь |
|---|---|---|
| 0,5 | 2× | Плоско, комфортно |
| 0,7 | ~3,3× | Ещё нормально, у колена |
| 0,8 | 5× | Хвост начинает растягиваться |
| 0,95 | 20× | p99 взрывается, нет запаса на всплеск |

</div>

<Quiz
  id="throughput-q1"
  lessonSlug="06-throughput-under-load"
  lang="ru"
  question="CPU идёт 75% → 82%, и p99 прыгает с 80 мс до 1400 мс, пока среднее едва двигается. Что это объясняет?"
  choices={[
    { label: "Утечка памяти внезапно сработала на 82%", misconception: "Никакая утечка не подразумевается; нелинейный прыжок — это очередь у насыщения, свойство нагрузки, а не утечка." },
    { label: "Ожидание в очереди масштабируется как 1/(1−ρ), поэтому за коленом ~70–80% малые приросты нагрузки рождают большие приросты задержки — и хвост это показывает, пока среднее прячет", correct: true },
    { label: "CPU троттлил из-за нагрева", misconception: "Тепловой троттлинг не указан; одна форма кривой объясняет взрыв у колена." },
    { label: "p99 просто шумный и не значим", misconception: "p99 — ровно значимый сигнал здесь; он раскрывает насыщение, которое среднее скрывает." },
  ]}
/>

<Quiz
  id="throughput-q2"
  lessonSlug="06-throughput-under-load"
  lang="ru"
  question="Почему среднее время ответа — обманчивая цель SLO по сравнению с p99?"
  choices={[
    { label: "Средние труднее вычислить, чем перцентили", misconception: "Средние тривиально вычислить; вопрос в том, что они раскрывают, а не в цене математики." },
    { label: "Среднее складывает медленные запросы в быстрые и прячет хвост, где живут реальные пользователи и усиление fan-out, поэтому читается «нормально» прямо до сбоя", correct: true },
    { label: "p99 всегда равен максимальной задержке", misconception: "p99 — это 99-й перцентиль, а не максимум; p99.9 и истинный максимум сидят выше него." },
    { label: "На средние нельзя ставить алерты", misconception: "Можно алертить на средние; суть в том, что это не тот сигнал, а не в том, что на него нельзя алертить." },
  ]}
/>

<Quiz
  id="throughput-q3"
  lessonSlug="06-throughput-under-load"
  lang="ru"
  question="Для Node-сервиса что самый прямой сигнал насыщения и что он подразумевает около 1,0?"
  choices={[
    { label: "Общее использование RAM системы", misconception: "RAM важна, но не сигнал насыщения цикла; цикл может быть насыщен при куче свободной памяти." },
    { label: "Утилизация event loop (ELU): около 1,0 цикл — узкое место, чинится лишь меньшей работой на запрос, выносом CPU или добавлением циклов", correct: true },
    { label: "Число открытых TCP-сокетов", misconception: "Число сокетов отражает соединения, не насыщение цикла; простаивающий цикл может держать много сокетов." },
    { label: "Глубина очереди диска", misconception: "Глубина диска сигналит давление хранилища, не JavaScript-цикл; ELU — сигнал, специфичный для цикла." },
  ]}
/>

</Explanation>

<StackDiagram label="Ожидание в очереди 1/(1−ρ): задержка взрывается за коленом" hue="var(--d-backend)" layers={[{label:"ρ = 0,95 → 20× ожидания",note:"p99 взрывается, нет запаса"},{label:"ρ = 0,80 → 5× ожидания",note:"хвост тянется, у обрыва"},{label:"ρ = 0,70 → 3,3× ожидания",note:"у колена — потолок цели"},{label:"ρ = 0,50 → 2× ожидания",note:"плоско, комфортно, запас"}]} caption="Работай при ≤70% утилизации. Последние 20% ёмкости стоят нелинейной задержки — ρ=0,95 это 20× ожидания от ρ=0,50 — и не оставляют запаса на всплески. Следи за ELU и хвостом (p99), а не за средним." />

<KeyTakeaway>Под нагрузкой среднее лжёт. Сервер — это очередь, и ожидание масштабируется как 1/(1−ρ): плоско до колена ~70–80%, затем нелинейный обрыв (ρ=0,95 значит 20× ожидания), поэтому работай с запасом и следи за хвостом — p95/p99/p99.9 — а не за средним, потому что fan-out делает 1-из-100 медленный запрос попадающим почти на каждую страницу. Head-of-line blocking масштабируется так же: ~3% застрявшей работы могут задержать ~30% запросов, поэтому один невынесенный CPU-отрезок или неограниченный fan-out отравляет несвязанный трафик. Один цикл Node — это одно ядро, поэтому ELU — сигнал насыщения; за этим модель рантайма (event loop vs горутины vs виртуальные потоки) — выбор под нагрузку.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="throughput-retrieval"
  lang="ru"
  questions={[
    { q: "Почему задержка взрывается у насыщения и что это значит для планирования ёмкости?", a: "Сервер — система очередей, и ожидание в очереди не линейно по нагрузке: пока утилизация ρ растёт, время ожидания масштабируется примерно как 1/(1−ρ), что около 2× при ρ=0,5, 3,3× при 0,7, 5× при 0,8 и 20× при 0,95. Поэтому система впитывает нагрузку незаметно до колена около 70–80% утилизации, а затем падает с обрыва — малые приросты темпа прихода рождают огромные приросты ожидания. Закон Литтла (L = λW) связывает задержку с заполнением, поэтому когда W взрывается у насыщения, число конкурентных запросов в полёте L взрывается тоже, потребляя больше памяти и соединений. Вывод — работать с запасом, целиться примерно в 60–70% связывающего ресурса, потому что последняя доля ёмкости стоит нелинейной задержки и не оставляет ничего на всплески; планирование ёмкости на самом деле планирование хвостовой задержки." },
    { q: "Почему следить за хвостом (p99), а не за средним, и как fan-out делает это хуже?", a: "Среднее складывает медленные запросы в быстрые и скрывает их, но реальные пользователи испытывают хвост — p95, p99, p99.9 — где насыщение, паузы GC и медленные зависимости всплывают первыми. p50 в 20 мс с p99 в 1400 мс означает, что 1 из 100 запросов примерно в 70× медленнее типичного. Fan-out усиливает это: страница, делающая 100 бэкенд-вызовов, при хвосте 1-из-100 почти наверняка попадёт в медленный хвост хотя бы на одном вызове, поэтому хвост зависимости становится типичным опытом страницы. Поэтому старшие команды ставят SLO на перцентили и алертят на тренды p99, а не на средние — среднее будет читаться нормально прямо до сбоя, пока хвост двигается первым." },
    { q: "Что значит «один цикл — это одно ядро» для масштабирования и чем отличаются другие модели рантайма?", a: "Один event loop Node исполняет одно ядро JavaScript: он чудесно масштабируется по конкурентному I/O, но не даёт параллелизма для CPU-работы, поэтому сигнал насыщения — утилизация event loop (ELU), доля времени, когда цикл занят, в паре с задержкой event loop. ELU около 1,0 означает, что цикл — узкое место, и единственные фиксы — делать меньше на запрос, выносить CPU воркерам или добавлять циклы через cluster или больше инстансов. Другие рантаймы меняют по-другому: горутины Go используют планировщик M:N с начальными стеками около 2 КБ и вытеснением, а виртуальные потоки Java добавляют лишь сотни байт оверхеда, смонтированные на потоках-носителях, обе дают писать блокирующий код, масштабирующийся до миллионов дешёвых потоков с реальным многоядерным параллелизмом. Ни одна не лучшая повсюду; старшее суждение — подогнать модель под нагрузку — I/O-bound против CPU-bound, уровень конкурентности, бюджет памяти — затем гонять с запасом и следить за хвостом." },
  ]}
/>

<Recap lang="ru">
Под нагрузкой среднее — не тот показатель. Сервер — это очередь, и ожидание в очереди масштабируется как 1/(1−ρ): комфортно плоско до колена около 70–80% утилизации, затем нелинейный обрыв, где ρ=0,95 значит двадцатикратное ожидание, поэтому сервис переходит от «нормально» к «горит» без смены кода. Закон Литтла связывает этот взрыв задержки с соответствующим взрывом конкурентного заполнения, поэтому насыщение питает ту же спираль памяти-и-соединений, что и неограниченный fan-out. Поскольку среднее прячет медленные запросы, хвост — p95, p99, p99.9 — это настоящий сигнал, и fan-out делает один-из-ста медленный вызов типичным опытом страницы на сто вызовов. Head-of-line blocking переносит внутрипроцессную заморозку на масштаб системы, где несколько процентов застрявшей работы задерживают треть запросов, поэтому невынесенный CPU-отрезок или неограниченный map отравляет несвязанный трафик. Факт ёмкости под всем этим: один цикл Node — это одно ядро, ELU (event-loop utilization, доля времени, когда цикл занят) — его датчик насыщения, а сама модель рантайма — event loop, горутины, виртуальные потоки — это решение под нагрузку, гоняемое с запасом. Это закрывает юнит async-и-блокировки и передаёт следующей заботе, что он постоянно вызывал: пулинг дорогих downstream-соединений, которые защищала ограниченная конкурентность. Теперь, когда видишь скачок p99 при нормально выглядящем среднем, знаешь: система пересекла колено кривой очередей — и нужно добавить запас, а не ждать, пока среднее догонит.
</Recap>
