---
concepts:
- event-loop
- libuv-phases
- macrotask
- microtask
- process-nexttick
- cooperative-concurrency
- poll-phase
deepensInto:
- backend/03-async-blocking/03-what-blocks-the-loop
estMin: 13
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- backend/03-async-blocking/01-overview
slug: 02-the-event-loop
sources:
- https://nodejs.org/learn/asynchronous-work/event-loop-timers-and-nexttick
- https://nodejs.org/api/process.html
- https://docs.libuv.org/en/v1.x/design.html
spiral:
- latency
status: ready
summary: 'Event loop — это один поток, выполняющий очереди колбэков в фиксированных
  фазах: timers, poll, check, close — осушая между каждой более приоритетную очередь
  микрозадач. Этот порядок объясняет setTimeout vs setImmediate vs Promise и почему
  конкурентность кооперативная.'
title: 'Event loop: один поток, упорядоченные фазы'
track: backend
unit: 03-async-blocking
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import drainPriority from "./infographics/drain-priority.svg?raw";

<Hook>
Разработчик пишет `setTimeout(fn, 0)` рядом с `setImmediate(fn)` рядом с `Promise.resolve().then(fn)`, ожидая, что они сработают в порядке исходника. Не сработают. Сначала бежит промис, потом — в зависимости от *того, где* стоит код — либо таймер, либо immediate. Это не баг и не гонка; это опубликованное расписание event loop. Цикл — не чёрный ящик, который «когда-нибудь выполнит асинхронщину». Это строгая упорядоченная машина, и как только видишь её фазы, тайминг асинхронности перестаёт быть фольклором.
</Hook>

<Crux>Один поток гоняет колбэки в фазах, осушая микрозадачи между каждой — поэтому конкурентность кооперативная, а не параллельная.</Crux>

<Explanation>

## Цикл — это последовательность фаз

Единственный поток из прошлого урока не выполняет колбэки в порядке прихода. Он крутит фиксированную последовательность **фаз** (в libuv, движке цикла Node), и у каждой фазы своя очередь колбэков, которую надо осушить перед переходом дальше. Именно знание этого порядка превращает предсказание тайминга из гадания в точный расчёт. Те, что важны изо дня в день:

- **Timers** — колбэки, у которых истёк таймаут `setTimeout`/`setInterval`.
- **Poll** — сердце: получить новые I/O-события и выполнить их колбэки (пришёл HTTP-запрос, закончилось чтение файла). Если ничего не ждёт, цикл *блокируется здесь*, ожидая на epoll/kqueue — именно здесь проводится время простоя.
- **Check** — колбэки, запланированные через `setImmediate`, выполняются сразу после poll.
- **Close** — колбэки событий закрытия (`socket.on('close')`).

Один полный проход по фазам — это **тик** (tick) цикла. Внутри фазы колбэки выполняются по одному, до конца — без вытеснения.

## Макрозадачи vs микрозадачи

Два этих слова прячут всю модель тайминга. Очереди фаз держат **макрозадачи**: колбэк таймера, I/O-колбэк, `setImmediate`. Отдельно от них — две очереди **микрозадач** с *более высоким приоритетом*:

- Колбэки **`process.nextTick`** (специфика Node), осушаются первыми.
- Реакции **промисов** (продолжения `.then`/`await`).

Правило: **после завершения каждого отдельного колбэка цикл полностью осушает очереди микрозадач, прежде чем выполнить следующий колбэк или сменить фазу.** Поэтому `Promise.resolve().then(fn)` обгоняет `setTimeout(fn, 0)` — реакция промиса есть микрозадача, выполняемая на ближайшем осушении, тогда как таймер ждёт фазы timers на более позднем тике.

<Infographic
  svg={drainPriority}
  label="Трёхуровневая пирамида приоритета: process.nextTick на вершине (осушается первым), ниже — реакции промисов, в основании — очереди фаз макрозадач (timers, poll, check, close); обе очереди микрозадач опустошаются после каждого колбэка прежде любой макрозадачи."
  caption="Микрозадачи приоритетнее любой фазы макрозадач, а nextTick приоритетнее промисов — именно поэтому разрешённый промис обгоняет setTimeout(0)."
  hue="var(--d-infra)"
/>

<Inset kind="why" lang="ru">
Зачем вообще отдельная, более приоритетная очередь микрозадач? Потому что цепочки промисов должны устаканиться *прежде*, чем программа вернётся к планировщику I/O, иначе порядок был бы непредсказуем между тиками. Микрозадачи позволяют единице синхронно выглядящей асинхронной работы (`await` разрешился, затем следующий `await`) завершиться как группа, прежде чем цикл сделает что-либо ещё. Опасность — зеркальная: микрозадача может запланировать другую микрозадачу, та — ещё одну, неограниченная цепочка (рекурсия `process.nextTick` или сбежавший цикл промисов) морит цикл голодом, потому что микрозадачи осушаются *до пустоты* прежде, чем выполнится любой I/O-колбэк. Так что микрозадачи дают плотный порядок, но потоп микрозадач способен заблокировать фазу poll так же жёстко, как синхронный цикл — цикл никогда не доберётся до своих сокетов.
</Inset>

## setTimeout(0) vs setImmediate

Эти два — классическая путаница. `setImmediate` всегда бежит в фазе **check**, сразу после poll; `setTimeout(fn, 0)` зажат до минимума ~1 мс и бежит в фазе **timers**. Внутри I/O-колбэка (то есть уже в poll или сразу после) `setImmediate` надёжно срабатывает *раньше* следующей фазы timers, поэтому выигрывает. На верхнем уровне скрипта порядок не гарантирован — он зависит от того, сколько времени цикл потратил на запуск. Старший вывод не в мелочи, а в модели: «сразу после I/O» и «после задержки таймера» — это *разные фазы*, а не одна очередь.

## Кооперативная, не параллельная

Поскольку один поток выполняет каждый колбэк до конца без вытеснения, конкурентность event loop **кооперативная**: каждый колбэк должен добровольно *уступить* — завершившись или наткнувшись на `await`, который отдаёт управление — чтобы выполнился любой другой колбэк. Два запроса никогда не выполняют JavaScript *в один и тот же момент*; они чередуются в точках уступки. В этом вся сила (никаких локов, никаких гонок данных по разделяемому состоянию внутри тика) и вся слабость (один колбэк, который никогда не уступает, замораживает всё), что следующий урок делает конкретным.

<div data-lesson-visual class="overflow-x-auto my-6">

| Планировщик | Тип очереди | Фаза / тайминг | Приоритет |
|---|---|---|---|
| `process.nextTick(fn)` | Микрозадача | После текущего колбэка, до промисов | Наивысший |
| `Promise.then` / `await` | Микрозадача | После текущего колбэка, после nextTick | Высокий |
| `setTimeout(fn, 0)` | Макрозадача | Фаза timers, следующий годный тик | Обычный |
| I/O-колбэк | Макрозадача | Фаза poll | Обычный |
| `setImmediate(fn)` | Макрозадача | Фаза check, сразу после poll | Обычный |

</div>

<Quiz
  id="event-loop-q1"
  lessonSlug="02-the-event-loop"
  lang="ru"
  question="Внутри I/O-колбэка ты планируешь `setTimeout(a, 0)`, `setImmediate(b)` и `Promise.resolve().then(c)`. В каком порядке они выполнятся?"
  choices={[
    { label: "a, b, c — в порядке исходника", misconception: "Порядок исходника не определяет выполнение; его определяют фазы цикла и осушение микрозадач." },
    { label: "c, затем b, затем a — промис (микрозадача) осушается первым, затем setImmediate (check, сразу после poll), затем таймер (timers, более поздний тик)", correct: true },
    { label: "b, a, c — макрозадачи всегда обгоняют микрозадачи", misconception: "У микрозадач приоритет выше; реакция промиса осушается раньше любой макрозадачи." },
    { label: "Все три выполнятся одновременно на разных потоках", misconception: "Цикл — один поток; колбэки никогда не выполняются одновременно, они стоят в очереди и упорядочены." },
  ]}
/>

<Quiz
  id="event-loop-q2"
  lessonSlug="02-the-event-loop"
  lang="ru"
  question="Почему рекурсивный `process.nextTick` (или сбежавшая цепочка промисов) может застопорить I/O, хотя никакой синхронный цикл не выполняется?"
  choices={[
    { label: "nextTick бежит на фоновом потоке, конкурирующем за CPU", misconception: "nextTick бежит на единственном потоке цикла; никакого фонового потока-конкурента нет." },
    { label: "Микрозадачи осушаются до пустоты после каждого колбэка прежде любого I/O-колбэка, поэтому самоперепланирующая микрозадача не даёт фазе poll выполниться", correct: true },
    { label: "У nextTick приоритет ниже, поэтому I/O морит его голодом", misconception: "Всё наоборот: nextTick наивысшего приоритета и осушается первым, именно поэтому он способен заморить I/O." },
    { label: "Он бросает ошибку, которая обрушивает фазу poll", misconception: "Никакая ошибка не бросается; цикл просто не продвигается за осушение микрозадач, чтобы дойти до poll." },
  ]}
/>

<DragOrder
  id="event-loop-drag1"
  lessonSlug="02-the-event-loop"
  lang="ru"
  prompt="Расставь по порядку один тик event loop по его основным фазам:"
  items={[
    "Фаза timers: выполнить истёкшие колбэки setTimeout/setInterval",
    "Осушить микрозадачи (nextTick, затем промисы)",
    "Фаза poll: выполнить готовые I/O-колбэки (или ждать на epoll/kqueue)",
    "Фаза check: выполнить колбэки setImmediate",
    "Фаза close: выполнить колбэки событий закрытия",
  ]}
/>

</Explanation>

<FlowDiagram label="Event loop: фиксированные фазы и осушение microtask" hue="var(--d-backend)" perRow={4} nodes={[{id:"timers",label:"Таймеры",sub:"setTimeout"},{id:"micro",label:"Микрозадачи",sub:"nextTick→then"},{id:"poll",label:"Опрос",sub:"I/O готово"},{id:"check",label:"Проверка",sub:"setImmediate"},{id:"close",label:"Закрытие",sub:"socket close"}]} edges={[{from:"timers",to:"micro"},{from:"micro",to:"poll"},{from:"poll",to:"check"},{from:"check",to:"close"},{from:"close",to:"timers",label:"следующий тик"}]} caption="После каждого колбэка цикл осушает microtask (сначала nextTick, затем реакции промисов) прежде, чем перейти к следующей фазе — поэтому разрешённый промис обгоняет setTimeout(0)." />

<KeyTakeaway>Event loop — это один поток, крутящий фиксированные фазы: timers, poll (где выполняются I/O-колбэки и проводится простой), check (setImmediate), close — выполняя каждый колбэк до конца без вытеснения. Между каждым колбэком он полностью осушает более приоритетные очереди микрозадач: сначала process.nextTick, затем реакции промисов. Этот порядок объясняет, почему разрешённый промис обгоняет setTimeout(0) и почему setImmediate (check) и setTimeout(0) (timers) живут в разных фазах. Модель — кооперативная конкурентность: колбэки чередуются только в точках уступки, поэтому самоперепланирующая микрозадача или неуступающий колбэк замораживают цикл.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="event-loop-retrieval"
  lang="ru"
  questions={[
    { q: "Какие основные фазы у event loop и что делает каждая?", a: "Единственный поток цикла крутит фиксированную последовательность фаз, каждую со своей очередью, осушаемой перед переходом дальше. Timers выполняет колбэки, у которых истёк таймаут setTimeout/setInterval. Poll — сердце: получает новые I/O-события и выполняет их колбэки, а если ничего не ждёт, блокируется здесь на epoll/kqueue, где и проводится время простоя. Check выполняет колбэки setImmediate сразу после poll. Close выполняет колбэки событий закрытия, например socket 'close'. Один полный проход — это тик, и внутри фазы колбэки выполняются по одному до конца без вытеснения." },
    { q: "В чём разница между макрозадачами и микрозадачами и каково правило осушения?", a: "Очереди фаз держат макрозадачи: колбэки таймеров, I/O-колбэки, setImmediate. Отдельно от них — две более приоритетные очереди микрозадач: колбэки process.nextTick (осушаются первыми) и реакции промисов из .then/await. Правило в том, что после завершения каждого отдельного колбэка цикл полностью осушает очереди микрозадач прежде, чем выполнить следующий колбэк или перейти к следующей фазе. Поэтому Promise.resolve().then(fn) бежит раньше setTimeout(fn, 0): реакция промиса — микрозадача, выполняемая на самом ближайшем осушении, тогда как таймер должен дождаться фазы timers на более позднем тике. Опасность симметрична — микрозадача, планирующая другую микрозадачу, образует неограниченную цепочку, которая осушается до пустоты прежде любого I/O-колбэка, поэтому морит фазу poll голодом так же, как синхронный цикл." },
    { q: "Почему конкурентность event loop называют кооперативной, а не параллельной, и что из этого следует?", a: "Поскольку один поток выполняет каждый колбэк до конца без вытеснения, каждый колбэк должен добровольно уступить — завершившись или наткнувшись на await, отдающий управление — прежде чем выполнится любой другой колбэк. Поэтому два запроса никогда не выполняют JavaScript в один и тот же момент; они чередуются только в точках уступки. Сила в том, что внутри тика нет локов и гонок данных по разделяемому состоянию, ведь параллельно ничего не выполняется. Слабость зеркальна: единственный колбэк, который никогда не уступает — длинный синхронный CPU-цикл или потоп микрозадач — замораживает все остальные соединения, потому что цикл не может двинуться дальше, пока тот колбэк кооперативно не вернётся. Это фундамент для понимания того, что блокирует цикл в продакшене." },
  ]}
/>

<Recap lang="ru">
Event loop — это строгая упорядоченная машина, а не размытый ящик «выполнит асинхронщину позже». Один поток крутит фиксированные фазы — timers, poll, check, close — и внутри каждой фазы выполняет очереди колбэков по одному до конца. Poll — центр тяжести: он выполняет готовые I/O-колбэки и, простаивая, блокируется на epoll/kqueue. Поперёк фаз идут две более приоритетные очереди микрозадач, сначала process.nextTick, затем реакции промисов, и определяющее правило в том, что цикл осушает микрозадачи до пустоты после каждого колбэка прежде, чем продвинуться — именно поэтому разрешённый промис обгоняет setTimeout(0) и почему setImmediate (фаза check) и setTimeout(0) (фаза timers) не взаимозаменяемы. Поскольку колбэки выполняются до конца без вытеснения, конкурентность кооперативная: запросы чередуются только там, где уступают, давая безопасность без локов внутри тика, но делая цикл заложником любого колбэка, который отказывается уступать. Этот сценарий заложника — что именно блокирует цикл и как это увидеть — следующий урок. Теперь, когда встретишь неожиданный порядок между setTimeout, setImmediate и разрешённым промисом, у тебя есть карта фаз, чтобы рассуждать, а не гадать.
</Recap>
