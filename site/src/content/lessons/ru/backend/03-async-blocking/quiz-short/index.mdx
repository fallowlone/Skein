---
concepts:
- blocking-io
- event-loop-schedule
- event-loop-blocking
- libuv-threadpool
- bounded-concurrency
- tail-latency
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://nodejs.org/learn/asynchronous-work/event-loop-timers-and-nexttick
- https://nodejs.org/learn/asynchronous-work/dont-block-the-event-loop
- https://nodejs.org/learn/modules/backpressuring-in-streams
status: ready
summary: "Промпты на свободное воспроизведение по всему юниту async/blocking. Сначала ответь по памяти, затем открой модельный ответ и сравни."
title: 'Async vs blocking: свободное воспроизведение'
track: backend
unit: 03-async-blocking
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. На каждый промпт скажи или напиши полный ответ по памяти прежде, чем открыть модельный — усилие припоминания и превращает юнит из конспекта в инстинкт.
</Hook>

<Goal lang="ru">
Восстанови хребет юнита, не подглядывая: две модели I/O, фазы loop и правило microtasks, что его блокирует и как это увидеть, куда выносить CPU-работу, как ограничить конкурентность и почему хвост взрывается у насыщения.
</Goal>

<RetrievalDrawer
  client:load
  id="03-async-blocking-short-retrieval"
  lang="ru"
  questions={[
    { q: "Сравни блокирующий thread-per-connection с неблокирующей моделью event loop и объясни, почему event loop выигрывает на 50 000 простаивающих соединений.", a: "В блокирующей модели поток вызывает read(), и ОС приостанавливает его, пока не придут байты, поэтому каждому одновременному соединению нужен свой поток — а каждый поток резервирует примерно 1–2 МБ стека плюс слот планировщика. Пятьдесят тысяч соединений поэтому стоят десятки ГБ стеков и шторм переключений контекста, даже когда соединения простаивают, потому что припаркованные потоки всё равно держат свои ресурсы. Неблокирующая модель ставит сокеты в неблокирующий режим и регистрирует их через средство ядра (epoll на Linux, kqueue на BSD/macOS); один поток спрашивает «какие дескрипторы готовы сейчас?», и ядро возвращает только их, примерно за O(1) независимо от числа отслеживаемых. Так что простаивающие соединения почти ничего не стоят, ведь loop не посещает их, пока у них нет данных — именно поэтому один поток event loop держит 50k простаивающих keep-alive комфортно, а thread-per-connection падает по памяти. Цена — код в форме колбэков или await вместо линейной логики." },
    { q: "Опиши фазы event loop и правило дренажа microtasks и используй их, чтобы упорядочить setTimeout(0), setImmediate и разрешённый промис внутри I/O-колбэка.", a: "Один поток циклически проходит фиксированные фазы: timers (истёкшие setTimeout/setInterval), poll (сердце — выполняет готовые I/O-колбэки и, простаивая, блокируется на epoll/kqueue), check (setImmediate) и close (колбэки события close). Внутри фазы колбэки выполняются по одному до завершения без вытеснения. Поперёк фаз идут две microtask-очереди более высокого приоритета — process.nextTick первой, затем promise-реакции — и определяющее правило: после каждого колбэка loop дренирует эти очереди до пустоты прежде, чем запустить следующий колбэк или сменить фазу. Поэтому внутри I/O-колбэка первым выполняется разрешённый Promise.then (microtask, дренируется сразу), затем setImmediate (фаза check, сразу после poll), затем setTimeout(0) (фаза timers, на следующем тике). Опасность симметрична: самопланирующийся microtask дренируется до пустоты перед любым I/O-колбэком, поэтому он морит голодом фазу poll так же, как синхронный цикл." },
    { q: "Почему один синхронный участок замораживает весь сервер и какой сигнал лучше всего его ловит?", a: "Конкурентность event loop кооперативна: каждый колбэк выполняется до завершения без вытеснения и уступает управление лишь завершившись или дойдя до await. Поэтому синхронный участок, занимающий реальное время — fs.readFileSync (~1200 мс), JSON.parse тела в несколько МБ (~800 мс), bcrypt.hashSync (~200–400 мс) или regex с катастрофическим бэктрекингом (секунды, под контролем атакующего) — мешает loop дойти до poll, так что ни один другой I/O-колбэк или таймер не выполняется весь этот срок. Это head-of-line blocking всего процесса: каждый одновременный запрос платит полную заморозку, из-за чего медленная строка тремя route дальше таймаутит двухстрочный health-check. Лучший сигнал — event-loop lag (delay): запланируй таймер и измерь, насколько поздно он сработал, через perf_hooks.monitorEventLoopDelay() как гистограмму p50/p99, с алертом около 100 мс. Он бьёт CPU, потому что высокий CPU может быть здоровой хорошо нарезанной работой, которая всё равно уступает, тогда как lag измеряет разрыв между тем, когда колбэк был должен, и когда loop до него добрался — ровно ту задержку, что чувствуют пользователи." },
    { q: "Какие два пула потоков стоят за loop, что выполняет каждый и сколько стоит worker thread?", a: "Пул потоков libuv (по умолчанию 4 потока, до 1024 через UV_THREADPOOL_SIZE) выполняет нативные операции без асинхронного примитива ОС — файловые вызовы, dns.lookup, async crypto и zlib — и никогда твой JavaScript; именно поэтому async bcrypt не замораживает loop. Worker threads совсем иное: отдельные V8-изоляты, каждый со своим loop, дающие настоящий многоядерный параллелизм для CPU-bound JavaScript вроде ресайза картинок или большого парсинга. Классическая ловушка — расширять пул libuv ради фикса JS CPU-узкого места: не выйдет, потому что пул не выполняет JS. Главная цена worker thread — передача данных: postMessage делает глубокую копию через structured clone (копия буфера в несколько МБ была ~268 мс против ~29 мс при передаче), поэтому используй transferables (владение переходит, без копии) или SharedArrayBuffer (общая память с Atomics), плюс переиспользуемый worker-пул (примерно один на ядро), а не worker'ы на каждый запрос, которые платят десятки мс старта. Выноси, лишь когда CPU-работа затмевает стоимость передачи сообщений; иначе разбивай на чанки на loop через setImmediate или масштабируйся горизонтально, когда CPU — потолок всего сервиса." },
    { q: "Объясни backpressure и ограниченную конкурентность и как выбирать лимит конкурентности k.", a: "Оба борются с одной болезнью — производить быстрее, чем потребляешь. Backpressure — обратная связь от медленного потребителя быстрому производителю: writable.write(chunk) возвращает false, как только внутренний буфер превышает highWaterMark (16 КБ для байтовых потоков, 16 объектов в object mode), твой сигнал прекратить запись до события drain; pipe() и pipeline() проводят это рукопожатие автоматически, так что файл 50 ГБ течёт через пару сотен КБ живого буфера, а ручные циклы write, игнорирующие булево, ловят OOM. Заметь: mark — лишь сигнальный порог, не жёсткий cap — одна огромная запись всё равно буферизуется целиком. Ограниченная конкурентность работает с N независимыми async-задачами: вместо await Promise.all(huge.map(fn)), который стартует всё сразу и ловит OOM, заливая downstream, ограничь не более чем k в полёте лимитером (p-limit) или паттерном worker-пул/for-await, тянущим новую работу по мере освобождения. k задаётся тем, что downstream способен принять — пул соединений на 20, лимит API, IOPS диска — а не числом элементов и не бесконечностью, потому что за этой точкой лишняя работа в полёте добавляет лишь очередь, память и ошибки." },
    { q: "Почему задержка взрывается у насыщения, почему смотреть на хвост, а не на среднее, и что значит «один loop — одно ядро»?", a: "Сервер — это система массового обслуживания, и время ожидания масштабируется примерно как 1/(1−ρ): около 2× при ρ=0.5, 3.3× при 0.7, 5× при 0.8, 20× при 0.95. Так что он поглощает нагрузку незаметно до колена около 70–80% утилизации, а потом срывается с обрыва — вот почему CPU может идти с 75% до 82%, пока p99 прыгает с 80 мс до 1400 мс: ты перешёл колено. Закон Литтла (L = λW) связывает этот взрыв задержки со взрывом одновременной занятости, тот же спираль, что у неограниченного fan-out. Среднее смешивает медленные запросы с быстрыми и прячет это, поэтому смотришь на хвост — p95/p99/p99.9 — где насыщение, GC-паузы и медленные зависимости всплывают первыми, а fan-out усиливает: плохой хвост 1-из-100 почти гарантирует попадание на странице из 100 вызовов. «Один loop — одно ядро» значит, что единственный Node event loop выполняет одно ядро JavaScript — масштабируется по конкурентному I/O, но не даёт параллелизма CPU — поэтому event-loop utilization (ELU) у 1.0 говорит, что loop — узкое место, лечится лишь меньшей работой на запрос, выносом CPU или добавлением loops. Работай с запасом; планирование ёмкости — это на самом деле планирование хвостовой задержки." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти — у тебя хребет юнита: модель I/O решает, как тратить ожидание, фазы loop плюс дренаж microtasks решают порядок, единственный синхронный участок замораживает всех (ловится event-loop lag, а не CPU), пул libuv выполняет нативный I/O, а CPU-bound JS требует worker thread, который ты платишь кормить данными, backpressure и лимит конкурентности подгоняют скорость производства к скорости потребления, а у насыщения хвост взрывается нелинейно — поэтому смотри p99 и ELU и работай с запасом, ведь один loop — одно ядро.
</Recap>
