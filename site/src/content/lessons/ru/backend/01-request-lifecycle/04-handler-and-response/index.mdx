---
concepts:
- handler
- serialization
- json-stringify
- status-codes
- content-length
- keep-alive
- response-buffering
deepensInto:
- backend/01-request-lifecycle/05-streaming-and-backpressure
estMin: 13
lang: ru
lessonType: topic
level: middle
order: 4
prereqs:
- backend/01-request-lifecycle/03-routing-and-middleware
slug: 04-handler-and-response
sources:
- https://nodejs.org/api/http.html
- https://www.rfc-editor.org/rfc/rfc9110
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Connection
spiral:
- latency
status: ready
summary: Обработчик выполняет логику, затем результат сериализуется, обрамляется статусом
  и заголовками и записывается в сокет. Стоимость сериализации и переиспользование
  keep-alive решают, во сколько обходится этот финальный отрезок.
title: 'Обработчик и ответ: от бизнес-логики до байтов на проводе'
track: backend
unit: 01-request-lifecycle
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import serializerTradeoff from "./infographics/serializer-tradeoff.svg?raw";

<Hook>
Эндпоинт, возвращающий список заказов, становится на 40% медленнее после запуска продукта. Запрос не изменился и по-прежнему отвечает за 8 мс. Замедление целиком в одной строке, которую никто не профилирует: превращение результата в JSON. Список вырос с 50 строк до 5000, и `JSON.stringify` теперь — самое дорогое, что делает запрос.
</Hook>

<Crux>После обработчика результат должен стать байтами — стоимость сериализации и переиспользование соединения решают цену последнего отрезка.</Crux>

<Explanation>

## Обработчик в основном ждёт

Ваш обработчик выполняет бизнес-логику, но для типичного запроса большая часть его реального времени — **не** вычисления, а ожидание запроса к БД, обращения к кешу или другого сервиса. Именно поэтому важен асинхронный I/O (следующий юнит), и поэтому «обработчик за 3 мс» обычно означает 3 мс CPU, обёрнутые вокруг десятков миллисекунд ожидания I/O. Задача обработчика — собрать данные и произвести объект-результат; сокета он ещё не касается.

## Сериализация — это реальная работа

Вот что большинство инженеров упускают, пока не откроют профайлер: когда обработчик вернул значение, работа ещё не закончена. Превращение объекта-результата в байты — это CPU, которое запрос платит всегда:

- **JSON** — `JSON.stringify` синхронен и масштабируется с размером объекта. На загруженном сервере с event-loop сериализация большого массива блокирует все остальные запросы на это время. Полезная нагрузка в 5000 строк может стоить несколько миллисекунд чистого CPU.
- **Схемные сериализаторы** (`fast-json-stringify` у Fastify, Protobuf, MessagePack) предкомпилируют форму и работают намного быстрее общего stringify, часто в 2–4 раза — поэтому высоконагруженные API объявляют схемы ответов.
- **Рендеринг HTML/шаблонов** обладает тем же свойством: это CPU, и оно синхронно, если фреймворк не стримит его.

Стоимость невидима, пока объект не вырос. Пагинация — не только UX-выбор; это контроль стоимости сериализации.

<Infographic
  svg={serializerTradeoff}
  label="Двустороннее сравнение общего JSON.stringify и схемного сериализатора: общий stringify синхронен, растёт с размером нагрузки, блокирует event loop и работает на базовой скорости 1x; схемный сериализатор предкомпилирует форму ответа, работает в 2–4 раза быстрее, но требует объявленной схемы."
  caption="Общий JSON.stringify — дефолт, но синхронен и O(размера нагрузки); схемный сериализатор меняет схему заранее на вывод в 2–4 раза быстрее на горячих эндпоинтах."
  hue="var(--d-infra)"
/>

## Статус, заголовки и обрамление

Прежде чем тело уйдёт, ответу нужны **строка статуса** и **заголовки**. Важны два решения по обрамлению:

- **`Content-Length`** — сервер знает полный размер тела заранее, отправляет его, и клиент точно знает, когда ответ закончился. Требует сначала забуферизовать всё тело.
- **`Transfer-Encoding: chunked`** — сервер стримит, не зная общий размер заранее (тема следующего урока).

Код статуса — часть контракта, а не украшение: `200` против `201` (created), `204` (нет тела), `4xx` (клиент должен изменить запрос), `5xx` (вина сервера, идемпотентный вызов безопасно повторить). Возврат `200` с телом-ошибкой внутри — частый антипаттерн, ломающий логику повторов и мониторинга у каждого клиента.

<div data-lesson-visual class="overflow-x-auto my-6">

| Решение | Стоимость | Когда кусается |
|---|---|---|
| `JSON.stringify` (общий) | O(размера), синхронно, блокирует loop | Большие массивы после роста данных |
| Схемный сериализатор | в 2–4 раза быстрее, предкомпилирован | Окупается на горячих, высокообъёмных эндпоинтах |
| Буфер, затем отправка (Content-Length) | Держит всё тело в памяти | Большие ответы дают всплеск памяти |
| Неверный статус (200 на ошибке) | Ломает повторы и алерты клиента | Тихо до самого инцидента |

</div>

## Keep-alive: платим за рукопожатие один раз

Открыть TCP- (и TLS-) соединение дорого — рукопожатие стоит одного или более round trip. **HTTP keep-alive** (`Connection: keep-alive`, по умолчанию в HTTP/1.1) переиспользует одно соединение для многих запросов, поэтому рукопожатие амортизируется. После того как ответ слился, соединение возвращается в простаивающий пул с обеих сторон, пока keep-alive таймаут его не закроет.

Поэтому переиспользование соединения — забота жизненного цикла: клиент, открывающий свежее соединение на каждый запрос, платит рукопожатие каждый раз, а сервер со слишком коротким keep-alive таймаутом форсирует переподключения под стабильной нагрузкой. Ответ не «готов», когда обработчик вернул значение — он готов, когда байты слиты, а соединение корректно удержано или закрыто.

<Quiz
  id="handler-resp-q1"
  lessonSlug="04-handler-and-response"
  lang="ru"
  question="Эндпоинт замедляется на 40% после роста результата с 50 до 5000 строк, но время запроса к БД не изменилось. Наиболее вероятная причина?"
  choices={[
    { label: "Таблица маршрутов выросла", misconception: "Число маршрутов не меняется с размером результата, а маршрутизация — микросекунды в любом случае. Здесь речь о производстве ответа, а не о поиске обработчика." },
    { label: "Сериализация большего результата (например, JSON.stringify) теперь стоит заметного синхронного CPU", correct: true },
    { label: "Переполняется очередь accept", misconception: "Переполнение очереди accept отклоняет соединения; оно не делает успешный ответ медленнее пропорционально его размеру." },
    { label: "Отключился keep-alive", misconception: "Keep-alive влияет на стоимость установки соединения, а не на стоимость сериализации большей нагрузки на строку." },
  ]}
/>

<Quiz
  id="handler-resp-q2"
  lessonSlug="04-handler-and-response"
  lang="ru"
  question="Почему возврат HTTP 200 с объектом-ошибкой внутри тела считается антипаттерном?"
  choices={[
    { label: "Ответы 200 не могут содержать JSON", misconception: "Ответы 200 регулярно содержат JSON. Проблема семантическая, а не в content type." },
    { label: "Клиенты, прокси, повторы и алерты завязаны на код статуса, поэтому 200 прячет сбои от каждого слоя, который их мониторит", correct: true },
    { label: "Он использует больше трафика, чем 500", misconception: "Стоимость в байтах та же. Вред в том, что статус больше не отражает успех или провал." },
    { label: "200 заставляет соединение закрыться", misconception: "Код статуса не управляет keep-alive. Проблема в том, что успешный статус искажает ошибку." },
  ]}
/>

<MetaphorComplete
  id="handler-resp-metaphor1"
  lessonSlug="04-handler-and-response"
  lang="ru"
  setup="Заполните пропуск: HTTP keep-alive позволяет многим запросам переиспользовать одно соединение, чтобы дорогое TCP/TLS _______ оплачивалось один раз, а не на каждый запрос."
  accepted={["рукопожатие", "хендшейк", "handshake", "установка", "установка соединения"]}
  canonical="рукопожатие"
  explanation="Открытие соединения стоит round trip-ов на TCP- (и TLS-) рукопожатие. Keep-alive амортизирует эту стоимость по многим запросам на одном соединении — поэтому клиент, открывающий свежее соединение на каждый запрос, намного медленнее под нагрузкой."
/>

</Explanation>

<FlowDiagram
  label="От объекта-результата обработчика к байтам, слитым в сокет"
  hue="var(--d-backend)"
  perRow={4}
  nodes={[
    { id: "handler", label: "Обработчик", sub: "объект-результат" },
    { id: "ser", label: "Сериализация", sub: "JSON · O(размер)" },
    { id: "frame", label: "Каркас", sub: "статус + длина" },
    { id: "drain", label: "Drain", sub: "keep-alive" },
  ]}
  edges={[
    { from: "handler", to: "ser" },
    { from: "ser", to: "frame" },
    { from: "frame", to: "drain" },
  ]}
  caption="Обработчик создаёт объект-результат (в основном ожидание I/O); сериализация — синхронное CPU, растущее с размером нагрузки; обрамление добавляет статус и Content-Length; drain пишет в keep-alive сокет. Ответ готов лишь когда байты слиты."
/>

<KeyTakeaway>Обработчик собирает данные (в основном ожидание I/O), затем результат сериализуется — реальное синхронное CPU, масштабирующееся с размером нагрузки, поэтому большие ответы и общий JSON.stringify бьют, а схемные сериализаторы помогают. Ответ обрамляется статусом и Content-Length или chunked, а keep-alive переиспользует соединение, чтобы рукопожатие платилось один раз, а не на каждый запрос.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="handler-resp-retrieval"
  lang="ru"
  questions={[
    { q: "Почему большая часть реального времени типичного обработчика — не CPU, и почему стоимость сериализации всё равно важна?", a: "Типичный обработчик большую часть времени ждёт I/O — запрос к БД, обращение к кешу, другой сервис — а не вычисляет. Поэтому «обработчик за 3 мс» обычно означает 3 мс CPU, обёрнутые вокруг десятков миллисекунд ожидания I/O, и поэтому важен асинхронный I/O. Сериализация, напротив, — это CPU, которое запрос платит всегда: превращение объекта-результата в байты (JSON.stringify) синхронно и масштабируется с размером объекта, поэтому большая нагрузка блокирует event loop на это время. Пагинация и схемные сериализаторы (fast-json-stringify, Protobuf) — контроль стоимости этой стадии." },
    { q: "Что требуют от сервера Content-Length и chunked transfer encoding, и как код статуса работает как контракт?", a: "Content-Length требует от сервера знать полный размер тела заранее, то есть забуферизовать всё тело перед отправкой; клиент тогда точно знает, когда ответ закончился. Chunked transfer encoding позволяет серверу стримить, не зная общий размер заранее. Код статуса — часть контракта API, а не украшение: 201 значит created, 204 — нет тела, 4xx — клиент должен изменить запрос, 5xx — вина сервера, безопасная для повтора идемпотентного вызова. Возврат 200 с ошибкой внутри ломает логику повторов и мониторинга клиента." },
    { q: "Что оптимизирует HTTP keep-alive и почему переиспользование соединения — забота жизненного цикла?", a: "Keep-alive переиспользует одно TCP/TLS-соединение для многих запросов, чтобы рукопожатие — стоящее одного или более round trip — амортизировалось, а не платилось на каждый запрос. После того как ответ слился, соединение возвращается в простаивающий пул с обеих сторон, пока keep-alive таймаут его не закроет. Это забота жизненного цикла, потому что ответ не закончен, когда обработчик вернул значение; он закончен, когда байты слиты, а соединение корректно удержано или закрыто. Клиент, открывающий свежее соединение на каждый запрос, или сервер со слишком коротким keep-alive таймаутом платит рукопожатие многократно под нагрузкой." },
  ]}
/>

<Recap lang="ru">
В центре луковицы выполняется обработчик, но большая часть его времени — ожидание I/O, а не вычисления; он производит объект-результат, не касаясь сокета. Превращение этого объекта в байты — стадия сериализации: синхронное CPU, масштабирующееся с размером нагрузки, где общий JSON.stringify на больших результатах блокирует event loop, а схемные сериализаторы или пагинация срезают стоимость. Затем ответ обрамляется строкой статуса и заголовками, используя Content-Length (сначала буфер) или chunked-кодирование, и код статуса несёт реальный смысл контракта, на который полагаются клиенты, прокси и алерты. Наконец, keep-alive амортизирует дорогое TCP/TLS-рукопожатие по многим запросам на одном соединении. Теперь, когда увидишь быстрый обработчик, но медленный эндпоинт, смотри на стоимость сериализации и настройки keep-alive, прежде чем делать вывод, что код неверен. Ответ по-настоящему готов лишь когда байты слиты — а когда клиент читает медленнее, чем сервер пишет, это сливание становится трудной задачей следующего урока: backpressure.
</Recap>
