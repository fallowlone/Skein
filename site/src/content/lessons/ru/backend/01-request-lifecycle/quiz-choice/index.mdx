---
concepts:
- accept-queue
- middleware-ordering
- serialization-cost
- backpressure
- timeout-budget
- tail-amplification
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://nodejs.org/api/http.html
- https://nginx.org/en/docs/http/ngx_http_core_module.html
- https://www.barroso.org/publications/TheTailAtScale.pdf
status: ready
summary: "Тест с выбором на синтез по всему жизненному циклу запроса — accept-очереди, порядок middleware как граница безопасности, стоимость сериализации, backpressure и амплификация хвоста при fan-out."
title: 'Жизненный цикл запроса: тест с выбором ответа'
track: backend
unit: 01-request-lifecycle
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всех семи остановок. Каждый отражает реальный инцидент — не стадию для называния, а симптом, который надо локализовать до одной остановки, плюс механизм, объясняющий его.
</Hook>

<Goal lang="ru">
Убедись, что по симптому прыгаешь к нужной остановке: связываешь лимиты accept-очереди, порядок middleware, стоимость сериализации, backpressure и амплификацию хвоста в один рефлекс принятия решения.
</Goal>

<Quiz
  id="01-request-lifecycle-choice-1"
  lessonSlug="01-request-lifecycle"
  lang="ru"
  question="Под всплеском трафика клиенты получают connection timeout и reset, но логи приложения полностью пусты — ни 500, ни трейсов обработчика. Какая остановка и почему логи пусты?"
  choices={[
    { label: "Accept. Accept-очередь ядра — ограниченная min(listen backlog, somaxconn), часто всё ещё 128 — переполнилась, потому что процесс перестал достаточно быстро вызывать accept(), и ядро отбросило завершённые соединения до того, как они дошли до твоего кода. Код не выполнялся — логировать нечего.", correct: true },
    { label: "Handle. Необработанное исключение в обработчике уронило запрос.", misconception: "Исключение выполняется внутри твоего кода, поэтому попадает в логи и обычно возвращает 500. Пустые логи означают, что запрос вообще не дошёл до приложения — отбрасывание выше, в accept-очереди ядра." },
    { label: "Serialize. JSON.stringify на большом ответе заблокировал event loop.", misconception: "Сериализация происходит сильно позже accept(), на запросах, которые дошли до кода. Если соединения отвергаются до запуска обработчика, сериализация ни при чём, и для прошедших запросов логи бы остались." },
    { label: "Drain. Backpressure проигнорирован, и ответ забуферизовался в памяти.", misconception: "Игнорирование backpressure даёт OOM на активно пишущихся ответах — это краш с симптомами роста кучи, а не тихий отказ соединений с пустыми логами во время всплеска." },
  ]}
/>

<Quiz
  id="01-request-lifecycle-choice-2"
  lessonSlug="01-request-lifecycle"
  lang="ru"
  question="Security-ревью находит, что один admin-эндпоинт обслуживает неаутентифицированные запросы, хотя auth-middleware реализован, протестирован и зарегистрирован. Наиболее вероятная причина?"
  choices={[
    { label: "Маршрут зарегистрирован до auth-middleware, поэтому обработчик закоротил запрос на ответ ещё до того, как auth получил управление. Порядок middleware — граница безопасности: защитные слои должны регистрироваться перед маршрутами, которые они охраняют.", correct: true },
    { label: "Radix-trie роутер сматчил неверный паттерн пути.", misconception: "Попадание в правильный admin-обработчик означает, что роутинг сработал. Сбой в том, что запрос не был аутентифицирован до запуска обработчика — это баг порядка, а не матчинга." },
    { label: "Формат токена изменился, и middleware больше не может его распарсить.", misconception: "Проблема парсинга/формата отвергала бы валидных пользователей (запрет доступа), а не пускала неаутентифицированных. Если пускает всех — auth просто не выполнялся, это вопрос порядка." },
    { label: "Роутинг O(n), поэтому запрос таймаутнулся до запуска auth.", misconception: "Роутинг — микросекунды независимо от числа маршрутов, и таймаут не пропустил бы auth молча, а провалил бы запрос. Обработчик просто оказался раньше auth-слоя в порядке регистрации." },
  ]}
/>

<Quiz
  id="01-request-lifecycle-choice-3"
  lessonSlug="01-request-lifecycle"
  lang="ru"
  question="Эндпоинт стал на 40% медленнее после роста результата с 50 до 5000 строк, но время запроса к БД не изменилось — те же 8 мс. Куда ушло время и какой первый фикс?"
  choices={[
    { label: "Serialize. JSON.stringify синхронен и масштабируется с размером объекта, поэтому payload в 100× больше теперь стоит несколько мс CPU и блокирует event loop. Первый фикс: пагинация, и рассмотри прекомпилированный schema-сериализатор (в 2–4× быстрее) на горячем пути.", correct: true },
    { label: "Route. Бо́льший ответ замедлил матчинг маршрута.", misconception: "Стоимость матчинга не зависит от размера ответа (и это всё равно микросекунды). Размер результата влияет на формирование ответа, а не на поиск обработчика." },
    { label: "Accept. Больший payload переполнил accept-очередь.", misconception: "Accept-очередь ограничивает ожидающие соединения, а не размер ответа. Она отвергает соединения под нагрузкой; она не делает успешный ответ медленнее пропорционально числу строк." },
    { label: "Drain. Keep-alive был отключён, добавив стоимость handshake на запрос.", misconception: "Keep-alive влияет на стоимость установки соединения, которая не масштабируется с числом строк в одном ответе. Замедление на 40%, отслеживающее размер payload, указывает на сериализацию." },
  ]}
/>

<Quiz
  id="01-request-lifecycle-choice-4"
  lessonSlug="01-request-lifecycle"
  lang="ru"
  question="Стриминговый экспорт даёт OOM только когда клиент скачивает по медленному каналу. Код вызывает res.write() в цикле и игнорирует возвращаемое значение. Каков точный механизм?"
  choices={[
    { label: "res.write() возвращает false, когда буфер writable-потока пересёк high-water mark (по умолчанию 16 КБ) — это сигнал flow control, что потребитель отстаёт. Игнорируя его и продолжая писать, ты складываешь непрочитанные байты в кучу процесса — единственный слой без flow control — пока ядро не убьёт по OOM.", correct: true },
    { label: "false означает, что запись провалилась, поэтому цикл ретраит и дублирует данные, пока память не заполнится.", misconception: "false не означает провал — данные приняты в буфер. Это сигнал backpressure «притормози», а не «ретрай». Рост — это непрочитанные данные в userland-буфере, а не дублированные записи." },
    { label: "Garbage collector не может работать, пока соединение открыто, поэтому куча растёт.", misconception: "GC работает нормально; он просто не может освободить буферы, на которые ещё ссылается очередь ожидающих записей. Эти байты — живые данные, которые потребитель не прочитал, а не мусор." },
    { label: "Медленные каналы портят TCP-контрольные суммы, вызывая ретрансмиты, которые съедают память.", misconception: "Ретрансмиты нормальны и ограничены TCP-стеком; они не вызывают неограниченного роста кучи. Рост на уровне приложения: скорость производства оторвана от скорости чтения клиента." },
  ]}
/>

<Quiz
  id="01-request-lifecycle-choice-5"
  lessonSlug="01-request-lifecycle"
  lang="ru"
  question="Страница продукта делает fan-out на 100 независимых сервисов параллельно и ждёт всех. У каждого p99 = 10 мс (1% шанс, что вызов превысит). Примерно какая доля загрузок страницы превышает 10 мс хотя бы на одном вызове и какой устойчивый фикс?"
  choices={[
    { label: "Около 63%: ты ждёшь всех, поэтому медленный, если медленный хотя бы один — 1 − 0.99^100 ≈ 0.63. Устойчивый фикс атакует сам хвост: hedge медленных запросов на ~p95 ко второй реплике, что схлопывает p99.9 ценой лишь нескольких процентов нагрузки.", correct: true },
    { label: "Около 1% — как у одного сервиса, ведь каждый вызов независим.", misconception: "1% — это частота на вызов. При 100 независимых вызовах и join «ждать всех» шанс, что ни один не медленный — 0.99^100, поэтому медленная доля компаундится до ≈63%. Независимость не защищает, когда ждёшь всех." },
    { label: "Около 10% — вероятность хвоста растёт линейно с числом fan-out.", misconception: "Амплификация хвоста не линейна. Компаундящееся 1 − (1 − p)^N растёт куда быстрее линейной оценки; при p=1%, N=100 это ≈63%, а не 10%." },
    { label: "0% — параллельные вызовы перекрываются, поэтому не могут все быть медленными одновременно.", misconception: "Ты медленный, если медленный любой один, а не только если все. Ожидание всех означает, что один медленный из 100 задаёт задержку всей страницы — именно это и амплифицирует хвост." },
  ]}
/>

<Quiz
  id="01-request-lifecycle-choice-6"
  lessonSlug="01-request-lifecycle"
  lang="ru"
  question="У запроса SLA 1 с. Сервис A (client timeout 1 с) вызывает сервис B (client timeout 1 с). Когда B зависает, A продолжает работать всю секунду после того, как клиент уже сдался. Почему и что это чинит?"
  choices={[
    { label: "Независимые per-hop таймауты не композируются: ничто не сообщает A, сколько бюджета осталось, поэтому A тратит свою полную секунду на работу, которую никто не ждёт. Фикс: пробрасывать deadline — точка входа выделяет общий бюджет, и каждый hop использует min(local default, remaining); модель, которую стандартизирует gRPC.", correct: true },
    { label: "Таймаут A просто слишком длинный; снижение до 500 мс чинит композицию.", misconception: "Длина — не корень проблемы, композиция. Даже хорошо подобранные локальные значения суммируются за SLA без общего пробрасываемого бюджета; фикс — deadline, а не меньшая константа." },
    { label: "Клиенты БД и RPC не могут иметь таймауты, поэтому A ждёт вечно.", misconception: "Клиенты поддерживают таймауты — здесь и A, и B ставят 1 с. Изъян в том, что изолированные таймауты не сворачиваются в один deadline, несомый вниз по цепочке вызовов." },
    { label: "Ядро игнорирует таймауты уровня приложения под нагрузкой.", misconception: "Таймауты приложения соблюдаются. Сбой архитектурный: не пробрасывается deadline с остатком бюджета, поэтому внутренние hop'ы переживают терпение вызывающего." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — один рефлекс: по симптому прыгай к остановке, которой он принадлежит. Пустые логи под всплеском — accept-очередь (somaxconn) отбросила соединения до запуска кода; неаутентифицированный эндпоинт — порядок middleware, а не логика auth; замедление, отслеживающее размер payload — сериализация; OOM под медленными клиентами — проигнорированный backpressure; медленная страница при здоровых сервисах — амплификация хвоста при fan-out, чинится пробрасываемыми deadline и hedging. Задержка — сумма по всем семи остановкам плюс очереди, поэтому карта превращает слепую отладку в один прыжок.
</Recap>
