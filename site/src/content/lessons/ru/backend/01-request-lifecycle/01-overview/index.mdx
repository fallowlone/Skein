---
concepts:
- request-lifecycle
- reverse-proxy
- middleware
- handler
- response-pipeline
deepensInto:
- backend/01-request-lifecycle/02-accept-and-parse
estMin: 10
lang: ru
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://nginx.org/en/docs/http/ngx_http_core_module.html
- https://nodejs.org/api/http.html
- https://www.barroso.org/publications/TheTailAtScale.pdf
spiral:
- latency
status: ready
summary: Каждый HTTP-запрос проходит один и тот же путь — accept, парсинг, маршрутизация,
  middleware, обработка, сериализация, drain. Знание карты подсказывает, где прячутся
  задержка, сбой и уязвимость.
title: 'Путь запроса: семь остановок от сокета до ответа'
track: backend
unit: 01-request-lifecycle
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import latencyCumulative from "./infographics/latency-cumulative.svg?raw";

<Hook>
Пользователь нажимает «Сохранить». Через 180 мс он видит зелёную галочку. За эти 180 мс запрос прошёл через балансировщик, полежал в очереди ядра, был разобран байт за байтом, сопоставлен с таблицей маршрутов, прошёл стопку middleware, выполнил ваш обработчик, был сериализован в JSON и отправлен обратно потоком — пройдя семь разных стадий, у каждой свой режим отказа. Большинство инженеров назовут обработчик. Немногие назовут остальные шесть.
</Hook>

<Crux>HTTP-запрос делает одни и те же семь остановок — и задержка, сбой и уязвимость прячутся каждая на своей.</Crux>

<Explanation>

## Семь остановок

Уберите фреймворк — и любой серверный запрос имеет одну и ту же форму. Названия отличаются в Express, Spring, net/http Go и Rails, но стадии — нет:

1. **Accept** — ядро завершает TCP-рукопожатие и передаёт процессу установленный сокет.
2. **Парсинг** — байты из сокета превращаются в строку запроса, заголовки и тело.
3. **Маршрутизация** — метод и путь сопоставляются с одним обработчиком.
4. **Middleware** — сквозные слои выполняются по порядку: auth, логирование, парсинг тела, rate limiting.
5. **Обработка** — выполняется бизнес-логика, обычно в ожидании БД или другого сервиса.
6. **Сериализация** — результат становится байтами: JSON, HTML, поток.
7. **Drain** — ответ записывается в сокет, который может быть медленнее обработчика, и соединение удерживается (keep-alive) или закрывается.

Вместе эти семь остановок образуют цепочку: пропустишь или неверно настроишь любую — получишь сбой, который выглядит загадочно, пока не знаешь карту. Следующие несколько минут сделают эту карту рефлексом.

Перед остановкой 1 обычно стоит **reverse proxy** (обратный прокси-сервер) (nginx, Envoy, облачный балансировщик): он терминирует TLS, выбирает бэкенд и может буферизовать весь запрос. Этот прокси — отдельный жизненный цикл со своими семью остановками на другой стороне.

## Где прячется каждая проблема

Карта важна потому, что каждый класс ошибок живёт на конкретной остановке. Отладка вслепую — это проверка всех семи; отладка с картой — это прыжок к одной.

<div data-lesson-visual class="overflow-x-auto my-6">

| Симптом | Подозреваемая остановка |
|---|---|
| Соединения отклоняются под нагрузкой | Accept — переполнилась очередь accept в ядре |
| 431 / 400 на больших cookie | Парсинг — превышен буфер заголовков |
| Срабатывает не тот обработчик | Маршрутизация — пересекающиеся шаблоны путей |
| Обход auth на одном эндпоинте | Middleware — порядок: обработчик сработал до слоя auth |
| p50 быстрый, p99 ужасный | Обработка — медленная нижестоящая зависимость |
| Высокий CPU на больших ответах | Сериализация — `JSON.stringify` на огромном объекте |
| Память растёт на медленных клиентах | Drain — backpressure проигнорирован, ответ буферизован в RAM |

</div>

## Задержка суммируется, а не локализуется

Полное время запроса — это **сумма** всех остановок плюс время ожидания в очередях между ними. Обработчик, выполняющийся за 5 мс, всё равно может дать ответ за 200 мс, если очередь accept была глубокой, тело буферизовал прокси, а клиент читал ответ медленно. Поэтому «код быстрый» и «эндпоинт медленный» истинны одновременно — они описывают разные остановки.

<Infographic
  svg={latencyCumulative}
  label="Горизонтальная столбчатая диаграмма тайминга одного запроса: обработчик 5 мс, очереди и шесть других остановок добавляют 195 мс, полное время ответа для пользователя 200 мс."
  caption="Обработчик — лишь малая доля ответа; задержка это сумма по всем семи остановкам плюс ожидание в очередях, поэтому быстрый код всё равно может ощущаться медленным."
  hue="var(--d-backend)"
/>

Senior-навык — не запоминание семи названий. Это рефлекс: *по этому симптому — какая остановка?* Когда увидишь всплеск p99 или лавину отклонённых соединений, первый вопрос должен быть «на какой остановке?», а не «что делает код?». Дальше юнит проходит каждую остановку в продакшн-деталях.

<Quiz
  id="lifecycle-overview-q1"
  lessonSlug="01-overview"
  lang="ru"
  question="У сервиса обработчик 3 мс, но под нагрузкой пользователи видят ответы по 250 мс. Какие остановки наиболее вероятны?"
  choices={[
    { label: "Только обработчик — измерение наверняка неверно", misconception: "Обработчик — лишь одна из семи остановок. Быстрый обработчик не делает ответ быстрым; ожидание в очередях и drain происходят вне его." },
    { label: "Accept (глубина очереди) и Drain (медленный клиент / backpressure) — они добавляют время вне обработчика", correct: true },
    { label: "Маршрутизация — сопоставление пути самое дорогое", misconception: "Сопоставление маршрута — это микросекунды даже на сотнях маршрутов. Оно почти никогда не доминирует в 250 мс." },
    { label: "Сериализация — превращение маленького объекта в JSON занимает 250 мс", misconception: "Сериализация маленького ответа — доли миллисекунды. Заметной её делают только очень большие объекты." },
  ]}
/>

<Quiz
  id="lifecycle-overview-q2"
  lessonSlug="01-overview"
  lang="ru"
  question="Почему reverse proxy перед сервером приложений важен для жизненного цикла запроса?"
  choices={[
    { label: "Он заменяет необходимость в семи остановках вашего приложения", misconception: "У прокси свой жизненный цикл. Ваше приложение всё равно выполняет accept, парсинг, маршрутизацию и т. д. — прокси стоит перед всем этим." },
    { label: "Он часто терминирует TLS и может буферизовать весь запрос целиком до того, как приложение увидит хоть байт", correct: true },
    { label: "Он выполняет вашу бизнес-логику, чтобы сервер приложений простаивал", misconception: "Reverse proxy пересылает запросы; он не выполняет ваши обработчики. Бизнес-логика остаётся в сервере приложений." },
    { label: "Он гарантирует, что обработчику никогда не придётся ждать БД", misconception: "Прокси никак не влияет на то, сколько обработчик ждёт БД — это полностью ниже прокси по течению." },
  ]}
/>

<DragOrder
  id="lifecycle-overview-drag1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставьте семь остановок запроса в порядке их прохождения:"
  items={[
    "Accept — ядро передаёт процессу установленный сокет",
    "Парсинг — байты становятся строкой запроса, заголовками, телом",
    "Маршрутизация — метод и путь сопоставлены с обработчиком",
    "Middleware — auth, логирование, парсинг тела выполняются по порядку",
    "Обработка — выполняется бизнес-логика, ждёт зависимости",
    "Сериализация — результат становится байтами",
    "Drain — ответ записан в сокет, соединение удержано или закрыто",
  ]}
/>

</Explanation>

<StackDiagram
  label="Семь остановок запроса сверху вниз и ошибка, прячущаяся на каждой"
  hue="var(--d-backend)"
  layers={[
    { label: "1. Accept", note: "переполнение очереди ядра → отказ" },
    { label: "2. Парсинг", note: "большие заголовки → 431/400" },
    { label: "3. Маршрутизация", note: "пересечение путей → не тот обработчик" },
    { label: "4. Middleware", note: "неверный порядок → обход auth" },
    { label: "5. Обработка", note: "медленная зависимость → скачок p99" },
    { label: "6. Сериализация", note: "большой объект → CPU на цикле" },
    { label: "7. Drain", note: "игнор backpressure → память" },
  ]}
  caption="Каждый запрос проходит одни и те же семь остановок по порядку; общая задержка — их сумма плюс ожидание в очередях, и каждый класс ошибок живёт ровно на одной остановке."
/>

<KeyTakeaway>Каждый запрос делает семь остановок: accept, парсинг, маршрутизация, middleware, обработка, сериализация, drain. Задержка — это сумма по всем плюс ожидание в очередях, поэтому быстрый обработчик может дать медленный ответ. Каждый класс ошибок живёт на одной остановке; карта превращает слепую отладку в один прыжок.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="lifecycle-overview-retrieval"
  lang="ru"
  questions={[
    { q: "Назовите семь остановок жизненного цикла запроса по порядку и по одной проблеме на каждой.", a: "1) Accept — ядро завершает рукопожатие; очередь accept может переполниться и отклонять соединения. 2) Парсинг — байты становятся запросом; слишком большие заголовки превышают лимит буфера (431/400). 3) Маршрутизация — путь сопоставлен с обработчиком; пересекающиеся шаблоны отправляют запрос не туда. 4) Middleware — сквозные слои по порядку; неверный порядок обходит auth. 5) Обработка — бизнес-логика; медленная зависимость доминирует в p99. 6) Сериализация — результат в байты; большие объекты делают JSON.stringify дорогим по CPU. 7) Drain — запись в сокет; медленный клиент плюс игнор backpressure буферизуют ответ в памяти." },
    { q: "Почему у сервиса может быть обработчик 3 мс, но время ответа 250 мс?", a: "Полное время ответа — это сумма всех семи остановок плюс ожидание в очередях между ними, а не только обработчик. Под нагрузкой запрос может ждать в очереди accept ядра ещё до парсинга, прокси может буферизовать тело, а клиент может читать ответ медленно, пока backpressure тормозит drain. Всё это вне обработчика, поэтому «код быстрый» и «эндпоинт медленный» верны одновременно." },
    { q: "Что добавляет reverse proxy перед семью остановками и почему у него свой жизненный цикл?", a: "Reverse proxy (nginx, Envoy, облачный балансировщик) обычно терминирует TLS, выбирает бэкенд и может буферизовать весь запрос или ответ. Он пересылает запросы, но не выполняет ваши обработчики. У него та же форма accept/парсинг/маршрутизация/пересылка/drain на своей стороне, поэтому один запрос пользователя проходит на самом деле через два жизненных цикла — прокси и сервера приложений — и задержка или сбой могут прятаться в любом из них." },
  ]}
/>

<Recap lang="ru">
Любой серверный HTTP-запрос проходит одни и те же семь остановок: accept (ядро передаёт сокет), парсинг (байты становятся запросом), маршрутизация (путь к обработчику), middleware (сквозные слои по порядку), обработка (бизнес-логика), сериализация (результат в байты) и drain (запись в сокет, keep-alive или закрытие). Reverse proxy спереди добавляет свою копию этого цикла. Полная задержка — сумма по остановкам плюс ожидание в очередях, поэтому быстрый обработчик всё равно может дать медленный эндпоинт. Каждый класс ошибок живёт на конкретной остановке — ценность карты в рефлексе прыжка к нужной. Теперь, когда встретишь сервис, который «медленный, но код в порядке», первый шаг — назвать остановку, а не читать обработчик. Следующий урок углубляется в первые две остановки: как ядро принимает соединение и как сырые байты становятся разобранным запросом.
</Recap>
