---
concepts:
- routing
- radix-trie
- middleware-chain
- ordering
- short-circuit
- cross-cutting-concerns
deepensInto:
- backend/01-request-lifecycle/04-handler-and-response
- backend/02-middleware-di/01-overview
estMin: 13
lang: ru
lessonType: topic
level: middle
order: 3
prereqs:
- backend/01-request-lifecycle/02-accept-and-parse
slug: 03-routing-and-middleware
sources:
- https://expressjs.com/en/guide/using-middleware.html
- https://github.com/julienschmidt/httprouter
- https://nginx.org/en/docs/http/ngx_http_core_module.html
spiral:
- latency
status: ready
summary: 'Маршрутизация связывает запрос с одним обработчиком; цепочка middleware
  решает, что выполняется вокруг него. Порядок — не косметика: он определяет, реально
  ли защищают auth, лимиты тела и сжатие.'
title: 'Маршрутизация и middleware: что выполняется и в каком порядке'
track: backend
unit: 01-request-lifecycle
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Security-ревью находит, что один админский эндпоинт принимает неаутентифицированные запросы. Auth-middleware существует, хорошо протестирован и применён — но зарегистрирован *после* этого маршрута. Обработчик сработал первым. Никто не написал баг; кто-то написал строки в неверном порядке.
</Hook>

<Crux>Маршрутизация выбирает обработчик; порядок middleware выбирает, что его защищает — неверный порядок молча отключает auth, лимиты и логи.</Crux>

<Explanation>

## Маршрутизация: сопоставление пути с обработчиком

Маршрутизация превращает `POST /users/42/orders` в один обработчик плюс извлечённые параметры (`userId=42`). Способ сопоставления определяет её стоимость:

- **Линейный перебор** — пробовать каждый маршрут по порядку. Просто, но O(n) по числу маршрутов; нормально для десятков.
- **Radix-trie** (httprouter, Echo, find-my-way у Fastify) — префиксное дерево по сегментам пути. O(длины пути), независимо от числа маршрутов, поэтому 10 и 10 000 маршрутов сопоставляются за одно время.

На практике маршрутизация — это микросекунды в любом случае; она почти не доминирует в задержке. Настоящие баги маршрутизации — это **корректность**: пересекающиеся шаблоны (`/users/:id` и `/users/me`), несовпадение по завершающему слешу и путаница методов (`GET`-маршрут перекрывает `POST` на том же пути).

## Middleware: цепочка вокруг обработчика

Middleware — функции, которые выполняются до и/или после обработчика, каждая может прочитать запрос, изменить его, прервать или передать управление дальше. Они обслуживают **сквозные задачи** (cross-cutting concerns) — то, что нужно каждому эндпоинту, но что ни один не должен реализовывать заново: аутентификация, логирование, парсинг тела, rate limiting, сжатие, обработка ошибок.

Модель — луковица. Запрос идёт *внутрь* через каждый слой к обработчику, затем ответ идёт *наружу* через те же слои в обратном порядке:

```
запрос  →  logging → auth → bodyParser → handler
ответ   ← logging ← auth ← bodyParser ← handler
```

Слой, вызывающий `next()`, передаёт управление внутрь. Слой, который отвечает *без* вызова `next()`, **прерывает цепочку** (short-circuit) — обработчик не выполняется. Именно так rate limiter возвращает 429, а auth-слой возвращает 401 до запуска бизнес-логики.

## Порядок — это граница безопасности

Поскольку каждый слой может прервать цепочку, *порядок* регистрации решает, что реально защищает обработчик:

<div data-lesson-visual class="overflow-x-auto my-6">

| Неверный порядок | Последствие |
|---|---|
| Маршрут зарегистрирован до auth-middleware | Обработчик выполняется без аутентификации |
| Парсер тела до лимита размера тела | Сервер буферизует загрузку 2 ГБ до отказа |
| Сжатие до auth | Сжатые ошибки утекают; CPU тратится на запросы, которые получат 401 |
| Обработчик ошибок зарегистрирован первым | Он ничего не ловит — ошибки проходят мимо него |
| Rate limiter после дорогой работы | Вы делаете работу, а потом отказываетесь её принять |

</div>

Правило, предотвращающее все пять: **дешёвые и защитные слои идут первыми** (лимиты размера, rate limit, auth), **дорогие и опциональные — последними** (парсинг тела, сжатие), а **обработчик ошибок — самым внешним**, чтобы охватывать всё.

## Куда реально уходит время

Middleware — ещё и место, где копится невидимая задержка. Каждый слой выполняется на каждом запросе. Парсер тела, разбирающий 1 МБ JSON, слой логирования с синхронным DNS-запросом, auth-слой, дёргающий удалённый сервис токенов без кеша — всё это добавляет миллисекунды к *каждому* запросу, а не только к медленным. Профилировать медленный эндпоинт — значит замерять цепочку, а не только обработчик.

<Quiz
  id="routing-mw-q1"
  lessonSlug="03-routing-and-middleware"
  lang="ru"
  question="Админский эндпоинт доступен без аутентификации, хотя auth-middleware реализован и протестирован. Наиболее вероятная причина?"
  choices={[
    { label: "Изменился формат токена auth", misconception: "Проблема формата токена отклоняла бы валидных пользователей, а не пропускала бы неаутентифицированных. Это проблема порядка, а не токена." },
    { label: "Маршрут зарегистрирован до auth-middleware, поэтому обработчик срабатывает первым и auth не успевает прервать цепочку", correct: true },
    { label: "Radix-trie сопоставил неверный путь", misconception: "Достижение правильного обработчика означает, что маршрутизация сработала. Сбой в том, что запрос ничего не аутентифицировало раньше." },
    { label: "БД отклонила auth-запрос", misconception: "Неудачный auth-запрос запретил бы доступ, а не разрешил. Обработчик выполнился без вызова auth вообще." },
  ]}
/>

<Quiz
  id="routing-mw-q2"
  lessonSlug="03-routing-and-middleware"
  lang="ru"
  question="Зачем ставить лимит размера запроса ПЕРЕД парсером тела в цепочке middleware?"
  choices={[
    { label: "Парсер тела не умеет читать размеры", misconception: "Парсер умеет читать размер, но только после того, как уже буферизовал тело — а это и есть стоимость, которую вы избегаете." },
    { label: "Чтобы слишком большая загрузка была отклонена до буферизации всего тела в память", correct: true },
    { label: "Потому что парсер медленнее проверки лимита на маленьких телах", misconception: "Дело не в относительной скорости на маленьких телах; дело в том, чтобы вообще не буферизовать огромное тело перед отказом." },
    { label: "Лимиты работают только на первой позиции middleware", misconception: "Никакой позиционной магии нет. Причина — порядок буферизации: отклонить до выделения памяти." },
  ]}
/>

<DragOrder
  id="routing-mw-drag1"
  lessonSlug="03-routing-and-middleware"
  lang="ru"
  prompt="Расставьте здравую цепочку middleware от самого внешнего (первый) к обработчику:"
  items={[
    "Обработчик ошибок охватывает всё (ловит throw ниже по цепочке)",
    "Логирование / трейсинг (присвоить request ID)",
    "Rate limiting (прервать 429 до выполнения работы)",
    "Аутентификация (прервать 401 до обработчика)",
    "Лимит размера тела, затем парсинг тела",
    "Обработчик (бизнес-логика)",
  ]}
/>

</Explanation>

<FlowDiagram
  label="Цепочка middleware в порядке регистрации, от запроса к обработчику"
  hue="var(--d-backend)"
  perRow={6}
  nodes={[
    { id: "req", label: "Запрос" },
    { id: "log", label: "Логирование", sub: "request id" },
    { id: "rate", label: "Rate limit", sub: "429" },
    { id: "auth", label: "Auth", sub: "401" },
    { id: "body", label: "Парсинг тела", sub: "лимит размера" },
    { id: "handler", label: "Обработчик" },
  ]}
  edges={[
    { from: "req", to: "log" },
    { from: "log", to: "rate" },
    { from: "rate", to: "auth" },
    { from: "auth", to: "body" },
    { from: "body", to: "handler" },
  ]}
  caption="Порядок — граница безопасности: каждый слой может прервать цепочку (rate limit → 429, auth → 401) до обработчика, а ответ разворачивается наружу в обратном порядке. Дешёвые защитные слои первыми, дорогие опциональные — последними, обработчик ошибок — самым внешним."
/>

<KeyTakeaway>Маршрутизация связывает запрос с одним обработчиком (radix-trie делает время сопоставления независимым от числа маршрутов); реальные риски маршрутизации — пересечения и путаница методов. Middleware — луковица сквозных слоёв, и любой слой может прервать цепочку, поэтому порядок регистрации — граница безопасности. Дешёвые защитные слои первыми, дорогие опциональные — последними, обработчик ошибок — самым внешним.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="routing-mw-retrieval"
  lang="ru"
  questions={[
    { q: "Чем radix-trie роутер отличается от линейного перебора и доминирует ли маршрутизация в задержке?", a: "Линейный роутер пробует каждый маршрут по порядку, поэтому время сопоставления O(n) по числу маршрутов. Radix-trie — префиксное дерево по сегментам пути, поэтому время O(длины пути) и независимо от числа маршрутов: 10 и 10 000 маршрутов сопоставляются за одно время. На практике оба — микросекунды, и маршрутизация редко доминирует в задержке; значимы риски корректности: пересекающиеся шаблоны вроде /users/:id и /users/me, несовпадение по завершающему слешу и путаница методов." },
    { q: "Объясните луковичную модель middleware и что значит «прерывание цепочки».", a: "Слои middleware оборачивают обработчик как луковица: запрос идёт внутрь через каждый слой к обработчику, затем ответ идёт наружу через те же слои в обратном порядке. Слой, вызывающий next(), передаёт управление внутрь; слой, который отвечает без вызова next(), прерывает цепочку (short-circuit), и обработчик не выполняется. Прерывание — это то, как rate limiter возвращает 429 или auth-слой возвращает 401 до запуска любой бизнес-логики." },
    { q: "Почему порядок middleware — граница безопасности и какое правило порядка?", a: "Поскольку любой слой может прервать цепочку, порядок регистрации решает, что реально охраняет обработчик. Маршрут до auth — обработчик выполняется без аутентификации; парсер тела до лимита размера — буферизуете огромную загрузку до отказа; сжатие до auth — тратите CPU на запросы, которые получат 401. Правило: дешёвые и защитные слои первыми (лимиты размера, rate limit, auth), дорогие и опциональные — последними (парсинг тела, сжатие), а обработчик ошибок — самым внешним, чтобы охватывать всё." },
  ]}
/>

<Recap lang="ru">
С разобранным запросом сервер выбирает, какой код выполнить. Маршрутизация связывает метод и путь ровно с одним обработчиком; radix-trie держит время сопоставления независимым от числа маршрутов, но кусаются баги корректности — пересечения и путаница методов. Вокруг обработчика — цепочка middleware, луковица сквозных слоёв (auth, логирование, парсинг тела, rate limiting, сжатие, обработка ошибок), где любой слой может прервать запрос. Эта возможность делает порядок регистрации границей безопасности и производительности: защитные дешёвые слои первыми, дорогие опциональные — последними, обработчик ошибок — самым внешним. Middleware — ещё и место, где тихо копится задержка на запрос. Следующая остановка — центр луковицы: сам обработчик и превращение его результата в ответ.
</Recap>
