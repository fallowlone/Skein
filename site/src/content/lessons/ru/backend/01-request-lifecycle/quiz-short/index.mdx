---
concepts:
- seven-stops
- accept-queue
- middleware-ordering
- backpressure
- keep-alive
- timeout-budget
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://nodejs.org/api/http.html
- https://nodejs.org/api/stream.html#backpressuring-in-streams
- https://www.barroso.org/publications/TheTailAtScale.pdf
status: ready
summary: "Промпты на свободное воспроизведение по жизненному циклу запроса. Сначала ответь своими словами, затем раскрой эталонный ответ и сверь с механизмами юнита."
title: 'Жизненный цикл запроса: тест на воспроизведение'
track: backend
unit: 01-request-lifecycle
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение лучше перечитывания. Для каждого промпта скажи или запиши полный ответ по памяти, прежде чем открыть эталон — усилие реконструкции механизма и закрепляет его.
</Hook>

<Goal lang="ru">
Реконструируй хребет юнита не подглядывая: семь остановок, две очереди ядра, почему порядок middleware — граница безопасности, механизм backpressure, keep-alive и как таймауты должны композироваться в бюджет.
</Goal>

<RetrievalDrawer
  client:visible
  id="01-request-lifecycle-short-retrieval"
  lang="ru"
  questions={[
    { q: "Назови семь остановок жизненного цикла запроса по порядку и один сбой на каждой.", a: "1) Accept — ядро завершает handshake и отдаёт сокет; accept-очередь может переполниться и отвергать соединения без логов приложения. 2) Parse — байты становятся запросом; раздутые заголовки (большая cookie или JWT) пробивают лимит размера и дают 431/400. 3) Route — метод и путь матчатся к одному обработчику; перекрывающиеся паттерны или путаница методов шлют запрос не туда. 4) Middleware — сквозные слои в порядке; неверный порядок обходит auth или лимит размера тела. 5) Handle — бизнес-логика, в основном ждёт I/O; медленный downstream доминирует в p99. 6) Serialize — результат становится байтами; JSON.stringify на большом объекте — синхронный CPU, блокирующий loop. 7) Drain — ответ пишется в сокет; медленный клиент плюс проигнорированный backpressure буферизует его в куче. Полная задержка — сумма по всем семи плюс очереди между ними." },
    { q: "Какие две очереди ядра стоят за listen()-сокетом, что ограничивает каждую и почему somaxconn = 128 — production-ловушка?", a: "SYN-очередь держит полуоткрытые соединения в середине handshake (сервер послал SYN-ACK, ждёт финальный ACK), ограничена net.ipv4.tcp_max_syn_backlog (~1024–2048). Accept-очередь держит полностью установленные соединения, ждущие вызова accept() приложением, ограничена min(listen backlog, net.core.somaxconn). somaxconn по умолчанию был 128 в Linux ~20 лет (поднят до 4096 в ядре 5.4), и многие container-образы всё ещё ставят 128. Даже фреймворк, запрашивающий listen(511) (дефолт Node), молча обрезается до 128, поэтому под всплеском 129 одновременно ожидающих соединений начинают отбрасываться — клиенты видят таймауты/reset, а приложение ничего не логирует, ведь код не выполнялся." },
    { q: "Почему порядок регистрации middleware — граница безопасности и каково правило порядка?", a: "Middleware оборачивает обработчик как луковица; любой слой, который отвечает без вызова next(), закорачивает запрос, и обработчик не запускается. Из-за этого порядок регистрации решает, что реально охраняет обработчик: маршрут до auth — обработчик выполняется неаутентифицированным; парсер тела до лимита размера — буферизуешь огромную загрузку до отклонения; сжатие до auth — тратишь CPU на запросы, которые получат 401. Правило: дешёвые и защитные слои первыми (лимиты размера, rate limit, auth), дорогие и опциональные последними (парсинг тела, сжатие), а error handler — снаружи, чтобы оборачивать всё ниже." },
    { q: "Проследи послойно, как медленный клиент вызывает серверный OOM при стриминговом ответе.", a: "Клиент-приложение читает медленно, поэтому его TCP-приёмный буфер заполняется и оно анонсирует меньшее receive window. Send-буфер ядра сервера больше не может сливаться в это сжимающееся окно и заполняется. Userland writable-поток больше не может проталкивать в буфер ядра, поэтому его собственный буфер заполняется и пересекает high-water mark (дефолт Node 16 КБ для байтовых потоков), и res.write() возвращает false. Этот false — сигнал flow control, не ошибка: данные приняты. Если приложение учитывает его (пауза до 'drain' или pipeline()), память держится плоской около high-water mark. Если игнорирует false и продолжает писать, разрыв между скоростью производства и скоростью чтения клиента копится в единственном слое без flow control — куче процесса — пока ядро не убьёт по OOM." },
    { q: "Что стоит сериализация запросу и почему статус-код — контракт, а не декорация?", a: "Превращение объекта результата в байты — CPU, который запрос платит всегда. JSON.stringify синхронен и масштабируется с размером объекта, поэтому большой массив (скажем, 5000 строк) стоит несколько мс и блокирует event loop для всех остальных запросов; прекомпилированные schema-сериализаторы (fast-json-stringify, Protobuf) работают в 2–4× быстрее, а пагинация — контроль стоимости сериализации, не только UX. Статус-код — часть контракта API: 201 created, 204 без тела, 4xx клиент должен изменить запрос, 5xx серверный сбой, безопасный для ретрая идемпотентных вызовов. Клиенты, прокси, ретраи и алерты ориентируются на статус, поэтому 200 с объектом ошибки внутри прячет сбои от каждого слоя, который их мониторит." },
    { q: "Что оптимизирует keep-alive и почему per-hop таймауты должны композироваться в пробрасываемый deadline?", a: "Keep-alive переиспользует одно TCP/TLS-соединение для многих запросов, чтобы handshake — один или более round trip — амортизировался вместо оплаты на запрос; после слива ответа соединение лежит в idle-пуле до keep-alive таймаута, поэтому ответ готов лишь когда байты слиты и соединение корректно сохранено или закрыто. Таймауты отдельны, но тоже элемент жизненного цикла: каждый исходящий вызов нуждается в таймауте («no timeout» — худший дефолт, один зависший downstream исчерпывает пулы), но независимые per-hop таймауты не композируются — запрос с SLA 1 с, вызывающий A (1 с), вызывающий B (1 с), позволяет A работать после того, как клиент сдался. Фикс — пробрасываемый deadline: точка входа выделяет бюджет, и каждый hop использует min(local default, remaining); модель, которую стандартизирует gRPC. Это важно, ведь на масштабе доминирует хвост — fan-out на N сервисов делает тебя медленным, если медленный любой, 1 − (1 − p)^N ≈ 63% при p=1%, N=100 — поэтому deadline плюс hedging на p95 защищают хвост." },
  ]}
/>

<Recap lang="ru">
Если ты смог реконструировать каждый ответ по памяти — ты держишь хребет юнита: семь остановок, чьи задержки суммируются плюс очереди; две очереди ядра, где somaxconn молча обрезает глубину accept; порядок middleware как граница безопасности; backpressure как userland-поверхность TCP flow control, дающая OOM при игнорировании; сериализация как реальный синхронный CPU и статус-код как контракт; и таймауты, которые должны композироваться в пробрасываемый deadline, ведь хвост fan-out, а не среднее, задаёт воспринимаемую пользователем задержку.
</Recap>
