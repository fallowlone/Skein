---
concepts:
- timeout
- deadline-propagation
- timeout-budget
- tail-latency
- fan-out
- p99
- hedged-requests
deepensInto:
- backend/06-circuit-breakers/01-overview
- backend/08-putting-it-together/01-overview
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 6
prereqs:
- backend/01-request-lifecycle/05-streaming-and-backpressure
slug: 06-timeouts-and-tail-latency
sources:
- https://www.barroso.org/publications/TheTailAtScale.pdf
- https://grpc.io/docs/guides/deadlines/
- https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/
spiral:
- latency
status: ready
summary: 'Каждому хопу нужен таймаут, и таймауты должны складываться в бюджет на весь
  запрос. На масштабе доминирует хвост: разветвитесь к достаточному числу сервисов
  — и самый медленный решает задержку почти каждого пользователя.'
title: 'Таймауты и хвостовая задержка: бюджеты, дедлайны и ловушка fan-out'
track: backend
unit: 01-request-lifecycle
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Каждый сервис в цепочке здоров: любая зависимость отвечает за 10 мс на 99-м перцентиле. Страница продукта разветвляется к 100 из них параллельно и ждёт всех. И всё же 63% загрузок страницы занимают больше секунды. Ни один сервис не медленный. Медленна математика хвоста — и интуиция большинства инженеров про средние полностью её прячет.
</Hook>

<Crux>Каждому хопу нужен таймаут, складывающийся в бюджет запроса — и за достаточным fan-out самая медленная зависимость задаёт задержку каждому.</Crux>

<Explanation>

## Таймаут на каждом хопе, или зависание на каждом инциденте

Сетевой вызов без таймаута — баг, ждущий инцидента. Когда зависимость перестаёт отвечать (не отказывает — *зависает*), вызов без таймаута ждёт вечно, удерживая поток или соединение. Достаточно таких вызовов — и пул исчерпан, и одна медленная зависимость роняет здоровый сервис. Каждому исходящему вызову — запросу к БД, кешу, HTTP, RPC — нужен явный таймаут. Дефолт в большинстве клиентов — *нет таймаута*, и это худший дефолт в backend-инженерии.

## Таймауты должны складываться в бюджет

Потаймауты на хоп, выставленные изолированно, лгут. Если у запроса SLA 1 с, но он вызывает сервис A (таймаут 1 с), который вызывает сервис B (таймаут 1 с), то когда B медленный, A ждёт всю секунду, а клиент уже сдался — A теперь делает работу, которую никто не ждёт. Лечение — **бюджет таймаута** (дедлайн): точка входа выделяет общий объём, и каждый хоп передаёт *оставшееся* время вниз. gRPC формализует это как **дедлайн**, распространяемый в метаданных; каждый сервис вычисляет свой локальный таймаут как `min(локальный дефолт, оставшийся бюджет)`.

<div data-lesson-visual class="overflow-x-auto my-6">

| Подход | Что использует каждый хоп | Режим отказа |
|---|---|---|
| Нет таймаутов | ∞ | Одна зависшая зависимость исчерпывает пулы, каскад |
| Независимые потаймауты на хоп | Фиксированное локальное значение | Внутренняя работа переживает терпение вызывающего |
| Распространяемый дедлайн (бюджет) | min(локальный, оставшийся) | Ограничен; внутренние хопы стоп, когда бюджет потрачен |

</div>

## Почему хвост, а не среднее, — это SLA

Пользователи не переживают ваше среднее. Они переживают *свой* запрос, и медленные — то, что они запоминают и что срывает алерты. Поэтому задержку отчитывают перцентилями: **p50** (медиана), **p99** (1 из 100 хуже), **p99.9**. Разрыв между p50 и p99 — «хвостовая задержка», вызванная паузами GC, очередями, промахами кеша, конкуренцией за блокировки и повторами.

Опасность — **усиление хвоста под fan-out**. Если один запрос к сервису медленный с вероятностью *p*, то запрос, разветвляющийся к *N* сервисам параллельно и ждущий всех, медленный, если медленный *любой* один — вероятность `1 − (1 − p)^N`. При посервисном p99 (p = 1%) и N = 100 это `1 − 0.99^100 ≈ 63%`. Это число из Hook, прямо из *The Tail at Scale* Дина и Барросо: сервис, разветвляющийся к 2000, оставляет около 20% запросов дольше секунды, даже когда p99 каждого бэкенда в норме.

## Защита хвоста: хеджирование, а не только таймауты

Таймаут ограничивает худший случай, но не улучшает типичный хвост. Техника из *The Tail at Scale* — **хеджированные запросы**: отправьте запрос, и если ответ не пришёл к задержке p95, отправьте *вторую* копию другой реплике и возьмите ту, что вернётся первой. Поскольку хеджируются только медленные ~5%, лишняя нагрузка мала (~5%), а хвост схлопывается — в измерениях Google отправка хеджа после задержки в 10 мс срезала p99.9 с 1800 мс до 74 мс ценой ~2% лишних запросов. **Связанные запросы** идут дальше: дубликаты говорят друг другу отмениться, как только один начал выполняться, обрезая зря потраченную работу.

<Inset kind="why" lang="ru">
Почему не просто повтор по таймауту вместо хеджирования? Повтор срабатывает только *после* того, как вы уже заплатили полный таймаут — поэтому он улучшает доступность, но не задержку, а наивные повторы усиливают нагрузку ровно тогда, когда сервис и так в беде (шторм повторов). Хеджирование срабатывает спекулятивно на p95, до таймаута, поэтому атакует задержку напрямую; и поскольку оно ограничено медленным хвостом, добавляет ограниченную нагрузку. Эти двое дополняют друг друга: хеджируй, чтобы срезать хвост, повторяй с backoff и jitter, чтобы пережить сбои, и circuit breaker (следующий юнит), чтобы остановить оба, когда зависимость по-настоящему лежит.
</Inset>

<Quiz
  id="timeouts-tail-q1"
  lessonSlug="06-timeouts-and-tail-latency"
  lang="ru"
  question="Страница разветвляется к 100 независимым сервисам параллельно и ждёт всех. У каждого p99 = 10 мс (1% шанс, что вызов превысит это). Примерно какая доля загрузок страницы превысит 10 мс хотя бы на одном вызове?"
  choices={[
    { label: "Около 1% — то же, что у одного сервиса", misconception: "Это было бы верно для одного вызова. При 100 независимых вызовах шанс, что ни один не медленный, — 0.99^100, поэтому медленная доля гораздо выше." },
    { label: "Около 63% — 1 − 0.99^100", correct: true },
    { label: "Около 10% — оно масштабируется линейно с перцентилем", misconception: "Усиление хвоста не линейно. Складывающееся 1 − (1 − p)^N растёт намного быстрее линейной оценки." },
    { label: "0% — параллельные вызовы не могут все быть медленными разом", misconception: "Вы ждёте ВСЕХ, поэтому вы медленны, если медленен ЛЮБОЙ — не только если все. Это и усиливает хвост." },
  ]}
/>

<Quiz
  id="timeouts-tail-q2"
  lessonSlug="06-timeouts-and-tail-latency"
  lang="ru"
  question="Почему независимые потаймауты на хоп не защищают запрос с общим SLA?"
  choices={[
    { label: "Потаймауты на хоп всегда слишком короткие", misconception: "Дело не в длине, а в композиции. Даже хорошо подобранные локальные таймауты могут сложиться за SLA без общего бюджета." },
    { label: "Внутренние хопы продолжают работать после того, как вызывающий уже сдался, потому что ничто не распространяет оставшийся бюджет", correct: true },
    { label: "Таймауты нельзя ставить на вызовы к базе данных", misconception: "Клиенты БД поддерживают таймауты. Проблема в том, что изолированные таймауты не складываются в дедлайн." },
    { label: "Ядро игнорирует таймауты приложения", misconception: "Таймауты приложения соблюдаются. Сбой архитектурный — никакой дедлайн не распространяется вниз по цепочке вызовов." },
  ]}
/>

<Quiz
  id="timeouts-tail-q3"
  lessonSlug="06-timeouts-and-tail-latency"
  lang="ru"
  question="Как хеджированный запрос снижает хвостовую задержку без большой лишней нагрузки?"
  choices={[
    { label: "Он повторяет каждый запрос дважды с самого начала", misconception: "Это примерно удвоило бы нагрузку. Хеджирование шлёт вторую копию только для медленного меньшинства, не для каждого запроса." },
    { label: "Он шлёт вторую копию, только если ответ не пришёл к ~p95, поэтому дублируются лишь медленные ~5%", correct: true },
    { label: "Он снижает p50 каждого сервиса", misconception: "Хеджирование целит в хвост, а не в медиану. Типичный быстрый запрос никогда не запускает хедж." },
    { label: "Он полностью отключает таймауты", misconception: "Хеджирование работает вместе с таймаутами; оно их не заменяет. Таймаут всё ещё ограничивает абсолютно худший случай." },
  ]}
/>

</Explanation>

<SequenceDiagram
  label="Бюджет дедлайна, распространяемый по цепочке вызовов, чтобы внутренние хопы стопались, когда бюджет потрачен"
  hue="var(--d-backend)"
  actors={["Клиент", "Сервис A", "Сервис B"]}
  messages={[
    { from: "Клиент", to: "Сервис A", label: "дедлайн = 1000 мс" },
    { from: "Сервис A", to: "Сервис B", label: "остаток = 700 мс" },
    { from: "Сервис B", to: "Сервис A", label: "ответ или дедлайн истёк" },
    { from: "Сервис A", to: "Клиент", label: "ответ в рамках бюджета" },
  ]}
  caption="Точка входа задаёт общий бюджет; каждый хоп передаёт остаток времени и берёт min(локальный таймаут, остаток). Когда бюджет потрачен, внутренние хопы останавливаются, а не делают работу, которую никто не ждёт."
/>

<KeyTakeaway>Ставьте явный таймаут на каждый исходящий вызов — «нет таймаута» это худший дефолт. Складывайте их в распространяемый дедлайн, чтобы внутренние хопы стопались, когда бюджет потрачен. Отслеживайте p99/p99.9, а не среднее: разветвитесь к N сервисам — и вы медленны, если медленен любой (1 − (1−p)^N ≈ 63% при p=1%, N=100). Хеджируйте медленные запросы на p95, чтобы схлопнуть хвост ценой ~5% лишней нагрузки.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="timeouts-tail-retrieval"
  lang="ru"
  questions={[
    { q: "Почему каждому исходящему вызову нужен явный таймаут, и почему распространяемый дедлайн лучше независимых потаймаутов на хоп?", a: "Вызов без таймаута ждёт вечно, когда зависимость зависает (а не отказывает), удерживая поток или соединение; достаточно зависших вызовов — и пул исчерпан, и одна медленная зависимость каскадом роняет сервис — а «нет таймаута» это дефолт в большинстве клиентов. Независимые потаймауты на хоп всё равно не работают, потому что они не складываются: если запрос с SLA 1 с вызывает A (1 с), который вызывает B (1 с), то когда B медленный, A продолжает работать после того, как клиент уже сдался. Распространяемый дедлайн (бюджет) это чинит: точка входа выделяет общий объём, и каждый хоп передаёт оставшееся время вниз, вычисляя локальный таймаут как min(локальный дефолт, оставшийся). gRPC формализует это как дедлайн в метаданных." },
    { q: "Объясните усиление хвоста под fan-out с математикой и каноническими числами.", a: "Если один вызов к сервису медленный с вероятностью p, то запрос, разветвляющийся к N сервисам параллельно и ждущий всех, медленный, если медленен любой один: вероятность 1 − (1 − p)^N. При посервисном p99 (p = 1%) и N = 100 это 1 − 0.99^100 ≈ 63% медленных запросов — хотя p99 каждого бэкенда в норме. Из The Tail at Scale Дина и Барросо: сервис, разветвляющийся к ~2000 бэкендам, оставляет примерно 20% запросов дольше секунды. Урок: на масштабе хвост, а не среднее, задаёт воспринимаемую пользователем задержку, поэтому нужно мерить p99/p99.9 и проектировать под самую медленную зависимость." },
    { q: "Что такое хеджированные и связанные запросы, и почему их предпочитают повторам для среза хвоста?", a: "Хеджированный запрос отправляет запрос, и если ответ не пришёл к задержке p95, шлёт вторую копию другой реплике и берёт ту, что вернётся первой. Поскольку хедж запускают только медленные ~5%, лишняя нагрузка ограничена (~5%), а хвост схлопывается — Google измерил, что хедж с задержкой 10 мс срезал p99.9 с 1800 мс до 74 мс ценой ~2% лишних запросов. Связанные запросы идут дальше: дубликаты отменяют друг друга, как только один начал выполняться, снижая зря потраченную работу. Они бьют повторы по задержке, потому что повтор срабатывает только после уже оплаченного полного таймаута (помогая доступности, не задержке), а наивные повторы усиливают нагрузку в беде; хеджирование срабатывает спекулятивно до таймаута и ограничено хвостом. Они дополняют друг друга: хеджируй ради задержки, повторяй с backoff и jitter ради сбоев, и circuit breaker, чтобы остановить оба, когда зависимость лежит." },
  ]}
/>

<Recap lang="ru">
Последняя остановка превращает запрос из «он возвращается» в «он возвращается вовремя». Каждому исходящему вызову нужен явный таймаут, потому что дефолт ждать вечно даёт одной зависшей зависимости исчерпать пулы и пойти каскадом. Но изолированные таймауты не складываются, поэтому они должны сворачиваться в распространяемый дедлайн, где каждый хоп использует min(локальный, оставшийся) — модель, которую стандартизирует gRPC. На масштабе среднее — ложь: пользователь чувствует свой запрос, поэтому отслеживай p99/p99.9, а fan-out (веерный вызов к нескольким сервисам одновременно) усиливает хвост жестоко — 1 − (1 − p)^N достигает 63% медленных при p=1%, N=100. Таймауты ограничивают худший случай, но не чинят типичный хвост; хеджированные и связанные запросы, запущенные на p95, схлопывают его ценой пары процентов лишней нагрузки. Теперь, когда увидишь p99, отличный по каждому отдельному сервису, но ужасный на уровне страницы, — нарисуй граф fan-out и примени математику, прежде чем добавлять кеши. Это мост к устойчивости: когда зависимость не просто медленна, а сбоит, таймаутов и хеджирования мало, и circuit breakers (автоматические выключатели) и bulkheads следующего юнита берут управление.
</Recap>
