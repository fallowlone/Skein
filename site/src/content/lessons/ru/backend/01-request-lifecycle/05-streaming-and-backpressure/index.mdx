---
concepts:
- streaming
- backpressure
- high-water-mark
- drain
- flow-control
- tcp-window
- slow-consumer
deepensInto:
- backend/01-request-lifecycle/06-timeouts-and-tail-latency
- backend/03-async-blocking/01-overview
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 5
prereqs:
- backend/01-request-lifecycle/04-handler-and-response
slug: 05-streaming-and-backpressure
sources:
- https://nodejs.org/api/stream.html#backpressuring-in-streams
- https://www.rfc-editor.org/rfc/rfc9113
- https://nodejs.org/api/http.html
spiral:
- latency
status: ready
summary: Ответ не закончен, когда обработчик вернул значение — он закончен, когда
  байты слиты. Если клиент читает медленнее, чем сервер пишет, и backpressure проигнорирован,
  ответ буферизуется в RAM, пока процесс не умрёт.
title: 'Стриминг и backpressure: когда клиент читает медленнее, чем вы пишете'
track: backend
unit: 01-request-lifecycle
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Сервис отчётов стримит CSV-экспорт на 400 МБ. В тестах работает. В проде он OOM-ится и перезапускается каждый день после обеда. Код экспорта в порядке — он читает строки и вызывает `res.write()` в плотном цикле. Баг в том, что один клиент скачивает через телефон на раздаче в 200 КБ/с, и сервер генерирует строки куда быстрее, чем телефон успевает их читать. Непрочитанные байты копятся в куче процесса, пока ядро его не убьёт.
</Hook>

<Crux>Если пишете быстрее, чем клиент читает, и игнорируете backpressure, неотправленные байты буферизуются в вашей куче, пока процесс не убьют.</Crux>

<Explanation>

## Буфер против стрима

Прежде чем написать хоть строку стримингового кода, спроси себя: нужен ли стриминг вообще, и если да — что произойдёт, когда клиент будет читать медленнее, чем ты пишешь? Ответ определяет, будет ли память плоской или сервис будет умирать каждую ночь по OOM.

Есть два способа отправить тело. **Буфер**: собрать весь ответ в памяти, выставить `Content-Length`, отправить. Просто, но экспорт на 400 МБ означает 400 МБ резидентной памяти на каждую одновременную загрузку. **Стрим**: производить тело по частям и писать каждую часть, как только она готова, используя `Transfer-Encoding: chunked`, чтобы общий размер не нужно было знать заранее. Стриминг держит память плоской — *если* вы уважаете backpressure.

## Backpressure: запись, которая говорит «стоп»

У каждого writable-стрима есть буфер с порогом, называемым **high-water mark** (дефолт Node — **16 КБ** для байтовых стримов, 64 КБ для файловых). Механизм:

- `res.write(chunk)` возвращает `true`, пока внутренний буфер ниже high-water mark — пишите дальше.
- Возвращает `false`, когда буфер полон — **прекратите писать** и ждите события `'drain'`, прежде чем продолжить.

`write()`, вернувший `false`, **не** означает, что запись провалилась. Он означает, что потребитель отстаёт и буфер полон. Если вы проигнорируете `false` и продолжите писать, данные не исчезают — они накапливаются во внутреннем буфере стрима в куче вашего процесса. Это и есть OOM. Лечение — соблюсти сигнал: приостановить производство до `'drain'`. `pipe()` и `pipeline()` делают это за вас автоматически, поэтому `pipeline(source, res)` — безопасный дефолт, а самописный `while`-цикл из `write()` — классический футган.

<div data-lesson-visual class="overflow-x-auto my-6">

| Паттерн | Память при медленном клиенте | Вердикт |
|---|---|---|
| Буферизовать всё тело, затем отправить | O(размера тела) на соединение | Только для маленьких тел |
| `write()` в цикле, игнор возвращаемого значения | Растёт без границ → OOM | Баг |
| `write()` + ждать `'drain'` на `false` | Плоская (≈ high-water mark) | Корректно |
| `pipeline(source, res)` | Плоская, обрабатывает ошибки + очистку | Корректно, предпочтительно |

</div>

## Почему буфер наполняется: это черепахи до самого TCP

Backpressure — не изобретение Node; это прикладная поверхность цепочки окон управления потоком. TCP-окно приёма клиента сообщает, сколько он может принять. Когда приложение клиента читает медленно, его буфер приёма наполняется, оно сжимает анонсированное окно, и буфер отправки в ядре сервера перестаёт сливаться. Тогда writable-стрим в userland перестаёт сливаться, `write()` возвращает `false` — и, если вы слушаете, ваш код прекращает производить. Каждый слой давит назад на тот, что выше. Игнорировать backpressure — значит расцепить вашу *скорость производства* со всей этой цепочкой, и разрыв накапливается в единственном месте без управления потоком: в вашей куче.

<Inset kind="why" lang="ru">
Почему HTTP/2 делает backpressure одновременно важнее и тоньше? HTTP/2 мультиплексирует много стримов поверх одного TCP-соединения, и у него свои окна управления потоком *на каждый стрим* (дефолт 64 КБ) поверх окна TCP уровня соединения. Один медленный стрим может застрять, если его окно исчерпано, но поскольку все стримы делят одно TCP-соединение, head-of-line blocking на уровне TCP может застопорить и несвязанные стримы. Поэтому HTTP/2-сервер должен уважать два слоя окон, а плохо ведущая себя большая загрузка может заморить маленькие одновременные запросы на том же соединении способами, невидимыми на уровне HTTP/1.1.
</Inset>

## Атака медленного потребителя

Тот же механизм — вектор отказа в обслуживании. **Slowloris** и slow-read атаки намеренно читают ответы по одному байту, чтобы держать соединения открытыми и занимать буферы на стороне сервера. Сервер, буферизующий на соединение и игнорирующий backpressure, можно исчерпать горсткой медленных клиентов — без всякого объёма. Защиты те же, что и исправления корректности, плюс лимиты: уважать backpressure, ограничивать буферизацию на соединение и ставить таймауты записи/простоя, чтобы застрявший слив был брошен, а не удержан навсегда (тема следующего урока).

<Quiz
  id="streaming-q1"
  lessonSlug="05-streaming-and-backpressure"
  lang="ru"
  question="Стриминговый эндпоинт OOM-ится только когда клиент скачивает по очень медленному каналу. Код вызывает res.write() в цикле и игнорирует возвращаемое значение. Что происходит?"
  choices={[
    { label: "Чтение с диска падает на медленных каналах", misconception: "Чтение источника в порядке. Проблема — рассогласование между тем, как быстро вы пишете, и как медленно клиент читает." },
    { label: "write() возвращает false, когда буфер полон, но код продолжает писать, поэтому неотправленные байты копятся в куче", correct: true },
    { label: "Медленные каналы портят TCP-контрольную сумму и форсируют ретрансмиты", misconception: "Ретрансмиты нормальны и ограничены; они не вызывают неограниченный рост кучи. Рост — это непрочитанные данные, буферизованные в userland." },
    { label: "Сборщик мусора отключён на медленных соединениях", misconception: "GC не может освободить буферы, на которые ещё ссылается очередь ожидающих записей. Данные живы, а не мусор." },
  ]}
/>

<Quiz
  id="streaming-q2"
  lessonSlug="05-streaming-and-backpressure"
  lang="ru"
  question="Что на самом деле означает res.write(), вернувший false?"
  choices={[
    { label: "Запись провалилась и данные потеряны", misconception: "Данные приняты в буфер; ничего не потеряно. false — сигнал управления потоком, а не ошибка." },
    { label: "Внутренний буфер пересёк high-water mark — нужно прекратить писать и возобновить на событии 'drain'", correct: true },
    { label: "Соединение закрыто клиентом", misconception: "Закрытое соединение поднимает ошибку или событие 'close', а не возврат false. false означает, что буфер полон, а не пропал." },
    { label: "Ответ завершён", misconception: "Завершение сигнализируется через end(), а не возвратом false из write(). false означает «помедленнее», а не «готово»." },
  ]}
/>

<Quiz
  id="streaming-q3"
  lessonSlug="05-streaming-and-backpressure"
  lang="ru"
  question="Почему backpressure описывают как прикладную поверхность управления потоком TCP?"
  choices={[
    { label: "Потому что Node переписывает TCP на JavaScript", misconception: "Node не переписывает TCP. Он выводит состояние управления потоком ядра наверх в ваш код через возвращаемое значение write()." },
    { label: "Медленный читатель сжимает своё TCP-окно приёма, что останавливает слив буфера отправки в ядре, что наполняет буфер в userland, что возвращает false вашему коду", correct: true },
    { label: "Backpressure существует только в HTTP, а не в TCP", misconception: "В TCP управление потоком через окно приёма было с самого начала. Прикладной backpressure едет поверх него." },
    { label: "TCP-окна не связаны с памятью приложения", misconception: "Они связаны напрямую: когда окно останавливает слив буфера отправки, буфер в userland — это место, где накапливается разрыв, если его игнорировать." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Цепочка backpressure от производителя к медленному клиенту и где копятся байты, если её игнорировать"
  hue="var(--d-backend)"
  perRow={5}
  nodes={[
    { id: "prod", label: "Производитель", sub: "res.write()" },
    { id: "buf", label: "Userland-буфер", sub: "HWM 16 КБ" },
    { id: "kern", label: "Буфер ядра" },
    { id: "win", label: "Окно TCP", sub: "приём" },
    { id: "client", label: "Медл. клиент", sub: "200 КБ/с" },
  ]}
  edges={[
    { from: "prod", to: "buf" },
    { from: "buf", to: "kern" },
    { from: "kern", to: "win" },
    { from: "win", to: "client" },
  ]}
  caption="Медленный клиент сужает окно TCP, буфер ядра перестаёт сливаться, userland-буфер переходит high-water mark, и write() возвращает false. Учтёте его — память остаётся плоской; проигнорируете — непрочитанные байты копятся в куче производителя до OOM."
/>

<KeyTakeaway>Стримьте большие тела вместо буферизации, но уважайте backpressure: write() возвращает false, когда буфер прошёл high-water mark (дефолт 16 КБ) — остановитесь и возобновите на 'drain', или используйте pipeline(), который делает это за вас. Игнор false буферизует непрочитанные байты в куче до OOM. Это userland-поверхность управления потоком TCP, и тот же механизм медленные клиенты используют для DoS.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="streaming-retrieval"
  lang="ru"
  questions={[
    { q: "Что именно сигнализирует res.write(), вернувший false, и какова правильная реакция?", a: "Он сигнализирует, что внутренний буфер writable-стрима пересёк high-water mark (дефолт Node — 16 КБ для байтовых стримов, 64 КБ для файловых) — потребитель читает медленнее, чем вы пишете. Это сигнал управления потоком, а не ошибка; данные, которые вы только что записали, приняты в буфер. Правильная реакция — прекратить писать и ждать события 'drain', прежде чем производить дальше. Игнорировать его и продолжать писать не теряет данные — оно накапливает неотправленные байты в куче процесса, что и есть OOM. Использование pipe()/pipeline() обрабатывает это автоматически и является предпочтительным дефолтом." },
    { q: "Проследите, как медленный клиент в итоге вызывает OOM на стороне сервера, слой за слоем.", a: "Приложение клиента читает медленно, поэтому его TCP-буфер приёма наполняется и оно анонсирует меньшее окно приёма. Буфер отправки в ядре сервера больше не может сливаться в это сжимающееся окно, поэтому наполняется. Writable-стрим в userland больше не может проталкивать в буфер ядра, поэтому его собственный буфер наполняется и пересекает high-water mark, заставляя write() вернуть false. Если приложение соблюдает этот сигнал, оно приостанавливает производство, и память остаётся плоской (≈ high-water mark). Если оно игнорирует false и продолжает писать, разрыв между скоростью производства и скоростью чтения клиента накапливается в единственном месте без управления потоком — куче процесса — пока ядро не убьёт его по OOM." },
    { q: "Почему HTTP/2 добавляет второй слой заботы о backpressure поверх TCP, и как медленная загрузка может навредить другим запросам?", a: "HTTP/2 мультиплексирует много логических стримов поверх одного TCP-соединения и добавляет свои окна управления потоком на каждый стрим (дефолт 64 КБ) поверх окна TCP уровня соединения. Сервер должен уважать оба. Поскольку все стримы делят одно TCP-соединение, одна большая или медленная загрузка может исчерпать общую ёмкость и через head-of-line blocking на уровне TCP застопорить маленькие одновременные запросы на том же соединении — эффект заморения, невидимый на уровне HTTP/1.1. Митигация — та же дисциплина: уважать управление потоком на каждом слое и ограничивать буферизацию и время на соединение." },
  ]}
/>

<Recap lang="ru">
Ответ заканчивается, когда его байты слиты, а не когда обработчик вернул значение — и трудный случай это клиент, читающий медленнее, чем сервер пишет. Буферизация всего тела стоит O(размера тела) памяти на соединение; стриминг держит память плоской лишь если уважать backpressure (противодавление на производителя). Сигнал — write(), возвращающий false, когда внутренний буфер пересекает high-water mark (порог заполнения, дефолт 16 КБ): остановиться и возобновить на 'drain', или дать pipeline() управлять этим. Игнорирование сигнала копит непрочитанные байты в куче, пока процесс не убьют по OOM. Backpressure — userland-поверхность цепочки окон управления потоком вплоть до окна приёма TCP, а HTTP/2 добавляет окна на каждый стрим поверх. Тот же механизм — вектор DoS медленного потребителя, и поэтому финальная остановка, таймауты, должна бросать слив, который никогда не завершается. Теперь, когда встретишь сервис, умирающий от OOM при определённых клиентах, проверяй каждый вызов res.write(): возвращаемое значение вообще проверяется?
</Recap>
