infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label Восемь слов, четыре роли
    children
      - label Слушать и масштабировать
        children
          - label Сервер — слушает сетевые подключения
          - label Statelessness — любой инстанс ответит
      - label Диалог
        children
          - label Запрос / Ответ — клиент спрашивает, сервер отвечает
          - label HTTP-метод — GET, POST, PUT, DELETE
          - label Код статуса — 200, 404, 500
      - label Маршрут и логика
        children
          - label Endpoint / Маршрут — URL-путь + метод
          - label Обработчик — функция для маршрута
      - label Хранить
        children
          - label База данных — общая, переживает перезапуски
theme
  palette #6366f1 #14b8a6 #f59e0b #8b5cf6
