---
concepts:
- middleware
- dependency-injection
- cross-cutting-concerns
- inversion-of-control
- composition-root
- coupling
deepensInto:
- backend/02-middleware-di/02-writing-middleware
- backend/02-middleware-di/03-inversion-of-control
estMin: 11
lang: ru
lessonType: topic
level: junior
order: 1
prereqs:
- backend/01-request-lifecycle/03-routing-and-middleware
slug: 01-overview
sources:
- https://expressjs.com/en/guide/using-middleware.html
- https://docs.nestjs.com/providers
- https://martinfowler.com/articles/injection.html
spiral:
- latency
status: ready
summary: 'Под каждым фреймворком лежат два паттерна: middleware перехватывает каждый
  запрос по порядку, а внедрение зависимостей связывает сервисы один раз на старте.
  Один управляет тем, что выполняется вокруг вашего кода; другой — тем, до чего код
  может дотянуться и как вы его тестируете.'
title: 'Middleware и DI: два паттерна, формирующие любой backend'
track: backend
unit: 02-middleware-di
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
Два инженера описывают один и тот же backend. Один говорит: «каждый запрос проходит через auth, логирование и валидацию до обработчика». Другой говорит: «обработчик получает клиент базы, кеш и mailer, которые он сам не создавал». Они описывают один код с двух ракурсов: запрос, текущий *сквозь* слои, и зависимости, текущие *внутрь* объектов. Эти два ракурса — middleware и внедрение зависимостей, и почти каждое решение по фреймворку, которое вы примете, относится к одному из них.
</Hook>

<Crux>Middleware перехватывает каждый запрос по порядку; DI связывает сервисы на старте — вместе решают, что выполняется и насколько тестируемо.</Crux>

<Explanation>

## Две ортогональные оси

Middleware и внедрение зависимостей отвечают на разные вопросы, и их путаница — частый источник мутной архитектуры:

- **Middleware** — про **ось запроса**: что выполняется *вокруг* обработчика на каждом запросе и в каком порядке. Это динамично, на каждый запрос, упорядочено: auth, логирование, парсинг тела, rate limiting. Прошлый юнит показал, почему этот порядок — граница безопасности.
- **Внедрение зависимостей** — про **ось связывания**: какие объекты куску кода *дают*, вместо того чтобы он конструировал их сам. Это в основном статично, решается на старте и про связанность и тестируемость: обработчик получает `UserRepository`, а не вызывает `new UserRepository()` внутри себя.

Одно — конвейер; другое — граф. Запрос путешествует по конвейеру; граф собирается один раз до того, как придёт хоть один запрос.

## Зачем нужен middleware: сквозные заботы

Auth, логирование, трейсинг, сжатие и обработка ошибок нужны почти каждому эндпоинту, но не принадлежат ни одному из них. Без middleware каждый обработчик переизобретает их — несогласованно. Middleware выносит эти **сквозные заботы** в упорядоченные слои, оборачивающие обработчик, чтобы они были написаны один раз и применялись единообразно.

## Зачем нужен DI: инверсия управления

Когда класс вызывает `new EmailService()` внутри себя, он приварен к этой конкретной реализации: нельзя подменить её фейком в тесте, нацелить на другой конфиг или переиспользовать класс в другом месте. **Инверсия управления** переворачивает это — класс объявляет, *что* ему нужно (отправитель писем), а что-то внешнее решает, *какую* конкретную реализацию он получит. Это внешнее место — **корень композиции**, единственная точка рядом со стартом, которая знает реальные реализации и связывает граф объектов.

<div data-lesson-visual class="overflow-x-auto my-6">

| | Middleware | Внедрение зависимостей |
|---|---|---|
| Ось | Запрос (что выполняется вокруг кода) | Связывание (что коду дают) |
| Когда | На каждый запрос, динамически | Один раз на старте, статически |
| Форма | Упорядоченный конвейер | Граф объектов |
| Решает | Сквозные заботы | Связанность, тестируемость |
| Ломается | Порядок → баги безопасности/производительности | Скрытый `new` → нетестируемый код |

</div>

## Они встречаются во фреймворке

Реальные фреймворки сплавляют оба. Контроллеры NestJS разрешаются из DI-контейнера *и* сидят за конвейером middleware/guard/interceptor. Express держит DI неявным (вы связываете руками или цепляете к `req`), но модель middleware явная. Распознавать, на какой оси живёт проблема, — рефлекс сеньора: «этот эндпоинт медленный на каждом запросе» — обычно middleware; «этот класс невозможно протестировать» — обычно DI.

У двух осей очень разные профили стоимости, и именно поэтому их стоит разделять. Стоимость middleware — *на каждый запрос и повторяющаяся*: каждый слой в цепочке Express обычно добавляет единицы микросекунд накладных расходов, но раздутая цепочка из 12–15 слоёв (auth, логирование, парсинг тела, сжатие, CORS, helmet, rate limit и т. д.) может добавлять порядка 0,3–0,5мс на p50 и 1–3мс на p99, когда в игру вступают GC и медленный асинхронный слой вроде аудит-лога — умноженное на каждый запрос, это уже реальная пропускная способность. Стоимость DI в основном *платится один раз на старте*: приложение NestJS с несколькими сотнями провайдеров тратит порядка десятков-сотен миллисекунд на сборку графа при загрузке, а затем обслуживает запросы синглтонами с почти нулевой стоимостью связывания на запрос. Так что ошибка в middleware облагает налогом каждый запрос навсегда; ошибка в DI обычно облагает только загрузку (с одним резким исключением — request-scoped провайдеры из урока 04, которые затягивают стоимость связывания обратно в горячий путь каждого запроса).

<Quiz
  id="mw-di-overview-q1"
  lessonSlug="01-overview"
  lang="ru"
  question="Класс-сервис вызывает `new StripeClient()` прямо в конструкторе, и команда понимает, что его невозможно юнит-тестировать без обращения к реальному Stripe API. На какой оси проблема?"
  choices={[
    { label: "Порядок middleware — вызов Stripe выполняется в неверном слое", misconception: "Порядок — про то, что выполняется вокруг запроса. Здесь проблема в том, что класс конструирует собственную зависимость, а это забота связывания/DI." },
    { label: "Внедрение зависимостей — захардкоженный `new` приваривает класс к реальной реализации, убирая шов для тестов", correct: true },
    { label: "Маршрутизация — маршрут попал в неверный обработчик", misconception: "Маршрутизация выбирает обработчик. Сложность тестирования класса в изоляции — про то, как поставляются его зависимости, а не как до него добираются." },
    { label: "Сериализация — ответ Stripe слишком велик для кодирования", misconception: "Сериализация — про превращение результатов в байты. Проблема в том, что зависимость нельзя подменить в тесте." },
  ]}
/>

<Quiz
  id="mw-di-overview-q2"
  lessonSlug="01-overview"
  lang="ru"
  question="Что лучше всего описывает разницу в том, *когда* действуют middleware и DI?"
  choices={[
    { label: "Оба выполняются заново на каждом запросе", misconception: "Middleware выполняется на каждом запросе, но граф зависимостей обычно собирается один раз на старте и переиспользуется — в этом весь смысл синглтонов." },
    { label: "Middleware выполняется на каждом запросе по порядку; граф DI в основном собирается один раз на старте и переиспользуется", correct: true },
    { label: "Оба собираются один раз на старте", misconception: "Граф DI собирается на старте, но middleware выполняется на каждом отдельном запросе — он динамический, а не статический." },
    { label: "DI выполняется на каждом запросе; middleware собирается один раз", misconception: "Всё наоборот: middleware — это конвейер на каждый запрос; связывание DI — это граф времени старта." },
  ]}
/>

<DragOrder
  id="mw-di-overview-drag1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Упорядочьте события от старта процесса до обслуженного запроса:"
  items={[
    "Корень композиции связывает граф объектов (репозитории, клиенты, сервисы)",
    "Фреймворк регистрирует конвейер middleware по порядку",
    "Сервер начинает слушать соединения",
    "Приходит запрос и течёт через конвейер middleware",
    "Обработчик выполняется, используя внедрённые в него зависимости",
    "Ответ формируется и возвращается",
  ]}
/>

</Explanation>

<StackDiagram
  label="Middleware (ось запроса) vs DI-связывание (ось старта): два ортогональных паттерна"
  hue="var(--d-backend)"
  layers={[
    { label: "Ось запроса: Middleware", note: "выполняется на каждый запрос, по порядку — auth → logging → body → rate-limit → handler" },
    { label: "Ось связывания: DI-граф", note: "собирается один раз на старте — корень композиции связывает конкретные объекты" },
  ]}
  caption="Middleware — конвейер на каждый запрос (динамический, упорядоченный); DI-граф собирается один раз на старте (статический). Одно — про то, что выполняется вокруг кода; другое — про то, что код получает."
/>

<KeyTakeaway>Middleware и DI ортогональны. Middleware — это конвейер на каждый запрос, выносящий сквозные заботы (auth, логирование) в упорядоченные слои; неверный порядок — это баг безопасности/производительности. DI — это связывание времени старта, дающее классу его зависимости вместо того, чтобы он их конструировал, через корень композиции; скрытые вызовы `new` уничтожают тестируемость. Понимание того, на какой оси живёт проблема, ведёт прямо к решению.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="mw-di-overview-retrieval"
  lang="ru"
  questions={[
    { q: "В чём разница между осью запроса и осью связывания, и зачем держать их раздельно?", a: "Ось запроса — это middleware: что выполняется вокруг обработчика на каждом запросе и по порядку — auth, логирование, парсинг тела, rate limiting. Это динамично и на каждый запрос, и порядок этих слоёв — граница безопасности и производительности. Ось связывания — это внедрение зависимостей: какие объекты куску кода дают, вместо того чтобы он конструировал их сам, и это решается в основном один раз на старте. Это про связанность и тестируемость. Одно — упорядоченный конвейер, по которому путешествует запрос; другое — граф объектов, собранный до прихода любого запроса. Раздельность позволяет рассуждать о «что выполняется на каждом запросе» независимо от «от чего зависит этот класс», и большинство backend-проблем чётко относятся к одной из осей." },
    { q: "Зачем нужен middleware и какую проблему создают сквозные заботы без него?", a: "Сквозные заботы — auth, логирование, трейсинг, сжатие, обработка ошибок — нужны почти каждому эндпоинту, но не принадлежат ни одному. Без middleware каждый обработчик переизобретает эти заботы, что дублируется и неизбежно несогласованно (один эндпоинт забывает auth, другой логирует другой формат). Middleware выносит их в упорядоченные слои, оборачивающие обработчик, так что каждая забота написана один раз и применяется единообразно по маршрутам, а порядок этих слоёв становится единственным местом, решающим, что защищает обработчик." },
    { q: "Что такое инверсия управления и какую роль играет корень композиции?", a: "Инверсия управления означает, что класс объявляет, что ему нужно (отправитель, репозиторий), вместо того чтобы конструировать конкретную реализацию самому через `new`. Что-то внешнее затем решает, какую конкретную реализацию он получит. Это внешнее место — корень композиции: единственная точка рядом со стартом приложения, которая знает реальные реализации и собирает весь граф объектов. Это убирает жёсткую связанность от внутриклассовых вызовов `new`, и именно это делает классы подменяемыми на фейки в тестах, перенастраиваемыми под разные окружения и переиспользуемыми — шов тестируемости живёт ровно там, где зависимость внедряется, а не конструируется." },
  ]}
/>

<Recap lang="ru">
Под каждым backend-фреймворком лежат два ортогональных паттерна. Middleware — ось запроса: упорядоченные слои, оборачивающие обработчик и выносящие сквозные заботы — auth, логирование, парсинг тела, rate limiting — чтобы они были написаны один раз и применялись единообразно, а их порядок служил границей безопасности и производительности. Внедрение зависимостей — ось связывания: вместо того чтобы класс конструировал своих коллабораторов через `new`, он объявляет, что ему нужно, а корень композиции собирает граф объектов один раз на старте, и именно это делает код подменяемым и тестируемым. Одно — конвейер на каждый запрос; другое — граф времени старта. Фреймворки их сплавляют — контроллер NestJS разрешается через DI и сидит за конвейером guard/interceptor — но рефлекс сеньора в том, чтобы спросить, на какой оси живёт проблема. Следующие уроки уходят вглубь каждой: сперва анатомия написания корректного middleware, затем механика инверсии управления и DI-скоупов.
</Recap>
