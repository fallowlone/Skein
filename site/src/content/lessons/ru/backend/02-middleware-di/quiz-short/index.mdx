---
concepts:
- request-vs-wiring-axis
- middleware-contract
- composition-root
- injection-scopes
- test-seam
- container-resolution
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://martinfowler.com/articles/injection.html
- https://martinfowler.com/articles/mocksArentStubs.html
- https://docs.nestjs.com/fundamentals/injection-scopes
status: ready
summary: "Подсказки на свободное воспроизведение по всему модулю про middleware и DI. Сначала ответьте по памяти, затем откройте эталонный ответ и сравните."
title: 'Middleware и DI: тест на свободное воспроизведение'
track: backend
unit: 02-middleware-di
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение по памяти лучше перечитывания. Для каждой подсказки проговорите или запишите полный ответ по памяти, прежде чем открыть эталонный — именно усилие припоминания закрепляет две оси модуля в долговременной памяти.
</Hook>

<Goal lang="ru">
Восстановите хребет модуля, не подглядывая: ось запроса против оси связывания, контракт middleware, composition root, три scope внедрения и их ловушки, тестовый шов и что делает контейнер в проде.
</Goal>

<RetrievalDrawer
  client:load
  id="02-middleware-di-short-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое ось запроса и ось связывания, и почему называние оси — senior-рефлекс?", a: "Ось запроса — это middleware: что выполняется вокруг обработчика на каждом запросе, по порядку — аутентификация, логирование, парсинг тела, ограничение частоты. Это динамично, на запрос, и порядок — граница безопасности и производительности. Ось связывания — это dependency injection: какие объекты кусок кода получает, а не конструирует сам, что решается в основном один раз при старте; это про связанность и тестируемость. Одно — упорядоченный pipeline, по которому идёт запрос; другое — граф объектов, собранный до прихода любого запроса. Назвать ось первой — значит сразу указать на фикс: 'медленно на каждом эндпоинте' почти всегда pipeline middleware, а 'невозможно тестировать в изоляции' почти всегда скрытое конструирование на оси связывания." },
    { q: "Сформулируйте контракт Express-middleware и два способа его нарушить.", a: "Express-middleware (req, res, next) должен сделать ровно одно из трёх: вызвать next(), чтобы передать управление следующему слою; завершиться отправкой ответа (res.json, res.end) без вызова next, закоротив цепочку; или вызвать next(err), чтобы перейти к error-middleware. Он нарушается двумя способами. Делать оба — отправить ответ и ещё вызвать next(), или дать двум путям ответить — приводит к двойной отправке и ошибке 'Cannot set headers after they are sent'. Не делать ничего — нет ответа и нет next() — подвешивает запрос до таймаута. Error-middleware вдобавок должен объявлять четыре параметра (err, req, res, next) и регистрироваться последним, иначе Express считает его обычным middleware и никогда не направляет в него ошибки." },
    { q: "Что такое inversion of control, что делает composition root и почему service locator — антипаттерн?", a: "Inversion of control означает, что класс объявляет, что ему нужно (абстракцию вроде PaymentGateway), вместо конструирования конкретной реализации через new; конкретный экземпляр поставляет нечто внешнее. Constructor injection — честная форма: зависимости видны в сигнатуре, а отсутствующая падает при старте. Composition root — единственное место у точки входа, где собирается весь граф объектов и единственное, что называет конкретные реализации, поэтому связывание живёт на краю, а логика в середине остаётся свободной от зависимостей. Service locator тоже избегает new, но класс сам достаёт зависимости из глобального реестра (Container.get), что снова прячет их из сигнатуры и откладывает сбой на рантайм глубоко в коде. Он легитимен только внутри фреймворка или composition root, а не разбросан по бизнес-логике." },
    { q: "Назовите три scope DI, ловушку каждого и правило, избегающее обеих главных ловушек.", a: "Singleton создаёт один экземпляр на весь процесс, общий для всех запросов — это default, идеален для сервисов без состояния, клиентов и пулов; его ловушка — хранение состояния на запрос, что течёт между конкурентными пользователями, ведь поле общее. Request-scoped создаёт один экземпляр на запрос; его ловушка — scope bubbling: один общий потребитель не может держать зависимость на запрос, поэтому lifetime запроса всплывает вверх по цепочке внедрения, и контейнер пересоздаёт большой кусок графа на каждом запросе, добавляя аллокации и латентность. Transient создаёт свежий экземпляр на каждое внедрение и не всплывает, ведь singleton может безопасно держать один transient. Правило: по умолчанию singleton без состояния и передавать данные на запрос аргументами методов; повышать до request scope только когда идентичность на запрос действительно нельзя передать." },
    { q: "Что такое тестовый шов и в чём разница между fake и mock?", a: "Шов — это точка внедрения, где продакшен-связывание заменяется на тестовое; он существует именно потому, что класс получает коллаборатора (PaymentGateway), а не конструирует StripeClient, так что тест может подставить заменитель. Fake или stub предоставляет реалистичное состояние — например репозиторий на in-memory Map, куда можно сохранить и прочитать обратно — и проверки касаются результата (заказ сохранён, сумма верна), поэтому он ломается только при реальной смене поведения. Mock запрограммирован ожиданиями о вызовах и проверяет взаимодействие (charge вызван один раз с этой суммой); поскольку mock-ожидание само является утверждением о реализации, он ломается при любой смене формы вызова, даже когда поведение сохранено. Дисциплина: мокать границы, которыми не владеете (платежи, почта, часы), подделывать то, чем владеете, и читать 'нужно десять моков' как запах дизайна." },
    { q: "Что делает DI-контейнер под капотом, почему циклы его ломают и почему предпочитать eager-старт?", a: "Контейнер читает объявления зависимостей, строит направленный граф и инстанцирует в порядке зависимостей через топологическую сортировку — сначала листья, потом их потребители — кэшируя каждый singleton, чтобы поздние потребители получали тот же экземпляр. Это механизированный composition root. Топологическая сортировка существует только для ациклического графа, поэтому у цикла (A нужен B, B нужен A) нет порядка 'сначала листья', и контейнер отказывается; он может проходить в dev и падать в проде, потому что резолвинг следует порядку загрузки модулей, что делает настоящий цикл недетерминированным латентным багом. Настоящий фикс — вынести общий третий класс, а не маскировать через forwardRef. Eager-инстанцирование строит весь singleton-граф при старте, поэтому отсутствующая env-переменная или неразрешимая зависимость роняет загрузку — громко, на деплое, ловится CI — вместо тихого сбоя на первом запросе позже; серверам стоит его предпочитать, а для крошечных графов ручное связывание в composition root часто яснее контейнера вовсе." },
  ]}
/>

<Recap lang="ru">
Если вы восстановили каждый ответ по памяти, вы держите хребет модуля: сначала называйте ось; на оси запроса middleware делает ровно одно из pass/terminate/forward, а порядок — граница безопасности и стоимости; на оси связывания классы объявляют потребности, а composition root их поставляет; по умолчанию singleton без состояния, scope повышать только вынужденно; выигрыш DI — это шов, где вы подделываете то, чем владеете, и мокаете границы, которыми не владеете; а контейнер — это тот же composition root, механизированный — топологически сортирующий граф, отвергающий циклы и падающий быстро при старте.
</Recap>
