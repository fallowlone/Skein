---
concepts:
- middleware-ordering
- dependency-injection
- injection-scopes
- test-doubles
- circular-dependency
- composition-root
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://docs.nestjs.com/fundamentals/injection-scopes
- https://martinfowler.com/articles/injection.html
- https://expressjs.com/en/guide/error-handling/
status: ready
summary: "Тест с множественным выбором на синтез по всему модулю про middleware и DI — две оси, контракт middleware, scope внедрения, тестовый шов и резолвинг контейнера."
title: 'Middleware и DI: тест с множественным выбором'
track: backend
unit: 02-middleware-di
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, проходящих сквозь весь модуль. Каждый отражает решение, которое вы принимаете на реальном бэкенде — на какой оси лежит проблема, что запрещает контракт, какой lifetime должен быть у объекта — а не определение для пересказа.
</Hook>

<Goal lang="ru">
Убедитесь, что вы можете разместить проблему на правильной оси (запрос против связывания), рассуждать о порядке middleware и контракте ответа, выбрать scope внедрения и прочитать сбой контейнера — тот синтез, к которому вели шесть уроков.
</Goal>

<Quiz
  id="02-middleware-di-choice-1"
  lessonSlug="02-middleware-di"
  lang="ru"
  question="Обработчик медленный на каждом без исключения эндпоинте, и отдельно — биллинговый класс невозможно юнит-тестировать без обращения к реальному Stripe API. На какой оси лежит каждая проблема?"
  choices={[
    { label: "Равномерная медлительность на запрос — это проблема middleware (ось запроса); нетестируемый класс — проблема DI (ось связывания): что-то выполняется на каждом запросе против класса, который сам конструирует свою зависимость.", correct: true },
    { label: "Обе — проблемы DI, потому что обе касаются сервисов.", misconception: "Только нетестируемый класс — это вопрос связывания. Медлительность на каждом эндпоинте указывает на что-то в pipeline на запрос — на ось запроса, а не на то, как сконструирован класс." },
    { label: "Обе — проблемы middleware, ведь обе проявляются во время запроса.", misconception: "Проблема тестируемости существует при конструировании, решается один раз при старте и не зависит от запроса. Это вопрос оси связывания (DI), а не middleware." },
    { label: "Ни то ни другое — обе про маршрутизацию.", misconception: "Маршрутизация лишь выбирает обработчик. Равномерная медлительность — это pipeline вокруг обработчиков; нетестируемость — скрытое конструирование. Senior-рефлекс — сначала назвать ось." },
  ]}
/>

<Quiz
  id="02-middleware-di-choice-2"
  lessonSlug="02-middleware-di"
  lang="ru"
  question="Express-middleware отправляет res.json(...) при ошибке валидации, а затем ещё и вызывает next(). Под нагрузкой периодически возникает 'Cannot set headers after they are sent'. Какое правило нарушено?"
  choices={[
    { label: "Контракт middleware: middleware должен сделать ровно одно из — передать управление через next(), завершиться отправкой ответа или переадресовать через next(err) — но не два сразу. Ответ плюс next() позволяют ответить и второму пути — двойная отправка.", correct: true },
    { label: "Middleware зарегистрирован в неправильном порядке.", misconception: "Порядок решает, что выполняется вокруг обработчика, а не эту ошибку. Сбой — это второй ответ в один сокет, то есть и завершение, и передача управления в одном middleware." },
    { label: "Express 5 больше не поддерживает res.json().", misconception: "res.json() не менялся. Дефект — вызов next() после уже отправленного ответа, что приводит ко второй записи в завершённый ответ." },
    { label: "Тело ответа было слишком большим для сокета.", misconception: "Размер тела не вызывает эту ошибку. Это именно вторая запись после того, как ответ уже был завершён один раз." },
  ]}
/>

<Quiz
  id="02-middleware-di-choice-3"
  lessonSlug="02-middleware-di"
  lang="ru"
  question="Центральный обработчик ошибок Express, написанный как (err, req, res), никогда не получает ошибки, хотя более ранний код вызывает next(err). Почему и каков фикс?"
  choices={[
    { label: "Express определяет error-middleware по арности — он должен объявлять четыре параметра (err, req, res, next). Функция с тремя аргументами считается обычным middleware, поэтому ошибки в неё не направляются. Добавьте неиспользуемый параметр next.", correct: true },
    { label: "Error-middleware нужно регистрировать первым, до маршрутов.", misconception: "Его нужно регистрировать последним, после всех маршрутов — но не поэтому он пропускается. Express пропускает его, потому что он объявляет три параметра вместо четырёх." },
    { label: "next(err) — недопустимый вызов в Express.", misconception: "next(err) — корректный способ переадресовать ошибку. Обработчик обходится исключительно из-за того, что у него три параметра, а не четыре." },
    { label: "Ошибки можно обрабатывать только внутри маршрута, где они возникли.", misconception: "Централизованный error-middleware — стандартная практика. Единственная причина, по которой этот пропущен — арность: чтобы быть распознанным, он должен принимать четыре параметра." },
  ]}
/>

<Quiz
  id="02-middleware-di-choice-4"
  lessonSlug="02-middleware-di"
  lang="ru"
  question="Периодически пользователь A видит имя аккаунта пользователя B. Аутентификация корректна. Сервис, зарегистрированный как singleton, хранит this.currentUser на запрос. Что произошло и каков самый чистый фикс?"
  choices={[
    { label: "Singleton — это один экземпляр, общий для всех конкурентных запросов, поэтому поле currentUser — общее состояние: запрос B перезаписывает то, что установил запрос A, и A читает значение B. Фикс: держите singleton без состояния и передавайте пользователя аргументом метода.", correct: true },
    { label: "Сделать каждый сервис transient, чтобы каждый вызов получал свежий экземпляр.", misconception: "Transient создаёт множество экземпляров, но не даёт чисто идентичности на запрос и тяжелее простой передачи значения. Гонка по общему полю решается тем, что состояние запроса не хранят на singleton." },
    { label: "База данных вернула неправильную строку при конкуренции.", misconception: "Слой данных корректен. Утечка — это общее мутируемое поле на process-wide singleton, перезаписываемое конкурентными запросами, а не дефект запроса." },
    { label: "Сделать весь модуль request-scoped для безопасности.", misconception: "Это максимизирует издержки scope bubbling, пересоздавая граф модуля на каждом запросе ради значения, которое можно просто передать аргументом. Предпочтительнее singleton без состояния." },
  ]}
/>

<Quiz
  id="02-middleware-di-choice-5"
  lessonSlug="02-middleware-di"
  lang="ru"
  question="Рефакторинг, сохраняющий поведение, разбивает один repo.save() на два сохранения внутри транзакции. Итоговое состояние идентично, но сорок тестов краснеют. О чём это говорит?"
  choices={[
    { label: "Эти тесты проверяют взаимодействия — mock-ожидания на число вызовов и аргументы — а не итоговое состояние, поэтому они связаны с реализацией. Mock-ожидание само по себе является утверждением о внутренних шагах кода.", correct: true },
    { label: "Рефакторинг внёс реальный баг.", misconception: "Поведение не изменилось, итоговое сохранённое состояние идентично. Тесты сломались на форме вызовов, а не на наблюдаемом дефекте — это признак over-mocking." },
    { label: "Репозиторий вообще не следовало внедрять.", misconception: "Внедрение создало шов, сделавший тесты возможными; проблема в том, что тесты проверяют через этот шов. Проверки на основе состояния пережили бы рефакторинг." },
    { label: "Покрытие кода было слишком низким, чтобы поймать изменение.", misconception: "Покрытие было высоким — в этом и ирония. Дело в виде проверки (взаимодействие против состояния), а не в объёме покрытия." },
  ]}
/>

<Quiz
  id="02-middleware-di-choice-6"
  lessonSlug="02-middleware-di"
  lang="ru"
  question="Приложение стартует в dev, но падает в проде с 'Nest can't resolve dependencies of UserService'. AuthService теперь импортирует UserService, который уже импортировал AuthService. Как это прочитать и каков настоящий фикс?"
  choices={[
    { label: "Циклическая зависимость. Резолвинг — это топологическая сортировка, существующая только для ациклического графа, поэтому контейнер отказывается; в dev прошло лишь потому, что порядок загрузки модулей замаскировал цикл. Настоящий фикс: вынести общую логику в третий класс, от которого зависят оба. forwardRef лишь прячет проблему.", correct: true },
    { label: "Обернуть одну сторону в forwardRef и выкатить — это разрешает цикл.", misconception: "forwardRef откладывает резолвинг одной стороны, чтобы разорвать дедлок, но сохраняет дефект дизайна. Цикл почти всегда означает два класса, которые должны быть одним, или отсутствующий общий третий класс." },
    { label: "Контейнер забагован; откатите версию фреймворка.", misconception: "Контейнер корректно сообщает, что у круга нет валидного порядка конструирования. Баг — в графе зависимостей, а не в резолвере." },
    { label: "Эти два сервиса нужно перенести в один модуль.", misconception: "Границы модулей — не причина: два класса в одном модуле тоже могут зациклиться. Проблема — сам цикл зависимостей, который убирает только ациклический редизайн." },
  ]}
/>

<Recap lang="ru">
Сквозная нить модуля — одна привычка: назвать ось, затем применить её дисциплину. Ось запроса (middleware) требует одного завершения на middleware и продуманного порядка; error-middleware нужны четыре параметра. Ось связывания (DI) означает, что классы объявляют потребности, а composition root их поставляет — по умолчанию singleton без состояния, повышать scope только вынужденно, а боль тестов читать как сигнал дизайна (мокать границы, которыми вы не владеете, и подделывать то, чем владеете). Контейнер — это тот же composition root, механизированный: он топологически сортирует граф, отвергает циклы и должен падать быстро при старте. Каждый вопрос здесь сводится к этим двум осям и издержкам каждой из них.
</Recap>
