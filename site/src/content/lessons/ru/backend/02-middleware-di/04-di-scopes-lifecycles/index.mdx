---
slug: 04-di-scopes-lifecycles
lang: ru
track: backend
unit: 02-middleware-di
order: 4
title: "Скоупы и время жизни DI: singleton, request, transient"
summary: "У связанного объекта есть время жизни: создан один раз, по разу на запрос или на внедрение. Неверный выбор — два противоположных бага: синглтон, держащий состояние запроса, утекает данные между пользователями, а request-скоуп тихо пересоздаёт половину графа на каждом вызове."
estMin: 15
status: ready
lessonType: topic
level: middle
prereqs:
  - backend/02-middleware-di/03-inversion-of-control
deepensInto:
  - backend/02-middleware-di/05-testing-and-seams
spiral: ["latency"]
concepts: ["singleton-scope", "request-scope", "transient-scope", "scope-bubbling", "shared-mutable-state", "lifecycle"]
sources:
  - https://docs.nestjs.com/fundamentals/injection-scopes
  - https://learn.microsoft.com/en-us/dotnet/core/extensions/dependency-injection
  - https://docs.spring.io/spring-framework/reference/core/beans/factory-scopes.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Баг-репорт говорит: пользователь A иногда видит имя аккаунта пользователя B. Код auth корректен. Утечка — в поле `currentUser` на сервисе, зарегистрированном как синглтон — один экземпляр, разделяемый каждым параллельным запросом. Запрос A ставит поле, запрос B перезаписывает миллисекундой позже, и запрос A читает значение B. Никто не писал баг безопасности. Кто-то выбрал неверное время жизни.
</Hook>

<Crux>У внедрённого объекта есть время жизни; два режима отказа — синглтон, держащий состояние запроса, и request-скоуп, тихо пересоздающий граф.</Crux>

<Explanation>

## Три времени жизни

DI-контейнер может создавать экземпляр с тремя ритмами:

- **Singleton** — один экземпляр на весь процесс, создан один раз и разделяем каждым запросом. Дефолт в NestJS, Spring и большинстве контейнеров. Быстро и легко по памяти, ведь ничего не пересоздаётся.
- **Request-scoped** — один экземпляр на входящий запрос, разделяем внутри этого запроса, выброшен после. Полезен для контекста на запрос (текущий пользователь, request ID, транзакция на запрос).
- **Transient** — свежий экземпляр на каждое внедрение. Полезен для stateful-помощников, которыми вообще нельзя делиться.

## Ловушка «синглтон с состоянием»

Баг из Hook — каноническая ошибка синглтона: синглтон должен быть **stateless** относительно запроса, ведь он разделяем всеми параллельными запросами. Хранение `this.currentUser` на синглтоне — утечка данных, ждущая случиться: параллельные запросы гонятся за общим полем. Лечение — либо пустить данные на запрос через аргументы метода (передавать `user`, а не хранить), либо сделать сервис request-scoped, чтобы каждый запрос получал свой экземпляр.

## Противоположная ловушка: всплытие скоупа

Request-скоуп выглядит безопасным дефолтом, и команды тянутся к нему — и платят скрытую цену. **Скоуп всплывает вверх по цепочке внедрения**: если request-scoped провайдер внедрён в контроллер, тот контроллер тоже становится request-scoped, и всё, что зависит от *него*. Один request-scoped логгер у самого низа может «заразить» весь граф выше, так что контейнер переинстанцирует большой срез ваших сервисов на **каждом запросе** вместо переиспользования синглтонов. Это лишние аллокации, лишнее давление на GC и измеримая задержка — ради того, что часто лишь удобство.

<Inset kind="why" lang="ru">
Почему request-скоуп всплывает, а transient — нет? Всплытие скоупа — про корректность разделения. Если сервис A request-scoped (один экземпляр на запрос) и контроллер C внедряет A, то C не может быть синглтоном — единственный общий C должен был бы держать ссылку на *какой-то* A, но A разный на каждый запрос. Значит C тоже должен создаваться на запрос; время жизни запроса распространяется вверх к каждому потребителю. Transient иначе: синглтон может безопасно держать transient-зависимость, ведь transient создан свежим в момент внедрения, и синглтон просто хранит этот один экземпляр. Transient меняет, сколько экземпляров существует, а не как долго должен жить потребитель, поэтому не навязывает потребителям более короткое время жизни.
</Inset>

## Выбор времени жизни

Правило, избегающее обеих ловушек: **по умолчанию singleton и держи синглтоны stateless**; тянись к request-скоупу лишь когда тебе вправду нужна идентичность на запрос, которую нельзя передать аргументом; используй transient экономно для намеренно неразделяемых stateful-помощников. Большинство случаев «мне нужен текущий пользователь здесь» лучше решаются передачей его через вызов, чем превращением сервиса в request-scoped и утягиванием всего графа вниз.

<div data-lesson-visual class="overflow-x-auto my-6">

| Скоуп | Экземпляров | Лучше для | Ловушка |
|---|---|---|---|
| Singleton | Один на процесс | Stateless-сервисы, клиенты, пулы | Держит состояние запроса → утечка данных |
| Request | Один на запрос | Текущий пользователь, транзакция запроса | Всплывает, пересоздаёт граф на запрос |
| Transient | Один на внедрение | Неразделяемые stateful-помощники | Неожиданное число экземпляров |

</div>

<Quiz
  id="scopes-q1"
  lessonSlug="04-di-scopes-lifecycles"
  lang="ru"
  question="Синглтон-сервис хранит `this.currentUser`, и иногда один пользователь видит данные другого. Почему?"
  choices={[
    { label: "База вернула не ту строку", misconception: "Слой данных корректен. Утечка — от перезаписи общего поля синглтона параллельными запросами." },
    { label: "Синглтон разделяем всеми параллельными запросами, поэтому один запрос перезаписывает поле, которое другой запрос позже читает", correct: true },
    { label: "Request-скоуп слишком медленный", misconception: "Дело не в скорости, а в корректности. Синглтон делит состояние на запрос между пользователями." },
    { label: "Transient-скоуп продублировал пользователя", misconception: "Transient создаёт свежие экземпляры; он не вызвал бы межпользовательскую утечку. Причина — общее состояние синглтона." },
  ]}
/>

<Quiz
  id="scopes-q2"
  lessonSlug="04-di-scopes-lifecycles"
  lang="ru"
  question="Почему превращение одного низкоуровневого провайдера в request-scoped может ударить по производительности множества сервисов?"
  choices={[
    { label: "Request-скоуп отключает кеширование строк базы", misconception: "Он не трогает кеш запросов. Цена в том, что request-скоуп всплывает и навязывает переинстанцирование зависимых сервисов." },
    { label: "Скоуп всплывает вверх по цепочке внедрения, поэтому каждый потребитель request-scoped провайдера тоже становится request-scoped и пересоздаётся на запрос", correct: true },
    { label: "Request-scoped провайдеры нельзя собрать сборщиком мусора", misconception: "Их собирают после запроса; цена — в пересоздании на запрос всего зависимого подграфа, а не в утечке." },
    { label: "Он заставляет все провайдеры стать transient", misconception: "Он не превращает других в transient. Он распространяет время жизни запроса вверх к потребителям." },
  ]}
/>

<Quiz
  id="scopes-q3"
  lessonSlug="04-di-scopes-lifecycles"
  lang="ru"
  question="Синглтону нужен пользователь текущего запроса. Какое чистейшее решение избегает обеих ловушек?"
  choices={[
    { label: "Хранить пользователя на синглтоне как поле", misconception: "Это ровно ловушка утечки данных: параллельные запросы гонятся за общим полем." },
    { label: "Передавать пользователя аргументом метода, чтобы синглтон оставался stateless, вместо превращения сервиса в request-scoped", correct: true },
    { label: "Сделать каждый сервис transient", misconception: "Transient создаёт много экземпляров и всё равно не даёт идентичность на запрос чисто; передача значения проще и избегает всплытия." },
    { label: "Сделать весь модуль request-scoped", misconception: "Это максимизирует цену всплытия, пересоздавая весь граф модуля на запрос ради значения, которое можно просто передать." },
  ]}
/>

</Explanation>

<KeyTakeaway>У внедрённых объектов есть время жизни: singleton (один на процесс, дефолт), request-scoped (один на запрос) или transient (один на внедрение). Синглтон, держащий состояние запроса, утекает данные между параллельными запросами; лечение — оставаться stateless и передавать данные на запрос аргументами. Request-скоуп всплывает вверх по цепочке внедрения, поэтому один request-scoped провайдер может навязать переинстанцирование всего графа на каждом запросе. По умолчанию stateless-синглтоны; request-скоуп — лишь когда вправду нужен.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="scopes-retrieval"
  lang="ru"
  questions={[
    { q: "Каковы три времени жизни DI и для чего лучше каждое?", a: "Singleton создаёт один экземпляр на весь процесс, разделяемый каждым запросом — дефолт в NestJS, Spring и большинстве контейнеров, идеален для stateless-сервисов, клиентов и пулов, ведь ничего не пересоздаётся. Request-scoped создаёт один экземпляр на входящий запрос, разделяемый внутри запроса и выброшенный после, полезен для настоящего контекста на запрос: текущий пользователь, request ID или транзакция на запрос. Transient создаёт свежий экземпляр на каждое внедрение, полезен для stateful-помощников, которыми никогда нельзя делиться. Дефолтом должен быть singleton со stateless-сервисами; два других — исключения под конкретные нужды." },
    { q: "Почему хранение состояния на запрос в синглтоне — баг утечки данных и как это чинят?", a: "Синглтон — один экземпляр, разделяемый всеми параллельными запросами, поэтому любое его поле — общее глобальное состояние. Если он хранит this.currentUser, запрос A ставит поле, а запрос B перезаписывает мгновением позже, так что A затем читает значение B — межпользовательская утечка данных, хотя код auth корректен. Лечение — держать синглтоны stateless относительно запроса: либо пускать данные на запрос через аргументы метода (передавать пользователя, а не хранить), либо, если идентичность на запрос вправду нужна, сделать тот сервис request-scoped, чтобы каждый запрос получал свой экземпляр. Передача аргумента обычно проще и избегает цены всплытия скоупа." },
    { q: "Объясните всплытие скоупа и почему request-скоуп может быть дорогим, а transient — нет.", a: "Скоуп всплывает вверх по цепочке внедрения из-за корректности разделения: если request-scoped провайдер A внедрён в контроллер C, C не может быть синглтоном — единственный общий C должен был бы ссылаться на единственный A, но A разный на каждый запрос — поэтому C тоже становится request-scoped, и всё, что зависит от C. Один request-scoped провайдер внизу графа может потому навязать контейнеру переинстанцирование большого среза сервисов на каждом запросе, добавляя аллокации, давление на GC и задержку. Transient не всплывает: синглтон может безопасно держать transient-зависимость, ведь transient создан свежим при внедрении и затем хранится, так что transient меняет, сколько экземпляров существует, а не как долго должен жить потребитель." },
  ]}
/>

<Recap lang="ru">
У каждого объекта, который связывает контейнер, есть время жизни, и выбор — решение про корректность и цену. Singleton — один экземпляр на процесс — это дефолт и верный дом для stateless-сервисов, клиентов и пулов, но хранение на нём состояния на запрос утекает данные между параллельными пользователями, канонический баг синглтона. Request-скоуп даёт каждому запросу свой экземпляр и честно чинит настоящую идентичность на запрос, но всплывает вверх по цепочке внедрения: один request-scoped провайдер может навязать пересоздание своих потребителей, и их потребителей, на каждом запросе, меняя переиспользование на аллокации и задержку. Transient создаёт свежий экземпляр на внедрение, не навязывая потребителям более короткое время жизни. Дисциплина — по умолчанию stateless-синглтоны и передача значений на запрос аргументами, повышая скоуп лишь когда нет чище варианта. Времена жизни готовят следующую заботу: с графом, связанным и заскоупленным, как DI становится швом, делающим систему тестируемой.
</Recap>
