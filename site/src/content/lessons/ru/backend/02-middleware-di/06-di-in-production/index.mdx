---
concepts:
- resolution-graph
- topological-sort
- circular-dependency
- forward-ref
- eager-instantiation
- lazy-instantiation
- container-cost
deepensInto:
- backend/08-putting-it-together/01-overview
estMin: 17
lang: ru
lessonType: topic
level: senior
order: 6
prereqs:
- backend/02-middleware-di/05-testing-and-seams
slug: 06-di-in-production
sources:
- https://docs.nestjs.com/fundamentals/circular-dependency
- https://docs.nestjs.com/providers
- https://blog.ploeh.dk/2014/05/19/di-friendly-framework/
spiral:
- latency
status: ready
summary: 'Контейнер — резолвер графа: топологически сортирует зависимости и инстанцирует
  их один раз. Острые края — циклы, которые он не упорядочит, жадный старт, вскрывающий
  баги конфига на бусте, и реальная цена: контейнер нужен не каждому приложению.'
title: 'DI-контейнеры в продакшене: графы разрешения, циклы и когда не стоит'
track: backend
unit: 02-middleware-di
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Приложение нормально стартует в разработке и падает на продакшен-сервере с `Nest can't resolve dependencies of the UserService (?, AuthService)`. В `UserService` ничего не менялось. Изменилось то, что `AuthService` теперь импортирует `UserService`, а `UserService` уже импортировал `AuthService` — цикл. В dev модули случайно загрузились в порядке, который замаскировал это; в prod — нет. Контейнер не глючит. Он говорит вам, что не может уложить круг в линию.
</Hook>

<Crux>DI-контейнер строит граф зависимостей топологически — у цикла нет валидного порядка, жадный старт падает быстро, и у машины реальная цена.</Crux>

<Explanation>

## Контейнер — резолвер графа

Снимите декораторы — и DI-контейнер делает одно: читает объявления зависимостей, строит направленный граф и **инстанцирует в порядке зависимостей** — сперва листья, затем то, что от них зависит, вверх до корня. Это **топологическая сортировка**. Чтобы создать `OrderService(db, payments)`, он сперва должен создать `db` и `payments`; чтобы создать их, разрешает *их* зависимости, рекурсивно. Для синглтонов он делает это **один раз** и кеширует экземпляр, так что второй потребитель `db` получает тот же объект. Контейнер — автоматизированный корень композиции из начала юнита: единственное место, знающее конкретику, теперь механизированное.

## Почему циклические зависимости его ломают

Топологическая сортировка существует лишь для **направленного ациклического графа**. Если `A` зависит от `B`, а `B` от `A`, нет порядка «листья сперва»: чтобы построить `A`, нужен `B`, но чтобы построить `B`, нужен `A`. Контейнер обнаруживает цикл и отказывается — это ошибка из Hook. Фреймворки дают лазейку — `forwardRef(() => Other)` в NestJS — которая ломает дедлок, откладывая разрешение одной стороны до конца конструирования. Но лазейка — запах, а не решение: цикл почти всегда означает два класса, которым стоило бы быть одним, или недостающий третий класс, от которого должны зависеть оба. Тянуться к `forwardRef`, чтобы заглушить ошибку, сохраняет дефект дизайна.

<Inset kind="why" lang="ru">
Почему цикл иногда появляется только в продакшене? Порядок разрешения может зависеть от порядка загрузки модулей, а тот может различаться между dev-сервером с hot-reload и холодным prod-бустом или между двумя сборщиками. При настоящем цикле *некоторые* порядки случайно разрешаются (первый класс частично сконструирован, когда второй просит его и получает полусобранную ссылку), а другие кидают. Это делает цикл латентным багом, всплывающим недетерминированно — зелено локально, красно на деплое. Реальный фикс — убрать цикл: вынести общую логику, нужную обоим классам, в третий класс, от которого зависят оба, превратив круг в дерево. `forwardRef` лишь прячет, что граф не ацикличен.
</Inset>

## Жадный против ленивого: когда всплывает отказ

Контейнеры различаются тем, **когда** инстанцируют. **Жадное** инстанцирование строит весь граф синглтонов на старте, до обслуживания трафика — NestJS делает так по умолчанию. **Ленивое** инстанцирование создаёт каждый провайдер при первом использовании. Компромисс — *когда кусает плохой конфиг*: жадный старт означает, что отсутствующая env-переменная или неразрешимая зависимость роняет буст — громко, немедленно, до того как затронут пользователь. Ленивый означает, что та же мисконфигурация прячется до первого запроса, попавшего в этот путь кода, возможно часами позже, возможно лишь на одном эндпоинте. Для серверных приложений сеньорское предпочтение подавляюще **жадное**: заплати слегка медленный буст, чтобы превратить ночной рантайм-пейджер в деплой-тайм отказ, который ловит CI.

## Скрытые цены

Контейнер не бесплатен. Он добавляет **время старта** (построение графа), **налог на обучение и отладку** (стектрейсы идут через код разрешения фреймворка, а ошибки «cannot resolve» — отдельный навык чтения) и соблазн **скрытого глобального состояния** — контейнер становится местом заначки синглтонов, которые на деле просто глобалы с лишними шагами. Для маленького сервиса, CLI или serverless-функции с крошечным графом ручное связывание в корне композиции — обычные вызовы `new` в одном файле точки входа — часто *яснее и быстрее* контейнера. Контейнер окупает цену, когда граф большой, времена жизни смешаны и связывание иначе было бы расползшимся ручным месивом. «Используй DI-контейнер» — решение по масштабу, а не дефолт.

<div data-lesson-visual class="overflow-x-auto my-6">

| Забота | Жадное (старт) | Ленивое (первое использование) |
|---|---|---|
| Отсутствующая зависимость | Падает на бусте, до трафика | Падает посреди запроса, позже |
| Время буста | Медленнее (строит весь граф) | Быстрее |
| Лучше для | Долгоживущих серверов | CLI, редко задеваемых путей |
| Видимость отказа | Громко, на деплое | Тихо, в рантайме |

</div>

<Quiz
  id="prod-di-q1"
  lessonSlug="06-di-in-production"
  lang="ru"
  question="Почему DI-контейнер не может разрешить циклическую зависимость между двумя сервисами?"
  choices={[
    { label: "Циклические зависимости потребляют слишком много памяти", misconception: "Дело не в памяти. У цикла нет валидного порядка конструирования, поэтому топологическая сортировка не может продолжиться." },
    { label: "Разрешение — топологическая сортировка, существующая лишь для ациклического графа; при A→B и B→A нет порядка «листья сперва»", correct: true },
    { label: "Контейнеры умеют только синглтоны", misconception: "Контейнеры умеют несколько скоупов; проблема цикла — про упорядочивание, не про время жизни." },
    { label: "Сервисы в разных модулях", misconception: "Границы модулей не причина. Два класса в одном модуле всё равно могут зациклиться; проблема в самом цикле зависимостей." },
  ]}
/>

<Quiz
  id="prod-di-q2"
  lessonSlug="06-di-in-production"
  lang="ru"
  question="Почему серверные приложения обычно предпочитают жадное инстанцирование графа зависимостей на старте?"
  choices={[
    { label: "Жадное инстанцирование делает запросы быстрее в обслуживании", misconception: "Скорость на запрос схожа, как только синглтоны существуют; реальная выгода — когда всплывают отказы, не задержка запроса." },
    { label: "Оно превращает мисконфигурацию в громкое падение на бусте до трафика, вместо тихого рантайм-отказа при первом использовании", correct: true },
    { label: "Ленивое инстанцирование не может создавать синглтоны", misconception: "Ленивое тоже может создавать синглтоны; оно лишь откладывает создание до первого использования. Разница в тайминге отказа, не в способности." },
    { label: "Жадное инстанцирование потребляет меньше памяти", misconception: "Жадное обычно держит больше разом; мотив — быстрый отказ на старте, не экономия памяти." },
  ]}
/>

<Quiz
  id="prod-di-q3"
  lessonSlug="06-di-in-production"
  lang="ru"
  question="Когда ручное связывание в корне композиции часто предпочтительнее принятия DI-контейнера?"
  choices={[
    { label: "Всегда — контейнеры антипаттерн", misconception: "Контейнеры окупают цену на больших графах со смешанными временами жизни; они не антипаттерн, просто не бесплатны." },
    { label: "Для маленького сервиса, CLI или serverless-функции с крошечным графом, где обычные вызовы `new` в одном файле точки входа яснее машинерии контейнера", correct: true },
    { label: "Как только в приложении больше трёх классов", misconception: "Три произвольно; решение про размер графа и сложность времён жизни, не про фиксированное число классов." },
    { label: "Только когда нет синглтонов", misconception: "Синглтоны легко связать руками; решающий фактор — общая сложность графа, не наличие синглтонов." },
  ]}
/>

</Explanation>

<KeyTakeaway>DI-контейнер — резолвер графа: он топологически сортирует объявления зависимостей и инстанцирует их — синглтоны один раз, с кешем. У циклической зависимости нет валидного порядка, поэтому контейнер отказывается; `forwardRef` лишь прячет, что граф не ацикличен, а реальный фикс — разорвать цикл. Жадное инстанцирование на старте делает мисконфигурацию падающей на бусте (громко, на деплое), а не на запросе (тихо, в рантайме), что серверам стоит предпочесть. У контейнера реальные цены — время старта, налог на отладку, соблазн скрытых глобалов — поэтому для маленьких графов ручное связывание в корне композиции часто яснее. Контейнеры — решение по масштабу, не дефолт.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="prod-di-retrieval"
  lang="ru"
  questions={[
    { q: "Что на самом деле делает DI-контейнер под капотом и как он обращается с синглтонами?", a: "Контейнер читает объявления зависимостей, строит направленный граф и инстанцирует в порядке зависимостей через топологическую сортировку — сперва листья, затем их потребители, вверх до корня. Чтобы создать OrderService(db, payments), он сперва разрешает db и payments, рекурсивно разрешая их зависимости. Для синглтонов он делает это ровно один раз и кеширует экземпляр, так что каждый последующий потребитель db получает тот же объект. Концептуально это корень композиции — единственное место, знающее конкретные реализации — механизированный: вместо ручного написания вызовов new в порядке зависимостей вы объявляете, что нужно каждому классу, а контейнер вычисляет порядок и строит граф." },
    { q: "Почему циклические зависимости ломают контейнер, почему цикл может появиться только в продакшене и каков реальный фикс?", a: "Топологическая сортировка существует лишь для направленного ациклического графа. Если A зависит от B, а B от A, нет порядка «листья сперва» — построение A требует B, а построение B требует A — поэтому контейнер обнаруживает цикл и отказывается. Он может появиться только в продакшене, ведь порядок разрешения может следовать порядку загрузки модулей, который различается между dev-сервером с hot-reload и холодным prod-бустом или между сборщиками; при настоящем цикле некоторые порядки разрешаются (один класс получает полусобранную ссылку), а другие кидают, делая это недетерминированным латентным багом. forwardRef откладывает разрешение одной стороны, чтобы разорвать дедлок, но лишь прячет дефект. Реальный фикс — убрать цикл, обычно вынести общую логику, нужную обоим классам, в третий класс, от которого зависят оба, превратив круг в дерево." },
    { q: "Сравните жадное и ленивое инстанцирование и объясните, когда контейнер стоит своей цены против ручного связывания.", a: "Жадное инстанцирование строит весь граф синглтонов на старте до обслуживания трафика (дефолт NestJS); ленивое создаёт каждый провайдер при первом использовании. Ключевая разница — когда кусает плохой конфиг: жадное роняет буст на отсутствующей env-переменной или неразрешимой зависимости — громко, немедленно, на деплое, ловится CI — а ленивое прячет тот же дефект до первого запроса в этот путь, возможно часами позже на одном эндпоинте. Серверам стоит предпочесть жадное: обменять медленный буст на быстрый отказ. Но у контейнера цены — время старта, налог на отладку, ведь стектрейсы идут через код разрешения, и соблазн заначить синглтоны, которые на деле глобалы. Для маленького сервиса, CLI или serverless-функции с крошечным графом ручное связывание обычными вызовами new в одном файле корня композиции яснее и быстрее. Контейнер окупает цену лишь когда граф большой и времена жизни смешаны; использовать его — решение по масштабу, не дефолт." },
  ]}
/>

<Recap lang="ru">
Под декораторами DI-контейнер — резолвер графа: он превращает объявления зависимостей в направленный граф и инстанцирует их в топологическом порядке, строя синглтоны один раз и кешируя их — корень композиции, механизированный. Это объясняет его острейший край: у циклической зависимости нет валидного порядка конструирования, поэтому контейнер отказывается, и поскольку разрешение может следовать порядку загрузки модулей, тот же цикл может пройти в dev и упасть в prod. forwardRef откладывает одну сторону, чтобы разорвать дедлок, но лишь маскирует граф, который стоило сделать ацикличным, вынеся общий третий класс. Контейнеры также выбирают, когда строить: жадное инстанцирование на старте превращает мисконфигурацию в громкое падение на деплое, что долгоживущим серверам стоит предпочесть тихому отказу при первом запросе. И машина не бесплатна — цена старта, налог на отладку и тяга к скрытому глобальному состоянию означают, что маленький граф часто лучше связать руками в обычном корне композиции. С понятыми middleware и DI — осью запроса и осью связывания, их механикой, скоупами, швами и продакшен-краями — трек может двинуться к тому, как блокирующая и асинхронная работа формируют пропускную способность под нагрузкой.
</Recap>
