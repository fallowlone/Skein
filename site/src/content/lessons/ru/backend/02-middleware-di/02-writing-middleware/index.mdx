---
slug: 02-writing-middleware
lang: ru
track: backend
unit: 02-middleware-di
order: 2
title: "Пишем middleware: сигнатуры, next() и три модели фреймворков"
summary: "Middleware — это функция с контрактом: прочитать запрос, при необходимости подействовать, затем вызвать next() или завершить ответ. Контракт различается в Express, Koa и Fastify, и самые частые продакшен-баги — это нарушение его одними и теми же тремя способами."
estMin: 14
status: ready
lessonType: topic
level: middle
prereqs:
  - backend/02-middleware-di/01-overview
deepensInto:
  - backend/02-middleware-di/03-inversion-of-control
spiral: ["latency"]
concepts: ["middleware-signature", "next", "error-middleware", "onion-model", "fastify-hooks", "encapsulation", "double-send"]
sources:
  - https://expressjs.com/en/guide/error-handling/
  - https://fastify.dev/docs/latest/Reference/Hooks/
  - https://koajs.com/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Middleware, добавляющий request ID, работает идеально. Его копия, которая ещё и вызывает асинхронный аудит-лог, начинает кидать `Cannot set headers after they are sent` под нагрузкой — но лишь иногда. Баг не в вызове аудита. Он в том, что асинхронная ошибка ускользает из middleware, дефолтный обработчик фреймворка отвечает, а потом отвечает и исходный путь кода. Два ответа, один сокет. Контракт middleware нарушен самым частым способом из существующих.
</Hook>

<Crux>Middleware читает запрос, опционально действует, затем делает ровно одно: next() для передачи управления или завершить ответ — никогда оба.</Crux>

<Explanation>

## Сигнатура Express

В Express middleware — это `(req, res, next) => {}`. У него ровно три законных окончания:

- **Пропустить дальше** — вызвать `next()`, чтобы передать управление следующему middleware.
- **Завершить** — отправить ответ (`res.json(...)`, `res.end()`) и *не* вызывать `next()`. Это коротит цепочку.
- **Упасть** — вызвать `next(err)`, чтобы перепрыгнуть к middleware обработки ошибок.

Два режима отказа — сделать **оба** (отправить ответ *и* вызвать `next()`, ведя к двойной отправке) или **ни одного** (нет ответа, нет `next()` — запрос висит до таймаута).

## Error-middleware — другая форма

Express распознаёт middleware обработки ошибок по **арности**: оно должно объявлять **четыре** параметра, `(err, req, res, next)`. Уберите неиспользуемый `next` — и Express сочтёт его обычным middleware и никогда не направит туда ошибки — тихий, бесящий баг. Error-middleware также должно быть зарегистрировано **последним**, после всех маршрутов, потому что цепочка достигает его лишь через `next(err)` из ранних слоёв.

<Inset kind="why" lang="ru">
Почему Express 5 изменил обработку асинхронных ошибок? В Express 4 обработчик маршрута, вернувший отклонённый промис, *не* достигал error-middleware автоматически — необработанное отклонение ускользало, что и есть баг двойного ответа из Hook. Команды оборачивали каждый асинхронный обработчик в хелпер `asyncHandler`, чтобы поймать и перенаправить. Express 5 делает так, что обработчики маршрутов и middleware, возвращающие промис, автоматически вызывают `next(err)` при отклонении, так что обёртка больше не нужна. Сигнатура error-middleware с четырьмя аргументами не изменилась; автоматическим стало лишь перенаправление.
</Inset>

## Koa: асинхронная луковица

Koa заменяет колбэк с тремя аргументами на `async (ctx, next) => {}` и настоящую **луковичную модель**. Код до `await next()` выполняется на пути внутрь; код после — на пути наружу, в обратном порядке. Это делает обёрточный тайминг тривиальным:

```
async (ctx, next) => {
  const start = Date.now();      // внутрь
  await next();                  // выполнить всё внутри
  ctx.set('X-Time', `${Date.now() - start}`); // наружу
}
```

Поскольку модель на промисах, `throw` где угодно естественно распространяется к вышестоящему `try/catch`-middleware — обработка ошибок это просто middleware, оборачивающий остальное в `try/catch` вокруг `await next()`.

## Fastify: хуки, а не один примитив, плюс инкапсуляция

Fastify разбивает единую идею middleware на именованные **хуки жизненного цикла**, срабатывающие в фиксированном порядке:

`onRequest → preParsing → preValidation → preHandler → handler → onSend → onResponse`

Это даёт отклонить рано на `onRequest` (до парсинга тела — самая дешёвая точка отказа) или выполнить логику на `preHandler`, когда уже есть валидированное тело. Бо́льшая разница — **инкапсуляция**: хуки (и декораторы), зарегистрированные внутри плагина, применяются лишь к маршрутам этого плагина, не глобально. Express-middleware, напротив, протекает вниз по всей цепочке от точки монтирования. Инкапсуляция — причина, по которой большие приложения Fastify остаются ограниченными по области, тогда как большие Express-приложения накапливают глобальный middleware.

<div data-lesson-visual class="overflow-x-auto my-6">

| Модель | Сигнатура | Обёртывание | Область |
|---|---|---|---|
| Express | `(req, res, next)` | Вручную; error-mw нужно 4 аргумента | Глобально от точки монтирования |
| Koa | `async (ctx, next)` | Нативная луковица через `await next()` | Глобальный стек |
| Fastify | Именованные хуки | Хуки в фиксированных точках жизненного цикла | Инкапсулировано на плагин |

</div>

<Quiz
  id="writing-mw-q1"
  lessonSlug="02-writing-middleware"
  lang="ru"
  question="Обработчик ошибок Express `(err, req, res) => {...}` никогда не получает ошибки, хотя ранний код вызывает next(err). Почему?"
  choices={[
    { label: "Обработчики ошибок должны регистрироваться первыми", misconception: "Error-middleware должно регистрироваться последним, не первым — но это не причина, по которой его пропускают. Проблема в сигнатуре." },
    { label: "Express определяет error-middleware по арности: оно должно объявлять четыре параметра, поэтому функция с тремя аргументами считается обычным middleware", correct: true },
    { label: "next(err) недопустим в Express", misconception: "next(err) — корректный способ перенаправить ошибки. Причина, по которой здесь её не ловят, — число параметров обработчика." },
    { label: "Ошибки можно обрабатывать только внутри маршрута", misconception: "Централизованное error-middleware — стандарт. Обработчик пропускают чисто потому, что он не объявляет четыре параметра." },
  ]}
/>

<Quiz
  id="writing-mw-q2"
  lessonSlug="02-writing-middleware"
  lang="ru"
  question="Какова первопричина ошибки `Cannot set headers after they are sent` из middleware?"
  choices={[
    { label: "Тело ответа было слишком большим", misconception: "Размер тела не вызывает эту ошибку. Она вызвана записью в ответ после того, как он уже однажды отправлен." },
    { label: "Ответ был завершён дважды — например, код отправил ответ и ещё дал пути ошибки отправить другой", correct: true },
    { label: "Middleware зарегистрирован не в том файле", misconception: "Расположение файла нерелевантно. Ошибка означает, что два пути кода оба записали ответ в один запрос." },
    { label: "next() вызвали без аргументов", misconception: "Вызов next() для передачи управления — норма. Ошибка именно про ответ более одного раза." },
  ]}
/>

<Quiz
  id="writing-mw-q3"
  lessonSlug="02-writing-middleware"
  lang="ru"
  question="Почему инкапсуляция Fastify важна по сравнению с middleware Express?"
  choices={[
    { label: "Она делает хуки быстрее middleware", misconception: "Инкапсуляция — про область, а не про чистую скорость. Суть в том, где хук применяется, а не как быстро он выполняется." },
    { label: "Хуки, зарегистрированные в плагине, применяются лишь к маршрутам этого плагина, поэтому сквозная логика не протекает глобально, как middleware Express", correct: true },
    { label: "Она позволяет middleware быть асинхронным", misconception: "Поддержка async отдельна от инкапсуляции. Middleware Koa и Express тоже бывает асинхронным; инкапсуляция — про область." },
    { label: "Она убирает необходимость в обработчике", misconception: "Хуки оборачивают обработчик, не заменяют его. Инкапсуляция лишь управляет тем, какие маршруты покрывает хук." },
  ]}
/>

</Explanation>

<KeyTakeaway>Middleware должно сделать ровно одно из: вызвать next() для пропуска, завершить отправкой ответа или вызвать next(err) — никогда оба (двойная отправка → «headers already sent») и никогда ни одного (зависание). Express определяет error-middleware по арности в четыре аргумента, а Express 5 авто-перенаправляет отклонённые промисы. Koa использует нативную асинхронную луковицу через `await next()`; Fastify использует фиксированные хуки жизненного цикла плюс инкапсуляцию, так что хуки плагина не протекают глобально.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="writing-mw-retrieval"
  lang="ru"
  questions={[
    { q: "Каковы законные окончания middleware Express и два способа нарушить контракт?", a: "У middleware Express (req, res, next) три законных окончания: вызвать next() для передачи управления следующему middleware; завершить отправкой ответа (res.json, res.end) без вызова next, что коротит цепочку; или вызвать next(err) для прыжка к middleware обработки ошибок. Контракт ломается двумя способами: сделать оба — отправить ответ и ещё вызвать next, либо дать двум путям ответить — что вызывает двойную отправку и ошибку 'Cannot set headers after they are sent'; или не сделать ни одного — нет ответа и нет next() — что подвешивает запрос до таймаута." },
    { q: "Как Express распознаёт error-middleware и что изменилось в Express 5?", a: "Express распознаёт error-middleware по арности: функция должна объявлять ровно четыре параметра, (err, req, res, next). Если убрать неиспользуемый next и написать три параметра, Express сочтёт его обычным middleware и никогда не направит туда ошибки — тихий баг. Error-middleware также должно регистрироваться последним, после всех маршрутов, ведь оно достигается лишь через next(err). В Express 4 обработчик маршрута, вернувший отклонённый промис, не достигал error-middleware автоматически, поэтому команды оборачивали обработчики в asyncHandler. Express 5 это изменил: обработчики и middleware, возвращающие промис, автоматически вызывают next(err) при отклонении. Сама сигнатура с четырьмя аргументами не изменилась." },
    { q: "Сравните луковичную модель Koa с хуками и инкапсуляцией Fastify.", a: "Middleware Koa — это async (ctx, next): код до await next() выполняется на пути внутрь, а код после — на пути наружу в обратном порядке — настоящая луковица, делающая обёрточный тайминг и обработку через try/catch естественными, ведь throw распространяется к вышестоящему catch вокруг await next(). Fastify вместо этого разбивает единую идею middleware на именованные хуки жизненного цикла, срабатывающие в фиксированном порядке (onRequest, preParsing, preValidation, preHandler, handler, onSend, onResponse), позволяя отклонить уже на onRequest до парсинга тела. Главное — хуки и декораторы Fastify инкапсулированы: зарегистрированные внутри плагина, они применяются лишь к маршрутам этого плагина, тогда как middleware Express протекает вниз по всей цепочке от точки монтирования. Инкапсуляция держит большие приложения ограниченными по области." },
  ]}
/>

<Recap lang="ru">
Middleware — это функция, связанная контрактом: прочитать запрос, при необходимости подействовать, затем сделать ровно одно из — передать управление через next(), завершить отправкой ответа или перенаправить ошибку через next(err). Его нарушение порождает два классических бага — ответить дважды («headers already sent») или не ответить никогда (зависание). Express определяет error-middleware по арности в четыре параметра и, начиная с версии 5, авто-перенаправляет отклонённые промисы, делая обёртку asyncHandler устаревшей. Koa выражает ту же идею как нативную асинхронную луковицу, где логика до и после await next() выполняется внутрь и наружу. Fastify разбивает middleware на фиксированные хуки жизненного цикла и добавляет инкапсуляцию, так что хуки на уровне плагина не протекают глобально, как middleware Express. С механикой оси запроса в руках следующий урок поворачивает к оси связывания: инверсии управления и тому, как зависимости реально добираются до класса.
</Recap>
