---
concepts:
- connection-lifecycle
- max-lifetime
- idle-timeout
- keepalive
- connection-validation
- stale-connection
- minimum-idle
deepensInto:
- backend/04-pooling/05-pool-exhaustion
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 4
prereqs:
- backend/04-pooling/03-acquisition-and-timeouts
slug: 04-health-and-lifecycle
sources:
- https://github.com/brettwooldridge/HikariCP/wiki/Configuration
- https://github.com/brettwooldridge/HikariCP/wiki/Bad-Behavior:-Handling-Database-Down
- https://www.postgresql.org/docs/current/runtime-config-client.html
spiral:
- resource-limits
- failure-modes
status: ready
summary: Пулевое соединение переиспользуется часами, в чём весь смысл — но база, фаерволы
  и балансировщики все оставляют за собой право убить его под тобой. Без max-lifetime,
  вытеснения по простою и валидации пул радостно выдаёт мёртвые соединения.
title: 'Жизненный цикл соединения: протухшие соединения и как их старить'
track: backend
unit: 04-pooling
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Сервис работает нормально весь день, затем в 3 ночи — во время затишья трафика — первые утренние запросы падают с «connection reset by peer». Ничего не деплоили, база здорова. Случилось то, что за тихую ночь `wait_timeout` базы, правило простоя фаервола и балансировщик тихо сбросили долго-простаивавшие пулевые соединения, но пул не заметил. Он держал эти мёртвые сокеты и уверенно выдал один первому утреннему запросу, который записал запрос в закрытую трубу. Величайшая сила пула — держать соединения открытыми для переиспользования — это и обязательство: соединение, которое он держит, могло умереть, не сказав ему.
</Hook>

<Crux>Пулевое соединение могут тихо убить база, фаервол или LB — поэтому старь по возрасту и валидируй до выдачи, иначе раздашь мёртвые сокеты.</Crux>

<Explanation>

## Удерживаемое соединение может умереть без ведома пула

Пул держит соединения открытыми именно затем, чтобы запросы пропускали рукопожатие — но «открыто на нашей стороне» не значит «живо из конца в конец». У TCP-соединения два конца и несколько middlebox-ов, любой из которых может снести его, пока сторона пула всё ещё выглядит `ESTABLISHED`:

- **Сама база.** Postgres и MySQL применяют лимиты простоя и времени жизни на стороне сервера (`idle_in_transaction_session_timeout`, MySQL `wait_timeout` по умолчанию **8 часов**). Когда сервер закрывает backend, клиентский сокет не уведомляется, пока в него снова не запишут.
- **Фаерволы и NAT-шлюзы.** Stateful-фаерволы сбрасывают простаивающие потоки через несколько минут, чтобы вернуть место в таблице; типичный облачный NAT-таймаут простоя около **350 секунд**. После него соединение — чёрная дыра, пакеты пропадают.
- **Балансировщики и прокси.** LB перед базой (или PgBouncer) имеет свои лимиты простоя и времени жизни и перерабатывает backend-ы на деплоях.

Итог: соединение, простаивающее в пуле, может быть тихо мёртвым, и пул узнаёт это лишь когда запрос пытается его использовать и получает reset или зависание.

## Три контроля держат пул свежим

Хороший пул защищается от протухания тремя кооперирующими настройками:

- **Max lifetime.** Уволь и замени каждое соединение после фиксированного возраста (HikariCP `maxLifetime`, дефолт **30 минут**) *независимо от того, выглядит ли оно здоровым*. Это проактивно ротирует соединения до того, как middlebox-ы или сервер их убьют. Важное правило: **maxLifetime должен быть короче собственного таймаута соединения базы** (напр. на несколько секунд под MySQL `wait_timeout`), чтобы пул всегда закрывал соединение до сервера.
- **Idle timeout.** Сжимай пул назад к минимуму в тихие периоды, вытесняя соединения, простаивающие дольше `idleTimeout` (дефолт **10 минут**). Это делает что-то лишь если `minimumIdle` задан ниже максимума — иначе пул держит все соединения вечно.
- **Keepalive / валидация.** Периодически прощупывай простаивающие соединения (`keepaliveTime`) и/или тестируй соединение при взятии до выдачи. Современный пул валидирует лёгким пингом на уровне протокола; если он провалится, соединение тихо выбрасывается и создаётся свежее — поэтому *запрос* никогда не видит мёртвый сокет.

## Валидируй в правильный момент, дёшево

У валидации есть цена: проверка на каждом взятии добавляет круговой обмен к каждому запросу, что может стереть выигрыш задержки, ради которого пул существует. Современный компромисс — **валидировать при взятии, но пропускать проверку, если соединение использовалось совсем недавно** — `aliveBypassWindow` HikariCP в **500 мс** значит, что соединение, возвращённое и снова взятое в пределах полусекунды, доверяется без прощупывания, по рассуждению, что оно не могло протухнуть так быстро. Это держит горячие соединения быстрыми, всё ещё ловя те, что простояли достаточно долго, чтобы быть под риском.

<Inset kind="why" lang="ru">
Зачем уволять соединения по `maxLifetime` проактивно, а не просто валидировать при взятии? Потому что валидация ловит лишь соединение, *уже мёртвое* в момент взятия — она ничего не делает для соединения, умирающего *после* выдачи, посреди запроса, когда фаервол наконец сбрасывает долгоживущий поток или база перерабатывает backend. Проактивная ротация по времени жизни атакует корень: она гарантирует, что ни одно соединение не живёт достаточно долго, чтобы вообще достичь этих внешних лимитов, поэтому опасная смерть в использовании становится исчезающе редкой. Два контроля дополняют, не дублируют — валидация обрабатывает соединение, протухшее простаивая в пуле, а maxLifetime обрабатывает соединение, протухшее бы в удержании запросом. Есть и польза стабильности: ротация соединений ровно размазывает цену переподключения по времени, а не даёт всему пулу состариться вместе и затем переподключиться стадом, когда база наконец закроет их все разом. Ограничение возраста соединения — та же дисциплина, что ограничение очереди ожидания — ограничиваешь ресурс намеренно, а не даёшь внешней системе ограничить его за тебя в худший момент.
</Inset>

<div data-lesson-visual class="overflow-x-auto my-6">

| Контроль | Что предотвращает | Ключевое ограничение |
|---|---|---|
| maxLifetime (~30 мин) | Соединение убито БД/фаерволом в удержании | Должен быть < wait_timeout БД |
| idleTimeout (~10 мин) | Удержание лишних простаивающих соединений вне пика | Действует лишь если minimumIdle < max |
| keepalive-прощуп | Простаивающий поток сброшен NAT/фаерволом | Интервал < лимита простоя middlebox |
| валидация при взятии | Выдача мёртвого сокета запросу | Пропуск в пределах aliveBypassWindow (500 мс) |

</div>

<Quiz
  id="lifecycle-q1"
  lessonSlug="04-health-and-lifecycle"
  lang="ru"
  question="После ночного затишья первые утренние запросы падают с 'connection reset', хотя ничего не деплоили и база здорова. Что случилось?"
  choices={[
    { label: "У базы за ночь кончилось место на диске", misconception: "Сбой хранилища не подразумевается; база здорова — сами соединения были тихо сброшены, пока простаивали." },
    { label: "Простаивающие пулевые соединения убиты wait_timeout БД, фаерволом или LB во время затишья, но пул держал мёртвые сокеты и выдал один", correct: true },
    { label: "Размер пула был мал для утреннего трафика", misconception: "Размер не при чём; удерживаемые соединения были мёртвы, не недостаточны. Первый запрос упал на протухшем сокете, не на полном пуле." },
    { label: "TLS-сертификаты истекли в полночь", misconception: "Истечение сертификата не подразумевается; сбой — протухшие TCP-соединения, сброшенные middlebox-ами в простой, не auth." },
  ]}
/>

<Quiz
  id="lifecycle-q2"
  lessonSlug="04-health-and-lifecycle"
  lang="ru"
  question="Почему maxLifetime должен быть короче собственного таймаута соединения базы (напр. MySQL wait_timeout)?"
  choices={[
    { label: "Чтобы пул всегда закрывал и заменял соединение до того, как сервер его убьёт, избегая протухших сокетов, о смерти которых пул не знает", correct: true },
    { label: "Чтобы запросы бежали быстрее на свежих соединениях", misconception: "Возраст соединения не меняет скорость запроса; суть в том, чтобы уволить соединения до того, как сервер закроет их без предупреждения." },
    { label: "Потому что база отвергает долгоживущие соединения при логине", misconception: "Долгоживущие соединения разрешены; сервер закрывает их по таймауту простоя/жизни, что пул должен опередить." },
    { label: "Чтобы снизить число соединений ниже max_connections", misconception: "maxLifetime ротирует соединения, не снижает число; ограничение — упорядочить увольнение пула до закрытия на стороне сервера." },
  ]}
/>

<Quiz
  id="lifecycle-q3"
  lessonSlug="04-health-and-lifecycle"
  lang="ru"
  question="Почему современные пулы валидируют при взятии, но пропускают проверку в коротком окне вроде aliveBypassWindow (500 мс)?"
  choices={[
    { label: "Потому что валидация навсегда отключает соединение", misconception: "Валидация не отключает соединения; она выбрасывает лишь те, что провалили прощуп, и заменяет их." },
    { label: "Валидация на каждом взятии добавляет круговой обмен на запрос; соединение, взятое снова в пределах 500 мс, не могло протухнуть, поэтому пропуск держит горячие соединения быстрыми", correct: true },
    { label: "Потому что соединения старше 500 мс всегда мертвы", misconception: "Возраст 500 мс не значит смерть; окно ровно обратное — соединения, использованные так недавно, доверяются как живые." },
    { label: "Чтобы сделать пул больше во время всплесков", misconception: "Окно обхода про цену валидации, не размер пула; оно не меняет, сколько соединений существует." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Жизненный цикл соединения: как пул держит соединения свежими"
  hue="var(--d-backend)"
  perRow={4}
  nodes={[
    {id:"idle",label:"idle",sub:"в пуле"},
    {id:"inuse",label:"in-use",sub:"запрос идёт"},
    {id:"stale",label:"stale",sub:"убито БД/FW"},
    {id:"retire",label:"retire",sub:"maxLifetime"},
    {id:"validate",label:"validate",sub:"при взятии"},
    {id:"fresh",label:"fresh conn",sub:"переподключение"}
  ]}
  edges={[
    {from:"idle",to:"inuse",label:"checkout"},
    {from:"inuse",to:"idle",label:"return"},
    {from:"idle",to:"stale",label:"БД/FW убивает"},
    {from:"idle",to:"retire",label:"возраст > maxLifetime"},
    {from:"idle",to:"validate",label:"проба при взятии"},
    {from:"stale",to:"retire",label:"обнаружено"},
    {from:"validate",to:"retire",label:"проба упала"},
    {from:"retire",to:"fresh",label:"заменить"}
  ]}
  caption="Соединения циклируют между idle и in-use (счастливый путь). Три контроля перехватывают несчастливый: maxLifetime убирает соединения по возрасту; валидация при взятии отбрасывает мёртвые сокеты; пул заменяет оба свежим соединением, чтобы запрос никогда не видел протухшего."
/>

<KeyTakeaway>Держание соединений открытыми для переиспользования — вся ценность пулинга и его обязательство: база (MySQL wait_timeout по умолчанию 8 часов), stateful-фаерволы и NAT (часто ~350 с простоя) и балансировщики все могут убить пулевое соединение, пока сторона пула всё ещё выглядит ESTABLISHED, поэтому пул радостно одалживает мёртвый сокет и запрос падает с reset. Три кооперирующих контроля держат его свежим: maxLifetime (~30 мин) проактивно увольняет соединения и должен быть короче собственного таймаута БД, чтобы пул закрывал первым; idleTimeout (~10 мин) сжимает пул вне пика, но действует лишь когда minimumIdle ниже максимума; и валидация при взятии плюс keepalive-прощупы ловят уже-мёртвые сокеты — пропуская проверку в пределах aliveBypassWindow (500 мс), чтобы держать горячие соединения быстрыми. Проактивная ротация и валидация дополняют друг друга: одна обрабатывает соединения, умирающие в простое, другая — умирающие бы в удержании.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="lifecycle-retrieval"
  lang="ru"
  questions={[
    { q: "Почему пулевое соединение может быть мёртвым, хотя пул думает, что оно открыто?", a: "Потому что у TCP-соединения два конца и несколько middlebox-ов между ними, и любой из них может снести его, пока локальный сокет пула выглядит ESTABLISHED. База применяет лимиты простоя и времени жизни на стороне сервера — у Postgres есть idle_in_transaction_session_timeout, у MySQL wait_timeout по умолчанию 8 часов — и когда сервер закрывает свой backend, клиент не уведомляется, пока в него снова не запишут. Stateful-фаерволы и NAT-шлюзы сбрасывают простаивающие потоки через минуты, чтобы вернуть место в таблице, с облачными NAT-таймаутами простоя обычно около 350 секунд, после чего пакеты просто пропадают. Балансировщики и прокси перед базой имеют свои лимиты простоя и жизни и перерабатывают backend-ы на деплоях. Так что соединение, простаивающее в пуле, может быть тихо мёртвым, и пул узнаёт это лишь когда запрос пытается его использовать и получает reset или зависание — вот почему первые запросы после тихого периода — классические жертвы." },
    { q: "Какие три контроля держат соединения пула свежими и какое ключевое ограничение у каждого?", a: "Первый, max lifetime: уволь и замени каждое соединение после фиксированного возраста — maxLifetime HikariCP по умолчанию 30 минут — независимо от видимого здоровья, проактивно ротируя соединения до того, как middlebox-ы или сервер их убьют. Его ключевое ограничение — maxLifetime должен быть короче собственного таймаута соединения базы, например на несколько секунд под MySQL wait_timeout, чтобы пул всегда закрывал соединение до сервера. Второй, idle timeout: сжимай пул к минимуму в тихие периоды, вытесняя соединения, простаивающие дольше idleTimeout, дефолт 10 минут — но это делает что-то лишь если minimumIdle задан ниже максимума, иначе пул держит каждое соединение вечно. Третий, keepalive и валидация: периодически прощупывай простаивающие соединения и тестируй соединение при взятии до выдачи, лёгким пингом на уровне протокола; провалившее тихо выбрасывается и заменяется, чтобы запрос не видел мёртвый сокет. Интервал keepalive должен быть короче лимита простоя middlebox, против которого он защищает." },
    { q: "Зачем уволять соединения по maxLifetime проактивно вместо опоры на одну валидацию?", a: "Потому что валидация ловит лишь соединение, уже мёртвое в момент взятия — она ничего не делает с соединением, умирающим после выдачи, посреди запроса, когда фаервол наконец сбрасывает долгоживущий поток или база перерабатывает backend. Проактивная ротация по времени жизни атакует корень, гарантируя, что ни одно соединение не живёт достаточно долго, чтобы достичь этих внешних лимитов, поэтому опасная смерть в использовании становится исчезающе редкой. Два дополняют, не дублируют: валидация обрабатывает соединение, протухшее простаивая в пуле, а maxLifetime обрабатывает то, что протухло бы в удержании запросом. Ровная ротация также размазывает цену переподключения по времени вместо того, чтобы дать всему пулу состариться вместе и переподключиться стадом, когда база закроет их все разом. Чтобы держать валидацию дешёвой, современные пулы валидируют при взятии, но пропускают прощуп, если соединение использовалось в коротком окне вроде aliveBypassWindow HikariCP в 500 миллисекунд, по рассуждению, что соединение, взятое так быстро, не могло протухнуть, что держит горячие соединения быстрыми, всё ещё ловя простаивающие под риском." },
  ]}
/>

<Recap lang="ru">
Переиспользование, делающее пулинг быстрым, делает его и хрупким: соединение, которое пул держит, может быть тихо убито базой (MySQL wait_timeout по умолчанию 8 часов), stateful-фаерволами и NAT, сбрасывающими простаивающие потоки через минуты, или балансировщиками, перерабатывающими backend-ы, всё пока сокет пула выглядит ESTABLISHED — поэтому он одалживает мёртвое соединение и запрос падает с reset, классически первый запрос после тихой ночи. Три контроля держат пул свежим: maxLifetime (~30 мин) проактивно ротирует соединения и должен быть короче собственного таймаута базы, чтобы пул всегда закрывал первым; idleTimeout (~10 мин) сжимает пул вне пика, но лишь когда minimumIdle ниже максимума; и валидация при взятии плюс keepalive-прощупы ловят уже-мёртвые сокеты, пропуская проверку в окне aliveBypassWindow 500 мс, чтобы горячие соединения оставались быстрыми. Проактивная ротация и валидация дополняют друг друга — одна ловит соединения, умирающие в простое, другая те, что умерли бы в использовании — и ровная ротация также избегает переподключения стадом. Свежие, ограниченные, валидированные соединения держат счастливый путь здоровым; следующий урок встречает то, что случается, когда они кончаются по неправильной причине: утечки, исчерпание и метрики, ловящие их раньше пользователей.
</Recap>
