---
concepts:
- pool-exhaustion
- connection-leak
- try-finally
- leak-detection
- async-boundary
- pool-metrics
- transaction-scope
deepensInto:
- backend/04-pooling/06-pooling-distributed
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 5
prereqs:
- backend/04-pooling/04-health-and-lifecycle
slug: 05-pool-exhaustion
sources:
- https://github.com/brettwooldridge/HikariCP/wiki/Configuration
- https://vladmihalcea.com/connection-leak-detection-hikaricp/
- https://github.com/brettwooldridge/HikariCP/wiki/About-Pool-Sizing
spiral:
- resource-limits
- failure-modes
status: ready
summary: Частейший сбой пула — не недоразмер, а утечка, где код берёт соединение и
  никогда не возвращает. Каждая утечка навсегда сжимает пул, пока не останется ничего,
  и инстинкт расширить пул лишь оттягивает тот же отказ.
title: 'Исчерпание пула: утечки и почему больший пул не спасёт'
track: backend
unit: 04-pooling
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Сервис работает часами, затем медленно каждый запрос начинает таймаутить на «unable to acquire connection». Рестарт чинит — на несколько часов, затем возвращается по расписанию. Это не нагрузка; это **утечка**. Где-то путь кода берёт соединение и на определённой ветке — обычно ветке ошибки — никогда не возвращает. Каждый раз, как та ветка бежит, пул навсегда теряет одно соединение. Пул на 20 переживает 20 утечек и затем мёртв, сколь бы мал ни был трафик. Фикс, к которому все тянутся первым — сделать пул больше — лишь меняет утечку с 3-часового отказа на 6-часовой.
</Hook>

<Crux>Утечка сжимает пул по одному взятию, пока он не пуст; больший пул лишь оттягивает ту же смерть — фикс в том, чтобы гарантировать возврат.</Crux>

<Explanation>

## Утечка — это взятие без возврата

Контракт пула прост: каждое взятие должно быть уравновешено возвратом. **Утечка** — нарушение контракта: соединение взято и затем на каком-то пути никогда не освобождено. Классический виновник — путь ошибки, пропускающий очистку:

```js
const conn = await pool.acquire();
const rows = await conn.query(sql);   // бросает здесь
conn.release();                       // недостижимо — утекло
```

Когда `query` бросает, исполнение прыгает мимо `release()`, и то соединение ушло из пула навсегда. Пул не знает, что заёмщик его бросил; с его точки зрения соединение всё ещё «взято, в использовании». Каждый прогон той ветки ошибки убирает ещё одно соединение из обращения. Вот почему утечка выглядит медленной бомбой: трафик нормален, затем за часы доступное число тикает вниз к нулю, и *каждый* запрос начинает падать — симптом идентичен массовому перегрузу, но причина — несколько строк кода на невезучей ветке.

Фикс структурный, не большие числа: гарантируй освобождение через `try/finally` (или языковую конструкцию, делающую то же — `using`, `defer`, контекст-менеджер, скоупнутую транзакцию фреймворка):

```js
const conn = await pool.acquire();
try {
  return await conn.query(sql);
} finally {
  conn.release();   // бежит на успехе И на броске
}
```

## Почему больший пул не чинит утечку

Рефлекс, когда соединения кончаются — поднять размер пула. Против утечки это хуже, чем бесполезно — оно превращает быстрый, очевидный отказ в медленный, загадочный, и не останавливает кровотечение. Здесь есть глубже, контринтуитивный результат: даже меры *устойчивости* показывают резко убывающую отдачу против утечек. Исследование влияния утечек нашло, что подъём пула с **5 до 100** соединений — 20× рост — улучшил скорость снижения отказов лишь с **96,8% до 62,8%** — суть в том, что бросать соединения в проблему покупает куда меньше, чем намекает рост размера, потому что ровная скорость утечки осушает любой пул; ты лишь поменял, как долго до пустоты. Единственный реальный фикс — перестать течь и *обнаруживать* утечки рано.

<Inset kind="why" lang="ru">
Почему расширение пула даёт такую плохую отдачу против утечки, когда это естественнейший первый ответ? Потому что утечка — это *скорость*, не фиксированная цена — каждое исполнение багнутой ветки убирает соединение навсегда, поэтому пул осушается со скоростью, заданной тем, как часто та ветка бежит, не размером пула. Больший пул — просто большее ведро с той же дырой: дольше пустеть, но опустеет. Хуже, большее ведро прячет дыру. С пулом на 5 утечка всплывает за минуты и указывает прямо на свежее изменение; с пулом на 100 она всплывает часами позже, задолго после деплоя, выглядя случайным перегрузом и отправляя тебя искать не там. Так большой пул стоит дважды — он не предотвращает отказ и уничтожает сигнал, что дал бы найти причину. Та же логика к повторам и прочим ручкам устойчивости поверх утечки: они размазывают отказ по времени, не адресуя, что ресурс убегает быстрее, чем возвращается. Дисциплина — ограничить *причину*, гарантировать возврат, а не раздувать буфер, оттягивающий симптом.
</Inset>

## Обнаруживай утечки и следи за правильными метриками

Поскольку утечки тихи до катастрофы, защита — наблюдаемость:

- **Порог обнаружения утечки.** Пул может предупредить, когда соединение удерживается дольше, чем должен любой легитимный запрос (HikariCP `leakDetectionThreshold`, напр. **2 с**). Соединение, вне дольше этого, почти наверняка утекло или застряло на патологически медленной операции — в любом случае хочешь знать, со стектрейсом того, кто его взял.
- **Четыре датчика пула.** Следи за **active** (в использовании), **idle** (свободно), **total** и **waiting** (потоков в очереди за соединением). Здоровый пул держит idle > 0 большую часть времени. Утечка показывается как active, лезущий вверх и не падающий назад; исчерпание показывается как idle, пришпиленный на 0, и waiting, лезущий вверх. Алертуй на **idle около 0** и **waiting > 0** устойчиво — они предшествуют отказу.

## Ловушка async-границы

Тонкая современная причина: удержание пулевого соединения через `await` чего-то *иного*, чем база. Если взять соединение и затем `await` медленного внешнего HTTP-вызова до запроса, ты держишь дефицитное соединение простаивающим на длительность вызова — не утекло, но *захоменено*. Под нагрузкой это исчерпывает пул прямо как утечка, потому что эффективная конкурентность теперь ограничена самым медленным, через что ты держишь соединение. Правило: **бери соединение как можно позже, держи его лишь на работу с базой и никогда не оборачивай несвязанный сетевой вызов внутрь взятия.**

<div data-lesson-visual class="overflow-x-auto my-6">

| Симптом | Active | Idle | Waiting | Вероятная причина |
|---|---|---|---|---|
| Здоров | Меняется | > 0 | 0 | Нормальная работа |
| Утечка | Лезет, не падает | → 0 | Лезет | Взятие без возврата на каком-то пути |
| Реальный перегруз | На максимуме | 0 | Высоко | Пул реально мал для нагрузки |
| Хоминг | Высоко | ~0 | Лезет | Соединение держится через несвязанный await |

</div>

<Quiz
  id="exhaustion-q1"
  lessonSlug="05-pool-exhaustion"
  lang="ru"
  question="Сервис таймаутит на взятии соединения после часов работы; рестарт чинит на несколько часов, затем повторяется. Трафик нормален всё время. Вероятнейшая причина?"
  choices={[
    { label: "Пул мал для трафика", misconception: "Трафик нормален, и рестарт временно чинит — это указывает на ресурс, осушаемый со временем, не на постоянное несоответствие размера." },
    { label: "Утечка соединения: какой-то путь кода берёт соединение и не возвращает, поэтому пул тикает вниз к пустоте, пока рестарт его не сбросит", correct: true },
    { label: "База отвергает логины", misconception: "Логины не падают; приложение не может даже взять из своего пула, и рестарт, сбрасывающий его, — подпись утечки, не отвержения сервером." },
    { label: "TLS-сессии истекают", misconception: "Сбой рукопожатия не подразумевается; постепенное осушение, излечимое рестартом, — паттерн утечки, не истечение сертификата или сессии." },
  ]}
/>

<Quiz
  id="exhaustion-q2"
  lessonSlug="05-pool-exhaustion"
  lang="ru"
  question="Почему расширение пула — плохой фикс утечки соединения?"
  choices={[
    { label: "Большие пулы всегда кладут базу", misconception: "Суть не в падении БД; суть в том, что утечка всё равно осушает любой пул — размер лишь меняет график." },
    { label: "Утечка осушает пул со скоростью, поэтому больший пул лишь дольше пустеет и ещё прячет причину, оттягивая симптом за деплой", correct: true },
    { label: "Большие пулы отключают обнаружение утечек", misconception: "Размер пула не выключает обнаружение утечек; проблема в том, что больше соединений лишь откладывают то же исчерпание." },
    { label: "Это делает отдельные запросы медленнее", misconception: "Скорость запроса не механизм; проблема в том, что скорость утечки опустошает пул независимо от размера." },
  ]}
/>

<Quiz
  id="exhaustion-q3"
  lessonSlug="05-pool-exhaustion"
  lang="ru"
  question="Почему удержание пулевого соединения через await несвязанного HTTP-вызова опасно, даже когда ничего не утекает?"
  choices={[
    { label: "HTTP-вызов портит соединение с базой", misconception: "Несвязанный вызов не портит соединение; вред — в удержании его простаивающим, не в повреждении." },
    { label: "Соединение захоменено простаивающим на длительность вызова, поэтому под нагрузкой эффективная конкурентность ограничена медленным вызовом и пул исчерпывается как при утечке", correct: true },
    { label: "Await всегда освобождает соединение автоматически", misconception: "Await не освобождает соединение; в этом и проблема — оно остаётся взятым через ожидание." },
    { label: "Это навсегда удваивает требование к размеру пула", misconception: "Оно не меняет настроенный размер; оно тратит имеющиеся соединения, держа их простаивающими через несвязанную работу." },
  ]}
/>

<DragOrder
  id="exhaustion-drag1"
  lessonSlug="05-pool-exhaustion"
  lang="ru"
  prompt="Упорядочи, как утечка соединения становится полным отказом:"
  items={[
    "Путь ошибки берёт соединение и пропускает освобождение",
    "Каждый прогон того пути навсегда убирает одно соединение из пула",
    "Active лезет вверх и не возвращается; idle тикает к нулю",
    "Пул пустеет, и каждый запрос таймаутит на взятии соединения",
  ]}
/>

</Explanation>

<FlowDiagram
  label="Как утечка соединения осушает пул до исчерпания"
  hue="var(--d-backend)"
  perRow={4}
  nodes={[
    {id:"borrow",label:"взятие",sub:"acquire()"},
    {id:"error",label:"путь ошибки",sub:"исключение"},
    {id:"leaked",label:"утечка конн",sub:"не возвращено"},
    {id:"shrink",label:"пул сжимается",sub:"active++, idle--"},
    {id:"empty",label:"пул пуст",sub:"idle = 0"},
    {id:"timeout",label:"всё таймаутит",sub:"каждый запрос"}
  ]}
  edges={[
    {from:"borrow",to:"error",label:"запрос бросает"},
    {from:"error",to:"leaked",label:"пропускает release()"},
    {from:"leaked",to:"shrink",label:"каждый прогон"},
    {from:"shrink",to:"empty",label:"повторяется"},
    {from:"empty",to:"timeout",label:"conn нет"}
  ]}
  caption="Каждый прогон пути ошибки берёт соединение и пропускает release(), навсегда убирая его. Пул тикает к idle = 0 за часы, пока каждый запрос не таймаутит на взятии — симптом идентичен перегрузу, причина иная."
/>

<KeyTakeaway>Частейший сбой пула — утечка, не недоразмер: взятие, пропускающее возврат на каком-то пути — классически ветка ошибки, прыгающая мимо release() — навсегда убирает соединение, поэтому пул тикает к пустоте за часы и каждый запрос падает как при массовом перегрузе, пока трафик нормален, излечимо лишь рестартом. Структурный фикс — гарантировать освобождение через try/finally или эквивалентную скоупнутую конструкцию, никогда не больший пул — исследование показало, что подъём пула 5→100 (20×) купил куда меньше устойчивости, чем намекает размер, потому что утечка осушает любой пул со своей скоростью, и больший пул ещё прячет причину, оттягивая симптом за деплой. Защищайся обнаружением утечки (предупреждение, когда соединение удерживается дольше ~2 с, со стектрейсом) и четырьмя датчиками — active, idle, total, waiting — алертуя на idle около 0 и waiting устойчиво выше 0. Также избегай хоминга: никогда не держи соединение через await несвязанной работы.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="exhaustion-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое утечка соединения и как она производит отказ, выглядящий перегрузом?", a: "Утечка — нарушение базового контракта пула, что каждое взятие должно быть уравновешено возвратом: соединение взято и затем на каком-то пути никогда не освобождено. Классический виновник — путь ошибки, пропускающий очистку: код вызывает acquire, гонит запрос, который бросает, и исполнение прыгает мимо вызова release, поэтому то соединение ушло из пула навсегда, пока пул всё ещё верит, что оно взято и в использовании. Каждый прогон той ветки убирает ещё одно соединение из обращения, поэтому пул на 20 переживает 20 утечек и затем мёртв независимо от того, сколь мал трафик. Симптом — медленная бомба: трафик остаётся нормальным, затем за часы доступное число тикает вниз к нулю и каждый запрос начинает падать на взятии соединения — идентично по виду массовому перегрузу, но вызвано несколькими строками на невезучей ветке, вот почему рестарт временно лечит. Фикс структурный: гарантируй освобождение через try/finally или эквивалентную скоупнутую конструкцию вроде using, defer, контекст-менеджера или скоупнутой транзакции фреймворка." },
    { q: "Почему увеличение пула — плохой ответ на утечку?", a: "Потому что утечка — скорость, не фиксированная цена: каждое исполнение багнутой ветки убирает соединение навсегда, поэтому пул осушается со скоростью, заданной тем, как часто та ветка бежит, не тем, сколь велик пул. Больший пул — то же ведро с той же дырой — дольше пустеет, но пустеет так же. Исследование влияния утечек нашло, что увеличение пула с 5 до 100 соединений, двадцатикратное, улучшило скорость снижения отказов куда меньше, чем подразумевает смена размера, потому что ровная скорость утечки побеждает любой пул. Хуже, большее ведро прячет дыру: с малым пулом утечка всплывает за минуты и указывает прямо на свежее изменение, тогда как с большим пулом она всплывает часами позже, задолго после деплоя, выглядя случайным перегрузом и отправляя искать не там. Так большой пул стоит дважды — он не предотвращает отказ и уничтожает сигнал, что локализовал бы причину. Та же логика к повторам и прочим ручкам устойчивости: они размазывают отказ по времени, не чиня, что ресурс убегает быстрее, чем возвращается. Дисциплина — ограничить причину, гарантировав возврат, а не раздувать буфер, оттягивающий симптом." },
    { q: "Как обнаруживать утечки рано, какие метрики важны и что за ловушка async-границы?", a: "Поскольку утечки тихи до катастрофы, защита — наблюдаемость. Порог обнаружения утечки заставляет пул предупредить, когда соединение удерживалось дольше, чем должен любой легитимный запрос — leakDetectionThreshold HikariCP около двух секунд — испуская стектрейс того, кто его взял, ведь соединение, вне так долго, почти наверняка утекло или застряло. Следи за четырьмя датчиками: active в использовании, idle свободно, total и waiting потоков в очереди за соединением; здоровый пул держит idle выше нуля большую часть времени, утечка показывает active, лезущий и не падающий, пока idle тикает к нулю, а реальный перегруз показывает idle, пришпиленный на ноле, с высоким waiting. Алертуй на idle около нуля и waiting устойчиво выше нуля, ведь они предшествуют отказу. Ловушка async-границы — тоньше: удержание пулевого соединения через await чего-то иного, чем база, такого как медленный внешний HTTP-вызов, хоменит дефицитное соединение простаивающим на весь вызов. Оно не утекло, но исчерпывает пул под нагрузкой прямо как утечка, потому что эффективная конкурентность становится ограничена самым медленным, через что ты держишь соединение. Правило — брать соединение как можно позже, держать его лишь на работу с базой и никогда не оборачивать несвязанный сетевой вызов внутрь взятия." },
  ]}
/>

<Recap lang="ru">
Исчерпание пула обычно идёт от утечки, а не недоразмера: взятие, пропускающее возврат на каком-то пути — почти всегда ветка ошибки, прыгающая мимо release() — навсегда вычитает соединение, поэтому пул тикает к пустоте за часы и каждый запрос падает на взятии, пока трафик выглядит нормально, временно излечимо рестартом. Фикс структурный — try/finally или эквивалентная скоупнутая конструкция, возвращающая соединение на успехе и на броске — и никогда не больший пул, потому что утечка осушает со своей скоростью, поэтому размер лишь меняет график; исследование 5→100 показало, что расширение покупает куда меньше устойчивости, чем ожидалось, и хуже, прячет причину, оттягивая симптом за деплой. Защищайся обнаружением утечки, предупреждающим, когда соединение удерживается дольше пары секунд со стектрейсом, и следи за четырьмя датчиками — active, idle, total, waiting — алертуя на idle около нуля и устойчивый waiting. И не хоменись: удержание соединения через await несвязанной работы исчерпывает пул прямо как утечка. Теперь, когда видишь idle ползущий к нулю с рестартами по расписанию, — не тянись к большему пулу, тянись к стектрейсу leakDetectionThreshold и ищи ветку ошибки, не вызывающую release(). Всё доселе предполагало один пул против одной базы — финальный урок масштабирует наружу, где N инстансов приложения, каждый со своим пулом, сталкиваются против одного max_connections, и мультиплексор соединений вроде PgBouncer становится обязателен.
</Recap>
