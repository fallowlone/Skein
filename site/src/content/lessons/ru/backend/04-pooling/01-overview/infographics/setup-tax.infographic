infographic chart-bar-plain-text
data
  title Пул убирает 34 мс налога на setup
  desc Тот же запрос на 1 мс — соединение-на-запрос добавляет 34 мс TCP + TLS + auth на каждый вызов; пул платит это раз
  values
    - label Запрос — настоящая работа
      value 1
    - label Ритуал setup, на каждый запрос
      value 34
    - label Тот же запрос, но с пулом
      value 1
theme
  palette #14b8a6 #f43f5e #6366f1
