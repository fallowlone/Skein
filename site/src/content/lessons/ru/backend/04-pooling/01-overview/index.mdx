---
concepts:
- connection-pool
- connection-lifecycle
- tcp-handshake
- tls-handshake
- backend-process
- checkout-return
- amortization
deepensInto:
- backend/04-pooling/02-pool-sizing
estMin: 11
lang: ru
lessonType: topic
level: junior
order: 1
prereqs:
- backend/03-async-blocking/06-throughput-under-load
slug: 01-overview
sources:
- https://github.com/brettwooldridge/HikariCP/wiki/About-Pool-Sizing
- https://www.postgresql.org/docs/current/runtime-config-resource.html
- https://www.systemoverflow.com/learn/networking-protocols/http-protocols/tls-handshake-latency-the-critical-path-tax-across-protocol-versions
spiral:
- latency
- resource-limits
status: ready
summary: Открыть соединение с базой недёшево — TCP, TLS и аутентификация стоят миллисекунд,
  а сервер выделяет целый backend-процесс. Пул создаёт небольшой набор соединений
  один раз и одалживает их, поэтому запросы платят за setup почти никогда вместо каждого
  раза.
title: 'Зачем пул: цена создания соединения'
track: backend
unit: 04-pooling
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Junior-сервис открывает свежее соединение с Postgres на каждый запрос, делает один быстрый `SELECT` и закрывает его. Запрос занимает 1 мс; обработка занимает 35. Недостающие 34 мс — чистый *setup*: TCP-рукопожатие, TLS-рукопожатие и база, аутентифицирующая пользователя и форкающая backend-процесс — каждый раз, выброшенное после одного запроса. Под нагрузкой база тратит больше CPU на создание и уничтожение соединений, чем на ответы. Никто не написал медленный запрос. Написали быстрый запрос, обёрнутый в дорогой ритуал, повторяемый на каждом запросе.
</Hook>

<Crux>Соединение стоит рукопожатий плюс серверный процесс; пул платит это раз и одалживает его, поэтому запросы почти не платят за setup.</Crux>

<Explanation>

## Соединение дорого создавать

Новичку соединение с базой кажется бесплатным — вызови `connect()`, получи объект. Под капотом открытие одного — цепочка медленных шагов:

- **TCP-рукопожатие** — круговой обмен для установки сокета (один RTT до того, как потекут байты).
- **TLS-рукопожатие** — настройка шифрования, **2 круговых обмена** на TLS 1.3, **3** на TLS 1.2; на канале с реальной задержкой это одно — десятки-сотни миллисекунд.
- **Аутентификация** — база проверяет учётные данные, часто со своим challenge/response-обменом.
- **Выделение backend** — Postgres *форкает выделенный OS-процесс* на соединение; MySQL порождает поток. Этот процесс резервирует память (**~2–3 МБ** общей/служебной памяти до того, как выполнит хоть один запрос).

Так что соединение — не дешёвая ручка, это TCP-сокет плюс TLS-сессия плюс живой серверный процесс. Создавать и сносить его на запрос значит платить всё это за работу в один запрос.

## Пул амортизирует цену

**Пул соединений** — небольшой долгоживущий набор уже открытых соединений, держимых наготове. Вместо `connect → query → close` поток становится:

1. **Взять** (acquire/borrow) готовое соединение из пула.
2. Выполнить запрос на нём.
3. **Вернуть** (release) его в пул — *не* закрыть, лишь пометить свободным для следующего запроса.

Дорогие рукопожатия случаются раз, когда пул наполняется, затем те же соединения переиспользуются тысячи раз. Это **амортизация**: фиксированная цена setup, размазанная по многим использованиям, пока доля на запрос не станет ничтожной. Запрос теперь платит ~0 мс setup вместо ~34, а база перестаёт жечь CPU на churn соединений.

<Inset kind="why" lang="ru">
Зачем держать соединения открытыми и простаивающими, а не открывать по требованию и закрывать по завершении? Затем, что весь смысл — *не* платить за setup на пути запроса. Простаивающее пулевое соединение стоит немного памяти на клиенте и сервере, но оно мгновенно доступно — взятие почти бесплатная внутрипроцессная операция. Альтернатива, открытие на запрос, переносит задержку рукопожатия прямо во время ожидания пользователя и превращает базу в фабрику соединений под нагрузкой. Пул меняет небольшую, ограниченную, постоянную цену (горстка простаивающих соединений) на устранение большой, повторяемой, на-каждый-запрос цены. Этот размен почти всегда оправдан для сервера, обрабатывающего больше чем струйку трафика.
</Inset>

## Пул — это ещё и лимит

Переиспользование — очевидная польза, но вторая работа пула важна не меньше: это **граница**. Пул размера 20 значит, что от этого сервиса к базе одновременно существует не более 20 соединений, сколько бы запросов ни пришло. Этот потолок — фича: он защищает базу, которая может держать лишь столько backend-ов, прежде чем упадёт (Postgres по умолчанию **max_connections = 100**). Без пула всплеск в 5 000 конкурентных запросов попытался бы открыть 5 000 соединений, и база отвергла бы их «too many clients». Пул превращает неограниченный спрос в фиксированное, выживаемое число — та же идея ограниченной конкурентности из прошлого юнита, применённая к самому дорогому downstream-ресурсу бэкенда.

<div data-lesson-visual class="overflow-x-auto my-6">

| | Соединение на запрос | Пулевые соединения |
|---|---|---|
| Цена setup платится | Каждый запрос (TCP+TLS+auth) | Раз, при наполнении пула |
| Задержка на запрос | + десятки мс рукопожатия | ~0 мс (внутрипроцессное взятие) |
| Нагрузка на стороне БД | Постоянный fork/teardown churn | Стабильный набор backend-ов |
| Конкурентность к БД | Неограниченная (всплеск → перегруз) | Ограничена размером пула |
| Отказ при всплеске | «too many clients», отказ | Запросы ждут или падают быстро |

</div>

<Quiz
  id="pooling-overview-q1"
  lessonSlug="01-overview"
  lang="ru"
  question="Хэндлер выполняет запрос на 1 мс, но каждый запрос занимает ~35 мс целиком, и CPU базы высок, хотя запросы тривиальны. Какова вероятнейшая причина?"
  choices={[
    { label: "Запросу нужен индекс", misconception: "Запрос на 1 мс уже быстр; недостающее время — setup соединения, не выполнение запроса." },
    { label: "Он открывает и закрывает соединение на запрос, поэтому каждый платит TCP + TLS + auth-рукопожатия, а БД churn-ит backend-процессы", correct: true },
    { label: "Сетевая карта слишком медленна", misconception: "Железо не подразумевается; накладные — это повторяемое рукопожатие и выделение backend, что пулинг убирает." },
    { label: "Postgres однопоточен", misconception: "Postgres использует процесс на соединение; цена здесь — создание этих процессов повторно, а не модель потоков." },
  ]}
/>

<Quiz
  id="pooling-overview-q2"
  lessonSlug="01-overview"
  lang="ru"
  question="Помимо переиспользования соединений, какую вторую защиту даёт бэкенду фиксированный пул?"
  choices={[
    { label: "Делает отдельные запросы быстрее на базе", misconception: "Пулинг не ускоряет выполнение запроса; он убирает цену setup и ограничивает число соединений." },
    { label: "Ограничивает число соединений к базе, поэтому всплеск трафика не откроет тысячи и не вызовет 'too many clients'", correct: true },
    { label: "Автоматически шифрует соединение", misconception: "Шифрование — работа TLS-рукопожатия, делаемого на соединение; пул его не даёт." },
    { label: "Прозрачно повторяет проваленные запросы", misconception: "Повторы — отдельная забота; пул управляет переиспользованием соединений и потолком конкурентности, не повторами." },
  ]}
/>

<DragOrder
  id="pooling-overview-drag1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Упорядочи жизненный цикл одного запроса с пулом соединений:"
  items={[
    "Взять готовое соединение из пула",
    "Выполнить запрос на одолженном соединении",
    "Вернуть соединение в пул (свободно, не закрыто)",
    "Поздний запрос берёт то же соединение и переиспользует его",
  ]}
/>

</Explanation>

<FlowDiagram
  label="Цикл взятия и возврата соединения из пула"
  hue="var(--d-backend)"
  perRow={4}
  nodes={[
    {id:"idle",label:"пул простаивает",sub:"conn готово"},
    {id:"checkout",label:"взятие",sub:"acquire()"},
    {id:"inuse",label:"занят",sub:"запрос идёт"},
    {id:"return",label:"возврат",sub:"release()"}
  ]}
  edges={[
    {from:"idle",to:"checkout",label:"запрос"},
    {from:"checkout",to:"inuse",label:"одолжить"},
    {from:"inuse",to:"return",label:"готово"},
    {from:"return",to:"idle",label:"переиспользовать"}
  ]}
  caption="Пулевое соединение циклирует idle → checkout → in-use → return → idle. Дорогие рукопожатия (TCP + TLS + auth) происходят раз при наполнении пула; каждый запрос платит лишь почти бесплатный шаг взятия."
/>

<KeyTakeaway>Открытие соединения с базой — цепочка медленных шагов: TCP-рукопожатие, TLS-рукопожатие (2 RTT на TLS 1.3, 3 на 1.2), аутентификация и серверный backend-процесс, резервирующий ~2–3 МБ до того, как что-то выполнит. Делать это на запрос значит платить десятки миллисекунд setup за один запрос и заставлять базу churn-ить процессы. Пул соединений открывает небольшой набор раз и одалживает их — взять, запрос, вернуть-не-закрыть — поэтому setup амортизируется до почти нуля на запрос. Пул — ещё и граница: он ограничивает соединения своим размером, защищая базу, у которой по умолчанию max_connections = 100, от перегруза всплеском.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="pooling-overview-retrieval"
  lang="ru"
  questions={[
    { q: "Почему создание соединения с базой дорого, шаг за шагом?", a: "Открытие соединения — не бесплатная ручка, а цепочка медленных операций. Сначала TCP-рукопожатие устанавливает сокет ценой одного кругового обмена. Затем TLS-рукопожатие настраивает шифрование — два круговых обмена на TLS 1.3 и три на TLS 1.2, поэтому на канале с реальной задержкой это одно может быть десятки-сотни миллисекунд. Затем база аутентифицирует учётные данные, часто со своим challenge/response-обменом. Наконец сервер выделяет backend: Postgres форкает выделенный OS-процесс на соединение (MySQL порождает поток), и этот процесс резервирует память — примерно 2–3 МБ общей/служебной — до того, как выполнит хоть один запрос. Так что соединение — это TCP-сокет плюс TLS-сессия плюс живой серверный процесс, и создавать его на запрос значит платить всё это за работу в один запрос, заставляя базу жечь CPU на fork/teardown churn." },
    { q: "Как работает пул соединений и что значит амортизация здесь?", a: "Пул соединений — небольшой долгоживущий набор уже открытых соединений, держимых наготове. Вместо connect → query → close запрос берёт (одалживает) готовое соединение, выполняет запрос и возвращает (освобождает) его в пул — соединение помечается свободным для следующего запроса, не закрывается. Дорогие рукопожатия случаются раз, когда пул наполняется, и затем те же соединения переиспользуются тысячи раз. Это и есть амортизация: фиксированная цена setup, размазанная по многим использованиям, пока доля на запрос не станет ничтожной. Итог в том, что запрос платит примерно ноль миллисекунд setup вместо десятков, а база перестаёт тратить CPU на постоянное создание и уничтожение соединений." },
    { q: "Почему пул — это ещё и граница, и почему это важно?", a: "У пула фиксированный максимальный размер, поэтому от сервиса к базе одновременно существует не более стольких соединений, сколько бы запросов ни пришло — пул на 20 значит никогда больше 20 backend-ов. Этот потолок — защита, потому что база способна держать лишь столько соединений, прежде чем деградирует; Postgres по умолчанию max_connections = 100, и каждое соединение — реальный backend-процесс, потребляющий память. Без пула всплеск тысяч конкурентных запросов попытался бы открыть тысячи соединений, и база отвергла бы их 'too many clients already'. Пул превращает неограниченный спрос в фиксированное, выживаемое число — тот же принцип ограниченной конкурентности из async-юнита, применённый к самому дорогому downstream-ресурсу бэкенда." },
  ]}
/>

<Recap lang="ru">
Соединение с базой выглядит бесплатным, но это цепочка дорогого setup: TCP-рукопожатие, TLS-рукопожатие (два круговых обмена на 1.3, три на 1.2), аутентификация и серверный backend-процесс, резервирующий пару мегабайт до любой работы. Открытие одного на запрос хоронит десятки миллисекунд рукопожатия в каждом вызове и заставляет базу жечь CPU на форк и снос процессов — быстрый запрос, обёрнутый в дорогой ритуал. Пул соединений чинит обе половины: открывает небольшой набор соединений раз и одалживает их (взять, запрос, вернуть-не-закрыть), амортизируя setup до почти нуля на запрос, и ограничивает общее число соединений своим размером, защищая базу со всего 100 backend-ами по умолчанию от перегруза-всплеском 'too many clients'. Пул — это ограниченная конкурентность для самого дорогого ресурса, которого касается бэкенд. Теперь, когда видишь сервис с быстрыми запросами, но медленными ответами и высоким CPU базы без видимой причины, первый вопрос должен быть: а не открывается ли здесь соединение на каждый запрос? Но пул помогает лишь при правильном размере — и следующий урок покажет, почему больше контринтуитивно не лучше и как вычислить число, реально максимизирующее пропускную способность.
</Recap>
