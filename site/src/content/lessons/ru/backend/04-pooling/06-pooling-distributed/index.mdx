---
concepts:
- connection-multiplexing
- pgbouncer
- transaction-pooling
- session-pooling
- max-connections
- serverless-connections
- fan-in
deepensInto:
- backend/05-idempotency-retries/01-why-idempotency
estMin: 17
lang: ru
lessonType: topic
level: senior
order: 6
prereqs:
- backend/04-pooling/05-pool-exhaustion
slug: 06-pooling-distributed
sources:
- https://www.pgbouncer.org/features.html
- https://github.com/brettwooldridge/HikariCP/wiki/About-Pool-Sizing
- https://www.postgresql.org/docs/current/runtime-config-connection.html
spiral:
- resource-limits
- scalability
status: ready
summary: Один пул против одной базы решён. Сложная версия — N инстансов приложения,
  каждый со своим пулом, все сталкивающиеся против одного max_connections, где математика
  переполняет и мультиплексор на уровне транзакций становится обязателен, с резкими
  компромиссами.
title: 'Пулинг на масштабе: много инстансов, одна база и PgBouncer'
track: backend
unit: 04-pooling
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import poolingModes from "./infographics/pooling-modes.svg?raw";

<Hook>
Один сервис с пулом на 20 здоров. Затем он масштабируется: автоскейлинг гонит 50 инстансов под нагрузкой, каждый со своим пулом на 20. Это 1 000 соединений, затребованных от Postgres, настроенного с дефолтным `max_connections = 100`. База начинает отвергать соединения с `FATAL: sorry, too many clients already`, и отвержение бьёт по *каждому* инстансу, поэтому весь флот деградирует разом. Ничего не утекает, и ни один пул не сконфигурён неверно. Арифметика просто переполнила: каждый инстанс задал размер пула изолированно, но все они черпают из одного общего жёсткого лимита — и это проблема, ради которой существует распределённый пулинг.
</Hook>

<Crux>N пулов вливаются в один max_connections и переполняют его; мультиплексор как PgBouncer вмещает тысячи клиентов через десятки соединений.</Crux>

<Explanation>

## Проблема fan-in

Пул на стороне клиента ограничивает соединения *от одного инстанса*. Он ничего не знает о других инстансах, указывающих на ту же базу. Реально важное ограничение глобально:

```
(число инстансов) × (размер пула на инстанс) ≤ max_connections базы
```

с вычетом запаса на admin, репликацию и инструменты миграции. С Postgres по умолчанию `max_connections = 100` и каждым соединением как реальным backend-процессом, потребляющим несколько мегабайт, нельзя просто поднять `max_connections` до 1 000 — база истратилась бы на память на процесс и планирование, ровно та конкуренция из урока про размер, теперь на масштабе самого сервера. Поэтому в горизонтально масштабируемом или автоскейлящемся флоте размер пула на инстанс должен быть крошечным (иногда 2–5), и даже тогда событие скейл-апа может взорвать бюджет. Это стена, толкающая к иной архитектуре.

## PgBouncer: мультиплексор в середине

**Пулер соединений** вроде **PgBouncer** сидит между приложениями и Postgres. Приложения подключаются к PgBouncer — дёшево, потому что клиентское соединение PgBouncer лёгкое, не backend-процесс — а PgBouncer держит небольшой пул *реальных* соединений к Postgres, одалживая их по нужде. Это **fan-in мультиплексор**: тысячи клиентских соединений вливаются в десятки серверных. Типичные соотношения драматичны — PgBouncer может предъявлять **тысячи клиентских соединений**, держа лишь несколько десятков реальных backend-ов Postgres, фактор мультиплексирования часто называемый около **25–50×** или больше. 1 000 затребованных флотом соединений становятся, скажем, 25 реальными backend-ами, и Postgres снова комфортен.

## Транзакционный пулинг покупает соотношение — и ломает состояние сессии

PgBouncer предлагает **режимы** пулинга, и выбор — весь компромисс:

- **Сессионный пулинг.** Клиент держит реальное серверное соединение всю свою клиентскую сессию. Безопасно и прозрачно — всё работает — но соотношение мультиплексирования бедное, потому что реальный backend связан столько, сколько клиент остаётся подключённым. Немногим лучше, чем без пулера по числу соединений.
- **Транзакционный пулинг.** Реальное серверное соединение назначается лишь *на длительность транзакции*, затем возвращается в пул в миг коммита транзакции. Это и даёт огромное соотношение, потому что соединения держатся миллисекунды, не минуты. Цена: **всё, что опирается на состояние сессии поперёк транзакций, ломается.** Подготовленные выражения (серверные), `SET` сессионных переменных, `LISTEN/NOTIFY`, advisory-блокировки и сессионные временные таблицы больше не работают, потому что следующее выражение может приземлиться на *другой* backend. Драйверы должны бежать в режиме, избегающем серверных подготовленных выражений, а код приложения не должен предполагать непрерывность сессии.

<Infographic
  svg={poolingModes}
  label="Сравнение сессионного и транзакционного пулинга PgBouncer бок о бок. Сессионный пулинг держит реальный backend всю клиентскую сессию, мультиплексирует около 1x и сохраняет состояние сессии в безопасности. Транзакционный пулинг держит backend лишь на миллисекунды транзакции, достигая соотношения 25–50x, которое сжимает 1000 клиентских соединений до ~25 реальных backend-ов, но ломает серверные prepared statements, SET, LISTEN/NOTIFY и advisory-блокировки."
  caption="Транзакционный пулинг покупает огромное соотношение именно тем, что не даёт ни одному клиенту удержать backend — тот же механизм ломает состояние сессии поперёк транзакций. Сессионный пулинг сохраняет это состояние, но едва снижает число соединений."
  hue="var(--d-infra)"
/>

<Inset kind="why" lang="ru">
Почему транзакционный пулинг даёт такое большое соотношение мультиплексирования, пока сессионный едва помогает? Потому что соотношение управляется тем, *как долго каждый клиент занимает реальный backend*. В сессионном пулинге клиент владеет backend-ом всё время, пока подключён — часто минуты или часы в основном простаивающего keep-alive — поэтому число реальных backend-ов должно быть близко к числу одновременно *подключённых* клиентов, ровно тому счёту, что ты пытался сжать. В транзакционном пулинге backend занят лишь пока транзакция реально исполняется, обычно несколько миллисекунд, затем передан следующему клиенту на лету; поскольку в любой миг лишь малая доля подключённых клиентов внутри активной транзакции, несколько десятков backend-ов обслуживают тысячи соединений. Соотношение по сути обратно скважности — доле времени, которую клиент проводит реально транзакируя против простоя. Тот же механизм — почему ломается состояние сессии: состояние вроде подготовленного выражения или SET живёт на конкретном backend, но транзакционный пулинг намеренно перемещает тебя на другой backend на следующей транзакции, чтобы держать скважность низкой. Нельзя иметь и максимум мультиплексирования, и стойкое состояние сессии на сервере — это одна монета, ведь мультиплексирование работает именно тем, что не даёт никакому клиенту удержать backend.
</Inset>

## Serverless делает это острым

Проблема fan-in достигает крайности с serverless-функциями, где каждый конкурентный вызов может быть своим процессом, желающим своё соединение, и вызовы масштабируются до сотен во всплеске. Традиционный пулинг предполагает долгоживущий процесс, амортизирующий пул по многим запросам; функция, живущая 200 мс, не может. Ответы той же формы — внешний пулер (PgBouncer или управляемый эквивалент вроде serverless data-прокси), впитывающий всплеск короткоживущих клиентов в стабильный набор реальных backend-ов, плюс агрессивно малые лимиты на функцию. Принцип никогда не меняется: число соединений базы — жёсткий, общий, дорогой ресурс, и каждый слой, вливающийся в него, должен быть ограничен.

<div data-lesson-visual class="overflow-x-auto my-6">

| Подход | Реальных backend-ов Postgres | Ёмкость клиентов | Оговорка |
|---|---|---|---|
| Только пулы на инстанс | инстансы × размер пула | Ограничено max_connections | Переполняет на скейл-апе |
| PgBouncer сессионный режим | ≈ конкурентных клиентов | Малое мультиплексирование | Безопасно, прозрачно, слабое соотношение |
| PgBouncer транзакционный режим | десятки | Тысячи (25–50×+) | Нет prepared / SET / LISTEN / advisory-блокировок |
| Serverless + data-прокси | стабильный малый набор | Всплеск короткоживущих функций | Лимит на функцию должен быть крошечным |

</div>

<Quiz
  id="distributed-q1"
  lessonSlug="06-pooling-distributed"
  lang="ru"
  question="Сервис, здоровый на одном инстансе с пулом на 20, начинает кидать 'FATAL: sorry, too many clients already' после автоскейла до 50 инстансов. Утечек пула нет. Почему?"
  choices={[
    { label: "Пул каждого инстанса повредился при скейл-апе", misconception: "Повреждения нет; каждый пул в порядке по отдельности — вместе они превышают общий лимит соединений базы." },
    { label: "50 инстансов × 20 соединений = 1 000 затребованных против дефолтного max_connections в 100, поэтому общий лимит переполняет и база отвергает соединения по всему флоту", correct: true },
    { label: "Автоскейлинг отключил пулинг", misconception: "Пулинг всё ещё включён; проблема в том, что много независимых пулов суммируются за один глобальный лимит, не в том, что пулинг встал." },
    { label: "Нагрузка запросов внезапно изменилась", misconception: "Нагрузка не причина; отвержение — чисто арифметика соединений, превышающая max_connections." },
  ]}
/>

<Quiz
  id="distributed-q2"
  lessonSlug="06-pooling-distributed"
  lang="ru"
  question="Почему транзакционный пулинг PgBouncer достигает куда большего соотношения мультиплексирования, чем сессионный?"
  choices={[
    { label: "Он сжимает сетевой трафик между приложением и базой", misconception: "Он не сжимает трафик; соотношение идёт от того, сколь кратко каждый клиент держит реальный backend." },
    { label: "Реальный backend держится лишь миллисекунды транзакции, не всю клиентскую сессию, поэтому несколько десятков backend-ов обслуживают тысячи в основном простаивающих соединений", correct: true },
    { label: "Он гонит запросы параллельно по нескольким базам", misconception: "Он не параллелит по базам; он мультиплексирует много клиентов на немногие backend-ы одной базы." },
    { label: "Он кеширует результаты запросов, поэтому база едва задета", misconception: "PgBouncer — мультиплексор соединений, не кеш результатов; соотношение про время занятости backend, не кеширование." },
  ]}
/>

<Quiz
  id="distributed-q3"
  lessonSlug="06-pooling-distributed"
  lang="ru"
  question="Какова главная цена корректности при переходе на транзакционный пулинг PgBouncer?"
  choices={[
    { label: "Транзакции становятся неатомарными", misconception: "Транзакции остаются атомарными; ломается состояние, что должно жить поперёк транзакций, не атомарность одной." },
    { label: "Ломается состояние через сессию — серверные prepared-выражения, SET-переменные, LISTEN/NOTIFY, advisory-блокировки — потому что следующая транзакция может бежать на другом backend", correct: true },
    { label: "Данные тихо портятся на коммите", misconception: "Порчи данных нет; проблема в том, что состояние сессии на сервере не следует за клиентом на новый backend." },
    { label: "Он заставляет каждый запрос быть только-чтение", misconception: "Записи полностью поддержаны; транзакционный пулинг не ограничивает тип запроса, лишь стойкое состояние сессии." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Fan-in через PgBouncer: N пулов → несколько реальных backend-ов Postgres"
  hue="var(--d-backend)"
  perRow={4}
  nodes={[
    {id:"app1",label:"инстанс 1",sub:"пул: 20 конн"},
    {id:"app2",label:"инстанс 2",sub:"пул: 20 конн"},
    {id:"appN",label:"инстанс N",sub:"пул: 20 конн"},
    {id:"pgb",label:"PgBouncer",sub:"лёгкие клиенты"},
    {id:"backends",label:"~25 backend-ов",sub:"реальных соединений"},
    {id:"pg",label:"Postgres",sub:"max_connections=100"}
  ]}
  edges={[
    {from:"app1",to:"pgb",label:"1000 итого"},
    {from:"app2",to:"pgb"},
    {from:"appN",to:"pgb"},
    {from:"pgb",to:"backends",label:"txn pooling"},
    {from:"backends",to:"pg",label:"соотношение 25–50×"}
  ]}
  caption="50 инстансов × пул 20 = 1 000 клиентских соединений вливаются в PgBouncer. С транзакционным пулингом PgBouncer держит лишь ~25 реальных backend-ов Postgres — соотношение мультиплексирования 40× — удерживая флот под max_connections = 100."
/>

<KeyTakeaway>Один пул против одной базы решён; сложная версия — fan-in: N инстансов, каждый со своим пулом, сталкиваются против одного max_connections, поэтому 50 инстансов × пул 20 = 1 000 затребованных переполняет дефолтные 100 Postgres, и база отвергает по всему флоту с "FATAL: sorry, too many clients already" — и нельзя просто поднять max_connections, потому что каждое соединение — реальный backend-процесс. Пулер соединений вроде PgBouncer мультиплексирует тысячи лёгких клиентских соединений в несколько десятков реальных backend-ов, часто 25–50× или больше. Соотношение идёт от транзакционного пулинга, держащего реальный backend лишь на миллисекунды транзакции, а не всю сессию — но это ломает всё, опирающееся на состояние сессии поперёк транзакций: серверные prepared-выражения, SET-переменные, LISTEN/NOTIFY, advisory-блокировки. Сессионный пулинг сохраняет их рабочими, но едва мультиплексирует. Serverless делает fan-in крайним, требуя внешнего пулера плюс крошечных лимитов на функцию, потому что число соединений базы всегда жёсткий, общий, дорогой ресурс.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="distributed-retrieval"
  lang="ru"
  questions={[
    { q: "Почему горизонтально масштабированный флот перегружает базу, даже когда каждый отдельный пул верно размерен?", a: "Потому что пул на стороне клиента ограничивает лишь соединения от одного инстанса и ничего не знает о других инстансах, указывающих на ту же базу, тогда как реально важное ограничение глобально: число инстансов, помноженное на размер пула на инстанс, должно остаться под max_connections базы, с вычетом запаса на admin, репликацию и инструменты миграции. Postgres по умолчанию ставит max_connections в 100, и каждое соединение — реальный backend-процесс, потребляющий несколько мегабайт, поэтому 50 инстансов, каждый с пулом на 20, требуют 1 000 соединений, и база отвергает их с FATAL: sorry, too many clients already, бья по каждому инстансу разом. Нельзя просто поднять max_connections до 1 000, потому что база тогда истратилась бы на память на процесс и планирование — та же конкуренция, что в уроке про размер, но на масштабе самого сервера. Поэтому пулы на инстанс должны быть крошечными в масштабированном флоте, и даже тогда событие скейл-апа может взорвать бюджет, что и есть стена, заставляющая иную архитектуру." },
    { q: "Что такое PgBouncer и как его режимы пулинга идут на компромисс?", a: "PgBouncer — пулер соединений, сидящий между приложениями и Postgres: приложения подключаются к нему дёшево, потому что клиентское соединение PgBouncer лёгкое, а не backend-процесс, и он держит небольшой пул реальных соединений к Postgres, одалживая их, действуя как fan-in мультиплексор, вливающий тысячи клиентских соединений в десятки серверных, часто соотношением 25–50× или больше. Его режимы — компромисс. Сессионный пулинг привязывает реальное серверное соединение к клиенту на всю его сессию — безопасно и полностью прозрачно, но соотношение мультиплексирования бедное, потому что backend занят, пока клиент остаётся подключённым, так что он едва снижает число соединений. Транзакционный пулинг назначает реальное соединение лишь на длительность транзакции и возвращает его в миг коммита, что даёт огромное соотношение, потому что backend-ы держатся миллисекунды, не минуты. Цена в том, что всё, опирающееся на состояние сессии поперёк транзакций, ломается — серверные prepared-выражения, SET сессионных переменных, LISTEN/NOTIFY, advisory-блокировки и сессионные временные таблицы — потому что следующее выражение может приземлиться на другой backend, поэтому драйверы должны избегать серверных prepared-выражений, а код не должен предполагать непрерывность сессии." },
    { q: "Почему высокое соотношение транзакционного пулинга и его потеря состояния сессии — две стороны одного механизма, и почему serverless — острый случай?", a: "Соотношение мультиплексирования управляется тем, как долго каждый клиент занимает реальный backend. В сессионном пулинге клиент владеет backend-ом всё время, пока подключён, часто минуты в основном простаивающего keep-alive, поэтому число реальных backend-ов должно приближаться к числу одновременно подключённых клиентов — тому самому счёту, что ты хотел сжать. В транзакционном пулинге backend занят лишь пока транзакция реально исполняется, обычно миллисекунды, затем передан следующему клиенту, и поскольку в любой миг лишь малая доля подключённых клиентов внутри активной транзакции, несколько десятков backend-ов обслуживают тысячи соединений; соотношение по сути обратно скважности. Тот же механизм — почему ломается состояние сессии: состояние вроде prepared-выражения или SET живёт на одном конкретном backend, но транзакционный пулинг намеренно перемещает тебя на другой backend на следующей транзакции, чтобы держать скважность низкой, поэтому нельзя иметь и максимум мультиплексирования, и стойкое состояние сессии на сервере. Serverless — крайность, потому что каждый конкурентный вызов может быть своим короткоживущим процессом, желающим своё соединение, и вызовы всплескивают в сотни, пока функция, живущая 200 мс, не может амортизировать пул; ответ той же формы — внешний пулер или управляемый data-прокси, впитывающий всплеск короткоживущих клиентов в стабильный набор реальных backend-ов, плюс агрессивно малые лимиты на функцию — потому что число соединений базы всегда жёсткий, общий, дорогой ресурс, что каждый вливающийся слой должен ограничить." },
  ]}
/>

<Recap lang="ru">
Решённый случай — один пул против одной базы; сложный — fan-in, где N инстансов, каждый размеряющий пул изолированно, сталкиваются против одного max_connections — 50 инстансов × пул 20 = 1 000 против дефолтных 100 Postgres, отвергнутых по всему флоту с "FATAL: sorry, too many clients already" — и подъём max_connections не помогает, потому что каждое соединение — реальный backend-процесс, конкурирующий за память и планирование. Пулер соединений вроде PgBouncer мультиплексирует тысячи лёгких клиентских соединений в несколько десятков реальных backend-ов, и большое соотношение идёт именно от транзакционного пулинга, занимающего backend лишь на миллисекунды транзакции вместо всей сессии. Это та же монета, что его цена: поскольку следующая транзакция может бежать на другом backend, ломается состояние через сессию — серверные prepared-выражения, SET, LISTEN/NOTIFY, advisory-блокировки — пока сессионный пулинг их сохраняет, но едва мультиплексирует. Serverless гонит fan-in к крайности и заставляет внешний пулер плюс крошечные лимиты на функцию. Это закрывает юнит пулинга: соединение базы — жёсткий, общий, дорогой ресурс, ограниченный на каждом слое. Теперь, когда видишь флот с "FATAL: too many clients already" и кто-то предлагает поднять max_connections, — знаешь настоящий фикс: уменьши пулы на инстанс и поставь PgBouncer в транзакционном режиме между ними и базой. Следующий юнит поворачивает от удержания ресурса к выживанию проваленного вызова — идемпотентность и повторы, где безопасное повторение запроса становится фундаментом устойчивости.
</Recap>
