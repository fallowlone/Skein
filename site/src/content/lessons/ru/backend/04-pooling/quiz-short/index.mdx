---
concepts:
- amortisation
- pool-sizing
- acquisition-timeout
- connection-lifecycle
- connection-leak
- connection-multiplexing
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://github.com/brettwooldridge/HikariCP/wiki/About-Pool-Sizing
- https://github.com/brettwooldridge/HikariCP/wiki/Configuration
- https://www.pgbouncer.org/features.html
status: ready
summary: "Промпты на свободное припоминание по юниту пулинга. Сначала ответь своими словами, затем раскрой модельный ответ и сравни."
title: 'Пулинг: свободное припоминание'
track: backend
unit: 04-pooling
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт проговори или запиши полный ответ по памяти, прежде чем открыть модельный — именно усилие припоминания закрепляет материал.
</Hook>

<Goal lang="ru">
Восстанови ключевые механизмы юнита — зачем нужен pool, как его размерять, что защищает acquisition timeout, почему соединения протухают, как leaks осушают pool и почему fan-in вынуждает мультиплексор — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:visible
  id="04-pooling-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему открытие соединения с базой дорого и какие две работы делает с этим pool?", a: "Открытие соединения — не бесплатный хэндл, а цепочка медленных шагов: TCP handshake (один round trip), TLS handshake (два round trips на TLS 1.3, три на 1.2, то есть десятки–сотни мс на реальном канале), аутентификация и серверный backend — Postgres форкает выделенный OS-процесс, резервирующий примерно 2–3 МБ ещё до единственного запроса. Делать это на запрос — значит хоронить десятки мс setup в каждом вызове и заставлять базу молотить процессы. Первая работа pool — амортизация: открыть малый набор соединений один раз и одалживать их (check out, query, return-not-close), чтобы пер-запросный setup упал почти до нуля. Вторая работа — быть границей: pool размера 20 значит, что от сервиса существует не более 20 соединений, ограничивая базу с дефолтным max_connections = 100 и защищая её от всплеска, иначе вызвавшего бы 'too many clients'." },
    { q: "Почему подъём pool size за малое число снижает пропускную способность и какова формула размера HikariCP?", a: "Потому что соединение — не единица параллелизма, а ядро — да. База реально выполняет одновременно столько запросов, сколько у неё CPU-ядер (плюс небольшой запас под запросы, застрявшие на I/O); каждое соединение сверх этого ждёт, а не работает, и это ожидание не бесплатно. Излишние соединения платят context-switch на time-slicing по немногим ядрам, сверхлинейной lock и latch contention на общих внутренних структурах и памятью на соединение, где Postgres work_mem выделяется на операцию на соединение. Так что за полезным числом больше соединений покупают contention, и пропускная способность падает, а задержка растёт. Формула — connections = (core_count x 2) + effective_spindle_count: удвоить ядра, чтобы освободившееся ядро могло гонять другой запрос сквозь I/O-столл, плюс по одному на независимый диск. 4-ядерная, одно-SSD машина выходит на (4 x 2) + 1 = 9, и pool около 10 регулярно бьёт pool из 100 на том же железе." },
    { q: "Что происходит, когда запросу нужно соединение, но все заняты, и как настраивать acquisition timeout?", a: "Он не ошибается, и pool не растёт за свой максимум — вызывающий блокируется в wait-очереди, пока соединение не вернётся или не сработает acquisition timeout, поэтому задержка запроса становится wait-for-connection плюс время запроса. Acquisition timeout (HikariCP connectionTimeout, дефолт 30 с) — это осознанный latency-бюджет на худший случай. Оставленный на 30 с — ловушка: каждый ждущий пиннит поток веб-сервера на всё ожидание, поэтому при медленной зависимости thread pool заполняется обречёнными ждущими и просто медленная база валит здоровый веб-слой — thread starvation. Слишком короткий (например, 50 мс) валит безобидные микровсплески. Правильное значение — примерно один-три обычных запроса — часто несколько сотен мс до пары секунд — достаточно, чтобы пережить всплеск, но достаточно коротко, чтобы реальная starvation падала быстро и освобождала upstream-ёмкость shed load, тронуть breaker или вернуть деградированный ответ." },
    { q: "Почему соединение pool может быть мёртвым, пока pool думает, что оно открыто, и какие три контроля держат pool свежим?", a: "У TCP-соединения два конца и несколько middlebox'ов, любой из которых может разорвать его, пока локальный сокет pool всё ещё выглядит ESTABLISHED: база навязывает серверные лимиты (MySQL wait_timeout по дефолту 8 часов, у Postgres есть idle_in_transaction_session_timeout), и клиента не уведомляют до следующей записи; stateful файрволы и NAT сбрасывают idle-потоки за минуты (cloud NAT idle timeout часто около 350 с); а балансировщики рециклят backend'ы. Так pool может выдать мёртвый сокет первому запросу после затишья и тот упадёт с reset. Три согласованных контроля защищают: maxLifetime (дефолт 30 мин) проактивно ретайрит каждое соединение по возрасту и должен быть короче собственного таймаута БД, чтобы pool всегда закрывал первым; idleTimeout (дефолт 10 мин) ужимает pool вне пика, но действует, только когда minimumIdle ниже максимума; и validation on borrow плюс keepalive-пробы ловят уже мёртвые сокеты, пропуская проверку в окне aliveBypassWindow 500 мс, чтобы горячие соединения оставались быстрыми." },
    { q: "Что такое connection leak, почему больший pool — плохой фикс и в чём связанная ловушка hoarding?", a: "Leak нарушает контракт pool, что каждый checkout сопоставлен return: соединение берётся и на каком-то пути — классически error-ветка, перепрыгивающая через release() — никогда не освобождается, поэтому pool всё ещё считает его in use, а соединение потеряно навсегда. Каждый прогон этой ветки убирает одно соединение, поэтому pool ратчетится до пустого за часы, и каждый запрос падает как при массовой перегрузке, хотя трафик нормален, лечится лишь рестартом. Больший pool — плохой фикс, потому что leak — это rate, а не фиксированная стоимость: он осушает любой pool со скоростью, с какой бежит баговая ветка, поэтому размер лишь меняет таймлайн, и хуже — больший pool прячет причину, оттягивая симптом на часы за пределы деплоя. Фикс структурный: гарантируй release через try/finally или эквивалентную scoped-конструкцию, плюс leak detection (предупреждать за ~2 с со стеком) и четыре датчика active/idle/total/waiting. Связанная ловушка — hoarding: удержание соединения через await на несвязанной работе (внешний HTTP-вызов или оставленная открытой простаивающая транзакция) пиннит его idle и истощает pool как leak, хотя ничего не теряется." },
    { q: "Почему горизонтально масштабированный флот переполняет одну базу и как PgBouncer transaction pooling это решает — какой ценой?", a: "Клиентский pool ограничивает соединения лишь от одного инстанса и ничего не знает о других; глобальное ограничение — (instances x pool size на инстанс) под max_connections базы, минус запас. С дефолтом Postgres в 100 и каждым соединением — реальным backend-процессом, 50 инстансов по pool 20 требуют 1000 соединений, и база отвергает их по всему флоту с 'FATAL: sorry, too many clients already'. Нельзя просто поднять max_connections до 1000 — это воссоздаёт contention из урока про sizing на масштабе сервера. Connection pooler вроде PgBouncer стоит впереди и мультиплексирует тысячи лёгких клиентских соединений в несколько десятков реальных backend'ов (часто 25–50x или больше). Большой ratio даёт transaction pooling, держащий реальный backend лишь на миллисекунды транзакции, а не на всю сессию — но это та же монета, что и его цена: поскольку следующая транзакция может попасть на другой backend, ломается состояние, переживающее сессию (server-side prepared statements, SET-переменные, LISTEN/NOTIFY, advisory locks). Session pooling сохраняет это состояние, но мультиплексирует слабо." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: pool амортизирует дорогой setup и ограничивает конкурентность; его размер мал и выведен из ядер, а не угадан большим; acquisition timeout решает fail-fast vs thread starvation; соединения протухают за спиной pool и должны ротироваться по maxLifetime (под таймаутом БД) и валидироваться on borrow; самый частый отказ — leak или hoard, чинимый гарантией return, а не увеличением pool; а на масштабе многие pool сходятся в один max_connections, вынуждая transaction-pooling мультиплексор, торгующий состоянием сессии ради ratio. Соединение — жёсткий, общий, дорогой ресурс — ограничивай его осознанно на каждом слое.
</Recap>
