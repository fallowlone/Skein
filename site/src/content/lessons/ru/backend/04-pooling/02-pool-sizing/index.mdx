---
concepts:
- pool-sizing
- connections-equals-cores
- context-switching
- lock-contention
- work-mem
- saturation
- little''''s-law
deepensInto:
- backend/04-pooling/03-acquisition-and-timeouts
estMin: 14
lang: ru
lessonType: topic
level: middle
order: 2
prereqs:
- backend/04-pooling/01-overview
slug: 02-pool-sizing
sources:
- https://github.com/brettwooldridge/HikariCP/wiki/About-Pool-Sizing
- https://www.postgresql.org/docs/current/runtime-config-resource.html
- https://wiki.postgresql.org/wiki/Number_Of_Database_Connections
spiral:
- latency
- resource-limits
status: ready
summary: Инстинкт — поднимать размер пула, пока база не перестанет быть узким местом,
  но за малым числом пропускная способность падает, потому что соединение — backend-процесс,
  конкурирующий за ядра, блокировки и память. Правильный размер — формула, а не догадка.
title: 'Размер пула: почему больше не значит быстрее'
track: backend
unit: 04-pooling
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
Команда видит, что база — узкое место, поэтому поднимает пул соединений с 20 до 100, ожидая примерно впятеро больше пропускной способности. Пропускная способность *падает*. Задержка лезет вверх, CPU базы прыгает, и работа, которую сервер реально заканчивает в секунду, идёт вниз. Они подняли единственное число, выглядевшее дросселем, и сделали всё хуже. Пул был не мал — он стал намного больше числа запросов, которые железо базы способно выполнить разом, и каждое лишнее соединение за этим пределом — не параллелизм, а конкуренция.
</Hook>

<Crux>Соединение — backend-процесс за ядра, блокировки и память; за малым числом больше соединений значит конкуренцию, не пропускную способность.</Crux>

<Explanation>

## Больше соединений — не больше параллелизма

Кажется очевидным, что больше соединений значит больше конкурентной работы. Это не так, и причина физична. У сервера базы фиксированное число ядер CPU и фиксированное число дисковых шпинделей (или очередей SSD). В любой миг он способен реально *выполнять* лишь столько запросов, сколько у него ядер; всё прочее ждёт. Соединение, держащее запрос, не магически бежит — оно конкурирует за ядро, на котором побежать.

Поэтому открыв 100 соединений на 8-ядерной коробке, ты не получаешь 100 запросов разом. Ты получаешь 8 бегущих и 92 дерущихся за те 8 ядер, плюс все накладные на переключение между ними. Размер пула задаёт, сколько запросов может быть *в полёте*; железо задаёт, сколько может реально *продвигаться*. Когда первое число сильно превышает второе, разрыв — чистая трата.

## Три налога раздутого пула

Каждое соединение за полезным числом не сидит тихо. Оно стоит тебе тремя способами разом:

- **Переключение контекста.** ОС нарезает много соединений по немногим ядрам по времени. Каждое переключение сбрасывает кеши CPU и стоит времени планировщика. Со 100 активными соединениями на 8 ядрах коробка тратит растущую долю CPU лишь на *смену того, какой запрос бежит*, вместо выполнения запросов.
- **Конкуренция за блокировки и латчи.** Конкурентные транзакции дерутся за блокировки строк, а внутренние латчи базы (буферный пул, WAL, таблицы блокировок) общие. Больше конкурентных соединений значит больше конкуренции на этих общих структурах — а конкуренция сверхлинейна, поэтому хуже растёт быстрее числа соединений.
- **Память.** В Postgres каждое соединение — форкнутый backend-процесс, держащий ~2–3 МБ базовой памяти, и хуже, `work_mem` выделяется *на операцию на соединение*. Запрос с тяжёлой сортировкой при `work_mem = 16 МБ` через 100 соединений может зарезервировать гигабайты; та же нагрузка на 10 соединениях не может.

Все три налога действуют одновременно: каждое лишнее соединение тормозит все остальные через переключение, конкуренцию и давление памяти. Упусти хотя бы один из них при анализе — и придёшь к выводу «ещё несколько соединений не навредят»; упусти все три — и окажешься в ситуации команды из хука.

## Формула: маленькая, выведенная из железа

Известная рекомендация HikariCP превращает это в стартовую формулу:

```
соединения = (число_ядер × 2) + эффективное_число_шпинделей
```

Интуиция: пока один запрос ждёт дисковый I/O, другой может использовать освобождённое ядро CPU — поэтому хочется примерно вдвое больше ядер, чтобы держать их занятыми сквозь паузы I/O, плюс одно на каждый независимый диск, способный обслужить seek параллельно. **4-ядерный сервер с одним SSD** даёт `(4 × 2) + 1 = 9` соединений. Не 90. Число, максимизирующее пропускную способность, шокирующе мало, и измеренные бенчмарки это подтверждают: пул на ~10 часто бьёт пул на 100 на том же железе, заканчивая больше запросов в секунду при меньшей задержке.

<Inset kind="why" lang="ru">
Почему пул на 10 бьёт пул на 100, когда у нагрузки явно больше 10 дел? Потому что лишние 90 не делают работу — они стоят в очереди *внутри базы* вместо *снаружи неё*, а стоять внутри хуже. Когда 100 соединений бьют в 8-ядерную коробку, база принимает все 100 и нарезает их по времени, платя налог переключения и конкуренции за блокировки на каждом, поэтому каждый отдельный запрос бежит медленнее и общее заканчивается позже. Когда 10 соединений бьют в ту же коробку, база гонит их почти на полной скорости, а прочие запросы ждут в очереди пула — дёшево, в памяти приложения, ничего не стоя базе. Та же общая работа, но малый пул даёт базе работать в её эффективной точке и толкает неизбежное ожидание в самое дешёвое место. Ограничение пула — тот же урок ограниченной конкурентности из async-юнита: лишнее ставишь в очередь где-то дёшево, а не вываливаешь на дорогой ресурс.
</Inset>

## Подбор размера — поиск точки насыщения

Формула — стартовая точка, не финальный ответ — у реальных нагрузок смесь CPU-bound и I/O-bound запросов, и правильное число находится измерением. Метод — поднимать конкурентность, следя за пропускной способностью и задержкой: пропускная способность растёт, затем выходит на плато (**точка насыщения**), затем *падает*, как конкуренция берёт верх. Лучший размер пула сидит на плоской вершине или чуть ниже — наименьший пул, достигающий пика пропускной способности. За ним ты покупаешь задержку без отдачи в пропускной способности, ровно поведение колена очереди из урока про пропускную способность, теперь движимое выбранным тобой пулом.

<div data-lesson-visual class="overflow-x-auto my-6">

| Размер пула (8 ядер) | Что реально происходит | Пропускная способность | Задержка |
|---|---|---|---|
| 4 (мал) | Ядра простаивают, ожидая I/O | Ниже пика | Низкая, но ёмкость трачена |
| ~10–16 (в точку) | Ядра заняты, минимум конкуренции | Пик | Наименьшая на пике |
| 50 | Тяжёлое переключение + конкуренция за блокировки | Ниже пика | Лезет вверх |
| 100 | База молотит планирование, не выполнение | Сильно ниже пика | Высокая, нестабильная |

</div>

<Quiz
  id="pool-sizing-q1"
  lessonSlug="02-pool-sizing"
  lang="ru"
  question="Команда поднимает пул с 20 до 100 на 8-ядерной базе ради пропускной способности, но та падает, а задержка лезет. Почему?"
  choices={[
    { label: "Пул теперь слишком мал, чтобы вместить все запросы", misconception: "Пул стал больше, не меньше; проблема в слишком многих соединениях, не в слишком немногих." },
    { label: "Лишь ~8 запросов реально бегут разом на 8 ядрах, поэтому 92 лишних соединения добавляют переключение контекста и конкуренцию за блокировки без параллелизма", correct: true },
    { label: "Большим пулам всегда нужна более быстрая сетевая карта", misconception: "Сетевое железо не подразумевается; цена — конкуренция на базе за ядра, блокировки и память." },
    { label: "Postgres ограничивает соединения 20 по умолчанию", misconception: "Дефолт max_connections — 100, не 20; проблема — конкуренция задолго до любого жёсткого потолка." },
  ]}
/>

<Quiz
  id="pool-sizing-q2"
  lessonSlug="02-pool-sizing"
  lang="ru"
  question="По эвристике HikariCP, какой разумный стартовый размер пула для 4-ядерной базы с одним SSD?"
  choices={[
    { label: "Около 9, из (4 × 2) + 1", correct: true },
    { label: "Около 40, примерно десять на ядро", misconception: "Десять-на-ядро дико перелетает; формула даёт однозначные числа, потому что выполнение ограничивают ядра, не соединения." },
    { label: "100, дефолтный max_connections Postgres", misconception: "max_connections — потолок для всего сервера, не цель на приложение; число, максимизирующее пропускную способность, куда меньше." },
    { label: "Как можно выше, насколько хватит памяти", misconception: "Максимизация соединений максимизирует конкуренцию, не пропускную способность; лучший размер мал и выведен из ядер плюс шпинделей." },
  ]}
/>

<Quiz
  id="pool-sizing-q3"
  lessonSlug="02-pool-sizing"
  lang="ru"
  question="Почему память на соединение — реальное ограничение при подборе пула Postgres, помимо базовой цены процесса?"
  choices={[
    { label: "Каждое соединение кеширует всю базу в памяти", misconception: "Соединения не кешируют базу по отдельности; общий буферный пул общий, не на соединение." },
    { label: "work_mem выделяется на операцию на соединение, поэтому запрос с тяжёлой сортировкой, помноженный на размер пула, может зарезервировать гигабайты", correct: true },
    { label: "Соединения хранят результаты запросов вечно", misconception: "Результаты не удерживаются на соединение бесконечно; цена на соединение — память процесса плюс выделения work_mem." },
    { label: "Память нерелевантна; для размера важен лишь CPU", misconception: "Память — реальная граница: базовая память backend плюс work_mem, помноженный по соединениям, может исчерпать RAM." },
  ]}
/>

</Explanation>

<StackDiagram
  label="Размер пула vs. пропускная способность на 8-ядерной базе"
  hue="var(--d-backend)"
  layers={[
    {label:"пул 100 — молотьба",note:"переключение + конкуренция блокировок; пропускная способность сильно ниже пика"},
    {label:"пул 50 — конкуренция",note:"тяжёлое переключение; пропускная способность ниже пика, задержка лезет"},
    {label:"пул ~10–16 — оптимум",note:"ядра заняты, минимум конкуренции; пик пропускной способности"},
    {label:"пул 4 — недоиспользование",note:"ядра простаивают на I/O; пропускная способность ниже пика"}
  ]}
  caption="layers[0] = TOP. Оптимальная зона — (ядра × 2) + шпиндели ≈ 9 для 4-ядерного / 1-SSD сервера. Выше неё каждое лишнее соединение добавляет накладные переключения и конкуренции, снижающие пропускную способность."
/>

<KeyTakeaway>Больше соединений — не больше параллелизма: база способна реально гнать разом столько запросов, сколько у неё ядер, поэтому пул сильно больше (ядра × 2 + шпиндели) добавляет переключение контекста, конкуренцию за блокировки и латчи и давление памяти на соединение — work_mem выделяется на операцию на соединение — без прироста пропускной способности. 4-ядерная коробка с одним SSD хочет около (4 × 2) + 1 = 9 соединений, а пул на ~10 рутинно бьёт пул на 100 на том же железе. Найди размер, поднимая конкурентность, пока пропускная способность не выйдет на плато (насыщение), затем сядь на наименьший пул, достигающий этого пика. Излишек должен ждать дёшево в очереди пула, не дорого внутри базы.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="pool-sizing-retrieval"
  lang="ru"
  questions={[
    { q: "Почему подъём размера пула за небольшим числом снижает пропускную способность вместо роста?", a: "Потому что единица параллелизма — не соединение, а ядро. Сервер базы способен реально выполнять разом лишь столько запросов, сколько у него ядер CPU (плюс немного запаса на запросы, застрявшие на дисковом I/O); всякое соединение сверх этого ждёт, не бежит. Открытие 100 соединений на 8-ядерной коробке даёт 8 продвигающихся запросов и 92 конкурирующих, и эта конкуренция не бесплатна: ОС платит цену переключения контекста, нарезая много соединений по немногим ядрам, база платит конкуренцию за блокировки и латчи на общих внутренних структурах (буферный пул, WAL, таблицы блокировок), растущую сверхлинейно, и каждое соединение Postgres держит ~2–3 МБ памяти backend-процесса плюс work_mem на операцию. Поэтому за полезным числом больше соединений покупают больше накладных и конкуренции, и общее число запросов, заканчиваемых в секунду, падает, пока задержка растёт." },
    { q: "Какова формула размера HikariCP и какое за ней рассуждение?", a: "Формула — соединения = (число_ядер × 2) + эффективное_число_шпинделей. Удвоение ядер потому, что пока один запрос застрял в ожидании дискового I/O, освобождённое ядро CPU может гнать другой запрос, поэтому хочется примерно два соединения на ядро, чтобы держать ядра занятыми сквозь паузы I/O. Член шпинделей добавляет одно соединение на каждый независимый диск, способный обслужить seek параллельно (для SSD это фактически параллельная ёмкость очереди устройства, часто считаемая за одно для одного SSD). 4-ядерный сервер с одним SSD поэтому стартует с (4 × 2) + 1 = 9 соединений. Поразительна малость числа: бенчмарки повторно показывают пул около 10, обгоняющий пул на 100 на том же железе, заканчивая больше работы в секунду при меньшей задержке, потому что малый пул даёт базе работать в её эффективной точке вместо молотьбы." },
    { q: "Как реально найти правильный размер пула для реальной нагрузки и почему малый пул лучше даже при избытке работы?", a: "Находишь измерением: поднимаешь предлагаемую конкурентность, следя за пропускной способностью и задержкой. Пропускная способность растёт, затем выходит на плато в точке насыщения, затем падает, как конкуренция берёт верх; лучший размер пула — наименьший, достигающий этой плоской вершины, сидящий на колене или чуть ниже. Малый пул лучше даже при обилии работы, потому что излишку приходится где-то ждать, а ждать внутри базы куда дороже, чем в очереди пула. Большой пул заставляет базу принять всё и нарезать по времени, платя налог переключения и конкуренции на каждом запросе, поэтому каждый бежит медленнее; малый пул гонит запросы почти на полной скорости и паркует излишек дёшево в памяти приложения. Та же общая работа, но малый пул держит дорогой ресурс в его эффективной точке и толкает неизбежное ожидание в самое дешёвое место — тот же принцип ограниченной конкурентности из async-юнита, применённый к базе." },
  ]}
/>

<Recap lang="ru">
Инстинкт чинить узкое место базы расширением пула — задом наперёд: соединение — реальный backend-процесс, конкурирующий за фиксированное число ядер, поэтому пул сильно больше того, что железо способно гнать, превращает лишние соединения в чистую конкуренцию — переключение контекста по слишком немногим ядрам, сверхлинейную конкуренцию за блокировки и латчи на общих внутренних структурах и память на соединение, где work_mem множится на операцию по каждому соединению. (ядра × 2) + шпиндели от HikariCP ставит 4-ядерную коробку с одним SSD на около 9 соединений, и пул на ~10 рутинно бьёт пул на 100 на идентичном железе, потому что малый пул даёт базе работать в её эффективной точке, пока излишек ждёт дёшево в очереди пула, а не молотит внутри движка. Правильный размер — наименьший пул, достигающий пика пропускной способности, найденный подъёмом конкурентности до плато. Теперь, когда видишь, что команда «пофиксила» проблему подъёмом пула и недоумевает, почему стало хуже — знаешь, куда смотреть: точка насыщения уже была пройдена. Знание размера отвечает, сколько соединений держать — следующий урок спрашивает, что случается с запросом, когда все они заняты: очередь ожидания, таймаут взятия и как этот таймаут становится намеренным дросселем задержки.
</Recap>
