---
concepts:
- pool-sizing
- acquisition-timeout
- connection-leak
- stale-connection
- pool-exhaustion
- pgbouncer-multiplexing
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://github.com/brettwooldridge/HikariCP/wiki/About-Pool-Sizing
- https://www.pgbouncer.org/features.html
- https://www.postgresql.org/docs/current/runtime-config-connection.html
status: ready
summary: "Синтез с множественным выбором по юниту пулинга — формула размера, acquisition timeout, мёртвые соединения, leak vs exhaustion и fan-in против max_connections."
title: 'Пулинг: тест с множественным выбором'
track: backend
unit: 04-pooling
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, пронизывающих весь юнит. Каждый отражает решение, которое ты принимаешь в реальном инциденте с пулингом — не определение для пересказа, а компромисс, который надо взвесить, когда pool пуст, течёт или сходится (fan-in) в одну общую базу.
</Hook>

<Goal lang="ru">
Убедись, что можешь связать pool size, acquisition timeout, жизненный цикл соединения, leaks и распределённый fan-in — синтез, к которому вели отдельные уроки.
</Goal>

<Quiz
  id="04-pooling-choice-1"
  lessonSlug="04-pooling"
  lang="ru"
  question="Команда делает базу узким местом и поднимает pool с 20 до 100 на 8-ядерном Postgres, ожидая примерно 5x пропускной способности. Пропускная способность падает, задержка растёт. Почему?"
  choices={[
    { label: "На 8 ядрах одновременно реально выполняются лишь около 8 запросов, поэтому лишние 92 соединения добавляют context-switching и сверхлинейную lock/latch contention — плюс work_mem, умноженный на каждое соединение — не давая никакого реального параллелизма.", correct: true },
    { label: "Pool теперь слишком мал, чтобы держать все запросы в полёте.", misconception: "Pool стал больше, а не меньше. Проблема в том, что слишком много соединений дерутся за фиксированное число ядер, а не в нехватке слотов." },
    { label: "Pool из 100 всегда требует более быстрого сетевого канала к базе.", misconception: "Сеть здесь не ограничение; стоимость — это on-database contention за ядра, локи и память на соединение, которую больший pool лишь усугубляет." },
    { label: "Postgres по умолчанию ограничивает соединения числом 20, поэтому лишние 80 отклоняются.", misconception: "max_connections по умолчанию равен 100, а не 20. Пропускная способность рушится из-за contention задолго до любого жёсткого лимита." },
  ]}
/>

<Quiz
  id="04-pooling-choice-2"
  lessonSlug="04-pooling"
  lang="ru"
  question="Запрос вниз по стеку, обычно занимающий 5 мс, замедляется до 500 мс; за секунды весь веб-слой перестаёт принимать запросы, хотя база всё ещё жива. Каков механизм и первый конфиг-фикс?"
  choices={[
    { label: "Медленные запросы держат все соединения pool, поэтому новые запросы сидят в acquisition-очереди до дефолтного connectionTimeout в 30 с — каждый пиннит поток веб-сервера — пока thread pool не заполнится. Сократи acquisition timeout до примерно 1–3x обычного запроса, чтобы истощённый pool падал быстро.", correct: true },
    { label: "Pool кончилась память и он упал; перезапусти с большей кучей.", misconception: "Pool не падал — он делает ровно то, на что настроен, заставляя вызывающих ждать. Дефект — слишком длинный acquisition timeout, пиннящий потоки, а не крах по памяти." },
    { label: "База отклонила соединения, так что подними max_connections.", misconception: "База жива и не отклоняет; вызывающие стоят в очереди внутри приложения за уже открытым соединением pool. Подъём max_connections ничего не даёт против acquisition-пайлапа." },
    { label: "Подними pool size, чтобы свободные соединения были всегда.", misconception: "Больший pool лишь оттянет ту же starvation при устойчивом замедлении и свалит больше конкурентности на и без того медленную базу. Рычаг — ограничить ожидание, а не увеличивать pool." },
  ]}
/>

<Quiz
  id="04-pooling-choice-3"
  lessonSlug="04-pooling"
  lang="ru"
  question="После ночного затишья первые утренние запросы падают с 'connection reset by peer'; ничего не деплоили, база здорова. Что произошло и какой контроль — корневая защита?"
  choices={[
    { label: "Простаивающие соединения pool были тихо убиты DB wait_timeout, файрволом или NAT idle-правилом (~350 с) во время затишья, но pool сохранил мёртвые сокеты. Проактивная ротация maxLifetime — заданная короче собственного таймаута БД — ретайрит соединения раньше, чем это сделают middlebox'ы.", correct: true },
    { label: "Pool был слишком мал для утреннего всплеска трафика.", misconception: "Дело не в размере: удерживаемые соединения были мёртвыми, а не недостаточными. Первый запрос упал на stale-сокете, а не на полном pool." },
    { label: "Одной validation on borrow достаточно, поэтому maxLifetime избыточен.", misconception: "Validation ловит лишь соединение, уже мёртвое в момент checkout; она ничего не делает с тем, что умирает посреди запроса после выдачи. maxLifetime и validation дополняют друг друга, а не дублируют." },
    { label: "TLS-сертификаты истекли в полночь.", misconception: "Никакого истечения сертификата не подразумевается; сбой — это stale TCP-соединения, сброшенные middlebox'ами во время простоя, обнаруженные лишь при следующей записи." },
  ]}
/>

<Quiz
  id="04-pooling-choice-4"
  lessonSlug="04-pooling"
  lang="ru"
  question="Сервис таймаутит на acquisition соединений после часов работы; рестарт чинит на несколько часов, затем повторяется, и всё это при нормальном трафике. Почему увеличение pool — неверный фикс?"
  choices={[
    { label: "Это leak — borrow, пропускающий свой return на каком-то пути, — а leak осушает pool со скоростью, заданной частотой этого пути, поэтому больший pool лишь дольше опустошается и к тому же прячет причину, оттягивая симптом за пределы деплоя.", correct: true },
    { label: "Большие pool всегда роняют базу, поэтому увеличивать небезопасно.", misconception: "Дело не в крахе БД; leak осушает любой pool независимо от размера. Увеличение лишь меняет таймлайн до опустошения, оно не останавливает течь." },
    { label: "Pool действительно слишком мал; увеличение здесь верно.", misconception: "Нормальный трафик плюс рестарт, временно лечащий, — это сигнатура leak (ресурс утекает со временем), а не несоответствие размера в установившемся режиме. Гарантируй return через try/finally." },
    { label: "Большие pool отключают leak detection, маскируя баг.", misconception: "Размер pool не выключает leak detection. Реальная причина провала увеличения в том, что больше соединений лишь отсрочат ту же exhaustion, которую гарантирует leak." },
  ]}
/>

<Quiz
  id="04-pooling-choice-5"
  lessonSlug="04-pooling"
  lang="ru"
  question="Хендлер берёт соединение из pool, затем await'ит медленный внешний HTTP-вызов до своего запроса. Ничего не течёт — try/finally всегда освобождает. Почему это всё равно истощает pool под нагрузкой?"
  choices={[
    { label: "Соединение придерживается (hoarded) простаивающим на весь внешний вызов, поэтому эффективная конкурентность ограничивается самым медленным, через что держится borrow. Бери соединение как можно позже и держи его только на время работы с базой.", correct: true },
    { label: "Внешний HTTP-вызов повреждает соединение с базой.", misconception: "Несвязанный вызов не повреждает соединение; вред — в удержании дефицитного соединения простаивающим, а не в его порче." },
    { label: "Await автоматически освобождает соединение, так что всё нормально.", misconception: "Await не освобождает соединение — в этом и проблема. Оно остаётся checked out на всё ожидание, придержанным всё это время." },
    { label: "Это навсегда удваивает требование к настроенному pool size.", misconception: "Это не меняет настроенный размер; это растрачивает имеющиеся соединения, держа их простаивающими через несвязанную работу." },
  ]}
/>

<Quiz
  id="04-pooling-choice-6"
  lessonSlug="04-pooling"
  lang="ru"
  question="Сервис, здоровый на одном инстансе (pool 20), начинает кидать 'FATAL: sorry, too many clients already' после автоскейла до 50 инстансов. Утечек pool нет. Каков фикс и его главная цена корректности?"
  choices={[
    { label: "50 x 20 = 1000 затребованных против дефолтного max_connections в 100; поставь PgBouncer в режим transaction pooling, чтобы мультиплексировать тысячи клиентов в несколько десятков реальных backend'ов. Цена: ломается состояние, переживающее сессию — server-side prepared statements, SET, LISTEN/NOTIFY, advisory locks.", correct: true },
    { label: "Pool каждого инстанса повредился во время скейл-апа; передеплой чинит.", misconception: "Никакого повреждения нет; каждый pool в отдельности в порядке. Все вместе они превышают один общий лимит соединений базы — арифметическая проблема fan-in." },
    { label: "Просто подними max_connections до 1000 и оставь pool по инстансам.", misconception: "Каждое соединение — реальный backend-процесс, жрущий память и планирование, так что 1000 backend'ов воссоздают contention из урока про sizing на масштабе сервера. Ответ — мультиплексор, а не более высокий потолок." },
    { label: "Переключи PgBouncer в session pooling ради лучшего ratio.", misconception: "Session pooling привязывает backend к клиенту на всю его сессию, поэтому мультиплексирует слабо. Большой ratio даёт transaction pooling — ценой состояния сессии." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна цепочка: pool амортизирует setup соединения и ограничивает конкурентность, поэтому его размер мал и выведен из ядер (а не угадан большим); когда он пустеет, acquisition timeout решает, упадёшь ли ты быстро или заморишь веб-слой; удерживаемые им соединения протухают и должны ротироваться по maxLifetime и валидироваться on borrow; самый частый отказ — leak (или hoarding через await), который больший pool лишь оттягивает и прячет; а на масштабе многие pool сходятся в один max_connections, вынуждая transaction-pooling мультиплексор, торгующий состоянием сессии. Каждый ответ возвращается к одной дисциплине — ограничивай ресурс осознанно (size, wait, age, return, fan-in), а не позволяй внешней системе ограничить его за тебя в худший момент.
</Recap>
