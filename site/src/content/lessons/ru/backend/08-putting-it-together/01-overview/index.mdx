---
concepts:
- composition
- production-backend
- failure-interaction
- seven-concerns
deepensInto:
- backend/08-putting-it-together/02-tracing-one-request
estMin: 12
lang: ru
lessonType: topic
level: junior
order: 1
prereqs:
- backend/07-graceful-shutdown/06-zero-downtime-deploys
slug: 01-overview
sources:
- https://sre.google/sre-book/table-of-contents/
- https://aws.amazon.com/builders-library/
- https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/
spiral:
- synthesis
- failure-modes
- resilience
- lifecycle
status: ready
summary: 'Трек учил семи заботам по одной; продакшн запускает их все разом. Капстоун
  пересобирает их в единую систему: каждый юнит был одним режимом отказа в изоляции,
  но реальный сервис встречает их в композиции, где трудные баги живут во взаимодействиях,
  а не внутри одного механизма.'
title: 'Собираем воедино: бэкенд — это система, а не стопка'
track: backend
unit: 08-putting-it-together
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Ты потратил семь юнитов, изучая семь вещей, каждую в своей чистой комнате. Request lifecycle — сам по себе. Middleware и dependency injection — сами по себе. Async против блокирующего I/O, пулы, идемпотентность, circuit breakers, graceful shutdown — каждый юнит брал одну заботу, изолировал её, находил её режим отказа и чинил его. Эта изоляция была сутью: нельзя понять исчерпание пула, одновременно тревожась о SIGTERM. Но это и ложь ради удобства, потому что реальный продакшн-бэкенд никогда не запускает одну заботу за раз. Он запускает *все семь одновременно, на одном запросе, под одной нагрузкой, во время одного деплоя*. Соединение, что пул тебе выдал, — то же соединение, что graceful shutdown должен сдренировать. Ретрай, что идемпотентность сделала безопасным, — тот же ретрай, что, помноженный по флоту, исчерпывает пул и размыкает breaker. Семь забот — не семь коробок, поставленных друг на друга; это семь сил, действующих на одну движущуюся систему разом. Этот капстоун-юнит — где чистые комнаты рушатся. Тяжелейшие продовые баги, что тебе доведётся дебажить, не живут *внутри* какого-то одного из этих механизмов — каждый механизм в изоляции работает ровно как учил юнит. Баги живут в *швах между ними*, во взаимодействиях, что ни один юнит не мог тебе показать, потому что взаимодействие не существует, пока все части не запустятся вместе.
</Hook>

<Crux>Продакшн-бэкенд — не семь механизмов друг на друге, а одна система, где они складываются, и трудные сбои живут в их взаимодействиях.</Crux>

<Explanation>

## О чём на самом деле был каждый юнит

Отступи на шаг — и семь юнитов сходятся в одну форму. Каждый брал одну точку, где бэкенд встречает реальность и копится давление:

- **Request lifecycle** — как один запрос входит, парсится, маршрутизируется, обрабатывается и отвечается, и где латентность прячется в хвосте.
- **Middleware и DI** — как сквозные заботы и зависимости подключаются так, чтобы обработчик оставался тонким и тестируемым.
- **Async против блокирующего I/O** — как один event loop обслуживает тысячи конкурентных запросов и что один блокирующий вызов делает со всеми ними.
- **Пулинг** — как ограниченный набор дорогих соединений разделяется, размеряется и защищается от исчерпания.
- **Идемпотентность и ретраи** — как at-least-once доставка превращается в effectively-once результаты, так что ретрай безопасен.
- **Circuit breakers и bulkheads** — как сервис перестаёт долбить падающую зависимость и изолирует радиус поражения.
- **Graceful shutdown** — как процесс и флот останавливаются, не роняя работу в полёте.

Прочитанные списком, они выглядят независимыми. Они не таковы. Каждый из них — дисциплина корректного поведения *под частичным отказом и нагрузкой* — а частичный отказ и нагрузка ровно те условия, где они сталкиваются.

## Композиция — это не сложение

Наивная ментальная модель аддитивна: сделай все семь верно — и у тебя корректный бэкенд, ровно как семь зелёных юнит-тестов намекают на работающую систему. Но корректность не складывается так чисто, потому что каждый механизм меняет *среду*, в которой работают остальные. Пул разумного размера корректен в одиночку — но его корректное поведение при исчерпании в том, чтобы заставлять вызывающих ждать, и это ожидание — то, что circuit breaker читает как медлительность, и размыкание этого breaker — то, что меняет нагрузку, генерируемую слоем ретраев. Ни один из этих механизмов не ошибается. *Взаимодействие* — вот где живёт сюрприз. Это тот же урок, что юниты про circuit breaker и graceful shutdown вбивали каждый со своей стороны: локальная корректность не складывается в глобальную корректность даром, потому что режимы отказа живут во взаимодействиях, а не в компонентах.

Когда смотришь на зелёные тесты и спрашиваешь «готово ли это к продакшену?» — ты смотришь на чистые комнаты, а не на швы. Цель этого юнита — сделать те швы видимыми.

## Зачем существует этот юнит

Остаток юнита делает эти взаимодействия конкретными. Мы проследим один запрос через каждый слой, чтобы увидеть их сложенными на одном пути кода. Мы посмотрим, как режимы отказа складываются — шторм ретраев, что питает исчерпание пула, что питает breaker. Мы добавим связующую ткань, что ранние юниты предполагали, но не назвали: наблюдаемость, чтобы *видеть* систему как одно целое, и контроль нагрузки, чтобы она *деградировала* как одно целое. И завершим ревью готовности, что превращает весь трек в чеклист. Цель — сдвиг от «я знаю семь механизмов» к «я могу рассуждать об одной системе, которую они образуют».

<Inset kind="why" lang="ru">
Зачем вообще учить семи заботам в изоляции, если настоящий навык — видеть их в композиции — почему не начать с целой системы? Потому что составная система неизучаема с холода. Взаимодействия обретают смысл лишь когда ты держишь корректную модель каждой части, а новичок, уставившийся на «шторм ретраев исчерпал пул и разомкнул breaker посреди деплоя», не имеет крючков, на которые что-либо повесить — ретрай, пул, breaker, деплой все не определены. Изоляция строит словарь; композиция его использует. Педагогика отражает инженерию: ты размеряешь пул в изоляции сначала, затем узнаёшь, что его поведение при исчерпании — вход для breaker, затем узнаёшь, что состояние breaker — сигнал, что ты должен наблюдать, затем узнаёшь, что всё это должно пережить rolling-деплой. Каждый слой обучаем лишь когда слой под ним твёрд, ровно поэтому юниты упорядочены как лестница и поэтому этот идёт последним. Более глубокая мысль в том, что «готовность к продакшену» — не свойство какого-то одного механизма, что можно отметить галочкой — это свойство *системы*, эмерджентное из того, как механизмы скомпонованы, а эмерджентные свойства нельзя проверить, инспектируя части. Нельзя дойти до неё юнит-тестами. Вот почему сеньорная бэкенд-работа непропорционально про швы: кто угодно может заставить пул или breaker работать в одиночку, но держать весь граф взаимодействий в голове — знать, что изменение бюджета таймаута рябью идёт в breaker, слой ретраев и дедлайн shutdown разом — вот навык, за который зарплата на самом деле платит.
</Inset>

<div data-lesson-visual class="overflow-x-auto my-6">

| Юнит | Одна забота | Взаимодействие, что он питает |
|---|---|---|
| Request lifecycle | Приём, маршрут, ответ; хвост латентности | Медленный хвост насыщает пул и loop |
| Middleware / DI | Подключить зависимости, тонкий обработчик | Где ставятся таймауты и breakers |
| Async против блокирующего | Один loop, много запросов | Один блок стопорит все конкурентные запросы |
| Пулинг | Ограничить дорогие соединения | Исчерпание выглядит как медлительность для breaker |
| Идемпотентность | Сделать ретраи безопасными | Ретраи — то, что усиливает нагрузку в шторм |
| Circuit breakers | Перестать долбить падающую зависимость | Размыкание меняет нагрузку, что видят все |
| Graceful shutdown | Остановиться, не роняя работу | Дренаж гонится с идемпотентной джобой на деплое |

</div>

<Quiz
  id="cap-overview-q1"
  lessonSlug="01-overview"
  lang="ru"
  question="Семь бэкенд-юнитов каждый работает корректно в изоляции, но тяжелейшие продовые инциденты команды приходят из мест, что ни один юнит не объясняет. В чём капстоун видит причину?"
  choices={[
    { label: "Продакшн запускает все семь забот разом на одних запросах и нагрузке, так что тяжелейшие сбои возникают из взаимодействий между механизмами, а не из ошибки одного механизма", correct: true },
    { label: "Один из семи механизмов реализован неверно", misconception: "Каждый механизм может быть индивидуально корректен, а система всё равно падает; суть капстоуна в том, что взаимодействия, а не один сломанный компонент, рождают тяжелейшие инциденты." },
    { label: "Команде нужен восьмой механизм, что они ещё не выучили", misconception: "Пробел не в недостающем механизме, а в композиции существующих; юнит добавляет наблюдаемость и контроль нагрузки как связующую ткань, не новую изолированную заботу." },
    { label: "Зелёные юнит-тесты гарантируют, что составная система корректна", misconception: "Композиция — не сложение: корректные части не суммируются в корректное целое, потому что каждый механизм меняет среду, в которой работают остальные." },
  ]}
/>

<Quiz
  id="cap-overview-q2"
  lessonSlug="01-overview"
  lang="ru"
  question="Почему капстоун говорит «композиция — это не сложение» про бэкенд?"
  choices={[
    { label: "Каждый механизм меняет среду, в которой работают остальные — исчерпание пула предстаёт breaker'у медлительностью, чьё размыкание меняет нагрузку слоя ретраев — так что корректные части могут сложиться в некорректное поведение", correct: true },
    { label: "Потому что добавление механизмов всегда замедляет систему", misconception: "Дело не в сырой цене производительности; дело в том, что механизмы взаимодействуют, так что локальная корректность не гарантирует глобальную, независимо от скорости." },
    { label: "Потому что только один механизм может быть активен за раз", misconception: "Все семь работают одновременно на одном запросе и нагрузке — эта одновременность ровно и есть причина, почему взаимодействия важны." },
    { label: "Потому что механизмы полностью независимы", misconception: "Они противоположность независимым: каждый — дисциплина поведения под частичным отказом и нагрузкой, ровно условия, где они сталкиваются." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Семь забот, одна система"
  hue="var(--d-backend)"
  perRow={4}
  nodes={[
    {id:"lifecycle", label:"Жизненный цикл", sub:"хвост латентности"},
    {id:"mw", label:"Middleware / DI", sub:"бюджет таймаута"},
    {id:"async", label:"Асинхронный I/O", sub:"event loop"},
    {id:"pool", label:"Пулинг", sub:"ограниченные конн."},
    {id:"idem", label:"Идемпотентность", sub:"безопасный ретрай"},
    {id:"breaker", label:"Прерыватель цепи", sub:"радиус поражения"},
    {id:"shutdown", label:"Плавное завершение", sub:"дренаж"},
  ]}
  edges={[
    {from:"lifecycle", to:"pool", label:"медленный хвост"},
    {from:"async", to:"pool", label:"один блок"},
    {from:"pool", to:"breaker", label:"ожидание=медленно"},
    {from:"idem", to:"pool", label:"шторм ретраев"},
    {from:"breaker", to:"idem", label:"меняет нагрузку"},
    {from:"shutdown", to:"pool", label:"дренаж гонится"},
    {from:"mw", to:"lifecycle", label:"устанавливает"},
  ]}
  caption="Каждый юнит — дисциплина под частичным отказом. Взаимодействия, а не один механизм, рождают тяжелейшие баги."
/>

<KeyTakeaway>Семь бэкенд-юнитов каждый изолировал одну заботу — request lifecycle, middleware и DI, async против блокирующего I/O, пулинг, идемпотентность и ретраи, circuit breakers и bulkheads, graceful shutdown — и эта изоляция была необходима, чтобы выучить каждую, но это ложь ради удобства, потому что продакшн запускает все семь разом, на одном запросе, под одной нагрузкой, во время одного деплоя. Каждый из семи на самом деле — дисциплина корректного поведения под частичным отказом и нагрузкой, ровно условия, где они сталкиваются. Композиция — это не сложение: сделать все семь верно не суммируется в корректный бэкенд, потому что каждый механизм меняет среду, в которой работают остальные — корректное поведение пула при исчерпании (заставлять вызывающих ждать) читается circuit breaker'у как медлительность, размыкание breaker меняет нагрузку, что генерирует слой ретраев, и ни один из этих механизмов не ошибается индивидуально. Тяжелейшие продовые баги поэтому живут в швах между механизмами, не внутри одного, потому что взаимодействие не существует, пока все части не запустятся вместе, и не находится инспекцией частей. Это тот же вывод, к которому юниты про circuit breaker и graceful shutdown пришли каждый со своей стороны: локальная корректность не складывается в глобальную корректность даром, потому что режимы отказа живут во взаимодействиях. Этот юнит делает те взаимодействия конкретными и добавляет связующую ткань, что ранние юниты предполагали — наблюдаемость, чтобы видеть систему как одно целое, контроль нагрузки, чтобы она деградировала как одно целое — завершаясь ревью готовности.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="cap-overview-retrieval"
  lang="ru"
  questions={[
    { q: "Какую единую форму разделяют семь бэкенд-юнитов и почему их изоляция вводит в заблуждение?", a: "Каждый юнит брал одну точку, где бэкенд встречает реальность под давлением: request lifecycle (приём, маршрут, ответ и хвост латентности), middleware и DI (подключение сквозных забот так, чтобы обработчик оставался тонким), async против блокирующего I/O (один event loop, обслуживающий тысячи запросов, и что один блок делает со всеми ними), пулинг (ограничение и защита дорогих соединений), идемпотентность и ретраи (превращение at-least-once в effectively-once), circuit breakers и bulkheads (прекращение долбёжки падающей зависимости и изоляция радиуса поражения) и graceful shutdown (остановка без потери работы в полёте). Каждый из них — дисциплина корректного поведения под частичным отказом и нагрузкой. Изолировать их было необходимо, чтобы выучить каждый, но это вводит в заблуждение, потому что продакшн никогда не запускает одну заботу за раз — он запускает все семь одновременно на одном запросе, одной нагрузке, одном деплое, ровно когда они сталкиваются. Соединение, что пул выдаёт, — то, что shutdown должен сдренировать; ретрай, что идемпотентность делает безопасным, — тот, что по флоту исчерпывает пул и размыкает breaker." },
    { q: "Почему «композиция — это не сложение» — главный тезис и где он отзывается в ранних юнитах?", a: "Наивная модель аддитивна — сделай все семь верно и у тебя корректный бэкенд, как семь зелёных тестов намекают на работающую систему. Но корректность не складывается чисто, потому что каждый механизм меняет среду, в которой работают остальные. Разумно размеренный пул корректен в одиночку, но его корректное поведение при исчерпании — заставлять вызывающих ждать; это ожидание circuit breaker читает как медлительность; размыкание breaker меняет нагрузку, что генерирует слой ретраев. Ни один из этих механизмов не ошибается — сюрприз живёт во взаимодействии. Готовность к продакшену поэтому — эмерджентное свойство системы, не проверяемое свойство одного механизма, так что нельзя дойти до неё юнит-тестами и нельзя найти тяжелейшие баги инспекцией частей. Это тот же вывод, к которому юнит про circuit breaker пришёл о распределённых режимах отказа, а юнит про graceful shutdown — о флотовых деплоях: локальная корректность не складывается в глобальную даром, потому что режимы отказа живут во взаимодействиях. Сеньорная бэкенд-работа непропорционально про эти швы — держать весь граф взаимодействий в голове, зная, что изменение бюджета таймаута рябью идёт в breaker, слой ретраев и дедлайн shutdown разом." },
  ]}
/>

<Recap lang="ru">
Семь юнитов, семь чистых комнат — и этот сносит стены. Каждый ранний юнит изолировал одну заботу, чтобы её можно было выучить, но продакшн-бэкенд запускает все семь разом на одном запросе, нагрузке и деплое, и каждый из них на самом деле — дисциплина корректного поведения под частичным отказом и нагрузкой, ровно условия, где они сталкиваются. Так что композиция — это не сложение: корректные части не суммируются в корректное целое, потому что каждый механизм меняет среду, в которой работают остальные, и тяжелейшие баги живут в швах, что ни один юнит не мог показать. Это тот же урок, к которому пришли юниты про circuit breaker и graceful shutdown — локальная корректность не складывается в глобальную даром. Остаток юнита делает швы конкретными: один запрос, прослеженный через каждый слой, режимы отказа, наблюдаемые в композиции, наблюдаемость, добавленная чтобы видеть систему как одно целое, контроль нагрузки, чтобы она деградировала как одно целое, и ревью готовности, что превращает весь трек в чеклист. Следующий урок начинает там, где должен — следуя за одним запросом весь путь вниз по стеку. Теперь, когда встретишь утверждение «все семь юнитов реализованы корректно» — первый вопрос не «работает ли каждый в изоляции?», а «что происходит, когда все они работают одновременно под одной нагрузкой, на одном запросе, во время одного деплоя?».
</Recap>
