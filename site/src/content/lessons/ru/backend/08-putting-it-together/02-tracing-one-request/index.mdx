---
slug: 02-tracing-one-request
lang: ru
track: backend
unit: 08-putting-it-together
order: 2
title: "Проследи один запрос: каждый юнит на одном пути кода"
summary: "Проведи один запрос через стек — и семь юнитов встают слоями: lifecycle принимает и парсит, middleware и DI подключают зависимости, async I/O и пул несут даунстрим-вызов, breaker его охраняет, ключ идемпотентности делает запись безопасной для ретрая, а shutdown ждёт её."
estMin: 16
status: ready
lessonType: topic
level: middle
prereqs:
  - backend/08-putting-it-together/01-overview
deepensInto:
  - backend/08-putting-it-together/03-when-failures-compose
spiral: ["synthesis", "lifecycle", "resource-limits", "idempotency"]
concepts: ["request-path", "layering", "end-to-end-trace", "composition", "timeout-budget"]
sources:
  - https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/
  - https://sre.google/sre-book/handling-overload/
  - https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";

<Hook>
Чистейший способ увидеть семь механизмов как одну систему — перестать говорить о них абстрактно и проследить один запрос весь путь вниз. Возьми конкретный: `POST /payments`, списать с карты, записать результат. Этот запрос не посещает один юнит — он касается *всех* их, по порядку, на одном пути кода. Он приходит на сокет, что юнит lifecycle научил тебя принимать и парсить. Он проходит через цепочку middleware, что аутентифицирует его и стартует таймаут, с зависимостями, поданными DI-контейнером. Обработчик бежит на event loop, что не должен блокироваться, так что вызов БД асинхронен; этот вызов одалживает соединение из пула; соединение пула говорит с платёжным провайдером через circuit breaker; запись, что он делает, охранена ключом идемпотентности, так что ретрай не может списать дважды. И под всем этим обработчик graceful shutdown ждёт — если SIGTERM приземлится, пока этот запрос в полёте, каждый слой, что он коснулся, должен размотаться в правильном порядке. Один запрос, семь юнитов, один непрерывный путь. Проследи его раз — и тезис «система, не стопка» из прошлого урока перестаёт быть лозунгом и становится вещью, на которую можно показать пальцем.
</Hook>

<Crux>Проследи один запрос — и каждый юнит встаёт по порядку: приём, middleware, async I/O, пуловый вызов под breaker, идемпотентная запись.</Crux>

<Explanation>

## Счастливый путь, слой за слоем

Проследи `POST /payments` от сокета до ответа — и юниты предстают как стопка слоёв, каждый передаёт следующему:

1. **Приём и парсинг (lifecycle).** Сервер принимает соединение, читает запрос с сокета, парсит заголовки и тело. Backpressure и лимиты размера тела живут здесь — кривое или раздутое тело отвергается до того, как стоит хоть что-то даунстриму.
2. **Middleware и DI.** Запрос проходит через цепочку: логирование, auth, rate-limit и, что критично, место, где **бюджет таймаута** ставится на весь запрос. DI-контейнер подаёт обработчику его зависимости — платёжный клиент, репозиторий — уже сконструированные и заскоупленные под этот запрос.
3. **Обработчик на event loop (async I/O).** Обработчик бежит, не блокируя loop. Каждый I/O-вызов await'ится, так что пока этот запрос ждёт БД или провайдера, тот же loop обслуживает тысячи других запросов.
4. **Одолжить соединение (пул).** Запись в БД нуждается в соединении, так что обработчик берёт одно из пула — с таймаутом получения, так что насыщенный пул падает быстро, а не висит вечно.
5. **Вызов провайдера через breaker.** Списание идёт к внешнему платёжному провайдеру, обёрнутое в circuit breaker и его собственный таймаут. Если провайдер падает, breaker коротит и обработчик берёт свой фолбэк, а не наваливает сверху.
6. **Записать идемпотентно.** Результат персистится под ключом идемпотентности, так что если клиент переретраит тот же `POST`, сервер вернёт исходный результат, а не спишет дважды.
7. **Ответить, затем shutdown ждёт.** Ответ пишется обратно. Если SIGTERM пришёл посреди списания, обработчик shutdown держал дверь: он перестал принимать новые запросы, держал этот живым и снесёт пул и loop только когда этот запрос ответит.

## Бюджет таймаута пронизывает всё это

Единая нить, что связывает эти слои, — **дедлайн**. Таймаут, поставленный в middleware (шаг 2), не только для обработчика — он должен быть *разделён* между всем даунстримом. Таймаут получения из пула, таймаут вызова провайдера и таймаут записи в БД все должны влезть *внутрь* общего бюджета запроса, с запасом. Если бюджет запроса 3 секунды, нельзя дать вызову провайдера 3-секундный таймаут, потому что тогда медленное получение плюс медленная запись пролетают дедлайн без времени ответить. Это урок про хвост латентности из юнита lifecycle, и урок про таймаут получения из юнита пулинга, и урок про таймаут вызова из юнита breaker, все примирённые на одном запросе: бюджеты вложены, не независимы, и должны суммироваться меньше целого.

## Один путь, один порядок

Заметь, порядок вынужден, не произволен. Ты парсишь до маршрутизации, получаешь соединение до его использования, ставишь breaker вокруг вызова, что он охраняет, и пишешь идемпотентно до ответа. И shutdown разматывает этот же граф *в обратную* — перестать принимать, дренировать в полёте, затем закрыть пул последним — что ровно правило обратного порядка зависимостей из юнита graceful shutdown. Путь запроса и путь shutdown — один и тот же граф зависимостей, прочитанный в противоположных направлениях.

<Inset kind="why" lang="ru">
Почему бюджет таймаута должен делиться вниз по стеку, а не просто ставиться один щедрый таймаут на весь запрос, давая каждому вызову брать сколько нужно? Потому что один внешний таймаут говорит тебе, *что* запрос был слишком медленным, но даёт каждому внутреннему слою разрешение съесть весь бюджет в одиночку, что делает сбой и более поздним, и менее диагностируемым. Если у запроса 3 секунды, а у вызова провайдера нет своего таймаута, зависший провайдер держит соединение, слот loop и сам запрос все полные 3 секунды до срабатывания внешнего дедлайна — и в течение этих 3 секунд то пуловое соединение недоступно всем остальным, так что одна медленная зависимость тихо осушает пул. Вложенные бюджеты чинят обе проблемы: дай получению из пула несколько сотен миллисекунд, вызову провайдера секунду-другую, записи свой ломоть, каждому с запасом, так что *самая внутренняя* медленная вещь падает первой, быстро и конкретно, освобождая свои ресурсы обратно системе, пока ещё есть время взять фолбэк и ответить внутри внешнего дедлайна. Вот почему юниты lifecycle, пулинга и breaker каждый настаивал на таймауте со своего угла — все они описывали один общий бюджет, видимый с разных слоёв. Более глубокая идея в том, что таймаут — не страховочная сетка, что ставишь раз наверху; это *контракт освобождения ресурсов*, обеспеченный на каждом слое, и контракт держится, лишь если внутренние дедлайны строго меньше внешнего. Ошибись с вложением — и каждый другой механизм деградирует: пул не может защитить себя, breaker не видит чистого таймаута для счёта, а shutdown не может предсказать, сколько займёт дренаж.
</Inset>

<div data-lesson-visual class="overflow-x-auto my-6">

| Шаг | Слой | Юнит | Страж на этом шаге |
|---|---|---|---|
| Приём и парсинг | Lifecycle | 01 | Лимит размера тела, backpressure |
| Auth, ставим дедлайн | Middleware / DI | 02 | Бюджет таймаута запроса |
| Бежит обработчик | Event loop | 03 | Никогда не блокировать loop |
| Одолжить соединение | Пул | 04 | Таймаут получения |
| Вызов провайдера | Breaker | 06 | Breaker + таймаут вызова |
| Персистить результат | Идемпотентность | 05 | Ключ идемпотентности |
| Ответить / размотать | Shutdown | 07 | Дренаж в полёте, закрыть пул последним |

</div>

<Quiz
  id="cap-trace-q1"
  lessonSlug="02-tracing-one-request"
  lang="ru"
  question="У запроса POST /payments общий бюджет 3 секунды, поставленный в middleware. Вызову провайдера дан свой 3-секундный таймаут без меньших внутренних дедлайнов. Что идёт не так?"
  choices={[
    { label: "Зависший провайдер может съесть все 3 секунды, держа своё пуловое соединение и слот loop, так что внешний дедлайн срабатывает слишком поздно, чтобы взять фолбэк или ответить — внутренние таймауты должны быть строго меньше внешнего бюджета", correct: true },
    { label: "Ничего — одного таймаута на весь запрос достаточно", misconception: "Один внешний таймаут даёт каждому внутреннему слою съесть весь бюджет в одиночку, делая сбой поздним, недиагностируемым и осушающим ресурсы; бюджеты должны быть вложены." },
    { label: "Запрос всегда закончится быстрее с одним большим таймаутом", misconception: "В плохом случае он заканчивается медленнее: самый внутренний медленный вызов держит ресурсы весь бюджет вместо того, чтобы упасть быстро и освободить их." },
    { label: "Ключ идемпотентности делает таймаут неважным", misconception: "Идемпотентность делает ретрай безопасным, но ничего не делает с зависшим вызовом, держащим соединение весь бюджет; это проблема вложения таймаутов." },
  ]}
/>

<Quiz
  id="cap-trace-q2"
  lessonSlug="02-tracing-one-request"
  lang="ru"
  question="Как путь запроса связан с путём разборки при graceful shutdown?"
  choices={[
    { label: "Это один граф зависимостей, прочитанный в противоположных направлениях: запрос получает ресурсы снаружи-внутрь (парс, пул, вызов, запись), а shutdown освобождает их в обратную — перестать принимать, дренировать в полёте, закрыть пул последним", correct: true },
    { label: "Они не связаны; shutdown закрывает всё разом", misconception: "Закрытие всего разом ломает запросы в полёте; shutdown следует обратному порядку зависимостей, что ровно путь запроса наоборот." },
    { label: "Shutdown закрывает пул первым, затем дренирует запросы", misconception: "Закрытие пула первым значит, что запрос в полёте врезается в мёртвый пул; хранилище закрывается последним, после того как всё, что его использует, сдренировано." },
    { label: "У пути запроса нет определённого порядка", misconception: "Порядок вынужден: парсишь до маршрутизации, получаешь до использования, оборачиваешь breaker вокруг его вызова — и shutdown обращает этот же вынужденный граф." },
  ]}
/>

<DragOrder
  id="cap-trace-drag1"
  lessonSlug="02-tracing-one-request"
  lang="ru"
  prompt="Расставь один запрос POST /payments, спускающийся по стеку:"
  items={[
    "Принять соединение и распарсить тело (lifecycle), обеспечивая лимиты размера",
    "Прогнать middleware: auth, затем поставить общий бюджет таймаута запроса; DI подаёт зависимости",
    "Получить пуловое соединение с таймаутом получения внутри бюджета",
    "Вызвать провайдера через circuit breaker, затем персистить результат под ключом идемпотентности",
  ]}
/>

</Explanation>

<KeyTakeaway>Следование за одним запросом — POST /payments, списать с карты, записать результат — делает семь юнитов одним упорядоченным путём кода. Слой lifecycle принимает соединение и парсит тело с лимитами размера и backpressure; middleware аутентифицирует и ставит общий бюджет таймаута запроса, пока DI подаёт зависимости обработчика, уже заскоупленные; обработчик бежит на event loop, не блокируя, так что loop обслуживает тысячи других запросов, пока этот ждёт; он одалживает соединение из пула с таймаутом получения, так что насыщенный пул падает быстро; он вызывает платёжного провайдера через circuit breaker со своим таймаутом вызова, коротя на фолбэк, если провайдер падает; он персистит результат под ключом идемпотентности, так что ретрай клиента возвращает исходный результат вместо двойного списания; и под всем этим обработчик graceful shutdown держит дверь, так что SIGTERM посреди списания держит этот запрос живым и сносит всё только после ответа. Нить, связывающая каждый слой, — дедлайн: таймаут, поставленный в middleware, должен делиться вниз по стеку, так что таймауты получения из пула, вызова провайдера и записи каждый влезает внутрь общего бюджета с запасом — вложены, не независимы, суммируясь меньше целого, так что самая внутренняя медленная вещь падает первой, быстро, и освобождает свои ресурсы, пока ещё есть время ответить. Порядок вынужден (парс до маршрута, получение до использования, breaker вокруг своего вызова), а graceful shutdown разматывает тот же граф зависимостей в обратную — перестать принимать, дренировать в полёте, закрыть пул последним. Путь запроса и путь shutdown — один граф, прочитанный в обе стороны.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="cap-trace-retrieval"
  lang="ru"
  questions={[
    { q: "Проведи POST /payments вниз по стеку и назови стража на каждом слое.", a: "Приём и парсинг (lifecycle): сервер читает запрос с сокета и парсит его, обеспечивая лимиты размера тела и backpressure, так что кривое или раздутое тело отвергается до того, как стоит хоть что-то даунстриму. Middleware и DI: запрос проходит через логирование, auth и rate-limit, и, что критично, место, где ставится общий бюджет таймаута; DI-контейнер подаёт обработчику его зависимости, сконструированные и заскоупленные под этот запрос. Обработчик на event loop (async I/O): каждый I/O-вызов await'ится, так что loop продолжает обслуживать тысячи других запросов, пока этот ждёт. Пул: запись одалживает соединение с таймаутом получения, так что насыщенный пул падает быстро, а не висит. Breaker: списание провайдера обёрнуто в circuit breaker и его таймаут вызова, коротя на фолбэк, если провайдер падает. Идемпотентность: результат персистится под ключом идемпотентности, так что переретраенный POST возвращает исходный результат вместо двойного списания. Shutdown: ответ пишется, и если SIGTERM пришёл посреди списания, обработчик shutdown держал этот запрос живым и сносит пул и loop только после ответа." },
    { q: "Почему бюджет таймаута должен быть вложен вниз по стеку и как это связывает юниты lifecycle, пулинга и breaker?", a: "Таймаут, поставленный в middleware, — общий бюджет запроса, и он должен делиться между всем даунстримом, потому что один щедрый внешний таймаут даёт каждому внутреннему слою разрешение съесть весь бюджет в одиночку — делая сбой более поздним, менее диагностируемым и осушающим ресурсы. Если у запроса 3 секунды, а у вызова провайдера нет внутреннего таймаута, зависший провайдер держит своё пуловое соединение, слот loop и запрос все полные 3 секунды до срабатывания внешнего дедлайна, и за это время соединение недоступно всем остальным, так что одна медленная зависимость тихо осушает пул. Вложенные бюджеты — несколько сотен миллисекунд на получение, около секунды на вызов провайдера, ломоть на запись, каждому с запасом — делают самую внутреннюю медленную вещь падающей первой, быстро и конкретно, освобождая её ресурсы, пока ещё есть время взять фолбэк и ответить внутри внешнего дедлайна. Вот почему юниты lifecycle (хвост латентности), пулинга (таймаут получения) и breaker (таймаут вызова) каждый настаивал на таймауте со своего угла: они описывали один общий бюджет, видимый с разных слоёв, и таймаут на самом деле — контракт освобождения ресурсов, что держится, лишь если внутренние дедлайны строго меньше внешнего." },
  ]}
/>

<Recap lang="ru">
Чтобы увидеть систему вместо стопки, ты следуешь за одним запросом — POST /payments — от сокета до ответа, и семь юнитов встают одним упорядоченным путём: приём и парсинг, middleware и DI ставят дедлайн и подключают зависимости, обработчик бежит асинхронно на loop, он одалживает пуловое соединение, вызывает провайдера через breaker, пишет под ключом идемпотентности, отвечает, а обработчик shutdown ждёт под всем этим всё время. Дедлайн — нить, что сшивает слои: бюджет запроса, поставленный в middleware, должен делиться так, чтобы каждый внутренний таймаут — получение, вызов провайдера, запись — влез внутрь него с запасом, вложенный, а не независимый, так что самая внутренняя медленная вещь падает первой и освобождает свои ресурсы вовремя для ответа. Порядок вынужден, и shutdown разматывает ровно тот же граф зависимостей в обратную — перестать принимать, дренировать в полёте, закрыть пул последним — так что путь запроса и путь разборки — один граф, прочитанный в обе стороны. Мы теперь видели все семь кооперирующимися на счастливом пути; следующий урок поднимает давление и смотрит, как они падают вместе, где корректное поведение одного механизма становится плохим входом следующего.
</Recap>
