---
concepts:
- composition
- failure-cascade
- timeout-budget
- load-shedding
- observability
- production-readiness
estMin: 14
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://sre.google/sre-book/addressing-cascading-failures/
- https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/
- https://aws.amazon.com/builders-library/using-load-shedding-to-avoid-overload/
status: ready
summary: "MCQ на синтез по всему backend-капстоуну: композиция вместо сложения, latency-каскад, вложенный timeout budget, goodput против throughput, хвост p99 и readiness как чек-лист."
title: 'Собираем воедино: тест с выбором ответа'
track: backend
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов, которые разом проходят поперёк всего backend-трека. Ни один не проверяет один механизм в изоляции — каждый сводит два-три из них на одном запросе под одной нагрузкой и просит прочитать взаимодействие так, как ты делал бы это в реальном инциденте.
</Hook>

<Goal lang="ru">
Убедись, что рассуждаешь о семи механизмах как об одной составной системе: как корректная часть становится плохим входом для другой, как timeout budget прошивает стек, почему goodput важнее throughput и почему readiness проверяют, а не чувствуют.
</Goal>

<Quiz
  id="08-putting-it-together-choice-1"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Каждый из семи backend-механизмов проходит свои тесты, но самые тяжёлые инциденты команды берутся из мест, которые ни один механизм по отдельности не объясняет. Каков диагноз капстоуна?"
  choices={[
    { label: "Продакшен запускает все семь забот сразу на одних запросах и нагрузке, поэтому самые тяжёлые сбои — эмерджентные свойства взаимодействий: композиция не равна сложению, и корректные части складываются в некорректное поведение.", correct: true },
    { label: "Один из семи механизмов наверняка тонко сломан; найти и починить.", misconception: "Каждый механизм может быть корректен по отдельности, а система всё равно падает. Баг живёт в швах между механизмами, а не внутри какого-то одного — сломанной части просто нет." },
    { label: "Команде не хватает восьмого механизма, которого она ещё не выучила.", misconception: "Дело в композиции имеющихся механизмов, а не в недостающем. Юнит добавляет observability и load control как соединительную ткань, связывающую семь, а не новую изолированную заботу." },
    { label: "Прохождение unit-тестов на каждом механизме гарантирует корректность составной системы.", misconception: "Production-ready — эмерджентное свойство системы, не проверяемое осмотром частей; корректные части не суммируются в корректное целое, потому что каждый механизм меняет среду, в которой работают остальные." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-2"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="p99 платёжного провайдера растёт с 40 мс до 4 с — без ошибок, просто медленные ответы. Через минуты медленными становятся даже эндпоинты, которые провайдер не вызывают. Чьё корректное поведение распространило сбой и через что?"
  choices={[
    { label: "Каждый медленный вызов держит свою pooled-соединение все 4 с, поэтому ограниченный pool опустошается; теперь любой запрос — даже несвязанный — блокируется на acquire соединения. Сбой генерализуется через общий pool, а не через прямую зависимость.", correct: true },
    { label: "Медленный провайдер напрямую замедляет базу, а она — всё остальное.", misconception: "База здорова. Связь — это общий pool и event loop, а не зависимость между провайдером и базой: сбой расходится через разделяемые ресурсы, а не через сами downstream-зависимости." },
    { label: "Несвязанные эндпоинты замедлило срабатывание breaker.", misconception: "Срабатывание breaker сбрасывает вызовы провайдера и освобождает соединения; оно облегчает каскад, а не вызывает генерализацию. Сначала медлительность разносит опустошение pool на удержанных соединениях." },
    { label: "Несвязанные эндпоинты, видимо, тоже втайне зовут провайдера.", misconception: "Им вообще не нужно трогать провайдера. Они тормозят, потому что конкурируют за тот же исчерпанный pool — именно так сбой одной зависимости становится проблемой всех." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-3"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="У обработчика POST /payments общий бюджет 3 секунды, заданный в middleware, но downstream-вызов провайдера, acquire из pool и запись в БД несут каждый свой таймаут в 3 секунды. Почему это неверно, хотя каждый таймаут по отдельности выглядит разумно?"
  choices={[
    { label: "Внутренние дедлайны должны быть строго меньше внешнего бюджета и в сумме, с запасом, меньше его; если каждый внутренний вызов может съесть все 3 с, один зависший шаг держит своё соединение и слот в loop весь бюджет, и внешний дедлайн срабатывает слишком поздно для fallback или ответа.", correct: true },
    { label: "Всё нормально — каждому слою стоит дать полный бюджет, чтобы ничего не обрезалось рано.", misconception: "Полный бюджет каждому внутреннему слою позволяет любому из них съесть его в одиночку — сбой становится поздним, хуже диагностируемым и истощающим ресурсы. Бюджеты вложены, а не независимы." },
    { label: "Idempotency key делает вложенность таймаутов неважной.", misconception: "Idempotency делает retry безопасным; он ничего не делает с зависшим вызовом, удерживающим pooled-соединение весь бюджет. Это проблема вложенности таймаутов, а не корректности retry." },
    { label: "Решение — поднять внешний бюджет до 9 секунд, чтобы все три влезли.", misconception: "Тогда каждый запрос потенциально медленный на 9 с, и зависший шаг всё равно голодает pool столько же. Решение — меньшие вложенные внутренние дедлайны, чтобы самое внутреннее медленное упало первым и вовремя освободило ресурсы." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-4"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Во время каскада медленный провайдер полностью восстановился — задержка вернулась к 40 мс — но сервис остаётся в коллапсе. О чём это говорит и каков правильный ответ?"
  choices={[
    { label: "Сбой метастабилен: накопившаяся очередь и продолжающиеся retry теперь поддерживают каскад независимо от исходного триггера, поэтому починка зависимости не помогает — надо атаковать петлю обратной связи, сбрасывая нагрузку и ограничивая retry, пока очередь не сливается быстрее, чем наполняется.", correct: true },
    { label: "Провайдер на самом деле не восстановился; метрики врут.", misconception: "Даже при по-настоящему восстановившемся провайдере система остаётся в коллапсе, потому что созданные ею очередь и retry теперь сами питают петлю. Эта самоподдерживающаяся петля — и есть определение метастабильности." },
    { label: "Breaker застрял открытым и ему просто нужно ещё время, чтобы закрыться.", misconception: "Ожидание не помогает метастабильному состоянию; петля самоподдерживающаяся. Нужна активная интервенция — сбросить нагрузку, слить очереди, обрезать retry — а не терпение." },
    { label: "Перезапуск одного пода очистит весь каскад.", misconception: "Один рестарт не сливает очередь на весь флот и не останавливает retry клиентов. Метастабильным сбоям нужны load shedding и контроль retry по всей системе, а не один bounce." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-5"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Сервис прошёл точку насыщения — спрос превышает ёмкость — и сейчас принимает и ставит в очередь каждый запрос. Почему добавление быстрого load shedding (503 с Retry-After на краю) увеличивает число хорошо обслуженных пользователей?"
  choices={[
    { label: "Принятые-но-незавершаемые запросы держат соединения и удлиняют очереди, утягивая запросы в пределах ёмкости за их собственные дедлайны, пока и они не станут отходами; дешёвый сброс лишнего на admission держит очереди короткими, и принятые запросы успевают в дедлайн как goodput.", correct: true },
    { label: "Shedding навсегда снижает общий спрос, так что серверу меньше работы.", misconception: "Отклонённые запросы не исчезают из мира — клиенты могут повторять — но дешёвый сброс на входе защищает ёмкость для принятых запросов. Выигрыш — goodput, а не снижение спроса." },
    { label: "503 дешевле вычислить, чем реальный ответ, поэтому throughput растёт.", misconception: "Механизм — управление очередью, а не скорость ошибки. Непринятие лишней работы держит очереди короткими, и запросы в пределах ёмкости успевают вовремя; дешёвый отказ лишь это позволяет." },
    { label: "Load shedding поднимает фактическую ёмкость сервера C.", misconception: "Ёмкость задана железом и downstream-зависимостями. Shedding не поднимает C; он не даёт тратить C на работу сверх неё, сохраняя goodput в пределах фиксированной ёмкости." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-6"
  lessonSlug="08-putting-it-together"
  lang="ru"
  question="Readiness-ревью сложного нового платёжного сервиса возвращает «все восемь гейтов полностью зелёные, без оговорок». Почему senior-ревьюер скорее скептичен, чем успокоен?"
  choices={[
    { label: "Глубина readiness калибруется по blast radius, и реальные сервисы включают осознанные компромиссы; «всё зелёное, без оговорок» на сложном сервисе обычно значит, что кто-то был нечестен о том, какие гейты приоткрыты и почему — чек-лист подогнали, а не продумали.", correct: true },
    { label: "«Всё зелёное» невозможно, потому что ни один сервис не бывает безопасен для запуска.", misconception: "Сервисы запускают безопасно. Скепсис не в том, что безопасность невозможна, а в том, что сложный новый сервис с нулём оговорок сигналит о недокументированных компромиссах, а не о настоящей полноте." },
    { label: "Новый сервис всегда должен возвращать «всё красное», пока не поработает в продакшене.", misconception: "Фиксированного цвета вердикта нет. Суть — честная калибровка по blast radius с явными оговорками, а не сплошной пессимизм." },
    { label: "Восьми гейтов просто слишком мало для оценки платёжного сервиса.", misconception: "Дело не в числе гейтов; дело в честной глубине каждого гейта пропорционально цене сбоя, с явно проговорёнными компромиссами, а не замазанными." },
  ]}
/>

<Recap lang="ru">
Сквозная линия трека — одно утверждение: композиция не равна сложению. Корректный pool, опустошающийся под медленным downstream, становится плохим входом для breaker; корректный retry становится штормом; корректный drain гонится со застрявшей in-flight задачей. Timeout budget должен вкладываться вниз по стеку, чтобы самое внутреннее медленное упало первым; под overload ты защищаешь goodput, сбрасывая лишнее намеренно; управлять можно лишь тем, что видишь на хвосте p99, а не на среднем; и readiness — это чек-лист, калиброванный по blast radius, а не чувство. Senior-работа в backend — читать граф взаимодействий до того, как он каскадирует.
</Recap>
