---
concepts:
- composition
- timeout-budget
- metastability
- goodput
- observability
- production-readiness
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://sre.google/sre-book/addressing-cascading-failures/
- https://sre.google/sre-book/handling-overload/
- https://sre.google/sre-book/reliable-product-launches/
status: ready
summary: "Промпты на свободное припоминание по всему backend-треку — композиция против сложения, вложенный timeout budget, метастабильный каскад, goodput против throughput, RED и хвост p99, readiness как чек-лист."
title: 'Собираем воедино: тест на припоминание'
track: backend
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание сильнее перечитывания. На каждый промпт восстанови полный ответ по всему треку из памяти, прежде чем открыть модельный ответ — усилие по стягиванию связей обратно и закрепляет синтез.
</Hook>

<Goal lang="ru">
Восстанови хребет юнита, не подглядывая: почему композиция не равна сложению, как вкладывается timeout budget, что делает каскад метастабильным, почему goodput — цель под overload, как RED и перцентили делают систему видимой и почему readiness — это чек-лист.
</Goal>

<RetrievalDrawer
  client:load
  id="08-putting-it-together-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему «композиция не равна сложению» — ядро backend-трека, и где живут самые тяжёлые баги?", a: "Наивная модель аддитивна: реализуй все семь механизмов корректно — и получишь корректный backend, как семь зелёных unit-тестов как бы сулят зелёную систему. Но корректность не складывается чисто, потому что каждый механизм меняет среду, в которой работают остальные. Разумно размеренный pool корректен сам по себе, но его корректное поведение при исчерпании — заставлять вызывающих ждать; это ожидание circuit breaker читает как медлительность; срабатывание breaker меняет нагрузку, которую генерирует слой retry. Ни один из механизмов не сломан — сюрприз живёт во взаимодействии. Поэтому production-ready — эмерджентное свойство системы, а не проверяемое свойство одного механизма: его нельзя достичь через unit-тесты, и самые тяжёлые баги живут в швах между механизмами, а не внутри какого-то одного, потому что взаимодействие не существует, пока все части не работают вместе под нагрузкой." },
    { q: "Проведи один запрос POST /payments вниз по стеку, назови страж на каждом слое и объясни, почему timeout budget должен вкладываться.", a: "Accept и parse (lifecycle): прочитать с сокета и распарсить, соблюдая лимит размера тела и backpressure. Middleware и DI: аутентифицировать, задать общий timeout budget запроса, DI-контейнер выдаёт scoped-зависимости. Обработчик на event loop (async I/O): каждый I/O-вызов await, чтобы loop продолжал обслуживать тысячи других запросов. Pool: взять соединение с acquisition timeout, чтобы исчерпанный pool падал быстро. Breaker: обернуть вызов провайдера в breaker и его собственный call timeout, замыкаясь на fallback, если провайдер сбоит. Idempotency: сохранить под idempotency key, чтобы повторный POST вернул исходный результат, а не списал дважды. Shutdown: записать ответ, и SIGTERM посреди charge держит запрос живым, разбирая только после ответа. Бюджет должен вкладываться, потому что единый щедрый внешний таймаут позволяет любому внутреннему слою съесть весь бюджет в одиночку; зависший провайдер тогда держит соединение, слот в loop и сам запрос весь бюджет, пока внешний дедлайн срабатывает слишком поздно для fallback. Вложенные внутренние дедлайны — acquire, вызов, запись, каждый строго меньше и в сумме с запасом меньше целого — делают самое внутреннее медленное падающим первым, освобождая ресурсы вовремя." },
    { q: "Проведи канонический latency-каскад звено за звеном и объясни, почему путь запроса и путь shutdown — один и тот же граф.", a: "Downstream замедляется (p99 провайдера идёт с 40 мс до 4 с, поздно, а не «down»). Pool опустошается: каждый медленный вызов держит соединение все 4 с, пока ограниченный pool не пуст. Задержка генерализуется: любой запрос, которому нужно соединение — даже не трогающий провайдера — блокируется на acquire, так сбой одной зависимости становится сбоем всего сервиса. Retry усиливают: клиенты и middleware повторяют медленные запросы, но сервис насыщен, поэтому каждый retry — ещё один захват соединения — retry storm. Breaker срабатывает: открывается на задержке провайдера, сбрасывая работу и меняя error rate, который видят все. Drain гонится с in-flight задачей: rolling deploy сливает под, чей graceful-shutdown пытается доделать те же charge, что таймаутят, поэтому drain не укладывается в grace period и SIGKILL забирает недоделанную задачу. Путь запроса берёт ресурсы снаружи-внутрь (parse, pool, вызов, запись); shutdown освобождает их в обратном порядке — перестать принимать, слить in-flight, закрыть pool последним — так что это один граф зависимостей, читаемый в противоположных направлениях." },
    { q: "Что такое метастабильность, почему починка исходной причины её не лечит и что надо делать вместо этого?", a: "Когда каскад запущен, система становится собственным источником нагрузки. Даже если провайдер восстановился до 40 мс, pool всё ещё полон соединений, удерживаемых за очередью, что выросла в медленный период; клиенты всё ещё повторяют, потому что всё ещё видят таймауты очереди; и каждый retry добавляет к этому бэклогу. Петля обратной связи, начатая медленным провайдером, теперь питается целиком очередью и retry, которые она создала — причину заменили её собственные следствия. У системы два устойчивых равновесия, здоровое (короткая очередь, быстрые ответы, мало retry) и схлопнутое (полная очередь, медленные ответы, много retry), и достаточно большой шок перебрасывает её из первого во второе, где она и остаётся. Поэтому нельзя ни переждать, ни вылечить починкой зависимости; надо атаковать петлю — сбросить нагрузку, чтобы очередь сливалась быстрее, чем retry её наполняют, ограничить или отключить retry, контролируемо сбросить breaker, иногда сжать конкурентность. Устойчивость — не отсутствие сбоя, а отсутствие усиливающей обратной связи при сбое." },
    { q: "Под overload, почему отклонение запросов — корректное поведение, и чем goodput отличается от throughput?", a: "У сервера конечная ёмкость C полезной работы в секунду; когда спрос D превышает C, ты не можешь завершить D, а лишь выбрать, что станет с D − C запросами, на которые ёмкости нет. Их принятие не делает их невидимыми — они сидят в очередях, держа ресурсы, поэтому даже те C запросов, что ты мог бы обслужить, ждут за ними и тоже пропускают дедлайны. Принятие работы сверх ёмкости вычитает обслуженные запросы, а не добавляет. Goodput — правильная цель: запросы, завершённые полезно, достаточно быстро, чтобы ответ ещё имел смысл — в отличие от throughput, считающего каждый обработанный запрос, включая отвеченные после того, как клиент уже отвалился по таймауту (чистые отходы, потратившие ресурсы и давшие отрицательную ценность). Дешёвый сброс лишнего на admission — быстрый 503 с Retry-After, с приоритетным дропом наименее ценной нагрузки — держит очереди короткими, и принятые запросы успевают в дедлайн как goodput. Его двойник — backpressure: сигналить наверх замедлиться через ограниченные очереди и fail-fast acquire. И каждый механизм — ручка load control: pool ограничивает работу в полёте, таймауты освобождают обречённую работу, breaker сбрасывает сбойную зависимость, ограниченные retry укрощают усилитель, shutdown сбрасывает уходящий узел, а observability говорит, где ты на кривой goodput." },
    { q: "Что измеряет RED, почему надо смотреть перцентили, а не среднее, и что делает readiness-чек-лист экстернализованной экспертизой, а не бюрократией?", a: "RED — внешний, центрированный на вызывающем взгляд: Rate (запросов в секунду, нагрузка), Errors (доля упавших, включая отказы breaker, а не только 5xx) и Duration (распределение времён ответа, а не среднее). Среднему задержки нельзя верить, потому что оно смешивает быстрые и медленные в число, которого никто не испытывает — 99 запросов по 20 мс и один по 4 с дают ~60 мс; смотри p50, p99 и p999, потому что каскад начинается как растущий p99 задолго до того, как двинется среднее, распределения задержек тяжелохвостые и мультимодальные, а пользователь, делающий 100 запросов на страницу, попадает в свой личный p99 почти на каждой загрузке. Каждый механизм эмитит leading-состояние: pool in-use против limit и acquire wait, loop lag, переходы breaker, retry rate, drain duration. Error budget превращает это в управление выпуском: катить фичи, пока он здоров, и переключаться на надёжность, когда он почти истрачен. Readiness-чек-лист — экстернализованная экспертиза, а не бюрократия: восемь failure mode независимы и не подсказывают друг друга, экспертиза деградирует под усталостью и цейтнотом, поэтому список делает полное рассмотрение надёжным, обучаемым и проверяемым — по той же причине, по которой опытные пилоты читают предполётный чек-лист. Он работает лишь подкреплённый пониманием, которое построил трек, поэтому идёт последним, а глубина readiness калибруется по blast radius с явными оговорками." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ из памяти, ты держишь хребет трека: композиция не равна сложению, поэтому корректные части складываются в некорректное целое; timeout budget вкладывается вниз по стеку, чтобы самое внутреннее медленное упало первым; каскад уходит в метастабильность и ломается на петле обратной связи, а не на причине; goodput, а не throughput, — цель под overload, защищаемая намеренным сбросом; RED и хвост p99 делают составную систему видимой; и readiness — это калиброванный по blast radius чек-лист, имеющий смысл лишь для того, кто понимает каждый перечисленный гейт.
</Recap>
