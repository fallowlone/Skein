---
concepts:
- idempotency
- idempotency-key
- at-least-once
- at-most-once
- retry-safe
deepensInto:
- backend/05-idempotency-retries/02-server-state-machine
estMin: 10
lang: ru
lessonType: topic
level: junior
order: 1
prereqs:
- backend/03-async-blocking
- backend/04-pooling
slug: 01-why-idempotency
sources:
- https://stripe.com/blog/idempotency
- https://docs.stripe.com/api/idempotent_requests
- https://brandur.org/idempotency-keys
spiral:
- statefulness
status: ready
summary: Сеть может проглотить ответ сервера. Без idempotency key клиент не может
  безопасно повторить запрос — каждый дублирующий POST рискует выполнить side effect
  дважды.
title: 'Зачем идемпотентность: безопасные retry'
track: backend
unit: 05-idempotency-retries
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Бея нажимает "Оплатить" на странице оформления заказа. Wifi пропадает до того, как ответ долетает. Она нажимает снова. У сервера нет способа отличить второй клик от первого — он списывает дважды.
</Hook>

<Crux>Как сделать каждый retry-safe POST ценой ровно одного исхода для пользователя, даже если сеть проглотила ответ.</Crux>

<Explanation>

## Проблема сетевых гарантий

Интернет гарантирует **at-least-once** доставку, если клиент повторяет запросы до получения ответа, и **at-most-once**, если никогда не повторяет. Ни то ни другое само по себе не работает:

- **At-most-once** — платёж, который не прошёл, потому что клиент сдался.
- **At-least-once без дедупликации** — двойное списание.

Закрыть этот разрыв — задача приложения. Инструмент — идемпотентность.

## Что такое idempotency key

**Idempotency key** — это непрозрачная клиентская строка, обычно UUID v4 (36 символов), передаваемая в HTTP-заголовке `Idempotency-Key`. Клиент выбирает **один ключ на логическую операцию**, а не на запрос. Каждый retry той же операции использует тот же самый ключ.

Сервер использует ключ как основной токен для ответа на вопрос: "я уже это обрабатывал?" Если да — возвращает исходный ответ без повторного запуска бизнес-логики. Если нет — обрабатывает и сохраняет результат.

<div data-lesson-visual class="overflow-x-auto my-6">

| Сценарий | Без ключа | С ключом |
|---|---|---|
| Ответ доставлен | 1 списание | 1 списание |
| Ответ потерян, клиент ретраит 1 раз | 2 списания | 1 списание (replay) |
| Ответ потерян, клиент ретраит 3 раза | 4 списания | 1 списание (replay) |
| Ключ истёк до retry | N/A | Обрабатывается как новый — риск дубликата |

</div>

## Метафора: конверт и журнал

Представь, что отправляешь деньги через курьера. Помечаешь конверт **"Заказ #A7-payment"** и говоришь: "Если этот конверт уже доставили — не доставляй снова". На каждом retry пишешь тот же номер заказа на новом конверте. Получатель проверяет журнал (idempotency cache) и либо доставляет конверт, либо говорит "уже сделано — вот квитанция".

Журнал **помнит**. Без него каждый конверт выглядит новым.

## HTTP-конвенция

Stripe популяризировал заголовок `Idempotency-Key` в 2014 году. Square, PayPal, Adyen и IETF-черновик `draft-ietf-httpapi-idempotency-key-header` следуют той же форме. RFC 9110 §9.2.2 перечисляет идемпотентные по спецификации HTTP-методы (GET, HEAD, PUT, DELETE, OPTIONS) — POST в этом списке нет, именно поэтому заголовок и существует.

<Quiz
  id="why-idem-q1"
  lessonSlug="01-why-idempotency"
  lang="ru"
  question="Какую проблему решает idempotency key?"
  choices={[
    { label: "Шифрование платёжных данных при передаче", misconception: "Шифрование — работа TLS. Idempotency key — про дублирующиеся side effect при retry." },
    { label: "Делает retry POST безопасным — без выполнения side effect дважды", correct: true },
    { label: "Заменяет authentication-токены", misconception: "Аутентификация — отдельная задача. Ключ идентифицирует операцию, а не вызывающего." },
    { label: "Ускоряет первый запрос", misconception: "Цель — корректность при retry, а не скорость." },
  ]}
/>

<Quiz
  id="why-idem-q2"
  lessonSlug="01-why-idempotency"
  lang="ru"
  question="Ненадёжная сеть потеряла ответ на POST /charge. Что безопаснее всего сделать клиенту?"
  choices={[
    { label: "Повторить без особых меток", misconception: "Голые retry могут создать дублирующиеся списания, если сервер уже обработал первый запрос." },
    { label: "Повторить с тем же Idempotency-Key, что и в первой попытке", correct: true },
    { label: "Переключиться на GET для безопасности", misconception: "GET не может создать списание. Смена метода не решает проблему дублирующейся записи." },
    { label: "Подождать час и надеяться, что сервер разобрался", misconception: "Ожидание ничего не меняет в том, прошла ли исходная запись или нет." },
  ]}
/>

<DragOrder
  id="why-idem-drag1"
  lessonSlug="01-why-idempotency"
  lang="ru"
  prompt="Поставь шаги безопасного retry в правильном порядке:"
  items={[
    "Клиент генерирует уникальный Idempotency-Key (UUID v4) для операции",
    "Клиент отправляет POST с заголовком Idempotency-Key",
    "Сеть или сервер падает до того, как клиент получает ответ",
    "Клиент повторяет тот же POST с ТЕМ ЖЕ Idempotency-Key",
    "Сервер узнаёт ключ и проигрывает кешированный ответ или завершает исходную работу",
    "Клиент получает одно подтверждение и прекращает retry",
  ]}
/>

<MetaphorComplete
  id="why-idem-metaphor1"
  lessonSlug="01-why-idempotency"
  lang="ru"
  setup="Заполни пропуск: idempotency key — это как метка на конверте с номером заказа, чтобы получатель проверил _______ перед повторной доставкой."
  accepted={["журнал", "журнале", "реестр", "реестре", "список", "списке", "ledger"]}
  canonical="журнал (или реестр / список — любая запись того, что уже было доставлено)"
  explanation="Суть — server-side память: получатель должен проверить, был ли уже виден ровно этот маркер. Без памяти каждый конверт выглядит новым."
/>

</Explanation>

<SequenceDiagram
  label="Тот же idempotency-key при retry: сервер возвращает сохранённый ответ, без повторного списания"
  hue="var(--d-backend)"
  actors={["Клиент", "Сервер", "Кеш"]}
  messages={[
    { from: "Клиент", to: "Сервер", label: "POST /charge Idempotency-Key: K1" },
    { from: "Сервер", to: "Кеш", label: "INSERT key K1 (in_progress)" },
    { from: "Сервер", to: "Клиент", label: "200 OK — списание выполнено" },
    { from: "Клиент", to: "Сервер", label: "retry POST Idempotency-Key: K1" },
    { from: "Сервер", to: "Кеш", label: "lookup K1" },
    { from: "Кеш", to: "Сервер", label: "status=completed, ответ сохранён" },
    { from: "Сервер", to: "Клиент", label: "200 OK (replay) — нового списания нет" },
  ]}
  caption="Один ключ на логическую операцию. Каждый retry использует тот же ключ; сервер возвращает кешированный ответ без повторного запуска бизнес-логики."
/>

<KeyTakeaway>Idempotency key позволяет клиенту повторять любой POST без того, чтобы сервер делал работу дважды. Один ключ на логическую операцию — каждый retry использует тот же ключ, сервер проигрывает сохранённый ответ.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="why-idem-retrieval"
  lang="ru"
  questions={[
    { q: "В чём разница between at-least-once и at-most-once доставкой, и почему ни то ни другое в одиночку не работает для платёжного API?", a: "At-most-once — клиент не ретраит, поэтому потерянный ответ оставляет платёж незавершённым. At-least-once — клиент ретраит до получения ответа, но без дедупликации каждый retry может создать новое списание. Idempotency key закрывает разрыв: at-least-once доставка плюс server-side дедупликация дают exactly-once наблюдаемое поведение." },
    { q: "Почему правило — один ключ на логическую операцию, а не один ключ на HTTP-запрос?", a: "Каждый retry той же операции должен нести тот же ключ, чтобы сервер мог распознать retry и проиграть исходный ответ. Если клиент генерирует новый ключ для каждой HTTP-попытки, сервер воспринимает каждую попытку как новую операцию и снова списывает. Ключ привязан к намерению, а не к проводной попытке." },
    { q: "Какие HTTP-методы идемпотентны по определению RFC 9110, и почему POST нуждается в заголовке?", a: "RFC 9110 §9.2.2 определяет GET, HEAD, PUT, DELETE и OPTIONS как идемпотентные — многократное применение даёт тот же результат. POST не идемпотентен по спецификации, потому что 'создать списание' — новый side effect каждый раз. Заголовок Idempotency-Key позволяет POST вести себя как идемпотентный, заставляя сервер дедуплицировать по ключу." },
  ]}
/>

<Recap lang="ru">
Интернет не может доставить запрос ровно один раз — только at-most-once (без retry) или at-least-once (retry до подтверждения). Платёжному API нужно exactly-once поведение, для которого обе стороны должны cooperate: клиент ретраит с тем же заголовком Idempotency-Key, сервер дедуплицирует по этому ключу как по основному lookup-токену. Stripe популяризировал заголовок `Idempotency-Key` в 2014 году; IETF-черновик `draft-ietf-httpapi-idempotency-key-header` формализует его. Следующий урок разбирает серверный state machine, который делает это работающим.
</Recap>
