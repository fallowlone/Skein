---
concepts:
- idempotency-key
- state-machine
- full-jitter
- retry-budget
- outbox-inbox
- effectively-once
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://stripe.com/blog/idempotency
- https://brandur.org/idempotency-keys
- https://event-driven.io/en/outbox_inbox_patterns_and_delivery_guarantees_explained/
status: ready
summary: "Промпты на свободное воспроизведение по всему юниту idempotency + retries. Сначала ответь по памяти, затем открой модельный ответ и сравни."
title: 'Идемпотентность и retry: тест на воспроизведение'
track: backend
unit: 05-idempotency-retries
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. На каждый промпт восстанови полный ответ по памяти, прежде чем открыть модельный — именно усилие вытащить механизм обратно делает так, что он держится под давлением инцидента.
</Hook>

<Goal lang="ru">
Восстанови хребет юнита, не подглядывая: почему exactly-once — иллюзия, машина ключа из четырёх состояний, full jitter и retry budget, композиция outbox + inbox, фикс конкурентности и наблюдаемость, которая превращает веру в доказательство.
</Goal>

<RetrievalDrawer
  client:visible
  id="05-idempotency-retries-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему exactly-once доставка — иллюзия и как юнит вместо этого даёт effectively-once?", a: "Сеть может дать только at-most-once (никогда не делать retry) или at-least-once (делать retry до подтверждения) — доставить сообщение ровно один раз по ненадёжному каналу нельзя, потому что отправитель никогда не отличит потерянный запрос от потерянного подтверждения. Юнит строит effectively-once, компонуя две дешёвые гарантии: at-least-once доставку (клиент делает retry с одним стабильным idempotency key на операцию) плюс серверный dedup (сервер распознаёт ключ и реплеит сохранённый результат вместо повторного side effect). Та же композиция есть на уровне сообщений: outbox даёт атомарность, at-least-once relay доставляет, идемпотентный консьюмер делает dedup. Ни один компонент не exactly-once; наблюдаемое поведение возникает из композиции." },
    { q: "Пройди четыре состояния, в которые сервер разрешает Idempotency-Key, с кодом статуса для каждого.", a: "New: строки нет, поэтому сервер атомарно вставляет (key, fingerprint, status=in_progress), выполняет бизнес-логику, сохраняет ответ и возвращает 200/201. In-flight: строка есть со status=in_progress — конкурентный запрос обрабатывает тот же ключ — поэтому сервер сразу возвращает 409 Conflict, и клиент отступает. Replay: строка есть со status=completed и fingerprint совпадает, поэтому сервер возвращает кэшированный ответ без бизнес-логики — клиента списали один раз. Mismatch: строка есть, но fingerprint запроса отличается (ключ переиспользован для другого намерения, например изменённой суммы), поэтому сервер возвращает 422 Unprocessable Entity, а не молча реплеит неверный ответ." },
    { q: "Почему request fingerprint важен наряду с ключом и что молча ломается без него?", a: "Fingerprint — это хэш тела запроса (и опционально метода, пути, ключевых заголовков). Он ловит баг клиента, где тот же ключ переиспользован для другой операции. Без него сервер реплеит исходный ответ неизменным — поэтому если клиент хотел списать $55, но переиспользовал ключ от списания $50, клиент видит ответ на $50, а намерение на $55 потеряно навсегда без ошибки где-либо. С fingerprint mismatch становится жёстким 422, заставляя клиента починить ключ или тело. Цена — один SHA-256 на запрос, микросекунды — дешёвая страховка от тихой потери данных." },
    { q: "Дай формулу full-jitter backoff, объясни, почему jitter не подлежит обсуждению, и скажи, почему пер-запросного cap на retry недостаточно.", a: "delay = random(0, min(cap, base × 2^attempt)); base — минимальное ожидание (100–500мс), cap — максимальное (30–60с), attempt — попытка с нуля. Jitter не подлежит обсуждению, потому что без него каждый клиент, упавший в один миг, выбирает идентичную задержку и делает retry в том же тике — синхронизированный thundering herd, который заново убивает восстанавливающийся сервис, превращая 30-секундный сбой в долгий outage. Пер-запросный cap в 3–6 попыток ограничивает одного клиента, но не флот: 10k конкурентных клиентов по 6 попыток — это 60k retry за короткое окно. Retry budget — token-bucket разрешённых retry в секунду на весь call site — задаёт потолок на флот и открывает circuit при исчерпании, чтобы клиенты падали быстро, а не усиливали outage. Клиенты также должны уважать Retry-After и делать retry только на 5xx/таймауты, никогда на 4xx." },
    { q: "Почему любой дизайн «записать БД, потом опубликовать в broker» ломается и как outbox + inbox это чинят?", a: "Запись в базу и публикация в broker — две отдельные операции с окном на сбой между ними: сбой после коммита БД, но до публикации — консьюмеры пропускают событие; поменяй порядок — сбой оставляет фантомное событие для записи, которая не закоммитилась. Атомарной «двойной отдельной записи» нет. Outbox-паттерн пишет событие в таблицу outbox в той же транзакции, что и бизнес-запись, поэтому атомарность становится заботой Postgres — строка outbox существует тогда и только тогда, когда бизнес-запись закоммитилась. Relay (поллинг или Debezium на WAL) публикует неопубликованные строки at-least-once, отмечая их по ACK брокера; если он падает после публикации, но до отметки, он переопубликует. Консьюмер применяет inbox-паттерн — INSERT event_id в processed_events ON CONFLICT DO NOTHING в своей транзакции — поэтому дубликат доставки пропускается. Композиция: атомарность Postgres + at-least-once relay + идемпотентный консьюмер = effectively-once." },
    { q: "Два конкурентных запроса с одним idempotency key. Почему SELECT-потом-INSERT небезопасен и что за production-хранилище?", a: "SELECT-потом-INSERT имеет зазор между statement; два запроса могут оба сделать SELECT (не увидеть строки), оба INSERT, оба обработать — двойное списание без автоматической сериализации. Фикс — один атомарный statement: INSERT ... ON CONFLICT (key) DO NOTHING RETURNING. Победитель получает строку и обрабатывает; проигравший получает пустой RETURNING, перечитывает строку и возвращает 409 (in_progress) или реплеит (completed). pg_advisory_xact_lock — альтернатива, но держит блокировку на всю транзакцию включая медленные внешние вызовы, рискуя исчерпать пул. Выше ~10k записей/с одиночный Postgres-primary насыщается, поэтому production платёжные API используют двухуровневый кэш: горячий путь Redis Cluster (SETNX, шардированный), подкреплённый durable Postgres-ledger, записанным через outbox. Redis в одиночку небезопасен для денег, потому что async fsync может потерять ключ на сбое, превращая следующий retry в двойное списание; на глобальном масштабе кэшу нужна active-active кросс-региональная репликация, чтобы сбой региона не терял ключи." },
  ]}
/>

<Recap lang="ru">
Если ты восстановил каждый ответ — ты держишь хребет юнита: exactly-once это иллюзия, поэтому ты строишь effectively-once из at-least-once плюс dedup; один ключ на операцию разрешается в new / 409 / replay / 422, а fingerprint защищает от тихой потери; retry используют full jitter, уважают Retry-After, делают retry только на 5xx и живут под общим retry budget; dual-write закрывается атомарностью outbox плюс at-least-once relay плюс идемпотентным inbox; конкурентное создание ключа становится атомарным через ON CONFLICT DO NOTHING RETURNING; а durable-хранилище должно пережить сбой, потому что потерянный ключ — это двойное списание.
</Recap>
