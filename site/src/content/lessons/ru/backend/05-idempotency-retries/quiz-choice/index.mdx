---
concepts:
- idempotency-key
- state-machine
- backoff-jitter
- outbox-inbox
- effectively-once
- concurrency-race
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://stripe.com/blog/idempotency
- https://brandur.org/idempotency-keys
- https://www.codereliant.io/p/retries-backoff-jitter
status: ready
summary: "Тест с выбором на синтез по всему юниту idempotency + retries — ключи, машина из четырёх состояний, backoff с jitter, outbox/inbox, гонки конкурентности и production-сбои."
title: 'Идемпотентность и retry: тест с выбором ответа'
track: backend
unit: 05-idempotency-retries
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Ни один не определение для заучивания — каждый это решение под нагрузкой, где неверный выбор это двойное списание, потерянное событие или самим себе устроенный outage.
</Hook>

<Goal lang="ru">
Убедись, что связываешь все движущиеся части, к которым вели уроки: idempotency key, серверную машину состояний, retry с jitter, композицию outbox + inbox и гонки конкурентности, которые превращают аккуратный дизайн в дублированный side effect.
</Goal>

<Quiz
  id="05-idempotency-retries-choice-1"
  lessonSlug="05-idempotency-retries"
  lang="ru"
  question="Мобильный клиент отправляет POST /charge, ответ теряется, и он делает retry. На retry он генерирует свежий UUID в заголовок Idempotency-Key. Каково последствие и почему?"
  choices={[
    { label: "Клиента спишут дважды. Ключ должен быть один-на-логическую-операцию; новый ключ на каждую HTTP-попытку заставляет сервер видеть каждый retry как совершенно новое списание, полностью убивая dedup.", correct: true },
    { label: "Клиента спишут один раз — сервер хэширует тело и распознаёт дубликат независимо от ключа.", misconception: "Fingerprint привязан к idempotency key. Без совпадающего ключа у сервера нет строки для сравнения, поэтому хэш тела вообще не консультируется — обрабатывается новое списание." },
    { label: "Retry отклонят с 422, потому что ключ сменился.", misconception: "422 (mismatch) требует существующей строки для этого ключа с другим fingerprint. У совершенно нового ключа строки нет, поэтому он трактуется как new (200/201), а не 422." },
    { label: "Всё нормально — коллизии UUID v4 настолько редки, что два списания с одним намерением сверятся ниже по стеку.", misconception: "Никакой автоматической сверки нет. Весь смысл ключа — заставить сервер делать dedup; новый ключ на попытку убирает этот механизм." },
  ]}
/>

<Quiz
  id="05-idempotency-retries-choice-2"
  lessonSlug="05-idempotency-retries"
  lang="ru"
  question="Запрос приходит с Idempotency-Key K1. Сервер находит строку K1 со status=in_progress. Почему вернуть 409 сразу, а не блокироваться до завершения первого запроса?"
  choices={[
    { label: "Блокировка держит серверное соединение на всю длительность первого запроса — включая его медленный внешний API-вызов — поэтому 409 даёт клиенту отступить и сделать retry, а к тому моменту строка обычно уже в состоянии replay.", correct: true },
    { label: "RFC 9110 предписывает 409 для конкурентного использования idempotency key.", misconception: "RFC 9110 ничего не говорит про конкурентность idempotency key; 409 здесь — широко принятая конвенция Stripe, выбранная ради эффективности соединений, а не соответствия спеке." },
    { label: "Потому что сервер не может прочитать строку, пока она in_progress.", misconception: "Строка читаема в любой момент — именно так сервер и понимает, что вернуть 409. Выбор про то, чтобы не держать соединение, а не про доступ на чтение." },
    { label: "409 заставляет клиента перегенерировать ключ, что предотвращает двойное списание.", misconception: "Клиент должен переиспользовать тот же ключ на retry. 409 сигналит «уже в полёте, отступи» — перегенерация ключа создала бы второе списание." },
  ]}
/>

<Quiz
  id="05-idempotency-retries-choice-3"
  lessonSlug="05-idempotency-retries"
  lang="ru"
  question="Сервис возвращает 503 с Retry-After: 30 во время outage. По всему флоту какое поведение клиента оставляет восстанавливающийся сервис живым?"
  choices={[
    { label: "Уважить Retry-After, затем применить full jitter — random(0, min(cap, base × 2^n)) — и держать общий retry budget на флот, чтобы суммарная скорость retry была ограничена, даже если каждый клиент упёрся в cap.", correct: true },
    { label: "Игнорировать Retry-After и использовать жёсткий фиксированный backoff 1с, чтобы клиент восстановился как можно быстрее.", misconception: "Синхронизированный retry в 1с от каждого клиента — это thundering herd, тот самый всплеск, что положил сервис. Игнорировать Retry-After — хрестоматийный анти-паттерн." },
    { label: "Обычный exponential backoff без jitter достаточно — удвоение задержки уже разносит нагрузку.", misconception: "Без jitter все клиенты, упавшие в один миг, выбирают идентичную задержку и делают retry в том же тике. Удвоение их не рассинхронизирует." },
    { label: "Cap в 6 попыток на запрос делает retry budget ненужным.", misconception: "Cap в 6 попыток на запрос всё равно позволяет 10k конкурентным клиентам сгенерировать 60k retry за короткое окно. Budget — это потолок на весь флот, которого пер-запросный cap дать не может." },
  ]}
/>

<Quiz
  id="05-idempotency-retries-choice-4"
  lessonSlug="05-idempotency-retries"
  lang="ru"
  question="Команда должна публиковать событие 'order.created' в Kafka каждый раз, когда строка заказа коммитится в Postgres. Почему outbox-паттерн верен, а «записать Postgres, потом опубликовать Kafka» нет?"
  choices={[
    { label: "Outbox пишет событие в таблицу Postgres в той же транзакции, что и заказ, поэтому атомарность — забота Postgres; relay затем доставляет at-least-once, а inbox консьюмера делает dedup — effectively-once без dual-write окна на сбой.", correct: true },
    { label: "Публикация в Kafka первой, а запись Postgres второй закрывает окно, потому что Kafka durable.", misconception: "Kafka-потом-Postgres просто переносит окно: сбой после публикации, но до коммита оставляет фантомное событие для записи, которой не было." },
    { label: "Продюсеры Kafka асинхронны, поэтому публикация не может упасть после записи в БД.", misconception: "Асинхронно или нет, публикация и коммит — две отдельные операции с окном на сбой между ними. Атомарной «двойной отдельной записи» не бывает." },
    { label: "Outbox делает доставку exactly-once, убирая нужду в идемпотентном консьюмере.", misconception: "Relay всё равно доставляет at-least-once (может переопубликовать, упав до отметки строки published). Именно dedup в inbox консьюмера делает это effectively-once." },
  ]}
/>

<Quiz
  id="05-idempotency-retries-choice-5"
  lessonSlug="05-idempotency-retries"
  lang="ru"
  question="Два запроса с одним и тем же idempotency key приходят на сервер в пределах миллисекунды. Хендлер делает SELECT-потом-INSERT в таблицу idempotency_keys. Что произойдёт и какой фикс?"
  choices={[
    { label: "Оба SELECT не видят строки, оба INSERT, оба обрабатывают — двойное списание. Фикс: INSERT ... ON CONFLICT (key) DO NOTHING RETURNING, один атомарный statement; проигравший гонку находит RETURNING пустым и перечитывает строку, чтобы вернуть 409 или replay.", correct: true },
    { label: "База данных сериализует эти два автоматически, поэтому второй запрос ждёт первый.", misconception: "У обычного SELECT-потом-INSERT нет блокировки между statement; ничто не сериализует два запроса. Окно гонки реально." },
    { label: "Ограничение первичного ключа заваливает второй INSERT, и приложение отдаёт 500.", misconception: "Если unique-ограничение есть, используй ON CONFLICT, чтобы превратить коллизию в управляемый 409/replay, а не падать с 500. Наивный код может и вовсе не иметь ограничения." },
    { label: "Поднять уровень изоляции до SERIALIZABLE само по себе делает SELECT-потом-INSERT безопасным без изменения кода.", misconception: "SERIALIZABLE может прервать одну транзакцию, но retry и обработку конфликта писать всё равно надо; чистый однострочный фикс — ON CONFLICT DO NOTHING RETURNING." },
  ]}
/>

<Quiz
  id="05-idempotency-retries-choice-6"
  lessonSlug="05-idempotency-retries"
  lang="ru"
  question="Платёжный API использует только Redis (async fsync, SETNX) как хранилище idempotency. Нода падает через миллисекунду после SETNX, теряя этот ключ. Какое production-чтение этого дизайна?"
  choices={[
    { label: "Небезопасно для денег: потерянный ключ означает, что следующий retry трактуется как new и может списать дважды. Фикс — двухуровневый кэш: горячий путь Redis, подкреплённый durable Postgres-ledger, записанным через outbox в той же транзакции.", correct: true },
    { label: "Достаточно безопасно — асинхронная репликация Redis гарантирует, что ключ переживёт сбой.", misconception: "Дефолтная репликация Redis асинхронна; сбой до того, как реплика получит запись, теряет ключ. Репликация сужает, но не убирает окно." },
    { label: "Клиента никогда не спишут, потому что потеря ключа откатывает платёж.", misconception: "Если списание уже дошло до платёжного процессора до сбоя — оно остаётся. Потеря dedup-ключа не отменяет уже случившийся side effect." },
    { label: "Cap клиентских retry на 3 делает вероятность двойного списания достаточно низкой, чтобы игнорировать.", misconception: "Юридическая ответственность за двойные списания требует архитектурной гарантии, а не низкой вероятности. Cap на retry снижает шансы, но не убирает их." },
  ]}
/>

<Recap lang="ru">
Юнит — это одна цепочка: клиент делает retry с одним ключом на операцию; сервер разрешает этот ключ в new / in-flight (409) / replay / mismatch (422) и атомарно сохраняет результат через ON CONFLICT; retry используют full jitter, уважают Retry-After и живут под общим retry budget; outbox + at-least-once relay + dedup в inbox превращают невозможную dual-write в effectively-once; а durable-хранилище должно пережить сбой, потому что потеря ключа — это и есть превращение аккуратного дизайна в двойное списание. Каждый production-сбой юнита — Stripe 2017, Knight Capital, AWS S3, GitHub 2018 — это at-least-once без идемпотентных консьюмеров или retry без jitter.
</Recap>
