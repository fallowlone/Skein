---
concepts:
- deregistration-race
- readiness-probe
- endpoint-propagation
- prestop-sleep
- load-balancer
- eventual-consistency
deepensInto:
- backend/07-graceful-shutdown/04-draining-and-shutdown-order
estMin: 16
lang: ru
lessonType: topic
level: middle
order: 3
prereqs:
- backend/07-graceful-shutdown/02-signals-and-grace-period
slug: 03-the-deregistration-race
sources:
- https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/
- https://cloud.google.com/blog/products/containers-kubernetes/kubernetes-best-practices-terminating-with-grace
- https://jorijn.com/en/knowledge-base/kubernetes/troubleshooting/kubernetes-graceful-shutdown-sigterm-pod-termination/
spiral:
- failure-modes
- resilience
- lifecycle
status: ready
summary: 'Тонкий баг — гонка: SIGTERM и удаление endpoint срабатывают параллельно,
  но задержка распространения значит, что балансировщик шлёт трафик в под, который
  уже начал выключаться. Фикс — сначала завалить readiness и переждать задержку перед
  закрытием слушателя.'
title: 'Гонка дерегистрации: останови маршрутизацию, прежде чем перестать принимать'
track: backend
unit: 07-graceful-shutdown
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Ты сделал всё правильно: настоящий SIGTERM-обработчик, приложение — PID 1, обработчик перестаёт принимать новые соединения в тот же миг, как сигнал приходит, и дренирует те, что в полёте. Деплоишь — и *всё равно* видишь всплеск ошибок connection-refused — но только секунду-другую, только в начале каждого раската. Причина — гонка, которую почти никто не угадывает с первого раза. Когда под начинает завершаться, две вещи происходят *параллельно*: оркестратор шлёт SIGTERM твоему процессу **и** велит остальному кластеру перестать слать тебе трафик. Вторая часть не мгновенна — она распространяется через control plane, kube-proxy на каждой ноде и балансировщик, занимая от менее секунды на маленьком кластере до десятков секунд на большом. Твой процесс, повинуясь SIGTERM, закрывает слушатель *немедленно*. Так что на окно распространения есть балансировщик, всё ещё уверенно шлющий запросы в под, что уже закрыл дверь — и каждый из них получает отказ. Ты перестал принимать *раньше*, чем мир перестал маршрутизировать.
</Hook>

<Crux>SIGTERM и дерегистрация гонятся; LB шлёт трафик во время распространения — сначала завали readiness и дай маршрутизации стечь.</Crux>

<Explanation>

## Двое часов, запущенных вместе, кончающихся врозь

Ментальная модель, что ломается здесь, — «балансировщик знает момент, когда я получил SIGTERM». Не знает. Удаление endpoint и доставка сигнала — это *независимые, конкурентные* действия, запущенные одним событием:

- **Путь сигнала** быстрый и локальный: kubelet на твоей ноде шлёт SIGTERM в твой контейнер за миллисекунды.
- **Путь маршрутизации** медленный и распределённый: API-сервер помечает endpoint not-ready, это изменение распространяется в kube-proxy каждой ноды (или во внешний балансировщик, или в ingress-контроллер, который может *опрашивать* по интервалу), и только тогда правила маршрутизации реально перестают слать тебе.

Поскольку путь маршрутизации **в конечном счёте согласован** (eventually consistent), есть окно — от «SIGTERM пришёл» до «последний роутер перестал слать тебе трафик» — в течение которого кластер всё ещё считает тебя валидным бэкендом. Сообщаемые числа: менее секунды на маленьких кластерах, но **10–30 секунд** на больших или с опрашивающими ingress-контроллерами. Если твой обработчик закрывает слушающий сокет в тот же миг, как приходит SIGTERM, каждый запрос, прибывающий в это окно, упирается в закрытый порт, и клиент получает *connection refused* — инверсия проблемы из урока один. Там ты вышел слишком рано и оборвал работу в полёте; здесь ты перестаёшь *принимать* слишком рано и отвергаешь работу, которую LB ещё шлёт.

## Фикс: меняй порядок, а не просто добавляй обработчик

Лекарство — заставить маршрутизацию остановиться *раньше*, чем ты перестаёшь принимать — перевернуть порядок двух часов. Есть два дополняющих рычага:

- **Сначала завали readiness-пробу.** Readiness — сигнал, которым оркестратор решает, слать ли тебе. В тот миг, как начинаешь выключаться, флипни свой readiness-эндпоинт в failing (или unready). Это *запускает* часы дерегистрации через нормальный механизм — но не заканчивает их, потому что распространение всё ещё занимает время.
- **Добавь preStop-сон, чтобы покрыть распространение.** Поскольку preStop hook бежит *перед* SIGTERM, и оркестратор блокируется на нём, `preStop`, который просто спит несколько секунд (обычно **5–15s**, под реальную задержку распространения твоего кластера), держит процесс открытым и принимающим, пока статус not-ready расходится. Только после сна приходит SIGTERM, и твой обработчик закрывает слушатель — к этому моменту роутеры догнали и больше не шлют тебе новый трафик.

Принцип: продолжай обслуживать, пока не уверен, что тебе больше ничего не маршрутизируется, *затем* перестань принимать, *затем* дренируй. Одного обработчика мало; порядок относительно дерегистрации — вот вся суть.

## Не ломай readiness неправильным способом

Частый автогол: люди делают так, что *liveness*-проба и readiness-проба делят один эндпоинт, или дают SIGTERM-обработчику немедленно возвращать ошибки из health-эндпоинта. Если **liveness**-проба начинает падать во время выключения, оркестратор может решить, что контейнер *сломан*, и убить или перезапустить его, обрезав твой дренаж. Держи liveness проходящей (процесс жив, и дренаж — здоровое поведение) и завали только **readiness** (не шли мне новый трафик). Две пробы отвечают на разные вопросы: liveness спрашивает «перезапустить тебя?», readiness спрашивает «маршрутизировать к тебе?» — и во время выключения честные ответы «нет» и «нет нового трафика» соответственно.

<Inset kind="why" lang="ru">
Почему платформа не может просто сделать удаление endpoint синхронным с сигналом, чтобы не было окна, вокруг которого надо инженерить? Потому что маршрутизация в распределённом кластере — не единый рубильник, который можно щёлкнуть атомарно — это *реплицированное состояние*, размазанное по многим независимым компонентам, и держать реплицированное состояние идеально согласованным на каждом изменении — ровно та дорогая координация, которой распределённые системы избегают ради пропускной способности. Когда под становится not-ready, этот факт должен дойти до API-сервера, записаться в объект endpoints, быть замеченным kube-proxy каждой ноды (который затем переписывает локальные правила iptables или IPVS) и отдельно дойти до любого внешнего балансировщика или ingress-контроллера, часть которых узнаёт изменения *опросом* по своему графику, а не пушем. Каждый из этих хопов независимо быстр, но коллективно асинхронен, и нет глобальных часов, говорящих «все обновились, теперь отпускай сигнал». Сделать это синхронным означало бы блокировать каждое завершение на подтверждении изменения самым медленным роутером во флоте — связав выключение пода с кластерным консенсусом, что заставило бы деплои ползти и само падало бы всякий раз, когда любой роутер медлит или недоступен. Так что платформа выбирает конечную согласованность и вручает *тебе* инструменты перекрыть разрыв: readiness, чтобы запустить часы, и preStop, чтобы переждать. Глубокий урок — тот же, что юнит про circuit breaker долбил снова и снова: в распределённой системе нельзя предполагать, что два события, запущенных вместе, *наблюдаются* вместе, и любая корректность, зависящая от их порядка, должна обеспечиваться намеренно, а не предполагаться.
</Inset>

<div data-lesson-visual class="overflow-x-auto my-6">

| Момент | Твой процесс | Балансировщик | Результат |
|---|---|---|---|
| Без защиты, SIGTERM пришёл | Закрывает слушатель мгновенно | Ещё маршрутизирует (не распространилось) | Connection refused на окно |
| Сначала завалить readiness | Продолжает принимать | Начинает помечать тебя not-ready | Часы запущены, ещё не кончились |
| preStop спит 5–15s | Продолжает принимать | Распространение завершается | Роутеры перестают слать новое |
| SIGTERM после сна | Теперь закрывает слушатель | Больше не маршрутизирует к тебе | Нет отказанных соединений |

</div>

<Quiz
  id="gs-race-q1"
  lessonSlug="03-the-deregistration-race"
  lang="ru"
  question="Сервис с верным SIGTERM-обработчиком, закрывающим слушатель немедленно, всё равно видит короткий всплеск connection-refused в начале каждого деплоя. Почему?"
  choices={[
    { label: "Дерегистрация endpoint в конечном счёте согласована и отстаёт от SIGTERM, так что на окно распространения балансировщик всё ещё маршрутизирует в под, который уже закрыл слушатель", correct: true },
    { label: "SIGTERM-обработчик слишком медленно стартует", misconception: "Обработчик работает нормально; проблема в том, что он закрывает слушатель до распространения маршрутизации, а не в том, что он стартует поздно." },
    { label: "Grace period короче времени дренажа", misconception: "Это гонка отказанных соединений в начале, а не проблема таймаута дренажа; слушатель закрылся слишком рано относительно дерегистрации." },
    { label: "Новые поды регистрируются слишком медленно", misconception: "Ошибки refused идут от старого пода, закрывающего порт, будучи всё ещё в таблицах маршрутизации, а не от тайминга регистрации новых подов." },
  ]}
/>

<Quiz
  id="gs-race-q2"
  lessonSlug="03-the-deregistration-race"
  lang="ru"
  question="Во время выключения почему надо завалить readiness-пробу, но держать liveness-пробу проходящей?"
  choices={[
    { label: "Readiness управляет маршрутизацией (завали её, чтобы остановить новый трафик), тогда как падающая liveness-проба говорит оркестратору, что контейнер сломан, и может заставить убить или перезапустить тебя, обрезав дренаж", correct: true },
    { label: "Обе пробы должны падать немедленно, чтобы ускорить выключение", misconception: "Падение liveness может вызвать kill/restart, прерывающий дренаж; во время здорового выключения должна падать только readiness." },
    { label: "Liveness управляет маршрутизацией, а readiness — перезапусками", misconception: "Всё наоборот: readiness управляет маршрутизацией, liveness — тем, считается ли контейнер сломанным и перезапускается ли." },
    { label: "Падение readiness портит запросы в полёте", misconception: "Readiness влияет только на маршрутизацию нового трафика; она не трогает запросы в полёте, которые продолжают дренироваться." },
  ]}
/>

</Explanation>

<SequenceDiagram
  label="Гонка дерегистрации: LB маршрутизирует во время распространения"
  hue="var(--d-backend)"
  actors={["Балансировщик", "Под"]}
  messages={[
    { from: "Балансировщик", to: "Под", label: "маршрут запроса (ещё валиден)" },
    { from: "Под", to: "Балансировщик", label: "preStop sleep — завалить readiness" },
    { from: "Балансировщик", to: "Под", label: "маршрут запроса (распространяется...)" },
    { from: "Под", to: "Балансировщик", label: "SIGTERM: закрыть слушатель после сна" },
    { from: "Балансировщик", to: "Под", label: "нет новых маршрутов (дерегистрирован)" },
  ]}
  caption="Без preStop-сна LB маршрутизирует запросы в под, уже закрывший слушатель — connection refused на окно распространения."
/>

<KeyTakeaway>Тонкий баг выключения — гонка двух часов, запущенных одним событием, но кончающихся врозь. Доставка сигнала быстрая и локальная — kubelet шлёт SIGTERM за миллисекунды — тогда как дерегистрация endpoint медленная и распределённая: статус not-ready должен распространиться через API-сервер, kube-proxy каждой ноды и любой внешний или опрашивающий балансировщик, путь конечной согласованности, занимающий менее секунды на маленьких кластерах, но 10–30s на больших. Обработчик, закрывающий слушатель в тот же миг, как приходит SIGTERM, поэтому отвергает каждый запрос, что балансировщик ещё маршрутизирует в это окно — ошибки connection-refused, инверсия выхода слишком рано. Фикс — порядок, а не просто обработчик: сначала завали readiness-пробу, чтобы запустить часы дерегистрации через нормальный механизм, и добавь preStop-сон (обычно 5–15s, под реальную задержку распространения), который держит процесс открытым и принимающим, пока маршрутизация не догнала, после чего SIGTERM закрывает слушатель безопасно. Держи liveness-пробу проходящей всё время — readiness управляет маршрутизацией, liveness — перезапусками, а падающая liveness-проба может заставить оркестратор убить контейнер и обрезать дренаж.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="gs-race-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое гонка дерегистрации и почему она вызывает ошибки connection-refused?", a: "Когда под начинает завершаться, два независимых действия срабатывают параллельно из одного события: kubelet шлёт SIGTERM в контейнер, быстрый локальный путь в миллисекунды, и оркестратор помечает endpoint not-ready, медленный распределённый путь, который должен дойти до API-сервера, распространиться в kube-proxy каждой ноды, переписывающий правила маршрутизации, и дойти до любого внешнего балансировщика или опрашивающего ingress-контроллера. Поскольку этот путь маршрутизации в конечном счёте согласован, нет глобального момента, когда все обновились — окно от прихода SIGTERM до остановки последнего роутера менее секунды на маленьких кластерах, но 10–30 секунд на больших или с опрашивающими ingress-контроллерами. Если обработчик закрывает слушающий сокет в тот же миг, как приходит SIGTERM, каждый запрос, прибывающий в это окно, упирается в закрытый порт, и клиент получает connection-refused. Это инверсия выхода слишком рано: там ты оборвал работу в полёте, здесь ты перестаёшь принимать до того, как мир перестал маршрутизировать к тебе." },
    { q: "Как починить гонку и почему завалить readiness, но не liveness?", a: "Лекарство — заставить маршрутизацию остановиться до того, как ты перестаёшь принимать, перевернув порядок двух часов двумя дополняющими рычагами. Во-первых, завали readiness-пробу в тот миг, как начинается выключение — readiness это то, чем оркестратор решает, слать ли тебе, так что флип в failing запускает часы дерегистрации через нормальный механизм, хотя распространение всё ещё занимает время. Во-вторых, добавь preStop hook, который просто спит несколько секунд (обычно 5–15s, под реальную задержку распространения твоего кластера); поскольку preStop бежит перед SIGTERM, и оркестратор блокируется на нём, сон держит процесс открытым и принимающим, пока статус not-ready расходится, так что к моменту прихода SIGTERM и закрытия слушателя роутеры догнали. Ты валишь readiness, но держишь liveness проходящей, потому что пробы отвечают на разные вопросы: readiness спрашивает «маршрутизировать к тебе?» (ответ: нет нового трафика), тогда как liveness спрашивает «перезапустить тебя?» (ответ: нет, процесс жив, и дренаж — здоровое поведение). Падающая liveness-проба во время выключения может заставить оркестратор решить, что контейнер сломан, и убить или перезапустить его, обрезав дренаж." },
  ]}
/>

<Recap lang="ru">
Даже идеальный SIGTERM-обработчик отвергает соединения, если закрывает слушатель слишком рано, потому что завершение запускает двое часов сразу, кончающихся врозь: доставка сигнала — быстрый локальный путь в миллисекунды, тогда как дерегистрация endpoint — медленный распределённый путь через API-сервер, kube-proxy каждой ноды и любой внешний или опрашивающий балансировщик — в конечном счёте согласованный, занимающий менее секунды на маленьких кластерах, но 10–30s на больших. В это окно балансировщик всё ещё маршрутизирует в под, чья дверь уже закрыта, давая ошибки connection-refused, инверсию обрыва-слишком-рано из урока один. Фикс — порядок: сначала завали readiness-пробу, чтобы запустить часы дерегистрации, и добавь preStop-сон около 5–15s, под реальное распространение, что держит процесс принимающим, пока маршрутизация не догнала — только тогда SIGTERM закрывает слушатель. Держи liveness проходящей, чтобы оркестратор не принял здоровый дренаж за сломанный контейнер и не перезапустил посреди выключения. С безопасно сдренированной маршрутизацией и наконец закрытым слушателем следующий урок берётся за то, что идёт после закрытия двери: дренаж запросов в полёте и закрытие каждого ресурса в правильном порядке. Теперь, когда видишь connection-refused в самом начале деплоя — не посреди дренажа, а в первые секунды раската — ты знаешь гонку и первым делом потянешься за preStop-сном.
</Recap>
