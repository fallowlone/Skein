---
concepts:
- termination-sequence
- deregistration-race
- reverse-dependency-order
- grace-period-budget
- requeue-idempotency
- fleet-coordination
estMin: 14
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/
- https://cloud.google.com/blog/products/containers-kubernetes/kubernetes-best-practices-terminating-with-grace
- https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/
status: ready
summary: "Промпты на свободное воспроизведение по всему юниту graceful shutdown. Ответь на каждый своими словами, затем открой модельный ответ и сравни."
title: 'Graceful shutdown: тест на свободное воспроизведение'
track: backend
unit: 07-graceful-shutdown
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Воспроизведение бьёт перечитывание. На каждый промпт скажи или напиши полный ответ по памяти до того, как откроешь модельный — усилие припоминания и есть то, что закрепляет последовательность shutdown к моменту, когда она нужна на деплое в 2 часа ночи.
</Hook>

<Goal lang="ru">
Восстанови хребет юнита — последовательность termination, гонку deregistration, teardown в reverse-dependency, бюджет дедлайна, безопасность requeue и координацию флота — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:visible
  id="07-graceful-shutdown-short-retrieval"
  lang="ru"
  questions={[
    { q: "Пройди последовательность termination пода Kubernetes и объясни, почему SIGTERM и SIGKILL категориально различны.", a: "Под помечается Terminating и убирается из endpoints Service; запускается preStop hook, если он определён, и оркестратор на нём блокируется; SIGTERM (сигнал 15) доставляется в PID 1; оркестратор ждёт terminationGracePeriodSeconds (дефолт 30s, покрывающий preStop плюс пост-SIGTERM вместе); затем срабатывает SIGKILL (сигнал 9), если процесс ещё жив. SIGTERM — это запрос: он прерывает процесс и запускает зарегистрированный тобой обработчик, давая шанс на drain и cleanup. SIGKILL — это команда ядру, никогда не доставляемая твоему коду: ни обработчика, ни cleanup, ни финального flush. Поэтому вся игра — завершить во время SIGTERM, чтобы SIGKILL никогда не сработал, что делает grace period жёстким дедлайном, обеспеченным неперехватываемым сигналом." },
    { q: "Что такое ловушка PID 1 и каковы два исправления?", a: "Внутри контейнера первый запущенный процесс становится PID 1, который оркестратор сигналит и которому ядро даёт особую семантику. Следуют две ловушки. Во-первых, сигналы идут только в PID 1, поэтому запуск приложения под shell — sh -c 'node server.js' — делает shell PID 1, и многие shell не форвардят SIGTERM детям, так что приложение никогда не видит сигнал, ничего не дрейнирует и получает SIGKILL на дедлайне. Во-вторых, у PID 1 нет дефолтных обработчиков сигналов: ядро не применяет обычный дефолт «terminate на SIGTERM» к защищённому слоту init, поэтому даже как PID 1 отсутствующий обработчик означает, что SIGTERM молча игнорируется. Исправления — сделать приложение PID 1 через exec-форму (CMD ['node','server.js'] или exec node server.js) или запустить крошечный init вроде tini/dumb-init, который форвардит сигналы и пожинает зомби — и всегда явно регистрировать обработчик." },
    { q: "Опиши гонку deregistration и двухрычажное исправление; почему проваливать readiness, но держать liveness зелёной?", a: "Termination запускает двое часов из одного события: kubelet шлёт SIGTERM за миллисекунды (быстро, локально), тогда как deregistration эндпоинта медленный и распределённый — статус not-ready должен дойти до API server, распространиться на kube-proxy каждой ноды и до любого внешнего или опрашивающего балансировщика, eventually-consistent путь, занимающий менее секунды на малых кластерах, но 10–30s на больших. Если обработчик закрывает листенер в тот же миг, как приходит SIGTERM, каждый запрос, который балансировщик ещё маршрутизирует в этом окне, бьётся в закрытый порт, и клиент получает connection-refused. Лекарство — порядок: сначала провали readiness probe, чтобы запустить часы deregistration, и добавь preStop sleep (≈5–15s, под реальную propagation), чтобы процесс продолжал принимать, пока routing не догонит, после чего листенер закроется безопасно. Проваливаешь readiness (нет нового трафика), но держишь liveness зелёной, потому что провалившаяся liveness probe говорит оркестратору, что контейнер сломан, и он может убить или перезапустить его, обрезав drain — readiness управляет маршрутизацией, liveness управляет перезапусками." },
    { q: "Почему один server.close() зависает и что такое reverse dependency order?", a: "server.close() прекращает принимать новые соединения и вызывает колбэк только когда все существующие соединения завершились, но он не трогает простаивающие keep-alive сокеты — HTTP/1.1 держит их открытыми по умолчанию — которые он всё ещё считает активными, поэтому колбэк не срабатывает, и процесс зависает до SIGKILL. Нужно дрейнировать keep-alive намеренно: слать Connection: close на shutdown-ответах, чтобы клиенты переподключились в другое место, и принудительно закрывать простаивающие сокеты (server.closeIdleConnections / closeAllConnections), позволяя активным запросам завершиться. Затем teardown в reverse dependency order, зеркало старта: сначала остановить HTTP-сервер и завершить in-flight запросы, затем остановить фоновые воркеры и сбросить очереди, затем закрыть datastores последними (пул БД, потом Redis). Причина конкретна — in-flight запрос посреди drain может ещё выдать query, поэтому раннее закрытие пула обрывает его и превращает graceful drain в потерю запросов; datastores — самая глубокая зависимость и закрываются последними. Оберни весь drain в guardian timeout ниже grace period, чтобы застрявший запрос не держал процесс заложником SIGKILL." },
    { q: "Почему grace period — это бюджет и какая disposition для длинных запросов против фоновых задач?", a: "Grace period (дефолт 30s) — это единое окно, которое должно покрыть preStop sleep (≈5–15s), drain keep-alive, teardown в reverse-dependency и запас guardian timeout — оставляя часто менее двадцати секунд на собственно завершение in-flight работы, поэтому любая единица работы, чей остаточный runtime превышает этот остаток, структурно не успеет, и await её просто приводит к SIGKILL посреди записи. Нельзя поднимать grace period без предела, потому что дедлайны оркестратора, node drain и spot-reclaim его ограничивают, так что для по-настоящему длинной работы нужна другая disposition. Длинный синхронный запрос нельзя поставить в requeue (клиент держит сокет), поэтому заверши его, если он близок, или отклони чисто через 503 + Retry-After (или откажись стартовать новые длинные операции, как только начался shutdown) — никогда не обрывай его молча посреди стрима. Фоновую задачу можно requeue: перестань тянуть новые задачи, заверши влезающее, и верни остальное, или дай visibility timeout её передоставить." },
    { q: "Почему requeue требует идемпотентности и почему флот безупречных per-instance shutdown не гарантирует zero-downtime деплой?", a: "Requeue и передоставка по visibility timeout — at-least-once: задача может выполниться снова с начала, поэтому воркер, убитый после частичного side effect (например, карта списана до ack), списывает снова на передоставке. Поэтому requeue безопасен только при идемпотентном consumer — двойной прогон задачи даёт то же конечное состояние через idempotency key, inbox/dedup-таблицу или no-op-on-applied state machine — с checkpointing для сокращения времени переделки. Что до флота: каждое свойство, важное на деплое, — это отношение между инстансами, которое один процесс не видит. Capacity зависит от того, обслуживает ли уже замена, поэтому surge новых подов до ready перед drain старых (maxUnavailable: 0, положительный maxSurge). Deregistration delay надо размерить под p99 флота (≈3× p99, при дефолтах cloud LB 30–60s, до 600s+ для длинных соединений). А thundering reconnect эмерджентен — тысячи корректных Connection: close, приземляющихся разом, опрокидывают холодные поды — поэтому jitter closes. Локальная корректность не складывается в глобальную бесплатно; сбои живут во взаимодействиях." },
  ]}
/>

<Recap lang="ru">
Если ты смог восстановить каждый ответ по памяти, ты держишь хребет юнита: последовательность termination кончается неперехватываемым SIGKILL, поэтому завершай во время SIGTERM — и убедись, что он реально доходит до обработчика в PID 1; гонка deregistration означает провали readiness и выжди propagation до того, как перестанешь принимать; teardown идёт в reverse dependency order с datastores последними, ограниченный guardian timeout; grace period — это бюджет, поэтому отклоняй длинные запросы и ставь задачи в requeue — но requeue это at-least-once и требует идемпотентности; а zero-downtime деплой — это fleet-уровневая оркестрация (surge перед drain, deregister перед terminate, jitter closes), упорядочивающая индивидуально-корректные shutdown так, чтобы они не сталкивались.
</Recap>
