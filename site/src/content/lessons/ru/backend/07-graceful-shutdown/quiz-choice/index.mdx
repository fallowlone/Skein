---
concepts:
- sigterm-sigkill
- deregistration-race
- reverse-dependency-order
- requeue-idempotency
- zero-downtime-deploy
- grace-period-budget
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle/
- https://cloud.google.com/blog/products/containers-kubernetes/kubernetes-best-practices-terminating-with-grace
- https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/
status: ready
summary: "Тест с выбором на синтез по всему юниту graceful shutdown — сигналы, гонка deregistration, порядок drain, безопасность requeue, бюджет grace period и zero-downtime деплои на уровне флота."
title: 'Graceful shutdown: тест с выбором ответа'
track: backend
unit: 07-graceful-shutdown
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает решение, которое ты принимаешь во время реального деплоя — не определение для заучивания, а порядок или disposition, которые нужно сделать правильно до того, как истечёт grace period.
</Hook>

<Goal lang="ru">
Убедись, что связываешь контракт сигналов, гонку deregistration, порядок drain, безопасность requeue и координацию флота — тот синтез, к которому вели отдельные уроки.
</Goal>

<Quiz
  id="07-graceful-shutdown-choice-1"
  lessonSlug="07-graceful-shutdown"
  lang="ru"
  question="Команда выкатывает корректный 45-секундный SIGTERM-обработчик drain, но запросы всё равно обрываются на каждом деплое. Dockerfile запускает приложение через sh -c 'node server.js'. В чём корневая причина?"
  choices={[
    { label: "Shell является PID 1 и получает SIGTERM, но не форвардит его дочернему node, поэтому обработчик никогда не запускается, и приложение получает SIGKILL на дедлайне grace period.", correct: true },
    { label: "45-секундный обработчик превышает дефолтный grace period 30s, поэтому обрезается посреди drain.", misconception: "45s drain против дефолтных 30s действительно был бы обрезан, но здесь обработчик не запускается вовсе — SIGTERM приходит shell как PID 1, а не node, поэтому ничего не дрейнируется." },
    { label: "SIGTERM-обработчики ненадёжны внутри контейнеров и должны быть заменены на liveness probe.", misconception: "Обработчики полностью надёжны, когда сигнал до них доходит; дефект — в доставке сигнала к PID 1. Liveness probe отвечает на другой вопрос (перезапускать ли тебя), а не на drain." },
    { label: "Node регистрирует обработчик слишком поздно на старте, поэтому ранний SIGTERM теряется.", misconception: "Регистрация фактически мгновенна на старте; сигнал доставляется shell, а не node, поэтому тайминг тут ни при чём." },
  ]}
/>

<Quiz
  id="07-graceful-shutdown-choice-2"
  lessonSlug="07-graceful-shutdown"
  lang="ru"
  question="Сервис с безупречным SIGTERM-обработчиком, который закрывает листенер в тот же миг, как приходит сигнал, всё равно даёт короткий всплеск connection-refused в самом начале каждого rollout. Почему и как чинить?"
  choices={[
    { label: "Deregistration эндпоинта eventually consistent и отстаёт от SIGTERM, поэтому во время окна propagation балансировщик всё ещё маршрутизирует на под, чей листенер уже закрыт. Сначала провали readiness и добавь preStop sleep (≈5–15s), чтобы выждать propagation до закрытия.", correct: true },
    { label: "Grace period короче времени drain, поэтому листенер закрывается принудительно слишком рано.", misconception: "Это гонка refused-соединений в начале shutdown, а не проблема drain-таймаута — листенер закрылся слишком рано относительно deregistration, а не слишком поздно." },
    { label: "Новые поды регистрируются слишком медленно, поэтому трафику некуда идти здоровому.", misconception: "Refused-ошибки идут от старого пода, закрывающего порт, пока он ещё в routing-таблицах, а не от тайминга регистрации новых подов." },
    { label: "Закрывай листенер ещё быстрее, в тот же миг что и SIGTERM, чтобы окно стало меньше.", misconception: "Закрытие быстрее расширяет окно отказов — лекарство обратное: продолжай принимать, пока routing не сдрейнировался, и только потом закрывай." },
  ]}
/>

<Quiz
  id="07-graceful-shutdown-choice-3"
  lessonSlug="07-graceful-shutdown"
  lang="ru"
  question="Обработчик shutdown закрывает HTTP-сервер и пул БД в одном шаге. Большинство деплоев чистые, но иногда несколько запросов падают посреди drain с ошибками pool-closed. Какой принцип нарушен?"
  choices={[
    { label: "Reverse dependency order: in-flight запрос может ещё выдать query во время drain, поэтому datastore — самая глубокая зависимость — должен закрываться последним, после того как HTTP-слой, который его использует, завершился. Закрытие пула параллельно обрывает in-flight queries.", correct: true },
    { label: "Grace period был слишком коротким, чтобы queries успели завершиться.", misconception: "У этих запросов было время — они упали потому, что пул убрали, пока они легитимно выполнялись. Им не хватило зависимости, а не времени." },
    { label: "Пул всегда нужно закрывать до HTTP-сервера, чтобы сбросить кэш соединений.", misconception: "Закрытие пула первым — это и есть баг: оно обрывает in-flight queries, которым он ещё нужен. Datastores закрываются последними в reverse dependency order." },
    { label: "Redis всегда нужно закрывать до базы, а порядок был перепутан.", misconception: "Нет фиксированного правила redis-потом-db; порядок — reverse-dependency. Оба datastore должны закрываться после HTTP-слоя, который от них зависит, в обратном порядке к старту." },
  ]}
/>

<Quiz
  id="07-graceful-shutdown-choice-4"
  lessonSlug="07-graceful-shutdown"
  lang="ru"
  question="Воркер очереди четыре минуты внутри задачи, когда приходит SIGTERM, а grace period 30s. Какая правильная disposition и что делает её безопасной?"
  choices={[
    { label: "Перестать тянуть новые задачи и вернуть задачу в очереди обратно (или дать visibility timeout её передоставить) — безопасно только потому, что consumer идемпотентен, поэтому at-least-once передоставка может выполниться снова без двойного применения.", correct: true },
    { label: "Продлить grace period, пока четырёхминутная задача не завершится.", misconception: "Grace period ограничен дедлайнами оркестратора, node drain и spot-reclaim; четырёхминутная задача структурно не помещается. Больше времени — не рычаг, нужна другая disposition." },
    { label: "Дать задаче завершиться, потому что очередь её терпеливо подождёт.", misconception: "Очередь подождёт, но оркестратор SIGKILL-нет воркер на дедлайне в любом случае — связывающее ограничение это дедлайн процесса, а не терпение очереди." },
    { label: "Сразу подтвердить (ack) сообщение, чтобы оно не было передоставлено, и продолжать работать.", misconception: "Ack до завершения означает, что SIGKILL теряет задачу без передоставки — обратное безопасности. Ack делается только после завершения работы." },
  ]}
/>

<Quiz
  id="07-graceful-shutdown-choice-5"
  lessonSlug="07-graceful-shutdown"
  lang="ru"
  question="Во время shutdown сервис начинает проваливать и readiness, и liveness probe, чтобы «выключиться быстрее». Drain'ы начинают обрезаться. Что пошло не так?"
  choices={[
    { label: "Провалившаяся liveness probe сообщает оркестратору, что контейнер сломан, поэтому он может убить или перезапустить его, обрезав drain. Проваливаться должна только readiness (остановить новый трафик); liveness должна оставаться зелёной, потому что дрейнирующий процесс здоров.", correct: true },
    { label: "Провал readiness повреждает in-flight запросы, которые ещё дрейнируются.", misconception: "Readiness управляет только маршрутизацией нового трафика; она не трогает in-flight запросы, которые продолжают дрейнироваться. Вред пришёл от провала liveness, а не readiness." },
    { label: "Liveness управляет маршрутизацией, а readiness — перезапусками, поэтому probe использовали наоборот.", misconception: "Всё наоборот: readiness управляет маршрутизацией, liveness — считается ли контейнер сломанным и перезапускается. Ошибка была в том, чтобы вообще проваливать liveness при здоровом drain." },
    { label: "Обе probe всегда должны проваливаться вместе, чтобы оставаться согласованными.", misconception: "Они отвечают на разные вопросы и должны расходиться во время shutdown: readiness нет (нет нового трафика), liveness да (ещё жив, и drain — здоровое поведение)." },
  ]}
/>

<Quiz
  id="07-graceful-shutdown-choice-6"
  lessonSlug="07-graceful-shutdown"
  lang="ru"
  question="Каждый shutdown отдельного инстанса безупречен, но rolling-деплои под нагрузкой всё равно сбрасывают тонкий слой 502, и иногда выжившие поды падают. Какая пара fleet-уровневых исправлений это решает?"
  choices={[
    { label: "Surge новых подов до ready перед drain старых (maxUnavailable: 0, положительный maxSurge), чтобы capacity никогда не проседала, и jitter на Connection: close, чтобы клиенты не переподключались все разом и не затоптали холодные поды.", correct: true },
    { label: "Поднять per-pod grace period и сократить интервал readiness probe.", misconception: "Grace period управляет тем, как дрейнирует один под, а не capacity флота или всплеском reconnect. 502 идут от проседания capacity и синхронных reconnect, а не от слишком короткого per-pod drain." },
    { label: "Отключить connection draining, чтобы поды выходили мгновенно и быстрее освобождали capacity.", misconception: "Мгновенный выход обрывает in-flight работу — исходный баг abrupt-kill — и ничего не делает для capacity. Capacity держится surge новых подов до ready, а не более быстрыми выходами." },
    { label: "Слать Connection: close всем клиентам в один миг, чтобы переподключения завершились быстро.", misconception: "Синхронные closes — это ровно то, что вызывает thundering reconnect, опрокидывающий холодные поды. Лекарство — jitter closes по рандомизированному окну." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна упорядоченная дисциплина. Сначала контракт сигналов — SIGTERM должен реально дойти до зарегистрированного обработчика в PID 1, иначе SIGKILL завершит всё на дедлайне grace period. Затем гонка deregistration: провали readiness и выжди propagation до того, как перестанешь принимать, чтобы не отказывать трафику, который балансировщик ещё маршрутизирует. Затем drain в reverse dependency order — HTTP первым, datastores последними — чтобы ни один in-flight запрос не потерял ресурс, который ему ещё нужен, и всё это под guardian timeout. Работа, не влезающая в бюджет дедлайна, отклоняется (длинные запросы) или ставится в requeue (задачи), и requeue безопасен только при идемпотентном consumer. Наконец, подними это до флота: surge перед drain, deregister перед terminate и jitter синхронных closes — потому что локальная корректность не складывается в глобальную бесплатно.
</Recap>
