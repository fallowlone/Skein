---
concepts:
- bulkhead
- failure-isolation
- thread-pool-isolation
- semaphore-isolation
- resource-partition
- concurrency-limit
- blast-radius
deepensInto:
- backend/06-circuit-breakers/05-fallbacks-and-degradation
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 4
prereqs:
- backend/06-circuit-breakers/03-thresholds-and-windows
slug: 04-bulkheads-and-isolation
sources:
- https://learn.microsoft.com/en-us/azure/architecture/patterns/bulkhead
- https://resilience4j.readme.io/docs/bulkhead
- https://github.com/Netflix/Hystrix/wiki/How-it-Works
spiral:
- failure-modes
- resilience
- resource-limits
status: ready
summary: Брейкер реагирует после накопления отказов, но общий пул потоков может осушить
  одна медленная зависимость ещё до срабатывания. Bulkhead ограничивает конкурентность
  на зависимость, чтобы больной даунстрим не сожрал весь бюджет — thread-pool против
  semaphore это компромисс.
title: 'Bulkheads: изоляция доменов отказа'
track: backend
unit: 06-circuit-breakers
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
Твой сервис зовёт три даунстрима — платежи, рекомендации и поиск — из одного общего пула воркеров на 50 потоков. Рекомендации, наименее важные из трёх, замедляются до 5 с. За секунды все 50 потоков припаркованы на вызовах рекомендаций, и теперь платежи и поиск тоже падают, потому что не осталось потоков их выполнить. Брейкер на рекомендациях *сработает* — но лишь после накопления достаточного числа отказов, и к тому времени урон нанесён: некритичная зависимость уронила критичную, потому что они черпали из одного ведра. Брейкер ограничивает, как *долго* ты зовёшь больную зависимость; bulkhead ограничивает, *сколько* твоей ёмкости любая одна зависимость может вообще удерживать.
</Hook>

<Crux>Bulkhead ограничивает конкурентность на зависимость, чтобы один медленный даунстрим не осушил весь бюджет потоков — изолируя домены отказа.</Crux>

<Explanation>

## Проблема общего бюджета

Брейкер *реактивен* — он срабатывает после накопления доли отказов по окну. Но каскад может случиться *до* его реакции, потому что пока брейкер ещё считает, каждый вызов в полёте к медленной зависимости держит поток из пула, общего со всеми остальными. Если этот пул общий на все зависимости, одна медленная зависимость может занять его весь, и теперь вызовы к совершенно здоровым зависимостям падают из-за нехватки потока. Брейкер в итоге срабатывает, но радиус поражения уже накрыл весь сервис.

Имя из судостроения: корпус делится на водонепроницаемые **bulkhead**-отсеки, чтобы пробоина затопила один отсек, а не весь корабль. Программный паттерн идентичен — раздели свои ресурсы, чтобы отказ был ограничен одним отсеком вместо потопления всего.

## Раздели бюджет на зависимость

Bulkhead **ограничивает конкурентность на зависимость**. Вместо одного пула на 50 потоков, общего на платежи, рекомендации и поиск, ты даёшь каждой свою ограниченную долю — скажем 20 / 10 / 20. Теперь если рекомендации замедляются, они могут припереть максимум свои 10; остальные 40 остаются доступны платежам и поиску. Вызовы рекомендаций сверх 10 отклоняются немедленно (или коротко ставятся в очередь), что *само* кормит сигнал отказа брейкера — но критически, отказ заперт в рекомендациях. Одна зависимость больше не может потратить ёмкость всего сервиса.

Это та же идея ограниченной конкурентности, что и размер пула, применённая для **изоляции**, а не пропускной способности: задача лимита не максимизировать работу, а ограничить радиус поражения любой одной зависимости.

## Два способа изолировать: потоки против семафоров

Когда тянешься к bulkhead, сразу встаёт вопрос: как изоляция работает под капотом — останавливает ли она застрявший вызов от припирания потока, или лишь не даёт стартовать новым? Ответ определяет, какой вариант выбрать.

Есть две реализации, и выбор — настоящий компромисс:

- **Thread-pool изоляция.** Каждая зависимость получает свой выделенный пул потоков (модель Hystrix по умолчанию, `coreSize = 10`). Вызывающий передаёт работу этому пулу и ждёт со своим таймаутом. Большое преимущество: поскольку вызов выполняется на *отдельном* потоке, вызывающий может **уйти от зависшего вызова** — если зависимость блокируется навсегда, таймаут вызывающего всё равно срабатывает и освобождает вызывающего. Цена — накладные расходы: каждый вызов платит передачу потока и переключение контекста, и ты содержишь много пулов.
- **Semaphore изоляция.** Простой счётчик ограничивает, сколько вызовов выполняется конкурентно (`SemaphoreBulkhead` у resilience4j, `maxConcurrentCalls = 25`, `maxWaitDuration = 0`). Вызов выполняется на *собственном* потоке вызывающего — без передачи, почти без накладных. Загвоздка: семафор умеет только *считать*; он не может прервать вызов, который уже блокируется. Если зависимость зависает, вызывающий поток зависает с ней, и семафор лишь останавливает *новые* вызовы — он не может спасти уже застрявшие.

Правило большого пальца: используй **semaphore** изоляцию для быстрых, внутрипроцессных или неблокирующих вызовов, где важны накладные и зависания невозможны; используй **thread-pool** изоляцию для сетевых вызовов, которые могут зависнуть, потому что только отдельный поток даёт реально бросить застрявший вызов.

<Inset kind="why" lang="ru">
Почему семафор не может защитить от зависающей зависимости, хотя он явно ограничивает конкурентность? Потому что семафор — это *только* счётчик: он выдаёт пропуск перед вызовом и освобождает после. У него нет своего потока и нет способа дотянуться до уже начавшегося вызова и остановить его. Если зависимость приняла твой запрос и потом никогда не отвечает, поток, взявший пропуск, сидит заблокированным в сетевом чтении бесконечно, всё ещё держа пропуск. Семафор честно предотвращает старт *нового* вызова, как только все пропуски взяты, так что он ограничивает, сколько потоков может застрять разом — но он не может отлепить уже застрявшие. Thread-pool bulkhead может, потому что вызов выполняется на потоке пула, пока вызывающий ждёт отдельно со своим таймаутом; когда таймаут срабатывает, вызывающий перестаёт ждать и забирает свой поток обратно, хотя поток пула всё ещё застрял на мёртвой зависимости. Цена этой силы реальна — передача потока и переключение контекста на каждом вызове, плюс память и накладные планирования многих пулов — так что ты не платишь её везде. Ты платишь её ровно там, где вызовы могут зависнуть, то есть сетевой I/O, и используешь дешёвый семафор везде, где зависание невозможно. Различие то же, что в уроке про таймауты: только *независимый* ожидающий может навязать дедлайн вызову, который он не контролирует.
</Inset>

<div data-lesson-visual class="overflow-x-auto my-6">

| | Thread-pool изоляция | Semaphore изоляция |
|---|---|---|
| Механизм | Выделенный пул на зависимость | Счётчик конкурентности |
| Выполняется на | Потоке пула (передача) | Собственном потоке вызывающего |
| Накладные | Выше (переключение контекста, много пулов) | Около нуля |
| Бросить зависший вызов? | Да — вызывающий таймаутит независимо | Нет — вызывающий блокируется с вызовом |
| Лучше для | Сетевых вызовов, что могут зависнуть | Быстрых, внутрипроцессных, неблокирующих |
| Пример по умолчанию | Hystrix coreSize 10 | resilience4j maxConcurrentCalls 25 |

</div>

<Quiz
  id="cb-bulkheads-q1"
  lessonSlug="04-bulkheads-and-isolation"
  lang="ru"
  question="Три даунстрима делят один пул на 50 потоков. Наименее важный замедляется до 5 с, и весь сервис падает, хотя его брейкер в итоге срабатывает. Что bulkhead добавляет, чего брейкер сам по себе не даёт?"
  choices={[
    { label: "Лимит конкурентности на зависимость, чтобы медленный даунстрим припёр максимум свою долю, а остальные сохранили потоки — брейкер лишь ограничивает, как долго ты зовёшь, не сколько ёмкости одна зависимость может удержать", correct: true },
    { label: "Более быстрое окно доли отказов", misconception: "Более быстрое окно срабатывает брейкер раньше, но не останавливает одну зависимость от осушения общего пула до того; изоляция — недостающий кусок." },
    { label: "Автоматические ретраи для медленной зависимости", misconception: "Ретраи добавили бы нагрузку, не изолировали; bulkhead разделяет бюджет ресурсов." },
    { label: "Шифрование на зависимость", misconception: "Bulkheads изолируют бюджеты конкурентности, не безопасность; шифрование не при чём." },
  ]}
/>

<Quiz
  id="cb-bulkheads-q2"
  lessonSlug="04-bulkheads-and-isolation"
  lang="ru"
  question="Почему thread-pool изоляция защищает от зависающего сетевого вызова, а semaphore изоляция нет?"
  choices={[
    { label: "Вызов выполняется на отдельном потоке пула, пока вызывающий ждёт со своим таймаутом, поэтому когда таймаут срабатывает, вызывающий забирает свой поток, хотя поток пула ещё застрял", correct: true },
    { label: "Пулы потоков делают сетевой вызов быстрее", misconception: "Изоляция потоками добавляет накладные, не скорость; её ценность в способности бросить зависший вызов." },
    { label: "Семафоры не ограничивают конкурентность", misconception: "Семафоры ограничивают конкурентность; они просто не могут прервать вызов, уже блокирующийся на потоке вызывающего." },
    { label: "Пулы потоков ретраят вызов автоматически", misconception: "Изоляция — не ретраи; суть в том, что вызывающий может таймаутить независимо от застрявшего вызова." },
  ]}
/>

</Explanation>

<StackDiagram
  label="Bulkhead разделяет бюджет потоков, чтобы одна зависимость не осушила остальные"
  hue="var(--d-backend)"
  layers={[
    {label:"Пул платежей",note:"20 потоков — критичен; изолирован от рекомендаций"},
    {label:"Пул поиска",note:"20 потоков — критичен; изолирован от рекомендаций"},
    {label:"Пул рекомендаций",note:"10 потоков — если медленный, только этот пул полнится (радиус ограничен)"}
  ]}
  caption="Без bulkhead все 50 потоков общие — медленные рекомендации голодят платежи и поиск до срабатывания брейкера. С пулами на зависимость каждый домен отказа изолирован; полная нагрузка на один не может осушить другие."
/>

<KeyTakeaway>Брейкер реактивен — он срабатывает лишь после накопления доли отказов — поэтому общий пул потоков может быть полностью осушён одной медленной зависимостью до того, как брейкер вообще среагирует, роняя вызовы к здоровым зависимостям из-за нехватки потока. Bulkhead чинит это, ограничивая конкурентность на зависимость: каждая получает свою ограниченную долю, так что медленный даунстрим может припереть максимум свою часть, а остальные остаются доступны — отказ заперт в одном отсеке, как в корпусе корабля. Это ограниченная конкурентность, применённая для изоляции, а не пропускной способности. Две реализации в компромиссе: thread-pool изоляция (Hystrix coreSize 10) выполняет каждый вызов на выделенном потоке пула, чтобы вызывающий мог бросить зависший вызов через свой таймаут, ценой передачи и переключения контекста на вызов; semaphore изоляция (resilience4j maxConcurrentCalls 25) — счётчик с почти нулевыми накладными, что выполняется на потоке вызывающего, но не может прервать вызов, уже блокирующийся. Используй семафоры для быстрых неблокирующих вызовов и пулы потоков для сетевых вызовов, что могут зависнуть.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="cb-bulkheads-retrieval"
  lang="ru"
  questions={[
    { q: "Почему circuit breaker недостаточно сам по себе, и что добавляет bulkhead?", a: "Брейкер реактивен: он срабатывает лишь после накопления доли отказов по окну, и каскад может случиться до его реакции, потому что пока брейкер ещё считает, каждый вызов в полёте к медленной зависимости держит поток из пула, общего со всеми остальными. Если этот пул общий на все зависимости, одна медленная зависимость может занять его весь, поэтому вызовы к совершенно здоровым зависимостям падают из-за нехватки потока, и брейкер в итоге срабатывает, но радиус поражения уже накрыл весь сервис. Bulkhead — названный по водонепроницаемым отсекам, что не дают пробоине корпуса потопить весь корабль — ограничивает конкурентность на зависимость, давая каждой свою ограниченную долю вместо одного общего пула, так что если одна замедляется, она может припереть максимум свою долю, пока остальные остаются доступны. Вызовы сверх доли отклоняются немедленно, что само кормит сигнал отказа брейкера, но отказ заперт в той одной зависимости. Это та же идея ограниченной конкурентности, что и размер пула, применённая для изоляции, а не пропускной способности: задача лимита — ограничить радиус поражения, не максимизировать работу." },
    { q: "Каков компромисс между thread-pool и semaphore изоляцией?", a: "Thread-pool изоляция даёт каждой зависимости свой выделенный пул потоков — модель Hystrix по умолчанию с coreSize 10 — и вызывающий передаёт работу этому пулу и ждёт со своим таймаутом. Её большое преимущество в том, что поскольку вызов выполняется на отдельном потоке, вызывающий может уйти от зависшего вызова: если зависимость блокируется навсегда, таймаут вызывающего всё равно срабатывает и освобождает вызывающего, хотя поток пула остаётся застрявшим. Цена — накладные: каждый вызов платит передачу потока и переключение контекста, и ты содержишь много пулов. Semaphore изоляция — это просто счётчик, ограничивающий конкурентные вызовы — SemaphoreBulkhead у resilience4j с maxConcurrentCalls 25 и maxWaitDuration 0 — и вызов выполняется на собственном потоке вызывающего почти без накладных. Загвоздка в том, что семафор умеет только считать; он не может прервать уже блокирующийся вызов, поэтому если зависимость зависает, вызывающий поток зависает с ней, и семафор лишь останавливает новые вызовы. Правило большого пальца: семафоры для быстрых, внутрипроцессных, неблокирующих вызовов, где важны накладные и зависания невозможны, пулы потоков для сетевых вызовов, что могут зависнуть, потому что только отдельный поток даёт реально бросить застрявший вызов." },
  ]}
/>

<Recap lang="ru">
Circuit breaker ограничивает, как долго ты зовёшь больную зависимость, но он реактивен — срабатывает лишь после накопления отказов, а тем временем общий пул потоков может быть полностью осушён одним медленным даунстримом, роняя вызовы к здоровым зависимостям из-за нехватки потока. Bulkhead закрывает этот разрыв, ограничивая конкурентность на зависимость, как водонепроницаемые отсеки корабля запирают пробоину: дай платежам, рекомендациям и поиску свои ограниченные доли, и медленная может припереть максимум свою часть, заперев отказ в одном отсеке. Это ограниченная конкурентность, применённая для изоляции, а не пропускной способности. Реализация — реальный компромисс: thread-pool изоляция выполняет каждый вызов на выделенном потоке пула, чтобы вызывающий мог бросить зависший вызов через свой таймаут, платя передачу и переключение контекста на вызов; semaphore изоляция — счётчик с почти нулевыми накладными на собственном потоке вызывающего, что ограничивает конкурентность, но не может прервать уже блокирующийся вызов. Используй семафоры для быстрой неблокирующей работы и пулы потоков для сетевых вызовов, что могут зависнуть. Теперь, когда проектируешь сервис с несколькими даунстримами, на каждую зависимость два вопроса: как долго брейкеру разрешено продолжать звать до срабатывания, и сколько общего бюджета потоков эта зависимость вообще может удержать? Оба ответа есть здесь. Брейкер и bulkhead вместе ограничивают как долго и сколько — но ни один не отвечает, что вызывающий должен *вернуть*, когда вызов отклонён. Следующий урок покрывает таймауты как триггер и фолбэки как ответ.
</Recap>
