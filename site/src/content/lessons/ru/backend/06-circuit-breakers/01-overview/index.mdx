---
concepts:
- circuit-breaker
- cascading-failure
- fast-fail
- thread-starvation
- fault-isolation
- timeout
deepensInto:
- backend/06-circuit-breakers/02-the-state-machine
estMin: 12
lang: ru
lessonType: topic
level: junior
order: 1
prereqs:
- backend/05-idempotency-retries/03-retry-strategies
slug: 01-overview
sources:
- https://martinfowler.com/bliki/CircuitBreaker.html
- https://learn.microsoft.com/en-us/azure/architecture/patterns/circuit-breaker
- https://sre.google/sre-book/addressing-cascading-failures/
spiral:
- failure-modes
- resilience
status: ready
summary: 'Зависимость редко падает чисто — она замедляется, и медленная зависимость
  опаснее мёртвой: каждый вызывающий ждёт, занимая поток, пока весь сервис не задохнётся.
  Circuit breaker быстро отказывает вызовам к больной зависимости, чтобы вызывающий
  перестал ждать.'
title: 'Зачем circuit breaker: медленная зависимость роняет вызывающего'
track: backend
unit: 06-circuit-breakers
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import threadHoldTime from "./infographics/thread-hold-time.svg?raw";

<Hook>
Платёжный провайдер во время инцидента замедляется с 50 мс до 5 с — он не падает, просто тормозит. Твой обработчик checkout зовёт его на каждый заказ и ждёт. За секунды каждый рабочий поток припаркован на 5-секундном платёжном вызове, пул потоков полон, и сервис перестаёт принимать *любой* запрос — включая главную страницу и листинг товаров, которые платежей вообще не касаются. Ничего не упало. Один даунстрим затормозил, твой код вежливо ждал, и ожидание расползлось, пока весь сервис не лёг. Circuit breaker — это тот элемент, который заметил бы, что платежи отказывают, и начал бы отклонять эти вызовы *мгновенно* — освобождая потоки и сохраняя жизнь остальному приложению.
</Hook>

<Crux>Медленная зависимость хуже мёртвой: каждый вызывающий ждёт, занимая поток. Брейкер быстро отказывает ей, чтобы вызывающие перестали ждать.</Crux>

<Explanation>

## Медленная зависимость опаснее мёртвой

Зависимость, которая *отказывает* в соединении, падает мгновенно — твой вызов возвращает ошибку за миллисекунду, и поток идёт дальше. Зависимость, которая *принимает* соединение и потом 5 секунд отвечает, гораздо хуже, потому что теперь каждый вызывающий блокируется на 5 секунд, держа дефицитные ресурсы: рабочий поток, сокет, взятое из пула соединение к БД, и часто вышестоящего вызывающего, ждущего его.

Это та же проблема занятости из юнита про пулы, теперь на слой выше. Под нагрузкой математика жестока: если обработчик держит поток 5 с вместо 50 мс, тому же трафику нужно **в 100 раз больше потоков**, чтобы успевать. Их никогда не хватает, поэтому запросы выстраиваются в очередь, пул потоков переполняется, и сервис уже не может обслуживать работу, никак не связанную с медленной зависимостью. Один больной даунстрим становится полным отказом — **каскадным сбоем**.

## Быстрый отказ лучше зависания

Решение контринтуитивно: когда зависимость отказывает, самое безопасное — *перестать её звать и вернуть ошибку немедленно*. Вернуть отказ за 1 мс строго лучше, чем таймаут за 5 с, потому что быстрый отказ **освобождает ресурсы вызывающего** — поток, соединение, слот вышестоящего — на что-то полезное: обслужить другие маршруты, вернуть деградированный ответ, сбросить нагрузку. Медленный успех, который так и не придёт, всё равно стоит тебе всего, что стоил бы настоящий успех; быстрый отказ не стоит почти ничего.

<Infographic
  svg={threadHoldTime}
  label="Столбчатая диаграмма: сколько рабочий поток занят на вызов — здоровый вызов держит его 50 мс, медленная зависимость без брейкера держит его 5000 мс (весь таймаут), а быстрый отказ с брейкером освобождает за ~1 мс."
  caption="Медленный таймаут припирает поток ~в 100× дольше здорового вызова; быстрый отказ брейкера возвращает его почти к нулю (~1 мс), близко к здоровой базе."
  hue="var(--d-backend)"
/>

**Circuit breaker** автоматизирует ровно это. Он стоит перед зависимостью, следит за вызовами, и когда отказы пересекают порог — **размыкается**: на период остывания он отклоняет вызовы мгновенно, даже не пытаясь, а потом осторожно проверяет, восстановилась ли зависимость. Имя буквальное: как электрический автомат, он размыкается, чтобы прекратить ток в неисправность, защищая всё, что подключено за ним.

<Inset kind="why" lang="ru">
Зачем отклонять вызовы, которые ты *мог бы* сделать, вместо того чтобы каждый пробовал и упирался в таймаут? Потому что каждая попытка к больной зависимости не бесплатна — она тратит поток, соединение и таймаут стенных часов ожидания, всё на вызов, который почти наверняка упадёт. Когда зависимость действительно лежит, эти попытки не дают пользы и приносят реальный вред: держат твои ресурсы припёртыми, наваливают ретраи на сервис, которому для восстановления нужно *меньше* нагрузки, и привязывают твою латентность к сломанной зависимости. Ставка брейкера статистическая — раз достаточно недавних вызовов упало, следующий с подавляющей вероятностью упадёт тоже, так что ожидаемая ценность попытки отрицательна. Быстрый отказ превращает медленный, жрущий ресурсы, усиливающий вред сбой в дешёвый и мгновенный, и возвращает освободившуюся ёмкость частям системы, которые ещё работают. Это та же дисциплина, что и ограниченная очередь ожидания: ты ограничиваешь ущерб, который сломанная штука может нанести, вместо того чтобы дать ей сожрать всю систему по дороге вниз.
</Inset>

## Что брейкер тебе даёт

Задай себе вопрос: что пришлось бы написать без брейкера, чтобы получить тот же эффект? Каждую зависимость — свой таймаут-и-ошибка-путь, продублированный по всей кодовой базе, скорее всего непоследовательно. Брейкер делает это единым, переиспользуемым решением.

Брейкер превращает неограниченный системный сбой в ограниченный локальный. Вместо того чтобы каждый вызывающий узнавал о поломке зависимости медленным дорогим путём — ждя таймаут, — брейкер узнаёт об этом *один раз*, а потом коротко-замыкает всех остальных дёшево, пока зависимость не докажет, что восстановилась. Именно это изменение и останавливает один медленный сервис от заваливания остальных.

<div data-lesson-visual class="overflow-x-auto my-6">

| | Без брейкера | С брейкером |
|---|---|---|
| Медленная зависимость | Каждый вызывающий ждёт весь таймаут | Первые несколько падают, остальные отклонены мгновенно |
| Стоимость потока/соединения | Припёрты на всё время ожидания | Освобождены сразу при быстром отказе |
| Радиус поражения | Весь сервис задыхается | Ограничен одной зависимостью |
| Нагрузка восстановления | Полный трафик продолжает долбить | Только струйка пробных вызовов |
| Латентность вызывающего | Следует за сломанным даунстримом | Остаётся быстрой (мгновенная ошибка) |

</div>

<Quiz
  id="cb-overview-q1"
  lessonSlug="01-overview"
  lang="ru"
  question="Платёжный провайдер замедляется с 50 мс до 5 с, но полностью не падает. За секунды весь сервис перестаёт обслуживать даже несвязанные маршруты. Почему медленный случай хуже прямого отказа?"
  choices={[
    { label: "Медленные ответы портят данные запроса", misconception: "Ничего не портится; вред в том, что каждый вызывающий блокируется на медленном вызове, а не в повреждении данных." },
    { label: "Каждый вызывающий блокируется на 5 с, держа поток и соединение, поэтому пул переполняется и сервис задыхается — мёртвая зависимость упала бы мгновенно и освободила поток", correct: true },
    { label: "Провайдер отвергает TLS-рукопожатие, когда медленный", misconception: "Рукопожатие проходит; проблема в медленном ответе после соединения, который припирает ресурсы." },
    { label: "Замедление затрагивает только платёжный маршрут", misconception: "Оно расползается: припаркованные потоки принадлежат общему пулу, поэтому несвязанные маршруты тоже задыхаются." },
  ]}
/>

<Quiz
  id="cb-overview-q2"
  lessonSlug="01-overview"
  lang="ru"
  question="Почему вернуть ошибку за 1 мс лучше, чем таймаут за 5 с, против отказывающей зависимости?"
  choices={[
    { label: "Быстрая ошибка освобождает поток, соединение и слот вышестоящего на полезную работу вместо того, чтобы припирать их на 5 с на вызове, который всё равно упадёт", correct: true },
    { label: "Быстрая ошибка заставляет зависимость восстановиться быстрее", misconception: "Ошибка не лечит зависимость напрямую; её ценность в освобождении ресурсов вызывающего (хотя меньшая нагрузка восстановлению помогает)." },
    { label: "Ошибки за 1 мс ретраятся автоматически", misconception: "Быстрый отказ не про ретраи; он про то, чтобы не тратить ресурсы на обречённый вызов." },
    { label: "Таймауты всегда теряют данные", misconception: "Таймауты не теряют данные по своей природе; вопрос в стоимости ожидания, а не в корректности." },
  ]}
/>

<DragOrder
  id="cb-overview-drag1"
  lessonSlug="01-overview"
  lang="ru"
  prompt="Расставь по порядку, как медленная зависимость каскадом превращается в полный отказ без брейкера:"
  items={[
    "Даунстрим-зависимость замедляется с миллисекунд до секунд",
    "Каждый вызывающий блокируется на ней, держа рабочий поток всё время ожидания",
    "Общий пул потоков заполняется припаркованными вызывающими",
    "Сервис больше не может принять ни один запрос, даже несвязанные маршруты",
  ]}
/>

</Explanation>

<FlowDiagram
  label="Как медленная зависимость каскадом роняет весь сервис без брейкера"
  hue="var(--d-backend)"
  perRow={4}
  nodes={[
    {id:"slow",label:"Медл. зависим.",sub:"50мс→5с"},
    {id:"block",label:"Потоки блокир.",sub:"держат 5с каждый"},
    {id:"pool",label:"Пул полон",sub:"~100× потоков"},
    {id:"starve",label:"Сервис падает",sub:"все маршруты"}
  ]}
  edges={[
    {from:"slow",to:"block",label:"каждый ждёт"},
    {from:"block",to:"pool",label:"занятость×100"},
    {from:"pool",to:"starve",label:"потоков нет"}
  ]}
  caption="Зависимость, принимающая соединение и медленно отвечающая, припирает по потоку на каждого вызывающего на всё ожидание. Под нагрузкой пул заполняется и сервис падает — даже маршруты, не касающиеся медленной зависимости."
/>

<KeyTakeaway>Зависимость редко падает чисто — она замедляется, и медленная зависимость опаснее мёртвой, потому что каждый вызывающий блокируется на всё время ожидания, держа поток, сокет и взятое из пула соединение. Под нагрузкой эта занятость множится (вызов 50 мс, ставший 5 с, требует ~в 100 раз больше потоков), поэтому пул переполняется и сервис задыхается на работе, не связанной с медленным даунстримом — каскадный сбой. Контринтуитивный фикс — быстрый отказ: вернуть ошибку за 1 мс лучше таймаута за 5 с, потому что это освобождает ресурсы вызывающего на полезную работу и перестаёт наваливать нагрузку на сервис, которому для восстановления нужно меньше. Circuit breaker автоматизирует это — следит за вызовами, размыкается при пересечении порога, отклоняет вызовы мгновенно во время остывания, потом проверяет восстановление — превращая неограниченный системный сбой в ограниченный локальный.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="cb-overview-retrieval"
  lang="ru"
  questions={[
    { q: "Почему медленная зависимость опаснее зависимости, которая полностью лежит?", a: "Потому что зависимость, отказывающая в соединении, падает мгновенно — вызов возвращает ошибку примерно за миллисекунду, и поток идёт дальше, — тогда как зависимость, которая принимает соединение и потом отвечает секундами, заставляет каждого вызывающего блокироваться на всё это время, держа дефицитные ресурсы: рабочий поток, сокет, взятое перед вызовом соединение к БД и часто вышестоящего вызывающего, ждущего его. Это та же проблема занятости из юнита про пулы, на слой выше. Под нагрузкой математика жестока: если обработчик держит поток 5 секунд вместо 50 миллисекунд, тому же трафику нужно примерно в сто раз больше потоков, и их никогда не хватает, поэтому запросы встают в очередь, пул потоков переполняется, и сервис уже не может обслуживать работу, никак не связанную с медленной зависимостью. Один больной даунстрим становится полным отказом — каскадным сбоем — ровно той ситуацией, которую circuit breaker и существует предотвращать." },
    { q: "Почему быстрый отказ лучше, чем дать каждому вызову упереться в таймаут, и что брейкер на самом деле делает?", a: "Быстрый отказ лучше, потому что вернуть отказ за миллисекунду освобождает ресурсы вызывающего — поток, соединение, слот вышестоящего — на что-то полезное, тогда как медленный успех, который так и не придёт, стоит тебе всего, что стоил бы настоящий успех. Каждая попытка к больной зависимости тратит поток, соединение и таймаут ожидания на вызов, который почти наверняка упадёт, и хуже того — продолжает наваливать нагрузку на сервис, которому для восстановления нужно меньше нагрузки. Circuit breaker автоматизирует ровно это: он стоит перед зависимостью, следит за вызовами, и когда отказы пересекают порог — размыкается, отклоняя вызовы мгновенно на период остывания, даже не пытаясь, а потом осторожно проверяет, восстановилась ли зависимость. Имя буквальное — как электрический автомат, он размыкается, чтобы прекратить ток в неисправность, защищая всё подключённое за ним, и превращает неограниченный системный сбой в ограниченный локальный, узнавая о неисправности один раз вместо того, чтобы каждый вызывающий переоткрывал её медленным путём." },
  ]}
/>

<Recap lang="ru">
Зависимость редко умирает чисто; она замедляется, и медленная — опасный случай, потому что каждый вызывающий ждёт всё время, держа поток, сокет и взятое из пула соединение — проблема занятости из юнита про пулы на слой выше. Под нагрузкой вызов, выросший с 50 мс до 5 с, требует примерно в сто раз больше потоков, поэтому пул переполняется и сервис задыхается даже на маршрутах, которые медленной зависимости не касаются: каскадный сбой, где один больной даунстрим роняет всё. Фикс контринтуитивен — когда зависимость отказывает, перестань её звать и верни ошибку немедленно, потому что отказ за 1 мс освобождает ресурсы вызывающего на полезную работу, а таймаут за 5 с припирает их на обречённом вызове и продолжает долбить сервис, которому нужно меньше нагрузки. Circuit breaker автоматизирует это: следит за вызовами, размыкается при пересечении порога, отклоняет вызовы мгновенно во время остывания, потом проверяет восстановление. Теперь, когда увидишь аварию, чьи симптомы расползлись далеко за сломанную зависимость — несвязанные маршруты падают с таймаутами, пулы потоков исчерпаны — первый вопрос: какой медленный даунстрим держит потоки, и стоит ли перед ним брейкер? Следующий урок раскрывает брейкер изнутри — трёхсостоянийную машину closed, open, half-open и таймер остывания, решающий, как быстро он проверяет восстановление.
</Recap>
