---
concepts:
- circuit-breaker
- half-open
- sliding-window
- bulkhead
- fallback
- retry-storm
estMin: 13
lang: ru
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://martinfowler.com/bliki/CircuitBreaker.html
- https://resilience4j.readme.io/docs/circuitbreaker
- https://sre.google/sre-book/addressing-cascading-failures/
status: ready
summary: "Промпты на свободное припоминание по всему юниту. Сначала ответь своими словами, затем раскрой эталонный ответ и сравни."
title: 'Circuit breakers: тест на припоминание'
track: backend
unit: 06-circuit-breakers
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Припоминание бьёт перечитывание. Для каждого промпта скажи или напиши полный ответ по памяти, прежде чем открыть эталон — именно усилие припоминания закрепляет материал.
</Hook>

<Goal lang="ru">
Реконструируй хребет юнита — почему медленное хуже мёртвого, машина из трёх состояний, условие срабатывания, bulkheads, пара timeout-и-fallback и сбои на масштабе флота — не подглядывая в уроки.
</Goal>

<RetrievalDrawer
  client:load
  id="06-circuit-breakers-short-retrieval"
  lang="ru"
  questions={[
    { q: "Почему медленная зависимость опаснее полностью мёртвой и какова ключевая работа circuit breaker'а?", a: "Зависимость, отвергающая соединения, отказывает примерно за миллисекунду, и поток идёт дальше, но та, что принимает соединение и затем отвечает секундами, заставляет каждого вызывающего блокироваться на всё ожидание, удерживая дефицитные ресурсы — рабочий поток, сокет, пулированное соединение и часто upstream-вызывающего, ждущего его. Под нагрузкой математика жестока: хендлер, удерживающий поток 5 с вместо 50 мс, требует примерно в 100 раз больше потоков, поэтому общий пул заполняется, запросы встают в очередь, и сервис больше не может обслуживать работу, не связанную с медленной зависимостью — один больной downstream становится каскадным системным сбоем. Ключевая работа circuit breaker'а — fast-fail: он наблюдает вызовы к зависимости, срабатывает, когда отказы переходят порог, и затем отвергает вызовы мгновенно, не пытаясь их выполнить, освобождая ресурсы вызывающего для полезной работы и превращая неограниченный системный сбой в ограниченный локальный." },
    { q: "Опиши три состояния circuit breaker'а и четыре перехода между ними.", a: "Closed — нормальное состояние: вызовы проходят насквозь, и breaker считает отказы; когда отказы переходят условие срабатывания, он переходит в open и стартует таймер cooldown. Open — состояние срабатывания: каждый вызов отвергается мгновенно как исключение или fallback, не трогая зависимость вообще; когда cooldown истекает, он переходит в half-open. Half-open — состояние зондирования: ограниченное число пробных вызовов пропускается для проверки восстановления, и если все успешны, breaker возвращается в closed и сбрасывает счётчики, а если хоть один падает — сразу обратно в open и перезапускает cooldown. Итак, четыре перехода: closed-в-open при слишком многих отказах, open-в-half-open по таймеру, half-open-в-closed при успехе и half-open-в-open при отказе — и каждый переход это решение о том, сколько нагрузки слать зависимости с неопределённым здоровьем. Half-open существует, чтобы только что восстановившийся хрупкий сервис зондировался струйкой, а не заливался полным backlog'ом, который снова уронил бы его." },
    { q: "Каково условие срабатывания и почему минимальный порог объёма необходим?", a: "Breaker не срабатывает на единственном отказе или абсолютном count'е — он срабатывает на failure rate по скользящему окну недавних вызовов (resilience4j failureRateThreshold и Hystrix errorThresholdPercentage оба по умолчанию 50%), что делает его scale-invariant: 50% значимы, видит ли эндпоинт 10 или 10 000 вызовов в секунду. Окно бывает count-based (последние N вызовов, resilience4j по умолчанию 100) или time-based (последние T секунд в бакетах, Hystrix использует 10 с, разбитые на десять односекундных бакетов), и оно скользит, так что считается лишь недавнее здоровье. Минимальный порог объёма (Hystrix requestVolumeThreshold 20, resilience4j minimumNumberOfCalls 100) требует, чтобы breaker увидел хотя бы столько вызовов перед срабатыванием, потому что при низком объёме rate — статистический шум: один отказ из одного это 100%, но ни о чём не говорит. Это важнейшая защита от флапа breaker'а на разовом блипе, и стоимость асимметрична: ложное срабатывание — самонаведённый сбой, а ожидание ещё нескольких вызовов почти ничего не стоит." },
    { q: "Почему breaker'а недостаточно самого по себе, что добавляет bulkhead и каков компромисс thread-pool против semaphore?", a: "Breaker реактивен — он срабатывает лишь после накопления failure rate — поэтому общий пул потоков может быть полностью осушён одной медленной зависимостью до реакции breaker'а, проваливая вызовы к здоровым зависимостям из-за нехватки потока. Bulkhead (назван по водонепроницаемым отсекам корабля) ограничивает конкурентность на зависимость, давая каждой свою ограниченную долю, чтобы медленный downstream пришпиливал максимум свою долю, пока остальные доступны, сдерживая сбой в одном отсеке. Реализация — реальный компромисс. Thread-pool isolation даёт каждой зависимости выделенный пул (Hystrix coreSize 10), и вызывающий ждёт со своим timeout, так что может уйти от зависшего вызова, даже если поток пула остаётся застрявшим — ценой hand-off и context switch на вызов. Semaphore isolation — счётчик с почти нулевыми накладными (resilience4j maxConcurrentCalls 25), работающий на собственном потоке вызывающего, но он умеет лишь считать: он не может прервать уже блокирующийся вызов, поэтому если зависимость висит, вызывающий поток висит с ней. Используй semaphore для быстрых неблокирующих вызовов, thread pool — для сетевых вызовов, которые могут зависнуть." },
    { q: "Почему timeout — триггер, а fallback — ответ, и почему graceful degradation должна быть редкой?", a: "Breaker считает отказы, но опасная медленная зависимость не даёт ошибки — она просто висит — поэтому без timeout вызов не успех и не отказ, невидим для breaker'а, который сидит закрытым весь сбой. Timeout (Hystrix по умолчанию 1 с) превращает зависание в считаемый отказ; breaker над зависимостью без timeout молча ничего не делает. Когда breaker открыт, вызовы отвергаются мгновенно, но во что? Fallback — это ответ, от лучшего к худшему: деградировавший-но-корректный ответ, убирающий сломанную фичу и сохраняющий страницу, last-known-good кэш, статический дефолт, очередь-на-потом через outbox для записей и чистый fail-fast 503 с Retry-After, когда никакой fallback не осмыслен — выбирается на место вызова, ведь отсутствующая карусель не должна провалить страницу, а отсутствующая авторизация платежа должна провалить заказ. Graceful degradation должна быть редкой, потому что постоянно срабатывающий fallback отмывает сбой в нормальную работу: он прячет реальный failure rate, оставляет редко тестируемый путь несущим и стирает сигнал, отличающий degraded от healthy, поэтому зрелые команды алертят на частоту деградации, а когда весь сервис перегружен, пол — это load shedding с deadline-aware очередью." },
    { q: "Как меняются breaker'ы, retries и shedding при переходе от одного процесса к флоту?", a: "Каждая одно-процессная интуиция ломается. Состояние breaker'а пер-инстансовое: пятьдесят инстансов держат пятьдесят независимых count'ов без общего вердикта, поэтому один может быть открыт, пока другой продолжает звать, а свежий инстанс должен заново открыть сбой, который его собратья уже знают; можно централизовать состояние в общем хранилище, но это добавляет сетевой вызов на горячем пути и новую зависимость, поэтому локальное состояние — прагматичный дефолт, ведь breaker — статистическое устройство, а не точный переключатель. Реальная опасность флота — retries под breaker'ом, которые умножаются по цепочке — канонические у Google 4×4×4 = 64 — в retry storm; фиксы: ретраить только на одном слое, ограничить абсолютный rate ретраев бюджетом на процесс вроде 60 в минуту, использовать exponential backoff с jitter и держать breaker над ретраями, чтобы открытый контур короткозамкнул весь стек. Восстановление тоже сбивается в стадо: breaker'ы, сработавшие вместе, разсрабатывают вместе, поэтому half-open пробам нужен тот же jitter, что и ретраям, потому что распределённый враг — синхронизация между клиентами, а не любой один клиент. И load shedding работает, лишь когда он координированный, priority-based и deadline-aware, иначе инстансы просто перемешивают одну и ту же нагрузку между собой — у каждого пер-инстансового защитного устройства должна быть история на уровне флота." },
  ]}
/>

<Recap lang="ru">
Если ты смог реконструировать каждый ответ по памяти, ты держишь хребет юнита: медленное хуже мёртвого, потому что вызывающие блокируются и голодят общий пул; машина из трёх состояний (closed считает, open отвергает, half-open зондирует) быстро отказывает больной зависимости; условие срабатывания — failure rate по скользящему окну с минимальным порогом объёма, где медленное считается отказом; bulkhead изолирует бюджет на зависимость там, где реактивный breaker не может; timeout делает зависание считаемым, а fallback решает, что вернуть, когда открыт; и на масштабе флота опасности — пер-инстансовое состояние, retry amplification, сбивающиеся в стадо пробы и некоординированный shedding — каждой нужен ответ на уровне флота.
</Recap>
