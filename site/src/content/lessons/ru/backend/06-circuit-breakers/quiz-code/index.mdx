---
concepts:
- state-transition
- failure-rate-threshold
- minimum-volume
- half-open
- timeout
estMin: 14
lang: ru
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://resilience4j.readme.io/docs/circuitbreaker
- https://github.com/Netflix/Hystrix/wiki/Configuration
- https://learn.microsoft.com/en-us/azure/architecture/patterns/circuit-breaker
status: ready
summary: "Читай реальный конфиг breaker'а и код места вызова, предсказывай переходы состояний и поведение срабатывания и выбирай фикс, который senior сделает первым."
title: 'Circuit breakers: чтение кода и конфига'
track: backend
unit: 06-circuit-breakers
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Поведение breaker'а решается в его конфиге и в месте вызова. Прочитай каждый сниппет, предскажи, что breaker реально делает под нагрузкой, и выбери изменение, которое senior сделает первым.
</Hook>

<Goal lang="ru">
Отработай цикл, который ты гоняешь на каждом ревью breaker'а: прочитай конфиг и обёртку вызова, предскажи переходы состояний и поведение срабатывания и заметь мисконфигурацию, из-за которой breaker срабатывает не так — или не срабатывает вовсе.
</Goal>

## Сниппет 1 — машина состояний

```java
// resilience4j: проводим один breaker через инцидент
CircuitBreaker cb = registry.circuitBreaker("payments");

// 1. closed: вызовы проходят, отказы считаются
cb.executeSupplier(() -> paymentClient.charge(req));   // ok
cb.executeSupplier(() -> paymentClient.charge(req));   // throws -> counted

// ... failure rate переходит failureRateThreshold ...
// breaker переходит: CLOSED -> OPEN, стартует waitDurationInOpenState

cb.executeSupplier(() -> paymentClient.charge(req));   // <- что тут?
```

<Quiz
  id="06-circuit-breakers-code-1"
  lessonSlug="06-circuit-breakers"
  lang="ru"
  question="Breaker только что перешёл CLOSED в OPEN, и таймер cooldown идёт. Приходит следующий вызов executeSupplier. Что происходит и какова нагрузка на зависимость прямо сейчас?"
  choices={[
    { label: "Вызов отвергается мгновенно с CallNotPermittedException — он никогда не достигает paymentClient.charge. В OPEN breaker короткозамыкает каждый вызов, не трогая зависимость, поэтому зависимость получает нулевую нагрузку и может восстановиться.", correct: true },
    { label: "Вызов проходит нормально, потому что breaker только считает; он не блокирует вызовы.", misconception: "OPEN — состояние отказа: оно короткозамыкает каждый вызов, не трогая зависимость. Подсчёт идёт в CLOSED; OPEN активно блокирует." },
    { label: "Вызов сразу выполняется как half-open проба.", misconception: "Half-open входит только после истечения waitDurationInOpenState. Пока cooldown не сработал, breaker остаётся в OPEN и отвергает." },
    { label: "Вызов встаёт в очередь до конца cooldown, затем выполняется.", misconception: "OPEN не ставит вызовы в очередь на потом — он отвергает их мгновенно, чтобы вызывающий мог сделать fallback. Очередь снова пришпилила бы ресурсы, ради освобождения которых breaker и существует." },
  ]}
/>

## Сниппет 2 — конфиг порога

```java
CircuitBreakerConfig.custom()
    .slidingWindowType(SlidingWindowType.COUNT_BASED)
    .slidingWindowSize(100)
    .failureRateThreshold(50)        // срабатывать при 50% отказов
    .minimumNumberOfCalls(100)       // ...но только после 100 вызовов
    .build();
// эндпоинт: /admin/report  ->  ~6 запросов в минуту
```

<Quiz
  id="06-circuit-breakers-code-2"
  lessonSlug="06-circuit-breakers"
  lang="ru"
  question="Этот конфиг применён к низконагруженному admin-эндпоинту, обслуживающему примерно 6 запросов/минуту. Зависимость за ним жёстко падает. Как ведёт себя breaker и почему?"
  choices={[
    { label: "Он остаётся CLOSED примерно 17 минут — он не может сработать, пока 100 вызовов не заполнят окно, а при 6/мин это столько и занимает. Минимальный порог объёма (рассчитанный на высокий трафик) слишком высок для этого эндпоинта; снизь minimumNumberOfCalls или используй time-based окно, чтобы breaker реагировал за ограниченное wall-clock время.", correct: true },
    { label: "Он срабатывает сразу на первом отказе, потому что зависимость жёстко упала.", misconception: "Ниже minimumNumberOfCalls breaker остаётся закрытым независимо от failure rate. При требуемых 100 и 6/мин он не может сработать около 17 минут — порог неправильно рассчитан под этот трафик." },
    { label: "Он срабатывает на 50 отказах, потому что это половина окна.", misconception: "Порог — это rate, а не сырой count, и его даже нельзя вычислить, пока не видно 100 вызовов. Проблема в том, что порог объёма блокирует срабатывание, а не в rate." },
    { label: "Размер окна 100 не влияет на низконагруженный эндпоинт.", misconception: "Он влияет сильно: при 6/мин 100 вызовов растягиваются примерно на 17 минут, поэтому count-based окно в 100 реагирует тут очень медленно. Именно поэтому для низкого трафика часто предпочитают time-based окно." },
  ]}
/>

## Сниппет 3 — half-open проба

```java
CircuitBreakerConfig.custom()
    .waitDurationInOpenState(Duration.ofSeconds(10))
    .permittedNumberOfCallsInHalfOpenState(10)
    // automaticTransitionFromOpenToHalfOpenEnabled = false (default)
    .build();
// зависимость восстановилась на секунде 4; breaker сработал на секунде 0
// между секундой 0 и секундой 30 вызовы не приходят
```

<Quiz
  id="06-circuit-breakers-code-3"
  lessonSlug="06-circuit-breakers"
  lang="ru"
  question="Зависимость восстановилась на секунде 4, а cooldown — 10 с, но между секундой 0 и секундой 30 вызовы не приходят. Когда этот breaker реально зондирует и снова открывается для трафика?"
  choices={[
    { label: "Не раньше, чем придёт вызов после секунды 10. С отключённым automaticTransition (дефолт) breaker не переходит OPEN в HALF_OPEN по одному таймеру — он ждёт следующего вызова после cooldown, чтобы запустить переход, поэтому простаивающий breaker никогда не зондирует зависимость, которой никто не пользуется.", correct: true },
    { label: "Ровно на секунде 10 breaker авто-переходит в half-open и зондирует сам.", misconception: "Авто-переход отключён по умолчанию, поэтому breaker не зондирует по одному таймеру. Ему нужен входящий вызов после cooldown, чтобы перейти в half-open." },
    { label: "На секунде 4, когда зависимость восстанавливается, ведь breaker наблюдает зависимость напрямую.", misconception: "У breaker'а нет прямого взгляда на здоровье зависимости — он узнаёт только из исходов вызовов. Он не может знать, что восстановление было на секунде 4, не отправив пробу." },
    { label: "Он остаётся открытым вечно, потому что вызовы не приходят.", misconception: "Он остаётся открытым лишь до прихода следующего вызова после cooldown; этот вызов запускает half-open. Он не застрял навсегда — он ждёт трафика, чтобы зондировать." },
  ]}
/>

## Сниппет 4 — взаимодействие с timeout

```java
// breaker считает отказы, но у вызова нет бюджета времени
Supplier<Resp> guarded = CircuitBreaker
    .decorateSupplier(cb, () -> recsClient.fetch(req));  // может висеть 30с

// во время инцидента recsClient.fetch не ошибается -- он просто висит
Resp r = guarded.get();
```

<Quiz
  id="06-circuit-breakers-code-4"
  lessonSlug="06-circuit-breakers"
  lang="ru"
  question="Во время инцидента recsClient.fetch перестаёт ошибаться и вместо этого висит по 30 с на вызов. Breaker подключён, но никогда не срабатывает. Какой единственный фикс с наибольшим рычагом?"
  choices={[
    { label: "Обернуть вызов в TimeLimiter (или thread-pool bulkhead с timeout), чтобы зависание стало TimeoutException, который breaker может посчитать. Breaker считает отказы, а зависание не успех и не отказ, пока timeout не превратит его в отказ — без бюджета времени breaker слеп к опасному медленному случаю.", correct: true },
    { label: "Снизить failureRateThreshold, чтобы медленные вызовы срабатывали раньше.", misconception: "Никакие отказы вообще не записываются, поэтому любой порог не релевантен. Зависание сначала надо превратить в считаемый отказ через timeout." },
    { label: "Увеличить sliding window, чтобы оно захватило больше зависаний.", misconception: "Большее окно всё равно записывает ноль отказов, потому что зависание без timeout не успех и не отказ. Окно не может захватить события, которые никогда не регистрируются." },
    { label: "Добавить ретраи, чтобы зависающий вызов в итоге прошёл.", misconception: "Ретрай 30-секундного зависания умножает стоимость ресурсов и никогда не заставит breaker его посчитать. Фикс — timeout, превращающий зависание в отказ, который breaker может видеть." },
  ]}
/>

<Recap lang="ru">
Поведение breaker'а читается в конфиге и в месте вызова. OPEN короткозамыкает каждый вызов мгновенно, так что зависимость получает нулевую нагрузку; минимальный порог объёма, рассчитанный на высокий трафик, тихо блокирует срабатывание на низконагруженном эндпоинте (используй меньший порог или time-based окно); с отключённым авто-переходом breaker зондирует только когда вызов приходит после cooldown, никогда по одному таймеру, поэтому простаивающий breaker остаётся открытым, пока не вернётся трафик; и breaker без timeout слеп к зависанию, потому что только бюджет времени превращает зависание в считаемый отказ. Диагностируй по конфигу и обёртке, затем чини ту одну настройку, что заставляет breaker срабатывать правильно.
</Recap>
