---
concepts:
- circuit-breaker
- half-open
- failure-rate-threshold
- bulkhead
- timeout
- retry-storm
estMin: 13
lang: ru
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://martinfowler.com/bliki/CircuitBreaker.html
- https://resilience4j.readme.io/docs/circuitbreaker
- https://sre.google/sre-book/addressing-cascading-failures/
status: ready
summary: "Тест с выбором на синтез по всему юниту — fast-fail, машина из трёх состояний, пороги срабатывания, bulkheads, fallback'и и сбои на масштабе флота."
title: 'Circuit breakers: тест с выбором ответа'
track: backend
unit: 06-circuit-breakers
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Шесть вопросов поперёк всего юнита. Каждый отражает решение, которое ты принимаешь в реальном инциденте — не определение для заучивания, а компромисс, который надо взвесить, когда downstream болен, а твои потоки заполняются.
</Hook>

<Goal lang="ru">
Убедись, что связываешь fast-fail, машину состояний, пороги срабатывания, bulkheads, fallback'и и сбои на масштабе флота — тот синтез, к которому вели отдельные уроки.
</Goal>

<Quiz
  id="06-circuit-breakers-choice-1"
  lessonSlug="06-circuit-breakers"
  lang="ru"
  question="Платёжный провайдер замедляется с 50 мс до 5 с, но не возвращает ошибок. За секунды весь сервис — включая маршруты, которые не трогают платежи — перестаёт отвечать. Почему медленный случай хуже полного отказа и какова роль breaker'а?"
  choices={[
    { label: "Каждый вызывающий блокируется на 5 с, удерживая рабочий поток и пулированное соединение, поэтому общий пул заполняется и неродственные маршруты голодают; мёртвая зависимость отказала бы за 1 мс и освободила поток. Breaker срабатывает после роста failure rate и быстро отказывает последующим вызовам, превращая неограниченный сбой в ограниченный.", correct: true },
    { label: "Медленные ответы повреждают состояние запроса, и breaker откатывает их.", misconception: "Ничего не повреждается — вред в занятости ресурсов, каждый вызывающий пришпилен к медленному вызову. Breaker освобождает ресурсы через fast-fail; он не откатывает данные." },
    { label: "Замедление затрагивает только маршрут платежей, но breaker случайно расширяет его.", misconception: "Припаркованные потоки берутся из общего пула, поэтому неродственные маршруты тоже голодают. Breaker сужает blast radius, а не расширяет его." },
    { label: "Медленная зависимость на самом деле безопаснее мёртвой, потому что вызов в итоге успешен.", misconception: "Медленный успех стоит всего, что стоил бы реальный успех, плюс ожидание; он пришпиливает ресурсы на весь timeout. Медленное — опасное состояние именно потому, что вызывающие продолжают ждать." },
  ]}
/>

<Quiz
  id="06-circuit-breakers-choice-2"
  lessonSlug="06-circuit-breakers"
  lang="ru"
  question="Breaker настроен с cooldown (resilience4j waitDurationInOpenState) в 1 с перед зависимостью, которой нужно около 20 с на перезапуск после падения. Что идёт не так?"
  choices={[
    { label: "Breaker зондирует снова каждую секунду — задолго до того, как зависимость восстановилась — поэтому half-open проба падает, и он снова открывается, осциллируя и выпуская очереди обречённых проб в сервис, которому нужно тихое время на возврат.", correct: true },
    { label: "Ничего — короткий cooldown всегда восстанавливается быстрее.", misconception: "Cooldown короче реального времени восстановления зависимости гарантирует, что проба попадёт в всё ещё сломанный сервис; breaker флапает вместо восстановления. Cooldown должен отслеживать реальное время восстановления." },
    { label: "Breaker никогда не может покинуть состояние open с cooldown в 1 с.", misconception: "Он покидает open каждую секунду, чтобы зондировать — в этом и проблема. Он снова входит в open, потому что проба падает, а не потому, что застрял." },
    { label: "Cooldown в 1 с полностью отключает состояние half-open.", misconception: "Cooldown — это лишь то, как долго длится open перед half-open; короткий всё равно идёт в half-open, просто слишком рано." },
  ]}
/>

<Quiz
  id="06-circuit-breakers-choice-3"
  lessonSlug="06-circuit-breakers"
  lang="ru"
  question="Низконагруженный admin-эндпоинт видит около 10 запросов/минуту. Один транзиентный timeout в 3 часа ночи срабатывает его breaker на весь cooldown, быстро отказывая каждому последующему запросу из-за единственного блипа. Какая настройка — правильный фикс, и почему не просто поднять failure-rate threshold?"
  choices={[
    { label: "Минимальный порог объёма (Hystrix requestVolumeThreshold, resilience4j minimumNumberOfCalls), чтобы breaker оставался закрытым, пока не увидит достаточно вызовов, чтобы rate стал статистически значимым. Поднятие rate threshold не помогает: 1 отказ из 1 — это 100%, что бьёт любой порог ниже 100%.", correct: true },
    { label: "Более короткий cooldown, чтобы ложное срабатывание прошло быстрее.", misconception: "Более короткий cooldown лишь снижает урон ложного срабатывания; он не останавливает срабатывание на единственном вызове. Порог объёма — это то, что предотвращает срабатывание." },
    { label: "Отключить детекцию медленных вызовов на этом эндпоинте.", misconception: "Детекция медленных вызовов тут ни при чём — ложное срабатывание пришло из failure rate, посчитанного на слишком малом числе вызовов, а не из медленного вызова." },
    { label: "Переключиться с count-based на time-based окно.", misconception: "Тип окна меняет определение «недавнего», но единственный отказ всё равно даёт 100% rate внутри любого окна. Только минимальный порог объёма защищает от срабатывания на единственном вызове." },
  ]}
/>

<Quiz
  id="06-circuit-breakers-choice-4"
  lessonSlug="06-circuit-breakers"
  lang="ru"
  question="Три downstream'а — платежи, рекомендации, поиск — делят один пул из 50 потоков. Рекомендации (наименее важные) замедляются до 5 с, и весь сервис падает, хотя их breaker в итоге срабатывает. Что добавляет bulkhead, чего breaker сам по себе не может?"
  choices={[
    { label: "Потолок конкурентности на зависимость, чтобы медленный downstream мог пришпилить максимум свою долю, пока платежи и поиск сохраняют свои потоки. Breaker лишь ограничивает, как долго ты зовёшь больную зависимость; bulkhead ограничивает, сколько общей ёмкости одна зависимость вообще может удерживать — и действует до того, как сработает реактивный breaker.", correct: true },
    { label: "Более быстрое failure-rate окно, чтобы breaker сработал до осушения пула.", misconception: "Более быстрое окно срабатывает раньше, но пул всё равно может осушиться до его реакции; только разбиение бюджета сдерживает сбой. Bulkhead — это недостающая изоляция, а не более быстрая реакция." },
    { label: "Автоматические ретраи для медленной зависимости, чтобы вызовы прошли.", misconception: "Ретраи добавляют нагрузку и так больной зависимости; они не изолируют. Bulkhead разбивает бюджет конкурентности на зависимость." },
    { label: "Ничего нового — breaker на каждой зависимости уже их изолирует.", misconception: "Breaker'ы на зависимость всё равно черпают из одного общего пула потоков, поэтому медленная зависимость осушает общий бюджет до реакции её breaker'а. Изоляция требует потолка конкурентности на зависимость, и это bulkhead." },
  ]}
/>

<Quiz
  id="06-circuit-breakers-choice-5"
  lessonSlug="06-circuit-breakers"
  lang="ru"
  question="Команда оборачивает шаткий сервис рекомендаций в breaker и выкатывает. В следующий инцидент breaker не срабатывает, а главная страница висит — вызовы рекомендаций не ошибаются, они занимают по 30 с каждый. Чего не хватает и какой второй кусок ещё нужен, когда breaker сработает?"
  choices={[
    { label: "Timeout — без него зависание не успех и не отказ, поэтому невидимо для breaker'а, который считает отказы. Также нужен fallback: когда breaker открыт, вызовы отвергаются мгновенно, но вызывающий должен вернуть что-то полезное (убрать карусель, сохранить страницу), а не жёсткий 500.", correct: true },
    { label: "Более высокий failure-rate threshold, чтобы медленные вызовы регистрировались.", misconception: "Порог не помогает, когда не считается ни одного отказа — зависания никогда не регистрируются как отказы без timeout. Timeout — это то, что превращает зависание в считаемый отказ." },
    { label: "Больший пул потоков, чтобы зависания не голодили потоки.", misconception: "Больший пул откладывает голодание, но breaker всё равно не считает зависания. Timeout — это то, что заставляет зависание выглядеть как отказ." },
    { label: "Более агрессивные ретраи на вызове рекомендаций.", misconception: "Ретрай 30-секундного зависания умножает урон. Breaker'у нужен timeout, чтобы ограничить и посчитать каждый вызов, плюс fallback на то, что вернуть, когда открыт." },
  ]}
/>

<Quiz
  id="06-circuit-breakers-choice-6"
  lessonSlug="06-circuit-breakers"
  lang="ru"
  question="Сервис A зовёт B зовёт C, каждый ретраит до 4 раз при отказе, на флоте из пятидесяти инстансов. C коротко вздрагивает. Дрожь превращается в устойчивый самонаведённый сбой. Каков доминирующий механизм и какой фикс его ограничивает?"
  choices={[
    { label: "Retry amplification: ретраи умножаются вниз по цепочке, поэтому один запрос к A становится 4×4×4 = 64 вызовами в C — retry storm. Держать breaker над ретраями короткозамыкает весь стек, как только он открывается; сочетай это с ретраем на одном слое, абсолютным бюджетом ретраев на процесс и exponential backoff с jitter.", correct: true },
    { label: "Breaker каждого инстанса независимо повреждает данные C.", misconception: "Breaker'ы не повреждают данные, а вред тут — мультипликативная нагрузка, а не корректность. 64x retry amplification — вот механизм." },
    { label: "Фикс — централизовать состояние всех пятидесяти breaker'ов в Redis, чтобы у них был один вердикт.", misconception: "Общее состояние покупает общий взгляд ценой сетевого вызова на горячем пути и новой зависимости; оно не адресует retry storm под breaker'ом. Storm ограничивается ограничением ретраев и держанием breaker'а над ними." },
    { label: "Один exponential backoff полностью предотвращает storm.", misconception: "Backoff разносит попытки одного клиента, но клиенты, упавшие вместе, ре-синхронизируются в волны; jitter — это то, что разрывает корреляцию, а breaker над ретраями — то, что ограничивает множитель." },
  ]}
/>

<Recap lang="ru">
Сквозная линия юнита — одна цепочка решений: медленная зависимость голодит общий пул, поэтому breaker быстро отказывает ей (closed считает отказы, open отвергает мгновенно, half-open зондирует), срабатывая на failure rate по скользящему окну с минимальным порогом объёма, причём медленное считается отказом. Bulkhead изолирует бюджет на зависимость, чтобы один больной downstream не осушил весь пул до срабатывания реактивного breaker'а; timeout превращает зависание в считаемый отказ, а fallback решает, что вернуть, когда открыт; на масштабе флота реальная опасность — retry amplification (4×4×4 = 64), укрощаемая держанием breaker'а над ретраями, ограничением абсолютного rate ретраев и jitter'ом и ретраев, и half-open проб. У каждого пер-инстансового защитного устройства должна быть история на уровне флота.
</Recap>
