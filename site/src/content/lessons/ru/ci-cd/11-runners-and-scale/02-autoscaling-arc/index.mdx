---
concepts:
- ci-cd-gha-arc
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: senior
order: 2
prereqs: []
slug: "02-autoscaling-arc"
sources:
- https://docs.github.com/en/actions/hosting-your-own-runners/managing-self-hosted-runners-with-actions-runner-controller/about-actions-runner-controller
- https://docs.github.com/en/actions/hosting-your-own-runners/managing-self-hosted-runners-with-actions-runner-controller/deploying-runner-scale-sets-with-actions-runner-controller
- https://docs.github.com/en/actions/hosting-your-own-runners/about-self-hosted-runners
spiral: []
status: ready
title: "Автоскейлинг через Actions Runner Controller: эфемерные поды, масштабируемые по очереди"
summary: "Actions Runner Controller (ARC) гоняет self-hosted раннеры как эфемерные поды Kubernetes. Runner scale set масштабирует поды до maxRunners, каждый обслуживает один джоб, давая hosted-подобную изоляцию на кластере с ёмкостью, что следует за очередью, а не за фиксированным пулом."
track: "ci-cd"
unit: "11-runners-and-scale"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Пул был восемь статичных self-hosted раннеров на EC2, всегда включённых, и в 9 утра в понедельник он стал бутылочным горлышком: сорок инженеров запушили перед стендапом, очередь выросла до 60 джобов, а медианное время ожидания достигло одиннадцати минут для тест-сьюта, который бежал четыре. Раннеры никогда не простаивали и никогда не хватало. Первый инстинкт платформенной команды был добавить статичной ёмкости — но восемь машин, тарифицируемых 24/7, уже жгли деньги по ночам, когда очередь пуста, и удвоение удвоило бы простаивающие потери, чтобы починить двухчасовой в день всплеск. Вместо этого они перешли на Actions Runner Controller: раннеры стали эфемерными подами Kubernetes, масштабировавшимися от нуля до сорока, когда очередь наполнялась, и обратно до нуля, когда она опустошалась. Время ожидания в понедельник упало до менее минуты, а ночной счёт — до нуля, потому что ни один под не бежал, когда не было джобов в очереди. Фиксированный пул платил за пиковую ёмкость круглосуточно, чтобы обслужить всплеск длиной два часа.
</Hook>

<Crux>ARC превращает раннеры в эфемерные поды, масштабируемые по очереди: ёмкость следует за спросом, а не за фиксированным пулом.</Crux>

<Explanation>

## Что такое ARC

Actions Runner Controller — это оператор Kubernetes, управляющий self-hosted раннерами как подами. Ты ставишь контроллер и определяешь **runner scale set** — Helm-релиз, привязанный к репозиторию, организации или enterprise регистрационным токеном. Слушатель scale set держит длинный поллинг к сервису Actions GitHub; когда джобы назначаются runner-группе этого scale set, слушатель узнаёт, сколько их в очереди, и контроллер создаёт столько же **эфемерных раннер-подов**. Каждый под регистрируется, гоняет **ровно один джоб**, затем завершается и заменяется. Нет персистентного раннера, накапливающего состояние между джобами — та же изоляция «один джоб — один раннер», что делает hosted-раннеры безопасными, теперь на твоём кластере.

Масштабирование ограничено двумя значениями, которые ты задаёшь: **minRunners** (тёплый пол простаивающих подов, поглощающий задержку холодного старта планирования пода и pull образа) и **maxRunners** (потолок, ограничивающий и радиус поражения, и облачный счёт). Между ними число подов следует за очередью: джобы в очереди гонят поды вверх к maxRunners, опустошение очереди даёт подам завершиться и не быть заменёнными, откатываясь к minRunners (который может быть нулём — scale-to-zero при простое).

<Quiz
  id="02-autoscaling-arc-quiz-1"
  lessonSlug="02-autoscaling-arc"
  lang="ru"
  question="В runner scale set ARC сколько джобов обрабатывает один эфемерный раннер-под, прежде чем будет заменён, и почему это важно?"
  choices={[
    { label: "Ровно один — под гоняет единственный джоб, затем завершается, давая каждому джобу чистое изолированное окружение без перенесённого состояния", correct: true },
    { label: "Столько, сколько влезет в время жизни пода — поды переиспользуются между джобами, чтобы амортизировать стоимость старта", correct: false, misconception: "Поды ARC эфемерны по дизайну: один джоб на под, затем завершение. Переиспользование между джобами вернуло бы утечку состояния персистентного раннера, ради предотвращения которой эфемерные раннеры и существуют." },
    { label: "Зависит от minRunners — тёплые поды обрабатывают несколько джобов, а холодные один", correct: false, misconception: "minRunners лишь задаёт, сколько простаивающих подов держать тёплыми для снижения задержки холодного старта; он не меняет правило один-джоб-на-под. Каждый под, тёплый или свежеспланированный, гоняет единственный джоб и заменяется." },
    { label: "До maxRunners джобов на под, прежде чем потолок заставит создать новый", correct: false, misconception: "maxRunners — это ограничение на параллельные поды, а не на джобы на под. Каждый под всё равно гоняет ровно один джоб; maxRunners лишь ограничивает, сколько таких подов существует одновременно." },
  ]}
/>

## Холодный старт, тёплый пол и компромисс ожидания в очереди

Цена эфемерности — **холодный старт**: свежесозданный под должен быть запланирован на узел (которому самому может понадобиться cluster autoscaler, чтобы добавить узел — десятки секунд до минут), сделать pull образа раннера, зарегистрироваться в GitHub и только потом подхватить джоб. Для четырёхминутного тест-сьюта 90-секундный холодный старт — это 38% накладных на первый джоб всплеска. `minRunners` — это рычаг: держи несколько подов тёплыми и простаивающими, чтобы фронт всплеска попал на готовый раннер, пока контроллер масштабирует остальные за ним. Компромисс прямой — тёплые поды стоят денег, пока простаивают, так что `minRunners` — это ручка между **задержкой ожидания в очереди** (ставь низко, плати меньше, жди больше в начале всплеска) и **отзывчивостью** (ставь выше, плати за простаивающее тепло, стартуй быстрее). Размеряй её под реальную форму твоего всплеска, а не под пик.

<Inset kind="connect" lang="ru">
ARC накладывается на cluster autoscaler, и два холодных старта складываются. Когда очередь всплёскивает выше, чем вмещают текущие узлы, ARC создаёт ожидающие поды, которые ни один узел не может запланировать; cluster autoscaler Kubernetes видит непланируемые поды и провижинит новые узлы. Так что холодный всплеск может платить *две* задержки: провижининг узла, затем старт пода. Команды, которым нужна быстрая реакция на всплеск, держат и тёплый пол `minRunners`, и небольшой запас свободной ёмкости узлов, принимая устойчивую стоимость простоя, чтобы избежать сложенной задержки холодного старта — тот же компромисс фиксированной стоимости против задержки, что у прежнего статичного пула, теперь настраиваемый двумя ручками вместо одной.
</Inset>

## Сайзинг maxRunners и образа раннера

`maxRunners` — твой потолок безопасности. Задай его из двух ограничений: твоего **лимита параллелизма GitHub Actions** (плановое ограничение на параллельные джобы — превышение просто ставит в очередь, так что maxRunners выше него тратит узлы впустую) и твоей **ёмкости кластера и бюджета** (каждый под потребляет CPU/memory requests; maxRunners, умноженный на per-pod request, должен влезть в твои узлы, а неограниченное масштабирование может нагнать облачный счёт или заголодить другие ворклоады на общем кластере). Образ пода тоже важен: лёгкий кастомный образ раннера с предзапечённым тулчейном резко срезает per-pod холодный старт против pull жирного образа и установки инструментов на старте джоба — та же логика, что у хорошо собранного CI base-образа, применённая к самому раннер-поду.

<Quiz
  id="02-autoscaling-arc-quiz-2"
  lessonSlug="02-autoscaling-arc"
  lang="ru"
  question="Команда ставит minRunners: 0 для минимизации стоимости и сообщает, что первый джоб после периода простоя ждёт около двух минут, прежде чем вообще стартует. Какова наиболее вероятная причина и прямой фикс?"
  choices={[
    { label: "Холодный старт: без тёплых подов первый джоб ждёт планирования пода (и возможно нового узла) и pull образа; подними minRunners выше 0, чтобы держать тёплый пол", correct: true },
    { label: "Лимит параллелизма GitHub Actions троттлит джоб; понизь maxRunners, чтобы остаться под ним", correct: false, misconception: "Упор в лимит параллелизма ставит джобы в очередь, когда много бежит разом — он не добавляет фиксированную двухминутную задержку первому джобу после простоя. Сигнатура здесь (простой, затем медленный первый джоб) — это холодный старт, чинимый тёплым полом minRunners." },
    { label: "Раннер-поды переиспользуются, и очистка предыдущего джоба блокирует; отключи эфемерный режим", correct: false, misconception: "Поды ARC эфемерны и не переиспользуются, так что нет очистки предыдущего джоба, на которой можно заблокироваться. Отключение эфемерного режима вернуло бы риск утечки состояния и это неверный фикс; задержка — холодный старт пода/узла." },
    { label: "Слушатель потерял длинный поллинг к GitHub; перезапусти контроллер", correct: false, misconception: "Сломанный слушатель остановил бы масштабирование полностью, а не добавил бы стабильный двухминутный старт джобам, которые всё же в итоге бегут. Паттерн соответствует холодному старту подов, масштабируемых от нуля, адресуемому через minRunners." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Слушатель scale set читает очередь джобов из GitHub, и контроллер масштабирует эфемерные раннер-поды между тёплым полом и твёрдым потолком"
  hue="var(--d-infra)"
  perRow={3}
  nodes={[
    { id: "github", label: "Очередь Actions", sub: "джобы назначены" },
    { id: "listener", label: "Слушатель", sub: "длинный поллинг" },
    { id: "controller", label: "Контроллер", sub: "сверяет поды" },
    { id: "pods", label: "Эфемерные поды", sub: "1 джоб каждый" },
    { id: "floor", label: "minRunners", sub: "тёплый пол" },
    { id: "ceiling", label: "maxRunners", sub: "твёрдый потолок" },
  ]}
  edges={[
    { from: "github", to: "listener", label: "число в очереди" },
    { from: "listener", to: "controller", label: "желаемое" },
    { from: "controller", to: "pods", label: "создать / снести" },
    { from: "floor", to: "pods", label: "держать тёплыми" },
    { from: "ceiling", to: "pods", label: "ограничить рост" },
  ]}
  caption="Число подов следует за очередью между minRunners и maxRunners; каждый под гоняет один джоб, затем умирает. Тёплый пол меняет стоимость простоя на меньшую задержку первого джоба; потолок ограничивает и радиус поражения, и облачные траты, и должен сидеть под лимитом параллелизма GitHub и ёмкостью кластера."
/>

<KeyTakeaway>Actions Runner Controller гоняет self-hosted раннеры как эфемерные поды Kubernetes, ведомые runner scale set: слушатель длинно-поллит GitHub на джобы в очереди, и контроллер масштабирует поды между minRunners (тёплый пол, покупающий меньшую задержку первого джоба ценой стоимости простоя) и maxRunners (потолок, ограничивающий радиус поражения и траты). Каждый под гоняет ровно один джоб, затем завершается, давая hosted-подобную изоляцию на твоём кластере, с ёмкостью, что следует за очередью, а не за фиксированным всегда-включённым пулом — ценой холодного старта, который ты настраиваешь тёплым полом, лёгким образом раннера и запасом ёмкости узлов.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-autoscaling-arc-retrieval"
  lang="ru"
  questions={[
    { q: "Опиши цикл масштабирования ARC и роли minRunners и maxRunners.", a: "Слушатель runner scale set держит длинный поллинг к сервису Actions GitHub и узнаёт, сколько джобов в очереди для его runner-группы. Контроллер ARC сверяет желаемое число подов: он создаёт эфемерные раннер-поды, каждый из которых регистрируется, гоняет ровно один джоб, затем завершается без переиспользования. minRunners — тёплый пол простаивающих подов, держимых готовыми, чтобы фронт всплеска попал на запланированный раннер вместо оплаты задержки холодного старта; он может быть нулём для scale-to-zero при простое. maxRunners — твёрдый потолок, ограничивающий и радиус поражения, и облачные траты, и должен ставиться под лимитом параллелизма GitHub Actions (выше него только очередь) и в пределах ёмкости кластера. Между ними число подов следует за очередью — растёт по мере назначения джобов, падает по мере опустошения." },
    { q: "Почему scale-to-zero (minRunners: 0) вводит задержку первого джоба, и что её усугубляет?", a: "Без тёплых подов первый джоб всплеска должен ждать, пока под будет создан и запланирован на узел, образ раннера спуллен, а регистрация в GitHub завершена, прежде чем джоб вообще стартует — холодный старт от десятков секунд до минуты и более. Он усугубляется cluster autoscaler'ом: если ни на одном существующем узле нет места, ожидающие поды ARC непланируемы, cluster autoscaler Kubernetes должен сперва запровижинить новый узел, и только потом под может стартовать — две сложенные задержки, провижининг узла плюс старт пода. Митигации — ненулевой тёплый пол minRunners, лёгкий предзапечённый образ раннера для укорочения pull и небольшой запас свободной ёмкости узлов, чтобы поды планировались без ожидания нового узла. Каждая митигация меняет устойчивую стоимость простоя на более быструю реакцию на всплеск." },
  ]}
/>

<Recap lang="ru">
Actions Runner Controller — это оператор Kubernetes, заменяющий фиксированный пул всегда-включённых self-hosted раннеров эфемерными подами, масштабируемыми по очереди джобов. Ты определяешь runner scale set, привязанный к репозиторию, организации или enterprise; его слушатель длинно-поллит сервис Actions GitHub на джобы в очереди, и контроллер сверяет число подов. Каждый под регистрируется, гоняет ровно один джоб и завершается — та же изоляция «один джоб — один раннер», что делает hosted-раннеры безопасными, привнесённая на твой кластер. Масштабирование ограничено minRunners, тёплым полом простаивающих подов, что снижает задержку первого джоба ценой оплаты простаивающего тепла, и maxRunners, твёрдым потолком, ограничивающим радиус поражения и облачные траты. Цена эфемерности — холодный старт: свежий под должен быть запланирован, сделать pull образа и зарегистрироваться перед запуском, а когда cluster autoscaler должен сперва добавить узел, две задержки складываются. Ты настраиваешь это ненулевым тёплым полом, лёгким предзапечённым образом раннера и небольшим запасом ёмкости узлов — каждое меняет устойчивую стоимость простоя на более быстрые всплески. Размеряй maxRunners под лимитом параллелизма GitHub Actions (выше него просто очередь) и в пределах ёмкости кластера и бюджета, ведь каждый под потребляет CPU и memory requests, а неограниченное масштабирование может заголодить соседей или нагнать счёт. Результат — ёмкость, что следует за спросом — ноль подов, когда очередь пуста, масштабирование до потолка, когда она наполняется — вместо оплаты пиковой ёмкости круглосуточно ради обслуживания всплеска длиной пару часов.
</Recap>
