---
concepts:
- ci-cd-gha-cache-cost
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 3
prereqs: []
slug: "03-cache-and-cost-strategies"
sources:
- https://docs.github.com/en/actions/using-workflows/caching-dependencies-to-speed-up-workflows
- https://docs.github.com/en/billing/managing-billing-for-github-actions/about-billing-for-github-actions
- https://docs.docker.com/build/cache/backends/gha/
spiral: []
status: ready
title: "Стратегия кэша и стоимости: иерархия restore-keys, стена вытеснения 10 ГБ и Docker layer cache"
summary: "Кэш GitHub Actions даёт репозиторию хранилище 10 ГБ с LRU-вытеснением. Точный key плюс широкие restore-keys превращают холодный кэш в тёплый; Docker layer caching через бэкенд gha кэширует слои сборки, но раздутый кэш, вытесняющий сам себя, может стоить больше отсутствия кэша."
track: "ci-cd"
unit: "11-runners-and-scale"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Команда добавила кэширование зависимостей для ускорения CI, и неделю это работало — установки по 90 секунд упали до 8. Потом hit rate кэша тихо рухнул почти до нуля, и никто не заметил, пока счёт за минуты раннеров не пополз обратно вверх. Причиной были потолок кэша 10 ГБ на репозиторий и LRU-вытеснение (вытеснение по принципу «давно не использовалось»): кто-то начал кэшировать весь `node_modules` *и* Docker build cache *и* кэш бинарника Cypress, каждый с настолько свободным ключом, что каждая ветка писала свежую запись на 2 ГБ. За день кэши репозитория проскочили 10 ГБ, и LRU начал вытеснять кэш зависимостей — с самым высоким hit rate — чтобы освободить место под per-branch Docker-кэши, каждый из которых использовался единожды. Команда платила стоимость записи кэша без выгоды чтения. Фиксом было не больше кэша, а *меньше*: убрать per-branch Docker-кэш до единственного скоупа `main`, затянуть ключи и дать высокоценному кэшу зависимостей пережить вытеснение.
</Hook>

<Crux>Кэширование окупается, только когда тёплый путь попадает: точный key, широкие restore-keys и пребывание под стеной LRU 10 ГБ.</Crux>

<Explanation>

## Ключ и лестница restore-keys

Когда добавляешь кэширование в CI, ты делаешь ставку: «это будет читаться чаще, чем стоит писать». Структура key и restore-keys — это то, что решает, окупится ставка или будет тихо сливать деньги каждый запуск.

Cache-action восстанавливает по первичному `key` и списку фолбэков `restore-keys`:

```yaml
- uses: actions/cache@v4
  with:
    path: ~/.npm
    key: npm-${{ runner.os }}-${{ hashFiles('package-lock.json') }}
    restore-keys: |
      npm-${{ runner.os }}-
```

`key` — это **точное** совпадение: хэш lockfile означает, что ключ меняется, только когда меняются зависимости, так что неизменный lockfile получает идеальное попадание, а изменённый — чистый промах (без устаревших зависимостей). На промахе `restore-keys` пробуются как **префиксные** совпадения, от самого свежего: `npm-Linux-` восстанавливает свежайший кэш от любого прошлого lockfile, так что бамп зависимости в одну строку всё равно тёпло стартует от вчерашнего `node_modules` и доустанавливает лишь дельту. Паттерн — **специфичный key, широкий фолбэк** — точный для корректности, префиксы для тепла. Кэш пишет новую запись только на промахе ключа; если точный ключ попал, новый кэш не сохраняется.

<Quiz
  id="03-cache-and-cost-strategies-quiz-1"
  lessonSlug="03-cache-and-cost-strategies"
  lang="ru"
  question="Workflow ключует кэш зависимостей на hashFiles('package-lock.json') с restore-key префиксом npm-Linux-. PR добавляет одну новую зависимость, меняя lockfile. Что происходит на этом запуске?"
  choices={[
    { label: "Точный ключ промахивается, но префикс npm-Linux- восстанавливает свежайший прошлый кэш, так что установка тянет лишь дельту, и сохраняется новый кэш с точным ключом", correct: true },
    { label: "Точный ключ промахивается, фолбэк не применяется, и джоб делает полную холодную установку с нуля", correct: false, misconception: "Префикс restore-keys — это именно фолбэк для этого случая: изменённый lockfile промахивается мимо точного ключа, но npm-Linux- матчит свежайший прошлый кэш, тёпло стартуя установку вместо полностью холодной." },
    { label: "Точный ключ попадает, потому что hashFiles игнорирует добавленные строки", correct: false, misconception: "hashFiles хэширует содержимое файла, так что добавление зависимости меняет lockfile и поэтому ключ — это намеренный промах, что верно, ведь набор зависимостей реально изменился." },
    { label: "Префиксное совпадение восстанавливает устаревший кэш, и новая зависимость тихо отсутствует", correct: false, misconception: "Восстановленный кэш тёпло стартует директорию, но шаг установки всё равно бежит и сверяется с изменённым lockfile, тяня новую зависимость. Префикс даёт фору, а не устаревшее финальное состояние." },
  ]}
/>

## Стена 10 ГБ и LRU-вытеснение

Каждый репозиторий получает **общий бюджет кэша 10 ГБ**. Когда записи толкают его за 10 ГБ, GitHub вытесняет записи по **least-recently-used** — а кэши также вытесняются, если **не тронуты 7 дней**. Два следствия гонят реальную стоимость:

- **Branch scope**: кэш, созданный на ветке, читается этой веткой и дочерними ветками; **кэши дефолтной ветки читаются всеми ветками**. Так что кэширование на `main` засевает каждый PR, тогда как кэширование на каждой feature-ветке плодит записи с низким переиспользованием, лишь жрущие бюджет 10 ГБ.
- **Вытеснение молчаливо и слепо к ценности**: LRU не знает, что у твоего кэша зависимостей hit rate 95%, а у per-branch Docker-кэша 1%. Если низкоценные кэши пишутся свежее, они выталкивают высокоценный. Кэширование *большего числа вещей* может сделать CI *медленнее*, вытеснив кэш, который был важен.

Дисциплина: кэшируй немногие высокоценные, часто-переиспользуемые вещи на дефолтной ветке с затянутыми ключами и сопротивляйся желанию кэшировать всё. Кэш, что пишется каждый запуск, но редко читается, — чистые накладные: ты платишь время аплоада и давление вытеснения без выгоды восстановления.

<Inset kind="why" lang="ru">
Почему переизбыток кэша активно вредит, а не просто нейтрален? Три складывающиеся стоимости. Первая: каждое сохранение кэша аплоадит артефакт, добавляя реальное время джобу, даже когда он никогда не восстанавливается. Вторая: каждая запись конкурирует за фиксированные 10 ГБ, так что низкоценная запись может вытеснить высокоценную, превращая будущее попадание в промах. Третья: вызванный ею промах — дорогой — полная холодная установка или пересборка — так что один потраченный впустую per-branch кэш на 2 ГБ может превратить восстановление зависимостей с 95% попаданий в 90-секундную холодную установку на следующем запуске *другой* ветки. Бюджет кэша — общий, дефицитный, LRU-управляемый ресурс; отношение к нему как к бесплатному ведёт прямо к более медленному и дорогому пайплайну.
</Inset>

## Docker layer cache: мощно и легко злоупотребить

Для сборок контейнеров BuildKit может использовать кэш GitHub Actions как бэкенд layer-кэша (`cache-from`/`cache-to: type=gha`). Сделанное правильно, это пропускает пересборку неизменных слоёв — многоминутная сборка образа падает до секунд, когда изменился лишь слой приложения. Две ловушки: `mode=max` кэширует *все* промежуточные слои (отличный hit rate, но крупно — легко несколько ГБ, что съедает бюджет 10 ГБ и триггерит вытеснение), тогда как `mode=min` кэширует только финальные слои (меньше, ниже hit rate). И упорядочивание Dockerfile так, чтобы зависимости устанавливались *до* копирования кода, — вот что вообще заставляет кэш попадать; скопируй код первым, и каждая сборка инвалидирует слой зависимостей. Layer-кэширование — это рычаг, но жирный `mode=max` кэш, вытесняющий твой кэш зависимостей, или плохо упорядоченный Dockerfile, который никогда не попадает, стоит больше, чем холодная сборка.

<Quiz
  id="03-cache-and-cost-strategies-quiz-2"
  lessonSlug="03-cache-and-cost-strategies"
  lang="ru"
  question="CI репозитория стал медленнее после того, как команда добавила Docker layer cache с type=gha,mode=max на каждой feature-ветке. Шаги установки зависимостей, что раньше попадали в кэш, теперь часто промахиваются. Какой наиболее вероятный механизм?"
  choices={[
    { label: "Крупные per-branch mode=max Docker-кэши заполнили бюджет 10 ГБ, и LRU вытеснил кэш зависимостей с высоким hit rate, чтобы освободить место", correct: true },
    { label: "mode=max отключает все остальные кэши в репозитории по дизайну", correct: false, misconception: "mode=max лишь управляет тем, сколько Docker-слоёв кэшируется; он не отключает другие кэши. Замедление идёт от крупных Docker-кэшей, потребляющих общие 10 ГБ и выталкивающих кэш зависимостей через LRU." },
    { label: "Docker layer-кэши и кэши зависимостей используют отдельные квоты, так что не могут влиять друг на друга", correct: false, misconception: "Они делят тот же бюджет кэша 10 ГБ на репозиторий. Крупный Docker-кэш конкурирует прямо с кэшем зависимостей, а LRU-вытеснение не уважает, что ценнее." },
    { label: "type=gha кэши зашифрованы и медленнее восстанавливаются, так что каждое восстановление теперь дольше", correct: false, misconception: "Симптом — шаги зависимостей промахиваются мимо кэша, а не медленные восстановления. Это указывает на вытеснение из общего бюджета 10 ГБ новыми крупными Docker-кэшами, а не на накладные шифрования восстановления." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Поиск кэша пробует точный ключ, откатывается через префиксы restore-key, и любая запись конкурирует за бюджет 10 ГБ под LRU-вытеснением"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "exact", label: "Точный ключ", sub: "хэш lockfile" },
    { id: "prefix", label: "restore-keys", sub: "префиксный фолбэк" },
    { id: "restore", label: "Тёплое восст.", sub: "доустановка дельты" },
    { id: "save", label: "Сейв на промахе", sub: "аплоад записи" },
    { id: "budget", label: "Бюджет 10 ГБ", sub: "на репозиторий" },
    { id: "evict", label: "LRU-вытеснение", sub: "слепо к ценности" },
  ]}
  edges={[
    { from: "exact", to: "prefix", label: "на промахе" },
    { from: "prefix", to: "restore", label: "свежайшее" },
    { from: "save", to: "budget", label: "потребляет" },
    { from: "budget", to: "evict", label: "выше 10 ГБ" },
    { from: "evict", to: "restore", label: "будущий промах" },
  ]}
  caption="Точный ключ для корректности, префиксы restore-key для тепла. Каждый сейв конкурирует за фиксированные 10 ГБ; LRU-вытеснение слепо к ценности, так что низко-переиспользуемый кэш может вытолкнуть высоко-попадающий и превратить будущее восстановление в дорогой холодный промах."
/>

<KeyTakeaway>Кэширование GitHub Actions окупается, только когда попадает тёплый путь: точный `key` (хэш lockfile) гарантирует корректность, широкие префиксы `restore-keys` дают тёплый старт на почти-промахе, а кэши, записанные на дефолтной ветке, засевают каждый PR. Но хранилище — общие 10 ГБ на репозиторий со слепым к ценности LRU-вытеснением (и 7-дневным истечением по простою), так что кэширование слишком многого — крупные per-branch записи, жирный Docker `mode=max` layer-кэш — может вытеснить тот единственный высоко-попадающий кэш, что был важен, и сделать CI медленнее и дороже, чем кэширование меньшего. Упорядочь Dockerfile так, чтобы слои зависимостей кэшировались до кода, и кэшируй немногие высокоценные вещи, а не всё.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-cache-and-cost-strategies-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни различие key против restore-keys и почему паттерн — специфичный key, широкий фолбэк.", a: "Первичный key — точное совпадение, обычно хэш lockfile, так что он меняется, только когда меняются зависимости — неизменный lockfile получает идеальное попадание, а изменённый чистый промах, никогда не отдавая устаревшие зависимости. restore-keys — префиксные фолбэки, пробуемые от свежайшего на промахе: префикс вроде npm-Linux- восстанавливает свежайший прошлый кэш от любого раннего lockfile, так что бамп зависимости в одну строку тёпло стартует от прошлой установки и тянет лишь дельту вместо полностью холодного. Специфичный ключ даёт корректность (точное совпадение по реальному набору зависимостей); широкий фолбэк даёт тепло (фору, когда точный ключ промахивается). Новая запись кэша сохраняется, только когда точный ключ промахнулся; точное попадание не сохраняет ничего." },
    { q: "Почему добавление большего числа кэшей может сделать CI медленнее, и что управляет хранилищем 10 ГБ?", a: "У каждого репозитория общий бюджет кэша 10 ГБ с least-recently-used вытеснением и 7-дневным истечением по простою, и вытеснение слепо к ценности: оно не знает, у какого кэша высокий hit rate. Так что крупный, низко-переиспользуемый кэш — скажем, per-branch Docker mode=max layer-кэш — записанный свежее, может вытеснить кэш зависимостей с высоким hit rate, чтобы остаться под 10 ГБ, превращая будущее дешёвое восстановление в дорогую холодную установку или пересборку. Переизбыток кэша также стоит время аплоада на каждом сейве, даже когда запись никогда не восстанавливается. Branch scope важен: кэши дефолтной ветки читаются всеми ветками и засевают каждый PR, тогда как per-feature-branch кэши имеют низкое переиспользование и лишь жрут бюджет. Дисциплина — кэшировать немногие высокоценные, часто-переиспользуемые вещи на дефолтной ветке с затянутыми ключами и упорядочивать Dockerfile так, чтобы слои зависимостей кэшировались до кода, а не кэшировать всё." },
  ]}
/>

<Recap lang="ru">
Кэширование GitHub Actions — рычаг, только когда тёплый путь реально попадает, и это держится на трёх вещах: ключе, фолбэках и бюджете. Первичный ключ должен быть точным значением — обычно хэшем lockfile — так чтобы он попадал, когда ничего не изменилось, и чисто промахивался, когда зависимости изменились, никогда не отдавая устаревшее состояние. Список restore-keys даёт префиксные фолбэки, пробуемые от свежайшего, так что изменённый lockfile всё равно тёпло стартует от свежайшего прошлого кэша и доустанавливает лишь дельту: специфичный ключ для корректности, широкие префиксы для тепла, с новой записью, сохраняемой только на промахе точного ключа. Жёсткое ограничение — хранилище: 10 ГБ на репозиторий, вытесняемые по least-recently-used и истекающие после 7 дней простоя, и вытеснение слепо к ценности — оно не отличит твой кэш зависимостей с 95% попаданий от per-branch Docker-кэша, использованного единожды. Вот почему кэширование большего может сделать CI медленнее: крупный низко-переиспользуемый кэш конкурирует за фиксированный бюджет и может вытолкнуть высокоценный, превращая будущее дешёвое восстановление в дорогую холодную установку, поверх стоимости аплоада, что каждый сейв платит даже когда не читается. Branch scope усугубляет это — кэши дефолтной ветки засевают каждый PR, тогда как per-feature-branch записи плохо переиспользуются и лишь жгут бюджет. Docker layer-кэширование через бэкенд type=gha мощно: оно пропускает неизменные слои и роняет многоминутную сборку до секунд, но mode=max кэширует каждый промежуточный слой и может вырасти до нескольких ГБ, что вытеснят твой кэш зависимостей, а Dockerfile, копирующий код до установки зависимостей, не попадает вовсе. Сквозная мысль — сдержанность: кэшируй немногие высокоценные, часто-переиспользуемые артефакты на дефолтной ветке с затянутыми ключами и хорошо упорядоченными Dockerfile, и относись к 10 ГБ как к дефицитному общему ресурсу, которым они и являются. Теперь, когда увидишь, что hit rate кэша рухнул после того, как кто-то «добавил ещё кэширования», знаешь куда смотреть: какая новая запись LRU-вытесняет ту, что реально работала.
</Recap>
