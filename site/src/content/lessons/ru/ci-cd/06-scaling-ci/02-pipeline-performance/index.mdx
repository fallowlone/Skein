---
concepts:
- cache-layers
- parallelism
- fail-fast
- flaky-quarantine
- ci-cost
- job-matrix
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-pipeline-performance
sources:
- https://docs.github.com/en/actions/using-workflows/caching-dependencies-to-speed-up-workflows
- https://docs.github.com/en/actions/writing-workflows/choosing-what-your-workflow-does/control-the-concurrency-of-workflows-and-workflow-runs
- https://docs.github.com/en/billing/managing-billing-for-github-actions/about-billing-for-github-actions
spiral: []
status: ready
summary: Сделай один пайплайн быстрым и дешёвым. Кэшируй зависимости по хэшу lock-файла с фоллбэком restore-keys, гони дешёвые проверки первыми за needs, распараллеливай только там, где это окупается, и держи flaky-тесты в карантине, чтобы параллелизм не покупал простой раннеров.
title: 'Производительность пайплайна: кэш, параллелизм, fail-fast'
track: ci-cd
unit: 06-scaling-ci
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Пул-реквест — в одну строку: переименованная переменная. CI крутится 35 минут — установка, сборка, юнит-тесты, потом полный Playwright e2e по трём браузерам — и *затем* падает. Ошибка — опечатка: `lenght` вместо `length`, ровно то, что typecheck ловит за две секунды. Но typecheck был шагом *внутри* e2e-джобы, после старта браузеров, поэтому самый дешёвый и самый верный провал ждал за самой медленной и самой нестабильной работой пайплайна. Помножь на сорок пушей в день — и команда жжёт часы человеческого ожидания и сотни раннер-минут ради того, что двухсекундная джоба могла сказать первой.
</Hook>

<Crux>Кэшируй зависимости по хэшу lock-файла, гони дешёвые проверки первыми за `needs` и распараллеливай только там, где это окупается.</Crux>

<Explanation>

## Кэш: правильный ключ — это хэш lock-файла

Столкнулся с медленным пайплайном и не знаешь, с чего начать? Ответ почти всегда один: закэшируй детерминированное и поставь быстрые проверки первыми. Вот как устроена каждая из этих частей.

Каждая CI-джоба стартует на чистом раннере, поэтому без кэша она переделывает каждый дорогой детерминированный шаг — прежде всего установку зависимостей — с нуля на каждом запуске. Кэш это чинит, но только если правильный его **ключ**. Ключ — это строка; кэш *попадает*, когда эта точная строка уже существует, иначе *промахивается*, и в конце успешной джобы сохраняется новый кэш. Значит, ключ обязан меняться ровно тогда, когда должно меняться содержимое, и никогда иначе.

Для зависимостей вход, определяющий `node_modules`, — это lock-файл, поэтому хэшируй его через `hashFiles`. Добавь `restore-keys` как упорядоченный список префиксов: на промахе GitHub идёт по ним по порядку и восстанавливает самый свежий кэш, чей ключ начинается с префикса, — *частичное* попадание, так что `npm ci` доустанавливает только дельту, а не все 1200 пакетов по сети.

```yaml
- uses: actions/cache@v4
  with:
    path: ~/.npm
    key: ${{ runner.os }}-node22-${{ hashFiles('**/package-lock.json') }}
    restore-keys: |
      ${{ runner.os }}-node22-
      ${{ runner.os }}-
```

Стоит различать три *слоя* кэша, каждый ключуется так же: **кэш зависимостей** (`~/.npm`, `~/.cache/pip`, кэш Go-модулей — часто автоматом через `cache: npm` у `setup-node`), **кэш сборки** (инкрементальный вывод компилятора, удалённый кэш Turborepo/Nx) и **кэш слоёв Docker** (`type=gha` с buildx). Антипаттерн, убивающий все три, один и тот же: **волатильный ключ**. Запеки в ключ таймстамп или `${{ github.run_id }}` — и он уникален на каждом запуске, поэтому *никогда* не попадает: кэш молча сохраняется и не читается ни разу, а ты вечно платишь полное время установки, веря, что у тебя есть кэш.

## Fail-fast: самое дешёвое и самое вероятное падение — первым

`needs` превращает плоский список джоб в DAG: джоба с `needs: [lint]` не стартует, пока `lint` не пройдёт успешно. Одно это ключевое слово даёт закрыть медленную дорогую работу за дешёвой — так что двухсекундный провал lint обрубает запуск ещё до того, как двадцатиминутный e2e-набор поднимет хоть один браузер. Правило порядка — **самое дешёвое и самое вероятное падение первым**: lint и typecheck бегут за секунды и ловят самые частые ошибки, поэтому они идут в начало DAG, а всё медленное висит на них через `needs`.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Проверка</th>
        <th class="border border-border px-3 py-2 text-left">Типичное время</th>
        <th class="border border-border px-3 py-2 text-left">Порядок</th>
        <th class="border border-border px-3 py-2 text-left">Потрачено на PR с опечаткой</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">lint / typecheck</td>
        <td class="border border-border px-3 py-2">~2–15 с</td>
        <td class="border border-border px-3 py-2">первой (гейт)</td>
        <td class="border border-border px-3 py-2">находит за секунды</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">юнит-тесты</td>
        <td class="border border-border px-3 py-2">~1–3 мин</td>
        <td class="border border-border px-3 py-2"><code>needs: lint</code></td>
        <td class="border border-border px-3 py-2">не бегут — пропущены</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">e2e (3 браузера)</td>
        <td class="border border-border px-3 py-2">~15–25 мин</td>
        <td class="border border-border px-3 py-2"><code>needs: [unit, build]</code></td>
        <td class="border border-border px-3 py-2">не бегут — пропущены</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><b>плоско (без needs)</b></td>
        <td class="border border-border px-3 py-2"><b>~25 мин</b></td>
        <td class="border border-border px-3 py-2"><b>всё разом</b></td>
        <td class="border border-border px-3 py-2"><b>платит полный e2e, потом падает на опечатке</b></td>
      </tr>
    </tbody>
  </table>
</div>

```yaml
jobs:
  lint:                       # ~10 с — дешёвый гейт
    runs-on: ubuntu-latest
    steps: [{ uses: actions/checkout@v4 }, { run: npm run lint && npm run typecheck }]

  unit:
    needs: lint               # не стартует, если lint упал
    runs-on: ubuntu-latest
    steps: [{ uses: actions/checkout@v4 }, { run: npm test }]

  build:
    needs: lint               # параллельно с unit — обоим нужен только lint
    runs-on: ubuntu-latest
    steps: [{ uses: actions/checkout@v4 }, { run: npm run build }]

  e2e:
    needs: [unit, build]      # медленный набор закрыт за всем дешёвым
    strategy:
      fail-fast: true         # один шард упал → отмена остальных
      matrix:
        browser: [chromium, firefox, webkit]
    runs-on: ubuntu-latest
    steps: [{ uses: actions/checkout@v4 }, { run: npx playwright test --project=${{ matrix.browser }} }]
```

<Inset kind="why" lang="ru">
`fail-fast: true` — матричный близнец упорядочивания через `needs`: это значение по умолчанию, и оно отменяет идущие и стоящие в очереди шарды в тот миг, когда один шард падает. На PR-проверке это и нужно — ты уже знаешь, что сборка красная, зачем платить за два других браузера до конца. Переключай на `false` только для ночного или релизного запуска, где нужен *полный* отчёт о падениях по каждой ячейке, а не самый быстрый красный.
</Inset>

## Параллелизм и `matrix`: дроби работу, но fan-out не бесплатен

Когда джобы независимы — `unit` и `build` выше обоим нужен только `lint` — они бегут параллельно автоматически, срезая wall-clock. `matrix` — компактный способ размножить одну джобу по списку (браузеры, версии Node, ОС), порождая по джобе на ячейку. Подвох — **стоимость**. GitHub считает минуты каждой джобы независимо и округляет каждую вверх до целой минуты, так что матрица 3 браузера × 2 ОС — это шесть отдельных джоб, каждая платит свой checkout, установку и восстановление кэша. Параллелизм меняет *оплачиваемые минуты* на *латентность*: пять шардов по минуте финишируют за минуту wall-clock, но в счёте — пять минут. Распараллеливай там, где выигрыш по латентности реален; ограничивай разрастание fan-out через `max-parallel`.

## Стоимость: минуты, размер раннера и простой

Прежде чем брать раннер побольше или раздувать матрицу, спроси себя: эта джоба реально ускорится, или дополнительные ядра просто будут простаивать и увеличивать счёт?

Раннер-минуты — это счёт. Linux — дешёвая база; Windows стоит примерно 2× за минуту, а macOS примерно 10×, так что неосторожная macOS-матрица — кратчайший путь к сюрпризу в инвойсе. Ещё два рычага режут в обе стороны. **Раннер побольше** (больше vCPU) заканчивает CPU-bound джобу быстрее, но стоит пропорционально дороже за минуту — окупается, только если джоба реально параллелится по ядрам; 16-ядерный раннер на однопоточной сборке просто платит 8× ни за что и **простаивает**. И **время восстановления кэша** не бесплатно: вытянуть кэш в несколько сотен МБ по сети может стоить дороже, чем пересборка, которую он экономит, — поэтому кэшируй медленно-производимое (зависимости, скомпилированный вывод), а не всё подряд.

<Quiz
  id="02-pipeline-performance-quiz-1"
  lessonSlug="02-pipeline-performance"
  lang="ru"
  question="Ключ кэша воркфлоу — `build-${{ github.run_id }}`. Сборки по-прежнему медленные, и кэш будто никогда не помогает. Почему?"
  choices={[
    { label: "github.run_id уникален на каждом запуске, поэтому ключ никогда не совпадает дважды — каждый запуск промахивается, сохраняет свежий кэш и ни разу не восстанавливает", correct: true },
    { label: "actions/cache работает только для npm, но не для вывода сборки", misconception: "actions/cache не зависит от содержимого — он сохраняет и восстанавливает любой путь, который ты дашь. Проблема в ключе, а не в том, что кэшируется." },
    { label: "Кэш слишком большой для восстановления, поэтому GitHub его пропускает", misconception: "GitHub не пропускает молча совпавший кэш. Дело в том, что ключ никогда не совпадает: run_id меняется на каждом запуске, поэтому восстанавливать нечего." },
    { label: "Нужны restore-keys; без них кэширование отключено", misconception: "restore-keys лишь добавляют префиксные фоллбэки. Даже с ними ключ с run_id не даёт сохранённого кэша, чей префикс осмысленно переиспользуется — сначала почини волатильный ключ, хэшируя lock-файл." },
  ]}
/>

<TradeoffMatrix
  id="02-pipeline-performance-tradeoff-1"
  lessonSlug="02-pipeline-performance"
  lang="ru"
  prompt="Wall-clock 35-минутного пайплайна забивает однопоточный `npm run build` плюс `npm ci`, переустанавливающий 1200 пакетов каждый запуск. Хочешь наибольший срез за наименьшую добавку оплачиваемой стоимости. Какой ход первым?"
  options={[
    { name: "Кэшировать ~/.npm по хэшу lock-файла (с restore-keys) и переставить lint/typecheck вперёд медленных джоб через needs", summary: "Кэш + дешёвое первым.", correct: true, justification: "Это бьёт по обеим реальным затратам практически с нулём добавочных оплачиваемых минут. Хэш lock-файла превращает переустановку 1200 пакетов в попадание за доли секунды на подавляющем большинстве пушей (промахивается только смена lock-файла), а префиксный фоллбэк restore-keys доустанавливает лишь дельту, когда промах есть. Постановка lint/typecheck первыми за needs значит, что сломанный PR падает за секунды, а не платит полные 35 минут. Тратишь почти ничего сверху и режешь и латентность, и средние минуты." },
    { name: "Перевести весь пайплайн на 16-ядерный раннер побольше", summary: "Коробка крупнее.", justification: "Раннер побольше помогает только работе, что параллелится по ядрам. Узкое место здесь — однопоточная сборка, которая не ускорится на 16 ядрах — ты просто заплатишь в несколько раз больше за минуту коробки, что почти простаивает. И это никак не убирает избыточную переустановку 1200 пакетов. Верный инструмент только для реально CPU-параллельных джоб, не для этой." },
    { name: "Размножить сборку в большую матрицу по многим раннерам", summary: "Массивный fan-out.", justification: "Матричный fan-out режет латентность только у работы, что дробится на независимые шарды; единый артефакт сборки не шардится, а каждая лишняя ячейка заново платит checkout, установку и восстановление кэша, плюс свои округлённые вверх минуты. Ты помножишь стоимость, не срезав время самой сборки. Береги matrix для естественно независимой работы вроде мультибраузерного e2e." },
    { name: "Полностью отключить кэш через --no-cache, чтобы запуски были воспроизводимыми", summary: "Выключить кэш.", justification: "Это задом наперёд — гарантирует медленный путь каждый запуск. Корректно ключёванный кэш по хэшу lock-файла уже детерминирован: он меняется ровно тогда, когда меняются зависимости. Забота о воспроизводимости решается ключом по lock-файлу, а не вечной платой за полную установку." },
  ]}
/>

<DragOrder
  id="02-pipeline-performance-drag-1"
  lessonSlug="02-pipeline-performance"
  lang="ru"
  prompt="Упорядочь эти CI-джобы в fail-fast DAG — самое дешёвое и самое вероятное падение первым, самое медленное закрыто последним:"
  items={[
    { id: "a", label: "lint + typecheck — секунды, ловит самые частые ошибки (гейт)" },
    { id: "b", label: "юнит-тесты — минута-две, needs: lint" },
    { id: "c", label: "сборка — параллельно с unit, needs: lint" },
    { id: "d", label: "e2e-матрица (3 браузера) — 15–25 мин, needs: [unit, build], fail-fast: true" },
    { id: "e", label: "деплой — только на зелёном, needs: e2e" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

<Inset kind="note" lang="ru">
**Карантин flaky-тестов** (повтор из юнита про тестирование): тест, который на том же коде то проходит, то падает, разъедает доверие и, хуже того, заминается авто-ретраями, что могут спрятать *настоящую* регрессию. Не давай известному flaky-тесту гейтить пайплайн. Помечай его, выноси в отдельную неблокирующую джобу (или ставь `test.fixme`/`continue-on-error`) и веди в списке на починку — чтобы зелёная галочка оставалась осмысленной, а реальный провал не был «переретраен».
</Inset>

</Explanation>

<FlowDiagram
  label="Fail-fast CI-DAG: дешёвый гейт lint/typecheck, который на успехе раскрывается в параллельные unit и build, гейтящие медленную e2e-матрицу, которая гейтит деплой"
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "lint", label: "lint+typecheck", sub: "~10 с · гейт" },
    { id: "unit", label: "юнит-тесты", sub: "needs: lint" },
    { id: "build", label: "сборка", sub: "needs: lint" },
    { id: "e2e", label: "e2e-матрица", sub: "fail-fast: true" },
    { id: "deploy", label: "деплой", sub: "needs: e2e" },
  ]}
  edges={[
    { from: "lint", to: "unit", label: "на зелёном" },
    { from: "lint", to: "build", label: "параллельно" },
    { from: "unit", to: "e2e", label: "needs" },
    { from: "build", to: "e2e", label: "needs" },
    { from: "e2e", to: "deploy", label: "на зелёном" },
  ]}
  caption="needs строит DAG: двухсекундный провал lint обрубает запуск до того, как двадцатиминутный e2e поднимет браузер. Самая дешёвая и самая вероятная проверка — первой; самое медленное висит на ней."
/>

<KeyTakeaway>Кэшируй зависимости по хэшу lock-файла; ключ с таймстампом не попадает. Закрывай медленные джобы за дешёвым lint/typecheck через `needs`, размножай через `matrix` лишь где окупается латентность, держи flaky в карантине.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-pipeline-performance-retrieval"
  lang="ru"
  questions={[
    { q: "Почему ключ кэша вроде `build-${{ github.run_id }}` делает кэш бесполезным, и какой ключ плюс фоллбэк верный?", a: "Кэш попадает, только когда строка его ключа уже существует с прошлого запуска. github.run_id уникален на каждом запуске воркфлоу, поэтому ключ никогда не совпадает дважды: каждый запуск промахивается, добросовестно сохраняет совершенно новый кэш в конце и затем ни разу его не восстанавливает — ты платишь полную установку или сборку каждый раз, веря, что кэш помогает. Ключ обязан меняться ровно тогда, когда должно меняться содержимое, и никогда иначе. Для зависимостей вход, определяющий содержимое, — это lock-файл, поэтому верный ключ его хэширует: key: ${{ runner.os }}-node22-${{ hashFiles('**/package-lock.json') }}. Дальше добавь restore-keys как упорядоченный список префиксов — ${{ runner.os }}-node22- затем ${{ runner.os }}- — чтобы при смене lock-файла и промахе точного ключа GitHub восстановил самый свежий кэш, чей ключ начинается с этого префикса: частичное попадание, дающее установщику доставить только дельту, а не все 1200 пакетов по сети." },
    { q: "Объясни, как `needs` и fail-fast режут время обратной связи, и в чём ловушка стоимости при размножении всего в матрицу.", a: "needs превращает плоское множество джоб в DAG: джоба с needs: [lint] не стартует, пока lint не пройдёт, так что можно закрыть медленную дорогую работу за дешёвой. Ставь lint и typecheck первыми — они бегут за секунды и ловят самые частые ошибки — и двухсекундный провал обрубает запуск ещё до того, как двадцатиминутный e2e поднимет браузер. Матричный fail-fast: true (по умолчанию) — близнец идеи: в тот миг, когда один шард падает, он отменяет оставшиеся идущие и стоящие в очереди шарды, так что заведомо красный запуск перестаёт платить за другие браузеры. Ловушка стоимости в том, что параллелизм не бесплатен: GitHub считает минуты каждой джобы независимо и округляет каждую вверх до целой минуты, так что матрица из шести ячеек — шесть отдельных джоб, каждая заново платит checkout, установку и восстановление кэша. Параллелизм меняет оплачиваемые минуты на латентность — пять шардов по минуте финишируют за минуту wall-clock, но в счёте пять минут — поэтому распараллеливай только там, где выигрыш по латентности реален, и ограничивай разрастание через max-parallel." },
  ]}
/>

<Recap lang="ru">
Быстрый и дешёвый пайплайн рождается из трёх ходов, наложенных вместе. Первое: кэшируй дорогие детерминированные шаги по ключу, который есть хэш lock-файла — `hashFiles('**/package-lock.json')` — с упорядоченными префиксами `restore-keys`, чтобы изменённый lock-файл всё равно восстанавливал частичный кэш и доустанавливал только дельту; роковая ошибка — волатильный ключ (таймстамп или `run_id`), уникальный на каждом запуске, поэтому он никогда не попадает, и ты платишь полное время установки, веря, что закэширован. Три слоя кэша — зависимостей, сборки и слоёв Docker — подчиняются одному правилу ключа. Второе: упорядочивай джобы — самое дешёвое и самое вероятное падение первым: `needs` строит DAG, так что двухсекундный провал lint/typecheck обрубает запуск до старта двадцатиминутного e2e, а матричный `fail-fast: true` отменяет остальные шарды, как только один краснеет. Третье: уважай стоимость: параллелизм и `matrix` режут латентность, но каждая джоба платит свои округлённые вверх минуты, раннеры Windows и macOS стоят примерно 2× и 10× Linux, а раннер побольше окупается лишь на реально многоядерной работе — иначе простаивает, платя больше. Держи известные flaky-тесты в неблокирующей джобе, чтобы авто-ретраи не прятали реальную регрессию и зелёная галочка продолжала что-то значить. Теперь, когда смотришь на медленный пайплайн, первые два вопроса: меняется ли ключ при смене lock-файла и стоит ли lint перед e2e?
</Recap>
