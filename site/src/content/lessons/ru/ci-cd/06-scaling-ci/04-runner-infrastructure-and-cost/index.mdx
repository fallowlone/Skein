---
concepts:
- hosted-vs-self-hosted
- ephemeral-runners
- autoscaling-arc
- spot-instances
- queue-time
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 4
prereqs: []
slug: "04-runner-infrastructure-and-cost"
sources:
- https://docs.github.com/en/actions/hosting-your-own-runners/managing-self-hosted-runners
- https://github.com/actions/actions-runner-controller
spiral: []
status: ready
summary: 'Счёт за CI — лишь половина стоимости: вторая — время очереди, помноженное на инженеро-часы. Запускай ephemeral-раннеры, автоскейль под очередь, ставь на spot ради экономии 60-90%, right-size и следи за p95 времени очереди.'
title: 'Инфраструктура раннеров и стоимость CI на масштабе'
track: ci-cd
unit: 06-scaling-ci
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Распоряжение пришло после того, как флапающий сторонний раннер слил токен: «перенести всё на большие hosted-раннеры, для надёжности». Сработало. И заодно сделало две вещи, которые никто не просчитал. Месячный счёт за CI вырос в 10 раз — самая большая машина на каждой задаче, даже на той, что линтит README. А когда финансы в ответ зажали concurrency, чтобы отыграть счёт назад, PR-ы начали стоять в очереди по 20+ минут на пике. Дашборд, на который все смотрели, показывал *длительность* задачи стабильной — выглядело нормально. Чего он не показывал — время очереди: сорок инженеров, каждый ждёт двадцать минут свободного раннера, по несколько раз в день. Счёт за раннеры, из-за которого они воевали, был погрешностью рядом с инженеро-часами, которые они сжигали в очереди. Этот урок — о флоте раннеров под твоими пайплайнами: hosted против self-hosted, ephemeral против persistent, автоскейлинг, spot и кривая стоимости, у которой две оси, а не одна.
</Hook>

<Crux>Стоимость CI = счёт за раннеры + ожидание инженеров. Запускай ephemeral-раннеры, автоскейль под очередь, ставь на spot, следи за p95.</Crux>

<Explanation>

## Hosted против self-hosted: кто владеет машиной

GitHub-hosted раннеры дают тебе чистую ephemeral-VM на каждую задачу с нулевыми операциями на твоей стороне — ты платишь поминутно и никогда ничего не патчишь. Это верный дефолт, пока не кусает одно из трёх: счёт на большом объёме, потолок спецификации (нужны большие машины, GPU или конкретное железо, которого нет в hosted-каталоге) или сетевой доступ (задача должна достучаться до сервисов внутри твоего VPC). Self-hosted раннеры закрывают все три — твои машины, дешевле на объёме, любая спецификация, доступ к VPC, — но ты наследуешь патчинг, скейлинг и **безопасность**.

Эта оговорка про безопасность — не сноска. Self-hosted раннер, привязанный к **публичному** репозиторию, опасен: pull request от незнакомца запускает *его* код на *твоей* инфраструктуре. Держи self-hosted раннеры подальше от публичных репозиториев или закрывай их обязательным одобрением для контрибьюторов-новичков.

```yaml
# Джоба, запиненная на твой self-hosted флот по лейблу.
jobs:
  build:
    runs-on: [self-hosted, linux, x64, ephemeral]  # лейблы выбирают флот
    steps:
      - uses: actions/checkout@v4
      - run: ./ci/build.sh
```

## Ephemeral-раннеры: чистая комната на каждую задачу

Именно эта практика делает self-hosted безопасным. **Ephemeral**-раннер исполняет ровно **одну** задачу, после чего уничтожается и пересоздаётся с нуля. **Persistent**-раннер остаётся поднятым и берёт задачу за задачей — и накапливает между ними состояние: оставшиеся файлы в рабочей директории, отравленный кеш зависимостей, фоновый процесс, порождённый предыдущей (возможно вредоносной) задачей. Этот осадок — канал заражения. Поздняя задача может прочитать оставленные секреты, попасть на подменённый бинарник или унаследовать испорченный кеш — и ничто не воспроизводимо, потому что каждая задача стартует оттуда, где предыдущая оставила машину.

Ephemeral-раннеры закрывают всё это. Каждая задача получает чистую комнату: детерминированное стартовое состояние, ноль межзадачных утечек, а скомпрометированная задача умирает вместе со своим раннером, а не лежит в засаде для следующей. Цена — время запуска на каждую задачу — ты платишь свежей загрузкой каждый раз, — которую ты смягчаешь **тёплыми пулами** (несколько простаивающих раннеров, заранее подготовленных) и быстрыми базовыми образами. Изоляцию нельзя получить никак иначе; время запуска — можно.

```bash
# Зарегистрировать self-hosted раннер, который завершается после одной джобы и не переиспользуется.
./config.sh --url https://github.com/acme/payments \
  --token "$REG_TOKEN" \
  --labels linux,x64 \
  --ephemeral            # <- уничтожить после одной джобы; состояние не выживает
./run.sh
```

<Inset kind="why" lang="ru">
Почему ephemeral-раннеры стоят своей платы за запуск на каждую задачу, когда быстрый persistent-раннер её пропускает? Потому что persistent-раннер несёт состояние между задачами — закешированный файл, оставшийся процесс, отравленную зависимость от предыдущей и, возможно, недоверенной задачи, — так что задачи ни воспроизводимы, ни изолированы, и одна скомпрометированная задача может вмешаться в каждую задачу, что приземлится на эту машину после. Ephemeral даёт чистую комнату на задачу: детерминированное состояние, ноль межзадачных утечек, а скомпрометированная задача умирает вместе со своим раннером, а не сохраняется. Плата за запуск смягчается тёплыми пулами и быстрыми образами; изоляция же попросту недоступна на persistent-флоте за любые деньги. Ты покупаешь свойство безопасности и воспроизводимости, а не производительности.
</Inset>

## Автоскейлинг: следуй за очередью, а не за догадкой

Фиксированный флот из N раннеров имеет два режима отказа, и константой из обоих не вырулить. Слишком мало раннеров — и задачи **встают в очередь**: разработчики ждут, и настоящая цена — это простой инженера, а не простой машины. Слишком много — и ты **платишь за простой** раннеров, что сидят тёплыми без дела. Какое бы N ты ни выбрал, оно неверно в какой-то момент дня, потому что спрос рваный: тихо в 3 ночи и громовой табун, когда все пушат перед стендапом.

Автоскейлинг заставляет N следовать за спросом. **Actions Runner Controller (ARC)** запускает ephemeral-раннеры как поды Kubernetes, которые скейлятся под очередь задач: когда очередь растёт, он поднимает больше подов-раннеров; когда она пустеет, он скейлит **до нуля**, так что простаивающие часы не стоят ничего. Ты получаешь лучшее из обоих — мощность при табуне, ноль счёта, когда его нет, — и, что важно, раннеры всё ещё ephemeral, так что автоскейлинг и изоляция-чистая-комната складываются, а не дерутся.

```yaml
# ARC: автоскейлинговый набор ephemeral-раннеров (скейлится под очередь, вплоть до нуля).
apiVersion: actions.github.com/v1alpha1
kind: AutoscalingRunnerSet
spec:
  githubConfigUrl: https://github.com/acme/payments
  minRunners: 0          # скейл до нуля, когда очередь пуста
  maxRunners: 200        # потолок, чтобы табун не разорил
  template:
    spec:
      containers:
        - name: runner
          image: ghcr.io/actions/actions-runner:latest
```

## У кривой стоимости две оси

Вот сеньорный пересмотр, который команда из военной истории упустила. Стоимость CI — не одно число. Это **$ за раннеры** плюс **$ инженерного времени** всех, кто ждёт CI. Оптимизируй только счёт — меньше раннеров, меньше машины, зажатый concurrency — и ты раздуешь время очереди и wall-clock, что умножается на каждого ждущего инженера. Инженеро-часы организации затмевают её счёт за CI, так что истинный оптимум — обычно **больше** параллелизма, а не меньше: добавляй раннеры, пока wall-clock не перестанет улучшаться, потому что дальше этой точки ты платишь за машины, которые не сокращают ожидание, но до неё каждая срезанная минута умножается на команду.

Два рычага делают «больше параллелизма» дешёвым. Первый — **spot / preemptible** инстансы: CC взаимозаменяем и безопасен к ретраю — вытесненная задача просто перезапускается, — так что это идеальная нагрузка для spot, срезающая счёт за раннеры на **60-90%**. Второй — **right-sizing**: большинству задач нужен маленький раннер; резервируй большие машины для тех немногих задач, что реально ускоряются на них. Линт README на 64-ядерном раннере — чистая растрата.

<TradeoffMatrix
  id="ci-cd-06-scaling-ci-04-runner-infrastructure-and-cost-tradeoff-1"
  lessonSlug="04-runner-infrastructure-and-cost"
  lang="ru"
  prompt="Твой счёт за CI высок И PR-ы стоят в очереди по 20 минут на пике. Инженеры простаивают в очереди по несколько раз в день. Какой дизайн флота чинит и то, и другое разом?"
  options={[
    { name: "Автоскейлинговые ephemeral-раннеры (ARC) на spot-инстансах, right-sized под задачу, с p95 времени очереди как отслеживаемой метрикой", summary: "Скейль под очередь, на spot.", correct: true, justification: "Это бьёт по обеим осям разом. Автоскейлинг добавляет мощность ровно когда очередь растёт и скейлит до нуля на простое, так что 20-минутная пиковая очередь рассасывается без оплаты простаивающих машин вне пика. Ephemeral держит каждую задачу чистой комнатой, так что self-hosting безопасен. Spot срезает счёт за раннеры на 60-90% на безопасной к ретраю нагрузке, а right-sizing прекращает платить по ставке большой машины за маленькие задачи. Слежение за p95 времени очереди означает, что ты видишь настоящую боль (ожидание инженеров), а не только длительность задачи. И счёт, и очередь падают." },
    { name: "Один большой persistent hosted-раннер с зажатым concurrency, чтобы контролировать счёт", summary: "Режет траты, раздувает очередь.", justification: "Это ровно ошибка из военной истории. Зажатие concurrency — это как раз способ ПОЛУЧИТЬ 20-минутную очередь: ты задушил пропускную способность ради защиты счёта, который мал рядом с инженеро-часами, теперь горящими в очереди. Persistent-раннер вдобавок накапливает межзадачное состояние — опасность утечки и невоспроизводимости. Он оптимизирует дешёвую ось и раздувает дорогую." },
    { name: "Много always-on больших раннеров, чтобы всегда была свободная мощность", summary: "Убивает очередь, платит за простой.", justification: "Это и правда рассасывает очередь — грубой силой, — но ценой того, что ты пытался срезать: флот, рассчитанный на пик, простаивает большую часть времени вне пика, и они все большие, так что ты платишь по ставке больших машин круглосуточно за мощность, используемую несколько часов в день. Автоскейлинг даёт тот же пиковый запас, скейля до нуля на простое; это ненастроенная версия, что чинит одну ось, бросая другую." },
    { name: "Снизить параллелизм тестов, чтобы срезать счёт", summary: "Назад по обеим осям.", justification: "Это двигает оба числа в неверную сторону. Меньше параллелизма означает более длинный wall-clock на прогон И более длинную очередь, поскольку задачи накапливаются, умножая ожидание инженеров — дорогую ось, — чтобы срезать дешёвую. Раз инженеро-часы затмевают счёт за CI, оптимум обычно БОЛЬШЕ параллелизма до точки, где wall-clock перестаёт улучшаться, а не меньше." },
  ]}
/>

<Quiz
  id="ci-cd-06-scaling-ci-04-runner-infrastructure-and-cost-quiz-1"
  lessonSlug="04-runner-infrastructure-and-cost"
  lang="ru"
  question="Ты выбираешь между GitHub-hosted и self-hosted раннерами. Какое утверждение схватывает суть трейда, который ты на самом деле делаешь?"
  choices={[
    { label: "Hosted = ноль операций, ephemeral-VM на задачу, поминутная оплата; self-hosted = дешевле на объёме плюс кастомные спецификации и доступ к VPC, но ты владеешь патчингом, скейлингом и безопасностью (и обязан держать их вне публичных репозиториев)", correct: true },
    { label: "Self-hosted раннеры всегда дешевле и безопаснее, так что предпочитай их, как только появится любой объём", misconception: "Дешевле на большом объёме — да, но не «безопаснее» даром. Self-hosted означает, что ТЫ владеешь патчингом, скейлингом и изоляцией, а self-hosted раннер на публичном репозитории запускает недоверенный код PR на твоей инфраструктуре — серьёзная supply-chain угроза. Трейд — это владение операциями и безопасностью в обмен на контроль над стоимостью и спецификацией, а не бесплатная победа по обоим." },
    { label: "Hosted-раннеры не могут получить большие машины или GPU, но в остальном эти два взаимозаменяемы", misconception: "Потолок спецификации — одна причина уйти на self-hosted, но не единственная ось. Стоимость на объёме и доступ к VPC тоже толкают к self-hosted, а в ответ ты берёшь на себя патчинг, скейлинг и владение безопасностью. Назвать их «в остальном взаимозаменяемыми» — значит спрятать ровно ту операционную и безопасностную цену, что решает судьбу реальных флотов." },
    { label: "Единственная разница — кто шлёт счёт; оба дают одну и ту же ephemeral-изоляцию автоматически", misconception: "Hosted даёт ephemeral-изоляцию по умолчанию; self-hosted НЕ даёт, пока ты намеренно не запустишь ephemeral-раннеры. Наивный self-hosted флот — persistent и накапливает межзадачное состояние. Изоляция — это то, что ты обязан инженерно построить на self-hosted, а не данность." },
  ]}
/>

<Quiz
  id="ci-cd-06-scaling-ci-04-runner-infrastructure-and-cost-quiz-2"
  lessonSlug="04-runner-infrastructure-and-cost"
  lang="ru"
  question="Почему автоскейлинговый флот ephemeral-раннеров бьёт фиксированный флот persistent-раннеров для нагруженной организации с рваным спросом?"
  choices={[
    { label: "Ephemeral даёт чистую комнату на задачу (нет утечки межзадачного состояния), а автоскейлинг следует за очередью — добавляя мощность при табуне, скейля до нуля на простое — так что ты избегаешь и ожидания в очереди, и оплаты простаивающих машин", correct: true },
    { label: "Фиксированный persistent-флот нормален, пока ты подбираешь N под пиковый спрос", misconception: "Подбор N под пик означает, что эта мощность простаивает каждый внепиковый час — ты платишь за машины размером с табун всю ночь. И persistent-раннеры всё ещё накапливают межзадачное состояние. Автоскейлинг даёт тот же пиковый запас, но скейлит до нуля на простое, а ephemeral убирает проблему утечки состояния, которая у фиксированного флота остаётся." },
    { label: "Persistent-раннеры быстрее, потому что пропускают запуск, а скорость всегда бьёт изоляцию", misconception: "Persistent-раннеры и правда пропускают запуск, но эта скорость покупает межзадачное заражение состоянием и невоспроизводимость, и она ничего не делает с проблемой очереди — фиксированный флот всё равно встаёт в очередь при табуне и платит за простой вне пика. Тёплые пулы возвращают большую часть стоимости запуска, не отказываясь от чистой комнаты." },
    { label: "Автоскейлинг снимает нужду мониторить время очереди, ведь у него всегда мгновенно хватает раннеров", misconception: "Автоскейлинг реагирует на очередь, он не делает очередь невозможной — есть латентность подъёма, а твой потолок maxRunners может упереться при большом табуне. p95 времени очереди всё ещё метрика, за которой ты следишь; если она лезет вверх, ты поднимаешь потолок или ускоряешь подготовку." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Цикл автоскейлинга-ephemeral, заменяющий фиксированный флот раннеров. Задача поставлена в очередь. Автоскейлер наблюдает рост глубины очереди. Он поднимает ephemeral-раннер на spot-инстансе, right-sized под задачу. Этот раннер исполняет ровно одну задачу, затем уничтожается. Когда очередь пуста, флот скейлится до нуля, так что простаивающие часы не стоят ничего. На всём протяжении p95 времени очереди — отслеживаемый сигнал: если оно растёт, поднимай потолок или ускоряй подготовку."
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "queued", label: "Задача в очереди", sub: "ждёт раннера" },
    { id: "observe", label: "Автоскейлер видит глубину", sub: "очередь растёт" },
    { id: "spin", label: "Поднять ephemeral-раннер", sub: "spot, right-sized" },
    { id: "run", label: "Прогнать одну задачу", sub: "чистая комната" },
    { id: "destroy", label: "Уничтожить раннер", sub: "состояние не выживает" },
    { id: "zero", label: "Скейл до нуля", sub: "простой бесплатен" },
  ]}
  edges={[
    { from: "queued", to: "observe", label: "сигнал глубины" },
    { from: "observe", to: "spin", label: "скейл вверх" },
    { from: "spin", to: "run", label: "одна задача" },
    { from: "run", to: "destroy", label: "ephemeral" },
    { from: "destroy", to: "zero", label: "очередь пуста" },
    { from: "zero", to: "queued", label: "следующий пуш" },
  ]}
  caption="Цикл замыкается на очереди: глубина гонит скейл вверх, каждый раннер ephemeral и умирает после одной задачи, а пустая очередь скейлит флот до нуля. p95 времени очереди — это ручка, за которой ты следишь, чтобы решить, достаточно ли высоки потолок и скорость подготовки."
/>

<KeyTakeaway>Стоимость CI = счёт за раннеры + ожидание инженеров, и инженеро-часы затмевают счёт. Запускай ephemeral-раннеры, автоскейль под очередь (до нуля), ставь на spot (−60-90%), right-size, следи за p95 очереди.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="ci-cd-06-scaling-ci-04-runner-infrastructure-and-cost-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, почему ephemeral-раннеры стоят своей платы за запуск на задачу против быстрого persistent-флота, и как автоскейлинг (ARC) складывается с ними.", a: "Persistent-раннер берёт задачу за задачей и накапливает состояние между ними: оставшиеся файлы рабочей директории, отравленный кеш зависимостей, фоновый процесс от предыдущей и, возможно, недоверенной задачи. Этот осадок — канал заражения: поздняя задача может унаследовать секреты, подменённый бинарник или испорченный кеш, и ничто не воспроизводимо, ведь каждая задача стартует оттуда, где предыдущая оставила машину. Ephemeral-раннер исполняет ровно одну задачу и затем уничтожается и пересоздаётся с нуля, давая чистую комнату на задачу: детерминированное стартовое состояние, ноль межзадачных утечек, а скомпрометированная задача умирает вместе со своим раннером. Цена — свежая загрузка на задачу, смягчаемая тёплыми пулами (несколько заранее подготовленных простаивающих раннеров) и быстрыми базовыми образами, — но изоляция это свойство безопасности и воспроизводимости, которое нельзя получить с persistent-флота ни за какие деньги. Автоскейлинг через Actions Runner Controller запускает эти ephemeral-раннеры как поды Kubernetes, скейлящиеся под очередь задач: больше подов, когда очередь растёт, скейл до нуля, когда она пустеет. Раз раннеры ephemeral, автоскейлинг и изоляция-чистая-комната складываются — ты получаешь мощность по требованию И свежую машину на задачу, а не выбираешь одно." },
    { q: "Разложи двухосевую модель стоимости CI и вытекающие из неё решения по флоту: hosted против self-hosted, spot, right-sizing и метрику, за которой следить.", a: "Стоимость CI — два числа, а не одно: счёт за раннеры плюс стоимость инженерного времени всех, кто ждёт CI. Инженеро-часы организации затмевают её счёт за CI, так что оптимизировать только счёт (меньше/меньше раннеров, зажатый concurrency) — это ловушка: она раздувает время очереди и wall-clock, умножая ожидание инженеров. Истинный оптимум обычно БОЛЬШЕ параллелизма: добавляй раннеры, пока wall-clock не перестанет улучшаться, ведь до этой точки каждая срезанная минута умножается на команду. Hosted-раннеры — это дефолт с нулём операций (чистая ephemeral-VM на задачу, поминутно), пока счёт на объёме, потолок спецификации (большие машины/GPU) или доступ к VPC не толкнут на self-hosted — дешевле на объёме и любая спецификация, но ты владеешь патчингом, скейлингом и безопасностью и обязан держать self-hosted раннеры вне публичных репозиториев (недоверенный код PR на твоей инфраструктуре). Два рычага делают больше параллелизма дешёвым: spot/preemptible инстансы, идеальные потому что CC безопасен к ретраю (вытесненная задача просто перезапускается), срезающие счёт за раннеры на 60-90%; и right-sizing — большинству задач нужен маленький раннер, так что резервируй большие машины для тех немногих, что на них ускоряются. Метрика, за которой следить, — это p95 времени ОЧЕРЕДИ (задача, ждущая свободного раннера), невидимое в логе прогона и прямо умножающее ожидание разработчиков, а не только длительность задачи. Если p95 времени очереди растёт, добавляй мощность или поднимай потолок автоскейлера." },
  ]}
/>

<Recap lang="ru">
Флот раннеров под твоими пайплайнами решает и счёт за CI, и скрытую стоимость ожидания инженеров. GitHub-hosted раннеры — дефолт с нулём операций — чистая ephemeral-VM на задачу, поминутно, — пока счёт на большом объёме, потолок спецификации (большие машины, GPU) или доступ к VPC не вынудят тебя на self-hosted, где ты получаешь контроль над стоимостью и спецификацией, но владеешь патчингом, скейлингом и безопасностью и никогда не должен привязывать self-hosted раннеры к публичным репозиториям, потому что PR незнакомца запустится на твоей инфраструктуре. Практика, делающая self-hosting безопасным, — ephemeral-раннеры: каждый раннер исполняет ровно одну задачу, затем уничтожается, давая чистую комнату на задачу, тогда как persistent-раннер накапливает межзадачное состояние (оставшиеся файлы, отравленный кеш, бродячий процесс), которое утекает между задачами и невоспроизводимо; плата за запуск на задачу смягчается тёплыми пулами и быстрыми образами, но изоляцию иначе не получить. Фиксированный флот отказывает обоими способами — слишком мало раннеров встают в очередь и тратят время инженеров, слишком много платят за простой, — так что автоскейлинг через Actions Runner Controller запускает ephemeral-раннеры как поды Kubernetes, скейлящиеся под очередь и до нуля на простое. Наконец, у стоимости CI две оси: счёт за раннеры плюс ожидание инженеров, и раз инженеро-часы затмевают счёт, оптимум обычно больше параллелизма, а не меньше — ставь безопасную к ретраю нагрузку CC на spot/preemptible инстансы ради среза счёта на 60-90%, делай right-sizing, чтобы большие машины обслуживали лишь те задачи, что выигрывают, и следи за p95 времени очереди, а не за длительностью задачи, ведь время очереди невидимо в логе прогона, но прямо умножает ожидание разработчиков.
</Recap>
