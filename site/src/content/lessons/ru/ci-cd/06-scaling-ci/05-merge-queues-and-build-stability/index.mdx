---
concepts:
- merge-queue
- speculative-build
- logical-conflict
- batching-and-bisection
- branch-protection
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 5
prereqs: []
slug: "05-merge-queues-and-build-stability"
sources:
- https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/configuring-pull-request-merges/managing-a-merge-queue
- https://bors.tech/
spiral: []
status: ready
summary: 'Проверки PR тестируют устаревший main, поэтому два независимо зелёных PR ломают main логическим конфликтом, которого git не видит. Merge queue спекулятивно собирает main + очередь PR до слияния, батчит ради пропускной способности и бисектит виновника — main зелёный по построению.'
title: 'Очереди слияния и зелёный main'
track: ci-cd
unit: 06-scaling-ci
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Команда сделала всё правильно. Защита ветки включена, обязательные проверки зелёные, каждый PR отревьюен. И `main` всё равно ломался примерно три раза в день. Шаблон был всегда один: два PR, каждый зелёный по отдельности, слиты с разницей в двадцать минут, и второй красил сборку на строке, которую ни один автор не трогал. В одну пятницу это был ренейминг — кто-то сократил `computeShippingCost` до `shippingCost` и обновил все одиннадцать мест вызова, зелёно. Сорока минутами раньше коллега открыл PR, добавлявший *новый* вызов `computeShippingCost`, тоже зелёный, отведённый от ветки до того, как ренейминг существовал. Git слил оба чисто — нет пересекающихся строк — и `main` не скомпилировался. Каждый push после этого был заблокирован за красной сборкой, пока кто-то не бисектил, не находил осиротевший вызов и не делал revert. Проверки не врали. Они отвечали на вопрос, который никто не задавал: «зелёный ли этот PR против `main` таким, каким он был, когда я тестировал?» — а не «против `main` таким, каким он станет, когда всё передо мной приземлится».
</Hook>

<Crux>Проверки PR тестируют устаревший main; merge queue спекулятивно собирает main + очередь PR до слияния — main зелёный по построению.</Crux>

<Explanation>

## Почему зелёные PR всё равно ломают main

Обязательная проверка на pull request не тестирует `main`. Она тестирует *голову твоего PR, слитую с `main` таким, каким он был в момент запуска CI*, — снимок. К моменту, когда твой PR действительно сливается, другие PR могли приземлиться впереди него, так что `main`, против которого валидировался твой код, больше не существует.

Слияние git блокирует только **текстовые** конфликты: два изменения пересекающихся строк. Оно полностью слепо к **логическим** (семантическим) конфликтам, где два изменения не делят ни единой строки, но несовместимы по смыслу:

```text
PR A (зелёный):  rename computeShippingCost -> shippingCost, обновить 11 вызовов
PR B (зелёный):  добавить НОВЫЙ вызов computeShippingCost(...) в файле, который PR A не трогал
                 (отведён до того, как PR A существовал)

git merge A затем B:  ноль пересекающихся строк  ->  сливается чисто
результат в main:     вызов функции, которой больше нет  ->  сборка красная
```

Каждый PR был зелёным против того `main`, на котором тестировался. Ни один прогон тестов не мог поймать поломку, потому что комбинация — A *и* B вместе — это состояние, которого нигде не существовало, пока слияние его не создало. Вот почему «просто перезапусти проверки на каждом PR» не спасает: перезапуск всё ещё тестирует каждый PR против `main`, который устаревает в тот же миг, как приземляется другой PR. Чем выше скорость слияния, тем чаще «протестировано против `main`, которого больше нет» выкатывает сломанный `main`.

## Merge queue: тестировать main-каким-он-станет

Merge queue (GitHub merge queue, Bors, Mergify) чинит это, **сериализуя интеграцию**. Вместо того чтобы слить PR напрямую, она конструирует **спекулятивную ветку** = текущий `main` + этот PR (плюс всё, что впереди него в очереди) и прогоняет полный набор обязательных проверок на *этом комбинированном результате*. PR приземляется только если сборка очереди — `main` ровно такой, каким он станет после слияния этого батча, — зелёная.

```yaml
# GitHub merge queue: обязательная проверка запускается на событии merge_group,
# то есть против спекулятивной ветки, которую строит очередь (main + очередь PR),
# А НЕ просто против головы PR. main защищён; ничто не пушит напрямую.
on:
  merge_group:        # <- спекулятивная сборка очереди
  pull_request:       # <- всё ещё на каждый PR ради быстрого фидбэка автору
jobs:
  required-checks:
    runs-on: ubuntu-latest
    steps:
      - run: ./ci/affected-build-and-test.sh   # держи БЫСТРОЙ — она гейтит очередь
```

Ничто не достигает `main`, не протестированное против ровно тех других изменений, что приземляются вместе с ним. PR с ренеймингом и PR с новым вызовом, поставленные в очередь вместе, были бы собраны на одной спекулятивной ветке — и эта сборка падает *до* того, как любой из них сольётся, так что виновник удерживается вне, а не отравляет всех. `main` остаётся зелёным не благодаря удаче или бдительности, а **по построению**: единственное, что вообще приземляется, — это комбинация, уже доказанно зелёная как комбинация.

<FlowDiagram
  label="Жизненный цикл merge queue, который держит main зелёным. Pull request, зелёный на своей ветке, входит в merge queue, а не сливается напрямую. Очередь строит спекулятивную ветку, равную текущему main плюс этот PR плюс всё, что впереди него в очереди, затем прогоняет полный набор обязательных проверок на этом комбинированном результате. Если спекулятивная сборка зелёная, весь батч сливается разом. Если красная, очередь бисектит батч, выгоняет PR, который её сломал, и перезапускает оставшиеся PR. В любом случае на main приземляется только комбинация, уже доказанно зелёная, так что main остаётся зелёным структурно."
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "pr", label: "PR зелёный", sub: "своя ветка" },
    { id: "queue", label: "merge queue", sub: "сериализ." },
    { id: "spec", label: "спекулятивн.", sub: "main + PR" },
    { id: "merge", label: "слить батч", sub: "если зелён." },
    { id: "bisect", label: "бисект+выгон", sub: "если красн." },
    { id: "green", label: "main зелён.", sub: "структурно" },
  ]}
  edges={[
    { from: "pr", to: "queue", label: "вход" },
    { from: "queue", to: "spec", label: "сборка" },
    { from: "spec", to: "merge", label: "зелён." },
    { from: "spec", to: "bisect", label: "красн." },
    { from: "merge", to: "green", label: "ландит" },
    { from: "bisect", to: "green", label: "перезапуск" },
  ]}
  caption="PR входит в очередь, очередь спекулятивно собирает main + очередь PR, и сливается только зелёная комбинация. Красная сборка бисектится, выгоняя виновника, — один плохой PR выгоняет сам себя, а не клинит очередь."
/>

## Батчинг и бисекция: пропускная способность при высоком объёме слияний

Тестировать по одному PR за раз против текущего `main` корректно, но медленно: при высоком объёме очередь становится строем в один ряд, и каждый прогон CI пропускает только один PR. Поэтому очереди **батчат** — спекулятивно собирают `main` + PR1 + PR2 + ... + PRn вместе. Если батч зелёный, *все n PR сливаются с одного прогона CI*. В этом выигрыш пропускной способности: одна дорогая сборка пропускает весь батч вместо n сборок, пропускающих n PR.

Риск в том, что один плохой PR в батче красит весь батч. Очередь справляется с этим, **бисектя**: она разбивает падающий батч, чтобы найти, какой PR виноват, **выгоняет** виновника (отбрасывает обратно его автору) и перезапускает оставшиеся PR меньшим батчем. Так один сломанный PR удаляет себя сам, а не блокирует четыре хороших PR, стоящих вокруг него в очереди.

```bash
# Батч из 4 собран спекулятивно вместе; сборка батча падает.
# Очередь бисектит, локализуя поломку, выгоняет виновника, перезапускает остальных.
queue batch: [PR1, PR2, PR3, PR4]   -> спекулятивная сборка КРАСНАЯ
  bisect [PR1, PR2] -> зелёный       # поломка во второй половине
  bisect [PR3, PR4] -> красный       # сужено
  bisect [PR3]      -> зелёный
  bisect [PR4]      -> красный        # PR4 — виновник
eject PR4 (обратно автору); re-run [PR1, PR2, PR3] -> зелёный -> слить 3
```

Компромисс прямой: **больше батчи = выше пропускная способность, но больше потерянной работы при падении** (красный батч из 8 выбрасывает сборку восьмёрки и запускает бисекцию). Меньшие батчи переделывают меньше, но пропускают меньше PR за прогон. Размер батча ты настраиваешь под свою **частоту падений** — флакающий или склонный к поломкам репозиторий хочет меньших батчей; дисциплинированный с редкими падениями может гонять большие батчи и редко платить налог бисекции.

<Inset kind="why" lang="ru">
Почему «требовать прохождения проверок PR перед слиянием» не держит main зелёным при высоком объёме слияний? Потому что проверки каждого PR прогоняются против `main` таким, каким он был, когда этот PR тестировался, а не каким он станет после слияния PR впереди него. Git блокирует только текстовые конфликты — пересекающиеся строки — так что логический конфликт (PR A переименовывает X, PR B добавляет вызов старого X в файле, который A не трогал) не имеет пересечений, сливается чисто и ломает `main`. Комбинация A-и-B — это состояние, которого нигде не существовало, пока слияние его не создало, так что прогон CI ни одного PR не мог её протестировать. Единственное, что действительно держит `main` зелёным, — это тестирование реальной пост-слиянной комбинации *до* её приземления, что ровно и делает спекулятивная сборка merge queue.
</Inset>

## Почему очереди нужны защита ветки и быстрый гейт

Merge queue работает только если `main` **защищён** — никаких прямых пушей — и комбинированная проверка очереди является **обязательным гейтом**. Если кто угодно может пушить в `main` напрямую, он обходит спекулятивную сборку, и вся гарантия испаряется: очередь может обещать «всё, что приземлилось, было доказано зелёным как комбинация» только если *всё* приземляется через очередь.

И гейт должен быть **быстрым**. Пропускная способность очереди ограничена длительностью гейтящей проверки: очередь не может пропустить PR2, пока не закончится сборка, включающая PR1, так что 40-минутная обязательная проверка ограничивает твою скорость слияния независимо от того, сколько PR ждёт. Это сквозная линия всего этого юнита — сборки только затронутого, кэширование, шардинг тестов, правильно подобранные раннеры существуют, чтобы обязательная проверка оставалась короткой, потому что здесь именно эта проверка дросселирует каждое слияние в организации. Медленный гейт не просто раздражает одного автора; он сериализует всю команду за собой.

<TradeoffMatrix
  id="ci-cd-06-scaling-ci-05-merge-queues-and-build-stability-tradeoff-1"
  lessonSlug="05-merge-queues-and-build-stability"
  lang="ru"
  prompt="main ломается несколько раз в день из-за PR, каждый из которых был зелёным на своей ветке — логические конфликты вроде ренейминга плюс устаревшего вызова. Ты сливаешь высокий объём PR. Что реально держит main зелёным, не дросселируя пропускную способность?"
  options={[
    { name: "Merge queue, которая спекулятивно собирает main + очередь PR до слияния, с батчингом + бисекцией, на защищённом main", summary: "Тест main-каким-станет.", correct: true, justification: "Это единственный вариант, который тестирует реальную пост-слиянную комбинацию до её приземления. Очередь строит спекулятивную ветку (текущий main + очередь PR) и сливает, только если эта комбинированная сборка зелёная, так что пара ренейминг-плюс-устаревший-вызов валит спекулятивную сборку и удерживается вне, а не ломает main — зелёный по построению. Батчинг держит пропускную способность (n PR проходят с одного прогона CI), а бисекция выгоняет один плохой PR вместо того, чтобы клинить очередь. Защищённый main гарантирует, что всё приземляется через очередь, что и делает гарантию рабочей." },
    { name: "Требовать проверки PR и просто перезапускать их на ветке PR перед слиянием", summary: "Всё ещё устаревший main.", justification: "Перезапуск на ветке PR всё ещё тестирует PR против main таким, каким он был при прогоне CI, а не каким он станет после приземления PR впереди него. В тот же миг, как сливается другой PR, твой перезапуск снова устаревает. Он не может поймать логический конфликт, потому что сломанная комбинация — состояние, которого нигде не существует, пока слияние его не создаст, — а проверка на каждый PR никогда не собирает эту комбинацию. Это ровно та политика, что уже падала." },
    { name: "Просить разработчиков делать rebase на свежий main перед слиянием", summary: "Вручную, гонка, без принуждения.", justification: "Ручной rebase сужает окно, но не закрывает его: между rebase, перезапуском и кликом слияния другой PR может приземлиться и снова устарить твою ветку. Он держится на дисциплине, не масштабируется с объёмом слияний, и сам rebase разрешает только текстовые конфликты — логический конфликт всё равно проскальзывает чисто. Это более медленное, более гоночное, принуждаемое человеком приближение того, что очередь делает механически." },
    { name: "Оставить прямые слияния и делать revert-and-reland вручную каждый раз, когда main ломается", summary: "Реактивно, блокирует всех.", justification: "Revert-and-reland — это уборка, а не профилактика: main уже сломан, и каждый push заблокирован за красной сборкой, пока кто-то не бисектит и не делает revert. При нескольких поломках в день это постоянное тушение пожаров и потерянное время команды, и это трактует структурную проблему (приземление непротестированных комбинаций) как операционную. Очередь делает поломку невозможной заранее, а не дешевле в восстановлении." },
  ]}
/>

<Quiz
  id="ci-cd-06-scaling-ci-05-merge-queues-and-build-stability-quiz-1"
  lessonSlug="05-merge-queues-and-build-stability"
  lang="ru"
  question="Два PR проходят все обязательные проверки на своих ветках, сливаются с разницей в пару минут, и второе слияние красит main — на строке, которую ни один автор не редактировал. Что произошло?"
  choices={[
    { label: "Логический (семантический) конфликт, который git не может обнаружить: каждый PR тестировался против main-на-момент-своего-PR, и комбинация обоих — например, ренейминг плюс устаревший вызов — возникла, только когда оба слились", correct: true },
    { label: "Один из PR, должно быть, пропустил обязательные проверки — зелёные PR не могут сломать main", misconception: "Оба PR действительно прошли. Ловушка в ТОМ, против чего они прошли: каждый тестировался против main таким, каким он был при прогоне его CI, а не против комбинации обоих PR. Git сливает их чисто, потому что нет текстового пересечения, но слитый результат — состояние, которого ни один прогон CI не собирал. Зелёные PR абсолютно могут сломать main так — в этом вся мотивация merge queue." },
    { label: "Флакающий тест, который прошёл на ветке PR и упал на main", misconception: "Флакость прерывиста и не связана со строкой, которую никто не редактировал. Описанный шаблон — два независимо зелёных PR, поломка на нетронутой строке, только после приземления обоих — это сигнатура логического конфликта (например, ренейминг + устаревший вызов), а не флакающего теста. Комбинация по-настоящему сломана, детерминированно, а не мигает." },
    { label: "Git не сумел обнаружить конфликт слияния и молча отбросил изменения одной стороны", misconception: "Git ничего не провалил и не отбросил — он слил корректно. Два PR трогают непересекающиеся строки, так что нет текстового конфликта для обнаружения. Поломка семантическая: код сливается прекрасно и логически несогласован (вызов переименованной функции). Git разрешает только текстовые конфликты; логические — ровно то, что он не может увидеть." },
  ]}
/>

<Quiz
  id="ci-cd-06-scaling-ci-05-merge-queues-and-build-stability-quiz-2"
  lessonSlug="05-merge-queues-and-build-stability"
  lang="ru"
  question="Как merge queue держит main зелёным И сохраняет пропускную способность, когда много PR приземляется в час?"
  choices={[
    { label: "Она спекулятивно собирает main + очередь PR вместе и сливает, только если эта комбинированная сборка зелёная; она батчит N PR в один прогон ради пропускной способности и бисектит при падении, выгоняя виновника", correct: true },
    { label: "Она прогоняет проверки каждого PR ещё раз против свежего main прямо перед слиянием", misconception: "Перезапуск против «свежего main» всё ещё тестирует каждый PR против снимка, который устаревает в момент слияния следующего PR, и никогда не собирает комбинацию очереди PR вместе — так что не может поймать логический конфликт между ними. Сила очереди — в СПЕКУЛЯТИВНОЙ сборке main + очереди PR как единого целого, что ровно и есть пост-слиянное состояние, до приземления чего-либо." },
    { label: "Она лочит main и заставляет PR сливаться строго по одному, полностью последовательно", misconception: "Строгое слияние по одному корректно, но медленно — при высоком объёме оно становится узким местом в один ряд, один прогон CI на PR. Реальные очереди батчат: спекулятивно тестируют main + PR1 + PR2 + ... вместе, сливают все с одного зелёного прогона и бисектят, выгоняя виновника, если батч падает. Батчинг — ровно то, как сохраняется пропускная способность." },
    { label: "Она расширяет раннеры и шардит тесты, так что каждый PR сливается мгновенно", misconception: "Более быстрые раннеры и шардинг укорачивают гейтящую проверку (и они важны, потому что длительность гейта ограничивает пропускную способность очереди), но скорость сама по себе никогда не предотвращает логический конфликт — быстрая сборка устаревшей комбинации всё равно неверна. Держать main зелёным требует собрать реальную пост-слиянную комбинацию (спекулятивную сборку), а не просто собрать неверное быстрее." },
  ]}
/>

</Explanation>

<KeyTakeaway>Проверки PR тестируют устаревший main, поэтому два зелёных PR ломают его логическим конфликтом, которого git не видит. Merge queue спекулятивно собирает main + очередь PR до слияния — зелёный по построению.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="ci-cd-06-scaling-ci-05-merge-queues-and-build-stability-retrieval"
  lang="ru"
  questions={[
    { q: "Почему два PR, каждый из которых проходит все обязательные проверки, всё равно могут сломать main при слиянии, и почему перезапуск проверок это не чинит?", a: "Обязательная проверка на PR не тестирует main — она тестирует голову PR, слитую с main таким, каким он был в момент запуска CI, снимок. К моменту слияния другие PR могли приземлиться впереди, так что main, против которого валидировался код, больше не существует. Слияние git блокирует только текстовые конфликты (пересекающиеся строки); оно слепо к логическим/семантическим конфликтам, где два изменения не делят ни строки, но несовместимы — канонический случай: PR A переименовывает computeShippingCost и обновляет его вызовы (зелёный), а PR B, отведённый раньше, добавляет НОВЫЙ вызов старого имени в файле, который A не трогал (зелёный). Git сливает оба чисто с нулём пересечений, и main теперь вызывает функцию, которой нет — красный. Ни один прогон CI не мог это поймать, потому что комбинация A-и-B — состояние, которого нигде не существовало, пока слияние его не создало. Перезапуск проверок на каждый PR не помогает: каждый перезапуск всё ещё тестирует один PR против main, который устаревает в миг приземления другого PR, и никогда не собирает комбинацию. Чем выше скорость слияния, тем чаще «протестировано против main, которого больше нет» выкатывает сломанный main." },
    { q: "Как merge queue держит main зелёным и как батчинг и бисекция сохраняют пропускную способность при высоком объёме слияний?", a: "Merge queue (GitHub merge queue, Bors, Mergify) сериализует интеграцию: вместо прямого слияния PR она строит спекулятивную ветку = текущий main + этот PR + всё, что впереди него в очереди, прогоняет полный набор обязательных проверок на этом комбинированном результате и сливает, только если эта сборка — main ровно такой, каким он станет после приземления батча, — зелёная. Так ничто не достигает main, не протестированное против ровно тех других изменений, что приземляются вместе с ним; main остаётся зелёным по построению, потому что единственное, что вообще приземляется, — комбинация, уже доказанно зелёная как комбинация. Тестировать по одному PR корректно, но медленно при объёме, поэтому очереди БАТЧАТ: спекулятивно собирают main + PR1 + ... + PRn вместе, и если зелёно, все n сливаются с одного прогона CI (одна дорогая сборка пропускает весь батч). Если батч КРАСНЫЙ, очередь БИСЕКТИТ — разбивает падающий батч, локализуя, какой PR сломал, выгоняет виновника обратно автору и перезапускает оставшиеся PR — так один плохой PR удаляет сам себя, а не клинит хорошие PR вокруг него. Компромисс: больше батчи = выше пропускная способность, но больше потерянной работы при падении, так что размер батча настраивается под частоту падений. Два предусловия: main должен быть защищён (нет прямых пушей, иначе PR обходят спекулятивную сборку) и гейтящая проверка должна быть БЫСТРОЙ, потому что пропускная способность очереди ограничена длительностью гейтящей проверки — вот почему сборки затронутого, кэширование, шардинг и правильные раннеры здесь важны." },
  ]}
/>

<Recap lang="ru">
Обязательные проверки PR не тестируют main — они тестируют голову PR, слитую с main таким, каким он был при запуске CI, снимок, который устаревает в момент приземления другого PR впереди него. Слияние git блокирует только текстовые конфликты (пересекающиеся строки); оно не видит логических/семантических конфликтов, где два непересекающихся изменения несовместимы по смыслу — классический случай: PR A переименовывает функцию и обновляет её вызовы, а PR B, отведённый раньше, добавляет новый вызов старого имени в файле, который A не трогал. Оба зелёные, git сливает их чисто, и main ломается на строке, которую ни один автор не редактировал, потому что комбинация обоих PR — состояние, которого нигде не существовало, пока слияние его не создало; перезапуск проверок на каждый PR это не ловит. Merge queue чинит это, сериализуя интеграцию: она строит спекулятивную ветку текущего main плюс очередь PR, прогоняет полный набор обязательных проверок на этом комбинированном результате и сливает, только если main-каким-он-станет зелёный — так main остаётся зелёным по построению. Чтобы держать пропускную способность высокой при объёме, очередь батчит N PR в один спекулятивный прогон, сливая все, если зелёно; если батч красный, она бисектит, локализуя и выгоняя виновника, затем перезапускает остальных, так один плохой PR удаляет себя сам, а не клинит очередь. Больше батчи означают больше пропускной способности, но больше потерянной работы при падении, так что размер батча настраивается под частоту падений. Гарантия держится, только если main защищён (всё приземляется через очередь) и гейтящая проверка быстрая, потому что пропускная способность очереди ограничена длительностью этой проверки — ровно поэтому сборки затронутого, кэширование, шардинг и правильные раннеры важны.
</Recap>
