---
concepts:
- test-sharding
- timing-balanced-splits
- flaky-tests
- flake-detection
- quarantine-lane
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 3
prereqs: []
slug: "03-test-sharding-and-flaky-quarantine"
sources:
- https://docs.github.com/en/actions/using-jobs/using-a-matrix-for-your-jobs
- https://playwright.dev/docs/test-sharding
spiral: []
status: ready
summary: 'У масштабирования тест-сьюта два режима отказа: серийный сьют, гейтящий каждый мердж, и flaky-тесты, которые компаундятся и держат CI красным почти всегда. Шардь по времени, а не по числу файлов; детектируй flake на ретрае и уводи с гейта — не делай глобальный ретрай политикой.'
title: 'Шардинг тестов и карантин flaky-тестов'
track: ci-cd
unit: 06-scaling-ci
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Сьют из 600 тестов крутился 40 минут серийно на каждом мердже — раздражает, но терпимо. Потом он начал падать. Не из-за чьего-то кода: несколько интеграционных тестов сползли до прохождения примерно в 95% запусков, и на 600 тестах это скомпаундилось в красные сборки примерно в трети случаев. Команда сделала очевидное и добавила `retries: 2` в конфиг раннера. CI стал медленнее — перепрогон падений не бесплатен — а красное упало, и все забыли. Через три недели в прод уехал баг в платежах. Тот «flaky»-тест, что мигал красным-зелёным, всё это время ловил настоящую гонку; политика ретрая тихо превращала его истинный негатив в зелёную галочку со второй попытки. Этот урок — про две вещи, которые ломают тест-сьют на масштабе: он слишком медленный, потому что крутится серийно, и ему нельзя доверять, потому что flakiness компаундится. Шардинг чинит первое. Детекция и карантин — а не ретраи — чинят второе.
</Hook>

<Crux>Шардь по времени теста на N раннеров; детектируй flake на ретрае и уводи с гейта — не делай глобальный ретрай политикой.</Crux>

<Explanation>

## Шардинг: превратить 40 серийных минут в total/N

У сьюта, который крутится серийно, wall-clock равен его суммарному CPU-времени. **Шардинг** разбивает тесты на N параллельных раннеров — CI-матрицу — так что каждый раннер выполняет лишь подмножество, и wall-clock падает примерно до `total / N + overhead`. Раннер даёт тебе флаг среза; CI даёт матрицу, которая запускает N копий джобы, каждую со своим индексом среза.

```yaml
# GitHub Actions: матрица разворачивает одну джобу в N параллельных шардов
jobs:
  test:
    strategy:
      fail-fast: false           # падение одного шарда не должно отменять остальные
      matrix:
        shard: [1, 2, 3, 4, 5, 6, 7, 8]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci
      - run: npx playwright test --shard=${{ matrix.shard }}/8
```

Каждый шард запускает `--shard=i/8`, выполняя примерно одну восьмую тестов. Затем финальная джоба **мерджит пошардовые отчёты** в один результат — у тебя не восемь отдельных вердиктов pass/fail, а один. Механика такая: разворачиваешь по индексу среза, гоняешь подмножество, собираешь частичные отчёты, мерджишь.

Ловушка в том, *как* раннер выбирает подмножество каждого шарда. Наивное разбиение — по числу файлов или по алфавиту: поделить 600 файлов на 8 групп по 75. Выглядит сбалансированно, но это не так, потому что тесты не равны по длительности: медленные интеграционные тесты кучкуются, один шард наследует большинство из них, и этот шард становится **стрэглером** (отстающим). Wall-clock ограничен *самым медленным* шардом, а не средним, так что если шард 3 идёт 18 минут, пока остальные семь финишируют за 6, твой сьют занимает 18 минут, и семь раннеров простаивали. Ты купил параллелизм и получил одну быструю полосу плюс семь пустых.

```bash
# Фикс: баланс по ВРЕМЕНИ, а не по числу файлов.
# Записывай длительности тестов из прошлых прогонов, затем разбивай так,
# чтобы каждый шард нёс примерно равное суммарное время (медленные тесты распределяются).
npx playwright test --shard=3/8   # раннер читает данные о времени для балансировки шардов
# Jest: --shard плюс time-aware sequencer; многие CI-инструменты (Knapsack,
# встроенный split) делают то же — передавай длительности из прошлого прогона.
```

## Убывающая отдача: оверхед задаёт пол

Шардинг не бесплатен в расчёте на раннер. Каждый шард платит **фиксированную цену старта** — checkout, установка зависимостей, прогрев кеша — до того, как выполнит хоть один тест. Назовём этот оверхед `c`. Wall-clock — это `total / N + c`. Поднимая N, ты ужимаешь `total / N` к нулю, но `c` не двигается, так что за некой точкой ты добавляешь раннеры, каждый из которых тратит на старт больше времени, чем на тесты. Восемь раннеров на 40-минутном сьюте с двухминутным оверхедом дают `5 + 2 = 7` минут; тридцать два раннера дают `1.25 + 2 = 3.25` минуты за вчетверо больше машин — а счёт растёт с N. Правило большого пальца: бери N там, где `total / N` всё ещё кратно нескольким `c`. Дальше ты платишь за оверхед, а не за скорость.

<FlowDiagram
  label="Шардированный тест-пайплайн с гейтом обработки flake. Один тест-сьют разворачивается на N параллельных раннеров, каждый гоняет сбалансированное по времени подмножество, выбранное так, чтобы каждый шард нёс примерно равную суммарную длительность; самый медленный шард задаёт wall-clock. Пошардовые отчёты мерджатся в один вердикт. На падении раннер ретраит этот тест один раз на том же коммите чисто чтобы классифицировать его: прошёл на ретрае — значит flaky, и его уводят в негейтящую полосу карантина, заводят тикет на починку, а гейт мерджа остаётся зелёным; упал снова — это реальное падение и блокирует мердж. Полоса карантина записывает результаты без гейтинга, так что flake перестают держать мерджи, пока чинится баг детерминизма."
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "suite", label: "тест-сьют", sub: "600 тестов" },
    { id: "shard", label: "шардинг по времени", sub: "N раннеров, мердж отчётов" },
    { id: "fail", label: "при падении", sub: "ретрай один раз для детекции" },
    { id: "flaky", label: "flaky?", sub: "прошёл на ретрае" },
    { id: "quar", label: "карантин", sub: "негейтящий + тикет" },
    { id: "real", label: "реальное падение", sub: "блок мерджа" },
  ]}
  edges={[
    { from: "suite", to: "shard", label: "разбить + смердж" },
    { from: "shard", to: "fail", label: "тест красный" },
    { from: "fail", to: "flaky", label: "классифицировать" },
    { from: "flaky", to: "quar", label: "да" },
    { from: "flaky", to: "real", label: "нет" },
  ]}
  caption="Шардинг (слева) режет wall-clock до total/N, гоняя сбалансированные по времени подмножества параллельно и мердж отчётов. Гейт flake (справа) одним ретраем классифицирует красный тест: прошёл на ретрае — в негейтящий карантин с тикетом; упал снова — блокирует мердж. Ретрай — сигнал детекции, не фикс."
/>

## Flaky-тесты: математика, которая держит сьют красным

Flaky-тест проходит часть запусков и падает в части на *том же* коммите — скажем, зелёный в 99% прогонов. Один такой — досада. Теперь масштабируй. Исходы тестов примерно независимы, так что вероятность того, что весь сьют зелёный, — это произведение вероятностей прохождения каждого теста: `P(green) = (1 - flake)^N`. Подставь 1% flake на 300 тестов: `0.99^300 ≈ 0.05`. Сьют зелёный примерно в **5% случаев** — он красный в **95% прогонов** по причинам, не имеющим отношения к ревьюимому изменению.

Это и есть убийца на масштабе, и урон сначала социальный, потом технический. Когда сьют красный почти всегда на зелёной кодовой базе, разработчики учатся, что красное ничего не значит. Они перестают читать падения, слепо ретраят до зелёного или мерджат на чутьё. И в тот момент, когда красное перестаёт быть сигналом, *настоящее* падение проскальзывает в шуме — никто не смотрит дважды на ещё одну красную сборку. Сьют стал игровым автоматом.

## Детектируй, карантинь, чини — а не слепо ретрай

В сеньорском плейбуке три шага по порядку:

1. **Детектируй автоматически.** Перепрогони падение один раз на *том же коммите*. Тест, который падает, а затем проходит без изменения кода, по определению flaky — пометь его и веди flake-rate по тесту со временем. Ретрай здесь — *классификатор*, а не лекарство: его единственная работа — отличить flaky от реального.
2. **Карантинь.** Убери известно-flaky тесты с **блокирующего пути** — гоняй их в отдельной негейтящей полосе, которая всё ещё записывает результаты, так что они перестают валить мердж, пока ты за ними наблюдаешь. Гейт сразу снова начинает что-то значить.
3. **Чини корневую причину.** У flaky-теста есть баг детерминизма — допущение о тайминге, общий фикстур, неотожиданный промис, зависимость от порядка. Почини это, докажи стабильность, верни тест на гейт.

Антипаттерн — делать **глобальный авто-ретрай** (`retries: 3` на весь сьют) постоянной политикой. Выглядит как фикс, потому что красное уходит, но делает три плохие вещи: прячет flakiness вместо того, чтобы вынести её на починку, замедляет каждую сборку перепрогоном, и — то, что кусается, — маскирует тесты, которые flaky *именно потому, что ловят настоящий перемежающийся баг*. Ретрай, превращающий красное настоящей гонки в зелёное со второй попытки, выбрасывает ровно тот сигнал, что был нужен. Ретрай хорош как механизм *детекции*; он яд как постоянная политика *гейта*.

<Inset kind="why" lang="ru">
Почему крошечный flake-rate на тест делает большой сьют красным почти всегда, и почему глобальный ретрай — неверный фикс? Потому что независимые вероятности flake компаундятся мультипликативно по сьюту: `P(all pass) = (1 - p)^N`, так что даже `p = 1%` на `N = 300` тестов даёт около 5% шанса, что весь сьют зелёный — он красный примерно в 95% прогонов вообще без изменения кода. Глобальный авто-ретрай замазывает симптом, но замедляет каждую сборку перепрогоном, и хуже — маскирует тесты, которые flaky именно потому, что ловят НАСТОЯЩИЙ перемежающийся баг; ретрай, переворачивающий такой тест из красного в зелёное, выбрасывает единственный нужный сигнал. Фикс — детектировать-и-карантинить flaky-тесты с гейта и чинить их детерминизм, чтобы гейт оставался доверенным и настоящее падение не пряталось в рутинном красном.
</Inset>

<TradeoffMatrix
  id="ci-cd-06-scaling-ci-03-test-sharding-and-flaky-quarantine-tradeoff-1"
  lessonSlug="03-test-sharding-and-flaky-quarantine"
  lang="ru"
  prompt="Сьют из 600 тестов красный примерно в 30% случаев из-за горстки flaky интеграционных тестов и медленный, потому что крутится серийно. Как сделать его и быстрым, и доверенным?"
  options={[
    { name: "Шардь по времени каждого теста на N раннеров (мердж отчётов), авто-детектируй flake ретраем падения один раз, карантинь известно-flaky тесты в негейтящую полосу и чини их детерминизм", summary: "Шардинг + детект + карантин + фикс.", correct: true, justification: "Это лечит оба режима отказа у источника. Сбалансированный по времени шардинг режет wall-clock до ~total/N, ровно размазывая медленные тесты, чтобы ни один шард не был стрэглером. Авто-детекция использует один ретрай на том же коммите чисто чтобы классифицировать красное как flaky-или-реальное. Карантин уводит flaky-тесты с блокирующего пути, так что гейт снова доверенный, пока результаты всё ещё записываются, а тикеты на починку чинят баг детерминизма, чтобы тесты вернулись на гейт. Гейт продолжает что-то значить, и настоящее падение не спрячется в рутинном красном. Это сеньорский дефолт." },
    { name: "Добавить глобальный retries: 3 на весь сьют", summary: "Прячет flakiness, маскирует реальные баги.", justification: "Глобальный ретрай заставляет красное исчезнуть, потому и соблазнителен, но делает три плохие вещи: прячет flakiness вместо вынесения на починку, замедляет каждую сборку перепрогоном, и — то, что уезжает инцидентами, — маскирует тест, который flaky потому, что ловит настоящий перемежающийся баг, переворачивая его истинное красное в зелёное со второй попытки. Ретрай — нормальный сигнал детекции; как постоянная политика гейта он выбрасывает нужный сигнал. Это ровно антипаттерн из инцидента." },
    { name: "Разбить тесты по алфавиту на 20 раннеров", summary: "Несбалансированные шарды + игнор flakiness.", justification: "Алфавитное (или по числу файлов) разбиение игнорирует, что тесты разнятся по длительности: медленные интеграционные тесты кучкуются в один шард, который становится стрэглером, и поскольку wall-clock ограничен самым медленным шардом, остальные 19 простаивают, пока один идёт долго. Ты заплатил за 20 раннеров и получил одну быструю полосу. И это ничего не делает с flakiness — сьют всё ещё красный треть времени. Неверно по обеим осям." },
    { name: "Удалить flaky-тесты", summary: "Выбрасывает покрытие и сигнал.", justification: "Удаление flaky-тестов убирает красное, но flaky-тест часто flaky потому, что прогоняет реальную конкурентность или интеграцию — ровно тот код, что больше всего стоит тестировать. Удаляя его, ты выбрасываешь покрытие и, как показал инцидент, можешь удалить тест, ловивший настоящий перемежающийся баг. Flakiness — это баг детерминизма для починки, а не тест для выбрасывания; карантин держит тест записывающим результаты, пока ты его чинишь." },
  ]}
/>

<Quiz
  id="ci-cd-06-scaling-ci-03-test-sharding-and-flaky-quarantine-quiz-1"
  lessonSlug="03-test-sharding-and-flaky-quarantine"
  lang="ru"
  question="Ты шардишь сьют на 8 раннеров, разбив тест-файлы по алфавиту на 8 равных групп. Wall-clock едва улучшается против серийного. Почему и что чинит это?"
  choices={[
    { label: "Тесты разнятся по длительности, так что медленные скучковались в один шард, ставший стрэглером; wall-clock ограничен самым медленным шардом, а не средним. Балансируй по данным о времени каждого теста.", correct: true },
    { label: "8 раннеров слишком мало — добавляй ещё, пока wall-clock не упадёт", misconception: "Добавление раннеров к несбалансированному разбиению не помогает: шард-стрэглер всё ещё держит медленный кластер и всё ещё гейтит wall-clock. И за некой точкой фиксированный оверхед старта каждого раннера доминирует, так что больше раннеров стоит дороже без ускорения. Проблема не в N, а в том, что разбиение игнорирует длительность — чини баланс, а не число." },
    { label: "Алфавитный порядок детерминирован, что мешает параллелизму", misconception: "Детерминизм порядка не имеет отношения к параллелизму — шарды всё равно идут конкурентно. Дело в балансе: равное ЧИСЛО файлов — не равное ВРЕМЯ, так что один шард наследует медленные интеграционные тесты и идёт долго, пока остальные простаивают. Разбивай по записанному времени каждого теста, чтобы шард нёс равную суммарную длительность." },
    { label: "Мердж пошардовых отчётов сериализует раннеры", misconception: "Мердж отчётов — дешёвый финальный шаг и не сериализует выполнение тестов — шарды шли параллельно, и в конце комбинируются лишь их результаты. Wall-clock высок, потому что шарды были несбалансированы по времени, оставив один стрэглер; разбиение по времени — это фикс." },
  ]}
/>

<Quiz
  id="ci-cd-06-scaling-ci-03-test-sharding-and-flaky-quarantine-quiz-2"
  lessonSlug="03-test-sharding-and-flaky-quarantine"
  lang="ru"
  question="В твоём большом сьюте есть тест, flaky примерно в 1% случаев. Команда добавляет глобальную политику retries: 3, и красное уходит. Что не так с этим как с постоянным фиксом?"
  choices={[
    { label: "Это прячет flakiness вместо починки, замедляет каждую сборку перепрогоном и может замаскировать тест, который flaky потому, что ловит НАСТОЯЩИЙ перемежающийся баг — перевернув истинное красное в зелёное. Вместо этого детектируй, карантинь и чини детерминизм.", correct: true },
    { label: "Ничего — ретраи это стандартный способ обработки flakiness на масштабе", misconception: "Глобальный ретрай — это антипаттерн, а не стандарт. Он замазывает симптом, оставляя баг детерминизма на месте, облагает налогом каждую сборку перепрогонами и — опасная часть — делает настоящее перемежающееся падение зелёным на ретрае, уводя баг в прод. Ретрай уместен как одноразовый сигнал детекции, классифицирующий красное как flaky-или-реальное, а не как постоянная политика гейта." },
    { label: "Трёх ретраев мало; подними до retries: 10, чтобы полностью убрать красное", misconception: "Поднятие числа ретраев делает маскировку хуже, не лучше: чем больше раз перепрогоняешь, тем вероятнее настоящий перемежающийся баг в итоге «пройдёт» и проскользнёт, и тем медленнее CI. Фикс не в большем числе ретраев — а в детекции flake, карантине его с гейта и починке бага детерминизма, который его вызывает." },
    { label: "Ретраи помогают, только если ты потом удалишь flaky-тест", misconception: "Удаление теста выбрасывает покрытие ровно того кода конкурентности/интеграции, что больше всего стоит тестировать, и может удалить тест, ловивший настоящий баг. Flaky-тест надо карантинить (негейтящий, всё ещё записывающий) и чинить его баг детерминизма, затем вернуть на гейт — а не ретраить-и-удалять." },
  ]}
/>

</Explanation>

<KeyTakeaway>Шардь по времени теста (wall-clock ≈ total/N; гейтит медленный шард, баланс важнее числа). Flakiness компаундится: 1% × 300 ≈ 5% зелёных. Детектируй flake ретраем, карантинь с гейта, чини детерминизм; ретрай не политика.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="ci-cd-06-scaling-ci-03-test-sharding-and-flaky-quarantine-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни шардинг тестов: механику, почему наивное разбиение проваливается и где наступает убывающая отдача.", a: "Шардинг разбивает тест-сьют на N параллельных раннеров — CI-матрицу — так что каждый раннер выполняет лишь подмножество (--shard=i/N), и wall-clock падает с серийного total до примерно total/N + overhead. Механика такая: развернуть по индексу среза (каждый шард получает свой индекс), прогнать подмножество, собрать пошардовые отчёты и смерджить их в один вердикт pass/fail — на выходе один результат, а не N. Наивное разбиение (по числу файлов или по алфавиту) проваливается, потому что тесты не равны по длительности: медленные интеграционные тесты кучкуются, один шард наследует большинство и становится стрэглером, а wall-clock ограничен самым медленным шардом, а не средним — так что семь раннеров могут финишировать за 6 минут, пока один идёт 18, и сьют занимает 18. Фикс — балансировать по ВРЕМЕНИ: записать длительности каждого теста из прошлых прогонов и разбить так, чтобы каждый шард нёс примерно равное суммарное время. Убывающая отдача наступает, потому что каждый шард платит фиксированную цену старта c (checkout, install, прогрев кеша): wall-clock — это total/N + c, так что с ростом N total/N ужимается к нулю, но c не двигается. За точкой, где total/N лишь небольшое кратное c, ты добавляешь раннеры, которые в основном стартуют, а не тестируют, и счёт растёт с N — бери N там, где total/N всё ещё кратно нескольким c." },
    { q: "Почему крошечный flake-rate делает большой сьют красным почти всегда, и каков плейбук детект-карантин-фикс против слепого ретрая?", a: "Исходы тестов примерно независимы, так что вероятность прохождения всего сьюта — это произведение вероятностей прохождения каждого теста: P(green) = (1 - flake)^N. 1% flake-rate на 300 тестов даёт 0.99^300 ≈ 0.05 — сьют зелёный лишь примерно в 5% прогонов и красный в ~95% случаев по причинам, не связанным с изменением. Урон сначала социальный, потом технический: когда красное ничего не значит, разработчики перестают читать падения и слепо ретраят или мерджат на чутьё, так что настоящее падение проскальзывает в шуме. Сеньорский плейбук — три шага: (1) ДЕТЕКТИРУЙ автоматически — перепрогони падение один раз на том же коммите; тест, который затем проходит без изменения кода, по определению flaky, так что пометь его и веди flake-rate по тесту (ретрай здесь — классификатор, не лекарство). (2) КАРАНТИНЬ — убери известно-flaky тесты в негейтящую полосу, которая всё ещё записывает результаты, так что они перестают валить мердж, и гейт снова доверенный. (3) ЧИНИ баг детерминизма (допущение о тайминге, общий фикстур, неотожиданный промис, зависимость от порядка), докажи стабильность и верни тест на гейт. Антипаттерн — делать глобальный авто-ретрай (retries: 3) постоянной политикой: он прячет flakiness вместо починки, замедляет каждую сборку перепрогоном и маскирует тест, который flaky потому, что ловит настоящий перемежающийся баг — переворачивая его истинное красное в зелёное и уводя баг в прод. Ретрай — нормальный сигнал детекции; он яд как постоянная политика гейта." },
  ]}
/>

<Recap lang="ru">
Тест-сьют ломается на масштабе двумя способами, и у каждого свой фикс. Он становится слишком медленным, потому что крутится серийно: шардинг разбивает тесты на N параллельных раннеров (CI-матрицу), каждый гоняет подмножество через --shard=i/N, после чего финальная джоба мерджит пошардовые отчёты в один вердикт, так что wall-clock падает с серийного total до примерно total/N + overhead. Ловушка — наивное разбиение по числу файлов или по алфавиту: тесты разнятся по длительности, медленные кучкуются в один шард-стрэглер, и поскольку wall-clock ограничен самым медленным шардом, балансируй по записанному времени каждого теста. Отдача убывает, потому что каждый шард платит фиксированную цену старта c, так что за точкой, где total/N — небольшое кратное c, ты платишь за оверхед, а не за скорость. Второй отказ — доверие: flakiness компаундится, потому что P(green) = (1 - flake)^N, так что 1% flake на 300 тестов оставляет сьют зелёным лишь в ~5% прогонов — красным почти всегда, что приучает разработчиков игнорировать красное и пускает настоящее падение проскользнуть. Сеньорский плейбук — детект (ретрай один раз на том же коммите, чтобы классифицировать flaky-vs-реальное), карантин (увести flaky-тесты в негейтящую полосу, которая всё ещё записывает результаты, чтобы гейт оставался доверенным) и починка бага детерминизма, затем возврат теста на гейт. Глобальный авто-ретрай как постоянная политика — антипаттерн: он прячет flakiness, замедляет каждую сборку и маскирует flaky-тест, который на деле ловит настоящий перемежающийся баг; ретрай — сигнал детекции, не политика гейта.
</Recap>
