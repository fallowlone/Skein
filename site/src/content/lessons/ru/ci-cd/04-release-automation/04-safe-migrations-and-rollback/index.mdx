---
concepts:
- expand-contract
- backward-compatible-migrations
- migrate-before-after-deploy
- online-ddl
- forward-fix-vs-rollback
deepensInto: []
estMin: 19
lang: ru
lessonType: topic
level: senior
order: 4
prereqs: []
slug: "04-safe-migrations-and-rollback"
sources:
- https://martinfowler.com/articles/evodb.html
- https://docs.github.com/en/actions/deployment
spiral: []
status: ready
summary: 'Код откатывается за секунды; база — нет. Деструктивная миграция, уехавшая вместе с деплоем, делает откат невозможным. Разбей любое изменение схемы на expand/contract по релизам, чтобы старый и новый код оба работали: аддитивное до деплоя, деструктивное после.'
title: 'Безопасные миграции схемы и откат'
track: ci-cd
unit: 04-release-automation
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Деплой, который положил прод, выглядел чистым. Один релиз привёз код, переставший использовать `users.legacy_name`, и в той же миграции — `ALTER TABLE users DROP COLUMN legacy_name`. Он стал зелёным. Через три часа латентный баг в несвязанной части того релиза вынудил откат — дежурный нажал кнопку, спасавшую нас десяток раз, переразвернув предыдущий артефакт. Каждый запрос тут же отдал 500. Старый артефакт всё ещё выполнял `SELECT ... legacy_name FROM users`, а колонки уже не было. Хуже того, накат вперёд тоже не вернул бы её: данные в `legacy_name` были дропнуты, а не заархивированы. Кнопка отката — вся наша история про безопасность — не сделала ничего, потому что единственное, что она не может переразвернуть, — это база. Этот урок о том, почему так происходит, и о дисциплине, которая держит откат настоящим: expand/contract.
</Hook>

<Crux>Код откатывается мгновенно; деструктивная миграция — нет. Разбей изменение схемы на expand/contract, чтобы старый и новый код работали.</Crux>

<Explanation>

## Почему миграция ломает откат

Откат работает на коде, потому что деплой — это просто подмена одного неизменяемого артефакта предыдущим: старый образ контейнера всё ещё существует, ты возвращаешь на него трафик, готово за секунды. У базы нет предыдущего артефакта. Это одна изменяемая stateful-сущность, и миграция мутирует её на месте. Как только `DROP COLUMN legacy_name` выполнился, «вернуться назад» не имеет смысла: колонка и её данные ушли, и даже если ты воссоздашь колонку, строки будут пустыми. Деплой и изменение схемы связаны в релизе, но обратима только одна половина этой пары. Поэтому «просто откати деплой» молча предполагает, что старый код всё ещё может прочитать схему, которую найдёт, — а деструктивное изменение, уехавшее с новым кодом, ломает ровно это предположение.

Это острее, чем кажется, из-за того, как раскатываются современные релизы. Rolling-, canary- или blue-green-деплой (разобранный в прошлом уроке) гоняет **две версии твоего кода одновременно** против **одной** базы. Есть окно — иногда минуты, иногда весь прогрев канарейки, — где vN-1 и vN обе обслуживают живой трафик. Если миграция vN изменила схему так, что vN-1 этого не переносит, ты сломал ещё работающую старую версию *до того, как вообще нажал откат*. Схема обязана удовлетворять каждую живую версию одновременно.

## Expand/contract: никогда не меняй колонку за один шаг

Дисциплина, которая делает это безопасным, — **параллельное изменение**, обычно называемое expand/contract. Ты никогда не мутируешь колонку деструктивно в одной миграции. Ты разбиваешь изменение по релизам так, чтобы в каждый отдельный момент любая работающая версия кода могла читать и писать схему такой, какая она сейчас.

```sql
-- РЕЛИЗ 1, шаг EXPAND: только аддитивно, выполняется ДО деплоя нового кода.
-- Старый код игнорирует новую колонку; новый код может начать её использовать. Обратно совместимо.
ALTER TABLE users ADD COLUMN full_name text;        -- nullable, без переписывания
CREATE INDEX CONCURRENTLY idx_users_full_name ON users (full_name);
```

```ts
// РЕЛИЗ 1, код приложения: DUAL-WRITE — пиши и старое, и новое на каждой мутации,
// и читай пока из старой колонки. Бэкфилл существующих строк батчами, не одним UPDATE.
async function setName(id: string, name: string) {
  await db.query(
    `UPDATE users SET legacy_name = $2, full_name = $2 WHERE id = $1`,
    [id, name],
  );
}
// задача бэкфилла: копируй существующие данные ограниченными батчами (например, 1k–10k строк за цикл)
//   UPDATE users SET full_name = legacy_name
//   WHERE full_name IS NULL AND id IN (<следующий батч>);   -- повторяй, пока не кончатся
```

```sql
-- РЕЛИЗ 2: переключи ЧТЕНИЯ на full_name (изменение только кода, без изменения схемы).
-- РЕЛИЗ 3, шаг CONTRACT: дропни старую колонку — ТОЛЬКО после того, как R2 полностью
-- раскатан и проверен, когда legacy_name больше никто не читает. Это единственный деструктивный шаг.
ALTER TABLE users DROP COLUMN legacy_name;
```

Переименование — каноничный случай, и это **не** один `RENAME COLUMN` — это добавь новую колонку + бэкфилл + dual-write + переключи чтения + дропни старую, растянутое на **три или более релизов**. Ни в один момент работающая версия не встречает схему, которую не умеет обработать, поэтому предыдущий артефакт всегда всё ещё запускается, и откат остаётся настоящим на всём пути.

## Порядок: аддитивное до деплоя, деструктивное после

Expand/contract диктует, *когда* каждая миграция выполняется относительно деплоя, и ошибка тут заново порождает исходный инцидент. **Аддитивная** миграция (expand) обязана выполниться **до** деплоя нового кода — колонка должна существовать в тот миг, как новый код стартует, иначе новый код падает на отсутствующей колонке. **Деструктивная** миграция (contract) обязана выполниться **после** того, как новый код полностью раскатан и ты уверен, что откатываться не будешь, — к этому моменту старую колонку никто не читает. Запуск деструктивного изменения вместе с деплоем — ровно то, что делает откат невозможным, потому что в момент, когда ты откатываешь деплой, старый код встречает схему, которую оставил после себя новый код.

<Inset kind="why" lang="ru">
Почему нельзя просто положить миграцию и её `down`-откат в один релиз и довериться `down`-скрипту, что он всё отменит? Потому что обратимость на бумаге — это не обратимость данных и не обратимость живого парка смешанных версий. `down`, который «восстанавливает» дропнутую колонку, может воссоздать пустую колонку, но не данные, которые уже удалены, — этих данных нет. А в rolling- или canary-деплое старый и новый код работают одновременно против одной базы, так что деструктивный `up` ломает ещё работающую старую версию *немедленно*, до того как `down` вообще сможет сработать. `down`-скрипт защищает тебя от провалившейся миграции; он не делает ничего против успешной миграции, чью схему предыдущий код не может прочитать. Только аддитивная последовательность expand/contract — где каждая версия работает со схемой на каждом шаге — реально держит откат безопасным.
</Inset>

## Online DDL: сама миграция может положить таблицу

Даже аддитивная миграция может вызвать простой, если DDL блокирует таблицу. В Postgres `ADD COLUMN` с неволатильным дефолтом быстрый и безопасный, но обычный `CREATE INDEX` берёт блокировку, останавливающую записи на всё время построения, — используй `CREATE INDEX CONCURRENTLY`, чтобы записи продолжали течь. Долгие `ALTER` берут блокировку `ACCESS EXCLUSIVE` и за очередью активных запросов могут застопорить всю таблицу под нагрузкой. А бэкфилл обязан быть **батчевым** — один гигантский `UPDATE users SET full_name = legacy_name` переписывает каждую строку в одной транзакции, держа блокировки, раздувая таблицу и работая достаточно долго, чтобы заблокировать autovacuum. Гоняй его ограниченными батчами в несколько тысяч строк, коммитя между ними.

## Накат-вперёд против отката

Поскольку база часто *не может* вернуться назад, сеньорский дефолт для плохого релиза, привёзшего изменение схемы, — нередко **накат-вперёд** (отгрузить исправленную версию вперёд), а не откат. Но это вариант, который ты держишь открытым только тем, что с самого начала делаешь каждую миграцию обратно совместимой: если каждое изменение схемы — expand/contract, предыдущий артефакт всё ещё запускается против новой схемы, так что откат *тоже* остаётся настоящим выбором, а не кнопкой, кладущей прод в 500. Обратно совместимые миграции не заменяют откат накатом-вперёд; они делают оба безопасными, чтобы ты выбирал по инциденту.

<TradeoffMatrix
  id="ci-cd-04-release-automation-04-safe-migrations-and-rollback-tradeoff-1"
  lessonSlug="04-safe-migrations-and-rollback"
  lang="ru"
  prompt="Тебе нужно переименовать активно используемую колонку с нулевым простоем и возможностью откатить деплой в любой момент. Какой подход?"
  options={[
    { name: "Expand/contract по релизам: добавь новую колонку + бэкфилл + dual-write, переключи чтения в более позднем релизе, дропни старую колонку только после того, как новый код проверен — аддитивная миграция до деплоя, деструктивная после полной раскатки", summary: "Параллельное изменение.", correct: true, justification: "Это единственный подход, где каждая работающая версия встречает схему, которую может прочитать, на каждом шаге. Expand (добавь nullable-колонку, индекс CONCURRENTLY) выполняется до нового кода, так что новый код находит колонку; приложение делает dual-write и бэкфилл батчами; более поздний релиз переключает чтения; деструктивный дроп выполняется только после того, как новая версия полностью раскатана и проверена. Поскольку ничего не уничтожается, пока старый код не исчез, предыдущий артефакт всегда всё ещё запускается — откат остаётся настоящим на всём пути. Это стоит 3+ релизов, и эта цена И ЕСТЬ безопасность." },
    { name: "ALTER TABLE users RENAME COLUMN в миграции деплоя", summary: "Связывает схему с деплоем.", justification: "RENAME атомарен для базы, но катастрофичен для парка смешанных версий: в тот миг, как он выполнится, старая версия (всё ещё обслуживающая трафик в окне rolling/canary или после отката) запрашивает имя колонки, которого больше нет, и отдаёт 500. Изменение схемы связано с деплоем, так что откат деплоя не откатывает переименование — это тот же инцидент, просто записанный иначе." },
    { name: "Дропни и воссоздай колонку в одной миграции, с down-скриптом для реверса", summary: "down не восстановит данные.", justification: "down-скрипт может воссоздать колонку, но не данные, которые up уже удалил, — обратимость на бумаге, не данных. А в живом деплое смешанных версий деструктивный up ломает старую версию немедленно, до того как сработает любой down. down защищает от провалившейся миграции, но никогда от успешной, чью схему предыдущий код не может прочитать." },
    { name: "Возьми окно обслуживания и переименуй напрямую, остановив трафик", summary: "Нарушает нулевой простой.", justification: "Остановка трафика действительно обходит проблему смешанных версий, но требованием был нулевой простой — окно обслуживания по определению есть простой, и для активно используемой колонки оно может быть долгим. Expand/contract достигает переименования вообще без окна, и именно поэтому это сеньорский дефолт для онлайн-изменения схемы." },
  ]}
/>

<Quiz
  id="ci-cd-04-release-automation-04-safe-migrations-and-rollback-quiz-1"
  lessonSlug="04-safe-migrations-and-rollback"
  lang="ru"
  question="Релиз привёз `DROP COLUMN legacy_name` вместе с кодом, переставшим её читать. Баг вынуждает откат к предыдущему артефакту. Почему код может откатиться, а деструктивная миграция — нет?"
  choices={[
    { label: "Деплой просто переуказывает на старый неизменяемый артефакт, но данные колонки уже ушли, а старый код всё ещё делает SELECT legacy_name — у базы нет предыдущего артефакта для переразвёртывания", correct: true },
    { label: "Откат восстанавливает и код, и схему атомарно, так что колонка возвращается с данными", misconception: "Откат лишь переуказывает трафик на предыдущий артефакт кода — он ничего не делает с базой, которая является изменяемым общим состоянием без «предыдущего артефакта». DROP уже уничтожил колонку и её данные; переразвёртывание старого кода значит лишь, что этот код теперь делает SELECT колонки, которой больше нет, и отдаёт 500. Схема и деплой связаны в релизе, но обратима только половина-деплой." },
    { label: "Инструмент миграций автоматически прогоняет down-скрипт во время отката деплоя", misconception: "Откат деплоя переразвёртывает старый артефакт; он не вызывает down-скрипт твоего инструмента миграций — это разные системы. Даже если бы вызывал, down для DROP может воссоздать пустую колонку, но не вернуть удалённые данные. Деструктивное изменение необратимо независимо от down-скрипта." },
    { label: "Старый код читает с реплики, поэтому он не заметил дропнутую колонку", misconception: "Реплики получают тот же DDL через репликацию — колонка дропается и на репликах тоже. Нет версии базы, где legacy_name всё ещё существует после выполнения миграции. Старый код отдаёт 500 и на primary, и на реплике одинаково." },
  ]}
/>

<Quiz
  id="ci-cd-04-release-automation-04-safe-migrations-and-rollback-quiz-2"
  lessonSlug="04-safe-migrations-and-rollback"
  lang="ru"
  question="Тебе нужно безопасно переименовать колонку по релизам через expand/contract. Какова последовательность и каков порядок аддитивной против деструктивной миграции относительно деплоев?"
  choices={[
    { label: "Добавь новую колонку + бэкфилл + dual-write, переключи чтения в более позднем релизе, затем дропни старую колонку — аддитивная миграция выполняется ДО деплоя нового кода, деструктивная — ПОСЛЕ полной раскатки", correct: true },
    { label: "Переименуй в одной миграции, но запусти её после деплоя, чтобы новый код был уже живой", misconception: "Запуск RENAME после деплоя всё равно мутирует одну колонку деструктивно: в момент его выполнения любая старая версия, всё ещё в окне rolling/canary, — или любой будущий откат — встречает отсутствующее имя колонки. Поздний порядок одного деструктивного переименования не делает его обратно совместимым; только разбиение на add + backfill + dual-write + switch + drop делает." },
    { label: "Добавь новую колонку ПОСЛЕ деплоя нового кода, чтобы миграция и код уехали вместе", misconception: "Если аддитивная миграция выполняется после нового кода, новый код стартует до того, как его колонка существует, и падает на отсутствующей колонке. Аддитивный expand обязан выполниться ДО нового кода, чтобы колонка присутствовала в тот миг, как новый код её читает. Только деструктивный contract выполняется после раскатки." },
    { label: "Сделай дроп в том же релизе, что и переключение чтений, чтобы минимизировать число релизов", misconception: "Дроп старой колонки в том же релизе, что переключает чтения, убирает твою цель отката: если откатишь этот релиз, чтения вернутся к старой колонке, которую ты только что дропнул. Contract (дроп) обязан быть ОТДЕЛЬНЫМ, более поздним релизом, выполняемым только после того, как переключение чтений полностью раскатано и проверено, — этот зазор и держит откат безопасным." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Последовательность expand/contract для переименования колонки с откат-безопасностью на каждом шаге. Релиз 1 expand: добавь новую nullable-колонку (и индекс CONCURRENTLY) ДО деплоя нового кода, чтобы колонка существовала, когда новый код стартует — старый код её игнорирует, обратно совместимо. Релиз 1 код: dual-write и старой, и новой колонки на каждой мутации и бэкфилл существующих строк ограниченными батчами. Релиз 2: переключи чтения на новую колонку, изменение только кода без изменения схемы. Релиз 3 contract: дропни старую колонку, выполняй только ПОСЛЕ того, как Релиз 2 полностью раскатан и проверен, когда старую колонку никто не читает — единственный деструктивный шаг. На каждом переходе предыдущий артефакт всё ещё работает против схемы, так что откат остаётся настоящим всё время."
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "expand", label: "R1 EXPAND", sub: "колонка, до деплоя" },
    { id: "dual", label: "R1 dual-write", sub: "+ бэкфилл батчами" },
    { id: "switch", label: "R2 переключи чтения", sub: "только код" },
    { id: "contract", label: "R3 CONTRACT", sub: "дроп старой, после проверки" },
    { id: "safe", label: "откат-безопасно", sub: "на каждом шаге" },
  ]}
  edges={[
    { from: "expand", to: "dual", label: "аддитивно" },
    { from: "dual", to: "switch", label: "данные готовы" },
    { from: "switch", to: "contract", label: "чтения уехали" },
    { from: "contract", to: "safe", label: "деструктив последним" },
  ]}
  caption="Переименование = 3+ релизов: expand (аддитивно, до деплоя) → dual-write + бэкфилл → переключи чтения → contract (деструктивно, после полной раскатки). Деструктивный шаг последний и изолированный, так что каждая прежняя версия всё ещё работает и откат никогда не отдаёт 500."
/>

<KeyTakeaway>Код откатывается за секунды; дропнутая колонка — нет. Применяй expand/contract: аддитивная миграция до деплоя, dual-write + бэкфилл батчами, переключи чтения, дропни старую после проверки. Переименование = 3+ релизов.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="ci-cd-04-release-automation-04-safe-migrations-and-rollback-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, почему деструктивная миграция ломает откат, особенно при rolling/canary/blue-green деплое, и пройди последовательность expand/contract и порядок миграций, держащий откат безопасным.", a: "Откат работает на коде, потому что деплой просто меняет один неизменяемый артефакт на предыдущий — старый образ всё ещё существует, ты переуказываешь трафик, готово за секунды. У базы нет предыдущего артефакта: это одна изменяемая stateful-сущность, и миграция мутирует её на месте. Как только DROP COLUMN выполнился, колонка и её данные ушли, так что «вернуться назад» бессмысленно — даже воссоздание колонки оставляет строки пустыми. Деплой и изменение схемы связаны в релизе, но обратима только половина-деплой, поэтому «просто откати деплой» молча предполагает, что старый код всё ещё может прочитать найденную схему. Это острее при rolling/canary/blue-green, потому что две версии кода работают одновременно против ОДНОЙ базы; есть окно, где vN-1 и vN обе обслуживают трафик, так что деструктивная миграция vN ломает ещё работающую vN-1 ДО того, как ты вообще нажал откат. Фикс — expand/contract (параллельное изменение): никогда не мутируй колонку деструктивно за один шаг; разбей это по релизам, чтобы каждая работающая версия могла читать/писать схему такой, какая она сейчас. (1) EXPAND — добавь новую структуру аддитивно (nullable-колонка, CREATE INDEX CONCURRENTLY), выполни ДО деплоя нового кода, чтобы колонка существовала, когда новый код стартует; старый код её игнорирует, обратно совместимо. (2) Задеплой код, делающий DUAL-WRITE старого и нового, и забэкфилль существующие строки ограниченными батчами (1k–10k), не одним гигантским UPDATE. (3) Более поздний релиз переключает ЧТЕНИЯ на новую колонку. (4) CONTRACT — только после того, как новый код полностью раскатан и проверен, отдельный поздний релиз дропает старую колонку, когда её никто не читает; это единственный деструктивный шаг. Поэтому переименование — 3+ релизов, никогда не один RENAME. Порядок: аддитивное до деплоя, деструктивное после полной раскатки — запуск деструктивного изменения с деплоем ровно то, что делает откат невозможным." },
    { q: "Почему down-скрипта недостаточно, какие опасности online DDL кусают даже аддитивные миграции и когда ты делаешь накат-вперёд вместо отката?", a: "down-скрипта недостаточно, потому что обратимость на бумаге — не обратимость данных или живого парка смешанных версий: down, который «восстанавливает» дропнутую колонку, воссоздаёт пустую колонку, но не удалённые данные, а деструктивный up ломает одновременно работающую старую версию немедленно, до того как сработает любой down. down защищает от ПРОВАЛИВШЕЙСЯ миграции, но никогда от УСПЕШНОЙ, чью схему предыдущий код не может прочитать — только аддитивный expand/contract держит откат безопасным. Опасности online DDL: даже аддитивный DDL может положить таблицу — обычный CREATE INDEX блокирует записи на всё время построения, поэтому используй CREATE INDEX CONCURRENTLY; долгие ALTER берут блокировку ACCESS EXCLUSIVE и могут застопорить таблицу под нагрузкой за очередью запросов; а бэкфилл обязан быть батчевым, потому что один гигантский UPDATE переписывает каждую строку в одной транзакции, держит блокировки, раздувает таблицу и блокирует autovacuum — гоняй ограниченные батчи в несколько тысяч строк, коммитя между ними. Накат-вперёд против отката: поскольку база часто не может вернуться, сеньорский дефолт для плохого релиза, привёзшего изменение схемы, — нередко накат-вперёд (отгрузить исправленную версию вперёд), а не откат. Но ты держишь этот вариант открытым, только делая каждую миграцию обратно совместимой: если каждое изменение схемы — expand/contract, предыдущий артефакт всё ещё запускается против новой схемы, так что откат ТОЖЕ остаётся настоящим выбором, а не кнопкой, кладущей прод в 500. Обратно совместимые миграции не заменяют откат накатом-вперёд; они делают оба безопасными, чтобы ты выбирал по инциденту." },
  ]}
/>

<Recap lang="ru">
Код откатывается за секунды, потому что деплой просто меняет один неизменяемый артефакт на предыдущий; у базы нет предыдущего артефакта, так что миграцию, которая уже выполнилась, нельзя авто-отменить. Деструктивное изменение — DROP COLUMN, RENAME, смена типа — уехавшее вместе с деплоем, и есть то, что делает откат невозможным: как только колонка ушла, старый артефакт всё ещё делает SELECT её и отдаёт 500, а данные восстановить нельзя. Это острее при rolling-, canary- или blue-green-деплое, где две версии кода работают разом против одной базы, так что деструктивное изменение ломает ещё работающую старую версию до того, как ты нажал откат. Дисциплина — expand/contract (параллельное изменение): никогда не мутируй колонку за один шаг. EXPAND добавляет новую структуру аддитивно (nullable-колонка, CREATE INDEX CONCURRENTLY) и выполняется ДО деплоя нового кода; приложение затем делает dual-write старого и нового и бэкфиллит существующие строки ограниченными батчами, не одним гигантским UPDATE; более поздний релиз переключает чтения; а CONTRACT дропает старую колонку только ПОСЛЕ того, как новый код полностью раскатан и проверен. Поэтому переименование — 3+ релизов, никогда не один RENAME, а порядок — аддитивное-до-деплоя, деструктивное-после-раскатки. down-скрипта недостаточно — он может воссоздать пустую колонку, но не удалённые данные, а деструктивный up ломает живую старую версию немедленно. Опасности online DDL кусают даже аддитивные миграции: CREATE INDEX CONCURRENTLY, чтобы избежать блокировок записи, следи за блокировками ACCESS EXCLUSIVE на долгих ALTER, батчи бэкфиллов. И поскольку база часто не может вернуться, сеньорский дефолт для плохого релиза, несущего схему, — часто накат-вперёд, но ты держишь и откат, и накат-вперёд безопасными, только делая каждую миграцию обратно совместимой с самого начала.
</Recap>
