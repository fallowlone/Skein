---
concepts:
- build-once-promote-many
- immutable-artifacts
- digest-pinning
- environment-promotion
- config-per-environment
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 5
prereqs: []
slug: "05-artifact-promotion-and-environments"
sources:
- https://12factor.net/build-release-run
- https://docs.github.com/en/actions/deployment/targeting-different-environments
spiral: []
status: ready
summary: 'Собери артефакт ровно один раз и продвигай тот же неизменяемый, запиненный по digest образ из dev в staging в prod — никогда не пересобирай на каждое окружение. Пересборка из того же коммита — не тот же артефакт; конфиг инжектится в рантайме, а prod закрыт за апрувом.'
title: 'Собери один раз, продвигай везде: артефакты и окружения'
track: ci-cd
unit: 04-release-automation
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Staging был зелёным три дня. Релиз-инженер запустил деплой в prod, который — как пайплайн делал всегда — прогнал собственный `docker build` из ровно того же коммита SHA, что собрал staging. Тот же Dockerfile, тот же коммит, те же зелёные тесты. Prod лёг за девяносто секунд. Постмортем нашёл разницу: за часы между сборкой staging и сборкой prod транзитивная зависимость опубликовала новый патч-релиз, и образ prod зарезолвил его там, где staging запинил старую версию. Два образа были собраны из идентичного исходника и не были одним и тем же бинарником. «Это прошло staging» оказалось предложением про артефакт, которого больше не существовало. Второй поворот ножа случился при откате: деплой целился в `:latest`, так что откат «к предыдущему образу» вытянул `:latest`, который к тому моменту перепушили, — вовсе не предыдущий образ. Этот урок про ту единственную дисциплину, что делает оба сценария невозможными: собери артефакт один раз и продвигай ровно этот артефакт везде.
</Hook>

<Crux>Собери артефакт один раз; продвигай тот же запиненный по digest образ через окружения. Конфиг инжекти в рантайме — не пересобирай на env.</Crux>

<Explanation>

## Собери один раз, продвигай многократно

Через десять минут ты увидишь, почему инцидент из вступления был неизбежен — и почему одна строка в пайплайне делает его невозможным навсегда.

Принцип идёт из разделения build/release/run в 12-factor: **build** превращает исходник в один неизменяемый артефакт (образ контейнера, пакет), **release** связывает этот артефакт с конкретным конфигом, а **run** исполняет release. Артефакт производится **ровно один раз**, затем *те же байты* движутся через окружения — dev, потом staging, потом prod. Ты никогда не запускаешь второй `build` под другое окружение.

Причина — и есть весь смысл наличия окружения staging. Если prod пересобирает, то бинарник, который ты тестировал в staging, и бинарник, который ты везёшь в prod, — это две разные сборки, и «это прошло staging» теперь утверждение про другой артефакт. Продвижение делает гарантию буквальной: байты, которые ты тестировал, — это байты, которые ты запускаешь.

```bash
# НЕВЕРНО — каждое окружение пересобирает, поэтому каждое запускает (возможно) другой бинарник
# job staging
docker build -t app:staging .      # сборка #1
# job prod (часами позже)
docker build -t app:prod .         # сборка #2 — НЕ тот же образ, что сборка #1
```

```bash
# ВЕРНО — собери ОДИН раз, зафиксируй digest, продвигай ровно этот образ везде
# job сборки (запускается один раз)
docker build -t registry/app:$GIT_SHA .
docker push registry/app:$GIT_SHA
DIGEST=$(docker inspect --format='{{index .RepoDigests 0}}' registry/app:$GIT_SHA)
# DIGEST теперь registry/app@sha256:abc123...  — продвигай ЕГО, никогда не пересобирай
```

## Пинуй по digest, а не по тегу

Когда ты пишешь `image: registry/app:latest` в манифесте деплоя — спроси себя: что именно это такое? Ответ меняется каждый раз, когда кто-то перепушивает тег, — и именно эта изменяемость стала вторым провалом из вступительной истории.

Продвижение осмысленно, только если «тот же артефакт» однозначен. **Тег** вроде `:latest` или даже `:v1.2.3` — это *изменяемый указатель*: его можно перепушить, чтобы он указывал на другой образ, в любой момент. `:latest` — классический выстрел в ногу: два деплоя `:latest` с разницей в минуты могут запустить два разных образа, и нет записи о том, что что-то изменилось. Даже semver-тег можно форс-пушнуть поверх.

Фикс — ссылаться на артефакт по его **неизменяемому content digest** — `registry/app@sha256:...` — который является хэшем самого содержимого образа. Digest нельзя перенаправить: `sha256:abc` *и есть* тот образ, байт в байт, навсегда. Деплой digest, запиши digest в release, и данный release становится воспроизводимым и аудируемым вплоть до бита.

```yaml
# Манифест деплоя ссылается на НЕИЗМЕНЯЕМЫЙ digest, а не на плавающий тег
spec:
  containers:
    - name: app
      # НЕ image: registry/app:latest   (изменяемый — могут перепушить под тобой)
      # НЕ image: registry/app:v1.2.3    (всё ещё перепушиваемый тег)
      image: registry/app@sha256:abc123def456...   # неизменяемый: ровно этот образ, всегда
```

## Конфиг на окружение, а не артефакт на окружение

Один и тот же артефакт работает в каждом окружении; различается только **конфиг**. URL баз данных, фича-флаги, секреты и настройки тенанта инжектятся в **рантайме** — переменные окружения, примонтированные секреты, config maps — а не запекаются в образ. Это правило «config в окружении» из 12-factor, и именно оно позволяет одному артефакту обслуживать все окружения.

Запекание конфига окружения в образ ломает продвижение двумя путями: оно вынуждает пересобирать на каждое окружение (потому что образ prod должен отличаться от образа staging) и протекает секретами prod в артефакт, который лежит в registry. Тот же digest плюс конфиг на окружение — единственная форма, которая держит артефакт неизменяемым, а секреты — вне его.

<Inset kind="why" lang="ru">
Почему «пересобрать из того же git-коммита под prod» не эквивалентно продвижению артефакта staging? Потому что сборка — не чистая функция исходного коммита. Версии транзитивных зависимостей, digest базового образа, версии build-инструментов, встроенные таймстампы и слои, скачанные по сети, — всё может различаться между двумя сборками одного коммита с разницей в минуты, ровно тот дрифт, что уронил prod в истории на старте. Так что пересборка может выдать другой бинарник, чем тот, что ты тестировал, обнуляя весь смысл staging. Продвижение точного артефакта по digest везёт протестированные байты; пересборка везёт обнадёживающее приближение, которому случилось разделить хэш коммита.
</Inset>

## Продвижение и гейты

Артефакт движется через путь с гейтами: **dev** авто-деплоится на сборку, **staging** авто-деплоится и прогоняет интеграционные тесты, **prod** требует ручного апрува. GitHub Actions моделирует это через *environments*, несущие обязательных ревьюеров и таймеры ожидания, — окружение prod просто не задеплоится, пока названный апрувер не подпишется. На апруве пайплайн продвигает уже протестированный артефакт; он не пересобирает.

GitOps-инструменты (Argo CD, Flux) делают само продвижение **git-коммитом**: деплой — это изменение манифеста окружения, двигающее запиненный digest. Это делает каждое продвижение декларативным, ревьюимым в PR, аудируемым в истории git и обратимым через revert коммита — откат становится `git revert`, который восстанавливает *точный* предыдущий digest, обходя ловушку отката `:latest` целиком.

<FlowDiagram
  label="Пайплайн собери-один-раз, продвигай-многократно. Один job сборки производит один неизменяемый образ, идентифицируемый по его content digest, registry/app@sha256. Ровно тот же digest затем продвигается через три окружения последовательно: dev деплоится автоматически, staging деплоится автоматически и прогоняет набор интеграционных тестов, затем гейт ручного апрува (обязательный ревьюер) охраняет prod, который деплоит идентичный digest. Ни одно окружение не пересобирает артефакт; каждое лишь перенацеливает свой манифест на тот же sha256 и поставляет собственный рантайм-конфиг — env vars, секреты, config maps — так что один протестированный бинарник работает везде с настройками под окружение, инжектированными во время запуска."
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "build", label: "собрать один раз", sub: "app@sha256" },
    { id: "dev", label: "dev", sub: "авто-деплой" },
    { id: "staging", label: "staging", sub: "авто + тесты" },
    { id: "gate", label: "гейт апрува", sub: "обязательный ревьюер" },
    { id: "prod", label: "prod", sub: "тот же digest" },
  ]}
  edges={[
    { from: "build", to: "dev", label: "продвинуть digest" },
    { from: "dev", to: "staging", label: "тот же sha256" },
    { from: "staging", to: "gate", label: "тесты прошли" },
    { from: "gate", to: "prod", label: "на апруве" },
  ]}
  caption="Один артефакт (app@sha256), N окружений. Тот же неизменяемый digest продвигается dev в staging в prod; конфиг инжектится на окружение в рантайме; prod закрыт за ручным апрувом — ни одно окружение не пересобирает."
/>

<TradeoffMatrix
  id="ci-cd-04-release-automation-05-artifact-promotion-and-environments-tradeoff-1"
  lessonSlug="05-artifact-promotion-and-environments"
  lang="ru"
  prompt="Тебе нужно перенести протестированный релиз из staging в prod с уверенностью, что prod запускает ровно тот код, что прошёл staging. Как ты его выкатишь?"
  options={[
    { name: "Продвинуть тот же неизменяемый артефакт по digest (registry/app@sha256:...), инжектить конфиг на окружение в рантайме и закрыть prod за апрувом обязательного ревьюера", summary: "Везти протестированные байты.", correct: true, justification: "Это и есть дисциплина собери-один-раз, продвигай-многократно. Артефакт собран ровно один раз и адресуется по своему неизменяемому content digest, так что байты, прошедшие staging, побайтово равны байтам, работающим в prod, — digest нельзя перенаправить. Конфиг на окружение инжектится в рантайме, так что один образ обслуживает все окружения и ни один секрет не запекается внутрь. Гейт апрува prod (environment GitHub Actions с обязательным ревьюером) означает, что человек подписывается на продвижение уже протестированного артефакта, а не запускает пересборку. Это единственный вариант, делающий «это прошло staging» истинным утверждением про prod." },
    { name: "Пересобрать образ из того же git-коммита под деплой prod", summary: "Тот же коммит, другой артефакт.", justification: "Это ровно инцидент со старта. Сборка — не чистая функция коммита: патчи транзитивных зависимостей, digest базового образа, версии build-инструментов и скачанные по сети слои — всё может различаться между двумя сборками одного SHA с разницей в минуты. Пересборка prod может зарезолвить зависимость, которую staging никогда не видел, так что ты везёшь непротестированный бинарник, лишь разделивший хэш коммита с тем, что ты тестировал. «Тот же коммит» — не «тот же артефакт»; вместо этого продвигай образ staging по digest." },
    { name: "Деплоить registry/app:latest и в staging, и в prod", summary: "Изменяемый тег, недетерминирован.", justification: "`:latest` — изменяемый указатель, который можно перепушить в любой момент, так что два деплоя `:latest` могут запустить два разных образа без записи об изменении, — а откат «к предыдущему образу» тянет то, на что `:latest` указывает сейчас, а не предыдущий образ. Staging и prod могут молча разойтись, а откат ненадёжен. Пинуй неизменяемый digest @sha256:, чтобы релиз был воспроизводим и обратим." },
    { name: "Скопировать образ staging, но перетегировать и пересобрать его для усиления под prod", summary: "Перетег + пересборка ломают неизменяемость.", justification: "Перетегирование безвредно, но пересборка под prod заново вносит ровно тот дрифт, что дисциплина предотвращает: пересобранный образ — новый артефакт, который никогда не тестировался в staging. Любое «усиление», меняющее байты, должно происходить до единственной сборки, а не после продвижения. Если ты меняешь байты, ты больше не везёшь протестированный артефакт — продвигай оригинальный digest нетронутым и инжектируй любые различия окружения как конфиг." },
  ]}
/>

<Quiz
  id="ci-cd-04-release-automation-05-artifact-promotion-and-environments-quiz-1"
  lessonSlug="05-artifact-promotion-and-environments"
  lang="ru"
  question="Почему собирать артефакт один раз и продвигать его через окружения, а не пересобирать на каждое окружение?"
  choices={[
    { label: "Потому что пересборка может отличаться от протестированного бинарника (дрифт зависимостей, смена базового образа), так что только продвижение точного артефакта везёт реально протестированные байты", correct: true },
    { label: "Потому что пересобирать на окружение медленнее, и единственная цена пересборки — лишние минуты CI", misconception: "Скорость — побочный бонус, а не суть. Настоящая причина — корректность: сборка не чистая функция коммита, так что пересборка prod может зарезолвить другие транзитивные зависимости или новый digest базового образа, чем staging. Даже будь пересборки мгновенными, они всё равно везли бы бинарник, который ты никогда не тестировал. Продвижение про гарантию того, что протестированные байты — это запускаемые байты, а не про экономию минут." },
    { label: "Потому что Docker запрещает собирать тот же образ дважды и откажет во второй сборке", misconception: "Docker ничего такого не делает — ты можешь пересобирать Dockerfile сколько угодно раз, и в этом ровно опасность: каждая пересборка — свежий, потенциально другой артефакт. Механически тебя ничто не останавливает; дисциплина собрать один раз и продвигать — это то, что ты накладываешь сам, чтобы prod запускал протестированный бинарник." },
    { label: "Потому что продвижение позволяет каждому окружению компилироваться с оптимизациями под окружение, запечёнными в отдельные образы", misconception: "Это противоположность продвижения — запекание различий под окружение в отдельные образы вынуждает пересобирать на каждое окружение и означает, что каждое запускает другой бинарник. Продвижение везёт ОДИН артефакт везде и инжектирует различия окружения как рантайм-конфиг, никогда как отдельные сборки." },
  ]}
/>

<Quiz
  id="ci-cd-04-release-automation-05-artifact-promotion-and-environments-quiz-2"
  lessonSlug="05-artifact-promotion-and-environments"
  lang="ru"
  question="Почему деплоить registry/app:latest опасно и на что нужно ссылаться вместо этого?"
  choices={[
    { label: "Тег — изменяемый указатель, который можно перепушить, так что :latest недетерминирован; ссылайся вместо этого на неизменяемый content digest @sha256:", correct: true },
    { label: ":latest в порядке для prod, пока ты также пушишь рядом semver-тег :v1.2.3", misconception: "Semver-тег вроде :v1.2.3 всё ещё изменяемый указатель — его можно форс-пушнуть, чтобы он указывал на другой образ, так что он несёт ту же недетерминированность, что и :latest. Единственная ссылка, которую нельзя перенаправить, — content digest, @sha256:. Пушить semver-тег полезно для людей, но деплои и откаты должны пинать digest." },
    { label: ":latest опасен только потому, что это длинная строка, в которой легко опечататься; используй короткий тег вместо этого", misconception: "Опасность не имеет отношения к длине строки. :latest опасен, потому что изменяем: два деплоя :latest могут запустить разные образы, а откат тянет то, на что он сейчас указывает. Более короткий тег ровно так же изменяем. Чинит это неизменяемость — digest @sha256:, а не краткость." },
    { label: "Опасно, потому что :latest всегда тянет новейший образ, что и нужно в prod", misconception: "Желание, чтобы prod авто-тянул «новейший образ», — это баг, а не фича: оно означает, что prod молча меняется всякий раз, когда кто-то перепушивает :latest, без ревью и без записи. Prod должен запускать конкретный, запиненный, неизменяемый digest, который меняется только через отревьюенное продвижение, — никогда тег, дрейфующий сам по себе." },
  ]}
/>

</Explanation>

<KeyTakeaway>Собери один раз, продвигай тот же образ — по digest @sha256:, не :latest — через dev, staging, prod. Пересборка из того же коммита — не тот же артефакт. Инжекти конфиг в рантайме; закрой prod за апрувом.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="ci-cd-04-release-automation-05-artifact-promotion-and-environments-retrieval"
  lang="ru"
  questions={[
    { q: "Сформулируй принцип собери-один-раз, продвигай-многократно и объясни, почему пересборка из того же git-коммита не эквивалентна продвижению протестированного артефакта.", a: "Собери-один-раз, продвигай-многократно идёт из разделения build/release/run в 12-factor: исходник превращается в один неизменяемый артефакт (образ контейнера или пакет) ровно один раз, и тот же артефакт — те же байты — затем продвигается через окружения dev в staging в prod. Ты никогда не запускаешь вторую сборку под другое окружение. Причина, по которой это не эквивалентно пересборке из того же коммита, в том, что сборка НЕ чистая функция исходного коммита: версии транзитивных зависимостей, digest базового образа, версии build-инструментов, встроенные таймстампы и скачанные по сети слои — всё может различаться между двумя сборками одного SHA с разницей в минуты. Так что пересборка prod может зарезолвить зависимость или базовый образ, которые staging никогда не тестировал, выдав другой бинарник, лишь разделивший хэш коммита. Это обнуляет весь смысл staging — «это прошло staging» становится утверждением про артефакт, который больше нигде не работает. Продвижение точного артефакта по digest везёт протестированные байты; пересборка везёт обнадёживающее приближение. Дисциплина делает гарантию буквальной: байты, которые ты тестировал, — это байты, которые ты запускаешь." },
    { q: "Объясни пиннинг по digest против тегов, где живёт конфиг окружения и как prod закрывается гейтом в пайплайне с продвижением.", a: "Тег вроде :latest или :v1.2.3 — изменяемый указатель: его можно перепушить, чтобы он указывал на другой образ, в любой момент, так что два деплоя :latest могут запустить разные образы без записи, а откат «к предыдущему образу» тянет то, на что :latest указывает сейчас, а не реальный предыдущий образ. Фикс — ссылаться на артефакт по его неизменяемому content digest, registry/app@sha256:..., который является хэшем содержимого образа и не может быть перенаправлен: sha256:abc И ЕСТЬ тот образ, байт в байт, так что release воспроизводим и аудируем. Конфиг окружения НЕ живёт в артефакте: тот же образ работает везде, а URL баз данных, флаги и секреты инжектятся в рантайме как env vars, примонтированные секреты или config maps («config в окружении» из 12-factor); запекание конфига в образ вынудило бы пересобирать на каждое окружение и протекло бы секретами prod в registry. Prod закрыт ручным апрувом: environments GitHub Actions несут обязательных ревьюеров и таймеры ожидания, так что деплой prod не пойдёт, пока апрувер не подпишется, и на апруве пайплайн продвигает уже протестированный digest, а не пересобирает. С GitOps (Argo CD/Flux) продвижение само является git-коммитом, двигающим запиненный digest в манифесте окружения, — декларативным, ревьюимым, аудируемым и обратимым через revert коммита, который восстанавливает точный предыдущий digest и избегает ловушки отката :latest." },
  ]}
/>

<Recap lang="ru">
Дисциплина — собери один раз, продвигай многократно: build превращает исходник в один неизменяемый артефакт ровно один раз, и тот же артефакт — те же байты — продвигается через dev, staging, потом prod, никогда не пересобираясь на окружение. Пересборка — это ловушка, потому что сборка не чистая функция коммита: патчи транзитивных зависимостей, digest (хэш содержимого образа) базового образа и версии build-инструментов могут различаться между двумя сборками одного SHA, так что пересборка prod может повезти бинарник, который staging никогда не тестировал, хоть он и разделяет хэш коммита. Продвижение осмысленно, только если артефакт однозначен, поэтому ты пинуешь по неизменяемому content digest (registry/app@sha256:...), а не по изменяемому тегу вроде :latest или :v1.2.3, который можно перепушить так, чтобы он указывал на другой образ, и который молча ломает и деплои, и откаты. Тот же артефакт обслуживает каждое окружение, потому что конфиг — URL баз данных, флаги, секреты — инжектится в рантайме («config в окружении» из 12-factor), а не запекается в образ, что вынудило бы пересборки на окружение и протекло бы секретами в registry. Артефакт движется через путь с гейтами: dev авто-деплоится, staging авто-деплоится и прогоняет интеграционные тесты, а prod сидит за ручным апрувом (environments GitHub Actions с обязательными ревьюерами и таймерами ожидания), который продвигает уже протестированный digest, а не пересобирает. GitOps делает каждое продвижение git-коммитом, двигающим запиненный digest в манифесте окружения, так что каждый деплой декларативен, ревьюим, аудируем и обратим через revert коммита — который восстанавливает точный предыдущий digest и обходит ловушку отката :latest целиком. Теперь, когда увидишь пайплайн с двумя шагами `docker build` — для staging и для prod — ты будешь знать, почему «прошло staging» больше не гарантия, и ровно какой один шаг сборки нужно убрать.
</Recap>
