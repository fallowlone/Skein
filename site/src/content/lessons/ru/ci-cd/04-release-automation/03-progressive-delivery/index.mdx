---
concepts:
- deploy-vs-release
- canary
- blue-green
- feature-flags
- automated-rollback
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 3
prereqs: []
slug: "03-progressive-delivery"
sources:
- https://martinfowler.com/bliki/CanaryRelease.html
- https://martinfowler.com/bliki/BlueGreenDeployment.html
- https://docs.github.com/en/actions/deployment
spiral: []
status: ready
summary: 'Задеплоить код — не то же, что зарелизить. Раздели их: выкатывай тёмным, открывай постепенно — канарейка 1-5% с авто-откатом по метрикам, blue-green для мгновенного флипа назад, фича-флаг как kill switch без редеплоя. Ограничь радиус поражения, а не ставь на staging.'
title: 'Прогрессивная доставка: канарейка, blue-green и флаги'
track: ci-cd
unit: 04-release-automation
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Тег нарезан, пайплайн зелёный, артефакт опубликован. Команда сделала всё, чему учили прошлые два урока — чистый SemVer, отревьюенный тег, подписанный релиз — а затем выкатила новую версию на 100% прода одним махом, потому что так деплой-скрипт делал всегда. За пятнадцать секунд error rate ушёл вертикально. Баг сериализации, который срабатывает только при реальных конкурентных записях — невидимый в однопоточном smoke-тесте staging — теперь падал для каждого пользователя. У них не было ни гейта, ни среза, чтобы откатить, только один рычаг, и он уже был опущен до упора. Откат был лихорадочным редеплоем предыдущей версии под крик инцидент-канала: минуты полного простоя, 100% пользователей задеты. Баг не был настоящим провалом. Провалом было выкатить его на всех сразу.
</Hook>

<Crux>Деплой кладёт код на серверы; релиз шлёт к нему пользователей. Раздели их: выкатывай тёмным, открывай постепенно, быстро откатывай.</Crux>

<Explanation>

## Деплой — это не релиз

Вот рамка, на которой держится весь урок. **Деплой** — это размещение нового кода на серверах: бинарь работает, контейнер поднят. **Релиз** — это направление пользовательского трафика к этому коду: решение, кто на самом деле на него попадёт. Прошлые уроки довели тебя до опубликованного артефакта; они не сказали, что хоть один пользователь уже должен его увидеть. Сеньорский ход — считать это двумя отдельными переключателями. Выкатывай код *тёмным* — задеплоенным, но без трафика — и затем крути ручку трафика вверх постепенно, следя за сигналами, готовый повернуть её обратно в тот момент, когда они деградируют. Всё ниже — это разные механизмы одной идеи: отдели «код здесь» от «пользователи на нём» и навесь ограничитель радиуса поражения на второй переключатель.

## Четыре стратегии выката

```yaml
# big-bang (recreate): останови всё, запусти новую версию — простой + 100% радиуса поражения
strategy:
  type: Recreate          # старые поды умирают, новые стартуют: зазор, где никто не обслуживает

# rolling: заменяй инстансы партиями — без простоя, но две версии работают одновременно
strategy:
  type: RollingUpdate
  rollingUpdate:
    maxSurge: 25%
    maxUnavailable: 0      # почти дефолт k8s: инкрементально, но БЕЗ явного гейта трафика
```

- **Big-bang (recreate / всё-сразу):** останови старую версию, запусти новую. Есть зазор простоя, и в момент, когда она поднялась, радиус поражения — 100%: все пользователи разом на коде, непроверенном в проде. Это провал из Hook. Приемлемо только для крошечных или внутренних приложений, где сбой не страшен.
- **Rolling:** заменяй инстансы партия за партией (дефолт Kubernetes). Без простоя — поэтому кажется безопасным — но две версии работают одновременно, так что они обязаны быть обратно/прямо совместимы, а плохая версия всё равно доходит до пользователей инкрементально *без явного гейта*, следящего, здорова ли она. Rolling управляет *доступностью*, а не *риском*.
- **Blue-green:** подними два полных окружения. Задеплой в GREEN (простаивающее), прогони smoke-тест в изоляции, затем атомарно переключи балансировщик / роутер с BLUE на GREEN. Переключение мгновенно, и откат тоже мгновенный — переключи роутер обратно на BLUE. Цена — примерно `2x` инфраструктуры на время переключения, и трудная часть — общее состояние: обе версии обычно ходят в одну базу, так что изменение схемы должно быть совместимо и с BLUE, и с GREEN через весь флип.
- **Canary (канарейка):** направь *маленький* процент реального трафика (1-5%) на новую версию, следи за её error rate, латентностью и бизнес-метриками против старой, затем поднимай `5 → 25 → 50 → 100%`, если остаётся здоровой, или прерывай, если нет. Канарейка ловит проблемы *на реальном продовом трафике* при крошечном радиусе поражения — ровно тот класс багов, что staging пропустил.

<Inset kind="why" lang="ru">
Зачем разделять деплой и релиз флагами и канарейками, вместо того чтобы просто тестировать жёстче в staging? Потому что staging структурно не может воспроизвести реальный микс трафика, конкуренцию, форму данных и масштаб прода — а именно там живут дорогие баги. Гонки, contention по горячему ключу, сериализатор, который ломается только при одновременных записях, единственный кривой payload, существующий в реальной таблице пользователей — ни один из них не проявится против восьми засеянных строк и одного тестового потока. Прогрессивная доставка принимает это и считает прод финальным тестом, но с привинченным ограничителем: открой изменение тонкому срезу, измерь его против baseline, и если оно плохое — ты сдержал ущерб этим срезом, а не поставил всю свою пользовательскую базу на то, что staging был репрезентативен. Тестировать жёстче — хорошо; просто это заканчивается раньше, чем баги, которые тебя кладут.
</Inset>

## Фича-флаги: переключатель релиза, которому не нужен редеплой

Канарейка двигает трафик на уровне *инфраструктуры*. **Фича-флаг** переносит решение о релизе на уровень *приложения*: оберни новое поведение в условие, задеплой с флагом OFF, и код едет тёмным, пока пользователи продолжают получать старый путь. Затем ты переключаешь флаг — для 1% пользователей, потом 10%, потом всех — *независимо от любого деплоя*. Новый код уже был на каждом сервере; ты просто меняешь значение конфига.

```ts
// новый код задеплоен везде, но тёмный, пока флаг его не откроет
if (flags.isEnabled("new-checkout-pipeline", { userId })) {
  return runNewCheckout(order);   // открыт на 1% -> 10% -> 100% через конфиг, без редеплоя
}
return runLegacyCheckout(order);  // все остальные, без изменений
```

Два свойства делают это любимым инструментом сеньора. Первое — это **мгновенный kill switch**: если `new-checkout-pipeline` ведёт себя плохо, ты переключаешь флаг обратно в OFF, и каждый пользователь на старом пути *за секунды* — без прогона пайплайна, без пересборки образа, без редеплоя. Второе — **таргетинг по пользователю / проценту**, что и питает A/B-тесты и dark launch (гоняй новый путь, отбрасывай его результат, просто измеряй его нагрузку). Если у тебя есть флаг, который «временно включили» полгода назад — ты уже в колонке затрат. Цена реальна: **долг флагов**. Каждый флаг — это ветка в коде и строка в конфиге; устаревшие флаги, которые так и не убрали, превращаются в комбинаторный бардак, который никто не смеет удалить. Флаги — это рычаг релиза, а не постоянный дом: открой их, узнай что нужно, потом удали.

## Автоматический откат: решает гейт, а не человек

Всё выше просто *безопаснее, если человек смотрит*. Сеньорская версия — **автоматическая**. Контроллер прогрессивной доставки — Argo Rollouts, Flagger или логика в твоём деплой-пайплайне — поднимает канарейку по расписанию и на каждом шаге прогоняет **аналитический запрос** к твоим метрикам: error rate, p99 латентность, скорость прожига SLO. Если запрос пробивает порог, контроллер **авто-прерывает и откатывает** — сдвигает трафик обратно на стабильную версию — без человека в петле. Вот ключевое число, которое надо усвоить: решение об откате **завязано на метрики, а не на ощущения** и не на то, что кто-то случайно пялится в дашборд в 16:00.

```yaml
# Канарейка Argo Rollouts с шагом анализа по метрике (набросок)
strategy:
  canary:
    steps:
      - setWeight: 5            # 5% трафика на новую версию
      - analysis:               # запрос к Prometheus; авто-прерывание при провале
          templates: [{ templateName: error-rate }]
      - setWeight: 25
      - analysis: { templates: [{ templateName: error-rate }] }
      - setWeight: 50
      - setWeight: 100          # полное продвижение только если каждый гейт прошёл
```

Прогони инцидент из Hook через это. Плохая версия едет на 5% трафика. Баг сериализации срабатывает под реальной конкуренцией, и аналитический запрос видит, как error rate на канарейке скачет за порог за секунды. Контроллер прерывает на 5% и сдвигает трафик обратно на стабильную — автоматически. Радиус поражения: 5% пользователей на несколько секунд вместо 100% пользователей на минуты. Баг всё равно случился; прогрессивная доставка просто сделала его не-событием.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Стратегия</th>
        <th class="border border-border px-3 py-2 text-left">Простой</th>
        <th class="border border-border px-3 py-2 text-left">Радиус поражения плохой версии</th>
        <th class="border border-border px-3 py-2 text-left">Откат</th>
        <th class="border border-border px-3 py-2 text-left">Цена / подвох</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Big-bang</strong></td>
        <td class="border border-border px-3 py-2">Да (зазор)</td>
        <td class="border border-border px-3 py-2">100% мгновенно</td>
        <td class="border border-border px-3 py-2">Редеплой старой (медленно)</td>
        <td class="border border-border px-3 py-2">Нет гейта; ок лишь для крошечных/внутренних</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Rolling</strong></td>
        <td class="border border-border px-3 py-2">Нет</td>
        <td class="border border-border px-3 py-2">Растёт инкрементально, без гейта</td>
        <td class="border border-border px-3 py-2">Откати партии</td>
        <td class="border border-border px-3 py-2">Две версии живы; должны быть совместимы</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Blue-green</strong></td>
        <td class="border border-border px-3 py-2">Нет</td>
        <td class="border border-border px-3 py-2">100% на флипе (но сначала smoke-тест)</td>
        <td class="border border-border px-3 py-2">Мгновенно: флипни роутер назад</td>
        <td class="border border-border px-3 py-2"><code>2x</code> инфры; общая БД — трудная часть</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Canary</strong></td>
        <td class="border border-border px-3 py-2">Нет</td>
        <td class="border border-border px-3 py-2">1-5%, сдержан гейтом</td>
        <td class="border border-border px-3 py-2">Авто-прерывание при пробое метрики</td>
        <td class="border border-border px-3 py-2">Нужны хорошие метрики + автоматизация</td>
      </tr>
    </tbody>
  </table>
</div>

<TradeoffMatrix
  id="ci-cd-04-release-automation-03-progressive-delivery-tradeoff-1"
  lessonSlug="03-progressive-delivery"
  lang="ru"
  prompt="Тебе нужно выкатить рискованное изменение на высоконагруженный сервис с минимально возможным радиусом поражения и мгновенным откатом. Как ты его выкатишь?"
  options={[
    { name: "Канарейка 1 -> 5 -> 25 -> 100% с автоматическим откатом по метрикам, за фича-флагом kill switch", summary: "Гейтированный рамп + kill switch.", correct: true, justification: "Это сеньорский ответ, потому что он минимизирует радиус поражения по двум независимым осям. Канарейка сначала открывает изменение реальному продовому трафику на 1-5%, и аналитический запрос по error rate / p99 / прожигу SLO авто-прерывает и сдвигает обратно на стабильную в тот момент, когда новая версия ведёт себя плохо — так баг бьёт по ~5% пользователей на секунды, а не по 100% на минуты, и человеку не нужно это замечать. Обёртка изменения в фича-флаг добавляет второй, без-редеплойный kill switch и даёт поднимать процент по пользователям независимо от деплоя. Решение продвигать завязано на метрики, а не на ощущения. Цена — реальные метрики + автоматизация, которые высоконагруженному сервису и так нужны." },
    { name: "Big-bang на 100% после прогона дополнительных тестов в staging", summary: "Ставка на staging.", justification: "Это ровно провал из Hook, наряженный под старательность. Staging структурно не может воспроизвести конкуренцию, форму данных, микс трафика и масштаб прода, так что классы багов, которые тебя кладут — гонки, contention по горячему ключу, реальное разнообразие payload — переживают любой объём дополнительного тестирования в staging и затем бьют по 100% пользователей разом, без среза, который можно откатить. Больше тестов в staging — хорошо, но они заканчиваются раньше этих багов; они не замена ограничителю радиуса поражения на релизе." },
    { name: "Флип blue-green без анализа канарейки", summary: "Мгновенный флип, без гейта.", justification: "Blue-green покупает тебе мгновенное, атомарное переключение и мгновенный флип назад, что по-настоящему ценно — но без анализа канарейки флип всё равно отправляет 100% трафика на новую версию одним движением. Smoke-тест на GREEN ловит грубую поломку, а не тонкий баг, который проявляется только под реальной конкурентной нагрузкой, так что ты всё ещё можешь перекинуть всю пользовательскую базу на сломанную версию и узнать об этом лишь из последующего инцидента. Силён в скорости отката, слаб в сдерживании радиуса для этого профиля риска." },
    { name: "Rolling update без явного health-гейта", summary: "Инкрементально, без гейта.", justification: "Rolling update избегает простоя и заменяет инстансы партия за партией, но управляет доступностью, а не риском: нет гейта, оценивающего, здорова ли новая версия, перед следующей партией. Плохая версия доходит до пользователей инкрементально и катится вперёд, пока что-то или кто-то не заметит, и к этому моменту большая доля может уже быть на ней. Лучше, чем big-bang, но это не стратегия сдерживания по метрикам." },
  ]}
/>

<Quiz
  id="ci-cd-04-release-automation-03-progressive-delivery-quiz-1"
  lessonSlug="03-progressive-delivery"
  lang="ru"
  question="В прогрессивной доставке в чём разница между деплоем и релизом и что разделяет фича-флаг?"
  choices={[
    { label: "Деплой кладёт код на серверы; релиз направляет к нему пользовательский трафик. Фича-флаг отделяет релиз (включение поведения для пользователей) от деплоя, так что ты открываешь или гасишь его через конфиг без редеплоя.", correct: true },
    { label: "Это одно и то же — раз код задеплоен, он зарелизен, а фича-флаг просто переключает окружения", misconception: "Деплой и релиз — намеренно отдельные переключатели. Деплой значит, что бинарь работает на серверах; релиз значит, что пользовательский трафик к нему направлен. Их разделяют именно для того, чтобы выкатить код тёмным и открыть его позже. Фича-флаг переключает поведение для пользователей, а не окружения." },
    { label: "Деплой направляет трафик; релиз собирает артефакт. Фича-флаг отделяет сборку от стадии тестов.", misconception: "Это переворачивает термины. Релиз направляет пользовательский трафик; деплой кладёт код на серверы. Сборка артефакта произошла раньше в CI. Фича-флаг отделяет включение поведения для пользователей от деплоя, работая как без-редеплойный переключатель релиза и kill switch." },
    { label: "Фича-флаг ничего не разделяет в рантайме; он лишь меняет, какую ветку собирает CI", misconception: "Фича-флаг — это рантайм-решение, вычисляемое в приложении: новый код задеплоен везде, но остаётся тёмным, пока флаг его не откроет, по пользователю или по проценту, без пересборки. В этом весь смысл — он отделяет релиз от деплоя, чтобы ты переключал поведение через конфиг." },
  ]}
/>

<Quiz
  id="ci-cd-04-release-automation-03-progressive-delivery-quiz-2"
  lessonSlug="03-progressive-delivery"
  lang="ru"
  question="Баг сериализации проявляется только при реальных конкурентных записях; его big-bang деплой на 100% положил весь сайт. Какой выкат сдержал бы его и по какому сигналу?"
  choices={[
    { label: "Канарейка на маленьком проценте (1-5%) с автоматическим аналитическим запросом, который прерывает и откатывает при пробое error rate / прожига SLO", correct: true },
    { label: "Больший набор тестов в staging, прогнанный перед тем же деплоем на 100%", misconception: "Staging не может воспроизвести реальную конкуренцию, запускающую этот баг, так что больше тестов в staging всё равно прошли бы, и деплой на 100% всё равно положил бы всех. Фикс не в том, чтобы тестировать жёстче перед полным деплоем — а в том, чтобы ограничить радиус поражения самого релиза канарейкой и гейтом по метрике." },
    { label: "Rolling update, который поймал бы его, потому что заменяет инстансы партиями", misconception: "У rolling update нет явного health-гейта, следящего за error rate перед каждой партией, так что плохая версия доходит до пользователей инкрементально и катится вперёд. Он управляет доступностью, а не риском. Сдерживанию нужен анализ канарейки, прерывающий при пробое метрики, чего обычный rolling update не даёт." },
    { label: "Blue-green, потому что smoke-тест на GREEN поймал бы баг конкуренции перед флипом", misconception: "Smoke-тест на простаивающем окружении GREEN идёт без реальной продовой конкуренции, так что он не выявит баг, который срабатывает только при одновременных записях — а флип blue-green затем отправляет 100% трафика разом. Нужен срез реального трафика плюс гейт по метрике, то есть канарейка." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Канареечный выкат с гейтом по метрике. Шаг 1: задеплой новую версию тёмной — она работает на серверах, но не получает пользовательского трафика. Шаг 2: сдвинь 5% реального продового трафика на канарейку. Шаг 3: аналитический запрос сравнивает error rate и p99 латентность канарейки с порогом. Ветвление по результату: если здорова — поднимай вес трафика 25, потом 50, потом 100%, полностью продвигая новую версию; если метрика пробивает порог — прерывай и откатывай, сдвигая весь трафик на стабильную версию без человека в петле. Ветка прерывания и есть то, что ограничивает радиус поражения срезом в 5%."
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "dark", label: "деплой тёмным", sub: "0% трафика" },
    { id: "canary", label: "канарейка 5%", sub: "реальный трафик" },
    { id: "gate", label: "анализ", sub: "err / p99 vs порог" },
    { id: "ramp", label: "рамп 25/50/100", sub: "здорова" },
    { id: "abort", label: "прервать + откат", sub: "пробой" },
  ]}
  edges={[
    { from: "dark", to: "canary", label: "сдвинь 5%" },
    { from: "canary", to: "gate", label: "измерь" },
    { from: "gate", to: "ramp", label: "здорова" },
    { from: "gate", to: "abort", label: "пробой" },
  ]}
  caption="Задеплой тёмным, потом отправь 5% реального трафика на канарейку и дай аналитическому запросу решить: здорова — рамп до 100%, пробой метрики — авто-прерывание обратно на стабильную. Гейт — это весь смысл: радиус поражения плохой версии ограничен срезом в 5%, человек не нужен."
/>

<KeyTakeaway>Деплой — не релиз: выкатывай тёмным, открывай постепенно. Канарейка 1-5% с авто-откатом по метрикам держит плохую версию в тонком срезе; blue-green даёт мгновенный флип назад; фича-флаг — это kill switch без редеплоя.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="ci-cd-04-release-automation-03-progressive-delivery-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни рамку «деплой против релиза» и сравни четыре стратегии выката (big-bang, rolling, blue-green, canary) по простою, радиусу поражения и откату.", a: "Деплой — это размещение нового кода на серверах: бинарь работает; релиз — это направление пользовательского трафика к этому коду. Их разделение — вся игра: выкатывай код тёмным (задеплоен, без трафика), потом крути ручку трафика вверх постепенно, следя за сигналами, готовый повернуть назад. Big-bang (recreate) останавливает старую версию и запускает новую: есть зазор простоя и 100% радиуса поражения мгновенно, а откат — это медленный редеплой старой версии; приемлемо лишь для крошечных/внутренних приложений. Rolling заменяет инстансы партия за партией (дефолт k8s): без простоя, но две версии работают разом, так что они должны быть совместимы, а плохая версия всё равно доходит до пользователей инкрементально без явного health-гейта — он управляет доступностью, а не риском. Blue-green поднимает два полных окружения, деплоит в простаивающее GREEN, прогоняет smoke-тест, затем атомарно флипает роутер с BLUE на GREEN: мгновенное переключение и мгновенный флип назад, ценой ~2x инфры на время переключения и трудной проблемы общей базы, с которой обе версии должны быть совместимы. Canary направляет малый процент (1-5%) реального трафика на новую версию, следит за error rate / латентностью / бизнес-метриками против baseline и поднимает 5 -> 25 -> 50 -> 100%, если здорова, или прерывает, если нет — она ловит проблемы на реальном продовом трафике при крошечном радиусе поражения, тот класс багов, что staging пропускает." },
    { q: "Что разделяет фича-флаг и какова его цена, и как автоматический откат по метрикам превращает инцидент из Hook в не-событие?", a: "Фича-флаг переносит решение о релизе на уровень приложения: ты оборачиваешь новое поведение в условие, деплоишь с флагом OFF, так что код едет тёмным, потом переключаешь флаг для 1%, потом 10%, потом 100% пользователей независимо от любого деплоя — код уже был на каждом сервере, ты лишь меняешь значение конфига. Это даёт два свойства: мгновенный kill switch (переключи обратно в OFF, и каждый пользователь на старом пути за секунды, без редеплоя) и таргетинг по пользователю / проценту, что питает A/B-тесты и dark launch. Цена — долг флагов: каждый флаг — это ветка кода плюс строка конфига, а устаревшие неубранные флаги становятся комбинаторным бардаком — флаги это временный рычаг, который надо удалить после того, как узнал нужное. Автоматический откат — сеньорский слой: контроллер (Argo Rollouts, Flagger или логика пайплайна) поднимает канарейку и на каждом шаге прогоняет аналитический запрос к метрикам — error rate, p99, прожиг SLO — и если он пробивает порог, авто-прерывает и сдвигает трафик обратно на стабильную без человека в петле; решение завязано на метрики, а не на ощущения. Прогони баг сериализации из Hook через это: плохая версия едет на 5%, баг срабатывает под реальной конкуренцией, аналитический запрос видит скачок error rate канарейки за секунды и прерывает обратно на стабильную. Радиус поражения: 5% пользователей на секунды вместо 100% на минуты — баг всё равно случился, прогрессивная доставка просто сделала его не-событием." },
  ]}
/>

<Recap lang="ru">
Задеплоить код — не то же самое, что зарелизить: деплой кладёт бинарь на серверы, релиз направляет к нему пользовательский трафик, и сеньорский ход — считать их двумя переключателями: выкатывай тёмным, потом открывай постепенно, следя за сигналами, готовый быстро откатить. Четыре стратегии выката торгуют простоем против радиуса поражения: big-bang останавливает старую и запускает новую с зазором простоя и 100% радиуса (ок лишь для крошечных/внутренних приложений); rolling заменяет инстансы партия за партией без простоя, но с двумя совместимыми версиями и без явного гейта, управляя доступностью, а не риском; blue-green поднимает два полных окружения и атомарно флипает роутер с BLUE на GREEN для мгновенного переключения и мгновенного флипа назад, ценой ~2x инфры и трудной проблемы общей базы; canary направляет 1-5% реального трафика на новую версию, следит за error rate / p99 / бизнес-метриками против baseline и поднимает 5/25/50/100%, если здорова, или прерывает, если нет, ловя баги, которые staging не может воспроизвести, при крошечном радиусе поражения. Фича-флаги отделяют релиз от деплоя на уровне приложения — деплой с флагом OFF, потом переключай по пользователю или проценту без редеплоя — давая мгновенный kill switch и таргетинг A/B/dark-launch, ценой долга флагов, который надо чистить. Сеньорская версия автоматическая: контроллер поднимает канарейку и прогоняет аналитический запрос по error rate / p99 / прожигу SLO, авто-прерывая и откатывая на стабильную при пробое метрики без человека в петле — решение завязано на метрики, а не на ощущения. Канарейка на 5% с этим гейтом поймала бы баг сериализации из Hook при 5% радиуса поражения на секунды, вместо того чтобы big-bang на 100% положил весь сайт на минуты. Теперь, когда увидишь деплой-скрипт, выкатывающий сразу на 100%, — будешь знать, какого рычага не хватает и чего стоило бы его иметь.
</Recap>
