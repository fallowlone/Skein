---
concepts:
- git-tags
- github-releases
- tag-triggered-workflow
- registry-auth
- npm-provenance
- least-privilege-token
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-tagging-and-publishing
sources:
- https://docs.github.com/en/actions/use-cases-and-examples/publishing-packages/publishing-nodejs-packages
- https://docs.npmjs.com/generating-provenance-statements
- https://docs.github.com/en/actions/security-for-github-actions/security-hardening-your-deployments/about-security-hardening-with-openid-connect
- https://docs.github.com/en/actions/use-cases-and-examples/publishing-packages/publishing-docker-images
spiral: []
status: ready
summary: 'Запушенный тег — это триггер, превращающий коммит в опубликованный релиз. Гейти workflow на тег вида v*.*.*, ограничивай токен через блок permissions и публикуй с provenance — чтобы утёкший PAT или публикация на каждый пуш никогда не отгрузились.'
title: 'Теги и публикация релизов из CI'
track: ci-cd
unit: 04-release-automation
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
В 16:00 загорается инцидент-канал: `acme-sdk@2.4.0-rc.1` уже в npm — релиз-кандидат, который не проходил QA, теперь устанавливается у любого, кто запустил `npm update`. Никто не нажимал «опубликовать». Это сделал пайплайн. Кто-то написал `on: push` в начале release-workflow, поэтому *каждый коммит в любую ветку* запускал `npm publish`, и бамп версии в feature-ветке улетел прямиком в публичный реестр. Фикс — одна строка YAML, но команда ещё и хранила долгоживущий npm automation token обычным секретом, и этот токен, способный публиковать *любой* их пакет, только что отработал на коде, который никто не ревьюил.
</Hook>

<Crux>Запушенный тег — триггер, превращающий отревьюенный коммит в релиз: гейти на `tags:`, ограничь токен, подпиши provenance.</Crux>

<Explanation>

## Тег — это именованный неизменяемый указатель на один коммит

Git-тег — это метка, привязывающая человекочитаемое имя к одному конкретному коммиту. Тегов два вида, и разница важна для релизов. **Lightweight-тег** — это просто указатель: имя тега и хэш коммита, и больше ничего. **Annotated-тег** — это полноценный объект в базе Git: он несёт имя и email теггера, временную метку, сообщение и — главное — его можно **подписать** через GPG/SSH. Для релиза почти всегда нужен annotated: `git tag -a v1.2.0 -m "Release 1.2.0"`. Он фиксирует, *кто* выпустил релиз и *когда*, он переживает `git describe`, и только его пробрасывает `git push --follow-tags`. Lightweight-теги — для черновых закладок, которые ты не собираешься публиковать.

Теги якорят релизы именно потому, что должны быть **неизменяемыми**: `v1.2.0` обязан указывать ровно на те байты, что ты протестировал, навсегда. Как только ты позволяешь тегу плавать — пере-указываешь его на новый коммит или тегаешь движущийся кончик ветки — ты ломаешь обещание, что версия есть фиксированный артефакт. А **GitHub Release** затем навешивает на этот тег заметки и скачиваемые ассеты, давая потребителям человекочитаемый changelog и стабильное место для бинарников. Тегай конкретный отревьюенный зелёный коммит — никогда изменяемый ref.

## Тег и есть триггер: `on: push: tags`

Вот механизм, связывающий тегирование с публикацией. GitHub Actions позволяет workflow подписаться на пуши тегов. Когда ты пушишь тег, попадающий под фильтр, workflow срабатывает — и *только* тогда. Именно это отделяет «каждый коммит собирается и тестируется» (твой обычный CI) от «релиз отгружается» (этот workflow). Глоб `v*.*.*` матчит `v1.2.0`, `v2.0.1` и так далее; а `branches: []` (или просто отсутствие branches) не даёт обычным коммитам его триггерить.

```yaml
name: publish
on:
  push:
    tags:
      - 'v*.*.*'        # срабатывает только на запушенный тег версии, напр. v1.2.0

permissions:            # least privilege: задание получает только то, что использует
  contents: read        # читать репозиторий для сборки
  id-token: write       # выпустить OIDC-токен для provenance

jobs:
  publish-npm:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          registry-url: https://registry.npmjs.org
      - run: npm ci
      - run: npm publish --provenance --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
```

Контраст с хуком — это весь урок. `on: push` (без фильтра) бежит на каждый пуш в любую ветку — так и сбежал релиз-кандидат. `on: push: tags: ['v*.*.*']` бежит только когда мейнтейнер осознанно пушит тег версии, а это отревьюенное, намеренное действие. Тег — это решение человека; workflow — автоматизация, которая этому решению доверяет.

<Inset kind="warning" lang="ru">
Тонкая ловушка: тег, созданный workflow через встроенный `GITHUB_TOKEN`, **не** триггерит событие `push` другого workflow — GitHub подавляет это, чтобы избежать бесконечных CI-циклов. Поэтому если инструмент вроде release-please или semantic-release нарезает твой тег *внутри* CI, твоё publish-задание на `on: push: tags` может молча так и не запуститься. Стандартные фиксы — триггерить на `on: release: [published]` либо пушить тег через PAT / GitHub App token вместо дефолтного `GITHUB_TOKEN`.
</Inset>

## Аутентификация: токен наименьших привилегий, а не долгоживущий PAT

Второй провал хука — это credential. Release-workflow нужно *чем-то* аутентифицироваться в реестре, и выбор тянется от опасного до дисциплинированного.

Встроенный **`GITHUB_TOKEN`** — дефолт, к которому стоит тянуться: GitHub выпускает его заново на каждый запуск, он истекает с концом задания, и ты ограничиваешь его блоком `permissions:`. Правило — **least privilege**: дай только то, что задание использует. Для публикации образа контейнера в GitHub Container Registry (GHCR) это `packages: write`; для npm-provenance — `id-token: write`. Важно: GitHub Actions выставляет токену *read-only* `contents`, как только ты объявляешь любой блок `permissions:`, поэтому, написав блок, ты одновременно даёшь нужный скоуп и отзываешь всё остальное. Опусти блок — и токен может унаследовать широкий write-скоуп на весь репозиторий: ошибка «забыл `permissions:`», превращающая сборочный токен в обузу.

Для пуша в **сторонний** реестр (npm, Docker Hub, облачный провайдер) предпочитай **OIDC**: задание выпускает короткоживущий подписанный токен, trust-политика реестра проверяет, что он пришёл из твоего конкретного workflow, и ты обмениваешь его на эфемерные креды. Ничего долгоживущего не хранится. Худший вариант — собственно баг хука — это **долгоживущий PAT или npm automation token**, вставленный секретом: он не истекает, его надо ротировать, и один утёкший токен со скоупом «публиковать любой пакет» — это готовая supply-chain-брешь.

```yaml
# Публикация образа контейнера в GHCR встроенным токеном
permissions:
  contents: read
  packages: write          # единственный скоуп, нужный пушу в GHCR

jobs:
  publish-image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}   # эфемерный, на один запуск
      - uses: docker/build-push-action@v6
        with:
          push: true
          tags: ghcr.io/${{ github.repository }}:${{ github.ref_name }}
```

## Provenance: привяжи пакет к сборке, которая его сделала

Капстоун — это **provenance**. Когда ты запускаешь `npm publish --provenance` на облачном раннере с `id-token: write`, npm использует OIDC-токен, чтобы сгенерировать подписанную аттестацию — через Sigstore, — связывающую опубликованный тарбол с конкретным исходным репозиторием, коммитом и инструкциями сборки, которые его произвели. Требования точны и их стоит запомнить: npm CLI `9.5.0+`, право `id-token: write` и поддерживаемый облачный CI-раннер (например, `ubuntu-latest` в GitHub Actions). Аттестация публична и проверяема, поэтому потребитель может подтвердить, *где и как* пакет был собран, до установки — закрывая щель, через которую атакующий с украденным токеном публикует отравленную сборку, выглядящую легитимной.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Измерение</th>
        <th class="border border-border px-3 py-2 text-left">Долгоживущий PAT / npm-токен секретом</th>
        <th class="border border-border px-3 py-2 text-left"><code>GITHUB_TOKEN</code> + <code>permissions:</code> / OIDC</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Время жизни</td>
        <td class="border border-border px-3 py-2">Бессрочно, пока вручную не ротируешь</td>
        <td class="border border-border px-3 py-2">Выпуск на запуск, истекает с концом задания</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Скоуп</td>
        <td class="border border-border px-3 py-2">Часто широкий — публиковать любой пакет</td>
        <td class="border border-border px-3 py-2">Ровно объявленный <code>permissions:</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Радиус поражения при утечке</td>
        <td class="border border-border px-3 py-2">Полный, пока не заметят и не отзовут</td>
        <td class="border border-border px-3 py-2">Один запуск; OIDC привязан к твоему workflow</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Provenance</td>
        <td class="border border-border px-3 py-2">Нет — публиковать может любой с токеном</td>
        <td class="border border-border px-3 py-2">Подпись связывает тарбол с коммитом</td>
      </tr>
    </tbody>
  </table>
</div>

<Quiz
  id="02-tagging-and-publishing-quiz-1"
  lessonSlug="02-tagging-and-publishing"
  lang="ru"
  question="Релиз-кандидат опубликовался в npm из коммита feature-ветки, который никто не ревьюил. Workflow начинается с `on: push`. В чём корневая причина?"
  choices={[
    { label: "`on: push` без фильтра тегов бежит на каждый пуш в любую ветку, поэтому бамп версии где угодно запустил `npm publish`; шаг публикации надо гейтить на `on: push: tags: ['v*.*.*']`", correct: true },
    { label: "npm publish недетерминирован и иногда срабатывает сам", misconception: "npm publish бежит только когда его вызывает шаг. Шаг отработал, потому что workflow триггерился на каждый пуш; сама публикация детерминирована. Баг — в триггере, не в npm." },
    { label: "npm-токен был слишком широко заскоуплен", misconception: "Широкий токен — реальная, но отдельная проблема: пере-скоупленный токен расширяет радиус поражения, когда публикация уже сработала. А сработала она, потому что у `on: push` не было фильтра тегов." },
    { label: "GitHub Actions всегда публикует на теги по умолчанию", misconception: "Никакой неявной публикации нет. Workflow опубликовал, потому что был явно настроен бежать на каждый пуш и звать npm publish. Гейтинг по фильтру тегов и делает это намеренным." },
  ]}
/>

<Quiz
  id="02-tagging-and-publishing-quiz-2"
  lessonSlug="02-tagging-and-publishing"
  lang="ru"
  question="Твоё задание на теге запускает `npm publish --provenance`, но падает: 'provenance generation requires id-token: write'. Блока `permissions:` нет. Что происходит и каков фикс?"
  choices={[
    { label: "Provenance нужен OIDC-токен, требующий `id-token: write`; добавь блок `permissions:` с `id-token: write` (и `contents: read`), чтобы задание выпустило подписывающий токен", correct: true },
    { label: "Provenance нужен долгоживущий npm-токен с дополнительными скоупами; сгенерируй и сохрани его", misconception: "Provenance подписывается через OIDC id-token, а не через более широкий npm-credential. Добавление долгоживущего токена не даёт id-token: write и возвращает риск утечки токена, о котором предупреждает урок." },
    { label: "Нужно бежать на self-hosted раннере, чтобы получить provenance", misconception: "Provenance требует поддерживаемый облачный раннер (например, ubuntu-latest в GitHub Actions), а не self-hosted. Недостаёт права id-token: write, а не другого типа раннера." },
    { label: "Понизь npm — в новых версиях убрали --provenance", misconception: "Provenance требует npm 9.5.0+; новые версии его поддерживают, а не убирают. Падение из-за отсутствующего права id-token: write, нужного для выпуска OIDC-токена." },
  ]}
/>

<TradeoffMatrix
  id="02-tagging-and-publishing-tradeoff-1"
  lessonSlug="02-tagging-and-publishing"
  lang="ru"
  prompt="Твой CI публикует пакет в публичный реестр на релизе. Выбери, как publish-задание аутентифицируется."
  options={[
    { name: "OIDC: id-token: write, обмен в реестре на короткоживущие креды, с npm provenance", summary: "Без хранимого токена.", correct: true, justification: "Современный дефолт для сторонних реестров. Ничего долгоживущего не хранится, креды выпускаются на запуск и быстро истекают, а trust-политика реестра привязывает их к твоему конкретному workflow, так что другой репозиторий опубликовать не сможет. Запусти на облачном раннере с id-token: write — и в придачу получишь подписанную provenance-аттестацию, связывающую тарбол с коммитом. Разовая настройка identity-провайдера покупает навсегда меньшую поверхность атаки." },
    { name: "Встроенный GITHUB_TOKEN, ограниченный блоком permissions: наименьших привилегий (напр. packages: write для GHCR)", summary: "Эфемерный, ограниченный.", justification: "Отлично для GitHub-нативных целей вроде GHCR: токен выпускается на запуск, истекает с концом задания, а блок permissions: даёт ровно один нужный публикации скоуп, отзывая остальное. Ограничение — охват: GITHUB_TOKEN аутентифицируется в реестрах самого GitHub, а не в произвольных сторонних, где ты откатываешься на OIDC или credential реестра." },
    { name: "Долгоживущий npm automation token, хранимый секретом репозитория", summary: "Статический токен.", justification: "Рабочий, но самый слабый из credential-вариантов: токен не истекает, его надо ротировать, и если он заскоуплен публиковать любой твой пакет, одна утечка — компрометация цепочки поставок. Допустимо лишь когда реестр не даёт OIDC/trusted-publishing; даже тогда скоупь его так узко, как позволяет реестр, и предпочитай токены на отдельный пакет." },
    { name: "Захардкодить npm-токен прямо в YAML workflow, чтобы публикация всегда работала", summary: "Inline-credential.", justification: "Никогда. Токен ложится в историю git открытым текстом, виден любому с доступом на чтение, едет в каждый форк и клон, и его нельзя замаскировать, потому что он не зарегистрированный секрет. Худший вариант — ровно то, ради предотвращения чего существуют аутентификация реестра и хранилище секретов." },
  ]}
/>

</Explanation>

<SequenceDiagram
  label="Запушенный тег версии триггерит release-workflow, который собирает, подписывает provenance и публикует артефакт в реестр"
  hue="var(--d-systems)"
  actors={["Dev", "Git", "CI", "Registry"]}
  messages={[
    { from: "Dev", to: "Git", label: "git push origin v1.2.0" },
    { from: "Git", to: "CI", label: "триггер (on: push: tags v*.*.*)" },
    { from: "CI", to: "CI", label: "сборка + подпись (OIDC provenance)" },
    { from: "CI", to: "Registry", label: "публикация tarball/образа + аттестация" },
    { from: "Registry", to: "Dev", label: "v1.2.0 доступна, верифицируема" },
  ]}
  caption="Запушенный тег — единственный триггер: намеренный отревьюенный тег версии запускает workflow, который выпускает короткоживущий OIDC-токен, подписывает provenance, связывая артефакт с коммитом, и публикует. Нет тега — нет публикации, и нигде не хранится долгоживущий credential."
/>

<KeyTakeaway>Тег — это триггер: гейти на `on: push: tags: ['v*.*.*']`, не на голый `on: push`. Ограничивай токен блоком `permissions:`, предпочитай OIDC долгоживущим PAT и `npm publish --provenance`.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-tagging-and-publishing-retrieval"
  lang="ru"
  questions={[
    { q: "Почему `on: push: tags: ['v*.*.*']` делает публикацию безопасной там, где `on: push` опасен, и какая одна тег-триггерная ловушка может молча пропустить задание?", a: "Голый `on: push` бежит на каждый пуш в любую ветку, поэтому любой коммит — включая бамп версии в неотревьюенной feature-ветке — зовёт `npm publish` и отгружает в реестр. Именно так релиз-кандидат сбегает в npm без чьего-либо нажатия «опубликовать». Фильтр `tags: ['v*.*.*']` переключает триггер с «любое изменение кода» на «мейнтейнер осознанно запушил тег версии», а это отревьюенное намеренное действие; вместе с `branches: []` он не срабатывает на обычные коммиты. Тег — решение человека, workflow — автоматизация, которая ему доверяет. Ловушка: тег, созданный внутри CI через встроенный GITHUB_TOKEN, не триггерит событие push другого workflow — GitHub подавляет это во избежание бесконечных циклов, — поэтому если release-please или semantic-release нарезает тег, твоё задание на `on: push: tags` может молча не запуститься. Чини триггером `on: release: [published]` либо пушем тега через PAT или GitHub App token вместо дефолтного GITHUB_TOKEN." },
    { q: "Сравни долгоживущий npm-токен со встроенным GITHUB_TOKEN с блоком permissions: и OIDC-provenance, и объясни, что именно доказывает `--provenance`.", a: "Долгоживущий npm automation token (или PAT), хранимый секретом, не истекает, ротируется вручную и часто пере-скоуплен на публикацию любого твоего пакета — поэтому одна утечка это supply-chain-брешь, эксплуатируемая, пока кто-то не заметит и не отзовёт. Встроенный GITHUB_TOKEN выпускается заново на запуск и истекает с концом задания; объявление блока permissions: даёт ровно нужный публикации скоуп (packages: write для GHCR, id-token: write для provenance) и, что важно, отзывает всё прочее, чиня пере-скоуп «забыл permissions:». Для сторонних реестров OIDC идёт дальше: задание выпускает короткоживущий подписанный токен, реестр проверяет, что он из твоего конкретного workflow, и ты обмениваешь его на эфемерные креды — ничего долгоживущего не хранится. `npm publish --provenance` затем этим id-token генерирует подписанную Sigstore аттестацию, связывающую опубликованный тарбол с точным исходным репозиторием, коммитом и инструкциями сборки; ему нужны npm 9.5.0+, id-token: write и облачный раннер. Он доказывает, где и как пакет собран, поэтому потребители могут проверить, что релиз произведён твоим пайплайном, а не подделан кем-то с украденным токеном." },
  ]}
/>

<Recap lang="ru">
Git-тег привязывает человекочитаемое имя к одному коммиту; для релизов используй annotated, в идеале подписанный тег (`git tag -a v1.2.0 -m ...`), потому что он фиксирует, кто и когда выпустил релиз, и считай его неизменяемым — тегай конкретный отревьюенный зелёный коммит, никогда движущийся кончик ветки, и навесь на него GitHub Release с заметками и ассетами. Тег — это триггер: гейтинг release-workflow на `on: push: tags: ['v*.*.*']` означает, что он срабатывает только когда мейнтейнер намеренно пушит тег версии, и это фикс классического провала, когда голый `on: push` отгружает релиз-кандидат из неотревьюенной ветки. Помни ловушку: тег, созданный внутри CI дефолтным GITHUB_TOKEN, не триггерит событие push другого workflow, поэтому тянись к `on: release: [published]` или PAT/App-токену, когда тег нарезает release-инструмент. Для аутентификации предпочитай эфемерный встроенный GITHUB_TOKEN, суженный блоком `permissions:` наименьших привилегий — `packages: write` для GHCR, `id-token: write` для provenance — долгоживущему PAT или npm-токену, чья единственная утечка это supply-chain-брешь; для сторонних реестров используй OIDC ради короткоживущих кредов. Наконец, `npm publish --provenance` с облачного раннера с `id-token: write` (npm 9.5.0+) подписывает Sigstore-аттестацию, связывающую пакет с точным коммитом и сборкой, чтобы потребители могли проверить, где и как он собран.
</Recap>
