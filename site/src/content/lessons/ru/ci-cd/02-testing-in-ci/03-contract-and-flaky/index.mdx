---
concepts:
- consumer-driven-contracts
- pact-broker
- can-i-deploy
- flaky-test-causes
- test-determinism
- quarantine-and-retries
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: senior
order: 3
prereqs: []
slug: 03-contract-and-flaky
sources:
- https://docs.pact.io/
- https://martinfowler.com/articles/contract-test.html
- https://docs.pact.io/pact_broker/can_i_deploy
- https://martinfowler.com/articles/practical-test-pyramid.html
spiral: []
status: ready
summary: 'Независимо деплоящиеся сервисы ломают друг друга через API; consumer-driven
  contract ловит это до деплоя без общего интеграционного окружения. А flaky-тесты
  подрывают доверие, пока green не перестанет значить «безопасно мёржить» — делай их детерминированными, не retry.'
title: 'Контрактное тестирование и борьба с flaky-тестами'
track: ci-cd
unit: 02-testing-in-ci
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Команда checkout переименовывает JSON-поле `total_cents` в `amount_cents` и катит в 11:00. Их тесты зелёные, сервис здоров. В 11:04 сервис orders начинает падать — он всё ещё читает `total_cents`, теперь `undefined`, и пишет `NaN` в реестр. Никто не гонял тест, который проверял бы оба сервиса вместе, потому что единственный такой жил в ночном интеграционном прогоне на 40 минут, который падает так часто, что письмо от него все игнорируют. Здесь два отдельных провала CI: не было быстрой проверки, что orders зависит от этого поля, а единственная медленная проверка, которая могла бы поймать, давно потеряла всё доверие из-за flaky.
</Hook>

<Crux>Независимо деплоящиеся сервисы ломают друг друга через API; consumer-driven contract ловит это в CI, а flaky подрывают доверие к green.</Crux>

<Explanation>

## Почему общее интеграционное окружение — не тот инструмент

Когда два сервиса деплоятся одним релизным поездом, end-to-end тест, поднимающий оба, честен: он тестирует ровно то, что едет в прод. В тот момент, когда они начинают деплоиться независимо — нормальное состояние любого парка микросервисов — эта честность испаряется. Полноценное интеграционное окружение должно держать *все* сервисы разом, что делает его медленным в провижининге, дорогим в поддержании актуальности и магнитом для flaky: любой из N сервисов в процессе деплоя валит прогон по причинам, не связанным с твоим изменением. Хуже того — оно тестирует *не тот вопрос*. Тебе не важно, совместимы ли все сервисы между собой в каком-то снапшоте staging; тебе важно, не сломает ли *версия, которую ты вот-вот задеплоишь*, тех, кто от неё зависит, и не сломают ли её те, от кого зависит она.

Consumer-driven contract testing отвечает ровно на этот вопрос, ни разу не запуская два сервиса в одном процессе. Идея: единственная часть API провайдера, которая имеет значение, — та, которой реально пользуются его consumer'ы. Так пусть каждый **consumer** запишет, как побочный продукт собственных unit-тестов, конкретные пары запрос/ответ, на которые он опирается, — эта запись и есть **contract**. Каждая пара запрос/ответ — это одна interaction: «когда я делаю GET `/orders/42`, я жду 200 с телом, где `amount_cents` — число». Тесты consumer'а гоняются против mock-провайдера, который проигрывает эти ответы, поэтому остаются быстрыми и изолированными. Затем **provider** забирает contract каждого consumer'а и в своём собственном pipeline проверяет, что может удовлетворить каждую interaction против настоящего хендлера. Переименование в checkout выше провалило бы provider-верификацию checkout'а в тот же миг, как прогнало бы contract orders'а — в собственном CI checkout'а, до мёржа, без единого задеплоенного где-либо orders.

## Broker и can-i-deploy

Деталь, превращающая два pipeline'а в скоординированный gate, — это **broker** (Pact Broker или его хостед-форма PactFlow). Consumer публикует туда свой contract, помеченный своей версией и веткой/окружением, куда он направляется. Pipeline провайдера тянет нужные contract'ы из broker'а, верифицирует их и публикует *результат верификации* обратно. Так broker держит матрицу: для каждой версии consumer'а и версии провайдера — проверено ли, что эта пара совместима?

Эта матрица и питает **can-i-deploy** — собственно deploy-gate. Прежде чем выкатить версию X сервиса в окружение, ты спрашиваешь broker `can-i-deploy --pacticipant orders --version X --to-environment production`, и он отвечает «да» только если у каждого сервиса, с которым `orders` будет общаться в проде, есть записанная, прошедшая верификация против contract'а orders на версии X (и наоборот). Это запрос по уже собранным результатам, а не свежий прогон тестов, поэтому он мгновенный. В этом и разница с общим окружением: вместо того чтобы *надеяться*, что снапшот staging отражает прод, broker *знает*, какие именно версии совместимы, потому что каждая сторона доказала это в своём быстром pipeline.

<SequenceDiagram
  label="Поток consumer-driven contract: consumer генерирует contract из собственных тестов и публикует в broker; provider тянет этот contract, верифицирует против настоящего хендлера в своём pipeline и публикует результат обратно; can-i-deploy затем запрашивает broker перед релизом любой из сторон."
  hue="var(--d-systems)"
  actors={["Consumer", "Broker", "Provider"]}
  messages={[
    { from: "Consumer", to: "Consumer", label: "unit-тесты vs mock-provider" },
    { from: "Consumer", to: "Broker", label: "publish contract @ v.X" },
    { from: "Provider", to: "Broker", label: "pull contract'ы consumer'ов" },
    { from: "Provider", to: "Provider", label: "verify каждую interaction vs хендлер" },
    { from: "Provider", to: "Broker", label: "publish результат верификации" },
    { from: "Provider", to: "Broker", label: "can-i-deploy --to production?" },
    { from: "Broker", to: "Provider", label: "да: все пары проверены совместимыми" },
  ]}
  caption="Нет общего интеграционного окружения, и два сервиса никогда не запускаются вместе. Каждая сторона доказывает совместимость в своём быстром pipeline; матрица верификации broker'а отвечает на can-i-deploy мгновенно."
/>

Contract-тесты — не замена schema- или OpenAPI-проверок: они отвечают на более узкий, острый вопрос. OpenAPI-diff говорит, что *спека* изменилась ломающим образом; contract говорит, ломает ли это изменение *реального consumer'а*. Переименование поля, которое никто не читает, — ломающее изменение схемы, но не бьёт ни одного contract'а, поэтому деплоится свободно. Ужесточение поля, которое парсят три consumer'а, проходит схему (всё ещё валидный JSON), но валит их contract'ы. Используй schema-линтинг как дешёвый первый фильтр; используй contract'ы, чтобы гейтить по реальной связанности.

<Inset kind="why" lang="ru">
Contract testing — социотехническая штука, а не просто инструмент. Contract — это договорённость между двумя командами, поэтому provider, который «чинит» проваленную верификацию, тихо отредактировав contract consumer'а, обнулил весь смысл — contract должен принадлежать consumer'у и меняться только по согласованию. Bi-directional contract testing (PactFlow) ослабляет это, позволяя провайдеру дать свою OpenAPI-спеку вместо прогона тестов, сгенерированных consumer'ом, обменивая часть точности на меньшую связанность тест-сьютов команд.
</Inset>

## Flaky-тесты: убийца доверия

Flaky-тест проходит и падает на *одном и том же коде* без изменений между прогонами. Он опаснее стабильно красного теста, потому что red, который ничего не значит, приучает инженеров игнорировать red. Как только «да просто перезапусти» становится рефлексом, твой pipeline перестаёт быть gate'ом: реальный регресс прячется в шуме, люди мёржат по наитию, и вся ценность CI — что green значит *безопасно мёржить* — пропадает. Относиться к flaky как к самостоятельной проблеме качества, с бюджетом и владельцем, — это сеньорский ход; терпеть её — это как тест-сьют умирает.

Причины — короткий повторяющийся список, и название причины диктует фикс.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Причина</th>
        <th class="border border-border px-3 py-2 text-left">Симптом</th>
        <th class="border border-border px-3 py-2 text-left">Детерминированный фикс</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Тайминг / race</td>
        <td class="border border-border px-3 py-2">Падает под нагрузкой CI, проходит локально; <code>sleep(100)</code> «чинит»</td>
        <td class="border border-border px-3 py-2">Жди условие (поллинг элемента/состояния), никогда не фиксированный sleep; контролируй порядок async</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Реальные часы / дата</td>
        <td class="border border-border px-3 py-2">Ломается в полночь, в конце месяца или на переходе DST</td>
        <td class="border border-border px-3 py-2">Внедри fake clock; заморозь время в тесте</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Общее состояние / зависимость от порядка</td>
        <td class="border border-border px-3 py-2">Проходит в одиночку, падает при перемешивании сьюта или параллельном прогоне</td>
        <td class="border border-border px-3 py-2">Изолируй состояние на тест (свежая БД/rollback транзакции); рандомизируй порядок, чтобы вскрыть это</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Реальная сеть / внешний сервис</td>
        <td class="border border-border px-3 py-2">Падает, когда третья сторона медленна или лежит</td>
        <td class="border border-border px-3 py-2">Замокай границу; вынеси интеграцию в contract-тест</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Загрязнение тестов</td>
        <td class="border border-border px-3 py-2">Тест B падает только после теста A (утёкший global/singleton/файл)</td>
        <td class="border border-border px-3 py-2">Сбрасывай global'ы в teardown; никаких кросс-тестовых фикстур</td>
      </tr>
    </tbody>
  </table>
</div>

Воркфлоу вокруг них — три хода. **Detect**: перезапускай сьют по расписанию и трекай pass/fail по каждому тесту во времени, чтобы flaky показывалась дашбордом, а не ощущением. **Quarantine**: когда тест флакает, помечай его как quarantine, чтобы он всё ещё гонялся и репортил, но *не* гейтил мёрж, — и заводи тикет. Это защищает сигнал pipeline'а, пока ты чинишь тест, без худшей альтернативы — удалить его. **Fix**: дотащи его до детерминизма по таблице выше; quarantine — это карантин, а не кладбище, и список quarantine, который только растёт, сам по себе провал.

Bounded retry — самый злоупотребляемый инструмент здесь. Авто-перезапуск упавшего теста, пока он не пройдёт, не чинит flaky — он её *прячет*, и спрячет реальный перемежающийся баг (настоящий race в проде) так же охотно. Единственное оправданное применение — маленький, ограниченный retry (например, 2 попытки) **только на end-to-end тестах**, где часть средовой flaky неустранима, и только когда каждый retry логируется и считается, чтобы flake-rate оставался видимым. Никогда не делай retry unit- или интеграционного теста: они должны быть детерминированными, и retry там — это признание, что ты пропустил фикс.

<TradeoffMatrix
  id="contract-flaky-tradeoff-1"
  lessonSlug="03-contract-and-flaky"
  lang="ru"
  prompt="Unit-тест калькулятора скидки на заказ проходит ~95% прогонов CI и падает в остальных, на неизменном коде. Что делаешь?"
  options={[
    { name: "Найти недетерминизм (часы, общее состояние, порядок) и сделать тест детерминированным", summary: "Чинить корневую причину.", correct: true, justification: "Unit-тест должен быть полностью детерминированным, поэтому pass-rate 95% — это дефект теста, а не фоновый шум. Фикс — назвать причину (почти всегда реальные часы, утёкшее общее состояние или зависимость от порядка) и убрать её: внедрить fake clock, изолировать состояние на тест, контролировать порядок async. Это единственный вариант, восстанавливающий сигнал теста; всё остальное сохраняет или прячет гниль." },
    { name: "Сразу в quarantine и дальше, оставив в quarantine", summary: "Пометить и забыть.", justification: "Quarantine — верный первый ход, чтобы защитить сигнал pipeline'а, но это карантин, а не пункт назначения. Оставить навсегда в quarantine — это как сьют тихо теряет покрытие; quarantine обязан идти с тикетом и фиксом. Как единственный ответ он останавливает кровь, но не лечит рану." },
    { name: "Добавить авто-retry (3 попытки), чтобы стал зелёным", summary: "Retry до прохода.", justification: "Retry unit-теста прячет flaky вместо починки и спрячет реальный перемежающийся баг — настоящий race в логике скидки — так же охотно, как средовую икоту. Bounded retry оправданы только на end-to-end тестах, где часть flaky неустранима, никогда на unit-тесте, который должен быть детерминированным." },
    { name: "Удалить тест — 95% достаточно, а он блокирует мёржи", summary: "Выбросить.", justification: "Удаление убирает покрытие реальной логики, чтобы заглушить проблему теста, — худший трейд из всех: ты теряешь сеть от регрессов, и следующий баг скидки уедет незамеченным. Если он прямо сейчас должен перестать гейтить мёржи — отправь его в quarantine (всё ещё гоняется, всё ещё репортит) и почини, не выбрасывай ассерт." },
  ]}
/>

<Quiz
  id="contract-flaky-quiz-1"
  lessonSlug="03-contract-and-flaky"
  lang="ru"
  question="Provider переименовывает JSON-поле, которое читает один consumer. Какая проверка вероятнее всего поймает это до деплоя и почему?"
  choices={[
    { label: "Contract-верификация провайдера — она проигрывает записанную interaction consumer'а против настоящего хендлера и падает, потому что ожидаемого поля больше нет", correct: true },
    { label: "Собственные unit-тесты consumer'а", misconception: "Тесты consumer'а гоняются против mock'а, который проигрывает СТАРЫЕ записанные ответы, поэтому остаются зелёными — consumer не знает, что provider изменился. Поломка вылезает на стороне ПРОВАЙДЕРА, когда он верифицирует contract consumer'а против нового хендлера." },
    { label: "Общее ночное интеграционное окружение", misconception: "Может и поймает, но полное интеграционное окружение медленное, flaky и работает постфактум — ровно та хрупкая, низкодоверная проверка, которую contract testing и существует, чтобы заменить. И оно не гейтит деплой конкретной версии." },
    { label: "OpenAPI schema-линт", misconception: "Schema-линтинг может пометить, что спека изменилась, но не скажет, ломает ли изменение РЕАЛЬНОГО consumer'а — переименованное поле, которое никто не читает, ломающий schema-diff, но не бьёт ни одного contract'а. Contract'ы гейтят по реальной связанности, а не только по форме спеки." },
  ]}
/>

<Quiz
  id="contract-flaky-quiz-2"
  lessonSlug="03-contract-and-flaky"
  lang="ru"
  question="Unit-тест падает ~1 раз из 20 на неизменном коде. Коллега вешает авто-retry 3x, чтобы билд позеленел. В чём сеньорское возражение?"
  choices={[
    { label: "Retry прячет flaky вместо починки и так же охотно замаскирует реальный перемежающийся баг; unit-тест обязан быть детерминированным, поэтому причину (часы/общее состояние/порядок) надо убрать", correct: true },
    { label: "Три retry — мало, должно быть пять", misconception: "Больше retry делает маскировку хуже, не лучше. Проблема не в числе; проблема в том, что retry unit-теста вообще. Фикс — детерминизм, а не больше попыток выкатить проходящий прогон." },
    { label: "Retry тут норм; unit-тесты по своей природе flaky", misconception: "Unit-тесты НЕ flaky по природе — они гоняются в процессе без сети и реальных часов, поэтому flaky там всегда устранимый дефект (реальные часы, общее состояние или зависимость от порядка). Bounded retry — для e2e-тестов, никогда для unit." },
    { label: "Билд надо настроить так, чтобы игнорировать результат этого теста", misconception: "Игнорировать результат — это просто постоянный невидимый quarantine без тикета и фикса — он тихо роняет покрытие. Quarantine должен держать тест гоняющимся и репортящим, пока ты делаешь его детерминированным." },
  ]}
/>

</Explanation>

<KeyTakeaway>Каждый consumer пишет используемые пары как contract; provider верифицирует его, а can-i-deploy гейтит релиз — без общего окружения. Flaky подрывают доверие к green: чини причину, quarantine, retry только e2e.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="contract-flaky-retrieval"
  lang="ru"
  questions={[
    { q: "Пройди consumer-driven contract testing от начала до конца и скажи, почему он лучше общего интеграционного окружения.", a: "Каждый consumer, как побочный продукт собственных unit-тестов, записывает конкретные пары запрос/ответ, на которые реально опирается, — эти interaction'ы и есть contract. Тесты consumer'а гоняются против mock-провайдера, проигрывающего эти ответы, поэтому остаются быстрыми и изолированными. Consumer публикует contract в broker (Pact Broker или PactFlow), помеченный своей версией и целевым окружением. Pipeline провайдера затем тянет contract каждого consumer'а и верифицирует против настоящего хендлера, что может удовлетворить каждую interaction; результат верификации он публикует обратно в broker. Так broker держит матрицу того, какие пары версия-consumer'а/версия-провайдера доказанно совместимы, и can-i-deploy мгновенно запрашивает эту матрицу перед релизом. Это лучше общего интеграционного окружения по трём пунктам: оно никогда не запускает два сервиса вместе, поэтому быстрое и не магнит для flaky; каждая сторона доказывает совместимость в своём pipeline до мёржа, а не постфактум в staging; и оно отвечает на правильный вопрос — безопасна ли ЭТА версия для деплоя против всех, с кем общается, — вместо надежды, что снапшот staging зеркалит прод. Оно дополняет, а не заменяет schema/OpenAPI-проверки: schema-diff говорит, что спека изменилась ломающе, а contract — ломает ли изменение реального consumer'а." },
    { q: "Чем опасны flaky-тесты и какова правильная реакция — включая, когда retry допустимы?", a: "Flaky-тест проходит и падает на идентичном коде, что хуже стабильно красного, потому что red, который ничего не значит, приучает команду игнорировать red. Как только «да просто перезапусти» становится рефлексом, pipeline перестаёт быть gate'ом — реальный регресс прячется в шуме, и green-билд больше не значит «безопасно мёржить». Правильная реакция — три хода. Detect: перезапускай по расписанию и трекай pass/fail по каждому тесту, чтобы flaky была дашбордом, а не ощущением. Quarantine: помечай flaky-тест, чтобы он всё ещё гонялся и репортил, но не гейтил мёрж, и заводи тикет — это защищает сигнал pipeline'а без худшего варианта удалить тест. Fix: дотащи до детерминизма, назвав причину — тайминг/race (жди условие, никогда фиксированный sleep), реальные часы (внедри fake clock), общее состояние или зависимость от порядка (изолируй состояние на тест, рандомизируй порядок, чтобы вскрыть), реальная сеть (замокай границу), загрязнение тестов (сбрасывай global'ы в teardown). Bounded retry допустимы только на end-to-end тестах, где часть средовой flaky неустранима, и только когда каждый retry логируется и считается, чтобы flake-rate оставался видимым; никогда не делай retry unit- или интеграционного теста, потому что там retry лишь прячет устранимый дефект и может замаскировать настоящий перемежающийся баг." },
  ]}
/>

<Recap lang="ru">
Независимо деплоящиеся сервисы ломают друг друга через API, и полное интеграционное окружение — не тот инструмент, чтобы это ловить: медленное, склонное к flaky, и оно тестирует, совместим ли какой-то снапшот staging между собой, а не безопасна ли версия, которую ты вот-вот отгрузишь. Consumer-driven contract testing отвечает на острый вопрос вместо этого — каждый consumer записывает пары запрос/ответ, которые реально использует, как contract, гоняет свои тесты против mock'а и публикует contract в broker; provider тянет каждый contract и верифицирует его против настоящего хендлера в своём pipeline, публикуя результат обратно. Матрица верификации broker'а питает can-i-deploy, мгновенный запрос, который гейтит релиз только когда каждая релевантная пара доказанно совместима. Contract'ы дополняют schema/OpenAPI-линтинг: схема говорит, что спека изменилась, contract — ломает ли это реального consumer'а. Вторая половина — доверие: flaky-тест, который проходит и падает на одном коде, приучает людей игнорировать red, и как только «да просто перезапусти» становится рефлексом, green больше не значит «безопасно мёржить». Детектируй flaky прогонами по расписанию и дашбордом, отправляй flaky-тест в quarantine, чтобы он всё ещё репортил, но не гейтил, пока заводишь тикет и чинишь, и делай его детерминированным, убивая причину — fake clock, изолированное состояние, контролируемый порядок, замоканные границы. Резервируй bounded retry только для end-to-end тестов, всегда с логом; retry на unit-тесте лишь прячет устранимый дефект и может спрятать реальный баг вместе с ним.
</Recap>
