---
concepts:
- test-pyramid
- unit-integration-e2e
- ice-cream-cone-antipattern
- branch-protection
- required-status-checks
- flaky-quarantine
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-test-pyramid-and-gates
sources:
- https://martinfowler.com/articles/practical-test-pyramid.html
- https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches/about-protected-branches
- https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches/about-protected-branches#require-status-checks-before-merging
spiral: []
status: ready
summary: 'Сформируй набор пирамидой — много быстрых unit-тестов, меньше integration, тонкий слой e2e — и делай блокирующими мерж только быстрые, надёжные проверки через branch protection. Переверни любое — и gate станет медленным, flaky и недоверенным.'
title: 'Тест-пирамида и обязательные проверки'
track: ci-cd
unit: 02-testing-in-ci
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
У команды было 600 end-to-end тестов и 40 unit-тестов. CI шёл 38 минут, падал примерно на каждом третьем прогоне, и никто не верил красной сборке — стандартный совет был «просто перезапусти». Два браузерных теста падали неделями; вместо того чтобы их чинить, кто-то пометил всю e2e-джобу *информационной*, чтобы мержи проходили. Gate стал декоративным. Набор был перевёрнутой пирамидой, и цена платилась на каждом pull request.
</Hook>

<Crux>Пирамида — много unit, меньше integration, мало e2e — и гейти мерж только на быстрых надёжных проверках; переверни — gate станет flaky.</Crux>

<Explanation>

## Пирамида: гранулярность, купленная скоростью и доверием

Набор тестов в CI — это портфель, и тест-пирамида («Practical Test Pyramid» Хэма Воке, продолжение идеи Майка Кона) говорит, как его взвесить: **много** мелких unit-тестов в основании, **некоторое количество** грубых integration-тестов в середине и **очень мало** высокоуровневых end-to-end тестов наверху. Форма кодирует трейдофф, который по мере подъёма идёт в противоположные стороны.

В **основании** unit-тесты гоняют одну функцию, класс или модуль в изоляции, с поддельными или застабленными зависимостями. Они выполняются за миллисекунды, падают с точным стек-трейсом и никогда не трогают сеть или диск — поэтому ты можешь позволить себе тысячи и гонять их на каждое сохранение. Их слабость — достоверность: unit-тест проходит против твоей *модели* мира, а не реальной базы или реального API, поэтому он может быть зелёным, пока интеграция сломана.

В **середине** integration-тесты проверяют, что две реальные части корректно общаются — твой код против реального Postgres в контейнере, реальный HTTP-вызов к застабленной, но точной по протоколу зависимости, ORM против реальной схемы. Они ловят баги, которые unit пропускает (неверный SQL, рассинхрон сериализации, дрейф миграций), ценой секунд на каждый и более тяжёлого сетапа.

Наверху end-to-end тесты гоняют всю систему так, как это делает пользователь — браузер, сервер приложения, база, всё вместе. Они дают наивысшее доверие, что штука реально работает, и они медленные, дорогие и, как пишет Воке, «печально известны своей flaky-природой и часто падают по неожиданным и непредсказуемым причинам». Именно поэтому слой должен оставаться тонким.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Слой</th>
        <th class="border border-border px-3 py-2 text-left">Охват</th>
        <th class="border border-border px-3 py-2 text-left">Скорость</th>
        <th class="border border-border px-3 py-2 text-left">Относительное число</th>
        <th class="border border-border px-3 py-2 text-left">Гонять в CI когда</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Unit</strong></td>
        <td class="border border-border px-3 py-2">Одна функция/модуль, зависимости поддельные</td>
        <td class="border border-border px-3 py-2">мс — тысячи за секунды</td>
        <td class="border border-border px-3 py-2">Много (широкое основание)</td>
        <td class="border border-border px-3 py-2">Каждый push; fail-fast первыми</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Integration</strong></td>
        <td class="border border-border px-3 py-2">Две реальные части (код + БД, API + протокол)</td>
        <td class="border border-border px-3 py-2">~секунды на каждый</td>
        <td class="border border-border px-3 py-2">Меньше (середина)</td>
        <td class="border border-border px-3 py-2">Каждый push; после прохода unit</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>End-to-end</strong></td>
        <td class="border border-border px-3 py-2">Вся система через UI</td>
        <td class="border border-border px-3 py-2">~десятки секунд до минут</td>
        <td class="border border-border px-3 py-2">Мало (тонкая верхушка)</td>
        <td class="border border-border px-3 py-2">Только критичные пути; flaky в карантин</td>
      </tr>
    </tbody>
  </table>
</div>

Градиент частоты — это вся игра: доверие растёт по мере подъёма, но вместе с ним растут время прогона, flakiness и цена отладки. Большую часть безопасности ты покупаешь дёшево в основании и резервируешь дорогую верхушку для горстки критичных пользовательских сценариев — оформление заказа работает, логин работает — которые оправдывают цену.

<Inset kind="why" lang="ru">
Процент coverage — слабый прокси безопасности. Он измеряет, какие *строки* выполнились во время тестов, а не то, проверил ли ты правильное *поведение*. Тест, который зовёт функцию и никогда не проверяет её вывод, может поднять coverage до 90%, ничего не доказав; и наоборот, маленький острый тест на ту единственную ветку, что ломается в проде, ценнее сотни строк, покрытых случайно. Совет Воке: «Проверяй наблюдаемое поведение» — утверждай, что входы x и y дают результат z, а не что вызвался конкретный приватный метод. Гонись за поведением, а не за числом.
</Inset>

## Ice-cream cone: почему переворот пирамиды бьёт больно

Переверни пирамиду — получишь **рожок мороженого** (ice-cream cone): тонкий шарик unit-тестов под жирной выпуклостью e2e-тестов — форма из хука. Это кажется безопасным («мы тестируем всё приложение так, как им пользуются!») и это ловушка, о которой Воке прямо предупреждает: она будет «кошмаром в поддержке и слишком долго гоняется».

Три цены складываются. **Медленно:** end-to-end тесты доминируют по часам на стене, поэтому фидбэк, который должен прийти за минуту, приходит за сорок — а медленный фидбэк — это ровно то, что CI существует предотвращать. **Flaky:** чем больше системы охватывает тест, тем больше движущихся частей могут икнуть — медленный рендер, гонка, сетевой сбой — поэтому падения перестают коррелировать с реальными багами. Как только красная сборка с равной вероятностью шум или сигнал, люди начинают перезапускать на красном, и набор теряет свою единственную работу. **Тяжело отлаживать:** упавший unit-тест указывает на одну функцию; упавший e2e-тест значит «где-то, в чём-то из пяти сервисов, что-то пошло не так» — ты получаешь скриншот и таймаут, а не стек-трейс.

Фикс — не удалять e2e-тесты, а **спускать утверждения вниз**. Большую часть того, что проверяет широкий браузерный тест — правила валидации, форматирование, обработку ошибок, граничные случаи — можно доказать быстрым unit- или integration-тестом. Оставь e2e за незаменимым, что он один доказывает: куски склеены вместе и критичный сценарий проходит до конца.

## Gates: как сделать нужные проверки блокирующими мерж

Зелёный набор бесполезен, если кто угодно может смержить красное. Механизм принуждения в GitHub — **branch protection**: правило на `main`, которое блокирует прямые пуши и прогоняет изменения через pull request'ы, удовлетворяющие условиям. Рычаги, важные для CI:

- **Required status checks** — именованные проверки (твоя unit-джоба, твоя integration-джоба), которые «должны иметь статус successful, skipped или neutral, прежде чем коллабораторы смогут внести изменения в защищённую ветку». Проверка, которой *нет* в списке обязательных, всё равно гоняется и репортит, но она **информационная**: она не может блокировать мерж. Это регулятор, которым ты решаешь, что несущее.
- **Require branches to be up to date before merging** — настройка **strict**. С ней PR должен быть перебазирован на свежую базу и заново пройти CI перед мержем, что закрывает дыру *семантического конфликта мержа* (два PR, каждый проходит в одиночку, но ломаются вместе). Цена — больше перезапусков; на нагруженном репозитории strict может стать бутылочным горлышком merge-очереди.
- **Required reviews** — N одобряющих ревью перед мержем, опционально со сбросом устаревших одобрений при новых коммитах.

Проектное решение — *какие* проверки делать блокирующими. Правило большого пальца: **гейти на проверках, которые быстрые и надёжные; держи медленные или flaky проверки информационными.** Твои unit- и integration-джобы обязательны. Flaky полный e2e-набор *не* должен быть обязательным — гейтить на нём значит, что недетерминированный тест может блокировать каждый мерж в организации, и «фикс», к которому тянутся люди — отключить gate, ровно как в хуке.

<TradeoffMatrix
  id="ci-test-pyramid-tradeoff-1"
  pieceSlug="01-test-pyramid-and-gates"
  lang="ru"
  prompt="9-минутный e2e-набор падает на ~15% прогонов из-за тайминговой flakiness, блокируя несвязанные PR. Что с ним делать как с required-проверкой?"
  options={[
    { name: "Отправить flaky e2e-тесты в карантин и держать required только маленький стабильный smoke-набор; трекать flakiness и чинить", summary: "Гейти на надёжном.", correct: true, justification: "Это ход сеньора. Required-проверка должна быть и быстрой, и доверенной; набор с 15% падений — ни то, ни другое, поэтому гейтить на нём значит, что недетерминированный шум блокирует реальную работу, и люди учатся игнорировать или отключать gate. Перенеси flaky-тесты в неблокирующую карантинную полосу, которая всё ещё гоняется и репортит (так ты сохраняешь сигнал и дашборд flakiness), и сделай required только маленький детерминированный smoke-подмножество, доказывающее, что критичный сценарий склеен. Карантинные тесты чинишь по своему графику, а не под давлением мержа." },
    { name: "Держать весь e2e-набор required и велеть всем перезапускать на красном", summary: "Перезапуск до зелёного.", justification: "Это режим отказа, о котором предупреждает урок. «Перезапуск на красном» приучает команду, что красная сборка ничего не значит, что уничтожает всю ценность гейта: как только падения с равной вероятностью шум или сигнал, настоящая регрессия проскальзывает незамеченной. Ты платишь за тесты и получаешь отрицательное доверие." },
    { name: "Сделать e2e-набор только информационным и не требовать больше ничего", summary: "Убрать все гейты.", justification: "Понизить flaky-набор правильно, но не требовать ничего оставляет ветку незащищённой — кто угодно может смержить код, проваливший быстрые надёжные unit- и integration-джобы. Суть в выборочном гейтинге: держать доверенные проверки блокирующими, а не отменять гейтинг целиком." },
    { name: "Поднять required-ревью до 3, чтобы люди ловили то, что flaky-тесты не могут", summary: "Больше глаз.", justification: "Ревью ловят проблемы дизайна и логики, а не тайминговую flakiness в браузерном наборе, а нагромождение обязательных одобрений просто тормозит всех, не решая реальную проблему: ненадёжная проверка в блокирующем пути. Чини или карантинь тесты; не проси людей компенсировать недетерминизм." },
  ]}
/>

<Quiz
  id="ci-test-pyramid-quiz-1"
  lessonSlug="01-test-pyramid-and-gates"
  lang="ru"
  question="У сервиса 95% line coverage и зелёный прогон CI, но баг сериализации уезжает в прод. Как это возможно?"
  choices={[
    { label: "Coverage измеряет, какие строки выполнились во время тестов, а не проверено ли правильное поведение — а unit-тест проходит против подделки, так что реальный рассинхрон по протоколу/БД может проскользнуть на integration- или e2e-слой", correct: true },
    { label: "95% coverage слишком мало; нужно 100%, чтобы быть в безопасности", misconception: "100% coverage всё равно доказывает лишь, что каждая строка выполнилась, а не что ты проверил правильный вывод. Coverage — слабый прокси: гонись за поведением, а не за числом. Набор со 100% покрытием, набитый тестами без утверждений, так же слеп." },
    { label: "Зелёный CI гарантирует корректность, значит сломана система сборки", misconception: "«Всё зелёное» — не то же, что «реально безопасно». Зелёное значит, что каждый написанный тест прошёл; оно ничего не говорит о поведении, которое ты забыл протестировать, или о слое (реальная БД/протокол), который твои unit-тесты подделали." },
    { label: "Сериализацию вообще нельзя покрыть unit-тестом", misconception: "Можно — но unit-тест мокает границу, поэтому валидирует твою модель сериализации, а не реальную. Ловить рассинхрон по протоколу/БД — ровно работа integration-слоя в середине пирамиды." },
  ]}
/>

<Quiz
  id="ci-test-pyramid-quiz-2"
  lessonSlug="01-test-pyramid-and-gates"
  lang="ru"
  question="На защищённой ветке какая конфигурация И блокирует мерж сломанного кода, И не даёт flaky-тесту блокировать каждый PR?"
  choices={[
    { label: "Пометить быстрые unit- и integration-джобы как required status checks; держать flaky полный e2e-набор гоняющимся, но информационным (не required), с маленьким стабильным smoke-тестом в required", correct: true },
    { label: "Сделать required каждую существующую проверку, включая flaky e2e-набор", misconception: "Сделать required flaky-проверку — значит поставить недетерминизм в блокирующий путь: одна тайминговая икота блокирует несвязанные PR по всему репозиторию, и аварийный выход команды — отключить gate. Гейти только на быстрых, надёжных проверках." },
    { label: "Не требовать ничего и полагаться на ревьюеров, чтобы заметить падения", misconception: "Без required status checks код, проваливший unit- и integration-джобы, можно смержить в момент, когда человек одобрил. Работа гейта — сделать доверенные проверки обязательными, а не опциональными." },
    { label: "Включить «require branches to be up to date» и больше ничего", misconception: "Strict up-to-date форсит перезапуски против свежей базы, но без required-проверок этим перезапускам нечего принуждать. Up-to-date дополняет required-проверки, а не заменяет их." },
  ]}
/>

</Explanation>

<StackDiagram
  label="Тест-пирамида: широкое основание из множества быстрых unit-тестов, более узкая середина из integration-тестов и тонкая верхушка из end-to-end тестов, с ростом доверия и цены к вершине"
  hue="var(--d-systems)"
  layers={[
    { label: "End-to-end (мало)", note: "вся система через UI — медленно, flaky → гейти только smoke-подмножество" },
    { label: "Integration (меньше)", note: "две реальные части: код + БД/протокол — секунды → required" },
    { label: "Unit (много)", note: "один модуль, подделки — мс, fail-fast → required" },
  ]}
  caption="Подъём по пирамиде меняет скорость и надёжность на доверие. Много дешёвых unit-тестов в основании, тонкая e2e-верхушка наверху. Переверни в ice-cream cone — и набор станет медленным, flaky и недоверенным, а gate, построенный на нём, придётся отключить."
/>

<KeyTakeaway>Строй пирамиду: много быстрых unit, меньше integration, тонкая e2e-верхушка — тестируй поведение, а не coverage %. Гейти только на быстрых доверенных проверках; flaky e2e в карантин.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="ci-test-pyramid-retrieval"
  lang="ru"
  questions={[
    { q: "Почему ice-cream cone (в основном e2e-тесты) делает CI-набор медленным, flaky и недоверенным — и в чём фикс?", a: "Пирамида взвешивает тесты по гранулярности: много быстрых unit-тестов в основании, меньше integration-тестов в середине, тонкая верхушка end-to-end тестов наверху, потому что доверие растёт по мере подъёма, но вместе с ним растут время прогона, flakiness и цена отладки. Ice-cream cone переворачивает это — тонкий шарик unit-тестов под жирной выпуклостью e2e-тестов — поэтому все три цены складываются. Медленно, потому что end-to-end тесты доминируют по часам на стене, превращая минутный фидбэк в сорокаминутный. Flaky, потому что чем больше системы охватывает тест, тем больше движущихся частей (рендеры, гонки, сетевые сбои) могут икнуть, поэтому падения перестают коррелировать с реальными багами; как только красная сборка с равной вероятностью шум или сигнал, люди перезапускают на красном, и набор теряет свою единственную работу. Тяжело отлаживать, потому что упавший e2e-тест даёт скриншот и таймаут вместо стек-трейса на одну функцию. Фикс — спускать утверждения вниз: доказывай валидацию, форматирование и граничные случаи быстрыми unit- и integration-тестами, а e2e оставь только за незаменимым, что он один доказывает — что куски склеены вместе и критичный сценарий проходит до конца." },
    { q: "Что такое branch protection и как решать, какие проверки делать required, а какие информационными?", a: "Branch protection — правило GitHub на ветке вроде main, которое блокирует прямые пуши и прогоняет изменения через pull request'ы, удовлетворяющие условиям. Важные для CI рычаги — required status checks (именованные проверки, которые должны иметь статус successful, skipped или neutral, прежде чем кто-либо сможет смержить), «require branches to be up to date before merging» (настройка strict, которая форсит перебазирование и перепрогон против свежей базы, чтобы два независимо проходящих PR не сложились в поломку) и required reviews. Проверка, которой нет в списке required, всё равно гоняется и репортит, но она лишь информационная — она не может блокировать мерж. Правило решения: гейти на проверках, которые быстрые и доверенные, и держи медленные или flaky проверки информационными. Твои unit- и integration-джобы обязательны, потому что они детерминированные и быстрые. Flaky полный e2e-набор не должен быть required, потому что гейтить на недетерминированном тесте значит, что одна тайминговая икота блокирует каждый мерж в организации, и аварийный выход команды — отключить gate. Вместо этого отправь flaky-тесты в неблокирующую карантинную полосу, которая всё ещё гоняется, и сделай required только маленькое стабильное smoke-подмножество. Держать CI быстрым — параллелить, шардировать, fail-fast — тоже важно, чтобы gate не стал бутылочным горлышком." },
  ]}
/>

<Recap lang="ru">
Тест-пирамида взвешивает CI-набор по гранулярности: широкое основание из множества быстрых unit-тестов, изолирующих один модуль подделками, более узкая середина из integration-тестов, проверяющих две реальные части (твой код против реальной базы или точной по протоколу зависимости), и тонкая верхушка из медленных, дорогих end-to-end тестов, гоняющих всю систему. Доверие растёт по мере подъёма, но вместе с ним — время прогона, flakiness и цена отладки, поэтому большую часть безопасности ты покупаешь дёшево в основании и резервируешь верхушку для горстки критичных сценариев. Переворот в ice-cream cone делает набор медленным, flaky и недоверенным, и фикс — спускать утверждения вниз, а не удалять e2e. Помни, что процент coverage измеряет выполненные строки, а не проверенное поведение — тестируй то, что система делает, а не какие строки выполнились, потому что «всё зелёное» — не то же, что «реально безопасно». Гейтинг — это то, что делает зелёный набор значимым: branch protection с required status checks блокирует мерж кода, проваливающего быстрые надёжные джобы, настройка strict up-to-date закрывает дыру семантического конфликта, и блокирующими ты делаешь только доверенные проверки. Flaky e2e-набору место в неблокирующей карантинной полосе с маленьким required smoke-подмножеством, а не в gate мержа — и CI ты держишь быстрым параллелизмом, шардированием и fail-fast, чтобы gate никогда не стал бутылочным горлышком.
</Recap>
