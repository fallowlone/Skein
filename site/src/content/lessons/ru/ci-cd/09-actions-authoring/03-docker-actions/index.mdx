---
concepts:
- ci-cd-gha-docker-actions
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 3
prereqs: []
slug: 03-docker-actions
sources:
- https://docs.github.com/en/actions/sharing-automations/creating-actions/creating-a-docker-container-action
- https://docs.github.com/en/actions/sharing-automations/creating-actions/dockerfile-support-for-github-actions
- https://docs.github.com/en/actions/sharing-automations/creating-actions/metadata-syntax-for-github-actions
spiral: []
status: ready
summary: 'Docker-экшен упаковывает всё своё окружение в контейнер — любой язык, любую системную зависимость — но работает только на Linux-раннерах и платит 30-90с pull или сборку образа за прогон против composite или JS-экшена в миллисекундах.'
title: 'Docker-экшены: полный контроль окружения ценой только-Linux и накладных образа'
track: ci-cd
unit: 09-actions-authoring
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Команде нужен был релизный экшен, запускавший Go-бинарь, `cosign` и запиненную версию `skopeo` — тулчейн, которого не было npm-пакетами и который означал бы скриптинг хрупких загрузок в composite-экшене. Так что они потянулись к Docker-экшену: `Dockerfile`, который `FROM`-ил известную базу, ставил точные инструменты и запускал entrypoint. Работало идеально. Затем они попробовали добавить экшен в воркфлоу, что бежал на `macos-latest` ради отдельного требования подписи, и джоба упала до запуска entrypoint: *Docker-контейнерные экшены поддерживаются только на Linux-раннерах.* А на Linux-джобах экшен, что в тестах казался быстрым, теперь добавлял плоские 40-70 секунд к каждому прогону — раннер собирал образ из `Dockerfile` на каждом вызове, ведь ничто его не пресобирало. Контейнер купил им произвольное окружение и взял с них стартовый налог произвольного окружения и привязку к ОС на каждом единственном прогоне.
</Hook>

<Crux>Docker-экшен отгружает всё окружение в контейнере, но работает только на Linux и платит 30-90с pull или сборку образа за прогон.</Crux>

<Explanation>

## Полный контроль окружения

Docker-экшен объявляет `runs.using: "docker"` и либо `image: "Dockerfile"` (собирается на раннере), либо `image: "docker://ghcr.io/org/img:tag"` (тянется пресобранным). Внутри контейнера вы контролируете *всё* — базовую ОС, системные библиотеки, любой языковой рантайм, точные версии инструментов — что не могут дать composite- и JavaScript-экшены. Если вашему экшену нужен `cosign`, конкретный `glibc`, скомпилированный бинарь Go или Rust или инструмент без npm-эквивалента, контейнер — единственный тип, упаковывающий это воспроизводимо:

```dockerfile
FROM alpine:3.20
RUN apk add --no-cache cosign skopeo
COPY entrypoint.sh /entrypoint.sh
RUN chmod +x /entrypoint.sh
ENTRYPOINT ["/entrypoint.sh"]
```

```yaml
# action.yml
runs:
  using: "docker"
  image: "Dockerfile"
  args:
    - ${{ inputs.tag }}
  env:
    REGISTRY_TOKEN: ${{ inputs.token }}
```

Контракт рантайма отличается от JavaScript-экшена. **Inputs приходят как переменные окружения `INPUT_*`** в контейнере (input `tag` становится `INPUT_TAG`), и их можно также передать позиционно через `args:`. **Outputs пишутся дозаписью в файл, названный `$GITHUB_OUTPUT`** — `echo "result=ok" >> "$GITHUB_OUTPUT"` изнутри entrypoint — а рабочий каталог bind-монтируется в `$GITHUB_WORKSPACE`. Никакого toolkit; вы говорите на протоколе напрямую через env-переменные и эти файлы.

<Quiz
  id="03-docker-actions-quiz-1"
  lessonSlug="03-docker-actions"
  lang="ru"
  question="Команда добавляет рабочий Docker-контейнерный экшен в джобу, что бежит на `runs-on: macos-latest`. Джоба падает до выполнения entrypoint экшена. В чём причина?"
  choices={[
    { label: "Docker-контейнерные экшены поддерживаются только на Linux-раннерах — они не могут работать на macOS или Windows-раннерах", correct: true },
    { label: "Dockerfile должен объявить `FROM macos`, чтобы работать на macOS-раннере", misconception: "Нет macOS Docker-базового образа для этого, и ОС раннера не переключается базовым образом. Docker-контейнерные экшены просто вообще не работают на macOS-раннерах — только Linux." },
    { label: "macOS-раннерам нужен `runs.using: \"docker-mac\"` в action.yml", misconception: "Такого рантайма не существует. Ограничение жёсткое: Docker-контейнерные экшены работают только на Linux-раннерах; для кросс-ОС нужен composite- или JavaScript-экшен." },
    { label: "Образ нужно пред-запушить в реестр, прежде чем macOS сможет его вытянуть", misconception: "Пред-пуш меняет латентность pull-vs-build, а не поддержку ОС. Даже пред-запушенный образ не может работать как Docker-экшен на macOS-раннере — платформа этого не поддерживает." },
  ]}
/>

## Цена: только-Linux и накладные образа

У силы контейнера две цены, обе платятся на каждом прогоне. Первая: **Docker-экшены работают только на Linux-раннерах** — не macOS, не Windows. Экшен, что вы хотите пригодным везде, не может быть Docker-экшеном; одно это толкает многие переиспользуемые экшены к JavaScript. Вторая: **контейнер должен материализоваться до запуска вашего кода**. С `image: "Dockerfile"` раннер *собирает* образ на каждом вызове, если слои не закэшированы — обычно 40-90 секунд. С `image: "docker://..."` он *тянет* пресобранный образ — быстрее, но всё равно десятки секунд для холодного pull нетривиального образа. Сравните с composite (миллисекунды) или JavaScript-экшеном (субсекундный старт node): Docker-экшен добавляет плоский попрогонный налог, невидимый в долгой джобе и жестокий в быстрой, триггерящейся сотни раз в день.

Митигация — **опубликовать пресобранный образ в реестр** (GHCR) и сослаться на него через `docker://`, превращая попрогонную *сборку* в попрогонный *pull* и давая кэшированию слоёв помочь. Но это добавляет релизный пайплайн для самого образа. Две заметки о безопасности наследуются из мира контейнеров и значат здесь больше, чем для других типов: **пиньте базовый образ по digest** (`FROM alpine:3.20@sha256:...`), чтобы сборка была воспроизводимой и пере-тегированная апстрим-база не могла молча изменить ваш экшен; и пиньте любые экшены, *что ваш воркфлоу использует для вызова этого*, по commit SHA — та же supply-chain-гигиена, что у других типов. Правило решения резкое: выбирайте Docker, только когда вам по-настоящему нужно окружение, что composite и JavaScript дать не могут, и примите только-Linux плюс стартовый налог как цену этого контроля.

<Quiz
  id="03-docker-actions-quiz-2"
  lessonSlug="03-docker-actions"
  lang="ru"
  question="Docker-экшен с `image: \"Dockerfile\"` добавляет ~60 секунд к каждому прогону воркфлоу, что срабатывает сотни раз в день. Логика контейнера быстрая. Какой самый эффективный фикс, сохраняющий его Docker-экшеном?"
  choices={[
    { label: "Пресобрать и опубликовать образ в реестр и сослаться через `image: \"docker://...\"`, превращая попрогонную сборку в более быстрый попрогонный pull, выигрывающий от кэширования слоёв", correct: true },
    { label: "Переключить `runs.using` на `node20`, чтобы убрать накладные контейнера", misconception: "Это перестаёт быть Docker-экшеном вовсе — отбрасывает произвольное окружение, что было причиной использовать Docker. Вопрос просит сохранить его Docker-экшеном; фикс — пресборка образа, а не смена типа." },
    { label: "Добавить больше слоёв `RUN`, чтобы Docker их закэшировал и сборка стала мгновенной", misconception: "Больше слоёв не сделают сборку с нуля на каждом прогоне мгновенной, а на свежем раннере кэш слоёв холодный. Реальный рычаг — pull пресобранного опубликованного образа вместо сборки на каждом прогоне." },
    { label: "Задать `args:`, чтобы пропустить шаг сборки образа", misconception: "args: передаёт позиционные inputs в entrypoint — это никак не связано с тем, собирается ли образ. Сборка происходит в любом случае; фикс — ссылка на пресобранный docker://-образ." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Выбор типа экшена по нужному окружению и цене, что он платит"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "need", label: "Нужно произвольное окружение?", sub: "не-npm инструменты, образ любой ОС" },
    { id: "docker", label: "Docker-экшен", sub: "using: docker" },
    { id: "build", label: "Сборка Dockerfile", sub: "~40-90с за прогон" },
    { id: "pull", label: "Pull docker://", sub: "~десятки с, холодный" },
    { id: "linux", label: "Только Linux-раннеры", sub: "нет macOS/Windows" },
    { id: "else", label: "Composite / JS", sub: "мс / субсекунда, кросс-ОС" },
  ]}
  edges={[
    { from: "need", to: "docker", label: "да" },
    { from: "need", to: "else", label: "нет" },
    { from: "docker", to: "build", label: "image: Dockerfile" },
    { from: "docker", to: "pull", label: "image: docker://" },
    { from: "docker", to: "linux", label: "лимит платформы" },
  ]}
  caption="Docker — тип, что выбираешь, лишь когда нужно окружение, что другие дать не могут — и цена это только-Linux плюс попрогонная сборка или pull образа против миллисекундно-субсекундного старта composite и JS-экшенов."
/>

<KeyTakeaway>Docker-экшен (`runs.using: "docker"`) упаковывает всё окружение — любую базовую ОС, системную библиотеку, языковой рантайм или не-npm инструмент — в контейнер, что есть контроль, которого composite и JavaScript-экшены дать не могут. Цена платится каждый прогон: он работает **только на Linux-раннерах**, и образ должен сначала материализоваться — попрогонная сборка из `image: "Dockerfile"` (обычно 40-90с) или pull из `image: "docker://..."` (десятки секунд холодный) против миллисекунд composite и субсекундного старта JS-экшена. Inputs приходят как env-переменные `INPUT_*` (без toolkit), outputs дозаписываются в `$GITHUB_OUTPUT`. Митигируй налог публикацией пресобранного образа и digest-пиннингом базы; выбирай Docker, лишь когда тебе по-настоящему нужно его окружение.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-docker-actions-retrieval"
  lang="ru"
  questions={[
    { q: "Какая уникальная способность Docker-экшена и две повторяющиеся цены, что с ней приходят?", a: "Его уникальная способность — полный контроль окружения: экшен отгружает свой контейнер, так что может использовать любую базовую ОС, любую системную библиотеку, любой языковой рантайм и инструменты без npm-эквивалента (cosign, конкретный glibc, скомпилированный Go/Rust-бинарь) — воспроизводимо. Composite- и JavaScript-экшены не могут так упаковать произвольное окружение. Две цены, платимые на каждом прогоне: (1) он работает только на Linux-раннерах — никогда macOS или Windows — так что экшен, что должен работать везде, не может быть Docker; и (2) образ должен материализоваться до запуска кода — попрогонная сборка из `image: \"Dockerfile\"` (обычно 40-90 секунд) или pull из `image: \"docker://...\"` (десятки секунд для холодного нетривиального образа) против миллисекунд composite и субсекундного старта node у JS-экшена." },
    { q: "Как работают inputs и outputs у Docker-экшена и как снизить его попрогонную стоимость старта?", a: "В отличие от JavaScript-экшена, @actions/toolkit нет. Inputs приходят как переменные окружения с именами `INPUT_<NAME>` внутри контейнера (input `tag` -> `INPUT_TAG`), и могут также передаваться позиционно через список `args:` в action.yml. Outputs пишутся дозаписью строк `name=value` в путь файла в `$GITHUB_OUTPUT` из entrypoint (напр. `echo \"result=ok\" >> \"$GITHUB_OUTPUT\"`); репо bind-монтируется в `$GITHUB_WORKSPACE`. Чтобы срезать стоимость старта, опубликуй пресобранный образ в реестр вроде GHCR и сошлись через `image: \"docker://ghcr.io/org/img:tag\"`, чтобы каждый прогон тянул вместо сборки и выигрывал от кэширования слоёв — и пинь базовый образ по digest (`FROM alpine:3.20@sha256:...`), чтобы сборка оставалась воспроизводимой и пере-тегированная апстрим-база не могла молча изменить экшен." },
  ]}
/>

<Recap lang="ru">
Docker-экшен ставит `runs.using: "docker"` и указывает `image:` либо на `Dockerfile` (собирается на раннере), либо на `docker://`-ссылку на пресобранный образ, и внутри этого контейнера вы контролируете всё окружение: базовую ОС, системные библиотеки, любой языковой рантайм, точные версии инструментов и инструменты без npm-эквивалента. Это та единственная способность, что composite- и JavaScript-экшены структурно дать не могут, и единственная причина выбрать Docker. Цена взимается на каждом единственном прогоне и приходит в двух частях. Первая: Docker-контейнерные экшены работают только на Linux-раннерах — не macOS, не Windows — так что экшен, задуманный пригодным везде, не может быть Docker-экшеном, что одно толкает большинство универсальных переиспользуемых экшенов к JavaScript. Вторая: образ должен материализоваться до запуска вашего entrypoint: сборка из `image: "Dockerfile"` добавляет примерно 40-90 секунд за прогон, если не закэширована, и даже пресобранный `docker://`-образ стоит десятки секунд на холодном pull — плоский налог, что исчезает внутри долгой джобы и доминирует в быстрой, триггерящейся сотни раз в день, против миллисекунд composite и субсекундного старта JS-экшена. Протокол рантайма голый: inputs приходят как env-переменные `INPUT_*` (и опциональные позиционные `args:`), outputs дозаписываются в `$GITHUB_OUTPUT`, репо монтируется в `$GITHUB_WORKSPACE`, и toolkit нет. Митигируй стартовый налог публикацией пресобранного образа в реестр и ссылкой через `docker://`, превращая попрогонную сборку в попрогонный pull с кэшированием слоёв, и пинь базовый образ по digest, чтобы сборка была воспроизводимой и пере-тегированная апстрим-база не могла молча изменить ваш экшен. Правило решения: выбирай Docker, лишь когда тебе по-настоящему нужно окружение, что другие типы дать не могут, и прими только-Linux плюс накладные образа как сознательную цену этого контроля.
</Recap>
