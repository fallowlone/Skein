---
concepts:
- ci-cd-gha-token-perms
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 1
prereqs: []
slug: "01-github-token-and-permissions"
sources:
- https://docs.github.com/en/actions/security-for-github-actions/security-guides/automatic-token-authentication
- https://docs.github.com/en/actions/security-for-github-actions/security-guides/security-hardening-for-github-actions
spiral: []
status: ready
title: "GITHUB_TOKEN: дефолтные права, least privilege и почему write-all — это footgun"
track: "ci-cd"
unit: "10-security-and-oidc"
summary: "Каждый job получает авто-минченный GITHUB_TOKEN, чей дефолтный scope может быть write-all на репо. Урезай его per-job через permissions до contents:read и поднимай лишь тот job, кому надо."
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Инцидент начался с Action для форматирования комментариев, который никто не запинил. Он запускался на каждом pull request, включая PR из форков, и в репо никогда не задавали блок `permissions:` — так что авто-минченный `GITHUB_TOKEN` приходил с легаси-дефолтом: запись почти на всё в репо. У этого Action была транзитивная зависимость, которую тремя релизами раньше тихо перевыпустил скомпрометированный аккаунт мейнтейнера. На следующем форк-PR эта зависимость использовала выданный ей токен, чтобы запушить коммит в релиз-ветку и открыть тег. Ни секрет не украли, ни пароль не сфишили. CI просто сделал ровно то, что разрешал его токен, а токен разрешал всё. Фикс — четыре строки в начале воркфлоу: `permissions: contents: read`. Однострочник пост-мортема: радиус поражения от supply-chain-компрометации в CI — это всё, что может токен job, а дефолтный токен мог почти всё.
</Hook>

<Crux>GITHUB_TOKEN авто-минтится на job и может дефолтиться в write-all; ограничь permissions по least privilege, чтобы сжать радиус поражения.</Crux>

<Explanation>

## Что это за токен и откуда его scope

В начале каждого job GitHub минтит свежий installation access token и отдаёт его как `secrets.GITHUB_TOKEN` (и `github.token`). Он привязан к текущему репо и истекает по окончании job — максимум на время прогона, никогда не сохраняется. Это хорошая новость. Плохая — его *широта*. У токена набор scope-прав — `contents`, `pull-requests`, `issues`, `packages`, `actions`, `id-token` и другие — каждый независимо `read`, `write` или `none`.

Откуда дефолтятся эти scope? Два слоя. Исторически репозитории создавались с **пермиссивным дефолтом**: токен получал read/write почти на все scope (`contents: write`, `issues: write`, `pull-requests: write`, …). Позже GitHub дал орг-ам переключить настройку орг/репо на **ограниченный дефолт** (`contents: read`, остальное `none`), и новые орги всё чаще стартуют с него — но полагаться на это нельзя. Единственный scope, что ты контролируешь наверняка, — тот, что пишешь сам:

```yaml
permissions:
  contents: read          # склонировать репо, и не более
```

Объяви `permissions:` — и каждый неперечисленный scope падает в `none`. Этот единственный блок превращает ambient write-all токен в read-only независимо от дефолтной настройки репо.

```yaml
# Top-level: применяется ко всем job как пол
permissions:
  contents: read

jobs:
  release:
    permissions:          # job-level: оверрайд только для этого job
      contents: write     # этот один job может запушить тег
      id-token: write     # и сминтить OIDC-токен (следующий урок)
    runs-on: ubuntu-latest
    steps: [...]
```

<Quiz
  id="01-github-token-and-permissions-quiz-1"
  lessonSlug="01-github-token-and-permissions"
  lang="ru"
  question="В воркфлоу вообще нет блока permissions:. Репо создан годы назад, и орг никогда не менял дефолтную настройку токена. Что может GITHUB_TOKEN в его job-ах?"
  choices={[
    { label: "Он получает пермиссивный легаси-дефолт — read/write почти на все scope (contents, issues, pull-requests, packages), так что скомпрометированный шаг может пушить коммиты и редактировать issue", correct: true },
    { label: "Он получает contents: read и больше ничего, ведь read-only всегда дефолт", misconception: "Read-only — это ограниченный дефолт, в который орг должен опт-ин-уться. Давний репо, чей орг не переключил настройку, всё ещё выдаёт старый пермиссивный write-all токен — обратное допущение ровно так и удивляет радиусом поражения." },
    { label: "У него нет прав, пока ты их явно не выдашь, так что job даже не может склонировать", misconception: "Отсутствие блока permissions не значит ноль — оно значит наследуй дефолт репо/орга, что может быть write-all. Deny-by-default для неперечисленных scope ты получаешь, лишь объявив блок permissions вообще." },
    { label: "Он может всё, что может личный аккаунт автора воркфлоу", misconception: "GITHUB_TOKEN — это scoped installation-токен, привязанный к репо, никогда не user-личность автора; он не дотянется до других репо или орг-админ-действий. Опасность — его широта внутри репо, не имперсонация." },
  ]}
/>

## Least privilege, per job

Дисциплина двухчастна. Сначала задай ограничительный **пол** в начале воркфлоу — `permissions: contents: read` (или даже `{}` для нуля scope) — чтобы каждый job стартовал без того, что ему не нужно. Затем **поднимай узко**: только job, что публикует релиз, получает `contents: write`; только job, что комментирует PR, получает `pull-requests: write`; только job, что аутентифицируется в облако, получает `id-token: write`. Job-level блок `permissions:` полностью заменяет унаследованный набор для этого job — он не аддитивен, так что перечисление одного scope роняет все остальные в `none`.

Это важно из-за того, как токен достаёт код. Каждый шаг в job — каждый сторонний Action, каждый `run:`-скрипт, каждая транзитивная npm- или pip-зависимость, что они выполняют — работает с тем же токеном job, доступным в окружении и для GitHub API. Per-step-песочницы токена нет. Так что scope токена — это объединение доверия, что ты распространяешь на каждую строку кода в этом job. Пиннинг Action-ов к полному commit SHA (`uses: owner/action@<40-символьный-sha>`) закрывает половину *какой код запускается*; least-privilege `permissions:` закрывает половину *что этот код может*. Нужны обе: пиннинг без скоупинга всё ещё выдаёт write-all токен запиненному-но-позже-скомпрометированному коду, а скоупинг без пиннинга всё ещё гоняет произвольный изменяемый код, лишь с меньшим охватом.

<Inset kind="why" lang="ru">
Зачем дефолтить top-level read-only пол вместо того, чтобы выдавать каждому job нужное с нуля? Потому что failure mode — это *забыть*. Новый job, добавленный через полгода, наследует top-level пол автоматически; если пол — `contents: read`, этот новый job безопасен-по-дефолту и становится опасным, лишь если кто-то сознательно его поднимет — видимая, ревьюимая строка в дифе. Без пола новый job молча наследует тот дефолт репо, что есть, и никто не замечает write-all токен до пост-мортема. Пол делает «безопасно» путём наименьшего сопротивления.
</Inset>

<Quiz
  id="01-github-token-and-permissions-quiz-2"
  lessonSlug="01-github-token-and-permissions"
  lang="ru"
  question="Воркфлоу задаёт permissions: contents: read в начале. Одному job надо запушить тег. Ревьюер спрашивает, почему не выдать contents: write на top-level, ведь так проще. Что верное возражение?"
  choices={[
    { label: "Top-level write делает каждый job — включая те, что гоняют недоверенный PR-код или сторонние Action — способным пушить в репо; ограничь contents: write только релиз-job, чтобы радиус поражения остался минимальным", correct: true },
    { label: "Top-level блоки permissions игнорируются GitHub; действуют только job-level блоки", misconception: "Top-level permissions абсолютно действуют — они задают пол, что наследует каждый job. Возражение не в том, что он игнорируется, а в том, что write-пол сверх-привилегирует каждый другой job в воркфлоу." },
    { label: "contents: write на любом уровне отключает OIDC-scope id-token", misconception: "Scope независимы; выдача contents: write никак не влияет на id-token. Реальная проблема — широта: каждый job, не OIDC-взаимодействие." },
    { label: "Реальной разницы нет; токен per-repo, так что write везде эквивалентен write в одном job", misconception: "Репо то же, но код не тот: write-all пол значит, что скомпрометированная зависимость в твоём тест-job может пушить в main, тогда как скоупинг write только в релиз-job значит, что лишь этот один доверенный job может менять репо." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Scope токена задаётся слоистыми дефолтами и оверрайдится явным блоком permissions per job"
  hue="var(--d-infra)"
  perRow={3}
  nodes={[
    { id: "mint", label: "Job стартует", sub: "токен авто-минтится" },
    { id: "default", label: "Дефолт репо/орг", sub: "пермиссивный ИЛИ ограниченный" },
    { id: "top", label: "Top-level права", sub: "пол: contents:read" },
    { id: "job", label: "Job-level права", sub: "заменяет, не добавляет" },
    { id: "scoped", label: "Урезанный токен", sub: "read-only по дефолту" },
    { id: "elevated", label: "Поднятый job", sub: "contents/id-token:write" },
  ]}
  edges={[
    { from: "mint", to: "default", label: "нет блока?" },
    { from: "mint", to: "top", label: "блок задан" },
    { from: "top", to: "job", label: "наследует" },
    { from: "job", to: "scoped", label: "минимум" },
    { from: "job", to: "elevated", label: "узкая выдача" },
  ]}
  caption="Без блока permissions токен наследует дефолт репо/орга, что может быть write-all. Явный блок делает каждый неперечисленный scope none; job-level блоки полностью заменяют унаследованный набор, так что поднимай лишь тот один job, кому надо."
/>

<KeyTakeaway>Каждый job авто-минтит привязанный-к-репо GITHUB_TOKEN, чья дефолтная широта может быть write-all, если орг не опт-ин-улся в ограниченный дефолт. Объявление блока permissions переключает неперечисленные scope в none, так что задай top-level contents:read пол и поднимай лишь тот конкретный job, кому нужен contents:write или id-token:write. Токен делится между каждым шагом, Action и транзитивной зависимостью в job без per-step-песочницы — так что его scope есть доверие, что ты распространяешь на весь этот код. Пинь Action-ы к SHA (какой код запускается) и ограничивай permissions (что он может); нужны обе.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-github-token-and-permissions-retrieval"
  lang="ru"
  questions={[
    { q: "Откуда берётся scope прав GITHUB_TOKEN, когда у воркфлоу нет блока permissions, и почему это рискованно?", a: "Без блока permissions токен наследует дефолтную настройку token-permissions репо/орга. Этот дефолт — либо пермиссивный (легаси: read/write почти на все scope — contents, issues, pull-requests, packages), либо ограниченный (contents:read, остальное none), и какой ты получишь, зависит целиком от того, опт-ин-улся ли орг в ограниченный дефолт. Read-only нельзя допускать. Риск в том, что давний репо молча выдаёт каждому job почти-write-all токен, так что любой скомпрометированный Action или транзитивная зависимость в любом job может пушить коммиты, редактировать issue или публиковать пакеты. Объявление блока permissions — единственный способ deny-by-default: каждый неперечисленный scope падает в none независимо от настройки репо." },
    { q: "Зачем ограничивать contents:write одним job вместо выдачи на top-level, и как это взаимодействует с пиннингом Action-ов?", a: "Токен job делится между каждым шагом, сторонним Action и транзитивной зависимостью в job без per-step-песочницы — так что scope токена есть объединение доверия, распространённого на весь этот код. Top-level contents:write пол значит, что скомпрометированная зависимость даже в тест-job могла бы пушить в репо; скоупинг write только в релиз-job держит радиус поражения в коде того одного job. Job-level блок permissions полностью заменяет унаследованный набор (не аддитивен), так что перечисление лишь contents:write роняет всё остальное в none. Пиннинг Action-ов к 40-символьному commit SHA контролирует, какой код запускается; least-privilege permissions контролирует, что этот код может. Нужны обе: скоупинг без пиннинга всё ещё гоняет произвольный изменяемый код, а пиннинг без скоупинга выдаёт write-all токен коду, что может быть скомпрометирован после того, как ты запинил." },
  ]}
/>

<Recap lang="ru">
GitHub минтит свежий GITHUB_TOKEN в начале каждого job, привязанный к текущему репо и истекающий по окончании job — но опасна его широта. Каждый scope (contents, issues, pull-requests, packages, actions, id-token) независимо read/write/none, и без блока permissions токен наследует дефолт репо/орга, что может быть легаси-пермиссивным write-all набором, а не ограниченным read-only. Объявление блока permissions делает каждый неперечисленный scope none, так что дисциплина: задай top-level contents:read (или `{}`) пол, затем поднимай узко на уровне job — лишь релиз-job получает contents:write, лишь облако-аутентификация-job получает id-token:write. Job-level блоки заменяют, не добавляют. Это важно, ведь токен делится между каждым шагом, Action и транзитивной зависимостью в job без per-step-песочницы, так что его scope есть доверие, что ты распространяешь на каждую строку кода там. Пинь Action-ы к commit SHA, чтобы контролировать, какой код запускается, и ограничивай permissions, чтобы контролировать, что он может; любая по отдельности оставляет реальную дыру. Read-only пол также делает «безопасно» дефолтом для job-ов, добавленных позже — они наследуют безопасность и становятся опасными лишь через видимое, ревьюимое поднятие в дифе.
</Recap>
