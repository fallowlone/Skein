---
concepts:
- poisoned-pipeline-execution
- pull-request-target
- github-token-permissions
- oidc-vs-secrets
- untrusted-build-isolation
deepensInto: []
estMin: 19
lang: ru
lessonType: topic
level: senior
order: 4
prereqs: []
slug: "04-pipeline-security-and-poisoned-builds"
sources:
- https://docs.github.com/en/actions/security-for-github-actions/security-guides/security-hardening-for-github-actions
- https://slsa.dev/spec/v1.0/threats
spiral: []
status: ready
summary: 'CI-раннер держит все секреты и может подменить артефакт, поэтому моделируй его как угрозу. Poisoned Pipeline Execution запускает код атакующего с твоими привилегиями — классически через pull_request_target, запускающий форк-PR. Изолируй сборку, default-deny токен, OIDC.'
title: 'Безопасность пайплайна и отравленные сборки'
track: ci-cd
unit: 05-supply-chain
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
PR пришёл от аккаунта, которого никто не знал — «контрибьютор» в первый раз, чинит опечатку в README плюс одно несвязанное изменение в test setup. Мейнтейнер пробежался глазами, увидел, что мелочь, и оставил CI прогнать его. Через сорок секунд воркфлоу зачекаутил ветку форка, прогнал сборку, а скрипт `prepare` в изменённом setup-файле прочитал окружение, base64-закодировал облачный deploy-ключ из `secrets` и отправил POST-ом на эндпоинт атакующего. Никакого merge не было. Мейнтейнер не одобрял деплой; он вообще ничего не одобрял. Воркфлоу использовал `pull_request_target` — который выполняется в контексте базового репозитория, с его секретами и write-токеном, — а затем зачекаутил и запустил код атакующего. Раннер сделал ровно то, что ему сказали. Этот урок о том, чтобы относиться к самому пайплайну как к поверхности атаки: раннер держит каждый секрет, который ты ему дал, и может подменить артефакт, который ты отгружаешь, так что скомпрометировать сборку часто проще — и хуже, — чем атаковать прод напрямую.
</Hook>

<Crux>Раннер держит все секреты и отгружает артефакт. Не запускай недоверенный PR-код там, где живут секреты — изолируй, default-deny, OIDC.</Crux>

<Explanation>

## Почему раннер — высокоценная цель

Прежде чем читать дальше: что произошло бы, если бы код, сейчас выполняющийся в твоей сборочной джобе, был враждебным? Подумай над ответом, потом продолжай.

CI-раннер — это не «просто сборочная коробка». На время джобы он держит каждый секрет, который ты отдал пайплайну, — deploy-ключи, креды реестра, облачные токены, ключи подписи, — и у него есть две вещи, которых хочет атакующий: исходящий доступ в сеть (чтобы эксфильтрировать) и доступ на запись к артефактам, которые ты вот-вот отгрузишь (чтобы подменить). Эта вторая способность — угроза класса SolarWinds: атакующему, контролирующему сборку, вообще не нужно вламываться в production. Он модифицирует артефакт *в момент сборки*, твой собственный пайплайн его подписывает и отгружает, и каждый клиент тянет забэкдоренный бинарь, прошедший все твои проверки. SLSA моделирует ровно это — угрозы против процесса сборки и provenance того, что она выдаёт, а не только исходника.

Так что пайплайн — это система, которую надо моделировать как угрозу, как и любую другую. Управляющий вопрос для каждой джобы: *если бы код, выполняющийся в этой джобе, был враждебным, до чего бы он дотянулся?* Ответ — «до каждого секрета и креда в области видимости плюс артефакт». Именно эта формулировка делает остальные защиты урока очевидными.

## Poisoned Pipeline Execution: ключевой режим отказа

**Poisoned Pipeline Execution (PPE)** (отравленное выполнение пайплайна) — зонтичное название центральной атаки: противник заставляет твой CI выполнить *его* код с привилегиями *пайплайна*. Ему не нужно заранее компрометировать раннер или красть креды — он убеждает пайплайн выполнить контролируемый атакующим код в привилегированном контексте, и секреты выпадают бесплатно.

На GitHub каноничный вектор PPE — это выбор триггера. Сравни два pull-request-триггера:

```yaml
# БЕЗОПАСНЫЙ ДЕФОЛТ для форк-PR: низкие привилегии, нет секретов, read-only токен.
on: pull_request

# ОПАСНО когда запускает PR-код: контекст базового репо, секреты + write-токен.
on: pull_request_target
```

`pull_request` выполняется в намеренно ограниченном контексте для форк-PR: нет секретов, read-only `GITHUB_TOKEN`. Это *правильное* место, чтобы собирать и тестировать недоверенный код, потому что даже если PR привозит вредоносный `postinstall` или отравленный тест, в области видимости нет ничего ценного для кражи. `pull_request_target` — противоположность: он существует для воркфлоу, которым нужно комментировать, проставлять метки или триажить, поэтому он выполняется в контексте **базового репозитория** — с твоими секретами и write-токеном. Сам триггер — не баг. Баг — это *комбинация*: воркфлоу `pull_request_target`, который затем чекаутит head PR-а и выполняет его.

```yaml
# АНТИ-ПАТТЕРН — так не делай. pull_request_target + чекаут недоверенного head.
on: pull_request_target
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}   # код, контролируемый атакующим
      - run: npm ci && npm test                             # запускает его с секретами в области видимости
```

В тот момент, когда этот сборочный шаг выполняется, контролируемый атакующим код исполняется в контексте, у которого есть твои секреты. Форк-PR от кого угодно из интернета может прочитать `secrets.*` и эксфильтрировать. Ни merge, ни одобрения, ни ревью кода, который реально выполнился, — один `npm ci` уже выполняет lifecycle-скрипты PR-а. Защита структурная: **никогда не чекаутить и не запускать недоверенный PR-код в джобе, у которой есть секреты.** Держи шаги, несущие секреты, физически отдельно от шагов с недоверенным кодом, требуй ручного одобрения, прежде чем воркфлоу от контрибьютора-новичка вообще запустится, и относись к head PR-а как к враждебному входу, как ты бы относился к телу запроса.

<Inset kind="why" lang="ru">
Почему `pull_request_target`, запускающий код PR-а, уникально опасен, когда `pull_request`, запускающий ровно тот же код, безопасен? Потому что два триггера выполняются в разных *контекстах*, по замыслу. `pull_request` намеренно прогоняет форк-PR в ограниченной песочнице: нет открытых секретов, read-only токен — безопасный дефолт именно потому, что код это недоверенный вход. `pull_request_target` существует для другой нужды: воркфлоу, которым надо комментировать или ставить метки PR-у, нужен доступ на запись и иногда секреты, поэтому он выполняется в контексте БАЗОВОГО репо с тем и другим. Это нормально *пока он никогда не выполняет код PR-а* — он предназначен действовать на метаданных. Уязвимость рождается в тот миг, когда такой воркфлоу чекаутит head-ref и запускает его: теперь контролируемый атакующим код исполняется внутри контекста, который держит твои секреты и write-токен. Триггер — не изъян; изъян — комбинация привилегированного контекста с выполнением недоверенного кода. Один код, два триггера, противоположный радиус поражения.
</Inset>

## Least-privilege GITHUB_TOKEN

Даже когда джоба легитимно выполняет твой собственный код, автоматически предоставляемый `GITHUB_TOKEN` — это кред, который стоит урезать. По умолчанию токен воркфлоу может быть широким; атакующий, добившийся выполнения кода в джобе с **write-scoped** токеном, может больше, чем читать, — он может пушить коммиты, нарезать релизы или двигать теги, превращая компрометацию сборки в компрометацию исходника. Фикс — default-deny: установи `permissions` на верхнем уровне в минимум, затем выдай только то, что нужно каждой джобе, по джобам.

```yaml
# Default-deny наверху; выдай минимум, по джобам.
permissions: {}                 # ничего по умолчанию
jobs:
  build:
    permissions:
      contents: read            # эта джоба только читает репо
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run build
```

Скоупь токены по джобам, а не по воркфлоу: джоба, выполняющая недоверенный код, должна иметь `permissions: {}` и никаких секретов, тогда как отдельная publish-джоба получает ровно тот write-скоуп, который ей нужен, и ничего больше. Write-токен в джобе, выполняющей недоверенный код, — это самонанесённый PPE.

## OIDC вместо долгоживущих секретов

Более глубокий фикс для «раннер держит крадомые секреты» — вообще перестать хранить крадомый секрет. Долгоживущий облачный ключ, лежащий в CI-секретах, широкий, редко ротируется, и одна эксфильтрация отдаёт его на всё время его жизни. **OIDC** убирает стоящий секрет: раннер запрашивает короткоживущий подписанный identity-токен, кодирующий, *какой репо, ветка и окружение* он выполняется как; облачный провайдер сверяет этот токен с trust-политикой и обменивает на **временные** креды, заскоупленные под эту нагрузку.

```yaml
# OIDC: нет стоящего облачного ключа в секретах. Раннер чеканит короткоживущую
# идентичность, облачный провайдер обменивает её на временные креды.
permissions:
  id-token: write              # разрешить раннеру запрашивать OIDC-токен
  contents: read
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::111122223333:role/deploy-prod
          aws-region: us-east-1
```

Тут есть реальный размен: OIDC требует настройки trust-политики на стороне облака (какая роль доверяет какому репо и ветке) против вставки одного ключа в секрет. Но радиус поражения украденного креда падает с «широкого долгоживущего ключа» до «токена, истекающего за минуты и работающего только для одной нагрузки» — часто почти ноль. Стоящего секрета, который атакующий бы эксфильтрировал, просто больше не существует.

## Выполнение недоверенных зависимостей на этапе сборки

У PPE есть более тихий родственник, которому не нужен ни вредоносный мейнтейнер, ни неверно настроенный триггер: вредоносная *зависимость*. Когда сборка устанавливает пакеты, их install- и build-lifecycle-скрипты (`preinstall`, `postinstall`, build-хуки) выполняются на раннере с привилегиями джобы — тот же радиус поражения, что и у любого другого кода в джобе. Скомпрометированная транзитивная зависимость может прочитать любые секреты, что держит джоба, и эксфильтрировать их, всё во время рутинного `npm ci`.

Митигации складываются: запускай установки с `--ignore-scripts`, где можешь, чтобы lifecycle-хуки не срабатывали автоматически; используй эфемерные, изолированные раннеры, чтобы скомпрометированная сборка не могла закрепиться или сделать pivot; применяй egress-фильтрацию, чтобы сборка, которой нечего звонить неизвестному хосту, просто не могла; и — самый дешёвый, самый эффективный контроль — **не давай сборочной джобе секреты, которые ей не нужны.** Сборке, компилирующей код, не нужен твой deploy-ключ в области видимости. Перенеси deploy-ключ в deploy-джобу, которая не выполняет недоверенный код зависимостей.

<TradeoffMatrix
  id="ci-cd-05-supply-chain-04-pipeline-security-and-poisoned-builds-tradeoff-1"
  lessonSlug="04-pipeline-security-and-poisoned-builds"
  lang="ru"
  prompt="Тебе нужно собирать и тестировать недоверенные форк-PR в CI — включая запуск их зависимостей и тестового набора — не отдавая атакующему твои секреты. Как ты это структурируешь?"
  options={[
    { name: "Запускать недоверенный PR-код под pull_request (нет секретов, read-only токен, least privilege), держать любые привилегированные шаги в отдельной джобе и использовать OIDC для деплоев, чтобы не было стоящего ключа для кражи", summary: "Изоляция недоверенного кода от секретов.", correct: true, justification: "Это точно совпадает с моделью угроз: джоба, выполняющая контролируемый атакующим код (сборка, тесты, lifecycle-скрипты), выполняется в ограниченном контексте pull_request — нет открытых секретов, read-only токен — так что даже вредоносному postinstall нечего ценного эксфильтрировать в области видимости. Всё привилегированное (комментирование, публикация, деплой) живёт в отдельной джобе, которая никогда не запускает PR-код, с least-privilege токеном по джобе. OIDC значит, что деплои чеканят короткоживущие, заскоупленные под нагрузку креды вместо крадомого стоящего ключа. Недоверенный код и секреты никогда не в одном контексте — структурное свойство, делающее PPE здесь невозможным." },
    { name: "Использовать pull_request_target и зачекаутить head PR-а, чтобы прогнать его тесты", summary: "Хрестоматийный PPE.", justification: "Это ровно та атака. pull_request_target выполняется в контексте базового репо С секретами и write-токеном; чекаут и запуск head PR-а исполняет контролируемый атакующим код внутри этого привилегированного контекста, так что форк-PR от кого угодно может прочитать secrets.* и эксфильтрировать deploy-ключ — без merge. Один npm ci уже запускает lifecycle-скрипты PR-а. Триггер — для действий на метаданных, никогда для выполнения недоверенного кода." },
    { name: "Дать джобе все секреты, но доверять только контрибьюторам, которых узнаёшь", summary: "На доверии, а не на изоляции.", justification: "«Узнать контрибьютора» — это не граница безопасности: аккаунты компрометируются, контрибьюторы-новички по определению неузнаваемы, а ранее доверенный контрибьютор может открыть вредоносный PR. В тот миг, когда любой недоверенный код выполняется с секретами в области видимости, ты в одном неверном решении от эксфильтрации. Изоляция (нет секретов в недоверенной джобе) держится независимо от того, кто открыл PR; доверие — нет." },
    { name: "Полностью отключить CI для форков", summary: "Безопасно, но убивает смысл.", justification: "Отказ собирать форк-PR действительно закрывает дыру, но он также ломает рабочий процесс контрибуции: ты не можешь тестировать community-PR перед merge, так что качество ревью падает и проект встаёт. Это выбрасывание ключевой способности ради избегания неверной настройки, которую правильная изоляция (pull_request, нет секретов) уже предотвращает. Защити пайплайн; не ампутируй его." },
  ]}
/>

<FlowDiagram
  label="Как форк-pull-request течёт через два выбора триггера и структурную защиту. Форк-PR от недоверенного интернет-контрибьютора можно обработать двумя путями. Путь pull_request прогоняет сборку и тесты PR-а в ограниченном контексте: нет открытых секретов и GITHUB_TOKEN read-only, так что даже вредоносному PR-коду или отравленному postinstall нечего ценного красть в области видимости — это безопасное место для недоверенного кода. Путь pull_request_target выполняется в контексте базового репо с секретами и write-токеном; если такой воркфлоу затем чекаутит и запускает head-код PR-а, код атакующего выполняется с твоими секретами и эксфильтрирует их. Защита структурная: раздели работу на бессекретную недоверенную сборочную джобу плюс отдельную привилегированную джобу и перейди на OIDC, чтобы не было стоящего ключа для кражи."
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "pr", label: "Форк-PR", sub: "недоверенный код" },
    { id: "safe", label: "pull_request", sub: "нет секретов, read-only" },
    { id: "ran", label: "запуск недов. сборки", sub: "нечего красть" },
    { id: "danger", label: "pull_request_target", sub: "секреты + write" },
    { id: "exfil", label: "чекаут + запуск PR", sub: "эксфильтрация" },
    { id: "fix", label: "раздели джобы + OIDC", sub: "структурная защита" },
  ]}
  edges={[
    { from: "pr", to: "safe", label: "безопасный путь" },
    { from: "safe", to: "ran", label: "изолировано" },
    { from: "pr", to: "danger", label: "опасный путь" },
    { from: "danger", to: "exfil", label: "PPE" },
    { from: "exfil", to: "fix", label: "ремедиация" },
  ]}
  caption="Один форк-PR, два триггера, противоположный радиус поражения. pull_request изолирует недоверенный код от секретов; pull_request_target, запускающий PR-код, отдаёт их. Фикс: бессекретная недоверенная сборочная джоба + отдельная привилегированная джоба + OIDC, чтобы не было стоящего ключа для утечки."
/>

<Quiz
  id="ci-cd-05-supply-chain-04-pipeline-security-and-poisoned-builds-quiz-1"
  lessonSlug="04-pipeline-security-and-poisoned-builds"
  lang="ru"
  question="Почему CI-раннер — приоритетная цель атаки, хотя это и не production-сервер?"
  choices={[
    { label: "Он держит каждый секрет, который ты дал пайплайну, И может подменить артефакт, который ты вот-вот отгрузишь — скомпрометируй сборку и сможешь украсть креды или отгрузить малварь, которую подпишет твой же пайплайн", correct: true },
    { label: "Он работает на публичном IP, так что напрямую достижим из интернета, как прод", misconception: "Достижимость не суть — раннеры обычно не открыты на входящие. Ценность в том, что раннер ДЕРЖИТ и ДЕЛАЕТ во время джобы: все секреты пайплайна в области видимости, и у него доступ на запись к артефакту. Атакующий, выполнивший там код, крадёт креды или бэкдорит сборку, независимо от сетевой экспозиции." },
    { label: "У него больше CPU, чем у прода, так что он полезен для майнинга", misconception: "Майнинг — реальное злоупотребление украденными CI-минутами, но не поэтому раннер — высокоценная цель для атакующего на supply chain. Серьёзная угроза в том, что раннер держит каждый секрет и может подменить отгружаемый артефакт — украсть креды и отравить сборку (класс SolarWinds), что куда разрушительнее сжигания вычислений." },
    { label: "Это единственная машина с паролем от production-базы", misconception: "У него может быть, а может и не быть пароля от БД, но формулировка «единственная машина с прод-кредами» упускает структурную причину. Раннер держит ВСЕ секреты, отданные пайплайну, и может модифицировать артефакт на лету — так что компрометация сборки даёт атакующему подменить отгружаемое или украсть любой кред в области видимости, а не один пароль." },
  ]}
/>

<Quiz
  id="ci-cd-05-supply-chain-04-pipeline-security-and-poisoned-builds-quiz-2"
  lessonSlug="04-pipeline-security-and-poisoned-builds"
  lang="ru"
  question="Форк-PR от неизвестного аккаунта эксфильтрировал облачный deploy-ключ репо без merge и без одобрения. Какой паттерн это вызвал и каков структурный фикс?"
  choices={[
    { label: "Воркфлоу pull_request_target зачекаутил и запустил код PR-а, так что код атакующего выполнился в контексте базового репо с секретами; фикс — запускать недоверенные сборки под pull_request (нет секретов, least-priv токен) и держать привилегированные шаги в отдельной джобе", correct: true },
    { label: "Deploy-ключ был закоммичен в репо, и атакующий прочитал его из git-истории", misconception: "Это другой (тоже реальный) отказ — захардкоженный секрет. Здесь ключ жил в CI-секретах и был эксфильтрирован в рантайме, потому что pull_request_target запустил код PR-а в контексте, открывающем secrets.* . Фикс не «убрать закоммиченный ключ»; фикс — никогда не выполнять недоверенный код там, где секреты в области видимости." },
    { label: "Branch protection была выключена, так что атакующий запушил прямо в main и прочитал секрет", misconception: "Пуша в main не было — секрет утёк во время PR-CI, до любого merge. Причина — pull_request_target, выполняющий код форка с присутствующими секретами, а фикс — изоляция: запускать недоверенный код под pull_request без секретов и ограничить привилегированные шаги отдельной джобой. Branch protection не адресует рантайм-экспозицию секрета." },
    { label: "У GITHUB_TOKEN был admin-скоуп, что позволило PR-у прочитать deploy-ключ", misconception: "Переразросшийся GITHUB_TOKEN — реальная проблема, решаемая default-deny permissions, но не так читается deploy-ключ из секретов — этот токен не даёт доступа к secrets.* . Deploy-ключ утёк, потому что pull_request_target запустил код PR-а в контексте, где секреты открыты; структурный фикс — изоляция недоверенного выполнения от секретов." },
  ]}
/>

</Explanation>

<KeyTakeaway>Раннер держит все секреты и отгружает артефакт. PPE запускает код атакующего с твоими привилегиями — не запускай недоверенный PR-код, где живут секреты. Изолируй (pull_request, нет секретов), default-deny токен, OIDC.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="ci-cd-05-supply-chain-04-pipeline-security-and-poisoned-builds-retrieval"
  lang="ru"
  questions={[
    { q: "Почему сам CI-пайплайн надо моделировать как угрозу и что такое Poisoned Pipeline Execution? Используй различие pull_request против pull_request_target.", a: "Раннер — высокоценная цель, потому что на время джобы он держит каждый секрет, отданный пайплайну (deploy-ключи, креды реестра, облачные токены, ключи подписи), и имеет и исходящий сетевой доступ, и доступ на запись к артефакту, который вот-вот отгрузится. Так что компрометация сборки даёт атакующему украсть креды ИЛИ подменить отгружаемый артефакт — класс SolarWinds, где твой собственный пайплайн подписывает и распространяет забэкдоренный бинарь, прошедший каждую проверку, при том что атакующий ни разу не коснулся прода. SLSA моделирует ровно эти угрозы сборки и provenance. Управляющий вопрос для любой джобы: если бы этот код был враждебным, до чего бы он дотянулся? Ответ: до каждого секрета в области видимости плюс артефакт. Poisoned Pipeline Execution (PPE) — ключевая атака: заставить CI выполнить код атакующего с привилегиями пайплайна. На GitHub каноничный вектор — триггер: pull_request прогоняет форк-PR в ограниченном контексте БЕЗ секретов и с read-only токеном (безопасное место для недоверенного кода), тогда как pull_request_target выполняется в контексте базового репо С секретами и write-токеном, потому что он предназначен действовать на метаданных PR-а (комментировать, метить). Баг — комбинация: воркфлоу pull_request_target, чекаутящий head PR-а и запускающий его, выполняет контролируемый атакующим код там, где живут секреты, так что форк-PR от кого угодно может прочитать secrets.* и эксфильтрировать — даже npm ci запускает lifecycle-скрипты PR-а. Один код, два триггера, противоположный радиус поражения." },
    { q: "Пройди защиты: изоляция недоверенной сборки, least-privilege GITHUB_TOKEN, OIDC и митигации недоверенных зависимостей.", a: "Изоляция структурна: никогда не чекаутить и не запускать недоверенный PR-код в джобе, у которой есть секреты. Запускай недоверенную сборку/тесты под pull_request (нет секретов, read-only токен), чтобы вредоносному postinstall нечего было ценного эксфильтрировать; держи любой привилегированный шаг (комментарий, публикация, деплой) в отдельной джобе, которая никогда не запускает PR-код; требуй одобрения, прежде чем воркфлоу контрибьютора-новичка запустится; относись к head PR-а как к враждебному входу. Least-privilege токен: автопредоставляемый GITHUB_TOKEN может быть широким, и выполнение кода в джобе с write-scoped токеном может пушить коммиты, нарезать релизы или двигать теги — превращая компрометацию сборки в компрометацию исходника. Default-deny через верхнеуровневый permissions: {} и выдача минимума по джобе (contents: read для сборки), скоупя по джобе, а не по воркфлоу; недоверенная джоба получает permissions: {} и никаких секретов. OIDC убирает стоящий секрет целиком: вместо долгоживущего облачного ключа в CI-секретах (широкий, редко ротируемый, украденный-раз-украденный-на-всю-жизнь) раннер запрашивает короткоживущий подписанный identity-токен, кодирующий, какой репо/ветка/окружение он выполняется как, а облачный провайдер обменивает его на временные, заскоупленные под нагрузку креды — размен это настройка trust-политики против почти нулевого радиуса поражения. Выполнение недоверенных зависимостей: install/build-скрипты вредоносной зависимости выполняются с привилегиями джобы во время npm ci, так что митигируй через --ignore-scripts, эфемерные изолированные раннеры, egress-фильтрацию и прежде всего не давая сборочной джобе секреты, которые ей не нужны — перенеси deploy-ключ в deploy-джобу." },
  ]}
/>

<Recap lang="ru">
CI-раннер надо моделировать как угрозу, потому что на время джобы он держит каждый секрет, который ты даёшь пайплайну, и имеет доступ на запись к артефакту, который ты отгружаешь, — так что компрометация сборки даёт атакующему украсть креды или подменить отгружаемое (класс SolarWinds, моделируемый SLSA), ни разу не коснувшись production. Ключевой режим отказа — Poisoned Pipeline Execution: заставить CI выполнить код атакующего с привилегиями пайплайна. На GitHub каноничный вектор — триггер: pull_request прогоняет форк-PR в ограниченном контексте без секретов и с read-only токеном (безопасное место для недоверенного кода), тогда как pull_request_target выполняется в контексте базового репо с секретами и write-токеном для действий на метаданных; баг — это воркфлоу pull_request_target, чекаутящий и запускающий head PR-а, выполняя код атакующего там, где живут секреты, так что любой форк-PR может прочитать secrets.* и эксфильтрировать — даже npm ci запускает lifecycle-скрипты PR-а. Защиты структурны: изолируй недоверенную сборку под pull_request без секретов и держи привилегированные шаги в отдельной джобе; default-deny GITHUB_TOKEN через permissions: &#123;&#125; и выдавай минимум по джобе, чтобы скомпрометированная джоба не могла пушить коммиты или нарезать релизы; замени долгоживущие облачные ключи на OIDC, где раннер чеканит короткоживущую заскоупленную под нагрузку идентичность, обмениваемую на временные креды, роняя радиус поражения украденного секрета почти до нуля; и сдерживай выполнение недоверенных зависимостей на этапе сборки через --ignore-scripts, эфемерные изолированные раннеры, egress-фильтрацию и не давая сборочной джобе секреты, которые ей не нужны. Теперь, ревьюируя workflow с pull_request_target, первый вопрос: чекаутит ли эта джоба и выполняет ли код PR-а? Если да — это PPE в ожидании своего часа.
</Recap>
