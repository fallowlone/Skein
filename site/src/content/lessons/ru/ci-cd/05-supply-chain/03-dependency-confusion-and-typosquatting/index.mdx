---
concepts:
- dependency-confusion
- typosquatting
- scope-registry-pinning
- lockfile-integrity
- install-scripts
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 3
prereqs: []
slug: "03-dependency-confusion-and-typosquatting"
sources:
- https://owasp.org/www-community/attacks/Dependency_Confusion
- https://docs.npmjs.com/cli/v10/configuring-npm/npmrc
spiral: []
status: ready
summary: 'Две атаки на разрешение имён, превращающие шаг install в RCE: dependency confusion (публичный пакет совпадает по имени с внутренним, но версия выше) и тайпсквоттинг (опечатка популярного имени). Пинни scope к реестру, ставь через npm ci, блокируй скрипты.'
title: 'Dependency confusion и тайпсквоттинг'
track: ci-cd
unit: 05-supply-chain
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Билд был зелёным годами. И вот однажды утром CI начал ронять проверку фрода ниже по конвейеру, дежурный инженер поднял логи раннера — и увидел, что шаг `install` звонит на хост, который никто не узнаёт. Пакет назывался ровно как одна из внутренних библиотек компании — имя, которое встречается только внутри монорепы и в стек-трейсах. Только эта копия пришла из публичного реестра, несла номер версии на порядок выше всего, что команда когда-либо выкатывала, и запустила postinstall-скрипт в тот самый момент, когда `npm install` её разрешил. Никто не добавлял зависимость. Никто не менял версию. Атакующий просто опубликовал пакет под собственным внутренним именем компании, выставил версию 99.9.9 и подождал, пока билд-машина её предпочтёт. Этот урок — про две атаки, которые превращают в оружие то, как пакетный менеджер преобразует имя в артефакт: dependency confusion и тайпсквоттинг.
</Hook>

<Crux>Атаки разрешения злоупотребляют «старшая версия побеждает» в общем namespace: пинни scope к реестру, ставь через npm ci, блокируй скрипты.</Crux>

<Explanation>

## Dependency confusion: побеждает старшая версия в общем пространстве имён

Когда пакетный менеджер разрешает имя, он спрашивает у реестра доступные версии и по умолчанию предпочитает старшую, удовлетворяющую диапазону. Ловушка в том, что «реестр» часто *во множественном числе*: многие конфигурации трактуют публичный реестр (npm, PyPI) как fallback (резервный источник, опрашиваемый при неудаче в основном) в **том же пространстве имён**, что и приватный. Так что если твой внутренний пакет `acme-internal-utils` существует приватно на `1.4.0`, а атакующий публикует **публичный** пакет с идентичным именем на `99.0.0`, резолвер сравнивает их в одном пространстве имён и берёт старшую версию — версию атакующего.

Единственная предпосылка для атакующего — твоё внутреннее *имя*. А эти имена утекают постоянно: `package.json`, закоммиченный в публичный репозиторий, дерево зависимостей, напечатанное в CI-логе, путь в стек-трейсе, вставленном в issue. Ничто из этого не секрет и ничто из этого не ощущается как уязвимость — пока не станет семенем атаки.

```bash
# Что резолвер по сути делает для БЕЗ-scope внутреннего имени:
#   запрос к приватному реестру  -> acme-internal-utils@1.4.0   (твой)
#   запрос к публичному  реестру -> acme-internal-utils@99.0.0  (атакующего, выше)
#   "побеждает старшая версия"   -> ставит 99.0.0 из публичного
# Затем install/postinstall-скрипт исполняется с секретами и сетью CI-джобы.
```

Полезная нагрузка исполняется потому, что пакетные менеджеры запускают lifecycle-скрипты (`postinstall`, `preinstall` — хуки, автоматически вызываемые пакетным менеджером до и после установки) во время установки *по дизайну* — именно так нативные модули собираются. На CI-раннере это значит, что код атакующего исполняется со всем, что держит джоба: токены реестра, облачные креды, возможность дотянуться до внутренних хостов. Корневая причина структурна, а не баг: **единое пространство имён разрешения**, где публичный реестр — тихий fallback и **побеждает старшая версия**, тогда как твои внутренние имена не зарезервированы публично.

## Фикс, который реально закрывает дыру: scope + пиннинг реестра

Решающая защита — сделать твои внутренние имена **неразрешимыми** из публичного реестра, чтобы сравнение выше вообще не происходило. Делаешь это, помещая внутренние пакеты под **scope** (`@acme/...`) и пинуя этот scope к приватному реестру в `.npmrc`:

```ini
# .npmrc — scope @acme НИКОГДА не проваливается в публичный реестр
@acme:registry=https://registry.internal.acme.com
//registry.internal.acme.com/:_authToken=${INTERNAL_NPM_TOKEN}
```

Теперь запрос на `@acme/utils` маршрутизируется *только* во внутренний реестр. Резолвер даже не спрашивает публичный реестр об этом scope, так что нет никакого публичного `@acme/utils@99.0.0`, чтобы победить — ловушка сравнения версий устранена структурно. Для легаси **без-scope** внутренних имён эквивалент — гарантировать, что резолвер никогда не проваливается в публичный для них (прокси, который fail-closed, или миграция их под scope). Приватность реестра не то же самое, что **изоляция пространства имён**; изоляцию даёт только пин.

<Inset kind="why" lang="ru">
Почему ты всё ещё уязвим просто *имея* внутренний пакет, если не пинишь scope к своему реестру? Потому что большинство резолверов трактуют публичный реестр как fallback в ТОМ ЖЕ пространстве имён, и «побеждает старшая версия» — так что публичный пакет с твоим внутренним именем и более высокой версией предпочитается твоему приватному. Сделать реестр приватным защищает его *содержимое*; с *именем* это не делает ничего. Резолвер всё так же охотно спрашивает публичный об имени, которое не может удовлетворить приватно, и атакующий, знающий имя, публикует там старшую версию. Только явный пин scope к твоему реестру (чтобы резолвер никогда не спрашивал публичный об этом scope) делает имя неподделываемым. Резервирование публичного имени или scope — публикация безобидной заглушки, чтобы никто другой не мог его заявить, — это второй слой «ремень и подтяжки».
</Inset>

## Lockfile, integrity и install-скрипты: слои под пином

Пин закрывает пространство имён; ещё три слоя ограничивают радиус поражения, если что-то всё же проскочит.

**Коммить lockfile и ставь через `npm ci`, а не `npm install`.** `npm install` имеет право *разрешать* — он может подтянуть новую старшую версию и переписать lockfile, ровно то поведение, которое эксплуатирует dependency confusion. `npm ci` ставит **только** то, что зафиксировано в lockfile, и проверяет каждый артефакт против хэша `integrity` (SHA субресурсной целостности, записанный во время лока). Если реестр отдаёт подменённый артефакт под ту же версию, проверка хэша падает и установка прерывается.

```bash
# npm install: может разрешить старшую версию и ПЕРЕПИСАТЬ lockfile (окно атаки)
npm install

# npm ci: ставит ТОЧНО зафиксированные версии, проверяет каждый integrity-хэш, падает на несовпадении
npm ci --ignore-scripts   # а также отказывает lifecycle-скриптам, где билд это позволяет
```

**Отключай install-скрипты, где можешь** (`--ignore-scripts`). Рычаг вредоносного пакета почти всегда — его `postinstall`; если скрипты не бегут, подменённый или сконфуженный пакет — это инертный код на диске, а не исполняемый код. Скрипты переразрешаешь намеренно для тех немногих зависимостей, которым реально нужна компиляция.

## Тайпсквоттинг: атака-сестра на тот же шаг установки

Dependency confusion целится в *твои* имена; тайпсквоттинг целится в *популярные* имена. Атакующий публикует пакеты на одну опечатку от чего-то известного — `expresss`, `lodahs`, `crossenv` — и ждёт, пока разработчик ошибётся в команде установки или скопирует опечатку из блога. Засквоттенный пакет затем запускает тот же вредоносный install-скрипт. Защита слоится иначе, потому что нет scope, который можно запинить: проверяй точное имя перед установкой, прогоняй установки через **приватный прокси-реестр** (Artifactory, Verdaccio), отдающий только проверенные пакеты из allow-list, сканируй зависимости инструментами (Socket, `npm audit`) и пинни всё через lockfile, чтобы опечатка не могла тихо попасть в воспроизводимый билд.

<TradeoffMatrix
  id="ci-cd-05-supply-chain-03-dependency-confusion-and-typosquatting-tradeoff-1"
  lessonSlug="03-dependency-confusion-and-typosquatting"
  lang="ru"
  prompt="Ты должен остановить dependency confusion для твоих внутренних @acme-пакетов — публичный пакет с тем же именем и более высокой версией не должен никогда дойти до билда. Какой подход реально закрывает дыру?"
  options={[
    { name: "Помести внутренние пакеты под scope @acme, запинь @acme к приватному реестру в .npmrc, зарезервируй публичный scope @acme и ставь через npm ci на закоммиченном lockfile", summary: "Запинь пространство имён.", correct: true, justification: "Это убирает ловушку сравнения версий структурно: с @acme:registry, запиненным в .npmrc, резолвер никогда не спрашивает публичный реестр об этом scope, так что нет публичного @acme/utils@99.0.0, чтобы победить. Резервирование публичного scope означает, что атакующий даже не может заявить имя. npm ci на закоммиченном lockfile затем ставит ровно зафиксированные версии с их integrity-хэшами, так что даже подменённый артефакт под запиненную версию провалит проверку хэша. Пин + резерв + lockfile — это слоистый, сеньорский ответ." },
    { name: "Положись на то, что реестр приватный — внутренние пакеты не публикуются публично, значит они в безопасности", summary: "Приватность не изоляция.", justification: "Приватный реестр защищает содержимое, а не пространство имён. Резолвер всё так же трактует публичный реестр как fallback для любого имени, которое не может удовлетворить приватно, и атакующий, узнавший внутреннее имя (из публичного package.json, CI-лога, стек-трейса), публикует там старшую версию. «Побеждает старшая версия» затем предпочитает публичный пакет. Приватность реестра — не изоляция имени; её даёт только явный пин scope к реестру." },
    { name: "Используй npm install и прогоняй npm audit после, чтобы поймать всё плохое", summary: "Аудит не того окна.", justification: "npm install имеет право разрешить новую старшую версию и переписать lockfile — это И ЕСТЬ механизм dependency confusion, так что это оставляет дверь открытой. А npm audit проверяет на ИЗВЕСТНО-уязвимые версии в advisory; свежеопубликованный confusion/тайпсквот-пакет ещё ни в одной базе advisory нет, так что audit ничего не сообщает, пока вредоносный postinstall уже отработал во время установки. Сканирование постфактум не отменит код, исполнившийся в момент install." },
    { name: "Переименуй внутренние пакеты в длинные неугадываемые строки, чтобы атакующий не угадал имя", summary: "Безопасность через неясность.", justification: "Неясность — не изоляция. Внутренние имена утекают постоянно — закоммиченные lockfile, CI-логи, трейсы ошибок, SBOM — так что «неугадываемое» быстро становится «опубликованным в логе», и в момент, когда имя известно, публичный пакет старшей версии побеждает ровно как раньше. Это ещё и усложняет использование каждого внутреннего пакета, не давая никакой структурной гарантии. Пин убирает атаку независимо от того, известно ли имя; переименование лишь откладывает её." },
  ]}
/>

<Quiz
  id="ci-cd-05-supply-chain-03-dependency-confusion-and-typosquatting-quiz-1"
  lessonSlug="03-dependency-confusion-and-typosquatting"
  lang="ru"
  question="Как атакующий через dependency confusion заставляет свой код исполниться внутри твоего CI-билда?"
  choices={[
    { label: "Он публикует ПУБЛИЧНЫЙ пакет с тем же именем, что у твоего внутреннего, и более высокой версией; резолвер предпочитает его в общем пространстве имён, и его install-скрипт бежит в CI", correct: true },
    { label: "Он угадывает твой токен реестра и пушит вредоносную версию прямо в твой приватный реестр", misconception: "Это была бы компрометация кредов, другая атака. Dependency confusion вообще не требует доступа к твоему реестру — атакующему нужно лишь знать внутреннее ИМЯ, затем он публикует одноимённый пакет в ПУБЛИЧНЫЙ реестр со старшей версией. Остальное делает «побеждает старшая версия» в общем пространстве имён, без какой-либо кражи токена." },
    { label: "Он эксплуатирует переполнение буфера в резолвере npm, чтобы перенаправить загрузку", misconception: "Никакого бага memory-safety тут нет. Механизм — полностью by-design поведение: публичный реестр — fallback в том же пространстве имён, и резолвер предпочитает старшую версию. Атакующий просто публикует публичный пакет старшей версии с твоим внутренним именем — «эксплойт» — это сама политика разрешения, а не дефект в коде резолвера." },
    { label: "Он отправляет pull request, добавляющий вредоносную зависимость в твой package.json", misconception: "Это потребовало бы вмержить PR и всплыло бы на ревью. Dependency confusion ничего не меняет в твоём репо — твой package.json всё так же называет внутренний пакет, который ты всегда использовал. Подмена случается в момент РАЗРЕШЕНИЯ, когда публичная старшая версия тихо предпочитается, — именно поэтому это проскальзывает мимо код-ревью." },
  ]}
/>

<Quiz
  id="ci-cd-05-supply-chain-03-dependency-confusion-and-typosquatting-quiz-2"
  lessonSlug="03-dependency-confusion-and-typosquatting"
  lang="ru"
  question="Почему `npm ci` на закоммиченном lockfile безопаснее `npm install` для сопротивления этой атаке?"
  choices={[
    { label: "npm ci ставит ровно зафиксированные версии и проверяет каждый integrity-хэш; он не разрешит тихо вредоносную старшую версию, тогда как npm install может пере-разрешить и переписать lockfile", correct: true },
    { label: "npm ci ставит офлайн, так что он вообще не связывается с реестром", misconception: "npm ci связывается с реестром, чтобы скачать зафиксированные артефакты; это не офлайн-режим. Его безопасность — от установки ТОЛЬКО версий, зафиксированных lockfile, и проверки каждой против записанного integrity-хэша, а не от пропуска сети. npm install же волен разрешить новую старшую версию и переписать lockfile." },
    { label: "npm ci автоматически блокирует все postinstall-скрипты по умолчанию", misconception: "npm ci запускает lifecycle-скрипты так же, как npm install, если не добавить --ignore-scripts. Блокировка скриптов — отдельный, дополняющий слой. Что npm ci даёт сверх npm install — точные, зафиксированные lockfile, проверенные по integrity установки без тихого пере-разрешения, — вот защита от dependency confusion здесь." },
    { label: "npm ci ставит только пакеты со scope-именами, игнорируя без-scope", misconception: "npm ci ставит всё из lockfile, со scope или без, — он не фильтрует по scope. Scope плюс пин реестра — отдельная защита, изолирующая пространство имён. Вклад npm ci — воспроизводимость: точные зафиксированные версии с проверенными integrity-хэшами, без апгрейда до старшей версии атакующего." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Как install-запрос на внутренний пакет разрешается с пином scope к реестру и без него. Запрос входит в резолвер. На запиненном пути .npmrc маппит @acme только на приватный реестр, так что резолвер спрашивает лишь этот реестр, получает твою версию и ставит твой код — публичный реестр для scope никогда не опрашивается. На незапиненном пути резолвер трактует публичный реестр как fallback в том же пространстве имён, находит там старшую версию атакующего, берёт её по правилу старшей версии и запускает её install-скрипт внутри CI с секретами джобы. Пин — это ворота, решающие, какой путь побежит."
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "req", label: "install @acme/utils", sub: "вход резолвера" },
    { id: "pin", label: "запиненный scope", sub: ".npmrc -> только приватный" },
    { id: "safe", label: "твоя версия", sub: "публичный не опрошен" },
    { id: "unpin", label: "незапиненный", sub: "публичный — fallback" },
    { id: "high", label: "атакующий 99.0.0", sub: "побеждает старшая" },
    { id: "rce", label: "install-скрипт бежит", sub: "секреты CI открыты" },
  ]}
  edges={[
    { from: "req", to: "pin", label: "scope запинен" },
    { from: "pin", to: "safe", label: "только приватный" },
    { from: "req", to: "unpin", label: "нет пина" },
    { from: "unpin", to: "high", label: "публичный fallback" },
    { from: "high", to: "rce", label: "postinstall" },
  ]}
  caption="Пин — это ворота: запинен — @acme разрешается только против твоего приватного реестра, и публичный никогда не опрашивается; незапинен — публичная старшая версия побеждает общее пространство имён, и её install-скрипт бежит в CI."
/>

<KeyTakeaway>Имена утекают; секретность не защита. Запинь scope к приватному реестру в .npmrc, чтобы публичный не спрашивали; ставь через npm ci (проверка integrity); блокируй скрипты. Тайпсквоты: прокси через allow-list.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="ci-cd-05-supply-chain-03-dependency-confusion-and-typosquatting-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни механизм dependency confusion от начала до конца: что нужно атакующему, почему резолвер берёт его пакет и где исполняется его код.", a: "Атакующему нужно лишь твоё внутреннее ИМЯ пакета — не твой реестр, не токен. Эти имена утекают рутинно: package.json в публичном репо, дерево зависимостей в CI-логе, путь во вставленном стек-трейсе. Атакующий публикует пакет с этим точным именем в ПУБЛИЧНЫЙ реестр (npm/PyPI) и ставит очень высокую версию, например 99.0.0. Уязвимость структурна: многие конфигурации трактуют публичный реестр как fallback в ТОМ ЖЕ пространстве имён, что и приватный, и резолвер предпочитает старшую версию, удовлетворяющую диапазону. Так он сравнивает твой приватный acme-internal-utils@1.4.0 с публичным acme-internal-utils@99.0.0 в одном пространстве имён и берёт старшую версию атакующего. Код исполняется потому, что пакетные менеджеры запускают lifecycle-скрипты (preinstall/postinstall) во время установки по дизайну — на CI-раннере это значит, что код атакующего исполняется со всем, что держит джоба: токены реестра, облачные креды, доступ к внутренним хостам. Корень: единое пространство имён разрешения с тихим публичным fallback, побеждает старшая версия, и внутренние имена не зарезервированы публично." },
    { q: "Разложи слоистые защиты и почему каждая нужна: scope+пин реестра, резервирование имени, lockfile + npm ci + integrity, ignore-scripts и вариант с тайпсквоттингом.", a: "Решающий слой — scope + ПИННИНГ реестра: помести внутренние пакеты под scope (@acme/...) и запинь @acme:registry=https://registry.internal в .npmrc. Теперь резолвер маршрутизирует @acme/* ТОЛЬКО в приватный реестр и никогда не спрашивает публичный об этом scope, так что нет публичного @acme/utils@99.0.0, чтобы победить — ловушка сравнения версий устранена структурно. Приватность реестра — не изоляция пространства имён; изоляцию даёт только пин. РЕЗЕРВИРОВАНИЕ публичного scope/имени (публикация безобидной заглушки) — «ремень и подтяжки», чтобы атакующий не мог заявить его. LOCKFILE + npm ci + INTEGRITY: коммить lockfile и ставь через npm ci, который ставит ровно зафиксированные версии и проверяет каждую против её integrity-хэша (субресурсной целостности) — npm install же может разрешить старшую версию и переписать lockfile, ровно то поведение, что эксплуатирует атака; подменённый артефакт под запиненную версию провалит проверку хэша. IGNORE-SCRIPTS (npm ci --ignore-scripts) блокирует postinstall, несущий нагрузку, превращая сконфуженный/подменённый пакет в инертный код, а не исполняемый; скрипты переразрешаешь намеренно для зависимостей, которым нужна компиляция. ТАЙПСКВОТТИНГ — атака-сестра на популярные имена (expresss, lodahs, crossenv) в расчёте на опечатку; поскольку нет scope для пина, защищайся проверкой точных имён, прогоном установок через приватный прокси-реестр (Artifactory/Verdaccio), отдающий только проверенные пакеты из allow-list, сканированием инструментами (Socket, npm audit) и пином через lockfile." },
  ]}
/>

<Recap lang="ru">
Две атаки на разрешение имён превращают рутинный шаг установки в удалённое выполнение кода. Dependency confusion злоупотребляет тем, как резолвер преобразует имя в артефакт: многие конфигурации трактуют публичный реестр как fallback в ТОМ ЖЕ пространстве имён, что и твой приватный, и «побеждает старшая версия» — так что атакующий, узнавший твоё внутреннее имя (утёкшее в публичном package.json, CI-логе или стек-трейсе), публикует одноимённый ПУБЛИЧНЫЙ пакет на версии 99.0.0, резолвер предпочитает его твоей приватной 1.4.0, и его install/postinstall-скрипт бежит в CI с секретами и сетью джобы. Решающий фикс — scope + пиннинг реестра: помести внутренние пакеты под scope (@acme/...) и запинь этот scope к приватному реестру в .npmrc, чтобы резолвер даже не спрашивал публичный об @acme/* и не было старшей публичной версии, чтобы победить — приватность реестра не изоляция пространства имён, изоляция только в пине. Зарезервируй публичный scope как второй слой. Под этим коммить lockfile и ставь через npm ci (не npm install): npm ci ставит ровно зафиксированные версии и проверяет каждый integrity-хэш, отказывая подменённому артефакту, тогда как npm install может пере-разрешить до вредоносной старшей версии и переписать lockfile. Блокируй install-скрипты через --ignore-scripts, где билд позволяет, чтобы сконфуженный или подменённый пакет был инертным, а не исполняемым. Тайпсквоттинг — атака-сестра на популярные имена (expresss, lodahs, crossenv) в ставке на опечатку; без scope для пина защищайся проверкой имён, прогоном установок через приватный прокси-реестр, отдающий только allow-list, сканированием зависимостей и пином всего через lockfile. Теперь, видя новый внутренний пакет без scope-префикса, задай вопрос: зарезервировано ли это имя в публичном реестре и запинен ли его scope в .npmrc?
</Recap>
