---
concepts:
- cosign-verify
- slsa-provenance
- admission-control
- policy-as-code
- fail-closed
deepensInto: []
estMin: 19
lang: ru
lessonType: topic
level: senior
order: 5
prereqs: []
slug: "05-verifying-provenance-and-policy-gates"
sources:
- https://slsa.dev/spec/v1.0/levels
- https://docs.sigstore.dev/cosign/verifying/verify/
spiral: []
status: ready
summary: 'В L01 ты подписал образы и сгенерировал SLSA provenance — но подпись, которую никто не проверяет, ничего не защищает. Половина про принуждение: проверяй подпись + provenance + identity на границе деплоя через admission controller, fail closed, как policy-as-code.'
title: 'Верификация provenance и политики-гейты'
track: ci-cd
unit: 05-supply-chain
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
В CI команда сделала всё правильно. Каждый образ подписан cosign, каждая сборка выдавала SLSA provenance attestation, зелёные галочки были прекрасны. Руководству показали слайд «supply chain: защищён». А потом, во время пятничного пожара, инженеру нужно было срочно выкатить хотфикс — он собрал образ на ноутбуке и запустил `kubectl set image` прямо на прод-кластер. Образ поехал. Ни подписи, ни provenance, ни слова возражения — потому что в кластере ничто никогда *не проверяло*. Подписи были декорацией. Весь пайплайн, производивший криптографическое доказательство происхождения, вешал это доказательство на стену, пока дверь стояла открытой. Этот урок — недостающая половина: не как подписывать, а как заставить кластер *отказываться* от всего, что не подписано и не provenance-нуто билдером, которому ты доверяешь, — и отказываться даже тогда, когда сама верификация ломается.
</Hook>

<Crux>Подпись без верификации — театр. Принуждай на границе деплоя: admission controller проверяет подпись + provenance + identity и fail closed.</Crux>

<Explanation>

## Подпись — это утверждение; защита — это верификация

L01 дал тебе половину «производства»: `cosign sign` прикрепил подпись к digest образа, а билд-платформа выдала SLSA provenance attestation, описывающий, что его собрало. Это настоящая криптография. И сама по себе она полностью инертна. Подпись — это всего лишь утверждение, привинченное к артефакту, — она ничего не меняет в том, что запускается. Защита возникает только в тот момент, когда что-то **отказывается запускать артефакт, не подписанный и не attestation-нутый доверенным билдером**. Если ничто не отказывается, подписи — настенное украшение.

Так что вопрос, на который отвечает этот урок, — не «как мне подписывать» (ты это сделал), а «что именно я верифицирую и где живёт отказ».

## Что ты верифицируешь: подпись, provenance и identity — по digest

Верификация — это три проверки, и пропуск любой из них оставляет дыру:

```bash
# 1. ПОДПИСЬ — этот конкретный digest образа подписан identity, которому я доверяю?
#    Keyless: OIDC identity подписавшего записан в Rekor (transparency log),
#    а сертификат цепляется к Fulcio. Ты пинишь КОГО, а не долгоживущий ключ.
cosign verify \
  --certificate-identity "https://github.com/acme/app/.github/workflows/release.yml@refs/heads/main" \
  --certificate-oidc-issuer "https://token.actions.githubusercontent.com" \
  ghcr.io/acme/app@sha256:abc123...

# 2. PROVENANCE — говорит ли SLSA attestation, что МОЙ билдер собрал это из МОЕГО коммита?
cosign verify-attestation \
  --type slsaprovenance \
  --certificate-identity "https://github.com/acme/app/.github/workflows/release.yml@refs/heads/main" \
  --certificate-oidc-issuer "https://token.actions.githubusercontent.com" \
  ghcr.io/acme/app@sha256:abc123...
```

Первая проверка отвечает на *«это подписано кем-то, кому я доверяю?»* Вторая отвечает на более сильный вопрос: *«был ли этот образ собран моим CI, из моего репозитория, на коммите, который я могу назвать?»* — вот почему provenance важен даже для «подписанного» образа. Атакующий, укравший ключ подписи, может подписать вредоносный образ; но ему нелегко подделать provenance attestation, утверждающий, что твоя билд-платформа собрала его из твоего источника. И каждая проверка прибита к **digest образа** (`@sha256:...`), никогда не к меняемому тегу — тег можно перенаправить на другой образ после того, как ты его проверил; digest — это содержимое.

## Третья проверка: политика на attestation

Проверить, что подпись валидна, недостаточно — ты должен утвердить, что *содержимое* совпадает с твоими ожиданиями. Policy-as-code (политика как код — правила безопасности, версионируемые в репозитории и применяемые автоматически) выражает это так, что оно под контролем версий, тестируемо и применяется единообразно:

```rego
# Набросок OPA/Rego: впускаем, только если builder == наш CI И source repo == наш
package supplychain

default allow = false

allow {
  input.provenance.builder.id == "https://github.com/acme/app/.github/workflows/release.yml@refs/heads/main"
  input.provenance.invocation.configSource.uri == "git+https://github.com/acme/app@refs/heads/main"
  input.image.digest == input.requested.digest
}
```

Это разница между «существует валидная подпись» и «валидная подпись от *моего билдера* для *моего источника* на *этом конкретном digest*». Утёкший ключ, форк, пересборка из подделанной ветки — все они дают подписи, которые криптографически валидны и отвергнуты политикой.

## Где живёт отказ: admission control

Ты мог бы запускать `cosign verify` в своём CD-джобе деплоя. Это лучше, чем ничего, и это слабый гейт: любой, кто деплоит *в обход* CI — оператор с `kubectl`, скомпрометированный CD-шаг, break-glass-скрипт — обходит его полностью. Это ровно инцидент с пятничным хотфиксом. CI-проверка защищает путь, проходящий через CI, а это не тот путь, которым ходят атакующие и паникующие инженеры.

Принуждение, которое *ничто не обходит*, живёт на рантайм-границе: Kubernetes **admission controller** (контроллер допуска — плагин API-сервера, перехватывающий каждый запрос на создание ресурса и способный его отклонить). На каждом создании Pod контроллер (Sigstore policy-controller, Kyverno или OPA Gatekeeper) перехватывает запрос, резолвит образ в его digest, верифицирует подпись + provenance против твоей политики и **отказывает в admission, если проверка не прошла**. Нет «задеплоить в обход» — единственный способ запустить Pod проходит через API-сервер, а API-сервер вызывает webhook.

```yaml
# Sigstore policy-controller: запускаться могут только подписанные + provenance-нутые
# образы из нашего CI
apiVersion: policy.sigstore.dev/v1beta1
kind: ClusterImagePolicy
metadata:
  name: require-acme-provenance
spec:
  images:
    - glob: "ghcr.io/acme/**"
  authorities:
    - keyless:
        identities:
          - issuer: "https://token.actions.githubusercontent.com"
            subject: "https://github.com/acme/app/.github/workflows/release.yml@refs/heads/main"
      attestations:
        - name: must-have-slsa-provenance
          predicateType: "https://slsa.dev/provenance/v1"
  # КРИТИЧНО: отказывай, когда верификация не может завершиться, не впускай по ошибке
  policy:
    fetchConfigFile: true
```

## Уровни SLSA: планка, против которой ты верифицируешь

SLSA — это фреймворк, определяющий, *насколько сильна* гарантия provenance. Более высокие уровни означают, что provenance сгенерирован самой билд-платформой (а не билд-скриптом), сборки изолированы и неподделываемы, а путь от источника к артефакту устойчив к подмене. Ты выбираешь целевой уровень — скажем, «все прод-образы должны соответствовать SLSA Build L3» — и твоя политика *верифицирует*, что входящие артефакты несут provenance, отвечающий этой планке, прежде чем они деплоятся. Уровень — это контракт; admission control — это то, что его принуждает.

## Fail-closed против fail-open: компромисс, решающий всё

Вот тонкость, которая повернула вторую половину этой истории. Проверка верификации может *не завершиться*: реестр недоступен, transparency log на миг недостижим, attestation отсутствует. Политика должна решить, что делать тогда, и ответов всего два:

- **Fail closed** — если верификация не может завершиться, **отказывай**. Артефакт не запускается.
- **Fail open** — если верификация не может завершиться, **впускай всё равно**. Артефакт запускается непроверенным.

Fail-open-политика — это *молчаливое отсутствие защиты*: каждый временный сбой инфраструктуры подписи или лога становится окном, в которое проезжает что угодно. Команда из истории сначала выкатила fail-open-политику; когда Rekor стал на миг недоступен, непроверенные образы проехали прямиком, и никто не заметил, потому что не было отказа, на который сработал бы алерт. Fail closed — единственный корректный дефолт.

Цена реальна, и ты проектируешь *под неё*, а не *вокруг неё*: fail-closed означает, что сбой реестра или лога может *блокировать деплои*. Доступность ты выкупаешь обратно, кешируя trust roots, мониторя путь верификации как зависимость первого класса и относясь к инфраструктуре подписи как к production-критичной, — **а не** ослабляя политику до fail-open, что меняет проблему доступности деплоя на отсутствие защиты как фичи.

<Inset kind="why" lang="ru">
Почему подпись в CI бесполезна без admission-политики, которая верифицирует на деплое? Потому что подпись — это лишь утверждение, прикреплённое к артефакту; она ничего не меняет в том, что запускается, пока верификатор активно не откажется от неподписанных или недоверенных артефактов. Если единственная проверка опциональна или живёт на стороне CI, то любой, кто деплоит в обход CI — оператор с kubectl, скомпрометированный CD-джоб, break-glass-хотфикс — выкатывает непроверенный код, и на подписи никто никогда не смотрит. Принуждение обязано жить на рантайм-границе admission, где гейтится сам *акт запуска* артефакта, а не акт его сборки. И оно должно fail closed: если оно впускает при ошибке верификации, каждый сбой инфраструктуры подписи молча снова открывает дверь, которую должно было запереть.
</Inset>

<TradeoffMatrix
  id="ci-cd-05-supply-chain-05-verifying-provenance-and-policy-gates-tradeoff-1"
  lessonSlug="05-verifying-provenance-and-policy-gates"
  lang="ru"
  prompt="Ты должен гарантировать, что в кластере могут запускаться только доверенные, provenance-нутые образы — даже против оператора, деплоящего в обход CI, или скомпрометированного CD-шага. Какой контроль это даёт?"
  options={[
    { name: "Kubernetes admission controller, проверяющий подпись + SLSA provenance + identity билдера и источника по digest на каждом создании Pod, и fail closed", summary: "Гейт уровня кластера, fail closed.", correct: true, justification: "Admission controller сидит на рантайм-границе, через которую обязан пройти каждый Pod — API-сервер вызывает webhook, так что никакого «задеплоить в обход» нет. Он проверяет три значимые вещи: подпись от доверенного identity, SLSA provenance attestation, связывающий образ с твоим билдером и коммитом источника, и совпадение ожиданий политики — всё прибито к неизменяемому digest. Главное — он fail closed: если верификация не может завершиться (реестр или лог недоступны, attestation отсутствует), он отказывает, а не впускает непроверенное. Это единственный вариант, который не обходит ни один путь и у которого нет режима тихого сбоя." },
    { name: "Подписывать каждый образ в CI и доверять операторам не обходить пайплайн", summary: "Подписи, которые никто не верифицирует.", justification: "Это ровно провал из истории: подписывание производит утверждение, но ничто не отказывает неподписанному артефакту, так что защита — декорация. Оператор, запускающий kubectl set image, или скомпрометированный CD-шаг выкатывает неподписанный образ, и кластер запускает его без единого слова. «Доверять людям не обходить» — это не контроль; это его отсутствие. Защита существует только там, где что-то отказывается запускать непроверенные артефакты." },
    { name: "Запускать cosign verify только в CD-джобе деплоя", summary: "Слабо: обходится.", justification: "Верификация в джобе деплоя строго лучше, чем её отсутствие, но она охраняет только путь, проходящий через CI. Любой, кто деплоит в обход CI — break-glass-хотфикс, ручной kubectl, скомпрометированный CD-раннер — никогда не попадает на проверку. Это верная верификация в неверном месте: она гейтит сборку/деплой-через-CI, а не запуск. Admission controller уровня кластера — это бэкстоп, который ничто не обходит." },
    { name: "Сканировать каждый образ на CVE во время деплоя", summary: "Совсем другая ось.", justification: "Сканирование CVE отвечает на «содержит ли этот образ известно-уязвимые пакеты?» — полезный, но ортогональный вопрос. Оно ничего не говорит о том, тот ли это образ, который твой CI собрал из твоего источника, и был ли он вообще подписан. Идеально чистый при сканировании образ может быть неподписанной сборкой атакующего. Верификация provenance и сканирование уязвимостей дополняют друг друга; сканирование не даёт гарантии доверенного происхождения, которая здесь нужна." },
  ]}
/>

<Quiz
  id="ci-cd-05-supply-chain-05-verifying-provenance-and-policy-gates-quiz-1"
  lessonSlug="05-verifying-provenance-and-policy-gates"
  lang="ru"
  question="Твой CI подписывает каждый образ и выдаёт SLSA provenance, и все сборки зелёные. Почему это само по себе не защищает прод?"
  choices={[
    { label: "Подпись, которую никто не верифицирует, инертна — защита существует, только если что-то отказывается запускать артефакт, не подписанный и не provenance-нутый доверенным билдером", correct: true },
    { label: "Подписывание в CI полностью защищает прод; зелёные сборки и есть гарантия", misconception: "Подписывание производит утверждение, прикреплённое к артефакту; оно не гейтит то, что запускается. Зелёные сборки доказывают, что подпись создана, а не что её кто-то проверяет. Без верификатора, который отказывает неподписанным/недоверенным артефактам на границе деплоя, неподписанный образ, задеплоенный в обход CI, запускается беспрепятственно — это канонический инцидент." },
    { label: "Защищает прод, пока подписи используют долгоживущий ключ вместо keyless OIDC", misconception: "Тип ключа не создаёт принуждения. Подписываешь ли ты долгоживущим ключом или keyless через Fulcio/Rekor, подпись всё равно ничего не делает, пока что-то не верифицирует её на деплое и не откажет при сбое. Пробел — в отсутствующей верификации, а не в методе подписи." },
    { label: "Защищает прод, потому что SLSA provenance автоматически принуждается Kubernetes", misconception: "Kubernetes сам по себе не верифицирует provenance. Provenance — это просто attestation, лежащий в реестре, пока admission controller (policy-controller, Kyverno, Gatekeeper), который ты установил и настроил, реально не прочитает и не принудит его. Из коробки кластер впускает любой образ, который может скачать." },
  ]}
/>

<Quiz
  id="ci-cd-05-supply-chain-05-verifying-provenance-and-policy-gates-quiz-2"
  lessonSlug="05-verifying-provenance-and-policy-gates"
  lang="ru"
  question="Неподписанный образ доехал до прода, хотя CI подписывал всё. Какое принуждение отсутствовало и какого режима сбоя это принуждение обязано конкретно избегать?"
  choices={[
    { label: "Admission-политика времени деплоя, верифицирующая подпись + provenance и ОТКАЗЫВАЮЩАЯ при сбое — и она должна fail CLOSED, никогда не fail-open при ошибках верификации", correct: true },
    { label: "Более строгая политика ротации ключей подписи в CI", misconception: "Ротация ключей — это гигиена для стороны подписи, но инцидент был на стороне верификации: ничто не отказало неподписанному образу на деплое. Никакая политика ротации не поможет, если ничто не проверяет подпись. Отсутствующий контроль — это верификация времени admission, которая отказывает, и она должна fail closed, чтобы сбои молча не открывали пробел заново." },
    { label: "Admission-политика верна, но она должна fail OPEN, чтобы деплои никогда не блокировались при сбое реестра или лога", misconception: "Fail-open — это ловушка, а не фикс. Политика, впускающая при ошибке верификации, — молчаливое отсутствие защиты: каждый временный сбой реестра или transparency log становится окном, в которое выкатываются непроверенные образы. Ты делаешь fail closed и выкупаешь доступность кешированными trust roots и мониторингом — никогда впуском по ошибке." },
    { label: "Гейт CVE-сканирования на деплое заблокировал бы неподписанный образ", misconception: "CVE-сканирование проверяет известно-уязвимые пакеты, а не то, подписан ли образ и есть ли у него provenance. Неподписанный образ может просканироваться идеально чисто. Отсутствующий контроль — это верификация подписи/provenance на admission с fail closed; сканирование — отдельная, дополняющая ось." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Петля верификации supply chain, закрывающая пробел, открытый L01. Билд-платформа собирает артефакт, затем подписывает его и attestation-ит SLSA provenance (половина производства, рассмотренная в L01), и пушит образ в реестр. На деплое запрос деплоя на запуск Pod доходит до Kubernetes admission controller, который и есть точка принуждения: он резолвит образ в его digest и верифицирует подпись, SLSA provenance и identity билдера и источника против политики под контролем версий. Если всё совпадает с доверенным билдером и источником, Pod впускается и запускается. Если любая проверка падает — неверный identity, отсутствующий provenance или верификация, которая не может завершиться, потому что реестр или transparency log недостижим — контроллер ОТКАЗЫВАЕТ Pod, делая fail closed вместо впуска непроверенного."
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "build", label: "сборка", sub: "билд-платформа CI" },
    { id: "sign", label: "подпись + attest", sub: "provenance (L01)" },
    { id: "registry", label: "реестр", sub: "образ @digest" },
    { id: "deploy", label: "запрос деплоя", sub: "создать Pod" },
    { id: "admit", label: "admission controller", sub: "проверка vs политика" },
    { id: "run", label: "ВПУСК", sub: "доверенный -> запуск" },
    { id: "deny", label: "ОТКАЗ (fail closed)", sub: "недоверенный / ошибка" },
  ]}
  edges={[
    { from: "build", to: "sign", label: "производим" },
    { from: "sign", to: "registry", label: "push" },
    { from: "registry", to: "deploy", label: "запрос pull" },
    { from: "deploy", to: "admit", label: "перехват" },
    { from: "admit", to: "run", label: "sig+prov+id OK" },
    { from: "admit", to: "deny", label: "сбой / недоверенный" },
  ]}
  caption="Admission controller — точка принуждения, которую ничто не обходит: каждое создание Pod перехватывается и верифицируется против политики, впускается, только если подпись + provenance + identity совпадают с доверенным билдером, и отказывается (fail closed) при любом несовпадении или ошибке верификации."
/>

<KeyTakeaway>Подпись без верификации — театр. Admission controller проверяет подпись + provenance + identity по digest на каждом создании Pod, делая fail closed — бэкстоп, которым обходимая CI-проверка быть не может.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="ci-cd-05-supply-chain-05-verifying-provenance-and-policy-gates-retrieval"
  lang="ru"
  questions={[
    { q: "L01 уже подписывает образы и выдаёт SLSA provenance. Объясни точно, почему это само по себе не защищает прод, какие три вещи ты обязан верифицировать и где должна жить верификация.", a: "Подписывание производит утверждение — подпись, прикреплённую к digest образа, плюс provenance attestation, описывающий, что его собрало, — но утверждение, которое никто не проверяет, ничего не меняет в том, что запускается. Защита существует только в момент, когда что-то отказывается запускать артефакт, не подписанный и не provenance-нутый доверенным билдером; если ничто не отказывается, подписи — декорация. Ты обязан верифицировать три вещи, все прибитые к неизменяемому digest образа (никогда к меняемому тегу): (1) ПОДПИСЬ — digest подписан identity, которому ты доверяешь, например keyless через сертификат Fulcio с OIDC identity подписавшего, записанным в Rekor, transparency log; (2) PROVENANCE — SLSA attestation говорит, что твой билдер собрал это из твоего репозитория на коммите, который можно назвать, — вот почему provenance важен даже для «подписанного» образа (украденный ключ может подписать вред, но не может легко подделать provenance от твоей билд-платформы); (3) ПОЛИТИКА на содержимом attestation — builder id == твой CI, source repo == твой, digest совпадает, — так что утёкший ключ, форк или пересборка из подделанной ветки криптографически валидны, но отвергнуты политикой. Верификация должна жить на рантайм-границе admission, не только в CI: CI-проверка cosign verify охраняет только путь через CI, тогда как оператор с kubectl, скомпрометированный CD-шаг или break-glass-хотфикс деплоят в обход. Kubernetes admission controller перехватывает каждое создание Pod и является гейтом, который ничто не обходит." },
    { q: "Пройди admission control как точку принуждения, роль уровней SLSA и почему политика обязана fail closed, а не fail open.", a: "Точка принуждения — это Kubernetes admission controller (Sigstore policy-controller, Kyverno или OPA Gatekeeper): на каждом создании Pod API-сервер вызывает его webhook, контроллер резолвит образ в его digest, верифицирует подпись + provenance + identity против правила policy-as-code под контролем версий и отказывает в admission при сбое. Поскольку единственный способ запустить Pod — через API-сервер, нет деплоя в обход — в отличие от CI-проверки, которая покрывает только CI-деплои. Уровни SLSA — это планка, против которой ты верифицируешь: более высокие уровни означают provenance, сгенерированный самой билд-платформой, изолированные неподделываемые сборки, устойчивый к подмене путь от источника к артефакту; ты целишься в уровень (например Build L3), и политика верифицирует, что входящие артефакты ему соответствуют, прежде чем они деплоятся — уровень это контракт, admission control его принуждает. Политика обязана fail CLOSED: когда верификация не может завершиться — реестр недоступен, transparency log недостижим, attestation отсутствует — она должна отказать, а не впустить. Fail-open-политика — молчаливое отсутствие защиты, потому что каждый временный сбой инфраструктуры подписи или лога становится окном, в которое выкатываются непроверенные образы без отказа, на который сработал бы алерт (второй баг истории: краткий сбой Rekor пропустил непроверенные образы). У fail-closed есть реальная цена — сбой может блокировать деплои — но ты платишь её кешированием trust roots, мониторингом пути верификации как production-зависимости первого класса и закалкой инфраструктуры подписи, никогда не ослаблением до fail-open." },
  ]}
/>

<Recap lang="ru">
L01 произвёл криптографическое доказательство происхождения — подписи cosign и SLSA provenance — но доказательство, которое никто не проверяет, ничего не защищает; этот урок закрывает петлю, верифицируя и принуждая на границе деплоя. Подпись — лишь утверждение, привинченное к артефакту: защита существует ровно тогда, когда что-то отказывается запускать артефакт, не подписанный и не provenance-нутый доверенным билдером. Верификация — три проверки, все прибитые к неизменяемому digest образа: подпись от доверенного identity (keyless через Fulcio, записано в transparency log Rekor); SLSA provenance attestation говорит, что твой билдер собрал это из твоего коммита источника (так что украденный ключ подписи, который может подписать вред, всё равно падает, потому что не может подделать provenance твоей билд-платформы); и правило policy-as-code утверждает builder id == твой CI, source repo == твой, digest совпадает. Уровни SLSA — это планка (более высокие уровни означают сгенерированные платформой, изолированные, неподделываемые сборки), и ты верифицируешь, что входящие артефакты соответствуют твоему целевому уровню, до деплоя. Отказ должен жить на рантайм-границе admission: Kubernetes admission controller (policy-controller, Kyverno, Gatekeeper) перехватывает каждое создание Pod и отказывает при сбое, бэкстоп, который ничто не обходит, — тогда как CI-проверка cosign verify охраняет только CI-путь и обходится оператором с kubectl, скомпрометированным CD-шагом или break-glass-хотфиксом (история: неподписанная сборка с ноутбука поехала в прод, потому что ничто не проверяло, а затем была отказана, как только появилась admission-политика). Наконец, политика обязана fail CLOSED: если верификация не может завершиться из-за недоступного реестра или transparency log, она отказывает, а не впускает, потому что fail-open это молчаливое отсутствие защиты — каждый сбой инфраструктуры подписи становится окном, в которое выкатываются непроверенные образы (второй баг истории, краткий сбой Rekor). Доступность ты выкупаешь обратно кешированием trust roots и мониторингом пути верификации, никогда не ослаблением до fail-open. Теперь, когда видишь слайд «все образы подписаны — supply chain защищён», задай вопрос: что именно отказывает неподписанному или непровенанс-нутому образу на границе кластера — и делает ли это fail closed?
</Recap>
