---
concepts:
- sbom
- slsa
- build-provenance
- sigstore
- cosign
- attestation
deepensInto: []
estMin: 17
lang: ru
lessonType: topic
level: senior
order: 1
prereqs: []
slug: 01-sbom-and-provenance
sources:
- https://slsa.dev/spec/v1.0/levels
- https://docs.sigstore.dev/cosign/signing/overview/
- https://cyclonedx.org/
- https://docs.github.com/en/actions/security-for-github-actions/using-artifact-attestations/using-artifact-attestations-to-establish-provenance-for-builds
spiral: []
status: ready
summary: SBOM инвентаризирует каждую зависимость, чтобы ответить «затронуты ли мы CVE-X?»
  за минуты; provenance и SLSA доказывают, кто и как собрал артефакт; keyless-подпись
  cosign плюс аттестация, проверенная на деплое, превращают всё это из бумажки в шлагбаум.
title: 'SBOM, build provenance и подпись: докажи, что отгружаешь'
track: ci-cd
unit: 05-supply-chain
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
В пятницу выходит CVE: бэкдор в широко используемой библиотеке сжатия, из тех, что активируются только внутри сборки. Руководство задаёт единственный важный вопрос — «мы отгружаем затронутую версию?». Команда тратит выходные на grep по lock-файлам в сорока репозиториях и ssh в работающие контейнеры, чтобы проверить, что реально установлено, потому что по памяти никто не ответит. Команда, у которой был SBOM на каждый выпущенный образ, ответила за восемь минут одним запросом. Разница не в таланте. Разница — машиночитаемый инвентарь, сгенерированный во время сборки, и дисциплина его хранить.
</Hook>

<Crux>SBOM говорит, что ты отгрузил; provenance — как собрано; лишь проверка на деплое делает любое из них шлагбаумом, а не бумажкой.</Crux>

<Explanation>

## SBOM: машиночитаемый инвентарь для запросов, а не PDF для архива

**Software Bill of Materials** — это полный машиночитаемый список каждого компонента артефакта: прямые и транзитивные зависимости, версии, лицензии и хэши, которые их пиннят. Два формата, которые важны, — **CycloneDX** (OWASP, заточен под безопасность и корреляцию уязвимостей) и **SPDX** (Linux Foundation / ISO 5962, естественный выбор для отчётности по лицензиям и комплаенсу). SBOM не пишут руками. Его генерируют в CI из собранного артефакта инструментом вроде `syft` или `cdxgen`, который проходит по базам пакетов образа и манифестам языков и выдаёт инвентарь.

Выигрыш — пятничный вопрос, на который отвечают за минуты. Когда выходит CVE, ты не делаешь grep по сорока репозиториям — ты запрашиваешь SBOM-ы, уже сохранённые на каждый релиз, и получаешь точное множество образов с затронутой версией и диапазоном. Свяжи `syft` со сканером вроде `grype`, и SBOM становится постоянной поверхностью CVE-корреляции: инвентарь генерируется один раз, а каждое новое уведомление автоматически сверяется с ним. Бэкдор xz-utils (CVE-2024-3094) — каноническая мотивация: build-time инъекция в `liblzma`, готовившаяся два года, — и индустриальный урок был прямым: нельзя защищать дерево зависимостей, которого не видишь, особенно транзитивные пакеты единственного мейнтейнера, о добавлении которых никто не помнит.

```bash
# Сгенерировать SBOM из собранного образа, в CycloneDX JSON
syft your-registry/app:1.4.2 -o cyclonedx-json=sbom.cdx.json

# Позже, когда выходит CVE-2024-3094 — ответить «затронуты ли мы?» из SBOM
grype sbom:sbom.cdx.json --fail-on high
```

## Provenance и SLSA: доказать, *как* собран артефакт

SBOM говорит, *что* внутри. **Build provenance** — это проверяемая запись того, *как* артефакт появился на свет: какой билдер его произвёл, из какого коммита исходника, с какими параметрами и входами сборки. Фреймворк, который это градирует, — **SLSA** (Supply-chain Levels for Software Artifacts); его Build-трек идёт от L0 до L3:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Уровень</th>
        <th class="border border-border px-3 py-2 text-left">Что гарантирует</th>
        <th class="border border-border px-3 py-2 text-left">Что делаешь, чтобы достичь</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><strong>L0</strong></td>
        <td class="border border-border px-3 py-2">Ничего — provenance не существует.</td>
        <td class="border border-border px-3 py-2">По умолчанию. Локальный <code>docker build</code> без записи.</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>L1</strong></td>
        <td class="border border-border px-3 py-2">Provenance <em>существует</em> и описывает, как шла сборка, — но может быть неполным или неподписанным.</td>
        <td class="border border-border px-3 py-2">Эмитить provenance из процесса сборки. Документация, ещё не гарантия от подделки.</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>L2</strong></td>
        <td class="border border-border px-3 py-2">Сборка идёт на <em>хостируемой</em> платформе, которая <em>подписывает</em> provenance — заметна для подмены.</td>
        <td class="border border-border px-3 py-2">Перенести сборки в управляемый CI (GitHub Actions и т.п.), который подписывает provenance за тебя.</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>L3</strong></td>
        <td class="border border-border px-3 py-2">Закалённый, <em>изолированный</em> билдер; provenance невозможно подделать даже тому, кто контролирует определение сборки.</td>
        <td class="border border-border px-3 py-2">Использовать изолированный эфемерный билдер (например, SLSA reusable workflow), где ключ подписи недостижим из шагов сборки.</td>
      </tr>
    </tbody>
  </table>
</div>

Скачок, который важен, — это L2 → L3. На L2 подпись доказывает, что provenance не изменили задним числом, но разработчик, контролирующий шаги сборки, всё ещё может заставить сборку врать о себе. L3 изолирует среду сборки и выносит генерацию provenance за пределы досягаемости кода самой сборки, так что метаданные точны *даже против инсайдера, владеющего определением пайплайна*. Это и есть планка для кода, которому ты реально доверяешь на деплое.

## Sigstore и cosign: keyless-подпись, записанная в публичный лог

Provenance и SBOM-ы хороши ровно настолько, насколько хороша подпись, связывающая их с конкретным артефактом, — а подпись всегда умирала на хранении ключа. **Sigstore** убирает долгоживущий ключ целиком. С keyless-подписью **cosign** твой CI-workflow аутентифицируется по **OIDC** (его workload-идентичность — «джоба `release.yml` в репозитории X на теге v1.4.2»), CA Sigstore **Fulcio** выпускает сертификат, связывающий эту идентичность с эфемерным ключом и самоуничтожающийся примерно за десять минут, ты подписываешь, а событие подписи пишется в **Rekor** — публичный, только-дополняемый лог прозрачности. Нет приватного ключа, который можно слить, ротировать или хранить в волте.

Артефакт связывается со своим provenance и SBOM через **аттестацию** — подписанное утверждение (формат in-toto) вида «для артефакта с дайджестом `sha256:…` вот его provenance / вот его SBOM». Action GitHub `attest-build-provenance` производит именно это для собранного образа и подписывает keyless через OIDC-идентичность workflow. Весь смысл Rekor в том, что подписант мониторит лог на свою собственную идентичность: неожиданная запись, подписанная как *ты*, — это обнаруженная компрометация, а не тихая.

```bash
# Keyless-подпись образа — OIDC-идентичность, эфемерный сертификат, лог в Rekor
COSIGN_EXPERIMENTAL=1 cosign sign your-registry/app@sha256:abcd...

# Привязать SBOM к дайджесту образа как подписанную аттестацию
cosign attest --predicate sbom.cdx.json --type cyclonedx \
  your-registry/app@sha256:abcd...

# На ДЕПЛОЕ — отвергнуть всё, что не подписано ожидаемой идентичностью workflow
cosign verify your-registry/app@sha256:abcd... \
  --certificate-identity-regexp 'https://github.com/acme/app/.github/workflows/release.yml@.*' \
  --certificate-oidc-issuer https://token.actions.githubusercontent.com
```

<Inset kind="why" lang="ru">
В GitHub Actions обычно пропускают сырой cosign и используют `actions/attest-build-provenance@v1`, который генерирует provenance в стиле SLSA для артефакта и подписывает его keyless OIDC-токеном джобы — без установки `cosign`, без управления ключами. Ему нужны `permissions: id-token: write` и `attestations: write`. Аттестация привязана к *дайджесту* образа, никогда к изменяемому тегу, — в этом весь смысл: тег вроде `:latest` можно перенаправить после проверки, поэтому подписывай и проверяй по дайджесту от начала до конца.
</Inset>

## Режим отказа: генерируешь доказательство, которое никогда не проверяешь

Каждый артефакт этого урока бесполезен, пока что-то *не откажется деплоить без него*. Доминирующий реальный отказ — не отсутствующий SBOM, а SBOM, сгенерированный в CI и ни разу не запрошенный; подпись, произведённая и ни разу не проверенная; аттестация, приложенная, и шаг деплоя, который её игнорирует. Подпись без проверки на деплое — это театр безопасности: ты заплатил за церемонию и не купил ни грамма гарантии. Шлагбаум — это `cosign verify` в пайплайне или admission-контроллер (Kyverno / Sigstore policy-controller), который отвергает любой образ, чья подпись и provenance не совпадают с ожидаемой идентичностью workflow, — запиненный по **дайджесту**, никогда по изменяемому тегу.

Другой тихий отказ — **доверие изменяемому базовому образу**: ты подписываешь свои слои, но `FROM someimage:latest` переразрешается в другие байты между сборкой и деплоем, так что твой подписанный артефакт стоит на непроверенном, заменяемом фундаменте. Пинни базовые образы по дайджесту и проверяй их тоже, иначе в цепочке доверия дыра в самом низу.

<TradeoffMatrix
  id="01-sbom-and-provenance-tradeoff-1"
  lessonSlug="01-sbom-and-provenance"
  lang="ru"
  prompt="Команда на хостируемом GitHub Actions хочет подписывать релизные образы и гейтить деплои. Выбери модель подписи и доверия."
  options={[
    { name: "Keyless cosign / artifact attestations: OIDC workload-идентичность, короткоживущий сертификат Fulcio, запись в Rekor", summary: "Без долгоживущего ключа.", correct: true, justification: "Выбор сеньора по умолчанию для сборок на хостируемом CI с OIDC-идентичностью. Нет долгоживущего приватного ключа, который можно слить, ротировать или сторожить в волте — сертификат связывает workload-идентичность сборки (конкретный workflow в конкретном репозитории и ref) и самоуничтожается за минуты, а Rekor даёт публичную, мониторируемую запись, так что неожиданная подпись как ты обнаружима. Проверка пиннится к ожидаемой идентичности workflow и issuer. Цена, которую принимаешь, — зависимость от публичной инфраструктуры Sigstore и правильно настроенного OIDC, что для хостируемого CI уже выполнено." },
    { name: "Долгоживущая пара ключей cosign в секрет-менеджере, монтируемая в джобу сборки", summary: "Классическое хранение ключа.", justification: "Рабочий и иногда обязательный вариант (air-gapped или нет пригодного OIDC-issuer), но он возвращает ровно ту проблему, что убирает keyless: приватный ключ, который можно вытащить из джобы, нужно ротировать, а кража которого позволяет атакующему подделывать твои подписи без публичного следа. Тянись к нему, только когда keyless реально невозможен, и тогда сторожи ключ как секрет высшего уровня." },
    { name: "Генерировать SBOM + provenance в CI, хранить их, но деплоить без шага проверки", summary: "Доказательство, без шлагбаума.", justification: "Это доминирующий реальный отказ, наряженный под безопасность. Ты платишь за каждую церемонию — SBOM, provenance, подпись, аттестация — и не гейтишь по ним ни на чём, так что подменённый или неподписанный образ деплоится ровно как хороший. Доказательство, которое не проверяешь, — бумажка. Вся ценность в шлагбауме проверки на деплое." },
    { name: "Подписывать по изменяемому тегу (:latest) и проверять тег на деплое", summary: "Тег можно перенаправить.", justification: "Тег — это подвижный указатель: его можно перенаправить на другие байты между моментом подписи и моментом проверки, так что проверка тега ничего не доказывает о том, что реально запускается. Подписывай и проверяй по неизменяемому дайджесту (sha256:...) от начала до конца — привязка к дайджесту и есть вся причина, почему аттестации целят в дайджест, а не в тег." },
  ]}
/>

<Quiz
  id="01-sbom-and-provenance-quiz-1"
  lessonSlug="01-sbom-and-provenance"
  lang="ru"
  question="Твой пайплайн генерирует SBOM и подписывает каждый образ keyless cosign, но шаг деплоя просто тянет и запускает образ. Подменённый образ запушен скомпрометированным токеном. Что произойдёт?"
  choices={[
    { label: "Он задеплоится — подпись без шлагбаума проверки на деплое (cosign verify или admission-политика) ничего не меняет в том, что разрешено запускать", correct: true },
    { label: "Он будет заблокирован, потому что cosign sign также отвергает неподписанные образы при pull", misconception: "cosign sign лишь производит подпись; он никак не влияет на pull или деплой. Ничто не проверяет подпись, пока ты не добавишь явный шаг cosign verify или admission-контроллер. Генерировать доказательство и не проверять его — доминирующий отказ цепочки поставок." },
    { label: "Он будет заблокирован, потому что SBOM-скан в CI уже отверг его", misconception: "SBOM-скан бежит против легитимно собранного артефакта в CI; он не стоит в пути деплоя образа, запушенного вне процесса украденным токеном. Останавливает его только шлагбаум проверки на деплое, привязанный к ожидаемой идентичности workflow." },
    { label: "Он задеплоится, но Rekor автоматически откатит его, когда плохая запись попадёт в лог", misconception: "Rekor — только-дополняемый лог прозрачности, а не точка применения. Он записывает события подписи, чтобы ты мог обнаруживать аномалии; он не может блокировать или откатывать деплой. Применение — это твой шаг проверки или admission-политика." },
  ]}
/>

<Quiz
  id="01-sbom-and-provenance-quiz-2"
  lessonSlug="01-sbom-and-provenance"
  lang="ru"
  question="В чём ключевая разница между SLSA Build L2 и L3?"
  choices={[
    { label: "L3 изолирует билдер, так что provenance невозможно подделать даже тому, кто контролирует определение сборки; L2 лишь гарантирует, что хостируемая платформа подписала provenance", correct: true },
    { label: "У L2 нет provenance, а L3 его добавляет", misconception: "Provenance существует начиная с L1. У L2 уже есть хостируемый, подписанный provenance — скачок L2→L3 про изоляцию и точность под инсайдером, контролирующим шаги сборки, а не про то, существует ли provenance вообще." },
    { label: "L3 требует SBOM, а L2 нет", misconception: "Уровни SLSA Build градируют целостность provenance (полноту, аутентичность, точность того, как собран артефакт), а не наличие SBOM. SBOM и SLSA — взаимодополняющие, раздельные заботы." },
    { label: "L2 использует keyless-подпись, а L3 — аппаратный ключ", misconception: "Различие уровней — про изоляцию билдера и устойчивость генерации provenance к подмене, а не про конкретный механизм подписи. Любой уровень может использовать keyless или ключевую подпись." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Пайплайн доверия цепочки поставок ПО от коммита исходника через сборку, SBOM и подпись, аттестацию в логе прозрачности, до шлагбаума проверки на деплое"
  perRow={5}
  hue="var(--d-systems)"
  nodes={[
    { id: "src", label: "Источник", sub: "commit" },
    { id: "build", label: "Сборка (CI)", sub: "OIDC id" },
    { id: "sbom", label: "SBOM + Подпись", sub: "syft/cosign" },
    { id: "att", label: "Аттестация", sub: "Rekor log" },
    { id: "verify", label: "Проверка", sub: "@ деплой" },
  ]}
  edges={[
    { from: "src", to: "build", label: "checkout" },
    { from: "build", to: "sbom", label: "артефакт+дайджест" },
    { from: "sbom", to: "att", label: "привязка к дайджесту" },
    { from: "att", to: "verify", label: "проверка политики" },
  ]}
  caption="Каждый шаг передаёт следующему более сильное утверждение, но только финальный шлагбаум Проверки превращает цепочку в применение. SBOM, provenance и подписи без шага проверки на деплое — бумажка, и всё привязывается к неизменяемому дайджесту, никогда к изменяемому тегу."
/>

<KeyTakeaway>SBOM отвечает «затронуты ли CVE-X?» за минуты; provenance с уровнями SLSA доказывает, как собрано; keyless-cosign связывает это аттестацией в Rekor. Ничто не считается без шлагбаума проверки на деплое, по дайджесту.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-sbom-and-provenance-retrieval"
  lang="ru"
  questions={[
    { q: "Пройди шаги генерация → подпись → аттестация → проверка и назови, что делает каждый шаг доверенным.", a: "В CI ты собираешь артефакт и фиксируешь его неизменяемый дайджест (sha256). Ты генерируешь SBOM из этого собранного артефакта через syft или cdxgen в CycloneDX или SPDX — машиночитаемый инвентарь каждой прямой и транзитивной зависимости, что позволяет ответить «затронуты ли мы CVE-X?» запросом, а не grep'ом по репозиториям. Ты подписываешь keyless через cosign: workflow аутентифицируется по OIDC своей workload-идентичностью, Fulcio выпускает ~10-минутный сертификат, связывающий эту идентичность с эфемерным ключом, ты подписываешь, а Rekor записывает событие в только-дополняемый публичный лог прозрачности — так нет долгоживущего ключа для утечки, и подписант может мониторить лог на неожиданные подписи как он. Ты связываешь SBOM и provenance с дайджестом через in-toto аттестацию (cosign attest или GitHub attest-build-provenance, который ещё и производит provenance в стиле SLSA). Наконец — шаг, который реально важен — на деплое ты запускаешь cosign verify или admission-контроллер, отвергающий любой образ, чья подпись, provenance и идентичность не совпадают с ожидаемым workflow, запиненный по дайджесту. Доверие идёт от: SBOM, сгенерированного из реального артефакта; подписи, привязанной к конкретной workload-идентичности, а не к крадомому ключу; публичного лога, делающего подделку обнаружимой; и шлагбаума проверки как единственного, что превращает всё это в применение." },
    { q: "Почему «мы генерируем SBOM и подписываем каждый образ» недостаточно и какие два пробела обычно остаются?", a: "Генерация и подпись — церемонии на стороне производства; они ничего не меняют в том, что разрешено запускать. Первый и доминирующий пробел — нет шлагбаума проверки на деплое: если шаг деплоя просто тянет и запускает, подменённый или неподписанный образ — скажем, запушенный скомпрометированным токеном — деплоится ровно как хороший, потому что ничто не проверяет подпись. Фикс — cosign verify в пайплайне или admission-контроллер (Kyverno / Sigstore policy-controller), привязанный к ожидаемой идентичности workflow и issuer, который отвергает всё несовпадающее. Второй пробел — доверие изменяемым ссылкам: подпись и проверка тега вроде :latest ничего не доказывают, потому что тег можно перенаправить на другие байты между подписью и проверкой, а FROM base:latest может переразрешиться между сборкой и деплоем, так что твои подписанные слои стоят на непроверенном фундаменте. Фикс — пиннить и проверять всё по неизменяемому дайджесту от начала до конца, поэтому аттестации и целят в дайджест, а не в тег." },
  ]}
/>

<Recap lang="ru">
Защита цепочки поставок ПО — это три раздельных утверждения, и только последнее — применение. SBOM — машиночитаемый инвентарь каждой прямой и транзитивной зависимости, сгенерированный в CI из собранного артефакта через syft или cdxgen в формате CycloneDX или SPDX, так что когда выходит CVE вроде бэкдора xz-utils, ты отвечаешь «затронуты ли мы?» запросом к сохранённым SBOM за минуты, а не grep'ом по репозиториям. Build provenance — проверяемая запись того, как собран артефакт: какой билдер, какой коммит, какие параметры, — градируемая SLSA от L1 (provenance существует) через L2 (хостируемая платформа его подписывает) до L3 (изолированный билдер делает provenance неподделываемым даже тем, кто контролирует определение сборки). cosign от Sigstore подписывает keyless: workflow аутентифицируется по OIDC, Fulcio выпускает ~10-минутный сертификат, привязанный к этой workload-идентичности, а Rekor записывает подпись в публичный только-дополняемый лог, так что нет долгоживущего ключа для хранения. In-toto аттестация связывает SBOM и provenance с неизменяемым дайджестом артефакта. Но режим отказа, который бьёт команды, — сгенерировать всё это и никогда не проверить: подпись без шлагбаума проверки на деплое или доверие изменяемому базовому образу или тегу, который можно перенаправить между подписью и проверкой. Шлагбаум — cosign verify или admission-политика, запиненная к ожидаемой идентичности workflow и дайджесту, — единственный шаг, превращающий бумажку в гарантию.
</Recap>
