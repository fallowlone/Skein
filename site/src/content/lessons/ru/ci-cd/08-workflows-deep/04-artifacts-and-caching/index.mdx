---
concepts:
- ci-cd-gha-artifacts-cache
deepensInto: []
estMin: 16
lang: ru
lessonType: topic
level: senior
order: 4
prereqs: []
slug: "04-artifacts-and-caching"
sources:
- https://docs.github.com/en/actions/using-workflows/storing-workflow-data-as-artifacts
- https://docs.github.com/en/actions/using-workflows/caching-dependencies-to-speed-up-workflows
- https://github.com/actions/cache
spiral: []
status: ready
title: "Артефакты и кэш: передача файлов между джобами и кэш, который не попадает"
summary: "Артефакты двигают файлы между джобами и наружу из запуска (дефолтное хранение 90 дней); кэш ускоряет повторные запуски через restore-ключ. Классический провал: ключ кэша без хэша lockfile, поэтому он не инвалидируется, или не попадает через окно вытеснения 10 ГБ / 7 дней."
track: "ci-cd"
unit: "08-workflows-deep"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
«Кэш сломан — он никогда не попадает». Каждый запуск CI всё ещё занимал восемь минут на переустановку зависимостей, хотя команда добавила `actions/cache` неделями ранее. Логи говорили `Cache not found for input keys` почти на каждом запуске. Проблема была в ключе: кто-то написал `key: node-modules` — константную строку. Запись кэша пишется под своим ключом, только если записи с этим точным ключом ещё нет; раз `node-modules` сохранён на самом первом запуске, он стал **неизменяемым**, поэтому каждый последующий запуск его находил... но команда также недоумевала, почему апгрейды зависимостей никогда не вступали в силу в CI. Оба симптома — один баг с двух сторон: ключ без хэша содержимого никогда не инвалидируется (так устаревший кэш служит вечно) *и*, из-за несвязанной опечатки в пути, сохранённая запись была пуста, так что restore был no-op. Фиксом был канонический паттерн — `key: node-${{ hashFiles('**/package-lock.json') }}` плюс префикс `restore-keys:` — который привязывает идентичность кэша к lockfile, так что он инвалидируется ровно тогда, когда меняются зависимости, и переиспользует близкое совпадение в остальных случаях.
</Hook>

<Crux>Артефакты двигают файлы между джобами; кэш ускоряет повторные запуски — ключ без хэша содержимого либо не попадает, либо не инвалидируется.</Crux>

<Explanation>

## Артефакты против кэша: две разные задачи

Они выглядят похоже — оба хранят файлы — но решают противоположные проблемы:

- **Артефакты** выносят файлы *из* workflow: выходы сборки, тест-отчёты, coverage, логи — то, что человек или поздний джоб скачивает. Загружай через `actions/upload-artifact`, забирай через `actions/download-artifact`. Это *единственный* способ передать файлы между джобами, ведь каждый джоб выполняется на свежем раннере. Дефолтное **хранение — 90 дней** (настраивается per-upload вниз до 1 дня или задаётся на весь репозиторий), после чего они удаляются.
- **Кэш** ускоряет *будущие* запуски той же работы: каталоги зависимостей (`~/.npm`, `node_modules`, `~/.cargo`), промежуточные результаты сборки. Используй `actions/cache`. Запись кэша ключуется и **неизменяема после записи** — и она best-effort, не гарантирована: по политике вытеснения GitHub удаляет кэши, не запрашивавшиеся **7 дней**, и когда кэши репозитория превышают **10 ГБ суммарно**, он вытесняет least-recently-used записи, чтобы остаться под лимитом.

Ментальное правило: артефакт = «мне нужен этот файл позже или человеку», кэш = «пересчёт этого медленный, а входы редко меняются». Никогда не используй кэш для выходов сборки, которые нельзя терять (он может быть вытеснен в любой момент); никогда не используй артефакты как кэш скорости (нет restore по ключу, а 90-дневное хранение стоит места).

```yaml
- uses: actions/upload-artifact@v4
  with:
    name: dist
    path: build/
    retention-days: 7      # сократи с дефолта 90 дней для эфемерных сборок

- uses: actions/cache@v4
  with:
    path: ~/.npm
    key: npm-${{ runner.os }}-${{ hashFiles('**/package-lock.json') }}
    restore-keys: |
      npm-${{ runner.os }}-
```

## Ключ кэша — это весь дизайн

Ключ кэша должен делать две противоречивые вещи: **инвалидироваться при изменении входов** и **попадать, когда они не меняются.** Канонический паттерн решает это двумя полями:

- **`key`** должен включать **хэш содержимого входов** — `hashFiles('**/package-lock.json')`. Когда lockfile меняется, хэш меняется, ключ меняется, и старый кэш правильно *не* используется. Ключ без хэша (`key: npm` из Hook) никогда не меняется, поэтому либо служит устаревшим кэшем вечно, либо — раз записи неизменяемы — фиксирует то, что сохранено первым.
- **`restore-keys`** — упорядоченный список **префиксов**, пробуемых при промахе точного `key`. При изменении lockfile точный ключ ещё не существует, но `restore-keys: npm-${{ runner.os }}-` совпадает с *предыдущим* кэшем как частичное попадание — так ты стартуешь от `node_modules` прошлой сборки и устанавливаешь только дельту, затем сохраняешь под новым точным ключом. Это и превращает холодный кэш в тёплый сквозь подъёмы зависимостей.

Семантика сохранения ловит людей: `actions/cache` сохраняет автоматически в конце джоба **только если точный `key` ещё не существовал** (попадание по точному ключу означает отсутствие сохранения). Неизменяемость означает, что нельзя перезаписать запись — чтобы «обновить» кэш, надо изменить ключ. А из-за вытеснения по 7-дневному простою и 10 ГБ-LRU кэш редко запускаемого workflow может просто исчезнуть, что *правильное* поведение, а не баг — кэш это оптимизация, обязанная терпеть промахи.

<Quiz
  id="04-artifacts-and-caching-quiz-1"
  lessonSlug="04-artifacts-and-caching"
  lang="ru"
  question="Workflow использует key: deps-${{ hashFiles('package-lock.json') }} с restore-keys: deps-. PR поднимает одну зависимость, меняя lockfile. Что происходит на том запуске и что сохраняется?"
  choices={[
    { label: "Точный ключ промахивается (новый хэш), restore-keys находит предыдущий кэш deps- как частичное попадание, поэтому установка быстрая, и в конце джоба сохраняется НОВЫЙ кэш под новым точным ключом", correct: true },
    { label: "Точный ключ промахивается, и ничего не восстанавливается, поэтому это полная холодная установка без выгоды", misconception: "restore-keys существует именно для этого случая: префикс deps- совпадает с кэшем прошлого запуска как частичное попадание, поэтому стартуешь тёплым со старого каталога зависимостей и ставишь только дельту — не холодная установка." },
    { label: "Старый кэш перезаписывается на месте новыми зависимостями под тем же ключом", misconception: "Кэши неизменяемы и ключуются по хэшу содержимого — изменение lockfile даёт НОВЫЙ ключ, поэтому пишется новая запись; старая никогда не перезаписывается, она лишь стареет через вытеснение." },
    { label: "Запуск падает, потому что точный ключ больше не существует", misconception: "Промах кэша никогда не ошибка — он проваливается к restore-keys, а затем к обычной установке. Кэш best-effort по дизайну; промах лишь означает отсутствие ускорения, не сбой." },
  ]}
/>

<Inset kind="why" lang="ru">
Почему записи кэша неизменяемы, а не перезаписываются при сохранении? Потому что иначе параллельные джобы гонялись бы за запись одного ключа, и читатель мог бы получить полузаписанный кэш, или две ветки могли бы затереть состояние зависимостей друг друга под общим ключом. Неизменяемость делает ключ стабильной идентичностью содержимого: один ключ всегда означает те же байты, поэтому попаданию можно доверять без перепроверки. Цена в том, что «обновить» кэш невозможно — ты делаешь *новый* ключ (что хэш содержимого делает за тебя автоматически при изменении входов) и даёшь LRU-вытеснению удалить старый. Вот почему хэш содержимого в ключе — не опциональный лоск; это то, что делает неизменяемую модель корректной, а не вечно устаревшей.
</Inset>

## Размеры и гигиена

Кэши и артефакты оба стоят места и оба имеют лимиты, кусающие на масштабе. Одна запись кэша должна быть каталогом зависимостей, а не всем workspace — кэширование `node_modules` плюс `~/.npm` плюс выхода сборки утраивает размер и толкает к 10 ГБ-LRU-обрыву быстрее, вытесняя *другие* полезные кэши. Артефакты по умолчанию хранятся 90 дней, что для per-PR выходов сборки расточительно; сократи `retention-days` до недели или меньше для эфемерных данных, а долгое хранение оставь релизам и аудит-логам. Сжимай перед загрузкой крупных артефактов и вообще не загружай `node_modules` как артефакт — для этого есть кэш. Дисциплина — сопоставлять каждое хранилище его задаче: долговечная передача → артефакт с намеренным хранением; оптимизация скорости с терпимыми промахами → кэш с ключом, хэширующим содержимое.

<Quiz
  id="04-artifacts-and-caching-quiz-2"
  lessonSlug="04-artifacts-and-caching"
  lang="ru"
  question="Команда кэширует выход сборки (финальный скомпилированный бинарь) вместо загрузки его как артефакт, чтобы «сэкономить время на скачивании». Почему это неправильное хранилище и что ломается?"
  choices={[
    { label: "Кэш best-effort и вытесняемый (7 дней простоя, 10 ГБ LRU), поэтому бинарь может исчезнуть между джобами или запусками; выходы сборки, которые нельзя терять, принадлежат артефакту с гарантированным хранением", correct: true },
    { label: "Кэш не может хранить бинарные файлы, только текстовые манифесты зависимостей", misconception: "Кэш хранит любые файлы, включая бинари — проблема не в типе файла, а в гарантии долговечности. Кэш может быть вытеснен в любой момент; артефакт хранится своё окно." },
    { label: "Кэши всегда медленнее восстанавливаются, чем скачивание артефактов", misconception: "Скорость восстановления сопоставима и не в этом дело. Реальный дефект — использование вытесняемого best-effort хранилища для выхода, который должен пережить — проблема корректности/долговечности, а не производительности." },
    { label: "Артефакты и кэш взаимозаменяемы, поэтому ничего на самом деле не ломается", misconception: "Они не взаимозаменяемы: кэш — оптимизация, терпящая промахи, артефакты — долговечная передача. Класть выход, который нужно сохранить, в кэш означает, что он может тихо исчезнуть, ломая нижестоящий джоб, которому он нужен." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Джоб build выдаёт долговечный артефакт для поздних джобов и кэш по хэшу содержимого для будущих запусков; restore-keys превращает промах ключа в тёплое частичное попадание"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "build", label: "джоб build", sub: "производит файлы" },
    { id: "artifact", label: "Артефакт", sub: "хранение 90д" },
    { id: "cache", label: "Сохранение кэша", sub: "key = hashFiles()" },
    { id: "exact", label: "Точный ключ", sub: "попал → нет save" },
    { id: "restore", label: "restore-keys", sub: "частичное попадание по префиксу" },
    { id: "evict", label: "Вытеснение", sub: "7д простоя / 10ГБ LRU" },
  ]}
  edges={[
    { from: "build", to: "artifact", label: "upload" },
    { from: "build", to: "cache", label: "save если новый ключ" },
    { from: "cache", to: "exact", label: "lookup" },
    { from: "exact", to: "restore", label: "промах" },
    { from: "cache", to: "evict", label: "устарел" },
    { from: "artifact", to: "build", label: "download позже" },
  ]}
  caption="Артефакты — долговечная передача (намеренное хранение); кэш — вытесняемая оптимизация, чей ключ по хэшу содержимого инвалидируется при изменении входов, а restore-keys восстанавливает тёплое частичное попадание."
/>

<KeyTakeaway>Артефакты и кэш оба хранят файлы, но решают противоположные проблемы: артефакты — долговечный способ передать файлы между джобами на свежих раннерах и наружу из запуска (дефолт хранения 90 дней, сокращаемый), а кэш — best-effort ускорение, которое GitHub вытесняет после 7 дней простоя или когда репозиторий проходит 10 ГБ суммарно (LRU). Ключ кэша — весь дизайн: он должен включать хэш содержимого вроде `hashFiles('**/package-lock.json')`, чтобы инвалидироваться ровно при изменении входов, с префиксами `restore-keys`, восстанавливающими тёплое частичное попадание при промахе. Записи неизменяемы — попадание по точному ключу пропускает сохранение, и ты «обновляешь» кэш только сменой ключа. Никогда не кэшируй выход сборки, который нельзя терять (он может исчезнуть); никогда не используй артефакты как кэш скорости.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-artifacts-and-caching-retrieval"
  lang="ru"
  questions={[
    { q: "Почему ключ кэша должен включать хэш содержимого и что restore-keys добавляет поверх него?", a: "Ключ кэша должен удовлетворить две противоположные цели: инвалидироваться при изменении входов и попадать, когда они не меняются. Хэш содержимого вроде hashFiles('**/package-lock.json') достигает первого — при изменении lockfile меняются хэш и, значит, ключ, поэтому устаревший кэш правильно не переиспользуется; константный ключ никогда не меняется и, раз записи неизменяемы после записи, фиксирует то, что сохранено первым, и служит этим вечно. restore-keys — упорядоченный список префиксов ключа, пробуемых только при промахе точного ключа: при подъёме зависимости новый точный ключ ещё не имеет записи, но префикс вроде npm-${{ runner.os }}- совпадает с предыдущим кэшем как частичное попадание, поэтому джоб стартует тёплым с каталога зависимостей прошлой сборки, ставит только дельту и затем сохраняет свежую запись под новым точным ключом. Вместе они дают корректную инвалидацию плюс тёплое переиспользование сквозь изменения." },
    { q: "Противопоставь артефакты и кэш по долговечности и применению и назови числа вытеснения и хранения.", a: "Артефакты — долговечная передача: они выносят файлы из workflow (выходы сборки, отчёты, логи) человеку или позднему джобу, являются единственным способом двигать файлы между джобами на свежих раннерах и хранятся 90 дней по умолчанию (настраивается вниз до 1 дня или на весь репозиторий). Кэш — best-effort оптимизация для повторных запусков (каталоги зависимостей, промежуточные результаты сборки): он ключуется, неизменяем после записи и явно вытесняем — GitHub удаляет кэши, не запрашивавшиеся 7 дней, и, когда кэши репозитория превышают 10 ГБ суммарно, вытесняет least-recently-used записи, чтобы остаться под потолком. Правило: выходы, которые нельзя терять, идут в артефакты с намеренным хранением; медленно-пересчитываемые, редко-меняющиеся входы идут в кэш с ключом по хэшу содержимого, терпящим промахи. Класть выход, который нужно сохранить, в кэш рискует тихим вытеснением; использовать артефакты как кэш скорости тратит место и не имеет restore по ключу." },
  ]}
/>

<Recap lang="ru">
Артефакты и кэш оба сохраняют файлы, но отвечают на противоположные нужды. Артефакты — долговечный канал: они выносят выходы сборки, отчёты, coverage и логи из workflow человеку или нижестоящему джобу, и поскольку каждый джоб выполняется на свежем раннере, они единственный способ двигать файлы между джобами — хранятся 90 дней по умолчанию, что стоит сокращать через `retention-days` для эфемерных per-PR данных, а долгое оставлять релизам и аудит-следам. Кэш — best-effort ускорение повторной работы вроде каталогов зависимостей и промежуточных результатов сборки: записи ключуются и неизменяемы после записи, и они явно вытесняемы — GitHub удаляет кэши, нетронутые 7 дней, и, за 10 ГБ суммарного кэша репозитория, вытесняет least-recently-used записи, так что промах — корректное поведение, которое дизайн обязан терпеть. Ключ кэша несёт всё бремя корректности: включи хэш содержимого вроде `hashFiles('**/package-lock.json')`, чтобы ключ менялся ровно при изменении входов — бесхэшевый константный ключ либо служит устаревшим кэшем вечно, либо, при неизменяемости, замораживает то, что сохранено первым — и добавь префиксы `restore-keys`, чтобы свежий промах точного ключа всё равно восстановил предыдущий кэш как тёплое частичное попадание, ставя только дельту перед сохранением под новым ключом. Сопоставляй каждое хранилище его задаче: долговечную передачу, которую нельзя терять, — в артефакт с намеренным хранением; медленно-пересчитываемые, редко-меняющиеся входы — в кэш по хэшу содержимого, для которого промахи бесплатны. Кэшируй каталог зависимостей, а не весь workspace, чтобы держаться вдали от 10 ГБ-LRU-обрыва, вытесняющего твои другие полезные кэши.
</Recap>
