---
concepts:
- cis-benchmark
- configuration-baseline
- drift
- compliance-as-code
- hardening-guide
deepensInto: []
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 3
prereqs: []
slug: 03-cis-benchmarks-and-baselines
sources:
- https://www.cisecurity.org/cis-benchmarks
- https://csrc.nist.gov/pubs/sp/800/123/final
spiral: []
status: ready
summary: 'CIS Benchmarks дают проверенную базу укрепления, чтобы её не изобретать.
  Зрелая работа — это цикл после: зафиксировать базу как код, а затем находить и закрывать
  дрейф конфигурации, пока атакующий не нашёл брешь.'
title: CIS benchmarks and baselines
track: security-defensive
unit: 03-hardening-defense-in-depth
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Пост-мортем на три страницы, а корневая причина — одна строка: `PermitRootLogin yes`. Полгода назад кто-то разбирал зависший деплой, зашёл по SSH под root, переключил этот флаг и не вернул обратно. В образе, который вы выкатили, стояло `no`. На работающем хосте — нет. Ни CVE, ни цепочки эксплойтов — просто настройка, уехавшая от базы, которую вы считали действующей, на машине, которую никто не перепроверял. Атакующий не взломал вход. Он вошёл. Вот почему укрепление — не разовая задача: база, которую вы задали и ни разу не сверяли, — это база, которую вы уже потеряли.
</Hook>

К концу урока вы поймёте, почему стартовать укрепление надо с CIS Benchmark, как зафиксировать эту базу так, чтобы она пережила столкновение с продакшеном, и как обнаруживать дрейф, который тихо открывает обратно закрытые вами дыры.

<Crux>CIS Benchmarks дают проверенную базу укрепления; работа — находить и закрывать дрейф конфигурации, пока он не стал эксплойтом.</Crux>

<Explanation>

## Что такое CIS Benchmark на самом деле

Вам не нужно с нуля изобретать ответ на вопрос «как выглядит укреплённый хост Ubuntu» — это месяцы исследований на каждую платформу, и вы что-нибудь упустите. **CIS Benchmark** — это уже готовый ответ: разработанное по консенсусу, предписывающее руководство по конфигурации для конкретной платформы — дистрибутива Linux, Windows Server, Kubernetes, PostgreSQL, nginx, аккаунта AWS, демона Docker. Их больше сотни, каждый поддерживается сообществом практиков и пересматривается под новые версии платформ и новые техники атак. Там, где общее руководство вроде NIST SP 800-123 описывает *принципы* укрепления сервера (минимизировать поверхность атаки, работать с наименьшими привилегиями, патчить, логировать), CIS Benchmark даёт *точные настройки*, которые реализуют эти принципы на стоящей перед вами машине — вплоть до файла, строки и ожидаемого значения.

Две структурные черты важны для того, как им пользоваться. Во-первых, рекомендации делятся на **Level 1** (практичное укрепление, которое не должно ломать нормальную работу — здравый дефолт для большинства парков) и **Level 2** (глубокая эшелонированная защита для сред высокой безопасности, где жертвуют частью функциональности или удобства). Во-вторых, каждая рекомендация помечена **Automated** или **Manual**: у Automated есть машинно-проверяемая процедура аудита и исправления, Manual требует человеческого суждения. Это разделение — самое важное, что зрелый инженер считывает с benchmark, потому что оно прямо говорит, какие контроли можно обеспечивать непрерывно, а какие требуют процесса.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Понятие benchmark</th>
        <th class="border border-border px-3 py-2 text-left">Что означает</th>
        <th class="border border-border px-3 py-2 text-left">Зрелое прочтение</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Level 1</td>
        <td class="border border-border px-3 py-2">Практичное укрепление, не должно ломать работу</td>
        <td class="border border-border px-3 py-2">База для всего парка; обеспечивать по умолчанию</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Level 2</td>
        <td class="border border-border px-3 py-2">Эшелонированная защита, может жертвовать функцией</td>
        <td class="border border-border px-3 py-2">Только для ценных хостов; сначала проверить на поломки</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Automated</td>
        <td class="border border-border px-3 py-2">Машинно-проверяемый аудит + исправление</td>
        <td class="border border-border px-3 py-2">Кодировать; проверять каждый хост, каждый день</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Manual</td>
        <td class="border border-border px-3 py-2">Требует человеческого суждения для оценки</td>
        <td class="border border-border px-3 py-2">За ним стоит процесс/ревью, а не правило сканера</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Profile</td>
        <td class="border border-border px-3 py-2">Набор рекомендаций, под которым вы подписались</td>
        <td class="border border-border px-3 py-2">Ваша база = benchmark минус задокументированные исключения</td>
      </tr>
    </tbody>
  </table>
</div>

## От benchmark к *вашей* базе

Benchmark — это не база. Benchmark — это меню; ваша **конфигурационная база** — это блюдо, под которым вы реально подписались: конкретный набор рекомендаций выбранного вами уровня, *минус исключения, которые ваша среда законно требует*, причём каждое исключение записано с причиной и владельцем. Именно на этом различии тихо ломается большинство программ укрепления. Команда, для которой целью становится «100% соответствие CIS», натыкается на рекомендацию, ломающую их приложение — скажем, отключение модуля ядра, нужного их агенту мониторинга, — и вместо того, чтобы записать обоснованное исключение, либо выкатывает сломанный хост, либо молча отключает всю проверку. Теперь база лжёт.

Зрелый ход — вывести базу один раз, осознанно. Начните с подходящего уровня (Level 1 для парка), пройдите по рекомендациям и по каждой примите явное решение: **применить** или **исключить-с-причиной**. На выходе — документ базы, в идеале машиночитаемый, где отклонение никогда не сюрприз. Почему это важно для обнаружения дрейфа — причинно: если вы не можете сказать, что такое «правильно», вы не сможете обнаружить «неправильно». Чистая база с аннотированными исключениями — это *эталон*, с которым сверяется каждый работающий хост. Нет эталона — нет обнаружения дрейфа, только надежда.

<Inset kind="why" lang="ru">
Почему не целиться просто в 100%? Потому что процент соответствия — это среднее, а среднее прячет именно те контроли, что важны. Хост может набрать 96%, при этом недостающие 4% — это «вход root по SSH включён» и «аудит-логирование отключено», две настройки, которых атакующий хочет больше всего. Скоринг CIS взвешивает каждую Automated-проверку примерно одинаково, поэтому число поощряет закрытие множества мелочей вместо одной критичной. Используйте балл как грубый тренд, никогда как цель. Цель — *этот конкретный набор контролей обеспечен, и каждое отклонение — записанное решение*, чего один процент выразить не может.
</Inset>

## Дрейф: как укреплённый хост сам себя раз-укрепляет

Вы укрепляете золотой образ, выкатываете его, идёте дальше. Потом приходит энтропия. **Конфигурационный дрейф** — это зазор, открывающийся со временем между базой, с которой хост был развёрнут, и его реальным работающим состоянием. Обычно это не злой умысел — это эксплуатация: инженер отключает контроль, чтобы разобрать инцидент в 2 часа ночи, и забывает включить обратно; обновление пакета сбрасывает конфиг к вендорскому дефолту; ручной `hotfix` чинит одну машину, но не остальные сорок; две команды владеют пересекающимся конфигом и каждая «исправляет» другую. Каждое изменение по отдельности разумно. В сумме — парк, где нет двух одинаковых хостов, и спроектированная вами защита существует только в образе, а не в продакшене.

Дрейф опасен именно потому, что по умолчанию невидим. Хост всё ещё работает. Приложение всё ещё обслуживает трафик. Ничто не алертит. Уехавшая настройка заявляет о себе лишь тогда, когда её найдёт атакующий — или аудитор. Поэтому работа защитника — сделать дрейф *видимым*: непрерывно переаудировать каждый работающий хост против базы и поднимать каждое отклонение, чтобы контроль, переключившийся с `no` на `yes`, выдал находку на следующий день, а не в пост-мортеме.

<FlowDiagram
  label="Жизненный цикл дрейфа: хост развёрнут из укреплённой базы CIS; со временем операционные изменения (отладочная правка, обновление пакета, сбросившее конфиг) уводят его от базы; без переаудита зазор остаётся невидимым, пока его не найдёт атакующий или аудитор; непрерывный аудит против базы обнаруживает отклонение, а исправление возвращает хост к эталону."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "base", label: "Укреплённая база", sub: "из CIS" },
    { id: "provision", label: "Хост развёрнут", sub: "совпадает с базой" },
    { id: "drift", label: "Операц. изменение", sub: "контроль уехал" },
    { id: "audit", label: "Непрерыв. аудит", sub: "против базы" },
    { id: "fix", label: "Исправить", sub: "назад к эталону" },
  ]}
  edges={[
    { from: "base", to: "provision" },
    { from: "provision", to: "drift", label: "энтропия" },
    { from: "drift", to: "audit", label: "находим" },
    { from: "audit", to: "fix", label: "сходимся" },
  ]}
  caption="Дрейф — состояние по умолчанию для любого долгоживущего хоста. Удержать парк на базе способен только цикл: аудит каждого хоста против эталона, выявление отклонений и исправление — однократного укрепления при развёртывании недостаточно."
/>

## Compliance as code: замыкаем цикл

Обнаруживать дрейф вручную не масштабируется дальше горстки хостов. Устойчивый паттерн — **compliance as code**: выразить базу как машиночитаемые, версионируемые правила и запускать их автоматически против каждого хоста по расписанию и в деплой-конвейере. CIS поставляет свои базы в структурированном формате и даёт инструмент (CIS-CAT), а открытая экосистема кодирует benchmarks как OpenSCAP/SCAP-контент или как политику в инструментах вроде Chef InSpec, Ansible или OPA. Форма одинакова независимо от инструмента: контроль — это правило с *аудитом* (как прочитать текущее значение) и часто *исправлением* (как выставить правильное), закоммиченное в репозиторий и запускаемое CI.

Кодирование базы как кода даёт вам четыре вещи разом. База становится **ревьюируемой** (изменение контроля проходит через pull request, а не через SSH-сессию в 2 ночи), **версионируемой** (вы можете доказать, что было «правильным» на любую дату — это любят аудиторы и требуют инциденты), **единообразно обеспеченной** (одни и те же правила бегут на хосте 1 и хосте 41, так что дрейф между хостами ловится) и **быстро обнаруживаемой** (плановый скан превращает полугодовую тихую регрессию в утреннюю находку). Оставшееся зрелое решение — что делать при отклонении: **только алерт** для контролей, где авто-починка могла бы сломать рабочую нагрузку (пусть согласует человек), против **авто-исправления** для контролей, где правильное значение однозначно и безопасно для обеспечения — сводя хост обратно к эталону, не дожидаясь тикета.

<TradeoffMatrix
  id="03-cis-baselines-tradeoff-1"
  lessonSlug="03-cis-benchmarks-and-baselines"
  lang="ru"
  prompt="Плановый скан compliance-as-code сообщает, что `PermitRootLogin` уехал с `no` на `yes` на 1 из 40 продакшен-хостов. Какой правильный дизайн ответа?"
  options={[
    { name: "Считать версионируемую базу источником истины: завести находку, а для однозначного контроля вроде этого — авто-исправить хост обратно на `no`, записав отклонение и кто/что его изменил", summary: "Найти, исправить, записать.", correct: true, justification: "Это замкнутый цикл. База-как-код — эталон, поэтому уехавший хост — обнаруженное отклонение, а не предмет спора. У `PermitRootLogin no` однозначно правильное значение, и нет законной причины рабочей нагрузки быть `yes`, поэтому авто-исправление безопасно и немедленно сводит хост к эталону — а записанное отклонение скармливается дальше в выяснение, почему он уехал (настоящая корневая причина)." },
    { name: "Обновить базу на `PermitRootLogin yes`, чтобы скан снова стал зелёным", summary: "Заглушить тревогу.", justification: "Это делает базу соответствующей реальности, вместо того чтобы делать реальность соответствующей базе — ровно наоборот. Весь смысл базы в том, что она кодирует *правильное* состояние; редактируя её под уехавшее, небезопасное значение, вы отмываете регрессию безопасности в «соответствие». Скан делает свою работу; подавляя его, вы намеренно открываете дыру обратно." },
    { name: "Вручную зайти по SSH на хост и починить одну машину сейчас; процесс менять не нужно", summary: "Разовая починка.", justification: "Починка симптома на одном хосте оставляет механизм нетронутым: ничто не мешает тому же контролю уехать снова завтра, на этом хосте или другом, и ничто не говорит, почему он переключился. Ручная разовая правка — тот же паттерн неотслеживаемого изменения, что и вызвал дрейф. Фиксом должен быть цикл, а не одна машина." },
    { name: "Авто-исправлять каждый уехавший контроль на каждом хосте немедленно, включая конфиги уровня приложения, без алертов", summary: "Слепо чинить всё.", justification: "Сплошное авто-исправление небезопасно для контролей, где «правильное» значение может сломать реальную нагрузку — модуль ядра, нужный агенту мониторинга, шифр, требуемый старым клиентом. Такие отклонения должны алертить человека для согласования, возможно с записью обоснованного исключения. Авто-исправлять только однозначные контроли; для остальных — только алерт. Тихое сплошное обеспечение порождает собственные сбои." },
  ]}
/>

<Quiz
  id="03-cis-baselines-quiz-1"
  lessonSlug="03-cis-benchmarks-and-baselines"
  lang="ru"
  question="В чём разница между CIS Benchmark и вашей конфигурационной базой?"
  choices={[
    { label: "Benchmark — консенсусное меню рекомендаций; ваша база — конкретный набор, под которым вы подписались, минус задокументированные исключения, нужные вашей среде", correct: true },
    { label: "Это одно и то же — «база» просто маркетинговое слово CIS для «benchmark»", misconception: "Они различны. Benchmark — общеплатформенное консенсусное руководство; база — ваша подогнанная приверженность его подмножеству с записанными исключениями. Их смешение — причина, по которой команды либо выкатывают сломанные хосты, либо молча отключают проверки." },
    { label: "База строже benchmark — она добавляет дополнительные контроли, до которых CIS не додумался", misconception: "База обычно — benchmark минус обоснованные исключения, а не надмножество. Цель не пере-укрепить CIS; цель — приверженность известному, аннотированному исключениями эталону, против которого можно мерить дрейф." },
    { label: "Benchmark применяется к работающим хостам; база — только к золотому образу", misconception: "Оба описывают желаемую конфигурацию. База — это именно то, против чего вы сверяете *работающие* хосты для обнаружения дрейфа; она не только для образа." },
  ]}
/>

<Quiz
  id="03-cis-baselines-quiz-2"
  lessonSlug="03-cis-benchmarks-and-baselines"
  lang="ru"
  question="Хост набирает 96% на CIS-скане. Почему зрелый инженер всё равно может счесть это серьёзной проблемой?"
  choices={[
    { label: "Потому что балл — это среднее, способное спрятать критичные контроли: недостающие 4% могут быть входом root по SSH и аудит-логированием — настройками, которых атакующий хочет больше всего", correct: true },
    { label: "Потому что всё ниже 100% означает, что хост полностью скомпрометирован", misconception: "Балл ниже 100% не означает компрометацию. Суть в том, что процент — грубое среднее; серьёзность зависит от того, *какие* контроли провалены, а не от голого числа." },
    { label: "Потому что CIS требует 100% для выдачи сертификата", misconception: "Дело не в механике сертификации. Дело в том, что среднее поощряет закрытие множества мелочей вместо одной критичной, поэтому число может выглядеть здоровым, пока высокоущербный контроль выключен." },
    { label: "Потому что балл 96% доказывает, что сканер неверно настроен", misconception: "Балл 96% — лишь измерение, а не свидетельство сломанного сканера. Зрелая тревога в том, что метрика прячет, какие именно контроли проваливаются." },
  ]}
/>

<DragOrder
  id="03-cis-baselines-drag-1"
  lessonSlug="03-cis-benchmarks-and-baselines"
  lang="ru"
  prompt="Упорядочь жизненный цикл укрепления-и-дрейфа от старта к устойчивому состоянию:"
  items={[
    { id: "a", label: "Выбрать CIS Benchmark + уровень для платформы" },
    { id: "b", label: "Вывести базу: по каждому контролю применить или исключить-с-причиной" },
    { id: "c", label: "Закодировать базу как версионируемый compliance-as-code" },
    { id: "d", label: "Непрерывно аудировать каждый работающий хост против базы" },
    { id: "e", label: "Исправить (или алерт + запись) на каждом обнаруженном отклонении" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Стартуйте укрепление с CIS Benchmark, а не с нуля — но benchmark это не база. Выведите базу (выбранный уровень минус записанные исключения), закодируйте её как compliance-as-code и непрерывно аудируйте работающие хосты против неё, чтобы дрейф становился находкой на следующий день, а не строкой пост-мортема.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-cis-baselines-retrieval"
  lang="ru"
  questions={[
    { q: "Объясните разницу между CIS Benchmark, конфигурационной базой и конфигурационным дрейфом — и почему все три нужны, чтобы реально оставаться укреплённым.", a: "CIS Benchmark — разработанное по консенсусу, предписывающее руководство по укреплению конкретной платформы (дистрибутив, Windows Server, Kubernetes, PostgreSQL, аккаунт AWS), дающее точные настройки — файл, строку, ожидаемое значение, — которые реализуют принципы, что общее руководство вроде NIST SP 800-123 лишь формулирует. Оно делит рекомендации на Level 1 (практичное укрепление парка) и Level 2 (эшелонированная защита, способная жертвовать функцией) и помечает каждую как Automated (машинно-проверяемая) или Manual (требует человеческого суждения). Ваша конфигурационная база — не сам benchmark: это конкретный набор рекомендаций, под которым вы подписались, выбранного уровня, минус исключения, законно нужные вашей среде, причём каждое записано с причиной и владельцем. База — это эталон. Конфигурационный дрейф — зазор, открывающийся со временем между этой базой и реальным работающим состоянием хоста, вызванный обычной эксплуатацией (оставленная отладочная правка, обновление пакета, сбросившее конфиг, hotfix, применённый к одной машине, а не к сорока). Все три нужны потому, что: benchmark избавляет от изобретения «укреплённого» самостоятельно; база превращает его в конкретный, аннотированный исключениями эталон, против которого можно мерить (нет эталона — нет обнаружения дрейфа); а отношение к дрейфу как к состоянию по умолчанию — это то, что вынуждает цикл непрерывного аудита, удерживающий парк на базе, а не оставляющий правильным лишь золотой образ." },
    { q: "Что такое «compliance as code» и как решать между алертом и авто-исправлением, когда скан обнаружил дрейф?", a: "Compliance as code означает выразить базу как машиночитаемые, версионируемые правила — каждый контроль это правило с аудитом (прочитать текущее значение) и часто исправлением (выставить правильное), — запускаемые автоматически против каждого хоста по расписанию и в деплой-конвейере, через инструменты вроде CIS-CAT, OpenSCAP/SCAP, Chef InSpec, Ansible или OPA. Это даёт четыре вещи: база становится ревьюируемой (изменение контроля — pull request, а не SSH в 2 ночи), версионируемой (можно доказать, что было «правильным» на любую дату — что нужно аудитам и инцидентам), единообразно обеспеченной (одни правила на хосте 1 и хосте 41, так что дрейф между хостами ловится) и быстро обнаруживаемой (плановый скан превращает полугодовую тихую регрессию в утреннюю находку). При обнаруженном отклонении зрелое решение — только алерт против авто-исправления. Авто-исправляйте контроли с однозначно правильным значением и без законной причины рабочей нагрузки отличаться (например, PermitRootLogin no) — сводите хост немедленно. Только алерт для контролей, где принудительная установка «правильного» значения может сломать реальную нагрузку (модуль ядра, нужный агенту мониторинга, шифр, требуемый старым клиентом) — поднимайте для согласования человеком, возможно с записью обоснованного исключения. И никогда не решайте скан редактированием базы под уехавшее небезопасное значение: это отмывает регрессию в «соответствие». Записанное отклонение должно также вести к выяснению корневой причины, почему он уехал." },
  ]}
/>

<Recap lang="ru">
CIS Benchmarks — консенсусные, предписывающие руководства по укреплению, их больше сотни, по одному на платформу, дающие точные настройки для старта, вместо того чтобы изобретать «укреплённое» самостоятельно, тогда как NIST SP 800-123 даёт лишь принципы. С каждого benchmark считывайте две вещи: Level 1 против Level 2 (дефолт парка против эшелонированной защиты для высокой безопасности) и Automated против Manual (что можно обеспечивать непрерывно против того, что требует процесса). Но benchmark — не база: ваша конфигурационная база — это конкретный набор, под которым вы подписались, минус исключения, записанные с причиной и владельцем, и этот аннотированный исключениями эталон — единственное, против чего можно мерить дрейф; нет эталона — нет обнаружения дрейфа. Конфигурационный дрейф — тихий, обыденный зазор, открывающийся между этой базой и работающим состоянием хоста, невидимый, пока его не найдёт атакующий или аудитор. Фикс — это цикл, а не разовый проход укрепления: закодируйте базу как compliance-as-code (ревьюируемую, версионируемую, единообразную, быструю), непрерывно аудируйте каждый работающий хост и на каждом отклонении либо авто-исправляйте однозначные контроли, либо алертите человека для тех, что могут сломать нагрузку. Теперь, видя, как выкатывают укреплённый образ, ваш следующий вопрос: что перепроверит работающий хост завтра — или мы просто укрепили образ и сочли дело сделанным?
</Recap>
