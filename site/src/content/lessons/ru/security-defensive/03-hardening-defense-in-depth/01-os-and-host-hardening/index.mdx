---
concepts:
- host-hardening
- attack-surface-reduction
- secure-defaults
- service-minimization
- baseline
deepensInto: []
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-os-and-host-hardening
sources:
- https://www.cisecurity.org/cis-benchmarks
- https://csrc.nist.gov/pubs/sp/800/123/final
spiral: []
status: ready
summary: 'Каждый запущенный сервис, открытый порт и поставляемое умолчание — это поверхность атаки. Харденинг ужимает хост до его реальной задачи, меняет разрешающие умолчания на безопасные и закрепляет измеримую базовую линию, делая дрейф видимым.'
title: 'Харденинг ОС и хоста'
track: security-defensive
unit: 03-hardening-defense-in-depth
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Свежий облачный образ Ubuntu загружается, и ещё до того, как ты в первый раз зайдёшь по SSH, `ss -tlnp` уже показывает горстку слушателей, которых ты не просил: рекурсивный резолвер, привязанный к публичному интерфейсу, спулер печати из десктопного мета-пакета, демон SNMP с до сих пор выставленным community-строкой `public`. Каждый — это сервис, которым ты не пользуешься, который не аудировал и не пропатчишь вовремя. И каждый — это дверь. Тебя ещё никто не атаковал. Образ просто приехал с открытыми дверями, и «умолчание» — самое опасное слово в инфраструктуре.
</Hook>

К концу этого урока ты будешь знать, как ужать хост ровно до той задачи, которую он выполняет, заменить разрешающие умолчания на безопасные и закрепить базовую линию так, чтобы дрейф конфигурации стал тем, что ты *видишь*, а не тем, что обнаруживаешь посреди инцидента.

<Crux>Любой запущенный сервис — поверхность атаки; сократи хост до задачи, ставь безопасные умолчания и закрепи базовую линию против дрейфа.</Crux>

<Explanation>

## Поверхность атаки — это сумма того, что достижимо

Прежде чем харденить хост, нужна точная модель того, до чего реально может дотянуться атакующий. **Поверхность атаки** хоста — это объединение всех интерфейсов, где недоверенный ввод встречается с твоим кодом: слушающие сетевые порты, запущенные сервисы и их RPC-эндпоинты, локальные setuid-бинарники, модули ядра, открытые файловые системы, плановые задачи и учётные данные, под которыми всё это работает. Каждый пункт — потенциальный вход или потенциальная эскалация. Рычаг защитника прост до жестокости и эффективен до жестокости: *тебя нельзя проэксплуатировать через сервис, который не запущен.* У удалённого кода нет CVE.

Именно поэтому **сокращение поверхности атаки** — самый высокорычажный шаг в безопасности хоста и самый дешёвый. Патч чинит один известный баг в сервисе; удаление сервиса чинит все баги, которые в нём когда-либо будут, включая ещё не обнаруженные. Когда прилетели классы багов вроде Shellshock (2014) и PrintNightmare (2021), отмахнулись не те хосты, что патчились быстрее всех, — а те, что вообще никогда не ставили уязвимый компонент. Сокращение бьёт реакцию.

## Минимизация сервисов: вычитай, прежде чем добавлять

Минимизация сервисов — это дисциплина запускать минимальный набор процессов, нужных хосту для его одной задачи, и ничего больше. Машине, которая обслуживает HTTP, не нужны почтовый агент, десктопный DNS-резолвер, Avahi/mDNS, CUPS или X-сервер. Операционный тест для каждого слушателя — одно предложение: *назови конкретную функцию, которую он обслуживает, и команду-владельца.* Не можешь — он не должен работать.

На практике ты проходишь по поверхности и срезаешь:

- **Аудит слушателей** — `ss -tulpn` (или `ss -tlnp` для TCP) сопоставляет каждый открытый порт с процессом и PID, который его держит. Всё, что не можешь назвать, останавливается.
- **Сначала отключи, потом удали** — `systemctl disable --now <svc>` не даёт ему пережить перезагрузку; `apt purge` / `dnf remove` стирает бинарник, чтобы его нельзя было снова включить или проэксплуатировать.
- **Привязывай к самому узкому интерфейсу** — сервис, который говорит только с localhost, должен слушать `127.0.0.1`, а не `0.0.0.0`. База, привязанная к `0.0.0.0` с дефолтным паролем, — так и попадают в записку с требованием выкупа.
- **Файрвол по умолчанию запрещает** — отбрасывай весь входящий, затем разрешай только названные порты. Файрвол — твоя страховка для сервиса, который ты забыл отключить.

<FlowDiagram
  label="Конвейер харденинга хоста: стартуем от широкой поверхности атаки по умолчанию, перечисляем каждый слушающий сервис, минимизируем до того, что требует задача хоста, применяем безопасные умолчания к остатку, фиксируем результат как измеренную базовую линию и непрерывно ловим дрейф от неё; именно цикл удерживает хост укреплённым во времени, а не только в первый день."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "surface", label: "Поверхность", sub: "порты + сервисы" },
    { id: "enumerate", label: "Перечисли", sub: "ss -tulpn" },
    { id: "minimize", label: "Минимизируй", sub: "disable + purge" },
    { id: "defaults", label: "Безоп. умолчания", sub: "deny-by-default" },
    { id: "baseline", label: "Базовая линия", sub: "CIS / измерено" },
    { id: "drift", label: "Лови дрейф", sub: "пере-скан, алерт" },
  ]}
  edges={[
    { from: "surface", to: "enumerate" },
    { from: "enumerate", to: "minimize", label: "назови или убей" },
    { from: "minimize", to: "defaults" },
    { from: "defaults", to: "baseline", label: "закрепи" },
    { from: "baseline", to: "drift", label: "следи" },
  ]}
  caption="Харденинг — это цикл, а не разовый чек-лист: минимизируй поверхность, запри выживших безопасными умолчаниями, заморозь результат как базовую линию и непрерывно лови дрейф обратно к «умолчанию»."
/>

## Безопасные умолчания: выжившие должны быть безопасны из коробки

У всех сервисов, переживших минимизацию, умолчания настроены на *быстрый старт*, а не на *продакшен-безопасность* — а это противоположные цели. Поставляемые умолчания оптимизированы под «работает с первого запуска без конфигурации», а это значит широкие привязки, многословные ошибки, разрешающие права и образцовые учётки. Харденинг переворачивает каждое из этих умолчаний в **deny-by-default**: конфигурация стартует с самой ограничительной настройки и открывает только то, что названо явно.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Поверхность</th>
        <th class="border border-border px-3 py-2 text-left">Рискованное умолчание</th>
        <th class="border border-border px-3 py-2 text-left">Укреплённая настройка</th>
        <th class="border border-border px-3 py-2 text-left">Почему важно</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">SSH</td>
        <td class="border border-border px-3 py-2">Пароль + вход под root</td>
        <td class="border border-border px-3 py-2">Только ключи, <code>PermitRootLogin no</code></td>
        <td class="border border-border px-3 py-2">Убивает подбор и credential-stuffing</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Привязка БД</td>
        <td class="border border-border px-3 py-2"><code>0.0.0.0</code>, образцовый пароль</td>
        <td class="border border-border px-3 py-2"><code>127.0.0.1</code>, ротируемый секрет</td>
        <td class="border border-border px-3 py-2">База недостижима из интернета</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Файрвол</td>
        <td class="border border-border px-3 py-2">Разрешать весь входящий</td>
        <td class="border border-border px-3 py-2">Drop по умолчанию, разрешить порты</td>
        <td class="border border-border px-3 py-2">Страховка для забытого сервиса</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Сервисы</td>
        <td class="border border-border px-3 py-2">Работа под root</td>
        <td class="border border-border px-3 py-2">Непривилег. юзер, сброшенные caps</td>
        <td class="border border-border px-3 py-2">Компрометация остаётся локальной</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Ошибки</td>
        <td class="border border-border px-3 py-2">Многословные трейсы, баннеры</td>
        <td class="border border-border px-3 py-2">Общие ошибки, версия скрыта</td>
        <td class="border border-border px-3 py-2">Лишает атакующего бесплатной разведки</td>
      </tr>
    </tbody>
  </table>
</div>

Два самых ценных умолчания для исправления — аутентификация и привилегии. SSH с парольной аутентификацией и включённым входом под root — самая брутфорсимая поверхность в публичном интернете; переход на вход только по ключам с `PermitRootLogin no` убирает весь класс атак на угадывание учёток. А принцип **наименьших привилегий** применим к каждому процессу: веб-сервер, скомпрометированный во время работы под root, — это скомпрометированный *хост*; тот же эксплойт против сервиса, работающего под непривилегированным пользователем со сброшенными Linux-capabilities, — это скомпрометированный *процесс*, разница между инцидентом и катастрофой.

<Inset kind="why" lang="ru">
Почему удалить сервис строго лучше, чем пропатчить его? Патч — это обещание про *известные* баги: он закрывает раскрытые на сегодня CVE, в день, когда ты его применил, при условии что ты применил его вовремя (а медианное время до патча в индустрии измеряется неделями, не часами). У удалённого сервиса нет поверхности атаки вообще: ни zero-day, ни задержки патча, ни агента, который надо держать свежим, ни конфигурации для дрейфа. Патчинг — гонка, которую ты бежишь вечно; удаление гонку завершает. Поэтому «а оно нам вообще нужно?» — первый вопрос харденинга, задаваемый раньше «оно пропатчено?».
</Inset>

## Базовые линии: сделать укреплённое состояние измеримым и долговечным

Хост, который ты захарденил руками, укреплён *один раз*. Через три месяца и сорок деплоев кто-то снова включил сервис, чтобы дебажить аварию, прогон config-management откатил настройку, пересборка образа уронила твои правила файрвола — а ты не в курсе, потому что «укреплён» жил только в памяти одного инженера. Лекарство — **базовая линия**: записанное, машинно-проверяемое определение известно-хорошего безопасного состояния хоста плюс непрерывное сравнение с ним.

Ровно это и дают **CIS Benchmarks** — построенные по консенсусу, предписывающие конфигурационные базовые линии для каждой ОС и платформы, где каждый контроль указывает и проверку аудита, и шаг исправления. NIST SP 800-123 формулирует ту же идею на уровне программы: укрепи до документированного стандарта, а затем *поддерживай* его. Базовая линия превращает харденинг из героического разового усилия в принудительно соблюдаемое свойство:

- **Закрепи как код** — закодируй базовую линию в Ansible/Chef/Puppet или в золотом образе, чтобы каждый хост рождался укреплённым, а не укреплялся постфактум.
- **Аудируй непрерывно** — инструменты вроде OpenSCAP, `Lynis` или скан CIS-CAT оценивают живой хост против бенчмарка и выдают балл плюс список проваленных контролей.
- **Алертуй на дрейф** — когда живой хост расходится с базовой линией, эта дельта — событие безопасности, а не сноска. Дрейф часто оказывается первым видимым признаком либо небрежного изменения, либо активного вторжения.

<TradeoffMatrix
  id="03-os-host-hardening-tradeoff-1"
  lessonSlug="01-os-and-host-hardening"
  lang="ru"
  prompt="Скан находит спулер печати CUPS, слушающий на продакшен-веб-хосте, у которого нет принтеров и который обслуживает только HTTP. Выбери лучший ответ харденинга."
  options={[
    { name: "Отключить и удалить: `systemctl disable --now cups`, затем удалить пакет, так как у хоста нет функции печати", summary: "Удалить его.", correct: true, justification: "Это сокращение поверхности атаки в чистом виде. Сервис не выполняет на этом хосте ни одной названной функции, поэтому правильный ход — удалить его целиком: это закрывает все текущие и будущие CVE в CUPS для этой машины, без агента для патча и без конфигурации для дрейфа. Минимизация сервисов означает: если не можешь назвать функцию и владельца — оно не должно работать." },
    { name: "Оставить работать, но убедиться, что стоит последняя пропатченная версия", summary: "Пропатчить.", justification: "Патчинг неиспользуемого сервиса строго хуже его удаления: ты сохраняешь поверхность атаки, подписываешься на вечную гонку патчей против будущих CVE и не получаешь ничего — хост не печатает. Удаление закрывает баги, которые ещё даже не раскрыты; патчинг — только сегодняшние." },
    { name: "Добавить правило файрвола, блокирующее порт CUPS от внешнего трафика, и двигаться дальше", summary: "Зафайрволить.", justification: "Правило файрвола — полезная страховка, но это пластырь поверх сервиса, которого не должно существовать. Он оставляет сервис достижимым с localhost и любого внутреннего пивота, всё ещё работающим как процесс, который можно проэксплуатировать, и всё ещё дрейфующим. Файрвол deny-by-default дополняет минимизацию, но не заменяет её." },
    { name: "Сменить пароль администратора CUPS на стойкий и логировать доступ к нему", summary: "Укрепить учётку.", justification: "Укрепление учётных данных сервиса, который тебе не нужен, — усилия не туда. Сервис остаётся поверхностью атаки независимо от пароля, а стойкий пароль ничего не даёт против неаутентифицированного RCE в демоне. Правильный ответ — удалить вещь, а не охранять её." },
  ]}
/>

<Quiz
  id="03-os-host-hardening-quiz-1"
  lessonSlug="01-os-and-host-hardening"
  lang="ru"
  question="Почему удаление неиспользуемого сервиса в общем случае более сильный контроль, чем держать его пропатченным?"
  choices={[
    { label: "У удалённого сервиса нулевая поверхность атаки — ни будущих CVE, ни задержки патча, ни агента или конфигурации для поддержки", correct: true },
    { label: "Удаление сервиса ускоряет загрузку хоста, и это главная польза для безопасности", misconception: "Время загрузки — не аргумент безопасности. Суть в том, что удалённый код нельзя проэксплуатировать вообще: это устраняет весь будущий поток уязвимостей, а не только сегодняшние." },
    { label: "Патчи всегда вносят новые баги, поэтому патчить никогда не стоит", misconception: "Патчинг необходим для сервисов, которые ты оставляешь. Утверждение не «никогда не патчить» — а что для сервиса, который тебе не нужен, удаление строго лучше вечной гонки патчей." },
    { label: "Зафайрволить порт эквивалентно удалению сервиса", misconception: "Файрвол прячет порт с одного направления; сервис всё ещё работает, всё ещё достижим через localhost или внутренний пивот и всё ещё накапливает CVE. Удаление устраняет поверхность целиком." },
  ]}
/>

<Quiz
  id="03-os-host-hardening-quiz-2"
  lessonSlug="01-os-and-host-hardening"
  lang="ru"
  question="Ты захарденил хост руками, и он проходит скан CIS с высоким баллом. Через шесть недель — какая наиболее вероятная причина, что он больше не соответствует?"
  choices={[
    { label: "Дрейф конфигурации — деплой, сессия дебага или прогон config-management молча откатил укреплённую настройку", correct: true },
    { label: "CIS Benchmark истёк, и хост автоматически понизили", misconception: "Бенчмарки не истекают, и хосты не понижаются автоматически. Настоящий враг укреплённого хоста во времени — дрейф: изменения, тихо уводящие его от базовой линии." },
    { label: "Харденинг постоянен после применения, поэтому он не мог измениться", misconception: "Это и есть опасное допущение. Харденинг, применённый руками, — состояние на момент времени; без закреплённой базовой линии и детекции дрейфа он распадается с каждым изменением." },
    { label: "Опубликован новый CVE, что автоматически понижает балл CIS", misconception: "Балл CIS оценивает конфигурацию против бенчмарка, а не ленту CVE. Новый CVE — вопрос патчинга; падение соответствия здесь о смене настроек конфигурации." },
  ]}
/>

<DragOrder
  id="03-os-host-hardening-drag-1"
  lessonSlug="01-os-and-host-hardening"
  lang="ru"
  prompt="Упорядочь рабочий процесс харденинга хоста от первого шага к последнему:"
  items={[
    { id: "a", label: "Перечисли слушателей и сервисы (ss -tulpn), чтобы отобразить реальную поверхность" },
    { id: "b", label: "Минимизируй: отключи и удали каждый сервис, который не можешь назвать" },
    { id: "c", label: "Примени безопасные умолчания к выжившим (deny-by-default, наименьшие привилегии)" },
    { id: "d", label: "Закрепи укреплённое состояние как базовую линию по CIS (как код)" },
    { id: "e", label: "Непрерывно сканируй на дрейф и алертуй на расхождения" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Харденинг хоста — это сначала вычитание: перечисли каждого слушателя, удали то, что не можешь назвать, затем переверни выживших в deny-by-default и наименьшие привилегии. У удалённого кода нет CVE. Наконец, закрепи результат как базовую линию по CIS и лови дрейф — потому что «укреплён руками» распадается в момент, когда кто-то деплоит.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-os-host-hardening-retrieval"
  lang="ru"
  questions={[
    { q: "Что такое поверхность атаки хоста и почему её сокращение даёт больший рычаг, чем патчинг?", a: "Поверхность атаки хоста — это объединение всех интерфейсов, где недоверенный ввод может достичь твоего кода: слушающие сетевые порты, запущенные сервисы и их RPC-эндпоинты, setuid-бинарники, загруженные модули ядра, открытые файловые системы, плановые задачи и привилегии, под которыми всё это работает. Каждый пункт — потенциальная точка входа или путь эскалации. Сокращение этой поверхности — минимизация сервисов — даёт больший рычаг, чем патчинг, из-за асимметрии: патч чинит один известный баг в сервисе в день применения (а медианное время до патча — недели), тогда как удаление сервиса устраняет все баги, которые в нём когда-либо будут, включая нераскрытые zero-day, без агента для поддержки и без конфигурации для дрейфа. Хосты, пережившие классы вроде Shellshock и PrintNightmare нетронутыми, были не самыми быстрыми в патчинге — а теми, что никогда не ставили уязвимый компонент. Первый вопрос харденинга — «а оно нам вообще нужно?», задаваемый раньше «оно пропатчено?»." },
    { q: "Что такое базовая линия и почему харденинг без неё распадается во времени?", a: "Базовая линия — это записанное, машинно-проверяемое определение известно-хорошего безопасного состояния хоста, например CIS Benchmark, который даёт построенные по консенсусу, предписывающие настройки по каждому контролю, каждая с проверкой аудита и шагом исправления. Харденинг руками — состояние на момент времени, живущее лишь в памяти одного инженера; за десятки деплоев, сессий дебага, прогонов config-management и пересборок образа настройки молча откатываются, и хост дрейфует обратно к «умолчанию», и никто не замечает до инцидента. Базовая линия чинит это, делая укреплённое состояние долговечным и принудительным: закрепи как код (Ansible/Chef/Puppet или золотой образ), чтобы хосты рождались укреплёнными; аудируй непрерывно инструментами вроде OpenSCAP, Lynis или CIS-CAT, чтобы оценивать живые хосты против бенчмарка; и считай любое расхождение с базовой линией событием безопасности — дрейф часто первый видимый признак либо небрежного изменения, либо активного вторжения. NIST SP 800-123 формулирует тот же цикл: укрепи до документированного стандарта, затем поддерживай его." },
  ]}
/>

<Recap lang="ru">
Харденинг хоста — это дисциплина ужать машину ровно до той задачи, которую она выполняет, и удержать её там. Начни с отображения поверхности атаки — каждый слушающий порт, сервис, setuid-бинарник и привилегии, под которыми всё работает, — потому что тебя нельзя проэксплуатировать через сервис, который не запущен. Сначала минимизируй: перечисли слушателей через `ss -tulpn`, затем отключи и удали всё, чью функцию и владельца не можешь назвать, ведь у удалённого кода нет CVE, а удаление бьёт вечную гонку патчей. Затем сделай выживших безопасными из коробки, перевернув разрешающие умолчания в deny-by-default и наименьшие привилегии: SSH только по ключам без входа под root, базы, привязанные к localhost, файрволы с drop по умолчанию, сервисы под непривилегированным пользователем со сброшенными capabilities. Наконец, сделай это долговечным: закрепи укреплённое состояние как базовую линию по CIS, выраженную кодом, аудируй живые хосты непрерывно через OpenSCAP или CIS-CAT и считай любой дрейф от базовой линии событием безопасности — потому что хост, укреплённый руками, укреплён ровно один раз и распадается со следующим деплоем. И теперь, когда ты загружаешь свежий образ и `ss -tlnp` показывает слушателей, которых ты не просил, твой первый вопрос не «оно пропатчено?» — а «могу ли я назвать, что это делает, и если нет, почему оно работает?».
</Recap>
