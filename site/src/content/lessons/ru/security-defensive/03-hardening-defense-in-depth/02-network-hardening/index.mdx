---
concepts:
- network-hardening
- egress-filtering
- segmentation
- secure-protocols
- default-deny
deepensInto: []
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-network-hardening
sources:
- https://www.cisecurity.org/cis-benchmarks
- https://csrc.nist.gov/pubs/sp/800/123/final
spiral: []
status: ready
summary: 'Default-deny в сети блокирует всё и разрешает только именованные потоки — включая egress, направление, о котором забывают чаще всего. Вместе с сегментацией и убийством протоколов открытым текстом это превращает захваченную машину из плацдарма в тупик.'
title: 'Network hardening'
track: security-defensive
unit: 03-hardening-defense-in-depth
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Атакующий пробивает один воркер ресайза картинок через CVE в зависимости. Машина внутренняя — публичного входа нет, никто не паникует. За девяносто секунд она подключается наружу к `45.9.x.x:443`, тянет вторую стадию пейлоада, сканирует плоскую сеть `10.0.0.0/8`, находит на соседнем хосте Redis без аутентификации и выгружает сессионные токены. Каждый из этих шагов пересёк сетевую границу, которая это разрешила. Входной фаервол, на котором все были помешаны, не сработал ни разу: воркер инициировал всё сам. Вот цена *незакалённой* сети — не взлом одной машины, а свободное перемещение после него.
</Hook>

К концу урока ты будешь знать, почему default-deny обязан покрывать egress, как сегментация сдерживает радиус поражения и почему убийство протоколов открытым текстом не подлежит обсуждению.

<Crux>Default-deny в сети блокирует весь трафик и разрешает только именованные потоки — включая egress — и убивает открытый текст.</Crux>

<Explanation>

## Default-deny — единственная масштабируемая позиция фаервола

Набор правил фаервола — это политика о том, что *может* происходить на проводе. Написать её можно ровно двумя способами. **Default-allow** разрешает всё и перечисляет, что блокировать, — ты выписываешь атаки, о которых подумал, и каждая, о которой не подумал, проходит. **Default-deny** дропает всё и перечисляет, что *разрешить*, — ты выписываешь потоки, которые приложению реально нужны, а всё остальное, известное и неизвестное, запрещено отсутствием правила.

Вся суть в этой асимметрии. Список default-allow — это блок-лист, а блок-лист полон ровно настолько, насколько богато было твоё воображение в день написания. Список default-deny — это allow-лист, и он отказывает *в закрытую*: новый сервис, новый порт, забытый отладочный эндпоинт, свежий CVE, открывший неожиданный слушатель, — ничто из этого не заработает, пока кто-то осознанно не напишет правило. Это тот же рефлекс `deny-by-default`, что управляет авторизацией в приложении, применённый слоем ниже — на сети. CIS Benchmarks и NIST SP 800-123 делают финальное правило неявного запрета базовой нормой именно поэтому: оно превращает «а мы не забыли это заблокировать?» в «а мы не забыли это разрешить?», и на второй вопрос рецензент действительно может ответить, просто прочитав набор правил.

Причина, по которой команды всё ещё отгружают default-allow, операционная, а не архитектурная: deny-by-default громко ломает всё, пока каждый легитимный поток не перечислен, а это перечисление — настоящая работа. Но эта боль и есть фича — она заставляет тебя знать собственный трафик.

## Egress — половина фаервола, о которой все забывают

Зайди в большинство сетей: входные правила жёсткие, а egress-правила — одна строка: `allow all outbound`. Эта одна строка и сделала сценарий из Hook возможным. Почти каждая современная цепочка атаки зависит от *исходящих* соединений, которые разрешил защитник: подтянуть вторую стадию пейлоада, маячить на command-and-control сервер и выгрузить украденные данные обратно наружу. Фаервол только-на-входе — это дверь с засовом снаружи и снятой внутренней ручкой: он не пускает войти и ничего не делает с тем, кто уже внутри и выходит с серебром.

**Egress-фильтрация** применяет default-deny к исходящему направлению: воркер может инициировать соединения только к тем конкретным адресам, которые нужны для его работы. Платёжному воркеру, который общается с одним банковским API и твоей базой, незачем открывать TLS-соединение к произвольному IP в интернете — поэтому он не может, и в момент попытки она запрещается *и логируется*, что часто и есть твой самый ранний сигнал компрометации. Самое ценное egress-правило на облачном хосте — заблокировать исходящий доступ к metadata-эндпоинту инстанса (`169.254.169.254`) для всего, чему он не нужен: именно этот link-local адрес превращает SSRF или захваченный контейнер в украденные облачные учётки.

<FlowDiagram
  label="Две сетевые позиции после одной и той же компрометации одной машины. Верхний путь (egress allow-all, плоская сеть): захваченный воркер свободно соединяется с C2, тянет пейлоад, сканирует плоскую подсеть и достаёт соседний Redis — полное латеральное движение. Нижний путь (egress default-deny, сегментированная сеть): та же компрометация упирается в запрещённую-и-залогированную egress-попытку и границу сегмента, поэтому застревает на одной машине."
  perRow={4}
  hue="var(--d-crypto)"
  nodes={[
    { id: "pop", label: "1 машина взята", sub: "CVE в зависимости" },
    { id: "egress", label: "Egress-попытка", sub: "наружу к C2" },
    { id: "allow", label: "Egress allow-all", sub: "успех" },
    { id: "spread", label: "Латеральное движение", sub: "плоский /8 + Redis" },
    { id: "deny", label: "Egress default-deny", sub: "запрет + лог" },
    { id: "segment", label: "Граница сегмента", sub: "блокирует east-west" },
    { id: "dead", label: "Тупик", sub: "радиус = 1" },
  ]}
  edges={[
    { from: "pop", to: "egress" },
    { from: "egress", to: "allow", label: "нет egress-правила" },
    { from: "allow", to: "spread", label: "свободное движение" },
    { from: "egress", to: "deny", label: "нет разрешения" },
    { from: "deny", to: "segment" },
    { from: "segment", to: "dead", label: "сдержано" },
  ]}
  caption="Одна компрометация, два исхода. Egress allow-all плюс плоская сеть превращают одну машину в плацдарм; egress default-deny плюс сегментация превращают её в тупик. Взлом идентичен — радиус поражения решает закалка сети."
/>

## Сегментация решает радиус поражения

**Плоская сеть** — один большой `10.0.0.0/8`, где каждый хост достаёт любой другой по любому порту, — означает, что радиус поражения *любой* компрометации — всё хозяйство целиком. Сегментация разбивает сеть на зоны (подсети, VLAN, security groups, Kubernetes NetworkPolicies, авторизация service-mesh) и навязывает default-deny *между* ними. East-west трафик — сервер-к-серверу, направление, по которому идёт латеральное движение, — теперь обязан пересечь границу, которая по умолчанию говорит «нет».

Выигрыш конкретен: помести слой базы в собственный сегмент, до которого добирается только слой приложения по порту 5432, — и захваченная фронтовая веб-машина не сможет тронуть базу напрямую, хотя обе живут «в сети». Помести каждый микросервис под свою политику — и захваченный ресайзер картинок не сможет сканировать соседей. Классический худший случай — одна машина скомпрометирована, а затем неаутентифицированные внутренние сервисы (Redis, Elasticsearch, админ-панель) достижимы плоско по всей подсети — это ровно то, что убирает сегментация. Ты здесь не предотвращаешь первый взлом; ты делаешь так, чтобы первый взлом был и последней машиной.

<Inset kind="why" lang="ru">
Почему «у нас есть периметровый фаервол» — недостаточно? Потому что периметр — это одна яичная скорлупа: твёрдая снаружи, мягкая везде внутри. В момент, когда одна вещь внутри скомпрометирована — фишнутый ноутбук, уязвимая зависимость, утёкшая учётка, — плоская внутренность даёт нулевое трение латеральному движению. Сегментация плюс внутренний default-deny — это то, что операционализирует *zero trust*: перестать считать «внутри сети» границей доверия и аутентифицировать и авторизовывать каждый хоп так, будто он пришёл из открытого интернета. Периметр всё ещё важен; ему просто больше не позволено быть единственным контролем.
</Inset>

## Убийство протоколов открытым текстом

Каждый протокол открытым текстом в твоей сети — это утечка учёток и вектор подмены, ждущий любого, кто видит провод, — а в общей или облачной сети «видеть провод» дешевле, чем думают команды. Правило закалки прямое: **никакого открытого текста для всего, что несёт учётки, данные или управление.** Замени незашифрованный протокол его аутентифицированным шифрованным преемником, а потом *выключи старый на сервере* — не просто «предпочитай» новый, потому что протокол, который ещё слушает, — это протокол, на который атакующий может тебя даунгрейднуть.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Убить это</th>
        <th class="border border-border px-3 py-2 text-left">Порт</th>
        <th class="border border-border px-3 py-2 text-left">Чем опасно</th>
        <th class="border border-border px-3 py-2 text-left">Вместо этого</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Telnet</td>
        <td class="border border-border px-3 py-2">23</td>
        <td class="border border-border px-3 py-2">Логин и каждое нажатие клавиши открытым текстом</td>
        <td class="border border-border px-3 py-2">SSH (22), аутентификация по ключам</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">FTP</td>
        <td class="border border-border px-3 py-2">21</td>
        <td class="border border-border px-3 py-2">Учётки и файлы открытым текстом</td>
        <td class="border border-border px-3 py-2">SFTP / FTPS</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">HTTP (внутренний)</td>
        <td class="border border-border px-3 py-2">80</td>
        <td class="border border-border px-3 py-2">Токены, куки, данные читаемы и подменяемы</td>
        <td class="border border-border px-3 py-2">HTTPS / mTLS, HSTS</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">SNMP v1/v2c</td>
        <td class="border border-border px-3 py-2">161</td>
        <td class="border border-border px-3 py-2">Community string — это пароль открытым текстом</td>
        <td class="border border-border px-3 py-2">SNMPv3 (auth + privacy)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">LDAP</td>
        <td class="border border-border px-3 py-2">389</td>
        <td class="border border-border px-3 py-2">Bind-учётки открытым текстом</td>
        <td class="border border-border px-3 py-2">LDAPS / StartTLS</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">TLS 1.0 / 1.1, SSLv3</td>
        <td class="border border-border px-3 py-2">—</td>
        <td class="border border-border px-3 py-2">Заведомо сломанные шифры, даунгрейдятся</td>
        <td class="border border-border px-3 py-2">TLS 1.2+ (лучше 1.3)</td>
      </tr>
    </tbody>
  </table>
</div>

«Внутренний HTTP — нормально, он никогда не покидает нашу сеть» — это опасная полуправда: как только ты признаёшь, что атакующий уже может быть *внутри* сети (вся посылка сегментации), внутренний открытый текст ровно так же открыт, как внешний. Шифруй и east-west трафик тоже — взаимный TLS между сервисами и есть ответ service-mesh, — чтобы плацдарм на одной машине не вручал атакующему отвод на трафик всех остальных.

<TradeoffMatrix
  id="02-network-hardening-tradeoff-1"
  lessonSlug="02-network-hardening"
  lang="ru"
  prompt="Платёжный сервис работает в плоском облачном VPC с egress allow-all. Ты можешь отгрузить одно изменение за спринт, чтобы максимально сократить радиус поражения при компрометации этого сервиса. Выбери ход с наибольшим рычагом."
  options={[
    { name: "Добавить egress default-deny: разрешить исходящее только к банковскому API и базе, остальное запретить + логировать, и заблокировать metadata-эндпоинт", summary: "Обрезать исходящее атакующего.", correct: true, justification: "Почти каждый пост-компрометационный шаг — подтянуть вторую стадию, маячить на C2, выгрузить данные, украсть облачные учётки через 169.254.169.254 — требует исходящего соединения, которое защитник сейчас разрешает. Ограничение egress до двух адресов, которые сервис реально использует, отказывает во всём этом отсутствием правила и превращает первую запрещённую попытку в алерт. Это прямая атака на фазу свободного перемещения, которая и наносит реальный ущерб." },
    { name: "Поставить WAF перед сервисом, чтобы инспектировать входящие запросы на известные сигнатуры атак", summary: "Закалить только вход.", justification: "WAF стережёт парадную дверь от известных входящих паттернов, но весь ущерб в сценарии — исходящий и east-west после компрометации, которую WAF не остановил (например, CVE в зависимости). Он ничего не делает с маячением атакующего наружу или сканированием соседей — а именно там кровоточит незакалённая сеть." },
    { name: "Чаще ротировать учётки сервиса и удлинить их", summary: "Гигиена секретов.", justification: "Хорошая гигиена, но ортогональна радиусу поражения. Как только машина взята, атакующий использует живые учётки и живую сеть изнутри; короткоживущие секреты помогают незначительно, но не останавливают исходящий C2, латеральное сканирование или кражу metadata. Это не сетевой ход с наибольшим рычагом." },
    { name: "Включить подробное логирование потоков на VPC, чтобы тщательно расследовать после инцидента", summary: "Наблюдать, не предотвращать.", justification: "Логирование необходимо для детекта и форензики, но само по себе это пассивный контроль: оно говорит, что атакующий перемещался свободно, — после того как он это сделал. Оно дополняет default-deny (который и генерирует высокосигнальные события «запрещённый egress») — но не заменяет границу, которая реально сдерживает взлом." },
  ]}
/>

<Quiz
  id="02-network-hardening-quiz-1"
  lessonSlug="02-network-hardening"
  lang="ru"
  question="Почему фаервол default-deny (allow-лист) фундаментально безопаснее, чем default-allow (блок-лист)?"
  choices={[
    { label: "Он отказывает в закрытую: любой новый порт, сервис или непредвиденная атака запрещены отсутствием разрешающего правила, а не проходят потому, что блокирующее правило ещё никто не написал", correct: true },
    { label: "Он быстрее, потому что фаервол вычисляет меньше правил", misconception: "Производительность тут ни при чём и надёжно не лучше. Разница в безопасности — в том, что allow-лист по умолчанию отказывает неизвестному, тогда как блок-лист останавливает только то, что ты явно перечислил." },
    { label: "Он делает шифрование ненужным, потому что ничто неожиданное не может подключиться", misconception: "Default-deny управляет тем, какие потоки разрешены; он не шифрует разрешённые. Тебе всё равно нужно убить протоколы открытым текстом на потоках, которые ты разрешаешь." },
    { label: "Он навсегда блокирует весь исходящий трафик, поэтому малварь никогда не сможет связаться", misconception: "Default-deny не блокирует всё навсегда — он отказывает по умолчанию, а затем явно разрешает потоки, нужные приложению. Ценность в том, что неперечисленные потоки (включая C2 малвари) запрещены, а не в том, что egress пропал целиком." },
  ]}
/>

<Quiz
  id="02-network-hardening-quiz-2"
  lessonSlug="02-network-hardening"
  lang="ru"
  question="На сервисе всё ещё включён TLS 1.0 «для старого клиента» рядом с TLS 1.3. Почему оставлять старую версию слушающей — реальный риск, а не просто легаси-мусор?"
  choices={[
    { label: "Атакующий может принудить даунгрейд на слабую версию, поэтому протокол, который всё ещё предлагается, всё ещё атакуем — предпочитать 1.3 недостаточно; старый нужно выключить", correct: true },
    { label: "TLS 1.0 жрёт больше CPU, поэтому это скорее проблема производительности, чем безопасности", misconception: "Проблема в безопасности, а не в CPU. До достижимого слабого протокола можно договориться вниз, открыв сломанные шифры независимо от того, какой сильный дефолт." },
    { label: "Всё нормально, пока TLS 1.3 — дефолт: клиент выбирает сильнейшую из поддерживаемых обоими", misconception: "On-path атакующий влияет на согласование; «поддерживают оба» включает слабую, и атаки даунгрейда эксплуатируют ровно это. Закалка — это убрать слабую опцию на сервере, а не просто предпочитать сильную." },
    { label: "TLS 1.0 нельзя логировать, поэтому теряешь аудит-трейл", misconception: "Логируемость тут ни при чём. Дело в том, что включённый слабый протокол остаётся согласуемым и потому эксплуатируемым через даунгрейд." },
  ]}
/>

<DragOrder
  id="02-network-hardening-drag-1"
  lessonSlug="02-network-hardening"
  lang="ru"
  prompt="Упорядочь эти ходы закалки сети от самого широкого сокращения радиуса поражения к самому локальному, как ты бы их вводил поэтапно:"
  items={[
    { id: "a", label: "Default-deny на входе И на egress как базовая норма фаервола (разрешать только именованные потоки)" },
    { id: "b", label: "Сегментировать сеть на зоны с default-deny между ними (сдержать east-west)" },
    { id: "c", label: "Запереть облачный metadata-эндпоинт и per-service egress-адреса" },
    { id: "d", label: "Выключить протоколы открытым текстом и слабый TLS на слушателях каждого сервиса" },
  ]}
  correctOrder={["a", "b", "c", "d"]}
/>

</Explanation>

<KeyTakeaway>Default-deny — единственная позиция фаервола, отказывающая в закрытую, — и она обязана покрывать egress, а не только вход, потому что пост-компрометационное движение исходящее. Сегментируй сеть, чтобы одна захваченная машина не достала остальные, запри metadata-эндпоинт и выключи каждый протокол открытым текстом и слабую версию TLS на сервере. Взлом неизбежен; радиус поражения — твой выбор в дизайне.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-network-hardening-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, почему egress-фильтрация важна не меньше входной, и назови самое ценное egress-правило на облачном хосте.", a: "Входная фильтрация не пускает атакующих подключиться внутрь, но почти каждый пост-компрометационный шаг — это исходящее соединение, которое разрешил защитник: подтянуть вторую стадию пейлоада, маячить на command-and-control сервер и выгрузить украденные данные. Фаервол с жёстким входом и сплошным правилом «allow all outbound» не делает ничего против всего этого — скомпрометированный воркер инициирует соединения изнутри. Egress-фильтрация применяет default-deny к исходящему направлению: воркер может соединяться только с конкретными адресами, нужными для его работы; всё остальное запрещено отсутствием правила, а запрещённая попытка логируется, что часто и есть самый ранний сигнал компрометации. Самое ценное egress-правило на облачном хосте — заблокировать исходящий доступ к metadata-эндпоинту инстанса (169.254.169.254) для всего, чему он легитимно не нужен, потому что именно этот link-local адрес превращает SSRF или захваченный контейнер в украденные облачные учётки." },
    { q: "Как сегментация и убийство протоколов открытым текстом вместе сдерживают компрометацию одной машины и почему «внутренний HTTP — нормально» неверно?", a: "Сегментация разбивает плоскую сеть — где любой хост достаёт любой другой по любому порту, так что у любой компрометации радиус поражения — всё хозяйство — на зоны (подсети, VLAN, security groups, Kubernetes NetworkPolicies, авторизация service-mesh) с default-deny между ними. East-west, сервер-к-серверу трафик, по которому идёт латеральное движение, теперь обязан пересечь границу, говорящую «нет» по умолчанию, поэтому захваченная веб-машина не достанет слой базы, а захваченный ресайзер картинок не просканирует соседей: первый взлом становится последней машиной. Убийство протоколов открытым текстом закрывает дополняющую брешь. «Внутренний HTTP — нормально, он никогда не покидает нашу сеть» неверно именно потому, что сегментация предполагает: атакующий уже может быть внутри сети; как только это так, внутренний открытый текст так же открыт, как внешний — учётки, токены и данные читаемы и подменяемы любым, у кого есть плацдарм на соседней машине. Фикс — шифровать и east-west трафик (взаимный TLS между сервисами) и выключить старые протоколы (Telnet, FTP, SNMPv1/2c, голый LDAP) и слабые версии TLS на сервере, ведь на протокол, который ещё слушает, можно даунгрейднуть." },
  ]}
/>

<Recap lang="ru">
Закалка сети — это дисциплина решать радиус поражения компрометации до того, как она случится. Фундамент — default-deny: фаервол, который дропает всё и разрешает только именованные потоки, нужные приложению, так что неизвестные и будущие атаки запрещены отсутствием правила, а не проскальзывают мимо неполного блок-листа. Критически, default-deny обязан покрывать egress, а не только вход — пост-компрометационное движение (вторые стадии пейлоада, маячение на C2, выгрузка данных, кража облачных учёток с 169.254.169.254) всё исходящее, поэтому сплошное правило «allow all outbound» — та брешь, которую большинство команд оставляют открытой. Сегментация навязывает default-deny между зонами, так что east-west латеральное движение упирается в границу, превращая одну захваченную машину в тупик вместо плацдарма по плоской сети. И каждый протокол открытым текстом — Telnet, FTP, внутренний HTTP, SNMPv1/2c, голый LDAP — плюс слабые версии TLS нужно выключить на сервере, а не просто пометить устаревшими, ведь на протокол, который ещё слушает, можно даунгрейднуть. Теперь, видя сервис с egress allow-all на плоской сети, твой первый вопрос: когда эту машину захватят, сможет ли она достать что-то, кроме двух адресов, которые ей реально нужны?
</Recap>
