---
concepts:
- blameless-postmortem
- root-cause
- corrective-action
- resilience
- feedback-loop
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 4
prereqs: []
slug: 04-postmortems-and-learning
sources:
- https://csrc.nist.gov/pubs/sp/800/61/r2/final
- https://attack.mitre.org/
spiral: []
status: ready
summary: 'Безвинный разбор меняет имя того, кто ошибся, на имя системы, которая ему
  это позволила. Его смысл — выпустить корректирующие действия с владельцами и датами,
  а не вынести вердикт. Если он породил вину — он не породил ничего.'
title: Постмортемы и извлечение уроков
track: security-defensive
unit: 02-incident-response
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Через три недели после взлома ты открываешь документ инцидента и под строкой «корневая причина» находишь одну фразу: *«Дежурный инженер выкатил изменение конфига, которое отключило MFA на админском SSO-приложении»*. Указанный фикс — *«Напомнили команде быть аккуратнее с конфигом SSO»*. Дело закрыто. Через восемь месяцев всё повторяется — другой инженер, та же консоль, тот же флаг, тот же диалог без шага подтверждения и без ревью-гейта. Первый постмортем нашёл виновного и назвал его причиной. Он назвал человека и оставил нетронутой машину, которая позволила одним кликом снять MFA со всех админов. Тот второй инцидент был написан в момент, когда первый разбор остановился на имени.
</Hook>

К концу этого урока ты поймёшь, почему безвинный постмортем порождает устойчивые корректирующие действия, а постмортем, движимый поиском виноватого, гарантирует повтор, — и что отличает «корневую причину» от человека, который просто оказался за клавиатурой.

<Crux>Безвинный разбор меняет «кто ошибся» на «что в системе ему это позволило» и выпускает владеемые корректирующие действия — не вердикт.</Crux>

<Explanation>

## Почему безвинность — инженерный выбор, а не доброта

Называть постмортем «безвинным» звучит как HR-любезность. Это не так. Это предпосылка для получения точных данных, а точные данные — единственный вход, из которого можно вывести настоящий фикс.

Механизм прост и беспощаден: люди, которые боятся наказания, скрывают. NIST SP 800-61, федеральное руководство по обработке инцидентов, построено вокруг финальной фазы — *постинцидентной активности, «извлечённых уроков»*, — вся ценность которой зависит от того, расскажут ли участники, что реально произошло, в каком порядке и во что они верили на каждом шаге. В тот миг, когда разбор может стоить кому-то квартала, поток правды иссякает. Дежурный инженер перестаёт добровольно говорить «я не был уверен, что раннбук актуален, поэтому импровизировал» — и ты теряешь единственный самый несущий факт во всей хронологии. Нельзя починить систему, которую не видишь, а страх делает систему невидимой.

Так что безвинность — это не «мы прощаем инженера». Это «мы исходим из того, что любой компетентный человек, помещённый в точно тот же контекст — те же инструменты, те же алерты, то же давление времени, те же устаревшие доки, — мог бы сделать то же самое». Этот переворот рамки и есть весь фокус. Он превращает вопрос из *кто плохо делает свою работу* (неотвечаемый, непочиняемый и лживый — обычно это твой лучший инженер, потому он и был дежурным) в *что в среде сделало неверное действие лёгким, а верное — трудным* (отвечаемый и починяемый в коде, конфиге и процессе).

Зрелый признак — это глагол. «Инженер **забыл** ограничить права» останавливается на человеке. «Деплой-инструмент **по умолчанию** давал широкие права и **не предупреждал**, когда изменение расширяло область MFA» обвиняет систему, которую можно изменить завтра. Тот же инцидент — два разных будущих.

<Inset kind="why" lang="ru">
Почему вина так приятна и так плохо работает? Ретроспективное искажение (hindsight bias). Когда исход уже известен, пропущенный сигнал кажется очевидным — «как они его не увидели?» — поэтому человеческая ошибка читается как причина. Но в момент решения этот сигнал сидел в стене из пятидесяти других алертов, дашборд был зелёным, а раннбук — на восемнадцать месяцев устаревшим. Вина — это мозг, сопоставляющий сложный сбой системы с одним близким человеком, потому что это когнитивно дешевле, чем размотать настоящую цепочку. Это ощущается как объяснение. Оно ничего не объясняет и ничего не чинит.
</Inset>

## Корневая причина — это цепочка, а не имя

Фраза «корневая причина» сбивает джунов с толку, заставляя искать *ту самую одну вещь*. Реальные инциденты — это цепочка скрытых условий, которые совпали: то, что инженерия безопасности называет моделью швейцарского сыра — несколько дыр в нескольких слоях случайно выстроились в линию, и опасность прошла сквозь все сразу. Остановись на первой найденной дыре — и ты залатаешь один ломтик, пока остальные останутся открытыми.

Дисциплина в том, чтобы продолжать спрашивать «а что позволило *это*?», пока не дойдёшь до условий, которые реально можешь изменить, — и ветвиться, потому что одной линии почти никогда нет. Возьмём инцидент с MFA:

- *MFA отключилось на админском приложении.* → Почему? Изменение конфига перещёлкнуло флаг принуждения.
- *Почему одно изменение его перещёлкнуло?* Консоль позволяла одному оператору переключить принуждение MFA без подтверждения и без второго согласующего.
- *Почему часами никто это не поймал?* Не было алерта на изменения политики аутентификации — SIEM это залогировал, но ни одно правило не сработало.
- *Почему оператор вообще правил конфиг SSO руками?* Документированный путь был сломан, поэтому ручные правки в консоли были фактическим раннбуком.

Заметь: ни одна из настоящих причин — не «инженер». Это отсутствующий гейт подтверждения, отсутствующий контроль «четырёх глаз», отсутствующее правило детектирования и сломанный автоматический путь. Каждое — это корректирующее действие с владельцем. Человек — *последняя дыра в сыре*, а не сыр. Заодно сопоставь это с MITRE ATT&CK: разметка техник инцидента (здесь — *Modify Authentication Process*, T1556) показывает, на какие детекции ты был слеп, — и отправляет пробел прямо в твой бэклог детектирования, вместо того чтобы дать ему испариться.

<FlowDiagram
  label="От симптома к системным причинам: MFA отключилось на админском SSO-приложении; вопрос «а что это позволило?» ветвится на отсутствующий гейт подтверждения, отсутствующее согласование четырёх глаз, отсутствующее правило детектирования на изменения политики аутентификации и сломанный автоматический путь, вынудивший ручные правки в консоли, — четыре владеемых корректирующих действия, и ни одно из них не человек для обвинения."
  perRow={3}
  hue="var(--d-crypto)"
  nodes={[
    { id: "symptom", label: "MFA отключено", sub: "админ SSO" },
    { id: "toggle", label: "Клик-переключатель", sub: "без подтверждения" },
    { id: "noapprove", label: "Нет четырёх глаз", sub: "один оператор" },
    { id: "nodetect", label: "Нет алерта", sub: "смена auth-политики" },
    { id: "brokenpath", label: "Сломана автоматизация", sub: "руки — де-факто" },
    { id: "actions", label: "Корректирующие действия", sub: "владелец + дата" },
  ]}
  edges={[
    { from: "symptom", to: "toggle", label: "почему?" },
    { from: "symptom", to: "noapprove", label: "почему?" },
    { from: "symptom", to: "nodetect", label: "почему?" },
    { from: "symptom", to: "brokenpath", label: "почему?" },
    { from: "toggle", to: "actions" },
    { from: "noapprove", to: "actions" },
    { from: "nodetect", to: "actions" },
    { from: "brokenpath", to: "actions" },
  ]}
  caption="Корневая причина — ветвящаяся цепочка скрытых условий, а не одно имя. Каждая ветвь оканчивается системным фиксом с владельцем — человек, который кликнул, это последняя дыра в швейцарском сыре, а не причина."
/>

## Корректирующие действия — единственный выход, который считается

Постмортем, который кончается на «мы обсудили и все многому научились», не породил ничего. Поставляемый результат — это список корректирующих действий, а корректирующее действие, переживающее столкновение с реальностью, обладает четырьмя свойствами, иначе это театр:

1. **Конкретное** — «упрочить SSO» это желание. «Требовать второго согласующего и шаг подтверждения с ручным вводом для любого изменения политики принуждения MFA» это действие.
2. **Владеемое** — поимённый человек, а не «команда». Невладеемые действия — это те, что всё ещё открыты на следующем инциденте.
3. **Датированное** — реальный срок, отслеживаемый в той же системе, что и продуктовая работа (Jira, Linear), а не зарытый в документе, который никто не переоткроет.
4. **Проверяемое** — ты можешь доказать, что оно выпущено. «Добавили алерт» → теперь есть правило, срабатывающее на изменения auth-политики, и ты протестировал его синтетическим изменением.

Иерархия тоже важна. Действия, которые *устраняют сбой*, бьют действия, которые *ловят его быстрее*, а те бьют действия, которые *напоминают людям быть аккуратнее*. «Напомнили команде» — слабейший возможный контроль, потому что опирается на память под давлением — ровно то, что уже отказало. Предпочитай **устранить** (сделать опасный переключатель невозможным без согласования), а не **детектировать** (алерт, когда это случилось), а не **обучать** (сказать людям не делать так). Обучение — это пол, никогда не фикс.

<TradeoffMatrix
  id="04-postmortems-tradeoff-1"
  lessonSlug="04-postmortems-and-learning"
  lang="ru"
  prompt="Изменение конфига отключило MFA на твоём админском SSO-приложении и часами оставалось незамеченным. Постмортему нужно корректирующее действие. Какой выбор — зрелый?"
  options={[
    { name: "Добавить обязательный гейт второго согласующего и шаг подтверждения с ручным вводом перед любым изменением политики принуждения MFA, плюс SIEM-правило с алертом на изменения auth-политики", summary: "Устранить + детектировать.", correct: true, justification: "Это бьёт по системе, а не по человеку. Гейт согласующего и шаг подтверждения делают опасное изменение в один клик структурно невозможным для одного оператора (устраняют сбой), а алерт ловит всё, что всё же просочится (эшелонированная защита). Оба конкретны, владеемы, датированы и проверяемы — можно протестировать, что гейт блокирует, а правило срабатывает. Это корректно применённая иерархия устранить → детектировать → обучать." },
    { name: "Поставить дежурного инженера, сделавшего изменение, на план повышения эффективности и напомнить команде быть аккуратнее с конфигом SSO", summary: "Обвинить + напомнить.", justification: "Это не-фикс, движимый виной. План эффективности наказывает твоего лучшего инженера за систему, которая позволила одним кликом снять MFA, что учит всех скрывать следующую ошибку — разрушая данные, на которых держится разбор. «Напомнить команде» — слабейший класс контроля: он опирается на память под давлением, ровно то, что уже отказало. Переключатель по-прежнему в одном клике; следующий инцидент уже написан." },
    { name: "Написать подробный раннбук, объясняющий правильный способ менять конфигурацию SSO, и сослаться на него в вики команды", summary: "Задокументировать.", justification: "Документация — это пол, а не фикс. Идеальный раннбук всё равно опирается на то, что человек его прочтёт, вспомнит и выполнит под давлением, — а инцидент показал, что документированный путь уже был сломан, потому люди и импровизировали в консоли. Документ ничего не меняет в том, может ли один оператор по-прежнему отключить MFA без гейта. Полезно как дополнение; бесполезно как основное корректирующее действие." },
    { name: "Отключить всю админскую SSO-консоль для всех, пока не завершится полный security-ревью платформы", summary: "Выжженная земля.", justification: "Сверхкоррекция, меняющая один простой на другой. Убийство всей консоли блокирует легитимную админскую работу на неопределённый ревью и создаёт давление включить её обратно быстро и неупрочнённой. Сбой был конкретным отсутствующим гейтом на одном конкретном действии, и фикс должен быть таким же узким — соразмерным, выпускаемым и проверяемым, — а не сплошной заморозкой, которую организация обойдёт за день." },
  ]}
/>

## Замыкание цикла обратной связи строит устойчивость

Смысл всего этого — *цикл*. Фаза извлечённых уроков NIST — не церемония закрытия: она питает следующий раунд подготовки. Каждый инцидент — это бесплатный, дорого оплаченный урок о том, где именно твоя система слаба; устойчивость — это свойство организации, превращающей эти уроки в постоянные изменения быстрее, чем появляются новые слабости.

У этого цикла есть зубы только при трёх условиях. Действия **доводятся до сделано** — пункт, всё ещё «открытый» на следующем квартальном обзоре, это корректирующее действие, которое ничего не скорректировало. Модель **перезапускается** — найденные пробелы (отсутствующий алерт, сломанная автоматизация) втекают обратно в правила детектирования, модели угроз и раннбуки, так что система, обрабатывающая инцидент N+1, измеримо лучше той, что обработала N. И знание **распространяется** — постмортем читают за пределами команды, что его написала, потому что тот же сломанный паттерн (привилегированный переключатель без гейта) почти наверняка есть в трёх других консолях, на которые ты ещё не посмотрел.

<DragOrder
  id="04-postmortems-drag-1"
  lessonSlug="04-postmortems-and-learning"
  lang="ru"
  prompt="Упорядочь цикл извлечения уроков после инцидента — от непосредственного итога к устойчивому улучшению:"
  items={[
    { id: "a", label: "Восстановиться и убедиться, что инцидент полностью устранён, прежде чем его разбирать" },
    { id: "b", label: "Провести безвинный разбор: восстановить хронологию от людей, которым безопасно говорить правду" },
    { id: "c", label: "Проследить корневую причину как ветвящуюся цепочку, пока не дойдёшь до изменяемых системных условий" },
    { id: "d", label: "Написать конкретные, владеемые, датированные, проверяемые корректирующие действия" },
    { id: "e", label: "Довести действия до сделано и вернуть пробелы в детекции, модели угроз и раннбуки" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

<Quiz
  id="04-postmortems-quiz-1"
  lessonSlug="04-postmortems-and-learning"
  lang="ru"
  question="Постмортем указывает корневую причину как «инженер забыл ограничить права деплоя», а фикс — «напомнили команде». В чём ключевой провал этого разбора?"
  choices={[
    { label: "Он остановился на человеке, а не на системе: настоящие причины (инструмент, дававший широкие права по умолчанию, отсутствие предупреждения при смене области) не адресованы, поэтому инцидент повторится", correct: true },
    { label: "Он не назначил инженеру достаточно работы по исправлению, чтобы искупить ошибку", misconception: "Назначить человеку больше работы — значит считать человека причиной. Смысл безвинного разбора обратный: инженер — последняя дыра в сыре; починяемые причины — это умолчания инструмента и отсутствующие ограждения." },
    { label: "Сначала следовало оценить серьёзность инцидента через CVSS", misconception: "CVSS оценивает известные уязвимости, а не разборы инцидентов. Оценка серьёзности не объясняет, почему разбор остановился на имени и выдал непроверяемое «напоминание» как единственное действие." },
    { label: "Ничего — «напомнили команде» это валидное корректирующее действие, раз все его признали", misconception: "«Напомнить команде» — слабейший возможный контроль: он опирается на память под давлением, ровно то, что уже отказало. Корректирующее действие должно быть конкретным, владеемым, датированным и проверяемым." },
  ]}
/>

<Quiz
  id="04-postmortems-quiz-2"
  lessonSlug="04-postmortems-and-learning"
  lang="ru"
  question="Почему психологическая безопасность — предпосылка точного анализа корневой причины, а не просто приятный бонус?"
  choices={[
    { label: "Если люди боятся наказания, они скрывают то, что реально произошло, и ты теряешь несущие факты и в итоге чинишь систему, которую не видишь целиком", correct: true },
    { label: "Потому что счастливые инженеры пишут более длинные документы постмортема", misconception: "Длина — не цель, а точность. Безопасность важна тем, что сохраняет правдивую хронологию, а не тем, что даёт больше слов." },
    { label: "Потому что безвинные разборы требуются комплаенс-фреймворками вроде SOC 2", misconception: "Даже там, где фреймворки поощряют извлечение уроков, инженерная причина причинна: страх подавляет честный рассказ, на котором держится анализ. Комплаенс — следствие этого, а не причина." },
    { label: "Это не предпосылка; умелый расследователь восстановит правду независимо от того, насколько людям безопасно", misconception: "Никакой расследователь не восстановит факт, который испуганный участник так и не озвучил. Деталь «я импровизировал, потому что раннбук выглядел устаревшим» всплывает, только когда признаться в этом безопасно." },
  ]}
/>

</Explanation>

<KeyTakeaway>Безвинный постмортем исходит из того, что любой компетентный инженер в том же контексте сделал бы то же самое, и прослеживает корневую причину как ветвящуюся цепочку системных условий — а не имя. Его единственный реальный выход — корректирующие действия: конкретные, владеемые, датированные и проверяемые, ранжированные устранить → детектировать → обучать. Доведи их до сделано и верни пробелы обратно — иначе тот же инцидент уже написан.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-postmortems-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни, почему «безвинность» — инженерное требование для анализа корневой причины, а не просто доброта, и какой переворот рамки она вынуждает.", a: "Безвинность — предпосылка получения точных данных, а точные данные — единственный вход, из которого выводится настоящий фикс. Механизм причинный: люди, боящиеся наказания, скрывают то, что произошло, поэтому в момент, когда разбор может стоить кому-то квартала, честная хронология иссякает — ты теряешь несущие факты вроде «я импровизировал, потому что раннбук выглядел устаревшим», и нельзя починить систему, которую не видишь. Фаза извлечённых уроков NIST SP 800-61 имеет ценность только если участники рассказывают, что реально произошло, по порядку, во что они верили на каждом шаге. Безвинность не значит «мы прощаем инженера»; она значит «мы исходим из того, что любой компетентный человек в точно том же контексте — те же инструменты, алерты, давление времени, устаревшие доки — мог сделать то же самое». Этот переворот превращает вопрос из «кто плохо делает работу» (неотвечаемый, непочиняемый и обычно лживый, ведь это часто твой лучший инженер) в «что сделало неверное действие лёгким, а верное — трудным» (отвечаемый и починяемый в коде, конфиге, процессе). Признак — глагол: «инженер забыл» останавливается на человеке; «инструмент по умолчанию давал широкие права и не предупреждал» обвиняет изменяемую систему." },
    { q: "Что делает корректирующее действие устойчивым и почему «напомнить команде» — слабейшее из них?", a: "Устойчивое корректирующее действие обладает четырьмя свойствами, иначе это театр: конкретное (поимённый контроль, а не «упрочить SSO»), владеемое (поимённый человек, а не «команда»), датированное (реальный срок, отслеживаемый в той же системе, что и продуктовая работа) и проверяемое (можно доказать, что выпущено и протестировано — например, алерт реально срабатывает на синтетическое изменение). Есть и иерархия: действия, устраняющие сбой, бьют действия, детектирующие его быстрее, а те бьют действия, обучающие людей быть аккуратнее. «Напомнить команде» — на дне, потому что опирается на память под давлением — ровно то, что уже отказало во время инцидента, — и оно невладеемо, недатировано и непроверяемо. Корневая причина — ветвящаяся цепочка (модель швейцарского сыра: несколько скрытых дыр совпали), поэтому каждая ветвь должна оканчиваться своим системным фиксом: отсутствующий гейт подтверждения становится обязательным согласованием, отсутствующий алерт — SIEM-правилом, сломанный автоматический путь — починенным пайплайном. Человек, который кликнул, — последняя дыра, а не сыр. Наконец, у цикла есть зубы, только если действия доводятся до сделано, пробелы возвращаются в правила детектирования и модели угроз (разметка техник по MITRE ATT&CK направляет слепые зоны в бэклог детектирования), а знание распространяется, потому что тот же негейтированный переключатель, вероятно, есть и в других консолях." },
  ]}
/>

<Recap lang="ru">
Безвинный постмортем — инженерный инструмент, а не HR-ритуал: он исходит из того, что любой компетентный инженер, помещённый в тот же контекст — те же инструменты, алерты, давление и устаревшие доки, — мог сделать то же самое, потому что это единственное допущение, которое держит людей говорящими тебе правду, а правда — это данные, на которых держится твой фикс. Корневая причина никогда не имя; это ветвящаяся цепочка скрытых условий (модель швейцарского сыра), и ты продолжаешь спрашивать «а что это позволило?», пока каждая ветвь не приземлится на что-то изменяемое — отсутствующий гейт согласования, отсутствующее правило детектирования, сломанный автоматический путь. Единственный выход, который считается, — корректирующие действия: конкретные, владеемые, датированные и проверяемые, ранжированные устранить → детектировать → обучать, где «напомнить команде» — пол, а не фикс. Затем ты замыкаешь цикл: доводишь действия до сделано, возвращаешь пробелы в детекции (размеченные по MITRE ATT&CK) и модели угроз и делишься уроком, потому что устойчивость — свойство организации, превращающей дорогие инциденты в постоянные изменения быстрее, чем появляются новые слабости. В следующий раз, когда прочтёшь постмортем, чья корневая причина — человек, а фикс — напоминание, ты будешь знать, что смотришь на причину следующего инцидента, а не на прошлый.
</Recap>
