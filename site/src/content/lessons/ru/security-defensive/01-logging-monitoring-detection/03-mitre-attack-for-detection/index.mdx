---
concepts:
- mitre-attack
- ttp
- detection-coverage
- technique
- threat-informed-defense
deepensInto: []
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 3
prereqs: []
slug: 03-mitre-attack-for-detection
sources:
- https://attack.mitre.org/
- https://csrc.nist.gov/pubs/sp/800/92/final
spiral: []
status: ready
summary: 'ATT&CK — общий каталог того, как действуют реальные противники. Свяжи
  каждое правило детекта с техникой, и покрытие перестанет быть ощущением и станет
  сеткой — называющей ровно те ходы атакующего, которых ты бы не увидел.'
title: MITRE ATT&CK для детекта
track: security-defensive
unit: 01-logging-monitoring-detection
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Член совета директоров задаёт твоему руководителю безопасности один вопрос: «мы защищены?» Раньше честным ответом было пожатие плечами — 600 правил детекта, дашборд вендора, светящийся зелёным, и никакого способа сказать, что они на самом деле ловят. Потом кто-то наложил правила на сетку известных техник противника, и зелёное стало рваным: плотные столбцы для атак на учётные данные и брутфорса, почти пустые столбцы для шагов, что идут *после* закрепления — разведка, латеральное движение, тихие «живущие на земле» команды, выглядящие ровно как занятый администратор. Шестьсот правил — а реальный взломщик мог пройти три полные стадии атаки, не задев ни одного. Сетка не добавила ни единого правила. Она лишь сделала дыры невозможными для дальнейшего игнорирования.
</Hook>

К концу урока ты будешь знать, как наложить свои детекты на MITRE ATT&CK, чтобы покрытие стало измеримой сеткой, и как читать эту сетку, чтобы найти пробелы, которые атакующий использует первыми.

<Crux>ATT&CK каталогизирует реальные техники атак; привязка детекта к технике превращает «мы защищены?» в сетку, называющую пробелы.</Crux>

<Explanation>

## Что такое ATT&CK на самом деле

Предыдущий урок назвал два идентификатора техник ATT&CK почти мимоходом — `T1110` Brute Force, `T1078` Valid Accounts — как цели для корреляционных правил. Эта небрежность скрывает, сколько структуры стоит за этими ID. **MITRE ATT&CK** (Adversarial Tactics, Techniques, and Common Knowledge) — это курируемая публичная база знаний о том, как разворачиваются реальные вторжения, построенная на наблюдаемых инцидентах, а не на теории. Это самое близкое, что есть у отрасли к общему словарю поведения атакующего: когда одна команда говорит «мы слабы по `T1021` Remote Services», любая другая команда точно знает, какой ход имеется в виду.

В модели три вложенных уровня, и различие между ними — суть всего:

- **Тактика — *зачем*.** Цель противника на шаге: *Initial Access* (первоначальный доступ), *Persistence* (закрепление), *Privilege Escalation* (повышение привилегий), *Lateral Movement* (латеральное движение), *Exfiltration* (эксфильтрация). В Enterprise-матрице около четырнадцати тактик, и они читаются слева направо как грубый порядок атаки — столбцы «цепочки убийства».
- **Техника — *как*.** Конкретный способ достичь тактики: в *Credential Access* это `T1110` Brute Force или `T1003` OS Credential Dumping. Именно техники (и их более тонкие субтехники) — то, против чего ты реально пишешь детекты.
- **Процедура — *как именно*.** Конкретная реализация, которую в реальности применила определённая группа — *этот* однострочник PowerShell, *этот* инструмент. Процедуры — наземная правда, делающая технику реальной.

Этот стек и есть смысл **TTP** — Tactics, Techniques, and Procedures, термин, который ты будешь слышать постоянно. Причина, по которой TTP важны для *детекта*, — устойчивость: атакующий меняет IP или хеш файла за секунды, но *поведение* — дамп LSASS ради кражи учёток, затем их использование для бокового перемещения — дорого менять, потому что оно привязано к тому, как объект реально достигается.

<Inset kind="why" lang="ru">
Зачем строить детекты на техниках, а не на индикаторах? Из-за **Пирамиды боли** — модели того, насколько больно атакующему, когда ты блокируешь тот или иной артефакт. Хеши и IP лежат внизу: их тривиально проротировать, поэтому детект на них мёртв к следующей кампании. TTP на вершине: детект *поведения* вынуждает атакующего фундаментально переосмыслить операцию, а не просто перегенерировать индикатор. Фиды индикаторов не бесполезны — это дешёвые быстрые победы, — но программа детекта, заякоренная лишь на хешах, навсегда на шаг позади. ATT&CK — это согласие отрасли драться на вершине пирамиды.
</Inset>

## Привязка детектов к матрице

Вот ход, превращающий ATT&CK из плаката в инженерный инструмент. Возьми каждое правило детекта и пометь его идентификаторами техник, которые оно покрывает. Теперь твой инвентарь детектов и матрица ATT&CK делят общий ключ, и ты можешь их *соединить* — ровно как нормализация позволила SIEM соединять логи из разных источников. Результат — **сетка покрытия**: матрица тактик × техник, где каждая ячейка окрашена в зависимости от того, можешь ли ты — и насколько хорошо — детектить эту технику.

<FlowDiagram
  label="Как детекты становятся сеткой покрытия: каждое правило помечается идентификаторами техник ATT&CK, которые оно покрывает, эта метка соединяет инвентарь правил с матрицей ATT&CK, проецируя его на сетку покрытия тактик по техникам; непокрытые ячейки — это видимые пробелы детекта, питающие решение о том, какие новые правила строить дальше."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "rules", label: "Правила детекта", sub: "контент SIEM" },
    { id: "tag", label: "Метка техники", sub: "правило → T1110" },
    { id: "matrix", label: "Матрица ATT&CK", sub: "тактики × тех" },
    { id: "grid", label: "Сетка покрытия", sub: "ячейки в цвете" },
    { id: "gaps", label: "Названные пробелы", sub: "пустые столбцы" },
  ]}
  edges={[
    { from: "rules", to: "tag" },
    { from: "tag", to: "matrix", label: "ключ" },
    { from: "matrix", to: "grid", label: "проекция" },
    { from: "grid", to: "gaps", label: "что пусто" },
  ]}
  caption="Привязка — это соединение: идентификатор техники — общий ключ между инвентарём правил и матрицей ATT&CK. Проекция этого соединения на матрицу даёт сетку покрытия, чьи пустые ячейки — твои пробелы детекта, питающие следующие правила. MITRE поставляет ATT&CK Navigator, который рисует ровно эту сетку."
/>

MITRE поставляет бесплатный инструмент, **ATT&CK Navigator**, рисующий эту сетку как цветную тепловую карту — зелёным там, где у тебя слоистый детект, жёлтым там, где одно хрупкое правило, красным там, где нет ничего. Ценность не в красивой картинке, а в том, что «мы защищены?» перестаёт быть ощущением и становится числом, которое можно защитить, приоритизировать и отслеживать во времени.

Одна дисциплина отделяет реальную привязку от спектакля: **метка должна отражать то, что правило действительно детектит, а не то, как оно названо.** Правило с именем «Детектор брутфорса», срабатывающее лишь на 100 неуспешных входов с одного IP, покрывает узкую долю `T1110` — оно полностью пропускает password spraying (`T1110.003`: несколько попыток по многим аккаунтам, что никогда не пробьёт порог на один IP). Завышать покрытие хуже, чем признать пробел, потому что это красит ячейку зелёным там, где атакующий проходит насквозь.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Тактика (зачем)</th>
        <th class="border border-border px-3 py-2 text-left">Пример техники</th>
        <th class="border border-border px-3 py-2 text-left">Детект, покрывающий её</th>
        <th class="border border-border px-3 py-2 text-left">Типичное реальное покрытие</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Credential Access</td>
        <td class="border border-border px-3 py-2"><code>T1110</code> Brute Force</td>
        <td class="border border-border px-3 py-2">Порог неуспешных входов + корреляция с успехом</td>
        <td class="border border-border px-3 py-2">Сильное — строят все</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Credential Access</td>
        <td class="border border-border px-3 py-2"><code>T1110.003</code> Password Spraying</td>
        <td class="border border-border px-3 py-2">Редкие неудачи по <em>многим</em> аккаунтам</td>
        <td class="border border-border px-3 py-2">Часто упускается — правила на один IP молчат</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Discovery</td>
        <td class="border border-border px-3 py-2"><code>T1087</code> Account Discovery</td>
        <td class="border border-border px-3 py-2">Всплески команд перечисления на хосте</td>
        <td class="border border-border px-3 py-2">Слабое — похоже на обычную работу админа</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Lateral Movement</td>
        <td class="border border-border px-3 py-2"><code>T1021</code> Remote Services</td>
        <td class="border border-border px-3 py-2">Необычная аутентификация хост-хост (новые пары)</td>
        <td class="border border-border px-3 py-2">Тонкое — east-west трафик редко логируется</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Exfiltration</td>
        <td class="border border-border px-3 py-2"><code>T1048</code> Эксфил по альт-протоколу</td>
        <td class="border border-border px-3 py-2">Аномалия исходящего объёма / DNS-туннеля</td>
        <td class="border border-border px-3 py-2">Лоскутное — нужна видимость egress</td>
      </tr>
    </tbody>
  </table>
</div>

Прочти столбец «типичное реальное покрытие» сверху вниз — и выскочит закономерность: команды сильны *слева* матрицы — на громких, ранних, хорошо понятых шагах вроде брутфорса — и тонки к *середине и правому краю*, где атакующий уже внутри и активность сливается с легитимными операциями. Этот «тяжёлый слева» профиль — самая частая форма покрытия детекта в индустрии, и именно на неё рассчитывает взломщик.

## Чтение сетки: threat-informed defense

Наивное прочтение сетки покрытия — «покрасить всё в зелёный». Это ловушка: в Enterprise-матрице сотни техник и субтехник, полное покрытие ни достижимо, ни оправданно, а погоня за ним тратит скудный бюджет инженерии детекта на техники, которые никакой нацеленный на *тебя* противник никогда не применит. Зрелый ход — **threat-informed defense** (защита, осведомлённая об угрозах): приоритизируй покрытие по техникам, которые, как известно, применяют группы, реально нацеленные на твой сектор.

ATT&CK поддерживает это напрямую. Он каталогизирует *группы* угроз (например, APT29, FIN7) и сопоставляет каждой техники, которые она наблюдаемо применяла. Так что ты не красишь всю матрицу — ты накладываешь наборы техник двух-трёх групп, релевантных твоей отрасли, и *это* наложение становится твоим списком приоритетов. Финтех взвешивает credential access и TTP финансового мошенничества; больница взвешивает техники-предвестники шифровальщика вроде `T1486` Data Encrypted for Impact. Покрытие становится вопросом *ожидаемой ценности*: детектить ходы, которые делают твои реальные противники, в порядке, в каком они их делают, прежде чем тратить доллар на экзотику.

<TradeoffMatrix
  id="03-mitre-attack-for-detection-tradeoff-1"
  lessonSlug="03-mitre-attack-for-detection"
  lang="ru"
  prompt="Твоя сетка покрытия ATT&CK плотно-зелёная слева (Initial Access, Credential Access) и почти пустая в середине (Discovery, Lateral Movement). У тебя бюджет на один квартал инженерии детекта. Выбери лучшее его применение."
  options={[
    { name: "Построить детекты для пустых столбцов Discovery и Lateral Movement, приоритизируя по техникам, которые применяют группы, нацеленные на твой сектор", summary: "Закрыть пробелы, threat-informed.", correct: true, justification: "Сетка говорит тебе: взломщик, прошедший сильную левую сторону, затем свободно оперирует через середину — ровно те пост-закрепленческие стадии, где копится большая часть dwell time и ущерба. Threat-informed defense гласит: не пытайся покрасить всю матрицу; приоритизируй пустые ячейки, через которые проходят реально нацеленные на твой сектор противники. Это трата с наивысшей ожидаемой ценностью: она закрывает путь, по которому пошёл бы реальный атакующий, в порядке, в каком он бы шёл." },
    { name: "Добавить второе и третье правило брутфорса, чтобы и так зелёный столбец Credential Access стал ещё зеленее", summary: "Углубить сильное.", justification: "Это театр покрытия. Ты тратишь бюджет там, где уже хорошо детектишь, делая зелёную ячейку зеленее, пока взломщик нетронутым идёт через Discovery и Lateral Movement. Наслоение правил на покрытую технику имеет круто убывающую отдачу; предельная ценность — в пустых ячейках, а не в тёмных." },
    { name: "Поставить цель покрасить в этом квартале каждую ячейку матрицы в зелёный ради полного покрытия", summary: "Покрыть всё.", justification: "В Enterprise-матрице сотни техник и субтехник; «всё зелёное» ни достижимо за квартал, ни оправданно. Погоня за тотальным покрытием размазывает конечный бюджет по техникам, которые нацеленный на тебя противник не применит, и квартал кончится раньше, чем ты осмысленно закроешь значимые пробелы. Приоритизируй по реальной угрозе, а не по полноте." },
    { name: "Купить фид угроз с вредоносными IP и хешами для блокировки на файрволе", summary: "Купить индикаторы.", justification: "Индикаторы лежат на дне Пирамиды боли — дёшево и полезно, но тривиально ротируются и ничего не делают для закрытия поведенческого пробела в Discovery или Lateral Movement. Атакующего, уже находящегося внутри, не остановит блок-лист хешей; недостающее — детект его поведения, что и представляют пустые столбцы." },
  ]}
/>

<Quiz
  id="03-mitre-attack-for-detection-quiz-1"
  lessonSlug="03-mitre-attack-for-detection"
  lang="ru"
  question="Почему детекты, построенные на техниках ATT&CK (TTP), устойчивее детектов, построенных на IP-адресах и хешах файлов?"
  choices={[
    { label: "Поведение привязано к тому, как достигается цель, и дорого менять, тогда как IP или хеш ротируется за секунды (Пирамида боли)", correct: true },
    { label: "Потому что идентификаторы техник никогда не меняются, поэтому правило не требует обслуживания", misconception: "ATT&CK эволюционирует, и правила всё равно требуют тюнинга. Устойчивость — про стоимость обхода для атакующего: поведение дорого менять, а не про то, что правило не требует обслуживания." },
    { label: "Потому что хеши и IP зашифрованы и не читаются SIEM", misconception: "Хеши и IP прекрасно читаются; проблема в том, что атакующему их тривиально проротировать, поэтому детект на них мёртв к следующей кампании." },
    { label: "Потому что техники автоматически покрывают всю матрицу, как только пометишь одно правило", misconception: "Пометка одного правила покрывает лишь те техники, которые оно действительно детектит. Покрытие матрицы рождается из многих привязанных правил, а завышение — известный сбой." },
  ]}
/>

<Quiz
  id="03-mitre-attack-for-detection-quiz-2"
  lessonSlug="03-mitre-attack-for-detection"
  lang="ru"
  question="Правило с именем «Детектор брутфорса» срабатывает лишь на 100+ неуспешных входов с одного IP. Как пометить T1110 Brute Force на сетке покрытия?"
  choices={[
    { label: "Частично — оно покрывает классический брутфорс из одного источника, но пропускает password spraying (T1110.003), так что не крась ячейку полностью зелёной", correct: true },
    { label: "Полностью покрыто — имя правила совпадает с техникой, значит T1110 зелёный", misconception: "Пометка по имени, а не по поведению, — ровно тот театр, которого надо избегать. Порог на один IP пропускает редкий спрей по многим аккаунтам; полная зелёнка прячет путь, по которому идёт атакующий." },
    { label: "Совсем не покрыто — детект из одного IP не считается брутфорсом", misconception: "Оно действительно детектит классический случай из одного источника, так что это реальное частичное покрытие. Пометить нулём так же неточно, как пометить полным; честная метка — частично." },
    { label: "Покрыто, и вдобавок автоматически покрывает Credential Access целиком", misconception: "Одно правило покрывает одну долю одной техники. Столбец-тактика содержит много техник (дамп, спрей, принуждение к аутентификации); одно правило не может покрыть столбец." },
  ]}
/>

<DragOrder
  id="03-mitre-attack-for-detection-drag-1"
  lessonSlug="03-mitre-attack-for-detection"
  lang="ru"
  prompt="Упорядочь эти тактики ATT&CK в грубой последовательности вторжения слева направо:"
  items={[
    { id: "a", label: "Initial Access — получить плацдарм" },
    { id: "b", label: "Credential Access — украсть логины и пароли" },
    { id: "c", label: "Discovery — изнутри картировать окружение" },
    { id: "d", label: "Lateral Movement — перейти на другие хосты" },
    { id: "e", label: "Exfiltration — вынести данные наружу" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>ATT&CK — общий каталог реальных техник противника (TTP). Помечай каждый детект идентификаторами техник, которые он правда покрывает, проецируй это соединение на матрицу — и «мы защищены?» становится сеткой, называющей пробелы. Помечай по поведению, а не по имени правила, и приоритизируй пустые ячейки через threat-informed defense — техники, которые реально применяют нацеленные на *тебя* группы, — а не гонись за полностью зелёной матрицей.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-mitre-attack-for-detection-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни иерархию тактика / техника / процедура в ATT&CK и почему детект на уровне TTP (а не IP и хешей) делает детект устойчивым.", a: "ATT&CK структурирует поведение противника в три вложенных уровня. Тактика — цель противника на шаге, «зачем»: Initial Access, Credential Access, Lateral Movement, Exfiltration; в Enterprise около четырнадцати тактик, читаемых слева направо как грубый порядок атаки. Техника — конкретный способ достичь тактики, «как»: например, T1110 Brute Force или T1003 OS Credential Dumping в Credential Access, часто уточняемые до субтехник (T1110.003 Password Spraying). Процедура — точная реализация в реальности, которую применила конкретная группа: конкретный инструмент или команда. Вместе это TTP. Детект на уровне TTP/поведения устойчив из-за Пирамиды боли: IP и хеши файлов лежат внизу, и атакующий ротирует их за секунды, поэтому детект на них мёртв к следующей кампании; поведение на вершине, потому что привязано к тому, как реально достигается цель (нужно сдампить учётки, чтобы их украсть, затем использовать для бокового движения), так что заставить атакующего сменить поведение дорого. Фиды индикаторов всё ещё дешёвые быстрые победы, но программа, заякоренная лишь на хешах, навсегда на кампанию позади." },
    { q: "Как привязка детектов к ATT&CK превращает «мы защищены?» в измеримую сетку и как приоритизировать строительство через threat-informed defense?", a: "Помечаешь каждое правило детекта идентификаторами техник ATT&CK, которые оно действительно покрывает. Идентификатор техники тогда — общий ключ между инвентарём правил и матрицей ATT&CK, так что их можно соединить — тот же ход, что нормализация дала для соединения логов из источников, — и спроецировать результат на сетку покрытия тактик × техник в виде тепловой карты (бесплатный ATT&CK Navigator от MITRE делает это): зелёным, где детект слоистый, жёлтым, где одно хрупкое правило, красным, где нет ничего. Дисциплина, делающая это честным, — пометка по тому, что правило реально детектит, а не по имени: правило брутфорса на один IP покрывает классический T1110, но пропускает password spraying T1110.003, так что оно частичное, а не зелёное; завышение красит ячейку, через которую атакующий проходит насквозь. Частая реальная форма — плотно-зелёная слева (громкие ранние шаги вроде брутфорса) и тонкая в середине и справа (Discovery, Lateral Movement), ровно там, где взломщик уже внутри и сливается. Ты НЕ пытаешься покрасить всю матрицу — в ней сотни техник, и полное покрытие не оправданно. Threat-informed defense приоритизирует по ожидаемой ценности: ATT&CK сопоставляет группам угроз (APT29, FIN7) техники, которые они наблюдаемо применяют, так что ты накладываешь две-три группы, нацеленные на твой сектор, и пусть это наложение будет твоей очередью строительства — закрой путь, которым идут реальные противники, в порядке, в каком они идут, прежде чем тратиться на экзотику." },
  ]}
/>

<Recap lang="ru">
MITRE ATT&CK — общий для отрасли каталог того, как разворачиваются реальные вторжения, структурированный как тактики (зачем — Initial Access, Lateral Movement, Exfiltration), техники (как — T1110 Brute Force, T1003 Credential Dumping) и процедуры (точная реализация в реальности) — вместе TTP. Детект на уровне TTP устойчив из-за Пирамиды боли: атакующий ротирует IP или хеш за секунды, но поведение дорого менять. Инженерный ход — пометить каждое правило детекта идентификаторами техник, которые оно правда покрывает, соединить инвентарь правил с матрицей по этому ключу и спроецировать на сетку покрытия — тепловую карту рисует ATT&CK Navigator от MITRE, — чтобы «мы защищены?» стало числом, которое можно защитить. Помечай по поведению, а не по имени правила: правило брутфорса на один IP — лишь частичное покрытие T1110, потому что пропускает password spraying. Обычная форма — сильно слева и пусто в середине, где и оперирует реальный взломщик. Не гонись за полностью зелёной матрицей; используй threat-informed defense, чтобы приоритизировать пустые ячейки по техникам, которые реально применяют нацеленные на твой сектор группы. В следующий раз, когда тебе покажут зелёный дашборд безопасности, твой первый вопрос — зрелый: какие техники он покрывает — и какие ходы атакующего на нём не появятся вовсе?
</Recap>
