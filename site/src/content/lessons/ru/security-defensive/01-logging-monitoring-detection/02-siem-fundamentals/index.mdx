---
concepts:
- siem
- log-aggregation
- correlation
- normalization
- alerting
deepensInto: []
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-siem-fundamentals
sources:
- https://attack.mitre.org/
- https://csrc.nist.gov/pubs/sp/800/92/final
spiral: []
status: ready
summary: 'SIEM собирает логи с каждого хоста, приводит их к одной схеме и коррелирует
  этот поток в горстку алертов. Сложность не в хранении — а в том, чтобы из миллионов
  событий в час вытащить те три, что что-то значат.'
title: SIEM fundamentals
track: security-defensive
unit: 01-logging-monitoring-detection
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
3 часа ночи. Атакующий входит с украденным паролем, сервис аутентификации отдаёт чистый `200`, а балансировщик, база данных, Linux-машина и audit-лог Kubernetes послушно пишут свои строки — каждый в своём формате, каждый в своём месте. Все записи, нужные чтобы поймать вторжение, уже существуют. Пробой не в том, что никто не залогировал; в том, что доказательство разбросано по сорока хостам в четырёх форматах, и никто не следил за единственной последовательностью — *новая гео-локация, потом неудачная MFA, потом sudo, потом исходящая передача 4 ГБ* — которая, прочитанная вместе, кричит о компрометации. SIEM существует, чтобы прочитать эти сорок логов как одну историю.
</Hook>

К концу урока ты будешь понимать, как SIEM превращает пожарный шланг сырых логов с каждого хоста в нормализованный, скоррелированный поток — и почему по-настоящему сложная часть это алертинг, а не хранение.

<Crux>SIEM собирает логи отовсюду, приводит их к одной схеме и коррелирует поток в считаные алерты, ради которых стоит будить человека.</Crux>

<Explanation>

## Что такое SIEM на самом деле

SIEM расшифровывается как **Security Information and Event Management** — но за аббревиатурой прячется настоящая работа. SIEM — это место, где каждый лог в твоей инфраструктуре собирается, делается сопоставимым и отслеживается на паттерны, которые ни один лог по отдельности не выдаст. NIST SP 800-92, канонический гайд по управлению логами, формулирует ядро проблемы точно: логи порождаются десятками источников в несовместимых форматах и объёмах, которые ни один человек не прочитает, поэтому вся ценность в их *централизации и анализе*, а не в самом акте записи.

Представь это как конвейер из четырёх стадий. Каждая стадия — место, где реальные системы ломаются в продакшене, поэтому стоит пройти их по порядку.

<FlowDiagram
  label="Конвейер SIEM в четырёх стадиях: сбор тянет сырые логи из каждого источника, нормализация парсит каждый формат в одну общую схему, корреляция соединяет нормализованные события между источниками на временном окне в правила, а алертинг отдаёт человеку только высокоточные совпадения, остальное оставляя для поиска и форензики."
  perRow={4}
  hue="var(--d-crypto)"
  nodes={[
    { id: "collect", label: "1. Сбор", sub: "агенты, syslog, API" },
    { id: "normalize", label: "2. Нормализация", sub: "парс → одна схема" },
    { id: "correlate", label: "3. Корреляция", sub: "join на окне" },
    { id: "alert", label: "4. Алерт", sub: "высокоточные → человек" },
    { id: "store", label: "Хранение", sub: "поиск + форензика" },
  ]}
  edges={[
    { from: "collect", to: "normalize", label: "сырые строки" },
    { from: "normalize", to: "correlate", label: "типизир. поля" },
    { from: "correlate", to: "alert", label: "матч правила" },
    { from: "normalize", to: "store", label: "всё" },
  ]}
  caption="Сбор и нормализация справляются с объёмом; корреляция и алертинг — с сигналом. Обрати внимание на вторую стрелку из нормализации: каждое нормализованное событие сохраняется для поиска, даже если ни одно правило не сработало — нельзя расследовать то, что не сохранил."
/>

## Стадии 1–2: сбор и нормализация — негламурная половина

**Сбор** — это сантехника: доставить каждый лог в одно место. Агенты (демон на каждом хосте, читающий файлы), syslog по сети, API облачных провайдеров (CloudTrail, GCP Audit Logs) и прямые интеграции — всё это кормит SIEM. Сбои здесь приземлённые и постоянные: хост, чей агент умер три недели назад, — это слепое пятно, о котором ты не знаешь, а неправильно настроенный фаервол, молча роняющий UDP-syslog, теряет записи без ошибки где-либо. Зрелый рефлекс — **мониторить отсутствие логов**: источник, внезапно замолчавший, сам по себе является алертом, потому что атакующие выключают логирование.

**Нормализация** — вот где живёт настоящая инженерия. Строка Linux `sshd`, JSON-событие AWS CloudTrail и access-лог nginx все описывают «кто-то что-то сделал откуда-то», но пишут это совершенно по-разному. Нормализация парсит каждый формат и отображает его поля в одну **общую схему** — так что `srcip`, `192.168.1.5` и `client_addr` все становятся единым полем вроде `source.ip`. Без этого корреляция невозможна: нельзя соединить неудачный вход по SSH с подозрительным запросом к базе, если «пользователь» — это `acct`, `user_name` и `principalId` в трёх разных логах.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Сырой источник</th>
        <th class="border border-border px-3 py-2 text-left">Родное поле «кто»</th>
        <th class="border border-border px-3 py-2 text-left">Родное поле «откуда»</th>
        <th class="border border-border px-3 py-2 text-left">Нормализованная схема</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Linux sshd</td>
        <td class="border border-border px-3 py-2"><code>user</code></td>
        <td class="border border-border px-3 py-2"><code>rhost</code></td>
        <td class="border border-border px-3 py-2"><code>user.name</code> / <code>source.ip</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">AWS CloudTrail</td>
        <td class="border border-border px-3 py-2"><code>userIdentity.principalId</code></td>
        <td class="border border-border px-3 py-2"><code>sourceIPAddress</code></td>
        <td class="border border-border px-3 py-2"><code>user.name</code> / <code>source.ip</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">nginx access</td>
        <td class="border border-border px-3 py-2">(нет — вывести из auth)</td>
        <td class="border border-border px-3 py-2"><code>$remote_addr</code></td>
        <td class="border border-border px-3 py-2"><code>user.name</code> / <code>source.ip</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">K8s audit</td>
        <td class="border border-border px-3 py-2"><code>user.username</code></td>
        <td class="border border-border px-3 py-2"><code>sourceIPs[]</code></td>
        <td class="border border-border px-3 py-2"><code>user.name</code> / <code>source.ip</code></td>
      </tr>
    </tbody>
  </table>
</div>

Четыре источника, четыре схемы, один язык запросов после нормализации. Именно поэтому зрелые SIEM опираются на общую модель полей — Elastic Common Schema (ECS) и OCSF (Open Cybersecurity Schema Framework) существуют ровно затем, чтобы `source.ip` означало одно и то же везде, и одно правило могло охватить любой источник.

## Стадия 3: корреляция — чтение логов как истории

Корреляция — причина существования SIEM. Единственный неудачный вход — это шум. Неудачный вход на сервисный аккаунт, который *никогда* не давал сбоев, из страны, которую ты никогда не видел, с последующим за девяносто секунд успешным входом и эскалацией привилегий — это атака, и увидеть её можно только **соединяя события между источниками на временном окне**. Этот join — ровно то, что сделала возможным нормализация.

Корреляционные правила бывают двух широких форм, и зрелые инженеры намеренно их смешивают:

- **Сигнатурные / known-bad правила** напрямую отображают поведение атакующего. Фреймворки вроде **MITRE ATT&CK** каталогизируют техники — скажем, `T1110` Brute Force или `T1078` Valid Accounts — и ты пишешь правила, детектирующие их телеметрию: «10 неудачных аутентификаций, потом успех с одного IP за 5 минут» отображается в credential stuffing. Они точны, но ловят только то, что ты назвал.
- **Аномальные / поведенческие правила** помечают отклонение от базовой линии: пользователь тянет в 50× свой обычный объём данных, сервер открывает исходящее соединение, которого никогда не делал. Они ловят неизвестное, но платят за это ложными срабатываниями, потому что «необычное» и «вредоносное» пересекаются лишь частично.

<Inset kind="why" lang="ru">
Почему бы просто не алертить на каждую отдельную подозрительную строку и пропустить корреляцию? Потому что объём тебя уничтожит. Инфраструктура среднего размера выдаёт миллионы событий в час, и заметная их доля в изоляции выглядит слегка подозрительно — каждый неудачный вход, каждый новый IP, каждая крупная загрузка. Алерти на каждое — и ты похоронишь реальную атаку под десятью тысячами безобидных; за неделю дежурный замьютит канал. Корреляция — это акт *поднятия планки*: она требует, чтобы несколько слабых сигналов выстроились в конкретной последовательности и окне, прежде чем что-либо достигнет человека, обменивая сырую полноту на точность, которая держит алерты заслуживающими доверия.
</Inset>

## Стадия 4: алертинг — где SIEM живут или умирают

Весь конвейер существует, чтобы произвести **небольшое число высокоточных алертов**. Эту часть команды недооценивают. Хранение — решённая, скучная проблема: можно докупить. Тюнинг — вечная. Каждое правило сидит на компромиссе между **ложными срабатываниями** (кричать «волки!», пока аналитики не перестанут слушать — *усталость от алертов*, задокументированный сбой за реальными пробоями, которые технически детектировали, а потом проигнорировали) и **ложными пропусками** (реальная атака, которая никогда не задевает правило). Правило, затянутое достаточно туго, чтобы быть тихим, может пропустить новую атаку; правило, ослабленное достаточно, чтобы поймать всё, топит команду. Зрелая мера SIEM — не сколько он принимает, а его **отношение сигнал/шум**: алертов в день, которые человек реально может разобрать, и доля тех из них, что настоящие.

<TradeoffMatrix
  id="02-siem-fundamentals-tradeoff-1"
  lessonSlug="02-siem-fundamentals"
  lang="ru"
  prompt="Твой SIEM выдаёт 800 алертов «подозрительный вход» в день; аналитики тихо замьютили канал, и в прошлом месяце сквозь него проскочил реальный захват аккаунта. Выбери ответ, который лучше всего чинит корневую проблему."
  options={[
    { name: "Сделай правило корреляционным: требуй вход с новой гео И неудачную MFA И смену привилегий в рамках одной сессии перед алертом, а остальное направь в поисковое хранилище", summary: "Поднять планку корреляцией.", correct: true, justification: "Болезнь — низкое сигнал/шум, вызывающее усталость от алертов, — ровно тот механизм, что позволил проигнорировать задетектированную атаку. Лекарство — перестать алертить на отдельные слабые сигналы и потребовать, чтобы несколько выстроились в конкретной последовательности и окне, для чего корреляция и нужна. Всё по-прежнему попадает в хранение, так что можно искать при расследовании; ты срезал объём алертов, не ослепнув. Это поднимает точность там, где важно: в очереди человека." },
    { name: "Увеличь хранение логов с 30 до 365 дней и докупи хранилище", summary: "Хранить больше.", justification: "Хранение — лёгкая, решённая половина проблемы и не имеет отношения к тому, почему алерт проигнорировали. Больше хранения помогает форензике постфактум, но не снижает 800 ежедневных ложных срабатываний и не восстанавливает доверие команды к каналу. Ты потратишь деньги на стадию, которая не была сломана." },
    { name: "Полностью отключи шумное правило входа, пока не будет времени его починить", summary: "Отключить правило.", justification: "Это обменивает ложные срабатывания на гарантированный ложный пропуск — теперь ты детектируешь ноль захватов аккаунтов, ровно ту атаку, что уже проскочила. Мьютить категорию детектирования за её шумность — это как пробои остаются невидимыми; цель — сделать правило точным, а не слепым." },
    { name: "Перенаправь все 800 алертов второй команде, чтобы каждая видела меньше", summary: "Разделить очередь.", justification: "Размазывание шума по большему числу людей не поднимает сигнал/шум; оно лишь распределяет усталость. То же низкоточное правило по-прежнему срабатывает 800 раз, ни один отдельный алерт не становится надёжнее, а ты добавил неоднозначность передачи о том, кто владеет реальным попаданием." },
  ]}
/>

<Quiz
  id="02-siem-fundamentals-quiz-1"
  lessonSlug="02-siem-fundamentals"
  lang="ru"
  question="Почему нормализация — предпосылка для корреляции, а не опциональная приятность?"
  choices={[
    { label: "Потому что корреляция соединяет события между источниками, а нельзя соединить поля, которые названы и устроены по-разному в каждом логе", correct: true },
    { label: "Потому что нормализация сжимает логи, и они занимают меньше места", misconception: "Нормализация — про общую схему, а не про сжатие. Её цель — сделать поля сопоставимыми между источниками; экономия места не является сутью и не гарантирована." },
    { label: "Потому что сырые логи зашифрованы, и нормализация их расшифровывает", misconception: "Нормализация парсит и отображает поля в общую схему; к шифрованию она отношения не имеет. Логи обычно не зашифрованы так, чтобы это блокировало парсинг." },
    { label: "Потому что алертинг умеет читать только JSON, поэтому всё нужно конвертировать в JSON", misconception: "Дело не в едином проводном формате вроде JSON — а в общей *модели полей* (например, source.ip означает одно и то же везде), чтобы одно правило могло охватить источники." },
  ]}
/>

<Quiz
  id="02-siem-fundamentals-quiz-2"
  lessonSlug="02-siem-fundamentals"
  lang="ru"
  question="SIEM принимает 40 миллионов событий/час и хранит их идеально, но дежурная команда замьютила свой канал алертов. Что на самом деле сломано?"
  choices={[
    { label: "Сигнал/шум: стадия алертинга порождает слишком много низкоточных алертов, поэтому реальные игнорируются (усталость от алертов)", correct: true },
    { label: "Сбор: 40 миллионов событий/час означает, что логи теряются", misconception: "Высокий объём, который идеально хранится, — признак того, что сбор работает, а не падает. Сбой ниже по конвейеру, на алертинге — слишком много шума, а не слишком мало данных." },
    { label: "Хранение: нужен кластер побольше, чтобы держать 40 миллионов событий/час", misconception: "События хранятся прекрасно; хранение — решённая, скучная половина. Нерешённая проблема — превратить этот объём в несколько алертов, достойных внимания человека." },
    { label: "Ничего — замьюченный канал просто означает тихий, безопасный период", misconception: "Замьюченный канал — противоположность безопасности: он означает, что реальные алерты игнорируются. Это усталость от алертов, задокументированный путь, которым задетектированные пробои остаются без реакции." },
  ]}
/>

<DragOrder
  id="02-siem-fundamentals-drag-1"
  lessonSlug="02-siem-fundamentals"
  lang="ru"
  prompt="Упорядочь четыре стадии конвейера SIEM, от сырого лога до человека:"
  items={[
    { id: "a", label: "Сбор — доставить каждый лог в одно место (агенты, syslog, облачные API)" },
    { id: "b", label: "Нормализация — распарсить каждый формат в одну общую схему" },
    { id: "c", label: "Корреляция — соединить нормализованные события между источниками на временном окне" },
    { id: "d", label: "Алерт — отдать человеку только высокоточные совпадения" },
  ]}
  correctOrder={["a", "b", "c", "d"]}
/>

</Explanation>

<KeyTakeaway>SIEM — это конвейер из четырёх стадий: собрать каждый лог, нормализовать его в одну схему, скоррелировать события между источниками на временном окне и алертить на немногие высокоточные совпадения. Хранение легко; сложная, зрелая часть — сигнал/шум: превратить миллионы событий в те три, что что-то значат.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-siem-fundamentals-retrieval"
  lang="ru"
  questions={[
    { q: "Пройди по четырём стадиям конвейера SIEM и назови реальный продакшен-сбой на каждой.", a: "SIEM работает как сбор → нормализация → корреляция → алерт. (1) Сбор доставляет каждый лог в одно место через агенты, syslog и облачные API (CloudTrail, K8s audit). Его сбой — тихие слепые пятна: мёртвый агент или оброненный UDP-syslog теряют записи без ошибки, поэтому отсутствие логов от источника само должно быть алертом, ведь атакующие отключают логирование. (2) Нормализация парсит каждый формат и отображает его поля в одну общую схему (например, ECS/OCSF), так что Linux sshd 'user'/'rhost', CloudTrail 'principalId'/'sourceIPAddress' и K8s 'user.username'/'sourceIPs' все становятся user.name/source.ip. Её сбой — хрупкие или отсутствующие парсеры, оставляющие события несоединимыми. (3) Корреляция соединяет нормализованные события между источниками на временном окне — этот join возможен только потому, что нормализация унифицировала поля — превращая последовательность вроде вход-с-новой-гео → неудачная MFA → sudo → крупная исходящая передача в одно детектирование. (4) Алертинг отдаёт человеку немногие высокоточные совпадения. Его сбой — усталость от алертов: слишком много низкоточных алертов заставляют команду замьютить канал и пропустить реальный. Хранение — лёгкая половина; сигнал/шум — сложная." },
    { q: "SIEM безупречно принимает 40 млн событий/час, но реальный захват аккаунта пропущен. Где проблема и как её чинить?", a: "Проблема не в сборе и не в хранении — безупречный приём высокого объёма означает, что эти стадии работают. Проблема на алертинге: низкое сигнал/шум. Если SIEM выдаёт сотни низкоточных алертов «подозрительный вход» в день, у аналитиков развивается усталость от алертов, и они мьютят канал, так что реальный захват аккаунта, который всё же задел правило, игнорируется — задокументированный паттерн, где пробой технически детектируют, а потом не реагируют. Лекарство — поднять планку корреляцией вместо алертинга на отдельные слабые сигналы: требовать, чтобы несколько сигналов выстроились в конкретной последовательности и окне (например, вход с новой гео И неудачная MFA И смена привилегий в рамках одной сессии), прежде чем что-либо достигнет человека, при этом всё остальное по-прежнему течёт в поисковое хранение для расследования. Ты обмениваешь сырую полноту на отдельных событиях на точность, которая держит очередь человека заслуживающей доверия. Важно, что ты НЕ «чинишь» это отключением шумного правила (это гарантирует ложный пропуск ровно той атаки), докупкой хранилища (несломанная стадия) или разделением того же шума между двумя командами (это размазывает усталость, не поднимая точности). Зрелая метрика — алертов/день, которые человек может разобрать, и доля настоящих, а не событий/час на входе." },
  ]}
/>

<Recap lang="ru">
SIEM — Security Information and Event Management — существует потому, что каждая запись, нужная чтобы поймать вторжение, уже есть, просто разбросана по десяткам хостов в несовместимых форматах, и никто не читает их как одну историю. Он работает как конвейер из четырёх стадий. Сбор доставляет каждый лог в одно место (агенты, syslog, облачные API) и трактует замолчавший источник как отдельный алерт. Нормализация парсит каждый формат в общую схему (ECS/OCSF), чтобы source.ip означало одно и то же в строке sshd, событии CloudTrail и audit-логе K8s — без чего корреляция невозможна. Корреляция соединяет эти нормализованные события между источниками на временном окне, смешивая сигнатурные правила, отображённые на техники MITRE ATT&CK, с аномальными правилами против базовой линии, так что последовательность слабых сигналов становится одним детектированием. Алертинг отдаёт человеку только высокоточные совпадения, и именно здесь SIEM живут или умирают: хранение легко, но вечный компромисс между ложными срабатываниями (усталость от алертов) и ложными пропусками (пропущенная атака) делает сигнал/шум настоящей зрелой мерой. В следующий раз, когда SIEM хвалят за приём 40 миллионов событий в час, твой первый вопрос правильный: со сколькими алертами в день человек реально работает, и сколько из них настоящие?
</Recap>
