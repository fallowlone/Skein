---
concepts:
- alert-fatigue
- tuning
- signal-to-noise
- precision-recall
- triage
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 5
prereqs: []
slug: 05-alert-quality-and-tuning
sources:
- https://attack.mitre.org/
- https://csrc.nist.gov/pubs/sp/800/92/final
spiral: []
status: ready
summary: 'Почти все алерты — шум, а стена шума прячет единственное реальное вторжение. Зрелый тюнинг поднимает точность — убивая малоценные правила и ограничивая остальные — не выбрасывая тихо тот истинный позитив, ради которого вы существуете.'
title: Alert quality and tuning
track: security-defensive
unit: 01-logging-monitoring-detection
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Взлом Target — это притча, которую каждый детекшн-инженер усваивает на собственной шкуре: сенсор FireEye *сработал*. Малварь была названа, алерт поднят, и он упал в консоль, которая уже захлёбывалась. Он лёг рядом с тысячами других алертов той недели — в основном безобидных, повторяющихся, игнорируемых, — поэтому единственный, что имел значение, разобрали как очередную погоду. Сорок миллионов карт спустя урок звучал не как «купите сенсор получше». Сенсор работал. Конвейер, который должен был превратить его вывод в *решение*, был обучен — алерт за проигнорированным алертом — считать всё шумом. Это и есть усталость от алертов, и это не человеческий промах, который чинится служебной запиской. Это математическая задача: когда 99% того, что доходит до аналитика, ложно, рациональный ход аналитика — перестать читать.
</Hook>

К концу урока ты будешь знать, почему почти все алерты — это шум, как тюнинговать детекты на точность, не выбрасывая тихо единственный истинный позитив, и как измерить, помог ли тюнинг или просто спрятал взлом.

<Crux>Почти все алерты — шум, прячущий вторжение. Тюнинг на точность: убери слабые правила, ограничь остальные — не теряя настоящий алерт.</Crux>

<Explanation>

## Почему стена алертов и есть настоящая уязвимость

Правило детектирования существует не для того, чтобы срабатывать; оно существует, чтобы *вызвать верное действие*. Между правилом и действием сидит человек с конечным бюджетом внимания, и этот бюджет — самый дефицитный ресурс во всём стеке. Как только ты это принимаешь, «у нас 600 правил и сияющий дашборд» перестаёт быть утешением и становится предупреждением: каждое малоценное правило, которое ты добавляешь, тратит то внимание, что нужно тебе для ценного.

Ловушка — в базовой частоте (base rate). В реальной среде подавляющее большинство активности легитимно. Допустим, правило помечает как подозрительное 1 событие из 1000, а среда генерирует 10 миллионов событий в день — это 10 000 алертов. Теперь допустим, правило по-настоящему хорошее, с 99% долей истинных срабатываний на реальных атаках, но реальные атаки редки: скажем, 5 настоящих вредоносных событий, спрятанных в этих 10 миллионах. Правило ловит все 5 (отличный recall), но при этом помечает, скажем, 0,1% из безобидных 10 миллионов как подозрительные — 10 000 ложных срабатываний. Твой аналитик теперь стоит перед 10 005 алертами, чтобы найти 5 настоящих. **Точность (precision) = 5 / 10 005 ≈ 0,05%.** Recall идеален; система всё равно бесполезна, потому что ни один человек не читает 10 005 алертов в поисках 5 иголок, прежде чем решить, что стог не стоит обыска.

Это ошибка базовой частоты в операционной форме, и именно поэтому «мы поймали, алерт сработал» — холодное утешение. Покрытие без точности не защищает тебя — оно *хоронит* тебя, и взлом прячется в обломках твоих же детектов.

## Precision, recall и рычаг, который ты на самом деле тянешь

Два числа управляют каждым решением о тюнинге, и их смешение — самая частая ошибка зрелого инженера:

- **Recall** (полнота, чувствительность) — из реальных атак какую долю ты поймал? Пропустить настоящую атаку — это *ложноотрицательное* (false negative). Это число хоронит компании.
- **Precision** (точность) — из алертов, что ты выдал, какая доля была настоящей? Кричать «волки» — это *ложноположительное* (false positive). Это число хоронит *аналитиков* — а выгоревший, десенсибилизированный аналитик дальше по цепочке порождает ложноотрицательное.

Эти два связаны твоим порогом. Ослабь правило (срабатывай на слабый сигнал) — recall растёт, но precision падает — больше шума. Затяни — precision растёт, но recall падает — ты рискуешь пропустить настоящий. **Тюнинг — это не «сделать так, чтобы алерты исчезли». Тюнинг — это осознанное перемещение рабочей точки вдоль этой кривой, с открытыми глазами на то, какую ошибку ты меняешь на какую.** Кардинальный грех — затянуть правило, пока шум не прекратится, *и не заметить, что ты затянул его за истинный позитив*: купить тихую консоль ценой слепоты.

<FlowDiagram
  label="Конвейер сигнал/шум: сырые события оцениваются правилом, которое делит их на истинные позитивы, ложные позитивы и пропущенные истинные позитивы (ложноотрицательные). Ложные позитивы порождают усталость от алертов, которая десенсибилизирует аналитика и превращает настоящий истинный позитив в проигнорированный алерт — провал Target. Тюнинг на точность подавляет ложные позитивы, не сдвигая порог за истинный позитив."
  perRow={4}
  hue="var(--d-crypto)"
  nodes={[
    { id: "events", label: "Сырые события", sub: "10М / день" },
    { id: "rule", label: "Правило детекта", sub: "порог" },
    { id: "fp", label: "Ложные позитивы", sub: "шум" },
    { id: "tp", label: "Истинный позитив", sub: "настоящий" },
    { id: "fatigue", label: "Усталость", sub: "аналитик глохнет" },
    { id: "ignored", label: "Игнор алерта", sub: "провал Target" },
    { id: "tune", label: "Тюнинг точности", sub: "давим FP" },
    { id: "action", label: "Верное действие", sub: "сдержано" },
  ]}
  edges={[
    { from: "events", to: "rule" },
    { from: "rule", to: "fp", label: "слабый" },
    { from: "rule", to: "tp", label: "сильный" },
    { from: "fp", to: "fatigue" },
    { from: "fatigue", to: "ignored", label: "глохнет" },
    { from: "tune", to: "fp", label: "режем шум" },
    { from: "tp", to: "action", label: "если виден" },
  ]}
  caption="Взлом прячется не в отсутствующем правиле — он прячется за ложными позитивами. Тюнинг на точность убирает шум, чтобы истинный позитив стал виден, не сдвигая за него порог (что превратило бы его в тихое ложноотрицательное)."
/>

## Как зрелый инженер реально тюнингует: убей, ограничь, обогати, разнеси по уровням

Ты не тюнингуешь, глобально поднимая порог, пока не станет тихо. Это и есть ход, который теряет истинный позитив. Ты тюнингуешь правило за правилом, и порядок имеет значение:

1. **Убей правила, которые никогда не могут вызвать действие.** Каждый алерт должен отображаться в решение: *если оно сработает, что я сделаю?* Правило, чей честный ответ — «ничего, мы всегда закрываем его как безобидное», — чистый налог: оно тратит внимание и приучает команду авто-отклонять. Удаление поднимает точность с нулевой ценой для recall, потому что оно изначально не ловило ни одной реальной атаки. Это самая дешёвая точность, которую ты когда-либо купишь.
2. **Ограничивай, а не глуши.** Сканер уязвимостей, который долбит сеть в 02:00, ночь за ночью триггерит твоё правило на сканирование портов. Неверный фикс — отключить детектирование сканирования портов. Верный фикс — *ограниченное подавление* (scoped suppression): исключи этот IP-источник и это окно, и алертуй, если паттерн появляется откуда угодно ещё или в любое другое время. Ты сохранил recall для атаки, что тебе важна, и убрал шум от доверенного источника. Опасность широких подавлений в том, что они гниют — доверенный IP переназначают, окно дрейфует, — поэтому у каждого подавления должен быть владелец и срок истечения, иначе оно становится постоянным слепым пятном, внутри которого может спрятаться атакующий.
3. **Обогащай, чтобы аналитик решал за секунды, а не за минуты.** Голое «неудачный вход с 10.2.3.4» вынуждает пятиминутное расследование. Тот же алерт, обогащённый тем, *чья* учётка, гео/ASN, «этот пользователь обычно входит из Берлина, а это — из хостинг-провайдера в другой стране» и ссылкой на недавнюю активность, — это решение за пять секунд. Обогащение поднимает *эффективную* точность: истинный позитив, тонувший в неоднозначности, теперь очевидно требует действия.
4. **Разнеси по уровням серьёзности и маршрутизируй соответственно.** Не каждый детект заслуживает звонка в 03:00. Сопоставь детекты с серьёзностью (тут помогает тактика ATT&CK: техника дампа учёток важнее, чем пинг-разведка) и направь низкоприоритетные, высокообъёмные сигналы на дашборд или в ежедневный дайджест, оставив пейджер для горстки тех, что действительно требуют человека сейчас. Уровни защищают бюджет внимания без потери recall — низкосигнальные события всё ещё *записываются*, просто не *прерывают*.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Ход</th>
        <th class="border border-border px-3 py-2 text-left">Эффект на precision</th>
        <th class="border border-border px-3 py-2 text-left">Эффект на recall</th>
        <th class="border border-border px-3 py-2 text-left">За каким сбоем следить</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Удалить всегда-безобидное правило</td>
        <td class="border border-border px-3 py-2">Вверх</td>
        <td class="border border-border px-3 py-2">Без изменений (ничего не ловило)</td>
        <td class="border border-border px-3 py-2">Удалить правило, которое *ловило* реальные — сначала убедись, что оно не давало истинных позитивов</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Глобально поднять порог</td>
        <td class="border border-border px-3 py-2">Вверх</td>
        <td class="border border-border px-3 py-2">Вниз — риск истинного позитива</td>
        <td class="border border-border px-3 py-2">Купить тихую консоль ценой слепоты к реальной атаке</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Ограниченное подавление (источник + окно)</td>
        <td class="border border-border px-3 py-2">Вверх</td>
        <td class="border border-border px-3 py-2">Сохранён для всего прочего</td>
        <td class="border border-border px-3 py-2">Подавление гниёт: устаревший IP/окно становится постоянным слепым пятном</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Обогатить алерт</td>
        <td class="border border-border px-3 py-2">Вверх (эффективная)</td>
        <td class="border border-border px-3 py-2">Без изменений</td>
        <td class="border border-border px-3 py-2">Источник обогащения медленный/недоступен и стопорит триаж</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Разнести и маршрутизировать по серьёзности</td>
        <td class="border border-border px-3 py-2">Вверх (по каналу)</td>
        <td class="border border-border px-3 py-2">Без изменений (всё ещё записано)</td>
        <td class="border border-border px-3 py-2">Неверно отнесённый критический падает в дайджест, который никто не читает</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="ru">
Почему просто не купить модель, которая оценивает всё и выводит только верхний 1%? Потому что оптимизатор чистой точности без нижней границы recall тихо научится никогда не алертовать на редкое, о чём у него меньше всего данных, — а это ровно то новое вторжение, что тебе нужнее всего поймать. Precision и recall — это *ограниченная* оптимизация: максимизируй точность **при условии** нижней границы recall на тех техниках, что реально тебя ранят. Вот почему тюнинг привязан к модели угроз (какие техники ATT&CK я не имею права пропустить?), а не к «уменьшить объём тикетов» как сырому KPI. Оптимизируй один объём тикетов — и самый дешёвый путь, что найдёт система, — перестать детектировать атаки, которые труднее всего детектировать.
</Inset>

## Как измерить, сработал ли тюнинг — а не просто стало ли тише

Более тихая консоль — не доказательство более качественной; консоль Target можно было сделать тише, удалив ровно то правило, что сработало. Поэтому ты измеряешь сделку явно:

- **Прокси точности** — какая доля сработавших алертов берётся в работу против авто-закрытия как безобидных? Правило, авто-закрываемое в 99% случаев, — проблема точности, которую видно, не дожидаясь атаки.
- **Recall, проверяемый намеренно** — нельзя измерить ложноотрицательные ожиданием (ты не знаешь, что пропустил). Ты изготавливаешь известные истинные позитивы: **purple-team учения и юнит-тесты детектов**, которые воспроизводят реальную технику (по ATT&CK) и утверждают, что правило срабатывает. Изменение тюнинга, ломающее purple-team детект, ловится *до того*, как атакующий использует ту же брешь.
- **Среднее время до детектирования/триажа** — обогащение и уровни должны тянуть это вниз; если MTTD растёт после прохода тюнинга, ты обменял ясность на тишину.

<TradeoffMatrix
  id="05-alert-quality-tuning-tradeoff-1"
  lessonSlug="05-alert-quality-and-tuning"
  lang="ru"
  prompt="Высокообъёмное правило сканирования портов срабатывает каждую ночь, потому что внутренний сканер уязвимостей на 10.0.0.9 запускается в 02:00. Аналитики начали игнорировать все алерты о сканировании портов. Выбери лучший ход тюнинга."
  options={[
    { name: "Ограниченное подавление: исключи источник 10.0.0.9 в окне сканирования 02:00 и продолжай алертовать на сканирование портов из любого другого источника или времени", summary: "Ограничивай, не глуши.", correct: true, justification: "Это убирает известно-безобидный шум, десенсибилизирующий команду, сохраняя recall для атаки, что тебе действительно важна, — сканирования портов с неожиданного хоста или в неожиданное время. Точность растёт, детект реальной атаки остаётся целым. Снабди подавление владельцем и сроком истечения, чтобы оно не сгнило тихо в постоянное слепое пятно, когда 10.0.0.9 переназначат." },
    { name: "Полностью отключить правило сканирования портов — оно явно слишком шумное", summary: "Удалить детект.", justification: "Это покупает тишину ценой слепоты. Сканирование портов — реальная техника разведки; удаление правила обнуляет recall для атакующего, делающего ровно то же, что и сканер. Шум шёл из одного доверенного источника, а не от того, что правило бесполезно, — режь шум, а не детект." },
    { name: "Глобально поднять порог правила, чтобы оно срабатывало только на очень агрессивные сканирования", summary: "Поднять порог.", justification: "Глобальный подъём порога слепо обменивает recall: аккуратный атакующий сканирует медленно и тихо, ровно под порогом «только агрессивные», так что ты теряешь скрытную разведку, что тебе важнее всего поймать, всё ещё алертуя на громкий безобидный сканер, если он агрессивен. Ты сдвинул рабочую точку, не нацелившись на реальный источник шума." },
    { name: "Отправлять все алерты сканирования портов прямо в ежедневный дайджест, на который никто не реагирует", summary: "Похоронить в дайджесте.", justification: "Уровни — реальный инструмент, но маршрутизация разведсигнала, что может предшествовать взлому, в нечитаемый дайджест — это неверное отнесение по уровню: ты спрятал истинный позитив, а не подавил ложные. Проблема — безобидный источник; ограничь его, а уровни назначай по подлинной серьёзности, а не по тому, какой канал тише." },
  ]}
/>

<Quiz
  id="05-alert-quality-tuning-quiz-1"
  lessonSlug="05-alert-quality-and-tuning"
  lang="ru"
  question="Правило ловит 100% реальных атак, но в среде с 10М событий/день также выдаёт 10 000 ложных позитивов в день против ~5 реальных событий. Почему это всё равно проваленный детект?"
  choices={[
    { label: "Recall идеален, но precision ~0,05%, так что аналитики тонут в шуме и рационально перестают читать — настоящий хоронится", correct: true },
    { label: "Потому что recall слишком низок, чтобы ловить атаки", misconception: "Recall здесь идеален — он ловит все 5 реальных событий. Сбой — в точности: 10 000 ложных позитивов хоронят 5 истинных, и это проблема базовой частоты, а не recall." },
    { label: "Потому что 10 миллионов событий в день вообще слишком много, чтобы логировать", misconception: "Объём логов — не вопрос; ты можешь всё хранить. Вопрос — в соотношении алерт-к-истинному-позитиву, доходящем до аналитика: точность, а не хранилище." },
    { label: "Потому что ложные позитивы безвредны, пока recall = 100%", misconception: "Ложные позитивы здесь — суть вреда: они вызывают усталость от алертов, десенсибилизирующую аналитиков и превращающую настоящий истинный позитив в проигнорированный алерт. Идеальный recall впустую, если алерт никто не читает." },
  ]}
/>

<Quiz
  id="05-alert-quality-tuning-quiz-2"
  lessonSlug="05-alert-quality-and-tuning"
  lang="ru"
  question="Нельзя измерить ложноотрицательные ожиданием (ты не знаешь, что пропустил). Как зрелая команда реально проверяет recall раньше атакующего?"
  choices={[
    { label: "Изготавливает известные истинные позитивы — purple-team учения и юнит-тесты детектов, воспроизводящие реальную технику ATT&CK и утверждающие, что правило срабатывает", correct: true },
    { label: "Считает, сколько алертов сработало за неделю, и полагает, что больше алертов = лучше recall", misconception: "Число алертов — сигнал точности/объёма, не recall. Поток ложных позитивов раздувает счёт, ничего не говоря об атаках, что ты пропустил." },
    { label: "Ждёт следующего взлома и смотрит, сработало ли правило", misconception: "Это измерение recall через получение взлома — слишком поздно и слишком дорого. Смысл — вскрывать ложноотрицательные проактивно, до того как противник использует брешь." },
    { label: "Опускает каждый порог, пока консоль снова не станет занятой", misconception: "Ослабление порогов поднимает шум, а не измеренный recall, и возвращает усталость от алертов, которую ты убирал тюнингом. Recall проверяют воспроизведением известных истинных техник, а не повторным добавлением шума." },
  ]}
/>

<DragOrder
  id="05-alert-quality-tuning-drag-1"
  lessonSlug="05-alert-quality-and-tuning"
  lang="ru"
  prompt="Упорядочь проход тюнинга на точность по шумному правилу от самого безопасного к последнему, чтобы никогда не потерять истинный позитив:"
  items={[
    { id: "a", label: "Убедись, что правило никогда не давало реального истинного позитива (проверь историю)" },
    { id: "b", label: "Определи конкретный безобидный источник/паттерн, порождающий шум" },
    { id: "c", label: "Примени ограниченное подавление (источник + окно) с владельцем и сроком истечения" },
    { id: "d", label: "Обогати оставшиеся алерты, чтобы истинный позитив очевидно требовал действия" },
    { id: "e", label: "Перезапусти purple-team тест, чтобы подтвердить, что реальная техника всё ещё срабатывает" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Почти все алерты — шум, и именно шум, а не отсутствующее правило, прячет реальное вторжение. Тюнингуй на точность, удаляя никогда-не-обрабатываемые правила и применяя ограниченные, владеемые, истекающие подавления; никогда не поднимай порог глобально, пока консоль не станет тихой, — так ты слепнешь к истинному позитиву. Проверяй recall purple-team воспроизведениями, чтобы доказать, что тюнинг не выбросил тот единственный алерт, ради которого ты существуешь.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-alert-quality-tuning-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни через базовую частоту, почему правило с идеальным recall всё равно может быть проваленным детектом — и что это значит для покрытия против точности.", a: "Реальные среды доминируются безобидной активностью, поэтому даже малая доля ложных срабатываний даёт огромное число ложных алертов относительно горстки реальных атак. Возьми среду с 10М событий/день и ~5 по-настоящему вредоносными событиями. Правило со 100% recall ловит все 5 — но если оно также помечает всего 0,1% из 10М безобидных событий, это 10 000 ложных позитивов. Аналитик теперь видит 10 005 алертов, чтобы найти 5 настоящих, так что точность около 0,05%. Ни один человек не читает 10 005 алертов в охоте на 5 иголок, прежде чем рационально заключить, что стог не стоит обыска, — это ошибка базовой частоты в операционной форме и ровно провал Target, где сенсор сработал, но алерт разобрали как шум. Вывод: покрытие без точности не защищает, оно хоронит. Добавление малоценных правил тратит конечный бюджет внимания аналитика, а реальное вторжение прячется не в отсутствующем правиле, а за стеной ложных позитивов, что порождают существующие правила. Задача тюнинга — поднять точность (убрать шум), защищая recall на тех техниках, что реально тебя ранят." },
    { q: "Пройди по тому, как зрелый инженер тюнингует шумное правило на точность, не выбрасывая истинный позитив, и как он доказывает, что не выбросил.", a: "Тюнингуй правило за правилом, в порядке безопасности. Сначала убей правила, которые никогда не могут вызвать действие: если честный ответ на «если оно сработает, что я сделаю?» — «ничего, мы всегда закрываем как безобидное», удали его — это поднимает точность с нулевой ценой для recall, потому что оно никогда не ловило реальную атаку (сначала сверься с историей). Второе — ограничивай, а не глуши: когда доверенный источник (скажем, ночной внутренний сканер уязвимостей) триггерит правило, не отключай детект — примени ограниченное подавление, что исключает этот источник и временное окно, всё ещё алертуя на тот же паттерн откуда угодно ещё, и дай подавлению владельца и срок истечения, чтобы оно не сгнило в постоянное слепое пятно, когда IP переназначат. Третье — обогащай оставшиеся алерты (чья учётка, гео/ASN, нормальный базовый профиль, недавняя активность), чтобы истинный позитив, тонувший в неоднозначности, стал решением за пять секунд, — поднимая эффективную точность. Четвёртое — разнеси по серьёзности и маршрутизируй: пейджи только на горстку высокосерьёзных техник, шли высокообъёмные низкосерьёзные сигналы на дашборд или в дайджест, чтобы бюджет внимания был защищён без потери recall (события всё ещё записаны). Чего ты никогда не делаешь — глобально поднимать порог, пока консоль не затихнет, потому что это тихо проскальзывает за истинный позитив и превращает его в ложноотрицательное. Чтобы доказать, что тюнинг не ослепил тебя, ты не ждёшь — ты изготавливаешь известные истинные позитивы через purple-team учения и юнит-тесты детектов, что воспроизводят реальную технику ATT&CK и утверждают, что правило всё ещё срабатывает, и следишь за прокси точности (доля взятых в работу против авто-закрытых) и средним временем до детектирования. Изменение тюнинга, ломающее purple-team детект, ловится до того, как атакующий использует ту же брешь." },
  ]}
/>

<Recap lang="ru">
Почти все алерты безопасности — шум, и взлом прячется не в правиле, что ты забыл написать, а за стеной ложных позитивов, что порождают твои существующие правила, — это ошибка базовой частоты в операционной форме и подлинный урок Target, где сенсор сработал в консоль, уже обученную его игнорировать. Recall (поймал ли ты реальную атаку?) и precision (были ли твои алерты настоящими?) связаны твоим порогом, и кардинальный грех — затянуть правило, пока не станет тихо, не заметив, что ты затянул его за истинный позитив. Зрелый инженер тюнингует правило за правилом: удаляет никогда-не-обрабатываемые правила (бесплатная точность), применяет ограниченные подавления с владельцем и сроком истечения вместо глушения целых детектов, обогащает алерты, чтобы истинный позитив очевидно требовал действия, и разносит по серьёзности, чтобы прерывала человека лишь по-настоящему срочная горстка. А поскольку ложноотрицательные нельзя измерить ожиданием, ты проверяешь recall намеренно — purple-team воспроизведениями и юнит-тестами детектов, что запускают известную технику ATT&CK и утверждают, что правило её ловит. Так что в следующий раз, когда кто-то празднует более тихий дашборд, твой первый вопрос: тише, потому что мы убрали шум, или тише, потому что мы просто ослепли к тому, что важен?
</Recap>
