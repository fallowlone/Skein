---
concepts:
- detection-engineering
- detection-as-code
- false-positive
- true-positive
- sigma
deepensInto: []
estMin: 18
lang: ru
lessonType: topic
level: senior
order: 4
prereqs: []
slug: 04-detection-engineering
sources:
- https://attack.mitre.org/
- https://csrc.nist.gov/pubs/sp/800/92/final
spiral: []
status: ready
summary: 'Правило, накликанное в консоли SIEM, не имеет истории, теста и рецензента.
  Detection engineering обращается с детектами как с кодом — Sigma-правила в Git,
  проверенные на образцах логов, отрецензированные в PR, — и тюнинг становится
  диффом, а не кликом, который никто не помнит.'
title: Detection engineering
track: security-defensive
unit: 01-logging-monitoring-detection
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
3 часа ночи, дежурный аналитик тонет. Детект «Suspicious PowerShell» сработал за ночь 4000 раз — и каждый раз это задание бэкапа, добавленное на сервер кем-то восемь месяцев назад. Аналитик делает то, что делает любой измотанный аналитик: открывает консоль SIEM, находит правило, добавляет исключение для этого хоста, жмёт «сохранить». Алерты прекращаются. Никто ничего не записывает. Через шесть недель другой инженер, глядя на то же шумное правило, *удаляет* условие целиком, чтобы оно замолчало, — и молча пробивает дыру, сквозь которую ровно та техника, ради которой правило и строилось, теперь проходит незамеченной. Нет ни коммита, ни диффа, ни ревью, ни теста, который бы это поймал. Правило никогда не было кодом. Оно было настройкой в коробке, а у настроек в коробке нет памяти.
</Hook>

К концу урока ты будешь знать, как писать, тестировать и версионировать детекты как код — Sigma-правила в Git, проверенные на образцах логов, отрецензированные в pull request, — чтобы тюнинг шумного правила был аудируемым инженерным изменением, а не забытым кликом.

<Crux>Detection-as-code держит Sigma-правила в Git, тестирует и рецензирует их, и тюнинг false positive — это дифф, а не забытый клик.</Crux>

<Explanation>

## Что такое detection engineering на самом деле

Ты уже знаешь, что правила SIEM превращают логи в алерты. Detection engineering — это дисциплина, которая задаёт вопрос потруднее: *как строить, менять и доверять этим правилам годами, в команде, не давая набору правил сгнить в шум?* Ответ, к которому пришла индустрия, — **detection-as-code**: обращайся с каждым детектом как с прикладным кодом. Он живёт в Git-репозитории, а не в консоли вендора. У него есть автор, сообщение коммита и рецензент. Он едет через CI. И, что критично, у него есть тесты: образцы лог-событий, которые *должны* его поднять (true positive), и образцы, которые *не должны* (негативы), прогоняемые на каждое изменение.

Модель «накликать в консоли» проваливается по тем же причинам, по каким проваливается ручная правка прода через GUI базы данных. Нет истории — ты не ответишь, кто изменил правило и зачем. Нет ревью — исключение, добавленное усталым аналитиком в 3 ночи, едет без второй пары глаз. Нет теста — правка одного символа в регулярке может тихо перестать ловить то, ради чего правило строилось, и *никто не узнает до самого пробоя*. Detection-as-code закрывает все три бреши, заимствуя мышцы, которые у тебя уже наработаны в доставке софта: контроль версий, код-ревью и автоматическое тестирование.

## Sigma: формат правил, который путешествует

Механизм, который делает это переносимым, — **Sigma**: универсальный сигнатурный формат для лог-детектов на основе YAML, слой «напиши один раз, нацель на многих» для мира SIEM. Ты описываешь *логику* детекта в вендоронезависимой схеме Sigma, а конвертер (эталонный инструмент — `sigma-cli` с бэкендами `pySigma`) компилирует это одно правило в язык запросов того бэкенда, который у тебя крутится: Splunk SPL, query DSL у Elastic, KQL-запрос Microsoft Sentinel и так далее. Пишешь логику один раз — даёшь тулчейну выдать диалект.

Сердце Sigma-правила — два поля. `detection.selection` перечисляет условия поле/значение, которые должны совпасть, а `condition` склеивает селекции булевой логикой. Вот правило реального вида для того самого алерта, что затопил нашего аналитика:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Поле Sigma</th>
        <th class="border border-border px-3 py-2 text-left">Пример значения</th>
        <th class="border border-border px-3 py-2 text-left">Что оно даёт тебе</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>title</code></td>
        <td class="border border-border px-3 py-2">Encoded PowerShell command line</td>
        <td class="border border-border px-3 py-2">Человеческое имя — то, что видно в алерте</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>logsource</code></td>
        <td class="border border-border px-3 py-2">product: windows, category: process_creation</td>
        <td class="border border-border px-3 py-2">К какому потоку логов применяется правило</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>detection.selection</code></td>
        <td class="border border-border px-3 py-2">Image endswith \powershell.exe; CommandLine contains '-enc'</td>
        <td class="border border-border px-3 py-2">Условия совпадения</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>detection.filter</code></td>
        <td class="border border-border px-3 py-2">CommandLine contains 'BackupAgent\\run.ps1'</td>
        <td class="border border-border px-3 py-2">Исключение тюнинга — в правиле, в Git</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>condition</code></td>
        <td class="border border-border px-3 py-2">selection and not filter</td>
        <td class="border border-border px-3 py-2">Булев клей — собственно логика</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>tags</code></td>
        <td class="border border-border px-3 py-2">attack.execution, attack.t1059.001</td>
        <td class="border border-border px-3 py-2">Маппинг на ATT&amp;CK — питает твою сетку покрытия</td>
      </tr>
    </tbody>
  </table>
</div>

Заметь: тюнинг живёт в `filter`, выраженный в `condition` как `selection and not filter`. Это то же самое исключение, что наш аналитик в 3 ночи сделал руками, — но теперь это строка в YAML-файле, в коммите, с сообщением о том, *почему* агент бэкапа исключён, доступная для ревью любому, кто позже задумается, не слишком ли широк этот вырез. Фикс не уехал в скрытую настройку консоли — он уехал в дифф.

## Жизненный цикл: детект — это софт

Важно это потому, что детекты никогда не бывают «готовы». Противник меняет флаг, вендор переименовывает поле лога, приложение начинает выдавать безобидный паттерн, похожий на вредоносный, — и правило должно меняться. Detection-as-code даёт этому изменению конвейер.

<FlowDiagram
  label="Жизненный цикл detection-as-code: аналитик пишет Sigma-правило с маппингом на технику ATT&CK, коммитит его в Git, CI конвертирует и прогоняет на образцах true-positive и негативных логов, коллега рецензирует pull request, правило деплоится в SIEM, а боевые false positive питают коммит тюнинга, который снова входит в тот же цикл."
  perRow={4}
  hue="var(--d-crypto)"
  nodes={[
    { id: "write", label: "Написать правило", sub: "Sigma + тег ATT&CK" },
    { id: "commit", label: "Коммит в Git", sub: "автор + сообщение" },
    { id: "ci", label: "Тесты в CI", sub: "TP горит / FP молчит" },
    { id: "review", label: "Ревью PR", sub: "вторая пара глаз" },
    { id: "deploy", label: "Деплой в SIEM", sub: "сконверченный запрос" },
    { id: "tune", label: "Тюнинг по FP", sub: "filter в диффе" },
  ]}
  edges={[
    { from: "write", to: "commit" },
    { from: "commit", to: "ci" },
    { from: "ci", to: "review", label: "зелёный" },
    { from: "review", to: "deploy", label: "одобрено" },
    { from: "deploy", to: "tune", label: "FP в проде" },
    { from: "tune", to: "ci", label: "цикл" },
  ]}
  caption="Детект движется как фича: написан, закоммичен, протестирован в CI, отрецензирован, задеплоен и перетюнен. Шаг тюнинга возвращается через тесты и ревью — поэтому фикс шума не может молча сломать true-positive случай."
/>

Шаг тестирования — несущий, и именно его команды пропускают первым. Каждое Sigma-правило должно ехать с фикстурами: хотя бы одно лог-событие, представляющее реальную технику атаки (утверждай, что оно горит), и набор заведомо безобидных событий, поверхностно похожих (утверждай, что они молчат). Когда аналитик в 3 ночи добавляет `filter: BackupAgent\run.ps1`, CI перепрогоняет вредоносную фикстуру и доказывает, что правило *всё ещё ловит атаку с encoded PowerShell* даже с новым вырезом. Инженер, который через шесть недель попытается удалить всё условие, увидит, как true-positive тест краснеет в pull request. Тест — это то, что превращает «сделай тихо» из ставки в безопасную операцию.

<Inset kind="why" lang="ru">
Почему false positive считается багом первого класса, а не просто раздражителем? Потому что усталость от алертов — сама по себе поверхность атаки. Аналитик, перед которым 4000 ложных тревог за ночь, не разбирает аккуратно 4001-ю — он рефлекторно сбрасывает очередь, и ровно тогда настоящая проскальзывает. Печально знаменитый пробой Target в 2013-м — канонический случай: вредонос *был* помечен, но алерт утонул в шуме и остался без действия. Так что доля false positive у детекта — не косметическая метрика; это разница между контролем, который работает, и тем, что обучил своих операторов его игнорировать. Тюнинг — не опциональная полировка, это поддержание контроля живым.
</Inset>

## Сигнал, который ты на самом деле тюнишь

Каждое изменение детекта — это сделка между двумя типами ошибок, и сказать, какой из них ты оптимизируешь, — и есть весь навык. **True positive** — это срабатывание правила на реальной вредоносной активности, то, что тебе нужно. **False positive** — это срабатывание на безобидной активности, то самое задание бэкапа. Затяни логику, чтобы убить false positive, — и рискуешь создать *false negative*: реальные атаки, которые теперь более узкое правило уже не ловит. Ослабь её, чтобы поймать больше вариантов, — и false positive растут, пока аналитик не отключится от тебя. Нет настройки, которая обнуляет оба; detection engineering — это практика осознанного выбора точки на этой кривой, для каждой техники, с записью выбора там, где его увидит следующий инженер.

<TradeoffMatrix
  id="04-detection-engineering-tradeoff-1"
  lessonSlug="04-detection-engineering"
  lang="ru"
  prompt="Детект «Encoded PowerShell» срабатывает 4000 раз за ночь, все — с одного хоста бэкапа, гоняющего известный скрипт. Нужно заставить его молчать, не ослепнув к атаке. Выбери зрелый ход."
  options={[
    { name: "Добавить узкий `filter` в Sigma-правило (`condition: selection and not filter`), исключающий путь этого скрипта, закоммитить с обоснованием и дать CI перепрогнать true-positive фикстуру", summary: "Тюнинг в диффе, доказать, что TP выживает.", correct: true, justification: "Это detection-as-code, работающий по задумке. Исключение узкое (один известный скрипт на одном хосте, а не весь PowerShell), оно живёт в Git с сообщением, почему, доступно для ревью, и CI перепрогоняет вредоносную фикстуру, доказывая, что правило всё ещё ловит реальную технику encoded PowerShell после выреза. Ты убил шум, не создав false negative, — и следующий инженер видит ровно, что исключено, и может откатить, если задание бэкапа когда-нибудь исчезнет." },
    { name: "Открыть консоль SIEM и добавить подавление по хосту для сервера бэкапа", summary: "Накликать исключение в коробке.", justification: "Это ровно тот провал в 3 ночи, ради предотвращения которого дисциплина и существует. Сегодня сработает, но нет коммита, нет обоснования, нет ревью и нет теста — поэтому никто потом не ответит, что подавлено и зачем, а подавление может молча сгнить в слепую зону, когда хост перепрофилируют. Это недокументированная настройка в коробке, а не инженерное изменение." },
    { name: "Удалить условие `-enc` из правила, чтобы оно перестало совпадать с шумным паттерном", summary: "Убрать шумное условие.", justification: "Так алерты прекратятся за счёт того, что правило перестанет детектить технику. Encoded командные строки — ключевой сигнал обфускации; удаление этого условия создаёт широкий false negative — атака, ради которой правило строилось, теперь проходит незамеченной. True-positive фикстура здесь покраснела бы, ровно поэтому тесты обязательны." },
    { name: "Поднять порог алерта так, чтобы он срабатывал только после 5000 событий в час", summary: "Завалить порогом.", justification: "Порог по объёму, настроенный на уровень шума, означает, что медленный и тихий атакующий, гоняющий технику несколько раз, спокойно сидит под планкой. Ты оптимизировал безобидный высокообъёмный случай и ослеп к реалистичному противнику, который тих по замыслу. Тюнинг порога адресует объём симптома, а не различие безобидное-против-вредоносного, которое делает filter." },
  ]}
/>

<Quiz
  id="04-detection-engineering-quiz-1"
  lessonSlug="04-detection-engineering"
  lang="ru"
  question="Почему detection-as-code держит исключение тюнинга внутри `filter` Sigma-правила, а не как подавление в консоли SIEM?"
  choices={[
    { label: "Чтобы вырез был версионирован, отрецензирован и перетестирован — виден в диффе с обоснованием, а не невидимая настройка консоли", correct: true },
    { label: "Потому что консоли SIEM вообще не умеют подавлять алерты", misconception: "Консоли совершенно точно умеют подавлять — это и есть проблема, а не ограничение. Суть в том, что у подавления в консоли нет истории, ревью и теста, а у `filter` в правиле — есть." },
    { label: "Потому что Sigma-фильтры работают быстрее, чем подавления в консоли", misconception: "Производительность тут ни при чём. После конвертации оба становятся логикой запроса бэкенда; ценность подхода filter-в-правиле — аудируемость и тестирование, а не скорость." },
    { label: "Потому что фильтры шифруются, а настройки консоли — нет", misconception: "Никакого шифрования тут нет. Выгода в том, что исключение живёт в контроле версий, где его можно отрецензировать и протестировать, а не в чём-либо про секретность." },
  ]}
/>

<Quiz
  id="04-detection-engineering-quiz-2"
  lessonSlug="04-detection-engineering"
  lang="ru"
  question="Инженер затянул правило, чтобы заглушить false positive, и сработало — алерты упали почти до нуля. О чём первым делом беспокоится зрелый специалист?"
  choices={[
    { label: "Что более узкая логика внесла false negative — реальные атаки, которые суженное правило больше не ловит, — что должна проверить true-positive фикстура", correct: true },
    { label: "Что правило теперь слишком медленно работает по объёму логов", misconception: "Производительность может иметь значение, но ключевой риск затягивания ради FP — ослепнуть к реальным атакам (false negative). Это и есть та сделка, вокруг которой построена дисциплина, и то, что ловит TP-фикстура." },
    { label: "Ни о чём — меньше алертов однозначно и есть цель", misconception: "Меньше алертов может означать и более здоровое правило, и слепое. «Тихо» хорошо только если правило всё ещё горит на реальной технике; иначе ты обменял шум на false negative." },
    { label: "Что лицензия SIEM тарифицируется за правило и изменение добавляет стоимость", misconception: "Лицензирование тут не инженерная забота. Существенный риск в том, что убийство false positive создало false negative, что проверяется перепрогоном вредоносной фикстуры." },
  ]}
/>

<DragOrder
  id="04-detection-engineering-drag-1"
  lessonSlug="04-detection-engineering"
  lang="ru"
  prompt="Упорядочь жизненный цикл detection-as-code для нового правила, от написания до боевого тюнинга:"
  items={[
    { id: "a", label: "Написать Sigma-правило и пометить его техникой ATT&CK" },
    { id: "b", label: "Закоммитить в Git с автором и сообщением-обоснованием" },
    { id: "c", label: "CI конвертирует правило и прогоняет на TP- и негативных фикстурах" },
    { id: "d", label: "Коллега рецензирует и одобряет pull request" },
    { id: "e", label: "Задеплоить сконверченный запрос в SIEM; поздние FP тюнить через тот же цикл" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Detection engineering обращается с детектами как с кодом: Sigma-правила в Git, размеченные на ATT&CK, протестированные на true-positive и безобидных фикстурах, отрецензированные в PR. Тюнинг false positive становится узким `filter` в диффе — аудируемым и перетестированным, — а не забытым кликом в консоли, который может молча ослепнуть к атаке.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-detection-engineering-retrieval"
  lang="ru"
  questions={[
    { q: "Объясни detection-as-code и почему он лучше, чем накликивание правил в консоли SIEM.", a: "Detection-as-code обращается с каждым детектом так, как ты обращаешься с прикладным кодом: правило живёт в Git-репозитории, а не в консоли вендора; у него есть автор, сообщение коммита и рецензент на pull request; оно едет через CI; и оно несёт тесты — образцы лог-событий, которые должны его поднять (true positive), и безобидные события, которые должны молчать (негативы), — прогоняемые на каждое изменение. Модель накликивания в консоли проваливается по тем же причинам, что и ручная правка прода через GUI базы: нет истории (не ответишь, кто и зачем менял правило), нет ревью (исключение усталого аналитика в 3 ночи едет невидимым для остальных) и нет теста (правка одного символа может тихо перестать ловить атаку, и никто не узнает до пробоя). Detection-as-code закрывает все три бреши, переиспользуя мышцы доставки софта, которые у команды уже есть, — контроль версий, код-ревью и автотесты, — так что фикс шумного правила становится аудируемым диффом, а не забытой настройкой в коробке." },
    { q: "Что такое Sigma, каковы его ключевые поля и как сделка false positive / true positive формирует тюнинг правила?", a: "Sigma — это универсальный, основанный на YAML, вендоронезависимый сигнатурный формат для лог-детектов: ты пишешь логику детекта один раз, а конвертер (sigma-cli с бэкендами pySigma) компилирует её в язык запросов каждого SIEM — Splunk SPL, Elastic DSL, Sentinel KQL. Его ключевые поля: logsource (к какому потоку логов применяться), detection.selection (условия совпадения), опциональный detection.filter (вырезы тюнинга), condition (булев клей, например 'selection and not filter') и tags (маппинг на ATT&CK, питающий сетку покрытия). Тюнинг — осознанная сделка между двумя типами ошибок: true positive — срабатывание на реальной вредоносной активности (то, что нужно); false positive — срабатывание на безобидной (задание бэкапа). Затяни логику, чтобы убить false positive, — рискуешь false negative: реальные атаки, которые более узкое правило уже не ловит; ослабь ради покрытия — false positive растут, пока аналитик не отключится от правила целиком (усталость от алертов, сама поверхность атаки, как в пробое Target 2013-го, где настоящий алерт утонул в шуме). Поэтому исключение кладут в filter правила, узко ограниченным, закоммиченным с обоснованием, и дают CI перепрогнать true-positive фикстуру, доказывая, что правило всё ещё ловит атаку после выреза, — выбирая точку на кривой FP/FN намеренно и записывая её там, где увидит следующий инженер." },
  ]}
/>

<Recap lang="ru">
Detection engineering — это дисциплина построения, изменения и доверия детектам годами, не давая набору правил сгнить в шум, и её ядро — detection-as-code: правила живут в Git, а не в консоли SIEM, с автором, сообщением коммита, ревью коллеги, CI и тестами. Sigma — вендоронезависимый YAML-формат, делающий это переносимым: пишешь логику один раз (logsource, selection, опциональный filter, condition, теги ATT&CK), а конвертер выдаёт запросы Splunk, Elastic или Sentinel. Тюнинг false positive становится узко ограниченным `filter`, выраженным как `selection and not filter`, закоммиченным с обоснованием и перепроверенным CI на true-positive фикстуре, — так что убийство шума никогда не может молча создать false negative. Всё держится на отношении к false positive как к багу первого класса, потому что усталость от алертов — поверхность атаки: правило, затопляющее операторов, обучило их игнорировать тот единственный алерт, что имел значение. Так что в следующий раз перед шумным детектом твой рефлекс уже не «накликать исключение в коробке» — это «ограничить filter, написать обоснование и доказать тестом, что атака всё ещё горит».
</Recap>