---
concepts:
- sbom
- software-supply-chain
- dependency-risk
- provenance
- slsa
deepensInto: []
estMin: 15
lang: ru
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-sbom-and-supply-chain
sources:
- https://csrc.nist.gov/pubs/sp/800/40/r4/final
- https://www.first.org/cvss/
spiral: []
status: ready
summary: 'Выходит новый CVE в зависимости, и первый вопрос — «затронуты ли мы?». SBOM — поштучная опись всего, что вы поставляете в сборке, — позволяет ответить за минуты, а не за дни. Provenance и SLSA добавляют недостающую половину: доверие к этой описи.'
title: 'SBOM и цепочка поставок'
track: security-defensive
unit: 04-secops-and-governance
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Декабрь 2021-го, поздний вечер: выходит CVE на Log4j 2 — `CVE-2021-44228`, удалённое выполнение кода через одну подделанную строку лога, оценка 10.0. Все security-каналы взрываются. Технический директор задаёт единственный важный вопрос: *затронуты ли мы?* И в комнате повисает тишина. Никто не может ответить. Log4j нет в верхнем уровне ни `package.json`, ни `pom.xml` — это транзитивная зависимость на четыре слоя вглубь, которую притянул логирующий адаптер, понадобившийся фреймворку. Она может быть в трёх сервисах, а может в тридцати. Команды, ответившие за двадцать минут, имели Software Bill of Materials — запрашиваемый, попсборочный список каждого компонента, который они поставляли. Команды, ответившие за девять дней, руками грепали репозитории исходников. Тот же баг, тот же патч — разница была целиком в том, знали ли они, что у них запущено.
</Hook>

К концу этого урока ты будешь знать, что на самом деле содержит SBOM, почему на вопрос «затронуты ли мы?» нельзя ответить без неё и почему знать свои зависимости — лишь половина работы: вторая половина — доверять им.

<Crux>SBOM — поштучная опись всего, что вы поставляете в сборке; без неё на свежий CVE нельзя даже ответить «затронуты ли мы?».</Crux>

<Explanation>

## Что такое SBOM на самом деле

Software Bill of Materials (опись состава ПО) — это формальная, машиночитаемая опись каждого компонента в программном продукте: прямых зависимостей, транзитивных зависимостей и часто OS-пакетов, запечённых в образ контейнера. Ментальная модель — это этикетка состава на продукте питания: не маркетинговый текст, а буквальный список того, что внутри, в формате, который машина может разобрать, а регулятор — проверить. На практике доминируют два стандарта: **SPDX** (стандарт ISO, `ISO/IEC 5962`) и **CycloneDX** (от OWASP, спроектированный с приоритетом безопасности). Оба выражают одни и те же ключевые факты — имя компонента, версию и уникальный идентификатор под названием **purl** (package URL, например `pkg:maven/org.apache.logging.log4j/log4j-core@2.14.1`) — плюс криптографический хеш, чтобы можно было доказать, что артефакт не подменили.

Единственное свойство, ради которого SBOM стоит генерировать, — то, что она захватывает **транзитивный** граф, а не только верхний уровень. Когда ты делаешь `npm install express`, ты притягиваешь десятки пакетов, которые никогда не называл; дерево зависимостей реального приложения регулярно доходит до нескольких сотен или тысячи узлов. Уязвимости, которые бьют, почти всегда живут в этом погребённом большинстве — Log4j была на четыре уровня вглубь, — а это ровно там, где человек, читающий `package.json`, их не видит. SBOM сплющивает весь этот граф в список, который можно запросить: *«встречается ли где-нибудь в этой сборке хоть одна версия `log4j-core`?»* превращается из многодневных раскопок в однострочный запрос.

Неочевидное правило: SBOM **генерируется на этапе сборки, для каждого артефакта**, а не пишется руками. Список зависимостей, поддерживаемый вручную, устаревает в тот момент, когда кто-то поднимает версию в lock-файле, а устаревший — хуже, чем никакой, потому что он уверенно лжёт. SBOM — это выход сборки, привязанный к конкретному digest образа, поэтому он описывает ровно те байты, которые ты задеплоил, а не то, что обещает README.

<FlowDiagram
  label="Жизненный цикл SBOM: сборка генерирует SBOM для каждого артефакта, она хранится рядом с digest образа, сканер уязвимостей непрерывно сопоставляет purl из SBOM с новыми CVE-фидами, и когда выходит свежий CVE, ты запрашиваешь «затронуты ли мы?» за минуты, а не грепаешь репозитории руками дни."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "build", label: "Сборка", sub: "компиляция + резолв зависимостей" },
    { id: "gen", label: "Генерация SBOM", sub: "на артефакт, все транзитивные" },
    { id: "store", label: "Хранение", sub: "привязка к digest образа" },
    { id: "scan", label: "Непрерывный скан", sub: "purl против CVE-фида" },
    { id: "answer", label: "Затронуты ли мы?", sub: "минуты, не дни" },
  ]}
  edges={[
    { from: "build", to: "gen" },
    { from: "gen", to: "store", label: "машиночитаемо" },
    { from: "store", to: "scan", label: "запрашиваемо" },
    { from: "scan", to: "answer", label: "новый CVE" },
  ]}
  caption="SBOM окупается только тогда, когда генерируется на сборке, хранится с артефактом и непрерывно пересканируется против свежих CVE-фидов — статичный документ, подшитый один раз и забытый, через полгода ни на что не отвечает."
/>

## Почему «затронуты ли мы?» — это весь смысл

SBOM не имеет ценности, лёжа в бакете. Её ценность реализуется, когда выходит новая уязвимость и ты подаёшь SBOM в сканер, который сопоставляет purl каждого компонента с CVE-фидом. Это разница между **описью** и **обнаружением**: опись говорит, что у тебя есть; скан говорит, что из этого теперь известно как опасное. Поскольку новые CVE публикуются каждый день против компонентов, которые ты заморозил месяцы назад, сопоставление должно быть **непрерывным** — уязвимость, которая была чистой на момент сборки, становится критической в день выхода своего CVE, а сохранённая SBOM — это то, что позволяет дотянуться назад и найти её, ничего не пересобирая и не перегрепывая.

Здесь же входит **приоритизация**, потому что реальный скан против дерева в тысячу узлов возвращает десятки попаданий, а пропатчить их все за неделю нельзя. CVSS — Common Vulnerability Scoring System — даёт каждому CVE **базовый балл** 0–10, вендоронезависимую серьёзность, сравнимую между командами. Но базовый балл намеренно худший-случай и без контекста: он предполагает, что уязвимый код достижим, а актив открыт. Зрелый ход — наложить **средовые и временные** метрики CVSS — *действительно ли этот компонент на достижимом пути? обращён ли актив в интернет? существует ли эксплойт в дикой природе?* — поверх базового балла. CVE с базовым 9.8 в dev-only сборочном инструменте, который ты никогда не поставляешь, — это меньший реальный риск, чем 7.5 в твоём обращённом в интернет сервисе аутентификации. SBOM говорит, *где* находится компонент, а это ровно тот контекст, что превращает сырой балл в реальный приоритет.

<Inset kind="why" lang="ru">
Почему грепать репозитории «недостаточно хорошо» вместо генерации SBOM? Потому что исходник ≠ артефакт. Репозиторий показывает, что ты *написал*; SBOM показывает, что ты *поставил*. Транзитивная зависимость, разрешённая твоим lock-файлом, OS-пакет базового образа, вендоренный бинарник, версия, по-разному запиненная в CI и локально, — ничего из этого не видно из `grep` по дереву исходников, и всё это уезжает в контейнер. Команды Log4j, которые грепали, нашли свои прямые использования и упустили адаптер фреймворка, который притянул её транзитивно. SBOM генерируется из разрешённой сборки, поэтому видит байты, а не намерения.
</Inset>

## Вторая половина: provenance и доверие

Знать, *что* ты поставляешь, — лишь половина проблемы цепочки поставок. Вторая половина — доверять, что компонент, который ты притянул, — это тот, который его автор действительно опубликовал, и этот вопрос стал острым после атак вроде **SolarWinds** (2020), где вредоносный код внедрили в *сборочный конвейер*, так что подписанный официальный артефакт сам оказался отравлен, и **Codecov** и захвата npm-пакета `event-stream`, где легитимный пакет перехватили и выкатили вредоносную версию. Во всех случаях имя зависимости и версия выглядели верно. SBOM сказала бы тебе, что ты запускаешь `этот` пакет; она не сказала бы, что пакет подменили выше по течению. Этот зазор закрывает **provenance**.

Provenance — это проверяемые, защищённые от подделки метаданные, отвечающие на вопрос *откуда взялся этот артефакт, из какого коммита исходника, собран каким конвейером?* — криптографически подписанные, чтобы их нельзя было подделать задним числом. **SLSA** (Supply-chain Levels for Software Artifacts, читается «сальса») — это фреймворк, который оценивает, насколько этому provenance можно доверять, по уровням: грубо, L1 означает, что provenance существует, L2 — что он подписан сборочным сервисом, а L3 — что сборка прошла в укреплённом, изолированном окружении, которое производитель не может подделать. Цель — комбинированная позиция: **SBOM**, чтобы знать свои компоненты, **provenance**, чтобы доверять их подлинности, и **SLA на патчинг** — вроде сроков, предписанных NIST SP 800-40, — чтобы заведомо плохой компонент чинился по часам, а не когда кто-нибудь заметит.

<TradeoffMatrix
  id="04-sbom-supply-chain-tradeoff-1"
  lessonSlug="02-sbom-and-supply-chain"
  lang="ru"
  prompt="Только что вышел критический CVE на удалённое выполнение кода в популярной логирующей библиотеке. Технический директор спрашивает: «Затронуты ли мы и где?» Какая способность реально отвечает на это за минуты?"
  options={[
    { name: "Непрерывно сканируемая SBOM, сгенерированная на каждой сборке, где purl каждого компонента сопоставляется с живым CVE-фидом", summary: "Запросить опись.", correct: true, justification: "Это ровно то, для чего нужна SBOM. Поскольку она захватывает полный транзитивный граф на артефакт и пересканируется против свежих CVE-фидов, вопрос становится однострочным запросом: встречается ли где-нибудь purl этого компонента любой версии в любой задеплоенной сборке и на каких сервисах? Ты получаешь точный, полный ответ за минуты — включая транзитивные использования на четыре уровня вглубь, которые человек никогда не заметит." },
    { name: "Грепать каждый репозиторий исходников по имени библиотеки и строке версии", summary: "Искать в исходнике.", justification: "Исходник — не артефакт. Grep находит твои прямые, написанные использования и упускает транзитивные зависимости, разрешённые lock-файлом, OS-пакеты базового образа и вендоренные бинарники — а именно там обычно прячется опасная копия. Это медленный, неполный путь, занявший у команд дни во время Log4j и всё равно упустивший экземпляры." },
    { name: "Проверить верхний уровень package.json / pom.xml каждого сервиса на библиотеку", summary: "Прочитать манифест.", justification: "Манифест верхнего уровня перечисляет только прямые зависимости. Компоненты, которые бьют, почти всегда транзитивны — Log4j притянул адаптер фреймворка на четыре слоя вглубь, — поэтому чтение манифеста даёт уверенный ответ, который неверен везде, где зависимость была косвенной." },
    { name: "Добавить криптографическую подпись (provenance/SLSA) в сборочный конвейер", summary: "Подписать сборки.", justification: "Provenance необходим, но отвечает на другой вопрос — «подлинен ли и не подменён ли этот артефакт?», — а не «запускаем ли мы уязвимую версию и где?». Подпись не перечислит, какие из твоих сервисов содержат CVE. SBOM — для описи и обнаружения; provenance — для доверия. Нужны оба, но на «затронуты ли мы?» отвечает только один." },
  ]}
/>

<Quiz
  id="04-sbom-supply-chain-quiz-1"
  lessonSlug="02-sbom-and-supply-chain"
  lang="ru"
  question="Почему SBOM нужно генерировать на сборке для каждого артефакта, а не поддерживать как написанный вручную документ?"
  choices={[
    { label: "Список, поддерживаемый вручную, устаревает в момент изменения lock-файла и описывает намерения, а не реально поставленные байты — SBOM со сборки привязана к настоящему digest образа", correct: true },
    { label: "SBOM, написанные вручную, не могут использовать формат SPDX или CycloneDX", misconception: "В принципе SPDX или CycloneDX можно написать руками; проблема не в формате, а в том, что опись, поддерживаемая вручную, рассинхронизируется с реально разрешённой сборкой и уверенно лжёт." },
    { label: "Регуляторы принимают только SBOM, произведённые CI-сервером", misconception: "Драйвер — точность, а не то, кто подписал. SBOM вручную проваливается, потому что не отслеживает транзитивное разрешение и пакеты базового образа, а не из-за регуляторного правила о производителе." },
    { label: "Генерация на сборке делает SBOM меньше и быстрее в сканировании", misconception: "Размер ни при чём — SBOM со сборки обычно больше, потому что захватывает полный транзитивный граф. Дело в верности тому, что реально поставлено, а не в размере файла." },
  ]}
/>

<Quiz
  id="04-sbom-supply-chain-quiz-2"
  lessonSlug="02-sbom-and-supply-chain"
  lang="ru"
  question="Атакующий компрометирует сборочный конвейер и поставляет отравленную версию официального, корректно названного пакета (в стиле SolarWinds). Какой контроль поймал бы это, а какой — нет?"
  choices={[
    { label: "Provenance/SLSA поймал бы (артефакт не совпал бы с доверенной подписанной сборкой); одна SBOM — нет, ведь имя и версия всё равно выглядят верно", correct: true },
    { label: "Одна SBOM ловит это, потому что компонент появился бы как новая запись", misconception: "SBOM перечислила бы ровно то имя и версию, которые задумал атакующий, — выглядит легитимно. SBOM отвечает «что мы запускаем?», а не «подменили ли это выше по течению?». Вопрос доверия — работа provenance." },
    { label: "CVSS-оценка поймала бы это, пометив высокую серьёзность", misconception: "CVSS оценивает известные, опубликованные CVE. У свежего внедрения в цепочку поставок ещё нет CVE, поэтому нечего оценивать; это проблема provenance/целостности, а не оценки серьёзности." },
    { label: "Ни тот, ни другой — отравленная официальная сборка по определению необнаружима", misconception: "Это ровно то, для чего существуют provenance и SLSA: проверяемые, подписанные метаданные, доказывающие, что артефакт пришёл из конкретного коммита и укреплённой сборки, так что версия с внедрением через конвейер проваливает проверку." },
  ]}
/>

<DragOrder
  id="04-sbom-supply-chain-drag-1"
  lessonSlug="02-sbom-and-supply-chain"
  lang="ru"
  prompt="Упорядочь рабочий процесс цепочки поставок от сборки до ответа на «затронуты ли мы?»:"
  items={[
    { id: "a", label: "Сборка разрешает все зависимости (прямые + транзитивные + базовый образ)" },
    { id: "b", label: "Сгенерировать SBOM на артефакт, привязанную к digest образа" },
    { id: "c", label: "Сохранить SBOM рядом с задеплоенным артефактом" },
    { id: "d", label: "Непрерывно сопоставлять каждый purl со свежими CVE-фидами" },
    { id: "e", label: "На новый CVE запросить SBOM и приоритизировать попадания по CVSS + достижимости" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>SBOM — это попсборочная, машиночитаемая опись каждого компонента, который ты поставляешь, включая транзитивные, и именно она превращает «затронуты ли мы?» из многодневного grep в однострочный запрос. Но опись — не доверие: дополни её provenance/SLSA, чтобы доказать подлинность компонентов, и SLA на патчинг, чтобы чинить плохие по часам.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-sbom-supply-chain-retrieval"
  lang="ru"
  questions={[
    { q: "Что именно содержит SBOM и почему важно генерировать её на сборке для каждого артефакта?", a: "Software Bill of Materials — это формальная, машиночитаемая опись каждого компонента в сборке: прямых зависимостей, транзитивных зависимостей и обычно OS-пакетов в образе контейнера. Каждая запись несёт имя, версию, уникальный идентификатор (purl / package URL, например pkg:maven/org.apache.logging.log4j/log4j-core@2.14.1) и криптографический хеш, чтобы артефакт нельзя было молча подменить. Два доминирующих формата — SPDX (стандарт ISO) и CycloneDX (OWASP, с приоритетом безопасности). Свойство, ради которого её стоит генерировать, — то, что она сплющивает полный транзитивный граф — регулярно доходящий до сотен или тысячи узлов — в список, который можно запросить, так что погребённая зависимость на четыре уровня вглубь (где жила Log4j) становится видимой. Её нужно генерировать на сборке и привязывать к конкретному digest образа, а не писать руками: список, поддерживаемый вручную, устаревает в момент изменения lock-файла и описывает намерения, а не реально поставленные байты, а устаревшая опись хуже, чем никакая, потому что уверенно лжёт." },
    { q: "Знать свои зависимости — лишь половина проблемы цепочки поставок; в чём вторая половина и что её закрывает?", a: "Вторая половина — доверие: знать, что ты запускаешь пакет с именем X версии Y, не говорит, что артефакт — тот, который его автор действительно опубликовал. Атаки вроде SolarWinds (вредоносный код внедрён в сборочный конвейер, так что подписанный официальный артефакт оказался отравлен), Codecov и захвата npm-пакета event-stream — все поставляли корректно выглядящие имена и версии — SBOM подтвердила бы, что ты запускаешь этот пакет, но не то, что его подменили выше по течению. Provenance закрывает этот зазор: проверяемые, криптографически подписанные, защищённые от подделки метаданные, отвечающие, откуда взялся артефакт, из какого коммита исходника, собран каким конвейером. SLSA (Supply-chain Levels for Software Artifacts) оценивает, насколько этому provenance можно доверять — грубо L1 provenance существует, L2 он подписан сборочным сервисом, L3 он прошёл в укреплённой, изолированной сборке, которую производитель не может подделать. Полная позиция — SBOM (знать компоненты) + provenance/SLSA (доверять подлинности) + SLA на патчинг (чинить заведомо плохие компоненты по часам, например по срокам NIST SP 800-40), и CVSS плюс достижимость, чтобы приоритизировать, какие CVE патчить первыми." },
  ]}
/>

<Recap lang="ru">
SBOM — это формальная, машиночитаемая опись каждого компонента, который ты поставляешь, — прямого, транзитивного и из базового образа, — выраженная в SPDX или CycloneDX с purl и хешем на компонент. Весь смысл её существования — вопрос, который навязывает свежий CVE: «затронуты ли мы и где?». Поскольку опасные компоненты почти всегда погребены глубоко в транзитивном графе (Log4j была на четыре уровня вглубь), чтение манифеста или grep по исходнику даёт уверенный-но-неверный ответ, тогда как SBOM со сборки, непрерывно сканируемая против CVE-фидов, отвечает на него за минуты — а дальше базовые баллы CVSS плюс контекст достижимости и открытости превращают десятки попаданий в реальный приоритет патчинга. Но опись — не доверие: атаки в стиле SolarWinds отравляют корректно названные официальные артефакты, что одна SBOM поймать не может, поэтому ты дополняешь её provenance и SLSA, чтобы доказать подлинность компонентов, и SLA на патчинг, чтобы чинить плохие по часам. Теперь, когда выходит критический CVE в зависимости, твой первый ход — не грепать, а запросить SBOM и спросить, какое из попаданий реально достижимо и открыто.
</Recap>
