---
concepts:
- python-performance-limits
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: senior
order: 4
prereqs: []
slug: "04-when-python-is-wrong"
sources:
- https://docs.python.org/3/library/asyncio.html
- https://peps.python.org/pep-0703/
- https://docs.python.org/3/howto/free-threading-python.html
spiral: []
status: ready
summary: 'Triage the bottleneck: I/O-bound and C-library-bound workloads keep Python; interpreter-bound hot loops at scale cost 10-50x per core. Free-threaded 3.13 changes thread math, not single-thread speed. Strangler-extract the hot path; keep Python where dev speed beats compute.'
title: 'When Python is the wrong tool: the decision tree and the salary-versus-servers arithmetic'
track: "python"
unit: "08-performance"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import rewriteArithmetic from "./infographics/rewrite-arithmetic.svg?raw";

<Hook>
The feature-scoring endpoint burned 35 ms of pure-Python CPU per request. At 4 000 RPS that meant twelve 8-core pods doing nothing but interpreting the same loop. One engineer rewrote that single endpoint in Go — same algorithm, line for line: 1.1 ms per request, and the fleet shrank from twelve pods to two. Compute bill down $14k a month. Leadership's next move was predictable: an RFC titled "Migrate all services to Go". The audit killed it in one slide — the other thirty Python services *combined* used fewer cores than the old scorer alone; they were CRUD glue, orchestration, and batch jobs idling on I/O, where Go would have bought nothing but a year of rewriting. The scorer was the bill. The rest were never the bill. The senior skill is not picking a language — it is locating the bill before proposing the invoice.
</Hook>

<Crux>I/O-bound or C-library-bound: Python is fine. Interpreter-bound at scale costs 10-50x per core — extract that path, keep the glue.</Crux>

<Explanation>

## Classify the bottleneck before judging the language

"Python is slow" is not a diagnosis. The diagnosis comes from lesson 1 — py-spy against the real workload — and lands in one of three buckets:

- **I/O-bound** — samples sit in socket waits, the CPU is mostly idle. Python is fine: an idle core is idle in every language. The levers are asyncio, connection pooling, and concurrency, not a rewrite. A Go port of a service that waits on Postgres still waits on Postgres.
- **C-library-bound** — frames live inside numpy, torch, the DB driver. Python is fine: it is already a thin orchestrator over compiled kernels (lesson 3's whole point). The levers are batching and feeding the C better, and a rewrite re-implements glue around the same kernels.
- **Interpreter-bound** — pure-Python frames are hot, at scale. Now the 10-50x per-core gap is real money. Escalate the ladder: algorithm first, vectorize or extend (lesson 3), and only when the path stays hot and stays the bill — rewrite *that path*.

## Free-threaded 3.13 changes the thread math, not the speed

The free-threaded build (PEP 703) makes the GIL optional: pure-Python threads can finally run in parallel. What it does **not** change is single-thread speed — early free-threaded builds actually pay a single-digit-percent overhead, and the specializing interpreter took time to return. So it moves one boundary: "use processes or released-GIL C for CPU parallelism" relaxes toward threads. The per-core interpretive gap that drives the cases below survives intact: forty slow cores in parallel are still forty slow cores.

## Where Python is the wrong tool

Three honest cases, each anchored in a number from this unit:

1. **Sustained CPU-bound services at scale.** 10-50x slower per core means 10-50x the fleet for the same throughput. On 2 cores nobody cares; at the Hook's twelve pods — or at hundreds of cores — the interpreter *is* the infrastructure bill.
2. **Hard-latency paths.** Generational GC pauses (a gen-2 pass over a big heap is milliseconds), allocator jitter, and interpreter variance fight a single-digit-millisecond p99 SLO. You can mitigate — `gc.freeze`, tuning thresholds — but you are negotiating with the platform, and the SLO does not negotiate.
3. **Memory-constrained, high-cardinality data.** Lesson 2's arithmetic: 28-byte ints and 250-byte objects put 10 M small records into gigabytes that Go or Rust hold in hundreds of megabytes. If the working set must fit in RAM per node, object overhead picks the language.

All three cases share the same shape: the Python overhead is real, measurable, and *already the bill* — not a theoretical concern. Without case 1's sustained core count, case 2's hard latency target, or case 3's cardinality-driven RAM cliff, the arithmetic almost certainly rejects the rewrite before you finish the RFC.

<Quiz
  id="04-when-python-is-wrong-quiz-1"
  lessonSlug="04-when-python-is-wrong"
  lang="en"
  question="Which of these services is Python actually the wrong tool for?"
  choices={[
    { label: "A pricing engine running a pure-Python scoring loop on 300 sustained cores — the 10-50x per-core gap multiplies directly into fleet cost", correct: true },
    { label: "An API gateway whose py-spy profile is 95% socket waits", misconception: "That service is I/O-bound: its cores are idle, and an idle core is idle in Go too. The rewrite would reproduce the same waiting with a different syntax; asyncio and pooling are the levers." },
    { label: "An ETL job spending its time inside pandas groupby and parquet I/O", misconception: "That time is already inside compiled C — Python is a thin orchestrator there. Rewriting the orchestration re-implements glue around the same kernels for roughly zero per-core gain." },
    { label: "Any service handling more than 1 000 requests per second", misconception: "RPS alone says nothing: 1 000 RPS of I/O-bound proxying runs happily on a pod or two of Python. The deciding variables are where the samples land (interpreter vs I/O vs C) and the core count at stake." },
  ]}
/>

## Where Python stays right: the salary-versus-servers arithmetic

Glue, orchestration, internal tools, data science, ETL control planes — domains where iteration speed dominates compute. Spell the arithmetic out, because it decides real RFCs. A loaded senior engineer-year is $150-300k. A cloud vCPU-year is roughly $200-600. A Python service wasting 10x on 4 cores burns maybe $10-20k a year — less than one engineer-*month*. A rewrite must displace more core-dollars than it costs in engineer-dollars, *forever*, maintenance included. Most services never clear that bar. The Hook's scorer cleared it more than tenfold — 240 wasted cores — which is exactly why it was the one service worth moving.

<Infographic
  svg={rewriteArithmetic}
  label="Side-by-side comparison of a 4-core and a 300-core interpreter-bound service, each 10x slower per core: the 4-core service wastes ~3.6 cores (~$1.4k/yr) and the rewrite (~$25-50k) never pays back, while the 300-core service wastes ~270 cores (~$108k/yr) and the same rewrite pays back in under a year."
  caption="Identical 10x inefficiency, identical rewrite cost — yet the verdict flips, because it is absolute core-dollars displaced, not the percentage, that must exceed the engineer-dollars spent."
  hue="var(--d-infra)"
/>

<Quiz
  id="04-when-python-is-wrong-quiz-2"
  lessonSlug="04-when-python-is-wrong"
  lang="en"
  question="A Python service runs on 4 vCPUs (~$1.6k/yr at $400/vCPU-yr). An RFC proposes a 2 engineer-month Go rewrite promising 10x per-core efficiency. Verdict?"
  choices={[
    { label: "Reject: the rewrite costs ~$25-50k of engineer time to save at most ~$1.4k/yr of compute — it never pays back, plus permanent dual-stack maintenance", correct: true },
    { label: "Approve: 10x efficiency is 10x cost savings, which always compounds", misconception: "10x of a small number is still small: the entire waste is ~$1.4k/yr. The percentage is irrelevant; only absolute core-dollars displaced versus engineer-dollars spent decides — and here the ratio is ~20:1 against." },
    { label: "Approve if the team is excited — morale and learning offset the cost", misconception: "Learning has value, but the RFC claims an efficiency justification, and the arithmetic refutes it. Funding rewrites on vibes is how 30-service migration mandates get written; fund learning explicitly instead." },
    { label: "Reject because Go could not actually deliver 10x on this workload", misconception: "On an interpreter-bound loop 10x is entirely plausible (the Hook saw ~30x). The proposal fails on economics at this scale, not feasibility — at 300 cores the same numbers approve it." },
  ]}
/>

## Migration pattern: strangler, not big bang

When a path does clear the bar, extract it — do not rewrite the world. Put the measured hot path behind a contract (protobuf or OpenAPI), implement that one service in Go or Rust, shadow traffic against it, shift percentages, keep the Python orchestration around it. Big-bang rewrites stall because the 90% of code that was never the bill must still be re-implemented bug-for-bug, while the strangler ships the 10% that pays. Each subsequent extraction is justified by its own core math — and in the Hook's system, after the scorer moved, *no other service ever qualified*.

</Explanation>

<FlowDiagram
  label="The honest decision tree: classify the bottleneck, then escalate only the path that is the bill"
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "prof", label: "py-spy the hot path", sub: "where do samples land?" },
    { id: "io", label: "I/O-bound", sub: "socket waits dominate" },
    { id: "clib", label: "C-library-bound", sub: "numpy/torch/driver frames" },
    { id: "interp", label: "Interpreter-bound", sub: "pure-Py frames hot" },
    { id: "stayio", label: "Stay Python", sub: "asyncio, pooling, workers" },
    { id: "stayc", label: "Stay Python", sub: "batch, feed the C better" },
    { id: "ladder", label: "Climb the ladder", sub: "algo, numpy, Cython" },
    { id: "extract", label: "Extract to Go/Rust", sub: "strangler behind a contract" },
  ]}
  edges={[
    { from: "prof", to: "io", label: "waiting" },
    { from: "prof", to: "clib", label: "in C already" },
    { from: "prof", to: "interp", label: "interpreting" },
    { from: "io", to: "stayio", label: "idle is idle" },
    { from: "clib", to: "stayc", label: "glue rewrite = 0x" },
    { from: "interp", to: "ladder", label: "lesson 3 first" },
    { from: "ladder", to: "extract", label: "still the bill" },
  ]}
  caption="Two of three branches end at 'stay Python' because the cores are idle or already running C. Only the interpreter-bound branch escalates — and rewrite is its last step, taken per-path when the salary-versus-servers arithmetic clears, never as a fleet mandate."
/>

<KeyTakeaway>Classify before judging: I/O-bound keeps Python (idle cores are idle in any language), C-library-bound keeps Python (it is already orchestrating compiled kernels), interpreter-bound at scale is where 10-50x per core becomes the bill. Free-threaded 3.13 changes the thread math, not single-thread speed. Python is wrong for sustained CPU at fleet scale, hard single-digit-ms p99 paths, and RAM-constrained high-cardinality data; right wherever engineer-dollars dominate core-dollars. When a path clears the arithmetic, strangler-extract it behind a contract and keep the glue.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-when-python-is-wrong-retrieval"
  lang="en"
  questions={[
    { q: "Walk the decision tree for a slow Python service: the three buckets, the evidence for each, and the prescribed move.", a: "Profile the real workload with py-spy first — the bucket is where samples land. I/O-bound: stacks in socket/DB waits, CPU idle; stay Python, apply asyncio, pooling, more workers — a rewrite reproduces the same waiting. C-library-bound: frames inside numpy/torch/drivers; stay Python — it is glue over compiled kernels, so improve batching and data layout instead. Interpreter-bound: pure-Python frames hot under sustained load; escalate in order — algorithm, vectorization or Cython/extension (lesson 3) — and only if the path remains hot AND the core count makes it the bill, extract that path to Go/Rust as a strangler service behind a protobuf/OpenAPI contract, shadowing then shifting traffic, keeping Python orchestration. Free-threaded 3.13 only relaxes the parallelism constraint (threads instead of processes); it does not change per-core speed, so it never flips a bucket." },
    { q: "State the salary-versus-servers arithmetic and apply it to both a 4-core and a 300-core interpreter-bound service.", a: "Inputs: loaded senior engineer-year $150-300k; cloud vCPU-year roughly $200-600. The rule: a rewrite must displace more core-dollars than it costs in engineer-dollars, permanently, including dual-stack maintenance. 4-core service at 10x waste: ~3.6 wasted cores, about $1.4k/yr at $400/vCPU — a 2-month rewrite (~$25-50k loaded) never pays back; reject and spend the effort where iteration speed earns. 300-core service at the same ratio: ~270 wasted cores, ~$108k/yr — a rewrite of that single hot path pays back inside a year and compounds; approve, but as a strangler extraction of the measured path, not a fleet migration, because the surrounding services are I/O-bound glue where the same arithmetic rejects the move. The Hook's audit is the pattern: one service was 240 cores of bill; thirty services together were not." },
  ]}
/>

<Recap lang="en">
This lesson is the unit's verdict step. Lessons 1-3 produce the evidence — where the samples land, what the objects cost, what an extension can and cannot buy — and the decision tree turns evidence into a move. I/O-bound and C-library-bound services stay Python, not out of loyalty but because their cores are idle or already executing compiled kernels, so a rewrite buys syntax. Interpreter-bound paths under sustained load are where the structural 10-50x per-core gap converts into fleet size, and even then the escalation runs algorithm, vectorize, extend before rewrite. Free-threaded 3.13 relaxes the parallelism rule without touching per-core speed. The economics gate every move: engineer-years cost six figures, vCPU-years cost hundreds, so a rewrite must displace more core-dollars than it consumes in engineer-dollars forever — arithmetic that rejects the 4-core vanity rewrite and approves the 300-core scorer extraction in the same breath. And the execution pattern is the strangler: one measured hot path behind a contract, traffic shadowed then shifted, Python keeping the orchestration it is best at. Now when you see an RFC proposing to migrate a service to Go or Rust, you know the first question: run py-spy on the real workload, classify the bottleneck, and put the core-dollar arithmetic in the room before the language debate starts.
</Recap>
