infographic compare-binary-horizontal-underline-text-vs
data
  title Same 10x waste, opposite verdict
  desc Absolute core-dollars decide the rewrite, not the percentage
  compares
    - label 4-core service — reject
      children
        - label ~3.6 wasted cores
          desc 10x slower per core, but only on 4 vCPU
        - label ~$1.4k/yr compute saved
          desc The entire waste, at $400/vCPU-yr
        - label Rewrite costs ~$25-50k
          desc 2 engineer-months, loaded, one-time
        - label Never pays back
          desc ~20:1 engineer-dollars against, plus dual-stack upkeep
    - label 300-core service — approve
      children
        - label ~270 wasted cores
          desc Same 10x gap, fleet-scale core count
        - label ~$108k/yr compute saved
          desc The waste now dwarfs the rewrite
        - label Rewrite costs ~$25-50k
          desc Same hot path, same effort
        - label Pays back in under a year
          desc Strangler-extract the path, keep the glue
theme
  palette #f43f5e #10b981
