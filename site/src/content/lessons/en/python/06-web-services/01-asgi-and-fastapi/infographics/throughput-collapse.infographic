infographic chart-column-simple
data
  title One uvicorn worker: where the throughput goes (req/s)
  desc Echo flatters the framework; a 20 ms DB query makes the pool the ceiling; one blocking call in async def caps the worker at ~5
  values
    - label JSON echo · 3–9k
      value 6000
    - label + 20 ms DB query
      value 500
    - label async def + 200 ms block
      value 5
theme
  palette #14b8a6 #f59e0b #f43f5e
