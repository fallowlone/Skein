infographic chart-column-simple
data
  title Connection budget vs the Postgres ceiling
  desc A lifespan pool sits under max_connections; a pool per request blows past it
  values
    - label Lifespan pool (4 workers x 2 pods)
      value 80
    - label Postgres max_connections
      value 100
    - label Pool-per-request at 50 rps
      value 500
theme
  palette #14b8a6 #f59e0b #f43f5e
