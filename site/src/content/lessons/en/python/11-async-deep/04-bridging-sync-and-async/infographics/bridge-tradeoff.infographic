infographic compare-binary-horizontal-underline-text-vs
data
  title Two server bridges, one async SDK: the Django incident
  desc Same coroutine, opposite outcomes — the wrong bridge is invisible until you read a waterfall
  compares
    - label asyncio.run per request
      children
        - label Fresh loop built then torn down every call
        - label Connection reuse 0%
        - label TLS handshake on every request
        - label Search latency 3x, CPU on handshakes +40%
    - label Long-lived loop + run_coroutine_threadsafe
      children
        - label One loop in a daemon thread, started at boot
        - label Connection pool and TLS sessions reused
        - label .result(timeout) blocks the caller, not the loop
        - label Latency drops below the old sync client
theme
  palette #f43f5e #14b8a6
