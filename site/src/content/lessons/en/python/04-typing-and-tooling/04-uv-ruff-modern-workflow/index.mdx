---
concepts:
- python-uv-ruff
deepensInto: []
estMin: 17
lang: en
lessonType: topic
level: middle
order: 4
prereqs: []
slug: "04-uv-ruff-modern-workflow"
sources:
- https://docs.astral.sh/uv/
- https://docs.astral.sh/ruff/
- https://packaging.python.org/en/latest/specifications/pyproject-toml/
spiral: []
status: ready
summary: 'uv collapses pip, venv, pipx and pip-tools into one Rust binary: a fast resolver, a universal lockfile, uv run and uvx. ruff replaces flake8, isort and black with one pass. pyproject.toml holds every config, and migration from legacy setups is incremental, not a rewrite.'
title: 'The 2025 toolchain: uv for environments and locking, ruff for lint and format, one pyproject.toml'
track: "python"
unit: "04-typing-and-tooling"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import migrationOrder from "./infographics/migration-order.svg?raw";

<Hook>
The CI pipeline took fourteen minutes, and six of them were `pip install`. Another ninety seconds went to the lint stage, where flake8, isort, and black ran in sequence — and twice a month they fought: black would rewrap an import block, isort would "fix" it back, and the build failed on a diff no human wrote. Onboarding was worse. The README's environment setup had eleven steps, two of them wrong since the Python 3.11 bump; new hires lost their first day to `pip` resolving for forty minutes and then failing on a transitive pin conflict that `requirements.txt` had no way to express. The migration took one engineer two days: `uv sync` restored the locked environment in eight seconds on a warm cache, `uv run pytest` made "works on my machine" mean the lockfile or nothing, and `ruff check && ruff format` replaced three quarreling tools with one pass that finished before the old stack had imported its plugins. The pipeline now runs in under five minutes; nobody has debugged a venv since.
</Hook>

<Crux>uv replaces pip+venv+pipx with one resolver and a lockfile; ruff replaces flake8+isort+black. pyproject.toml is the single config.</Crux>

<Explanation>

## uv: one binary where five tools used to be

uv (Astral, written in Rust) collapses pip, venv, pip-tools, pipx, and most of pyenv into one binary. The core loop: `uv add httpx` writes the dependency into `pyproject.toml`, resolves the full graph, and pins everything in `uv.lock`; `uv sync` makes the project's `.venv` match the lockfile exactly — removing strays, not just adding; `uv run pytest` guarantees the command executes in that synced environment, creating it on the fly if a teammate just cloned the repo. `uvx ruff` runs a tool in an isolated, cached environment — pipx without the second tool. `uv python install 3.12` even manages interpreters. Two properties do the heavy lifting. First, speed: the resolver is 10–100× faster than pip with a warm cache (Astral's published benchmarks; cold installs are nearer 8–10×), which turns "resolve the whole graph from scratch" from a coffee break into a blink — that is what makes always-resolving-from-pyproject practical. Second, the lockfile: `uv.lock` is *universal* — resolved once for all platforms and Python versions, with hashes — so the Linux CI box and the macOS laptop install byte-identical graphs. The honest caveats: `uv.lock` is uv's own format (a standard, PEP 751's `pylock.toml`, is only now emerging), and the tool is young and VC-backed — but because all inputs live in standard `pyproject.toml`, the exit path back to pip is real.

```python
# /// script
# requires-python = ">=3.12"
# dependencies = ["httpx"]
# ///
# PEP 723 inline metadata: `uv run fetch.py` builds this one-off
# environment on demand — no requirements.txt for a single script.
import httpx
print(httpx.get("https://example.com").status_code)
```

<Quiz
  id="04-uv-ruff-modern-workflow-quiz-1"
  lessonSlug="04-uv-ruff-modern-workflow"
  lang="en"
  question="A teammate clones the repo and immediately runs uv run pytest with no setup. What happens?"
  choices={[
    { label: "uv creates the .venv, installs the exact graph pinned in uv.lock, then runs pytest inside it — the command is guaranteed to see the locked environment", correct: true },
    { label: "It fails — you must create and activate a virtualenv before uv can run anything", misconception: "Inverting the model is the point: uv run owns the environment lifecycle. It creates and syncs .venv on demand from the lockfile; manual venv activation becomes optional ceremony, not a prerequisite." },
    { label: "It runs whatever pytest is on the system PATH, like a shell alias would", misconception: "That is exactly the failure mode uv run exists to prevent. The command resolves inside the project environment built from uv.lock, so a stray system pytest with different plugin versions never leaks into the run." },
    { label: "It installs the latest versions of all dependencies, since nothing is pinned yet", misconception: "The clone already contains uv.lock — the pins travel with the repo. uv sync/run installs those exact versions with hash verification; fresh resolution happens only when you change dependencies or ask for upgrades." },
  ]}
/>

## ruff: lint and format in one pass

When lint lives in CI and takes 90 seconds, engineers stop caring about it. When it runs in under a second on every save, it becomes the same kind of instant feedback as a syntax error — and that changes which bugs get caught before review. That is the core argument for ruff.

ruff is the same consolidation applied to code quality: one Rust binary that re-implements flake8 (plus most of its popular plugins), isort, pyupgrade, and a large slice of pylint as `ruff check`, and a black-compatible formatter as `ruff format` (Astral measures the formatter as >99.9% identical to black on black's own test corpus). Rules are opt-in by family in `pyproject.toml`: `E`/`F` (pycodestyle/pyflakes) as the baseline, `I` for import sorting, `B` for flake8-bugbear's likely-bugs, `UP` for outdated syntax, with `--fix` auto-applying safe corrections. Speed is the headline number and it is not marketing: ruff lints CPython-scale codebases in well under a second where the flake8 stack took tens of seconds — fast enough to run the *entire rule set on every save*, which changes behavior: lint stops being a CI-only nag and becomes feedback you see while typing. The consolidation also ends the tri-tool fights from the Hook, because one tool with one config cannot disagree with itself about import wrapping. Tradeoffs to state: a residue of plugin-specific flake8 rules and some pylint checks have no ruff equivalent, and ruff intentionally does not type-check — it complements, never replaces, mypy/pyright.

## pyproject.toml as the single config, and the migration path

Everything above reads its configuration from one file:

```toml
[project]
name = "billing"
requires-python = ">=3.12"
dependencies = ["httpx>=0.27", "pydantic>=2.7"]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]

[tool.uv]
dev-dependencies = ["pytest>=8", "mypy>=1.10"]
```

One file replaces `requirements.txt`, `requirements-dev.txt`, `setup.cfg`, `.flake8`, `.isort.cfg`, and `pyproject`'s old black section. Migration from a legacy repo is incremental, and the order matters: **(1)** adopt ruff first — it is config-only, zero runtime risk; start with `E`,`F`, delete flake8/isort/black configs, expand rule families one PR at a time so the diff stays reviewable. **(2)** Use uv as a faster pip without changing workflow: `uv pip install -r requirements.txt` is a drop-in that proves the speed claim on your own repo. **(3)** Move dependencies into `pyproject.toml` with `uv add`, commit `uv.lock`, switch CI to `uv sync --frozen` so the build fails if the lockfile is stale instead of silently re-resolving. **(4)** Replace global tool installs with `uvx`. The anti-pattern is doing it as one big-bang PR that swaps packaging, linting, and formatting simultaneously — when something breaks, you cannot tell which layer did it.

<Infographic
  svg={migrationOrder}
  label="Four-step migration sequence ordered by risk: adopt ruff (config-only, zero runtime risk), then use uv as a drop-in faster pip, then move dependencies to pyproject.toml and commit uv.lock with uv sync --frozen in CI, then replace global tool installs with uvx."
  caption="Migrate one revertible layer at a time, lowest risk first — a big-bang swap of packaging, lint, and format makes the first failure impossible to attribute."
  hue="var(--d-infra)"
/>

<Quiz
  id="04-uv-ruff-modern-workflow-quiz-2"
  lessonSlug="04-uv-ruff-modern-workflow"
  lang="en"
  question="How does committing uv.lock differ from committing a pip freeze > requirements.txt snapshot?"
  choices={[
    { label: "uv.lock is resolved universally — one resolution valid for all platforms and Python versions, with hashes — while pip freeze captures only what happened to be installed on this machine and platform", correct: true },
    { label: "There is no real difference; both pin exact versions", misconception: "Both pin, but pip freeze pins the result of one environment's history: platform-specific wheels, forgotten manual installs, missing markers for other OSes. The Linux CI box can legitimately resolve a different graph than the macOS laptop." },
    { label: "uv.lock pins only direct dependencies, leaving transitive ones flexible", misconception: "It is the opposite — the lockfile pins the entire transitive graph with hashes. Flexible ranges live in pyproject.toml; the lockfile exists precisely to remove all remaining freedom at install time." },
    { label: "requirements.txt is the safer choice because every tool understands it", misconception: "Compatibility is real but the format cannot express per-platform resolution or hash-verified universal graphs. uv can still export (uv export) to requirements format when an external tool needs it — you lose nothing by locking properly." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Five legacy tools collapse into two binaries reading one config file, with the lockfile making every environment reproducible"
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "legacy", label: "pip + venv + pipx", sub: "plus pip-tools, pyenv" },
    { id: "uv", label: "uv", sub: "resolve, sync, run" },
    { id: "lock", label: "uv.lock", sub: "universal, hashed" },
    { id: "lint", label: "flake8 + isort + black", sub: "three configs, fights" },
    { id: "ruff", label: "ruff", sub: "check + format, one pass" },
    { id: "toml", label: "pyproject.toml", sub: "single source of config" },
  ]}
  edges={[
    { from: "legacy", to: "uv", label: "replaced by" },
    { from: "uv", to: "lock", label: "pins graph" },
    { from: "lint", to: "ruff", label: "replaced by" },
    { from: "uv", to: "toml", label: "reads" },
    { from: "ruff", to: "toml", label: "reads" },
  ]}
  caption="uv run executes inside the environment uv.lock describes — 'works on my machine' becomes 'works as locked'. ruff consolidates lint and format so tools cannot disagree. Migrate incrementally: ruff first, uv as fast pip second, lockfile third."
/>

<KeyTakeaway>uv is pip+venv+pipx+pip-tools in one Rust binary: 10–100× faster resolution warm, a universal hashed `uv.lock`, `uv run` guaranteeing the locked environment, `uvx` for tools. ruff is flake8+isort+black in one pass, fast enough to lint on every save. Both read `pyproject.toml`, and the migration is incremental — ruff first, uv as a drop-in pip, then the lockfile.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-uv-ruff-modern-workflow-retrieval"
  lang="en"
  questions={[
    { q: "Walk through what uv add, uv sync, uv run, and uvx each do, and what makes uv.lock different from a pip freeze snapshot.", a: "uv add writes the dependency into pyproject.toml, resolves the full graph, and pins it in uv.lock. uv sync makes .venv match the lockfile exactly, removing strays as well as installing. uv run executes a command inside that synced environment, creating it on demand — a fresh clone can go straight to uv run pytest. uvx runs a tool in an isolated cached environment, replacing pipx. uv.lock is a universal resolution: computed once for all platforms and Python versions with hashes, so CI on Linux and a laptop on macOS install identical graphs — whereas pip freeze records only what one machine happened to have, with no markers for other platforms and no hash verification. In CI, uv sync --frozen fails the build if the lockfile is stale." },
    { q: "What is the safe migration order from pip+flake8+isort+black, and why is the big-bang swap an anti-pattern?", a: "Order by risk: (1) ruff first — pure config change, zero runtime impact; start with E and F rules, delete the flake8/isort/black configs, then widen rule families (I, B, UP) one reviewable PR at a time. (2) uv as a drop-in faster pip — uv pip install -r requirements.txt changes no workflow but proves the speed on your repo. (3) Move deps to pyproject.toml via uv add, commit uv.lock, and flip CI to uv sync --frozen so stale lockfiles fail loudly. (4) uvx for global tools. Big-bang swaps of packaging, linting, and formatting in one PR are an anti-pattern because the first breakage cannot be attributed: you cannot tell whether the resolver, a new lint rule, or the formatter caused it, and the rollback reverts everything including the parts that worked." },
  ]}
/>

<Recap lang="en">
The 2025 Python toolchain is a consolidation story. uv replaces pip, venv, pip-tools, pipx, and much of pyenv with one Rust binary: `uv add` resolves and pins into a universal, hash-verified `uv.lock`; `uv sync` makes the environment match it exactly; `uv run` guarantees commands execute inside that environment, building it on demand from a fresh clone; `uvx` runs tools in isolated caches; `uv python install` manages interpreters. Resolution runs 10–100× faster than pip on a warm cache, which is what makes lockfile-first workflows feel free, and the lockfile is resolved once for every platform — the end of "resolves differently on the CI box." ruff applies the same consolidation to quality: `ruff check` re-implements flake8 and its plugin ecosystem, isort, and pyupgrade; `ruff format` is black-compatible to >99.9% on black's own corpus; one pass finishes in well under a second at CPython scale, fast enough to move linting from CI nag to on-save feedback, and one config ends the black-vs-isort wars. Everything reads `pyproject.toml` — dependencies, rule selection, line length — replacing a half-dozen dotfiles. Migrate in risk order: ruff (config-only), uv as drop-in pip, then `pyproject.toml` plus committed lockfile with `uv sync --frozen` in CI, then `uvx`; never as one big-bang PR, because the first failure becomes unattributable. Caveats stay caveats: `uv.lock` is tool-specific until PEP 751 matures, ruff does not type-check, and a few flake8/pylint rules have no equivalent yet. Now when you see a CI pipeline that spends six minutes on `pip install` or a lint stage that fights itself on import ordering, you know the exact four-step migration that fixes it — and the exact reason to start with ruff, not with the lockfile.
</Recap>
