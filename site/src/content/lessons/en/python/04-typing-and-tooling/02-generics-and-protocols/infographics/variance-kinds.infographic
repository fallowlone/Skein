infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label Is container[Dog] a container[Animal]?
    children
      - label Invariant — list[T] (mutable)
        children
          - label No: callee could append Cat
      - label Covariant — Sequence[T] (read-only)
        children
          - label Yes: read-only, only produces
      - label Contravariant — Callable[[T], None]
        children
          - label Flipped: Animal-handler serves Dog
theme
  palette #f43f5e #10b981 #6366f1
