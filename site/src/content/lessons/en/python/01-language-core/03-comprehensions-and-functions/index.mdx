---
concepts:
- comprehensions
- generator-expressions
- mutable-default-args
- args-kwargs
- closures-legb
deepensInto: []
estMin: 17
lang: en
lessonType: topic
level: middle
order: 3
prereqs: []
slug: 03-comprehensions-and-functions
sources:
- https://docs.python.org/3/tutorial/datastructures.html
- https://docs.python.org/3/reference/expressions.html
- https://docs.python.org/3/tutorial/controlflow.html
- https://docs.python.org/3/faq/programming.html
spiral: []
status: ready
summary: 'Comprehensions are Pythonic map/filter; generator expressions stream lazily over huge inputs. Functions add a sharp trap — default args are evaluated once at def time — plus *args/**kwargs and LEGB closures unlike JS.'
title: 'Comprehensions and functions'
track: python
unit: 01-language-core
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
A teammate ports a JS data pipeline to Python and writes `result = list(map(lambda x: x * 2, filter(lambda x: x > 0, nums)))`. It works, but it reads like a translation, not Python. Then a different function, `def add_tag(tag, tags=[])`, starts returning tags from *previous calls* it never received — a ghost accumulating across the whole process. Both are the same lesson: Python's expression-level tools and its function model do not behave like JavaScript's, and the gaps bite exactly where you stop paying attention.
</Hook>

<Crux>Comprehensions are Python's map/filter; generators make them lazy. Default args evaluate once at def time — the classic mutable trap.</Crux>

<Explanation>

## Comprehensions: map and filter, fused into one expression

A list comprehension is Python's idiomatic replacement for chaining `.map()` and `.filter()`. The shape `[f(x) for x in xs if pred(x)]` reads in evaluation order — source, filter, transform — and produces a list in one pass. Where JavaScript writes `xs.filter(pred).map(f)`, Python writes the transform first and the filter last, but the data flows the same way: each element of `xs` is tested by `pred`, and the survivors are passed through `f`.

```python
nums = [-2, -1, 0, 1, 2, 3]
# JS: nums.filter(x => x > 0).map(x => x * 2)
doubled = [x * 2 for x in nums if x > 0]   # [2, 4, 6]

# Same syntax, different brackets, different collection:
squares = {x: x * x for x in range(4)}      # dict: {0: 0, 1: 1, 2: 4, 3: 9}
unique  = {len(w) for w in ["a", "bb", "cc"]}  # set: {1, 2}
```

The `{...}` form gives you a **dict** or **set** comprehension depending on whether you write `k: v` or a bare element — there is no JS object-literal equivalent that is as direct. This is the first reason comprehensions matter: one syntax family covers map, filter, and the construction of every built-in collection.

<FlowDiagram
  label="How a list comprehension transforms a source iterable through a filter and a mapping into a new list"
  hue="var(--accent)"
  perRow={4}
  nodes={[
    { id: "src", label: "Source", sub: "for x in xs" },
    { id: "pred", label: "Filter", sub: "if pred(x)" },
    { id: "map", label: "Transform", sub: "f(x)" },
    { id: "out", label: "New list", sub: "[ ... ]" },
  ]}
  edges={[
    { from: "src", to: "pred", label: "each x" },
    { from: "pred", to: "map", label: "kept" },
    { from: "map", to: "out", label: "f(x)" },
  ]}
  caption="A comprehension is map and filter fused: every element flows from the source through the predicate, and survivors are transformed into the output collection in a single pass."
/>

Nested comprehensions exist — `[c for row in matrix for c in row]` flattens a 2D list, with the loops read left-to-right just as if you had nested `for` statements. But this is where readability dies fast. A comprehension with two `for` clauses and a conditional is at the edge of what a reader can parse at a glance; three is past it. The senior judgment call: a comprehension should fit in your head in one read. The moment it does not, write the explicit loop. Cleverness in a comprehension is a cost you pay on every future read.

## Generators: the same shape, but lazy

Swap the square brackets for parentheses and `[f(x) for x in xs]` becomes a **generator expression** `(f(x) for x in xs)`. This is not a cosmetic change. The list comprehension is *eager*: it builds the entire list in memory before the next line runs. The generator is *lazy*: it computes one value at a time, only when asked, and holds essentially constant memory regardless of how many elements flow through it.

```python
# Eager: materializes 10 million ints — hundreds of MB resident.
total = sum([x * x for x in range(10_000_000)])

# Lazy: one value at a time, memory stays flat.
total = sum(x * x for x in range(10_000_000))
```

The same idea drives the `yield` keyword. A function containing `yield` is a generator function: calling it does not run the body — it returns a generator object that runs lazily, pausing at each `yield` and resuming on the next request. This is how you stream data that does not fit in memory.

```python
def read_lines(path):
    with open(path) as f:
        for line in f:
            yield line.rstrip("\n")   # one line resident at a time, not the whole file

for line in read_lines("huge.log"):    # constant memory over a 50 GB file
    ...
```

This is the real senior point of the whole first half. Reach for an **eager list** when you need the result more than once, need its length, or need to index into it. Reach for a **lazy generator** when the input is large or unbounded, you only pass over it once, and you want memory to stay flat — a streaming pipeline, a file reader, an API paginator.

<Inset kind="why" lang="en">
A generator is single-use: it is an iterator that exhausts as you consume it. After one full pass, it is empty — a second `for` loop over the same generator object yields nothing, and `list(gen)` after you have already iterated returns `[]`. This surprises people coming from JS arrays, which you can iterate any number of times. If you need to traverse the data twice, either materialize it once with `list(gen)` or build a fresh generator. The laziness that saves memory is exactly what makes it one-shot.
</Inset>

## Functions: arguments, and the trap that defines Python

`def` defines a function; arguments can be passed positionally or by keyword, and parameters can carry defaults: `def connect(host, port=5432, *, timeout=30)`. The bare `*` marks everything after it **keyword-only** — callers must write `timeout=10`, never a bare positional — which is how you keep a call site readable as the signature grows. `*args` collects extra positionals into a tuple and `**kwargs` collects extra keywords into a dict; at the call site the same `*`/`**` *unpack* an iterable or mapping into arguments, the Python analog of JS spread.

```python
def log(level, *args, **kwargs):
    print(level, args, kwargs)

log("INFO", "started", port=8080)   # INFO ('started',) {'port': 8080}
```

Now the trap. **A default argument is evaluated exactly once, when the `def` statement runs — not on each call.** If the default is a mutable object, every call that omits that argument shares the *same* object, and mutations persist across calls.

```python
def add_tag(tag, tags=[]):     # the [] is created ONCE, at def time
    tags.append(tag)
    return tags

add_tag("a")   # ['a']
add_tag("b")   # ['a', 'b']  ← the same list, surviving across calls
```

This is not a bug in Python; it falls directly out of "defaults are evaluated at definition time." JavaScript re-evaluates default parameters on every call, so `function f(x = [])` is fresh each time — the exact opposite. The idiomatic Python fix is the sentinel:

```python
def add_tag(tag, tags=None):
    if tags is None:
        tags = []          # a fresh list on every call that omits it
    tags.append(tag)
    return tags
```

<SequenceDiagram
  label="Why the mutable default leaks across calls: the default list is created once when the def statement runs, stored on the function object, and shared by every later call that omits the argument"
  hue="var(--accent)"
  actors={["def time", "default list", "call #1", "call #2"]}
  messages={[
    { from: "def time", to: "default list", label: "tags=[] built once" },
    { from: "call #1", to: "default list", label: "append('a')" },
    { from: "call #2", to: "default list", label: "sees ['a'] ghost" },
  ]}
  caption="JavaScript re-evaluates default parameters on every call; Python evaluates them once, at def time. Both calls mutate the one stored list — the sentinel fix (tags=None, create inside the body) gives every call a fresh list."
/>

## Lambdas, closures, and LEGB scope

`lambda` makes an anonymous function, but it is deliberately limited: a single expression, no statements, no annotations. It is *not* the JS arrow function — there is no multi-line lambda. Use it for a throwaway key function (`sorted(words, key=lambda w: len(w))`) and reach for a named `def` the instant it wants a second line.

Functions are first-class — passed, returned, stored — and an inner function that references an outer function's variable forms a **closure**, capturing that variable by reference. Name resolution follows **LEGB**: Python looks in the **L**ocal scope, then any **E**nclosing function scopes, then the **G**lobal (module) scope, then **B**uilt-ins. Crucially, assigning to a name inside a function makes it local *for the whole function* unless you say otherwise — `global` rebinds a module-level name, and `nonlocal` rebinds a name in the nearest enclosing function. JS resolves scope lexically too, but `let`/`const` give you per-block bindings, whereas Python's scope unit is the function, and reassignment-implies-local is a sharper edge.

```python
def make_counter():
    count = 0
    def tick():
        nonlocal count    # without this, count += 1 raises UnboundLocalError
        count += 1
        return count
    return tick
```

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">JavaScript idiom</th>
        <th class="border border-border px-3 py-2 text-left">Python idiom</th>
        <th class="border border-border px-3 py-2 text-left">Note</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>xs.map(f)</code></td>
        <td class="border border-border px-3 py-2"><code>[f(x) for x in xs]</code></td>
        <td class="border border-border px-3 py-2">Comprehension, one pass</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>xs.filter(p)</code></td>
        <td class="border border-border px-3 py-2"><code>[x for x in xs if p(x)]</code></td>
        <td class="border border-border px-3 py-2">Filter is the trailing <code>if</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>x =&gt; x * 2</code></td>
        <td class="border border-border px-3 py-2"><code>lambda x: x * 2</code></td>
        <td class="border border-border px-3 py-2">Single expression only; else <code>def</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>f(...arr)</code> / <code>&#123;...obj&#125;</code></td>
        <td class="border border-border px-3 py-2"><code>f(*seq)</code> / <code>f(**mapping)</code></td>
        <td class="border border-border px-3 py-2">Spread → unpacking</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>function(...args)</code></td>
        <td class="border border-border px-3 py-2"><code>def f(*args, **kwargs)</code></td>
        <td class="border border-border px-3 py-2">Rest params → <code>*args</code>/<code>**kwargs</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">generators / <code>yield</code></td>
        <td class="border border-border px-3 py-2"><code>(f(x) for x in xs)</code> / <code>yield</code></td>
        <td class="border border-border px-3 py-2">Same lazy idea, parens not brackets</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>function f(x = [])</code> fresh each call</td>
        <td class="border border-border px-3 py-2"><code>def f(x=None)</code> + sentinel</td>
        <td class="border border-border px-3 py-2">Python default evaluated once at def</td>
      </tr>
    </tbody>
  </table>
</div>

<Quiz
  id="py-comp-fn-quiz-1"
  lessonSlug="03-comprehensions-and-functions"
  lang="en"
  question="What does add_tag('a'); add_tag('b') print if defined as def add_tag(tag, tags=[]): tags.append(tag); return tags?"
  choices={[
    { label: "['a'] then ['a', 'b'] — the default list is created once at def time and shared across calls that omit tags", correct: true },
    { label: "['a'] then ['b'] — each call gets a fresh empty list", misconception: "That is JavaScript's behavior, where defaults re-evaluate per call. In Python the default object is created once when def runs, so it persists across calls." },
    { label: "It raises an error because you can't mutate a default argument", misconception: "Mutating a default is allowed — that is exactly the trap. The list is a real shared object and append works; it just keeps the data between calls." },
    { label: "['a', 'b'] both times — the list resets but keeps history", misconception: "There is no reset. The same list object accumulates, so the first call returns ['a'] and the second returns ['a', 'b']." },
  ]}
/>

<Quiz
  id="py-comp-fn-quiz-2"
  lessonSlug="03-comprehensions-and-functions"
  lang="en"
  question="g = (x*x for x in range(3)). After total = sum(g), what does list(g) return?"
  choices={[
    { label: "[] — a generator is a single-use iterator; sum() exhausted it, so the second pass yields nothing", correct: true },
    { label: "[0, 1, 4] — generators can be iterated as many times as you like", misconception: "That is a list, not a generator. A generator expression is a one-shot iterator: once consumed it is empty, unlike a JS array." },
    { label: "[0, 1, 4] — list() rebuilds it from the original range", misconception: "list() does not re-run the generator's source; it just drains whatever the generator has left, which after sum() is nothing." },
    { label: "It raises StopIteration", misconception: "Iterating an exhausted generator with for/list does not raise — it simply produces no items and returns an empty list." },
  ]}
/>

<TradeoffMatrix
  id="py-comp-fn-tradeoff-1"
  lessonSlug="03-comprehensions-and-functions"
  lang="en"
  prompt="A pipeline must transform a 50 GB log file line by line and write a summary, on a box with 2 GB RAM. Pick the construct."
  options={[
    { name: "Generator: def lines(): ... yield ... — process one line at a time", summary: "Constant memory.", correct: true, justification: "Right for a memory-bound, single-pass stream. A generator holds one line resident at a time, so memory stays flat regardless of file size — a 50 GB file processes fine in 2 GB of RAM. You pass over the data once, which is exactly what summarizing needs, and you never materialize the whole file. This is the canonical use of yield: streaming data larger than memory." },
    { name: "Eager list: lines = [transform(l) for l in open(path)]", summary: "Materializes everything.", justification: "This builds the entire transformed file in memory before the summary even starts — 50 GB of data into a 2 GB box means an immediate MemoryError or thrashing. List comprehensions are the wrong tool the moment the input does not comfortably fit; their eagerness is a liability here, not a feature." },
    { name: "Read the whole file with f.read(), then split and map", summary: "Whole file in a string.", justification: "Worse than the eager list: f.read() pulls all 50 GB into a single string first, blowing memory before any transform runs. Reading a huge file whole is the classic out-of-memory mistake; stream it line by line instead." },
    { name: "List comprehension but call gc.collect() between batches", summary: "Force GC.", justification: "Garbage collection cannot help when the data is still referenced — the comprehension holds every transformed line live in the result list, so there is nothing to collect. GC is not a substitute for laziness; the fix is to not materialize the whole thing, i.e. a generator." },
  ]}
/>

</Explanation>

<KeyTakeaway>Use `[f(x) for x in xs if p(x)]` instead of map/filter; switch `[]` to `()` for a lazy generator over huge inputs. And never use a mutable default — `def f(x=[])` shares one list across calls; use `x=None` + a sentinel.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="py-comp-fn-retrieval"
  lang="en"
  questions={[
    { q: "When should you choose a generator expression over a list comprehension, and what is the cost?", a: "Choose a generator when the input is large or unbounded, you only need to pass over it once, and you want memory to stay flat. A list comprehension is eager — it builds the entire result in memory before the next line runs, which is fine when you need the data more than once, need its length, or need to index into it, but disastrous for a 50 GB file or an infinite stream. A generator is lazy: it computes one value at a time, on demand, so it processes data far larger than RAM at constant memory. The cost is twofold. First, a generator is single-use — it is an iterator that exhausts as you consume it, so a second pass yields nothing and you must either materialize with list() or build a fresh generator. Second, you lose len() and indexing, because nothing is stored. The rule of thumb: stream with a generator, collect with a list, and decide based on whether you traverse the data once or many times and whether it fits in memory." },
    { q: "Why does def f(x=[]) accumulate state across calls, and what is the idiomatic fix?", a: "Because a default argument is evaluated exactly once — when the def statement executes, not on each call. The [] is created a single time and bound to the parameter; every call that omits that argument receives the very same list object, so appends from one call are visible in the next, and state leaks across the whole program. This falls directly out of Python's model: function defaults are stored on the function object at definition time. It is the opposite of JavaScript, where default parameters re-evaluate on every invocation and so are fresh each call. The idiomatic fix is the sentinel pattern: write def f(x=None), then inside the body do `if x is None: x = []`, which creates a genuinely fresh list on every call that omits the argument. Use None (or another immutable sentinel) for any mutable default — list, dict, set — and never put a mutable literal directly in the signature." },
  ]}
/>

<Recap lang="en">
Comprehensions are Python's fused map-and-filter: `[f(x) for x in xs if pred(x)]` replaces a `.filter().map()` chain in one readable pass, and the `{...}` form builds dicts and sets too — but a comprehension that no longer fits in your head in one read should become an explicit loop. Swapping the brackets for parentheses turns it into a lazy generator expression, the same lazy machinery as `yield`: one value at a time, constant memory, single-use. Collect with a list when you need the result repeatedly, its length, or indexing; stream with a generator when the input is large or one-pass. On the function side, `*args`/`**kwargs` collect and unpack arguments, the bare `*` makes parameters keyword-only, and `lambda` is a single-expression throwaway — reach for `def` the moment it grows. The defining trap: default arguments are evaluated once at def time, so a mutable default like `[]` is shared across calls; use `None` plus a sentinel. Finally, name resolution is LEGB, scope is per-function not per-block, and assignment makes a name local unless you reach for `global` or `nonlocal` to rebind an outer one.
</Recap>
