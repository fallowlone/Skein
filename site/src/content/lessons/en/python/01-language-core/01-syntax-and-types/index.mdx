---
concepts:
- significant-indentation
- names-are-references
- numeric-tower
- truthiness-and-none
- equality-vs-identity
- type-hints-not-enforced
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-syntax-and-types
sources:
- https://docs.python.org/3/reference/lexical_analysis.html
- https://docs.python.org/3/reference/expressions.html
- https://docs.python.org/3/library/stdtypes.html
- https://docs.python.org/3/library/typing.html
spiral: []
status: ready
summary: 'Python blocks are whitespace, names are just references, ints are arbitrary-precision, and type hints never run — coming from JS/TS the gotchas are == vs is and that None is the only nullish.'
title: 'Syntax and types: Python through a JS/TS lens'
track: python
unit: 01-language-core
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A JS developer ports a function to Python, runs it, and `a == b` is `True` for two lists — but `a is b` is `False`, and a colleague's nearly-identical check returns `True` for two small integers and `False` for two large ones. Nothing in the type hints warned them; `mypy` was never run in CI. They reach for `===`, find it doesn't exist, and assume `==` is the strict one. It isn't. Every one of these surprises traces back to four facts about Python's syntax and type model that are quietly different from JavaScript — and getting them wrong produces code that passes today and breaks on input you didn't test. By the end of this lesson you'll know exactly why each of these bites and what to do differently.
</Hook>

<Crux>Python blocks are indentation, names are references, ints are unbounded, hints never run — and `==` is value while `is` is identity.</Crux>

<Explanation>

## Blocks are whitespace; variables are just names

Python has no braces and no semicolons. A block is a run of lines at the same indentation under a header line ending in `:`. The indentation *is* the syntax — mixing tabs and spaces, or being off by one space, is a `IndentationError`, not a style nit. The community standard (PEP 8) is four spaces.

```python
def classify(n: int) -> str:
    if n < 0:
        return "negative"
    elif n == 0:
        return "zero"
    return "positive"          # same level as the if → still in classify
```

Variables need no keyword. There is no `let`, `const`, or `var`: assignment both declares and binds. And a name is a *reference* to an object, exactly like a JS object reference — `b = a` does not copy a list, it makes `b` point at the same list, so mutating through one is visible through the other. Python has no built-in `const`; immutability is a property of the *value* (tuples, strings, frozensets are immutable), not of the binding.

```python
a = [1, 2, 3]
b = a            # b references the SAME list, no copy
b.append(4)
print(a)         # [1, 2, 3, 4] — mutated through b
```

## The numeric tower: `int`, `float`, and two division operators

JavaScript has one number type — an IEEE-754 double — so `9999999999999999` silently loses precision. Python's `int` is **arbitrary-precision**: it grows to whatever size the value needs, no overflow, no silent rounding. `float` is the IEEE-754 double you know. The trap for JS developers is division: `/` **always** produces a `float`, even on two ints, and `//` is **floor division** (rounds toward negative infinity).

```python
type(2 ** 100)        # <class 'int'> — 1267650600228229401496703205376, exact
7 / 2                 # 3.5    (float, always)
7 // 2                # 3      (floor division, int)
-7 // 2               # -4     (floors toward -infinity, not toward zero)
7 % 3                 # 1      (modulo)
```

Strings are immutable sequences. Interpolation is the **f-string** — Python's template literal — using `{}` instead of `${}`, and it can hold any expression: `f"{name} has {len(items)} items"`. There is no implicit number-to-string coercion: `"x" + 1` is a `TypeError`, not `"x1"`.

<FlowDiagram
  label="Python expression evaluation chain from a literal to its type to a comparison branching into value equality versus identity"
  hue="var(--d-systems)"
  perRow={4}
  nodes={[
    { id: "lit", label: "7 / 2", sub: "expr" },
    { id: "obj", label: "float 3.5", sub: "object" },
    { id: "cmp", label: "compare", sub: "x ? y" },
    { id: "eq", label: "x == y", sub: "value" },
    { id: "is", label: "x is y", sub: "identity" },
    { id: "true", label: "equal?", sub: "__eq__" },
    { id: "same", label: "same obj?", sub: "id(x)==id(y)" },
  ]}
  edges={[
    { from: "lit", to: "obj", label: "evals to" },
    { from: "obj", to: "cmp", label: "bound" },
    { from: "cmp", to: "eq", label: "==" },
    { from: "cmp", to: "is", label: "is" },
    { from: "eq", to: "true", label: "calls" },
    { from: "is", to: "same", label: "checks" },
  ]}
  caption="`==` dispatches to the object's `__eq__` (value comparison); `is` compares object identity directly. They answer different questions and routinely disagree."
/>

## Truthiness, `None`, and the `==` vs `is` gotcha

Like JS, Python has truthiness, but the falsy set differs: `False`, `None`, `0`, `0.0`, `""`, and **every empty collection** (`[]`, `{}`, `()`, `set()`) are falsy; everything else is truthy. So `if items:` is the idiomatic "is this list non-empty?" check — there is no separate `length > 0` ceremony.

`None` is Python's single nullish value — it replaces *both* JS `null` and `undefined`. There is no `undefined`; a missing attribute raises, a missing dict key raises, and an absent argument defaults to whatever you declared (commonly `None`). The idiomatic test is `if x is None:`, **not** `== None`, because `None` is a singleton and identity is exact and fast.

That brings the real gotcha. **`==` compares value; `is` compares identity** (whether two names point at the literally same object in memory). JS has nothing exactly like `is` — `===` is still a value comparison for primitives. Confusing them is the classic bug, and it's made sneaky by **small-int caching**: CPython pre-allocates the integers from −5 to 256, so `is` *accidentally* returns `True` for small ints and `False` for large ones.

```python
a = 256;  b = 256
a is b          # True  — both are the SAME cached object

x = 257;  y = 257
x is y          # False — two distinct objects (value-equal, not identity-equal)
x == y          # True  — value comparison, the one you actually wanted

[] == []        # True  — equal value
[] is []        # False — two separate list objects
```

Rule of thumb: use `is` only for `None`, `True`, `False`, and sentinel singletons; use `==` for everything else. Booleans are themselves a subclass of `int` — `True == 1` and `False == 0` are `True` — another sharp edge when you compare loosely.

<Quiz
  id="py-syntax-types-quiz-equality"
  lessonSlug="01-syntax-and-types"
  lang="en"
  question="x = 257; y = 257. What do x == y and x is y return, and why?"
  choices={[
    { label: "x == y is True (equal value) but x is y is False, because 257 is outside CPython's −5..256 cached-int range, so the two literals are distinct objects", correct: true },
    { label: "Both True — Python interns all equal integers to one object", misconception: "Only small ints (−5..256) are pre-cached/interned. 257 produces two distinct objects, so `is` is False even though the values are equal." },
    { label: "Both False — 257 is too large for an int and overflows", misconception: "Python ints are arbitrary-precision; 257 never overflows. == is True (equal value); only `is` (identity) is False." },
    { label: "x == y is False, x is y is True — == is the strict comparison", misconception: "There is no === in Python. == is the value comparison (True here); `is` is identity (False here because they are different objects)." },
  ]}
/>

## Type hints describe; they do not enforce

If you're coming from TypeScript with the expectation that annotating a parameter as `int` actually stops a `str` from getting through at runtime — this section is the one that will save you a production incident.

This is the deepest break from TypeScript. Python's type hints (PEP 484) look like TS annotations — `def f(x: int) -> str:`, `count: int = 0`, `items: list[str]` — but the interpreter **ignores them at runtime**. Passing a `str` where an `int` is hinted runs fine until something actually breaks on the value. Hints are checked *statically* by a separate tool — `mypy` or `pyright` — that you run in CI; they are essentially advisory metadata stored in `__annotations__`.

```python
def repeat(s: str, n: int) -> str:
    return s * n

repeat("ab", 3)       # 'ababab' — correct
repeat(3, "x")        # hints say (str, int) but Python never checks:
                      # 3 * "x" runs and returns 'xxx' — wrong types, no error, a silent bug
repeat("ab", "3")     # "ab" * "3" — TypeError at runtime, since str * str is invalid
```

The TS contrast is subtle. TS types are also erased at compile time — they don't exist at runtime either. The difference is *ecosystem and enforcement*: in TS, code that fails the type-checker normally doesn't ship (the build fails); in Python, the program runs regardless of whether you ever ran `mypy`. Treat hints as documentation that a checker can verify, not as a runtime guard. If you need runtime validation, that's a library (Pydantic, `dataclasses` with checks), not the type system.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">JS / TS</th>
        <th class="border border-border px-3 py-2 text-left">Python</th>
        <th class="border border-border px-3 py-2 text-left">Note</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>let x = 1; const y = 2;</code></td>
        <td class="border border-border px-3 py-2"><code>x = 1; y = 2</code></td>
        <td class="border border-border px-3 py-2">No keyword; no built-in const (immutability is the value's)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>a === b</code> (value, no coercion)</td>
        <td class="border border-border px-3 py-2"><code>a == b</code></td>
        <td class="border border-border px-3 py-2">No <code>===</code>; <code>==</code> is the value compare</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>Object.is(a, b)</code> / ref identity</td>
        <td class="border border-border px-3 py-2"><code>a is b</code></td>
        <td class="border border-border px-3 py-2">Identity; use only for None/True/False/sentinels</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>null</code> and <code>undefined</code></td>
        <td class="border border-border px-3 py-2"><code>None</code></td>
        <td class="border border-border px-3 py-2">One nullish; test with <code>is None</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>`$&#123;name&#125;: $&#123;n&#125;`</code></td>
        <td class="border border-border px-3 py-2"><code>f"&#123;name&#125;: &#123;n&#125;"</code></td>
        <td class="border border-border px-3 py-2">f-string; holds any expression</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>number</code> (one IEEE-754 type)</td>
        <td class="border border-border px-3 py-2"><code>int</code> / <code>float</code></td>
        <td class="border border-border px-3 py-2">int is arbitrary-precision; <code>/</code> always float, <code>//</code> floors</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>typeof x</code></td>
        <td class="border border-border px-3 py-2"><code>type(x)</code> / <code>isinstance(x, T)</code></td>
        <td class="border border-border px-3 py-2"><code>type()</code> returns the class object</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="en">
Why does `b = a` not copy? Because Python (like JS for objects) has *reference semantics*: every variable is a name bound to an object, and assignment rebinds the name without touching the object. Mutable objects (`list`, `dict`, `set`) are shared; immutable ones (`int`, `str`, `tuple`, `frozenset`) can't be changed in place, so sharing them is invisible. This is also why a mutable default argument — `def f(items=[])` — is a famous footgun: the list is created once at definition and shared across every call.
</Inset>

<TradeoffMatrix
  id="py-syntax-types-tradeoff-nullcheck"
  lessonSlug="01-syntax-and-types"
  lang="en"
  prompt="You must check whether a variable holding an optional value is absent. Pick the correct, idiomatic test."
  options={[
    { name: "if x is None:", summary: "Identity vs the singleton.", correct: true, justification: "None is a guaranteed singleton, so identity (is) is exact, fast, and can't be fooled by a custom __eq__. This is the documented idiom (PEP 8 mandates `is`/`is not` for None) and it precisely answers 'is this the absent value?' without conflating it with other falsy values." },
    { name: "if x == None:", summary: "Value comparison.", justification: "Works for plain None but is wrong style and fragile: a class can override __eq__ so that `obj == None` returns True for a non-None object, silently breaking the check. PEP 8 explicitly says use `is`. Avoid." },
    { name: "if not x:", summary: "Truthiness.", justification: "Too broad — it's True for None AND for 0, '', [], {}, and False. If x can legitimately be 0 or an empty collection, this misclassifies a present-but-falsy value as absent. Only correct when you genuinely mean 'falsy', not 'absent'." },
    { name: "if x === undefined:", summary: "JS habit.", justification: "Python has neither === nor undefined. This is a syntax error. The single nullish value is None, tested with `is None`." },
  ]}
/>

</Explanation>

<KeyTakeaway>Indentation is the block syntax; names are references (no copy on assign); int is unbounded and `/` is always float; `None` is the one nullish (test with `is`); `==` is value, `is` is identity (beware small-int caching); and type hints never run — only `mypy`/`pyright` check them.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="py-syntax-types-retrieval"
  lang="en"
  questions={[
    { q: "Explain the difference between == and is in Python, and why `257 is 257` can be False while `256 is 256` is True.", a: "== compares value: it dispatches to the object's __eq__ method and asks 'do these represent equal data?'. is compares identity: it asks 'are these two names bound to the literally same object in memory?' (equivalent to id(x) == id(y)). They answer different questions, so they routinely disagree — [] == [] is True (equal value) but [] is [] is False (two distinct list objects). The integer case is a CPython implementation detail called small-int caching: the interpreter pre-allocates and reuses one shared object for every integer from −5 to 256, so both 256 literals resolve to that same cached object and `is` is True. 257 is outside that range, so each literal builds a fresh object; they're value-equal (== is True) but distinct identities (is is False). The lesson is to use is only for None, True, False and sentinels, and == for value comparison everywhere else — never rely on integer identity." },
    { q: "A teammate from TypeScript adds type hints everywhere and expects bad calls to fail at runtime like a compiled TS build. What's wrong with that expectation, and what actually enforces the types?", a: "Python type hints (PEP 484) are not runtime checks. The interpreter parses and stores them (in __annotations__) but never validates them while the program runs — passing a str where an int is hinted executes normally until some operation happens to fail on the actual value, or silently produces wrong results. Enforcement comes from a separate static type checker — mypy or pyright — that you run, typically in CI, before the code ships. The TS comparison is subtle: TS types are also erased and don't exist at runtime, but a failing TS type-check normally fails the build so bad code doesn't ship; in Python, the program runs whether or not anyone ran the checker. So hints are advisory documentation that a checker can verify, not a runtime guard. For actual runtime validation of incoming data you reach for a library like Pydantic or hand-written checks, not the type system itself." },
  ]}
/>

<Recap lang="en">
Coming from JS/TS, Python's syntax and type model rearrange a handful of fundamentals. Blocks are defined by indentation under a header line ending in `:` — the whitespace is the syntax, and there are no braces or semicolons. Variables need no keyword; assignment declares and binds, and a name is a reference to an object, so `b = a` shares rather than copies — there is no built-in `const`, only immutable values. The numeric model splits JS's single number into an arbitrary-precision `int` and an IEEE-754 `float`, with `/` always yielding a float and `//` doing floor division. Strings interpolate via f-strings and never coerce numbers implicitly. Truthiness treats `0`, `""`, `None`, and every empty collection as falsy, and `None` is the lone nullish value — replacing both JS `null` and `undefined` and tested with `is None`. The sharpest gotcha is `==` (value) versus `is` (identity): they disagree on equal-but-distinct objects, and small-int caching makes `is` deceptively return True for ints in −5..256. Finally, type hints look like TS annotations but are never enforced at runtime; `mypy` or `pyright` checks them statically, and unlike a TS build that blocks on type errors, a Python program runs regardless — so treat hints as verifiable documentation, not a guard. Now when you see a reviewer write `if x is None` where you'd write `== None`, or strip your `[]` default to `None`, you'll know exactly why — and you'll do the same.
</Recap>
