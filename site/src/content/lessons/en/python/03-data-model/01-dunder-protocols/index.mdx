---
concepts:
- python-dunders
deepensInto: []
estMin: 17
lang: en
lessonType: topic
level: middle
order: 1
prereqs: []
slug: "01-dunder-protocols"
sources:
- https://docs.python.org/3/reference/datamodel.html
spiral: []
status: ready
summary: 'Dunders are how objects plug into Python syntax: __repr__ for debugging, the __eq__/__hash__ contract (define eq, lose hash), the __bool__/__len__ truthiness chain, and operator dunders that return NotImplemented so the other operand gets a turn. Protocols beat inheritance.'
title: 'Dunder protocols: __repr__, the eq/hash contract, truthiness, and NotImplemented'
track: "python"
unit: "03-data-model"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import eqHashContract from "./infographics/eq-hash-contract.svg?raw";

<Hook>
The deduplication job had run nightly for a year. Then someone added a sensible-looking `__eq__` to the `Order` class so tests could compare orders by id — and the next night the job crashed with `TypeError: unhashable type: 'Order'`. Nobody had touched the dedup code; it still did `seen = set()` and `if order in seen`. The change that broke it was three lines away in a different file: defining `__eq__` without `__hash__` makes Python silently set `__hash__ = None`, because equal objects must hash equal and Python cannot guarantee that for your custom equality. The author had run the comparison tests — all green — and shipped. The incident review's takeaway went on the team wiki: dunder methods are not isolated conveniences, they are a contract system. Touch one clause of the data model and the interpreter holds you to the rest of the contract, sometimes in a job that runs at 3 a.m.
</Hook>

<Crux>Dunders are protocols: define __eq__ and you lose __hash__; return NotImplemented so Python can try the other operand.</Crux>

<Explanation>

In ten minutes you will know exactly which dunder broke the nightly job — and why fixing it requires touching two methods, not one.

## __repr__ and __str__: the debugging contract

`repr(x)` calls `x.__repr__()` and targets developers: unambiguous, ideally `eval`-able or at least identifying (`Order(id=42, total=Decimal('19.90'))`). `str(x)` calls `__str__` and targets users; if you don't define it, `str` falls back to `__repr__` — so define `__repr__` first and `__str__` only when you need a friendlier form. The default `__repr__` is `<Order object at 0x7f...>`, which is what your logs and tracebacks show at 3 a.m. when you skipped this. Containers always use `repr` for their elements — `print([order])` shows the repr even though you called `print` — which is exactly the behavior you want when reading a dumped list of 200 orders.

```python
from decimal import Decimal

class Order:
    def __init__(self, id: int, total: Decimal):
        self.id, self.total = id, total

    def __repr__(self) -> str:
        # !r recursively uses repr of the fields — Decimal('19.90'), not 19.90
        return f"Order(id={self.id!r}, total={self.total!r})"

print(repr(Order(42, Decimal("19.90"))))  # Order(id=42, total=Decimal('19.90'))
```

## The __eq__/__hash__ contract

The rule the interpreter enforces: if `a == b` then `hash(a) == hash(b)` must hold, because dicts and sets locate objects by hash bucket first, equality second. Default `__eq__` is identity (`is`), default `__hash__` is derived from `id()` — consistent. The moment you define `__eq__`, Python sets `__hash__ = None` on that class: your objects become unhashable until you supply a `__hash__` that agrees with your equality (hash the same tuple of fields you compare). The trap from the Hook is the silent half of this: nothing fails at class definition time, only at the first `set`/`dict` use. And a second-order trap: hashable objects must be *effectively immutable* in their hashed fields — mutate `order.id` after inserting into a set and the object is now in the wrong bucket, present but unfindable. `x in s` returns False while `x` sits in `s`.

```python
class Order:
    def __init__(self, id: int):
        self.id = id

    def __eq__(self, other):
        if not isinstance(other, Order):
            return NotImplemented   # not False — see below
        return self.id == other.id

    def __hash__(self):
        return hash(self.id)        # same fields as __eq__, or the contract breaks
```

<Infographic
  svg={eqHashContract}
  label="Side-by-side comparison of the __eq__/__hash__ contract. Left (correct): define __eq__ AND __hash__ over the same fields, so equal objects hash equal, instances stay hashable in sets and dicts, and hashed fields are kept immutable. Right (the trap): define __eq__ alone, so Python sets __hash__ = None, instances become unhashable with no error at class-definition time, and the code crashes only at the first set or dict use."
  caption="Equal objects must hash equal, so defining __eq__ without a matching __hash__ makes instances unhashable — and the failure surfaces not at definition time but at the first set/dict use, possibly in a job far from your diff."
  hue="var(--d-infra)"
/>

<Quiz
  id="01-dunder-protocols-quiz-1"
  lessonSlug="01-dunder-protocols"
  lang="en"
  question="You add __eq__ to a class comparing by id, run the equality tests (green), and ship. A nightly job doing `if obj in seen_set` now crashes with TypeError. Why?"
  choices={[
    { label: "Defining __eq__ sets __hash__ = None on the class, so instances became unhashable and set membership raises", correct: true },
    { label: "Sets require __lt__ for ordering, which the class never defined", misconception: "Sets are hash-based, not ordered — they never call __lt__. Ordering dunders matter for sorted() and comparisons, not for set membership." },
    { label: "The __eq__ signature is wrong; it must accept only objects of the same class", misconception: "__eq__ always receives arbitrary objects — that is why the isinstance check and NotImplemented exist. A 'wrong' operand type does not raise TypeError from set insertion." },
    { label: "Sets call __eq__ on every element, and comparing against non-Order elements raised", misconception: "Sets compare only within a hash bucket, and the crash is unhashable type at insertion — before any __eq__ runs. The hash is gone, not the equality." },
  ]}
/>

## Truthiness and length: the __bool__/__len__ chain

When you write `if response:` on your own collection class, does Python call `__eq__`, `__bool__`, or something else entirely? The answer determines whether an empty collection is falsy — or whether every instance silently passes the check.

`if x:` never calls `__eq__`. It calls `bool(x)`, which tries `__bool__` first; if absent, it falls back to `__len__` (nonzero length is truthy); if both are absent, every instance is truthy. This chain is why `if response:` on a custom collection does the right thing for free once you define `__len__` — and why it backfires on objects where emptiness is not falseness. The classic production bite: an ORM query object or a `datetime.time` of midnight ending up in `if value:` and being treated as "no value". If your class has a length but "empty" is still meaningful data, either define `__bool__` returning `True` explicitly or make call sites use `if value is not None:`.

## Operator dunders and NotImplemented

`a + b` runs `type(a).__add__(a, b)`; if that returns the sentinel `NotImplemented`, Python tries the reflected `type(b).__radd__(b, a)`; only if both decline does it raise `TypeError`. (One refinement: if `type(b)` is a *subclass* of `type(a)`, the reflected method gets the first try, so subclasses can override behavior in mixed expressions.) `NotImplemented` is a return value meaning "I don't know this operand type, ask the other side" — it is not the exception `NotImplementedError`, which is what abstract methods raise to mean "subclass forgot to implement me". Returning `False` from `__eq__` for unknown types is the subtle version of this bug: you make `Order(1) == 1` definitively false instead of letting `int` have a say, and you break symmetric behavior for someone else's wrapper type. This is also why protocols beat inheritance in Python: `len()`, `in`, iteration, arithmetic all dispatch on dunders, so any class implementing the methods participates — no base class required. Duck typing is dunder dispatch.

<Quiz
  id="01-dunder-protocols-quiz-2"
  lessonSlug="01-dunder-protocols"
  lang="en"
  question="Your Money.__add__ raises NotImplementedError for unknown operand types. A teammate's Discount class defines __radd__ to handle Money + Discount. What happens to `money + discount`?"
  choices={[
    { label: "It crashes with NotImplementedError — raising aborts dispatch, so Discount.__radd__ never gets called; __add__ should return NotImplemented instead", correct: true },
    { label: "Python catches the exception and falls back to __radd__, so it works", misconception: "The fallback is triggered only by the NotImplemented return value. A raised exception propagates immediately — Python never swallows exceptions to retry the other operand." },
    { label: "It works because __radd__ is checked before __add__", misconception: "The left operand's __add__ is tried first (reflected first only when the right type is a subclass of the left). Discount is not a Money subclass, so __add__ runs — and raises." },
    { label: "NotImplementedError and NotImplemented are the same thing spelled differently", misconception: "They are unrelated: NotImplemented is a sentinel value for binary-operator dispatch; NotImplementedError is an exception for unimplemented abstract methods. Confusing them breaks operator fallback." },
  ]}
/>

</Explanation>

<SequenceDiagram
  label="How Python evaluates a + b over time: the interpreter asks type(a).__add__ first; a NotImplemented return hands the expression to the reflected type(b).__radd__; if both decline, the result is TypeError"
  hue="var(--d-systems)"
  actors={["Interpreter", "type(a)", "type(b)"]}
  messages={[
    { from: "Interpreter", to: "type(a)", label: "__add__(a, b)" },
    { from: "type(a)", to: "Interpreter", label: "NotImplemented" },
    { from: "Interpreter", to: "type(b)", label: "__radd__(b, a)" },
    { from: "type(b)", to: "Interpreter", label: "value / declines" },
  ]}
  caption="NotImplemented is a routing signal, not an error: it hands the expression to the other operand. If both sides decline, Python raises TypeError. Raising NotImplementedError from __add__ aborts this handshake — the reflected method never runs and other types can never interoperate with yours."
/>

<KeyTakeaway>Dunders are the contract between your class and Python syntax. `__repr__` is for developers and is what containers print. Defining `__eq__` silently sets `__hash__ = None` — supply a hash over the same fields, and keep them immutable. `bool(x)` tries `__bool__`, then `__len__`, then defaults to truthy. Binary operators dispatch left `__add__`, then reflected `__radd__` on `NotImplemented` — return the sentinel, never raise `NotImplementedError`, or you break interop with other types.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-dunder-protocols-retrieval"
  lang="en"
  questions={[
    { q: "State the __eq__/__hash__ contract, what Python does when you define only __eq__, and the mutation trap that survives even a correct implementation.", a: "Contract: if a == b, then hash(a) == hash(b), because dicts and sets find objects by hash bucket first and confirm with equality second. Define __eq__ alone and Python sets __hash__ = None on the class — instances become unhashable, which fails not at class definition but at the first set/dict use. The fix is a __hash__ over the same tuple of fields __eq__ compares. The surviving trap is mutation: hash an object, insert it into a set, then mutate a hashed field — the object now lives in the wrong bucket, so `x in s` returns False even though x is physically inside s. Hashed fields must be effectively immutable for the object's container lifetime." },
    { q: "Walk through what happens for `a + b` when the types differ, and explain why returning False or raising NotImplementedError from a dunder are both bugs.", a: "Python calls type(a).__add__(a, b). If it returns the sentinel NotImplemented, Python tries the reflected type(b).__radd__(b, a); if type(b) subclasses type(a), the reflected method is tried first so subclasses can win. Only when both return NotImplemented does Python raise TypeError. Returning False from __eq__ for unknown types is a bug because it gives a definitive 'not equal' instead of letting the other operand answer — breaking symmetry with wrapper types. Raising NotImplementedError is worse: an exception aborts dispatch immediately, so __radd__ never runs and no other type can ever interoperate with yours. NotImplemented is a routing value; NotImplementedError is for abstract methods a subclass forgot." },
  ]}
/>

<Recap lang="en">
The data model is Python's protocol system: syntax like `==`, `in`, `if x:`, `len()`, and `+` dispatches to dunder methods, so any class implementing the right methods participates — duck typing is dunder dispatch, no inheritance required. `__repr__` serves developers (unambiguous, field-revealing, used by containers and tracebacks); `__str__` serves users and falls back to `__repr__`. The eq/hash contract is the unit's sharpest edge: equal objects must hash equal, so defining `__eq__` makes Python silently set `__hash__ = None`, and the failure surfaces only at the first set or dict use — possibly in a nightly job far from your diff. Restore `__hash__` over the same fields you compare and treat those fields as immutable, or objects get lost inside their own containers. Truthiness walks a chain — `__bool__`, then `__len__`, then default-truthy — which is convenient for collections and a trap for objects where empty is valid data. Binary operators run left `__add__` first, hand off to reflected `__radd__` when they return the `NotImplemented` sentinel (subclass right operands get priority), and raise `TypeError` only when both sides decline. Return `NotImplemented` for unknown operand types; raising `NotImplementedError` aborts dispatch and forecloses interop with types you have never met. Now when you see a `TypeError: unhashable type` surface far from your diff, or a midnight job that breaks on `in set` after a perfectly green test run, you know exactly which contract was touched and which clause was left unfulfilled.
</Recap>
