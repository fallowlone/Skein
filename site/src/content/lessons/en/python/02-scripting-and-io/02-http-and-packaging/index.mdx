---
concepts:
- requests-http
- raise-for-status
- request-timeout
- llm-api-call
- venv-isolation
- requirements-pinning
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-http-and-packaging
sources:
- https://requests.readthedocs.io/en/latest/user/quickstart/
- https://www.python-httpx.org/async/
- https://docs.python.org/3/library/venv.html
- https://packaging.python.org/en/latest/guides/writing-pyproject-toml/
spiral: []
status: ready
summary: 'A useful Python tool is two skills: HTTP calls with requests (always a timeout, raise_for_status, env-var keys) and venv isolation that keeps one project deps from breaking another.'
title: 'HTTP requests, venv and packaging'
track: python
unit: 02-scripting-and-io
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
You write a 30-line Python script that calls an LLM API to summarise a batch of tickets. It works on your laptop. You hand it to a teammate; `pip install requests` fails — their global Python already pins an incompatible `urllib3` for another project. You fix that, and the script hangs forever on one request because the provider had a blip and you never set a timeout. Then a security review finds the API key hard-coded in the file you just pushed. None of these are hard problems — they are the three things a JS/TS dev coming to Python keeps tripping over: HTTP done carelessly, no environment isolation, and secrets in source.
</Hook>

<Crux>A useful Python tool is HTTP with a timeout, status check and env-var key — run in a venv so one project's deps never break another's.</Crux>

In the next few minutes you will see exactly why each of the three teammate failures happens and the minimal change that prevents each one from reaching production.

<Explanation>

## HTTP with `requests`: the four things you must not skip

`requests` is Python's de-facto HTTP client — it is to Python what `fetch` is to the browser, except synchronous and far more ergonomic than the standard-library `urllib`. A GET with query params and a JSON response is three lines:

```python
import requests

resp = requests.get(
    "https://api.example.com/search",
    params={"q": "python", "limit": 10},
    headers={"Accept": "application/json"},
    timeout=10,
)
resp.raise_for_status()          # turn a 4xx/5xx into an exception
data = resp.json()               # parsed dict, like fetch's await resp.json()
```

A POST that sends JSON is the same shape — pass `json=` and `requests` serialises the body and sets `Content-Type: application/json` for you:

```python
resp = requests.post(url, json={"name": "ada"}, timeout=10)
```

Two habits separate a script that survives production from one that hangs or lies:

- **Always pass `timeout=`.** This is the senior point, and the one beginners miss. By default `requests` waits *forever*. A single slow or dead upstream will freeze your whole script — or every worker in a pool — with no error and no recovery. `fetch` has the same gap (you need an `AbortController`); in Python you just pass a number.
- **Call `raise_for_status()`.** Unlike `fetch`, which rejects only on network failure and happily resolves a 500, `requests` does not raise on a 4xx/5xx by default — `resp` looks fine and `resp.json()` may return an error body you treat as data. `raise_for_status()` converts a bad status into an `HTTPError` you can catch. (`fetch` is the mirror image: you must check `resp.ok` yourself.)

For many parallel calls or an async app, reach for **`httpx`** — same API surface as `requests`, but it adds `async`/`await` and HTTP/2. A retry with backoff is worth wiring in for any real upstream: on a `429` or `503`, wait `0.5s, 1s, 2s…` and retry a few times rather than failing on the first blip. (Libraries like `tenacity`, or `urllib3`'s `Retry` adapter, do this for you.)

## The AI-glue payoff: calling an LLM HTTP API safely

Most "AI integration" is exactly this pattern: POST a JSON body to a provider endpoint with an auth header, then parse the JSON that comes back. The senior detail is **where the key lives** — never in source.

```python
import os
import requests

api_key = os.environ.get("LLM_API_KEY")  # read from the environment, not the file
if not api_key:
    raise RuntimeError("LLM_API_KEY is not set")

resp = requests.post(
    "https://api.provider.example/v1/chat/completions",
    headers={"Authorization": f"Bearer {api_key}"},
    json={
        "model": "some-model",
        "messages": [{"role": "user", "content": "Summarise this ticket: ..."}],
    },
    timeout=30,                          # LLMs are slow; give them room, but bound it
)
resp.raise_for_status()
reply = resp.json()["choices"][0]["message"]["content"]
```

`os.environ["LLM_API_KEY"]` is Python's `process.env.LLM_API_KEY`. You set it in your shell, a `.env` loaded by `python-dotenv`, or your deploy platform's secret store — and the key never appears in the repo, in `git history`, or in a screen-share. Hard-coding it is the single most common way credentials leak. The generous `timeout=30` matters here too: model responses are slow, so a tight timeout cancels good requests, but *no* timeout means one stuck generation hangs your tool indefinitely.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Node / TypeScript</th>
        <th class="border border-border px-3 py-2 text-left">Python</th>
        <th class="border border-border px-3 py-2 text-left">Note</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>fetch()</code></td>
        <td class="border border-border px-3 py-2"><code>requests</code> / <code>httpx</code></td>
        <td class="border border-border px-3 py-2">requests is sync; httpx adds async + HTTP/2</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>await resp.json()</code></td>
        <td class="border border-border px-3 py-2"><code>resp.json()</code></td>
        <td class="border border-border px-3 py-2">synchronous; no <code>await</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">check <code>resp.ok</code> yourself</td>
        <td class="border border-border px-3 py-2"><code>resp.raise_for_status()</code></td>
        <td class="border border-border px-3 py-2">neither raises on 4xx/5xx by default</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>AbortController</code></td>
        <td class="border border-border px-3 py-2"><code>timeout=</code></td>
        <td class="border border-border px-3 py-2">requests waits forever without it</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>process.env.X</code> + dotenv</td>
        <td class="border border-border px-3 py-2"><code>os.environ["X"]</code> + python-dotenv</td>
        <td class="border border-border px-3 py-2">keep secrets out of source</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>package.json</code></td>
        <td class="border border-border px-3 py-2"><code>pyproject.toml</code> / <code>requirements.txt</code></td>
        <td class="border border-border px-3 py-2">project + dependency manifest</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>node_modules/</code></td>
        <td class="border border-border px-3 py-2"><code>.venv/</code></td>
        <td class="border border-border px-3 py-2">per-project install location</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>npm install</code></td>
        <td class="border border-border px-3 py-2"><code>pip install</code> (or <code>uv</code>)</td>
        <td class="border border-border px-3 py-2">resolve + install deps</td>
      </tr>
    </tbody>
  </table>
</div>

<Quiz
  id="02-http-and-packaging-quiz-1"
  lessonSlug="02-http-and-packaging"
  lang="en"
  question="Your script calls an LLM API and occasionally hangs for minutes with no error. What is the most likely cause?"
  choices={[
    { label: "No timeout= was passed, so requests waits forever when the upstream stalls — bound it (e.g. timeout=30) and add a retry on 429/503", correct: true },
    { label: "resp.json() is slow on large bodies", misconception: "Parsing a JSON body is milliseconds, not minutes. A multi-minute hang with no error is the signature of a request blocked on the network with no timeout, not of JSON parsing." },
    { label: "requests can't talk to HTTPS endpoints", misconception: "requests handles TLS out of the box. The symptom — hanging, not failing — points to a missing timeout on a stalled connection, not a protocol problem." },
    { label: "You forgot to await the response", misconception: "requests is synchronous — there is nothing to await. (That's httpx's async mode.) The hang comes from the default infinite timeout, not from missing await." },
  ]}
/>

<Quiz
  id="02-http-and-packaging-quiz-2"
  lessonSlug="02-http-and-packaging"
  lang="en"
  question="A provider returns a 500 with an error JSON body. Your code does resp.json() without raise_for_status(). What happens?"
  choices={[
    { label: "requests does not raise on 5xx by default, so you parse the error body and treat it as a valid result — raise_for_status() would have turned it into a catchable HTTPError", correct: true },
    { label: "requests automatically retries the 500 until it succeeds", misconception: "requests does not retry on its own — you wire that up (tenacity, urllib3 Retry). Without raise_for_status the 500 silently flows through as data." },
    { label: "resp.json() raises because the status is 500", misconception: "resp.json() only fails if the body isn't valid JSON. A 500 with a JSON error body parses fine — the bad status is invisible unless you call raise_for_status()." },
    { label: "The 500 is returned as None", misconception: "resp is a normal Response with a real body. The status code is in resp.status_code; nothing becomes None. You must check the status explicitly." },
  ]}
/>

## Environments and packaging: why `venv` exists

Ask yourself: how many Python projects do you or your teammates have on one machine? Each one has opinions about which library versions it needs — and by default Python lets them all fight over the same global install folder. Here is the teammate's failure from the hook. Python, by default, installs packages into one **global** site-packages shared by every project on the machine. Project A needs `urllib3==1.26`; project B needs `urllib3==2.x`. Installed globally, the second `pip install` overwrites the first — and now whichever project loads the wrong version breaks. There is no `node_modules` per project to keep them apart.

A **virtual environment** (`venv`) is that per-project boundary. It is a lightweight, isolated copy of the interpreter with its own site-packages, so each project's dependencies live in their own folder and never collide:

```bash
python -m venv .venv          # create an isolated env in ./.venv
source .venv/bin/activate     # macOS/Linux  (Windows: .venv\Scripts\activate)
pip install requests httpx    # installs INTO .venv, not globally
```

While activated, `python` and `pip` point at `.venv` — analogous to how `node_modules` scopes packages to one project, except a venv isolates the interpreter itself, not just the libraries. You `.gitignore` the `.venv/` folder (like `node_modules/`) and commit a manifest instead so anyone can recreate it.

<SequenceDiagram
  label="venv create then install then run isolation flow"
  hue="var(--d-systems)"
  actors={["You", "venv", "PyPI"]}
  messages={[
    { from: "You", to: "venv", label: "python -m venv .venv" },
    { from: "You", to: "venv", label: "source activate" },
    { from: "You", to: "PyPI", label: "pip install requests" },
    { from: "PyPI", to: "venv", label: "→ .venv/site-packages" },
    { from: "You", to: "venv", label: "python tool.py" },
    { from: "venv", to: "You", label: "isolated deps, no clash" },
  ]}
  caption="The venv is the boundary: pip installs into .venv/site-packages, and the activated python runs against only those deps — so project A's urllib3 never overwrites project B's."
/>

## Pinning: `requirements.txt` and the move to `pyproject.toml`

Isolation is half the story; **reproducibility** is the other half. If you `pip install requests` today and your teammate does it next month, you may get different versions. Pin them. The classic file is `requirements.txt`:

```text
requests==2.32.3
httpx==0.27.2
```

Recreate the exact set with `pip install -r requirements.txt` inside a fresh venv. This is `package.json` + a lockfile, split across convention rather than one file. The modern, standardised home for project metadata and dependencies is **`pyproject.toml`** (PEP 621) — declaring deps there, and using a fast resolver like **`uv`** or **`poetry`**, gives you a real lockfile and one-command setup, much closer to the `npm install` experience. For a quick script `requirements.txt` is fine; for anything you ship or share, prefer `pyproject.toml`.

<Inset kind="why" lang="en">
Why not just `pip install --user` or install globally and skip the venv? Because "global" is shared mutable state across every project and even the OS's own Python tooling. One project's pin silently changes another's runtime, upgrades become Russian roulette, and on some systems pip into the system Python is blocked outright (PEP 668's "externally-managed-environment"). A venv per project makes dependencies explicit, disposable, and reproducible — delete `.venv`, recreate from the manifest, done.
</Inset>

<TradeoffMatrix
  id="02-http-and-packaging-tradeoff-1"
  lessonSlug="02-http-and-packaging"
  lang="en"
  prompt="You're starting a small Python tool that two teammates will also run. How do you manage its dependencies?"
  options={[
    { name: "A venv per project + a pinned manifest (requirements.txt or pyproject.toml), .venv gitignored", summary: "Isolated and reproducible.", correct: true, justification: "This is the standard. The venv isolates this project's deps from every other project and the system Python, so no install can break a neighbour; the pinned manifest makes the exact set reproducible on any machine — delete .venv, recreate from the manifest. It mirrors the node_modules + package.json model your teammates already know, and sidesteps PEP 668's block on installing into the system interpreter." },
    { name: "Install everything globally with pip install so it's available everywhere", summary: "One shared site-packages.", justification: "Global is shared mutable state: project A's urllib3 pin overwrites project B's, upgrades for one tool silently break another, and modern systems often refuse pip into the system Python (externally-managed-environment). Convenient for a one-off REPL, dangerous the moment you have two projects or a teammate." },
    { name: "A venv but no manifest — everyone runs pip install <pkg> by hand", summary: "Isolated, not reproducible.", justification: "The isolation is right but you've dropped reproducibility: nothing records the exact versions, so two machines drift apart and 'works on mine' returns. Always commit a requirements.txt or pyproject.toml so the env can be rebuilt deterministically." },
    { name: "Commit the whole .venv/ folder to git so teammates get it instantly", summary: "Ship the env itself.", justification: "A venv contains absolute paths and platform-specific binaries; it does not move between machines or OSes and bloats the repo. Gitignore .venv and commit the manifest instead — that's the portable, reviewable artifact, exactly as you gitignore node_modules and commit package.json." },
  ]}
/>

<DragOrder
  id="02-http-and-packaging-drag-1"
  lessonSlug="02-http-and-packaging"
  lang="en"
  prompt="Order the steps to set up and run an HTTP tool that calls an LLM API safely:"
  items={[
    { id: "a", label: "python -m venv .venv — create the isolated environment" },
    { id: "b", label: "source .venv/bin/activate — activate it so pip/python point at it" },
    { id: "c", label: "pip install -r requirements.txt — install pinned deps into the venv" },
    { id: "d", label: "export LLM_API_KEY=… (or load a .env) — key in the environment, not source" },
    { id: "e", label: "python tool.py — POST with auth header + timeout, then raise_for_status()" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Two skills make a useful Python tool. HTTP: `requests`/`httpx`, always a `timeout=`, `raise_for_status()`, and the key from `os.environ` — never source. Environment: a `venv` per project for isolation, a pinned `requirements.txt`/`pyproject.toml` for reproducibility.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-http-and-packaging-retrieval"
  lang="en"
  questions={[
    { q: "Walk through making a safe POST to an LLM API in Python: what must you set, and how does each differ from JS fetch?", a: "Use requests.post(url, headers={...}, json={...}, timeout=N). Four things matter. (1) The body: pass json={...} and requests serialises it and sets Content-Type: application/json — the parsed reply comes back from resp.json(), synchronously, with no await, unlike fetch where you await resp.json(). (2) Auth: put the key in headers as Authorization: Bearer <key>, and read that key from os.environ['LLM_API_KEY'], Python's process.env — never hard-code it, so it stays out of the repo and git history. (3) timeout=: requests waits forever by default, so a stuck upstream hangs your whole script; pass a bound (e.g. 30s for a slow LLM). fetch has the same gap and needs an AbortController. (4) resp.raise_for_status(): requests does NOT raise on a 4xx/5xx, so without it you'd parse an error body as a valid result; raise_for_status turns a bad status into a catchable HTTPError. fetch is the mirror — it resolves a 500 and you must check resp.ok yourself. For many calls or async, swap requests for httpx, same API plus async/await and HTTP/2, and add a retry-with-backoff on 429/503." },
    { q: "Your teammate's pip install breaks another project. Explain why, and the venv + manifest fix end to end.", a: "By default pip installs into one global site-packages shared by every project and the system Python. Project A pins urllib3==1.26 and project B needs urllib3==2.x; installed globally the second install overwrites the first, so whichever project loads the wrong version breaks — there's no per-project node_modules to keep them apart, and many systems now block pip into the system interpreter (PEP 668). The fix is a virtual environment per project: python -m venv .venv creates an isolated interpreter with its own site-packages; source .venv/bin/activate makes python and pip point at it; pip install then writes into .venv, not globally, so projects can't collide. For reproducibility you commit a manifest — requirements.txt with pinned versions (requests==2.32.3) or, better for shipped projects, pyproject.toml with a resolver like uv/poetry for a real lockfile — and gitignore the .venv folder itself, since it holds absolute paths and platform binaries that don't move between machines. Anyone recreates the exact env with python -m venv .venv && pip install -r requirements.txt. It's the node_modules + package.json model, with the venv isolating the interpreter, not just the libraries." },
  ]}
/>

<Recap lang="en">
A useful Python tool is two skills working together. First, HTTP: `requests` is Python's `fetch`, but synchronous — `requests.get/post` with `params`/`json`, and `resp.json()` with no `await`. The senior habits are non-negotiable: always pass `timeout=` (requests waits forever otherwise, and one stalled upstream hangs the whole script), always call `raise_for_status()` (requests does not raise on a 4xx/5xx, so an error body sails through as data), and for many or async calls reach for `httpx` plus a retry-with-backoff on 429/503. The AI-glue payoff is the same shape: POST a JSON body with an `Authorization: Bearer` header to a provider, read the key from `os.environ` so it never lives in source, and give slow models a generous-but-bounded timeout. Second, environments: Python installs globally by default, so two projects' dependency pins collide — a `venv` per project is the isolating boundary (its own interpreter and site-packages), gitignored like `node_modules`. Pin versions in a `requirements.txt`, or better a `pyproject.toml` with `uv`/`poetry`, so the exact environment is reproducible on any machine. Isolation plus pinning is what turns "works on my laptop" into a tool your teammates can actually run. Now when you see a hung script or a teammate's broken install, you know which two missing lines to add before anything else: `timeout=` and `venv`.
</Recap>
