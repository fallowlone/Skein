---
concepts:
- context-managers
- text-vs-binary-encoding
- line-by-line-reading
- pathlib
- argparse
- exit-codes
deepensInto: []
estMin: 17
lang: en
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-files-and-cli
sources:
- https://docs.python.org/3/library/functions.html#open
- https://docs.python.org/3/library/argparse.html
- https://docs.python.org/3/library/pathlib.html
- https://docs.python.org/3/library/sys.html#sys.exit
spiral: []
status: ready
summary: 'A real Python script reads CLI arguments, opens files under a with-block that
  closes them even on exception, streams large files line by line, and exits with a
  status code the shell can branch on.'
title: 'Files and CLI arguments'
track: python
unit: 02-scripting-and-io
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import encodingTrap from "./infographics/encoding-trap.svg?raw";

<Hook>
A nightly job that "worked on my machine" mangles every accented name in production. The cause is one line: `open("names.csv")`. On the developer's macOS, `open` defaulted to UTF-8; on the Linux box under a `C` locale it defaulted to ASCII-ish `latin-1`, so `José` round-tripped to garbage. Two days later a different script crashes mid-write and leaves a half-flushed, truncated output file because nothing ever closed the handle. Both bugs are the same lesson: file and CLI I/O in Python has defaults that quietly differ across machines, and a script that ignores them is a script that fails in CI, not on your laptop.
</Hook>

<Crux>A robust script reads args, opens files in a `with`-block that closes on error, sets utf-8 encoding, and returns a status code.</Crux>

By the end of this lesson you will know why those two production bugs happen, what the one-line fix is for each, and how to wire them together into a script that behaves identically on every machine.

<Explanation>

## `with`: the close that survives an exception

`open()` returns a file object that holds an OS resource — a file descriptor and a write buffer. If you never close it, two things rot. The descriptor leaks (the OS caps how many you may hold; a long-running process eventually hits `Too many open files`), and on a writable file your buffered bytes may never reach disk, so the output is silently truncated. The naive fix is `f.close()` at the end — but if anything between `open` and `close` raises, the `close` line is skipped and you leak anyway. This is the same failure as JavaScript code that forgets a `finally`.

The `with` statement solves it structurally. `open()` is a *context manager*: it defines `__enter__` (run on entry) and `__exit__` (run on exit). Python guarantees `__exit__` runs when the block ends **for any reason** — normal fall-through, `return`, or a raised exception that propagates out. For a file, `__exit__` calls `close()`, which flushes the buffer and releases the descriptor. That guarantee is the whole point: you get correct cleanup without a `try/finally` and without trusting yourself to remember.

```python
# Leaks the descriptor and may truncate output if transform() raises:
f = open("out.txt", "w", encoding="utf-8")
f.write(transform(data))   # if this raises, close() below never runs
f.close()

# Correct: __exit__ runs (and closes/flushes) even when transform() raises:
with open("out.txt", "w", encoding="utf-8") as f:
    f.write(transform(data))
# file is closed and flushed here, exception or not
```

<Inset kind="why" lang="en">
"Closed on exception" does not mean "swallows the exception." A plain `with` runs `__exit__` (closing the file) and then lets the exception keep propagating — your cleanup happens, but the error still crashes the script, which is what you want. Only a context manager that explicitly returns truthy from `__exit__` suppresses the error, and `open()`'s never does. So `with` gives you guaranteed cleanup *and* honest failures.
</Inset>

## Text vs binary, and the encoding you must name

Why does this matter more than it looks? Because your script will be deployed in containers, CI pipelines, and teammates' machines — none of which you control. `open()` has two modes. Text mode (the default, `"r"` / `"w"`) decodes bytes to `str` on read and encodes `str` to bytes on write, using an **encoding**. Binary mode (`"rb"` / `"wb"`) gives you raw `bytes` with no decoding — what you want for images, archives, or any non-text payload.

The trap is the default encoding. When you omit `encoding=`, text mode uses `locale.getpreferredencoding()`, which depends on the machine's locale environment — UTF-8 on a typical Mac, but possibly `cp1252` on Windows or `ascii`/`latin-1` under a bare `C` locale in a container. The same file then decodes differently on different hosts, which is exactly the production bug from the hook. The rule is unconditional: **for text files, always pass `encoding="utf-8"`** (or whatever the file actually is). Make the encoding explicit and the script behaves identically everywhere.

```python
# Reads correctly on every host, regardless of locale:
with open("names.csv", "r", encoding="utf-8") as f:
    header = f.readline()

# Binary: no decoding, you get bytes:
with open("logo.png", "rb") as f:
    magic = f.read(8)        # b'\x89PNG\r\n\x1a\n'
```

<Infographic
  svg={encodingTrap}
  label="Two-column comparison of open()'s text-mode encoding. Omitting the encoding argument falls back to the machine's locale default — utf-8 on Mac and modern Linux, cp1252 on Windows, latin-1 in a bare C-locale container — so the same file decodes differently per host. Passing encoding='utf-8' fixes the byte-to-string mapping so the file decodes identically on every host."
  caption="Leave encoding off and your bytes are hostage to the host's locale; naming utf-8 is the unconditional one-line fix that makes a script read the same file identically on every machine."
  hue="var(--d-infra)"
/>

## Read patterns: don't load a 4 GB file into RAM

How you read matters as much as that you read. `f.read()` returns the **entire** file as one string — fine for a small config, catastrophic for a multi-gigabyte log, because it pulls the whole thing into memory at once. `f.readlines()` is just as greedy: it builds a list of every line in memory. The memory-safe pattern is to **iterate the file object directly** — `for line in f:` — which streams one line at a time and never holds more than a single line plus a buffer, so a 4 GB file processes in constant memory.

```python
# DON'T on big files — both load everything into RAM:
data = f.read()              # whole file as one str
lines = f.readlines()        # list of every line

# DO — streams line by line, constant memory:
with open("app.log", "r", encoding="utf-8") as f:
    for line in f:           # one line at a time
        if "ERROR" in line:
            print(line, end="")
```

## `pathlib.Path`: paths as objects, not strings

Manipulating paths with string concatenation is how you ship `"logs" + "/" + name` that breaks on Windows and double-slashes on edge cases. `pathlib.Path` (modern, since 3.4) models a path as an object with operators and methods, replacing most of the old `os.path` string functions. The `/` operator joins segments portably, and `Path` carries `.exists()`, `.read_text()`, `.glob()`, and friends.

```python
from pathlib import Path

src = Path("data") / "input.csv"     # os.path.join(..) — but readable, cross-platform
if src.exists():
    text = src.read_text(encoding="utf-8")   # open+read+close in one call
out = src.with_suffix(".out.csv")            # data/input.out.csv
```

If you come from Node, the mental map is direct: `path.join` becomes the `/` operator, `fs.existsSync` becomes `.exists()`, and `fs.readFileSync(p, "utf8")` becomes `Path(p).read_text(encoding="utf-8")`.

## CLI input: `sys.argv`, then graduate to `argparse`

A script gets its arguments from `sys.argv`, a list where `sys.argv[0]` is the script name and the rest are the user's words. Reading it by hand is fine for a one-off, but it scales badly: no `--flags`, no type conversion, no usage message, and an out-of-range index is an unhandled `IndexError`.

```python
import sys
# Fragile: positional only, crashes on missing arg, everything is a str
infile = sys.argv[1]          # IndexError if the user forgot it
limit = int(sys.argv[2])      # ValueError on bad input, no help text
```

The right tool the moment a script has real options is **`argparse`** (standard library). You declare positionals and `--flags`, give them `type=` for conversion, and argparse parses `sys.argv`, validates, converts types, generates `-h/--help`, and on a bad invocation prints usage to stderr and exits with code 2 — all for free.

```python
import argparse

parser = argparse.ArgumentParser(description="Filter and transform a CSV.")
parser.add_argument("infile", help="path to the input CSV")          # positional
parser.add_argument("-o", "--out", help="output path (default: stdout)")
parser.add_argument("-n", "--limit", type=int, default=0,            # auto-converts to int
                    help="max rows to emit (0 = all)")
args = parser.parse_args()    # validates, fills defaults, exits 2 on misuse
# args.infile, args.out, args.limit are ready to use
```

Roughly, `process.argv` plus a flags library in Node collapses into `argparse` here — argument parsing, type coercion, and the `--help` screen in one declaration.

## stdin/stdout/stderr and the exit code that CI reads

When you write a script, the person running it is often not a person — it's cron, a Makefile, a GitHub Actions step. A well-behaved CLI tool participates in the shell. Normal output goes to **stdout** (`print()` writes there by default) so it can be piped. Diagnostics — errors, progress, warnings — go to **stderr** (`print(..., file=sys.stderr)`) so they don't corrupt piped data. And the script ends with an **exit code**: `0` means success, any non-zero means failure. The shell exposes it as `$?`, `&&` chains on it, and CI marks the step failed on non-zero — so the exit code is how every automated caller learns whether your script worked.

`sys.exit(code)` sets it. Pass an int for the status; pass a string and Python prints it to stderr and exits with code 1. Returning from `main()` without calling `sys.exit` exits 0. An uncaught exception exits with code 1 and a traceback on stderr.

```python
import sys

if not args.infile:
    print("error: no input file", file=sys.stderr)   # diagnostics → stderr
    sys.exit(2)                                       # non-zero → caller sees failure
print(result)                                         # data → stdout
sys.exit(0)                                           # explicit success
```

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Node / JS</th>
        <th class="border border-border px-3 py-2 text-left">Python</th>
        <th class="border border-border px-3 py-2 text-left">Note</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>fs.readFileSync(p, "utf8")</code></td>
        <td class="border border-border px-3 py-2"><code>open(p, encoding="utf-8").read()</code></td>
        <td class="border border-border px-3 py-2">Wrap in <code>with</code>; always name the encoding</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>createReadStream</code> + line split</td>
        <td class="border border-border px-3 py-2"><code>for line in f:</code></td>
        <td class="border border-border px-3 py-2">Streams big files in constant memory</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>process.argv</code></td>
        <td class="border border-border px-3 py-2"><code>sys.argv</code> / <code>argparse</code></td>
        <td class="border border-border px-3 py-2"><code>argv[0]</code> is the script; argparse for real flags</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>path.join</code>, <code>fs.existsSync</code></td>
        <td class="border border-border px-3 py-2"><code>Path / ".."</code>, <code>.exists()</code></td>
        <td class="border border-border px-3 py-2"><code>pathlib</code> models paths as objects</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>console.error</code></td>
        <td class="border border-border px-3 py-2"><code>print(..., file=sys.stderr)</code></td>
        <td class="border border-border px-3 py-2">Keep diagnostics off stdout</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>process.exit(code)</code></td>
        <td class="border border-border px-3 py-2"><code>sys.exit(code)</code></td>
        <td class="border border-border px-3 py-2">0 = ok; non-zero = failure for shell/CI</td>
      </tr>
    </tbody>
  </table>
</div>

Put the pieces together and a real script is a pipeline: parse arguments, read input, transform, write output (file or stdout), and exit with a status the caller can branch on.

<FlowDiagram
  label="Lifecycle of a Python CLI script from argument parsing through reading, transforming, writing, and exiting with a status code"
  hue="var(--d-systems)"
  perRow={5}
  nodes={[
    { id: "args", label: "Parse args", sub: "argparse" },
    { id: "read", label: "Read input", sub: "with open" },
    { id: "xform", label: "Transform", sub: "for line in f" },
    { id: "write", label: "Write out", sub: "file / stdout" },
    { id: "exit", label: "Exit", sub: "sys.exit" },
  ]}
  edges={[
    { from: "args", to: "read", label: "infile" },
    { from: "read", to: "xform", label: "lines" },
    { from: "xform", to: "write", label: "result" },
    { from: "write", to: "exit", label: "status" },
  ]}
  caption="args in → open under a with-block → stream + transform → write to file or stdout → exit 0 on success, non-zero on failure. The with-block closes the handle even if transform raises mid-pipeline."
/>

<Quiz
  id="01-files-and-cli-quiz-1"
  lessonSlug="01-files-and-cli"
  lang="en"
  question="Why prefer `with open(...)` over `f = open(...)` then `f.close()`?"
  choices={[
    { label: "open() is a context manager, so __exit__ runs and closes/flushes the file when the block ends for any reason — including a raised exception that propagates out", correct: true },
    { label: "with is faster because it skips buffering", misconception: "with changes nothing about buffering or speed; it is about guaranteed cleanup. Both forms buffer identically — the difference is whether close() runs when an exception is thrown." },
    { label: "with automatically retries the operation if it fails", misconception: "with has no retry semantics. It runs __exit__ (close/flush) on the way out and lets any exception keep propagating; it does not re-run the body." },
    { label: "f.close() does not actually flush the write buffer", misconception: "close() does flush. The real problem with the manual form is that if a line between open and close raises, the close line is skipped entirely — so nothing flushes and the descriptor leaks." },
  ]}
/>

<Quiz
  id="01-files-and-cli-quiz-2"
  lessonSlug="01-files-and-cli"
  lang="en"
  question="A script calls sys.exit(0) even when a file was missing and it printed an error. What breaks?"
  choices={[
    { label: "Callers can't detect the failure: the shell sees $? = 0 and && chains continue, and CI marks the step green even though the run was broken", correct: true },
    { label: "Nothing — printing the error message is enough", misconception: "A message on the terminal is invisible to automation. Pipelines, && chains, and CI branch on the exit code, not on what was printed. Exit non-zero on failure." },
    { label: "stdout gets corrupted", misconception: "The exit code is independent of stdout. The bug is purely that a failed run reports success via its status code, misleading every automated caller." },
    { label: "Python raises a SystemError for an inconsistent code", misconception: "Python does not validate that your exit code matches reality; sys.exit(0) always reports success. It is your job to exit non-zero when the work failed." },
  ]}
/>

<TradeoffMatrix
  id="01-files-and-cli-tradeoff-1"
  lessonSlug="01-files-and-cli"
  lang="en"
  prompt="You must process a 6 GB application log, keeping only ERROR lines. How do you read it?"
  options={[
    { name: "Iterate the file object: for line in f", summary: "Constant memory.", correct: true, justification: "Iterating the open file streams one line at a time, holding only the current line plus a buffer regardless of file size — so a 6 GB log processes in roughly constant memory. This is the standard pattern for large or unbounded inputs, and it composes naturally with a filter like `if 'ERROR' in line`." },
    { name: "data = f.read(), then split on newlines", summary: "Whole file in RAM.", justification: "read() pulls the entire 6 GB into a single string before you touch a byte — it will exhaust memory or thrash swap. read() is fine only for small files you know fit comfortably in RAM, like a config." },
    { name: "lines = f.readlines()", summary: "List of all lines.", justification: "readlines() is just as greedy as read(): it materializes a list containing every line in memory at once. Same out-of-memory failure on a multi-gigabyte file; use direct iteration instead." },
    { name: "Open in binary mode and read fixed-size chunks, splitting lines yourself", summary: "Manual chunking.", justification: "This works and is occasionally needed for exotic formats, but it is needless complexity here: you would hand-roll line splitting and decoding that text-mode iteration already does correctly. Reach for it only when you genuinely need byte-level control." },
  ]}
/>

</Explanation>

<KeyTakeaway>Open files under `with` so they close and flush even on exception; pass `encoding="utf-8"`; iterate big files line by line; use `pathlib` for paths and `argparse` for flags; `sys.exit` non-zero on failure so shell and CI can tell.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-files-and-cli-retrieval"
  lang="en"
  questions={[
    { q: "Explain exactly why `with open(...)` closes the file when an exception is raised, and what would go wrong with a plain open()/close() pair instead.", a: "open() returns a file object that is a context manager — it implements __enter__ (returned as the `as` target) and __exit__. The with statement guarantees __exit__ runs when the block is left for ANY reason: normal completion, a return, or an exception propagating out of the body. For a file, __exit__ calls close(), which flushes the write buffer to the OS and releases the file descriptor. So if a line inside the block raises, Python still runs __exit__ on the way out — the file is closed and flushed — and then lets the exception continue propagating (a plain with does not swallow it). With the manual form `f = open(...)`, body, `f.close()`, an exception in the body skips straight past the close() line. The descriptor then leaks (and long-running processes eventually hit the OS open-file limit) and, for a writable file, the buffered bytes are never flushed, so the output is silently truncated or empty. with removes the need for a try/finally and removes the chance you forget it." },
    { q: "What does the exit code of a script communicate, how do you set it in Python, and why does it matter more than a printed message?", a: "The exit code (a.k.a. status code) is the small integer a process returns when it ends: 0 means success and any non-zero value means failure. It is the machine-readable verdict that every caller reads. The shell exposes the last code as $?; `cmd && next` only runs `next` if cmd exited 0; CI marks a pipeline step failed the instant a command exits non-zero. A printed error message is for humans and is invisible to all of that automation. In Python you set it with sys.exit(code): an int becomes the status; a string is printed to stderr and the process exits 1; returning from main without calling sys.exit exits 0; and an uncaught exception exits 1 with a traceback on stderr. The practical rule: send data to stdout, send diagnostics to stderr, and call sys.exit with a non-zero code whenever the work actually failed — otherwise a broken run reports success and silently poisons pipelines and CI." },
  ]}
/>

<Recap lang="en">
A production-grade Python script treats file and CLI I/O as something that must behave identically on every host, not just your laptop. Open files under a `with`-block: `open()` is a context manager whose `__exit__` runs — closing and flushing the file — when the block ends for any reason, including a propagating exception, so you never leak a descriptor or ship a truncated output. Always pass `encoding="utf-8"` for text, because the default encoding depends on the machine's locale and is the source of the "works on my Mac, breaks in the container" class of bug; reach for binary mode only for non-text payloads. Read big files by iterating the file object line by line rather than `read()`/`readlines()`, which load everything into memory. Use `pathlib.Path` to build paths as objects with the `/` operator instead of brittle string concatenation. Take arguments with `argparse` once you have real flags — it parses, type-converts, validates, and generates `--help` for free — falling back to raw `sys.argv` only for throwaways. Finally, route data to stdout and diagnostics to stderr, and call `sys.exit` with a non-zero code on failure so the shell, `&&` chains, and CI can branch on whether your script actually worked. Now when you see a script that silently mangles names in production or leaves a half-written output file, you'll know exactly which two lines to reach for first.
</Recap>
