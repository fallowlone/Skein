infographic compare-quadrant-quarter-simple-card
data
  title Four ways to stabilise plans across deploys — only one is the default
  desc One axis: keep the planner adaptive vs pin it hard. The further you pin, the worse a default it makes.
  compares
    - label Statistics-first — the default
    - label pg_hint_plan — last resort
    - label join_collapse_limit=1 — per-role
    - label External cache — wrong default
theme
  palette #10b981 #f59e0b #6366f1 #f43f5e
