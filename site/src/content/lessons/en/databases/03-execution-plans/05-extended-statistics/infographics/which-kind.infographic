infographic compare-hierarchy-row-letter-card-rounded-rect-node
data
  title CREATE STATISTICS
  compares
    - label dependencies (PG 10)
      children
        - label Symptom: WHERE A AND B underestimates
        - label Cause: A determines B (country -> region)
        - label Stores: functional-dependency factors
    - label mcv (PG 12)
      children
        - label Symptom: frequent combo mis-estimated
        - label Cause: (US, shipped) is very common
        - label Stores: most-common value tuples
    - label ndistinct (PG 10)
      children
        - label Symptom: GROUP BY overestimates groups
        - label Cause: distinct combos << product of n_distinct
        - label Stores: real combination count
theme
  palette #6366f1 #f59e0b #14b8a6
