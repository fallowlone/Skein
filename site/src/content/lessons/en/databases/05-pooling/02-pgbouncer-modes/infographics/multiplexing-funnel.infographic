infographic sequence-funnel-simple
data
  title Why transaction mode pays off: the multiplexing fan-in
  desc 10,000 clients collapse onto ~20 Postgres backends
  sequences
    - label 10,000 app clients
      desc Connections the application opens
    - label 100 client conns held
      desc max_client_conn default cap
    - label 20 Postgres backends
      desc pool_size default — the real cost
theme
  palette #6366f1 #14b8a6 #f59e0b
