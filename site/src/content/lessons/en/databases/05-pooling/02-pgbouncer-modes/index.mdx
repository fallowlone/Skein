---
concepts:
- PgBouncer
- transaction-mode_pool
- session-mode
- multiplexing
- idle-in-transaction
deepensInto: []
estMin: 12
lang: en
lessonType: topic
level: middle
order: 2
prereqs:
- databases/05-pooling/01-why-pools-exist
slug: 02-pgbouncer-modes
sources:
- https://www.pgbouncer.org/features.html
- https://www.pgbouncer.org/config.html
- https://www.pgbouncer.org/faq.html
- https://www.crunchydata.com/blog/prepared-statements-in-transaction-mode-for-pgbouncer
spiral:
- statefulness
- latency
status: ready
summary: Transaction mode gives 10–100× multiplexing but breaks SET, LISTEN, SQL PREPARE,
  and session-level advisory locks; each has a transaction-mode-safe equivalent.
title: PgBouncer session, transaction, and statement modes
track: databases
unit: 05-pooling
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";

<Hook>
A team switches PgBouncer from session to transaction mode. Their deployed `SET search_path = 'app, public'` suddenly stops working. The app has been in production for six months. Nothing changed — except the pooler mode.
</Hook>

<Crux>Transaction mode returns a backend after each commit; session state (SET, LISTEN, PREPARE) vanishes. Every breakage has a safe substitute.</Crux>

<Explanation>

## The three PgBouncer pooling modes

**Session mode**: a backend is leased to a client for the entire duration of the client's connection. PgBouncer is essentially acting as a connection broker — 1:1 between client connections and backend connections. Everything works: SET, LISTEN, temp tables, session-level advisory locks, classic SQL PREPARE. Multiplexing is zero; you gain only auth offload and a connection queue.

**Transaction mode**: a backend is leased only for the duration of one transaction (BEGIN...COMMIT, or one autocommit statement). Between transactions the backend returns to the pool and can be handed to a different client. This is the mode that enables real multiplexing — a 10,000-client app can operate on 20–50 backends. The cost is that session state is not preserved across transaction boundaries.

**Statement mode**: backend returned after every single statement. Multi-statement transactions are forbidden. Used almost exclusively for PL/Proxy-style horizontal sharding; not a general-purpose mode.

**The production default is transaction mode** — session mode gives up the multiplexing win; statement mode is too restrictive for OLTP.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Feature</th>
        <th class="border border-border px-3 py-2 text-left">Session mode</th>
        <th class="border border-border px-3 py-2 text-left">Transaction mode</th>
        <th class="border border-border px-3 py-2 text-left">Fix in transaction mode</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Multiplexing ratio</td>
        <td class="border border-border px-3 py-2">None (1:1)</td>
        <td class="border border-border px-3 py-2">10–100×</td>
        <td class="border border-border px-3 py-2">—</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>SET search_path</code> at connect</td>
        <td class="border border-border px-3 py-2">Works</td>
        <td class="border border-border px-3 py-2">Lost after first transaction</td>
        <td class="border border-border px-3 py-2"><code>ALTER ROLE app SET search_path = ...</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>SET LOCAL</code> inside a transaction</td>
        <td class="border border-border px-3 py-2">Works</td>
        <td class="border border-border px-3 py-2">Works (scoped to that transaction)</td>
        <td class="border border-border px-3 py-2">—</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>LISTEN / NOTIFY</code></td>
        <td class="border border-border px-3 py-2">Works</td>
        <td class="border border-border px-3 py-2">Broken — NOTIFY lands on the wrong backend</td>
        <td class="border border-border px-3 py-2">Dedicated session-mode connection for LISTEN</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">SQL-level <code>PREPARE name AS ...</code></td>
        <td class="border border-border px-3 py-2">Works</td>
        <td class="border border-border px-3 py-2">EXECUTE lands on different backend</td>
        <td class="border border-border px-3 py-2">Use protocol-level prepared statements (driver-managed); PgBouncer 1.21+ handles these</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>pg_advisory_lock()</code> session-level</td>
        <td class="border border-border px-3 py-2">Works</td>
        <td class="border border-border px-3 py-2">Lock owned by whichever backend handled the call</td>
        <td class="border border-border px-3 py-2"><code>pg_advisory_xact_lock()</code> (transaction-level)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Temp tables</td>
        <td class="border border-border px-3 py-2">Works</td>
        <td class="border border-border px-3 py-2">May not survive to next transaction</td>
        <td class="border border-border px-3 py-2"><code>ON COMMIT DROP</code> or promote to real table</td>
      </tr>
    </tbody>
  </table>
</div>

## Why SET breaks in transaction mode

In session mode, the connection owned by a client is always the same backend. A `SET search_path = 'app, public'` issued at startup runs on backend B1 and stays on B1 forever.

In transaction mode, PgBouncer returns B1 to the pool after the first `COMMIT`. The next query from the same client may land on B2, which has never seen the `SET`. The setting is invisible — silently gone.

**Fix**: `ALTER ROLE app SET search_path = 'app, public'` — Postgres applies this to every new connection for that role, so every backend starts with the right setting regardless of which one is assigned.

## Why LISTEN breaks, and the dedicated-connection fix

`LISTEN channel` registers the current backend to receive `NOTIFY channel` messages. In session mode the backend is permanent — events arrive reliably. In transaction mode the backend is returned to the pool between transactions. A `NOTIFY` lands on whatever backend Postgres picks, which may be serving a different client; the original listener never hears it.

**Fix**: open one dedicated connection that bypasses PgBouncer or uses session mode exclusively for LISTEN. Keep all OLTP queries on the transaction-mode pool. The two connection strings coexist.

<Inset kind="why" lang="en">
Why does PgBouncer even have session mode? For teams migrating gradually and for applications that genuinely require session semantics (legacy PREPARE, session advisory locks, long-lived temp tables). Session mode is the safe default; transaction mode is the performance upgrade that requires an application audit first.
</Inset>

<NumbersCard
  id="card-pgbouncer-modes"
  title="PgBouncer mode comparison"
  stats={[
    { label: "Session mode: multiplexing", value: "None (1:1)" },
    { label: "Transaction mode: multiplexing", value: "10–100×" },
    { label: "Statement mode: use case", value: "PL/Proxy sharding only" },
    { label: "Default pool_size", value: "20" },
    { label: "Default max_client_conn", value: "100" },
    { label: "PgBouncer memory per pooled conn", value: "~2 KB" },
    { label: "Postgres backend memory", value: "~5–10 MB" },
    { label: "Prepared statements in tx mode", value: "PgBouncer 1.21+" },
  ]}
/>

<Misconception id="mc-session-mode-safe">
  <p><strong>"Use session mode — it works like a direct connection."</strong> Session mode is the compatibility choice, not the performance choice. Zero multiplexing benefit: client count still drives backend count directly. Transaction mode is the point of a pooler; plan the migration instead of avoiding it.</p>
</Misconception>

<Quiz
  id="02-pgbouncer-modes-quiz-1"
  lessonSlug="02-pgbouncer-modes"
  lang="en"
  question="Your team switches PgBouncer from session to transaction mode and `SET search_path = 'app, public'` issued at connect time stops working. Why?"
  choices={[
    { label: "In transaction mode, a backend is leased per transaction — the SET ran on a different backend than later queries, so the SET is invisible to them. Fix: ALTER ROLE app SET search_path = ...", correct: true },
    { label: "Postgres dropped support for SET in recent versions", misconception: "SET still works; the issue is which backend executes which statement across transaction boundaries." },
    { label: "PgBouncer caches SET results and serves stale ones", misconception: "PgBouncer does not cache SETs; it simply routes each transaction to a potentially different backend." },
    { label: "search_path is case-sensitive and the schema name has a capital letter", misconception: "The root cause is the multiplexing model, not casing." },
  ]}
/>

<Quiz
  id="02-pgbouncer-modes-quiz-2"
  lessonSlug="02-pgbouncer-modes"
  lang="en"
  question="Best fix when an application on PgBouncer transaction mode needs LISTEN/NOTIFY for cache invalidation?"
  choices={[
    { label: "Open a separate dedicated connection in session mode (or directly to Postgres) for LISTEN; keep the main pool in transaction mode for OLTP", correct: true },
    { label: "Switch the whole application to session mode", misconception: "That throws away all multiplexing for a feature that only needs one dedicated connection." },
    { label: "Poll a table every 100 ms instead of using LISTEN", misconception: "Works but wastes load; LISTEN on a dedicated session connection is the clean solution." },
    { label: "Use NOTIFY from within transactions only — that fixes LISTEN too", misconception: "NOTIFY in transactions works; LISTEN is the broken side in transaction mode. They are asymmetric." },
  ]}
/>

<DragOrder
  id="02-pgbouncer-modes-drag-1"
  lessonSlug="02-pgbouncer-modes"
  lang="en"
  prompt="Order the things to verify when adopting PgBouncer transaction mode, from highest-leverage to lowest:"
  items={[
    { id: "a", label: "Audit the app for SET (not SET LOCAL) — migrate to ALTER ROLE or SET LOCAL" },
    { id: "b", label: "Audit for classic SQL PREPARE — switch to driver protocol-level prepared statements" },
    { id: "c", label: "Audit for LISTEN — isolate on a dedicated session-mode connection" },
    { id: "d", label: "Audit for pg_advisory_lock (session-level) — switch to pg_advisory_xact_lock" },
    { id: "e", label: "Enable max_prepared_statements 1000 (PgBouncer 1.21+) for protocol prepares" },
    { id: "f", label: "Set server_reset_query = DISCARD ALL if any state leakage is possible" },
    { id: "g", label: "Set idle_in_transaction_session_timeout in Postgres GUC as a safety net" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g"]}
/>

</Explanation>

<KeyTakeaway>Transaction mode gives 10–100× multiplexing but requires an app audit: SET becomes ALTER ROLE; LISTEN gets a dedicated session connection; SQL PREPARE becomes protocol-level; session advisory locks become transaction-level. Session mode is safe but gives up all multiplexing benefit.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-pgbouncer-modes-retrieval"
  lang="en"
  questions={[
    { q: "Explain what changes when an app moves from PgBouncer session mode to transaction mode — what works, what breaks, and what becomes possible.", a: "In session mode each client is 1:1 with a backend for its whole connection lifetime; everything works identically to a direct Postgres connection but multiplexing is zero. In transaction mode PgBouncer leases a backend only per transaction — between commits the backend returns to the pool and may serve a different client. Multiplexing becomes real: 10,000 clients on 20 backends. What breaks: (1) SET outside a transaction — runs on one backend, next query is on another. Fix: ALTER ROLE for permanent defaults, SET LOCAL inside transactions. (2) Classic SQL PREPARE/EXECUTE — different backends. Fix: driver protocol-level prepared statements; PgBouncer 1.21+ tracks these. (3) LISTEN — notifications land on the wrong backend. Fix: dedicated session-mode connection. (4) pg_advisory_lock (session-level) — bound to the backend, which returns to the pool. Fix: pg_advisory_xact_lock. (5) Temp tables — may not survive to the next transaction on a different backend. Fix: ON COMMIT DROP." },
    { q: "Why does LISTEN break in PgBouncer transaction mode and what is the correct architectural fix?", a: "LISTEN registers the current Postgres backend to receive channel notifications. When PgBouncer returns that backend to the pool after a transaction commit, a subsequent NOTIFY on that channel is delivered to whichever backend Postgres happens to choose — which may be serving a different client. The original listener never receives the event. The fix is to open one dedicated connection that either connects directly to Postgres (bypassing PgBouncer entirely) or uses PgBouncer in session mode; this connection is kept alive solely for LISTEN/NOTIFY. All OLTP queries stay on the transaction-mode pool. The two connection strings coexist in the application config; this is the standard pattern." },
    { q: "What is server_reset_query = DISCARD ALL in PgBouncer and when must it be set?", a: "server_reset_query is a SQL command PgBouncer runs against a backend before returning it to the idle pool between client leases. DISCARD ALL clears all session state: temporary tables, cursors, prepared statements, GUC overrides from SET. It is necessary when you have any risk of session state leaking between clients — for example if your application or a library issues SET without SET LOCAL and you have not fully audited all code paths. In statement or transaction mode with a fully-audited application it can be left empty or lighter (e.g., RESET ALL) to avoid the round-trip; but as a safety default, especially during a migration to transaction mode, DISCARD ALL prevents one client's session state from being visible to another." },
  ]}
/>

<Recap lang="en">
PgBouncer offers three modes: session (1:1, zero multiplexing, everything works), transaction (10–100× multiplexing, requires app audit), and statement (only for sharding proxies). Transaction mode is the production standard. It breaks four patterns — `SET` outside a transaction (fix: `ALTER ROLE`), `LISTEN` (fix: dedicated session-mode connection), SQL-level `PREPARE` (fix: driver protocol-level prepared statements with PgBouncer 1.21+), and session-level advisory locks (fix: `pg_advisory_xact_lock`). Each break has an exact equivalent that is safe in transaction mode; none requires dropping the feature, only moving it.
</Recap>
