infographic chart-column-simple
data
  title Per-request P99 latency: new connection vs pooled
  desc Same database, same queries — the difference is connection setup
  values
    - label New connection per request (ms)
      value 80
    - label Borrow from pool (ms)
      value 4
theme
  palette #f43f5e #14b8a6
