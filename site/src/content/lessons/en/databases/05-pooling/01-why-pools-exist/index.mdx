---
concepts:
- connection-pool
- backend-process
- client-side-pool
- server-side-pooler
- pool-exhaustion
deepensInto: []
estMin: 8
lang: en
lessonType: topic
level: junior
order: 1
prereqs:
- databases/04-mvcc-isolation/01-mvcc-basics
slug: 01-why-pools-exist
sources:
- https://www.pgbouncer.org/features.html
- https://node-postgres.com/guides/pool-sizing
- https://www.postgresql.org/docs/current/runtime-config-connection.html
spiral:
- statefulness
- latency
status: ready
summary: Every Postgres connection forks a heavyweight OS process; a pool caches a
  small set of open connections so queries never pay the setup cost again.
title: 'Connection pools: amortising the cost of a Postgres backend'
track: databases
unit: 05-pooling
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Otto writes `new Client(); client.connect(); await client.query(...); client.end()` on every request. P99 is 80 ms — mostly the connect. Sven switches to a pool. P99 drops to 4 ms, same database, same queries.
</Hook>

<Crux>A pool caches open TCP connections so queries skip TCP + TLS + auth overhead. Without one, max_connections caps concurrent backends at ~100.</Crux>

<Explanation>

## What opening a Postgres connection actually costs

Opening a Postgres connection is not cheap: TCP handshake, TLS handshake, Postgres protocol startup, authentication, and forking a backend OS process. Together: **5–50 ms per new connection and ~10 MB of memory per backend** that Postgres must allocate.

Doing this for every web request makes connection setup the dominant cost — before a single byte of SQL runs.

**A pool amortises the cost**: open the connections once at startup, reuse them for thousands of queries. The pool's overhead per request is a sub-millisecond list lookup, not a full OS round-trip.

## The taxi-rank metaphor

A connection pool is a taxi rank, not a ride-share app. The rank has a few cabs idling at the kerb, ready to go. You walk up, take a cab, ride, return it. Total time per trip: the ride itself.

Calling a new ride-share per block means waiting for dispatch, waiting for arrival, then the ride. Most of the time is setup. A pool eliminates the dispatch — a few connections sit open, ready to be borrowed for the next query.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Without pool</th>
        <th class="border border-border px-3 py-2 text-left">With pool</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">New TCP + TLS + auth per request: 5–50 ms overhead</td>
        <td class="border border-border px-3 py-2">Borrow idle connection: under 1 ms overhead</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Each backend: ~10 MB Postgres memory</td>
        <td class="border border-border px-3 py-2">Pool holds 5–20 connections; memory fixed</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">40 workers × 20 connections = 800 — exceeds max_connections=100</td>
        <td class="border border-border px-3 py-2">PgBouncer multiplexes 800 clients onto 50 backends</td>
      </tr>
    </tbody>
  </table>
</div>

## Two layers: client-side and server-side

Modern stacks use **two pool layers**:

1. **Client-side pool** — lives inside each application process (node-postgres `Pool`, HikariCP for Java, asyncpg for Python). Caches 5–20 TCP connections per worker. Eliminates per-request connect cost.

2. **Server-side pooler** (PgBouncer, Supavisor, Odyssey) — a separate process in front of Postgres. Accepts thousands of client connections from all workers and routes them onto a small set of real Postgres backends. Eliminates the max_connections cap problem.

Together these two layers multiply: the client pool kills per-request socket overhead, and the server pooler collapses dozens of workers' connections down to a backend count Postgres can actually saturate. Remove either layer and one problem returns.

The combination: each worker has ~10 cheap client-pool connections to PgBouncer; PgBouncer maintains 20–50 real backends. 10,000 application clients become sustainable on a Postgres with `max_connections = 100`.

<Inset kind="why" lang="en">
Why does Postgres use one OS process per connection instead of threads? History: Postgres predates good threading on most Unix systems. The process model isolates crashes (one backend OOM does not corrupt others) and simplifies memory management — each backend has a private heap. The downside is the irreducible per-connection overhead that pools are designed to absorb.
</Inset>

## The scale failure without a server-side pooler

A team runs 4 workers, client pool `max: 20`, Postgres `max_connections = 100`. They scale to 40 workers. Total connections: 40 × 20 = 800. Postgres hard-caps at 100. Result: `FATAL: sorry, too many clients already` on 700 attempts. The app falls over.

Fix: insert PgBouncer. Workers connect to PgBouncer (8000 client connections are cheap — ~2 KB each). PgBouncer maintains 50 real backends. Total Postgres backends: 50. Problem gone.

<Quiz
  id="01-why-pools-exist-quiz-1"
  lessonSlug="01-why-pools-exist"
  lang="en"
  question="Why is opening a new Postgres connection per HTTP request a bad idea?"
  choices={[
    { label: "Each connection costs ~10 MB of database memory and 5–50 ms of handshake time; doing it per-request makes connection setup the dominant cost, and Postgres has a hard cap on concurrent connections", correct: true },
    { label: "Postgres is single-threaded, so multiple connections do not help", misconception: "Postgres handles many connections in parallel via processes. The issue is per-connection cost, not single-threading." },
    { label: "TLS handshakes are not supported per-connection in Postgres", misconception: "TLS works fine; it just adds to the per-connection overhead." },
    { label: "Postgres charges a fee per connection", misconception: "It does not bill — it allocates OS resources that are limited." },
  ]}
/>

<Quiz
  id="01-why-pools-exist-quiz-2"
  lessonSlug="01-why-pools-exist"
  lang="en"
  question="An app worker has a client pool of max 20. You scale to 40 workers and Postgres has max_connections=100. What happens?"
  choices={[
    { label: "Postgres rejects most connection attempts because 40 × 20 = 800 far exceeds max_connections=100", correct: true },
    { label: "Postgres automatically divides max_connections equally among workers", misconception: "It does not — max_connections is a hard cluster-wide cap." },
    { label: "Client pool connections are free and unlimited", misconception: "Each client connection to Postgres opens a real OS-level backend process." },
    { label: "The worker count automatically adapts to max_connections", misconception: "Workers do not self-limit; the database rejects the excess and returns errors." },
  ]}
/>

<DragOrder
  id="01-why-pools-exist-drag-1"
  lessonSlug="01-why-pools-exist"
  lang="en"
  prompt="Order the steps that happen when an application borrows a connection from a pool:"
  items={[
    { id: "a", label: "Application code calls pool.query(sql, params)" },
    { id: "b", label: "Pool looks for an idle connection in its cache" },
    { id: "c", label: "If found: borrow it; if not and under max: open a new one; if at max: wait or reject" },
    { id: "d", label: "Pool sends the query to Postgres over the borrowed connection" },
    { id: "e", label: "Postgres executes the query and returns rows" },
    { id: "f", label: "Pool returns the connection to the idle list — does NOT close it" },
    { id: "g", label: "Application code receives the rows and continues" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g"]}
/>

<MetaphorComplete
  id="01-why-pools-exist-metaphor-1"
  lessonSlug="01-why-pools-exist"
  lang="en"
  setup="Fill in the blank: a connection pool is to Postgres connections what a thread pool is to OS ___ — a small cache of expensive, reusable resources that amortises setup cost across many short tasks."
  accepted={["threads", "thread"]}
  canonical="threads"
  explanation="Thread pools cache OS threads so that work items do not pay thread-creation overhead per task. Connection pools cache database connections so that queries do not pay TCP + TLS + auth overhead per request. Same pattern, different resource."
/>

</Explanation>

<FlowDiagram
  label="A server-side pooler multiplexes many client connections onto a few real Postgres backends."
  perRow={3}
  hue="var(--d-data)"
  nodes={[
    { id: "w1", label: "Worker 1", sub: "client pool", col: 0, row: 0 },
    { id: "w2", label: "Worker 2", sub: "client pool", col: 0, row: 1 },
    { id: "wn", label: "Worker N", sub: "8000 clients", col: 0, row: 2 },
    { id: "pgb", label: "PgBouncer", sub: "~2 KB / client", col: 1, row: 1 },
    { id: "b1", label: "backend 1", sub: "~10 MB", col: 2, row: 0 },
    { id: "b2", label: "backend …", sub: "~10 MB", col: 2, row: 1 },
    { id: "b50", label: "backend 50", sub: "max_connections", col: 2, row: 2 },
  ]}
  edges={[
    { from: "w1", to: "pgb" },
    { from: "w2", to: "pgb" },
    { from: "wn", to: "pgb" },
    { from: "pgb", to: "b1" },
    { from: "pgb", to: "b2" },
    { from: "pgb", to: "b50" },
  ]}
  caption="8000 cheap client connections fan into PgBouncer, which maintains only ~50 real backends — keeping Postgres well under max_connections = 100."
/>

<KeyTakeaway>A connection pool caches open Postgres connections: client-side pools (5–20 per worker) cut per-request setup cost; server-side poolers (PgBouncer) multiplex thousands of client connections onto tens of real backends, bypassing max_connections limits.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-why-pools-exist-retrieval"
  lang="en"
  questions={[
    { q: "In two sentences: what is a connection pool and why is one always needed in production Postgres?", a: "A connection pool is a cache of pre-opened, reusable TCP connections to Postgres, owned by either the application (client pool) or a separate process in front of Postgres (server pooler like PgBouncer). Without pooling, each backend costs 5–10 MB and a slot in max_connections (default 100), so you cannot scale beyond a handful of workers before the database rejects connections; the pool amortises the 5–50 ms per-connection setup cost across thousands of queries." },
    { q: "What are the two pool layers and what does each one solve?", a: "Layer 1: the client-side pool lives inside each application worker and caches 5–20 connections; it eliminates the TCP + TLS + auth cost per request (turns 50 ms overhead into under 1 ms). Layer 2: the server-side pooler (PgBouncer etc.) sits in front of Postgres and multiplexes thousands of inbound client connections onto a small real-backend pool; it solves the max_connections exhaustion that occurs when many workers each hold a client pool." },
    { q: "Why does Postgres use one OS process per connection, and what is the practical consequence for pooling?", a: "Postgres predates reliable threading on Unix and uses a process-per-connection model for crash isolation and simple private memory management. The consequence is that each connection forks a backend (~10 MB resident), and max_connections is a hard cap on how many can exist simultaneously. Every production Postgres deployment needs a pooler precisely because this overhead cannot be removed from the server side." },
  ]}
/>

<Recap lang="en">
Postgres creates one OS process per connection — each backend consumes ~10 MB and a slot in `max_connections` (default 100). Opening a connection costs 5–50 ms of TCP + TLS + auth overhead. A connection pool fixes both problems: a client-side pool caches connections per worker so queries never pay the setup cost; a server-side pooler like PgBouncer multiplexes thousands of client connections onto a few dozen real backends, keeping Postgres backend count well under `max_connections`. Without both layers, scaling from 4 to 40 workers with a pool of 20 per worker generates 800 connection attempts against a 100-slot limit — guaranteed failures. Now when you see "sorry, too many clients already" or P99 latency dominated by connect time, the first question is always which pool layer is missing.
</Recap>
