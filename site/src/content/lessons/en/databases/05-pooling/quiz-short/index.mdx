---
concepts:
- backend-process
- pgbouncer-modes
- pool-sizing
- pool-exhaustion
- idle-in-transaction
estMin: 14
lang: en
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.pgbouncer.org/features.html
- https://github.com/brettwooldridge/HikariCP/wiki/About-Pool-Sizing
- https://www.postgresql.org/docs/current/runtime-config-connection.html
status: ready
summary: "Free-recall prompts across the pooling unit. Answer each in your own words first, then reveal the model answer and compare."
title: 'Connection pooling: free-recall review'
track: databases
unit: 05-pooling
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory before you open the model answer — the effort of recall is what makes the mechanism stick when you need it during an incident.
</Hook>

<Goal lang="en">
Reconstruct the unit's spine from memory — why backends are heavyweight, what transaction mode trades away, how to size both pool layers, why exhaustion is hold-time, and how idle-in-transaction harms beyond the pool.
</Goal>

<RetrievalDrawer
  client:load
  id="05-pooling-short-retrieval"
  lang="en"
  questions={[
    { q: "Why is a Postgres connection expensive, and what are the two pool layers that absorb that cost?", a: "Each Postgres connection makes the postmaster fork a dedicated backend OS process: ~5–10 MB resident memory, a proc-array slot, a lock-table entry, plus ~5 ms fork-and-init on the server and TCP+TLS+auth on the network — together 5–50 ms per new connection. Two layers absorb this. Layer 1, the client-side pool (node-postgres Pool, HikariCP, asyncpg), lives inside each worker and caches 5–20 connections so queries skip per-request setup, turning ~50 ms overhead into sub-millisecond checkout. Layer 2, the server-side pooler (PgBouncer, Supavisor, Odyssey), sits in front of Postgres and multiplexes thousands of cheap inbound client connections (~2 KB each) onto a small set of real backends, bypassing the max_connections cap. The combination lets ~10,000 clients run on a Postgres with max_connections = 100." },
    { q: "What does PgBouncer transaction mode buy, what does it break, and what is the safe substitute for each break?", a: "Transaction mode leases a backend only for the duration of one transaction, returning it to the pool at COMMIT/ROLLBACK; this is what gives 10–100× multiplexing (session mode is 1:1 and gives none). It breaks any state that assumes a stable backend: (1) bare SET outside a transaction — fix with ALTER ROLE for permanent defaults or SET LOCAL inside the transaction; (2) LISTEN/NOTIFY — notifications land on whatever backend is free, so isolate LISTEN on a dedicated session-mode (or direct) connection; (3) classic SQL PREPARE/EXECUTE — Execute lands on a backend that never saw the Parse, so use driver protocol-level prepared statements (PgBouncer 1.21+ tracks them); (4) session-level pg_advisory_lock — fix with pg_advisory_xact_lock; (5) temp tables — use ON COMMIT DROP or promote to a real table. None of these require dropping the feature, only moving it." },
    { q: "How do you size the two pool layers, and why is pool_size NOT the client count?", a: "PgBouncer pool_size (the real-backend ceiling) follows the HikariCP formula (cores × 2) + effective_spindle_count, plus ~50% burst headroom — on a 16-core NVMe box with the working set in RAM that is roughly 32 + 50% ≈ 48, not 200. It is sized to the Postgres CPU because throughput peaks at a small multiple of core count and degrades past it. The per-worker client pool is sized from concurrency, not cores: concurrent_requests × db_calls_per_request × avg_query_ms / request_budget_ms. For example 100 concurrent requests × 2 calls × 5 ms / 50 ms budget = 20 connections per worker. Client connections to PgBouncer are cheap (~2 KB), so thousands of them multiplex onto the small backend pool. Matching pool_size to client count creates hundreds of active backends past the throughput peak and makes p99 worse, not better." },
    { q: "Why is pool exhaustion almost always a hold-time problem, and why does raising pool_size usually fail?", a: "Exhaustion fires when every backend is checked out and no slot is free; at the pooler this shows as cl_waiting climbing, and at Postgres the in-use backends are either state=active with old query_start (slow query or lock wait) or state=idle in transaction with old xact_start (a BEGIN held open across slow work — the dominant cause, usually an external API call inside the transaction). The lever is how long each checkout is held, not how many slots exist. Raising pool_size from 24 to 100 gives more backends held by the same long transactions: if each holds for 10 s, more concurrent holds just add proc-array and lock-manager contention, dropping per-backend throughput. The correct sequence is diagnose the hold (pg_stat_activity, SHOW POOLS), fix the cause (add the index, move the API call outside the transaction, ensure COMMIT/ROLLBACK on error paths), then set idle_in_transaction_session_timeout = 60s; raise pool_size only after confirming checkouts are short." },
    { q: "Beyond occupying a pool slot, what else does an idle-in-transaction backend harm, and why?", a: "Three simultaneous harms. (1) The pool slot is occupied, so that backend cannot serve another client — the visible, immediate symptom. (2) Any rows the open transaction touched stay row-locked, so other writers block and can cascade into lock-wait queues. (3) The transaction pins an MVCC snapshot: VACUUM cannot reclaim dead tuples with xmin newer than this backend's horizon (backend_xmin), so a transaction left open for an hour prevents VACUUM from cleaning a hot table for that whole hour, causing silent table bloat that grows until queries start seq-scanning through dead tuples. Pool exhaustion is the fast symptom; the MVCC bloat is the slow one that compounds. Both share the same root cause, which is why idle_in_transaction_session_timeout protects against both at once." },
    { q: "When do you reach past PgBouncer, and what is the serverless connection-storm fix?", a: "PgBouncer 1.21+ is the 2026 default for ~80% of OLTP: single-threaded C, ~2 KB per client connection, battle-tested, and now compatible with prepared statements. Reach past it only on measured evidence. If a single PgBouncer node pegs one CPU core at very high QPS (around 100k QPS), horizontal-scale it behind HAProxy/keepalived, or move to multi-threaded Odyssey (highest single-node throughput) or PgCat (Rust, adds replica routing/sharding). For massive inbound counts — multi-tenant SaaS with thousands of tenant databases, or serverless/edge — use Supavisor (BEAM, millions of inbound connections) or RDS Proxy. Serverless is a distinct problem: cold starts open fresh backends and storm max_connections. The fix is always a persistent colocated pooler to absorb the burst, plus an HTTP-based driver (Neon driver, Cloudflare Hyperdrive) for true edge so each query is a stateless HTTP request rather than a persistent backend, with aggressive idle timeouts to reclaim dead-worker connections." },
  ]}
/>

<Recap lang="en">
If you could reconstruct each answer from memory you hold the unit's spine: backends are heavyweight processes, so two pool layers exist — a client pool per worker to kill per-request setup, and a server pooler to multiplex thousands of clients onto a backend count sized to the CPU via (cores × 2) + spindles. Transaction mode is the multiplexing default and trades connect-time session state, each break with an exact substitute, with PgBouncer 1.21+ restoring prepared statements. Exhaustion is hold-time, not size; idle-in-transaction also pins an MVCC snapshot and blocks VACUUM; and you only outgrow PgBouncer when you have measured its single-thread ceiling or face serverless/multi-tenant scale.
</Recap>
