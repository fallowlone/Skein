---
concepts:
- hot-update
- heap-only-tuple
- fillfactor
- read-committed
- repeatable-read
- serializable
- write-skew
- lost-update
- '40001'
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: middle
order: 3
prereqs:
- databases/04-mvcc-isolation/02-row-versions-and-snapshots
slug: 03-hot-updates-and-isolation-levels
sources:
- https://www.postgresql.org/docs/current/transaction-iso.html
- https://www.postgresql.org/docs/current/storage-hot.html
- https://www.postgresql.org/docs/current/mvcc-serialization-failure-handling.html
spiral:
- statefulness
status: ready
summary: HOT updates cut index write cost by up to 70% when conditions are right;
  the four isolation levels decide which concurrency anomalies the database prevents
  vs leaves to the application.
title: 'HOT updates and isolation levels: what you gain and what you pay'
track: databases
unit: 04-mvcc-isolation
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
Every UPDATE in Postgres writes a new tuple. If the table has five indexes, each UPDATE writes five new index entries. HOT updates eliminate that cost — but only under two specific conditions. And the isolation level you pick determines which correctness bugs the database prevents vs which it leaves to your application code.
</Hook>

<Crux>HOT saves 30–70% WAL on update-heavy tables; isolation level decides which anomalies Postgres catches vs which the application must handle.</Crux>

<Explanation>

## HOT updates: an optimization with a famous cost model

If an UPDATE touches **no indexed column** and the **new tuple fits on the same heap page** as the old, Postgres can perform a **heap-only tuple (HOT)** update:

- The old tuple gets flag `HEAP_HOT_UPDATED` — "follow the `t_ctid` chain to find me"
- The new tuple gets flag `HEAP_ONLY_TUPLE` — "no index entry points at me directly"
- The index continues pointing at the original tuple; the heap chain carries the lookup the rest of the way
- **Result: two index writes become zero**

On UPDATE-heavy workloads where most columns are not indexed, this is the difference between WAL doubling and barely growing — a 30–70% WAL savings.

HOT chains **break** when the new version cannot fit on the original page; once broken, every subsequent UPDATE pays the index-write cost again. The `fillfactor` storage parameter (`ALTER TABLE t SET (fillfactor=85)`) reserves free space on each page precisely so HOT chains have room to grow.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Condition</th>
        <th class="border border-border px-3 py-2 text-left">HOT eligible?</th>
        <th class="border border-border px-3 py-2 text-left">Index writes</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">No indexed column changed, new tuple fits same page</td>
        <td class="border border-border px-3 py-2">Yes</td>
        <td class="border border-border px-3 py-2">0</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">No indexed column changed, but page is full</td>
        <td class="border border-border px-3 py-2">No (chain breaks)</td>
        <td class="border border-border px-3 py-2">1 per index</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Indexed column changed</td>
        <td class="border border-border px-3 py-2">No</td>
        <td class="border border-border px-3 py-2">1 per index</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="en">
`pg_stat_all_tables.n_tup_hot_upd` divided by `n_tup_upd` gives the HOT ratio. On UPDATE-heavy schemas you want this above 80%. Below 50% means fillfactor is too aggressive or the schema has the wrong index set for the workload.
</Inset>

## The four isolation levels Postgres exposes

SQL standard defines four levels by listing anomalies they allow or prohibit: dirty reads, non-repeatable reads, phantom reads, and serialization anomalies.

| Level | Dirty read | Non-repeatable read | Phantom read | Write skew |
|---|---|---|---|---|
| READ UNCOMMITTED | Possible (Postgres: no) | Possible | Possible | Possible |
| READ COMMITTED (default) | No | Possible | Possible | Possible |
| REPEATABLE READ | No | No | No (PG) | Possible |
| SERIALIZABLE | No | No | No | No |

**Postgres specifics:**
- Read Uncommitted is silently treated as Read Committed; Postgres never allows dirty reads at any level.
- Read Committed (the default) gives each *statement* a fresh snapshot.
- Repeatable Read gives the *whole transaction* one snapshot taken at the first statement; Postgres implements RR as snapshot isolation — stronger than the SQL standard's RR (no phantom reads) but weaker than true serializable (write skew is still possible).
- Serializable (since 9.1) layers Serializable Snapshot Isolation (SSI) on top of RR: Postgres tracks read–write dependencies and aborts transactions whose commit order cannot correspond to any serial schedule, throwing SQLSTATE 40001.

## What the application must do

Under **READ COMMITTED**: the application is responsible for avoiding lost updates via:
- `SELECT ... FOR UPDATE` (pessimistic locking on the row), or
- `UPDATE ... WHERE version = ?` with optimistic concurrency, or
- `RETURNING` to confirm the row had the expected value.

Under **REPEATABLE READ** and **SERIALIZABLE**: the database raises SQLSTATE `40001 serialization_failure` when a conflict is detected. The application's only correct response is to retry the entire transaction. Frameworks like Django, ActiveRecord, and Hibernate ship retry middlewares.

## When to pick which level

- **READ COMMITTED**: 90% of business logic — maximum throughput, fresh snapshot per statement.
- **REPEATABLE READ**: when the transaction reads a set of rows multiple times and needs consistency between those reads (e.g., a report that sums fields then cross-checks the aggregate version).
- **SERIALIZABLE**: when the business logic has an invariant only preserved by absence of write skew (e.g., "at least one doctor on call", "no account goes below zero"). Adds up to 5x cost and requires the application to handle retries.

## Write skew under Repeatable Read

Write skew is the canonical Repeatable Read anomaly Postgres does not prevent. Two transactions read the same set, decide based on the set, and each modifies a different row in that set. Both commit; the invariant is broken.

<TraceScenario
  id="03-hot-trace-1"
  lessonSlug="03-hot-updates-and-isolation-levels"
  lang="en"
  scenario="Trace a write-skew anomaly under Repeatable Read that Serializable would prevent."
  steps={[
    { prompt: "Setup: a doctors table with on_call boolean. Two doctors, alice and bob, both on_call=true. The application invariant: at least one doctor must remain on_call at all times.", reveal: "Two app instances each let one doctor go off-call only if at least one other remains on-call." },
    { prompt: "T1 (alice removing her shift): SELECT COUNT(*) FROM doctors WHERE on_call = true. Returns 2. Decides safe to proceed.", reveal: "Under Repeatable Read snapshot taken at BEGIN, T1's view: two doctors on call." },
    { prompt: "T2 (bob removing his shift, concurrent with T1): same SELECT, same snapshot view. Returns 2.", reveal: "T2's snapshot also sees two on-call doctors. Both transactions think they are safe." },
    { prompt: "T1: UPDATE doctors SET on_call = false WHERE name = 'alice'. Commits.", reveal: "T1 commits successfully. No row-version conflict because T1 touched alice and T2 touched bob." },
    { prompt: "T2: UPDATE doctors SET on_call = false WHERE name = 'bob'. Commits.", reveal: "Under Repeatable Read, T2 commits too. Now both doctors are off-call. The invariant is broken. This is write skew." },
    { prompt: "Under Serializable, what would have happened?", reveal: "SSI tracks read-write dependencies. T1 read the set of on-call doctors and wrote alice; T2 read the same set and wrote bob. SSI detects the cycle and aborts one of them with SQLSTATE 40001. The retry runs after the first commit and now sees only one doctor on call — the invariant holds." },
  ]}
/>

<NumbersCard
  id="card-iso-cost"
  title="MVCC + isolation: the numbers that matter"
  stats={[
    { label: "Tuple header size", value: "23 B (plus alignment)" },
    { label: "Transaction id width", value: "32 bit; freeze at 2^31" },
    { label: "HOT update WAL savings", value: "30–70% on update-heavy workloads" },
    { label: "Default isolation level", value: "READ COMMITTED" },
    { label: "Serialization failure code", value: "SQLSTATE 40001" },
    { label: "Bloat target (>50 GB tables)", value: "&lt;20% dead tuples" },
    { label: "Autovacuum defaults", value: "3 workers, 60s naptime, 20% scale" },
    { label: "VACUUM FULL lock", value: "ACCESS EXCLUSIVE" },
  ]}
/>

<Quiz
  id="03-hot-quiz-1"
  lessonSlug="03-hot-updates-and-isolation-levels"
  lang="en"
  question="A HOT (heap-only tuple) update can occur when..."
  choices={[
    { label: "Any column changes, as long as the table has no indexes", misconception: "HOT applies even on indexed tables — the requirement is that no INDEXED column changes." },
    { label: "No indexed column changes AND the new tuple fits on the same heap page as the old", correct: true },
    { label: "The update is inside a SELECT FOR UPDATE clause", misconception: "FOR UPDATE is about locking, not HOT eligibility." },
    { label: "Autovacuum has just run on the table", misconception: "Autovacuum has nothing to do with HOT — HOT is decided at the UPDATE moment based on page space and column changes." },
  ]}
/>

<Quiz
  id="03-hot-quiz-2"
  lessonSlug="03-hot-updates-and-isolation-levels"
  lang="en"
  question="At which isolation level does Postgres detect lost-update anomalies and raise SQLSTATE 40001 instead of silently overwriting?"
  choices={[
    { label: "Read Uncommitted", misconception: "Postgres treats Read Uncommitted as Read Committed; neither raises 40001 on lost updates." },
    { label: "Read Committed", misconception: "RC re-reads each statement with a fresh snapshot. Lost updates are application-detectable but not raised by the database." },
    { label: "Repeatable Read", correct: true, justification: "Postgres RR is snapshot isolation. If two transactions update the same row, the second to commit gets 40001 instead of silently overwriting." },
    { label: "Only Serializable", misconception: "Serializable does detect lost updates, but Repeatable Read already does. SSI extends to write-skew, which RR does NOT catch." },
  ]}
/>

<Quiz
  id="03-hot-quiz-3"
  lessonSlug="03-hot-updates-and-isolation-levels"
  lang="en"
  question="Two concurrent transactions each read the on-call doctor count (2), decide they can safely go off-call, and update different rows. Both commit. The invariant breaks. Which isolation level would have prevented this?"
  choices={[
    { label: "READ COMMITTED", misconception: "RC gives a fresh snapshot per statement but does not track read-write dependencies. Write skew is still possible." },
    { label: "REPEATABLE READ", misconception: "RR prevents non-repeatable reads and phantom reads but explicitly allows write skew — this exact scenario is the canonical example." },
    { label: "SERIALIZABLE (SSI)", correct: true, justification: "SSI tracks rw-antidependencies: T1 read the on-call set and wrote alice; T2 read the same set and wrote bob. SSI detects the cycle and aborts one transaction." },
    { label: "No isolation level prevents write skew in Postgres", misconception: "Serializable in Postgres does prevent write skew via SSI." },
  ]}
/>

</Explanation>

<StackDiagram
  label="The four Postgres isolation levels from weakest at the top to strongest at the bottom; each lower level prevents one more concurrency anomaly."
  hue="var(--d-data)"
  layers={[
    { label: "READ UNCOMMITTED (treated as Read Committed)", note: "no dirty reads in PG" },
    { label: "READ COMMITTED · default", note: "allows non-repeatable + phantom + write skew" },
    { label: "REPEATABLE READ · snapshot isolation", note: "stops non-repeatable + phantom; allows write skew" },
    { label: "SERIALIZABLE · SSI", note: "stops write skew too; may raise 40001" },
  ]}
  caption="Top = weakest. Each step down prevents one more anomaly. Postgres RR (snapshot isolation) already blocks phantom reads but still allows write skew, which only SERIALIZABLE catches."
/>

<KeyTakeaway>HOT updates save 30–70% index write cost when no indexed column changes and the new version fits on the same page — set fillfactor below 100 to preserve that room. Isolation level decides who catches anomalies: RC leaves lost updates to the app; RR raises 40001 on concurrent row updates but allows write skew; Serializable (SSI) catches write skew at the cost of retries and predicate-lock overhead.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-hot-retrieval"
  lang="en"
  questions={[
    { q: "What two conditions must both be true for a Postgres UPDATE to use HOT, and what happens when either fails?", a: "Both conditions: (1) no indexed column is changed, and (2) the new tuple fits on the same heap page as the old. If condition 1 fails, a new index entry must be written (one per index on the table). If condition 2 fails, the chain breaks and the new tuple goes to a different page, also forcing index entries. Once a HOT chain breaks, every subsequent update on that row pays full index cost until a fillfactor-compatible empty slot opens on the original page." },
    { q: "Why is Postgres's Repeatable Read stricter than the SQL standard's Repeatable Read, but weaker than its Serializable?", a: "Postgres implements RR as snapshot isolation. SQL-standard RR allows phantom reads; snapshot isolation does not — a transaction sees the same row set on every read. So Postgres RR is stricter than the standard. However, snapshot isolation allows write skew: two transactions read the same set, decide based on the set, and each modifies a different row in that set. Both commit; the invariant the set was supposed to protect is broken. True serializable execution prevents this. Postgres SERIALIZABLE adds Serializable Snapshot Isolation (SSI), which tracks read-write dependencies and aborts whichever transaction would create an unserializable schedule." },
    { q: "What is the correct application response to SQLSTATE 40001 under Repeatable Read or Serializable?", a: "Retry the entire transaction from the beginning with a fresh snapshot. A partial retry (re-running only the failing statement) is wrong because the problem is the transaction's entire read set, not one statement. Frameworks like Django, ActiveRecord, and Hibernate ship retry middlewares. In raw SQL: catch 40001, roll back, and replay the same statements." },
  ]}
/>

<Recap lang="en">
HOT (heap-only tuple) updates avoid writing new index entries when an UPDATE changes no indexed column and the new tuple fits on the same page — saving 30–70% WAL on update-heavy workloads. The fillfactor storage parameter reserves page headroom to keep HOT chains intact. Isolation levels define correctness: READ COMMITTED (default) re-snapshots per statement and leaves lost updates to the application; REPEATABLE READ (snapshot isolation) raises SQLSTATE 40001 on concurrent row updates but allows write skew; SERIALIZABLE adds SSI to detect write skew cycles, at the cost of predicate-lock overhead and higher retry rates. Applications at any level above RC must implement retry loops that replay the entire transaction on 40001.
</Recap>
