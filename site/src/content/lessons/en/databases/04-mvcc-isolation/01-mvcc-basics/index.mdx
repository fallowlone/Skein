---
concepts:
- mvcc
- snapshot
- vacuum
- lost-update
deepensInto: []
estMin: 8
lang: en
lessonType: topic
level: junior
order: 1
prereqs:
- databases/03-execution-plans/01-explain-and-plans
slug: 01-mvcc-basics
sources:
- https://www.postgresql.org/docs/current/mvcc.html
- https://www.postgresql.org/docs/current/transaction-iso.html
- https://www.postgresql.org/docs/current/routine-vacuuming.html
spiral:
- statefulness
- multiplexing
status: ready
summary: MVCC keeps every row''''s history so each transaction sees a stable snapshot
  without locking the rows other people are touching.
title: 'MVCC: why readers and writers never wait for each other'
track: databases
unit: 04-mvcc-isolation
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
A five-minute analytics report and a high-volume checkout flow are running at the same time on the same database. Without special design, one would freeze the other. With MVCC, neither waits — and the trick costs nothing at read time.
</Hook>

<Crux>MVCC keeps multiple row versions so each transaction sees a consistent snapshot — readers never block writers, writers never block readers.</Crux>

<Explanation>

## What MVCC does in one sentence

MVCC keeps every row's history so each transaction sees a stable picture of the database without having to lock the rows other people are touching.

**Why care.** Without it, a five-minute analytics query would freeze every order coming in, and your dashboard would either lie about totals or block the checkout button.

## The library metaphor

Imagine a busy library where every book has many copies stamped with the date they were taken off the shelf. When a reader asks for "the catalog as of when I started reading", the librarian hands them a labeled copy frozen at that moment, even while a librarian behind the counter is binding a new edition. The reader reads their copy in peace. The librarian adds a new edition. Neither has to wait for the other; they are looking at different stamped copies of the same shelf.

Later, a janitor sweeps through and discards copies nobody is reading anymore. That sweep is what Postgres calls VACUUM, and the janitor only throws out a copy when no reader is still holding its date-stamp.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Without MVCC</th>
        <th class="border border-border px-3 py-2 text-left">With MVCC</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Reader locks the row; writer waits</td>
        <td class="border border-border px-3 py-2">Reader sees an old version; writer creates a new one</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Writer locks the row; reader waits</td>
        <td class="border border-border px-3 py-2">Writer creates a new version; reader still sees the old</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Analytics query blocks checkout</td>
        <td class="border border-border px-3 py-2">Analytics query reads frozen snapshot; checkout proceeds</td>
      </tr>
    </tbody>
  </table>
</div>

## Sven and Otto — the concrete scenario

An app server runs a monthly report (Sven). The database receives thousands of orders per second (Otto). Without MVCC, the report either locks the orders table (freezing checkout) or sees half-finished updates. With MVCC, Sven gets a snapshot frozen at the report start; new orders go to fresh versions Sven cannot see. Report finishes, checkout never stalls.

## The lost-update gap MVCC does not close

Two browser tabs edit the same profile. Tab A loads, you type. Tab B loads, edits one field, saves. You save Tab A: Tab B's edit silently disappeared. That is **lost update**. MVCC alone does not fix it — it still gives both tabs a valid snapshot, but does not serialize their writes. Ask the database with `SELECT FOR UPDATE` or a stricter isolation level (covered in lesson 03).

<Inset kind="why" lang="en">
MVCC was not invented by Postgres. The concept dates to 1978 (Reed's work at MIT). Oracle shipped it in version 7 (1992); Postgres had a form of it from the very start, and it was one reason Postgres was chosen over competitors in multi-user workloads. MySQL InnoDB added MVCC in 2000.
</Inset>

<Quiz
  id="01-mvcc-basics-quiz-1"
  lessonSlug="01-mvcc-basics"
  lang="en"
  question="What does MVCC actually do?"
  choices={[
    { label: "It locks every row a transaction touches", misconception: "Locking is what MVCC replaces for reads. Readers never block on data being modified." },
    { label: "It keeps multiple versions of each row so different transactions can each see a consistent picture", correct: true },
    { label: "It compresses old rows to save space", misconception: "Old versions take up MORE space, not less, until VACUUM reclaims them." },
    { label: "It replicates data to another machine", misconception: "Replication is unrelated. MVCC is about one database server's concurrency." },
  ]}
/>

<Quiz
  id="01-mvcc-basics-quiz-2"
  lessonSlug="01-mvcc-basics"
  lang="en"
  question="A long-running analytics query and a high-volume checkout flow are running at the same time. With MVCC, what happens?"
  choices={[
    { label: "The analytics query blocks checkout writes until it finishes", misconception: "MVCC's whole point is that readers do not block writers." },
    { label: "Checkout writes block the analytics query", misconception: "Writers do not block readers either. Both run concurrently." },
    { label: "Both run concurrently; the analytics query sees a frozen snapshot while checkout writes fresh row versions", correct: true },
    { label: "The database picks one to roll back to avoid conflicts", misconception: "There is nothing to roll back — the operations never conflict at the row level." },
  ]}
/>

<DragOrder
  id="01-mvcc-basics-drag-1"
  lessonSlug="01-mvcc-basics"
  lang="en"
  prompt="Put the life of one row's update in order:"
  items={[
    { id: "a", label: "Transaction A starts; gets snapshot tagged with its transaction id" },
    { id: "b", label: "Transaction A reads the row at version v1 — the version stamped before A started" },
    { id: "c", label: "Transaction B updates the row, creating version v2 stamped with B's transaction id" },
    { id: "d", label: "Transaction B commits — v2 is now the latest visible version for new snapshots" },
    { id: "e", label: "Transaction A still reads v1 because that is what its snapshot allows" },
    { id: "f", label: "Eventually no snapshot needs v1; VACUUM marks v1's space reusable" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f"]}
/>

<MetaphorComplete
  id="01-mvcc-basics-metaphor-1"
  lessonSlug="01-mvcc-basics"
  lang="en"
  setup="Fill in the blank: MVCC is like a library where each reader gets a date-stamped _______ instead of the shelf book itself."
  accepted={["copy", "snapshot", "version", "duplicate"]}
  canonical="copy (or snapshot, version — any term for an independent, time-stamped reference)"
  explanation="The point of the metaphor is that nobody fights for the shelf book. Every reader leaves with their own copy stamped with the moment they asked, and the shelf keeps adding new editions independently."
/>

</Explanation>

<SequenceDiagram
  label="MVCC lets a reading transaction and a writing transaction proceed at the same time without blocking each other."
  hue="var(--d-data)"
  actors={["Reader (Sven)", "Writer (Otto)"]}
  messages={[
    { from: "Reader (Sven)", to: "Reader (Sven)", label: "BEGIN: snapshot taken, reads row v1" },
    { from: "Writer (Otto)", to: "Writer (Otto)", label: "UPDATE: creates row v2, marks v1 dead" },
    { from: "Writer (Otto)", to: "Writer (Otto)", label: "COMMIT: v2 visible to new snapshots" },
    { from: "Reader (Sven)", to: "Reader (Sven)", label: "still reads v1 (its snapshot predates v2)" },
  ]}
  caption="Time flows top to bottom. The reader keeps seeing v1 from its frozen snapshot while the writer commits v2 — neither transaction waits for the other."
/>

<KeyTakeaway>MVCC stores multiple row versions so readers and writers see separate snapshots and never block each other. The cost is storage: dead versions accumulate until VACUUM reclaims them. Lost updates still require explicit locking or a stricter isolation level.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-mvcc-basics-retrieval"
  lang="en"
  questions={[
    { q: "In one sentence: why does a five-minute analytics query in Postgres not block checkout writes?", a: "MVCC hands the analytics query a snapshot frozen at the moment it started, so new checkout writes create fresh row versions the analytics query cannot see — the writers do not wait for the reader and the reader does not wait for the writers." },
    { q: "What is a lost update, and why does MVCC not prevent it?", a: "A lost update is when two transactions read the same row, both compute a new value based on what they read, and both write back — the second write silently overwrites the first. MVCC does not prevent it because both transactions got a valid snapshot; it does not serialize their writes. Prevention requires SELECT FOR UPDATE (pessimistic locking) or a stricter isolation level." },
    { q: "What does VACUUM do and why is it needed after MVCC updates?", a: "VACUUM scans the heap and marks dead tuple slots (old row versions no transaction can see anymore) as reusable for future inserts. Every UPDATE in Postgres writes a new row version and marks the old one dead; without periodic VACUUM the file would grow without bound and every seq scan would crawl past dead rows it cannot return." },
  ]}
/>

<Recap lang="en">
MVCC keeps every row as a chain of versions, each stamped with the transaction that wrote it. When a transaction starts, Postgres gives it a snapshot — a frozen view of which versions are visible. Readers see old versions; writers create new ones; neither ever blocks the other. The storage cost is dead versions accumulating until VACUUM reclaims them. MVCC does not prevent lost updates: two concurrent transactions can still overwrite each other's work if the application does not use row-level locking or a stricter isolation level.
</Recap>
