infographic chart-column-simple
data
  title Same index, 200M rows: build time vs blast radius
  desc CONCURRENTLY costs 2.25x the build time to take zero DML downtime
  values
    - label CREATE INDEX (ACCESS EXCLUSIVE, blocks reads + writes)
      value 20
    - label CREATE INDEX CONCURRENTLY (SHARE UPDATE EXCL, blocks nothing)
      value 45
theme
  palette #f43f5e #14b8a6
