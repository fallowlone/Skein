---
concepts:
- schema-migration
- migration-versioning
- deploy-order
- migration-tool
deepensInto: []
estMin: 8
lang: en
lessonType: topic
level: junior
order: 1
prereqs:
- databases/01-relational-model/01-what-a-relation-is
slug: 01-what-a-migration-is
sources:
- https://www.postgresql.org/docs/current/sql-altertable.html
- https://www.prisma.io/docs/orm/prisma-migrate/getting-started
- https://flywaydb.org/documentation/
spiral:
- statefulness
status: ready
summary: A migration is a versioned, ordered SQL script that changes the database
  schema in lock-step with the application code that depends on it.
title: What a schema migration is and why it replaces ad-hoc DDL
track: databases
unit: 06-migrations
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Otto wants to add a `phone` column to `users`. He opens psql and runs `ALTER TABLE users ADD COLUMN phone TEXT`. Three months later a new engineer joins, clones the repo, and gets errors because their database has no `phone` column. Nobody knows what to change. The schema has drifted.
</Hook>

<Crux>A migration is a versioned SQL script moving the schema from state N to N+1, keeping every environment reproducible.</Crux>

<Explanation>

## The problem with ad-hoc DDL

Running `ALTER TABLE` directly in psql works once, on one machine, for one engineer. The moment a second environment exists — staging, CI, a teammate's laptop — the schema diverges silently. Nobody tracks what changed or when. Forward reproducibility is gone.

A **migration file** encodes the change as a named, ordered artefact:

```sql
-- migrations/20260514_add_user_phone.sql
ALTER TABLE users ADD COLUMN phone TEXT;
```

The migration tool records which files have run in a `schema_migrations` table. A new engineer running `prisma migrate dev` or `flyway migrate` gets all pending migrations applied in order. The database reaches the same known state on every machine.

## Migrations are versioned like commits

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Git commit</th>
        <th class="border border-border px-3 py-2 text-left">Migration file</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Moves codebase from state N to N+1</td>
        <td class="border border-border px-3 py-2">Moves schema from state N to N+1</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Ordered by timestamp/hash</td>
        <td class="border border-border px-3 py-2">Ordered by version prefix (timestamp or V1/V2)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Applied once; immutable after merge</td>
        <td class="border border-border px-3 py-2">Applied once per database; never edited after deploy</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Reviewable as a diff in a PR</td>
        <td class="border border-border px-3 py-2">Reviewable SQL in a PR, linted by Squawk or Atlas</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Reproducible: `git clone` + history = same code</td>
        <td class="border border-border px-3 py-2">Reproducible: `migrate up` from blank = same schema</td>
      </tr>
    </tbody>
  </table>
</div>

## Deploy order matters

The database schema and the application code that uses it must be compatible at every moment of the deploy, because production traffic does not pause for your migration.

Two rules:

1. **Additive change** (new column the new code reads): run the migration **before** deploying code. Old code ignores the new column; new code finds it ready.
2. **Removal** (drop a column the old code reads): deploy new code **first**, wait for old code to drain, then run the migration. Otherwise old code queries a non-existent column and fails.

Single-transaction migrations that drop or rename columns in one step are the classic deploy-coupled bug; the fix is to split changes into safe additive steps — covered in lesson 05.

<Inset kind="why" lang="en">
Why do migration tools use a `schema_migrations` table instead of comparing the live schema against the desired schema? Diffing schemas is error-prone — two schemas can look identical structurally while having different indexes, constraints, or permissions. Tracking which migration files have run is simpler, auditable, and survives database restores.
</Inset>

<Quiz
  id="01-what-a-migration-is-quiz-1"
  lessonSlug="01-what-a-migration-is"
  lang="en"
  question="Why use migration files instead of running ALTER TABLE directly in psql?"
  choices={[
    { label: "Migrations are versioned, reviewable, reproducible across environments, and auditable — they answer 'what changed and when' for every schema state change", correct: true },
    { label: "Postgres requires migration files for DDL", misconception: "Postgres accepts any DDL ad-hoc; the issue is reproducibility and auditability, not a Postgres requirement." },
    { label: "Migration files are faster than psql DDL", misconception: "The same SQL runs identically either way. The benefit is workflow discipline, not execution speed." },
    { label: "psql does not support ALTER TABLE", misconception: "psql supports all DDL. The problem is forward reproducibility across environments, not psql capability." },
  ]}
/>

<Quiz
  id="01-what-a-migration-is-quiz-2"
  lessonSlug="01-what-a-migration-is"
  lang="en"
  question="A migration adds a new column that the new application code reads. What is the correct deploy order?"
  choices={[
    { label: "Run the migration first, then deploy app code — old code ignores the new column; new code finds it ready", correct: true },
    { label: "Deploy app code first, then run the migration", misconception: "New code tries to read a column that does not exist yet and fails until the migration runs." },
    { label: "Run migration and code deploy simultaneously", misconception: "There is a window where new code is live but the column is not yet present — requests fail." },
    { label: "Order does not matter for additive changes", misconception: "Order always matters. The only safe additive order is: migration first." },
  ]}
/>

<DragOrder
  id="01-what-a-migration-is-drag-1"
  lessonSlug="01-what-a-migration-is"
  lang="en"
  prompt="Order the lifecycle steps of a production schema migration:"
  items={[
    { id: "a", label: "Write the migration SQL file with a version prefix (timestamp or V-number)" },
    { id: "b", label: "Open a PR: SQL is reviewed, linted by Squawk or Atlas CI" },
    { id: "c", label: "Merge the PR — migration file is now in version control" },
    { id: "d", label: "CI runs migration against staging; tests pass" },
    { id: "e", label: "Migration runs on production before the new code deploys" },
    { id: "f", label: "Application code that depends on the schema change is deployed" },
    { id: "g", label: "Migration tool records the file as applied in schema_migrations" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g"]}
/>

<MetaphorComplete
  id="01-what-a-migration-is-metaphor-1"
  lessonSlug="01-what-a-migration-is"
  lang="en"
  setup="Fill in the blank: a database migration is to schema changes what a git ___ is to code changes — a versioned, ordered, reviewable unit that moves the system from state N to state N+1."
  accepted={["commit", "commits"]}
  canonical="commit"
  explanation="A commit takes code from one state to another with history, review, and rollback. A migration does the same for schema. Both have ordered ancestry; both are immutable once shipped; both are how teams reason about change over time."
/>

</Explanation>

<FlowDiagram
  label="A schema migration travels an ordered pipeline: write versioned SQL, review and lint it in a PR, merge, run on CI staging, apply on production before the new code deploys, then record it as applied in schema_migrations."
  perRow={4}
  hue="var(--d-data)"
  nodes={[
    { id: "write", label: "Write SQL", sub: "version prefix" },
    { id: "pr", label: "PR review", sub: "Squawk / Atlas lint" },
    { id: "merge", label: "Merge", sub: "in version control" },
    { id: "staging", label: "CI staging", sub: "tests pass" },
    { id: "prod", label: "migrate prod", sub: "before code" },
    { id: "deploy", label: "Deploy code", sub: "reads new schema" },
    { id: "record", label: "schema_migrations", sub: "marked applied" },
  ]}
  edges={[
    { from: "write", to: "pr" },
    { from: "pr", to: "merge" },
    { from: "merge", to: "staging" },
    { from: "staging", to: "prod" },
    { from: "prod", to: "deploy" },
    { from: "deploy", to: "record" },
  ]}
  caption="Each migration is an ordered, reviewable artefact — applied once per database, recorded in schema_migrations, so every environment reaches the same schema state."
/>

<KeyTakeaway>A migration is a versioned SQL script applied once per database in order — making the schema reproducible across every environment, just like git makes code reproducible.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-what-a-migration-is-retrieval"
  lang="en"
  questions={[
    { q: "In two sentences: what is a schema migration and what problem does it solve over ad-hoc DDL?", a: "A migration is a versioned, ordered SQL file that moves the database schema from one state to the next, tracked by a tool in a schema_migrations table. Ad-hoc DDL runs on one machine and leaves no record — migrations make the schema reproducible across staging, CI, and production, and auditable in version control history." },
    { q: "Why does the deploy order of migration vs app code matter, and what is the rule for an additive change?", a: "Production traffic does not pause during a deploy, so both the old and new app versions must be compatible with the current schema at every moment. For an additive change (new column the new code reads), run the migration first: old code ignores the new column, new code finds it immediately. Running code first creates a window where new code references a column that does not exist yet — requests fail." },
    { q: "What does the schema_migrations table contain and why is it better than diffing the schema?", a: "The schema_migrations table records the version identifiers of every migration file that has been applied, in order. Diffing a live schema against a desired schema is fragile — two schemas can look structurally equivalent while differing in index definitions, constraint names, or permissions. Tracking applied migrations is deterministic and survives database restores." },
  ]}
/>

<Recap lang="en">
A migration is a versioned SQL file that moves the database schema from state N to N+1 and is recorded in the `schema_migrations` table once applied. Running `ALTER TABLE` ad-hoc works once but leaves no history and diverges environments silently. Migration tools (Prisma Migrate, Flyway, golang-migrate, Atlas) apply pending files in order on every environment so the schema is always reproducible from blank. Deploy order is not optional: for additive changes, run the migration first so the schema is ready when new code lands.
</Recap>
