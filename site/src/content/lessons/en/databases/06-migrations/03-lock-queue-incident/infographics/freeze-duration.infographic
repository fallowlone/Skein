infographic chart-column-simple
data
  title Table-freeze duration: lock_timeout off vs on
  desc Same 8 ms migration — the freeze is set by how long the ALTER waits in the queue
  values
    - label No lock_timeout (default 0)
      value 60
    - label lock_timeout = 2s
      value 2
theme
  palette #f43f5e #14b8a6
