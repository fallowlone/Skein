---
concepts:
- ADD-COLUMN-instant
- pg-attribute-missing-val
- volatile-default
- NOT-NULL-pattern
- backfill
deepensInto: []
estMin: 10
lang: en
lessonType: topic
level: junior
order: 2
prereqs:
- databases/06-migrations/01-what-a-migration-is
slug: 02-add-column-safely
sources:
- https://www.postgresql.org/docs/current/sql-altertable.html
- https://www.postgresql.org/docs/current/explicit-locking.html
- https://xata.io/blog/migrations-and-exclusive-locks
spiral:
- statefulness
- latency
status: ready
summary: A constant default makes ADD COLUMN instant on any table size since PG 11;
  volatile defaults and NOT NULL without backfill still rewrite or block.
title: 'ADD COLUMN: instant in PG 11+ vs rewrite in older Postgres'
track: databases
unit: 06-migrations
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A team deploys `ALTER TABLE orders ADD COLUMN status TEXT NOT NULL DEFAULT 'pending'` on a 50M-row table. On Postgres 10 the database freezes for 15 minutes. On Postgres 11+, the same statement finishes in milliseconds. Same SQL, completely different outcome. In the next ten minutes you will understand why — and which ADD COLUMN forms are still dangerous even on the latest Postgres.
</Hook>

<Crux>ADD COLUMN with a constant default is instant since PG 11 — metadata only, no row rewrite. Volatile defaults still rewrite.</Crux>

<Explanation>

## How Postgres 11 made ADD COLUMN instant

Before PG 11, `ALTER TABLE t ADD COLUMN c TEXT DEFAULT 'x'` rewrote every row to materialise the default value. On a 100M-row table that meant minutes holding `AccessExclusiveLock` — every reader and writer blocked for the duration.

Since PG 11, when the default is **non-volatile** (a literal string, number, boolean, or stable expression), Postgres stores the default in `pg_attribute` system catalog:
- `pg_attribute.atthasmissing = true`
- `pg_attribute.attmissingval = 'x'`

Existing rows keep their original physical storage — the column is not written. When you `SELECT` an old row, Postgres reads `attmissingval` and synthesises the value at read time. New rows inserted after the migration store the column normally. The lock hold time drops from minutes to milliseconds.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Operation</th>
        <th class="border border-border px-3 py-2 text-left">PG 10</th>
        <th class="border border-border px-3 py-2 text-left">PG 11+</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>ADD COLUMN c TEXT DEFAULT 'pending'</code></td>
        <td class="border border-border px-3 py-2">Full table rewrite — minutes per 100M rows</td>
        <td class="border border-border px-3 py-2">Instant — default in pg_attribute metadata</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>ADD COLUMN c TEXT DEFAULT clock_timestamp()</code></td>
        <td class="border border-border px-3 py-2">Full table rewrite</td>
        <td class="border border-border px-3 py-2">Full table rewrite — volatile default, must materialise per row</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>ADD COLUMN c TEXT</code> (no default)</td>
        <td class="border border-border px-3 py-2">Instant — NULL for existing rows</td>
        <td class="border border-border px-3 py-2">Instant — NULL for existing rows</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>ADD COLUMN c TEXT NOT NULL</code> (no default, rows exist)</td>
        <td class="border border-border px-3 py-2">Fails — existing NULLs violate NOT NULL</td>
        <td class="border border-border px-3 py-2">Fails — existing NULLs violate NOT NULL</td>
      </tr>
    </tbody>
  </table>
</div>

## Volatile defaults still rewrite

When you see a `DEFAULT` that calls a function, ask yourself: does this function return the same value every time? If not, Postgres cannot use the fast path. `DEFAULT clock_timestamp()`, `DEFAULT random()`, `DEFAULT gen_random_uuid()` — any volatile function — cannot be stored as a single missing value because each row needs a different result. Postgres must materialise the default per-row, which means a full table rewrite under `AccessExclusiveLock`.

**Fix**: add the column without a default (instant), then backfill existing rows with an `UPDATE` in batches, then set the default for future inserts with `ALTER TABLE t ALTER COLUMN c SET DEFAULT gen_random_uuid()`.

## Adding NOT NULL safely on a large table

`ADD COLUMN c TEXT NOT NULL DEFAULT 'x'` is fine on PG 11+ for a constant default — instant, no rewrite. The danger is adding NOT NULL to an existing nullable column that may have NULLs:

```sql
-- WRONG on a large table: scans every row under AccessExclusiveLock
ALTER TABLE users ALTER COLUMN email_verified SET NOT NULL;
```

The safe multi-step pattern (covered in depth in lesson 04):

1. Add the column without NOT NULL: instant.
2. Backfill existing NULLs in small batches.
3. Add `CHECK (col IS NOT NULL) NOT VALID` — enforces for new rows only, no table scan.
4. `VALIDATE CONSTRAINT` — scans under a lighter lock that does not block writes.
5. `ALTER COLUMN SET NOT NULL` — fast because the constraint already proves validity.

<NumbersCard
  id="card-add-column-costs"
  title="ADD COLUMN cost comparison"
  stats={[
    { label: "ADD COLUMN constant default, PG 11+", value: "Instant (ms)" },
    { label: "ADD COLUMN volatile default, any PG", value: "Minutes per 100M rows" },
    { label: "ADD COLUMN no default", value: "Instant (NULL for existing rows)" },
    { label: "Lock taken by ADD COLUMN", value: "AccessExclusiveLock (brief or long)" },
    { label: "Read-time cost for missing-default rows", value: "Negligible (single pg_attribute lookup)" },
    { label: "Cost disappears as rows are UPDATE-d", value: "Passively, via normal traffic" },
  ]}
/>

<Inset kind="why" lang="en">
Why did Postgres wait until version 11 (2018) to make ADD COLUMN instant? The change required a new mechanism in the heap tuple reading code: every read of an old-format tuple has to check `atthasmissing` and inject the stored default. This made the read path slightly more complex and required careful testing. The payoff was enormous — it eliminated the most common cause of migration-induced downtime pre-2018.
</Inset>

<Quiz
  id="02-add-column-safely-quiz-1"
  lessonSlug="02-add-column-safely"
  lang="en"
  question="In PG 11+, which ADD COLUMN form is instant (no table rewrite)?"
  choices={[
    { label: "ALTER TABLE t ADD COLUMN x TEXT DEFAULT 'pending'", correct: true },
    { label: "ALTER TABLE t ADD COLUMN x TEXT DEFAULT clock_timestamp()", misconception: "Volatile default forces per-row evaluation — full table rewrite even in PG 11+." },
    { label: "ALTER TABLE t ALTER COLUMN x TYPE bigint USING x::bigint", misconception: "Type changes with USING rewrite the table — different operation entirely." },
    { label: "ALTER TABLE t ADD COLUMN x TEXT NOT NULL (with no default, rows exist)", misconception: "This fails because existing rows would be NULL, violating the constraint." },
  ]}
/>

<Quiz
  id="02-add-column-safely-quiz-2"
  lessonSlug="02-add-column-safely"
  lang="en"
  question="What does PG 11 store in pg_attribute to make ADD COLUMN with a constant default instant?"
  choices={[
    { label: "atthasmissing = true and attmissingval = the default value — old rows are not rewritten; the default is synthesised at read time", correct: true },
    { label: "A trigger that fills in the default for each old row on first read", misconception: "No trigger is involved — the read path checks pg_attribute directly." },
    { label: "A background job that fills in the default over time", misconception: "There is no background job; the default is synthesised lazily at query time." },
    { label: "Nothing — the table is updated immediately but faster than before", misconception: "Existing rows are not touched. The metadata change makes the read path produce the value." },
  ]}
/>

<DragOrder
  id="02-add-column-safely-drag-1"
  lessonSlug="02-add-column-safely"
  lang="en"
  prompt="Order the steps to safely add a NOT NULL column to a 100M-row production table:"
  items={[
    { id: "a", label: "ADD COLUMN with a constant DEFAULT (instant in PG 11+; NULL default if none needed)" },
    { id: "b", label: "Deploy app code that writes the new column on every INSERT and UPDATE" },
    { id: "c", label: "Backfill existing NULL rows in batches of 1k–10k with pg_sleep between batches" },
    { id: "d", label: "ADD CONSTRAINT email_verified_nn CHECK (col IS NOT NULL) NOT VALID (instant, no scan)" },
    { id: "e", label: "VALIDATE CONSTRAINT (scans under SHARE UPDATE EXCLUSIVE — does not block writes)" },
    { id: "f", label: "ALTER COLUMN SET NOT NULL (fast — constraint already proved validity)" },
    { id: "g", label: "Optionally DROP the CHECK constraint (it is redundant once NOT NULL is set)" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g"]}
/>

</Explanation>

<FlowDiagram
  label="On PG 11+ ADD COLUMN branches on the default: a constant default is stored in pg_attribute metadata and synthesised at read time so the operation is instant, while a volatile default must be materialised per row, forcing a full table rewrite."
  perRow={3}
  hue="var(--d-data)"
  nodes={[
    { id: "add", label: "ADD COLUMN", sub: "brief ACCESS EXCL" },
    { id: "constant", label: "Constant default", sub: "'pending', 42" },
    { id: "volatile", label: "Volatile default", sub: "now(), uuid()" },
    { id: "meta", label: "pg_attribute", sub: "attmissingval set" },
    { id: "rewrite", label: "Rewrite every row", sub: "materialise per row" },
    { id: "instant", label: "Instant", sub: "synthesised at read" },
    { id: "slow", label: "Minutes / 100M rows", sub: "ACCESS EXCL held" },
  ]}
  edges={[
    { from: "add", to: "constant" },
    { from: "add", to: "volatile" },
    { from: "constant", to: "meta" },
    { from: "volatile", to: "rewrite" },
    { from: "meta", to: "instant" },
    { from: "rewrite", to: "slow" },
  ]}
  caption="Constant default: metadata-only, instant on any table size. Volatile default: per-row materialisation, a full rewrite under ACCESS EXCLUSIVE."
/>

<KeyTakeaway>ADD COLUMN with a constant default is instant on PG 11+ via stored metadata; volatile defaults still rewrite. Never set NOT NULL in one step on large tables — use ADD COLUMN + backfill + NOT VALID + VALIDATE + SET NOT NULL instead.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-add-column-safely-retrieval"
  lang="en"
  questions={[
    { q: "How does PG 11+ make ADD COLUMN with a constant default instant, and what is the read-time behaviour for old rows?", a: "PG 11 stores the default in pg_attribute.attmissingval (with atthasmissing = true) instead of materialising it into each row. Old rows retain their original storage. When a query reads an old row that lacks the new column, the executor checks pg_attribute and injects the missing value at read time. New rows inserted after the migration store the column normally. The lock-hold time drops from minutes (full rewrite) to milliseconds (catalog update)." },
    { q: "Why do volatile defaults still cause a full table rewrite even on PG 11+?", a: "A volatile function like clock_timestamp() or random() produces a different value per call. Postgres cannot store a single shared missing value in pg_attribute because each row needs its own unique result. The database must visit every existing row, call the function, and write the result — a full heap rewrite under AccessExclusiveLock, just as before PG 11." },
    { q: "What is the multi-step pattern to add a NOT NULL constraint to a nullable column on a large table?", a: "Step 1: ADD COLUMN with no NOT NULL (instant, existing rows get NULL). Step 2: deploy app code that always writes the column. Step 3: backfill NULLs in 1k–10k-row batches with pg_sleep breathing room. Step 4: ADD CONSTRAINT c CHECK (col IS NOT NULL) NOT VALID — enforces for new rows, no table scan. Step 5: VALIDATE CONSTRAINT — scans under SHARE UPDATE EXCLUSIVE (does not block DML). Step 6: ALTER COLUMN SET NOT NULL — fast because the constraint already proves all rows pass." },
  ]}
/>

<Recap lang="en">
Since PG 11, `ADD COLUMN` with a non-volatile constant default stores the value in `pg_attribute.attmissingval` and returns it synthetically at read time — no rows are touched, the operation completes in milliseconds on any table size. Volatile defaults (functions like `clock_timestamp()`) still require per-row materialisation and cause a full rewrite. Adding `NOT NULL` to an existing nullable column without a backfill scan is never safe in a single `ALTER COLUMN SET NOT NULL` on a large table; the correct path splits the work into an instant column add, a batched backfill, a `NOT VALID` constraint, a `VALIDATE CONSTRAINT` under a light lock, and finally a fast `SET NOT NULL`. Now when you see an `ADD COLUMN` in a migration file, check the default first: is it a literal or a function call? That single question tells you whether the operation is instant or a production risk.
</Recap>
