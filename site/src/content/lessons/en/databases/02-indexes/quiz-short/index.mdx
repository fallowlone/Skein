---
concepts:
- leading-column-rule
- partial-index
- index-only-scan
- visibility-map
- index-types
- fk-index
estMin: 14
lang: en
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.postgresql.org/docs/current/indexes-multicolumn.html
- https://www.postgresql.org/docs/current/indexes-index-only-scans.html
- https://www.postgresql.org/docs/current/indexes-examine.html
status: ready
summary: "Free-recall prompts across the indexes unit — answer each in your own words first, then reveal the model answer and compare."
title: 'Indexes: free-recall review'
track: databases
unit: 02-indexes
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory before you open the model answer — the effort of recall is what makes the leading-column rule and the Visibility Map stick.
</Hook>

<Goal lang="en">
Reconstruct the unit's core mechanisms — the leading-column rule, when partial beats full, what makes an index-only scan fire, how to choose an index type, and the most common production failures — without looking back at the lessons.
</Goal>

<RetrievalDrawer
  client:load
  id="02-indexes-short-retrieval"
  lang="en"
  questions={[
    { q: "Why does a composite index on (a, b, c) help WHERE a = ? and WHERE a = ? AND b = ?, but not WHERE b = ? — and what is the one design rule that follows?", a: "A multi-column B-tree is sorted primarily by a, then by b within equal a, then by c within equal (a, b). The engine navigates the tree using that sort order, so it can only narrow when the filter supplies a leading prefix — a, then (a, b), then (a, b, c). A filter on b alone gives no starting point in the global ordering, so the index offers no entry; the planner falls back to a seq scan (or a full index scan, usually worse). The design rule (the leading-column rule): make the leading column the one that is always present in the filter — typically the tenant/scope column in a multi-tenant app — then add secondary columns in selectivity order, and put ORDER BY columns last so the index also serves the sort without a separate Sort node." },
    { q: "When does a partial index win over a full index, and what must the query satisfy to use it?", a: "A partial index (CREATE INDEX ... WHERE predicate) indexes only the rows matching the predicate. It wins whenever the hot queries always target a minority of rows — e.g. a tasks table that is 80% done but only open/in_progress are queried, or a soft-delete pattern (WHERE deleted_at IS NULL). The index is proportional to the matching fraction: a 20%-matching predicate yields an index ~20% the size of the full one, with proportionally lower write overhead — and writes to non-matching rows (done rows) never touch it at all. To use it, the query's WHERE clause must logically imply the index predicate: WHERE workspace_id = $1 AND status = 'pending' can use a WHERE status = 'pending' partial index; WHERE workspace_id = $1 alone cannot, because it would also need non-pending rows. Bonus: a partial UNIQUE index (UNIQUE (email) WHERE deleted_at IS NULL) enforces uniqueness only among live rows, enabling soft-delete-and-re-register." },
    { q: "What two conditions must both hold for an index-only scan to avoid heap fetches, and how do you keep them true in production?", a: "(1) Coverage: every column the query references — in WHERE, ORDER BY, and SELECT — must be in the index, either as a key column or an INCLUDE column. INCLUDE (Postgres 11+) stores projection-only columns in the leaf pages without making them part of the sort key, so you cover the projection without disturbing the sort order. (2) Visibility: the Visibility Map bit for each matching row's heap page must be set (all tuples on the page visible to all transactions). The index has no xmin/xmax, so a clear VM bit forces Postgres to fetch the heap page to check visibility — these show up as Heap Fetches in EXPLAIN ANALYZE. VACUUM sets VM bits; any write clears them. Production discipline: on IOS-dependent tables lower autovacuum_vacuum_scale_factor (e.g. to 0.05), monitor pg_stat_user_tables.n_dead_tup, and kill long-running transactions that block VACUUM from reclaiming tuples." },
    { q: "Name when you would reach past B-tree for GIN, GiST, BRIN, and pgvector HNSW, and the cost each carries.", a: "B-tree covers ~95% of cases: equality, range, sort, and prefix LIKE on ordered types. GIN: many values per row — JSONB containment (@>), array overlap, full-text tsvector (@@) — built as an inverted index; cost is 2-5× the column size and high write overhead (one posting-list update per key/lexeme per insert), mitigated by fastupdate or narrowing to expression indexes on hot fields. GiST: data with no total order — PostGIS geometry, range types, nearest-neighbour (ORDER BY geom <-> point); cost is more complex, extension-dependent updates. BRIN: huge, physically-ordered, append-mostly tables (time-series by created_at) — stores min/max per block range, so the index is a few MB even on terabytes; useless if data is not physically ordered, because every block range spans the whole value range. pgvector HNSW: semantic similarity over embeddings; large (vector_dim × rows, tens of GB), slow writes (graph rebalancing), approximate recall (95-99%), and needs a model inference pipeline." },
    { q: "Why are foreign-key columns a recurring production trap, and what is the rule?", a: "When you declare REFERENCES posts(id), Postgres indexes the referenced column (the parent PK) automatically, but never the referencing column on the child (comments(post_id)). So an ON DELETE CASCADE — or any join/filter of the child by its FK — sequential-scans the entire child table to find matching rows. The classic incident: DELETE FROM posts WHERE id = 42 takes minutes and holds locks while it scans 100M+ comments. The rule: index every FK column (CREATE INDEX CONCURRENTLY on comments(post_id)) unless you can prove the parent is never deleted/updated in the referenced column and no query filters or joins the child by that FK." },
    { q: "List the seven production index failure modes and the single diagnostic that exposes each.", a: "(1) Missing FK index — slow cascade/join; EXPLAIN the DELETE or join shows a Seq Scan on the child. (2) Implicit type coercion (WHERE bigint_col = '42') — EXPLAIN shows a cast in the filter and a Seq Scan; fix with typed parameters. (3) Stale statistics — EXPLAIN ANALYZE shows estimated rows wildly off from actual; fix with ANALYZE / extended statistics. (4) Index bloat — index size several times the expected size for the row count; pgstattuple_approx confirms; fix with REINDEX CONCURRENTLY. (5) Wrong composite order — query filters a non-leading column and seq-scans; check leading column vs the filter. (6) JSONB GIN cardinality bomb — GIN 10-50× the column size, insert latency spike, cache-hit ratio drops; narrow to expression indexes on known-hot fields. (7) 'Has an index but still slow' — EXPLAIN (ANALYZE, BUFFERS) shows a Sort node after the index scan or high Heap Fetches; fix with a covering composite that serves filter + sort + projection. The universal tool is EXPLAIN (ANALYZE, BUFFERS) on the exact query with production-realistic parameters." },
  ]}
/>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the unit's spine: a composite is usable only from a leading prefix, so design it around the always-present filter and trail it with the ORDER BY columns; partial indexes shrink in proportion to the hot subset and skip cold-row writes; an index-only scan needs both full coverage (key + INCLUDE) and a current Visibility Map (VACUUM); the index type must fit the data shape; FK columns are never auto-indexed; and the seven failure modes all surface under one diagnostic — EXPLAIN (ANALYZE, BUFFERS) on the real query.
</Recap>
