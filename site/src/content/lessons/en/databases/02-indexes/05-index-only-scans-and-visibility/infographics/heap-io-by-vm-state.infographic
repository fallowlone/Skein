infographic chart-column-simple
data
  title Heap I/O for the same 1,000-row dense query
  desc Same index, same rows — the Visibility Map decides the cost
  values
    - label Index scan (heap fetch per row)
      value 1000
    - label Index-only scan, stale VM (Heap Fetches high)
      value 1000
    - label Index-only scan, current VM (Heap Fetches: 0)
      value 10
theme
  palette #f43f5e #f59e0b #14b8a6
