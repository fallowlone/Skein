---
concepts:
- b-tree-index
- sequential-scan
- index-scan
- write-amplification
- create-index-concurrently
deepensInto: []
estMin: 12
lang: en
lessonType: topic
level: junior
order: 1
prereqs:
- databases/01-relational-model/01-what-a-relation-is
slug: 01-index-anatomy
sources:
- https://www.postgresql.org/docs/current/indexes.html
- https://www.postgresql.org/docs/current/indexes-types.html
- https://use-the-index-luke.com/
spiral:
- latency
status: ready
summary: An index is a sorted copy of column values that lets Postgres skip full table
  scans. Every index trades write speed and storage for read speed.
title: What an index is and how it speeds up queries
track: databases
unit: 02-indexes
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A team's "filter by status" dashboard takes 50ms on 100k rows in staging. In production with 50M rows it takes 8 seconds. The schema is correct. The query is correct. One DDL line — `CREATE INDEX CONCURRENTLY ON orders(status)` — brings it back to 20ms. That line is the entirety of this lesson.
</Hook>

By the end of this lesson you will know exactly why that one line works, what it costs, and when adding it makes things worse instead of better.

<Crux>An index is a sorted copy of column values for one question. Every index taxes every write. Pick deliberately.</Crux>

<Explanation>

## What an index is

An index is a separate data structure — usually a B-tree — that stores a sorted copy of one or more column values plus pointers to the actual rows. When a query filters by an indexed column, Postgres navigates the tree in O(log n) steps instead of scanning every row.

The book-index metaphor holds precisely: a book's back-matter index is a sorted list of terms with page numbers. To find "MVCC", you open the index (one O(log n) lookup), read the page number, and jump there. Without the index you scan every page. A database index is identical — a copy of column values, organized for fast lookup by one specific question.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Without index</th>
        <th class="border border-border px-3 py-2 text-left">With index</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Sequential scan — every row touched</td>
        <td class="border border-border px-3 py-2">B-tree walk — O(log n) then fetch matching rows</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">1M rows: ~10ms; 100M rows: ~2–10s</td>
        <td class="border border-border px-3 py-2">1M or 100M rows: ~1–5ms</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">No extra storage, no write overhead</td>
        <td class="border border-border px-3 py-2">~10–30% table size per index; every write maintains the index</td>
      </tr>
    </tbody>
  </table>
</div>

## The cost: indexes tax every write

Why not index every column and call it a day? Because every index you add is a permanent tax on every future write. The question is not "could an index help this query?" but "is the read speedup worth the write slowdown on this table?"

Every index is a separate on-disk structure that must stay synchronized with the table. Every `INSERT`, `UPDATE`, or `DELETE` that touches an indexed column must also update the index — costing CPU and I/O. A table with ten unused indexes on a write-heavy workload spends 10× more on writes than necessary.

Typical per-index write overhead: 5–50 µs per write. At 10,000 inserts/second with ten indexes, that is an extra 0.5–5ms of overhead per second of clock time — measurable, and often the bottleneck when teams over-index.

Index storage on disk: roughly `bytes_per_key × row_count × 1.3`. For a BIGINT key (8 bytes), a 100M-row table produces a ~1 GB index. Each index beyond the first multiplies this.

## A concrete scenario

Consider: `SELECT * FROM orders WHERE user_id = 42` on a table with 10M rows. Without an index: sequential scan, ~2 seconds. After `CREATE INDEX ON orders(user_id)`: index lookup plus heap fetch, ~3 milliseconds. Same query, 600× speedup, one DDL line.

Postgres ships six index types. B-tree is the default and handles 95% of production use — equality, range, `ORDER BY`, `LIKE 'abc%'`. The other five (GIN, GiST, BRIN, Hash, Bloom) serve specific data shapes and are covered in lesson 04.

## Production rule: always use CONCURRENTLY

`CREATE INDEX` takes an `ACCESS EXCLUSIVE` lock — all reads and writes are blocked for the duration. On a table with active traffic, even a 5-minute lock is an incident.

`CREATE INDEX CONCURRENTLY` uses a multi-phase build that does not take the exclusive lock. It scans the table twice and waits for in-flight transactions between phases. It is 2–3× slower and cannot be wrapped in a transaction, but it produces no visible impact on application traffic.

If a `CONCURRENTLY` build fails (e.g., a unique-index encounters a duplicate, or the process is killed), the index is left in an `INVALID` state (`indisvalid = false` in `pg_indexes`). The planner treats it as non-existent. Fix: `DROP INDEX CONCURRENTLY` and retry.

<Inset kind="why" lang="en">
Why does Postgres not just index every column by default? Because every index on a write-heavy table is a write tax. A table with 20 indexes and 50k inserts/second can spend more time maintaining indexes than writing rows. The discipline is intentional: measure the hot queries, add indexes for those, audit and drop the unused. Over-indexing and under-indexing are equally damaging production patterns.
</Inset>

<DragOrder
  id="01-index-anatomy-drag-1"
  lessonSlug="01-index-anatomy"
  lang="en"
  prompt="Order the steps to add an index correctly in production:"
  items={[
    { id: "a", label: "Run EXPLAIN ANALYZE on the slow query to confirm a seq scan is the bottleneck" },
    { id: "b", label: "Identify the columns being filtered or sorted" },
    { id: "c", label: "Pick the index type (B-tree default; GIN for JSONB; GiST for geo)" },
    { id: "d", label: "Use CREATE INDEX CONCURRENTLY to avoid locking the table" },
    { id: "e", label: "Wait for the build to complete (can take minutes to hours on large tables)" },
    { id: "f", label: "Re-run EXPLAIN ANALYZE to confirm the planner now uses the index" },
    { id: "g", label: "Monitor write latency after deploy — every index adds a small write cost" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g"]}
/>

<Quiz
  id="01-index-anatomy-quiz-1"
  lessonSlug="01-index-anatomy"
  lang="en"
  question="An index is created on the column users(email). Which query benefits from it?"
  choices={[
    { label: "SELECT * FROM users WHERE email = 'alice@x.com'", correct: true },
    { label: "SELECT * FROM users WHERE display_name = 'Alice'", misconception: "The index is on email, not display_name." },
    { label: "INSERT INTO users (...) VALUES (...)", misconception: "Indexes do not speed up inserts; they slow them down (the index must be updated)." },
    { label: "SELECT COUNT(*) FROM users", misconception: "COUNT(*) needs all rows; a single-column index does not help with this in Postgres." },
  ]}
/>

<Quiz
  id="01-index-anatomy-quiz-2"
  lessonSlug="01-index-anatomy"
  lang="en"
  question="What is the cost of adding an index?"
  choices={[
    { label: "No cost — indexes are free", misconception: "Every index costs storage and slows down every write to the indexed column." },
    { label: "Storage on disk plus a small overhead on every INSERT/UPDATE/DELETE that touches the indexed columns", correct: true },
    { label: "Only a one-time creation cost", misconception: "Creation cost matters, but the per-write overhead is the long-term cost." },
    { label: "Network bandwidth between client and server", misconception: "Indexes are server-side; they do not affect network." },
  ]}
/>

<MetaphorComplete
  id="01-index-anatomy-metaphor-1"
  lessonSlug="01-index-anatomy"
  lang="en"
  setup="Fill in the blank: a database index is to a SQL query what a book _______ is to finding a topic — a copy of part of the content, organised for fast lookup."
  accepted={["index"]}
  canonical="index"
  explanation="A book index is the closest everyday analogue: alphabetical list of terms with page numbers, a small separate structure that makes the main content quickly searchable by one dimension. Database indexes generalize this to arbitrary column types and conditions."
/>

</Explanation>

<FlowDiagram
  label="Index scan path: a filter on an indexed column walks the B-tree from root to leaf in O(log n), then fetches the matching row from the heap."
  perRow={5}
  hue="var(--d-data)"
  nodes={[
    { id: "query", label: "Query", sub: "WHERE user_id = 42" },
    { id: "root", label: "B-tree root", sub: "O(log n) walk" },
    { id: "leaf", label: "Leaf page", sub: "sorted keys + TIDs" },
    { id: "heap", label: "Heap fetch", sub: "follow TID" },
    { id: "row", label: "Row" },
  ]}
  edges={[
    { from: "query", to: "root" },
    { from: "root", to: "leaf" },
    { from: "leaf", to: "heap" },
    { from: "heap", to: "row" },
  ]}
  caption="Instead of touching every row (sequential scan), the index walks the tree top-down to the matching leaf, then follows the pointer to fetch only the rows that match."
/>

<KeyTakeaway>An index is a sorted copy of column values that turns full-table scans into O(log n) lookups. Every index adds ~10–30% storage overhead and 5–50 µs per write. Always use CREATE INDEX CONCURRENTLY in production; check pg_indexes for INVALID state afterward.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-index-anatomy-retrieval"
  lang="en"
  questions={[
    { q: "In two sentences, why are indexes not free and why do production teams not just index every column?", a: "Every index is a separate sorted data structure on disk that must be kept in sync with the table — every INSERT, UPDATE, or DELETE that touches an indexed column has to update the index too, which costs CPU and I/O. A table with ten unused indexes on a write-heavy workload spends 10× more on writes than necessary, and the indexes also consume storage (often 10–30% of the table size each) and pollute the planner's choices." },
    { q: "What happens if CREATE INDEX CONCURRENTLY fails mid-way, and how do you recover?", a: "The index is left in an INVALID state — indisvalid = false in pg_indexes. The planner treats it as non-existent, so queries do not use it, but the index still consumes storage. Recovery: DROP INDEX CONCURRENTLY on the invalid index, then retry CREATE INDEX CONCURRENTLY." },
    { q: "A query does a sequential scan on a 50M-row table. You add an index. What does EXPLAIN ANALYZE show before and after?", a: "Before: Seq Scan on the table, estimating and touching all 50M rows. After (if the index is selective enough): Index Scan or Index Only Scan, touching only the matching leaf entries in the B-tree and fetching a small number of heap rows. The 'rows' estimate and actual time both drop dramatically — from seconds to milliseconds for a selective filter." },
  ]}
/>

<Recap lang="en">
An index is a separate sorted data structure — typically a B-tree — that stores copies of one or more column values with pointers to the matching heap rows. It turns full-table sequential scans (O(n)) into tree walks (O(log n)), reducing a 2-second query on 10M rows to 3ms. The tradeoff: every index costs 10–30% of table size in storage and 5–50 µs per write per index. Always add indexes with `CREATE INDEX CONCURRENTLY` to avoid table-level locks in production. If the build fails, check `pg_indexes` for `indisvalid = false` and retry after dropping the invalid entry.

Now when you see a slow query on a large table, your first instinct should be `EXPLAIN ANALYZE` — and if it shows a Seq Scan, you know exactly what to reach for and what it will cost.
</Recap>
