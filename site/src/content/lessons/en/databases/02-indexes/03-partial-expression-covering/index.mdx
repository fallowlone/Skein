---
slug: 03-partial-expression-covering
lang: en
track: databases
unit: 02-indexes
order: 3
title: "Partial, expression, and covering indexes"
summary: "Three index modifiers that change what a B-tree costs and what it can answer: partial restricts to a row subset, expression indexes a derived value, INCLUDE adds projection columns without affecting the sort key."
estMin: 14
status: ready
lessonType: topic
level: middle
prereqs: ["02-leading-column-rule"]
deepensInto: []
spiral: ["latency"]
concepts: ["partial-index", "expression-index", "covering-index", "include-clause", "unique-partial-index", "index-only-scan", "soft-delete"]
sources:
  - https://www.postgresql.org/docs/current/indexes-partial.html
  - https://www.postgresql.org/docs/current/indexes-expressional.html
  - https://www.postgresql.org/docs/current/indexes-index-only-scans.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook>
A tasks table has 100M rows. 80% have `status = 'done'` and are never queried by the dashboard. The full B-tree on `(workspace_id, created_at)` is 2 GB and every insert touches it. A partial index `WHERE status IN ('open','in_progress')` is 400 MB, 5× cheaper to maintain, and the dashboard queries run just as fast — because it only indexes the rows the dashboard actually needs.
</Hook>

<Crux>Partial indexes cover a row subset. Expression indexes key on derived values. INCLUDE columns add projections to leaves for heap-free scans.</Crux>

<Explanation>

## Partial indexes

A partial index is a B-tree (or any index type) built only over rows matching a `WHERE` predicate.

```sql
CREATE INDEX ON orders(user_id) WHERE status = 'pending';
```

This indexes only pending orders — typically a small fraction of the table. The result: smaller index, faster lookup on the hot subset, cheaper write overhead (the index is only touched when `status = 'pending'`).

For the planner to use a partial index, the query's `WHERE` clause must logically imply the index's predicate. A query `WHERE user_id = 42 AND status = 'pending'` can use the index. A query `WHERE user_id = 42` alone cannot (it would need to match all statuses, but the index only covers pending rows).

**Production use cases:**
- Soft-delete pattern: `WHERE deleted_at IS NULL` — only active rows are indexed; historical deleted rows are excluded.
- Hot-subset filters: a task table where 95% of rows are `status = 'done'` — index only `'open'` and `'in_progress'`.
- Feature flags: `WHERE feature_x_enabled = true` — index only rows where the flag is active.

**Partial unique indexes** are a powerful variant. `CREATE UNIQUE INDEX ON users (email) WHERE deleted_at IS NULL` enforces "email is unique among non-deleted users." This lets you soft-delete a user and later re-register the same email — impossible with a simple `UNIQUE` constraint on the `email` column.

## Expression indexes

An expression index indexes a function of one or more columns, not the columns themselves.

```sql
CREATE INDEX ON users (LOWER(email));
```

This lets `WHERE LOWER(email) = 'alice@x.com'` use the index. Without it, `LOWER(email)` is evaluated on every row, defeating any index on the `email` column.

Common patterns:
- Case-insensitive search: `LOWER(email)`, `UPPER(code)`.
- Date truncation: `DATE_TRUNC('day', created_at)` for group-by-day queries.
- JSON path extraction: `(data->>'event_type')` — indexes one field inside a JSONB column.
- Computed values: `COALESCE(first_name, '') || ' ' || COALESCE(last_name, '')` for name-search.

The query must use the exact expression for the planner to recognize the index. `WHERE LOWER(email) = ?` uses the index. `WHERE email = ?` does not (unless the email is already lowercase and a regular index exists).

## Covering indexes with INCLUDE

A regular B-tree index stores key columns in both internal nodes and leaf pages. The `INCLUDE` clause (Postgres 11+) adds extra columns to the leaf pages only — they are not part of the sort key, but they are stored in the leaf, available for the query.

When a query's `SELECT` projection needs only columns that are present in the index (either as key columns or `INCLUDE` columns), Postgres can answer the query without fetching the row from the heap at all — this is an **index-only scan**. It is 10–100× faster than an index scan for hot reads because it eliminates the heap fetch entirely.

```sql
CREATE INDEX CONCURRENTLY idx_orders_workspace_pending_recent
ON orders (workspace_id, created_at DESC)
WHERE status = 'pending'
INCLUDE (id, total_cents);
```

This index:
- Filters by `workspace_id` (leading column, tenant scope).
- Sorts by `created_at DESC` (serves the `ORDER BY` without a Sort step).
- Partial `WHERE status = 'pending'` — only covers ~20% of the table.
- `INCLUDE (id, total_cents)` — the typical dashboard projection is covered, no heap fetch needed.

<NumbersCard
  id="card-index-cost"
  title="Indexes: size, build, and write cost"
  stats={[
    { label: "B-tree size, 8-byte key, 100M rows", value: "~1 GB" },
    { label: "B-tree depth, 100M rows", value: "~4 levels" },
    { label: "B-tree lookup time", value: "~5-50 μs" },
    { label: "Index build (CREATE INDEX CONCURRENTLY)", value: "~10-30 min / 100M rows" },
    { label: "Index write overhead per INSERT/UPDATE", value: "~5-50 μs per index touched" },
    { label: "Typical index overhead vs table size", value: "~10-30% per index" },
    { label: "Index-only scan vs index scan speedup", value: "~10-100x on hot reads" },
    { label: "Partial index size, 5%-matching predicate", value: "~5% of full B-tree" },
  ]}
/>

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Modifier</th>
        <th class="border border-border px-3 py-2 text-left">What it does</th>
        <th class="border border-border px-3 py-2 text-left">When to use</th>
      </tr>
    </thead>
    <tbody>
      <tr><td class="border border-border px-3 py-2">Partial (WHERE)</td><td class="border border-border px-3 py-2">Index only rows matching predicate</td><td class="border border-border px-3 py-2">Hot subset is &lt;20% of table; or unique constraint on non-deleted rows</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">Expression</td><td class="border border-border px-3 py-2">Index a computed value</td><td class="border border-border px-3 py-2">Query filters on LOWER(col), DATE_TRUNC, JSON field, or other function</td></tr>
      <tr><td class="border border-border px-3 py-2">INCLUDE</td><td class="border border-border px-3 py-2">Store extra columns in leaf pages without affecting sort</td><td class="border border-border px-3 py-2">Query projects columns beyond the key; enables index-only scan</td></tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="en">
Why are partial indexes underused in practice? Because they require the developer to know the predicate before writing the query. Many teams add indexes by looking at slow queries after the fact, not by thinking about data distribution at schema design time. The discipline: when a new hot query always filters by a specific status, flag, or soft-delete condition, build the partial index at the same time the feature ships. The cost is near-zero at feature time; the benefit is 10–20× cheaper index maintenance across the table's lifetime.
</Inset>

<FadedExample
  client:load
  id="fe-idx-1"
  lessonSlug="03-partial-expression-covering"
  lang="en"
  title="Pick the right index for a hot dashboard query"
  steps={{
    solved: (
      <div class="space-y-3 text-sm">
        <p><strong>Query (runs every dashboard page-load, ~50/sec):</strong></p>
        <pre class="text-xs bg-zinc-100 p-2 rounded overflow-x-auto"><code>{`SELECT id, total_cents, created_at
FROM orders
WHERE workspace_id = $1
  AND status = 'pending'
ORDER BY created_at DESC
LIMIT 50;`}</code></pre>
        <p><strong>Right index:</strong></p>
        <pre class="text-xs bg-zinc-100 p-2 rounded overflow-x-auto"><code>{`CREATE INDEX CONCURRENTLY idx_orders_workspace_pending_recent
ON orders (workspace_id, created_at DESC)
WHERE status = 'pending'
INCLUDE (id, total_cents);`}</code></pre>
        <p><strong>Why each choice:</strong> Leading column workspace_id (matches the dominant filter and is the tenant boundary). Sort column created_at DESC (matches the ORDER BY exactly — index serves the sort without an extra Sort step). Partial WHERE status = 'pending' (the dashboard only shows pending; the index is ~5% of full-table size, ~20x cheaper to maintain). INCLUDE (id, total_cents) makes the index covering — the query is satisfied entirely from the index (index-only scan, no heap fetch). Expected plan: Index Only Scan on idx_orders_workspace_pending_recent, returning 50 rows in under 1ms on a 100M-row table.</p>
      </div>
    ),
    semi: {
      prompt: "If a second dashboard query is SELECT ... WHERE workspace_id = $1 AND status = 'shipped' ORDER BY created_at DESC, would the above partial index help? Answer yes or no.",
      blanks: [
        { id: "answer", expected: "no", placeholder: "yes or no" },
      ],
    },
    blank: {
      prompt: "Why does the composite (workspace_id, created_at DESC) work but (created_at DESC, workspace_id) would not?",
      reveal: (
        <div class="space-y-2 text-sm">
          <p>The leading column rule. The dashboard always filters by workspace_id first (this is multi-tenant — every query is scoped to one workspace). With workspace_id as the leading column, the index narrows to that workspace's rows immediately, and the secondary sort by created_at DESC gives the planner the requested order for free. With created_at DESC as the leading column, the index is globally sorted by time across all workspaces; to find one workspace's recent orders, the planner has to scan all recent orders and filter by workspace — most of which it discards. The order of columns in a composite index is the order of selectivity priorities; put the most-filtered column first.</p>
        </div>
      ),
    },
  }}
  misconceptions={{
    "answer:yes": "Partial indexes only help queries that match the WHERE clause exactly. status='shipped' does not match WHERE status='pending', so the planner cannot use this index.",
  }}
/>

<Quiz
  id="03-partial-expression-covering-quiz-1"
  lessonSlug="03-partial-expression-covering"
  lang="en"
  question="A query: WHERE LOWER(email) = 'alice@x.com'. Which index does the planner use?"
  choices={[
    { label: "CREATE INDEX ON users (email)", misconception: "This indexes the raw email column. The function LOWER() is evaluated on every row at query time, preventing use of the raw-email index." },
    { label: "CREATE INDEX ON users (LOWER(email))", correct: true },
    { label: "No index can help here", misconception: "An expression index on LOWER(email) is exactly what the planner needs." },
    { label: "CREATE INDEX ON users (email text_pattern_ops)", misconception: "text_pattern_ops helps LIKE prefix searches, not case-insensitive equality." },
  ]}
/>

<Quiz
  id="03-partial-expression-covering-quiz-2"
  lessonSlug="03-partial-expression-covering"
  lang="en"
  question="A table has 10M rows, 95% with status = 'done'. A dashboard queries only WHERE status = 'open'. Which is the best index strategy?"
  choices={[
    { label: "CREATE INDEX ON tasks(status, created_at) — covers both filter and sort", misconception: "This full index covers all 10M rows including the 9.5M 'done' rows the dashboard never queries. It is 20× larger than it needs to be." },
    { label: "CREATE INDEX ON tasks(created_at) WHERE status = 'open' — partial on the hot subset only", correct: true },
    { label: "No index needed — only 5% match, so the planner will not use an index anyway", misconception: "5% of 10M is 500k rows. Postgres will use the index if it is selective enough; 5% is selective." },
    { label: "CREATE INDEX ON tasks(status) — simpler is better", misconception: "A full index on status covers all 10M rows and does not serve the ORDER BY created_at. The partial index is 20× smaller and equally fast for the hot queries." },
  ]}
/>

</Explanation>

<KeyTakeaway>Partial indexes (WHERE clause) cover only a row subset — use them when queries always filter a hot minority. Expression indexes cover computed values — required for LOWER(), DATE_TRUNC(), JSON fields. INCLUDE columns make an index covering for a query's projection, enabling index-only scans that skip the heap entirely (10–100x faster). All three are underused productivity levers.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-partial-expression-covering-retrieval"
  lang="en"
  questions={[
    { q: "Why can a partial unique index enforce a constraint that a plain UNIQUE constraint on a column cannot?", a: "A plain UNIQUE constraint on email prevents any two rows from having the same email. A partial unique index on email WHERE deleted_at IS NULL enforces uniqueness only among non-deleted rows — so you can have one active row with email 'alice@x.com' and any number of soft-deleted rows with the same email. This is the soft-delete re-register pattern: the user is soft-deleted, later creates a new account with the same email, and the constraint allows it because the deleted row's deleted_at IS NOT NULL (excluded from the partial index)." },
    { q: "What two conditions must hold for Postgres to use an index-only scan?", a: "(1) Every column the query needs (both in the WHERE and SELECT) must be present in the index — either as a key column or as an INCLUDE column. (2) The Visibility Map bit for the row's heap page must be set (all tuples on the page are visible to all active transactions). Without condition (1), Postgres must fetch the row from the heap for the projected columns. Without condition (2), Postgres must fetch the row to check tuple visibility — even though it has the data from the index." },
    { q: "A query filters on DATE_TRUNC('day', created_at) = '2026-01-15'. There is a plain index on created_at. Does the planner use it?", a: "No. The function DATE_TRUNC is evaluated on every row, preventing use of the raw created_at index. The planner does a sequential scan, evaluating the function on each row. Fix: create an expression index on DATE_TRUNC('day', created_at) — the query must match the exact expression. Alternatively, rewrite the query as WHERE created_at >= '2026-01-15' AND created_at < '2026-01-16' (a range on the raw column), which the plain created_at index can serve." },
  ]}
/>

<Recap lang="en">
Three modifiers turn a plain B-tree into a targeted tool. Partial indexes (WHERE predicate) restrict coverage to matching rows — 5% coverage means 5% of full-index size and write cost. Expression indexes cover computed values like `LOWER(email)` or `(data->>'field')` — the query must use the exact same expression. INCLUDE columns (Postgres 11+) store projection-only columns in the leaf pages without affecting the sort key, enabling index-only scans that never touch the heap. Used together — partial + composite + INCLUDE — a well-designed index can cover an entire hot dashboard query with near-zero heap I/O.
</Recap>
