---
slug: 01-the-seven-acts
lang: en
track: databases
unit: 08-putting-it-together
order: 1
title: "The seven acts: from CREATE TABLE to Citus"
summary: "A single product grows from a 1-table MVP to a 1-billion-row Citus cluster through seven moments of database failure — each one names the trigger, the symptom, and the lever that resolves it."
estMin: 12
status: ready
lessonType: topic
level: junior
prereqs:
  - databases/01-relational-model/01-what-a-relation-is
  - databases/02-indexes/01-index-anatomy
  - databases/03-execution-plans/01-explain-and-plans
  - databases/04-mvcc-isolation/04-vacuum-and-bloat
  - databases/05-pooling/01-why-pools-exist
  - databases/06-migrations/01-what-a-migration-is
  - databases/07-sharding/01-why-sharding-exists
deepensInto:
  - databases/08-putting-it-together/02-schema-indexes-plans
spiral: ["statefulness", "latency"]
concepts: ["seven-acts", "growth-story", "lever-order", "capstone-synthesis"]
sources:
  - https://www.postgresql.org/docs/current/
  - https://wiki.postgresql.org/wiki/Don't_Do_This
  - https://github.blog/2021-08-31-partitioning-githubs-relational-databases-scale/
  - https://docs.citusdata.com/en/stable/
  - https://www.cybertec-postgresql.com/en/postgresql-vacuum-and-bloat/
  - https://www.pgmustard.com/blog/postgres-row-estimates-misleading
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook>
Day 0 at a SaaS startup. The PM wants "search users by email." One engineer, one Postgres, one table. Three years later: 1 billion rows, six Citus shards, and a runbook the size of a textbook. Between those two points are seven moments when the database broke and the team learned.
</Hook>

<Crux>Seven prior pieces are seven times a product outgrew its database — starting from CREATE TABLE. The order is not optional.</Crux>

<Explanation>

The seven pieces of this chapter map one-to-one onto seven acts of product growth. Each act has a trigger, a symptom, and a lever. Skip a lever and the next act costs more to resolve.

**Act 1 — Day 0, schema design.** "users(email, name, org_id)." Should `email` be `UNIQUE`? Should `org_id` be a foreign key? Should `prefs` be a side table or `JSONB`? Decisions: `email` is `UNIQUE NOT NULL CITEXT`. `org_id` is `BIGINT REFERENCES orgs(id) ON DELETE CASCADE`. `prefs` is `JSONB` with a GIN index when search demands it. A surrogate `id BIGSERIAL` insulates from email changes. Bend the rules only with measured throughput pressure, never as a default.

**Act 2 — Week 1, 10K rows, the first slow query.** Email-search endpoint p95 climbs from 30 ms to 800 ms. The planner does a sequential scan. `CREATE INDEX users_email_idx ON users(email)`. The leading-column rule of B-tree means `WHERE email = ?` resolves with two page reads. p95 drops to 4 ms.

**Act 3 — Month 1, 100K rows, the planner lies.** Half the requests are fast, half take 600 ms. `EXPLAIN ANALYZE` shows the planner sometimes picks a seq-scan despite the index. Row estimate is off by 30×: stale statistics. `ANALYZE users;` rebuilds the histograms. The planner picks plans from statistics, not from data; statistics maintenance is the operational discipline.

**Act 4 — Month 6, the silent bloat.** A nightly report runner holds one transaction open for four hours. `VACUUM` cannot reclaim dead tuples below the `xmin` horizon. The `users` table swells from 200 MB to 80 GB. Fix: kill the long transaction, set `idle_in_transaction_session_timeout = 60s`, run `pg_repack` to reclaim disk. Permanent fix: replicate to a read replica for reporting.

**Act 5 — Year 1, 1M users + 50 app pods, the connection storm.** Each pod opens its own Postgres backend on cold-start. At pod-rollout the cluster sees 1000 concurrent backends; the kernel scheduler thrashes; 4 ms queries take 4 s. PgBouncer in transaction-mode: 100 server-side backends, 10000 client connections multiplexed. Sizing rule: `pool_size = active_concurrent_transactions × safety_factor`, not `max_app_workers`.

**Act 6 — Year 2, the migration that froze prod.** Multi-tenancy day. `ALTER TABLE users ADD COLUMN tenant_id BIGINT NOT NULL DEFAULT 0` takes an `AccessExclusiveLock` and triggers a full table rewrite. Prod freezes for eight minutes. The expand-contract recipe: add nullable column, backfill in batches, add `CHECK ... NOT VALID`, `VALIDATE CONSTRAINT`, `SET NOT NULL`, drop the check.

**Act 7 — Year 3, 1B rows, the hot shard.** Citus is rolled out, shard key is `tenant_id`. Tenant `Acme` accounts for 40% of all queries. Acme's shard saturates while the others idle. Fix: co-location of related tables on the same shard key; online resharding with `citus_rebalance_table_shards`.

<NumbersCard
  id="card-seven-acts"
  title="Seven acts — trigger and lever at a glance"
  stats={[
    { label: "Act 1 — Day 0", value: "Schema: surrogate keys, FKs, constraints" },
    { label: "Act 2 — Week 1, 10K rows", value: "Index on the filtered column" },
    { label: "Act 3 — Month 1, 100K rows", value: "ANALYZE to refresh statistics" },
    { label: "Act 4 — Month 6, 200 MB → 80 GB", value: "Kill long tx, pg_repack, timeout" },
    { label: "Act 5 — Year 1, 50 pods", value: "PgBouncer transaction-mode pool" },
    { label: "Act 6 — Year 2, multi-tenancy", value: "Expand-contract migration recipe" },
    { label: "Act 7 — Year 3, 1B rows", value: "Citus sharding with co-location" },
  ]}
  data-lesson-visual
/>

**Why the order matters.**

A database is like a growing city. The schema is zoning. Indexes are the street map. Execution plans are the traffic dispatcher. MVCC is multiple lanes so cars pass without colliding. The connection pool is the parking garage. Migrations are construction crews who must not close every road at once. Sharding is annexing new districts when one city block can't hold the traffic. A skipped lesson means the city keeps growing, but the wrong layer is overloaded.

Each act unlocks the next only if you nailed the previous one. Skip Act 1 (schema) and Acts 2–7 fight a losing battle against inefficient joins. Skip Act 2 (indexing) and Act 3 planning decisions are made blind. Skip Act 3 (stats) and Act 4 vacuum is the only lever left. Skip Act 4 (bloat) and Act 5 pool threads see table scans get slower. Skip Act 5 (pooling) and Act 6 migrations are starved by connection storms. Skip Act 6 (lock safety) and Act 7 sharding is impossible. The order is a constraint imposed by physics and Postgres internals.

<Inset kind="why" lang="en">
The price of a late fix is exponential: Act 1 mistakes cost days to fix; Act 7 mistakes cost months of resharding. Teams that skip early acts pay catastrophic costs later. Teams that over-engineer early acts (sharding a 10 GB dataset) waste resources on problems that do not exist yet. The art is knowing your growth trajectory and timing each act to land just before the prior act's failure modes become production pain.
</Inset>

<Quiz
  id="01-the-seven-acts-quiz-1"
  lessonSlug="01-the-seven-acts"
  lang="en"
  question="At 10K rows, the email-search endpoint is suddenly slow. Cheapest first lever?"
  choices={[
    { label: "Add a PgBouncer pool", misconception: "Connection pooling helps at high pod-count, not at low data volume with a missing index." },
    { label: "Create an index on the filtered column", correct: true },
    { label: "Shard the table by tenant_id", misconception: "Sharding is a year-3 lever, not a week-1 lever." },
    { label: "Run VACUUM FULL", misconception: "VACUUM helps with bloat, not with a query that lacks an index." },
  ]}
/>

<Quiz
  id="01-the-seven-acts-quiz-2"
  lessonSlug="01-the-seven-acts"
  lang="en"
  question="The disk is full but the row count has not changed. Most likely cause?"
  choices={[
    { label: "Missing index", misconception: "Indexes use disk but rarely cause sudden growth without row changes." },
    { label: "Bloat from a long-running transaction blocking VACUUM", correct: true },
    { label: "The query planner is broken", misconception: "Planner choices do not change disk usage." },
    { label: "A migration ran twice", misconception: "Migration replays are rare; bloat from a blocked VACUUM is the common cause." },
  ]}
/>

<Quiz
  id="01-the-seven-acts-quiz-3"
  lessonSlug="01-the-seven-acts"
  lang="en"
  question="Why is sharding a year-3 lever, not a year-1 lever?"
  choices={[
    { label: "Sharding is harder than indexes and largely irreversible once tenants are spread across shards", correct: true },
    { label: "Sharding does not work below 1B rows", misconception: "Sharding works at any scale; the operational complexity is what costs you." },
    { label: "Postgres does not support sharding", misconception: "Citus, Vitess, and declarative partitioning all exist." },
    { label: "The data must be migrated before sharding can start", misconception: "The reason is operational cost and irreversibility, not data migration requirements." },
  ]}
/>

<DragOrder
  id="01-the-seven-acts-drag-1"
  lessonSlug="01-the-seven-acts"
  lang="en"
  prompt="Order the seven scale-tier levers from earliest (Day 0) to latest (Year 3):"
  items={[
    { id: "a", label: "Design the relational schema (tables, keys, constraints)" },
    { id: "b", label: "Add the right index for the query" },
    { id: "c", label: "Verify the execution plan uses the index; run ANALYZE" },
    { id: "d", label: "Hunt the long transaction blocking VACUUM" },
    { id: "e", label: "Put a connection pooler in front of Postgres" },
    { id: "f", label: "Migrate schema safely with expand-contract" },
    { id: "g", label: "Shard the largest table across nodes" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g"]}
/>

</Explanation>

<KeyTakeaway>Seven acts, seven levers — schema, index, stats, bloat, pool, migration, shard. Each skipped lever compounds cost at the next tier. The order is a constraint, not a suggestion; Act 1 mistakes cost days, Act 7 mistakes cost months.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-the-seven-acts-retrieval"
  lang="en"
  questions={[
    { q: "Name the seven levers in order and give one symptom that signals each tier.", a: "Schema (greenfield design pressure). Index (single-column query slow — sequential scan despite small data). Stats (index exists but seq-scan chosen half the time). Bloat (disk full, row count unchanged, vacuum logs zero reclaimed). Pool (cold-start connection storm — 4 ms queries take 4 s). Migration (ALTER blocks prod — AccessExclusiveLock queue). Sharding (one tenant dominates load — shard saturates while others idle)." },
    { q: "Why does the order of acts matter — why is skipping Act 2 not 'fine, I'll add indexes later'?", a: "Each act's solution assumes the prior acts are sound. A missing index at 10K rows is a one-line fix; a missing index at 1B rows requires a concurrent build that adds hours of write overhead. More critically: Act 3 (statistics) is only useful if the indexes exist for the planner to pick between; Act 5 pool threads experience table-scan latency as the worst case when indexes are absent; Act 7 sharding distributes an already-slow query across shards and makes it slower. The order is a constraint imposed by Postgres internals." },
    { q: "Trace the city metaphor: match each layer (schema, index, plans, MVCC, pool, migrations, sharding) to its city element.", a: "Schema = zoning (land-use decisions made at day 0 that are expensive to change). Index = street map (how you find a row). Plans/stats = traffic dispatcher (picks which roads to use based on last known traffic). MVCC = multiple lanes per road (cars pass without colliding). Pool = parking garage (finite slots, multiplexed across drivers). Migrations = construction crew (cannot close all roads at once; expand-contract is the recipe for one lane at a time). Sharding = annexing new districts (when one city block can no longer hold the load)." },
  ]}
/>

<Recap lang="en">
A product's database passes through seven growth tiers, each with a distinct trigger and a single correct lever. Schema decisions made on Day 0 compound into every later act — a surrogate key and proper FK constraints are cheap greenfield choices that become expensive retrofits at scale. Indexes fix queries at 10K rows; missing one at 1B rows is a multi-hour concurrent build under traffic. Stale statistics cause the planner to ignore existing indexes; ANALYZE is the operational discipline. Bloat from a long-held transaction can grow a 200 MB table to 80 GB in days; the lever is killing the long transaction, not adding more disk. Connection storms at pod-rollout time require a connection pooler, not more backends. Schema migrations that take an AccessExclusiveLock queue every query behind them; expand-contract avoids the freeze. Sharding distributes load but multiplies every operational task — it is the last resort, entered deliberately after all prior levers are applied. The order is a constraint imposed by Postgres internals: skip one act and every subsequent act costs exponentially more.
</Recap>
