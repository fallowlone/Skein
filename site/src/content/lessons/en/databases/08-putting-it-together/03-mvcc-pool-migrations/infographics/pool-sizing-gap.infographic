infographic chart-bar-plain-text
data
  title Pool sizing: what the naive config opens vs what the DB can schedule
  desc Size on active concurrent transactions, not max_app_workers
  values
    - label Naive: 50 pods x 20 workers open at rollout
      value 1000
    - label PgBouncer server-side backends (10000 clients multiplexed)
      value 100
    - label Sweet spot ceiling, 16-core SSD server
      value 60
    - label Sweet spot floor, 16-core SSD server
      value 30
theme
  palette #f43f5e #14b8a6 #6366f1
