infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label USE + RED signal maps to which act
    children
      - label High backend count, low CPU
        children
          - label Act 5 — pooling problem (deploy pooler)
      - label High dead tuples, stable row count
        children
          - label Act 4 — vacuum/bloat (kill xmin pinners)
      - label One queryid, rising duration
        children
          - label Act 2 or 3 — index or stats problem
      - label Rising p95, one tenant's shards hot
        children
          - label Act 7 — hot shard problem
theme
  palette #f43f5e #f59e0b #6366f1 #14b8a6
