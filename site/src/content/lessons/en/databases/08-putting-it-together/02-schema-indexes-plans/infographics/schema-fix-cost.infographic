infographic sequence-ascending-steps
data
  title Cost of a wrong type rises with table size
  desc The same Day-0 schema mistake; the bill grows as rows accumulate
  order asc
  sequences
    - label 1M rows — hours
      desc Fix the wrong type in place; cheap enough to absorb
    - label 100M rows — days + maintenance window
      desc Full-table type change; needs downtime or expand-contract
    - label 1B rows — multi-week project
      desc Any schema change is a planned migration of its own
theme
  palette #14b8a6 #f59e0b #f43f5e
