---
concepts:
- relation
- tuple
- attribute
- domain
- candidate-key
- primary-key
- foreign-key
- constraint
- referential-integrity
- normalization
deepensInto: []
estMin: 12
lang: en
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-what-a-relation-is
sources:
- https://www.postgresql.org/docs/current/ddl-constraints.html
- https://en.wikipedia.org/wiki/Relational_model
- https://www.postgresql.org/docs/current/datatype.html
spiral:
- statefulness
- encapsulation
status: ready
summary: The core vocabulary of the relational model — relations, tuples, candidate
  keys, and why declarative constraints are the model''''s enduring win.
title: 'What a relation is: tables, rows, keys, and constraints'
track: databases
unit: 01-relational-model
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import PersonaTag from "~/components/pedagogy/PersonaTag.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A team stores "user has many tags" as a comma-separated column. Six months later: "how many users have tag X?" — every row must be parsed. A relational design answers that question as a point lookup. The difference is the model you start with.
</Hook>

<Crux>A relation is a set of tuples sharing a fixed shape, and the engine enforces that shape — so you cannot commit bad data.</Crux>

<Explanation>

## What a relation is

Edgar Codd's 1970 paper defined the foundations of every SQL database since. In ten minutes you will know why the comma-separated column breaks and what to do instead. The vocabulary is small:

- **Relation** — a set of tuples sharing the same shape. The table is the SQL implementation.
- **Tuple** — a single row; every tuple in a relation has the same attributes.
- **Attribute** — a column; each draws values from a *domain* (a type).
- **Candidate key** — a minimal subset of attributes that uniquely identifies every tuple. A table may have several candidate keys; one is designated the *primary key*.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Concept</th>
        <th class="border border-border px-3 py-2 text-left">SQL term</th>
        <th class="border border-border px-3 py-2 text-left">What it means</th>
      </tr>
    </thead>
    <tbody>
      <tr><td class="border border-border px-3 py-2">Relation</td><td class="border border-border px-3 py-2">Table</td><td class="border border-border px-3 py-2">A set of rows sharing one shape</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">Tuple</td><td class="border border-border px-3 py-2">Row</td><td class="border border-border px-3 py-2">One record</td></tr>
      <tr><td class="border border-border px-3 py-2">Attribute</td><td class="border border-border px-3 py-2">Column</td><td class="border border-border px-3 py-2">One named typed value per row</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">Domain</td><td class="border border-border px-3 py-2">Type</td><td class="border border-border px-3 py-2">The set of valid values for an attribute</td></tr>
      <tr><td class="border border-border px-3 py-2">Candidate key</td><td class="border border-border px-3 py-2">PRIMARY KEY / UNIQUE</td><td class="border border-border px-3 py-2">A minimal row identifier</td></tr>
    </tbody>
  </table>
</div>

## Why constraints are the model's enduring win

When you inherit a codebase and find data inconsistencies that "shouldn't be possible," almost always the answer is: no constraints. The relational model does not just store data — it refuses bad data. Constraints are declarative rules the engine checks before any insert, update, or delete:

- **PRIMARY KEY** — uniquely identifies each row; implies NOT NULL and UNIQUE.
- **FOREIGN KEY** — a column referencing a primary or unique key in another table; the engine refuses orphan rows.
- **NOT NULL** — this attribute must always have a value.
- **UNIQUE** — this column or column set has no duplicates across rows.
- **CHECK** — an arbitrary boolean expression evaluated on every write; e.g. `CHECK (amount >= 0)`.

Without constraints you have a key-value store with SQL syntax. Application bugs can commit bad data. With constraints, the engine refuses on behalf of every caller — application code does not have to remember the rules.

## The metaphor

A relational schema is a library catalogue. Each table is a drawer (books, authors, loans). Each row is a card with the same fields. A loan card has member-id and book-id pointing at other drawers (foreign keys). The librarian (the engine) refuses to file a loan card if the book-id does not exist. The system stays coherent without humans re-checking.

## A practical scenario

<PersonaTag id="sven" lang="en" /> wants a "favourites" feature for a marketplace. <PersonaTag id="otto" lang="en" /> asks the shape question. Sven says "user has many favourites." Otto reaches for the model: users, items, favourites table with `(user_id, item_id)` as PK plus two FKs. Three DDL lines and the feature is structurally correct — no duplicates, no orphans, queryable both ways. A JSON-array shortcut breaks the moment you ask "who favourited this item."

Another team stores addresses as comma-separated text on the user row. Six months later marketing asks "how many users in Oregon?" — every text field must be parsed. A year later finance asks "sales by state" — same problem on every order. A relational design (addresses table with structured columns) is two extra schema lines and three orders of magnitude cheaper to query.

<Inset kind="why" lang="en">
The cost of the relational model is the constraint check at write time and the discipline of designing the schema before you can write code. Senior engineers pay that cost knowingly; new teams either pay it accidentally (denormalize first, regret later) or skip it (treat the database as a key-value store and accumulate years of integrity debt). This lesson is about why the first path is almost always cheaper across the lifetime of a system.
</Inset>

<DragOrder
  id="01-what-a-relation-is-drag-1"
  lessonSlug="01-what-a-relation-is"
  lang="en"
  prompt="Order the steps to design a schema for 'user has many addresses':"
  items={[
    { id: "a", label: "Identify the entities: User, Address" },
    { id: "b", label: "For each entity, pick a primary key (id BIGSERIAL or uuid)" },
    { id: "c", label: "Identify the relationship: one user has many addresses (1:N)" },
    { id: "d", label: "Add a user_id column to addresses with REFERENCES users(id)" },
    { id: "e", label: "Add NOT NULL on the FK (every address must belong to a user)" },
    { id: "f", label: "Add an index on addresses(user_id) so 'list addresses for user X' is fast" },
    { id: "g", label: "Decide ON DELETE: CASCADE or RESTRICT" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g"]}
/>

<Quiz
  id="01-what-a-relation-is-quiz-1"
  lessonSlug="01-what-a-relation-is"
  lang="en"
  question="What is a primary key in a relational table?"
  choices={[
    { label: "A column or set of columns that uniquely identifies each row, with NOT NULL and UNIQUE enforced together", correct: true },
    { label: "The first column of the table", misconception: "Position is not what defines a primary key." },
    { label: "Any indexed column", misconception: "Indexes are a separate concept; not every index is a primary key." },
    { label: "A column generated automatically by the database", misconception: "PK can be natural (business value) or surrogate (auto-generated); auto-generation is not required." },
  ]}
/>

<Quiz
  id="01-what-a-relation-is-quiz-2"
  lessonSlug="01-what-a-relation-is"
  lang="en"
  question="What does a foreign key declaration buy you?"
  choices={[
    { label: "Speed — foreign keys make joins faster", misconception: "Foreign keys do not make joins faster; indexes do." },
    { label: "Referential integrity — the database refuses to insert a row pointing at a non-existent parent, and ON DELETE clauses decide what happens when the parent is removed", correct: true },
    { label: "Compression — foreign-key columns take less space", misconception: "FK columns are not specially compressed." },
    { label: "Replication consistency", misconception: "Replication is a separate subsystem; FK enforces local integrity." },
  ]}
/>

<MetaphorComplete
  id="01-what-a-relation-is-metaphor-1"
  lessonSlug="01-what-a-relation-is"
  lang="en"
  setup="Fill in the blank: the database engine refuses bad inserts because of declared _______ — rules like NOT NULL, UNIQUE, FOREIGN KEY, CHECK."
  accepted={["constraints", "constraint"]}
  canonical="constraints"
  explanation="Constraints are declarative rules the database checks at write time. They are what move correctness from 'every application must remember' to 'the engine guarantees'. Without them you have a key-value store with SQL syntax."
/>

</Explanation>

<FlowDiagram
  label="A favourites join table holds two foreign keys, one pointing at the users table and one at the items table; each foreign key arrow runs from the child row to the parent it must reference."
  hue="var(--d-data)"
  perRow={3}
  nodes={[
    { id: "users", label: "users", sub: "id PK" },
    { id: "fav", label: "favourites", sub: "user_id, item_id PK" },
    { id: "items", label: "items", sub: "id PK" },
  ]}
  edges={[
    { from: "fav", to: "users", label: "user_id FK" },
    { from: "fav", to: "items", label: "item_id FK" },
  ]}
  caption="The composite PK (user_id, item_id) blocks duplicates; the two foreign keys point at real parent rows, so the engine refuses any orphan favourite."
/>

<KeyTakeaway>The relational model stores data in tables of typed, constrained rows. The engine enforces PRIMARY KEY, FOREIGN KEY, NOT NULL, UNIQUE, and CHECK at write time — so bad data cannot be committed regardless of which application touches the table.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-what-a-relation-is-retrieval"
  lang="en"
  questions={[
    { q: "In two sentences, why is storing 'user has many tags' as a JSON array column usually worse than a tags table plus a user_tags join table?", a: "The JSON-array shortcut works for 'show me one user's tags' but breaks every other query: 'who has tag X' requires scanning every row and parsing every array; 'how many users have tag X' same problem; 'rename tag X to Y' requires rewriting every row that contains it. The relational shape makes all three queries point lookups, lets the engine index from either direction, and uses foreign keys to guarantee that user_tags only references real users and real tags." },
    { q: "Name the five constraint kinds a relational engine enforces and state what each one does.", a: "PRIMARY KEY — uniquely identifies each row, implies NOT NULL and UNIQUE. FOREIGN KEY — a reference to a key in another table; refuses orphan rows. NOT NULL — the column must always have a value. UNIQUE — no duplicate values across rows for this column set. CHECK — an arbitrary boolean expression evaluated on every write; refuses rows where it is false." },
    { q: "What is the difference between a candidate key and a primary key?", a: "A candidate key is any minimal set of attributes that uniquely identifies every tuple in a relation. A table may have several candidate keys. The primary key is one chosen candidate key designated as the main identifier; it implies NOT NULL and UNIQUE. Other candidate keys can be expressed as UNIQUE NOT NULL constraints." },
  ]}
/>

<Recap lang="en">
The relational model defines data as sets of typed tuples sharing a fixed shape (a relation). Tables, rows, and columns are the SQL implementations of relations, tuples, and attributes. Every row is identified by a candidate key — one is the primary key. Foreign keys link tables and let the engine refuse orphan rows. The five constraint kinds (PK, FK, NOT NULL, UNIQUE, CHECK) are the declarative guarantees that move correctness from every application to the database boundary. The cost is schema-design discipline before code; the payoff is correctness every caller gets for free. Now when you see a "user has many X" requirement, you will reach for a join table with two foreign keys before you reach for a JSON column.
</Recap>
