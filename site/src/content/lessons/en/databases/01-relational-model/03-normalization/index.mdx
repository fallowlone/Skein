---
concepts:
- 1Nf
- 2NF
- 3NF
- BCNF
- normalization
- denormalization
- stored-aggregate
- wide-read-model
- materialised-path
- ORM-N+1
- schema-convention
deepensInto:
- 04-jsonb-and-arrays
estMin: 16
lang: en
lessonType: topic
level: middle
order: 3
prereqs:
- 02-constraints-and-keys
slug: 03-normalization
sources:
- https://www.postgresql.org/docs/current/ddl-constraints.html
- https://en.wikipedia.org/wiki/Boyce%E2%80%93Codd_normal_form
- https://www.postgresql.org/docs/current/datatype.html
spiral:
- statefulness
status: ready
summary: 1NF through BCNF in plain terms, when to deliberately denormalize, the ORM
  traps that quietly break schema discipline, and naming conventions that pay back
  across years.
title: Normal forms, denormalization, and why schemas stick
track: databases
unit: 01-relational-model
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import normalizeVsDenormalize from "./infographics/normalize-vs-denormalize.svg?raw";

<Hook>
A junior engineer stores the customer's city on every order row — "it's convenient for reports." Two years later, the customer moves cities. Now half the historical orders show the old city and half show the new one. The same fact lives in two places and they disagree. Normalization is the discipline that prevents this.
</Hook>

<Crux>Normalization removes duplicate facts so rows cannot disagree. Denormalize only when you control the reconciliation.</Crux>

<Explanation>

## Normal forms in plain terms

Normalization is a series of progressively stricter rules for splitting tables so no fact is stored twice. By the end of this section you will be able to spot a violation on sight and know exactly which table the offending column belongs in.

**1NF — atomic cells.** Every cell must hold a single value. No comma-separated lists inside a column, no repeating groups, no arrays disguised as column names (`tag1`, `tag2`, `tag3`). If you can parse a cell with `split(',')`, the table violates 1NF.

**2NF — no partial key dependency.** Every non-key column must depend on the *whole* primary key, not part of it. Only matters for composite primary keys. Example violation: a table `order_items(order_id, product_id, product_name)` — `product_name` depends on `product_id` alone, not on the composite key. Fix: move `product_name` to a products table.

**3NF — no transitive dependency.** Every non-key column must depend *only* on the primary key, not on another non-key column. Example violation: `orders(order_id, customer_id, customer_city)` — `customer_city` depends on `customer_id`, not on `order_id`. Fix: move `customer_city` to a customers (or addresses) table.

**BCNF — every determinant is a candidate key.** Stricter than 3NF; matters when a table has multiple overlapping candidate keys. Rare in production; common in academic examples. Violation example: two candidate keys share an attribute and one is not a superkey. In practice, target 3NF; go to BCNF only if you can state the specific overlapping-candidate-key scenario.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Normal form</th>
        <th class="border border-border px-3 py-2 text-left">Rule</th>
        <th class="border border-border px-3 py-2 text-left">Violation shape</th>
        <th class="border border-border px-3 py-2 text-left">Fix</th>
      </tr>
    </thead>
    <tbody>
      <tr><td class="border border-border px-3 py-2">1NF</td><td class="border border-border px-3 py-2">Atomic cells, no repeating groups</td><td class="border border-border px-3 py-2">Comma-separated list in one column</td><td class="border border-border px-3 py-2">Separate table or typed array</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">2NF</td><td class="border border-border px-3 py-2">Full key dependency</td><td class="border border-border px-3 py-2">Non-key col depends on part of composite PK</td><td class="border border-border px-3 py-2">Move to the owning table</td></tr>
      <tr><td class="border border-border px-3 py-2">3NF</td><td class="border border-border px-3 py-2">No transitive dependency</td><td class="border border-border px-3 py-2">Non-key col depends on another non-key col</td><td class="border border-border px-3 py-2">Extract to its own table</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">BCNF</td><td class="border border-border px-3 py-2">Every determinant is a candidate key</td><td class="border border-border px-3 py-2">Multiple overlapping candidate keys</td><td class="border border-border px-3 py-2">Decompose; rare in practice</td></tr>
    </tbody>
  </table>
</div>

Together, 1NF through BCNF form a progression: each form is strictly stronger than the one before. In practice, 3NF is the target — beyond that the decomposition cost outweighs the correctness gain. The point is not following rules — it is eliminating the failure mode where two rows can disagree about the same fact. If you can update Anna's email in three tables, two will be wrong within a year.

## Denormalization: when and why

Normalization optimises for write correctness. It can be expensive on the read side because answering a question requires joining many tables. Denormalization deliberately duplicates data to avoid joins on hot read paths.

**Stored aggregates.** An `order_total_cents` column on orders that caches `SUM(order_items.unit_price_cents * quantity)`. Refreshed by a trigger or application code on each line-item write. Reporting hits a single column instead of aggregating millions of rows.

**Wide read models.** A `user_profile` table that combines name, email, current address, latest order count — fed by triggers or application code from the normalised source tables. Dashboard queries hit one row.

**Materialised paths.** Storing the full ancestry path of a tree node as a text column (`/root/section/subsection/`) to avoid recursive joins when reading hierarchies. Classic pattern for comment trees or org charts.

The discipline: denormalize on the read side, keep the authoritative facts normalised, and accept that the duplicates must be reconciled by something — a trigger, a scheduled job, application code. The failure mode is denormalize-everywhere with no reconciliation plan. Every column eventually disagrees with every other.

<Infographic
  svg={normalizeVsDenormalize}
  label="Two-column comparison of computing a workspace's total revenue. Left: the normalized approach re-runs SUM over order_items on every dashboard load — no duplicate fact to reconcile and correct under ~1M rows, but a bottleneck above ~10M rows. Right: the denormalized stored-aggregate column makes the dashboard read one column at a ~1-5 microsecond write-time trigger cost, but the duplicate must be reconciled by that trigger."
  caption="The whole normalize-vs-denormalize choice in one frame: normalization moves the cost to read time and keeps facts un-duplicated; a stored aggregate moves the cost to write time (~1-5 µs/write) and trades a reconciliation obligation for an O(1) dashboard read once the live SUM crosses ~10M rows."
  hue="var(--d-infra)"
/>

<Inset kind="why" lang="en">
Why 3NF and not 4NF, 5NF, or DKNF? Normal forms above 3NF address multi-valued dependencies and join dependencies that appear in highly academic schemas. Real production tables almost never exhibit those shapes. 3NF is the point of diminishing returns: beyond it, you split tables that do not need splitting, add joins that do not add correctness, and slow reads for schemas that had no integrity problem to begin with.
</Inset>

## ORM traps that quietly break schema discipline

You might be writing perfect 3NF schemas by hand — but if your team uses an ORM, the generated DDL can silently undo that discipline. These three traps appear in nearly every ORM-managed codebase at some point.

ORMs abstract away SQL but can introduce schema-level mistakes.

**N+1 queries.** An ORM loads a list of 100 orders, then for each order fetches the customer separately. Result: 101 queries where 1 join would do. In many ORMs this is the default unless you declare an eager-load (`include`, `with`, `select_related`). The schema implication: every FK relation must be annotated with the correct default loading strategy; leaving it to implicit lazy-load accumulates invisible N+1s.

**Implicit schema generation.** Many ORMs can generate the schema from the model definition. The generated schema often lacks: CHECK constraints, composite indexes, index on the FK column, named constraints. The result is a syntactically valid schema that violates the discipline of this lesson — no constraints, no format enforcement, generic index strategy. Treat ORM-generated schemas as a starting point, not the finished artifact; add constraints and indexes manually or via migration files.

**Missing `updated_at` refresh.** ORMs frequently auto-manage `created_at` but not `updated_at`. A missing `updated_at` makes incremental replication and auditing impossible. Enforce via a trigger (`BEFORE UPDATE ... SET updated_at = now()`) or ORM lifecycle hook; never trust the application to remember.

## Naming conventions that pay back

These look pedantic; they pay back across every dashboard, every migration, every cross-team query.

- **Plural table names** (`users`, `orders`, `order_items`) — singular is also defensible; pick one and enforce it everywhere.
- **snake_case** — Postgres folds unquoted identifiers to lower case; snake_case is the production default.
- **`id` as the primary key name** on every table; foreign keys named `<referenced_table_singular>_id` (e.g., `user_id`, `order_id`).
- **`created_at`, `updated_at`, `deleted_at`** for temporal columns. `deleted_at IS NOT NULL` is the soft-delete pattern.
- **Boolean columns** prefixed `is_`, `has_`, `can_` or named as a clear property (`active`, not `flag`).
- **CHECK constraints naming** — name every constraint: `CONSTRAINT chk_orders_status CHECK (status IN (...))`. Unnamed constraints produce cryptic error messages on violation.

Mature platforms CI-lint these conventions so they are enforced across teams, not remembered by individuals.

<DragOrder
  id="03-normalization-drag-1"
  lessonSlug="03-normalization"
  lang="en"
  prompt="Order these schema evolution actions from cheapest to most expensive:"
  items={[
    { id: "a", label: "Add a CHECK constraint on an existing column" },
    { id: "b", label: "Add a UNIQUE index (CREATE INDEX CONCURRENTLY)" },
    { id: "c", label: "Add a FOREIGN KEY (requires validation pass over existing rows)" },
    { id: "d", label: "Change a column type (TEXT → INTEGER) — table rewrite" },
    { id: "e", label: "Split a JSONB column into typed columns — backfill migration" },
    { id: "f", label: "Split one table into two with FK — dual-write window, parity validation" },
    { id: "g", label: "Change a primary key — cascades through every FK and index; almost never done" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g"]}
/>

<Quiz
  id="03-normalization-quiz-1"
  lessonSlug="03-normalization"
  lang="en"
  question="A table stores `orders(order_id, customer_id, customer_city)`. Which normal form does it violate and why?"
  choices={[
    { label: "1NF — customer_city contains multiple values", misconception: "A city name is a single atomic value. 1NF is about multi-valued cells." },
    { label: "3NF — customer_city transitively depends on customer_id, not on order_id", correct: true },
    { label: "2NF — customer_city partially depends on the composite key", misconception: "order_id alone is the PK here (not composite), so 2NF partial-dependency does not apply." },
    { label: "BCNF — there are two overlapping candidate keys", misconception: "BCNF requires multiple overlapping candidate keys; this table has one." },
  ]}
/>

<TradeoffMatrix
  id="03-normalization-tradeoff-1"
  lessonSlug="03-normalization"
  lang="en"
  prompt="A reporting dashboard needs the total revenue per workspace, recalculated every 5 minutes. Pick the implementation."
  options={[
    { name: "Stored aggregate: a workspace_revenue_cents column on workspaces, updated by a BEFORE INSERT/UPDATE/DELETE trigger on order_items", summary: "Denormalized but reconcilable.", correct: true, justification: "The trigger keeps the aggregate consistent at write time. The dashboard reads a single column per workspace. Write overhead is one trigger per order-item write (~1-5 μs). Correct when the dashboard query cost exceeds the trigger overhead — measurably true at tens of millions of rows." },
    { name: "Re-aggregate from order_items on every dashboard load: SELECT SUM(...) FROM order_items JOIN orders WHERE workspace_id = X", summary: "Pure normalised approach.", justification: "Correct at small scale (under ~1M order_items). Becomes the bottleneck above ~10M rows because every dashboard request aggregates the full history. Profile first; move to stored aggregate only when measured." },
    { name: "JSONB column on workspaces storing {revenue: 12345, last_updated: '...'}", summary: "Wrong tool.", justification: "JSONB buys nothing here: the revenue is a single typed value. JSONB adds query overhead and loses type enforcement. Use INTEGER or BIGINT." },
    { name: "A separate reporting database synced by a nightly ETL job", summary: "Overkill for 5-minute freshness.", justification: "A separate reporting DB is correct at very large scale (billions of rows, BI tools). For 5-minute freshness on a single-tenant workspace stat, a stored aggregate is cheaper to operate and simpler to keep consistent." },
  ]}
/>

<Quiz
  id="03-normalization-quiz-2"
  lessonSlug="03-normalization"
  lang="en"
  question="An ORM-generated schema has no named CHECK constraints and no index on FK columns. What are the two concrete failure modes this causes in production?"
  choices={[
    { label: "Cryptic error messages on constraint violations; and FK-driven queries (list this user's orders) scan the full table instead of using an index", correct: true },
    { label: "The database refuses the schema migration", misconception: "Missing names and indexes are not syntax errors — Postgres accepts the schema. The failure is operational, not structural." },
    { label: "ORMs cannot write to unnamed constraints", misconception: "ORMs write via INSERT/UPDATE without referencing constraint names — naming only affects error messages and human-readability of EXPLAIN output." },
    { label: "Foreign keys do not enforce referential integrity without an index on the FK column", misconception: "FK enforcement does not require an index. The index is for query performance — without it, FK-driven SELECT queries do table scans." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="A 3NF fix: the orders table holding customer_city is split so customer_city moves to a customers table, and the slimmed orders table now references customers through a foreign key arrow pointing from orders to customers."
  hue="var(--d-data)"
  perRow={3}
  nodes={[
    { id: "before", label: "orders (violates 3NF)", sub: "order_id, customer_id, customer_city" },
    { id: "orders", label: "orders (3NF)", sub: "order_id, customer_id FK" },
    { id: "customers", label: "customers", sub: "customer_id PK, customer_city" },
  ]}
  edges={[
    { from: "before", to: "orders", label: "extract" },
    { from: "before", to: "customers", label: "extract" },
    { from: "orders", to: "customers", label: "customer_id FK" },
  ]}
  caption="customer_city depended on customer_id, not on order_id — a transitive dependency. Moving it to customers stores the fact once, so two order rows can never disagree about a customer's city."
/>

<KeyTakeaway>Target 3NF: no fact lives in two places, so two rows cannot disagree. Denormalize deliberately (stored aggregates, wide read models) with explicit reconciliation. ORM-generated schemas miss constraints and indexes; add them. Naming conventions are enforced by CI, not memory.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-normalization-retrieval"
  lang="en"
  questions={[
    { q: "Explain in your own words why 3NF is the production-default normal form, and when teams legitimately go above (BCNF) or below (deliberate denormalization).", a: "3NF eliminates the most common schema-integrity failure — storing the same fact in two places where they can disagree — without requiring the painful many-tables decomposition of higher forms. BCNF only matters when a table has multiple overlapping candidate keys, which is rare in production. Deliberate denormalization below 3NF is appropriate when (a) a read path is hot enough that joining is measured as the bottleneck, (b) the duplicated value is a computed aggregate that can be reconciled by a trigger, or (c) an audit column captures a value as it was at write time (unit price on an order item should not update when the product price changes). Normalise first, denormalize specific reads with explicit reconciliation." },
    { q: "Name three concrete ORM traps that quietly violate schema discipline and the fix for each.", a: "N+1 queries: ORM lazy-loads each FK relation separately; fix is explicit eager-load declarations per relation. Implicit schema generation: ORM-generated DDL lacks CHECK constraints, named constraints, and FK-column indexes; fix is to add them in migration files or a post-generate edit step. Missing updated_at refresh: ORM auto-manages created_at but not updated_at; fix is a BEFORE UPDATE trigger or lifecycle hook that sets updated_at = now() on every write." },
    { q: "What is the materialised-path pattern and when does it beat recursive CTEs for tree queries?", a: "Materialised path stores the full ancestor chain of a tree node as a text column (e.g., '/1/4/12/'). A subtree query becomes WHERE path LIKE '/1/4/%' — a simple prefix scan, indexable with a text B-tree or text_pattern_ops index. It beats a recursive CTE when (a) the tree is read far more often than written, (b) the tree is deep (recursive CTEs have a per-level cost), or (c) the tree is large and the CTE would scan millions of rows to build the ancestor list. The trade: inserting a node requires writing the full path; moving a subtree requires updating every descendant's path — expensive for large moves." },
  ]}
/>

<Recap lang="en">
Normal forms eliminate redundant facts: 1NF (atomic cells), 2NF (full-key dependency), 3NF (no transitive dependency), BCNF (every determinant is a candidate key). Target 3NF for production schemas. Denormalize deliberately — stored aggregates, wide read models, materialised paths — with explicit reconciliation; never denormalize without a plan for keeping the copies consistent. ORM-generated schemas skip constraints and indexes; add them. Naming conventions (snake_case, plural tables, id PK, created_at/updated_at/deleted_at, named CHECK constraints) are enforced by CI. Schema decisions are sticky; the cost of the wrong normal form is paid at migration time, not design time. Now when you see a column that stores the same fact as another table, ask which table is the authority — and put the fact there exactly once.
</Recap>
