infographic chart-column-simple
data
  title Tenant join latency: co-located vs cross-shard
  desc Same query — co-located stays single-node; break co-location and it fans out
  values
    - label Co-located join (single worker, ms)
      value 5
    - label Cross-shard fan-out (all workers, ms)
      value 500
theme
  palette #14b8a6 #f43f5e
