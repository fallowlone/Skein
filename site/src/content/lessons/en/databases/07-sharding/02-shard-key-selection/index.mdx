---
concepts:
- shard-key
- hash-sharding
- range-sharding
- directory-sharding
- consistent-hashing
deepensInto: []
estMin: 12
lang: en
lessonType: topic
level: middle
order: 2
prereqs:
- databases/07-sharding/01-why-sharding-exists
slug: 02-shard-key-selection
sources:
- https://docs.citusdata.com/en/stable/get_started/concepts.html
- https://www.citusdata.com/blog/2023/08/04/understanding-partitioning-and-sharding-in-postgres-and-citus/
- https://learn.microsoft.com/en-us/azure/cosmos-db/postgresql/concepts-sharding-models
spiral:
- statefulness
- latency
status: ready
summary: A good shard key has four properties — selectivity, uniform distribution,
  stability, and availability at routing time. Hash, range, list, and directory strategies
  each trade different failure modes.
title: 'Shard-key selection: hash, range, list, and directory strategies'
track: databases
unit: 07-sharding
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import strategyTradeoffs from "./infographics/strategy-tradeoffs.svg?raw";

<Hook>
Two teams both shard their Postgres by `customer_id`. One uses hash sharding; six months later they have no hot shards. The other uses range sharding with the same key; they have a permanent hot tail on the shard holding the highest-ID customers because new signups all go there. Same column, different strategy, opposite outcome.
</Hook>

<Crux>The shard key governs load distribution, query co-location, and resharding cost — all four properties must hold at once.</Crux>

<Explanation>

## The four properties of a good shard key

If you get the shard key wrong, no amount of hardware or tuning can save you — the damage is baked into the schema for years. What does "right" look like?

A shard key must satisfy all four of these simultaneously:

1. **Selectivity**: 95%+ of your queries filter by it. Queries without the shard key must fan out to every shard — expensive and slow.
2. **Uniform distribution**: each shard receives roughly equal data and traffic. Uneven keys produce hot shards.
3. **Stable**: the value never changes for a given row. Changing a shard key would require moving the row to a different shard — a problem you cannot solve at update time.
4. **Available at routing time**: every API request that touches this data must carry the shard key. If requests arrive without it, you cannot route them without a cross-shard scan.

For multi-tenant B2B SaaS, `tenant_id` almost always satisfies all four. For B2C, `user_id`. For geo-partitioned systems, `region`. Designs that do not have a column meeting all four properties are red flags — split the data model until one emerges. Together these four properties form a filter: when you see a candidate column pass all four, you have found your key; if any one fails, keep looking.

## The four strategies

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Strategy</th>
        <th class="border border-border px-3 py-2 text-left">How it works</th>
        <th class="border border-border px-3 py-2 text-left">Win</th>
        <th class="border border-border px-3 py-2 text-left">Risk</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2 font-mono">Hash</td>
        <td class="border border-border px-3 py-2">shard = hash(key) mod N</td>
        <td class="border border-border px-3 py-2">Uniform on uniform keys</td>
        <td class="border border-border px-3 py-2">Hot shard if keys are skewed; changing N moves most rows</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2 font-mono">Range</td>
        <td class="border border-border px-3 py-2">Each shard owns a key range (e.g., tenant_id 1-1000)</td>
        <td class="border border-border px-3 py-2">Natural for time-series; easy to add a shard at the tail</td>
        <td class="border border-border px-3 py-2">Hot tail: newest records all on one shard</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2 font-mono">List</td>
        <td class="border border-border px-3 py-2">Explicit map: key → shard</td>
        <td class="border border-border px-3 py-2">Maximum control</td>
        <td class="border border-border px-3 py-2">Does not scale to millions of keys</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2 font-mono">Directory</td>
        <td class="border border-border px-3 py-2">External lookup table maps key → shard; move tenants by updating one row</td>
        <td class="border border-border px-3 py-2">Move individual tenants between shards; Citus tenant isolation uses this</td>
        <td class="border border-border px-3 py-2">Lookup cost on every request; the shard map is critical infrastructure</td>
      </tr>
    </tbody>
  </table>
</div>

**Hash sharding** is the default for most OLTP systems because it distributes evenly when keys are uniformly distributed. The problem is that real-world tenants are power-law distributed: one customer is 100–1000× the median. Hash still routes that customer to a single shard — a hot shard.

**Range sharding** is natural for time-series data (shard by month) but creates a hot tail by default: all new writes go to the latest shard while older shards are idle. Production time-series systems typically combine range (by time bucket) with hash (within the bucket) to spread write load.

**Directory-based sharding** is the most flexible: a lookup table maps `tenant_id → connection_string`. Moving a tenant means updating one row. This is the foundation of Citus's tenant isolation primitive and of app-level routing in multi-tenant SaaS. The cost is that the lookup table becomes critical-tier-1 infrastructure — it must be highly available, low-latency, and cached aggressively.

**Consistent hashing** is a hash variant used mostly in caches (Memcached, Redis Cluster) where adding or removing a shard moves only K/N keys instead of most of them. Some Postgres sharding setups use directory-based consistent hashing for online shard rebalancing.

Each strategy trades one failure mode for another. Hash buys even distribution but loses range friendliness; range buys time-series convenience but creates a hot tail; directory buys per-tenant flexibility but makes the lookup table tier-1 infrastructure. When you see a time-series write pattern with an auto-increment key, that is the "hot tail" warning sign — reach for a composite approach.

<Infographic
  svg={strategyTradeoffs}
  label="Taxonomy tree of the four sharding strategies — hash, range, list, and directory — each branching into the win it buys and the failure mode it accepts."
  caption="There is no free strategy: every win buys exactly one failure mode. Match the strategy to the failure you can tolerate, not the win you want."
  hue="var(--d-infra)"
/>

## The shard-key contract in practice

The key must be in the primary key of every sharded table. If `orders` is sharded by `tenant_id`, then `(tenant_id, order_id)` is the primary key — not just `order_id`. This guarantees uniqueness across shards and forces every query path to carry `tenant_id`.

<Inset kind="why" lang="en">
Why is changing the shard key so expensive? The shard key determines where each row physically lives. Changing it means moving every row to its new shard — which involves reading the old shard, writing to the new shard, deleting from the old shard, and doing this for potentially billions of rows while production traffic continues. Teams have spent months executing shard-key changes. The lesson: choose carefully from the start and validate the key against production access patterns and tenant size distribution before committing.
</Inset>

<Quiz
  id="02-shard-key-selection-quiz-1"
  lessonSlug="02-shard-key-selection"
  lang="en"
  question="A SaaS has 5000 tenants where the top 5 tenants generate 60% of traffic. What is the best sharding approach?"
  choices={[
    { label: "Hash by tenant_id plus dedicated shards for the top 5 largest tenants — via Citus tenant isolation or app-level directory routing", correct: true },
    { label: "Pure hash sharding by tenant_id with no exceptions", misconception: "Hash distributes evenly only on uniform keys. With 5 dominant tenants generating 60% of traffic, those tenants each land on a single shard that will saturate while others sit idle." },
    { label: "Range sharding by tenant_id (tenant 1–1000 on shard A, etc.)", misconception: "Range sharding gives control but does not solve the skew problem — the large tenants each land in their range's shard which becomes a hot shard." },
    { label: "Shard by created_at timestamp instead of tenant_id", misconception: "Range on time creates a hot tail — all new writes go to the latest shard. And most queries carry tenant_id, not a time range, so the shard key fails the selectivity test." },
  ]}
/>

<Quiz
  id="02-shard-key-selection-quiz-2"
  lessonSlug="02-shard-key-selection"
  lang="en"
  question="A query searches users by email globally (no tenant_id available). What happens on a tenant_id-sharded cluster?"
  choices={[
    { label: "Fan-out: the query must go to every shard; P99 latency equals the slowest shard's response; this path should be rare or offloaded outside the sharded cluster", correct: true },
    { label: "Postgres rejects the query because the shard key is missing", misconception: "The query runs; it is simply expensive. Postgres will execute it on every shard and the coordinator merges results — it does not refuse." },
    { label: "The query goes to shard 0 by default when no key is present", misconception: "There is no default shard. Without a shard key, the coordinator must consult every shard to produce a complete result." },
    { label: "The query is fast if an index on email exists on every shard", misconception: "The index helps per-shard execution speed but does not eliminate the fan-out. You still pay N× the network overhead and P99 = max(shard latencies)." },
  ]}
/>

<DragOrder
  id="02-shard-key-selection-drag-1"
  lessonSlug="02-shard-key-selection"
  lang="en"
  prompt="Order the shard-key evaluation steps before committing to a key for a multi-tenant B2B SaaS:"
  items={[
    { id: "a", label: "Audit query patterns: list the WHERE columns across the top-100 queries by frequency" },
    { id: "b", label: "Validate the key is in 95%+ of queries and available in every API request" },
    { id: "c", label: "Measure tenant size distribution on production data — find the top 5% by traffic and data volume" },
    { id: "d", label: "Confirm the key is stable (never updated) and non-null on every sharded table" },
    { id: "e", label: "Design outlier policy: tenants above X% cluster traffic get dedicated shards" },
    { id: "f", label: "Commit the key to an ADR and document the resharding cost if you later need to change it" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f"]}
/>

</Explanation>

<FlowDiagram
  label="Hash sharding computes hash of the shard key modulo N; on a uniform key this maps tenants evenly across all four shards, with no shard favoured."
  perRow={4}
  hue="var(--d-data)"
  nodes={[
    { id: "k", label: "tenant_id", sub: "shard key" },
    { id: "h", label: "hash(key) mod 4", sub: "uniform spread" },
    { id: "s0", label: "Shard 0" },
    { id: "s1", label: "Shard 1" },
    { id: "s2", label: "Shard 2" },
    { id: "s3", label: "Shard 3" },
  ]}
  edges={[
    { from: "k", to: "h" },
    { from: "h", to: "s0", label: "=0" },
    { from: "h", to: "s1", label: "=1" },
    { from: "h", to: "s2", label: "=2" },
    { from: "h", to: "s3", label: "=3" },
  ]}
  caption="A good key is selective, stable, and available at routing time — and uniform, so hash(key) mod N lands keys evenly. A skewed key sends one dominant tenant to a single shard: a hot shard."
/>

<KeyTakeaway>A shard key must be selective (in 95%+ of queries), uniformly distributed, stable, and available at routing time. Hash is the default; directory gives flexibility for outlier isolation. Changing a key later costs months — validate against production access patterns before committing.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-shard-key-selection-retrieval"
  lang="en"
  questions={[
    { q: "Name the four required properties of a good shard key and explain why each matters.", a: "(1) Selectivity: 95%+ of queries filter by it — without it, queries fan out to all shards and are expensive. (2) Uniform distribution: each shard gets roughly equal data and traffic — non-uniform keys produce hot shards. (3) Stable: the value never changes per row — changing it would require moving the row to a different shard mid-write. (4) Available at routing time: every API request carries it — without it you cannot route without a cross-shard scan." },
    { q: "What is the main failure mode of range sharding on a time-series key, and how do production systems mitigate it?", a: "Range sharding on time creates a hot tail: all new writes go to the latest time range's shard while older shards sit idle. Mitigation: combine range (time bucket) with hash (by user_id or tenant_id within the bucket) so writes spread across shards within each time window. Alternatively, use hash sharding by a non-time key and rely on partition pruning within each shard for time-range queries." },
    { q: "Why does the shard key need to be part of the primary key on every sharded table?", a: "The shard key determines which physical instance a row lives on. If the primary key is just (order_id), uniqueness is per-shard — two different shards could both have order_id=42. Making the primary key (tenant_id, order_id) guarantees global uniqueness across shards and forces every query path to carry tenant_id so the router can direct it to the right shard without a scan." },
  ]}
/>

<Recap lang="en">
A shard key must satisfy four properties at once: selectivity (in 95%+ of queries), uniform distribution (no one shard gets overloaded), stability (never updated), and availability at routing time. Hash sharding distributes evenly on uniform keys but creates hot shards when tenants are skewed; range sharding is natural for time-series but creates a hot tail; directory-based sharding is the most flexible and is used by Citus's tenant isolation primitive. Validate the key against real production access patterns and tenant size distribution before committing — changing it later is a months-long project of moving every row. Now when you see a new sharding proposal, run it through all four properties before signing off: a key that fails even one of them will betray you at scale.
</Recap>
