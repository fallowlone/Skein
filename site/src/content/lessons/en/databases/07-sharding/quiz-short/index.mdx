---
concepts:
- shard-key
- partitioning-vs-sharding
- colocation
- hot-shard
- cross-shard-transaction
- resharding
estMin: 14
lang: en
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://docs.citusdata.com/en/stable/get_started/concepts.html
- https://www.postgresql.org/docs/current/ddl-partitioning.html
- https://www.crunchydata.com/blog/designing-your-postgres-database-for-multi-tenancy
status: ready
summary: "Free-recall prompts across the sharding unit. Answer each in your own words first, then reveal the model answer and compare."
title: 'Sharding: free-recall review'
track: databases
unit: 07-sharding
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory before you open the model answer — the effort of recall is what makes the mechanism stick when you face it in a design review.
</Hook>

<Goal lang="en">
Reconstruct the unit's spine — the four shard-key properties, partitioning vs sharding, co-location, the hot-shard playbook, cross-shard transactions, and online resharding — without looking back at the lessons.
</Goal>

<RetrievalDrawer
  client:visible
  id="07-sharding-short-retrieval"
  lang="en"
  questions={[
    { q: "Name the four properties a shard key must satisfy at once, and why each matters.", a: "(1) Selectivity: 95%+ of queries filter by it — queries lacking it fan out to every shard and pay P99 = slowest shard. (2) Uniform distribution: each shard gets roughly equal data and traffic — skewed keys produce hot shards. (3) Stable: the value never changes for a row — changing it would force moving the row to a different shard mid-write, a problem you cannot solve at update time. (4) Available at routing time: every API request carries it — without it you cannot route without a cross-shard scan. For B2B SaaS tenant_id usually satisfies all four; if no column does, split the data model until one emerges. The key must also be in the primary key of every sharded table (e.g. (tenant_id, order_id)) so uniqueness holds across shards." },
    { q: "What is the difference between partitioning and sharding, and how do production systems compose them?", a: "Partitioning splits one table into sub-tables on a single Postgres instance (PG 10+ declarative PARTITION BY). It buys two things: partition pruning (a filter on the partition key scans only relevant partitions) and instant retention DROP (DROP TABLE on an old partition is milliseconds, no DELETE bloat or huge VACUUM). It does not add throughput — all partitions share one primary's WAL, CPU, and IO. Sharding distributes rows across N separate Postgres instances by a shard key, giving ~N× throughput and storage at N× operational cost. Production composes both: shard by tenant_id across instances for capacity, then partition by created_at within each shard for pruning and retention. The heuristic: partition for manageability, shard for capacity — and shard only once single-Postgres limits are measured." },
    { q: "Explain co-location in Citus and what query performance does when it is violated.", a: "Co-location means tables distributed by the same key have their corresponding shards on the same physical worker — shard 0 of orders and shard 0 of payments both on Worker 0, etc. A join across co-located tables filtered by the shard key (tenant_id = 42) runs entirely on one worker, like a normal single-Postgres join with full index use, single-digit milliseconds. Violate it — distribute payments by payment_id instead of tenant_id — and the coordinator must gather partial joins from every worker and merge: every worker participates, P99 equals the slowest worker, latency rises 10–100×. Citus also has reference tables (full copy on every worker, joined locally, written via slow 2PC) for small mostly-read lookups. Co-location is the design contract that makes a sharded cluster behave like one Postgres for the 99% tenant-scoped case." },
    { q: "Why does hash sharding still produce hot shards, and what is the production playbook to detect and fix them?", a: "Hash sharding (shard = hash(tenant_id) mod N) distributes evenly only when keys are uniform. Real tenants are power-law distributed: one or two accounts generate 60–80% of traffic, and hash routes each to exactly one shard — so the dominant tenant saturates its shard while others idle. It is structural, not a bug. Detect with leading indicators: shard size skew (max/median > 1.5× from citus_shards) and per-tenant query load (pg_stat_statements aggregated by tenant_id, alert above 5% of cluster total_exec_time) — both visible weeks before the lagging CPU alert. Immediate fix: SELECT isolate_tenant_to_new_shard('orders', tenant_id, 'CASCADE') copies only that tenant's rows to a new shard via logical replication with a sub-second write pause. Durable fix: a tiered automation policy — auto-isolate tenants above 5% cluster load, escalate >20% to a dedicated worker, >50% to a dedicated cluster, and pre-isolate large enterprise accounts at onboarding rather than waiting for a spike." },
    { q: "When does a transaction require two-phase commit on a sharded cluster, what does 2PC cost, and what is its dangerous failure mode?", a: "A transaction touching rows on more than one shard requires 2PC. Co-located tenant-scoped updates (orders + inventory both keyed by tenant_id = 42) stay single-shard and use a normal Postgres transaction; a transaction also touching a differently-keyed table (e.g. a global ledger by account_id) spans shards and Citus runs PREPARE TRANSACTION then COMMIT PREPARED. Cost: at least two round-trips (prepare + commit) so ~2× latency, plus held connections during the prepare phase that contend under concurrency. Dangerous failure mode: in-doubt transactions — if the coordinator crashes between PREPARE and COMMIT PREPARED, prepared transactions sit in pg_prepared_xacts holding locks (and can block VACUUM) until manually resolved. Monitor pg_prepared_xacts on every worker, alert on rows older than 5 minutes, keep a COMMIT/ROLLBACK PREPARED runbook — and prefer co-location or eventual consistency (outbox/saga) so most transactions never need 2PC." },
    { q: "How does Citus online resharding work, why is the per-shard write pause sub-second, and why is changing the shard key still a months-long project?", a: "Citus 11.1+ moves a shard by creating an empty shard on the target worker, starting a logical replication slot on the source filtered to that shard's rows, copying existing rows then streaming changes until lag is near zero, briefly pausing writes (sub-second), atomically switching the shard-map metadata to the target, then resuming and dropping the source. The pause is sub-second because the bulk of the data moved during streaming replication — only the last in-flight changes drain during the pause. It is idempotent and resumable from pg_stat_subscription if the coordinator crashes, and uses logical (not physical) replication so it moves exactly the shard's rows, not every tenant's pages. Changing the shard key is different and far worse: every row must be rewritten to a new location, via dual-write + backfill + cut-over or a parallel cluster fed by CDC — months of work. That is why teams often endure a wrong key rather than pay the migration, and why the choice is lived with for years." },
  ]}
/>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the unit's spine: a good shard key is selective, uniform, stable, and present at routing time; partitioning prunes and manages retention on one machine while sharding adds throughput across many; co-location keeps the 99% tenant-scoped case single-node; hot shards are an expected power-law consequence answered by tenant isolation and a tiered policy; cross-shard transactions drag in 2PC with its in-doubt risk, so design single-shard; and online resharding is cheap (sub-second pauses, logical replication) while a shard-key change is the expensive, lived-with-for-years decision.
</Recap>
