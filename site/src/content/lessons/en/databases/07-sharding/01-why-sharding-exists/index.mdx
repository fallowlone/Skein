---
concepts:
- sharding
- shard-key
- hot-shard
- single-postgres-ceiling
deepensInto: []
estMin: 8
lang: en
lessonType: topic
level: junior
order: 1
prereqs:
- databases/06-migrations/01-what-a-migration-is
slug: 01-why-sharding-exists
sources:
- https://www.postgresql.org/docs/current/ddl-partitioning.html
- https://docs.citusdata.com/en/stable/get_started/concepts.html
- https://wiki.postgresql.org/wiki/Scaleout_Design
spiral:
- statefulness
status: ready
summary: Every Postgres instance has hard CPU, storage, and throughput ceilings; sharding
  is the only path past them — but it multiplies every operational task by the shard
  count.
title: 'Why sharding exists: the single-Postgres ceiling'
track: databases
unit: 07-sharding
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";

<Hook>
Otto's Postgres is running at 95% CPU, the working set has grown past what fits in RAM, and writes are saturating the single primary. Read replicas help for reads, but the write bottleneck is the machine itself. There is no bigger instance left to buy.
</Hook>

<Crux>Sharding splits one dataset across many Postgres instances by a shard key — the only path past a single machine's ceiling.</Crux>

<Explanation>

## The single-Postgres ceiling

A single Postgres instance has hard limits:

- **Working set vs RAM**: once the hot portion of your data no longer fits in `shared_buffers` plus OS page cache, every query starts hitting disk. Cache-hit ratio (`blks_hit / (blks_hit + blks_read)`) dropping below 95–99% is the leading signal.
- **Write throughput**: a single primary can sustain roughly 10–50k transactions per second depending on hardware. Above that, WAL generation dominates CPU and the primary becomes the bottleneck.
- **Storage**: practical ceiling around 1–10 TB of working set in RAM on the largest available machines.

Vertical scaling (more RAM, faster NVMe) buys time — often years. The right order is: fix slow queries first, add indexes, add read replicas, vertically scale. Sharding is the last resort, not the first.

## What sharding does

Sharding takes one logical dataset (all your `orders`, all your `users`) and splits it across N physical Postgres instances. Each row lives on exactly one shard, determined by a **shard key** — a column or combination of columns (typically `tenant_id` for B2B SaaS, `user_id` for B2C).

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Single Postgres</th>
        <th class="border border-border px-3 py-2 text-left">Sharded (4 instances)</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">All rows on one machine</td>
        <td class="border border-border px-3 py-2">Rows split by shard key across 4 machines</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">One CPU/IO ceiling</td>
        <td class="border border-border px-3 py-2">~4× the CPU/IO capacity</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Simple operations (1 backup, 1 migration)</td>
        <td class="border border-border px-3 py-2">4× operations (4 backups, migration runs 4×)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Cross-table joins: free</td>
        <td class="border border-border px-3 py-2">Cross-shard joins: expensive</td>
      </tr>
    </tbody>
  </table>
</div>

The win is linear-ish scale-out: 10 shards give roughly 5–8× the throughput (overhead from coordination reduces the theoretical 10×). The cost is that every operational task — backups, migrations, upgrades, VACUUM — must run on every shard.

## The hot-shard failure mode

The most important failure mode to design against from day one is the **hot shard**: one shard receives far more traffic than the others because the shard key was poorly chosen, or because one tenant grew 1000× larger than the median.

**Example**: a SaaS shards `orders` by `customer_id`. An enterprise customer signs and their orders are 5× all other customers combined. Their shard hits 95% CPU while the other shards sit at 10%. The cluster effectively runs at single-shard capacity for the dominant customer.

The fix — moving that tenant to a dedicated shard — is covered in lesson 05. The lesson here is: shard-key selection is the single most consequential design decision in a sharded system, and bad choices are lived with for years.

## The library metaphor

Sharding is the difference between one library with a million books (one building, one lookup desk, find anything by walking around) and ten libraries with 100k books each (ten buildings, you need a directory of "which library has this book"). The single library is simpler. The ten-library setup needs a directory (the **shard map**) and a rule for which building to go to before you set off.

Books that get checked out a million times a year fill one library while the others sit empty — that is the hot-shard failure.

<Inset kind="why" lang="en">
Why not just use a bigger cloud instance? Vertical scaling is almost always cheaper than sharding for the first few years. The decision point is when you have measured that the largest available instance is still insufficient at peak — usually 10 TB working set, 50k+ QPS sustained write load, or multi-year projections that cross those thresholds. Sharding is a one-way door: once your schema and app logic are written for a sharded world, reversing it is a multi-month project.
</Inset>

<Quiz
  id="01-why-sharding-exists-quiz-1"
  lessonSlug="01-why-sharding-exists"
  lang="en"
  question="What does a shard key determine?"
  choices={[
    { label: "Which physical Postgres instance a row lives on — the application hashes or ranges this column to route reads and writes to the correct shard", correct: true },
    { label: "The encryption key for data on that shard", misconception: "Encryption is unrelated to sharding. The shard key is a routing key, not a security key." },
    { label: "The primary key of every table", misconception: "The shard key is often part of the primary key but its specific role is routing to the right instance, not just uniquely identifying a row." },
    { label: "Postgres picks it automatically based on table size", misconception: "You choose the shard key explicitly. A wrong choice is the source of most sharding incidents." },
  ]}
/>

<Quiz
  id="01-why-sharding-exists-quiz-2"
  lessonSlug="01-why-sharding-exists"
  lang="en"
  question="A B2B SaaS shards 'orders' by customer_id. One enterprise customer generates 5× the traffic of all others combined. What is the result?"
  choices={[
    { label: "Hot shard — the customer's shard saturates CPU/IO while other shards sit idle; the cluster runs at effectively single-shard capacity for that customer's requests", correct: true },
    { label: "Postgres automatically rebalances rows across shards when one fills up", misconception: "Postgres does not rebalance automatically. Hash routing assigns rows to a fixed shard; the shard stays hot until you explicitly move the tenant." },
    { label: "All shards share load equally because they all participate in the query", misconception: "Only the shard owning the customer's rows handles their queries. Other shards are uninvolved and idle." },
    { label: "Performance is fine because indexes still work on the hot shard", misconception: "Indexes help query speed within a shard, but they cannot redistribute load away from an overloaded shard." },
  ]}
/>

<DragOrder
  id="01-why-sharding-exists-drag-1"
  lessonSlug="01-why-sharding-exists"
  lang="en"
  prompt="Order the scaling strategies from cheapest to most expensive (operational complexity), before reaching for sharding:"
  items={[
    { id: "a", label: "Fix slow queries: add missing indexes, rewrite inefficient SQL" },
    { id: "b", label: "Add read replicas to offload read-heavy traffic from the primary" },
    { id: "c", label: "Add caching layers (Redis, in-process) for hot read paths" },
    { id: "d", label: "Vertical scaling: upgrade to a larger instance (more RAM, faster NVMe)" },
    { id: "e", label: "Shard: split the dataset across multiple Postgres instances by a shard key" },
  ]}
  correctOrder={["a", "c", "b", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Sharding splits one Postgres across N instances by a shard key, giving roughly N× capacity — but multiplying every operation N× and making the hot-shard failure mode the primary design risk. Exhaust vertical scaling and replicas first.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-why-sharding-exists-retrieval"
  lang="en"
  questions={[
    { q: "What are the three hard ceilings of a single Postgres instance that sharding addresses?", a: "Working set vs RAM (cache-hit ratio drops below 95-99% when data no longer fits in shared_buffers + OS cache), write throughput (single primary saturates at ~10–50k QPS, WAL generation dominates CPU), and storage (practical ceiling ~1-10 TB working set in RAM on the largest available machines). Sharding spreads each of these across N instances." },
    { q: "In one sentence: what is the hot-shard failure mode and what causes it?", a: "A hot shard is when one shard receives a disproportionate share of traffic — caused by an uneven distribution of data or load across the shard key, most commonly because one tenant or key range grows far larger than the median." },
    { q: "Why is the operational cost of sharding described as 'N×'?", a: "Once sharded, every operation that ran once on a single Postgres must now run on every shard: backups (N pg_basebackups), schema migrations (N migration runs), VACUUM, minor and major version upgrades, logical replication slots for CDC, and per-shard monitoring. The operational headcount and automation budget scale proportionally with shard count if not addressed." },
  ]}
/>

<Recap lang="en">
A single Postgres instance has hard ceilings on working set (RAM), write throughput, and storage that vertical scaling can only delay, not eliminate. Sharding splits the dataset across N physical Postgres instances by a shard key, giving roughly N× the capacity at the cost of N× the operational complexity — every migration, backup, and upgrade runs once per shard. The central failure mode is the hot shard: one shard saturating because the key was poorly chosen or a single tenant outgrew the rest. Senior engineers exhaust indexing, caching, read replicas, and vertical scaling before sharding — and when they do shard, they pick the key deliberately and design the hot-shard response before the first incident.
</Recap>
