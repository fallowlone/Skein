---
concepts:
- security-groups
- network-acls
- stateful-vs-stateless
- ephemeral-ports
- defense-in-depth
- SAA-C03:Design Secure Architectures
deepensInto: []
estMin: 17
lang: en
lessonType: topic
level: middle
order: 2
prereqs: []
slug: "02-security-groups-and-nacls"
sources:
- https://docs.aws.amazon.com/vpc/latest/userguide/vpc-security-groups.html
- https://docs.aws.amazon.com/vpc/latest/userguide/vpc-network-acls.html
- https://docs.aws.amazon.com/vpc/latest/userguide/nacl-rules.html
spiral: []
status: ready
summary: 'AWS gives you two packet filters: security groups (instance-level, stateful, allow-only, SG-referencing) and network ACLs (subnet-level, stateless, ordered, deny-capable). Stateful vs stateless is the distinction that bites — open ephemeral return ports or watch responses vanish.'
title: 'Security groups and network ACLs: two filters, two state models'
track: "aws"
unit: "04-networking"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A team locks down a subnet with a custom network ACL: inbound rule 100 allows 443 from the internet, everything else denied. The web servers in that subnet stop responding. Not slowly — completely. curl from outside hangs and times out. The security group is wide open on 443, the route table is fine, the servers are healthy. Hours of packet captures later, someone notices the SYN arrives and the SYN-ACK leaves the instance but never reaches the client. The NACL allowed the inbound request on 443 — but it never allowed the outbound response, which leaves on an ephemeral port like 54321, not 443. A NACL is stateless: it does not remember the inbound connection, so the return packet is a brand-new flow that hit the implicit deny. One outbound rule for ports 1024-65535 fixed it. The bug wasn't a typo. It was forgetting that one of your two filters has no memory.
</Hook>

<Crux>Two filters: security groups (instance, stateful, allow-only) and NACLs (subnet, stateless, ordered). Stateful vs stateless is what bites.</Crux>

<Explanation>

## Two layers, two completely different state models

Why does a correctly-configured security group sometimes fail to deliver traffic? Because the second filter — the one at the subnet edge — follows completely different rules.

A packet headed for your EC2 instance crosses two filters, and they behave nothing alike. The **network ACL** guards the subnet edge — it inspects traffic entering and leaving the subnet. The **security group** guards the elastic network interface (ENI) attached to the instance itself. A packet from the internet hits the NACL first, then the SG; a response leaves through the SG first, then the NACL. Both must permit the traffic or it dies.

The defining difference is **state**. A security group is **stateful**: if it allows an inbound request, the response is automatically allowed back out regardless of outbound rules — and if it allows an outbound request (the default SG allows all egress), the response is allowed back in regardless of inbound rules. The SG remembers the connection. A network ACL is **stateless**: it has no memory of prior packets. If you allow inbound 443, you have separately allowed the *outbound* return traffic, and that return traffic does not leave on 443 — it leaves on an **ephemeral port**. The client opened the connection from a high random port, so the server's reply is destined for that port. AWS recommends opening **1024-65535** for ephemeral return traffic, because different clients pick from different ranges: a NAT gateway and many ELBs use 1024-65535, Linux kernels typically use 32768-60999, and Windows clients use 49152-65535. Open the union, or some clients silently break.

<FlowDiagram
  label="A request packet and its response crossing the subnet network ACL and the instance security group, showing the stateless return-traffic trap"
  hue="var(--d-systems)"
  nodes={[
    { id: "client", label: "Client", sub: "src port 54321, dst 443" },
    { id: "naclIn", label: "NACL inbound", sub: "rule allows 443, stateless" },
    { id: "sgIn", label: "SG inbound", sub: "allows 443, stateful, remembers flow" },
    { id: "app", label: "Instance / ENI", sub: "replies: src 443, dst 54321" },
    { id: "sgOut", label: "SG outbound", sub: "auto-allowed (stateful)" },
    { id: "naclOut", label: "NACL outbound", sub: "MUST allow 1024-65535 or dropped" },
  ]}
  edges={[
    { from: "client", to: "naclIn", label: "request" },
    { from: "naclIn", to: "sgIn", label: "subnet to ENI" },
    { from: "sgIn", to: "app", label: "deliver" },
    { from: "app", to: "sgOut", label: "response" },
    { from: "sgOut", to: "naclOut", label: "ENI to subnet" },
    { from: "naclOut", to: "client", label: "ephemeral port" },
  ]}
  caption="The SG remembers the flow, so the response sails back out. The NACL does not — the response is a new packet on an ephemeral port, and if the NACL has no outbound rule for 1024-65535 it is dropped at the subnet edge."
/>

## How rules are evaluated, and what each can express

The two filters even *decide* differently. A **security group is allow-only and evaluated as a whole**. You cannot write a deny rule in an SG — its only verb is "allow this." Every rule across every SG attached to the ENI is unioned: if *any* rule permits the packet, it passes. There is no order and no first-match; it is one big OR of allows, with an implicit deny for anything not matched. The default SG that ships with a VPC allows **no inbound** traffic and **all outbound** traffic, and as a convenience contains a self-referencing rule so members can talk to each other.

A **network ACL is an ordered list and supports explicit deny**. Each rule has a number from 1 to 32766; AWS evaluates **lowest number first, and the first match wins** — no further rules are checked. Because it can say *deny*, a NACL can do something an SG fundamentally cannot: block a specific bad IP or CIDR. Put a DENY for `198.51.100.0/24` at rule 90, above your ALLOW `0.0.0.0/0` at rule 100, and that range is shut out while everyone else gets in. Every NACL ends with an unremovable `*` rule that denies anything unmatched. The default NACL allows all inbound and outbound; a **custom** NACL starts by denying everything until you add rules.

```text
# Custom NACL — INBOUND rules (evaluated low to high, first match wins)
Rule   Type        Port        Source            Allow/Deny
90     HTTPS       443         198.51.100.0/24   DENY    # block one bad CIDR first
100    HTTPS       443         0.0.0.0/0         ALLOW   # everyone else may reach 443
130    Ephemeral   1024-65535  0.0.0.0/0         ALLOW   # return traffic for outbound conns
*      ALL         ALL         0.0.0.0/0         DENY    # implicit, cannot be removed

# Custom NACL — OUTBOUND rules (stateless: you MUST open the return path)
Rule   Type        Port        Destination       Allow/Deny
100    Ephemeral   1024-65535  0.0.0.0/0         ALLOW   # responses to inbound 443 leave here
110    HTTPS       443         0.0.0.0/0         ALLOW   # if the instance initiates outbound 443
*      ALL         ALL         0.0.0.0/0         DENY
```

When you find yourself hardcoding an IP range into a security group rule, ask whether you can reference a role instead — the answer usually yes. The idiomatic least-privilege move lives on the SG side: **reference another security group as the source** instead of a CIDR. Rather than "allow 5432 from 10.0.2.0/24" on your database SG, write "allow 5432 from the app-tier SG." Now any instance carrying the app-tier SG can reach Postgres and nothing else can — and the rule keeps working as the app tier scales and its IPs churn. You never hardcode an address; you grant access to a *role*.

```bash
# Least-privilege DB access: allow Postgres ONLY from instances in the app-tier SG
aws ec2 authorize-security-group-ingress \
  --group-id sg-db0000000000db \
  --protocol tcp --port 5432 \
  --source-group sg-app00000000app
```

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Property</th>
        <th class="border border-border px-3 py-2 text-left">Security group</th>
        <th class="border border-border px-3 py-2 text-left">Network ACL</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Operates at</td>
        <td class="border border-border px-3 py-2">Instance / ENI level</td>
        <td class="border border-border px-3 py-2">Subnet level</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">State</td>
        <td class="border border-border px-3 py-2">Stateful — return traffic auto-allowed</td>
        <td class="border border-border px-3 py-2">Stateless — must allow both directions + ephemeral ports</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Rule verbs</td>
        <td class="border border-border px-3 py-2">Allow only (no deny)</td>
        <td class="border border-border px-3 py-2">Allow and deny</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Evaluation</td>
        <td class="border border-border px-3 py-2">All rules unioned (any allow passes)</td>
        <td class="border border-border px-3 py-2">Numbered, lowest first, first match wins</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Can reference an SG as source</td>
        <td class="border border-border px-3 py-2">Yes — the least-privilege idiom</td>
        <td class="border border-border px-3 py-2">No — CIDR only</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Default (VPC default object)</td>
        <td class="border border-border px-3 py-2">No inbound, all outbound</td>
        <td class="border border-border px-3 py-2">Allow all in and out</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Default (custom object)</td>
        <td class="border border-border px-3 py-2">No inbound, all outbound</td>
        <td class="border border-border px-3 py-2">Deny all until you add rules</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="en">
Why give us two filters that overlap? Defense in depth with division of labor. The security group is your scalpel: per-resource least privilege, expressed against roles (other SGs) rather than addresses, and stateful so you never reason about return paths. The NACL is your blunt subnet-wide guardrail: it can deny — so it can blackhole a known-bad CIDR or enforce a coarse "these two tiers may never talk" rule that no single SG can guarantee, because an SG can only grant, never forbid. Most teams run permissive default NACLs and do all real work in SGs, reaching for a NACL only when they need a deny or a subnet-level blast-radius limit. Using a NACL where an SG belongs is how you end up debugging dropped ephemeral return traffic at 2am.
</Inset>

<TradeoffMatrix
  id="02-security-groups-and-nacls-tradeoff-1"
  lessonSlug="02-security-groups-and-nacls"
  lang="en"
  prompt="You must block one specific malicious /24 from reaching every instance in a subnet, no exceptions, regardless of any allow rule someone adds later. Which filter does the job?"
  options={[
    { name: "A deny rule in the subnet's network ACL, numbered below the allow rules", summary: "Stateless, subnet-wide, supports deny.", correct: true, justification: "Right tool. Only a network ACL can express deny, and it operates at the subnet edge so the block covers every instance at once and cannot be undone by an over-broad security group rule on any single instance. Number the DENY for the /24 lower than your ALLOW 0.0.0.0/0 and first-match-wins shuts the CIDR out before the allow is ever evaluated. This is exactly what NACLs are for: a coarse, deny-capable, subnet-wide guardrail." },
    { name: "Remove the offending CIDR from every security group", summary: "Tighten the SG allow rules.", justification: "Impossible by construction. Security groups are allow-only — there is no deny verb. You cannot 'subtract' a CIDR from an allow rule; the best you could do is narrow every allow rule on every SG to exclude that range, which is fragile, easy to miss on a new SG, and trivially re-opened by anyone who adds 0.0.0.0/0 later. The whole point of needing a guaranteed block is that SGs cannot provide one." },
    { name: "A self-referencing security group rule", summary: "Allow from the same SG.", justification: "Wrong mechanism for a block. SG self-referencing is a least-privilege ALLOW pattern (members talk to each other); it grants access, it does not deny it. It does nothing to stop an external malicious /24 and isn't a deny tool at all." },
    { name: "Detach the instances' security groups", summary: "Remove the SG entirely.", justification: "Catastrophic and still wrong. Removing SGs doesn't deny the bad CIDR, and you'd lose all your other access control in the process. You'd break legitimate traffic while still having no way to express the targeted deny, which only a NACL provides." },
  ]}
/>

<Quiz
  id="02-security-groups-and-nacls-quiz-1"
  lessonSlug="02-security-groups-and-nacls"
  lang="en"
  question="A subnet's custom network ACL allows inbound TCP 443 from 0.0.0.0/0, and the security group allows 443 too, but external clients still can't complete a request. What's the most likely cause?"
  choices={[
    { label: "The NACL has no outbound rule allowing the ephemeral return ports (1024-65535), so the response is dropped at the subnet edge — NACLs are stateless and don't auto-allow return traffic", correct: true },
    { label: "The security group is stateless and forgot the return rule", misconception: "Security groups are stateful, not stateless — they automatically allow return traffic for an allowed flow. The component that forgets return traffic is the network ACL, which is stateless and needs an explicit outbound rule for ephemeral ports." },
    { label: "The NACL needs a second inbound rule on 443 with a higher number", misconception: "Inbound 443 is already allowed; a duplicate higher-numbered rule changes nothing because first-match-wins already permitted the inbound request. The missing piece is the outbound ephemeral-port rule for the response." },
    { label: "Security groups can't allow port 443", misconception: "Security groups allow any port you specify, including 443. The problem isn't the SG inbound rule; it's the stateless NACL not permitting the outbound response on its ephemeral port." },
  ]}
/>

<Quiz
  id="02-security-groups-and-nacls-quiz-2"
  lessonSlug="02-security-groups-and-nacls"
  lang="en"
  question="On your database security group, which rule best expresses 'only the app tier may connect to Postgres' and keeps working as the app tier autoscales?"
  choices={[
    { label: "Allow inbound TCP 5432 with the app-tier security group as the source (source-group reference)", correct: true },
    { label: "Allow inbound TCP 5432 from the current app instances' IP addresses as a CIDR list", misconception: "Hardcoding IPs breaks the moment the app tier scales out or replaces instances and their addresses change. Referencing the app-tier SG as the source grants access to the role, not the addresses, so it survives autoscaling automatically." },
    { label: "Add a deny rule on the DB SG for everything except the app tier", misconception: "Security groups have no deny verb — you cannot write 'deny everything except.' You express least privilege by allowing only the specific source (the app-tier SG); everything not allowed is implicitly denied." },
    { label: "Open 5432 to 0.0.0.0/0 and rely on the NACL to filter", misconception: "Opening a database port to the whole internet is the classic exposure mistake; a permissive NACL won't save you, and you've abandoned per-resource least privilege. Reference the app-tier SG instead." },
  ]}
/>

</Explanation>

<KeyTakeaway>Two filters, two state models. Security group: instance/ENI-level, stateful (return traffic auto-allowed), allow-only, all rules unioned, can reference another SG as source — your least-privilege scalpel. Network ACL: subnet-level, stateless (open ephemeral return ports 1024-65535 or responses drop), ordered low-to-high first-match-wins, supports explicit deny — your subnet-wide guardrail for blocking a CIDR. Do least privilege in SGs; reach for a NACL only when you need a deny or a subnet blast-radius limit.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-security-groups-and-nacls-retrieval"
  lang="en"
  questions={[
    { q: "Contrast security groups and network ACLs across state, rule verbs, evaluation, scope, and defaults — and explain the ephemeral-port trap.", a: "A security group filters at the instance/ENI level; a network ACL filters at the subnet edge. State is the key divide: SGs are stateful — allow an inbound (or outbound) flow and its response is automatically allowed back regardless of the opposite-direction rules — while NACLs are stateless and remember nothing, so you must allow both directions explicitly. SGs are allow-only with no order: every rule across every attached SG is unioned, any allow passes, and unmatched traffic is implicitly denied. NACLs are an ordered numbered list (1-32766), evaluated lowest-first with first-match-wins, and support explicit deny. Scope: an SG can reference another SG as its source (the least-privilege idiom); a NACL takes CIDRs only. Defaults: the VPC's default SG allows no inbound and all outbound; the default NACL allows everything both ways, but a custom NACL denies everything until you add rules. The ephemeral-port trap: because a NACL is stateless, the response to an allowed inbound 443 leaves on a high random port (the client's source port), not 443 — so you must add an outbound rule for ephemeral ports 1024-65535, or the response is dropped at the subnet edge and the connection hangs." },
    { q: "When do you actually use a NACL instead of just security groups, and why can't an SG do it?", a: "Reach for a NACL when you need something an SG fundamentally cannot express: an explicit deny, or a coarse subnet-wide guardrail independent of any single instance's config. An SG is allow-only — it can grant access but never forbid it — so it cannot block a specific malicious CIDR; any instance could still be reachable from that range if its SG allows the port. A NACL can place a DENY for that /24 numbered below the allow rules, and because it sits at the subnet edge the block covers every instance at once and can't be re-opened by an over-broad SG rule. NACLs are also useful to enforce tier separation ('these two subnets may never talk') as a blast-radius limit. The common practice: do all least-privilege work in SGs (stateful, role-referencing, no return-path reasoning), and keep NACLs permissive except where you specifically need a deny — because a stateless filter where a stateful one belongs is how you end up debugging dropped ephemeral return traffic." },
  ]}
/>

<Recap lang="en">
Your AWS resources sit behind two packet filters with opposite personalities. The security group guards the instance's ENI: it is stateful, so a response to any allowed flow is automatically permitted in the reverse direction; it is allow-only, with every attached rule unioned and unmatched traffic implicitly denied; and it can name another security group as a source, which is the idiomatic least-privilege pattern — "allow 5432 from the app-tier SG" survives autoscaling because it grants a role, not an address. The network ACL guards the subnet edge: it is stateless and remembers nothing, so you must allow both directions and crucially open the ephemeral return ports 1024-65535, or responses to an allowed inbound connection silently drop; its rules are numbered and evaluated lowest-first with first-match-wins; and it supports explicit deny, which lets it block a specific bad CIDR — something a security group can never do because it has no deny verb. Use the two in concert: SGs as the precise, stateful, per-resource scalpel where almost all real access control lives, and NACLs as the coarse, deny-capable, subnet-wide guardrail you reach for only when you need to blackhole a CIDR or limit a blast radius. The senior instinct is remembering which filter has memory and which doesn't. Now when you see a connection that silently hangs after the SYN-ACK leaves the instance, check the NACL outbound rules for ephemeral ports before assuming the SG is wrong.
</Recap>
