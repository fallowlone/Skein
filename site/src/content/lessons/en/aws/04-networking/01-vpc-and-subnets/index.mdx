---
concepts:
- vpc
- subnets
- internet-gateway
- nat-gateway
- vpc-endpoints
- SAA-C03:Design Secure Architectures
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: middle
order: 1
prereqs: []
slug: "01-vpc-and-subnets"
sources:
- https://docs.aws.amazon.com/vpc/latest/userguide/vpc-nat-gateway.html
- https://docs.aws.amazon.com/vpc/latest/privatelink/gateway-endpoints.html
- https://docs.aws.amazon.com/vpc/latest/userguide/subnet-basics.html
spiral: []
status: ready
summary: 'A VPC is an isolated virtual network you define by CIDR; subnets carve it per-AZ, and "public" vs "private" is decided by routing, not a flag. Master IGW, NAT, and gateway endpoints — or eat a five-figure egress bill.'
title: 'VPC and subnets: CIDR, routing, and the NAT egress trap'
track: "aws"
unit: "04-networking"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A data team runs a nightly Spark job in a private subnet that reads and writes terabytes to S3. It works flawlessly for a year. Then finance forwards the AWS bill with one line circled in red: tens of thousands of dollars under "NAT Gateway — data processing." Nobody changed the code. What happened is that every byte to S3 had been routed through a NAT Gateway the whole time, and NAT charges a per-GB data-processing fee on top of its hourly rate. The traffic never needed NAT at all — S3 has a free VPC Gateway Endpoint that keeps it off NAT entirely. One route-table entry, added in five minutes, erased the line item. The lesson is brutal and common: in a VPC, where your packets go is decided by routing you configure, and the wrong default route is a silent invoice.
</Hook>

<Crux>A VPC is an isolated network you define by CIDR; subnets are per-AZ slices. Public vs private is pure routing — and routing sets your bill.</Crux>

<Explanation>

## CIDR, subnets, and the per-AZ rule

By the end of this section you will know exactly which two things make a subnet "public," and why the wrong answer costs money every hour.

A **VPC** (Virtual Private Cloud) is a logically isolated virtual network you carve out inside an AWS Region. You define its address space with a CIDR block, for example `10.0.0.0/16` — that gives you 65,536 private IPv4 addresses to hand out. Nothing inside one VPC can reach another VPC or the internet unless you explicitly wire a path; isolation is the default.

You don't put resources directly in a VPC — you put them in **subnets**, which carve the VPC CIDR into smaller ranges like `10.0.1.0/24`. The single most important structural fact: **a subnet lives entirely in one Availability Zone and cannot span AZs.** An AZ is an isolated datacenter cluster, so if you place every subnet in one AZ and that AZ has an outage, your whole tier goes dark. High availability is therefore non-negotiable: spread subnets across at least two AZs and run a copy of each tier in each.

```text
VPC CIDR        10.0.0.0/16          (65,536 IPs, one Region)
  AZ us-east-1a
    public      10.0.0.0/24          web / load balancer / NAT
    private     10.0.1.0/24          app servers, databases
  AZ us-east-1b
    public      10.0.2.0/24
    private     10.0.3.0/24
```

A subnet's CIDR must be a subset of the VPC CIDR, and the two cannot overlap each other. AWS also reserves the first four and last IP in every subnet, so a `/24` gives you 251 usable addresses, not 256 — a detail that bites when you size subnets too tight for an autoscaling fleet. When you plan your CIDR layout, always leave headroom: a `/24` looks generous until you have 200 Lambda ENIs or an EKS node pool expanding under load.

## "Public" and "private" are routing, not a flag

There is no checkbox that makes a subnet public. **A subnet is public only if two things are true:** its associated **route table** sends `0.0.0.0/0` (all internet-bound traffic) to an **Internet Gateway (IGW)**, *and* the instances in it have public IP addresses. Remove either and the same subnet is effectively private. The IGW is a horizontally-scaled, redundant VPC component that provides bidirectional internet connectivity; AWS does not charge an hourly fee for the gateway itself (you pay normal data-transfer rates).

Every subnet is associated with exactly one **route table**, a list of `destination → target` rules evaluated by longest-prefix match. Every route table has an immutable **local route** for the VPC CIDR, which is what lets any subnet talk to any other subnet inside the VPC with no extra configuration.

```text
# Public subnet route table
Destination     Target
10.0.0.0/16     local              # intra-VPC, always present
0.0.0.0/0       igw-0abc123        # internet, both directions

# Private subnet route table
Destination     Target
10.0.0.0/16     local
0.0.0.0/0       nat-0def456        # egress only, via NAT in a public subnet
```

A **NAT Gateway** is how private instances reach *out* to the internet — for OS updates, package registries, third-party APIs — without being reachable *from* the internet. Per AWS docs, instances in a private subnet connect outbound through a NAT Gateway, but external services cannot initiate a connection back. A public NAT Gateway must live in a **public subnet** and have an Elastic IP, and its route ultimately sends traffic to the IGW. It is a managed, **zonal** resource: it exists in one AZ.

<FlowDiagram
  label="Traffic paths in a two-tier VPC: public subnet to the internet via IGW, private subnet egress via NAT, and S3 via a free gateway endpoint"
  hue="var(--d-systems)"
  nodes={[
    { id: "app", label: "App instance", sub: "private subnet 10.0.1.0/24" },
    { id: "nat", label: "NAT Gateway", sub: "public subnet, zonal, per-GB processed" },
    { id: "igw", label: "Internet Gateway", sub: "no hourly gateway fee" },
    { id: "net", label: "Internet", sub: "APIs, updates, registries" },
    { id: "vpce", label: "S3 Gateway Endpoint", sub: "no additional charge" },
    { id: "s3", label: "Amazon S3", sub: "stays on AWS network" },
  ]}
  edges={[
    { from: "app", to: "nat", label: "0.0.0.0/0 egress" },
    { from: "nat", to: "igw", label: "translated" },
    { from: "igw", to: "net", label: "outbound" },
    { from: "app", to: "vpce", label: "pl-s3 prefix list" },
    { from: "vpce", to: "s3", label: "free, bypasses NAT" },
  ]}
  caption="The app reaches general internet through NAT (billed per GB) but reaches S3 through a gateway endpoint that costs nothing and never touches NAT. Routing chooses the path — and the path chooses the bill."
/>

## The egress-cost trap and how endpoints kill it

Here is the trap that produces five-figure surprises. A NAT Gateway bills on **two** axes: an hourly charge for each gateway, *plus* a per-GB **data-processing charge** on every byte that flows through it (prices are illustrative and region-dependent — see the [Amazon VPC pricing page](https://aws.amazon.com/vpc/pricing/) for current rates). When a private instance talks to S3, DynamoDB, or another high-volume service through the default `0.0.0.0/0` to NAT route, every gigabyte is taxed by that data-processing fee — and at terabyte scale it dwarfs the actual compute cost.

The fix is a **VPC Gateway Endpoint**. Per AWS docs, gateway endpoints provide connectivity to **S3 and DynamoDB** "without requiring an internet gateway or a NAT device," and there is **no additional charge** for using them. You enable one by selecting your private subnets' route tables; AWS auto-adds a route whose destination is an AWS-managed **prefix list** for the service and whose target is the endpoint. Because routing uses longest-prefix match, S3-bound traffic now takes the free endpoint route instead of the NAT default — and stays entirely on the AWS network.

For services without a gateway endpoint, **Interface Endpoints (PrivateLink)** put an ENI in your subnet so traffic to that service stays off the public internet (these carry their own hourly + per-GB charge, but avoid NAT data-processing and the internet path).

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Path component</th>
        <th class="border border-border px-3 py-2 text-left">Direction</th>
        <th class="border border-border px-3 py-2 text-left">Cost shape</th>
        <th class="border border-border px-3 py-2 text-left">HA concern</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Internet Gateway</td>
        <td class="border border-border px-3 py-2">Inbound + outbound</td>
        <td class="border border-border px-3 py-2">No hourly gateway fee; data transfer applies</td>
        <td class="border border-border px-3 py-2">Region-wide, redundant</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">NAT Gateway</td>
        <td class="border border-border px-3 py-2">Outbound only</td>
        <td class="border border-border px-3 py-2">Hourly + per-GB data processing</td>
        <td class="border border-border px-3 py-2">Zonal — one per AZ for HA</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Gateway Endpoint (S3/DynamoDB)</td>
        <td class="border border-border px-3 py-2">To service only</td>
        <td class="border border-border px-3 py-2">No additional charge</td>
        <td class="border border-border px-3 py-2">Per route table, no ENI</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Interface Endpoint (PrivateLink)</td>
        <td class="border border-border px-3 py-2">To service only</td>
        <td class="border border-border px-3 py-2">Hourly + per-GB (no NAT fee)</td>
        <td class="border border-border px-3 py-2">One ENI per AZ for HA</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="en">
Why does NAT have a separate per-AZ failure story? A NAT Gateway is zonal — it lives in exactly one AZ. If you build one NAT Gateway in `us-east-1a` and point the private route tables of every AZ at it, you get two failure modes at once. First, if `us-east-1a` fails, instances in `1b` and `1c` lose all egress even though their own AZ is healthy — the single NAT is an AZ-scoped single point of failure. Second, in normal operation, traffic from `1b`/`1c` instances crosses AZ boundaries to reach that NAT, and cross-AZ traffic is itself billed per GB. The fix is one NAT Gateway per AZ, with each AZ's private route table pointing at the NAT in its *own* AZ: it removes the SPOF and the cross-AZ data charge in a single move.
</Inset>

<TradeoffMatrix
  id="01-vpc-and-subnets-tradeoff-1"
  lessonSlug="01-vpc-and-subnets"
  lang="en"
  prompt="Private app servers across three AZs read and write 40 TB/month to S3 and also call a handful of third-party HTTPS APIs. You need egress that is highly available and not financially radioactive. Pick the primary design."
  options={[
    { name: "S3 Gateway Endpoint for S3, plus one NAT Gateway per AZ for the third-party APIs", summary: "Free endpoint for the bulk, NAT only for the rest.", correct: true, justification: "Right fit. The 40 TB of S3 traffic is the cost bomb, and an S3 gateway endpoint carries no additional charge and bypasses NAT entirely, so that volume stops accruing data-processing fees and stays on the AWS network. The third-party HTTPS APIs have no gateway endpoint, so they genuinely need NAT — and you run one NAT Gateway per AZ so each AZ's egress survives a neighbor's failure and avoids cross-AZ data charges. This splits traffic by what each path actually needs: free endpoint for the heavy known service, HA NAT for the long tail." },
    { name: "Route all egress, including S3, through a single NAT Gateway", summary: "One NAT, simplest to set up.", justification: "This is exactly the trap. Every one of the 40 TB to S3 is taxed by NAT's per-GB data-processing charge for no reason, since a free gateway endpoint exists. Worse, a single NAT is an AZ-scoped single point of failure, and instances in the other two AZs pay cross-AZ data charges to reach it. Simplicity here buys you a five-figure line item and an outage waiting to happen." },
    { name: "One NAT Gateway per AZ for everything, no endpoints", summary: "HA NAT, no special routing.", justification: "Better than a single NAT because it is highly available and avoids cross-AZ hops, but it still routes all 40 TB of S3 traffic through NAT and pays the per-GB data-processing fee on every byte. You have solved availability and ignored cost. Adding the free S3 gateway endpoint removes the largest charge with one route-table change." },
    { name: "Give the app servers public IPs in public subnets", summary: "No NAT needed at all.", justification: "Wrong on security and still not free. Public subnets expose the app servers to inbound internet connections, which is precisely what private subnets plus NAT exist to prevent for backend tiers. You would also still pay data-transfer charges, and you would have thrown away the isolation that makes the architecture defensible. Egress design is not an excuse to make backends internet-facing." },
  ]}
/>

<Quiz
  id="01-vpc-and-subnets-quiz-1"
  lessonSlug="01-vpc-and-subnets"
  lang="en"
  question="What actually makes a subnet 'public' in a VPC?"
  choices={[
    { label: "Its route table sends 0.0.0.0/0 to an Internet Gateway and its instances have public IPs — public/private is defined by routing, not a setting", correct: true },
    { label: "A 'public' checkbox you toggle when creating the subnet", misconception: "There is no such flag. AWS decides reachability by what the subnet's route table points to. A subnet with a 0.0.0.0/0 route to an Internet Gateway is public; remove that route and the identical subnet is private." },
    { label: "Placing the subnet in a special public Availability Zone", misconception: "AZs are not public or private. Every AZ can host both public and private subnets; the difference is purely whether the route table reaches an Internet Gateway, not which AZ the subnet sits in." },
    { label: "Attaching a NAT Gateway to the subnet", misconception: "NAT Gateways give private subnets outbound-only internet access; they don't make a subnet public. A public subnet routes to an Internet Gateway and accepts the possibility of inbound connections — the opposite of what NAT provides." },
  ]}
/>

<Quiz
  id="01-vpc-and-subnets-quiz-2"
  lessonSlug="01-vpc-and-subnets"
  lang="en"
  question="Private instances move 30 TB/month to S3 and the NAT Gateway data-processing line is enormous. Cheapest correct fix?"
  choices={[
    { label: "Add an S3 VPC Gateway Endpoint so S3 traffic bypasses NAT entirely at no additional charge", correct: true },
    { label: "Buy a bigger NAT Gateway instance type to lower the per-GB rate", misconception: "NAT Gateway is a managed service with no instance type to upsize, and its per-GB data-processing charge applies regardless of throughput. Scaling it up wouldn't reduce the per-GB fee — the right move is to route S3 off NAT via a free gateway endpoint." },
    { label: "Move the instances to public subnets so they hit S3 directly", misconception: "That exposes backend instances to inbound internet traffic and still doesn't use the free endpoint path; you'd pay data transfer and lose isolation. A gateway endpoint keeps instances private and removes the charge." },
    { label: "Compress the data before sending — there's nothing else to do about NAT cost", misconception: "Compression only shrinks volume at the margins and ignores the real problem: this traffic should never traverse NAT. An S3 gateway endpoint eliminates the data-processing charge for S3 outright, which no amount of compression can match." },
  ]}
/>

</Explanation>

<KeyTakeaway>A VPC is an isolated network defined by a CIDR (e.g. 10.0.0.0/16); subnets slice it and each lives in one AZ, so spread across AZs for HA. A subnet is public only if its route table sends 0.0.0.0/0 to an Internet Gateway and instances have public IPs — routing defines public vs private. Private subnets egress via a zonal NAT Gateway (hourly + per-GB), so run one per AZ to avoid an AZ SPOF and cross-AZ charges. The egress trap: S3/DynamoDB through NAT racks up data-processing fees — use a free VPC Gateway Endpoint to bypass NAT entirely.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-vpc-and-subnets-retrieval"
  lang="en"
  questions={[
    { q: "What defines a public vs a private subnet, and how do private subnets reach the internet?", a: "Public vs private is decided by routing, not a setting. A subnet is public only when its associated route table sends 0.0.0.0/0 to an Internet Gateway AND its instances carry public IP addresses; remove either and the same subnet behaves as private. Every route table also has an immutable local route for the VPC CIDR, which lets all subnets in the VPC reach each other with no extra config. A private subnet has no 0.0.0.0/0 route to an IGW, so to reach out (for OS updates, APIs) it routes 0.0.0.0/0 to a NAT Gateway that lives in a public subnet. The NAT lets instances initiate outbound connections but blocks unsolicited inbound — that asymmetry is the whole point of keeping backend tiers private while still letting them pull updates." },
    { q: "Explain the NAT egress-cost trap and how VPC endpoints fix it, plus the NAT high-availability rule.", a: "A NAT Gateway bills two ways: an hourly charge per gateway plus a per-GB data-processing charge on every byte through it (illustrative, region-dependent — see the VPC pricing page). When private instances send high-volume traffic to S3 or DynamoDB through the default 0.0.0.0/0 to NAT route, every gigabyte is taxed, producing five-figure surprise bills at terabyte scale. The fix is a VPC Gateway Endpoint for S3/DynamoDB: per AWS docs it needs no internet gateway or NAT device and carries no additional charge — you add it to the private route tables, AWS inserts a route to the service's prefix list, and longest-prefix match sends that traffic over the free endpoint, staying on the AWS network. For other services, an interface endpoint (PrivateLink) keeps traffic off the internet. HA rule: a NAT Gateway is zonal, so a single NAT is an AZ-scoped single point of failure and forces cross-AZ data charges from other AZs; run one NAT Gateway per AZ with each AZ's private route table pointing at its own NAT." },
  ]}
/>

<Recap lang="en">
A VPC is a logically isolated virtual network you define by a CIDR block such as 10.0.0.0/16, and you place resources in subnets that carve that CIDR into smaller ranges. The structural rule that drives every availability decision is that a subnet lives entirely in one Availability Zone and cannot span AZs, so you spread subnets across at least two AZs to survive a zone outage. Public versus private is not a flag but a consequence of routing: a subnet is public only when its route table sends 0.0.0.0/0 to an Internet Gateway and its instances have public IPs, while a private subnet routes outbound through a zonal NAT Gateway sitting in a public subnet, which lets instances initiate connections out but blocks unsolicited inbound. The NAT Gateway is also where money quietly burns, because it charges per hour and per GB of data processed, so routing high-volume S3 or DynamoDB traffic through it produces five-figure surprises that a free VPC Gateway Endpoint eliminates by sending that traffic over a prefix-list route that never touches NAT and stays on the AWS network. Finish the picture with the high-availability rule — one NAT Gateway per AZ, each AZ's route table pointing at its own NAT — to remove both the single-point-of-failure and the cross-AZ data charge, and you have a VPC whose paths, and whose bill, are chosen on purpose. Now when you open the AWS Cost Explorer and see an unexpected NAT Gateway line, your first question is: which route table is sending that traffic through NAT instead of through a free endpoint?
</Recap>
