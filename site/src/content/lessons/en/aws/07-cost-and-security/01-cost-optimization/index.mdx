---
concepts:
- savings-plans
- spot-instances
- data-transfer-egress
- nat-gateway-cost
- idle-waste-rightsizing
- cost-allocation-tags
- SAA-C03:Design Cost-Optimized Architectures
- CLF-C02:Billing, Pricing, and Support
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: middle
order: 1
prereqs: []
slug: "01-cost-optimization"
sources:
- https://docs.aws.amazon.com/savingsplans/latest/userguide/what-is-savings-plans.html
- https://docs.aws.amazon.com/savingsplans/latest/userguide/plan-types.html
- https://docs.aws.amazon.com/vpc/latest/userguide/nat-gateway-pricing.html
- https://docs.aws.amazon.com/awsaccountbilling/latest/aboutv2/activating-tags.html
- https://aws.amazon.com/ec2/pricing/
spiral: []
status: ready
summary: 'Cost is an architecture property. The biggest levers, in order: commit baseline compute to Savings Plans/Spot, kill idle waste, and stop paying for egress and NAT you did not need. Cost Explorer, Budgets, and activated tags give you the visibility to do it.'
title: 'Cost optimization: purchase options, egress, idle waste, and visibility'
track: "aws"
unit: "07-cost-and-security"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The bill lands on the first of the month and it is forty percent over forecast. Nobody shipped a new feature. The team opens Cost Explorer and finds three quiet culprits: a fleet of EC2 instances running 24/7 entirely On-Demand because "we'll buy Savings Plans later"; a single line item called "data transfer" that nobody can explain, traced back to a chatty service whose pods landed in a different Availability Zone from the database they hammer all day; and a NAT gateway processing terabytes for a workload that only ever talks to S3. None of it is a bug. Every dollar is for real compute and real bytes. The surprise is not that AWS charged — it's that the architecture had been quietly choosing to pay full price, cross-AZ, through a metered gateway, for months, and nobody had attributed a cent of it because nothing was tagged.
</Hook>

<Crux>Cost is an architecture property, not a billing chore. Top levers: commit steady compute, kill idle, drop egress and NAT you never needed.</Crux>

<Explanation>

## Lever 1: compute purchase options — match commitment to baseline

The same EC2 capacity has four prices, and the gap between the cheapest and the priciest is enormous. **On-Demand** is the default: maximum flexibility, no commitment, and the highest per-hour rate. You pay it for every hour an instance runs whether it is busy or idle. It is the right price for unpredictable or short-lived work — and the wrong price for a baseline that runs around the clock.

**Savings Plans** trade flexibility for a discount: you commit to a steady amount of compute spend (measured in dollars per hour) for a one- or three-year term, and in return you save up to ~72% versus On-Demand. There are two shapes worth knowing. A **Compute Savings Plan** is the flexible one — its discount (up to ~66% off) applies automatically across instance family, size, Region, OS, *and* across EC2, Fargate, and Lambda, so you can re-architect underneath it without losing the rate. An **EC2 Instance Savings Plan** is cheaper (up to ~72% off) but narrower: you commit to a specific instance family in a chosen Region, and the discount only follows size and OS changes within that family. **Reserved Instances** are the older mechanism that predates Savings Plans; they bundle a discount with optional capacity reservation and are largely superseded by Savings Plans for pure cost savings.

At the far end, **Spot Instances** sell AWS's spare capacity at up to ~90% off On-Demand — with one catch: AWS can reclaim the instance at any time, giving only a **two-minute interruption notice** (delivered via instance metadata and an EventBridge event). Spot is a gift for *interruptible, stateless, fault-tolerant* work — batch jobs, CI runners, big-data processing, anything that can checkpoint and resume — and a trap for anything stateful that cannot survive being killed mid-flight.

The senior rule is to **match commitment to the shape of demand**: cover the steady 24/7 baseline with a Savings Plan or RI, absorb the unpredictable burst on top with On-Demand, and run interruptible batch on Spot. Pricing is region-dependent and changes over time — always confirm current numbers on the [EC2 pricing page](https://aws.amazon.com/ec2/pricing/); the percentages here are AWS's published *up-to* maximums, not a quote.

<FlowDiagram
  label="A single workload's demand split across three purchase options by demand shape"
  hue="var(--d-systems)"
  nodes={[
    { id: "base", label: "Steady 24/7 baseline", sub: "always-on floor of demand" },
    { id: "burst", label: "Unpredictable burst", sub: "spiky peaks above baseline" },
    { id: "batch", label: "Interruptible batch", sub: "stateless, can resume" },
    { id: "sp", label: "Savings Plan / RI", sub: "commit, up to ~72% off" },
    { id: "od", label: "On-Demand", sub: "flexible, full price" },
    { id: "spot", label: "Spot", sub: "up to ~90% off, 2-min notice" },
  ]}
  edges={[
    { from: "base", to: "sp", label: "commit the floor" },
    { from: "burst", to: "od", label: "pay flex on top" },
    { from: "batch", to: "spot", label: "cheapest, reclaimable" },
  ]}
  caption="Don't buy one option for the whole workload. Commit the predictable baseline, pay On-Demand only for the burst above it, and push anything interruptible onto Spot."
/>

## Lever 2: data transfer and NAT — the silent line items

Compute is the bill everyone watches; data transfer is the bill that surprises them. The rough mental model: **ingress from the internet is generally free, egress is not**, and traffic that crosses an Availability Zone boundary or a Region boundary is metered even when it never leaves AWS. A microservice architecture that gossips constantly between services in *different* AZs can rack up cross-AZ transfer charges that dwarf the compute doing the talking. The fix is architectural: keep chatty traffic in-AZ (co-locate the talkers), front internet egress with **CloudFront** so the origin serves bytes once and the CDN serves them cheaply thereafter, and reach AWS services like S3 and DynamoDB through **VPC endpoints** instead of routing that traffic out through a gateway.

That gateway is the second silent line item. A **NAT gateway** is billed two ways at once: a per-hour charge for every hour it is provisioned, *plus* a per-GB charge for every gigabyte it processes — both region-dependent (see [NAT gateway pricing](https://docs.aws.amazon.com/vpc/latest/userguide/nat-gateway-pricing.html)). The classic surprise is a private-subnet workload whose only "external" traffic is to S3: every byte is being metered through the NAT for no reason. AWS's own guidance is to put resources in the *same AZ* as the NAT gateway to avoid stacking cross-AZ charges on top, and to use VPC interface or gateway endpoints for AWS-service traffic so it bypasses the NAT entirely.

```bash
# Find your top spend movers fast with Cost Explorer, grouped by usage type
aws ce get-cost-and-usage \
  --time-period Start=2026-05-01,End=2026-06-01 \
  --granularity MONTHLY \
  --metrics "UnblendedCost" \
  --group-by Type=DIMENSION,Key=USAGE_TYPE \
  --query 'ResultsByTime[0].Groups[?Metrics.UnblendedCost.Amount>`100`]'
# Look for *-DataTransfer-Out-Bytes, NatGateway-Bytes, and *-DataTransfer-Regional-Bytes
```

## Lever 3: idle and waste — you pay for what you forgot

A large share of most bills is pure waste: capacity that is provisioned, billed, and doing nothing. The usual suspects are **unattached EBS volumes** left behind after instances were terminated, **old snapshots** nobody prunes, **over-provisioned instances** sized for a peak that never comes, **idle RDS** databases kept "just in case," forgotten **dev/test environments** that run nights and weekends, and **load balancers** wired to nothing. Two tools attack this directly: **Compute Optimizer** analyzes utilization and recommends right-sizing (down a size, or to a cheaper family), and a simple scheduler that stops non-production instances outside working hours can cut a dev account's compute bill by more than half — those environments are idle ~128 of the 168 hours in a week.

## Lever 4: visibility — you can't optimize what you can't attribute

Every lever above is invisible without measurement, and the foundation is **cost allocation tags**. Tagging resources by `team`, `env`, and `service` lets you slice the bill by who owns what — but there is a sharp gotcha: a user-defined tag does **not** show up in cost reports until you *activate* it in the Billing and Cost Management console, and after you apply it, AWS notes a new tag key can take up to 24 hours to appear in the Billing console for activation, and once activated it only applies to costs going forward, not retroactively. Around the tags sit three tools: **Cost Explorer** to analyze and forecast spend, **AWS Budgets** to fire alerts (or even automated actions) when spend crosses a threshold, and **Cost Anomaly Detection** to catch a spike the day it starts instead of on the first of the month.

```json
{
  "BudgetName": "monthly-prod-ceiling",
  "BudgetType": "COST",
  "TimeUnit": "MONTHLY",
  "BudgetLimit": { "Amount": "5000", "Unit": "USD" },
  "CostFilters": { "TagKeyValue": ["user:env$prod"] },
  "NotificationsWithSubscribers": [
    {
      "Notification": { "NotificationType": "ACTUAL", "ComparisonOperator": "GREATER_THAN", "Threshold": 80 },
      "Subscribers": [{ "SubscriptionType": "EMAIL", "Address": "platform-oncall@example.com" }]
    }
  ]
}
```

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Lever</th>
        <th class="border border-border px-3 py-2 text-left">Typical waste</th>
        <th class="border border-border px-3 py-2 text-left">Fix</th>
        <th class="border border-border px-3 py-2 text-left">Tool</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Compute purchase</td>
        <td class="border border-border px-3 py-2">24/7 baseline on full On-Demand</td>
        <td class="border border-border px-3 py-2">Savings Plan for baseline, Spot for batch</td>
        <td class="border border-border px-3 py-2">Cost Explorer SP recommendations</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Data transfer</td>
        <td class="border border-border px-3 py-2">Cross-AZ chatter, internet egress</td>
        <td class="border border-border px-3 py-2">Co-locate in-AZ, CloudFront, VPC endpoints</td>
        <td class="border border-border px-3 py-2">Cost Explorer by usage type</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">NAT gateway</td>
        <td class="border border-border px-3 py-2">Per-hour + per-GB on AWS-bound traffic</td>
        <td class="border border-border px-3 py-2">VPC endpoints for S3/DynamoDB, same-AZ</td>
        <td class="border border-border px-3 py-2">VPC flow logs, Cost Explorer</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Idle waste</td>
        <td class="border border-border px-3 py-2">Orphan EBS, idle RDS, over-provisioned</td>
        <td class="border border-border px-3 py-2">Right-size, schedule dev shutdowns</td>
        <td class="border border-border px-3 py-2">Compute Optimizer</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Visibility</td>
        <td class="border border-border px-3 py-2">Untagged, unattributable spend</td>
        <td class="border border-border px-3 py-2">Activate cost allocation tags, set budgets</td>
        <td class="border border-border px-3 py-2">Budgets, Anomaly Detection</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="en">
Why is data transfer so easy to miss? Because no single resource shows it. Compute, storage, and databases each have a tidy line you can stare at; transfer is an emergent property of *how the pieces talk*. A cross-AZ charge has no owning resource — it is the byproduct of two services landing in different AZs, a decision made by a scheduler, not a human. That is exactly why tagging and Cost Explorer's usage-type grouping matter: the only way to see transfer cost is to slice the bill by the *flow*, because nothing in the console will surface it as "this service is expensive to talk to."
</Inset>

<TradeoffMatrix
  id="01-cost-optimization-tradeoff-1"
  lessonSlug="01-cost-optimization"
  lang="en"
  prompt="A production API runs a steady fleet of ~20 instances 24/7 at high utilization, with occasional traffic spikes, plus a nightly batch pipeline that re-encodes media and can safely restart from a checkpoint. You want the cheapest mix without risking the API's availability. Pick the purchase strategy."
  options={[
    { name: "Savings Plan for the 24/7 baseline, On-Demand for spikes, Spot for the nightly batch", summary: "Commitment matched to each demand shape.", correct: true, justification: "Right fit, and it's the canonical answer. The steady 24/7 floor is exactly what a Savings Plan is for — commit it for a 1- or 3-year term and take up to ~72% off the part of demand that never goes away. The occasional spike above the baseline is unpredictable, so pay On-Demand only for that thin slice rather than over-committing. The nightly batch is stateless and checkpoint-restartable, so it's the textbook Spot workload: up to ~90% off, and a two-minute interruption notice it can absorb by resuming from the last checkpoint. Each piece of demand pays the price that matches its shape." },
    { name: "Put everything, including the production API, on Spot to maximize the discount", summary: "Cheapest sticker price.", justification: "Maximizes discount and minimizes availability. Spot can be reclaimed at any time with only a two-minute warning; a steady production API that gets instances pulled out from under it mid-request will drop traffic and breach its SLO. Spot is for interruptible, stateless, fault-tolerant work — a user-facing API at high utilization is none of those. The batch belongs on Spot; the API does not." },
    { name: "Keep the entire fleet On-Demand and 'buy Savings Plans later'", summary: "No commitment, full flexibility.", justification: "This is the surprise-bill anti-pattern from the hook. A 24/7 baseline on full On-Demand pays the highest possible rate for the most predictable demand you have — the easiest, safest discount on the table, left on the floor. 'Later' tends to mean never. You'd also pay full On-Demand for the batch, which could have run at up to ~90% off on Spot. Flexibility you don't need on a steady baseline is just money lost." },
    { name: "Buy a 3-year EC2 Instance Savings Plan covering 100% of current usage including the batch", summary: "Deepest commit, lowest unit rate.", justification: "Over-commits and locks in. An EC2 Instance SP is narrower (one instance family in one Region) and committing 100% of usage — including spiky and batch demand that comes and goes — means paying for committed capacity during every trough. You'd also forfeit the up-to-~90% Spot discount on the batch by paying committed rates for it. Commit the steady floor, not the variable top." },
  ]}
/>

<Quiz
  id="01-cost-optimization-quiz-1"
  lessonSlug="01-cost-optimization"
  lang="en"
  question="You commit to a Compute Savings Plan, then later migrate a service from EC2 to Fargate and shift it to another Region. What happens to your discount?"
  choices={[
    { label: "It keeps applying — a Compute Savings Plan's discount follows usage across instance family, size, Region, and across EC2, Fargate, and Lambda", correct: true },
    { label: "It is lost, because the plan is tied to the original EC2 instance family", misconception: "That describes an EC2 Instance Savings Plan, which is locked to a specific family in a chosen Region. The Compute Savings Plan is the flexible one: it applies automatically regardless of family, size, Region, OS, and across EC2/Fargate/Lambda — which is exactly why it carries a slightly smaller maximum discount than the EC2 Instance plan." },
    { label: "It applies only to the Fargate portion, not the cross-Region move", misconception: "A Compute Savings Plan is Region-flexible by design; moving Regions does not break it. The whole point of the Compute (versus EC2 Instance) plan is that you can re-architect underneath the commitment without losing the rate." },
    { label: "Nothing changes because Savings Plans only ever apply to raw EC2", misconception: "Compute Savings Plans explicitly extend to Fargate and Lambda usage, not just raw EC2. Only the narrower EC2 Instance Savings Plan is EC2-family-specific." },
  ]}
/>

<Quiz
  id="01-cost-optimization-quiz-2"
  lessonSlug="01-cost-optimization"
  lang="en"
  question="Your bill shows a large, growing 'data transfer' charge, but every instance looks correctly sized. What is the most likely architectural cause?"
  choices={[
    { label: "Services that talk constantly are split across different Availability Zones (or going to the internet / through NAT), so the traffic between them is metered cross-AZ", correct: true },
    { label: "Your instances are over-provisioned and need right-sizing", misconception: "Right-sizing fixes compute cost, not transfer cost. Data transfer is an emergent property of how components communicate — cross-AZ chatter, internet egress, and NAT data processing — and is invisible at the per-instance level no matter how well-sized each box is." },
    { label: "Ingress traffic from the internet is being billed", misconception: "Ingress from the internet is generally free; egress and cross-boundary (cross-AZ / cross-Region) traffic are what get metered. The growing charge points to chatty cross-AZ flows or internet egress, not inbound bytes." },
    { label: "Cost Explorer is double-counting the bytes", misconception: "Cost Explorer reports real metered usage. The signature of an unexplained, growing transfer line is real cross-AZ or NAT-processed traffic — slice the bill by usage type to find which flow owns it, then co-locate or add a VPC endpoint." },
  ]}
/>

</Explanation>

<KeyTakeaway>Cost is an architecture property. Pull the levers in order of leverage: (1) match purchase commitment to demand shape — Savings Plans (up to ~72% off, Compute SP flexible across EC2/Fargate/Lambda) for the steady baseline, On-Demand for burst, Spot (up to ~90% off, 2-min notice) for interruptible batch; (2) stop paying for cross-AZ chatter, internet egress, and per-hour-plus-per-GB NAT you didn't need — co-locate, CloudFront, VPC endpoints; (3) kill idle waste with Compute Optimizer and dev shutdowns; (4) make it visible with activated cost allocation tags, Cost Explorer, Budgets, and Anomaly Detection. Prices are region-dependent and illustrative — confirm on the AWS pricing pages.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-cost-optimization-retrieval"
  lang="en"
  questions={[
    { q: "List the compute purchase options from priciest to cheapest, and the rule for matching them to a workload.", a: "On-Demand is the priciest and most flexible — no commitment, billed per running hour whether busy or idle; right for unpredictable or short-lived work. Savings Plans trade a 1- or 3-year dollars-per-hour commitment for up to ~72% off: a Compute Savings Plan is flexible (its up-to-~66% discount applies across instance family, size, Region, OS, and across EC2/Fargate/Lambda), while an EC2 Instance Savings Plan is cheaper (up to ~72%) but locked to a specific instance family in a chosen Region. Reserved Instances are the older mechanism, largely superseded by Savings Plans for cost. Spot Instances are cheapest — up to ~90% off — but reclaimable with only a two-minute interruption notice, so only for interruptible, stateless, fault-tolerant work like batch and CI. The rule: cover the steady 24/7 baseline with a Savings Plan or RI, pay On-Demand only for the unpredictable burst on top, and run interruptible batch on Spot. Prices are region-dependent — confirm on the EC2 pricing page." },
    { q: "Where does money silently leak beyond compute, and which tools give you the visibility to catch it?", a: "Two silent line items: data transfer and NAT. Ingress from the internet is generally free, but egress and any traffic crossing an Availability Zone or Region boundary is metered — so services that chat constantly across different AZs rack up cross-AZ charges; fix it by co-locating talkers in-AZ, fronting internet egress with CloudFront, and reaching S3/DynamoDB through VPC endpoints. A NAT gateway bills both per provisioned hour and per GB processed, so a workload whose only external traffic is to AWS services is paying NAT for nothing — use VPC endpoints to bypass it and keep resources in the NAT's AZ. Then there's idle waste: orphan EBS volumes, old snapshots, idle RDS, over-provisioned instances, forgotten dev environments, and unused load balancers — attack with Compute Optimizer right-sizing and scheduled dev shutdowns. Visibility ties it together: activate cost allocation tags (a user-defined tag does nothing until activated in the Billing console, and can take up to 24h to appear plus up to 24h to activate), then use Cost Explorer to analyze and forecast, Budgets to alert on thresholds, and Cost Anomaly Detection to catch spikes early. You can't optimize what you can't attribute." },
  ]}
/>

<Recap lang="en">
Cost is an architecture property, and the levers pay off in order of leverage. First, match compute purchase options to demand shape: On-Demand is the flexible full-price default, Savings Plans take up to ~72% off in exchange for a 1- or 3-year dollars-per-hour commitment (the Compute Savings Plan staying flexible across instance family, Region, and EC2/Fargate/Lambda, the EC2 Instance plan trading flexibility for a slightly deeper discount locked to one family and Region), and Spot sells spare capacity at up to ~90% off but can be reclaimed with only a two-minute notice — so commit the steady 24/7 baseline, pay On-Demand for the burst on top, and run interruptible, stateless batch on Spot. Second, stop paying for data movement you didn't need: cross-AZ chatter and internet egress are metered, and a NAT gateway charges per hour and per GB, so co-locate chatty services in one AZ, front egress with CloudFront, and route AWS-service traffic through VPC endpoints. Third, kill idle waste — orphan EBS, idle RDS, over-provisioned instances, forgotten dev environments — with Compute Optimizer right-sizing and scheduled shutdowns. Fourth, make all of it visible: activate cost allocation tags so spend is attributable by team, env, and service, then run Cost Explorer, Budgets, and Anomaly Detection. All prices here are AWS's published up-to maximums and are region-dependent and illustrative — confirm current numbers on the AWS pricing pages before you quote them.
</Recap>
