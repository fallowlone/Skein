---
concepts:
- cloud-platform
- service-categories
- managed-vs-self-managed
- elasticity-and-pay-per-use
- vendor-lock-in
- CLF-C02:Cloud Concepts
deepensInto: []
estMin: 15
lang: en
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-what-aws-is
sources:
- https://docs.aws.amazon.com/whitepapers/latest/aws-overview/introduction.html
- https://docs.aws.amazon.com/whitepapers/latest/aws-overview/types-of-cloud-computing.html
- https://aws.amazon.com/about-aws/global-infrastructure/
spiral: []
status: ready
summary: AWS is a global utility-computing platform — on-demand compute, storage, networking, and managed services billed per use. This lesson maps the service categories and core vocabulary the rest of the track assumes.
title: What AWS actually is
track: aws
unit: 00-start-here
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A two-person startup gets a feature on the front page of a news aggregator. Traffic goes from 40 requests a minute to 8,000 in under a minute. They never bought a server. There is no rack, no colocation contract, no "let's order more capacity, it'll arrive in three weeks." A line in an autoscaling config noticed the CPU climb and started more instances; a managed load balancer spread the flood across them; a managed database absorbed the read spike from a replica it had been quietly keeping warm. The bill at the end of the month is a few hundred dollars more than usual. That elasticity — capacity that appears in seconds and is billed by the second — is what AWS sells. It is also what makes the bill, and the architecture, easy to get wrong.
</Hook>

<Crux>AWS is a utility-computing platform: compute, storage, networking, and managed services, billed per use and rented, not owned.</Crux>

By the end of this lesson you'll know the six service categories, how to read the managed-vs-self-managed tradeoff, and why that same elasticity that saved the startup can silently run up a five-figure bill if you're not paying attention.

<Explanation>

## AWS is a utility, not a product

The clearest way to understand AWS is by analogy to the electrical grid. You do not run a generator in your basement; you draw power from a shared utility, pay for what you consume, and the utility handles generation, distribution, and reliability. AWS — Amazon Web Services, launched in 2006 — is that utility for computing. Instead of buying servers, racking them in a data center, wiring networking, and replacing failed disks at 3 a.m., you rent those capabilities over an API and pay only for what you use.

That phrase "over an API" is the whole shift. Every resource in AWS — a virtual server, a terabyte of storage, a database, a DNS record, a firewall rule — is created, changed, and destroyed by a programmatic call. The web console you click through is just a front end over the same API the SDKs and Terraform use. This is why infrastructure becomes code, why a script can stand up an entire environment in minutes, and why the same elasticity that saved the startup in the hook can, run carelessly, spin up a thousand instances and a five-figure bill just as fast.

AWS today is not one service but **over 200** of them, available on-demand with pay-as-you-go pricing, running in 190+ countries. Nobody learns all 200. The skill is not memorizing services — it is knowing the *categories*, so that when you have a problem you know which shelf to look on.

## The mental model: service categories

Treat AWS as a hardware store organized by aisle. You do not need to know every product; you need to know what each aisle is for. Six categories cover the vast majority of what a working application uses:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Category</th>
        <th class="border border-border px-3 py-2 text-left">What it gives you</th>
        <th class="border border-border px-3 py-2 text-left">Flagship services (a map, not a syllabus)</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Compute</strong></td>
        <td class="border border-border px-3 py-2">Somewhere to run code</td>
        <td class="border border-border px-3 py-2"><code>EC2</code> (virtual machines), <code>Lambda</code> (functions, no server to manage), <code>ECS/EKS</code> (containers)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Storage</strong></td>
        <td class="border border-border px-3 py-2">Somewhere to put bytes</td>
        <td class="border border-border px-3 py-2"><code>S3</code> (object storage for files/backups/static sites), <code>EBS</code> (disks for EC2)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Database</strong></td>
        <td class="border border-border px-3 py-2">Somewhere to put structured data</td>
        <td class="border border-border px-3 py-2"><code>RDS</code> (managed Postgres/MySQL), <code>DynamoDB</code> (managed NoSQL)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Networking</strong></td>
        <td class="border border-border px-3 py-2">How traffic reaches and moves between things</td>
        <td class="border border-border px-3 py-2"><code>VPC</code> (private network), <code>ELB</code> (load balancing), <code>Route 53</code> (DNS), <code>CloudFront</code> (CDN)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Security &amp; identity</strong></td>
        <td class="border border-border px-3 py-2">Who can do what</td>
        <td class="border border-border px-3 py-2"><code>IAM</code> (permissions), <code>KMS</code> (encryption keys), <code>Secrets Manager</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Observability</strong></td>
        <td class="border border-border px-3 py-2">What is happening, and what happened</td>
        <td class="border border-border px-3 py-2"><code>CloudWatch</code> (metrics/logs/alarms), <code>CloudTrail</code> (audit log of every API call)</td>
      </tr>
    </tbody>
  </table>
</div>

A typical web app touches one service from most of these aisles: code on **EC2** or **Lambda**, files in **S3**, data in **RDS**, traffic via a **VPC** behind an **ELB** with **Route 53** out front, access governed by **IAM**, and the whole thing watched by **CloudWatch**. Learn the aisles first; the specific product names attach themselves naturally once you know what problem they solve.

## Managed vs self-managed: the axis that defines AWS

The single most important distinction in the whole platform is how much of the operational work AWS does for you. The same capability — say, a Postgres database — is available at several points along a spectrum, and the choice is a real tradeoff, not a default.

At the **self-managed** end, you run EC2 (a raw virtual machine), install Postgres yourself, and own everything above the hypervisor: OS patches, Postgres upgrades, backups, replication, failover, monitoring. You get total control and the lowest per-hour price, and you inherit all the 3 a.m. pages. At the **managed** end, you use RDS: AWS runs the database engine, applies patches, takes automated backups, and can fail over to a standby in another Availability Zone automatically. You pay more per hour and give up some knobs, but you stop owning the operational toil. Further still are **serverless** services like Lambda and DynamoDB, where there is no instance to size at all — you are billed per request and per millisecond, and capacity is the provider's problem entirely.

This managed-vs-self-managed axis is the lens for almost every AWS decision. "Should we use RDS or run Postgres on EC2?" is really "is the operational work worth the premium for *this* workload?" For most teams, most of the time, the answer is to buy the managed version and spend the saved hours on the product — but it is a decision, and senior engineers make it deliberately.

<Inset kind="why" lang="en">
"Managed" does not mean "no responsibility." AWS operates under a **shared responsibility model**: AWS secures the cloud *infrastructure* — physical data centers, hypervisor, the managed service's internals — while you remain responsible for security *in* the cloud — your IAM policies, your data, your network rules, your application code. A managed database is patched for you, but an S3 bucket left public, or an IAM role with `*` permissions, is entirely your failure, not Amazon's. Most real-world AWS breaches are misconfigurations on the customer's side of that line.
</Inset>

<Quiz
  id="01-what-aws-is-quiz-1"
  lessonSlug="01-what-aws-is"
  lang="en"
  question="A team needs a Postgres database and wants to minimize the operational work of patching, backups, and failover. Which AWS choice fits, and why?"
  choices={[
    { label: "RDS — a managed database service where AWS handles engine patching, automated backups, and Multi-AZ failover, trading a per-hour premium for far less operational toil", correct: true },
    { label: "EC2 with Postgres installed manually, because it is the cheapest per hour", misconception: "It is the cheapest per hour, but it is the self-managed end of the spectrum: you now own OS patches, Postgres upgrades, backups, replication, and failover. The team explicitly wanted to minimize that operational work, so the per-hour saving is paid back many times over in toil." },
    { label: "S3, because it is AWS's main storage service", misconception: "S3 is object storage — files, backups, static assets — not a relational database. You cannot run SQL queries or transactions against it. It is the wrong aisle for structured, queried data." },
    { label: "CloudWatch, because the team cares about reliability", misconception: "CloudWatch is observability — metrics, logs, and alarms. It tells you when the database is unhealthy; it is not a database. Reliability of the data itself comes from the database service you choose (here, managed RDS)." },
  ]}
/>

## Global infrastructure, in one paragraph

AWS runs in physical locations called **Regions** (39+ of them worldwide, e.g. `us-east-1`, `eu-west-1`), each an independent geographic area. Inside every Region are multiple **Availability Zones** (AZs) — 120+ in total — which are physically separated groups of data centers with independent power and cooling but joined by low-latency links, so spreading your app across AZs survives a single data center failure. Out at the edge are hundreds of **edge locations** (CloudFront points of presence) that cache content close to users for low latency. You will deepen all of this in the next lesson; for now, hold the hierarchy **Region → Availability Zone → edge**, and the rule that high availability on AWS means running across at least two AZs.

## Why teams choose AWS — and what it costs

Teams pick AWS for two reasons above all. **Breadth**: 200+ services mean you can almost always assemble what you need without inventing it, from a queue to a managed Kubernetes cluster to a machine-learning endpoint. **Elasticity**: you scale up in seconds for a traffic spike and scale down when it passes, paying only for what you used — exactly the property that saved the startup in the hook. Add the maturity of the platform and a deep hiring pool, and AWS is the safe institutional default.

The downsides are equally real and worth naming as a senior engineer would. **Cost complexity**: pay-per-use is a double-edged sword — pricing spans compute hours, storage GB-months, request counts, and *data transfer out* (egress), which surprises teams more than anything else; an idle-but-forgotten resource or a chatty cross-region call can quietly bleed money. **Lock-in**: the more you lean on proprietary managed services (DynamoDB, Lambda, SQS), the harder and costlier it is to leave — the convenience and the lock-in are the same feature. **Operational surface**: AWS hands you enormous power and an enormous number of ways to misconfigure it; IAM alone is a deep discipline, and a single over-permissive policy or public bucket is a breach. None of these are reasons not to use AWS; they are the things you manage deliberately because you chose to use it.

<TradeoffMatrix
  id="01-what-aws-is-tradeoff-1"
  pieceSlug="01-what-aws-is"
  lang="en"
  prompt="A new web service needs a relational database. The team is small and wants to ship features, not operate infrastructure. Which option fits best?"
  options={[
    { name: "RDS (managed relational database): AWS runs patching, backups, and Multi-AZ failover", summary: "Buy the operations.", correct: true, justification: "Right for a small team optimizing for shipping: a managed service removes the recurring toil of patching, backups, replication, and failover, which is exactly the work this team does not want to own. You pay a per-hour premium and give up some low-level knobs, but you trade them for engineering hours spent on the product. This is the standard default, chosen deliberately rather than by reflex." },
    { name: "Postgres self-installed on EC2: a raw VM you patch and operate yourself", summary: "Cheapest per hour, most toil.", justification: "Lowest per-hour price and total control, but it is the self-managed end of the spectrum — you now own OS patches, engine upgrades, backups, replication, and 3 a.m. failover. For a small team that wants to ship features, that operational burden usually costs far more than the per-hour saving. Justified only when you need control RDS does not expose." },
    { name: "DynamoDB (serverless NoSQL): no instance to size, billed per request", summary: "Great, but not relational.", justification: "An excellent low-ops choice when the data model fits, but it is NoSQL — no SQL joins or ad-hoc relational queries — so adopting it for a workload that genuinely needs a relational database means reshaping your data model around access patterns. The requirement here was explicitly relational, so this is the wrong tool, however little it needs operating." },
    { name: "Run nothing on AWS; buy a physical server and host it in the office", summary: "Defeats the point.", justification: "This throws away the entire reason to use a cloud utility: no elasticity, weeks of procurement, your own power/cooling/hardware failures, and capital expense up front. It is the pre-cloud model the platform exists to replace. Reasonable only for narrow compliance or latency edge cases, never as a default for a new web service." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="A typical web application mapped onto AWS service categories: traffic flows from the user through DNS, CDN, and a load balancer to compute, which reads and writes storage, a database, and identity, all observed by CloudWatch"
  hue="var(--d-systems)"
  perRow={4}
  nodes={[
    { id: "user", label: "User", sub: "browser" },
    { id: "dns", label: "Route 53", sub: "DNS" },
    { id: "cdn", label: "CloudFront", sub: "CDN / edge" },
    { id: "lb", label: "ELB", sub: "load balancer" },
    { id: "compute", label: "EC2 / Lambda", sub: "compute" },
    { id: "db", label: "RDS", sub: "database" },
    { id: "s3", label: "S3", sub: "storage" },
    { id: "watch", label: "CloudWatch", sub: "observability" },
  ]}
  edges={[
    { from: "user", to: "dns", label: "resolve" },
    { from: "dns", to: "cdn", label: "route" },
    { from: "cdn", to: "lb", label: "miss" },
    { from: "lb", to: "compute", label: "spread" },
    { from: "compute", to: "db", label: "query" },
    { from: "compute", to: "s3", label: "files" },
    { from: "compute", to: "watch", label: "metrics" },
  ]}
  caption="One service per aisle: compute runs the code, S3 holds files, RDS holds structured data, the VPC/ELB/Route 53/CloudFront stack moves traffic, IAM governs access, and CloudWatch watches it all. Learn the categories first; the product names attach to the problems they solve."
/>

<KeyTakeaway>AWS is a utility-computing platform you rent over an API: 200+ on-demand services billed per use. Learn it by category (compute, storage, database, networking, security, observability) and by the managed-vs-self-managed axis. Its strengths — breadth and elasticity — come paired with cost complexity, lock-in, and a large misconfiguration surface.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-what-aws-is-retrieval"
  lang="en"
  questions={[
    { q: "What does it actually mean to say AWS is a 'utility-computing platform', and why is 'over an API' the key phrase?", a: "It means AWS sells computing the way an electric utility sells power: instead of buying, racking, and operating your own servers, storage, and networking, you rent those capabilities on demand and pay only for what you consume — capacity that appears in seconds and is billed by the second or the request. The phrase 'over an API' is the heart of it because every resource — a virtual server, a bucket, a database, a DNS record, a firewall rule — is created, changed, and destroyed by a programmatic call. The web console is just a front end over that same API. That is what turns infrastructure into code: a script or a Terraform file can stand up a whole environment in minutes, and the same elasticity that absorbs a traffic spike can, run carelessly, spin up a thousand instances and a huge bill just as fast. So AWS is less a product you buy than a metered utility you assemble, programmatically, and are responsible for using well." },
    { q: "Explain the managed-vs-self-managed axis using a database, and tie it to the shared responsibility model.", a: "The same capability — a Postgres database — exists at several points on a spectrum of how much operational work AWS does for you. Self-managed: you run Postgres on EC2, a raw virtual machine, and own everything above the hypervisor — OS patches, engine upgrades, backups, replication, failover, monitoring — for the lowest per-hour price and total control, plus all the 3 a.m. pages. Managed (RDS): AWS runs the engine, patches it, takes automated backups, and fails over to a standby in another Availability Zone; you pay a premium and lose some knobs but shed the toil. Serverless (DynamoDB, Lambda): there is no instance to size at all — you are billed per request and per millisecond. Crucially, 'managed' is not 'no responsibility': under the shared responsibility model AWS secures the cloud (data centers, hypervisor, the service's internals) while you secure what is in the cloud (your IAM policies, data, network rules, code). A patched RDS instance does not save you from a public S3 bucket or an IAM role with wildcard permissions — those stay your responsibility, and most real breaches live exactly there." },
  ]}
/>

<Recap lang="en">
AWS is a global utility-computing platform: launched in 2006, it offers 200+ on-demand services — compute, storage, networking, and managed higher-level services — billed per use and provisioned in seconds over an API, across 190+ countries. The way to hold it in your head is by category — compute (EC2, Lambda), storage (S3), database (RDS, DynamoDB), networking (VPC, ELB, Route 53, CloudFront), security and identity (IAM, KMS), observability (CloudWatch, CloudTrail) — and by the managed-vs-self-managed axis that runs through every category: the same capability is available raw (you operate it), managed (AWS operates the engine), or serverless (no instance at all), and choosing among them is a real tradeoff of control versus operational toil. The global infrastructure stacks as Region → Availability Zone → edge, with high availability meaning at least two AZs. Teams choose AWS for breadth and elasticity and accept its costs: pay-per-use pricing that is easy to overrun (especially data egress), lock-in that grows with every proprietary managed service, a large misconfiguration surface, and a shared responsibility model where securing your data, IAM, and config is always your job. Now when you're evaluating whether to use a managed service or run it yourself, you have the axis and the cost model to make that call deliberately — not by reflex.
</Recap>
