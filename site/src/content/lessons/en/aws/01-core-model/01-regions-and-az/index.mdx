---
concepts:
- regions
- availability-zones
- multi-az-high-availability
- regional-vs-global-services
- data-residency
- SAA-C03:Design Resilient Architectures
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-regions-and-az
sources:
- https://docs.aws.amazon.com/whitepapers/latest/aws-fault-isolation-boundaries/availability-zones-and-regions.html
- https://docs.aws.amazon.com/AmazonRDS/latest/UserGuide/Concepts.MultiAZ.html
- https://aws.amazon.com/about-aws/global-infrastructure/regions_az/
spiral: []
status: ready
summary: AWS infrastructure is a hierarchy of failure-and-latency domains — Regions
  are isolated geographic areas, Availability Zones are independent datacenters inside
  one. Architect across ≥2 AZs or a single-AZ outage takes you down.
title: Regions and availability zones
track: aws
unit: 01-core-model
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The EC2 instances ran in `us-east-1a`. The RDS database ran in `us-east-1a`. The load balancer pointed at `us-east-1a`. It all worked perfectly — until the morning a single availability zone in `us-east-1` lost power. Every layer of the stack was in that one zone, so the whole product went dark while a competitor in the same Region, who had spread across three zones, never dropped a request. The architecture wasn't wrong because it was complex. It was wrong because it had no second failure domain.
</Hook>

<Crux>Pick a Region for latency, law, and price; span ≥2 Availability Zones so one datacenter's failure can't take you down.</Crux>

<Explanation>

## Region, AZ, edge: the failure-and-latency hierarchy

AWS's global footprint is not one cloud. It is a deliberately layered set of isolation boundaries, and your job as an architect is to place workloads on the right boundary for the blast radius and latency you can tolerate.

A **Region** is an independent geographic area — `us-east-1` (N. Virginia), `eu-central-1` (Frankfurt), `ap-southeast-1` (Singapore). Regions are isolated from each other by design: they share no datacenters, and AWS does not automatically replicate your data between them. You choose a Region for three reasons. **Latency** — put compute near your users. **Data residency and law** — GDPR or a banking regulator may forbid customer data from leaving Frankfurt, and because data stays in a Region unless *you* copy it out, choosing `eu-central-1` is itself a compliance control. **Price** — the same instance type can cost noticeably more in some Regions than others.

An **Availability Zone (AZ)** is one or more discrete datacenters *inside* a Region, each with independent power, cooling, and physical networking, placed far enough apart that a fire, flood, or power failure in one is unlikely to hit another — yet close enough (typically within ~100 km / single-digit milliseconds) to keep replication cheap. AWS runs roughly 33 Regions worldwide, and every Region has at least three AZs (most have three to six). The two properties that make AZs the workhorse of resilient design are in tension by design: AZs **fail independently** (the isolation guarantee), yet are joined by **high-bandwidth, low-latency private links** — inter-AZ round trips inside a Region are typically **~1-2 ms**, versus tens-to-hundreds of ms between Regions (e.g. `us-east-1`↔`eu-west-1` is roughly **70-90 ms** round trip). That 1-2 ms link is why you can run a database's synchronous standby in another AZ without a meaningful latency tax, while a cross-Region synchronous standby would pay 70-90 ms on every write.

Below the Region sit two latency-only constructs worth naming. **Edge locations** are the CloudFront CDN points-of-presence — hundreds of them, far more than there are Regions — that cache content close to users. **Local Zones** push a slice of compute and storage into a metro area for ultra-low-latency workloads. Both reduce latency; neither is where your durable state lives.

<FlowDiagram
  label="The AWS isolation hierarchy from a Region down through its independent Availability Zones to the subnets you place inside each AZ."
  hue="var(--d-infra)"
  perRow={3}
  nodes={[
    { id: "region", label: "Region", sub: "eu-central-1", col: 1, row: 0 },
    { id: "az-a", label: "AZ-a", sub: "datacenter set 1", col: 0, row: 1 },
    { id: "az-b", label: "AZ-b", sub: "datacenter set 2", col: 1, row: 1 },
    { id: "az-c", label: "AZ-c", sub: "datacenter set 3", col: 2, row: 1 },
    { id: "sn-a", label: "Subnet a", sub: "10.0.1.0/24", col: 0, row: 2 },
    { id: "sn-b", label: "Subnet b", sub: "10.0.2.0/24", col: 1, row: 2 },
    { id: "sn-c", label: "Subnet c", sub: "10.0.3.0/24", col: 2, row: 2 },
  ]}
  edges={[
    { from: "region", to: "az-a" },
    { from: "region", to: "az-b" },
    { from: "region", to: "az-c" },
    { from: "az-a", to: "sn-a" },
    { from: "az-b", to: "sn-b" },
    { from: "az-c", to: "sn-c" },
  ]}
  caption="A Region contains several independent AZs; a subnet lives in exactly one AZ. Spreading subnets across AZs is how a VPC becomes multi-AZ — the failure domain you build on top of."
/>

## Regional by default, global by exception

Most AWS services are **regional**: when you create an EC2 instance, an RDS database, an SQS queue, or an S3 bucket, it exists in one Region and is managed by that Region's control plane. This has a sharp consequence for resilience — a regional service is only as available as the AZs you spread it across. **You must architect across at least two AZs for high availability**, because a single-AZ deployment inherits the availability of one datacenter, and datacenters fail.

A small set of services are **global**, with one control plane spanning all Regions: **IAM** (your users, roles, and policies are account-wide), **Route 53** (DNS, with health checks that can fail traffic between Regions), and **CloudFront** (the CDN, served from edge locations everywhere). Knowing which bucket a service falls into tells you where its failure domain is and whether you even need to think about multi-Region for it.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Scope</th>
        <th class="border border-border px-3 py-2 text-left">Examples</th>
        <th class="border border-border px-3 py-2 text-left">Failure domain</th>
        <th class="border border-border px-3 py-2 text-left">What you must do</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Zonal</td>
        <td class="border border-border px-3 py-2"><code>EC2</code> instance, EBS volume, single subnet</td>
        <td class="border border-border px-3 py-2">One AZ</td>
        <td class="border border-border px-3 py-2">Replicate the resource into ≥2 AZs yourself</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Regional</td>
        <td class="border border-border px-3 py-2"><code>S3</code>, SQS, DynamoDB, RDS Multi-AZ, ALB</td>
        <td class="border border-border px-3 py-2">Spans AZs in one Region</td>
        <td class="border border-border px-3 py-2">Enable/configure its multi-AZ option; pick the Region for law &amp; latency</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Global</td>
        <td class="border border-border px-3 py-2">IAM, Route&nbsp;53, CloudFront</td>
        <td class="border border-border px-3 py-2">All Regions</td>
        <td class="border border-border px-3 py-2">Nothing for AZ resilience; used to route across Regions</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="en">
Why doesn't AWS just replicate everything across Regions for you? Because cross-Region replication is a tradeoff you must own, not a default. Copying data to a second Region adds **inter-Region network latency** (tens to hundreds of milliseconds, far above the single-digit-ms link between AZs) and **egress cost** (you pay per GB to move data out of a Region). It can also break a compliance posture that depends on data *never* leaving its Region. So AWS keeps your data inside one Region unless you explicitly opt into replication — making "stay in-Region" the safe, cheap, lawful default and "go multi-Region" a deliberate, costed decision for disaster recovery or global latency.
</Inset>

## A concrete multi-AZ design

Resilience stops being abstract the moment you draw the VPC. Take a classic three-tier web service in `eu-central-1` and span it across two AZs (three is better for quorum-based systems):

- **VPC** with one subnet per AZ per tier — say a public subnet in `eu-central-1a` and `eu-central-1b` for the load balancer, and a private subnet in each AZ for app servers and the database.
- **Application Load Balancer** attached to *both* public subnets. An ALB is a regional service that runs nodes in every AZ you enable; it health-checks targets and stops routing to an AZ whose instances are unhealthy.
- **Auto Scaling group** of app servers spread across both private subnets, so capacity survives the loss of one AZ.
- **RDS Multi-AZ**: a primary in `eu-central-1a` with a **synchronous standby** in `eu-central-1b`. AWS replicates writes to the standby and, on a primary failure, automatically fails over by repointing the database's DNS name to the standby — typically within a minute or two, with no committed-data loss because replication was synchronous.

The failure mode this defends against is concrete: if `eu-central-1a` goes dark, the ALB stops sending traffic to its unhealthy targets, the Auto Scaling group still has servers in `eu-central-1b`, and RDS promotes the standby. The product stays up on the surviving AZ. Contrast the single-AZ version from the hook: every tier in one zone means **the AZ outage is a full outage** — there is nothing to fail over to. Quantify the blast radius: a real single-AZ event (the well-documented `us-east-1` zonal failures are the canonical example) takes 100% of a single-AZ stack offline for the duration of the event — minutes to hours — while the cost of avoiding it was the second AZ's standby and the ~$0.01/GB cross-AZ replication traffic, often a few dollars a month on a small fleet. The asymmetry is the whole argument: single-AZ "saves" the price of a standby and buys you a 100%-outage blast radius. This is exactly the resilience that the SAA-C03 "Design Resilient Architectures" domain tests: a passing answer almost always involves spreading a workload across multiple AZs.

<TradeoffMatrix
  id="01-regions-and-az-tradeoff-1"
  lessonSlug="01-regions-and-az"
  lang="en"
  prompt="A regulated EU fintech needs a highly available production database. Customer data must stay in the EU. Which deployment fits best?"
  options={[
    { name: "RDS Multi-AZ in eu-central-1 (primary in one AZ, synchronous standby in another)", summary: "HA inside one EU Region.", correct: true, justification: "This is the standard senior answer. The synchronous standby in a second AZ gives automatic failover with no committed-data loss if the primary's AZ fails, so a single-datacenter outage is survivable. Because both nodes live in eu-central-1, customer data never leaves the EU — the data-residency requirement is satisfied by the Region choice itself. AZs are linked by single-digit-ms private networking, so synchronous replication doesn't impose a latency tax. This meets HA and compliance at the same time." },
    { name: "A single large RDS instance in eu-central-1a with daily snapshots", summary: "One AZ, backups only.", justification: "Compliant on residency but not highly available. Everything sits in one AZ, so that AZ's outage is a full outage, and restoring from a daily snapshot loses up to a day of writes and takes far longer than an automatic failover. Snapshots are disaster recovery, not high availability — they do not protect against the everyday single-AZ failure this question is really about." },
    { name: "RDS primary in eu-central-1 with a synchronous standby in us-east-1", summary: "Cross-Region sync standby.", justification: "Two problems. First, replicating customer data to us-east-1 moves it outside the EU, breaking the residency requirement. Second, synchronous replication across Regions pays tens-to-hundreds of ms of inter-Region latency on every write, crippling throughput — synchronous standbys are an in-Region, cross-AZ pattern, not cross-Region." },
    { name: "EC2 instances running your own database, all in eu-central-1a behind one ALB", summary: "Self-managed, single AZ.", justification: "You take on replication, failover, and patching yourself, and you still placed everything in one AZ — so an AZ outage is a full outage despite the operational burden. The ALB can span AZs, but it has nothing to route to if every database node is in the one zone that failed. This is the single-AZ trap with extra work." },
  ]}
/>

<Quiz
  id="01-regions-and-az-quiz-1"
  lessonSlug="01-regions-and-az"
  lang="en"
  question="Your entire stack — EC2, RDS, and the load balancer — runs in a single Availability Zone. That AZ suffers a power failure. What happens, and what was the design mistake?"
  choices={[
    { label: "The whole application goes down because every tier shared one failure domain; the fix is to spread compute, the database (Multi-AZ), and the load balancer across at least two AZs", correct: true },
    { label: "Nothing — AWS automatically moves your resources to another AZ when one fails", misconception: "AWS does not silently relocate your zonal resources. An EC2 instance or single-AZ RDS lives in one AZ; if that AZ fails, the resource is unavailable. Resilience across AZs is something you architect, not something AWS does for you by default." },
    { label: "Only the database goes down; EC2 and the load balancer keep serving", misconception: "All three tiers were in the same AZ, so all three lose their datacenter at once. There is no surviving tier — the outage is total. Independent failure only helps you if you actually placed resources in different AZs." },
    { label: "Route 53 fails the traffic over to another Region automatically", misconception: "Route 53 can fail over between Regions only if you have built a second Region to fail over to and configured health checks. A single-Region, single-AZ deployment has no standby target, so DNS failover has nowhere to send traffic." },
  ]}
/>

</Explanation>

<KeyTakeaway>Region = isolated geographic area (choose for latency, law, price; data stays in it unless you replicate). AZ = independent datacenter inside a Region, joined by low-latency links. Most services are regional, so span ≥2 AZs or a single-AZ outage is a full outage. IAM, Route 53, CloudFront are global.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-regions-and-az-retrieval"
  lang="en"
  questions={[
    { q: "Why must you architect a regional service across at least two Availability Zones, and what does an RDS Multi-AZ deployment do when its AZ fails?", a: "A regional service like RDS or EC2 ultimately runs in physical datacenters, and an Availability Zone is the failure boundary for those datacenters: each AZ has independent power, cooling, and networking, so one can fail while the others keep running. If you put your whole workload in a single AZ, you inherit the availability of one datacenter — when that AZ has a power or network outage, your resource is simply unavailable and there is nothing to fail over to, which is why the hook's all-in-one-zone stack went fully dark. Spreading across at least two AZs means the loss of one zone leaves a surviving copy. RDS Multi-AZ implements exactly this: it runs a primary in one AZ and a synchronous standby in another, replicating every committed write to the standby. When the primary's AZ fails, RDS automatically fails over by repointing the database's DNS endpoint to the standby — usually within a minute or two — and because replication was synchronous there is no committed-data loss. The AZs are linked by single-digit-millisecond private networking, so synchronous replication across them doesn't impose a meaningful latency penalty, which is why this is an in-Region, cross-AZ pattern rather than cross-Region." },
    { q: "What makes a Region different from an AZ, which AWS services are global rather than regional, and why does data residency follow from the Region choice?", a: "A Region is an independent geographic area such as eu-central-1 or us-east-1; Regions share no datacenters and AWS does not replicate your data between them automatically. An Availability Zone is one or more discrete datacenters inside a single Region, isolated for independent failure but joined to its sibling AZs by high-bandwidth, low-latency private links. So the Region is the coarse boundary you pick for latency, law, and price, and the AZ is the fine boundary you span for high availability. Most services are regional — EC2, RDS, S3, SQS, DynamoDB — meaning they exist in one Region and are only as available as the AZs you spread them across. A small set are global with a single control plane across all Regions: IAM (account-wide users, roles, policies), Route 53 (DNS and cross-Region failover), and CloudFront (the CDN served from edge locations). Data residency follows from the Region choice because your data physically stays inside the Region you created the resource in unless you explicitly replicate it elsewhere — so choosing eu-central-1 and never replicating out is itself a compliance control that keeps EU customer data in the EU, while going multi-Region is a deliberate decision that adds inter-Region latency and per-GB egress cost." },
  ]}
/>

<Recap lang="en">
AWS global infrastructure is a nested set of failure-and-latency domains. A Region is an isolated geographic area — Regions share no datacenters, and your data stays inside one unless you replicate it, which makes the Region choice your lever for latency, law (data residency), and price. Inside each Region are at least three Availability Zones, discrete datacenters with independent power, cooling, and networking but joined by single-digit-millisecond private links, so they fail independently yet support synchronous replication between them. Most AWS services are regional and are only as available as the AZs you spread them across, so high availability means architecting across at least two AZs: subnets per AZ, an ALB spanning them, an Auto Scaling group of app servers in each, and RDS Multi-AZ with a synchronous standby that fails over automatically. A small set of services are global — IAM, Route 53, CloudFront. The failure mode to fear is the single-AZ deployment: when that one zone goes dark, the whole product does, because there is no second failure domain to fall back on. Cross-Region is a separate, deliberate choice that buys disaster recovery and global latency at the price of inter-Region latency and egress cost — exactly the kind of resilient-architecture reasoning SAA-C03 expects.
</Recap>
