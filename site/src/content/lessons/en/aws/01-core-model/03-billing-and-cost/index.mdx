---
concepts:
- pay-per-use
- data-transfer-egress
- ec2-purchase-options
- free-tier
- cost-controls
- CLF-C02:Billing, Pricing, and Support
deepensInto: []
estMin: 15
lang: en
lessonType: topic
level: middle
order: 3
prereqs: []
slug: 03-billing-and-cost
sources:
- https://docs.aws.amazon.com/whitepapers/latest/how-aws-pricing-works/welcome.html
- https://docs.aws.amazon.com/cost-management/latest/userguide/budgets-managing-costs.html
- https://docs.aws.amazon.com/AmazonS3/latest/userguide/aws-pricing.html
- https://docs.aws.amazon.com/AWSEC2/latest/UserGuide/instance-purchasing-options.html
spiral: []
status: ready
summary: AWS bills pay-per-use across compute, storage, and data transfer — and egress
  is the silent killer. Match the workload to On-Demand, Reserved/Savings Plans, or
  Spot, and read the bill as an architecture signal.
title: Billing and cost basics
track: aws
unit: 01-core-model
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
The instance bill was flat all month. Then finance forwarded a screenshot: a single line item, "Data Transfer Out — Internet," had quietly grown to more than the EC2 fleet that produced the traffic. Nobody provisioned it, nobody approved it, and nobody saw it coming — because egress has no dashboard you stare at, no instance you can stop. The compute cost was the part everyone watched. The bill that hurt was the part nobody was looking at.
</Hook>

<Crux>AWS bills pay-per-use; read the bill as an architecture signal — surprises are almost always egress, idle capacity, or a NAT Gateway.</Crux>

<Explanation>

## Pay-per-use and the three cost dimensions

There is no flat AWS subscription. You pay for what you provision and consume, metered per service, and the total is the sum of three recurring dimensions that show up on almost every bill.

**Compute time.** An EC2 instance, a Lambda invocation, a Fargate task — you pay per second or per hour that capacity exists, whether or not it is doing useful work. This is the trap of *idle provisioned capacity*: a stopped EC2 instance costs nothing for compute, but a *running* one you forgot about bills 24/7. Provisioned-but-idle is the most common avoidable line item.

**Storage (GB-month).** A gigabyte stored for a month is the unit. S3 Standard runs roughly **$0.023 per GB-month** in common regions (and is designed for **11 nines** — 99.999999999% — of durability, so the cost buys near-zero data-loss risk); EBS volumes, snapshots, and RDS storage are billed the same way. Storage also meters *requests*, not just bytes: S3 charges around **$0.0004 per 1,000 GET** and **$0.005 per 1,000 PUT** requests, so a workload making millions of tiny object reads can run up a request bill that rivals the byte storage itself. Storage is usually the *predictable* part of the bill — it grows slowly and you can see it coming. The catch is that storage you stop using keeps billing until you delete it: orphaned EBS volumes from terminated instances and forgotten snapshots accrue silently.

**Data transfer (egress).** This is the dimension that ambushes teams. The rule of thumb: **ingress (data into AWS) is generally free; egress (data out to the internet) is not** — commonly around **$0.09 per GB** for the first tier, varying by region and volume. Worse, transfer *inside* AWS is not free either: traffic crossing Availability Zones typically costs **~$0.01/GB each way** (so ~$0.02/GB for a round trip between two services in different AZs), and cross-Region transfer runs **~$0.02/GB**. A chatty microservice mesh sprawled across AZs, or a database replicating across Regions, can run up data-transfer charges that dwarf the compute generating them. Put numbers on it: a service pushing **50 TB/month** of cross-AZ chatter at ~$0.02/GB round-trip is **~$1,000/month** you can erase just by pinning the two services into the same AZ — pure architecture, zero feature change.

<Inset kind="why" lang="en">
Why is egress the silent killer and ingress free? AWS *wants* your data in — ingress is the on-ramp to services you'll then pay to run. Getting data *out* is where they monetize, and where your architecture leaks money you can't see on any instance dashboard. The NAT Gateway is the classic example: every private-subnet instance reaching the internet routes through it, and you pay both an **hourly charge (~$0.045/hr, ~$33/month just to exist)** *and* a **per-GB data-processing charge (~$0.045/GB)** on everything that flows through — on top of the egress cost if that traffic then leaves to the internet. Concretely: a fleet pulling **100 TB/month** of container images and package downloads through a NAT Gateway is roughly **$4,500/month in NAT processing alone** — a charge that appears nowhere on any instance dashboard and is often fixed for pennies by adding a VPC Gateway Endpoint for S3 (free) so that traffic never touches the NAT. Prices quoted here are illustrative and vary by region and time — always confirm against the live pricing page.
</Inset>

## EC2 purchase options: the price/commitment/interruption tradeoff

The same EC2 instance can cost wildly different amounts depending on *how* you buy it. There are three buckets, trading price against commitment and reliability.

**On-Demand** is the default: no commitment, pay the full rate per second/hour, start and stop whenever. It is the most expensive per hour and the most flexible — right for spiky, unpredictable, or short-lived workloads where you can't forecast usage.

**Reserved Instances / Savings Plans** are a commitment for a discount. You promise 1 or 3 years of usage (Savings Plans commit to a dollars-per-hour spend; Reserved Instances to a specific instance configuration) and AWS discounts the rate — often **up to ~72% off On-Demand**. Right for steady, baseline, always-on workloads whose floor you can predict a year out. The risk is the commitment itself: you pay even if usage drops.

**Spot** sells AWS's spare capacity at up to **~90% off On-Demand** — but AWS can reclaim it with a two-minute warning whenever it needs the capacity back. Right only for fault-tolerant, interruptible work: batch jobs, CI runners, stateless workers, big-data processing that checkpoints. Wrong for anything that can't survive sudden termination.

Real fleets blend all three: Reserved/Savings Plans cover the predictable baseline, On-Demand absorbs the variable middle, and Spot soaks up bursty interruptible load at a steep discount.

<TradeoffMatrix
  id="aws-03-billing-tradeoff-1"
  lessonSlug="03-billing-and-cost"
  lang="en"
  prompt="You run a stateless batch pipeline (image processing) that can resume from checkpoints and has no deadline. Which purchase option minimizes cost?"
  options={[
    { name: "Spot Instances — spare capacity at up to ~90% off, reclaimable on 2-minute notice", summary: "Cheapest, interruptible.", correct: true, justification: "This is the textbook Spot workload: stateless, checkpoint-able, and deadline-free, so a two-minute reclaim warning costs you only the work since the last checkpoint, which resumes elsewhere. You capture the deepest discount AWS offers — up to ~90% off On-Demand — precisely because the work tolerates the one downside Spot has, interruption. Pair it with multiple instance types/AZs so the Spot pool rarely runs dry." },
    { name: "On-Demand — full rate, no commitment, never reclaimed", summary: "Flexible but priciest.", justification: "On-Demand is the right default for spiky or short-lived work you can't forecast, but here you'd pay the highest per-hour rate for a workload that explicitly tolerates interruption. You're buying reliability the job doesn't need. Use On-Demand for the unpredictable middle of a fleet, not for fault-tolerant batch." },
    { name: "3-year Reserved Instances — up to ~72% off for a 1- or 3-year commitment", summary: "Cheap if always-on.", justification: "Reserved/Savings Plans win for a steady, always-on baseline you can predict a year out. A bursty batch pipeline that runs intermittently doesn't fit a 24/7 commitment — you'd pay for reserved hours the job isn't using. The discount is real but the commitment is mismatched to interruptible, intermittent load." },
    { name: "On-Demand on the largest instance type to finish faster", summary: "Brute force.", justification: "Bigger instances finish sooner but cost proportionally more per hour, so for an embarrassingly parallel, deadline-free batch the total compute-seconds barely change while you pay the premium On-Demand rate. Throwing a large On-Demand box at it optimizes for speed you don't need and ignores the ~90% Spot discount the workload qualifies for." },
  ]}
/>

## The Free Tier and its traps

New accounts get three flavors of free, and conflating them is how the trap springs. **12-month free** offerings (e.g., 750 hours/month of a `t2.micro`/`t3.micro`, 5 GB of S3) expire one year after signup — then bill at full rate, often unnoticed. **Always-free** offerings (e.g., 1M Lambda requests/month, a slice of DynamoDB) persist but have hard caps. **Trials** are short-term (30–90 days) per-service freebies.

The traps are structural. Free-tier limits are *per category*, not a blanket "free account" — exceed 750 instance-hours by running two instances, or spill past the S3 cap, and you're billed on the overage immediately. The 12-month clock is the big one: teams stand up a "free" demo, forget it, and twelve months later it silently converts to a paying instance running 24/7. And the Free Tier covers the *service* but rarely the *data transfer* — your free instance can still generate billable egress. Treat the Free Tier as a metered allowance with an expiry, not as "AWS is free for a year."

## Cost controls: make the bill observable before it surprises you

You cannot manage what you can't see, and AWS bills after the fact — so the entire game is making spend observable *early*. Four tools form the baseline.

**AWS Budgets** lets you set a spend (or usage) threshold and get alerted — by email or SNS — when actual or *forecasted* cost crosses it. This is your tripwire; a forecasted-overage alert catches a runaway egress charge mid-month instead of in the next invoice. **Cost Explorer** is the analysis surface: it breaks spend down by service, region, and time so you can answer "what grew, and when." **Cost-allocation tags** let you attribute spend to teams, projects, or environments — tag everything (`team`, `env`, `service`) and the bill becomes a per-owner ledger instead of one opaque number. The classic **billing alarm** (a CloudWatch alarm on `EstimatedCharges`) is the simplest first line of defense for a personal or small account.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Dimension</th>
        <th class="border border-border px-3 py-2 text-left">On-Demand</th>
        <th class="border border-border px-3 py-2 text-left">Reserved / Savings Plans</th>
        <th class="border border-border px-3 py-2 text-left">Spot</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Discount vs On-Demand</td>
        <td class="border border-border px-3 py-2">baseline (0%)</td>
        <td class="border border-border px-3 py-2">up to ~72%</td>
        <td class="border border-border px-3 py-2">up to ~90%</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Commitment</td>
        <td class="border border-border px-3 py-2">none</td>
        <td class="border border-border px-3 py-2">1 or 3 years</td>
        <td class="border border-border px-3 py-2">none</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Interruption risk</td>
        <td class="border border-border px-3 py-2">none</td>
        <td class="border border-border px-3 py-2">none</td>
        <td class="border border-border px-3 py-2">reclaimed on 2-min notice</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Best-fit workload</td>
        <td class="border border-border px-3 py-2">spiky, unpredictable, short-lived</td>
        <td class="border border-border px-3 py-2">steady always-on baseline</td>
        <td class="border border-border px-3 py-2">fault-tolerant, interruptible batch</td>
      </tr>
    </tbody>
  </table>
</div>

<Quiz
  id="aws-03-billing-quiz-1"
  lessonSlug="03-billing-and-cost"
  lang="en"
  question="An app's EC2 cost is flat, but the monthly bill jumped sharply. Where do you look first?"
  choices={[
    { label: "Data transfer — egress to the internet, plus cross-AZ/cross-Region traffic and a NAT Gateway, is the dimension that grows invisibly without any instance change", correct: true },
    { label: "Storage — S3 must have doubled in price", misconception: "Storage (~$0.023/GB-month) grows slowly and predictably, and AWS rarely raises unit prices. A sharp jump with flat compute points at data transfer, not at a storage price change." },
    { label: "Compute — a Reserved Instance discount must have expired", misconception: "If compute were the cause the EC2 line item wouldn't be flat. The premise says it is flat, so the growth is in another dimension — almost always egress or NAT Gateway data processing." },
    { label: "Nothing to investigate — bills naturally fluctuate month to month", misconception: "A sharp jump with no infrastructure change is a signal, not noise. The bill is an architecture signal; unexplained growth usually means egress, idle capacity, or a NAT Gateway quietly processing terabytes." },
  ]}
/>

<Quiz
  id="aws-03-billing-quiz-2"
  lessonSlug="03-billing-and-cost"
  lang="en"
  question="You want to be warned mid-month if spend is trending past your limit, before the invoice arrives. Which tool?"
  choices={[
    { label: "AWS Budgets with a forecasted-cost alert — it fires when actual or projected spend crosses a threshold, catching a runaway charge during the month", correct: true },
    { label: "Cost Explorer, refreshed daily by hand", misconception: "Cost Explorer is for after-the-fact analysis — breaking down what grew and when. It has no alerting; you'd have to remember to look. Budgets is the proactive tripwire that pushes an alert to you." },
    { label: "Cost-allocation tags", misconception: "Tags attribute spend to teams/projects so the bill is a per-owner ledger, but tagging alone doesn't notify you of an overage. Pair tags with a Budget to get alerted per owner." },
    { label: "Wait for the monthly invoice and reconcile", misconception: "The invoice arrives after the money is spent — exactly the surprise you're trying to avoid. The whole point of Budgets is to make spend observable before the bill lands." },
  ]}
/>

</Explanation>

<StackDiagram
  label="AWS bill broken into its recurring cost dimensions, from the steady predictable storage layer up to the volatile data-transfer layer that causes surprise bills"
  hue="var(--d-systems)"
  layers={[
    { label: "Data transfer (egress)", note: "~$0.09/GB out; cross-AZ ~$0.01/GB; NAT GW — the silent killer" },
    { label: "Compute time", note: "per second/hour; idle running capacity bills 24/7" },
    { label: "Storage (GB-month)", note: "S3 ~$0.023/GB-mo; predictable, but orphans linger" },
    { label: "Purchase model", note: "On-Demand / Reserved / Spot sets the compute rate" },
  ]}
  caption="Read the bill bottom-up: the purchase model sets your compute rate, storage grows predictably, but the surprise almost always lives in the top layer — egress, cross-AZ traffic, or a NAT Gateway no dashboard shows you."
/>

<KeyTakeaway>AWS is pay-per-use across compute, storage, and data transfer. Egress is the silent killer (ingress is free); match workloads to On-Demand / Reserved / Spot by their price-commitment-interruption tradeoff; the Free Tier is a metered allowance that expires; and AWS Budgets, Cost Explorer, tags, and a billing alarm make spend observable before the invoice does.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="aws-03-billing-retrieval"
  lang="en"
  questions={[
    { q: "EC2 cost is flat but the bill spiked. Explain the most likely cause and why no instance dashboard shows it.", a: "The most likely cause is data transfer, specifically egress and internal cross-zone traffic, not compute. AWS meters three recurring dimensions — compute time, storage GB-month, and data transfer — and only the first two map cleanly to a resource you can watch and stop. Egress (data leaving AWS to the internet, ~$0.09/GB) is billed by volume with no instance to throttle, ingress is generally free so it lulls you, and traffic crossing Availability Zones (~$0.01/GB each way) or Regions adds up invisibly as a chatty service mesh scales. The classic culprit is a NAT Gateway: every private-subnet instance reaching the internet routes through it, and you pay both an hourly charge and a per-GB data-processing charge on everything flowing through, so terabytes turn into a large line item that looks like nothing in the console. Because none of this attaches to a single instance you can stop, it has no dashboard you naturally stare at — which is exactly why it surprises teams. You find it in Cost Explorer (break down by 'Data Transfer Out' and by service) and you catch it next time with a forecasted-cost AWS Budget." },
    { q: "When do you pick On-Demand vs Reserved/Savings Plans vs Spot, and what is the tradeoff axis?", a: "The axis is price versus commitment versus interruption tolerance. On-Demand is full rate, zero commitment, never reclaimed — pick it for spiky, unpredictable, or short-lived workloads where you can't forecast usage and need the work to just run. Reserved Instances / Savings Plans trade a 1- or 3-year commitment for up to ~72% off — pick them for the steady, always-on baseline you can predict a year out, accepting that you pay for the commitment even if usage drops. Spot sells spare capacity at up to ~90% off but AWS can reclaim it on two minutes' notice — pick it only for fault-tolerant, interruptible work like batch jobs, CI runners, and stateless or checkpointing workers, never for anything that can't survive sudden termination. Mature fleets blend all three: Reserved/Savings Plans cover the predictable floor, On-Demand absorbs the variable middle, and Spot soaks up bursty interruptible load at the deepest discount. The mistake is paying On-Demand rates for a predictable baseline (overpaying) or putting a stateful, deadline-bound service on Spot (under-buying reliability)." },
  ]}
/>

<Recap lang="en">
AWS billing is pay-per-use with no flat subscription, and almost every bill is the sum of three dimensions: compute time (you pay for capacity that exists, so idle running instances are the most common avoidable cost), storage measured in GB-month (S3 Standard around $0.023/GB-month, predictable but orphaned volumes and snapshots linger), and data transfer — the dimension that ambushes teams, because ingress is generally free while egress to the internet runs around $0.09/GB, cross-AZ traffic costs roughly $0.01/GB each way, cross-Region costs more, and a NAT Gateway charges both hourly and per GB processed. The same EC2 instance costs very differently by purchase model: On-Demand for spiky unpredictable work, Reserved Instances or Savings Plans for a steady baseline at up to ~72% off in exchange for a 1- or 3-year commitment, and Spot for fault-tolerant interruptible batch at up to ~90% off with a two-minute reclaim risk. The Free Tier is three things — 12-month, always-free, and trials — and its traps are per-category limits, the 12-month expiry that silently converts demos to paying resources, and the fact that it rarely covers data transfer. Make spend observable before the invoice does: AWS Budgets for forecasted-cost alerts, Cost Explorer for breaking down what grew, cost-allocation tags so the bill is a per-owner ledger, and a billing alarm as the simplest tripwire. Above all, read the bill as an architecture signal — surprise charges are almost always egress, idle provisioned capacity, or a NAT Gateway nobody was watching. (All prices are illustrative and vary by region and time; confirm against the live AWS pricing page.)
</Recap>
