---
concepts:
- reverse-proxy
- load-balancer
- backend-pool
- health-check
- two-connections
deepensInto:
- 02-balancing-algorithms
- 03-l4-vs-l7-balancing
estMin: 8
lang: en
lessonType: topic
level: junior
order: 1
prereqs:
- 03-tcp-handshake/01-the-three-way-handshake
- 06-http-versions/01-http-fundamentals
- 07-cdn-edge/01-cdn-what-and-why
slug: 01-what-reverse-proxies-do
sources:
- https://www.haproxy.com/blog/layer-4-vs-layer-7-load-balancing/
- https://www.nginx.com/resources/glossary/reverse-proxy-server/
- https://www.envoyproxy.io/docs/envoy/latest/intro/what_is_envoy
spiral:
- latency
- statefulness
status: ready
summary: A reverse proxy sits invisibly between clients and backends — it completes
  the client connection, picks a backend, and forwards the request, decoupling scaling
  from client visibility.
title: What reverse proxies do
track: networking
unit: 09-proxy-load-balancing
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
You click a link and connect to what feels like a single server — but behind that address, dozens of machines are waiting to handle your request. Something invisible decided which one served you, and you will never know which it was.
</Hook>

<Crux>A reverse proxy intercepts every client connection, picks a backend, and forwards the request — clients never know the real servers exist.</Crux>

<Explanation>

## The hidden intermediary

A **reverse proxy** sits between clients and a pool of backend servers. Clients connect to the proxy believing it is the real server. Understanding this sequence is what lets you reason about every routing, security, and observability decision that follows. The proxy:

1. Completes the TCP (and TLS) handshake with the client.
2. Picks a backend from its pool.
3. Opens a **separate** TCP connection to that backend.
4. Forwards the client's request to the backend.
5. Waits for the backend's response and relays it back to the client.

The client never sees the backend's IP address or knows how many backends exist. This is the defining property: **the client talks to one address; the proxy talks to many.**

## Why two connections instead of packet-forwarding

A dumb packet forwarder (layer 3 NAT) passes packets unchanged — it cannot inspect or modify the payload. A reverse proxy that routes on HTTP must **parse the request** to decide where it goes. Parsing requires owning both sides of the conversation: the client-side TCP connection (to read the request) and the backend-side TCP connection (to write it out).

This two-connection model is what makes the proxy both powerful and visible to each side. The backend sees the proxy's IP, not the client's — a concern addressed in the next lesson.

## The restaurant metaphor

A restaurant with one **host** (the load balancer) and multiple **chefs** (the backends). When a customer (client) arrives the host decides which chef is least busy, passes the order, and returns the dish to the customer. Customers never talk to chefs. If a chef calls in sick, the host stops sending orders to that station — seamlessly.

<NumbersCard
  id="card-lb-basics"
  title="Reverse proxy basics"
  data-lesson-visual
  stats={[
    { label: "Client connections seen by client", value: "1 (to the proxy IP)" },
    { label: "TCP connections per request (L7 proxy)", value: "2 (client→proxy + proxy→backend)" },
    { label: "Backends visible to the client", value: "0 (proxy hides them all)" },
    { label: "Health check interval (typical)", value: "10–30 s" },
    { label: "Backends a single nginx can front", value: "hundreds" },
    { label: "Deployment zero-downtime: drain + swap", value: "yes, via health check + draining" },
  ]}
/>

## What happens when a backend crashes

The proxy's health-check loop probes every backend on a fixed interval (10–30 s). After 2–3 consecutive failures, the backend is marked **unhealthy** and new requests are no longer routed to it. In-flight requests on the still-open connection either complete or timeout. When the backend recovers, it is added back after 2–3 consecutive successes.

This means:
- Clients are routed only to **healthy** backends.
- A backend crash is invisible to new clients within one health-check interval.
- Active connections to the crashed backend still fail — the proxy cannot resurrect a TCP connection that died.

<Inset kind="why" lang="en">
**Why a forward proxy is different.** A forward proxy (like a corporate web filter) sits on the *outbound* path: the client knows about it and connects to it intentionally. A reverse proxy sits on the *inbound* path: the client is unaware of it and thinks it is talking to the origin server. Forward proxies enforce client policy; reverse proxies enforce server-side scaling and resilience.
</Inset>

<Quiz
  id="lb-jr-quiz-1"
  pieceSlug="01-what-reverse-proxies-do"
  lang="en"
  question="Why does a reverse proxy open TWO separate TCP connections instead of forwarding packets directly to the backend?"
  choices={[
    { label: "To encrypt traffic twice", misconception: "TLS is a separate layer; the proxy architecture is not about double encryption." },
    { label: "Because it must parse the HTTP request to decide which backend to route to, which requires owning both connections", correct: true },
    { label: "To make the traffic harder to intercept", misconception: "That is TLS's job, not proxy architecture." },
    { label: "It does not — reverse proxies forward packets at layer 3", misconception: "Layer-3 forwarding cannot inspect HTTP. An HTTP-aware proxy requires two TCP connections." },
  ]}
/>

<Quiz
  id="lb-jr-quiz-2"
  pieceSlug="01-what-reverse-proxies-do"
  lang="en"
  question="A backend crashes mid-request. What does the load balancer do for the NEXT client request that arrives?"
  choices={[
    { label: "It routes to the crashed backend anyway and hopes it recovers", misconception: "Health checks detect the crash and remove the backend from the pool." },
    { label: "It routes to a healthy backend from the pool, because health checks have already (or will soon) remove the crashed backend", correct: true },
    { label: "It returns 200 OK with a cached response from memory", misconception: "The LB is not a cache; it routes requests to backends." },
    { label: "It tears down all existing connections to all backends immediately", misconception: "Only the connection to the crashed backend is affected." },
  ]}
/>

<DragOrder
  id="lb-jr-drag-1"
  pieceSlug="01-what-reverse-proxies-do"
  lang="en"
  prompt="Order the sequence when a client makes a request through a load balancer:"
  items={[
    "Client establishes TCP connection to the load balancer",
    "Client sends HTTP request to the load balancer",
    "Load balancer picks a healthy backend",
    "Load balancer opens a TCP connection to that backend",
    "Load balancer forwards the client's request to the backend",
    "Backend processes the request and sends a response",
    "Load balancer forwards the response back to the client",
  ]}
/>

<MetaphorComplete
  id="lb-jr-metaphor-1"
  pieceSlug="01-what-reverse-proxies-do"
  lang="en"
  setup="A load balancer is like a restaurant _______ who seats guests and directs orders to the kitchen."
  accepted={["host", "hostess", "maître d'", "receptionist", "manager"]}
  canonical="host (any role that directs traffic)"
  explanation="The load balancer, like a restaurant host, receives all incoming requests and routes them to available backends based on current load, hiding the kitchen from customers entirely."
/>

</Explanation>

<SequenceDiagram
  label="A reverse proxy holds two TCP connections per request: the client connects to the proxy, the proxy opens a separate connection to a backend, forwards the request, and relays the backend's response back to the client."
  hue="var(--d-network)"
  actors={["Client", "Reverse proxy", "Backend"]}
  messages={[
    { from: "Client", to: "Reverse proxy", label: "TCP+TLS, then HTTP request" },
    { from: "Reverse proxy", to: "Backend", label: "separate TCP conn + request" },
    { from: "Backend", to: "Reverse proxy", label: "HTTP response" },
    { from: "Reverse proxy", to: "Client", label: "relays response" },
  ]}
  caption="The proxy owns both sides: one connection to the client (to read the request) and a separate connection to a chosen backend (to forward it). The client sees only the proxy's address; the backend sees only the proxy's IP."
/>

<KeyTakeaway>A reverse proxy opens two TCP connections per request — one to the client, one to a backend — giving it the power to route, health-check, and hide the backend pool. The client sees one IP; the proxy handles the rest.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-what-reverse-proxies-do-retrieval"
  lang="en"
  questions={[
    { q: "Why does a load balancer need to detect backend health?", a: "If a backend crashes or becomes overloaded, the load balancer must stop routing new requests to it and send them to healthier backends instead. Without health detection the LB would send requests to dead backends and clients would see failures." },
    { q: "What is the key difference between a forward proxy and a reverse proxy?", a: "A forward proxy is on the client side: the client knows about it and uses it intentionally for outbound access control or privacy. A reverse proxy is on the server side: the client is unaware of it and thinks it is talking directly to the origin server." },
    { q: "Why does a reverse proxy open two TCP connections per request rather than forwarding packets at layer 3?", a: "To route by HTTP path, hostname, or header, the proxy must parse the request — which requires owning both the client-side connection (to read the request) and the backend-side connection (to forward it). Layer-3 forwarding passes raw packets without parsing HTTP." },
  ]}
/>

<Recap lang="en">
A reverse proxy acts as the public face of an entire backend pool: clients connect to one IP, the proxy picks a healthy backend, opens its own connection to that backend, and shuttles traffic between the two. Two TCP connections per request are the price of being able to parse and route HTTP. Health checks run in the background every 10–30 seconds; after 2–3 failures a backend is removed from the pool and traffic is immediately redistributed. This decoupling — clients talk to one address, backends are swapped freely — is what makes zero-downtime deployments and backend scaling invisible to users. Now when you see a traffic spike disappear after adding a machine, or a deployment roll out without a single user-visible error, you will know the reverse proxy is doing exactly what you just learned.
</Recap>
