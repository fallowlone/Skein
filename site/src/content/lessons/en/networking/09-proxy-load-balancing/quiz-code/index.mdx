---
concepts:
- nginx-upstream-config
- power-of-two-choices
- proxy-protocol
- connection-draining
- circuit-breaker-config
estMin: 14
lang: en
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://www.envoyproxy.io/docs/envoy/latest/intro/arch_overview/upstream/circuit_breaking
- https://www.haproxy.com/blog/layer-4-vs-layer-7-load-balancing/
- https://www.rfc-editor.org/rfc/rfc7239.html
status: ready
summary: "Read real LB configs, a balancing snippet, a PROXY-protocol header, and a graceful-shutdown handler — then pick the behaviour or the highest-leverage fix."
title: 'Proxy and load balancing: config and code reading'
track: networking
unit: 09-proxy-load-balancing
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
LB behaviour is decided in config files and shutdown handlers, not in slide diagrams. Read each snippet, predict what it does under load, and choose the fix a senior engineer would make first.
</Hook>

<Goal lang="en">
Practise the loop you run on every LB incident: read the upstream config or the handler, predict the failure mode, and reach for the change that actually fixes it.
</Goal>

## Snippet 1 — the nginx upstream block

```nginx
upstream api {
    server 10.0.0.1:8080;
    server 10.0.0.2:8080;
    server 10.0.0.3:8080;
}

server {
    location / {
        proxy_pass http://api;
    }
}
```

<Quiz
  id="09-proxy-load-balancing-code-1"
  lessonSlug="09-proxy-load-balancing"
  lang="en"
  question="Request cost varies widely and one backend periodically GC-pauses. With this default config, what happens, and what is the highest-leverage one-line change?"
  choices={[
    { label: "Default nginx upstream is round-robin: it keeps sending the paused backend its fair share regardless of load, so it falls further behind. Add `least_conn;` (nginx's load-aware policy) so new requests favour the less-loaded backends.", correct: true },
    { label: "Nothing changes — nginx defaults to least-connections and already routes around the slow node.", misconception: "The nginx default is round-robin, not least_conn. With no directive it is blind to load and keeps feeding the paused backend." },
    { label: "The config is invalid because no balancing algorithm is declared.", misconception: "The block is valid; omitting the directive simply selects the round-robin default. The problem is the default being load-blind, not a syntax error." },
    { label: "Add a fourth backend — more capacity is the only real fix for a slow node.", misconception: "Adding capacity does not stop round-robin from routing into the paused node. The fix is a load-aware policy, not raw capacity." },
  ]}
/>

## Snippet 2 — power-of-two-choices, by hand

```python
import random

def pick_backend(backends):
    a, b = random.sample(backends, 2)        # two distinct, uniform at random
    return a if a.active_conns <= b.active_conns else b
```

<Quiz
  id="09-proxy-load-balancing-code-2"
  lessonSlug="09-proxy-load-balancing"
  lang="en"
  question="This is the core of power-of-two-choices. Why does sampling exactly two — rather than scanning all N for the true minimum — matter in production?"
  choices={[
    { label: "It is O(1) instead of O(N), and the per-request randomness means different clients sample different pairs — so when a backend recovers they do not all surge to it at once (no synchronized herd).", correct: true },
    { label: "It is just least-connections with a random tiebreak; behaviour is identical.", misconception: "True least-connections scans all N for the global minimum, which is O(N) and synchronizes every client onto the same recovered node. Sampling two breaks both." },
    { label: "Sampling two is a CPU optimization only; load distribution is worse than scanning all N.", misconception: "Sampling two is not just cheaper — it achieves ~12% imbalance versus ~100% for pure random, and it desynchronizes the herd, which scanning all N does not." },
    { label: "It must use random.sample with replacement so a backend can be compared against itself.", misconception: "It samples two DISTINCT backends without replacement; comparing a backend to itself would be a no-op and defeat the comparison step." },
  ]}
/>

## Snippet 3 — the PROXY protocol header

```
PROXY TCP4 203.0.113.195 198.51.100.7 56324 443\r\n
GET /orders HTTP/1.1
Host: shop.example.com
```

<Quiz
  id="09-proxy-load-balancing-code-3"
  lessonSlug="09-proxy-load-balancing"
  lang="en"
  question="A raw-TCP backend (not HTTP) needs the real client IP for rate limiting. The LB prepends the line above. Which statement is correct?"
  choices={[
    { label: "The PROXY line is inserted at the transport layer before any application bytes, so it works for any TCP/UDP protocol and is harder to spoof than X-Forwarded-For — a client cannot easily get its forged line to arrive before the trusted proxy's.", correct: true },
    { label: "This is the same as X-Forwarded-For; both are HTTP headers the backend parses identically.", misconception: "X-Forwarded-For is an HTTP header (application layer); PROXY protocol is a transport-layer preamble that works even for non-HTTP protocols like PostgreSQL or MQTT." },
    { label: "203.0.113.195 is the LB's IP and 198.51.100.7 is the client's.", misconception: "The format is `PROXY <proto> <src> <dst> <sport> <dport>`: 203.0.113.195 is the source (client) and 198.51.100.7 is the destination the proxy connected on." },
    { label: "The backend can trust this even on a directly reachable port, since the header is cryptographically signed.", misconception: "The PROXY header is not signed. The backend must only accept it from known trusted-proxy source IPs; a directly reachable backend can still be fed a forged line." },
  ]}
/>

## Snippet 4 — graceful shutdown on SIGTERM

```go
func main() {
    srv := &http.Server{Addr: ":8080", Handler: mux}
    go srv.ListenAndServe()

    <-sigterm                                  // LB has marked us draining
    ctx, cancel := context.WithTimeout(context.Background(), 25*time.Second)
    defer cancel()
    srv.Shutdown(ctx)                          // stop accepting, finish in-flight
}
```

<Quiz
  id="09-proxy-load-balancing-code-4"
  lessonSlug="09-proxy-load-balancing"
  lang="en"
  question="This handler cooperates with connection draining. What does srv.Shutdown do, and what is the one production gap to watch?"
  choices={[
    { label: "It closes the listening socket (no new connections) and lets in-flight requests finish within the 25 s timeout. The gap: long-lived WebSocket/SSE streams may exceed 25 s, so the LB drain timeout AND this context must both be sized for the longest connection.", correct: true },
    { label: "It force-closes all connections immediately, so clients get a reset — draining requires a separate library.", misconception: "Shutdown is graceful: it stops accepting and waits for in-flight requests up to the timeout. Force-close is what you AVOID by calling it." },
    { label: "The 25 s timeout is irrelevant because the LB controls drain timing entirely.", misconception: "Both matter. If the app exits before the LB stops routing, clients get resets; if the LB drain timeout is shorter than this context, the LB force-closes mid-request anyway. They must be aligned." },
    { label: "Shutdown blocks forever until every connection closes, so the timeout is unnecessary.", misconception: "Shutdown respects the context: at 25 s it returns and remaining connections are force-closed. Without the timeout a stuck stream would hang the deploy indefinitely." },
  ]}
/>

<Recap lang="en">
LB problems live in config and handlers. The nginx default is round-robin, so a load-aware policy like least_conn (or P2C upstream) is the fix for a load-blind pool. Power-of-two-choices is two random samples plus one comparison — O(1) and herd-desynchronizing, not merely a cheaper least-connections. The PROXY protocol preamble restores the client IP for any transport-layer protocol but must be trusted only from known proxy IPs. And graceful shutdown (close-listener, drain in-flight, timeout) only works when the app's drain window and the LB's drain timeout are sized together for the longest-lived connection.
</Recap>
