---
concepts:
- round-robin
- weighted-round-robin
- least-connections
- power-of-two-choices
- herding-problem
- load-imbalance
deepensInto:
- 06-retry-storms-and-circuit-breakers
- 07-resilient-lb-architecture
estMin: 12
lang: en
lessonType: topic
level: middle
order: 2
prereqs:
- 09-proxy-load-balancing/01-what-reverse-proxies-do
slug: 02-balancing-algorithms
sources:
- https://www.nginx.com/blog/nginx-power-of-two-choices-load-balancing-algorithm/
- https://www.envoyproxy.io/docs/envoy/latest/intro/arch_overview/upstream/load_balancing/load_balancers.html
- https://www.haproxy.com/blog/layer-4-vs-layer-7-load-balancing/
spiral:
- latency
status: ready
summary: Round-robin ignores real load; least-connections causes herding; power-of-two-choices
  is O(1) and ~12% imbalance — the modern default and why.
title: 'Balancing algorithms: round-robin to power-of-two-choices'
track: networking
unit: 09-proxy-load-balancing
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import loadImbalance from "./infographics/load-imbalance.svg?raw";

<Hook>
You have 4 backend servers. Requests should be distributed evenly. Round-robin does exactly that — until one server slows down and still gets its turn. Every choice of algorithm has a cost. The wrong one turns a slow backend into an avalanche.
</Hook>

<Crux>Round-robin is blind to load; least-connections causes synchronized herding; power-of-two-choices beats both in O(1) with ~12% imbalance.</Crux>

<Explanation>

## Round-robin: simple but load-blind

The LB maintains a rotating pointer over the backend list: B1, B2, B3, B4, B1, B2, …. Each request advances the pointer one step. Why does this matter to you? Because the algorithm you ship determines which failures you will debug at 2 a.m. — blind request-count distribution or synchronized herding.

**Properties:**
- O(1) per routing decision.
- Distributes *request count* evenly, not *actual load*.
- A 1 ms API call and a 100 ms file upload both count as "one request."

**When it fails:** If one backend is slower (GC pause, heavier queries), it accumulates open connections faster than round-robin can detect. The LB keeps sending it its fair share of requests; the slow backend falls further behind.

**Weighted round-robin** assigns each backend a weight proportional to capacity. B1 (weight 2) receives twice as many requests as B2 (weight 1). Weights must be set manually and do not adapt to real-time load changes.

## Least-connections: adapts but herds

Route each new request to the backend with the **fewest active connections** at that instant. This directly accounts for slow backends: a backend processing long requests accumulates more connections and receives fewer new ones.

**The herding problem.** Suppose B1 gets overloaded, accumulates 1 000 connections, and the LB stops routing to it. B1's GC pause ends. Its counter drops to 10. At that exact instant, every client perceiving the updated counter simultaneously routes to B1 — a new spike. The synchronized burst can overload B1 again within milliseconds. The herd has stampeded.

Least-connections also requires an O(log N) min-heap scan per request for precise tracking, or O(1) approximate tracking with a single "last minimum" pointer.

## Power-of-two-choices: the modern default

Algorithm (per request):
1. Pick **two** backends uniformly at random.
2. Route to the one with **fewer active connections**.

That's it. Two comparisons, zero global scan.

**Why it beats both alternatives:**
- Pure random → ~100% load imbalance (some backends get 2× the average).
- Least-connections → O(N) scan + synchronized herding.
- Power-of-two-choices → **~12% imbalance** (proven by Adler 1996), O(1) per request, random-noise-desynchronized herding.

The randomness prevents all clients from noticing a backend's recovery at the same instant. The comparison prevents the worst-case assignment. Together: near-optimal at constant cost.

<Infographic
  svg={loadImbalance}
  label="Column chart comparing load imbalance across three algorithms: pure random ~100% over average, power-of-two-choices ~12%, least-connections ~0% (but herds)."
  caption="Power-of-two-choices collapses pure random's ~100% imbalance to ~12% — nearly matching least-connections' even spread, but at O(1) and without the synchronized herd."
  hue="var(--d-infra)"
/>

Nginx, Envoy, HAProxy, and AWS ALB all default to this algorithm or a variant of it.

<NumbersCard
  id="card-lb-algo-numbers"
  title="Balancing algorithm comparison"
  data-lesson-visual
  stats={[
    { label: "Round-robin decision cost", value: "O(1)" },
    { label: "Least-connections decision cost", value: "O(log N)" },
    { label: "Power-of-two-choices decision cost", value: "O(1)" },
    { label: "Pure-random load imbalance", value: "~100%" },
    { label: "Power-of-two-choices imbalance", value: "~12%" },
    { label: "Least-connections imbalance", value: "~0% (but herds)" },
  ]}
/>

<FadedExample
  client:load
  id="lb-mid-faded-1"
  pieceSlug="02-balancing-algorithms"
  lang="en"
  title="Tracing power-of-two-choices over 1 000 requests"
  steps={{
    solved: `1. Request 1: pick B2 (8 conns) and B4 (12 conns) at random → route to B2 (fewer).
2. B2 now has 9 conns, B4 has 12.
3. Request 2: pick B1 (11 conns) and B3 (11 conns) → tied, pick B1 (tiebreaker: lower ID).
4. Request 3: pick B2 (9 conns) and B4 (12 conns) → route to B2.
5. After 1 000 requests: all four backends cluster around 6–7 active connections each, ~12% imbalance.
6. Compare: pure random would leave one backend at ~14 conns while another sits at ~3.`,
    semi: `1. Request 1: pick B2 (?? conns) and B4 (?? conns) → route to ??? (fewer).
2. B2 now has ?? conns, B4 has ??.
3. Request 2: pick B1 (?? conns) and B3 (?? conns) → ???, pick ???.
4. After 1 000 requests: all backends cluster around ??–?? active connections, ~??% imbalance.
5. Compare: pure random would leave one backend at ??x the others.`,
    blank: `1. Request 1: pick ?? backends at random → route to ?? (fewer ?).
2. The winner now has ?? more connection(s).
3. After 1 000 requests: backends cluster near ??, imbalance ~??%.
4. Compare: pure random imbalance: ~??%.`,
  }}
  misconceptions={{
    "LB picks the same two backends for every request": "The two backends are picked fresh at random per request. No affinity — session stickiness is a separate feature.",
    "Power-of-two-choices is just least-connections with a random start": "Key difference: least-connections scans all N backends; power-of-two-choices samples exactly two. That removes the O(N) cost and breaks synchronization.",
  }}
/>

<Quiz
  id="lb-mid-quiz-1"
  pieceSlug="02-balancing-algorithms"
  lang="en"
  question="Why does power-of-two-choices prevent the 'herding' problem that pure least-connections exhibits?"
  choices={[
    { label: "Because it picks backends at random, not based on load at all", misconception: "Random picking alone makes herding worse. The prevention comes from randomness desynchronizing the herd, combined with still routing to the less-loaded backend." },
    { label: "Because the random selection ensures different clients see recovery at different times, preventing a synchronized surge to the recovering backend", correct: true },
    { label: "Because it requires more CPU and spreads the surge over time", misconception: "CPU cost is not the mechanism. Desynchronization is." },
    { label: "Power-of-two-choices does not prevent herding", misconception: "It substantially reduces herding vs pure least-connections via randomization." },
  ]}
/>

<DragOrder
  id="lb-mid-drag-1"
  pieceSlug="02-balancing-algorithms"
  lang="en"
  prompt="Order the backends by connection count and trace the power-of-two-choices winner:"
  items={[
    "Pick backend A (2 connections) and backend D (10 connections) at random.",
    "Since A has fewer connections, route the request to A.",
    "A now has 3 connections, D still has 10.",
    "Next request: pick backend B (5 connections) and backend C (6 connections).",
    "Route to B (fewer).",
    "After many requests, all backends cluster around the same load (≈6–7 connections each), ≈12% imbalance.",
  ]}
/>

<Inset kind="edgecase" lang="en">
**Weighted power-of-two-choices.** When backends have different capacity (one has 4 cores, another has 16), pure power-of-two-choices distributes connections equally rather than proportionally to capacity. Weight the random selection probability by backend capacity: a 4× heavier backend is 4× more likely to be one of the two candidates. This preserves the O(1) property while adapting to heterogeneous fleets.
</Inset>

<Quiz
  id="lb-mid-quiz-2"
  pieceSlug="02-balancing-algorithms"
  lang="en"
  question="Round-robin distributes requests evenly by count. Why does this fail when request cost varies widely?"
  choices={[
    { label: "Because round-robin does not support TLS", misconception: "TLS is handled separately; it has nothing to do with balancing algorithms." },
    { label: "Because a slow 100 ms request and a fast 1 ms request each consume one turn, so a backend processing large uploads accumulates connections faster than round-robin can detect", correct: true },
    { label: "Because round-robin is O(N) per decision", misconception: "Round-robin is O(1). The problem is that it is load-blind, not that it is slow." },
    { label: "Because round-robin only works for TCP, not HTTP", misconception: "Round-robin works at both L4 and L7." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Power-of-two-choices: the load balancer samples two backends at random from the pool, compares their active connection counts, and routes the request to the one with fewer connections."
  hue="var(--d-network)"
  perRow="4"
  nodes={[
    { id: "lb", label: "Load balancer", sub: "sample 2 of 4", col: 0, row: 0 },
    { id: "b1", label: "Backend B1", sub: "11 conns", col: 0, row: 1 },
    { id: "b2", label: "Backend B2", sub: "8 conns ✓", col: 1, row: 1 },
    { id: "b3", label: "Backend B3", sub: "11 conns", col: 2, row: 1 },
    { id: "b4", label: "Backend B4", sub: "12 conns", col: 3, row: 1 },
  ]}
  edges={[
    { from: "lb", to: "b2", label: "sampled (fewer → route here)" },
    { from: "lb", to: "b4", label: "sampled" },
  ]}
  caption="Two random samples (B2 and B4), one comparison: route to B2 because it has fewer active connections. No global scan of all four backends, and the randomness means different requests sample different pairs — so a recovering backend is not stampeded all at once."
/>

<KeyTakeaway>Round-robin is load-blind; least-connections herds; power-of-two-choices samples two random backends and picks the less loaded — O(1), ~12% imbalance, no synchronized herding. It is the production default in Nginx, Envoy, and AWS ALB.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-balancing-algorithms-retrieval"
  lang="en"
  questions={[
    { q: "What is the herding problem with least-connections, and why does power-of-two-choices avoid it?", a: "When a slow backend recovers, its connection count drops suddenly. All clients tracking least-connections see the same drop simultaneously and surge to that backend, overloading it again. Power-of-two-choices avoids this by picking two backends at random per request: different clients pick different pairs, so they perceive the recovery at staggered times rather than all at once." },
    { q: "What load imbalance does power-of-two-choices achieve, and why is that better than pure random?", a: "~12% imbalance, proven mathematically (Adler 1996). Pure random produces ~100% imbalance (some backends get 2× the average). The single comparison step — always route to the less-loaded of the two sampled backends — eliminates the worst-case assignment at O(1) cost." },
    { q: "When would you prefer weighted round-robin over power-of-two-choices?", a: "When all requests cost roughly the same (homogeneous workload) and backends have fixed, known capacity differences that change rarely. Weighted round-robin is simpler to reason about when load is predictable. Power-of-two-choices is better when request cost varies or backend capacity is dynamic." },
  ]}
/>

<Recap lang="en">
Three families of balancing algorithms each make a different tradeoff. Round-robin is O(1) but distributes request count, not actual load — a slow backend drowns in its fair share. Least-connections routes to the backend with the fewest open connections and adapts to real load, but the global minimum update creates a synchronized herding burst when a backend recovers. Power-of-two-choices samples exactly two backends at random and routes to the less loaded one: O(1), ~12% imbalance (vs ~100% for pure random), and randomness desynchronizes the herd. Nginx, Envoy, and AWS ALB default to power-of-two-choices or a variant for exactly these properties. Now when you see a slow backend dragging down a service, the first question to ask is: which algorithm is the LB using — and does it account for connection duration, or just count?
</Recap>
