---
concepts:
- l4-load-balancer
- l7-load-balancer
- x-forwarded-for
- rfc-7239-forwarded
- proxy-protocol
- tls-termination
- content-routing
deepensInto:
- 07-resilient-lb-architecture
estMin: 11
lang: en
lessonType: topic
level: middle
order: 3
prereqs:
- 09-proxy-load-balancing/01-what-reverse-proxies-do
- 05-tls-handshake/01-what-tls-does
- 06-http-versions/01-http-fundamentals
slug: 03-l4-vs-l7-balancing
sources:
- https://www.haproxy.com/blog/layer-4-vs-layer-7-load-balancing/
- https://www.rfc-editor.org/rfc/rfc7239.html
- https://www.envoyproxy.io/docs/envoy/latest/intro/arch_overview/upstream/load_balancing/load_balancers.html
spiral:
- latency
status: ready
summary: L4 routes on IP:port at 1–5 µs with 10–40 Gbps throughput; L7 parses HTTP
  at 100–500 µs for content routing — and X-Forwarded-For, RFC 7239, and PROXY protocol
  each preserve the client IP differently.
title: L4 vs L7 load balancing and client-IP preservation
track: networking
unit: 09-proxy-load-balancing
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import RFCQuiz from "~/components/pedagogy/RFCQuiz.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
Your service logs show every request coming from the same IP — the load balancer's IP. You can't rate-limit by client, can't geo-block, can't audit who did what. That is the price of not thinking carefully about which layer your load balancer operates at.
</Hook>

<Crux>L4 routes on IP:port at wire speed; L7 parses HTTP at 100× more latency — both hide client IP; restore with XFF, RFC 7239, PROXY protocol.</Crux>

<Explanation>

## Layer 4: routing on IP and port

An L4 load balancer sees the **TCP or UDP header** only. It routes on the 4-tuple: source IP, source port, destination IP, destination port. It never reads the HTTP payload. When you pick the wrong layer, you either pay 100× extra latency you don't need, or you find out your rate-limiting code can't see client IPs at all.

**Mechanics:**
- Sees a TCP SYN from the client.
- Picks a backend by the configured algorithm.
- Either rewrites the destination IP (NAT mode) or opens a new TCP connection to the backend (proxy mode).
- Forwards raw TCP segments in both directions.

**Cost:** ~1–5 µs per packet, &lt;1% CPU. Throughput: 10–40 Gbps per server.

**What L4 can do:**
- Route any TCP/UDP protocol (PostgreSQL, Redis, MQTT, custom binary).
- Handle millions of connections per second.
- Terminate TCP independently of HTTP.

**What L4 cannot do:**
- Inspect or modify HTTP headers, path, or host.
- Retry on HTTP 5xx errors (it does not understand HTTP).
- Perform TLS termination without understanding the TLS record format.
- Route different `/api/v1/` and `/api/v2/` paths to different backends.

## Layer 7: routing on HTTP content

An L7 load balancer terminates the **HTTP (and TLS)** connection, parses the request, and applies routing rules before forwarding to a backend.

**Mechanics:**
1. Terminates the client's TLS — it sees the plaintext.
2. Reads the HTTP method, path, `Host` header, custom headers.
3. Applies routing rules (e.g., `/api/` → pool A, `/static/` → pool B).
4. Opens a new connection (or reuses a pool) to the chosen backend.
5. Forwards the request (adding proxy headers like `X-Forwarded-For`).

**Cost:** ~100–500 µs per request, 5–15% CPU. Throughput: 1–10 Gbps per server.

**What L7 adds over L4:**
- Content-aware routing: path, hostname, header, cookie.
- Retry on 5xx (understands HTTP semantics).
- Per-endpoint metrics, rate limiting, auth checks.
- TLS termination once at the LB edge instead of on every backend.
- Traffic splitting (10% → canary, 90% → stable).

<Misconception id="lb-l7-always-better">
"L7 is always better." Not true. L4 is 10–100× faster and handles UDP and non-HTTP protocols (databases, game servers, custom binary). L7 adds latency but enables content routing. Speed vs smarts: L4 for raw throughput (CDN edge, game servers), L7 for microservices and HTTP routing.
</Misconception>

<NumbersCard
  id="card-l4-l7-compare"
  title="L4 vs L7 load balancing"
  data-lesson-visual
  stats={[
    { label: "L4 hop latency", value: "1–5 µs" },
    { label: "L7 hop latency", value: "100–500 µs" },
    { label: "L4 throughput per server", value: "10–40 Gbps" },
    { label: "L7 throughput per server", value: "1–10 Gbps" },
    { label: "L4 CPU per packet", value: "&lt;1%" },
    { label: "L7 CPU per request", value: "5–15%" },
    { label: "L4 protocols", value: "any TCP/UDP" },
    { label: "L7 protocols", value: "HTTP, gRPC, WebSocket" },
  ]}
/>

## Client-IP preservation

When the LB forwards a request to the backend, the backend sees the **LB's IP** as the TCP source address — not the real client. Three standards exist to restore this:

### X-Forwarded-For (XFF)

```
X-Forwarded-For: 203.0.113.195
```

The LB appends the client IP to this header. HTTP-only. Trivially spoofed: a malicious client can send `X-Forwarded-For: 1.2.3.4` and an unvalidated proxy appends rather than overwrites. Only the **rightmost IP added by a trusted proxy** is reliable.

### RFC 7239 Forwarded header

```
Forwarded: for=203.0.113.195;proto=https;host=example.com
```

Standardized (RFC 7239), structured format, harder to parse incorrectly. Includes protocol and host alongside the IP. Still HTTP-only but less prone to accidental misparse.

### PROXY protocol (HAProxy)

A text or binary header **prepended before the TLS/HTTP payload**:

```
PROXY TCP4 203.0.113.195 93.184.216.34 35646 80\r\n
```

Works for **any TCP/UDP protocol** — no HTTP required. The backend TCP stack reads the header before any application-layer parsing. Harder to spoof than an HTTP header because it is inserted at the transport layer by a trusted proxy. Nginx, HAProxy, and Envoy all support PROXY protocol.

**Which to choose:** HTTP services behind trusted proxies → XFF or RFC 7239. Non-HTTP services (PostgreSQL, MQTT, custom binary) or where strict tamper-resistance matters → PROXY protocol.

<TraceScenario
  id="lb-mid-trace-1"
  pieceSlug="03-l4-vs-l7-balancing"
  lang="en"
  scenario="Trace a request that switches from L4 to L7 as it moves through a two-tier LB architecture."
  steps={[
    { prompt: "A client connects to an L4 load balancer on port 443. What does the L4 LB see, and what can it route on?", reveal: "The L4 LB sees the 4-tuple (client IP, client port, LB IP, 443). It can route on IP and port only — it cannot read the TLS SNI or HTTP path. It selects a backend from its pool based on the 5-tuple hash or round-robin and forwards the raw TCP stream." },
    { prompt: "The L4 LB forwards to an L7 LB (e.g., an Envoy instance). The L7 LB terminates TLS. What does it see now?", reveal: "After TLS termination the L7 LB reads the plaintext HTTP request: method, path, Host header, custom headers. It can now route: /api/ → backend pool A, /admin/ → backend pool B. It also sees the real client IP and adds X-Forwarded-For." },
    { prompt: "The L7 LB adds X-Forwarded-For and forwards to backend pool A. The backend reads XFF. What trust assumption must it make?", reveal: "The backend must trust only the LB's X-Forwarded-For. If the backend is reachable directly (not only via the LB), a client can spoof XFF. The backend should only trust XFF from known-LB IPs and validate the rightmost (most recently added) IP in the header." },
    { prompt: "A backend needs the real client IP for rate limiting but is a raw TCP service (not HTTP). How does it get the client IP?", reveal: "Use PROXY protocol: the L4 LB prepends 'PROXY TCP4 <client IP> <backend IP> <sport> <dport>\\r\\n' before the TCP payload. The backend's application reads this line first. It works regardless of what protocol follows (HTTP, PostgreSQL, MQTT, etc.)." },
  ]}
/>

<RFCQuiz
  id="lb-mid-rfc-1"
  pieceSlug="03-l4-vs-l7-balancing"
  lang="en"
  question="Which RFC standardizes the Forwarded header for preserving client IP through proxies?"
  choices={[
    { rfc: "RFC 7239", title: "Forwarded HTTP Extension", correct: true },
    { rfc: "RFC 9110", title: "HTTP Semantics" },
    { rfc: "RFC 3986", title: "URI Generic Syntax" },
    { rfc: "RFC 1918", title: "Address Allocation for Private Internets" },
  ]}
/>

<Inset kind="why" lang="en">
**Why L4 + L7 in series?** Many production architectures use an L4 LB at the network edge (BGP anycast, absorbs floods, handles millions of connections) and an L7 LB behind it (terminates TLS, applies routing policies, emits per-endpoint metrics). The L4 layer handles the raw packet pressure cheaply; the L7 layer adds intelligence without needing to scale to packet-rate throughput. AWS uses this two-tier pattern: Network Load Balancer (L4) → Application Load Balancer (L7).
</Inset>

<Quiz
  id="lb-mid-quiz-l4-l7"
  pieceSlug="03-l4-vs-l7-balancing"
  lang="en"
  question="You need to route /api/payments/ to a PCI-DSS-compliant backend pool and /api/catalog/ to a regular pool. Which load balancer layer can do this, and why?"
  choices={[
    { label: "L4, because it handles all TCP traffic", misconception: "L4 sees only IP and port, not the URL path. It cannot distinguish /payments/ from /catalog/." },
    { label: "L7, because it parses the HTTP request and can route by path, host, and headers", correct: true },
    { label: "Either layer, because both read HTTP headers", misconception: "L4 does not read HTTP headers. Only L7 parses HTTP." },
    { label: "Neither — you need DNS for path-based routing", misconception: "DNS operates at the hostname level, not the URL path level. Path routing requires L7." },
  ]}
/>

</Explanation>

<StackDiagram
  label="A load balancer reads the network stack only as deep as its layer. An L7 balancer terminates TLS and parses HTTP content (path, host, headers); an L4 balancer reads only the TCP/UDP header (the IP and port 4-tuple) and never inspects the payload."
  hue="var(--d-network)"
  layers={[
    { label: "L7: HTTP request (method, path, Host, headers)", note: "L7 only · 100–500 µs" },
    { label: "TLS record (terminated and decrypted at L7)", note: "L7 only" },
    { label: "TCP / UDP header (IP:port 4-tuple)", note: "L4 reads here · 1–5 µs" },
    { label: "IP packet payload (raw bytes, unparsed)", note: "L4 forwards as-is" },
  ]}
  caption="L4 routes on the 4-tuple at wire speed for any TCP/UDP protocol but cannot see the path or host. L7 climbs higher — terminating TLS and parsing HTTP — to route by content, paying 100× the latency for the privilege."
/>

<KeyTakeaway>L4 routes on IP:port at wire speed (1–5 µs, 10–40 Gbps) for any protocol; L7 parses HTTP for content routing at 100–500 µs per request. Either way, the backend sees the LB's IP — restore the client IP with X-Forwarded-For (HTTP), RFC 7239 Forwarded (structured HTTP), or PROXY protocol (any TCP/UDP).</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-l4-vs-l7-balancing-retrieval"
  lang="en"
  questions={[
    { q: "You want to route by HTTP path and retry on 5xx. Which LB layer is required, and what is its latency cost?", a: "L7 load balancing. It terminates HTTP, parses the request, and understands HTTP semantics (5xx means application error → retryable). Latency cost: 100–500 µs per request, 5–15% CPU, throughput 1–10 Gbps — vs L4's 1–5 µs at 10–40 Gbps." },
    { q: "Why is X-Forwarded-For spoofable, and how does PROXY protocol improve on it?", a: "X-Forwarded-For is an HTTP header that any client can set before the proxy appends its own value. Without strict validation of which proxy IPs are trusted, the backend cannot know which IP in the list is genuine. PROXY protocol is a binary/text header prepended at the TCP layer by the proxy — a client sending raw TCP bytes cannot easily inject a PROXY header that arrives before the proxy's own, so it is much harder to forge." },
    { q: "When would you choose L4 over L7 even for HTTP traffic?", a: "When raw throughput matters more than content routing: CDN edge points absorbing DDoS floods, game servers (UDP), database protocol load balancing (PostgreSQL, Redis), or any non-HTTP binary protocol. L4 costs 10–100× less CPU per packet and handles 10× more throughput." },
  ]}
/>

<Recap lang="en">
L4 load balancers route on the TCP/UDP 4-tuple — they are fast (1–5 µs, 10–40 Gbps) and protocol-agnostic but cannot inspect HTTP. L7 load balancers terminate TLS, parse HTTP, and enable path/host/header routing and 5xx retries — at the cost of 100–500 µs latency and 5–15% CPU. Both types hide the client's IP from the backend; the three standards to restore it are X-Forwarded-For (de facto but spoofable), RFC 7239 Forwarded (structured, HTTP-only), and PROXY protocol (binary, any TCP/UDP, hardest to spoof). Production architectures often stack L4 at the edge and L7 behind it: raw packet throughput handled cheaply at L4, content intelligence at L7. Now when you see your service logs showing every request from the load balancer's IP, you know exactly which header to validate — and which proxy to trust.
</Recap>
