infographic chart-column-simple
data
  title The price of content routing: 100× latency
  desc Per-hop latency, range upper bound — L4 reads the 4-tuple, L7 parses HTTP
  values
    - label L4 (IP:port, µs)
      value 5
    - label L7 (parses HTTP, µs)
      value 500
theme
  palette #14b8a6 #f43f5e
