infographic chart-column-simple
data
  title Per-client state at 10,000 req/sec (entries stored)
  desc O(1) algorithms hold 1-2 counters; the sliding window log holds one entry per request
  values
    - label Fixed window (1 counter)
      value 1
    - label Token bucket (counter + timestamp)
      value 2
    - label Sliding window counter (2 counters)
      value 2
    - label Sliding window log (1 entry/request)
      value 10000
theme
  palette #6366f1 #14b8a6 #10b981 #f43f5e
