---
concepts:
- amplification-attack
- reflection
- dns-amplification
- ntp-amplification
- memcached-amplification
- syn-flood
- syn-cookies
- http2-rapid-reset
deepensInto:
- 06-defense-in-depth
estMin: 12
lang: en
lessonType: topic
level: middle
order: 2
prereqs:
- 01-ddos-what-and-why
- 03-tcp-handshake/02-state-machine-and-teardown
- 04-dns-resolution/01-resolution-chain
slug: 02-amplification-and-floods
sources:
- https://blog.cloudflare.com/ddos-threat-report-2025-q4/
- https://www.cisa.gov/news-events/alerts/2023/10/10/http2-rapid-reset-vulnerability-cve-2023-44487
- https://datatracker.ietf.org/doc/html/rfc4987
spiral:
- statefulness
- latency
status: ready
summary: Amplification attacks exploit open third-party servers to multiply attacker
  bandwidth 50–1280x; state-exhaustion attacks fill TCP backlog tables to drop legitimate
  connections.
title: Amplification attacks and state exhaustion
track: networking
unit: 11-network-security
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";

<Hook>
Your CDN is absorbing the attack, but the DDoS traffic is coming from legitimate DNS resolvers — not from the attacker's IP at all. Blocking the resolvers blocks all DNS. The attacker sent one small query per resolver; each resolver responded to the victim with 50x more data. Welcome to amplification.
</Hook>

<Crux>Amplification multiplies bandwidth 50-1280x via open servers; SYN floods fill TCP tables — no single IP to block either.</Crux>

<Explanation>

**Amplification and reflection: the mechanism.** An amplification attack exploits a stateless, UDP-based protocol on a third-party server. The attacker sends a small request with a forged source IP (the victim's IP). The third-party server responds to the source IP — which is the victim — with a much larger response. The attacker does not need to receive anything; the third-party does all the work. The "amplification factor" is the ratio of response size to request size.

| Protocol | Query size | Response size | Amplification |
|----------|-----------|---------------|---------------|
| DNS (ANY record) | 40 bytes | ~1,700 bytes | 42x |
| NTP (monlist) | 40 bytes | 468 bytes | 12x (up to 556x) |
| Memcached (UDP) | 50 bytes | 51,200 bytes | 1,024x |

<div data-lesson-visual class="my-4 p-4 border rounded bg-gray-50">
  <p class="text-sm font-semibold mb-2">DNS amplification attack flow</p>
  <ol class="text-sm list-decimal list-inside space-y-1">
    <li>Attacker sends DNS query, source IP = victim's IP, dest = open resolver</li>
    <li>Open resolver receives it and replies to source IP (victim) with large response</li>
    <li>Victim receives unsolicited large responses from thousands of resolvers</li>
    <li>Each attacker query costs 40 bytes; each victim hit is ~1,700 bytes</li>
    <li>Result: 1 Gbps attacker bandwidth → 42 Gbps of attack on victim</li>
  </ol>
</div>

<Misconception id="amplification-cost" title="Heads-up: amplification is the economist's choice for attackers">
Botnets cost ~$50–500/month. Amplification attacks cost nothing: send one query, get 50x amplified response. DNS open-resolvers, NTP servers, and misconfigured memcached are free weapons. That is why they are targeted constantly.
</Misconception>

**HTTP/2 Rapid Reset (CVE-2023-44487).** HTTP/2 allows multiple parallel streams. The Rapid Reset attack sends a stream and immediately cancels it — before the server sends any response. The server allocates memory and CPU for the request anyway. Sending and resetting thousands of streams per second exhausts the server at the CPU level, not the bandwidth level. A single small botnet (20,000 machines) generated 398 million requests per second against Google using this technique in 2023 — the highest request-rate DDoS on record at the time. The fix: HTTP/2 servers must rate-limit stream creation rate and treat high-reset-rate clients as attackers.

<NumbersCard
  id="card-attack-numbers"
  title="Attack statistics from the field"
  stats={[
    { label: "Requests/sec (HTTP/2 Rapid Reset)", value: "398M" },
    { label: "Largest DDoS (Dec 2025)", value: "31.4 Tbps" },
    { label: "Memcached amplification factor", value: "1,024x" },
    { label: "Botnet machines (Rapid Reset attack)", value: "~20K" },
  ]}
/>

<TraceScenario
  id="mid-trace-1"
  lessonSlug="02-amplification-and-floods"
  lang="en"
  scenario="Trace a DNS amplification attack from attacker to victim."
  steps={[
    { prompt: "Step 1: what does the attacker send?", reveal: "A UDP packet with source IP = victim's IP and destination IP = a public DNS resolver. The query requests a large DNS record (e.g., ANY record for a domain with DNSSEC signatures)." },
    { prompt: "Step 2: what does the resolver do?", reveal: "It receives the query and sends a response back to the source IP in the packet — which is the victim's IP. The response is much larger than the query (42x amplification for DNS)." },
    { prompt: "Step 3: what is the attacker's role after sending the query?", reveal: "Nothing. The attacker sends one small query and the resolver sends a larger response directly to the victim. The attacker amplifies their attack with minimal bandwidth." },
    { prompt: "Step 4: why is this hard to defend?", reveal: "The responses come from legitimate DNS resolvers, not from the attacker. Blocking the resolver's IP blocks all DNS traffic. The solution is rate-limiting outbound DNS responses and closing open resolvers (stop responding to queries from the public Internet)." },
  ]}
/>

**State-exhaustion: SYN floods.** The TCP three-way handshake requires the server to store state for every in-progress connection. The SYN flood sends millions of SYN packets with spoofed source IPs. For each SYN, the server allocates a slot in its SYN backlog and waits for the ACK that will never come (because the source IP is fake). When the backlog fills, the server drops new SYN packets — including those from legitimate clients. Default Linux SYN backlog: 128–512 slots; under attack it fills in milliseconds.

**SYN cookies: trading memory for CPU.** Linux SYN cookies (enabled by default since kernel 2.6) avoid storing backlog state. When the backlog would overflow, the server encodes the connection parameters (ISN) as a cookie derived from the source IP, port, and timestamp. No slot is allocated. If the client replies with a correct ACK, the server reconstructs the state from the cookie and completes the handshake. The cost: slightly higher CPU per new connection; the benefit: no memory exhaustion under flood conditions.

<Quiz
  id="mid-quiz-1"
  lessonSlug="02-amplification-and-floods"
  lang="en"
  question="In a DNS amplification attack, why does the attacker use a forged source IP?"
  choices={[
    { label: "To hide their own IP from the DNS resolver", misconception: "The resolver logs the IP that sent the query; forging the source changes who receives the response." },
    { label: "To make the DNS response go to the victim's IP instead of back to the attacker", correct: true },
    { label: "To speed up the query", misconception: "Source IP has no effect on query speed." },
    { label: "To bypass authentication", misconception: "DNS queries are not authenticated by default; forged source is about redirecting the response." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  lessonSlug="02-amplification-and-floods"
  lang="en"
  question="Why does SYN cookie defense trade memory for CPU?"
  choices={[
    { label: "It allocates more memory per connection to avoid CPU lookup", misconception: "SYN cookies reduce memory usage — the server stores nothing for incoming SYNs." },
    { label: "It encodes connection state in the ISN cookie rather than storing it, requiring a hash computation on each ACK", correct: true },
    { label: "It uses a shared memory pool across all CPUs", misconception: "SYN cookies work per-core; there is no shared pool." },
    { label: "It defers the TCP handshake to a separate worker process", misconception: "SYN cookies work in the kernel network stack, not a separate process." },
  ]}
/>

<Inset kind="edgecase" lang="en">
ACK floods are the less-known cousin of SYN floods. The attacker sends millions of ACK packets to a port with no active connection. The kernel must respond with RST packets (there is no connection to acknowledge). Under heavy ACK flood, the RST rate can consume significant CPU. Defense: TCP RST rate limiting (limit RSTs per second per source IP) and firewall-level suppression — drop ACKs that do not match any existing connection in the conntrack table.
</Inset>

</Explanation>

<KeyTakeaway>Amplification attacks use third-party servers to multiply attacker bandwidth 42–1024x by forging the source IP on UDP queries. State-exhaustion attacks fill TCP backlog tables using spoofed SYNs — SYN cookies eliminate the memory state at the cost of hash computation per ACK.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-amplification-and-floods-retrieval"
  lang="en"
  questions={[
    { q: "Why is amplification so much more cost-effective than a direct botnet flood?", a: "With amplification, the attacker sends tiny queries (40 bytes) and a third-party sends 42–1024x larger responses directly to the victim. A botnet of 10,000 machines sending 100 Mbps each = 1 Tbps; with 1024x memcached amplification, the same bandwidth generates 1 Pbps equivalent of attack traffic. The bottleneck is the open resolver's response capacity, not the attacker's bandwidth." },
    { q: "What is the HTTP/2 Rapid Reset vulnerability and why does it bypass normal rate limiting?", a: "Rapid Reset (CVE-2023-44487) sends and immediately cancels HTTP/2 streams. The server allocates CPU/memory for each stream even when cancelled. Many rate limiters count completed requests or responses — not stream-creation events — so the attacker stays under the limit while exhausting server resources. The fix is to rate-limit stream creation rate regardless of whether streams are completed." },
    { q: "How does SYN cookie defense eliminate SYN backlog exhaustion?", a: "Instead of allocating a backlog slot for each SYN, the server encodes the connection parameters in the ISN (Initial Sequence Number) as a hash. No memory is consumed. When the client replies with ACK, the server recomputes the hash to verify and reconstruct the state. Flood traffic simply produces hash computations with no matching ACK — no memory is exhausted." },
  ]}
/>

<Recap lang="en">
Amplification attacks exploit open UDP services (DNS, NTP, Memcached) that respond to spoofed queries — the attacker sends 40 bytes, the victim receives 1,700+ bytes per query, multiplied across thousands of resolvers. HTTP/2 Rapid Reset is the application-layer equivalent: 20,000 botnet machines generated 398M req/sec by exploiting stream cancellation. SYN floods exhaust server connection tables by sending millions of half-open connections using spoofed IPs; SYN cookies defend by encoding state in the TCP sequence number instead of storing it in memory. All three attacks share the same property: the attacker spends little, the defender pays much.
</Recap>
