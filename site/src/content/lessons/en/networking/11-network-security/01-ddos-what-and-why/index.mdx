---
concepts:
- ddos
- volumetric-attack
- state-exhaustion
- application-layer-attack
- rate-limiter
- cdn-scrubbing
deepensInto:
- 02-amplification-and-floods
- 03-rate-limiting-algorithms
estMin: 10
lang: en
lessonType: topic
level: junior
order: 1
prereqs:
- 03-tcp-handshake/01-the-three-way-handshake
- 07-cdn-edge/01-what-cdns-do
slug: 01-ddos-what-and-why
sources:
- https://blog.cloudflare.com/ddos-threat-report-2025-q4/
- https://www.cloudflare.com/learning/ddos/what-is-a-ddos-attack/
- https://www.cisa.gov/sites/default/files/publications/understanding-and-responding-to-ddos-attacks_508c.pdf
spiral:
- statefulness
- latency
status: ready
summary: A DDoS attack overwhelms a service with traffic from many sources simultaneously
  — making it unavailable to real users without stealing data or breaking code.
title: 'DDoS: what it is and why it works'
track: networking
unit: 11-network-security
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import costAsymmetry from "./infographics/cost-asymmetry.svg?raw";

<Hook>
Your service is working fine. Then, without warning, requests start timing out. The traffic looks normal on your monitor — same request count, same sizes — but responses slow from 50 ms to 5 seconds. You check the logs and see 10 million requests per second from a hundred thousand different IPs. An adversary is attacking.
</Hook>

<Crux>DDoS attacks saturate your service from many sources at once — making defense impossible without distributed infrastructure at the edge.</Crux>

<Explanation>

**What DDoS means.** Distributed Denial of Service — an attacker sends so much traffic from so many different sources that your service cannot respond to real users. The attacker does not steal data or inject commands. They just make your service unavailable. The "distributed" part is what makes it hard to stop: you cannot simply block one IP address.

**Why it happens constantly.** Cloudflare blocks on average ~110 million DDoS requests per day. Botnets — networks of malware-infected computers and IoT devices — are cheap to rent. Attacker cost: ~$50–500/month for a 10 Gbps-capable botnet service. For a competitor or bad actor, that is a low-cost way to take down a rival's service.

**The three types of attacks.**

Before you can choose a defense, you need to know which layer the attacker is hitting — because each type requires a fundamentally different countermeasure.

Volumetric attacks (L3/L4) aim to saturate the network pipe itself — gigabits of UDP, ICMP, or DNS responses flood the link before your server even sees the packets.

Protocol/state-exhaustion attacks (L3/L4) aim to fill connection-tracking tables and exhaust kernel memory — SYN floods force your server to hold thousands of half-open TCP connections until it runs out of slots and drops legitimate connections.

Application-layer attacks (L7) look like legitimate traffic but are expensive to process — HTTP floods, slow requests that hold connections open for minutes (Slowloris), or queries hitting expensive database paths. These are the hardest to stop because they pass every port and rate check.

<div data-lesson-visual class="overflow-x-auto my-4">
  <table class="text-sm w-full border-collapse">
    <thead>
      <tr class="bg-gray-100">
        <th class="border p-2 text-left">Attack type</th>
        <th class="border p-2 text-left">Layer</th>
        <th class="border p-2 text-left">Goal</th>
        <th class="border p-2 text-left">Example</th>
        <th class="border p-2 text-left">Defense</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border p-2">Volumetric</td>
        <td class="border p-2">L3/L4</td>
        <td class="border p-2">Saturate the pipe</td>
        <td class="border p-2">UDP flood, DNS amplification</td>
        <td class="border p-2">CDN scrubbing, anycast absorption</td>
      </tr>
      <tr>
        <td class="border p-2">State exhaustion</td>
        <td class="border p-2">L3/L4</td>
        <td class="border p-2">Fill connection tables</td>
        <td class="border p-2">SYN flood</td>
        <td class="border p-2">SYN cookies, rate limiting</td>
      </tr>
      <tr>
        <td class="border p-2">Application-layer</td>
        <td class="border p-2">L7</td>
        <td class="border p-2">Exhaust CPU/memory</td>
        <td class="border p-2">HTTP flood, Slowloris</td>
        <td class="border p-2">WAF, adaptive concurrency limits</td>
      </tr>
    </tbody>
  </table>
</div>

**How a CDN scrubbing center helps.** When you serve traffic directly from your origin, a 100 Gbps attack saturates your upstream link and legitimate requests never reach your server. A CDN with DDoS scrubbing routes all traffic to its nearest Point of Presence (PoP) first. The PoP has provisioned bandwidth to absorb the attack across its global infrastructure, and only clean traffic is forwarded to your origin. You stay online because the attack is distributed across the CDN's network, not concentrated on your server.

<NumbersCard
  id="ddos-scale-numbers"
  title="DDoS scale in 2025–2026"
  stats={[
    { label: "Cloudflare daily DDoS blocks", value: "~110M requests" },
    { label: "Largest attack (Dec 2025)", value: "31.4 Tbps" },
    { label: "Botnet rental cost", value: "$50–500/month" },
    { label: "Legitimate user avg req/sec", value: "10–20 req/s" },
  ]}
/>

<Quiz
  id="jr-quiz-1"
  lessonSlug="01-ddos-what-and-why"
  lang="en"
  question="Why is DDoS hard to defend against compared to a single attacker?"
  choices={[
    { label: "Attackers use more powerful computers", misconception: "The attacker's CPU power is irrelevant. The issue is the number of sources." },
    { label: "Because the traffic comes from many sources, you cannot simply block one IP address", correct: true },
    { label: "Because attackers use encryption that you cannot inspect", misconception: "Attackers can use encryption, but the real challenge is the distributed nature of the sources." },
    { label: "Because DDoS attacks are physically destructive", misconception: "DDoS is purely digital traffic. No hardware is damaged." },
  ]}
/>

**The arms race.** Bea runs a web service. Sven says "I'll flood you with so much traffic you cannot serve real users." Bea replies "I have a rate limiter — I reject the thousandth request per second from any IP." Sven: "I will use a thousand different IPs." Bea: "Then I have a WAF to detect the attack pattern. Legitimate users make normal requests; your flood is suspicious." This back-and-forth is not hypothetical — it describes real attacker adaptations measured over years.

<Infographic
  svg={costAsymmetry}
  label="A two-way comparison: an attacker with a botnet rents 10 Gbps for $50–500 a month, floods the pipe at 100 Gbps from thousands of IPs at 10 million requests per second; a defender alone at the origin cannot buy 100 Gbps of upstream bandwidth, gains nothing by blocking one IP, has only 10–20 req/s of real users to protect, and watches the origin pipe saturate."
  caption="The cost of attacking is asymmetric to the cost of defending alone — which is why you cannot out-scale the attacker at your origin and need shared edge infrastructure."
  hue="var(--d-infra)"
/>

<Inset kind="why" lang="en">
Why do most DDoS defenses live at the CDN layer and not at your origin server? Because the economics are asymmetric: an attacker with a botnet can generate 100 Gbps for $500/month. A single origin server cannot buy 100 Gbps of upstream bandwidth. A CDN amortizes that capacity across millions of customers, so the per-customer cost of defense is tiny. This is the fundamental answer to DDoS: you cannot out-scale the attacker alone; you need shared infrastructure.
</Inset>

<DragOrder
  id="jr-drag-1"
  lessonSlug="01-ddos-what-and-why"
  lang="en"
  prompt="Order the DDoS defense layers from edge to origin:"
  items={[
    "DDoS scrubbing center absorbs volumetric traffic",
    "Rate limiter rejects excessive requests",
    "WAF detects application-layer attack patterns",
    "Origin server logs and alerts",
  ]}
/>

<MetaphorComplete
  id="jr-metaphor-1"
  lessonSlug="01-ddos-what-and-why"
  lang="en"
  setup="A DDoS attack is like _______ the coffee shop — the shop is not damaged, but it cannot serve legitimate customers."
  accepted={["flooding", "overwhelming", "crowding", "flooding with people", "mobbing"]}
  canonical="flooding (with requests)"
  explanation="The attacker overwhelms the service's capacity with meaningless traffic, making it unavailable to legitimate users. The service itself is not compromised — just temporarily unreachable."
/>

</Explanation>

<FlowDiagram
  label="DDoS traffic from many sources is absorbed by the CDN scrubbing PoP, which forwards only clean traffic to the origin server"
  hue="var(--d-network)"
  perRow={4}
  nodes={[
    { id: "bots", label: "Botnet", sub: "many sources" },
    { id: "pop", label: "CDN PoP", sub: "scrubbing" },
    { id: "origin", label: "Origin", sub: "your server" },
    { id: "users", label: "Real users", sub: "stay served" },
  ]}
  edges={[
    { from: "bots", to: "pop", label: "100 Gbps flood" },
    { from: "pop", to: "origin", label: "clean traffic only" },
    { from: "users", to: "pop", label: "normal requests" },
  ]}
  caption="The scrubbing PoP absorbs the volumetric flood across the CDN's global capacity and proxies only legitimate requests to your origin — the origin pipe is never saturated, so real users stay served."
/>

<KeyTakeaway>DDoS attacks make services unavailable by overwhelming them from many sources simultaneously. Defense requires distributed infrastructure — a CDN absorbs volumetric floods; rate limiters and WAFs stop protocol and L7 attacks. A single origin server cannot absorb 100+ Gbps alone.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-ddos-what-and-why-retrieval"
  lang="en"
  questions={[
    { q: "In one sentence: why does a CDN with DDoS scrubbing help you survive an attack?", a: "The CDN's globally distributed scrubbing centers are provisioned with enough bandwidth to absorb attacks meant for your origin — only clean traffic is forwarded, so you stay online even when the attack exceeds your origin pipe capacity." },
    { q: "Name the three types of DDoS attacks and the layer each targets.", a: "Volumetric (L3/L4) — saturate the network pipe; state-exhaustion (L3/L4) — fill connection-tracking tables; application-layer (L7) — exhaust CPU and memory with expensive-to-process legitimate-looking requests." },
    { q: "Why can a botnet operator with $500/month threaten a service that costs much more to run?", a: "Because DDoS attack cost is asymmetric: generating 10 Gbps of junk traffic is cheap (rented botnets); absorbing 10 Gbps at the origin requires expensive upstream bandwidth. The defense only becomes affordable when amortized across millions of customers via a CDN." },
  ]}
/>

<Recap lang="en">
DDoS attacks work by saturating a service from many simultaneous sources — volumetric attacks flood the pipe, state-exhaustion attacks fill connection tables, and L7 attacks look like legitimate traffic but consume CPU. No single IP block stops distributed attacks; the attacker's goal is simply unavailability, not data theft. The practical defense is to route traffic through a CDN with scrubbing centers that can absorb attacks across global PoPs — a 100 Gbps attack becomes manageable when spread across 330+ nodes. Cloudflare alone blocks ~110 million DDoS requests per day, which illustrates how continuous this threat is in production. Now when you see traffic timing out and the logs show requests flooding in from thousands of IPs, you know you are looking at a DDoS — and that defending alone at the origin is the wrong answer.
</Recap>
