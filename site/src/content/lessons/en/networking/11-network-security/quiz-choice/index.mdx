---
concepts:
- ddos-vectors
- amplification
- rate-limiting
- waf-tuning
- mtls
- rpki-rov
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://blog.cloudflare.com/ddos-threat-report-2025-q4/
- https://owasp.org/www-project-modsecurity-core-rule-set/
- https://isbgpsafeyet.com/
status: ready
summary: "Multiple-choice synthesis across the network-security unit: attack vectors, amplification economics, rate-limiting tradeoffs, WAF tuning, mTLS identity, and RPKI/ROV."
title: 'Network security: multiple-choice review'
track: networking
unit: 11-network-security
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each mirrors a call you make mid-incident — which layer is failing, which knob actually moves the attacker's cost — not a definition to recite.
</Hook>

<Goal lang="en">
Confirm you can connect attack vector to defense layer: which tool stops which attack, why amplification and Rapid Reset bypass naive limits, and why some defenses (RPKI without ROV, HSTS without preload) are cosmetic on their own.
</Goal>

<Quiz
  id="11-network-security-choice-1"
  lessonSlug="11-network-security"
  lang="en"
  question="An attacker rents a botnet pushing 1 Gbps directly, then pivots to memcached reflection at a 1,024x amplification factor with the same 1 Gbps of upstream. What changes, and why is it harder to stop?"
  choices={[
    { label: "The victim now sees up to ~1 Tbps, and the flood arrives from legitimate memcached servers with spoofed-victim source IPs — blocking those sources blocks legitimate infrastructure, so you must absorb at an anycast edge and get operators to close the open servers upstream.", correct: true },
    { label: "Nothing changes — the attacker's 1 Gbps upstream caps the attack at 1 Gbps regardless of amplification.", misconception: "Amplification's whole point is that the third-party server's response bandwidth, not the attacker's, sets the volume. 1 Gbps of 1,024x reflection approaches 1 Tbps at the victim." },
    { label: "It is easier to stop because the reflecting servers' IPs are known and can be blocklisted at the firewall.", misconception: "Those are real DNS/NTP/memcached servers; blocklisting them breaks legitimate traffic. The fix is absorbing volume at an anycast edge and closing open services, not blocking reflectors." },
    { label: "A WAF will catch it because the payloads match known attack signatures.", misconception: "Amplification is an L3/L4 volumetric flood that saturates the pipe before any HTTP layer; a WAF inspects L7 content and never sees these packets in time." },
  ]}
/>

<Quiz
  id="11-network-security-choice-2"
  lessonSlug="11-network-security"
  lang="en"
  question="A public API uses a strict fixed-window limit of 100 req/min per IP. A scraper consistently pulls ~200 requests in a few seconds without ever being blocked. What is the mechanism, and which algorithm fixes it cheaply?"
  choices={[
    { label: "The fixed-window boundary problem: the scraper fires ~100 at the end of one window and ~100 at the start of the next, so two windows' worth land in a short span. A sliding-window counter (two counters, O(1) memory) estimates across the boundary and closes the gap.", correct: true },
    { label: "The limiter is leaking because fixed window uses O(n) memory and drops counters under load.", misconception: "Fixed window is O(1) — one counter per window. The defect is accuracy at the boundary, not memory." },
    { label: "Token bucket is the only fix, because fixed window cannot be made accurate.", misconception: "A sliding-window counter fixes the boundary burst at O(1) cost. Token bucket is a fine choice too, but it deliberately allows bursts up to capacity — it is not 'the only' fix." },
    { label: "Switch to a sliding-window log for exact counting; it is the right default at any scale.", misconception: "A sliding-window log is exact but O(n) per client — at thousands of req/sec it stores a timestamp per request and is impractical at scale. The counter gives near-exact accuracy at O(1)." },
  ]}
/>

<Quiz
  id="11-network-security-choice-3"
  lessonSlug="11-network-security"
  lang="en"
  question="Black Friday is in three days. Your WAF at PL2 blocks ~70% of an ongoing L7 attack; raising it to PL4 reaches ~95% coverage but pushes legitimate-customer false positives to ~5%. What is the senior move?"
  choices={[
    { label: "Keep the WAF moderate (PL2-3) and add adaptive concurrency limiting at the origin, which rejects only when the system is already overloaded — so no paying customer is blocked pre-emptively, and the attack fails when it tries to saturate capacity.", correct: true },
    { label: "Go to PL4 and accept 5% false positives as the cost of security.", misconception: "5% blocked customers on Black Friday is direct revenue loss and churn. A false positive that blocks a buyer is worse here than letting bounded attack traffic through." },
    { label: "Stay at PL2 and accept that 30% of the attack gets through.", misconception: "Accepting the bypass means the degradation continues; you have solved nothing. You need another layer, not resignation." },
    { label: "Deploy a second vendor's WAF in front of the first for double coverage.", misconception: "Stacking two high-false-positive WAFs compounds false positives rather than reducing them. Add a load-reactive layer, not a second content-matching layer." },
  ]}
/>

<Quiz
  id="11-network-security-choice-4"
  lessonSlug="11-network-security"
  lang="en"
  question="A platform team proposes locking down service-to-service traffic with network IP allowlists (only pods in subnet 10.2.0.0/16 may call payments). Why does a senior engineer push for mTLS instead?"
  choices={[
    { label: "An IP allowlist only proves a packet came from somewhere in the subnet — if any pod there is compromised it can impersonate any service. mTLS proves the caller holds a valid short-lived certificate for a specific identity, so a breached pod cannot move laterally as another service.", correct: true },
    { label: "mTLS is faster than IP filtering because it skips the TCP handshake.", misconception: "mTLS adds a handshake cost (tens of ms per new connection on older hardware); its value is cryptographic identity and encryption, not speed." },
    { label: "IP allowlists cannot be expressed in Kubernetes, so mTLS is the only option.", misconception: "NetworkPolicies express IP/label allowlists fine. The point is that network location is not identity — mTLS binds identity cryptographically." },
    { label: "mTLS removes the need for any network policy because it encrypts traffic.", misconception: "Encryption is not authorization; mTLS authenticates identity, and you still layer network policy as defense-in-depth. They solve different problems." },
  ]}
/>

<Quiz
  id="11-network-security-choice-5"
  lessonSlug="11-network-security"
  lang="en"
  question="Your prefixes all have valid RPKI ROAs published, yet a hijacked announcement from an unauthorized AS still pulls a chunk of your traffic away for an hour. How is that possible?"
  choices={[
    { label: "Publishing a ROA only declares the legitimate origin — it does nothing unless routers enforce ROV. Only ~25% of networks drop RPKI-invalid routes, so the hijack still propagates through the ~75% that do not validate.", correct: true },
    { label: "The ROA must have expired; a valid ROA makes hijacks technically impossible.", misconception: "Even a perfectly valid, current ROA cannot stop a hijack on networks that do not enforce ROV. ROA publication and ROV enforcement are two separate halves." },
    { label: "DNSSEC was not enabled, so the hijack succeeded at the DNS layer.", misconception: "This is a BGP/routing hijack, not DNS. DNSSEC protects DNS response integrity; it has no effect on route propagation." },
    { label: "BGPsec would have prevented it and is universally deployed, so the ROA was redundant.", misconception: "BGPsec adoption is minimal due to CPU/path-signing overhead — it is not universally deployed. The realistic defense today is ROA publication plus widespread ROV enforcement." },
  ]}
/>

<Quiz
  id="11-network-security-choice-6"
  lessonSlug="11-network-security"
  lang="en"
  question="You front everything with a CDN, rate limits, and a WAF. The attacker switches to GET requests with a unique ?x=random per request against your most expensive query; cache-hit rate collapses from 95% to 5% and the origin database melts. Which layer actually saves you?"
  choices={[
    { label: "Adaptive concurrency limiting at the origin: when in-flight requests exceed the capacity threshold it sheds load with fast 503s, so the database stays responsive — it reacts to actual overload rather than trying to fingerprint a per-IP rate that no single IP exceeds.", correct: true },
    { label: "A stricter per-IP rate limit, because the flood must be coming from a few hot IPs.", misconception: "The premise is a distributed flood where no single IP exceeds the limit; tightening per-IP thresholds blocks real users before it touches the attack." },
    { label: "Anycast edge absorption, since that already handles volumetric floods.", misconception: "Edge absorption stops Gbps-scale volume, but this is low-volume, cache-busting L7 traffic that looks legitimate and passes straight through to the origin." },
    { label: "DNSSEC, to stop the cache poisoning that dropped the cache-hit rate.", misconception: "The cache-hit drop is the attacker appending unique query params to bypass the CDN cache, not DNS poisoning. DNSSEC is unrelated to HTTP cache behavior." },
  ]}
/>

<Recap lang="en">
The through-line is one map: attack vector to defense layer. Amplification turns a small attacker into a volumetric flood you must absorb at an anycast edge, not blocklist. Rate-limiter choice is a tradeoff — fixed window leaks at the boundary, the sliding-window counter fixes it at O(1). WAF paranoia trades coverage for false positives, so you pair a moderate WAF with load-reactive adaptive concurrency rather than chasing perfect content matching. mTLS gives identity that IP allowlists cannot. And RPKI without ROV — like HSTS without preload — is only half a control. No single layer stops every vector; that is the entire premise of defense in depth.
</Recap>
