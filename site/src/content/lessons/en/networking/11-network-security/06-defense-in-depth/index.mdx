---
concepts:
- defense-in-depth
- anycast-scrubbing
- adaptive-concurrency-limiting
- load-shedding
- circuit-breaker
- observability-under-attack
- attack-economics
- mTLS-service-mesh
- spiffe
- rfc-5961
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: senior
order: 6
prereqs:
- 02-amplification-and-floods
- 03-rate-limiting-algorithms
- 04-waf-and-firewalls
- 05-dns-bgp-security
slug: 06-defense-in-depth
sources:
- https://blog.cloudflare.com/ddos-threat-report-2025-q4/
- https://www.cisa.gov/news-events/alerts/2023/10/10/http2-rapid-reset-vulnerability-cve-2023-44487
- https://datatracker.ietf.org/doc/html/rfc5961
spiral:
- statefulness
- latency
status: ready
summary: No single defense stops all DDoS vectors — anycast edge absorption, rate
  limiting, WAF, mTLS, and adaptive load shedding form the layers; attack economics
  favor the attacker only if you defend alone.
title: Defense-in-depth architecture and attack economics
track: networking
unit: 11-network-security
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import DebugLog from "~/components/pedagogy/DebugLog.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import DesignPrompt from "~/components/pedagogy/DesignPrompt.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook>
You deployed a CDN, rate limiting, and a WAF. Then the attacker switches to cache-miss HTTP floods targeting your most expensive database query, spreading across 10,000 IPs each under your per-IP limit. No single defense catches it. You need to understand how the layers interact and when to escalate to humans.
</Hook>

<Crux>No single defense stops all DDoS. Layer anycast edge, rate limits, WAF, mTLS, adaptive concurrency, and CDN to invert attack economics.</Crux>

<Explanation>

**Defense-in-depth architecture: the full stack.** No single defense stops all attacks. The layered approach:

1. **Anycast edge scrubbing** — every CDN PoP is an active scrubbing center. Attacks are ingested at the nearest PoP rather than concentrated on the origin. Combined capacity across 330+ Cloudflare PoPs exceeds 37 Tbps. A 10 Gbps attack becomes a rounding error spread across many nodes.
2. **Stateless L3/L4 rate limits** — per-ASN, per-prefix rate limits drop obvious amplification sources and SYN flood sources before TCP state is created.
3. **WAF at the edge** — detects application-layer patterns (SQLi, XSS, bot fingerprints). Running at PL2 (balanced false-positive/coverage), not PL4 (paranoid) for a public API.
4. **Token bucket per IP + per user** — stops obvious botnets and authenticated user abuse.
5. **Adaptive concurrency limiting at origin** — when in-flight requests exceed capacity threshold, reject new requests with fast 503. Service remains stable; users get errors instead of timeouts.
6. **Observability and human escalation** — when automated defenses fail, on-call engineers add custom rules.

<div data-lesson-visual class="overflow-x-auto my-4">
  <table class="text-sm w-full border-collapse">
    <thead>
      <tr class="bg-gray-100">
        <th class="border p-2 text-left">Layer</th>
        <th class="border p-2 text-left">What it stops</th>
        <th class="border p-2 text-left">What it misses</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border p-2">Anycast edge</td>
        <td class="border p-2">Volumetric floods (Gbps-scale)</td>
        <td class="border p-2">Intelligent low-rate attacks per IP</td>
      </tr>
      <tr>
        <td class="border p-2">Stateless L3/L4 rate limits</td>
        <td class="border p-2">Amplification, SYN floods</td>
        <td class="border p-2">HTTP-level attacks on valid ports</td>
      </tr>
      <tr>
        <td class="border p-2">WAF (PL2)</td>
        <td class="border p-2">Known attack signatures, bot patterns</td>
        <td class="border p-2">Zero-days, obfuscated payloads, business-logic abuse</td>
      </tr>
      <tr>
        <td class="border p-2">Rate limit (per IP/user)</td>
        <td class="border p-2">Obvious botnets, auth abuse</td>
        <td class="border p-2">Distributed botnets with residential proxies</td>
      </tr>
      <tr>
        <td class="border p-2">Adaptive concurrency</td>
        <td class="border p-2">Distributed overload, cache-miss floods</td>
        <td class="border p-2">Attacks below the overload threshold</td>
      </tr>
      <tr>
        <td class="border p-2">mTLS</td>
        <td class="border p-2">Lateral movement inside the network</td>
        <td class="border p-2">External-facing attack vectors</td>
      </tr>
    </tbody>
  </table>
</div>

**mTLS in service meshes with SPIFFE.** Istio or Linkerd deploy sidecar proxies on every pod. The control plane (Istiod) runs a SPIFFE-compatible certificate authority. At startup, each sidecar receives a short-lived certificate (1–24 hours). Certificates rotate via SDS (Service Workload API) push — no sidecar restart needed. Every service-to-service call: (1) mTLS handshake (20–50 ms overhead per new connection on older hardware), (2) encrypted payload, (3) both sides verify certificates. Prevents lateral movement if the pod network is compromised. Cost: cert rotation adds operational complexity and monitoring burden (expired cert = infrastructure incident, not a bug).

**Protocol/state-exhaustion in depth.** SYN floods: each SYN allocates a half-open connection slot in the server's backlog. When backlog overflows, the server drops new SYN packets from legitimate clients. SYN cookies encode connection state as a cryptographic cookie in the ISN — no memory allocated; legitimate clients reply with a valid ACK that decodes the cookie. ACK floods: RST rate limiting (limit RSTs per second) and firewall suppression of unmatched ACKs. TCP RST injection (on-path MITM): RFC 5961 challenge-ACK forces the attacker to know the exact sequence number rather than just be in-window.

**Rate limiting internals: distributed systems complexity.** Token bucket with Redis backing: `T = min(C, T + R * delta_time)`. Distributed: each request atomically `INCR key; EXPIRE key window`. At 100k req/sec, Redis adds 0.5–1 ms per request = 50–100 ms total added latency. Mitigation: local per-server counter + periodic Redis sync (accepts slight inaccuracy, cuts to microsecond decisions). HyperLogLog for approximate rate limiting: ~1.6 kB per sketch, ~2% error, suitable for ASN-level or IP-range limits.

**Adaptive concurrency: load shedding formula.** Track in-flight requests `Q`. Set max_queue threshold. For new requests, survival probability = `max(0, 1 - Q / max_queue)`. Accept the request with that probability. This creates graceful degradation: at 50% overload, 50% of new requests are rejected; at 100% overload, all new requests are rejected. Users see fast 503s instead of queue timeouts (which can be 30+ seconds). Circuit breakers reject requests to backends with recent failure rates above threshold — preventing cascading failure across the entire service graph.

<TraceScenario
  id="sr-trace-1"
  lessonSlug="06-defense-in-depth"
  lang="en"
  scenario="Trace a sophisticated attack using amplification + application-layer tactics."
  steps={[
    { prompt: "Step 1: attacker starts a 10 Gbps memcached amplification attack. What happens without a CDN?", reveal: "Traffic hits your origin pipe; requests timeout; service is down. Without a CDN, you pay for bandwidth and have no defense against the volume." },
    { prompt: "Step 2: you migrate to a CDN (Cloudflare). The 10 Gbps is absorbed at the edge. What is the attack cost to the attacker?", reveal: "The CDN's amplification detection (response/request ratio anomaly) drops memcached traffic. The attacker would need legitimate HTTP traffic to bypass this layer." },
    { prompt: "Step 3: attacker switches to HTTP floods — legitimate-looking GET requests, 100,000 req/sec from a botnet. What does rate limiting do?", reveal: "Rate limiter at the CDN edge rejects IPs doing >10,000 req/sec. Each botnet IP is throttled, but with 1,000 botnet IPs doing 100 req/sec each, the attack still lands. Legitimate users average 10 req/s; 100 req/s is borderline." },
    { prompt: "Step 4: attacker adds ?x=random to every request (bypassing cache), targeting expensive database endpoints. Cache-hit rate drops from 95% to 5%. What is the new problem?", reveal: "Origin is overloaded by cache-miss requests hitting the database. Rate limiting per IP is insufficient — no single IP does 100,000 req/sec. Adaptive concurrency limiting rejects when in-flight > threshold; WAF detects the ?x=random pattern." },
    { prompt: "Step 5: attacker adapts the random param with realistic User-Agents and spreads further. Service is still slow. What is the operational response?", reveal: "Escalate to on-call engineer. This is no longer a bot attack the automation can handle. Operator tuning: raise WAF paranoia level, add a custom rule for the attack fingerprint, or accept degradation until the attacker loses interest (attacks are expensive to sustain)." },
  ]}
/>

<DebugLog
  id="sr-debug-1"
  lessonSlug="06-defense-in-depth"
  lang="en"
  title="WAF anomaly scoring during an attack"
  output={`2026-05-15 14:23:00 | requests=45000/sec | score-p50=0.5 | score-p95=3.2 | score-p99=8.1
2026-05-15 14:24:00 | requests=120000/sec | score-p50=6.1 | score-p95=14.2 | score-p99=28.5
2026-05-15 14:25:00 | requests=450000/sec | score-p50=12.3 | score-p95=19.8 | score-p99=32.1
2026-05-15 14:26:00 | blocked=380000 | allowed=70000 | score-threshold=5`}
  outputLang="log"
  question="The WAF is scoring traffic and blocking at threshold=5. What is happening in this timeline, and what should the operator do?"
  hint="Look at the request rate climb (45k → 120k → 450k) and the anomaly score distribution (p99 climbing from 8 to 28+). What does the block decision at 14:26 mean?"
  answer="This is an application-layer attack. Requests climb 10x in 3 minutes; anomaly scores spike (p99=28.5 at peak). The WAF blocks at threshold=5, allowing 70k/sec through — the threshold is too permissive given p99=28. Operator should: (1) lower threshold to 8–10; (2) enable PL2–3 for stricter rules; (3) if too many false positives, focus on rate limiting instead; (4) identify the attack pattern (identical headers? unusual User-Agents? missing Referers?) and add a signature rule targeting it."
/>

**Attack economics.** Attacker cost: ~$50–500/month for a botnet service capable of 10 Gbps sustained. Origin defense cost: ~$500/month CDN bill for 10 Gbps sustained traffic. If the attacker can generate 100 Gbps, the origin cannot match. But a CDN with global PoPs ingests 100s of Tbps and spreads the cost across millions of customers — per-customer cost is tiny. The economics favor the attacker only if you defend alone. Sharing infrastructure (CDN) is the answer: you cannot out-scale a botnet; you can make the attack economically unattractive by making it fail.

**Observability at attack time.** Key signals: (1) request rate per second — 10x normal is suspicious; (2) geographic distribution of sources — all from one ASN is suspicious; (3) anomaly score p99 — normal users score &lt;1, attack traffic scores &gt;10; (4) cache-hit rate — attack traffic targeting unique parameters shows sudden drop. Alert thresholds: request rate 10x baseline, anomaly score p99 spike, source-IP entropy drop (100 IPs instead of 100,000), or cache-hit rate drop below 80% during a traffic spike. Human escalation if attack persists >5 minutes or exceeds 50 Gbps.

<TradeoffMatrix
  id="sr-tradeoff-1"
  lessonSlug="06-defense-in-depth"
  lang="en"
  prompt="An e-commerce service is under sustained application-layer attack. You must pick a defense strategy."
  options={[
    { name: "WAF at PL4 (paranoia level 4)", summary: "Strict inspection, high false positives.", justification: "Catches 95% of attacks but blocks 5% of legitimate customers. Black Friday is in 3 days — you cannot afford to block 5% of sales." },
    { name: "Rate limiting (10 req/sec per IP) + token bucket", summary: "Cheap, low false positives.", justification: "Blocks only obvious botnets. Intelligent attackers using residential proxies (10 different IPs per attacker) bypass this — each IP stays under 10 req/sec." },
    { name: "Adaptive concurrency limiting + load shedding", summary: "Reject when in-flight > 5,000 requests.", correct: true, justification: "When the origin gets overloaded (queue backs up), reject new requests with a fast 503. Legitimate users get through; the attack fails because rejections cascade. False positives only happen when your service is already broken — which is bad anyway." },
    { name: "Increase origin capacity 10x", summary: "Scale vertically or horizontally.", justification: "Brute-force approach. Works if the attack is smaller than your new capacity, but if the attack scales to match, it only delays the problem and wastes money." },
  ]}
/>

<DesignPrompt
  id="sr-design-1"
  lessonSlug="06-defense-in-depth"
  lang="en"
  prompt="Design the DDoS defense architecture for a 100 Gbps-capable video CDN serving global users. The CDN operates 50 PoPs in 30 countries."
  constraints={[
    "Absorb 100+ Gbps attacks at the edge without exceeding 10% of any PoP's capacity.",
    "Defend against volumetric (L3/L4), protocol (SYN floods), and application-layer (HTTP floods) attacks.",
    "Maintain p50 latency &lt; 50 ms and p99 &lt; 200 ms for legitimate users during attack.",
    "Detect and block new attack patterns within 60 seconds.",
  ]}
  canonical="Edge anycast absorption: All 50 PoPs are active scrubbing centers. Attack traffic is ingested at the nearest PoP — not rerouted to a centralized center. Each PoP can absorb ~2 Gbps before hitting 10% capacity; combined = 100 Gbps. Volumetric: stateless L3/L4 rate limits per-ASN, per-prefix. Discard amplification responses with suspicious response/request ratios. Protocol: SYN flood detection (if SYN rate from one ASN > 100k/sec, rate-limit); RST throttling (if RST rate > 10k/sec from one IP, rate-limit). Application: token bucket at 10,000 req/sec per IP globally, 1,000 per PoP locally. WAF anomaly scoring at PL2. Cache-hit rate tracking: if below 80% during spike, enable adaptive limiting. Detection: flow-data aggregation per PoP (NetFlow exports to central analytics). Alert on request rate >10x baseline, anomaly score p99 spike, source-IP entropy drop, or geographic anomaly. Response: automatic rate limiting escalation every 30 seconds; manual on-call escalation if attack persists >5 minutes or exceeds 50 Gbps at any PoP."
  keyPoints={[
    "Anycast ingestion distributes attack load; no single point of failure.",
    "Stateless L3/L4 filtering catches 80% of volumetric attacks with minimal CPU.",
    "WAF at PL2 balances detection vs false positives for a public API.",
    "Adaptive limiting rejects on overload, not preemptive IP blocking.",
    "Human escalation at 5 minutes catches novel attack patterns.",
    "Measure false-positive rate and origin load continuously.",
  ]}
/>

<Inset kind="why" lang="en">
Why is adaptive concurrency limiting the preferred answer for a Black Friday e-commerce attack, not WAF PL4? WAF PL4 makes preemptive decisions at the request level based on content patterns. If those patterns are wrong (5% false positive), you block paying customers. Adaptive concurrency limiting makes reactive decisions at the system level based on actual load. It never blocks a request preemptively — it only rejects when the system is already overloaded (which is bad regardless of attack). The tradeoff: adaptive limiting accepts that some attack requests go through until the system hits capacity, then rejects everything equally. That is acceptable when the alternative is blocking 5% of legitimate Black Friday customers.
</Inset>

<Quiz
  id="sr-quiz-1"
  lessonSlug="06-defense-in-depth"
  lang="en"
  question="Your WAF is at Paranoia Level 2 and attacks get through (only 70% blocked). You raise it to PL4. Legitimate customers now complain (5% false positives). What is a better approach?"
  choices={[
    { label: "Accept the 5% false positive rate as the cost of security", misconception: "False positives have hidden costs: customer churn, support tickets, revenue loss. Not acceptable for a production service." },
    { label: "Keep PL2 and accept 30% attack bypass", misconception: "Accepting bypass means service degradation continues and you solve nothing." },
    { label: "Combine PL3 with rate limiting and adaptive load shedding", correct: true },
    { label: "Deploy a second WAF from a different vendor alongside the current one", misconception: "Two WAFs with high false positive rates compound the problem." },
  ]}
/>

</Explanation>

<KeyTakeaway>Defense-in-depth stacks anycast edge, stateless L3/L4 filters, WAF at PL2, per-IP/user rate limits, and adaptive concurrency limiting at origin. No single layer stops all attacks. Attack economics favor defenders when using shared CDN infrastructure — a startup cannot absorb 100 Gbps alone; a CDN amortizes it across millions of customers.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-defense-in-depth-retrieval"
  lang="en"
  questions={[
    { q: "Why is adaptive concurrency limiting preferable to WAF PL4 for a high-traffic production service under attack?", a: "WAF PL4 makes preemptive decisions based on content pattern matching, causing 5% false positives — 1 in 20 legitimate users blocked. Adaptive concurrency limiting reacts to actual system load, only rejecting requests when the server is already overloaded. No legitimate user is blocked pre-emptively; the cost is that some attack traffic gets through until overload is reached, but fast 503s protect the origin from cascade failure. For a public service, false positives (blocking real users) are worse than false negatives (letting some attack traffic through) when the attack is bounded by concurrency limits." },
    { q: "During a Rapid Reset attack (CVE-2023-44487), why does the attack bypass HTTP/1.1-only rate limiting?", a: "HTTP/1.1 rate limiting typically counts completed requests (one request = one response). HTTP/2 Rapid Reset sends a stream and resets it before a response is sent. The server allocates CPU/memory for the stream anyway. A rate limiter counting only responses never sees the cancelled streams. The fix: rate-limit by stream creation rate (both completed and reset streams count) or by in-flight request count. Google, nghttp2, and OpenSSL shipped patches in October 2023 adding stream-reset rate limiting." },
    { q: "What metrics should an on-call engineer monitor during a DDoS attack and what thresholds signal escalation?", a: "Key metrics: (1) request rate per second — 10x normal is suspicious; (2) anomaly score p99 — should be <1 for normal users, >10 for attack traffic; (3) source-IP entropy — all traffic from 100 IPs instead of 100,000 suggests a concentrated botnet; (4) cache-hit rate — sudden drop during a traffic spike suggests cache-bypass attack; (5) origin queue depth — rising queue means the attack is reaching the origin. Escalation thresholds: request rate 10x baseline, anomaly p99 spike, source-IP entropy drop, cache-hit rate below 80%, or attack persists >5 minutes." },
  ]}
/>

<Recap lang="en">
Defense-in-depth against DDoS requires stacking multiple layers because no single defense stops all vectors. Anycast edge absorption distributes volumetric attacks across 330+ global PoPs; stateless L3/L4 filters drop amplification and SYN floods before they consume connection state; WAF at PL2 detects known application-layer patterns with tolerable false positives; adaptive concurrency limiting at the origin rejects on overload rather than preemptive IP blocking. Attack economics favor defenders only when using shared CDN infrastructure — a botnet generating 100 Gbps for $500/month is defeated by a CDN that amortizes defense capacity across millions of customers. When automated defenses fail, observability (request rate, anomaly scores, cache-hit rate, source-IP entropy) gives on-call engineers the signal they need to add custom rules within the 60-second escalation window.
</Recap>
