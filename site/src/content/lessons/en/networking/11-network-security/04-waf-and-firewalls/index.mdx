---
concepts:
- waf
- firewall
- owasp-crs
- paranoia-level
- anomaly-scoring
- mtls
- spiffe
- hsts
- hsts-preload
- ssl-stripping
deepensInto:
- 06-defense-in-depth
estMin: 14
lang: en
lessonType: topic
level: middle
order: 4
prereqs:
- 01-ddos-what-and-why
- 05-tls-handshake/01-tls-what-and-why
- 03-rate-limiting-algorithms
slug: 04-waf-and-firewalls
sources:
- https://owasp.org/www-project-modsecurity-core-rule-set/
- https://www.cloudflare.com/learning/ddos/ddos-attack-tools/slowloris/
- https://datatracker.ietf.org/doc/html/rfc6797
spiral:
- statefulness
- latency
status: ready
summary: Firewalls block L3/L4 nonsense; WAFs inspect HTTP content against attack
  patterns; mTLS authenticates service-to-service identity; HSTS preload prevents
  TLS downgrade attacks.
title: WAFs, firewalls, mTLS, and HSTS
track: networking
unit: 11-network-security
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import paranoiaTradeoff from "./infographics/paranoia-tradeoff.svg?raw";

<Hook>
Your firewall blocks port scans and obvious nonsense. But the attacker is sending valid HTTP requests to port 443. The firewall passes them all — it cannot see inside the encrypted TLS payload. You need a different tool for application-layer attacks, one that understands HTTP: a WAF.
</Hook>

<Crux>Firewalls block L3/L4; WAFs block HTTP. Neither alone is enough. mTLS cryptographically identifies services; HSTS preload defeats downgrade.</Crux>

<Explanation>

**Firewalls vs WAFs: the fundamental difference.** A firewall (L3/L4) inspects IP and TCP headers: source IP, destination port, TCP flags. It blocks port scanning, IP spoofing, and protocol violations. It cannot see inside encrypted HTTPS payloads. A Web Application Firewall (WAF, L7) inspects HTTP request/response content after TLS termination: headers, query parameters, request bodies — matching against known attack patterns (SQL injection, XSS, path traversal). A WAF must see plaintext to inspect it, so it terminates TLS at the edge or sits on the origin behind TLS termination. Both are necessary: the firewall stops network-level nonsense, the WAF stops application-level attacks.

<div data-lesson-visual class="overflow-x-auto my-4">
  <table class="text-sm w-full border-collapse">
    <thead>
      <tr class="bg-gray-100">
        <th class="border p-2 text-left">Tool</th>
        <th class="border p-2 text-left">Layer</th>
        <th class="border p-2 text-left">What it inspects</th>
        <th class="border p-2 text-left">What it stops</th>
        <th class="border p-2 text-left">What it misses</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border p-2">Firewall</td>
        <td class="border p-2">L3/L4</td>
        <td class="border p-2">IP src/dst, TCP flags, port</td>
        <td class="border p-2">Port scans, spoofed IPs, protocol violations</td>
        <td class="border p-2">Encrypted payloads, valid HTTPS attacks</td>
      </tr>
      <tr>
        <td class="border p-2">WAF</td>
        <td class="border p-2">L7</td>
        <td class="border p-2">HTTP headers, query params, body</td>
        <td class="border p-2">SQLi, XSS, path traversal, bot patterns</td>
        <td class="border p-2">L3/L4 floods, SYN floods, IP-level attacks</td>
      </tr>
    </tbody>
  </table>
</div>

**WAF detection modes.** How does a WAF tell a SQL injection attempt from a normal search query containing the word "union"? The answer depends on the detection mode you choose. Signature-based: match against a database of known attack patterns. OWASP ModSecurity Core Rule Set (CRS) contains patterns like `union select` (SQL injection), `<script>` (XSS), and `../../../etc/passwd` (path traversal). Fast and low false positives on known traffic, but misses zero-days and obfuscated patterns (e.g., `un/**/ion select`).

Anomaly/ML-based: model normal traffic, flag statistical outliers. Catches novel attacks but requires tuning — too strict and legitimate users get blocked, too loose and attacks slip through.

**OWASP CRS anomaly scoring.** Each rule in OWASP CRS assigns a score (1–8 points). A request is blocked when the total score >= 5. Paranoia Levels (PL1–PL4) control how many rules are active: PL1 is permissive (fewer false positives, misses some attacks); PL4 is strict (catches more, but blocks more legitimate users too).

<NumbersCard
  id="false-positive-tradeoff"
  title="WAF tuning tradeoff"
  stats={[
    { label: "Legitimate requests blocked (PL1)", value: "~0.1% false positive" },
    { label: "Legitimate requests blocked (PL2)", value: "~0.5%" },
    { label: "Legitimate requests blocked (PL3)", value: "~2%" },
    { label: "Legitimate requests blocked (PL4)", value: "~5%" },
    { label: "Attacks blocked (PL1)", value: "~70% coverage" },
    { label: "Attacks blocked (PL4)", value: "~95% coverage" },
  ]}
/>

**WAF tuning in production.** Start at PL1 in detection mode (log and alert but do not block) for 1–2 weeks. Collect baseline metrics: anomaly score distribution on real traffic, rule hits by category. Then raise the paranoia level and observe the false-positive rate. If false positives exceed tolerance, tune rules: whitelist known false positives, adjust scoring weights, or exclude low-confidence rules. Target: block 95%+ of attacks while affecting less than 0.1% of real users.

<Infographic
  svg={paranoiaTradeoff}
  label="PL1 versus PL4 compared on two metrics: PL1 gives about 70% attack coverage at about 0.1% legitimate requests blocked, while PL4 gives about 95% coverage at about 5% blocked — a 50x jump in false positives for 25 more points of coverage."
  caption="Raising the paranoia level buys attack coverage at the cost of false positives — which is why you start at PL1 in detection mode and tune toward 95% coverage at under 0.1% blocked."
  hue="var(--d-infra)"
/>

<Quiz
  id="mid-quiz-1"
  lessonSlug="04-waf-and-firewalls"
  lang="en"
  question="Why is a WAF ineffective against attacks like SYN floods?"
  choices={[
    { label: "Because WAF only inspects L7 (application layer), but SYN floods happen at L4 (transport)", correct: true },
    { label: "Because SYN flood attacks are encrypted", misconception: "SYN floods use TCP flags, not payload encryption." },
    { label: "Because WAF cannot distinguish legitimate SYN from attack SYN", misconception: "Both are true, but the L4 vs L7 distinction is the fundamental reason." },
    { label: "Because firewalls cannot be combined with WAF", misconception: "Both layers are necessary and should be used together." },
  ]}
/>

**mTLS for service-to-service identity.** Mutual TLS: both client and server present X.509 certificates to each other, not just the server to the client. In a service mesh (Istio, Linkerd), every sidecar proxy uses mTLS to talk to peer sidecars, enforcing zero-trust: a request from Pod A to Pod B is encrypted and authenticated. SPIFFE (Secure Production Identity Framework for Everyone) issues short-lived certificates (often ~1-hour expiration) via an SDS (Service Discovery Service) that the sidecar loads and rotates automatically. The sidecar key never leaves the local container; the control plane refreshes the cert out-of-band.

Cost: each new connection handshake adds 20–50 ms on older hardware; cert rotation adds operational complexity. Benefit: lateral movement is prevented even if the container network is compromised.

**TLS stripping and HSTS.** SSL stripping (Moxie Marlinspike, 2009): a MITM keeps the user on HTTP while talking HTTPS to the real server, decrypting both directions. The user never knows they are on plaintext. Defense: HTTP Strict Transport Security (HSTS) — the header `Strict-Transport-Security: max-age=31536000` tells the browser "only use HTTPS for this origin for the next year." But the header is delivered over HTTP in the first response — if the first request is stripped, the header is stripped too.

**HSTS preload.** A browser-shipped list of domains that must always be HTTPS, enforced before the domain is ever visited. Google, Firefox, Chrome, and Safari ship preload lists. You request inclusion for your domain; it ships in the next browser release. From then on, HTTPS is enforced even for first-time visitors. Minimum max-age for preload: 31,536,000 seconds (1 year). The 1-year requirement exists because preload ships in browser releases (4-week cycles for Chrome) — a shorter max-age would expire before users install the next update.

<TraceScenario
  id="mid-trace-1"
  lessonSlug="04-waf-and-firewalls"
  lang="en"
  scenario="Trace the defense-in-depth approach to stopping a DDoS attack."
  steps={[
    { prompt: "Step 1: 100 Gbps volumetric attack hits your origin IP. You have no CDN. What happens?", reveal: "Your origin pipe is saturated. No legitimate requests reach the server. Your service is down." },
    { prompt: "Step 2: now you sign up for Cloudflare (a CDN with DDoS scrubbing). What changes?", reveal: "Traffic is routed to the nearest Cloudflare PoP (scrubbing center) first. Cloudflare absorbs the attack across its infrastructure and proxies only clean traffic to your origin." },
    { prompt: "Step 3: attacker now sends HTTP floods (smaller packets, looks more legitimate). What does Cloudflare do?", reveal: "A rate limiter at the edge rejects requests from IPs making more than N requests per second. Legitimate users average 10–20 req/s; attackers try thousands per second." },
    { prompt: "Step 4: attacker uses 10k IPs simultaneously, each under the per-IP rate limit. What is the next layer?", reveal: "The WAF detects that requests have identical payloads, headers, or patterns — impossible for real diverse users. It blocks the pattern using signature-based or anomaly-score rules." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  lessonSlug="04-waf-and-firewalls"
  lang="en"
  question="Why does HSTS preload require a minimum max-age of 1 year rather than 1 month?"
  choices={[
    { label: "Because browsers only check HSTS once per year", misconception: "Browsers check HSTS on every request; the max-age controls how long they remember it." },
    { label: "Because browser releases ship every 4 weeks and a 1-month max-age could expire before users install the next update", correct: true },
    { label: "Because the HSTS header is cached by CDNs for 1 year", misconception: "HSTS is a browser policy, not a CDN cache directive." },
    { label: "Because 1 year is required to get DNSSEC validation", misconception: "HSTS and DNSSEC are independent mechanisms." },
  ]}
/>

<Inset kind="why" lang="en">
Why is mTLS the correct answer for zero-trust microservices, not network-level IP allowlisting? IP allowlisting only proves "this packet came from a pod in subnet X" — it does not prove which service sent it. If any pod in the allowed subnet is compromised, it can impersonate any other service. mTLS proves "this request came from a pod that holds a valid certificate for service Y, issued by our control plane at timestamp T." Short-lived certificates (1 hour) mean that even if a cert is stolen, it expires quickly. Network-level controls are a fallback; cryptographic identity is the correct primitive.
</Inset>

</Explanation>

<StackDiagram
  label="Inbound traffic passes top to bottom: firewall inspects L3/L4 headers, TLS terminates, the WAF inspects L7 HTTP content, then clean requests reach the origin"
  hue="var(--d-network)"
  layers={[
    { label: "Firewall (L3/L4)", note: "IP, port, TCP flags" },
    { label: "TLS termination", note: "decrypt to plaintext" },
    { label: "WAF (L7)", note: "HTTP body, params, headers" },
    { label: "Origin", note: "only clean requests" },
  ]}
  caption="Each request passes through both layers in order. The firewall drops port scans, spoofed IPs, and protocol violations but cannot see inside encrypted payloads; after TLS termination the WAF inspects plaintext HTTP for SQLi, XSS, and path-traversal patterns. Neither layer alone is sufficient — the firewall misses HTTP attacks on port 443, the WAF misses L3/L4 floods."
/>

<KeyTakeaway>Firewalls block L3/L4 attacks; WAFs block L7 attacks by inspecting HTTP content. Neither alone is sufficient. mTLS enforces cryptographic service identity in microservices. HSTS preload prevents TLS downgrade attacks before the first visit. Start WAF at PL1 detection mode, then tune to less than 0.1% false positives before blocking.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-waf-and-firewalls-retrieval"
  lang="en"
  questions={[
    { q: "Explain the difference between RPKI and DNSSEC. Why do they protect different layers?", a: "RPKI (Resource Public Key Infrastructure) protects BGP (L3): it authorizes which AS can announce which IP prefix, preventing route hijacking. DNSSEC (DNS Security Extensions) protects DNS responses (L4 application): it cryptographically signs DNS RRsets, preventing cache poisoning. BGP hijacking redirects traffic at the routing level; DNSSEC stops forged DNS responses. They protect different problems — RPKI is routing integrity, DNSSEC is DNS response integrity. You need both." },
    { q: "What is SSL stripping and why does HSTS preload solve the problem that a regular HSTS header cannot?", a: "SSL stripping downgrades a user's HTTPS connection to HTTP by intercepting the first HTTP request and keeping the user on HTTP while the attacker talks HTTPS to the server. A regular HSTS header is sent in the HTTP response — if the first request is already stripped, the header never reaches the browser. HSTS preload ships a list of HTTPS-only domains in the browser binary itself, enforcing HTTPS before any request is ever made, making SSL stripping impossible for preloaded domains." },
    { q: "Your WAF is at PL2 and attacks are getting through. You raise it to PL4. False positives jump to 5%. What is a better strategy than accepting either extreme?", a: "Combine PL3 with rate limiting and adaptive load shedding. PL3 catches more attacks than PL2 (~90% vs ~80%) with fewer false positives than PL4 (~2% vs ~5%). Add token-bucket rate limiting at the edge to stop volume-based attacks before they reach the WAF. Add adaptive concurrency limiting at the origin to reject overload without needing perfect WAF accuracy. This multi-layer approach reduces reliance on any single tool being tuned perfectly." },
  ]}
/>

<Recap lang="en">
Firewalls and WAFs operate at different layers — firewalls block IP/TCP-level nonsense, WAFs block HTTP-level attacks (SQLi, XSS, path traversal) using OWASP CRS anomaly scoring. WAF paranoia levels trade false-positive rate against attack coverage: PL1 is 0.1% false positives but 70% coverage; PL4 is 95% coverage but 5% false positives. Start in detection mode and tune before blocking. mTLS provides cryptographic service identity in microservices, preventing lateral movement even if the network is compromised; SPIFFE automates short-lived cert issuance and rotation. HSTS preload enforces HTTPS before the first visit, defeating SSL stripping entirely — but requires a 1-year max-age and explicit registration in browser preload lists. Now when you inherit a WAF configured at PL4 and see a flood of support tickets about blocked legitimate requests, you know exactly where to start: switch to detection mode, collect two weeks of baseline data, and tune down from there.
</Recap>
