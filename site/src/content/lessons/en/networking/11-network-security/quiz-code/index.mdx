---
concepts:
- syn-cookies
- token-bucket-redis
- http2-rapid-reset
- waf-anomaly-scoring
estMin: 14
lang: en
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://datatracker.ietf.org/doc/html/rfc4987
- https://www.cisa.gov/news-events/alerts/2023/10/10/http2-rapid-reset-vulnerability-cve-2023-44487
- https://owasp.org/www-project-modsecurity-core-rule-set/
status: ready
summary: "Read real config, a distributed rate-limiter snippet, an HTTP/2 server setting, and a WAF log line — predict the security behaviour and pick the highest-leverage fix."
title: 'Network security: config and log reading'
track: networking
unit: 11-network-security
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Security incidents are diagnosed in config files, limiter code, and log lines — not in slide decks. Read each artifact, predict how it behaves under attack, and choose the fix a senior engineer would make first.
</Hook>

<Goal lang="en">
Practise the loop you run in every incident: read the artifact, find the gap between what it does and what the operator assumed, and reach for the highest-leverage change.
</Goal>

## Snippet 1 — the SYN-flood sysctl

```ini
# /etc/sysctl.d/hardening.conf
net.ipv4.tcp_max_syn_backlog = 4096
net.core.somaxconn          = 4096
net.ipv4.tcp_synack_retries = 5
net.ipv4.tcp_syncookies     = 0   # disabled
```

<Quiz
  id="11-network-security-code-1"
  lessonSlug="11-network-security"
  lang="en"
  question="An ops engineer 'hardened' the host by growing the SYN backlog to 4096 and left tcp_syncookies=0. Under a spoofed SYN flood, what happens, and what is the highest-leverage fix?"
  choices={[
    { label: "A bigger backlog only buys a few more milliseconds — a flood fills 4096 half-open slots just as fast, and with syncookies off the kernel drops legitimate SYNs once full. Set tcp_syncookies=1 so the kernel encodes connection state in the ISN and allocates no slot under overflow.", correct: true },
    { label: "4096 slots is plenty; the host is fully protected and no change is needed.", misconception: "Backlog size is linear headroom; a flood sending millions of SYNs/sec saturates any fixed backlog in milliseconds. The structural fix is SYN cookies, not a larger table." },
    { label: "Raise tcp_synack_retries higher so half-open connections are kept longer and eventually complete.", misconception: "More retries keeps spoofed half-open entries alive longer, worsening exhaustion. You want them gone faster, and cookies remove the slot entirely." },
    { label: "Disabling syncookies is correct because cookies break legitimate clients behind NAT.", misconception: "SYN cookies are a fallback that engages only on backlog overflow and interoperate with normal clients; the real cost is a small hash computation per ACK, not broken NAT clients." },
  ]}
/>

## Snippet 2 — the distributed token bucket

```lua
-- per-request limiter, called on every API hit
local count = redis.call("INCR", key)
if count == 1 then
  redis.call("EXPIRE", key, window_seconds)
end
if count > limit then
  return DENY
end
return ALLOW
```

<Quiz
  id="11-network-security-code-2"
  lessonSlug="11-network-security"
  lang="en"
  question="This Redis-backed limiter runs on every request across a fleet of app servers. Under a 100k req/sec attack, what is the operational problem, and what is the standard mitigation?"
  choices={[
    { label: "Every decision is a synchronous Redis round-trip (~0.5–1 ms), so at 100k req/sec the limiter adds latency and makes Redis a hot single dependency. Mitigation: a local per-server counter for the fast path with periodic Redis sync, trading slight global inaccuracy for microsecond decisions.", correct: true },
    { label: "INCR is not atomic, so concurrent requests corrupt the counter and the limit is meaningless.", misconception: "INCR is atomic in Redis — that is the reason it is used here. The issue is per-request network latency and Redis becoming a bottleneck, not atomicity." },
    { label: "The EXPIRE only fires on count==1, so the key never expires and memory leaks forever.", misconception: "Setting EXPIRE on first increment is the correct fixed-window idiom; the window's TTL is established once per window. There is no leak." },
    { label: "Nothing is wrong; Redis-backed counters are free and always the right default.", misconception: "They are correct but not free — each decision costs a round-trip. At high request rates you mitigate with local counters plus periodic global sync." },
  ]}
/>

## Snippet 3 — the HTTP/2 server config

```nginx
http2_max_concurrent_streams 128;
# no limit on the rate of stream creation / reset
keepalive_requests 100000;
```

<Quiz
  id="11-network-security-code-3"
  lessonSlug="11-network-security"
  lang="en"
  question="This config caps concurrent streams at 128 but never limits the rate of stream creation. Why does it stay vulnerable to HTTP/2 Rapid Reset (CVE-2023-44487)?"
  choices={[
    { label: "Rapid Reset opens a stream and immediately sends RST_STREAM before any response, so it never holds 128 concurrent streams — it churns create-then-cancel thousands of times per second, and the server still allocates CPU/memory per stream. The fix is to rate-limit stream creation (counting reset streams), not concurrency.", correct: true },
    { label: "The concurrency cap of 128 already stops Rapid Reset because the attacker cannot exceed 128 streams.", misconception: "The attack deliberately stays under the concurrency cap by cancelling each stream instantly; the cost is in the create/reset churn the server still pays, which the cap never sees." },
    { label: "keepalive_requests at 100000 is the vulnerability; lowering it stops Rapid Reset.", misconception: "Connection keepalive count is unrelated; Rapid Reset abuses stream lifecycle within a connection. The fix is bounding stream-creation/reset rate." },
    { label: "Rapid Reset is an L3/L4 volumetric attack, so an HTTP server setting cannot affect it.", misconception: "Rapid Reset is an application-layer (L7) resource-exhaustion attack on the HTTP/2 stream state machine; server-side stream-rate limiting is exactly the mitigation vendors shipped in 2023." },
  ]}
/>

## Snippet 4 — the WAF anomaly-score log

```
14:23 | req=45000/s  | score-p50=0.5 | score-p99=8.1  | threshold=5
14:24 | req=120000/s | score-p50=6.1 | score-p99=28.5 | threshold=5
14:25 | req=450000/s | score-p50=12.3| score-p99=32.1 | threshold=5
14:26 | blocked=380000 allowed=70000 | threshold=5
```

<Quiz
  id="11-network-security-code-4"
  lessonSlug="11-network-security"
  lang="en"
  question="Reading this WAF anomaly-scoring timeline (block when score &ge; threshold=5), which reading is correct?"
  choices={[
    { label: "This is an L7 attack: request rate climbs 10x in three minutes and even the median anomaly score crosses the threshold by 14:24. 70k req/sec still slips through because the threshold is permissive relative to a p99 of 32 — tighten the threshold and pair it with rate limiting rather than relying on the WAF alone.", correct: true },
    { label: "Traffic is healthy because the p50 score stays in single digits.", misconception: "By 14:24 the p50 is 6.1 — above the threshold of 5 — and p99 is 28.5. The whole distribution has shifted into attack territory; this is not healthy traffic." },
    { label: "The WAF is broken because it blocked 380k requests; it should never block that many.", misconception: "Blocking 380k of a 450k/sec flood is the WAF working. The concern is the 70k that still pass given how high p99 is — a tuning gap, not a malfunction." },
    { label: "threshold=5 means 5% of requests are blocked.", misconception: "The threshold is the anomaly-score cutoff (block when summed rule score &ge; 5), not a percentage of traffic." },
  ]}
/>

<Recap lang="en">
Every defense is read in config and logs. A bigger SYN backlog is linear headroom; SYN cookies are the structural fix. A Redis limiter is correct but every decision is a round-trip — mitigate with a local fast path plus periodic sync. Concurrency caps do not stop Rapid Reset; you must bound stream-creation rate including resets. And a WAF anomaly log tells you, at a glance, whether the score distribution has shifted into attack territory and whether your threshold is letting traffic through. Read the artifact, find the gap between intent and behavior, fix the highest-leverage one first.
</Recap>
