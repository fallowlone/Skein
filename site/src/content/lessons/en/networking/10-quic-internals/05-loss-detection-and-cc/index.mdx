---
concepts:
- monotonic-packet-numbers
- ack-ranges
- probe-timeout
- karn-algorithm
- cubic
- bbr
- pluggable-cc
deepensInto:
- 07-deployment-and-cpu
estMin: 12
lang: en
lessonType: topic
level: middle
order: 5
prereqs:
- 10-quic-internals/02-quic-streams-and-hol
- 10-quic-internals/03-handshake-and-1rtt
slug: 05-loss-detection-and-cc
sources:
- https://datatracker.ietf.org/doc/html/rfc9002
- https://datatracker.ietf.org/doc/html/rfc9000
- https://blog.cloudflare.com/the-road-to-quic/
spiral:
- latency
status: ready
summary: QUIC uses monotonically increasing packet numbers and pluggable congestion
  control — NewReno, CUBIC, BBR — eliminating Karn''''s algorithm ambiguity and enabling
  per-application CC upgrades.
title: Loss detection and congestion control
track: networking
unit: 10-quic-internals
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import RFCQuiz from "~/components/pedagogy/RFCQuiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import ccSpectrum from "./infographics/cc-spectrum.svg?raw";

<Hook>
TCP retransmits a dropped packet with the same sequence number. The ACK that comes back is ambiguous — did the server acknowledge the original send or the retransmit? QUIC gives every packet a unique number, and this single change makes loss detection unambiguous and congestion control pluggable.
</Hook>

<Crux>Monotonic QUIC packet numbers kill Karn's algorithm ambiguity; user-space placement makes congestion control pluggable per connection.</Crux>

<Explanation>

## Monotonic packet numbers

QUIC packet numbers are **strictly increasing integers** — a lost packet gets a **new** packet number on retransmit. Unlike TCP where retransmitted packets carry the same sequence number (creating ambiguity about which send the ACK acknowledges — Karn's algorithm workaround), QUIC never reuses a packet number.

**ACK ranges:** QUIC ACK frames list ranges of acknowledged packet numbers:
```
ack_ranges: [1000..1005, 1008..1010]
```
Gap at 1006 and 1007 means those packets are not yet received. This is equivalent to TCP SACK but native to the protocol.

**Loss declaration** happens when:
1. A later packet is acknowledged and a **reorder window** has expired (typically 1.5× max RTT after the out-of-order packet was sent).
2. Or the **PTO (Probe Timeout)** fires — analogous to TCP's RTO but computed per packet-number space.

The monotonic model is simpler than TCP's but requires retransmitted frames to be re-serialized with new packet numbers — a minor CPU cost.

<div data-lesson-visual class="rounded-lg border border-slate-200 bg-slate-50 p-4 my-4 text-sm space-y-2">
  <div class="font-semibold text-slate-700 mb-1">Packet number model: TCP vs QUIC</div>
  <div class="grid grid-cols-2 gap-4 text-xs font-mono">
    <div class="space-y-1">
      <div class="font-semibold text-slate-500">TCP (ambiguous)</div>
      <div>Send seq=100 → LOST</div>
      <div>Retransmit seq=100 → ACK 101</div>
      <div class="text-amber-600">ACK: original or retransmit?</div>
      <div class="text-amber-600">→ Karn's algorithm required</div>
    </div>
    <div class="space-y-1">
      <div class="font-semibold text-slate-500">QUIC (unambiguous)</div>
      <div>Send pkt#100 → LOST</div>
      <div>Retransmit as pkt#101 → ACK 101</div>
      <div class="text-green-600">ACK#101 = retransmit confirmed</div>
      <div class="text-green-600">→ No ambiguity, no workaround</div>
    </div>
  </div>
</div>

## Pluggable congestion control

Because QUIC lives in user space, **congestion control is pluggable**. RFC 9002 specifies NewReno as the baseline. Major implementations ship:

- **NewReno** — slow start, additive increase, halve cwnd on loss.
- **CUBIC** (quiche from Cloudflare, Linux kernel standard) — aggressive probing after loss with a cubic function.
- **BBR** (Google, YouTube, Cloudflare edges) — model bottleneck bandwidth and minimum RTT; ignore random loss.
- **HyStart++** — exit slow start early by detecting queueing, used with CUBIC.

Applications can select the algorithm at connection open. A single QUIC library can support multiple algorithms. This agility is impossible with kernel TCP where changing congestion control requires recompiling or sysctl changes across millions of servers. Together these four algorithms cover the full deployment spectrum — NewReno for safe defaults, CUBIC for aggressive LAN probing, BBR for high-throughput WAN; without pluggable CC, QUIC would be locked to the same algorithm for a decade the way kernel TCP has been.

<Infographic
  svg={ccSpectrum}
  label="A four-item list of QUIC congestion-control algorithms and their deployment niches: NewReno is the RFC 9002 safe-default baseline; CUBIC is the Cloudflare quiche and Linux-kernel standard for aggressive low-RTT LAN probing; BBR is the Google YouTube and Cloudflare-edge choice for high-throughput WAN that ignores random loss; HyStart++ pairs with CUBIC to exit slow start early"
  caption="Pluggable CC means one QUIC library carries the whole spectrum — each algorithm owns a niche, swappable per connection in user space with no kernel patch."
  hue="var(--d-infra)"
/>

**The trade-off:** Flexibility makes benchmarking hard — comparing two QUIC stacks with different CC algorithms is not a fair comparison of QUIC itself.

<Quiz
  id="05-mid-quiz-1"
  lessonSlug="05-loss-detection-and-cc"
  lang="en"
  question="Why does QUIC avoid Karn's algorithm, and what design choice makes this possible?"
  choices={[
    { label: "QUIC does not retransmit packets at all — it uses FEC instead", misconception: "QUIC does retransmit lost data but with new packet numbers. There is no FEC in the base spec." },
    { label: "QUIC uses monotonically increasing packet numbers — retransmits get new numbers so the ACK unambiguously identifies which send was received", correct: true },
    { label: "QUIC uses larger sequence numbers than TCP, reducing the probability of wrap-around", misconception: "Karn's algorithm is about retransmit ambiguity, not wraparound. The key is unique packet numbers per send." },
    { label: "QUIC is built on UDP which handles ACK disambiguation at the OS level", misconception: "UDP has no ACK mechanism at all. QUIC implements its own on top of UDP." },
  ]}
/>

<Quiz
  id="05-mid-quiz-2"
  lessonSlug="05-loss-detection-and-cc"
  lang="en"
  question="Why is pluggable congestion control in QUIC significant compared to kernel TCP?"
  choices={[
    { label: "Because QUIC runs over UDP which inherently allows any CC algorithm", misconception: "UDP is just a transport layer; the CC algorithm is an application-level choice in QUIC regardless of UDP." },
    { label: "Kernel TCP's CC algorithm requires OS recompile or sysctl to change; QUIC libraries can switch CC per-connection in user space without kernel involvement", correct: true },
    { label: "Because QUIC has more packet numbers than TCP, giving CC more data to work with", misconception: "The number of packet numbers is not the key differentiator. The key is user-space deployability." },
    { label: "QUIC's CC is pluggable because it runs faster than TCP's CC", misconception: "Speed is not the reason. User-space placement enables CC evolution without OS updates." },
  ]}
/>

<RFCQuiz
  id="05-rfc-1"
  lessonSlug="05-loss-detection-and-cc"
  lang="en"
  question="Which RFC defines QUIC's loss detection algorithm and congestion control baseline?"
  choices={[
    { rfc: "RFC 9002", title: "QUIC Loss Detection and Congestion Control", correct: true },
    { rfc: "RFC 9000", title: "QUIC: A UDP-Based Multiplexed and Secure Transport" },
    { rfc: "RFC 9001", title: "QUIC Cryptography" },
    { rfc: "RFC 9114", title: "HTTP/3" },
  ]}
/>

<Inset kind="why" lang="en">
**Why not just use TCP's congestion control in QUIC?** TCP's congestion control is implemented in the kernel's socket layer, where it has direct access to packet timestamps, kernel buffers, and NIC offload. Reimplementing it in user space (as QUIC does) costs CPU — each packet must be processed by user code without hardware assistance. The benefit: user space can iterate faster, can access application-level metadata (e.g., video stream priority) to make smarter CC decisions, and can be updated with the application without touching the OS.
</Inset>

<NumbersCard
  id="05-cc-numbers"
  title="QUIC congestion control algorithms"
  stats={[
    { label: "RFC 9002 baseline", value: "NewReno (slow start + AIMD)" },
    { label: "Cloudflare quiche default", value: "CUBIC (aggressive probing)" },
    { label: "Google YouTube / GCP", value: "BBR (bandwidth-RTT model)" },
    { label: "User-space CC update cycle", value: "weekly (vs monthly+ for kernel TCP)" },
    { label: "Per-connection CC selection", value: "at connection open" },
  ]}
/>

</Explanation>

<SequenceDiagram
  label="Monotonic packet numbers: the client sends packet 100 which is lost, retransmits the same data as a new packet 101, and the server's ACK 101 unambiguously identifies the retransmit — no Karn's-algorithm ambiguity"
  hue="var(--d-network)"
  actors={["Client", "Server"]}
  messages={[
    { from: "Client", to: "Server", label: "pkt#100 [STREAM data] — LOST" },
    { from: "Client", to: "Server", label: "pkt#101 [same data, new number]" },
    { from: "Server", to: "Client", label: "ACK 101 — retransmit confirmed" },
  ]}
  caption="Every send gets a new, strictly increasing packet number, so an ACK names exactly one transmission. The RTT sample from ACK 101 is always valid — unlike TCP, which reuses sequence numbers and must discard ambiguous retransmit samples (Karn's algorithm)."
/>

<KeyTakeaway>QUIC's monotonic packet numbers make loss detection unambiguous — no Karn's algorithm needed. User-space placement makes congestion control pluggable: NewReno, CUBIC, or BBR can be selected per-connection and updated without kernel changes.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-loss-detection-retrieval"
  lang="en"
  questions={[
    { q: "What is Karn's algorithm and why does QUIC not need it?", a: "Karn's algorithm is a TCP workaround: when a retransmit is ACKed, TCP cannot tell if the ACK is for the original send or the retransmit — so it ignores the RTT sample. QUIC avoids this by using monotonically increasing packet numbers. A retransmit gets a new packet number, so the ACK unambiguously identifies which send was received. RTT samples are always valid." },
    { q: "How does QUIC's ACK range mechanism work, and what TCP feature does it resemble?", a: "QUIC ACK frames list ranges of acknowledged packet numbers (e.g., ack_ranges: [1000..1005, 1008..1010]). Gaps represent unacknowledged packets. This is equivalent to TCP's Selective ACK (SACK) but is native to QUIC and always active — SACK in TCP is optional and must be negotiated." },
    { q: "What is PTO in QUIC and how does it differ from TCP's RTO?", a: "PTO (Probe Timeout) is QUIC's equivalent of TCP's RTO (Retransmission Timeout). Like RTO, PTO is computed from SRTT and RTTVAR and fires when no ACK arrives. The key difference: PTO is computed per packet-number space, so a timeout in the Handshake space does not reset the 1-RTT congestion window. TCP's RTO is global to the connection." },
  ]}
/>

<Recap lang="en">
TCP's sequence number reuse on retransmit creates Karn's algorithm ambiguity — you cannot measure RTT from a retransmitted ACK. QUIC eliminates this by assigning every send a new, strictly increasing packet number. ACK frames carry ranges so gaps in acknowledged numbers reveal lost packets exactly, equivalent to TCP SACK but always active. Loss is declared via a reorder window or PTO (computed per packet-number space). Because QUIC runs in user space, congestion control is pluggable: RFC 9002 specifies NewReno as the baseline, but CUBIC and BBR ship in major libraries and can be selected per-connection. Applications on CDN edges can update their CC algorithm weekly without touching the kernel. Now when you read a QUIC implementation's configuration and see a `cc_algorithm = bbr` option, you will know what made that possible — user-space placement meaning the protocol can swap the entire loss model with a config flag, no kernel patch required.
</Recap>
