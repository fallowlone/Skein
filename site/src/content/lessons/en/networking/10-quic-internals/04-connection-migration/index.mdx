---
concepts:
- connection-id
- path-validation
- path-challenge
- anti-amplification
- connection-migration
deepensInto:
- 07-deployment-and-cpu
estMin: 10
lang: en
lessonType: topic
level: middle
order: 4
prereqs:
- 03-tcp-handshake/01-the-three-way-handshake
- 10-quic-internals/01-why-quic
- 10-quic-internals/03-handshake-and-1rtt
slug: 04-connection-migration
sources:
- https://datatracker.ietf.org/doc/html/rfc9000
- https://blog.cloudflare.com/the-road-to-quic/
- https://datatracker.ietf.org/doc/html/rfc9002
spiral:
- statefulness
- latency
status: ready
summary: QUIC identifies connections by an opaque Connection ID rather than the 4-tuple,
  so connections survive IP address changes — WiFi to cellular — without reconnecting.
title: Connection IDs and network migration
track: networking
unit: 10-quic-internals
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import Misconception from "~/components/layout/Misconception.astro";

<Hook>
You step outside the office and your phone switches from WiFi to LTE. Your TCP connections all die — every app reconnects, re-authenticates, re-loads. QUIC makes this switch invisible to the application.
</Hook>

<Crux>QUIC Connection IDs decouple session identity from network address so connections survive IP changes without reconnecting.</Crux>

<Explanation>

## Why TCP breaks on IP change

TCP connections are identified by the **4-tuple**: client IP, client port, server IP, server port. When the client's IP changes — WiFi to cellular, NAT rebind — the 4-tuple is broken. The server sees packets from an unknown source and ignores them. The client must start a new TCP handshake, re-negotiate TLS, and re-establish application state. This takes seconds and is visible to users.

## Connection IDs decouple identity from address

QUIC introduces an **opaque Connection ID** (up to 20 bytes, typically 8) chosen by the server. The Connection ID is carried in every QUIC packet header. The server maps Connection IDs to connection state, not IP addresses.

When the client's IP changes:

1. The client sends a QUIC packet from the new address with the **same Connection ID**.
2. The server recognizes the Connection ID, notes the new source address.
3. The server sends a **PATH_CHALLENGE** frame — a random 8-byte token.
4. The client echoes the token in a **PATH_RESPONSE** frame.
5. Path validated — the server updates the connection's current address and unlocks full send rate.

**All stream state, flow-control windows, and congestion state survive the migration.** The connection continues as if nothing happened.

<div data-lesson-visual class="rounded-lg border border-slate-200 bg-slate-50 p-4 my-4 text-sm space-y-3">
  <div class="font-semibold text-slate-700">WiFi-to-cellular migration timeline</div>
  <div class="space-y-1 font-mono text-xs">
    <div class="flex gap-2"><span class="text-slate-400 w-8">t=0</span><span>Client on WiFi: 192.168.1.100:54321, Connection ID 0x12345678</span></div>
    <div class="flex gap-2"><span class="text-slate-400 w-8">t=1s</span><span class="text-amber-600">Client switches to LTE: 203.0.113.50:55555 (same Connection ID)</span></div>
    <div class="flex gap-2"><span class="text-slate-400 w-8">t=1s</span><span>Server: sees Connection ID 0x12345678 from new address — sends PATH_CHALLENGE</span></div>
    <div class="flex gap-2"><span class="text-slate-400 w-8">t=1.1s</span><span>Client: sends PATH_RESPONSE — path validated</span></div>
    <div class="flex gap-2"><span class="text-slate-400 w-8">t=1.1s</span><span class="text-green-600">All streams resume at full bandwidth — no reconnect, no re-auth</span></div>
  </div>
</div>

## Anti-amplification limit

Before validating the client's new address, the server may send **at most 3× the bytes it received** from that address. This prevents UDP-based amplification attacks: an attacker cannot spoof the client's address to make the server send large responses to a victim.

Clients must send Initial packets padded to at least **1200 bytes** so servers have headroom for a ~3600-byte response burst. Once PATH_RESPONSE is verified, the server unlocks full send rate.

<TraceScenario
  id="04-mid-trace-1"
  lessonSlug="04-connection-migration"
  lang="en"
  scenario="Trace a client switching from WiFi to cellular mid-connection."
  steps={[
    { prompt: "Client on WiFi sends packets with Connection ID 0x12345678 from IP 192.168.1.100 port 54321.", reveal: "Server notes the connection is at 192.168.1.100:54321." },
    { prompt: "Client steps outside, loses WiFi, switches to cellular with a new IP 203.0.113.50 port 55555.", reveal: "New IP:port but Connection ID is still 0x12345678 in the QUIC packet." },
    { prompt: "Server receives packet from 203.0.113.50:55555 with Connection ID 0x12345678. What does it do?", reveal: "Server recognizes the Connection ID (matches an active connection) but the 4-tuple is new. It sends a PATH_CHALLENGE frame to 203.0.113.50:55555 to validate the new address." },
    { prompt: "Client responds with PATH_RESPONSE. Does the server immediately send at full bandwidth to the new address?", reveal: "No. Until address validation completes, the server sends at most 3× the bytes it received (anti-amplification). Once PATH_RESPONSE is verified, the server unlocks full send rate and updates its record of the connection's current address." },
    { prompt: "All stream states, flow-control windows, and congestion state survive the migration. Why?", reveal: "Because the connection is identified by Connection ID, not 4-tuple. All metadata is attached to the connection, so moving the connection to a new path moves all streams with it." },
  ]}
/>

<Quiz
  id="04-mid-quiz-1"
  lessonSlug="04-connection-migration"
  lang="en"
  question="In QUIC, what does the anti-amplification limit (3x rule) prevent?"
  choices={[
    { label: "A client from sending more data than the server expects", misconception: "Anti-amplification protects against reflection attacks — the server is the potential amplifier, not the client." },
    { label: "An on-path attacker from spoofing the client's address and forcing the server to send massive responses to a victim IP", correct: true },
    { label: "The server from accepting too many connections simultaneously", misconception: "Connection limits are a different concern; anti-amplification is about UDP reflection attacks." },
    { label: "QUIC packets from being retransmitted more than 3 times", misconception: "Anti-amplification is not about retransmission count. It is about response-to-received-data ratio." },
  ]}
/>

<Quiz
  id="04-mid-quiz-2"
  lessonSlug="04-connection-migration"
  lang="en"
  question="Why can QUIC survive a client's IP change from WiFi to cellular while TCP cannot?"
  choices={[
    { label: "Because QUIC re-establishes the TCP connection automatically in the background", misconception: "QUIC does not use TCP. The point is that QUIC identifies connections by Connection ID, not 4-tuple." },
    { label: "QUIC connections are identified by Connection ID, not 4-tuple — so the same Connection ID on a new address is still the same connection", correct: true },
    { label: "Because UDP packets do not require a fixed IP address", misconception: "UDP packets still require a source IP. The trick is that QUIC's connection identifier is in the payload, not the IP layer." },
    { label: "Because QUIC uses keep-alive packets to maintain the connection", misconception: "Keep-alive packets maintain idle connections but do not survive IP changes." },
  ]}
/>

<Misconception
  id="04-udp-blocking-failure"
  title="Heads-up: QUIC is fragile to UDP blocking"
>
3–5% of networks block UDP outright. When silently dropped, QUIC times out and falls back to HTTP/2, adding latency. Browsers race QUIC and TCP in parallel, so whichever succeeds first wins. Fallback to HTTP/2 must be seamless.
</Misconception>

<Inset kind="edgecase" lang="en">
Connection ID rotation prevents an observer from linking a client's old and new IP addresses via the Connection ID. The server periodically sends **NEW_CONNECTION_ID** frames offering fresh IDs; the client switches to a new one when migrating. This provides privacy — an observer cannot correlate a user's WiFi session with their cellular session — without hiding the user's IP from the server.
</Inset>

</Explanation>

<KeyTakeaway>QUIC Connection IDs let connections survive network changes by decoupling session identity from IP address. Path validation (PATH_CHALLENGE/PATH_RESPONSE) prevents spoofing; the 3x anti-amplification limit prevents reflection attacks during validation.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-connection-migration-retrieval"
  lang="en"
  questions={[
    { q: "Explain why QUIC can survive a client's IP address change from WiFi to cellular while TCP cannot, and what role the Connection ID plays.", a: "TCP connections are identified by the 4-tuple (client IP, client port, server IP, server port). When the client's IP changes the 4-tuple is broken and TCP considers it a new connection — all connection state is discarded. QUIC connections are identified by a Connection ID chosen by the server. When the client's IP changes the Connection ID is still in the QUIC packet. The server recognizes it, validates the new path via PATH_CHALLENGE/PATH_RESPONSE, and moves the connection to the new address. All stream state and congestion state survive." },
    { q: "What is the anti-amplification limit in QUIC and why is it needed?", a: "Before validating the client's address, the server may send at most 3x the bytes it received from that address. This prevents UDP amplification attacks: an attacker cannot spoof a victim's IP as the client source, because the server's large response would go to the victim. Clients pad Initial packets to 1200 bytes minimum to give servers room for 3600 bytes of response." },
    { q: "What frames does QUIC use to validate a new client address after migration?", a: "The server sends a PATH_CHALLENGE frame with a random 8-byte token. The client echoes the token back in a PATH_RESPONSE frame. Once received, the server considers the new address validated and unlocks full send rate." },
  ]}
/>

<Recap lang="en">
TCP's fundamental assumption — that a connection equals a 4-tuple — breaks whenever a mobile client changes networks. QUIC sidesteps this by identifying connections with an opaque Connection ID that lives in the QUIC packet header, not in the IP or UDP header. When the client's IP changes, the Connection ID remains constant and the server can detect the migration: it sends a PATH_CHALLENGE to verify the new address is reachable, waits for a PATH_RESPONSE, and then migrates the entire connection — all streams, all flow-control state — to the new path. During validation, the 3x anti-amplification limit prevents an attacker from exploiting the migration window as a reflection vector. Connection ID rotation further lets the client change IDs on migration so observers cannot link the two network identities.
</Recap>
