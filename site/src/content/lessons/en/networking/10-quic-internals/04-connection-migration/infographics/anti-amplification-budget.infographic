infographic chart-column-simple
data
  title Anti-amplification budget before path validation
  desc Server may send at most 3x the bytes it received from an unvalidated address
  values
    - label Client Initial received (padded, bytes)
      value 1200
    - label Max server response burst (3x, bytes)
      value 3600
theme
  palette #6366f1 #f43f5e
