infographic chart-column-simple
data
  title Head-of-line blocking delay: TCP vs QUIC (ms)
  desc Same path — 0.5% random loss, 100 ms RTT. Per-stream recovery turns a 230 ms stall into 0.4 ms: a 575x win
  values
    - label TCP ordered delivery
      value 230
    - label QUIC per-stream
      value 0.4
theme
  palette #f43f5e #14b8a6
