---
concepts:
- quic
- user-space-transport
- udp-foundation
- middlebox-ossification
- connection-migration
deepensInto:
- 02-quic-streams-and-hol
- 03-handshake-and-1rtt
estMin: 8
lang: en
lessonType: topic
level: junior
order: 1
prereqs:
- 03-tcp-handshake/01-the-three-way-handshake
- 05-tls-handshake/01-tls-what-and-why
- 06-http-versions/01-http-fundamentals
slug: 01-why-quic
sources:
- https://datatracker.ietf.org/doc/html/rfc9000
- https://blog.cloudflare.com/the-road-to-quic/
- https://www.debugbear.com/blog/http3-vs-http2-performance
spiral:
- latency
- statefulness
status: ready
summary: QUIC moves transport logic out of the kernel and onto UDP so the protocol
  can evolve freely — trading CPU overhead for faster handshakes and independent streams.
title: Why QUIC and not TCP+TLS
track: networking
unit: 10-quic-internals
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook>
You know TCP. You know TLS. You know how they stack: the browser does a TCP handshake (1 RTT), then a TLS handshake on top (1 more RTT), then finally sends the HTTP request. On a 100 ms intercontinental link that is 200 ms of overhead before your page even starts loading.
</Hook>

<Crux>QUIC runs TCP-like transport over UDP in user space — no kernel updates, 1-RTT handshakes, independent streams.</Crux>

<Explanation>

## Two problems with TCP

TCP is old. Adding new TCP options requires kernel updates, OS patches shipped months later, and then you hope middleboxes — firewalls, NATs, load balancers — do not silently drop the option. This **ossification** meant TCP could barely evolve for decades.

The second problem: losing a single packet stalls all streams on the connection, even the ones with unrelated data. This condition is called **head-of-line blocking**.

QUIC's radical solution: move the transport logic to user space where it can be updated with every application, run it over UDP (which middleboxes have no special knowledge of), and encrypt almost the entire packet so middleboxes cannot inspect it.

<div data-lesson-visual class="rounded-lg border border-slate-200 bg-slate-50 p-4 my-4 text-sm font-mono space-y-2">
  <div class="font-semibold text-slate-700 mb-2">TCP+TLS vs. QUIC handshake latency</div>
  <div class="space-y-1">
    <div class="flex gap-2 items-center"><span class="w-28 text-slate-500">TCP SYN/ACK</span><span class="bg-blue-200 rounded px-2 py-0.5">1 RTT</span></div>
    <div class="flex gap-2 items-center"><span class="w-28 text-slate-500">TLS ClientHello</span><span class="bg-blue-200 rounded px-2 py-0.5">1 RTT</span></div>
    <div class="flex gap-2 items-center"><span class="w-28 text-slate-500">First HTTP byte</span><span class="bg-blue-300 rounded px-2 py-0.5 font-semibold">= 2 RTT total</span></div>
  </div>
  <div class="border-t border-slate-300 pt-2 mt-2">
    <div class="flex gap-2 items-center"><span class="w-28 text-slate-500">QUIC Initial</span><span class="bg-green-200 rounded px-2 py-0.5">1 RTT</span></div>
    <div class="flex gap-2 items-center"><span class="w-28 text-slate-500">First HTTP byte</span><span class="bg-green-300 rounded px-2 py-0.5 font-semibold">= 1 RTT total</span></div>
  </div>
  <div class="text-xs text-slate-500 mt-1">On a 100 ms intercontinental link: TCP+TLS costs 200 ms; QUIC costs 100 ms</div>
</div>

## The one-sentence definition

QUIC is a transport protocol running on top of UDP that carries a TLS 1.3 handshake, multiplexes many independent data streams, and survives network migration — your laptop switching from WiFi to cellular without losing the connection.

**The metaphor.** Imagine postal mail: TCP is like sending envelopes one at a time in strict order. Lose one and all others wait. QUIC is like a postman with one bag holding many separate envelopes; lose one and the rest keep moving. Also, QUIC's bag is encrypted.

## One scenario end to end

You open a mobile app that streams video over QUIC while sending periodic beacon requests (analytics) and real-time chat. In TCP each would use its own connection and pay three handshakes. In QUIC they share one connection, so you pay one 1-RTT handshake and then the app can use stream ID 0 for video, stream ID 2 for analytics, stream ID 1 for chat. A dropped packet in stream 1 does not block stream 0's video frames from arriving. When you move from WiFi to 4G the connection ID stays valid, all streams stay alive, and the game never even notices.

<Quiz
  id="01-jr-quiz-1"
  lessonSlug="01-why-quic"
  lang="en"
  question="Why is QUIC in user space instead of the kernel?"
  choices={[
    { label: "User space is faster than kernel space", misconception: "User space is actually slower — the trade-off is flexibility and evolution speed, not raw speed." },
    { label: "So applications control congestion logic and the protocol can evolve without recompiling the OS kernel", correct: true },
    { label: "Because UDP does not work in the kernel", misconception: "UDP works fine in the kernel — both TCP and UDP traditionally ran there. QUIC's placement is a design choice." },
    { label: "To avoid middlebox filtering", misconception: "User-space placement helps evolution, not filtering avoidance. Middleboxes see QUIC as UDP packets regardless of where it runs." },
  ]}
/>

<Inset kind="why" lang="en">
RFC 9000 took thirteen years to standardize (published May 2021). The core difficulty was convincing the Internet community to trust a new transport in user space — a place traditionally reserved for application code, not reliable delivery guarantees. Google shipped an early prototype (gQUIC) in Chrome in 2012, measured real-world latency wins at YouTube scale, and donated the data to the IETF as evidence.
</Inset>

<DragOrder
  id="01-jr-drag-1"
  lessonSlug="01-why-quic"
  lang="en"
  prompt="Order the key events in a QUIC connection from laptop switching to mobile:"
  items={[
    "Laptop sends QUIC Initial packet with Connection ID (e.g., 0x12345678)",
    "Mobile receives, opens connection, connection ID stays bound to the session",
    "Laptop moves to cellular — IP address changes but Connection ID is still 0x12345678",
    "Mobile recognizes the Connection ID, validates the new IP, and keeps the connection alive",
    "All streams and flow control state survive the move — no reconnect needed",
  ]}
/>

<NumbersCard
  id="01-quic-latency-summary"
  title="QUIC latency highlights"
  stats={[
    { label: "Handshake cost", value: "1 RTT (vs TCP+TLS 2 RTT)" },
    { label: "Resumption cost", value: "0 RTT on cached session ticket" },
    { label: "Page load on lossy mobile", value: "30–55% faster than HTTP/2" },
    { label: "Web traffic on HTTP/3", value: "~21% (2026)" },
  ]}
/>

</Explanation>

<KeyTakeaway>QUIC escapes kernel ossification by running over UDP in user space, cuts connection setup from 2 RTT to 1 RTT, and lets independent streams carry concurrent data without blocking each other.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-why-quic-retrieval"
  lang="en"
  questions={[
    { q: "What is middlebox ossification and how does QUIC avoid it?", a: "Middleboxes (firewalls, NATs) only understand existing TCP options and silently drop new ones, freezing TCP's evolution. QUIC runs over UDP, which middleboxes treat as opaque, and encrypts most of its header — so there is nothing for a middlebox to interpret or strip." },
    { q: "In one sentence: what is the main latency advantage of QUIC over TCP+TLS?", a: "QUIC merges the TCP handshake and TLS handshake into one 1-RTT exchange (instead of TCP's 1 RTT plus TLS's 1 RTT), saving one round-trip of latency before application data can flow." },
    { q: "How does QUIC keep a connection alive when the client's IP address changes?", a: "QUIC connections are identified by a Connection ID, not the 4-tuple. When the client's IP changes the Connection ID stays the same, the server validates the new path via PATH_CHALLENGE/PATH_RESPONSE, and all streams continue without reconnecting." },
  ]}
/>

<Recap lang="en">
TCP's kernel-only evolution model left the protocol frozen by middlebox ossification for decades — any new TCP option gets stripped before it reaches the peer. QUIC sidesteps this by running over UDP in user space, where any application can ship updated congestion control weekly. The merged transport+TLS handshake saves one full round trip: 100 ms on an intercontinental link, 150 ms on a cross-Pacific link. Independent stream multiplexing means a dropped packet blocks only the stream it belongs to, not the entire connection. The cost is CPU: user-space packet processing and per-packet AES-GCM encryption add 15–30% overhead per byte over kernel TCP.
</Recap>
