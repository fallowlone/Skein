---
concepts:
- quic-handshake
- crypto-frame
- packet-number-space
- 0-rtt
- 1-rtt
- encryption-levels
deepensInto:
- 06-0rtt-and-encryption
estMin: 12
lang: en
lessonType: topic
level: middle
order: 3
prereqs:
- 05-tls-handshake/02-tls-record-and-handshake
- 10-quic-internals/01-why-quic
- 10-quic-internals/02-quic-streams-and-hol
slug: 03-handshake-and-1rtt
sources:
- https://datatracker.ietf.org/doc/html/rfc9000
- https://datatracker.ietf.org/doc/rfc9001/
- https://blog.cloudflare.com/even-faster-connection-establishment-with-quic-0-rtt-resumption/
spiral:
- latency
status: ready
summary: QUIC fuses the transport and TLS layers into one exchange — the client sends
  TLS ClientHello in the first packet and application data flows after just one round
  trip.
title: Integrated handshake and 1-RTT
track: networking
unit: 10-quic-internals
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import rttToFirstByte from "./infographics/rtt-to-first-byte.svg?raw";

<Hook>
TCP finishes its handshake. Then TLS starts its own. These are separate protocols with separate round trips. Every new connection pays that double toll. QUIC refuses to accept that — it runs both handshakes in the same packet exchange.
</Hook>

<Crux>QUIC fuses transport and TLS into one 1-RTT exchange by embedding TLS CRYPTO frames directly in QUIC Initial packets.</Crux>

<Explanation>

## The merged handshake

QUIC eliminates the separate transport handshake layer. Instead of TCP SYN/SYN-ACK (1 RTT) followed by TLS ClientHello/ServerHello (another RTT), QUIC carries the TLS handshake inside CRYPTO frames that travel in QUIC packets.

The sequence:

1. **Client** sends an Initial packet containing a CRYPTO frame with the TLS **ClientHello**.
2. **Server** responds with its own Initial packet containing a CRYPTO frame with **ServerHello + EncryptedExtensions + Certificate + CertificateVerify** — all in one packet burst.
3. **Client** sends a Handshake packet with a CRYPTO frame containing **Finished**.
4. Both sides transition to **1-RTT** state. The client can send stream data immediately after step 1 completes.

**Result:** 1 RTT from client start to receiving server's response, vs TCP+TLS's 2 RTT. Together these four steps mean transport and TLS finish in a single flight — skip any one of them and you are back to the two-round-trip tax TCP+TLS imposes.

<div data-lesson-visual class="rounded-lg border border-slate-200 bg-slate-50 p-4 my-4 text-sm space-y-2">
  <div class="font-semibold text-slate-700 mb-2">QUIC handshake packet sequence</div>
  <div class="space-y-1 font-mono text-xs">
    <div class="flex gap-3"><span class="text-blue-600 w-16">Client</span><span class="text-slate-400">→</span><span class="text-green-700 w-16">Server</span><span>Initial [CRYPTO: ClientHello] + Padding</span></div>
    <div class="flex gap-3"><span class="text-blue-600 w-16">Client</span><span class="text-slate-400">←</span><span class="text-green-700 w-16">Server</span><span>Initial [CRYPTO: ServerHello + EE + Cert + CertVerify]</span></div>
    <div class="flex gap-3"><span class="text-blue-600 w-16">Client</span><span class="text-slate-400">←</span><span class="text-green-700 w-16">Server</span><span>Handshake [CRYPTO: Finished]</span></div>
    <div class="flex gap-3"><span class="text-blue-600 w-16">Client</span><span class="text-slate-400">→</span><span class="text-green-700 w-16">Server</span><span>Handshake [CRYPTO: Finished] + 1-RTT [STREAM data]</span></div>
    <div class="text-xs text-slate-500 mt-2">Total: 1 RTT before client can send stream data. TCP+TLS = 2 RTT.</div>
  </div>
</div>

<Infographic
  svg={rttToFirstByte}
  label="Column chart of round trips before the first stream byte: TCP plus TLS 1.3 takes 2 round trips, a new QUIC connection takes 1, and QUIC 0-RTT resumption takes 0."
  caption="Merging transport and TLS halves a cold connection's setup to one round trip; a cached session ticket drops resumption to zero."
  hue="var(--d-infra)"
/>

## Four encryption levels

QUIC defines four distinct encryption levels, each with its own key:

1. **Initial** — derived deterministically from the destination Connection ID. Both sides can compute these keys before exchanging any material, so the Initial packet is encryptable immediately.
2. **0-RTT** — derived from a session ticket cached from a previous connection. Allows the client to send application data in the ClientHello without waiting for the server's response.
3. **Handshake** — derived from the shared secrets after the ClientHello-ServerHello exchange. Used for Finished messages.
4. **1-RTT** — the final post-handshake keys. All subsequent stream data uses these.

## Three packet-number spaces

The four encryption levels map to three packet-number spaces:

- **Initial/Handshake** — each has its own packet-number sequence (0, 1, 2, ...).
- **Application Data** — 0-RTT and 1-RTT share one space.

Separation is critical: **loss detection is independent per space**. A lost Initial packet triggers PTO in the Initial space only — it does not shrink the congestion window for 1-RTT data.

<Quiz
  id="03-mid-quiz-1"
  lessonSlug="03-handshake-and-1rtt"
  lang="en"
  question="Why does QUIC have separate packet-number spaces for Initial/Handshake vs. 1-RTT?"
  choices={[
    { label: "To prevent packet-number wraparound", misconception: "Wraparound is a non-issue with 62-bit packet numbers. Separation is about loss detection." },
    { label: "Because the encryption keys differ, so a packet lost in one space has independent loss detection from another space", correct: true },
    { label: "To allow the server to process Initial packets before Handshake packets", misconception: "Both can arrive out of order; separate packet-number spaces are about encryption keys and loss logic." },
    { label: "Because UDP reorders packets more than TCP", misconception: "UDP does not reorder; out-of-order delivery is an artifact of the network path, not UDP vs. TCP." },
  ]}
/>

<TraceScenario
  id="03-mid-trace-1"
  lessonSlug="03-handshake-and-1rtt"
  lang="en"
  scenario="Trace a QUIC connection from client initiation to the first stream data."
  steps={[
    { prompt: "Client sends Initial packet with ClientHello in CRYPTO frame. What encryption key does it use?", reveal: "The keys derived from the QUIC Initial Secrets, which are deterministic from the destination connection ID. Both sides can derive these keys without exchanging material, so the Initial packet is encryptable by both sides immediately." },
    { prompt: "Server receives Initial packet. What does it respond with?", reveal: "Server sends Initial packet containing CRYPTO frames with ServerHello + EncryptedExtensions + Certificate + CertificateVerify, encrypted with the Handshake keys (derived from the shared secrets now that it has the ClientHello)." },
    { prompt: "Client receives server Initial. What state is the client in?", reveal: "The client now has the server's keying material and can derive the 1-RTT keys. The handshake is complete from the client's perspective; it can send stream data in 1-RTT packets." },
    { prompt: "Client sends first stream data in a 1-RTT packet. What is the RTT elapsed since the Initial?", reveal: "Exactly 1 RTT from client Initial to server Initial receipt, no additional wait. Compare TCP+TLS which is 1 RTT (TCP 3-way handshake) + 1 RTT (TLS) = 2 RTT." },
  ]}
/>

<DragOrder
  id="03-mid-drag-1"
  lessonSlug="03-handshake-and-1rtt"
  lang="en"
  prompt="Order the packet types in a QUIC handshake from initial to completion:"
  items={[
    "Client sends Initial packet with ClientHello",
    "Server sends Initial packet with ServerHello + EncryptedExtensions + Certificate + CertificateVerify",
    "Server sends Handshake packet with Finished",
    "Client sends Handshake packet with Finished",
    "Both sides transition to 1-RTT state and can send Short Header packets with stream data",
  ]}
/>

<Inset kind="why" lang="en">
QUIC's Initial packet uses **deterministic keys derived from the Connection ID** — an apparently paradoxical choice. Why encrypt if both sides can compute the key? The goal is not secrecy at this stage but integrity: encrypting the Initial prevents passive observers from trivially tampering with the ClientHello. The Handshake and 1-RTT keys, derived from DH key exchange material, provide actual confidentiality.
</Inset>

<NumbersCard
  id="03-handshake-numbers"
  title="QUIC handshake latency"
  stats={[
    { label: "QUIC new connection", value: "1 RTT before first stream byte" },
    { label: "TCP + TLS 1.3 new connection", value: "2 RTT before first stream byte" },
    { label: "QUIC 0-RTT resumption", value: "0 RTT (data in ClientHello)" },
    { label: "Saving on 100 ms link", value: "100 ms per new connection" },
    { label: "Saving on 150 ms link (Asia-US)", value: "150 ms per new connection" },
  ]}
/>

</Explanation>

<SequenceDiagram
  label="QUIC merged handshake: client sends Initial with TLS ClientHello, server replies with ServerHello plus certificate, then both reach 1-RTT and stream data flows after one round trip"
  hue="var(--d-network)"
  actors={["Client", "Server"]}
  messages={[
    { from: "Client", to: "Server", label: "Initial [CRYPTO: ClientHello]" },
    { from: "Server", to: "Client", label: "Initial [CRYPTO: ServerHello+Cert]" },
    { from: "Server", to: "Client", label: "Handshake [CRYPTO: Finished]" },
    { from: "Client", to: "Server", label: "Handshake [Finished] + 1-RTT [STREAM data]" },
  ]}
  caption="Transport and TLS complete in the same packet exchange — one round trip to first stream byte, versus TCP's handshake plus a separate TLS handshake (2 RTT)."
/>

<KeyTakeaway>QUIC embeds TLS CRYPTO frames in its Initial packets so transport and TLS complete in one round trip. Four encryption levels and three packet-number spaces keep loss detection independent across handshake phases.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-handshake-retrieval"
  lang="en"
  questions={[
    { q: "Why does QUIC's 1-RTT handshake save latency compared to TCP+TLS, and what do the numbers look like on intercontinental links?", a: "TCP requires 1 RTT for the three-way handshake (SYN, SYN-ACK, ACK). TLS 1.3 running on top requires another RTT. Total: 2 RTT before the first application byte. QUIC merges both: the client sends Initial with ClientHello, the server replies with keying material, and the client can send stream data in the next packet. On a 100 ms intercontinental link, TCP+TLS costs 200 ms; QUIC costs 100 ms." },
    { q: "What are the four QUIC encryption levels and what is each used for?", a: "Initial: deterministic from Connection ID, used for ClientHello/ServerHello exchange. 0-RTT: from session ticket, used for early data before server Finished. Handshake: from DH exchange, used for Finished messages. 1-RTT: final post-handshake keys, used for all stream data." },
    { q: "Why are packet-number spaces separated per encryption level?", a: "Loss detection must be independent per encryption level. A lost Initial packet triggers PTO only in the Initial space — it cannot reduce the congestion window for 1-RTT data. Without separation, a loss in the handshake phase would incorrectly signal congestion for application traffic." },
  ]}
/>

<Recap lang="en">
QUIC's handshake merges what TCP and TLS kept separate: a CRYPTO frame inside a QUIC Initial packet carries the TLS ClientHello so both transport and TLS complete in one round trip instead of two. The server responds with its full Certificate chain in the same burst, and the client can send stream data immediately after. Four encryption levels (Initial, 0-RTT, Handshake, 1-RTT) map to three packet-number spaces so that loss detection in each phase does not interfere with the others. On a 100 ms link this saves 100 ms per new connection — 150 ms on cross-Pacific paths. 0-RTT resumption goes further: a cached session ticket lets the client attach application data to the ClientHello itself, reaching zero round trips at the cost of replay vulnerability for non-idempotent requests. Now when you measure a cold QUIC connection in a packet capture and see data flowing after just one round trip, you will know exactly which packet carried both the transport negotiation and the TLS key exchange — it was the Initial.
</Recap>
