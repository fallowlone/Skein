---
concepts:
- user-space-transport
- head-of-line-blocking
- connection-migration
- 0-rtt-replay
- packet-encryption
- cpu-cost
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://datatracker.ietf.org/doc/html/rfc9000
- https://datatracker.ietf.org/doc/html/rfc9002
- https://blog.cloudflare.com/the-road-to-quic/
status: ready
summary: "Multiple-choice synthesis across the QUIC unit — user-space transport, stream independence, migration, the 1-RTT and 0-RTT handshake, encryption, and deployment CPU cost."
title: 'QUIC internals: multiple-choice review'
track: networking
unit: 10-quic-internals
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each mirrors a real decision — when QUIC wins, when TCP wins, and what the design buys and costs — not a definition to recite.
</Hook>

<Goal lang="en">
Confirm you can connect the pieces: why QUIC lives in user space, what stream independence and Connection IDs actually buy, where the 1-RTT and 0-RTT handshakes save and where 0-RTT bites, and why the same protocol that wins on mobile loses on a fast LAN.
</Goal>

<Quiz
  id="10-quic-internals-choice-1"
  lessonSlug="10-quic-internals"
  lang="en"
  question="Two services both serve HTTP. One is an intercontinental API (small payloads, 120 ms RTT, ~1% mobile loss); the other streams 1 Gbps static assets to LAN clients. You can run HTTP/3 (QUIC) or HTTP/2 (TCP) on each. What is the right pairing and why?"
  choices={[
    { label: "QUIC for the API, TCP for the LAN assets. The API is round-trip-bound, so QUIC's 1-RTT handshake and per-stream loss recovery pay off; the LAN service is CPU-bound on a fast link, where QUIC's user-space per-byte cost saturates a core and goodput drops ~45% with no HoL benefit.", correct: true },
    { label: "QUIC for both — one protocol simplifies tooling and QUIC is strictly newer.", misconception: "On a fast LAN QUIC's per-packet AES-GCM and user-space framing make CPU the bottleneck before the link fills; goodput falls ~45% and there is no head-of-line benefit on a near-lossless short path. Newer is not faster everywhere." },
    { label: "TCP for both — QUIC's CPU overhead always outweighs its latency win.", misconception: "On the 120 ms lossy API path the network, not CPU, is the bottleneck, so QUIC's overhead is invisible while its 1-RTT handshake and stream independence cut real latency. Overhead only bites where CPU saturates first." },
    { label: "QUIC for the LAN assets (raw throughput) and TCP for the API.", misconception: "This inverts the tradeoff. QUIC helps where round trips and loss dominate (the API); TCP's kernel offload (kTLS, GSO, RSS) wins where you push bytes fast on a clean link (the LAN assets)." },
  ]}
/>

<Quiz
  id="10-quic-internals-choice-2"
  lessonSlug="10-quic-internals"
  lang="en"
  question="An HTTP/2-over-TCP service and an HTTP/3-over-QUIC service both multiplex three concurrent responses. At 0.5% packet loss and 100 ms RTT, one packet carrying response B is dropped. What happens to responses A and C, and why?"
  choices={[
    { label: "Under HTTP/2 all three stall until B's packet is retransmitted (~230 ms); under HTTP/3 only B's stream waits (~0.4 ms) while A and C keep delivering. TCP enforces a single ordered byte stream so loss blocks every multiplexed stream; QUIC gives each stream independent ordering and loss recovery.", correct: true },
    { label: "Both protocols stall all three — multiplexing means they share one delivery order regardless of transport.", misconception: "HTTP/2 multiplexes at the HTTP layer but rides one ordered TCP stream, so loss blocks all of it. QUIC moves multiplexing into the transport, giving each stream its own ordering — that is exactly the HoL fix the unit is about." },
    { label: "Under both, A and C continue because HTTP-layer multiplexing already isolates streams.", misconception: "HTTP/2 multiplexing cannot help: the blocking is at the TCP transport beneath it. Only QUIC, which owns the transport, can isolate streams under loss." },
    { label: "Under HTTP/3 the whole connection resets and all three restart from a checkpoint.", misconception: "QUIC retransmits only the lost STREAM frame for B; A and C are untouched and the connection never resets. Per-stream recovery is the entire point." },
  ]}
/>

<Quiz
  id="10-quic-internals-choice-3"
  lessonSlug="10-quic-internals"
  lang="en"
  question="A user walks out of the office; their phone hands off WiFi to LTE mid-download. The QUIC connection survives but TCP would have died. What carries the connection across the IP change, and what does the server do before trusting the new address?"
  choices={[
    { label: "QUIC identifies the connection by an opaque Connection ID in the packet header, not the 4-tuple, so the same ID arrives from the new IP. The server sends a PATH_CHALLENGE token, waits for the matching PATH_RESPONSE, and meanwhile sends at most 3x the bytes it received (anti-amplification) until the new path is validated.", correct: true },
    { label: "QUIC silently re-runs the handshake on the new IP in the background, so it looks seamless but is really a fresh connection.", misconception: "No new handshake happens. All stream, flow-control, and congestion state survive because the Connection ID — not the IP — identifies the session. A re-handshake would lose exactly that state." },
    { label: "UDP packets do not need a fixed source IP, so the connection just continues with no extra step.", misconception: "UDP packets still carry a source IP, and a blind switch would open a reflection-amplification vector. QUIC must validate the new path via PATH_CHALLENGE/PATH_RESPONSE and throttle to 3x until then." },
    { label: "Keep-alive packets hold the 4-tuple open across the network change.", misconception: "Keep-alives only stop an idle connection from timing out; they cannot preserve a connection whose 4-tuple changed. Connection-ID-based identity is what survives the migration." },
  ]}
/>

<Quiz
  id="10-quic-internals-choice-4"
  lessonSlug="10-quic-internals"
  lang="en"
  question="Your team enables 0-RTT resumption to shave a round trip off reconnects. A week later, finance reports occasional duplicate fund transfers. What is the mechanism and the correct fix?"
  choices={[
    { label: "0-RTT data rides the ClientHello with no TLS-record replay protection, so an on-path attacker (or a retried packet) can replay the ciphertext and the server re-executes it. Restrict 0-RTT to idempotent requests; answer non-idempotent ones (POST/DELETE) with 425 Too Early to force a retry after the 1-RTT handshake, and isolate session-ticket keys per server.", correct: true },
    { label: "0-RTT data is sent in cleartext, so the attacker reads the transfer and reissues it.", misconception: "0-RTT is encrypted with the session-ticket key; the attacker cannot read it. The flaw is that the same ciphertext can be replayed and re-processed — a replay, not a disclosure, problem." },
    { label: "Lower the session-ticket lifetime to zero so no 0-RTT is ever possible — replay is otherwise unsolvable.", misconception: "Disabling 0-RTT outright throws away the latency win it was enabled for. The standard fix is idempotency enforcement plus 425 Too Early, which keeps 0-RTT for safe requests." },
    { label: "Switch the congestion controller from CUBIC to BBR; the duplication is a retransmit artifact of aggressive probing.", misconception: "Congestion control has nothing to do with this. Duplicate transfers come from replayed 0-RTT early data executing a non-idempotent operation twice." },
  ]}
/>

<Quiz
  id="10-quic-internals-choice-5"
  lessonSlug="10-quic-internals"
  lang="en"
  question="After moving to HTTP/3, your network team complains that their packet-level dashboards (per-flow request counts, RTT histograms, slow-client detection) have gone dark, and they can no longer inject an RST to kill a misbehaving flow at the middlebox. How do you read this?"
  choices={[
    { label: "Expected. QUIC's short-header packets encrypt the packet number and entire payload, so observers see only the Connection ID and UDP 4-tuple — no sequence numbers, no stream data, no forgeable RST. The fix is application-layer observability: qlog (RFC 9312), browser timing APIs, and eBPF probes on the user-space socket.", correct: true },
    { label: "A misconfiguration — QUIC should expose the same packet metadata TCP does once decryption keys are loaded on the monitor.", misconception: "QUIC deliberately encrypts almost the whole packet end to end; a passive monitor has no keys and is not meant to. The opacity is by design, not a config error." },
    { label: "QUIC moved to a non-standard port the tools cannot see; point them at the new port and the dashboards return.", misconception: "QUIC runs on UDP 443. The metadata is missing because it is encrypted, not because the port is hidden — changing ports changes nothing." },
    { label: "The RST capability is gone because UDP has no reset, but request counts should still be visible in the clear.", misconception: "Both are gone for the same reason: packet numbers and payload are encrypted, so neither request accounting nor a forged RST is possible without the key. Loss of RST injection is actually a security win." },
  ]}
/>

<Quiz
  id="10-quic-internals-choice-6"
  lessonSlug="10-quic-internals"
  lang="en"
  question="A colleague argues QUIC is strictly superior to TCP because 'it runs in user space, so it is faster.' Where is the reasoning wrong, and what does user-space placement actually buy?"
  choices={[
    { label: "User space is slower per byte, not faster — every packet pays AES-GCM, VarInt decode, and framing in application code instead of NIC/kernel offload, costing 15–30% more CPU. What it buys is evolvability: pluggable per-connection congestion control (NewReno/CUBIC/BBR), weekly updates shipped with the app, and immunity to middlebox ossification — not raw speed.", correct: true },
    { label: "The reasoning is correct — bypassing the kernel removes syscall overhead, so user-space transport is inherently faster.", misconception: "User-space QUIC adds syscalls (one sendmsg per datagram without GSO) and loses NIC crypto offload, so it is slower per byte. Its advantage is agility and HoL-free streams, not throughput." },
    { label: "It is wrong because QUIC actually runs in the kernel like TCP; the speed claim is the only accurate part.", misconception: "QUIC runs in user space by design — that is the source of both its CPU cost and its ability to evolve without kernel updates. The location is right; the 'faster' conclusion is what is wrong." },
    { label: "User space is faster only because it lets QUIC skip encryption that TCP must perform.", misconception: "QUIC encrypts more than TCP, not less — almost the entire packet, per packet. The CPU cost is higher, and the payoff is evolution speed and stream independence." },
  ]}
/>

<Recap lang="en">
The unit resolves to one judgment call: QUIC trades CPU for latency and resilience. User-space placement buys evolvability (pluggable CC, no ossification) at 15–30% more CPU per byte; stream independence kills head-of-line blocking under loss; Connection IDs survive network migration with path validation and a 3x anti-amplification guard; the merged 1-RTT handshake saves a round trip and 0-RTT saves another at the price of replay (idempotent-only, 425 Too Early); near-full encryption stops RST injection and sniffing but blinds packet-level monitoring. Reach for QUIC on round-trip-bound, lossy, mobile paths — keep TCP where a fast clean link makes CPU the bottleneck.
</Recap>
