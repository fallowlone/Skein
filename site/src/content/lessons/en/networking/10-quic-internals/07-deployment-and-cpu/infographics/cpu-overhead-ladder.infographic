infographic chart-column-simple
data
  title Clawing back QUIC's CPU tax with mitigations
  desc Per-byte CPU overhead vs kernel TCP — UDP GSO recovers ~20%, affinity takes it to 15%
  values
    - label Naive user-space QUIC
      value 30
    - label + UDP GSO (batched sendmsg)
      value 20
    - label + GSO + core affinity
      value 15
theme
  palette #f43f5e #f59e0b #14b8a6
