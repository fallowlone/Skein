---
concepts:
- dns-referral
- authority-section
- additional-section
- glue-record
- a-record
- aaaa-record
- cname
- mx-record
- txt-record
- ns-record
- soa-record
- edns0
- axfr
- ixfr
- rd-bit
- tc-flag
- stub-resolver
- open-resolver
deepensInto:
- 03-ttl-and-caching
estMin: 14
lang: en
lessonType: topic
level: middle
order: 2
prereqs:
- 01-dns-what-it-does
slug: 02-the-resolver-walk
sources:
- https://datatracker.ietf.org/doc/html/rfc1034
- https://datatracker.ietf.org/doc/html/rfc1035
- https://datatracker.ietf.org/doc/html/rfc6891
- https://datatracker.ietf.org/doc/html/rfc2308
- https://hpbn.co/dns-anatomy/
spiral:
- statefulness
status: ready
summary: How a recursive resolver follows referrals from root to authoritative, what
  the DNS message sections mean, and why missing glue breaks everything.
title: 'The resolver walk: referrals, record types, and glue'
track: networking
unit: 04-dns-resolution
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
A resolver asking for `cdn.example.co.uk` does not get an answer from the root server. It gets a referral — "ask the `.uk` TLD." Then another referral — "ask `example.co.uk`'s authoritative server." Only on the third query does it get an actual IP address. Understanding what travels in those referral responses is what separates a developer who can debug DNS from one who just runs `dig` and hopes.
</Hook>

<Crux>A referral carries NS in Authority and glue A/AAAA in Additional — together they point the resolver forward without a circular dependency.</Crux>

<Explanation>

## Iterative vs recursive queries

Why does this distinction matter in production? Because a server that blurs the line — accepting recursive queries from external clients — becomes a free amplifier for DDoS attackers. The resolver's queries to root, TLD, and authoritative servers are **iterative**: the RD (Recursion Desired) bit is cleared. Those servers return only what they know, plus a referral — they never chase the tree for the caller. Only the client's original query to the resolver has RD=1, asking the resolver to do the full walk.

This distinction matters for security. An authoritative server that accepts RD=1 from external clients — an **open resolver** — can be abused as a DNS-reflection amplifier: an attacker sends spoofed-source queries and the server sends large responses to the victim. Production authoritatives (Route 53, Cloudflare DNS) never recurse for external clients.

## DNS message sections

A DNS message has four sections:

| Section | Purpose |
|---|---|
| **Question** | The query name, type (A, MX, …), class (IN) |
| **Answer** | Records that directly answer the question |
| **Authority** | NS records pointing to the next-hop nameserver |
| **Additional** | Pre-fetched A/AAAA records for names in Authority |

A referral has nothing in Answer, and has Authority + Additional. The AA (Authoritative Answer) bit is set only when the responding server owns the zone — a referral from a TLD server has AA=0.

## The referral chain for `cdn.example.co.uk`

1. **Resolver → root**: asks for `cdn.example.co.uk`. Root has no answer. It returns Authority: `ns.nic.uk` (NS record for `.uk`), plus Additional: `ns.nic.uk A 193.0.0.1` (glue). No Answer section.
2. **Resolver → `.uk` TLD**: asks same QNAME. TLD returns Authority: `ns1.example.co.uk` (NS for `example.co.uk`), plus Additional: `ns1.example.co.uk A 198.51.100.4` (glue). Still no Answer.
3. **Resolver → authoritative**: asks same QNAME. Authoritative returns Answer: `cdn.example.co.uk A 203.0.113.10 TTL=300`. AA bit set.

Together these three hops mean the resolver never guesses the next nameserver's address — it is always handed it explicitly in the glue. Without step 1's glue, step 2 is a dead end.

<TraceScenario
  id="mid-trace-1"
  pieceSlug="02-the-resolver-walk"
  lang="en"
  scenario="Trace a cold DNS lookup of cdn.example.co.uk from a clean cache."
  steps={[
    { prompt: "Resolver has nothing cached. First step?", reveal: "Pick a root server (the resolver caches root NS + A records at startup) and send iterative query (RD=0) for cdn.example.co.uk." },
    { prompt: "Root responds. What does the resolver receive?", reveal: "Referral: NS records for .uk in Authority section, glue A records for those NS in Additional section. Answer section is empty." },
    { prompt: "Resolver queries .uk TLD. What does it ask?", reveal: "Same QNAME cdn.example.co.uk, RD=0. The TLD is iterative — it returns only what it knows." },
    { prompt: "TLD responds. What now?", reveal: "Another referral: NS records for example.co.uk plus glue. Resolver still has no final IP." },
    { prompt: "Resolver queries example.co.uk authoritative. What does it receive?", reveal: "Answer section: A record cdn.example.co.uk 203.0.113.10 TTL=300. AA bit set. This is the final answer." },
    { prompt: "Next identical query within TTL?", reveal: "Cached. Returns immediately with no upstream queries." },
  ]}
/>

## Glue records and the circular dependency

When a zone delegates to a nameserver **inside its own zone** (the NS for `example.com` is `ns1.example.com`), the parent zone must include the A record for `ns1.example.com` as **glue** in the referral. Without glue, the resolver cannot find the nameserver without first resolving `example.com` — a chicken-and-egg loop.

Missing glue causes intermittent SERVFAIL or referral loops. Out-of-bailiwick NS records (e.g., `ns1.somednsprovider.org`) need no glue because the resolver can ask a different zone for their IP.

<NumbersCard
  id="card-dns-record-types"
  title="Common DNS record types"
  data-lesson-visual
  stats={[
    { label: "A", value: "IPv4 address" },
    { label: "AAAA", value: "IPv6 address" },
    { label: "CNAME", value: "alias → another name (never an IP)" },
    { label: "MX", value: "mail server hostname + priority" },
    { label: "TXT", value: "free text — SPF, DKIM, verification" },
    { label: "NS", value: "delegation — nameserver for the zone" },
    { label: "SOA", value: "zone authority: serial, refresh, TTL mins" },
    { label: "CAA", value: "which CAs may issue certs for this domain" },
    { label: "HTTPS/SVCB", value: "advertises ALPN + ECH keys (RFC 9460)" },
  ]}
/>

## CNAME and the apex rule

A CNAME record points a name to another name: `www CNAME example.com`. The resolver then looks up the target. **CNAME is forbidden at the zone apex** (the bare domain like `example.com`) per RFC 1034, because SOA and NS records must also exist at the apex, and CNAME would override every other record type at that name. CDN-specific ALIAS/ANAME records and the new HTTPS record (RFC 9460) work around this.

<Quiz
  id="mid-quiz-1"
  pieceSlug="02-the-resolver-walk"
  lang="en"
  question="When a recursive resolver gets a referral from a TLD server, where does next-hop information come from?"
  choices={[
    { label: "The Answer section", misconception: "A referral has an empty Answer section. Referral data lives in Authority + Additional." },
    { label: "Authority section (NS records) plus Additional section (glue A/AAAA records)", correct: true },
    { label: "Only the Header flags", misconception: "Flags indicate recursion and DNSSEC state, not the referral target." },
    { label: "The Question section repeated", misconception: "The Question section echoes the query; it never carries referral targets." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="02-the-resolver-walk"
  lang="en"
  question="Why is CNAME forbidden at the zone apex (example.com) per RFC 1034?"
  choices={[
    { label: "Modern DNS discourages it but RFC 1034 does not forbid it", misconception: "RFC 1034 forbids it explicitly at the apex." },
    { label: "SOA and NS records must live at the apex — CNAME would override every other record type there", correct: true },
    { label: "CNAME TTL is too low for an apex record", misconception: "TTL is independent of this rule. The real reason is the SOA/NS conflict." },
    { label: "IPv6 requires AAAA, not CNAME", misconception: "AAAA is an address record. Neither AAAA nor CNAME is the issue here." },
  ]}
/>

## UDP, TCP, and EDNS0

DNS defaults to UDP port 53. If a response exceeds the UDP buffer, the server sets the **TC (truncation) flag** and the resolver retries over TCP port 53. TCP is also required for zone transfers (AXFR/IXFR). A firewall that blocks TCP/53 while allowing UDP/53 silently breaks DNSSEC responses and zone transfers.

EDNS0 (RFC 6891) adds an OPT pseudo-record to negotiate larger UDP buffers (typically 1232 or 4096 bytes) and carry extension options including the DNSSEC OK (DO) bit, ECS (client subnet), and EDNS Cookies. Every modern resolver advertises EDNS0; without it DNSSEC responses are truncated.

<DragOrder
  id="mid-drag-1"
  pieceSlug="02-the-resolver-walk"
  lang="en"
  prompt="Order the steps when a client opens https://shop.example.com from a clean cache:"
  items={[
    "Browser asks OS resolver for shop.example.com",
    "OS resolver forwards to configured upstream (ISP or 1.1.1.1)",
    "Resolver walks root → .com → example.com authoritative",
    "Resolver returns A record (IP) to browser",
    "Browser performs TCP handshake to that IP",
    "Browser performs TLS handshake on top of TCP",
    "Browser sends HTTPS request and receives the page",
  ]}
/>

<Inset kind="why" lang="en">
**Stub resolver vs full recursive resolver.** Your operating system runs a stub resolver — a thin client that forwards queries to a configured upstream and caches briefly. Linux uses `systemd-resolved` or `nscd`; macOS uses `mDNSResponder`; Windows uses the DNS Client service. The stub does almost no work itself. The full recursive resolver — the one that walks root→TLD→authoritative — lives upstream (your router, ISP, or a public resolver like 1.1.1.1). Browsers often bypass the OS stub entirely: Chrome's Async DNS Resolver and Firefox's TRR query DoH directly. Flushing the OS stub cache (`sudo systemd-resolve --flush-caches`) does not flush the upstream resolver's cache.
</Inset>

</Explanation>

<SequenceDiagram
  label="Referral chain for cdn.example.co.uk: Root and the .uk TLD each return a referral (NS in Authority plus glue A in Additional), and only the authoritative server returns the answer with the AA bit set"
  hue="var(--d-network)"
  actors={["Resolver", "Root", "TLD", "Authoritative"]}
  messages={[
    { from: "Resolver", to: "Root", label: "QNAME, RD=0" },
    { from: "Root", to: "Resolver", label: "referral: NS .uk + glue (Answer empty)" },
    { from: "Resolver", to: "TLD", label: "QNAME, RD=0" },
    { from: "TLD", to: "Resolver", label: "referral: NS example.co.uk + glue" },
    { from: "Resolver", to: "Authoritative", label: "QNAME, RD=0" },
    { from: "Authoritative", to: "Resolver", label: "Answer: A 203.0.113.10, AA=1" },
  ]}
  caption="Each referral carries NS records in the Authority section and glue A/AAAA in Additional — pointing the resolver one hop deeper without a circular dependency. Only the authoritative reply has a non-empty Answer and the AA bit set."
/>

<KeyTakeaway>A DNS referral carries NS records in Authority and glue A/AAAA in Additional — the resolver follows this chain root→TLD→authoritative. Missing glue on in-bailiwick NS creates a circular dependency and causes SERVFAIL. CNAME is forbidden at the zone apex because SOA and NS must co-exist there.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-the-resolver-walk-retrieval"
  lang="en"
  questions={[
    { q: "What is the operational difference between an authoritative server and a recursive resolver?", a: "An authoritative server owns zones and answers from local zone files; it returns referrals for names outside its authority and never recurses for callers. A recursive resolver owns nothing — it walks the tree from root downward, follows referrals, caches results, and returns the final answer to clients. Roles never mix in production: a mixed server is an open resolver, a known DNS-reflection amplification vector." },
    { q: "Why must glue records exist for in-bailiwick nameservers?", a: "When a zone delegates to ns1.example.com and ns1.example.com is itself inside example.com, the resolver cannot find ns1's IP without first resolving example.com — a circular dependency. The parent zone breaks the cycle by including ns1's A record as glue in the referral response. Without glue the resolver loops or fails with SERVFAIL." },
    { q: "Why does DNS fall back from UDP to TCP for some queries?", a: "DNS uses UDP port 53 by default. When a response exceeds the EDNS0 buffer size (or 512 bytes without EDNS0), the server sets the TC (truncation) flag. The resolver retries over TCP. Zone transfers (AXFR/IXFR) always use TCP. Blocking TCP/53 silently breaks DNSSEC responses and zone replication." },
  ]}
/>

<Recap lang="en">
A recursive resolver walks the DNS tree iteratively, collecting referrals at each level. Each referral carries NS records in the Authority section plus glue A/AAAA records in Additional — together they break any circular dependency for in-bailiwick nameservers. The AA (Authoritative Answer) bit is only set when the responding server owns the zone. DNS record types go far beyond A records: CNAME aliases names, MX routes mail, TXT carries SPF/DKIM, NS delegates zones, SOA defines zone authority. CNAME is forbidden at the zone apex because SOA and NS must co-exist there. DNS defaults to UDP; large responses (DNSSEC, zone transfers) fall back to TCP. EDNS0 extends UDP buffers and carries DNSSEC flags. Now when you see a SERVFAIL or a referral loop in dig output, check the glue first — a missing in-bailiwick A record in the Additional section is the most common silent culprit.
</Recap>
