infographic chart-pie-plain-text
data
  title Who sees SERVFAIL when the chain snaps
  desc One broken DS after a KSK rollover splits users by resolver, not by ISP
  values
    - label Validating resolvers — SERVFAIL (Cloudflare, Google, Quad9)
      value 30
    - label Non-validating resolvers — still resolve
      value 70
theme
  palette #f43f5e #14b8a6
