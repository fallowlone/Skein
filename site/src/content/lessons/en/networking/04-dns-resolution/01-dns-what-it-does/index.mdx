---
concepts:
- dns
- recursive-resolver
- root-server
- tld-server
- authoritative-nameserver
- a-record
- ttl
- dns-cache
deepensInto:
- 02-the-resolver-walk
estMin: 8
lang: en
lessonType: topic
level: junior
order: 1
prereqs:
- 03-tcp-handshake/06-bbr-and-production-ops
slug: 01-dns-what-it-does
sources:
- https://datatracker.ietf.org/doc/html/rfc1034
- https://datatracker.ietf.org/doc/html/rfc1035
- https://hpbn.co/dns-anatomy/
spiral:
- statefulness
status: ready
summary: DNS turns human-friendly names into IP addresses through a hierarchy of servers
  — the phonebook the whole internet depends on.
title: 'DNS: what it does and why it exists'
track: networking
unit: 04-dns-resolution
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook>
You type `google.com` into your browser. The browser does not know where Google is — it knows names, not numbers. Before it can open a TCP connection or send an HTTP request, it needs the IP address. That lookup is DNS, and it happens silently on every page load.
</Hook>

<Crux>DNS maps a name to an IP by asking root, TLD, then authoritative servers in sequence — and caching every answer.</Crux>

<Explanation>

## What DNS does in one sentence

DNS (Domain Name System) translates a human-friendly name like `google.com` into the numeric IP address (`142.250.185.46`) the network layer needs to route packets. Without DNS you would need to memorise dozens of IP addresses per service and update them every time a server moved.

## The tree of servers

DNS is a global hierarchy. Three tiers handle every query:

- **Root servers** — 13 named servers (A–M), each replicated to hundreds of locations via anycast. They do not store domain records; they know which TLD server to ask next.
- **TLD servers** — one set per top-level domain (`.com`, `.org`, `.uk`, `.io`, etc.). They know which authoritative server owns a given domain.
- **Authoritative nameservers** — own the zone files for individual domains (`google.com`, `example.co.uk`). They return the final answer.

<NumbersCard
  id="card-dns-tree"
  title="DNS tree at a glance"
  data-lesson-visual
  stats={[
    { label: "Root servers (named)", value: "13 (A–M)" },
    { label: "Root instances (anycast)", value: "1 000+" },
    { label: "TLD servers", value: "one set per TLD (.com, .org, …)" },
    { label: "Authoritative servers", value: "one per registered domain" },
    { label: "DNS wire protocol", value: "UDP/TCP port 53" },
    { label: "Typical cold lookup latency", value: "30–100 ms" },
    { label: "Typical warm lookup (cached)", value: "under 1 ms" },
  ]}
/>

## The phonebook metaphor

Imagine you want to call a restaurant you know only by name. You call directory assistance (the **recursive resolver**). The operator does not know the number but knows how to find it. She calls a central office (root server), gets redirected to a regional directory (TLD server), and from there to the restaurant's front desk (authoritative server). The front desk gives the number; the operator relays it and writes it down. The next caller asking the same question gets the cached answer instantly.

## The resolution dialogue

A user's browser sends a query to its configured recursive resolver. If the resolver has no cached answer, it walks the tree:

1. **Browser asks resolver:** "What is the IP for `example.com`?"
2. **Resolver asks root:** "Who handles `.com`?" Root: "Ask the `.com` TLD server."
3. **Resolver asks `.com` TLD:** "Who handles `example.com`?" TLD: "Ask this authoritative server."
4. **Resolver asks authoritative:** "IP for `example.com`?" Auth: "`203.0.113.10`, TTL=300."
5. **Resolver returns IP to browser**, caches the answer for 300 seconds.

On the next query within those 300 seconds the resolver skips steps 2–4 and answers from cache.

<DragOrder
  id="jr-drag-1"
  pieceSlug="01-dns-what-it-does"
  lang="en"
  prompt="Order a cold lookup of example.com:"
  items={[
    "Browser asks recursive resolver for example.com",
    "Resolver asks root: who handles .com?",
    "Root replies with referral to .com TLD",
    "Resolver asks .com TLD: who handles example.com?",
    "TLD replies with referral to authoritative server",
    "Resolver asks authoritative: IP of example.com?",
    "Authoritative replies with the A record",
    "Resolver caches and returns IP to browser",
  ]}
/>

## One scenario end to end

You click a link. Underneath, the browser does DNS first — to learn the IP. Once it has the IP it opens a TCP connection to that address, then adds TLS on top, and finally sends the HTTP request. Every new browser tab repeats the DNS step unless the answer is still cached. DNS is always the first step of an internet connection.

<Quiz
  id="jr-quiz-1"
  pieceSlug="01-dns-what-it-does"
  lang="en"
  question="What does DNS actually return for a standard domain lookup?"
  choices={[
    { label: "A website's HTML content", misconception: "HTML is fetched by HTTP after DNS. DNS only returns an address." },
    { label: "An IP address (the A or AAAA record)", correct: true },
    { label: "An encrypted session key", misconception: "TLS handles keys, not DNS." },
    { label: "A list of routers between you and the destination", misconception: "Routing is the IP layer's job; DNS returns the final address." },
  ]}
/>

<Quiz
  id="jr-quiz-2"
  pieceSlug="01-dns-what-it-does"
  lang="en"
  question="Why does caching exist at every layer of DNS?"
  choices={[
    { label: "To save disk space on authoritative servers", misconception: "Caching is about latency and load, not disk storage." },
    { label: "Most names are looked up repeatedly — caching avoids a full tree walk every time", correct: true },
    { label: "To hide the answer from network observers", misconception: "Caches return the same answer; they do not hide it." },
    { label: "Authoritative servers are usually offline", misconception: "They run 24/7. Caching is an optimisation, not a workaround." },
  ]}
/>

<MetaphorComplete
  id="jr-metaphor-1"
  pieceSlug="01-dns-what-it-does"
  lang="en"
  setup="DNS is like a global _______ that translates human-readable names into IP addresses."
  accepted={["phonebook", "directory", "phone book", "address book"]}
  canonical="phonebook (or directory)"
  explanation="DNS is the internet's phonebook: name in, number out. The resolver does the lookup; the authoritative server is the source of truth."
/>

<Inset kind="why" lang="en">
**Why 13 root servers, not one.** DNS was designed in 1987 when the internet was tiny. The 13-server limit came from the 512-byte UDP limit — you can fit exactly 13 IPv4 addresses in one DNS response. Today each "root server" is really hundreds of machines spread across every continent via anycast: your root query reaches the nearest instance, typically under 5 ms, without you knowing which physical machine answered.
</Inset>

</Explanation>

<KeyTakeaway>DNS translates names to IPs via a three-tier hierarchy — root, TLD, authoritative — with a recursive resolver doing the tree walk on your behalf and caching answers per TTL to avoid repeating that walk on every request.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-dns-what-it-does-retrieval"
  lang="en"
  questions={[
    { q: "Why do almost all DNS lookups feel instant after the first one?", a: "Because every layer caches answers. Once a resolver has learned the IP for a name it returns the cached answer for subsequent callers instead of repeating the full tree walk. The first lookup pays full latency (30–100 ms); subsequent lookups within the TTL window are near-free." },
    { q: "What is the role of the recursive resolver, and how does it differ from an authoritative server?", a: "The recursive resolver walks the tree on behalf of the client: it asks root, TLD, and authoritative servers in sequence, caches each answer, and returns the final IP. An authoritative server owns a zone and answers queries only for names in that zone — it does not recurse. The two roles never mix in production." },
    { q: "Name the three tiers of the DNS hierarchy in order.", a: "Root servers (know which TLD to ask), TLD servers (know which authoritative server to ask), authoritative servers (return the final IP from zone files)." },
  ]}
/>

<Recap lang="en">
DNS is a globally distributed hierarchy that maps human-readable names to IP addresses. When a browser needs to connect to a new domain, it asks a recursive resolver. The resolver walks from root servers, to TLD servers, to the authoritative nameserver for that domain, collecting referrals at each step. The final answer — an A record with the IPv4 address and a TTL — is returned to the browser and cached at every layer. TTL governs how long caches hold the answer before re-querying. A warm cache makes DNS nearly instant; a cold walk costs 30–100 ms across three round-trips. DNS happens before TCP, before TLS, and before HTTP — it is the first step of every internet connection.
</Recap>
