---
concepts:
- doh
- dot
- doq
- ecs
- edns-client-subnet
- kaminsky-attack
- sad-dns
- cache-poisoning
- 0x20-encoding
- anycast-resolver
- rrl
- rfc-8484
- rfc-7858
- rfc-9250
- rfc-7871
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: senior
order: 5
prereqs:
- 04-dnssec-chain-of-trust
slug: 05-encrypted-dns
sources:
- https://datatracker.ietf.org/doc/html/rfc8484
- https://datatracker.ietf.org/doc/html/rfc7858
- https://datatracker.ietf.org/doc/html/rfc9250
- https://datatracker.ietf.org/doc/html/rfc7871
- https://blog.cloudflare.com/sad-dns-explained/
- https://netguardia.com/privacy/anonymity/encrypted-dns-in-2026-doh-dot-dnscrypt-and-oblivious-dns-over-https/
- https://packet.guru/blog/DNS-Encryption-in-2026
spiral: []
status: ready
summary: Why plaintext UDP/53 is a surveillance and attack surface, how DoH/DoT/DoQ
  address it, what EDNS Client Subnet leaks, and how Kaminsky''''s attack and SAD
  DNS shaped modern defences.
title: 'Encrypted DNS: DoH, DoT, DoQ, and cache poisoning'
track: networking
unit: 04-dns-resolution
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import poisoningDefenceLayers from "./infographics/poisoning-defence-layers.svg?raw";

<Hook>
Traditional DNS runs over UDP port 53 in plaintext. Every domain you visit is visible to your ISP, every coffee-shop router between you and the resolver, and any nation-state tap on the line. A company building a privacy-focused product cannot let its users' DNS traffic expose their browsing habits. And separately, an attacker who can race the authoritative server can poison a cache and redirect users to a malicious IP — a risk that predates HTTPS and has been partially re-opened twice since it was first patched. By the end of this lesson you will know which encrypted DNS protocol to pick for which threat model, where ECS leaks you anyway, and what "cache poisoning is solved" actually means in 2026.
</Hook>

<Crux>DoH hides DNS in HTTPS; DoT port 853 is trivially blocked; DoQ adds QUIC. ECS leaks subnet. Cache poisoning needs DNSSEC and encryption.</Crux>

<Explanation>

## Why plaintext DNS is a problem

When you build a feature that calls an external API, you probably think about HTTPS for the payload — but do you think about what the DNS lookup for that API hostname reveals? Traditional DNS sends every query in plaintext over UDP port 53. An eavesdropper on the network — your ISP, a shared Wi-Fi router, a government tap — can see every domain you query. The content of the page is protected by HTTPS, but the hostname lookup itself is fully visible. DNS is also unauthenticated in transport: a response can be forged as long as the attacker can guess or observe the transaction ID and source port.

## DoH, DoT, and DoQ compared

| Protocol | Port | Transport | Censor resistance | Notes |
|---|---|---|---|---|
| **DoH** (RFC 8484) | 443 | TLS over TCP/QUIC | High — indistinguishable from HTTPS | Browser default; HTTP/2 HoL blocking on large bursts |
| **DoT** (RFC 7858) | 853 | TLS over TCP | Low — port 853 blockable in one firewall rule | Clean protocol; predictable to block |
| **DoQ** (RFC 9250) | 853 | QUIC (TLS 1.3) | Medium | Per-stream queries; connection migration; 0-RTT |

<NumbersCard
  id="card-encrypted-dns"
  title="Encrypted DNS adoption (2026)"
  data-lesson-visual
  stats={[
    { label: "Firefox DoH adoption (US)", value: "above 85%" },
    { label: "Active DoH resolvers globally", value: "931+" },
    { label: "DoH latency (typical)", value: "12–18 ms" },
    { label: "DoT latency (typical)", value: "20–25 ms" },
    { label: "DoQ latency (APNIC 2026)", value: "12–15 ms (40% lower than DoT)" },
    { label: "Top-100K sites with HTTPS records", value: "4.2%" },
  ]}
/>

## Why DoH beats DoT against censorship

DoT runs on port 853. A censor's firewall can add one rule to drop all traffic to port 853 and every DoT client is silently broken. DoH runs on port 443 alongside ordinary HTTPS traffic — from the network's perspective it is indistinguishable from any HTTPS connection. Blocking port 443 takes down all HTTPS, which is economically untenable. To block DoH specifically, a censor must enumerate and blacklist the resolver's IP addresses — a whack-a-mole game the user can defeat by pointing to a different DoH endpoint.

The same property defeats enterprise DNS filtering. Corporate networks use DNS-based controls to block malicious or policy-violating domains. If browsers bypass the corporate resolver via DoH, the controls fail. Enterprises push a policy (via MDM or group policy) to disable browser DoH and force traffic through the on-premises resolver.

<TraceScenario
  id="sr-trace-1"
  pieceSlug="05-encrypted-dns"
  lang="en"
  scenario="Trace a client connecting via DoH to https://dns.cloudflare.com/dns-query."
  steps={[
    { prompt: "Client is configured for DoH. First step?", reveal: "Client performs a TLS handshake to dns.cloudflare.com:443, using SNI to request the correct certificate." },
    { prompt: "TLS handshake completes. What does the client send next?", reveal: "An HTTP POST to /dns-query with the DNS message in the body, Content-Type: application/dns-message (RFC 8484)." },
    { prompt: "Server responds. What can a network eavesdropper observe?", reveal: "Only that the client connected to a Cloudflare IP on port 443 and exchanged HTTPS traffic. The actual DNS query (the domain being resolved) is encrypted inside TLS. The ISP sees neither the query nor the answer." },
    { prompt: "What happens if the DoH server is slow or down?", reveal: "Client waits for timeout (~5 s) or falls back to plaintext UDP/53 if configured. Fallback undoes the privacy benefit entirely." },
    { prompt: "How does DoH differ from DoT in terms of censor resistance?", reveal: "DoT uses a dedicated port 853 — one firewall rule blocks it wholesale. DoH runs on port 443 alongside all HTTPS; blocking 443 takes down the whole web, so censors must target specific IPs instead." },
  ]}
/>

## DoQ specifics (RFC 9250)

DNS over QUIC uses QUIC streams: each query is a separate stream with no head-of-line blocking (a problem that plagues DoH over HTTP/2 when one query stalls). QUIC connection migration allows the same DoQ session to survive a Wi-Fi → LTE handover without a re-handshake — important for mobile clients. 0-RTT resumption sends the DNS query inside the ClientHello when a cached session ticket exists, saving one RTT.

RFC 9250 warns that 0-RTT data is replayable — an attacker capturing a 0-RTT DoQ query could replay it to time-correlate a user. Mitigation: single-use resumption tokens, fresh QUIC connections after location changes. Production: Quad9, AdGuard, and NextDNS ship DoQ; Cloudflare, Quad9 place `dnsdist` or `doqd` in front of BIND/Unbound.

## EDNS Client Subnet (ECS): geo-routing vs privacy

ECS (RFC 7871) lets a recursive resolver attach the client's IP prefix (typically /24 IPv4 or /56 IPv6) to upstream queries. CDN authoritatives use the prefix to return the nearest edge IP. Without ECS the authoritative can only see the resolver's IP — which may be hundreds of kilometres from the actual user.

The privacy cost is real: every authoritative server in the upstream path now learns roughly which neighbourhood is browsing the domain. Cloudflare 1.1.1.1 disables ECS by default for this reason. Google 8.8.8.8 enables it by default. Using a DoH resolver that then forwards ECS upstream negates most of the privacy benefit of encrypting the transport.

## Cache poisoning: Kaminsky, SAD DNS, and defences

**Kaminsky (2008).** Original DNS used a 16-bit transaction ID plus a fixed source UDP port. An attacker triggering many queries for random subdomains of a target zone could race the legitimate authoritative response and inject a forged record — if the forged response arrived first with the right 16-bit ID (65536 attempts in the worst case, far fewer in practice). Patch: RFC 5452 mandates randomising the source UDP port (~65K ports × 65K IDs = ~4 billion combinations).

**0x20 encoding.** Mixes uppercase and lowercase in the QNAME; well-behaved authoritatives echo the exact capitalisation. An attacker who cannot observe the query does not know the capitalisation pattern, adding ~30 bits of effective entropy.

**SAD DNS (2020).** Revived the threat: Linux's global ICMP rate-limit counter is observable from another machine. An off-path attacker can send spoofed ICMP error packets to infer the source port being used by the resolver for a given query — collapsing the 4-billion search space dramatically. Linux mitigated by randomising per-socket ICMP rate-limit state. Practical defence: DNSSEC + DoH/DoT — once the channel is authenticated and encrypted, the attack surface for classic spoofing disappears.

Together these three mitigations build in layers: source-port randomisation raises the bar to 4 billion guesses, 0x20 adds entropy the attacker cannot observe, and DNSSEC makes a successfully injected record useless because it fails authentication. Without all three, each mitigation is individually bypassable.

<Infographic
  svg={poisoningDefenceLayers}
  label="Four ascending defence layers against DNS cache poisoning: original 16-bit TXID with a fixed port gives only 65536 forgery guesses; source-port randomisation (RFC 5452) raises it to ~4 billion combinations; 0x20 case-mixing adds ~30 bits an off-path attacker cannot observe; DNSSEC plus encrypted transport make any injected record fail authentication and remove off-path injection entirely."
  caption="Each layer multiplies attacker cost — source-port randomisation, 0x20 entropy, then DNSSEC — and none alone is sufficient."
  hue="var(--d-infra)"
/>

<TradeoffMatrix
  id="sr-tradeoff-1"
  pieceSlug="05-encrypted-dns"
  lang="en"
  prompt="A privacy-focused mobile app chooses an encrypted DNS protocol. Network conditions vary; port blocking is possible."
  options={[
    { name: "DoH on port 443", summary: "Looks like HTTPS to network.", justification: "Best censorship resistance — indistinguishable from HTTPS. HTTP/2 HoL blocking stalls queries on congested links but HTTP/3 fixes this. Most widely supported (Chrome, Firefox, Safari)." },
    { name: "DoT on port 853", summary: "Dedicated TLS port.", justification: "Easy server-side deploy. Port 853 trivially blockable — hostile ISPs block it wholesale. Bad choice for adversarial networks." },
    { name: "DoQ on UDP port 853", summary: "QUIC-based modern.", correct: true, justification: "TLS 1.3 confidentiality + QUIC connection migration (survives Wi-Fi→LTE) + 0-RTT resumption + per-stream queries eliminate HoL. Mobile-first. Adoption growing; Cloudflare, Quad9, AdGuard ship it. Port 853 still blockable but DoQ's QUIC transport can be proxied over 443 if needed." },
    { name: "Plain UDP/53", summary: "No encryption.", justification: "Every query visible to network observers. Worst privacy choice. Only acceptable as a fallback." },
  ]}
/>

<Quiz
  id="sr-quiz-1"
  pieceSlug="05-encrypted-dns"
  lang="en"
  question="Why does DoH defeat network-level domain censorship better than DoT?"
  choices={[
    { label: "DoH uses stronger encryption than DoT", misconception: "Both use TLS. The difference is port, not encryption strength." },
    { label: "DoT uses a dedicated port (853) that censors can block wholesale; DoH runs on port 443 alongside all HTTPS traffic and cannot be blocked without taking down the web", correct: true },
    { label: "DoH responses are smaller so they complete before a firewall can inspect them", misconception: "Response size is unrelated to censor resistance. The key is that port 443 is economically impossible to block." },
    { label: "DoH is authenticated by DNSSEC while DoT is not", misconception: "DNSSEC and DoH/DoT are orthogonal. DNSSEC validates data integrity; DoH/DoT encrypt the channel. Both can be used independently." },
  ]}
/>

<Inset kind="why" lang="en">
**EDNS Cookies (RFC 7873).** A lightweight per-pair authentication mechanism: client and server each maintain a 64-bit secret used to compute a cookie sent in every query and response. Cookies do not encrypt anything but make forging arbitrary off-path source IPs significantly harder — an attacker who cannot observe the exchange cannot compute a valid cookie. EDNS Cookies cut DNS amplification reflection attacks by orders of magnitude when enabled at the resolver tier. BIND and Knot Resolver enable them by default. Some firewall middleboxes strip the EDNS Cookie option and silently break the protection.
</Inset>

## Anycast resolver routing

Public resolvers (1.1.1.1, 8.8.8.8, 9.9.9.9) announce one IP prefix from dozens or hundreds of PoPs worldwide via BGP anycast. BGP picks the shortest AS-path from each ISP to the nearest PoP. Cloudflare has 100+ PoPs, Google has ~40, Quad9 ~150. Pitfall: if your ISP peers with the operator only in a distant city, your traffic to "the nearest" PoP traverses that city — a typical 30 ms penalty. Diagnose with `dig +short @1.1.1.1 whoami.cloudflare TXT CH` to see which PoP answered.

</Explanation>

<SequenceDiagram
  label="DoH conceals a DNS query inside HTTPS: the client opens TLS to the resolver on port 443 and POSTs the DNS message in the encrypted body, so the on-path eavesdropper sees only HTTPS to a resolver IP, never the domain"
  hue="var(--d-network)"
  actors={["Client", "Eavesdropper", "DoH resolver"]}
  messages={[
    { from: "Client", to: "DoH resolver", label: "TLS handshake :443 (SNI)" },
    { from: "Client", to: "DoH resolver", label: "POST /dns-query (encrypted body)" },
    { from: "DoH resolver", to: "Client", label: "encrypted DNS answer" },
    { from: "DoH resolver", to: "Eavesdropper", label: "sees only: HTTPS to resolver IP:443" },
  ]}
  caption="Because the query and answer ride inside TLS on port 443, the eavesdropper cannot tell DoH apart from any other HTTPS connection — only the resolver IP and port are visible. DoH hides which domains you look up; DoT on port 853 carries the same TLS but on a port one firewall rule can block."
/>

<KeyTakeaway>DoH hides DNS on port 443, making queries indistinguishable from HTTPS and defeating port-blocking censors; DoT on 853 is easily blocked; DoQ adds QUIC migration and 0-RTT for mobile. ECS improves CDN geo-routing but leaks client subnet to every upstream server. DNSSEC + encrypted transport together defeat cache poisoning — neither alone is sufficient.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-encrypted-dns-retrieval"
  lang="en"
  questions={[
    { q: "An engineer claims EDNS Client Subnet is mandatory for accurate CDN geo-routing. Where is the flaw in that claim?", a: "ECS sharpens CDN geo-routing by providing the client's IP prefix to the authoritative CDN server, allowing it to return the nearest edge. But without ECS the CDN uses the resolver's IP — accurate enough for most cases because ISP resolvers are usually near their clients. ECS is an optimisation, not a requirement. The privacy cost is real: every authoritative server in the upstream path sees the client's coarse location. Cloudflare disables ECS by default for this reason. The honest trade-off: slight latency improvement in return for advertising your subnet to every domain you visit." },
    { q: "Describe the Kaminsky attack and the two defences that closed it.", a: "Kaminsky (2008) showed that DNS's 16-bit transaction ID plus fixed source port gave only 65536 combinations to guess. An attacker triggering many queries for random subdomains of a target zone could inject a forged response with the correct ID before the legitimate authoritative replied, poisoning the cache. Defences: (1) RFC 5452 source-port randomisation adds ~65K ports to the guess space, yielding ~4 billion combinations; (2) 0x20 case-mixing adds capitalisation entropy the attacker cannot predict. DNSSEC removes the attack surface entirely by authenticating responses cryptographically." },
    { q: "What is the difference between what DoH/DoT provide and what DNSSEC provides?", a: "DoH/DoT encrypt the DNS channel — they hide which domains you are querying from network observers between you and the resolver. DNSSEC authenticates DNS responses — it proves the A record data was signed by the legitimate zone's key and was not forged in transit. The two are orthogonal: DoH without DNSSEC hides queries but cannot verify the answer's integrity; DNSSEC without DoH authenticates the data but leaves the query visible to ISPs and eavesdroppers. Full protection requires both." },
  ]}
/>

<Recap lang="en">
Plaintext DNS over UDP port 53 exposes every domain query to ISPs and network observers. DoH (port 443) hides queries inside HTTPS, making it indistinguishable from normal web traffic and resistant to port-blocking censorship. DoT (port 853) offers the same TLS encryption but on a dedicated port trivially blocked by firewalls. DoQ (QUIC on port 853) adds connection migration and 0-RTT for mobile environments. EDNS Client Subnet improves CDN geo-routing accuracy but broadcasts the client's IP prefix to every upstream authoritative — Cloudflare disables it by default. Cache poisoning via Kaminsky's 2008 attack and the 2020 SAD DNS side-channel are mitigated by source-port randomisation, 0x20 encoding, and DNSSEC; using an encrypted transport removes the off-path injection attack surface entirely. Anycast routing ensures public resolvers answer from the nearest PoP; diagnose PoP selection with `dig +short @1.1.1.1 whoami.cloudflare TXT CH`. Now when you evaluate a DNS provider for a privacy-focused product, check three things: Does it support DoH or DoQ? Does it disable ECS by default? Does it validate DNSSEC? A "yes" on all three is the minimum bar.
</Recap>
