infographic chart-column-simple
data
  title Retry spread after a mass disconnect: jitter vs none
  desc Same client count, same restart — jitter turns a SYN spike into a gentle ramp the server can absorb
  values
    - label No jitter (fixed 1s) — thundering herd
      value 1
    - label Jittered backoff (±50%)
      value 10
theme
  palette #f43f5e #14b8a6
