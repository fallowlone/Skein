---
slug: 01-upgrade-handshake
lang: en
track: networking
unit: 08-websocket-realtime
order: 1
title: "WebSocket: the HTTP upgrade handshake"
summary: "How a normal HTTP request becomes a persistent bidirectional message pipe in one round-trip — and what each header in the upgrade does."
estMin: 10
status: ready
lessonType: topic
level: junior
prereqs:
  - "03-tcp-handshake/01-the-three-way-handshake"
  - "05-tls-handshake/01-what-tls-does"
  - "06-http-versions/01-http-fundamentals"
deepensInto:
  - "02-frame-format"
  - "03-transport-alternatives"
spiral: ["statefulness", "latency"]
concepts: ["websocket", "http-upgrade", "101-switching-protocols", "sec-websocket-key", "sec-websocket-accept", "full-duplex"]
sources:
  - https://datatracker.ietf.org/doc/html/rfc6455
  - https://developer.mozilla.org/en-US/docs/Web/API/WebSockets_API/Writing_WebSocket_servers
  - https://hpbn.co/websocket/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook>
You open Discord. A friend sends you a message. Your browser shows it instantly — no page reload, no polling. That instant delivery flows through a persistent TCP connection that HTTP alone cannot provide. WebSocket gives both sides the ability to send at any moment, without asking first.
</Hook>

<Crux>WebSocket upgrades HTTP to a full-duplex frame pipe in one round-trip — after the 101, both sides send freely without waiting for a request.</Crux>

<Explanation>

## The problem HTTP cannot solve

HTTP is request-response: the client asks, the server answers, then the conversation is over. For live chat, stock tickers, multiplayer games, or collaborative editing — anything where the **server needs to push data without waiting for a new client request** — HTTP is structurally wrong. Each HTTP request adds at minimum one full round-trip of latency before any data can flow back.

WebSocket solves this by converting an existing HTTP connection into a persistent **bidirectional channel** where either side can send at any time.

## The metaphor

With HTTP you send letters at the post office: you write one, mail it, and wait for a reply. With WebSocket you pick up a telephone: once connected, both sides talk freely without taking turns.

## The upgrade handshake step by step

The connection starts as a normal HTTP/1.1 request. The client signals that it wants to switch protocols:

```
GET /chat HTTP/1.1
Host: example.com
Upgrade: websocket
Connection: Upgrade
Sec-WebSocket-Key: dGhlIHNhbXBsZSBub25jZQ==
Sec-WebSocket-Version: 13
```

Each header has a precise purpose:

- **`Upgrade: websocket`** — declares the target protocol.
- **`Connection: Upgrade`** — tells intermediate proxies this is a protocol switch, not a standard keepalive.
- **`Sec-WebSocket-Key`** — 16 random bytes, base64-encoded. The server uses it to prove it intentionally processed the upgrade (not a cached HTTP response — see the Inset below).
- **`Sec-WebSocket-Version: 13`** — specifies RFC 6455, the only version used in practice.

The server validates the request (GET method, HTTP/1.1, valid headers) and replies:

```
HTTP/1.1 101 Switching Protocols
Upgrade: websocket
Connection: Upgrade
Sec-WebSocket-Accept: s3pPLMBiTxaQ9kYGzzhZRbK+xOo=
```

The `Sec-WebSocket-Accept` value is computed as:

```
base64(SHA-1(Sec-WebSocket-Key + "258EAFA5-E914-47DA-95CA-C5AB0DC85B11"))
```

The fixed string (`258EAFA5-…`) is a magic GUID defined in the RFC. After the 101 response, **both sides stop speaking HTTP**. The TCP connection is now a raw WebSocket stream.

<NumbersCard
  id="card-ws-handshake-numbers"
  title="WebSocket handshake facts"
  data-lesson-visual
  stats={[
    { label: "Extra round-trips for WebSocket upgrade", value: "0 (uses the existing HTTP connection)" },
    { label: "HTTP status code for upgrade success", value: "101 Switching Protocols" },
    { label: "RFC defining WebSocket", value: "RFC 6455 (2011)" },
    { label: "Sec-WebSocket-Version in production", value: "13 (only version used)" },
    { label: "Frame header overhead (small server→client frame)", value: "2 bytes" },
    { label: "Message latency after handshake", value: "0–5 ms" },
  ]}
/>

## One scenario end to end

You open Discord in your browser:

1. Browser does TCP + TLS to `gateway.discord.gg`.
2. Browser sends the HTTP Upgrade request above.
3. Server replies 101. Both sides discard HTTP parsing.
4. From now on, Discord's server pushes message frames to your browser the moment a friend sends something. Your browser sends frames (typing indicators, messages) in the other direction without opening a new connection.
5. When you close the tab, a **close frame** is exchanged and the TCP connection ends.

<Inset kind="why" lang="en">
**Why the Sec-WebSocket-Accept computation defends against cache poisoning.** In the early web, a malicious JavaScript on `site-a.com` could open a TCP connection to an intermediate proxy and craft bytes that looked like a valid HTTP response. If the proxy cached those bytes, other users' requests could be answered with malicious content. The `Sec-WebSocket-Key` + GUID + SHA-1 roundtrip makes it essentially impossible for a JavaScript to forge an Accept header without knowing the exact key the server would generate — the proxy cannot cache the result of a computation it never performed.
</Inset>

<Quiz
  id="ws-jr-quiz-1"
  pieceSlug="01-upgrade-handshake"
  lang="en"
  question="Why does WebSocket need an HTTP Upgrade request instead of just opening a raw TCP connection to a new port?"
  choices={[
    { label: "Because proxies don't understand raw TCP", misconception: "Proxies understand TCP fine. The real reason is that the connection already starts as HTTP and must formally switch." },
    { label: "Because the connection already exists as HTTP, so the protocol must formally agree to stop speaking HTTP and start speaking WebSocket frames", correct: true },
    { label: "To encrypt the connection", misconception: "TLS is a separate layer applied before the WebSocket upgrade." },
    { label: "For backwards compatibility with old servers", misconception: "It is a forward-looking design to reuse existing HTTP connections." },
  ]}
/>

<Quiz
  id="ws-jr-quiz-2"
  pieceSlug="01-upgrade-handshake"
  lang="en"
  question="After a WebSocket upgrade completes with 101 Switching Protocols, what is the HTTP protocol used for?"
  choices={[
    { label: "HTTP is still used for every frame", misconception: "WebSocket frames have their own binary format; HTTP is no longer spoken." },
    { label: "HTTP headers are still sent with each frame", misconception: "After the upgrade, HTTP is abandoned; WebSocket uses a compact binary frame format." },
    { label: "The connection is no longer HTTP — it is pure WebSocket until closed", correct: true },
    { label: "HTTP is used for control frames only", misconception: "All frames, control and data, use the WebSocket binary format after upgrade." },
  ]}
/>

<DragOrder
  id="ws-jr-drag-1"
  pieceSlug="01-upgrade-handshake"
  lang="en"
  prompt="Order the WebSocket handshake steps:"
  items={[
    "Client sends HTTP GET with Upgrade: websocket and Sec-WebSocket-Key",
    "Server validates the request and computes Sec-WebSocket-Accept",
    "Server replies with 101 Switching Protocols and the Accept header",
    "Both sides drop HTTP; the connection becomes bidirectional WebSocket",
    "Either side can now send frames at any time",
  ]}
/>

<MetaphorComplete
  id="ws-jr-metaphor-1"
  pieceSlug="01-upgrade-handshake"
  lang="en"
  setup="Fill in the blank: WebSocket is like _______ where both sides can talk freely without taking turns."
  accepted={["a phone call", "a conversation", "a dialogue", "chat", "talking", "speaking"]}
  canonical="a phone call (or face-to-face conversation)"
  explanation="Unlike HTTP where the client makes a request and waits for a response, WebSocket lets both sides send at any moment, just like a real conversation where either party can speak."
/>

</Explanation>

<KeyTakeaway>WebSocket reuses an HTTP/1.1 connection: one GET + 101 response switches both sides to a binary frame protocol where either can send at any time. The Sec-WebSocket-Key/Accept exchange proves the server intentionally processed the upgrade, not a cache.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-upgrade-handshake-retrieval"
  lang="en"
  questions={[
    { q: "In one sentence: why doesn't a server just use repeated HTTP requests to push data to the client?", a: "Each HTTP request requires the client to initiate it and the server to respond — a full round-trip of latency every time; WebSocket keeps one persistent connection open so the server can push instantly without waiting for a new request, eliminating the RTT cost between each push." },
    { q: "What does the 101 Switching Protocols response signal, and what happens on the wire immediately after?", a: "101 signals that both sides agree to abandon HTTP and switch to the WebSocket frame protocol; immediately after the server sends the 101 response, the TCP connection carries raw WebSocket frames in both directions — no more HTTP headers are exchanged." },
    { q: "Why is Sec-WebSocket-Key added to a fixed GUID before hashing, rather than hashed directly?", a: "The fixed GUID (258EAFA5-…) ensures that only a WebSocket-aware server can compute the correct Accept value — a generic HTTP cache or proxy that just echoes the request would not know the GUID, so it cannot forge the correct Sec-WebSocket-Accept header." },
  ]}
/>

<Recap lang="en">
HTTP is pull-only: every data delivery requires the client to ask first. WebSocket breaks that constraint by upgrading a normal HTTP/1.1 connection into a full-duplex channel. The upgrade requires exactly one extra HTTP round-trip: a GET with `Upgrade: websocket` and a random `Sec-WebSocket-Key`, answered by `101 Switching Protocols` and a SHA-1-derived `Sec-WebSocket-Accept`. After the 101 response, no HTTP is spoken — both sides exchange compact binary frames where either can initiate. The entire WebSocket handshake adds zero extra TCP connections and zero extra TLS sessions. Message latency after the handshake is 0–5 ms, limited only by the network RTT.
</Recap>
