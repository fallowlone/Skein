---
slug: 03-transport-alternatives
lang: en
track: networking
unit: 08-websocket-realtime
order: 3
title: "WebSocket vs SSE vs long-polling: choosing the right transport"
summary: "Latency, overhead, and proxy compatibility compared across WebSocket, Server-Sent Events, and long-polling — so you pick the right tool before you build."
estMin: 10
status: ready
lessonType: topic
level: middle
prereqs:
  - "08-websocket-realtime/01-upgrade-handshake"
  - "06-http-versions/01-http-fundamentals"
deepensInto:
  - "07-production-architecture"
spiral: ["latency"]
concepts: ["server-sent-events", "sse", "long-polling", "eventsource", "subprotocol", "permessage-deflate", "transport-choice"]
sources:
  - https://datatracker.ietf.org/doc/html/rfc6455
  - https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events
  - https://hpbn.co/websocket/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook>
Before you reach for WebSocket, ask whether you actually need bidirectional communication. SSE handles 90% of "server needs to push" use cases with zero custom reconnection logic and friendlier proxies. Picking the wrong transport costs you weeks of debugging or unnecessary complexity.
</Hook>

<Crux>WebSocket wins on latency and bidirectionality; SSE wins on simplicity and proxy compat; long-polling is the HTTP-only fallback.</Crux>

<Explanation>

## Three transports, three models

| | WebSocket | SSE | Long-polling |
|--|-----------|-----|-------------|
| Direction | **Bidirectional** | Server→client only | Server→client (client polls) |
| Protocol | Binary frame (RFC 6455) | `text/event-stream` over HTTP | Plain HTTP |
| Overhead | 2–6 bytes per frame | ~5 bytes + metadata per event | ~500+ bytes headers per request |
| Latency | 0–5 ms | 3–8 ms | Poll interval + RTT (100–60000 ms) |
| Reconnect | Application must implement | Browser `EventSource` handles it | Application re-opens request |
| Proxy support | Requires `Upgrade` support | Works with any HTTP proxy | Works everywhere |

## WebSocket — full-duplex, minimal overhead

After the upgrade handshake, either side sends frames at any time. Per-message overhead is 2–6 bytes. The server can push without the client asking; the client can push without a new HTTP connection. This is the right choice when:

- The client sends data frequently (game input, collaborative editing, trading orders).
- Latency budget is under 10 ms.
- You control the proxy/infrastructure (or know it supports `Upgrade`).

Downside: no built-in reconnection. If the connection drops (proxy idle timeout, network glitch), your code must detect close code 1006 and reconnect with backoff.

## SSE — server push, browser handles everything

SSE uses a normal persistent HTTP response with `Content-Type: text/event-stream`. The server writes newline-delimited events:

```
event: price-update
data: {"symbol":"AAPL","price":182.34}
id: 4291

```

The browser's native `EventSource` API parses events and automatically reconnects using the `Last-Event-ID` header if the connection drops. Latency is 3–8 ms. Zero reconnection code to write. Works through any HTTP/1.1 proxy including corporate firewalls.

SSE is wrong when:
- The client needs to send data (you need a separate REST endpoint for that, which is awkward).
- You need binary payloads (SSE is text-only).

## Long-polling — the universal fallback

The client opens an HTTP request and the server holds it open until data arrives (or a timeout fires). When the server responds, the client immediately opens a new request. This simulates server push using only standard HTTP.

Latency = the time the server holds the request + one RTT. At best, 100 ms for a server that answers immediately. At worst, 60+ seconds for a server with a long timeout before giving up and responding empty.

Overhead is high: every poll carries full HTTP headers (~500+ bytes). At 50,000 clients polling every 100 ms, that is 500,000 HTTP requests/second. Essentially no modern application uses long-polling as a primary transport — it is the graceful fallback for environments that block WebSocket.

<NumbersCard
  id="card-transport-comparison"
  title="Transport latency and overhead comparison"
  data-lesson-visual
  stats={[
    { label: "WebSocket frame header (server→client)", value: "2 bytes" },
    { label: "WebSocket message latency after handshake", value: "0–5 ms" },
    { label: "SSE event overhead (minimum)", value: "~5 bytes + metadata" },
    { label: "SSE message latency", value: "3–8 ms" },
    { label: "Long-poll HTTP headers per request", value: "~500+ bytes" },
    { label: "Long-poll effective latency (best case)", value: "100–500 ms" },
  ]}
/>

## Subprotocol and extension negotiation

The client can request an application-layer subprotocol during the upgrade:

```
Sec-WebSocket-Protocol: chat, superchat
```

The server picks one and echoes it back. Subprotocols let teams version their message format without touching the WebSocket layer — `chat` uses JSON messages, `superchat` uses a compact binary format. If the server sends no subprotocol header, the connection has no named protocol (still valid; the application defines the message format implicitly).

Extensions work similarly via `Sec-WebSocket-Extensions`. The most important extension in practice is `permessage-deflate` (RFC 7692): DEFLATE compression per message. On text payloads (JSON, HTML) compression ratios of 50–90% are typical. On small messages (< 64 bytes) compression can expand the payload. On already-compressed data (images, video) it achieves nothing. The memory cost is significant: at default settings, each compressor consumes ~256 KB and each decompressor ~44 KB — at 100k idle connections, that is ~25 GB of RAM from compression state alone. Most production deployments disable `permessage-deflate` by default.

<Inset kind="why" lang="en">
**Why SSE is underused.** SSE was standardised in HTML5 in 2009 but was largely ignored because WebSocket launched at the same time and was more exciting. In practice, SSE covers most notification, feed, and dashboard use cases cleanly: server pushes events, browser reconnects automatically, no special proxy configuration needed. The cases that truly require WebSocket are those where the client sends data at high frequency — games, collaborative editors, trading terminals.
</Inset>

<Quiz
  id="ws-mid-quiz-3"
  pieceSlug="03-transport-alternatives"
  lang="en"
  question="A company has 1,000 WebSocket connections. The old approach opens 1,000 TCP connections to the backend, each consuming ~10 KB of memory at idle. Moving to HTTP/2 extended CONNECT (RFC 8441), the client multiplexes all WebSocket connections over one TCP connection consuming 50 KB total. What is the approximate memory saving?"
  choices={[
    {
      label: "9.95 MB (99.5% reduction from 10 MB to 50 KB)",
      correct: true,
      misconception: "HTTP/2 multiplexing concentrates TCP socket state into one connection, freeing ~9.95 MB. Streams in HTTP/2 are just integer IDs — negligible memory.",
    },
    {
      label: "1 MB (only TLS session memory saved)",
      correct: false,
      misconception: "TLS session memory is a few KB per connection. The bulk of idle connection memory is TCP socket kernel buffers.",
    },
    {
      label: "No savings — each CONNECT stream still allocates 10 KB",
      correct: false,
      misconception: "HTTP/2 streams share one TCP connection's buffers. A stream is 4-byte stream ID + 4-byte flow control window — not a full socket.",
    },
    {
      label: "10 MB (HTTP/2 eliminates all connection state)",
      correct: false,
      misconception: "HTTP/2 still maintains TCP state (the one connection) and HTTP/2 frame state. You save ~9.95 MB, not 10 MB.",
    },
  ]}
/>

<TradeoffMatrix
  id="ws-mid-tradeoff-1"
  pieceSlug="03-transport-alternatives"
  lang="en"
  prompt="A notification system pushes ~5 updates per minute to 100k users. Users never send data to the server. Which transport?"
  options={[
    { name: "WebSocket", summary: "Full duplex, binary frames.", justification: "Bidirectionality is unnecessary here — users only receive. WebSocket adds upgrade complexity and requires reconnection logic with no payoff over SSE for this use case." },
    { name: "SSE (Server-Sent Events)", summary: "Server push, auto-reconnect.", correct: true, justification: "Server-only push, browser EventSource handles reconnection automatically with Last-Event-ID, works through all HTTP proxies, text/event-stream format is simple to debug. At 5 updates per minute latency difference vs WebSocket is irrelevant. Correct choice." },
    { name: "Long-polling", summary: "HTTP-only fallback.", justification: "At 100k users, long-polling generates 100k open HTTP requests simultaneously on the server. Header overhead is 500+ bytes per poll. This works but wastes bandwidth and server connections compared to SSE." },
    { name: "WebSocket over HTTP/2 with multiplexing", summary: "Advanced multiplexed transport.", justification: "Overkill. HTTP/2 multiplexing is valuable when you need many concurrent WebSocket streams from the same client. Here you only need one notification channel per user." },
  ]}
/>

</Explanation>

<KeyTakeaway>WebSocket (0–5 ms, bidirectional) beats SSE (3–8 ms, server-push) on latency and direction; SSE beats WebSocket on proxy friendliness and built-in reconnect. Long-polling is the universal fallback. Match the transport to data-flow direction before optimizing latency.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-transport-alternatives-retrieval"
  lang="en"
  questions={[
    { q: "Name two use cases where SSE is a better choice than WebSocket, and explain why.", a: "1) Notification feeds or dashboards: data flows server→client only, browser EventSource reconnects automatically with Last-Event-ID, zero custom reconnection code needed. 2) Environments with strict HTTP proxies (corporate networks): SSE is plain HTTP/1.1 and passes every proxy; WebSocket requires Upgrade support which many corporate proxies and firewalls block or mangle." },
    { q: "Why does permessage-deflate consume so much memory at scale, and what is the production mitigation?", a: "At default window_bits=15, each DEFLATE compressor context uses ~256 KB and each decompressor ~44 KB; at 100k idle connections that is ~25 GB just for compression state. Mitigation: disable permessage-deflate entirely for idle connections, or negotiate a smaller window_bits (e.g., 12) which drops per-connection memory to ~50 KB at the cost of lower compression ratio." },
    { q: "A browser sends data to the server every 50 ms (e.g., mouse positions for a collaborative editor). Why is SSE insufficient here, and what is the correct transport?", a: "SSE is server→client only; the browser cannot send on the SSE stream. For frequent client-to-server sends you would need separate HTTP POST requests at 50 ms intervals — 20 requests/second per user, each with ~500 bytes of headers. WebSocket is the correct choice: the browser sends frames (2–6 bytes overhead, no new connections) over the persistent WebSocket connection, keeping latency below 5 ms and overhead minimal." },
  ]}
/>

<Recap lang="en">
Three transports solve the "server needs to push" problem at different trade-offs. WebSocket offers the lowest latency (0–5 ms) and true bidirectionality — the right choice when clients send data frequently. SSE streams `text/event-stream` over a persistent HTTP connection, with the browser's `EventSource` handling reconnection via `Last-Event-ID` automatically — the right choice for server-push-only use cases in proxy-heavy environments. Long-polling simulates push over plain HTTP at 500+ bytes of header overhead per poll and ~100–500 ms effective latency — it is the universal fallback, not a primary transport. WebSocket subprotocols let teams version their message schema; the `permessage-deflate` extension adds 50–90% text compression but costs ~256 KB RAM per connection compressor context.
</Recap>
