infographic chart-column-simple
data
  title WebSocket timeout budget (seconds)
  desc Default 60s idle timeout silently drops quiet connections — raise it 60x and ping under the limit
  values
    - label Proxy default idle timeout
      value 60
    - label Server ping interval
      value 30
    - label Slow-reader close threshold
      value 30
    - label Production read/send timeout
      value 3600
theme
  palette #f43f5e #f59e0b #8b5cf6 #14b8a6
