---
concepts:
- ip-packet
- hop-by-hop-routing
- best-effort
- ipv4
- ipv6
- ttl
deepensInto:
- 02-reading-the-ip-header
estMin: 10
lang: en
lessonType: topic
level: junior
order: 1
prereqs:
- 01-physical-link/01-bits-on-the-wire
slug: 01-the-ip-envelope
sources:
- https://datatracker.ietf.org/doc/html/rfc791
- https://datatracker.ietf.org/doc/html/rfc8200
- https://hpbn.co/building-blocks-of-tcp/
spiral:
- encapsulation
status: ready
summary: IP is the addressed envelope every chunk of Internet traffic travels inside
  — no router knows the full route, yet millions cooperate to deliver the packet.
title: The IP envelope
track: networking
unit: 02-ip-packet
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
At the moment you click a link, your browser packages your request into an IP packet — an envelope with your address and the server's address. Here is the radical part: no single router between you and that server knows the complete path. Each router just looks at the destination address, consults its own local map, and forwards the packet to the next hop.
</Hook>

<Crux>How does a packet find a server when no router knows the full route?</Crux>

<Explanation>

## What IP does in one sentence

IP is the addressed envelope every chunk of Internet traffic travels inside. Source address, destination address, payload, and a small header — that's IP.

Every protocol you use — TCP, UDP, ICMP, every video chunk, every webpage — rides inside IP packets. The packet is the unit of routing for the entire Internet.

## The postal metaphor

Imagine the postal system. Each letter has a from-address and a to-address. Local post offices don't know where Tokyo is, but each one knows "anything for Asia goes via this truck." Letters hop from local office to regional to national until they reach the destination's local office. No single office holds a map of the whole world — only the next hop.

IP works the same way. Routers hold partial maps and make local decisions. At each hop the router reads the destination address, checks its routing table, and forwards the packet to whichever neighbour is the best next step. After 10–15 such hops the packet arrives.

## One scenario end to end

Your browser already finished the physical link (it knows how to put bits on a wire). It now builds an IP packet for every segment it wants to send. Each packet travels independently. The layers above — TCP, TLS, HTTP — all hand bytes to IP, which adds the envelope.

<NumbersCard
  id="card-ip-basics"
  title="IP basics"
  data-lesson-visual
  stats={[
    { label: "IPv4 address size", value: "32 bits (4 bytes)" },
    { label: "IPv6 address size", value: "128 bits (16 bytes)" },
    { label: "IPv4 address space", value: "~4.3 billion" },
    { label: "Typical hop count", value: "10–15 routers" },
    { label: "IPv4 header minimum", value: "20 bytes" },
    { label: "IPv6 header (fixed)", value: "40 bytes" },
  ]}
/>

## IPv4 vs IPv6

IPv4 uses 32-bit addresses written as four decimal octets (e.g. `93.184.216.34`). IPv6 uses 128-bit addresses written as eight 16-bit hex groups (e.g. `2001:db8::1`). The practical differences:

- **Address space.** IPv4 has ~4.3 billion addresses — exhausted. IPv6 has 340 undecillion — effectively unlimited.
- **Header.** IPv4 has a variable header with a checksum computed at every hop. IPv6 has a fixed 40-byte header with no checksum (delegated to L2 and L4) and no router-level fragmentation.
- **Adoption.** Most new deployments run both (dual-stack). IPv6 reached ~45–50% of global Internet traffic by 2026.

<Inset kind="why" lang="en">
**Why IP is connectionless.** IP is best-effort: no acknowledgements, no retransmissions, no ordering. A router that runs out of buffer space drops packets without telling anyone. The decision was deliberate (RFC 791, 1981): keep the network layer simple so it scales to billions of devices, and push reliability to TCP. This design choice explains why every reliability mechanism lives in TCP or QUIC, not in IP itself.
</Inset>

<Quiz
  id="jr-quiz-1"
  pieceSlug="01-the-ip-envelope"
  lang="en"
  question="What does an IP packet carry in its header?"
  choices={[
    { label: "Only the destination address", misconception: "Headers carry both source and destination plus other fields." },
    { label: "Source address, destination address, TTL, protocol type, checksum, length", correct: true },
    { label: "The full text of your HTTP request", misconception: "That sits in the payload, not the header." },
    { label: "Your browser's user-agent", misconception: "Application-level data lives much further up the stack." },
  ]}
/>

<Quiz
  id="jr-quiz-2"
  pieceSlug="01-the-ip-envelope"
  lang="en"
  question="What is the practical difference between IPv4 and IPv6?"
  choices={[
    { label: "Different speeds", misconception: "Speed depends on the network, not the IP version." },
    { label: "IPv6 has a much larger address space (128-bit vs 32-bit) so it can address far more devices, plus a simpler header", correct: true },
    { label: "IPv6 is encrypted", misconception: "Neither version is encrypted by default; TLS provides that." },
    { label: "IPv6 replaces TCP", misconception: "IP and TCP are different layers; both versions of IP carry TCP." },
  ]}
/>

<DragOrder
  id="jr-drag-1"
  pieceSlug="01-the-ip-envelope"
  lang="en"
  prompt="Order what happens to a packet at one router hop:"
  items={[
    "Packet arrives on input interface",
    "Router decrements TTL by 1; if it hits 0 the packet is dropped",
    "Router looks up destination address in routing table",
    "Router selects the next hop (longest prefix match)",
    "Router re-encapsulates packet in a new link-layer frame",
    "Router sends frame out the matching output interface",
  ]}
/>

<MetaphorComplete
  id="jr-metaphor-1"
  pieceSlug="01-the-ip-envelope"
  lang="en"
  setup="Fill in the blank: an IP packet is like an _______ — it has a from-address, a to-address, and contents inside."
  accepted={["envelope", "letter", "parcel", "package"]}
  canonical="envelope (or letter / parcel)"
  explanation="The postal analogy fits IP perfectly: addressed wrapper around content, routed hop by hop by intermediaries who only know the next step."
/>

</Explanation>

<FlowDiagram
  label="An IP packet travels left to right from the source host through a chain of routers to the destination server; each router reads only the destination address and forwards to the next hop."
  hue="var(--d-network)"
  perRow={5}
  nodes={[
    { id: "host", label: "Host", sub: "src→dst" },
    { id: "r1", label: "Router", sub: "next hop" },
    { id: "r2", label: "Router", sub: "next hop" },
    { id: "r3", label: "Router", sub: "next hop" },
    { id: "server", label: "Server", sub: "dst" },
  ]}
  edges={[
    { from: "host", to: "r1", label: "fwd" },
    { from: "r1", to: "r2", label: "fwd" },
    { from: "r2", to: "r3", label: "fwd" },
    { from: "r3", to: "server", label: "fwd" },
  ]}
  caption="No single router knows the full route. Each reads the destination address, consults its own routing table, and forwards to the best next hop — millions of independent greedy decisions deliver the packet after 10–15 hops."
/>

<KeyTakeaway>IP is the addressed envelope of the Internet: source IP, destination IP, and payload. No router knows the full path — each makes a local greedy decision, forwarding toward the destination one hop at a time.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-the-ip-envelope-retrieval"
  lang="en"
  questions={[
    { q: "In one sentence: why is it remarkable that the Internet works given no router knows the full route?", a: "Because every router makes a local greedy decision based on its partial view, and routing protocols (BGP, OSPF) keep those partial views consistent enough that the packet always advances — millions of independent routers cooperating without any central coordinator." },
    { q: "Why did the designers of IP choose best-effort delivery instead of reliable delivery?", a: "To keep the network layer simple so it scales to billions of devices. Reliability is pushed to TCP at the endpoints, where it is cheaper and easier than in every router on every path." },
    { q: "What happens to a packet whose TTL reaches zero?", a: "The router discards the packet and sends an ICMP 'time exceeded' message back to the source. This prevents loops from keeping packets alive forever in misrouted networks." },
  ]}
/>

<Recap lang="en">
IP is the universal addressed envelope: every chunk of Internet traffic — TCP segment, UDP datagram, ICMP message — travels inside an IP packet carrying source and destination address. No router knows the full path; each makes a local longest-prefix-match decision and hands the packet to the next hop. After 10–15 hops the packet arrives. IPv6 extends this model with 128-bit addresses (ending the address-space crisis) and a simpler fixed header. IP's deliberate choice of best-effort, connectionless delivery is why TCP, TLS, and QUIC all live above it.
</Recap>
