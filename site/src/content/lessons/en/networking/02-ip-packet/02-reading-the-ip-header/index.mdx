---
concepts:
- ipv4-header
- ttl
- dscp
- ecn
- protocol-field
- checksum
- ipv6-header
- next-header
deepensInto:
- 03-routing-and-forwarding
estMin: 12
lang: en
lessonType: topic
level: middle
order: 2
prereqs:
- 01-the-ip-envelope
slug: 02-reading-the-ip-header
sources:
- https://datatracker.ietf.org/doc/html/rfc791
- https://datatracker.ietf.org/doc/html/rfc8200
- https://datatracker.ietf.org/doc/html/rfc9330
spiral:
- encapsulation
status: ready
summary: 'Every field in the IPv4 and IPv6 headers has a job: TTL prevents loops,
  DSCP marks priority, the protocol field hands the payload to the right L4 handler.'
title: Reading the IP header
track: networking
unit: 02-ip-packet
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import headerTradeoff from "./infographics/header-tradeoff.svg?raw";

<Hook>
Every time a router forwards a packet, it reads and modifies the IP header. It decrements the TTL by one, recomputes the checksum, and makes a forwarding decision based on the destination address. Knowing which field does what lets you diagnose network problems and understand why IPv6 dropped some fields entirely.
</Hook>

<Crux>IP header fields: TTL caps hops, DSCP/ECN mark priority and congestion, the protocol field routes payload to the right L4 handler.</Crux>

<Explanation>

## The IPv4 header (20 bytes minimum)

IPv4 packets begin with a 20-byte fixed header (more if options are present):

| Field | Size | Purpose |
|-------|------|---------|
| Version | 4 bits | Always 4 for IPv4. |
| IHL (header length) | 4 bits | Number of 32-bit words in the header; minimum 5 (= 20 bytes). |
| ToS / DSCP + ECN | 8 bits | Originally a precedence field; redefined as Differentiated Services Code Point (6 bits) for QoS marking + ECN (2 bits). |
| Total length | 16 bits | Header + payload in bytes; maximum 65,535. |
| Identification | 16 bits | Tags fragments from the same original packet. |
| Flags + Fragment offset | 16 bits | "Don't fragment" flag, "more fragments" flag, and offset within original packet. |
| TTL | 8 bits | Hop limit; each router decrements by 1; drop if 0. |
| Protocol | 8 bits | Next-layer protocol: 6 = TCP, 17 = UDP, 1 = ICMP, 58 = ICMPv6. |
| Header checksum | 16 bits | CRC over the header only (not payload); recomputed at every hop because TTL changes. |
| Source IP | 32 bits | Sender's address. |
| Destination IP | 32 bits | Recipient's address. |

**Why the header checksum is only over the header.** The payload is protected by the L4 checksum (TCP/UDP). Computing a checksum over the full packet at every router would be prohibitively expensive at line rate; only the header checksum is needed because TTL changes.

## TTL — time to live

TTL is an 8-bit hop counter, not a time measurement (despite the name). Every router decrements it by 1. When it reaches 0 the router discards the packet and sends an ICMP "time exceeded" back to the source. This prevents routing loops from keeping packets circulating forever.

`traceroute` exploits TTL: it sends probes with TTL = 1, 2, 3, … Each router that drops one (because TTL hit zero) replies with "time exceeded," revealing its address and round-trip time — the path traces itself hop by hop.

## DSCP and ECN — the quality knobs

Why should you care? Because misconfigured DSCP is why your voice call degrades when a bulk upload starts on the same link — and because ECN is why modern TCP can back off without dropping packets at all.

The top 8 bits of the third byte carry two orthogonal quality signals:

- **DSCP (6 bits).** Differentiated Services Code Point. Routers use it to prioritise traffic in their queues. Common values: EF (Expedited Forwarding — voice/real-time), AF (Assured Forwarding — video), CS (Class Selector — legacy compatibility). End-to-end DSCP rarely survives the public Internet because each AS re-marks at ingress; it reliably influences queueing only within a single network.
- **ECN (2 bits).** Explicit Congestion Notification. A congested router marks a packet with CE (Congestion Experienced) instead of dropping it. The receiving host echoes the signal back to the sender via TCP, which reduces its congestion window — lower latency without the loss. ECN is enabled by default on modern Linux. L4S (RFCs 9330–9332) extends ECN to achieve ultra-low latency via dual-queue traffic shaping.

<NumbersCard
  id="card-header-fields"
  title="IPv4 header fields at a glance"
  data-lesson-visual
  stats={[
    { label: "Minimum header size", value: "20 bytes" },
    { label: "TTL initial value (Linux default)", value: "64" },
    { label: "Protocol: TCP", value: "6" },
    { label: "Protocol: UDP", value: "17" },
    { label: "Protocol: ICMP", value: "1" },
    { label: "Max packet size (total length field)", value: "65,535 bytes" },
  ]}
/>

## The IPv6 header — simplified on purpose

IPv6 has a fixed 40-byte header with no checksum and no fragmentation fields:

- **Version (4 bits):** Always 6.
- **Traffic Class (8 bits):** Same DSCP + ECN semantics as IPv4.
- **Flow Label (20 bits):** Identifies a flow (e.g. a TCP connection) for consistent ECMP treatment without re-hashing every packet.
- **Payload Length (16 bits):** Size of the payload only (not the header itself).
- **Next Header (8 bits):** Same role as IPv4's Protocol field — identifies the extension header or L4 protocol that follows.
- **Hop Limit (8 bits):** Same as IPv4's TTL.
- **Source / Destination (128 bits each):** The 16-byte addresses.

**Why no checksum.** IPv6 relies on L2 (Ethernet FCS) and L4 (TCP/UDP checksum) for integrity. Eliminating per-hop checksum recomputation reduces per-router work and matches modern hardware capabilities.

**Extension headers** chain after the fixed header via the Next Header field — analogous to IPv4 options but cleaner. Common extensions: Hop-by-Hop Options, Routing (used by Segment Routing v6), Fragment, Destination Options.

<Infographic
  svg={headerTradeoff}
  label="Side-by-side comparison of the IPv4 and IPv6 headers. IPv4: 20-byte minimum header, a header checksum recomputed at every hop, routers may fragment in transit, options inline in the header, TTL caps hops and Protocol picks the L4 handler. IPv6: 40-byte fixed header, no header checksum at all, routers never fragment, options chained via Next Header extension headers, with Hop Limit and Next Header replaying the TTL and Protocol roles plus a Flow Label."
  caption="IPv6 buys a smaller per-hop budget — no checksum recompute, no router fragmentation — by paying for a larger fixed header and pushing options out into chained extension headers."
  hue="var(--d-infra)"
/>

<Inset kind="why" lang="en">
**Why IPv6 routers never fragment.** IPv4 allowed routers to split an oversized packet into smaller fragments — useful but it added CPU work at every hop and created security holes (some firewalls inspected only the first fragment, letting attackers hide malicious payloads in subsequent fragments). IPv6 forbids router fragmentation. If a packet is too large, the router sends an ICMP "packet too big" back to the sender, which must retransmit at a smaller size. Complexity moves to the sender, router stays simple.
</Inset>

<Quiz
  id="mid-quiz-1"
  pieceSlug="02-reading-the-ip-header"
  lang="en"
  question="What does the TTL field protect against?"
  choices={[
    { label: "Packet corruption", misconception: "That's what the checksum is for." },
    { label: "Packets looping forever in misrouted networks — TTL caps the maximum number of hops", correct: true },
    { label: "Eavesdropping", misconception: "TTL has nothing to do with confidentiality." },
    { label: "Reassembly errors", misconception: "Reassembly uses the ID + fragment offset fields, not TTL." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="02-reading-the-ip-header"
  lang="en"
  question="Why does IPv6 drop the IP-level header checksum?"
  choices={[
    { label: "IPv6 is always encrypted", misconception: "Neither IPv4 nor IPv6 is encrypted by default." },
    { label: "IPv6 relies on L2 (Ethernet FCS) and L4 (TCP/UDP) for integrity; eliminating per-hop recomputation reduces router work", correct: true },
    { label: "IPv6 checksums are computed in hardware", misconception: "The design choice is architectural, not about hardware implementation." },
    { label: "IPv6 packets never change in transit", misconception: "Hop Limit (TTL equivalent) still changes; the point is the checksum was over the header only anyway." },
  ]}
/>

<DragOrder
  id="mid-drag-1"
  pieceSlug="02-reading-the-ip-header"
  lang="en"
  prompt="Order the IPv4 header fields (after Version + IHL):"
  items={[
    "ToS / DSCP + ECN (QoS and congestion marking)",
    "Total length (header + payload)",
    "Identification + Flags + Fragment offset",
    "TTL (hop limit)",
    "Protocol (TCP=6, UDP=17, ICMP=1)",
    "Header checksum",
    "Source IP, Destination IP",
  ]}
/>

</Explanation>

<StackDiagram
  label="The 20-byte IPv4 header laid out field by field in wire order, top to bottom: Version and IHL, ToS with DSCP and ECN, Total length, Identification with flags and fragment offset, TTL, Protocol, Header checksum, Source IP, Destination IP."
  hue="var(--d-network)"
  layers={[
    { label: "Version + IHL", note: "4 = IPv4, header length" },
    { label: "ToS / DSCP + ECN", note: "priority + congestion" },
    { label: "Total length", note: "header + payload bytes" },
    { label: "Identification + Flags + Frag offset", note: "fragment reassembly" },
    { label: "TTL", note: "hop limit, −1 per router" },
    { label: "Protocol", note: "TCP=6 UDP=17 ICMP=1" },
    { label: "Header checksum", note: "over header only" },
    { label: "Source IP", note: "32 bits" },
    { label: "Destination IP", note: "32 bits" },
  ]}
  caption="Every IPv4 packet carries these fields in this order. TTL is decremented at each hop, so the header changes — which is why the checksum (over the header only) must be recomputed at every router, and exactly the per-hop work IPv6 dropped."
/>

<KeyTakeaway>The IPv4 header is 20 bytes of protocol machinery: TTL caps hops, DSCP/ECN signal priority and congestion, the Protocol field hands the payload to the right L4 handler. IPv6 simplifies to a fixed 40-byte header with no checksum and no router fragmentation.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-reading-the-ip-header-retrieval"
  lang="en"
  questions={[
    { q: "Why does a router recompute the IPv4 header checksum on every hop?", a: "The TTL field is decremented by 1 at every hop, so the header changes. The checksum covers the header, so it must be recalculated. This is also why IPv6 dropped the IP-level checksum — eliminating this per-hop work at the cost of relying on L2/L4 integrity checks." },
    { q: "What does the DSCP field do, and why does it rarely work end-to-end on the public Internet?", a: "DSCP (6 bits in the ToS byte) marks packets with a traffic class so routers can prioritise them in their queues. It rarely survives the public Internet because each Autonomous System re-marks (overwrites) DSCP at ingress — operators don't trust markings from external networks. DSCP only reliably controls queueing within a single network (enterprise, datacentre)." },
    { q: "How does traceroute reveal the path using TTL?", a: "traceroute sends probes with TTL=1, 2, 3, … Each router that receives a probe with TTL=1 decrements it to 0, drops the packet, and sends ICMP 'time exceeded' back. That ICMP response includes the router's IP address. By incrementing TTL one at a time, traceroute collects the address of each hop from source to destination." },
  ]}
/>

<Recap lang="en">
The IPv4 header packs 20 bytes of routing machinery into every packet. TTL (hop limit) prevents routing loops by decrementing at each router and triggering a discard at zero. DSCP marks QoS class; ECN signals congestion without dropping. The Protocol field routes the payload to TCP, UDP, or ICMP. The header checksum covers only the header and must be recomputed at every hop because TTL changes — which is exactly why IPv6 eliminated it, relying on L2 and L4 for integrity. IPv6 also fixed the header to 40 bytes, banned router fragmentation, and added a 20-bit Flow Label for ECMP consistency. Now when you open a packet capture and see TTL=1, you know the next hop will drop it; when you see DSCP=EF on a media stream going through your network, you know whether your QoS policy is actually being applied.
</Recap>
