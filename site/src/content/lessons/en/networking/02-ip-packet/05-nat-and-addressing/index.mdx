---
concepts:
- nat
- cgnat
- rfc1918
- ipv6-slaac
- anycast
- dual-stack
- happy-eyeballs
- stun-turn-ice
deepensInto:
- 06-ip-security-and-operations
estMin: 14
lang: en
lessonType: topic
level: middle
order: 5
prereqs:
- 04-mtu-and-fragmentation
slug: 05-nat-and-addressing
sources:
- https://datatracker.ietf.org/doc/html/rfc1918
- https://datatracker.ietf.org/doc/html/rfc8200
- https://datatracker.ietf.org/doc/html/rfc8305
- https://datatracker.ietf.org/doc/html/rfc4862
spiral: []
status: ready
summary: NAT extended the IPv4 Internet past 4 billion addresses but broke end-to-end
  connectivity. IPv6, SLAAC, anycast, and dual-stack are the principled way forward.
title: NAT, addressing, and IPv6 deployment
track: networking
unit: 02-ip-packet
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import natTraversalLadder from "./infographics/nat-traversal-ladder.svg?raw";

<Hook>
WebRTC video calls need both parties to connect directly — but a typical user is behind two NAT layers. The STUN server has to tell each side its public IP and port. The TURN server relays the call if direct connection fails. Understanding why NAT exists, how it works, and what it breaks is essential for any full-stack engineer building peer-to-peer or real-time features.
</Hook>

<Crux>NAT shares one public IP across many hosts, extending IPv4 — but breaks end-to-end connectivity. IPv6 is the fix.</Crux>

<Explanation>

## RFC 1918 private addressing

IPv4's 32-bit address space holds ~4.3 billion addresses. That ran out. RFC 1918 reserved three ranges for private use:

| Range | CIDR | Size |
|-------|------|------|
| 10.0.0.0/8 | Class A private | 16.7 million |
| 172.16.0.0/12 | Class B private | 1 million |
| 192.168.0.0/16 | Class C private | 65,536 |

Hosts on private addresses cannot be reached directly from the public Internet. They can only initiate outbound connections (after which the NAT table holds the mapping for replies).

## Network Address Translation (NAT)

**NAT** (RFC 2663) rewrites packets at the network boundary:

1. **Outgoing packet:** source IP (private, e.g. `192.168.1.10`) + source port (e.g. `45231`) → rewritten to router's public IP + a unique source port (e.g. `203.0.113.5:12345`). The router stores this mapping.
2. **Incoming reply:** destination IP + port (`203.0.113.5:12345`) → router looks up the NAT table → rewrites back to `192.168.1.10:45231` and forwards to the internal host.

NAT enabled the IPv4 Internet to grow past its address limit but it fundamentally broke **end-to-end addressing**: the public Internet sees one IP for many internal hosts, and internal hosts cannot accept unsolicited inbound connections.

**What NAT breaks:**
- Peer-to-peer protocols (BitTorrent, VoIP, WebRTC): either side needs to initiate — NAT only permits outbound-initiated connections.
- Port forwarding: must be configured manually for each service you want externally reachable.
- Abuse-tracking: a single public IP may hide hundreds of unrelated users.

Together these three breakages mean that NAT converts the Internet from a symmetric peer-to-peer network into one where only the side behind a public IP can truly listen — which is why any real-time feature you build will eventually need STUN or TURN.

## Carrier-Grade NAT (CGNAT)

Mobile carriers and ISPs deploy CGNAT to share one public IPv4 address across hundreds or thousands of customers:

1. Customer's home router NATs to `100.64.x.x` (RFC 6598 shared address space — "NAT64 carrier space").
2. The carrier's CGNAT NATs again to a public IP.

Double NAT compounds the P2P problem. CGNAT users cannot host any service on their IP, and peer-to-peer NAT traversal is harder. Rate-limiting by IP is meaningless (hundreds of unrelated users share one IP).

## Working around NAT: STUN/TURN/ICE

WebRTC and modern P2P protocols use a three-tier approach:

- **STUN** (Session Traversal Utilities for NAT): a STUN server tells each peer its public IP and port as seen from outside the NAT. Peers try to connect directly using this information.
- **TURN** (Traversal Using Relays around NAT): if direct connection fails (e.g. symmetric NAT, CGNAT), a TURN server relays all traffic. Latency penalty; only fallback.
- **ICE** (Interactive Connectivity Establishment): the framework that iterates through STUN candidates, direct addresses, and TURN until one path works.

<Infographic
  svg={natTraversalLadder}
  label="An ascending three-step ladder for NAT traversal: STUN discovers the public mapping and tries a direct connection, ICE iterates every candidate until one path works, and TURN relays all traffic as the last-resort fallback."
  caption="Each rung trades latency for a higher chance of connecting: STUN gives a cheap direct path, ICE chooses, and TURN always works but adds relay latency and server cost — which is why CGNAT users so often fall to TURN."
  hue="var(--d-infra)"
/>

<NumbersCard
  id="card-nat"
  title="NAT and IPv6 by the numbers"
  data-lesson-visual
  stats={[
    { label: "IPv4 public address pool", value: "~3.7 billion usable" },
    { label: "CGNAT clients per public IP", value: "100–10,000" },
    { label: "IPv6 global traffic (2026)", value: "~45–50%" },
    { label: "Google IPv6 milestone", value: "50.1% on 2026-03-28" },
    { label: "IPv6 /48 per site", value: "281 trillion /64 subnets" },
    { label: "SLAAC interface ID (privacy ext.)", value: "64 bits, randomised per site" },
  ]}
/>

## IPv6 addresses: structure and SLAAC

IPv6 addresses are 128 bits, written as eight colon-separated hex groups. Shorthand: leading zeros omitted, longest run of zeros replaced by `::`. Example: `2001:db8::1`.

Typical allocation: `/48` prefix to a site → `/64` per subnet within the site. SLAAC (Stateless Address Autoconfiguration, RFC 4862) lets a host derive its own address:

1. Router broadcasts a `/64` prefix in Router Advertisement messages.
2. Host generates the low 64 bits (interface ID) — originally EUI-64 from MAC address, now random for privacy (RFC 8981 privacy extensions).
3. Host checks for duplicates (Duplicate Address Detection), then starts using the address.
4. No DHCP required for basic connectivity.

**Privacy extensions** (RFC 8981) randomise the interface ID per site to prevent cross-site tracking via IPv6 addresses. Modern OSes enable this by default.

## Anycast: one IP, many locations

**Anycast** announces the same IP prefix from multiple geographic locations. BGP routes each user to the AS-path-shortest POP. Applications: DNS resolvers (1.1.1.1, 8.8.8.8), CDN edges, root nameservers, DDoS-protected services.

Anycast works well for **stateless services** (UDP DNS) — each packet may go to a different POP. For **stateful services** (TCP, TLS), short flows are fine (BGP routing is stable for the duration), but long flows need either stable BGP or session-state replication across POPs (Cloudflare replicates TLS session tickets globally for this reason).

## Dual-stack and Happy Eyeballs

Most current deployments run **dual-stack**: both IPv4 and IPv6 enabled simultaneously. IPv6-first for content delivery, IPv4 as fallback.

**Happy Eyeballs v2** (RFC 8305): when a dual-stack host resolves a hostname, it races IPv4 and IPv6 connect attempts with a short head-start for IPv6. If IPv6 succeeds first, use it; if IPv4 wins or IPv6 stalls, use IPv4 — seamlessly, without user-visible delay. This makes IPv6 adoption safe even when IPv6 paths have intermittent problems.

<Inset kind="why" lang="en">
**Why NAT does not provide security.** NAT hides internal IP addresses from the public Internet, which some operators interpret as a security benefit. But NAT is not a firewall: it does not inspect traffic, does not enforce policy, and does not prevent an internal host that has been compromised from reaching out. A real stateful firewall is required regardless of whether NAT is present. Conflating NAT with security is a dangerous misconception in practice.
</Inset>

<Quiz
  id="mid-quiz-1"
  pieceSlug="05-nat-and-addressing"
  lang="en"
  question="Why does NAT break true peer-to-peer protocols?"
  choices={[
    { label: "NAT is too slow for P2P traffic", misconception: "NAT adds microseconds — negligible for P2P." },
    { label: "NAT only allows connections initiated from the inside; the public Internet has no way to reach a specific internal host unless the NAT mapping was established by an outgoing connection", correct: true },
    { label: "NAT encrypts traffic, which P2P protocols cannot handle", misconception: "NAT does not encrypt; it rewrites addresses." },
    { label: "P2P protocols are banned by RFC 1918", misconception: "RFC 1918 defines private address ranges, nothing about protocols." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="05-nat-and-addressing"
  lang="en"
  question="What does SLAAC allow an IPv6 host to do?"
  choices={[
    { label: "Obtain a temporary IPv4 address from the carrier", misconception: "SLAAC is IPv6-only and requires no DHCP." },
    { label: "Derive its own IPv6 address from the router-advertised /64 prefix plus a self-generated interface identifier, without needing DHCP", correct: true },
    { label: "Share an IPv6 address with other hosts on the subnet", misconception: "Each host generates a unique interface ID — addresses are not shared." },
    { label: "Replace STUN for NAT traversal", misconception: "SLAAC is about address assignment, not NAT traversal." },
  ]}
/>

<TradeoffMatrix
  id="sr-tradeoff-1"
  pieceSlug="05-nat-and-addressing"
  lang="en"
  prompt="A startup runs services across 3 cloud regions and wants resilient global addressing. Pick the addressing strategy."
  options={[
    { name: "Single IPv4 anycast prefix announced from all regions", summary: "One IP, BGP picks region.", correct: true, justification: "Same IP everywhere, BGP routes users to nearest region, automatic failover if one region withdraws its announcement. Industry-standard for global services. Cost: requires owning a public ASN + IP block + BGP peerings — non-trivial setup but mature operationally." },
    { name: "Region-specific IPv4 addresses, GeoDNS routes users", summary: "DNS-level routing.", justification: "Works but cache-affected: DNS TTL on user side delays failover. Bad on mobile where IP changes frequently. Cheaper than anycast (no BGP needed) but less responsive to outages." },
    { name: "IPv6-only with anycast", summary: "Modern + anycast.", justification: "Best in theory but ~50% of users are still IPv4-only — they cannot reach you. Dual-stack required, which means doing both." },
    { name: "Per-region IPv4 with manual DNS updates", summary: "Manual everything.", justification: "Maximum control but minimum reliability. Outages require human intervention. Don't ship this for anything that needs to be up at 3am." },
  ]}
/>

</Explanation>

<SequenceDiagram
  label="NAT rewrites addresses at the boundary: the internal host's outbound packet has its private source rewritten by the NAT router to a public IP and port, and the server's reply is rewritten back to the private host using the stored mapping."
  hue="var(--d-network)"
  actors={["Host (private)", "NAT router", "Server"]}
  messages={[
    { from: "Host (private)", to: "NAT router", label: "src 192.168.1.10:45231" },
    { from: "NAT router", to: "Server", label: "src 203.0.113.5:12345 (mapped)" },
    { from: "Server", to: "NAT router", label: "dst 203.0.113.5:12345" },
    { from: "NAT router", to: "Host (private)", label: "dst 192.168.1.10:45231 (restored)" },
  ]}
  caption="The NAT table stores the private↔public mapping when the outbound packet leaves, then uses it to restore the original private address on the reply. Because the mapping only exists after an outbound packet, the public Internet cannot initiate inbound connections — which is exactly what breaks peer-to-peer."
/>

<KeyTakeaway>NAT hides many hosts behind one public IP — IPv4 gets address exhaustion relief, but end-to-end reachability breaks. P2P needs STUN/TURN/ICE to punch through. IPv6 + SLAAC + Happy Eyeballs is the principled fix.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="05-nat-and-addressing-retrieval"
  lang="en"
  questions={[
    { q: "An engineer claims you cannot fix CGNAT issues from the application side. Where is the flaw in that argument?", a: "You cannot remove the address sharing — that is the carrier's deployment — but you can mitigate its effects. Use UDP hole-punching protocols (STUN/TURN/ICE for WebRTC) so P2P works through CGNAT. Don't rate-limit by IP for abuse detection: hundreds of unrelated users share one IP, so rate-limit by user ID or device ID instead. Provide IPv6 connectivity — CGNAT only affects IPv4; IPv6 has abundant address space, giving each user a unique routable address." },
    { q: "Why does Happy Eyeballs v2 give IPv6 a head start but still fall back to IPv4?", a: "IPv6 adoption is not yet universal — some paths have IPv6 configured but broken (misconfigured routers, filtering). If IPv6 always won the race, users on broken IPv6 paths would experience delays or failures. The head-start (250 ms in RFC 8305) favours IPv6 when it works, allowing the Internet to migrate toward IPv6 without harming users on imperfect paths." },
    { q: "What is anycast and what kinds of services use it?", a: "Anycast announces the same IP prefix from multiple geographic locations simultaneously. BGP routes each user to the AS-path-closest POP. Services: DNS resolvers (1.1.1.1, 8.8.8.8), CDN edge nodes, DDoS scrubbing centres, root nameservers. It works naturally for stateless UDP services (DNS). For stateful TCP services, short flows are fine (BGP is stable during a connection); long-lived sessions need care because BGP re-convergence can reroute mid-flow." },
  ]}
/>

<Recap lang="en">
RFC 1918 private ranges (10/8, 172.16/12, 192.168/16) let organisations reuse addresses internally; NAT at the boundary rewrites source IP+port so many internal hosts share one public IP. This extended IPv4's life but broke end-to-end connectivity: inbound connections fail without explicit port-forwarding, and P2P protocols require STUN/TURN/ICE workarounds. CGNAT at mobile carriers adds a second NAT layer. IPv6's 128-bit address space eliminates the need for NAT: every device gets a routable public address, SLAAC auto-configures without DHCP, and privacy extensions randomise the interface ID per site. Anycast uses BGP to route users to the geographically nearest copy of a service. Dual-stack + Happy Eyeballs v2 let applications race IPv4 and IPv6, migrating transparently as IPv6 deployment matures. Now when you debug a WebRTC call that fails only for certain users, check first whether they are behind CGNAT — that single fact predicts whether direct ICE candidates will ever work.
</Recap>
