---
concepts:
- longest-prefix-match
- ttl
- pmtud-black-hole
- nat-traversal
- bgp-hijack
- ip-spoofing
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://datatracker.ietf.org/doc/html/rfc791
- https://datatracker.ietf.org/doc/html/rfc8200
- https://datatracker.ietf.org/doc/html/rfc4271
- https://datatracker.ietf.org/doc/html/rfc2827
status: ready
summary: "Multiple-choice synthesis across the IP-packet unit — header semantics, longest-prefix routing, MTU black holes, NAT traversal, and BGP/spoofing defences."
title: 'IP packets: multiple-choice review'
track: networking
unit: 02-ip-packet
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one mirrors a call you make in a real incident — a packet that dies somewhere, a connection that stalls only on large payloads, a prefix that goes dark from one AS — not a field to recite, but a behaviour to reason about.
</Hook>

<Goal lang="en">
Confirm you can connect header semantics, longest-prefix forwarding, MTU and PMTUD, NAT traversal, and the IP-layer security model — the synthesis the individual lessons built toward.
</Goal>

<Quiz
  id="02-ip-packet-choice-1"
  lessonSlug="02-ip-packet"
  lang="en"
  question="A monitoring tool reports that an IPv4 router recomputes the header checksum on every packet it forwards, but the equivalent IPv6 router does not. Why the difference, and what does IPv6 rely on instead?"
  choices={[
    { label: "IPv4 must recompute because TTL is decremented at every hop, changing the header the checksum covers. IPv6 dropped the IP-level checksum entirely and relies on the L2 frame check (Ethernet FCS) and the L4 (TCP/UDP) checksum for integrity.", correct: true },
    { label: "IPv6 recomputes the checksum in hardware, so the tool just does not see it.", misconception: "The difference is architectural, not an implementation detail. IPv6 has no IP-level header checksum field at all — there is nothing to recompute." },
    { label: "IPv4 recomputes it because the payload changes in transit; IPv6 payloads are immutable.", misconception: "The IPv4 header checksum covers only the header, never the payload. It changes because TTL changes, not because of the payload." },
    { label: "IPv6 is encrypted, so a plaintext checksum is meaningless.", misconception: "Neither IPv4 nor IPv6 is encrypted by default. IPv6 simply delegates integrity to L2 and L4 to cut per-hop router work." },
  ]}
/>

<Quiz
  id="02-ip-packet-choice-2"
  lessonSlug="02-ip-packet"
  lang="en"
  question="A router's FIB holds both 10.0.0.0/8 via eth0 and 10.4.0.0/16 via eth1. A packet arrives for 10.4.2.9. Which path wins and what principle decides it?"
  choices={[
    { label: "eth1 via the /16. Longest-prefix match selects the most specific route — the one sharing the most leading bits with the destination — so the /16 overrides the broader /8 aggregate.", correct: true },
    { label: "eth0 via the /8, because it was installed first and covers a larger range.", misconception: "Route age and range size do not decide forwarding. The more specific prefix always wins under longest-prefix match, regardless of insertion order." },
    { label: "The router load-balances across both, since both match.", misconception: "ECMP only splits across paths of equal cost to the same prefix. Here the prefixes differ in length, so the more specific one is chosen outright — no balancing." },
    { label: "The packet is dropped as ambiguous because two entries match.", misconception: "Overlapping prefixes are normal under CIDR and never ambiguous — longest-prefix match deterministically resolves to the most specific entry." },
  ]}
/>

<Quiz
  id="02-ip-packet-choice-3"
  lessonSlug="02-ip-packet"
  lang="en"
  question="A service behind a WireGuard VPN works for small requests but file uploads hang forever with no error. tcpdump shows the client retransmitting the same large segment about once a second. Root cause and most robust fix?"
  choices={[
    { label: "A PMTUD black hole: the tunnel MTU is below 1500, oversized packets get dropped, and the ICMP 'packet too big' is filtered so the sender never shrinks. Most robust fix: clamp TCP MSS on the VPN gateway so endpoints never build oversized packets — it never depends on ICMP.", correct: true },
    { label: "Packet corruption on the tunnel; raise the retransmit timeout to ride it out.", misconception: "There is no corruption — the segments are dropped for being too large, and a longer timeout just stalls longer. The fix is to stop sending oversized packets, not to wait." },
    { label: "The VPN is too slow for large transfers; switch to a faster cipher.", misconception: "Throughput is not the issue — small requests succeed instantly. The packets exceed the path MTU and are silently dropped; cipher speed is irrelevant." },
    { label: "Just unblock ICMP and rely on PMTUD.", misconception: "Unblocking ICMP works only where you control every middlebox on the path. MSS clamping (or PLPMTUD) is more robust because it never depends on ICMP surviving end to end." },
  ]}
/>

<Quiz
  id="02-ip-packet-choice-4"
  lessonSlug="02-ip-packet"
  lang="en"
  question="A WebRTC feature fails for some users on a mobile carrier and falls back to a relay. The carrier uses CGNAT. Why does CGNAT make direct peer connection harder, and what is the principled long-term fix?"
  choices={[
    { label: "CGNAT adds a second NAT layer on top of the home router's NAT, so neither peer has a stable, reachable public IP:port for an unsolicited connection — STUN often fails and traffic falls back to a TURN relay. The principled fix is IPv6, giving each device a globally routable address and removing NAT entirely.", correct: true },
    { label: "CGNAT encrypts the packets so WebRTC's DTLS cannot negotiate.", misconception: "CGNAT rewrites addresses and ports; it does not encrypt. The failure is unreachability of an internal host, not an encryption clash." },
    { label: "The fix is to rate-limit abusive peers by their public IP.", misconception: "Under CGNAT hundreds of unrelated users share one public IP, so per-IP rate limiting is meaningless — and it does nothing to enable a direct connection." },
    { label: "CGNAT blocks UDP, so WebRTC must use TCP.", misconception: "CGNAT forwards UDP fine; STUN/TURN are UDP-based. The problem is double address translation defeating inbound reachability, not the transport protocol." },
  ]}
/>

<Quiz
  id="02-ip-packet-choice-5"
  lessonSlug="02-ip-packet"
  lang="en"
  question="Your site is reachable from your office but a customer on one ISP times out completely. A looking-glass query inside that ISP shows your prefix as RPKI 'invalid'. What most likely happened and what do you do first?"
  choices={[
    { label: "Your ROA most likely expired (or the announcing origin AS no longer matches it), so ROV-enabled routers on that ISP drop the route as invalid. First action: renew/fix the ROA at your RIR; validation re-engages within hours as caches refresh. Then monitor ROA expiry like any certificate.", correct: true },
    { label: "The customer's ISP is censoring you; escalate to a regulator.", misconception: "An RPKI 'invalid' state is a route-origin validation failure, almost always a ROA problem on your side — not deliberate censorship. Fix the ROA first." },
    { label: "Your server is down; restart it.", misconception: "The server is up — you reach it from the office. The failure is in inter-domain routing for that one AS, signalled by the RPKI 'invalid' state, not a host crash." },
    { label: "Switch DNS to a different IP to route around it.", misconception: "A new IP without a matching valid ROA would be filtered the same way, and changing addresses does not fix the origin-validation mismatch. Correct the ROA instead." },
  ]}
/>

<Quiz
  id="02-ip-packet-choice-6"
  lessonSlug="02-ip-packet"
  lang="en"
  question="An attacker sends small spoofed UDP queries to open resolvers with your server's IP as the source, and your link drowns under multi-gigabit responses you never asked for. Which single defence, applied at the attacker's network, stops the attack at its origin?"
  choices={[
    { label: "BCP 38 egress filtering at the attacker's ISP: it checks that packets leaving the network carry a source IP from address space that network is authorised to use, so the spoofed-source query never escapes. It is the single biggest unilateral fix — but global adoption sits near 60%.", correct: true },
    { label: "Enabling RPKI ROV on your own routers.", misconception: "RPKI validates BGP route origins to stop prefix hijacks — a different attack. It does nothing about spoofed-source UDP amplification." },
    { label: "Blocking all ICMP at your firewall.", misconception: "This is a reflection/amplification flood over UDP, not ICMP — and blocking ICMP would just create PMTUD black holes. It does not stop the spoofed traffic." },
    { label: "Lowering your server's TTL so the responses expire sooner.", misconception: "TTL caps hop count on packets you send; it has no effect on inbound flood volume. The fix is source-address validation at the spoofing network." },
  ]}
/>

<Recap lang="en">
The through-line across the unit is one packet's life: the header carries TTL, protocol, and addresses (IPv6 dropping the per-hop checksum); routers forward by longest-prefix match in a hardware FIB that BGP keeps globally consistent; MTU mismatches must fragment or shrink, and a blocked ICMP turns that into a silent PMTUD black hole; NAT and CGNAT trade address-exhaustion relief for broken end-to-end reachability that STUN/TURN/ICE and ultimately IPv6 work around; and because IP authenticates neither source nor route, BCP 38, uRPF, and RPKI — all needing operator cooperation — are the defences. Every failure mode resolves back to those same mechanics.
</Recap>
