---
concepts:
- mtu
- fragmentation
- pmtud
- pmtud-black-hole
- mss-clamping
- plpmtud
- df-bit
deepensInto:
- 05-nat-and-addressing
estMin: 12
lang: en
lessonType: topic
level: middle
order: 4
prereqs:
- 03-routing-and-forwarding
slug: 04-mtu-and-fragmentation
sources:
- https://datatracker.ietf.org/doc/html/rfc8200
- https://datatracker.ietf.org/doc/html/rfc8900
- https://datatracker.ietf.org/doc/html/rfc4821
spiral: []
status: ready
summary: A packet larger than a link''''s MTU must be shrunk or split. IPv4 routers
  can fragment; IPv6 forbids it. PMTUD black holes happen when ICMP is blocked.
title: MTU and fragmentation
track: networking
unit: 02-ip-packet
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
A developer deploys a service behind a VPN. Small requests work fine. Large requests — a file upload, a big JSON payload — silently hang. No error message. The server receives nothing. The problem is a mismatch between packet size and the maximum the link can carry, combined with a firewall blocking the error message that would have fixed it.
</Hook>

<Crux>When a packet exceeds an MTU, it must fragment (IPv4) or shrink (IPv6). If ICMP is blocked, the sender never learns — PMTUD black hole.</Crux>

<Explanation>

## What MTU is

**Maximum Transmission Unit (MTU)** is the largest IP packet (header + payload) that a link can carry without fragmentation. Ethernet's standard MTU is 1500 bytes. Different link technologies have different MTUs:

<NumbersCard
  id="card-mtu"
  title="Typical MTU sizes"
  data-lesson-visual
  stats={[
    { label: "Ethernet (standard)", value: "1500 bytes" },
    { label: "PPPoE (DSL)", value: "1492 bytes" },
    { label: "Wi-Fi (802.11)", value: "~1500 bytes" },
    { label: "Jumbo frames (10G/40G)", value: "9000 bytes" },
    { label: "IPv6 minimum required MTU", value: "1280 bytes" },
    { label: "VPN tunnels (WireGuard, IPSec)", value: "1400–1450 bytes" },
  ]}
/>

The **Path MTU** is the smallest MTU on the entire path from source to destination. A packet must fit within the Path MTU or it will be dropped somewhere on the path.

## IPv4 fragmentation

When an IPv4 packet arrives at a router whose egress link has a smaller MTU, the router splits the packet into **fragments**:

1. The router copies the identification field to all fragments.
2. It sets the "more fragments" flag on all but the last.
3. Each fragment carries an offset into the original packet.
4. The destination reassembles all fragments with the same ID + source IP.

Fragmentation is expensive: any one lost fragment forces retransmission of the whole original datagram. Each fragment is routed independently. Fragmentation at routers adds latency and CPU work.

**The DF (Don't Fragment) bit.** When set in the IPv4 header Flags field, the router may not fragment the packet. If it doesn't fit the egress MTU, the router drops it and sends ICMP "fragmentation needed" back to the source. This enables PMTUD.

## IPv6 — no router fragmentation

IPv6 routers **never fragment**. If a packet is too large, the router drops it and sends ICMPv6 "Packet Too Big" back to the sender. The sender must reduce the packet size and retransmit. This moves complexity to the sender and keeps router processing simple.

IPv6 senders are required to support a minimum MTU of 1280 bytes (they may always send 1280-byte packets to any destination). Path MTU Discovery is mandatory for IPv6.

## Path MTU Discovery (PMTUD)

PMTUD discovers the smallest MTU on the path. The sender sets DF=1 (IPv4) and sends full-sized packets. When a router drops one because it's too large, it sends ICMP "packet too big" (or "fragmentation needed") back. The sender adjusts its packet size down and retries.

**PMTUD black holes** occur when a firewall or middlebox silently drops ICMP. The sender never receives the "packet too big" signal. It keeps transmitting full-size packets, which keep getting dropped. The connection **stalls** — small requests work (they fit), large ones hang forever.

**Symptoms of a PMTUD black hole:**
- Small HTTP requests succeed; large payloads or files hang.
- `ping -M do -s 1450 destination` fails with no response.
- `traceroute` shows the path fine, but TCP sessions stall at large sizes.

<Misconception id="r-blackhole" title="Path MTU Discovery black holes">
A router drops your packet and sends ICMP "packet too big," but a middlebox silently discards the ICMP. You never learn to shrink packets, retransmit at 1500 bytes forever, hitting ~1 second timeouts. TCP MSS clamping and PLPMTUD mitigate this in practice.
</Misconception>

## Fixes for PMTUD black holes

**TCP MSS clamping:** the MSS (Maximum Segment Size) is negotiated in the TCP handshake. A router on the path (VPN gateway, PPPoE terminator) rewrites the MSS option in SYN packets to a safe value (e.g. 1452 = 1500 - 20 IP - 20 TCP - 8 PPPoE). Endpoints never negotiate a segment size that would create an oversized packet. This is the pragmatic fix for most VPN and PPPoE deployments.

**PLPMTUD (RFC 4821):** Packetisation-Layer PMTUD. The sender probes with increasingly large TCP segments; if a probe doesn't return an ACK, it backs off. Does not rely on ICMP at all — immune to ICMP filtering. Modern stacks default to PLPMTUD.

**Open ICMP policies:** allow ICMP type 3 (destination unreachable) and ICMPv6 type 2 (packet too big) through firewalls. Filtering all ICMP is a common but incorrect "security" policy that breaks PMTUD and traceroute.

<TraceScenario
  id="mid-trace-1"
  pieceSlug="04-mtu-and-fragmentation"
  lang="en"
  scenario="Diagnose: packets are silently dropped when their size exceeds 1400 bytes."
  steps={[
    { prompt: "What is the symptom from the user's perspective?", reveal: "Small requests work. Large requests (file upload, big JSON) hang or fail. Browser may show 'connection reset' or just time out." },
    { prompt: "What is happening at the network level?", reveal: "Path MTU is 1400 bytes (a VPN or PPPoE link overhead). Packets larger than that exceed the link MTU. Router sends ICMP 'packet too big' back. Sender should adjust MSS." },
    { prompt: "Why does it 'silently' fail in some cases?", reveal: "If a firewall blocks ICMP, the 'packet too big' message never reaches the sender. Sender keeps retransmitting full-size packets, which keep getting dropped. PMTUD black hole — connections stall." },
    { prompt: "Diagnostic command?", reveal: "ping -M do -s 1400 destination — sends a 1400-byte packet with DF (don't fragment) set. If it fails without an ICMP reply, the path MTU is smaller. Bisect to find the actual MTU." },
    { prompt: "Fix?", reveal: "Enable ICMP packet-too-big through firewalls. Or use TCP MSS clamping on the VPN/PPPoE gateway: rewrite TCP MSS option to a safe value so endpoints never send oversized packets in the first place." },
  ]}
/>

<Quiz
  id="mid-quiz-1"
  pieceSlug="04-mtu-and-fragmentation"
  lang="en"
  question="Why does IPv6 forbid router-level fragmentation?"
  choices={[
    { label: "Modern routers are too slow to fragment", misconception: "Modern routers are fast; the design choice is about simplicity and security." },
    { label: "Fragmentation hurts performance, complicates routers, and creates security holes; IPv6 pushes the burden to senders via Path MTU Discovery", correct: true },
    { label: "IPv6 packets are always small enough", misconception: "IPv6 supports jumbo packets too; the rule is a design decision, not a size guarantee." },
    { label: "Fragmentation is encrypted in IPv6", misconception: "IPv6 has no built-in encryption of headers." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="04-mtu-and-fragmentation"
  lang="en"
  question="What is a PMTUD black hole?"
  choices={[
    { label: "A router that drops all packets larger than 1280 bytes", misconception: "A PMTUD black hole specifically involves ICMP being blocked, not the size threshold." },
    { label: "When ICMP 'packet too big' messages are blocked by a firewall, the sender never learns to shrink packets and connections stall silently", correct: true },
    { label: "A routing loop that absorbs packets", misconception: "Routing loops are different — TTL eventually terminates them. A black hole here means silent drops." },
    { label: "IPv6 fragmentation failure", misconception: "IPv6 forbids router fragmentation; the black hole is specifically about ICMP filtering preventing PMTUD." },
  ]}
/>

</Explanation>

<SequenceDiagram
  label="A PMTUD black hole: the sender transmits a full-size packet, the router drops it and sends ICMP 'packet too big' back, but the firewall silently discards that ICMP, so the sender never shrinks and keeps retransmitting the same oversized packet."
  hue="var(--d-network)"
  actors={["Sender", "Firewall", "Router"]}
  messages={[
    { from: "Sender", to: "Router", label: "1500-byte packet, DF=1" },
    { from: "Router", to: "Firewall", label: "ICMP packet-too-big" },
    { from: "Firewall", to: "Sender", label: "ICMP dropped (blocked)" },
    { from: "Sender", to: "Router", label: "retransmit 1500-byte (still!)" },
  ]}
  caption="The router drops the oversized packet and signals the correct MTU via ICMP, but a firewall that filters ICMP swallows the signal. The sender never learns to shrink — small requests fit and work, large ones hang forever. Fixes that don't depend on ICMP: TCP MSS clamping and PLPMTUD."
/>

<KeyTakeaway>MTU is the link-layer size limit. IPv4 routers can fragment; IPv6 routers cannot. Path MTU Discovery learns the path's bottleneck MTU via ICMP — but firewalls that block ICMP create silent PMTUD black holes. The pragmatic fix is TCP MSS clamping or PLPMTUD.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-mtu-and-fragmentation-retrieval"
  lang="en"
  questions={[
    { q: "You run tcpdump and see TCP retransmissions every ~1 second, no loss detected, the connection runs through a VPN tunnel. What is a likely PMTUD-related root cause?", a: "VPN tunnels add overhead (e.g. IPSec adds ~20+ bytes). The path MTU is 1500 bytes (Ethernet), but tunnel encapsulation pushes user packets over that. The router drops the oversized frame and sends ICMP 'packet too big', but if a firewall filters ICMP, the sender never learns. It retransmits at 1500 bytes forever, hitting 1-second RTO each time. Fix: clamp TCP MSS on the VPN interface, or lower the VPN interface MTU manually." },
    { q: "Why does IPv6 require the sender to perform PMTUD, while IPv4 routers can fragment on the fly?", a: "IPv4 allowed routers to fragment to accommodate smaller MTUs, but fragmentation adds CPU and complexity at every hop. IPv6 removed it: if a packet is too large, the router sends ICMPv6 'Packet Too Big' to the sender, which shrinks and retransmits. This trades sender complexity for router simplicity — routers forward millions of packets per second; putting the burden on the sender is cheaper. But it only works if ICMPv6 is not filtered." },
    { q: "What is TCP MSS clamping and why is it used instead of relying on PMTUD?", a: "MSS (Maximum Segment Size) is the maximum TCP payload a sender agrees to use, negotiated via the MSS option in SYN/SYN-ACK packets. MSS clamping is a technique where a router on the path rewrites the MSS option in TCP SYNs to a safe value that accounts for the link's MTU overhead (e.g. 1452 for PPPoE). Endpoints then never attempt to send segments that would create oversized IP packets. It avoids reliance on ICMP entirely, which makes it robust against ICMP-filtering firewalls that cause PMTUD black holes." },
  ]}
/>

<Recap lang="en">
MTU is the link's maximum frame size. Ethernet standard is 1500 bytes; VPN tunnels reduce it further by adding header overhead. IPv4 routers can fragment an oversized packet into smaller pieces, but any lost fragment forces retransmission of the entire datagram. IPv6 bans router fragmentation entirely: if a packet is too large, the router sends ICMPv6 "Packet Too Big" and the sender must shrink. Path MTU Discovery relies on these ICMP signals — when a firewall silently drops ICMP, the sender never learns to shrink, and large connections stall (PMTUD black hole). Pragmatic fixes: TCP MSS clamping (rewrites the MSS option in SYNs to a safe value, immune to ICMP filtering) and PLPMTUD (RFC 4821, probes via TCP segments).
</Recap>
