---
concepts:
- ipv4-header-decode
- traceroute-ttl
- longest-prefix-match
- mss-clamping
- mtr-icmp-ratelimit
estMin: 14
lang: en
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://datatracker.ietf.org/doc/html/rfc791
- https://datatracker.ietf.org/doc/html/rfc8200
- https://datatracker.ietf.org/doc/html/rfc4632
status: ready
summary: "Read real packet dumps, route tables, and traceroute output; decode the header, predict the forwarding decision, and pick the highest-leverage fix."
title: 'IP packets: dump and trace reading'
track: networking
unit: 02-ip-packet
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Packet dumps, route tables, and traceroute output are where IP-layer problems are actually diagnosed. Read each one, work out what the bytes and columns mean, then choose the call a senior engineer would make first.
</Hook>

<Goal lang="en">
Practise the loop you run in every routing or path-MTU incident: decode the header or table in front of you, predict the behaviour, and reach for the fix that does not depend on a signal you cannot guarantee.
</Goal>

## Snippet 1 — decode the IPv4 header

A `tcpdump -X` capture shows the first 20 bytes of an IPv4 packet:

```
45 00 00 3c 1c 46 40 00 28 06 a6 ec c0 a8 00 0a 5d b8 d8 22
```

```
byte 0:  45        -> version 4, IHL 5 (5 * 4 = 20-byte header)
byte 8:  28        -> TTL = 0x28 = 40
byte 9:  06        -> protocol = 6
bytes 12-15: c0 a8 00 0a -> source 192.168.0.10
bytes 16-19: 5d b8 d8 22 -> destination 93.184.216.34
```

<Quiz
  id="02-ip-packet-code-1"
  lessonSlug="02-ip-packet"
  lang="en"
  question="From this header, what is the L4 protocol, what is the TTL, and what does the TTL value 40 most likely tell you about the packet's journey so far?"
  choices={[
    { label: "Protocol 6 is TCP and TTL is 40. Common initial TTLs are 64 (Linux) and 128 (Windows); 40 is consistent with a Linux origin (64) that has already crossed about 24 hops — TTL is a hop counter decremented once per router.", correct: true },
    { label: "Protocol 6 is UDP and TTL is 40 milliseconds left to live.", misconception: "Protocol 6 is TCP (17 is UDP), and TTL is a hop count, not a time value despite the name — each router decrements it by one." },
    { label: "Protocol 6 is ICMP and TTL 40 means 40 hops remain guaranteed.", misconception: "ICMP is protocol 1; 6 is TCP. TTL caps the maximum remaining hops but guarantees nothing about the path length ahead." },
    { label: "The header is 60 bytes because IHL is 5.", misconception: "IHL counts 32-bit words: 5 words = 20 bytes, the minimum header. A 60-byte header would need IHL 15." },
  ]}
/>

## Snippet 2 — the route table

```
$ ip route show
default via 203.0.113.1 dev eth0
10.0.0.0/8 via 10.4.0.1 dev eth1
10.4.0.0/16 via 10.4.0.1 dev eth1
10.4.2.0/24 dev eth2 scope link
```

<Quiz
  id="02-ip-packet-code-2"
  lessonSlug="02-ip-packet"
  lang="en"
  question="A packet for 10.4.2.9 arrives. Which line forwards it, and which line would handle a packet for 8.8.8.8?"
  choices={[
    { label: "10.4.2.9 matches the /24 (the most specific of the three overlapping 10.x routes), so it goes out eth2; 8.8.8.8 matches none of the specifics and falls to the default route 0.0.0.0/0 via 203.0.113.1.", correct: true },
    { label: "10.4.2.9 takes the /8 because it is listed first; 8.8.8.8 is dropped because no line matches.", misconception: "Order in the table does not decide forwarding — longest-prefix match picks the /24. And 8.8.8.8 is not dropped: the default route matches everything as a last resort." },
    { label: "10.4.2.9 takes the /16 because it has the next-hop 10.4.0.1; 8.8.8.8 takes the /8.", misconception: "The /24 is more specific than the /16, so it wins for 10.4.2.9. And 8.8.8.8 is outside 10.0.0.0/8, so it uses the default route, not the /8." },
    { label: "Both match the default route since it is the safest.", misconception: "The default (0.0.0.0/0) is the least specific prefix; it is used only when no longer prefix matches. 10.4.2.9 has three more specific matches available." },
  ]}
/>

## Snippet 3 — the traceroute

```
$ traceroute -n example.com
 1  192.168.1.1      0.9 ms
 2  203.0.113.1      8.4 ms
 3  * * *
 4  198.51.100.7    24.1 ms
 5  93.184.216.34   71.2 ms
```

<Quiz
  id="02-ip-packet-code-3"
  lessonSlug="02-ip-packet"
  lang="en"
  question="Hop 3 shows three asterisks but hops 4 and 5 reply normally. What is happening at hop 3, and is the path broken?"
  choices={[
    { label: "Hop 3 either rate-limits or does not generate ICMP 'time exceeded', so its TTL-expiry probes go unanswered — but it still forwards data-plane packets, which is why hops 4 and 5 reply. The path is fine; the hop is just invisible to traceroute.", correct: true },
    { label: "Hop 3 is down and the packets are being dropped there.", misconception: "If hop 3 dropped data-plane traffic, hops 4 and 5 could not reply. Asterisks mean only that the ICMP TTL-exceeded reply is missing, not that forwarding failed." },
    { label: "The route has a loop at hop 3 that TTL is breaking.", misconception: "A loop would never let the trace progress to hops 4 and 5. The asterisks are a missing/rate-limited ICMP response, a common and benign configuration." },
    { label: "Hop 3 is encrypting the packet so traceroute cannot read it.", misconception: "Routers do not encrypt forwarded IP packets. The missing line is purely about that router not returning an ICMP time-exceeded message." },
  ]}
/>

## Snippet 4 — MSS clamping on the gateway

```
# VPN/PPPoE gateway, packets stalling on large transfers
# iptables rule under consideration:
iptables -t mangle -A FORWARD -p tcp --tcp-flags SYN,RST SYN \
  -j TCPMSS --clamp-mss-to-pmtu
```

<Quiz
  id="02-ip-packet-code-4"
  lessonSlug="02-ip-packet"
  lang="en"
  question="Large transfers through this gateway stall while small ones work. What does this rule do, and why is it preferred over just unblocking ICMP?"
  choices={[
    { label: "It rewrites the TCP MSS option in passing SYN packets down to a value that fits the path MTU, so endpoints never negotiate a segment size that produces an oversized packet. It is preferred because it does not depend on ICMP 'packet too big' surviving across every middlebox — the cause of PMTUD black holes.", correct: true },
    { label: "It blocks all SYN packets larger than the MTU, forcing retransmission.", misconception: "The rule edits the MSS option inside the SYN; it does not block SYNs. It prevents oversized packets by lowering the negotiated segment size, not by dropping connections." },
    { label: "It fragments the oversized TCP segments at the gateway.", misconception: "MSS clamping avoids fragmentation entirely by keeping segments small at negotiation time. It edits the handshake, it does not split packets." },
    { label: "It is identical to unblocking ICMP and adds no robustness.", misconception: "Unblocking ICMP only helps if every middlebox on the path passes ICMP. MSS clamping works regardless of ICMP filtering, which is exactly why it is the pragmatic fix." },
  ]}
/>

<Recap lang="en">
Every IP-layer incident is read in bytes and columns: the first nibble of an IPv4 header gives version and length, byte 8 is TTL (a hop counter, not a clock) and byte 9 is the L4 protocol; a route table is resolved by longest-prefix match with the default route as the last resort; traceroute asterisks usually mean a hop that withholds ICMP, not a broken path; and an MSS-clamp rule fixes path-MTU stalls without depending on ICMP. Decode what is in front of you, predict the behaviour, then fix the cause — not the symptom.
</Recap>
