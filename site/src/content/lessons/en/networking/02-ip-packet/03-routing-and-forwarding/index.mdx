---
concepts:
- longest-prefix-match
- fib
- routing-table
- cidr
- bgp
- as
- ecmp
- arp
- nd
deepensInto:
- 04-mtu-and-fragmentation
estMin: 14
lang: en
lessonType: topic
level: middle
order: 3
prereqs:
- 02-reading-the-ip-header
slug: 03-routing-and-forwarding
sources:
- https://datatracker.ietf.org/doc/html/rfc4271
- https://datatracker.ietf.org/doc/html/rfc4632
- https://datatracker.ietf.org/doc/html/rfc1122
spiral: []
status: ready
summary: 'How routers decide where a packet goes next: longest-prefix match in the
  FIB, CIDR aggregation, BGP between autonomous systems, and ECMP across equal-cost
  paths.'
title: Routing and forwarding
track: networking
unit: 02-ip-packet
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
When a packet arrives at a router, the router has microseconds to decide where to send it. It consults a table holding millions of destination prefixes, finds the best match, and transmits the packet — all at line rate. Understanding this mechanism explains why a single bad BGP announcement can reroute global traffic, and why ECMP can cause elephant flows.
</Hook>

<Crux>Routing builds the map; forwarding uses it. FIB answers "which interface?" at line rate in hardware; BGP keeps the map globally consistent.</Crux>

<Explanation>

## CIDR and longest-prefix match

IPv4 addresses were once divided into rigid classes (A: /8, B: /16, C: /24). Classless Inter-Domain Routing (CIDR, RFC 4632) replaced classes with variable-length prefixes: any prefix from /1 to /32 is valid.

A **CIDR prefix** like `10.0.0.0/24` means "the first 24 bits identify the network; the last 8 bits identify the host." That prefix covers 256 addresses.

| Prefix | Size | Usable hosts |
|--------|------|-------------|
| /24 | 256 addresses | 254 (subtract .0 network, .255 broadcast) |
| /16 | 65,536 addresses | 65,534 |
| /30 | 4 addresses | 2 (for point-to-point links) |
| /8 | 16,777,216 addresses | 16,777,214 |

When multiple prefixes match a destination, the router picks the **longest match** (the most specific prefix). A packet to `10.0.0.5` matches both `10.0.0.0/8` and `10.0.0.0/24`; the /24 wins because it is more specific.

CIDR enables **route aggregation**: a single announcement of `10.0.0.0/8` covers 16 million addresses. Without aggregation, the BGP routing table would exceed practical memory limits.

## Routing table vs forwarding table (FIB)

**Routing table:** built by routing protocols (BGP, OSPF, IS-IS). It stores all known prefixes, their attributes, and alternatives. May have millions of entries.

**Forwarding Information Base (FIB):** the data-plane table the router uses for every packet. It is a trimmed-down per-destination next-hop view compiled from the routing table. Implemented in TCAM hardware on modern routers for line-rate lookups.

The split enables a clean architecture:
- **Control plane** (software): receives routing-protocol updates, computes best paths, writes the routing table, syncs the FIB.
- **Data plane** (hardware ASIC): uses the FIB for every packet at terabit-per-second speed.

Routing table updates flow from BGP/OSPF → routing table → FIB in milliseconds. Bugs in the data plane (rare) manifest as silent packet loss rather than obvious crashes.

## ARP and Neighbor Discovery

IP routing gets a packet to the right subnet — but there is one more problem: how does the router actually build the frame to send across the local wire? That is what ARP solves, and getting it wrong produces silent drops that `traceroute` never shows.

Once a router knows the next-hop IP it must learn that next-hop's MAC address to build the link-layer frame. **ARP** (IPv4) broadcasts "who has 10.0.0.5?" on the local subnet; the owner replies with its MAC. The router caches this.

**Neighbor Discovery (ND)** is IPv6's equivalent — it uses ICMPv6 multicast instead of broadcast, and also handles router advertisements, address autoconfiguration, and duplicate address detection.

<NumbersCard
  id="card-routing"
  title="Routing scale"
  data-lesson-visual
  stats={[
    { label: "Global BGP table size (2026)", value: "~1,000,000 prefixes" },
    { label: "ECMP typical path count", value: "4–64 equal-cost paths" },
    { label: "ARP cache timeout (Linux)", value: "60 s" },
    { label: "BGP convergence time (tuned)", value: "1–5 s" },
    { label: "OSPF convergence (datacenter)", value: "~200 ms" },
    { label: "TCAM lookup (hardware FIB)", value: "nanoseconds" },
  ]}
/>

## BGP: the Internet's routing protocol

The Internet is divided into **Autonomous Systems (AS)** — networks operated by one organisation, each with a unique AS number. Within an AS, interior gateway protocols (OSPF, IS-IS) compute routes. Between ASes, **BGP** (RFC 4271) negotiates which prefixes each AS announces and prefers.

BGP is a **path-vector protocol**: each route carries the list of AS numbers it has traversed. A router prefers a shorter AS-path (fewer ASes to traverse), with many tie-breakers (local preference, MED, community attributes). BGP is deliberately policy-driven: an AS can prefer expensive direct peering over cheap transit, or avoid certain ASes entirely.

**Route leaks and hijacks.** A misconfigured or malicious AS can announce a prefix it does not own. Traffic destined for the legitimate owner reroutes to the attacker. RPKI (Resource Public Key Infrastructure) lets prefix owners sign their announcements so receivers can validate authenticity (more on this in the security lesson).

## ECMP: splitting traffic across equal-cost paths

When a router has multiple **equal-cost paths** to a destination, it uses **ECMP** (Equal-Cost Multi-Path) to distribute load. The router hashes the 5-tuple (source IP, source port, destination IP, destination port, protocol) and picks a path by hash modulo path-count. All packets of one TCP flow hash to the same path (preserving ordering within the flow); different flows distribute across paths.

Datacentre Clos (leaf-spine) fabrics rely on ECMP for bisection bandwidth. Pitfalls:
- **Elephant flows:** one large flow saturates one path while others sit idle.
- **Hash polarisation:** the same hash function applied at consecutive hops causes systematic imbalance.
- Modern designs use weighted ECMP, flowlet switching (splitting a flow during quiet moments), or CONGA for load-aware rebalancing.

<Inset kind="why" lang="en">
**Why BGP allows asymmetric routing.** Each AS independently selects its outbound path per destination. The path a packet takes outbound may differ from the path a response takes inbound — the two sides of a conversation may traverse completely different ASes. This is legal and common (called asymmetric routing). It matters for latency (outbound: 60 ms, return: 150 ms because of a detour), for traceroute interpretation (trace in each direction separately), and for stateful firewalls (which can see only one direction of traffic).
</Inset>

<TraceScenario
  id="mid-trace-1"
  pieceSlug="03-routing-and-forwarding"
  lang="en"
  scenario="Trace a packet from a home PC in London to a server in San Francisco."
  steps={[
    { prompt: "Packet leaves PC. First-hop router?", reveal: "Home Wi-Fi router on a private 192.168.x.x network. NAT replaces the source IP with the router's public IP + a unique source port." },
    { prompt: "Packet enters the ISP. What does the ISP edge router do?", reveal: "Looks up destination in its routing table via longest-prefix match. Likely follows a default route (0.0.0.0/0) to the ISP backbone, or a more specific BGP-learned prefix." },
    { prompt: "Packet traverses ~10–15 hops across multiple ASes. What does each router do?", reveal: "Decrement TTL, longest-prefix-match the destination in the FIB, forward to next hop. May re-encapsulate in different link-layer frames (MPLS label, Ethernet, etc.)." },
    { prompt: "A multi-AS segment uses MPLS. What changes?", reveal: "MPLS labels (not IP lookups) steer the packet through the provider backbone. Label forwarding is faster than longest-prefix IP lookup and enables traffic engineering. IP packet unchanged inside." },
    { prompt: "Packet arrives at the destination network. What happens?", reveal: "Edge router does final longest-prefix-match, forwards to internal subnet. ARP resolves the server's MAC if not cached. Server's NIC receives the frame and delivers to the OS." },
  ]}
/>

<Quiz
  id="mid-quiz-1"
  pieceSlug="03-routing-and-forwarding"
  lang="en"
  question="What does a /24 CIDR prefix mean?"
  choices={[
    { label: "24 IP addresses", misconception: "It's the prefix length in bits, not address count. /24 = 256 addresses." },
    { label: "The first 24 bits of the address identify the network; the remaining 8 bits identify the host (256 addresses total)", correct: true },
    { label: "24 packets per second", misconception: "Prefix length has nothing to do with rate." },
    { label: "24 ms latency to the next hop", misconception: "Not a latency value." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="03-routing-and-forwarding"
  lang="en"
  question="Why does a router maintain both a routing table and a forwarding table (FIB)?"
  choices={[
    { label: "They are identical; it's just two names for the same thing", misconception: "They serve different planes: routing table for control plane policy, FIB for data plane speed." },
    { label: "The routing table is built by routing protocols and holds all alternatives; the FIB is a hardware-optimised subset for line-rate lookups — the two planes evolve at different timescales", correct: true },
    { label: "The FIB holds IPv4 routes, routing table holds IPv6 routes", misconception: "Both tables serve both address families." },
    { label: "The routing table is only used at startup", misconception: "BGP and OSPF continuously update the routing table." },
  ]}
/>

<DragOrder
  id="mid-drag-1"
  pieceSlug="03-routing-and-forwarding"
  lang="en"
  prompt="Order route resolution at a router when a packet arrives:"
  items={[
    "Decrement TTL; drop and send ICMP if TTL is 0",
    "Look up destination IP in FIB (longest-prefix match)",
    "Identify the next-hop IP and outbound interface",
    "ARP/ND resolve next-hop MAC if not cached",
    "Build new L2 frame with destination MAC",
    "Transmit frame on output interface",
  ]}
/>

</Explanation>

<FlowDiagram
  label="Steps a router performs when a packet arrives, in order: decrement TTL, look up the destination in the FIB by longest-prefix match, pick the next-hop IP and interface, resolve the next-hop MAC via ARP or ND, build a new L2 frame, then transmit."
  hue="var(--d-network)"
  perRow={3}
  nodes={[
    { id: "ttl", label: "Decrement TTL", sub: "drop if 0" },
    { id: "fib", label: "FIB lookup", sub: "longest prefix" },
    { id: "nexthop", label: "Next hop", sub: "IP + iface" },
    { id: "arp", label: "ARP / ND", sub: "resolve MAC" },
    { id: "frame", label: "Build L2 frame", sub: "dst MAC" },
    { id: "tx", label: "Transmit", sub: "out iface" },
  ]}
  edges={[
    { from: "ttl", to: "fib" },
    { from: "fib", to: "nexthop" },
    { from: "nexthop", to: "arp" },
    { from: "arp", to: "frame" },
    { from: "frame", to: "tx" },
  ]}
  caption="Forwarding is the data-plane lookup: the FIB answers 'which interface?' by longest-prefix match in hardware at line rate, then the router re-encapsulates the unchanged IP packet in a fresh link-layer frame for the next hop."
/>

<KeyTakeaway>Routing is the map, forwarding is the lookup. CIDR enables longest-prefix-match aggregation; the FIB delivers line-rate decisions in hardware; BGP keeps millions of routers globally consistent but allows asymmetry and is vulnerable to misconfigured announcements.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-routing-and-forwarding-retrieval"
  lang="en"
  questions={[
    { q: "What is longest-prefix match, and why is it needed?", a: "When multiple routing table entries match a destination IP, longest-prefix match selects the most specific one (most bits in common). It is needed because CIDR allows overlapping prefixes at different granularities — a /24 default in one region may be more specifically covered by a /28 in another. Without longest-prefix match, the more specific route could not override the aggregate." },
    { q: "Explain why a single misbehaving BGP announcement can take a domain offline globally.", a: "BGP is the inter-AS routing protocol. If a malicious or misconfigured AS announces a prefix it doesn't own (a route hijack or leak), neighbouring ASes may accept the announcement and propagate it via their BGP sessions. Traffic destined for the legitimate owner now routes to the attacker instead. The 2008 Pakistan Telecom/YouTube hijack and multiple 2024 incidents are well-known examples. Mitigations: RPKI ROAs sign prefix-origin mappings so receivers can validate; route filters limit which prefixes a peer can announce; AS-path anomaly alerts catch incidents quickly." },
    { q: "Why can ECMP cause elephant-flow problems and what do modern designs do about it?", a: "ECMP hashes the 5-tuple per flow, so all packets of a single TCP connection follow the same path. A single large (elephant) flow can saturate one path while others sit idle. Hash polarisation at consecutive hops can worsen imbalance. Modern solutions: weighted ECMP for capacity-aware load balancing, flowlet switching (split a flow during inter-packet gaps), and CONGA / power-of-two-choices for load-aware rebalancing. Programmable switches (P4, Tofino) allow custom ECMP logic." },
  ]}
/>

<Recap lang="en">
Routing builds a global map of reachable prefixes; forwarding uses it at line rate. CIDR variable-length prefixes enable aggregation — a /8 announcement covers 16 million addresses and keeps the BGP table manageable. Routers split control plane (routing table, BGP/OSPF updates) from data plane (FIB in TCAM hardware, nanosecond lookups). BGP propagates prefix reachability between Autonomous Systems via path-vector with AS-path; it is policy-driven and allows asymmetric routing by design. ECMP distributes flows across equal-cost paths by 5-tuple hash — great for aggregate throughput, problematic for elephant flows. Now when you see a BGP outage notification or a saturated uplink in a datacenter, you will know whether to look at FIB inconsistencies, AS-path anomalies, or ECMP hash collisions — and where the actual fix lives.
</Recap>
