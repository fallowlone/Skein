---
concepts:
- phase-chain
- round-trip-elimination
- preload-scanner
- distributed-tracing
- cascade-defense
- quic-migration
estMin: 14
lang: en
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://hpbn.co/
- https://opentelemetry.io/
- https://datatracker.ietf.org/doc/html/rfc9000
status: ready
summary: "Free-recall prompts spanning the whole networking track — the phase chain, round-trip elimination, the render pipeline, tracing, cascades, and QUIC migration. Answer from memory, then compare."
title: 'Putting it together: free-recall review'
track: networking
unit: 12-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. Each prompt spans several lessons at once — say or write a complete answer from memory before you open the model answer. The effort of reconstructing the connection across chapters is what makes the whole system stick.
</Hook>

<Goal lang="en">
Reconstruct the track as one system: the fixed phase chain, why eliminating round-trips beats shortening them, how the browser turns the last bytes into pixels, how a trace localises a slow hop, and how the resilience stack stops one failure from taking down all twelve layers.
</Goal>

<RetrievalDrawer
  client:load
  id="12-putting-it-together-short-retrieval"
  lang="en"
  questions={[
    { q: "Walk one HTTPS request from keystroke to first paint and name which actor owns each phase. Why must the first four phases run in sequence?", a: "DNS (resolver) turns the hostname into an IP; TCP (browser plus origin or CDN edge) opens the socket with a 3-way handshake; TLS (browser, origin, and the CA via the certificate) negotiates keys; HTTP carries the request to the origin, which processes and responds; the browser then fetches subresources, builds the DOM and CSSOM, lays out, paints, and fires LCP. DNS, TCP, TLS, HTTP are strictly sequential because each consumes the previous one's output — you cannot open a socket without an IP, cannot exchange TLS keys without a connection, cannot send an encrypted request without keys. That dependency chain is why round-trip count, not round-trip speed, is the primary latency lever." },
    { q: "Explain why eliminating round-trips beats shortening them, and list the levers that eliminate one or more.", a: "Shortening an RTT (moving the server closer) caps out at the speed of light through fibre; eliminating an RTT removes the wait entirely. The big levers: a CDN edge cache hit collapses DNS-distance, the origin round-trip, and processing into one nearby response; connection keep-alive and HTTP/2-or-3 pooling remove the TCP handshake on subsequent requests; TLS 1.3 session resumption (PSK) removes the certificate round-trip; QUIC 0-RTT piggybacks the request onto the handshake for idempotent methods; the OS DNS cache removes the resolver walk on repeat visits. The warm-vs-cold TTFB gap (175 ms to under 30 ms) is the whole argument in one number." },
    { q: "The network finishes fast but LCP is poor. Walk the critical render path and name the four highest-impact fixes.", a: "After HTML bytes arrive, the browser parses HTML into the DOM while the preload scanner races ahead to discover subresource URLs; CSS is fetched and parsed into the CSSOM, which is render-blocking (nothing paints until it is ready); DOM plus CSSOM form the render tree, then layout, paint, and compositing; classic JS without defer/async blocks parsing. The four fixes: CDN-cache the HTML to cut TTFB, preload the hero image so the LCP element is not discovered late, defer non-critical JS off the main thread, and add explicit width/height to images to prevent CLS. The preload scanner is defeated by URLs synthesised at runtime by JavaScript." },
    { q: "p95 latency tripled overnight. How does distributed tracing localise the slow hop, and which sampling strategy guarantees you have a trace?", a: "W3C Trace Context propagates one traceparent header (trace-id plus span-id) through every hop — browser, CDN edge worker, load balancer, application, database, external API. Each hop emits a timed span under the same trace-id, so the backend renders a waterfall and the single longest span is the bottleneck — no cross-clock grep. To guarantee a trace for the incident you combine head-based sampling (cheap steady-state baseline, e.g. 1-5%) with tail-based sampling at the OTel Collector that keeps 100% of errors and slow requests, because pure head-based almost never captures the rare slow request you actually need." },
    { q: "Describe how a single slow dependency becomes a site-wide outage, and the layered defenses that stop it.", a: "Every layer has independent retry logic, so one slow dependency triggers retries at DNS, TCP, TLS, the HTTP client, and the application all at once — a 99.9% origin sees 3x then 10x its normal traffic within 30 s and tips into 100% errors: amplification turns slow into catastrophic. Defenses: exponential backoff with full jitter staggers retries to kill the thundering herd; a circuit breaker fails fast while a dependency is down and probes for recovery half-open; bulkheads give each dependency its own thread pool so one cannot starve the rest; load shedding returns a fast 503 when the queue exceeds capacity; CDN stale-while-revalidate keeps serving cached content during origin overload; and error budgets make the velocity-vs-reliability trade-off a number rather than an argument." },
    { q: "Why does QUIC survive a WiFi-to-cellular handoff while TCP+HTTP/2 breaks, and what deployment constraint makes it actually work?", a: "TCP identifies a connection by the 4-tuple (src IP, src port, dst IP, dst port), so when the phone's IP changes the connection is dead and the client re-pays DNS, TCP, and TLS. QUIC identifies a connection by an opaque Destination Connection ID carried in every packet, independent of IP; on the new path the client sends a PATH_CHALLENGE and the server validates with PATH_RESPONSE on the same connection ID, continuing the existing streams, congestion state, and keys after about one extra RTT. The constraint: the load balancer must route packets by Destination Connection ID, not source IP — a source-IP-routing LB sends the post-migration packet to a backend with no state for that connection and migration fails." },
  ]}
/>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the track's spine: the request is a fixed phase chain owned by distinct actors, round-trip count is the latency lever and eliminating round-trips beats shortening them, the render pipeline decides LCP once the bytes land, distributed tracing localises a slow hop in seconds, the resilience stack stops one slow dependency from cascading, and QUIC's connection ID lets a connection outlive an IP change — provided the load balancer routes by it.
</Recap>
