---
concepts:
- request-lifecycle
- round-trip-elimination
- cdn-edge-cache
- layered-defense
- cascading-failure
- quic-migration
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://hpbn.co/
- https://datatracker.ietf.org/doc/html/rfc9000
- https://web.dev/articles/vitals/
status: ready
summary: "Cross-track multiple-choice synthesis for the networking capstone — one URL through DNS, TCP/QUIC, TLS, proxy gates, render, and the cascade defenses that keep it standing."
title: 'Putting it together: multiple-choice review'
track: networking
unit: 12-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that each cut across several chapters of the track at once. None is a single-lesson definition — each is a decision a senior engineer makes when a real page load misbehaves and the cause could live in any layer.
</Hook>

<Goal lang="en">
Confirm you can reason about the whole stack as one system: which layer owns a symptom, which optimisation removes round-trips versus shortens them, and how a single slow dependency turns into a site-wide outage.
</Goal>

<Quiz
  id="12-putting-it-together-choice-1"
  lessonSlug="12-putting-it-together"
  lang="en"
  question="A returning user loads a CDN-fronted page. You want the biggest single cut to time-to-first-byte. Which lever wins, and why?"
  choices={[
    { label: "A CDN edge cache hit. It collapses DNS, TCP, TLS, and origin processing into one response from a PoP ~20 ms away instead of an origin ~200 ms away — it removes round-trips and shortens the survivors at the same time.", correct: true },
    { label: "Upgrade TLS 1.2 to TLS 1.3 to save a handshake round-trip.", misconception: "TLS 1.3 saves one RTT on a new connection, but on a warm return visit TLS already resumes at 0-RTT. The dominant cost is the origin round-trip, which only an edge cache hit removes." },
    { label: "Move the origin to a faster CPU so processing time drops.", misconception: "Faster origin compute helps only the processing span. The chain is round-trip bound, not CPU bound — shortening one span you already pay does far less than removing whole round-trips via the cache." },
    { label: "Raise the DNS TTL so the lookup is cached longer.", misconception: "A longer TTL helps DNS on repeat visits, but DNS is already sub-millisecond from the OS cache on a return visit. It is not where the milliseconds are." },
  ]}
/>

<Quiz
  id="12-putting-it-together-choice-2"
  lessonSlug="12-putting-it-together"
  lang="en"
  question="On a warm return visit over HTTP/3 the request is sent inside the QUIC Initial packet, before the server replies. The handler creates an order row. What is the correct server behaviour and why?"
  choices={[
    { label: "Reject the 0-RTT early data for this method with 425 Too Early and require the full handshake. Captured 0-RTT data is replayable, so a non-idempotent POST could create the order twice.", correct: true },
    { label: "Accept it — 0-RTT is fully encrypted, so replay is not possible.", misconception: "0-RTT encrypts the payload, but an attacker who captures the encrypted packet can resend it verbatim. Encryption does not prevent replay; idempotency does." },
    { label: "Accept it but make the handler slower so the handshake finishes first.", misconception: "Timing tricks do not close the replay window and break under load. The defined mechanism is to refuse 0-RTT for non-idempotent methods with 425 Too Early." },
    { label: "Accept it because GET and POST are both safe under 0-RTT.", misconception: "Only idempotent methods (GET, HEAD, OPTIONS) are replay-safe at 0-RTT. A POST that mutates state is exactly the dangerous case." },
  ]}
/>

<Quiz
  id="12-putting-it-together-choice-3"
  lessonSlug="12-putting-it-together"
  lang="en"
  question="A botnet of 10,000 residential IPs each sends about 1 req/s. Your per-IP rate limit is 5 req/s and your WAF sees ordinary browser fingerprints. The origin is buckling. What actually stops this?"
  choices={[
    { label: "A global adaptive concurrency limit at origin: when in-flight requests exceed capacity, shed the excess with a fast 503 regardless of per-IP quota — while the CDN keeps serving cached responses to real users.", correct: true },
    { label: "Lower the per-IP limit to 0.5 req/s.", misconception: "Each bot is already under the limit at 1 req/s, and a stricter per-IP cap punishes legitimate browsers that burst many subresources at once. Per-IP limiting is the wrong axis for a distributed flood." },
    { label: "Tighten the WAF to paranoia level 4.", misconception: "PL4 blocks legitimate traffic that merely resembles attack patterns and still cannot distinguish a low-rate bot with a real browser fingerprint. Signature matching is not a volumetric defense." },
    { label: "Block the botnet's ASNs at the edge.", misconception: "Residential botnet IPs share ASNs with real users, so ASN blocks cause collateral damage. The durable fix is bounding concurrency at origin, not blocklisting networks." },
  ]}
/>

<Quiz
  id="12-putting-it-together-choice-4"
  lessonSlug="12-putting-it-together"
  lang="en"
  question="The network finishes in 150 ms but LCP fires at 3.2 s. The hero image is injected by JavaScript after framework boot. What is the root cause and the highest-leverage fix?"
  choices={[
    { label: "The preload scanner never sees the hero URL because JS synthesises it at runtime, so the fetch starts late. Fix: emit the image URL in the initial HTML (or a preload hint) so the scanner fetches it in parallel with CSS.", correct: true },
    { label: "TLS resumption failed, adding a round-trip.", misconception: "A failed resumption costs tens of milliseconds, not seconds, and the network already completed in 150 ms. The gap is the render pipeline, not the handshake." },
    { label: "CSS is render-blocking, so paint is delayed by design.", misconception: "Render-blocking CSS is intentional and accounts for a small, bounded delay. A 3-second LCP gap points at the hero resource being discovered late, not at the CSSOM." },
    { label: "The origin TTFB is too slow.", misconception: "TTFB is inside the 150 ms the network already spent. The 3-second gap is browser-side: the LCP element fetched late because the preload scanner could not discover it." },
  ]}
/>

<Quiz
  id="12-putting-it-together-choice-5"
  lessonSlug="12-putting-it-together"
  lang="en"
  question="A third-party API slows to 5 s. Within 30 s the whole site is down, though that API powers only checkout. Which combination of defenses would have contained the blast radius?"
  choices={[
    { label: "A bulkhead (a dedicated thread pool per dependency) plus a circuit breaker on the slow API: checkout fails fast while every other endpoint keeps its own threads and stays up.", correct: true },
    { label: "Exponential backoff alone on the client retries.", misconception: "Backoff slows each client's retry cadence but, with one shared pool across thousands of clients, the slow dependency still occupies every thread. Backoff without isolation does not stop one dependency starving the rest." },
    { label: "A bigger shared thread pool so more requests can wait at once.", misconception: "A larger shared pool only lets more threads pile up on the slow dependency before it saturates; it postpones, not prevents, the total stall. Isolation (bulkhead), not size, contains it." },
    { label: "Raise the API client timeout so requests do not fail.", misconception: "A longer timeout holds each thread, connection, and buffer for longer on the slow call, accelerating exhaustion. Fast failure plus isolation is the cascade defense, not patience." },
  ]}
/>

<Quiz
  id="12-putting-it-together-choice-6"
  lessonSlug="12-putting-it-together"
  lang="en"
  question="A commuter on a train reports the app reloads each time the train enters a tunnel and the phone hops WiFi to cellular. The app runs HTTP/3. Where is the deployment bug?"
  choices={[
    { label: "The QUIC load balancer routes by source IP instead of Destination Connection ID, so after the IP change the PATH_CHALLENGE lands on a backend with no state for that connection — migration fails and the client must reconnect.", correct: true },
    { label: "HTTP/3 cannot survive IP changes; this is expected behaviour.", misconception: "Surviving IP changes is exactly what QUIC connection migration provides via the Destination Connection ID. The reload means migration is being broken downstream, not that QUIC lacks the feature." },
    { label: "The TLS session ticket expired during the handoff.", misconception: "Ticket lifetime is unrelated to a mid-session network change. Migration reuses the existing keys and streams on the new path; the failure is connection-ID routing, not resumption." },
    { label: "TCP head-of-line blocking stalled the streams.", misconception: "HoL blocking is a TCP/HTTP/2 problem that QUIC's independent streams avoid, and it does not cause a full reload on a network change. The symptom is a broken migration path." },
  ]}
/>

<Recap lang="en">
The through-line of the whole track is one system: a fixed phase chain (DNS, TCP/QUIC, TLS, proxy gates, origin, render) where round-trip count is the latency lever, and where removing round-trips (edge cache, 0-RTT, pooling) beats shortening them. The same composition fails as a system — distributed floods slip past per-IP limits, a late-discovered hero defeats the preload scanner, one slow dependency cascades through uncoordinated retries, and a connection-ID-blind load balancer breaks QUIC migration. Senior debugging is knowing which layer owns the symptom and which defense bounds the blast radius.
</Recap>
