---
concepts:
- request-lifecycle
- seven-actors
- round-trip
- CDN-edge
- render-pipeline
deepensInto:
- 02-dns-tcp-tls-sequence
- 03-rendering-and-web-vitals
estMin: 10
lang: en
lessonType: topic
level: junior
order: 1
prereqs:
- 03-tcp-handshake/01-the-three-way-handshake
- 04-dns-resolution/01-dns-what-it-does
- 05-tls-handshake/01-what-tls-does
- 06-http-versions/01-http-fundamentals
slug: 01-the-twelve-layers
sources:
- https://hpbn.co/
- https://web.dev/articles/vitals/
- https://datatracker.ietf.org/doc/html/rfc9110
spiral:
- encapsulation
- latency
status: ready
summary: When you type a URL, seven actors — browser, resolver, router, origin, CA,
  CDN edge, and render engine — execute twelve phases in sequence. This lesson maps
  the whole journey.
title: 'The twelve layers: one URL, seven actors'
track: networking
unit: 12-putting-it-together
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
You type a URL and a page appears in under 400 ms. That gap is not magic — it is twelve distinct phases executed by seven actors who have never met each other and never will. Knowing which actor owns which phase is the foundation of every performance and debugging skill in this chapter.
</Hook>

<Crux>One URL triggers a twelve-phase protocol chain; knowing which actor owns each phase is the first step in diagnosing any slow page.</Crux>

<Explanation>

## The seven actors

Every HTTPS request passes through a cast of characters, each owning exactly one concern:

- **Bea (browser)** — initiates the request, renders the result.
- **Rex (recursive DNS resolver)** — translates the hostname to an IP address.
- **Rita (router network)** — carries packets hop-by-hop across the internet.
- **Cara (certificate authority)** — vouched for the server's identity before the request ever started; her signature lives inside the server's certificate.
- **Patty (CDN/proxy)** — intercepts the connection at a nearby edge point of presence (PoP), serves cached content, or forwards to origin.
- **Sven (origin server)** — computes the real response when no cache can answer.
- **Bea's render engine** — parses the HTML/CSS/JS and paints pixels.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="text-sm w-full border-collapse">
    <thead>
      <tr class="bg-slate-100">
        <th class="border px-3 py-2 text-left">Phase</th>
        <th class="border px-3 py-2 text-left">Actor</th>
        <th class="border px-3 py-2 text-left">What happens</th>
        <th class="border px-3 py-2 text-left">Typical cost</th>
      </tr>
    </thead>
    <tbody>
      <tr><td class="border px-3 py-1">1. DNS lookup</td><td class="border px-3 py-1">Rex</td><td class="border px-3 py-1">hostname → IP</td><td class="border px-3 py-1">&lt;1 ms warm / 30–100 ms cold</td></tr>
      <tr><td class="border px-3 py-1">2. TCP connect</td><td class="border px-3 py-1">Rita + Sven/Patty</td><td class="border px-3 py-1">3-way handshake</td><td class="border px-3 py-1">~10–100 ms (RTT)</td></tr>
      <tr><td class="border px-3 py-1">3. TLS handshake</td><td class="border px-3 py-1">Bea + Sven + Cara</td><td class="border px-3 py-1">key exchange, cert verify</td><td class="border px-3 py-1">1 RTT new / 0 RTT resumed</td></tr>
      <tr><td class="border px-3 py-1">4. Proxy / rate-limit check</td><td class="border px-3 py-1">Patty</td><td class="border px-3 py-1">health check, cache lookup, WAF</td><td class="border px-3 py-1">&lt;1–5 ms</td></tr>
      <tr><td class="border px-3 py-1">5. HTTP request</td><td class="border px-3 py-1">Bea → Sven</td><td class="border px-3 py-1">GET / with headers</td><td class="border px-3 py-1">~1 RTT</td></tr>
      <tr><td class="border px-3 py-1">6. Origin processing</td><td class="border px-3 py-1">Sven</td><td class="border px-3 py-1">auth, DB query, serialise</td><td class="border px-3 py-1">10–200 ms</td></tr>
      <tr><td class="border px-3 py-1">7. HTTP response</td><td class="border px-3 py-1">Sven → Bea</td><td class="border px-3 py-1">status + headers + body</td><td class="border px-3 py-1">~1 RTT + body size</td></tr>
      <tr><td class="border px-3 py-1">8. Subresource fetches</td><td class="border px-3 py-1">Bea (parallel)</td><td class="border px-3 py-1">CSS, JS, fonts, images</td><td class="border px-3 py-1">1–3 RTT (parallelised)</td></tr>
      <tr><td class="border px-3 py-1">9. DOM + CSSOM parse</td><td class="border px-3 py-1">Bea</td><td class="border px-3 py-1">build render tree</td><td class="border px-3 py-1">0–50 ms</td></tr>
      <tr><td class="border px-3 py-1">10. Layout + paint</td><td class="border px-3 py-1">Bea</td><td class="border px-3 py-1">position elements, rasterise</td><td class="border px-3 py-1">50–200 ms</td></tr>
      <tr><td class="border px-3 py-1">11. JavaScript execute</td><td class="border px-3 py-1">Bea</td><td class="border px-3 py-1">event handlers, hydration</td><td class="border px-3 py-1">0–500 ms</td></tr>
      <tr><td class="border px-3 py-1">12. LCP painted</td><td class="border px-3 py-1">Bea</td><td class="border px-3 py-1">largest visible element ready</td><td class="border px-3 py-1">total: 200–4000 ms</td></tr>
    </tbody>
  </table>
</div>

## Why the order is fixed

When you see a page take 800 ms and want to know where the time went, you need to know which actor owns which phase — otherwise you are optimizing the wrong thing. Each phase depends on the output of the previous one. DNS must resolve before Bea knows which IP to connect to. TCP must complete before TLS can exchange keys. TLS must complete before HTTP can send the encrypted request. This strict sequencing is why **round-trip count, not round-trip speed, is the primary latency lever** — you can shorten each RTT by moving the server closer, but you cannot skip the RTTs without redesigning the protocol.

The one exception: **CDN edge caching** short-circuits the chain. If Patty has the answer in cache, phases 4–7 collapse into a single CDN response. Bea still pays DNS + TCP + TLS (phases 1–3), but those happen against a server 20 ms away instead of a server 200 ms away. That is the chapter's central performance insight in one sentence.

## The delivery metaphor

Think of the request as a package delivery:

- You place an order (HTTP request).
- The address is looked up in a phone book (DNS).
- A van is dispatched on a road network (TCP + Rita's routing).
- The parcel is sealed in tamper-evident wrap (TLS).
- A local depot might have the item already stocked (CDN cache).
- The parcel arrives at your door (response).
- You unbox and use it (browser render).

Together, these seven steps explain why swapping the delivery truck for a faster one (bigger bandwidth) rarely fixes perceived slowness — you still pay for the phone book lookup, the road network, and the unsealing. Each role is independent; each can be measured and optimised separately.

<Inset kind="why" lang="en">
The strict layer dependency exists for a good reason: each layer can be optimised, replaced, or failed independently. DNS can be changed without touching TLS. TLS can be upgraded (1.2 → 1.3) without changing HTTP semantics. This is the **separation of concerns** principle applied to networking at planetary scale.
</Inset>

<Quiz
  id="jr-q1"
  lessonSlug="01-the-twelve-layers"
  lang="en"
  question="In what order do the first four phases run for a fresh HTTPS request?"
  choices={[
    { label: "HTTP, TLS, TCP, DNS", misconception: "Reverse — DNS happens first because the browser needs an IP before it can open any socket." },
    { label: "DNS, TCP, TLS, HTTP", correct: true },
    { label: "TCP, HTTP, TLS, DNS", misconception: "TCP cannot run without an IP address, which comes from DNS first." },
    { label: "All four in parallel", misconception: "They strictly depend on each other; parallelism only helps within a phase, not across phases." },
  ]}
/>

<DragOrder
  id="jr-drag-1"
  lessonSlug="01-the-twelve-layers"
  lang="en"
  prompt="Order the phases from keystroke to first paint:"
  items={[
    "DNS resolves the hostname to an IP",
    "TCP three-way handshake",
    "TLS 1.3 handshake and certificate verify",
    "HTTP request sent",
    "HTTP response received",
    "Browser fetches subresources (CSS, JS, fonts) in parallel",
    "Browser lays out DOM and paints the first visible content",
  ]}
/>

<MetaphorComplete
  id="jr-metaphor-1"
  lessonSlug="01-the-twelve-layers"
  lang="en"
  setup="Fill in the blank: the network stack is a _______ where each layer wraps the one below it."
  accepted={["onion", "stack", "layered cake", "set of nested envelopes", "nested envelopes"]}
  canonical="onion (or stack)"
  explanation="Each layer encapsulates the one below: HTTP inside TLS inside TCP inside IP inside Ethernet. The same byte travels through every wrapper on its way out."
/>

</Explanation>

<StackDiagram
  label="The twelve phases of one URL load, executed top to bottom in fixed order: DNS lookup, TCP connect, TLS handshake, proxy check, HTTP request, origin processing, HTTP response, subresource fetches, DOM and CSSOM parse, layout and paint, JavaScript execute, LCP painted"
  hue="var(--d-network)"
  caption="Each phase depends on the output of the one above it, so they run strictly top to bottom. Round-trip count — not round-trip speed — is the primary latency lever, and a CDN edge cache hit collapses phases 4–7 into one nearby response."
  layers={[
    { label: "1. DNS lookup", note: "Rex: hostname → IP" },
    { label: "2. TCP connect", note: "Rita + Sven: 3-way handshake" },
    { label: "3. TLS handshake", note: "Bea + Sven + Cara: keys + cert" },
    { label: "4. Proxy / rate-limit check", note: "Patty: cache, WAF" },
    { label: "5. HTTP request", note: "Bea → Sven: GET /" },
    { label: "6. Origin processing", note: "Sven: auth, DB, serialise" },
    { label: "7. HTTP response", note: "Sven → Bea: status + body" },
    { label: "8. Subresource fetches", note: "Bea: CSS, JS, fonts" },
    { label: "9. DOM + CSSOM parse", note: "Bea: render tree" },
    { label: "10. Layout + paint", note: "Bea: position + rasterise" },
    { label: "11. JavaScript execute", note: "Bea: handlers, hydration" },
    { label: "12. LCP painted", note: "Bea: largest element ready" },
  ]}
/>

<KeyTakeaway>Seven actors execute twelve phases in a fixed sequence; CDN edge caching is the highest-leverage optimisation because it shortens all three handshakes at once.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-the-twelve-layers-retrieval"
  lang="en"
  questions={[
    { q: "Name the seven actors in an HTTPS request and one concern each owns.", a: "Bea (browser, renders), Rex (DNS, resolves), Rita (router, forwards packets), Cara (CA, signed the cert), Patty (CDN/proxy, caches/routes), Sven (origin, computes), Bea's render engine (paints pixels)." },
    { q: "Why can you not parallelise DNS, TCP, TLS, and HTTP?", a: "Each phase depends on the previous phase's output: DNS gives the IP, TCP opens the socket, TLS negotiates keys, only then HTTP can send an encrypted request. The dependency chain forces sequential execution." },
    { q: "What makes CDN edge caching the highest-leverage latency optimisation?", a: "A CDN edge hit collapses origin processing and long-distance round-trips into one nearby response. DNS, TCP, and TLS still run, but against a server 20 ms away instead of 200 ms away — shortening every handshake at once." },
  ]}
/>

<Recap lang="en">
One URL triggers a twelve-phase chain executed by seven actors: Rex resolves the hostname, Rita's routers carry packets, Bea negotiates TLS with Sven using Cara's certificate, Patty may answer from cache, and Bea's render engine paints the result. The phases are strictly ordered — each depends on the previous — so round-trip count, not round-trip speed, is the primary latency lever. A CDN edge hit at 20 ms instead of 200 ms shortens all three handshakes simultaneously. Now when you see a slow page load, your first question is: which actor owns the slow phase — and is the bottleneck the round-trip count or the round-trip time?
</Recap>
