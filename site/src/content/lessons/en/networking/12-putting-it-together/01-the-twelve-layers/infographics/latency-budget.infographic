infographic chart-bar-plain-text
data
  title Where the ~800 ms goes: typical per-phase cost
  desc DNS + TCP + TLS round-trips are a fixed ~150 ms before any byte of HTML — exactly what a CDN edge hit compresses
  values
    - label DNS lookup (cold)
      value 50
    - label TCP connect (1 RTT)
      value 50
    - label TLS handshake (1 RTT)
      value 50
    - label Origin processing
      value 100
    - label Layout + paint
      value 125
    - label JavaScript execute
      value 250
theme
  palette #6366f1 #14b8a6 #f59e0b
