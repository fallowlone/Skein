---
concepts:
- critical-render-path
- LCP
- INP
- CLS
- preload-scanner
- render-blocking-CSS
- resource-hints
- fetchpriority
deepensInto:
- 06-observability-and-traces
estMin: 14
lang: en
lessonType: topic
level: middle
order: 3
prereqs:
- 02-dns-tcp-tls-sequence
- 06-http-versions/04-headers-caching-cors
slug: 03-rendering-and-web-vitals
sources:
- https://web.dev/articles/vitals/
- https://web.dev/articles/lcp
- https://web.dev/articles/inp
- https://hpbn.co/
spiral:
- latency
- multiplexing
status: ready
summary: How the browser turns bytes into pixels — the DOM/CSSOM merge, CSS render-blocking,
  the preload scanner, JavaScript deferral — and how LCP, INP, and CLS measure the
  user''''s experience of that pipeline.
title: Critical render path and Core Web Vitals
track: networking
unit: 12-putting-it-together
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";

<Hook>
The network portion of a page load is done in 150 ms. LCP fires at 3.2 seconds. The gap is the browser's rendering pipeline — and most of it is caused by decisions you made: which CSS to load, where you placed scripts, whether images have dimensions, what JavaScript runs on the main thread before paint. Understanding this pipeline is the second half of page load performance.
</Hook>

<Crux>CSS blocks paint until loaded; JS blocks parse unless deferred; preload scanner saves you — unless URLs are hidden behind JavaScript.</Crux>

<Explanation>

## The critical render path

After the browser receives the HTML bytes, rendering proceeds in several overlapping stages:

1. **HTML parse → DOM.** The HTML parser builds the Document Object Model as bytes stream in. Parsing starts before the full body has arrived — streaming parse.
2. **CSS parse → CSSOM.** When the browser encounters a `<link rel="stylesheet">` tag, it fetches the CSS file and builds the CSS Object Model. **Nothing paints until the CSSOM is ready** — CSS is render-blocking. Why? Showing a page with unstyled content (FOUC — Flash Of Unstyled Content) and then reflowing it causes layout instability that degrades perceived performance. This is intentional, not a bug.
3. **DOM + CSSOM → render tree.** The browser merges the DOM and CSSOM into a render tree containing only visible elements with their computed styles.
4. **Layout.** The browser calculates the exact position and size of each element.
5. **Paint.** The browser rasterises the render tree into pixels.
6. **Compositing.** GPU layers are merged into the final frame.

JavaScript complicates this: a `<script>` tag without `async` or `defer` blocks HTML parsing. The parser pauses, the script is fetched and executed, then parsing resumes. This is why classic scripts placed in `<head>` dramatically slow first paint.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="text-sm w-full border-collapse">
    <thead>
      <tr class="bg-slate-100">
        <th class="border px-3 py-2 text-left">Resource type</th>
        <th class="border px-3 py-2 text-left">Blocks HTML parse?</th>
        <th class="border px-3 py-2 text-left">Blocks render?</th>
        <th class="border px-3 py-2 text-left">Fix</th>
      </tr>
    </thead>
    <tbody>
      <tr><td class="border px-3 py-1">CSS (stylesheet)</td><td class="border px-3 py-1">No</td><td class="border px-3 py-1">Yes</td><td class="border px-3 py-1">inline critical CSS; preload</td></tr>
      <tr><td class="border px-3 py-1">JS (classic, in &lt;head&gt;)</td><td class="border px-3 py-1">Yes</td><td class="border px-3 py-1">Yes</td><td class="border px-3 py-1">add defer or async</td></tr>
      <tr><td class="border px-3 py-1">JS (async)</td><td class="border px-3 py-1">Partially</td><td class="border px-3 py-1">Partially</td><td class="border px-3 py-1">executes when ready, not in order</td></tr>
      <tr><td class="border px-3 py-1">JS (defer)</td><td class="border px-3 py-1">No</td><td class="border px-3 py-1">No</td><td class="border px-3 py-1">executes after HTML parse, in order</td></tr>
      <tr><td class="border px-3 py-1">Images (no dimensions)</td><td class="border px-3 py-1">No</td><td class="border px-3 py-1">Causes CLS</td><td class="border px-3 py-1">add width/height attributes</td></tr>
      <tr><td class="border px-3 py-1">Fonts (WOFF2)</td><td class="border px-3 py-1">No</td><td class="border px-3 py-1">Causes FOIT/FOUT</td><td class="border px-3 py-1">preload + font-display:swap</td></tr>
    </tbody>
  </table>
</div>

## The preload scanner

The browser runs a lightweight HTML tokeniser called the **preload scanner** ahead of the main parser. It looks for `<link>`, `<script>`, and `<img>` URLs and fires requests for them before the main parser has processed those tags. Without it, subresource discovery would be sequential — each resource only fetched after the main parser reached it.

The preload scanner is defeated by:
- JavaScript that writes `<script src="...">` via `document.write`.
- Complex client-side routing that builds URLs after framework initialisation.
- Lazy-loading schemes that hide URLs behind `data-src` attributes.

Modern frameworks (Next.js, Astro, SvelteKit) emit `<link rel="preload">` hints automatically to compensate.

## Resource hints

| Hint | Effect | Cost |
|------|--------|------|
| `<link rel="dns-prefetch" href="//cdn.example.com">` | Resolves DNS in background | Minimal |
| `<link rel="preconnect" href="//cdn.example.com" crossorigin>` | DNS + TCP + TLS in background | One idle socket per origin |
| `<link rel="preload" as="font" crossorigin>` | Fetches font before parser reaches it | Bandwidth |
| `<link rel="modulepreload">` | Fetches ES module + its imports | Bandwidth |
| `<link rel="prefetch">` | Fetches likely-next-navigation resource (low priority) | Bandwidth when idle |

**fetchpriority attribute.** You can override the browser's default resource scheduling:
- `<img src="hero.jpg" fetchpriority="high">` — hero image loaded before other images.
- `<script src="analytics.js" fetchpriority="low">` — defer non-critical JS below CSS and fonts.

## Core Web Vitals

Google's Core Web Vitals quantify user-perceived performance. **Search ranking uses field RUM data** (real user measurements via Chrome UX Report), not synthetic Lighthouse scores.

**LCP — Largest Contentful Paint.** When the largest visible element finishes rendering. Usually the hero image or main heading. Good: under 2.5 s. Poor: over 4 s. Primary causes of poor LCP: slow server TTFB, render-blocking resources delaying the main image, large unoptimised image files.

**INP — Interaction to Next Paint** (replaced FID in March 2024). The longest delay between any user input (click, tap, keypress) and the resulting visual update across the entire page lifecycle. Good: under 200 ms. Poor: over 500 ms. Primary cause: long tasks on the main thread holding the UI unresponsive.

**CLS — Cumulative Layout Shift.** Unexpected movement of visible elements during page load. Score: sum of (impact fraction × distance fraction) for each unexpected shift. Good: under 0.1. Primary causes: images without declared dimensions, ads inserting content, web fonts causing text reflow.

<Inset kind="why" lang="en">
Google uses **field RUM data** (real user measurements from Chrome UX Report) for ranking, not Lighthouse. A page scoring 95 in Lighthouse on your laptop may score 55 in CrUX if real users are on slow Android phones in poor signal. Optimise for the field, not the lab.
</Inset>

<Quiz
  id="mid-q1"
  lessonSlug="03-rendering-and-web-vitals"
  lang="en"
  question="Why does CSS block rendering by default?"
  choices={[
    { label: "Browsers cannot parse CSS in parallel with HTML", misconception: "Modern browsers parse CSS very fast; blocking is a correctness choice, not a performance limitation." },
    { label: "Showing the page unstyled (FOUC) and then reflowing it causes layout instability that hurts perceived performance", correct: true },
    { label: "It is a bug in older browsers that was never fixed", misconception: "It is intentional behaviour across all major engines." },
    { label: "CSS must download before HTML parsing starts", misconception: "HTML parsing starts immediately; CSS only blocks render, not parse." },
  ]}
/>

<TraceScenario
  id="mid-trace-render"
  lessonSlug="03-rendering-and-web-vitals"
  lang="en"
  scenario="Trace what the browser does during HTML streaming — from first byte to LCP."
  steps={[
    { prompt: "First bytes of HTML arrive. What does the browser do immediately?", reveal: "Starts the HTML parser AND the preload scanner in parallel. Parser builds the DOM incrementally; preload scanner looks ahead for <link>, <script>, <img> URLs to fire requests before the parser reaches them." },
    { prompt: "Parser encounters <link rel=stylesheet href=main.css>. HTML parsing continues. Rendering?", reveal: "Browser fetches main.css. HTML parsing continues (CSS does not block parse). Rendering is blocked — nothing paints until the CSSOM is ready. If main.css takes 200 ms to arrive, LCP is delayed 200 ms." },
    { prompt: "Parser encounters <script defer src=app.js>. What happens?", reveal: "Browser fetches app.js (preload scanner may have already started this). The defer attribute means: do not block parsing; execute the script after the HTML is fully parsed. Main thread is not blocked." },
    { prompt: "CSS arrives. CSSOM built. Render tree constructed. Browser paints first frame. What metric fires?", reveal: "First Contentful Paint (FCP) fires when any text or image is painted. LCP fires when the largest visible element (hero image, main heading) is painted — could be the same frame or later if the hero image arrives after the first paint." },
    { prompt: "Hero image loads 400 ms after HTML. LCP fires. What determined whether LCP was Good or Poor?", reveal: "LCP = TTFB + hero image load time + render time. If TTFB was 150 ms, hero image 400 ms (network) + 20 ms (decode/layout), LCP = ~570 ms — Good. If the hero image was not preloaded and the preload scanner missed it (because it was dynamically injected by JavaScript), LCP could be 2+ seconds — Poor." },
  ]}
/>

<DragOrder
  id="mid-drag-1"
  lessonSlug="03-rendering-and-web-vitals"
  lang="en"
  prompt="Order these interventions by their impact on LCP (highest to lowest, assuming LCP is the hero image):"
  items={[
    "Cache HTML at CDN edge so TTFB drops from 300 ms to 20 ms",
    "Add <link rel=preload as=image> for the hero image so it fetches in parallel with CSS",
    "Convert the hero image from JPEG to WebP (30% smaller file size)",
    "Add fetchpriority=high to the hero <img> tag",
    "Remove render-blocking analytics script from <head> (move to defer)",
  ]}
/>

<Quiz
  id="mid-q2"
  lessonSlug="03-rendering-and-web-vitals"
  lang="en"
  question="What is INP measuring that FID did not?"
  choices={[
    { label: "The time from page load to first user interaction", misconception: "That is closer to TTI (Time to Interactive)." },
    { label: "The longest delay between any user input and the resulting visual response across the entire page lifecycle", correct: true },
    { label: "How many layout shifts happened during load", misconception: "That is CLS." },
    { label: "The time the main thread was blocked", misconception: "Total Blocking Time (TBT) measures main-thread blocking; INP is the user-perceived input lag." },
  ]}
/>

</Explanation>

<KeyTakeaway>CSS blocks paint until loaded; defer all non-critical JS; preload the hero image; add dimensions to all images. These four rules prevent the four most common LCP and CLS failures.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-rendering-and-web-vitals-retrieval"
  lang="en"
  questions={[
    { q: "Why is the preload scanner critical to fast page loads, and what defeats it?", a: "The preload scanner is a lightweight HTML tokeniser that runs ahead of the main parser to discover <link>, <script>, and <img> URLs and fire requests before the main parser has finished. Without it, subresource requests would only start after the main parser reaches each tag — sequential network discovery. The preload scanner is defeated by JavaScript that synthesises resource URLs at runtime (e.g. document.write of a script src), by complex client-side routing, and by lazy-loading schemes that hide URLs behind data attributes." },
    { q: "What causes CLS, and how do you fix it?", a: "CLS is caused by content that shifts after it is painted: images without declared width/height (browser reserves no space, reflows when image loads), ads inserting new content above existing text, web fonts causing text reflow. Fix: add explicit width+height to all images, reserve space for ads via fixed-height containers, use font-display:swap and preload fonts to reduce FOIT/FOUT." },
    { q: "Google ranks pages using field RUM data, not Lighthouse. What is the practical implication?", a: "Your Lighthouse score on a fast dev laptop may not reflect real user experience on slow Android phones on 3G. Google's CrUX (Chrome UX Report) aggregates field measurements from real Chrome users. A page can score 95 in Lighthouse and 55 in CrUX. To improve rankings, measure LCP/INP/CLS in the field using RUM tools (SpeedCurve, Sentry, Vercel Analytics), not just synthetic Lighthouse runs." },
  ]}
/>

<Recap lang="en">
The browser's critical render path converts bytes into pixels across six stages: HTML parse, CSS parse, render tree construction, layout, paint, and compositing. CSS is render-blocking — nothing paints until the CSSOM is ready — making it the highest-priority resource to deliver. JavaScript without defer or async blocks HTML parsing, delaying everything. The preload scanner rescues you by firing requests for subresources ahead of the main parser, but it is defeated by dynamically injected URLs. Core Web Vitals (LCP, INP, CLS) measure the user's perception of this pipeline; Google uses field RUM data (real Chrome users), not Lighthouse scores, for ranking. The four highest-impact fixes are: CDN-cache HTML (reduce TTFB), preload the hero image, defer non-critical JS, and add image dimensions.
</Recap>
