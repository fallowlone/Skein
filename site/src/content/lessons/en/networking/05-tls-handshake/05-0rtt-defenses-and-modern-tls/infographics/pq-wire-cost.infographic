infographic compare-binary-horizontal-underline-text-vs
data
  title Classical vs hybrid PQ key exchange
  desc Quantum safety costs bytes: the hybrid key_share blows past the TCP MTU
  compares
    - label Classical X25519
      children
        - label Public key
          desc 32 bytes
        - label ClientHello fits one segment
          desc under the 1500-byte MTU
        - label Quantum risk
          desc store-now, decrypt-later breaks it
    - label Hybrid X25519MLKEM768
      children
        - label ML-KEM-768 public key
          desc 1184 bytes (+ 1088-byte ciphertext)
        - label ClientHello forces TCP segmentation
          desc over 1500 bytes
        - label Secure if either primitive holds
          desc Chrome 131+ default, over 33% of Cloudflare traffic
theme
  palette #0ea5e9 #8b5cf6
