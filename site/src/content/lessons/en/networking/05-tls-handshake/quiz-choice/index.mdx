---
concepts:
- one-rtt-handshake
- forward-secrecy
- session-resumption
- 0-rtt-replay
- cipher-suite
- ech-sni
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://datatracker.ietf.org/doc/html/rfc8446
- https://datatracker.ietf.org/doc/html/rfc8470
- https://blog.cloudflare.com/rfc-8446-aka-tls-1-3/
status: ready
summary: "Multiple-choice synthesis across the TLS 1.3 unit: 1-RTT key shares, forward secrecy, PSK resumption, 0-RTT replay, cipher-suite separation, and ECH."
title: 'TLS 1.3: multiple-choice review'
track: networking
unit: 05-tls-handshake
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each mirrors a decision you make designing or debugging a TLS deployment — not a definition to recite, but a tradeoff to weigh between speed, secrecy, and replay safety.
</Hook>

<Goal lang="en">
Confirm you can connect the 1-RTT handshake, forward secrecy, resumption, 0-RTT replay risk, cipher-suite separation, and SNI privacy — the synthesis the individual lessons built toward.
</Goal>

<Quiz
  id="05-tls-handshake-choice-1"
  lessonSlug="05-tls-handshake"
  lang="en"
  question="TLS 1.2 needed two round-trips for a cold handshake; TLS 1.3 needs one. What single design change collapsed the RTT, and why does Perfect Forward Secrecy come along for free?"
  choices={[
    { label: "The client puts an ephemeral ECDHE key_share in the very first message (ClientHello), so the server derives the shared secret and replies encrypted in one pass. Because both sides use fresh ephemeral keys discarded after the connection, a later theft of the server's long-term key cannot decrypt past sessions — that is PFS.", correct: true },
    { label: "TLS 1.3 dropped certificate validation, removing a whole round-trip.", misconception: "Certificate validation still happens in 1-RTT — it is encrypted under the handshake traffic key in the same pass. The RTT was saved by moving the key_share into ClientHello, not by dropping authentication." },
    { label: "TLS 1.3 uses RSA key transport, which is one message shorter.", misconception: "The opposite: TLS 1.3 removed RSA key transport. RSA transport is exactly what destroys forward secrecy, because a stolen long-term key decrypts every recorded past session." },
    { label: "It compresses the certificate so everything fits in one packet.", misconception: "Certificate size is unrelated to RTT count. The round-trip saving comes from embedding the ECDHE key_share early, letting the server derive the secret immediately." },
  ]}
/>

<Quiz
  id="05-tls-handshake-choice-2"
  lessonSlug="05-tls-handshake"
  lang="en"
  question="A warm PSK-resumption handshake skips certificate validation entirely. How does the client still know it is talking to the legitimate server and not an impostor?"
  choices={[
    { label: "The PSK was established during a prior fully-authenticated handshake; only a server that completed that handshake and holds the STEK to decrypt the ticket can use the PSK, so reusing it authenticates the server implicitly.", correct: true },
    { label: "It does not — PSK resumption is unauthenticated, which is why it is only used for static assets.", misconception: "Resumption is authenticated: the PSK itself is the credential. Only the server holding the session-ticket encryption key can recover and use the PSK." },
    { label: "The client silently re-fetches and re-validates the certificate on the resumed connection.", misconception: "There is no extra certificate fetch on resumption — that is the whole point of skipping PKI cost. The PSK provides the proof of identity instead." },
    { label: "DNSSEC validates the server identity before resumption.", misconception: "DNSSEC is unrelated to TLS resumption. The authentication carries over from the original handshake via the PSK." },
  ]}
/>

<Quiz
  id="05-tls-handshake-choice-3"
  lessonSlug="05-tls-handshake"
  lang="en"
  question="An engineer enables 0-RTT globally to cut latency. A week later, finance reports occasional duplicate transfers. What is the root cause and the correct fix?"
  choices={[
    { label: "0-RTT early data is replayable: an on-path attacker (or a retrying network) can resend the captured 0-RTT ClientHello, so the POST /transfer executes twice. Restrict 0-RTT to idempotent routes and return 425 Too Early for any mutating handler that arrives in early data.", correct: true },
    { label: "0-RTT uses a weaker cipher that corrupted the request body and duplicated it.", misconception: "0-RTT uses the same AEAD ciphers as 1-RTT data. The flaw is replay, not cipher strength — the same valid ciphertext can be delivered more than once." },
    { label: "The TLS session ticket expired mid-flight and the client retransmitted.", misconception: "Ticket expiry causes a fresh handshake, not a duplicated mutation. The duplicate is a replay of early data that the application processed twice." },
    { label: "Switch off PSK resumption — it is incompatible with POST requests.", misconception: "PSK resumption is fine for any method; it is 0-RTT early data specifically that is replayable. The fix is route-level idempotency policy plus 425 Too Early, not disabling resumption." },
  ]}
/>

<Quiz
  id="05-tls-handshake-choice-4"
  lessonSlug="05-tls-handshake"
  lang="en"
  question="In TLS 1.3 the cipher suite TLS_AES_128_GCM_SHA256 no longer names the key exchange or signature algorithm — unlike TLS 1.2. What did this separation buy?"
  choices={[
    { label: "Key exchange (named group), authentication (signature algorithm), and the bulk AEAD-plus-hash are negotiated on three independent axes, collapsing TLS 1.2's hundreds of combinatorial suites to five and removing whole classes of weak/mismatched combinations.", correct: true },
    { label: "It let the client change the cipher suite mid-connection without renegotiation.", misconception: "Mid-connection rotation is KeyUpdate, which reuses the same suite. The separation is about negotiation axes, not changing the suite during the session." },
    { label: "It made RSA key transport selectable independently of the cipher.", misconception: "RSA key transport was removed in TLS 1.3 entirely. The separated axes are named group, signature algorithm, and AEAD+hash — all forward-secret key exchange." },
    { label: "It allowed non-AEAD ciphers like CBC to be paired with stronger hashes.", misconception: "TLS 1.3 only permits AEAD ciphers; CBC-mode and the MAC-then-encrypt combinations were removed. The separation does not reintroduce them." },
  ]}
/>

<Quiz
  id="05-tls-handshake-choice-5"
  lessonSlug="05-tls-handshake"
  lang="en"
  question="A middlebox rewrites the ALPN list in ClientHello to strip 'h2' and force HTTP/1.1. The connection aborts before any data flows. Why?"
  choices={[
    { label: "Every handshake byte feeds the running transcript hash, and the Finished message is an HMAC over that transcript. Tampering with ALPN changes the transcript, so the Finished HMAC fails to verify on both sides and the connection aborts — the same mechanism that catches version-downgrade attempts.", correct: true },
    { label: "ALPN is encrypted, so the middlebox corrupted the ciphertext.", misconception: "ALPN is in the plaintext ClientHello, so a middlebox can read and edit it — but it is still folded into the transcript hash, so the edit is detected by the Finished MAC." },
    { label: "The server detected the wrong source IP and refused.", misconception: "Source IP is unrelated. The abort is the Finished HMAC mismatch caused by the altered transcript, which is how TLS 1.3 makes the handshake tamper-evident." },
    { label: "HTTP/1.1 is not supported in TLS 1.3, so the downgrade is rejected.", misconception: "HTTP/1.1 runs fine over TLS 1.3. The rejection is the transcript-integrity check, not a protocol-version restriction." },
  ]}
/>

<Quiz
  id="05-tls-handshake-choice-6"
  lessonSlug="05-tls-handshake"
  lang="en"
  question="A security review flags that the corporate proxy can still log which hostnames employees visit, even on TLS 1.3. Which mechanism leaks this, and what closes the gap?"
  choices={[
    { label: "SNI is sent in plain text in ClientHello so the server can pick the right certificate for a shared IP, exposing the hostname to any on-path observer. Encrypted ClientHello (ECH) encrypts the inner ClientHello via HPKE under the origin's DNS-published key, so observers see only the generic front-end name.", correct: true },
    { label: "The certificate's Subject Alternative Name is broadcast in clear text after the handshake.", misconception: "In TLS 1.3 the Certificate message is encrypted under the handshake traffic key — the SAN is not visible. The plaintext leak is SNI in ClientHello, which ECH addresses." },
    { label: "OCSP requests reveal the hostname; enabling OCSP stapling fixes the privacy leak entirely.", misconception: "OCSP stapling does remove the live OCSP privacy leak, but the hostname is still exposed via plaintext SNI. Closing that gap requires ECH, not stapling." },
    { label: "Nothing leaks — TLS 1.3 encrypts the entire ClientHello by default.", misconception: "TLS 1.3 does not encrypt ClientHello by default; SNI, ALPN, and supported groups are visible. ECH is the opt-in extension that encrypts the sensitive inner hello." },
  ]}
/>

<Recap lang="en">
The through-line of the unit is one chain of tradeoffs: the ECDHE key_share in ClientHello buys the 1-RTT handshake and forward secrecy together; PSK resumption trades fresh authentication for speed but stays authenticated via the ticket; 0-RTT trades a round-trip for replay exposure, contained only by idempotency plus 425 Too Early; cipher-suite separation shrinks the attack surface to five suites; and the transcript hash makes every negotiated parameter tamper-evident. SNI still leaks the hostname until ECH encrypts it. Speed, secrecy, and replay safety are the three axes you balance on every TLS decision.
</Recap>
