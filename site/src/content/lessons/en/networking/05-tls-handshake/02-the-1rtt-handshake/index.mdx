---
concepts:
- clienthello
- serverhello
- ecdhe
- key-share
- pfs
- certificate-chain
- transcript-hash
- finished
- certificate-verify
deepensInto:
- 03-session-resumption-and-0rtt
- 04-key-schedule-and-extensions
estMin: 14
lang: en
lessonType: topic
level: middle
order: 2
prereqs:
- 01-what-tls-does
slug: 02-the-1rtt-handshake
sources:
- https://datatracker.ietf.org/doc/html/rfc8446
- https://hpbn.co/transport-layer-security-tls/
- https://blog.cloudflare.com/rfc-8446-aka-tls-1-3/
spiral:
- statefulness
status: ready
summary: How ClientHello key shares, ECDHE ephemeral keys, and the transcript hash
  collapse TLS 1.2''''s two-RTT negotiation into one — and why Perfect Forward Secrecy
  follows automatically.
title: 'The 1-RTT handshake: key shares and ECDHE'
track: networking
unit: 05-tls-handshake
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";
import PacketDot from "~/components/diagram/PacketDot.astro";

<Hook>
The browser opens a TCP connection and immediately sends a ClientHello. That single message carries the cipher list AND the client's half of the encryption key — the server can compute the shared secret on the spot and reply with encrypted data in one pass. This is how TLS 1.3 cuts two round-trips to one.
</Hook>

<Crux>TLS 1.3 embeds the ECDHE key share in ClientHello; the server derives the shared secret immediately and replies encrypted in one RTT.</Crux>

<Explanation>

## The 1-RTT handshake step by step

**Step 1 — ClientHello.** The client sends: the TLS version list (via the `supported_versions` extension), supported cipher suites, supported named groups (curves), supported signature algorithms, and a `key_share` extension containing at least one ECDHE public key — typically `x25519`.

**Step 2 — ServerHello.** The server picks a cipher suite (e.g. `TLS_AES_128_GCM_SHA256`), picks a named group from the client's key_share, generates its own ephemeral keypair, and replies with its public key. Both sides now have each other's ephemeral public keys and can run ECDHE to derive the shared secret.

**Step 3 — Encrypted handshake.** The server immediately derives the handshake traffic key from the shared secret and sends `EncryptedExtensions`, `Certificate`, `CertificateVerify`, and server `Finished` — all encrypted — back to back in the same network pass.

**Step 4 — Client completes.** The client derives the same handshake traffic key, decrypts the server's messages, validates the certificate chain, validates the `CertificateVerify` signature over the transcript, verifies the server `Finished` HMAC, sends client `Finished`, and starts sending application data.

Total round-trips: **one.** ClientHello leaves the client, ServerHello + encrypted handshake arrive, client Finished + first app data leave — all in a single network exchange.

<NumbersCard
  id="card-handshake-wire"
  title="Wire cost of a TLS 1.3 cold handshake"
  data-lesson-visual
  stats={[
    { label: "ClientHello", value: "~512 bytes" },
    { label: "ServerHello", value: "~150 bytes" },
    { label: "Certificate (ECDSA P-256 chain)", value: "1–3 KB" },
    { label: "CertificateVerify (ECDSA)", value: "~70 bytes" },
    { label: "Server Finished", value: "~50 bytes" },
    { label: "Client Finished", value: "~50 bytes" },
    { label: "Total RTTs", value: "1" },
  ]}
/>

<DiagramFrame label="A single packet travels from Client to Server carrying ClientHello, then a second packet returns from Server to Client carrying ServerHello and the encrypted handshake — the one round trip that 1-RTT costs" hue="var(--d-network)" caption="The whole handshake fits in one round trip: ClientHello out, ServerHello plus the encrypted handshake back. After that single round-trip latency, application data flows.">
  <svg viewBox="0 0 360 120" width="360" height="120" style="display:block;margin:0 auto;max-width:100%;height:auto;">
    <rect x="6" y="46" width="80" height="28" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="46" y="64" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--ink)">Client</text>
    <rect x="274" y="46" width="80" height="28" rx="6" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="314" y="64" text-anchor="middle" font-size="11" font-family="var(--font-body)" fill="var(--ink)">Server</text>
    <text x="180" y="30" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--muted)">ClientHello (key_share)</text>
    <text x="180" y="104" text-anchor="middle" font-size="10" font-family="var(--font-body)" fill="var(--muted)">ServerHello + cert/params</text>
    <path data-path d="M86 60 L274 60" fill="none" stroke="var(--hairline-2)" stroke-width="1.4" stroke-dasharray="4 4" />
    <PacketDot color="var(--d-network)" durationMs={1500} size={9} />
    <PacketDot color="var(--d-network)" durationMs={1500} delayMs={1700} size={9} />
  </svg>
</DiagramFrame>

## ECDHE: the key exchange behind key shares

Both sides agree on a curve (usually `x25519` for performance, or `P-256` for FIPS). Each generates a private scalar (a random 256-bit integer) and computes a public point: `public = private · G`, where `G` is the curve's base generator. Bea sends her public point in the key_share; Sven sends his back. Each side computes the shared secret by multiplying the other's public point by their own private scalar:

- Bea: `shared = bea_private · sven_public`
- Sven: `shared = sven_private · bea_public`

Both arrive at the same point on the curve. An eavesdropper who saw only the public points cannot recover it without solving the elliptic-curve discrete-log problem — intractable for properly sized curves on classical hardware.

## Perfect Forward Secrecy (PFS)

Both the client and server use **ephemeral** key pairs generated fresh for this connection and discarded after it. If an attacker later steals Sven's long-term private key from his disk, the attacker **cannot decrypt past sessions** — those sessions used ephemeral keys that are already gone. This is Perfect Forward Secrecy.

TLS 1.2 allowed RSA key transport: the client encrypted the session secret with the server's long-term public key. Stealing the private key meant decrypting every recorded past session. TLS 1.3 **mandates** ephemeral key exchange, making PFS the default with no configuration needed.

## Certificate chain validation

When you debug a TLS handshake failure in production, it is almost always this step: certificate chain validation is where misconfigured servers, expired intermediates, and missing trust-store entries show up as cryptic alerts. Understanding every check makes diagnosing `ERR_CERT_AUTHORITY_INVALID` a five-second job instead of a thirty-minute hunt.

When the client receives `Certificate`, it sees a chain: the leaf certificate for the hostname, then one or more intermediate certificates. It walks the chain bottom-up:

1. Verify the leaf was signed by the intermediate's public key.
2. Verify the intermediate was signed by a root.
3. Confirm the root is in the local trust store (the CA bundle shipped with OS/browser).
4. Check the leaf's Subject Alternative Names (SANs) include the connection hostname.
5. Check validity period (`notBefore <= now <= notAfter`).
6. Check revocation (CRLite, stapled OCSP, or short-lived cert policy).

If any step fails, the connection terminates with a fatal alert (e.g. `unknown_ca`). All six checks must pass — a server that sends only the leaf certificate without its intermediate will fail step 2, even if the leaf itself is perfectly valid.

## The transcript hash and Finished

Every byte sent during the handshake is fed into a running SHA-256 digest called the **transcript hash**. The `Finished` message is `HMAC(finished_key, transcript_hash)`. If a single bit on the wire was flipped or replaced — by a network bug or a man-in-the-middle — the HMAC will not match and the connection aborts. This is how TLS 1.3 detects downgrade attacks: the transcript records the version the client originally proposed, so a middlebox that tries to force TLS 1.2 breaks the Finished check.

<TraceScenario
  id="mid-trace-1"
  pieceSlug="02-the-1rtt-handshake"
  lang="en"
  scenario="Trace a successful TLS 1.3 cold handshake from ClientHello to first encrypted application byte."
  steps={[
    { prompt: "Client sends ClientHello. What fields matter most?", reveal: "client random, supported cipher suites, supported named groups, supported signature algorithms, and key_share with at least one ECDHE public key (typically x25519)." },
    { prompt: "Server picks cipher suite + named group. What does it send back?", reveal: "ServerHello with server random, chosen cipher suite, and the server's key_share. Both sides now have each other's public keys and can derive the ECDHE shared secret." },
    { prompt: "What handshake messages are sent encrypted under the server handshake traffic key?", reveal: "EncryptedExtensions, Certificate, CertificateVerify, and server Finished — back-to-back in the same RTT." },
    { prompt: "Client validates and responds. What does the client send?", reveal: "Client Finished (HMAC over the entire transcript) plus the first encrypted application data record." },
    { prompt: "Total RTT cost?", reveal: "Exactly one RTT. ClientHello goes out, ServerHello + encrypted handshake come back, client Finished + app data go out — completing in one round trip." },
  ]}
/>

<Misconception
  id="pfs-misconception"
  title="'TLS 1.3 uses ephemeral keys, so session resumption also has PFS'"
>
PSK-only resumption uses the cached pre-shared key with no fresh ECDHE secret. A leaked STEK lets an attacker decrypt past resumed sessions. For PFS, the resumption ClientHello must include a fresh key_share (PSK-DHE mode).
</Misconception>

<Quiz
  id="mid-quiz-1"
  pieceSlug="02-the-1rtt-handshake"
  lang="en"
  question="Why does TLS 1.3 mandate ephemeral key exchange (ECDHE) and remove RSA key transport?"
  choices={[
    { label: "ECDHE is faster than RSA key transport", misconception: "Performance is a side effect. The real driver is Perfect Forward Secrecy." },
    { label: "To enable Perfect Forward Secrecy — past sessions stay safe even if the long-term key leaks later", correct: true },
    { label: "RSA keys are no longer trusted by browsers", misconception: "RSA is still used for certificate signatures; it is just not used for key transport in TLS 1.3." },
    { label: "ECDHE supports post-quantum cryptography", misconception: "Classical ECDHE is not post-quantum. Hybrid PQ key exchange is a separate addition." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="02-the-1rtt-handshake"
  lang="en"
  question="What integrity guarantee does the TLS 1.3 transcript hash provide?"
  choices={[
    { label: "It encrypts the handshake messages", misconception: "Encryption is separate. The transcript hash is about integrity, not confidentiality." },
    { label: "It detects any tampering with handshake bytes — the Finished MAC will fail to verify", correct: true },
    { label: "It speeds up the handshake by skipping the certificate", misconception: "Certificate verification still happens. Transcript hash is unrelated to speed." },
    { label: "It is only used for session resumption", misconception: "Transcript hash is computed on every handshake, fresh or resumed." },
  ]}
/>

<DragOrder
  id="mid-drag-1"
  pieceSlug="02-the-1rtt-handshake"
  lang="en"
  prompt="Order the certificate chain validation steps:"
  items={[
    "Receive Certificate message containing leaf + intermediate(s)",
    "Confirm leaf certificate's SAN matches the connection hostname",
    "Walk the chain: verify each cert's signature using the issuer's public key",
    "Find a root in the local trust store matching the topmost issuer",
    "Check validity period (notBefore <= now <= notAfter)",
    "Check revocation status (CRLite, stapled OCSP, or short-lived cert policy)",
  ]}
/>

<Inset kind="why" lang="en">
**RSA vs ECDSA certificates.** The signature algorithm of the server certificate is independent of the ECDHE key exchange. ECDSA P-256 certificates are roughly 5x faster to verify than RSA-2048 and produce signatures four times smaller. An edge serving a million handshakes per second pays five times the CPU bill on RSA. Cloud providers default to ECDSA chains; RSA is offered only for legacy clients that cannot handle ECDSA.
</Inset>

</Explanation>

<SequenceDiagram
  label="TLS 1.3 1-RTT handshake: ClientHello carries the key_share, the server replies with ServerHello plus the encrypted EncryptedExtensions, Certificate, CertificateVerify and server Finished, then the client sends Finished and the first application data"
  hue="var(--d-crypto)"
  actors={["Client", "Server"]}
  messages={[
    { from: "Client", to: "Server", label: "ClientHello (key_share, ciphers, groups)" },
    { from: "Server", to: "Client", label: "ServerHello (key_share)" },
    { from: "Server", to: "Client", label: "{EncryptedExtensions, Certificate, CertificateVerify, Finished}" },
    { from: "Client", to: "Server", label: "Finished + app data" },
  ]}
  caption="ClientHello carries the ECDHE key_share, so the server derives the shared secret on receipt and sends the rest of the handshake — encrypted under the handshake traffic key — in the same pass. One round-trip: ClientHello out, ServerHello plus encrypted handshake back, then client Finished plus first app data out."
/>

<KeyTakeaway>TLS 1.3's 1-RTT handshake puts the client's ECDHE public key in ClientHello so the server derives the shared secret immediately; ephemeral key pairs guarantee Perfect Forward Secrecy; and the transcript HMAC in Finished detects any mid-flight tampering.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-the-1rtt-handshake-retrieval"
  lang="en"
  questions={[
    { q: "Why does the server send HelloRetryRequest, and what does the client do in response?", a: "The server sends HelloRetryRequest when the client's key_share extension does not include a named group the server is willing to use. The HRR message carries a cookie and the named group the server wants. The client then sends a fresh ClientHello with a key_share for that group plus the cookie. This adds one RTT but is rare because modern clients offer multiple key shares up front (typically x25519 plus P-256)." },
    { q: "What happens when certificate chain validation fails (unknown CA)?", a: "The client sends a fatal Alert with code unknown_ca (code 48) and closes the connection. The user sees ERR_CERT_AUTHORITY_INVALID. No application data is exchanged. Common causes: self-signed cert, internal CA not in the trust store, or an expired intermediate (e.g. the Let's Encrypt DST Root CA X3 expiry in 2021)." },
    { q: "Explain ECDHE in one paragraph. Why can both sides arrive at the same secret without exchanging it?", a: "Each side generates a private scalar and a public point (private * G on the curve). They exchange public points. Each then multiplies the other's public point by their own private scalar: Bea gets bea_private * sven_public; Sven gets sven_private * bea_public. By ECDH math both results equal bea_private * sven_private * G — the same point. An eavesdropper who sees only the public points cannot reverse the multiplication (the elliptic-curve discrete-log problem)." },
  ]}
/>

<Recap lang="en">
TLS 1.3 compresses the handshake to one RTT by embedding the client's ECDHE public key in the first message. The server immediately runs elliptic-curve Diffie-Hellman to compute the shared secret, encrypts the rest of the handshake under a derived traffic key, and sends certificate, CertificateVerify, and Finished in the same pass. Because both sides use fresh ephemeral keys, compromising the server's long-term signing key later cannot decrypt past sessions — this is Perfect Forward Secrecy. The transcript hash fed into every Finished message detects any tampering with handshake bytes, including downgrade attempts by middleboxes. Now when you see a TLS alert like `unknown_ca` or a Finished verification failure in your logs, you will know exactly which step in this chain broke and what to fix first.
</Recap>
