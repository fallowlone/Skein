---
slug: 03-session-resumption-and-0rtt
lang: en
track: networking
unit: 05-tls-handshake
order: 3
title: "Session resumption and 0-RTT"
summary: "PSK tickets let warm connections skip certificate validation; 0-RTT early data eliminates the handshake RTT for read-only requests — but replay attacks constrain what can safely go in early data."
estMin: 12
status: ready
lessonType: topic
level: middle
prereqs: ["02-the-1rtt-handshake"]
deepensInto: ["05-0rtt-defenses-and-modern-tls"]
spiral: ["statefulness"]
concepts: ["psk", "new-session-ticket", "session-resumption", "0-rtt", "early-data", "replay-attack", "425-too-early", "idempotent"]
sources:
  - https://datatracker.ietf.org/doc/html/rfc8446
  - https://datatracker.ietf.org/doc/html/rfc8470
  - https://blog.cloudflare.com/tls-session-resumption-full-speed-and-secure/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import Misconception from "~/components/layout/Misconception.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook>
The first TLS connection to a server costs one full RTT. But when you reload the same page five minutes later, the browser already knows that server. TLS 1.3 lets it skip certificate validation entirely and — in the right conditions — attach the first request to the handshake itself, paying zero extra RTTs.
</Hook>

<Crux>PSK resumption skips certificate validation on warm connections; 0-RTT early data cuts the handshake RTT — both carry security tradeoffs.</Crux>

<Explanation>

## NewSessionTicket and PSK resumption

After a successful handshake, the server sends a `NewSessionTicket` message — an encrypted blob containing a **pre-shared key (PSK)** and metadata (ticket age, lifetime). The client caches it keyed by hostname.

On the next connection to the same server, the client includes a `pre_shared_key` extension in its ClientHello with the ticket. The server decrypts the ticket, recognizes the PSK, and skips certificate validation entirely. It derives the session keys from the PSK instead of ECDHE.

**Cost:** still one full RTT (the ClientHello must still reach the server), but no expensive PKI operations. Certificate parsing and chain validation are eliminated.

## 0-RTT early data

If the browser has a cached PSK, it can encrypt the first request using the PSK's key and attach it to the ClientHello itself — before the handshake completes. The server decrypts and processes the request immediately, then sends both the handshake response and the application response. Cost for the request: **zero extra RTTs**.

<NumbersCard
  id="card-rtt-comparison"
  title="RTT cost comparison"
  data-lesson-visual
  stats={[
    { label: "TLS 1.2 cold", value: "2 RTT" },
    { label: "TLS 1.3 cold", value: "1 RTT" },
    { label: "TLS 1.3 warm (PSK resumption)", value: "1 RTT (no cert)" },
    { label: "TLS 1.3 0-RTT", value: "0 RTT for request" },
    { label: "At 100 ms RTT: 0-RTT saving", value: "100 ms vs cold TLS 1.3" },
    { label: "Typical ticket lifetime", value: "up to a few hours (impl-defined)" },
  ]}
/>

## The replay problem

0-RTT has a fundamental security weakness: **it is not protected against replay attacks**. An attacker on the wire can capture the 0-RTT ClientHello and resend it to the server. The server, seeing a valid PSK ticket and valid early data, may process the request again.

- For `GET /news`: replay is harmless — same read, same result.
- For `POST /transfer`: replay is catastrophic — the database row is created twice, money is transferred twice.

The simplest rule: **0-RTT is only safe for idempotent requests** where replaying produces no additional side effects. `GET`, `HEAD`, and `OPTIONS` are typically safe. `POST`, `PUT`, `DELETE` that mutate state are unsafe.

## RFC 8470 and 425 Too Early

RFC 8470 defines the HTTP response code **425 Too Early**: the server returns this status when it receives a 0-RTT request for a route that is not safe to replay. The client must retry after the handshake completes. Frameworks (Spring, Express, Caddy) expose middleware that inspects the `Early-Data: 1` request header and returns `425 Too Early` for any handler that mutates state.

<Misconception
  id="0rtt-safe-misconception"
  title="'0-RTT is a free performance win — just enable it'"
>
0-RTT is only safe for idempotent paths. A GET that mutates a cart is technically GET but semantically a mutation — replays double the cart contents. Mark routes explicitly as 0-RTT-eligible or forbidden; never rely on HTTP method alone.
</Misconception>

<TraceScenario
  id="mid-trace-1"
  pieceSlug="03-session-resumption-and-0rtt"
  lang="en"
  scenario="A POST /transfer arrives with TLS early_data and ticket_age = 5 seconds. Trace the server's decision path."
  steps={[
    { prompt: "Step 1: Is ticket_age inside the replay window (default 10 s)?", reveal: "5 s is inside the 10 s window. The ticket is not trivially stale. Move to the next check." },
    { prompt: "Step 2: Has this ticket nonce been seen before (single-use cache)?", reveal: "Server checks (ticket_nonce, age_bucket) in its anti-replay LRU. If already seen, reject early data." },
    { prompt: "Step 3: Ticket is fresh and unseen. What is the third check?", reveal: "Application-layer policy: POST /transfer is not idempotent. Framework returns 425 Too Early. Client must retry after Finished." },
    { prompt: "Step 4: A GET /catalog/42 arrives with the same valid 0-RTT context. What happens?", reveal: "All three checks pass: in window, not seen, idempotent. Server processes the GET in 0-RTT and responds, saving one RTT." },
  ]}
/>

<Quiz
  id="mid-quiz-1"
  pieceSlug="03-session-resumption-and-0rtt"
  lang="en"
  question="Why can an attacker replay 0-RTT early data but not regular TLS 1.3 data?"
  choices={[
    { label: "0-RTT data uses a weaker cipher", misconception: "0-RTT uses the same AEAD ciphers. The issue is replay protection, not cipher strength." },
    { label: "0-RTT data is derived from a static PSK with no per-connection nonce covering the early_data, so the same ciphertext is valid on replay", correct: true },
    { label: "Regular TLS 1.3 encrypts twice", misconception: "Regular data uses one AEAD pass. The difference is the handshake-derived nonce that prevents replay after Finished." },
    { label: "Replay is only possible on HTTP/2", misconception: "The replay window is a TLS-layer issue, independent of the HTTP version above." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="03-session-resumption-and-0rtt"
  lang="en"
  question="PSK resumption skips certificate validation. How does the client still know it is talking to the right server?"
  choices={[
    { label: "It does not — PSK resumption has no authentication", misconception: "The PSK itself is the authentication: only a server that completed the original handshake (and holds the STEK to decrypt the ticket) can use the PSK." },
    { label: "The PSK was established during a previous authenticated handshake — reusing it authenticates the server implicitly", correct: true },
    { label: "The client re-fetches the certificate on every request", misconception: "There is no extra certificate fetch on resumption. The PSK credential is the proof." },
    { label: "The client verifies DNSSEC before resuming", misconception: "DNSSEC is not involved in TLS resumption." },
  ]}
/>

<Inset kind="why" lang="en">
**NewSessionTicket obfuscation.** Each ticket includes a `ticket_age_add` — a random value the client adds to the real ticket age before sending `obfuscated_ticket_age`. This stops on-path observers from correlating tickets across connections by their visible age. It also makes timing-based replay window checks more robust: the server subtracts `ticket_age_add` to recover the real age and compare it to its expected RTT window.
</Inset>

</Explanation>

<KeyTakeaway>PSK resumption avoids certificate PKI cost on warm connections (still 1 RTT); 0-RTT eliminates the handshake RTT for idempotent requests but is inherently replayable — restrict it to GET/HEAD/OPTIONS and enforce 425 Too Early for mutations.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-session-resumption-and-0rtt-retrieval"
  lang="en"
  questions={[
    { q: "What does the server send after a handshake to enable future resumption?", a: "One or more NewSessionTicket messages — each is an encrypted blob containing a pre-shared key, a ticket nonce, a lifetime, and a ticket_age_add obfuscator. The client caches them keyed by hostname and presents them in the pre_shared_key extension of future ClientHello messages." },
    { q: "Why is 0-RTT limited to idempotent HTTP methods?", a: "0-RTT early data can be captured and replayed by an attacker. Replaying an idempotent GET produces the same result with no side effects. Replaying a POST that creates a row, a PUT that updates state, or a DELETE that removes data causes the action to execute twice — duplicate charges, double rows, etc. The HTTP spec defines idempotent methods, but application logic must also be checked: a GET that mutates a cart is idempotent by HTTP but dangerous for 0-RTT." },
    { q: "What status code should a server return for a 0-RTT POST to a mutating endpoint, and what must the client do?", a: "425 Too Early (RFC 8470). The server rejects the early_data, buffers or discards the request body, and the client must retransmit the request after the handshake Finished exchange completes. The Early-Data: 1 request header signals to the application layer that the request arrived in early data." },
  ]}
/>

<Recap lang="en">
After a TLS 1.3 handshake, the server issues a NewSessionTicket containing a pre-shared key. On the next connection, the client presents this ticket so the server can skip certificate validation — still one RTT, but cheaper. With a cached PSK, the client can also encrypt the first request inside the ClientHello itself as 0-RTT early data: the server processes it before the handshake completes, saving a full RTT. The catch is replay vulnerability: the same 0-RTT ClientHello can be captured and resent by an attacker. Safe use is restricted to idempotent requests (GET, HEAD, OPTIONS), and servers must return 425 Too Early for any mutating endpoint that receives early data.
</Recap>
