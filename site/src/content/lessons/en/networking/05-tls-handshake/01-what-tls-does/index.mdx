---
slug: 01-what-tls-does
lang: en
track: networking
unit: 05-tls-handshake
order: 1
title: "What TLS does and why it exists"
summary: "TLS builds a sealed channel over an untrusted network — before TLS 1.3 that took two round-trips; understanding why sets up everything that follows."
estMin: 8
status: ready
lessonType: topic
level: junior
prereqs: ["04-dns-resolution/05-encrypted-dns"]
deepensInto: ["02-the-1rtt-handshake"]
spiral: ["encapsulation"]
concepts: ["tls", "https", "sealed-channel", "certificate-authority", "pki", "confidentiality", "integrity"]
sources:
  - https://datatracker.ietf.org/doc/html/rfc8446
  - https://hpbn.co/transport-layer-security-tls/
  - https://developer.mozilla.org/en-US/docs/Web/Security/Transport_Layer_Security
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook>
TCP agreed on sequence numbers, DNS resolved a name to an IP. Now the browser and server must agree on encryption before anything that matters can travel. That negotiation is the TLS handshake — and it is the difference between a clear-text postcard and a sealed envelope.
</Hook>

<Crux>TLS builds a sealed channel: anyone can intercept it, but only the sender and receiver can read what is inside.</Crux>

<Explanation>

## What TLS does in one sentence

TLS (Transport Layer Security) wraps every byte of application data in a symmetric-key envelope that nobody on the network can read. The envelope is negotiated via asymmetric cryptography during the handshake; once negotiated, the symmetric key handles all data at line speed.

Plain HTTP sends every word you type in clear text. Your password, your bank balance, the article you are reading — visible to anyone on the same coffee-shop wifi, your ISP, or a corporate proxy. TLS turns that same wire into a locked tunnel.

## The sealed-envelope metaphor

Bea wants to mail Sven a message, but she fears the mailman will read it. She puts it in a box, locks it with a seal, and mails it. Only Sven has a matching key. Before mailing, both agree on the seal and the key: Bea proposes locks she knows, Sven picks one and sends his half of the key plus an ID card from Cara the trusted issuer. They combine halves into a shared seal. TLS 1.3 made the meeting shorter by letting Bea send her half in the first message, instead of waiting for Sven to ask.

## Three guarantees TLS provides

1. **Confidentiality** — nobody on the wire can read the data.
2. **Integrity** — nobody can tamper with the data without detection.
3. **Authentication** — the server proves it owns the domain (via a certificate issued by a trusted CA).

<NumbersCard
  id="card-tls-overview"
  title="TLS 1.3 at a glance"
  data-lesson-visual
  stats={[
    { label: "TLS 1.2 cold handshake", value: "2 RTT" },
    { label: "TLS 1.3 cold handshake", value: "1 RTT" },
    { label: "TLS 1.3 resumed (PSK)", value: "1 RTT (no cert check)" },
    { label: "TLS 1.3 0-RTT", value: "0 RTT for request" },
    { label: "HTTPS share of web traffic (2025)", value: "≥95%" },
    { label: "Browser shows padlock when", value: "TLS handshake succeeded" },
  ]}
/>

## One scenario end to end

You type `https://`. The browser has already finished DNS (it knows the IP) and TCP (a connection is open). Now it builds the TLS seal. Once built, every HTTP request and response rides inside it. The browser shows a padlock.

The padlock means: **nobody on the network can read what passes between you and this hostname.** It does not mean the website is trustworthy — a scam site can have a valid padlock. It does not mean your device is safe. It guarantees only the channel.

<Quiz
  id="jr-quiz-1"
  pieceSlug="01-what-tls-does"
  lang="en"
  question="What does TLS actually protect?"
  choices={[
    { label: "Only the password field I type", misconception: "TLS protects the entire channel, not just one field. Everything you send and receive is sealed." },
    { label: "The whole connection between you and the website", correct: true },
    { label: "The website's content from being changed by hackers later", misconception: "TLS protects data in transit, not data at rest on the server." },
    { label: "Your device from being hacked", misconception: "TLS is about the wire. A compromised device is a separate problem TLS cannot fix." },
  ]}
/>

<Quiz
  id="jr-quiz-2"
  pieceSlug="01-what-tls-does"
  lang="en"
  question="The padlock icon in the browser means..."
  choices={[
    { label: "This website is safe to trust with anything", misconception: "It only means the channel is sealed. A scam site can have a valid padlock." },
    { label: "Nobody on the network can read what passes between you and this hostname", correct: true },
    { label: "Your password cannot be stolen anywhere", misconception: "Channel-only guarantee. Compromised device or compromised server still leaks." },
    { label: "The website has been audited by a certificate authority", misconception: "CAs verify domain control, not website behaviour." },
  ]}
/>

<DragOrder
  id="jr-drag-1"
  pieceSlug="01-what-tls-does"
  lang="en"
  prompt="Put the TLS handshake steps in order (simplified):"
  items={[
    "Bea sends her list of supported ciphers and her half of the key",
    "Sven picks a cipher, sends his half of the key and his certificate",
    "Bea checks the certificate against trusted CAs",
    "Both sides combine their halves into a shared secret",
    "Every message after this point is encrypted",
  ]}
/>

<MetaphorComplete
  id="jr-metaphor-1"
  pieceSlug="01-what-tls-does"
  lang="en"
  setup="Fill in the blank: TLS turns your connection into a sealed _______ that only the website can open."
  accepted={["envelope", "tunnel", "box", "channel", "container"]}
  canonical="envelope (or tunnel, box, channel)"
  explanation="The sealed-envelope metaphor: anyone can see it pass, no one but the intended recipient can read it."
/>

<Inset kind="why" lang="en">
**Why TLS 1.3 cut the handshake to one RTT.** TLS 1.2 needed two round-trips: one to agree on cipher parameters, then another to exchange key material. TLS 1.3 moved the client's key material into the very first message — the ClientHello — so the server can compute the shared secret and start encrypting in the same pass. This saves a full network round-trip (80–300 ms on intercontinental links) on every cold connection.
</Inset>

</Explanation>

<KeyTakeaway>TLS builds a sealed channel providing confidentiality, integrity, and server authentication — the handshake negotiates the key once, then symmetric encryption protects all data, and TLS 1.3 does this in one RTT instead of two.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-what-tls-does-retrieval"
  lang="en"
  questions={[
    { q: "In one sentence: what would happen if you visited a banking site without TLS?", a: "Anyone on the same network (coffee-shop wifi, ISP, corporate proxy) could read your username, password, balance, and every action in plain text — TLS protects data in transit, not just passwords." },
    { q: "What three guarantees does TLS provide, and which one does the certificate serve?", a: "Confidentiality (nobody reads the data), integrity (nobody tampers undetected), and authentication (the server proves it owns the domain). The certificate serves authentication: it binds the server's public key to a domain name, signed by a trusted CA." },
    { q: "Why did TLS 1.2 need two RTTs and TLS 1.3 needs only one?", a: "TLS 1.2 separated parameter negotiation (first RTT) from key exchange (second RTT). TLS 1.3 put the client's ephemeral public key in the first message so the server can derive the shared secret immediately and respond with encrypted data in the same pass." },
  ]}
/>

<Recap lang="en">
TLS wraps every byte of application data in an encrypted envelope that anyone on the network can see passing but only the sender and receiver can read. It provides three guarantees: confidentiality (encryption), integrity (MAC/transcript hash), and authentication (the server certificate proves domain ownership to a trusted CA). Before TLS 1.3, building this channel cost two network round-trips; TLS 1.3 collapsed it to one by sending the client's key material in the opening message. The padlock icon means only the channel is sealed — not that the website itself is safe or the device is secure.
</Recap>
