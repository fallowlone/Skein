---
concepts:
- bbr
- cubic
- reno
- bbr-v3
- tcp-congestion
- production-observability
- rst
- mptcp
- ktls
- quic
- ss-command
- nstat
- tcp-congestion-control
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: senior
order: 6
prereqs:
- 05-syn-cookies-and-tfo
slug: 06-bbr-and-production-ops
sources:
- https://datatracker.ietf.org/doc/html/rfc9293
- https://datatracker.ietf.org/doc/html/rfc8684
- https://kernel.org/doc/html/latest/networking/tcp.html
- https://man7.org/linux/man-pages/man7/tcp.7.html
spiral:
- latency
status: ready
summary: BBR''''s bandwidth-RTT model sustains throughput on lossy paths where CUBIC
  collapses. Production ss/tcpdump/nstat output, RST semantics, MPTCP, kTLS, and TCP''''s
  relationship to QUIC.
title: BBR, production observability, and beyond TCP
track: networking
unit: 03-tcp-handshake
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import DesignPrompt from "~/components/pedagogy/DesignPrompt.astro";
import DebugLog from "~/components/pedagogy/DebugLog.astro";
import RFCQuiz from "~/components/pedagogy/RFCQuiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook>
A video-streaming service ships 4K segments over intercontinental cellular paths (150 ms RTT, 1% random loss). CUBIC throughput collapses to a fraction of the link's capacity. Switching to BBR sustains throughput near line rate on the same path. The difference is not bandwidth — it is a different answer to the question "what does a dropped packet mean?"
</Hook>

<Crux>BBR models the bottleneck via bandwidth and RTT, ignoring loss; CUBIC reacts to loss. Pick based on the path's loss profile.</Crux>

<Explanation>

## BBR vs CUBIC vs Reno

**Reno (classic):** halve cwnd on loss, additive increase per RTT. Simple, widely implemented.

**CUBIC (Linux default since 2.6.19):** a cubic-curve growth function — concave probe below the previous max, convex probe above it. Reduces ~0.7× on loss. Recovers throughput on high-BDP paths faster than Reno.

**BBR (Bottleneck Bandwidth and RTT):** abandons loss-based signalling entirely. It estimates the path's bottleneck bandwidth (via delivered-byte rate) and minimum RTT (via packet timestamps) directly, then paces sends to match. Loss is treated as ambiguous noise — it could be congestion or random drop. On 1% random loss, BBR sustains near-line-rate throughput; CUBIC settles into a small fraction.

**BBRv3 (Google's 2023 release):** fixes BBRv2's premature-probe and convergence bugs. Deployed across google.com, YouTube, Cloudflare, and Netflix edges. Not merged into mainline Linux as of early 2025; requires a custom kernel or third-party backport. Mainline Linux 6.x ships BBR 1.x.

**Practical guidance:** stick with CUBIC for general-purpose servers on standard kernels, standard datacenter and regional ISP paths where loss is rare and loss genuinely means congestion. Switch to BBR for cross-continental WAN, cellular, or satellite paths where 0.5–2% random loss is normal and CUBIC's cuts collapse throughput.

Set per-socket: `setsockopt(SOL_TCP, TCP_CONGESTION, "bbr")`. System-wide: `net.ipv4.tcp_congestion_control=bbr`.

<TraceScenario
  id="sr-trace-1"
  pieceSlug="06-bbr-and-production-ops"
  lang="en"
  scenario="Trace a BBR congestion episode on a lossy cellular path and explain why BBR sustains throughput while CUBIC collapses."
  steps={[
    { prompt: "A phone on cellular (1% random packet loss, 50 ms RTT, 20 Mbps bottleneck) runs CUBIC. What happens to cwnd?", reveal: "CUBIC treats every packet loss as a congestion signal and cuts cwnd × 0.7 on each loss event. On 1% loss this means CUBIC spends most of the time in recovery, never reaching the link's 20 Mbps capacity — throughput collapses to under 2 Mbps." },
    { prompt: "The same path switches to BBR. How does BBR's loss response differ?", reveal: "BBR does not treat loss as a congestion signal. It continuously estimates the bottleneck bandwidth (via ACK rate) and minimum RTT (via packet-send timestamps). It ignores packet loss as ambiguous — could be congestion or random drop. BBR paces sends at approximately bandwidth × RTT, filling the pipe without overflow." },
    { prompt: "The phone loses 1% of packets. Does BBR's estimate change?", reveal: "No. BBR's bandwidth and RTT estimates are independent of loss. A 1% drop does not alter the send rate. Throughput remains near 20 Mbps." },
    { prompt: "CUBIC is now competing with BBR on the same link. What is the trade-off?", reveal: "CUBIC is less aggressive than BBR on deep-buffered lossy paths, so CUBIC starves in the presence of BBR. BBR gets most of the bandwidth while CUBIC gets the remainder. On terrestrial links with shallow buffers CUBIC is competitive because loss is rarer and BBR offers no advantage." },
    { prompt: "What kernel setting enables BBR per-socket, and what is the caveat in 2026?", reveal: "setsockopt(SOL_TCP, TCP_CONGESTION, 'bbr') switches to BBR. BBRv3 is not in mainline Linux as of early 2025; standard-kernel deployments use BBR 1.x. Google, Cloudflare, and Netflix run BBRv3 on custom kernels." },
  ]}
/>

<TradeoffMatrix
  id="sr-tradeoff-1"
  pieceSlug="06-bbr-and-production-ops"
  lang="en"
  prompt="A video-streaming service ships 4K segments over long-RTT cellular networks (RTT often greater than 150 ms, sporadic loss 0.5–2%). Pick the TCP congestion control algorithm + tuning combination."
  options={[
    { name: "CUBIC + default Linux tunables", summary: "Out-of-the-box default.", justification: "CUBIC cuts cwnd aggressively on every loss; on a path with 0.5% random loss the throughput collapses to a fraction of the link capacity. This is the symptom every cellular streaming service hits." },
    { name: "BBR + IW=10 + tcp_notsent_lowat tuned", summary: "Bandwidth-and-RTT model + tuned headroom.", correct: true, justification: "BBR ignores random loss as a congestion signal and uses RTT + delivered bandwidth instead, sustaining throughput on lossy paths. tcp_notsent_lowat reduces socket-buffer bloat (better p99 latency). Initial Window of 10 MSS gets a video segment moving in the first RTT." },
    { name: "Reno + huge socket buffers", summary: "Classic + buffer everything.", justification: "Reno is even more loss-sensitive than CUBIC. Large buffers create bufferbloat — latency spikes that hurt adaptive bitrate switching. Wrong direction." },
    { name: "CUBIC + huge MSS via jumbo frames", summary: "More bytes per packet.", justification: "Jumbo frames break on most Internet paths (no end-to-end 9000-byte MTU between phones and CDN edges). Does not address the actual issue: loss sensitivity." },
  ]}
/>

## Production observability

`ss -tin` dumps live cwnd, RTT, RTT variance, retransmits, and backoff state per connection — no kernel overhead. Run it on any production host.

```bash
$ ss -tin state established | grep -A1 "dport 443"
# Output includes: cwnd:10 ssthresh:2147483647 rtt:42.8/8.5 acked:142 retrans:0/0
```

Key fields:
- `cwnd`: current congestion window in MSS
- `rtt/rttvar`: smoothed RTT and variance in milliseconds
- `retrans:sent/outstanding`: total retransmissions
- `ssthresh`: slow-start threshold (2147483647 = infinity = in slow start)

`ss -s` summarises socket counts by state — watch CLOSE-WAIT spikes.

`nstat` exposes counters from `/proc/net/netstat`: `RetransSegs`, `TCPSlowStartRetrans`, `TCPDSACKRecv`. For long-term monitoring, `tcp_diag` feeds Prometheus exporters (`node_exporter` exposes most metrics); the SLO-relevant metrics are retransmission rate, p95/p99 RTT, and the CLOSE-WAIT:ESTABLISHED ratio.

<DebugLog
  id="sr-debug-1"
  pieceSlug="06-bbr-and-production-ops"
  lang="en"
  title="ss output during an outage — diagnose the issue"
  output={`$ ss -tan state established | wc -l
12384
$ ss -tan state close-wait | wc -l
9821
$ ss -tan state time-wait | wc -l
1247
$ ss -s
Total: 12500
TCP:   23552 (estab 12384, closed 8920, orphaned 2, timewait 1247)
$ ps -p 1234 -o pid,stat,rss,vsz,cmd
  PID STAT  RSS    VSZ CMD
 1234 Ssl 8392000 12000000 /usr/bin/app-server`}
  outputLang="log"
  question="12k ESTABLISHED + 9.8k CLOSE-WAIT sockets and RSS is climbing. What is the bug and the fix?"
  hint="CLOSE-WAIT means the peer sent FIN but the application has not called close(). What does 9.8k of them imply?"
  answer="9.8k CLOSE-WAIT means 9.8k sockets where the remote peer finished sending and sent FIN, but the application has NOT called close(). The kernel cannot reclaim them. This is a socket leak: missing defer conn.Close() in Go, missing try-with-resources in Java, forgotten close() in the C error path. Each leaked CLOSE-WAIT pins kernel memory and a file descriptor. RSS confirms the leak. Fix: audit the connection-close path, add defer/try-with-resources, and watch CLOSE-WAIT count return to near zero."
/>

## RST semantics

A TCP RST is an abrupt connection close — no FIN exchange, no TIME-WAIT, the receiver drops connection state immediately. It occurs when:

- A packet arrives for a port no one is listening on.
- The application calls `close()` on a socket with unread data and `SO_LINGER` with lingertime=0.
- The peer sends garbage that violates the state machine.
- A stateful firewall decides the connection is idle.

**RST attacks:** an attacker who can guess sequence numbers within the receive window can forge an RST and tear down an established connection. RFC 5961 tightens the acceptable RST window. Long-lived idle connections (BGP sessions, SSH) are most vulnerable.

## MPTCP (RFC 8684)

Multipath TCP carries one logical connection across multiple paths (Wi-Fi + cellular, multi-NIC server). The MPTCP handshake adds an `MP_CAPABLE` option in SYN/SYN-ACK/ACK; if both ends support it the first sub-flow is established, and additional sub-flows can be opened on different interfaces via `MP_JOIN`. iOS uses MPTCP since iOS 7 for Siri. Linux 5.6+ ships RFC 8684. Limited adoption outside Apple because middleboxes that do not understand the option fall back to plain TCP.

## kTLS + TCP

kTLS (Linux 4.13+ TX, 4.17+ RX, NIC offload in 6.0+) moves symmetric TLS record encryption into the kernel via `setsockopt(SOL_TLS, ...)`. After the user-space TLS handshake completes, the kernel takes over record encryption; combined with `sendfile()`, files move from page-cache to NIC without entering user space. Netflix reports 8–29% CPU savings on static asset delivery. kTLS does not change TCP behaviour — congestion control, retransmits, window management all remain standard.

## TCP's relationship to QUIC

TCP is one layer in the stack; TLS sits directly on top; HTTP/1.1 and HTTP/2 ride on TLS. HTTP/3 is the exception — it runs on QUIC, which uses UDP and reinvents reliability and congestion control in user space. The reason: evolving TCP in kernel space proved too slow. The lessons of TCP — sequence numbers, ACKs, congestion control, slow start, fast retransmit — all reappear in QUIC, just at a different layer. Understanding TCP makes QUIC mechanistically transparent; the inverse is not true.

<RFCQuiz
  id="sr-rfc-1"
  pieceSlug="06-bbr-and-production-ops"
  lang="en"
  question="Which RFC specifies RACK-TLP, the modern loss detection algorithm used by Linux to avoid waiting for the RTO timer?"
  choices={[
    { rfc: "RFC 6298", title: "Computing the retransmission timer" },
    { rfc: "RFC 8985", title: "RACK-TLP: a time-based loss detection algorithm", correct: true },
    { rfc: "RFC 7413", title: "TCP Fast Open" },
    { rfc: "RFC 9293", title: "TCP base spec" },
  ]}
/>

<DesignPrompt
  id="sr-design-1"
  pieceSlug="06-bbr-and-production-ops"
  lang="en"
  prompt="Design the kernel-tunable set for a high-traffic API gateway terminating 200k HTTPS connections/second. Outbound traffic to ~50 backend pools, mostly short HTTP/1.1 requests with keep-alive."
  constraints={[
    "No external dependencies beyond Linux >= 6.0 sysctl.",
    "Resist SYN floods targeting the public-facing listener.",
    "Avoid TIME-WAIT exhaustion on outbound traffic to the backend pools.",
    "Keep latency p99 under 50 ms under steady-state load.",
  ]}
  canonical="Public listener: net.ipv4.tcp_syncookies=1 (default); tcp_max_syn_backlog=65535; somaxconn=65535; tcp_synack_retries=2 (faster failure under attack); tcp_fastopen=2 (server-side enabled). Outbound to backends: net.ipv4.ip_local_port_range=1024 65535 (~64k ephemeral ports); net.ipv4.tcp_tw_reuse=1 (safe ephemeral-port reuse); enable keep-alive in the application connection pool so each backend connection serves many requests. Congestion: tcp_congestion_control=bbr (lower latency than cubic under bursty load); IW=10 default. Buffers: tcp_notsent_lowat=131072 to cap socket-buffer bloat; rmem/wmem auto-scaling (rmem_max=16M, wmem_max=16M). Disable Nagle in the application via TCP_NODELAY on every socket. Measure with ss -tin; alert on retransmission rate >0.1% and p99 RTT >30 ms."
  keyPoints={[
    "tcp_syncookies + raised backlog defends against SYN flood without bricking legitimate traffic.",
    "tcp_tw_reuse on outbound, NOT tcp_tw_recycle (removed in 4.12, breaks NAT).",
    "Wide local port range + connection pooling are the real defence against TIME-WAIT exhaustion.",
    "BBR congestion control reduces p99 RTT and resists random loss better than CUBIC.",
    "TCP_NODELAY at the socket level eliminates Nagle/delayed-ACK stalls on small request payloads.",
    "Measure continuously; do not tune blind.",
  ]}
/>

<Inset kind="why" lang="en">
**Why QUIC runs over UDP instead of extending TCP.** Every TCP feature must be implemented in kernels worldwide — a process that takes decades due to the long tail of un-updated systems. QUIC runs in user space (or as a library), so features can be added and deployed with a browser or server update rather than a kernel upgrade. The price is reinventing everything TCP provides (reliability, ordering, congestion control) in user space, but the benefit is the ability to evolve at Internet speed rather than kernel-update speed. TCP is not going away — it carries the vast majority of Internet traffic and will for decades — but QUIC represents the acknowledgement that TCP's kernel-baked protocol ossification is a genuine engineering constraint.
</Inset>

</Explanation>

<KeyTakeaway>BBR models the bottleneck directly (bandwidth + min-RTT) and ignores loss, sustaining throughput on lossy paths where CUBIC collapses. Production monitoring: ss -tin for live cwnd/RTT, nstat for retransmit counters, Prometheus node_exporter for SLOs. RST tears down connections without TIME-WAIT; QUIC reinvents TCP in user space to escape protocol ossification.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-bbr-and-production-ops-retrieval"
  lang="en"
  questions={[
    { q: "Explain why BBR sustains throughput on a path with 1% random loss where CUBIC collapses.", a: "CUBIC treats every loss as a congestion signal and cuts the congestion window aggressively (×0.7), then probes back slowly. On a path with 1% random loss, CUBIC spends most of its time in recovery — the loss rate is high enough that CUBIC never reaches the path's actual capacity; throughput settles at a small fraction of the bottleneck. BBR ignores loss as a congestion signal. It estimates bottleneck bandwidth by measuring delivered bytes over time and minimum RTT by tracking the lowest observed RTT in a window, then paces sends to fill but not overflow the bottleneck. Random packet drops do not perturb the estimate. Result: BBR sustains throughput close to path capacity on lossy paths where CUBIC cannot." },
    { q: "What does ss -tin tell you about a live TCP connection that netstat does not?", a: "ss uses the kernel's tcp_diag netlink interface and exposes per-connection live state: cwnd (congestion window in MSS), ssthresh, smoothed RTT and RTT variance (rtt/rttvar in ms), bytes sent/acked, retransmission counts (retrans:sent/outstanding), send/receive buffer usage, and socket memory. netstat shows only connection tuple and state. ss -tin gives you the data you need to diagnose congestion collapse, buffer bloat, or a retransmission storm without installing additional tooling." },
    { q: "What is the relationship between TCP and QUIC, and why did QUIC not simply extend TCP?", a: "QUIC reinvents the reliability, ordering, and congestion-control mechanisms of TCP in user space, running over UDP. It was designed this way because extending TCP requires changes to kernels worldwide — a process that takes 5–15 years due to the long tail of un-updated systems. UDP passes through virtually all middleboxes unchanged, so QUIC can be deployed as a library update in a browser or server. The cost is duplicating TCP's mechanisms; the benefit is the ability to iterate at application speed. HTTP/3 runs on QUIC; HTTP/1.1 and HTTP/2 run on TCP. Understanding TCP's sequence numbers, ACKs, congestion control, and state machine makes QUIC mechanistically transparent." },
  ]}
/>

<Recap lang="en">
BBR estimates the network's bottleneck bandwidth and minimum RTT directly, ignoring loss as a congestion signal. CUBIC cuts the window on every loss event — on a path with 1% random loss, CUBIC settles at a fraction of capacity while BBR sustains near line rate. BBRv3 is deployed at Google, Cloudflare, and Netflix but is not yet in mainline Linux (early 2025). The production toolkit: ss -tin for live per-connection cwnd, RTT, and retransmit state; nstat for kernel counters; node_exporter for Prometheus SLOs. RST closes connections immediately without TIME-WAIT, enabling injection attacks on long-lived sessions. MPTCP spreads one connection across multiple network paths. kTLS moves TLS record encryption into the kernel for zero-copy static serving. QUIC runs TCP-like reliability in user space over UDP, decoupling protocol evolution from kernel upgrade cycles.
</Recap>
