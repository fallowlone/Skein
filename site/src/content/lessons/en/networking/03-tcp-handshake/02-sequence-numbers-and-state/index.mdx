---
concepts:
- isn
- sequence-number
- tcp-state-machine
- established
- syn-sent
- syn-recv
- fin
- time-wait
- close-wait
- rtt-cost
deepensInto:
- 03-flow-and-congestion-control
estMin: 14
lang: en
lessonType: topic
level: middle
order: 2
prereqs:
- 01-the-three-way-handshake
slug: 02-sequence-numbers-and-state
sources:
- https://datatracker.ietf.org/doc/html/rfc9293
- https://datatracker.ietf.org/doc/html/rfc6528
- https://hpbn.co/building-blocks-of-tcp/
spiral:
- statefulness
- latency
status: ready
summary: How ISN randomisation hardens TCP against injection attacks, and the full
  state machine from CLOSED to TIME-WAIT.
title: Sequence numbers and connection state
track: networking
unit: 03-tcp-handshake
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
You know the three steps of the handshake. Now look at what those steps actually exchange: random 32-bit counters whose specific values are not arbitrary — they are a security mechanism, a timing mechanism, and a state machine checkpoint all at once.
</Hook>

<Crux>ISN randomisation blocks TCP injection attacks; the state machine governs what both sides can do from CLOSED to TIME-WAIT.</Crux>

<Explanation>

## The three-step dance in detail

Bea sends `SYN seq=X`. She moves to state `SYN-SENT`. Sven receives it, allocates a buffer for the incoming direction, moves to state `SYN-RECV`, and replies `SYN-ACK seq=Y ack=X+1`. The `ack=X+1` means "I saw your sequence number X; the next byte I expect from you is X+1." (The SYN flag itself consumes one sequence number even though no data byte is sent.) Bea then sends `ACK seq=X+1 ack=Y+1` and moves to state `ESTABLISHED`. Sven receives it, moves to state `ESTABLISHED`, and the connection is live.

<FadedExample
  client:load
  id="seq-numbers"
  pieceSlug="02-sequence-numbers-and-state"
  lang="en"
  title="Sequence number negotiation"
  steps={{
    solved: (
      <div class="space-y-3">
        <p><strong>The handshake in sequence numbers:</strong></p>
        <ul class="list-disc list-inside space-y-1 text-sm">
          <li>Bea sends: <code class="bg-purple-100 px-1">SYN seq=42_000_000</code></li>
          <li>Sven replies: <code class="bg-blue-100 px-1">SYN-ACK seq=99_000_000 ack=42_000_001</code></li>
          <li>Bea confirms: <code class="bg-purple-100 px-1">ACK seq=42_000_001 ack=99_000_001</code></li>
        </ul>
        <p class="text-xs text-gray-600 mt-3"><strong>Why <code>ack = peer_seq + 1</code>?</strong> The SYN flag itself consumes one sequence number. Even though no data byte is sent, the receiver expects the next byte at seq+1. Same logic applies to FIN flags during connection teardown.</p>
        <p class="text-xs text-gray-600"><strong>After handshake:</strong> Bea's first data byte uses seq=42_000_001. If she sends 200 bytes, the next segment starts at seq=42_000_201. Sven acknowledges with <code>ack=42_000_201</code>, confirming all 200 bytes arrived.</p>
      </div>
    ),
    semi: {
      prompt: "Bea sends SYN seq=15_000_000. What is the ACK number Sven includes in his SYN-ACK?",
      blanks: [
        {
          id: "ack1",
          expected: "15000001",
          placeholder: "Enter the ACK number (e.g., 15000001)",
        },
      ],
    },
    blank: {
      prompt: "Now Sven's SYN-ACK carries seq=88_000_000. Bea must acknowledge Sven's sequence number. What does Bea's final ACK packet carry as its ACK field?",
      reveal: (
        <div class="space-y-2">
          <p><strong>Answer: ack=88_000_001</strong></p>
          <p class="text-sm">The SYN flag in Sven's SYN-ACK packet consumes one sequence number, so Bea acknowledges the next expected byte at 88_000_001.</p>
        </div>
      ),
    },
  }}
  misconceptions={{
    "ack1:15000000": "Close — but the SYN flag itself consumes one sequence number, so the ACK is one greater.",
    "ack1:15000002": "Off by one. The SYN consumes exactly one sequence number, not two.",
  }}
/>

## Initial Sequence Numbers (ISN) — why random?

RFC 6528 specifies that ISN is computed as:

```
ISN = M + F(saddr, sport, daddr, dport, secret_key)
```

where `M` increments once per ~4 µs (making it hard to guess from the outside), and `F` is a hash of the 4-tuple and a secret key. Older implementations used a simple counter, which attackers exploited: an attacker could guess the next ISN, forge a packet with the right sequence number, and inject commands into an established connection. Modern ISNs are cryptographically random per connection, making blind injection impossible without eavesdropping.

## The 1-RTT cost

Bea sends SYN, waits for SYN-ACK (1 RTT), sends ACK, and only then can she send an HTTP request. If the RTT is 100 ms (across a continent), 100 ms of latency is baked into the connection before the first application byte lands. This RTT matters because it decides the cadence of the entire application.

<NumbersCard
  id="card-rtt-costs"
  title="Handshake RTT costs"
  data-lesson-visual
  stats={[
    { label: "LAN RTT (cold)", value: "0.1–1 ms" },
    { label: "Regional RTT", value: "20–40 ms" },
    { label: "Intercontinental RTT", value: "100–300 ms" },
    { label: "TFO cold start", value: "1 RTT + data" },
    { label: "TFO warm start", value: "0 RTT (data in SYN)" },
    { label: "HTTP/2 benefit", value: "1 connection, many streams" },
  ]}
/>

## Connection close: FIN and TIME-WAIT

Closing is the mirror of opening: each side independently sends FIN to signal "I have no more data," and the peer ACKs. Connections sit in TIME-WAIT for 2×MSL (~120 s typical) so any straggler retransmission can land harmlessly instead of being mistaken for a new connection's data. The side that initiates close pays the TIME-WAIT cost; on busy clients this is fine, on busy servers it can become a problem — see the lesson on SYN cookies and TFO for TIME-WAIT exhaustion at scale.

## The state machine

The states you will see in `ss -tan` output:

| State | Meaning |
|-------|---------|
| `LISTEN` | Server waiting for SYN |
| `SYN-SENT` | Client has sent SYN |
| `SYN-RECV` | Server has sent SYN-ACK |
| `ESTABLISHED` | Handshake done, normal traffic |
| `FIN-WAIT-1` | Close initiator sent FIN |
| `FIN-WAIT-2` | Received ACK of own FIN |
| `CLOSE-WAIT` | Received FIN from peer, must call close() |
| `LAST-ACK` | Sent FIN, awaiting final ACK |
| `TIME-WAIT` | Post-close cooldown (2×MSL) |
| `CLOSED` | No connection |

An accumulation of `CLOSE-WAIT` on a server usually means the application is not calling `close()` on accepted sockets — a resource leak.

<TraceScenario
  id="mid-trace-1"
  pieceSlug="02-sequence-numbers-and-state"
  lang="en"
  scenario="Trace a successful TCP three-way handshake and the first data byte."
  steps={[
    { prompt: "Client sends SYN. What state is the client in?", reveal: "SYN-SENT. The client allocates a half-open connection structure and starts the SYN retransmission timer." },
    { prompt: "Server receives SYN. What does it allocate and what does it reply?", reveal: "Server allocates a request_sock (or generates a SYN cookie if backlog is full), moves to SYN-RECV, and replies SYN-ACK with its own ISN plus ack = client_isn + 1." },
    { prompt: "Client receives SYN-ACK. What does the ACK number tell the client?", reveal: "ack = client_isn + 1 confirms the server received the client's SYN. Client moves to ESTABLISHED." },
    { prompt: "Client sends ACK. What does the server do?", reveal: "Server validates the ACK, allocates the full socket (or validates the SYN cookie), and moves to ESTABLISHED. The handshake is complete." },
    { prompt: "Client's first data byte. What sequence number does it carry?", reveal: "client_isn + 1. The SYN consumed one sequence number, so the next byte sent uses the value the server already acknowledged as expected." },
  ]}
/>

<TraceScenario
  id="mid-trace-2"
  pieceSlug="02-sequence-numbers-and-state"
  lang="en"
  scenario="Trace what happens when the SYN packet is lost on the way to the server."
  steps={[
    { prompt: "Client sends SYN. Server never sees it. What does the client do?", reveal: "Client's SYN retransmission timer fires (default ~1s, doubled on each retry up to 5-6 attempts). It resends SYN with the same ISN." },
    { prompt: "After two more retries the SYN still fails. What happens?", reveal: "Linux retries up to tcp_syn_retries times (default 6, ~127s total). After that, connect() returns ETIMEDOUT to the application." },
    { prompt: "What if the SYN reached the server but SYN-ACK was lost?", reveal: "Client still retransmits SYN; server treats the duplicate SYN as a retransmit and re-sends its SYN-ACK with the same server ISN." },
    { prompt: "Why is this safe even on a flaky network?", reveal: "Because both ISNs are fixed for the handshake — duplicates are idempotent. The first ACK from either side that arrives completes the handshake." },
  ]}
/>

<DragOrder
  id="mid-drag-1"
  pieceSlug="02-sequence-numbers-and-state"
  lang="en"
  prompt="Order the TCP state transitions for a client opening then closing a connection:"
  items={[
    "CLOSED",
    "SYN-SENT (after send(SYN))",
    "ESTABLISHED (after receive(SYN-ACK) and send(ACK))",
    "FIN-WAIT-1 (after send(FIN))",
    "FIN-WAIT-2 (after receive(ACK of FIN))",
    "TIME-WAIT (after receive(FIN) and send(ACK))",
    "CLOSED (after 2×MSL expires)",
  ]}
/>

<Quiz
  id="mid-quiz-1"
  pieceSlug="02-sequence-numbers-and-state"
  lang="en"
  question="Why is the SYN flag treated as consuming one sequence number even though no data byte is sent?"
  choices={[
    { label: "TCP increments sequence by 1 for every packet", misconception: "TCP increments by bytes sent, not by packets." },
    { label: "Because the SYN must be individually acknowledgeable — without consuming a number, retransmits would be ambiguous", correct: true },
    { label: "It is a leftover from older protocol versions", misconception: "The behaviour is designed, not legacy. It enables reliable handshake retransmission." },
    { label: "To pad the packet size", misconception: "TCP headers handle padding separately; sequence semantics are about reliability." },
  ]}
/>

<Inset kind="why" lang="en">
**Why older ISN implementations were dangerous.** Before RFC 6528, many TCP stacks incremented the ISN by a fixed amount per second (~250,000 per second on BSD-derived systems). An attacker who could observe the timing of one connection could predict the next ISN with reasonable accuracy and forge an ACK or data segment — the classic TCP sequence number prediction attack. Randomised ISNs, combined with cryptographic mixing of the 4-tuple, make this attack computationally infeasible without being able to observe the actual SYN-ACK.
</Inset>

</Explanation>

<FlowDiagram
  label="TCP state machine for one connection: CLOSED to SYN-SENT to ESTABLISHED on open, then FIN-WAIT-1 to FIN-WAIT-2 to TIME-WAIT to CLOSED on close"
  hue="var(--d-network)"
  perRow={7}
  nodes={[
    { id: "closed", label: "CLOSED" },
    { id: "synsent", label: "SYN-SENT" },
    { id: "estab", label: "ESTABLISHED" },
    { id: "fw1", label: "FIN-WAIT-1" },
    { id: "fw2", label: "FIN-WAIT-2" },
    { id: "tw", label: "TIME-WAIT" },
    { id: "closed2", label: "CLOSED" },
  ]}
  edges={[
    { from: "closed", to: "synsent", label: "send SYN" },
    { from: "synsent", to: "estab", label: "recv SYN-ACK / send ACK" },
    { from: "estab", to: "fw1", label: "send FIN" },
    { from: "fw1", to: "fw2", label: "recv ACK" },
    { from: "fw2", to: "tw", label: "recv FIN / send ACK" },
    { from: "tw", to: "closed2", label: "2×MSL expires" },
  ]}
  caption="Each transition is triggered by sending or receiving a specific flag. The active-closer path passes through TIME-WAIT, holding the four-tuple for 2×MSL so straggler retransmissions land harmlessly."
/>

<KeyTakeaway>The handshake exchanges cryptographically random ISNs (RFC 6528) to prevent injection attacks; the 11-state machine tracks both sides from CLOSED through ESTABLISHED to TIME-WAIT, with each transition triggered by sending or receiving a specific flag.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-sequence-numbers-and-state-retrieval"
  lang="en"
  questions={[
    { q: "Why are Initial Sequence Numbers (ISNs) randomised rather than starting from zero or incrementing globally?", a: "Older stacks used predictable counters, letting attackers guess the next ISN and forge TCP segments (injection or RST attacks). RFC 6528 requires ISN = M + F(saddr,sport,daddr,dport,secret_key), where M is a time-based counter and F is a cryptographic hash of the 4-tuple and a secret key. This makes guessing an ISN infeasible without eavesdropping on the actual handshake." },
    { q: "What does TIME-WAIT mean, how long does it last, and why does it exist?", a: "TIME-WAIT is the state a TCP endpoint enters after sending the final ACK of a connection close. It lasts 2×MSL (~120s typical). It exists so that any delayed retransmissions of the FIN can arrive and be absorbed harmlessly rather than being mistaken for a new connection's data on the same four-tuple. Without TIME-WAIT, a fast-reuse of the same (src-IP, src-port, dst-IP, dst-port) could corrupt the new connection with stale data from the old one." },
    { q: "What does a large number of CLOSE-WAIT sockets on a server almost always indicate?", a: "The application is not calling close() on accepted sockets after the remote peer has sent FIN. The kernel receives the peer's FIN and moves to CLOSE-WAIT, waiting for the application to call close(). If the application never does, the socket stays in CLOSE-WAIT indefinitely, leaking file descriptors and kernel memory. Common causes: missing defer conn.Close() in Go, missing try-with-resources in Java, missing close() in the error path in C." },
  ]}
/>

<Recap lang="en">
The three-way handshake exchanges Initial Sequence Numbers (ISNs) in both directions. ISNs are computed as a time-based counter plus a cryptographic hash of the connection 4-tuple (RFC 6528), making injection attacks infeasible. The SYN flag consumes one sequence number so the exchange is individually acknowledgeable. Once ESTABLISHED, both sides track the connection through an 11-state machine: LISTEN, SYN-SENT, SYN-RECV, ESTABLISHED, FIN-WAIT-1, FIN-WAIT-2, CLOSE-WAIT, CLOSING, LAST-ACK, TIME-WAIT, CLOSED. TIME-WAIT persists for 2×MSL (~120s) to absorb delayed retransmissions. CLOSE-WAIT accumulation is the most common socket-leak symptom — the application is not calling close() on accepted sockets.
</Recap>
