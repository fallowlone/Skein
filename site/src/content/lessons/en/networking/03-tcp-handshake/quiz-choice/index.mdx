---
concepts:
- handshake-rtt
- isn-randomisation
- syn-cookies
- congestion-control
- nagle-delayed-ack
- time-wait-exhaustion
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://datatracker.ietf.org/doc/html/rfc9293
- https://datatracker.ietf.org/doc/html/rfc6528
- https://datatracker.ietf.org/doc/html/rfc7413
status: ready
summary: "Multiple-choice synthesis across the TCP-handshake unit — RTT cost, ISN randomisation, SYN cookies, congestion control, Nagle stalls, and TIME-WAIT exhaustion."
title: 'TCP handshake: multiple-choice review'
track: networking
unit: 03-tcp-handshake
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one mirrors a call you make in a real incident — not a flag to name, but a tradeoff to weigh while latency or memory is on fire.
</Hook>

<Goal lang="en">
Confirm you can connect the handshake's RTT cost, ISN security, flood defence, congestion control, and the canonical production pathologies — the synthesis the individual lessons built toward.
</Goal>

<Quiz
  id="03-tcp-handshake-choice-1"
  lessonSlug="03-tcp-handshake"
  lang="en"
  question="A REST client opens a fresh TCP connection per request to an origin 140 ms RTT away, then layers TLS 1.3. Why does each call feel slow even though the payload is tiny, and what is the structural fix?"
  choices={[
    { label: "The handshake costs 1 RTT before any data, and TLS adds another — ~280 ms of round-trips are baked in before the first byte. Fix: a connection pool with keep-alive so one handshake amortises over many requests.", correct: true },
    { label: "The server is CPU-bound on the tiny payload.", misconception: "A tiny payload is not a CPU problem. The latency is the chain of round-trips (TCP handshake + TLS) paid per connection, which is exactly what keep-alive amortises." },
    { label: "TCP slow start caps the first response, so raise the initial window.", misconception: "A tiny response fits inside the default IW=10 (~14.6 KB) in the first RTT; slow start is not the bottleneck. The cost is the per-connection handshake round-trips." },
    { label: "Nagle is holding the request — set TCP_NODELAY.", misconception: "Nagle stalls pipelined small writes on one connection, not the cold-start round-trips of opening a brand-new connection per request. The fix here is reuse, not TCP_NODELAY." },
  ]}
/>

<Quiz
  id="03-tcp-handshake-choice-2"
  lessonSlug="03-tcp-handshake"
  lang="en"
  question="Why does RFC 6528 compute the Initial Sequence Number as a time-based counter plus a keyed hash of the 4-tuple, rather than starting from zero?"
  choices={[
    { label: "A predictable ISN lets an off-path attacker guess the next sequence number and forge a segment into an established connection. Per-connection cryptographic randomisation makes blind injection infeasible without eavesdropping.", correct: true },
    { label: "Random ISNs encrypt the connection's payload.", misconception: "ISN randomisation is not encryption — TCP carries cleartext. It only stops sequence-number prediction; confidentiality is TLS's job, a layer above." },
    { label: "It load-balances connections across CPU cores.", misconception: "ISN selection has nothing to do with core steering (that is RSS/RPS on the NIC). RFC 6528 is purely an anti-injection defence." },
    { label: "It lets both sides skip the third ACK.", misconception: "Every connection still needs all three steps. The ACK proves liveness regardless of how the ISN is chosen; randomisation changes the value, not the step count." },
  ]}
/>

<Quiz
  id="03-tcp-handshake-choice-3"
  lessonSlug="03-tcp-handshake"
  lang="en"
  question="During a SYN flood your public listener stays up with tcp_syncookies=1, but legitimate clients on a 200 ms transcontinental path report throughput stuck near a trickle. Why?"
  choices={[
    { label: "A SYN cookie encodes only the MSS index in the ISN; Window Scaling, SACK, and Timestamps offered in the SYN are silently dropped for cookie-validated connections. Without scaling the window caps at 64 KiB, throttling high-BDP paths.", correct: true },
    { label: "SYN cookies allocate so much memory that the kernel throttles bandwidth.", misconception: "Cookies trade CPU for memory — they allocate no request_sock at all. The throughput hit comes from losing Window Scaling/SACK/Timestamps in the cookie, not from memory pressure." },
    { label: "The flood saturates the uplink, so real clients get the leftover bandwidth.", misconception: "A SYN flood is tiny packets that exhaust the half-open queue, not link bandwidth. The per-connection throughput cap comes from the dropped scaling option on cookie-validated handshakes." },
    { label: "SYN cookies force every connection onto Reno congestion control.", misconception: "Cookies do not change the congestion-control algorithm. They drop TCP options carried in the SYN; the 64 KiB window cap is the throughput limiter." },
  ]}
/>

<Quiz
  id="03-tcp-handshake-choice-4"
  lessonSlug="03-tcp-handshake"
  lang="en"
  question="A 4K-video service over cellular (150 ms RTT, ~1% random loss) sees CUBIC throughput collapse to a fraction of link capacity. Switching to BBR restores near-line-rate. What is the underlying reason?"
  choices={[
    { label: "CUBIC treats every loss as congestion and cuts cwnd (~0.7x), so at 1% random loss it lives in recovery and never reaches capacity. BBR models bottleneck bandwidth and min-RTT directly and ignores loss as ambiguous, so random drops do not perturb its send rate.", correct: true },
    { label: "BBR sends a bigger initial window, so it is just faster from the start.", misconception: "Both can start at IW=10. The durable difference is BBR's loss-agnostic bandwidth/RTT model versus CUBIC's loss-triggered window cuts — not a one-time initial-window edge." },
    { label: "BBR disables retransmission, so lost packets cost nothing.", misconception: "BBR still retransmits lost data — reliability is unchanged. It simply does not interpret loss as a congestion signal, so it does not collapse its rate on random drops." },
    { label: "The cellular link is faster under BBR because BBR uses jumbo frames.", misconception: "Congestion control does not change MSS, and jumbo frames do not survive cellular paths. The win is purely the loss-versus-congestion modelling difference." },
  ]}
/>

<Quiz
  id="03-tcp-handshake-choice-5"
  lessonSlug="03-tcp-handshake"
  lang="en"
  question="A Redis client on the same datacenter host shows a steady ~40–200 ms p99 on small pipelined commands, with no loss and no CPU pressure. Root cause?"
  choices={[
    { label: "Nagle holds the second small write until the first is ACKed, while the peer's delayed-ACK holds that ACK up to 40 ms — each side waits for the other. Fix: TCP_NODELAY on the socket.", correct: true },
    { label: "The connection is hitting slow start on every command.", misconception: "An established connection is past slow start, and tiny commands do not stress cwnd. The stall is the Nagle plus delayed-ACK deadlock, cured by TCP_NODELAY." },
    { label: "TIME-WAIT sockets are slowing new commands.", misconception: "TIME-WAIT affects opening new connections (port reuse), not the latency of commands on an already-established socket. The 40 ms quantum points squarely at delayed ACK." },
    { label: "The server is delaying because the ISN was not randomised.", misconception: "ISN choice has no bearing on per-command latency. The 40 ms figure is the delayed-ACK timer interacting with Nagle." },
  ]}
/>

<Quiz
  id="03-tcp-handshake-choice-6"
  lessonSlug="03-tcp-handshake"
  lang="en"
  question="A load balancer returns EADDRNOTAVAIL on outbound connect() to one backend; ss shows ~30k TIME-WAIT sockets to that origin. A teammate proposes lowering MSL to drain TIME-WAIT faster. What is the correct read?"
  choices={[
    { label: "Lowering MSL risks accepting a stale FIN or segment as new-connection data on a reused 4-tuple — silent corruption. Use tcp_tw_reuse (safe via Timestamps/PAWS), widen ip_local_port_range, and structurally fix it with a keep-alive connection pool.", correct: true },
    { label: "Lowering MSL is the standard fix and is perfectly safe.", misconception: "TIME-WAIT exists to absorb delayed retransmissions on the same 4-tuple; shortening it invites stale-segment corruption. The safe levers are tcp_tw_reuse plus connection pooling." },
    { label: "Enable tcp_tw_recycle for fast recycling.", misconception: "tcp_tw_recycle was removed in Linux 4.12 because it dropped legitimate SYNs from clients behind NAT. Never enable it; use tcp_tw_reuse instead." },
    { label: "Turn on SYN cookies to free the ports.", misconception: "SYN cookies defend the inbound half-open SYN queue; they do nothing for outbound ephemeral-port exhaustion in TIME-WAIT. Wrong layer entirely." },
  ]}
/>

<Recap lang="en">
The unit's through-line is one decision chain: the handshake costs a round-trip you amortise with connection reuse, ISN randomisation secures it, SYN cookies defend it under flood (at the cost of dropped options), congestion control decides how fast it fills the pipe (CUBIC reacts to loss, BBR models bandwidth and RTT), and the canonical production failures — Nagle plus delayed-ACK stalls, CLOSE-WAIT leaks, TIME-WAIT exhaustion — each have one correct lever and several plausible wrong ones. Knowing which lever, and why the others fail, is the synthesis.
</Recap>
