---
concepts:
- sliding-window
- flow-control
- mss
- window-scaling
- sack
- slow-start
- congestion-avoidance
- cwnd
- ssthresh
- rto
- srtt
- rack-tlp
- cubic
- reno
deepensInto:
- 06-bbr-and-production-ops
estMin: 16
lang: en
lessonType: topic
level: middle
order: 3
prereqs:
- 02-sequence-numbers-and-state
slug: 03-flow-and-congestion-control
sources:
- https://datatracker.ietf.org/doc/html/rfc9293
- https://datatracker.ietf.org/doc/html/rfc6298
- https://datatracker.ietf.org/doc/html/rfc8985
- https://datatracker.ietf.org/doc/html/rfc2018
- https://datatracker.ietf.org/doc/html/rfc7323
spiral:
- latency
status: ready
summary: Sliding windows let the receiver pace the sender; slow start and CUBIC let
  TCP discover the network''''s capacity without overloading it.
title: Flow control and congestion control
track: networking
unit: 03-tcp-handshake
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A fresh TCP connection between London and Sydney has no idea how fast the link is. It starts cautiously, doubling its send rate every round-trip until it hits a limit — then it backs off and tries again. This dance between sender and network is how TCP fills a 10 Gbps pipe or politely shares a 1 Mbps one.
</Hook>

<Crux>Sliding windows let the receiver pace the sender; slow start and congestion avoidance discover network capacity without overloading it.</Crux>

<Explanation>

## Sliding window and flow control

Once established, every TCP segment carries a **window size** — the number of bytes the receiver is willing to accept beyond the last acknowledged byte. The sender keeps a sliding window of unacknowledged bytes in flight, bounded by the minimum of the receiver's advertised window and its own congestion window. As ACKs arrive the window slides forward, allowing new bytes. If the receiver's application is slow to drain the socket buffer, the window shrinks; if the buffer fills entirely the receiver advertises `window=0` and the sender pauses. This is end-to-end flow control: the receiver dictates the pace, the sender obeys.

## MSS, window scaling, and SACK

Three options negotiated during the SYN/SYN-ACK exchange significantly affect throughput:

**Maximum Segment Size (MSS):** the largest TCP payload the stack can handle without IP fragmentation. Typical: 1460 bytes on Ethernet (1500 MTU minus 20 IP + 20 TCP headers), or 1220 bytes on tunnelled networks.

**Window Scaling (RFC 7323):** the 16-bit window field is multiplied by 2^scale, allowing windows up to 1 GiB. Without this, a 64 KiB window cap on a 100 ms RTT link limits throughput to ~5 Mbps regardless of actual link speed (bandwidth-delay product = 64 KiB / 0.1s ≈ 640 KiB/s). Essential for high-bandwidth long-RTT paths.

**Selective Acknowledgements (SACK, RFC 2018):** the receiver lists exact ranges of bytes received past a gap, so the sender retransmits only the missing pieces rather than everything after the gap. On a lossy long-RTT path SACK can double effective throughput.

<NumbersCard
  id="card-window"
  title="Window and throughput numbers"
  data-lesson-visual
  stats={[
    { label: "Ethernet MSS", value: "1460 bytes (1500 MTU)" },
    { label: "16-bit window max", value: "64 KiB (capped without scaling)" },
    { label: "Window scale max", value: "1 GiB (2^30)" },
    { label: "Linux default IW", value: "10 MSS (~14.6 KB)" },
    { label: "Bandwidth × RTT (BDP)", value: "100 Mbps × 100 ms = 1.25 MB" },
    { label: "BDP sets min window needed", value: "to fill a high-BDP path" },
  ]}
/>

## Retransmit timer math (RFC 6298)

If a segment goes un-ACKed within the retransmission timeout (RTO), the sender resends it. RFC 6298 specifies:

```
SRTT ← (1 − 1/8)·SRTT + (1/8)·new_RTT
RTTVAR ← (1 − 1/4)·RTTVAR + (1/4)·|SRTT − new_RTT|
RTO = SRTT + max(G, 4·RTTVAR)
```

Where SRTT is the smoothed RTT and RTTVAR is RTT variance (EWMAs). The first RTO defaults to 1s; after a timeout it doubles (exponential backoff) until either the segment arrives or the connection gives up. Karn's algorithm forbids sampling RTT from retransmitted segments because the responding ACK is ambiguous.

Modern Linux uses **RACK-TLP (RFC 8985)** for faster loss detection: RACK declares a segment lost when a later segment was ACKed and a reorder-window has expired — no need to wait for the RTO timer. TLP (Tail Loss Probe) re-sends the last unacknowledged segment one RTT after the last send to avoid hanging on the final packet of a flight.

<Quiz
  id="mid-quiz-1"
  pieceSlug="03-flow-and-congestion-control"
  lang="en"
  question="What does the receiver's advertised window in the TCP header mean?"
  choices={[
    { label: "The number of bytes the receiver has already received", misconception: "That value is the ACK number, not the window." },
    { label: "The number of additional bytes the receiver is willing to accept beyond the last ACKed byte", correct: true },
    { label: "The number of segments the receiver expects per RTT", misconception: "TCP windows are measured in bytes, not segments." },
    { label: "The maximum segment size", misconception: "MSS is a separate option negotiated in SYN, not the window field." },
  ]}
/>

## Slow start and congestion avoidance

A fresh connection has no idea how fast the network is. **Slow start** opens the congestion window exponentially (1, 2, 4, 8, ... MSS per RTT) until either a loss occurs or the slow-start threshold (ssthresh) is reached. After that, **congestion avoidance** grows the window linearly (~1 MSS per RTT for Reno/CUBIC).

On loss, the algorithm differs by variant:
- **Reno:** halves the window, then linear increase.
- **CUBIC (Linux default since 2.6.19):** reduces less aggressively then probes back with a cubic curve — faster recovery on high-BDP paths.
- **BBR:** ignores loss entirely as a congestion signal; uses RTT + delivered-bytes measurements instead (covered in the BBR lesson).

<DragOrder
  id="mid-drag-1"
  pieceSlug="03-flow-and-congestion-control"
  lang="en"
  prompt="Order the sender's congestion-window growth in TCP slow start:"
  items={[
    "Initial: cwnd = 10 MSS (Linux default IW=10)",
    "After 1 RTT and ACKs: cwnd doubles to 20 MSS",
    "After 2 RTT: cwnd doubles to 40 MSS",
    "Continues exponential growth until ssthresh or loss",
    "On reaching ssthresh: enter congestion avoidance (linear +1 MSS/RTT)",
    "On packet loss: reduce cwnd per the congestion-control variant",
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="03-flow-and-congestion-control"
  lang="en"
  question="Why is exponential backoff used for retransmissions instead of a constant timer?"
  choices={[
    { label: "It makes the math simpler in the kernel", misconception: "It is about congestion behaviour, not implementation simplicity." },
    { label: "To reduce load on a congested or broken network — each retry waits longer so a partial outage does not get hammered", correct: true },
    { label: "Because RFC 793 said so without giving a reason", misconception: "RFC 6298 explains the engineering motivation explicitly." },
    { label: "To allow time for human intervention", misconception: "The mechanism is automated and runs at millisecond scale." },
  ]}
/>

<TraceScenario
  id="mid-trace-1"
  pieceSlug="03-flow-and-congestion-control"
  lang="en"
  scenario="Trace a SACK recovery episode after one packet is lost in a window of 10 segments."
  steps={[
    { prompt: "Segments 1-10 are sent. Segment 5 is dropped. Segments 6-10 arrive. What does the receiver report?", reveal: "The receiver sends ACK=5 (acknowledging segments 1-4) plus SACK blocks indicating segments 6-10 have arrived. The sender now knows exactly what is missing." },
    { prompt: "Without SACK, what would the sender have to retransmit?", reveal: "Without SACK, the sender sees three duplicate ACKs (for segment 4) and fast-retransmits segment 5, but does not know if segments 6-10 arrived. It may retransmit all of them (go-back-N behaviour). With SACK it retransmits only segment 5." },
    { prompt: "Why does this matter on a 200 ms RTT path?", reveal: "On a 200 ms path, each unnecessary retransmission costs 200 ms of wasted time. With SACK, one lost packet costs one extra RTT regardless of window size. Without SACK, one lost packet in a 10-segment window may cost the time to retransmit 9 segments unnecessarily." },
  ]}
/>

<Inset kind="why" lang="en">
**Why slow start starts fast.** The name is misleading: Linux 3.0+ (RFC 6928) sets the initial congestion window (IW) to 10 MSS (~14.6 KB), not 1 MSS. This means the first burst of data is already 10 segments, chosen to match the typical HTTP response size for small API calls. Slow start only looks slow compared to the eventual bandwidth; against the classic IW=1 it is a 10x improvement for the first RTT.
</Inset>

</Explanation>

<FlowDiagram
  label="TCP congestion-window phase machine: slow start grows cwnd exponentially until ssthresh, then congestion avoidance grows it linearly; on loss cwnd is reduced and the cycle resumes"
  hue="var(--d-network)"
  perRow={4}
  nodes={[
    { id: "iw", label: "Initial window", sub: "cwnd = 10 MSS" },
    { id: "ss", label: "Slow start", sub: "×2 per RTT" },
    { id: "ca", label: "Congestion avoidance", sub: "+1 MSS per RTT" },
    { id: "loss", label: "Loss detected", sub: "reduce cwnd" },
  ]}
  edges={[
    { from: "iw", to: "ss", label: "first flight" },
    { from: "ss", to: "ca", label: "reach ssthresh" },
    { from: "ca", to: "loss", label: "packet drop" },
    { from: "loss", to: "ca", label: "CUBIC ×0.7, resume" },
    { from: "ss", to: "loss", label: "early loss" },
  ]}
  caption="Slow start probes capacity exponentially; crossing ssthresh switches to linear congestion avoidance. A loss cuts cwnd (CUBIC ×0.7) and sets a new ssthresh, then growth resumes — the sawtooth that lets TCP track a changing path."
/>

<KeyTakeaway>Sliding windows provide flow control (receiver-paced); SACK limits retransmission scope; slow start opens exponentially to probe capacity; CUBIC grows cubically after loss. RACK-TLP detects loss faster than classic RTO by watching ACK ordering rather than waiting for a timer.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-flow-and-congestion-control-retrieval"
  lang="en"
  questions={[
    { q: "Explain how Window Scaling (RFC 7323) affects achievable throughput on a 100 ms RTT, 1 Gbps link.", a: "Without window scaling the window field is 16 bits, capping the window at 65535 bytes. Throughput = window / RTT = 65535 / 0.1s ≈ 655 KB/s, far below the 1 Gbps link capacity. With window scaling, the window can grow to 1 GiB (scale factor 14). To fill the pipe, the window must be at least bandwidth × RTT = 1 Gbps × 0.1s = 12.5 MB — achievable with scaling enabled. Without scaling on an intercontinental high-bandwidth link, throughput is throttled by the protocol regardless of physical capacity." },
    { q: "What is RACK-TLP and why does it improve on the classic RTO-based loss detection?", a: "RACK (Recent ACKment) declares a segment lost when a segment sent after it has been acknowledged and a brief reorder window has expired — it uses ACK ordering rather than a timer. TLP (Tail Loss Probe) retransmits the last unacknowledged segment after one RTT to avoid hanging when the last packet of a burst is lost and there are no later ACKs to trigger RACK. Together they detect loss within 1–2 RTTs in most cases, while the classic RTO-based detection waits SRTT + 4*RTTVAR (at least 200ms and often seconds after backoff). RACK-TLP is the default loss detection in Linux since kernel 5.x." },
    { q: "Why does a connection with high random loss perform poorly under CUBIC but not under BBR?", a: "CUBIC treats every packet loss as a signal that the network is congested and cuts the congestion window aggressively (0.7× on loss). On a path with 1% random loss (e.g., cellular), CUBIC spends most of its time in recovery, never reaching the link's actual capacity. BBR ignores loss as a congestion signal; it estimates bottleneck bandwidth via delivered-byte rate and minimum RTT, then paces sends accordingly. Random drops do not perturb the BBR estimate, so throughput stays near path capacity." },
  ]}
/>

<Recap lang="en">
TCP flow control uses a sliding window: the receiver advertises how many bytes it can accept, the sender never exceeds that. Three options negotiated in SYN/SYN-ACK sharply affect throughput: MSS (segment size), Window Scaling (extends 64 KiB to 1 GiB window), and SACK (pinpoints missing ranges so only lost segments are retransmitted). Congestion control starts fresh connections in slow start (exponential window growth) then switches to congestion avoidance (linear growth) after hitting ssthresh. CUBIC, Linux's default since 2006, uses a cubic recovery curve after loss. The retransmission timer is computed from smoothed RTT (RFC 6298) with exponential backoff. RACK-TLP, the default since Linux 5.x, detects loss faster by watching ACK ordering rather than waiting for a timer to fire.
</Recap>
