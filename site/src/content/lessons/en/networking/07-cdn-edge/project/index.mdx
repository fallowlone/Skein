---
concepts:
- cache-control
- vary-header
- origin-shield
- stale-while-revalidate
- edge-worker
- cache-tags
estMin: 240
lang: en
level: senior
order: 10
prereqs: []
slug: project
sources:
- https://datatracker.ietf.org/doc/html/rfc9111
- https://developers.cloudflare.com/workers/runtime-apis/htmlrewriter/
status: ready
summary: "Hands-on project — front a mixed-content site with a CDN, tune cache keys and TTLs, add an edge composition worker, and prove hit rate and latency with before/after numbers."
title: 'CDN and edge: build and tune an edge delivery layer'
track: networking
unit: 07-cdn-edge
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Reading about cache keys and stampedes is not the same as raising a real hit rate from 40% to 95%. Put a CDN in front of a mixed-content site, get the cache keys and TTLs right, compose one page at the edge, and prove every step with measurements.
</Hook>

<Goal lang="en">
Turn the unit's mental model into a working edge delivery layer: route to the nearest edge, build correct cache keys, defend origin with shield and stale-while-revalidate, compose a page from per-TTL fragments, and verify hit rate and latency with before/after numbers.
</Goal>

<ProjectBrief
  id="07-cdn-edge-project"
  lessonSlug="07-cdn-edge"
  lang="en"
  objective="Front a small mixed-content site (static assets + a cacheable HTML page + a per-user fragment) with a real CDN, tune cache keys and TTLs to push the cache hit rate above 90% for cacheable content, compose one page at the edge, and prove the win with measured before/after numbers."
  requirements={[
    "Stand up a small origin with at least four content classes: content-hashed static assets (JS/CSS), a cacheable HTML page, a public read-only API (JSON), and one personalised endpoint that reads a session cookie.",
    "Put a real CDN in front (Cloudflare, Fastly, or CloudFront free tier) and capture a baseline: cache hit rate per content class, p50/p95 edge latency from two regions, and origin request rate under a load test.",
    "Set correct Cache-Control per class — content-hashed assets: public, max-age=31536000, immutable; HTML: public, max-age plus stale-while-revalidate and stale-if-error; public API: public, s-maxage plus SWR; personalised endpoint: private (or no-store). Confirm via response headers and cf-cache-status (or equivalent).",
    "Audit the Vary header on every cacheable response: keep Accept-Encoding, remove any User-Agent or Cookie that fragments the key. Demonstrate the hit-rate impact by measuring before and after the Vary fix.",
    "Enable the origin shield / tiered cache and show, under a multi-region load test that expires a popular URL, that concurrent edge misses collapse to one origin request rather than one per POP.",
    "Write an edge worker (or ESI config) that composes one page from independently-cached fragments: long-TTL chrome plus a per-user fragment fetched fresh per request; parallelise the fetches and bound the volatile one with a timeout and a cached fallback.",
    "Wire an automated cache-tag (or URL-pattern) purge into a mock deploy step and show that updating the HTML page invalidates all edges within seconds, with no stale split across regions.",
  ]}
  acceptance={[
    "A before/after table: cache hit rate per content class, p95 edge latency from two regions, and origin request rate under identical load — measured, not estimated.",
    "The personalised endpoint never serves one user's response to another (verified with two distinct sessions hitting the same URL), and cacheable responses carry a minimal correct Vary.",
    "Under the load test, a popular URL's expiry produces one origin request (shield plus SWR working), and users continue to get sub-50 ms responses across the expiry.",
    "The composed page assembles at the edge from at least two fragments with different TTLs, and a deploy-triggered purge clears all edges within ~5 seconds with no cross-region stale split.",
  ]}
  stretch={[
    "Add a one-page on-call runbook: per-region hit-rate and p99 dashboards, the Vary-cardinality alert, the deploy-purge checklist, and a triage tree from 'hit rate dropped' to root cause.",
    "Enforce origin protection: require mTLS (or an authenticated header) from the CDN to origin and show that direct-to-origin requests bypassing the CDN are rejected.",
    "Add a second CDN and DNS-based steering with a 30 s TTL; show traffic shifting to the healthy CDN when you mark the first unhealthy, and confirm purges and edge-worker logic behave identically on both.",
    "Send a 103 Early Hints response from the edge with Link: preload for the page's critical CSS, and measure the change in Largest Contentful Paint against the no-hints baseline.",
  ]}
/>

<Recap lang="en">
This is the loop you will run on every real edge delivery layer: route to the nearest edge, build a minimal correct cache key (audit Vary, mark cookie endpoints private), set per-class TTLs with stale-while-revalidate and stale-if-error, collapse the herd with an origin shield, compose pages from per-TTL fragments at the edge, and make invalidation automatic with deploy-triggered cache-tag purge — verifying hit rate and latency with before/after numbers under identical load. Doing it once on a small site makes the production version muscle memory.
</Recap>
