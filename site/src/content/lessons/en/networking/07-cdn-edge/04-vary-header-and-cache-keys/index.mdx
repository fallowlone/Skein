---
concepts:
- vary-header
- cache-key
- cache-poisoning
- accept-encoding
- accept-language
- cookie-caching
- faded-example
deepensInto:
- 05-stale-while-revalidate-and-stampede
- 07-cdn-operations-and-observability
estMin: 12
lang: en
lessonType: topic
level: middle
order: 4
prereqs:
- 06-http-versions/04-headers-caching-cors
- 07-cdn-edge/03-tiered-cache-and-cache-control
slug: 04-vary-header-and-cache-keys
sources:
- https://datatracker.ietf.org/doc/html/rfc7234
- https://datatracker.ietf.org/doc/html/rfc9111
- https://portswigger.net/research/practical-web-cache-poisoning
spiral:
- latency
status: ready
summary: 'How CDN cache keys are built, why Vary: User-Agent destroys hit rates, and
  why a missing Vary: Cookie can serve one user''''''''s account page to another.'
title: Vary header and cache keys
track: networking
unit: 07-cdn-edge
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import Misconception from "~/components/layout/Misconception.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Your CDN hit rate is 5% despite setting max-age=3600. The dashboard shows thousands of unique cache entries per URL. Somewhere in the origin response headers there is a Vary line that is silently fragmenting every URL into thousands of separate cache keys — one per unique browser header combination. This is the most common CDN misconfiguration in production.
</Hook>

<Crux>CDN cache key = URL + method + Vary headers; Vary: User-Agent creates a slot per browser version, collapsing hit rate to near zero.</Crux>

<Explanation>

## How the cache key is built

A CDN cache stores responses indexed by a **cache key**. The default key is `(URL, HTTP method, Host)`. The `Vary` response header extends the key with additional request header dimensions:

```
Cache-Control: public, max-age=3600
Vary: Accept-Encoding, Accept-Language
```

This tells the cache: maintain **separate entries** for each distinct value of `Accept-Encoding` and `Accept-Language`. A request for `/page` with `Accept-Language: en` and another with `Accept-Language: ru` create two separate cache entries — both correct language versions are served from cache.

**RFC 7234 §4.1** formalises this: "A cache MUST use the value of the `Vary` header field to determine if a stored response may be used for a given request."

## The Vary footgun

`Vary` works correctly for `Accept-Encoding` (gzip vs brotli — ~5 distinct values) and `Accept-Language` (~10–50 values per site). But some headers have thousands or millions of distinct values:

| Vary header | Distinct values | Effect |
|-------------|----------------|--------|
| `Accept-Encoding` | ~5 (gzip, br, identity…) | Safe — small fragmentation |
| `Accept-Language` | ~50 per typical site | Usually fine |
| `User-Agent` | Thousands (every browser × version × OS) | **Cache destroyed** |
| `Cookie` | Per-user (millions) | **Effectively uncacheable** |
| `Authorization` | Per authenticated user | **Effectively uncacheable** |

`Vary: User-Agent` is the classic footgun: developers add it to serve different HTML for mobile vs. desktop. The result is one cache entry per unique User-Agent string — every Chrome release, every iOS version, every Safari build. The cache fills up with unique entries that will never be reused; hit rate plummets to near zero.

<NumbersCard
  id="card-vary-impact"
  title="Vary header impact on cache hit rate"
  data-lesson-visual
  stats={[
    { label: "No Vary (or Vary: Accept-Encoding only)", value: "90–98% hit rate (normal operation)" },
    { label: "Vary: Accept-Language added (10 values)", value: "80–90% — small fragmentation" },
    { label: "Vary: User-Agent added", value: "<10% — near-zero effective caching" },
    { label: "Vary: Cookie added", value: "~0% — per-user entries never reused" },
    { label: "Vary: Authorization added", value: "~0% — per-token entries never reused" },
  ]}
/>

<FadedExample
  client:load
  id="fade-cache-key"
  pieceSlug="04-vary-header-and-cache-keys"
  lang="en"
  title="Cache key surprises"
  steps={{
    solved: (
      <div class="space-y-4">
        <p><strong>Scenario: the same URL, different Accept-Language — Vary set correctly.</strong></p>
        <ul class="list-disc list-inside space-y-2 text-sm mb-4">
          <li><strong>Step 1:</strong> User A (London) fetches <code>/api/products</code>. Accept-Language: en. CDN cache miss. Origin responds: <code>Cache-Control: max-age=3600, Vary: Accept-Language</code>.</li>
          <li><strong>Step 2:</strong> CDN stores entry with key: [URL=/api/products, Accept-Language=en].</li>
          <li><strong>Step 3:</strong> User B (also London) fetches <code>/api/products</code>. Accept-Language: ru. Key [URL=/api/products, Accept-Language=ru] does NOT exist. Cache miss → origin queried → Russian products cached.</li>
          <li><strong>Result:</strong> Both users see the correct language. Vary ensures separate, correct cache entries.</li>
        </ul>
      </div>
    ),
    semi: {
      prompt: "Same scenario. Origin responds with Cache-Control: max-age=3600 but FORGETS to add Vary: Accept-Language. User A fetches in en, User B fetches in ru. What does User B see?",
      blanks: [
        {
          id: "bug",
          expected: "english",
          placeholder: "What language does User B see? (english/russian/error)",
        },
      ],
    },
    blank: {
      prompt: "A logged-in user navigates to /account, sees their name and email. The CDN logs show a cache hit. An hour later, a different user (not logged in) visits the same /account URL and sees... the first user's name and email. Cache logs show hits for both. What did the engineer forget?",
      reveal: (
        <div class="space-y-2">
          <p><strong>Answer: They forgot to mark the response as private or to add Vary: Cookie.</strong></p>
          <p class="text-sm">The endpoint reads a session cookie and returns personalised content. Cookies are NOT part of the default cache key. The first request caches [URL=/account, method=GET] and stores personalised content. The second request (different user, different session_id cookie) matches the same cache key — the cached personalised content serves to the wrong user.</p>
          <p class="text-xs text-gray-600 mt-2"><strong>Fix:</strong> Add Cache-Control: private (bypasses CDN, only browser caches), or add Vary: Cookie. Modern practice prefers private for all user-specific endpoints.</p>
        </div>
      ),
    },
  }}
  misconceptions={{
    "bug:russian": "Not quite. The CDN treats both requests as cache-equivalent (same URL, no Vary header). The first response (English) is cached and keyed only by URL. The second request (Russian Accept-Language) matches the same key and retrieves the cached English response.",
  }}
/>

## Cookies are NOT in the cache key by default

This is the most security-relevant Vary fact. Cookies are not part of the default cache key. If an endpoint reads a session cookie and returns personalised content:

- **Missing `Cache-Control: private`** → CDN caches the personalised response
- **Missing `Vary: Cookie`** → different users with different cookies get the same cached response

Result: one user's account page is served to another user — a **data leak**.

**Rule of thumb:** for any endpoint that reads a cookie, set `Cache-Control: private`. Only use `Vary: Cookie` if you need CDN caching for cookie-differentiated content (rare, complex, requires a cookie whose value is stable and low-cardinality).

<Misconception
  id="r-cookies-not-in-key"
  title="Cookies are not in the cache key — mark user endpoints private"
>
CDNs cache by URL + Vary headers. Cookies are NOT in the key by default. An endpoint reading a session cookie without Cache-Control: private can serve one user's data to another. Audit every route: reads cookie? Mark it private or no-store.
</Misconception>

<Inset kind="edgecase" lang="en">
**Cache poisoning via unkeyed headers.** James Kettle's research (PortSwigger, 2018) documented how CDNs can be tricked into caching a malicious response. If the origin reads a request header (e.g. `X-Forwarded-Host`) to build the response body, but that header is NOT included in the CDN's cache key (not in Vary), an attacker can send a crafted request with `X-Forwarded-Host: evil.com` — origin returns a page with evil.com links, CDN caches it by URL only, and serves the poisoned page to all subsequent users. Mitigation: keep CDN and origin tightly aligned on which headers are key-significant; strip unrecognised forwarding headers at the CDN edge; use cache-key normalisation to drop non-significant query params.
</Inset>

<Quiz
  id="mid-quiz-1"
  pieceSlug="04-vary-header-and-cache-keys"
  lang="en"
  question="What does Vary: Accept-Encoding actually do in a CDN cache?"
  choices={[
    { label: "Tells the cache to encode the response before storing it", misconception: "Encoding is decided by the Content-Encoding response header, not Vary." },
    { label: "Tells the cache to store separate entries per distinct value of the Accept-Encoding request header", correct: true },
    { label: "Disables caching for this resource", misconception: "Vary parameterises caching by key dimensions — it does not disable it." },
    { label: "Forces clients to send compressed requests", misconception: "Clients decide their Accept-Encoding; Vary affects how the cache keys responses." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="04-vary-header-and-cache-keys"
  lang="en"
  question="What is the practical effect of Vary: Authorization on a shared CDN cache?"
  choices={[
    { label: "Faster responses for authenticated users", misconception: "Caching for authenticated users is mostly eliminated by Vary: Authorization, not improved." },
    { label: "Cache fragments per unique Authorization header value — typically one entry per logged-in user, destroying cache effectiveness", correct: true },
    { label: "Prevents unauthenticated users from reading the cache", misconception: "Vary: Authorization creates per-user entries; it does not protect them from being served to others." },
    { label: "Adds transport encryption", misconception: "Headers do not perform encryption; that is TLS's role." },
  ]}
/>

<DragOrder
  id="mid-drag-1"
  pieceSlug="04-vary-header-and-cache-keys"
  lang="en"
  prompt="Order these Vary header values from safest (least cache fragmentation) to most dangerous (most fragmentation):"
  items={[
    "Vary: Accept-Encoding — ~5 distinct values (gzip, brotli, identity, deflate, none)",
    "Vary: Accept-Language — ~10–50 values per typical site",
    "Vary: Accept — content-type negotiation, ~5–10 values",
    "Vary: User-Agent — thousands of unique values across browsers and versions",
    "Vary: Cookie — potentially millions of unique values (one per active session)",
  ]}
/>

</Explanation>

<FlowDiagram
  label="One URL combined with the Vary header splits into one separate cache entry per distinct header value; low-cardinality headers stay safe, high-cardinality ones shatter the cache"
  hue="var(--d-network)"
  perRow={3}
  nodes={[
    { id: "url", label: "GET /page", sub: "+ Vary header" },
    { id: "en", label: "Cache entry", sub: "Accept-Language: en" },
    { id: "ru", label: "Cache entry", sub: "Accept-Language: ru" },
    { id: "ua", label: "1000s of entries", sub: "Vary: User-Agent" },
  ]}
  edges={[
    { from: "url", to: "en", label: "key 1" },
    { from: "url", to: "ru", label: "key 2" },
    { from: "url", to: "ua", label: "footgun" },
  ]}
  caption="The cache key = URL + method + every header named in Vary. Vary: Accept-Language makes one entry per language (safe, ~10–50). Vary: User-Agent makes one entry per browser×version×OS string — thousands of near-unique keys that are never reused, collapsing the hit rate."
/>

<KeyTakeaway>The CDN cache key = URL + method + headers in Vary. Vary: Accept-Encoding and Accept-Language are safe (small cardinality). Vary: User-Agent destroys hit rates (thousands of unique values). Missing Vary: Cookie or Cache-Control: private on user-specific endpoints can leak one user's response to another — audit every new route that reads cookies or auth headers.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-vary-header-and-cache-keys-retrieval"
  lang="en"
  questions={[
    { q: "Explain why Vary: Accept-Encoding is required for an endpoint that serves gzipped responses to some clients and uncompressed to others.", a: "Without Vary: Accept-Encoding, the CDN keys responses only by URL + method. The first response it caches (say a gzip-encoded version for a gzip-supporting client) gets served to every subsequent client, including those that did not advertise gzip support. Those clients receive a gzip-compressed body and try to render it as plain text, breaking the page. Vary: Accept-Encoding tells the cache to maintain separate entries per distinct value of the Accept-Encoding request header, so the gzip variant and the identity variant are stored and served correctly." },
    { q: "A news site's article page has Vary: User-Agent to serve different layouts for mobile vs. desktop. Cache hit rate is 3%. What is the root cause and how do you fix it?", a: "User-Agent has thousands of distinct values (every browser × version × OS). The cache creates one entry per unique User-Agent string; since most combinations appear only once, almost no entry is ever reused. Fix: remove Vary: User-Agent. Instead, detect mobile/desktop at the CDN level (e.g. Cloudflare Workers checking CF-Device-Type header) and rewrite the URL to /mobile/path or /desktop/path, or serve a single responsive HTML and use CSS media queries. The URL or a low-cardinality header becomes the differentiator, not the raw User-Agent." },
    { q: "Why are cookies not in the default CDN cache key, and what is the security risk this creates?", a: "CDNs default to keying by URL + method + Vary-listed headers. Cookies are not in Vary by default because including them would make almost every user-specific response a unique cache entry (near-zero hit rate). The risk: an endpoint that reads a session cookie to return personalised content — without Cache-Control: private or Vary: Cookie — will have its first response cached under just the URL key. Any subsequent user requesting the same URL gets the first user's cached response. This is a data leak. Fix: mark all cookie-reading endpoints Cache-Control: private (no CDN cache) or no-store." },
  ]}
/>

<Recap lang="en">
The CDN cache key is built from the URL, HTTP method, Host, and any request headers listed in the response's `Vary` header. `Vary: Accept-Encoding` is safe — only ~5 possible values. `Vary: Accept-Language` is usually safe — ~10–50 values. `Vary: User-Agent` is catastrophic — thousands of unique Browser×version×OS combinations, each becoming a separate cache entry that is never reused, collapsing hit rates to near zero. Cookies are NOT part of the default key, which is an efficiency choice that creates a security risk: endpoints that serve personalised content without `Cache-Control: private` can leak one user's response to another. Audit every route that reads cookies or auth headers and mark it `private` or `no-store`.
</Recap>
