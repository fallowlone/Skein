infographic chart-column-simple
data
  title Cold-start latency by edge runtime (p99, ms)
  desc V8 isolates vs a full container runtime — the 100x gap that disqualifies Lambda@Edge for latency-sensitive paths
  values
    - label Cloudflare Workers (V8 isolate)
      value 5
    - label Lambda@Edge (Node container)
      value 600
theme
  palette #14b8a6 #f43f5e
