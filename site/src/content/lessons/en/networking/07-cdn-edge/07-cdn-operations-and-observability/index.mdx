---
concepts:
- cache-tags
- multi-cdn
- waf
- bot-management
- rum
- cache-hit-ratio
- origin-shield-offload
- mTLS
- argo
- global-accelerator
- design-case-study
deepensInto: []
estMin: 14
lang: en
lessonType: topic
level: senior
order: 7
prereqs:
- 07-cdn-edge/05-stale-while-revalidate-and-stampede
- 07-cdn-edge/06-edge-workers-and-esi
slug: 07-cdn-operations-and-observability
sources:
- https://blog.cloudflare.com/argo-intelligent-routing/
- https://httpwg.org/specs/rfc8297.html
- https://developer.chrome.com/docs/web-platform/early-hints
spiral:
- latency
status: ready
summary: Cache-tag purge, multi-CDN steering, WAF at edge, the key metrics that catch
  CDN incidents before users notice, and a news-publisher design case study.
title: CDN operations and observability
track: networking
unit: 07-cdn-edge
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import DebugLog from "~/components/pedagogy/DebugLog.astro";
import DesignPrompt from "~/components/pedagogy/DesignPrompt.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
You shipped a hotfix at 3 AM. Origin is updated. But 20 minutes later, 40% of users are still on the old version — different CDN regions have different cache states, a stale edge in Asia-Pacific is serving yesterday's data, and your monitoring only shows aggregate error rates that look fine. CDN incidents are invisible until they are not.
</Hook>

<Crux>Production CDN ops: per-region hit rate, deploy-triggered purge, WAF + mTLS origin protection, explicit graceful degradation.</Crux>

<Explanation>

## Cache-tag purge: surgical invalidation at scale

For a site with thousands of URLs (news publisher, e-commerce catalogue), URL-based purge is operationally unmanageable. **Cache tags** (Cloudflare Enterprise, Fastly) solve this:

1. Origin sets `Cache-Tag: article-1001, category-tech` on article responses.
2. On article edit, the CMS calls `POST /cdn/purge {"tag": "article-1001"}`.
3. CDN invalidates all cached responses carrying that tag — across all POPs, within seconds.
4. The category tag allows purging all articles in a category (`tag: category-tech`) in one API call.

Without cache tags: every edit triggers O(URLs) purge calls. With cache tags: every edit triggers O(tags) calls, typically 1–5.

## Multi-CDN traffic steering

Large operators (Netflix, Apple, major news sites) run two or more CDNs simultaneously for:

- **Vendor resilience**: one CDN outage does not take the site down.
- **Regional optimisation**: one CDN may have better peering in Asia, another in Latin America.
- **Commercial leverage**: competing CDN contracts reduce per-GB costs.

Steering mechanism: **DNS-based steering** (NS1 Pulsar, Cedexis Openmix, custom). These aggregate real-user-monitoring (RUM) measurements and update DNS records every few seconds to route to the best-performing CDN per region. DNS TTL: 30 s for fast steering response.

**Cost:** operational complexity. Purges, headers, and edge-worker code must work identically on every CDN. A purge issued to CDN A does not automatically clear CDN B — each requires its own API call.

<NumbersCard
  id="card-cdn-ops-numbers"
  title="CDN operations key numbers"
  data-lesson-visual
  stats={[
    { label: "Cache-tag purge propagation time (Cloudflare, Fastly)", value: "1–5 seconds globally" },
    { label: "Multi-CDN DNS TTL for steering", value: "30 s (fast failover)" },
    { label: "mTLS edge-to-origin: protects against origin IP exposure", value: "CDN client cert required by origin" },
    { label: "WAF OWASP Top 10 block rate (typical production)", value: "0.1–2% of requests (adjust per app)" },
    { label: "Healthy cache hit rate (static assets)", value: ">90%" },
    { label: "Healthy cache hit rate (HTML pages)", value: ">70%" },
    { label: "Origin shield offload ratio target", value: ">90% of edge misses never reach origin" },
  ]}
/>

## BGP-level optimisation: Argo and Global Accelerator

Anycast picks the BGP-closest POP, not the latency-closest. On intercontinental paths, BGP "closest" and "lowest latency" diverge significantly.

**Cloudflare Argo Smart Routing** and **AWS Global Accelerator** measure actual end-to-end latency from all POPs continuously and route traffic over a **private backbone** (not the public internet) to the lowest-latency POP. Typical saving: 30–50% reduction in p95 latency on intercontinental paths. Cost: per-GB premium pricing on backbone traversal. Worth it for latency-sensitive APIs; usually overkill for static-asset delivery where BGP is already efficient.

**mTLS edge-to-origin.** Even with Anycast protecting origin by obscuring its IP, attackers can discover origin IP via DNS history, certificate transparency logs, or misconfigured direct access paths. mTLS (mutual TLS): origin accepts connections only if the client presents the CDN's certificate. Without the CDN cert, direct-to-origin requests are rejected — origin IP exposure no longer matters.

## WAF and bot management at edge

CDNs sit in the request path for all traffic — making them the cheapest layer for attack defence:

- **WAF (Web Application Firewall)**: matches request patterns against OWASP Top 10 rule sets (SQL injection, XSS, path traversal, command injection). Block in under 1 ms at edge, no origin involvement.
- **Bot management**: JA3/JA4 TLS fingerprinting (fingerprint the TLS ClientHello), behavioural analysis, IP reputation to distinguish human from automated traffic. Blocks credential stuffing, scraping, and API abuse.
- **Rate limiting**: per-IP, per-token, per-route. Configured at edge; enforced without origin round-trips.
- **DDoS scrubbing**: volumetric attacks (L3/L4) absorbed at edge before reaching origin. Cloudflare's Anycast network spans 330+ cities, distributing attack traffic across all POPs.

## 103 Early Hints

RFC 8297 defines the `103 Early Hints` informational response, sent before the final `200 OK`. The edge can send `Link: </style.css>; rel=preload` in a 103 response while the origin generates the main HTML. The browser starts fetching critical assets before HTML arrives, saving one RTT from the critical render path. As of 2026: 93% browser support, ~5% real-world adoption. Vercel leads with ~2.8%; Cloudflare and Fastly remain below 1%. Adoption friction: the edge must know which resources to hint per page — not easily automatable without framework support.

## Key observability metrics

A CDN incident often starts as a metric drift before it becomes a user complaint:

| Metric | Target | Alert threshold |
|--------|--------|----------------|
| Cache hit rate (static assets) | &gt;90% | &lt;80% triggers investigation |
| Cache hit rate (HTML pages) | &gt;70% | &lt;60% triggers investigation |
| Origin shield offload ratio | &gt;90% | &lt;80% — edges may be contacting origin directly |
| p95 edge response time per region | &lt;50 ms | &gt;100 ms — regional POP issue |
| p99 edge response time per region | &lt;200 ms | &gt;500 ms — severe regional degradation |
| Vary-key cardinality per URL | &lt;100 | &gt;1000 — check for Vary: User-Agent footgun |
| WAF block rate | 0.1–2% | &gt;5% — possible attack; &lt;0.01% — WAF rules too loose |

Export from CDN dashboards to Prometheus/OTel for SLO alerting. CDN-native dashboards (Cloudflare Analytics, Fastly Real-Time) are useful for deep-dive but not for cross-CDN correlation.

<DebugLog
  id="sr-debug-1"
  pieceSlug="07-cdn-operations-and-observability"
  lang="en"
  title="curl -I output revealing a CDN misconfiguration"
  output={`$ curl -I https://example.com/article/123
HTTP/2 200
date: Wed, 13 May 2026 14:33:00 GMT
content-type: text/html; charset=utf-8
cache-control: public, max-age=3600
cf-cache-status: MISS
vary: User-Agent, Accept-Encoding, Cookie, Authorization
age: 0
server: cloudflare`}
  outputLang="log"
  question="Cache hit rate is 5%. What is wrong with the response headers, and how do you fix it?"
  hint="Look at the Vary header. How many distinct combinations of those four headers exist across real users?"
  answer="Vary lists four headers: User-Agent (~thousands of distinct values: browser × version × OS), Accept-Encoding (~5), Cookie (potentially unique per session), Authorization (every logged-in user gets their own variant). Combined cardinality is effectively per-user — the cache key is almost unique per request, destroying cache effectiveness entirely. Fix: drop Vary: User-Agent (serve responsive HTML instead of per-agent variants). Drop Vary: Cookie and Authorization — for personalised endpoints use Cache-Control: private instead. After the fix, Vary becomes Vary: Accept-Encoding only; hit rate should approach 90%+ for this cacheable HTML."
/>

<TraceScenario
  id="sr-trace-1"
  pieceSlug="07-cdn-operations-and-observability"
  lang="en"
  scenario="Origin is down for 8 minutes during a database failover. Users hitting the CDN during the outage. What do users experience with and without stale-if-error configured?"
  steps={[
    { prompt: "Without stale-if-error: a user requests a product page during the outage. Cache was fresh 20 minutes ago (max-age=300, now expired). What do they see?", reveal: "A 503 Service Unavailable — the CDN forwards to origin, origin is down, CDN returns the error. Worse: the CDN may cache the 503 response for a short duration, serving it to all users even after origin recovers." },
    { prompt: "With stale-if-error=86400 (1 day): same scenario. What do users see?", reveal: "The product page from 20 minutes ago — slightly stale but functional. The CDN serves the last-known-good response because origin returned a 5xx. Users can still browse, add to cart (with the stale page), and complete purchases. The 8-minute outage is invisible to most users." },
    { prompt: "Origin recovers. How does the CDN know to stop serving stale content and return to serving fresh responses?", reveal: "On the next revalidation attempt after origin recovers, origin returns a 200 with a fresh response. The CDN caches it and serves fresh content from that point. No manual intervention needed — the stale-if-error directive only applies while origin is returning errors." },
    { prompt: "What content types should NOT use stale-if-error?", reveal: "Anything where correctness is critical: prices (stale prices cause wrong charges), inventory levels (stale availability causes overselling), authentication endpoints (stale auth state creates security issues), checkout confirmation pages. Use stale-if-error for read-only browsing pages, documentation, and marketing content only." },
  ]}
/>

<DesignPrompt
  id="sr-design-1"
  pieceSlug="07-cdn-operations-and-observability"
  lang="en"
  prompt="Design CDN configuration for a news publisher: 50M monthly readers, articles with embedded paywalled content, real-time breaking-news banner, and reader comments."
  constraints={[
    "Article body content (most page bytes) can be stale up to 5 minutes.",
    "Breaking-news banner must update within 30 seconds globally.",
    "Reader comments are user-specific (cannot share across users) but the comment list itself is shared.",
    "Paywall: anonymous users see 3 free articles per month per IP, then a paywall block.",
  ]}
  canonical="Three-layer cache strategy. (1) Article body and chrome: cache at edge max-age=300, stale-while-revalidate=3600. Tag with article-id for fast purge on edits. (2) Breaking-news banner: edge worker fetches from a globally-replicated KV store with 30 s TTL — one origin fetch per edge per 30 s, not one per user. (3) Comment list: shared, cache max-age=60. User's own comment interaction state injected by edge worker reading from session KV. (4) Paywall: edge worker counts anonymous reads in KV per IP+month; at count > 3, edge returns paywall HTML without contacting origin — saves origin the request and cannot be bypassed by clearing browser state. (5) mTLS edge↔origin. WAF + rate-limiting at edge. (6) Monitor: cache hit rate per article URL prefix, breaking-news KV latency, paywall block rate, p95 edge latency per region."
  keyPoints={[
    "Three-layer cacheability: article body (5 min TTL + SWR), breaking news (30 s KV, not per-user fetch), per-user state (edge worker from session KV).",
    "stale-while-revalidate prevents cache stampede on popular articles at expiry.",
    "Cache-tag article-id enables surgical purge on edits without clearing other articles.",
    "Edge worker enforces paywall counting without origin round-trips — not bypassable by clearing cookies.",
    "Per-region observability catches regional cache issues before users notice.",
  ]}
/>

<Quiz
  id="sr-quiz-1"
  pieceSlug="07-cdn-operations-and-observability"
  lang="en"
  question="Why does multi-CDN traffic steering use DNS rather than HTTP-level redirects?"
  choices={[
    { label: "DNS is faster to update than load balancers", misconception: "Load balancers can be updated instantly; DNS has TTL propagation delays. The reason is latency, not update speed." },
    { label: "HTTP redirects add a full extra round-trip per request; DNS steering happens before any HTTP traffic flows, selecting the best CDN with no per-request overhead", correct: true },
    { label: "CDNs do not support HTTP-level redirects", misconception: "CDNs fully support HTTP redirects; they are just not used for steering because of the latency cost." },
    { label: "HTTP redirects only work for GET requests", misconception: "HTTP redirects work for all methods; the reason for DNS steering is the round-trip latency cost." },
  ]}
/>

</Explanation>

<KeyTakeaway>CDN production operations require: cache-tag purge for surgical invalidation at scale, automated purge in deploy pipelines, multi-CDN steering via DNS for resilience, mTLS edge-to-origin to protect against IP exposure, WAF and bot management at edge, and per-region metric alerting on cache hit rates and p99 latency before user-impacting incidents surface in error rates.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-cdn-operations-and-observability-retrieval"
  lang="en"
  questions={[
    { q: "Explain the difference between origin shield and the standard edge cache, and when shield is critical.", a: "The standard edge cache stores responses close to users. Each region may have dozens or hundreds of POPs, each with its own cache. On a cache miss, every POP independently fetches from origin — if 100 POPs in one region all miss the same popular URL simultaneously, origin sees 100 requests. The origin shield is a regional cache between edges and origin: all edges in a region funnel their misses through one shield. The shield absorbs the burst; only one origin request happens per region regardless of how many edges miss. Critical for: traffic spikes after invalidation, popular content expiry events, expensive-to-generate origin responses. Cheap CDN plans omit shield as a cost optimisation; production-grade plans include it by default." },
    { q: "A deploy pipeline updates origin but doesn't purge the CDN. Users in Europe see stale content 30 minutes after deploy. Users in Asia see fresh content immediately. Why the discrepancy?", a: "Different CDN edges have different cache state ages at deploy time. The Asian edge may have recently refreshed its cache (its TTL was closer to expiry) while the European edge had a full TTL window remaining. Without an automated post-deploy purge, each edge waits for its individual TTL to expire before fetching fresh content from origin. Fix: add a CDN purge step to the deploy pipeline that runs immediately after origin is updated, invalidating all affected URLs or cache tags across all regions simultaneously." },
    { q: "Your CDN cache hit rate drops from 92% to 45% over two days. List three possible causes in order of likelihood based on common production incidents.", a: "(1) Vary header regression — most common cause. A new code deploy added Vary: User-Agent or Vary: Cookie to a frequently-accessed endpoint, fragmenting the cache wildly. Check the Vary header on recently-changed endpoints. (2) Content type change — an endpoint that returned cacheable responses now returns Cache-Control: private or no-store after a feature change (e.g. added user-specific data to a previously-shared page). (3) Traffic mix change — new high-traffic endpoint being served (maybe a new API route) that was never configured as cacheable. New routes default to pass-through without explicit Cache-Control: public." },
  ]}
/>

<Recap lang="en">
CDN operations at production scale require four capabilities. (1) Cache-tag purge: assign semantic tags to cached responses, purge by tag on content updates — O(tags) API calls instead of O(URLs). (2) Deploy pipeline integration: every deploy triggers a purge of affected URL patterns or tags immediately after origin updates. (3) Multi-CDN resilience: DNS-based steering (30 s TTL) with RUM data routes users to the best-performing CDN per region; mTLS edge-to-origin prevents bypass after IP exposure. (4) Observability: cache hit rate per URL prefix, origin-shield offload ratio, p95/p99 edge response time per region, Vary-key cardinality. Alert on metric drift — a hit-rate drop precedes user complaints by minutes to hours. WAF and bot management at edge stop attacks before they reach origin.
</Recap>
