---
concepts:
- cdn
- edge-server
- pop
- cache-hit
- cache-miss
- origin-server
- anycast
- geodns
deepensInto:
- 02-anycast-and-geodns
- 03-tiered-cache-and-cache-control
estMin: 8
lang: en
lessonType: topic
level: junior
order: 1
prereqs:
- 04-dns-resolution/01-dns-what-it-does
- 03-tcp-handshake/01-the-three-way-handshake
slug: 01-cdn-what-and-why
sources:
- https://blog.cloudflare.com/cloudflare-connected-in-over-300-cities/
- https://almanac.httparchive.org/en/2025/cdn
- https://www.akamai.com/us/en/why-akamai/
spiral:
- latency
status: ready
summary: A CDN moves copies of your content to hundreds of edge servers worldwide
  so each user fetches from a server 20 ms away instead of 200 ms.
title: 'CDN: putting content next door'
track: networking
unit: 07-cdn-edge
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import PersonaTag from "~/components/pedagogy/PersonaTag.astro";

<Hook>
When <PersonaTag id="bea" lang="en" /> in London loads a page whose server sits in California, every request travels 200 ms round-trip — and nothing in your code can fix that. The speed of light is finite. A CDN closes the gap by moving content, not by speeding up the network.
</Hook>

<Crux>CDN copies content to edge servers worldwide; requests travel 20 ms not 200 ms — latency beaten by moving data closer, not faster.</Crux>

<Explanation>

## What a CDN does in one sentence

A **CDN (Content Delivery Network)** keeps copies of your website on hundreds of servers around the world so the user's browser fetches bytes from a server 20 ms away instead of one 200 ms away.

## Why latency is the real problem

Speed of light is ~300,000 km/s in vacuum; in fiber it's slower (~200,000 km/s). London to California is ~8,700 km. The one-way minimum is 8,700 / 200,000 = **~43 ms**. Round-trip: ~87 ms. Add TCP handshake, TLS, queuing — you're at **200–250 ms** per request. No code optimisation closes that gap. A CDN closes it by moving the content.

## The warehouse metaphor

Imagine you publish a magazine. Without a CDN you mail every reader a copy from one print shop. With a CDN you ship pallets to local distribution centres in every city; each reader picks up a copy from their nearest one. Same magazine, far shorter trip. Each **Point of Presence (PoP)** is one of those warehouses — a data center with CDN servers.

## One scenario end to end

<PersonaTag id="bea" lang="en" /> in London navigates to a CDN-fronted site:

1. Browser does [DNS](/en/networking/04-dns-resolution/) — returns the IP of the **nearest CDN edge server** (either via Anycast routing or GeoDNS).
2. [TCP](/en/networking/03-tcp-handshake/) + [TLS](/en/networking/05-tls-handshake/) + HTTP request all happen against the **edge**, not the origin.
3. **Cache hit**: edge has the page cached → response in ~20 ms.
4. **Cache miss**: edge forwards to origin (or an intermediate shield), caches the answer, returns it.

<PersonaTag id="patty" lang="en" /> in Tokyo asks the same URL and lands on the Tokyo edge with its own cached copy. <PersonaTag id="sven" lang="en" />, the origin server in California, sees one request from each city's edge and almost no traffic from users directly.

<NumbersCard
  id="card-cdn-basic-numbers"
  title="Edge hit vs. origin round-trip"
  data-lesson-visual
  stats={[
    { label: "Origin (California → London) round-trip", value: "~200–250 ms" },
    { label: "Edge cache hit (London POP)", value: "~10–20 ms" },
    { label: "Speed-up factor on a cache hit", value: "10–20×" },
    { label: "Cloudflare edge POPs (2026)", value: "330+ cities, 120+ countries" },
    { label: "Typical cache hit ratio (static assets)", value: "90–98%" },
    { label: "Origin shield traffic absorption", value: "90%+ of edge misses" },
  ]}
/>

## When does a CDN NOT help?

A CDN helps when content can be **shared across users** — static assets (JS, CSS, images), HTML pages without user-specific data. It does not help when **every response is unique** (bank balance, personalised feed, checkout total) — there is nothing to cache, so the request still hits origin. The connection still terminates at the edge, which saves TLS setup latency, but the response must be fetched fresh each time.

<Inset kind="why" lang="en">
**Why three mechanisms work together.** Anycast IP routing (same address advertised from many cities) delivers requests to the BGP-nearest POP without any DNS special-casing. GeoDNS adds explicit control: the CDN operator returns different A records by resolver geography, so a London resolver gets a London edge IP. The edge cache then serves most requests without ever contacting the origin. All three cooperate: Anycast or GeoDNS finds the right edge; the cache does the heavy lifting.
</Inset>

<Quiz
  id="jr-quiz-1"
  pieceSlug="01-cdn-what-and-why"
  lang="en"
  question="What is the main job of a CDN?"
  choices={[
    { label: "Encrypt your traffic end-to-end", misconception: "Encryption is TLS's job, not the CDN's." },
    { label: "Serve copies of your content from many geographic locations so each user fetches from a nearby edge", correct: true },
    { label: "Block bots automatically", misconception: "Many CDNs offer bot protection, but caching is the core purpose." },
    { label: "Speed up your origin server's database queries", misconception: "Origin DB is unrelated to CDN edge caching." },
  ]}
/>

<Quiz
  id="jr-quiz-2"
  pieceSlug="01-cdn-what-and-why"
  lang="en"
  question="When does a CDN NOT reduce latency?"
  choices={[
    { label: "When the page is cacheable and the user is far from origin", misconception: "That is precisely when CDN helps the most." },
    { label: "When every response must be personalised and unique per user — there is nothing to cache", correct: true },
    { label: "When the user has slow Wi-Fi", misconception: "CDN cannot fix last-mile bandwidth, but still helps with everything before it." },
    { label: "When the site uses HTTPS", misconception: "CDNs work great with HTTPS — they terminate TLS at the edge." },
  ]}
/>

<DragOrder
  id="jr-drag-1"
  pieceSlug="01-cdn-what-and-why"
  lang="en"
  prompt="Order what happens on a CDN cache HIT:"
  items={[
    "Browser DNS resolves to CDN edge IP via Anycast or GeoDNS",
    "Browser opens TCP + TLS connection to the edge",
    "Browser sends HTTP request",
    "Edge looks up the URL in its cache and finds a fresh copy",
    "Edge returns the cached response to the browser",
  ]}
/>

<MetaphorComplete
  id="jr-metaphor-1"
  pieceSlug="01-cdn-what-and-why"
  lang="en"
  setup="Fill in the blank: a CDN is like a chain of _______ distributing your content so each user picks up from the closest one."
  accepted={["warehouses", "warehouse", "distribution centres", "distribution centers", "depots", "depot"]}
  canonical="warehouses (or distribution centres)"
  explanation="Each edge POP is a small warehouse holding popular content close to local users. Origin is the central factory that wholesales to warehouses."
/>

</Explanation>

<KeyTakeaway>A CDN defeats latency by replicating content to edge POPs worldwide; Anycast or GeoDNS routes each user to the nearest edge; a cache hit returns a response in ~20 ms instead of ~200 ms. CDNs help cacheable content only — personalised responses must still round-trip to origin.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-cdn-what-and-why-retrieval"
  lang="en"
  questions={[
    { q: "In one sentence: what trade-off does a CDN ask you to accept in exchange for speed?", a: "Cached content is by definition slightly out of date — when the origin changes, users may see the old version until the cache expires or is purged; the CDN trades freshness for latency, and your job is to choose appropriate TTLs and use invalidation tools (purges, cache-tags) so the trade is acceptable per content type." },
    { q: "Name the two mechanisms a CDN uses to route a user to the nearest edge server.", a: "Anycast IP (same IP advertised from many locations, BGP selects nearest) and GeoDNS (authoritative DNS returns different A records based on the resolver's geographic location)." },
    { q: "Why does a CDN still help latency for personalised responses even though it cannot cache them?", a: "The CDN terminates the TCP+TLS connection at the edge (physically close to the user), so the user pays only the local RTT for the connection setup; the origin fetch happens over the CDN's optimised backbone, not the public internet all the way from the user." },
  ]}
/>

<Recap lang="en">
A CDN (Content Delivery Network) replicates content to hundreds of edge Points of Presence (POPs) worldwide so users fetch from a server 20 ms away instead of 200 ms away. Anycast routing or GeoDNS directs each request to the nearest POP; the edge's local cache serves it without contacting origin. A cache hit is 10–20× faster than an origin round-trip — the win comes entirely from proximity, not from faster hardware. CDNs help most for cacheable content (static assets: JS, CSS, images) and least for personalised responses where caching is impossible. The origin sees only a fraction of total traffic, typically under 10% for a well-configured static site.
</Recap>
