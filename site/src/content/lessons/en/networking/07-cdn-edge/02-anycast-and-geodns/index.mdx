---
slug: 02-anycast-and-geodns
lang: en
track: networking
unit: 07-cdn-edge
order: 2
title: "Anycast and GeoDNS: routing to the nearest edge"
summary: "How the same IP address advertised from 300 cities and geography-aware DNS cooperate to send every user to their closest CDN edge server."
estMin: 10
status: ready
lessonType: topic
level: middle
prereqs:
  - "04-dns-resolution/02-the-resolver-walk"
  - "07-cdn-edge/01-cdn-what-and-why"
deepensInto:
  - "07-cdn-operations-and-observability"
spiral: ["latency"]
concepts: ["anycast", "geodns", "bgp", "pop", "edns-client-subnet", "origin-shield", "dns-resolver"]
sources:
  - https://blog.cloudflare.com/argo-intelligent-routing/
  - https://datatracker.ietf.org/doc/html/rfc7871
  - https://blog.cloudflare.com/cloudflare-connected-in-over-300-cities/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook>
You want every user — whether in London, Tokyo, or São Paulo — to hit the nearest CDN server automatically, without manually configuring routes or maintaining separate URLs per region. Two mechanisms solve this: Anycast and GeoDNS. They work differently and are often used together.
</Hook>

<Crux>Anycast routes BGP to the nearest POP from one IP; GeoDNS returns region-specific IPs — proximity routing with different trade-offs.</Crux>

<Explanation>

## Anycast IP routing

With **Anycast**, the CDN operator announces the **same IP address** (e.g. `93.184.216.1`) from multiple geographic locations simultaneously via BGP. BGP routing selects the "nearest" edge location as measured by AS path length or hop count. When a user makes a request, their local router sends it to the closest POP advertising that IP — no special DNS needed, just standard BGP routing.

**Upside:** works automatically; any network-level change (POP added, withdrawn) takes effect within BGP convergence time (seconds to minutes). **Downside:** BGP "nearest" is hop-count nearest, not latency-nearest. Two cities equidistant by hops can differ by 50 ms of real latency.

## GeoDNS

With **GeoDNS**, the CDN's authoritative nameserver returns **different A records** based on the geographic location of the resolving nameserver. A query from a London resolver returns a London edge IP; a Tokyo resolver gets Tokyo. This gives the operator explicit control over routing — you can steer different regions to different POPs manually.

**The resolver vs. the user.** GeoDNS sees the **resolver's** IP, not the browser's. A user in Toronto using Google's 8.8.8.8 resolver gets the IP for whichever region Google's DNS server appears to come from — often Mountain View, CA — which may route them to a US POP instead of a Toronto one. That's suboptimal.

**EDNS Client Subnet (ECS)** partially fixes this: resolvers that opt in send the user's /24 subnet prefix alongside the DNS query, letting the authoritative server return a location-aware IP. Cost: the user's subnet prefix travels up the DNS delegation chain, reducing privacy. Apple's iCloud Private Relay and Cloudflare's 1.1.1.1 disable ECS for this reason, accepting slightly suboptimal routing.

<NumbersCard
  id="card-anycast-geodns"
  title="Anycast vs. GeoDNS comparison"
  data-lesson-visual
  stats={[
    { label: "Anycast selection basis", value: "BGP AS path length (hop count)" },
    { label: "GeoDNS selection basis", value: "Resolver IP geolocation" },
    { label: "Anycast BGP convergence on POP change", value: "Seconds to minutes" },
    { label: "GeoDNS TTL (typical)", value: "30–60 s for fast steering" },
    { label: "EDNS Client Subnet granularity", value: "/24 prefix (user subnet)" },
    { label: "Cloudflare smart routing (Argo) p95 saving", value: "30–50% on intercontinental paths" },
  ]}
/>

## How CDNs combine both

Most large CDNs use Anycast at the IP layer and GeoDNS on top to combine their strengths. Anycast handles DDoS absorption well — volumetric attacks are spread across every POP advertising the IP. GeoDNS provides explicit failover: withdraw a POP from DNS when unhealthy, so new connections stop arriving within one DNS TTL.

<Inset kind="why" lang="en">
**Why Anycast alone is not enough for CDNs.** Anycast routes by BGP metrics, not by actual latency. A user's ISP might peer with a CDN POP that is geographically distant but has a short AS path. Cloudflare Argo Smart Routing and AWS Global Accelerator add a measurement layer: they probe actual round-trip latency from many POPs continuously and route traffic over a **private backbone** (bypassing the public internet) to the lowest-latency POP. Typical saving: 30–50% reduction in p95 latency on intercontinental hops. Cost: per-GB premium pricing for backbone traversal. Worth it for latency-sensitive APIs; usually overkill for static-asset delivery.
</Inset>

<Quiz
  id="mid-quiz-1"
  pieceSlug="02-anycast-and-geodns"
  lang="en"
  question="A user in Toronto uses 8.8.8.8 as their DNS resolver. Why might they land on a US West Coast CDN POP instead of a Toronto one?"
  choices={[
    { label: "Their ISP blocks the Canadian POP", misconception: "ISP blocks are unrelated to this routing issue." },
    { label: "GeoDNS sees Google's resolver IP (from Mountain View, CA) rather than the user's Toronto IP and returns a US POP address", correct: true },
    { label: "Anycast only works in the US", misconception: "Anycast is a global routing scheme." },
    { label: "Toronto POPs do not support HTTPS", misconception: "HTTPS support is unrelated to POP selection." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="02-anycast-and-geodns"
  lang="en"
  question="What is the main limitation of pure Anycast routing for CDN proximity?"
  choices={[
    { label: "It only works with IPv6", misconception: "Anycast works with both IPv4 and IPv6." },
    { label: "BGP selects the AS-path-nearest POP, which may not be the latency-nearest — a 10-hop POP can have worse latency than a 12-hop one", correct: true },
    { label: "It cannot handle TLS connections", misconception: "TLS is independent of routing; Anycast handles TCP/TLS just fine." },
    { label: "It requires changing DNS records per user", misconception: "The whole point of Anycast is that no per-user DNS customisation is needed." },
  ]}
/>

<TraceScenario
  id="mid-trace-1"
  pieceSlug="02-anycast-and-geodns"
  lang="en"
  scenario="Trace how a London user reaches the correct CDN edge with GeoDNS."
  steps={[
    { prompt: "Browser resolves cdn.example.com. Which DNS server does the query ultimately reach?", reveal: "The CDN's authoritative nameserver, after the recursive resolver walks the DNS delegation chain: root → TLD → CDN's NS records." },
    { prompt: "The authoritative server sees the query from a London-area resolver IP. What does it return?", reveal: "An A record containing the IP of the London (or European) CDN edge POP — not the California origin IP." },
    { prompt: "Browser connects to that London edge IP via TCP + TLS. What is the advantage?", reveal: "TCP and TLS handshakes complete in ~5–10 ms (local London RTT) instead of ~100–200 ms (California). The TLS session key exchange and certificate validation happen at the edge, not the origin." },
    { prompt: "Six months later, the operator withdraws the London POP for maintenance. How long until users stop landing there?", reveal: "Within one DNS TTL — typically 30–60 seconds for CDN records. Existing TCP connections stay on the old POP until they close; new DNS resolutions start returning an alternative POP." },
  ]}
/>

<DragOrder
  id="mid-drag-1"
  pieceSlug="02-anycast-and-geodns"
  lang="en"
  prompt="Order a request path using Anycast routing (no GeoDNS involved):"
  items={[
    "Browser issues DNS query — resolver walks to CDN authoritative NS",
    "Authoritative NS returns the single Anycast IP (same IP for all regions)",
    "Browser's TCP SYN packet enters the internet with that destination IP",
    "ISP routers apply BGP forwarding: forward toward the CDN AS advertising the shortest path",
    "Packet arrives at the CDN POP with the shortest BGP path from this ISP",
    "TLS handshake + HTTP request completes at that POP",
  ]}
/>

</Explanation>

<KeyTakeaway>Anycast routes by BGP hop count (automatic, DDoS-resilient, but not latency-optimal); GeoDNS routes by resolver geography (explicit, fast to update, but resolver location may not match user location). Smart routing layers (Argo, Global Accelerator) add actual latency measurement on top of both.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-anycast-and-geodns-retrieval"
  lang="en"
  questions={[
    { q: "Explain in two sentences why Anycast routing and GeoDNS solve the same problem but fail in different ways.", a: "Anycast routes by BGP AS-path length, which is a proxy for latency but not a perfect one — a hop-count-close POP can still be latency-far due to peering agreements or congestion. GeoDNS routes by the resolver's IP geolocation, which is a proxy for user location but not a perfect one — a user in Toronto using Google's resolver in Mountain View gets routed to a US POP instead of Canada." },
    { q: "What does EDNS Client Subnet do, and what is the privacy cost?", a: "ECS makes the recursive resolver include the user's /24 subnet prefix in the DNS query so the CDN's authoritative server can return a geographically accurate POP address rather than routing based on the resolver's location. The cost is privacy: the user's network prefix is now visible to every DNS server in the delegation chain — root, TLD, authoritative — rather than staying within the resolver." },
    { q: "A CDN operator discovers their London POP is experiencing high packet loss. How do they stop new users from landing there, and how fast does it take effect?", a: "They withdraw the London POP's DNS record (GeoDNS) or its BGP Anycast announcement (Anycast routing). With GeoDNS, new requests stop arriving within one DNS TTL — typically 30–60 seconds for CDN records. With BGP withdrawal, convergence is seconds to a few minutes depending on AS propagation. Existing connections on the POP continue until they close naturally." },
  ]}
/>

<Recap lang="en">
Anycast and GeoDNS are the two proximity routing mechanisms CDNs use to send every user to the nearest edge server. Anycast advertises one IP from all POPs and lets BGP pick the closest by hop count — automatic and DDoS-resilient, but not perfectly latency-optimal. GeoDNS returns different A records per resolver geography — explicit and fast to change, but only as accurate as the resolver's location relative to the user. Most CDNs combine both: Anycast for scale and DDoS absorption, GeoDNS for failover control, and optionally smart routing (Cloudflare Argo, AWS Global Accelerator) over a private backbone to correct for BGP's latency blindspot.
</Recap>
