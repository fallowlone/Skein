---
concepts:
- cache-control
- origin-shield
- s-maxage
- stale-while-revalidate
- cache-invalidation
- cache-purge
- cache-tags
- content-hashing
- conditional-request
- etag
deepensInto:
- 04-vary-header-and-cache-keys
- 05-stale-while-revalidate-and-stampede
estMin: 12
lang: en
lessonType: topic
level: middle
order: 3
prereqs:
- 06-http-versions/04-headers-caching-cors
- 07-cdn-edge/01-cdn-what-and-why
slug: 03-tiered-cache-and-cache-control
sources:
- https://datatracker.ietf.org/doc/html/rfc7234
- https://web.dev/articles/stale-while-revalidate/
- https://docs.aws.amazon.com/AmazonCloudFront/latest/DeveloperGuide/
spiral:
- latency
status: ready
summary: How edge cache, origin shield, and Cache-Control directives cooperate so
  90%+ of requests never touch origin — and what to set per content type.
title: Tiered cache and Cache-Control
track: networking
unit: 07-cdn-edge
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";

<Hook>
Your CDN is in front of the origin, but the origin alarm is still firing on every deploy. New users miss the cache, every edge POP fetches from origin simultaneously, and suddenly 200 edge servers generate 200 parallel origin requests for the same uncached URL. This is not a bug — it is a missing architecture layer: the origin shield.
</Hook>

<Crux>Cache-Control tells each cache layer its TTL; origin shield funnels all edge misses into one origin request, preventing stampedes.</Crux>

<Explanation>

## The four-layer cache hierarchy

When a browser makes a request, four caches may answer before origin:

1. **Browser memory cache** (~sub-ms, in-tab lifetime)
2. **Browser disk cache** (~10 ms, persists across tabs)
3. **CDN edge POP cache** (~20–50 ms over the network from user)
4. **CDN origin shield / regional cache** (~50–100 ms)

Only if all four miss does the origin server answer (~100–300 ms depending on geography). Each layer obeys the same `Cache-Control` directives but applies them independently.

## Cache-Control directives

The `Cache-Control` response header tells every caching layer what to do with the response:

| Directive | Meaning |
|-----------|---------|
| `max-age=3600` | Fresh for 1 hour at **both** browser and CDN |
| `s-maxage=86400` | Fresh for 1 day at **CDN only** (browser ignores `s-maxage`) |
| `public` | Any cache (browser, CDN, proxy) may store this |
| `private` | Only the browser cache; CDN must not store |
| `no-store` | No cache anywhere may store this response |
| `no-cache` | Must revalidate with origin before serving stored copy |
| `stale-while-revalidate=604800` | Serve stale for up to 7 days while refreshing in background |
| `immutable` | Do not revalidate — content will never change at this URL |

**Most important rule:** without `Cache-Control: public` (or `s-maxage`) the CDN **passes through** — no caching happens. Origin response headers govern cacheability; the CDN cannot cache what the origin does not permit.

<NumbersCard
  id="card-cache-control-tiers"
  title="Cache-Control by content type"
  data-lesson-visual
  stats={[
    { label: "Static assets (content-hashed JS/CSS)", value: "public, max-age=31536000, immutable" },
    { label: "Images (with hash in URL)", value: "public, max-age=31536000, immutable" },
    { label: "HTML pages (frequently updated)", value: "public, max-age=300, stale-while-revalidate=3600" },
    { label: "API responses (public, read-only)", value: "public, s-maxage=60, stale-while-revalidate=600" },
    { label: "User-specific API (auth required)", value: "private, no-store" },
    { label: "Checkout / transactional pages", value: "no-store" },
  ]}
/>

## Origin shield architecture

Every CDN region may have dozens of edge POPs. Without a shield, a cache miss at all 200 POPs for a popular URL generates **200 simultaneous origin requests** — a thundering herd.

**Origin shield** (called "Tiered Cache" at Cloudflare, "Origin Shield" at Fastly, same feature at CloudFront) is a **regional intermediate cache** between edge POPs and origin. All edge POPs in a region funnel their misses through **one shield node**. The shield maintains a larger cache; it typically absorbs 90%+ of misses without contacting origin.

Workflow for a cold URL:
1. 200 edge POPs all miss simultaneously.
2. All 200 forward to the regional shield.
3. Shield has one outstanding origin request; the other 199 queue.
4. Origin responds once; shield propagates to all 199 waiting edges.
5. Origin saw **1 request** instead of 200.

## Static-asset versioning: making invalidation unnecessary

The cleverest cache trick: include the **content hash** in the filename (e.g. `app.f3a2b8c.js`). Set `max-age=31536000, immutable` (1 year). When the file changes, the hash changes, the URL changes — the cache has no entry for the new URL and fetches fresh automatically. No purge needed. Tools that do this automatically: Webpack, Vite, esbuild, Astro.

## Cache invalidation patterns (when you need them)

1. **TTL expiry** — wait for `max-age` to expire. Cheap, no API calls, but slow for urgent fixes.
2. **Explicit purge** — CDN API call: `POST /purge {"url": "/api/products"}`. Fast, but slow when purging thousands of URLs per deploy.
3. **Cache tags** — tag responses at origin (`Cache-Tag: article-1001`), purge by tag (`POST /purge {"tag": "article-1001"}`). Most flexible; requires CDN enterprise tier (Fastly, Cloudflare Enterprise).

## Conditional requests and 304 Not Modified

After a browser's local cache expires, it revalidates with `If-None-Match: <etag>`. If the response has not changed, the CDN (or origin) returns **304 Not Modified** — empty body, saves bandwidth. CDNs handle ETags transparently for cacheable responses, forwarding to origin only when the ETag actually changed.

<Misconception
  id="r-cache-purge-browser"
  title="Purging the CDN does not clear the browser cache"
>
CDN purge removes entries from edge and shield caches only. Browsers obey the `max-age` they received on first fetch. To force a fresh browser fetch, change the URL (content-hash versioning) or set a short `max-age`.
</Misconception>

<Quiz
  id="mid-quiz-1"
  pieceSlug="03-tiered-cache-and-cache-control"
  lang="en"
  question="What does the s-maxage directive do that max-age alone does not?"
  choices={[
    { label: "It sets a longer TTL for the browser cache", misconception: "Browsers ignore s-maxage; it only applies to shared caches like CDNs." },
    { label: "It overrides max-age for shared caches (CDN, proxy) while leaving the browser's max-age unaffected", correct: true },
    { label: "It disables stale-while-revalidate", misconception: "s-maxage and stale-while-revalidate are independent directives." },
    { label: "It tells the CDN to never revalidate", misconception: "That is what immutable does for browser caches; CDN revalidation policy is separate." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="03-tiered-cache-and-cache-control"
  lang="en"
  question="Why are static-asset URLs typically content-hashed (e.g. app.f3a2b8c.js)?"
  choices={[
    { label: "To prevent the browser from caching them", misconception: "The goal is the opposite — to cache aggressively for up to a year." },
    { label: "So the URL changes whenever the content changes, allowing a 1-year TTL without ever serving stale content", correct: true },
    { label: "For security audits", misconception: "Content hashes here are about cache invalidation, not auditing." },
    { label: "To compress the file", misconception: "Compression is a separate concern from filename hashing." },
  ]}
/>

<TraceScenario
  id="mid-trace-1"
  pieceSlug="03-tiered-cache-and-cache-control"
  lang="en"
  scenario="Trace a cold load of an article page through the full CDN cache hierarchy."
  steps={[
    { prompt: "Browser DNS-resolves the hostname. What does it get?", reveal: "An A record pointing to the CDN edge IP closest to the user, via Anycast or GeoDNS." },
    { prompt: "Browser opens TCP + TLS to the edge. Edge checks its local cache — cache miss. Where does the edge forward?", reveal: "To the regional origin shield. Shield checks its cache. If still a miss, shield forwards to origin." },
    { prompt: "Origin responds. What does the response carry to be cacheable?", reveal: "Cache-Control: public, max-age=3600 (or s-maxage for CDN-only TTL). An ETag for future revalidation. A Vary header listing any dimensions that affect response content." },
    { prompt: "Shield caches the response and forwards to edge. Edge caches and returns to browser. Next user requests the same URL within 3600 seconds?", reveal: "Edge cache hit — served in ~20 ms without contacting shield or origin. This continues for up to 3600 seconds (1 hour) for every subsequent identical request." },
    { prompt: "You deploy a new version of the article. Max-age is 3600 s and only 10 minutes have passed. How do users get the new version?", reveal: "Either wait 50 more minutes for natural TTL expiry, or issue a CDN purge for the article URL / cache tag. Purge propagates to all edges within seconds." },
  ]}
/>

<DragOrder
  id="mid-drag-1"
  pieceSlug="03-tiered-cache-and-cache-control"
  lang="en"
  prompt="Order Cache-Control directives from most-cacheable to least-cacheable:"
  items={[
    "public, max-age=31536000, immutable — 1 year, never re-fetched",
    "public, max-age=3600 — 1 hour at any cache",
    "public, max-age=60, stale-while-revalidate=600 — 60s fresh, then stale-serve for 10 min",
    "private, max-age=300 — browser only, 5 min",
    "no-cache — caches must revalidate every request",
    "no-store — no cache may store this at all",
  ]}
/>

</Explanation>

<KeyTakeaway>Cache-Control governs what gets cached and for how long; s-maxage targets CDNs only while max-age applies to all caches. Origin shield collapses edge misses into one origin request, preventing stampedes. Content-hashed URLs with immutable TTLs eliminate the cache invalidation problem for static assets entirely.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-tiered-cache-and-cache-control-retrieval"
  lang="en"
  questions={[
    { q: "Why is content-hashed URL versioning preferred over manual cache purges for static assets?", a: "Hashed URLs make the cache invalidation problem disappear by construction: when a CSS file changes, its hash changes, so the URL changes, and the cache has no entry for the new URL. The old URL stays cached with max-age=1 year but is never requested again. Manual purges work but are slow, expensive at scale (thousands of URLs per deploy), and create a race window between deploy and purge completion where users can receive stale content." },
    { q: "An origin shield is between edges and origin. 200 edges all miss the same URL simultaneously. How many origin requests does the shield generate?", a: "One. The shield receives all 200 edge miss requests, recognises they are all for the same URL, makes one origin request, waits for the response, then propagates it to all 200 waiting edges. Without the shield, origin would receive 200 simultaneous requests — a thundering herd that can saturate origin capacity." },
    { q: "Your HTML page has Cache-Control: public, max-age=3600. You push a hotfix deploy. Users continue seeing the old page for up to 1 hour. What is the fastest way to make all CDN edges serve the new version within seconds?", a: "Issue a CDN purge for the page URL (or its cache-tag if you use tag-based invalidation). Most CDN platforms propagate purges to all edge POPs within 1–5 seconds. Purge only clears the CDN cache — browser caches still hold the old version for up to their locally-stored max-age, which is why short TTLs (max-age=300) or stale-while-revalidate work better than long ones for mutable content like HTML." },
  ]}
/>

<Recap lang="en">
The CDN cache hierarchy has four layers — browser memory, browser disk, edge POP, and origin shield — each obeying the same `Cache-Control` directives. The key directives: `max-age` sets the freshness window for all caches; `s-maxage` overrides it for shared caches (CDN) only; `private` excludes CDN caching; `no-store` forbids all caching. The origin shield prevents thundering herds by collapsing all edge misses in a region into a single origin request. Content-hashed URLs with `immutable, max-age=31536000` eliminate the cache invalidation problem for static assets entirely. For mutable content, choose between TTL expiry (simple), URL purge (fast but URL-specific), or cache-tag purge (flexible, requires enterprise tier).
</Recap>
