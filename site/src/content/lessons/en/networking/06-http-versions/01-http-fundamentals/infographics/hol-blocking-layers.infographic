infographic chart-column-simple
data
  title Head-of-line blocking points each version still has
  desc HTTP/1.1 blocks at the request layer and inherits TCP ordering; /2 fixes the HTTP layer but TCP-layer HOL remains; /3 (QUIC) fixes both
  values
    - label HTTP/1.1
      value 2
    - label HTTP/2
      value 1
    - label HTTP/3
      value 0
theme
  palette #f43f5e #f59e0b #10b981
