---
concepts:
- http
- request-response
- http1.1
- http2
- http3
- head-of-line-blocking
- parallel-connections
- alpn
deepensInto:
- 02-http2-multiplexing
- 03-http3-and-quic
estMin: 8
lang: en
lessonType: topic
level: junior
order: 1
prereqs:
- 03-tcp-handshake/01-the-three-way-handshake
- 05-tls-handshake/01-what-tls-does
slug: 01-http-fundamentals
sources:
- https://datatracker.ietf.org/doc/html/rfc9110
- https://datatracker.ietf.org/doc/html/rfc9112
- https://web.dev/articles/performance-http2/
spiral:
- multiplexing
status: ready
summary: HTTP is the protocol browsers and servers use to exchange pages, APIs, and
  assets — and its three live versions each try a different trick to beat head-of-line
  blocking.
title: 'HTTP: the request-response language of the web'
track: networking
unit: 06-http-versions
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import PersonaTag from "~/components/pedagogy/PersonaTag.astro";

<Hook>
TLS sealed the channel. TCP numbered the bytes. Now comes the protocol on top: HTTP — the language browsers and servers speak to request and receive web pages, APIs, images, fonts, and scripts. For thirty years, HTTP/1.1 handled nearly all traffic. Then browsers demanded more.
</Hook>

<Crux>Why each HTTP version exists, what each version changed, and why head-of-line blocking is the core antagonist.</Crux>

<Explanation>

## What HTTP does in one sentence

HTTP (HyperText Transfer Protocol) is the **request-response language** browsers and servers use to exchange web pages, APIs, images — everything you see in a browser. The client asks, the server answers. That's it. But *how* they ask and answer is where the versions diverge.

## The restaurant metaphor

Think of HTTP as a waiter taking orders at a restaurant.

**HTTP/1.1:** One waiter per table, one order at a time per waiter — you wait for each plate before ordering the next. To go faster, the restaurant hires six waiters per customer (six parallel connections). It works, but it's wasteful.

**HTTP/2:** One waiter takes ten orders at once and brings dishes back in whatever order the kitchen finishes them. One connection, many requests in flight simultaneously.

**HTTP/3:** Same as /2, but a delayed dish in the kitchen no longer makes other dishes wait — they come out independently. Built on a different transport (QUIC over UDP) that isolates delays per request.

<NumbersCard
  id="card-http-versions-overview"
  title="HTTP versions at a glance"
  data-lesson-visual
  stats={[
    { label: "HTTP/1.1 parallel TCP connections per origin", value: "6–8" },
    { label: "HTTP/2 streams per connection", value: "≥100 (RFC default)" },
    { label: "HTTP/3 deployment (April 2026, Cloudflare Radar)", value: "21.1% of web traffic" },
    { label: "HTTP/2 site support", value: ">95% of web" },
    { label: "Connection warm-up (HTTP/3 with resumption)", value: "0 RTT for request" },
    { label: "HPACK header savings (avg vs HTTP/1.1)", value: "~80–90% after first request" },
  ]}
/>

## One scenario end to end

You navigate to a site. The browser does [DNS](/en/networking/04-dns-resolution/), [TCP](/en/networking/03-tcp-handshake/), [TLS](/en/networking/05-tls-handshake/), then sends the first HTTP request. The page's HTML names dozens more files; the browser fires those requests using whatever HTTP version the server agreed to during **ALPN** (Application Layer Protocol Negotiation — a TLS extension that lets the client and server pick `h2`, `h3`, or `http/1.1` during the handshake). Modern sites get HTTP/2 or HTTP/3; old origins still see HTTP/1.1.

<PersonaTag id="bea" lang="en" /> the browser to <PersonaTag id="sven" lang="en" /> the server: "GET /index.html." Sven replies with the HTML. Bea sees `<script src="app.js">` inside and immediately says: "GET /app.js." In HTTP/1.1, Bea waits for each file to finish on the same connection before asking for the next. In HTTP/2 and /3, Bea fires all the GETs at once and Sven streams them back interleaved.

## The core antagonist: head-of-line blocking

**Head-of-line (HOL) blocking** is the pattern where a slow item at the front of a queue stalls everything behind it. HTTP/1.1 has HOL blocking at the request level — each connection can carry only one in-flight request-response at a time. HTTP/2 fixed HOL blocking at the *HTTP layer* by multiplexing streams, but TCP underneath still has HOL blocking at the *transport layer*. HTTP/3 fixed HOL at both layers by using QUIC, which has stream-level loss recovery.

<Inset kind="why" lang="en">
**Why not just open more connections?** HTTP/1.1's workaround of six parallel TCP connections costs six separate handshakes (each 1–2 RTT), six congestion windows starting from zero (TCP slow start), and server-side state for all six. On a 100 ms RTT link, those six cold-starts cost 100–200 ms each. HTTP/2's single connection pays that cost once.
</Inset>

<Quiz
  id="jr-quiz-1"
  pieceSlug="01-http-fundamentals"
  lang="en"
  question="What is the main reason HTTP/1.1 is slow for modern web pages?"
  choices={[
    { label: "Browsers do not support it well", misconception: "Browsers fully support HTTP/1.1. The protocol design is the bottleneck." },
    { label: "Each request waits for the previous response on the same connection — browsers must open many parallel connections to parallelise", correct: true },
    { label: "It encrypts everything twice", misconception: "HTTP/1.1 doesn't encrypt anything by itself; encryption is TLS's job." },
    { label: "It only works on Wi-Fi", misconception: "HTTP runs on any TCP-capable network." },
  ]}
/>

<Quiz
  id="jr-quiz-2"
  pieceSlug="01-http-fundamentals"
  lang="en"
  question="What is the headline win of HTTP/3 over HTTP/2?"
  choices={[
    { label: "Different cipher suites", misconception: "Cipher suites are TLS's territory, not HTTP's." },
    { label: "On a lossy network one lost packet no longer stalls every concurrent stream — streams are independent at the loss-recovery level", correct: true },
    { label: "Smaller URLs", misconception: "URLs are the same across HTTP versions." },
    { label: "Built-in caching", misconception: "Caching semantics are mostly the same across versions." },
  ]}
/>

<DragOrder
  id="jr-drag-1"
  pieceSlug="01-http-fundamentals"
  lang="en"
  prompt="Order the HTTP versions chronologically:"
  items={[
    "HTTP/1.0 — one request per connection, no keep-alive",
    "HTTP/1.1 — keep-alive, pipelining (rarely worked in practice)",
    "HTTP/2 — multiplexing many streams over one TCP connection + HPACK header compression",
    "HTTP/3 — same shape but over QUIC (UDP) so packet loss is per-stream",
  ]}
/>

<MetaphorComplete
  id="jr-metaphor-1"
  pieceSlug="01-http-fundamentals"
  lang="en"
  setup="Fill in the blank: HTTP is the _______ format browsers and servers use to ask for and send back web content."
  accepted={["request-response", "request response", "question-answer", "ask-and-answer"]}
  canonical="request-response"
  explanation="HTTP is fundamentally request-response: client asks, server answers. The versions change how many requests can be in flight at once."
/>

</Explanation>

<KeyTakeaway>HTTP is the request-response protocol for the web; HTTP/1.1 parallelises via 6 TCP connections, HTTP/2 multiplexes streams on one TCP (but TCP-level HOL blocking remains), and HTTP/3 moves to QUIC for stream-level loss isolation — ALPN picks the version during TLS.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-http-fundamentals-retrieval"
  lang="en"
  questions={[
    { q: "In one sentence: why did people invent HTTP/2 and HTTP/3 if HTTP/1.1 already worked?", a: "HTTP/1.1 forced browsers to open many parallel TCP connections to fetch a modern web page fast, and even with parallel connections any single slow response blocked further requests on the same line — /2 and /3 let many requests share one connection, with /3 also surviving packet loss without freezing all requests." },
    { q: "What is ALPN and when does it run?", a: "ALPN (Application Layer Protocol Negotiation) is a TLS extension: during the TLS handshake, the client advertises supported HTTP versions ([h2, http/1.1]) and the server picks one. It runs inside the TLS handshake, so there is no extra round-trip for version negotiation." },
    { q: "Why does HTTP/2 still have HOL blocking even though it multiplexes streams?", a: "HTTP/2 multiplexes at the HTTP layer but still runs over TCP, which delivers bytes strictly in order. A single lost TCP packet causes TCP to buffer all subsequent bytes until retransmission — blocking every HTTP/2 stream multiplexed on that connection. HTTP/3 escapes this because QUIC provides per-stream loss recovery." },
  ]}
/>

<Recap lang="en">
HTTP is the request-response protocol that browsers and servers use to exchange pages, APIs, and assets. HTTP/1.1 parallelises by opening 6–8 TCP connections per origin — each connection handles one request at a time. HTTP/2 introduced stream multiplexing on a single TCP connection, eliminating application-layer HOL blocking, but TCP's ordered delivery still causes transport-layer HOL blocking on packet loss. HTTP/3 moves to QUIC over UDP, giving each stream independent loss recovery. All three versions coexist: ALPN negotiates which to use during the TLS handshake on every connection.
</Recap>
