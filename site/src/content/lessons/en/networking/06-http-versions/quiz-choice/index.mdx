---
concepts:
- multiplexing
- head-of-line-blocking
- quic-stream-isolation
- hpack-qpack
- server-push
- extensible-priorities
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://datatracker.ietf.org/doc/html/rfc9113
- https://datatracker.ietf.org/doc/html/rfc9114
- https://blog.cloudflare.com/http3-the-past-present-and-future/
status: ready
summary: "Multiple-choice synthesis across the HTTP-versions unit — multiplexing, layered HOL blocking, QUIC stream isolation, header compression, and production protocol selection."
title: 'HTTP versions: multiple-choice review'
track: networking
unit: 06-http-versions
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each mirrors a protocol-selection or diagnosis call you make in production — not a definition to recite, but a tradeoff to weigh under real network conditions.
</Hook>

<Goal lang="en">
Confirm you can connect multiplexing, the two layers of head-of-line blocking, QUIC's stream isolation, header-compression ordering, and the operational signals that decide which HTTP version actually wins.
</Goal>

<Quiz
  id="06-http-versions-choice-1"
  lessonSlug="06-http-versions"
  lang="en"
  question="An API does 50 concurrent sub-requests per page. On a fibre path (loss under 0.1%) HTTP/2 is fast; on a 1.5%-loss cellular path it is measurably slower than HTTP/1.1 with 6 connections. Why?"
  choices={[
    { label: "All 50 HTTP/2 streams share one TCP connection, so one lost segment stalls every stream until retransmission. HTTP/1.1's 6 connections are independent loss-recovery domains, so a loss pauses only ~1/6 of the work. The fix is HTTP/3, not reverting to /1.1.", correct: true },
    { label: "HTTP/2 framing overhead dominates on cellular and HTTP/1.1 has none.", misconception: "The 9-byte HTTP/2 frame header is negligible. The regression is TCP-layer HOL blocking: one lost packet stalls all multiplexed streams on the single connection." },
    { label: "HPACK compression fails under packet loss, corrupting headers.", misconception: "HPACK does not corrupt under loss; TCP redelivers in order. The issue is that the shared TCP byte stream stalls every stream while the lost segment is retransmitted." },
    { label: "Cellular networks throttle HTTP/2 specifically.", misconception: "Carriers do not throttle by HTTP version. The cause is structural: a single TCP connection has one loss-recovery domain, so loss stalls every stream at once." },
  ]}
/>

<Quiz
  id="06-http-versions-choice-2"
  lessonSlug="06-http-versions"
  lang="en"
  question="HTTP/2 multiplexes independent streams, yet it still suffers head-of-line blocking. At which layer, and why does HTTP/3 escape it?"
  choices={[
    { label: "HTTP/2 removes HOL blocking at the application layer but inherits it at the transport layer: TCP delivers one in-order byte stream, so a lost segment buffers all later bytes for every stream. HTTP/3 runs on QUIC, which tracks loss per packet and recovers per stream.", correct: true },
    { label: "HTTP/2 has HOL blocking at the application layer because its streams are not truly independent.", misconception: "HTTP/2 streams are independent at the HTTP layer; interleaving is free. The remaining HOL blocking is purely transport-layer, from TCP's ordered byte stream." },
    { label: "Both /2 and /3 have identical HOL blocking; HTTP/3 just adds more streams.", misconception: "Stream count is not the point. QUIC gives each stream its own loss recovery, so a lost packet stalls only the streams whose data it carried — TCP cannot." },
    { label: "HTTP/3 escapes HOL blocking by disabling retransmission entirely.", misconception: "QUIC still retransmits lost stream data reliably. It isolates loss per stream rather than abandoning reliability." },
  ]}
/>

<Quiz
  id="06-http-versions-choice-3"
  lessonSlug="06-http-versions"
  lang="en"
  question="Why did HTTP/3 replace HPACK with QPACK, and what would happen if it had kept HPACK over QUIC?"
  choices={[
    { label: "HPACK's dynamic table is ordered and shared, so a HEADERS frame referencing an entry must be processed after the frame that added it. On QUIC's independent streams, an out-of-order or lost table-update frame would block decoding on unrelated streams — re-creating HOL blocking at the header layer. QPACK moves table updates onto a dedicated control stream and references only acknowledged entries.", correct: true },
    { label: "HPACK is slower than QPACK, so HTTP/3 needed a faster compressor.", misconception: "Compression ratio and speed are comparable. The change is about ordering: HPACK's shared ordered table defeats QUIC's stream independence; QPACK preserves it." },
    { label: "HPACK is insecure (CRIME-style) and QPACK fixes the leak.", misconception: "Both HPACK and QPACK already defend against header compression-oracle attacks. The reason for QPACK is the stream-independence ordering problem on QUIC." },
    { label: "QUIC cannot carry HPACK's static table, so a new format was required.", misconception: "QPACK actually keeps a static table (99 entries). The real driver is decoupling dynamic-table updates so one stream's headers never block another's." },
  ]}
/>

<Quiz
  id="06-http-versions-choice-4"
  lessonSlug="06-http-versions"
  lang="en"
  question="A team plans to ship HTTP/2 Server Push to preload critical CSS. What should a senior reviewer say?"
  choices={[
    { label: "Don't — Server Push is deprecated and was removed from Chrome in 2022. The server can't see the browser's cache, so it re-pushes already-cached assets and wastes bandwidth. Use 103 Early Hints plus link rel=preload, which let the browser check its cache and decide.", correct: true },
    { label: "Good idea — Server Push is the standard way to eliminate the request round-trip for critical assets.", misconception: "Push looked good on cold-cache benchmarks but lost in production: blind re-pushing of cached assets, no help on warm loads, and middlebox breakage. It is deprecated." },
    { label: "Fine, but only over HTTP/3 where push is more reliable.", misconception: "HTTP/3 does not rehabilitate push; the cache-visibility problem is identical. The modern replacement on any version is 103 Early Hints plus preload." },
    { label: "Push is fine as long as you also gzip the pushed bytes.", misconception: "Compression is unrelated. The defect is operational — the server guessing what the client cached — not the encoding of pushed bytes." },
  ]}
/>

<Quiz
  id="06-http-versions-choice-5"
  lessonSlug="06-http-versions"
  lang="en"
  question="An operator turns on HTTP/3 at the edge. Overnight the ALPN dashboard shows h3 share collapsing from 21% to 1% with no application errors. Most likely cause and right first move?"
  choices={[
    { label: "A network path started dropping UDP/443 (firewall or DDoS-scrubber rule, ISP routing change). Clients try QUIC, fail, and silently fall back to HTTP/2 — invisible to users but visible in ALPN distribution. First move: break the fallback rate down by region and ASN to localise the blocking hop.", correct: true },
    { label: "The TLS certificate expired, so HTTP/3 handshakes fail.", misconception: "An expired cert would break HTTP/2 over TCP too and surface loud errors. A silent h3-only collapse with h2 absorbing traffic points to UDP/443 being blocked on a path." },
    { label: "QPACK ran out of dynamic-table space and disabled itself.", misconception: "QPACK table exhaustion degrades compression, not protocol selection. A full ALPN collapse to h2 is a UDP-reachability problem, not a header-compression one." },
    { label: "Browsers randomly rotate between HTTP versions, so this is normal noise.", misconception: "Browsers do not randomly rotate versions; they pick h3 when Alt-Svc or SVCB advertises it and UDP works. A 20x drop is a network event, not noise." },
  ]}
/>

<Quiz
  id="06-http-versions-choice-6"
  lessonSlug="06-http-versions"
  lang="en"
  question="A real-time multiplayer backend serves 100k mostly-cellular connections. Position updates are useless if they arrive late but a missed one is harmless. Which stack fits best, and why?"
  choices={[
    { label: "HTTP/3 with WebTransport. QUIC stream isolation stops one player's loss from stalling others; connection migration survives Wi-Fi to cellular handovers without re-handshaking; and unreliable QUIC DATAGRAM frames carry position updates without retransmission, which would arrive too late anyway.", correct: true },
    { label: "HTTP/2 with WebSocket over RFC 8441 — multiplex everything on one connection.", misconception: "RFC 8441 saves connection setup but rides TCP: one lost packet on lossy cellular stalls every game-state stream. It also lacks unreliable datagrams for fast position updates." },
    { label: "HTTP/1.1 with classic WebSocket upgrade — maximum compatibility.", misconception: "Each WebSocket needs its own TCP connection paying a cold handshake on every mobile reconnect, with no loss isolation and no unreliable channel — the worst fit for lossy real-time." },
    { label: "Raw UDP with a custom protocol for full control.", misconception: "Raw UDP re-implements congestion control, encryption, NAT and mobile-firewall traversal, and anti-replay — all of which QUIC already provides. Only justified for extreme niche cases." },
  ]}
/>

<Recap lang="en">
The unit's through-line is one progression: HTTP/1.1 parallelises with many connections, HTTP/2 multiplexes streams on one TCP connection (removing application-layer HOL blocking but inheriting transport-layer HOL blocking), and HTTP/3 moves to QUIC for per-stream loss recovery, QPACK header compression, and connection migration. The production lens is constant — match the version to the network (HTTP/3 on lossy and mobile, HTTP/2 on reliable east-west), watch ALPN and h3-fallback-by-ASN as your health signal, and prefer 103 Early Hints and RFC 9218 priorities over the dead Server Push.
</Recap>
