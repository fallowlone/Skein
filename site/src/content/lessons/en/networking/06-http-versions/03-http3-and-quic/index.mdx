---
concepts:
- http3
- quic
- udp
- stream-isolation
- qpack
- connection-migration
- 0rtt
- alt-svc
- alpn-h3
- quic-packet
deepensInto:
- 06-http3-production
estMin: 14
lang: en
lessonType: topic
level: middle
order: 3
prereqs:
- 02-http2-multiplexing
- 05-tls-handshake/03-session-resumption-and-0rtt
slug: 03-http3-and-quic
sources:
- https://datatracker.ietf.org/doc/html/rfc9114
- https://datatracker.ietf.org/doc/html/rfc9000
- https://datatracker.ietf.org/doc/html/rfc9001
- https://datatracker.ietf.org/doc/html/rfc9204
- https://blog.cloudflare.com/http3-the-past-present-and-future/
spiral:
- multiplexing
status: ready
summary: HTTP/3 replaces TCP with QUIC — a UDP-based transport that gives each stream
  its own loss recovery, adds connection migration for mobile, and integrates TLS
  into the handshake.
title: 'HTTP/3 and QUIC: stream-level loss isolation'
track: networking
unit: 06-http-versions
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";

<Hook>
HTTP/2 fixed the application-layer queue. TCP re-introduced the same queue at the byte level. HTTP/3's answer: throw away TCP entirely and rebuild reliability on UDP — per stream, not per connection. One lost packet stalls one stream, not fifty.
</Hook>

<Crux>HTTP/3 runs on QUIC/UDP where each stream recovers from packet loss independently — one lost packet can't stall all streams.</Crux>

<Explanation>

## Why QUIC instead of TCP

TCP guarantees delivery in order. A packet containing bytes 1000–1099 of the stream gets lost. TCP retains incoming bytes 1100–1199 in a buffer, waiting for the retransmitted packet. Only after retransmission does the application receive bytes 1100+. At the HTTP/2 layer, if stream #1 uses bytes 1000–1099 and stream #2 uses bytes 1100–1199, **both streams block** — even though stream #2 has already arrived.

QUIC (RFC 9000) is built on **UDP** and reimplements reliability at the stream level:

- **QUIC numbers packets, not byte offsets of a unified stream.** Each packet carries frames from one or more streams, each frame tagged with its stream ID and offset.
- **Loss recovery is per stream.** When packet N is lost, only the streams whose data was in packet N retransmit. Streams whose data arrived in other packets keep flowing.
- **TLS 1.3 is built into QUIC.** The QUIC handshake and TLS handshake happen together — there is no separate TCP handshake then TLS handshake. A cold QUIC connection costs 1 RTT; with session resumption, 0 RTT for application data.

<NumbersCard
  id="card-quic-facts"
  title="QUIC and HTTP/3 key numbers"
  data-lesson-visual
  stats={[
    { label: "QUIC transport", value: "UDP port 443 (by convention)" },
    { label: "Cold QUIC connection setup", value: "1 RTT (TLS 1.3 integrated)" },
    { label: "0-RTT resumption", value: "0 RTT for request data (with session ticket)" },
    { label: "HTTP/3 streams", value: "1,000+ per connection (CDN configs)" },
    { label: "HTTP/3 share of web traffic (April 2026)", value: "21.1% (Cloudflare Radar)" },
    { label: "HTTP/3 site support", value: "38.8% of websites (W3Techs, April 2026)" },
  ]}
/>

## QPACK: header compression without stream coupling

HTTP/3 replaces HPACK with **QPACK** (RFC 9204). The problem with HPACK on QUIC: HPACK's dynamic table is ordered, so if a HEADERS frame adding entry index 60 arrives out of order (or is lost), all subsequent HEADERS frames that reference index 60 are blocked. On TCP this is invisible; on QUIC streams it would defeat stream independence.

QPACK's fix: **separate the dynamic table updates into a dedicated control stream.** Regular HEADERS frames on request streams reference only the static table or absolute indexes they know have already been acknowledged. Stream B's HEADERS only blocks on stream B's own dependencies, not on stream A's table updates.

The static table in QPACK has 99 pre-indexed entries (vs HPACK's 61), covering even more common headers and method+status combinations.

## Connection migration

QUIC connections are identified by a **connection ID** — a token chosen at connection setup — not by the 4-tuple (src IP, src port, dst IP, dst port). When a mobile device switches from WiFi to LTE (changing its local IP), the QUIC connection can migrate:

1. Client sends a PATH_CHALLENGE from the new IP.
2. Server validates and responds with PATH_RESPONSE.
3. Connection continues without re-handshaking.

HTTP/2 on TCP would lose the connection on an IP change — the 4-tuple changes, the TCP connection resets, triggering a new handshake (1–2 RTT). For mobile users, QUIC connection migration is a visible latency win on network handovers.

## HTTP/3 version negotiation

HTTP/3 cannot be negotiated via ALPN alone — QUIC packets travel on UDP, and many firewalls block UDP/443. Two discovery paths exist:

1. **Alt-Svc header:** An HTTP/1.1 or HTTP/2 response includes `Alt-Svc: h3=":443"; ma=86400`. The browser notes this and tries HTTP/3 on the *next* navigation to that origin (within `ma` seconds).
2. **HTTPS / SVCB DNS records** (RFC 9460): the server publishes a DNS record advertising HTTP/3 support, so browsers can try it even on the first connection.

On first navigation, the browser uses TCP-based HTTP/1.1 or /2, learns about /3 via Alt-Svc, then attempts /3 on subsequent navigations. If /3 fails (UDP blocked), the browser falls back silently to /2.

<TraceScenario
  id="mid-trace-h3"
  pieceSlug="03-http3-and-quic"
  lang="en"
  scenario="Trace a browser page load over HTTP/3."
  steps={[
    { prompt: "Browser has already visited the site and stored an Alt-Svc h3 hint. What happens first?", reveal: "Browser opens a QUIC connection to port 443 (UDP). The QUIC Initial packet includes a TLS 1.3 ClientHello with key shares. ALPN in the TLS extension selects h3." },
    { prompt: "Browser sends 50 stream requests. What does this look like at the QUIC level?", reveal: "Each stream is a separate QUIC stream (bidirectional, with its own stream ID). Frames from different streams interleave in QUIC packets." },
    { prompt: "One packet is dropped by the network. What happens to each stream?", reveal: "Only the streams whose data was in the lost packet wait for retransmission. Streams unaffected by that packet keep flowing — stream-level loss isolation." },
    { prompt: "Mid-page the user switches Wi-Fi to cellular (IP changes). What happens?", reveal: "QUIC connection migration: client sends a PATH_CHALLENGE from the new IP, server validates, connection continues without re-handshaking. HTTP/2 on TCP would require a new 1–2 RTT handshake." },
  ]}
/>

## 0-RTT resumption

QUIC inherits TLS 1.3's 0-RTT resumption: if the client has a cached session ticket from a previous connection, the first QUIC packet can carry application data (the HTTP request) alongside the TLS handshake material. This saves one full RTT — meaningful on intercontinental links (100–300 ms).

The same caveat as TLS 1.3 0-RTT applies: **0-RTT data is replayable**. A network attacker could replay the initial request. Servers must only honour 0-RTT for idempotent methods (GET, HEAD). For non-safe methods (POST), modern stacks respond `425 Too Early`, prompting the client to retry after the handshake completes.

<Misconception
  id="r-h3-always-wins"
  title="HTTP/3 does NOT always beat HTTP/2"
>
HTTP/3 stream isolation only matters on lossy networks. On wired links with low loss, TCP rarely retransmits. HTTP/3 adds CPU cost (QUIC runs in userspace) and complexity. For datacentre east-west on reliable links, HTTP/2 is often faster. HTTP/3 wins on mobile and long-RTT paths.
</Misconception>

<Quiz
  id="mid-quiz-1"
  pieceSlug="03-http3-and-quic"
  lang="en"
  question="Why does QUIC use packet numbers rather than byte offsets for loss recovery?"
  choices={[
    { label: "Because UDP doesn't have byte counting", misconception: "UDP can carry any payload — byte counting could be added in QUIC's framing. The choice is deliberate." },
    { label: "Because numbering packets lets each stream track only the packets carrying its data, enabling per-stream retransmission without stalling others", correct: true },
    { label: "To reduce header size", misconception: "Packet numbers don't directly reduce header size." },
    { label: "To avoid NAT rewriting", misconception: "NAT traversal is a separate concern handled by connection IDs." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="03-http3-and-quic"
  lang="en"
  question="What does a browser do when it first visits a site and HTTP/3 is available?"
  choices={[
    { label: "It connects via QUIC immediately", misconception: "The browser doesn't know about HTTP/3 until it receives an Alt-Svc header or HTTPS DNS record — these come after a first TCP connection." },
    { label: "It uses HTTP/1.1 or /2 first, learns about HTTP/3 via Alt-Svc or HTTPS record, then tries /3 on subsequent visits", correct: true },
    { label: "It tries all three versions in parallel and picks the fastest", misconception: "Browsers don't race protocols — they fall back gracefully." },
    { label: "It always upgrades via the Upgrade header", misconception: "Upgrade: works for WebSocket but not for HTTP/3 which runs on a different transport (UDP)." },
  ]}
/>

<DragOrder
  id="mid-drag-h3"
  pieceSlug="03-http3-and-quic"
  lang="en"
  prompt="Order the events in an HTTP/3 connection after a browser has already stored an Alt-Svc h3 hint:"
  items={[
    "DNS lookup resolves the origin IP",
    "Browser opens a QUIC connection (UDP) to port 443",
    "QUIC Initial packet carries TLS ClientHello with key shares",
    "Server responds: TLS ServerHello + QUIC SETTINGS",
    "Handshake completes in 1 RTT (or 0 RTT if resuming)",
    "Browser sends HEADERS frames for all page resources as QUIC streams",
    "Server sends interleaved DATA frames across all streams",
  ]}
/>

</Explanation>

<KeyTakeaway>HTTP/3 runs on QUIC (UDP) where TLS is integrated and each stream has independent loss recovery — one lost packet stalls only the streams whose data was in it. QPACK replaces HPACK to preserve stream independence. Connection migration survives IP changes without re-handshaking. 0-RTT resumes in zero round-trips but only for idempotent methods.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-http3-and-quic-retrieval"
  lang="en"
  questions={[
    { q: "Why does HTTP/3 use QPACK instead of HPACK?", a: "HPACK uses an ordered dynamic table shared across all streams. On TCP, ordering is guaranteed so a missing table entry always arrives before it's needed. On QUIC, streams can receive frames out of order — a HEADERS frame on stream A referencing dynamic table entry index 60 might arrive before the frame that added index 60. QPACK decouples table updates into a separate control stream, so regular streams only reference entries they know are already acknowledged. This preserves QUIC's stream independence at the header-compression layer." },
    { q: "How does QUIC connection migration work, and why does it matter for mobile?", a: "QUIC connections use a connection ID — not the TCP 4-tuple (src IP, src port, dst IP, dst port). When a mobile device switches from WiFi to LTE (IP changes), the client sends a PATH_CHALLENGE from the new address. The server validates with PATH_RESPONSE. The connection continues without re-handshaking (saving 1–2 RTT). On HTTP/2 over TCP, any IP change destroys the connection and requires a new handshake — visible as a network hiccup on mobile." },
    { q: "When does 0-RTT save a round-trip and when is it dangerous?", a: "0-RTT saves one RTT on resumed QUIC connections: the client sends the HTTP request in the same packet as the TLS handshake material, using a cached session ticket. It's safe for idempotent methods (GET, HEAD) where replaying the request has no side effects. It's dangerous for non-idempotent methods (POST, DELETE) because a network attacker could replay the first packet, causing the server to process the request twice. Servers should respond 425 Too Early to 0-RTT non-safe requests, forcing a retry after full handshake." },
  ]}
/>

<Recap lang="en">
HTTP/3 replaces TCP with QUIC — a transport built on UDP that integrates TLS 1.3 and provides per-stream loss recovery. A lost packet stalls only the streams whose data it carried; other streams continue unaffected, solving the TCP HOL blocking problem. QPACK replaces HPACK for header compression, decoupling dynamic table updates from stream frames. QUIC connection IDs enable connection migration when a device's IP changes (WiFi → LTE), avoiding re-handshaking. HTTP/3 is discovered via Alt-Svc headers or HTTPS DNS records — browsers fall back to HTTP/2 silently if UDP is blocked. 0-RTT resumption sends request data with the handshake but is only safe for idempotent methods.
</Recap>
