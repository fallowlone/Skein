---
concepts:
- chunked-transfer
- sse
- server-sent-events
- websocket
- rfc-8441
- grpc
- grpc-web
- trailers
- brotli
- compression
- crime-breach
- rate-limiting
- token-bucket
deepensInto:
- 07-http-design-and-security
estMin: 12
lang: en
lessonType: topic
level: middle
order: 5
prereqs:
- 02-http2-multiplexing
slug: 05-streaming-and-websockets
sources:
- https://datatracker.ietf.org/doc/html/rfc9110
- https://datatracker.ietf.org/doc/html/rfc6455
- https://datatracker.ietf.org/doc/html/rfc8441
- https://grpc.io/docs/what-is-grpc/introduction/
spiral: []
status: ready
summary: HTTP is not just request-response — chunked transfer, SSE, WebSockets, and
  gRPC each punch a hole in the single-response model for real-time and bidirectional
  communication.
title: HTTP streaming, SSE, WebSockets, and gRPC
track: networking
unit: 06-http-versions
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
REST APIs return a complete response body. But live dashboards, chat, real-time prices, and AI token streams need something different: data arriving in pieces, or bidirectional. HTTP stretches to cover these cases — through chunked transfer, SSE, WebSocket upgrades, and gRPC trailers — but each choice carries distinct tradeoffs.
</Hook>

<Crux>Chunked transfer, SSE, WebSocket, and gRPC trailers each extend HTTP past request-response — match the abstraction to your traffic shape.</Crux>

<Explanation>

## Chunked transfer encoding

What do AI token streaming, live log tailing, and generated HTML have in common? None of them know the final response size before they start. That's exactly the problem chunked transfer solves. HTTP/1.1 supports `Transfer-Encoding: chunked` for streaming responses without knowing the total length upfront. The server sends **size-prefixed chunks** until a zero-length chunk closes the body:

```
HTTP/1.1 200 OK
Transfer-Encoding: chunked
Content-Type: text/plain

5\r\n
Hello\r\n
6\r\n
 World\r\n
0\r\n
\r\n
```

This allows the server to begin sending data before it has computed the entire response — useful for generated HTML, streaming AI completions, and log tailing. HTTP/2 and HTTP/3 do not use chunked encoding; DATA frames natively handle variable-length bodies without an explicit framing layer.

## Server-Sent Events (SSE)

SSE (`Content-Type: text/event-stream`) is a long-lived HTTP response delivering JavaScript-readable events from server to browser **one-way**. The browser sends one GET request; the server keeps the connection open and pushes events:

```
HTTP/1.1 200 OK
Content-Type: text/event-stream
Cache-Control: no-cache

data: {"temp": 72.4}\n\n
data: {"temp": 72.6}\n\n
```

SSE is built on standard HTTP, travels through all proxies without protocol upgrades, reconnects automatically if the connection drops, and works on HTTP/2 (where it rides one stream among many, not blocking other requests). The limitation: **server-to-client only** — the client cannot push data back on the same SSE stream (it must open a separate request).

SSE is the right choice for: live dashboards, notification streams, AI token streaming (`text/event-stream` is how ChatGPT-style UIs stream tokens). WebSocket is the right choice for: bidirectional real-time (chat, collaborative editing, multiplayer game state).

## WebSockets

WebSockets (RFC 6455) start as HTTP/1.1 and upgrade to a **binary bidirectional frame protocol**:

1. Client sends `GET /ws HTTP/1.1` with `Upgrade: websocket` and `Connection: Upgrade`.
2. Server replies `101 Switching Protocols`.
3. The TCP connection is now a WebSocket tunnel — binary frames (text or binary) flow in both directions.

WebSockets are **incompatible with HTTP/2 multiplexing** — once a connection switches to WebSocket protocol, it is no longer an HTTP/2 connection. The fix: **RFC 8441** (WebSocket over HTTP/2) — a client sends an extended CONNECT request with `:protocol: websocket` to create a single HTTP/2 stream that behaves like a WebSocket tunnel. This lets one HTTP/2 connection multiplex many WebSocket connections alongside regular requests.

<NumbersCard
  id="card-streaming-comparison"
  title="HTTP streaming options compared"
  data-lesson-visual
  stats={[
    { label: "SSE — direction", value: "Server → Client only" },
    { label: "SSE — protocol upgrade required", value: "None — plain HTTP" },
    { label: "WebSocket — direction", value: "Bidirectional" },
    { label: "WebSocket — HTTP/2 support", value: "RFC 8441 extended CONNECT" },
    { label: "gRPC — transport", value: "HTTP/2 (always)" },
    { label: "gRPC — bidirectional streaming", value: "Yes (4 modes: unary, server-streaming, client-streaming, bidi)" },
  ]}
/>

## gRPC: trailers and HTTP/2 dependency

gRPC is a high-performance RPC framework that layers on HTTP/2. Key mechanics:

- gRPC encodes requests and responses as **Protocol Buffers** (binary serialised structs) in HTTP/2 DATA frames.
- Each gRPC call is one HTTP/2 stream. Multiple concurrent RPCs multiplex on one connection.
- **Trailers carry the result:** gRPC status and message arrive as trailing HEADERS frames *after* the DATA frames (body). The gRPC status code is separate from HTTP's own status code — gRPC always sends `200 OK` at the HTTP level; the actual result (OK, NOT_FOUND, etc.) comes in the `grpc-status` trailer.

Together these three mechanics mean gRPC's success or failure is invisible to HTTP-level monitoring unless you explicitly read trailers — without doing so, every gRPC call will look like a successful `200 OK` even when the RPC itself failed. gRPC-Web exists because browsers cannot read HTTP trailers reliably. gRPC-Web rewrites the server's trailing headers as a final **base64-encoded message in the response body**, which browsers can read. A proxy (Envoy, gRPC-Web proxy) translates between real gRPC (trailers) and gRPC-Web (body-embedded trailers).

## Compression

Servers compress response bodies via `Content-Encoding: gzip|br|zstd`. **Brotli (br)** compresses ~20% better than gzip on text, at comparable decompression speed. Servers choose based on the client's `Accept-Encoding` header.

**Security concern — CRIME and BREACH attacks:** these attacks exploited HTTPS + compression to leak session tokens by observing response size changes as an attacker injects controlled content. The mitigation: **disable compression for responses that include both attacker-controlled input and sensitive data in the same response body**. Static asset compression is safe (precomputed, no user input). Dynamic API responses mixing session tokens with query-reflected content should either skip compression or use randomised padding.

## HTTP-level rate limiting

Servers return `429 Too Many Requests` with `Retry-After: <seconds>` to throttle abusive clients. Common algorithms:

- **Token bucket:** bucket refills at a fixed rate; burst allowed up to capacity. Best for bursty-but-average-rate workloads.
- **Leaky bucket:** requests drain at a fixed rate; excess is queued or dropped. Smooths bursts.
- **Fixed window:** count requests per time window (e.g. 100/minute). Vulnerable to burst at window boundary.
- **Sliding window:** count requests over the last N seconds. More accurate, more memory.

CDNs (Cloudflare, Fastly) rate-limit at the edge before requests reach origin — the first line of defence. Origin servers add a second layer. Clients should respect `Retry-After` and use exponential back-off to avoid thundering-herd retries.

<Quiz
  id="mid-quiz-1"
  pieceSlug="05-streaming-and-websockets"
  lang="en"
  question="Why does gRPC use HTTP/2 trailers for status rather than the HTTP status code?"
  choices={[
    { label: "HTTP status codes don't have enough values", misconception: "HTTP has hundreds of status codes. The reason is about timing, not quantity." },
    { label: "The gRPC status is only known after the full response body (stream of messages) has been sent — trailers arrive after the body, making them the right place for end-of-stream metadata", correct: true },
    { label: "To avoid browser CORS restrictions", misconception: "CORS applies to any HTTP request; it's not specific to trailers." },
    { label: "Because HTTP/1.1 doesn't support trailers", misconception: "HTTP/1.1 does support trailers via chunked transfer; the reason is about stream semantics, not version support." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="05-streaming-and-websockets"
  lang="en"
  question="When should you choose SSE over WebSocket?"
  choices={[
    { label: "When you need bidirectional real-time communication", misconception: "SSE is server-to-client only. Bidirectional requires WebSocket or HTTP/3 WebTransport." },
    { label: "When the server pushes events and the client only needs to listen — SSE is simpler, works through proxies without upgrade, and auto-reconnects", correct: true },
    { label: "When you need binary message framing", misconception: "SSE sends text events. WebSocket supports binary frames natively." },
    { label: "When HTTP/2 is not available", misconception: "SSE works on both HTTP/1.1 and HTTP/2; the choice is about traffic direction, not HTTP version." },
  ]}
/>

<DragOrder
  id="mid-drag-ws"
  pieceSlug="05-streaming-and-websockets"
  lang="en"
  prompt="Order the steps in a WebSocket handshake over HTTP/1.1:"
  items={[
    "Client sends GET /ws HTTP/1.1 with Upgrade: websocket and Sec-WebSocket-Key",
    "Server replies 101 Switching Protocols with Sec-WebSocket-Accept",
    "HTTP protocol ends on this TCP connection",
    "WebSocket bidirectional frame protocol begins on the same TCP connection",
    "Both client and server can now send frames to each other at any time",
  ]}
/>

<Inset kind="why" lang="en">
**Why gRPC-Web rewrites trailers into the body.** Browser XHR and fetch() APIs cannot read HTTP trailers — the API simply doesn't expose them. Trailers are headers that arrive *after* the response body, and the browser's HTTP client hides them. gRPC-Web solves this by encoding trailers as a final framed message in the body itself, prefixed with a specific byte to distinguish it from response data. A JavaScript gRPC-Web library parses this final message to extract grpc-status. Server-side, an Envoy or nginx-grpc-web proxy translates between real gRPC (with trailers) and gRPC-Web (with body-embedded trailers).
</Inset>

</Explanation>

<SequenceDiagram
  label="WebSocket upgrade: an HTTP/1.1 GET with Upgrade header is answered by 101 Switching Protocols, after which both sides send binary frames in either direction"
  hue="var(--d-network)"
  actors={["Client", "Server"]}
  messages={[
    { from: "Client", to: "Server", label: "GET /ws · Upgrade: websocket" },
    { from: "Server", to: "Client", label: "101 Switching Protocols" },
    { from: "Client", to: "Server", label: "WS frame (e.g. chat message)" },
    { from: "Server", to: "Client", label: "WS frame (push at any time)" },
  ]}
  caption="After 101 the HTTP protocol ends on this TCP connection and a bidirectional WebSocket frame channel begins. Unlike SSE (server-to-client only), either side may send a frame at any moment."
/>

<KeyTakeaway>Chunked transfer streams variable-length HTTP responses; SSE is the right choice for server-to-client event streams (simpler, proxy-friendly, auto-reconnect); WebSocket (upgraded from HTTP) handles bidirectional traffic; gRPC runs on HTTP/2 and puts final RPC status in HTTP trailers — which gRPC-Web proxies rewrite into body frames for browser compatibility.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-streaming-and-websockets-retrieval"
  lang="en"
  questions={[
    { q: "What is the difference between SSE and WebSocket and when should you pick each?", a: "SSE (Server-Sent Events) is server-to-client only — the server keeps a long-lived HTTP response open and pushes text events. It works through all HTTP proxies without upgrades, auto-reconnects, and works on HTTP/2. WebSocket is bidirectional — after a 101 Switching Protocols upgrade, both sides can send frames freely. Pick SSE for live dashboards, notification feeds, and AI token streams where only the server sends data. Pick WebSocket for chat, collaborative editing, and multiplayer games where the client also sends data in real time." },
    { q: "Why do CRIME and BREACH attacks target HTTPS+compression and what is the mitigation?", a: "CRIME and BREACH exploited the fact that TLS compresses before encrypting (in older configs) or that the server compresses HTTP responses before encrypting. An attacker who can inject controlled text into the same response as a session token can observe response size changes — compression shrinks repeated patterns, so guessing the correct token byte causes a size drop. The mitigation: disable compression for responses that mix attacker-influenced input with sensitive data in the same body. Static assets are safe to compress; dynamic API responses mixing session tokens with reflected query strings should skip compression or pad randomised data." },
    { q: "How does RFC 8441 let WebSockets coexist with HTTP/2 multiplexing?", a: "Plain WebSocket (RFC 6455) requires HTTP/1.1 — the 101 Switching Protocols upgrade ends the HTTP protocol on that TCP connection entirely. RFC 8441 adds an extended CONNECT method in HTTP/2: the client sends CONNECT with :protocol: websocket to open a single HTTP/2 stream, and that stream becomes the WebSocket tunnel. Other HTTP/2 streams on the same connection continue normally. This means one HTTP/2 connection can multiplex dozens of regular requests alongside multiple WebSocket connections, instead of dedicating a separate TCP connection per WebSocket." },
  ]}
/>

<Recap lang="en">
HTTP streaming extends beyond single request-response via chunked transfer encoding (variable-length bodies), SSE (long-lived server-to-client text event streams), WebSocket (bidirectional binary frames via 101 upgrade, or RFC 8441 extended CONNECT on HTTP/2), and gRPC (binary RPCs multiplexed on HTTP/2, with RPC status in trailing HEADERS frames). gRPC-Web proxies rewrite trailers into body frames for browser compatibility. Compression (Brotli/gzip via Content-Encoding) saves bandwidth but requires care on responses mixing controlled input with sensitive data (CRIME/BREACH). Rate limiting via 429 + Retry-After uses token bucket or sliding window algorithms; CDN edge rate limiting is the first line of defence. Now when you design a real-time feature, you'll reach for SSE when the server pushes and the client listens — and for WebSocket only when you genuinely need bidirectional frames.
</Recap>
