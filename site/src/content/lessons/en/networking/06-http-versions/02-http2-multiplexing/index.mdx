---
concepts:
- http2
- stream-multiplexing
- hpack
- headers-frame
- data-frame
- server-push
- rst-stream
- settings-frame
- flow-control
- head-of-line-blocking
deepensInto:
- 03-http3-and-quic
- 06-http3-production
estMin: 14
lang: en
lessonType: topic
level: middle
order: 2
prereqs:
- 01-http-fundamentals
- 05-tls-handshake/02-the-1rtt-handshake
slug: 02-http2-multiplexing
sources:
- https://datatracker.ietf.org/doc/html/rfc9113
- https://datatracker.ietf.org/doc/html/rfc7541
- https://web.dev/articles/performance-http2/
- https://blog.cloudflare.com/http3-the-past-present-and-future/
spiral:
- multiplexing
status: ready
summary: HTTP/2 replaced HTTP/1.1''''s parallel-connections workaround with multiplexed
  streams on one TCP connection — HPACK compresses headers, and server push proved
  a dead end.
title: 'HTTP/2: streams, frames, and HPACK'
track: networking
unit: 06-http-versions
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import holBlastRadius from "./infographics/hol-blast-radius.svg?raw";

<Hook>
A browser loading a page with 50 sub-resources opens 6 TCP connections to the same origin, fills them with requests, and queues the rest. Each connection pays its own handshake cost. HTTP/2 collapses all that into one connection with 50 concurrent streams — but the fix comes with its own failure mode.
</Hook>

<Crux>HTTP/2 multiplexes streams on one TCP connection; HPACK compresses headers; TCP-layer HOL blocking remains.</Crux>

<Explanation>

## HTTP/1.1: pipelining and six connections

Why does a modern page open six TCP connections to the same server? Not by accident — it's a workaround baked into browser engines to survive a fundamental HTTP/1.1 limitation. HTTP/1.1 (RFC 9112) sends one request per TCP connection and waits for the complete response before sending the next on that connection. To parallelise, browsers open **6–8 connections** to the same origin. A browser loading 10 critical JavaScript files opens 6 connections, sends one request on each, then queues the remaining 4. Once a response completes, the connection is freed and fetches the next file.

**Pipelining** (RFC 7230) allowed clients to send multiple requests without waiting for responses — but it rarely worked. Proxies broke it, and if request #1 stalled waiting for a slow server response, request #2 would block behind it on the same connection anyway — head-of-line (HOL) blocking on the response side. Nearly all browsers disabled pipelining by default.

## HTTP/2: the framing layer

HTTP/2 (RFC 9113) abandons the one-request-per-connection model. A **single TCP connection** carries many concurrent **streams** — each stream is an independent request-response pair.

**Frames** are the unit of transmission:

- **HEADERS frame** — opens a stream and carries request or response metadata (status, headers). A stream is created when the client sends a HEADERS frame with a new stream ID.
- **DATA frames** — carry body content for the stream.
- **RST_STREAM** — aborts a stream without closing the connection.
- **SETTINGS** — negotiates connection parameters (max concurrent streams, initial window size, header table size).
- **WINDOW_UPDATE** — flow control: tells the sender how many more bytes the receiver is willing to buffer.

The server can **interleave** responses from different streams: 100 bytes of stream #1's response, then 100 bytes of stream #3's, then more of stream #1 — all on the same underlying TCP connection. Order within a single stream is preserved; order between streams is up to the server.

<NumbersCard
  id="card-h2-framing"
  title="HTTP/2 framing facts"
  data-lesson-visual
  stats={[
    { label: "HTTP/2 frame header size", value: "9 bytes (type, flags, length, stream ID)" },
    { label: "Stream ID space", value: "31 bits (odd = client-initiated, even = server push)" },
    { label: "Default flow-control window", value: "65,535 bytes per stream" },
    { label: "Common server setting: max concurrent streams", value: "100 (RFC 9113 recommendation)" },
    { label: "SETTINGS frame negotiated", value: "at connection start, before first request" },
    { label: "RST_STREAM cost", value: "one frame — no TCP teardown needed" },
  ]}
/>

## HPACK header compression

When you profile an HTTP/1.1 page load, you may notice that headers alone account for tens of kilobytes of repeated data. HTTP/2 has a specific answer for this. HTTP/1.1 sends full headers on every request — typically 400–800 bytes per request even if only the path changes. HTTP/2 uses **HPACK** (RFC 7541) to compress HEADERS frames:

1. **Static table** — 61 pre-defined (name, value) pairs with common headers (`":method": "GET"`, `":status": "200"`, `"content-type": "text/html"`, etc.). A single byte index replaces the full header.
2. **Dynamic table** — entries added per connection as new headers appear. If the first request sends `user-agent: Mozilla/5.0`, the second request can reference it with a 1–2 byte index, sending only the delta.

Result: after the first request to a domain, subsequent requests reduce header overhead from ~400 bytes to **~20–50 bytes**. On a page with 50 sub-resources, that saves ~18 KB of header data.

**The catch:** the dynamic table is ordered and shared across all streams. If index 60 is added to the table and a HEADERS frame referencing index 60 is lost, subsequent HEADERS frames cannot decode index 60 until retransmission arrives. On TCP this is invisible (TCP delivers in order anyway). On QUIC-based HTTP/3, this ordering dependency breaks stream independence — which is why HTTP/3 replaced HPACK with **QPACK** (covered in the next lesson).

## HTTP/2 flow control

Each HTTP/2 connection has **connection-level** and **stream-level** flow control via WINDOW_UPDATE frames — the receiver tells the sender how many bytes it is willing to buffer. Defaults: 65,535 bytes per stream, sometimes raised to 16 MiB by modern servers for high-throughput APIs.

`SETTINGS_MAX_CONCURRENT_STREAMS` bounds how many streams a client can open simultaneously (typical: 100). Hitting the limit forces the client to queue requests, undoing some of the multiplexing benefit. Tuning: raise the window size on long-lived streams; raise max-concurrent-streams for chatty APIs.

<TraceScenario
  id="mid-trace-h2"
  pieceSlug="02-http2-multiplexing"
  lang="en"
  scenario="Trace a modern browser loading a page over HTTP/2 from clean state."
  steps={[
    { prompt: "Browser completes DNS, TCP, TLS. What does it advertise in TLS ALPN?", reveal: "ALPN extension lists [h2, http/1.1]. Server picks h2." },
    { prompt: "TLS handshake completes. What does the browser send first?", reveal: "An HTTP/2 SETTINGS frame declaring its capabilities (max concurrent streams, header table size, initial window), then a HEADERS frame for the page request." },
    { prompt: "Server responds. What does the response look like at the framing level?", reveal: "HEADERS frame with status 200 and response headers, followed by one or more DATA frames carrying the HTML body, ended with END_STREAM flag." },
    { prompt: "Browser parses the HTML and discovers 50 sub-resources. What does it do?", reveal: "Opens 50 streams on the same TCP connection by sending 50 HEADERS frames. Server interleaves DATA frames for each as it produces them." },
    { prompt: "One TCP packet is lost mid-page. What happens to all 50 streams?", reveal: "TCP stalls the byte stream until the lost packet is retransmitted. All 50 HTTP/2 streams pause — this is the TCP-layer HOL blocking that HTTP/3 exists to fix." },
  ]}
/>

## Server Push: the feature that failed

HTTP/2 introduced **Server Push** — the server could send resources the browser had not yet requested. On receiving a GET /index.html, the server could send a PUSH_PROMISE for /style.css, betting the browser would need it.

In theory, this saves a request-response round-trip. In practice it failed on three counts:

1. **The server has no visibility into what the browser already cached.** It blindly pushes /style.css even if the browser has a fresh cached copy, wasting bandwidth.
2. **Push only helps on the first navigation** — on warm loads, the browser is already caching aggressively and pushed resources arrive unrequested.
3. **Middleboxes and CDNs didn't handle push correctly**, leading to duplicate transfers.

Chrome removed Server Push in 2022. RFC 9113 deprecates PUSH_PROMISE entirely. **The modern replacement: `103 Early Hints` + `<link rel="preload">`** — the server sends a 103 informational response pointing to critical sub-resources, letting the browser decide whether to fetch them. This gives the browser agency to check its cache first.

<Misconception
  id="r-h2-push-dead"
  title="Server Push is NOT a performance win"
>
Server Push looked good on cold-cache benchmarks. In production with CDNs and warm browser caches, the server's guess about what the browser needs is wrong often enough to make Push a net loss. Use 103 Early Hints and preload link headers instead.
</Misconception>

<Quiz
  id="mid-quiz-1"
  pieceSlug="02-http2-multiplexing"
  lang="en"
  question="Why is Server Push effectively dead in 2026?"
  choices={[
    { label: "Servers cannot encrypt push frames", misconception: "Encryption works fine for push frames; the issue is operational, not cryptographic." },
    { label: "The server cannot know what the client has cached, leading to wasted bandwidth and duplicate transfers; 103 Early Hints + preload won the architectural debate", correct: true },
    { label: "It is too fast for browsers to handle", misconception: "Performance was never the limitation of Server Push." },
    { label: "It breaks TLS", misconception: "TLS is unaffected by push semantics." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="02-http2-multiplexing"
  lang="en"
  question="HTTP/2 multiplexing eliminates HOL blocking — true or false?"
  choices={[
    { label: "True — multiple streams mean no waiting", misconception: "HTTP/2 eliminates HOL at the HTTP layer but TCP underneath still has HOL blocking: one lost packet stalls all streams." },
    { label: "False — HTTP/2 eliminates application-layer HOL but TCP still has transport-layer HOL blocking on packet loss", correct: true },
    { label: "True — HPACK removes the blocking", misconception: "HPACK is header compression, unrelated to HOL blocking." },
    { label: "False — HTTP/2 has worse HOL blocking than HTTP/1.1", misconception: "HTTP/2 is better for HOL at the HTTP layer; the remaining issue is TCP-level, not introduced by HTTP/2." },
  ]}
/>

<DragOrder
  id="mid-drag-1"
  pieceSlug="02-http2-multiplexing"
  lang="en"
  prompt="Order the events a modern browser does to fetch https://example.com over HTTP/2:"
  items={[
    "DNS resolution for example.com",
    "TCP handshake",
    "TLS 1.3 handshake (ALPN selects h2)",
    "HTTP/2 SETTINGS frame exchanged",
    "HEADERS frame sent for page request",
    "DATA frames received with HTML body",
    "50 more HEADERS frames sent for sub-resources",
    "DATA frames interleaved from all 50 streams",
  ]}
/>

<TraceScenario
  id="mid-trace-hol"
  pieceSlug="02-http2-multiplexing"
  lang="en"
  scenario="Diagnose: why does this HTTP/2 API service have higher latency on mobile networks than HTTP/1.1?"
  steps={[
    { prompt: "Step 1: what network metric tells you immediately if packet loss is the problem?", reveal: "Packet loss percentage — measure via tcpdump retransmits or carrier metrics. On mobile 0.5–2% is common; on fibre <0.1%." },
    { prompt: "Step 2: you measure 1.2% loss on the mobile path. The API does 50 concurrent requests per page. What happens in HTTP/2?", reveal: "All 50 requests share one TCP connection. A single lost packet stalls the byte stream, blocking all 50 HTTP/2 streams until retransmission (~100 ms at high latency). Page load stalls visibly." },
    { prompt: "Step 3: same scenario over HTTP/1.1 with 6 parallel connections. What happens?", reveal: "6 independent TCP connections, each with its own loss recovery. A lost packet on connection #1 stalls only the requests on that connection (~8 requests). Connections #2–6 continue unaffected. Only ~17% of work pauses, not 100%." },
    { prompt: "Step 4: should this API service switch back to HTTP/1.1 on mobile?", reveal: "Not necessarily. The better fix is HTTP/3: independent QUIC streams escape TCP HOL entirely. Each stream has its own retransmit timer. If HTTP/3 is unavailable (UDP blocked), HTTP/1.1 fallback is acceptable for high-loss paths." },
  ]}
/>

<Infographic
  svg={holBlastRadius}
  label="Column chart comparing how much of a 50-request page stalls when one TCP packet is lost on a 1.2%-loss mobile path: HTTP/2 on one TCP connection stalls 100 percent of work, HTTP/1.1 across six connections stalls about 17 percent."
  caption="Multiplexing's win is also its failure mode: one lost packet stalls every HTTP/2 stream sharing the connection (100%), while HTTP/1.1's six connections isolate the loss to ~17% of requests."
  hue="var(--d-infra)"
/>

</Explanation>

<SequenceDiagram
  label="HTTP/2 multiplexing: the client opens two streams over one TCP connection and the server interleaves DATA frames from both streams back"
  hue="var(--d-network)"
  actors={["Client", "Server"]}
  messages={[
    { from: "Client", to: "Server", label: "HEADERS stream 1 (GET /app.js)" },
    { from: "Client", to: "Server", label: "HEADERS stream 3 (GET /style.css)" },
    { from: "Server", to: "Client", label: "DATA stream 1 (chunk)" },
    { from: "Server", to: "Client", label: "DATA stream 3 (chunk)" },
    { from: "Server", to: "Client", label: "DATA stream 1 (chunk, END_STREAM)" },
  ]}
  caption="Both requests go out without waiting; the server interleaves DATA frames from stream 1 and stream 3 on the same TCP connection. Order within a stream is preserved; order between streams is the server's choice."
/>

<KeyTakeaway>HTTP/2 multiplexes streams on one TCP connection, cutting from 6–8 handshakes to 1 and using HPACK to shrink headers ~80–90%; the remaining weakness is TCP-layer HOL blocking — one lost packet stalls all streams — which HTTP/3 and QUIC fix. Server Push is dead; use 103 Early Hints instead.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-http2-multiplexing-retrieval"
  lang="en"
  questions={[
    { q: "Explain why HTTP/2 multiplexing does NOT fully solve head-of-line blocking.", a: "HTTP/2 multiplexes many request streams over a single TCP connection. Application-layer streams are independent, but TCP underneath delivers bytes strictly in order. When a single TCP segment is lost, TCP buffers all subsequent bytes until the missing segment is retransmitted; meanwhile every HTTP/2 stream multiplexed on that connection stalls. On a low-loss link this is invisible. On a lossy link (cellular, satellite, 1%+ packet loss) the stall hurts. HTTP/3 over QUIC fixes it because QUIC's packet-level reliability is stream-scoped: a lost packet affects only the streams whose data was in that packet." },
    { q: "What is HPACK and what is its main limitation?", a: "HPACK (RFC 7541) compresses HTTP/2 HEADERS frames using a static table of 61 common headers and a dynamic table of per-connection additions. After the first request to a domain, subsequent requests reduce header overhead from ~400 bytes to ~20–50 bytes. The limitation: the dynamic table is ordered and shared across all streams. If a HEADERS frame adding entry index 60 is lost, all subsequent HEADERS frames referencing index 60 cannot decode until retransmission arrives. On TCP this is invisible; on QUIC streams it would break independence, so HTTP/3 uses QPACK instead." },
    { q: "What replaced HTTP/2 Server Push and why does it work better?", a: "103 Early Hints + <link rel='preload'>: the server sends a 103 informational response listing critical sub-resources before the main 200 response arrives. The browser receives the hint and decides whether to fetch each resource — checking its cache first. This is better than Server Push because the browser has agency to skip resources it already cached; the server doesn't guess blindly. CDNs and proxies also handle 103 Early Hints correctly, unlike push frames." },
  ]}
/>

<Recap lang="en">
HTTP/2 introduced stream multiplexing over a single TCP connection, replacing HTTP/1.1's six-connection workaround. Frames (HEADERS, DATA, RST_STREAM, SETTINGS, WINDOW_UPDATE) are the unit of transfer; streams interleave freely on one connection. HPACK cuts header size by 80–90% using a static table and per-connection dynamic table. Flow control via WINDOW_UPDATE prevents buffer exhaustion. Server Push — the server proactively sending uncached resources — was removed from Chrome in 2022 because the server cannot know what the browser has cached; 103 Early Hints replaced it. The remaining open problem: a single lost TCP packet stalls every stream — TCP-layer HOL blocking that HTTP/3 solves with QUIC. Now when you see a page slow down exclusively on a lossy mobile link, you'll know to check whether HTTP/2's TCP-layer HOL blocking is the culprit — and whether upgrading to HTTP/3 is worth it.
</Recap>
