---
concepts:
- bufferbloat
- active-queue-management
- fq-codel
- cake
- congestion-control
- bbr
- cubic
deepensInto:
- 05-the-datacentre-fabric
estMin: 12
lang: en
lessonType: topic
level: senior
order: 4
prereqs:
- 03-latency-math
slug: 04-bufferbloat-and-congestion
sources:
- https://www.bufferbloat.net/projects/bloat/wiki/Introduction/
- https://datatracker.ietf.org/doc/html/rfc8290
- https://datatracker.ietf.org/doc/html/rfc8033
- https://research.google/pubs/bbr-congestion-based-congestion-control/
spiral:
- latency
status: ready
summary: Oversized buffers at the network edge trade packet drops for latency — a
  saturated link queues 100s of ms, breaking interactive apps while throughput numbers
  stay green.
title: Bufferbloat and congestion
track: networking
unit: 01-physical-link
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import latencyUnderLoad from "./infographics/latency-under-load.svg?raw";

<Hook>
Someone on the team is on a video call and their voice keeps freezing — but the speed test says 300 Mbps, green across the board. Meanwhile a cloud backup is uploading in the background. The throughput is fine. The latency is not. That gap has a name.
</Hook>

<Crux>Bufferbloat: oversized edge buffers absorb packets instead of dropping them, so a saturated link queues 100s of ms of latency.</Crux>

<Explanation>

## The buffer that ate your latency

A network buffer exists to smooth bursts: when packets arrive faster than the link drains them, they wait in a queue instead of being dropped. That is healthy in moderation. Bufferbloat is the failure that comes from too much of it.

The edge of the Internet — DOCSIS cable modems, LTE basebands, home routers — is where the fast side (your gigabit LAN) meets the slow side (the metered uplink). That mismatch is the bottleneck, and vendors over-provisioned the buffer at exactly that point. A modem can hold **100–500 ms** of buffered data. Under saturation, every packet behind a full backup queue waits the full depth of that buffer.

The symptom is unmistakable once you know it: idle ping is 20 ms, then someone starts an upload and ping climbs to 300–500 ms. Throughput looks perfect — the link is fully used — but every interactive packet (a DNS lookup, a SYN, a video frame) sits behind the bulk transfer. Calls stutter, games lag, web pages stall, all while the speed test reads green.

## Why TCP needs the drop

The deeper cause is a mismatch between buffers and congestion control. Loss-based TCP — CUBIC, the Linux default for years — has no other way to learn the link is full. It keeps increasing its sending window until a packet is **dropped**, reads that drop as "the pipe is full," and backs off.

A correctly sized buffer drops a packet early, when the queue is shallow, so TCP gets the congestion signal while latency is still low. An oversized buffer **absorbs** the packet instead. TCP sees no loss, assumes there is room, and pushes more data — which the giant buffer also absorbs. The window inflates until the buffer is finally, completely full, hundreds of milliseconds deep. Vendors thought "never drop a packet" was the safe choice. It is the exact opposite: by hiding the congestion signal, over-buffering guarantees the queue runs deep.

<NumbersCard
  id="card-bufferbloat"
  title="Bufferbloat quick reference"
  data-lesson-visual
  stats={[
    { label: "Edge buffer depth (DOCSIS / LTE)", value: "100–500 ms of queued data" },
    { label: "Idle vs saturated ping (no AQM)", value: "20 ms → 300–500 ms" },
    { label: "Saturated ping with fq_codel / CAKE", value: "stays under ~30 ms" },
    { label: "CUBIC over GEO (~600 ms RTT)", value: "starves — windows sent before loss returns" },
    { label: "BBR throughput recovery on GEO", value: "most of link capacity, loss-independent" },
    { label: "LEO RTT (Starlink, ~550 km orbit)", value: "~50–60 ms — CUBIC behaves terrestrial" },
  ]}
/>

## Why it persists

Bufferbloat has been understood since the 1980s, yet it is still everywhere. Three reasons:

1. **Wrong incentive.** Vendors treated packet drops as a defect and over-provisioned buffers to "avoid drops" — the wrong fix, because the drop *is* the signal.
2. **Invisible metric.** Residential users measure throughput, not latency under load. A speed test runs on an otherwise-idle link and never sees the bloat. The problem only surfaces when an interactive app fights a bulk transfer.
3. **Deployment lag.** The cure ships in firmware. It needs the router to run Smart Queue Management (SQM), and a lot of carrier-issued kit never gets that update.

The fix itself is not exotic.

## Active Queue Management

Active Queue Management (AQM) — marketed as Smart Queue Management on home routers — drops or ECN-marks packets **early and fairly**, before the queue grows deep, restoring the congestion signal TCP needs.

- **fq_codel** (RFC 8290) — flow-queued CoDel. CoDel ("Controlled Delay") watches how long packets *dwell* in the queue, not how many there are; once dwell time exceeds a target (~5 ms) for too long, it starts dropping. The flow-queue half hashes flows into separate sub-queues so one bulk upload cannot starve a latency-sensitive flow.
- **PIE** (RFC 8033) — Proportional Integral controller Enhanced. Estimates queue delay and drops with a probability tuned to hold delay near a target. PIE is the AQM mandated by DOCSIS 3.1, so it ships inside modern cable modems.
- **CAKE** — Common Applications Kept Enhanced. fq_codel plus built-in bandwidth shaping, per-host fairness, and DOCSIS/ATM framing compensation. It is the SQM most home-router projects (OpenWrt) reach for.

The shared idea: cap *queueing delay*, not queue *length*. Under full saturation, a link with CAKE or fq_codel keeps ping under ~30 ms instead of letting it balloon to 500 ms.

<Infographic
  svg={latencyUnderLoad}
  label="Column chart of ping under load: an idle link is 20 ms, the same link saturated through an oversized buffer spikes to 500 ms, and saturated with fq_codel or CAKE it stays at 30 ms."
  caption="Saturation alone does not raise latency — the oversized buffer does. AQM holds queueing delay near idle (~30 ms) on the very same saturated link that bloats to 500 ms without it."
  hue="var(--d-infra)"
/>

<Inset kind="why" lang="en">
**Why SQM has to shape below line rate.** AQM can only manage a queue it actually owns. If the bottleneck buffer lives inside the ISP's modem, your router's queue is never the one that fills — packets sail through your router and pile up downstream where you have no control. So SQM deliberately shapes egress to ~90–95% of the real uplink rate. That moves the bottleneck *back into your router*, where fq_codel or CAKE governs it. You trade a sliver of peak throughput for a queue you can actually discipline — almost always the right trade for an interactive household.
</Inset>

## When the buffer is not the problem: BBR vs CUBIC

Congestion control also breaks on long-RTT paths, and there the answer is a different algorithm rather than a different queue.

LEO satellite (Starlink, ~550 km orbit, ~50–60 ms total RTT) behaves like a terrestrial link — loss-based CUBIC works fine. GEO satellite (~36,000 km orbit, ~600 ms RTT) does not. With a 600 ms feedback loop, by the time a loss signal travels back to the sender, **multiple full windows have already been transmitted**. Loss-based control reacts far too late; CUBIC ramps slowly and never fills the pipe — it starves.

**BBR** (Bottleneck Bandwidth and Round-trip propagation time) sidesteps loss entirely. It actively probes the path's bandwidth and minimum RTT, builds a model of the bottleneck, and **paces** sends to that model. Because it does not wait for a drop, the 600 ms feedback delay no longer cripples it, and BBR recovers most of the GEO link's capacity. The same loss-independence makes BBR strong on any lossy path — cellular, congested Wi-Fi.

These two threads meet at the cell tower: mobile operators deploy CAKE-style AQM at the radio cell so the buffer for users sharing one cell stays disciplined. The pattern to remember: **BBR + AQM + small buffers** wins on high-latency or lossy paths; **CUBIC + a sanely sized buffer** is fine on terrestrial wired links.

<Quiz
  id="quiz-bufferbloat"
  pieceSlug="04-bufferbloat-and-congestion"
  lang="en"
  question="A modem buffers 300 ms of data and 'never drops a packet.' Why does this make latency worse, not better?"
  choices={[
    { label: "Dropping packets is always a defect, so a no-drop modem is strictly better", misconception: "The drop is the congestion signal loss-based TCP depends on — suppressing it is the cause of bufferbloat, not a feature." },
    { label: "Loss-based TCP only learns the link is full when a packet is dropped. A giant buffer absorbs packets instead of dropping them, so TCP keeps inflating its window until the buffer is hundreds of ms deep", correct: true },
    { label: "More buffer means slower memory, which adds latency", misconception: "Buffer memory access is nanoseconds — irrelevant. The latency is queueing delay: packets waiting behind other packets." },
    { label: "The modem CPU is too slow to forward 300 ms of packets", misconception: "Forwarding rate is not the issue; the packets are queued by design, waiting their turn behind a full buffer." },
  ]}
/>

<TradeoffMatrix
  id="tradeoff-bufferbloat"
  pieceSlug="04-bufferbloat-and-congestion"
  lang="en"
  prompt="A household runs video calls that stutter whenever a cloud backup uploads. Pick the fix."
  options={[
    { name: "Upgrade to a faster uplink tier", summary: "Buy more bandwidth.", justification: "More bandwidth raises the saturation ceiling but does not remove the oversized buffer — the backup still saturates the new link and ping still balloons. Throughput was never the problem." },
    { name: "Enable SQM (CAKE / fq_codel) on the router, shaped just below line rate", summary: "Active Queue Management.", correct: true, justification: "Moves the bottleneck into a queue the router controls, drops/marks early, and keeps queueing delay capped under ~30 ms even when the backup saturates the link. Directly targets bufferbloat." },
    { name: "Tell users to schedule backups for the night", summary: "Avoid contention.", justification: "A workaround, not a fix. It hides the symptom and breaks the moment any other bulk transfer (OS update, photo sync) runs during a call. The buffer is still bloated." },
    { name: "Switch every device to a wired connection", summary: "Remove Wi-Fi.", justification: "Helps Wi-Fi-specific contention but the edge bottleneck and its oversized buffer are upstream of the LAN entirely — wired or wireless, the modem queue still fills under saturation." },
  ]}
/>

<TraceScenario
  id="trace-bufferbloat"
  pieceSlug="04-bufferbloat-and-congestion"
  lang="en"
  scenario="A remote worker reports video calls freeze every afternoon. Speed tests look perfect. Trace the diagnosis."
  steps={[
    { prompt: "Step 1: the speed test reads 300 Mbps down / 40 Mbps up, green. What does that miss?", reveal: "A standard speed test runs on an otherwise-idle link — it measures throughput, never latency under load. Bufferbloat only appears when a bulk transfer and an interactive flow compete. The right test runs a ping while saturating the link (the bufferbloat test on Waveform or DSLReports, or a manual ping during an upload)." },
    { prompt: "Step 2: idle ping to 8.8.8.8 is 18 ms. Start a large upload and re-ping. Ping climbs to 420 ms. What is happening?", reveal: "Classic bufferbloat. The upload saturates the uplink; the modem's oversized buffer queues hundreds of ms of data. Every interactive packet — including the video call's audio frames — sits behind that queue. Throughput is fine; queueing delay is the killer. The freezes line up with afternoon backup or sync jobs." },
    { prompt: "Step 3: the router supports SQM. How do you configure it?", reveal: "Enable SQM (CAKE preferred, fq_codel as fallback) and set the shaped egress rate to ~90–95% of the measured real uplink — e.g. 36 Mbps for a 40 Mbps link. Shaping below line rate moves the bottleneck queue into the router, where CAKE governs it. Set the link-layer adaptation to DOCSIS if it is a cable connection." },
    { prompt: "Step 4: after enabling CAKE, re-run the saturated ping test. What should you see, and what did you trade?", reveal: "Saturated ping should now stay under ~30 ms instead of 420 ms — calls stop freezing. The trade: peak upload throughput drops by the ~5–10% you shaved off via shaping. For an interactive household that is the correct trade. If the modem itself is the only bottleneck and cannot be bypassed (no SQM-capable router in bridge mode), the fix needs firmware with PIE/AQM on the modem side." },
  ]}
/>

<FadedExample
  client:load
  id="fe-bbr-04"
  pieceSlug="04-bufferbloat-and-congestion"
  lang="en"
  title="BBR throughput on GEO satellite"
  steps={{
    solved: (
      <p>GEO satellite: 25 Mbps link, RTT = 600 ms. BDP = 25×10⁶ × 0.6 / 8 = <strong>1.875 MB</strong>. CUBIC's window must grow to 1.875 MB of unACKed data before any loss signal arrives. With CUBIC's default growth, this takes many round trips — the link stays underutilised. BBR probes bandwidth directly and fills the pipe without waiting for a loss.</p>
    ),
    semi: {
      prompt: <p>LEO satellite (Starlink): 150 Mbps link, RTT = 50 ms. BDP = 150×10⁶ × 0.05 / 8 = _____ KB. Is CUBIC practical here?</p>,
      blanks: [
        { id: "bdp", expected: /^937\.?5?$/, placeholder: "? KB" },
      ],
    },
    blank: {
      prompt: <p>Intercontinental fibre: 10 Gbps link, RTT = 160 ms (NY→Sydney). BDP = ? MB. Explain why a default 64 KB receive window is catastrophically small here.</p>,
      reveal: <p>BDP = 10×10⁹ × 0.16 / 8 = <strong>200 MB</strong>. A 64 KB receive window is 200 MB / 64 KB = 3,125× too small. TCP stalls after sending 64 KB, then waits 160 ms for an ACK before sending the next 64 KB — effective throughput: 64 KB / 0.16 s = ~3.2 Mbps on a 10 Gbps link. Modern kernels auto-tune buffers; BBR models BDP explicitly. Window scaling (RFC 1323) raises the cap to 1 GB.</p>,
    },
  }}
/>

</Explanation>

<FlowDiagram
  label="The bufferbloat feedback loop: an oversized buffer absorbs the packet instead of dropping it, so loss-based TCP sees no congestion signal, inflates its window, and the queue grows deeper, which loops back to feed the buffer"
  hue="var(--d-hardware)"
  perRow={4}
  nodes={[
    { id: "absorb", label: "Buffer absorbs", sub: "no drop" },
    { id: "nosignal", label: "TCP sees", sub: "no loss" },
    { id: "inflate", label: "Window", sub: "inflates" },
    { id: "deeper", label: "Queue", sub: "100s of ms" },
  ]}
  edges={[
    { from: "absorb", to: "nosignal" },
    { from: "nosignal", to: "inflate" },
    { from: "inflate", to: "deeper" },
    { from: "deeper", to: "absorb", label: "loops back" },
  ]}
  caption="Because the drop is the congestion signal, suppressing it makes the loop run away: loss-based CUBIC keeps inflating its window and the buffer keeps absorbing until queueing delay hits hundreds of ms. AQM (CAKE, fq_codel, PIE) breaks the loop by dropping early, before the queue grows deep."
/>

<KeyTakeaway>Bufferbloat trades drops for latency: oversized edge buffers hide the congestion signal loss-based TCP needs, so a saturated link queues 100s of ms. AQM (CAKE, fq_codel, PIE) drops early and caps queueing delay.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-bufferbloat-and-congestion-retrieval"
  lang="en"
  questions={[
    { q: "Explain bufferbloat and why it persists despite being a known problem since the 1980s.", a: "Bufferbloat: excessive buffering at network bottlenecks (cable modems, LTE basebands, home routers) causes hundreds of ms of queueing delay under saturation. Loss-based TCP relies on packet drops to signal congestion; oversized buffers delay the drop signal, letting TCP keep inflating its window and pushing more data into the queue, so latency balloons far beyond the actual link transit. It persists because vendors over-provisioned buffers to 'avoid drops' (the wrong fix — the drop is the signal); residential users measure throughput, not latency, so the problem is invisible until interactive apps suffer; and the fix needs router firmware with SQM (CAKE, fq_codel, PIE), which carrier-issued kit often never gets. The cure is well known; the deployment lag is the issue." },
    { q: "What does Active Queue Management do differently from a plain FIFO buffer, and name three AQM algorithms.", a: "A plain FIFO buffer only drops when it is completely full (tail drop), which on an oversized buffer means hundreds of ms of latency before TCP ever gets a signal. AQM drops or ECN-marks packets early and fairly, before the queue grows deep, so the congestion signal reaches the sender while latency is still low. The key idea is to cap queueing delay rather than queue length. Three algorithms: fq_codel (RFC 8290 — flow-queued Controlled Delay, drops when packet dwell time exceeds ~5 ms and isolates flows into sub-queues); PIE (RFC 8033 — Proportional Integral controller Enhanced, drops with a probability tuned to hold queue delay near a target, mandated by DOCSIS 3.1); and CAKE (fq_codel plus bandwidth shaping and per-host fairness, common on OpenWrt routers)." },
    { q: "Why does loss-based CUBIC starve over a GEO satellite link while BBR recovers most of the throughput?", a: "GEO satellite has ~600 ms RTT (36,000 km orbit). Loss-based CUBIC only learns the path is congested when a packet is dropped, but with a 600 ms feedback loop, multiple full windows are transmitted before any loss signal returns to the sender — the algorithm reacts far too late and never fills the pipe, so it starves. BBR (Bottleneck Bandwidth and Round-trip propagation time) does not depend on loss: it actively probes the path's bandwidth and minimum RTT, builds a model of the bottleneck, and paces sends to that model. Because it never waits for a drop, the long feedback delay no longer cripples it, and BBR recovers most of the GEO link's capacity. The same loss-independence makes BBR strong on lossy cellular and congested Wi-Fi paths. LEO links (~50–60 ms RTT) behave terrestrial, so CUBIC is fine there." },
  ]}
/>

<Recap lang="en">
Bufferbloat is oversized buffering at the network edge — DOCSIS modems, LTE basebands, home routers hold 100–500 ms of queued data. Loss-based TCP like CUBIC needs an early packet drop to sense congestion; a giant buffer absorbs the drop instead, so TCP keeps inflating its window until ping balloons from 20 ms to 300–500 ms while throughput still looks green. It persists because vendors chased "no drops," users measure throughput not latency, and the fix ships in firmware. Active Queue Management — fq_codel (RFC 8290), PIE (RFC 8033), CAKE — drops or marks early and fairly, capping queueing delay under ~30 ms even at saturation; SQM shapes below line rate to own the bottleneck queue. On long-RTT paths the answer is a different algorithm: BBR probes bandwidth and RTT instead of waiting for loss, recovering throughput on GEO satellite where CUBIC starves. Now when you see a speed test that says "green" while video calls freeze, run a latency-under-load test before touching the application — that 400 ms spike during an upload is bufferbloat, and the fix is a router setting, not a code change.
</Recap>
