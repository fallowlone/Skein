---
concepts:
- latency-floor
- shannon-capacity
- bufferbloat
- clos-fabric
- coherent-optics
- bandwidth-delay-product
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://hpbn.co/primer-on-latency-and-bandwidth/
- https://www.bufferbloat.net/projects/bloat/wiki/Introduction/
- https://en.wikipedia.org/wiki/Shannon%E2%80%93Hartley_theorem
status: ready
summary: "Multiple-choice synthesis across the physical-link unit — latency floors, Shannon ceilings, bufferbloat, RoCE fabrics, and the frontier."
title: 'The physical link: multiple-choice review'
track: networking
unit: 01-physical-link
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one mirrors a call you make in a real incident — distinguishing physics from a bug, picking the right lever, reading the failure mode — not a definition to recite.
</Hook>

<Goal lang="en">
Confirm you can connect propagation floors, the Shannon ceiling, bandwidth-delay product, bufferbloat versus long-RTT congestion control, and the datacentre fabric — the synthesis the individual lessons built toward.
</Goal>

<Quiz
  id="01-physical-link-choice-1"
  lessonSlug="01-physical-link"
  lang="en"
  question="A team upgrades its NYC→Sydney link from 10 Gbps to 100 Gbps and is surprised that page-load latency does not improve. What is the correct read?"
  choices={[
    { label: "Bandwidth and propagation delay are independent. The ~16,000 km path at ~200,000 km/s sets a ~160 ms RTT floor that no bandwidth buys down — only a shorter physical path (closer server, CDN edge) lowers it.", correct: true },
    { label: "The TCP connections need to be restarted to pick up the new link rate.", misconception: "A bandwidth upgrade takes effect on the existing path immediately; no restart changes the speed of light over 16,000 km." },
    { label: "100 Gbps optics inherently add serialisation latency that cancels the gain.", misconception: "Faster line rates lower serialisation delay, not raise it. Serialisation is microseconds here regardless; the 160 ms floor is propagation, not serialisation." },
    { label: "The bottleneck is the receive window, so more bandwidth cannot help until the window grows.", misconception: "A small window does cap throughput on a high-BDP path, but that is a throughput problem — it still does not change the propagation floor that sets first-byte latency." },
  ]}
/>

<Quiz
  id="01-physical-link-choice-2"
  lessonSlug="01-physical-link"
  lang="en"
  question="An RF team wants more Wi-Fi capacity. They can either double the channel width or push from 1024-QAM to 4096-QAM. Which moves capacity more, and why?"
  choices={[
    { label: "Doubling channel width. In Shannon's C = B·log₂(1+S/N), capacity is linear in B but logarithmic in S/N — so doubling bandwidth doubles the ceiling, while a denser constellation adds only ~2 bits/symbol and demands a much cleaner RF environment.", correct: true },
    { label: "Pushing to 4096-QAM — more bits per symbol is always the cheapest capacity.", misconception: "QAM density rides the logarithmic S/N term: 1024→4096-QAM adds only 2 bits/symbol and needs ~45 dB SNR, which most real links never sustain. Bandwidth is the linear lever." },
    { label: "They are equivalent — both double the bit rate.", misconception: "They are not equivalent. Doubling B doubles capacity; the QAM jump is a small logarithmic gain that often regresses under real-world noise." },
    { label: "Neither matters once the link is set up; throughput is fixed by the standard.", misconception: "Both B and modulation order are exactly the levers the standard exposes; SNR decides which QAM is even achievable." },
  ]}
/>

<Quiz
  id="01-physical-link-choice-3"
  lessonSlug="01-physical-link"
  lang="en"
  question="A household runs a video call that stutters whenever a cloud backup uploads, yet the speed test reads 300/40 Mbps green. Idle ping is 18 ms; under upload it climbs to 420 ms. Diagnosis and first fix?"
  choices={[
    { label: "Bufferbloat. The upload saturates the uplink and the modem's oversized buffer queues hundreds of ms; loss-based TCP never sees an early drop so it keeps the queue full. Fix: enable SQM (CAKE/fq_codel) shaped to ~90–95% of line rate so the router owns and disciplines the queue.", correct: true },
    { label: "The uplink is too slow — buy a faster tier.", misconception: "Throughput was never the problem; the speed test is green. A faster uplink raises the saturation ceiling but leaves the oversized buffer, so ping still balloons under the backup." },
    { label: "Wi-Fi interference — move everyone to wired Ethernet.", misconception: "The bloated buffer sits at the edge modem, upstream of the LAN. Wired or wireless, the modem queue still fills under saturation; wiring does not touch it." },
    { label: "Switch the call app's codec to use less bandwidth.", misconception: "A lower-bitrate codec sends fewer bytes but still queues behind the full buffer. The latency comes from queueing delay, not from the call's bandwidth." },
  ]}
/>

<Quiz
  id="01-physical-link-choice-4"
  lessonSlug="01-physical-link"
  lang="en"
  question="The same loss-based CUBIC sender runs well over a Starlink LEO link (~50 ms RTT) but starves over a GEO link (~600 ms RTT). Why, and what is the right response on GEO?"
  choices={[
    { label: "With a 600 ms feedback loop, multiple full windows are sent before any loss signal returns, so loss-based control reacts far too late and never fills the pipe. Switch to BBR, which models bottleneck bandwidth and RTT and paces sends without waiting for a drop.", correct: true },
    { label: "GEO links drop more packets, so CUBIC backs off constantly — add FEC to stop the drops.", misconception: "The problem is the length of the feedback loop, not a high loss rate. CUBIC starves because its loss signal is stale by the time it arrives, not because losses are frequent." },
    { label: "GEO bandwidth is too low for CUBIC; only more bandwidth fixes it.", misconception: "Capacity is not the issue — CUBIC underutilises the GEO pipe it has. The fix is a loss-independent algorithm (BBR), not more bandwidth." },
    { label: "Enable AQM on the GEO modem to cap queueing delay.", misconception: "AQM addresses bufferbloat (a queue problem), not a slow control loop. On GEO the issue is the algorithm reacting too late; BBR is the lever, not queue management." },
  ]}
/>

<Quiz
  id="01-physical-link-choice-5"
  lessonSlug="01-physical-link"
  lang="en"
  question="A 64-GPU training job runs 3× slow. Each leaf has 2×400G uplinks but 8×200G server NICs, and PFC PAUSE frames appear on every uplink. What is happening?"
  choices={[
    { label: "The leaf is 2:1 oversubscribed (1.6 Tbps down vs 800 Gbps up), so an all-to-all AllReduce can only push half its gradient traffic at once. The PFC pauses are RoCE backpressure from full switch buffers. Fix: add uplinks to reach 1:1 bisection bandwidth (or place the job on a non-blocking pod).", correct: true },
    { label: "The GPUs are too slow; the network is fine because PFC means no packets are dropped.", misconception: "No-drop is exactly the point of PFC, but the PAUSE frames are the symptom of congestion, not health. GPU utilisation is low because the oversubscribed fabric stalls the collective." },
    { label: "RoCE should run over TCP, which would retransmit and avoid the pauses.", misconception: "RoCE deliberately bypasses the kernel TCP stack for zero-copy RDMA; it requires lossless Ethernet (PFC/ECN). The fix is correct bisection bandwidth, not switching transport." },
    { label: "Disable PFC so packets flow freely.", misconception: "Disabling PFC makes the fabric lossy; RDMA has no retransmission, so dropped packets stall or abort the collective entirely. PFC is required — the fix is removing the oversubscription causing the backpressure." },
  ]}
/>

<Quiz
  id="01-physical-link-choice-6"
  lessonSlug="01-physical-link"
  lang="en"
  question="A high-frequency-trading firm wants the lowest possible NYC↔Chicago round-trip latency. Which physical-layer investment actually helps?"
  choices={[
    { label: "Lay a hollow-core fibre route along the shortest path. Light through air propagates at near-vacuum speed (~50% faster than solid glass), so it lowers the propagation floor itself — the one thing software cannot touch.", correct: true },
    { label: "Upgrade the existing route to 800G coherent optics.", misconception: "Coherent 800G raises throughput, but propagation delay is set by path length and signal speed. A bandwidth upgrade does not move the latency floor — the wrong lever for a latency-bound goal." },
    { label: "Add a LEO satellite backup link for a faster path.", misconception: "A ~550 km up-and-down LEO hop adds round-trip distance versus a direct ~1,200 km terrestrial route. LEO is for coverage and redundancy, not for beating fibre between two well-connected cities." },
    { label: "Switch the route's TLS handshake to post-quantum ML-KEM.", misconception: "Post-quantum crypto changes the handshake's security posture, not its propagation delay. Right call for long-term confidentiality, irrelevant to a latency objective." },
  ]}
/>

<Recap lang="en">
The through-line of the unit is one separation: the propagation floor (distance ÷ signal speed) is physics you design around, and everything above it is engineering you can fix. Shannon caps a link's bits/s (bandwidth linear, SNR logarithmic); BDP says how many bytes must be in flight to fill it; bufferbloat and long-RTT starvation are two different congestion failures with two different fixes (AQM versus BBR); the datacentre fabric trades the same bandwidth and loss budgets at hyperscale; and only a shorter path — or hollow-core fibre — ever lowers the floor.
</Recap>
