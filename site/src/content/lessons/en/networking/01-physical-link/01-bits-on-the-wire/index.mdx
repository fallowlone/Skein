---
concepts:
- physical-layer
- bandwidth
- propagation-delay
- latency-floor
- bdp
deepensInto:
- 02-modulation-and-shannon
estMin: 10
lang: en
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-bits-on-the-wire
sources:
- https://hpbn.co/primer-on-latency-and-bandwidth/
- https://www.submarinecablemap.com/
- https://en.wikipedia.org/wiki/Speed_of_light
- https://datatracker.ietf.org/doc/html/rfc1122
spiral:
- latency
status: ready
summary: Voltage, photons, and radio waves carry every byte you send. The speed of
  light through a medium sets the latency floor nothing above can escape.
title: Bits on the wire
track: networking
unit: 01-physical-link
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
You click a link. 200 milliseconds later a page appears in Tokyo. That gap is not servers being slow — it is physics. Every byte your browser sent started as a physical perturbation: voltage on copper, a laser pulse through glass, or a radio wave through air.
</Hook>

<Crux>Bandwidth sets how many bits per second a medium carries; its length and signal speed set a latency floor no protocol escapes.</Crux>

<Explanation>

## What the physical layer actually does

The physical layer has one job: turn bits into a signal, push that signal through a medium, and decode it back to bits on the other side. Every higher layer — IP, TCP, TLS, HTTP — waits for this to happen.

Three media carry virtually all the world's bits:

- **Copper wire** (Ethernet, DSL, USB) — voltage changes on metal conductors.
- **Optical fibre** (LAN, submarine cables) — laser pulses through a glass core.
- **Radio** (Wi-Fi, 4G/5G, satellite) — modulated electromagnetic waves through air.

Think of any link as a hose. It has two properties: **thickness** (bandwidth — how many bits per second fit) and **length** (propagation delay — how long it takes a bit to travel from one end to the other). Every network decision either deals with the hose being too thin, or with the hose being too long.

## The latency floor you cannot escape

Light travels at roughly 300,000 km/s in vacuum and about 200,000 km/s in glass fibre (the glass slows it by about 33%). That finite speed creates an absolute floor:

| Route | Distance | Minimum one-way | Minimum RTT |
|-------|----------|----------------|-------------|
| NYC → London | 5,500 km | ~28 ms | ~55 ms |
| NYC → Sydney | 16,000 km | ~80 ms | ~160 ms |

Real cables add routing overhead, so the actual RTT NYC → London is 70–90 ms and NYC → Sydney is 200–220 ms. No software, no hardware, no protocol makes a photon travel faster. You can only design *around* this floor.

<NumbersCard
  id="card-numbers"
  title="Typical link properties"
  data-lesson-visual
  stats={[
    { label: "Gig Ethernet", value: "1 Gb/s, 100 m" },
    { label: "10G fibre (LR)", value: "10 Gb/s, 10 km" },
    { label: "Wi-Fi 6 (5 GHz)", value: "~1 Gb/s @ 5 m" },
    { label: "Subsea optical", value: "100+ Tb/s aggregate" },
    { label: "Light in glass", value: "~200,000 km/s" },
    { label: "NYC → London RTT", value: "70–90 ms" },
  ]}
/>

## End-to-end journey of a bit

When you click a link, here is what happens at the physical layer:

1. Your OS hands packet bytes to the network driver.
2. Your NIC encodes bytes as voltage / light / radio signal.
3. The signal travels through local cable or Wi-Fi to your router.
4. Your router decodes back to bytes, processes the IP packet, re-encodes for the WAN uplink — usually fibre going to your ISP.
5. The ISP's backbone fibre carries the signal across submarine cables to the destination region.
6. Destination NIC decodes signal back to bytes; OS delivers to application.

Each hop involves decode → process → re-encode. Long-distance hops use submarine fibre with optical amplifiers (EDFAs) every ~80 km to keep the signal strong without converting to electrical.

<Quiz
  id="jr-quiz-1"
  pieceSlug="01-bits-on-the-wire"
  lang="en"
  question="What physical media carries most long-distance Internet traffic?"
  choices={[
    { label: "Copper telephone wires", misconception: "Used at the last mile and inside buildings, not for long-haul." },
    { label: "Single-mode optical fibre (laser pulses through glass), often as submarine cables crossing oceans", correct: true },
    { label: "Wi-Fi only", misconception: "Wi-Fi is the last hop to user devices, not the backbone." },
    { label: "Microwave satellite links", misconception: "Satellites carry a small fraction; submarine fibre dominates." },
  ]}
/>

<Quiz
  id="jr-quiz-2"
  pieceSlug="01-bits-on-the-wire"
  lang="en"
  question="Why is the New York → Sydney round-trip latency irreducible below ~200 ms?"
  choices={[
    { label: "Bad routing", misconception: "Routing adds milliseconds but the physics is the floor." },
    { label: "Light through glass travels ~200,000 km/s; the path is ~16,000 km, so physics alone bounds the round-trip near 200 ms", correct: true },
    { label: "Server slowness", misconception: "Server compute is a separate, smaller number." },
    { label: "DNS resolution time", misconception: "DNS is one-time, milliseconds, not 200 ms." },
  ]}
/>

<Inset kind="why" lang="en">
**Why CDNs exist.** If a user in São Paulo needs content from a server in New York, the physics give a 30 ms one-way floor. A CDN edge node in São Paulo reduces that to 2–3 ms. CDNs do not make the network faster — they shorten the physical path. Same physics, shorter hose.
</Inset>

<DragOrder
  id="jr-drag-1"
  pieceSlug="01-bits-on-the-wire"
  lang="en"
  prompt="Order the physical journey of bits from your laptop to a server overseas:"
  items={[
    "Operating system hands packet bytes to the network driver",
    "Network card encodes bytes as physical signal (voltage, light, or radio)",
    "Signal traverses local cable / fibre / wireless link to your router",
    "Router decodes, re-encodes onto the next link (often a fibre uplink to the ISP)",
    "ISP's fibre carries the signal across submarine cables to the destination region",
    "Destination's network card decodes the signal back into bytes",
    "Destination's operating system delivers the packet to the application",
  ]}
/>

<FadedExample
  client:load
  id="fe-bdp-1"
  pieceSlug="01-bits-on-the-wire"
  lang="en"
  title="Bandwidth-delay product worked example"
  steps={{
    solved: (
      <p>A 100 Mb/s link with 80 ms RTT: BDP = 100×10⁶ × 0.08 / 8 = <strong>1,000,000 bytes = ~1 MB</strong>. TCP needs a receive window of at least 1 MB to fully use this link.</p>
    ),
    semi: {
      prompt: <p>A 1 Gb/s link with 100 ms RTT. BDP = 1×10⁹ × 0.1 / 8 = _____ bytes = _____ MB.</p>,
      blanks: [
        { id: "bytes", expected: "12500000", placeholder: "? bytes" },
        { id: "mb", expected: /^12\.?5?$/, placeholder: "? MB" },
      ],
    },
    blank: {
      prompt: <p>A 10 Gb/s link with 200 ms RTT. Calculate the BDP in bytes and MB.</p>,
      reveal: <p>BDP = 10×10⁹ × 0.2 / 8 = <strong>250,000,000 bytes = 250 MB</strong>. A satellite link at 10 Gbps would need a 250 MB TCP window to be fully utilised — far beyond default values.</p>,
    },
  }}
/>

</Explanation>

<FlowDiagram
  label="Physical journey of a bit: OS to NIC encode, across the local link to a router that decodes and re-encodes, onto ISP submarine fibre, then the destination NIC decodes and the destination OS delivers"
  hue="var(--d-hardware)"
  perRow={7}
  nodes={[
    { id: "os", label: "Source OS", sub: "bytes" },
    { id: "nic", label: "NIC", sub: "encode" },
    { id: "link", label: "Local link", sub: "signal" },
    { id: "router", label: "Router", sub: "decode→encode" },
    { id: "isp", label: "ISP fibre", sub: "submarine" },
    { id: "dnic", label: "Dest NIC", sub: "decode" },
    { id: "dos", label: "Dest OS", sub: "bytes" },
  ]}
  edges={[
    { from: "os", to: "nic" },
    { from: "nic", to: "link" },
    { from: "link", to: "router" },
    { from: "router", to: "isp" },
    { from: "isp", to: "dnic" },
    { from: "dnic", to: "dos" },
  ]}
  caption="Each hop is decode → process → re-encode. Bytes become a physical signal (voltage, light, or radio) at every NIC and router, then back to bytes — and the submarine-fibre leg is where the speed-of-light latency floor is paid."
/>

<KeyTakeaway>Bandwidth is how wide the hose is (bits/s); latency is how long the hose is (time). A bigger pipe does not move the first byte faster — only a shorter path does.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-bits-on-the-wire-retrieval"
  lang="en"
  questions={[
    { q: "Why doesn't buying more bandwidth reduce page-load latency?", a: "The first byte still has to travel the cable at light speed. Bandwidth affects throughput (bytes/s), not propagation delay. Reduce latency by shortening the path (CDN) or eliminating round-trips." },
    { q: "Light travels at ~200,000 km/s in glass. What is the minimum one-way latency for a 5,500 km transatlantic cable?", a: "5500 / 200000 = 0.0275 s = ~28 ms one-way. Real RTT is ~70–90 ms due to routing and signal processing overhead." },
    { q: "What is the bandwidth-delay product and why does TCP need window scaling for high-BDP paths?", a: "BDP = bandwidth × RTT — the max bytes in flight at saturation. On a 10 Gbps / 100 ms link, BDP = 125 MB. TCP's default 64 KB window is far smaller; window scaling (RFC 1323) lets it open up to MBs, filling the pipe." },
  ]}
/>

<Recap lang="en">
Every byte travels as a physical signal — voltage on copper, photons through glass, radio waves through air. The medium has two properties: bandwidth (bits per second, set by the cable and modulation scheme) and propagation delay (distance divided by signal speed, ~200,000 km/s in fibre). Higher protocols layer on top but cannot beat these physics: a New-York-to-Sydney request will always take at least ~200 ms round-trip. Practical engineering means knowing the floor numbers: bandwidth-bound problems need more parallelism or compression; latency-bound problems need closer content (CDN) or fewer round-trips (QUIC 0-RTT, TLS resumption).
</Recap>
