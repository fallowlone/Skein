---
slug: 05-the-datacentre-fabric
lang: en
track: networking
unit: 01-physical-link
order: 5
title: "The datacentre fabric"
summary: "Clos spine-leaf topology, RoCE for GPU clusters, 800G optics, kernel-bypass NICs, and the power-and-cooling ceiling — the physical layer at hyperscale."
estMin: 13
status: ready
lessonType: topic
level: senior
prereqs: ["04-bufferbloat-and-congestion"]
deepensInto: ["06-the-physical-frontier"]
spiral: ["bandwidth"]
concepts: ["clos-topology", "spine-leaf", "ecmp", "roce", "lossless-ethernet", "smartnic-dpu", "datacentre-cooling"]
sources:
  - https://www.ieee802.org/3/df/
  - https://www.juniper.net/documentation/us/en/software/junos/cli-reference/topics/topic-map/clos-ip-fabric.html
  - https://docs.nvidia.com/networking/display/mlnxofedv461000/rdma+over+converged+ethernet+(roce)
  - https://aws.amazon.com/ec2/nitro/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import DebugLog from "~/components/pedagogy/DebugLog.astro";
import DesignPrompt from "~/components/pedagogy/DesignPrompt.astro";

<Hook>
A GPU training job that should finish in 6 hours is taking 18. The GPUs are 90% idle, waiting. Nothing is wrong with the model or the code — the bottleneck is the datacentre fabric, the physical mesh of switches and fibre that moves gradients between machines.
</Hook>

<Crux>A hyperscale fabric is a Clos spine-leaf mesh: enough cross-section bandwidth that any server can talk to any other at full rate.</Crux>

<Explanation>

## Clos topology: spine and leaf

A classic two-tier or three-tier Clos fabric replaces a single large switch — which would be a single point of failure and a hardware engineering nightmare — with a mesh of commodity switches. In a **spine-leaf** design every top-of-rack (ToR) **leaf** switch has one uplink to every **spine** switch. No two leaves are connected directly; all traffic flows through the spines.

This gives you three things at once. **Non-blocking bisection bandwidth**: if you have N leaves and M spines, and each leaf has one M-speed uplink per spine, then cutting the fabric in half exposes N/2 × M uplinks on each side — any server can saturate its NIC to any other server simultaneously, because every leaf has equal uplink capacity. **No spanning-tree loops**: ECMP (Equal-Cost Multi-Path) hashes each flow's 5-tuple across the available spine uplinks, so all paths are active simultaneously — no ports blocked by STP. **Horizontal scaling**: add more spines to add bisection bandwidth; add more leaves to add server ports. You scale without forklift upgrades.

Modern hyperscale fabrics run **400G Ethernet** between leaf and spine, sometimes already **800G** (IEEE 802.3df, finalised February 2024). The uplinks are Direct Attach Copper (DAC) cables inside a rack and pre-terminated **MTP/MPO** multi-fibre trunk cables between racks and rows — structured cabling at a scale that makes individual patch cables impractical.

<NumbersCard
  id="card-fabric"
  title="Hyperscale fabric quick reference"
  data-lesson-visual
  stats={[
    { label: "Spine↔leaf link speed (2024)", value: "400G Ethernet; 800G in leading builds" },
    { label: "Server NIC (GPU cluster)", value: "100–400 Gbps RDMA NIC" },
    { label: "Oversubscription target (AI training)", value: "1:1 (full bisection bandwidth)" },
    { label: "PFC pause storm risk (RoCE)", value: "freezes a fabric region if mis-configured" },
    { label: "GPU rack power draw", value: "50–100 kW (air cooling ceiling ~15 kW)" },
    { label: "800G Ethernet standard", value: "IEEE 802.3df, approved Feb 2024" },
  ]}
/>

## RoCE and lossless Ethernet

GPU training requires collective communication — every node needs to exchange gradient updates with every other node in operations like AllReduce. With normal TCP, the kernel copies data from GPU → CPU → NIC → network → NIC → CPU → GPU. That double copy burns CPU cycles and adds latency that serialises the collective, idle-ing the GPUs.

**RDMA (Remote Direct Memory Access)** lets a NIC write directly into a remote machine's memory with zero CPU involvement and zero copy. Over InfiniBand this was standard; over standard Ethernet it became **RoCE (RDMA over Converged Ethernet)**. The catch: RDMA has no built-in retransmission. A single dropped packet stalls or aborts the RDMA operation, so the fabric must be **lossless**.

Two mechanisms make Ethernet lossless:

- **PFC (Priority Flow Control)** — when a switch buffer fills beyond a threshold, it sends a PAUSE frame upstream on a per-traffic-class basis, applying backpressure all the way to the sender. No packet is dropped; the sending port is held off instead.
- **ECN (Explicit Congestion Notification)** — as a switch queue builds, the switch marks packets with a congestion bit (instead of dropping). The receiver reflects the mark back to the sender (DCQCN in RoCE v2), which reduces its rate before PFC has to act.

Lossless Ethernet is not automatic — it requires per-port, per-priority flow-control knobs to be tuned precisely. Over-aggressive PFC creates **pause storms** (backpressure propagates across the fabric, freezing unrelated flows) or **deadlock** (circular dependency of paused ports). This is why GPU clusters are typically on a physically isolated lossless fabric, separate from the general IP network.

<Inset kind="why" lang="en">
**Why SmartNICs and DPUs exist.** As server NICs hit 400 Gbps, the host CPU can no longer keep up with packet processing for virtualised tenants — the NIC alone can generate more interrupts than a CPU can handle. The answer is to move the work off the host entirely. A **SmartNIC** (e.g. NVIDIA ConnectX-7) handles SR-IOV, VXLAN encapsulation/decapsulation, and traffic policing in NIC silicon. A **DPU** (Data Processing Unit, e.g. AWS Nitro, NVIDIA BlueField) goes further: it runs a full OS on an ARM core inside the NIC, handles the entire VM networking stack — security groups, VPC routing, encrypted overlay — with zero CPU cycles from the tenant. AWS Nitro offloads all networking and storage I/O off the host CPU, giving tenants 100% of the CPU they pay for.
</Inset>

## Power and cooling: the real ceiling

A standard 42U rack with 1U servers draws 5–15 kW. A rack of eight H100 GPUs draws 50–100 kW. **Air cooling** moves heat by blowing air over heatsinks and out the back of the rack; it handles roughly 15 kW per rack — the ASHRAE Class A2 envelope. A 100 kW rack needs **liquid cooling**.

Two approaches dominate. **Direct-to-chip (DtC)** runs coolant through cold plates bolted directly to the GPU die and voltage regulators, removing heat where it is generated. Air still handles the remaining ~20% of rack heat (storage, NICs, fans). **Full immersion** submerges server boards in a dielectric fluid (engineered oil or a fluorocarbon). Immersion handles 100% of the rack heat with no fans, and the thermal mass of the fluid smooths out demand spikes — but it complicates maintenance and cable management.

The hard constraint for new GPU deployments is often not switch ports or fibre — it is **power capacity**. A datacentre hall sized for 10 kW/rack is already paid for in HVAC and PDUs; retrofitting it for 100 kW/rack racks means replacing power feeds, cooling loops, and possibly the building transformer. That is a multi-year capital project. Stranded power is why hyperscalers now buy or build facilities expressly for GPU density from the ground up.

<TraceScenario
  id="trace-fabric"
  pieceSlug="05-the-datacentre-fabric"
  lang="en"
  scenario="A distributed training job on 64 GPUs (8 servers × 8 GPUs) runs 3× slower than expected. Diagnose the fabric bottleneck."
  steps={[
    { prompt: "Step 1: GPU utilisation is 35%. Is the network the bottleneck?", reveal: "GPU utilisation below ~80% during training almost always means the GPUs are waiting for data — either the data loader, or the network collective. AllReduce during backprop is all-to-all, so a network stall serialises the entire training step. Check the training framework's profiler for 'NCCL' wait time — if it is > 50% of step time, the fabric is the problem." },
    { prompt: "Step 2: NCCL wait time is 65% of each training step. You see PFC PAUSE frames on every leaf uplink. What does that mean?", reveal: "PFC PAUSE frames indicate the fabric is experiencing backpressure — RoCE traffic is filling switch buffers. Either the fabric is oversubscribed (more GPU NICs than uplink bandwidth supports), or DCQCN rate limiting is misconfigured and flows are being throttled aggressively. Check if the leaf uplinks are 400G and the servers have 200 Gbps NICs: 8 servers × 200 Gbps = 1.6 Tbps down, you need 4 × 400G uplinks per leaf minimum for 1:1." },
    { prompt: "Step 3: the leaf has 2 × 400G uplinks but 8 × 200G server NICs. What is the oversubscription ratio, and what is the fix?", reveal: "Oversubscription = downlink total / uplink total = (8 × 200G) / (2 × 400G) = 1.6 Tbps / 800 Gbps = 2:1. For AllReduce on 64 GPUs, 2:1 oversubscription means only half the gradient traffic can flow simultaneously — 3× slower than a non-blocking fabric is consistent. Fix: add 2 more 400G uplinks to each leaf (reach 1:1), or route training jobs to a fabric pod that has 1:1 bisection bandwidth." },
    { prompt: "Step 4: you fix the oversubscription. Pause frames stop but training is still 1.5× slow. What next?", reveal: "With correct oversubscription, check DCQCN ECN marking thresholds. If ECN marks kick in too aggressively, RDMA senders back off before the queue is actually congested, limiting throughput. Also verify that PFC priority classes are correctly separated: RoCE traffic must be on a dedicated lossless priority class. A misconfigured priority map sends RoCE on the same class as TCP traffic, making PFC affect general traffic instead of only RDMA." },
  ]}
/>

<DebugLog
  id="debug-fabric"
  pieceSlug="05-the-datacentre-fabric"
  lang="en"
  title="Fabric health: leaf uplink diagnostics"
  logs={[
    { line: "$ ethtool -S eth0 | grep -E 'pfc_pause|ecn_marked|fec_corrected'", kind: "cmd" },
    { line: "     pfc_pause_tx_class3: 145823", kind: "warn", note: "High PFC pause TX — this port is backpressuring upstream. Check if buffer threshold is too low or if the connected NIC is overrunning." },
    { line: "     ecn_marked_tc3: 2187", kind: "info", note: "Some ECN marking is healthy — senders are being notified before PFC kicks in." },
    { line: "     fec_corrected_blocks: 0", kind: "ok", note: "Zero FEC corrections — optical signal is clean. If this were non-zero it would indicate a marginal fibre or transceiver." },
    { line: "$ mlxlink -d /dev/mst/mt4125_pciconf0 --show_fec", kind: "cmd" },
    { line: "  Effective FEC: RS-FEC (528, 514)", kind: "ok", note: "Reed-Solomon FEC active on this 400G port — correct for intra-fabric links." },
    { line: "$ show int ethernet 1/1/49 counters | grep 'input error'", kind: "cmd" },
    { line: "  input errors: 0", kind: "ok", note: "No input errors. If non-zero on a 400G link, suspect a dirty MTP/MPO connector or a bent-radius violation in the fibre trunk." },
  ]}
/>

<DesignPrompt
  id="design-fabric"
  pieceSlug="05-the-datacentre-fabric"
  lang="en"
  prompt="Design a non-blocking 512-GPU fabric for an AI training cluster. Servers have 400 Gbps RoCE NICs."
  constraints={[]}
  keyPoints={[
    "Clos spine-leaf with ECMP for non-blocking bisection bandwidth.",
    "Lossless Ethernet (PFC + ECN) is mandatory for RoCE.",
    "Liquid cooling required above the ~15 kW/rack air ceiling.",
    "Pre-terminated MTP/MPO cabling for speed and serviceability.",
    "Power feed sized for GPU draw to avoid stranded capacity.",
    "Per-port optical-power and BER monitoring catches degradation early.",
  ]}
  canonical="Clos spine-leaf fabric: 800G optical between spine and leaf, 400G from leaf to each server. ECMP across every leaf's uplinks for non-blocking bisection bandwidth. Lossless Ethernet — PFC plus ECN — on all RoCE-carrying paths, with PFC thresholds tuned to avoid pause storms and deadlock. Liquid cooling per rack (direct-to-chip or full immersion); air cooling cannot remove 50–100 kW. Cable plant: pre-terminated MTP/MPO fibre trunks for spine-leaf runs, structured fibre/Cat6a for the management network. Power: dual-feed redundant PDUs per rack, sized for GPU draw with headroom. Observability: per-port optical-power and BER counters, FEC error monitoring, traffic per priority class — catch link degradation before it becomes an outage."
/>

<FadedExample
  client:load
  title="Fabric oversubscription ratio"
  steps={[
    {
      label: "Solved — 1:1 (non-blocking) leaf",
      code: `Leaf has 8 × 25 Gbps downlinks to servers and 4 × 100 Gbps uplinks to spines.
Downlink total : 8 × 25 = 200 Gbps
Uplink total   : 4 × 100 = 400 Gbps
Oversubscription ratio = downlink / uplink = 200 / 400 = 0.5 → non-blocking

For GPU clusters the target is 1:1 (full bisection bandwidth) so no AllReduce flow ever stalls.
A 4:1 ratio (200 Gbps down, 50 Gbps up) would throttle 75% of traffic in an
all-to-all collective like AllReduce — exactly the 6 h → 18 h blowup from the Hook.`,
    },
    {
      label: "Semi-solved — 4 spines, 400G uplinks per leaf, 8 × 200G servers",
      code: `Leaf has 8 × 200 Gbps downlinks to servers and 4 × 400 Gbps uplinks to spines.
Downlink total : ??? Gbps
Uplink total   : ??? Gbps
Oversubscription = downlink / uplink = ???

// Fill in: is this fabric non-blocking for GPU training?`,
    },
    {
      label: "Blank — design a non-blocking GPU fabric tier",
      code: `// A GPU rack has 8 servers, each with a 400 Gbps RDMA NIC.
// You have 800 Gbps leaf uplinks.
// How many uplinks does the leaf need to stay non-blocking?
// What happens to AllReduce latency if oversubscription is 2:1?`,
    },
  ]}
/>

</Explanation>

<KeyTakeaway>A Clos spine-leaf fabric gives any-to-any bandwidth via ECMP; RoCE removes the kernel for GPU clusters but demands lossless Ethernet; and 50–100 kW GPU racks make liquid cooling and power capacity the real ceiling.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-the-datacentre-fabric-retrieval"
  lang="en"
  questions={[
    { q: "What is non-blocking bisection bandwidth in a Clos fabric, and how does ECMP achieve it?", a: "A Clos spine-leaf fabric is non-blocking when the total uplink capacity out of a leaf equals the total capacity down to its servers — cut the fabric in half and the two halves still exchange traffic at full rate, with no oversubscription. ECMP (Equal-Cost Multi-Path) achieves it by hashing each flow's 5-tuple and pinning that flow to one of the leaf's equal-cost uplinks, one per spine. Different flows land on different spines, so aggregate bandwidth is the sum of all uplinks, while each individual flow stays on one path and keeps its packets in order." },
    { q: "Why does RoCE require lossless Ethernet, and what two mechanisms provide it?", a: "RoCE (RDMA over Converged Ethernet) lets a NIC write directly into a remote machine's memory with no kernel and no CPU copy — but RDMA has no built-in retransmission, so a single dropped packet stalls or crashes the flow. The fabric must therefore never drop packets. Two mechanisms make Ethernet lossless: PFC (Priority Flow Control) sends PAUSE frames upstream when a switch buffer fills, applying backpressure per traffic class instead of dropping; and ECN (Explicit Congestion Notification) marks packets as queues build so senders slow down before PFC has to act. Misconfigured, they cause PFC deadlock or pause storms that freeze a fabric region." },
    { q: "Why is power and cooling now the limiting factor for datacentre GPU deployment?", a: "A traditional rack draws 5–15 kW and air cooling handles it; a GPU training rack draws 50–100 kW, far past the ~15 kW air-cooling ceiling, so liquid cooling (direct-to-chip or immersion) becomes mandatory. The hard constraint is often stranded power: an older datacentre with spare floor space and free switch ports still cannot host GPU racks if its power feed and cooling loop were sized for 10 kW racks — retrofitting means replacing PDUs, HVAC loops, and possibly the building transformer, a multi-year capital project." },
  ]}
/>

<Recap lang="en">
A hyperscale datacentre fabric is a Clos spine-leaf mesh: each top-of-rack leaf has one uplink to every spine switch, and ECMP hashes flows across those uplinks to deliver non-blocking bisection bandwidth — 400G/800G optical in the spine, 100–400G to servers. GPU clusters use RoCE to write directly into remote memory with no kernel copy, which forces lossless Ethernet via PFC and ECN; tune the flow control wrong and a pause storm freezes the fabric. The data plane is steadily moving off the host CPU into SmartNICs and DPUs — AWS Nitro offloads VM networking entirely. 800G Ethernet (IEEE 802.3df) ships today, but the binding constraint is now power: 50–100 kW GPU racks demand liquid cooling and enough provisioned power that capacity is not stranded.
</Recap>
