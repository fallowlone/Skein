---
concepts:
- bandwidth-delay-product
- crc-errors
- sqm-shaping
- oversubscription
estMin: 14
lang: en
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://hpbn.co/primer-on-latency-and-bandwidth/
- https://datatracker.ietf.org/doc/html/rfc8290
- https://www.juniper.net/documentation/us/en/software/junos/cli-reference/topics/topic-map/clos-ip-fabric.html
status: ready
summary: "Read a BDP calculation, an ethtool counter dump, an SQM shaping config, and a leaf oversubscription sum — then pick the read a senior engineer would make."
title: 'The physical link: code and trace reading'
track: networking
unit: 01-physical-link
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Physical-link problems are diagnosed in counters, configs, and back-of-envelope sums — not prose. Read each artifact, predict the behaviour, and choose the move a senior engineer makes first.
</Hook>

<Goal lang="en">
Practise the loop you run on every link incident: read the number or the config, decide whether you are fighting physics or a misconfiguration, and reach for the right lever.
</Goal>

## Snippet 1 — the receive window on a fat pipe

```python
# Intercontinental fibre: 10 Gbps link, 160 ms RTT (NYC -> Sydney)
bandwidth_bps = 10_000_000_000
rtt_s         = 0.160
bdp_bytes     = bandwidth_bps * rtt_s / 8       # bytes in flight at saturation

rcv_window    = 64 * 1024                        # legacy default, no window scaling
print(bdp_bytes)                                 # 200_000_000  -> 200 MB
print(bdp_bytes / rcv_window)                    # ~3125
```

<Quiz
  id="01-physical-link-code-1"
  lessonSlug="01-physical-link"
  lang="en"
  question="With BDP = 200 MB and a 64 KB receive window, what throughput does this connection actually get on the 10 Gbps link, and why?"
  choices={[
    { label: "About 3.2 Mbps. The sender can have only 64 KB unacknowledged, then stalls one full 160 ms RTT waiting for an ACK: 64 KB / 0.16 s ≈ 3.2 Mbps — the pipe is 3125× under-filled. Fix: enable window scaling (RFC 1323) so the window can open to the ~200 MB BDP.", correct: true },
    { label: "Near 10 Gbps — modern NICs pace sends to line rate regardless of window.", misconception: "The receive window is a hard cap on bytes in flight, independent of NIC speed. With 64 KB you can never have more than 64 KB unacknowledged, so the link sits idle most of each RTT." },
    { label: "Throughput is fine; the 64 KB window only affects latency, not bandwidth.", misconception: "On a high-BDP path the window directly bounds throughput: bytes-in-flight ÷ RTT. A window far below BDP is the classic under-utilisation bug — it costs throughput, not latency." },
    { label: "It is a propagation problem — only a shorter cable raises the throughput.", misconception: "Propagation sets the RTT, but here throughput is capped by the small window, not the floor. Window scaling fixes it with no change to the path." },
  ]}
/>

## Snippet 2 — the interface counters

```
$ ethtool -S eth0 | grep -E 'rx_crc|rx_errors|link'
     rx_crc_errors: 48213
     rx_errors:     48213
$ ethtool eth0 | grep -E 'Speed|Duplex'
     Speed: 1000Mb/s
     Duplex: Full
```

<Quiz
  id="01-physical-link-code-2"
  lessonSlug="01-physical-link"
  lang="en"
  question="The link negotiated 1 Gbps full-duplex, but rx_crc_errors is climbing steadily. What does this mean and what is the first action?"
  choices={[
    { label: "Frames are arriving but failing the CRC/FCS check — bits are being corrupted on the wire by a bad cable, dirty/failing SFP, or marginal optical power. First action: replace the cable and/or transceiver; if errors persist, measure optical RX power against the transceiver's sensitivity spec.", correct: true },
    { label: "The NIC is overloaded — CRC errors mean the CPU cannot keep up with packet processing.", misconception: "CRC errors are about signal integrity on the medium, not CPU load. A busy CPU shows drops/overruns, not invalid frame check sequences." },
    { label: "It is a software bug in the driver; reload the kernel module.", misconception: "A failing CRC means the bits physically changed in transit before the driver ever saw them. Reloading the driver cannot fix a corrupted signal on the cable." },
    { label: "Harmless — CRC errors are expected at gigabit speeds and can be ignored.", misconception: "Non-zero CRC errors mean data is silently corrupted at line rate and must be retransmitted; they are never normal on a healthy link and should be chased to root cause." },
  ]}
/>

## Snippet 3 — the SQM shaping config

```
# tc / CAKE on the WAN egress of a 40 Mbps-uplink home router
tc qdisc replace dev wan root cake bandwidth 36Mbit docsis
#                                            ^^^^^^^ shaped BELOW the 40 Mbps line rate
```

<Quiz
  id="01-physical-link-code-3"
  lessonSlug="01-physical-link"
  lang="en"
  question="Why is the CAKE shaper set to 36 Mbit on a 40 Mbps uplink instead of the full 40, and what does the `docsis` keyword do?"
  choices={[
    { label: "Shaping to ~90% of line rate moves the bottleneck queue from the ISP modem (which you cannot control) into the router, where CAKE drops/marks early and caps queueing delay. `docsis` enables DOCSIS link-layer framing compensation so per-packet overhead accounting is accurate. You trade ~10% peak throughput for a disciplined queue.", correct: true },
    { label: "36 Mbit is a typo; CAKE should always be set to the exact line rate to avoid wasting bandwidth.", misconception: "Setting it to line rate leaves the bottleneck in the modem's oversized buffer, where CAKE has no control — and bufferbloat returns. The sub-line-rate shape is the whole point of SQM." },
    { label: "The 4 Mbps headroom is reserved for the call's audio; `docsis` prioritises voice.", misconception: "CAKE does not statically reserve a slice for voice, and `docsis` is framing compensation, not a priority class. The headroom exists to keep the bottleneck inside the router." },
    { label: "Shaping below line rate only helps download; this config does nothing for the upload-saturation case.", misconception: "This is the egress (upload) qdisc — exactly the direction a backup saturates. Shaping egress below line rate is what tames upload bufferbloat." },
  ]}
/>

## Snippet 4 — the leaf oversubscription sum

```
# One leaf switch in a GPU pod
downlinks_to_servers = 8 * 200e9      # 8 servers x 200 Gbps RoCE NICs
uplinks_to_spines    = 2 * 400e9      # 2 x 400 Gbps to spines

oversub = downlinks_to_servers / uplinks_to_spines
# = 1.6 Tbps / 0.8 Tbps = 2.0   ->  2:1
```

<Quiz
  id="01-physical-link-code-4"
  lessonSlug="01-physical-link"
  lang="en"
  question="The leaf is 2:1 oversubscribed. For a 64-GPU AllReduce job, what does this predict, and how do you reach non-blocking?"
  choices={[
    { label: "AllReduce is all-to-all, so at 2:1 only half the gradient traffic can leave the leaf at once — roughly halving collective throughput and stalling GPUs, consistent with a 2–3× slowdown. Reach 1:1 by adding two more 400G uplinks per leaf (or scheduling the job onto a pod with full bisection bandwidth).", correct: true },
    { label: "2:1 is fine for training; oversubscription only matters for north-south Internet traffic.", misconception: "GPU collectives are east-west and all-to-all — the worst case for oversubscription. 2:1 directly halves the bandwidth available to AllReduce, which is why the job stalls." },
    { label: "Add more GPUs to the job to amortise the oversubscription.", misconception: "More GPUs increase the all-to-all traffic, worsening the bottleneck. The fix is more uplink bandwidth per leaf, not more endpoints behind the same uplinks." },
    { label: "Switch the NICs from 200G to 100G to match the uplinks.", misconception: "Halving NIC speed lowers the demand but also halves per-GPU bandwidth — you hide the ratio by making everything slower. Non-blocking means raising uplink capacity to match the downlinks, not crippling the servers." },
  ]}
/>

<Recap lang="en">
Every artifact pointed at the same separation: a 64 KB window on a 200 MB-BDP path is a throughput bug (raise the window, not the cable); climbing rx_crc_errors is a corrupted signal (swap the cable/SFP, never ignore it); CAKE shaped below line rate moves the bottleneck queue where you can discipline it; and a 2:1 leaf throttles an all-to-all collective by half. Read the number, decide physics-versus-config, then pick the matching lever.
</Recap>
