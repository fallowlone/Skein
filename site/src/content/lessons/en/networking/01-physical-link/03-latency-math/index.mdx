---
concepts:
- propagation-delay
- rtt
- latency-floor
- submarine-cables
- dwdm
- link-debugging
deepensInto:
- 04-bufferbloat-and-congestion
estMin: 12
lang: en
lessonType: topic
level: middle
order: 3
prereqs:
- 02-modulation-and-shannon
slug: 03-latency-math
sources:
- https://hpbn.co/primer-on-latency-and-bandwidth/
- https://www.submarinecablemap.com/
- https://en.wikipedia.org/wiki/Propagation_delay
- https://en.wikipedia.org/wiki/Submarine_communications_cable
spiral:
- latency
- rtt
status: ready
summary: Geography, medium, and hops each add measurable delay. Knowing the numbers
  lets you distinguish bugs from physics — and design around the floor you cannot
  lower.
title: Latency math
track: networking
unit: 01-physical-link
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import rttByTechnology from "./infographics/rtt-by-technology.svg?raw";

<Hook>
A support ticket says "latency spiked from 30 ms to 500 ms." Is that the database? The network? The CDN? Before you read a single log, you need the floor numbers — the latency physics forces on every route. Once you know them, anomalies become obvious.
</Hook>

<Crux>Propagation delay = distance ÷ signal speed. Everything above that floor is engineering — queues, hops, serialisation.</Crux>

<Explanation>

## The latency formula

One-way propagation delay = distance / signal speed.

Light in vacuum: 300,000 km/s. Light in glass fibre: ~200,000 km/s (the glass slows it by ~33%). These numbers give you latency floors no software can beat.

<NumbersCard
  id="card-latency"
  title="Latency floors by route"
  data-lesson-visual
  stats={[
    { label: "NYC → London (5,500 km)", value: "28 ms min → 70–90 ms real RTT" },
    { label: "NYC → Sydney (16,000 km)", value: "80 ms min → 200–220 ms real RTT" },
    { label: "Same continent (2,000 km)", value: "10 ms min → 20–30 ms RTT" },
    { label: "LAN (100 m, Cat6)", value: "<0.5 µs propagation" },
    { label: "LEO satellite (550 km alt)", value: "~20–50 ms RTT total" },
    { label: "GEO satellite (36,000 km alt)", value: "~600 ms RTT total" },
  ]}
/>

## Per-technology latency

| Technology | Typical RTT | Bandwidth | Notes |
|---|---|---|---|
| GEO satellite | ~600 ms | 25–100 Mbps | Physics: 36,000 km altitude |
| LEO satellite (Starlink) | 20–50 ms | 50–300 Mbps | Much closer orbit |
| DOCSIS cable (loaded) | 50–200 ms | 100–500 Mbps | Bufferbloat under saturation |
| 4G LTE | 30–60 ms | 10–100 Mbps | Scheduling adds to propagation |
| 5G sub-6 GHz | 15–30 ms | 100 Mbps–1 Gbps | Better scheduling |
| FTTH fibre | 2–10 ms | 1 Gbps symmetric | ISP edge to home |
| Gigabit LAN | &lt;1 ms | 1 Gbps | Within building |

Real RTT is always higher than the propagation floor because routing adds distance, each router adds a small processing delay (~1 µs for modern hardware), and queuing can add milliseconds under load.

<Infographic
  svg={rttByTechnology}
  label="Horizontal bar chart of typical real round-trip latency by link technology: GEO satellite ~600 ms, loaded DOCSIS cable ~125 ms, 4G LTE ~45 ms, LEO Starlink ~35 ms, 5G sub-6 GHz ~22 ms, FTTH fibre ~6 ms, gigabit LAN ~1 ms."
  caption="A 600x spread set by orbit altitude and medium, not bandwidth: GEO's 36,000 km dwarfs everything, and a 100 Gbps upgrade moves none of these bars — only a shorter path does."
  hue="var(--d-infra)"
/>

## Why real RTT exceeds the floor

Take NYC → London (theoretical floor: 55 ms RTT at 200,000 km/s). Real RTT is 70–90 ms — 30–60% above the floor. The excess comes from:

1. **Routing overhead**: cable routes are not straight lines; the actual cable path is longer than the great-circle distance.
2. **Router processing**: each intermediate router reads the IP header and looks up the routing table (~microseconds each, dozens of hops).
3. **Serialisation delay**: time to push a full packet (1500 bytes) onto the wire at the link rate. At 1 Gbps: 1500 × 8 / 10⁹ = 12 µs. Negligible for high-bandwidth links, significant at 10 Mbps.
4. **Queueing delay**: at any bottleneck link, packets wait behind others. Under load this can add tens of milliseconds — covered in lesson 04.

These four components stack: items 1–3 are roughly fixed for a given path, but item 4 is variable and under your control. A path that looks fine at low load can blow up to 10× its idle RTT under saturation — and that gap is almost always queueing, not physics.

## The Internet's submarine backbone

~500 submarine cables connect the continents. Key facts:

- Each cable carries 10–30 Tbps via **DWDM** (Dense Wavelength-Division Multiplexing): dozens of wavelengths on one fibre pair, each wavelength ~100–400 Gbps.
- **EDFAs** (erbium-doped fibre amplifiers) regenerate the optical signal every ~80 km without converting to electrical.
- Cable failures (anchor strikes, undersea landslides, ship anchors) happen monthly; redundancy and BGP rerouting keep traffic flowing.
- Hyperscalers (Google, Meta, Microsoft) own private cables — MAREA, Dunant, Curie — to guarantee capacity for their traffic.

<Quiz
  id="quiz-floor"
  pieceSlug="03-latency-math"
  lang="en"
  question="A developer says 'we just upgraded our NYC→Sydney link to 100 Gbps and latency didn't improve.' Why not?"
  choices={[
    { label: "They didn't restart the TCP connections", misconception: "TCP connections don't need a restart for bandwidth upgrade to take effect." },
    { label: "Bandwidth and propagation delay are independent. The upgrade increased throughput but the 16,000 km physics floor (~200 ms RTT) is unchanged — only a shorter cable or closer server reduces latency", correct: true },
    { label: "100 Gbps links always have higher latency", misconception: "Higher bandwidth links don't inherently add latency." },
    { label: "Routers can't forward 100 Gbps", misconception: "Modern routers handle 400G+ per port in production." },
  ]}
/>

## Practical debugging at the link layer

Before reaching for packet captures or service logs, spend two minutes at the physical layer — one bad cable or a NIC auto-negotiating down to 100 Mbps can look like application slowness and waste hours of debugging time.

When a network path behaves unexpectedly, these tools locate the layer:

```bash
# Linux: interface counters — errors, drops, overruns
ip -s link show eth0

# NIC settings: speed, duplex, auto-neg
ethtool eth0

# Vendor NIC statistics: CRC errors, link restarts
ethtool -S eth0 | grep -E "rx_crc|rx_error|tx_error"

# Wi-Fi signal and rate
iw dev wlan0 link

# Traceroute with ICMP timestamps (shows per-hop RTT)
traceroute -I 8.8.8.8
mtr --report 8.8.8.8
```

Interpreting what you see:
- **rx_crc_errors > 0**: frames arriving garbled — bad cable, dirty SFP, or marginal signal. Replace cable or transceiver first.
- **Link auto-negotiated to 100 Mbps** when you expected 1 Gbps: cable or port issue forced fallback. Replace the cable (Cat5e damaged pair).
- **Traceroute RTT jump at hop N**: latency added at that router or the link between hop N-1 and N. Not necessarily the router's fault — ICMP rate-limiting can make it look slow.
- **Wi-Fi "connected at 54 Mbps"**: client is using legacy 802.11g rates — very far from AP, or old device. Move AP or device closer.

<DragOrder
  id="drag-latency"
  pieceSlug="03-latency-math"
  lang="en"
  prompt="Order link technologies from highest to lowest typical real-world round-trip latency:"
  items={[
    "GEO satellite (~600 ms RTT — orbit is 36,000 km)",
    "DOCSIS cable modem under load with bufferbloat (~100–200 ms RTT)",
    "LEO satellite Starlink (~20–50 ms RTT — orbit only 550 km)",
    "4G LTE (~30–60 ms RTT)",
    "FTTH fibre home connection (~5–10 ms RTT to ISP edge)",
    "Gigabit LAN Ethernet (<1 ms RTT in building)",
  ]}
/>

<FadedExample
  client:visible
  id="fe-latency"
  pieceSlug="03-latency-math"
  lang="en"
  title="Propagation delay calculation"
  steps={{
    solved: (
      <p>NYC → London: 5,500 km, light in glass ~200,000 km/s. One-way delay = 5500 / 200000 = <strong>0.0275 s = 27.5 ms</strong>. RTT floor = 55 ms. Actual RTT ~70–90 ms due to routing + queueing overhead.</p>
    ),
    semi: {
      prompt: <p>NYC → Tokyo: approximately 10,800 km of undersea cable. One-way propagation delay = 10800 / 200000 = _____ ms. RTT floor = _____ ms.</p>,
      blanks: [
        { id: "oneway", expected: "54", placeholder: "? ms one-way" },
        { id: "rtt", expected: "108", placeholder: "? ms RTT floor" },
      ],
    },
    blank: {
      prompt: <p>Starlink LEO satellite at 550 km altitude. Signal travels up to satellite (~550 km) + back to ground station (~550 km) + ground to server (~10 ms ground network). Calculate minimum RTT.</p>,
      reveal: <p>1100 km / 300,000 km/s (signal in air ≈ vacuum speed) = 3.7 ms one-way to/from satellite. Round-trip to satellite only = ~7.3 ms. Add ~10 ms ground network → <strong>~18 ms minimum RTT</strong>. Real Starlink is 20–50 ms due to satellite routing topology and queuing — much better than GEO's 600 ms.</p>,
    },
  }}
/>

<Inset kind="why" lang="en">
**Why traceroute lies.** Many routers rate-limit ICMP packets used by traceroute — you may see `* * *` hops or unusually high RTT at a hop even though the path beyond it is fine. Use `mtr` (Matt's Traceroute) for a live view that aggregates many probes, or `traceroute -T` (TCP mode) which router ACLs less often block. Never conclude "the problem is at hop N" just because traceroute shows high RTT there unless everything beyond it is also broken.
</Inset>

</Explanation>

<FlowDiagram
  label="Real RTT is the propagation floor plus four additive delays: routing geometry, router processing, serialisation, and queueing"
  hue="var(--d-hardware)"
  perRow={5}
  nodes={[
    { id: "prop", label: "Propagation", sub: "dist ÷ speed" },
    { id: "route", label: "Routing", sub: "longer path" },
    { id: "proc", label: "Processing", sub: "~1 µs/hop" },
    { id: "ser", label: "Serialisation", sub: "pkt ÷ rate" },
    { id: "queue", label: "Queueing", sub: "ms under load" },
  ]}
  edges={[
    { from: "prop", to: "route", label: "+" },
    { from: "route", to: "proc", label: "+" },
    { from: "proc", to: "ser", label: "+" },
    { from: "ser", to: "queue", label: "= real RTT" },
  ]}
  caption="Propagation (distance ÷ ~200,000 km/s) is the irreducible floor; the four delays to its right are engineering you can measure and often reduce. NYC→London: a 55 ms floor becomes a 70–90 ms real RTT."
/>

<KeyTakeaway>Memorise the floors: 28 ms one-way across the Atlantic, 80 ms across the Pacific, ~3.7 ms to LEO satellite, ~120 ms to GEO. Every millisecond above those floors is engineering you can fix. The floors themselves you cannot.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-latency-math-retrieval"
  lang="en"
  questions={[
    { q: "Light in glass travels at ~200,000 km/s. NYC → Sydney is ~16,000 km. What is the theoretical one-way propagation delay, and why is the real RTT 200–220 ms rather than 160 ms?", a: "Theoretical one-way: 16000/200000 = 80 ms. RTT floor: 160 ms. Real RTT is 200–220 ms because: (1) actual cable routes are longer than great-circle distance; (2) each router adds processing delay; (3) queuing at congested links adds variable delay. The 25–35% overhead above the physics floor is normal for long intercontinental paths." },
    { q: "How does DWDM multiply fibre capacity without needing more fibre?", a: "DWDM (Dense Wavelength-Division Multiplexing) encodes many different wavelengths (colours) of light on one fibre simultaneously. Each wavelength carries an independent 100–400 Gbps optical channel. One fibre pair might carry 60–80 wavelengths, giving 6–30 Tbps aggregate. EDFAs amplify all wavelengths together at each amplification point. DWDM is why submarine cables can carry 10–30 Tbps despite a small physical diameter." },
    { q: "rx_crc_errors is non-zero on a 10G NIC. What does this indicate and what do you do?", a: "rx_crc_errors counts frames that arrived but had an invalid CRC — the frame check sequence (FCS) didn't match. This means bits were corrupted in transit: the most common causes are a damaged cable, a dirty or failing SFP transceiver, or marginal optical power. First action: replace the cable and/or SFP. If CRC errors stop, that was the fault. If they persist after cable replacement, check optical power with a meter — if RX power is below the transceiver's sensitivity spec, there's a fibre path problem. Never ignore non-zero CRC errors; they mean data is silently corrupted at line rate." },
  ]}
/>

<Recap lang="en">
Propagation delay = distance ÷ signal speed (~200,000 km/s in glass). The floors: 28 ms one-way transatlantic, 80 ms transpacific, ~4 ms to LEO satellite, ~120 ms to GEO. Real RTT exceeds the floor by 25–60% due to routing geometry, router processing, and queuing. The Internet's backbone is ~500 submarine cables using DWDM (dozens of wavelengths per fibre) with EDFA amplifiers every 80 km. Key debugging tools: `ip -s link show`, `ethtool -S` (CRC errors, link state), `mtr` (traceroute with aggregated probes). When traceroute shows high RTT at a single hop but the path beyond is healthy, it's usually ICMP rate-limiting — not a real bottleneck. Now when you see a latency spike, check the floor first: if the path length accounts for most of the RTT, the problem is geography — move the server closer. If the RTT is far above the floor, start with `mtr` and `ethtool -S` before touching the application.
</Recap>
