---
concepts:
- modulation
- shannon-capacity
- pam5
- ofdm
- qam
- ethernet-framing
- bdp
- fec
deepensInto:
- 03-latency-math
estMin: 12
lang: en
lessonType: topic
level: middle
order: 2
prereqs:
- 01-bits-on-the-wire
slug: 02-modulation-and-shannon
sources:
- https://hpbn.co/primer-on-latency-and-bandwidth/
- https://en.wikipedia.org/wiki/Shannon%E2%80%93Hartley_theorem
- https://en.wikipedia.org/wiki/PAM-5
- https://standards.ieee.org/ieee/802.3ab/1405/
- https://datatracker.ietf.org/doc/html/rfc9002
spiral:
- bandwidth
- snr
status: ready
summary: PAM-5 on copper, photons through glass, OFDM over radio — every medium has
  its own encoding scheme. Shannon''''s theorem sets the absolute ceiling no engineering
  can exceed.
title: Modulation and Shannon
track: networking
unit: 01-physical-link
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
Your laptop says "1 Gbps" and your phone says "Wi-Fi 6, 9.6 Gbps theoretical." Neither number comes out of thin air — it's a direct consequence of a formula Claude Shannon wrote in 1948. Every engineering decision about link speed traces back to that same equation.
</Hook>

<Crux>Every medium encodes bits differently, but Shannon's C = B·log₂(1 + S/N) sets a capacity ceiling no engineering can exceed.</Crux>

<Explanation>

## Three modulation regimes

Virtually all the world's bits travel over one of three media, each with its own encoding:

**Differential voltage on copper (Ethernet, DSL, USB).** Two wires carry mirrored signals; the receiver measures the *difference* to cancel common-mode noise. Gigabit Ethernet uses PAM-5 (Pulse Amplitude Modulation, 5 levels) at 125 MBaud per pair across all four pairs simultaneously. Five voltage levels encode 2.32 bits per symbol; the actual coding is cleverly mapped to carry ~1 Gbps after framing overhead.

**Photons through glass (single-mode and multi-mode fibre).** A laser switches on and off (or shifts phase/amplitude for coherent modulation) at billions of Hz. Single-mode fibre has a 9 µm core — light propagates without bouncing, enabling tens to thousands of kilometres. Multi-mode fibre (50/62.5 µm core) is cheaper but limited to ~500 m at 1 Gbps. Submarine cables are single-mode with erbium-doped fibre amplifiers (EDFAs) every ~80 km, regenerating the optical signal without converting to electrical.

**Modulated radio (Wi-Fi 6, 5G, satellite).** A carrier frequency in the GHz range is encoded with OFDM (Orthogonal Frequency Division Multiplexing): the spectrum is sliced into hundreds of narrow orthogonal subcarriers, each carrying a QAM constellation. 1024-QAM packs 10 bits per symbol; Wi-Fi 7 pushes 4096-QAM for 12 bits per symbol. When a client is far from the AP and SNR drops, the system falls back to lower QAM (256-QAM → QPSK) to maintain reliability.

## Shannon's theorem

Claude Shannon proved in 1948 that the maximum error-free bit rate over any channel is:

**C = B · log₂(1 + S/N)**

where B is bandwidth in Hz and S/N is the signal-to-noise ratio (linear, not dB). Two implications:

1. **Doubling bandwidth doubles capacity** — linear relationship. This is why 5G millimetre-wave (80 GHz channels) is so fast: the B term is enormous.
2. **Doubling SNR adds only ~1 bit per symbol** — logarithmic. Chasing ever-higher modulation gives diminishing returns. Going from 1024-QAM to 4096-QAM requires a pristine RF environment but yields only 2 extra bits per symbol.

<DiagramFrame
  label="Modulation maps a group of bits onto one symbol drawn from a fixed alphabet of signal levels, and Shannon's law caps the achievable bitrate at C = B times log base 2 of (1 + SNR): wider bandwidth raises the ceiling linearly while higher SNR raises it only logarithmically."
  hue="var(--d-network)"
  caption="Each symbol carries log2(levels) bits (5 levels → ~2.32 bits; 1024-QAM → 10 bits). Symbol rate is capped by bandwidth B; bits per symbol is capped by SNR. Shannon multiplies the two: more B scales capacity linearly, more SNR only logarithmically."
>
  <svg viewBox="0 0 560 300" width="560" height="300" font-family="var(--font-body)">
    <text x="16" y="24" font-size="13" fill="var(--ink)">Bits grouped, then mapped to one symbol (signal level)</text>

    <rect x="16" y="36" width="150" height="30" rx="5" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="91" y="56" text-anchor="middle" font-size="12" font-family="var(--font-mono)" fill="var(--ink)">1 0 1 1 0 …</text>

    <line x1="166" y1="51" x2="214" y2="51" stroke="var(--accent)" stroke-width="1.6" />
    <text x="190" y="44" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--accent)">map</text>

    <text x="232" y="42" font-size="11" font-family="var(--font-mono)" fill="var(--muted)">PAM-5: 5 amplitude levels</text>
    <line x1="232" y1="54" x2="392" y2="54" stroke="var(--hairline-strong)" stroke-width="0.6" />
    <line x1="252" y1="50" x2="252" y2="58" stroke="var(--muted)" stroke-width="1" />
    <line x1="288" y1="50" x2="288" y2="58" stroke="var(--muted)" stroke-width="1" />
    <line x1="312" y1="48" x2="312" y2="60" stroke="var(--accent)" stroke-width="1.6" />
    <line x1="336" y1="50" x2="336" y2="58" stroke="var(--muted)" stroke-width="1" />
    <line x1="372" y1="50" x2="372" y2="58" stroke="var(--muted)" stroke-width="1" />
    <text x="312" y="76" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--accent)">chosen level</text>
    <text x="232" y="92" font-size="11" font-family="var(--font-mono)" fill="var(--ink)">log2(5) ≈ 2.32 bits / symbol</text>

    <text x="440" y="42" font-size="11" font-family="var(--font-mono)" fill="var(--muted)">more levels</text>
    <text x="440" y="60" font-size="11" font-family="var(--font-mono)" fill="var(--ink)">1024-QAM:</text>
    <text x="440" y="78" font-size="11" font-family="var(--font-mono)" fill="var(--ink)">10 bits / symbol</text>
    <text x="440" y="96" font-size="10" font-family="var(--font-mono)" fill="var(--muted)">(needs high SNR)</text>

    <line x1="16" y1="116" x2="544" y2="116" stroke="var(--hairline-strong)" stroke-width="0.6" />

    <text x="16" y="142" font-size="13" fill="var(--ink)">Shannon capacity: the hard ceiling</text>
    <text x="16" y="170" font-size="16" font-family="var(--font-mono)" fill="var(--accent)">C = B · log2(1 + SNR)</text>

    <rect x="16" y="190" width="158" height="46" rx="5" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="95" y="209" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--ink)">B — bandwidth (Hz)</text>
    <text x="95" y="227" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--muted)">caps symbols/sec</text>

    <rect x="190" y="190" width="158" height="46" rx="5" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="269" y="209" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--ink)">SNR — signal/noise</text>
    <text x="269" y="227" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--muted)">caps bits/symbol</text>

    <line x1="348" y1="213" x2="396" y2="213" stroke="var(--accent)" stroke-width="1.6" />
    <rect x="396" y="190" width="148" height="46" rx="5" fill="var(--card)" stroke="var(--accent)" stroke-width="1.4" />
    <text x="470" y="209" text-anchor="middle" font-size="12" fill="var(--ink)">C — max bitrate</text>
    <text x="470" y="227" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--muted)">bits/sec ceiling</text>

    <text x="16" y="262" font-size="11" font-family="var(--font-mono)" fill="var(--muted)">2× B  →  2× C  (linear)</text>
    <text x="16" y="282" font-size="11" font-family="var(--font-mono)" fill="var(--muted)">2× SNR  →  +~1 bit/symbol  (logarithmic, diminishing)</text>
  </svg>
</DiagramFrame>

<NumbersCard
  id="card-modulation"
  title="Modulation quick reference"
  data-lesson-visual
  stats={[
    { label: "PAM-5 (1G Ethernet)", value: "5 levels, 125 MBaud/pair" },
    { label: "1024-QAM (Wi-Fi 6)", value: "10 bits/symbol, needs ~35 dB SNR" },
    { label: "4096-QAM (Wi-Fi 7)", value: "12 bits/symbol, needs ~45 dB SNR" },
    { label: "Shannon limit (40 dB SNR, 20 MHz)", value: "~266 Mbps theoretical max" },
    { label: "Coherent 400G optics", value: "256-QAM PAM-4 + FEC" },
    { label: "EDFA spacing (submarine)", value: "~80 km optical amplifiers" },
  ]}
/>

## Ethernet framing

At the link layer, every Ethernet frame carries:
- **14-byte header**: destination MAC (6 B) + source MAC (6 B) + EtherType (2 B).
- **Payload**: 46–1500 bytes (MTU 1500) or up to 9000 bytes for jumbo frames.
- **4-byte FCS**: CRC-32 frame check sequence. A bad FCS means the frame is dropped at the NIC.

Switches learn MAC-to-port mappings by watching source MACs on incoming frames. When the destination MAC isn't yet in the table, the switch floods the frame to all ports on the VLAN. ARP resolves IP addresses to MACs within the same L2 segment (covered later in the networking track).

## Wi-Fi essentials

Real-world Wi-Fi throughput is always lower than the theoretical "marketing" peak:
- **802.11ax (Wi-Fi 6)**: theoretical 9.6 Gbps; typical single-user 300–800 Mbps at 5 m on 5 GHz.
- **802.11be (Wi-Fi 7)**: 4096-QAM, channels up to 320 MHz, Multi-Link Operation (MLO) — simultaneous streams on 2.4/5/6 GHz. Practical 1–3 Gbps per client in clean RF.
- Wi-Fi is a **shared medium**: all clients on one AP contend for the same channel. A slow client using BPSK (because it's far away) holds the channel longer per frame, starving faster clients.
- 2.4 GHz: only 3 non-overlapping channels (1, 6, 11 in 20 MHz mode). Dense apartment buildings are saturated.
- 6 GHz (Wi-Fi 6E/7): 60+ non-overlapping channels, no legacy interference, shorter range.

## Cellular modulation

4G LTE and 5G NR both use OFDM on the air interface. Key numbers:

- **4G LTE**: 30–60 ms RTT, 10–100 Mbps typical. Latency dominated by scheduling, not propagation.
- **5G sub-6 GHz**: 15–30 ms RTT, 100 Mbps–1 Gbps.
- **5G mmWave**: sub-10 ms RTT, multi-Gbps — but only to ~200 m, blocked by walls and rain.
- **5G URLLC** (Ultra-Reliable Low-Latency Communication): sub-1 ms air-interface latency target for industrial robotics — achieved by mini-slots and dedicated scheduling, not physics.

## Bandwidth-delay product

BDP = bandwidth × RTT. On a 10 Gbps link with 100 ms RTT:

BDP = 10 × 10⁹ × 0.1 / 8 = **125 MB**

This is the maximum number of bytes "in flight" at saturation. If TCP's receive window is smaller, the sender stalls waiting for ACKs and the link goes underutilised. Modern Linux auto-tunes receive buffers; BBR congestion control models BDP explicitly to pace sends.

<Quiz
  id="quiz-shannon"
  pieceSlug="02-modulation-and-shannon"
  lang="en"
  question="Shannon's theorem says capacity grows linearly with bandwidth but logarithmically with SNR. What does this mean for engineering?"
  choices={[
    { label: "Always double the SNR first — it's the most cost-effective path to more capacity", misconception: "Logarithmic growth means doubling SNR adds only ~1 bit/symbol regardless of how much SNR you start with." },
    { label: "Doubling the channel bandwidth (B) doubles the capacity ceiling; doubling SNR only adds ~1 bit per symbol — so chasing bandwidth beats chasing SNR at high signal quality", correct: true },
    { label: "SNR doesn't matter once the physical layer is set up", misconception: "SNR determines which modulation scheme is achievable — low SNR forces lower QAM." },
    { label: "Capacity is independent of SNR", misconception: "S/N is directly in the formula." },
  ]}
/>

<DragOrder
  id="drag-encoding"
  pieceSlug="02-modulation-and-shannon"
  lang="en"
  prompt="Order the encoding steps from data byte to transmitted signal:"
  items={[
    "Byte arrives at network card from OS",
    "Card adds Ethernet framing (header + FCS)",
    "Line coding is applied (PAM-5 for 1G copper, NRZ for many fibre links)",
    "FEC adds redundancy bits to allow error correction",
    "Modulator converts coded symbols to physical signal (voltage / light / radio)",
    "Signal travels through the medium",
    "Receiver reverses every step to recover original bytes",
  ]}
/>

<FadedExample
  client:load
  id="fe-shannon"
  pieceSlug="02-modulation-and-shannon"
  lang="en"
  title="Shannon capacity worked example"
  steps={{
    solved: (
      <p>5 GHz Wi-Fi channel, 80 MHz wide, SNR = 30 dB (linear: 1000). C = 80×10⁶ × log₂(1001) ≈ 80×10⁶ × 9.97 ≈ <strong>798 Mbps theoretical max</strong>. Wi-Fi 6 rates of ~600–800 Mbps are right at this ceiling with protocol overhead.</p>
    ),
    semi: {
      prompt: <p>6 GHz channel (Wi-Fi 6E), 160 MHz wide, SNR = 40 dB (linear: 10,000). C = 160×10⁶ × log₂(10,001) = _____ Mbps?</p>,
      blanks: [
        { id: "cap", expected: /^2[12]\d\d$/, placeholder: "? Mbps" },
      ],
    },
    blank: {
      prompt: <p>Millimetre-wave 5G: 800 MHz channel, SNR = 20 dB (linear: 100). Calculate the theoretical Shannon capacity in Gbps.</p>,
      reveal: <p>C = 800×10⁶ × log₂(101) ≈ 800×10⁶ × 6.66 ≈ <strong>5.33 Gbps</strong>. Huge B term is what makes mmWave fast despite moderate SNR. Real 5G mmWave peaks ~4 Gbps in ideal conditions — close to ceiling.</p>,
    },
  }}
/>

<Inset kind="pitfall" lang="en">
**The slow-client Wi-Fi problem.** One weak client using BPSK (1 bit/symbol) holds the radio channel much longer per frame than a nearby client using 1024-QAM (10 bits/symbol). Even though MU-MIMO helps, the slow client still degrades everyone else. Fixes: move the AP closer (raise SNR → higher QAM), enable band steering (push the weak client to 2.4 GHz), or add a second AP for better coverage. In enterprise environments, minimum RSSI policies kick out clients below a signal threshold rather than letting them drag down the cell.
</Inset>

</Explanation>

<KeyTakeaway>Shannon's C = B·log₂(1+S/N) is the universal ceiling. More bandwidth scales linearly — buy it when you can. More SNR scales logarithmically — it's expensive and yields diminishing returns. Every link engineer optimises these two levers.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-modulation-and-shannon-retrieval"
  lang="en"
  questions={[
    { q: "Why does 5G millimetre-wave achieve multi-Gbps speeds despite similar or lower SNR than sub-6 GHz?", a: "Shannon: C = B·log₂(1+S/N). The B (bandwidth) term for mmWave channels is 400–800 MHz vs 20–100 MHz for sub-6 GHz. Even with the same log term, the much larger B yields 4–8× more capacity. SNR may actually be lower at distance (path loss is worse at high frequency), but the bandwidth advantage dominates in line-of-sight deployments." },
    { q: "What is PAM-5 and why does Gigabit Ethernet use it?", a: "PAM-5 (Pulse Amplitude Modulation with 5 levels: -2, -1, 0, +1, +2 volts) encodes 2.32 bits per symbol. 1000BASE-T runs at 125 MBaud on each of 4 pairs simultaneously. The five levels allow 1 Gbps over standard Cat5e copper up to 100 m. PAM-5 has strong noise immunity because differential signalling cancels common-mode interference." },
    { q: "What is the bandwidth-delay product and when does it become a performance bottleneck?", a: "BDP = bandwidth × RTT. On a 10 Gbps / 100 ms RTT path, BDP = 125 MB. TCP needs this many bytes in flight at once to fully utilise the link. If the receive window cap or sender buffer is smaller than BDP, the sender is idle waiting for ACKs — the link runs below capacity. High-BDP paths (intercontinental, satellite) suffer most. Modern kernels auto-tune; BBR models BDP explicitly to prevent under-utilisation." },
  ]}
/>

<Recap lang="en">
Three media carry the world's bits: copper (differential voltage, PAM-5), glass fibre (laser pulses, coherent modulation, EDFA amplifiers), and radio (OFDM with QAM constellations). Shannon's theorem C = B·log₂(1+S/N) sets the maximum bit rate for any channel — linear in bandwidth, logarithmic in SNR. Ethernet frames carry 14-byte headers + 4-byte FCS; the maximum payload is 1500 bytes (standard) or 9000 bytes (jumbo). Wi-Fi is a shared medium where slow clients using low QAM hurt everyone. The bandwidth-delay product (BDP = bandwidth × RTT) determines how many bytes TCP must keep in flight to saturate a link — the reason window scaling and BBR matter on high-latency paths.
</Recap>
