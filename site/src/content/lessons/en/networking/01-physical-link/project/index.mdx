---
concepts:
- latency-budget
- bufferbloat-diagnosis
- sqm-shaping
- link-debugging
- bandwidth-delay-product
estMin: 240
lang: en
level: senior
order: 10
prereqs: []
slug: project
sources:
- https://hpbn.co/primer-on-latency-and-bandwidth/
- https://www.bufferbloat.net/projects/bloat/wiki/Introduction/
status: ready
summary: "Hands-on project — measure your own link's latency floor versus reality, expose bufferbloat under load, tame it with SQM, and prove the fix with before/after numbers."
title: 'The physical link: audit and tame a real link'
track: networking
unit: 01-physical-link
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Reading about latency floors and bufferbloat is not the same as measuring them on a link you own. Take a real connection — your home router, a cloud VM, or a lab link — separate the physics from the engineering, drive it into bufferbloat, and bring the latency back with evidence at every step.
</Hook>

<Goal lang="en">
Turn the unit's mental model into a reproducible measurement loop: compute the propagation floor, measure the real RTT and decompose the excess, expose bufferbloat under saturation, apply AQM, and verify the fix with before/after numbers.
</Goal>

<ProjectBrief
  id="01-physical-link-project"
  lessonSlug="01-physical-link"
  lang="en"
  objective="Take a real link you control (home uplink, cloud VM, or lab link), characterise its physical limits, expose and fix bufferbloat under load, and prove every claim with measurements rather than the marketing numbers."
  requirements={[
    "Pick two destinations — one near (same metro/region) and one far (another continent). For each, compute the propagation floor from great-circle distance ÷ ~200,000 km/s, then measure real RTT with ping and mtr. Tabulate floor vs real and explain the gap (routing geometry, hops, queueing).",
    "Measure your link's actual bandwidth in both directions (a real transfer or iperf3, not just a speed-test widget), and compute its bandwidth-delay product to the far destination. State whether the default TCP window/buffers can fill it, and whether window scaling is in play.",
    "Run a bufferbloat test the right way: measure idle ping, then re-measure ping WHILE saturating the uplink with a bulk transfer. Record the idle-vs-loaded latency and the increase — this is the bufferbloat signature, invisible to an idle speed test.",
    "Inspect link health with the unit's tools: ip -s link, ethtool / ethtool -S (CRC/error counters, negotiated speed/duplex), and iw dev link if wireless. Confirm the link is clean (zero CRC errors, expected speed) so you are diagnosing queueing, not a faulty cable.",
    "Apply Active Queue Management: enable SQM (CAKE preferred, fq_codel fallback) on the egress, shaped to ~90–95% of the measured real uplink rate, with the correct link-layer adaptation (e.g. docsis for cable). If you cannot run SQM on the device, do it on a Linux box in the path via tc.",
    "Re-run the identical saturated-ping test and record after numbers next to before. Loaded latency should drop from hundreds of ms toward under ~30 ms; note the peak throughput you traded away via shaping.",
  ]}
  acceptance={[
    "A before/after table: propagation floor, idle RTT, loaded RTT (pre-SQM), loaded RTT (post-SQM), and measured up/down bandwidth — all measured, not estimated.",
    "A short decomposition of the near and far RTT: how many ms are propagation floor versus routing/queueing excess, with the percentage over floor.",
    "Evidence the link layer is clean: CRC/error counters at zero and the link negotiated at the expected speed/duplex, captured from ethtool / ip -s link.",
    "A one-paragraph write-up stating which numbers are physics you cannot change and which were engineering you fixed, and what you traded (peak throughput) for the latency win.",
  ]}
  stretch={[
    "Add a Shannon sanity check for your wireless link: read the channel width and SNR (iw / vendor tools), compute the theoretical capacity ceiling, and compare it to the real measured throughput — explain the gap.",
    "Script the whole audit so it runs unattended and emits the before/after table as JSON or a chart — a reusable link-health probe you could schedule.",
    "Compare two transports on the far path: a default loss-based sender versus BBR (sysctl net.ipv4.tcp_congestion_control=bbr), and measure throughput and latency-under-load on each.",
    "Write a one-page on-call runbook: how to tell physics from a bug at the link layer, the floor numbers for your common routes, the bufferbloat test procedure, and the SQM settings that fixed it.",
  ]}
/>

<Recap lang="en">
This is the loop you run on any real link incident: compute the floor, measure the reality, decompose the excess into physics versus engineering, expose bufferbloat by pinging under load (never on an idle link), confirm the link layer is clean, then apply AQM shaped below line rate and verify with before/after numbers. Doing it once on a link you own turns the unit's floor numbers and the bufferbloat signature into instinct you will reach for in production.
</Recap>
