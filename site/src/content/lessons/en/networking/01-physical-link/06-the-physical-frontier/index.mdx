---
concepts:
- coherent-optics
- chromatic-dispersion
- fec-coding-gain
- wifi-7
- 5g-advanced
- hollow-core-fibre
- post-quantum-crypto
deepensInto: []
estMin: 13
lang: en
lessonType: topic
level: senior
order: 6
prereqs:
- 04-bufferbloat-and-congestion
- 05-the-datacentre-fabric
slug: 06-the-physical-frontier
sources:
- https://en.wikipedia.org/wiki/Hollow-core_fiber
- https://standards.ieee.org/ieee/802.11be/7516/
- https://www.3gpp.org/specifications-technologies/releases/release-18
- https://csrc.nist.gov/pubs/fips/203/final
spiral:
- latency
status: ready
summary: Coherent optics, Wi-Fi 7, 5G-Advanced, hollow-core fibre — the physical layer
  keeps moving, but one mental model decodes every new link technology you will ever
  meet.
title: The physical frontier
track: networking
unit: 01-physical-link
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import RFCQuiz from "~/components/pedagogy/RFCQuiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import fecCodingGain from "./infographics/fec-coding-gain.svg?raw";

<Hook>
Every few years a vendor announces a link technology that sounds like science fiction — light through air, terahertz radio, quantum-secured keys. Most engineers feel they have to relearn the physical layer each time. They don't: one model decodes them all.
</Hook>

<Crux>Any unfamiliar link technology yields 80% of its behaviour to three questions: what medium, what encoding, what frame format.</Crux>

<Explanation>

## Long-haul fibre is an analog fight

Light through 5,000 km of single-mode glass is not a clean digital channel — it is an analog signal being slowly destroyed. Three impairments dominate. **Chromatic dispersion**: different wavelengths travel at slightly different speeds, so a pulse spreads out and bleeds into the next symbol. **Polarisation-mode dispersion (PMD)**: the two orthogonal polarisations of light arrive at slightly different times. **Four-wave mixing**: in a DWDM system packing 60+ wavelengths into one fibre, nonlinear effects make adjacent channels generate spurious tones that interfere with their neighbours.

For decades the fix was physical: dispersion compensation modules (DCMs) — spools of specially designed fibre that bend the dispersion back. Modern long-haul has moved the fight into DSP. **Coherent detection** recovers not just the amplitude of the light but its *phase*, the same way a radio receiver mixes against a local oscillator. Once you have phase, you can run higher-order modulation (16-QAM, 64-QAM on light) and let a digital signal processor undo chromatic dispersion and PMD in software after the fact. The payoff is dramatic: a modern coherent transceiver carries 800 Gbps on a single wavelength, and 60+ wavelengths share one fibre — tens of terabits per fibre pair.

## FEC is what makes the SNR margin real

The raw optical channel on a long submarine span has a bit error rate around 1e-6 — one bad bit in a million, far too noisy to ship. Yet the delivered BER is better than 1e-12. The entire gap is forward error correction. Long-haul optics use a concatenated scheme: an **LDPC** (Low-Density Parity Check) inner code with iterative soft-decision decoding, wrapped in a **Reed-Solomon** outer code that mops up residual burst errors. The result is 10 dB or more of *coding gain* — the channel behaves as if it had ten decibels more signal than it physically does, which translates directly into more bits per symbol or longer reach between amplifiers.

The trade is latency and silicon: FEC adds redundant bits (overhead, typically 7–25%), the decoder burns power and adds a fixed processing delay, and once you are FEC-limited the channel fails as a cliff — degrade the SNR slightly past the correction threshold and BER jumps from 1e-12 to unusable, with no gradual warning.

<Infographic
  svg={fecCodingGain}
  label="Horizontal bar chart of error-free orders of magnitude on a submarine span: the raw optical channel reaches 6 (BER 1e-6), while after forward error correction it reaches 12 (BER 1e-12) — FEC doubles the error-free decades."
  caption="The entire 1e-6 → 1e-12 gap is forward error correction: LDPC inner plus Reed-Solomon outer buys 10+ dB of coding gain, doubling the error-free orders of magnitude the channel physically delivers."
  hue="var(--d-infra)"
/>

<NumbersCard
  id="card-frontier"
  title="Frontier link technologies — the numbers"
  data-lesson-visual
  stats={[
    { label: "Coherent long-haul", value: "800 Gbps/wavelength, 60+ λ/fibre" },
    { label: "FEC coding gain (submarine)", value: "raw BER 1e-6 → post-FEC 1e-12, 10+ dB" },
    { label: "Wi-Fi 7 (802.11be)", value: "320 MHz channels, 4096-QAM (12 bits/symbol)" },
    { label: "Hollow-core fibre", value: "~50% faster than solid glass (light in air)" },
    { label: "QKD over fibre", value: "~100 km range without quantum repeaters" },
    { label: "Post-quantum (ML-KEM)", value: "shipping in TLS now, NIST FIPS 203" },
  ]}
/>

## The radio frontier: Wi-Fi 7, 5G-Advanced, 6G

Radio keeps mining the same Shannon levers — more bandwidth, more bits per symbol. **Wi-Fi 7 (802.11be)** doubles the channel width to 320 MHz, pushes modulation to 4096-QAM (12 bits per symbol versus 10 for Wi-Fi 6), and adds Multi-Link Operation — one client transmitting on 2.4, 5, and 6 GHz concurrently rather than picking one. **5G-Advanced** (3GPP Release 18, finalised in 2024) is the mid-life upgrade to 5G: AI/ML optimisation of the air interface, 20%+ RAN power reduction, and tighter satellite (non-terrestrial network) integration. **6G** research (2030+) is reaching for terahertz frequencies, an AI-native air interface designed around learned components from day one, and integrated sensing — the network using its own radio to map the physical environment, not just carry data.

<Inset kind="why" lang="en">
**Why hollow-core fibre is a bigger deal than it sounds.** Light in solid glass travels at ~200,000 km/s — the glass slows it by a third. Hollow-core fibre guides light through *air* held inside a microstructured glass lattice, so it propagates at close to vacuum speed: roughly 50% faster than solid glass. For most workloads that is irrelevant. For latency-bound businesses — high-frequency trading, where a transatlantic round-trip shaved by milliseconds is worth real money — it rewrites the economics. It is the rare physical-layer change that lowers the *latency floor* itself, the floor that no software optimisation can touch. Field trials are running now; broad deployment is the work of the late 2020s.
</Inset>

## Co-packaged optics, LEO, and the quantum question

Two more shifts worth tracking. **Co-packaged optics (CPO)** moves the optical transceiver off the pluggable front panel and integrates it directly into the switch ASIC package — cutting the electrical distance signals travel, slashing per-port power, and enabling denser fabrics as port speeds climb past 800G. **LEO satellite constellations** (Starlink and peers) reshape the latency-versus-coverage trade: an orbit at ~550 km gives 20–50 ms RTT, an order of magnitude better than GEO's ~600 ms, while reaching places no fibre will ever go.

Then the quantum question, where it pays to separate hype from practice. **Quantum key distribution (QKD)** uses the physics of photons to detect any eavesdropper on a key exchange — but it runs over fibre with a hard range limit near 100 km, because you cannot amplify a quantum state and quantum repeaters are still a research problem. The practical thing here *now* is the opposite: **post-quantum cryptography**. ML-KEM and ML-DSA (NIST's standardised lattice algorithms, FIPS 203/204) are classical software that resists attack by a future quantum computer, and they are already shipping inside TLS handshakes. QKD is a decade-plus from changing day-to-day networking; post-quantum crypto is changing it this year.

<RFCQuiz
  id="frontier-rfc-1"
  pieceSlug="06-the-physical-frontier"
  lang="en"
  question="Which IEEE 802.11 amendment defines Wi-Fi 7, with 320 MHz channels and 4096-QAM?"
  choices={[
    { rfc: "IEEE 802.11be", title: "Extremely High Throughput (Wi-Fi 7)", correct: true },
    { rfc: "IEEE 802.11ax", title: "High Efficiency (Wi-Fi 6 / 6E)" },
    { rfc: "IEEE 802.11ac", title: "Very High Throughput (Wi-Fi 5)" },
    { rfc: "IEEE 802.3df", title: "800 Gigabit Ethernet" },
  ]}
/>

<Quiz
  id="frontier-quiz-1"
  pieceSlug="06-the-physical-frontier"
  lang="en"
  question="A vendor pitches QKD as the answer to quantum computers breaking your TLS. What is the realistic position for a fullstack engineer today?"
  choices={[
    { label: "Deploy QKD immediately — it is the only quantum-safe option", misconception: "QKD needs dedicated fibre, is range-limited to ~100 km without repeaters, and protects only key exchange — it cannot replace TLS across the public Internet." },
    { label: "QKD is a long-horizon research technology; the practical defence shipping now is post-quantum cryptography (ML-KEM/ML-DSA) inside TLS", correct: true },
    { label: "Nothing is needed — quantum computers cannot break RSA or ECDH", misconception: "A sufficiently large quantum computer breaks classical RSA and ECDH via Shor's algorithm; that is exactly why NIST standardised post-quantum algorithms." },
    { label: "Switch all traffic to one-time pads", misconception: "One-time pads need pre-shared key material as long as the data — impractical at Internet scale, and not what post-quantum standards address." },
  ]}
/>

## The durable model

Standards documents for link technologies run to hundreds of pages, and they keep arriving. You do not need to read them cover to cover. When you hit an unfamiliar link technology — a new Wi-Fi version, an optical standard, an industrial fieldbus — three sections tell you 80% of what you need:

1. **Physical medium** — copper, fibre, or a radio band. This sets the propagation speed (latency floor) and the noise environment.
2. **Encoding scheme** — modulation order, line coding, and FEC. This sets bits per symbol and how gracefully the link degrades.
3. **Frame format** — header, payload boundaries, MTU. This sets overhead and how the link layer hands off to the network layer.

Cross-reference those three and you can estimate bandwidth, latency, error behaviour, and where real deployments will struggle — without reading the spec front to back. Skip any one of them and you will be surprised: miss the medium and you will not know the latency floor; miss the encoding and you will not anticipate the failure mode; miss the frame format and you will overpay in overhead or fight MTU mismatches.

<TradeoffMatrix
  id="frontier-tradeoff-1"
  pieceSlug="06-the-physical-frontier"
  lang="en"
  prompt="A high-frequency trading firm wants the lowest possible NYC↔Chicago round-trip latency. Pick the physical-layer investment."
  options={[
    { name: "Upgrade existing fibre route to 800G coherent optics", summary: "More bandwidth on the same path.", justification: "800G coherent raises throughput, but propagation delay is set by path length and the speed of light in glass — bandwidth upgrades do not move the latency floor. Wrong lever for a latency-bound problem." },
    { name: "Deploy a hollow-core fibre route along the shortest path", summary: "Light travels through air, ~50% faster.", correct: true, justification: "Hollow-core fibre lowers the propagation floor itself — light at near-vacuum speed plus the shortest geographic path is the only investment here that actually reduces minimum RTT, which is exactly what HFT pays for." },
    { name: "Add LEO satellite backup link", summary: "Satellite path for resilience.", justification: "A ~550 km LEO hop adds round-trip distance versus a direct terrestrial route over ~1,200 km — useful for coverage and redundancy, not for beating a fibre route on latency between two well-connected cities." },
    { name: "Switch the route's TLS to post-quantum ML-KEM", summary: "Quantum-safe key exchange.", justification: "Post-quantum crypto changes the security posture of the handshake, not the propagation delay. It is the right call for long-term confidentiality, but irrelevant to a latency objective." },
  ]}
/>

<FadedExample
  client:visible
  title="Applying the three-question model"
  steps={[
    {
      label: "Solved — coherent 800G long-haul link",
      code: `1. Medium: single-mode fibre (SMF). Propagation speed ~200,000 km/s in glass.
   Latency floor: 5,000 km path → ~25 ms one-way, ~50 ms RTT.

2. Encoding: 64-QAM on two orthogonal polarisations, coherent detection.
   Bits per symbol: 6 bits/symbol × 2 polarisations × 100 Gbaud ≈ 800 Gbps gross.
   FEC overhead ~15% → net ~680 Gbps payload per wavelength.
   Failure mode: cliff edge — SNR drops past threshold, BER jumps to unusable.

3. Frame format: OTU4 optical transport frame (4 × 100G lanes in ODU4).
   Overhead ~3%, carries Ethernet or IP unchanged in the OTU4 payload.

Conclusion: extremely high bandwidth, fixed propagation latency, cliff-edge failure.
The only way to cut RTT further is a shorter geographic path or hollow-core fibre.`,
    },
    {
      label: "Semi-solved — Wi-Fi 7 access link",
      code: `1. Medium: radio, 6 GHz band (Wi-Fi 7 uses 6 GHz for 320 MHz channels).
   Propagation speed: ~300,000 km/s (air ≈ vacuum).
   Typical indoor range: 30–60 m. Latency floor: ???

2. Encoding: ???-QAM, ??? MHz channel, ??? bits/symbol.
   Expected peak rate per stream (look up 802.11be spec): ???

3. Frame format: 802.11 MAC frame.
   Overhead vs. Ethernet: ??? (hint: 802.11 headers are larger — RTS/CTS, ACK).`,
    },
    {
      label: "Blank — hollow-core fibre NYC↔Chicago",
      code: `// Geographic distance: ~1,200 km straight line.
// Solid glass: propagation speed ~200,000 km/s → one-way delay = ???
// Hollow-core fibre: propagation speed ~295,000 km/s → one-way delay = ???
// Savings per round trip: ???

// Why does this matter for HFT?  (hint: think about basis trades and arbitrage windows)`,
    },
  ]}
/>

</Explanation>

<StackDiagram
  label="The durable three-question model for any link technology, read top to bottom: physical medium sets the latency floor, encoding sets bits-per-symbol and failure mode, frame format sets overhead"
  hue="var(--d-hardware)"
  layers={[
    { label: "1. Physical medium", note: "latency floor + noise" },
    { label: "2. Encoding scheme", note: "bits/symbol + FEC" },
    { label: "3. Frame format", note: "overhead + MTU" },
  ]}
  caption="Cross-reference these three sections of any spec — copper/fibre/radio, modulation and FEC, header and MTU — and you decode ~80% of a new link technology's bandwidth, latency, and error behaviour without reading it cover to cover."
/>

<KeyTakeaway>The physical layer keeps advancing — coherent optics, Wi-Fi 7, hollow-core fibre — but the model is fixed: medium sets the latency floor, encoding sets bits-per-symbol and failure mode, frame format sets overhead.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="06-the-physical-frontier-retrieval"
  lang="en"
  questions={[
    { q: "What does coherent detection recover that direct detection does not, and why does that matter for long-haul fibre?", a: "Coherent detection recovers the phase of the optical signal as well as its amplitude, by mixing the received light against a local oscillator. Phase information enables higher-order modulation (16-QAM, 64-QAM on light) for more bits per symbol, and lets a DSP undo chromatic dispersion and polarisation-mode dispersion in software after reception — replacing physical dispersion compensation modules. This is what lets a modern coherent transceiver carry 800 Gbps on one wavelength with 60+ wavelengths per fibre." },
    { q: "Raw BER on a submarine span is ~1e-6 but delivered BER is ~1e-12. Explain the gap and its cost.", a: "Forward error correction closes the gap. Long-haul optics use a concatenated code — an LDPC inner code with iterative soft-decision decoding plus a Reed-Solomon outer code for burst errors — yielding 10+ dB of coding gain. The channel behaves as if it had 10 dB more SNR than it physically does. The cost: 7–25% redundant-bit overhead, decoder power and a fixed processing latency, and cliff-edge failure — past the correction threshold BER jumps from 1e-12 to unusable with no gradual warning." },
    { q: "What three things should you read about any unfamiliar link technology, and what does each tell you?", a: "Physical medium (copper/fibre/radio band) — sets the propagation speed and so the latency floor, plus the noise environment. Encoding scheme (modulation order, line coding, FEC) — sets bits per symbol and how gracefully the link degrades. Frame format (header, payload boundaries, MTU) — sets overhead and the hand-off to the network layer. Cross-referencing those three gets you 80% of the bandwidth, latency, and error behaviour without reading the full spec." },
  ]}
/>

<Recap lang="en">
Long-haul single-mode fibre is an analog fight against chromatic dispersion, PMD, and four-wave mixing; coherent detection recovers phase so a DSP can undo those impairments and run higher-order modulation, carrying 800 Gbps per wavelength. FEC — LDPC inner plus Reed-Solomon outer — buys 10+ dB of coding gain, turning a raw 1e-6 BER into a delivered 1e-12, at the cost of overhead and cliff-edge failure. The radio frontier mines the same Shannon levers: Wi-Fi 7 with 320 MHz channels and 4096-QAM, 5G-Advanced (3GPP Release 18), and 6G research toward terahertz. Hollow-core fibre is the rare change that lowers the latency floor itself; post-quantum cryptography (ML-KEM) is the practical quantum defence shipping in TLS today, while QKD remains range-limited research. The durable takeaway: read three things — medium, encoding, frame format — and any new link technology is 80% decoded. Now when a vendor announces the next revolutionary link standard, open the spec to those three sections first: you will spend minutes instead of days getting to the answer that actually matters for your architecture.
</Recap>
