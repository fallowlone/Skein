---
concepts:
- least-privilege
- iam-policy
- wildcard-permissions
- permission-boundary
- policy-review
deepensInto: []
estMin: 15
lang: en
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-least-privilege-policies
sources:
- https://aws.amazon.com/iam/
- https://cheatsheetseries.owasp.org/cheatsheets/Authorization_Cheat_Sheet.html
spiral: []
status: ready
summary: A tight policy grants exactly the actions and resources a role needs and nothing more. Wildcards (`Action:*`, `Resource:*`) feel convenient and quietly become the blast radius. Permission boundaries cap what a role can ever grant — the guardrail least privilege needs at scale.
title: Least-privilege policies
track: security-cloud
unit: 01-cloud-iam
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A service needs to read one config object from one S3 bucket. The ticket says "give the role S3 access," so someone attaches `AmazonS3FullAccess` — a managed policy that grants `s3:*` on `Resource: *`. It works, the deploy goes green, everyone forgets about it. Two years later that same role's access key leaks through a logged environment variable in a crash report. The attacker doesn't get one config object. They get `s3:GetObject` on every bucket in the account, `s3:DeleteBucket`, `s3:PutBucketPolicy` to make any bucket public, and `s3:PutObject` to plant a payload in your static-site bucket. One wildcard, granted for a one-object read, became the entire account's data plane. Nobody decided to give the service that power. The policy just never said no.
</Hook>

By the end of this lesson you'll know how to write a policy that grants exactly the actions and resources a role needs, why wildcards turn a small leak into a large breach, and how a permission boundary caps the damage even when someone writes a sloppy policy anyway.

<Crux>Grant exactly the actions and resources a role needs; wildcards and missing boundaries are how privilege quietly sprawls.</Crux>

<Explanation>

## What a tight policy actually looks like

Least privilege is one rule: a principal should hold exactly the permissions required to do its job, for exactly as long as it needs them — and not one permission more. The hard part isn't the principle, it's that the *convenient* policy and the *correct* policy look almost identical, and the convenient one ships faster.

An IAM policy answers two questions for every request: **which actions** (`s3:GetObject`, `dynamodb:Query`) and **on which resources** (a specific bucket ARN, a specific table). A tight policy names both. The service that reads one config object should hold this:

```json
{
  "Effect": "Allow",
  "Action": "s3:GetObject",
  "Resource": "arn:aws:s3:::acme-config/service-x/*"
}
```

One action, one resource prefix. If that key leaks, the attacker gets read access to one folder in one bucket — a contained, boring incident. The blast radius is the policy, and you wrote the policy small on purpose.

The reflex to build is: **start from deny, add the single action you can name, scope it to the single ARN you can name, and stop.** Every time you reach for a `*`, you are saying "I don't know which actions or resources this needs" — and an attacker who steals the credential knows exactly what to do with that uncertainty.

## Why wildcards bite

Wildcards are the default failure mode because they are genuinely useful while you're building and genuinely dangerous once you ship. There are two wildcards, and both hurt.

`Action: "s3:*"` widens *what* the principal can do. The service needed `GetObject`; the wildcard also handed it `PutObject`, `DeleteObject`, `PutBucketPolicy`, `PutBucketAcl`. Now a read credential can overwrite your data, delete it, or make a private bucket public. `Resource: "*"` widens *where* it applies — from one bucket to every bucket, including ones that don't exist yet. The two compose: `s3:*` on `*` is full control of your entire object store from a credential that was provisioned to read a config file.

<FlowDiagram
  label="How a wildcard turns a single leaked read credential into account-wide compromise: a role provisioned for s3:GetObject on one bucket is instead granted s3:* on Resource star; when the key leaks the attacker inherits delete, make-public, and write-payload across every bucket, while a scoped policy would have contained the same leak to one folder."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "need", label: "Real need", sub: "GetObject, 1 bucket" },
    { id: "grant", label: "What was granted", sub: "s3:* on *" },
    { id: "leak", label: "Key leaks", sub: "crash-log env var" },
    { id: "blast", label: "Attacker inherits", sub: "delete, make-public" },
    { id: "tight", label: "Scoped policy", sub: "leak = 1 folder" },
  ]}
  edges={[
    { from: "need", to: "grant", label: "over-grant" },
    { from: "grant", to: "leak" },
    { from: "leak", to: "blast", label: "full s3 control" },
    { from: "need", to: "tight", label: "the fix" },
  ]}
  caption="A wildcard decouples the credential's power from its purpose. The role only ever needed one action on one bucket; s3:* on Resource * means the day a key leaks, the attacker's reach is the whole object store, not the one folder the service actually touched."
/>

There's a quieter cost too: wildcards make audit impossible. When a policy says `s3:GetObject` on a named ARN, a reviewer can read it and know precisely what the role can do. When it says `s3:*` on `*`, the only honest answer to "what can this role do?" is "everything to everything" — and nobody can reason about a blast radius that large. Tight policies are self-documenting; wildcards erase the documentation.

<Inset kind="why" lang="en">
Why does `iam:*` deserve special fear, above even `s3:*`? Because IAM permissions are *self-amplifying*. A role with `iam:PutRolePolicy` or `iam:AttachRolePolicy` can grant itself — or any role — any permission in the account. This is **privilege escalation**: the attacker doesn't need the dangerous permission directly, only the permission to assign it. A role with `iam:*` and nothing else is effectively account-admin, because step one is to write itself an admin policy. This is exactly why the next guardrail — the permission boundary — exists: to make "grant yourself more" impossible even when `iam:PutRolePolicy` is present.
</Inset>

## Permission boundaries: the guardrail least privilege needs at scale

Writing one tight policy is easy. Keeping *every* policy tight, across hundreds of roles and dozens of engineers who all have the power to create roles, is the real problem. This is what **permission boundaries** solve.

A permission boundary is a policy attached to a principal that sets the **maximum** permissions it can ever have — a ceiling, not a grant. The principal's effective permissions are the **intersection** of its identity policies and its boundary: a permission is allowed only if *both* permit it. So even if a developer (or an attacker who has compromised that developer's role) attaches `AdministratorAccess` to a new role, the boundary clips it back down to whatever the boundary allows.

The canonical use is **delegated role creation**. You let developers create roles for their own services — that's good for velocity — but you require that every role they create carries a boundary you control. Now a developer can grant their service `s3:GetObject` on their bucket, but they *cannot* create a role with `iam:*` or access to the production database, because the boundary forbids it and the intersection wins. The boundary turns "trust every engineer to write a perfect policy" into "trust the one boundary policy, and let engineers move fast inside it."

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Identity policy says</th>
        <th class="border border-border px-3 py-2 text-left">Boundary says</th>
        <th class="border border-border px-3 py-2 text-left">Effective (intersection)</th>
        <th class="border border-border px-3 py-2 text-left">Why</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>s3:GetObject</code></td>
        <td class="border border-border px-3 py-2"><code>s3:&#42;</code></td>
        <td class="border border-border px-3 py-2"><code>s3:GetObject</code></td>
        <td class="border border-border px-3 py-2">Both allow it; the narrower grant wins</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>s3:&#42;</code></td>
        <td class="border border-border px-3 py-2"><code>s3:GetObject, s3:PutObject</code></td>
        <td class="border border-border px-3 py-2"><code>s3:GetObject, s3:PutObject</code></td>
        <td class="border border-border px-3 py-2">Boundary is the ceiling; the wildcard is clipped</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>iam:&#42;</code> (admin)</td>
        <td class="border border-border px-3 py-2">no <code>iam:&#42;</code></td>
        <td class="border border-border px-3 py-2"><em>denied</em></td>
        <td class="border border-border px-3 py-2">Escalation blocked: boundary never permitted it</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>dynamodb:Query</code></td>
        <td class="border border-border px-3 py-2"><code>s3:&#42;</code> only</td>
        <td class="border border-border px-3 py-2"><em>denied</em></td>
        <td class="border border-border px-3 py-2">Not in the boundary → no intersection → no access</td>
      </tr>
    </tbody>
  </table>
</div>

A boundary is not a substitute for tight policies — it's a backstop. The identity policy is where you express *intent* (this role reads config); the boundary is where you express *limit* (no role in this account may ever touch IAM or prod data). You want both: tight policies so the common case is correct, and a boundary so a single sloppy policy can't become an account compromise.

## Reviewing a policy like a senior

When a policy crosses your review, you're scanning for three things in order. First, **wildcards**: any `Action: "*"`, `"s3:*"`, or `Resource: "*"` is a question, not necessarily a bug — but it must be justified, and "it was easier" isn't a justification. Second, **the dangerous verbs**: `iam:*`, `iam:PutRolePolicy`, `iam:PassRole`, `sts:AssumeRole` on a broad resource, `*:Delete*`, anything that can change *other* permissions or destroy data. Third, **resource scope**: does each statement name the specific ARNs it touches, or does it leave `Resource: "*"` because nobody enumerated them?

The senior move is to drive policies *down* over time, not just gate new ones. Cloud providers expose **access-analyzer / last-accessed** data: which actions a role has actually used in the last 90 days. A role granted `s3:*` that has only ever called `GetObject` and `ListBucket` is telling you its real policy — generate the tight one from observed usage and replace the wildcard. Least privilege isn't a one-time grant you get right at creation; it's a ratchet you tighten as you learn what the role actually does.

<TradeoffMatrix
  id="02-least-privilege-tradeoff-1"
  lessonSlug="02-least-privilege-policies"
  lang="en"
  prompt="A service role currently has `AmazonS3FullAccess` (`s3:*` on `*`) but, per last-accessed data, has only ever called `s3:GetObject` and `s3:ListBucket` on one bucket. Pick the right way to bring it to least privilege."
  options={[
    { name: "Replace it with a policy granting `s3:GetObject` and `s3:ListBucket` scoped to that one bucket's ARN, and attach a permission boundary so future edits can't re-widen it", summary: "Scope to observed use + boundary.", correct: true, justification: "This is least privilege done right: the grant matches what the role demonstrably needs (from last-accessed data), it's scoped to the named resource, and the boundary makes the tightening durable — even if someone later attaches a broad policy, the intersection clips it. You've fixed the policy and prevented the regression in one move." },
    { name: "Leave `s3:*` but add an explicit `Deny` for `s3:DeleteBucket` and `s3:PutBucketPolicy`", summary: "Deny-list the scary actions.", justification: "Deny-listing is whack-a-mole: you blocked two dangerous actions and left dozens (PutObject overwrite, PutBucketAcl, GetObject on every other bucket) wide open. Least privilege is an allow-list of what's needed, not a deny-list of what you happened to think of. The blast radius is still 'all of S3 minus two verbs.'" },
    { name: "Keep `AmazonS3FullAccess` — it's an AWS-managed policy, so it's maintained and safe", summary: "Trust the managed policy.", justification: "Managed doesn't mean least-privilege. `AmazonS3FullAccess` is maintained in the sense that AWS keeps its action list current — which means it gets *broader* as S3 adds actions, not narrower. It grants `s3:*` on `*` by design; using it for a single-bucket read is exactly the over-grant that turns a leaked key into a full-S3 compromise." },
    { name: "Replace `Resource: \"*\"` with the bucket ARN but keep `Action: \"s3:*\"`", summary: "Scope resource, keep action wildcard.", justification: "Half a fix. Scoping the resource is real progress — the leak no longer spans every bucket — but `s3:*` on that one bucket still grants Delete, PutBucketPolicy (make it public), and overwrite. A read credential should not be able to delete the bucket it reads. You must scope the action too." },
  ]}
/>

<Quiz
  id="02-least-privilege-quiz-1"
  lessonSlug="02-least-privilege-policies"
  lang="en"
  question="A role's only permission is `iam:PutRolePolicy` on `*`. It has no `s3:*`, no `dynamodb:*`, nothing else. How dangerous is it?"
  choices={[
    { label: "Effectively account-admin — it can write itself a policy granting any permission, so the one IAM verb is total escalation", correct: true },
    { label: "Harmless — it can't read or write any data, it only edits IAM policies", misconception: "Editing IAM policies is the most dangerous power there is: the role's first move is to attach itself an `AdministratorAccess` policy, after which it has every data permission. The permission to grant permissions is the permission to grant yourself everything." },
    { label: "Moderately risky — it can edit policies but a boundary always stops it automatically", misconception: "A boundary stops it only if one is attached. Boundaries aren't automatic; without one, `iam:PutRolePolicy` is a clear escalation path. The boundary is precisely the control you'd add to neutralize this, but it has to be deliberately in place." },
    { label: "Low risk because changing a policy requires a separate approval step in IAM", misconception: "IAM has no built-in approval gate on `PutRolePolicy`. If the API call is permitted, it takes effect immediately. Any approval workflow is something you build around IAM, not a guarantee IAM provides." },
  ]}
/>

<Quiz
  id="02-least-privilege-quiz-2"
  lessonSlug="02-least-privilege-policies"
  lang="en"
  question="A role has an identity policy allowing `s3:*` and a permission boundary allowing only `s3:GetObject` and `s3:ListBucket`. The role tries `s3:DeleteObject`. What happens and why?"
  choices={[
    { label: "Denied — effective permissions are the intersection of identity policy and boundary, and DeleteObject isn't in the boundary", correct: true },
    { label: "Allowed — the identity policy grants `s3:*`, and identity policies take precedence over boundaries", misconception: "Boundaries are a ceiling, not a lower-priority grant. The effective permission set is the intersection: an action is allowed only if both the identity policy AND the boundary permit it. The boundary clips `s3:*` down to its two actions." },
    { label: "Allowed, but logged as a boundary violation for later review", misconception: "It's not allowed-then-flagged; it's denied at evaluation time. The boundary doesn't permit DeleteObject, so the intersection excludes it and the call fails outright." },
    { label: "Denied, but only because DeleteObject is inherently a privileged action", misconception: "DeleteObject isn't specially privileged in IAM — it's denied here purely because the boundary doesn't include it. If the boundary had listed DeleteObject, the intersection would allow it." },
  ]}
/>

<DragOrder
  id="02-least-privilege-drag-1"
  lessonSlug="02-least-privilege-policies"
  lang="en"
  prompt="Order the steps to take an over-permissioned role (`s3:*` on `*`) down to least privilege:"
  items={[
    { id: "a", label: "Pull last-accessed / access-analyzer data to see which actions the role actually used" },
    { id: "b", label: "Write a policy listing only those observed actions" },
    { id: "c", label: "Scope each statement's Resource to the specific ARNs the role touched" },
    { id: "d", label: "Replace the wildcard policy and attach a permission boundary as a ceiling" },
    { id: "e", label: "Re-check last-accessed after a cycle to catch any action you cut too aggressively" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Grant the exact actions on the exact resources a role needs — start from deny and add the one ARN you can name. Wildcards (`s3:*`, `Resource: *`) make a leaked credential as powerful as your whole account and erase auditability. A permission boundary caps what any role can ever hold, so one sloppy policy can't become a breach. Tighten toward observed usage; least privilege is a ratchet, not a one-time grant.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-least-privilege-retrieval"
  lang="en"
  questions={[
    { q: "Explain why a single wildcard like `s3:*` on `Resource: *` is so dangerous, even when the role only ever reads one object.", a: "A wildcard decouples a credential's power from its purpose. The role was provisioned to do one thing — read one config object — but `s3:*` grants every S3 action (GetObject but also PutObject, DeleteObject, DeleteBucket, PutBucketPolicy, PutBucketAcl) and `Resource: *` applies them to every bucket in the account, including ones that don't exist yet. The day that credential leaks — through a logged env var, a crash report, a compromised dependency — the attacker doesn't get the one object the service touched; they inherit full control of the entire object store: they can read every bucket, overwrite or delete data, and make any private bucket public to plant a payload. The blast radius is defined by the policy, not by what the code actually does, so a one-object read becomes an account-wide data-plane compromise. Wildcards also destroy auditability: a reviewer can reason about `s3:GetObject` on a named ARN, but the only honest answer to 'what can a role with `s3:*` on `*` do?' is 'everything to everything,' which is a blast radius nobody can reason about." },
    { q: "What is a permission boundary, how does it differ from an identity policy, and why is it the control that makes least privilege survivable at scale?", a: "A permission boundary is a policy attached to a principal that defines the maximum permissions it can ever have — a ceiling, not a grant. A principal's effective permissions are the intersection of its identity policies and its boundary: an action is allowed only if BOTH permit it. The identity policy expresses intent ('this role reads config'); the boundary expresses limit ('no role in this account may ever touch IAM or prod data'). They differ in direction — identity policies grant, boundaries cap — and you want both. At scale the problem isn't writing one tight policy, it's keeping every policy tight across hundreds of roles and many engineers who can all create roles. The boundary solves delegated role creation: you let developers create roles for velocity but require every created role to carry a boundary you control, so even if someone attaches `AdministratorAccess` or `iam:*`, the intersection clips it back. This is also the specific defense against IAM privilege escalation — a role with `iam:PutRolePolicy` can otherwise grant itself anything, but it can't exceed a boundary it didn't write. A boundary doesn't replace tight policies; it's the backstop that stops a single sloppy policy from becoming an account compromise." },
  ]}
/>

<Recap lang="en">
Least privilege is one rule: a principal holds exactly the actions, on exactly the resources, it needs — and nothing more. The trap is that the convenient policy and the correct one look almost identical, and the convenient one (`AmazonS3FullAccess`, `s3:*` on `*`) ships faster. Wildcards are the dominant failure: `Action: *` widens what a role can do, `Resource: *` widens where, and together they turn a leaked read credential into full control of your object store while erasing any chance of auditing the blast radius. IAM verbs like `iam:PutRolePolicy` are worse still, because the permission to grant permissions is the permission to grant yourself everything — that's privilege escalation. Permission boundaries are the guardrail that makes this survivable at scale: a ceiling whose intersection with the identity policy clips any over-grant, so delegated role creation stays fast without trusting every engineer to write a perfect policy. And least privilege is a ratchet, not a one-time grant — drive policies down toward observed last-accessed usage over time. Next time you see `s3:*` on a role that reads one bucket, your first question is: what has this role actually called, and what's the one ARN I can scope it to?
</Recap>
