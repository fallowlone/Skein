---
concepts:
- kubernetes-rbac
- service-account
- role-binding
- least-privilege
- cluster-admin
deepensInto: []
estMin: 15
lang: en
lessonType: topic
level: middle
order: 3
prereqs: []
slug: 03-kubernetes-rbac
sources:
- https://kubernetes.io/docs/concepts/security/
- https://owasp.org/www-project-kubernetes-top-ten/
spiral: []
status: ready
summary: 'Kubernetes RBAC is deny-by-default, but the foot-guns are in the grants: a wildcard verb, a Role that can create pods, or a stray cluster-admin binding quietly equals full-cluster takeover. Least privilege here is a verb-by-verb discipline, not a checkbox.'
title: Kubernetes RBAC
track: security-cloud
unit: 02-container-and-kubernetes-security
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A teammate is debugging a flaky deploy and the fastest way to make the error go away is a one-liner everyone has typed at 2 a.m.: `kubectl create clusterrolebinding ci-debug --clusterrole=cluster-admin --serviceaccount=ci:runner`. The pipeline goes green, the ticket closes, nobody reverts it. Six months later that CI runner's pod is popped through an unrelated app bug — and because its service-account token is mounted at `/var/run/secrets/kubernetes.io/serviceaccount/token` and bound to `cluster-admin`, the attacker doesn't get one pod. They get every secret in every namespace, the ability to schedule a privileged pod on any node, and a path to the underlying host. RBAC was on the whole time. It said "yes" because someone told it to.
</Hook>

By the end of this lesson you'll be able to read a Role/RoleBinding pair and say exactly what it grants, spot the three bindings that silently equal cluster takeover, and design a service-account that can do its job and nothing else.

<Crux>Kubernetes RBAC is deny-by-default, but one wildcard verb or a cluster-admin binding hands an attacker the whole cluster.</Crux>

<Explanation>

## The four nouns, and why two of them are the trap

Kubernetes RBAC is built from exactly four object kinds, and the entire security model lives in how you combine them.

- A **Role** is a namespaced list of rules: tuples of `apiGroups`, `resources`, and `verbs` (`get`, `list`, `watch`, `create`, `update`, `patch`, `delete`). A Role grants nothing until it is bound.
- A **ClusterRole** is the same shape but cluster-scoped — it can name cluster-wide resources (nodes, persistentvolumes), and it can be reused across namespaces.
- A **RoleBinding** attaches a Role (or a ClusterRole) to a subject *inside one namespace*.
- A **ClusterRoleBinding** attaches a ClusterRole to a subject *across the entire cluster*.

The subject is usually a **ServiceAccount** — the identity every pod runs as. Here is the part people miss: by default each pod gets a service-account token mounted into its filesystem, and that token *is* the pod's credential to the API server. So the blast radius of any RBAC grant is "whatever code an attacker can get running in a pod that uses that service account." RBAC is genuinely **deny-by-default** — a brand-new service account can do essentially nothing — but the danger isn't the default. It's the grants you add, because two combinations turn a small mistake into total compromise: a `ClusterRoleBinding` (cluster-wide, not one namespace) and the built-in `cluster-admin` ClusterRole (every verb on every resource, including `*`).

<FlowDiagram
  label="How a Kubernetes API request is authorized: a pod presents its mounted service-account token, the API server authenticates it, then RBAC evaluates the verb plus resource against every Role and ClusterRole bound to that subject; a single matching allow rule grants the request, and there are no deny rules, so a wildcard or a cluster-admin binding short-circuits the whole check to yes."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "pod", label: "Pod", sub: "runs as SA" },
    { id: "token", label: "SA token", sub: "auto-mounted" },
    { id: "api", label: "API server", sub: "authn ok" },
    { id: "rbac", label: "RBAC check", sub: "verb+resource" },
    { id: "allow", label: "Allowed", sub: "any match wins" },
  ]}
  edges={[
    { from: "pod", to: "token" },
    { from: "token", to: "api", label: "presents" },
    { from: "api", to: "rbac", label: "authz" },
    { from: "rbac", to: "allow", label: "1 allow rule" },
  ]}
  caption="RBAC is additive and allow-only: it unions every rule bound to the subject and grants the request if any one rule matches. There is no deny rule to claw a grant back, so the safe posture is to never over-grant in the first place — one wildcard verb or a cluster-admin binding makes the check return yes for everything."
/>

## The cluster-admin foot-guns, ranked

The reason RBAC incidents look identical across companies is that the same three patterns keep appearing. Here is what each one actually grants, why it's tempting, and the safe replacement.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Foot-gun</th>
        <th class="border border-border px-3 py-2 text-left">What it really grants</th>
        <th class="border border-border px-3 py-2 text-left">Why it happens</th>
        <th class="border border-border px-3 py-2 text-left">Least-privilege fix</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">cluster-admin binding</td>
        <td class="border border-border px-3 py-2">Every verb on every resource, all namespaces — full takeover</td>
        <td class="border border-border px-3 py-2">"Make the error go away" debugging; copy-pasted Helm values</td>
        <td class="border border-border px-3 py-2">A scoped Role with the 3-4 verbs actually used</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Wildcard verb/resource (<code>&#42;</code>)</td>
        <td class="border border-border px-3 py-2">Today's resources <em>and every future one</em> the operator adds</td>
        <td class="border border-border px-3 py-2">"I don't know the exact verbs yet, I'll tighten later"</td>
        <td class="border border-border px-3 py-2">Enumerate verbs explicitly; never ship <code>verbs: ["*"]</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>create pods</code> / <code>pods/exec</code></td>
        <td class="border border-border px-3 py-2">Schedule a privileged pod or exec in — pivot to host/other SAs</td>
        <td class="border border-border px-3 py-2">Looks like a normal app verb; reviewers wave it through</td>
        <td class="border border-border px-3 py-2">Treat pod-create as privileged; gate with Pod Security + policy</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>get/list secrets</code></td>
        <td class="border border-border px-3 py-2">Read every secret in scope, including other SAs' tokens</td>
        <td class="border border-border px-3 py-2">"The app needs one secret" → granted on the whole verb</td>
        <td class="border border-border px-3 py-2">Scope by <code>resourceNames</code>; prefer projected/CSI secrets</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>escalate</code> / <code>bind</code> on roles</td>
        <td class="border border-border px-3 py-2">Grant yourself permissions you don't have — privilege escalation</td>
        <td class="border border-border px-3 py-2">Hidden in an operator's ClusterRole nobody audits</td>
        <td class="border border-border px-3 py-2">Never grant <code>escalate</code>/<code>bind</code> to workloads</td>
      </tr>
    </tbody>
  </table>
</div>

The throughline: RBAC has no `deny` rule. You cannot "subtract" a permission after the fact — the union of all bound rules is what the subject can do, and if any rule says yes, it's yes. That is *why* over-granting is so dangerous: there's no safety net rule to catch you, so least privilege has to be enforced at write time, verb by verb. The aggregation behavior makes this worse: ClusterRoles labeled `rbac.authorization.k8s.io/aggregate-to-edit: "true"` get *automatically* merged into the built-in `edit` role, so a custom resource's operator can silently widen what `edit`-bound users can do without you touching your bindings.

<Inset kind="mistake" lang="en">
The most common real-world escalation isn't `cluster-admin` directly — it's `create pods` combined with no Pod Security restriction. If a service account can create a pod, an attacker who owns that SA can schedule a pod with `hostPath: /` mounted, or `hostPID: true`, read the kubelet's credentials off the node, and pivot to *every* workload on that node — including ones with far more powerful tokens. That's why "can this SA create pods?" is the single highest-signal question in an RBAC review, and why pod-create must be paired with the Pod Security Admission `restricted` profile to mean anything.
</Inset>

## Designing a least-privilege service account

The senior reflex is to build grants bottom-up from observed need, not top-down from convenience. The workflow:

1. **Start from deny.** A new ServiceAccount has no permissions. Resist the urge to bind a broad built-in role "to get unblocked."
2. **Enumerate the actual verbs.** Run the workload, capture which API calls it makes (audit logs, or `kubectl auth can-i --list`), and grant exactly those — `get`/`list`/`watch` on the specific resources, nothing wildcarded.
3. **Prefer Role over ClusterRole, RoleBinding over ClusterRoleBinding.** Keep the grant in one namespace unless the resource is genuinely cluster-scoped. A namespaced grant caps the blast radius at one namespace.
4. **Scope secrets by `resourceNames`.** If the app needs one secret, name it; don't grant the whole `secrets` verb.
5. **Turn off the token mount when unused.** Set `automountServiceAccountToken: false` on pods that never call the API server — most app pods don't, and an unmounted token is a credential that can't be stolen.
6. **Audit with `kubectl auth can-i`.** Before shipping, ask the cluster directly: `kubectl auth can-i create pods --as=system:serviceaccount:app:web -n app` should return `no`.

<TradeoffMatrix
  id="03-kubernetes-rbac-tradeoff-1"
  lessonSlug="03-kubernetes-rbac"
  lang="en"
  prompt="A backend pod needs to read one ConfigMap and one Secret in its own namespace to boot. CI is red because the SA can't read them. Pick the correct grant."
  options={[
    { name: "A Role in the app namespace with get on the named configmap and secret (via resourceNames), bound with a RoleBinding to the pod's ServiceAccount", summary: "Scoped, named, namespaced.", correct: true, justification: "This is least privilege done right: the grant is namespaced (Role + RoleBinding, blast radius = one namespace), limited to the exact verbs the pod uses (get, not list/watch/delete), and scoped to the specific objects by resourceNames so the SA can't read other secrets in the namespace. If the pod is later compromised, the stolen token reads two named objects and nothing else." },
    { name: "Bind the built-in `view` ClusterRole to the ServiceAccount with a ClusterRoleBinding so it can read what it needs", summary: "Just use view.", justification: "`view` grants read on most resource types across the cluster when bound cluster-wide — including every ConfigMap and Secret in every namespace. A compromised token now reads the whole cluster's config and secrets. It 'works' but the blast radius is the entire cluster instead of two named objects." },
    { name: "Add a Role with get/list/watch on secrets and configmaps (whole verbs, no resourceNames) in the namespace", summary: "Whole-verb in-namespace.", justification: "Better than cluster-wide, but still over-granted: list on secrets lets the token read every secret in the namespace, including other service accounts' tokens, which is a classic lateral-movement step. The fix is to name the two objects with resourceNames and drop list/watch the pod doesn't use." },
    { name: "Set the pod's serviceAccountName to default and rely on the namespace's default permissions", summary: "Use default SA.", justification: "The default ServiceAccount has no useful read permissions by design, so this won't fix CI — and worse, piling grants onto the default SA means every pod in the namespace that didn't override it inherits them. Each workload should have its own minimal SA, never share the default." },
  ]}
/>

<Quiz
  id="03-kubernetes-rbac-quiz-1"
  lessonSlug="03-kubernetes-rbac"
  lang="en"
  question="A Role grants `verbs: [list]` on `secrets` in one namespace, bound to a pod's ServiceAccount. The pod is compromised. What can the attacker do with the mounted token?"
  choices={[
    { label: "Read every Secret in that namespace — including other service accounts' tokens — enabling lateral movement", correct: true },
    { label: "Nothing, because `list` only returns names, not secret values", misconception: "In Kubernetes, `list secrets` returns the full objects including their data, not just names. `list` on secrets is effectively read-all-secrets in scope — one of the most over-granted and dangerous verbs." },
    { label: "Read secrets cluster-wide, because secrets are a cluster-scoped resource", misconception: "Secrets are namespaced, and this is a Role + RoleBinding, so the grant is capped at one namespace. The damage is still serious within that namespace, but it doesn't reach other namespaces here." },
    { label: "Only the one secret the app was designed to use, because RBAC infers intent from the code", misconception: "RBAC has no idea what the app 'intends.' The grant is `list` on the whole `secrets` resource, so the token reads all of them; scoping to one object requires resourceNames." },
  ]}
/>

<Quiz
  id="03-kubernetes-rbac-quiz-2"
  lessonSlug="03-kubernetes-rbac"
  lang="en"
  question="Why is `create pods` such a high-signal permission to flag in an RBAC review, even without an explicit cluster-admin binding?"
  choices={[
    { label: "A pod-create grant lets an attacker schedule a privileged/hostPath pod and pivot to the node and other workloads' tokens", correct: true },
    { label: "Creating pods consumes cluster quota and can cause a denial of service", misconception: "Resource exhaustion is real but secondary. The security reason pod-create is dangerous is that a created pod can mount the host, gain node access, and steal more powerful tokens — a full privilege-escalation path, not just noisy capacity use." },
    { label: "It's harmless on its own; only `cluster-admin` is dangerous", misconception: "That's the trap. Pod-create plus no Pod Security restriction is one of the most common real escalations — you don't need cluster-admin to take over a node if you can schedule a privileged pod." },
    { label: "Pods can't be deleted once created, so the grant is irreversible", misconception: "Pods are deletable; that's not the issue. The risk is what a maliciously-specified pod can mount and access on the host the moment it's scheduled." },
  ]}
/>

<DragOrder
  id="03-kubernetes-rbac-drag-1"
  lessonSlug="03-kubernetes-rbac"
  lang="en"
  prompt="Order these steps for designing a least-privilege service account, from first to last:"
  items={[
    { id: "a", label: "Start from deny — a fresh ServiceAccount with no bindings" },
    { id: "b", label: "Enumerate the exact verbs the workload actually calls" },
    { id: "c", label: "Grant a namespaced Role (prefer Role over ClusterRole), scoped by resourceNames" },
    { id: "d", label: "Bind it with a RoleBinding to that one ServiceAccount" },
    { id: "e", label: "Verify with `kubectl auth can-i` that nothing extra is allowed" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>RBAC is deny-by-default and allow-only — there is no deny rule, so the union of every bound grant is what a stolen service-account token can do. Build grants bottom-up: enumerate real verbs, prefer namespaced Role/RoleBinding, scope secrets by resourceNames, and treat cluster-admin bindings, wildcard verbs, and `create pods` as takeover-equivalent.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-kubernetes-rbac-retrieval"
  lang="en"
  questions={[
    { q: "Walk through how a single cluster-admin ClusterRoleBinding on a CI service account becomes full-cluster compromise when one pod is breached.", a: "Every pod runs as a ServiceAccount, and by default Kubernetes mounts that SA's bearer token into the pod at /var/run/secrets/kubernetes.io/serviceaccount/token — it's the pod's credential to the API server. If that SA is the subject of a ClusterRoleBinding to the built-in cluster-admin ClusterRole, the token authorizes every verb on every resource in every namespace, because RBAC is additive and allow-only and cluster-admin holds the wildcard `*`. So the moment an attacker gets code execution in that pod (through any unrelated app bug), they read the token off disk and replay it against the API server with full privileges: dump every Secret in every namespace (including other SAs' tokens), schedule a privileged pod with hostPath: / on any node to reach the underlying host, and move laterally to any workload. RBAC never failed — it was on the whole time and returned 'yes' because the binding told it to. The defenses are: never bind cluster-admin to a workload SA (use a scoped Role with the handful of verbs actually used), set automountServiceAccountToken: false on pods that don't call the API, and keep grants namespaced so a breach caps at one namespace." },
    { q: "Why is `list secrets` more dangerous than it looks, and what's the least-privilege alternative?", a: "Two reasons. First, in Kubernetes `list secrets` returns the full secret objects including their data, not just names — so the verb is effectively 'read every secret in scope,' and within a namespace that includes other service accounts' mounted tokens, which is a ready-made lateral-movement step. Second, RBAC can't infer intent: granting the `secrets` verb because the app needs one secret hands over all of them, since there's no deny rule to subtract the rest. The least-privilege alternative is to scope by resourceNames — a Role with verbs: [get] on the specific named secret — so the SA can read exactly the object it needs and nothing else; combine that with a namespaced RoleBinding (not cluster-wide), and prefer projected/CSI secret mounts so the value is delivered to the pod without granting any API read at all. The review reflex: any grant on the whole `secrets` resource without resourceNames, and especially `list` or `watch`, is a finding." },
  ]}
/>

<Recap lang="en">
Kubernetes RBAC is assembled from four objects — Role and ClusterRole (the verb/resource rules) plus RoleBinding and ClusterRoleBinding (which attach them to a subject, usually a ServiceAccount). The model is genuinely deny-by-default and allow-only: a fresh SA can do nothing, and the union of every bound rule is what its auto-mounted token can do, with no deny rule to claw a grant back. That's why the foot-guns are all over-grants, not defaults: a cluster-admin ClusterRoleBinding (every verb everywhere), a wildcard verb that also covers future resources, `create pods` (schedule a privileged pod, pivot to the node), `list secrets` (read every secret including other tokens), and `escalate`/`bind` (grant yourself more). Least privilege is a write-time discipline — start from deny, enumerate the real verbs, prefer namespaced Role + RoleBinding, scope secrets by resourceNames, turn off the token mount when unused, and verify with `kubectl auth can-i`. Now when you see a binding in a review, your first question is: what's the subject, is it cluster-wide, and can it create pods or read secrets?
</Recap>
