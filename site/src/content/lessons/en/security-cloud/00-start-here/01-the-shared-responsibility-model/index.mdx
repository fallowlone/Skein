---
concepts:
- shared-responsibility-model
- identity-is-the-perimeter
- cloud-misconfiguration
- iaas-paas-saas
- blast-radius
deepensInto: []
estMin: 15
lang: en
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-the-shared-responsibility-model
sources:
- https://aws.amazon.com/compliance/shared-responsibility-model/
- https://owasp.org/www-project-cloud-native-application-security-top-10/
spiral: []
status: ready
summary: The cloud provider secures the platform; you secure how you use it — and the line moves with IaaS, PaaS, and SaaS. The dominant failure isn't a 0-day, it's a misconfiguration on your side of the line. Identity is the new perimeter; blast radius is the metric.
title: The shared-responsibility model
track: security-cloud
unit: 00-start-here
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A team ships an analytics feature. The data lands in an S3 bucket, the app reads it, everyone moves on. Eight months later a security researcher emails: the bucket is world-readable and has been indexed by a public scanner. Inside are six million customer records. Nobody disabled encryption, nobody got phished, no CVE was exploited — someone left a bucket policy at `"Principal": "*"` because they assumed the cloud "secures storage." The provider *did* secure the disks, the datacenter, and the hypervisor. The bucket policy was never theirs to write. That gap — between what the provider runs and what you configure — is where most cloud breaches actually live.
</Hook>

By the end of this lesson you'll know exactly where the customer-vs-provider security line sits across IaaS, PaaS, and SaaS, and why a misconfiguration on *your* side of that line — not a 0-day — is the failure mode you'll spend your career defending against.

<Crux>The provider secures the platform; you secure how you use it. The line moves with the service model; your misconfig is the top failure.</Crux>

<Explanation>

## The line, and why it moves

Every major cloud frames security the same way: **security *of* the cloud is the provider's job; security *in* the cloud is yours.** AWS calls it the Shared Responsibility Model, and Azure and Google publish near-identical versions. The provider owns the physical datacenter, the hardware, the hypervisor, and the managed-service software it runs for you. You own your data, your identities and access policies, your network rules, and — depending on the service — the operating system and application code on top.

The trap is treating that boundary as fixed. It is not: **the more managed the service, the more of the stack the provider absorbs, and the less is left to you.** That is the entire point of the IaaS → PaaS → SaaS spectrum.

- **IaaS** (a raw EC2 instance, a bare VM): you get compute, storage, and network primitives. The provider secures the hypervisor and below; *everything from the guest OS up is yours* — patching, runtime, the app, the data, and the IAM and network config around it.
- **PaaS** (a managed app platform, a serverless function, a managed database): the provider also runs and patches the OS and runtime. You stop worrying about kernel CVEs, but you still own your code, your data, and — critically — the access and network configuration.
- **SaaS** (a finished product like a hosted CRM): the provider runs almost the whole stack. Your responsibility shrinks to your **data** and your **identity/access configuration** — who can log in, with what permissions, and how.

Notice the one column that never crosses to the provider, at any tier: **data and identity are always yours.** That is the single most important reading of the whole model.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Layer</th>
        <th class="border border-border px-3 py-2 text-left">IaaS (raw VM)</th>
        <th class="border border-border px-3 py-2 text-left">PaaS (managed runtime)</th>
        <th class="border border-border px-3 py-2 text-left">SaaS (finished product)</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2 font-medium">Data (yours)</td>
        <td class="border border-border px-3 py-2">Customer</td>
        <td class="border border-border px-3 py-2">Customer</td>
        <td class="border border-border px-3 py-2">Customer</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2 font-medium">IAM / access policy</td>
        <td class="border border-border px-3 py-2">Customer</td>
        <td class="border border-border px-3 py-2">Customer</td>
        <td class="border border-border px-3 py-2">Customer</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2 font-medium">Network config</td>
        <td class="border border-border px-3 py-2">Customer</td>
        <td class="border border-border px-3 py-2">Customer</td>
        <td class="border border-border px-3 py-2">Provider*</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2 font-medium">Application</td>
        <td class="border border-border px-3 py-2">Customer</td>
        <td class="border border-border px-3 py-2">Customer</td>
        <td class="border border-border px-3 py-2">Provider</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2 font-medium">Runtime</td>
        <td class="border border-border px-3 py-2">Customer</td>
        <td class="border border-border px-3 py-2">Provider</td>
        <td class="border border-border px-3 py-2">Provider</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2 font-medium">Operating system</td>
        <td class="border border-border px-3 py-2">Customer</td>
        <td class="border border-border px-3 py-2">Provider</td>
        <td class="border border-border px-3 py-2">Provider</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2 font-medium">Compute / virtualization</td>
        <td class="border border-border px-3 py-2">Provider</td>
        <td class="border border-border px-3 py-2">Provider</td>
        <td class="border border-border px-3 py-2">Provider</td>
      </tr>
    </tbody>
  </table>
  <p class="text-xs mt-2 opacity-70">*In SaaS the provider runs the network, but you still configure tenant-level access and any IP/allowlist settings exposed to you. Data and IAM never cross to the provider at any tier.</p>
</div>

## Identity is the new perimeter

In a datacenter, the perimeter was a place: a firewall, a VLAN, a locked rack. Inside was trusted, outside was hostile, and "security" largely meant guarding the edge. The cloud erases that geography. Your services, data stores, and management plane are reachable over the internet through APIs, and the only thing standing between a request and your resources is **who the request proves it is, and what that identity is permitted to do.**

So the perimeter moved. It is no longer a network boundary you can point at — it is the set of identities (users, roles, service accounts, access keys) and the policies attached to them. A leaked access key, an over-permissive IAM role, a service account with `*:*`, or an unauthenticated endpoint *is* the breach path. There is no inner network to fall back on. This is why "identity is the new perimeter" is not a slogan — it is a literal description of where the wall is now, and it is why the next unit in this track is about IAM.

The practical consequence is **least privilege as a default reflex**: every identity gets only the permissions it actually needs, scoped to the specific resources and actions it uses, and nothing more. A wildcard policy is not a convenience — it is a pre-staged breach.

<FlowDiagram
  label="Why misconfiguration dominates: the provider hardens the platform layers (datacenter, hypervisor, managed OS/runtime), so attackers stop attacking those and instead probe the customer-owned layers — a public bucket policy, an over-permissive IAM role, an open security group. The fix is least privilege and deny-by-default config on the customer side, which shrinks the blast radius."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "platform", label: "Provider hardens platform", sub: "DC, hypervisor, managed OS" },
    { id: "shift", label: "Attack shifts up", sub: "to your config" },
    { id: "misconfig", label: "Misconfiguration", sub: "public bucket, wide IAM" },
    { id: "breach", label: "Data exposed", sub: "no 0-day needed" },
    { id: "fix", label: "Least privilege", sub: "deny by default" },
  ]}
  edges={[
    { from: "platform", to: "shift", label: "so attackers" },
    { from: "shift", to: "misconfig" },
    { from: "misconfig", to: "breach", label: "no check" },
    { from: "misconfig", to: "fix", label: "the fix" },
  ]}
  caption="The provider's hardening pushes attackers up the stack onto the layers you own. The dominant cloud failure is a misconfiguration there — and least-privilege, deny-by-default configuration is what shrinks the blast radius."
/>

## Misconfiguration, not 0-days, is the dominant failure

Here is the senior-level reframe of cloud security. The headlines say "breach," and you picture a brilliant exploit. The reality is almost always duller and entirely your side of the line: a storage bucket left public, an IAM policy with a wildcard, a database with no auth bound to a public IP, a security group open to `0.0.0.0/0`, a secret committed to a repo, default credentials never rotated. Industry post-mortems and cloud-provider guidance converge on the same finding year after year — **the overwhelming majority of cloud data exposures trace to customer misconfiguration, not to a vulnerability in the provider's platform.** Gartner's widely cited framing is that through 2025, the vast majority of cloud security failures will be the customer's fault. The provider's side of the line is, frankly, hardened far better than most teams can harden their own datacenter.

That changes where you spend effort. You are not primarily hunting for novel exploits; you are making sure the boring controls are correct and *stay* correct: every bucket private by default, every IAM policy scoped, every network rule justified, every secret in a secret manager. This is why the rest of this track is about **posture** — continuously checking that your side of the line is configured the way you think it is — far more than it is about exotic attacks.

<Inset kind="why" lang="en">
Why is the customer's side so much weaker than the provider's, given the same engineers? Because the provider configures one hardened platform millions of times with deep automation and a dedicated security org, while every customer reinvents their config under deadline pressure, with broad defaults that favor "it works" over "it's locked down." A new bucket is easy to make public; a new role is easy to grant `*`. The model doesn't make you less safe — it hands you a powerful, sharp tool and trusts you to hold it correctly. Most breaches are someone cutting themselves.
</Inset>

## Blast radius: the metric that ties it together

Least privilege isn't an end in itself — it's how you control **blast radius**: the scope of damage when (not if) one identity, key, or component is compromised. Assume any single credential will eventually leak. The question that matters is: *when it does, how much can the attacker reach?*

- A leaked key scoped to read one bucket exposes one bucket.
- A leaked key with `AdministratorAccess` exposes the entire account.

Same leak, wildly different outcomes — and the difference is entirely the permissions you granted in advance. This is the through-line for the whole track: IAM scoping, container isolation, infrastructure-as-code reviewed before it ships, and posture scanning all exist to keep blast radius small. You design as if compromise is inevitable, because at scale it is, and you make sure the cost of any single compromise stays contained.

<TradeoffMatrix
  id="01-shared-responsibility-tradeoff-1"
  lessonSlug="01-the-shared-responsibility-model"
  lang="en"
  prompt="Your app stores customer exports in an S3 bucket. A teammate says 'the cloud provider secures our storage, so the bucket is safe by default.' What's the correct response under the shared-responsibility model?"
  options={[
    { name: "The provider secures the storage infrastructure, but the bucket policy and access are yours: make it private by default, scope read access to a least-privilege role, and enable access logging", summary: "Your side of the line.", correct: true, justification: "Storage durability, encryption-at-rest hardware, and the disks are the provider's responsibility — but who can read the bucket is configuration you own. Default-private plus a narrowly scoped role is exactly the least-privilege, deny-by-default control the model demands. This is the layer attackers actually hit." },
    { name: "Correct — since the provider runs the storage service, bucket access is handled for us and needs no policy", summary: "Trust the platform.", justification: "This is the exact mistake that causes public-bucket breaches. The provider secures the platform (disks, datacenter, durability), but the bucket policy — who can access the data — is always the customer's side of the line. Assuming it's handled is how six-million-record leaks happen." },
    { name: "Rename the bucket to something unguessable so attackers can't find it", summary: "Obscure the name.", justification: "Security through obscurity. Public buckets are found by automated scanners that enumerate and index them regardless of name, and the URL leaks through logs, code, and referrers. An unguessable name with a public policy is still public." },
    { name: "Buy a third-party WAF and route bucket traffic through it", summary: "Bolt-on perimeter.", justification: "A WAF guards application HTTP traffic, not direct object access to a misconfigured bucket policy. This bolts a perimeter onto the wrong layer instead of fixing the access policy at the source — exactly the 'old perimeter' thinking the cloud model replaces with identity-scoped access." },
  ]}
/>

<Quiz
  id="01-shared-responsibility-quiz-1"
  lessonSlug="01-the-shared-responsibility-model"
  lang="en"
  question="You move an app from a self-managed VM (IaaS) to a managed serverless platform (PaaS). Which responsibility shifts to the provider — and which stays with you?"
  choices={[
    { label: "OS and runtime patching shift to the provider; your code, your data, and your IAM/access config stay with you", correct: true },
    { label: "Everything shifts to the provider — that's the point of going serverless", misconception: "PaaS absorbs the OS and runtime, but your application code, your data, and your access/identity configuration never cross the line. Those stay yours at every tier including SaaS." },
    { label: "Nothing changes — the responsibility line is the same for all cloud services", misconception: "The line is not fixed; it moves with the service model. The more managed the service, the more of the lower stack the provider absorbs. That movement is the whole point of IaaS vs PaaS vs SaaS." },
    { label: "Your data becomes the provider's responsibility because they now run the platform", misconception: "Data is the one thing that is always the customer's responsibility, at every tier. The provider running the platform never makes your data their job to secure." },
  ]}
/>

<Quiz
  id="01-shared-responsibility-quiz-2"
  lessonSlug="01-the-shared-responsibility-model"
  lang="en"
  question="An access key leaks. It was scoped with a wildcard policy granting `*:*` across the account 'to save time.' What concept does this most directly violate, and why does it matter?"
  choices={[
    { label: "Least privilege — the wildcard gives the leaked key account-wide blast radius instead of containing it to the few actions the key actually needed", correct: true },
    { label: "Defense in depth — the key should have had a second password", misconception: "The issue isn't a missing second factor on the key; it's that the key's permissions were unbounded. Least privilege limits what a single compromised identity can reach, which is the blast-radius problem here." },
    { label: "Encryption at rest — the key should have been encrypted in storage", misconception: "Encryption protects data at rest; it does nothing about an over-broad permission set. Even a perfectly stored key with `*:*` exposes the whole account once leaked." },
    { label: "Nothing — leaked keys are the provider's responsibility to rotate", misconception: "Keys and the policies attached to them are squarely the customer's side of the line. Identity is the new perimeter, and scoping it is your job, not the provider's." },
  ]}
/>

<DragOrder
  id="01-shared-responsibility-drag-1"
  lessonSlug="01-the-shared-responsibility-model"
  lang="en"
  prompt="Order these layers from the one the provider always owns (top) to the one the customer always owns (bottom):"
  items={[
    { id: "a", label: "Compute / virtualization (hypervisor, hardware)" },
    { id: "b", label: "Operating system (managed in PaaS/SaaS)" },
    { id: "c", label: "Application code (yours in IaaS/PaaS)" },
    { id: "d", label: "IAM / access policy (always the customer's)" },
    { id: "e", label: "Data (always the customer's)" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Security *of* the cloud is the provider's; security *in* the cloud is yours — and the line moves with IaaS → PaaS → SaaS, but data and IAM are always yours. Identity is the new perimeter, misconfiguration (not a 0-day) is the dominant failure, and least privilege is how you keep blast radius small.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-shared-responsibility-retrieval"
  lang="en"
  questions={[
    { q: "Explain the shared-responsibility model and how the customer-vs-provider line moves across IaaS, PaaS, and SaaS.", a: "The model splits cloud security into 'security of the cloud,' which is the provider's job, and 'security in the cloud,' which is yours. The provider always owns the physical datacenter, hardware, and hypervisor (the compute/virtualization layer). What you own depends on how managed the service is. In IaaS — a raw VM — the provider secures only the hypervisor and below; everything from the guest OS up is yours: OS patching, runtime, application code, data, plus the IAM and network configuration. In PaaS — a managed runtime or serverless platform — the provider also runs and patches the OS and runtime, so you stop worrying about kernel CVEs, but you still own your code, your data, and your access/network config. In SaaS — a finished hosted product — the provider runs almost the whole stack, and your responsibility shrinks to your data and your identity/access configuration. The constant across every tier is that data and IAM never cross to the provider: they are always the customer's responsibility. The line moves down the stack toward the provider as the service gets more managed, but it never crosses those two." },
    { q: "Why is 'identity is the new perimeter,' and why is misconfiguration — not a 0-day — the dominant cloud failure mode?", a: "In a datacenter the perimeter was physical: a firewall and a trusted internal network. The cloud exposes your resources and management plane over the internet through APIs, so the only thing gating a request is the identity it proves and the permissions that identity holds — there is no inner trusted network to fall back on. That makes identities (users, roles, service accounts, access keys) and their attached policies the actual perimeter; a leaked key or an over-permissive role is itself the breach path. Misconfiguration dominates because the provider hardens its platform extremely well — datacenter, hypervisor, managed OS and runtime — far better than most teams could, so attackers stop attacking those layers and probe the layers you own instead: public storage buckets, wildcard IAM policies, open security groups, exposed databases, committed secrets. Year after year, post-mortems and provider guidance find the overwhelming majority of cloud exposures trace to customer misconfiguration rather than a flaw in the provider's platform. The defense is least privilege and deny-by-default configuration on your side of the line, which keeps the blast radius — how much a single leaked credential or compromised component can reach — small, on the assumption that some compromise is eventually inevitable." },
  ]}
/>

<Recap lang="en">
The shared-responsibility model splits cloud security into security *of* the cloud (the provider's job: datacenter, hardware, hypervisor, and the managed OS/runtime in higher tiers) and security *in* the cloud (yours). The line is not fixed — it slides down the stack toward the provider as you move from IaaS to PaaS to SaaS — but two layers never cross: your data and your IAM/access configuration are always yours. Because resources are reachable over APIs with no inner trusted network, identity becomes the new perimeter: a leaked key or over-permissive role is the breach path, so least privilege is the default reflex. And the dominant cloud failure is not a 0-day in the provider's platform — that side is hardened better than you could manage — but a misconfiguration on your side: a public bucket, a wildcard policy, an open security group. The metric that ties it together is blast radius: you scope every identity and resource as if compromise is inevitable, so that when one credential leaks, it reaches as little as possible. That through-line — least privilege to contain blast radius — is what the IAM, container, infrastructure-as-code, and posture units of this track all build on.
</Recap>
