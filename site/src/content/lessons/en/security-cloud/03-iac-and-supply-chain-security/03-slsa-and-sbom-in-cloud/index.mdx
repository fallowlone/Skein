---
concepts:
- slsa
- sbom
- build-provenance
- artifact-signing
- supply-chain-integrity
deepensInto: []
estMin: 15
lang: en
lessonType: topic
level: middle
order: 3
prereqs: []
slug: 03-slsa-and-sbom-in-cloud
sources:
- https://slsa.dev/
- https://owasp.org/www-project-devsecops-guideline/
spiral: []
status: ready
summary: 'SolarWinds and the XZ backdoor shipped through trusted build pipelines, not exploits. SLSA provenance plus signing proves how an artifact was built and that it is the one you reviewed; an SBOM lists what is inside so you can answer ''am I affected'' in minutes.'
title: SLSA and SBOM in delivery
track: security-cloud
unit: 03-iac-and-supply-chain-security
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Your scanners were green. Every dependency had a clean CVE report, the image passed the registry policy gate, and the deploy went out on a Friday. Three weeks later you learn the version you shipped was never the version in your repo: someone with write access to a self-hosted build runner swapped one compiled object after the tests ran. That is exactly the SolarWinds shape — a malicious DLL injected into a *trusted, signed, passing* build, distributed to 18,000 organizations. No exploit kit cracked the perimeter. The perimeter built the malware for them. The whole class of supply-chain compromise lives in the gap between "this artifact passed" and "this artifact is the one I reviewed" — and closing that gap is what SLSA provenance, artifact signing, and SBOMs are for.
</Hook>

By the end of this lesson you'll know what provenance and signing actually prove (and what they don't), where an SBOM fits, and how to reach the SLSA level that would have caught a SolarWinds-style tamper.

<Crux>Provenance proves how an artifact was built, signing proves it is that one, an SBOM lists what is inside — so you trust what deploys.</Crux>

<Explanation>

## The trust gap a passing build leaves open

A normal pipeline answers "did the code pass?" It does not answer "is the thing I'm about to deploy the genuine output of that passing run, unmodified, from source I control?" Those are different questions, and the second one is where supply-chain attacks live. SolarWinds (a build-system implant), Codecov (a leaked bash-uploader credential rewriting CI output), and the 2024 XZ Utils backdoor (a malicious maintainer landing obfuscated code into release tarballs that *differed from the git tree*) all bypassed every input-scanning control teams had. None of them was an injection bug or an unpatched CVE. They were tampering with the *production of the artifact* — and your dependency scanner, by design, never looks there.

So the supply-chain problem decomposes into three questions you must be able to answer about anything you deploy:

- **How was it built?** — the build's *provenance*: which source commit, which builder, which parameters, with no human able to silently inject steps. This is SLSA's domain.
- **Is this the exact artifact that was built?** — *integrity*, established by a cryptographic *signature* over the artifact's digest, verified at admission time.
- **What is inside it?** — its *bill of materials*: every component and version, so when the next Log4Shell drops you can answer "are we affected?" from an index instead of a frantic grep across every service. This is the SBOM.

SLSA, signing, and SBOM are not competing standards — they answer those three questions in order, and you need all three. Provenance with no signature is a claim anyone can forge; a signature with no provenance proves *who* released it but not *that the build was honest*; both without an SBOM leave you blind the day a transitive dependency turns out to be radioactive.

<FlowDiagram
  label="The supply-chain trust chain: source commit flows into a hardened, isolated builder that emits both the artifact and signed SLSA provenance attesting how it was built; an SBOM enumerates the artifact's components; at deploy time an admission gate verifies the signature and provenance and rejects anything whose digest, source, or builder identity doesn't match policy."
  perRow={3}
  hue="var(--d-crypto)"
  nodes={[
    { id: "src", label: "Source commit", sub: "git, 2-person review" },
    { id: "build", label: "Hardened builder", sub: "isolated, ephemeral" },
    { id: "artifact", label: "Artifact + digest", sub: "sha256:..." },
    { id: "prov", label: "Signed provenance", sub: "SLSA attestation" },
    { id: "sbom", label: "SBOM", sub: "every component" },
    { id: "gate", label: "Admission gate", sub: "verify or reject" },
  ]}
  edges={[
    { from: "src", to: "build", label: "input" },
    { from: "build", to: "artifact", label: "produces" },
    { from: "build", to: "prov", label: "attests" },
    { from: "artifact", to: "sbom", label: "scanned" },
    { from: "artifact", to: "gate", label: "deploy" },
    { from: "prov", to: "gate", label: "verified" },
  ]}
  caption="The three controls compose: a hardened builder emits the artifact, its signed SLSA provenance, and an SBOM; the admission gate verifies signature + provenance against policy before anything runs. Tampering after the build breaks the digest match and is rejected."
/>

## SLSA: provenance and the levels ladder

SLSA (Supply-chain Levels for Software Artifacts, pronounced "salsa") is a framework that grades how trustworthy a build process is. Its core artifact is **provenance**: a signed, machine-readable statement that says *this output digest was produced from that source revision, by that builder, with these parameters*. The point is non-repudiation of the build itself — a verifier downstream can check that the binary in front of it actually came out of the pipeline it claims to, rather than being swapped in afterward.

SLSA v1.0 defines a **Build track** with levels that escalate the *guarantees about the builder*, not the amount of paperwork:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Build level</th>
        <th class="border border-border px-3 py-2 text-left">What it requires</th>
        <th class="border border-border px-3 py-2 text-left">What it stops</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">L0</td>
        <td class="border border-border px-3 py-2">No guarantees</td>
        <td class="border border-border px-3 py-2">Nothing — the default</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">L1</td>
        <td class="border border-border px-3 py-2">Scripted build + provenance exists (may be unsigned)</td>
        <td class="border border-border px-3 py-2">Mistakes-of-record; "what built this?" is answerable</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">L2</td>
        <td class="border border-border px-3 py-2">Hosted build platform + signed provenance</td>
        <td class="border border-border px-3 py-2">Forged provenance; tamper after build is detectable</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">L3</td>
        <td class="border border-border px-3 py-2">Hardened, isolated builder; provenance is unforgeable by build steps</td>
        <td class="border border-border px-3 py-2">A malicious build step injecting itself — the SolarWinds shape</td>
      </tr>
    </tbody>
  </table>
</div>

The jump that matters is **L2 → L3**. At L2 a signed provenance proves the artifact wasn't swapped *after* the build, but a compromised build script could still lie in the provenance it generates. L3 demands the build run in an **isolated, ephemeral environment** where the provenance is generated by the *platform*, not by user-controlled build steps — so the malicious step that swapped SolarWinds' object can't also forge a clean attestation about itself. That isolation is the actual security boundary; the level number is just shorthand for "how much can a bad build step lie."

The practical path is unglamorous: most managed CI (GitHub Actions with the official SLSA generator, GCB, Tekton Chains) gives you signed L2/L3 provenance for the cost of a workflow change. The trap is a *self-hosted runner you reuse across jobs* — shared state between builds collapses you back below L3 no matter what attestation you emit, because a prior job can poison the next.

## Signing: keyless, with a transparency log

Provenance is only as good as the signature over it, and key management is where signing programs go to die. The modern answer is **keyless signing** (Sigstore's cosign): instead of a long-lived private key sitting in a secret store waiting to leak, the builder authenticates with a short-lived OIDC identity (e.g. the GitHub Actions workload identity), Fulcio issues an ephemeral certificate bound to that identity, the signature is made, the key is discarded, and the whole event is recorded in **Rekor**, a public append-only transparency log.

This buys two things a static key can't. First, there's no key to steal — the signing key existed for seconds. Second, the Rekor entry is *tamper-evident*: a backdated or forged signature has no matching transparency-log record, so verification can require "must appear in Rekor before timestamp T." You verify by *identity and provenance*, not by trusting that a key was kept safe for three years.

<Inset kind="mistake" lang="en">
The seductive mistake is treating a green signature as proof of safety. A signature only proves *integrity and origin* — "this is the exact artifact identity X published." It says nothing about whether the artifact is malicious. The XZ backdoor was, in a sense, properly released by the project's own maintainer; a signature would have verified cleanly. Signing stops *tampering and impersonation*; it does not vouch for intent. You still need provenance (was the build honest?), SBOM-driven scanning (what's inside?), and code review (is the source itself trustworthy?). Treating "signed" as "safe" is how a verified-but-evil artifact sails through the gate.
</Inset>

## SBOM: answering "are we affected?" in minutes

An SBOM (Software Bill of Materials) is the ingredient list: every component, version, and license inside an artifact, in a standard machine format — **SPDX** or **CycloneDX**. You generate it at build time (e.g. Syft, or your build tool's native exporter), attach it as a signed attestation alongside the provenance, and store it where incident response can query it.

Its value is asymmetric and shows up on exactly one kind of day. When Log4Shell broke in December 2021, the teams without an SBOM spent days running `find` across hundreds of services to discover whether `log4j-core` was bundled — often transitively, three levels deep, inside a fat JAR they didn't know contained it. The teams *with* a queryable SBOM index answered "which of our 400 deployed artifacts contain log4j-core in the affected range?" in minutes and scoped their response to the handful that did. The SBOM doesn't prevent the vulnerability; it collapses your mean-time-to-answer from days to a query, which is the difference between a controlled rollout and a weekend of panic.

The senior nuance: an SBOM is only trustworthy if it's generated from the *built artifact* and *signed* as an attestation, not hand-maintained in a wiki. A stale or build-from-source-only SBOM that misses what the final image actually bundled is worse than none, because it answers "are we affected?" with false confidence.

<TradeoffMatrix
  id="03-slsa-sbom-tradeoff-1"
  lessonSlug="03-slsa-and-sbom-in-cloud"
  lang="en"
  prompt="A team builds release artifacts on a self-hosted CI runner that is reused across all jobs, signs them with a long-lived private key stored in the secrets manager, and emits SLSA provenance. They want to actually prevent a SolarWinds-style tamper. Pick the change that closes the real gap."
  options={[
    { name: "Move builds to an isolated, ephemeral builder where provenance is generated by the platform (target SLSA L3), and adopt keyless signing with a transparency log", summary: "Fix the build boundary.", correct: true, justification: "The SolarWinds shape is a malicious build step that injects itself AND lies in its own attestation. A reused self-hosted runner shares state across jobs, so a prior job can poison the next and provenance generated by user-controlled steps can be forged — that's below L3 no matter what you emit. Running each build in an isolated, ephemeral environment with platform-generated provenance (L3) is the actual security boundary, and keyless signing removes the long-lived key as a theft target while Rekor makes backdated signatures detectable." },
    { name: "Rotate the long-lived signing key every 30 days and store it in an HSM", summary: "Harden the key.", justification: "Better key hygiene, but it doesn't touch the real gap. The tamper happens during the build on a shared runner; a perfectly protected key will faithfully sign the tampered artifact. You'd produce a cryptographically valid signature over malware. Key rotation addresses key theft, not build integrity." },
    { name: "Add more dependency and image vulnerability scanners to the pipeline", summary: "Scan harder.", justification: "Input scanning never looks at whether the artifact is the honest output of the build. SolarWinds, Codecov, and XZ all passed input scans — the malicious change was introduced in or after the build, not as a known-vulnerable dependency. More scanners catch known CVEs, not build tampering." },
    { name: "Require two-person review on all source commits and call it done", summary: "Review the source only.", justification: "Source review is necessary (it's part of stopping an XZ-style malicious commit) but insufficient here: the attack modifies the artifact during the build, after the reviewed source is in. Without build isolation and verifiable provenance, honest source can still produce a tampered binary." },
  ]}
/>

<Quiz
  id="03-slsa-sbom-quiz-1"
  lessonSlug="03-slsa-and-sbom-in-cloud"
  lang="en"
  question="What does an artifact signature (e.g. cosign) actually prove?"
  choices={[
    { label: "Integrity and origin — this is the exact artifact that identity X published, unmodified", correct: true },
    { label: "That the artifact is free of malware", misconception: "A signature says nothing about intent. The XZ backdoor would have verified cleanly because the project's own maintainer released it. Signing stops tampering and impersonation, not malicious-but-authentic code." },
    { label: "That the build ran in an isolated environment", misconception: "That's a SLSA L3 provenance property, established by the build platform — not something a signature over the artifact digest proves on its own." },
    { label: "That every dependency inside it is up to date", misconception: "Dependency freshness is what an SBOM plus vulnerability scanning answers. A signature is silent about what's inside the artifact." },
  ]}
/>

<Quiz
  id="03-slsa-sbom-quiz-2"
  lessonSlug="03-slsa-and-sbom-in-cloud"
  lang="en"
  question="Why is the SLSA L2 → L3 jump the one that matters for a SolarWinds-style attack?"
  choices={[
    { label: "L3 runs the build in an isolated, ephemeral environment with platform-generated provenance, so a malicious build step can't forge a clean attestation about itself", correct: true },
    { label: "L3 requires more documentation and a signed compliance form", misconception: "SLSA levels grade guarantees about the builder, not paperwork. L3 is about build isolation and unforgeable provenance, not documents." },
    { label: "L3 adds vulnerability scanning of all dependencies", misconception: "Dependency scanning isn't part of the SLSA build track at all; that's the SBOM/scanning concern. L3 is about whether the build process itself can be trusted." },
    { label: "L2 has no signature, L3 adds one", misconception: "L2 already requires signed provenance. The L2→L3 jump is build-step isolation so the provenance can't be forged by a compromised step — not the presence of a signature." },
  ]}
/>

<DragOrder
  id="03-slsa-sbom-drag-1"
  lessonSlug="03-slsa-and-sbom-in-cloud"
  lang="en"
  prompt="Order the verified supply-chain pipeline from source to running workload:"
  items={[
    { id: "a", label: "Reviewed source commit enters an isolated, ephemeral builder" },
    { id: "b", label: "Builder produces the artifact + its digest" },
    { id: "c", label: "Platform emits signed SLSA provenance and an SBOM attestation" },
    { id: "d", label: "Admission gate verifies signature, provenance, and policy" },
    { id: "e", label: "Artifact admitted and the workload runs" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>A passing build doesn't prove the artifact is the honest, unmodified output of source you control. SLSA provenance proves how it was built (L3's isolation is the boundary that stops a SolarWinds-style tamper), signing proves it's that exact artifact, and an SBOM lets you answer "are we affected?" in minutes. Verify all three at the admission gate — a signature alone proves origin, not safety.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-slsa-sbom-retrieval"
  lang="en"
  questions={[
    { q: "Explain the three questions SLSA, signing, and SBOM each answer, and why you need all three.", a: "They decompose the supply-chain trust problem. SLSA provenance answers 'how was it built?' — a signed statement that this output digest came from that source revision, by that builder, with those parameters, ideally generated by an isolated platform so build steps can't forge it. Artifact signing answers 'is this the exact thing that was built?' — a cryptographic signature over the artifact's digest, verified at admission, proving integrity and origin (identity X published exactly this, unmodified). An SBOM answers 'what is inside it?' — a standard machine-readable list (SPDX or CycloneDX) of every component and version, so when a new CVE like Log4Shell drops you can query which deployed artifacts are affected in minutes instead of grepping for days. You need all three because they cover different gaps: provenance with no signature is a forgeable claim; a signature with no provenance proves who released it but not that the build was honest; and both without an SBOM leave you blind to a radioactive transitive dependency. None of the three, alone, closes the gap between 'this artifact passed' and 'this artifact is the honest output of source I trust, and I know what's in it.'" },
    { q: "Why is the SLSA L2→L3 jump the security boundary, and what does keyless signing add on top?", a: "At L2 you already have signed provenance, which proves the artifact wasn't swapped after the build — but the provenance can be generated by user-controlled build steps, so a compromised step (the SolarWinds shape: inject a malicious object after tests, then attest a clean build) can lie about itself. L3 requires the build to run in an isolated, ephemeral environment where the provenance is generated by the platform, not the build steps — that isolation is the actual boundary, because the malicious step can no longer forge a clean attestation about its own behavior. A reused self-hosted runner collapses you below L3 regardless of what you emit, because shared state lets a prior job poison the next. Keyless signing (Sigstore: Fulcio + cosign + Rekor) complements this by removing the long-lived private key as a theft target: the builder authenticates with a short-lived OIDC workload identity, gets an ephemeral certificate, signs, discards the key, and the event is recorded in Rekor, a public append-only transparency log. So there's no key to steal, and a backdated or forged signature is detectable because it has no matching Rekor entry — you verify by identity and provenance rather than by trusting a key stayed safe for years." },
  ]}
/>

<Recap lang="en">
Supply-chain attacks like SolarWinds, Codecov, and the XZ backdoor didn't break the perimeter — they tampered with how the artifact was produced, in the gap between "this build passed" and "this is the honest, unmodified output of source I trust." Three controls close that gap, each answering a different question. SLSA provenance answers how it was built: a signed attestation binding the output digest to a source revision and builder, where the L2→L3 jump — running the build in an isolated, ephemeral environment with platform-generated provenance — is the boundary that stops a malicious build step from forging a clean record about itself (and a reused self-hosted runner silently drops you below it). Signing answers whether it's that exact artifact, with keyless Sigstore signing removing the long-lived key as a theft target and Rekor's transparency log making backdated signatures detectable — but a signature proves origin and integrity, never that the code is benign. An SBOM (SPDX or CycloneDX, generated from the built artifact and signed as an attestation) answers what's inside, collapsing "are we affected by this new CVE?" from a days-long grep into a minutes-long query. Verify all three at the admission gate, and the next time a build comes back green your first question is no longer "did it pass?" but "can I prove this is the artifact my pipeline honestly built from source I reviewed — and do I know what's in it?"
</Recap>
