---
concepts:
- dependency-scanning
- sca
- cve
- image-scanning
- false-positive
deepensInto: []
estMin: 15
lang: en
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-image-and-dependency-scanning
sources:
- https://slsa.dev/
- https://owasp.org/www-project-devsecops-guideline/
spiral: []
status: ready
summary: 'Scanners flag every CVE in your dependencies and base images, but most don''t reach exploitable code. Reachability, severity, and fix-availability are how seniors turn a wall of red into a short, ranked list of things that actually matter.'
title: Image and dependency scanning
track: security-cloud
unit: 03-iac-and-supply-chain-security
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
You wire Trivy into CI on a Friday. Monday's first push fails the build with **312 vulnerabilities, 41 critical**. The image is a stock `node:20` plus a dozen npm packages you didn't write. Nobody touched the cryptography this weekend; you just turned on a flashlight that was always pointing at a pile of debt. The temptation is to set `--severity HIGH,CRITICAL --exit-code 0` and move on — to keep the light on but unplug the alarm. That's the wrong reflex, and it's also the most common one. The real job isn't *finding* CVEs; the scanner does that for free. The job is deciding which of the 312 can actually be reached and exploited in your running service — because 280 of them never will be.
</Hook>

By the end of this lesson you'll know how SCA and image scanners produce that wall of red, and how reachability, fix-availability, and severity turn it into a short ranked list you can act on without going numb to the alerts.

<Crux>Scanners find every CVE for free; the senior skill is ranking by reachability and fix-availability so real risk isn't drowned in noise.</Crux>

<Explanation>

## Two scanners, one inventory problem

A container image is a stack of two software supply chains, and you have to scan both. The **OS layer** is whatever your base image ships — `glibc`, `openssl`, `zlib`, hundreds of Debian or Alpine packages, most of which you never invoke. The **application layer** is your direct and transitive dependencies: the `package-lock.json`, `go.sum`, or `requirements.txt` that pulls in code you actually call. Both are scanned the same way and it's worth being precise about how, because the mechanism explains the noise.

A scanner does **not** analyze your code for bugs. It builds a *bill of materials* — every package and exact version present — then joins that list against a vulnerability database (the NVD plus distro-specific feeds like Debian Security Tracker, GitHub Advisories, and the language ecosystems). The output is set arithmetic: `installed_packages ∩ known_vulnerable_versions`. That join is why a fresh `node:20` image reports dozens of CVEs on day one — you installed an OS with known-vulnerable packages, and the scanner is reporting a fact, not a judgment. This is what OWASP's DevSecOps guideline calls **Software Composition Analysis (SCA)**, and it's the cheapest high-value control you can add to a pipeline. It's also why turning it on feels like being buried.

## Why the wall of red is mostly noise

The set-arithmetic that makes scanning cheap is exactly what makes it noisy. A CVE in `installed_packages ∩ known_vulnerable_versions` tells you the *vulnerable code is present*. It does **not** tell you that the code is loaded, called, or reachable with attacker-controlled input. That gap is the source of nearly all the noise, and it has three distinct shapes a senior learns to recognize.

<FlowDiagram
  label="Funnel from raw scanner output to real risk: a scanner reports every CVE where a vulnerable version is installed, but most never reach exploitable code. Filtering by reachability (is the vulnerable function actually called?), fix-availability (is there a patched version?), and exploit context (severity in your deployment) collapses 312 findings to roughly 8 that warrant action this sprint."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "all", label: "312 CVEs reported", sub: "package ∩ CVE DB" },
    { id: "reach", label: "Reachable?", sub: "code actually called" },
    { id: "fix", label: "Fix available?", sub: "patched version exists" },
    { id: "ctx", label: "Exploitable here?", sub: "your deployment" },
    { id: "act", label: "~8 to act on", sub: "ranked, this sprint" },
  ]}
  edges={[
    { from: "all", to: "reach", label: "most unused" },
    { from: "reach", to: "fix" },
    { from: "fix", to: "ctx", label: "or won't-fix" },
    { from: "ctx", to: "act", label: "real risk" },
  ]}
  caption="The scanner gives you the leftmost box for free. Every arrow to the right is a human judgment the scanner cannot make: reachability, fix-availability, and deployment context are what turn 312 findings into the ~8 that actually warrant work."
/>

The three shapes of noise:

- **Unreachable code.** A CVE in `glibc`'s NIS resolver, in a font-parsing path of an image library you only use for thumbnails, or in a CLI subcommand your service never invokes. The vulnerable bytes are on disk; the vulnerable *path* is dead. Reachability analysis — does any call chain from your entrypoint reach the vulnerable function? — is the single biggest noise reducer, and the newest scanners (and tools like the **VEX** standard, Vulnerability Exploitability eXchange) exist specifically to record "present but not affected."
- **No fix yet (or a `won't-fix`).** Distros routinely mark low-severity CVEs `won't-fix` because the fix isn't worth the regression risk. Failing your build on a CVE with no upstream patch just means the build can never go green — you've built a ratchet that only tightens.
- **Severity ≠ exploitability in context.** A CVSS 9.8 in a dev-only dependency that's tree-shaken out of the production bundle is not a 9.8 *for you*. CVSS scores the vulnerability in the abstract; only you know whether it's exposed.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Finding</th>
        <th class="border border-border px-3 py-2 text-left">CVSS</th>
        <th class="border border-border px-3 py-2 text-left">Reachable?</th>
        <th class="border border-border px-3 py-2 text-left">Fix?</th>
        <th class="border border-border px-3 py-2 text-left">Senior action</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">RCE in JSON lib you call on every request</td>
        <td class="border border-border px-3 py-2">9.8</td>
        <td class="border border-border px-3 py-2">Yes</td>
        <td class="border border-border px-3 py-2">Patch released</td>
        <td class="border border-border px-3 py-2">Bump now; this is the alert</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">RCE in a dev-only CLI, tree-shaken out of prod</td>
        <td class="border border-border px-3 py-2">9.8</td>
        <td class="border border-border px-3 py-2">No</td>
        <td class="border border-border px-3 py-2">Patch released</td>
        <td class="border border-border px-3 py-2">Bump opportunistically; don't gate</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">DoS in glibc NIS path you never invoke</td>
        <td class="border border-border px-3 py-2">7.5</td>
        <td class="border border-border px-3 py-2">No</td>
        <td class="border border-border px-3 py-2">won't-fix</td>
        <td class="border border-border px-3 py-2">Suppress with VEX + rationale</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Auth bypass in your web framework</td>
        <td class="border border-border px-3 py-2">8.1</td>
        <td class="border border-border px-3 py-2">Yes</td>
        <td class="border border-border px-3 py-2">No patch yet</td>
        <td class="border border-border px-3 py-2">Compensating control + track</td>
      </tr>
    </tbody>
  </table>
</div>

## Where the gate goes, and what it gates on

Knowing the noise shapes lets you place the gate correctly. The wrong design — the one everyone reaches for first — is "fail the build if any CRITICAL exists." It produces three failure modes in week one: builds that can't go green (CVEs with no fix), alert fatigue (so the one real RCE scrolls past with the 40 unreachable ones), and the inevitable `--exit-code 0` that silently disables the whole control. A scanner that's been quietly neutered is *worse* than no scanner, because it lets you believe you're covered.

The senior gate composes three predicates: **severity AND fix-available AND not-suppressed**. Fail the build only on a fixable, high-or-critical, unsuppressed finding — because that's the set where the build failing actually changes someone's behavior (bump the version). Everything else gets a different workflow: track it, suppress it with a recorded reason, or accept it. The goal is a gate where a red build always means "do this specific thing," never "go acknowledge 40 things you can't act on."

<Inset kind="why" lang="en">
Why scan the registry continuously, not just at build time? Because the join between your bill-of-materials and the CVE database moves *under you*. An image that scanned clean on Monday can have three new criticals on Thursday — not because the image changed, but because the database did when a new CVE was published against a package you already shipped. The famous example is Log4Shell: millions of artifacts went from "clean" to "critical RCE" overnight with zero code change. Build-time scanning answers "is this clean now?"; registry/runtime scanning answers "is what we're *running* still clean?" — and only the second one catches the CVE disclosed after you deployed.
</Inset>

## Cutting the input, not just filtering the output

The cheapest way to win the triage game is to have less to triage. Two structural moves shrink the input before any filtering:

- **Minimal base images.** A `node:20` image is ~1.1 GB and carries the entire Debian userland — a shell, `apt`, dozens of libraries your app never touches, each a potential CVE. A `distroless` or `alpine` image carries only the runtime. Swapping `node:20` for `gcr.io/distroless/nodejs20` routinely drops OS-layer findings from ~150 to single digits, because you deleted the packages, not just suppressed their CVEs. Fewer packages is the only fix that removes the *vulnerable code itself* rather than arguing about its reachability.
- **Pin and update deliberately.** Lockfiles make scans reproducible (you scan exactly what ships), and a steady dependency-update cadence (Dependabot/Renovate) means you're applying small patches continuously instead of facing a 200-CVE cliff at the next forced major upgrade.

<TradeoffMatrix
  id="02-image-dep-scanning-tradeoff-1"
  lessonSlug="02-image-and-dependency-scanning"
  lang="en"
  prompt="Your CI scan reports 312 CVEs (41 critical) on a freshly-enabled pipeline. Pick the response a senior ships first."
  options={[
    { name: "Gate the build only on fixable + high/critical + unsuppressed findings; route the rest to a tracked backlog", summary: "Composed gate.", correct: true, justification: "This makes a red build always mean a specific, doable action: a patch exists, the severity is real, and nobody has reviewed-and-suppressed it. Unfixable and unreachable findings get a different workflow (track/suppress with rationale) instead of blocking the pipeline. It's the only option that keeps the gate meaningful AND keeps the team able to ship." },
    { name: "Set --severity HIGH,CRITICAL --exit-code 0 so the scan reports but never fails the build", summary: "Neuter the gate.", justification: "This is the most common reflex and it's a trap. A scanner that can never fail the build is a dashboard nobody reads; the one real exploitable RCE scrolls by with the 40 unreachable findings. A quietly-disabled control is worse than none because it manufactures false confidence." },
    { name: "Fail the build on any CRITICAL, no exceptions, starting today", summary: "Hard gate everything.", justification: "Sounds rigorous, breaks immediately. Many criticals have no upstream fix (won't-fix) or are unreachable, so the build can never go green and the team's first move is to disable the gate entirely. A gate you can't satisfy trains people to route around it." },
    { name: "Switch the base image to distroless and call it done", summary: "Slim and stop.", justification: "Minimal base images are a real and powerful move — they delete OS-layer CVEs at the source — but they only touch the OS layer. Your application dependencies (the npm/pip/go packages you actually call) are untouched, and those are where reachable, exploitable CVEs most often live. Necessary, not sufficient." },
  ]}
/>

<Quiz
  id="02-image-dep-scanning-quiz-1"
  lessonSlug="02-image-and-dependency-scanning"
  lang="en"
  question="An image that passed its scan clean on Monday shows 3 new criticals on Thursday with no code or dependency change. What happened?"
  choices={[
    { label: "The vulnerability database changed: new CVEs were published against packages already in the image", correct: true },
    { label: "The scanner is flaky and produced a false positive — re-run it", misconception: "Re-running won't help; the finding is real. Scan results are a join against a live CVE feed, so the same artifact legitimately changes status as new CVEs are disclosed. This is exactly why you scan the registry continuously, not just at build." },
    { label: "Someone must have pushed a new image; results never change for a fixed digest", misconception: "Results absolutely change for a fixed digest. The bill-of-materials is frozen, but the CVE database it's joined against is not — Log4Shell flipped millions of unchanged artifacts from clean to critical overnight." },
    { label: "Cosmic noise in CVSS scoring; ignore sub-weekly changes", misconception: "These are newly-disclosed vulnerabilities, not scoring jitter. Ignoring them is how a post-deployment CVE (the most dangerous kind, since it's already running) goes unpatched." },
  ]}
/>

<Quiz
  id="02-image-dep-scanning-quiz-2"
  lessonSlug="02-image-and-dependency-scanning"
  lang="en"
  question="A CVSS 9.8 RCE is reported in a dev-only build tool that's tree-shaken out of your production bundle. Should it fail the production deploy gate?"
  choices={[
    { label: "No — it's not reachable in the deployed artifact; suppress it with a recorded reason and patch it opportunistically", correct: true },
    { label: "Yes — 9.8 is critical and all criticals must block, regardless of context", misconception: "CVSS scores the vulnerability in the abstract, not in your deployment. A critical in code that never ships to production isn't a 9.8 for you. Gating on it trains the team to disable the gate." },
    { label: "No — and you should delete the finding from the report entirely so it never reappears", misconception: "Deleting silently is wrong; the right move is a recorded suppression (e.g. VEX 'not affected') with a rationale, so the decision is auditable and survives the next scan instead of being silently re-triaged." },
    { label: "Yes — dev dependencies are always production risks because builds run in CI", misconception: "CI exposure is a real but separate concern with a different blast radius; it doesn't make the package reachable in the deployed service. Conflating the two is how every finding becomes a deploy-blocker." },
  ]}
/>

<DragOrder
  id="02-image-dep-scanning-drag-1"
  lessonSlug="02-image-and-dependency-scanning"
  lang="en"
  prompt="Order the triage funnel from raw scanner output to a shippable decision:"
  items={[
    { id: "a", label: "Scanner joins your bill-of-materials against the CVE database → raw findings" },
    { id: "b", label: "Drop findings whose vulnerable code isn't reachable from your entrypoint" },
    { id: "c", label: "Separate fixable findings from won't-fix / no-patch-yet" },
    { id: "d", label: "Rank the fixable+reachable set by severity in your deployment context" },
    { id: "e", label: "Gate the build on that ranked set; track/suppress the rest with a reason" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Finding CVEs is free; the scanner reports every vulnerable version it can join against the CVE database. The senior skill is the funnel: drop unreachable code, separate fixable from won't-fix, rank by exploitability in your deployment, and gate only on fixable+high+unsuppressed so a red build always means a specific action. Slim the base image and update continuously to shrink the input before you even filter it.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-image-dep-scanning-retrieval"
  lang="en"
  questions={[
    { q: "Explain how an SCA/image scanner produces its findings and why a fresh node:20 image reports dozens of CVEs on day one.", a: "A scanner doesn't analyze your code for bugs — it builds a bill of materials (every OS package and every direct/transitive application dependency, at exact versions) and joins that inventory against a vulnerability database: the NVD plus distro feeds (Debian Security Tracker), GitHub Advisories, and per-language ecosystems. The result is set arithmetic — installed_packages ∩ known_vulnerable_versions. A fresh node:20 image reports dozens of CVEs because it ships the full Debian userland (shell, apt, hundreds of libraries), many of which have published CVEs; the scanner is reporting the factual presence of vulnerable versions, not making a judgment that you're exploitable. That's also the root of the noise: presence of vulnerable bytes is not the same as the vulnerable code being loaded, called, or reachable with attacker input." },
    { q: "Why is 'fail the build on any CRITICAL' a worse gate than 'fail only on fixable + high/critical + unsuppressed', and how do reachability and registry scanning fit in?", a: "'Fail on any CRITICAL' breaks in week one for three reasons: many criticals have no upstream fix (won't-fix), so the build can never go green; many are unreachable (a CVE in a glibc path or dev-only tool you never call), so they don't represent real risk; and the resulting alert fatigue hides the one exploitable RCE among 40 noise findings. The team's predictable response is --exit-code 0, which silently disables the control — worse than no scanner because it manufactures false confidence. The composed gate (severity AND fix-available AND not-suppressed) makes a red build always mean a specific action: a patch exists for a real, unreviewed finding, so 'bump the version' is the unambiguous fix. Reachability analysis (does any call chain from your entrypoint reach the vulnerable function?) is the single biggest noise reducer and underlies the VEX 'present but not affected' standard. Registry/runtime scanning matters because the join moves under you: the bill-of-materials is frozen for a digest but the CVE database isn't, so an artifact that scanned clean can become critical overnight when a new CVE is disclosed — Log4Shell being the canonical case. Build-time scanning answers 'clean now?'; continuous registry scanning answers 'is what we're running still clean?'" },
    { q: "You inherit a pipeline with --severity HIGH,CRITICAL --exit-code 0 (scan reports but never fails) and ~300 standing findings. What's your sequence to make the gate meaningful again without halting delivery?", a: "First, shrink the input structurally: swap the fat base image for distroless/alpine, which typically deletes most OS-layer findings at the source (fewer packages, not suppressed CVEs), and confirm lockfiles are pinned so scans are reproducible. Then triage the remainder through the funnel — drop unreachable findings (record them as VEX 'not affected' with a rationale so they don't re-surface), separate fixable from won't-fix/no-patch, and rank the fixable+reachable set by exploitability in your actual deployment. Re-enable the gate as a composed predicate — fixable AND high/critical AND unsuppressed → exit non-zero — so a red build always maps to 'bump this version,' and route everything else to a tracked backlog with owners. Finally, add continuous registry scanning so post-deployment CVEs (the Log4Shell case) get caught, and a steady update cadence (Dependabot/Renovate) so you apply small patches continuously instead of facing the next 200-CVE cliff. The throughline: never gate on findings the team can't act on, because that's what trained them to disable the gate in the first place." },
  ]}
/>

<Recap lang="en">
A container is two supply chains — the OS base image and your application dependencies — and an SCA/image scanner inventories both, then joins that bill of materials against a CVE database. Finding vulnerabilities is therefore free and noisy: a CVE means a vulnerable version is *present*, not that its code is reachable, exploitable, or even fixable. The senior move is the triage funnel: drop unreachable findings, separate fixable from won't-fix, rank the rest by exploitability in your deployment, and gate the build only on fixable + high/critical + unsuppressed so a red build always means a concrete action — never "go acknowledge 40 things you can't fix." Shrink the input first with a minimal base image and continuous updates, and scan the registry continuously because the CVE database moves under you (Log4Shell flipped millions of unchanged artifacts overnight). The reflex to kill is `--exit-code 0`: a quietly-neutered scanner is worse than none, because it lets you believe you're covered.
</Recap>
