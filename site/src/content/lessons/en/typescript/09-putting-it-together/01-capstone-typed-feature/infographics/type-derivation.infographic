infographic hierarchy-tree-curved-line-rounded-rect-node
data
  title One schema, every type derived
  desc Declare the domain once; every other type is a function of it
  root
    label Comment — one zod schema (source of truth)
    children
      - label type Comment — z.infer of schema
      - label NewComment — Comment.pick subset
      - label CreateResult — union carrying Comment
      - label Paginated — makePage infers T
      - label AppRouter — typeof appRouter
        children
          - label client Comment[] — inferred end to end
theme
  palette #6366f1 #14b8a6 #f59e0b
