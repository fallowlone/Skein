---
concepts:
- default-type-parameters
- inference
- noinfer
- precedence
- ergonomics
deepensInto:
- typescript/08-real-world/04-library-generics
estMin: 15
lang: en
lessonType: topic
level: middle
order: 3
prereqs:
- typescript/03-generics/02-constraints
slug: 03-defaults-and-inference
sources:
- https://www.typescriptlang.org/docs/handbook/2/generics.html
- https://www.typescriptlang.org/docs/handbook/release-notes/typescript-5-4.html#the-noinfer-utility-type
spiral:
- inference
- ergonomics
status: ready
summary: Default type parameters fill the tail when inference finds nothing, but
  inference always wins where it can. `NoInfer<T>` (TS 5.4) blocks inference at a
  position so one argument can decide T for the rest.
title: Defaults and inference
track: typescript
unit: 03-generics
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A `createStore<State, Action = AnyAction>` signature ships happily. Then someone calls `createStore(reducer)` and gets a confusing error about `Action`, while another calls `createStore<MyState>(reducer)` and the action type is silently `AnyAction` instead of being inferred from the reducer. The default did its job — and that is exactly the problem: it papered over an inference that should have happened.
</Hook>

<Crux>A default applies only when inference finds nothing — inference always wins, so a careless default can mask a real inference failure.</Crux>

<Explanation>

## What a default does

Why add a default at all? Because sometimes there is genuinely nothing in the call to infer from — and forcing the user to write a type argument every time makes an API painful. A default type parameter supplies a fallback type, used when the checker can neither infer nor be told the type argument:

```ts
function emptyArray<T = string>(): T[] {
  return [];
}

const a = emptyArray();
//    ^? const a: string[]    — default kicked in (nothing to infer)
const b = emptyArray<number>();
//    ^? const b: number[]    — explicit argument overrides the default
```

Defaults look like value-parameter defaults but live in type space. They are most valuable on types/classes where there is genuinely nothing to infer from (more on classes next lesson).

## Inference beats the default

The first rule: **inference always wins.** A default only fills in when inference has no information for that parameter.

```ts
function box<T = string>(value: T): { value: T } {
  return { value };
}

const x = box(42);
//    ^? const x: { value: number }   — inferred from the argument, NOT string
const y = box();
// Error: Expected 1 arguments, but got 0.  (value is required)
```

`T` is inferred as `number` from the argument; the `= string` default is irrelevant because inference succeeded. A default never *overrides* a usable inference — it only backstops the absence of one.

<FlowDiagram
  label="Default vs inference decision"
  hue="var(--accent)"
  caption="For each type parameter: if an explicit argument is given, use it; else if inference finds a type, use that; only if both fail does the default apply."
  perRow={3}
  nodes={[
    { id: "explicit", label: "explicit arg?", sub: "box<number>()" },
    { id: "infer", label: "can infer?", sub: "from call args" },
    { id: "use", label: "resolve T", sub: "explicit > infer > default" },
  ]}
  edges={[
    { from: "explicit", to: "use", label: "yes: use it" },
    { from: "explicit", to: "infer", label: "no" },
    { from: "infer", to: "use", label: "yes / else default" },
  ]}
/>

## Partial explicit arguments: all-or-nothing, then defaults fill the tail

Explicit type arguments are **positional and all-or-nothing for the leading parameters**: you cannot supply the second while inferring the first. But defaults let you stop early — once a parameter has a default, you may omit it and everything after it.

```ts
function make<A, B = A>(a: A, b: B): [A, B] {
  return [a, b];
}

const m = make("x", 9);
//    ^? const m: [string, number]   — both inferred
const n = make<string>("x", 9);
// Error: Expected 2 type arguments, but got 1?  No — this is allowed:
//        B has a default (= A), so a single explicit argument is legal.
//    ^? const n: [string, number]   — A explicit, B still inferred to number
```

The rule: you may omit trailing type arguments that have defaults, but you cannot skip a leading one to specify a later one. Defaults fill the *tail*; inference can still refine a defaulted parameter if a value supplies it.

## Ergonomic generics: constraint + default together

The most usable library generics pair a constraint (the requirement) with a default (the common case):

```ts
type EventMap = Record<string, unknown>;

class Emitter<Events extends EventMap = Record<string, never>> {
  emit<K extends keyof Events>(key: K, payload: Events[K]): void {
    // ...
  }
}

// Common case: no events declared — default makes emit(...) reject any key.
const bare = new Emitter();
// Rich case: declare the map and get fully typed emit.
const typed = new Emitter<{ login: { userId: string } }>();
typed.emit("login", { userId: "u1" }); // ok
// @ts-expect-error  "logout" is not a key of the declared events
typed.emit("logout", { userId: "u1" });
```

`extends EventMap` requires whatever you pass to be an event map; `= Record<string, never>` makes the zero-config case behave sensibly.

## NoInfer: blocking inference at a position (TS 5.4)

Sometimes you want *one* argument to decide `T` and the *others* to merely be checked against it — not to participate in inferring `T`. Before TS 5.4 this leaked:

```ts
// One default, value must be one of the allowed strings.
function createState<T>(initial: T, allowed: T[]): T {
  return initial;
}

// BUG (pre-NoInfer): T is inferred from BOTH arguments, so T widens.
const s = createState("seattle", ["seattle", "denver"]);
//    ^? const s: string   — fine here, but the typo guard is gone:
createState("seatle", ["seattle", "denver"]); // no error! T = string
```

Because `allowed: T[]` also feeds inference, a typo in `initial` just widens `T` to `string` and the "must be in the list" guarantee evaporates. `NoInfer<T>` tells the checker to *not* infer `T` from that position:

```ts
function createState<T>(initial: T, allowed: NoInfer<T>[]): T {
  return initial;
}

const ok = createState("seattle", ["seattle", "denver"]);
//    ^? const ok: "seattle"   — T fixed by `initial` alone
createState("seatle", ["seattle", "denver"]);
// T is pinned to the literal "seatle" by `initial`; the array must now be
// "seatle"[], so each real element is rejected:
// Error: Type '"seattle"' is not assignable to type '"seatle"'.
```

Now `T` is inferred solely from `initial` as the literal `"seatle"`, and `allowed` is *checked against* `T` rather than feeding it — so the mismatched array elements flag the typo. (Fix the typo to `"seattle"` and T becomes `"seattle"`, the array matches, and it compiles.)

## The numbers behind a masked inference

The cost of a default that papers over inference is not abstract — it is counted in call sites that have to compensate. Picture a `createStore<State, Action = AnyAction>` exported from a shared package and called in, say, 70 places across an app. The `Action = AnyAction` default means none of those calls *error* when the action type fails to flow from the reducer; instead each one silently resolves `Action` to `AnyAction`, and the breakage surfaces downstream as `any`-typed actions in reducers and middleware. When the team finally tightens the type (drops the default, or fixes inference), every call site that was leaning on the fallback now needs attention — commonly *dozens* of explicit `createStore<MyState, MyAction>(...)` annotations added by hand, the exact work the default was hiding. That is the migration tax of a convenient default: paid late, all at once, across the call graph rather than at the one signature.

`NoInfer<T>` arrived in TypeScript 5.4 (March 2024); before that, the same effect required hand-rolled tricks like an intersection with a phantom type or a second type parameter, each adding instantiations and obscuring the signature. On 5.4+ it is a single wrapper with no runtime cost and no extra type parameter — the cheapest available fix for "one argument should decide `T`, the rest are checked." If your `tsconfig` targets an older TypeScript, you simply don't have it, which is itself a reason to keep the toolchain current.

<Inset kind="why" lang="en">
Why is a "helpful" default dangerous? A default makes a parameter optional in inference's eyes. If you put a default on a parameter that *should* have been inferred from a value — like `Action = AnyAction` on a store whose actions are fully derivable from its reducer — then a call that fails to infer doesn't error; it silently resolves to the default. The bug surfaces far away, as `AnyAction` flowing where a precise union was expected. Reach for a default only when there is genuinely nothing to infer from (often a class with no constructor argument carrying the type), not as a cover for an inference you couldn't get to work.

The tradeoff is ergonomics now versus a silent failure mode later. A default genuinely smooths the zero-config case — `new Emitter()` with no events is pleasant to write. But that same convenience converts a would-be compile error into a quiet widening to the fallback type, and the senior question is always "if inference here failed, do I want a loud error or a silent `AnyAction`?" When you do want the loud error, omit the default and let the missing inference surface; when the fallback truly is the right answer for the no-information case, keep it but reach for `NoInfer` on any parameter that should *check against* `T` rather than *feed* it. Defaults for genuine no-information slots; never as a substitute for inference you couldn't make work.
</Inset>

<Quiz
  id="03-defaults-and-inference-quiz-1"
  lessonSlug="03-defaults-and-inference"
  lang="en"
  question="For `function box<T = string>(value: T): { value: T }`, what is the type of `box(42)`?"
  choices={[
    { label: "{ value: number } — inference from the argument wins; the default only applies when nothing can be inferred", correct: true },
    { label: "{ value: string } — the default = string overrides the argument", misconception: "A default never overrides a usable inference; it is purely a fallback when inference finds nothing." },
    { label: "{ value: string | number } — the default and the inferred type are unioned", misconception: "Defaults and inferred types are not combined; inference simply takes precedence." },
    { label: "An error, because a default and an argument conflict", misconception: "There is no conflict; inference resolves T to number and the default is ignored." },
  ]}
/>

<Quiz
  id="03-defaults-and-inference-quiz-2"
  lessonSlug="03-defaults-and-inference"
  lang="en"
  question="In `createState<T>(initial: T, allowed: NoInfer<T>[])`, why does wrapping the second parameter in `NoInfer` matter?"
  choices={[
    { label: "It stops `allowed` from contributing to inferring T, so T is fixed by `initial` alone — a value in `allowed` not matching the initial literal is then a type error", correct: true },
    { label: "It makes `allowed` optional", misconception: "NoInfer changes inference behaviour, not optionality; the parameter is still required." },
    { label: "It infers T from `allowed` instead of `initial`", misconception: "It does the opposite — it prevents allowed from being an inference source." },
    { label: "It converts the array to readonly", misconception: "NoInfer has nothing to do with readonly; it only blocks inference at that position." },
  ]}
/>

<DragOrder
  id="03-defaults-and-inference-drag-1"
  pieceSlug="03-defaults-and-inference"
  lang="en"
  prompt="Order the precedence the checker uses to resolve a single type parameter T:"
  items={[
    "If the caller wrote an explicit type argument for T, use it",
    "Otherwise, try to infer T from the call arguments that mention it",
    "Apply the constraint as an upper bound to validate the chosen T",
    "Only if no explicit argument and no inference exist, fall back to the default",
    "If none of the above produce a type, an unconstrained T resolves to unknown",
  ]}
/>

<MetaphorComplete
  id="03-defaults-and-inference-metaphor-1"
  pieceSlug="03-defaults-and-inference"
  lang="en"
  setup="Fill in the blank: between inference and a default, _______ always wins; the default is only a fallback for when there is nothing to infer."
  accepted={["inference", "infer", "inferring"]}
  canonical="inference"
  explanation="A default type parameter never overrides a usable inference. The checker resolves T in order: an explicit type argument, then inference from the call arguments, and only if both are absent does the default apply. This is why a default placed on a parameter that *should* be inferred is dangerous — it silently backstops a missed inference instead of surfacing an error."
/>

</Explanation>

<KeyTakeaway>Default type parameters are pure fallbacks: explicit arguments win, then inference, and only then the default. Defaults fill the trailing parameters; they never override a usable inference. `NoInfer<T>` (TS 5.4) removes a position from inference so one argument can decide `T` and the rest are checked against it — the cure for a default that masks a real inference failure.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-defaults-and-inference-retrieval"
  lang="en"
  questions={[
    { q: "State the precedence between explicit type arguments, inference, and defaults, and explain the all-or-nothing rule for explicit arguments.", a: "The checker resolves each type parameter in strict precedence: (1) an explicit type argument the caller wrote always wins; (2) otherwise it tries to infer the parameter from the call arguments that mention it; (3) only if there is neither an explicit argument nor any inference does the default apply; (4) with none of those, an unconstrained parameter falls to `unknown`. A default never overrides a usable inference — `box<T = string>(value: T)` called as `box(42)` yields `{ value: number }`, not string. For explicit arguments the rule is all-or-nothing for the LEADING parameters: you cannot specify a later type argument while inferring an earlier one. Defaults relax this for the TAIL — once a parameter has a default you may omit it and everything after it, e.g. `make<A, B = A>` accepts a single explicit `A` with `B` still inferred." },
    { q: "Describe the bug NoInfer fixes, using the `createState(initial, allowed)` example.", a: "Consider `createState<T>(initial: T, allowed: T[]): T` meant to guarantee `initial` is one of `allowed`. Because `allowed: T[]` also feeds inference, a typo in `initial` doesn't error — instead `T` simply widens to the union of everything, e.g. `string`, and the 'must be in the list' guard silently disappears: `createState('seatle', ['seattle', 'denver'])` compiles. Wrapping the second parameter as `allowed: NoInfer<T>[]` (TS 5.4) removes that position as an inference source, so `T` is fixed by `initial` ALONE — here the literal `'seattle'`. Now `allowed` is merely checked against `T`, and the typo `'seatle'` is rejected because it is not assignable to `'seattle' | 'denver'`. NoInfer is the precise tool when one argument should DECIDE the type parameter and the others should only be VALIDATED against it." },
  ]}
/>

<Recap lang="en">
Default type parameters are fallbacks, not overrides: the checker prefers an explicit type argument, then inference, and only reaches the default when both are absent — so a default placed on a parameter that should have been inferred quietly masks the failure. Defaults fill trailing parameters and the all-or-nothing rule governs leading explicit arguments. Pairing a constraint with a default gives ergonomic library generics, and `NoInfer<T>` (TS 5.4) lets you exclude a position from inference so one argument decides `T` while the rest are merely checked. Next we move generics into classes and interfaces, where instance-scoped type parameters and the static-side restriction introduce a new family of pitfalls. Now when you see a generic with a default and something feels off downstream, check whether inference was supposed to fire — a silent `AnyAction` is often a masked inference, not a deliberate choice.
</Recap>
