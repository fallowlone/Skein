---
concepts: [discriminated-unions, tagged-unions, literal-discriminant, exhaustiveness, assert-never, state-machines]
deepensInto: ["typescript/02-everyday-types/05-index-access-types"]
estMin: 14
lang: en
lessonType: topic
level: middle
order: 4
prereqs: ["typescript/02-everyday-types/03-type-guards"]
slug: "04-discriminated-unions"
sources:
  - https://www.typescriptlang.org/docs/handbook/2/narrowing.html#discriminated-unions
spiral: [control-flow, never, state-machines]
status: ready
summary: "A shared literal tag turns a union into a discriminated union: switch on the tag narrows each branch, and an assertNever default makes adding a variant a compile error everywhere it is unhandled."
title: "Discriminated unions"
track: "typescript"
unit: "02-everyday-types"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Your API result type is `{ loading: boolean; error?: string; data?: User }`. Every component checks `if (!loading && !error && data)` and reaches into `data` — sometimes `data` is defined while `error` is also set, sometimes neither is. The type allows all eight combinations, but only three are real states. A teammate ships a screen that reads `data.name` during an error and it crashes. The bug is not in the code; it is in the type. The fix is to make illegal states unrepresentable — and the tool for that is the discriminated union.
</Hook>

<Crux>A shared literal tag narrows a union per branch to one variant's fields — and a `never` default catches every unhandled case.</Crux>

<Explanation>

## The pattern: a common literal tag

A **discriminated (tagged) union** is a union of object types that all share one property — the *discriminant* — whose type is a distinct literal in each member. Switching or branching on that property narrows the whole object to the matching variant:

```ts
type Result =
  | { kind: "ok"; data: string }
  | { kind: "error"; message: string }
  | { kind: "loading" };

function render(r: Result): string {
  switch (r.kind) {
    case "ok":
      return r.data;
      //     ^? (parameter) r: { kind: "ok"; data: string }
    case "error":
      return r.message;
      //     ^? r: { kind: "error"; message: string }
    case "loading":
      return "…";
  }
}
```

In each `case`, `r` is narrowed to the single member whose `kind` matches the literal, so `r.data` is reachable in `"ok"` and `r.message` in `"error"` — and nowhere else. This replaces the eight-combination boolean soup from the Hook with three mutually exclusive, fully-typed states. Illegal states (data present during an error) are now unrepresentable.

<FlowDiagram
  label="Switching on a discriminant narrows each branch to one variant"
  hue="var(--d-frontend)"
  perRow={4}
  nodes={[
    { id: "u", label: "Result", sub: "ok | error | loading" },
    { id: "s", label: "switch", sub: "r.kind" },
    { id: "ok", label: "case ok", sub: "{ data }" },
    { id: "er", label: "case error", sub: "{ message }" },
    { id: "ld", label: "case loading", sub: "{}" },
    { id: "ex", label: "default", sub: "r: never" },
  ]}
  edges={[
    { from: "u", to: "s", label: "read tag" },
    { from: "s", to: "ok", label: "ok" },
    { from: "s", to: "er", label: "error" },
    { from: "s", to: "ld", label: "loading" },
    { from: "s", to: "ex", label: "else" },
  ]}
  caption="Each case narrows to one variant. The default receives never once every variant is handled — the basis of exhaustiveness checks."
/>

## Requirements for narrowing to work

The checker recognises a discriminated union only when:

1. **The discriminant is a literal type**, not a widened primitive. `kind: "ok"` works; `kind: string` does not. (Recall the `let`/`const` widening from the narrowing lesson — copying the tag into a `let` breaks this.)
2. **Every member declares the discriminant** with a distinct literal. If two members share `kind: "ok"`, the checker cannot tell them apart and merges their fields.
3. **You branch on the discriminant itself** (`r.kind`), not on a derived value.

```ts
// Broken: widened discriminant
type Bad = { kind: string; data: string } | { kind: string; err: string };
declare const b: Bad;
if (b.kind === "data") {
  b.data;
  // Error: Property 'data' does not exist on type 'Bad'.
  //   — kind is `string`, not a literal, so no narrowing happens
}
```

## Exhaustiveness with `assertNever`

The real power is catching *missing* cases at compile time. When every variant has been handled, the value in the `default` branch has type `never`. A helper that accepts only `never` then forces a compile error the moment a new variant is added but left unhandled:

```ts
function assertNever(x: never): never {
  throw new Error("Unexpected variant: " + JSON.stringify(x));
}

function render(r: Result): string {
  switch (r.kind) {
    case "ok": return r.data;
    case "error": return r.message;
    case "loading": return "…";
    default: return assertNever(r);
    //                          ^? r: never  — fine today
  }
}
```

Now add a fourth variant `{ kind: "retrying"; attempt: number }` to `Result` and *do not* touch `render`. The `default` branch no longer sees `never` — it sees `{ kind: "retrying"; attempt: number }`:

```ts
// Error: Argument of type '{ kind: "retrying"; attempt: number; }'
//   is not assignable to parameter of type 'never'.
```

Every `switch` that forgot the new case lights up red across the codebase. This is the single most valuable property of discriminated unions for senior work: the type system turns "find all the places that need updating" from a manual grep into a guaranteed compile error.

<Inset kind="why" lang="en">
Why `never` and not, say, `default: throw`? A bare `throw` runs at *runtime*; `assertNever` shifts the failure to *compile time*. The trick is that `never` is the empty type: only a value typed `never` is assignable to a `never` parameter. While the union is fully handled, the narrowed leftover is `never` and the call type-checks. The instant the union grows, the leftover is a real type, no longer assignable to `never`, and the build breaks before anyone runs the code. You get a runtime guard *and* a compile-time guarantee from the same line.
</Inset>

## Why this beats booleans and `in`

The boolean-flags model (`loading`, `error`, `data` all independent) allows `2^n` combinations, most of them illegal. The `in`-guard model from the previous lesson works but is fragile if keys overlap. A discriminant tag is unambiguous, self-documenting, and — crucially — exhaustiveness-checkable. It is the canonical shape for: API responses (`ok`/`error`), Redux/reducer actions (`type` field), UI/network state machines (`idle`/`loading`/`success`/`failure`), AST nodes, and parser tokens.

```ts
// Redux-style action — the discriminant is conventionally `type`
type Action =
  | { type: "increment"; by: number }
  | { type: "reset" };

function reduce(state: number, a: Action): number {
  switch (a.type) {
    case "increment": return state + a.by;
    case "reset": return 0;
    default: return assertNever(a);
  }
}
```

<DragOrder
  id="04-du-dragorder"
  pieceSlug="04-discriminated-unions"
  lang="en"
  prompt="Order the steps to add a new variant safely to an exhaustively-checked discriminated union."
  items={[
    "Add the new variant (with its literal tag) to the union type",
    "Recompile — every switch with an assertNever default now errors",
    "The default branch no longer narrows to never; it sees the new variant",
    "Add the missing case to each flagged switch",
    "Recompile clean — the leftover is never again",
  ]}
/>

<FadedExample
  client:load
  id="04-du-faded"
  pieceSlug="04-discriminated-unions"
  lang="en"
  title="Model a state machine with a discriminant"
  steps={{
    solved: (
      <div class="space-y-3 text-sm">
        <p><strong>Replace illegal-state booleans with a tagged union:</strong></p>
        <pre class="text-xs bg-zinc-100 p-2 rounded overflow-x-auto"><code>{`// Before: 8 combinations, only 3 valid
type Bad = { loading: boolean; error?: string; data?: User };

// After: 3 states, illegal ones unrepresentable
type Fetch =
  | { status: "idle" }
  | { status: "loading" }
  | { status: "success"; data: User }
  | { status: "failure"; error: string };

function view(s: Fetch): string {
  switch (s.status) {
    case "idle":    return "Start";
    case "loading": return "Spinner";
    case "success": return s.data.name;   // data only here
    case "failure": return s.error;       // error only here
    default: return assertNever(s);
  }
}`}</code></pre>
        <p><strong>Why it is better:</strong> <code>data</code> is reachable only in <code>"success"</code> and <code>error</code> only in <code>"failure"</code>. You cannot accidentally read <code>data</code> during a failure — the type forbids it. The <code>assertNever</code> default guarantees that adding a <code>"retrying"</code> state later forces you to handle it here.</p>
      </div>
    ),
    semi: {
      prompt: "In the switch above, inside `case \"success\"`, how many properties does the checker guarantee on `s` besides `status`?",
      blanks: [
        { id: "answer", expected: "1", placeholder: "a number" },
      ],
    },
    blank: {
      prompt: "You change the discriminant declaration from `status: \"loading\"` to `status: string` on one member. What breaks, and why?",
      reveal: (
        <div class="space-y-2 text-sm">
          <p>Narrowing collapses. Once any member's discriminant is the widened type <code>string</code> instead of a literal, TypeScript can no longer treat the union as discriminated — comparing <code>s.status === "success"</code> cannot prove which member you have, because <code>string</code> overlaps every literal. The <code>case</code> branches stop narrowing (<code>s.data</code> becomes an error), and the <code>assertNever</code> default stops type-checking because the leftover is no longer <code>never</code>. The discriminant must be a literal (or literal union) on <em>every</em> member; this is exactly why you keep tags as <code>const</code> string literals and never copy them through a <code>let</code>.</p>
        </div>
      ),
    },
  }}
  misconceptions={{
    "answer:0": "In the success branch s is narrowed to { status: \"success\"; data: User }, so beyond status it guarantees exactly one property: data.",
  }}
/>

<Quiz
  id="04-du-quiz-1"
  lessonSlug="04-discriminated-unions"
  lang="en"
  question="What is the type of the value in the `default` branch of an exhaustive `switch` over a fully-handled discriminated union?"
  choices={[
    { label: "`never` — every member has been narrowed away, leaving the empty type, which is why assertNever type-checks", correct: true },
    { label: "The full union, since default catches everything", misconception: "default is only reached after all known cases; the checker has already subtracted them, so the leftover is never, not the full union." },
    { label: "`unknown`", misconception: "An exhausted union narrows to never (the empty type), not unknown (the top type)." },
    { label: "`any`", misconception: "It is never. assertNever relies on this: only a never value is assignable to a never parameter." },
  ]}
/>

<Quiz
  id="04-du-quiz-2"
  lessonSlug="04-discriminated-unions"
  lang="en"
  question="You add a new variant to a discriminated union but forget to handle it in a switch that has an `assertNever` default. What happens?"
  choices={[
    { label: "A compile error at the assertNever call, because the default branch now narrows to the new variant instead of never", correct: true },
    { label: "Nothing — the default catches it silently at runtime", misconception: "The point of assertNever is to fail at COMPILE time. The leftover is no longer never, so the call does not type-check." },
    { label: "The new variant is automatically handled by the closest matching case", misconception: "There is no automatic matching; an unhandled literal falls to default, and assertNever rejects it at compile time." },
    { label: "A runtime warning but the build still passes", misconception: "It is a hard compile error, not a warning — the build fails until you add the missing case." },
  ]}
/>

</Explanation>

<KeyTakeaway>
A discriminated union is a union of object types sharing a literal-typed tag (`kind`/`type`/`status`). Branching on that tag narrows each case to exactly one variant, so each variant's fields are reachable only where they exist — illegal states become unrepresentable, replacing boolean-flag soup. Add an `assertNever(x: never): never` default and the leftover type is `never` while the union is fully handled; the moment you add a variant and forget a case, the leftover becomes a real type that is no longer assignable to `never`, turning every unhandled site into a compile error. The discriminant must be a literal on every member — widen it to `string` (or copy it through a `let`) and all narrowing collapses.
</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-du-retrieval"
  lang="en"
  questions={[
    { q: "What makes a union a discriminated union, and what are the requirements for the checker to narrow it?", a: "A discriminated (tagged) union is a union of object types that all share one property — the discriminant — whose type is a distinct literal in each member (e.g. kind: 'ok' | 'error' | 'loading'). Branching on that property narrows the object to the matching variant. Requirements: (1) the discriminant must be a literal type on every member, not a widened primitive like string; (2) every member must declare it with a distinct literal so the checker can tell members apart; (3) you must branch on the discriminant itself (s.kind), not a derived value. Violate any of these — especially widening the tag to string by copying it through a let — and narrowing silently stops." },
    { q: "Explain the assertNever exhaustiveness pattern and why it shifts failure to compile time.", a: "Define assertNever(x: never): never that throws. Put `default: return assertNever(x);` at the end of a switch on the discriminant. While every variant is handled, the checker has subtracted them all, so the value reaching default is narrowed to never — the empty type — and only a never value is assignable to a never parameter, so the call type-checks. When you add a new variant and forget its case, the default branch now narrows to that new variant (a real type), which is NOT assignable to never, producing a compile error at the assertNever call. Every switch that forgot the case lights up across the codebase. The same line also throws at runtime if an unexpected value sneaks in, so you get a compile-time guarantee and a runtime guard together." },
    { q: "Why is a discriminated union better than independent boolean flags or an in-guard for modeling state?", a: "Independent booleans like { loading; error?; data? } allow 2^n combinations, most of them illegal (data present during an error), and nothing stops a component from reading data in the wrong state. An in-guard works but is fragile when keys overlap between members. A discriminated union with a literal tag makes states mutually exclusive and fully typed: each variant's fields are reachable only in its branch, illegal states are unrepresentable, the shape is self-documenting, and — uniquely — it is exhaustiveness-checkable via assertNever. That is why it is the canonical shape for API results, reducer actions, UI/network state machines, AST nodes, and tokens." },
  ]}
/>

<Recap lang="en">
Discriminated unions are the everyday senior pattern this whole unit builds toward. By giving every member of a union a shared literal tag, you let the checker narrow the entire object by reading that one field: in each `case`, the variant's exclusive fields become reachable and illegal combinations become unrepresentable — the antidote to boolean-flag soup. Pair the `switch` with an `assertNever(x: never)` default and exhaustiveness becomes a compile-time guarantee: the leftover type is `never` while every variant is handled, and adding a variant without updating a `switch` turns into a hard build error at every unhandled site. The one rule that keeps it working: the discriminant must be a literal on every member, never a widened `string`. Next, **index access types** show how to derive types *from* these unions and from data — `Result["kind"]`, `Action["type"]` — so your tags and value types stay a single source of truth instead of being duplicated by hand. Now when you see `{ loading: boolean; error?: string; data?: User }` in a codebase, you will know exactly what to replace it with — and why the compiler will catch every place that misses the migration.
</Recap>
