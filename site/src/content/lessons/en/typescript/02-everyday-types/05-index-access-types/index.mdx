---
concepts: [keyof, indexed-access, value-type-union, number-index, single-source-of-truth, index-signature]
deepensInto: ["typescript/04-type-system-deep/03-mapped-types"]
estMin: 14
lang: en
lessonType: topic
level: middle
order: 5
prereqs: ["typescript/02-everyday-types/04-discriminated-unions"]
slug: "05-index-access-types"
sources:
  - https://www.typescriptlang.org/docs/handbook/2/keyof-types.html
  - https://www.typescriptlang.org/docs/handbook/2/indexed-access-types.html
spiral: [derivation, single-source-of-truth, keyof]
status: ready
summary: "keyof gives the union of a type's keys; T[K] indexes into a type. Index with a union of keys to get a union of value types, and Arr[number] to get an array's element type."
title: "keyof and indexed access types"
track: "typescript"
unit: "02-everyday-types"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import deriveVsDuplicate from "./infographics/derive-vs-duplicate.svg?raw";

<Hook>
You define a `User` interface, then hand-write a second type `UserKey = "id" | "name" | "email"` for a function that picks a field. A teammate adds `role` to `User` and ships. Three months later a runtime bug: a config form lets users edit `role`, but a validation helper keyed off the stale `UserKey` silently skips it. The two definitions drifted because they were written twice. Index access types delete the duplicate: `keyof User` *is* the key union, derived automatically, and `User[K]` *is* the value type. Define the data once; let the types follow.
</Hook>

<Crux>`keyof T` is the union of T's keys; `T[K]` is the type stored at key K — so types can be derived from data instead of duplicated by hand.</Crux>

<Explanation>

## `keyof`: the union of a type's keys

`keyof T` produces a union of the literal types of T's property keys:

```ts
type User = { id: string; name: string; age: number };

type UserKey = keyof User;
//   ^? type UserKey = "id" | "name" | "age"
```

This union is *derived* — add a property to `User` and `UserKey` updates automatically. It is the building block for any operation that must iterate or constrain over a type's own keys.

## `T[K]`: indexed access

`T[K]` looks up the type stored at key `K` in `T`. The key must be a valid key of `T` (or you get an error):

```ts
type Name = User["name"];
//   ^? type Name = string
type Age = User["age"];
//   ^? type Age = number

type Bad = User["nope"];
// Error: Property 'nope' does not exist on type 'User'.
```

Indexing is by *type*, not by value: `User["name"]` is using the *string literal type* `"name"` as the index, not a runtime string. This is why it works on types that have no runtime existence.

<FlowDiagram
  label="From an object type to a derived value type via keyof and indexed access"
  hue="var(--d-frontend)"
  perRow={4}
  nodes={[
    { id: "t", label: "User", sub: "{ id; name; age }" },
    { id: "k", label: "keyof User", sub: "id | name | age" },
    { id: "i", label: "User[K]", sub: "index by key" },
    { id: "v", label: "value type", sub: "string | number" },
  ]}
  edges={[
    { from: "t", to: "k", label: "keyof" },
    { from: "k", to: "i", label: "pick K" },
    { from: "i", to: "v", label: "resolves to" },
  ]}
  caption="keyof yields the key union; indexing by a key (or the whole key union) yields the corresponding value type(s)."
/>

## Indexing with a union of keys → union of value types

When you need to say "the value at any of these keys", you do not write a separate union by hand — you index by the key union and let the checker derive it. This is where `keyof` and `T[K]` compose into a real single-source-of-truth tool.

Index with a *union* of keys and you get the *union* of the corresponding value types:

```ts
type IdOrAge = User["id" | "age"];
//   ^? type IdOrAge = string | number

type AllValues = User[keyof User];
//   ^? type AllValues = string | number   ("id"→string, "name"→string, "age"→number)
```

`T[keyof T]` — "index by every key" — is the idiom for "the union of all value types in `T`". Duplicate value types collapse (`string | string | number` = `string | number`). This is a frequent senior tool: deriving an allowed-values union from a config object, or the payload-type union from a discriminated union's members.

```ts
type Action =
  | { type: "add"; n: number }
  | { type: "clear" };

type ActionType = Action["type"];
//   ^? type ActionType = "add" | "clear"   — the tag union, derived from the variants
```

Notice this works straight on a *union* type: `Action["type"]` reads `type` from each member and unions the results. That keeps the discriminant tags from the previous lesson as a single source of truth.

<Infographic
  svg={deriveVsDuplicate}
  label="A two-column comparison: hand-written duplicate types cause writing the same shape twice, silent drift when a field is added, edits in every place, and runtime bugs from a stale key union; types derived with keyof and T[K] give a single source of truth, auto-update when a field is added, need zero edits, and let the checker reject a wrong key at compile time."
  caption="Same shape, two strategies: a hand-written key/value union drifts and breaks at runtime, while keyof and T[K] derive it from the data and update for free."
  hue="var(--d-infra)"
/>

## `number`-indexing arrays and tuples → element type

Arrays and tuples are indexed by `number`. So `T[number]` is the *element type*:

```ts
type Names = string[];
type Element = Names[number];
//   ^? type Element = string

const roles = ["admin", "editor", "viewer"] as const;
type Role = typeof roles[number];
//   ^? type Role = "admin" | "editor" | "viewer"
```

The `as const` + `[number]` combo is the canonical way to derive a literal union *from a runtime array*, so the array of values and its type stay in lockstep. For tuples, `[number]` gives the union of all positions, while a literal index gives one slot:

```ts
type Pair = [string, number];
type First = Pair[0];
//   ^? type First = string
type Either = Pair[number];
//   ^? type Either = string | number
```

<MetaphorComplete
  id="05-index-metaphor"
  pieceSlug="05-index-access-types"
  lang="en"
  setup="keyof is like asking a form which fields it has; T[K] is like asking what kind of value field K holds. Deriving types this way keeps one ___ of truth instead of two definitions that drift."
  accepted={["source", "single source", "origin"]}
  canonical="source"
  explanation="keyof + indexed access derive keys and value types from the original definition, so there is a single source of truth — change the data shape once and every derived type updates, eliminating the drift of hand-maintained duplicates."
/>

## Pitfall: `keyof` on index-signature types

When a type has an index signature, `keyof` returns the *signature key type*, not a finite literal union:

```ts
type Dict = { [key: string]: number };
type K1 = keyof Dict;
//   ^? type K1 = string | number
//      (string index signatures also admit number keys, because JS coerces numeric keys to strings)

type NumDict = { [key: number]: boolean };
type K2 = keyof NumDict;
//   ^? type K2 = number
```

This surprises people: `keyof` a string-indexed type is `string | number`, not `string`, because a numeric property access like `dict[0]` is legal (JS turns it into `dict["0"]`). If you later try to constrain a generic `K extends keyof Dict` expecting precise keys, you instead get the broad `string | number` and lose the precision you wanted. Prefer a concrete object type (or `Record<SpecificUnion, V>`) when you need a finite key union.

<Inset kind="edgecase" lang="en">
Indexing with a literal versus a union changes precision dramatically. `User["id"]` is a single value type (`string`). `User[keyof User]` is the *whole* value-type union. And `User[string]` is an error unless `User` has a string index signature — indexing requires the index type to be assignable to a key of the target. This is why `obj[someStringVariable]` often errors on a precisely-typed object: a runtime `string` is wider than the object's finite key union, so the access is unsound. Narrow the key to `keyof T` first.
</Inset>

## Why this bridges to mapped types

Indexed access answers "what type is at *this* key". The natural next question — "transform *every* key" — is **mapped types** (unit 04): `{ [K in keyof T]: ... }` iterates the `keyof T` union and uses `T[K]` to read each value type. Everything you do with mapped types is built on the two operators in this lesson, so internalising `keyof` and `T[K]` now pays off directly there.

<FadedExample
  client:load
  id="05-index-faded"
  pieceSlug="05-index-access-types"
  lang="en"
  title="Derive instead of duplicate"
  steps={{
    solved: (
      <div class="space-y-3 text-sm">
        <p><strong>Replace a hand-written union with a derived one:</strong></p>
        <pre class="text-xs bg-zinc-100 p-2 rounded overflow-x-auto"><code>{`const ROUTES = {
  home: "/",
  profile: "/u/:id",
  settings: "/settings",
} as const;

// Derived — never drifts from ROUTES
type RouteName = keyof typeof ROUTES;
//   ^? "home" | "profile" | "settings"
type RoutePath = typeof ROUTES[keyof typeof ROUTES];
//   ^? "/" | "/u/:id" | "/settings"

function go(name: RouteName) { /* ... */ }
go("profile");   // OK
go("about");     // Error: not assignable to RouteName`}</code></pre>
        <p><strong>Why:</strong> <code>typeof ROUTES</code> lifts the runtime object into a type; <code>keyof</code> derives the key union and <code>[keyof typeof ROUTES]</code> derives the value union. Add a route to <code>ROUTES</code> and both types update with zero edits — the object is the single source of truth.</p>
      </div>
    ),
    semi: {
      prompt: "For `const xs = [1, 2, 3] as const`, what is `typeof xs[number]`? (write the literal union)",
      blanks: [
        { id: "answer", expected: /^1\s*\|\s*2\s*\|\s*3$/, placeholder: "a | b | c" },
      ],
    },
    blank: {
      prompt: "Why is `keyof { [k: string]: number }` equal to `string | number` rather than just `string`?",
      reveal: (
        <div class="space-y-2 text-sm">
          <p>A string index signature in TypeScript also permits numeric property access, because JavaScript coerces numeric object keys to strings at runtime: <code>obj[0]</code> is really <code>obj["0"]</code>. So the set of keys you may legally use is <code>string | number</code>, and <code>keyof</code> reports exactly that. The practical consequence: if you write a generic constrained by <code>K extends keyof Dict</code> hoping for precise string keys, you actually get the looser <code>string | number</code>. When you need a finite, precise key union, model the shape with explicit properties or <code>Record&lt;Union, V&gt;</code> rather than a bare index signature.</p>
        </div>
      ),
    },
  }}
  misconceptions={{
    "answer:number": "With `as const` the tuple elements keep their literal types, so xs[number] is the literal union 1 | 2 | 3, not the widened number.",
  }}
/>

<Quiz
  id="05-index-quiz-1"
  lessonSlug="05-index-access-types"
  lang="en"
  question="Given `type T = { a: string; b: number; c: string }`, what is `T[keyof T]`?"
  choices={[
    { label: "`string | number` — the union of all value types, with the duplicate string collapsed", correct: true },
    { label: "`\"a\" | \"b\" | \"c\"` — the keys", misconception: "That is keyof T (the keys). T[keyof T] indexes BY those keys to get the value types." },
    { label: "`string | number | string`", misconception: "Union members are deduplicated; identical types collapse, so it is string | number, not with a repeated string." },
    { label: "`{ a: string; b: number; c: string }`", misconception: "Indexed access extracts value types, it does not return the object type itself." },
  ]}
/>

<Quiz
  id="05-index-quiz-2"
  lessonSlug="05-index-access-types"
  lang="en"
  question="You have `const perms = ['read','write'] as const`. Which type gives you the union `'read' | 'write'`?"
  choices={[
    { label: "`typeof perms[number]`", correct: true },
    { label: "`keyof typeof perms`", misconception: "keyof of an array type gives index keys plus array methods (\"0\" | \"1\" | \"length\" | \"push\" | ...), not the element values." },
    { label: "`typeof perms`", misconception: "That is the readonly tuple type readonly [\"read\", \"write\"], not the union of its element types." },
    { label: "`perms[number]`", misconception: "perms is a value, not a type; you must lift it with typeof first, then index by number." },
  ]}
/>

</Explanation>

<KeyTakeaway>
`keyof T` is the union of T's key literal types; `T[K]` indexes into T to get the type stored at key `K`. Indexing with a *union* of keys yields the *union* of value types, so `T[keyof T]` is "all value types" and `Action["type"]` derives a discriminant's tag union straight from the variants. Arrays and tuples are indexed by `number`, so `Arr[number]` is the element type and `typeof arr[number]` (with `as const`) derives a literal union from a runtime array. These operators let you derive types from a single source of truth instead of hand-duplicating them. Watch the index-signature pitfall: `keyof` a string-indexed type is `string | number`, not `string`. This is the foundation mapped types build on.
</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-index-retrieval"
  lang="en"
  questions={[
    { q: "What do keyof T and T[K] each produce, and why is indexing 'by type, not by value'?", a: "keyof T produces a union of the literal types of T's keys (e.g. keyof { id: string; age: number } = 'id' | 'age'). T[K] is indexed access: it returns the type stored at key K (e.g. User['age'] = number). It is by TYPE because the index is a type — User['name'] uses the string-literal TYPE 'name', not a runtime value — which is why it works on types that have no runtime presence and why the key must be assignable to keyof T or you get an error." },
    { q: "What does T[keyof T] give, and how does indexed access behave on a union and on arrays?", a: "T[keyof T] indexes by every key, producing the union of all value types in T (duplicates collapse: string | string | number = string | number). On a union type, indexed access distributes over members: Action['type'] reads the type field from each member and unions them, deriving the discriminant tag union from the variants — keeping tags a single source of truth. Arrays and tuples are indexed by number, so Arr[number] is the element type; combined with `as const`, typeof arr[number] derives a literal union directly from a runtime array. For tuples, a literal index (Pair[0]) gives one slot while [number] gives the union of all positions." },
    { q: "Why does keyof on an index-signature type surprise you, and when should you avoid it?", a: "keyof { [k: string]: V } is string | number, NOT string, because a string index signature also permits numeric property access — JavaScript coerces numeric keys to strings (obj[0] becomes obj['0']), so number is a legal key too. A number index signature gives keyof = number. The trap: a generic constrained by K extends keyof Dict, expecting precise string keys, actually receives the loose string | number and loses precision. When you need a finite, precise key union, use a concrete object type with named properties or Record<SpecificUnion, V> instead of a bare index signature." },
  ]}
/>

<Recap lang="en">
Index access types close the unit by turning the union machinery into a way to *derive* types from data. `keyof T` gives the union of a type's keys and `T[K]` reads the type at a key; indexing by a union of keys yields the union of value types, so `T[keyof T]` is every value type and `Action["type"]` extracts a discriminant tag union straight from the variants you modelled last lesson. Arrays index by `number`, making `Arr[number]` the element type and `typeof arr[number]` (with `as const`) a literal union pulled from a runtime array — the antidote to hand-written, drift-prone duplicate unions. The one pitfall to remember is that `keyof` on a string index signature is `string | number`, not `string`. These two operators — `keyof` and `T[K]` — are exactly the primitives that **mapped types** in unit 04 use to transform every key of a type at once, so the derivation skills you built here carry directly into the type-system-deep unit. Now when you see a hand-maintained key union drifting from its object, you will reach for `keyof typeof` instead of patching the duplicate.
</Recap>
