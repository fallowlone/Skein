---
concepts:
  - module
  - moduleResolution
  - node16-nodenext
  - bundler-resolution
  - esm-cjs-interop
  - verbatimModuleSyntax
deepensInto:
  - "typescript/07-config-modules-build/03-declaration-files"
estMin: 15
lang: en
lessonType: topic
level: middle
order: 2
prereqs:
  - "typescript/07-config-modules-build/01-tsconfig-strictness"
slug: "02-module-resolution"
sources:
  - https://www.typescriptlang.org/docs/handbook/modules/theory.html
  - https://www.typescriptlang.org/docs/handbook/modules/reference.html
spiral:
  - module-systems
  - editor-vs-runtime
status: ready
summary: "`module` and `moduleResolution` decide how import strings become files — the classic 'works in the editor, fails at runtime' bug. `node16`/`nodenext` enforce real Node ESM rules; `bundler` (TS 5.0) relaxes them. `tsc` never rewrites `paths` — the bundler must."
title: "module & moduleResolution: how an import string becomes a file"
track: "typescript"
unit: "07-config-modules-build"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import modeTradeoff from "./infographics/mode-tradeoff.svg?raw";

<Hook>
The editor was green. `import { parse } from "./parser"` resolved, autocompleted, type-checked. CI built it. Then production crashed: `ERR_MODULE_NOT_FOUND: Cannot find module '/app/dist/parser'`. The project was ESM (`"type": "module"`), and Node ESM requires the `.js` extension in the import — but `moduleResolution` was set to `node`, an old mode that doesn't enforce that rule. TypeScript happily resolved a path that Node would never load.
</Hook>

<Crux>`module` sets emitted syntax; `moduleResolution` sets how files are found — mismatch the runtime and the editor passes code it rejects.</Crux>

<Explanation>

## Two settings, two jobs

When you see `ERR_MODULE_NOT_FOUND` in production on code that type-checked fine, the culprit is almost always here: two settings that look related but control completely different things.

`module` decides the **syntax TypeScript emits** (`require`/`exports` for CommonJS, `import`/`export` for ES2020+). `moduleResolution` decides the **algorithm that turns an import specifier into a file on disk**. They are separate, and the modern advice is to set `module` and let `moduleResolution` follow from it:

```jsonc
{
  "compilerOptions": {
    "module": "nodenext",          // emit ESM or CJS per package.json "type"
    "moduleResolution": "nodenext" // resolve the way Node 16+ actually does
  }
}
```

The available resolution modes, oldest to newest:

<div data-lesson-visual class="overflow-x-auto my-6">

| moduleResolution | walks | honours package.json `exports`? | `.js` ext required in ESM? | when |
| --- | --- | --- | --- | --- |
| `classic` | relative + ambient only | no | no | never (legacy, pre-1.6) |
| `node` (a.k.a. `node10`) | `node_modules` like CJS `require` | no | no | old CJS projects only |
| `node16` | CJS or ESM per file's package type | yes | yes (in ESM files) | Node, pinned to 16 behaviour |
| `nodenext` | same as node16, tracks newest Node | yes | yes (in ESM files) | Node libraries/apps today |
| `bundler` (TS 5.0) | `exports` + extensionless, no `.js` rule | yes | no | esbuild / Vite / webpack |

</div>

<Infographic
  svg={modeTradeoff}
  label="Head-to-head of two TypeScript moduleResolution modes: legacy node, which ignores package.json exports and skips the required .js extension, versus nodenext, which honours exports conditions and requires .js so the editor and the Node runtime resolve identically."
  caption="Of these five modes, two decide the bug: node green-lights imports Node rejects, while nodenext resolves the way the runtime actually runs — the cure for 'works in the editor, ERR_MODULE_NOT_FOUND at runtime'."
  hue="var(--d-infra)"
/>

## How resolution walks: bare specifiers and `exports`

For a *relative* import (`./parser`) the resolver tries files next to the importer. For a *bare* specifier (`lodash`, `@scope/pkg`) it walks up `node_modules` directories, then consults the package's `package.json`. Under `node16`/`nodenext`/`bundler` it reads the `exports` field and picks a target by matching **conditions** — `import` vs `require`, `types`, `node`, `default`:

```jsonc
// node_modules/@acme/sdk/package.json
{
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs",   // chosen for ESM `import`
      "require": "./dist/index.cjs"   // chosen for CJS `require`
    }
  }
}
```

`exports` is an **encapsulation boundary**: if a path isn't listed, it can't be imported, even if the file exists. `import "@acme/sdk/internal/util"` fails unless `./internal/util` is in `exports`. The old `node` mode ignores `exports` entirely, which is exactly why a package resolves under `node` but breaks under `nodenext` (or vice-versa).

<FlowDiagram
  label="Module resolution algorithm: turning one import specifier into a file under node16/nodenext."
  hue="var(--accent)"
  perRow={3}
  nodes={[
    { id: "spec", label: "import \"X\"", sub: "specifier" },
    { id: "rel", label: "Relative?", sub: "./ or ../" },
    { id: "file", label: "Try file + ext", sub: ".ts/.tsx/.js" },
    { id: "walk", label: "Walk node_modules", sub: "up the tree" },
    { id: "pkg", label: "Read package.json", sub: "exports field" },
    { id: "cond", label: "Match condition", sub: "import/require/types" },
    { id: "target", label: "Resolved file", sub: "+ its .d.ts" },
    { id: "fail", label: "Not found", sub: "TS2307 / runtime" },
  ]}
  edges={[
    { from: "spec", to: "rel" },
    { from: "rel", to: "file", label: "yes" },
    { from: "rel", to: "walk", label: "bare" },
    { from: "walk", to: "pkg" },
    { from: "pkg", to: "cond", label: "has exports" },
    { from: "cond", to: "target" },
    { from: "file", to: "target", label: "exists" },
    { from: "file", to: "fail", label: "missing" },
    { from: "cond", to: "fail", label: "no match" },
  ]}
  caption="A relative specifier resolves against the importer's directory; a bare one walks node_modules and matches an exports condition. The wrong moduleResolution mode skips the exports/extension steps, which is how the editor and the runtime disagree."
/>

## The surprising rule: `.js` extensions on TS imports

Under `node16`/`nodenext`, in an ESM file you must write the import with a **`.js`** extension even though the source file is `parser.ts`:

```ts
// src/index.ts — ESM (package.json "type": "module")
import { parse } from "./parser.js"; // ✅ resolves parser.ts, emits import "./parser.js"
import { parse } from "./parser";    // ❌ TS2835: relative import needs ".js" extension
import { parse } from "./parser.ts"; // ❌ TS5097: an import path can't end with ".ts"
```

You write the **output** extension (`.js`), TypeScript checks against the **source** (`.ts`), and the emit is left untouched so Node finds the real file at runtime. `bundler` mode turns this requirement off because bundlers resolve extensionless imports themselves.

<Inset kind="why" lang="en">
Why force `.js` and not `.ts`? Because TypeScript's design rule is that it **never rewrites import specifiers** in emit (with the lone exception of changing the import *form* for `module: commonjs`). The string you write is the string Node sees. Since Node ESM requires the extension and the file on disk is `parser.js` after compilation, you must write `parser.js`. Writing `parser.ts` would emit `parser.ts`, which doesn't exist at runtime.
</Inset>

## `paths` aliases: tsc checks them, doesn't emit them

`baseUrl`/`paths` let you write `import { db } from "@/db"` instead of `../../../db`. Critically, **`tsc` resolves these for type-checking but does not rewrite them in emitted JS** — the literal `"@/db"` is left in the output. At runtime, Node has no idea what `@/db` is.

```jsonc
{ "compilerOptions": { "baseUrl": ".", "paths": { "@/*": ["src/*"] } } }
```

So `paths` only works if **something else** rewrites the alias: a bundler (Vite, esbuild, webpack with a matching `resolve.alias`), or a runtime loader (`tsconfig-paths`, or a `package.json` `imports` field). The classic failure: type-checks and bundles fine via the dev server, then a plain `node dist/index.js` throws `Cannot find module '@/db'`.

## Interop: `esModuleInterop` and `verbatimModuleSyntax`

CommonJS has no real default export; `module.exports = x` is a whole-namespace assignment. `esModuleInterop: true` synthesises a default so `import express from "express"` works against a CJS module, and emits `__importDefault` helpers. Without it you'd need `import express = require("express")` or `import * as express`.

`verbatimModuleSyntax` (TS 5.0, replacing `importsNotUsedAsValues` and the emit half of `isolatedModules` concerns) makes emit literal: a regular `import`/`export` is emitted as-is, and only `import type` / `export type` are erased. This forces you to be explicit about what's a type-only import, which is exactly what single-file transpilers need:

```ts
import { type User, createUser } from "./user.js";
// emit: import { createUser } from "./user.js";  — `User` erased, `createUser` kept verbatim
```

<DragOrder
  id="07-modres-drag-1"
  pieceSlug="02-module-resolution"
  lang="en"
  prompt="Order the steps node16/nodenext takes to resolve import { x } from '@acme/sdk' in an ESM file:"
  items={[
    { id: "a", label: "See the bare specifier @acme/sdk (not relative), so search node_modules" },
    { id: "b", label: "Walk up node_modules directories until @acme/sdk/package.json is found" },
    { id: "c", label: "Read the package's exports field instead of guessing main/index" },
    { id: "d", label: "Match the import condition (ESM context) to pick ./dist/index.mjs and its types" },
  ]}
/>

<Quiz
  id="07-modres-quiz-1"
  lessonSlug="02-module-resolution"
  lang="en"
  question="An ESM project type-checks in VS Code but `node dist/index.js` throws Cannot find module './parser'. The tsconfig has moduleResolution: 'node'. What is the root cause?"
  choices={[
    { label: "moduleResolution: 'node' doesn't enforce Node ESM's required file extension, so TS accepted an extensionless import that Node rejects — it should be 'nodenext' and the import should be './parser.js'.", correct: true },
    { label: "The .ts file wasn't compiled; dist/parser.js doesn't exist.", misconception: "If compilation were skipped the error would mention the .ts source or be a build error. Here parser.js exists; the import specifier just lacks the .js extension Node ESM requires, which 'node' mode doesn't enforce." },
    { label: "esModuleInterop is off, so the default import failed.", misconception: "esModuleInterop affects default-import shape for CJS modules, not whether a relative path resolves. The failure is the missing .js extension under the wrong resolution mode." },
    { label: "paths aliases weren't rewritten by tsc.", misconception: "'./parser' is a relative import, not a paths alias. The issue is the resolution mode not requiring the .js extension that Node ESM needs." },
  ]}
/>

</Explanation>

<KeyTakeaway>
`module` sets the **import/export syntax TypeScript emits**; `moduleResolution` sets the **algorithm that maps a specifier to a file**. Match them to the runtime: `nodenext`/`node16` follow real Node ESM/CJS rules (read `package.json` `exports`, match `import`/`require`/`types` conditions, and **require the `.js` extension** on relative ESM imports of `.ts` files); `bundler` (TS 5.0) keeps `exports` but drops the extension rule for esbuild/Vite; the legacy `node` mode ignores `exports` entirely, which is the classic source of "resolves in the editor, `ERR_MODULE_NOT_FOUND` at runtime". TypeScript **never rewrites import specifiers** in emit, so `paths`/`baseUrl` aliases are type-check-only — a bundler or loader must rewrite them or `node dist/index.js` throws. `esModuleInterop` synthesises a default export for CJS modules; `verbatimModuleSyntax` makes emit literal, erasing only `import type`/`export type`, which is what single-file transpilers need.
</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="07-modres-retrieval"
  lang="en"
  questions={[
    { q: "What is the difference between `module` and `moduleResolution`, and why does the wrong mode cause 'works in editor, fails at runtime'?", a: "`module` controls the syntax TypeScript EMITS — require/exports for CommonJS, import/export for ESM. `moduleResolution` controls the ALGORITHM that turns an import specifier into a file on disk. The runtime-mismatch bug happens because the compiler resolves with one set of rules while Node runs with another: e.g. moduleResolution: 'node' ignores package.json `exports` and does not require the `.js` extension that Node ESM mandates, so TS green-lights an import Node will reject with ERR_MODULE_NOT_FOUND. Setting module/moduleResolution to nodenext makes the compiler resolve the same way the runtime does." },
    { q: "Under node16/nodenext, why must you write `./parser.js` to import a file called parser.ts, and what general rule does this follow?", a: "TypeScript's design rule is that it never rewrites import specifiers in emit (except changing the import form under module: commonjs). The string you write is the string Node sees at runtime. Node ESM requires the file extension, and after compilation the file on disk is parser.js — so you must write `./parser.js`. TypeScript checks that specifier against the source parser.ts but leaves the `.js` in the output untouched. Writing `./parser` errors (TS2835, extension required) and `./parser.ts` errors (TS5097, can't end in .ts). The `bundler` mode disables this requirement because bundlers resolve extensionless imports themselves." },
    { q: "Why do `paths`/`baseUrl` aliases work in the editor and bundler but break under plain `node dist/index.js`?", a: "Because tsc resolves `paths` for type-checking but does NOT rewrite them in emitted JS — the literal alias string (e.g. '@/db') is left in the output. At runtime Node has no concept of '@/db'. The alias only works if something else performs the rewrite: a bundler with a matching resolve.alias (Vite/esbuild/webpack), or a runtime loader like tsconfig-paths, or a package.json `imports` field. So a dev server that bundles will work while `node dist/index.js` throws Cannot find module '@/db'. The fix is to align the bundler/runtime alias config with tsconfig, or use `imports` subpath patterns that Node itself understands." },
  ]}
/>

<Recap lang="en">
You can now reason about how an import string becomes a file: `module` emits the syntax, `moduleResolution` finds the target, `exports`/conditions gate package access, the `.js`-extension rule trips ESM newcomers, and `paths` need a bundler to survive emit. Next, **declaration files** — once your code resolves, how do you ship the *types* for it? `.d.ts` files, ambient and module declarations, and the augmentation traps that silently fail to merge. Now when you see `ERR_MODULE_NOT_FOUND` in production on code that the editor accepted, your first question is: does `moduleResolution` match the runtime, and did you write the `.js` extension on that relative import?
</Recap>
