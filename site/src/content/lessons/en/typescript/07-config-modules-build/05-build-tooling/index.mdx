---
concepts:
  - transpile-only
  - isolatedModules
  - tsc-noemit
  - type-check-gate
  - verbatimModuleSyntax
  - const-enum
deepensInto:
  - "typescript/08-real-world/05-common-pitfalls"
estMin: 14
lang: en
lessonType: topic
level: middle
order: 5
prereqs:
  - "typescript/07-config-modules-build/04-project-references"
slug: "05-build-tooling"
sources:
  - https://www.typescriptlang.org/docs/handbook/release-notes/typescript-5-0.html
  - https://www.typescriptlang.org/tsconfig#isolatedModules
spiral:
  - editor-vs-runtime
  - type-erasure
status: ready
summary: "esbuild/swc/babel transpile file-by-file and DO NOT type-check — a type error never stops the bundle. That model needs `isolatedModules` (no cross-file const enums, demands `export type`). The pattern: the bundler builds, and `tsc --noEmit` is the separate CI type gate."
title: "Type-checking vs transpilation: why your bundler doesn't catch type errors"
track: "typescript"
unit: "07-config-modules-build"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
A teammate shipped code where `const total: number = someUserId;` — assigning a string to a `number`. Vite built it without a peep, the app deployed, and the bug only surfaced in production when arithmetic returned `NaN`. "But it's TypeScript!" — yes, but Vite uses esbuild, which **transpiles** TypeScript and never type-checks it. The type annotation was deleted, not verified. Nothing in the build pipeline ran the type checker.
</Hook>

<Crux>Bundlers (esbuild/swc/babel) strip types per-file without checking them; type safety exists only if `tsc --noEmit` runs as a separate gate.</Crux>

<Explanation>

## Two completely different jobs: checking vs transpiling

`tsc` does **two** things: it *type-checks* (analyses the whole program for type errors) and it *emits* JavaScript. Bundlers split these apart and keep only the second:

<div data-lesson-visual class="overflow-x-auto my-6">

| tool | type-checks? | emit | speed | sees other files? |
| --- | --- | --- | --- | --- |
| `tsc` | yes (whole program) | JS + `.d.ts` | slow | yes — cross-file analysis |
| `esbuild` | **no** | JS | very fast | no — one file at a time |
| `swc` | **no** | JS | very fast | no — one file at a time |
| `babel` (preset-typescript) | **no** | JS | fast | no — one file at a time |
| `tsc --noEmit` | yes | nothing | slow | yes — the CI type gate |

</div>

The reason bundlers are 10–100× faster than `tsc` is *exactly* that they don't type-check. Type analysis requires building a graph of the whole program; transpilation just deletes type syntax token-by-token in one file. A type error is invisible to them — it's just syntax they erase.

## `isolatedModules`: making single-file transpilation safe

Because esbuild/swc/babel compile **one file at a time** with no knowledge of other files, some TypeScript constructs are *unsafe* to transpile that way — they need whole-program information. `isolatedModules: true` makes `tsc` flag exactly those constructs, so your code is portable to a single-file transpiler:

```ts
// with isolatedModules, these error in tsc:

// 1. A type-only re-export that LOOKS like a value re-export:
export { SomeType } from "./types";
//       ^^ TS1205-ish: a single-file compiler can't tell if SomeType is a
//          type (erase the export) or a value (keep it). Fix:
export type { SomeType } from "./types"; // now unambiguous — erase it

// 2. const enum across files — needs the enum's values inlined from another file:
import { Color } from "./enums";
const c = Color.Red; // const enum can't be inlined without cross-file info
```

`isolatedModules` doesn't change emit; it *restricts what you may write* so that per-file transpilation produces correct output. The two big things it effectively forbids: **`const enum` consumed across files** (a single-file compiler can't inline values it can't see) and **ambiguous type-only re-exports** (forcing `export type` / `import type`).

<Inset kind="why" lang="en">
Why can't a single-file transpiler handle `export { SomeType } from "./types"`? Because to emit correct JS it must decide whether `SomeType` is a *type* (erase the re-export entirely — types don't exist at runtime) or a *value* (keep the re-export). Making that decision requires opening `./types` and checking what `SomeType` is — cross-file knowledge the transpiler refuses to gather for speed. `export type { SomeType }` tells it unambiguously "this is a type, erase it", which is why `isolatedModules` (and `verbatimModuleSyntax`) push you toward explicit `type` modifiers.
</Inset>

## The pattern: bundler builds, `tsc --noEmit` gates

The production setup decouples the two jobs. The bundler produces the artifact fast; `tsc --noEmit` (often `tsc -b --noEmit` for monorepos) runs in parallel or in CI purely to *check types* — it emits nothing, just fails the build if a type is wrong:

```jsonc
// package.json scripts
{
  "scripts": {
    "build": "vite build",            // esbuild transpiles — NO type checking
    "typecheck": "tsc --noEmit",      // the real type gate — emits nothing
    "ci": "npm run typecheck && npm run build"
  }
}
```

<SequenceDiagram
  label="Build pipeline: source fans out to tsc --noEmit type checking in parallel with esbuild transpilation, then bundle."
  hue="var(--accent)"
  actors={["source .ts", "tsc --noEmit", "esbuild", "bundle"]}
  messages={[
    { from: "source .ts", to: "tsc --noEmit", label: "whole-program check" },
    { from: "tsc --noEmit", to: "bundle", label: "pass/fail gate (no emit)" },
    { from: "source .ts", to: "esbuild", label: "per-file transpile" },
    { from: "esbuild", to: "bundle", label: "emit JS (types erased)" },
    { from: "bundle", to: "source .ts", label: "ship only if gate passed" },
  ]}
  caption="Two independent paths from the same source: tsc --noEmit checks types and emits nothing (the gate); esbuild transpiles each file and emits JS without checking. CI ships only when the gate passes — the bundler alone would never catch a type error."
/>

`verbatimModuleSyntax` (TS 5.0) reinforces this: it makes import/export emit literal so a regular `import` is always kept and only `import type`/`export type` are erased. That predictability is what a transpiler needs — no guessing whether an import was "used as a value". It supersedes the old `importsNotUsedAsValues` and addresses the same single-file concerns `isolatedModules` flags. (Note: `tsc` itself still does declaration emit — `.d.ts` generation — which a transpiler never does, so library builds keep `tsc` for types even when esbuild produces the JS.)

<MetaphorComplete
  id="07-build-metaphor-1"
  pieceSlug="05-build-tooling"
  lang="en"
  setup="A transpiler like esbuild is to type-checking as a fast photocopier is to ___ — it reproduces the pages quickly but never reads them for mistakes."
  accepted={["a proofreader", "proofreader", "an editor", "editor", "proofreading", "a reviewer", "reviewer"]}
  canonical="a proofreader"
  explanation="esbuild copies (transpiles) the document fast but, like a photocopier, never proofreads the content for errors — that's a separate job. tsc --noEmit is the proofreader: it reads the whole document for type mistakes but produces no copy. You need both: the copier for speed, the proofreader for correctness."
/>

<Quiz
  id="07-build-quiz-1"
  lessonSlug="05-build-tooling"
  lang="en"
  question="Your Vite app builds and deploys even though a file has a clear type error (assigning a string to a number). Why didn't the build fail, and how do you catch it?"
  choices={[
    { label: "Vite uses esbuild, which transpiles per-file and never type-checks — the annotation was erased, not verified. Add a separate `tsc --noEmit` step (in CI) as the type gate.", correct: true },
    { label: "The type error was a warning, not an error, so the build continued.", misconception: "esbuild doesn't classify type errors at all — it doesn't run the type checker. There is no warning/error distinction because no type analysis happens. You must run tsc --noEmit separately." },
    { label: "Vite type-checks only in production mode, and this was a dev build.", misconception: "Vite never type-checks via esbuild in any mode. Type checking is delegated to tsc (or a plugin like vite-plugin-checker that runs tsc in the background); the bundler itself only transpiles." },
    { label: "isolatedModules was off, so checking was skipped.", misconception: "isolatedModules restricts which constructs you may write for safe single-file transpilation; it does not enable or disable type checking. Bundlers don't type-check regardless of that flag." },
  ]}
/>

</Explanation>

<KeyTakeaway>
`tsc` both **type-checks** (whole-program analysis) and **emits** JS; bundlers (esbuild, swc, babel) keep only the emit half — they **transpile each file in isolation and never type-check**, which is why they're 10–100× faster and why a real type error sails through the build untouched (the annotation is *erased*, not *verified*). Because single-file transpilers have no cross-file knowledge, `isolatedModules: true` makes `tsc` flag constructs that need whole-program info: it effectively forbids **`const enum` consumed across files** (values can't be inlined without seeing the source) and **ambiguous type-only re-exports** (you must write `export type` / `import type`). The production pattern decouples the jobs: the bundler builds fast, and **`tsc --noEmit` runs as a separate gate** (in CI) that emits nothing and exists purely to fail on type errors. `verbatimModuleSyntax` (TS 5.0) makes import erasure literal and predictable — only `import type`/`export type` are stripped — which is exactly what a transpiler needs; and `tsc` still owns `.d.ts` declaration emit, which no transpiler produces.
</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-build-retrieval"
  lang="en"
  questions={[
    { q: "Why does a type error pass through an esbuild/swc/babel build, and what is the fix?", a: "Those tools are transpile-only: they strip TypeScript syntax file-by-file and emit JavaScript, but they never run the type checker. A type error is just annotation syntax they erase — it's invisible to them, which is exactly why they're 10–100× faster than tsc (no whole-program graph to build). So a clear error like assigning a string to a number compiles and deploys. The fix is to run type checking as a separate step: `tsc --noEmit` (it emits nothing, just analyses the whole program and fails on type errors), wired into CI alongside the bundler — e.g. `tsc --noEmit && vite build`. A dev-time plugin like vite-plugin-checker can surface the same check in the editor." },
    { q: "What is isolatedModules, why is it required for single-file transpilers, and what does it forbid?", a: "isolatedModules: true makes tsc flag any construct whose correct transpilation needs whole-program (cross-file) information — guaranteeing the code can be compiled correctly one file at a time by esbuild/swc/babel. It does NOT change emit; it restricts what you may write. The two big things it effectively forbids: (1) `const enum` consumed across files — a single-file compiler can't inline the enum's values without reading the defining file; (2) ambiguous type-only re-exports/imports like `export { SomeType } from './x'` where the transpiler can't tell if SomeType is a type (erase) or value (keep) — you must disambiguate with `export type` / `import type`. It is required (or strongly recommended) whenever a non-tsc transpiler does your emit." },
    { q: "What is the bundler-plus-tsc-noEmit pattern, and where does verbatimModuleSyntax / declaration emit fit?", a: "The pattern decouples emit from checking: a fast bundler (Vite/esbuild/swc) produces the JS artifact, while `tsc --noEmit` runs separately (in CI or a parallel script) purely as the type gate — it emits nothing and fails the pipeline if any type is wrong, so CI ships only when types pass. verbatimModuleSyntax (TS 5.0) supports this by making import/export emit literal: a regular import is always kept and only `import type`/`export type` are erased, removing the transpiler's guesswork about whether an import was used as a value (it supersedes importsNotUsedAsValues). Separately, declaration emit (.d.ts) is still tsc's job — no transpiler generates types — so library builds run tsc for declarations even when esbuild produces the runtime JS." },
  ]}
/>

<Recap lang="en">
You now know the division of labour that defines a modern TypeScript build: transpilers emit JS per-file and never check types, `isolatedModules` keeps your code safe for that model (no cross-file const enums, explicit `export type`), and `tsc --noEmit` is the gate that actually enforces type safety while the bundler handles speed. This closes the config/modules/build unit. Next, in **real-world TypeScript**, we apply all of it — common pitfalls where these config and emit subtleties bite teams in production code.
</Recap>
