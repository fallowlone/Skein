---
concepts:
  - project-references
  - composite
  - incremental
  - tsbuildinfo
  - build-graph
  - tsc-build
deepensInto:
  - "typescript/07-config-modules-build/05-build-tooling"
estMin: 14
lang: en
lessonType: topic
level: middle
order: 4
prereqs:
  - "typescript/07-config-modules-build/02-module-resolution"
slug: "04-project-references"
sources:
  - https://www.typescriptlang.org/docs/handbook/project-references.html
spiral:
  - module-systems
  - incrementality
status: ready
summary: "Project references split a monorepo into `tsconfig` projects that `tsc --build` compiles in dependency order. `composite` (which forces `declaration`) enables referencing and `.tsbuildinfo` caching. Traps: stale buildinfo, circular references, the 'must have composite' error."
title: "Project references: tsc --build, composite, and the incremental build graph"
track: "typescript"
unit: "07-config-modules-build"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A 200k-line monorepo took 90 seconds to type-check on every save, because one big `tsconfig` re-checked *all* packages whenever any file changed. Someone split it into referenced projects and added `composite: true` — and a one-line edit to the `web` package now re-checks only `web`, reusing the cached output of `core` and `ui`. The first build wrote a `.tsbuildinfo` per project; subsequent builds read it and skip unchanged projects. But the migration started with a wall of `error TS6306: Referenced project must have setting "composite": true`.
</Hook>

<Crux>`tsc --build` compiles `tsconfig`s in dependency order, reusing cached `.tsbuildinfo` — but only `composite` projects can be referenced.</Crux>

<Explanation>

## The shape: a project that points at its dependencies

Each package has its own `tsconfig.json`. A package that depends on another lists it under `references`, and the *referenced* project must be `composite`:

```jsonc
// packages/web/tsconfig.json
{
  "compilerOptions": { "composite": true, "outDir": "dist" },
  "references": [
    { "path": "../core" },   // web depends on core
    { "path": "../ui" }      // and on ui
  ]
}
```

```jsonc
// packages/core/tsconfig.json
{
  "compilerOptions": {
    "composite": true,        // REQUIRED to be referenced
    "declaration": true,      // composite implies this; types are the cross-project contract
    "outDir": "dist"
  }
}
```

`composite: true` flips on three things: it forces `declaration: true` (so the project emits `.d.ts` — the *interface* other projects consume instead of source), it forces `incremental` behaviour with a `.tsbuildinfo`, and it requires that every input file is covered by `include`/`files` (so the build can hash inputs reliably).

<Inset kind="why" lang="en">
Why does `composite` *require* `declaration`? Because the whole point of a referenced project is that downstream projects don't re-read its source — they read its emitted `.d.ts`. The `.d.ts` is the cross-project type contract. Without declaration emit there'd be nothing for `web` to type-check against except `core`'s raw `.ts`, which defeats the isolation (and the speed). So `composite` and `declaration` are inseparable: a referenced project must publish its types.
</Inset>

## `tsc --build` walks the reference graph

A normal `tsc` build of a project with references would fail or ignore them. You must use **build mode**: `tsc --build` (or `tsc -b`). Build mode reads the reference graph, topologically sorts it, and builds dependencies *before* dependents — and skips any project whose inputs are unchanged since its `.tsbuildinfo`:

```bash
tsc --build packages/web   # builds core, then ui, then web — in dependency order
tsc --build --verbose      # prints which projects are up-to-date vs rebuilt
tsc --build --clean        # deletes all outputs + .tsbuildinfo (the stale-cache cure)
tsc --build --force        # ignores .tsbuildinfo, rebuilds everything
```

<FlowDiagram
  label="tsc --build dependency DAG: ordering and incremental skipping across referenced monorepo packages."
  hue="var(--accent)"
  perRow={3}
  nodes={[
    { id: "shared", label: "shared", sub: "composite, leaf" },
    { id: "core", label: "core", sub: "→ shared" },
    { id: "ui", label: "ui", sub: "→ core" },
    { id: "api", label: "api", sub: "→ core" },
    { id: "web", label: "web", sub: "→ ui, core" },
    { id: "info", label: ".tsbuildinfo", sub: "per project" },
    { id: "skip", label: "Up-to-date?", sub: "hash inputs" },
    { id: "emit", label: "Emit .js + .d.ts", sub: "or reuse" },
  ]}
  edges={[
    { from: "shared", to: "core" },
    { from: "core", to: "ui" },
    { from: "core", to: "api" },
    { from: "ui", to: "web" },
    { from: "core", to: "web" },
    { from: "info", to: "skip", label: "read" },
    { from: "skip", to: "emit", label: "changed" },
    { from: "skip", to: "emit", label: "unchanged: reuse" },
  ]}
  caption="Build mode topologically sorts the reference DAG (shared → core → ui/api → web). Each project's .tsbuildinfo lets the build skip it when its inputs hash unchanged, so a leaf edit only rebuilds that project and its dependents."
/>

## Incrementality and `.tsbuildinfo`

`.tsbuildinfo` is a cache of file hashes and signatures. On rebuild, `tsc -b` compares current inputs to the cached state; if a project's inputs (and the `.d.ts` of its dependencies) are unchanged, it's *up to date* and skipped entirely. Crucially, a project only needs to rebuild a dependent when its **public `.d.ts` actually changed** — an internal-only edit that doesn't alter the emitted types lets dependents stay cached (the "signature" optimisation).

This is also why editors feel snappier: each referenced project is type-checked in isolation, so the language server doesn't re-analyse the whole monorepo on every keystroke.

## The traps

**`error TS6306: Referenced project must have setting "composite": true`** — you referenced a project that isn't composite. Add `composite: true` to *the referenced* one (a common confusion: people add it to the referrer).

**Stale `.tsbuildinfo`** — if you change something the cache doesn't track (a compiler upgrade with a quirk, a hand-edited `dist`), the build may wrongly think a project is up to date. Cure: `tsc -b --clean` then rebuild, or `--force`.

**Circular references** — `A` references `B` and `B` references `A` is rejected (`TS6202`); a build DAG must be acyclic. Break the cycle by extracting the shared types into a third leaf project both depend on.

**Path mapping across projects** — `paths` aliases still aren't rewritten on emit (lesson 02), so cross-project imports should go through the package's real entry/`exports`, not a `paths` alias that only the type-checker understands.

<DragOrder
  id="07-projref-drag-1"
  pieceSlug="04-project-references"
  lang="en"
  prompt="Order what `tsc --build packages/web` does for a graph where web → ui → core and ui → core:"
  items={[
    { id: "a", label: "Read web's references, then transitively collect ui and core into the build graph" },
    { id: "b", label: "Topologically sort: core has no deps, so it builds first" },
    { id: "c", label: "For each project, hash inputs vs .tsbuildinfo; rebuild only if changed, else skip" },
    { id: "d", label: "Build dependents after their dependencies: core → ui → web, emitting .js + .d.ts" },
  ]}
/>

<Quiz
  id="07-projref-quiz-1"
  lessonSlug="04-project-references"
  lang="en"
  question="You run `tsc -b` and get TS6306: Referenced project 'packages/core' must have setting 'composite': true. Where do you add composite?"
  choices={[
    { label: "In packages/core/tsconfig.json — the REFERENCED project must be composite so it emits .d.ts and a .tsbuildinfo to be consumed.", correct: true },
    { label: "In the referrer (packages/web), because it owns the references array.", misconception: "The referrer lists references, but the requirement is on the target: the project being referenced must be composite. Add it to packages/core, not packages/web." },
    { label: "In a root tsconfig that all projects extend.", misconception: "A shared base can set composite, but every referenced project must actually be composite. The error names packages/core specifically — that project (or its base) needs the setting." },
    { label: "Nowhere; use `tsc` without -b and the error goes away.", misconception: "Plain tsc ignores references and won't build the graph correctly. Project references require build mode (-b); the fix is to make the referenced project composite, not to drop build mode." },
  ]}
/>

</Explanation>

<KeyTakeaway>
Project references split a monorepo into per-package `tsconfig`s; a package lists its dependencies under `references`, and each *referenced* project must set `composite: true`. `composite` forces `declaration: true` (the emitted `.d.ts` is the cross-project type contract that lets dependents avoid re-reading source), enables `.tsbuildinfo` caching, and requires full `include`/`files` coverage. You build with **build mode** — `tsc --build` / `tsc -b` — which collects the reference graph, topologically sorts it, builds dependencies before dependents, and **skips any project whose inputs hash unchanged**; a dependent only rebuilds when a dependency's public `.d.ts` actually changes. Editors gain the same isolation, so the language server checks one project at a time instead of the whole repo. The traps: `TS6306` means *the referenced* project isn't composite (add it there, not on the referrer); a stale `.tsbuildinfo` is cured with `tsc -b --clean`/`--force`; circular references (`TS6202`) require extracting shared types into a third leaf; and cross-project imports must use real entry points, not `paths` aliases that emit unrewritten.
</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-projref-retrieval"
  lang="en"
  questions={[
    { q: "What does `composite: true` do, and why does it force `declaration`?", a: "composite marks a project as referenceable. It does three things: (1) it forces declaration: true so the project emits .d.ts; (2) it turns on incremental .tsbuildinfo caching; (3) it requires every input file to be covered by include/files so inputs can be hashed reliably. It forces declaration because the whole point of a referenced project is that dependents read its emitted .d.ts — the cross-project type contract — instead of re-reading its source. Without declaration emit there would be no published types for a dependent to check against, defeating both the isolation and the speed that references exist to provide." },
    { q: "What does `tsc --build` do that plain `tsc` doesn't, and how does it use the reference graph?", a: "Plain tsc ignores references and compiles a single project's files. Build mode (tsc --build / tsc -b) reads the `references` arrays to assemble the dependency DAG, topologically sorts it, and builds dependencies BEFORE dependents (e.g. core before ui before web). For each project it compares current inputs against the cached .tsbuildinfo; if inputs (and the .d.ts of dependencies) are unchanged, the project is up-to-date and skipped entirely. A dependent only rebuilds when a dependency's PUBLIC .d.ts actually changed — an internal edit with no type-signature change leaves dependents cached. Useful flags: --verbose (shows up-to-date vs rebuilt), --clean (delete outputs + .tsbuildinfo), --force (rebuild ignoring the cache)." },
    { q: "Name the three classic project-reference failures and their fixes.", a: "(1) TS6306 'Referenced project must have setting composite: true' — the REFERENCED project (not the referrer) lacks composite; add it to the dependency's tsconfig (or a base it extends). (2) Stale .tsbuildinfo — the cache wrongly reports a project up-to-date after an untracked change (compiler upgrade, hand-edited dist); cure with `tsc -b --clean` then rebuild, or `--force` to ignore the cache. (3) Circular references (TS6202) — A references B and B references A; the build DAG must be acyclic, so extract the shared types into a third leaf project both depend on. Bonus trap: cross-project paths aliases don't survive emit (tsc doesn't rewrite them), so import through the package's real entry/exports, not a type-only alias." },
  ]}
/>

<Recap lang="en">
You can now structure a monorepo for fast, isolated builds: `references` declare the graph, `composite` (forcing `declaration`) makes a project consumable, `tsc -b` orders and caches the build via `.tsbuildinfo`, and you know how to read `TS6306`, clear stale caches, and break cycles. Next, **build tooling** — references organise `tsc`'s own work, but most projects hand emit to esbuild/swc/babel; we'll see why those transpilers *don't type-check*, why `isolatedModules` is then mandatory, and how `tsc --noEmit` becomes the CI type gate alongside the bundler.
</Recap>
