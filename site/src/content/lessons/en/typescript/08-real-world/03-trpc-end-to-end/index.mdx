---
concepts:
  - trpc
  - end-to-end-types
  - typeof-types
  - inference-cost
  - import-type
deepensInto:
  - "typescript/09-putting-it-together/01-capstone-typed-feature"
estMin: 15
lang: en
lessonType: topic
level: senior
order: 3
prereqs:
  - "typescript/08-real-world/02-zod-and-validation"
  - "apis/01-rest-modeling/01-overview"
slug: "03-trpc-end-to-end"
sources:
  - https://trpc.io/docs
  - https://www.typescriptlang.org/docs/handbook/2/typeof-types.html
spiral:
  - inference
  - single-source-of-truth
  - api-contracts
status: ready
summary: "tRPC gives end-to-end type safety with no codegen: the client infers its whole API type from `typeof appRouter`, so types flow across the wire at compile time. 'No codegen' means client and server share the type (`import type`); the cost is inference time and editor lag."
title: "tRPC: end-to-end types with no codegen, and what that costs"
track: "typescript"
unit: "08-real-world"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
You rename a server procedure from `getUser` to `getUserById` and add a `includeDeleted: boolean` argument. You hit save. Before you touch the client at all, three call sites across two frontend files light up red: `Property 'getUser' does not exist`, and the one place that called it is now missing the new argument. No codegen ran. No OpenAPI file regenerated. No `npm run gen`. The client just *knew* — because the client's API type is not a separate copy, it is an inference of the server's router type, recomputed on every keystroke. That is tRPC, and the magic is entirely the type system you already learned.
</Hook>

<Crux>tRPC infers the client API type from `typeof appRouter` at compile time — no codegen, but the client must share the server's type.</Crux>

<Explanation>

## The whole trick: `typeof appRouter`

On the server you build a router. Its *type* — not its value — encodes every procedure name, input, and output. You export that type and hand it to the client as a generic:

```ts
// server/router.ts
import { initTRPC } from "@trpc/server";
import { z } from "zod";
const t = initTRPC.create();

export const appRouter = t.router({
  getUserById: t.procedure
    .input(z.object({ id: z.number(), includeDeleted: z.boolean() }))
    .query(({ input }) => findUser(input.id)), // returns User
});

export type AppRouter = typeof appRouter; // ^? the entire router's type
```

```ts
// client/trpc.ts
import { createTRPCClient, httpBatchLink } from "@trpc/client";
import type { AppRouter } from "../server/router"; // type-only — see below

const trpc = createTRPCClient<AppRouter>({ links: [httpBatchLink({ url: "/trpc" })] });

const user = await trpc.getUserById.query({ id: 42, includeDeleted: false });
// ^? User  — inferred end-to-end from the server's return type
// trpc.getUser  // Error: Property 'getUser' does not exist
```

`createTRPCClient<AppRouter>` takes the server router *type* and, through a large internal mapped/conditional type, transforms it into the client's call interface: `appRouter`'s `getUserById.query` procedure becomes `trpc.getUserById.query(input): Promise<output>`. The input type comes from your zod schema's `z.infer`; the output type comes from the resolver's return type. Both flow to the client with zero generated files. (In a real app you rarely call `.query` directly — you feed it through a client cache like React Query, where these inferred types carry through to your hooks; see the [Frontend track](/en/learn/frontend) on client caches.)

<Inset kind="why" lang="en">
This is `typeof` in type space (the operator from unit 04) doing the heavy lifting. `typeof appRouter` lifts a *runtime value* (the router object) into a *type* the client can consume. The client never imports the router's runtime — only its type — so the server's database calls, secrets, and dependencies never reach the browser bundle. The contract is the type; the implementation stays home.
</Inset>

## "No codegen" — what it actually means and its hard limits

REST + OpenAPI generates a `client.ts` file from a spec; GraphQL generates types from a schema. tRPC generates *nothing*. The trade is that the client must be able to `import type` the server's `AppRouter` — which means:

- **Same TypeScript build / monorepo.** Client and server compile together (or the server ships a `.d.ts` the client consumes). There is no wire format describing the API; the *type* is the contract, and a type can only cross a compile boundary, not a network or language one.
- **TypeScript on both ends.** A Swift or Kotlin client cannot consume a TS type. For polyglot or third-party consumers you need a real wire contract — OpenAPI or GraphQL — which is exactly when their codegen pays off.

<SequenceDiagram
  label="tRPC inference path: server router type to inferred client to typed call site"
  hue="var(--d-frontend)"
  actors={["router", "AppRouter type", "client call"]}
  messages={[
    { from: "router", to: "AppRouter type", label: "typeof appRouter" },
    { from: "AppRouter type", to: "client call", label: "createTRPCClient<AppRouter>" },
    { from: "client call", to: "client call", label: "infer input from z schema" },
    { from: "client call", to: "client call", label: "infer output from resolver" },
  ]}
  caption="No file is generated. The arrows are pure compile-time type computation: the value typeof-lifted to AppRouter, mapped into a client interface, with input/output inferred per procedure. It only works because AppRouter can be imported as a type."
/>

## The cost: router inference time and editor lag

That convenience is paid for in compiler work. The client's type is a *function of the entire server router type*, recomputed by `tsc` and the editor's language service. On a router with hundreds of procedures, deeply nested sub-routers, and complex zod schemas, you feel it: hover tooltips that spin, autocomplete that lags, `tsc` runs that crawl. The inference doesn't get cached the way a generated file would — there is no file. Mitigations:

- **Split into sub-routers** and avoid one monolithic 500-procedure router; smaller units re-infer faster.
- **Annotate procedure return types explicitly** so the resolver's output type doesn't have to be inferred through your whole data layer each time.
- **Keep zod schemas reasonable** — a giant `z.discriminatedUnion` with 40 members is expensive to infer everywhere it's used.
- **Reach for codegen when inference stops scaling**: this is a genuine point where OpenAPI/GraphQL's "generate once, read a file" model wins on compiler performance, trading live inference for a build step.

## The one footgun: importing server runtime by accident

Because the server and client live in one repo, it is easy to write `import { appRouter }` (value) instead of `import type { AppRouter }` (type). The value import drags the *entire server runtime* — database drivers, secrets, Node-only modules — into the client bundle, and may even ship credentials to the browser. Always `import type` for the router on the client. Enable `"verbatimModuleSyntax": true` (unit 07) so a stray value-import of a type is a compile error, not a silent bundle bloat.

<Quiz
  id="08-trpc-quiz-1"
  lessonSlug="03-trpc-end-to-end"
  lang="en"
  question="tRPC gives 'end-to-end type safety with no codegen'. What does 'no codegen' require in exchange?"
  choices={[
    { label: "The client must be able to import the server's AppRouter as a TYPE — same TS build/monorepo — because the type itself is the contract and can only cross a compile boundary.", correct: true },
    { label: "Nothing — tRPC works across any languages and separate repos like REST does.", misconception: "There is no wire-format contract to consume from another language or repo. A Swift client or a separately-built service cannot import a TS type, so they need OpenAPI/GraphQL instead." },
    { label: "A runtime schema is sent over the network so the client can rebuild the types.", misconception: "Nothing about the types travels at runtime. The inference is purely compile-time from typeof appRouter; the client shares the type at build, not over the wire." },
    { label: "You must run a generation step, just a faster one than OpenAPI.", misconception: "tRPC generates no files at all — that is the whole point. The client type is recomputed by tsc from the imported router type on every keystroke." },
  ]}
/>

<DragOrder
  id="08-trpc-drag-1"
  pieceSlug="03-trpc-end-to-end"
  lang="en"
  prompt="Order how a server procedure's types reach a typed client call site in tRPC:"
  items={[
    { id: "a", label: "Server builds appRouter; each procedure carries a zod input schema and a resolver return type" },
    { id: "b", label: "Export the router's TYPE: type AppRouter = typeof appRouter" },
    { id: "c", label: "Client imports it with `import type` and passes it: createTRPCClient<AppRouter>" },
    { id: "d", label: "A mapped/conditional type turns the router type into the client interface, inferring each input/output" },
    { id: "e", label: "The call site trpc.x.query(input) is fully typed; renaming a server procedure breaks the client at compile time" },
  ]}
/>

</Explanation>

<KeyTakeaway>
tRPC delivers **end-to-end type safety with no codegen**: `type AppRouter = typeof appRouter` lifts the server router *value* into a *type*, and `createTRPCClient<AppRouter>` runs a large internal mapped/conditional type that turns it into the client's call interface — input types from each procedure's `z.infer`, output types from each resolver's return type, all computed at compile time. "No codegen" is paid for by a hard constraint: the client must **share** the server's type (same TS build / monorepo; TypeScript both ends), so a polyglot or third-party consumer still needs OpenAPI/GraphQL. The running cost is **inference time** — a giant router makes `tsc` and the editor crawl; mitigate by splitting sub-routers, annotating resolver return types, keeping schemas modest, and switching to codegen when live inference stops scaling. The sharp footgun: `import type` the router, never a value import, or you ship the entire server runtime (and secrets) into the browser bundle.
</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="08-trpc-retrieval"
  lang="en"
  questions={[
    { q: "Mechanically, how does the client get its API types in tRPC with no codegen?", a: "The server builds a router value (appRouter) whose TYPE encodes every procedure name, input schema, and resolver return type. You export `type AppRouter = typeof appRouter`, using the typeof type operator to lift the runtime value into a type. The client does `createTRPCClient<AppRouter>(...)`, passing that type as a generic. Internally tRPC has a large mapped/conditional type that walks AppRouter and produces the client call interface: each procedure x.query becomes trpc.x.query(input): Promise<output>, where the input type is the procedure's z.infer of its zod schema and the output type is the resolver's return type. All of this is recomputed by tsc/the language service at compile time — no file is generated, so renaming a server procedure immediately breaks the client's call sites." },
    { q: "What does 'no codegen' force in exchange, and when should you prefer OpenAPI/GraphQL codegen instead?", a: "It forces the client to be able to import the server's AppRouter AS A TYPE. That means same TypeScript build or monorepo (or the server ships a .d.ts the client consumes), and TypeScript on both ends — because the contract IS the type, and a type can only cross a COMPILE boundary, not a network or language boundary. There is no wire-format spec. So a Swift/Kotlin client, a third-party consumer, or a service built separately cannot use tRPC's inference; for those you want OpenAPI or GraphQL, whose codegen produces a real, language-agnostic contract from a spec/schema. You also prefer codegen when tRPC's live inference stops scaling on compiler performance — a generated file is read once instead of re-inferred." },
    { q: "Why does a giant tRPC router slow the editor, and what is the runtime import footgun?", a: "The client's API type is a function of the ENTIRE server router type, recomputed by tsc and the editor's language service on every keystroke — there is no generated file to cache. A router with hundreds of procedures, deep sub-routers, and big zod schemas (e.g. a 40-member discriminatedUnion) makes hovers spin, autocomplete lag, and tsc crawl. Mitigations: split into smaller sub-routers, annotate resolver return types explicitly so output isn't inferred through the whole data layer, keep schemas modest, and move to codegen when inference no longer scales. The footgun: because client and server share a repo, writing `import { appRouter }` (a VALUE import) instead of `import type { AppRouter }` drags the entire server runtime — DB drivers, secrets, Node modules — into the browser bundle. Always import the router as a type, and enable verbatimModuleSyntax so a stray value-import of a type fails to compile." },
  ]}
/>

<Recap lang="en">
You now understand the engine behind tRPC's "no codegen" claim: `typeof appRouter` lifts the router into a type, the client infers its whole API from it at compile time, the price is shared-type coupling plus inference cost, and the footgun is a value import that leaks the server into the browser. You've now seen three points on the boundary-typing spectrum — hand-modelled contracts and Result types, zod runtime validation, and tRPC's compile-time inference. This capstone of *consuming* typed systems sets up the inverse skill: in the next lesson you'll *design* generic library APIs that infer this beautifully for your own callers, and it all comes together in the typed-feature capstone in unit 09.
</Recap>
