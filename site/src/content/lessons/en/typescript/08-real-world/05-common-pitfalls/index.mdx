---
concepts:
  - type-pitfalls
  - branding
  - structural-typing
  - object-keys
  - array-covariance
deepensInto: []
estMin: 15
lang: en
lessonType: topic
level: senior
order: 5
prereqs:
  - "typescript/03-generics/05-variance-intuition"
slug: "05-common-pitfalls"
sources:
  - https://www.typescriptlang.org/docs/handbook/declaration-files/do-s-and-don-ts.html
  - https://www.typescriptlang.org/docs/handbook/2/everyday-types.html
spiral:
  - structural-typing
  - variance
  - narrowing
status: ready
summary: "The senior pitfalls roundup: `as`/`!` hiding bugs, `any` leaks, structural typing letting wrong objects through, `Object.keys` returning `string[]`, array covariance, mutation invalidating narrowing, forgotten `as const`. For each: the symptom, why it compiles, the fix."
title: "Common pitfalls: traps that compile clean and bite later"
track: "typescript"
unit: "08-real-world"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";

<Hook>
Every trap in this lesson has one thing in common: it **compiles clean**. The build is green, the PR is approved, the types look airtight — and then a `userId` gets passed where an `orderId` was expected, a narrowed value is `undefined` two lines later, or `Object.keys` hands you a loop variable you can't index with. These are the bugs that survive review precisely because the type checker said yes. A senior's edge isn't knowing more syntax; it's knowing where the checker's "yes" is *unsound* or *imprecise*, and reaching for the right fix on reflex.
</Hook>

<Crux>The dangerous TypeScript bugs are the ones that compile — unsound by design, imprecise by default, or hidden behind an escape hatch.</Crux>

<Explanation>

## The roundup table

Each row is a trap that passes the compiler. Read it as: symptom, why it slips through, and the reflex fix.

<div data-lesson-visual class="overflow-x-auto my-6">

| Pitfall | Why it compiles | The fix |
| --- | --- | --- |
| `as` hides a real mismatch | A cast asserts a type with no runtime check; the compiler trusts you | Validate at boundaries (zod); reserve `as` for knowledge the compiler truly lacks |
| `!` non-null abuse | `!` erases `null`/`undefined` from the type with no proof | Narrow with a real check (`if (x)`), or fix the type to not include null |
| `any` leak from a lib | `any` is assignable to and from everything; it spreads silently | Type lib boundaries as `unknown` and validate; turn on `noImplicitAny` |
| Structural typing lets wrong IDs through | `UserId` and `OrderId` are both `string` — structurally identical | Brand the type: `string & { __brand: "UserId" }` |
| `Object.keys(o)` is `string[]` | Extra runtime keys could exist, so `keyof` would be unsound | Cast deliberately: `Object.keys(o) as (keyof typeof o)[]` (you accept the risk) |
| Array covariance unsoundness | `Dog[]` is assignable to `Animal[]`, but pushing a `Cat` then breaks it | Accept `readonly T[]` in APIs so callers can't mutate |
| Mutation invalidates narrowing | A narrowed `let`/property can change between check and use | Use `const`, copy to a local, or re-check after any call that could mutate |
| `as const` forgotten | Without it, `{ x: 1 }` widens to `{ x: number }`, literals to their base type | Add `as const` to keep literal types and `readonly` tuples |

</div>

## Branding: defeating structural typing on purpose

Unit 01 taught that TypeScript is structural — two types with the same shape are interchangeable. Usually a feature; here a hazard. `type UserId = string` and `type OrderId = string` are *the same type*, so passing one where the other is wanted compiles. Branding adds a phantom property that exists only in the type:

```ts
type UserId = string & { readonly __brand: "UserId" };
type OrderId = string & { readonly __brand: "OrderId" };

function getUser(id: UserId) { /* ... */ }
const order = "ord_1" as OrderId;

// getUser(order); // Error: 'OrderId' is not assignable to 'UserId'
//   the __brand discriminates them even though both are string at runtime
const user = "usr_1" as UserId;
getUser(user); // ok
```

The `__brand` never exists at runtime (it's `string` underneath), but the *type* now distinguishes the two, so a mixed-up ID is a compile error. This is the standard fix for "structural typing let the wrong primitive through."

## Array covariance: the canonical unsoundness

TypeScript deliberately treats arrays as **covariant** — `Dog[]` is assignable to `Animal[]` — for ergonomics. But arrays are mutable, which makes this unsound:

```ts
const dogs: Dog[] = [new Dog()];
const animals: Animal[] = dogs; // allowed — covariance
animals.push(new Cat());        // compiles! Animal[] accepts a Cat
dogs[1].bark();                 // runtime crash — it's a Cat, no bark()
```

The compiler accepted every line, yet `dogs` now contains a `Cat`. The fix in API design: accept `readonly Animal[]` when you only read, so a caller can't smuggle a `Cat` into someone's `Dog[]`.

<Inset kind="why" lang="en">
Why did TS choose an unsound rule? Pure soundness would forbid `Dog[]` where `Animal[]` is expected, breaking enormous amounts of reasonable read-only code (you pass a `Dog[]` to a function that just iterates `Animal[]`). The team traded a narrow unsoundness — only triggered by *mutation through the widened reference* — for everyday ergonomics. `readonly` arrays restore soundness exactly by removing the mutation that breaks it.
</Inset>

## Mutation invalidates narrowing

Narrowing (unit 02) is only valid until the value can change. A mutable binding or property narrowed in an `if` can be reset by any intervening call the compiler can't see through:

```ts
function f(box: { v: string | null }) {
  if (box.v !== null) {
    sideEffect();        // could set box.v = null — compiler assumes it didn't
    box.v.toUpperCase(); // ^? string  — but might throw at runtime
  }
}
```

The compiler narrows `box.v` to `string` inside the `if` and *keeps* that assumption across `sideEffect()`, because it can't prove the call left `box.v` alone. The fix: copy to a `const` local immediately after the check (`const v = box.v;`) so the narrowed value can't be mutated out from under you.

<Quiz
  id="08-pit-quiz-1"
  lessonSlug="05-common-pitfalls"
  lang="en"
  question="`const animals: Animal[] = dogs` (where dogs is Dog[]) compiles, then `animals.push(new Cat())` also compiles and corrupts dogs. Why does TypeScript allow this?"
  choices={[
    { label: "Arrays are covariant by design — Dog[] is assignable to Animal[] for ergonomics — and that rule is deliberately unsound under mutation; readonly arrays fix it.", correct: true },
    { label: "It is a TypeScript bug that will be fixed in a future release.", misconception: "It is an intentional, documented trade-off: full soundness would reject too much reasonable read-only code. The escape is to accept readonly T[] when you only read." },
    { label: "Dog and Cat are structurally identical so the push is actually type-safe.", misconception: "They are different types; the push is genuinely unsafe. It compiles only because the array was widened to Animal[] via covariance, which permits pushing any Animal." },
    { label: "The push is rejected at compile time, so there is no real problem.", misconception: "It compiles. animals is typed Animal[], and Cat is an Animal, so push is allowed — yet the same backing array is dogs, which now holds a Cat." },
  ]}
/>

<DragOrder
  id="08-pit-drag-1"
  pieceSlug="05-common-pitfalls"
  lang="en"
  prompt="Order the diagnosis-to-fix workflow for a bug that compiled clean but crashed at runtime:"
  items={[
    { id: "a", label: "Symptom: a runtime crash on code the type checker approved" },
    { id: "b", label: "Locate the unsound or imprecise spot: an as/! escape hatch, covariance, a mutation, or a structural collision" },
    { id: "c", label: "Explain WHY it compiled — which guarantee TS gave up (or never had) at that line" },
    { id: "d", label: "Apply the reflex fix: validate, brand, use readonly/const, or add as const" },
    { id: "e", label: "Add a guardrail (lint rule, stricter flag) so the same class of bug can't recur" },
  ]}
/>

</Explanation>

<KeyTakeaway>
The TypeScript bugs that bite real codebases are the ones that **compile clean** — they are unsound by design, imprecise by default, or hidden behind an escape hatch. `as` and `!` assert types with no proof (validate at boundaries; narrow with real checks); `any` from a library spreads silently (type boundaries as `unknown`). **Structural typing** treats `UserId` and `OrderId` (both `string`) as interchangeable — **brand** them with a phantom `& { __brand }` to discriminate at compile time while staying `string` at runtime. **`Object.keys` returns `string[]`**, not `keyof`, because extra runtime keys could exist; cast deliberately if you accept that. **Array covariance** (`Dog[]` assignable to `Animal[]`) is an intentional unsoundness that breaks under `push` — accept `readonly T[]` in read-only APIs. **Mutation invalidates narrowing**: copy a narrowed value to a `const` local before any call that could change it. And a forgotten **`as const`** silently widens literals. For each: know the symptom, know *why* it compiled, and reach for the fix on reflex.
</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="08-pit-retrieval"
  lang="en"
  questions={[
    { q: "Why is branding needed, and how does `string & { __brand: 'UserId' }` work without affecting runtime?", a: "TypeScript is structural: two types with the same shape are interchangeable. So `type UserId = string` and `type OrderId = string` are literally the same type, and passing a UserId where an OrderId is wanted compiles — a real bug the checker can't catch. Branding intersects the primitive with a phantom property that exists only in the type system: `type UserId = string & { readonly __brand: 'UserId' }`. The intersection makes UserId NOT assignable to OrderId (their __brand literals differ), so a mixed-up ID is a compile error. At runtime the value is just a string — the __brand property is never created; you mint a branded value with a deliberate cast (`'usr_1' as UserId`) at the one place you trust it. It's the standard fix for structural collisions among primitives (IDs, units, tokens)." },
    { q: "Explain array covariance unsoundness and the readonly fix.", a: "TypeScript deliberately makes arrays covariant: Dog[] is assignable to Animal[], for ergonomics (you want to pass a Dog[] to a function that iterates Animal[]). But arrays are mutable, so this is unsound: const animals: Animal[] = dogs; animals.push(new Cat()); compiles — animals is Animal[] and Cat is an Animal — yet animals and dogs are the SAME backing array, so dogs now contains a Cat and dogs[1].bark() crashes. Every line passed the checker. The team accepted this narrow unsoundness (only triggered by mutating through the widened reference) for everyday convenience. The fix in API design is to accept readonly Animal[] when you only read: a readonly array has no push, so a caller can't smuggle a Cat into someone's Dog[], restoring soundness exactly where it was lost." },
    { q: "Why can mutation invalidate a narrowing, and what is the reflex fix; also why is Object.keys typed string[]?", a: "Narrowing holds only while the value can't change. If you narrow box.v to string in an if, then call sideEffect(), the compiler KEEPS the string assumption across the call because it can't prove the call left box.v alone — but sideEffect() might have set box.v = null, so box.v.toUpperCase() can throw at runtime despite compiling. Reflex fix: immediately copy the narrowed value to a const local (const v = box.v) so nothing can mutate it out from under the narrowing; or re-check after any call that could mutate. Separately, Object.keys(o) is typed string[], not (keyof typeof o)[], on purpose: at runtime an object can carry extra own-enumerable keys beyond those in its static type (it could be a wider object assigned to a narrower type), so treating the keys as keyof would be unsound. If you know your object has exactly its declared keys, cast deliberately: Object.keys(o) as (keyof typeof o)[], accepting the risk." },
  ]}
/>

<Recap lang="en">
You can now name the senior pitfalls on sight — `as`/`!` escapes, `any` leaks, structural ID collisions, `Object.keys` widening, array covariance, mutation-broken narrowing, forgotten `as const` — and for each you know the symptom, why it compiled, and the reflex fix (validate, brand, `readonly`/`const`, deliberate casts). That closes the real-world unit: you can type the messy boundaries of real systems, consume them safely with zod and tRPC, design library APIs that infer well, and spot the traps that compile clean. Everything now converges in the typed-feature capstone in unit 09, where you build one end-to-end feature applying all of it at once. Now when you see a green build on a PR that passes a raw `string` ID into a function expecting a different kind of ID, you know the structural collision it hides — and you reach for a brand before approving.
</Recap>
