infographic hierarchy-tree-curved-line-rounded-rect-node
data
  title Bugs that compile clean: three root causes
  desc The eight traps map onto three reasons the checker still says yes
  root
    label Compiles clean, bites later
    children
      - label Escape hatch — no proof
        children
          - label as — cast, no runtime check
          - label ! non-null — erases null, no proof
          - label any — leaks in from libs
      - label Unsound by design
        children
          - label Structural ID collision
          - label Array covariance + push
          - label Mutation breaks narrowing
      - label Imprecise by default
        children
          - label Object.keys is string[]
          - label Forgotten as const widens
theme
  palette #6366f1 #14b8a6 #f59e0b
