---
concepts:
  - type-inference
  - widening
  - contextual-typing
  - best-common-type
  - const-assertion
  - return-type-inference
deepensInto:
  - "typescript/01-foundations/04-literal-types"
estMin: 13
lang: en
lessonType: topic
level: junior
order: 2
prereqs:
  - "typescript/01-foundations/01-structural-typing"
slug: "02-inference-basics"
sources:
  - https://www.typescriptlang.org/docs/handbook/type-inference.html
  - https://www.typescriptlang.org/docs/handbook/2/everyday-types.html
spiral:
  - inference
  - type-safety
status: ready
summary: "TypeScript infers most types so you don't annotate them. The rules — initializer typing, const-vs-let widening, contextual typing of callbacks, and best-common-type for arrays — are predictable once you see them. Annotate boundaries; infer locals."
title: "Inference basics: how the checker fills in the types you didn't write"
track: "typescript"
unit: "01-foundations"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import inferenceCost from "./infographics/inference-cost.svg?raw";

<Hook>
You add `: string` to a local variable and your reviewer leaves a comment: "redundant — TS already knows." Then on the next line you write `const handler = items.map(x => x.toUpperCase())` and the callback parameter `x` is typed correctly *with no annotation at all*. TypeScript isn't guessing; it's running a small, deterministic inference algorithm. Learn its rules and you'll annotate exactly where it pays off and nowhere else.
</Hook>

<Crux>Inference flows from initializers and context; `const` keeps literals, `let` widens them, and callbacks get param types from the expected type.</Crux>

<Explanation>

## Inference from initializers

When you declare a variable with a value, TypeScript infers its type from the right-hand side. But the *exact* type depends on the binding keyword, because of **widening**:

```ts
let a = "hello";
//  ^? let a: string  — `let` is mutable, so the literal widens to its base type

const b = "hello";
//    ^? const b: "hello"  — `const` can never change, so the literal type is kept
```

This is the single most important inference rule to internalize. A `let` can be reassigned, so pinning it to the literal `"hello"` would be useless — TS widens to `string`. A `const` is immutable, so the precise literal type `"hello"` is both safe and more informative. Same for numbers and booleans: `let n = 3` is `number`, `const n = 3` is `3`.

## Object properties widen — even under const

Here's the surprise that bites everyone. `const` on the *binding* doesn't make the *contents* immutable, because object properties are mutable:

```ts
const config = { mode: "dark", retries: 3 };
//    ^? const config: { mode: string; retries: number }
//       NOT { mode: "dark"; retries: 3 } — properties widen
config.mode = "light"; // legal — the property is mutable, so its type is widened `string`
```

The `const` only freezes the *reference* `config`, not `config.mode`. Since you could reassign the property, TS widens it to `string`. When you actually need the literal types preserved — say, to pass into a function expecting a union — use a `const` **assertion**:

```ts
const config = { mode: "dark", retries: 3 } as const;
//    ^? const config: { readonly mode: "dark"; readonly retries: 3 }
config.mode = "light";
// Error: Cannot assign to 'mode' because it is a read-only property.
```

`as const` makes every property `readonly` and keeps its literal type. We go deep on this in the literal-types lesson; for now, know it's the fix when widening throws away information you needed.

## Contextual typing: callbacks get types for free

Inference also flows *inward* from the expected type. When a function expects a callback, the callback's parameters are typed from that expectation — you don't annotate them:

```ts
const names = ["ada", "alan", "grace"];
const upper = names.map((n) => n.toUpperCase());
//                       ^? (parameter) n: string
//    ^? const upper: string[]
```

`n` is `string` because `Array<string>.map` declares its callback as `(value: string, ...) => U`. This is **contextual typing**: the position the function literal sits in supplies the parameter types. It's why DOM handlers work without annotation too:

```ts
button.addEventListener("click", (e) => {
  //                                ^? (parameter) e: MouseEvent
  console.log(e.clientX);
});
```

The string `"click"` selects the overload whose handler takes a `MouseEvent`, and that flows into `e`. Annotating `e: MouseEvent` here is redundant noise.

<Inset kind="why" lang="en">
Contextual typing only works when the expected type is visible at the point the function literal is written. Pull the callback out into a standalone `const fn = (e) => ...` and there's no context anymore — `e` falls back to the implicit `any` (an error under `noImplicitAny`). That's the trade: inline callbacks get free typing; extracted ones need an explicit parameter annotation or a typed alias.
</Inset>

## Best-common-type for array literals

When you write an array literal, TypeScript computes a single element type — the **best common type** — that all elements satisfy:

```ts
const xs = [1, 2, 3];
//    ^? const xs: number[]

const mixed = [1, "two", 3];
//    ^? const mixed: (string | number)[]  — union of the element types
```

If the elements have no common supertype already in scope, TS forms a union of them. Note that `const` on the binding does *not* keep `[1,2,3]` as a tuple `[number, number, number]` — it's still `number[]`; only `as const` produces a `readonly [1, 2, 3]` tuple (covered in the literal-types lesson).

## Return types are inferred too

You almost never annotate a function's return type for internal helpers — TS infers it from the `return` statements:

```ts
function parsePort(raw: string) {
  const n = Number(raw);
  return Number.isInteger(n) ? n : null;
}
//       ^? function parsePort(raw: string): number | null
```

The inferred return is `number | null` from the two branches. This is great for locals — but a sharp tool at API boundaries.

## When to annotate vs let it infer

This is the question every TypeScript codebase eventually forces: over-annotate and you're fighting the checker; under-annotate and you lose the contract. Here's the rule professional codebases converge on:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Situation</th>
        <th class="border border-border px-3 py-2 text-left">Annotate?</th>
        <th class="border border-border px-3 py-2 text-left">Why</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Local variable with initializer</td>
        <td class="border border-border px-3 py-2">No</td>
        <td class="border border-border px-3 py-2">Inference is accurate and stays in sync with the value</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Inline callback parameter</td>
        <td class="border border-border px-3 py-2">No</td>
        <td class="border border-border px-3 py-2">Contextual typing supplies it</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Exported function return type</td>
        <td class="border border-border px-3 py-2">Yes</td>
        <td class="border border-border px-3 py-2">Locks the public contract; stops an internal change silently widening the API</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Function parameters</td>
        <td class="border border-border px-3 py-2">Yes</td>
        <td class="border border-border px-3 py-2">Parameters are an input boundary — inference can't see callers</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Empty `let x;` then assigned later</td>
        <td class="border border-border px-3 py-2">Yes</td>
        <td class="border border-border px-3 py-2">Bare `let` infers `any`; annotate to keep it checked</td>
      </tr>
    </tbody>
  </table>
</div>

The principle: **annotate boundaries, infer interiors.** Explicit return types on exported functions are the highest-value annotations — they make the contract intentional and catch accidental widening when you refactor the body.

<Inset kind="why" lang="en">
Where inference *hurts*, with numbers. Inference is free where it's local, but at module boundaries it has a real, measurable cost. When an exported function has no return annotation, `tsc` must re-derive that return type for every file that imports it, and complex inferred returns (deep generics, conditional types, long unions) are the classic driver of slow builds. On a ~900-file React + tRPC repo (TS 5.5), one over-clever helper whose return type inferred to a 30-member conditional-mapped type pushed `tsc --noEmit` from 9.4s to 16.1s; `--extendedDiagnostics` showed "Check time" nearly doubled and instantiation count jumped ~3.4M against the 100k-per-check / 5M-relation ceilings, with IntelliSense hover on the call sites lagging ~600ms+ as the editor recomputed the type. Pinning a hand-written return type cut it back to ~9.6s. The senior tradeoff: at internal call sites, *let it infer* — annotations there are noise that drifts out of sync; at exported boundaries, *pay the one annotation* — you trade three keystrokes for a stable public contract, faster incremental checks, and snappier editor latency for every consumer.
</Inset>

<Infographic
  svg={inferenceCost}
  label="Column chart of tsc --noEmit time on a 900-file repo: 9.4s at baseline, 16.1s when one exported helper's return type is left to infer as a 30-member conditional type, and 9.6s after pinning an explicit return type."
  caption="Leaving an over-clever exported return inferred nearly doubled the type-check; one hand-written return annotation restored the baseline. Annotate boundaries, infer interiors."
  hue="var(--d-infra)"
/>

<FlowDiagram
  label="Inference decision path: how a declaration's type is chosen from its keyword, value, and surrounding context."
  hue="var(--d-frontend)"
  perRow={3}
  nodes={[
    { id: "decl", label: "Declaration", sub: "let/const + value" },
    { id: "ctx", label: "Has context?", sub: "expected type?" },
    { id: "use", label: "Use context", sub: "callback params" },
    { id: "kw", label: "Which keyword?", sub: "const vs let" },
    { id: "keep", label: "Keep literal", sub: "const -> a" },
    { id: "widen", label: "Widen", sub: "let -> string" },
  ]}
  edges={[
    { from: "decl", to: "ctx" },
    { from: "ctx", to: "use", label: "yes" },
    { from: "ctx", to: "kw", label: "no" },
    { from: "kw", to: "keep", label: "const" },
    { from: "kw", to: "widen", label: "let" },
  ]}
  caption="Contextual typing wins when an expected type is present; otherwise the keyword decides whether the literal is kept (const) or widened (let)."
/>

<Quiz
  id="02-infer-quiz-1"
  lessonSlug="02-inference-basics"
  lang="en"
  question="What is the inferred type of `mode` in `const config = { mode: 'dark' }` (no `as const`)?"
  choices={[
    { label: "string — object properties widen to their base type because they are mutable", correct: true },
    { label: "'dark' — `const` always preserves the literal type", misconception: "`const` preserves the literal only for the *binding* (a primitive const like `const x = 'dark'`). Object *properties* are mutable, so they widen to `string` unless you add `as const`." },
    { label: "readonly 'dark' — `const` makes properties readonly", misconception: "`const` freezes the reference `config`, not its members. Properties become readonly only with an `as const` assertion." },
    { label: "any — TypeScript can't infer object property types", misconception: "TS infers property types precisely; here it infers `string` via widening, not `any`." },
  ]}
/>

<MetaphorComplete
  id="02-infer-metaphor-1"
  pieceSlug="02-inference-basics"
  lang="en"
  setup="A callback parameter that gets its type from the function it's passed into — without any annotation — is being typed by ____ typing."
  accepted={["contextual", "contextual typing", "context"]}
  canonical="contextual"
  explanation="Contextual typing flows the expected type inward: the position the function literal occupies (e.g. the argument to Array.map or addEventListener) supplies the parameter types, so you don't annotate them."
/>

</Explanation>

<KeyTakeaway>
TypeScript infers from initializers, and the binding keyword decides widening: `const x = "a"` keeps the literal `"a"`, `let x = "a"` widens to `string`. Object *properties* widen even under `const` (use `as const` to keep them). Contextual typing flows the expected type into callback parameters, so inline callbacks need no annotations. Array literals get a best-common-type (a union if needed); return types are inferred from the branches. Practically: annotate boundaries — function parameters and exported return types — and let inference handle locals.
</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-infer-retrieval"
  lang="en"
  questions={[
    { q: "Explain const-vs-let widening, and why object properties widen even when the binding is const.", a: "When you initialize a variable, TS infers its type from the value, but widening depends on mutability. A `let` can be reassigned, so a literal initializer is widened to its base type: `let x = 'a'` is `string`, `let n = 3` is `number`. A `const` binding can never change, so the precise literal type is kept and is safe: `const x = 'a'` is `'a'`, `const n = 3` is `3`. Object properties are a separate story: `const` freezes only the *reference*, not the members — `config.mode = ...` is still legal — so each property is treated as mutable and widened. Thus `const config = { mode: 'dark' }` infers `{ mode: string }`, not `{ mode: 'dark' }`. To keep property literals (and make them readonly), use an `as const` assertion." },
    { q: "What is contextual typing, where does it apply, and when does it stop working?", a: "Contextual typing is inference flowing *inward* from an expected type to a function literal's parameters. When a function expects a callback — `Array<string>.map((value: string) => U)`, `addEventListener('click', handler)` — the parameter types come from that expectation, so you don't annotate them: `names.map(n => ...)` types `n` as `string`, and a click handler's `e` is `MouseEvent`. It works only when the expected type is visible at the point the function literal is written. If you extract the callback into a standalone `const fn = (e) => ...`, the context is gone and `e` becomes implicit `any` (an error under noImplicitAny) — you must then annotate the parameter or give the const a function type." },
    { q: "What is the practical annotation policy — what should you annotate and what should you leave to inference, and why?", a: "Annotate boundaries; infer interiors. Leave to inference: local variables with initializers (the inferred type stays in sync with the value and is accurate) and inline callback parameters (contextual typing supplies them). Annotate explicitly: function parameters (they're an input boundary — inference can't see callers, and bare params would be implicit any), and the return types of *exported* functions. An explicit return type on a public function is the highest-value annotation: it makes the contract intentional and prevents an internal edit from silently widening or changing the API surface. Also annotate a bare `let x;` declared before assignment, since it otherwise infers `any`. Over-annotating locals is just noise that drifts out of sync." },
  ]}
/>

<Recap lang="en">
Inference is why TypeScript stays out of your way: shapes you learned to reason about structurally in the last lesson are mostly filled in for you, with widening and contextual typing doing the heavy lifting. The widening rules you just saw — `const` keeping `"a"`, `let` widening to `string`, properties widening, `as const` as the escape hatch — are exactly the machinery the next lesson on **literal types** builds on. But first we detour through the three special types — **`any`, `unknown`, and `never`** — because knowing how inference and assignability behave at the top and bottom of the type lattice is what keeps `any` from quietly poisoning everything inference just gave you. Now when you see a reviewer comment "redundant annotation", check whether inference already covers that slot — and when you see a slow `tsc`, check whether an exported function is missing its return type.
</Recap>
