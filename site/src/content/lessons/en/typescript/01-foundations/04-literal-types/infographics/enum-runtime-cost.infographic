infographic chart-column-simple
data
  title Runtime JS shipped per construct (bytes)
  desc A literal union erases to nothing; each numeric enum emits a reverse-mapped object — about 150–400 bytes
  values
    - label Literal union
      value 0
    - label enum (best case)
      value 150
    - label enum (worst case)
      value 400
theme
  palette #14b8a6 #f59e0b #f43f5e
