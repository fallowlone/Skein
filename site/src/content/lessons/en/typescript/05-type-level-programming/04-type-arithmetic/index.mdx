---
concepts:
- tuple-length-arithmetic
- type-level-add-subtract
- type-level-compare
- template-literal-number-parse
- bounded-recursion
deepensInto:
- typescript/05-type-level-programming/05-limits-and-perf
estMin: 18
lang: en
lessonType: topic
level: senior
order: 4
prereqs:
- typescript/05-type-level-programming/01-recursion-in-types
slug: "04-type-arithmetic"
sources:
- https://github.com/type-challenges/type-challenges
- https://www.typescriptlang.org/docs/handbook/2/conditional-types.html
spiral:
- recursion
- tuples
status: ready
summary: TypeScript has no type-level numbers, so you encode N as a tuple of length N — concatenate to add, infer-off the front to subtract, and compare lengths to order — bounded and slow by construction.
title: Type-level arithmetic
track: typescript
unit: "05-type-level-programming"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import tupleOps from "./infographics/tuple-ops.svg?raw";

<Hook>
You are typing a fixed-size matrix library: `Vec<3>` plus `Vec<3>` is fine, but `Vec<3>` plus `Vec<4>` must be a compile error, and `dot(Vec<3>, Vec<3>)` should return a `number` only when the lengths match. To express "3 + 4" or "is 3 less than 4?" the compiler needs arithmetic — but TypeScript has no type-level `+`. The trick the entire type-challenges arithmetic canon is built on: a number `N` is represented as a tuple of length `N`, and you do math by growing, shrinking, and measuring tuples. It works up to about a thousand — and it is gloriously slow.
</Hook>

<Crux>With no type-level numbers, encode `N` as a tuple of length `N`: add by concatenating, subtract by inferring off the front.</Crux>

<Explanation>

## The representation: a number is a tuple's length

Ask yourself: if you can't write `3 + 4` as a type expression, how do you enforce at compile time that two vectors have the same dimension? The whole trick fits in one insight. There is no `+` operator at the type level. The standard encoding is: the number `N` is *any* tuple with `length` equal to `N`. Build one by tail-recursively pushing `unknown` until the length matches:

```ts
type BuildTuple<N extends number, Acc extends unknown[] = []> =
  Acc["length"] extends N ? Acc : BuildTuple<N, [unknown, ...Acc]>;

type Three = BuildTuple<3>;
//   ^? type Three = [unknown, unknown, unknown]
type Len = Three["length"];
//   ^? type Len = 3
```

This is exactly the tail-recursive `Build` from lesson 1. The whole arithmetic library reduces to: convert numbers to tuples, manipulate the tuples, read `["length"]` back out.

<FlowDiagram
  label="BuildTuple<3>: push unknown onto the accumulator until its length reaches the target, then read the length back as the number."
  hue="var(--accent)"
  perRow={3}
  nodes={[
    { id: "n0", label: "Acc=[]", sub: "length 0" },
    { id: "n1", label: "Acc=[u]", sub: "length 1" },
    { id: "n2", label: "Acc=[u,u]", sub: "length 2" },
    { id: "n3", label: "Acc=[u,u,u]", sub: "length 3 = N" },
    { id: "n4", label: "read length", sub: "-> 3" },
  ]}
  edges={[
    { from: "n0", to: "n1", label: "push" },
    { from: "n1", to: "n2", label: "push" },
    { from: "n2", to: "n3", label: "push" },
    { from: "n3", to: "n4", label: "stop, measure" },
  ]}
  caption="Each tail-recursive step grows the accumulator by one element until Acc['length'] equals N; the tuple then encodes the number, and ['length'] decodes it."
/>

## Add: concatenate the two tuples

Adding `A + B` is building both tuples, spreading them into one, and reading the combined length:

```ts
type Add<A extends number, B extends number> =
  [...BuildTuple<A>, ...BuildTuple<B>]["length"] & number;

type Seven = Add<3, 4>;
//   ^? type Seven = 7
```

The `& number` narrows `["length"]` (which is typed as `number` in general) back to the literal. Concatenation is the type-level `+`.

## Subtract: infer the difference off the front

Subtracting `A - B` builds the tuple for `A`, then strips `B` elements from the front by matching a prefix of `BuildTuple<B>` and inferring the rest:

```ts
type Subtract<A extends number, B extends number> =
  BuildTuple<A> extends [...BuildTuple<B>, ...infer Rest]
    ? Rest["length"]
    : never;   // A < B: no such prefix, underflow -> never

type Two = Subtract<5, 3>;
//   ^? type Two = 2
type Under = Subtract<3, 5>;
//   ^? type Under = never   (negative numbers are unrepresentable)
```

The pattern `[...BuildTuple<B>, ...infer Rest]` says "a tuple that starts with B elements, with Rest being whatever follows." `Rest["length"]` is `A - B`. There is no representation for negatives, so underflow falls through to `never` — a deliberate signal, not a bug.

## Compare: race the lengths

`GreaterThan<A, B>` walks both tuples down in lockstep; whichever empties first is smaller:

```ts
type GreaterThan<A extends number, B extends number> =
  BuildTuple<B> extends [...BuildTuple<A>, ...infer _Rest]
    ? false        // B is at least as long as A  ->  A <= B
    : true;        // B is NOT a prefix-superset of A  ->  A > B

type G1 = GreaterThan<5, 3>;   // ^? type G1 = true
type G2 = GreaterThan<3, 5>;   // ^? type G2 = false
type G3 = GreaterThan<4, 4>;   // ^? type G3 = false
```

## Parsing numeric literals from strings

When you write a router type that extracts `:42` from a path string, you need a numeric type back, not a string. `${number}` in a template literal matches a numeric string, but the captured `infer` is still a *string*. To get a numeric *type* back, infer it then check `extends number` after a coercion through another template:

```ts
type ParseInt<S extends string> =
  S extends `${infer N extends number}` ? N : never;

type P = ParseInt<"42">;
//   ^? type P = 42   (the `infer N extends number` does the string->number type coercion)
```

`infer N extends number` (TS 4.8+) both captures and coerces: it tries to interpret the matched substring as a numeric literal type. This is how router/path types pull `:42`-style segments into numbers.

<Infographic
  svg={tupleOps}
  label="Taxonomy tree: the root 'a number N is a tuple of length N' branches into four operations — Add concatenates two tuples, Subtract infers a prefix off the front, GreaterThan races the tuple lengths, and ParseInt coerces a numeric string — showing every type-level arithmetic operation reduces to growing, shrinking, or measuring tuples."
  caption="One encoding, four operations: Add, Subtract, GreaterThan and ParseInt are all just tuple manipulation — which is why it stays correct for small fixed sizes but is bounded near the ~1000 recursion limit and slow."
  hue="var(--d-infra)"
/>

<Inset kind="why" lang="en">
Why is this bounded and slow? Every number is materialized as an actual tuple of that length, and `BuildTuple<N>` runs N tail-recursive steps. `Add<400, 400>` builds two 400-element tuples and concatenates an 800-element one — and the compiler does this on every reference, every keystroke in your editor. The tuple-length ceiling sits near the recursion limit (~1000 with tail calls), so arithmetic above a few hundred either errors with "excessively deep" or makes type checking visibly lag. This is a correctness tool for small fixed sizes, not a general calculator.
</Inset>

<Quiz
  id="04-arith-quiz-1"
  lessonSlug="04-type-arithmetic"
  lang="en"
  question="Why does `Subtract<3, 5>` resolve to `never` rather than `-2`?"
  choices={[
    { label: "Numbers are encoded as tuple lengths and there is no tuple of negative length; BuildTuple<3> cannot match the prefix pattern [...BuildTuple<5>, ...infer Rest], so the conditional falls to its `never` branch — underflow is unrepresentable.", correct: true },
    { label: "TypeScript rounds negative results to never automatically", misconception: "There is no automatic rounding; the never comes from the prefix match failing, because a 3-tuple has no 5-element prefix." },
    { label: "Subtraction is not possible at the type level", misconception: "Subtraction works for A >= B via infer-off-the-front; only the negative (underflow) case yields never." },
    { label: "The `& number` intersection produces never", misconception: "& number narrows length to a literal; it is not what produces never here — the failed prefix match is." },
  ]}
/>

<DragOrder
  id="04-arith-drag-1"
  pieceSlug="04-type-arithmetic"
  lang="en"
  prompt="Order the evaluation of Add<2, 3> from number encoding to the final numeric type."
  items={[
    "BuildTuple<2> -> [unknown, unknown]",
    "BuildTuple<3> -> [unknown, unknown, unknown]",
    "Spread both into one tuple -> [unknown, unknown, unknown, unknown, unknown]",
    "Read ['length'] of the combined tuple -> 5",
    "Narrow with & number to the literal type 5",
  ]}
/>

</Explanation>

<KeyTakeaway>With no type-level numbers, encode `N` as a tuple of length `N`. Add by concatenating two tuples and reading `["length"]`; subtract by matching `[...BuildTuple<B>, ...infer Rest]` and reading `Rest["length"]` (underflow yields `never` — negatives are unrepresentable); compare by checking whether one tuple is a prefix-superset of the other. Parse numeric literals with `` `${infer N extends number}` ``. It is correct for small fixed sizes but bounded near the ~1000 recursion limit and slow — every number is a real tuple the compiler materializes on every reference.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-arith-retrieval"
  lang="en"
  questions={[
    { q: "How are numbers represented at the type level, and how do you implement Add and Subtract on that representation?", a: "A number N is represented as a tuple whose length is N (the elements are placeholders, typically `unknown`). You build one tail-recursively: type BuildTuple<N, Acc = []> = Acc['length'] extends N ? Acc : BuildTuple<N, [unknown, ...Acc]>. Add<A,B> = concatenate the two tuples and read the length: [...BuildTuple<A>, ...BuildTuple<B>]['length'] & number — concatenation IS addition because lengths add. Subtract<A,B> = strip B elements off the front by inferring the remainder: BuildTuple<A> extends [...BuildTuple<B>, ...infer Rest] ? Rest['length'] : never. The infer captures everything after the first B elements; its length is A - B. When A < B the prefix cannot match and it resolves to never, because negative lengths are unrepresentable." },
    { q: "How do you compare two type-level numbers, and how do you parse a numeric literal out of a string?", a: "Comparison races the tuple lengths via prefix matching. GreaterThan<A,B> = BuildTuple<B> extends [...BuildTuple<A>, ...infer _] ? false : true — if B's tuple starts with A's tuple as a prefix then B >= A (so A is not greater), otherwise A > B. Equal numbers give false (>= holds). Parsing uses a template literal with constrained infer: type ParseInt<S> = S extends `${infer N extends number}` ? N : never. The `infer N extends number` (TS 4.8+) both matches the numeric substring AND coerces it from a string to a numeric literal type — without the `extends number` constraint you would only get a string back, not a usable number type." },
    { q: "Why is tuple-length arithmetic both bounded and slow, and what is it actually good for?", a: "Bounded: each number is materialized as an actual tuple of that length, so the largest number you can build is capped by the recursion/instantiation limit — roughly 1000 with tail-recursion elimination, far less for naive recursion. Going past it triggers 'Type instantiation is excessively deep and possibly infinite.' Slow: BuildTuple<N> performs N recursive instantiations, and operations like Add<400,400> materialize and concatenate hundreds of tuple elements — the compiler re-evaluates these on every reference, so heavy type arithmetic produces visible editor lag and slow tsc. It is the right tool ONLY for small fixed sizes: validating array arity, typing fixed-length vectors/matrices, enforcing tuple lengths in an API. For anything that needs real numeric range, do the check at runtime instead of the type level." },
  ]}
/>

<Recap lang="en">
Type-level arithmetic has one core idea: since TypeScript has no type-level numbers, represent `N` as a tuple of length `N`. BuildTuple grows the tuple tail-recursively; Add concatenates and reads `["length"]`; Subtract infers the remainder after a B-length prefix (underflow to `never`); GreaterThan checks prefix-superset relationships; and `` `${infer N extends number}` `` parses numeric literals out of strings. The whole scheme is correct for small fixed sizes and useful for typed fixed-length APIs and array-arity validation — but it is bounded near the ~1000 recursion limit and materializes real tuples the compiler re-evaluates constantly. Now when you see `Vec<3>` and `Vec<4>` failing to add, you know the compiler is counting tuple elements — and when it slows down, you know why.
</Recap>
