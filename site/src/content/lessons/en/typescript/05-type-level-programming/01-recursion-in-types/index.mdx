---
concepts:
- recursive-conditional-types
- tuple-recursion
- tail-recursion-elimination
- instantiation-depth
- base-case
deepensInto:
- typescript/05-type-level-programming/04-type-arithmetic
estMin: 16
lang: en
lessonType: topic
level: senior
order: 1
prereqs:
- typescript/04-type-system-deep/02-the-infer-keyword
slug: "01-recursion-in-types"
sources:
- https://www.typescriptlang.org/docs/handbook/2/conditional-types.html#inferring-within-conditional-types
- https://www.typescriptlang.org/docs/handbook/release-notes/typescript-4-5.html#tail-recursion-elimination-on-conditional-types
- https://github.com/type-challenges/type-challenges
spiral:
- recursion
- conditional-types
status: ready
summary: A recursive conditional type peels one element off a tuple, recurses on the rest, and stops at a base case — like a runtime recursion, but the compiler is the interpreter and depth is finite.
title: Recursion in types
track: typescript
unit: "05-type-level-programming"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import depthWall from "./infographics/depth-wall.svg?raw";

<Hook>
You are writing the types for a tiny router library. The path `"/users/:id/posts/:slug"` should yield `{ id: string; slug: string }` — automatically, at compile time, with no codegen step. To get there you need the type system to walk a structure one element at a time and accumulate results. That is recursion, and the compiler is your interpreter. It will happily run your type program — right up until it prints `Type instantiation is excessively deep and possibly infinite.` and gives up. Knowing where that wall sits, and how TypeScript 4.5 moved it, is the difference between a clever type and a shipped one.
</Hook>

<Crux>A recursive type peels one element with `[infer H, ...infer T]`, recurses on the rest, stops at a base case — and depth is bounded.</Crux>

In ten minutes you will know exactly why the compiler gives up and how to push that wall from 50 levels to 1000.

<Explanation>

## The shape of every recursive type

A recursive conditional type has exactly two arms, just like a well-formed recursive function:

- a **base case** that returns a concrete result without recursing, and
- a **recursive case** that does a little work and refers to itself on a smaller input.

The "smaller input" is almost always a tuple with its head split off. The real lever is `[infer Head, ...infer Tail]`. In a conditional type's `extends` clause this pattern matches a non-empty tuple, binds the first element to `Head` and *everything after it* to `Tail`. The base case is the empty tuple `[]`, which fails the `extends` check and falls through to the `else` arm.

## Reverse: the canonical walk

```ts
type Reverse<T extends readonly unknown[]> =
  T extends readonly [infer Head, ...infer Tail]
    ? [...Reverse<Tail>, Head]
    : [];

type R = Reverse<[1, 2, 3]>;
//   ^? type R = [3, 2, 1]
```

Read it as a trace. `Reverse<[1,2,3]>` matches, `Head = 1`, `Tail = [2,3]`, so it becomes `[...Reverse<[2,3]>, 1]`. That recurses to `[...Reverse<[3]>, 2, 1]`, then `[...Reverse<[]>, 3, 2, 1]`, and `Reverse<[]>` hits the base case `[]`. The accumulated spreads collapse to `[3, 2, 1]`.

<FlowDiagram
  label="Reverse unrolling: each step splits [Head, ...Tail] and pushes Head behind the recursive result until the empty-tuple base case."
  hue="var(--accent)"
  perRow={3}
  nodes={[
    { id: "n0", label: "Reverse<[1,2,3]>", sub: "H=1 T=[2,3]" },
    { id: "n1", label: "Reverse<[2,3]>", sub: "H=2 T=[3]" },
    { id: "n2", label: "Reverse<[3]>", sub: "H=3 T=[]" },
    { id: "n3", label: "Reverse<[]>", sub: "base -> []" },
    { id: "n4", label: "collapse", sub: "[3,2,1]" },
  ]}
  edges={[
    { from: "n0", to: "n1", label: "peel 1" },
    { from: "n1", to: "n2", label: "peel 2" },
    { from: "n2", to: "n3", label: "peel 3" },
    { from: "n3", to: "n4", label: "unwind" },
  ]}
  caption="The descent shrinks the tuple by one element per step; the empty tuple is the base case that stops the descent and starts the unwind."
/>

## Join and Includes — same skeleton, different accumulation

```ts
type Join<T extends readonly string[], D extends string> =
  T extends readonly [infer H extends string, ...infer R extends string[]]
    ? R extends [] ? H : `${H}${D}${Join<R, D>}`
    : "";

type J = Join<["a", "b", "c"], "-">;
//   ^? type J = "a-b-c"

type Includes<T extends readonly unknown[], U> =
  T extends readonly [infer H, ...infer R]
    ? [H] extends [U] ? [U] extends [H] ? true : Includes<R, U> : Includes<R, U>
    : false;

type Inc = Includes<[1, 2, 3], 2>;
//   ^? type Inc = true
```

`Join` carries the separator `D` along the recursion and special-cases the last element (`R extends []`) so the string does not end with a stray delimiter. `Includes` short-circuits the moment it finds a match — the recursive call only happens in the "not yet found" branch. (Note the `[H] extends [U]` and `[U] extends [H]` wrapping — that opts out of distribution so the comparison is exact; you will meet that trick in the next lesson.)

<Inset kind="why" lang="en">
Why `infer H extends string`? The constraint on `H` lets you use `H` in a template literal `${H}` immediately, without a second conditional to prove it is a string. Constrained `infer` (TS 4.7+) is how you keep recursive string types readable instead of nesting `H extends string ? ... : never` at every step.
</Inset>

## The depth wall — and why tail position matters

When you hit "Type instantiation is excessively deep and possibly infinite.", the question is whether you have a wrong base case or simply a non-tail call — the same error message covers both, and the fix is different for each. The compiler bounds recursion to keep type checking from hanging. Historically, a recursive conditional type blew up around **50** levels of self-reference with:

```ts
type BuildBad<N extends number, Acc extends unknown[] = []> =
  Acc["length"] extends N ? Acc : [unknown, ...BuildBad<N, [unknown, ...Acc]>];
//                                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
// Error: Type instantiation is excessively deep and possibly infinite.
// (the recursive call is wrapped in [unknown, ...] — NOT in tail position)
```

TypeScript 4.5 added **tail-recursion elimination on conditional types**. When the recursive call is the *entire* result of a conditional branch — nothing wrapping it — the compiler reuses the evaluation frame instead of nesting, and the effective limit jumps to roughly **1000**. The fix is to thread an accumulator and return it directly:

```ts
type Build<N extends number, Acc extends unknown[] = []> =
  Acc["length"] extends N
    ? Acc                                     // base case
    : Build<N, [unknown, ...Acc]>;            // TAIL call: the whole branch IS the recursion

type T999 = Build<999>["length"];
//   ^? type T999 = 999   (no error — tail-recursion elimination)
```

The structural difference: `[unknown, ...Build<...>]` wraps the call, so each frame must wait for the inner result — not eliminable. `Build<N, [unknown, ...Acc]>` *is* the result — eliminable. Restructure your recursion so the self-call sits in tail position and the wall moves from 50 to 1000.

<Infographic
  svg={depthWall}
  label="Side-by-side comparison of a non-tail recursive call versus a tail call: the non-tail call wraps the self-call in a tuple, keeps every frame on the stack, and walls out around 50 levels with the 'excessively deep' error; the TS 4.5 tail call makes the whole branch the self-call, reuses the evaluation frame via tail-recursion elimination, threads an accumulator, and lifts the limit to about 1000 so Build<999> type-checks."
  caption="The only structural difference is whether the self-call is wrapped or IS the whole branch — and that one position decides whether the depth wall sits at ~50 or ~1000."
  hue="var(--d-infra)"
/>

<Quiz
  id="01-rec-quiz-1"
  lessonSlug="01-recursion-in-types"
  lang="en"
  question="Why does `type Bad<T> = T extends [infer H, ...infer R] ? [Bad<R>, H] : []` hit the depth limit far sooner than a tail-recursive version?"
  choices={[
    { label: "The recursive call `Bad<R>` is nested inside a tuple `[Bad<R>, H]`, so it is not in tail position and TypeScript 4.5 tail-recursion elimination cannot apply — each frame is kept on the stack.", correct: true },
    { label: "Tuples cannot be returned from conditional types", misconception: "Tuples are perfectly valid results of conditional types; the issue is the call position, not the return shape." },
    { label: "`infer` is not allowed in recursive types", misconception: "infer is exactly how tuple recursion peels elements; it is required, not forbidden." },
    { label: "The base case `[]` is wrong and causes infinite recursion", misconception: "The base case is correct; the empty tuple terminates the descent. The limit is hit by non-tail nesting, not a missing base case." },
  ]}
/>

<DragOrder
  id="01-rec-drag-1"
  pieceSlug="01-recursion-in-types"
  lang="en"
  prompt="Order the evaluation of Reverse<[1, 2, 3]> from first match to final collapsed result."
  items={[
    "Reverse<[1,2,3]> matches, Head=1, Tail=[2,3] -> [...Reverse<[2,3]>, 1]",
    "Reverse<[2,3]> matches, Head=2, Tail=[3] -> [...Reverse<[3]>, 2, 1]",
    "Reverse<[3]> matches, Head=3, Tail=[] -> [...Reverse<[]>, 3, 2, 1]",
    "Reverse<[]> hits the base case and returns []",
    "Spreads collapse to the final type [3, 2, 1]",
  ]}
/>

</Explanation>

<KeyTakeaway>Every recursive type is a base case plus a recursive case walking `[infer Head, ...infer Tail]`. A missing or wrong base case is an infinite type; a non-tail recursive call hits the ~50-level wall and errors with "Type instantiation is excessively deep and possibly infinite." Structure the self-call in tail position (the whole branch is the call, with an accumulator) and TypeScript 4.5 tail-recursion elimination lifts the limit to ~1000.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-rec-retrieval"
  lang="en"
  questions={[
    { q: "What are the two arms every well-formed recursive conditional type must have, and what does the [infer Head, ...infer Tail] pattern do in the recursive arm?", a: "A base case that returns a concrete type without recursing (typically the empty-tuple branch returning []), and a recursive case that does a small amount of work and calls itself on a smaller input. In the recursive arm, [infer Head, ...infer Tail] matches a non-empty tuple, binding the first element to Head and all remaining elements to Tail — so each step shrinks the input by one element. The empty tuple fails this match and falls through to the base case, terminating the descent. Without a correct base case the descent never stops and the type is infinite." },
    { q: "What is tail-recursion elimination on conditional types, when does it apply, and how does it change the depth limit?", a: "Introduced in TypeScript 4.5: when the recursive call to a conditional type is the ENTIRE result of a branch (nothing wraps it — e.g. `... ? Acc : Self<N, [unknown, ...Acc]>`), the compiler reuses the evaluation frame instead of nesting a new one. This raises the effective recursion limit from roughly 50 to roughly 1000. It does NOT apply when the call is nested, e.g. [unknown, ...Self<...>] or [Self<...>, H], because the wrapping tuple must wait for the inner result. The practical technique: thread an accumulator parameter and return the recursive call directly as the branch result." },
    { q: "What exactly causes the error 'Type instantiation is excessively deep and possibly infinite.' and what are the two distinct root causes to check?", a: "It fires when the compiler exceeds its instantiation-depth budget for a type. Two distinct causes: (1) a genuinely infinite type — a missing or incorrect base case, so the recursion never terminates; check that the empty-tuple (or other terminal) branch returns a concrete type and does not recurse. (2) a finite but non-tail recursion that nests deeper than ~50 frames — correct base case, but the self-call is wrapped in a tuple/object so elimination cannot apply; restructure into tail position with an accumulator to raise the wall to ~1000. The same message covers both, so diagnose by checking the base case first, then the call position." },
  ]}
/>

<Recap lang="en">
A recursive type is a recursive function the compiler runs: a base case plus a recursive case that peels `[infer Head, ...infer Tail]` and self-calls on the shorter tuple. Reverse, Join, and Includes share that skeleton, differing only in what they accumulate. The compiler bounds depth: a non-tail call walls out around 50 with "Type instantiation is excessively deep and possibly infinite.", but TypeScript 4.5 tail-recursion elimination lifts that to ~1000 when the self-call is the whole branch. Now when you see that error, your first move is to check the base case, then check whether the self-call is wrapped — those two questions cover every case.
</Recap>
