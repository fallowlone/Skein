---
concepts:
- value-space
- type-space
- declaration-namespaces
- typeof-bridge
- keyof-operator
- erasure-vs-classes
deepensInto:
- typescript/01-foundations/01-structural-typing
estMin: 13
lang: en
lessonType: topic
level: junior
order: 2
prereqs:
- typescript/00-start-here/01-why-types-deep
slug: 02-type-vs-value-space
sources:
- https://www.typescriptlang.org/docs/handbook/2/everyday-types.html
- https://www.typescriptlang.org/docs/handbook/2/typeof-types.html
spiral:
- mental-model
status: ready
summary: Every TypeScript identifier lives in value space, type space, or both;
  `typeof` bridges value-to-type and `keyof` stays type-to-type, and the classic
  "only refers to a type / a value" errors come from crossing the worlds.
title: Type space vs value space
track: typescript
unit: 00-start-here
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import namespaceMembership from "./infographics/namespace-membership.svg?raw";

<Hook>
You write `function logShape(t: Shape) {}` and the editor is happy. You write `const s = Shape` and the editor underlines `Shape` in red: *"'Shape' only refers to a type, but is being used as a value here."* Same word, same file, two completely different verdicts. The reason is the single most clarifying idea in all of TypeScript: there are two parallel namespaces — a *value space* that exists at runtime and a *type space* that is erased before it — and most beginner confusion is really one identifier being summoned into the wrong world.

Once you see the two-namespace model, you can decode any "only refers to" error in seconds and navigate `typeof`, `keyof`, and class duality with confidence.
</Hook>

<Crux>Every name lives in value space, type space, or both; `typeof` bridges value→type, and a wrong world is the "refers to a type/value" error.</Crux>

<Explanation>

## Two parallel namespaces

Why does TypeScript need two separate namespaces at all? Because types are erased — the runtime world and the compile-time world are physically separate, and a name can live in one, the other, or both. If you do not know which world a name belongs to, errors like "only refers to a type" will seem arbitrary rather than mechanical.

TypeScript maintains two separate symbol tables. The **value space** holds things that exist at runtime — variables, functions, the constructor side of a class. The **type space** holds things that are erased before runtime — type aliases, interfaces, the instance shape of a class. The same name can be registered in one, the other, or both, and *position* decides which table the compiler consults.

```ts
const port = 3000;        // `port` is in VALUE space only
type Port = number;       // `Port` is in TYPE space only

const x: Port = port;     // type position uses Type table, value position uses value table — fine
const y = Port;
// Error: 'Port' only refers to a type, but is being used as a value here.
```

A type annotation position (after `:`, in `extends`, inside `<...>`) is read against the type table. An expression position (the right-hand side of `=`, a function argument, after `return`) is read against the value table. `Port` exists only in the type table, so summoning it in an expression position fails — and the error tells you exactly which world it lives in.

## Who lives where

The crucial detail for everyday code: declarations differ in *which* namespaces they populate.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Declaration</th>
        <th class="border border-border px-3 py-2 text-left">Value space?</th>
        <th class="border border-border px-3 py-2 text-left">Type space?</th>
        <th class="border border-border px-3 py-2 text-left">Survives to runtime?</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>const x = …</code> / <code>let</code> / <code>function</code></td>
        <td class="border border-border px-3 py-2">Yes</td>
        <td class="border border-border px-3 py-2">No</td>
        <td class="border border-border px-3 py-2">Yes</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>type T = …</code> / <code>interface I</code></td>
        <td class="border border-border px-3 py-2">No</td>
        <td class="border border-border px-3 py-2">Yes</td>
        <td class="border border-border px-3 py-2">No (erased)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>class C</code></td>
        <td class="border border-border px-3 py-2">Yes (constructor)</td>
        <td class="border border-border px-3 py-2">Yes (instance type)</td>
        <td class="border border-border px-3 py-2">Yes</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>enum E</code></td>
        <td class="border border-border px-3 py-2">Yes (object)</td>
        <td class="border border-border px-3 py-2">Yes (member type)</td>
        <td class="border border-border px-3 py-2">Yes</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>namespace N</code></td>
        <td class="border border-border px-3 py-2">Yes</td>
        <td class="border border-border px-3 py-2">Yes</td>
        <td class="border border-border px-3 py-2">Yes</td>
      </tr>
    </tbody>
  </table>
</div>

<Infographic
  svg={namespaceMembership}
  label="A taxonomy tree grouping TypeScript declarations by namespace membership: const/let/function live in value space only (survive runtime), type/interface live in type space only (erased before runtime), and class/enum/namespace live in both spaces (survive runtime)."
  caption="Five declaration kinds collapse into three buckets — and the bucket decides whether the name survives to runtime, which is why an interface can't be instanceof-checked but a class can."
  hue="var(--d-infra)"
/>

A `class` is the one that trips people up most: its name means *two different things* depending on position. In value position it is the constructor function; in type position it is the instance shape.

```ts
class Logger {
  level = "info";
}

const a = new Logger();   // value position: `Logger` is the constructor
//    ^? const a: Logger
function use(l: Logger) {} // type position: `Logger` is the instance type
const ctor: typeof Logger = Logger; // the constructor's TYPE (see next section)
```

## The bridges: `typeof` (value→type) and `keyof` (type→type)

You frequently have a *value* and want to talk about its *type* without writing the type out by hand. The `typeof` **type operator** is the bridge from value space into type space.

```ts
const config = { host: "localhost", port: 5432, tls: false };

type Config = typeof config;
//   ^? type Config = { host: string; port: number; tls: boolean }

function connect(c: typeof config) {} // reuse the inferred shape, no duplication
```

Note: this is the *type-level* `typeof`, used in a type position — it is unrelated to JavaScript's runtime `typeof x` operator that returns the string `"object"`. Same keyword, two worlds.

`keyof` is a different bridge — it stays entirely inside type space, mapping an object *type* to the union of its *keys*.

```ts
type Config = typeof config;
type ConfigKey = keyof Config;
//   ^? type ConfigKey = "host" | "port" | "tls"
```

Compose them and you get the workhorse pattern for typed lookups:

```ts
function get<K extends keyof typeof config>(key: K): (typeof config)[K] {
  return config[key];
}
const p = get("port");
//    ^? number
const h = get("host");
//    ^? string
get("nope");
// Error: Argument of type '"nope"' is not assignable to parameter of type
//        'keyof typeof config'.  ("host" | "port" | "tls")
```

<Inset kind="why" lang="en">
Why does `typeof config` produce a concrete object type rather than just `object`? Because the type-level `typeof` reads the *static type the checker already inferred* for that binding — it does not run anything. For a `const` whose initializer is an object literal, the checker inferred `{ host: string; port: number; tls: boolean }`, so `typeof config` is exactly that. If you had written `let config` or annotated it as `Record<string, unknown>`, `typeof config` would yield that wider type instead. The operator is a pointer into the inference result, not a fresh analysis.
</Inset>

## The two classic errors, decoded

Both halves of the world-crossing mistake have their own exact diagnostic, and reading the wording tells you which direction you crossed.

```ts
interface User { id: string }

const a: User = { id: "1" };   // fine: User in type position
const b = User;
// Error: 'User' only refers to a type, but is being used as a value here.
//        (you used a type-only name where a value is required)

const handler = () => {};
type T = handler;
// Error: 'handler' refers to a value, but is being used as a type here.
//        Did you mean 'typeof handler'?
//        (you used a value-only name where a type is required — bridge it with typeof)
```

The fix is always the same logic: figure out which space the name lives in, and either move to the matching position or use a bridge (`typeof`) to cross deliberately. And because `interface`/`type` are type-only, they vanish at runtime — you cannot `instanceof User` an interface — whereas a `class` survives, which is exactly why classes are the tool when you need a runtime-checkable shape. When you encounter either diagnostic in a real codebase, read the wording first: it tells you the direction of the crossing and the bridge you need.

<StackDiagram
  label="Where a name resolves by position: type-only names answer in type space, value-only names answer in value space, and class/enum answer in both."
  hue="var(--d-frontend)"
  caption="Position decides the namespace. `typeof` is the one-way bridge from a value into its type; `keyof` operates entirely within type space."
  layers={[
    { label: "type T / interface I", note: "type space only — erased" },
    { label: "const / let / function", note: "value space only — runtime" },
    { label: "class C / enum E", note: "BOTH spaces — survives runtime" },
    { label: "typeof value  →  type", note: "value→type bridge" },
    { label: "keyof Type  →  type", note: "type→type, stays in type space" },
  ]}
/>

<FlowDiagram
  label="Resolving an identifier: the compiler picks the value table for expression positions and the type table for annotation positions, and a name absent from the consulted table raises the matching only-refers-to error."
  hue="var(--accent)"
  perRow={3}
  nodes={[
    { id: "name", label: "Identifier", sub: "e.g. User" },
    { id: "pos", label: "Position?", sub: "expr vs annotation" },
    { id: "vtab", label: "Value table", sub: "expression position" },
    { id: "ttab", label: "Type table", sub: "annotation position" },
    { id: "ok", label: "Resolves", sub: "found in table" },
    { id: "err", label: "only-refers-to error", sub: "absent from table" },
  ]}
  edges={[
    { from: "name", to: "pos", label: "where used" },
    { from: "pos", to: "vtab", label: "RHS / arg" },
    { from: "pos", to: "ttab", label: "after :" },
    { from: "vtab", to: "ok", label: "present" },
    { from: "ttab", to: "ok", label: "present" },
    { from: "vtab", to: "err", label: "type-only name" },
  ]}
  caption="Same identifier, two tables. The wording of the error (type used as value, or value used as type) tells you which table came up empty and which direction you crossed."
/>

<Quiz
  id="ts-00-02-quiz-1"
  lessonSlug="02-type-vs-value-space"
  lang="en"
  question="`const settings = { dark: true, lang: 'en' }`. You want a type that is the union `'dark' | 'lang'`. Which expression produces it?"
  choices={[
    { label: "keyof typeof settings — typeof bridges the value into its inferred object type, then keyof maps that type to its key union", correct: true },
    { label: "keyof settings", misconception: "`settings` is a value, not a type. `keyof` needs a type operand, so this errors with 'settings refers to a value, but is being used as a type here.' You must bridge with typeof first." },
    { label: "typeof settings", misconception: "That yields the object type { dark: boolean; lang: string }, not the union of its keys. You still need keyof on top of it." },
    { label: "Object.keys(settings)", misconception: "That is a runtime value of type string[], not a type-level union — and its element type is the widened string, not the literal keys." },
  ]}
/>

<MetaphorComplete
  id="ts-00-02-metaphor-1"
  pieceSlug="02-type-vs-value-space"
  lang="en"
  setup="A class name is like a word that means two things by context: in an expression it is the runtime constructor, and in an annotation it is the instance ___ — the shape an instance has."
  accepted={["type", "shape", "instance type"]}
  canonical="type"
  explanation="In value position `class C` resolves to the constructor function (you can `new` it); in type position the same name resolves to the instance type — the structural shape an instance of C has. One identifier, both namespaces."
/>

</Explanation>

<KeyTakeaway>TypeScript keeps two namespaces — value space (runtime) and type space (erased) — and position decides which one a name resolves against. `const`/`function` are value-only, `type`/`interface` are type-only (and vanish at runtime), `class`/`enum`/`namespace` are both. `typeof` bridges a value into its inferred type; `keyof` stays in type space mapping a type to its key union. The "only refers to a type/value" errors are just a name summoned into the wrong world.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="ts-00-02-retrieval"
  lang="en"
  questions={[
    { q: "Which declarations populate the value namespace, the type namespace, or both, and why does it matter at runtime?", a: "`const`/`let`/`var`/`function` are value-only — they exist at runtime and have no type-space meaning. `type` aliases and `interface` are type-only — they are erased before runtime, so you cannot use them in value position (e.g. no `instanceof` against an interface). `class` and `enum` populate both: a class name is the constructor in value position and the instance type in type position, and both survive to runtime; an enum is both an object value and a member type. `namespace` also occupies both. It matters because anything type-only disappears in the emitted JavaScript, so if you need a shape you can check at runtime you must reach for a class or a runtime validator, not an interface." },
    { q: "What is the difference between type-level `typeof` and `keyof`, and how do they compose?", a: "Type-level `typeof x` (used in a type position) is the value→type bridge: it reads the static type the checker already inferred for the value `x` and gives you that type — e.g. `typeof config` becomes `{ host: string; port: number; tls: boolean }`. It is unrelated to JavaScript's runtime `typeof` that returns a string. `keyof T` stays entirely within type space: it maps an object type to the union of its property-key literals — e.g. `keyof Config` is `'host' | 'port' | 'tls'`. They compose as `keyof typeof value`: bridge the value to its type, then extract the key union — the standard pattern for a typed `get(key)` whose key parameter is constrained to the real keys and whose return type is the indexed property type `(typeof value)[K]`." },
    { q: "Decode the two errors: 'X only refers to a type, but is being used as a value here' vs 'X refers to a value, but is being used as a type here'.", a: "Both mean you summoned a name into the wrong namespace, and the wording tells you the direction. 'Only refers to a type, used as a value' means the name exists only in the type table (a `type`/`interface`) and you put it in an expression position (RHS of `=`, an argument, `new X`) — the value table came up empty. 'Refers to a value, used as a type' means the name exists only in the value table (a `const`/`function`) and you put it in a type position (after `:`, in `extends`, in `<...>`) — the compiler often suggests `typeof X` because that is the bridge to cross deliberately. The fix is to identify the name's namespace and either move to the matching position or bridge with `typeof`." },
  ]}
/>

<Recap lang="en">
The value/type-space split is the lens that makes the rest of the track legible: position chooses a namespace, `typeof` and `keyof` are the bridges between them, and the "only refers to" errors are simply a name in the wrong world. You also saw that type-only declarations evaporate at runtime while classes and enums persist — a direct application of the erasure idea from the previous lesson. Now when you see one of those error messages in the editor, you will not guess — you will read the wording, identify which table came up empty, and reach for the right fix or bridge immediately.
</Recap>
