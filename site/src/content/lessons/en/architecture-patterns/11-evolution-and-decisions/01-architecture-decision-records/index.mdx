---
slug: "01-architecture-decision-records"
lang: en
track: "architecture-patterns"
unit: "11-evolution-and-decisions"
order: 1
title: "Architecture Decision Records"
summary: "An ADR captures one architectural decision — context, forces, the choice, and consequences — so future engineers know WHY, not just what was decided. Lightweight, version-controlled, and superseded rather than deleted."
estMin: 20
status: ready
lessonType: topic
level: senior
concepts:
  - architecture-decision-record
  - decision-log
  - architectural-fitness
  - status-lifecycle
  - decision-rationale
  - version-controlled-docs
prereqs: []
sources:
  - https://adr.github.io/
  - https://cognitect.com/blog/2011/11/15/documenting-architecture-decisions
  - https://martinfowler.com/articles/architecturally-evident-coding-style.html
  - https://www.thoughtworks.com/radar/techniques/lightweight-architecture-decision-records
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Six months after the B2B billing platform switched from a REST-based inter-module communication model to an event-driven one, a new engineer asked why. The team scoured Slack, Confluence, and pull requests for three days and found fragments: a thread that said "we discussed this in the March meeting," a PR description that said "switching to events," and a Jira ticket that said "technical debt." Nobody could reconstruct the forces that drove the decision — the chatty REST calls causing cascading failures during billing runs, the three alternatives they considered, the reason they rejected a message queue in favor of domain events, or the tradeoffs they consciously accepted. When a failure mode appeared that the event-driven model couldn't handle, the team had to debate the original decision all over again without the original context. Two engineers wanted to revert; two wanted to push forward. They had no record of what they had already considered.

An Architecture Decision Record would have taken thirty minutes to write. Instead, it cost three days of archaeology and an unresolved architectural argument.
</Hook>

<Crux>An ADR captures one decision — its context, forces, and consequences — so future readers know WHY, not just what was chosen.</Crux>

<Explanation>

## What an ADR is and what it is not

An Architecture Decision Record is a short document — typically one to two pages — that records a single significant architectural decision. Michael Nygard proposed the format in 2011. The key insight is that the decision itself is less important than the reasoning: the context that made the decision necessary, the forces pulling in different directions, the alternatives considered, and the consequences accepted.

An ADR is not:
- A design document (those describe how something is built)
- A requirements document (those describe what it must do)
- A post-mortem (those describe what went wrong)
- A living document (an ADR is immutable once accepted; it is superseded, not edited)

The format Nygard proposed has five sections:

**Title.** A short imperative phrase: "Use domain events for inter-module communication" not "Event-driven architecture decision."

**Status.** Proposed → Accepted → Superseded (by ADR-NNN) | Deprecated. Status is never "rejected" for decisions that were accepted — those are superseded when a newer decision overrides them. Rejected proposals exist as Proposed ADRs that were never accepted.

**Context.** The forces and circumstances that made this decision necessary. What was the problem? What constraints existed? What was the current situation?

**Decision.** The choice made, stated plainly. "We will use domain events published to an in-process event bus for all cross-module communication."

**Consequences.** The tradeoffs accepted. What becomes easier? What becomes harder? What new risks are introduced?

<FlowDiagram
  label="ADR status lifecycle"
  caption="An ADR moves from Proposed to Accepted when the team agrees. Accepted ADRs are never edited — when a newer decision supersedes them, the old ADR is marked Superseded and links to the new one. This creates a decision log where the full history of reasoning is preserved."
  nodes={[
    { id: "proposed", label: "Proposed", sub: "written, under discussion" },
    { id: "accepted", label: "Accepted", sub: "agreed, becomes an artifact" },
    { id: "superseded", label: "Superseded", sub: "overridden by a later ADR" },
    { id: "deprecated", label: "Deprecated", sub: "no longer relevant (context gone)" },
    { id: "new-adr", label: "New ADR", sub: "written to override old decision" },
  ]}
  edges={[
    { from: "proposed", to: "accepted", label: "team agrees" },
    { from: "accepted", to: "superseded", label: "new decision overrides" },
    { from: "new-adr", to: "superseded", label: "ADR-NNN supersedes ADR-MMM" },
    { from: "accepted", to: "deprecated", label: "context no longer applies" },
  ]}
/>

## Why the WHY is the value

Consider two records of the same decision:

**Record A (commit message):** "Switch inter-module communication to domain events."

**Record B (ADR):** "Context: The billing module calls the ordering module's REST API synchronously during invoice generation. When order volume spikes, this causes cascading timeouts — billing runs fail because ordering is slow, and the retry amplifies the load. We evaluated three options: (1) add a queue between REST callers, (2) switch to domain events on an in-process bus, (3) redesign billing to own a read-model of order data. We rejected (1) because it adds operational complexity (a broker to manage) without solving the coupling. We rejected (3) because billing owning order data violates data ownership boundaries (ADR-004). Decision: We adopt domain events on an in-process bus (no broker). Consequences: cross-module calls become asynchronous; billing logic must handle events arriving out of order; we accept that billing cannot immediately confirm an order's current status — it works from the last event it received."

Record B lets a future engineer understand: (a) why the problem existed, (b) what was considered and rejected and why, (c) what tradeoffs were consciously accepted. When a new problem appears — say, billing receiving events out of order — the team can see that out-of-order delivery was already known and accepted. They don't re-debate the original decision; they work within the frame that was set.

Record A forces archaeology. Record B is institutional memory made explicit.

<Inset kind="why">
Why does the ADR have to capture rejected alternatives? Because the alternatives that were rejected are what prevent re-litigation. Without a record of why option (1) was rejected, the next engineer who hits a problem will independently re-propose option (1). With the record, the discussion can start from "we rejected this for reason X — has X changed?" rather than from zero. The rejected path is as informative as the chosen one.
</Inset>

<Quiz
  id="arch-11-01-quiz-1"
  lessonSlug="01-architecture-decision-records"
  lang="en"
  question="The billing platform team has an accepted ADR (ADR-007) that says 'use domain events for cross-module communication.' A year later, performance profiling shows that the in-process event bus creates a bottleneck when processing 50,000 concurrent orders. A senior engineer proposes replacing it with a Kafka-based broker. What is the correct way to handle this using ADRs?"
  choices={[
    { label: "Write a new ADR (ADR-012: 'Use Kafka broker for cross-module events') that references ADR-007 as superseded, documents the new context (the performance bottleneck, the scale threshold, Kafka's operational overhead tradeoff), states the new decision, and marks ADR-007 as 'Superseded by ADR-012'. ADR-007 is never deleted or edited — it remains in the decision log so future engineers can see the full history: what was decided when, under what forces, and why it was later changed.", correct: true },
    { label: "Edit ADR-007 to add the new decision. ADRs should be living documents that evolve with the system.", misconception: "Editing an accepted ADR destroys the historical record. An ADR is a record of what was decided at a specific point in time under specific forces. If you edit it, future engineers cannot tell what the original decision was, what forces existed then, and why the system changed. The immutable nature of ADRs is what makes them useful as institutional memory. The correct action is to write a new ADR that supersedes the old one." },
    { label: "Delete ADR-007 since it is no longer valid and write ADR-012 from scratch without referencing the old decision.", misconception: "Deleting ADRs removes historical context that future engineers need. The sequence of decisions — and why each was made — is exactly what the decision log is designed to preserve. A new engineer joining the team after ADR-012 should be able to read both ADRs and understand: the system started with an in-process bus (for these reasons), hit this specific bottleneck at this scale, and then switched to Kafka (with these new tradeoffs)." },
    { label: "Leave ADR-007 as-is and make the Kafka change without a new ADR, since the decision is now in the code and the code is the best documentation.", misconception: "The code shows what was chosen; it cannot show why Kafka was preferred over alternatives (e.g., RabbitMQ, Redis Streams, SNS/SQS), what tradeoffs were accepted (operational complexity, at-least-once delivery, consumer group management), or what the performance threshold was that triggered the change. Without an ADR, the next performance problem will trigger the same re-debate all over again." },
  ]}
/>

## ADRs as first-class artifacts: version control and location

The lightweight ADR format is specifically designed to live next to the code, version-controlled in the same repository. Nygard's original proposal puts them in `doc/arch/adr-NNN-title.md`. ThoughtWorks Radar (2016) lists Lightweight Architecture Decision Records as a technique to adopt specifically because they are cheap and version-controlled.

The benefits of co-locating ADRs with code:
- **Change history:** when the code changes, the ADR history shows when and why the decision was made, correlatable with the commit that implemented it.
- **Discoverability:** engineers exploring the codebase find ADRs where they work (in the repo, not in a wiki that may be stale or inaccessible).
- **Pull request workflow:** an ADR can be proposed in a pull request, reviewed alongside the implementation change, and merged when the decision is accepted.
- **Durability:** wikis drift, Confluence pages go stale, Slack threads are archived. The ADR in the repo survives as long as the codebase does.

<Inset kind="note">
The ADR format is deliberately minimal. Some teams add sections (Pros/Cons tables, risk ratings, links to RFC-style discussions). That is fine as long as the core information is present: context, decision, consequences. The Nygard format is a minimum, not a maximum. The failure mode is over-engineering the format until writing ADRs becomes so burdensome that nobody writes them. A two-paragraph ADR is infinitely better than no ADR.
</Inset>

<Quiz
  id="arch-11-01-quiz-2"
  lessonSlug="01-architecture-decision-records"
  lang="en"
  question="A team stores architecture decisions in Confluence. An engineer argues that Confluence is better than ADRs in the repo because Confluence has rich formatting, search, and commenting. A senior engineer disagrees. What is the most important structural argument against Confluence for decision records?"
  choices={[
    { label: "The most important argument is durability and correlation with code. Confluence pages can be edited silently (no immutable history), can become inaccessible (permission changes, org migration), and cannot be correlated with the specific commit that implemented the decision. A decision in the repo is version-controlled: you can `git log doc/adr/` to see all decisions in chronological order, and `git blame` to correlate a decision with the implementation PR. When the Confluence instance is migrated or the team loses access, the decision history is gone. The code outlives the wiki.", correct: true },
    { label: "Confluence is actually better for ADRs because it has richer tooling. The engineer arguing for Confluence is correct.", misconception: "Rich formatting is a secondary concern. The critical properties of an ADR are immutability (decisions are not silently edited), durability (the record outlives the tool), and correlation with code. Confluence fails on durability and correlation. A plain Markdown file in the repo is durable, version-controlled, and correlated with implementation commits — none of which Confluence provides natively." },
    { label: "The argument against Confluence is that it is slower to write than Markdown. Speed is the primary factor in whether ADRs get written.", misconception: "Speed of writing is a secondary concern. The primary structural problem is that Confluence doesn't provide the version-control properties that make ADRs useful: immutable history, discoverability alongside the code, and correlation with implementation commits. A Confluence page can be edited without a trace, making it structurally unable to serve as an immutable decision record." },
    { label: "Both formats are equivalent for decision records. The team should choose whichever format their engineers prefer.", misconception: "They are not equivalent. Version-controlled ADRs in the repo provide immutability, durability, and code correlation. Confluence provides none of those properties. The choice is not aesthetic — it determines whether the decision record will still be accessible and accurate in five years, and whether it can be correlated with the implementation that followed." },
  ]}
/>

## What decisions warrant an ADR

Not every decision needs an ADR. The signal is: would a senior engineer joining the team in two years need to know why this decision was made, and could they not figure it out from the code?

Decisions that warrant an ADR:
- Architectural style choices (event-driven vs request-response, modular monolith vs microservices)
- Significant technology adoptions (choosing a specific event store, a message broker, an ORM)
- Rejected alternatives that are likely to be re-proposed ("we considered X and rejected it because…")
- Decisions with non-obvious consequences or accepted tradeoffs
- Decisions that constrain future options ("we cannot change to X without re-implementing Y")

Decisions that do not need an ADR:
- Routine implementation choices (naming conventions, file structure within a module)
- Framework-idiomatic patterns (using dependency injection because the framework uses it)
- Decisions that are obvious from the code and domain

The billing platform example: "Use PostgreSQL" probably doesn't need an ADR if the team is a Rails shop and PostgreSQL is the default. "Use PostgreSQL event sourcing over a dedicated event store like EventStoreDB" absolutely needs an ADR, because there are non-obvious forces (operational familiarity with PG, the cost of running EventStoreDB in production, the GDPR deletion problem with event sourcing) that future engineers cannot infer from the code.

<Quiz
  id="arch-11-01-quiz-3"
  lessonSlug="01-architecture-decision-records"
  lang="en"
  question="The B2B platform engineering lead reviews the team's ADR backlog and finds four candidate decisions. Which one most clearly warrants an ADR?"
  choices={[
    { label: "'We use TypeScript path aliases to enforce module boundaries instead of a separate architecture test library.' — This decision has non-obvious forces (ArchUnit is Java-only, ts-arch adds a dependency, path aliases are already in the TypeScript toolchain), rejected alternatives (ts-arch, ESLint import rules as the only mechanism, no enforcement), and accepted tradeoffs (path aliases prevent resolution but don't assert dependency direction — circular imports between modules could still form). A future engineer seeing the tsconfig.json paths section cannot infer any of this from the code.", correct: true },
    { label: "'We use async/await instead of callbacks in our Node.js services.' — This is a widely adopted Node.js idiom with no significant architectural tradeoffs. Future engineers will understand why from the code and community norms.", misconception: "This is a routine implementation choice. Using async/await is now the idiomatic Node.js style; the forces and consequences are well understood by any Node.js engineer. This does not constrain future options in a non-obvious way, and future engineers will not need to be told why async/await was used." },
    { label: "'We name our database tables in snake_case.' — This is a naming convention with minimal architectural consequence.", misconception: "Naming conventions are routine implementation decisions. snake_case is the PostgreSQL convention; the choice is idiomatic and does not require an ADR. There are no non-obvious tradeoffs or rejected alternatives that a future engineer needs to know about to understand the system." },
    { label: "'We use Prettier for code formatting.' — This is a tooling decision that affects developer workflow.", misconception: "Tooling decisions like formatter choice are routine and reversible. The forces (team consistency, CI enforcement) are obvious from the configuration files. This does not affect the architecture in a way that a future engineer needs to understand to make structural decisions." },
  ]}
/>

</Explanation>

<KeyTakeaway>An ADR is immutable once accepted — never edited, only superseded. Its value is the WHY: context, rejected alternatives, and consequences accepted. Store ADRs version-controlled in the repository next to the code they govern. The rejected paths are as important as the chosen one: they prevent re-litigation of decisions already made.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-11-01-retrieval"
  lang="en"
  questions={[
    { q: "What are the five sections of Nygard's ADR format, and why is the 'Context' section the most important?", a: "Title (short imperative phrase), Status (Proposed/Accepted/Superseded/Deprecated), Context (forces and circumstances driving the decision), Decision (the choice made), Consequences (tradeoffs accepted). Context is the most important because it explains why the decision was necessary — the problem it solved, the constraints that existed, the forces pulling in different directions. Without context, the decision looks arbitrary. With context, future engineers can judge whether the original forces still apply and whether the decision should be revisited." },
    { q: "What is the ADR status lifecycle, and why are accepted ADRs never deleted or edited?", a: "Proposed → Accepted → Superseded (by ADR-NNN) or Deprecated. Accepted ADRs are never edited because editing destroys the historical record — future engineers need to see what was decided at a specific point in time under specific forces. If the decision changes, a new ADR supersedes the old one and links to it. The old ADR stays in the log. This creates a chain: ADR-007 accepted (when and why), ADR-012 supersedes ADR-007 (new context, new forces, new tradeoffs). Deleting or editing breaks the chain and forces archaeology to reconstruct the history." },
    { q: "Why should ADRs be stored in the code repository rather than a wiki or project management tool?", a: "Three reasons: immutability (version-controlled files have a change history; wikis can be silently edited), durability (the repo outlives Confluence instances, org migrations, and tool changes), and correlation (git log and git blame let engineers see when a decision was made and correlate it with the implementation commit). A Confluence page that was correct in 2022 may have been edited incorrectly in 2024 — there is no easy way to recover the original. An ADR in git preserves the original with full authorship and date." },
  ]}
/>

<Recap lang="en">
The billing platform's three-day archaeology hunt — reconstructing a decision that took an hour to make — is the failure mode ADRs prevent. The decision log is not about bureaucracy; it is about institutional memory made explicit and durable.

The format is deliberately minimal: context, decision, consequences. The immutability is deliberate: accepted ADRs are superseded, not edited. The location is deliberate: next to the code in version control, where engineers work and where the record will outlive any wiki.

The most important part of an ADR is the rejected alternatives. Without them, every engineer who encounters the same problem will re-propose the same alternatives. With them, the team can start from the existing analysis and ask: "Has the context changed enough to revisit this?"

ADRs are the bridge between the architecture as it exists and the architecture as it came to be. Without them, every structural decision looks like it was inevitable. With them, engineers understand that every decision was a tradeoff — and can evaluate whether the forces that drove it still apply.
</Recap>
