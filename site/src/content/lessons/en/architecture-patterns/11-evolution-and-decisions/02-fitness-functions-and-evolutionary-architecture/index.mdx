---
slug: "02-fitness-functions-and-evolutionary-architecture"
lang: en
track: "architecture-patterns"
unit: "11-evolution-and-decisions"
order: 2
title: "Fitness Functions and Evolutionary Architecture"
summary: "A fitness function is an automated test that an architectural property still holds. Fitness functions let architecture evolve incrementally under protection — the wrong-way detector that fails the build before a violation reaches production."
estMin: 22
status: ready
lessonType: topic
level: senior
concepts:
  - fitness-function
  - evolutionary-architecture
  - architectural-characteristic
  - cyclic-dependency-check
  - latency-budget
  - continuous-vs-triggered
prereqs: []
sources:
  - https://www.oreilly.com/library/view/building-evolutionary-architectures/9781492097532/
  - https://www.thoughtworks.com/radar/techniques/evolutionary-architecture
  - https://www.archunit.org/
  - https://martinfowler.com/articles/building-evolutionary-architectures.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import continuousVsTriggered from "./infographics/continuous-vs-triggered.svg?raw";

<Hook>
Eighteen months after the B2B billing platform established its modular monolith with enforced boundaries, the team hired twelve new engineers. The module rules were in an ADR and in an onboarding doc. The ESLint import restriction rules existed — but nobody had checked them in four months because the lint step had been accidentally removed from the CI pipeline during a Dockerfile refactor. Nobody noticed, because the build still passed.

By the time someone ran the architecture tests manually, there were 23 boundary violations across six modules. Billing was importing from ordering internals. The notifications module was calling the inventory module's repository layer directly. Worse: the violations had been deployed to production. The architectural structure the team had spent months building had been eroded — silently, incrementally, one shortcut at a time — while the CI pipeline reported green.

The problem was not the rules. The problem was that nothing was continuously measuring whether the rules still held.
</Hook>

<Crux>A fitness function is an automated test that an architectural property still holds — the wrong-way detector that fails the build.</Crux>

<Explanation>

## What a fitness function is

The term comes from Neal Ford, Rebecca Parsons, and Patrick Kua's book *Building Evolutionary Architectures* (O'Reilly, 2017). They borrowed it from evolutionary computing, where a fitness function scores how well a candidate solution satisfies the problem's requirements.

In architecture, a **fitness function** is any mechanism that provides an objective, automated assessment of whether an architectural characteristic still holds. It is not documentation. It is not a code review checklist. It runs in CI and produces a pass or fail.

Architectural characteristics — sometimes called "ilities" — are the non-functional properties the architecture must preserve:
- **Modularity:** no module imports from another module's internals
- **Latency:** the p99 response time of the billing API is under 200ms
- **Cyclic dependency freedom:** no package-level cycles between modules
- **Security:** no sensitive data appears in log output
- **Scalability:** the system can process 10,000 invoice generations per minute

A fitness function turns these from aspirations into build gates.

<FlowDiagram
  label="Fitness function as a CI gate on the architecture"
  caption="A fitness function runs inside CI. When a code change violates an architectural property — a boundary violation, a cycle, a latency regression — the fitness function fails the build before the violation reaches production. The developer sees the failure immediately, while the change is still local."
  nodes={[
    { id: "code-change", label: "Code change", sub: "PR or push" },
    { id: "ci", label: "CI pipeline", sub: "build, test, lint" },
    { id: "fitness-fn", label: "Fitness function", sub: "architectural property check" },
    { id: "pass", label: "Pass", sub: "property still holds" },
    { id: "fail", label: "Fail", sub: "property violated — build blocked" },
    { id: "production", label: "Production", sub: "receives only conforming code" },
  ]}
  edges={[
    { from: "code-change", to: "ci", label: "triggers" },
    { from: "ci", to: "fitness-fn", label: "runs" },
    { from: "fitness-fn", to: "pass", label: "property holds" },
    { from: "fitness-fn", to: "fail", label: "violation detected" },
    { from: "pass", to: "production", label: "deploy proceeds" },
  ]}
/>

## Atomic vs holistic fitness functions

Ford, Parsons, and Kua distinguish two dimensions of fitness functions: scope and frequency.

**Atomic fitness functions** test a single architectural characteristic in isolation:
- "No cyclic dependencies between modules" — a single ArchUnit rule
- "No direct imports from another module's internal package" — a ts-arch or ESLint rule
- "All public API methods return within 50ms in unit tests" — a performance assertion in the test suite

**Holistic fitness functions** test a combination of characteristics that only emerge together:
- "Under a 10,000 RPM load, the system maintains p99 < 200ms AND error rate < 0.1% AND no OOM errors" — a load test in CI
- "The deployed system passes security scanning AND dependency audit AND no cross-boundary access in production traces" — a composite gate

Most teams start with atomic fitness functions because they are cheap to write and fast to run. Holistic functions are more expensive but catch emergent failure modes that atomic tests miss.

<Inset kind="why">
Why is this called "evolutionary architecture"? Because fitness functions enable *guided incremental change*. Without fitness functions, the architecture degrades through accumulated shortcuts — each individual change is small and defensible, but the cumulative effect is erosion. With fitness functions, each change is evaluated against the architectural properties the team committed to. The architecture can evolve — new patterns added, old ones removed — but only in directions that preserve the properties that matter. The fitness function is what makes change safe. It is the difference between "we evolved the architecture on purpose" and "the architecture drifted until we noticed."
</Inset>

<Quiz
  id="arch-11-02-quiz-1"
  lessonSlug="02-fitness-functions-and-evolutionary-architecture"
  lang="en"
  question="The billing platform's architecture team wants to add a fitness function to prevent the boundary violations that silently crept in. They have two proposals: (A) Add an ArchUnit test that asserts 'no class in billing.internal may import from ordering.internal' — runs in the unit test suite on every commit. (B) Add an architectural review checklist that senior engineers fill out in every PR. Which is a fitness function, and why does it matter?"
  choices={[
    { label: "Option A is the fitness function. A fitness function must be automated and objective — it produces a pass/fail without human judgment. Option B (a checklist) depends on human review, which fails under deadline pressure, is skipped during onboarding, and produces inconsistent results depending on who reviews. The billing platform's violations accumulated precisely because human review is not continuous. An ArchUnit test runs on every commit, catches violations immediately, and cannot be skipped without disabling the test — the violation fails the build before it can be merged.", correct: true },
    { label: "Option B is better because senior engineers can catch nuanced violations that automated tests miss.", misconception: "Senior engineers catching nuanced violations is valuable, but it is code review, not a fitness function. Code review is not continuous, not objective, and not automated. The fitness function catches the objective, measurable violations that do not require judgment. The billing platform's violations were not nuanced — they were direct imports from internal packages, exactly the kind of objective check that an automated tool handles reliably. Senior review catches things that cannot be automated; fitness functions handle what can be." },
    { label: "Both are fitness functions. Any mechanism that evaluates architectural quality qualifies.", misconception: "A checklist completed by humans is not a fitness function by the Ford/Parsons/Kua definition. The critical property is automation: a fitness function runs without human intervention and produces a deterministic result. A manual checklist produces results that vary by reviewer, can be skipped, and does not run continuously. Only Option A meets the automation requirement." },
    { label: "Neither is a fitness function. Fitness functions only apply to performance characteristics like latency and throughput.", misconception: "Fitness functions apply to any architectural characteristic, not just performance. Modularity (no boundary violations), dependency structure (no cycles), security (no sensitive data in logs), and coupling constraints are all valid targets for fitness functions. ArchUnit enforcing package dependency rules is a classic example of a modularity fitness function." },
  ]}
/>

## Triggered vs continuous fitness functions

The second dimension in Ford/Parsons/Kua is frequency:

**Triggered fitness functions** run on demand — in CI, on merge, on a schedule. They are appropriate for checks that are expensive to run (load tests, full security scans, integration tests against a staging environment) or that test properties that can only be verified against a built artifact.

**Continuous fitness functions** run on every change, in the development loop, as fast as possible. They are appropriate for structural checks (dependency rules, cyclic dependencies, import restrictions) that can be evaluated against source code without building or deploying.

For the billing platform's boundary violation problem, a **continuous** fitness function is correct: the ArchUnit or ts-arch check runs in the unit test suite on every commit, failing immediately when a violation is introduced. There is no lag between violation and detection.

A load test that verifies p99 < 200ms under 10,000 RPM is necessarily **triggered** — it requires a running environment and takes minutes to execute. It runs nightly or before a release, not on every commit.

<Infographic
  svg={continuousVsTriggered}
  label="Side-by-side comparison of continuous and triggered fitness functions across five dimensions: when they run, cost, whether they need a build, the property type they check, and detection lag. Continuous functions run on every commit in about 2 seconds against source code to catch structural violations instantly; triggered functions run on a schedule and take minutes against a built environment to verify emergent properties like a load test."
  caption="The cost of a check decides its frequency: a ~2-second structural check gates every commit, while an 8-minute load test runs nightly — running it on every commit would kill velocity."
  hue="var(--d-infra)"
/>

<Quiz
  id="arch-11-02-quiz-2"
  lessonSlug="02-fitness-functions-and-evolutionary-architecture"
  lang="en"
  question="The billing team has three proposed fitness functions: (1) An ArchUnit rule asserting no cyclic module dependencies — takes 2 seconds in the test suite. (2) A load test asserting p99 < 150ms under 5,000 concurrent invoice requests — takes 8 minutes against a staging environment. (3) A dependency vulnerability scan against the full dependency tree — takes 30 seconds. How should these be classified as triggered vs continuous, and at what frequency should each run?"
  choices={[
    { label: "(1) Continuous — runs on every commit as part of the unit test suite. It is fast (2s), requires no external environment, and catches violations immediately when they are introduced. (2) Triggered — runs nightly or before a release. It requires a live staging environment and takes 8 minutes; running it on every commit would slow the build to unusability and require a staging environment on every developer machine. (3) Triggered — runs daily or on a schedule, not on every commit. 30 seconds is acceptable in CI but the vulnerability database only changes when new CVEs are published, making continuous execution wasteful. Schedule it daily or on dependency file changes.", correct: true },
    { label: "All three should be continuous — every fitness function should run on every commit to catch violations immediately.", misconception: "Fitness functions have real cost. An 8-minute load test on every commit means a 10-minute feedback loop for every engineer change — this actively harms development velocity. Continuous fitness functions must be fast (seconds, not minutes) and require no external infrastructure. Slower checks are triggered on a schedule or against specific events (PR merge, nightly). The goal is maximum signal at minimum friction, not maximum coverage regardless of cost." },
    { label: "All three should be triggered on a weekly schedule to balance coverage with development speed.", misconception: "A weekly schedule for the cyclic dependency check means a violation introduced Monday is not caught until the following Monday — by which time six more changes have stacked on top of it. Structural checks (1) should be continuous because they are cheap and their value is immediate feedback. Weekly schedules are appropriate for comprehensive security audits, not architectural invariants that can be checked in 2 seconds." },
    { label: "(1) and (3) should be triggered on PR merge; (2) should run nightly. All checks require a CI environment.", misconception: "ArchUnit tests (1) do not require a CI environment — they run as unit tests on the local machine and in CI. Separating them from the regular test suite (by making them PR-merge only) creates a gap: violations introduced between the last full run and the merge may have been built upon by other engineers. Continuous checks that run locally and in CI catch violations at the earliest possible moment." },
  ]}
/>

## Implementing fitness functions: practical examples

**ArchUnit (Java) — dependency direction:**
```java
// Fail the test if any class in billing imports from ordering.internal
@AnalyzeClasses(packages = "com.platform")
class ModuleBoundaryTest {
  @ArchTest
  static final ArchRule billingMayNotImportOrderingInternals =
    noClasses().that().resideInAPackage("..billing..")
      .should().dependOnClassesThat()
      .resideInAPackage("..ordering.internal..");
}
```

**ts-arch (TypeScript) — no cycles:**
```typescript
// Fail the test if any circular dependency exists between modules
describe("architecture", () => {
  it("has no cyclic module dependencies", async () => {
    const result = await tsarch.FileSystem
      .fromConfig("tsconfig.json")
      .onlyFiles(["src/**/*.ts"])
      .noCycles();
    expect(result).toEqual([]);
  });
});
```

**Latency budget (unit test assertion):**
```typescript
it("invoice generation completes within 100ms", async () => {
  const start = performance.now();
  await billingService.generateInvoice(testOrderId);
  const elapsed = performance.now() - start;
  expect(elapsed).toBeLessThan(100);
});
```

The commonality: each is a test that produces a pass/fail. Each runs in CI. Each catches a specific architectural property. The property was previously only maintained by human convention; the fitness function makes it a machine-enforced invariant.

<Inset kind="note">
The boundary violations in the billing platform were caught eventually — but only after they had been in production for months. The cost of fixing them was proportional to how long they had existed: six modules had violations, some of them load-bearing. Had a fitness function been running continuously, each violation would have been caught within hours of being introduced, when only one engineer's change was involved. The cost of fixing a violation is O(1) when caught immediately; it grows roughly linearly with the number of changes stacked on top of it.
</Inset>

<Quiz
  id="arch-11-02-quiz-3"
  lessonSlug="02-fitness-functions-and-evolutionary-architecture"
  lang="en"
  question="After fixing the 23 boundary violations, the billing platform team considers adding fitness functions. A junior engineer asks: 'Can we just add the fitness functions now without refactoring the violations? Then the fitness functions will fail and we can fix them as we go.' Why is this reasoning flawed?"
  choices={[
    { label: "The reasoning is flawed because a failing fitness function in CI blocks all merges — not just the merges that introduce new violations. If the fitness function is added to a codebase that already has 23 violations, CI will fail on every PR from every engineer until all 23 violations are fixed. This paralyzes the team: no feature work can merge while the architectural violations exist. The correct approach is to fix the violations first, add the fitness function to a clean codebase, and then the fitness function catches future violations without blocking current work.", correct: true },
    { label: "The reasoning is correct. Adding the fitness function first creates visible pressure to fix the violations. The failing build motivates the team to prioritize the fixes.", misconception: "Visible pressure is a poor substitute for prioritized work. If the build has been failing for three weeks because of known violations, engineers will locally skip the failing test or add it to an exclusion list — fitness functions lose credibility when they are ignored for long periods. The correct approach is to fix violations before adding the gate, so the gate starts clean and stays clean. 'The build is always failing anyway' is the fastest way to destroy confidence in CI." },
    { label: "Both approaches are equivalent. What matters is that the fitness function exists, not when it was added.", misconception: "Timing matters significantly. A fitness function added to a broken codebase creates an unhealthy CI state from day one. Engineers learn that the CI failure is not meaningful (it was failing before their change). The fitness function loses its signal value. Starting clean — fix violations, add the gate, maintain green — preserves the signal: a failing fitness function means this specific change introduced a violation, not that the violations are in some historical backlog." },
    { label: "The reasoning is flawed because fitness functions cannot detect pre-existing violations — they only detect new violations introduced after the function is added.", misconception: "Fitness functions detect all violations in the current codebase, not just new ones. ArchUnit will flag every existing boundary violation the first time it runs. This is precisely why the order matters: run the fitness function on the current codebase to discover the full inventory of violations, fix them all, then add the function to CI so it starts from a clean baseline." },
  ]}
/>

</Explanation>

<KeyTakeaway>A fitness function is an automated, objective test that an architectural property still holds — it runs in CI and fails the build when violated. Atomic functions check one property (dependency rule, latency budget, no cycles); holistic functions check emergent combinations. Continuous functions run on every commit; triggered functions run on schedule or event. Together, they turn "architecture" from a convention that erodes under pressure into a machine-enforced invariant.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-11-02-retrieval"
  lang="en"
  questions={[
    { q: "What is a fitness function, and what distinguishes it from a code review checklist or an architectural document?", a: "A fitness function is an automated, objective mechanism that tests whether an architectural characteristic still holds. It produces a deterministic pass/fail without human judgment. A code review checklist depends on human review — which is inconsistent, skippable, and not continuous. An architectural document describes intent but does not measure whether the intent is being met. Only the fitness function runs on every commit, catches violations immediately when they are introduced, and cannot be bypassed without disabling the check. The key properties are: automated (no human in the loop), objective (pass/fail, not a judgment), and continuous (runs with every change)." },
    { q: "What is the difference between atomic and holistic fitness functions? Give an example of each.", a: "An atomic fitness function tests one architectural characteristic in isolation — it is fast, cheap, and focused. Example: an ArchUnit rule asserting that no class in billing.internal imports from ordering.internal. A holistic fitness function tests a combination of characteristics that only emerge together — it is slower but catches systemic failures. Example: a load test asserting that under 10,000 RPM the system maintains p99 < 200ms AND error rate < 0.1% AND no OOM errors. These properties cannot be verified independently — they emerge from the interaction of caching, database connection pooling, and GC behavior under real load." },
    { q: "Why does 'evolutionary architecture' require fitness functions rather than just good documentation and senior review?", a: "Because architecture erodes incrementally — one shortcut at a time, each individually defensible, cumulatively destructive. Good documentation and senior review fail under deadline pressure: engineers skip the checklist, reviewers approve a 'small exception,' the CI lint step gets removed during a refactor. Fitness functions do not have deadline pressure. They run on every commit, and they do not make exceptions. Evolutionary architecture is architecture that changes on purpose — the fitness functions define the envelope within which change is safe. Without them, 'evolutionary' means 'drifts until someone notices.' With them, it means 'guided incremental change that preserves the properties that matter.'" },
  ]}
/>

<Recap lang="en">
The billing platform's silent erosion — 23 boundary violations accumulated over months while CI reported green — is the failure mode fitness functions prevent. The violations were not introduced by careless engineers; they were introduced by engineers who had no immediate feedback that a boundary was being crossed.

A fitness function turns an architectural intention into a machine-enforced invariant. The ArchUnit test does not trust engineers to remember the rules; it catches violations the moment they are introduced, before any code review, before any deployment, while the change is still local and cheap to fix.

Evolutionary architecture is not architecture that drifts. It is architecture that changes on purpose, guided by fitness functions that define the properties worth preserving. The fitness function is the complement to the ADR: the ADR records why a property was chosen; the fitness function enforces that the property is still being maintained.

The practical sequence: write the ADR to record the decision, implement the fitness function to enforce it, run the fitness function continuously to catch drift. When the fitness function starts failing for a principled reason — the forces have changed, a new pattern supersedes the old — write a new ADR to supersede the original, update the fitness function, and proceed.
</Recap>
