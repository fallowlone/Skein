---
slug: "01-ports-and-adapters"
lang: en
track: "architecture-patterns"
unit: "04-hexagonal"
order: 1
title: "Ports and adapters"
summary: "In hexagonal architecture, a port is a domain-owned interface — the application defines it, not the infrastructure. An adapter is any component that implements or calls a port. This symmetry makes HTTP, CLI, and the database all 'just adapters' plugged into the domain's boundary."
estMin: 22
status: ready
lessonType: topic
level: middle
concepts:
  - port
  - adapter
  - domain-owned-interface
  - application-boundary
  - hexagonal-architecture
  - symmetry
prereqs: []
sources:
  - https://alistair.cockburn.us/hexagonal-architecture/
  - https://martinfowler.com/bliki/HexagonalArchitecture.html
  - https://www.informit.com/store/clean-architecture-a-craftsmans-guide-to-software-structure-9780134494166
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The B2B order platform had three delivery channels at launch: an HTTP REST API for the web frontend, a CLI tool for the ops team, and a batch job that ran every night pulling orders from a partner's SFTP drop. Each channel had its own copy of the order confirmation logic. When the product team added a billing approval step to the confirmation flow, three engineers updated three different code paths. Two of them got it right. The third missed the edge case for orders over $10,000. The bug lived in production for six weeks because nobody knew the CLI tool had its own confirmation logic. The core problem was not the three channels — it was that the application had no boundary. There was no single definition of "confirm an order" that every entry point called. When Alistair Cockburn described hexagonal architecture in 2005, this was exactly the problem he was trying to solve: the application needed a clear boundary that every caller — human, automated, or test — had to cross through the same interface.
</Hook>

<Crux>A port is a domain-owned interface that defines how the outside world interacts with the application; an adapter is any component that implements or calls it — UI and database are both just adapters.</Crux>

<Explanation>

## What a port actually is

The word "port" in hexagonal architecture is borrowed from hardware: a port is a socket — a defined protocol for connecting. In software, a port is an interface that the **application itself defines**. This ownership is what distinguishes hexagonal architecture from layered architecture.

In the B2B order platform, the application might define a port like this:

```typescript
// Defined inside the application — in the domain's own package
interface OrderConfirmationPort {
  confirmOrder(orderId: string, approverId: string): Promise<ConfirmationResult>;
}
```

This interface is owned by the application. It expresses what the application can do — in domain language, not in HTTP language or CLI language. The application defines the contract; the outside world must conform to it.

This is the inversion that distinguishes hexagonal from layered architecture. In a standard layered stack, the application layer calls `OrderRepository`, which is an infrastructure class — the application depends on infrastructure. In hexagonal, the application defines `IOrderRepository` as a port — infrastructure depends on the application's interface definition. The dependency arrow runs from infrastructure toward the domain, never the other way.

## What an adapter actually is

An adapter is any component that translates between the outside world and a port. An adapter either:

- **Calls a port** (the HTTP controller calls `OrderConfirmationPort.confirmOrder` when a POST request arrives)
- **Implements a port** (the `PostgresOrderRepository` implements `IOrderRepository` to fulfil the domain's data needs)

The critical insight is that these two categories are **symmetric**. The HTTP controller and the PostgreSQL repository are both adapters — one on the left side of the hexagon (calling in), one on the right side (called out). They are structurally equivalent: each one is a translation layer between an external technology and a port interface.

This symmetry is what makes the architecture's name apt. The hexagon has faces, and each face can have a port. An HTTP adapter plugs into one face; a gRPC adapter could plug into the same face (using the same port). A PostgreSQL adapter plugs into a different face; an in-memory adapter could plug into the same face. The application core does not know or care which adapters are plugged in.

<FlowDiagram
  label="Hexagonal architecture: the application core surrounded by ports and adapters"
  nodes={[
    { id: "http", label: "HTTP Controller", sub: "driving adapter (left)" },
    { id: "cli", label: "CLI Adapter", sub: "driving adapter (left)" },
    { id: "port-in", label: "OrderConfirmationPort", sub: "primary port — domain-owned" },
    { id: "core", label: "Application Core", sub: "domain + use cases" },
    { id: "port-out", label: "IOrderRepository", sub: "secondary port — domain-owned" },
    { id: "postgres", label: "PostgresOrderRepository", sub: "driven adapter (right)" },
    { id: "inmem", label: "InMemoryOrderRepository", sub: "driven adapter (right — test)" },
  ]}
  edges={[
    { from: "http", to: "port-in", label: "calls" },
    { from: "cli", to: "port-in", label: "calls" },
    { from: "port-in", to: "core", label: "drives" },
    { from: "core", to: "port-out", label: "calls (source dep)" },
    { from: "postgres", to: "port-out", label: "implements (source dep)" },
    { from: "inmem", to: "port-out", label: "implements (source dep)" },
  ]}
  caption="The application core defines both ports. Driving adapters (HTTP, CLI) call the primary port to activate the application. Driven adapters (Postgres, InMemory) implement the secondary port to fulfil the domain's infrastructure needs. Every arrow at the port boundary points toward the application — the domain owns the contract."
/>

## The symmetry: UI and database are both just adapters

This is the most practically useful consequence of hexagonal architecture. The HTTP layer is not privileged — it is just the adapter that translates HTTP requests into port calls. The database is not privileged — it is just the adapter that implements the repository port. Both are **external** to the application core.

This has an immediate structural consequence: you can swap either side independently. Swap the HTTP adapter for a gRPC adapter — the application core does not change. Swap PostgreSQL for DynamoDB — the application core does not change. Add a CLI adapter alongside the HTTP adapter — the application core does not change. This is exactly what the B2B order platform was missing: if the order confirmation logic had lived behind a single `OrderConfirmationPort`, the HTTP controller, CLI adapter, and batch job adapter would all have called the same port. One bug fix in one place.

<Inset kind="why" lang="en">
Why did Cockburn call it "hexagonal" rather than circular or square? The hexagon has no special significance — six faces is not a requirement. Cockburn chose the hexagon so diagrams would have room to draw multiple adapters on each side without the shape becoming too narrow. The meaningful shape is that the application is an *enclosed area* with explicit entry/exit points (the ports), not an open layer stack where anything can import anything. The number of faces is a drawing convenience, not an architectural constraint.
</Inset>

## Ports as the application's API and SPI

There are two categories of port, which map directly to the two sides of the hexagon:

**Primary ports (API — Application Programming Interface)** define how the outside world drives the application. They are the use cases the application exposes. In the B2B platform: `OrderConfirmationPort`, `InvoiceGenerationPort`, `BillingApprovalPort`. Driving adapters call these ports.

**Secondary ports (SPI — Service Provider Interface)** define what the application needs from infrastructure. The application calls these ports; infrastructure implements them. In the B2B platform: `IOrderRepository`, `IPaymentGateway`, `IEmailNotifier`, `IEventPublisher`. These are exactly the dependency-inverted interfaces from unit 02 — DIP applied at the architectural boundary.

The names API and SPI come from the Java ecosystem but the concept is universal. The application defines both sides: what it offers (API/primary ports) and what it requires (SPI/secondary ports). The outside world must conform to both.

<Inset kind="note" lang="en">
A common mistake is to think "the port is the HTTP endpoint" or "the port is the database table." Neither. Ports are defined in domain language. `OrderConfirmationPort.confirmOrder(orderId, approverId)` speaks order domain language — it knows nothing about HTTP verbs, JSON bodies, SQL tables, or message queue topics. The translation between HTTP-speak and domain-speak is exactly the job of the adapter. This separation — domain language at the port, technology language in the adapter — is what makes the application testable without HTTP infrastructure and replaceable without domain rewrites.
</Inset>

<Quiz
  id="arch-04-01-quiz-1"
  lessonSlug="01-ports-and-adapters"
  lang="en"
  question="The team defines IOrderRepository in the infrastructure package alongside PostgresOrderRepository. The domain's OrderService imports it from there. Is this hexagonal architecture, and why or why not?"
  choices={[
    { label: "No. In hexagonal architecture, ports are defined by the application/domain — the consumer owns the interface. If IOrderRepository lives in the infrastructure package, the infrastructure team controls it and can change it without the domain's consent. The domain now depends on an interface owned by infrastructure — the dependency arrow still points outward (domain → infrastructure package). The structural inversion has not occurred.", correct: true },
    { label: "Yes. An interface is present between the domain and infrastructure, which is the hexagonal architecture requirement.", misconception: "Having an interface is necessary but not sufficient for hexagonal architecture. The key structural requirement is port ownership: the domain must own the port definition. An interface in the infrastructure package is owned by infrastructure — the domain still depends on infrastructure's definitions. This is the same structural problem the opening story illustrated with the billing module importing the order team's interface." },
    { label: "Yes, because IOrderRepository is a port — any interface between layers qualifies as a hexagonal port.", misconception: "Not every interface is a port in the hexagonal sense. A port is specifically a domain-owned interface that defines what the application requires from or offers to the outside world, in domain language. An interface that lives in the infrastructure package, is defined for implementation convenience, and speaks in infrastructure terms (like returning a QueryBuilder) is not a hexagonal port — it is an interface-shaped infrastructure detail." },
    { label: "It depends on whether the interface methods use domain types or infrastructure types.", misconception: "Method types matter for good design, but the primary structural requirement is ownership — where the interface is defined and who controls its evolution. An interface with domain-typed methods but owned by the infrastructure package is still not a hexagonal port, because infrastructure controls when it changes. Ownership precedes naming." },
  ]}
/>

<Quiz
  id="arch-04-01-quiz-2"
  lessonSlug="01-ports-and-adapters"
  lang="en"
  question="The B2B platform has an HTTP controller, a CLI adapter, and a batch job adapter — all three activate the order confirmation use case. How should these adapters relate to the OrderConfirmationPort in hexagonal architecture?"
  choices={[
    { label: "All three adapters call the same OrderConfirmationPort. Each adapter translates its own input format (HTTP request body, CLI arguments, SFTP file row) into the domain parameters required by the port method. The application core implements confirmOrder once. Bug fixes and new logic in confirmation apply once and reach all three entry points.", correct: true },
    { label: "Each adapter should have its own port variant to match its input format — HttpOrderConfirmationPort, CliOrderConfirmationPort, BatchOrderConfirmationPort — so the application is not forced to handle adapter-specific concerns.", misconception: "Splitting ports by adapter defeats the purpose. The port is the application's definition of what it can do — in domain terms. HttpOrderConfirmationPort, CliOrderConfirmationPort, and BatchOrderConfirmationPort would all define the same use case (confirm an order) in three different places. The translation from HTTP/CLI/batch input to domain parameters is the adapter's job, not the port's job. One port; multiple adapters." },
    { label: "The HTTP controller should own the primary port since HTTP is the dominant entry point; the CLI and batch adapters call the controller internally.", misconception: "Routing CLI and batch calls through the HTTP controller to access the application's logic is a form of the original problem — now the CLI and batch adapters depend on the HTTP adapter. The application boundary is the port, not any specific adapter. All entry points call the port directly; none of them should call another adapter to reach the application." },
    { label: "The batch job adapter should be considered a secondary (driven) adapter because it runs asynchronously, while the HTTP controller and CLI are primary (driving) adapters.", misconception: "The distinction between driving and driven adapters is not about synchrony — it is about which side initiates. Driving adapters call the application (they initiate interaction). The batch job adapter initiates order processing by reading from the SFTP drop and calling the confirmation port — it is driving the application, not being driven by it. It is a primary adapter, not a secondary one. Driven/secondary adapters implement ports the application calls out to (repositories, payment gateways, notification systems)." },
  ]}
/>

<Quiz
  id="arch-04-01-quiz-3"
  lessonSlug="01-ports-and-adapters"
  lang="en"
  question="After applying hexagonal architecture to the B2B order platform, the team wants to replace PostgreSQL with a document store for the orders collection. Which components change, and which stay the same?"
  choices={[
    { label: "Only the driven adapter changes: the new DocumentOrderRepository implements the existing IOrderRepository port. The port definition, the application core (domain + use cases), and all driving adapters (HTTP, CLI, batch) are untouched. No primary port changes because the application's offered capabilities did not change.", correct: true },
    { label: "The IOrderRepository port must change because document stores have different query capabilities than relational databases.", misconception: "The port is defined in domain language — what the application needs — not in database capability language. If the document store can fulfil the same domain queries (findById, findByCustomer, save), the port interface is unchanged. If the document store cannot fulfil a domain query the current port requires, you have a capability gap to solve in the adapter, not a reason to change the port API. The port should only change when the domain's requirements change." },
    { label: "The application core changes because it must be updated to take advantage of document-store-specific features.", misconception: "Changing the application core to exploit database-specific features breaks the hexagonal boundary. The application core is allowed to know only about its ports. Features specific to the database implementation belong in the driven adapter. If you find the application core changing when you swap databases, it means the database's implementation details were leaking through the port — a sign the port was not purely domain language." },
    { label: "All components change because any data model migration requires updating the entire stack.", misconception: "A data model migration changes how data is stored and retrieved — that is infrastructure knowledge, which lives in the driven adapter. The port defines the domain view of data (findOrder returns an Order domain object). The adapter translates between the domain view and the storage format. A document store migration requires a new adapter and potentially a data migration script; it does not require touching the application core or the driving adapters." },
  ]}
/>

</Explanation>

<KeyTakeaway>A port is an interface defined and owned by the application — the domain specifies what it needs and what it offers, in domain language. An adapter is any component that crosses the port boundary, either calling a primary port (driving adapter) or implementing a secondary port (driven adapter). The symmetry — UI and database are both adapters — means either side can be replaced without touching the core.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-04-01-retrieval"
  lang="en"
  questions={[
    { q: "What makes a port a 'port' in hexagonal architecture — how is it different from any other interface?", a: "Three properties together define a hexagonal port: (1) it is owned by the application (domain or application layer), not by infrastructure or the presentation layer; (2) it speaks domain language — method names, parameter types, and return types are expressed in domain concepts, not HTTP verbs, SQL parameters, or message queue topics; (3) it defines either what the application offers to the outside world (primary port) or what the application requires from infrastructure (secondary port). An interface that lives in the infrastructure package, is shaped by infrastructure implementation concerns, or speaks database/HTTP language is not a port — it is an interface-shaped infrastructure detail, and it carries the same coupling risks as importing the concrete class." },
    { q: "The B2B platform had three adapters (HTTP, CLI, batch) each with their own copy of confirmation logic. How does hexagonal architecture fix this structurally?", a: "Hexagonal architecture introduces a single primary port — OrderConfirmationPort — owned by the application. All three driving adapters call this port. The confirmation logic exists once, inside the application core. Each adapter is responsible only for translating its input format into the domain parameters the port method requires (HTTP body parsing, CLI argument parsing, SFTP file row parsing) and for formatting the port's result back into the appropriate output format (HTTP response, terminal output, log entry). Adding a fourth entry point (say, a webhook receiver) adds one adapter and zero changes to the confirmation logic. Bug fixes apply to one place and reach all entry points." },
    { q: "What are primary ports and secondary ports, and which side of the hexagon does each sit on?", a: "Primary ports (API) sit on the left/driving side. They define what the application offers — its use cases in domain language. Driving adapters (HTTP controllers, CLI runners, message queue consumers, test drivers) call primary ports to activate application behaviour. Secondary ports (SPI) sit on the right/driven side. They define what the application requires from the outside world to do its work — repository interfaces, payment gateway interfaces, notification interfaces, event publisher interfaces. The application calls secondary ports; driven adapters implement them. The application owns both: it defines what it offers and what it needs. Adapters on both sides conform to the application's definitions." },
  ]}
/>

<Recap lang="en">
Hexagonal architecture solves the problem of the application having no clear boundary — where every entry point contains its own copy of the business logic, and a single bug fix must be replicated across multiple codebases.

A port is an interface owned by the application. It speaks domain language and defines either what the application offers (primary port) or what it requires from infrastructure (secondary port). The domain writes the contract; the outside world must conform to it.

An adapter is a translation layer between an external technology and a port. Driving adapters (HTTP controllers, CLI runners, batch jobs, test drivers) call primary ports to activate the application. Driven adapters (PostgreSQL repositories, Stripe payment clients, SendGrid email senders) implement secondary ports to fulfil the domain's infrastructure needs.

The symmetry of this arrangement — that HTTP and PostgreSQL are both just adapters on different sides — is the practical payoff. Swap the HTTP adapter for gRPC, the application core does not change. Swap PostgreSQL for a document store, the application core does not change. Add a new entry point, the confirmation logic does not duplicate. The application core is an enclosed area; the ports are its only legal entry and exit points; the adapters are the conforming connectors the outside world provides.

This architecture is the structural answer to the layering leaks from unit 03. The transaction-script trap is prevented because the port forces a defined use-case boundary. Layer-skipping is prevented because infrastructure can only reach the core through port implementations — infrastructure depends on the domain's interfaces, not the other way around. The fat service is prevented because each primary port corresponds to a bounded use case, not an open method list.
</Recap>
