infographic hierarchy-tree-lr-curved-line-rounded-rect-node
data
  title BillingProcessor — LCOM = 2
  desc Two method clusters that share no fields → split into two classes
  root
    label BillingProcessor (LCOM = 2)
    children
      - label Invoice cluster
        children
          - label generateInvoice()
          - label calculateTax()
          - label shares orderItems + customerId
      - label Email cluster
        children
          - label sendEmailNotification()
          - label formatEmailBody()
          - label shares emailAddress + emailTemplate
theme
  palette #6366f1 #14b8a6
