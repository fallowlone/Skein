---
slug: "02-monolith-to-services"
lang: en
track: "architecture-patterns"
unit: "10-decomposition"
order: 2
title: "Monolith to Services"
summary: "Decompose along bounded-context seams using the strangler fig: incrementally route features to new services while shrinking the monolith. Each service owns its data — no shared database."
estMin: 25
status: ready
lessonType: topic
level: senior
concepts:
  - strangler-fig
  - bounded-context-seam
  - database-per-service
  - decomposition-heuristic
  - data-ownership
  - incremental-extraction
prereqs: []
sources:
  - https://martinfowler.com/bliki/StranglerFigApplication.html
  - https://samnewman.io/books/monolith_to_microservices/
  - https://microservices.io/patterns/decompose/decompose-by-business-capability.html
  - https://martinfowler.com/articles/break-monolith-into-microservices.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The B2B billing platform had 200,000 lines in one Rails monolith. The CTO approved a migration to services. The team chose the straightforward approach: pick the most complex subsystem — order management — and rewrite it as a microservice. Six engineers worked for four months on the new OrderService. On launch day, they ran the new service alongside the monolith and switched traffic. Within an hour, the new service and the monolith were serving contradictory order states. The problem: the migration had moved the order API, but the order data was still in the shared database. Billing still wrote directly to the `orders` table. Inventory still read the `orders` table via its ORM. The OrderService had the API, but not ownership of the data. Two months later the project was paused. The lesson: you cannot extract a service by moving the API. You must move the API and the data ownership together, incrementally, along seams that already exist in the domain.
</Hook>

<Crux>Extract services along bounded-context seams using the strangler fig; each service owns its data — no shared database.</Crux>

<Explanation>

## Finding the seams: bounded contexts as decomposition units

The right seam for service extraction is a bounded context (unit 06): a region of the model where one consistent set of concepts and language applies. A bounded context already has a natural API surface — the translations it exports to other contexts — and a natural data boundary — the aggregates it owns.

Extracting along the wrong seam is the most common and most expensive mistake in microservices migrations. Wrong seams produce services that are tightly coupled at the data level even when they are separated at the API level. Signs of a wrong seam:

- The extracted service reads tables it doesn't own, requiring the monolith to keep those tables alive.
- Two services must coordinate on every write to maintain consistency, effectively requiring a distributed transaction.
- A single business operation requires API calls to three or four services in a fixed sequence — the services are fragments of a workflow, not autonomous capabilities.

Right seams correspond to business capabilities that can be described in one sentence with no "and": "manages the inventory of products," "handles the billing lifecycle," "tracks order status from placement to delivery." If the description requires "and," it may be two seams glued together.

<Inset kind="why">
Bounded contexts are the unit of decomposition because they are the unit of model coherence. Within a bounded context, every term has one meaning. The `Order` in the ordering bounded context is not the same `Order` in the billing bounded context — one cares about fulfillment state, the other about payment state. Splitting along this boundary means each service has its own model and its own data, with no shared understanding forced by a shared schema.
</Inset>

## The strangler fig pattern

The strangler fig (Fowler, 2004) is an incremental migration pattern named after the tropical vine that grows around an existing tree, eventually replacing it. Applied to software:

1. **Put a proxy in front of the monolith.** All traffic passes through the proxy (an API gateway, a routing layer, or a reverse proxy). The monolith handles everything initially.
2. **Extract one capability into a new service.** The new service owns a slice of the domain — the right-sized bounded context.
3. **Route that capability's traffic to the new service.** The proxy directs requests for the extracted capability to the new service. The monolith's code for that capability becomes dead.
4. **Remove the dead code from the monolith.** The monolith shrinks. Repeat for the next capability.

At any point, the system is operational. The monolith handles everything the new services don't yet handle. The migration is continuous and reversible at each step — if the new service fails, the proxy can route traffic back to the monolith's path.

<FlowDiagram
  label="Strangler fig migration: billing extracted from the B2B monolith"
  caption="Requests enter via the proxy. Billing traffic is routed to the new BillingService, which owns the billing data. All other traffic still goes to the monolith. The monolith's billing code is now dead and can be removed. The proxy is the migration control plane."
  nodes={[
    { id: "client", label: "Client", sub: "B2B platform frontend / API consumers" },
    { id: "proxy", label: "API Gateway / Proxy", sub: "routes by capability" },
    { id: "monolith", label: "Monolith", sub: "ordering, inventory, notifications — still live" },
    { id: "billing-svc", label: "BillingService", sub: "extracted — owns billing data" },
    { id: "billing-db", label: "Billing DB", sub: "owned exclusively by BillingService" },
    { id: "monolith-db", label: "Monolith DB", sub: "orders, inventory, notifications" },
  ]}
  edges={[
    { from: "client", to: "proxy", label: "all requests" },
    { from: "proxy", to: "billing-svc", label: "billing/* routes" },
    { from: "proxy", to: "monolith", label: "all other routes" },
    { from: "billing-svc", to: "billing-db", label: "owns and reads/writes" },
    { from: "monolith", to: "monolith-db", label: "reads/writes" },
  ]}
/>

## Data ownership and the database-per-service rule

The most violated rule in microservices migrations is data ownership. The strangler fig can route API traffic to the new service on day one, but migrating data ownership takes longer. Until data ownership transfers, the service is a facade, not an autonomous unit.

**Database-per-service** means each service has its own persistence store, with no other service reading or writing its tables. This is not primarily a technology choice (one PostgreSQL instance can serve multiple services on different schemas) — it is an ownership contract:

- Only BillingService writes to the `invoices` and `charges` tables.
- No other service holds an ORM model for those tables.
- Other services that need billing data call BillingService's API or consume its events.

The migration sequence for data ownership:

1. Identify all code in the monolith that reads or writes the tables being migrated.
2. Wrap those access points behind an interface in the monolith that matches the new service's API surface.
3. Deploy the new service and point the monolith's interface implementation at the new service's API.
4. Migrate the data to the new schema.
5. Remove the monolith's tables (or schema-lock them so writes are rejected).

<Quiz
  id="arch-10-02-quiz-1"
  lessonSlug="02-monolith-to-services"
  lang="en"
  question="A team uses the strangler fig to extract InventoryService from the B2B monolith. After two months, InventoryService handles all inventory API traffic. However, BillingService (still in the monolith) continues to read the `inventory_reservations` table directly via the shared ORM. The team lead says: 'The extraction is complete — all API traffic goes to InventoryService.' A senior engineer says the extraction is not complete. Who is right and why?"
  choices={[
    { label: "The senior engineer is right. The extraction is incomplete because data ownership has not transferred. BillingService's direct ORM access to `inventory_reservations` means InventoryService does not exclusively own its data — it cannot safely change the schema, split the table, or move to a different database. The strangler fig routes API traffic, but a service is not autonomous until it owns its persistence. The extraction is complete only when no code outside InventoryService reads or writes the inventory tables.", correct: true },
    { label: "The team lead is right. API traffic routing is the definition of service extraction. Data access is a performance optimization, not an ownership concern.", misconception: "API traffic routing is a necessary but not sufficient condition for service extraction. If BillingService reads the `inventory_reservations` table directly, InventoryService cannot migrate its schema, move to a different database, or optimize its data model without breaking billing. Data ownership is what makes a service autonomous — not API traffic routing." },
    { label: "Both are partially right. The API extraction is complete; the data migration is a separate, independent project that doesn't affect the service's autonomy.", misconception: "Data ownership and API routing are not independent. A service that does not own its data is not autonomous. Other services reading its tables create coupling at the data layer that is more dangerous than API coupling — schema changes are harder to version than API changes, and the coupling is invisible until a migration breaks something." },
    { label: "The senior engineer is correct, but the fix is simple: just restrict ORM access with a read-only database user for BillingService.", misconception: "A read-only user restricts writes but not schema coupling. BillingService's ORM model for `inventory_reservations` still couples it to the table's column names, types, and relationships. When InventoryService needs to rename a column, split a table, or add a non-nullable column, it must coordinate with billing — the coupling is structural, not just about writes." },
  ]}
/>

## Decompose by capability, not by layer

The antipattern that produces wrong seams is decomposing the monolith by technical layer: splitting the presentation layer, the business logic layer, and the data layer into separate services. This creates three services that implement one business capability — they are fragments of a workflow, not autonomous capabilities.

A service extracted by layer:
- Cannot be independently deployed (the logic service depends on the data service for every operation).
- Cannot be independently tested (the logic service has no behavior without the data service).
- Forces synchronous coupling for every request (three network hops where zero were needed).

A service extracted by business capability owns its full slice of the stack: its API, its logic, and its persistence. It is the vertical slice architectural approach applied to service decomposition.

<Quiz
  id="arch-10-02-quiz-2"
  lessonSlug="02-monolith-to-services"
  lang="en"
  question="An architect proposes decomposing the B2B billing monolith into three services: DataService (all database access), BusinessLogicService (all domain rules), and ApiService (all HTTP endpoints). Each request from the frontend would go to ApiService, which calls BusinessLogicService, which calls DataService. What structural problems does this decomposition create?"
  choices={[
    { label: "This is decomposition by layer, not by capability. Every request requires three synchronous network hops instead of zero. None of the three services can be independently deployed — deploying a new business rule requires a coordinated deployment of all three. None can be independently tested — BusinessLogicService has no behavior without DataService, and ApiService has no behavior without both. The decomposition has created the operational complexity of microservices (three deployable units, network failures, versioning) with none of the autonomy benefits (each service is still coupled to the others for every request).", correct: true },
    { label: "The main problem is that three services are not enough for a B2B platform. More services are needed to achieve real independence.", misconception: "The problem is the decomposition axis, not the number of services. Even with ten services decomposed by layer, the same structural problems apply: every request crosses multiple services, all must deploy together, and none can be tested independently. The solution is to decompose by business capability, not to add more layers." },
    { label: "The decomposition is correct. This is how service-oriented architecture (SOA) is supposed to work — with distinct service tiers for presentation, logic, and data.", misconception: "Classic SOA tiers are a deployment and scaling pattern, not an autonomy pattern. A three-tier SOA architecture still couples tiers for every request — it improves horizontal scaling but not service autonomy. Microservices' value proposition is autonomous deployability and independent failure isolation, which requires capability-based, not layer-based, decomposition." },
    { label: "The DataService creates a single point of failure but otherwise the architecture is sound. The fix is to add a DataService replica.", misconception: "Adding a DataService replica addresses availability but not the structural problems. The coupling between services (BusinessLogicService depends on DataService for every operation) is not addressed by replication. The real problem is that the three services implement one capability — they must act in concert for any request — making the 'microservice' label misleading." },
  ]}
/>

## Sequence: what to extract first

Not all modules are equally good starting points for extraction. The strangler fig works best when the first extracted service:

**Has low coupling to other monolith internals.** If the billing capability only needs to call two internal APIs (and those calls can be wrapped behind interfaces), extraction is relatively contained. If it shares tables with every other module, extraction requires migrating every access point simultaneously.

**Has high change frequency.** A capability that releases changes twice a week while everything else releases monthly gains the most from independent deployment. Extracting a stable, rarely-changed capability doesn't move the needle on deployment agility.

**Is already modular internally.** If the ordering capability in the monolith is a coherent module with a clear internal structure and owned tables, extraction is mostly plumbing. If it is entangled with shared code and tables, extraction requires refactoring before extraction.

**Has clear ownership.** A team that already owns the capability can own its service. Cross-team shared code is the hardest to extract because no one team can authorize the removal of the shared code.

<Quiz
  id="arch-10-02-quiz-3"
  lessonSlug="02-monolith-to-services"
  lang="en"
  question="The B2B billing platform is planning strangler fig extractions. The team has two candidates: (A) Notification module — sends emails and SMS, releases rarely, is called by every other module via a shared NotificationService class, reads no tables of its own (writes to `notifications` table), and its code is clean and self-contained. (B) Reporting module — generates invoices and billing reports, releases frequently, reads the `orders`, `invoices`, `customers`, and `charges` tables directly via ORM, and its code is heavily entangled with billing logic. Which module should be extracted first and why?"
  choices={[
    { label: "Module A (Notifications) should be extracted first. Despite being called by every module, the coupling is inbound (other modules call it) — not outbound (it doesn't depend on other modules' internals). Extracting it requires adding an HTTP client or event subscription in each caller module, but the notification module itself reads nothing it doesn't own and writes only its own table. The extraction is contained to one data table and one call interface. Module B (Reporting) has deep outbound data coupling — it reads four tables owned by other contexts. Before extracting it, all four direct table accesses must be replaced with API calls to the owning services, which is a larger scope of work that should happen after the simpler extraction builds the team's migration capability.", correct: true },
    { label: "Module B (Reporting) should be extracted first because it releases frequently and gaining independent deployment will have more business impact.", misconception: "Release frequency determines priority among extractions of similar difficulty, not across extractions of very different difficulty. Module B's deep table coupling means extraction requires migrating four sets of data access before any traffic can be routed to the new service. Attempting a complex extraction first risks failure and demoralizes the team. The strangler fig pattern advises starting with lower-coupling extractions to build capability and confidence." },
    { label: "Neither module should be extracted first. The monolith should be refactored internally before any extraction.", misconception: "Internal refactoring and strangler fig extraction are not mutually exclusive phases. The strangler fig specifically avoids requiring a full internal refactor before any extraction — it enables incremental migration by routing traffic. Module A's extraction requires only a proxy configuration change and adding HTTP clients in callers, with no internal monolith refactoring required." },
    { label: "Both modules should be extracted simultaneously to save time.", misconception: "Parallel extractions multiply the risk during migration. If both extractions encounter problems simultaneously, the team must debug two service integrations at once while the monolith is partially dismantled. The strangler fig is explicitly sequential and reversible: extract one capability, validate it in production, then proceed. Parallelism defeats the risk-reduction property of the pattern." },
  ]}
/>

</Explanation>

<KeyTakeaway>Decompose by business capability along bounded-context seams, using the strangler fig to route traffic incrementally. Each service must own its data — no shared database. Migrate API routing and data ownership together; routing alone produces a facade, not a service.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-10-02-retrieval"
  lang="en"
  questions={[
    { q: "What is the strangler fig pattern and why does it avoid the 'big bang' rewrite problem?", a: "The strangler fig is an incremental migration pattern: put a proxy in front of the monolith, extract one capability at a time into a new service, route that capability's traffic to the new service via the proxy, remove the dead code from the monolith, and repeat. It avoids the big bang rewrite problem because the system is always operational — the monolith handles everything the new services don't yet cover. Each extraction is independently deployable and reversible: if the new service fails, the proxy can route traffic back to the monolith. This means mistakes are caught in production with limited blast radius, rather than all at once on a single cutover day." },
    { q: "Why is 'database-per-service' an ownership contract, not just a technology choice?", a: "Database-per-service is an ownership rule: only one service writes to a given set of tables. Any other service that reads or writes those tables creates schema coupling — the owning service cannot change its schema without coordinating with every consumer. The ownership contract is independent of technology: a single PostgreSQL instance can serve multiple services on different schemas, each with its own ownership. The contract is violated when another service's ORM model points at a table owned by a different service. Technology isolation (separate instances) enforces the contract mechanically, but even on shared infrastructure the contract must hold." },
    { q: "What is wrong with decomposing a monolith by technical layer (presentation / logic / data as three services)?", a: "Layer decomposition creates services that are not autonomous: every request requires a coordinated chain of calls across all three services. None can be independently deployed (a business logic change requires deploying all three), and none can be independently tested (the logic service has no behavior without the data service). The operational complexity of microservices — three deployable units, network failures, versioning — is incurred with none of the autonomy benefits. Correct decomposition is by business capability: each service owns its full vertical slice — API, logic, and persistence — and can be deployed, scaled, and failed independently." },
  ]}
/>

<Recap lang="en">
The billing platform's failed extraction moved the API without moving the data. The monolith's ORM still owned the tables; the new OrderService had the endpoint but not the data. Every write was a race condition between the service and the monolith's leftover code. The lesson: a service is not autonomous until it owns its persistence.

The strangler fig solves this incrementally. A proxy in front of the monolith routes traffic slice by slice to new services. Each slice is extracted with its data ownership transferred — the monolith's direct table access replaced with API calls to the new service — before the next slice begins. At any point, the system is working; the monolith is simply smaller.

The right seam for extraction is a bounded context: a business capability that can be named without "and." Decomposing by technical layer — splitting presentation, logic, and data into separate services — produces the worst possible outcome: distributed coupling with no autonomy. Decomposing by capability produces services that can be deployed, tested, and failed in isolation.

Start with the capability that has the lowest outbound coupling and the highest change frequency. Build the muscle for strangler fig extractions on an easy case, then apply it to the harder ones.
</Recap>
