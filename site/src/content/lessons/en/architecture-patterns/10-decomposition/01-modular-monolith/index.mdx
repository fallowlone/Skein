---
slug: "01-modular-monolith"
lang: en
track: "architecture-patterns"
unit: "10-decomposition"
order: 1
title: "The Modular Monolith"
summary: "A modular monolith enforces hard seams between features inside a single deployable. Package-by-feature, not by layer. Clean modules are the prerequisite for any future extraction — you earn the right to split."
estMin: 22
status: ready
lessonType: topic
level: senior
concepts:
  - modular-monolith
  - package-by-feature
  - module-boundary
  - monolith-first
  - coupling
  - seam
prereqs: []
sources:
  - https://martinfowler.com/bliki/MonolithFirst.html
  - https://samnewman.io/books/building_microservices_2nd_edition/
  - https://www.kamilgrzybek.com/design/modular-monolith-primer/
  - https://martinfowler.com/bliki/MicroservicePremium.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import boundaryCost from "./infographics/boundary-cost.svg?raw";

<Hook>
The B2B billing platform started as a single Rails app organized by layer: `app/models/`, `app/controllers/`, `app/services/`. Eighteen months in, the team had 60 developers and a deploy that took 40 minutes. Every change to the billing logic required touching files in `models/`, `services/`, and sometimes `controllers/`. Nobody could tell which service classes were safe to call from billing without checking three directories. A tech lead proposed splitting into microservices. Management approved. Six months later, the team had three services that deployed in lockstep, shared a database, and were harder to change than the original monolith. What went wrong? They broke apart a mudball and got a distributed mudball. The modular structure they needed was internal discipline, not a network boundary.
</Hook>

<Crux>A modular monolith enforces seams inside one deployable — package by feature, not layer — earning the right to split later.</Crux>

<Explanation>

## Package-by-layer vs package-by-feature

The classic Rails / Spring / Django scaffold organizes code by technical role: `models/`, `controllers/`, `services/`, `repositories/`. This is package-by-layer. It feels clean at project start. It becomes a liability at scale.

**Package-by-layer** groups every model from every domain together. `OrderService` and `InvoiceService` and `CustomerService` all live in `services/`. When you want to understand the ordering domain, you read across four directories. When you add a feature to ordering, you scatter changes across the same four directories. Every domain change touches every layer. The coupling is invisible: nothing prevents `InvoiceService` from calling `OrderRepository` directly.

**Package-by-feature** (or package-by-domain) groups everything for one business capability together. The `ordering/` module contains its own models, its own services, its own repositories, its own API surface. The `billing/` module is a peer with its own internals. Cross-module access is explicit and controlled.

<FlowDiagram
  label="Package-by-layer vs package-by-feature for the B2B platform"
  caption="Package-by-layer scatters one domain across four directories. Package-by-feature keeps one domain entirely inside one module with a controlled public boundary. Cross-module calls only happen at the boundary, not across internal layers."
  nodes={[
    { id: "layer-models", label: "models/", sub: "Order, Invoice, Customer (all mixed)" },
    { id: "layer-services", label: "services/", sub: "OrderSvc, InvoiceSvc, CustomerSvc (all mixed)" },
    { id: "layer-repos", label: "repositories/", sub: "OrderRepo, InvoiceRepo, CustomerRepo (all mixed)" },
    { id: "feat-ordering", label: "ordering/", sub: "Order, OrderService, OrderRepo — internal to module" },
    { id: "feat-billing", label: "billing/", sub: "Invoice, BillingService, InvoiceRepo — internal to module" },
    { id: "feat-boundary", label: "ordering/api", sub: "public surface — only entry point for other modules" },
  ]}
  edges={[
    { from: "layer-services", to: "layer-models", label: "unrestricted access" },
    { from: "layer-services", to: "layer-repos", label: "unrestricted access" },
    { from: "feat-billing", to: "feat-boundary", label: "explicit call through boundary" },
    { from: "feat-boundary", to: "feat-ordering", label: "enters module at public API" },
  ]}
/>

## Enforcing module boundaries

The modular monolith earns its name from enforcement. Packaging files into subdirectories is not a module boundary. A boundary must be enforced so that violations are a build error, not just a code-review comment.

Enforcement mechanisms by language/ecosystem:

- **Java modules (JPMS):** `module-info.java` declares what is exported and what is requires. Attempts to access an unexported package fail at compile time.
- **TypeScript with `@typescript-eslint/no-restricted-imports`:** configure eslint rules to ban cross-module imports except through index re-exports. Violations fail the lint gate.
- **NestJS modules:** `@Module({ exports: [...] })` makes explicit what the module surface is. Injectable services not in `exports` are invisible to outside modules.
- **Go packages with tooling (`golang.org/x/tools/go/analysis`):** custom analysis passes can assert that no package in `internal/ordering/` is imported directly from `internal/billing/`.
- **ArchUnit (Java) / `ts-arch` (TypeScript):** architecture tests that assert dependency rules. Fail the test suite if `billing` imports a non-public class from `ordering`.

The key insight is that the mechanism must be automated. If enforcing the boundary requires human review, it will erode under deadline pressure. The modular monolith is a commitment to automation.

<Inset kind="why">
Why does the boundary need to be inside a single deployable and not a network boundary? Because a network boundary introduces latency, distributed failure modes, and serialization overhead for every cross-module call. An internal module boundary costs nothing at runtime: it is a compile-time or test-time constraint. You can experiment with the shape of the boundary cheaply. If you later extract a module into a service, the boundary is already defined and tested — the extraction is a mechanical operation, not an architectural redesign.
</Inset>

<Infographic
  svg={boundaryCost}
  label="Side-by-side comparison of an internal module boundary versus a network boundary. The internal boundary has zero runtime cost (a compile-time or test-time constraint), is reshaped cheaply, and makes later extraction a mechanical split. The network boundary adds latency and failure modes, locks the seam into API and team contracts, and is expensive to reshape."
  caption="The same seam is nearly free inside one deployable but expensive once it crosses the network — which is why you experiment with the boundary cheaply first and only then earn the right to split."
  hue="var(--d-infra)"
/>

## The Fowler MonolithFirst default

Martin Fowler's "MonolithFirst" argument is not a preference for monoliths. It is a statement about information: at the start of a project, you do not know where the right service boundaries are. Domain boundaries are discovered through use, not designed upfront.

The failure mode Fowler observed repeatedly: teams start with microservices because "we know we'll need to scale," decompose prematurely along the wrong seams, and then face two problems simultaneously — building the product and refactoring the service boundaries. The cost of a wrong microservice boundary is high: shared data, API versioning, distributed transactions, and two teams who now must coordinate every schema change.

The modular monolith is the intermediate position: commit to clean internal structure now, defer the distribution decision until the domain is understood. When you do split, you split along seams that have been validated by real use — not speculated upfront.

<Quiz
  id="arch-10-01-quiz-1"
  lessonSlug="01-modular-monolith"
  lang="en"
  question="A team architect proposes: 'We should split into microservices now, while the codebase is small. It will be harder to split later when there is more code.' A senior engineer disagrees. What is the specific flaw in the architect's reasoning?"
  choices={[
    { label: "The flaw is that splitting early doesn't reduce the cost of splitting — it increases it. The cost of a wrong microservice boundary is high: once two services communicate over a network, changing that boundary requires API versioning, data migration, and cross-team coordination. Splitting while the domain is small and poorly understood guarantees the boundaries will be wrong. The MonolithFirst argument is precisely that boundaries are discovered through use, not designed upfront. A modular monolith lets you discover the right seams cheaply, then split along them when you have evidence.", correct: true },
    { label: "The architect is correct. Splitting early is always easier because there is less code to migrate.", misconception: "This ignores the cost of wrong boundaries. Splitting early with an unknown domain doesn't reduce migration cost — it locks in premature decisions. The code volume is not the primary cost; the boundary correctness is. A wrong boundary in a microservice is expensive to fix because the split is now encoded in network contracts, deployment pipelines, and team ownership." },
    { label: "The architect is wrong because microservices always add more complexity than they remove for any team smaller than 100 engineers.", misconception: "Team size is a factor in the decision, but the core issue is domain knowledge, not headcount. The MonolithFirst argument is about information availability: you don't know where the right boundaries are until the domain is understood through real use. A small team with a well-understood domain might reasonably split earlier; a large team exploring a new domain should defer." },
    { label: "The architect is correct for greenfield projects. MonolithFirst only applies to legacy codebases being migrated.", misconception: "MonolithFirst was specifically formulated for greenfield projects. Fowler's observation was that teams building new systems premature-optimize for distribution. Legacy migration has different forces — the modular monolith can still apply as an intermediate step (strangler fig with modular internal structure), but the original argument is about new systems." },
  ]}
/>

## What makes a good module boundary

Not every subdirectory is a module. A good module boundary is defined by:

**Explicit public API.** The module exposes a surface — a set of types, functions, or interfaces — and hides its internals. Callers depend on the public API, not on implementation classes. When the internal implementation changes, only the module's own code changes.

**Single business capability.** A module should map to one bounded context (unit 06) or one business capability. `ordering/` handles the lifecycle of an order. `billing/` handles invoicing and payment. `inventory/` handles stock. Mixed-capability modules — `ordering-and-billing/` — betray that the seam is wrong.

**Independent test suite.** A module can be tested without standing up other modules. If testing `billing` requires importing live code from `ordering`, the dependency is wrong — `billing` should depend on an interface or a test double for what it needs from `ordering`.

**Low afferent coupling on internals.** The module's internal classes are not imported by anything outside the module. All external access flows through the public API. If the internal `Order` class is imported directly by `BillingService`, the boundary is already broken.

<Quiz
  id="arch-10-01-quiz-2"
  lessonSlug="01-modular-monolith"
  lang="en"
  question="The billing platform's codebase has an `ordering/` module and a `billing/` module. A code review reveals: BillingService imports OrderRepository from `ordering/repositories/OrderRepository` directly, bypassing the `ordering/api` public surface. An engineer argues: 'It's just a read — we're not modifying ordering data, so the boundary isn't really violated.' What is the structural problem with this reasoning?"
  choices={[
    { label: "The structural problem is that boundary violations are not about read vs write — they are about coupling. BillingService now directly depends on OrderRepository's interface, package location, and internal implementation. If the ordering team changes OrderRepository's method signatures, renames the class, or restructures its internals, BillingService breaks — even though billing is supposedly a separate module. The boundary's purpose is to let each module's internals change independently. A read-only direct import is still a direct import: it creates the same coupling as a write, just on fewer operations.", correct: true },
    { label: "The engineer is correct. Read-only access does not violate module boundaries because it doesn't affect ordering's state.", misconception: "Boundary violations are about coupling, not state mutation. Reading OrderRepository directly couples BillingService to OrderRepository's interface and location. The ordering module can no longer refactor its repository layer without checking what billing depends on. The boundary exists to allow independent evolution, not to prevent state corruption." },
    { label: "The problem is that BillingService should not exist in the billing module — billing logic should be in the ordering module since it depends on order data.", misconception: "This conflates data access with module ownership. BillingService depending on order data doesn't mean billing belongs in ordering. The correct fix is for BillingService to depend on the ordering module's public API — a method like OrderApi.getOrderSummary() — which the ordering module controls and keeps stable. Merging modules because one reads from another would produce the monolith anti-pattern." },
    { label: "The problem is a circular dependency: if billing reads from ordering, ordering must also read from billing, creating a cycle.", misconception: "A cycle is not implied. One-way dependency (billing → ordering's public API) is correct and does not imply a reverse dependency. The issue is that billing is accessing ordering's internals, not the direction of dependency. Cycles would be a separate problem — detected by ADP (unit 02)." },
  ]}
/>

## When to split: earning the right

The modular monolith is not a permanent destination. It is the state you should be in before you split. The split is earned, not granted.

Signs you have earned the right to extract a module into a service:

- The module's public API is stable and has been used by multiple callers without changing contract.
- The module has its own data store (even within a shared DB, its tables are owned exclusively by the module).
- The module's test suite is independent — it can be run in isolation.
- The module's deployment cadence is clearly different from other modules (it needs to release multiple times a week while others release monthly).
- The team owning the module is large enough that communication overhead across the module boundary is now more expensive than the deployment boundary.

If these conditions are not met, the split will be premature and the service boundary will be wrong. The most expensive mistake in microservices is not staying monolithic too long — it is splitting too early along the wrong seams.

<Quiz
  id="arch-10-01-quiz-3"
  lessonSlug="01-modular-monolith"
  lang="en"
  question="The billing platform's inventory module has been in production for six months. The team considers extracting it as a service. An engineer reviews the module and finds: (a) InventoryService.getAvailableStock() is imported by both OrderService and BillingService through the public API. (b) The inventory module's database tables are also queried directly by BillingService via a shared ORM connection. (c) The inventory test suite requires a running OrderService to mock some edge cases. Which of these findings should block the extraction, and why?"
  choices={[
    { label: "Findings (b) and (c) should block extraction. (b) reveals data coupling: if inventory is extracted as a service, BillingService can no longer query its tables directly — it must call the service's API. All queries currently bypassing the module boundary must be migrated before extraction, which is a data ownership problem that must be solved in the monolith first. (c) reveals a test dependency that means the module is not independently testable — a prerequisite for service extraction. (a) is not a problem: multiple callers using the public API is exactly the pattern that validates the boundary is correct.", correct: true },
    { label: "All three findings should block extraction. (a) shows too many dependents on the module, making the API surface too large to safely version.", misconception: "Multiple callers using the public API is a sign the boundary is correct and stable — it validates the API surface, not increases extraction risk. The number of callers does not block extraction; the stability of the API contract is what matters. Findings (b) and (c) are the actual blockers." },
    { label: "Only finding (b) blocks extraction. Database coupling is the only hard dependency; test dependencies can be fixed after extraction.", misconception: "An untestable module in isolation becomes an untestable service in isolation — the test dependency reveals real runtime coupling. Fixing test dependencies after extraction means you will discover the coupling as production failures. Both (b) and (c) must be resolved before extraction." },
    { label: "None of the findings should block extraction. These are all issues that can be resolved in parallel with the extraction process.", misconception: "Resolving database coupling and test isolation in parallel with extraction means you are simultaneously refactoring and distributing — the highest-risk combination in software engineering. The modular monolith pattern specifically requires resolving these blockers before crossing the network boundary, because once the service is deployed, fixing them requires coordinated API migration and data ownership transfer." },
  ]}
/>

</Explanation>

<KeyTakeaway>A modular monolith enforces hard, automated seams between business capabilities inside one deployable. Package-by-feature, enforce boundaries at build time, and make each module independently testable. You earn the right to extract a service by having a stable public API, owned data, and isolated tests — not by planning for future scale.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="arch-10-01-retrieval"
  lang="en"
  questions={[
    { q: "What is the core difference between package-by-layer and package-by-feature, and why does it matter for module boundaries?", a: "Package-by-layer groups all code of the same technical role together (all models, all services, all repositories) across every business domain. This makes cross-domain dependencies invisible — OrderService can import CustomerRepository with no friction. Package-by-feature groups all code for one business capability together. The boundary between modules is explicit: only the public API surface is accessible. This matters because module boundaries can only be enforced when code is organized by the unit you want to isolate — the business capability — not by technical role. Layer organization makes it structurally impossible to enforce a domain boundary." },
    { q: "What does 'enforcing module boundaries' mean in practice, and why must enforcement be automated?", a: "Enforcing a module boundary means making it a build error or test failure to access a module's internals from outside the module. Mechanisms include JPMS module descriptors in Java, ESLint import restriction rules in TypeScript, NestJS module exports, and architecture tests (ArchUnit, ts-arch) that assert dependency rules. Enforcement must be automated because human code review erodes under deadline pressure. A boundary that can be violated without a CI failure is a convention, not a contract — and conventions are not module boundaries." },
    { q: "What are the signs that a module has 'earned the right' to be extracted as a microservice?", a: "The module has a stable public API that has been used without contract changes. The module owns its data — even within a shared database, its tables are exclusively written by the module's own code. The module's test suite runs in isolation without requiring other modules to be running. The module's deployment cadence is genuinely different from other modules. The team ownership overhead now exceeds the network boundary overhead. All five conditions should hold before extraction — meeting only some of them means the extraction will create a distributed coupling problem that is harder to fix than staying modular." },
  ]}
/>

<Recap lang="en">
The billing platform's failed microservices migration broke apart a mud ball and got a distributed mud ball. The missing ingredient was not a network boundary — it was internal discipline. A modular monolith provides that discipline: hard seams between business capabilities, enforced by build tooling, organized by feature rather than layer.

The MonolithFirst pattern is not nostalgia for the monolith. It is a statement about information: you do not know the right service boundaries until the domain is understood through production use. Splitting prematurely locks in wrong boundaries at high cost. Splitting after a period of modular monolith discipline means splitting along seams that have been validated by real callers, stable APIs, and independent test suites.

The module boundary inside a deployable costs nothing at runtime — it is a compile-time or test-time constraint. It can be reshaped cheaply. Once it becomes a network boundary, reshaping it requires API versioning, data migration, and cross-team coordination. The modular monolith is the phase where you do the cheap experimentation so that the network boundary, when it comes, is placed correctly.
</Recap>
