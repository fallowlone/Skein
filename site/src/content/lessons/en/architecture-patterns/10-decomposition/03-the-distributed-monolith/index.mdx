---
slug: "03-the-distributed-monolith"
lang: en
track: "architecture-patterns"
unit: "10-decomposition"
order: 3
title: "The Distributed Monolith"
summary: "Services that deploy in lockstep, call each other synchronously across N hops, and share a database are a distributed monolith: every operational cost of distribution with none of the autonomy. The dual-write problem and temporal coupling are its defining symptoms."
estMin: 22
status: ready
lessonType: topic
level: senior
concepts:
  - distributed-monolith
  - lockstep-deployment
  - temporal-coupling
  - shared-database
  - dual-write
  - chatty-services
prereqs: []
sources:
  - https://samnewman.io/books/building_microservices_2nd_edition/
  - https://martinfowler.com/bliki/MicroservicePremium.html
  - https://microservices.io/patterns/data/shared-database.html
  - https://www.infoq.com/articles/microservices-anti-patterns/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Eighteen months after the B2B billing platform split into "microservices," the on-call rotation was miserable. A single order-placement request now touched five services in sequence: ApiGateway called OrderService, which called InventoryService, which called BillingService, which called NotificationService. Each hop added 20–40ms of latency and a new failure mode. If NotificationService was slow, order placement timed out. The team could not deploy BillingService without coordinating with OrderService because the two services expected the same version of the `Order` schema — which lived in a shared database both services wrote to. Every deploy required a Slack thread to confirm ordering. A post-mortem revealed the root cause: they had split the monolith by technical layer (API, logic, data as separate services), not by business capability. They had five deployable units, five on-call pages, five sets of deployment pipelines, and five times the network surface area — with zero independent deployability. What they built was a distributed monolith.
</Hook>

<Crux>Lockstep deploys and chatty sync chains mean you built a distributed monolith: all the cost of distribution, none of the benefit.</Crux>

<Explanation>

## What makes a distributed monolith

A distributed monolith is a set of services that appear independent but are tightly coupled at runtime and at deploy time. It combines the worst properties of both architectures:

- **From the monolith:** a change to one service requires changes and coordinated deploys across many services; the codebase is hard to understand as a whole because the coupling is implicit (network calls and shared schema, not visible imports).
- **From distributed systems:** network latency on every inter-service call, partial failure modes that are harder to debug than process-level exceptions, and the operational overhead of multiple deployable units.

The diagnostic question is not "how many services do we have?" — it is "can each service be deployed and failed independently?" If the answer is no, you have a distributed monolith regardless of how many services the architecture diagram shows.

Three structural symptoms define the pattern:

**1. Lockstep deployment.** Services must be deployed together, or in a specific sequence, because they share a data schema or API contract that is not versioned. A change to the `orders` table requires coordinating OrderService, BillingService, and InventoryService in the same deploy window.

**2. Chatty synchronous call chains.** A single client request fans out to N synchronous hops through services that cannot respond until downstream services respond. Each hop adds latency and failure probability. Under load, a slow service at the end of the chain produces timeouts that cascade upstream.

**3. Shared database.** Multiple services read and write the same tables via a shared ORM or shared connection pool. No service exclusively owns its data; schema changes affect every service that queries the changed table.

<FlowDiagram
  label="Distributed monolith: one order-placement request, five synchronous hops"
  caption="A single client request fans out through five services in sequence. Each hop adds latency and a failure point. NotificationService at the end of the chain can cause the entire order placement to time out. No service can be deployed independently because all share the orders schema."
  nodes={[
    { id: "client", label: "Client", sub: "places order" },
    { id: "api", label: "ApiGateway", sub: "routes request" },
    { id: "order", label: "OrderService", sub: "creates order" },
    { id: "inventory", label: "InventoryService", sub: "reserves stock" },
    { id: "billing", label: "BillingService", sub: "charges payment" },
    { id: "notify", label: "NotificationService", sub: "sends confirmation" },
    { id: "shared-db", label: "Shared DB", sub: "orders, charges, inventory — all tables" },
  ]}
  edges={[
    { from: "client", to: "api", label: "POST /orders" },
    { from: "api", to: "order", label: "sync call" },
    { from: "order", to: "inventory", label: "sync call" },
    { from: "inventory", to: "billing", label: "sync call" },
    { from: "billing", to: "notify", label: "sync call" },
    { from: "order", to: "shared-db", label: "reads/writes orders table" },
    { from: "billing", to: "shared-db", label: "reads/writes charges table" },
  ]}
/>

## The dual-write problem

The shared database produces a structural defect that surfaces as data inconsistency: the dual-write problem.

Consider BillingService creating a charge and updating an order status in sequence:

```
1. INSERT INTO charges (order_id, amount) VALUES (123, 450.00)
2. UPDATE orders SET status = 'charged' WHERE id = 123
```

Both statements succeed locally. But `orders` is also written by OrderService. If OrderService concurrently writes `status = 'cancelled'` after step 1 but before step 2, the order ends up in the `charged` state even though it was cancelled. BillingService and OrderService each wrote their local view of the truth to the same table, with no owner enforcing consistency.

The dual-write problem cannot be fully solved by transactions when the writes span services: a transaction that wraps both services' writes is a distributed transaction — which reintroduces 2PC and its failure modes (unit 09). The correct fix is to give each service exclusive ownership of its tables, eliminating the possibility of concurrent writes from different services to the same record.

<Inset kind="why">
The dual-write problem is not a bug in the application code — it is a structural consequence of shared data ownership. No amount of careful sequencing eliminates it when two services write to the same table, because the sequencing guarantee requires a distributed lock, which is 2PC in disguise. The only structural fix is exclusive data ownership per service.
</Inset>

<Quiz
  id="arch-10-03-quiz-1"
  lessonSlug="03-the-distributed-monolith"
  lang="en"
  question="The billing platform's BillingService and OrderService both write to the shared `orders` table. A developer proposes fixing the dual-write inconsistency by always writing in a fixed order: OrderService writes first, then BillingService reads OrderService's write before making its own write. What is the structural flaw in this approach?"
  choices={[
    { label: "Fixed write ordering does not prevent concurrent writes — it only establishes a convention. If two concurrent requests execute (one order cancellation from OrderService, one charge from BillingService), both threads will follow the fixed order independently, but their writes can still interleave. Thread A (OrderService): sets status='cancelled'. Thread B (BillingService): reads status (sees 'charged'), writes status='charged'. Final state: charged, even though the cancellation committed first. Enforcing ordering between concurrent threads across different services requires a distributed lock, which is 2PC. The structural fix is exclusive ownership: only one service writes to any given table.", correct: true },
    { label: "The approach is correct. Fixed write ordering eliminates concurrent write conflicts by serializing all writes through a defined sequence.", misconception: "Fixed ordering is a convention among developers, not an enforced ordering in the database. Two concurrent requests — one order cancellation and one charge — each follow the convention independently. Their interleaved execution still produces race conditions. Only a distributed lock (2PC) or exclusive data ownership eliminates this; fixed ordering does neither." },
    { label: "The approach works for most cases but fails during service restarts. The fix is to add retry logic.", misconception: "Service restarts are not the failure mode. The race condition occurs between concurrent normal requests — no restart required. Retries would not fix a race condition where both writers succeed but produce an inconsistent final state; retrying a successful write may worsen the inconsistency by applying a stale value again." },
    { label: "The problem is only that BillingService is not using the correct database isolation level. Using SERIALIZABLE isolation would fix the concurrent write conflict.", misconception: "SERIALIZABLE isolation on a single database connection prevents conflicts within that connection's transaction, but the two services hold separate connections and separate transactions. A conflict between two separate serializable transactions results in one transaction being aborted with a serialization error — then requiring a retry — not in a guaranteed correct final state. And if the services are on different database instances, SERIALIZABLE isolation has no cross-service effect at all." },
  ]}
/>

## Temporal coupling: the hidden coupling of synchronous chains

Temporal coupling is a form of runtime coupling that occurs when one service can only complete its work if another service is available at the same moment. Every synchronous service call creates temporal coupling.

In the billing platform's five-service chain, OrderService is temporally coupled to InventoryService, BillingService, and NotificationService. If any one of them is unavailable when an order is placed — even briefly, during a deploy, or during a GC pause — the entire order-placement operation fails. The failure of a notification service (low business priority) causes order placement (high business priority) to fail. This is the inverted risk profile of temporal coupling: the most critical business operation fails when the least critical service is unavailable.

Temporal coupling also makes independent deployment harder. If OrderService must call InventoryService synchronously on every request, they cannot be tested independently. The behavior of OrderService under InventoryService failures must be tested with a live InventoryService or with carefully crafted test doubles. In practice, teams skip this testing because it is hard, producing the distributed monolith's characteristic fragility.

<Quiz
  id="arch-10-03-quiz-2"
  lessonSlug="03-the-distributed-monolith"
  lang="en"
  question="The billing platform's on-call team observes that NotificationService failures (email/SMS sending) cause order-placement failures. The root cause is the synchronous call chain. An engineer proposes: 'We should increase NotificationService's SLA to match OrderService's SLA — if notifications are more reliable, the chain will be more reliable.' What is the structural problem with this approach?"
  choices={[
    { label: "Increasing NotificationService's reliability treats the symptom, not the cause. The cause is temporal coupling: OrderService cannot complete until NotificationService completes. Even with an improved SLA, NotificationService (a notification system) is operationally coupled to OrderService (a transactional system) — a slow notification burst, a SendGrid rate limit, or a short NotificationService deploy window will still block order placement. The structural fix is to decouple the call: OrderService commits the order and publishes an event; NotificationService consumes the event asynchronously. Notifications become best-effort and cannot block the transaction.", correct: true },
    { label: "The approach is correct. Improving NotificationService's reliability is the right way to improve the system's overall reliability under the current architecture.", misconception: "Improving one component's reliability does not change the structural property that a failure in that component cascades to all upstream components in a synchronous chain. A 99.9% SLA (8.7 hours downtime/year) for each of five chained services produces a combined SLA of 99.5% (43 hours downtime/year) — compounding failures. The structural fix is to break the chain, not strengthen its weakest link." },
    { label: "The approach is correct as a short-term fix, but the long-term fix is to make OrderService retry failed notification calls.", misconception: "Retrying a synchronous call to a slow NotificationService extends the order-placement latency for every retry. If notifications are slow because of load, retries increase the load further. Retrying a synchronous call does not decouple the systems; it adds backpressure to an already coupled chain. The decoupling fix (event-driven) is necessary regardless of retry behavior." },
    { label: "The approach works if NotificationService is moved to be called after the database write, not before. The order of calls in the chain determines whether failures cascade.", misconception: "In a synchronous chain, the order of calls determines which service's failure is visible at which point in processing, but all services in the chain are still temporally coupled. Moving NotificationService to after the database write means order state is saved before the failure — but order placement still blocks and returns an error to the client if notification fails. The coupling is not reduced by reordering calls in a synchronous chain." },
  ]}
/>

## Diagnosing a distributed monolith

The billing platform's architecture review checklist for the distributed monolith:

**Change one service, must change three.** If adding a field to the `Order` domain object requires code changes in OrderService, BillingService, and InventoryService — even when only one of them introduces the field — the services are structurally coupled. A true service boundary means the field change is internal to one service's codebase.

**A single request fans out to N sync hops.** If the system's sequence diagram for a single user action shows a waterfall of synchronous calls through four services, each of which must complete before the previous can respond, the services are an implementation detail of one workflow, not autonomous capabilities.

**Deploys require a coordination thread.** If deploying BillingService requires a Slack thread to confirm with the OrderService team that the shared schema migration is compatible, the deployment coupling is explicit. Autonomous services can deploy any time without inter-team coordination.

**No service can fail in isolation.** If bringing InventoryService down for maintenance causes order placement to fail, order placement is not isolated from inventory. Autonomous services degrade gracefully when their dependencies fail — they do not propagate failure upstream.

<Quiz
  id="arch-10-03-quiz-3"
  lessonSlug="03-the-distributed-monolith"
  lang="en"
  question="The billing platform's architecture team audits their five-service system. They find: (1) Every order-placement request makes four synchronous calls in sequence. (2) All five services share a single PostgreSQL instance with tables owned by no one in particular. (3) BillingService deploys to production three times a week; OrderService deploys once a month. (4) When BillingService is deployed, OrderService continues to function correctly. An engineer argues: 'Finding (3) and (4) prove our services are independent — different deploy cadences and BillingService deploys don't break OrderService.' Is this argument sound?"
  choices={[
    { label: "The argument is unsound. Findings (3) and (4) show that BillingService's deploys don't break OrderService — but they say nothing about whether OrderService's deploys break BillingService, and they say nothing about runtime coupling. Findings (1) and (2) reveal the distributed monolith: four synchronous hops mean temporal coupling (OrderService can only complete when all downstream services respond), and a shared database means schema coupling (any table change can break any service). The deploy cadences and the safe BillingService deploys are consistent with a distributed monolith — they prove only that BillingService's isolated schema is stable, not that the system has service autonomy.", correct: true },
    { label: "The argument is sound. Different deploy cadences are the strongest evidence of service autonomy.", misconception: "Different deploy cadences show that one service can deploy without immediately breaking another, but they do not show runtime independence or schema independence. In a distributed monolith, services can have different deploy frequencies while still being temporally coupled (synchronous chains) and schema-coupled (shared database). A service that is called synchronously four times on every request is not autonomous even if it deploys independently." },
    { label: "The argument is sound for finding (3) but not finding (4). Deploy cadence proves autonomy; whether a deploy breaks another service is not relevant.", misconception: "Finding (4) is actually the more operationally significant test of deployment independence. But both findings together only establish that BillingService's changes are compatible with the current OrderService binary. They do not test whether OrderService changes affect BillingService, and they do not address runtime coupling (synchronous chains) or schema coupling (shared database), which are the defining symptoms of the distributed monolith." },
    { label: "The argument is unsound only because of finding (2). If the shared database is replaced with separate databases, findings (3) and (4) would prove service autonomy.", misconception: "Separate databases would address schema coupling but not temporal coupling. Even with separate databases, four synchronous hops per request means OrderService cannot complete when any downstream service is slow or down. A service with its own database but synchronous dependencies on four other services is still a distributed monolith in terms of runtime coupling." },
  ]}
/>

## How to fix a distributed monolith

The distributed monolith is fixed by the same approach that prevents it: decompose by business capability along bounded-context seams (lesson 02), enforce data ownership, and replace chatty synchronous chains with asynchronous event-driven communication where the business allows eventual consistency (unit 09).

The incremental fix:

1. **Identify the chattiest synchronous chain.** The call chain that fans out the furthest is the most urgent temporal coupling to break.
2. **Determine which calls are blocking vs. which are notifications.** Sending a confirmation notification does not need to block the order transaction. Reserving inventory does — the business requires knowing if stock is available before committing the order.
3. **Replace notifications with events.** OrderService publishes an `OrderPlaced` event; NotificationService subscribes and sends the email asynchronously. The order-placement transaction completes without waiting for notifications.
4. **Address data ownership.** Identify which tables each service actually owns. Move exclusive write access to the owning service. Replace all other access with API calls to the owning service.
5. **Version APIs, not schemas.** When two services must share a concept (the order status), they share it through an API with a versioned contract — not through a shared table that both write to.

</Explanation>

<KeyTakeaway>A distributed monolith has services that deploy in lockstep, call each other through chatty synchronous chains, and share a database — the full operational cost of distribution with none of the autonomy. Fix it by decomposing along capability seams, enforcing data ownership, and replacing synchronous notification chains with events.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-10-03-retrieval"
  lang="en"
  questions={[
    { q: "What are the three structural symptoms that define a distributed monolith?", a: "Lockstep deployment: services must be deployed together or in a fixed sequence because they share a schema or API contract that is not versioned. A change to one shared table requires coordinating deploys across every service that queries it. Chatty synchronous call chains: a single client request fans out through N services in sequence, each waiting for the next. Latency and failure probability compound with every hop. Shared database: multiple services read and write the same tables with no exclusive ownership. Schema changes affect every service; concurrent writes from different services to the same record produce dual-write inconsistencies." },
    { q: "What is the dual-write problem and why can it not be fixed by a transaction wrapping both services' writes?", a: "The dual-write problem is a race condition where two services write to the same record, producing an inconsistent final state because each service's write reflects its local view of the truth. A transaction wrapping both services' writes is a distributed transaction — it requires a coordinator to lock records across both services' connections simultaneously. This is 2PC, which reintroduces coordinator single-point-of-failure, cross-service lock holding, and availability dependency on all participants. The correct fix is exclusive data ownership: only one service writes to a given table, making concurrent writes from different services structurally impossible." },
    { q: "What is temporal coupling and how does it differ from deployment coupling?", a: "Temporal coupling is a runtime coupling: one service can only complete its work if another service is available at the same moment. Every synchronous service call creates temporal coupling. Deployment coupling is a build/release coupling: one service cannot be deployed without coordinating with another service because they share a schema or API contract. Both are symptoms of the distributed monolith, but they occur at different times. A system can have deployment coupling without temporal coupling (services that share a schema but communicate asynchronously), and temporal coupling without deployment coupling (services that can deploy independently but call each other synchronously). The distributed monolith typically has both." },
  ]}
/>

<Recap lang="en">
The billing platform's five-service system was a distributed monolith in every meaningful sense: a change to the order domain required changes in three services, order placement blocked on notification sending, and a shared database made every schema change a coordinated event. They had five deployment pipelines and five on-call rotations, but zero independent deployability.

The distributed monolith emerges when teams split by technical layer rather than business capability — producing fragments of a workflow rather than autonomous capabilities — or when they route API traffic to new services without transferring data ownership. The network boundary is cosmetic if the data coupling and temporal coupling remain.

The diagnostic is simple: can each service be deployed and failed independently? If deploying billing requires a coordination thread with the ordering team, the answer is no. If a notification service outage brings down order placement, the answer is no. The number of services on the architecture diagram is irrelevant.

The fix is the same prescription as prevention: decompose by bounded-context seam, give each service exclusive data ownership, and replace synchronous chains with asynchronous events wherever the business can accept eventual consistency. The hard-won lesson is that distribution is not the goal — autonomous deployability is.
</Recap>
