---
slug: "01-bounded-contexts"
lang: en
track: "architecture-patterns"
unit: "06-domain-driven-design"
order: 1
title: "Bounded contexts"
summary: "A bounded context is the explicit boundary within which a domain model is consistent. The same word — Order, Customer, Product — means a different model in each context. Bounded contexts are the primary unit of decomposition in DDD."
estMin: 24
status: ready
lessonType: topic
level: senior
concepts:
  - bounded-context
  - subdomain
  - ubiquitous-language
  - model-consistency
  - strategic-ddd
  - domain-decomposition
prereqs: []
sources:
  - https://martinfowler.com/bliki/BoundedContext.html
  - https://www.domainlanguage.com/ddd/reference/
  - https://www.informit.com/store/implementing-domain-driven-design-9780321834577
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import subdomainVsContext from "./infographics/subdomain-vs-context.svg?raw";

<Hook>
The B2B order platform had a single "Order" entity shared across the entire backend. It had 47 fields. The Sales team needed an Order to capture negotiated line items, discount tiers, and the assigned account executive. The Billing team needed an Order to track invoice amounts, payment terms, and tax jurisdiction. The Shipping team needed an Order to know the delivery address, package dimensions, and carrier preferences. Each team kept adding fields to the same entity. The entity grew. The fields for Sales meant nothing to Billing. The fields for Billing confused the Shipping service. Validation rules clashed: Sales said an Order was valid the moment a quote was signed; Billing said it was only valid once a payment method was confirmed; Shipping said it was only valid once a warehouse had picked the items. Three teams, three definitions of "valid Order," all fighting over one class. A senior architect joining the team asked a single question: "Do you really believe Sales, Billing, and Shipping are talking about the same thing when they say 'Order'?" They were not. The entity had three distinct identities conflated into one. The fix was not to add more validation flags. It was to draw three explicit boundaries and give each team its own model.
</Hook>

<Crux>A bounded context is the boundary for one consistent domain model — the same word means something different in each context, by design.</Crux>

<Explanation>

## The central problem: one word, many models

Domain-driven design starts from an observation that trips up almost every large system: natural language is ambiguous. The word "Customer" in a CRM means a contact with a sales history, a relationship owner, and a lead score. In a billing system, "Customer" means an account with a payment method, a credit limit, and outstanding invoices. In a shipping system, "Customer" means a delivery address and a contact phone number.

If your codebase has a single `Customer` class shared by all three systems, one of two things happens. Either the class grows to contain all three meanings (a 60-field god object that no team fully understands), or the class carries the meaning of whichever team wrote it first, and every other team constantly adapts around fields that do not apply to them.

Eric Evans's insight in *Domain-Driven Design* (2003) was that trying to unify these into one model is not a goal — it is a mistake. Each subdomain has its own language, and that language produces its own model. The goal is not a single unified model; it is a set of models that are internally consistent within their boundaries.

A **bounded context** is that boundary. It is the explicit declaration: "Within this context, the word 'Customer' means exactly this, the word 'Order' means exactly this, and this model is internally consistent." Outside this context, the same word may mean something else — and that is fine, because we have made the boundary explicit.

<FlowDiagram
  label="One word, three models: Order across Sales, Billing, and Shipping contexts"
  caption="The word 'Order' appears in three bounded contexts. Each context has its own model with different fields, validation rules, and lifecycle. Arrows show that contexts communicate across an explicit boundary — they do not share the same Order class. Each arrow represents a cross-context integration point, not a shared import."
  nodes={[
    { id: "sales", label: "Sales Context", sub: "Order: line items, discount, account exec, quote status" },
    { id: "billing", label: "Billing Context", sub: "Order: invoice amount, payment terms, tax jurisdiction" },
    { id: "shipping", label: "Shipping Context", sub: "Order: delivery address, dimensions, carrier, pick status" },
    { id: "integration", label: "Integration layer", sub: "translates between context models" },
  ]}
  edges={[
    { from: "sales", to: "integration", label: "OrderPlaced event" },
    { from: "integration", to: "billing", label: "BillingOrderCreated" },
    { from: "integration", to: "shipping", label: "ShipmentOrderCreated" },
  ]}
/>

## Subdomains vs bounded contexts: problem space vs solution space

These two terms are often used interchangeably and they should not be. The distinction is between what the business does and how your code organizes it.

A **subdomain** is a part of the business problem space — it is discovered by talking to domain experts, not designed by engineers. Your business has a Sales subdomain, a Billing subdomain, a Shipping subdomain. These exist regardless of how you build software. Some subdomains are core (they differentiate the business — getting them right is a competitive advantage), some are supporting (necessary but not differentiating — you could buy them), and some are generic (solved problems — just use off-the-shelf software).

A **bounded context** is a decision in the solution space — it is designed by your team. One subdomain may be served by one bounded context (the common case) or by multiple bounded contexts (when a large core domain warrants separate teams or separate models for different aspects). One bounded context may span multiple subdomains (sometimes done with generic/supporting subdomains that are simple enough not to warrant their own context).

The heuristic: identify subdomains first (by talking to the business), then decide how many bounded contexts to create for each subdomain based on team topology, model complexity, and deployment needs.

<Infographic
  svg={subdomainVsContext}
  label="Side-by-side comparison of a subdomain versus a bounded context. The subdomain is the problem space: what the business does, discovered by talking to domain experts, classified as core, supporting, or generic, and it tells you what is needed. The bounded context is the solution space: how the code organizes it, a boundary designed by your team and enforced by package or service boundaries, one model with one ubiquitous language per team and deployment unit, and it tells you how the system is built."
  caption="Subdomain = problem space (discovered, what the business needs); bounded context = solution space (designed, how your team organizes the code). Confusing the two puts boundaries in the wrong place."
  hue="var(--d-infra)"
/>

<Inset kind="why" lang="en">
Why does the distinction matter in practice? Because teams often conflate the two and end up with a bounded context boundary that does not match any real business boundary. A context split driven by technology ("we'll have a microservice for authentication") without a corresponding model split just creates an RPC call where a function call used to be — the same logical model, now distributed. The bounded context boundary is meaningful only when the model on each side is genuinely different. If your "two contexts" are running the same conceptual model, you have distributed a monolith, not decomposed a domain.
</Inset>

## What a bounded context actually contains

A bounded context is not just a service boundary or a database boundary (though it often aligns with those). It is a **linguistic and model boundary** that contains:

- A **ubiquitous language** — the precise vocabulary shared by developers and domain experts in this context. "Order" in the Sales context has a specific definition that every developer and every product manager on that team uses consistently. (The next lesson focuses on ubiquitous language in depth.)
- A **domain model** — the classes, aggregates, value objects, and rules that implement that vocabulary. The model is valid and consistent within the boundary. Nothing outside the boundary should directly access the internals of this model.
- An **explicit integration interface** — the boundary is explicit. Other contexts integrate through events, APIs, or translation layers — not by importing each other's internal classes.

The boundary is enforced in code. In a monolith, this means package boundaries enforced by architecture tests (ArchUnit, dependency-cruiser, or module system constraints). In a service architecture, the service boundary IS the context boundary — you cannot import across a network call.

## The primary unit of decomposition

In strategic DDD, bounded contexts are the primary unit of decomposition. Before asking "how many microservices should we have?" or "what should each service do?", you ask: "how many bounded contexts does this domain have, and what is the model in each?"

Each bounded context maps to:
- One team (or one squad) with clear ownership
- One ubiquitous language shared between developers and domain experts on that team
- One codebase (or at least one module with enforced boundaries)
- One deployment unit (in a services architecture) or one package cluster (in a monolith)

The size of a bounded context is driven by how much model can remain internally consistent under one team's ownership — not by lines of code, not by database tables, not by API endpoints.

<FlowDiagram
  label="Bounded contexts as the unit of decomposition in the B2B platform"
  caption="Three bounded contexts, each with its own model of the domain. Dashed boxes indicate context boundaries — no class crosses them. Communication happens through explicit integration points only. All arrows represent data flow at integration boundaries, not source-code imports."
  nodes={[
    { id: "sales-ctx", label: "Sales BC", sub: "owns: SalesOrder, Quote, AccountExec, Discount" },
    { id: "billing-ctx", label: "Billing BC", sub: "owns: Invoice, PaymentMethod, TaxRule, BillingOrder" },
    { id: "shipping-ctx", label: "Shipping BC", sub: "owns: Shipment, Carrier, WarehousePick, ShippingOrder" },
    { id: "catalog-ctx", label: "Product Catalog BC", sub: "owns: Product, SKU, Price — shared reference data" },
  ]}
  edges={[
    { from: "sales-ctx", to: "billing-ctx", label: "OrderConfirmed event" },
    { from: "sales-ctx", to: "shipping-ctx", label: "OrderConfirmed event" },
    { from: "catalog-ctx", to: "sales-ctx", label: "Product reference (read)" },
    { from: "catalog-ctx", to: "shipping-ctx", label: "Product reference (read)" },
  ]}
/>

<Quiz
  id="arch-06-01-quiz-1"
  lessonSlug="01-bounded-contexts"
  lang="en"
  question="The B2B platform has a single shared `Order` class used by Sales, Billing, and Shipping. The class has grown to 47 fields. A developer proposes: 'We should split this into three subclasses — SalesOrder, BillingOrder, ShippingOrder — all extending a base Order class.' Why does this not solve the bounded-context problem?"
  choices={[
    { label: "Because subclassing keeps the models coupled at the base class level. Any change to the base Order forces a recompile (and likely a change review) for all three subclasses. More importantly, it keeps the three teams sharing a single class hierarchy — the Billing team's changes to BillingOrder risk breaking the SalesOrder or ShippingOrder if the base class changes. A bounded context requires that each context's model be independently owned, independently deployable, and free from source-code dependencies on sibling context models. Subclassing within a shared package violates all three.", correct: true },
    { label: "Because subclassing only works for value objects, not entities.", misconception: "Subclassing works for both value objects and entities in object-oriented code. The problem is not a restriction on what can be subclassed — it is that subclassing keeps models coupled through the class hierarchy. The base class is a shared dependency; any change to it ripples to all subclasses across all contexts." },
    { label: "Because bounded contexts require microservices, and subclassing only works in a monolith.", misconception: "Bounded contexts are a model-level concept, not a deployment-level one. They apply equally in a monolith (enforced by package boundaries and architecture tests) and in a services architecture (enforced by the service boundary). The subclassing proposal would be equally problematic in both." },
    { label: "Because all three subclasses would still have 47 fields each.", misconception: "The 47-field problem is a symptom of conflating three models, not the root cause. Splitting into subclasses could reduce field count, but that does not create independent models. The root cause is shared ownership and shared coupling. A proper bounded context split means three independent models with their own classes, their own validation rules, and their own lifecycle — not three subclasses of a shared base." },
  ]}
/>

<Quiz
  id="arch-06-01-quiz-2"
  lessonSlug="01-bounded-contexts"
  lang="en"
  question="Your team has identified three subdomains: Order Management (core), Tax Calculation (supporting), and Email Notifications (generic). A junior developer proposes one bounded context per subdomain. A senior architect says: 'We should use off-the-shelf software for Tax and Email — that means they are not our bounded contexts at all.' What is the architect's reasoning?"
  choices={[
    { label: "Generic and supporting subdomains solved by external software or SaaS have their model defined by that software, not by your team. You integrate with them; you do not own their model. The bounded context boundary is between YOUR codebase and THEIR system — represented in your codebase by an Anticorruption Layer that translates their model into your language. Creating a bounded context for them would mean designing your own model of something you have decided not to own, which defeats the purpose of buying it.", correct: true },
    { label: "Supporting and generic subdomains are always too simple to need a bounded context.", misconception: "Complexity is not the criterion. A supporting subdomain solved with custom code CAN and SHOULD have its own bounded context with its own model and ubiquitous language — especially if it is rich enough to have its own domain expert. The architect's reasoning is specifically about EXTERNALLY-PROVIDED subdomains where the model is owned by the vendor, not your team." },
    { label: "Bounded contexts only apply to core subdomains because only core subdomains have domain experts.", misconception: "Domain experts exist for supporting subdomains too — accountants are domain experts for tax calculation. The distinction is not whether experts exist, but whether your team owns the model. When you use external software for a subdomain, you integrate with that software's model rather than designing your own, and the integration point becomes an Anticorruption Layer." },
    { label: "You cannot have more than one bounded context per team, so the architect is reducing the count.", misconception: "There is no hard rule of one bounded context per team (though it is a common heuristic). The architect's reasoning is about model ownership: if a subdomain is handled by external software, your team does not own or maintain a domain model for it. The distinction is ownership, not team count." },
  ]}
/>

<Quiz
  id="arch-06-01-quiz-3"
  lessonSlug="01-bounded-contexts"
  lang="en"
  question="Two engineers are debating the bounded context split for a new feature. Engineer A says: 'Customer means the same thing in Sales and in Customer Support — they both care about the same person.' Engineer B says: 'But Sales cares about the customer's purchase history, lead score, and account executive; Support cares about their open tickets, service tier, and escalation history — those are genuinely different models.' How do you determine whether to use one bounded context or two?"
  choices={[
    { label: "The test is whether the models can evolve independently without coordinating across teams. If the Sales and Support models for 'Customer' need to change in response to different forces (Sales reacts to CRM product requirements; Support reacts to ticketing system requirements), and each change must be coordinated with the other team, you have the wrong boundary. If Sales can enrich its Customer model without touching Support's model, and vice versa, they belong in separate contexts with an explicit integration contract for the shared identifier. Engineer B's evidence — different fields, different concerns, different lifecycle events — suggests two separate models, i.e. two contexts.", correct: true },
    { label: "If they share a customer ID, they must be in the same bounded context.", misconception: "Sharing an identifier is exactly how bounded contexts integrate, not a reason to merge them. Contexts reference each other's aggregates by ID, not by importing each other's classes (the next lesson covers aggregate IDs in depth). A shared customer ID is a pointer that crosses the context boundary — it is the integration mechanism, not evidence that the two models should be unified." },
    { label: "Two contexts is always better than one because it reduces coupling.", misconception: "Over-splitting is a real failure mode. If the two 'contexts' share the same model, same team, same release cycle, and must coordinate every schema change, then splitting them creates distributed-monolith coupling with the overhead of a network call. The right number of contexts is determined by whether the models are genuinely distinct and whether they can evolve independently — not by a dogma of 'more is better.'" },
    { label: "Use one context if both teams use the same database table for customers.", misconception: "Database table sharing is an implementation detail, not a model boundary. Two contexts can read from the same database table via separate access layers — the context boundary is about ownership of the model and the ubiquitous language, not about database physical layout. (In practice, sharing a table does create coupling, but it is a tactical problem to be solved, not the criterion for context placement.)" },
  ]}
/>

</Explanation>

<KeyTakeaway>A bounded context is the explicit linguistic and model boundary within which one consistent domain model lives. The same word means a different thing in each context — and that is correct. Bounded contexts are the primary unit of decomposition in DDD; they are placed by identifying subdomains (problem space) and deciding how many context boundaries (solution space) best serve the team topology and model needs.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-06-01-retrieval"
  lang="en"
  questions={[
    { q: "What is a bounded context, and why does the same word intentionally mean different things in different contexts?", a: "A bounded context is the explicit boundary within which one domain model is internally consistent. The ubiquitous language inside the context is precise — every term has one meaning that all developers and domain experts on that team share. Outside the boundary, the same word may mean something different, because a different team, with different domain experts, is solving a different part of the business problem. This is not a failure to agree — it is a recognition that 'Customer' in the Sales context (lead score, account executive, pipeline stage) and 'Customer' in the Billing context (payment method, credit limit, invoices) are genuinely different models that will evolve in different directions. Forcing them into one model means every Sales change must negotiate with Billing, and vice versa. Separating them lets each context evolve at its own pace behind an explicit integration boundary." },
    { q: "What is the difference between a subdomain and a bounded context?", a: "A subdomain is a part of the business's problem space — it is discovered by talking to domain experts and exists whether or not you build software. Your business has a Sales subdomain, a Billing subdomain, a Shipping subdomain. Subdomains are categorized as core (differentiating — get this right for competitive advantage), supporting (necessary but not differentiating), or generic (solved problems — buy or use off-the-shelf). A bounded context is a design decision in your solution space — it is the context boundary you draw around a model in code. Ideally one core subdomain maps to one bounded context with one team. But a large core subdomain might warrant multiple bounded contexts (different teams for different model aspects), and a simple supporting subdomain might not justify its own context at all. The subdomain tells you what the business needs; the bounded context tells you how you have organized the code." },
    { q: "What does 'primary unit of decomposition' mean in the context of DDD?", a: "In strategic DDD, you answer 'how do we decompose this system?' not by counting microservices or APIs, but by identifying bounded contexts. Each bounded context represents a model with a consistent language, an owning team, a deployment boundary (in services) or an enforced package boundary (in a monolith), and an explicit integration interface with every other context. Decomposing by bounded context ensures that each piece of the system has a clear ownership model, a coherent internal language, and a real reason for its existence rooted in the business domain — not just an organizational or technical convenience." },
  ]}
/>

<Recap lang="en">
The opening story's 47-field Order entity was not a problem of poor object-oriented design. It was a problem of three distinct models — Sales, Billing, Shipping — being forced into one class because no one had drawn an explicit boundary.

A bounded context solves this by declaring: within this boundary, this word means exactly this, this model is consistent, and changes here do not require coordination with that other team. The boundary is linguistic (ubiquitous language), model-level (a consistent set of classes and rules), and structural (enforced by package or service boundaries in code).

Bounded contexts are not the same as subdomains. Subdomains are the business's problem space — discovered, not designed. Bounded contexts are your solution space — designed by your team based on subdomains, team topology, and model complexity.

The next lesson examines the ubiquitous language inside a bounded context — why it is a design tool, not just a naming convention, and how naming drift is the first signal that a boundary is in the wrong place.
</Recap>
