---
slug: "04-context-mapping"
lang: en
track: "architecture-patterns"
unit: "06-domain-driven-design"
order: 4
title: "Context mapping"
summary: "A context map documents how bounded contexts integrate. The patterns — Shared Kernel, Customer/Supplier, Conformist, ACL, Open Host Service — describe team relationships and translation strategies, not just API shapes."
estMin: 26
status: ready
lessonType: topic
level: senior
concepts:
  - context-map
  - anticorruption-layer
  - shared-kernel
  - open-host-service
  - conformist
  - context-integration
prereqs: []
sources:
  - https://martinfowler.com/bliki/BoundedContext.html
  - https://www.domainlanguage.com/ddd/reference/
  - https://www.informit.com/store/implementing-domain-driven-design-9780321834577
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The B2B order platform's Sales context was calling a third-party tax-calculation service. The service had its own model of "products" — it called them "taxable items," with a `commodityCode`, a `taxJurisdiction`, and a `lineNetAmount`. The Sales context had its own model of a `LineItem` with a `NegotiatedPrice` and a `productId`. The developer integrating the two contexts took the path of least resistance: they used the tax service's object model directly in the Sales context. The `LineItem` class grew a `commodityCode` field. The `Quote` service method that called the tax service began returning the tax service's `TaxCalculationResult` directly to the rest of the Sales context. Within three months, the entire Sales codebase was scattered with `commodityCode` and `taxJurisdiction` fields that meant nothing to any Sales domain expert. When the tax service provider changed their API — replacing `taxJurisdiction` with a `jurisdictionCode` enum — the change propagated into the Sales context's core domain model. A Sales feature change required understanding the tax service's object model. The Sales context had been corrupted by the tax service's language. This is what Evans calls "model corruption through integration." When two bounded contexts integrate without an explicit translation layer, the upstream model leaks into the downstream context. The downstream context's ubiquitous language is gradually replaced by the upstream's language. The context boundary dissolves. The context map is the strategic tool that prevents this: a diagram and a set of named patterns that describe not just how contexts communicate, but what power relationship they have and what translation strategy they use.
</Hook>

<Crux>A context map names the integration pattern between every pair of contexts — describing team relationship and translation strategy, not just API topology.</Crux>

<Explanation>

## What a context map is

A context map is a strategic overview of the bounded contexts in a system and the relationships between them. It answers:

- Which contexts exist?
- How does each context integrate with the others?
- What is the team relationship at each integration point?
- What translation strategy prevents model corruption?

The context map is not an architecture diagram of services or APIs. It is a **team and model diagram**. The arrows on a context map carry labels that describe organizational and modeling relationships — Shared Kernel, Customer/Supplier, Conformist, Anticorruption Layer — not just "calls" or "sends event to."

The B2B platform's context map includes: Sales, Billing, Shipping, Product Catalog, Tax Calculation (external). Each pair of contexts that integrates has a labeled relationship. The label determines how teams interact and how models translate.

## The integration patterns

### Shared Kernel

Two contexts share a small, explicitly agreed subset of the domain model. Both teams own the shared piece — changes to it require coordination between both teams. This is the tightest coupling and the highest coordination cost.

Use it when: two contexts genuinely share a core concept that would be expensive to duplicate or translate. In the B2B platform, the `Money` value object (amount + currency) might be a shared kernel element — both Sales and Billing need it, it is stable, and having two separate `Money` implementations that could silently differ is dangerous.

Avoid it for: anything that evolves at different rates in each context. A shared kernel that one team wants to change and the other does not creates a coordination bottleneck.

### Customer/Supplier

One context (the Supplier) produces data or services that another (the Customer) consumes. The Supplier's model defines the interface. The Customer adapts to it. But critically, the relationship is negotiated: the Customer can request changes, the Supplier considers them, and the interface evolves with explicit coordination.

This is the most common pattern between internal teams. In the B2B platform, Product Catalog is a Supplier to Sales: Sales consumes catalog data. The Product Catalog team maintains the API; the Sales team can request additions or changes; the Catalog team prioritizes and schedules them.

The power relationship matters. If the Supplier team ignores the Customer team's needs, the Customer team is forced to work around the Supplier's model — which often leads to model corruption in the Customer context.

### Conformist

The Customer context has no leverage over the Supplier. The Supplier will not change its interface to suit the Customer. The Customer simply conforms: it adopts the Supplier's model as-is, including its language, its object shapes, and its lifecycle. The Customer context's ubiquitous language is subordinated to the upstream's language.

This is the right choice when: the Supplier is a large external system with no interest in your needs (a SaaS platform, a third-party data feed), AND the Supplier's model is reasonable enough to use directly, AND the cost of building a translation layer exceeds the benefit of keeping your own model.

The B2B platform might be Conformist with respect to a payment processor — if the processor's API is stable and its model of "payment," "charge," and "refund" maps cleanly enough to Billing's concepts, conforming may be pragmatic.

The danger: once you conform, you have imported the upstream's ubiquitous language into your context. Your model now evolves at the Supplier's pace. Model drift in the upstream propagates directly into your context.

### Anticorruption Layer (ACL)

The Customer context translates between the Supplier's model and its own model, protecting its own ubiquitous language. The ACL is an explicit translation component — it lives at the integration boundary, speaks the Supplier's language on the outside, and the Customer's language on the inside.

This is the pattern the B2B platform's Sales context should have used with the tax service. The ACL would translate: `LineItem` → `TaxableItem` (when calling the tax service), `TaxCalculationResult` → `QuoteTaxSummary` (when returning the result to Sales). The Sales context's model never sees `commodityCode` or `taxJurisdiction`. The ACL absorbs the translation.

The ACL is the default choice when integrating with external systems whose model you do not control and whose language differs significantly from your own.

<Inset kind="why" lang="en">
Why "anticorruption"? Because the alternative — letting the upstream model leak into your context — is described by Evans as "corruption" of your domain model. The Conformist pattern is a deliberate choice to accept that corruption because the cost of building a translation layer is not justified. The ACL is the explicit rejection of that corruption: we will spend the engineering effort to maintain our own model rather than let the upstream's language replace ours. The cost of an ACL is real (translation code to write, maintain, and test). The benefit is that your context's model remains yours — it evolves in response to your domain experts, not in response to your upstream's API changes.
</Inset>

### Open Host Service (OHS) and Published Language (PL)

When a context is a Supplier to many downstream contexts, it can formalize its integration interface as an **Open Host Service**: a well-defined protocol that any downstream can integrate with, without requiring custom negotiation. The OHS is stable, versioned, and designed for consumption by multiple consumers.

A **Published Language** is the formal, well-documented data format used by the OHS — typically a versioned event schema, a public API contract, or a standard format. In the B2B platform, the Product Catalog context might expose a Published Language of product events (`ProductCreated`, `ProductPriceChanged`, `ProductDiscontinued`) with a stable schema that Sales, Billing, and Shipping all consume. The schema is versioned; breaking changes are communicated in advance.

The OHS + PL pattern reduces the coordination overhead of the Customer/Supplier relationship at scale — the Supplier does not negotiate with each customer individually; it maintains a published contract.

<FlowDiagram
  label="B2B platform context map: integration patterns between all contexts"
  caption="Each arrow represents a named context-mapping relationship. ACL = Anticorruption Layer (Sales protects its model from the Tax service). OHS/PL = Open Host Service / Published Language (Product Catalog publishes stable events). C/S = Customer/Supplier (negotiated interface). Conformist = Billing adopts the payment processor's model as-is."
  nodes={[
    { id: "sales", label: "Sales BC", sub: "upstream: Product Catalog (C/S), Tax Service (ACL)" },
    { id: "billing", label: "Billing BC", sub: "upstream: Sales (C/S), Payment Processor (Conformist)" },
    { id: "shipping", label: "Shipping BC", sub: "upstream: Sales (C/S), Product Catalog (OHS)" },
    { id: "catalog", label: "Product Catalog BC", sub: "OHS+PL: publishes ProductEvents schema" },
    { id: "tax", label: "Tax Service (external)", sub: "no negotiation possible" },
    { id: "payment", label: "Payment Processor (external)", sub: "stable model, no leverage" },
  ]}
  edges={[
    { from: "catalog", to: "sales", label: "C/S (negotiated)" },
    { from: "catalog", to: "shipping", label: "OHS/PL (published events)" },
    { from: "sales", to: "billing", label: "C/S (OrderConfirmed event)" },
    { from: "sales", to: "shipping", label: "C/S (OrderConfirmed event)" },
    { from: "tax", to: "sales", label: "ACL (Sales translates)" },
    { from: "payment", to: "billing", label: "Conformist (Billing adapts)" },
  ]}
/>

## Reading a context map: power and translation

The most important thing a context map communicates is not the API topology — it is the **power relationship** between teams and the **translation strategy** at each boundary.

Power relationship: in a Customer/Supplier relationship, the Supplier has leverage. If the Supplier is responsive and the teams have good communication, the Customer can evolve. If the Supplier is unresponsive (a large platform team with many customers), the Customer eventually becomes Conformist by default — they cannot get changes they need, so they adapt their model to the Supplier's constraints.

Translation strategy: ACL means "we translate at the boundary, our model is protected." Conformist means "we have accepted the upstream model." Shared Kernel means "we jointly own this piece." These choices have compounding consequences over years. A team that was Conformist ten years ago now has an internal model deeply coupled to an upstream service's 2015 API design.

<Inset kind="note" lang="en">
The context map is not a one-time diagram. It should be updated when: a new integration is added, a relationship changes (a Customer/Supplier becomes Conformist because the Supplier stops responding), or a context is split or merged. The map is a living document of the team's organizational and architectural decisions. A stale context map is as harmful as stale code comments — it describes a system that no longer exists.
</Inset>

<Quiz
  id="arch-06-04-quiz-1"
  lessonSlug="04-context-mapping"
  lang="en"
  question="The Sales context is integrating with an internal Inventory context. The Sales team wants to check product availability before confirming a Quote. The Inventory team says: 'Use our API directly — just call our InventoryItem endpoint and use the InventoryStatus enum we return.' The Sales tech lead says: 'We need an ACL here.' The Inventory team pushes back: 'Why add translation overhead for an internal service? It's wasteful.' How do you decide?"
  choices={[
    { label: "The decision hinges on whether the Inventory context's model — InventoryItem, InventoryStatus — belongs in the Sales context's ubiquitous language. If the Sales context's domain experts talk about 'availability' and 'stock,' not 'inventory items' and 'inventory status,' then using the Inventory model directly imports foreign language into Sales. Every Sales developer now needs to understand Inventory concepts to read Sales code. When Inventory renames InventoryStatus values or changes InventoryItem fields, Sales must change too. An ACL translates: Inventory's InventoryStatus.IN_STOCK becomes Sales's ProductAvailability.AVAILABLE — the Sales context speaks its own language internally. The overhead is real but bounded, and it protects the Sales model from evolving at Inventory's pace.", correct: true },
    { label: "ACLs are only needed for external third-party services, not for internal services between teams.", misconception: "The need for an ACL is determined by whether the upstream's model matches the downstream's ubiquitous language, not by whether the service is internal or external. Internal services built by different teams with different domain experts can have just as much model divergence as external services. Many of the most damaging cases of model corruption come from internal services where teams assumed 'we can just share the model' because they work in the same organization." },
    { label: "The Sales team should become Conformist and use the Inventory model directly — it reduces code and internal services should share models.", misconception: "Conformist is a deliberate strategic choice, not the default for internal services. Choosing Conformist means accepting that the Sales context's model will evolve at the Inventory team's pace, that the Sales context's ubiquitous language will include Inventory concepts, and that Inventory API changes will propagate directly into Sales. This may be acceptable for a very stable, low-divergence integration. But labeling it as 'reducing code' obscures the real cost: the Sales context gives up its model autonomy." },
    { label: "Use a Shared Kernel: both teams agree on a shared AvailabilityStatus type that both use, avoiding the translation overhead.", misconception: "A Shared Kernel for availability status means both teams must coordinate on every change to the shared type. If Inventory needs to add a new status value (e.g., RESERVED_FOR_RECALL), they must negotiate with Sales before making the change. A Shared Kernel is appropriate when two teams genuinely co-own a concept. Availability is an Inventory concept that Sales consumes — it is not co-owned. The ACL puts the translation responsibility clearly on the consumer (Sales), which is where it belongs." },
  ]}
/>

<Quiz
  id="arch-06-04-quiz-2"
  lessonSlug="04-context-mapping"
  lang="en"
  question="The Product Catalog context publishes a `ProductPriceChanged` event with a schema versioned as v1. Six months later, the Catalog team wants to add a `previousPrice` field to the event. Sales and Shipping are both consumers. What does the Open Host Service / Published Language pattern require the Catalog team to do?"
  choices={[
    { label: "Adding a field to an event schema is a non-breaking change (consumers can ignore the new field) — the Catalog team can deploy it without coordinating consumer teams, but should notify them and update the Published Language documentation. If the change were breaking (renaming a field, changing a field type, removing a field), the OHS/PL pattern requires versioning: publish v2 of the schema, maintain v1 for a deprecation period, coordinate with all consumers to migrate before retiring v1. The Published Language's value is that consumers can rely on schema stability — breaking changes without versioning destroy that trust.", correct: true },
    { label: "The Catalog team must get explicit approval from both Sales and Shipping before any schema change, even additive ones.", misconception: "The OHS/PL pattern reduces coordination overhead for additive changes precisely by making the contract public and stable. Additive changes (new optional fields) do not break existing consumers — they can be deployed unilaterally with a changelog notification. Requiring explicit approval for every additive change defeats the purpose of having a Published Language: it reverts to Customer/Supplier negotiation for every change." },
    { label: "The Catalog team should not version events — versioning creates duplicate code and consumers should always use the latest schema.", misconception: "Unversioned event schemas with breaking changes force all consumers to update simultaneously or break. In a system with multiple independent teams, synchronized deploys across all consumers are operationally expensive and error-prone. Schema versioning lets each consumer migrate at their own pace, maintaining backward compatibility during the transition window. The 'duplicate code' cost of versioning is real but much smaller than the coordination cost of forced synchronized deploys." },
    { label: "Because Sales and Shipping are Conformist to the Catalog, they must update their code immediately when the schema changes.", misconception: "Sales and Shipping consuming Product Catalog events does not make them Conformist — it likely makes them Customers in a Customer/Supplier or OHS relationship. Conformist means adopting the upstream model into the downstream's own domain model with no translation layer. If Sales and Shipping have their own model of product pricing and translate the Catalog's events into their own domain events via an ACL, they are not Conformist and they are protected from upstream schema changes at their boundary." },
  ]}
/>

<Quiz
  id="arch-06-04-quiz-3"
  lessonSlug="04-context-mapping"
  lang="en"
  question="Two years ago the Billing context became Conformist to an internal Payments context, importing the Payments team's `PaymentMethod`, `ChargeResult`, and `RefundPolicy` types directly into Billing's domain model. The Payments team has now been acquired by another company and their service is being replaced with a third-party processor. The Billing context must migrate. Why is this migration significantly harder than it would have been if Billing had used an ACL?"
  choices={[
    { label: "Because Billing is Conformist, the Payments model is embedded throughout its domain model — method signatures, event types, database columns, and test fixtures all reference `PaymentMethod`, `ChargeResult`, and `RefundPolicy`. Migrating to the new processor means changing every one of these references to the new processor's equivalent types. If Billing had an ACL, the Payments model would exist only inside the ACL translation layer. The migration would be: rewrite the ACL to translate between the Billing domain model and the new processor's API. The Billing domain model itself — its aggregates, domain events, ubiquitous language — would be untouched. ACL-isolated migrations are bounded to one component; Conformist migrations are unbounded through the codebase.", correct: true },
    { label: "The migration would be equally hard with an ACL because the ACL would also need to be rewritten for the new processor.", misconception: "The ACL does need to be rewritten — that is its job. But the ACL is one bounded component. Rewriting one component is categorically different from updating references scattered across an entire domain model. The value of the ACL is that the blast radius of an upstream change is contained to one translation component. With Conformist, the blast radius extends as far as the upstream model has spread through the downstream codebase." },
    { label: "The Conformist approach was correct because it reduced code at the time — the increased migration cost is an acceptable trade-off.", misconception: "This is a legitimate trade-off argument only if it was explicitly evaluated at the time. 'Reduced code at the time' is a short-term gain; 'migration cost two years later' is a long-term cost. The DDD framework asks teams to make this trade-off consciously: Conformist is correct when the upstream model is stable and the translation cost is genuinely high. Using Conformist as the default because 'it reduces code' without evaluating the migration risk is not a trade-off — it is deferred cost." },
    { label: "The problem is not Conformist versus ACL — it is that internal services should never be replaced by third-party ones.", misconception: "Internal services are replaced by external ones routinely — a custom payments implementation being replaced by Stripe or Adyen is a common business decision. The architectural question is not whether this should happen but how well the system is structured to absorb it. ACL isolation makes the system resilient to upstream changes regardless of whether the upstream is internal or external." },
  ]}
/>

</Explanation>

<KeyTakeaway>A context map names the integration pattern at every context boundary — Shared Kernel, Customer/Supplier, Conformist, ACL, Open Host Service. Each pattern describes both the team power relationship and the translation strategy. The ACL is the default protection against model corruption from upstream contexts; Conformist is the conscious decision to accept that corruption when translation cost outweighs the benefit of model independence.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-06-04-retrieval"
  lang="en"
  questions={[
    { q: "What is a context map, and what does it communicate beyond API topology?", a: "A context map is a strategic diagram of all bounded contexts in a system and the named relationships between them. Beyond API topology (which service calls which), it communicates: the team power relationship at each boundary (does the downstream team have leverage to request changes from the upstream?), the translation strategy (does the downstream protect its model with an ACL, or conform to the upstream model?), and the organizational consequences of each integration choice. A Customer/Supplier relationship means teams negotiate. Conformist means the downstream accepts the upstream's model and evolution pace. ACL means the downstream invests in translation to protect its own language. These are architectural decisions with compounding consequences — a Conformist choice made in year one shapes the migration cost in year three." },
    { q: "What is the difference between Conformist and Anticorruption Layer, and when is each appropriate?", a: "Both describe a downstream context integrating with an upstream whose model it cannot change. The difference is translation strategy. Conformist: the downstream adopts the upstream's model directly into its domain model — no translation layer. The upstream's language becomes part of the downstream's ubiquitous language. Appropriate when the upstream model is stable, maps cleanly to the downstream's concepts, and the cost of building a translation layer is not justified. ACL: the downstream maintains its own model and translates at the boundary. The upstream's language exists only in the ACL component; the downstream's domain model is insulated. Appropriate when the upstream model diverges significantly from the downstream's language, or when the upstream is likely to change its model in ways that would propagate into the downstream's domain. The ACL is the more expensive choice upfront; Conformist is the more expensive choice long-term when the upstream changes." },
    { q: "What is the Open Host Service pattern, and how does it reduce coordination overhead compared to Customer/Supplier?", a: "In a Customer/Supplier relationship, the Supplier negotiates the interface with each Customer individually. As the number of Customers grows, this becomes a coordination bottleneck — every Customer wants something slightly different, and the Supplier must manage a queue of requests. The Open Host Service pattern formalizes the Supplier's interface as a public, stable, versioned protocol that any Consumer can integrate with without direct negotiation. The Published Language defines the schema explicitly (versioned events, OpenAPI contract). Additive changes are backward-compatible and deployed without consumer coordination. Breaking changes are versioned — v2 is published alongside v1, consumers migrate at their own pace, v1 is deprecated after a communicated deadline. OHS/PL trades the flexibility of individual negotiation for the scalability of a published contract." },
  ]}
/>

<Recap lang="en">
The opening story's model corruption — Sales context filled with `commodityCode` and `taxJurisdiction` from an external tax service — was not an accident. It was the predictable result of integrating without a named strategy. The developer took the path of least resistance: use the upstream model directly. The bounded context boundary dissolved.

A context map names the strategy at every integration point. Shared Kernel: both teams co-own a small piece. Customer/Supplier: negotiated interface, downstream has leverage. Conformist: downstream accepts the upstream model consciously. ACL: downstream translates at the boundary and protects its own model. Open Host Service: upstream publishes a stable versioned contract for multiple consumers.

Each pattern describes a team relationship, not just a technical shape. The ACL's value is not avoiding code — it is making the upstream change's blast radius a single translation component rather than the entire domain model. Conformist's danger is not theoretical — it materializes when the upstream's model changes years later.

The context map is drawn once and updated continuously. It is the strategic picture that tells every team: here is who you depend on, how much power you have, and what your model protection strategy is.
</Recap>
