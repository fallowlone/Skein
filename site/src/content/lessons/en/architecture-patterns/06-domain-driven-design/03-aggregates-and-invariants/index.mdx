---
slug: "03-aggregates-and-invariants"
lang: en
track: "architecture-patterns"
unit: "06-domain-driven-design"
order: 3
title: "Aggregates and invariants"
summary: "An aggregate is a transactional consistency boundary. The aggregate root is the only external entry point. One transaction modifies one aggregate. Size aggregates by the invariant they enforce, not by the data graph — small is the right default."
estMin: 24
status: ready
lessonType: topic
level: senior
concepts:
  - aggregate
  - aggregate-root
  - invariant
  - transactional-consistency
  - aggregate-by-id
  - aggregate-size
prereqs: []
sources:
  - https://martinfowler.com/bliki/DDD_Aggregate.html
  - https://www.domainlanguage.com/ddd/reference/
  - https://www.informit.com/store/implementing-domain-driven-design-9780321834577
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import aggregateSizing from "./infographics/aggregate-sizing.svg?raw";

<Hook>
The B2B order platform's Sales context had a `Quote` that contained `LineItem` objects. A `LineItem` had a `NegotiatedPrice`. A `Quote` had a business rule: the total value of all line items must not exceed the customer's credit limit at the time of submission. The rule seemed simple. Then the team hit a concurrency problem. Two account executives were editing the same Quote simultaneously — one adding a high-value line item, one removing a different one. Both edits passed the credit-limit check in isolation because each read the Quote before the other's write landed. After both writes committed, the total exceeded the limit. The invariant — the credit-limit rule — had been violated despite both writes being individually valid. The team's first fix was to add an optimistic lock on the `Quote` row. That worked for Quote-level conflicts, but then another problem surfaced: a developer had written a batch job that updated `LineItem` prices directly, bypassing the `Quote` entirely. The batch job had no knowledge of the credit-limit rule because it was checking only the `LineItem`, not the `Quote` that owned it. Two separate transactions, two separate invariant checks, same corruption. The team had accidentally split what should have been one consistency unit into two. The aggregate pattern is the answer to this class of problem. An aggregate defines the transactional consistency boundary: a cluster of domain objects treated as one unit for the purposes of data changes. The aggregate root is the single entry point. Every mutation goes through it. The invariants it enforces are guaranteed to hold at the end of every transaction.
</Hook>

<Crux>An aggregate is a transactional consistency boundary: one root, one transaction, one invariant cluster — outside must reference by ID.</Crux>

<Explanation>

## What an aggregate enforces

An aggregate is not a grouping of related data. It is a grouping of data around a **consistency invariant** — a business rule that must hold true across multiple objects after every operation.

The `Quote` aggregate enforces: "the total negotiated value of all line items must not exceed the customer's credit limit at submission." This rule spans the `Quote` and all its `LineItem` objects. The `Quote` cannot delegate this check to `LineItem` because `LineItem` does not know about the total. No individual `LineItem` can violate the rule — only the collection as a whole can.

This is the defining criterion for an aggregate: **what is the smallest set of objects I must load and lock together to enforce a specific business invariant?** That set is the aggregate. The object at the root of that set, the one that enforces the invariant, is the **aggregate root**.

The aggregate root is the only entry point for external code. No object outside the aggregate holds a direct reference to an internal `LineItem`. External code calls `quote.addLineItem(...)` or `quote.removeLineItem(...)`. The `Quote` root runs the invariant check. The `LineItem` objects are internal implementation — they cannot be accessed, modified, or deleted by going around the root.

<FlowDiagram
  label="Quote aggregate: root enforces the invariant over internal entities"
  caption="The Quote aggregate root is the only external entry point. LineItem and NegotiatedPrice are internal entities and value objects — no external code holds a direct reference to them. The credit-limit invariant is enforced by the root across all line items. Arrows show ownership and access direction: external code accesses only the root."
  nodes={[
    { id: "ext", label: "External code", sub: "calls quote.addLineItem(), quote.submit()" },
    { id: "root", label: "Quote (aggregate root)", sub: "enforces: total <= creditLimit at submission" },
    { id: "li1", label: "LineItem (internal)", sub: "productId, quantity, NegotiatedPrice" },
    { id: "li2", label: "LineItem (internal)", sub: "productId, quantity, NegotiatedPrice" },
    { id: "price", label: "NegotiatedPrice (value object)", sub: "amount, currency — immutable" },
  ]}
  edges={[
    { from: "ext", to: "root", label: "entry point only" },
    { from: "root", to: "li1", label: "owns" },
    { from: "root", to: "li2", label: "owns" },
    { from: "li1", to: "price", label: "contains" },
    { from: "li2", to: "price", label: "contains" },
  ]}
/>

## One transaction modifies one aggregate

This rule is the structural consequence of the consistency boundary. If one transaction modifies two aggregates, you have either:

1. Put the wrong objects in the same aggregate (the invariant actually spans both, so they should be one aggregate), or
2. Accepted eventual consistency between the two aggregates (the invariant does not need to hold synchronously across both, so they communicate via events).

The "one transaction, one aggregate" rule is why the batch job in the opening story caused problems. It modified `LineItem` in one transaction without going through `Quote`. The `Quote`'s invariant check never ran. The fix is to remove direct `LineItem` writes entirely — all mutations go through `Quote.addLineItem()` or `Quote.updateLineItemPrice()`, which enforces the invariant after every change.

<Inset kind="why" lang="en">
Why is this rule so strict? Because "one transaction modifies one aggregate" is what makes the aggregate boundary enforceable. If two aggregates can be modified in the same transaction, then any invariant that spans those two aggregates must be checked in every transaction that touches either. You can no longer know which transactions need the check without analyzing all of them. The aggregate boundary becomes meaningless. Once you commit to one transaction per aggregate, the aggregate root becomes the only place invariant logic needs to live — and you can reason about it in isolation.
</Inset>

## Referencing other aggregates by ID

When one aggregate needs to reference another, it does so by **identifier only** — not by object reference. The `Quote` aggregate in Sales might need to reference the Customer it belongs to. But `Customer` is a separate aggregate (it has its own invariants around credit history and account status). The `Quote` aggregate holds a `customerId` — a value object wrapping the customer's identifier — not a direct reference to the `Customer` object.

This has three consequences:

1. **No cascade loading**: loading a `Quote` does not automatically load the `Customer` graph. Each aggregate is loaded independently.
2. **No cascade mutation**: the aggregate root can only enforce invariants over the objects it owns. It cannot mutate a `Customer` through a reference — it would have to call `Customer` through its own root.
3. **Explicit cross-aggregate consistency**: if an operation must change both a `Quote` and a `Customer`, those changes happen in separate transactions, coordinated through domain events (a `QuoteSubmitted` event may trigger a process that updates `Customer.openQuoteCount`). This is eventual consistency by design.

<FlowDiagram
  label="Cross-aggregate reference by ID in the Sales context"
  caption="Quote holds a customerId (a typed value object) to reference the Customer aggregate. No direct object reference. Each aggregate is loaded and transacted independently. The arrow represents an ID reference, not a source-code import or object ownership."
  nodes={[
    { id: "quote", label: "Quote aggregate", sub: "id, customerId (reference), lineItems, status" },
    { id: "customer", label: "Customer aggregate", sub: "id, creditLimit, accountStatus, openQuoteCount" },
    { id: "quote-repo", label: "QuoteRepository", sub: "load(quoteId): Quote" },
    { id: "customer-repo", label: "CustomerRepository", sub: "load(customerId): Customer" },
  ]}
  edges={[
    { from: "quote", to: "customer", label: "customerId (ID ref only)" },
    { from: "quote-repo", to: "quote", label: "hydrates" },
    { from: "customer-repo", to: "customer", label: "hydrates" },
  ]}
/>

## Sizing aggregates: by invariant, not by data graph

The most common aggregate design mistake is making aggregates too large. Teams look at the object graph — Quote has LineItems, LineItems have Products, Products have PricingRules, PricingRules have Tiers — and bundle everything into one massive aggregate because "it all belongs together."

The correct sizing question is: **what is the invariant that must hold transactionally?** If the invariant is "total line-item value must not exceed the credit limit at submission," then the aggregate is Quote + LineItems. That is it. `Product` is a separate aggregate — its own rules (name, description, catalog status) have nothing to do with the Quote's credit-limit rule. `PricingRule` is likely a separate aggregate. The Quote holds a `productId` reference and a `NegotiatedPrice` value object — it does not own the Product.

Small aggregates have two critical advantages:

**Lock contention**: a large aggregate is locked for the entire duration of a transaction. If your `Order` aggregate contains every line item, every shipment, every payment, and every customer interaction log, then every order operation — adding a line item, updating a shipping address, logging a customer note — locks the entire order. In a busy system, this creates serialization bottlenecks. Small aggregates lock only what needs to be locked for the specific invariant.

**Conceptual clarity**: a large aggregate that bundles unrelated things is a signal that the invariants have not been clearly identified. "Everything about an Order" is not an invariant. "The submitted value of a Quote must not exceed the customer's credit limit" is an invariant. Aggregates sized by invariant make the business rules visible in the code.

The default for aggregate size is: **as small as the invariant allows**. Start with the smallest aggregate that enforces the invariant. Expand only when a real invariant demands it — not when the object graph suggests it.

<Infographic
  svg={aggregateSizing}
  label="A two-way comparison of aggregate sizing strategies. Sizing by the data graph produces a large aggregate that bundles everything related, locks the whole graph on every write, cascade-loads data the operation never touches, and hides the spanning invariant. Sizing by the invariant produces a small aggregate that holds only the set the rule needs, locks only the invariant's data so independent aggregates commit in parallel, loads each aggregate alone with others referenced by ID, and makes each rule visible in code with cross-aggregate sync handled by domain events."
  caption="Size by the invariant, not the data graph: the small aggregate trades synchronous cross-aggregate consistency for lower lock contention, independent loading, and clearer rules."
  hue="var(--d-infra)"
/>

<Quiz
  id="arch-06-03-quiz-1"
  lessonSlug="03-aggregates-and-invariants"
  lang="en"
  question="A developer proposes: 'The Order aggregate should contain OrderLineItems, Shipments, and Payments, because all three are part of an order.' A DDD-experienced reviewer objects. What is the reviewer's core argument?"
  choices={[
    { label: "The reviewer's argument is: what invariant spans all three? 'OrderLineItems, Shipments, and Payments are all part of an order' describes a data relationship, not a consistency invariant. If the invariant is 'the total shipped quantity must not exceed the total ordered quantity,' that spans Order and OrderLineItems — not Payments. If the invariant is 'a payment cannot exceed the order total,' that spans Order and Payments — not Shipments. These are separate invariants that can be enforced in separate aggregates. Bundling all three creates a large aggregate that serializes every order operation and loads the entire object graph for operations that need only one slice.", correct: true },
    { label: "The reviewer's argument is that aggregates must map one-to-one to database tables, and these are three tables.", misconception: "Aggregate boundaries are a domain model concern, not a database schema concern. An aggregate can span multiple tables (an Order root with an order_line_items child table). Database tables do not determine aggregate boundaries; the business invariants do." },
    { label: "The reviewer's argument is that aggregates should not exceed 100 lines of code.", misconception: "There is no line-of-code limit for aggregates. The sizing criterion is the invariant. A simple invariant might produce a 20-line aggregate. A complex invariant might produce a 300-line aggregate. The reviewer's objection is about invariant clarity and lock contention, not code size." },
    { label: "The reviewer's argument is that Shipment and Payment belong in separate bounded contexts, so they cannot be in the same aggregate.", misconception: "This may be true (Shipment and Payment might well belong in separate bounded contexts — Shipping and Billing). But that is a separate concern from the aggregate sizing argument. Even within a single context, the sizing question is: what invariant does the aggregate enforce? Bundling things that are 'related' without identifying the spanning invariant is wrong regardless of context placement." },
  ]}
/>

<Quiz
  id="arch-06-03-quiz-2"
  lessonSlug="03-aggregates-and-invariants"
  lang="en"
  question="The Quote aggregate enforces: 'total line-item value must not exceed the customer's credit limit at submission.' The team wants to update the customer's credit limit when a Quote is submitted (to reserve the credit). They propose: inside `quote.submit()`, call `customer.reserveCredit(quote.total())` directly. What is wrong with this design?"
  choices={[
    { label: "Calling `customer.reserveCredit()` inside `quote.submit()` modifies two aggregates in one transaction — Quote (changing its status to Submitted) and Customer (changing its reserved credit). This violates the one-transaction-one-aggregate rule. It also means the Quote aggregate holds a direct object reference to Customer, not just a customerId, which couples the two aggregates' loading and locking. The correct design: `quote.submit()` changes Quote's status and emits a `QuoteSubmitted` domain event. A separate process (a saga or an event handler) handles the event and calls `customer.reserveCredit()` in its own transaction. The credit reservation becomes eventually consistent, which is acceptable — a brief window between Quote submission and credit reservation is usually fine, and the alternative (distributed locking across two aggregates) is worse.", correct: true },
    { label: "The call is fine because it goes through the Customer aggregate root (`customer.reserveCredit()`) rather than mutating the Customer's fields directly.", misconception: "Going through the aggregate root is necessary but not sufficient. The problem is not how the Customer is mutated — it is that a single transaction is mutating two aggregates. The aggregate root rule ensures invariants within an aggregate are enforced. It does not make cross-aggregate mutation in one transaction acceptable." },
    { label: "The problem is that `quote.submit()` should not know about credit limits — that logic belongs in a domain service.", misconception: "The credit-limit check at submission IS the Quote aggregate's invariant — it is appropriate for the aggregate root to enforce it. The problem is not where the invariant check lives; it is that enforcing it by mutating a separate aggregate in the same transaction violates the aggregate boundary. A domain service could orchestrate the cross-aggregate interaction, but via separate transactions and domain events — not via a direct call within `submit()`." },
    { label: "This design is correct. The quote and the customer credit reservation are part of the same business operation and must happen atomically.", misconception: "The assumption that 'same business operation' requires 'same transaction' is the root cause of many over-engineered aggregates. Most cross-aggregate consistency requirements can tolerate a brief eventual consistency window. The alternative — locking two aggregates in one transaction — introduces distributed lock contention and defeats the scalability benefit of small aggregates. Design for eventual consistency between aggregates unless a hard transactional requirement has been explicitly validated with the business." },
  ]}
/>

<Quiz
  id="arch-06-03-quiz-3"
  lessonSlug="03-aggregates-and-invariants"
  lang="en"
  question="A team has an `Order` aggregate that contains every `LineItem`, and they are experiencing lock contention: order processing is slow because two operations that touch different line items on the same order cannot run concurrently. A developer proposes: 'Make each LineItem its own aggregate.' What is the trade-off the team must evaluate before doing this?"
  choices={[
    { label: "Splitting LineItem into its own aggregate removes the lock contention — operations on different line items can now run in parallel transactions. But it also removes the ability to enforce any invariant that spans the order and its line items in a single transaction. If there is an invariant like 'the total order value must not exceed X,' that invariant can no longer be checked atomically — it becomes eventually consistent, enforced by a process that reads all LineItems after the transaction. The team must confirm with domain experts whether any spanning invariant exists. If none does — if LineItems are truly independent — the split is correct. If a spanning invariant exists, the team must either accept eventual enforcement or restructure the invariant.", correct: true },
    { label: "Splitting LineItem into its own aggregate is always safe because aggregates should be as small as possible.", misconception: "Small aggregates are the default, but 'as small as possible' means 'as small as the invariant allows' — not 'as small as technically feasible.' If a business invariant spans the order and its line items, splitting them breaks the ability to enforce that invariant atomically. The sizing is driven by the invariant, not by a general preference for small." },
    { label: "The correct fix for lock contention is to use optimistic locking on the Order aggregate, not to split it.", misconception: "Optimistic locking reduces the cost of contention but does not eliminate it — concurrent operations on the same aggregate still fail and retry. If the contention is structural (two operations that have no logical conflict are serialized because they touch the same aggregate), the root cause is that the aggregate is too large. Optimistic locking is a tool for handling genuine conflicts; it does not fix artificial contention caused by oversized aggregates." },
    { label: "LineItems can never be their own aggregate because they do not have a natural identity without an Order.", misconception: "Entities within an aggregate do have identity (a LineItem has an ID within the scope of an Order). When promoted to their own aggregate, they gain a globally unique identity and their own lifecycle. Whether it makes domain sense for a LineItem to exist and be modified independently of its Order is exactly the invariant question — the answer depends on the business rules, not on whether the object currently has identity." },
  ]}
/>

</Explanation>

<KeyTakeaway>An aggregate is a transactional consistency boundary — a cluster of objects with one root that enforces a specific business invariant. All mutations go through the root. One transaction modifies one aggregate. Other aggregates are referenced by ID only. Size aggregates by the invariant they must enforce, not by the data graph — small is the right default, and the tension between small aggregates and cross-aggregate consistency is resolved through domain events and eventual consistency.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="arch-06-03-retrieval"
  lang="en"
  questions={[
    { q: "What is the aggregate root, and why is it the only external entry point?", a: "The aggregate root is the entity at the top of the aggregate's object graph — the one that owns all internal objects and enforces the aggregate's invariants. It is the only external entry point because the invariant can only be checked over the full state of the aggregate. If external code could mutate an internal entity directly (bypassing the root), it could change the aggregate's state without triggering the invariant check. The batch job in the opening story is exactly this failure: it updated LineItem directly, bypassing Quote, and the credit-limit invariant never ran. All mutations enter through the root, the root loads all necessary internal state, it applies the change, it checks the invariant, and it either commits the consistent state or rejects the operation." },
    { q: "Why does 'one transaction modifies one aggregate' follow from the aggregate boundary?", a: "The aggregate boundary says: this cluster of objects has one consistent state, enforced by one root. If a single transaction modifies two aggregates, there is no single root enforcing consistency across both — each root enforces only its own invariants. You can check each aggregate's invariants individually, but if there is a cross-aggregate invariant, neither root knows about it, so it goes unchecked. The rule 'one transaction, one aggregate' ensures every transaction has exactly one root to enforce, and the invariant check is complete. Cross-aggregate consistency that genuinely requires synchronous enforcement is a signal that the two objects belong in the same aggregate. Cross-aggregate consistency that can tolerate a brief window is handled via domain events in separate transactions." },
    { q: "What is the correct criterion for sizing an aggregate, and why does 'size by data graph' fail?", a: "The correct criterion: what is the smallest set of objects that must be loaded and locked together to enforce a specific business invariant? That set is the aggregate. Sizing by data graph ('everything related to an Order') fails because relatedness is not a consistency requirement. LineItems are related to Orders in the data graph, but Shipments, Payments, and Customer notes are also related to Orders. If you bundle all of them, you have an aggregate that holds every order operation under one lock. The lock contention is severe, and no single invariant actually spans all of it. 'Size by invariant' produces small aggregates by default, because most invariants span only a handful of objects. You expand only when a specific invariant requires it." },
  ]}
/>

<Recap lang="en">
The opening story's concurrency bug came from splitting one consistency unit — the Quote with its credit-limit invariant — into two separately mutable objects. The fix was not a new lock; it was recognizing that Quote and its LineItems form one aggregate, with Quote as the root that enforces the credit-limit rule across all line items in every transaction.

An aggregate is a transactional consistency boundary. The root is the only entry point. All mutations flow through it so the invariant check always runs. One transaction modifies one aggregate. Other aggregates are referenced by ID — no direct object references cross aggregate boundaries.

Sizing is the critical skill. The question is not "what is related?" but "what invariant must hold transactionally?" Start with the smallest aggregate that enforces the invariant. Resist the pull to include everything related — large aggregates serialize unrelated operations and load unnecessary data. Cross-aggregate consistency is resolved through domain events and eventual consistency, which is almost always acceptable because very few business rules require simultaneous atomic changes across multiple domain objects.

The next lesson examines how multiple bounded contexts relate to each other — the strategic integration patterns that let independent contexts communicate without leaking their models into each other.
</Recap>
