infographic compare-quadrant-quarter-simple-card
data
  title Dual-write: four outcomes, two dangerous
  desc DB write × broker publish share no atomicity — lost and phantom events are the failures retry can't fix
  compares
    - label DB ok, publish ok: correct
    - label DB ok, publish fails: event lost
    - label DB fails, publish ok: phantom event
    - label DB fails, publish fails: safe retry
theme
  palette #10b981 #f59e0b #f43f5e #6366f1
