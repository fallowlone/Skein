---
slug: "01-choreography-vs-orchestration"
lang: en
track: "architecture-patterns"
unit: "09-event-driven"
order: 1
title: "Choreography vs orchestration"
summary: "Choreography lets services react to events with no central coordinator — the flow is implicit. Orchestration uses a central process-manager that explicitly directs each step. The real tradeoff is coupling vs visibility."
estMin: 25
status: ready
lessonType: topic
level: senior
concepts:
  - choreography
  - orchestration
  - process-manager
  - event-driven
  - saga-orchestrator
  - coupling-vs-visibility
prereqs: []
sources:
  - https://microservices.io/patterns/data/saga.html
  - https://www.enterpriseintegrationpatterns.com/patterns/messaging/ProcessManager.html
  - https://martinfowler.com/articles/201701-event-driven.html
  - https://microservices.io/patterns/decomposition/event-driven.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import couplingVsVisibility from "./infographics/coupling-vs-visibility.svg?raw";

<Hook>
A team at a B2B billing platform started with four services communicating via events — choreography. Each service subscribed to events from other services and emitted its own. The overall order flow existed as emergent behavior across the subscriptions. Eighteen months later they had twelve services. When a new requirement arrived — "pause the order process if the customer exceeds their credit limit" — nobody knew which service to change. They spent three days tracing logs to map the implicit flow across twelve event subscriptions. Then they considered switching to an orchestrated saga pattern, which would make the flow explicit in one place. But that meant a central orchestrator that all twelve services would depend on. They had traded one problem for another.
</Hook>

<Crux>Choreography: implicit flow — decoupled but invisible. Orchestration: explicit flow in one place — visible but a coupling hub.</Crux>

<Explanation>

## Choreography: implicit flow from event subscriptions

In a choreographed system, each service knows only its own role: "when I see event X, I do Y and emit Z." There is no central authority. The overall process flow is not declared anywhere — it emerges from the combination of subscriptions across all services.

The B2B billing example with four services:

- `OrderService` receives a customer request and emits `OrderPlaced`
- `PaymentService` subscribes to `OrderPlaced`, charges the customer, emits `PaymentProcessed`
- `InventoryService` subscribes to `PaymentProcessed`, reserves stock, emits `InventoryReserved`
- `ShippingService` subscribes to `InventoryReserved`, schedules delivery, emits `ShipmentScheduled`

No service knows the full picture. `PaymentService` does not know that `ShippingService` exists. The flow is loosely coupled: adding a fifth service that reacts to `PaymentProcessed` requires zero changes to `PaymentService`. But the flow is invisible: the only way to understand the complete order lifecycle is to read every service's subscription and event catalog.

<FlowDiagram
  label="Choreography: services react to events with no central coordinator"
  caption="Each service subscribes to events from the previous step and emits its own. The overall order flow is implicit — it exists only as the aggregate behavior of four independent subscriptions. No service is aware of the full flow."
  nodes={[
    { id: "order", label: "OrderService", sub: "emits OrderPlaced" },
    { id: "payment", label: "PaymentService", sub: "subscribes: OrderPlaced, emits PaymentProcessed" },
    { id: "inventory", label: "InventoryService", sub: "subscribes: PaymentProcessed, emits InventoryReserved" },
    { id: "shipping", label: "ShippingService", sub: "subscribes: InventoryReserved, emits ShipmentScheduled" },
    { id: "done", label: "Flow complete", sub: "no further events" },
  ]}
  edges={[
    { from: "order", to: "payment", label: "OrderPlaced" },
    { from: "payment", to: "inventory", label: "PaymentProcessed" },
    { from: "inventory", to: "shipping", label: "InventoryReserved" },
    { from: "shipping", to: "done", label: "ShipmentScheduled" },
  ]}
/>

## Orchestration: explicit flow in one place

In an orchestrated system, a central process-manager — the orchestrator — holds the complete business flow and directs each participant step by step. The orchestrator knows: "first charge the customer, then reserve inventory, then schedule shipment." Participants are called by the orchestrator; they do not subscribe to each other's events.

The B2B billing example with an `OrderSaga` orchestrator:

- `OrderSaga` receives an order creation command
- It calls `PaymentService`: "charge customer X for order Y"
- On success, it calls `InventoryService`: "reserve stock for order Y"
- On success, it calls `ShippingService`: "schedule delivery for order Y"
- If any step fails, `OrderSaga` knows exactly what has been done and can coordinate compensation

The flow is now explicit and visible in one place. When the requirement "pause if credit limit exceeded" arrives, there is one service to change: `OrderSaga`. The tradeoff is that all participants now depend on the orchestrator — it has become a coupling hub.

<FlowDiagram
  label="Orchestration: a central OrderSaga directs each service step by step"
  caption="The OrderSaga orchestrator holds the complete flow and directs participants explicitly. No service subscribes to another. The flow is visible and modifiable in one place, but all participants depend on the orchestrator."
  nodes={[
    { id: "saga", label: "OrderSaga", sub: "central orchestrator, owns the flow" },
    { id: "payment", label: "PaymentService", sub: "step 1: charge customer" },
    { id: "inventory", label: "InventoryService", sub: "step 2: reserve stock" },
    { id: "shipping", label: "ShippingService", sub: "step 3: schedule delivery" },
  ]}
  edges={[
    { from: "saga", to: "payment", label: "1. charge" },
    { from: "payment", to: "saga", label: "charged" },
    { from: "saga", to: "inventory", label: "2. reserve" },
    { from: "inventory", to: "saga", label: "reserved" },
    { from: "saga", to: "shipping", label: "3. schedule" },
    { from: "shipping", to: "saga", label: "scheduled" },
  ]}
/>

<Quiz
  id="arch-09-01-quiz-1"
  lessonSlug="01-choreography-vs-orchestration"
  lang="en"
  question="The B2B billing platform's team traced their 12-service choreographed system for three days trying to find where to implement 'pause if credit limit exceeded.' A developer proposes switching to full orchestration. A senior engineer objects: 'We should not switch the entire system — there is a structural reason choreography became hard to trace, but that reason is specific to this flow.' What structural property of choreography caused the traceability problem, and what is the senior engineer implying?"
  choices={[
    { label: "The traceability problem came from the implicit, emergent nature of the flow. In choreography, the process exists only as the combined behavior of all subscriptions — it is not declared anywhere. As services multiply, no single place describes what happens in what order, which services are involved, or what constitutes a complete transaction. The senior engineer is implying that the solution is not to abandon choreography globally, but to recognize that flows with many participants, frequent business-rule changes, or complex coordination requirements are poor fits for choreography. For simpler, stable flows between few services, choreography remains appropriate.", correct: true },
    { label: "The traceability problem came from poor documentation. If the team had maintained a service-dependency diagram, they would have found the right service in minutes. The senior engineer is suggesting they document before rewriting.", misconception: "Documentation is a symptom treatment, not a structural fix. In choreography, the flow is genuinely implicit — it exists only at runtime as the aggregate behavior of subscriptions. Documentation of a choreographed system becomes stale as services evolve. The problem is architectural: the coupling topology is distributed across subscription definitions in every service, not consolidated in one place." },
    { label: "The traceability problem came from insufficient logging. If each service logged every event it processed, the flow would be traceable via distributed tracing. The senior engineer is suggesting they add tracing before switching patterns.", misconception: "Distributed tracing helps observe what happened in a running system, but it does not make the intended flow visible at design time. The requirement 'pause if credit limit exceeded' is a design-time question: which service owns this logic? Tracing cannot answer that. The structural problem is that in choreography, no service owns the flow, so there is no obvious owner for new cross-cutting business rules." },
    { label: "The traceability problem came from using events instead of synchronous RPC calls. If services called each other synchronously, the call graph would be traceable. The senior engineer is suggesting they replace events with direct calls.", misconception: "Synchronous RPC calls have their own coupling and availability problems, and traceability is not guaranteed — deeply nested synchronous call chains can be as hard to follow as event subscriptions. More importantly, the lesson's tradeoff is specifically between choreography and orchestration within event-driven systems, not between event-driven and synchronous architectures." },
  ]}
/>

## The real tradeoff: coupling vs visibility

The binary framing — "choreography is decoupled, orchestration is coupled" — obscures the real tradeoff. Both patterns involve coupling; they just couple different things differently.

**Choreography coupling:** each service is coupled to the event schema. `PaymentService` is coupled to the shape of `OrderPlaced`. If `OrderPlaced` gains a required field, `PaymentService` must be updated. Services are decoupled from each other's existence but coupled to the event contract.

**Orchestration coupling:** each service is coupled to the orchestrator's expectations. `PaymentService` must respond to the orchestrator's `ChargeCustomer` command in a way the orchestrator understands. The orchestrator is coupled to all participants.

The visibility/control dimension is where orchestration wins clearly: the flow is auditable, testable, and modifiable in one place. Business rules ("pause if credit limit exceeded", "retry payment twice before failing") live in the orchestrator, not scattered across subscriber logic. This matters most in flows with frequent requirement changes or complex multi-step coordination.

<Infographic
  svg={couplingVsVisibility}
  label="Side-by-side comparison of choreography (left) and orchestration (right) across five dimensions: flow, coupling, visibility, ownership of a new cross-cutting rule, and best fit. Choreography's flow is implicit and coupled to the event schema; orchestration's flow is explicit and coupled to a central orchestrator."
  caption="Both patterns couple — choreography to the event schema, orchestration to the coordinator. The real tradeoff is visibility: orchestration makes the flow auditable and changeable in one place, choreography keeps it implicit but invisible."
  hue="var(--d-infra)"
/>

<Inset kind="note">
Neither pattern is a default. Choreography suits stable, simple flows between few services where the decoupling benefit outweighs the visibility cost. Orchestration suits complex, frequently-changing flows where business rules must be legible, auditable, and modifiable without touching participant services. Many real systems use both: choreography for independent domain events (a notification service reacting to any order event), orchestration for complex coordinated transactions (the order fulfillment saga).
</Inset>

<Quiz
  id="arch-09-01-quiz-2"
  lessonSlug="01-choreography-vs-orchestration"
  lang="en"
  question="A team is building a new notification system that sends emails when various events occur across their platform: order placed, payment received, shipment scheduled, support ticket opened. They are choosing between choreography and orchestration for the notification flow. Which pattern fits better and why?"
  choices={[
    { label: "Choreography fits better. The notification service can subscribe to each relevant event independently, with no central coordinator needed. The flow is simple: event arrives, send email. Each event-to-notification mapping is independent of the others — there is no multi-step coordination, no compensation required, no complex business-rule logic that changes frequently. Choreography's decoupling benefit is high here (notification logic stays local to the notification service) and its visibility cost is low (the flow is simple enough that the implicit structure is not hard to understand).", correct: true },
    { label: "Orchestration fits better because it gives a central place to manage all notification templates and routing rules, making future changes easier.", misconception: "Notification routing logic can live within the notification service itself — it does not require an orchestrator. The defining reason to choose orchestration is multi-step coordination with compensation, not centralized configuration. A notification service that reads its own routing rules from config is not a choreography vs orchestration problem." },
    { label: "Neither pattern fits — notifications should use a dedicated message queue with point-to-point delivery, not publish/subscribe.", misconception: "This conflates the delivery mechanism (queue vs pub/sub) with the coordination pattern (choreography vs orchestration). The notification system described is event-driven by nature; the question is whether a central coordinator is needed. It is not, because there is no multi-step transaction to coordinate — each notification is an independent reaction to a single event." },
    { label: "Orchestration fits better because with multiple event types you need a central service that knows which template to use for each event.", misconception: "Template selection logic per event type is internal to the notification service — it does not require a central orchestrator external to that service. Orchestration is about coordinating multiple external services through a sequence of steps. A single service choosing behavior based on event type is not orchestration — it is a switch statement." },
  ]}
/>

## When each pattern breaks down

**Choreography breaks down when:**
- The flow spans many services and business rules require cross-service coordination logic
- Requirements change frequently and the right service to modify is not obvious
- Debugging a failed transaction requires tracing events across many services with no single point of truth
- A new "global" business rule must affect the flow but has no natural owner in the choreographed topology

**Orchestration breaks down when:**
- The orchestrator accumulates business logic beyond coordination (it becomes a god-object)
- All flow changes require touching the orchestrator, making it a deployment bottleneck
- The orchestrator becomes a single point of failure for all coordinated transactions
- The coordinator is technically correct but the business team owns participant services independently and the coupling to a shared orchestrator creates organizational friction

The B2B billing platform's 12-service problem was a choreography-breaking-down scenario: too many services, too many cross-cutting business rules, emergent flow too hard to trace. But switching blindly to a monolithic `OrderSaga` that orchestrates all 12 services would create a fragile coupling hub. The right design would likely identify which flows need orchestration (the critical order fulfillment path) and which can stay choreographed (ancillary reactions like audit logging, notifications, metrics).

<Quiz
  id="arch-09-01-quiz-3"
  lessonSlug="01-choreography-vs-orchestration"
  lang="en"
  question="A team's OrderSaga orchestrator has grown to 800 lines. It now contains: routing logic (call PaymentService), business rules ('if customer tier is gold, skip credit check'), retry policies ('retry payment up to 3 times'), and formatting logic ('format the order summary for the shipping label'). A senior engineer says the orchestrator has failed. What failure mode is this, and what is the structural fix?"
  choices={[
    { label: "The orchestrator has become a god-object — it has accumulated business logic, domain rules, retry policies, and presentation concerns that belong in participant services. The structural fix is to return the orchestrator to its coordination role: it directs the sequence (call A, then B, then C) and reacts to outcomes (success/failure) but delegates domain logic to the participants. Credit check logic belongs in PaymentService. Retry policy belongs in the messaging infrastructure or a retry decorator on the call. Label formatting belongs in ShippingService. An orchestrator should be readable as a plain-language description of the business process, not a logic-heavy service.", correct: true },
    { label: "The orchestrator has grown large because orchestration is inherently a bad pattern. The team should refactor to choreography to remove the central coordinator.", misconception: "Orchestrator growth is a code organization problem, not a pattern-level indictment. The fix is to move non-coordination concerns back to participants — not to abandon the pattern. Choreography would reintroduce the traceability problems the team had before. The right move is a leaner orchestrator, not no orchestrator." },
    { label: "800 lines is not inherently a problem. If the logic is well-tested, the orchestrator can be as large as needed to correctly coordinate the flow.", misconception: "Size is a symptom here, but the underlying problem is boundary violation. Business rules, retry policies, and presentation logic do not belong in a coordination component regardless of test coverage. A well-tested god-object is still a god-object — it creates coupling between concerns that should evolve independently, and it makes the overall flow harder to read." },
    { label: "The fix is to split the OrderSaga into multiple smaller sagas, each responsible for a subset of services.", misconception: "Splitting into smaller orchestrators may be the right move if the flow genuinely decomposes into independent sub-flows. But it does not fix the core problem: business logic, retry policies, and formatting code do not belong in any orchestrator regardless of size. The split would produce multiple god-objects instead of one." },
  ]}
/>

</Explanation>

<KeyTakeaway>Choreography = implicit, emergent flow with no central coordinator — loosely coupled but hard to trace as complexity grows. Orchestration = explicit flow in one place — visible and controllable but creates a coupling hub. Choose based on flow complexity and how frequently business rules change.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-09-01-retrieval"
  lang="en"
  questions={[
    { q: "What makes choreography hard to debug when a transaction fails mid-flow?", a: "In choreography the flow is implicit — no single component knows the complete sequence. When a failure occurs, the only way to reconstruct what happened is to correlate events across all participant services' logs using a shared correlation ID. There is no authoritative record of where in the sequence the failure happened, which steps completed, or what state each service is in. With many services the correlation effort scales poorly. An orchestrated system has a single saga record that shows exactly which steps completed, which are pending, and which failed." },
    { q: "What is the key coupling difference between choreography and orchestration?", a: "In choreography, services are coupled to the event schema — each subscriber depends on the shape of the events it consumes. Services do not depend on each other's existence. In orchestration, participant services are coupled to the orchestrator — they must respond to the orchestrator's commands in the expected way. The orchestrator is coupled to all participants. Choreography distributes coupling across event contracts; orchestration concentrates coupling at the coordinator." },
    { q: "Name two signs that a choreographed flow should be migrated to orchestration.", a: "First: the flow involves many services and implementing a new cross-cutting business rule (like 'pause if credit limit exceeded') requires knowing which of many services to change — no obvious owner. Second: debugging a failed multi-service transaction requires tracing events across many services, and the reconstruction effort is disproportionate to the value — the implicit flow has become too complex to reason about without a centralized view of the process state." },
  ]}
/>

<Recap lang="en">
The B2B billing team's three-day log-tracing session was a choreography failure. With twelve services, the implicit flow — which exists only as the aggregate behavior of twelve event subscriptions — had grown beyond the team's ability to reason about it. When a new cross-cutting rule arrived, there was no obvious place to put it.

Choreography's strength is decoupling: services react to events without knowing each other exists. That strength becomes a liability when the flow is complex, when business rules must span multiple participants, or when the "where should this change go?" question takes days to answer.

Orchestration makes the flow explicit in one place. The `OrderSaga` knows the complete sequence, owns the business rules, and is the single place to add "pause if credit limit exceeded." The cost is that all participants depend on the orchestrator — it is a coupling hub. And if the orchestrator accumulates business logic beyond coordination, it becomes a god-object.

The practical pattern: use choreography for simple, stable reactions (notifications, audit logging, metrics) and orchestration for complex, frequently-changing transactions where the flow must be legible, auditable, and modifiable. Many systems use both simultaneously.
</Recap>
