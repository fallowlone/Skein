---
slug: "01-what-architecture-actually-is"
lang: en
track: "architecture-patterns"
unit: "00-start-here"
order: 1
title: "What architecture actually is"
summary: "Architecture is the set of decisions that are expensive to change — not diagrams, not patterns. Design and architecture occupy different cost horizons. 'Best practice' is a category error: context determines fit, not a pattern name."
estMin: 18
status: ready
lessonType: topic
level: middle
concepts:
  - architecture-definition
  - design-vs-architecture
  - fitness-for-context
  - reversibility
  - structural-decision
  - best-practice-fallacy
prereqs: []
sources:
  - https://www.oreilly.com/library/view/fundamentals-of-software/9781492043447/
  - https://martinfowler.com/ieeeSoftware/whoNeedsArchitect.pdf
  - https://www.infoq.com/articles/no-best-practices/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A startup ships a monolith. It works. Two years later the team has grown to thirty engineers and every feature takes six weeks because everything touches the payments module. The CTO calls it an "architecture problem" — but nobody ever decided to couple payments to everything. They just never decided not to. That coupling accumulated one small shortcut at a time, each reasonable in isolation, until reversing it would require rewriting half the system. The team had been making architectural decisions all along — they just hadn't been naming them as such.
</Hook>

<Crux>Architecture is the set of decisions that are hard to undo — the ones whose cost of reversal grows with every line of code you write on top of them.</Crux>

<Explanation>

## The cost-of-change definition

The most useful definition of software architecture is not about boxes-and-arrows diagrams or named patterns. Ralph Johnson put it simply: architecture is the decisions that are hard to change. Martin Fowler refined this to the decisions that, once made, cost more and more to reverse as the system grows around them.

This framing immediately separates architecture from ordinary design. If you choose a variable name today and decide tomorrow you want a better one, a rename refactor takes twenty seconds. That is design — low cost of reversal. If you choose to keep your billing logic inside your HTTP handler and twelve months later decide you want to serve the same billing from a background job, a mobile app, and a partner API, you have a structural problem that will require weeks of risky surgery. That is architecture — the high-cost-of-reversal decision.

The implication is uncomfortable: **you are making architectural decisions constantly, whether or not you call them that.** The question is not whether to make them, but whether to make them intentionally.

<FlowDiagram
  label="Design decisions vs architectural decisions: cost of reversal over time"
  nodes={[
    { id: "day1", label: "Day 1", sub: "decision made" },
    { id: "design", label: "Design decision", sub: "rename a variable" },
    { id: "arch", label: "Architectural decision", sub: "couple billing to HTTP layer" },
    { id: "rev-design", label: "Reversal cost: minutes", sub: "IDE rename refactor" },
    { id: "rev-arch", label: "Reversal cost: weeks", sub: "extract, re-route, re-test" },
  ]}
  edges={[
    { from: "day1", to: "design", label: "low coupling" },
    { from: "day1", to: "arch", label: "high coupling" },
    { from: "design", to: "rev-design", label: "any time" },
    { from: "arch", to: "rev-arch", label: "12 months later" },
  ]}
  caption="The same moment of indecision branches into very different reversal costs depending on how deeply the rest of the system grows around the original choice."
/>

## Design vs architecture: two different horizons

Design and architecture are not two levels of a hierarchy; they operate on different time horizons and cost envelopes.

**Design** is the shape of a solution within an already-settled structural context. Which class holds this method? How should this function be named? How does this module's API look? These choices matter for readability and correctness, but they can be revised without structural surgery. Design decisions live inside the boundaries that architecture has drawn.

**Architecture** draws those boundaries. Where does the boundary between the billing domain and the shipping domain sit? Does each domain own its database schema, or do they share one? Can the ordering service call the billing service synchronously, or must it go through an event? These decisions propagate structurally — they constrain every design decision made downstream of them.

The confusion arises because the same technology can be either. Choosing PostgreSQL as your database for a prototype is a design decision — you can switch. Choosing PostgreSQL as the single shared database whose schema is directly queried by eight services, each assuming specific table structures, is an architectural decision. You have embedded the assumption so deeply that the database cannot be replaced without replacing all eight services at once.

<Inset kind="why" lang="en">
Why does the distinction matter in practice? Because teams treat the two kinds of decisions differently. Design decisions get revised in code review. Architectural decisions get made in conversations that may not be recorded anywhere. This asymmetry is how coupling silently accumulates: nobody says "today we are making an architectural decision to couple billing to the HTTP layer." They say "let's just call the billing function directly from the handler for now" — a sentence that sounds like a design decision but is, in context, an architectural one.
</Inset>

## The "best practice" category error

"Use microservices" is a best practice. "Use hexagonal architecture" is a best practice. "Separate your business logic from your infrastructure" is a best practice. These prescriptions appear in books, talks, and engineering blogs, and they are not wrong — they describe patterns that solved real problems for specific teams in specific contexts. The error is treating the prescription as context-free.

Best practices are solutions to forces. The hexagonal architecture pattern exists because the force "I need to test my business logic without a database" and the force "I need to run the same application from an HTTP server, a CLI, and a message queue" exert real pressure. If those forces are present in your system, hexagonal architecture is a good fit. If your system is a CRUD API that will never have those forces, hexagonal architecture adds layers, interfaces, and indirection without corresponding benefit. It is not "best" for you — it is overkill.

The correct question is never "what is the best practice?" It is "what forces is my system subject to, and what structures answer those forces?" This is the frame that the rest of this track uses. Every pattern we examine will be presented as the answer to a named set of forces — and the tradeoffs it makes in the process.

<FlowDiagram
  label="A pattern is a match between forces and structure — not a universal prescription"
  nodes={[
    { id: "forces", label: "Forces", sub: "testability, multi-entrypoint, no DB in tests" },
    { id: "hex", label: "Hexagonal architecture", sub: "ports + adapters" },
    { id: "fit", label: "Good fit", sub: "forces are present" },
    { id: "overfit", label: "Overkill", sub: "forces are absent" },
    { id: "crud", label: "Simple CRUD API", sub: "no testability pressure" },
  ]}
  edges={[
    { from: "forces", to: "hex", label: "answered by" },
    { from: "hex", to: "fit", label: "system has these forces" },
    { from: "hex", to: "overfit", label: "applied to" },
    { from: "crud", to: "overfit", label: "has different forces" },
  ]}
  caption="A structural pattern answers specific forces. Applied without those forces, the same pattern becomes unnecessary complexity — the same reason hexagonal architecture is brilliant in one system and bureaucratic in another."
/>

## What architecture is not

Two things commonly masquerade as architecture:

**Diagrams.** A diagram describes an architecture — it is not the architecture. Teams often confuse the artifact with the reality. The architecture is the actual coupling structure in the codebase; the diagram is a model of it. When the diagram says "billing is a separate service" but the code shares a database schema with ordering, the diagram is wrong and the coupling is the reality.

**Up-front design.** Architecture does not require a design phase before a line of code. Many of the most durable systems were architected incrementally — each decision made when the forces were understood well enough to make a good one. What architecture requires is intentionality: recognizing which decisions are expensive to change, making them deliberately, and recording the reasons. That can happen at any point in a project.

<Inset kind="note" lang="en">
The practical test for whether a decision is architectural: ask "if I need to reverse this in twelve months, how much of the codebase will I have to touch?" If the answer is "most of it," the decision is architectural regardless of whether it was labeled as such. This test also reveals why apparently minor decisions — like whether a service exposes its internal data model as its public API — become architectural as the system grows consumers.
</Inset>

<Quiz
  id="arch-00-01-quiz-1"
  lessonSlug="01-what-architecture-actually-is"
  lang="en"
  question="A team decides to put billing logic directly inside HTTP route handlers. Six months later, they need billing from a background job and a partner webhook. What makes this an architectural decision rather than a design decision?"
  choices={[
    { label: "The cost of reversing it grows with every feature built on top of the coupled structure — reversing now requires changes across many call sites and tests, not a single refactor.", correct: true },
    { label: "It involved a large file, so it is architectural by definition.", misconception: "File size is not the criterion. The cost-of-reversal test is: how much of the codebase must change to undo this? Coupling billing to HTTP handlers propagates that assumption everywhere billing is touched." },
    { label: "It was a bad decision, and architectural decisions are the bad ones.", misconception: "Architectural decisions are not bad by definition — they are the ones that are expensive to change. A good architectural decision can be excellent; a bad one can be low cost if the system has not grown around it yet." },
    { label: "It touched more than one file, which crosses the threshold into architecture.", misconception: "Scope in files is not the test. A single-file decision that embeds a cross-cutting structural assumption (like 'billing happens inside HTTP') is architectural; a multi-file refactor of variable names is not." },
  ]}
/>

<Quiz
  id="arch-00-01-quiz-2"
  lessonSlug="01-what-architecture-actually-is"
  lang="en"
  question="Your team is building a CRUD API for a simple internal tool that will never have multiple entry points and has no testing pressure on the business logic layer. A senior engineer suggests adopting hexagonal architecture. What is the most accurate response?"
  choices={[
    { label: "Hexagonal architecture answers forces (testability, multi-entrypoint, DB isolation) that are absent here — adopting it would add indirection without the corresponding benefit, making it overkill for this context.", correct: true },
    { label: "Best practices should always be followed regardless of context.", misconception: "Best practices are solutions to forces. Applied without those forces, they are not 'best' — they are overhead. The question is always whether the forces that the pattern addresses are present in your system." },
    { label: "Hexagonal architecture is always better than a layered approach.", misconception: "Neither pattern is universally better. Hexagonal architecture answers specific forces; if those forces are absent, a simple layered approach has less indirection and is often the better fit." },
    { label: "The team should use it to prepare for future requirements.", misconception: "YAGNI applies structurally too. Adding layers for hypothetical future forces is speculative architecture — the forces may never materialize, and the overhead is real from day one. Architect when the forces are understood, not when they are imagined." },
  ]}
/>

<Quiz
  id="arch-00-01-quiz-3"
  lessonSlug="01-what-architecture-actually-is"
  lang="en"
  question="An up-to-date architecture diagram shows billing as an isolated service. The actual code has billing logic called directly from the order service's repository layer via a shared internal library. Which is the real architecture?"
  choices={[
    { label: "The code — the actual coupling in the codebase is the real architecture. The diagram describes a model that does not match reality.", correct: true },
    { label: "The diagram, because it represents the intended architecture.", misconception: "Intention is not architecture. Architecture is the actual coupling structure of the running system. A diagram that diverges from the code is a stale model, not a competing architecture." },
    { label: "Neither — architecture exists only in the design documents approved by the architect.", misconception: "Architecture is an emergent property of the code, not a document. The coupling between modules exists whether or not anyone named it. The practical test is: what must change when requirements change?" },
    { label: "The diagram, because diagrams are the authoritative artifact in architecture.", misconception: "Diagrams describe an architecture; they are not the architecture. The source of truth is the code's actual dependency structure — what modules import what, what data is shared, what can be changed independently." },
  ]}
/>

</Explanation>

<KeyTakeaway>Architecture is the decisions that are expensive to reverse — not diagrams, not patterns. Design operates inside the structure architecture draws. Every named pattern is a solution to specific forces; without those forces, the same pattern is overhead. Ask not "what is the best practice?" but "what forces is my system under, and what structure answers them?"</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-00-01-retrieval"
  lang="en"
  questions={[
    { q: "How do you distinguish an architectural decision from a design decision?", a: "By the cost of reversal over time. A design decision can be reversed quickly (a rename, a method extraction) with no structural surgery. An architectural decision becomes more expensive to reverse with every feature built on top of it — because the rest of the system grows around the assumption it encodes. The practical test: how much of the codebase must change to undo this in twelve months?" },
    { q: "Why is 'best practice' a category error when applied to architectural patterns?", a: "Because patterns are solutions to forces, not universal prescriptions. A pattern like hexagonal architecture answers specific forces: testability without infrastructure, multiple entry points, isolation of the domain. Applied to a system where those forces are absent, the same pattern adds layers and indirection without benefit. The correct question is always: what forces is this system under, and what structure answers them?" },
    { q: "If the architecture diagram and the code disagree, which is the real architecture?", a: "The code. Architecture is the actual coupling structure of the system — what depends on what, what can change independently, what must change together. A diagram that diverges from the code is a stale model. The coupling in the codebase is the reality, regardless of what the diagram says." },
  ]}
/>

<Recap lang="en">
Architecture is the set of decisions that grow more expensive to reverse as the system matures around them. This separates it from design — which operates inside an already-settled structural context and remains cheap to revise. The distinction is not about diagram level or file count: a single-line decision that couples billing to the HTTP layer is architectural; a multi-file rename refactor is not.

Named patterns — hexagonal, clean, DDD, CQRS — are answers to specific forces. They are not best practices to follow universally; they are solutions that are worth the complexity they introduce only when the forces they answer are actually present. Adopting them without the forces is speculative architecture that creates overhead without corresponding benefit.

The diagram is not the architecture — the code is. Intentions, design documents, and box-and-arrow pictures describe the architecture someone wanted. The actual coupling in the codebase is the architecture that exists. Throughout this track, we will name the forces first and the structural answers second — that is the only way to reason about whether a given structure is right for a given system.
</Recap>
