---
slug: "01-command-query-split"
lang: en
track: "architecture-patterns"
unit: "07-cqrs"
order: 1
title: "Command-query split"
summary: "One model optimized for writes and another for reads — CQS principle scaled to architecture. Commands express intent and may be rejected; queries have no side effects. The split begins with two object models, not two databases."
estMin: 22
status: ready
lessonType: topic
level: senior
concepts:
  - cqs-principle
  - cqrs
  - command
  - query
  - write-model
  - read-model
prereqs: []
sources:
  - https://cqrs.files.wordpress.com/2010/11/cqrs_documents.pdf
  - https://martinfowler.com/bliki/CQRS.html
  - https://learn.microsoft.com/en-us/azure/architecture/patterns/cqrs
  - https://udidahan.com/2009/12/09/clarified-cqrs/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import modelShapeSplit from "./infographics/model-shape-split.svg?raw";

<Hook>
The B2B billing platform's order domain had a single `Order` model — a normalized, carefully guarded aggregate. It enforced three invariants: a submitted order's line-item total must not exceed the customer's credit limit; an order cannot be modified once approved; and payment terms must match the customer's contract tier. The team was proud of the model. Every write went through the aggregate root. Invariants held. Then the product team asked for a new orders dashboard: the customer-facing screen that shows every order a company has placed, with status, total, assigned account manager name, the last payment event, and whether the order is overdue. The query touched seven tables. It joined `orders`, `line_items`, `customers`, `contracts`, `account_managers`, `payments`, and `billing_events`. The ORM loaded the full aggregate graph for every order on screen, then discarded 80% of the data after rendering the view. Response time on the dashboard was 3.4 seconds. The team tried adding indexes, denormalizing a few columns, and caching the response. Every fix made the write model slightly worse — the denormalized columns had to be kept in sync; the cache invalidation logic leaked into the command handlers. The real problem was structural: the same model was being asked to serve two incompatible purposes. For writes, the model needed to be normalized, invariant-guarding, and aggregate-shaped. For reads, it needed to be denormalized, view-shaped, and joined across many entities. No single model can do both well. That tension — one model, two irreconcilable purposes — is what CQRS resolves.
</Hook>

<Crux>CQRS splits write from read: commands change state through a guarded aggregate; queries read from a view-optimized projection.</Crux>

<Explanation>

## CQS: the principle behind the pattern

CQRS builds on a simpler idea from Bertrand Meyer: Command-Query Separation (CQS). The principle says a method should either change state (a command) or return a value (a query), but never both. A method that returns a value and also has a side effect violates CQS — callers cannot safely call it multiple times, and the return value can no longer be trusted to be pure.

Applied at the object level:
- `order.submit()` — command, changes state, returns nothing (or a domain event)
- `order.getTotal()` — query, reads state, returns a value, changes nothing

The value of CQS at the object level is clarity: you can call a query method without fear of side effects. You know a command will not return data that depends on the caller reading it.

CQRS takes the same separation and scales it to the **architectural level**: instead of one object with command methods and query methods, you have two distinct **models** — one for writes, one for reads.

<FlowDiagram
  label="CQS at method level vs CQRS at model level"
  caption="CQS separates command and query methods on the same object. CQRS separates them into two distinct models. The write model is normalized and invariant-guarding; the read model is denormalized and view-shaped. Arrows show the direction of data flow from command through the write model, and from a query through the read model."
  nodes={[
    { id: "client", label: "Client", sub: "issues commands or queries" },
    { id: "cmd", label: "Command handler", sub: "PlaceOrder, SubmitOrder, ApproveOrder" },
    { id: "write", label: "Write model (aggregate)", sub: "normalized, invariant-guarding, transactional" },
    { id: "qry", label: "Query handler", sub: "GetOrderDashboard, GetOrderSummary" },
    { id: "read", label: "Read model (projection)", sub: "denormalized, view-shaped, no invariants" },
  ]}
  edges={[
    { from: "client", to: "cmd", label: "command" },
    { from: "cmd", to: "write", label: "mutates" },
    { from: "client", to: "qry", label: "query" },
    { from: "qry", to: "read", label: "reads" },
  ]}
/>

## Why one model cannot serve both purposes

A write model is shaped by invariants. The `Order` aggregate is normalized because invariants must be checked over a consistent, minimal set of data. If you denormalize — storing the account manager's name on the order row — then every name change in `account_managers` must propagate to every order. The aggregate now has to protect two invariants: the business invariants about orders, and a referential integrity invariant about denormalized data. These invariants fight each other.

A read model is shaped by views. The dashboard query needs account manager name, last payment event, and overdue status in a single fast fetch. No invariant guards these fields — they just need to be correct and fast. The right structure is a pre-joined, denormalized row that the query can return directly without loading the full aggregate graph.

The structural incompatibility is real: normalization serves writes; denormalization serves reads. When forced to coexist in one model, each degrades the other.

<Infographic
  svg={modelShapeSplit}
  label="Side-by-side comparison of the write model and the read model. The write model is normalized, invariant-guarding, aggregate-shaped and transactional, and handles commands. The read model is denormalized, view-shaped, has no invariants, and answers queries."
  caption="The two concerns have opposite structural requirements — normalization for writes, denormalization for reads — which is exactly why CQRS gives each its own model."
  hue="var(--d-infra)"
/>

<Inset kind="why">
This is not primarily a performance optimization. It is a modeling observation. A normalized aggregate is the right shape for enforcing invariants transactionally. A denormalized view is the right shape for answering questions efficiently. Trying to make one structure serve both roles means it is the right shape for neither. CQRS is not "add a read replica" — it is a modeling decision that says the write concern and the read concern have different structural requirements and should be modeled separately.
</Inset>

## Commands: intent, not CRUD

A command in CQRS is not a record update. It is an expression of **intent** from the outside world toward the domain. `SubmitOrder` is a command. `ApproveOrderForShipping` is a command. `RejectLineItemNegotiation` is a command. These names come from the domain language (see unit 06 on DDD and ubiquitous language).

Commands may be **rejected**. When a `SubmitOrder` command arrives and the order total exceeds the customer's credit limit, the write model rejects it. The command did not silently fail — it was evaluated against an invariant and explicitly refused. The command handler returns a domain event on success or a domain error on rejection.

This is different from a generic `PATCH /orders/:id`. A PATCH says nothing about intent. A command says exactly what the actor intends to do and why the domain might refuse it.

Commands also make the write model's responsibility clearer. The write model handles commands. It does not answer queries. Its job is to receive an expression of intent, check invariants, mutate state if the invariants pass, and emit events. That is the entire job.

## Queries: no side effects, view-shaped

A query in CQRS is a question. `GetOrderDashboard(customerId)`, `GetOrderSummaryForApproval(orderId)`, `ListOverdueOrdersForAccountManager(managerId)` are queries. They return data. They do not change state. They may be called multiple times safely.

Because queries have no side effects, they do not need to go through the write model's invariant machinery. They read from the read model directly — a pre-built, view-shaped data structure optimized for the specific question being asked.

Different queries may have different read models. The dashboard query and the approval summary query serve different views with different data needs. CQRS allows each query to be backed by the most efficient read model for its specific shape.

<Inset kind="note">
Separate models do not mean separate databases — at least not initially. In the simplest CQRS implementation, the write model and the read model are two different in-process object graphs reading from the same database. The write model reads normalized rows and enforces invariants; the read model executes a different, view-optimized query against the same database, perhaps using a database view or a query that returns a flat DTO. Separate databases (a write store and a read store) are an **optional infrastructure step** that becomes relevant when the scaling or latency requirements justify it. Many teams apply CQRS at the model level and never reach the point where separate stores are needed.
</Inset>

<Quiz
  id="arch-07-01-quiz-1"
  lessonSlug="01-command-query-split"
  lang="en"
  question="A developer says: 'We implemented CQRS by adding a read replica database. Commands go to the primary, queries go to the replica.' Has CQRS been correctly applied?"
  choices={[
    { label: "No. Adding a read replica is a replication topology decision, not a CQRS model split. CQRS means the write concern and the read concern are modeled separately — two distinct object graphs with different shapes. If the 'read model' on the replica is the same normalized aggregate the write side uses, just on a different server, the structural problem is unchanged: reads still load the full aggregate graph and discard most of it. CQRS starts with modeling the read side differently — as denormalized, view-shaped projections — regardless of whether the physical storage is the same server or a separate replica.", correct: true },
    { label: "Yes. Routing commands to the primary and queries to the replica is exactly what CQRS prescribes.", misconception: "CQRS is a modeling pattern, not an infrastructure topology. Routing to a read replica does not change the model. The write model and the read model in CQRS are two different object graph shapes with different structural requirements — not the same model on two physical servers." },
    { label: "Partially. The infrastructure is correct but the team also needs to add an event bus between the primary and replica.", misconception: "The infrastructure split is irrelevant to whether CQRS has been applied. CQRS is about having two distinct models — one shaped for writes, one shaped for reads. An event bus is one mechanism for keeping a separately-stored read model in sync with the write side, but it is only needed if you choose to store the read model separately. The primary issue here is that the model shape has not been separated." },
    { label: "Yes, because writes and reads now go to different servers, satisfying the command/query separation.", misconception: "Separation of servers is not the same as separation of models. A command and a query can go to different servers while using the exact same model shape on both — the write model on the primary and the same model on the replica. CQRS requires different model shapes, not different servers." },
  ]}
/>

<Quiz
  id="arch-07-01-quiz-2"
  lessonSlug="01-command-query-split"
  lang="en"
  question="In the CQS principle, a method that returns a value AND changes state violates CQS. Why is this a structural problem and not just a style preference?"
  choices={[
    { label: "A method that returns a value and changes state cannot be safely called multiple times. The second call returns a different value (because the first call already mutated state) or has already exhausted a side-effect (e.g. popped a queue item). Callers cannot reason about the return value in isolation — they must know the state the method leaves behind. This creates hidden temporal coupling: the return value is only correct on the first call in a given sequence. Separating commands and queries eliminates this: queries are referentially transparent (safe to call any number of times), commands are explicitly state-changing (callers know not to expect a value). The clarity is structural, not stylistic.", correct: true },
    { label: "It violates CQS because it makes the method harder to name — you can't name it as both a verb (command) and a noun (query) simultaneously.", misconception: "Naming difficulty is a symptom of a structural problem, not the cause. The structural problem is temporal coupling and the inability to safely call the method multiple times. The naming difficulty arises because the method is doing two things; the real issue is what that does to callers." },
    { label: "It is only a style preference. CQS is a guideline, and mixing command and query in a single method is acceptable when it simplifies the API.", misconception: "CQS is a design principle with structural consequences. A method that both returns a value and mutates state creates hidden temporal coupling. Callers cannot safely call it twice, cannot cache the return value, and cannot reason about the return value without understanding the side effect. These are structural correctness issues, not style preferences." },
    { label: "It is a problem only in concurrent systems, because concurrent callers might observe different return values.", misconception: "The problem exists in single-threaded code too. Even without concurrency, calling a method that returns-and-mutates twice returns different values for the same apparent input state. The structural issue is that the return value has a dependency on call order, which is a form of temporal coupling regardless of threading." },
  ]}
/>

<Quiz
  id="arch-07-01-quiz-3"
  lessonSlug="01-command-query-split"
  lang="en"
  question="The team applies CQRS to the Order domain. A junior engineer proposes: 'The SubmitOrder command handler should return the full Order DTO so the UI can refresh the screen immediately.' What is the CQRS-aligned response?"
  choices={[
    { label: "Returning a full Order DTO from a command handler blurs the command/query boundary. A command's job is to change state and emit a domain event; returning a view-shaped DTO makes the command handler also responsible for building query results, coupling it to the read model's shape. The CQRS-aligned approach: the command handler returns either nothing (void) or a minimal acknowledgment (e.g. the new orderId or a success/failure signal). The UI then issues a query to the read model to fetch the updated view. This keeps the write side and read side cleanly separated. A brief extra round-trip is acceptable — and the read model is usually fast because it is denormalized and view-optimized.", correct: true },
    { label: "The proposal is fine. Returning the DTO from the command is just an optimization that avoids an extra round-trip, and CQRS does not prohibit it.", misconception: "While pragmatic compromises exist in real systems, the structural cost is real: the command handler now couples itself to the read model's shape. Changes to the read model (adding a field, changing a join) require changes to the command handler. The command handler has taken on a second responsibility. In a CQRS system designed for maintainability, the separation is worth the extra round-trip." },
    { label: "The proposal is wrong because command handlers must always return void — returning anything from a command is a CQS violation.", misconception: "Returning a minimal acknowledgment (an orderId, a success/error flag, a domain event) from a command handler is not a CQS violation — these are not query results. CQS violations occur when a method returns state-dependent data while also changing state. Returning a freshly generated identifier or a confirmation token is a common pragmatic exception that does not couple the command handler to the read model's shape." },
    { label: "The proposal is correct because in CQRS the write model should always return the current state after a command so the read model can be updated.", misconception: "The write model's job after a command is to emit domain events, not to return view-shaped data. The read model is updated by projecting from those domain events — not by receiving a DTO from the command handler. Returning the current state from the command handler couples the write and read sides in the wrong direction." },
  ]}
/>

</Explanation>

<KeyTakeaway>CQRS separates the write model (normalized, invariant-guarding, aggregate-shaped) from the read model (denormalized, view-shaped, query-optimized). Commands express intent toward the domain and may be rejected; queries answer questions with no side effects. The split is a modeling decision — two object graphs with different structures. Separate databases are optional infrastructure, not the definition of CQRS.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-07-01-retrieval"
  lang="en"
  questions={[
    { q: "What is the structural incompatibility that makes one model serve both reads and writes poorly?", a: "A write model needs to be normalized so invariants can be checked over a minimal, consistent set of data without maintaining denormalized redundancy. A read model needs to be denormalized so queries can return view-shaped data in a single fast fetch without loading the full aggregate graph. Normalization and denormalization are structurally opposite. When forced into one model, each degrades the other: denormalized columns must be kept in sync with writes (adding complexity to command handlers), and normalized reads must join many tables and load data that gets discarded (degrading query performance). CQRS resolves the incompatibility by allowing each side to have the shape it actually needs." },
    { q: "What is the difference between a command and a query in CQRS, and why can commands be rejected?", a: "A command is an expression of intent toward the domain — SubmitOrder, ApproveLineItem, RejectNegotiation. It changes state and returns nothing (or a minimal acknowledgment). Commands may be rejected because the write model checks invariants before applying the change; if an invariant fails (e.g. order total exceeds credit limit), the command is refused. A query is a question — GetOrderDashboard, ListOverdueOrders. It reads from the read model, returns data, and has no side effects. Queries cannot be 'rejected' in the same sense — they either return data or return empty results. The key distinction: commands are evaluated against business rules and may fail; queries are just reads." },
    { q: "Does CQRS require separate databases for the write and read sides?", a: "No. Separate databases are optional infrastructure, not the definition of CQRS. In the simplest CQRS implementation, the write model and read model are two different in-process object graphs reading from the same database — the write model loads normalized rows and enforces invariants; the read model executes a different, view-optimized query (perhaps a database view or a flat DTO query) against the same database. Separate stores become relevant when the scaling or latency requirements justify them — e.g. when the read side needs to be on a different storage technology optimized for the query pattern. Many teams apply CQRS at the model level and never need separate stores." },
  ]}
/>

<Recap lang="en">
The opening story's 3.4-second dashboard was not a missing index problem. It was a modeling mismatch: one aggregate shaped to guard invariants was being used to serve a view that needed denormalized data from seven tables. The structural tension between normalization (writes need it) and denormalization (reads need it) is what CQRS names and resolves.

CQS — Meyer's principle — separates command and query methods on the same object. CQRS scales that separation to the architectural level: two distinct models, one per concern. The write model is normalized, invariant-guarding, and aggregate-shaped. The read model is denormalized, view-shaped, and query-optimized. Commands express intent and may be rejected. Queries answer questions and have no side effects.

The split is a modeling decision, not an infrastructure deployment. Separate databases are optional. A team can implement CQRS with two object graphs reading the same database and gain most of the structural benefit.

The next lesson examines the read side in depth: how denormalized read models are built by projecting from the write side, the synchronization problem this creates, and how to bound the staleness.
</Recap>
