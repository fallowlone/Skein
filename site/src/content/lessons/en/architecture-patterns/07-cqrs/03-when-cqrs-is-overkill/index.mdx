---
slug: "03-when-cqrs-is-overkill"
lang: en
track: "architecture-patterns"
unit: "07-cqrs"
order: 3
title: "When CQRS is overkill"
summary: "CQRS buys structural separation at real cost: two models, eventual consistency, projection infra. It pays off only where read/write asymmetry is genuine. Most CRUD does not qualify. CQRS and event sourcing are independent — conflating them doubles the cost."
estMin: 20
status: ready
lessonType: topic
level: senior
concepts:
  - cqrs-cost
  - partial-cqrs
  - crud
  - cqrs-vs-event-sourcing
  - read-write-asymmetry
  - eventual-consistency
prereqs: []
sources:
  - https://martinfowler.com/bliki/CQRS.html
  - https://cqrs.files.wordpress.com/2010/11/cqrs_documents.pdf
  - https://udidahan.com/2009/12/09/clarified-cqrs/
  - https://learn.microsoft.com/en-us/azure/architecture/patterns/cqrs
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import cqrsVsEventSourcing from "./infographics/cqrs-vs-event-sourcing.svg?raw";

<Hook>
After the billing platform team shipped CQRS for the order domain, the pattern became the team's default answer. The account management module needed a screen to edit company details — name, address, billing contact. A developer proposed splitting it into a `CompanyAggregate` write model and a `CompanyProfileView` read model with an async projection. The tech lead pushed back: "What invariant does the write model guard? What read/write asymmetry justifies the projection?" The developer paused. The company edit screen had no complex invariants. A company had a name, an address, and a billing contact — three fields, one table, one form. Writes and reads needed exactly the same data. There was no performance problem with reading the normalized row directly. The projection would add an async worker, a domain event, a separate read model table, and eventual consistency — for a screen that a user edits once a month. The tech lead's verdict: this is CRUD. CQRS here is accidental complexity, not structural clarity. The same team had the opposite problem in the order approval workflow: it had no CQRS, it had one normalized `Order` model used for everything, and the approval dashboard was painfully slow because it joined across seven tables for every row. The right answer is not "always CQRS" or "never CQRS." It is recognizing which parts of the system have genuine read/write asymmetry and applying the pattern only there.
</Hook>

<Crux>Apply CQRS where read/write asymmetry is real and invariants justify a separate write model — not by default. Most CRUD does not.</Crux>

<Explanation>

## The real cost of CQRS

CQRS is not free. Before applying it, a team should understand exactly what it introduces:

**Two models to design, build, and maintain.** Every write-side change must be propagated to one or more read models. Each new read model is a new projection to write, test, and operate. Each change to the write-side schema must be evaluated for its effect on every projection.

**Eventual consistency where there was none.** An async projection means the read model can be stale. This is a correctness concern that must be explicitly communicated, managed, and designed around. Users see data that is not the latest. Some queries that used to be strongly consistent are now eventually consistent.

**Projection infrastructure.** Async projection requires a mechanism for observing write-side changes: a domain event bus, change-data capture, or an outbox pattern. Each of these has operational overhead — deployments, monitoring, failure handling, dead-letter queues.

**More complex debugging.** When a user asks "why does my dashboard show the wrong status," the answer may involve the write model, the projection pipeline, the read model, and the eventual consistency window. The causal chain is longer and harder to trace.

These costs are worth paying when the structural benefit is real. They are waste when applied to problems that do not have genuine read/write asymmetry.

<FlowDiagram
  label="CQRS cost vs benefit: where the trade-off favors the pattern"
  caption="CQRS pays off when the write model's invariant complexity and the read model's query complexity pull in structurally incompatible directions. Where both are simple and symmetric, the costs outweigh the benefits. Arrows show the decision path: assess read/write asymmetry first, then structural incompatibility, then apply or reject CQRS."
  nodes={[
    { id: "assess", label: "Assess the domain", sub: "Does this part have genuine read/write asymmetry?" },
    { id: "yes-asym", label: "Yes: asymmetry is real", sub: "complex invariants on writes, complex joins on reads" },
    { id: "no-asym", label: "No: symmetric CRUD", sub: "same data, same shape, simple form" },
    { id: "apply", label: "Apply CQRS locally", sub: "two models, projection, eventual consistency" },
    { id: "skip", label: "Skip CQRS", sub: "one model, normalized, direct reads" },
  ]}
  edges={[
    { from: "assess", to: "yes-asym", label: "asymmetric" },
    { from: "assess", to: "no-asym", label: "symmetric" },
    { from: "yes-asym", to: "apply", label: "justified" },
    { from: "no-asym", to: "skip", label: "overkill" },
  ]}
/>

## Most CRUD does not need CQRS

A large proportion of application screens are symmetric: the same fields written to the database are the same fields displayed back. An address form writes a name, street, city, and postal code. The edit screen reads those same four fields. The list screen shows those same fields with a filter. No invariant guards the address fields. No complex join is needed. No read/write structural incompatibility exists.

CQRS for this screen adds two models, a projection, and eventual consistency for zero structural benefit. The write model would be `CompanyAddress`. The read model would be `CompanyAddressView`. The projection would copy the four fields from the write table to the read table. This is not architecture — it is ceremony.

The heuristic: **if the data a screen writes and the data it reads have the same shape and the same fields, CQRS adds complexity without structural benefit.**

For the billing platform:
- Company profile edit (name, address, billing contact) — CRUD, no CQRS
- User account settings (email, timezone, notification preferences) — CRUD, no CQRS
- Order approval dashboard (seven-table join, status aggregation, overdue flags) — genuine asymmetry, CQRS justified
- Order submission (credit-limit invariant, line-item invariants, status transitions) — complex write invariants, CQRS justified

<Inset kind="why">
The question to ask before applying CQRS is not "could we split this into commands and queries?" The answer to that is always yes — any domain can be split. The question is: "does the write concern and the read concern have structurally incompatible requirements that justify maintaining two separate models?" If the answer is yes, CQRS resolves a real structural tension. If the answer is no, CQRS is architectural gold-plating.
</Inset>

## Partial and local CQRS

CQRS does not have to be applied to an entire application. The right scope is the **bounded context** or the **domain area** where read/write asymmetry actually exists.

In the billing platform, the order management domain has genuine asymmetry: the write side has complex invariants (credit limit, approval workflow, line-item validation), and the read side has complex queries (dashboard, manager pipeline, finance report). CQRS is justified there.

The account management domain is mostly CRUD. Applying CQRS to account management to be "consistent with the order domain" would be cargo-cult architecture — copying the pattern without copying the forces that justify it.

The right answer: apply CQRS locally to the order management domain. Let the account management domain use a simple normalized model with direct reads. The two domains do not need to be architecturally consistent with each other — they need to be appropriate for their own forces.

This is called **local CQRS** or **partial CQRS**: CQRS applied selectively where the structural benefit is present, not applied uniformly across the whole system.

## CQRS is not event sourcing

The most common misunderstanding about CQRS is conflating it with event sourcing. They are independent patterns that happen to complement each other well. Conflating them doubles the adoption cost.

**CQRS** splits the write model from the read model. The write side stores current state in a normalized relational table. The read side stores denormalized views. The projection reads the write-side table directly or observes domain events.

**Event sourcing** (covered in unit 08) stores state as an append-only sequence of events rather than current state. The current state is computed by replaying the event log. Event sourcing is a different write-side storage strategy, independent of how reads are served.

You can have CQRS without event sourcing: the write side is a normalized relational table, the read side is a denormalized projection of that table. This is the most common form of CQRS in production.

You can have event sourcing without CQRS: a service uses an event log as its write store, but serves reads directly from the event log by replaying on demand — no separate read models.

You can have both: the write side is an event log; read models are built by projecting the event log. This combination is powerful but carries the cost of both patterns.

<Infographic
  svg={cqrsVsEventSourcing}
  label="A 2x2 matrix of CQRS (split read/write models) against event sourcing (event-log write store): neither gives plain CRUD; CQRS without event sourcing gives projected reads; event sourcing without CQRS serves reads by replaying the log; both together pay for both patterns."
  caption="CQRS and event sourcing vary on independent axes — three of the four cells are valid designs. Combining them is powerful but pays for both, so it is never a prerequisite for adopting CQRS alone."
  hue="var(--d-infra)"
/>

<Inset kind="note">
Teams that learn about CQRS often encounter it in the context of event sourcing and assume the two always go together. They then decide they cannot adopt CQRS without also adopting event sourcing, which raises the adoption cost to the point where neither gets adopted. The reality: CQRS with a relational write model and a projection that reads the write table is a completely valid, low-ceremony starting point. Event sourcing is an optional — and significant — additional commitment.
</Inset>

<Quiz
  id="arch-07-03-quiz-1"
  lessonSlug="03-when-cqrs-is-overkill"
  lang="en"
  question="A team is building a system with three domains: Order (complex approval workflow, credit-limit invariants, multi-table dashboard), Product Catalog (name, description, price, image — simple CRUD, displayed exactly as stored), and Notifications (user preferences for email/SMS, toggled on a settings page). Which domains should use CQRS?"
  choices={[
    { label: "Only Order. The Order domain has genuine read/write asymmetry: complex write invariants (credit-limit, approval workflow) and complex reads (multi-table dashboard). Product Catalog and Notifications are symmetric CRUD — the same data written is the same data read back. Applying CQRS to them adds two models, projections, and eventual consistency for zero structural benefit.", correct: true },
    { label: "All three. Consistency across the system is important, and having different patterns in different domains creates maintenance complexity.", misconception: "Applying CQRS uniformly to maintain consistency is cargo-cult architecture. The pattern should follow the forces — CQRS makes sense where read/write asymmetry justifies it. Forcing it onto symmetric CRUD domains adds accidental complexity. The maintenance complexity of mixed patterns is far lower than the complexity of CQRS applied to domains that do not need it." },
    { label: "Order and Product Catalog. Product Catalog has read-heavy access patterns (many users browsing), so a separate read model would improve performance.", misconception: "Read-heavy access is not sufficient justification for CQRS. If the read model and write model have the same shape — product name, description, price, image, read exactly as written — there is no structural benefit to a separate read model. A caching layer or a CDN would address the read-heavy load without the structural cost of CQRS. CQRS is justified by structural incompatibility between reads and writes, not by read volume alone." },
    { label: "None. CQRS is too complex for any production system that does not have dedicated platform engineering to maintain it.", misconception: "CQRS is widely used in production systems where the read/write asymmetry justifies it. The question is not whether CQRS is ever appropriate — it is whether it is appropriate for a specific domain. The Order domain in this system has genuine asymmetry and justifies CQRS. The concern about maintenance complexity is real, but it is an argument for applying CQRS selectively, not for rejecting it entirely." },
  ]}
/>

<Quiz
  id="arch-07-03-quiz-2"
  lessonSlug="03-when-cqrs-is-overkill"
  lang="en"
  question="A team wants to adopt CQRS for their order domain. An engineer says: 'We cannot adopt CQRS without also adopting event sourcing — they go together.' Is this correct?"
  choices={[
    { label: "No. CQRS and event sourcing are independent patterns. CQRS splits the write model from the read model — the write side can be a normalized relational table; the read side is a denormalized projection. Event sourcing replaces the write-side storage strategy with an append-only event log. You can apply CQRS with a relational write model and never use event sourcing. The two patterns complement each other well when combined, but combining them doubles the adoption cost and complexity. Starting with CQRS on a relational write model is a valid, lower-cost entry point.", correct: true },
    { label: "Yes. CQRS requires an event log on the write side so that projections can replay history to rebuild read models.", misconception: "Read model rebuild by replay is possible with or without event sourcing. A projection can replay by re-reading the write-side relational table from scratch. Event sourcing makes rebuild by replay more powerful (because the full event history is preserved), but it is not a requirement for CQRS. Projections can be built from any mechanism that observes write-side changes — a relational table, domain events on a bus, or an event log." },
    { label: "Yes. Without event sourcing, there is no way to keep the read model in sync with the write model.", misconception: "Read model synchronization does not require event sourcing. A synchronous projection updates the read model in the same transaction as the write. An async projection observes write-side changes via domain events on a message bus or change-data capture — none of which require the write side to use an event log. Event sourcing is one source of write-side change events; a domain event published by the command handler is another." },
    { label: "Partially. You can adopt CQRS without event sourcing, but you lose the ability to rebuild read models.", misconception: "You can rebuild read models without event sourcing. A projection can rebuild by re-reading the write-side relational table and re-running its transformation logic over all existing rows. Event sourcing makes rebuilding richer (it preserves the sequence and exact state of every historical change), but basic rebuild by re-projection from a relational source is available without it." },
  ]}
/>

<Quiz
  id="arch-07-03-quiz-3"
  lessonSlug="03-when-cqrs-is-overkill"
  lang="en"
  question="Six months after applying CQRS to the order domain, the team finds that a new engineer spent a week debugging a bug where the order dashboard showed wrong status. The root cause: the projection had a bug and the read model had stale data for some orders. The team lead asks: 'Was CQRS the wrong choice here?' How should they evaluate this?"
  choices={[
    { label: "The debugging complexity is a known cost of CQRS with async projection — not evidence that CQRS was the wrong choice. The right evaluation: was the structural benefit (clean write model, fast dashboard, independent evolvability) worth the operational cost (projection bugs, eventual consistency debugging)? If the dashboard's performance and the order domain's write-side clarity were real gains, and the team has the tooling and discipline to operate projections reliably, CQRS remains justified. If the team consistently struggles with projection bugs and the original performance problem could have been solved with a well-designed SQL view, they should reconsider. The correct question is not 'did we hit a bug' but 'is the structural trade-off right for our context?'", correct: true },
    { label: "Yes, CQRS was the wrong choice. A bug that takes a week to debug indicates the system is too complex. They should revert to a single model.", misconception: "All architectural patterns introduce new failure modes. A single-model approach would have different bugs (lock contention, query performance, invariant violations) that could also take a week to debug. The question is not whether CQRS introduced a new failure mode — it did — but whether the failure modes and operational costs are acceptable given the structural benefits. Reverting to a single model because of one debugging incident discards the structural benefits without evaluating the full trade-off." },
    { label: "CQRS was correct, and the solution is to add more monitoring to the projection pipeline to catch stale data sooner.", misconception: "Better monitoring is a good operational improvement, but it does not address the evaluation question. The team should evaluate whether the structural trade-off is still justified — not just patch the operational side. Better monitoring is part of operating CQRS responsibly, but the question of whether CQRS was the right choice for this domain requires evaluating both sides of the trade-off, not just improving operations." },
    { label: "CQRS was wrong because eventual consistency should never be acceptable in a financial system.", misconception: "Financial systems use eventual consistency extensively — payment reconciliation, account balance updates, reporting pipelines. The acceptability of eventual consistency depends on the specific data and the specific operation, not on the industry. A dashboard view showing slightly stale order status for a few seconds is very different from a payment clearing decision. The team should evaluate staleness acceptability per read model, not reject eventual consistency categorically." },
  ]}
/>

</Explanation>

<KeyTakeaway>CQRS introduces two models, eventual consistency, and projection infrastructure — real costs that pay off only where read/write asymmetry is genuine. Most CRUD screens are symmetric and do not justify the split. Apply CQRS locally to the domain areas where it is warranted; let other domains stay simple. CQRS and event sourcing are independent patterns: you can have one without the other, and conflating them unnecessarily doubles the adoption cost.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-07-03-retrieval"
  lang="en"
  questions={[
    { q: "What are the main costs CQRS introduces, and when do they pay off?", a: "CQRS introduces: (1) two models to design, build, and maintain — every write-side change must be reflected in read models via projections; (2) eventual consistency where there was none — async projections mean the read model can be stale, which must be communicated to users and managed operationally; (3) projection infrastructure — an event bus, change-data capture, or outbox pattern with its own operational overhead; (4) longer debugging chains — a wrong value on a dashboard may trace through the write model, the projection pipeline, the read model, and the staleness window. These costs pay off when: the write side has complex invariants that benefit from a clean, normalized aggregate; the read side has complex queries that benefit from denormalized projections; and the two shapes are structurally incompatible. Where reads and writes need the same data in the same shape, CQRS is accidental complexity." },
    { q: "What is partial/local CQRS and why is it preferred over applying CQRS uniformly?", a: "Partial or local CQRS is applying the pattern only to the domain areas where genuine read/write asymmetry exists, while leaving symmetric CRUD domains on a simple normalized model. It is preferred over uniform application because the justification for CQRS is specific to the structural forces in each domain. A complex order domain with approval workflows, credit-limit invariants, and multi-table dashboards has those forces. A company address edit screen does not. Applying CQRS to the address screen to be 'consistent with the order domain' copies the pattern without copying the forces that justify it — this is cargo-cult architecture. Local CQRS means each domain is appropriately structured for its own forces, not architecturally homogeneous for its own sake." },
    { q: "Are CQRS and event sourcing the same pattern? Can you have one without the other?", a: "They are different patterns. CQRS splits the write model from the read model — the write side can be a normalized relational table; the read side is a denormalized projection. Event sourcing (unit 08) replaces the write-side storage strategy with an append-only event log. You can have CQRS without event sourcing: write to a relational table, project into read models from that table. You can have event sourcing without CQRS: use an event log as the write store and serve reads directly from it without separate read models. You can have both together: the combination is powerful (rebuild read models by replaying the event log) but carries the cost of both patterns. Conflating the two is the most common adoption mistake — teams assume they cannot adopt CQRS without also adopting event sourcing, which raises the cost to the point where neither gets adopted." },
  ]}
/>

<Recap lang="en">
The opening story had the right instinct — the tech lead asked the right questions: what invariant does the write model guard, and what asymmetry justifies the projection? For the company address edit screen, the answers were "none" and "none." CQRS there was ceremony, not architecture.

The costs of CQRS are real: two models, eventual consistency, projection infrastructure, and longer debugging chains. Those costs buy real structural benefits — a clean write model that guards invariants, a read model shaped exactly for each query, independent evolvability of the two sides. The trade-off is justified where the asymmetry is genuine.

Apply CQRS locally to the domain areas where it is warranted. Let other domains stay on simple normalized models. Architectural patterns should follow structural forces, not be applied for consistency's sake.

CQRS and event sourcing are independent. You can apply CQRS with a relational write model today, without committing to an event log. Event sourcing (unit 08) is a separate, significant architectural decision about write-side storage strategy — complementary to CQRS but not required by it.
</Recap>
