infographic chart-column-simple
data
  title Fold cost per aggregate load: snapshot vs cold replay
  desc Same order, 4,000 events — a snapshot at seq 3800 folds only the 200-event tail (20x less work)
  values
    - label Cold load (fold from event 1)
      value 4000
    - label Snapshot-assisted load
      value 200
theme
  palette #f43f5e #14b8a6
