---
slug: "01-the-dependency-rule"
lang: en
track: "architecture-patterns"
unit: "02-dependency-direction"
order: 1
title: "The dependency rule"
summary: "Source-code dependencies must point toward stable abstractions. When concrete modules depend on other concrete modules, changes cascade upward. The dependency rule breaks that rigidity by separating flow-of-control from flow-of-source-dependency."
estMin: 20
status: ready
lessonType: topic
level: middle
concepts:
  - dependency-rule
  - source-code-dependency
  - stability
  - flow-of-control
  - flow-of-dependency
  - abstraction-direction
prereqs: []
sources:
  - https://blog.cleancoder.com/uncle-bob/2012/08/13/the-clean-architecture.html
  - https://www.informit.com/store/clean-architecture-a-craftsmans-guide-to-software-structure-9780134494166
  - https://martinfowler.com/articles/dipInTheWild.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The B2B order platform had a billing module and an order module. Billing depended on order, which depended on the database access layer, which depended on the PostgreSQL driver. Straightforward — each layer depends on the layer beneath it. Then the business asked for a new payment workflow: billing now needed to call the payment gateway directly, and the payment gateway needed to know about order status to prevent double-charging. The payment gateway module was added and made to depend on order as well. Two months later the team needed to extract billing into a separate service. They could not do it: billing imported order, order imported the database layer, the payment gateway imported order too, and billing had started importing helpers from the payment gateway module as a shortcut. The dependencies formed a web. Every module knew about every other concrete module. Extracting billing meant carrying the entire platform with it. The team had never asked the question that would have prevented this: in which direction should source-code dependencies point?
</Hook>

<Crux>Source-code dependencies must point toward stable abstractions — concrete modules depend on abstract ones, never the reverse, regardless of which direction control flows at runtime.</Crux>

<Explanation>

## Two different flows: control and source-code dependency

The confusion that causes most dependency problems is conflating two distinct things that happen simultaneously in code: the **flow of control** and the **flow of source-code dependency**.

**Flow of control** is what executes at runtime. When the billing module calls `orderService.getOrder(id)`, control flows from billing into the order service. This is the execution path — who calls whom.

**Flow of source-code dependency** is what the compiler sees. When billing has `import { OrderService } from '../order/orderService'`, billing has a source-code dependency on the order module. At compile time, if `orderService.ts` changes its exported shape, billing must be recompiled. This is the dependency path — who knows about whom.

These two flows are independent. They can point in the same direction, or they can be made to point in opposite directions. The dependency rule is about the second flow, not the first. Control will always flow wherever the runtime calls dictate. But you get to choose which direction knowledge — the import, the `requires`, the type reference — travels between modules.

The critical question is: **if a module changes, which other modules are forced to know about it?**

<FlowDiagram
  label="Control flow vs source-code dependency flow"
  nodes={[
    { id: "billing", label: "BillingModule", sub: "concrete — calls OrderService" },
    { id: "order", label: "OrderService", sub: "concrete — implements logic" },
    { id: "iorder", label: "IOrderPort", sub: "abstract — interface owned by billing" },
  ]}
  edges={[
    { from: "billing", to: "order", label: "control flows →" },
    { from: "billing", to: "iorder", label: "import (source dep)" },
    { from: "order", to: "iorder", label: "implements (source dep)" },
  ]}
  caption="Control still flows from BillingModule to OrderService at runtime. But the source-code dependency is inverted: both BillingModule and OrderService depend on IOrderPort. BillingModule no longer imports OrderService directly — it only knows about the interface. OrderService changes do not force BillingModule recompilation."
/>

## The dependency rule: point toward stability

Robert Martin's dependency rule, first stated in the context of Clean Architecture, is the principle that formalizes which direction source-code dependencies should point:

> **Source-code dependencies must point in the direction of stability.**

Stability here has a specific meaning: a module is stable if it is unlikely to change. The two forces that make a module stable are:
- **High afferent coupling (Ca)**: many things depend on it, so it has strong pressure to stay put — changing it breaks too many callers.
- **Abstract, not concrete**: it defines an interface or contract, not an implementation. Interfaces do not change because of implementation details; they change only when the contract itself must evolve.

The dependency rule follows from this: if a module A depends on a module B, then whenever B changes, A may be forced to change too. To minimize forced changes, A should depend on the things least likely to change — the stable, abstract things — not on the things most likely to change — the volatile, concrete things.

In the B2B platform, the billing module should depend on the concept of "something that provides order data" — an interface — rather than on the specific `PostgresOrderRepository` that implements it today. If the implementation switches to a different database, or if the order team refactors the repository internals, billing is untouched. Billing's dependency is on the contract, which is stable, not on the implementation, which is volatile.

<Inset kind="why" lang="en">
Why does pointing toward stability reduce rigidity? Because rigidity — the inability to change one module without cascading changes — is caused by dependencies running from stable modules toward volatile ones. If billing (relatively stable, many things call it) depends directly on the PostgreSQL repository (volatile, implementation details change), then every internal change to the repository forces a potential recompile and recheck of billing and every module billing exports. Reversing this direction — so billing depends only on an interface, and the repository implements that interface — means the repository can change freely as long as the contract holds. Stability becomes a property of where the dependency arrow points, not just of how often the code changes.
</Inset>

## Concrete depends on abstract, not the reverse

The practical form of the dependency rule is: **concrete things depend on abstract things, never the reverse**.

An abstract module (an interface, a protocol, a port in hexagonal terminology) is a contract. It defines what something *does* without specifying how. It changes only when the contract evolves — which happens rarely and intentionally.

A concrete module (a class, a service, a repository implementation) is an execution unit. It implements the contract using a specific technology, algorithm, or external dependency. It changes whenever the implementation needs to change — which happens frequently.

If a concrete module A depends on another concrete module B, A must change whenever B's implementation changes. This creates chains of forced changes that ripple across the codebase. The billing module depending on `PostgresOrderRepository` rather than `IOrderRepository` is an example: every time the order team changes the Postgres implementation, billing is in the blast radius.

Reversing the direction — concrete depends on abstract — breaks the chain. Billing depends on `IOrderRepository`. `PostgresOrderRepository` implements `IOrderRepository`. Now the dependency chain is:

```
BillingService → IOrderRepository ← PostgresOrderRepository
```

Both billing and the Postgres implementation depend on the interface. Neither depends on the other. The implementation can be swapped for a test double, a different database, or a REST API client — and billing does not change.

In the B2B platform, this is why the payment gateway extraction problem happened: there was no abstraction layer. Every module depended directly on every other concrete module. Making concrete modules depend on abstractions is the structural move that would have allowed billing to be extracted cleanly.

<Inset kind="note" lang="en">
The words "stable" and "abstract" are correlated but not identical. A very widely used, very concrete class (like a utility library) might be stable in practice because nobody changes it. But stability-by-convention is fragile — it depends on social agreement, not structural enforcement. Making the dependency point toward an interface makes stability structural: the interface changes only when the contract changes, not when the implementation changes. Structure is more reliable than convention.
</Inset>

<Quiz
  id="arch-02-01-quiz-1"
  lessonSlug="01-the-dependency-rule"
  lang="en"
  question="The billing module contains the line `import { PostgresOrderRepository } from '../order/infra/postgres-order-repository'`. What exactly is wrong with this import, and what is the correct structural fix?"
  choices={[
    { label: "Billing has a source-code dependency on a concrete implementation detail of the order module. The fix is to make billing depend on an interface (IOrderRepository) instead, with PostgresOrderRepository implementing that interface — so billing is insulated from any change to the Postgres implementation.", correct: true },
    { label: "Billing should not import anything from the order module — all modules should be independent.", misconception: "Modules need to collaborate — zero cross-module imports is not the goal. The problem is not that billing imports from order; it is that billing imports from a concrete implementation class rather than from a stable abstraction. The fix is to introduce an interface and invert the dependency, not to eliminate the dependency entirely." },
    { label: "The import path uses relative `..` segments — it should be an absolute import.", misconception: "The import path format is not the structural problem here. The issue is that billing is importing a concrete repository implementation, creating a source-code dependency on a volatile module. The fix is introducing an interface, regardless of import path style." },
    { label: "PostgresOrderRepository is in an `infra` subdirectory — it should be moved to a shared location.", misconception: "Moving the file does not change the dependency relationship. Billing would still import a concrete class. The structural problem is the direction of the dependency (concrete-to-concrete), not the location of the file." },
  ]}
/>

<Quiz
  id="arch-02-01-quiz-2"
  lessonSlug="01-the-dependency-rule"
  lang="en"
  question="At runtime, the BillingService calls orderRepository.getOrder(id), which executes database logic and returns an order. At compile time, BillingService imports IOrderRepository (an interface), and PostgresOrderRepository implements IOrderRepository. Which statement correctly describes the relationship between control flow and source-code dependency here?"
  choices={[
    { label: "Control flows from BillingService through IOrderRepository to PostgresOrderRepository at runtime. Source-code dependency flows from BillingService to IOrderRepository, and from PostgresOrderRepository to IOrderRepository — both pointing toward the abstraction. Control and dependency point in different directions.", correct: true },
    { label: "Control and source-code dependency both flow in the same direction: BillingService → IOrderRepository → PostgresOrderRepository.", misconception: "This is wrong about source-code dependencies. PostgresOrderRepository does not depend on IOrderRepository in the sense that IOrderRepository needs PostgresOrderRepository to compile — it is the other way around. PostgresOrderRepository implements (depends on) IOrderRepository: it must conform to the interface's shape. The interface has no knowledge of PostgresOrderRepository." },
    { label: "Source-code dependency and control flow always point in the same direction — you cannot separate them.", misconception: "This is exactly the misconception the dependency rule corrects. Through the use of an interface or abstract type, you can make control flow in one direction (caller → implementation) while source-code dependency points in the opposite direction (implementation → interface). The interface sits at the boundary: the caller imports it, and the implementation implements it. Neither imports the other." },
    { label: "The interface IOrderRepository has a source-code dependency on PostgresOrderRepository because it defines what PostgresOrderRepository must implement.", misconception: "This is backwards. The interface defines a contract in isolation — it has no knowledge of which classes implement it. PostgresOrderRepository must import and implement IOrderRepository; IOrderRepository has no import of PostgresOrderRepository. The dependency arrow points from the concrete to the abstract, not the reverse." },
  ]}
/>

<Quiz
  id="arch-02-01-quiz-3"
  lessonSlug="01-the-dependency-rule"
  lang="en"
  question="The team wants to extract the billing module into a standalone service. After following the dependency rule — making all cross-module dependencies point toward interfaces — the extraction takes two days. Before the rule was applied, the same extraction was abandoned after a week because of cascading import chains. What structural property did the dependency rule establish that made extraction feasible?"
  choices={[
    { label: "Each module's source-code dependencies now terminate at stable interfaces, not at other concrete modules. The billing service's compile-time knowledge boundary is exactly the set of interfaces it depends on — it has no compile-time knowledge of other modules' implementations. Extraction requires moving billing and its interfaces; nothing else needs to change.", correct: true },
    { label: "The dependency rule reduced the number of modules in the system.", misconception: "The dependency rule does not remove modules — it redirects the arrows between them. The same modules exist before and after; what changes is whether they depend on each other's concrete implementations or on shared interfaces. Fewer modules is not the mechanism; cleaner dependency boundaries are." },
    { label: "The dependency rule made every module stateless, which allowed them to be deployed independently.", misconception: "The dependency rule is about the direction of source-code imports, not about state. A module can follow the dependency rule perfectly and still be stateful. Extractability comes from having well-defined compile-time boundaries (interfaces), not from the presence or absence of state." },
    { label: "The dependency rule enforced that no module calls another module more than once.", misconception: "The dependency rule says nothing about call frequency. It governs which direction the source-code dependency arrow points. A module following the dependency rule can call another module's interface as many times as needed — what it cannot do is import the concrete implementation." },
  ]}
/>

</Explanation>

<KeyTakeaway>The dependency rule separates two flows: control (which module calls which at runtime) and source-code dependency (which module imports which at compile time). The rule says source-code dependencies must point toward stability and abstraction — concrete modules depend on interfaces, interfaces never depend on concrete modules. Violating this rule creates rigid systems where changing any concrete module cascades forced changes upward through every module that imported it.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-02-01-retrieval"
  lang="en"
  questions={[
    { q: "What is the difference between flow of control and flow of source-code dependency? Why does the dependency rule only govern the second one?", a: "Flow of control is what executes at runtime — which module calls which method on which object. Flow of source-code dependency is what the compiler sees — which modules reference (import) which other modules. The dependency rule governs source-code dependency because that is what determines rigidity: if module A imports module B's concrete class, then any change to B potentially forces A to recompile and change. Control can flow wherever runtime needs dictate — that is fine. The problem is that when control flow is used as a proxy for import structure (A calls B, therefore A imports B's concrete class), volatility propagates up the call stack. By separating the two — using interfaces so A imports a contract and B implements it — control can still flow from A to B at runtime, but A has no compile-time knowledge of B's concrete implementation." },
    { q: "What does 'stable' mean in the context of the dependency rule, and why does pointing toward stability reduce rigidity?", a: "In the dependency rule, a stable module is one unlikely to change. Two structural properties correlate with stability: high afferent coupling (many things depend on it, creating pressure to remain backward-compatible) and abstractness (it defines a contract, not an implementation — the contract changes only when the business contract changes, not when an implementation detail changes). Pointing toward stability reduces rigidity because rigidity is caused by volatile concrete modules being in the import chain of many other modules. If billing imports PostgresOrderRepository and that repository changes its internal structure, billing is in the blast radius — not because billing uses the changed functionality, but because it imported the module. Replacing that import with a stable interface removes billing from the blast radius of implementation changes." },
    { q: "In the B2B order platform, before the dependency rule was applied, why did extracting the billing module require carrying the whole platform with it?", a: "Because billing's imports formed a transitive closure over the whole platform. Billing imported PostgresOrderRepository (concrete). PostgresOrderRepository imported the database driver and shared utilities. The payment gateway imported the order module (concrete). Billing had also imported helpers from the payment gateway module. The compiler resolves all transitive imports: to compile billing as a standalone service, you need everything billing imports, and everything those modules import, recursively. With no abstraction layer, that transitive closure was the entire platform. After applying the dependency rule — making billing depend only on IOrderRepository and IPaymentGateway interfaces — billing's transitive closure contained only the interface definitions. Extraction required only billing + its interfaces, not the entire platform." },
  ]}
/>

<Recap lang="en">
The dependency rule is a discipline about which direction source-code imports point, not about which direction calls flow at runtime. Flow of control (who calls whom) and flow of source-code dependency (who imports whom) are independent. The rule says: source-code dependencies must point toward stable abstractions.

Stability means unlikely to change. An abstract module — an interface, a port, a contract — is structurally stable because it changes only when the contract evolves. A concrete module — a repository implementation, a gateway adapter, a framework class — is volatile because it changes whenever its implementation needs to change. The dependency rule says volatile concrete modules depend on stable abstract ones, never the reverse.

The structural consequence: concrete depends on abstract, abstract never depends on concrete. In the B2B platform, BillingService imports IOrderRepository (abstract, stable). PostgresOrderRepository implements IOrderRepository. Neither depends on the other. The dependency arrow runs: BillingService → IOrderRepository ← PostgresOrderRepository. Control still flows from billing to the repository at runtime; source-code dependency runs from both toward the interface.

Violating this rule — concrete importing concrete — creates chains of forced changes. Fixing it requires introducing an abstraction at every boundary where a volatile module is imported by a module that should be insulated from its implementation. The next lesson examines the mechanism that performs this inversion: the Dependency Inversion Principle.
</Recap>
