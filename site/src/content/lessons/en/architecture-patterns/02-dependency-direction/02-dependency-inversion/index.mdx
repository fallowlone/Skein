---
slug: "02-dependency-inversion"
lang: en
track: "architecture-patterns"
unit: "02-dependency-direction"
order: 2
title: "Dependency inversion"
summary: "DIP: high-level modules should not depend on low-level modules — both depend on abstractions. The key insight: the consumer, not the provider, owns the interface. This is the mechanism behind hexagonal and clean architecture boundaries."
estMin: 22
status: ready
lessonType: topic
level: middle
concepts:
  - dependency-inversion-principle
  - interface-ownership
  - high-level-module
  - low-level-module
  - compile-time-boundary
  - inversion-mechanism
prereqs: []
sources:
  - https://www.informit.com/articles/article.aspx?p=1372233&seqNum=2
  - https://www.informit.com/store/clean-architecture-a-craftsmans-guide-to-software-structure-9780134494166
  - https://martinfowler.com/articles/dipInTheWild.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import interfaceOwnership from "./infographics/interface-ownership.svg?raw";

<Hook>
The billing team at the B2B order platform designed a clean interface for the order repository. They put it in the order module's package: `order/interfaces/IOrderRepository.ts`. Billing imported it from there. The arrangement looked tidy — the interface was right next to the implementation. But six months later the order team decided to restructure their package. They moved the interface, split it, and changed some method signatures. Billing broke. The payment gateway broke. The audit service broke. The order team was confused: they had published an interface, so they thought consumers were protected. They were not, because the interface lived in the order module's package. The order module still owned it. Every time the order team touched their package, they could break every consumer — interface or no interface. The team had the right idea (an interface as the boundary) but the wrong ownership model. The Dependency Inversion Principle is not just about having an interface. It is about who owns it.
</Hook>

<Crux>DIP: consumers define the abstractions they need; providers implement them — the arrow always points toward the consumer.</Crux>

<Explanation>

## The two halves of DIP

Robert Martin's Dependency Inversion Principle has two statements, and both must hold:

1. **High-level modules should not depend on low-level modules. Both should depend on abstractions.**
2. **Abstractions should not depend on details. Details should depend on abstractions.**

The first statement is familiar — it is the dependency rule from the previous lesson in module form. High-level modules (billing, in the B2B platform) contain the policy: the business logic that decides what should happen. Low-level modules (the order repository, the payment gateway) contain the mechanism: the technical plumbing that makes it happen. Policy should not be coupled to mechanism.

The second statement is the subtle one — and it is what the opening story got wrong. Putting the interface in the low-level module's package makes the abstraction a detail of the low-level module. The high-level consumer imports the interface *from* the low-level module. The low-level module can still break the consumer by restructuring its package, renaming its interface, or splitting it into multiple interfaces. The abstraction is technically present, but ownership has not been inverted.

**The DIP requires that the high-level module own the abstraction it depends on.**

<Infographic
  svg={interfaceOwnership}
  label="Side-by-side comparison of two interface-ownership models: when the provider (low-level module) owns the interface versus when the consumer (high-level module) owns it, listing the consequences of each."
  caption="DIP is not 'use an interface' — it is who owns it. Only a consumer-owned interface actually inverts the dependency and survives the provider's refactors."
  hue="var(--d-infra)"
/>

## Who owns the interface?

Interface ownership is the core of DIP. An interface is owned by whoever defines it and controls its evolution. When you ask "should this interface change?", the answer should come from the consumer's needs, not from the provider's implementation convenience.

In the B2B platform, the billing module needs order data. The interface it needs — `IOrderProvider` or `IOrderPort` — should live in billing's package (or in a shared boundary package that billing controls). It declares exactly what billing requires: `getConfirmedOrder(id: string): Promise<ConfirmedOrderDTO>`. Nothing more. The order module then implements this interface by writing a class that satisfies billing's contract.

The dependency arrows now run:

```
BillingService   ──imports──→   IOrderPort   (owned by billing)
                                      ↑
                              implements
                                      │
                         PostgresOrderRepository   (in order module)
```

The order module's repository depends on billing's interface — not the other way around. If billing's needs change, the interface changes and the repository must update. If the order team changes how they fetch order data internally, they update their repository implementation; billing's interface is untouched.

<FlowDiagram
  label="DIP: before and after interface ownership inversion"
  nodes={[
    { id: "billing-before", label: "BillingService", sub: "BEFORE: imports concrete repo" },
    { id: "repo-before", label: "PostgresOrderRepository", sub: "BEFORE: owned by order module" },
    { id: "billing-after", label: "BillingService", sub: "AFTER: imports its own interface" },
    { id: "iport", label: "IOrderPort", sub: "owned by billing module" },
    { id: "repo-after", label: "PostgresOrderRepository", sub: "AFTER: implements billing's interface" },
  ]}
  edges={[
    { from: "billing-before", to: "repo-before", label: "BEFORE: concrete dep" },
    { from: "billing-after", to: "iport", label: "imports (source dep)" },
    { from: "repo-after", to: "iport", label: "implements (source dep)" },
  ]}
  caption="Before DIP: BillingService imports PostgresOrderRepository directly — a concrete-to-concrete dependency. After DIP: BillingService imports IOrderPort (which it owns), and PostgresOrderRepository implements IOrderPort. The source-code dependency arrow from the repository now points toward billing's boundary, not away from it."
/>

## The compile-time boundary vs the runtime boundary

DIP is fundamentally about drawing a boundary that the compiler can enforce. On one side of the boundary: the high-level module and the interface it owns. On the other side: the low-level module that implements it. Neither side needs to know about the other's concrete details.

This distinction between what can be known at compile time and what is resolved at runtime is the key to modularity:

**At compile time**, BillingService only knows about `IOrderPort`. It knows the method signatures, the parameter types, the return types. It knows the contract. It does not know that `PostgresOrderRepository` exists. The compiler cannot even see `PostgresOrderRepository` from billing's perspective.

**At runtime**, something must wire `IOrderPort` to `PostgresOrderRepository`. This is the job of dependency injection: a composition root or a container resolves which concrete implementation satisfies which interface, and passes it to billing. At that moment, control flows from billing through the interface to the repository implementation.

This separation is the core of what makes architectural patterns like hexagonal architecture (unit 04) and clean architecture (unit 05) work. The hexagon in hexagonal architecture is the high-level domain, surrounded by interface definitions it owns (ports). Adapters — the concrete implementations — live outside the hexagon and depend on the ports inward. The concentric circles in clean architecture are layers where each inner circle owns the interfaces that outer circles implement. DIP is the mechanism that makes those patterns structurally enforceable.

<Inset kind="why" lang="en">
Why does interface ownership matter so much in practice? Because without it, the abstraction is a fiction. The interface may exist as a TypeScript interface or a Java abstract class, but if it lives in the low-level module's package, the low-level team can change it. They can add required methods, change signatures, deprecate and remove things — and the high-level consumer breaks. The word "inversion" in DIP refers specifically to this: in the naive design, the high-level module depends on the low-level module. After DIP, the low-level module depends on the high-level module's interface. The dependency arrow is literally inverted. This only happens when the interface lives in the consumer's domain.
</Inset>

## DIP in the B2B order platform: a before/after

**Before DIP**, the B2B platform had this structure:

- `billing/` imports `order/infra/postgres-order-repo.ts` → concrete-to-concrete
- `billing/` imports `payment/stripe-gateway.ts` → concrete-to-concrete
- `order/` imports `db/postgres-driver.ts` → concrete-to-concrete

Every module could break every other module through implementation changes. Adding a new payment provider required changing billing. Swapping the database required touching everything.

**After DIP**:

- `billing/ports/IOrderPort.ts` — owned by billing; defines `getConfirmedOrder`
- `billing/ports/IPaymentPort.ts` — owned by billing; defines `chargeCustomer`
- `order/adapters/BillingOrderAdapter.ts` — implements `IOrderPort`, lives in order module but depends on billing's interface
- `payment/adapters/StripePaymentAdapter.ts` — implements `IPaymentPort`, lives in payment module but depends on billing's interface

Billing now has zero compile-time knowledge of the order module's internals or the payment module's internals. The order and payment modules depend on billing's boundary definitions. Adding a new payment provider means writing a new adapter that implements `IPaymentPort` — billing itself does not change.

<Inset kind="note" lang="en">
A common confusion: DIP does not mean "always use an interface for everything." It means that at the significant architectural boundaries — where a high-level policy module needs to cross into low-level mechanism territory — the interface should be owned by the high-level side. For internal details within a module, or for stable, third-party infrastructure libraries, the overhead of interface inversion may not be worth it. DIP is a tool for architectural boundaries, not a universal code style rule.
</Inset>

<Quiz
  id="arch-02-02-quiz-1"
  lessonSlug="02-dependency-inversion"
  lang="en"
  question="The order module publishes an interface `IOrderRepository` in its own package: `order/interfaces/IOrderRepository.ts`. The billing module imports and uses it. The order team later refactors, splitting the interface into `IOrderReader` and `IOrderWriter`. Billing breaks. What went wrong from a DIP perspective?"
  choices={[
    { label: "The interface was owned by the order module (the provider), not by billing (the consumer). The order team could change the interface because it was in their package. DIP requires the consumer to own the interface — billing should have defined IOrderPort in its own package, and the order module should have implemented that interface.", correct: true },
    { label: "The order team should not have changed a published interface — backwards compatibility is always required.", misconception: "Backwards compatibility conventions are a social agreement, not a structural enforcement. The structural problem is that the interface lived in the order module's package, so the order team was in a position to change it at all. Ownership determines who can change what. DIP moves the interface to the consumer's domain precisely so that the consumer controls when it changes." },
    { label: "Billing should have used the concrete PostgresOrderRepository instead of the interface, which would have been more stable.", misconception: "Using the concrete class directly is the pre-DIP problem, not the solution. Concrete implementations change even more frequently than interfaces. The issue in the story is not interface use — it is interface ownership. Using a concrete class would make billing even more fragile, not less." },
    { label: "The order team split one interface into two — interfaces should never be split.", misconception: "Splitting a large interface into smaller, focused ones is often a good design move (Interface Segregation Principle). The problem is not the split itself — it is that the split happened without billing's input, because the interface was owned by the order module. If billing owned the interface, any split would be driven by billing's needs and done with billing's consent." },
  ]}
/>

<Quiz
  id="arch-02-02-quiz-2"
  lessonSlug="02-dependency-inversion"
  lang="en"
  question="The billing module defines IOrderPort in its own package. At compile time, BillingService.ts only imports IOrderPort. At runtime, a dependency injection container wires IOrderPort to PostgresOrderRepository. A new developer says: 'BillingService still calls the PostgreSQL database at runtime — DIP didn't actually remove the dependency.' How do you respond?"
  choices={[
    { label: "DIP separates compile-time knowledge from runtime behavior. BillingService has no compile-time dependency on PostgresOrderRepository — the compiler cannot see it from billing's context. At runtime, the DI container performs the wiring. The dependency that DIP removes is the source-code import, which is what causes rigidity and cascading recompilation. The runtime call is still there; that is fine — runtime calls are how code executes.", correct: true },
    { label: "The developer is right — DIP does not reduce real dependencies, only creates the illusion of decoupling.", misconception: "DIP reduces compile-time coupling, which is the source of rigidity. Compile-time coupling is what forces recompilation, what makes extraction impossible, and what creates cascading change pressure. Runtime coupling (one module calling another) cannot be eliminated — that is just code executing. DIP is specifically about the compile-time dependency, not the runtime call." },
    { label: "To remove the runtime dependency, you should use message queues instead of direct calls.", misconception: "Message queues address runtime coupling in distributed systems. DIP is about source-code dependency direction within a codebase. These are different concerns. The billing module can use a direct runtime call through an interface (DIP applied correctly) or use a message queue — both are orthogonal to DIP. DIP governs the import structure, not the communication protocol." },
    { label: "You should add an anti-corruption layer between billing and the DI container to make the separation complete.", misconception: "An anti-corruption layer is a DDD pattern for translating between bounded contexts. The described setup (BillingService imports IOrderPort, container wires to PostgresOrderRepository) already correctly applies DIP. There is no missing structural layer. The developer's confusion is about compile-time vs runtime, not about missing translation." },
  ]}
/>

<Quiz
  id="arch-02-02-quiz-3"
  lessonSlug="02-dependency-inversion"
  lang="en"
  question="In hexagonal architecture (which you will see in unit 04), the domain defines 'ports' — interfaces for all outbound dependencies. The domain module owns these ports. Infrastructure adapters implement them. Which statement explains why hexagonal architecture is structurally just DIP applied at the architectural layer?"
  choices={[
    { label: "Each port is an interface owned by the high-level domain module (the consumer of external services). Each adapter is a low-level concrete module that implements the port. Both domain and adapter depend on the port interface — exactly DIP's structure: high-level owns the abstraction, low-level depends on it to plug in.", correct: true },
    { label: "Hexagonal architecture is unrelated to DIP — it is about making the system testable, not about dependency direction.", misconception: "Testability is one benefit of hexagonal architecture, but the structural mechanism that enables it is DIP. The domain can be tested without infrastructure because it depends only on interfaces (ports) it owns, not on concrete infrastructure. The testability benefit flows from the dependency inversion, not from a separate principle." },
    { label: "In hexagonal architecture, the infrastructure layer owns the ports, not the domain — that is what makes adapters pluggable.", misconception: "This is exactly backwards. If infrastructure owned the ports, the domain would depend on infrastructure's definitions — which is the pre-inversion problem. In hexagonal architecture, the domain owns the ports. Infrastructure adapters depend on the domain's port definitions to implement them. Ownership by the domain is what makes infrastructure replaceable without touching the domain." },
    { label: "DIP applies only within a single module; hexagonal architecture applies it across the whole system boundary.", misconception: "DIP can be and is applied at the architectural level — it is not confined to a single module. Robert Martin's Clean Architecture applies DIP across the entire layered system. Hexagonal architecture applies it at the domain boundary. Both are DIP; the scope differs, but the mechanism is identical: high-level owns the interface, low-level implements it." },
  ]}
/>

</Explanation>

<KeyTakeaway>DIP is not just "use an interface." The interface must be owned by the consumer — the high-level policy module — not by the provider. When the consumer owns the interface, the source-code dependency arrow inverts: the low-level provider now depends on the high-level consumer's boundary definition. This is the compile-time boundary that hexagonal ports, clean architecture use-case interfaces, and every other inversion-based pattern are built on.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="arch-02-02-retrieval"
  lang="en"
  questions={[
    { q: "What are the two statements of DIP, and why does the second one (about abstractions not depending on details) matter as much as the first?", a: "First statement: high-level modules should not depend on low-level modules — both should depend on abstractions. Second statement: abstractions should not depend on details — details should depend on abstractions. The second statement matters because putting an interface in a low-level module's package technically satisfies the first statement (billing now imports an interface, not a class) but violates the second. The interface is still defined and controlled by the low-level module (a detail). When the low-level module restructures, renames, or splits the interface, consumers still break. The second statement requires that the interface be defined on the high-level side — that the abstraction itself not be a product of the low-level detail. Only then does the dependency actually invert." },
    { q: "In the B2B platform, where exactly should IOrderPort live, and why does its package location determine who can change it?", a: "IOrderPort should live in the billing module's package (or in a dedicated shared boundary package that billing controls). Package location determines ownership because in most module systems, a module's package is its exclusive namespace. If IOrderPort lives in `billing/ports/`, only the billing team modifies it — they change it when billing's needs change. If it lives in `order/interfaces/`, the order team can modify it at any time, for their own reasons, and billing is at the mercy of those decisions. Interface ownership = package ownership = who controls evolution." },
    { q: "What is the 'compile-time boundary' that DIP draws, and how does a dependency injection container relate to it?", a: "The compile-time boundary is the line at which BillingService's source-code knowledge ends: BillingService.ts imports IOrderPort (the interface) and nothing else about the order module. At compile time, the compiler sees billing's code and billing's interface — it does not need to resolve PostgresOrderRepository to compile BillingService.ts. That class is invisible to billing at compile time. The DI container's job is to perform the runtime resolution: it knows that IOrderPort should be satisfied by PostgresOrderRepository and passes a constructed instance to BillingService at startup. The container operates at the other side of the compile-time boundary. This is exactly the division between what the architect controls (compile-time dependency direction) and what the operations layer controls (runtime wiring)." },
  ]}
/>

<Recap lang="en">
The Dependency Inversion Principle has two halves. The first: high-level modules (business policy) should not depend on low-level modules (technical mechanism) — both should depend on abstractions. The second: those abstractions should be owned by the high-level side, not the low-level side.

Interface ownership is the operational consequence. An interface defined in the low-level module's package is still owned by the low-level module — the provider can change it, split it, move it, and break every consumer. An interface defined in the high-level module's package is owned by the consumer — it changes only when the consumer's requirements change, and the provider must conform to the consumer's contract.

The structural result: after DIP, the source-code dependency arrow from the low-level module points toward the high-level module's interface, not away from it. The provider depends on the consumer's boundary definition. This is the actual inversion.

This mechanism is what makes hexagonal architecture (unit 04) and clean architecture (unit 05) work structurally. In hexagonal architecture, the domain owns its ports — every outbound interface is defined by the domain. Infrastructure adapters implement those ports and therefore depend inward. In clean architecture, the dependency rule runs toward the center — inner circles define the interfaces, outer circles implement them. DIP is not an architectural pattern in itself; it is the underlying mechanism that all those patterns rely on to enforce their dependency boundaries at compile time.
</Recap>
