---
slug: "01-the-concentric-rule"
lang: en
track: "architecture-patterns"
unit: "05-clean-and-onion"
order: 1
title: "The concentric dependency rule"
summary: "Clean and Onion architecture arrange layers as concentric circles. The one absolute rule: source-code dependencies point inward only, toward higher-level policy. This generalizes hexagonal's two-sided inversion to any number of rings."
estMin: 22
status: ready
lessonType: topic
level: middle
concepts:
  - clean-architecture
  - onion-architecture
  - concentric-layers
  - dependency-rule
  - dependency-inversion
  - higher-level-policy
prereqs: []
sources:
  - https://blog.cleancoder.com/uncle-bob/2012/08/13/the-clean-architecture.html
  - https://jeffreypalermo.com/2008/07/the-onion-architecture-part-1/
  - https://www.informit.com/store/clean-architecture-a-craftsmans-guide-to-software-structure-9780134494166
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The B2B order platform had grown into a four-layer stack: presentation, application, domain, infrastructure. The team had read about hexagonal architecture and carefully placed interfaces at every boundary. But six months in, they found something wrong: the domain layer was importing from the infrastructure layer to get at a shared utility for formatting money values. The utility lived in infrastructure because it had been put there alongside the database currency-conversion code. The interface boundaries were intact — the domain called an interface, not a concrete class — but the interface itself was defined inside the infrastructure package, which meant the domain depended on infrastructure's package for the interface definition. The dependency inversion was incomplete. The interface existed but the domain did not own it. When Clean Architecture was published in 2017, Robert Martin was not proposing something entirely new. He was taking the core insight of hexagonal architecture — ports owned by the application — and systematizing it into a rule that applied to every layer boundary, not just the domain/infrastructure divide. The rule he stated was precise: source-code dependencies can only point inward — toward higher-level policy.
</Hook>

<Crux>In Clean and Onion architecture, every source-code dependency must point toward the center — inner rings never know about outer rings.</Crux>

<Explanation>

## The concentric-circle model

Both Clean Architecture (Robert Martin, 2012 blog post, 2017 book) and Onion Architecture (Jeffrey Palermo, 2008) organize a system as a set of concentric circles — rings nested inside each other, with the most important and stable code at the center and the most volatile, infrastructure-specific code at the outer edge.

The rings differ slightly between the two models, but the logic is the same. Martin's canonical four rings are:

1. **Entities** (innermost) — enterprise-wide business rules; the most stable code in the system.
2. **Use Cases** — application-specific business rules; orchestrate entities to achieve goals.
3. **Interface Adapters** — translate between the use-case format and the outside world (controllers, presenters, gateways).
4. **Frameworks and Drivers** (outermost) — web frameworks, databases, external libraries; the most volatile code.

Palermo's Onion uses slightly different names but the same logic: the Domain Model at the center, then Domain Services, then Application Services, then the outer Infrastructure ring. Both models arrive at the same structural constraint.

<FlowDiagram
  label="Concentric dependency rule: all source-code dependencies point inward"
  caption="Each ring may only import from rings closer to the center. The Entities ring imports nothing inside the system. The Frameworks ring may import from any inner ring. No inner ring knows any outer ring exists. All arrows represent source-code import dependencies."
  nodes={[
    { id: "entities", label: "Entities", sub: "enterprise business rules (innermost)" },
    { id: "usecases", label: "Use Cases", sub: "application business rules" },
    { id: "adapters", label: "Interface Adapters", sub: "controllers, presenters, gateways" },
    { id: "frameworks", label: "Frameworks & Drivers", sub: "DB, web, UI (outermost)" },
  ]}
  edges={[
    { from: "usecases", to: "entities", label: "imports" },
    { from: "adapters", to: "usecases", label: "imports" },
    { from: "frameworks", to: "adapters", label: "imports" },
  ]}
/>

## The dependency rule — why it is a rule, not a guideline

The single enforced constraint is: **a source-code dependency (import, reference, compilation dependency) may only point inward.** Nothing in an inner ring can name anything in an outer ring. An entity cannot import a use case. A use case cannot import a controller. An interface adapter cannot import the web framework — the web framework imports the adapter.

This is not a principle of style. It is a structural constraint with a specific payoff: if nothing in the inner rings knows about the outer rings, the inner rings can be compiled, tested, and reasoned about in complete isolation from infrastructure. You can run all your entity and use-case tests without a database, a web server, or an ORM being present.

The violation in the opening story — the domain importing a money-formatting utility from the infrastructure package — broke this rule. Even though an interface was involved, the domain had to import the interface from the infrastructure package to use it. The import path crossed the boundary in the wrong direction. The fix is the same as in hexagonal: move the interface into the inner ring (the domain defines it), and let the outer ring implement it.

<Inset kind="why" lang="en">
Why does stability increase toward the center? Inner rings contain pure business logic: the rules that change only when the business model itself changes. Outer rings contain technology choices: databases, frameworks, protocols — these change for technology reasons (a new ORM version, a cloud provider change, a third-party API deprecation) that have nothing to do with business logic. By structuring dependencies to point toward the stable center, you ensure that technology changes cannot force business-logic changes. The database can be replaced without rewriting a use case. A web framework upgrade cannot require an entity change. Stability and abstraction correlate: the innermost code is the most abstract (pure business rules, no technology detail) and the most stable; the outermost is the most concrete and the most volatile.
</Inset>

## How Clean Architecture generalizes hexagonal

Unit 04 introduced hexagonal architecture: ports owned by the application, adapters on both sides. Clean and Onion architecture generalize this from a single boundary (domain / infrastructure) to N concentric boundaries.

In hexagonal, the inversion happens once: the domain defines the port interfaces; infrastructure implements them; the dependency arrow points inward. Clean and Onion apply the same inversion at every ring boundary. The Interface Adapters ring defines the interfaces that Frameworks & Drivers must implement (exactly as the domain defined secondary ports in hexagonal). The Use Cases ring defines the interfaces (output ports, presenter interfaces) that the Interface Adapters ring must implement. The inversion propagates all the way in.

<FlowDiagram
  label="Hexagonal vs Clean: one inversion vs N inversions"
  caption="Hexagonal inverts the dependency at a single domain/infrastructure boundary. Clean Architecture applies the same inversion at every ring boundary. Both result in source-code dependencies pointing inward throughout. All arrows represent source-code import dependencies."
  nodes={[
    { id: "hex-domain", label: "Hexagonal: Domain", sub: "defines port interfaces" },
    { id: "hex-infra", label: "Hexagonal: Infrastructure", sub: "implements port interfaces" },
    { id: "clean-entities", label: "Clean: Entities", sub: "innermost; no imports" },
    { id: "clean-uc", label: "Clean: Use Cases", sub: "imports entities; defines output ports" },
    { id: "clean-adapters", label: "Clean: Interface Adapters", sub: "implements output ports; imports use cases" },
    { id: "clean-fw", label: "Clean: Frameworks", sub: "implements adapter interfaces; imports adapters" },
  ]}
  edges={[
    { from: "hex-infra", to: "hex-domain", label: "imports (source dep)" },
    { from: "clean-uc", to: "clean-entities", label: "imports" },
    { from: "clean-adapters", to: "clean-uc", label: "imports" },
    { from: "clean-fw", to: "clean-adapters", label: "imports" },
  ]}
/>

## Clean vs Onion — the overlap and the difference

Clean Architecture and Onion Architecture are independently conceived but structurally convergent. Both arrived at the same dependency rule. The practical differences are:

**Onion Architecture** (Palermo, 2008) emphasizes the **Domain Model** as the absolute center — pure domain objects with no dependencies. It puts Domain Services in the next ring, Application Services next, and Infrastructure at the outside. The emphasis is on the domain model being free from all infrastructure and application concerns.

**Clean Architecture** (Martin, 2012/2017) labels the rings differently and places an explicit **Interface Adapters** ring that is responsible for all data format translation between the use-case world and the outside world. It also distinguishes **Entities** (enterprise-wide rules, potentially shared across apps) from **Use Cases** (application-specific rules).

In practice, teams often conflate the two or use a hybrid. The important thing is not which ring names you use — it is whether you enforce the dependency rule. A codebase that calls itself "Clean Architecture" but has use cases importing framework classes violates the rule regardless of the diagram on the whiteboard.

<Inset kind="note" lang="en">
The concentric model is not a fixed number of rings. Martin explicitly states the diagram with four rings is illustrative; real systems might have more. What is non-negotiable is the direction of dependency — inward only — and the principle of ownership: the inner ring defines the interface; the outer ring implements it. You can add an Anti-Corruption Layer ring, a Reporting ring, an External Services ring — as long as every new ring's dependencies point inward, the architecture is valid.
</Inset>

<Quiz
  id="arch-05-01-quiz-1"
  lessonSlug="01-the-concentric-rule"
  lang="en"
  question="The B2B platform's Use Cases ring imports a CurrencyFormatter interface from the Infrastructure ring because that is where the money-formatting utility was originally written. The interface has no infrastructure-specific methods — it is pure. Is this a violation of the dependency rule, and why?"
  choices={[
    { label: "Yes — a violation. The import path crosses the boundary in the wrong direction: the Use Cases ring has a source-code dependency on a package in the Infrastructure ring. The interface being 'pure' does not change where it is defined. The Use Cases ring now depends on the Infrastructure package for the interface definition, and any change to that Infrastructure package can force a recompile of Use Cases. The fix: move the interface definition into the Use Cases ring (where its consumer lives) and let Infrastructure import from Use Cases to implement it.", correct: true },
    { label: "No — not a violation. An interface has no implementation detail, so importing an interface from a ring does not create a real dependency.", misconception: "Source-code dependency is determined by the import path, not by whether the imported artifact is an interface or a class. The compiler resolves the import from the Infrastructure package; if that package changes (is renamed, restructured, or has its type signatures modified), the Use Cases ring recompiles. The dependency is real — the interface is in the wrong package, which is the wrong ownership." },
    { label: "No — not a violation as long as the Use Cases ring only imports the interface and not any concrete class from Infrastructure.", misconception: "This confuses 'no implementation coupling' with 'no source-code dependency.' The import of the interface creates a source-code dependency on the Infrastructure package's compilation unit. The dependency rule is about source-code dependencies — import paths — not about concrete vs abstract types. Moving the interface into the Use Cases ring eliminates the import; that is the structural fix, not choosing which Infrastructure artifact to import." },
    { label: "It depends on whether the Use Cases ring uses Dependency Injection to receive the implementation at runtime.", misconception: "Runtime dependency injection affects runtime coupling, not source-code dependencies. If the Use Cases ring has `import { CurrencyFormatter } from '../infrastructure/formatters'` in its source, it has a source-code dependency on Infrastructure regardless of how instances are assembled at runtime. DI resolves which implementation is provided at runtime; it does not change which package the interface definition lives in." },
  ]}
/>

<Quiz
  id="arch-05-01-quiz-2"
  lessonSlug="01-the-concentric-rule"
  lang="en"
  question="A team wants to add a PDF report generation ring outside the Interface Adapters ring, between Interface Adapters and Frameworks. The PDF library is a third-party vendor package. Where should the PDF ring's interfaces be defined, and which ring should implement them?"
  choices={[
    { label: "The PDF ring's interfaces (e.g., IReportRenderer) should be defined in the Interface Adapters ring — the consumer. The PDF ring implements those interfaces using the third-party vendor library. This preserves inward dependencies: Interface Adapters defines the contract; the outer PDF ring implements it; no inner ring imports from the PDF ring.", correct: true },
    { label: "The interfaces should be defined in the PDF ring itself since that is where the PDF logic lives. Interface Adapters imports these interfaces to use them.", misconception: "Defining the interface in the PDF ring and having Interface Adapters import from it creates an outward dependency: Interface Adapters (inner) depends on the PDF ring (outer). This violates the dependency rule. The interface must be defined where it is consumed — in the inner ring — so that the outer ring must conform to the inner ring's contract, not the reverse." },
    { label: "No interfaces are needed — Interface Adapters can import the vendor PDF library directly since it is just a tool.", misconception: "Importing the vendor PDF library directly into Interface Adapters couples an inner ring to an outer-ring technology. When the vendor changes its API, or when you want to swap PDF libraries, Interface Adapters must change. The inversion — defining an interface in Interface Adapters, implementing it in a PDF adapter ring — keeps Interface Adapters independent of the specific PDF technology." },
    { label: "The new ring violates Clean Architecture because the canonical model has exactly four rings.", misconception: "Martin explicitly states that four rings is illustrative, not prescriptive. Additional rings are permitted as long as the dependency rule holds: every ring's source-code dependencies point inward only. A PDF rendering ring between Interface Adapters and Frameworks is architecturally valid if Interface Adapters defines the interfaces and the PDF ring implements them." },
  ]}
/>

<Quiz
  id="arch-05-01-quiz-3"
  lessonSlug="01-the-concentric-rule"
  lang="en"
  question="An Onion Architecture codebase has this structure: Domain Model → Domain Services → Application Services → Infrastructure. A developer says: 'Onion and hexagonal are the same thing — both just put an interface between the domain and the database.' What is missing from this description?"
  choices={[
    { label: "The statement captures only the innermost/outermost inversion. Onion Architecture applies the dependency rule at every ring boundary — Domain Services cannot import Application Services, Application Services cannot import Infrastructure. Hexagonal inverts one boundary (domain/infrastructure). Onion and Clean generalize to N concentric boundaries with the inversion applied consistently at each. The 'just an interface at the database' reading misses the inward-only rule at the inner ring boundaries.", correct: true },
    { label: "The statement is accurate. Both architectures put an interface between the domain and infrastructure, which is the only meaningful structural difference from layered architecture.", misconception: "Hexagonal architecture does have a single primary inversion (domain owns the secondary ports). Onion Architecture adds the constraint that Application Services cannot import Infrastructure directly either — the inversion applies at every boundary, not just domain/infrastructure. The concentric rule is stricter than 'put an interface at the database.'" },
    { label: "The statement is wrong because hexagonal architecture does not use interfaces — it uses adapters.", misconception: "Hexagonal architecture absolutely uses interfaces (ports). The driving adapters call the primary port interface; the driven adapters implement secondary port interfaces. The port IS the interface. This is not the missing piece in the description — the missing piece is the multi-ring generalization." },
    { label: "The statement is wrong because Onion Architecture does not have an infrastructure ring — infrastructure is just a detail that lives outside the architecture.", misconception: "Onion Architecture explicitly names an Infrastructure ring at the outermost layer. The infrastructure is part of the model — it is placed at the outside precisely because it is the most volatile, detail-specific layer. The ring exists; it is just forced to depend on inner rings, never the reverse." },
  ]}
/>

</Explanation>

<KeyTakeaway>Clean and Onion architecture organize code into concentric rings. The single enforced rule: source-code dependencies only point inward, toward higher-level policy. Inner rings define interfaces; outer rings implement them. This generalizes hexagonal's single inversion to every ring boundary, making the entire inner system independently testable and infrastructure-agnostic.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-05-01-retrieval"
  lang="en"
  questions={[
    { q: "What is the dependency rule in Clean Architecture, and why does it produce independently testable inner rings?", a: "The dependency rule: source-code dependencies (imports, compilation dependencies) may only point inward — toward higher-level policy. An inner ring may never name anything in an outer ring. The consequence for testability: if Entities and Use Cases have no imports from Interface Adapters or Frameworks, they can be compiled and tested with zero infrastructure present. No database, no web server, no ORM is needed to run a Use Case test. You instantiate the entity and use-case classes, call their methods, and assert on their results. Infrastructure is absent because nothing in the inner rings references it. This is not an accident — it is the structural payoff of enforcing the dependency direction." },
    { q: "How does Clean Architecture generalize hexagonal architecture?", a: "Hexagonal architecture applies the dependency inversion at one boundary: the domain defines port interfaces (primary and secondary), and infrastructure implements the secondary ports. The dependency arrow at that boundary points inward (infrastructure → domain interface). Clean Architecture applies the same inversion at every ring boundary. The Use Cases ring defines output port interfaces (e.g., a presenter interface); Interface Adapters implements them. The Interface Adapters ring defines its own interfaces where needed; Frameworks & Drivers implement them. The inversion propagates from every outer ring to every inner ring. Hexagonal is a special case of Clean: a two-ring system where the domain is the inner ring and everything else is the outer." },
    { q: "What is the structural difference between Onion Architecture and Clean Architecture?", a: "Both models enforce the same dependency rule: source-code dependencies point inward only. The differences are in ring names and emphasis. Onion (Palermo, 2008) names four rings: Domain Model (innermost), Domain Services, Application Services, Infrastructure. It emphasizes that the domain model is completely free of all other concerns — no application logic, no infrastructure, nothing. Clean Architecture (Martin, 2012) names: Entities, Use Cases, Interface Adapters, Frameworks & Drivers. It adds an explicit Interface Adapters ring responsible for all data format translation between the use-case world and the external world. In practice, teams use hybrid labeling and the distinction is mostly academic. The invariant both enforce is the direction of the dependency arrow." },
  ]}
/>

<Recap lang="en">
Clean and Onion architecture are concentric-ring models that generalize hexagonal architecture's core insight to every layer boundary. The architectural center holds the most stable, abstract code — pure business rules that change only when the business itself changes. The outer rings hold technology choices — databases, frameworks, protocols — that change for technology reasons unrelated to business logic.

The dependency rule states this precisely: source-code dependencies may only point inward. An inner ring may never import from an outer ring. The inner ring defines the interface; the outer ring implements it. Every ring boundary applies this inversion.

The opening story's violation — a Use Cases ring importing an interface from the Infrastructure package — was technically subtle: there was an interface, there was no concrete import. But the interface definition lived in the wrong ring, and the Use Cases ring had an import path pointing outward. The fix is ownership: move the interface into the ring that consumes it.

Clean Architecture (Martin) and Onion Architecture (Palermo) differ mainly in ring names and emphasis. Both arrive at the same structural rule. The number of rings is not fixed at four — systems add rings as needed. What is non-negotiable is the direction: inward only.

The next lesson examines what lives at the second ring — the Use Cases ring — and how the interactor pattern captures "what the application does" as a first-class structural unit.
</Recap>
