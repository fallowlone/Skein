---
slug: "02-use-cases-and-interactors"
lang: en
track: "architecture-patterns"
unit: "05-clean-and-onion"
order: 2
title: "Use cases and interactors"
summary: "The Use Cases ring orchestrates entities to achieve one application goal. An interactor is the class that implements a use case: it receives a request model, calls entities, and returns a response model — never a framework type."
estMin: 22
status: ready
lessonType: topic
level: middle
concepts:
  - use-case
  - interactor
  - request-model
  - response-model
  - input-port
  - output-port
prereqs: []
sources:
  - https://blog.cleancoder.com/uncle-bob/2012/08/13/the-clean-architecture.html
  - https://www.informit.com/store/clean-architecture-a-craftsmans-guide-to-software-structure-9780134494166
  - https://jeffreypalermo.com/2008/07/the-onion-architecture-part-2/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import ringRuleSplit from "./infographics/ring-rule-split.svg?raw";

<Hook>
The B2B order platform's backend had grown to over forty service methods. When a new engineer joined, their onboarding task was to find where invoice generation happened. They searched for `generateInvoice` and found it called from six places: the HTTP controller, a background job, a webhook handler, a batch reconciliation script, an admin CLI, and an email-processing service. Each callsite passed slightly different arguments. Two of them skipped the "billing hold" check. One used the database connection object directly. There was no single place in the codebase that said: "here is what it means to generate an invoice — the input it requires, the steps it takes, the output it produces." The logic was smeared across caller code. When Clean Architecture describes the Use Cases ring, it is answering exactly this problem: there should be one class per application use case, and that class should encode everything the use case needs — input shape, entity orchestration, output shape. The interactor is that class.
</Hook>

<Crux>An interactor owns one use case: receives a request model, orchestrates entities, returns a response model — all in domain terms only.</Crux>

<Explanation>

## The Use Cases ring and what it owns

In Clean Architecture's ring model, the Use Cases ring sits directly outside the Entities ring. It contains **application-specific business rules** — rules that express what this particular application does with the entities.

The distinction matters. An entity contains **enterprise-wide** business rules: an Order must have at least one line item; an Invoice total must equal the sum of its line items. These rules would be true in any application built on this business. A use case contains **application-specific** rules: to generate an invoice, first check that the order is confirmed, then check for billing holds, then create the invoice entity, then publish an event. These steps are specific to this application's process. Different applications built on the same domain (say, a mobile app vs an operations console) might have different use cases but share the same entities.

<Infographic
  svg={ringRuleSplit}
  label="Side-by-side comparison of the Entities ring and the Use Cases ring. The Entities ring holds enterprise-wide invariants true in any application — an order needs at least one line item, an invoice total equals the sum of its line items, the most stable inner core. The Use Cases ring holds application-specific rules — confirm the order then check the billing hold, orchestrate entities and services through interfaces, and host shared application logic like CheckBillingHoldService."
  caption="The split decides placement: a billing-hold check consults an external service, so it is application logic and belongs in the Use Cases ring — not an entity invariant."
  hue="var(--d-infra)"
/>

The Use Cases ring owns:
- One class per use case (the interactor)
- The input port interface (what the use case accepts)
- The output port interface (what the use case produces)
- The request model (the data structure crossing in)
- The response model (the data structure crossing out)

## The interactor pattern

An interactor is the class that implements a single use case. It contains the application-logic steps for that use case and nothing else. It does not know about HTTP, SQL, JSON, or any framework concept.

```typescript
// All types here are in the Use Cases ring — no framework imports
interface GenerateInvoiceInputPort {
  execute(request: GenerateInvoiceRequest): Promise<void>;
}

interface GenerateInvoiceOutputPort {
  presentSuccess(response: GenerateInvoiceResponse): void;
  presentError(error: InvoiceGenerationError): void;
}

class GenerateInvoiceInteractor implements GenerateInvoiceInputPort {
  constructor(
    private readonly orders: IOrderRepository,
    private readonly invoices: IInvoiceRepository,
    private readonly billingHolds: IBillingHoldChecker,
    private readonly presenter: GenerateInvoiceOutputPort,
  ) {}

  async execute(request: GenerateInvoiceRequest): Promise<void> {
    const order = await this.orders.findById(request.orderId);
    if (!order) {
      this.presenter.presentError(new InvoiceGenerationError('ORDER_NOT_FOUND'));
      return;
    }
    if (order.status !== OrderStatus.Confirmed) {
      this.presenter.presentError(new InvoiceGenerationError('ORDER_NOT_CONFIRMED'));
      return;
    }
    const hold = await this.billingHolds.checkForHold(order.customerId);
    if (hold.active) {
      this.presenter.presentError(new InvoiceGenerationError('BILLING_HOLD_ACTIVE'));
      return;
    }
    const invoice = Invoice.createFor(order);
    await this.invoices.save(invoice);
    this.presenter.presentSuccess(new GenerateInvoiceResponse(invoice.id, invoice.total));
  }
}
```

`GenerateInvoiceInteractor` is the one canonical place that says: "here is what generating an invoice means." Every caller — HTTP controller, batch job, CLI, webhook handler — calls this interactor. No caller can skip the billing hold check because it is inside the interactor, not in the caller.

<FlowDiagram
  label="Interactor orchestration: source-code dependencies across the Use Cases ring boundary"
  caption="The interactor sits in the Use Cases ring. It imports entity types and repository/service interfaces (all defined in inner rings or the Use Cases ring itself). The HTTP controller and batch job are in the Interface Adapters ring; they import the interactor's input port interface. All arrows represent source-code import dependencies pointing inward."
  nodes={[
    { id: "http", label: "HTTP Controller", sub: "Interface Adapters ring" },
    { id: "batch", label: "Batch Job Adapter", sub: "Interface Adapters ring" },
    { id: "input-port", label: "GenerateInvoiceInputPort", sub: "Use Cases ring — interface" },
    { id: "interactor", label: "GenerateInvoiceInteractor", sub: "Use Cases ring — implementation" },
    { id: "output-port", label: "GenerateInvoiceOutputPort", sub: "Use Cases ring — interface" },
    { id: "presenter", label: "HTTP Presenter", sub: "Interface Adapters ring — implements output port" },
    { id: "entities", label: "Order / Invoice entities", sub: "Entities ring" },
  ]}
  edges={[
    { from: "http", to: "input-port", label: "imports" },
    { from: "batch", to: "input-port", label: "imports" },
    { from: "interactor", to: "input-port", label: "implements" },
    { from: "interactor", to: "output-port", label: "calls" },
    { from: "presenter", to: "output-port", label: "implements" },
    { from: "interactor", to: "entities", label: "imports" },
  ]}
/>

## Request models and response models — why they exist

A common early shortcut is to pass the HTTP request object directly into the use case:

```typescript
// Wrong: Use Cases ring importing a framework type
async execute(req: ExpressRequest): Promise<ExpressResponse>
```

This creates a source-code dependency from the Use Cases ring to the HTTP framework — an outward-pointing dependency that violates the concentric rule. Worse, it makes the use case untestable without a real HTTP request object.

The fix is request models and response models: plain data structures defined in the Use Cases ring, carrying exactly the data the use case needs in domain terms.

```typescript
// Correct: Use Cases ring defines its own data shapes
class GenerateInvoiceRequest {
  constructor(
    public readonly orderId: OrderId,
    public readonly requestedBy: UserId,
  ) {}
}

class GenerateInvoiceResponse {
  constructor(
    public readonly invoiceId: InvoiceId,
    public readonly total: Money,
    public readonly generatedAt: Date,
  ) {}
}
```

The HTTP controller (in Interface Adapters) parses the HTTP request and constructs a `GenerateInvoiceRequest`. The interactor never sees an `ExpressRequest`. The interactor produces a `GenerateInvoiceResponse`. The HTTP presenter (also in Interface Adapters) converts it to a JSON HTTP response. The translation between framework types and use-case types is the Interface Adapters ring's entire job.

<Inset kind="why" lang="en">
Why go to the trouble of defining separate request/response models instead of passing a plain object or a generic dictionary? Because the request model documents exactly what a use case needs. It is the use case's API contract — verifiable by type checking, discoverable by IDE navigation, explicit in code review. A plain object or generic dictionary hides this contract; any caller can pass any shape and only fails at runtime. The request model is documentation that the compiler enforces. This is especially important as the team grows: the interactor is the authoritative specification of what a use case requires. New callers read it, not the HTTP controller.
</Inset>

## Input ports and output ports

The interactor communicates through two port interfaces, both defined in the Use Cases ring:

**Input port** (`GenerateInvoiceInputPort`): the interface the interactor implements. Callers (HTTP controllers, CLI adapters, batch jobs) import this interface and call `execute`. They never import the concrete interactor class. This means the entire interactor implementation can be swapped or mocked in tests without changing any caller.

**Output port** (`GenerateInvoiceOutputPort`): the interface the interactor calls to deliver results. The interactor does not return a value — it calls the output port, which the Interface Adapters ring implements (as a presenter). This is the dependency inversion applied to the response side: the interactor does not know about HTTP status codes, JSON serialization, or streaming — it calls a domain-language method like `presentSuccess(response)` and lets the presenter decide how to render it.

This two-port structure mirrors hexagonal architecture's primary and secondary ports, now applied specifically at the Use Cases ring boundary. The interactor is the application "hexagon" for that one use case.

<Inset kind="note" lang="en">
Many implementations skip the output port and just return the response model from the execute method instead. This is simpler and still valid — it only gives up the ability to have the interactor drive multiple presentation concerns from one call. For most use cases, returning the response model directly is the pragmatic choice. The output port pattern is most useful when one use case must update multiple output channels simultaneously (update the UI, publish an event, write a log) without knowing what those channels are. Both approaches preserve the dependency rule; the choice is a tradeoff between simplicity and flexibility.
</Inset>

<Quiz
  id="arch-05-02-quiz-1"
  lessonSlug="02-use-cases-and-interactors"
  lang="en"
  question="The B2B platform's GenerateInvoiceInteractor currently accepts a GenerateInvoiceRequest and calls a GenerateInvoiceOutputPort presenter. The team wants to add a REST endpoint AND a GraphQL endpoint, both triggering the same invoice generation. What changes, and what stays the same?"
  choices={[
    { label: "The interactor stays completely unchanged. Two new Interface Adapter components are added: a REST controller (parses HTTP request, constructs GenerateInvoiceRequest, calls the input port) and a GraphQL resolver (parses GraphQL arguments, constructs the same GenerateInvoiceRequest, calls the same input port). Each has its own presenter implementing GenerateInvoiceOutputPort for its output format. Zero business logic is duplicated.", correct: true },
    { label: "The interactor needs a new execute method variant (executeFromGraphQL) since GraphQL arguments have a different shape than REST request bodies.", misconception: "The interactor works with GenerateInvoiceRequest — a use-case-ring data structure with no HTTP or GraphQL awareness. Both REST and GraphQL controllers translate their respective input formats into the same GenerateInvoiceRequest before calling the input port. There is no GraphQL-specific method on the interactor; the translation work belongs in the adapter (the resolver), not in the use case." },
    { label: "A new GenerateInvoiceInteractor subclass should be created for the GraphQL endpoint to avoid changing existing tested code.", misconception: "Subclassing the interactor to handle a new delivery mechanism defeats the whole point of the architecture. The use case — what it means to generate an invoice — is identical for REST and GraphQL. The interactor is reused unchanged. Only the adapter layer changes: a new resolver joins the existing controller, both calling the same interactor through the same input port." },
    { label: "The GenerateInvoiceRequest model must be updated to carry a 'source' field so the interactor can know whether it was called from REST or GraphQL.", misconception: "The interactor must not know which delivery mechanism triggered it — that is infrastructure knowledge, which lives in the outer rings. Adding a 'source' field to the request model imports delivery-mechanism awareness into the Use Cases ring, violating the concentric rule. If the interactor ever branches on 'source', it has coupled application logic to infrastructure." },
  ]}
/>

<Quiz
  id="arch-05-02-quiz-2"
  lessonSlug="02-use-cases-and-interactors"
  lang="en"
  question="A developer argues: 'The output port pattern adds unnecessary indirection. Our interactor should just return a GenerateInvoiceResponse and let the controller serialize it.' Is this valid, and what does it give up?"
  choices={[
    { label: "Valid — returning the response model directly is a common and reasonable simplification. It gives up the ability to call multiple output channels from within the interactor (e.g., simultaneously update a UI presenter and publish an audit event from one execute call). For use cases with a single output concern, returning directly is simpler and still preserves the dependency rule.", correct: true },
    { label: "Not valid — the output port is required by Clean Architecture. Removing it violates the dependency rule.", misconception: "The dependency rule requires that source-code dependencies point inward — it does not mandate the output port pattern. Returning a response model from execute() and having the controller serialize it still keeps the interactor free of framework types. The output port is one way to achieve the decoupling, not the only way. Martin describes it as one option; many teams find returning a response model simpler for most use cases." },
    { label: "Not valid — if the interactor returns a response model, the controller must import from the Use Cases ring, creating an outward dependency.", misconception: "The controller is in the Interface Adapters ring (outer ring) importing from the Use Cases ring (inner ring). That is an inward-pointing dependency from the perspective of the import's direction: controller depends on use case, not the reverse. The dependency rule is satisfied — a controller importing a use-case response model is correct, because the outer ring is depending on the inner ring." },
    { label: "Valid — but only if the response model is defined in the Interface Adapters ring so the controller owns it.", misconception: "The response model should be defined in the Use Cases ring — it is part of the use case's output contract. If the Interface Adapters ring defines the response model, the interactor would need to import it from there, which is an outward dependency from Use Cases to Interface Adapters. Response models, like request models, belong to the use case." },
  ]}
/>

<Quiz
  id="arch-05-02-quiz-3"
  lessonSlug="02-use-cases-and-interactors"
  lang="en"
  question="The team discovers that PlaceOrderInteractor and GenerateInvoiceInteractor both need to check for billing holds on a customer. They extract a CheckBillingHoldService with shared logic. In which ring does CheckBillingHoldService belong?"
  choices={[
    { label: "Use Cases ring — because the billing hold check is application-specific logic (this platform's rule for what must be verified before an order is placed or an invoice generated). It is not a pure entity rule; it is a cross-cutting application concern. Placing it in the Use Cases ring lets both interactors use it while keeping it free of framework or infrastructure knowledge.", correct: true },
    { label: "Entities ring — because billing holds are a domain concept and should live with the domain objects.", misconception: "The Entities ring contains enterprise-wide business rules that are independent of any application: an Order's line item structure, an Invoice's total invariant. Whether a billing hold blocks an operation is an application-level check — it involves consulting an external service or repository (IBillingHoldChecker), which is not a pure in-memory entity rule. Application-level cross-cutting services belong in the Use Cases ring." },
    { label: "Interface Adapters ring — because it is shared utility code that adapts between different use cases.", misconception: "The Interface Adapters ring translates between use-case types and framework types. It does not contain application business logic. A billing hold check is application logic — it encodes a rule about when to proceed with an operation. Logic that belongs in Use Cases should not be pushed into Interface Adapters; that would create the reverse problem of application rules leaking into the translation layer." },
    { label: "It should be duplicated in each interactor to avoid coupling between use cases.", misconception: "Duplicating business logic violates DRY and creates divergence risk: the two interactors may check billing holds differently as the code evolves. Extracting shared application logic into a Use Cases ring service is the correct move. 'Coupling between use cases' is not the right concern here — use cases in the same ring may share services. The rule is about ring-to-ring dependency direction, not about intra-ring coupling." },
  ]}
/>

</Explanation>

<KeyTakeaway>An interactor is the single authoritative class for one use case: it receives a request model (no framework types), orchestrates entities through repository and service interfaces, and delivers results through an output port or a returned response model. The request model, response model, input port, and output port are all defined in the Use Cases ring — never in an outer ring.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-05-02-retrieval"
  lang="en"
  questions={[
    { q: "What does an interactor own, and what is it forbidden from importing?", a: "An interactor owns the logic for exactly one use case: the steps to orchestrate entities, the checks to run, the sequence of operations. It imports from the Entities ring (entity classes and value objects) and from the Use Cases ring itself (repository interfaces, service interfaces, request/response models, port interfaces — all defined in the Use Cases ring or the Entities ring). It is forbidden from importing anything from the Interface Adapters ring or the Frameworks ring: no HTTP request objects, no ORM-specific types, no framework annotations, no database connection objects. If an interactor imports from an outer ring, it has an outward-pointing source-code dependency, which violates the concentric rule and makes it impossible to test the interactor without instantiating that outer-ring technology." },
    { q: "What is the role of a request model, and why is it not the HTTP request object?", a: "A request model is a plain data structure defined in the Use Cases ring that carries exactly the data a use case needs, expressed in domain terms. Its role is to be the use case's input contract: it documents what the use case requires, is verified by type checking, and is independent of any delivery mechanism. The HTTP request object is a framework type from the Frameworks & Drivers ring — importing it into the Use Cases ring creates an outward source-code dependency. The Interface Adapters ring (the HTTP controller) parses the HTTP request and constructs a request model, then calls the input port with it. This translation is the controller's job. The interactor receives only domain-typed data." },
    { q: "What are input ports and output ports at the Use Cases ring boundary?", a: "Input port: an interface defined in the Use Cases ring that the interactor implements. Callers (in the Interface Adapters ring) import this interface and call its execute method. No caller imports the concrete interactor class — only the interface. This allows the interactor to be replaced (for testing or alternative implementations) without changing any caller. Output port: an interface defined in the Use Cases ring that the interactor calls to deliver results. The Interface Adapters ring (a presenter class) implements this interface. The interactor calls domain-language methods like presentSuccess(response) without knowing whether the output will be an HTTP response, a JSON payload, a CLI print, or a message queue publish. The inversion is symmetric with the input port: the inner ring defines the interface; the outer ring implements it." },
  ]}
/>

<Recap lang="en">
The Use Cases ring solves the "smeared logic" problem: when the same application operation is implemented differently across six callsites, there is no single place that encodes what the operation means. The interactor pattern addresses this directly — one class, one use case, one place to find the complete logic.

An interactor receives a request model (a plain data structure defined in the Use Cases ring, containing domain-typed fields and no framework types), orchestrates entities through interface-defined repositories and services (also defined in the Use Cases ring), and delivers results through an output port or a returned response model. Every caller — HTTP controller, CLI adapter, batch job, webhook handler — constructs a request model and calls the input port. They all go through the same logic, including all the same checks.

The two-port structure (input port and output port) applies the dependency inversion symmetrically: callers depend on the input port interface (inward dependency from Interface Adapters to Use Cases); the interactor calls the output port (the Interface Adapters ring implements the output port interface, which is defined in Use Cases). No inner-ring code names any outer-ring type.

The next lesson examines the Entity core — the innermost ring, which contains the most stable code — and the honest cost of this whole structure: the mapping tax, the ceremony, and the circumstances under which the overhead is not worth it.
</Recap>
