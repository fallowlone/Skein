---
slug: "02-anemic-vs-rich-domain"
lang: en
track: "architecture-patterns"
unit: "03-layered-architecture"
order: 2
title: "Anemic vs rich domain model"
summary: "An anemic domain model puts all behavior in services and leaves domain objects as pure data bags. It is a recognized anti-pattern for complex domains because it destroys encapsulation and lets invariants drift. For simple CRUD it is often the pragmatic choice."
estMin: 22
status: ready
lessonType: topic
level: middle
concepts:
  - anemic-domain-model
  - rich-domain-model
  - domain-invariant
  - encapsulation
  - transaction-script
  - domain-service
prereqs: []
sources:
  - https://martinfowler.com/bliki/AnemicDomainModel.html
  - https://martinfowler.com/eaaCatalog/transactionScript.html
  - https://www.informit.com/store/domain-driven-design-tackling-complexity-in-the-heart-9780321125217
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import anemicVsRich from "./infographics/anemic-vs-rich.svg?raw";

<Hook>
The B2B order platform had an Order class with twenty fields: id, status, customerId, lineItems, invoiceId, totalAmount, approvedBy, approvedAt, confirmedAt, cancelledAt, and more. The class had getters and setters for each field and nothing else. All the logic lived in OrderService. There was a method `confirmOrder`, a method `cancelOrder`, a method `approveOrder`, and a method `applyDiscount`. Three months after the platform launched, a bug was found in production: a cancelled order had been invoiced. The root cause was that `confirmOrder` and the billing job both changed the order's status field directly — one via `setStatus('confirmed')`, one via an ORM update — without knowing about each other. The invariant "a cancelled order cannot be confirmed or invoiced" existed in comments and in the team's heads, but nowhere in the code. Because the Order class owned no behavior, it also enforced no rules. Any piece of code in the system could set any field to any value at any time. The model was technically object-oriented — it used a class — but it was not actually encapsulating anything.
</Hook>

<Crux>Anemic models separate data from governing behavior, letting invariants drift. Rich models co-locate behavior with data they protect.</Crux>

<Explanation>

## What an anemic domain model looks like

Martin Fowler coined the term "Anemic Domain Model" in 2003 to describe a pattern that had become pervasive: domain objects that are little more than named data structures — getters, setters, and no real behavior — combined with service classes that contain all the logic.

```typescript
// Anemic: Order is a data bag
class Order {
  id: string;
  status: string;
  lineItems: LineItem[];
  totalAmount: number;
  customerId: string;
  approvedBy?: string;
  approvedAt?: Date;

  // Only getters and setters — no behavior, no enforcement
  setStatus(s: string) { this.status = s; }
  setApprovedBy(userId: string) { this.approvedBy = userId; }
}

// All logic in the service
class OrderService {
  async confirmOrder(orderId: string, userId: string) {
    const order = await this.repo.findById(orderId);
    if (order.status === 'pending') {
      order.setStatus('confirmed');
      order.setApprovedBy(userId);
      order.approvedAt = new Date();
      await this.repo.save(order);
    }
  }
}
```

The service knows the rules. The order object enforces nothing. Any code anywhere can call `order.setStatus('confirmed')` directly, bypassing every check in `OrderService`. The invariant "only pending orders can be confirmed" exists only inside one method of one service class.

Fowler's verdict: this is an anti-pattern because it defeats the purpose of object-oriented design. An object that owns no behavior and enforces no invariants is not a domain model — it is a glorified C struct with Java syntax.

## Why it becomes a problem — with complex invariants

The anemic model's rot appears gradually, and it appears specifically when **invariants become complex or multiple**.

In the B2B order platform, the rules governing an order's lifecycle multiplied over time:
- A pending order can be confirmed only if it has at least one line item.
- An order above $10,000 requires approval before confirmation.
- A confirmed order can be cancelled only within 24 hours.
- A cancelled order cannot be invoiced, confirmed, or re-opened.
- An order's total amount must equal the sum of its line items at all times.

Each of these rules was implemented inside a different service method. `confirmOrder` checked two of them. `cancelOrder` checked one. The billing job checked none — it had been written by a different team that did not know the order lifecycle rules. The bug was not a programming mistake — it was a structural mistake. The rules were scattered across service methods that had no guarantee of being the only code path that could mutate the order.

<FlowDiagram
  label="Anemic model: behavior scattered across services"
  nodes={[
    { id: "order", label: "Order (anemic)", sub: "data only — fields + setters" },
    { id: "os", label: "OrderService", sub: "confirmOrder, cancelOrder, approve" },
    { id: "billing", label: "BillingJob", sub: "reads and mutates order directly" },
    { id: "report", label: "ReportingService", sub: "reads and updates status field" },
  ]}
  edges={[
    { from: "os", to: "order", label: "mutates via setters" },
    { from: "billing", to: "order", label: "mutates via setters" },
    { from: "report", to: "order", label: "mutates via setters" },
  ]}
  caption="In the anemic model, every service can reach into the Order object and mutate any field. There is no single owner of the invariants — rules are wherever the developer happened to write them. A new caller (BillingJob, ReportingService, a background task) can bypass all of them."
/>

**Encapsulation is the core property the anemic model destroys.** Encapsulation is not about hiding fields behind getter/setter method calls — that is just syntactic wrapping. Real encapsulation means that an object's internal state can only be changed through operations that enforce the object's invariants. If any external code can call `setStatus('confirmed')` directly, the Order object provides no encapsulation regardless of whether `status` is private.

## The rich domain model alternative

A rich domain model places behavior with the data it governs. The Order object owns its state transitions and enforces its invariants at the boundary of each operation:

```typescript
// Rich: Order owns its invariants
class Order {
  private status: OrderStatus;
  private lineItems: LineItem[];
  private totalAmount: Money;
  private approvalRecord?: ApprovalRecord;

  confirm(approvedBy: UserId): void {
    if (this.status !== OrderStatus.Pending) {
      throw new InvalidOrderTransitionError(
        `Cannot confirm an order in status ${this.status}`
      );
    }
    if (this.lineItems.length === 0) {
      throw new OrderInvariantViolation('Cannot confirm an order with no line items');
    }
    if (this.totalAmount.greaterThan(Money.of(10_000, 'USD')) && !this.approvalRecord) {
      throw new OrderInvariantViolation('Orders above $10,000 require prior approval');
    }
    this.status = OrderStatus.Confirmed;
    this.confirmedAt = new Date();
  }

  cancel(): void {
    if (this.status === OrderStatus.Cancelled) {
      throw new InvalidOrderTransitionError('Order already cancelled');
    }
    this.status = OrderStatus.Cancelled;
    this.cancelledAt = new Date();
  }
}
```

Now the BillingJob cannot set `status = 'confirmed'` directly — it must call `order.confirm()`, which enforces every relevant rule. The billing bug from the opening story becomes impossible: calling `confirm()` on a cancelled order throws an error. No second caller can bypass it.

<Infographic
  svg={anemicVsRich}
  label="Side-by-side comparison of the anemic and rich domain models across four dimensions: where logic lives, how state is mutated, where invariants are enforced, and which domains each fits."
  caption="The anemic model leaves data open to any caller, so invariants scatter; the rich model channels every change through intent methods that enforce invariants at the boundary. Pick by the domain's real invariant complexity, not by reflex."
  hue="var(--d-infra)"
/>

<Inset kind="why" lang="en">
Why does co-locating behavior with data matter beyond style preference? Because the domain object becomes the single point of truth for what valid state looks like. With an anemic model, the truth is distributed across every service method that ever touches the object — and across every developer who might someday write a new service method. With a rich model, the truth is in one place. You can read the Order class and know every state transition rule without reading the rest of the codebase. That property matters most when the domain is complex and the team is large.
</Inset>

## The honest counter-case: when anemic is fine

Fowler's critique is correct for complex domains with meaningful invariants. It is NOT a universal law. The anemic model is perfectly appropriate when:

**The domain is genuinely CRUD.** A user profile management service — create, read, update, delete user records — has no state machine, no multi-step lifecycle, no invariants beyond "email must be unique." A rich domain model here adds indirection and boilerplate with no return. An anemic model is honest about what the domain is.

**The domain is simple and stable.** Configuration tables, lookup tables, reference data — this is data management, not domain modeling. ORM entities with getters and setters are the right fit.

**The application is short-lived or throwaway.** A data migration script, a one-time ETL job, an internal admin panel used by three people — the cost of establishing a rich domain model is not justified by the return.

The anti-pattern label applies when a team uses an anemic model for a complex domain with meaningful lifecycle invariants — and then experiences the consequences (invariant violations, scattered logic, inability to reason about state) while insisting it was the right choice. The problem is not anemic-vs-rich as a religious question. The problem is using the wrong tool for the domain's actual complexity.

<Inset kind="note" lang="en">
The Transaction Script pattern (also Fowler's term) is the anemic approach taken to its logical conclusion: a procedure that runs through a series of steps to complete a business transaction, with no persistent domain objects at all. Transaction Script works well for simple workflows where the procedural clarity outweighs the loss of encapsulation. It becomes a problem when the procedures grow long, overlap in their state mutations, and start duplicating the same business rules in multiple places. The procedural approach scales with simplicity; the rich domain model scales with invariant complexity.
</Inset>

<Quiz
  id="arch-03-02-quiz-1"
  lessonSlug="02-anemic-vs-rich-domain"
  lang="en"
  question="The B2B order platform has an invariant: a confirmed order can only be cancelled within 24 hours of confirmation. This rule is checked in `OrderService.cancelOrder()`. A new background job reads orders and calls `order.setStatus('cancelled')` directly to clean up stale orders. What structural property of the anemic model allowed this bug to appear?"
  choices={[
    { label: "The anemic Order object provides no encapsulation: any caller can mutate its status field directly via setters, bypassing the invariant check in OrderService. Because behavior and data are separated, the invariant only exists in one service method — it is invisible to any caller that does not know about that method.", correct: true },
    { label: "The background job was written incorrectly — it should have called OrderService.cancelOrder() instead of setting the field directly. The fix is to correct the job, not to change the architecture.", misconception: "Calling the correct service method is one fix for one caller. The structural problem is that nothing forces a caller to go through the service — the Order object's API permits direct mutation. The next new caller (a third-party integration, a migration script, a new team member) faces the same invitation to bypass the check. The architecture makes the correct path easy to miss and the incorrect path easy to take." },
    { label: "The invariant is a business rule and should be enforced by a database constraint, not in code.", misconception: "Database constraints can enforce simple data rules (non-null, uniqueness, check constraints). A time-based conditional rule — 'cancellation is only valid if now minus confirmedAt is less than 24 hours' — is too dynamic and business-semantics-rich for a database constraint. Business rules with this level of complexity belong in the domain model." },
    { label: "The bug occurred because OrderService and the background job were not transactional — the fix is to add distributed locking.", misconception: "Distributed locking prevents concurrent races — two callers mutating the same record simultaneously. That is a different problem. This bug is not a concurrency issue; it is an invariant bypass issue. A single caller writing a stale cancellation is the problem, and it would occur even with no concurrency. The root cause is that the invariant check is not at the boundary of the mutation." },
  ]}
/>

<Quiz
  id="arch-03-02-quiz-2"
  lessonSlug="02-anemic-vs-rich-domain"
  lang="en"
  question="A team is building a B2B order platform billing module. The billing module creates invoices, stores payment records, and links them to orders. There are no complex state transitions — an invoice is created once, marked paid once, and never changes state again. Should the team use a rich domain model or an anemic model for Invoice?"
  choices={[
    { label: "Anemic model is appropriate. Invoice has a trivial lifecycle (created → paid) with no branching invariants, no complex conditions, no need for multiple callers to be prevented from bypassing checks. A rich model would add boilerplate indirection for no structural benefit.", correct: true },
    { label: "Rich model always. Domain objects should always own their state transitions, regardless of how simple they are.", misconception: "The rich model earns its cost when the domain has meaningful complexity — multiple callers that could bypass invariants, complex lifecycle state machines, rules that grow over time. For a simple two-state entity with a single transition, a rich model adds indirection and ceremony without preventing any real structural problem. The pragmatic choice is the anemic model, with the understanding that if Invoice complexity grows, the model should be revisited." },
    { label: "Use a service layer that enforces invoice state transitions, and keep Invoice as a data record — that is the industry standard.", misconception: "A service that enforces the two-state transition on an Invoice data record is exactly the anemic model. The question is whether this is the right choice for the Invoice's complexity, not whether it has an industry label. For a two-state entity, it is a reasonable choice — but it should be a deliberate one, not a reflex." },
    { label: "Neither — for billing, the Transaction Script pattern (procedural code, no domain objects) is the only correct choice.", misconception: "Transaction Script is a valid option for simple billing logic, but it is not the only one. The question is which level of abstraction matches the complexity. For Invoice with two states and no branching, anemic model (or even Transaction Script) works. For Invoice with complex credit note logic, partial payments, disputes, and audit trails, a richer model earns its cost." },
  ]}
/>

<Quiz
  id="arch-03-02-quiz-3"
  lessonSlug="02-anemic-vs-rich-domain"
  lang="en"
  question="Refactoring an anemic Order to a rich domain model, the team discovers they cannot simply add methods like `confirm()` to the Order class — the existing code calls `order.setStatus()` in 47 different places. What does this distribution tell you, and what does it mean for the refactoring?"
  choices={[
    { label: "It tells you the invariants have been enforced inconsistently across 47 sites — some with checks, some without. The refactoring must identify all 47 call sites, determine which checks each one was (or was not) performing, consolidate the correct invariant logic into Order.confirm(), Order.cancel(), etc., and replace all setter calls with the appropriate domain operation. The distribution is not a reason to avoid the refactoring — it is evidence of how badly it is needed.", correct: true },
    { label: "47 call sites means the domain model is too large and should be split into smaller services before refactoring.", misconception: "The number of call sites is a symptom of the anemic model (no enforcement at the boundary invites everyone to mutate directly), not evidence that the domain is too large. Splitting into smaller services would distribute the 47 call sites further without eliminating the invariant enforcement gap. The refactoring that matters is introducing the domain method that enforces the invariant, then replacing the scattered setters with it." },
    { label: "The existing code is correct — 47 sites calling setStatus means the logic is well-distributed. No refactoring is needed.", misconception: "Distribution of mutation logic is the opposite of well-designed — it means the invariant is repeated (or missed) 47 times. Consistency in a distributed anemic model is maintained by convention and review, not by structure. One missed call site produces a bug. The goal of the refactoring is to make the correct path the only path." },
    { label: "The solution is to add a database trigger that enforces the state machine, removing the need to refactor application code.", misconception: "A database trigger can enforce some state constraints, but it cannot access application-level context (the approval record object, the user's authorization level, the business rules for the transition). More importantly, database triggers move business logic into infrastructure, deepening the layering violation. The right fix is a domain method that encapsulates the invariant at the application level." },
  ]}
/>

</Explanation>

<KeyTakeaway>An anemic domain model separates data from the behavior that governs it, scattering invariant enforcement across service methods. This is Fowler's anti-pattern for complex domains: any caller can bypass the checks. A rich domain model co-locates behavior with data, enforcing invariants at the boundary of each state transition. For genuinely simple CRUD domains, the anemic model is pragmatic and appropriate — the anti-pattern label applies when complexity is real but the model stays thin.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-03-02-retrieval"
  lang="en"
  questions={[
    { q: "What makes an anemic domain model an anti-pattern, and what property of the domain determines whether it is actually a problem?", a: "Fowler's critique: an anemic domain model uses classes as named data structures (fields + getters/setters) with all behavior in services. This destroys encapsulation — any code can mutate any field via setters, bypassing any invariant check in the service layer. A new caller, a new integration, a background job written by a different team can all reach around the service and mutate state directly. The invariants exist only as implicit conventions, not as enforced structural guarantees. The property that makes this a real problem is domain complexity: when the domain has meaningful lifecycle invariants, multiple callers, and rules that can be violated by bypassing the service, the anemic model fails at its core job. For genuinely simple CRUD domains (no meaningful invariants, single-state entities, reference data), the anemic model is appropriate — the indirection of a rich model costs more than it buys." },
    { q: "What does real encapsulation mean in the context of a domain object, and how does a rich domain model provide it?", a: "Real encapsulation is not about making fields private or adding getter/setter methods — it is about ensuring that an object's state can only change through operations that enforce its invariants. A domain object with a private `status` field and a public `setStatus(string)` method has syntactic privacy but no encapsulation — any caller can still set any status value. A rich domain model provides encapsulation by replacing setter-based mutation with intent-expressing methods: `order.confirm()`, `order.cancel()`, `order.approve()`. Each method contains the full invariant logic for that transition. The state field is not directly settable — it changes only as a consequence of calling a method that has verified all the preconditions. Any caller that wants to change Order state must go through a method that enforces the rules, making invariant violations structurally impossible rather than just conventionally discouraged." },
    { q: "When is the anemic domain model an appropriate choice, and how do you tell when a domain has outgrown it?", a: "The anemic model is appropriate when: the domain is genuinely CRUD (no meaningful lifecycle, no invariants beyond data integrity constraints); the application is simple and stable (configuration tables, reference data, throwaway scripts); the team understands the tradeoff and has chosen it deliberately. The domain has outgrown it when: bugs appear because callers bypassed the service (the direct setter was easier to call); invariant logic is duplicated in multiple service methods; a new team member or integration writes code that mutates state without knowing the rules; a code review catches direct field mutations routinely. The tell is scattered enforcement: if you search the codebase for direct mutations of a domain object's state and find them in five different files, the invariants are being maintained by convention only. That is the moment to migrate to a richer model." },
  ]}
/>

<Recap lang="en">
The anemic domain model is the natural result of building a layered architecture and then hollowing out the domain layer. Domain objects become data bags — fields, getters, setters — and services carry all the logic. It looks like object-oriented design because it uses classes, but it provides none of the encapsulation that makes OO useful.

Fowler's critique: the anemic model is an anti-pattern for complex domains because it separates behavior from the data the behavior is supposed to protect. Any caller can bypass the service that contains the rules and mutate domain object state directly. Invariants drift into implicit conventions, maintained by code review and team discipline rather than structural enforcement. When the domain grows in complexity and the number of callers grows, those conventions break.

The rich domain model puts behavior with data. `Order.confirm()` carries the full invariant logic for a confirmation transition. No caller can reach around it. The domain object's API is the set of valid operations, and each operation enforces its preconditions.

The honest counter-case: for genuinely simple CRUD domains — two-state entities, reference data, short-lived scripts — the anemic model is pragmatic and appropriate. The boilerplate and indirection of a rich model add cost without buying anything. The decision should be made deliberately, based on the domain's actual invariant complexity, not as a reflexive pattern choice.

The next lesson examines what happens when layering is applied but the discipline does not hold: the transaction script trap, layer-skipping, and the fat service that becomes the god object of the application.
</Recap>
