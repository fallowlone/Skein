infographic compare-hierarchy-row-letter-card-rounded-rect-node
data
  title Layered architecture: 3 leaks
  compares
    - label Transaction-script trap
      children
        - label Sign: domain hollow, service = app + domain
        - label Gap: nothing forces domain to hold logic
        - label Fix: rich domain core (hexagonal)
    - label Layer-skipping
      children
        - label Sign: controller imports repository directly
        - label Gap: no compiler stops importing downward
        - label Fix: domain owns its ports (inversion)
    - label Fat service / god object
      children
        - label Sign: 61 methods, 11 infra imports @ 18mo
        - label Gap: no use-case boundary defined
        - label Fix: each use case = one primary port
theme
  palette #6366f1 #14b8a6 #f59e0b
