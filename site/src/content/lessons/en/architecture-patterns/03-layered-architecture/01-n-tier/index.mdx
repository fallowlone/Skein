---
slug: "01-n-tier"
lang: en
track: "architecture-patterns"
unit: "03-layered-architecture"
order: 1
title: "N-tier layered architecture"
summary: "Layered architecture divides code into horizontal tiers — presentation, application, domain, infrastructure — each depending only on the layer below. It buys replaceability and testability but creates a sinkhole when layers merely pass data through without adding value."
estMin: 20
status: ready
lessonType: topic
level: middle
concepts:
  - layered-architecture
  - n-tier
  - strict-layering
  - relaxed-layering
  - architecture-sinkhole
  - layer-replaceability
prereqs: []
sources:
  - https://martinfowler.com/bliki/PresentationDomainDataLayering.html
  - https://www.informit.com/store/clean-architecture-a-craftsmans-guide-to-software-structure-9780134494166
  - https://www.oreilly.com/library/view/software-architecture-patterns/9781491971437/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The B2B order platform started as a single file — a request handler that read from the database, ran some validation, and returned a response. Within six months the file was 3,000 lines. The team split it into "layers": a controller package, a service package, a repository package. The layering made the code navigable. Tests could be written against services without a live database. When the team switched from a REST-based database client to a native driver, only the repository package changed. Two years later, the platform had twelve service classes. Each service method followed an identical pattern: validate the input, call the repository, return the result. None of the service methods did anything except delegate — they called one repository method and returned whatever it returned, unchanged. Every change required touching the controller, the service, and the repository in lockstep. The layering that was supposed to provide replaceability had become a ceremony: three files changed for every one-line business rule. The team had the layers right but had never asked what each layer was supposed to add.
</Hook>

<Crux>Layered architecture enforces downward-only dependencies. Value depends on whether each layer adds real logic or just passes data.</Crux>

<Explanation>

## The four standard layers

The canonical n-tier model names four layers, each with a specific responsibility:

**Presentation layer** — handles all user-facing concerns: HTTP request parsing, input deserialization, response formatting, authentication tokens, content negotiation. It knows about the web protocol. It does not know about business rules.

**Application layer** — orchestrates use cases. It sequences calls to the domain and infrastructure, manages transactions, enforces authorization policy, and publishes events. It knows about the flow of a use case. It does not contain business logic — it delegates to the domain.

**Domain layer** — contains the business model: entities, value objects, domain services, and the rules that govern them. In the B2B order platform, "an order cannot be confirmed if it has unpaid invoices" is domain logic. This layer has no knowledge of the database, the HTTP framework, or the queue system.

**Infrastructure layer** — implements the ports that the domain and application layers define: database repositories, email senders, message queue publishers, external API clients. It knows about technology. It does not know about business rules.

<FlowDiagram
  label="Four-layer dependency stack"
  nodes={[
    { id: "pres", label: "Presentation", sub: "HTTP, serialization, auth tokens" },
    { id: "app", label: "Application", sub: "use-case orchestration, transactions" },
    { id: "domain", label: "Domain", sub: "entities, rules, invariants" },
    { id: "infra", label: "Infrastructure", sub: "DB, email, queue, external APIs" },
  ]}
  edges={[
    { from: "pres", to: "app", label: "depends on (source-code)" },
    { from: "app", to: "domain", label: "depends on (source-code)" },
    { from: "app", to: "infra", label: "depends on (source-code)" },
    { from: "domain", to: "infra", label: "depends on (source-code) — relaxed only" },
  ]}
  caption="In strict layering, Presentation depends on Application, Application depends on Domain, Domain depends on nothing, Infrastructure implements interfaces defined by Domain or Application. In relaxed layering, Application may also depend on Infrastructure directly. The dependency arrows always point downward — no layer imports from a layer above it."
/>

## Strict versus relaxed layering

**Strict layering** requires that each layer depends only on the layer immediately below it, and on nothing else. Presentation calls Application; Application calls Domain; Domain defines interfaces that Infrastructure implements. Domain has zero knowledge of Infrastructure — it does not import the database driver, the ORM, or any framework class.

Strict layering gives you the strongest isolation: the domain can be tested with no database, no queue, no external service. It can be understood without knowledge of any technology. It is the foundation on which hexagonal and clean architecture are built (those patterns are strict layering taken to its logical conclusion, which units 04 and 05 cover).

**Relaxed layering** (also called "open layer" architecture) allows a layer to call any layer below it, not just the immediately adjacent one. Presentation may call Domain directly, bypassing Application. Application may import Infrastructure directly rather than through an interface.

Relaxed layering is pragmatic: for simple CRUD operations the application layer adds no value, and forcing a call through it is ceremony. The tradeoff is that relaxed layering is easier to violate without noticing — once you allow skipping layers for convenience, the discipline erodes and you end up with presentation importing repository classes directly.

<Inset kind="why" lang="en">
Why does layering exist at all? The original force it addresses is replaceability. When the B2B platform switched database drivers, only the infrastructure layer changed — because no business logic had leaked into it. When the team needed to test order confirmation rules in isolation, they could — because the domain layer had no dependency on the database. Both benefits require that the layer boundaries hold: that no higher layer imports a lower layer's implementation details. The moment business logic drifts into a repository, or presentation logic drifts into a domain entity, those benefits disappear.
</Inset>

## What layering actually buys — and its cost

The concrete benefits of a well-maintained layered architecture:

**Replaceability** — the infrastructure can be swapped (new database, new message broker, new email provider) without touching the domain. This works only when the domain layer defines the interface and infrastructure implements it.

**Independent testability** — the domain can be tested with pure in-memory doubles. The application layer can be tested with fake infrastructure. No end-to-end test needed to verify a business rule.

**Navigability** — engineers know where to look for each kind of change. Changing a URL route: Presentation. Changing the flow of a use case: Application. Changing a business rule: Domain. Changing the SQL: Infrastructure.

The cost: every cross-cutting change (adding a new field to an entity, exposing it in the API, and persisting it) requires touching four files in four packages. The layered architecture trades horizontal modularity for vertical rigidity. Adding a feature is a vertical cut through all layers; the architecture makes that cut explicit and therefore somewhat tedious.

## The architecture sinkhole anti-pattern

The architecture sinkhole is the failure mode described in the opening story: a layer that adds no value and simply passes calls through to the layer below.

```
// Service that is a pure sinkhole — adds nothing
class OrderService {
  async getOrder(id: string) {
    return this.orderRepository.findById(id);  // zero transformation, zero logic
  }
  async createOrder(dto: CreateOrderDto) {
    return this.orderRepository.save(dto);  // zero validation, zero domain logic
  }
}
```

A sinkhole service is not just useless — it is harmful. It adds indirection without adding value. It forces every change to touch an extra file. It trains the team to add methods to the service as a reflex, even when the service has nothing to contribute.

The sinkhole is not a sign that layering was wrong — it is a sign that the domain layer was hollowed out. If the domain layer contains no real logic, the application layer has nothing to orchestrate, and the service layer becomes a pass-through. The fix is not to remove the layers — it is to push the logic where it belongs: into the domain (units 03-02 covers this in depth as the anemic-domain-model pattern).

<Inset kind="note" lang="en">
Mark Richards and Neal Ford recommend the "80/20 rule" for sinkholes: if more than 20% of the layer's requests simply pass through with no transformation, the architecture is accumulating sinkhole debt. This is not a precise metric but a diagnostic — if you find yourself writing empty delegation methods routinely, the layer structure is not paying for itself.
</Inset>

<Quiz
  id="arch-03-01-quiz-1"
  lessonSlug="01-n-tier"
  lang="en"
  question="In the B2B order platform, a new rule is added: orders worth more than $10,000 must be approved by a senior account manager before confirmation. Where does this logic belong in a four-layer architecture, and why?"
  choices={[
    { label: "Domain layer. The rule 'orders above $10,000 require approval' is a business invariant — it governs what a valid state transition for an order looks like. Business invariants belong in the domain layer, not in the application or presentation layers, so they can be enforced regardless of which interface triggers the operation.", correct: true },
    { label: "Application layer. Approval workflows are use-case orchestration and belong in the application layer, not the domain.", misconception: "The orchestration of HOW the approval is requested (notify the manager, wait for a response, then proceed) belongs in the application layer. But the rule THAT an approval is required for orders above $10,000 is a business invariant — a property of what makes an order valid. Invariants belong in the domain layer, so they cannot be bypassed by any use case that skips the application layer check." },
    { label: "Presentation layer. The rule should be enforced at the API level via request validation.", misconception: "Presentation-layer validation guards against malformed input (missing fields, wrong types). Business rules — rules that govern domain state transitions — must live in the domain, or they can be bypassed by any code that calls the lower layers directly. A rule enforced only in the controller can be bypassed by a batch job or a background worker that calls the repository directly." },
    { label: "Infrastructure layer. The rule should be enforced by a database constraint.", misconception: "Database constraints can enforce data-level invariants (non-null, uniqueness, referential integrity). But conditional workflow rules — 'if amount > $10,000, require an approval record' — are too complex for a database constraint and too rich with business semantics to belong in infrastructure. Infrastructure implements persistence; it does not own business rules." },
  ]}
/>

<Quiz
  id="arch-03-01-quiz-2"
  lessonSlug="01-n-tier"
  lang="en"
  question="The team uses strict layering. An engineer proposes that the domain layer should import a logging library directly to record when invariant violations occur. What does this violate and what is the correct approach?"
  choices={[
    { label: "This violates strict layering: importing a logging library couples the domain to an infrastructure concern (the log transport, configuration, and format). In strict layering the domain has zero infrastructure dependencies. The correct approach is to define an IDomainEventLogger interface in the domain layer and inject an implementation from the infrastructure layer.", correct: true },
    { label: "Logging is a cross-cutting concern, so importing it anywhere is fine — it does not count as an infrastructure dependency.", misconception: "Logging libraries are infrastructure. They write to files, streams, or external services — all of which are I/O concerns. In strict layering, any I/O belongs in infrastructure. Even logging. The domain layer stays pure by defining an interface for any observability concern it needs; infrastructure provides the implementation." },
    { label: "This only violates layering if the logging library also imports domain types.", misconception: "The direction of the violation is from the domain importing infrastructure, not the reverse. Even if the logging library has no knowledge of the domain, the domain having a compile-time dependency on the logging library means swapping the logging library requires changing the domain. That is the violation strict layering prevents." },
    { label: "The domain layer can import stable, widely-used infrastructure libraries without violating layering — the rule only applies to volatile dependencies.", misconception: "The layering rule is about architectural category, not about how stable the library is. Logging is infrastructure regardless of how stable it is. If the domain can import 'stable' infrastructure, the line becomes negotiable and gradually everything becomes 'stable enough' to import. Strict layering requires a hard boundary." },
  ]}
/>

<Quiz
  id="arch-03-01-quiz-3"
  lessonSlug="01-n-tier"
  lang="en"
  question="A team has a 50-method OrderService where 40 of the methods simply call the corresponding OrderRepository method and return the result unchanged. What architectural problem does this describe, and what is the root cause?"
  choices={[
    { label: "The architecture sinkhole anti-pattern: 80% of the application layer passes through with no transformation or orchestration. The root cause is that the domain layer has no real logic — business rules that should live in the domain have either been pushed into the repository (infrastructure) or never written at all. The service layer has nothing to orchestrate, so it becomes a pass-through.", correct: true },
    { label: "The application layer is too large — it should be split into smaller services. The root cause is poor service design.", misconception: "The problem is not size — it is that the methods add no value. Splitting 50 empty delegation methods into two 25-method services still gives you 50 sinkholes. The root cause is the missing domain layer, not the structure of the application layer." },
    { label: "The repository layer is doing too much work. The fix is to add logic to the service methods instead.", misconception: "Adding arbitrary logic to service methods does not fix the sinkhole — it just moves behavior from infrastructure to application without putting it in the domain. The fix is to identify the business rules, invariants, and transformations that belong in the domain layer, and move them there. Domain logic should live in domain entities and domain services, not in application services." },
    { label: "This is acceptable and expected in a CRUD application — layered architecture is not suitable for simple data access patterns.", misconception: "For genuinely simple CRUD with no business rules, a thinner architecture (without a distinct application or domain layer) is a reasonable choice. But the sinkhole is not 'acceptable' — it is a warning that the layers are adding overhead without benefit. The honest response is either to add genuine domain logic (if the domain has rules) or to simplify the architecture by removing the unnecessary layers." },
  ]}
/>

</Explanation>

<KeyTakeaway>N-tier layered architecture separates presentation, application, domain, and infrastructure concerns into horizontal layers where each depends only downward. It buys replaceability and independent testability when the boundaries hold. The architecture sinkhole — layers that add no value and just pass through — is the failure mode that emerges when the domain layer is hollowed out of real logic.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="arch-03-01-retrieval"
  lang="en"
  questions={[
    { q: "What are the four standard layers in n-tier architecture and what is each layer's specific responsibility?", a: "Presentation: handles HTTP, request parsing, serialization, authentication tokens — everything web-protocol-facing. Application: orchestrates use cases, manages transactions, sequences domain and infrastructure calls, enforces authorization. Domain: contains business entities, value objects, and the rules that govern them — the invariants, state-transition rules, and policies of the business. Infrastructure: implements the technical ports the domain and application require — database repositories, email senders, queue publishers, external API clients. The key discipline: each layer knows about the layers below it and nothing above it. Domain knows nothing about infrastructure; application knows nothing about HTTP." },
    { q: "What is the difference between strict and relaxed layering, and what is the tradeoff?", a: "Strict layering: each layer may only depend on the immediately adjacent layer below. Presentation calls Application only. Application calls Domain and (through interfaces) Infrastructure. Domain has zero infrastructure imports. Relaxed layering: each layer may depend on any layer below it, skipping layers when convenient. Presentation may call Domain directly; Application may import Infrastructure classes directly. The tradeoff: strict layering gives the strongest isolation — the domain can be tested and understood with no infrastructure knowledge — but requires more interface boilerplate and more discipline to maintain. Relaxed layering is pragmatic for simple cases but erodes more easily; once skipping is permitted for convenience, the pattern tends to drift toward presentation importing repositories." },
    { q: "What is the architecture sinkhole anti-pattern and what is its root cause?", a: "The sinkhole is a layer that adds no transformation, orchestration, or logic — it simply calls the layer below and returns the result unchanged. A service method that does nothing except call one repository method is a sinkhole. The root cause is a hollowed-out domain layer: if the domain layer contains no real business logic, the application layer has nothing to orchestrate, and it becomes a pass-through. The sinkhole is not a sign to restructure the layers — it is a sign to fill the domain layer with the real invariants, state-transition rules, and policies of the business. The diagnostic: if more than ~20% of a layer's requests pass through without transformation, the layer is accumulating sinkhole debt." },
  ]}
/>

<Recap lang="en">
N-tier layered architecture is the default starting structure for most applications because it directly addresses the most common force: the need to change infrastructure (switch databases, change frameworks, add a new delivery channel) without touching business logic, and the need to test business logic without a real database.

The four layers — presentation, application, domain, infrastructure — each own a distinct concern. The dependency rule inside a layered architecture says every layer depends only downward: presentation calls application, application calls domain, domain defines interfaces that infrastructure implements. No layer imports from a layer above it.

Strict layering enforces that each layer depends only on the layer immediately below. Relaxed layering permits skipping layers for pragmatic cases. Both are valid; strict layering gives stronger isolation at the cost of more discipline.

The architecture sinkhole is the failure mode: a layer that passes calls through without adding value. Sinkholes emerge when the domain layer is empty — when business rules have drifted into the repository or simply never been captured in domain objects. The fix is not to remove the layers; it is to restore the domain layer's responsibility. The next lesson examines exactly what that means: the difference between an anemic domain model (data bags + service logic) and a rich domain model (behavior with the data it guards).
</Recap>
