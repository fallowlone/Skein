---
concepts:
- docker-scanning
deepensInto: []
estMin: 15
lang: en
lessonType: topic
level: senior
order: 2
prereqs: []
slug: "02-image-scanning"
sources:
- https://trivy.dev/latest/docs/
- https://github.com/anchore/grype
- https://github.com/GoogleContainerTools/distroless
spiral: []
status: ready
summary: 'A fat base (ubuntu/node) carries 300+ OS packages and a long CVE tail you never call; distroless drops that to single digits. Trivy and Grype match package versions against vuln databases — they find known CVEs in your SBOM, not exploitability, so the lever is shrinking the base.'
title: 'Image scanning: why a fat base ships a CVE backlog, and what Trivy and Grype actually measure'
track: "docker"
unit: "07-security"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The breach post-mortem named a CVE nobody on the team had ever heard of: a remote-code-execution flaw in a logging library — Log4Shell — that the application did not even use directly. It had been pulled in transitively, baked into a `node:18` image months earlier, and shipped in production untouched because nobody had scanned the image after the base was chosen. The kicker came when an engineer finally ran a scanner against that same image: it reported over three hundred packages and dozens of high and critical CVEs, most in OS libraries the service never called — a curl here, a perl module there, a font library that existed only because the base image was a near-complete Debian userland. The vulnerable logging jar was one needle in a haystack the team had been carrying for no reason. The fix was not a frantic CVE hunt; it was rebuilding on a distroless base that shipped the runtime and almost nothing else, which dropped the package count from the three-hundreds into single digits and took the CVE backlog down with it.
</Hook>

<Crux>Scanners report known CVEs in your SBOM, not exploitability; a fat base ships 300+ packages, distroless drops it to single digits.</Crux>

<Explanation>

## What a scanner actually does — and does not

Before you can act on a scan report, you need to understand what the tool is actually measuring — because confusing "reported CVE count" with "risk to your running service" leads to either panic or false confidence.

A container vulnerability scanner like **Trivy** or **Grype** is, at its core, a join. It first builds a Software Bill of Materials (**SBOM**): it walks the image layers and enumerates every installed artifact it can identify — OS packages from the `dpkg`/`rpm`/`apk` database, plus language dependencies from lockfiles like `package-lock.json`, `go.sum`, or `Gemfile.lock` — each with a name and exact version. Then it matches those name+version pairs against a vulnerability database (the NVD plus distro-specific feeds like Debian and Alpine security trackers, GitHub advisories, and so on) and emits every CVE whose affected-version range includes what you shipped. That is the whole mechanism, and its limits follow directly from it: a scanner finds *known* vulnerabilities in *declared* versions. It does not know whether the vulnerable code path is ever reached at runtime, whether a fix has been backported by your distro (a frequent source of false positives when the upstream version string looks vulnerable but the distro patched it), or whether anything not in a recognized package manifest — a hand-`curl`'d binary, a vendored `.so` — is vulnerable at all. Scanning measures your *exposure surface as declared*, not your *exploitability*.

This is why the result is dominated by your base image, not your code. Ship on `ubuntu` or `node` and the scanner enumerates the entire base userland: a typical fat base carries **300-plus OS packages**, and at any given moment a slice of those have outstanding high or critical CVEs — not because your service is insecure, but because you are carrying a near-complete Linux distribution to run one binary. Most of those packages — shells, package managers, compression libraries, an interpreter or two — are never invoked by your application. They are pure attack surface and pure CVE backlog. The single most effective scan-result reducer is therefore not patching but *removing*: a **distroless** base (just glibc, CA certs, and your runtime, with no shell and no package manager) cuts the package count to single digits, and the CVE count collapses with it because there is simply almost nothing left to match against the database.

<Quiz
  id="02-image-scanning-quiz-1"
  lessonSlug="02-image-scanning"
  lang="en"
  question="A team's service code has zero known vulnerabilities, but Trivy reports 40 high/critical CVEs against the built image. Where do those CVEs almost certainly come from, and what is the highest-leverage fix?"
  choices={[
    { label: "From the fat base image's OS packages the app never calls — switching to a distroless/minimal base removes most of them by removing the packages", correct: true },
    { label: "From the application code; the scanner must be detecting a flaw the developers missed", misconception: "The premise says the service code has zero known vulnerabilities, and scanners report known CVEs in declared package versions. A clean app on a fat base still inherits the base's hundreds of OS packages and their CVEs — the result is dominated by the base, not the code." },
    { label: "They are all false positives and can be safely ignored with a blanket ignore rule", misconception: "Some may be backport false positives, but blanket-ignoring is reckless — the right move is to shrink the base so there is little to flag, then triage what remains, not suppress everything." },
    { label: "From the scanner being misconfigured; a correct config would report zero", misconception: "The CVEs are real matches against real packages in the image. Reconfiguring the scanner does not remove packages — only changing the base image (or patching) reduces what the scanner has to match against." },
  ]}
/>

<Inset kind="why" lang="en">
Why not just gate the pipeline on zero CVEs and be done? Because a hard zero-critical gate against a fat base is unmeetable theater — the base will always have *some* freshly disclosed CVE with no fix yet, so the gate either blocks every deploy or gets bypassed into uselessness. The senior policy separates two things: shrink the base so the count is naturally low (single digits, not hundreds), and gate on *fixable* criticals with a fixed version available, not on the raw count. A CVE with no upstream fix is a tracking item, not a deploy blocker; a critical with a one-line base bump available is. Counting is cheap; deciding which counts is the work.
</Inset>

## Reading severity, fixability, and the SBOM you keep

Scanners bucket findings by severity (`CRITICAL`/`HIGH`/`MEDIUM`/`LOW`, from CVSS) and, crucially, by *fixability* — whether a fixed version exists. The number that should drive action is "criticals and highs with a fix available," because those are the ones a base bump or dependency update actually resolves; the rest are tracked, not chased. Equally important is the artifact the scan produces along the way: the SBOM is worth keeping in its own right. Stored alongside the image (in formats like CycloneDX or SPDX, often only tens to a few hundred kilobytes of JSON), it lets you answer "are we affected by tomorrow's CVE?" by querying the inventory you already recorded, instead of re-scanning from scratch under incident pressure — which is exactly the question Log4Shell forced every team to answer overnight. Scanning is therefore two outputs: a prioritized, fixability-aware finding list that drives the base-shrink-then-patch loop, and a durable SBOM that turns the next zero-day from a frantic image archaeology dig into a database lookup.

<Quiz
  id="02-image-scanning-quiz-2"
  lessonSlug="02-image-scanning"
  lang="en"
  question="What is the most useful way to gate a CI pipeline on scanner results for an image built on a minimal base?"
  choices={[
    { label: "Fail on critical/high CVEs that have a fix available, and track (not block) ones with no upstream fix yet", correct: true },
    { label: "Fail on any CVE of any severity, so nothing vulnerable ever ships", misconception: "A blanket any-CVE gate is unmeetable — there is almost always some low/medium or unfixable finding, so the gate either blocks every deploy or gets disabled. Gating on fixable criticals/highs is both strict and achievable." },
    { label: "Fail only if the total CVE count exceeds 300", misconception: "A raw count threshold is meaningless — it rewards a fat base that happens to sit at 299 and punishes a clean image with one genuine fixable critical. Severity and fixability, not raw count, are what should gate." },
    { label: "Never gate; scanning is informational only and should not block deploys", misconception: "Without a gate, scanning becomes a dashboard nobody reads. A fixability-aware gate on criticals/highs is what turns the scan into an enforced control while staying meetable." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="A scanner builds an SBOM from image layers, joins it against vuln databases, and emits findings the pipeline gates on"
  hue="var(--d-security)"
  perRow={3}
  nodes={[
    { id: "image", label: "Image layers", sub: "fat base + app" },
    { id: "sbom", label: "SBOM build", sub: "name + version list" },
    { id: "db", label: "Vuln databases", sub: "NVD + distro feeds" },
    { id: "join", label: "Version join", sub: "match affected ranges" },
    { id: "findings", label: "Findings", sub: "severity + fixability" },
    { id: "gate", label: "CI gate", sub: "fail on fixable critical" },
  ]}
  edges={[
    { from: "image", to: "sbom", label: "enumerate" },
    { from: "sbom", to: "join", label: "versions" },
    { from: "db", to: "join", label: "ranges" },
    { from: "join", to: "findings", label: "matches" },
    { from: "findings", to: "gate", label: "prioritize" },
  ]}
  caption="The scanner measures declared exposure, not exploitability: it flags known CVEs in identified package versions. Shrinking the base shrinks the SBOM, which shrinks the join result — the highest-leverage lever sits before the scanner, at the FROM line."
/>

<KeyTakeaway>A scanner builds an SBOM from image layers and joins package name+version against vuln databases, so it reports known CVEs in declared versions — not whether the vulnerable code is reachable. Results are dominated by the base: a fat base ships 300+ OS packages your app never calls, each a CVE-matchable line; distroless cuts that to single digits and the CVE count collapses with it. Gate on fixable criticals/highs, not raw count, and keep the SBOM so the next zero-day is a database lookup, not an archaeology dig.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-image-scanning-retrieval"
  lang="en"
  questions={[
    { q: "Describe exactly what a scanner like Trivy or Grype does, and three things it cannot tell you.", a: "At its core a scanner is a join. It first builds an SBOM by walking the image layers and enumerating every identifiable installed artifact — OS packages from the dpkg/rpm/apk database and language dependencies from lockfiles — each with a name and exact version. Then it matches those name+version pairs against vulnerability databases (NVD plus distro and ecosystem feeds) and emits every CVE whose affected-version range includes what you shipped. Its limits follow directly: it cannot tell you whether the vulnerable code path is ever reached at runtime (it measures declared exposure, not exploitability); it cannot always tell that a distro has backported a fix even though the version string still looks vulnerable, producing false positives; and it cannot see anything outside a recognized package manifest, like a hand-curl'd binary or a vendored .so, so those go unflagged regardless of their real risk." },
    { q: "Why does the base image dominate scan results, and what is the highest-leverage way to reduce them?", a: "Because the scanner enumerates the entire image, including the whole base userland. A typical fat base (ubuntu, node) carries 300-plus OS packages — shells, package managers, interpreters, compression and font libraries — and at any time a slice of those have outstanding high/critical CVEs, even when your application code is clean. Most are never invoked by the service; they are pure attack surface and pure CVE backlog. So the highest-leverage reducer is not patching but removing: a distroless base ships just glibc, CA certs, and the runtime, with no shell or package manager, cutting the package count to single digits. The CVE count collapses with it because there is almost nothing left for the scanner to match against the database. The lever sits at the FROM line, before the scanner runs." },
  ]}
/>

<Recap lang="en">
A container scanner like Trivy or Grype is fundamentally a join: it builds a Software Bill of Materials by enumerating every identifiable installed artifact in the image layers — OS packages from dpkg/rpm/apk and language dependencies from lockfiles, each with a name and exact version — then matches those pairs against vulnerability databases (NVD plus distro and ecosystem feeds) and emits every CVE whose affected range covers a shipped version. Its limits fall straight out of the mechanism: it reports known CVEs in declared versions, not whether the vulnerable path is reachable, not always whether a distro backported the fix (a common false-positive source), and not anything outside a recognized manifest. That is why results are dominated by the base, not your code: a fat base like ubuntu or node carries 300-plus OS packages and a steady tail of high/critical CVEs, most in libraries the service never calls, so a clean app still inherits hundreds of findings. The single highest-leverage reducer is removal, not patching — a distroless base with just glibc, CA certs, and the runtime drops the package count to single digits and the CVE count with it. Gate the pipeline on fixable criticals and highs rather than a raw count, treat unfixable CVEs as tracking items, and keep the SBOM (CycloneDX or SPDX, tens to hundreds of KB) so the next zero-day — the Log4Shell question every team had to answer overnight — becomes a query against an inventory you already recorded instead of a frantic re-scan under incident pressure. Now when you see a scan report with hundreds of findings, your first question is not "which CVE do I patch?" but "what base image am I on, and can I switch to distroless today?"
</Recap>
