---
concepts:
- docker-cgroups
deepensInto: []
estMin: 17
lang: en
lessonType: topic
level: senior
order: 2
prereqs: []
slug: "02-cgroups"
sources:
- https://docs.kernel.org/admin-guide/cgroup-v2.html
- https://man7.org/linux/man-pages/man7/cgroups.7.html
- https://github.com/opencontainers/runtime-spec/blob/main/config-linux.md
spiral: []
status: ready
summary: "If namespaces decide what a container sees, cgroups decide what it can consume. cgroup v2 controllers meter CPU, memory, pids, and IO. memory.max is a hard wall — cross it and the kernel OOM-kills inside the cgroup; CPU is a quota, not a wall."
title: "Cgroups: the resource budget the kernel actually enforces"
track: "docker"
unit: "02-runtime-internals"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The service had been stable for months, then started dying under no extra traffic — exit code 137, a few times an hour, always mid-request. 137 is 128 + 9: SIGKILL. The dashboards showed the JVM heap nowhere near its `-Xmx`, so the team chased a phantom leak for a week. The truth was in `dmesg`: `Memory cgroup out of memory: Killed process`. The container's `memory.max` was 512 MiB. The JVM, told only about the *host's* 64 GiB, sized its off-heap buffers, metaspace, and thread stacks for a machine it wasn't running on — and the sum crossed 512 MiB. The kernel does not negotiate at that line: when a cgroup's charged memory hits `memory.max` and reclaim can't free enough, the in-cgroup OOM killer fires and SIGKILLs a process *inside that cgroup*, not on the host. The fix wasn't more memory; it was telling the runtime the truth (`-XX:MaxRAMPercentage` reading the cgroup limit). The container could always see the host's RAM. It just wasn't allowed to use it.
</Hook>

<Crux>Namespaces fence what a process sees; cgroups fence what it consumes. A cgroup is a kernel-enforced budget, and memory.max is a hard wall.</Crux>

<Explanation>

## Controllers: one tree, metered resources

A control group (cgroup) is a node in a tree of processes that the kernel accounts and limits per **controller**. Modern systems run **cgroup v2**, a single unified hierarchy (one mount at `/sys/fs/cgroup`) replacing v1's separate per-controller trees. The controllers a container runtime cares about:

- **cpu** — `cpu.max` is `"QUOTA PERIOD"` in microseconds: `"50000 100000"` means 50 ms of CPU per 100 ms window = 0.5 cores. `cpu.weight` (default 100) sets proportional share under contention.
- **memory** — `memory.max` is the hard limit in bytes; `memory.high` is a soft throttle that triggers reclaim before the wall; `memory.low` protects a minimum from reclaim.
- **pids** — `pids.max` caps the number of processes/threads, the direct guard against a fork bomb or a leaking entrypoint (see the previous lesson).
- **io** — `io.max` rate-limits block IO (BPS and IOPS) per device.

`runc` writes the container's limits from the OCI `config.json` `linux.resources` block into these files at startup, then moves the container's PID 1 into the cgroup so every descendant is accounted together. `docker run -m 512m --cpus=0.5 --pids-limit=200` becomes `memory.max=536870912`, `cpu.max="50000 100000"`, `pids.max=200`. Reading `memory.current` or `cpu.stat` from inside or outside the cgroup is how monitoring sees real consumption — not what `free` reports, which (pre-cgroup-aware tooling) still shows the host.

```text
/sys/fs/cgroup/system.slice/docker-<id>.scope/
├── cpu.max          50000 100000          # 0.5 core
├── cpu.stat         usage_usec, nr_throttled, throttled_usec
├── memory.max       536870912             # 512 MiB hard wall
├── memory.high      483183820             # ~90%, soft throttle
├── memory.current   421330944             # live charge
├── memory.events    oom 0  oom_kill 0     # increments on each OOM-kill
└── pids.max         200
```

## CPU is a quota; memory is a wall

The two limits fail in *opposite* ways, and conflating them is the classic mistake. **CPU is throttled, never killed**: if a container exceeds its `cpu.max` quota within a period, the kernel simply stops scheduling it until the next period — latency spikes, `nr_throttled` climbs, but the process lives. **Memory is a hard wall**: cross `memory.max` and there is no "throttle harder" — the kernel runs reclaim, and if it can't free enough, the cgroup-scoped OOM killer SIGKILLs a process *within the cgroup*. That is the 137 from the Hook. `memory.high` exists precisely to soften this: set below `memory.max`, it throttles allocations and forces reclaim early, turning a hard kill into back-pressure — but it is not a guarantee, only `memory.max` is the wall.

<Quiz
  id="02-cgroups-quiz-1"
  lessonSlug="02-cgroups"
  lang="en"
  question="Container A is pinned at its cpu.max quota and getting slow; container B just got SIGKILLed with exit 137. Same node. What distinguishes the two failure modes?"
  choices={[
    { label: "CPU over-limit is throttled — the kernel pauses scheduling until the next period, so A stays alive but slow; memory over-limit hits a hard wall, so B was OOM-killed inside its cgroup", correct: true },
    { label: "Both are OOM kills; A just hasn't crossed its memory limit yet", misconception: "A's symptom is CPU throttling (nr_throttled rising), not memory pressure — exceeding cpu.max never kills a process, it only delays its scheduling. The two controllers fail differently." },
    { label: "B was killed by the host OOM killer for using too much host RAM, unrelated to its cgroup", misconception: "Exit 137 with a per-cgroup memory.max points to the cgroup-scoped OOM killer firing inside B's own cgroup when its charge hit memory.max — it does not require host-wide memory pressure." },
    { label: "A is throttled because its pids.max was reached, blocking new threads", misconception: "pids.max exhaustion blocks fork/clone with EAGAIN; it does not produce CPU throttling. A's slowdown is cpu.max quota throttling, a distinct controller." },
  ]}
/>

<Inset kind="why" lang="en">
Why does the JVM (or Go, or Node) misbehave here at all? Historically, language runtimes read `/proc/cpuinfo` and `sysconfig` for "how many cores / how much RAM" — which report the host, because that file is not namespaced the way the cgroup limit is. A runtime then sizes thread pools, GC heaps, and buffer caches for a 64-core / 64-GiB box while living in a 0.5-core / 512-MiB cgroup. Modern runtimes are cgroup-aware (Java's `UseContainerSupport`, on by default since JDK 10; Go's `GOMAXPROCS` auto-tuning in 1.25), but the failure mode persists wherever an old runtime, an unaware library, or a hand-set flag overrides the limit. The cgroup enforces the budget regardless; the runtime just has to be told the budget exists.
</Inset>

## Why this is isolation, not virtualization

A cgroup does not give a container its *own* CPU or *own* RAM — it gives it a metered slice of the host's. There is no second pool of memory behind a hypervisor; `memory.current` charges the same physical pages the host kernel manages, and an unlimited container (`memory.max=max`) competes for all of host RAM. This is the whole point of containers being cheap: no duplicated kernel, no pre-reserved guest RAM, near-zero overhead — and the whole reason they are not VMs. The kernel is shared, so the *budget*, not a *boundary*, is what stands between a noisy neighbor and the rest of the node. Set the budgets, or a single container with no `memory.max` can drive the host into global OOM and take down every other container on the box.

<Quiz
  id="02-cgroups-quiz-2"
  lessonSlug="02-cgroups"
  lang="en"
  question="A container runs with no memory limit (memory.max=max) and develops a slow leak. What happens to the OTHER containers on the same node, and why?"
  choices={[
    { label: "They are at risk: with no cap the leaking container charges host physical pages until the host-wide OOM killer fires, which may kill any process on the node — cgroups meter a shared pool, they do not give each container a private one", correct: true },
    { label: "They are fully protected — each container has its own isolated memory pool that the leak cannot touch", misconception: "Containers share the host's physical memory; a cgroup meters a slice of that shared pool, it does not allocate a private pool. An uncapped leaker competes for the same pages every other container uses." },
    { label: "Nothing happens because the kernel automatically caps every container at an equal share", misconception: "There is no automatic equal-share for memory — without memory.max a cgroup is unlimited and can consume all host RAM. Equal proportional sharing under contention is a CPU (cpu.weight) concept, not a memory guarantee." },
    { label: "The leaking container is throttled like CPU until memory frees up", misconception: "Memory has no throttle-to-zero like CPU; without memory.high/max set, allocations are not slowed. When host RAM runs out the response is OOM-kill, not throttling." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="A process's resource requests are metered by cgroup v2 controllers, each with its own failure response"
  hue="var(--d-infra)"
  perRow={3}
  nodes={[
    { id: "proc", label: "Container process", sub: "charged to its cgroup" },
    { id: "cpu", label: "cpu.max", sub: "quota / period" },
    { id: "mem", label: "memory.max", sub: "hard byte wall" },
    { id: "pids", label: "pids.max", sub: "process count cap" },
    { id: "throttle", label: "Throttle", sub: "pause until next period" },
    { id: "oom", label: "OOM-kill", sub: "SIGKILL → exit 137" },
  ]}
  edges={[
    { from: "proc", to: "cpu", label: "CPU time" },
    { from: "proc", to: "mem", label: "page charge" },
    { from: "proc", to: "pids", label: "fork/clone" },
    { from: "cpu", to: "throttle", label: "over quota" },
    { from: "mem", to: "oom", label: "over max, reclaim fails" },
  ]}
  caption="Same host RAM and CPU underneath — a cgroup meters a slice, it does not create a private pool. CPU over-limit throttles and survives; memory over-limit kills inside the cgroup."
/>

<KeyTakeaway>Cgroups are the enforcement half of containerization: cgroup v2's unified hierarchy meters CPU (cpu.max quota), memory (memory.max hard wall, memory.high soft throttle), pids, and IO per controller, written by runc from the OCI config. The failure modes diverge — CPU over-quota is throttled and survives, memory over-max triggers the cgroup-scoped OOM killer (exit 137). A cgroup meters a slice of the shared host resources, it does not create a private pool, so an uncapped container is a node-wide risk. Tell the runtime the budget or it will size itself for the host and die.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-cgroups-retrieval"
  lang="en"
  questions={[
    { q: "Contrast how the cpu and memory controllers respond when a container exceeds its limit, and give the concrete kernel mechanism for each.", a: "CPU is a quota, not a wall: cpu.max is 'QUOTA PERIOD' in microseconds (e.g. 50000 100000 = 0.5 core), and exceeding it makes the kernel stop scheduling the cgroup until the next period — nr_throttled and throttled_usec rise in cpu.stat, latency spikes, but the process always survives. Memory is a hard wall: when charged memory reaches memory.max and reclaim cannot free enough, the cgroup-scoped OOM killer SIGKILLs a process inside that cgroup, producing exit code 137 (128+9). memory.high is a softer line below memory.max that forces reclaim and throttles allocations early, converting a kill into back-pressure, but only memory.max is the guaranteed ceiling." },
    { q: "Explain why a cgroup is isolation rather than virtualization, and what goes wrong if a container runs with no memory.max.", a: "A cgroup does not give a container its own CPU or RAM; it meters a slice of the host's shared resources. memory.current charges the same physical pages the host kernel manages — there is no hypervisor-backed second pool. That is why containers are cheap (no duplicated kernel, no pre-reserved guest RAM) and why they are not VMs: the kernel is shared, so a budget, not a hard boundary, separates neighbors. With memory.max=max a container is unlimited and competes for all host RAM; a leak there drives the host into global OOM, whose killer can take out any process on the node, not just the offender. Setting per-container limits is what keeps one noisy container from collapsing the whole host." },
  ]}
/>

<Recap lang="en">
If namespaces decide what a container sees, cgroups decide what it is allowed to consume — and the kernel enforces it. A control group is a node in a process tree accounted and limited per controller; modern systems use cgroup v2, a single unified hierarchy under /sys/fs/cgroup. The controllers that matter for containers are cpu (cpu.max as a quota/period pair, cpu.weight for proportional share), memory (memory.max as the hard byte wall, memory.high as a soft throttle that reclaims early, memory.low to protect a floor), pids (pids.max against fork bombs and zombie leaks), and io (io.max for block IO). runc writes these from the OCI config's linux.resources and places the container's PID 1 into the cgroup so all descendants are charged together. The two headline limits fail oppositely: exceed cpu.max and you are throttled — paused until the next period, slow but alive, with nr_throttled climbing; exceed memory.max and the cgroup-scoped OOM killer SIGKILLs a process inside the cgroup, surfacing as exit 137. The classic outage is a runtime that reads the host's cores and RAM instead of its cgroup limit, sizes itself for a machine it isn't on, and crosses memory.max — fixed not with more memory but by making the runtime cgroup-aware. And because a cgroup meters a slice of shared host resources rather than carving out a private pool, an uncapped container is a node-wide hazard: a leak with no memory.max can drive the host into global OOM and kill unrelated containers. Set the budgets; the kernel will hold the line exactly where you draw it.
</Recap>
