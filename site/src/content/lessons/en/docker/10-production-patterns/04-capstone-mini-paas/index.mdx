---
concepts:
- docker-capstone-paas
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: senior
order: 4
prereqs: []
slug: "04-capstone-mini-paas"
sources:
- https://docs.docker.com/build/building/multi-stage/
- https://distribution.github.io/distribution/
- https://kubernetes.io/docs/concepts/workloads/controllers/deployment/
spiral: []
status: ready
summary: 'A mini-PaaS ties the whole track together: a registry pins immutable digests, the build ships slim distroless artifacts, run enforces resource limits, honest health gates traffic, and a rolling deploy with maxUnavailable and preStop swaps versions with zero dropped requests.'
title: 'Capstone: a mini-PaaS — registry, build, run, health, and zero-downtime rollout'
track: "docker"
unit: "10-production-patterns"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The rollback didn't roll back. An incident had just been "fixed" by redeploying the previous version — `myapp:latest` from before the bad change — but the new pods came up running the *broken* code anyway, and the outage continued for another twenty minutes while everyone stared at a deploy that said success. The cause was a mutable tag: `latest` had been overwritten by the bad build, so "redeploy latest" pulled the bug. Worse, half the surviving old pods were still on the previous `latest` from cache, so the cluster was running two different versions under one tag and nobody could tell which pod had which code. The fix was a rule that turned a fragile workflow into a real platform: every image is referenced by its immutable **digest** (`myapp@sha256:...`), never a moving tag. This capstone assembles the whole track — registry, build, run, health, rollout — into the smallest system that has this property and the four others a production platform needs: immutability, bounded resources, honest health, and a rollout that never drops a request.
</Hook>

<Crux>A mini-PaaS is five contracts: a digest-pinned registry, slim build, bounded run, honest health, and a rollout that drops zero requests.</Crux>

<Explanation>

## The five stages, as one pipeline

Before reading the five contracts below, notice what the hook incident actually was: not a single missing feature, but a missing *composition*. Each contract individually looked acceptable; together they were missing the one that makes the others meaningful.

Everything in this unit is a stage of the same machine. A minimal production platform — your mini-PaaS — is the composition of five contracts, each of which fails the others if it is wrong.

**1 · Registry — immutability.** The registry is the source of truth for artifacts, and its one non-negotiable rule is that deployments reference an **immutable digest**, not a mutable tag. `myapp:latest` can be overwritten; `myapp@sha256:9f2c...` cannot — the same digest always pulls the exact same bytes. Pin digests in the deploy manifest and a rollback genuinely returns to known-good code, two pods can never disagree about what `latest` means, and an image-pull is reproducible across nodes and time. Tags are convenient labels for humans; digests are the contract for machines.

**2 · Build — slim and reproducible.** A multi-stage build (from the slimming lesson) compiles in a toolchain stage and ships a ~25 MB distroless artifact, so the new version pulls onto cold nodes in ~2 s during the rollout instead of stalling it for 90 s. The build is pinned (base image by digest, dependencies by lockfile) so the digest is deterministic — the same source produces the same image.

**3 · Run — bounded resources.** Every container runs with a memory limit (from the OOM lesson) so one bad release cannot OOM the node and take down the other version mid-rollout, plus a cgroup-aware runtime target (GOMEMLIMIT / MaxRAMPercentage) so the runtime stays under the wall. Bounded resources are what make it safe to run old and new versions side by side during a rollout.

**4 · Health — honest gating.** A readiness probe that runs a real query (from the health lesson) gates traffic: a new pod receives no requests until it is genuinely ready, and a sick pod drains. This is the signal the rollout controller waits on — without honest readiness, "rollout complete" is a lie and traffic hits a pod that cannot serve.

<Quiz
  id="04-capstone-mini-paas-quiz-1"
  lessonSlug="04-capstone-mini-paas"
  lang="en"
  question="A rollback redeploys myapp:latest yet the pods still run the broken code, and surviving old pods run a different build under the same tag. What platform rule prevents this class of bug?"
  choices={[
    { label: "Reference images by immutable digest (myapp@sha256:...) instead of a mutable tag, so a rollback returns to exact known-good bytes and no two pods can disagree about what a tag means", correct: true },
    { label: "Pull images more aggressively with imagePullPolicy Always", misconception: "Always re-pulling a mutable latest just guarantees you fetch whatever overwrote it — including the bad build — and does nothing about two pods cached on different versions of the same tag. The fix is immutability via digest pinning, not pull frequency." },
    { label: "Add more replicas so the broken ones are outvoted", misconception: "Replica count does not change which code a tag resolves to; if latest points at the broken build, more replicas just run more broken pods. Digest pinning is what makes a rollback deterministic." },
    { label: "Restart the registry to clear its tag cache", misconception: "The tag is not stale cache — it was genuinely overwritten to point at the bad image. Restarting the registry cannot recover the old mapping; only referencing the immutable digest gives a stable, reproducible deployment." },
  ]}
/>

## The rollout — composing it into zero downtime

The fifth contract is the rolling deploy, and it only works because the other four hold. A rolling update replaces pods incrementally under two knobs: **maxUnavailable** (how many existing pods may be down at once — set low, e.g. 0 or 25%, to preserve capacity) and **maxSurge** (how many extra pods may be created above the desired count, e.g. 25%, to add new-version capacity before removing old).

```yaml
strategy:
  type: RollingUpdate
  rollingUpdate:
    maxUnavailable: 0      # never drop below desired capacity
    maxSurge: 1            # bring up one new pod before retiring an old one
```

The sequence ties the whole track together. The controller creates a new pod from the **digest-pinned slim image** (stages 1–2); it pulls in ~2 s and starts under its **memory limit** (stage 3); the controller waits for the new pod's **honest readiness probe** to pass before sending it traffic (stage 4); only then does it terminate an old pod, which runs its **preStop → SIGTERM → drain** lifecycle (from the health lesson) so its in-flight requests finish; and it repeats until every pod is the new version — with `maxUnavailable: 0` capacity never dips, and with honest readiness no traffic ever lands on a not-ready pod. Remove any one contract and the rollout breaks: a mutable tag makes rollback unreliable, a fat image stalls the surge, a missing memory limit lets the new version OOM the node hosting the old one, a dishonest probe routes traffic to a dead pod, and a missing preStop drops the in-flight requests of the pods being retired. That is the lesson of the whole unit: production containers are not five independent best practices — they are one pipeline where each stage is load-bearing for the next.

<Quiz
  id="04-capstone-mini-paas-quiz-2"
  lessonSlug="04-capstone-mini-paas"
  lang="en"
  question="In a maxUnavailable:0, maxSurge:1 rolling update, what does the controller wait on before terminating an old pod, and why does that gate matter?"
  choices={[
    { label: "It waits for the new pod's honest readiness probe to pass, so capacity never dips below desired and traffic is only shifted once the new version can genuinely serve", correct: true },
    { label: "It waits a fixed timer regardless of the new pod's state", misconception: "A fixed timer is exactly the bug honest readiness avoids: the new pod might still be warming up when the old one is retired, dropping capacity and routing to a not-ready pod. The controller gates on the readiness probe passing, not a clock." },
    { label: "It waits for the old pod's liveness probe to fail", misconception: "The old pod is healthy; the controller terminates it deliberately, it does not wait for liveness to fail. The gate is the NEW pod becoming ready, which is what preserves capacity during the surge." },
    { label: "It terminates the old pod immediately and creates the new one afterward", misconception: "That order would drop below desired capacity (maxUnavailable:0 forbids it) and risk an outage window. maxSurge:1 means the new pod comes up and passes readiness first, then the old pod is retired." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="A mini-PaaS pipeline flows from a digest-pinned registry through build, resource-bounded run and honest health into a zero-downtime rollout"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "registry", label: "Registry", sub: "digest-pinned, immutable" },
    { id: "build", label: "Build", sub: "multi-stage, ~25 MB" },
    { id: "run", label: "Run", sub: "memory limit + GOMEMLIMIT" },
    { id: "health", label: "Health", sub: "honest readiness gate" },
    { id: "surge", label: "maxSurge new pod", sub: "ready before retire" },
    { id: "drain", label: "Old pod drains", sub: "preStop → SIGTERM" },
  ]}
  edges={[
    { from: "registry", to: "build", label: "pin digest" },
    { from: "build", to: "run", label: "fast cold pull" },
    { from: "run", to: "health", label: "bounded, stays up" },
    { from: "health", to: "surge", label: "ready passes" },
    { from: "surge", to: "drain", label: "then retire old" },
  ]}
  caption="Each stage is load-bearing for the next: immutable digests make rollback real, slim images keep the surge fast, memory limits let old and new run side by side, honest readiness gates the swap, and preStop drains the retiring pod — so the rollout drops zero requests."
/>

<KeyTakeaway>A mini-PaaS is the whole unit composed into one pipeline: a registry that references immutable digests (not mutable tags) so rollback returns to exact known-good bytes; a multi-stage slim build that pulls in ~2 s instead of stalling the rollout; a resource-bounded run so one release cannot OOM the node hosting the other version; honest readiness that gates traffic to genuinely-ready pods; and a rolling deploy with maxUnavailable:0 / maxSurge that brings up a ready new pod before draining an old one through preStop → SIGTERM. Each contract is load-bearing — remove any one and the rollout drops requests, ships the wrong code, or takes down the node — which is the point: production containers are one pipeline, not five independent tips.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-capstone-mini-paas-retrieval"
  lang="en"
  questions={[
    { q: "Why must a deployment reference an image by digest rather than a tag, and what specific failures does digest pinning prevent?", a: "A tag like latest is a mutable pointer — it can be overwritten to point at a different image — whereas a digest (myapp@sha256:...) is content-addressed and immutable: the same digest always resolves to the exact same bytes. Pinning the digest prevents three failures. First, a broken rollback: if you redeploy a tag that was overwritten by the bad build, you re-ship the bug; the digest returns to known-good code deterministically. Second, version skew: two pods can cache different images under the same tag, so the cluster runs mismatched code under one label and nobody can tell which pod runs what; a digest makes every pod provably identical. Third, non-reproducible pulls: the same manifest on different nodes or at different times can pull different bytes from a moving tag, but a digest is reproducible across nodes and time. Tags are human labels; digests are the machine contract." },
    { q: "Walk a zero-downtime rolling update (maxUnavailable:0, maxSurge:1) and explain how each of the unit's five contracts is load-bearing for it.", a: "The controller creates a new pod from the digest-pinned slim image (registry immutability + build slimming — it pulls in ~2 s so the surge is not stalled), the pod starts under its memory limit with a cgroup-aware runtime target (run bounding — so the new version cannot OOM the node hosting the old one), and the controller waits for the new pod's honest readiness probe to pass before routing traffic to it (health gating — so traffic only reaches a pod that can genuinely serve). With maxUnavailable:0 capacity never drops below desired, and maxSurge:1 means a new pod is ready before any old pod is retired. Only then does the controller terminate an old pod, which runs preStop → SIGTERM → drain (lifecycle — its in-flight requests finish before exit), and it repeats until all pods are the new version. Remove any contract and it breaks: a mutable tag makes rollback unreliable, a fat image stalls the surge, a missing limit lets the new version OOM the node, a dishonest probe routes to a dead pod, and no preStop drops the retiring pods' in-flight requests." },
  ]}
/>

<Recap lang="en">
This capstone assembles the unit into the smallest system that behaves like a production platform: a mini-PaaS that is five contracts composed into one pipeline, each load-bearing for the next. The registry's rule is immutability — reference images by digest, not by a mutable tag, so a rollback returns to exact known-good bytes, no two pods disagree about what a tag means, and pulls are reproducible across nodes and time; the opening incident, where redeploying an overwritten latest re-shipped the bug, is precisely what digest pinning kills. The build is multi-stage and slim, shipping a ~25 MB distroless artifact that pulls onto cold nodes in ~2 s so the rollout's surge is fast rather than stalled, and pinned so the same source yields the same digest. The run is resource-bounded — a memory limit plus a cgroup-aware target like GOMEMLIMIT — so a bad release cannot OOM the node that is hosting the other version mid-rollout, which is what makes it safe to run two versions side by side. Health is honest readiness that runs a real query, gating traffic so a new pod takes no requests until it can genuinely serve and a sick pod drains; this is the signal the rollout controller waits on. And the rolling deploy ties it together: with maxUnavailable:0 capacity never dips and maxSurge brings up a ready new pod before retiring an old one, which then drains through preStop → SIGTERM so its in-flight requests finish. Pull any single thread and the whole rollout unravels — mutable tags break rollback, fat images stall the surge, missing limits crash the node, dishonest probes misroute, and no preStop drops requests. The unit's thesis is exactly this composition: production containers are bounded resources, honest health, slim images, and a graceful lifecycle, working as one system under an orchestrator. Now when you review a Dockerfile, a Kubernetes manifest, or a deploy runbook, you can read it as a pipeline: does each stage hold the contract that the next stage depends on?
</Recap>
