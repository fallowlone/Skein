---
concepts:
- docker-logs-events
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: senior
order: 2
prereqs: []
slug: "02-logs-and-events"
sources:
- https://docs.docker.com/reference/cli/docker/container/logs/
- https://docs.docker.com/engine/logging/configure/
- https://docs.docker.com/reference/cli/docker/system/events/
spiral: []
status: ready
summary: 'docker logs replays whatever the logging driver captured from stdout/stderr — the default json-file driver''s max-size and max-file silently rotate old lines away. docker events is the daemon''s real-time stream: the OOM kill and restart no app log recorded.'
title: 'docker logs and events: the rotation that eats your evidence and the daemon stream that kept it'
track: "docker"
unit: "09-debugging-containers"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The incident review wanted the logs from 02:00, when the worker started dropping jobs. The engineer ran `docker logs worker --since 02:00 --until 02:30` and got back exactly nothing for that window — the oldest line was 02:47. Not a clock problem: the worker had logged a stack trace per dropped job at thousands of lines a second, and the default json-file driver was configured with `max-size=10m, max-file=3`. Thirty megabytes of ring buffer at that volume holds about ninety seconds. By the time anyone looked, the rotation had already chewed through the evidence and replaced it with newer noise. The root-cause line existed, was captured, and was deleted by the logging driver doing exactly what it was told. What *did* survive was in a place nobody had checked: `docker events --since 02:00` still held the daemon's own record — `oom`, then `die`, then `start` — the kernel killing the worker and the restart policy reviving it, a story the application logs could never have told because the app was dead before it could write it.
</Hook>

<Crux>docker logs replays only what the driver holds — json-file rotation silently eats old lines. docker events is what the daemon did to it.</Crux>

<Explanation>

## logs is a replay of the driver's buffer, not a file you own

`docker logs` does not read your application's log file. It replays whatever the container's **logging driver** captured from PID 1's stdout and stderr. With the default `json-file` driver, every line is wrapped as JSON on disk under `/var/lib/docker/containers/<id>/<id>-json.log`, and — critically — that file is **rotated by the driver's own settings**, which most teams never tune:

```bash
# Default driver: json-file. Rotation is OFF unless you set it,
# but production images and daemon.json usually set a small cap:
docker run --log-driver json-file \
  --log-opt max-size=10m --log-opt max-file=3 worker
# => at most 3 files × 10m = 30m of history. A chatty app burns
#    through that in seconds, and docker logs can never show more.

docker logs worker --since 02:00 --until 02:30 --timestamps
docker logs worker --tail 200 -f          # follow the live tail
```

The trap is that the rotation is invisible from the `docker logs` side: there is no error, no gap marker — the oldest line is simply whatever survived, and `--since` for a window the driver has already discarded returns empty as if nothing happened. Two more facts that bite seniors: (1) only stdout/stderr is captured — an app logging to a *file inside the container* produces nothing for `docker logs` at all; and (2) some drivers are not even replayable — under `journald` or `syslog` you query the host's journal, and under `splunk`/`gelf` `docker logs` may refuse entirely because the bytes left the host. The lesson: treat `docker logs` as a small, lossy ring buffer for live tailing, and ship logs off the node (driver → aggregator) for anything you must keep.

<Quiz
  id="02-logs-and-events-quiz-1"
  lessonSlug="02-logs-and-events"
  lang="en"
  question="docker logs worker --since 02:00 --until 02:30 returns nothing, but the worker was definitely logging heavily then. The driver is json-file with max-size=10m, max-file=3. What happened to the evidence?"
  choices={[
    { label: "The json-file driver rotated those lines out — 30m of buffer at that volume only held minutes, so the 02:00 window was already overwritten with no error or gap marker", correct: true },
    { label: "docker logs has a bug that loses lines older than 30 minutes", misconception: "There is no time-based eviction bug; rotation is size-based and explicit. 30m of buffer (3 × 10m) simply cannot hold a high-volume window, so old lines are discarded — exactly as the max-size/max-file config instructs." },
    { label: "The --since flag silently failed because the container restarted", misconception: "--since filters by timestamp against whatever the driver still holds; a restart does not break it. The window returned empty because those bytes were rotated away, not because the flag malfunctioned." },
    { label: "The app must have logged to a file, so docker logs saw nothing", misconception: "That is a real failure mode for docker logs in general, but here the worker was confirmed logging to stdout heavily and recent lines do appear — the missing window is rotation loss, not a stdout/file mismatch." },
  ]}
/>

## events is the daemon narrating itself in real time

When the application could not log the thing that killed it — because it was the *engine* or the *kernel* that acted — `docker events` is where the story lives. It is a live stream of every state transition the daemon performs, on containers, images, volumes, and networks, with `--since`/`--until` for replay over the daemon's retained window:

```bash
docker events --since 02:00 --until 02:30 \
  --filter container=worker --filter event=oom --filter event=die
# 02:14:09  container oom    worker
# 02:14:09  container die    worker  (exitCode=137)
# 02:14:11  container start  worker
```

The `oom → die(137) → start` triplet is the signature of a memory crash-loop, and **none of it appears in `docker logs`** — the app was SIGKILLed mid-breath, with no chance to write a farewell. Events surface what is otherwise invisible: OOM kills, health-status flips (`health_status: unhealthy`), restart-policy actions, `kill`/`stop`/`destroy`, and volume mounts. Pair it with `docker stats` for live cgroup memory/CPU and you can watch a container climb toward its limit and get reaped in real time. The mental model: `logs` is what the *workload* said; `events` is what the *engine* did to it — and on the worst nights only the second one was ever written down.

<Quiz
  id="02-logs-and-events-quiz-2"
  lessonSlug="02-logs-and-events"
  lang="en"
  question="A container is crash-looping on memory but docker logs shows only a clean startup banner each cycle — no error, no stack trace. Where is the actual failure recorded?"
  choices={[
    { label: "In docker events — the oom and die(137) transitions are logged by the daemon; the app was SIGKILLed before it could write anything, so logs only ever shows the next clean start", correct: true },
    { label: "In docker logs with a higher --tail value", misconception: "No tail depth helps when the bytes were never written: an OOM-kill is a SIGKILL the app cannot catch, so it produces no log line at all. The record exists only in the daemon's event stream, not in the app's stdout." },
    { label: "Nowhere — an OOM kill leaves no trace until you add memory monitoring", misconception: "The daemon records every oom and die with exit code in docker events, and inspect preserves OOMKilled/ExitCode. The trace is there; you were looking in logs, which the killed app could not write to." },
    { label: "Only in the host kernel ring buffer via dmesg", misconception: "dmesg does show the kernel OOM-killer's verdict, but docker events also captures the oom and die(137) at the container level with the container name attached — the more direct place to look first, and replayable with --since." },
  ]}
/>

<Inset kind="why" lang="en">
Why does Docker split the story across two streams instead of one log? Because they have different authors and different survival guarantees. logs is the workload narrating itself — rich, but only as durable as the driver's ring buffer and only written while the process lives. events is the daemon narrating its actions on the container — sparse, but it captures the moments the workload cannot: the SIGKILL, the restart, the health flip. The split is the point: when the app dies mid-sentence, the daemon's pen keeps moving.
</Inset>

</Explanation>

<FlowDiagram
  label="An incident question routes to the stream that actually recorded the answer"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "q", label: "What happened?", sub: "pick the stream" },
    { id: "said", label: "What the app said", sub: "stdout/stderr" },
    { id: "did", label: "What the engine did", sub: "oom/die/start" },
    { id: "logs", label: "docker logs", sub: "driver replay" },
    { id: "rot", label: "json-file rotation", sub: "max-size×max-file = window" },
    { id: "events", label: "docker events", sub: "--since replayable" },
  ]}
  edges={[
    { from: "q", to: "said", label: "app msg" },
    { from: "q", to: "did", label: "kill/restart" },
    { from: "said", to: "logs", label: "replay" },
    { from: "logs", to: "rot", label: "old lines gone" },
    { from: "did", to: "events", label: "daemon log" },
  ]}
  caption="docker logs replays only what the driver still holds; json-file's max-size × max-file is the entire history window, and a chatty app rotates the evidence away with no gap marker. docker events records the oom/die/start the app could never write."
/>

<KeyTakeaway>docker logs is not your app's log file — it replays whatever the logging driver captured from stdout/stderr, and the default json-file driver's max-size × max-file is a small ring buffer that silently rotates old lines into oblivion with no gap marker. Logs to a file inside the container produce nothing. When the engine or kernel did the killing — OOM, restart, health flip — only docker events recorded it, because the SIGKILLed app could not write its own ending. logs is what the workload said; events is what the engine did to it.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-logs-and-events-retrieval"
  lang="en"
  questions={[
    { q: "Why can docker logs return empty for a window when the app was definitely logging then, and what configuration controls it?", a: "docker logs replays whatever the container's logging driver captured from PID 1's stdout/stderr — not your application's own log file. With the default json-file driver, lines are stored under /var/lib/docker/containers/<id>/<id>-json.log and rotated by max-size and max-file log-opts: the total history is at most max-file × max-size (e.g. 3 × 10m = 30m). A high-volume app burns through that in seconds to minutes, so --since for a window the driver has already discarded returns empty with no error and no gap marker — the oldest surviving line is simply whatever fit. Two more loss modes: an app logging to a file inside the container produces nothing for docker logs at all (only stdout/stderr is captured), and drivers like journald/syslog/splunk/gelf send bytes off-host so docker logs queries elsewhere or refuses. Treat docker logs as a small lossy ring buffer for live tailing and ship logs to an aggregator for anything you must keep." },
    { q: "What does docker events capture that docker logs structurally cannot, and why does it matter in a crash-loop?", a: "docker events is the daemon's real-time stream of state transitions it performs on containers/images/volumes/networks, replayable with --since/--until over the daemon's retained window. It records oom, die (with exit code), start, kill, stop, destroy, and health_status flips. In a memory crash-loop you see the oom → die(exitCode=137) → start triplet — and none of it appears in docker logs, because an OOM kill is a SIGKILL the app cannot catch, so it writes no farewell line and the next cycle's logs only show a clean startup banner. logs is what the workload said (and only while it lived); events is what the engine did to the container, capturing exactly the moments the workload could not. Pairing docker events with docker stats lets you watch a container climb toward its memory limit and get reaped live." },
  ]}
/>

<Recap lang="en">
docker logs is not a window onto your application's log file — it replays whatever the container's logging driver captured from PID 1's stdout and stderr. With the default json-file driver those lines live in /var/lib/docker/containers/<id>/ and are rotated by max-size and max-file, so the entire history you can ever retrieve is max-file × max-size; a chatty app burns through a 30m buffer in seconds, and --since for an already-rotated window returns empty with no error and no gap marker — the evidence is gone and nothing tells you. Two further traps: an app that logs to a file inside the container gives docker logs nothing, since only stdout/stderr is captured, and drivers like journald, syslog, splunk, and gelf ship bytes off-host so docker logs queries elsewhere or refuses outright. So treat docker logs as a small, lossy ring buffer for live tailing and ship anything durable to an aggregator. When the thing that killed the container was the engine or the kernel, the app could not log it — and that is what docker events is for: the daemon's real-time, --since-replayable stream of state transitions, where a memory crash-loop shows the oom → die(137) → start triplet that never reaches docker logs because the SIGKILLed app wrote no ending. logs is what the workload said; events is what the engine did to it. Pair events with docker stats to watch a container climb to its limit and get reaped in real time.
</Recap>
