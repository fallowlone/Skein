---
concepts:
- docker-compose-model
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: senior
order: 1
prereqs: []
slug: "01-compose-model"
sources:
- https://docs.docker.com/compose/compose-application-model/
- https://github.com/compose-spec/compose-spec/blob/master/spec.md
- https://docs.docker.com/compose/networking/
spiral: []
status: ready
summary: 'Compose turns a multi-container topology into a declarative YAML file: a project namespace, a default network with DNS by service name, named volumes, and a desired-state convergence model. The file is the local environment, not a wiki of run commands.'
title: 'The Compose model: a project, a network, and services that find each other by name'
track: "docker"
unit: "06-compose-and-local-dev"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The onboarding doc was a wiki page titled "How to run the stack locally" and it was 41 numbered steps long: create a network by hand, run Postgres with the right port and password, run Redis, build the API image, run it with eleven environment flags pointing at the other two by IP, then the worker, then the frontend. Every new hire lost a day to it, and the page drifted — step 17 still referenced a container name that had been renamed in step 9. A staff engineer deleted the whole page and replaced it with one file, `compose.yaml`, plus one command. The IPs disappeared because services now found each other by name over a project network the tool created automatically. The eleven flags collapsed into an `environment` block. The thing that had been tribal knowledge spread across a wiki became a diff in version control that a reviewer could read top to bottom. The win was not fewer keystrokes — it was that the environment stopped being a sequence of commands someone had to perform correctly and became a description of a desired state the tool converges to.
</Hook>

<Crux>Compose declares a project: a network with name-based DNS, named volumes, and desired-state convergence — the environment becomes a file.</Crux>

<Explanation>

## A project is a namespace

A Compose file describes one **project**, and the project name (the directory name by default, or `-p`) is the prefix on everything Compose creates: containers, networks, volumes, labels. That namespace is what lets `compose up` and `compose down` be idempotent — Compose looks up resources by the `com.docker.compose.project` label, not by guessing, so re-running `up` reconciles the existing project rather than spawning duplicates. Two checkouts of the same repo in two directories get two independent projects with two networks and two database volumes, no collision. The model is **desired-state convergence**, not a script: you declare the set of services you want running, and `up` diffs that against what exists and makes the minimum changes — start what is missing, recreate what changed, leave what already matches. This is why editing one service's image and re-running `up` recreates that one container and not the whole stack.

```yaml
# compose.yaml — the whole local environment as data
services:
  api:
    build: .
    environment:
      DATABASE_URL: postgres://app:secret@db:5432/app   # "db" is a DNS name
      REDIS_URL: redis://cache:6379
    ports:
      - "8080:8080"
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: app
    volumes:
      - pgdata:/var/lib/postgresql/data   # named volume survives `down`
  cache:
    image: redis:7

volumes:
  pgdata:
```

## Services find each other by name, not by IP

When you run `compose up`, Compose creates a **default network** for the project (named `<project>_default`) and attaches every service to it. On that network Docker runs an embedded DNS server, and **every service is resolvable by its service name** — `api` reaches Postgres at the hostname `db`, port `5432`, with no IP anywhere in the config. This is the single biggest reason the wiki of `run` commands disappears: the brittle part of manual orchestration was always wiring containers to each other by address, and addresses change on every restart. Service-name DNS makes the wiring stable and readable. Two details bite at senior level: the DNS name is the **service** key, and `container_name` does not change it — override the container name and the DNS entry is still `db`. And the published `ports:` mapping (`"8080:8080"`) is for the *host* to reach the container; service-to-service traffic uses the container port directly over the project network and needs no `ports:` entry at all. A common mistake is publishing the database port to the host "so the API can reach it" — the API reaches it over the network by name regardless; publishing it only exposes the DB to your laptop.

<Quiz
  id="01-compose-model-quiz-1"
  lessonSlug="01-compose-model"
  lang="en"
  question="In the file above, the api service connects to Postgres using the hostname db. Where does that hostname come from, and what resolves it?"
  choices={[
    { label: "It is the service key db, resolved by Docker's embedded DNS on the project's default network", correct: true },
    { label: "It is the container's name, which you must set with container_name: db or DNS fails", misconception: "The DNS entry is the service key, not container_name. Setting container_name to something else does not change the name api uses to reach it — db still resolves. container_name mainly affects what you type in docker commands, and it breaks scaling because names must be unique." },
    { label: "Compose writes the resolved IP into /etc/hosts at build time, so it is baked into the image", misconception: "Nothing about networking is baked at build time — build produces an image with no IPs. DNS resolution happens at runtime over the project network, which is exactly why addresses can change between restarts without breaking the config." },
    { label: "It only works because the db service publishes ports: 5432:5432 to the host", misconception: "Service-to-service traffic uses the project network and the container port directly; no published port is needed. Publishing 5432 only exposes Postgres to the host machine and is often an unwanted attack surface." },
  ]}
/>

<Inset kind="why" lang="en">
Why is convergence better than a startup script? A script encodes a *path* — do this, then this — and a path can be half-run: it dies at step 17 and leaves the environment in a state no one designed. Convergence encodes a *destination*: `up` always drives toward the declared set, so an interrupted `up` re-run finishes the job rather than compounding the mess, and `down` followed by `up` is a clean rebuild because the desired state is the single source of truth. The file says what should be true, and the tool's job is to make reality match — the same mental model as Kubernetes manifests or Terraform, learned cheaply on your laptop.
</Inset>

## Named volumes are the durable layer

Containers are disposable; the data under them usually is not. A **named volume** (`pgdata` above) is a Compose-managed, project-scoped storage object that outlives container recreation: `compose up` after an image change recreates the `db` container but re-mounts the same `pgdata`, so your local database keeps its rows. The sharp edges are the destructive commands: plain `compose down` removes containers and the default network but **keeps named volumes**, while `compose down -v` deletes them — that flag is the difference between "restart the stack" and "wipe the local database." Anonymous volumes (a bare path with no name) are the trap: they are created fresh on each recreate and orphaned, so a service that should persist data but uses an anonymous volume silently loses it on the next `up`. Name the volume, mount it at the data path, and `down`/`up` becomes safe to run all day.

<Quiz
  id="01-compose-model-quiz-2"
  lessonSlug="01-compose-model"
  lang="en"
  question="A developer runs compose down then compose up after changing the Postgres image tag, and is surprised their seeded local data is gone. Which single fact explains the most likely cause?"
  choices={[
    { label: "The data path was mounted as an anonymous volume, which is recreated empty on each container recreate rather than reused", correct: true },
    { label: "compose down always deletes named volumes, so the data was wiped by down", misconception: "Plain compose down keeps named volumes — only compose down -v deletes them. If a named volume were mounted at the data path, the data would have survived. The loss points to an anonymous (unnamed) volume that is not reused." },
    { label: "Changing the image tag forces all named volumes to be reformatted", misconception: "Recreating a container from a new image does not touch the contents of a named volume mounted into it; the volume is re-attached as-is. A version bump can require a data migration, but it does not erase the volume." },
    { label: "Service-name DNS reset, so the API could not reconnect and the data appeared missing", misconception: "DNS has nothing to do with data persistence. A connectivity issue would surface as connection errors, not as an empty but reachable database." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="A Compose project: one network with DNS by service name, services wired by name, named volume as the durable layer"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "file", label: "compose.yaml", sub: "desired state" },
    { id: "project", label: "project", sub: "namespace + labels" },
    { id: "net", label: "default network", sub: "embedded DNS" },
    { id: "api", label: "api", sub: "build: ." },
    { id: "db", label: "db", sub: "postgres:16" },
    { id: "vol", label: "pgdata", sub: "named volume" },
  ]}
  edges={[
    { from: "file", to: "project", label: "up converges" },
    { from: "project", to: "net", label: "creates" },
    { from: "net", to: "api", label: "attaches" },
    { from: "api", to: "db", label: "dns: db:5432" },
    { from: "db", to: "vol", label: "persists" },
  ]}
  caption="Service-name DNS replaces hardcoded IPs; the named volume survives compose down and is deleted only by compose down -v. Two checkouts in two directories become two isolated projects."
/>

<KeyTakeaway>A Compose file is a declarative description of one project: a namespace, a default network with embedded DNS so services reach each other by service name (not IP, not container_name), named volumes for the durable layer, and desired-state convergence so up reconciles instead of scripting. Published ports are for the host only; service-to-service traffic uses the project network. compose down keeps named volumes; down -v wipes them. The environment becomes a reviewable file, not a runbook.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-compose-model-retrieval"
  lang="en"
  questions={[
    { q: "Explain how one Compose service reaches another without any IP address in the configuration, and one subtlety that surprises people.", a: "On compose up, Compose creates a default network for the project and attaches every service to it; Docker runs an embedded DNS server on that network that resolves each service by its service key. So api connects to Postgres at hostname db, port 5432, with no IP anywhere. The subtlety: the DNS name is the service key, not container_name — overriding container_name does not change the name other services use, db still resolves. A second subtlety: published ports (8080:8080) are only for the host to reach a container; service-to-service traffic uses the container port directly over the project network and needs no ports entry, so publishing a database port to the host is usually unnecessary exposure rather than a connectivity requirement." },
    { q: "Contrast a startup script with Compose's convergence model, and explain how named volumes interact with down and down -v.", a: "A script encodes a path that can half-run and leave an undesigned state; Compose encodes a desired set of services and converges to it, so a re-run of up finishes an interrupted job and reconciles against existing project resources (matched by the project label) rather than spawning duplicates. Editing one service and re-running up recreates only that container. Named volumes are the durable layer: they survive container recreation, so a db container rebuilt from a new image re-mounts the same data. Plain compose down removes containers and the default network but keeps named volumes; compose down -v deletes them. Anonymous (unnamed) volumes are the trap — they are recreated empty on each recreate, so data mounted there is silently lost on the next up." },
  ]}
/>

<Recap lang="en">
Compose collapses a multi-container local environment from a wiki of run commands into a single declarative file describing one project. The project name namespaces everything Compose creates and lets up and down be idempotent: up diffs the declared desired state against what exists and makes the minimum change, so editing one service recreates one container, not the stack. Services find each other over a default project network where Docker's embedded DNS resolves each service by its service key — api reaches Postgres at the hostname db, no IP anywhere, and container_name does not change that DNS name. Published ports are only for the host; service-to-service traffic uses the container port over the project network, so publishing a database port is exposure, not a requirement. Named volumes are the durable layer that survives container recreation; plain compose down keeps them and compose down -v deletes them, while anonymous volumes are recreated empty on every up and silently lose data. The payoff is conceptual, not cosmetic: the environment becomes a reviewable, version-controlled description of a desired state — the same mental model as Kubernetes manifests, learned on a laptop.
</Recap>
