---
slug: "quiz-short"
lang: en
track: "system-design-cases"
unit: "02-social-feed"
order: 6
title: "Social-feed cases: free-recall review"
summary: "Free-recall prompts across the social-feed cases. Reconstruct each design from memory — notification pipeline, feed fan-out hybrid, chat push/ordering, autocomplete precompute — before revealing the model answer."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - fan-out
  - hybrid-fanout
  - message-ordering
  - delivery-receipts
  - top-k-per-prefix
  - latency-budget
prereqs:
  - system-design-cases/02-social-feed/01-notification-system
  - system-design-cases/02-social-feed/02-news-feed
  - system-design-cases/02-social-feed/03-chat-system
  - system-design-cases/02-social-feed/04-search-autocomplete
sources:
  - https://developer.apple.com/documentation/usernotifications/setting_up_a_remote_notification_server
  - https://engineering.fb.com/2014/06/12/core-infra/tao-the-power-of-the-graph/
  - https://github.com/donnemartin/system-design-primer
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, reconstruct the full design from memory — the pipeline, the tradeoff, the failure mode — before opening the model answer. The effort of rebuilding the architecture is what makes these four cases stick.
</Hook>

<Crux>Reconstruct the four cases: notification pipeline, feed hybrid, chat push/ordering, autocomplete precompute.</Crux>

<Explanation>

Rebuild each design without looking back: the notification fan-out pipeline and what prevents the retry storm; the feed's fan-out tradeoff and the celebrity hybrid; chat's push transport, ordering, and delivery ticks; and why autocomplete precomputes everything offline.

<RetrievalDrawer
  client:load
  id="02-social-feed-short-retrieval"
  lang="en"
  questions={[
    { q: "Reconstruct the notification pipeline and the three pieces that prevent the retry storm.", a: "Event API (validate, persist, enqueue, return immediately) then event queue then fan-out worker (resolve recipients, paginate with checkpointing, provider topics for celebrity broadcast) then preference + dedup filter (idempotency key) then per-channel queues + workers (each owns one provider with its own rate limit, retry, DLQ). The retry storm — duplicates and re-throttling when a recovered provider's backlog flushes — is prevented by three things together: exponential backoff with jitter, a bounded attempt count feeding a DLQ, and an idempotency key checked before send. Missing any one reopens the hole." },
    { q: "State the feed's fan-out tradeoff, the read:write ratio that decides it, and the celebrity hybrid.", a: "Fan-out-on-write (push) precomputes each follower's feed so reads are an O(1) cache lookup but writes amplify by follower count; fan-out-on-read (pull) stores each post once and assembles at read time so writes are cheap but every read is an unamortized scatter-gather. A feed is ~100:1 read-heavy, so push is the default. The breaker is the power-law follower distribution: a celebrity's post under push is a tens-of-millions write storm, so you pull celebrities (store once, merge at read time) and push everyone else — the hybrid, with a follower-count threshold routing authors. It works because celebrities make push expensive yet are rare per reader, so pulling them costs each reader only a few extra lookups." },
    { q: "Explain chat's transport, the persist-before-ack rule, ordering, and the three ticks.", a: "Transport is persistent WebSockets to a stateful connection-gateway tier (scaled by concurrent connections), with a connection registry mapping each user/device to their gateway so a stateless service can route. Delivery is at-least-once with dedup on a client message id, and the rule is persist before you ack — the 'sent' tick means durably stored, not 'server saw it'. Ordering is per-conversation via a monotonic sequence number assigned at persist time on the conversation's shard (which doubles as the offline sync cursor). The three ticks: sent (persisted), delivered (recipient's device received and acked), read (recipient opened the chat) — each an acknowledgment flowing back to the sender." },
    { q: "Why must autocomplete precompute, and how does the trie with top-k per node serve in O(1)?", a: "The ~100 ms budget (tens of ms for the server) cannot fit an aggregation over billions of log rows, and every keystroke is a request, so live ranking is both too slow and a keystroke storm. So an offline pipeline counts query frequency over a rolling, time-decayed window, filters rare/banned terms, and builds a trie. A trie stores strings by character from the root, so a prefix is one node and its completions are the subtree below; instead of traversing that subtree, the build stores the top-k completions directly at every node. Serving is then O(prefix length) to walk down plus O(1) to read the cached top-k, published as an immutable snapshot and fronted by an edge cache for popular prefixes." },
  ]}
/>

</Explanation>

<KeyTakeaway>Reconstruct each case from memory — notification pipeline, feed hybrid, chat push/ordering, autocomplete precompute — and you hold the spine: read-heavy social systems push cost to the write/build side.</KeyTakeaway>

<Recap lang="en">
If you can rebuild each design from memory, you hold the unit's spine. The **notification system** is an async fan-out pipeline whose retry storm is prevented by backoff+jitter, a bounded-retry **DLQ**, and an **idempotency key**. The **news feed** defaults to **push** (read-heavy, ~100:1) but **pulls celebrities** in a hybrid because the power-law tail breaks a single strategy. The **chat system** is **server-push** over WebSockets routed via a connection registry, **persists before it acks**, and orders messages with a **per-conversation sequence number**. **Autocomplete** precomputes **top-k per trie node** offline and serves an O(1) read behind an edge cache, never ranking live per keystroke. The through-line: at this depth, every read-heavy social system moves the expensive work to the write or build side so the user-facing path is a cheap, safe lookup.
</Recap>
