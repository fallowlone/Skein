---
slug: "project"
lang: en
track: "system-design-cases"
unit: "02-social-feed"
order: 8
title: "Social-feed cases: design and defend a feed-backed app"
summary: "Hands-on project: produce a design doc for a social app with a news feed, notifications, and presence — choosing fan-out strategies, the celebrity hybrid, delivery guarantees, and ordering — then justify each threshold with a number and validate the riskiest with a prototype."
estMin: 240
status: ready
lessonType: topic
level: senior
concepts:
  - fan-out
  - hybrid-fanout
  - idempotency-dedup
  - message-ordering
  - feed-cache
  - latency-budget
prereqs:
  - system-design-cases/02-social-feed/01-notification-system
  - system-design-cases/02-social-feed/02-news-feed
  - system-design-cases/02-social-feed/03-chat-system
  - system-design-cases/02-social-feed/04-search-autocomplete
sources:
  - https://engineering.fb.com/2014/06/12/core-infra/tao-the-power-of-the-graph/
  - https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/
  - https://github.com/donnemartin/system-design-primer
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Knowing each case in isolation is not the same as composing them into one coherent system a staff engineer would sign off on. Take a real-shaped social app, write the design doc end to end — feed, notifications, presence, with every fan-out and consistency choice justified by a number — and then prove your two riskiest decisions with a prototype, not a hand-wave.
</Hook>

<Crux>Compose the four cases into one design doc — fan-out, the celebrity hybrid, delivery, ordering — justify each by a number, then prototype.</Crux>

<Explanation>

This project makes the unit operational: you integrate the four cases into one system, decide each tradeoff against an estimate, and measure to confirm the two predictions most likely to bite — the celebrity fan-out cost and the at-least-once-plus-dedup delivery guarantee.

<ProjectBrief
  id="02-social-feed-project"
  lessonSlug="project"
  lang="en"
  objective="Produce a complete design doc for a social app (a short-form posting app with a home feed, push/email notifications, and presence), making and justifying the fan-out, hybrid, delivery, and ordering decisions from the unit — then build a small prototype that empirically validates your celebrity fan-out strategy and your at-least-once-with-dedup notification path."
  requirements={[
    "Write a one-paragraph brief with explicit input assumptions: total users, daily active users, average and tail (celebrity) follower counts, posts/user/day, feed opens/user/day, and notifications/user/day. State every assumption.",
    "Estimate the load: average and peak feed-read QPS, post-write QPS, and notification QPS (requests/day ÷ ~10^5, with a justified peak multiplier), each rounded to one significant figure. Derive the read:write ratio and use it to justify the default fan-out strategy.",
    "Specify the feed: choose fan-out-on-write as the default and define the celebrity threshold (the follower count above which an author is pulled at read time), with the arithmetic showing why pure push fails at the tail. Describe the feed cache (post ids, capped, per-user) and cursor pagination.",
    "Specify notifications: the async fan-out pipeline, per-channel queues, the idempotency key, retry with backoff+jitter feeding a DLQ, and the priority lane that keeps transactional sends ahead of bulk. State the delivery guarantee (at-least-once + dedup) and why exactly-once is not on the table.",
    "Specify ordering and consistency: per-conversation/per-feed sequence or timestamp strategy, which surfaces are eventually consistent (feed, presence, read receipts) and which need stronger guarantees, and why that staleness is acceptable.",
    "Write the architecture decisions as a short list, each justified by a specific number ('celebrity threshold = 1M followers, because a push above that exceeds the X writes/min the feed store sustains').",
    "Build a prototype (any language): a publisher that fans out posts to follower feeds with a configurable celebrity threshold (push below it, pull above it), and a notification sender with a retry+DLQ path and an idempotency store.",
    "Validate the celebrity hybrid: measure feed-write volume and read-assembly cost as you vary the threshold and inject a high-follower author, showing the write storm under pure push and its disappearance under the hybrid.",
    "Validate delivery: inject provider failures and a backlog re-flush, and show that the idempotency key collapses duplicates to one delivery while the DLQ captures permanently-failed sends.",
  ]}
  acceptance={[
    "A design doc readable in a few minutes: load estimates (avg + peak, one significant figure), the read:write ratio, and the fan-out/hybrid/delivery/ordering decisions, each citing the number that drives it.",
    "An explicit celebrity threshold with the arithmetic that sets it, plus the feed-cache and pagination design.",
    "A working prototype whose measurements show the celebrity write storm under pure push and its removal under the hybrid, with the threshold and follower counts that produced each.",
    "A delivery demonstration: a fault-injection run where duplicates are collapsed by the idempotency key and permanently-failed sends land in the DLQ, with the before/after duplicate counts.",
    "A short write-up: one paragraph on what the estimate revealed before any code, and one on where the prototype's behavior differed from the napkin and why.",
  ]}
  stretch={[
    "Add a chat surface: persistent WebSocket connections, a connection registry, and per-conversation sequence numbers; demonstrate consistent ordering and reconnect-and-sync via the sequence as a cursor.",
    "Add autocomplete for user/topic search: an offline build of a trie with top-k per node, and show the read path is an O(1) lookup versus a live log scan.",
    "Add presence: show the fan-out trap (a popular user's online event) and implement pull-for-visible-contacts instead, measuring the update volume difference.",
    "Recompute the whole plan under 10x growth and identify the first threshold you cross (feed-cache shard, DB shard, more gateways), demonstrating how the estimate reshapes the design as inputs scale.",
  ]}
/>

</Explanation>

<KeyTakeaway>Composing the cases is the real skill: one design doc where the feed hybrid, notification delivery, and ordering each rest on a number — then a prototype proving the riskiest two. Estimate first, validate second.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-social-feed-project-retrieval"
  lang="en"
  questions={[
    { q: "What load estimates drive the feed and notification design, and how does each become a decision?", a: "Feed-read QPS and post-write QPS give the read:write ratio (typically ~100:1), which justifies fan-out-on-write as the default — do the work once and amortize over reads. The tail follower count sets the celebrity threshold: the follower count above which a push exceeds the writes-per-minute the feed store sustains, so those authors are pulled instead. Notification QPS plus the per-channel split sizes the queues and rate limits, and the bursty peak justifies the async, queue-buffered pipeline with a transactional priority lane. Each number maps to one architecture decision." },
    { q: "How do you validate the two riskiest decisions in a prototype?", a: "Celebrity hybrid: build a publisher with a configurable threshold (push below, pull above), inject a high-follower author, and measure feed-write volume and read-assembly cost — you should see the write storm under pure push and its disappearance once celebrities are pulled. Delivery guarantee: build a notification sender with retry+DLQ and an idempotency store, inject provider failures and a backlog re-flush, and show duplicates collapse to one delivery via the idempotency key while permanently-failed sends land in the DLQ. The estimate predicts; the prototype confirms." },
    { q: "Why design and estimate first, then prototype, rather than building immediately?", a: "The estimate is order-of-magnitude reasoning that eliminates wrong shapes in minutes — it tells you the read:write ratio (hence push vs pull), the celebrity threshold, and which surfaces can be eventually consistent — before you spend a sprint building the wrong system. The prototype is slower and only validates what you built, so doing the estimate first makes it targeted: you confirm the celebrity fan-out cost and the delivery guarantee specifically, the two decisions most likely to bite, instead of benchmarking blindly." },
  ]}
/>

<Recap lang="en">
This project composes the four cases into one system and forces every decision to rest on a number. You write a **design doc** for a feed-backed social app: estimate **feed-read, post-write, and notification QPS** (avg and peak, one significant figure), derive the **read:write ratio**, and use it to choose **fan-out-on-write** as the feed default with a **celebrity threshold** that the arithmetic sets, plus a capped **feed cache** and **cursor pagination**. You specify the **notification pipeline** (async fan-out, per-channel queues, **idempotency key**, retry+jitter into a **DLQ**, a transactional priority lane) with an explicit **at-least-once-plus-dedup** guarantee, and you state which surfaces are **eventually consistent** and why. Then you **close the loop**: a prototype that shows the **celebrity write storm** vanish under the hybrid, and a fault-injection run where the **idempotency key** collapses duplicates while the DLQ captures failures. Estimate first, validate second — the napkin tells you the shape and the riskiest predictions; the prototype confirms them. An engineer who has built this once composes social systems deliberately instead of discovering the celebrity tail in production.
</Recap>
