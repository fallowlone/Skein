---
slug: "project"
lang: en
track: "system-design-cases"
unit: "01-foundational"
order: 8
title: "Foundational cases: write a full design doc"
summary: "Hands-on project: pick one of the four foundational cases and produce a complete design document — requirements, back-of-envelope estimate, high-level architecture diagram, two deep-dive components, and a bottlenecks-and-tradeoffs section a senior engineer would sign off."
estMin: 240
status: ready
lessonType: topic
level: senior
concepts:
  - design-document
  - capacity-estimation
  - high-level-design
  - deep-dive
  - bottlenecks-and-tradeoffs
  - delivery-semantics
prereqs:
  - 01-foundational/01-key-value-store
  - 01-foundational/02-url-shortener
  - 01-foundational/03-web-crawler
  - 01-foundational/04-distributed-message-queue
sources:
  - https://github.com/donnemartin/system-design-primer
  - https://www.allthingsdistributed.com/files/amazon-dynamo-sosdp2007.pdf
  - https://kafka.apache.org/documentation/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Reading four worked case studies is not the same as producing one yourself under the constraints a real review imposes. Pick one of the four systems, write the full design document end to end — the requirements, the estimate that picks the shape, the architecture, two hard components, and the failure modes — and defend every threshold with a number a staff engineer could check.
</Hook>

<Crux>Turn one case into a design doc: requirements, an estimate that picks the shape, an architecture, two deep-dives, and the bottlenecks.</Crux>

<Explanation>

This project makes the unit operational: you run the full design loop for one system, justify each decision with the arithmetic from the cases, and surface the tradeoffs that separate a design that works from one that survives. The deliverable is a document, not code — the skill is reasoning, not typing.

<ProjectBrief
  id="01-foundational-project"
  lessonSlug="project"
  lang="en"
  objective="Produce a complete senior-level design document for ONE of the four foundational systems (distributed key-value store, URL shortener, web crawler, or distributed message queue), following the same structure the cases used: requirements → estimation → high-level design → deep dive → bottlenecks and tradeoffs. Every numeric threshold in the doc must be justified by a back-of-envelope calculation, and every component choice by an explicit trade."
  requirements={[
    "State functional and non-functional requirements separately, and identify the ONE non-functional requirement that forces the rest of the design (e.g. 'always writable during a partition' for the KV store, '~100:1 read:write' for the shortener, 'never overload a host' for the crawler, '1 GB/sec durable' for the queue).",
    "Do a back-of-envelope estimate: derive QPS (or messages/sec), storage over a retention window, and one memory/bandwidth figure, each rounded to one significant figure, and state which resource breaks first.",
    "Draw a high-level architecture diagram (boxes and arrows) showing the request/data path end to end, and write a one-paragraph walkthrough of a single request flowing through it.",
    "Deep-dive TWO hard components for your system — e.g. quorum + versioning (KV), code generation + the redirect/cache path (shortener), the frontier + dedup layers (crawler), partitions/ISR + delivery semantics (queue) — explaining the mechanism and the alternative you rejected.",
    "Write a bottlenecks-and-tradeoffs section naming at least four failure modes or hard limits (hot keys/partitions, the rehash problem, cache stampede, unclean leader election, at-least-once duplicates, traps, etc.) and how you mitigate each.",
    "For every design choice, write the explicit trade in one sentence ('302 over 301 because clicks are the product, accepting that we serve every redirect').",
    "Include a 'what I'd build first' paragraph: the minimal version that meets the forcing requirement, and the first threshold (from your estimate) at which you'd add the next piece.",
  ]}
  acceptance={[
    "A design doc with all five sections (requirements, estimation, HLD with a diagram, two deep dives, bottlenecks) readable in about ten minutes by another engineer.",
    "Every numeric threshold is backed by a shown calculation (no bare numbers), and the doc explicitly names which resource breaks first.",
    "Each of the two deep-dive components states the mechanism AND the alternative rejected, with the reason — not just a description of the chosen design.",
    "The bottlenecks section names at least four real failure modes specific to the chosen system, each with a mitigation, not generic 'add more servers' answers.",
    "A reviewer could disagree with a choice and see exactly which assumption to change — every decision is traceable to a stated requirement or number.",
  ]}
  stretch={[
    "Write the doc for a SECOND system and extract the shared skeleton: which moves (partition, replicate, dedup, cache, order) recur, and what changes the shape between the two.",
    "Add a 10x-growth section: recompute the estimate at 10x load and identify the first threshold you cross (a new shard, a cache tier, more partitions) and what in the design must change.",
    "Add an explicit consistency/availability stance: state where your system sits on CAP/PACELC and walk through exactly what a network partition does to reads and writes.",
    "Critique one of the four worked cases: find a requirement it under-served or a tradeoff it glossed, and propose the change with the number that justifies it.",
  ]}
/>

</Explanation>

<KeyTakeaway>A design doc makes the unit operational: requirements (find the forcing one), an estimate that picks the shape, an architecture, two deep dives with the rejected alternative, and the failure modes named up front.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-foundational-project-retrieval"
  lang="en"
  questions={[
    { q: "What are the five sections of a design doc, and what does each contribute?", a: "Requirements (functional + non-functional, and the one non-functional that forces the design), Estimation (QPS, storage, memory/bandwidth to one significant figure — which resource breaks first), High-level design (the request/data path as a diagram plus a walkthrough), Deep dive (two hard components, each with the mechanism and the rejected alternative), and Bottlenecks & tradeoffs (the failure modes and hard limits named before you build). The estimate picks the shape; the deep dives prove you understand the hard parts; the bottlenecks prove you know where it breaks." },
    { q: "Why justify every threshold with a number and name the rejected alternative?", a: "A bare number ('we need 100 partitions') is unfalsifiable; a shown calculation (1000 MB/s ÷ 10 MB/s per partition) lets a reviewer disagree by changing an assumption rather than your conclusion — that's what makes a doc reviewable. Naming the rejected alternative (counter vs KGS, vector clocks vs LWW, 301 vs 302) proves the choice was a decision, not a default, and records the trade so a future reader knows what changes if the requirement shifts. Designs are judged on the quality of the trades, not the cleverness of the boxes." },
    { q: "What does the 'forcing requirement' do, and why find it first?", a: "Each system has one non-functional requirement that determines almost everything downstream: 'always writable during a partition' forces the KV store toward no-master + eventual consistency; '~100:1 read:write' forces the shortener toward cache-and-redirect; 'never overload a host' forces the crawler's two-stage frontier; '1 GB/sec durable' forces the queue toward a partitioned, replicated log on sequential disk. Finding it first means the rest of the design follows from one principle instead of a pile of independent choices — and it's the first thing a strong reviewer looks for." },
  ]}
/>

<Recap lang="en">
This project turns the four case studies into a repeatable workflow you can apply to any "design X" prompt. Pick one system and write the **five-section design doc**: **requirements** (and the single non-functional requirement that forces the rest — always-writable, read-heavy, never-overload-a-host, or durable-firehose); an **estimate** that derives QPS, storage, and a memory/bandwidth figure to one significant figure and names which resource breaks first; a **high-level design** with a diagram and a request walkthrough; **two deep dives** that each give the mechanism and the alternative you rejected with the reason; and a **bottlenecks-and-tradeoffs** section naming at least four real failure modes with mitigations. Justify every threshold with a shown calculation so a reviewer can disagree by changing an assumption, and record the trade behind every choice. An engineer who has written one of these end to end stops treating system design as a memorised diagram and starts treating it as reasoning with numbers — which is exactly what the whole track is teaching.
</Recap>
