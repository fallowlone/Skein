---
slug: "quiz-choice"
lang: en
track: "system-design-cases"
unit: "01-foundational"
order: 5
title: "Foundational cases: multiple-choice review"
summary: "Multiple-choice synthesis across the foundational case studies: the KV store's quorum, the shortener's read:write ratio and redirect status, the crawler's frontier and dedup, and the message queue's ordering and delivery semantics."
estMin: 13
status: ready
lessonType: topic
level: senior
concepts:
  - quorum
  - read-heavy-caching
  - url-frontier
  - delivery-semantics
  - partitioned-log
  - dedup
prereqs:
  - 01-foundational/01-key-value-store
  - 01-foundational/02-url-shortener
  - 01-foundational/03-web-crawler
  - 01-foundational/04-distributed-message-queue
sources:
  - https://www.allthingsdistributed.com/files/amazon-dynamo-sosdp2007.pdf
  - https://kafka.apache.org/documentation/
  - https://github.com/donnemartin/system-design-primer
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the four case studies. Each is a decision you make at a whiteboard — which quorum setting, which short-code scheme, why the frontier is two-stage, which delivery semantics — not a definition to recite.
</Hook>

<Crux>Six decisions across the unit: a quorum guarantee, a read-heavy redirect, the crawler frontier, dedup, and delivery semantics.</Crux>

<Explanation>

Confirm you can pick the quorum that gives read-your-write, read the shortener's read:write ratio and choose its redirect status, explain why the crawler frontier separates priority from politeness, and name the delivery semantics from where the offset is committed.

<Quiz
  id="01-foundational-choice-1"
  lessonSlug="quiz-choice"
  lang="en"
  question="A Dynamo-style store runs N=3. A client must be able to read back a value it just wrote, using a strict quorum. Which R/W setting is the minimal one that guarantees it?"
  choices={[
    { label: "W=2, R=2 — R + W = 4 > N = 3, so the read and write sets must overlap on at least one node holding the latest write.", correct: true },
    { label: "W=1, R=1 — one write, one read is enough.", misconception: "R + W = 2 ≤ N = 3, so the read and write sets can be disjoint and the read can miss the new value. You need R + W > N; W=2,R=2 is the balanced minimal choice." },
    { label: "W=3, R=3 — only writing and reading all replicas is safe.", misconception: "That works but isn't minimal and kills availability (any node down blocks the op). R + W > N is the actual requirement, met by W=2,R=2 with far better availability." },
    { label: "Raise N to 5 — more replicas guarantee freshness.", misconception: "Replica count alone doesn't guarantee overlap; the relationship R + W > N does. With N=3, W=2,R=2 already guarantees the read sees the write, no extra replicas needed." },
  ]}
/>

<Quiz
  id="01-foundational-choice-2"
  lessonSlug="quiz-choice"
  lang="en"
  question="Your URL shortener earns money from click analytics. An engineer switches redirects to 301 to 'cut server load.' What breaks, and what's the right status?"
  choices={[
    { label: "301 is cached by browsers/proxies, so repeat clicks skip your server — your analytics go blank and you can never repoint the link. Use 302 so every click returns through you, and size the cache/read tier to absorb it.", correct: true },
    { label: "Nothing breaks; 301 just makes it faster.", misconception: "301 caches the mapping in browsers, so you stop seeing repeat clicks — fatal when clicks are the product — and you lose the ability to change or expire the link. 302 preserves both at the cost of serving every click." },
    { label: "301 breaks uniqueness of short codes.", misconception: "The redirect status has nothing to do with code uniqueness (that's set at generation). 301 breaks analytics and repointing because browsers cache the mapping; 302 is the fix." },
    { label: "Switch to 307 to keep analytics.", misconception: "The analytics-preserving choice is 302 (temporary, not cached); 307 is a method-preserving temporary redirect, not the standard shortener answer. The core point is temporary (not cached) vs permanent (cached)." },
  ]}
/>

<Quiz
  id="01-foundational-choice-3"
  lessonSlug="quiz-choice"
  lang="en"
  question="A crawler pulls URLs strictly by priority from a single queue and keeps DDoSing individual sites. Why, and what fixes it?"
  choices={[
    { label: "Pulling by priority can fire many high-priority URLs that all live on the same host back-to-back. A two-stage frontier adds back queues mapped one-per-host with a next-allowed timestamp, enforcing a per-host crawl-delay regardless of priority.", correct: true },
    { label: "The crawler is too slow; add more fetcher threads.", misconception: "More threads make the hammering worse. The problem is no per-host rate limit — the fix is back queues (one host each, with a crawl-delay timestamp), not more concurrency." },
    { label: "It's missing a bloom filter, so it re-fetches the same host.", misconception: "A bloom filter dedupes URLs; it doesn't pace requests to a host. Politeness comes from the frontier's back queues enforcing a per-host delay, a separate concern from dedup." },
    { label: "Robots.txt is being ignored; just honor it.", misconception: "Honoring robots crawl-delay helps, but the structural fix is the two-stage frontier whose back queues enforce one-request-per-host-per-delay even where robots gives no delay. A single priority queue can't do that." },
  ]}
/>

<Quiz
  id="01-foundational-choice-4"
  lessonSlug="quiz-choice"
  lang="en"
  question="You need a user's events processed in order in a Kafka-like log with many partitions. What guarantees that, and what's the trap?"
  choices={[
    { label: "Hash a stable partition key (e.g. user_id) so all of that user's messages land in one partition, which is strictly ordered by offset. The trap: ordering is per-partition only — no key (round-robin) scatters events across partitions and loses order.", correct: true },
    { label: "Increase the replication factor so order is preserved.", misconception: "Replication is about durability, not ordering. Order is per-partition; you keep a user's events ordered by routing them to one partition via a partition key, not by adding replicas." },
    { label: "Use a single partition for the whole topic.", misconception: "That gives total order but caps throughput and consumer parallelism to one — defeating partitioning. The right move is a per-user partition key so each user is ordered while the topic still scales across partitions." },
    { label: "Enable exactly-once delivery; it also orders messages.", misconception: "Exactly-once is about duplicates/loss, not ordering. Ordering for a user comes from a stable partition key routing their events to one ordered partition." },
  ]}
/>

<Quiz
  id="01-foundational-choice-5"
  lessonSlug="quiz-choice"
  lang="en"
  question="A consumer commits its offset AFTER processing each message. The process crashes after a side effect but before the commit. On restart, what happens?"
  choices={[
    { label: "At-least-once: the message is re-read and reprocessed, so the side effect can happen twice — which is why the consumer must be idempotent (dedupe by message ID). No message is lost.", correct: true },
    { label: "At-most-once: the message is skipped and lost.", misconception: "Skipping happens when you commit BEFORE processing. Committing AFTER means an uncommitted message is re-read on restart — at-least-once, with possible duplicates, not loss." },
    { label: "Exactly-once automatically, because the offset wasn't committed.", misconception: "Committing after processing gives at-least-once (duplicates on retry), not exactly-once. Exactly-once needs an idempotent consumer or an atomic offset-plus-side-effect transaction." },
    { label: "The whole partition is reassigned and skipped.", misconception: "A crash triggers a rebalance, but the partition resumes from the last committed offset — re-reading the uncommitted message. That's at-least-once: reprocessing, not skipping." },
  ]}
/>

<Quiz
  id="01-foundational-choice-6"
  lessonSlug="quiz-choice"
  lang="en"
  question="A crawler's exact seen-URL set would need ~1 TB of RAM. You swap in a bloom filter. Which statement is correct?"
  choices={[
    { label: "It has no false negatives but a tunable false-positive rate — it may occasionally mark a genuinely new URL as 'seen' and skip it, which is an acceptable cost (one missed page in billions) for ~100x less memory.", correct: true },
    { label: "It may produce false negatives, re-fetching pages you already crawled.", misconception: "Bloom filters never give false negatives: 'not seen' means definitely new. The error is a false positive (a new URL flagged seen and skipped) — the opposite direction." },
    { label: "It's lossless compression of the exact set.", misconception: "It's probabilistic, not lossless. It trades a tunable false-positive rate for huge memory savings — the reason it fits where the exact 1 TB set doesn't." },
    { label: "It removes the need for content hashing too.", misconception: "URL dedup (the bloom filter) and content dedup (hashing the body) catch different duplicates — the same content reached via http/https or tracking params has different URLs. You still need both." },
  ]}
/>

</Explanation>

<KeyTakeaway>Case design is the same moves: a quorum (R+W>N) for read-your-write, the read:write ratio and redirect status for the shortener, a frontier and bloom filter for the crawler, a partition key for the queue.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-foundational-choice-retrieval"
  lang="en"
  questions={[
    { q: "What quorum gives read-your-write at N=3, and why?", a: "W=2, R=2 (any setting with R + W > N). By pigeonhole, a write touching W of N nodes and a read touching R of N must share a node when R + W > N, so the read always includes at least one replica holding the latest acknowledged write. W=1,R=1 (sum 2 ≤ 3) can read disjoint nodes and miss the write." },
    { q: "Why is the crawler frontier two-stage, and what does at-least-once force on a consumer?", a: "Frontier: front queues order by priority (crawl important pages first), back queues map one-per-host with a next-allowed timestamp (never hammer a host) — a single queue can't serve both goals. Delivery: committing the offset after processing is at-least-once, so a crash between side effect and commit reprocesses the message — every consumer with side effects must be idempotent (dedupe by message ID)." },
  ]}
/>

<Recap lang="en">
The through-line across the four cases is that each is the same handful of moves applied to a new shape. The **KV store** is a quorum decision: `R + W > N` (e.g. W=2,R=2 at N=3) guarantees a read overlaps the latest write. The **URL shortener** is a read:write read: ~100:1 means the redirect path is everything, and the **301-vs-302** status trades load for analytics/control (302 when clicks are the product). The **crawler** is a two-stage **frontier** (priority in front, one-host-per-back-queue politeness in back) plus a **bloom filter** (no false negatives, tunable false positives) and content hashing for the duplicates URLs can't catch. The **message queue** is a **partitioned log**: a stable partition key keeps a user's events ordered (order is per-partition only), and **offset-commit timing** sets the delivery semantics — commit after processing is at-least-once, so consumers must be idempotent. Recognise the move and the design writes itself.
</Recap>
