---
slug: "quiz-short"
lang: en
track: "system-design-cases"
unit: "01-foundational"
order: 6
title: "Foundational cases: free-recall review"
summary: "Free-recall prompts across the foundational case studies. Reconstruct each design from memory — the KV store's three blocks, the shortener's read path, the crawler's frontier and dedup, the queue's log — before revealing the model answer."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - quorum
  - read-heavy-caching
  - url-frontier
  - delivery-semantics
  - partitioned-log
  - dedup
prereqs:
  - 01-foundational/01-key-value-store
  - 01-foundational/02-url-shortener
  - 01-foundational/03-web-crawler
  - 01-foundational/04-distributed-message-queue
sources:
  - https://www.allthingsdistributed.com/files/amazon-dynamo-sosdp2007.pdf
  - https://kafka.apache.org/documentation/
  - https://github.com/donnemartin/system-design-primer
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, reconstruct the full design from memory — the components, the numbers, the trade — before you open the model answer. The effort of rebuilding the architecture is what makes the patterns transfer to the next case you're handed.
</Hook>

<Crux>Reconstruct the four cases from memory: the KV store blocks, the shortener read path, the crawler frontier and dedup, the queue log.</Crux>

<Explanation>

Rebuild each design without looking back: the three ideas a Dynamo-style store stacks, why a URL shortener is a read problem, what the crawler frontier and dedup layers each do, and how a partitioned log delivers ordering, durability, and replay.

<RetrievalDrawer
  client:visible
  id="01-foundational-short-retrieval"
  lang="en"
  questions={[
    { q: "Reconstruct the Dynamo-style KV store: the central trade and the three building blocks.", a: "Central trade: availability over consistency (CAP), forced by 'always writable, even during a partition' — so it accepts eventual consistency and resolves conflicts at read time, with no master. Three blocks: (1) consistent-hashing ring partitions keys, each key's N replicas are its preference list, adding a node moves ~1/N of keys; (2) replication + quorum, R + W > N guarantees a read overlaps the latest write, with sloppy quorums + hinted handoff so writes never block; (3) versioning — vector clocks surface concurrent siblings for the app to merge (or last-write-wins where dropping a loser is fine). Gossip tracks membership; read-repair and Merkle anti-entropy heal drift." },
    { q: "Why is a URL shortener a read problem, and how do you mint the code?", a: "It's ~100:1 read-heavy: a link is written once and read many times, so ~40 writes/sec is trivial while ~12K reads/sec at peak is the system. The redirect path is cache-aside over a KV store, with the hot 20% cached (~3 TB total over 5 years doesn't fit in RAM). Code minting: avoid hashing (truncation collides, taxes every write); use a counter + base62 (unique by construction, but a global counter needs range allocation and produces guessable sequential codes) or a key-generation service (pre-mints random unique codes offline, no collisions, unguessable, clean custom aliases). 302 keeps analytics; 301 caches in browsers to cut load." },
    { q: "Reconstruct the crawler: what the frontier and the two dedup layers each do.", a: "The two-stage frontier separates priority (front queues — crawl important pages first) from politeness (back queues — one host each, with a next-allowed timestamp enforcing a crawl-delay), because a single queue can't serve both. Dedup has two layers for two problems: a bloom filter answers 'seen this URL?' in a few bits each (no false negatives, tunable false positives) so tens of billions of URLs fit in RAM and you don't re-fetch; a content hash (normalized, or SimHash for near-dupes) drops the same bytes reached via http/https, tracking params, or mirrors so you don't re-index. Traps (calendars, infinite pagination) are fenced with depth/URL caps and content hashing; freshness drives recrawl by each page's estimated change rate, making the crawler continuous." },
    { q: "Reconstruct the message queue: log vs mailbox, partitions, ISR, and delivery semantics.", a: "It's an append-only log retained for a window, read by a consumer-tracked offset — not a mailbox that deletes on delivery — which unlocks replay, multiple independent consumer groups, and crash recovery. Sequential disk append + partitioning makes ~1 GB/sec possible. Partitions are the atom: parallelism (throughput scales with count), per-partition ordering (route a user's events with a partition key), and the cap on consumer parallelism (one consumer per partition per group). Durability: a partition leader replicates to the ISR; acks=all commits only when all in-sync replicas have it and a new leader is elected from the ISR, so no committed write is lost (unclean-leader-election is the CAP choice if the ISR empties). Delivery semantics come from offset-commit timing: before processing = at-most-once (loss); after = at-least-once (duplicates); exactly-once = idempotent consumer or atomic offset+effect transaction." },
  ]}
/>

</Explanation>

<KeyTakeaway>Reconstruct each case from memory — the KV store blocks, the shortener read path, the crawler frontier and dedup, the queue log — and you hold the spine: the same moves (partition, replicate, dedup, order) reshaped.</KeyTakeaway>

<Recap lang="en">
If you could rebuild each design from memory, you hold the unit's spine: the four cases are the same moves reshaped. The **KV store** picks availability over consistency and stacks a hash ring, a quorum (`R + W > N`), and versioning, with gossip and anti-entropy underneath. The **URL shortener** is a ~100:1 read problem solved with a counter/KGS for codes and a cache for the redirect, with 302-vs-301 trading analytics for load. The **crawler** is a two-stage frontier (priority + politeness) plus two dedup layers (bloom filter for URLs, content hash for bytes), made continuous by freshness-driven recrawl and hardened against traps. The **message queue** is an append-only partitioned log: partitions for parallelism and ordering, the ISR with `acks=all` for durability, and offset-commit timing for the delivery semantics. The transferable lesson is that every new "design X" prompt decomposes into requirements, an estimate that picks the shape, a few hard components, and the bottlenecks you name before you build.
</Recap>
