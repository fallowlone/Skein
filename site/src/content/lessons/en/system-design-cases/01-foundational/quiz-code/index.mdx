---
slug: "quiz-code"
lang: en
track: "system-design-cases"
unit: "01-foundational"
order: 7
title: "Foundational cases: numbers and config reading"
summary: "Read real capacity math and config from the four cases, then do the arithmetic: a quorum overlap check, a shortener QPS/keyspace estimate, a crawler bloom-filter sizing, and a Kafka partition-count calculation."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - quorum
  - capacity-estimation
  - bloom-filter-dedup
  - partitioned-log
  - base62-encoding
  - read-heavy-caching
prereqs:
  - 01-foundational/01-key-value-store
  - 01-foundational/02-url-shortener
  - 01-foundational/03-web-crawler
  - 01-foundational/04-distributed-message-queue
sources:
  - https://www.allthingsdistributed.com/files/amazon-dynamo-sosdp2007.pdf
  - https://kafka.apache.org/documentation/
  - https://github.com/donnemartin/system-design-primer
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Design bugs hide in the numbers, not the prose: a quorum that doesn't overlap, a QPS that's off by 10^5, a bloom filter sized for the wrong error rate, a partition count that caps throughput. Read each snippet, do the arithmetic in your head, and pick the answer a senior engineer would commit to.
</Hook>

<Crux>Read the config from each case, find the quorum, QPS, bloom-filter size, or partition count, do the math, and pick the fix it supports.</Crux>

<Explanation>

Practise the loop you run during a design review: locate the numbers in the config, apply the rule (quorum overlap, daily-to-QPS, bloom-filter bits, partition throughput), and choose the change the arithmetic actually supports.

## Snippet 1 — the quorum config

```yaml
# distributed KV store
replication_factor_N: 3
write_quorum_W: 1
read_quorum_R: 1
# "tuned for low latency"
```

<Quiz
  id="01-foundational-code-1"
  lessonSlug="quiz-code"
  lang="en"
  question="With this config, can a client always read back a value it just wrote on a strict quorum, and what's the fix if not?"
  choices={[
    { label: "No: R + W = 2 ≤ N = 3, so the read set and write set can be disjoint and the read can miss the new value. Fix by making R + W > N — e.g. W=2, R=2 — so the sets must overlap.", correct: true },
    { label: "Yes — one write and one read are always consistent.", misconception: "Only if R + W > N. Here R + W = 2 ≤ 3, so the single read can hit two nodes that never got the W=1 write. You need R + W > N for guaranteed overlap." },
    { label: "No, and the fix is to raise N to 5.", misconception: "Raising N without raising R and W keeps R + W ≤ N and makes overlap less likely, not more. The fix is the inequality R + W > N, e.g. W=2,R=2 at N=3." },
    { label: "Yes, because writes always go to all N replicas.", misconception: "A write only needs W acks to return; with W=1 just one replica is guaranteed to have it. A read of R=1 can hit a different replica. R + W > N is what forces overlap." },
  ]}
/>

## Snippet 2 — the shortener estimate

```python
def shortener_capacity(new_urls_per_month: float, read_write_ratio: float = 100):
    writes_per_sec = new_urls_per_month / (30 * 86_400)
    reads_per_sec = writes_per_sec * read_write_ratio
    return round(writes_per_sec), round(reads_per_sec)

# 100 million new URLs/month
print(shortener_capacity(100_000_000))
```

<Quiz
  id="01-foundational-code-2"
  lessonSlug="quiz-code"
  lang="en"
  question="Roughly what does shortener_capacity(100_000_000) return, and which number drives the architecture?"
  choices={[
    { label: "~40 writes/sec and ~4,000 reads/sec. The read number drives everything — at ~100:1 the redirect read path (cache-aside over a KV store) is the system; the ~40 writes/sec is trivial.", correct: true },
    { label: "~40 writes/sec and ~40 reads/sec — reads and writes are equal.", misconception: "The ratio is 100:1, so reads = writes × 100 ≈ 4,000/sec, not 40. The whole point of a shortener is that reads massively dominate, which is why you cache the redirect path." },
    { label: "~100,000,000 writes/sec — that's the monthly figure.", misconception: "That's per MONTH. Dividing by ~2.6M seconds/month gives ~40 writes/sec. Always convert the monthly total to per-second before reasoning about load." },
    { label: "~4,000 writes/sec and ~40 reads/sec.", misconception: "You swapped them. Writes are the small number (~40/sec); reads are writes × 100 ≈ 4,000/sec. Reads dominate, so the redirect/cache path is where the design goes." },
  ]}
/>

## Snippet 3 — the bloom-filter sizing

```python
# crawler seen-URL set
exact_set_urls = 10_000_000_000       # 10 billion seen URLs
bytes_per_url_exact = 100             # ~100 bytes/URL stored exactly
bits_per_url_bloom = 10               # ~1% false-positive target

exact_ram_bytes = exact_set_urls * bytes_per_url_exact
bloom_ram_bytes = exact_set_urls * bits_per_url_bloom / 8
```

<Quiz
  id="01-foundational-code-3"
  lessonSlug="quiz-code"
  lang="en"
  question="Roughly how much RAM does each approach need, and what does the bloom filter cost you?"
  choices={[
    { label: "Exact set ≈ 1 TB (10^10 × 100 B); bloom filter ≈ 12.5 GB (10^10 × 10 / 8). The bloom filter is ~80x smaller, at the cost of occasionally flagging a new URL as seen and skipping it — fine at this scale.", correct: true },
    { label: "Both ≈ 1 TB — the bloom filter doesn't save memory.", misconception: "The bloom filter uses ~10 bits per URL vs 100 bytes (800 bits) exact — roughly 80x less, ~12.5 GB vs ~1 TB. Saving that memory is the entire reason to use it for web-scale dedup." },
    { label: "Bloom filter ≈ 1 TB; exact set ≈ 12.5 GB.", misconception: "Reversed. The exact set at 100 bytes/URL is ~1 TB; the bloom filter at ~10 bits/URL is ~12.5 GB. The bloom filter is the small one — that's the point." },
    { label: "Bloom filter ≈ 12.5 GB with zero downside.", misconception: "The memory math is right, but it's not free: a bloom filter has a tunable false-positive rate, so it may occasionally mark a genuinely new URL as seen and skip it. Acceptable here, but a real cost." },
  ]}
/>

## Snippet 4 — the Kafka partition count

```python
# Kafka-like topic sizing
target_throughput_mb_s = 1000     # ~1 GB/sec at peak
per_partition_mb_s = 10           # ordered, replicated write ceiling per partition
consumers_in_group = 8

partitions_needed = target_throughput_mb_s / per_partition_mb_s
```

<Quiz
  id="01-foundational-code-4"
  lessonSlug="quiz-code"
  lang="en"
  question="How many partitions does this topic need, and what's the consequence for the 8-consumer group?"
  choices={[
    { label: "~100 partitions (1000 / 10). With ~100 partitions and only 8 consumers in the group, each consumer reads ~12 partitions — fine; the group could scale up to 100 consumers max (one partition per consumer) before adding more does nothing.", correct: true },
    { label: "8 partitions — one per consumer.", misconception: "8 partitions cap throughput at 8 × 10 = 80 MB/s, far below the 1 GB/s target. You need ~100 partitions for throughput; consumer count is a separate limit (max consumers = partitions)." },
    { label: "~100 partitions, but only 8 can ever be used because there are 8 consumers.", misconception: "All ~100 partitions are written and read; with 8 consumers each simply handles ~12 partitions. Consumers ≤ partitions is the cap on parallelism, not a cap on how many partitions exist or are used." },
    { label: "~10 partitions (1 GB / 10 MB, treating units as equal).", misconception: "Watch the units: 1000 MB/s ÷ 10 MB/s = ~100 partitions, not 10. Mixing GB and MB drops a factor of ~10 — exactly the kind of unit slip that under-provisions a topic." },
  ]}
/>

</Explanation>

<KeyTakeaway>Every case decision is arithmetic in the config: R+W>N for quorum overlap, daily ÷ seconds × ratio for QPS, bits × count for the bloom filter, target ÷ per-partition for the partition count. Watch the units.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-foundational-code-retrieval"
  lang="en"
  questions={[
    { q: "How do you check a quorum config gives read-your-write, and what's the trap?", a: "Check R + W > N. If the sum is ≤ N (e.g. W=1, R=1 at N=3 → sum 2), the read and write sets can be disjoint, so a read can miss a just-written value. The fix is any setting with R + W > N (e.g. W=2, R=2). The trap is thinking 'I wrote it, so a read sees it' — that only holds when the quorums are forced to overlap by the inequality." },
    { q: "How do you size a crawler's bloom filter, and how many Kafka partitions for a throughput target?", a: "Bloom filter: ~10 bits/URL for a ~1% false-positive rate, so 10 billion URLs ≈ 10^10 × 10 / 8 ≈ 12.5 GB — vs ~1 TB for an exact set at 100 bytes/URL (~80x smaller), at the cost of occasionally skipping a new URL. Partitions: target throughput ÷ per-partition ceiling, e.g. 1000 MB/s ÷ 10 MB/s ≈ 100 partitions — and max consumers in a group = partition count, so ~100 partitions supports up to 100 parallel consumers. Watch units: mixing GB and MB drops a factor of 10." },
  ]}
/>

<Recap lang="en">
Every design decision in these cases reduces to arithmetic you read straight off the config. The **quorum** check is `R + W > N`: a config of W=1, R=1 at N=3 sums to 2 ≤ 3, so reads can miss writes — fix with W=2, R=2. The **shortener estimate** converts 100M URLs/month to ~40 writes/sec and, at 100:1, ~4,000 reads/sec — and the read number is the one that drives the cache-and-redirect architecture. The **bloom-filter sizing** shows ~10 bits/URL gives ~12.5 GB for 10 billion URLs versus ~1 TB exact (~80x smaller), at the cost of a small false-positive rate. The **partition count** is target throughput ÷ per-partition ceiling (1000 ÷ 10 ≈ 100 partitions), which also caps consumer parallelism at the partition count. The senior habit is the same across all four: find the numbers, do the math, watch the units, and pick the fix the arithmetic supports — not the one that hides it.
</Recap>
