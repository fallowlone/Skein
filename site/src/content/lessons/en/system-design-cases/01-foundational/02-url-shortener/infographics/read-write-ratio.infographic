infographic chart-column-simple
data
  title Peak load: writes vs reads (per second)
  desc ~100:1 read-heavy — the redirect path is the whole system
  values
    - label Writes / sec (peak)
      value 120
    - label Reads / sec (peak)
      value 12000
theme
  palette #f59e0b #6366f1
