---
slug: "quiz-short"
lang: en
track: "system-design-cases"
unit: "03-media-storage"
order: 6
title: "Media & storage: free-recall review"
summary: "Free-recall prompts across the media-storage cases. Reconstruct each design in your own words first — the bytes/metadata split, the transcoding pipeline, chunking and conflict resolution, durability via erasure coding, and email at scale — then reveal and compare."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - control-vs-data-path
  - transcoding
  - chunking-dedup
  - erasure-coding
  - per-user-partitioning
  - incremental-sync
prereqs:
  - system-design-cases/03-media-storage/01-youtube
  - system-design-cases/03-media-storage/02-google-drive
  - system-design-cases/03-media-storage/04-distributed-email
sources:
  - https://netflixtechblog.com/high-quality-video-encoding-at-scale-d159db052746
  - https://docs.aws.amazon.com/AmazonS3/latest/userguide/DataDurability.html
  - https://github.com/donnemartin/system-design-primer
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, reconstruct a full design from memory — the data flow, the split, the failure mode it avoids — before you open the model answer. The effort of rebuilding the architecture is what makes it stick.
</Hook>

<Crux>Reconstruct the spine: the bytes/metadata split, transcoding for adaptive bitrate, chunking, erasure-coded durability, per-user email.</Crux>

<Explanation>

Reconstruct each design without looking back: how YouTube's write and read paths split, what transcoding produces and why, how Drive moves the fewest bytes and resolves conflicts, how an object store reaches eleven nines cheaply, and how email scales by partitioning everything per user.

<RetrievalDrawer
  client:visible
  id="03-media-short-retrieval"
  lang="en"
  questions={[
    { q: "Reconstruct YouTube's two paths and the rule that governs both.", a: "Write path (heavy, async): the client uploads bytes directly to object storage via a presigned URL, the API writes a small metadata row and enqueues a transcode job, and a worker fleet transcodes the source — fanning it into a ladder of renditions (240p…4K) cut into short segments with an HLS/DASH manifest, done as parallel chunk-transcode-then-stitch. Read path (fast): the viewer pulls the manifest and segments from a CDN near them, which origin-pulls from object storage only on a miss (near-100% edge hit for popular content). The rule governing both: the API stays on the control path, never the data path — bytes never touch the app server or the database. View counts are eventually consistent (logged and aggregated), and egress, not storage, dominates cost." },
    { q: "Why does transcoding produce a ladder of segmented renditions plus a manifest?", a: "Because the viewer's bandwidth isn't yours to control and changes mid-video. Transcoding produces a ladder of renditions (multiple resolutions/bitrates, often multiple codecs), each cut into short 2–10s segments, with a manifest (HLS .m3u8 / DASH .mpd) listing each rendition's segment URLs. This enables adaptive bitrate streaming: the player picks, per segment, the highest rendition its current measured bandwidth sustains, switching seamlessly because all renditions share segment boundaries — avoiding both buffering (file too big for the pipe) and mush (file too small for the screen). It's done as parallel fan-out (split → transcode chunks across workers → stitch) so a long upload finishes in minutes." },
    { q: "How does Drive move the fewest bytes, and how does it resolve conflicting edits without data loss?", a: "Content-addressed chunking: split each file into chunks keyed by content hash, so syncing a change re-chunks, compares hashes, and uploads only chunks with new hashes (delta sync — a one-paragraph edit moves ~4 MB not 200 MB), with dedup (identical chunks stored once across versions/files/users) and integrity falling out free. The file splits into metadata (versioned chunk list, transactional DB) and chunks (immutable bytes, blob store); upload chunks first, then commit the version. Conflicts: each version records its parent, so a divergent sync (parent no longer the head) is detected and resolved by keeping a conflicted copy (or auto-merging collaborative docs via OT/CRDT) — never a silent last-writer-wins overwrite." },
    { q: "How does an S3-like store reach ~11 nines durability cheaply, and where does consistency live?", a: "Spread redundancy across independent failure domains and use erasure coding for large cold data: split into k data + m parity shards (e.g. 10+4), reconstruct from any k, tolerate m losses — ~1.4× overhead for 4-loss tolerance vs 3× replication for only 2 — decisive at exabyte scale; still replicate small hot objects whose reconstruction fan-out would hurt. The interface is a flat key→bytes namespace (no real directories; LIST prefix is a range scan). Consistency lives in the metadata service: its commit is the linearization point, so an object is fully indexed or invisible, and strong read-after-write comes from not acking a PUT until metadata is durable. Durability is a race: background repair must rebuild failed shards faster than new failures accumulate. Multipart upload makes huge objects reliable via independent, parallel, retryable parts with an atomic complete." },
    { q: "How does Gmail-scale email scale, and name two heavy-byte and structured-state decisions.", a: "Partition everything by user — message store, per-user inverted search index, and change log all shard by user ID, because mailboxes are independent with no cross-user joins on the hot path. Spam (~90% of inbound) is rejected at SMTP ingest before storage (cheap IP/auth checks then content classification) so you never pay durable storage for discarded mail. Heavy bytes: attachments are split off and stored content-addressed and deduplicated in object storage (a newsletter PDF to 50M users is one ref-counted blob with 50M pointers, not 50M copies). Structured state: search is a per-user inverted index (not a scan, sub-100 ms), labels are a many-to-many overlay (not folders, so reorganizing moves no bytes), and clients sync incrementally via a per-mailbox monotonic change log (HISTORYID/MODSEQ/JMAP state) — O(changes), not O(mailbox)." },
  ]}
/>

</Explanation>

<KeyTakeaway>Reconstruct each design from memory — YouTube's two paths, the transcoding ladder, Drive's chunking, erasure-coded durability, per-user email — and you hold the spine: separate heavy bytes from structured state.</KeyTakeaway>

<Recap lang="en">
If you could reconstruct each design from memory, you hold the unit's spine. **YouTube** splits an async write path (direct-to-object-store upload, transcode into a segmented rendition ladder + manifest for **adaptive bitrate**) from a fast read path (viewer→CDN), keeping the API on the control path and making view counts eventually consistent. **Drive** moves the fewest bytes via **content-addressed chunking** (delta sync + dedup) and never overwrites — the version chain detects conflicts, resolved as conflicted copies. An **object store** reaches ~11 nines cheaply with **erasure coding** (10+4 ≈ 1.4× vs 3× replication) for cold data, keeps consistency in the metadata service, and uses multipart upload for huge objects. **Email** partitions everything by user, rejects spam at ingest, deduplicates attachments in object storage, serves search from a per-user inverted index, models labels as a many-to-many overlay, and syncs incrementally via a change log. The single thread: separate the heavy bytes (object storage, deduplicated, CDN-served) from the structured state (metadata, indexes, versions, change logs), and spend your consistency budget only where a human would notice.
</Recap>
