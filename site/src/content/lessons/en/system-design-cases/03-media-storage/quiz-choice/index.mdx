---
slug: "quiz-choice"
lang: en
track: "system-design-cases"
unit: "03-media-storage"
order: 5
title: "Media & storage: multiple-choice review"
summary: "Multiple-choice synthesis across the media-storage cases: the control-path/data-path split, transcoding ladders and adaptive bitrate, content-addressed chunking and delta sync, erasure coding for durability, and per-user partitioning for email."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - control-vs-data-path
  - adaptive-bitrate
  - chunking-dedup
  - erasure-coding
  - per-user-partitioning
  - eventual-consistency
prereqs:
  - system-design-cases/03-media-storage/02-google-drive
  - system-design-cases/03-media-storage/03-object-storage
  - system-design-cases/03-media-storage/04-distributed-email
sources:
  - https://docs.aws.amazon.com/AmazonS3/latest/userguide/DataDurability.html
  - https://developer.mozilla.org/en-US/docs/Web/Media/Guides/Streaming/HLS
  - https://github.com/donnemartin/system-design-primer
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each is a decision you make at a whiteboard designing a media system — keeping bytes off the app path, choosing a redundancy scheme, picking a partition key — not a definition to recite.
</Hook>

<Crux>Six decisions: the data-path split, adaptive bitrate, chunking and dedup, erasure coding, per-user partitioning, eventual consistency.</Crux>

<Explanation>

Confirm you can keep large bytes off the app servers, justify a transcoding ladder, reason about content-addressed chunking and delta sync, choose erasure coding vs replication on cost, partition email by user, and recognize where a number can safely be approximate.

<Quiz
  id="03-media-choice-1"
  lessonSlug="quiz-choice"
  lang="en"
  question="Across YouTube, Drive, object storage, and email, what single principle governs how large bytes move?"
  choices={[
    { label: "Keep bytes off the app server's data path: upload them directly to object storage (presigned URL / multipart), reference them by key, and serve them from a CDN or the data plane — the API stays on the control path.", correct: true },
    { label: "Stream bytes through the API tier so it can validate and transform them centrally.", misconception: "Proxying multi-GB bytes through the API makes it the bottleneck and conflates control with data. Every design in this unit keeps the app server on the control path: bytes go direct to object storage and are served from a CDN/data plane." },
    { label: "Store bytes in the primary database as BLOBs for transactional safety.", misconception: "BLOBs in the database is the anti-pattern this whole unit rejects — petabytes don't belong in a relational store. Bytes live in object storage; the DB holds small metadata that references them." },
    { label: "Cache the full media objects in Redis so reads are always fast.", misconception: "In-memory caches hold small hot objects, not GB media; you'd blow memory on a few files. Large media is served from a CDN (purpose-built for big byte ranges), and the metadata/small objects from Redis." },
  ]}
/>

<Quiz
  id="03-media-choice-2"
  lessonSlug="quiz-choice"
  lang="en"
  question="Why does a video platform transcode one upload into a ladder of renditions cut into short segments with a manifest?"
  choices={[
    { label: "To enable adaptive bitrate streaming: the player picks, per segment, the highest rendition its current bandwidth sustains, switching seamlessly between segments because they share boundaries — avoiding buffering and mush as the network changes.", correct: true },
    { label: "To save storage by keeping only the smallest rendition.", misconception: "It costs MORE storage (many renditions), not less. The purpose is adaptive bitrate: serve each viewer the best quality their changing bandwidth allows, from a phone on 3G to a TV on fibre, from the same source." },
    { label: "Because object storage can't store a single large file.", misconception: "Object storage handles huge objects fine (multipart upload). The ladder exists for adaptive playback across heterogeneous devices and networks, not a storage limitation." },
    { label: "To let the CDN compress the video further at the edge.", misconception: "CDNs cache, they don't transcode. The renditions are produced up front by the transcoding pipeline so the player can switch bitrate per segment to match the viewer's network." },
  ]}
/>

<Quiz
  id="03-media-choice-3"
  lessonSlug="quiz-choice"
  lang="en"
  question="A user edits one paragraph of a 200 MB file in Drive. What makes the sync efficient, and what extra benefit comes free?"
  choices={[
    { label: "Content-addressed chunking: re-chunk, compare chunk hashes, upload only the 1–2 changed chunks (delta sync) — and identical chunks across versions/files/users are stored once (deduplication), which falls out of the same hashing.", correct: true },
    { label: "Gzip the whole file before upload so it transfers faster.", misconception: "Compression still moves the whole 200 MB for a one-line edit. Chunking + hash comparison moves only the changed chunk (delta sync), orders of magnitude less, and gives dedup for free." },
    { label: "Diff the file and store a patch in the metadata database.", misconception: "Byte diffs don't belong in the metadata DB and reinvent chunking poorly. Content-addressed chunks give delta sync, dedup, and integrity in one mechanism — upload only chunks with new hashes." },
    { label: "Upload the whole file but mark it low-priority so it transfers in the background.", misconception: "You're still moving 200 MB for a one-line change. The efficient design uploads only the changed chunks via content-hash comparison; dedup is the free bonus." },
  ]}
/>

<Quiz
  id="03-media-choice-4"
  lessonSlug="quiz-choice"
  lang="en"
  question="An S3-like store must hit ~11 nines of durability for exabytes of cold data at minimum storage cost. Which scheme, and why?"
  choices={[
    { label: "Erasure coding (e.g. 10+4): 10 data + 4 parity shards across independent failure domains, reconstruct from any 10, tolerate 4 losses — ~1.4× overhead vs 3× replication, with better fault tolerance. Decisive at exabyte scale.", correct: true },
    { label: "Triple replication, since more copies always means more durability.", misconception: "3× costs 200% overhead and tolerates only 2 losses; erasure coding (10+4) hits 11 nines at ~40% overhead AND tolerates 4 losses. For large cold data, replication's storage cost is ruinous at exabyte scale." },
    { label: "A single copy with strong checksums to catch corruption.", misconception: "Checksums detect corruption but can't recover a failed disk — one death loses the only copy. Durability needs redundancy across independent failure domains (erasure shards or replicas), not just detection." },
    { label: "Erasure-code everything, including tiny hot objects, to maximize savings.", misconception: "Erasure coding costs read fan-out and CPU (gather k shards + reconstruct), bad for small hot objects. The right call is to replicate hot/small and erasure-code cold/large — matching the scheme to the object profile." },
  ]}
/>

<Quiz
  id="03-media-choice-5"
  lessonSlug="quiz-choice"
  lang="en"
  question="Designing Gmail-scale email, what is the natural partition key, and why does it make the system scale cleanly?"
  choices={[
    { label: "The user: mailboxes are independent with no cross-user joins on the hot path, so sharding the message store, search index, and change log all by user ID scales horizontally with no hot cross-shard queries.", correct: true },
    { label: "The message ID, so each message lands on a random shard for even spread.", misconception: "Sharding by message ID scatters one user's mail across all shards, so listing or searching a mailbox fans out everywhere. The user is the clean key because a mailbox's operations stay on one shard — no cross-user joins." },
    { label: "The sender's domain, to group mail by origin.", misconception: "Mail is read and organized per recipient, not per sender domain; that key would create huge hot partitions for popular senders and scatter a user's inbox. Partition by user — the unit of independent access." },
    { label: "The timestamp, so recent mail is co-located.", misconception: "Time-based partitioning makes the newest partition a write hotspot and still scatters each user's mailbox across time shards. The user is the natural key: independent mailboxes, no cross-user joins." },
  ]}
/>

<Quiz
  id="03-media-choice-6"
  lessonSlug="quiz-choice"
  lang="en"
  question="A video's view count gets hundreds of thousands of increments per second. Which design respects the eventual-consistency lesson?"
  choices={[
    { label: "Log view events to a stream and aggregate them asynchronously, refreshing the displayed count periodically — nobody can tell 1,000,000 from 1,000,200, so trade exact freshness for write scalability.", correct: true },
    { label: "Synchronously UPDATE a single counter row per view, inside a transaction for correctness.", misconception: "That's one hot row every viewer contends on — write throughput collapses on lock waits. Counts are exactly where eventual consistency is safe: log events and aggregate asynchronously." },
    { label: "Require the count to be strongly consistent because users trust exact numbers.", misconception: "Users can't perceive the difference between 1,000,000 and 1,000,200, and strong consistency on a viral counter melts under load. Spend your consistency budget on things humans notice, like a creator seeing their own upload." },
    { label: "Shard the videos table so the counter row splits across shards.", misconception: "One video's count is a single logical value; sharding the table doesn't split that row's contention. The fix is to stop updating synchronously — log events to a stream and aggregate." },
  ]}
/>

</Explanation>

<KeyTakeaway>Media systems are structure decided up front: keep bytes off the app path, transcode for adaptive bitrate, chunk for delta sync and dedup, erasure-code cold data, partition by user, eventual consistency for counters.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-media-choice-retrieval"
  lang="en"
  questions={[
    { q: "What's the recurring rule for moving large bytes, and why?", a: "Keep bytes off the app server's data path: the client uploads them directly to object storage (presigned URL or multipart), the service stores only a key/metadata reference, and reads are served from a CDN or the data plane. The API stays on the control path. This is why YouTube, Drive, object storage, and email all separate a metadata/control layer from the byte storage — proxying GB-scale bytes through the app tier or stuffing them in a database is the anti-pattern the whole unit rejects." },
    { q: "When do you erasure-code vs replicate, and why partition email by user?", a: "Erasure-code large cold data (e.g. 10+4: reconstruct from any 10 of 14 shards, tolerate 4 losses at ~1.4× overhead vs 3× replication's 200% for only 2-loss tolerance) — decisive on storage cost at exabyte scale; but replicate small hot objects whose read fan-out and reconstruction CPU would hurt. Email partitions by user because mailboxes are independent with no cross-user joins on the hot path, so the message store, per-user inverted search index, and change log all shard by user ID and scale horizontally with no hot cross-shard queries." },
  ]}
/>

<Recap lang="en">
The through-line across all four cases is that media systems are shaped before you build them. **Keep large bytes off the app server's data path** — upload direct to object storage, reference by key, serve from a CDN/data plane — so the API stays on the control path. **Transcode** one upload into a ladder of segmented renditions so **adaptive bitrate** can match each viewer's changing network. **Content-addressed chunking** gives **delta sync** (move only changed chunks) and **dedup** (identical chunks stored once) from one mechanism. **Erasure coding** (10+4 ≈ 1.4× overhead) hits 11 nines far cheaper than 3× replication for cold data, while you still replicate small hot objects. **Partition email by user** because mailboxes have no cross-user joins on the hot path. And accept **eventual consistency** where humans can't tell — a viral view counter is logged and aggregated, never synchronously incremented on one hot row. Get the structure right and the rest is detail; get it wrong and no tuning saves you.
</Recap>
