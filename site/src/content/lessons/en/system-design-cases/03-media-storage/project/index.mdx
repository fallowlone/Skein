---
slug: "project"
lang: en
track: "system-design-cases"
unit: "03-media-storage"
order: 8
title: "Media & storage: build a chunked, deduplicated upload-and-sync service"
summary: "Hands-on project: build a small file service that uploads bytes direct to object storage, splits files into content-addressed chunks for delta sync and dedup, stores metadata separately, and resolves conflicts via a version chain — the spine of every design in this unit."
estMin: 300
status: ready
lessonType: topic
level: senior
concepts:
  - control-vs-data-path
  - chunking-dedup
  - delta-sync
  - conflict-resolution
  - object-storage
  - metadata-vs-blob
prereqs:
  - system-design-cases/03-media-storage/01-youtube
  - system-design-cases/03-media-storage/02-google-drive
  - system-design-cases/03-media-storage/03-object-storage
sources:
  - https://dropbox.tech/infrastructure/streaming-file-synchronization
  - https://docs.aws.amazon.com/AmazonS3/latest/userguide/DataDurability.html
  - https://github.com/donnemartin/system-design-primer
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Reading that "bytes go to object storage and you sync deltas via content-addressed chunks" is not the same as building a service that actually does it. Build a small but real upload-and-sync service: bytes never touch your app server's data path, files are stored as deduplicated chunks, an edit syncs only the changed chunks, and two conflicting edits both survive. When you're done you'll have implemented, in miniature, the spine shared by YouTube, Drive, and S3.
</Hook>

<Crux>Build a sync service: bytes direct to object store, content-addressed chunks for delta sync and dedup, separate metadata, version-chain.</Crux>

<Explanation>

This project makes the unit operational. You'll separate the control path from the data path, implement content-addressed chunking and measure the delta-sync and dedup wins, store metadata apart from bytes, and prove that the version chain prevents the silent-overwrite failure that the Drive lesson warns about.

<ProjectBrief
  id="03-media-storage-project"
  lessonSlug="project"
  lang="en"
  objective="Build a small file upload-and-sync service that stores files as content-addressed chunks in an object store (real S3/MinIO or a local blob directory), keeps metadata in a separate database, syncs edits by transferring only changed chunks, deduplicates identical chunks, and resolves conflicting edits via a version chain — then measure the bandwidth and storage wins your design produces."
  requirements={[
    "Stand up an object store (MinIO, S3, or a local directory acting as a blob store) and a separate metadata database (SQLite/Postgres). Define the split clearly: chunks (immutable bytes, keyed by content hash) live in the object store; file metadata (name, version, ordered chunk-hash list) lives in the DB.",
    "Implement upload so bytes go straight to the blob store, not through an in-memory buffer or a DB column: split the file into fixed-size chunks (e.g. 1–4 MB), hash each (SHA-256), upload only chunks whose hash isn't already stored, then commit a new file version listing the chunk hashes — chunks first, metadata second.",
    "Implement download/read by looking up the version's chunk list in metadata and streaming the chunks from the blob store in order; put a small cache (in-memory or Redis) in front of the metadata lookups (not the chunk bytes).",
    "Implement delta sync: given an edited file, re-chunk it, diff its chunk hashes against the previous version, upload only the new-hash chunks, and commit a new version — log how many bytes you uploaded vs the file size.",
    "Implement deduplication and prove it: upload the same file (or a shared chunk) under two different names/users and show only one physical copy is stored; keep a reference count so a chunk is deleted only when its last referencing version is gone.",
    "Implement conflict resolution: have each version record its parent. When a sync arrives whose parent is no longer the current head (simulate two clients editing from the same base), detect the divergence and keep BOTH as conflicted copies instead of overwriting — never lose an edit.",
    "Add a tiny multipart-style upload for one large file: split into parts, upload parts independently with one part deliberately failing and retried alone, then 'complete' to assemble — show the object becomes visible only after completion.",
    "Measure and report: for a representative edit (e.g. change one chunk of a 100 MB file), report bytes moved by delta sync vs a naive full re-upload, and the storage saved by dedup across duplicate uploads — to one significant figure.",
  ]}
  acceptance={[
    "A working service where an upload stores chunks in the object store and a metadata row in the DB, with NO file bytes ever held in a DB column or buffered whole in app memory — demonstrate this with a multi-hundred-MB file.",
    "A delta-sync demonstration: editing one chunk of a large file uploads only ~one chunk's worth of bytes, with a logged before/after byte count showing the order-of-magnitude saving vs full re-upload.",
    "A dedup demonstration: the same content stored twice occupies one physical chunk (shown via the blob store's object count and the reference count), and deleting one referencing version does not delete the still-referenced chunk.",
    "A conflict demonstration: two edits from the same parent version both survive as conflicted copies, with the version chain (parent pointers) shown — a last-writer-wins overwrite must NOT occur.",
    "A short write-up: the metadata-vs-blob split you chose and why, the measured delta-sync and dedup wins to one significant figure, and one paragraph on what would change to reach S3-like durability (erasure coding across failure domains) at real scale.",
  ]}
  stretch={[
    "Replace fixed-size chunking with content-defined chunking (a rolling hash boundary) and demonstrate that inserting bytes at the START of a file now re-syncs only the affected chunk instead of every subsequent chunk — quantify the difference.",
    "Add a push-style change notification: instead of clients polling for changes, have the server notify a connected client (WebSocket/long-poll) when a file changes, and have the client then pull only the metadata diff and missing chunks.",
    "Add an eventually-consistent counter (e.g. download count) implemented by logging events and aggregating asynchronously, and contrast its throughput with a synchronous per-event UPDATE under a load test.",
    "Simulate erasure coding: split a chunk into k data + m parity fragments, store them in separate 'nodes' (directories), delete up to m fragments, and reconstruct the chunk from the survivors — measuring the storage overhead vs a 3-copy replication of the same chunk.",
  ]}
/>

</Explanation>

<KeyTakeaway>Building the service makes the unit real: keep bytes off the app path, store content-addressed chunks separately from metadata, sync only changed chunks, dedup identical ones, resolve conflicts via the version chain.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-media-storage-project-retrieval"
  lang="en"
  questions={[
    { q: "What is the core structural split the project forces you to build, and why?", a: "The metadata-vs-blob split: immutable, content-addressed chunks (keyed by content hash) live in an object/blob store, while file metadata (name, version, ordered chunk-hash list, parent pointer) lives in a separate transactional database. They have opposite needs — bytes are large, immutable, write-once-read-many (cheap durable bulk storage), while metadata is small, queried, and transactionally updated (indexes + ACID). Building both proves why every design in the unit keeps bytes off the app's data path: upload chunks directly to the blob store, then commit a metadata version referencing them (chunks first, metadata second)." },
    { q: "How do you demonstrate delta sync, dedup, and conflict safety, and what would scale them to S3-like durability?", a: "Delta sync: edit one chunk of a large file, diff chunk hashes against the prior version, upload only the new-hash chunk, and log bytes-moved vs file-size (an order-of-magnitude win). Dedup: store identical content twice and show one physical chunk plus a reference count, so deleting one version keeps the still-referenced chunk. Conflict safety: each version records its parent, so a sync whose parent isn't the current head is detected as a branch and BOTH edits survive as conflicted copies — never last-writer-wins. To reach S3-like durability at real scale you'd replace single-copy chunk storage with erasure coding across independent failure domains (k+m shards, reconstruct from any k) for cold data and replication for hot, with background repair outpacing failures." },
  ]}
/>

<Recap lang="en">
This project turns the media-storage unit into a thing you have built. You stand up an **object store** for bytes and a **separate metadata database**, then implement the spine shared by YouTube, Drive, and S3: **upload bytes directly to the blob store** (never through an app-memory buffer or a DB column), split files into **content-addressed chunks**, and commit a metadata **version** listing the chunk hashes — chunks first, metadata second. From that one mechanism you get **delta sync** (edit a chunk, upload only that chunk, and measure the order-of-magnitude bandwidth saving), **deduplication** (identical chunks stored once with a reference count), and **integrity** for free. You make the system safe with a **version chain** — each version records its parent, so two edits from the same base both survive as **conflicted copies** rather than a silent overwrite — and you add a **multipart-style** upload to make a large file's transfer reliable. The deliverable is not just code but **measurement**: the delta-sync and dedup wins to one significant figure, and a paragraph on what reaching real **S3-like durability** would require (erasure coding across failure domains). Build this once and the unit's designs stop being diagrams and become muscle memory.
</Recap>
