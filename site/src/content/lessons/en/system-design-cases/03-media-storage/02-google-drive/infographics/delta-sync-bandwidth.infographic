infographic chart-column-simple
data
  title One-paragraph edit to a 200 MB file: bytes moved per device
  desc Naive re-upload vs delta sync of only the changed chunks — a 25x win
  values
    - label Naive full re-upload (MB)
      value 200
    - label Delta sync, 1-2 chunks (MB)
      value 8
theme
  palette #f43f5e #14b8a6
