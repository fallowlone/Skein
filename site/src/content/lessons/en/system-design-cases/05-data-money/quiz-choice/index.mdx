---
slug: "quiz-choice"
lang: en
track: "system-design-cases"
unit: "05-data-money"
order: 7
title: "Data & money: multiple-choice review"
summary: "Multiple-choice synthesis across the data-and-money cases: cardinality, exactly-once and watermarks, idempotency and the double-entry ledger, the lost update, exchange determinism, and the double-booking race."
estMin: 13
status: ready
lessonType: topic
level: senior
concepts:
  - cardinality
  - exactly-once
  - idempotency
  - lost-update
  - determinism
  - double-booking
prereqs:
  - system-design-cases/05-data-money/01-metrics-monitoring
  - system-design-cases/05-data-money/03-payment-system
  - system-design-cases/05-data-money/04-digital-wallet
  - system-design-cases/05-data-money/05-stock-exchange
sources:
  - https://prometheus.io/docs/practices/instrumentation/
  - https://docs.stripe.com/api/idempotent_requests
  - https://martinfowler.com/articles/lmax.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the unit. Each is a decision you make in a design review where the output is money or correctness-critical data — not a definition to recite, but the reasoning that prevents a double-charge, a cardinality OOM, or a double-booking.
</Hook>

<Crux>Six decisions across the cases: cardinality, exactly-once, idempotency, the lost update, exchange determinism, and double-booking.</Crux>

<Explanation>

Confirm you can spot the cardinality bomb, separate exactly-once from late events, apply an idempotency key, defeat a lost update, justify a single-threaded exchange core, and prevent a double-booking.

<Quiz
  id="05-data-money-choice-1"
  lessonSlug="quiz-choice"
  lang="en"
  question="A metrics team adds a label `user_id` (millions of values) to a high-traffic metric. What breaks, and why?"
  choices={[
    { label: "Active-series cardinality explodes: series count is the product of label cardinalities, so one unbounded label multiplies series into the billions and exhausts the TSDB's memory/index — the sample rate barely changes. Put high-cardinality values in logs/traces, not labels.", correct: true },
    { label: "Ingest throughput doubles; add a faster write path.", misconception: "The sample RATE barely moves; what explodes is the ACTIVE-SERIES count (the product of label cardinalities), which sets the memory/index footprint. A faster write path doesn't help a cardinality blowup." },
    { label: "Nothing — labels are just extra columns costing a few bytes each.", misconception: "A label isn't a column; it forks a whole new time series the DB tracks with index + memory overhead. A unique-per-user label means millions of series — the classic OOM, not 'a few bytes'." },
    { label: "Disk fills but memory is fine.", misconception: "The binding constraint is MEMORY for active series (index + open chunks), not disk. Unbounded cardinality blows the in-memory index first; that's what kills the box." },
  ]}
/>

<Quiz
  id="05-data-money-choice-2"
  lessonSlug="quiz-choice"
  lang="en"
  question="In ad-click aggregation, a click arrives 30 minutes late; its event-time window closed long ago. Exactly-once is enabled. Is it counted, and how do you still bill it?"
  choices={[
    { label: "Exactly-once doesn't help — it prevents double-APPLYING events the engine sees, not late events the window excluded. The streaming layer drops it; the batch reconciliation layer re-reads all raw events with no time pressure and includes it in the authoritative total.", correct: true },
    { label: "Exactly-once guarantees it's counted in its window.", misconception: "Exactly-once is about not double-applying; lateness is a windowing/watermark concern. A window that already closed won't admit it — the batch layer recovers it, not the exactly-once mechanism." },
    { label: "Set the watermark lag to 30+ minutes so nothing is late.", misconception: "That taxes EVERY result with 30 min of staleness to rescue a rare straggler. Size the watermark for the common case and let the batch layer (no time pressure) catch the tail." },
    { label: "It lands in the current window, counted in the wrong minute — fine for billing.", misconception: "Aggregation is by EVENT time precisely so it doesn't land in the wrong minute. Its real window closed; the stream drops it and batch reconciliation recovers it." },
  ]}
/>

<Quiz
  id="05-data-money-choice-3"
  lessonSlug="quiz-choice"
  lang="en"
  question="A client charges a card, the PSP succeeds, but the response is lost; the client retries. What prevents the double charge, and where must the key originate?"
  choices={[
    { label: "A client-generated idempotency key sent on both attempts: the server's atomic check-and-store finds the key on retry and returns the original result instead of charging again. The client must mint it before the first attempt, since only a pre-existing key survives a lost response.", correct: true },
    { label: "A server-generated transaction ID reused on retry.", misconception: "If the response was lost, the client never received the server's ID, so the retry can't reuse it — it looks like a fresh payment. The key must come from the client before the first attempt." },
    { label: "Wrap the charge in a DB transaction so it's atomic.", misconception: "A local transaction can't span the external PSP call, and it doesn't help when the RESPONSE is lost after the PSP already charged. You need a client idempotency key to recognize the retry." },
    { label: "Don't retry on timeout.", misconception: "Not retrying risks a genuinely-missed charge (the timeout is ambiguous). The correct move is to retry SAFELY with an idempotency key, so duplicates are recognized and missed charges still complete." },
  ]}
/>

<Quiz
  id="05-data-money-choice-4"
  lessonSlug="quiz-choice"
  lang="en"
  question="Two concurrent debits ($70, $60) both read a $100 wallet balance, both pass their check, both write back, and the wallet overdraws. What is this and the cleanest single-wallet fix?"
  choices={[
    { label: "The lost-update problem: a read-write gap lets both compute from stale $100. Fix with an atomic conditional write — UPDATE … SET balance = balance - 70 WHERE balance >= 70 — making check-and-decrement one indivisible op; the second sees 30 and affects zero rows.", correct: true },
    { label: "A deadlock; add a retry loop.", misconception: "Nothing deadlocks — both succeed, which is the bug. It's a lost update from a read-modify-write race. A retry loop doesn't close the read-write gap; an atomic conditional write (or a lock) does." },
    { label: "Replication lag; read from the primary.", misconception: "It happens on a single primary — two concurrent transactions reading the same row before either writes. The fix is concurrency control (atomic conditional write / SELECT FOR UPDATE), not where you read." },
    { label: "Eventual consistency; use a stronger store.", misconception: "Even a strongly consistent store loses the update if the app reads-decides-writes with a gap. Make the decide-and-write atomic at the DB or hold a row lock — not merely change the consistency model." },
  ]}
/>

<Quiz
  id="05-data-money-choice-5"
  lessonSlug="quiz-choice"
  lang="en"
  question="An exchange's matching core is single-threaded over a sequenced stream. An engineer proposes multiple matching threads 'to go faster.' Why is that wrong?"
  choices={[
    { label: "Fairness needs one total order; concurrent matching against the same book creates a race where 'who filled first' depends on scheduling, replicas diverge, and replay can't reproduce trades. The single thread is also faster here (no locks, book in cache). Scale by partitioning across instruments, not one book.", correct: true },
    { label: "It's fine — more threads always help throughput.", misconception: "For a matching book, concurrency destroys the single total order that fairness and audit require, and adds lock/cache contention. The core is deliberately single-threaded; you parallelize everything AROUND it." },
    { label: "Threads are fine if you add locks around the book.", misconception: "Locks reintroduce scheduling-dependent ordering (nondeterminism) and contention, defeating the purpose. A single thread over a sequenced stream needs no locks and stays deterministic." },
    { label: "Only the durability log needs to be single-threaded.", misconception: "It's the MATCHING core that must be single-threaded for a deterministic total order; the log is appended in that same order. Parallel matching breaks determinism regardless of the log." },
  ]}
/>

<Quiz
  id="05-data-money-choice-6"
  lessonSlug="quiz-choice"
  lang="en"
  question="Two guests book the last room for the same night within a second; both saw '1 available' and both got confirmations. What is this, and the correct fix for a multi-night stay?"
  choices={[
    { label: "The double-booking problem — a lost-update race on inventory. Fix by making check-and-reserve one atomic, all-or-nothing transaction that conditionally decrements the counter for EVERY night in the range; the second booking matches zero rows and is rejected.", correct: true },
    { label: "A caching bug; disable the availability cache.", misconception: "Even a perfectly fresh read lets two requests see '1' before either writes. The guarantee belongs at the atomic check-and-reserve; search may be slightly stale and advisory." },
    { label: "Decrement each night with independent updates, no transaction.", misconception: "Per-night updates must be ALL-OR-NOTHING in one transaction, or a 3-night stay books 2 nights then fails on the 3rd. And without atomicity per night you still have the race." },
    { label: "Make availability search strongly consistent.", misconception: "Search is ~100× the traffic; forcing it strongly consistent kills scalability and STILL can't guarantee the room (someone books between search and click). The guarantee is the atomic reserve." },
  ]}
/>

</Explanation>

<KeyTakeaway>Data-and-money correctness is a checklist of named hazards: the cardinality bomb, exactly-once, the idempotency key, the lost update, exchange determinism, and the double-booking race — each with a precise, atomic fix.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-data-money-choice-retrieval"
  lang="en"
  questions={[
    { q: "Why is exactly-once not the answer to a late event?", a: "Exactly-once prevents double-APPLYING events the engine processes (via at-least-once + idempotent/transactional application); a late event is one the time window already excluded. Lateness is a watermark/windowing concern — you size the watermark for the common case and recover true stragglers in the batch reconciliation layer, which re-reads all raw events with no time pressure to produce the authoritative total." },
    { q: "What's the common thread between the lost update and the double-booking?", a: "Both are the same read-modify-write race: two requests read a value (a balance, '1 available') before either writes, so both proceed and one overwrites the other — money created or a room sold twice. The fix is identical in principle: make check-and-act one atomic operation (an atomic conditional write / single ACID transaction / row lock) so there's no stale-read-then-write gap. The wallet does it per balance; the hotel does it per night across the range, all-or-nothing." },
  ]}
/>

<Recap lang="en">
The unit's hazards are a named checklist you run in any design review where the output is money or correctness-critical data. The **cardinality bomb**: series count is the product of label cardinalities, so an unbounded label OOMs the TSDB — keep labels bounded. **Exactly-once vs lateness**: exactly-once stops double-applying, not late events, which a watermark sizes for and batch reconciliation recovers. The **idempotency key**: client-minted, atomic check-and-store, so a retry after a lost response returns the original result instead of charging twice. The **lost update** and the **double-booking** are the same read-modify-write race — fixed by making check-and-act one atomic operation (per balance, or per night all-or-nothing). And **exchange determinism**: a single sequenced total order through a single-threaded core, so replicas and replay agree — never parallelize one book. Each hazard has a precise, atomic fix, and recognizing which one you're facing is the senior skill this unit builds.
</Recap>
