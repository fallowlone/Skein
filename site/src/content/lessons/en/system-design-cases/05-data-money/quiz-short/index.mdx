---
slug: "quiz-short"
lang: en
track: "system-design-cases"
unit: "05-data-money"
order: 8
title: "Data & money: free-recall review"
summary: "Free-recall prompts across the data-and-money cases. Reconstruct each design from memory — cardinality, exactly-once and reconciliation, idempotency and the ledger, the lost update, exchange determinism, double-booking — then reveal and compare."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - cardinality
  - exactly-once
  - idempotency
  - lost-update
  - determinism
  - double-booking
prereqs:
  - system-design-cases/05-data-money/01-metrics-monitoring
  - system-design-cases/05-data-money/02-ad-click-aggregation
  - system-design-cases/05-data-money/03-payment-system
  - system-design-cases/05-data-money/06-hotel-reservation
sources:
  - https://www.confluent.io/blog/exactly-once-semantics-are-possible-heres-how-apache-kafka-does-it/
  - https://martinfowler.com/eaaDev/EventSourcing.html
  - https://martinfowler.com/bliki/LambdaArchitecture.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, reconstruct a full answer from memory — the mechanism and the fix — before you open the model answer. The effort of rebuilding is what makes the cardinality rule, exactly-once, and the lost-update fix stick.
</Hook>

<Crux>Reconstruct the unit's spine: cardinality, exactly-once, idempotency and the ledger, the lost update, exchange determinism, double-booking.</Crux>

<Explanation>

Reconstruct each case's core decision without looking back: what kills a metrics DB, what exactly-once does and doesn't cover, how an idempotency key and a double-entry ledger keep money correct, how to defeat the lost update, why an exchange is single-threaded, and how to prevent a double-booking.

<RetrievalDrawer
  client:load
  id="05-data-money-short-retrieval"
  lang="en"
  questions={[
    { q: "What kills a metrics database, and how do you keep it alive?", a: "Cardinality, not sample rate. A time series is identified by its full label set, so the number of series is the PRODUCT of every label's distinct-value count; each series carries an index entry and an in-memory open chunk, so active series set the memory footprint. One unbounded label (user_id, request_id, raw URL) multiplies series into the billions and OOMs the box, while the sample rate barely moves. Keep it alive by never putting unbounded values in labels — normalize to bounded forms (route templates, tiers) and push high-cardinality detail into logs/traces. Choose pull collection for free failure detection, downsample into tiered retention, and isolate the alerting path from dashboard queries." },
    { q: "What does exactly-once cover in ad-click aggregation, and what handles the rest?", a: "Exactly-once means each event's EFFECT is applied once — engineered as at-least-once delivery + idempotent/transactional application: dedup retries by click_id, and bind output commit to input offset via checkpoints + a transactional sink so a crash-replay doesn't double-count. It only covers state INSIDE the engine's boundary, so external side effects must be made idempotent themselves. It does NOT handle late events — those are a windowing concern: aggregate by event time, close windows on a watermark sized for the common case, and recover true stragglers in a batch reconciliation layer that re-reads all raw events for the authoritative billing total (lambda)." },
    { q: "How do an idempotency key and a double-entry ledger keep a payment correct?", a: "The client mints an idempotency key per intended payment and sends it on every attempt; the server's atomic check-and-store returns the saved result on a retry instead of charging again — and the client must own the key because only a key created before the first attempt survives a lost response. Money lives in an append-only double-entry ledger: every movement is two entries summing to zero (debit = credit), immutable (corrections are new compensating entries, so the audit trail is intact) and balanced (money is conserved by construction; a balance is a verifiable sum, not a mutable field). A saga handles the cross-system transaction with compensations, and a daily reconciliation against the PSP is the final arbiter — correctness over availability." },
    { q: "What is the lost update, and what's the single common fix shared with double-booking?", a: "Two transactions read the same value (a balance, '1 available'), each computes a new value from the stale read, and one overwrites the other — overdrawing a wallet or selling a room twice, with no error logged. The shared fix is to make check-and-act ONE atomic operation, closing the read-modify-write gap: an atomic conditional write (UPDATE … SET balance = balance - X WHERE balance >= X), a row lock (SELECT FOR UPDATE), or CAS on a version. The wallet applies it per balance; the hotel applies it per night across the range, all-or-nothing in a single transaction. Event-source the balance so it's a verifiable fold over a log; keep both single-shard so the atomic op is a local ACID transaction." },
    { q: "Why is a stock exchange single-threaded, and how does that give recovery and audit?", a: "Fairness and audit require ONE canonical total order of events, identical on every replica and reproducible years later. A single sequencer assigns global sequence numbers; a single-threaded matching engine applies events one at a time against an in-memory order book by price-time priority, so there's no concurrency race over 'who filled first' — and it's faster (no locks, book in cache). Because the core is DETERMINISTIC (no clock reads or randomness inside the match — those are captured as sequencer inputs), the log of sequenced inputs IS the system: replay it to rebuild the exact book, run hot replicas at the same sequence number, and answer an auditor by replaying to that point. Scale by partitioning across instruments, never by parallelizing one book." },
  ]}
/>

</Explanation>

<KeyTakeaway>Reconstruct each case from memory — cardinality, exactly-once, the ledger, the lost update, exchange determinism, double-booking — and you hold the unit's spine: correctness under load is named hazards with atomic fixes.</KeyTakeaway>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the unit's spine. **Cardinality** (the product of label cardinalities) kills a metrics DB, so keep labels bounded. **Exactly-once** is at-least-once + idempotent/transactional application and covers only the engine's internal state — late events are a watermark concern recovered by **batch reconciliation** (lambda). A **client idempotency key** with atomic check-and-store stops the double charge, and an append-only **double-entry ledger** keeps money conserved, immutable, and auditable. The **lost update** and the **double-booking** are the same read-modify-write race, fixed by one atomic check-and-act (per balance, or per night all-or-nothing). And an exchange is **single-threaded over a sequenced total order** so a deterministic core makes the log enough for recovery, replicas, and audit. The thread tying them together: when the output is money or correctness-critical data, you reason in named hazards and apply precise, atomic fixes — before production teaches them to you.
</Recap>
