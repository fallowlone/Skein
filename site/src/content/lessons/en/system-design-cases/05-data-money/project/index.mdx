---
slug: "project"
lang: en
track: "system-design-cases"
unit: "05-data-money"
order: 10
title: "Data & money: build a correct money mover"
summary: "Hands-on project: build a small wallet/ledger service that survives retries and concurrency — a double-entry ledger, client idempotency keys, an atomic transfer that defeats the lost update, and a reconciliation pass — proven with adversarial tests."
estMin: 240
status: ready
lessonType: topic
level: senior
concepts:
  - idempotency-keys
  - double-entry-ledger
  - lost-update
  - concurrency-control
  - reconciliation
  - event-sourcing
prereqs:
  - system-design-cases/05-data-money/03-payment-system
  - system-design-cases/05-data-money/04-digital-wallet
sources:
  - https://docs.stripe.com/api/idempotent_requests
  - https://www.postgresql.org/docs/current/transaction-iso.html
  - https://martinfowler.com/eaaDev/EventSourcing.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Reading that idempotency keys and a double-entry ledger prevent double-charges and lost updates is not the same as building a service that survives a retry storm and a thousand concurrent transfers without losing a cent. Build a small money mover, then prove its correctness the only way that counts: with adversarial tests that fire duplicate requests and concurrent debits at it and check that the books still balance.
</Hook>

<Crux>Build a wallet/ledger for retries and concurrency: double-entry, idempotency keys, an atomic transfer, reconciliation — with hostile tests.</Crux>

<Explanation>

This project makes the unit operational: you'll implement the two load-bearing patterns — idempotency and the double-entry ledger — defeat the lost update with an atomic transfer, and then attack your own system with duplicates and concurrency to prove the invariant that the books always balance.

<ProjectBrief
  id="05-data-money-project"
  lessonSlug="project"
  lang="en"
  objective="Build a small wallet/ledger service (any language + a real transactional database) that moves money between accounts correctly under retries and concurrency. It must implement an append-only double-entry ledger, accept client idempotency keys, perform atomic transfers that cannot double-spend or lose updates, and include a reconciliation pass that verifies the books balance — then prove all of it with adversarial tests."
  requirements={[
    "Model an append-only double-entry ledger: a transfer writes two entries (debit + credit) that sum to zero, entries are immutable (never updated or deleted), and an account balance is derived by summing its entries — not stored as a mutable field. Seed a few accounts with opening-balance entries.",
    "Implement a transfer(from, to, amount, idempotency_key) operation in a single database transaction: check sufficient funds, append the debit and credit entries, and commit atomically — both legs or neither.",
    "Add idempotency: persist the idempotency key with the transfer result under a unique constraint, and on a repeated key return the original result without moving money again. Reject a reused key whose parameters differ.",
    "Defeat the lost update: ensure two concurrent transfers from the same account cannot both succeed past the balance (use an atomic conditional check, SELECT … FOR UPDATE, or a serializable transaction) — and document which mechanism you chose and why.",
    "Write a reconciliation pass: independently recompute every account balance by summing its entries and assert the global invariant that total debits equal total credits (money is conserved); make it runnable on demand and report any imbalance.",
    "Adversarial test 1 — retry storm: fire the SAME transfer request N times concurrently (same idempotency key) and assert exactly one set of ledger entries was created and the balance moved once.",
    "Adversarial test 2 — concurrent debits: fire many concurrent transfers from one account whose total exceeds its balance, and assert the account never goes negative and the books still balance (no lost update, no money created).",
    "Write a short design note: state your isolation level / locking choice, why the balance is derived rather than stored, and one failure mode reconciliation would catch that the hot path alone would not.",
  ]}
  acceptance={[
    "An append-only ledger where balances are computed by summing immutable entries, with a seed script and a query that prints any account's balance and its entry history.",
    "A transfer that is atomic (both entries or neither) and idempotent (a repeated key returns the original result with no second movement; a conflicting key is rejected).",
    "A passing retry-storm test: N concurrent identical requests produce exactly one transfer's worth of entries and a single balance change.",
    "A passing concurrent-debit test: many over-committed concurrent transfers leave the account non-negative and the global debits = credits invariant intact.",
    "A reconciliation command that recomputes balances from entries and reports the books as balanced (and would flag an injected imbalance), plus the design note explaining the isolation/locking choice and one failure reconciliation catches.",
  ]}
  stretch={[
    "Add a cross-shard transfer as a saga: split accounts across two databases, implement debit→credit with a compensating action, model the in-flight pending state, and show reconciliation healing a transfer you deliberately interrupt between the two legs.",
    "Add a snapshot/materialized balance and serve reads from it (snapshot + fold the tail of recent entries), then prove the snapshot always equals the full re-fold — the event-sourcing read optimization without losing verifiability.",
    "Add an external 'PSP' stub whose response you can drop/delay, put a charge behind the idempotency key, and show that a lost response + client retry does not double-charge, while a daily reconciliation against the PSP's report still catches a divergence.",
    "Stress the hot account: make one account the target of most transfers and measure how your locking choice serializes it; compare pessimistic locking vs an atomic conditional write vs optimistic/CAS under high contention and report the throughput and retry rates.",
  ]}
/>

</Explanation>

<KeyTakeaway>Building a correct money mover makes the unit real: a double-entry ledger, idempotency keys, an atomic transfer that defeats the lost update, and reconciliation — proven by retry-storm and concurrent-debit tests.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="05-data-money-project-retrieval"
  lang="en"
  questions={[
    { q: "Why derive balances by summing immutable entries instead of storing a mutable balance field?", a: "A mutable balance is what ENABLES the lost update (read-modify-write race) and erases history. An append-only double-entry ledger makes every movement two immutable entries summing to zero (debit = credit), so money is conserved by construction, the balance is a verifiable sum over history (re-fold and compare to detect corruption), and the full audit trail is intact (a correction is a new compensating entry, never an edit). The project's reconciliation pass relies on this: it independently re-sums entries and asserts total debits equal total credits — impossible to do meaningfully against a mutable field." },
    { q: "How do the two adversarial tests prove the two core invariants?", a: "Retry storm (idempotency): fire the SAME request with one idempotency key N times concurrently; correct behaviour is exactly one set of ledger entries and one balance change, because the atomic check-and-store under a unique key admits the first and returns the saved result to the rest — proving retries can't double-spend. Concurrent debits (lost update): fire many concurrent transfers from one account whose total exceeds its balance; correct behaviour is the account never goes negative and total debits still equal total credits, because the atomic conditional check / lock / serializable transaction closes the read-write gap — proving concurrency can't create money or overdraw." },
    { q: "What does the reconciliation pass catch that the hot path alone cannot?", a: "The hot path enforces correctness per operation, but ambiguity and external systems leave residue: a lost PSP response (charged but not recorded, or recorded as failed when it succeeded), an interrupted cross-shard saga left in-flight, or a subtle bug that drifted a balance. Reconciliation is the independent audit: it re-derives every balance from the immutable entries and asserts the global debits = credits invariant, and (with a PSP stub) compares the ledger to the processor's settlement report — catching divergences the per-operation path can't see, then flagging them for a compensating entry. It's why a money system can be available yet provably correct: the slow exhaustive pass is the final arbiter." },
  ]}
/>

<Recap lang="en">
This project turns the unit's two load-bearing patterns into working code. You build an **append-only double-entry ledger** where every transfer is two immutable entries summing to zero and a balance is a **derived sum**, never a mutable field — so money is conserved by construction and the books are verifiable. The **transfer** is one atomic transaction (both legs or neither) guarded by a **client idempotency key** (atomic check-and-store under a unique constraint), and the **lost update** is defeated by an atomic conditional check, a row lock, or a serializable transaction. Then you attack your own system: a **retry storm** (N identical concurrent requests must move money exactly once) proves idempotency, and **concurrent over-committed debits** (the account must never go negative, debits must still equal credits) prove the lost update is closed. Finally a **reconciliation pass** independently re-sums entries and asserts the global invariant — the layer that catches lost responses, interrupted sagas, and drift the hot path can't see. An engineer who has built this once stops trusting a single response as the truth about money and starts trusting the reconciled, append-only ledger — and knows, with tests to prove it, that retries and concurrency can't make the books lie.
</Recap>
