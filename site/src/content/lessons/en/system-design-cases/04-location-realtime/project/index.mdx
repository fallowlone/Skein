---
slug: "project"
lang: en
track: "system-design-cases"
unit: "04-location-realtime"
order: 8
title: "Location & realtime: build a live proximity + leaderboard service"
summary: "Hands-on project: build a working find-nearby service over a spatial index (querying neighbor cells), add a realtime push channel for moving entities by region, and back a leaderboard with a sorted set — then prove rank and proximity correctness with a small test harness."
estMin: 240
status: ready
lessonType: topic
level: senior
concepts:
  - spatial-index
  - cell-boundary
  - pub-sub-by-region
  - websockets
  - redis-zset
  - rank-query
prereqs:
  - 04-location-realtime/01-proximity-service
  - 04-location-realtime/02-nearby-friends
  - 04-location-realtime/03-google-maps
  - 04-location-realtime/04-gaming-leaderboard
sources:
  - https://h3geo.org/docs/highlights/indexing/
  - https://redis.io/docs/latest/develop/data-types/sorted-sets/
  - https://redis.io/docs/latest/develop/interact/pubsub/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Reading that you should query neighbor cells and put rank in a sorted set is not the same as building a service that survives a moving load. Take the unit's three primitives — a spatial index, a region push channel, and a sorted set — and assemble a small but real location-and-realtime service, then prove the two correctness properties most likely to bite you: that proximity finds the across-the-boundary neighbor, and that a player's rank is right among millions.
</Hook>

<Crux>Build find-nearby over a spatial index, region-keyed push, and a sorted-set leaderboard — then prove proximity and rank correctness.</Crux>

<Explanation>

This project makes the unit operational: you implement the coarse-then-fine proximity query, the region fan-out that avoids a broadcast, and the `O(log n)` rank engine, then write tests that catch the canonical bugs — the missed boundary neighbor and the wrong global rank.

<ProjectBrief
  id="04-location-realtime-project"
  lessonSlug="project"
  lang="en"
  objective="Build a working location-and-realtime service with three parts — a find-nearby query over a spatial index, a realtime push channel for moving entities routed by region, and a sorted-set leaderboard — and prove with a test harness that proximity returns across-boundary neighbors and that ranks (including global rank under sharding) are correct."
  requirements={[
    "Pick a spatial index (geohash, an H3/S2 library, or a quadtree you write) and justify the choice from the access pattern; ingest a dataset of static points (e.g. 100k synthetic places) and index each by cell.",
    "Implement find-nearby(lat, lng, radius): convert the point to its cell, fetch the cell AND its neighbor ring sized to the radius, then filter the union by exact great-circle (haversine) distance — coarse filter then fine filter.",
    "Add a cache keyed by (cell, filters) for the read path and show a measured hit-rate improvement on a skewed query load (most queries hitting a few hot cells).",
    "Build a realtime channel: simulate N moving entities that emit location pings, store each with a short TTL keyed by region cell, and publish to a per-cell pub/sub topic; a subscriber receives only its cell's pings and re-subscribes when an entity it tracks crosses a boundary.",
    "Push updates to a client over a persistent connection (WebSocket or SSE), and enforce a friendship/visibility filter at the subscriber edge so a non-friend's location is never delivered.",
    "Back a leaderboard with a sorted set (Redis ZSET or your own skip list): support update (ZADD), top-k (ZREVRANGE), my-rank (ZREVRANK), and the window around a player; encode a deterministic tiebreaker into a composite score.",
    "Shard the leaderboard by score range across at least two partitions and implement global rank as in-shard rank plus the summed sizes of higher-scoring shards (maintain per-shard cardinality counters).",
    "Write a test harness that asserts the two correctness properties: a brute-force O(n) scan agrees with find-nearby on randomized queries (including points deliberately near cell boundaries), and a brute-force sort agrees with the sorted-set/sharded global rank for randomized scores.",
  ]}
  acceptance={[
    "A find-nearby endpoint that, on randomized inputs including boundary-adjacent points, returns exactly the same set as a brute-force distance scan — demonstrating the neighbor-ring query fixes the cell-boundary problem.",
    "A measured cache hit-rate on the skewed read load, with a one-line note on why caching by cell pays off given the read-heavy, slow-changing place data.",
    "A working realtime demo where a moving entity's pings reach only subscribers of its current region cell (shown by a log or counter), with the subscription re-pointing on a boundary crossing and a non-friend never receiving a location.",
    "A leaderboard whose update, top-k, my-rank, and window operations all run without a full scan, with ties resolved deterministically by the composite score.",
    "A test proving global rank under score-range sharding equals the brute-force sorted rank for randomized scores, plus a short note on how per-shard counters make it O(shards) rather than a cross-shard scan.",
  ]}
  stretch={[
    "Add density adaptivity: switch a hot, overflowing cell to a finer resolution (or a quadtree subdivision) and show the candidate count per query drop, keeping dense-area latency bounded.",
    "Add a tiny routing component: build a small road graph, contract it into a hierarchy, and show that a bidirectional upward search returns the same shortest path as plain Dijkstra while expanding far fewer nodes.",
    "Add adaptive ping rates to the realtime channel (slower when an entity is stationary, faster when moving) and measure the write reduction versus a fixed interval at equal perceived freshness.",
    "Add time-windowed leaderboards (daily / weekly / all-time) as separate sorted sets and discuss how this both shrinks each board and spreads write load.",
  ]}
/>

</Explanation>

<KeyTakeaway>A working build makes the unit real: coarse-then-fine proximity, region push, an O(log n) sorted-set leaderboard. Prove the two that matter — find-nearby matches a brute scan on boundary points; rank matches a sort.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-location-realtime-project-retrieval"
  lang="en"
  questions={[
    { q: "What are the three primitives this project assembles, and what does each demonstrate?", a: "(1) A find-nearby query over a spatial index that fetches the cell plus its neighbor ring and filters by exact distance — demonstrating the coarse-then-fine pattern and the cell-boundary fix, with caching by cell on the read-heavy load. (2) A realtime push channel that stores moving entities with TTL keyed by region and publishes per-cell over a persistent connection, with the friendship filter at the subscriber edge — demonstrating region fan-out instead of a broadcast. (3) A sorted-set leaderboard supporting update, top-k, my-rank, and window in O(log n), sharded by score range with global rank from per-shard counters — demonstrating that rank lives in the structure." },
    { q: "How do you prove the two correctness properties, and why those two?", a: "Proximity: run randomized find-nearby queries — deliberately including points near cell boundaries — and assert the result equals a brute-force O(n) distance scan; this catches the canonical single-cell bug, since a service that queries only one cell will disagree exactly on boundary points. Rank: generate randomized scores, compute global rank via the sorted set under score-range sharding (in-shard rank + summed higher-shard sizes), and assert it matches a brute-force full sort; this catches off-by-one and cross-shard summation errors. They're chosen because they're the two properties most likely to be silently wrong — both look fine in casual testing and fail only on edge cases." },
    { q: "Why build all three together rather than in isolation?", a: "Because the unit's lesson is that the right design follows the access pattern, and seeing the three side by side makes the contrast concrete: the same region-cell idea is a search index for static points (proximity), a routing key for moving ones (realtime), and the boundary-handling instinct reappears as score-range sharding in the leaderboard. Building them together forces you to reuse the cell abstraction across read-heavy and write-heavy workloads and to feel why coarse-then-fine, edge filtering, and structure-held rank are the same discipline applied to different load shapes." },
  ]}
/>

<Recap lang="en">
This project turns the location-and-realtime unit into a working system built from **three primitives**. The **proximity** part implements coarse-then-fine search over a spatial index — fetch the cell **plus its neighbor ring**, filter by exact distance, and cache by cell on the read-heavy load — and is proven correct against a brute-force scan **including boundary-adjacent points**, the test that exposes the single-cell bug. The **realtime** part stores moving entities with a **TTL keyed by region cell**, publishes per-cell over a persistent connection, re-subscribes on boundary crossings, and enforces a friendship filter at the **subscriber edge** rather than broadcasting. The **leaderboard** part uses a **sorted set** for `O(log n)` update, top-k, my-rank, and window, with a composite-score tiebreaker, and shards by score range so **global rank** is in-shard rank plus higher-shard counts — proven against a brute-force sort. Building all three together makes the unifying lesson tangible: the same region-cell abstraction is a search index, a routing key, and a partition axis, and the right structure always follows the **access pattern**.
</Recap>
