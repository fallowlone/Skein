---
slug: "quiz-short"
lang: en
track: "system-design-cases"
unit: "04-location-realtime"
order: 6
title: "Location & realtime: free-recall review"
summary: "Free-recall prompts across the location-and-realtime cases. Answer each from memory first — the index choice, the boundary fix, the fan-out layer, the routing preprocessing, the rank structure — then reveal the model answer and compare."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - spatial-index
  - cell-boundary
  - pub-sub-by-region
  - contraction-hierarchies
  - redis-zset
  - score-sharding
prereqs:
  - 04-location-realtime/01-proximity-service
  - 04-location-realtime/02-nearby-friends
  - 04-location-realtime/03-google-maps
  - 04-location-realtime/04-gaming-leaderboard
sources:
  - https://h3geo.org/docs/highlights/indexing/
  - https://redis.io/docs/latest/develop/data-types/sorted-sets/
  - https://datatracker.ietf.org/doc/html/rfc6455
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, reconstruct a full answer from memory — including the mechanism, not just the name — before opening the model answer. The effort of rebuilding the spatial index, the region fan-out, and the sorted set is what makes them stick.
</Hook>

<Crux>Reconstruct the unit's spine: spatial indexing and boundaries, write-heavy fan-out, routing preprocessing, and the leaderboard's sorted set.</Crux>

<Explanation>

Reconstruct each case from memory without looking back: how you index space and dodge the boundary trap, why nearby-friends is write-heavy and how region pub/sub bounds the fan-out, why plain Dijkstra fails and what fixes it, and the structure that makes update, top-k, and rank cheap at once.

<RetrievalDrawer
  client:load
  id="04-location-realtime-short-retrieval"
  lang="en"
  questions={[
    { q: "Compare geohash, quadtree, and S2/H3, and explain the cell-boundary problem and its fix.", a: "Geohash interleaves lat/lng bits into a string so nearby points usually share a prefix (B-tree-searchable), but the prefix property breaks at boundaries and cells are non-uniform in area. A quadtree recursively splits space into four, subdividing only dense cells — density-adaptive but a stateful tree to maintain. S2 (cube + Hilbert curve, 64-bit IDs) and H3 (hexagons) give near-equal-area, cleanly nesting cells; H3's six equidistant neighbors make ring expansion clean — the production choice. The cell-boundary problem: a point's true nearest neighbor can sit just across a cell line, so a single-cell query misses it. Fix: query the cell AND its neighbors (3×3 block or H3 kRing sized to the radius) as a coarse filter, then rank by exact great-circle distance — and match cell resolution to the radius (S2/H3 'coverings')." },
    { q: "Why is nearby-friends write-heavy, and how do WebSockets and region pub/sub make it work?", a: "Every active sharing phone pings every few seconds — ≈2M writes/sec for 10M sharers — but the data is ephemeral and worthless within seconds, so it needs an in-memory, TTL-expiring store, not a durable DB. Clients hold persistent WebSocket connections so the server pushes the instant there's news (polling pays full request/response per check and fixes latency to the interval). Region pub/sub bounds the fan-out: divide the world into cells, publish each ping to exactly its cell's channel, and have gateways subscribe only to cells where they hold users — so a Berlin ping never reaches Tokyo. The cheap 'friend and in-range' filter runs at the subscriber edge, never as a graph+geo join on the ingest firehose (fan-out-on-write). Privacy is structural: TTL expiry, opt-in per relationship, and the edge friendship filter." },
    { q: "Why does plain Dijkstra fail for Maps routing, and how do contraction hierarchies and live traffic fit together?", a: "Dijkstra fans out in all directions and expands millions of nodes per query on a continental graph; A* helps but still explores a wide cone — neither affordable at planetary QPS. Contraction hierarchies preprocess the graph offline: rank nodes by importance, contract them, add distance-preserving shortcut edges, and run a bidirectional query that only moves UPWARD — riding a few high-level shortcuts so it touches thousands of nodes, not millions, while still returning the exact shortest path. Because pure contraction bakes weights into shortcuts, live traffic (a continuous GPS-probe stream aggregated per road segment for ETA) would force a re-contraction — so production separates slow-changing STRUCTURE (preprocessed rarely) from fast-changing WEIGHTS (overlaid live). Tiles, meanwhile, are a separate CDN problem." },
    { q: "Why is a sorted set the right structure for a leaderboard, and how do top-k, my-rank, ties, and sharding work?", a: "The board needs cheap update, top-k, AND rank-of-one together; a SQL table makes 'my rank' an O(n) COUNT(*) WHERE score > mine, fatal under polling. A Redis ZSET (a skip list with per-node spans + a hash map) gives ZADD, ZREVRANGE, and ZREVRANK all in O(log n) — rank by summing precomputed jump-widths, no counting. Top-k is shared (compute once, cache for all); my-rank is personal (computed live per request, cheap via ZREVRANK); the window around a player reuses the rank. Ties (equal scores order arbitrarily by member name) are fixed by encoding a tiebreaker into a composite score (points plus inverse timestamp). When one node is too small/slow, shard by score range: top-k reads the highest range, and global rank = in-shard rank + summed sizes of higher shards from cheap counters (O(shards)). The ZSET is a fast in-RAM index over a durable source of truth, rebuilt on restart." },
    { q: "What single insight links all four cases, and where does each diverge?", a: "All four turn 'compare against everything' into 'touch only a sliver', via precomputation or a routing key. They diverge on the access pattern: proximity is read-heavy and static, so you build a heavy spatial index and cache by cell; nearby-friends is write-heavy and ephemeral, so you drop durability, push over WebSockets, and route by region pub/sub; Maps is precomputation-dominated (tile pyramid + contraction hierarchies) with structure separated from live traffic weights; the leaderboard needs three operations cheap at once, met by a sorted set's O(log n) rank. The shared boundary instinct recurs: query neighbor cells (proximity), re-subscribe on cell crossing (friends), partition the road graph along low-traffic cuts (Maps), and shard by score range with cross-shard rank (leaderboard)." },
  ]}
/>

</Explanation>

<KeyTakeaway>Recall each case from memory — spatial index, boundary fix, region fan-out, contraction hierarchies, sorted-set rank — and you hold the spine: design follows the access pattern, turning 'scan all' into 'touch a slice'.</KeyTakeaway>

<Recap lang="en">
If you could rebuild each answer from memory, you hold the unit's spine. **Proximity** indexes space (geohash/quadtree/S2-H3) and queries a cell **plus its neighbors** as a coarse filter, ranking survivors by exact distance and caching per cell. **Nearby-friends** inverts it into a **write firehose** of ephemeral pings, pushed over **WebSockets** from a TTL store and routed by **region pub/sub** with the friend-and-range filter at the subscriber edge. **Maps** is dominated by **precomputation** — a tile pyramid on a CDN and routing via **contraction hierarchies** — with live traffic kept as a separate weight overlay so it never forces a re-contraction. The **leaderboard** uses a **sorted set** to make update, top-k, and rank-of-one all `O(log n)`, where SQL makes rank an `O(n)` count, and shards by score range with global rank from per-shard counters. The unifying idea: each design **follows the access pattern** and turns "compare against everything" into "touch only a sliver".
</Recap>
