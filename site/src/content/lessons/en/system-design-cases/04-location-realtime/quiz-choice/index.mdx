---
slug: "quiz-choice"
lang: en
track: "system-design-cases"
unit: "04-location-realtime"
order: 5
title: "Location & realtime: multiple-choice review"
summary: "Multiple-choice synthesis across the location-and-realtime cases: spatial indexing and the cell-boundary trap, write-heavy location fan-out, contraction hierarchies for routing, and the sorted set behind a leaderboard's rank query."
estMin: 13
status: ready
lessonType: topic
level: senior
concepts:
  - spatial-index
  - cell-boundary
  - pub-sub-by-region
  - contraction-hierarchies
  - redis-zset
  - rank-query
prereqs:
  - 04-location-realtime/01-proximity-service
  - 04-location-realtime/02-nearby-friends
  - 04-location-realtime/03-google-maps
  - 04-location-realtime/04-gaming-leaderboard
sources:
  - https://s2geometry.io/
  - https://redis.io/docs/latest/develop/data-types/sorted-sets/
  - https://www.microsoft.com/en-us/research/publication/customizable-route-planning/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six decisions that cut across the four cases. Each is a choice you make at a whiteboard — how to index space, where to run the fan-out, which algorithm survives a continental graph, what structure answers "my rank among millions" — not a definition to recite.
</Hook>

<Crux>Six cross-case decisions: a spatial index, the boundary fix, region fan-out, the routing preprocessing, and the leaderboard's rank query.</Crux>

<Explanation>

Confirm you can pick a spatial index for the access pattern, fix the cell-boundary trap, place location fan-out at the right layer, reject plain Dijkstra at continental scale, and choose the structure that makes rank-of-one cheap.

<Quiz
  id="04-location-realtime-choice-1"
  lessonSlug="quiz-choice"
  lang="en"
  question="A find-nearby service queries only the user's own geohash cell and ranks the places in it. Users report a clearly close shop is sometimes missing. What's the fix?"
  choices={[
    { label: "Query the cell AND its neighbors (the 3×3 block, or an H3 kRing), treating the cell index as a coarse filter, then rank by exact distance — the nearest place can sit just across a boundary in an adjacent cell.", correct: true },
    { label: "Increase the geohash precision so cells are smaller and more accurate.", misconception: "Smaller cells make the boundary problem WORSE — more edges to fall across. The fix is to query neighboring cells, not to shrink them; resolution should match the radius, but neighbors are still required." },
    { label: "Rebuild the spatial index nightly to fix stale assignments.", misconception: "The assignment isn't stale; the shop is correctly in its own cell. A single-cell query structurally can't see places just over the line — you must query the surrounding cells." },
    { label: "Add a read replica so the query sees more rows.", misconception: "A replica returns the same single-cell result, just from another node. The missing shop is in an adjacent cell that was never queried — fetch the neighbors." },
  ]}
/>

<Quiz
  id="04-location-realtime-choice-2"
  lessonSlug="quiz-choice"
  lang="en"
  question="A fixed-resolution square grid overflows in Times Square (one cell holds tens of thousands of places) while ocean cells are empty, and dense-query latency is terrible. What structural change helps most?"
  choices={[
    { label: "Use a density-adaptive index — a quadtree, or the variable resolution of S2/H3 — so dense areas subdivide into finer cells and sparse areas stay coarse, bounding the candidate count per query.", correct: true },
    { label: "Cache every cell so the database is never hit.", misconception: "Caching cuts read load but a dense cell still has tens of thousands of candidates to rank per query. You need adaptive resolution to bound the candidate count, with caching layered on top." },
    { label: "Use coarser cells everywhere to reduce the cell count.", misconception: "Coarser cells make dense overflow worse — each holds even more places. You need FINER cells where it's dense, which adaptive indexes (quadtree, S2/H3) provide." },
    { label: "Switch from geohash to a single global cell.", misconception: "One cell means scanning everything — the opposite of indexing. Density-adaptive resolution is the fix: fine where dense, coarse where sparse." },
  ]}
/>

<Quiz
  id="04-location-realtime-choice-3"
  lessonSlug="quiz-choice"
  lang="en"
  question="A nearby-friends system ingests 2M location pings/second. An engineer proposes, on each ping, loading the publisher's friends, fetching their locations, and pushing to the nearby ones. Why prefer region-keyed pub/sub?"
  choices={[
    { label: "Fan-out-on-write runs an expensive graph + geo join on the hottest path for every ping. Publishing to a region channel decouples ingest from matching; the cheap 'friend and in-range' filter runs at the subscriber edge on a stream already narrowed to one region.", correct: true },
    { label: "Pub/sub guarantees exactly-once delivery, which direct push can't.", misconception: "Neither guarantees exactly-once, and it doesn't matter — a dropped ping is replaced in seconds. The reason is decoupling the firehose from the friend-matching, not delivery semantics." },
    { label: "Direct push can't use WebSockets, but pub/sub can.", misconception: "Both push over WebSockets at the edge. The difference is region pub/sub publishes once per cell and filters cheaply at the subscriber, instead of a full graph+geo join per ping on ingest." },
    { label: "Pub/sub stores location history for free.", misconception: "Live location is ephemeral and deliberately not persisted on this path; history, if needed, is a separate batch store. The reason for pub/sub is moving the matching off the ingest hot path." },
  ]}
/>

<Quiz
  id="04-location-realtime-choice-4"
  lessonSlug="quiz-choice"
  lang="en"
  question="A team builds continental routing with plain Dijkstra; it's correct but takes seconds per query, expanding millions of nodes. Which preprocessing makes queries touch only thousands of nodes while still returning the exact shortest path?"
  choices={[
    { label: "Contraction hierarchies: rank nodes by importance and contract them, adding distance-preserving shortcut edges, so a bidirectional query only moves upward and rides a few high-level shortcuts instead of crawling every street.", correct: true },
    { label: "Precompute and store the shortest path for every source–destination pair.", misconception: "That's ~N² pairs on tens of millions of nodes — impossible to compute or store. Contraction hierarchies preprocess the GRAPH (shortcuts), making any query fast without an impossible cache." },
    { label: "Drop minor roads to shrink the graph.", misconception: "Dropping roads changes the answer (valid routes vanish) and still leaves a large graph. CH keeps correctness via distance-preserving shortcuts, not by deleting edges." },
    { label: "Run Dijkstra on more CPUs in parallel.", misconception: "Bidirectional/parallel Dijkstra still expands a huge frontier. The order-of-magnitude win is preprocessing the graph into a hierarchy with shortcuts, not raw compute." },
  ]}
/>

<Quiz
  id="04-location-realtime-choice-5"
  lessonSlug="quiz-choice"
  lang="en"
  question="A leaderboard stores scores in a SQL table indexed on score. Top-10 is fast, but showing each of 12M players their own live rank crushes the database. What's the root cause and fix?"
  choices={[
    { label: "'My rank' is SELECT COUNT(*) WHERE score > mine — an O(n) count per player per query. Use a Redis sorted set: ZREVRANK returns rank in O(log n) because rank lives in the skip-list spans, so it never scans.", correct: true },
    { label: "The index is fragmented; rebuild it and add replicas.", misconception: "An index makes top-k fast but can't make COUNT(*) WHERE score > mine cheap — it's O(n) per rank query, and replicas just spread an expensive query. The fix is a ZSET with O(log n) ZREVRANK." },
    { label: "Precompute and cache every player's rank.", misconception: "Maintaining ranks for all 12M players is O(n) per score change and mostly wasted (most are never viewed). A ZSET computes any rank in O(log n) on read — no global rank cache needed." },
    { label: "Move to a document database for faster writes.", misconception: "A document store still scans to count higher scores. The right tool is an ordered, rank-aware structure: a sorted set with O(log n) ZREVRANK." },
  ]}
/>

<Quiz
  id="04-location-realtime-choice-6"
  lessonSlug="quiz-choice"
  lang="en"
  question="You shard a leaderboard by score range across nodes because one node can't hold it. How do you still answer a player's GLOBAL rank without a scan?"
  choices={[
    { label: "Global rank = the player's in-shard rank (ZREVRANK, O(log n)) PLUS the summed sizes of all higher-scoring shards, read from cheap per-shard cardinality counters — an O(shards) addition.", correct: true },
    { label: "Query every shard for all players above the score and count them.", misconception: "That's an O(n) cross-shard scan — what sharding was meant to avoid. Use in-shard rank plus the per-shard size counters of higher shards: O(shards)." },
    { label: "Global rank is impossible once sharded; only top-k works.", misconception: "It's tractable: in-shard rank plus the summed sizes of higher shards from counters. Score-range sharding preserves both top-k and global rank." },
    { label: "Replicate the whole board onto one node to compute rank.", misconception: "That recreates the single-node memory and throughput bottleneck you sharded away. Use in-shard rank plus higher-shard counts from maintained counters." },
  ]}
/>

</Explanation>

<KeyTakeaway>The answers come from the access pattern: query neighbor cells not one; fan out by region at the edge; preprocess the road graph into shortcuts, not Dijkstra; put rank in a sorted set so 'my rank' is O(log n), not O(n).</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-location-realtime-choice-retrieval"
  lang="en"
  questions={[
    { q: "Why must a proximity query fetch a cell's neighbors, not just the cell?", a: "A point's true nearest neighbor can sit just across a cell line in an adjacent cell. A single-cell query structurally can't see it. Treat the cell index as a coarse filter: fetch the cell AND its surrounding cells (a 3×3 block or an H3 kRing sized to the radius), then rank by exact distance. Coarse-then-fine, with the neighbor ring matched to the search radius." },
    { q: "Why is rank-of-one cheap in a ZSET but expensive in SQL?", a: "In SQL, 'my rank' is SELECT COUNT(*) WHERE score > mine — an O(n) count per query, fatal when millions poll. A Redis ZSET is a skip list whose nodes store spans (how many elements each pointer jumps over), so ZREVRANK sums a few precomputed jump-widths along an O(log n) path — rank without counting. Update, top-k, and rank are all O(log n) at once." },
  ]}
/>

<Recap lang="en">
The through-line of the unit is that the right design falls out of the **access pattern**. **Proximity** is read-heavy and slow-changing, so you build a spatial index and query a cell **plus its neighbors** as a coarse filter, ranking the survivors by exact distance; density skew is fixed by adaptive resolution (quadtree, S2/H3). **Nearby-friends** inverts this into a write firehose, so you push over WebSockets from an ephemeral TTL store and route by **region pub/sub**, running the friend-and-range filter at the subscriber edge rather than as a graph+geo join on ingest. **Maps** is dominated by **precomputation**: tiles on a CDN, and routing via **contraction hierarchies** so a query rides a few shortcuts instead of expanding millions of nodes, with live traffic kept as a separate weight overlay. The **leaderboard** needs update, top-k, and rank-of-one together, which a **sorted set** delivers in `O(log n)` where a SQL `ORDER BY` makes rank an `O(n)` count — and it shards by score range with global rank computed from per-shard counters.
</Recap>
