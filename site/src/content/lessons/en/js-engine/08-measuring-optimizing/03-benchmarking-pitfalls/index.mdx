---
slug: "03-benchmarking-pitfalls"
lang: en
track: "js-engine"
unit: "08-measuring-optimizing"
order: 3
title: "Why your microbenchmark lies"
summary: "The traps that make microbenchmarks report fantasy numbers: dead-code elimination, constant folding and hoisting, un-accounted JIT warm-up, OSR mid-measure, GC noise, console.log/try-catch deopts in the timed region, and micro-vs-macro mismatch"
estMin: 15
status: ready
lessonType: topic
level: senior
concepts:
  - dead-code elimination
  - constant folding
  - JIT warm-up
  - on-stack replacement
  - GC noise
  - black-hole sink
  - performance.now
  - macro vs micro
spiral:
  - performance-cliffs
  - measurement
prereqs:
  - "02-monomorphism-discipline"
sources:
  - https://mrale.ph/blog/2012/12/15/microbenchmarks-fairy-tales.html
  - https://github.com/tinylibs/tinybench
  - https://v8.dev/blog/jetstream2
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
You wrote a loop to prove version A is faster than version B. The loop ran in 0.0ms. You ship version A. In production it is no faster — sometimes slower. The benchmark did not lie about timing; it measured a program the optimiser had quietly rewritten into nothing, because the result was never used. Almost every confident microbenchmark you have read is wrong in one of a handful of specific ways.
</Hook>

<Crux>An optimising JIT will delete, fold, hoist, or warm-up your benchmark until it measures something other than the code you wrote.</Crux>

<Explanation>

## Why the JIT is your benchmark's adversary

Before you write another benchmark loop, ask yourself: what does TurboFan do when it sees a computation whose result is never observed? It removes it — completely. That is the root of every trap below.

A microbenchmark is a tiny program whose output you ignore, run in a tight loop — which is exactly the program an optimising compiler is *best* at demolishing. TurboFan's whole job is to remove work that does not affect observable behaviour. If your benchmark's result is never observed, the work that produced it is, by definition, removable. The traps below are all consequences of the optimiser doing its job on code you did not intend it to optimise away.

<FlowDiagram
  label="The anatomy of a misleading benchmark versus a sound one: the misleading path lets the optimiser eliminate, fold and hoist the body and measures warm-up, while the sound path consumes the result into a sink, randomises inputs, warms up before timing, and isolates allocation."
  hue="var(--accent)"
  caption="Two harnesses, same intent. The top path measures a phantom; the bottom path measures steady-state work because it defeats each optimiser trap on purpose."
  nodes={[
    { id: "mislead", label: "Misleading", sub: "result unused", col: 0, row: 0 },
    { id: "dce", label: "DCE + fold", sub: "body deleted", col: 1, row: 0 },
    { id: "phantom", label: "0ms phantom", sub: "fantasy number", col: 2, row: 0 },
    { id: "sound", label: "Sound", sub: "consume + randomise", col: 0, row: 1 },
    { id: "warm", label: "Warm up", sub: "reach steady tier", col: 1, row: 1 },
    { id: "real", label: "Real number", sub: "steady-state cost", col: 2, row: 1 },
  ]}
  edges={[
    { from: "mislead", to: "dce", label: "optimiser" },
    { from: "dce", to: "phantom", label: "measures nothing" },
    { from: "sound", to: "warm", label: "sink + inputs" },
    { from: "warm", to: "real", label: "measures work" },
  ]}
/>

## Trap 1 — dead-code elimination (DCE)

If the loop body computes a value nothing reads, the optimiser proves the computation is observably useless and deletes it. Your "1 billion ops" benchmark becomes an empty loop — sometimes the loop itself is removed too. The fix is a **black-hole sink**: accumulate every result into a variable you read *after* the timed region (and ideally print or return), so the optimiser cannot prove the work is dead.

```js
// LIES — result discarded, body eliminated
for (let i = 0; i < 1e9; i++) { expensive(i); }

// HONEST — sink forces the work to be observable
let sink = 0;
for (let i = 0; i < 1e9; i++) { sink += expensive(i); }
console.log(sink); // read it so DCE cannot remove the loop
```

## Trap 2 — constant folding and loop-invariant hoisting

If the input is a literal constant, the optimiser computes the answer once at compile time (**constant folding**) and your loop measures nothing. If a sub-expression does not change across iterations, it is **hoisted** out of the loop (loop-invariant code motion) and runs once instead of N times. The fix: make inputs vary unpredictably — derive them from the loop index or a pre-generated array of random values the optimiser cannot precompute (a "blackbox" input).

## Trap 3 — un-accounted JIT warm-up

The first iterations run in Ignition (interpreted), then Sparkplug, then Maglev/TurboFan once the function is hot. If you time from iteration zero, you average cold interpretation, compilation pauses, and steady-state machine code into one meaningless number — and a "fast" result may just be a benchmark too short to ever reach TurboFan. Discard a warm-up phase, then measure; or report tiers separately if you care about both start-up and steady state.

## Trap 4 — OSR mid-measurement

One giant `for` loop can trigger **on-stack replacement**: V8 compiles and swaps in optimised code *while the loop is still running*, so the early part of the very loop you are timing runs interpreted and the rest runs optimised — a discontinuity baked into a single sample. Prefer many calls to a smaller benchmark function (which warms up the *function*, not one loop) over one enormous loop.

## Trap 5 — GC noise in the timed region

If the body allocates, the garbage collector will eventually pause inside your measurement, attributing GC time to your code as a random spike. Either remove allocation from the timed region, or run the benchmark many times and report a robust statistic (median/min) rather than the mean, so a stray GC pause does not dominate. Report distribution, not a single number.

## Trap 6 — deopts you smuggled into the timed region

`console.log` inside the timed loop, a `try/catch` that V8 will not optimise across, or a polymorphic call introduced by the harness itself can deopt the very function you are measuring — so you measure the deoptimised version. Keep the timed region pure: no logging, no exceptions, one stable shape and type, and verify with `--trace-deopt` that the hot function is not bailing during measurement.

## Trap 7 — micro-vs-macro mismatch

Even a perfect microbenchmark answers only "how fast is this isolated pattern in a synthetic loop". Your app runs it interleaved with other work, with cold caches, real input distributions, and GC competition — where the micro winner can lose. Microbenchmarks pick between two implementations of a known hot spot; they do not tell you the hot spot matters. For end-to-end truth use **macro** suites (JetStream, Speedometer) or profile the real app. For honest *micro* numbers, use a harness built to defeat these traps: `tinybench` or `mitata` handle warm-up, sinks, and statistics for you.

<NumbersCard
  title="Honest-benchmark checklist"
  data-lesson-visual
  stats={[
    { label: "Consume results into", value: "a black-hole sink" },
    { label: "Inputs", value: "randomised / blackboxed" },
    { label: "Warm-up", value: "discard, then measure" },
    { label: "Timer", value: "performance.now() (sub-ms)" },
    { label: "Report", value: "median/min, not mean" },
    { label: "Verify hot fn", value: "--trace-deopt = no bail" },
  ]}
/>

<Misconception id="mc-zero-ms">
  <p><strong>"The loop ran in 0ms, so this code is essentially free."</strong> A 0ms result almost never means "infinitely fast" — it means dead-code elimination deleted the body because its result was unused. Consume the result in a sink and read it after timing; the honest number is never zero.</p>
</Misconception>

<Quiz
  id="q-zero-ms-cause"
  lessonSlug="03-benchmarking-pitfalls"
  lang="en"
  question="Your benchmark loop over a pure function reports 0.0ms for a billion iterations. What happened?"
  choices={[
    { label: "Dead-code elimination removed the loop body because its result was never read", correct: true },
    { label: "V8 is genuinely that fast — pure functions are free", misconception: "No real computation of a billion iterations is free. A 0ms result is the optimiser proving the work is unobservable and deleting it, not the work being instant." },
    { label: "performance.now() lacks the resolution to measure it", misconception: "performance.now() has sub-millisecond resolution; it would still show non-zero for a billion real iterations. The body was eliminated." },
    { label: "The garbage collector ran and reset the timer", misconception: "GC does not reset timers; it would only add time. A zero result is DCE, not GC." },
  ]}
/>

<Quiz
  id="q-warmup"
  lessonSlug="03-benchmarking-pitfalls"
  lang="en"
  question="You time from the very first iteration of a hot function. Why is the number misleading?"
  choices={[
    { label: "It averages cold interpretation, compilation pauses, and steady-state machine code into one number", correct: true },
    { label: "The first iteration is always the fastest, biasing the result low", misconception: "The first iterations are the SLOWEST (interpreted + compiling), biasing a from-zero average high — and conflating tiers regardless of direction." },
    { label: "performance.now() is not yet initialised on iteration zero", misconception: "The timer is ready immediately; the issue is mixing warm-up tiers into the measurement, not timer initialisation." },
    { label: "Warm-up has no effect; V8 optimises before main() runs", misconception: "V8 optimises adaptively at runtime after a function gets hot, not ahead of time. Early iterations genuinely run in lower tiers." },
  ]}
/>

<DragOrder
  id="do-honest-harness"
  pieceSlug="03-benchmarking-pitfalls"
  lang="en"
  prompt="Order the steps to turn a naive loop into an honest microbenchmark."
  items={[
    "Feed randomised / blackboxed inputs so nothing can be constant-folded",
    "Accumulate each result into a black-hole sink read after timing",
    "Run a warm-up phase and discard it so the function reaches steady tier",
    "Measure many runs and report median/min, then verify no deopt with --trace-deopt",
  ]}
/>

<Inset kind="edgecase" lang="en">
The cruel twist: a microbenchmark can be *too* favourable. In a tiny loop, one object shape and one type reach the hot function, so the IC is perfectly monomorphic and TurboFan inlines everything — a fast number. In production the same function sees five shapes, goes megamorphic, and never inlines. The micro number was honest about the micro conditions and dishonest about your app. This is why a passing microbenchmark is necessary but never sufficient: confirm with a macro run or a production profile.
</Inset>

</Explanation>

<KeyTakeaway>An optimising JIT will eliminate dead code, fold constants, hoist invariants, and conflate warm-up tiers until your benchmark measures a phantom. Defeat each trap on purpose: sink the result, randomise inputs, discard warm-up, isolate allocation, keep the timed region deopt-free, and confirm micro wins with a macro run.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="benchmarking-pitfalls-retrieval"
  lang="en"
  questions={[
    { q: "Explain dead-code elimination in a benchmark and the black-hole sink that defeats it.", a: "An optimising compiler removes any computation whose result is not observable. In a microbenchmark you typically discard the result of the function under test, so the optimiser can prove the computation has no observable effect and delete the loop body — sometimes the loop entirely — yielding a ~0ms 'infinitely fast' result that measures nothing. The fix is a black-hole sink: accumulate every result into a variable (sink += f(i)) and then read that variable after the timed region (print it or return it). Because the sink's value is observed, the optimiser cannot prove the work is dead and must keep it. Real harnesses (tinybench, mitata) provide a do-not-eliminate sink internally; mrale's classic write-up on microbenchmark fairy-tales documents the trap in detail." },
    { q: "What are JIT warm-up and OSR, and how do they corrupt a from-zero measurement?", a: "Code starts in Ignition (interpreted), is promoted to Sparkplug, then Maglev/TurboFan once it runs enough to be hot. If you start timing at iteration zero you blend cold interpretation, the compilation pauses themselves, and steady-state machine code into one number — and a benchmark too short to reach TurboFan reports a number that has nothing to do with the optimised steady state. On-stack replacement (OSR) makes it worse for one-giant-loop benchmarks: V8 can compile and swap in optimised code while the loop is mid-flight, so part of the very loop you time runs interpreted and part runs optimised. Fixes: run and discard a warm-up phase, then measure steady state (or report tiers separately); and prefer many invocations of a small benchmark function over a single enormous loop so you warm up the function rather than triggering OSR inside the measured loop." },
    { q: "Why is a passing microbenchmark necessary but not sufficient, and what do macro suites add?", a: "A microbenchmark measures one isolated pattern in synthetic conditions: one input shape, hot caches, no competing work. Those conditions often make the code look better than reality — a monomorphic IC and full inlining that the same function never gets in production, where it sees varied shapes, cold caches, real input distributions, and GC competition. So a micro win can evaporate or reverse end to end. Microbenchmarks are good for choosing between two implementations of a known hot spot, not for proving the hot spot matters or that a change helps the whole app. Macro suites (JetStream, Speedometer) and real-app CPU profiles measure end-to-end, realistic workloads, capturing the interactions a microbenchmark hides. Use micro to pick an implementation, macro/profile to confirm it helps in situ." },
  ]}
/>

<Recap lang="en">
A microbenchmark is the program an optimising JIT is best at demolishing, because its result is ignored and its loop is tight. Seven traps follow: dead-code elimination deletes a body whose result is unused (defeat with a black-hole sink read after timing); constant folding and loop-invariant hoisting precompute literal or unchanging inputs (defeat with randomised/blackboxed inputs); un-accounted warm-up blends Ignition, compilation pauses and TurboFan steady state into one number (discard a warm-up phase or report tiers separately); OSR swaps optimised code into a still-running giant loop (prefer many small calls); GC noise injects pauses into the timed region (isolate allocation, report median/min); console.log/try-catch/polymorphism smuggle a deopt into the measured function (keep the region pure, verify with --trace-deopt); and micro-vs-macro mismatch means even a perfect micro number does not reflect the app (confirm with JetStream/Speedometer or a production profile). For honest micro numbers use tinybench or mitata, which handle sinks, warm-up and statistics for you. Now when you see a 0ms benchmark result, you will not celebrate — you will reach for a black-hole sink and run it again.
</Recap>
