---
slug: "05-string-internals"
lang: en
track: "js-engine"
unit: "02-values-and-memory"
order: 5
title: "How V8 stores strings"
summary: "V8 represents strings as a tree of types: flat Seq strings (Latin1 or UTF-16), ConsString (O(1) concatenation node), SlicedString (O(1) substring that pins its parent), ThinString, and ExternalString. Internalised strings compare by pointer."
estMin: 14
status: ready
lessonType: topic
level: middle
concepts:
  - SeqString
  - ConsString
  - SlicedString
  - ThinString
  - ExternalString
  - internalized strings
  - flattening
  - parent retention
deepensInto:
  - "06-garbage-collection/01-why-gc"
spiral:
  - performance-cliffs
prereqs:
  - "04-the-heap"
sources:
  - https://v8.dev/blog/cost-of-javascript-2019
  - https://iliazeus.github.io/articles/js-string-optimizations-en/
  - https://v8.dev/docs
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
You parse a 50 MB log file, pull out a 12-character request ID with `line.slice(0, 12)`, push it into an array, and let everything else go. Hours later the process is sitting on hundreds of megabytes for an array of tiny IDs. The strings you kept are 12 characters each — but each one is secretly holding its entire source line, and through it the whole buffer, alive. V8's string representations are why, and why one `+ ''` fixes it.
</Hook>

<Crux>V8 strings are a tree of types — flat Seq, O(1) ConsString and SlicedString — so concat and slice are cheap but can pin memory.</Crux>

<Explanation>

## A string is not always a flat array of characters

The naive model — "a string is a contiguous block of characters" — is only one of V8's several string representations. V8 picks a representation to make the *common operations* (concatenation, slicing, comparison) cheap, and only materialises a flat array of characters when it has to. A single JS string value can be any of these HeapObject kinds:

- **SeqOneByteString / SeqTwoByteString** — the flat case. The characters are stored contiguously right in the object. V8 uses a **one-byte (Latin1)** backing when every character fits in 8 bits, and a **two-byte (UTF-16)** backing otherwise — so an ASCII string takes half the memory of one with any non-Latin1 character.
- **ConsString** — a concatenation node. `a + b` does *not* copy characters; it allocates a small node holding **pointers to `a` and `b`** (`first` and `second`) and a total length. Concatenation is therefore **O(1)**, and repeated concatenation builds a *tree* of ConsStrings.
- **SlicedString** — a substring view. `str.slice(i, j)` allocates a node holding a **pointer to the parent string** plus an offset and a length. The substring is **O(1)** and shares the parent's characters — but it keeps the entire parent alive.
- **ThinString** — a forwarding pointer. When a string is internalised (see below) but an old non-internalised copy still exists, the old one becomes a ThinString that just points at the canonical internalised string.
- **ExternalString** — characters owned by C++ outside the V8 heap (e.g. a source file the embedder kept in its own buffer). V8 holds a pointer to that external storage instead of copying.

<FlowDiagram
  label="The result of building a string by concatenation: a + b becomes a ConsString node pointing at two leaves, and a slice of that result becomes a SlicedString node pointing back into the parent it views."
  hue="var(--accent)"
  caption="String operations build a tree, not a flat buffer. ConsString shares its operands; SlicedString shares (and pins) its parent. Both are O(1) until something forces a flatten."
  nodes={[
    { id: "cons", label: "ConsString", sub: "a + b", col: 1, row: 0 },
    { id: "a", label: "Seq 'a'", sub: "first", col: 0, row: 0 },
    { id: "b", label: "Seq 'b'", sub: "second", col: 2, row: 0 },
    { id: "slice", label: "SlicedString", sub: "view of cons", col: 1, row: 1 },
  ]}
  edges={[
    { from: "cons", to: "a", label: "first" },
    { from: "cons", to: "b", label: "second" },
    { from: "slice", to: "cons", label: "parent + range" },
  ]}
/>

## Internalised strings: equality in one cycle

V8 maintains an internal **string table**. Identifiers, property names, and string literals are **internalised**: deduplicated so there is exactly one canonical copy of each distinct string. The payoff is enormous for the engine's own work — comparing two internalised strings for equality is a single **pointer comparison** (1 cycle), not a character-by-character scan whose cost scales with length.

This is why property access is fast: the property name in your code and the name stored in the object's Map are the same internalised string, so the lookup compares pointers. When you create a fresh non-internalised string that equals an existing internalised one (say by reading it from input), and it is then used as a property key, V8 may turn it into a **ThinString** forwarding to the canonical copy, recovering pointer-equality for future comparisons.

## Flattening: the hidden cost of using a ConsString

A ConsString is cheap to *build* but cannot be used directly for operations that need contiguous characters — most importantly, **indexing** (`str[i]`, `charCodeAt`), regex, and passing to C++ APIs. The first time you do something that needs the flat form, V8 **flattens** the string: it walks the ConsString tree and copies all the characters into a single Seq string, then rewrites the node to point at the flat result.

So the cost of a `+`-built string is deferred, not free. If you build a huge string with `+=` in a loop and then index it once, you pay one O(n) flatten at that moment. Usually that is fine — but a *pathological* pattern is building a deep ConsString tree and repeatedly triggering re-flattening, or holding many un-flattened cons trees that bloat the heap. (Modern V8 handles `+=` loops well; the classic advice to always use an array + `join` matters most for truly enormous builds.)

## The memory pitfall: a slice that pins its parent

Back to the Hook. `bigString.slice(0, 12)` returns a **SlicedString** that holds a pointer to `bigString`. As long as that 12-character slice is reachable, **`bigString` cannot be collected** — the GC sees a live reference to it through the slice. Keep thousands of tiny slices of a giant file and you keep the giant file (and its backing buffer) entirely alive, even though logically you only wanted a few hundred bytes.

```js
// LEAK: each id is a SlicedString pinning its whole 50 MB source line / buffer.
const ids = lines.map(line => line.slice(0, 12));

// FIX: force a fresh, flat, independent copy that does NOT reference the parent.
const ids = lines.map(line => (line.slice(0, 12) + '').slice());
// or, explicit and clear:
const ids = lines.map(line => {
  const s = line.slice(0, 12);
  return s.repeat(1);            // forces flattening into a standalone Seq string
});
```

The cure is to **force a flatten into a standalone string** that copies the characters and drops the parent pointer. Concatenating `+ ''`, or any operation that produces a fresh Seq string, breaks the retention chain so the GC can reclaim the parent. The trade is an O(slice-length) copy now in exchange for releasing an O(parent-length) retention — almost always worth it for long-lived small slices.

<NumbersCard
  title="String representation numbers"
  data-lesson-visual
  stats={[
    { label: "ASCII backing (Latin1)", value: "1 byte / char" },
    { label: "Non-Latin1 backing (UTF-16)", value: "2 bytes / char" },
    { label: "ConsString concat cost", value: "O(1) — a node, no copy" },
    { label: "SlicedString substring cost", value: "O(1) — pins the parent" },
    { label: "Internalised string equality", value: "1 pointer comparison" },
    { label: "Flatten on first index of a cons", value: "O(n) one-time copy" },
  ]}
/>

<Misconception id="mc-slice-cheap">
  <p><strong>"slice() is cheap and only keeps a few bytes."</strong> The slice is O(1) and tiny — but it is a SlicedString pointing at the <em>entire</em> parent, which cannot be collected while the slice lives. Small slices pinning huge parents is a classic leak. Force a copy (<code>slice + ''</code>) to release the parent.</p>
</Misconception>

<Quiz
  id="q-cons-cost"
  lessonSlug="05-string-internals"
  lang="en"
  question="What does `a + b` actually allocate in V8 for two existing strings, and what is the cost?"
  choices={[
    { label: "A new flat string with all characters copied — O(n)", misconception: "That's what flattening does later, on demand. The concatenation itself doesn't copy; it builds a ConsString node." },
    { label: "A ConsString node holding pointers to a and b — O(1), no character copy until something forces a flatten", correct: true },
    { label: "Nothing — V8 mutates a in place", misconception: "Strings are immutable in JS; a is never mutated. A new ConsString node is allocated to represent the concatenation." },
    { label: "A SlicedString of a and b", misconception: "SlicedString represents a substring view of one parent. Concatenation of two strings produces a ConsString, not a SlicedString." },
  ]}
/>

<Quiz
  id="q-slice-retention"
  lessonSlug="05-string-internals"
  lang="en"
  question="You keep an array of 10,000 twelve-character slices taken from 10,000 large lines, and discard the lines. Why does memory stay high?"
  choices={[
    { label: "V8 forgot to run GC on the slices", misconception: "GC runs fine; the slices are genuinely reachable. The issue is what each slice keeps reachable, not a GC failure." },
    { label: "Each slice is a SlicedString holding a pointer to its parent line, so every large line stays alive through its slice", correct: true },
    { label: "Slices are stored in old space and old space never shrinks", misconception: "Old space can shrink after major GC. The real cause is the parent retention via SlicedString, not space behaviour." },
    { label: "Twelve-character strings are always two-byte and large", misconception: "Twelve chars is tiny regardless of encoding. The memory is held by the pinned parents, not by the slices' own size." },
  ]}
/>

<DragOrder
  id="do-flatten"
  pieceSlug="05-string-internals"
  lang="en"
  prompt="Order what happens when you build a string with repeated + and then call charCodeAt on it once."
  items={[
    "Each + allocates a ConsString node pointing at its two operands",
    "Repeated + builds a tree of ConsStrings, no characters copied",
    "charCodeAt needs contiguous characters at a given index",
    "V8 flattens: walks the tree and copies all chars into one Seq string",
    "The node is rewritten to point at the flat result for future access",
  ]}
/>

<Inset kind="edgecase" lang="en">
The one-byte vs two-byte choice is per string and decided at creation: a string of pure ASCII gets a Latin1 backing at 1 byte per character; introduce a single emoji or Cyrillic character and the whole string is stored two-byte at 2 bytes per character. This is why concatenating user text with non-Latin1 content can double the memory of an otherwise-ASCII payload, and why some servers normalise or segment text to keep large hot strings one-byte.
</Inset>

</Explanation>

<KeyTakeaway>V8 strings are a tree of representations: flat Seq (Latin1 or UTF-16), O(1) ConsString for concat, O(1) SlicedString for substrings (which pins the parent), ThinString, and ExternalString. Internalised strings compare by pointer in one cycle. Building with + defers an O(n) flatten until you index; and a tiny slice keeps its whole parent alive until you force a standalone copy.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="string-internals-retrieval"
  lang="en"
  questions={[
    { q: "Name V8's string representations and what each optimises.", a: "SeqOneByteString and SeqTwoByteString are the flat forms with characters stored contiguously — Latin1 (1 byte/char) when all characters fit in 8 bits, UTF-16 (2 bytes/char) otherwise. ConsString is a concatenation node holding pointers to its two operands, making a + b O(1) with no character copy and building a tree under repeated concatenation. SlicedString is a substring view holding a pointer to its parent plus an offset and length, making slice O(1) but keeping the parent alive. ThinString is a forwarding pointer to a canonical internalised string. ExternalString points at characters owned by C++ outside the V8 heap, avoiding a copy of embedder-owned data." },
    { q: "What are internalised strings and why do they make property access fast?", a: "V8 keeps an internal string table that deduplicates identifiers, property names, and string literals so there is exactly one canonical copy of each distinct string. Comparing two internalised strings for equality is a single pointer comparison (one cycle) rather than a length-dependent character scan. Property access exploits this: the property name in your code and the name recorded in the object's Map are the same internalised string, so the lookup compares pointers. A fresh non-internalised string that equals an existing one can be turned into a ThinString forwarding to the canonical copy, restoring pointer-equality for later comparisons." },
    { q: "Explain flattening and the SlicedString retention pitfall, and how to fix the leak.", a: "A ConsString is cheap to build but can't be used directly where contiguous characters are needed — indexing, charCodeAt, regex, C++ APIs — so the first such use forces a flatten: V8 walks the cons tree, copies all characters into one Seq string (O(n)), and rewrites the node to the flat result. Separately, a SlicedString holds a pointer to its entire parent, so a tiny slice keeps the whole parent (and its backing buffer) alive and uncollectable while the slice is reachable — a classic leak when you keep many small slices of huge sources. Fix it by forcing a standalone flat copy that drops the parent pointer, e.g. slice + '' or any op that yields a fresh Seq string; you pay an O(slice-length) copy to release an O(parent-length) retention." },
  ]}
/>

<Recap lang="en">
V8 does not store every string as a flat character array; it chooses among several HeapObject representations to keep common operations cheap. Seq strings are the flat form, backed by Latin1 at one byte per character for pure-ASCII text and by UTF-16 at two bytes otherwise, so a single non-Latin1 character doubles a string's memory. ConsString makes concatenation O(1) by allocating a node that points at its two operands rather than copying characters, building a tree under repeated +. SlicedString makes substrings O(1) by holding a pointer to the parent plus an offset and length — but that pointer keeps the whole parent alive, so retaining many small slices of large sources is a classic memory leak fixed by forcing a standalone flat copy such as slice + ''. ThinString forwards to a canonical internalised string, and ExternalString points at characters owned by embedder C++ to avoid a copy. Internalised strings, kept in V8's string table, let equality and property-name comparison be a single pointer comparison instead of a character scan, which is a major reason property access is fast. The deferred cost of building with + is flattening: the first operation needing contiguous characters (indexing, regex) walks the cons tree and copies everything into one Seq string in O(n). Knowing these representations turns surprising string memory and CPU behaviour into predictable consequences of how V8 stores text.
</Recap>
