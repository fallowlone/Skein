---
slug: "02-microtasks-vs-macrotasks"
lang: en
track: "js-engine"
unit: "07-async-deep"
order: 2
title: "Microtasks vs macrotasks: ordering"
summary: "One macrotask runs per loop turn; the entire microtask queue drains after each macrotask and whenever the JS stack empties. A microtask that enqueues a microtask runs in the same drain — the root of starvation. Plus Node's process.nextTick ordering."
estMin: 13
status: ready
lessonType: topic
level: middle
concepts:
  - microtask vs macrotask
  - microtask drain
  - microtask starvation
  - process.nextTick
  - FIFO ordering
  - task sources
deepensInto:
  - "07-async-deep/03-promise-internals"
spiral:
  - statefulness
  - performance-cliffs
prereqs:
  - "01-event-loop-recap"
sources:
  - https://html.spec.whatwg.org/multipage/webappapis.html#microtask-queue
  - https://nodejs.org/en/learn/asynchronous-work/event-loop-timers-and-nexttick
  - https://developer.mozilla.org/en-US/docs/Web/API/HTML_DOM_API/Microtask_guide
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A junior asks you to "fix the bug" where this prints `1 4 3 2` instead of `1 2 3 4`. There is no bug. The order is the contract: a `setTimeout` callback waits its turn as a macrotask while a `Promise.then` jumps ahead as a microtask. If you cannot predict that interleaving cold, you cannot reason about a race in production — and you certainly cannot explain why a tight promise chain just froze the tab.
</Hook>

<Crux>One macrotask per loop turn; the whole microtask queue drains after each macrotask and whenever the stack empties.</Crux>

<Explanation>

## Two queues, two cadences

The previous lesson fixed who owns what: the host owns the loop and the macrotask queues; the engine owns the microtask queue. This lesson makes the *ordering* precise, because the two queues drain on completely different cadences.

- **Macrotasks** (the spec calls them simply *tasks*) — exactly **one** runs per loop turn. Sources: `setTimeout`/`setInterval` callbacks, I/O completion callbacks, `MessageChannel`/`postMessage` messages, fired DOM events, parsed HTML chunks. After running one, the loop does *not* immediately run the next macrotask.
- **Microtasks** — the **entire** queue is drained after each macrotask, and again whenever the JavaScript call stack empties. Sources: `Promise.then`/`.catch`/`.finally` reactions, `queueMicrotask` jobs, `await` continuations, `MutationObserver` callbacks.

The asymmetry is the whole point. Macrotasks are rationed — one per turn — so the loop can render and handle input between them. Microtasks are *exhaustive* — drained to empty — so a coherent unit of async follow-up (a promise chain) completes atomically before anything else happens. The browser overview ([browser/01-event-loop/04-microtask-starvation](/learn/browser/04-microtask-starvation)) shows the failure mode at runtime depth; here we tie it to the queue mechanics.

<SequenceDiagram
  label="One loop turn from the queues' perspective: the host loop dequeues exactly one macrotask and runs it, the engine then drains the entire microtask queue to empty, and only afterwards does the host render and dequeue the next single macrotask."
  hue="var(--accent)"
  caption="One macrotask per turn; the entire microtask queue drained between macrotasks. The microtask drain is exhaustive, the macrotask dequeue is one-at-a-time."
  actors={["Macrotask Q", "Loop", "Microtask Q"]}
  messages={[
    { from: "Macrotask Q", to: "Loop", label: "dequeue ONE task" },
    { from: "Loop", to: "Microtask Q", label: "run task; enqueue reactions" },
    { from: "Microtask Q", to: "Loop", label: "drain ALL (FIFO)" },
    { from: "Loop", to: "Loop", label: "render" },
    { from: "Macrotask Q", to: "Loop", label: "dequeue NEXT task" },
  ]}
/>

## The canonical ordering example

```javascript run
console.log("1: sync start");

setTimeout(() => console.log("4: macrotask (setTimeout)"), 0);

Promise.resolve()
  .then(() => console.log("3a: microtask one"))
  .then(() => console.log("3b: microtask two"));

queueMicrotask(() => console.log("3c: queueMicrotask"));

console.log("2: sync end");
// Output: 1 → 2 → 3a → 3c → 3b → 4
```

Walk it precisely. The synchronous script *is the current macrotask*. It runs to completion first: `1`, then `2`. Along the way it scheduled a macrotask (the `setTimeout`) and microtasks (the `.then` and the `queueMicrotask`). When the script's stack empties, the engine drains the microtask queue in **FIFO** order: the first `.then` (`3a`) and the `queueMicrotask` (`3c`) were enqueued during the sync run, so they go first; `3a`'s handler returns, which *now* settles the chained promise and enqueues `3b` — appended to the same drain — so `3b` runs before the drain ends. Only when the microtask queue is finally empty does the host pick the next macrotask: `4`.

<NumbersCard
  title="Microtask vs macrotask at a glance"
  data-lesson-visual
  stats={[
    { label: "Macrotasks run per loop turn", value: "exactly 1" },
    { label: "Microtasks run per checkpoint", value: "all, to empty" },
    { label: "Drain order within a queue", value: "FIFO" },
    { label: "Render can interleave between", value: "macrotasks only" },
    { label: "queueMicrotask vs Promise.then", value: "same queue" },
    { label: "Node: process.nextTick vs Promise", value: "nextTick first" },
  ]}
/>

## Why a microtask that enqueues a microtask is dangerous

Because the microtask checkpoint drains to *empty*, a microtask that enqueues another microtask before returning is processed **in the same drain**, not the next one. There is no "next turn" boundary to escape to. Repeat it and the queue never empties:

```javascript
function spin() {
  Promise.resolve().then(spin); // re-arms inside the same checkpoint
}
spin(); // freezes: the checkpoint never completes, the engine never returns
```

This is **microtask starvation**. The host never gets the thread back (the engine is stuck inside `PerformMicrotaskCheckpoint` from lesson 1), so no macrotask runs, no frame paints, no input is handled. Contrast with a macrotask self-loop — `setTimeout(spin, 0)` — which re-arms on a *host* queue, so the engine returns between iterations and the page stays responsive (just busy). The cure for starvation is always to insert a macrotask-level yield into the chain.

<Misconception id="mc-microtask-after-task">
  <p><strong>"A microtask runs after the current task, like the next setTimeout."</strong> No — the whole microtask queue drains <em>before</em> the next macrotask and before rendering. A microtask scheduled by another runs in the same drain — so a microtask loop can freeze the page; a setTimeout loop cannot.</p>
</Misconception>

## Node specifics: process.nextTick and per-phase draining

Node complicates the picture with two extra rules. First, `process.nextTick` callbacks run in a queue that is **separate from and drained before** the Promise microtask queue. So in Node, `process.nextTick` always beats `Promise.resolve().then`. Second, Node's loop runs in **phases** (timers → pending callbacks → poll → check → close), and historically Node drained microtasks *between phases* rather than after every single callback — though since Node 11 it drains microtasks after each individual macrotask callback, bringing it closer to browser semantics.

```javascript
// In Node:
setTimeout(() => console.log("timeout"), 0);     // timers phase (macrotask)
setImmediate(() => console.log("immediate"));     // check phase (macrotask)
Promise.resolve().then(() => console.log("promise")); // microtask
process.nextTick(() => console.log("nextTick"));  // nextTick queue — first

// Order: nextTick → promise → (timeout|immediate, order between these
// two is not guaranteed from the main module)
```

The practical senior takeaway: never rely on `setTimeout` vs `setImmediate` ordering from the top level, but *do* rely on `process.nextTick` running before promise microtasks. And beware: `process.nextTick` has the same starvation hazard as a microtask loop — a recursive `nextTick` starves the phase loop just as a recursive `.then` starves the browser.

<FlowDiagram
  label="The decision a callback's source determines its queue and cadence: timer and I/O callbacks go to a host macrotask queue (one per turn); promise reactions and queueMicrotask go to the engine microtask queue (drained to empty); in Node, process.nextTick goes to its own queue drained before promises."
  hue="var(--accent)"
  caption="Source determines queue determines cadence. nextTick (Node only) drains before the promise queue; promise/queueMicrotask drain to empty; timers run one per turn."
  nodes={[
    { id: "src", label: "Callback source", sub: "what scheduled it", col: 0, row: 1 },
    { id: "nt", label: "nextTick queue", sub: "Node: drains first", col: 1, row: 0 },
    { id: "mt", label: "Microtask queue", sub: "drain to empty", col: 1, row: 1 },
    { id: "mac", label: "Macrotask queue", sub: "one per turn", col: 1, row: 2 },
  ]}
  edges={[
    { from: "src", to: "nt", label: "process.nextTick" },
    { from: "src", to: "mt", label: ".then / queueMicrotask" },
    { from: "src", to: "mac", label: "setTimeout / I/O" },
  ]}
/>

<Quiz
  id="q-ordering"
  lessonSlug="02-microtasks-vs-macrotasks"
  lang="en"
  question="Inside a `setTimeout` callback you run `Promise.resolve().then(work)` and also schedule another `setTimeout(other, 0)`. In what order do `work` and `other` run?"
  choices={[
    { label: "`work` first — it is a microtask that drains before the loop picks the next macrotask `other`", correct: true },
    { label: "`other` first — it was scheduled with a 0 ms delay so it fires immediately", misconception: "A 0 ms timer is still a macrotask. The microtask queue drains to empty after the current macrotask, before the loop dequeues the next macrotask. work runs first." },
    { label: "They run concurrently on separate threads", misconception: "JavaScript is single-threaded run-to-completion. Neither runs concurrently; work (microtask) runs, then the loop turns and runs other (macrotask)." },
    { label: "`other` first because timers have higher priority than promises", misconception: "There is no such priority. Microtasks always drain between macrotasks, so the promise reaction precedes the next timer regardless of delay." },
  ]}
/>

<Quiz
  id="q-node-nexttick"
  lessonSlug="02-microtasks-vs-macrotasks"
  lang="en"
  question="In Node, you schedule `process.nextTick(a)` and `Promise.resolve().then(b)` from the same synchronous block. Which runs first, and why?"
  choices={[
    { label: "`a` — the nextTick queue is separate and drains before the Promise microtask queue", correct: true },
    { label: "`b` — promises are the standard microtask queue and take priority", misconception: "In Node, process.nextTick has its own queue that is drained before the Promise microtask queue at each opportunity. nextTick wins." },
    { label: "Whichever was scheduled first, by FIFO across both", misconception: "They are not in one FIFO queue. nextTick has a dedicated queue drained ahead of promises, regardless of scheduling order." },
    { label: "`b` — process.nextTick is a macrotask and runs next turn", misconception: "process.nextTick is not a macrotask; it is a microtask-like callback in its own pre-promise queue, drained within the same turn before promises." },
  ]}
/>

<DragOrder
  id="do-canonical-order"
  pieceSlug="02-microtasks-vs-macrotasks"
  lang="en"
  prompt="Order the output of the canonical example: a sync script that schedules one setTimeout, a two-link .then chain, and one queueMicrotask. Drag the log lines into the order they print."
  items={[
    "1: sync start (synchronous, current macrotask)",
    "2: sync end (synchronous, still the same macrotask)",
    "3a: first .then handler (microtask, enqueued during sync)",
    "3c: queueMicrotask (microtask, enqueued during sync)",
    "3b: second .then handler (microtask, enqueued by 3a in the same drain)",
    "4: setTimeout callback (next macrotask, after the queue drains)",
  ]}
/>

<Inset kind="edgecase" lang="en">
`await` continuations are microtasks too, so they obey the same exhaustive-drain rule. A function that `await`s in a loop will interleave its resumptions with every other pending microtask before any macrotask runs. This is why mixing `await` inside a hot synchronous-feeling loop can quietly defer a render far longer than you expect — the awaits keep the microtask queue non-empty. Lesson 04 makes the await-to-microtask mapping explicit.
</Inset>

</Explanation>

<KeyTakeaway>One macrotask runs per loop turn; the entire microtask queue drains (FIFO, to empty) after each macrotask and whenever the stack empties. A microtask enqueued during the drain runs in the same drain — the mechanical cause of starvation. In Node, `process.nextTick` drains before the Promise queue, with the same recursion hazard.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-microtasks-vs-macrotasks-retrieval"
  lang="en"
  questions={[
    { q: "State the exact draining rule for macrotasks vs microtasks, and name the sources of each.", a: "Macrotasks (the spec's 'tasks'): exactly one runs per loop turn — sources are setTimeout/setInterval, I/O completion callbacks, MessageChannel/postMessage, fired DOM events, parsed HTML chunks. Microtasks: the entire queue drains in FIFO order after each macrotask and whenever the JS call stack empties — sources are Promise.then/catch/finally reactions, queueMicrotask, await continuations, and MutationObserver callbacks. The asymmetry (one macrotask vs all microtasks) lets the loop render and handle input between macrotasks while a promise chain completes atomically as the tail of its task." },
    { q: "Trace why `1, setTimeout(4), Promise.then(3a).then(3b), queueMicrotask(3c), 2` prints 1 2 3a 3c 3b 4.", a: "The synchronous script is the current macrotask; it runs to completion: 1 then 2, scheduling one macrotask (setTimeout=4) and microtasks along the way. When the stack empties, the microtask queue drains FIFO: 3a (first .then) and 3c (queueMicrotask) were enqueued during the sync run, so 3a then 3c. When 3a's handler returns, the chained promise settles and enqueues 3b onto the same drain, so 3b runs before the drain ends. Only when the microtask queue is empty does the loop pick the next macrotask, printing 4." },
    { q: "How does Node's ordering differ from the browser, and what is the practical rule?", a: "Node adds a process.nextTick queue that is separate from and drained before the Promise microtask queue, so process.nextTick always beats Promise.resolve().then. Node's loop also runs in phases (timers, pending, poll, check, close); since Node 11 it drains microtasks after each individual macrotask callback (close to browser semantics). Practical rules: never rely on setTimeout vs setImmediate ordering from the top level, but do rely on nextTick running before promise microtasks; and beware that recursive process.nextTick starves the phase loop exactly as a recursive .then starves the browser." },
  ]}
/>

<Recap lang="en">
Macrotasks and microtasks drain on opposite cadences. Exactly one macrotask runs per loop turn — setTimeout, I/O completions, message events, DOM events — after which the loop can render and handle input. The entire microtask queue, by contrast, drains FIFO to empty after each macrotask and whenever the JS stack empties — Promise reactions, queueMicrotask, await continuations, MutationObserver. Because the drain is exhaustive, a microtask that enqueues another microtask runs in the same drain, not a later turn; repeated, this is microtask starvation, which freezes the page because the engine never finishes the checkpoint and never returns the thread to the host. A macrotask self-loop, by contrast, returns the thread each turn and stays responsive. Node adds a process.nextTick queue drained before the Promise queue (so nextTick wins) and a phase-based loop; since Node 11 microtasks drain after every macrotask callback, close to browser behaviour. The canonical ordering example — 1 2 3a 3c 3b 4 — is the muscle memory every senior needs to reason about async races. The next lesson opens the Promise itself to explain why reactions queue the way they do.
</Recap>
