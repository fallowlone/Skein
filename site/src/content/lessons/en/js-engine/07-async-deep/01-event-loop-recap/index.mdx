---
slug: "01-event-loop-recap"
lang: en
track: "js-engine"
unit: "07-async-deep"
order: 1
title: "The engine vs the loop"
summary: "The engine has no event loop — the host (browser, Node via libuv) owns it. What the engine provides is the microtask queue and one operation: PerformMicrotaskCheckpoint. The host runs a task; on return the engine drains microtasks."
estMin: 12
status: ready
lessonType: topic
level: middle
concepts:
  - event loop ownership
  - microtask queue
  - PerformMicrotaskCheckpoint
  - run-to-completion
  - host vs engine
  - macrotask
deepensInto:
  - "07-async-deep/02-microtasks-vs-macrotasks"
spiral:
  - statefulness
  - performance-cliffs
prereqs:
  - "06-garbage-collection/06-measuring-heap"
sources:
  - https://tc39.es/ecma262/#sec-jobs
  - https://html.spec.whatwg.org/multipage/webappapis.html#event-loops
  - https://v8.dev/blog/fast-async
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
You have read a dozen times that "JavaScript has an event loop." Then you open the V8 source and grep for it — and there is no event loop in there. There is a parser, an interpreter, optimising compilers, a garbage collector, and a queue of pending jobs. The loop that famously schedules your callbacks lives somewhere else entirely: in Chromium's message loop, in Node's libuv. The engine and the loop are two different programs.
</Hook>

<Crux>The engine owns the microtask queue and one checkpoint operation; the host owns the loop that picks tasks and calls into the engine.</Crux>

<Explanation>

## The engine does not have an event loop

The browser overview lessons treat the event loop as one machine — [browser/01-event-loop/01-loop-model](/learn/browser/01-loop-model) is the runtime view. This unit is the engine view, and the first correction is structural: **the engine has no event loop**. ECMAScript, the language V8 implements, never mentions one. The spec defines *jobs* and a *job queue*, and it says hosts must eventually run queued jobs — but the algorithm that decides *when* and *which task to run next* is explicitly left to the host (via the HOST hooks `HostEnqueuePromiseJob`, `HostCallJobCallback`).

So the division of labour is:

- **The host** (Chromium's message loop, Node plus libuv, Deno, Bun) owns the loop. It holds the macrotask queues (timers, I/O completions, message events), it decides which task runs next, and it drives rendering. It is the `while (true)` at the top.
- **The engine** (V8) owns one queue — the **microtask queue** (the ECMAScript "jobs queue") — and exposes one operation to drain it: `PerformMicrotaskCheckpoint`. It runs the JavaScript the host hands it, to completion, and on the way out it drains its own microtasks.

When people say "the event loop," they mean the host loop. When they say "microtasks run after the current task," they mean the engine's checkpoint. Conflating the two is why async ordering feels like folklore instead of mechanism.

## The shape of one turn: host calls in, engine returns, microtasks drain

A single turn of the host loop is a call *into* the engine and a return *out* of it. The host picks one macrotask (say, a fired `click` event or an expired timer), and calls into the engine on the associated JavaScript callback. The engine pushes a stack frame, runs your code **to completion** — JavaScript is run-to-completion, there is no pre-emption mid-function — and when the call stack empties, the engine performs a microtask checkpoint: it drains every queued microtask (Promise reactions, `queueMicrotask` jobs) until the queue is empty, including microtasks that other microtasks enqueued. Only then does control return to the host, which renders (in the browser) and picks the next macrotask.

<SequenceDiagram
  label="One turn of the host event loop expressed as a call into the engine and back: the host hands the engine a task, the engine runs it to completion, drains its own microtask queue, and only then returns control to the host, which renders and picks the next task."
  hue="var(--accent)"
  caption="The loop is the host's. The microtask queue and the drain (PerformMicrotaskCheckpoint) are the engine's. Microtasks are drained inside the engine before it returns — the host never sees them as separate tasks."
  actors={["Host loop", "V8 engine"]}
  messages={[
    { from: "Host loop", to: "V8 engine", label: "run task (callback)" },
    { from: "V8 engine", to: "V8 engine", label: "run to completion" },
    { from: "V8 engine", to: "V8 engine", label: "drain microtasks" },
    { from: "V8 engine", to: "Host loop", label: "return (stack empty)" },
    { from: "Host loop", to: "Host loop", label: "render, pick next task" },
  ]}
/>

The crucial detail: the microtask drain happens **inside** the single host-to-engine call, before the engine returns. From the host's point of view, running the click handler and running every promise reaction it scheduled is *one indivisible task*. That is why microtasks always run before the next timer, before the next render, before the next anything: they are not a separate task the host schedules — they are the tail of the task that is already running.

## Who provides the checkpoint, and who calls it

`PerformMicrotaskCheckpoint` is V8's; the *decision to call it* is partly shared. V8 will auto-run microtasks when the JS call stack empties (the default "auto" microtask policy used by the browser). Node uses an **explicit** policy: libuv runs a phase, and Node calls `runMicrotasks()` itself at well-defined points (after each macrotask callback, and between phases) — which is why Node's ordering has its own rules we cover next lesson. Either way, the queue and the drain are the engine's; the *cadence* is the host's.

<NumbersCard
  title="Who owns what in 'the event loop'"
  data-lesson-visual
  stats={[
    { label: "The while-loop / task selection", value: "host" },
    { label: "Macrotask queues (timers, I/O)", value: "host" },
    { label: "Rendering steps", value: "host (browser)" },
    { label: "Microtask queue (jobs queue)", value: "engine" },
    { label: "PerformMicrotaskCheckpoint", value: "engine" },
    { label: "When the checkpoint fires", value: "host policy" },
  ]}
/>

<Misconception id="mc-js-has-loop">
  <p><strong>"JavaScript has an event loop."</strong> The runtime does; the engine does not. ECMAScript defines a jobs queue and leaves scheduling to the host. V8 provides the microtask queue and <code>PerformMicrotaskCheckpoint</code>; Chromium and libuv provide the actual loop that picks tasks and calls into the engine.</p>
</Misconception>

## Run-to-completion is the load-bearing rule

Everything above rests on one engine guarantee: **run-to-completion**. Once the host hands the engine a callback, that callback runs start-to-finish with no other JavaScript interleaving. No second task, no microtask, nothing pre-empts it. This is what makes a `for` loop atomic from the program's view and what makes `await` (next lessons) need an explicit *suspension* mechanism — the engine cannot just "pause" a function the way an OS pauses a thread, because run-to-completion forbids it. The function must return to the host, and a microtask must resume it later.

<FlowDiagram
  label="The fixed order within one host loop turn: the host selects a macrotask, the engine runs it to completion, the engine drains the microtask queue, then the browser host renders, then the host loops back to select the next macrotask."
  hue="var(--accent)"
  caption="One turn. Microtasks sit between 'run task' and 'render' — they are the tail of the current task, not a new task."
  nodes={[
    { id: "pick", label: "Pick macrotask", sub: "host", col: 0, row: 0 },
    { id: "run", label: "Run to completion", sub: "engine", col: 1, row: 0 },
    { id: "drain", label: "Drain microtasks", sub: "engine", col: 2, row: 0 },
    { id: "render", label: "Render", sub: "host (browser)", col: 3, row: 0 },
  ]}
  edges={[
    { from: "pick", to: "run", label: "call in" },
    { from: "run", to: "drain", label: "stack empties" },
    { from: "drain", to: "render", label: "return" },
  ]}
/>

<Quiz
  id="q-who-owns-loop"
  lessonSlug="01-event-loop-recap"
  lang="en"
  question="You grep the V8 source and find no event loop. Where does the loop that schedules your `setTimeout` callbacks actually live?"
  choices={[
    { label: "In the host — Chromium's message loop in the browser, libuv in Node", correct: true },
    { label: "In V8's Ignition interpreter, as a hidden bytecode handler", misconception: "Ignition executes bytecode for one call; it does not loop over tasks. The interpreter has no concept of a timer queue or a next task." },
    { label: "In the ECMAScript specification's event-loop algorithm", misconception: "ECMAScript defines a jobs (microtask) queue and leaves the loop and task scheduling to the host. There is no event-loop algorithm in ECMA-262." },
    { label: "In the garbage collector, which ticks between collections", misconception: "Orinoco reclaims memory; it has nothing to do with task scheduling. The loop is the host's." },
  ]}
/>

<Quiz
  id="q-checkpoint-timing"
  lessonSlug="01-event-loop-recap"
  lang="en"
  question="From the host's perspective, running a click handler plus every promise reaction it schedules counts as how many tasks?"
  choices={[
    { label: "One task — the microtask drain happens inside the same host-to-engine call, before the engine returns", correct: true },
    { label: "Two tasks — the handler is one, the microtask drain is a second the host schedules", misconception: "The host never schedules the drain as a separate task. PerformMicrotaskCheckpoint runs inside the engine before control returns to the host, so it is the tail of the same task." },
    { label: "One task per promise reaction, plus one for the handler", misconception: "Each reaction is a microtask, not a host task. They all drain within the single host turn — the host sees one task, not N+1." },
    { label: "Zero — promise reactions run on a separate thread", misconception: "JavaScript is single-threaded run-to-completion. Reactions run on the same thread, inside the same checkpoint." },
  ]}
/>

<DragOrder
  id="do-one-turn"
  pieceSlug="01-event-loop-recap"
  lang="en"
  prompt="Order what happens in one turn of the browser's host loop, starting from an expired timer, ending just before the next task is picked."
  items={[
    "Host picks the timer macrotask and calls into the engine",
    "Engine runs the timer callback to completion (run-to-completion)",
    "Engine performs the microtask checkpoint, draining all queued microtasks",
    "Engine returns control to the host with the stack empty",
    "Host runs the rendering steps, then loops back to pick the next task",
  ]}
/>

<Inset kind="why" lang="en">
Why split it this way at all? Because one engine (V8) must serve hosts with wildly different loops — a browser that must render at 60 fps, a server that must not render at all and instead juggles thousands of sockets through libuv. Putting the loop in the host lets each host schedule for its own world while reusing one language core. The engine's contract is narrow on purpose: "give me a callback, I run it to completion and drain my jobs, then I hand you back the thread."
</Inset>

</Explanation>

<KeyTakeaway>The engine has no event loop. It owns the microtask queue and `PerformMicrotaskCheckpoint`; the host owns the loop that picks tasks and calls in. One host turn is: pick task → engine runs to completion → engine drains microtasks → return → render → repeat. Microtasks are the tail of the current task, not a new one.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-event-loop-recap-retrieval"
  lang="en"
  questions={[
    { q: "Precisely divide responsibilities between the engine and the host for 'the event loop'.", a: "The host (Chromium's message loop, Node's libuv, Deno, Bun) owns the loop itself: the macrotask queues (timers, I/O completions, message/DOM events), the algorithm that selects the next task, and rendering. The engine (V8) owns exactly one queue — the microtask queue, which ECMAScript calls the jobs queue — and one operation to drain it, PerformMicrotaskCheckpoint. The engine runs the JavaScript the host hands it to completion and drains microtasks before returning. ECMAScript deliberately leaves task scheduling to the host via HostEnqueuePromiseJob and friends, which is why the same V8 can power a 60 fps browser and a socket-juggling server." },
    { q: "Why do microtasks always run before the next macrotask and before rendering, with no exceptions in the browser?", a: "Because the microtask drain is not a separate task the host schedules — it happens inside the single host-to-engine call, after the callback's stack empties and before the engine returns control. From the host's perspective, the callback plus all the microtasks it scheduled are one indivisible task. So there is no point at which the host could insert another macrotask or a render between the callback and its microtasks: the engine has not returned yet." },
    { q: "Run-to-completion forbids the engine from pausing a function mid-execution. What consequence does that have for async/await?", a: "Because no JavaScript can pre-empt a running function, the engine cannot 'pause' an async function the way an OS pauses a thread. Instead, await must cause the function to return to the host, registering a continuation as a microtask that resumes the function later when the awaited value settles. async/await is therefore desugared into a state machine the engine drives via microtasks — covered in lesson 04 of this unit." },
  ]}
/>

<Recap lang="en">
"JavaScript has an event loop" is a useful lie that hides the real architecture. The engine — V8 — has no loop; it implements the ECMAScript language, owns the microtask (jobs) queue, and exposes one operation, PerformMicrotaskCheckpoint, to drain it. The loop lives in the host: Chromium's message loop in the browser, libuv in Node. One turn of that loop is a call into the engine and a return out of it — the host picks a macrotask, the engine runs it to completion (run-to-completion, no pre-emption), the engine drains its microtask queue including any microtasks scheduled during the drain, and only then returns, after which the browser host renders and picks the next task. The microtask drain happens inside that single call, which is why microtasks are the tail of the current task rather than a new one. The browser uses V8's automatic microtask policy; Node uses an explicit policy where libuv and Node decide the cadence — the queue is still the engine's, the timing is the host's. The next lesson makes the ordering between the two queues precise.
</Recap>
