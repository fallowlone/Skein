---
slug: "01-scope-chains"
lang: en
track: "js-engine"
unit: "05-closures-scope"
order: 1
title: "Scopes, contexts, and the scope chain"
summary: "Lexical scoping is identifier resolution along a chain of scopes. V8 describes scopes at compile time with ScopeInfo and materialises only the captured ones at runtime as heap Context objects linked by outer pointers — most loads resolve to a fixed (depth, slot)."
estMin: 13
status: ready
lessonType: topic
level: middle
concepts:
  - lexical scoping
  - scope chain
  - ScopeInfo
  - Context
  - context-allocated variable
  - slot index
  - dynamic scope
deepensInto:
  - "05-closures-scope/02-closure-memory"
spiral:
  - performance-cliffs
  - statefulness
prereqs:
  - "04-the-jit/06-osr"
sources:
  - https://v8.dev/blog/scope-info
  - https://v8.dev/docs
  - https://mathiasbynens.be/notes/shapes-ics
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
You read a variable named `total` inside a deeply nested callback. The engine has to find which `total` you mean — and it has to do it fast, on a hot path, millions of times. The naive answer is "search the surrounding functions at runtime." The real answer is that V8 already worked out the exact location at compile time, and at runtime it is usually a single indexed load from a heap object. Until you write `with` or sloppy `eval` — and then the whole scheme collapses.
</Hook>

<Crux>V8 plans scopes via ScopeInfo at compile time, so most reads become a fixed (depth, slot) load from a heap Context, not a search.</Crux>

<Explanation>

## Lexical scope: a chain decided by where you wrote the code

JavaScript is **lexically scoped**: the meaning of an identifier is fixed by the textual nesting of functions and blocks, not by who called whom at runtime. When you reference `total`, the language defines a search: look in the current scope, then its enclosing scope, then the next, up to the global scope. The first declaration found wins. That ordered sequence of enclosing scopes is the **scope chain**.

The word "search" is doing a lot of damage in most people's mental models. A linear runtime search of named variables — a dictionary lookup per access — would make every variable read in a closure painfully slow. V8 does not do that for the common case. It splits the work into two halves: a compile-time analysis that figures out *where* each variable lives, and a runtime structure that holds only the variables that actually need to survive.

## ScopeInfo: the compile-time blueprint

When V8's parser and bytecode compiler (Ignition) process a function, they build a **ScopeInfo** object for each scope. ScopeInfo is pure compile-time metadata attached to the function's `SharedFunctionInfo`. It records:

- which variables the scope declares (the names),
- which of them are **context-allocated** — must live on the heap because an inner function captures them — versus **stack/register-allocated** (purely local),
- the **slot index** each context-allocated variable occupies,
- the kind of scope (function, block, eval, module, with), and a link to the enclosing ScopeInfo.

Crucially, the compiler can usually resolve a variable reference to a fixed pair: **(context depth, slot index)**. "The variable `total` is two scopes out, at slot 3." That resolution is baked into the bytecode as a `LdaContextSlot`-style instruction. No name lookup happens at runtime — it is an array index into a known object at a known depth.

<NumbersCard
  title="What resolution costs, by kind"
  data-lesson-visual
  stats={[
    { label: "Local in register/stack", value: "0 loads (in a reg)" },
    { label: "Context slot, depth 0", value: "1 indexed load" },
    { label: "Each extra scope of depth", value: "+1 pointer hop" },
    { label: "Global / script-context var", value: "cell load (cached)" },
    { label: "Truly dynamic (with / sloppy eval)", value: "named runtime lookup" },
    { label: "ScopeInfo lives on", value: "SharedFunctionInfo" },
  ]}
/>

## Context: the runtime scope chain

At runtime, the scopes that must outlive their stack frame are materialised as **Context** objects. A Context is a small heap object — internally a fixed-size array (a `FixedArray` flavour) — that holds the context-allocated variables of one scope, plus a pointer to its **outer** Context. That chain of outer pointers *is* the runtime scope chain.

Three things follow, and they govern everything in this unit:

1. **Only captured variables go in a Context.** If the compiler can prove a variable is purely local — declared, used, and never referenced by any inner function — it stays in a CPU register or on the stack and is never heap-allocated. It vanishes when the frame returns. No closure can reach it because none was created over it.
2. **A Context is keyed by slot, not by name.** Reading `total` two levels out is: dereference outer, dereference outer again, load slot 3. Fixed work, no hashing.
3. **The global scope is special.** It is the global object (`globalThis`) plus a **script context** that holds top-level `let`/`const`. Top-level `var` and function declarations become properties on the global object; top-level `let`/`const` live in the script context as cells. Global reads therefore go through a cell or a property load, not a Context slot, and are cached by inline caches the same way object property reads are.

<FlowDiagram
  label="Three nested function scopes materialised as a runtime chain of Context objects, each holding only its captured variables and pointing to its outer Context, ending at the script and global scope."
  hue="var(--accent)"
  caption="The runtime scope chain. Each Context is a heap array of captured slots with an outer pointer. Reading an identifier is (depth, slot) — hop outer that many times, then index. Purely local variables never appear here; they live in registers."
  nodes={[
    { id: "inner", label: "Context: inner()", sub: "slot0: i", col: 0, row: 0 },
    { id: "mid", label: "Context: outer()", sub: "slot0: total", col: 0, row: 1 },
    { id: "script", label: "Script context", sub: "top-level let/const", col: 0, row: 2 },
    { id: "global", label: "Global object", sub: "var / functions", col: 0, row: 3 },
  ]}
  edges={[
    { from: "inner", to: "mid", label: "outer" },
    { from: "mid", to: "script", label: "outer" },
    { from: "script", to: "global", label: "outer" },
  ]}
/>

## Why the fast path exists — and what destroys it

The whole optimisation rests on the compiler *knowing the layout statically*. Two language features break that guarantee and force V8 onto a slow, name-based path:

- **`with (obj)`** injects an object whose properties are not known until runtime, so any identifier inside might resolve to a property of `obj`. The compiler cannot pre-compute a slot; it must emit a dynamic lookup that checks the object first. This poisons the scope for optimisation.
- **Sloppy-mode `eval`** can declare new variables in the calling scope (e.g. an `eval` of `var x = 1`). Because the compiler cannot know what names `eval` will introduce, it must keep the scope dynamic — variables can no longer be resolved to fixed slots, and the enclosing function often cannot be optimised well.

Strict mode neutralises the `eval` case: `eval` in strict mode gets its own scope and cannot leak declarations outward, so the surrounding scope stays statically analysable. This is one of the concrete performance reasons strict mode exists.

```js
function fast(rows) {
  let total = 0;                 // captured by the arrow -> context-allocated
  rows.forEach(r => total += r); // 'total' resolves to (depth 1, slot 0)
  return total;
}

function slow(rows, cfg) {
  with (cfg) {                   // every identifier below is now dynamic
    let total = 0;               // V8 cannot prove where 'limit' comes from
    rows.forEach(r => { if (total < limit) total += r; });
    return total;
  }
}
```

<Misconception id="mc-runtime-search">
  <p><strong>"Reading an outer variable scans the parent scopes by name at runtime."</strong> Only for `with` / sloppy `eval`. Normally V8 resolves each reference to a fixed (depth, slot) at compile time, so a closure read is a couple of pointer hops plus one indexed load — not a per-access name search.</p>
</Misconception>

<Quiz
  id="q-scope-context-vs-stack"
  lessonSlug="01-scope-chains"
  lang="en"
  question="A function declares a local `n`, increments it, returns it, and creates no inner functions. Where does V8 keep `n`?"
  choices={[
    { label: "In a register or stack slot — it is never captured, so no Context is allocated", correct: true },
    { label: "In a heap Context, because every `let` binding is context-allocated", misconception: "Only variables captured by an inner function are context-allocated. A purely local binding stays in a register/stack slot and is freed when the frame returns." },
    { label: "As a property on the global object", misconception: "Only top-level `var`/function declarations become global-object properties; a function-local binding does not." },
    { label: "In the script context", misconception: "The script context holds top-level `let`/`const`, not a variable local to some inner function." },
  ]}
/>

<Quiz
  id="q-scope-resolution"
  lessonSlug="01-scope-chains"
  lang="en"
  question="What does V8 typically resolve an outer-variable reference to, at compile time?"
  choices={[
    { label: "A fixed (context depth, slot index) pair baked into the bytecode", correct: true },
    { label: "A string key that is hashed against a scope dictionary on every read", misconception: "That is the slow dynamic path used only under `with`/sloppy `eval`. The normal path is a precomputed depth+slot, no hashing." },
    { label: "The variable's memory address, fixed for the program's lifetime", misconception: "Contexts are heap objects that can be reallocated (e.g. per loop iteration); the resolution is relative — depth and slot — not an absolute address." },
    { label: "Nothing — resolution is fully deferred to runtime in all cases", misconception: "Resolution is done at compile time via ScopeInfo for everything except genuinely dynamic scopes." },
  ]}
/>

<DragOrder
  id="do-scope-resolution"
  pieceSlug="01-scope-chains"
  lang="en"
  prompt="Order how V8 resolves a read of an outer variable on a hot path, from compile time to the runtime load."
  items={[
    "Parser builds a scope tree and a ScopeInfo per scope",
    "Compiler marks captured variables context-allocated and assigns each a slot index",
    "The reference is encoded as a fixed (context depth, slot index) in the bytecode",
    "At runtime, hop the outer pointer 'depth' times, then load that slot from the Context",
  ]}
/>

<Inset kind="why" lang="en">
Why not put every variable in a Context for simplicity? Because Context objects are heap allocations the GC must track, and a heap load is far costlier than a register. V8's whole strategy is to keep variables in registers/stack by default and pay for a Context only when capture forces it. The next two lessons are about exactly when that cost is incurred and what it retains.
</Inset>

</Explanation>

<KeyTakeaway>A scope chain is resolved lexically. V8 plans it at compile time with ScopeInfo — marking which variables are captured and assigning each a slot — so most reads become a fixed (depth, slot) load from a heap Context. Uncaptured variables never leave registers; `with` and sloppy `eval` force the slow named lookup and block optimisation.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="scope-chains-retrieval"
  lang="en"
  questions={[
    { q: "What is ScopeInfo and how does it differ from a Context?", a: "ScopeInfo is compile-time metadata attached to a function's SharedFunctionInfo: it lists the variables a scope declares, which are context-allocated vs stack/register-allocated, the slot index of each context-allocated variable, the scope kind, and a link to the enclosing scope. It exists once per function, shared across all invocations. A Context is the runtime counterpart: a per-invocation heap object (a FixedArray flavour) that actually holds the captured variables' values for one scope, with an outer pointer to the enclosing Context. ScopeInfo is the blueprint; the Context is the materialised instance. Most importantly, ScopeInfo lets V8 resolve a reference to a fixed (depth, slot) so no runtime name lookup is needed." },
    { q: "Which variables end up in a Context, and which stay in registers or on the stack?", a: "Only variables captured by an inner function (a nested function or arrow that references them) are context-allocated, because they must outlive the stack frame that created them. A variable that is declared, used, and never referenced by any inner function is proven local by the compiler and stays in a CPU register or stack slot — it is freed when the frame returns and no closure can reach it. This is why creating a closure that captures a variable is the event that forces heap allocation, and why a hot function with no captured variables pays nothing for scoping." },
    { q: "Why do `with` and sloppy-mode `eval` make scope resolution slow, and how does strict mode help?", a: "Both break the compiler's ability to know the scope layout statically. `with (obj)` injects an object whose properties are unknown until runtime, so any identifier inside might be a property of obj — V8 must emit a dynamic, name-based lookup that checks the object first instead of a fixed slot load. Sloppy `eval` can declare new variables in the calling scope, so the compiler cannot assign fixed slots and must keep the scope dynamic, often blocking optimisation of the whole enclosing function. Strict mode fixes the eval case: a strict-mode eval gets its own scope and cannot leak declarations outward, so the surrounding scope stays statically analysable and fast." },
  ]}
/>

<Recap lang="en">
JavaScript is lexically scoped: an identifier's meaning is fixed by textual nesting, and resolution walks an ordered chain of enclosing scopes up to global. V8 splits this into compile time and runtime. At compile time the parser builds a ScopeInfo per scope — recording which variables exist, which are context-allocated because an inner function captures them, and the slot index of each — so most references compile to a fixed (context depth, slot index). At runtime, captured scopes are materialised as Context objects: small heap arrays of slots linked by outer pointers, which form the actual scope chain. Reading an outer variable is a few pointer hops plus one indexed load, not a name search. Variables the compiler proves are local stay in registers or on the stack and are freed with the frame. The global scope is the global object plus a script context for top-level let/const. The fast path depends on the compiler knowing the layout statically, which is exactly what `with` and sloppy-mode `eval` destroy by forcing dynamic, name-based lookup — strict mode preserves it for eval. This sets up the next lesson: a closure is a function plus a pointer to the Context it was created in, and that pointer is what keeps captured variables alive.
</Recap>
