---
slug: "03-context-allocation"
lang: en
track: "js-engine"
unit: "05-closures-scope"
order: 3
title: "When V8 allocates a Context (and what it costs)"
summary: "V8 allocates a Context only when an inner function captures a variable; otherwise variables stay in registers, free. A let loop whose body captures the binding allocates a fresh Context per iteration — real cost in hot loops. How to minimise captures."
estMin: 14
status: ready
lessonType: topic
level: middle
concepts:
  - context allocation
  - per-iteration binding
  - capture
  - hot loop
  - var vs let capture
  - hoisting closures
deepensInto:
  - "05-closures-scope/04-call-site-polymorphism"
spiral:
  - performance-cliffs
  - statefulness
prereqs:
  - "02-closure-memory"
sources:
  - https://v8.dev/blog/scope-info
  - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/let
  - https://v8.dev/docs
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
You change a `for (var i …)` to `for (let i …)` because a linter told you to, and a microbenchmark that builds a million callbacks gets measurably slower. Nothing about your logic changed. What changed is that `let` gives each iteration its own binding — and when the body captures that binding, V8 must allocate a brand-new heap Context on every single turn of the loop. The semantics you wanted came with an allocation you didn't see.
</Hook>

<Crux>V8 allocates a Context only on capture; a captured per-iteration `let` forces one fresh Context per loop turn.</Crux>

<Explanation>

## The allocation rule: capture, nothing else

A Context is not allocated because you opened a function or a block. It is allocated only when the compiler determines that a variable in that scope is **captured** by an inner function — referenced by a nested function or arrow that may outlive the current frame. If no inner function closes over anything, the scope needs no Context at all: its variables live in registers or stack slots and cost nothing beyond the frame.

This is the single most important cost model in this unit. Most functions you write allocate **zero** Contexts, because they either define no inner functions or define inner functions that capture nothing. The cost appears precisely at capture.

```js
function noCapture(xs) {
  let sum = 0;
  for (let i = 0; i < xs.length; i++) sum += xs[i];
  return sum;            // no inner function -> no Context, all registers
}

function capture(xs) {
  let sum = 0;
  xs.forEach(x => sum += x); // the arrow captures 'sum' -> 'sum' context-allocated
  return sum;                // one Context for this scope
}
```

## The per-iteration trap: `let` in a loop + capture

`let` and `const` declared in a `for` loop header create a **fresh binding per iteration**. This is the feature that makes the classic "all my callbacks print the last value" bug go away. But the implementation has a cost when the body captures that binding.

```js
const fns = [];
for (let i = 0; i < n; i++) {
  fns.push(() => i);   // captures THIS iteration's 'i'
}
// Each callback returns its own i: fns[3]() === 3. Correct!
// But: a NEW Context is allocated for each iteration to hold that iteration's i.
```

For the closures to each see a different `i`, each iteration's `i` must live somewhere distinct — so V8 allocates a fresh Context per iteration and copies the binding in. With `n` large and the body capturing, that is `n` heap allocations and `n` objects for the GC to trace. Contrast `var`, which has one binding for the whole loop:

```js
const fns = [];
for (var i = 0; i < n; i++) {
  fns.push(() => i);   // all capture the SAME 'i'
}
// Every callback returns the final i (=== n). One binding, one Context.
```

`var` allocates at most one Context here (still one, because the binding is captured) — but it does not multiply by `n`. The widely-repeated advice "always use `let`" is correct for correctness, but in a genuinely hot loop that creates closures, the per-iteration Context is a real, measurable cost.

<NumbersCard
  title="Context allocations by pattern (loop of n)"
  data-lesson-visual
  stats={[
    { label: "Loop, no inner function", value: "0 Contexts" },
    { label: "Loop body captures, `var i`", value: "1 Context total" },
    { label: "Loop body captures, `let i`", value: "n Contexts (1/iter)" },
    { label: "Inner fn captures nothing", value: "0 Contexts" },
    { label: "Forces a Context: eval / with", value: "always" },
    { label: "GC objects to trace from n closures", value: "scales with n" },
  ]}
/>

<FlowDiagram
  label="A loop that captures a let binding allocates a separate Context per iteration, each holding that iteration's value, whereas the equivalent var loop shares a single Context across all iterations."
  hue="var(--accent)"
  caption="Per-iteration vs shared. With captured `let`, iterations 0..2 each get their own Context (correct distinct values, n allocations). With `var`, all iterations share one Context (one allocation, one shared value)."
  nodes={[
    { id: "let", label: "let i (captured)", sub: "per-iteration", col: 0, row: 0 },
    { id: "c0", label: "Context i=0", sub: "iter 0", col: 1, row: 0 },
    { id: "c1", label: "Context i=1", sub: "iter 1", col: 2, row: 0 },
    { id: "c2", label: "Context i=2", sub: "iter 2", col: 3, row: 0 },
    { id: "var", label: "var i (captured)", sub: "shared", col: 0, row: 1 },
    { id: "shared", label: "One Context", sub: "all iters", col: 1, row: 1 },
  ]}
  edges={[
    { from: "let", to: "c0", label: "alloc" },
    { from: "let", to: "c1", label: "alloc" },
    { from: "let", to: "c2", label: "alloc" },
    { from: "var", to: "shared", label: "alloc once" },
  ]}
/>

## What forces a Context, and how to minimise it

Things that force context allocation: any closure over a variable, a `with` block, a direct `eval` (it can read/declare locals), and in some cases observing `arguments` in ways that defeat analysis. The levers to reduce it on hot paths:

- **Hoist the closure out of the loop.** If the callback does not actually need each iteration's binding, define it once outside the loop. No per-iteration Context.
- **Pass data as arguments instead of capturing.** A function that receives `x` as a parameter does not capture it; parameters are cheaper than a captured Context slot and keep the call shape predictable (next lesson).
- **Use an index, not a captured binding, when you only need the value at call time.** Sometimes a plain `for` with `var` plus an explicit copy is the right tool in a million-iteration hot loop — measure it.
- **Keep big or long-lived data out of captured scopes** (the retention lesson) — a separate concern from allocation count but the same discipline.

Applied together, these four levers reduce both the number of Context objects the GC must trace and the number of heap allocations per hot iteration. Without hoisting, even a trivial `() => i` inside a tight loop silently multiplies allocations by `n` — the kind of cost that only shows up in an allocation profile, not in a code review.

```js
// Hot path: avoid n per-iteration Contexts when capture isn't needed.
function attach(handlers, fn) {            // fn defined ONCE, captures nothing per-iter
  for (let i = 0; i < handlers.length; i++) handlers[i].on(fn);
}
```

<Misconception id="mc-closures-free">
  <p><strong>"Closures are free — they're just functions."</strong> Creating a closure that captures a variable forces a heap Context the GC must trace. In a hot loop, a captured per-iteration `let` allocates one Context <em>per iteration</em>. Closures are cheap, not free; on hot paths, count your captures.</p>
</Misconception>

<Quiz
  id="q-ctx-per-iter"
  lessonSlug="03-context-allocation"
  lang="en"
  question="`for (let i = 0; i < n; i++) cbs.push(() => i);` — how many Contexts does V8 allocate for the loop, and why?"
  choices={[
    { label: "n — one per iteration, because each callback captures that iteration's fresh `let` binding", correct: true },
    { label: "1 — V8 reuses a single Context for the whole loop", misconception: "That is the `var` behaviour. With captured `let`, each iteration needs its own binding, so each gets its own Context." },
    { label: "0 — closures don't allocate Contexts", misconception: "A closure that captures a variable forces a Context; here it forces one per iteration." },
    { label: "2 — one for the loop and one for the function", misconception: "The count scales with iterations when the per-iteration binding is captured, not a fixed 2." },
  ]}
/>

<Quiz
  id="q-ctx-minimise"
  lessonSlug="03-context-allocation"
  lang="en"
  question="A hot loop pushes a callback that does NOT need each iteration's value. What is the cheapest fix?"
  choices={[
    { label: "Define the callback once outside the loop so nothing is captured per iteration", correct: true },
    { label: "Switch `let` to `const`", misconception: "`const` is also per-iteration when captured; the binding kind isn't the issue, the capture is." },
    { label: "Wrap the loop body in another arrow", misconception: "Adding a closure adds captures and Contexts; it makes allocation worse, not better." },
    { label: "Add `try/catch` around the loop", misconception: "Exception handling is unrelated to context allocation and can itself inhibit optimisation." },
  ]}
/>

<DragOrder
  id="do-alloc-cost"
  pieceSlug="03-context-allocation"
  lang="en"
  prompt="Order these loops by Context allocations for n iterations, from zero allocations to the most."
  items={[
    "Loop summing into a local, no inner function (0 Contexts)",
    "Loop capturing a `var` binding in a callback (1 Context)",
    "Loop capturing a per-iteration `let` binding (n Contexts)",
    "Loop with a `with` block plus per-iteration capture (n Contexts, all unoptimisable)",
  ]}
/>

<Inset kind="edgecase" lang="en">
V8 is not naive about this: if a loop body's closure provably does not escape and the binding is not actually observed per-iteration, the optimiser can sometimes avoid materialising distinct Contexts. But you cannot rely on it — escape analysis is fragile and bails on many real shapes. Treat per-iteration capture as "n allocations until proven otherwise", and verify hot paths with `--trace-gc` or an allocation profile rather than trusting the optimiser to save you.
</Inset>

</Explanation>

<KeyTakeaway>A Context is allocated only when an inner function captures a variable — most functions allocate zero. The trap is a captured per-iteration `let` in a hot loop: that is one fresh Context per iteration. Hoist closures out of loops, pass data as arguments, and capture only what you must.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="context-allocation-retrieval"
  lang="en"
  questions={[
    { q: "What exactly triggers a Context allocation, and what does not?", a: "A Context is allocated only when the compiler sees that a variable in a scope is captured by an inner function — a nested function or arrow that references it and may outlive the frame. Opening a function or a block does not by itself allocate a Context; a scope whose inner functions capture nothing (or that has no inner functions) keeps its variables in registers or stack slots at no heap cost. Additionally, a `with` block and a direct sloppy `eval` force a Context because they make the scope dynamic. The mental model: zero Contexts by default, one per captured scope, and a multiplier when capture happens per loop iteration." },
    { q: "Why can `for (let …)` be slower than `for (var …)` when the body creates closures, and is that a reason to avoid let?", a: "`let`/`const` in a loop create a fresh binding per iteration so each captured closure sees its own value — this is the correct, desirable semantics that fixes the classic last-value bug. The cost is that, to give each iteration a distinct binding, V8 allocates a fresh Context per iteration when the body captures the binding: n iterations means n Context allocations and n objects for the GC to trace. `var` has a single binding and at most one Context. It is not a reason to abandon let in general — correctness wins — but in a genuinely hot loop that builds many closures, the per-iteration Context is a real, measurable cost worth eliminating by hoisting the closure or passing values as arguments." },
    { q: "How do you minimise context allocation on a hot path?", a: "Capture less. If a callback does not need each iteration's binding, define it once outside the loop so nothing is captured per iteration. Prefer passing data as function arguments over capturing it from an enclosing scope — parameters do not create Context slots and keep call shapes predictable. When you only need a value at call time, an index plus an explicit read can beat a captured per-iteration binding in a million-iteration loop. Avoid `with` and sloppy `eval`, which force a Context and also block optimisation. Verify with an allocation profile or --trace-gc rather than assuming escape analysis will remove the allocations for you." },
  ]}
/>

<Recap lang="en">
The cost model of scoping in V8 reduces to one rule: a Context is allocated only when an inner function captures a variable. Functions with no inner functions, or whose inner functions capture nothing, allocate zero Contexts and keep everything in registers and stack slots. The expensive and easy-to-miss case is a loop whose body captures a per-iteration `let`/`const` binding: because each iteration needs its own binding to preserve the correct distinct-value semantics, V8 allocates a fresh Context every iteration — n iterations, n heap objects, n things for the GC to trace — whereas the equivalent captured `var` shares a single binding and a single Context. `with` and direct sloppy `eval` also force a Context and additionally defeat optimisation. To stay cheap on hot paths: define closures once outside loops when per-iteration state is not needed, pass data as arguments rather than capturing it, and reserve per-iteration capture for where the semantics genuinely require it. V8's escape analysis can sometimes elide these allocations but is unreliable, so measure with an allocation profile. Minimising captures also sets up the next lesson — fewer, more uniform closures keep call sites monomorphic. Now when you profile a hot loop and see unexpectedly high allocation pressure, count the closures created per iteration before reaching for anything else — a captured `let` is often the whole story.
</Recap>
