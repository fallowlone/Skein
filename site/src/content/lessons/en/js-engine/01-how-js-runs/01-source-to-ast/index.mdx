---
slug: "01-source-to-ast"
lang: en
track: "js-engine"
unit: "01-how-js-runs"
order: 1
title: "From source text to AST"
summary: "Before a line of code runs, V8 scans UTF-16 source into tokens and a recursive-descent parser builds an Abstract Syntax Tree. The AST is a transient intermediate form — fed to the bytecode generator, then discarded. Parse cost is linear in source bytes"
estMin: 12
status: ready
lessonType: topic
level: junior
concepts:
  - scanner
  - tokens
  - parser
  - recursive descent
  - Abstract Syntax Tree
  - parse cost
deepensInto:
  - "02-lazy-parsing"
spiral:
  - performance-cliffs
  - startup-latency
prereqs:
  - "00-start-here/02-the-engine-landscape"
sources:
  - https://v8.dev/blog/scanner
  - https://v8.dev/blog/preparser
  - https://web.dev/articles/optimizing-content-efficiency-javascript-startup-optimization
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
You ship a 4 MB bundle and the page sits blank for 600ms before a single character renders. The network finished a third of a second ago. What happened in between? V8 had to read every byte of that text, chop it into tokens, and build a tree out of it — all before your first statement could even begin to execute. That blank gap is the parser, and its cost is proportional to how much source you handed it.
</Hook>

<Crux>The scanner turns source into tokens; the parser builds an AST — a transient tree the bytecode generator consumes once, then drops.</Crux>

<Explanation>

## Two stages before anything runs

When the runtime hands V8 a string of JavaScript, two things happen before the first instruction executes, and they are distinct phases:

1. **Scanning (lexing).** The scanner reads the raw source — V8 stores it as **UTF-16** internally — one character at a time and groups characters into **tokens**: the indivisible words of the language. `const x = 42;` becomes the token stream `Keyword(const)`, `Identifier(x)`, `Punctuator(=)`, `Number(42)`, `Punctuator(;)`. Whitespace and comments are consumed and dropped. The scanner is the hot inner loop of parsing — V8 has rewritten it repeatedly for UTF-16 throughput because it touches every single byte.

2. **Parsing.** The parser pulls tokens from the scanner and assembles them into an **Abstract Syntax Tree (AST)** — a structured, nested representation of the program's grammar. `const x = 42;` becomes a `VariableDeclaration` node holding a `VariableDeclarator` with an `Identifier` (`x`) and a `NumericLiteral` (`42`). The flat token stream becomes a tree that captures *structure*: which expression is nested in which statement, which block scopes which variable.

<FlowDiagram
  label="The front-end pipeline: raw source text is scanned into a token stream, the parser assembles tokens into an Abstract Syntax Tree, the bytecode generator walks the tree to emit bytecode, and the tree is then discarded."
  hue="var(--accent)"
  perRow={4}
  caption="The AST is an intermediate, not an output. It exists only long enough to generate bytecode, then it is garbage-collected. Bytecode — not the AST — is what runs."
  nodes={[
    { id: "src", label: "Source", sub: "UTF-16 text", col: 0, row: 0 },
    { id: "tok", label: "Tokens", sub: "scanner output", col: 1, row: 0 },
    { id: "ast", label: "AST", sub: "parser output", col: 2, row: 0 },
    { id: "bc", label: "Bytecode", sub: "generator output", col: 3, row: 0 },
  ]}
  edges={[
    { from: "src", to: "tok", label: "scan" },
    { from: "tok", to: "ast", label: "parse" },
    { from: "ast", to: "bc", label: "generate" },
  ]}
/>

## How the parser builds the tree: recursive descent + Pratt

V8's parser is a hand-written **recursive-descent** parser: one function per grammar rule, each calling the others as the grammar nests. `ParseStatement` may call `ParseExpression`, which may call `ParseBlock`, which calls `ParseStatement` again — the call stack mirrors the nesting of your code. This is why a pathologically deeply-nested expression (thousands of nested parentheses) can throw a `RangeError: Maximum call stack size exceeded` *at parse time*, before execution.

Statements are straightforward to parse top-down, but expressions have **precedence**: `a + b * c` must bind `b * c` first. A naive recursive-descent parser handles this with one function per precedence level, which is verbose. V8 instead uses **operator-precedence (Pratt) parsing** for expressions: a single loop that consumes operators while their binding power is high enough, climbing precedence levels on demand. The result is the same correct tree, built with far less code and fewer function calls.

```js
// a + b * c parses to this tree shape (the * binds tighter than +):
//        (+)
//       /   \
//      a    (*)
//          /   \
//         b     c
```

## The AST is transient — it is NOT what runs

This is the single most important correction to the naive model. People imagine the engine "walking the AST" to execute the program, like a tree-walking interpreter from a compilers course. **V8 does not do this.** The AST's only consumer is the **bytecode generator**: V8 walks the tree once, emits Ignition bytecode for it, and then the tree becomes unreachable and is reclaimed by the garbage collector. From that point on, *the bytecode runs* — the AST is gone. (The next lessons in this unit cover bytecode and the interpreter loop.)

So the AST is an **intermediate representation**: a convenient structured form that exists only to be lowered into something the interpreter can execute efficiently. Keeping it around would waste memory; tree-walking it would be far slower than running flat bytecode.

<Misconception id="mc-ast-runs">
  <p><strong>"The engine executes the AST by walking the tree."</strong> No — the AST is consumed once by the bytecode generator and then discarded. What actually executes is Ignition bytecode. The AST is a transient parse product, not the runtime representation of your program.</p>
</Misconception>

## Parse cost is linear in source size

Scanning touches every byte; parsing visits roughly every token once. So parse time grows **linearly with source size** — double the bytes, roughly double the parse cost. This is why bundle size has a direct, mechanical effect on startup latency that is separate from download time: even a fully-cached bundle still has to be re-scanned and re-parsed on a cold start.

<NumbersCard
  title="Where the parser's cost shows up"
  data-lesson-visual
  stats={[
    { label: "Scanning + parsing as share of JS startup", value: "up to ~15-20%" },
    { label: "Parse cost vs source bytes", value: "roughly linear" },
    { label: "Internal source encoding in V8", value: "UTF-16" },
    { label: "Flag to dump the AST in d8", value: "--print-ast" },
    { label: "Fast C++ JSON path", value: "JSON.parse" },
    { label: "AST lifetime after bytecode emit", value: "discarded (GC'd)" },
  ]}
/>

You can see the tree V8 actually builds: run `d8 --print-ast script.js` and it prints the AST node-by-node. It is the clearest way to confirm that, say, an arrow function and a `function` expression produce different node types, or that an `await` desugars the way the spec says.

## Failure mode: a giant data literal parsed eagerly

Here is the production trap. A team inlines a large dataset directly into a JS module as an object or array literal — a multi-megabyte "JSON-in-JS":

```js
// data.js — 3 MB of this, inlined into the bundle
export const LOOKUP = { "0001": { name: "...", coords: [] }, /* 50k more */ };
```

Because it is JavaScript source, V8 must **fully scan and parse all 3 MB as an expression** on the cold path — building AST nodes for every key, value, and nested literal — before the module finishes evaluating. That is pure linear parse tax on the critical path, and it blocks first paint.

The fix is to move the data out of JS and into a real `.json` file, loaded as a string and handed to `JSON.parse`:

```js
const LOOKUP = JSON.parse(await fetch("/data.json").then(r => r.text()));
```

`JSON.parse` has a dedicated, hand-tuned **C++ fast path** that is dramatically faster than the general JavaScript parser building AST nodes — it does not produce an AST, does not allocate scope objects, and parses a far simpler grammar. The same bytes cost a fraction of the time. The rule of thumb: **data should travel as data, not as code.** This is exactly what bundlers like webpack and esbuild do when they extract large constants, and what V8's own team recommends for big payloads.

<Quiz
  id="q-ast-runs"
  lessonSlug="01-source-to-ast"
  lang="en"
  question="After V8 builds the AST for your function, what happens to that tree?"
  choices={[
    { label: "The interpreter walks the AST node-by-node to run the program", misconception: "V8 is not a tree-walking interpreter. The AST is never executed directly; bytecode is." },
    { label: "The bytecode generator consumes it once, then the AST is discarded and garbage-collected", correct: true },
    { label: "It is kept in memory for the lifetime of the program for fast re-execution", misconception: "Keeping the AST would waste memory. Once bytecode is emitted, the tree is unreachable and reclaimed." },
    { label: "It is compiled directly to machine code, skipping bytecode", misconception: "The AST becomes Ignition bytecode first; machine code only comes later, for hot functions, via the JIT." },
  ]}
/>

<Quiz
  id="q-json-vs-js"
  lessonSlug="01-source-to-ast"
  lang="en"
  question="You have a 3 MB lookup table slowing cold start. Why is moving it to a .json file parsed by JSON.parse faster than inlining it as a JS object literal?"
  choices={[
    { label: "JSON.parse runs on a background thread, so it is free", misconception: "JSON.parse is synchronous on the main thread; the win is not threading, it's a simpler, faster parser." },
    { label: "JSON.parse uses a dedicated C++ fast path for a simpler grammar and builds no AST or scopes", correct: true },
    { label: "JSON files are not parsed at all, they are memory-mapped", misconception: "JSON.parse genuinely parses the text into objects; it is just far cheaper than the general JS parser." },
    { label: "JSON is smaller on disk, so there is less to download", misconception: "The bytes are similar; the win is parse cost, not transfer size — it holds even when both are cached." },
  ]}
/>

<DragOrder
  id="do-frontend-pipeline"
  pieceSlug="01-source-to-ast"
  lang="en"
  prompt="Order the front-end stages a fresh script passes through, from raw text to the form that actually executes."
  items={[
    "The scanner reads UTF-16 source and emits a token stream",
    "The recursive-descent parser assembles tokens into an AST",
    "The bytecode generator walks the AST and emits Ignition bytecode",
    "The AST becomes unreachable and is garbage-collected; bytecode runs",
  ]}
/>

<Inset kind="why" lang="en">
Why UTF-16 internally? JavaScript strings are defined by the spec as sequences of UTF-16 code units (which is why `"smiley".length` counts surrogate pairs as 2). V8 keeps source and strings in that encoding so string operations match spec semantics without re-encoding, and the scanner is tuned to walk two-byte units fast. Source that is pure ASCII can use a one-byte representation as an optimisation, but the logical model is UTF-16.
</Inset>

</Explanation>

<KeyTakeaway>Scanning turns UTF-16 source into tokens; a recursive-descent parser (Pratt for expressions) turns tokens into an AST. The AST is transient — consumed once by the bytecode generator, then discarded. Parse cost is linear in source bytes, so large inlined data literals tax startup; move big payloads to `.json` parsed by the fast C++ `JSON.parse`.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="source-to-ast-retrieval"
  lang="en"
  questions={[
    { q: "Walk the front-end pipeline from source text to the representation that actually executes, naming each artifact.", a: "The scanner reads UTF-16 source and emits a token stream (keywords, identifiers, punctuators, literals; whitespace and comments dropped). The recursive-descent parser — using Pratt/operator-precedence parsing for expressions so a + b * c binds correctly — assembles tokens into an Abstract Syntax Tree. The bytecode generator walks the AST once and emits Ignition bytecode. The AST then becomes unreachable and is garbage-collected. The bytecode, not the AST, is what executes." },
    { q: "Why is 'the engine walks the AST to run the program' wrong, and what runs instead?", a: "V8 is not a tree-walking interpreter. The AST has exactly one consumer — the bytecode generator — which lowers it to compact Ignition bytecode and then drops the tree. Keeping or walking the AST would waste memory and be far slower than executing flat register-based bytecode. So the AST is a transient intermediate representation; Ignition bytecode is the runtime form of your program." },
    { q: "A 3 MB lookup table inlined as a JS object literal is slowing cold start. Explain the mechanism and the fix.", a: "Because it is JavaScript source, V8 must fully scan and recursive-descent parse all 3 MB into AST nodes on the cold path before the module finishes evaluating — parse cost is linear in bytes, so it directly delays first paint, even when the bundle is cached. The fix is to move the data to a .json file loaded as a string and passed to JSON.parse, which has a dedicated C++ fast path: a simpler grammar, no AST, no scope allocation, dramatically faster for the same bytes. Data should travel as data, not as code." },
  ]}
/>

<Recap lang="en">
Before any JavaScript runs, V8 puts the source through a front-end pipeline. The scanner reads the UTF-16 source byte by byte and groups it into tokens — the words of the language — discarding whitespace and comments. A hand-written recursive-descent parser then assembles those tokens into an Abstract Syntax Tree, using Pratt/operator-precedence parsing for expressions so precedence (`a + b * c`) comes out right; deeply nested code can even overflow the parser's call stack. Crucially, the AST is a transient intermediate representation: its only consumer is the bytecode generator, which walks it once to emit Ignition bytecode, after which the tree is discarded by the GC. The bytecode — not the AST — is what executes. Because scanning and parsing visit every byte and token, parse cost is roughly linear in source size, giving bundle size a direct, mechanical effect on cold-start latency separate from download time. The classic failure mode is a multi-megabyte data literal inlined as JS, which V8 must fully parse into AST nodes on the critical path; the fix is to ship it as a `.json` file and use the C++ fast path in `JSON.parse`. Use `d8 --print-ast` to see the exact tree V8 builds.
</Recap>
