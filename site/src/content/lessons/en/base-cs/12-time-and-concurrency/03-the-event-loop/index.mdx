---
concepts:
- event-loop
- callback-queue
- stack-empty-check
- run-to-completion
- callback-ordering
estMin: 24
lang: en
lessonType: coding
mathPrereqs: []
order: 3
prereqs:
- 02-blocking-vs-non-blocking
- 02-the-call-stack
- 01-what-flow-means
slug: 03-the-event-loop
sources:
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Execution_model
- https://html.spec.whatwg.org/multipage/webappapis.html#event-loops
- https://javascript.info/event-loop
status: ready
summary: The event loop is a queue of pending callbacks plus a loop that, whenever
  the call stack is empty, takes the next callback off the queue and runs it. It is
  why synchronous code finishes before any callback runs.
title: The event loop
track: base-cs
unit: 12-time-and-concurrency
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
The last lesson left one thing unexplained. A non-blocking call hands over a **callback**
and returns. Milliseconds later the device finishes — and *something* calls that callback.
The lesson called it "the result-delivery mechanism" and moved on. This lesson is that
mechanism, named and traced.

It is called the **event loop**, and it is simpler than its reputation suggests. It is two
things: a **queue** that holds callbacks waiting to run, and a **loop** that takes them off
the queue one at a time. Once you have traced it, a famous puzzle dissolves: why a program
that prints `1`, then schedules a callback that prints `2`, then prints `3`, produces the
output `1`, `3`, `2` — not `1`, `2`, `3`. The `2` is late on purpose, and the event loop
is exactly why.
</Hook>

<Goal lang="en">
After this lesson you can define the **event loop** as a callback queue plus a loop that
drains it, state the rule "run a callback only when the call stack is empty", explain why
synchronous code always finishes before any callback runs, and trace a small program to
predict its output order.
</Goal>

<Idea lang="en">
Two pieces from earlier units are already in place. From Unit 08: the **call stack** — the
stack of frames for the currently running function calls; the program is "running
synchronous code" whenever the stack is non-empty. From this unit's lesson 02: a
non-blocking call hands over a **callback** to be run when its slow work finishes.

The event loop adds one new piece and one rule.

**The new piece — the callback queue.** A **queue** is a waiting line: items are added at
the back and removed from the front, in first-in-first-out order (the first callback added
is the first one run). The **callback queue** holds callbacks that are *ready to run* —
their slow device has already finished — but have not run yet. When a device finishes, its
callback is not run immediately; it is placed at the back of this queue.

**The rule — run a callback only when the stack is empty.** The **event loop** is a loop
that repeats forever: *check whether the call stack is empty; if it is, take the front
callback off the queue and run it; if the stack is not empty, do nothing and check again.*

Two consequences follow directly. First, **synchronous code always finishes first**: while
any synchronous code is running, the stack is non-empty, so the loop will not start a
callback. Only after the stack drains completely can a queued callback begin. Second, a
callback **runs to completion**: once started, it gets its own stack frames and runs fully
before the loop takes the next callback — callbacks never interrupt each other.

The program below uses `setTimeout(fn, 0)` — a non-blocking call that means "put `fn` on
the callback queue." Even with a `0` delay, `fn` cannot run until the synchronous code
finishes and the stack is empty. That is what produces the `1, 3, 2` order.
</Idea>

<Code lang="en">

<AnnotatedCode
  code={`console.log(1);                  // synchronous — runs now

setTimeout(() => {
  console.log(2);                // the callback's body — runs later
}, 0);

console.log(3);                  // synchronous — runs now`}
  annotations={[
    { line: 1, text: "Synchronous call. The stack is non-empty; this runs immediately. Output so far: 1" },
    { line: 3, text: "setTimeout is a non-blocking call: it hands the callback over and returns at once. It does NOT run the callback." },
    { line: 4, text: "This line is inside the callback. It will run only when the event loop later picks the callback up." },
    { line: 5, text: "The 0 means 'queue the callback as soon as possible' — but 'soon' still means after the stack empties." },
    { line: 7, text: "Synchronous call. Runs right after setTimeout returns — before the callback. Output so far: 1, 3" },
  ]}
  caption="setTimeout(fn, 0) queues fn. The synchronous lines 1 and 3 run first; the callback's console.log(2) runs only after the stack is empty. Output: 1, 3, 2."
/>
</Code>

<Trace lang="en">
Step through the program. The cells are the **call stack** (bottom-to-top, oldest at
left). Watch the synchronous lines run with the stack non-empty, the callback get queued,
and the event loop run it only once the stack is empty.

<AlgoTrace
  id="trace-event-loop"
  lang="en"
  code={[
    "console.log(1);",
    "",
    "setTimeout(() => {",
    "  console.log(2);",
    "}, 0);",
    "",
    "console.log(3);"
  ]}
  frames={[
    {
      caption: "Program starts. The script's frame is on the stack. Stack is non-empty. Queue: empty. Output: (none).",
      line: 1,
      cells: [
        { value: "script", mark: "active" }
      ]
    },
    {
      caption: "Line 1: console.log(1) runs synchronously. Output: 1. The stack is still non-empty, so the event loop runs nothing.",
      line: 1,
      cells: [
        { value: "script", mark: "active" },
        { value: "log(1)", mark: "done" }
      ]
    },
    {
      caption: "Line 3: setTimeout(fn, 0) is a non-blocking call. It hands the callback over and returns at once. The callback is now waiting — it will be queued, not run.",
      line: 3,
      cells: [
        { value: "script", mark: "active" },
        { value: "setTimeout", mark: "done" }
      ]
    },
    {
      caption: "The 0 ms 'timer' is already up, so the callback is placed at the back of the callback QUEUE. Queue now holds: [fn]. The stack is still non-empty — the loop cannot run fn yet.",
      line: 3,
      cells: [
        { value: "script", mark: "active" }
      ]
    },
    {
      caption: "Line 7: console.log(3) runs synchronously — it was next in the script, ahead of any callback. Output: 1, 3. Queue still holds: [fn].",
      line: 7,
      cells: [
        { value: "script", mark: "active" },
        { value: "log(3)", mark: "done" }
      ]
    },
    {
      caption: "The script finishes. Its frame is popped. The call stack is now EMPTY. Queue holds: [fn].",
      line: 7,
      cells: []
    },
    {
      caption: "Event loop: stack is empty, so take the front callback off the queue and run it. fn's frame is pushed. Queue is now empty.",
      line: 4,
      cells: [
        { value: "fn", mark: "active" }
      ]
    },
    {
      caption: "Line 4 (inside fn): console.log(2) runs. Output: 1, 3, 2. The callback ran last — exactly because it had to wait for an empty stack.",
      line: 4,
      cells: [
        { value: "fn", mark: "active" },
        { value: "log(2)", mark: "done" }
      ]
    },
    {
      caption: "fn finishes and runs to completion. Its frame is popped. Stack empty, queue empty. The event loop keeps looping, finding nothing to do. Final output: 1, 3, 2.",
      line: 4,
      cells: []
    }
  ]}
/>
</Trace>

<Inset kind="why" lang="en">
**Why does `setTimeout(fn, 0)` not run `fn` right away — the delay is zero?** The `0` sets
the *timer*, not the *run*. It means "the callback is allowed to be queued immediately."
But being queued is not being run. The event loop's one rule still applies: a callback runs
only when the call stack is empty. At the moment `setTimeout` is called, the script's frame
is still on the stack and `console.log(3)` has not run yet. So `fn` waits in the queue until
the script finishes. `0` is the minimum *queueing* delay; it can never shortcut the
empty-stack rule.
</Inset>

<Inset kind="mistake" lang="en">
A common mistake is to read `setTimeout(fn, 0)` as "run `fn` now" or "run `fn` in 0
milliseconds." It means neither. It means "put `fn` on the callback queue; it becomes
eligible to run only after the call stack empties." If the synchronous code after it takes
50 ms, the `0`-delay callback waits 50 ms. The delay number is a *minimum* wait before
queueing, never a guarantee of when the callback runs.
</Inset>

<PracticeSet
  id="the-event-loop-practice"
  lessonSlug="03-the-event-loop"
  lang="en"
  problems={[
    {
      prompt: "A program runs: console.log(1); setTimeout(() => console.log(2), 0); console.log(3). What is the first number printed?",
      answer: 1,
      hint: "console.log(1) is synchronous and first in the script. Synchronous code runs before any callback.",
    },
    {
      prompt: "Same program. What is the last number printed?",
      answer: 2,
      hint: "console.log(2) is inside the callback. The callback runs only after the stack empties — after 1 and 3. So 2 is last.",
    },
    {
      prompt: "The event loop runs a queued callback only when the call stack has how many frames on it?",
      answer: 0,
      hint: "The rule: run a callback only when the call stack is empty — that is 0 frames.",
    },
    {
      prompt: "Three callbacks A, B, C are queued in that order, and the stack is empty. The event loop runs them. Which runs first — type 1 for A, 2 for B, 3 for C?",
      answer: 1,
      hint: "The callback queue is first-in-first-out. A was added first, so A runs first.",
    },
    {
      prompt: "A program: console.log(10); setTimeout(() => console.log(20), 0); setTimeout(() => console.log(30), 0). The synchronous part prints how many numbers before any callback runs?",
      answer: 1,
      hint: "Only console.log(10) is synchronous. Both setTimeout callbacks are queued. So 1 number prints before the callbacks run.",
    },
  ]}
/>

<Check lang="en">
  <Quiz
    id="the-event-loop-check"
    pieceSlug="03-the-event-loop"
    lang="en"
    question="What is the event loop, and what rule governs when a callback runs?"
    choices={[
      {
        label: "It is a callback queue plus a loop that, whenever the call stack is empty, takes the front callback off the queue and runs it — so all synchronous code finishes before any callback runs",
        correct: true,
      },
      {
        label: "It is a mechanism that runs each callback the instant its device finishes, interrupting whatever synchronous code is currently running",
        misconception: "The event loop never interrupts running code. A ready callback goes into the queue and waits. It runs only when the call stack is empty — that is, only after the current synchronous code has fully finished. Callbacks do not preempt synchronous code.",
      },
      {
        label: "It is a faster CPU mode that runs callbacks and synchronous code at the same time",
        misconception: "The event loop is not a hardware mode and runs nothing in parallel. It is a plain loop that picks one callback at a time from a queue, and only when the stack is empty. One thing runs at a time — that is the point of the next lesson.",
      },
    ]}
  />
</Check>

<Recap lang="en">
The **event loop** is the mechanism that runs callbacks. It is two parts: a **callback
queue** — a first-in-first-out waiting line holding callbacks whose slow work has finished
— and a **loop** that repeats forever, checking one rule: *if the call stack is empty, take
the front callback off the queue and run it; otherwise wait.* Two facts fall out of that
rule. **Synchronous code always finishes first**, because while it runs the stack is
non-empty and the loop will not start a callback. And each callback **runs to completion**
without interruption, because the loop does not take the next one until the current
callback's frames have all popped. This is why `console.log(1); setTimeout(cb, 0);
console.log(3)` prints `1, 3, 2`: the `0`-delay callback is only *queued* fast — it still
waits for the stack to empty, which happens after `1` and `3` have printed.
</Recap>
