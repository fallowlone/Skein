---
concepts:
- async-motivation
- blocking-vs-non-blocking
- event-loop
- concurrency-vs-parallelism
- race-condition
- synchronization
estMin: 14
lang: en
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Execution_model
- https://nodejs.org/learn/asynchronous-work/overview-of-blocking-vs-non-blocking
- https://www.freecodecamp.org/news/concurrency-vs-parallelism-whats-the-difference-and-why-should-you-care/
status: ready
summary: "Free-recall prompts across the whole unit — why async exists, blocking vs non-blocking, the event loop's rule, concurrency vs parallelism, and how shared state creates race conditions that locks fix."
title: 'Time and concurrency: free-recall review'
track: base-cs
unit: 12-time-and-concurrency
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory before you open the model answer — the effort of recalling is what makes the idea stick. These cover the whole unit, ending where the lessons pointed: what happens once concurrent tasks share data.
</Hook>

<Goal lang="en">
Reconstruct the unit's spine from memory — why async exists, the blocking/non-blocking split, the event loop's one rule, the concurrency-vs-parallelism distinction, and why shared state forces synchronization — without looking back at the lessons.
</Goal>

<RetrievalDrawer
  client:load
  id="12-time-and-concurrency-short-retrieval"
  lang="en"
  questions={[
    { q: "Why does async exist? Use the CPU-vs-device speed gap in your answer.", a: "A CPU runs roughly one instruction per nanosecond, but a slow device — a disk at about 10 ms, a network at tens to hundreds of ms — takes millions of CPU cycles to answer. If the program tells the CPU to stop and wait, the CPU sits idle: powered on but doing no useful work, throwing away every cycle of the wait. Async exists so the CPU does other useful work during that gap instead of idling. It does not make the device faster — the disk still takes its 10 ms — it only stops the fast CPU from being wasted while the slow device works." },
    { q: "What is the difference between a blocking and a non-blocking call, and what is a callback for?", a: "A blocking call does not return until its work is done; when the work is slow, its stack frame stays stuck on the stack for the whole wait and the next line cannot run — the program is frozen. A non-blocking call hands the request to the device and returns immediately, so the program keeps running while the device works in the background. Because the call returns before the result exists, it cannot return the result the ordinary way. So the program supplies a callback: a function handed over with the instruction 'run this when the result is ready.' When the device finishes, the result is delivered by calling that callback with the result as its argument." },
    { q: "What is the event loop, and what single rule governs when a callback runs?", a: "The event loop is two things: a callback queue (a first-in-first-out waiting line of callbacks whose slow work has finished) and a loop that repeats forever. Its one rule: if the call stack is empty, take the front callback off the queue and run it; otherwise wait. Two consequences follow. Synchronous code always finishes first, because while it runs the stack is non-empty and the loop will not start a callback. And each callback runs to completion without interruption, because the loop does not take the next one until the current callback's frames have all popped. This is why console.log(1); setTimeout(cb, 0); console.log(3) prints 1, 3, 2 — the 0-delay callback is only queued fast, not run fast." },
    { q: "What is the difference between concurrency and parallelism, and why can a single-core machine be concurrent but never parallel?", a: "A core is one fetch-decode-execute engine and runs exactly one instruction at a time; a thread is one ordered stream of instructions. Concurrency is interleaving many tasks on one core so they take turns — at any instant only one runs, but all make progress over time (the event loop is concurrency). Parallelism is many tasks literally running at the same instant, which requires multiple cores, one instruction per core in flight together. A single-core machine can interleave tasks (concurrency) but it physically executes one instruction at a time, so it can never have two tasks running in the same instant (parallelism). The slogan: concurrency is about structure, parallelism is about execution." },
    { q: "What is a race condition, and why does counter = counter + 1 across two threads expose one?", a: "A race condition is when the result of a program depends on the unpredictable order (the interleaving) in which concurrent tasks run, and some orders give a wrong answer. counter = counter + 1 looks like one step but is really three: read counter, add one, write counter back. If two threads run it in parallel on shared state, both can read the same old value before either writes, then both write the same new value — so one increment is lost (a lost update). Run with no synchronization, the final count comes out below the true total and varies from run to run, because the outcome depends on how the reads and writes happen to interleave." },
    { q: "What does a lock do to fix a race, and what is the cost of using one?", a: "A lock makes a section of code indivisible: a thread must acquire the lock to enter the protected section, and while it holds the lock no other thread can enter. So the read-add-write can no longer interleave — threads take turns through the shared update, and the result becomes correct and deterministic. The cost is that the protected section runs serially: threads queue for the lock instead of running it in parallel, which removes parallelism for exactly that section. The senior rule is correctness first, then minimise the cost by keeping the locked region as small as possible and having threads share as little state as possible — not by dropping the lock." },
  ]}
/>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the unit's spine: **async** exists because the CPU must not sit idle across the huge CPU-vs-device speed gap; a **non-blocking** call returns immediately and delivers its result later through a **callback**; the **event loop** runs callbacks one at a time, only when the call stack is empty, so synchronous code always finishes first. That is **concurrency** — interleaving on one core, taking turns — as opposed to **parallelism**, true simultaneity on multiple cores. And the moment concurrent tasks share state, order matters: a **race condition** can lose updates because read-add-write is not atomic, and a **lock** restores correctness by serialising the shared section at the cost of its parallelism.
</Recap>
