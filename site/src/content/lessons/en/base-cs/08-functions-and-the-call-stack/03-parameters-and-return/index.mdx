---
concepts:
- parameter
- argument
- return-value
- frame-boundary
- pass-by-value
estMin: 20
lang: en
lessonType: coding
mathPrereqs: []
order: 3
prereqs:
- 01-what-a-function-is
- 02-the-call-stack
- 04-stack-and-heap
- 01-a-variable-is-a-named-cell
slug: 03-parameters-and-return
sources:
- https://www.nand2tetris.org/
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Functions
- https://www.cs.cmu.edu/afs/cs/academic/class/15213-f15/www/lectures/09-machine-advanced.pdf
status: ready
summary: Parameters are values copied into the new frame before the function body
  runs. The return value is handed back to the caller when RET fires. Both pass data
  through the stack frame boundary.
title: Parameters and return values
track: base-cs
unit: 08-functions-and-the-call-stack
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
You can now trace the call stack as functions are called and returned. The next question
is: how does data get into a function, and how does the result get back out?

A function that always works on the same fixed values is almost useless. Real functions
take **parameters** — values provided by the caller at call time — and produce a
**return value** that the caller can use. These two mechanisms are the interface between
a function and the code that calls it, and they both operate through the stack frame.
</Hook>

<Goal lang="en">
After this lesson you can explain what a parameter is (a value copied into the new frame
at call time), what a return value is (a value produced by the function and handed back
to the caller), and trace a function call that takes parameters and returns a value,
showing the frame contents at each step.
</Goal>

<Idea lang="en">
**Parameters** are declared in the function signature. When the caller issues the `CALL`,
the values it supplies (called **arguments**) are copied into the new frame before the
function body starts executing. Inside the function, parameters look and behave exactly
like local variables — they are named cells inside the frame. The difference is that
they are pre-populated with the caller's values rather than being set by assignment.

In TypeScript syntax, `function add(x: number, y: number): number` declares two
parameters `x` and `y`. At the call site `add(3, 5)`, the values `3` and `5` are the
arguments that get copied into the new frame's `x` and `y` cells.

**Return values** work in the reverse direction. When the function body reaches a
`return` statement, it computes the value to return and makes it available to the caller.
In hardware, this is typically done by placing the return value in a CPU register (a
fast storage slot inside the CPU itself) before firing `RET`. The caller can then read
that register after `RET` restores the program counter.

The key insight: parameters pass data *into* a frame; the return value passes data *out*.
Both crossings happen at the frame boundary — the moment of `CALL` (in) and `RET` (out).

The function to trace: `add(x, y)` takes two numbers and returns their sum. The trace
shows `add(3, 5)`, with the stack before the call, during execution, and after the return.
</Idea>

<Code lang="en">

<AnnotatedCode
  code={`function add(x: number, y: number): number {
  let result = x + y;   // result is a local variable in add's frame
  return result;         // hands result back to the caller
}

function main(): void {
  let total = add(3, 5); // caller: arguments 3 and 5 passed to add
  // total now holds 8
}`}
  annotations={[
    { line: 1, text: "x and y are parameters — they live in add's frame, pre-filled with the caller's arguments" },
    { line: 2, text: "result is a local variable — add's frame now holds x, y, and result" },
    { line: 3, text: "return: places the value of result in a register, then RET fires" },
    { line: 7, text: "add(3,5): arguments 3 and 5 are copied into add's new frame as x and y" },
    { line: 8, text: "after RET: the register holding 8 is read; total is assigned 8 in main's frame" },
  ]}
  caption="add(3, 5): parameters enter add's frame at call; return value leaves via a register at RET."
/>
</Code>

<Trace lang="en">
Trace `main()` which calls `add(3, 5)`. Each cell shows one frame's contents.

<AlgoTrace
  id="trace-params-return"
  lang="en"
  code={[
    "function add(x: number, y: number): number {",
    "  let result = x + y;",
    "  return result;",
    "}",
    "",
    "function main(): void {",
    "  let total = add(3, 5);",
    "}"
  ]}
  frames={[
    {
      caption: "main() called. main's frame pushed. total not yet assigned.",
      line: 6,
      cells: [
        { value: "main\ntotal=?", mark: "active" }
      ]
    },
    {
      caption: "Line 7: add(3,5) called. add's frame pushed with x=3, y=5 pre-filled.",
      line: 7,
      cells: [
        { value: "main\ntotal=?", mark: "done" },
        { value: "add\nx=3,y=5", mark: "active" }
      ]
    },
    {
      caption: "Line 2: result = x + y = 3 + 5 = 8. result stored in add's frame.",
      line: 2,
      cells: [
        { value: "main\ntotal=?", mark: "done" },
        { value: "add\nx=3,y=5\nres=8", mark: "active" }
      ]
    },
    {
      caption: "Line 3: return result. Value 8 placed in return register. RET fires.",
      line: 3,
      cells: [
        { value: "main\ntotal=?", mark: "done" },
        { value: "add\nret=8", mark: "target" }
      ]
    },
    {
      caption: "add's frame popped. Return register holds 8. main resumes at line 7.",
      line: 7,
      cells: [
        { value: "main\ntotal=?", mark: "active" }
      ]
    },
    {
      caption: "Line 7 completes: total = 8 (read from return register). main's frame updated.",
      line: 7,
      cells: [
        { value: "main\ntotal=8", mark: "active" }
      ]
    },
    {
      caption: "Line 8: main returns. main's frame popped. Stack empty.",
      line: 8,
      cells: []
    }
  ]}
/>
</Trace>

<Inset kind="why" lang="en">
Why are parameters copied into the frame rather than shared with the caller? Because each
function call needs an independent workspace. If `add` could modify the caller's `3` and
`5`, calling `add` twice with different arguments would be impossible to reason about.
Copying the values means the callee's frame is entirely self-contained — the callee can
freely modify its `x` and `y` cells without changing anything in the caller's frame. This
"copy-in" behaviour is called **pass-by-value** and it is the default in most languages
for primitive values like numbers.
</Inset>

<PracticeSet
  id="parameters-and-return-practice"
  lessonSlug="03-parameters-and-return"
  lang="en"
  problems={[
    {
      prompt: "function multiply(a: number, b: number): number { return a * b; } — How many parameters does multiply have?",
      answer: 2,
      hint: "The parameters are declared in the function signature: a and b. Count them: 2.",
    },
    {
      prompt: "multiply(4, 6) is called. What value does multiply return?",
      answer: 24,
      hint: "return a * b = 4 * 6 = 24.",
    },
    {
      prompt: "After multiply(4, 6) returns, how many stack frames are on the stack (assuming it was called from main and nothing else is active)?",
      answer: 1,
      hint: "multiply's frame is popped on return. Only main's frame remains. 1 frame.",
    },
    {
      prompt: "function square(n: number): number { return n * n; } — square(7) is called. What value is placed in the return register?",
      answer: 49,
      hint: "return n * n = 7 * 7 = 49.",
    },
    {
      prompt: "function add(x: number, y: number): number { let r = x + y; return r; } — add's frame holds x, y, and r. How many named cells does add's frame contain?",
      answer: 3,
      hint: "x (parameter), y (parameter), and r (local variable) — 3 named cells in add's frame.",
    },
  ]}
/>

<Check lang="en">
  <Quiz
    id="parameters-and-return-check"
    pieceSlug="03-parameters-and-return"
    lang="en"
    question="When add(3, 5) is called, where do the values 3 and 5 live during add's execution?"
    choices={[
      {
        label: "They are copied into add's stack frame as the parameters x and y, where they live as named cells in the frame",
        correct: true,
      },
      {
        label: "They stay in main's frame; add reads them from main's frame when it needs them",
        misconception: "Parameters are copied into the callee's own frame, not read from the caller's frame. This copy-in is what makes each call independent — the callee has its own workspace and cannot accidentally modify the caller's variables.",
      },
      {
        label: "They are placed in global memory that any function can access",
        misconception: "Arguments are not placed in global memory. They are copied into the new stack frame created for the call. The frame is private to that call — it is popped and discarded when the function returns.",
      },
    ]}
  />
</Check>

<Recap lang="en">
**Parameters** are named cells in a function's stack frame, pre-filled with the
**arguments** (values) the caller supplied at call time. Inside the function, parameters
behave exactly like local variables. The values are **copied** into the frame (pass-by-
value for primitives), so the callee has an independent workspace. A **return value** is
produced by the `return` statement: the value is placed in a CPU register, `RET` fires,
the frame is popped, and the caller reads the return value from the register. Parameters
carry data *into* the frame at `CALL`; the return value carries data *out* of the frame
at `RET`.
</Recap>
