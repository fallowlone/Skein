---
slug: 05-a-toy-cpu
lang: en
track: base-cs
unit: 03-the-processor
order: 5
title: "A toy CPU"
summary: "Put it all together: a toy CPU with two registers and a four-instruction set. Walk a small program — load two numbers, add them, store the result — through the fetch–decode–execute loop step by step."
estMin: 25
status: ready
lessonType: concept
prereqs: ["01-the-instruction", "02-fetch-decode-execute", "03-registers", "04-machine-code"]
mathPrereqs: []
concepts: ["toy-cpu", "program-trace", "instruction-cycle-trace"]
sources:
  - https://www.nand2tetris.org/
  - https://en.wikipedia.org/wiki/Instruction_cycle
  - https://www.cs.cmu.edu/afs/cs/academic/class/15213-f15/www/lectures/05-machine-basics.pdf
---

import Hook from "../../../../../../components/lesson/Hook.astro";
import Goal from "../../../../../../components/lesson/Goal.astro";
import Step from "../../../../../../components/lesson/Step.astro";
import WorkedExample from "../../../../../../components/lesson/WorkedExample.astro";
import Check from "../../../../../../components/lesson/Check.astro";
import Recap from "../../../../../../components/lesson/Recap.astro";
import Inset from "../../../../../../components/lesson/Inset.astro";
import MachineFigure from "../../../../../../components/algo/MachineFigure.astro";
import PracticeSet from "../../../../../../components/math/PracticeSet.astro";
import Quiz from "../../../../../../components/pedagogy/Quiz.astro";

<Hook>
You have learned about instructions, the fetch–decode–execute cycle, registers, and machine
code as individual ideas. Now it is time to put them all together in one place and watch
them work as a unified system.

In this lesson you will build a mental model of a complete — if tiny — CPU. It has two
registers, a four-instruction set, and a small memory. Then you will trace a real program
through it: load two numbers from memory, add them, store the result. Every single fetch,
decode, and execute step is shown explicitly. By the end, the CPU will no longer be a
black box. It will be a machine you can run in your head.
</Hook>

<Goal lang="en">
After this lesson you can describe the components of a minimal CPU (registers, PC, memory,
ALU), read and write programs in the toy instruction set, and trace the fetch–decode–execute
cycle step by step for a complete small program.
</Goal>

<Step n={1}>
**The toy CPU's components.** Our toy CPU has these parts:

- **Two general-purpose registers**: R0 and R1. Each holds one 8-bit value (0–255).
- **A program counter (PC)**: holds the memory address of the next instruction to fetch.
  Also 8-bit, so it can address memory cells 0–255.
- **Memory**: 256 cells, each holding one 8-bit value. Addresses 0–9 will hold our
  program (the instructions). Addresses 200 and 201 will hold our data (the input
  numbers). Address 202 will hold our output (the result).
- **An Arithmetic Logic Unit (ALU)**: a circuit that can add two 8-bit values and store
  the result in a register.

The toy CPU cannot do very much — it is designed to demonstrate the essentials, not to be
practical. Real CPUs have 16–31 general-purpose registers, can address gigabytes of memory,
and have instruction sets with hundreds of opcodes. But the principles are the same.
</Step>

<Step n={2}>
**The toy instruction set.** Our CPU understands exactly four instructions. Each
instruction is exactly one byte long. The top 2 bits are the opcode; the remaining 6 bits
are the operand.

| Opcode (bits 7–6) | Name | Meaning |
|---|---|---|
| `00` | LOAD Ra, addr | Load value from memory address `addr` into register Ra (bit 5 selects R0 or R1) |
| `01` | STORE addr, Ra | Store register Ra's value to memory address `addr` |
| `10` | ADD R0, R1 | Add R0 and R1; store result in R0. No operand bits used. |
| `11` | JUMP addr | Set PC to `addr` (6-bit address in operand bits) |

Because each instruction is one byte, addresses in LOAD and STORE instructions are only 6
bits wide — they can only address cells 0–63. For a real CPU these would be wider, but one
byte per instruction keeps our trace simple.

For our program, we will write instructions in plain English and map them to opcodes when
needed:

```
LOAD R0, 200    ; R0 ← memory[200]
LOAD R1, 201    ; R1 ← memory[201]
ADD  R0, R1     ; R0 ← R0 + R1
STORE 202, R0   ; memory[202] ← R0
```

This program loads two numbers from memory, adds them, and stores the result.
</Step>

<Inset kind="why" lang="en">
**Why only four instructions?** Real instruction sets have hundreds of opcodes. But you
only need a handful to demonstrate everything that matters: data movement (LOAD/STORE),
computation (ADD), and control flow (JUMP). Every more complex instruction is a
combination or extension of these ideas. nand2tetris uses a similarly minimal instruction
set (called "Hack") to build a complete working computer from first principles; the entire
language fits in 16 bits.
</Inset>

<Step n={3}>
**The initial state of memory.** Before the program runs, the OS loads the program's
instruction bytes into memory starting at address 0, and places the input data at addresses
200 and 201. The PC is set to 0.

```
Address  Content    Meaning
0        LOAD R0, 200  ← first instruction (PC starts here)
1        LOAD R1, 201  ← second instruction
2        ADD R0, R1    ← third instruction
3        STORE 202, R0 ← fourth instruction
...
200      25             ← first input number
201      17             ← second input number
202      0              ← result slot (empty)
```

Registers before execution: R0 = 0, R1 = 0.

Now the CPU starts its loop.
</Step>

<Step n={4}>
**The program counter drives everything.** Before tracing each cycle in detail, notice the
key role of the PC:

- The PC starts at 0 — so the first instruction fetched is the one at address 0.
- After each non-JUMP instruction, the PC increases by 1 (because each instruction is
  1 byte in our toy CPU).
- If the program contained a JUMP, the PC would be set to the jump target instead.

The PC is the cursor that sweeps through the instruction sequence. It is the only reason
the CPU knows which instruction is "next."
</Step>

<MachineFigure
  kind="cells"
  cells={[
    { value: "LOAD R0,200", addr: "0",   mark: "active" },
    { value: "LOAD R1,201", addr: "1" },
    { value: "ADD R0,R1",   addr: "2" },
    { value: "STORE 202,R0",addr: "3" },
    { value: "···",         addr: "···" },
    { value: "25",          addr: "200", mark: "target" },
    { value: "17",          addr: "201", mark: "target" },
    { value: "0",           addr: "202" },
  ]}
  caption="Memory before execution. Instructions occupy addresses 0–3. Data inputs sit at 200 and 201; the result slot at 202 is 0. PC = 0 (highlighted instruction)."
/>

<WorkedExample lang="en">
**Full trace of the four-instruction program.**

We track: PC (program counter), R0, R1, and the content of memory address 202 after each
cycle.

---

**Cycle 1**

State before: PC = 0, R0 = 0, R1 = 0, mem[202] = 0.

- **Fetch**: Read instruction at address PC = 0. Instruction = `LOAD R0, 200`.
- **Decode**: Opcode `00` → LOAD. Operand bit 5 → destination = R0. Address field = 200.
- **Execute**: Read memory[200]. Result = 25. Write 25 into R0.
- **PC advance**: PC = 0 + 1 = 1.

State after: PC = 1, **R0 = 25**, R1 = 0, mem[202] = 0.

---

**Cycle 2**

State before: PC = 1, R0 = 25, R1 = 0, mem[202] = 0.

- **Fetch**: Read instruction at address PC = 1. Instruction = `LOAD R1, 201`.
- **Decode**: Opcode `00` → LOAD. Destination = R1. Address = 201.
- **Execute**: Read memory[201]. Result = 17. Write 17 into R1.
- **PC advance**: PC = 1 + 1 = 2.

State after: PC = 2, R0 = 25, **R1 = 17**, mem[202] = 0.

---

**Cycle 3**

State before: PC = 2, R0 = 25, R1 = 17, mem[202] = 0.

- **Fetch**: Read instruction at address PC = 2. Instruction = `ADD R0, R1`.
- **Decode**: Opcode `10` → ADD. No address operand. Sources = R0, R1. Destination = R0.
- **Execute**: ALU computes R0 + R1 = 25 + 17 = 42. Write 42 into R0.
- **PC advance**: PC = 2 + 1 = 3.

State after: PC = 3, **R0 = 42**, R1 = 17, mem[202] = 0.

---

**Cycle 4**

State before: PC = 3, R0 = 42, R1 = 17, mem[202] = 0.

- **Fetch**: Read instruction at address PC = 3. Instruction = `STORE 202, R0`.
- **Decode**: Opcode `01` → STORE. Source = R0. Destination address = 202.
- **Execute**: Write R0's value (42) into memory[202].
- **PC advance**: PC = 3 + 1 = 4.

State after: PC = 4, R0 = 42, R1 = 17, **mem[202] = 42**.

---

The program is done. Memory address 202 now holds 42, the sum of the two input numbers
(25 + 17). The CPU will attempt to fetch the instruction at address 4 — but there is no
fifth instruction in our program, so in a real system the OS would have detected the end
of the program and stopped the process before reaching address 4.

The entire computation required exactly four fetch–decode–execute cycles. Each cycle was
the same three-step structure. The CPU never needed to "understand" what it was computing —
it just ran its loop.
</WorkedExample>

<Inset kind="edgecase" lang="en">
**What happens when the PC runs off the end?** In our toy example the PC would reach
address 4 and try to fetch whatever byte is there — garbage, or perhaps another program's
code. Real operating systems prevent this: the program binary contains metadata telling
the OS where the program ends (or there is an explicit HALT instruction or a system call
to exit). The OS sets up memory protection so the program cannot execute beyond its own
region. In practice, the last instruction of almost every program is a call to the OS
"exit" system call, which terminates the process cleanly.
</Inset>

<PracticeSet
  id="a-toy-cpu-practice"
  lessonSlug="05-a-toy-cpu"
  lang="en"
  problems={[
    {
      prompt: "In our toy CPU, each instruction is 1 byte. After executing the instruction at PC = 5 (not a JUMP), what is the new value of PC?",
      answer: 6,
      hint: "PC advances by the instruction size. 1-byte instruction at address 5 → PC = 5 + 1 = 6.",
    },
    {
      prompt: "Before cycle 3 in our trace, R0 = 25 and R1 = 17. After ADD R0, R1 executes, what value does R0 hold?",
      answer: 42,
      hint: "ADD puts R0 + R1 into R0. 25 + 17 = 42.",
    },
    {
      prompt: "After cycle 4 (STORE 202, R0) in our trace, what value does memory address 202 hold?",
      answer: 42,
      hint: "STORE writes R0's value into memory[202]. R0 = 42 at that point, so memory[202] = 42.",
    },
    {
      prompt: "How many fetch–decode–execute cycles did our 4-instruction program require to compute 25 + 17 and store the result?",
      answer: 4,
      hint: "One cycle per instruction: LOAD R0, LOAD R1, ADD, STORE. That is 4 cycles.",
    },
    {
      prompt: "At the start of cycle 1, the PC = 0. After all 4 cycles complete, PC = ?",
      answer: 4,
      hint: "PC advances by 1 after each of the four 1-byte instructions: 0 → 1 → 2 → 3 → 4.",
    },
  ]}
/>

<Check lang="en">
  <Quiz
    id="a-toy-cpu-check"
    pieceSlug="05-a-toy-cpu"
    lang="en"
    question="In the toy CPU trace, what is the role of the ADD instruction compared to the LOAD instructions?"
    choices={[
      {
        label: "LOAD instructions bring values from memory into registers; ADD operates on values already in registers and stores the result in a register without touching memory",
        correct: true,
      },
      {
        label: "LOAD adds two numbers; ADD loads a number from memory into a register",
        misconception: "Those descriptions are reversed. LOAD reads a value from a memory address into a register. ADD performs addition on two register values. Memory is not accessed during ADD — the inputs are already in R0 and R1 from the LOAD steps.",
      },
      {
        label: "LOAD and ADD do the same thing; the difference is only which register they target",
        misconception: "They do fundamentally different things. LOAD is a memory instruction that reads from a memory address. ADD is an arithmetic instruction that uses the ALU to add two register values. They touch different hardware units (memory bus vs ALU) and neither has the same behaviour as the other.",
      },
    ]}
  />
</Check>

<Recap lang="en">
A minimal CPU needs: **registers** (fast working storage — R0, R1), a **program counter**
(a register holding the next instruction's address), **memory** (byte-addressed cells for
instructions and data), and an **ALU** (the circuit that computes arithmetic). The CPU's
behaviour is entirely captured by the fetch–decode–execute cycle: fetch the instruction
at PC, decode its opcode and operands, execute the corresponding action, advance PC. A
four-instruction program (LOAD, LOAD, ADD, STORE) runs in four cycles and computes a sum
without the CPU ever "knowing" it was adding — it only ran its loop. Every larger program
— no matter how complex — is the same loop running longer, on more instructions, with a
larger register file and more memory. The mechanism never changes.
</Recap>
