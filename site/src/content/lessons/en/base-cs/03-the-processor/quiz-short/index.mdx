---
concepts:
- fetch-decode-execute
- program-counter
- register
- machine-code
- stored-program
- instruction-set
estMin: 13
lang: en
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.nand2tetris.org/
- https://en.wikipedia.org/wiki/Instruction_cycle
- https://en.wikipedia.org/wiki/Processor_register
status: ready
summary: Free-recall prompts across the processor unit. Answer each from memory first, then reveal the model answer and compare.
title: 'The processor: free-recall review'
track: base-cs
unit: 03-the-processor
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory before you open the model answer — the effort of pulling the mechanism out of your head is what makes it stick.
</Hook>

<Goal lang="en">
Reconstruct the unit's core mechanisms from memory: what an instruction is, how the fetch-decode-execute loop runs, what the program counter and registers do, and why a program is just bytes in memory.
</Goal>

<RetrievalDrawer
  client:load
  id="03-the-processor-short-retrieval"
  lang="en"
  questions={[
    { q: "What is a CPU instruction, and what two parts does almost every instruction have?", a: "A CPU instruction is the smallest, indivisible command the processor can carry out in a single step — it cannot be broken into smaller pieces. Almost every instruction has two parts: the operation (the opcode), which says what to do, such as ADD, LOAD, STORE, or JUMP; and the operands, which say what to do it to — the inputs, such as which registers, which memory address, or which immediate constant. ADD needs two register inputs; LOAD needs an address to read from; JUMP needs a target address. A few instructions, like HALT, take no operands." },
    { q: "Describe the fetch-decode-execute cycle and the role of the program counter in it.", a: "The CPU repeats one loop forever. Fetch: it reads the instruction stored at the address held in the program counter (PC) into the instruction register. Decode: the control unit reads the opcode and produces control signals that route the operands and select the functional unit. Execute: the chosen unit carries out the action — the ALU for arithmetic, the memory bus for LOAD/STORE, or a PC rewrite for JUMP. Then the PC advances by the instruction's byte size (so the next fetch reads the following instruction), unless a JUMP set it to a target. The PC is the cursor that tells the CPU which instruction is next; without it the loop could not move forward." },
    { q: "Why does the CPU keep working values in registers rather than reading and writing main memory for every step?", a: "Registers are tiny storage slots built directly into the CPU, on the same silicon as the ALU, so reading one takes about 1-2 clock cycles. Main memory (RAM) is a separate chip reached over a bus, and an access costs roughly 100-300 cycles — about 100-300x slower. If the CPU went to RAM for every intermediate value, simple loops would spend almost all their time waiting on memory. Instead the principle is: bring the data into the CPU's neighbourhood (a register), do the work there at full speed, and write the result back to RAM once at the end. Registers are that neighbourhood." },
    { q: "What is the difference between a general-purpose register and a special-purpose register? Give an example of each.", a: "A general-purpose register (GPR) has no fixed meaning — any instruction can use it to hold any value, and the compiler decides which value lives where. Examples: R0/R1 in the toy CPU, or RAX/RBX on x86-64 (which has 16 GPRs; ARM has 31). A special-purpose register has a fixed hardware role. The program counter holds the address of the next instruction; the stack pointer tracks the top of the call stack; the flags (status) register records outcomes like zero, carry, or negative that conditional jumps consult; the instruction register holds the instruction currently being executed. The program counter is itself just a register — advancing it is the normal PC-update that happens every cycle." },
    { q: "State the stored-program (von Neumann) principle and explain why it matters in practice.", a: "The stored-program principle says a program's instructions and its data live in the same memory, both as ordinary bytes; there is no hardware difference between an instruction byte and a data byte. Bytes are treated as an instruction only when the program counter points at them during a fetch, and as data when a LOAD or STORE reaches them. It matters because it means a single machine can run any program: you change what the computer does just by loading different bytes into memory — no rewiring. That is why you install software without opening the hardware, why one phone CPU runs a browser and a game, and why just-in-time compilers can generate machine code into memory at runtime. (The OS uses memory protection to stop a program from executing data it should not.)" },
    { q: "What is machine code, and what is an instruction set (ISA)? Why can't an ARM binary run directly on an x86-64 chip?", a: "Machine code is the raw bit patterns that directly encode instructions for a specific CPU — the only language the hardware actually understands. Each instruction is a specific sequence of bytes: an opcode field plus operand fields (register numbers, immediate values, or addresses). The instruction set, or Instruction Set Architecture (ISA), is the complete vocabulary of instructions a CPU model understands, together with their encodings — it is the contract between software and hardware. x86-64, ARM, and RISC-V are different ISAs with different opcode encodings, so a binary built for ARM is a stream of bytes the x86-64 decoder cannot interpret as its own instructions. Running it requires recompiling for the target ISA or using an emulator/translation layer." },
  ]}
/>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the unit's spine: an instruction is an operation plus operands; the CPU runs them in the fetch-decode-execute loop driven by the program counter; registers hold the working values because they are 100-300x faster than RAM; some registers (PC, stack pointer, flags, instruction register) have fixed hardware roles; and the whole program is just machine-code bytes in shared memory — the stored-program idea — which is why a single machine can run any program and why each ISA defines which bytes a given chip can run.
</Recap>
