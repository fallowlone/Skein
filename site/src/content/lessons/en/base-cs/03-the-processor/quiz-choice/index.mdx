---
concepts:
- fetch-decode-execute
- program-counter
- register
- machine-code
- instruction-set
- stored-program
estMin: 13
lang: en
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.nand2tetris.org/
- https://en.wikipedia.org/wiki/Instruction_cycle
- https://en.wikipedia.org/wiki/Von_Neumann_architecture
status: ready
summary: "Multiple-choice synthesis across the processor unit: instructions, the fetch-decode-execute loop, the program counter, registers, machine code, and the stored-program idea."
title: 'The processor: multiple-choice review'
track: base-cs
unit: 03-the-processor
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. None is a definition to recite — each makes you connect the instruction, the loop, the registers, and the bytes in memory into one working machine.
</Hook>

<Goal lang="en">
Confirm you can connect the pieces the lessons built separately: an instruction is an operation plus operands, the CPU runs them in the fetch-decode-execute loop driven by the program counter, registers hold the working values, and the whole program is just machine-code bytes in memory.
</Goal>

<Quiz
  id="03-the-processor-choice-1"
  lessonSlug="03-the-processor"
  lang="en"
  question="A program adds one million numbers in a loop. Why does the CPU keep the running total in a register instead of in main memory?"
  choices={[
    { label: "A register sits on the same silicon as the ALU and is read in 1-2 clock cycles, while a RAM access costs roughly 100-300 cycles; keeping the running total in a register avoids paying that latency a million times.", correct: true },
    { label: "Main memory can only hold instructions, not numbers, so the total has to live in a register.", misconception: "Main memory holds both instructions and data with no distinction in the storage medium. Registers exist for speed, not because RAM cannot store a number." },
    { label: "Registers can hold larger numbers than memory cells, so the total will not overflow.", misconception: "A register and a memory cell hold the same fixed width of bits. Overflow is not the reason; the reason is that register access is far faster than RAM access." },
    { label: "It makes no real difference; the compiler picks one at random.", misconception: "The choice is the whole point of registers: bring the value into the CPU's neighbourhood, work on it at register speed, and write it back to RAM once at the end." },
  ]}
/>

<Quiz
  id="03-the-processor-choice-2"
  lessonSlug="03-the-processor"
  lang="en"
  question="The CPU just finished executing a normal ADD instruction (not a jump). What happens to the program counter, and why?"
  choices={[
    { label: "It advances by the size of that instruction, so the next fetch reads the instruction that immediately follows in memory. Sequential execution is the default; a jump is the exception that overrides it.", correct: true },
    { label: "It is incremented by exactly 1, because the program counter counts instructions executed.", misconception: "Despite the name, the PC holds an address, not a count. It advances by the instruction's byte size — 2 in the toy CPU, 4 on ARM, 1-15 on x86-64 — not always by 1." },
    { label: "It is set to the result of the ADD, so execution continues from that address.", misconception: "Arithmetic results go into a destination register. The PC is only overwritten by a jump or branch, never by the value an ADD computed." },
    { label: "It stays the same until a jump moves it.", misconception: "If the PC never advanced on its own, the CPU would fetch the same instruction forever. Sequential advance after every non-jump instruction is what drives the loop forward." },
  ]}
/>

<Quiz
  id="03-the-processor-choice-3"
  lessonSlug="03-the-processor"
  lang="en"
  question="A junior says: 'My code is written in Python, so the CPU runs Python directly.' What is wrong with this?"
  choices={[
    { label: "The CPU understands only the bit patterns of its own instruction set. Python must be translated — compiled or interpreted — into those instructions before any of it can run on the chip.", correct: true },
    { label: "Nothing is wrong; modern CPUs have a built-in Python instruction set.", misconception: "No mainstream CPU decodes Python. x86-64, ARM, and RISC-V each understand only their own machine-code encoding; a language runtime bridges the gap." },
    { label: "Python is too slow to compile, so it never reaches the CPU at all.", misconception: "Python absolutely reaches the CPU — via the interpreter, whose own machine code executes on behalf of each Python statement. The CPU runs the interpreter's instructions, not Python text." },
    { label: "The CPU runs Python only after it is rewritten in C.", misconception: "Rewriting in C is not required. The interpreter executes Python without translating it to native code; either way the CPU only ever runs machine code from its own instruction set." },
  ]}
/>

<Quiz
  id="03-the-processor-choice-4"
  lessonSlug="03-the-processor"
  lang="en"
  question="During the Decode step of the cycle, which part of the CPU does the work, and what is it producing?"
  choices={[
    { label: "The control unit reads the opcode and emits control signals that route the operands and select the right functional unit (ALU, memory bus, or PC update) for the Execute step.", correct: true },
    { label: "The ALU performs the arithmetic, because decoding means computing the result early.", misconception: "The ALU runs during Execute, not Decode. Decode only identifies the instruction and sets up the control signals; nothing is computed yet." },
    { label: "Main memory decodes the bytes and sends back the meaning of the instruction.", misconception: "Memory just returns the raw bytes during Fetch. Interpreting those bytes is the control unit's job inside the CPU, during Decode." },
    { label: "The program counter decodes the instruction so it knows how far to advance.", misconception: "The PC stores an address and advances after Execute. Decoding the opcode is the control unit's job, not the PC's." },
  ]}
/>

<Quiz
  id="03-the-processor-choice-5"
  lessonSlug="03-the-processor"
  lang="en"
  question="Two memory cells at addresses 100 and 101 hold the bytes that encode an instruction. The same byte values also appear at addresses 200 and 201, where the program stores data. What makes one pair an instruction and the other pair data?"
  choices={[
    { label: "Nothing about the bytes differs. They are an instruction only when the program counter points at them during a fetch, and data when a LOAD or STORE reaches them. This is the stored-program (von Neumann) idea.", correct: true },
    { label: "Instruction bytes are stored in a separate instruction-only memory chip that data can never enter.", misconception: "That describes a strict Harvard architecture. General-purpose CPUs use the von Neumann model: instructions and data share one memory, distinguished only by context." },
    { label: "Instruction bytes carry a hidden tag bit that marks them as executable.", misconception: "There is no per-byte instruction tag in main memory. Executable-versus-data is enforced by OS memory protection over whole regions, not by a flag inside each byte." },
    { label: "The CPU re-reads the bytes and recognises the opcode pattern as 'an instruction'.", misconception: "Many opcode patterns are also valid data values. The CPU does not classify a byte by its value — it treats it as an instruction purely because the PC pointed at it." },
  ]}
/>

<Quiz
  id="03-the-processor-choice-6"
  lessonSlug="03-the-processor"
  lang="en"
  question="A program compiled for an Apple Silicon (ARM) Mac will not run as-is on an Intel (x86-64) laptop, even though both are 64-bit CPUs. Why?"
  choices={[
    { label: "ARM and x86-64 are different instruction sets with different opcode encodings. The instruction set is the contract between software and hardware, and the two chips do not share it.", correct: true },
    { label: "x86-64 is faster, so it refuses to run a binary built for the slower ARM chip.", misconception: "Speed is irrelevant. The binary is a stream of ARM machine-code bytes that the x86-64 decoder simply cannot interpret as its own instructions." },
    { label: "The ARM binary is encrypted and only Apple hardware holds the key.", misconception: "It is not an encryption issue. The bytes are perfectly readable; they just encode ARM instructions that an x86-64 chip has no decoding for." },
    { label: "Both being 64-bit, they share an instruction set, so it actually does run unchanged.", misconception: "Word size (64-bit) is not the same as instruction set. Same width, different encodings — which is exactly why a translation layer or emulator is needed to bridge them." },
  ]}
/>

<Recap lang="en">
The through-line of the unit is one machine. An instruction is an operation plus its operands; the CPU runs instructions in the fetch-decode-execute loop, with the program counter naming the next one and advancing by the instruction's size unless a jump overrides it. Registers hold the working values at CPU speed because RAM is 100-300x slower. Decode is the control unit turning an opcode into control signals. And the whole program is just machine-code bytes in shared memory — instructions only because the program counter walks through them. The instruction set is the contract that decides which chip can run which bytes.
</Recap>
