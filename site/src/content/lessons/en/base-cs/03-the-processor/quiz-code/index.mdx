---
concepts:
- instruction-trace
- register-state
- program-counter
- machine-code-decode
- clock-cycle
estMin: 14
lang: en
level: middle
order: 9
prereqs: []
slug: quiz-code
sources:
- https://www.nand2tetris.org/
- https://en.wikipedia.org/wiki/Instruction_cycle
- https://en.wikipedia.org/wiki/Machine_code
status: ready
summary: Trace small programs in the toy instruction set, predict register and program-counter state, decode a machine-code word, and count clock cycles.
title: 'The processor: trace and decode'
track: base-cs
unit: 03-the-processor
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Understanding the processor means being able to run it in your head. Each snippet below is a tiny program or a single machine-code word; read it, step the loop, and predict the state the way the CPU would produce it.
</Hook>

<Goal lang="en">
Practise the core skill of the unit: take instructions plus an initial state, step the fetch-decode-execute loop by hand, and read off the resulting register values, program-counter value, machine-code meaning, and cycle count.

These snippets use the toy instruction set from the unit: each instruction is 2 bytes, the program counter advances by 2 after every non-JUMP instruction, and the registers are R0 and R1.
</Goal>

## Snippet 1 — tracing register state

```
; Initial state: R0 = 0, R1 = 0, PC = 0
; Memory: mem[200] = 9, mem[201] = 4

0: LOAD  R0, 201
2: LOAD  R1, 200
4: ADD   R0, R1     ; R0 <- R0 + R1
6: STORE 202, R0
```

<Quiz
  id="03-the-processor-code-1"
  lessonSlug="03-the-processor"
  lang="en"
  question="After all four instructions execute, what value is stored in memory address 202?"
  choices={[
    { label: "13 — LOAD R0,201 puts 4 in R0; LOAD R1,200 puts 9 in R1; ADD sets R0 = 4 + 9 = 13; STORE writes R0 (13) to mem[202].", correct: true },
    { label: "9 — STORE writes whatever is at address 200.", misconception: "STORE 202, R0 writes the value held in register R0, not the contents of some memory address. By then R0 holds the sum 13, not 9." },
    { label: "5 — the program subtracts the two inputs.", misconception: "The only arithmetic instruction here is ADD, which adds R0 and R1. There is no subtraction; 4 + 9 = 13." },
    { label: "Cannot tell — R0 was never given a starting value.", misconception: "R0 is loaded from mem[201] = 4 in the first instruction, so its initial 0 is overwritten before the ADD. The result is fully determined: 13." },
  ]}
/>

## Snippet 2 — the program counter and a jump

```
; Initial state: PC = 0, R0 = 0
; Each instruction is 2 bytes.

0: LOAD R0, 200
2: JUMP 6
4: ADD  R0, R1      ; never reached
6: STORE 202, R0
```

<Quiz
  id="03-the-processor-code-2"
  lessonSlug="03-the-processor"
  lang="en"
  question="Which instruction at address 4 does NOT run, and what is the program-counter value immediately after the JUMP at address 2 executes?"
  choices={[
    { label: "ADD at address 4 is skipped; right after the JUMP, PC = 6. A JUMP sets the PC to its target instead of advancing by 2, so the next fetch comes from address 6, not 4.", correct: true },
    { label: "ADD at address 4 still runs; after the JUMP, PC = 4 because the PC always advances by 2.", misconception: "A JUMP overrides the normal +2 advance. It writes its target (6) into the PC, so address 4 is stepped over entirely." },
    { label: "Nothing is skipped; after the JUMP, PC = 8.", misconception: "The PC is set to the jump target 6, not advanced past it. Address 4 is skipped, and the next fetch is at 6." },
    { label: "STORE at address 6 is skipped; after the JUMP, PC = 6.", misconception: "PC = 6 is correct, but that means the fetch at address 6 (STORE) does run. The instruction stepped over is the ADD at address 4." },
  ]}
/>

## Snippet 3 — decoding a machine-code word

```
; 16-bit instruction encoding:
;   bits 15-12 (4 bits) = opcode
;   bits 11-8  (4 bits) = destination register number
;   bits 7-0   (8 bits) = immediate value
; opcode 0001 = LOAD-IMMEDIATE (put the constant into the register)

Word in memory:  0001 0011 00010010
```

<Quiz
  id="03-the-processor-code-3"
  lessonSlug="03-the-processor"
  lang="en"
  question="Decoding this 16-bit word, which instruction does it represent?"
  choices={[
    { label: "LOAD-IMMEDIATE into register 3 of the constant 18 — opcode 0001 = LOAD-IMMEDIATE, register field 0011 = 3, immediate 00010010 = 16 + 2 = 18.", correct: true },
    { label: "LOAD-IMMEDIATE into register 3 of the constant 12 — the immediate is read as hex 12.", misconception: "The immediate field is an 8-bit binary value, not hex. Binary 00010010 = 16 + 2 = 18, not 12." },
    { label: "ADD into register 1 — the opcode is 0011.", misconception: "The opcode is the top 4 bits, 0001 (LOAD-IMMEDIATE). 0011 is the destination-register field, which names register 3, not an opcode." },
    { label: "Cannot decode — the word is just data, not an instruction.", misconception: "Whether bytes are an instruction is set by the program counter, not by the bytes. Given that the PC points here, the fields decode unambiguously: LOAD-IMMEDIATE, register 3, value 18." },
  ]}
/>

## Snippet 4 — counting clock cycles

```
; A program runs 6 instructions in a tight loop body.
; On this CPU every instruction takes exactly 1 clock cycle.
; The loop body runs 100 times. The CPU clock runs at 2 GHz
; (2,000,000,000 clock cycles per second).
```

<Quiz
  id="03-the-processor-code-4"
  lessonSlug="03-the-processor"
  lang="en"
  question="How many clock cycles does the loop body consume in total, and roughly how long does that take at 2 GHz?"
  choices={[
    { label: "600 cycles total (6 instructions x 100 iterations), which at 2 billion cycles/second is about 300 nanoseconds.", correct: true },
    { label: "6 cycles total, because the 6 instructions are counted once regardless of iterations.", misconception: "Each iteration re-runs the loop body, so the cycles multiply by the iteration count: 6 x 100 = 600, not 6." },
    { label: "600 cycles total, which at 2 GHz takes about 300 milliseconds.", misconception: "The cycle count is right but the time is off by a million. 600 cycles / 2e9 cycles per second = 3e-7 s = 300 nanoseconds, not milliseconds." },
    { label: "100 cycles total, one per iteration.", misconception: "Each iteration runs all 6 instructions, and each instruction is 1 cycle, so an iteration costs 6 cycles. 100 iterations x 6 = 600." },
  ]}
/>

<Recap lang="en">
Every question here was answered by stepping the same loop: fetch the instruction the PC points to, decode its opcode and operands, execute it (LOAD and STORE move data between memory and registers; ADD uses the ALU on register values; JUMP rewrites the PC), then advance the PC by the instruction size — unless a JUMP set it directly. Machine code decodes by splitting the word into opcode and operand fields. And run time is just instructions x iterations x cycles-per-instruction, divided by the clock rate. Trace it by hand and the processor stops being a black box.
</Recap>
