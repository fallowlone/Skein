---
concepts:
- binding
- assignment
- mutation
- aliasing
- reference-vs-value
- const
estMin: 13
lang: en
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Data_structures#primitive_values
- https://tc39.es/ecma262/multipage/ecmascript-language-statements-and-declarations.html
status: ready
summary: "Multiple-choice synthesis across the variables unit — binding vs value, assignment as a store, mutation and state, copy vs reference semantics, and aliasing traps."
title: 'Variables and state: multiple-choice review'
track: base-cs
unit: 06-variables-and-state
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one is a decision about what is actually stored in a cell — a value or a reference — and what changes when you assign or mutate. Get the memory model right and aliasing bugs stop being surprises.
</Hook>

<Goal lang="en">
Confirm you can connect the four ideas the unit built up: a variable is a name bound to a cell, assignment overwrites that cell, mutation changes state in place, and primitives copy while objects share by reference.
</Goal>

<Quiz
  id="06-variables-and-state-choice-1"
  lessonSlug="06-variables-and-state"
  lang="en"
  question="After 'let score = 0', the program runs 'score = 42'. What changed in memory?"
  choices={[
    { label: "Only the contents of the cell bound to 'score' changed — 0 was overwritten with 42. The name and the name-to-address binding stayed exactly the same.", correct: true },
    { label: "The name 'score' itself became 42.", misconception: "A name is a source-code label; it never holds a value and never 'becomes' a number. The name 'score' stays 'score' — the cell it is bound to now contains 42." },
    { label: "A new cell was allocated for 42 and 'score' was rebound to it.", misconception: "Re-assignment writes in place into the existing cell. It does not allocate a new cell or change the binding — only the cell's contents change." },
    { label: "Both 0 and 42 are now stored, with 42 marked as current.", misconception: "Assignment is a destructive overwrite. The previous value 0 is gone; the cell holds only 42. Memory keeps no history of past values." },
  ]}
/>

<Quiz
  id="06-variables-and-state-choice-2"
  lessonSlug="06-variables-and-state"
  lang="en"
  question="Why is reading '=' as 'equals' a trap, and what should you read it as instead?"
  choices={[
    { label: "Read it as 'gets'. 'x = x + 1' is not a claim that x equals x+1; it is a store command: evaluate the right side first, then write the result into x's cell. The old value is overwritten.", correct: true },
    { label: "It is fine to read it as 'equals' — 'x = x + 1' simply asserts a true equation.", misconception: "As an equation 'x = x + 1' is impossible. It is not an equation at all; it is a two-step command — compute x+1, then store it back — so it is read 'x gets x+1'." },
    { label: "Read it as 'compare' — assignment checks whether the two sides are already equal.", misconception: "Comparison is a different operator (== or ===). A single '=' performs a write, not a test. It always stores the right-hand value into the left-hand cell." },
    { label: "The right side is stored unevaluated and computed only when x is next read.", misconception: "TypeScript evaluates eagerly. The right-hand side is computed to a concrete value immediately, and that value is written into the cell now — there is no deferred evaluation." },
  ]}
/>

<Quiz
  id="06-variables-and-state-choice-3"
  lessonSlug="06-variables-and-state"
  lang="en"
  question="A bug report says 'I changed obj2 and obj1 mutated on its own.' The code was 'let obj2 = obj1'. What actually happened?"
  choices={[
    { label: "'let obj2 = obj1' copied the reference, not the object. obj1 and obj2 are aliases for one object on the heap, so a mutation through either name is visible through the other. To get an independent copy, copy the object explicitly (e.g. {...obj1}).", correct: true },
    { label: "Assignment set up a live link that keeps the two objects synchronised.", misconception: "There is no ongoing sync. Assignment copied the address once; afterwards both variables independently hold the same address, which points to a single object. There were never two objects." },
    { label: "obj1 mutated because objects are always passed by deep copy.", misconception: "Objects are not deep-copied on assignment — only the reference is copied. The single underlying object is shared, which is exactly why the mutation appeared on both names." },
    { label: "This is impossible; mutating obj2 cannot affect obj1.", misconception: "It is the defining behaviour of aliasing. Both names hold the same reference to one object, so 'obj2.field = x' is the same write that 'obj1.field' reads back." },
  ]}
/>

<Quiz
  id="06-variables-and-state-choice-4"
  lessonSlug="06-variables-and-state"
  lang="en"
  question={"'const user = { name: \"A\" }; user.name = \"B\";' runs without error. Yet 'const x = 1; x = 2;' throws. Why?"}
  choices={[
    { label: "const seals the binding — the cell cannot be re-pointed — but does not freeze the contents at that reference. Rebinding user (user = ...) is rejected, while mutating the object it points to (user.name = ...) is allowed.", correct: true },
    { label: "const on an object is silently ignored, so any operation is allowed.", misconception: "const is enforced for objects too — rebinding 'user = somethingElse' still throws. What it does not do is prevent mutation of the object's properties; it only seals which reference the cell holds." },
    { label: "Objects are exempt from const because they are not values.", misconception: "Objects are values; the variable's cell holds a reference value. const seals that reference against replacement, but the heap object behind it is not frozen, so property mutation is fine." },
    { label: "The first line works only because the property already existed.", misconception: "Whether the property exists is irrelevant. const guards the binding, not the contents; adding a new property (user.age = 1) would also be allowed." },
  ]}
/>

<Quiz
  id="06-variables-and-state-choice-5"
  lessonSlug="06-variables-and-state"
  lang="en"
  question="'let a = [1,2]; let b = a; b.push(3);' — then code elsewhere reads a.length expecting 2. Why does it see 3?"
  choices={[
    { label: "Arrays are objects, so reference semantics apply. b holds the same reference as a; push mutates the one shared array in place, so a.length is 3. Independent length needs an explicit copy (e.g. let b = [...a]).", correct: true },
    { label: "push creates a new array and rebinds b, leaving a at length 2.", misconception: "push mutates the existing array in place; it does not create a new array or rebind anything. Because a and b alias that array, the new length is visible through a." },
    { label: "a.length is 3 only because both were declared with let.", misconception: "let vs const is irrelevant to sharing. The arrays are shared because the reference was copied on 'let b = a'; the declaration keyword does not change that." },
    { label: "Arrays are primitives, so b got an independent copy.", misconception: "Arrays are objects in JavaScript, not primitives. Assigning one copies its reference, so both names point at a single array — only primitives (number, string, boolean) copy by value." },
  ]}
/>

<Quiz
  id="06-variables-and-state-choice-6"
  lessonSlug="06-variables-and-state"
  lang="en"
  question="What is the difference between a program's 'state' and the 'mutations' that produced it?"
  choices={[
    { label: "State is the complete snapshot of all live variables' current values at one instant. A mutation is a single in-place change to one cell. State is the cumulative result of all prior mutations, not the list of mutations themselves.", correct: true },
    { label: "State is the full history of every assignment since startup.", misconception: "State is the current snapshot, not the history. Reaching score=10 by one big increment or ten small ones gives different histories but identical state — only the present values matter." },
    { label: "State is only the variables changed by the most recent assignment.", misconception: "State covers every live variable, including ones untouched for a long time. Their current values are still part of the program's condition right now." },
    { label: "Mutation and state are the same thing described two ways.", misconception: "They are distinct: state is the values held now; mutation is the act of changing one of those values in place. You can have unchanging state (only const) with no mutation at all." },
  ]}
/>

<Recap lang="en">
The through-line: a variable is a name bound to a cell, and the name and the value are different things. Assignment is a destructive store — 'gets', not 'equals'. Mutation changes a cell in place, and state is the snapshot of all current cell contents. The decisive split is what sits in the cell: a primitive value (copied on assignment, so changes do not propagate) or a reference to a heap object (copied as an address, creating aliases where one mutation is visible through every name). const seals which reference a cell holds, never the contents behind it.
</Recap>
