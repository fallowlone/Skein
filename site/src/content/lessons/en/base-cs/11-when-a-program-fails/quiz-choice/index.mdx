---
concepts:
- error-vs-exception
- stack-unwinding
- reading-a-trace
- undefined-behaviour
- silent-failure
- debugging-as-reasoning
estMin: 13
lang: en
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Control_flow_and_error_handling
- https://en.wikipedia.org/wiki/Undefined_behavior
- https://cacm.acm.org/research/modern-debugging/
status: ready
summary: "Multiple-choice synthesis across the failure unit: error vs exception, stack unwinding, reading a trace, undefined behaviour, and debugging as reasoning."
title: 'When a program fails: multiple-choice review'
track: base-cs
unit: 11-when-a-program-fails
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one is a small judgement call you make in front of a real failure — not a definition to recite, but a distinction to hold steady: condition versus mechanism, loud versus silent, reasoning versus guessing.
</Hook>

<Goal lang="en">
Confirm you can connect the four lessons into one picture: an error is a condition, an exception is the mechanism that unwinds the stack, the stack trace is the snapshot of that unwind, undefined behaviour is the dangerous silent case, and debugging is reasoning over a deterministic machine.
</Goal>

<Quiz
  id="11-when-a-program-fails-choice-1"
  lessonSlug="11-when-a-program-fails"
  lang="en"
  question="A program tries to open a file that does not exist. Which of these is the error, and which is the exception?"
  choices={[
    { label: "The error is the condition — the requested file is absent, a fact about the program's state. The exception is the mechanism the runtime uses in response: stop normal flow and unwind the stack searching for a handler.", correct: true },
    { label: "The error and the exception are the same thing — both mean the program is broken and must stop.", misconception: "They vary independently. The same missing-file condition can be raised as an exception in one program and handled with a plain check in another. A program can raise an exception, catch it, and continue completely normally — nothing is permanently broken." },
    { label: "The error is what the programmer's code detects; the exception is what the CPU detects in hardware.", misconception: "Both live at the runtime/program level. The split is condition versus mechanism, not software versus hardware: the error is the bad situation, the exception is the runtime's stack-unwinding response to it." },
    { label: "The exception is the condition and the error is the runtime's response to it.", misconception: "It is the other way round. The error is the condition (a fact); the exception is the response (a process of unwinding the stack to find a handler)." },
  ]}
/>

<Quiz
  id="11-when-a-program-fails-choice-2"
  lessonSlug="11-when-a-program-fails"
  lang="en"
  question="main calls a, a calls b, b raises an exception. Only main has a handler. How many frames does the runtime pop, and where does normal control flow resume?"
  choices={[
    { label: "Two frames are popped — b (no handler) then a (no handler) — unwinding stops at main, whose handler runs, and normal control flow resumes inside main.", correct: true },
    { label: "Three frames are popped and the program crashes.", misconception: "A crash only happens when unwinding reaches the bottom with no handler anywhere. main has a handler, so unwinding stops there and main's frame is never popped — only b and a are popped." },
    { label: "Zero frames are popped — main's handler catches it before any unwinding happens.", misconception: "The handler is in main, but the exception was raised in b. The runtime must unwind b and a first to reach the frame that actually has the handler." },
    { label: "One frame is popped — b is abandoned and a's caller main handles it directly.", misconception: "a sits between b and main and has no handler, so a's frame is also popped. Two frames (b, then a) are popped before unwinding reaches main." },
  ]}
/>

<Quiz
  id="11-when-a-program-fails-choice-3"
  lessonSlug="11-when-a-program-fails"
  lang="en"
  question="A four-line stack trace reads (top to bottom): divide, getAge, loadUser, main. A new engineer fixes main first. Why is that the wrong instinct?"
  choices={[
    { label: "The bottom line is the program entry — it appears in essentially every trace and is almost never the culprit. The throw site is the top line (divide); reading starts there, at that function and line number.", correct: true },
    { label: "main is correct to fix because it is the outermost call, so the bug must originate there.", misconception: "The outermost call is just where the program started. The exception surfaced at the top line, divide. main appears in the trace only because it is on the call chain, not because it is at fault." },
    { label: "The trace should be read bottom-up, so main being at the bottom means it threw last.", misconception: "A trace is read top-down. The top line is the throw site — where the exception was raised — and each line below is the caller of the line above it, down to the entry at the bottom." },
    { label: "Neither line matters — a trace just lists every function defined in the file.", misconception: "A trace is not the program's function list. It contains only the frames that were active on the call stack at the throw — the exact chain of calls that led to the exception." },
  ]}
/>

<Quiz
  id="11-when-a-program-fails-choice-4"
  lessonSlug="11-when-a-program-fails"
  lang="en"
  question="One program reads index 5 of a 3-element array and raises an 'index out of range' exception. Another reads the same out-of-range cell and returns whatever bits are there, with no error. Which is the more dangerous failure, and why?"
  choices={[
    { label: "The second is more dangerous. It is undefined behaviour: no exception, no trace, no stop — the program carries a garbage value forward, so the failure can survive testing, ship, and corrupt real data far from its cause.", correct: true },
    { label: "The first is more dangerous, because raising an exception crashes the program and crashes are the worst outcome.", misconception: "A loud, defined failure is the good case: it stops control flow and points a trace at the line, so you find and fix it fast. The silent failure is the dangerous one precisely because nothing flags it." },
    { label: "They are equally dangerous — both read the wrong cell, so the outcome is the same.", misconception: "Reading the wrong cell is the same bug, but the outcomes diverge completely: the defined error is caught and reported; the undefined-behaviour read is silent and propagates a wrong value with no trace." },
    { label: "The second is safer because it does not crash, so the program keeps serving users.", misconception: "Continuing to run with a garbage value is not safety — it is the danger. The program serves wrong results that look correct, which is worse than stopping loudly." },
  ]}
/>

<Quiz
  id="11-when-a-program-fails-choice-5"
  lessonSlug="11-when-a-program-fails"
  lang="en"
  question="In JavaScript, reading index 9 of a 3-element array gives undefined (not a crash), and then your code computes undefined + 1. What happens, and why does it resemble undefined behaviour even though each step is defined?"
  choices={[
    { label: "undefined + 1 is NaN, and NaN spreads silently through every later calculation with no exception raised. Each step is defined, but the wrong value propagates quietly — the same silent-failure pattern as true undefined behaviour.", correct: true },
    { label: "undefined + 1 raises an exception, so the runtime stops and prints a trace at that line.", misconception: "No exception is raised. undefined + 1 evaluates to NaN and the program keeps running. The danger is exactly that it does not stop." },
    { label: "undefined + 1 is 1, because undefined counts as zero in arithmetic.", misconception: "undefined does not coerce to 0 in addition; undefined + 1 is NaN. That NaN then contaminates every calculation it touches." },
    { label: "It is true undefined behaviour because JavaScript does not check array bounds.", misconception: "JavaScript does check bounds — an out-of-range read returns undefined, which is defined behaviour. The undefined-behaviour-style danger comes from using that undefined without noticing, not from the read itself." },
  ]}
/>

<Quiz
  id="11-when-a-program-fails-choice-6"
  lessonSlug="11-when-a-program-fails"
  lang="en"
  question="A program returns a wrong total. An engineer starts flipping comparisons and re-running until the number looks right. What is the principled objection, grounded in how the machine works?"
  choices={[
    { label: "The machine is deterministic — the same step on the same state always gives the same result — so the bug has a fixed, findable cause. Guessing can hide the symptom while the defect survives; reasoning (hypothesis, narrow the range, check state) locates the actual guilty step.", correct: true },
    { label: "Nothing is wrong with it — random changes until the symptom disappears is the fastest reliable way to remove a bug.", misconception: "Making the visible symptom vanish is not removing the bug. A guess can mask a defect that resurfaces with different input. Determinism means the cause is fixed and reasoning can find it." },
    { label: "The objection is that computers behave randomly, so you must try many fixes until one happens to work.", misconception: "Computers do not behave randomly — fetch-decode-execute is deterministic. The same state always gives the same result, which is exactly what makes the bug findable by reasoning instead of luck." },
    { label: "Flipping comparisons is fine as long as you keep the change that makes the test pass.", misconception: "A passing test after a blind change does not prove the cause was found; it may only hide one path to the symptom. You want to change a line only after reasoning has pinned the exact diverging step." },
  ]}
/>

<Recap lang="en">
The unit is one chain: an error is the condition, an exception is the mechanism that stops normal flow and unwinds the call stack frame by frame looking for a handler, and the stack trace is that stack frozen at the throw — read top-down, throw site first. Undefined behaviour is the dangerous case where no error is raised at all and a garbage value travels silently. And because the machine is deterministic, the response to any failure is reasoning, not guessing: form a testable hypothesis, narrow the range, read the trace, check state, then fix the step you have proven is wrong.
</Recap>
