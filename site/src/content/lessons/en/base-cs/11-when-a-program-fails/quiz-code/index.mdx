---
concepts:
- reading-a-trace
- classify-the-error
- out-of-bounds-read
- defined-vs-undefined
- throw-site-vs-cause
estMin: 14
lang: en
level: middle
order: 9
prereqs: []
slug: quiz-code
sources:
- https://v8.dev/docs/stack-trace-api
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Error/stack
- https://en.wikipedia.org/wiki/Undefined_behavior
status: ready
summary: "Read a stack trace and short snippets, then classify each failure: which line threw, whether it is a defined error or undefined behaviour, and where the real cause lives."
title: 'When a program fails: code and trace reading'
track: base-cs
unit: 11-when-a-program-fails
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
A failure is read in two places: the code that ran and the trace the runtime printed. For each snippet below, do what a working engineer does first — locate the throw site, name the kind of failure, and decide where the real cause lives.
</Hook>

<Goal lang="en">
Practise the reading loop the unit built toward: parse a stack trace top-down to find the throw site, classify a failure as a defined error or undefined behaviour, and separate where an exception surfaced from where its cause actually sits.
</Goal>

## Snippet 1 — read the trace

```
Error: Cannot read properties of undefined (reading 'name')
    at formatLabel (render.ts:14)
    at renderRow (render.ts:31)
    at renderTable (render.ts:48)
    at main (app.ts:9)
```

<Quiz
  id="11-when-a-program-fails-code-1"
  lessonSlug="11-when-a-program-fails"
  lang="en"
  question="Reading this trace top-down, which statement is correct?"
  choices={[
    { label: "The throw site is formatLabel at render.ts:14 — the top line — and it surfaced because something passed it a value that was undefined. renderRow, renderTable, and main are the call chain that led there; main at the bottom is just the entry point.", correct: true },
    { label: "main at app.ts:9 is the culprit because it is the bottom line of the trace.", misconception: "The bottom line is the program entry — it is in nearly every trace and is almost never the cause. The throw site is the top line, formatLabel at render.ts:14." },
    { label: "The trace lists every function in render.ts, in source order.", misconception: "A trace is not the file's function list. It is only the frames active on the call stack at the throw — the chain main to renderTable to renderRow to formatLabel." },
    { label: "renderRow threw, because it is in the middle of the chain.", misconception: "The throw site is always the top line. renderRow appears below formatLabel, meaning renderRow was waiting at line 31 on its call to formatLabel — it did not throw." },
  ]}
/>

## Snippet 2 — classify the failure

```js
function loadUser(record) {
  return record.profile.name.toUpperCase();
}
loadUser({ profile: null });  // profile is null, not an object
```

<Quiz
  id="11-when-a-program-fails-code-2"
  lessonSlug="11-when-a-program-fails"
  lang="en"
  question="record.profile is null, so the code reads .name from null. What kind of failure is this in JavaScript, and how will you find out about it?"
  choices={[
    { label: "A defined error. Reading a property of null is specified to raise an exception ('Cannot read properties of null'), so control flow stops and a stack trace points at this line. Loud, found, fixed.", correct: true },
    { label: "Undefined behaviour — the read returns garbage bits and the program keeps running silently.", misconception: "Reading a property of null is a defined error in JavaScript: it raises an exception. Undefined behaviour is the silent case with no error raised — this is the loud, reported case." },
    { label: "Nothing — JavaScript returns undefined for null.name, so the program continues normally.", misconception: "null.name does not return undefined; reading a property of null raises a TypeError. (Reading a missing property of an object would give undefined, but null is not an object.)" },
    { label: "It depends on the runtime — sometimes it crashes, sometimes it returns garbage.", misconception: "It is deterministic and defined: the same null read always raises the same exception. There is no garbage-bits outcome in a language that checks this case." },
  ]}
/>

## Snippet 3 — out of bounds, two languages

```
prices = [10, 20, 30]   // valid indices 0, 1, 2
read prices[5]          // index 5 — two past the end
```

<Quiz
  id="11-when-a-program-fails-code-3"
  lessonSlug="11-when-a-program-fails"
  lang="en"
  question="The same out-of-range read runs in a bounds-checked language and in a low-level unchecked language. How do the two outcomes differ?"
  choices={[
    { label: "The bounds-checked language raises a defined 'index out of range' exception — loud, with a trace. The unchecked language computes the address past the array and reads whatever leftover bits are there — undefined behaviour, silent, returning a meaningless value as if it were real.", correct: true },
    { label: "Both languages raise an exception, just with different messages.", misconception: "Only the bounds-checked language raises an exception. The unchecked low-level language does no check — it reads the out-of-range cell and returns garbage with no error at all." },
    { label: "Both return undefined, because an index past the end always yields undefined.", misconception: "undefined is JavaScript's specific defined result. A low-level unchecked language has no such guarantee — it reads whatever bits sit at the computed address." },
    { label: "Neither does anything, because index 5 simply does not exist.", misconception: "The index not being valid is the whole point. The bounds-checked language detects that and throws; the unchecked one ignores it and reads memory it should not." },
  ]}
/>

## Snippet 4 — throw site is not the cause

```js
function parseAge(text) {
  const n = Number(text);
  if (Number.isNaN(n)) throw new Error("not a number: " + text);
  return n;
}

function loadProfile(raw) {
  return { age: parseAge(raw.age) };   // raw.age came from upstream
}
// raw.age is "thirty" — never validated before reaching here
```

<Quiz
  id="11-when-a-program-fails-code-4"
  lessonSlug="11-when-a-program-fails"
  lang="en"
  question="The trace tops out at parseAge ('not a number: thirty'). A debugging hypothesis must separate where the error surfaced from where the cause lives. Which reading is right?"
  choices={[
    { label: "parseAge is the throw site, but it is behaving correctly — it validated and reported bad input. The cause is upstream: raw.age arrived as 'thirty' because it was never validated before loadProfile. Follow the chain past the throw site to find the real defect.", correct: true },
    { label: "parseAge is the bug — delete the throw so it stops failing.", misconception: "parseAge is doing its job: detecting bad input and reporting it loudly. Removing the throw would turn a loud, defined failure into a silent one — the worse outcome. The defect is the unvalidated 'thirty' upstream." },
    { label: "The cause is always the top line of the trace, so the fix belongs in parseAge.", misconception: "The top line is where the error surfaced, not always where the cause lives. Here a caller passed bad data; reasoning follows the chain to the upstream step where state first went wrong." },
    { label: "There is no bug — the program threw, so it worked as intended.", misconception: "Throwing on bad input is correct behaviour, but the bad input itself is the defect. The unit's point is to keep reasoning past the throw site to the step that first diverged." },
  ]}
/>

<Recap lang="en">
Every failure is read in code and trace: a trace is parsed top-down, the top line is the throw site and the bottom is just the entry; a property read on null is a defined error that raises loudly, while an unchecked out-of-bounds read is undefined behaviour that returns garbage silently; and the throw site is where the error surfaced, not always where the cause lives. Classify the failure, locate where it surfaced, then reason down the chain to the step that first went wrong.
</Recap>
