---
slug: 02-the-stack-trace
lang: en
track: base-cs
unit: 11-when-a-program-fails
order: 2
title: "The stack trace"
summary: "A stack trace is the snapshot of the call stack at the moment an exception is raised — the list of frames from where it threw down to the program entry. Read top-down, it shows what threw and through what chain of calls."
estMin: 22
status: ready
lessonType: coding
prereqs:
  - "02-the-call-stack"
  - "03-parameters-and-return"
  - "01-errors-vs-exceptions"
mathPrereqs: []
concepts: ["stack-trace", "trace-frame", "top-of-trace", "reading-a-trace", "exception-propagation"]
sources:
  - https://v8.dev/docs/stack-trace-api
  - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Error/stack
  - https://www.nand2tetris.org/
---

import Hook from "../../../../../../components/lesson/Hook.astro";
import Goal from "../../../../../../components/lesson/Goal.astro";
import Idea from "../../../../../../components/lesson/Idea.astro";
import Code from "../../../../../../components/lesson/Code.astro";
import Trace from "../../../../../../components/lesson/Trace.astro";
import Check from "../../../../../../components/lesson/Check.astro";
import Recap from "../../../../../../components/lesson/Recap.astro";
import Inset from "../../../../../../components/lesson/Inset.astro";
import AnnotatedCode from "../../../../../../components/algo/AnnotatedCode.astro";
import AlgoTrace from "../../../../../../components/algo/AlgoTrace.astro";
import PracticeSet from "../../../../../../components/math/PracticeSet.astro";
import Quiz from "../../../../../../components/pedagogy/Quiz.astro";

<Hook>
When a program crashes, it does not just say "something went wrong." It prints a block of
text — usually several lines, each naming a function and a line number. New programmers
often skip past it as noise. It is the opposite of noise: it is the single most useful
piece of information the runtime can give you.

That block is the **stack trace**. It is a photograph of the call stack — the exact stack
of frames from Unit 08 — taken at the instant the exception was raised. Once you can read
it, a crash stops being a mystery and becomes a precise statement: *this function threw,
and here is the exact chain of calls that led to it.*
</Hook>

<Goal lang="en">
After this lesson you can define a stack trace as the snapshot of the call stack at the
moment an exception is raised, identify the topmost frame as the throw site and the bottom
frame as the program entry, read a trace top-down to reconstruct the call chain, and trace
how an exception propagates frame by frame as the runtime unwinds.
</Goal>

<Idea lang="en">
Recall the **call stack** from Unit 08: a stack of **frames**, one per active function
call, the most recently called function on top, the program entry at the bottom. At any
instant the stack holds exactly the frames of all currently active calls.

A **stack trace** is what you get when you write that stack down. The moment an exception
is raised, the runtime records the call stack as it is right then: a list of frames, one
line per frame. Each line names the function and the line number where that function was
currently executing — for the top frame, the line that raised the exception; for every
frame below, the line where it called the function above it.

The trace is printed **top frame first**:

- **The top line** is the frame that raised the exception — the *throw site*. This is
  where the error actually surfaced.
- **Each line below** is the caller of the line above it, going down toward the program
  entry. The bottom line is the outermost call — typically `main` or the entry point.

So reading top-down, a trace answers two questions at once: *what threw* (top line) and
*through what chain of calls the program reached that point* (every line below). It is the
call stack from Unit 08, frozen and printed.

The program traced below has the call chain `main` → `loadUser` → `getAge` → `divide`.
`divide` raises an exception, and you will watch the trace build up as the runtime
unwinds.
</Idea>

<Code lang="en">

<AnnotatedCode
  code={`function divide(a: number, b: number): number {
  if (b === 0) throw new Error("divide by zero");
  return a / b;
}

function getAge(birthYears: number, people: number): number {
  return divide(birthYears, people);
}

function loadUser(): number {
  return getAge(60, 0);          // people = 0 — will cause the throw
}

function main(): void {
  loadUser();
}

main();`}
  annotations={[
    { line: 2, text: "Throw site: divide detects b === 0 and raises the exception. This is the TOP line of the trace." },
    { line: 7, text: "getAge called divide here — this line appears in the trace below divide." },
    { line: 11, text: "loadUser called getAge with people = 0 — this line appears below getAge." },
    { line: 15, text: "main called loadUser here — this line appears below loadUser." },
    { line: 18, text: "Program entry: main() called. The BOTTOM line of the trace." },
  ]}
  caption="Four nested functions. divide raises an exception when b is 0. The stack trace will list all four frames, divide on top."
/>

The stack trace printed when this program crashes looks like this — top frame first:

<AnnotatedCode
  code={`Error: divide by zero
    at divide (app.ts:2)
    at getAge (app.ts:7)
    at loadUser (app.ts:11)
    at main (app.ts:15)`}
  annotations={[
    { line: 1, text: "The exception's message — the error description passed to new Error(...)." },
    { line: 2, text: "TOP frame: divide, line 2 — the throw site. This is where the exception was raised." },
    { line: 3, text: "getAge, line 7 — getAge was waiting at line 7, the call to divide." },
    { line: 4, text: "loadUser, line 11 — loadUser was waiting at line 11, the call to getAge." },
    { line: 5, text: "BOTTOM frame: main, line 15 — the program entry's call to loadUser." },
  ]}
  caption="The crash output. Read top-down: divide threw, called from getAge, called from loadUser, called from main."
/>
</Code>

<Trace lang="en">
Step through the crash. The cells are the call stack, bottom-to-top (oldest at left).
Watch the frames push as calls nest, then the exception propagate as the runtime unwinds —
each popped frame is one line the trace records.

<AlgoTrace
  id="trace-stack-trace"
  lang="en"
  code={[
    "function divide(a, b) {",
    "  if (b === 0) throw new Error('divide by zero');",
    "  return a / b;",
    "}",
    "function getAge(birthYears, people) {",
    "  return divide(birthYears, people);",
    "}",
    "function loadUser() {",
    "  return getAge(60, 0);",
    "}",
    "function main() {",
    "  loadUser();",
    "}",
    "main();"
  ]}
  frames={[
    {
      caption: "main() called. main's frame pushed. The bottom of the stack.",
      line: 11,
      cells: [
        { value: "main", mark: "active" }
      ]
    },
    {
      caption: "Line 12: loadUser() called. loadUser's frame pushed on top.",
      line: 12,
      cells: [
        { value: "main", mark: "done" },
        { value: "loadUser", mark: "active" }
      ]
    },
    {
      caption: "Line 9: getAge(60, 0) called. getAge's frame pushed. people is 0.",
      line: 9,
      cells: [
        { value: "main", mark: "done" },
        { value: "loadUser", mark: "done" },
        { value: "getAge", mark: "active" }
      ]
    },
    {
      caption: "Line 6: divide(60, 0) called. divide's frame pushed. Four frames now active.",
      line: 6,
      cells: [
        { value: "main", mark: "done" },
        { value: "loadUser", mark: "done" },
        { value: "getAge", mark: "done" },
        { value: "divide", mark: "active" }
      ]
    },
    {
      caption: "Line 2: b === 0 is true. divide raises the exception. The trace is recorded NOW: this is the throw site, the top line.",
      line: 2,
      cells: [
        { value: "main", mark: "done" },
        { value: "loadUser", mark: "done" },
        { value: "getAge", mark: "done" },
        { value: "divide", mark: "target" }
      ]
    },
    {
      caption: "Unwinding: divide has no handler. divide's frame popped — recorded as trace line 'at divide'.",
      line: 6,
      cells: [
        { value: "main", mark: "done" },
        { value: "loadUser", mark: "done" },
        { value: "getAge", mark: "target" }
      ]
    },
    {
      caption: "Unwinding: getAge has no handler. getAge's frame popped — recorded as 'at getAge'.",
      line: 9,
      cells: [
        { value: "main", mark: "done" },
        { value: "loadUser", mark: "target" }
      ]
    },
    {
      caption: "Unwinding: loadUser has no handler. loadUser's frame popped — recorded as 'at loadUser'.",
      line: 12,
      cells: [
        { value: "main", mark: "target" }
      ]
    },
    {
      caption: "Unwinding: main has no handler. main's frame popped — recorded as 'at main'. Stack empty: the program crashes and prints the full trace.",
      line: 14,
      cells: []
    }
  ]}
/>
</Trace>

<Inset kind="why" lang="en">
**Why is the trace recorded at the throw, not at the crash?** Because the call stack is
*destroyed* by unwinding. By the time the program actually crashes, every frame has been
popped — the stack is empty. If the runtime waited until then to look, there would be
nothing left to describe. So it captures the snapshot the instant the exception is raised,
while all the frames are still present, and then prints that captured snapshot when the
unwind finishes with no handler.
</Inset>

<Inset kind="mistake" lang="en">
A common mistake is to read the trace bottom-up and blame `main`, because `main` is the
familiar name at the bottom. The bottom frame is just the program entry — it is in *every*
trace and almost never the culprit. The throw site is the **top** line. Start your reading
there: that function, at that line number, is where the error surfaced.
</Inset>

<PracticeSet
  id="the-stack-trace-practice"
  lessonSlug="02-the-stack-trace"
  lang="en"
  problems={[
    {
      prompt: "A stack trace has 4 lines: divide, getAge, loadUser, main (top to bottom). Which line number (counting the top line as 1) names the throw site — the function that raised the exception?",
      answer: 1,
      hint: "The top line of a trace is always the throw site — the frame that raised the exception. That is line 1.",
    },
    {
      prompt: "Same 4-line trace: divide, getAge, loadUser, main (top to bottom). Counting the top line as 1, which line names the program entry?",
      answer: 4,
      hint: "The bottom line of a trace is the program entry — the outermost call. In a 4-line trace that is line 4.",
    },
    {
      prompt: "main calls loadUser calls getAge calls divide. divide throws. The stack trace captured at the throw has how many frames?",
      answer: 4,
      hint: "The trace is the snapshot of the call stack at the throw. All four functions are active, so the trace has 4 frames: divide, getAge, loadUser, main.",
    },
    {
      prompt: "No function in the chain has a handler. The runtime unwinds. How many frames does it pop before the stack is empty and the program crashes?",
      answer: 4,
      hint: "With no handler anywhere, every frame is popped: divide, getAge, loadUser, main. 4 frames popped, then the trace is printed.",
    },
    {
      prompt: "In a trace, the line 'at getAge (app.ts:7)' sits directly below 'at divide'. Line 7 of getAge is which event — type 1 if it is where getAge called divide, 2 if it is where getAge raised its own exception.",
      answer: 1,
      hint: "Every frame below the top shows the line where that function called the function above it. getAge line 7 is the call to divide, not a throw.",
    },
  ]}
/>

<Check lang="en">
  <Quiz
    id="the-stack-trace-check"
    pieceSlug="02-the-stack-trace"
    lang="en"
    question="What is a stack trace, and how do you read it?"
    choices={[
      {
        label: "It is the snapshot of the call stack at the moment an exception is raised; read top-down, the top line is the throw site and each line below is the caller, down to the program entry at the bottom",
        correct: true,
      },
      {
        label: "It is a list of every function in the program, printed in the order the functions are defined in the source file",
        misconception: "A trace is not the program's function list. It is only the frames that were active on the call stack at the throw — the chain of calls that led to the exception. Functions not on that chain do not appear at all.",
      },
      {
        label: "It is read bottom-up: the bottom line is always the function that caused the crash",
        misconception: "The bottom line is the program entry — it is in every trace and is almost never the culprit. The throw site is the TOP line. Read top-down, starting from the function and line number on the first line.",
      },
    ]}
  />
</Check>

<Recap lang="en">
A **stack trace** is the call stack from Unit 08, frozen and written down at the instant
an exception is raised. It is a list of **frames**, one line each, naming a function and
the line it was executing. It is printed **top frame first**: the top line is the *throw
site* — the function and line where the exception was raised — and each line below is the
caller of the line above it, down to the **program entry** at the bottom. The runtime
captures this snapshot at the throw, while the frames still exist, because unwinding then
pops every frame and empties the stack. To read a trace, start at the top: that line tells
you exactly what threw and where; the lines below tell you the chain of calls that got the
program there.
</Recap>
