---
concepts:
- bit
- place-value
- encoding
- boolean-logic
- logic-gate
- functional-completeness
estMin: 13
lang: en
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.nand2tetris.org/
- https://en.wikipedia.org/wiki/UTF-8
status: ready
summary: "Multiple-choice synthesis across the unit: why two states win, place value, encodings, boolean completeness, and gates building arithmetic."
title: 'What a computer is: multiple-choice review'
track: base-cs
unit: 01-what-a-computer-is
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that span the whole unit — from why a transistor has two states, up to how those same two states become numbers, letters, colours, truth values, and finally arithmetic. Each one asks you to connect two ideas, not recite one definition.
</Hook>

<Goal lang="en">
Confirm you can trace the single thread that runs through the unit: two-state hardware to bits to place value to encodings to boolean logic to gates to arithmetic. If the connections hold, the unit's mental model is solid.
</Goal>

<Quiz
  id="01-what-a-computer-is-choice-1"
  lessonSlug="01-what-a-computer-is"
  lang="en"
  question="A junior engineer asks why chips do not use ten voltage levels (one per decimal digit) to pack more into each wire. What is the real reason hardware settled on two?"
  choices={[
    { label: "Two levels leave a wide voltage gap between 'low' and 'high', so ordinary electrical noise cannot push a signal across the gap and corrupt a bit. Ten cramped levels would flip under tiny fluctuations.", correct: true },
    { label: "Binary arithmetic is mathematically faster than decimal arithmetic, so two states compute quicker.", misconception: "Speed is not the reason; hardware could be built for decimal (ENIAC was). The decisive factor is reliability — a wide noise margin between just two levels." },
    { label: "Computers were invented before decimal numbers were widely understood.", misconception: "Decimal has been used for millennia, and early machines were decimal. Binary was a deliberate engineering choice for noise immunity, not a historical accident." },
    { label: "Two states use exactly half the power of ten states.", misconception: "Power is not the driver. The point is the size of the gap relative to noise: two levels maximise that margin, so reads stay reliable." },
  ]}
/>

<Quiz
  id="01-what-a-computer-is-choice-2"
  lessonSlug="01-what-a-computer-is"
  lang="en"
  question="A colleague says 'a 16-bit number can hold twice as much as an 8-bit number.' Why is this wrong, and what is the right statement?"
  choices={[
    { label: "Each added bit doubles the count of representable patterns, so 16 bits is not 2x but 2⁸ = 256x the range of 8 bits — 65,536 values versus 256.", correct: true },
    { label: "It is right: doubling the bit count doubles the values, so 16 bits holds twice the range of 8 bits.", misconception: "Adding bits multiplies the count, it does not add or double linearly. Going from 8 to 16 bits multiplies the range by 2⁸ = 256, not by 2." },
    { label: "Wrong, because 16 bits holds 16x the range of 8 bits — one factor per extra bit.", misconception: "Each extra bit multiplies by 2, not by adding a factor of 1. Eight extra bits means 2⁸ = 256x, not 16x." },
    { label: "Wrong, because the leftmost bit is a sign bit, so 16 bits really holds the same range as 8.", misconception: "Sign is a separate interpretation choice. For unsigned ranges, 16 bits holds 65,536 values — 256x the 8-bit range, regardless of sign." },
  ]}
/>

<Quiz
  id="01-what-a-computer-is-choice-3"
  lessonSlug="01-what-a-computer-is"
  lang="en"
  question="A teammate insists the byte 01000001 'is the letter A.' Correct the statement at the level this unit teaches."
  choices={[
    { label: "The byte is not inherently anything. 01000001 is the number 65 under a numeric reading, the letter 'A' under ASCII, and one channel of a dark colour under RGB. Meaning comes from the encoding the reader applies, agreed in advance.", correct: true },
    { label: "Correct — 01000001 always decodes to 'A' because that is its Unicode identity.", misconception: "Nothing about the bits forces the letter reading. The same byte is 65 as a number or a colour channel; only the chosen encoding fixes the meaning." },
    { label: "Wrong — that byte is actually the number 65, which is its one true meaning; 'A' is a display trick.", misconception: "65 is no more 'true' than 'A'. Both are valid interpretations under different encodings. Neither is privileged; the bits carry no meaning by themselves." },
    { label: "Correct, as long as the file is saved in UTF-8 rather than UTF-16.", misconception: "The point is deeper than UTF-8 vs UTF-16: a numeric reader or an image reader sees the same byte as a number or a colour. The encoding agreement, not just the text encoding, decides the meaning." },
  ]}
/>

<Quiz
  id="01-what-a-computer-is-choice-4"
  lessonSlug="01-what-a-computer-is"
  lang="en"
  question="Why is a truth table called the definition of a boolean operation, rather than just a summary of it?"
  choices={[
    { label: "A truth table lists the output for every possible input combination, so it pins down the operation completely — two operations are identical exactly when their truth tables match, with nothing left unspecified.", correct: true },
    { label: "Because the table shows the fastest hardware implementation of the operation.", misconception: "A truth table says nothing about implementation or speed; it only specifies input-to-output behaviour. That behaviour is the whole definition." },
    { label: "Because most operations have inputs that the table can only approximate.", misconception: "Boolean inputs are exhaustively 0 or 1, so the table covers every case exactly — no approximation. That completeness is precisely why it counts as the definition." },
    { label: "Because the table also encodes operator precedence between AND, OR, and NOT.", misconception: "Precedence is a separate parsing rule for expressions. A single operation's truth table just maps its own inputs to outputs — that mapping defines it." },
  ]}
/>

<Quiz
  id="01-what-a-computer-is-choice-5"
  lessonSlug="01-what-a-computer-is"
  lang="en"
  question="Someone claims XOR must be a hardware primitive because 'if statements need it.' Using what the unit teaches about completeness, how do you respond?"
  choices={[
    { label: "XOR needs no new primitive: AND, OR, NOT are functionally complete, so XOR = (A AND NOT B) OR (NOT A AND B). In fact NAND alone is complete — an entire computer can be built from one gate type.", correct: true },
    { label: "True — XOR cannot be expressed with AND, OR, NOT, so it must be its own primitive gate.", misconception: "XOR is built directly from AND, OR, NOT. Those three are functionally complete, so no extra primitive is required for XOR or any other boolean function." },
    { label: "True, because XOR has a different truth table from AND and OR, and different tables require different primitives.", misconception: "Having a distinct truth table does not require a distinct primitive. Disjunctive normal form builds any truth table from AND, OR, NOT alone." },
    { label: "Partly true: XOR can be built from AND and OR, but only if NOT is unavailable.", misconception: "It is the opposite — NOT is essential to the construction (to negate inputs per row). With AND, OR, NOT together, every boolean function including XOR is expressible." },
  ]}
/>

<Quiz
  id="01-what-a-computer-is-choice-6"
  lessonSlug="01-what-a-computer-is"
  lang="en"
  question="In a half-adder summing two bits, why is the sum bit wired from an XOR circuit and the carry bit from an AND gate?"
  choices={[
    { label: "Adding two bits gives sum 1 exactly when the inputs differ (0+1 or 1+0) — that is XOR — and a carry of 1 exactly when both are 1 (1+1 = binary 10) — that is AND. Each output matches one gate's truth table.", correct: true },
    { label: "Sum comes from AND and carry from OR, because addition is fundamentally an AND operation.", misconception: "AND is 1 only when both inputs are 1, which matches the carry, not the sum. The sum is 1 when inputs differ — that is XOR, not AND." },
    { label: "Sum comes from OR, since 'at least one bit set' means the sum is 1.", misconception: "For inputs 1 and 1, OR gives 1, but the correct sum bit is 0 (with carry 1). OR cannot produce the sum; XOR (true only when inputs differ) does." },
    { label: "Both outputs come from the same AND gate, read at two different times.", misconception: "Sum and carry are different functions of the same inputs, so they need different gates wired in parallel — XOR for sum, AND for carry — not one gate read twice." },
  ]}
/>

<Recap lang="en">
The unit is one chain: two-state hardware exists because a wide noise gap makes reads reliable; bits grouped by place value become numbers, where each added bit doubles the range; the same bits become letters, colours, or sound only through an agreed encoding; a bit read as a truth value feeds AND, OR, NOT, which are functionally complete; and those operations, realised as gates and wired into circuits, build a half-adder and ultimately all of arithmetic. Every link rests on the one below it.
</Recap>
