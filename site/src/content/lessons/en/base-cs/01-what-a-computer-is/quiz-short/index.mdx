---
concepts:
- bit
- place-value
- encoding
- truth-table
- functional-completeness
- half-adder
estMin: 13
lang: en
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.nand2tetris.org/
- https://en.wikipedia.org/wiki/Positional_notation
status: ready
summary: "Free-recall prompts across the unit — say the full answer from memory, then reveal the model answer and compare."
title: 'What a computer is: free-recall review'
track: base-cs
unit: 01-what-a-computer-is
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Recall beats re-reading. For each prompt, say or write a full answer from memory before you open the model answer. The effort of reconstructing the idea — not recognising it — is what locks it in.
</Hook>

<Goal lang="en">
Reconstruct the unit's spine without looking back: why two states, how place value turns bits into numbers, why bits need an encoding, why three boolean operations suffice, and how gates climb from logic to arithmetic.
</Goal>

<RetrievalDrawer
  client:visible
  id="01-what-a-computer-is-short-retrieval"
  lang="en"
  questions={[
    { q: "Why does hardware use exactly two states instead of ten, and what is a bit?", a: "A bit (binary digit) is the smallest unit of information: a single choice between two states — 0 or 1, off or on, low or high voltage. Hardware uses two states rather than ten because voltage is never perfectly stable: temperature, nearby signals, and supply variation cause small fluctuations. Two levels (say 0–1 V for low and 3–5 V for high) leave a wide gap that noise almost never crosses, so a read reliably recovers the intended value. Ten cramped levels would leave gaps so small that a tiny fluctuation flips a digit. The bigger the gap relative to the noise, the more reliable the read — so two states maximise the margin and binary won." },
    { q: "Explain place value in binary and why each added bit doubles the representable range.", a: "Binary uses the same positional idea as decimal, but with base 2: the rightmost place is worth 2⁰ = 1, then 2, 4, 8, 16, and so on — each place is twice the one to its right. A number is the sum of each 1-bit's place weight. Each extra bit doubles the count of patterns because the new bit can independently be 0 or 1, so every existing pattern spawns two: 1 bit gives 2 patterns, 2 bits give 4, 3 bits give 8. That is why going from 8 to 16 bits multiplies the range by 2⁸ = 256, not by 2." },
    { q: "What is an encoding, and why must it be agreed in advance? Give the byte 01000001 as an example.", a: "An encoding is a shared convention that assigns meaning to a bit pattern. Bits carry no inherent meaning — the same byte 01000001 is the number 65 under a numeric reading, the letter 'A' under ASCII, and one channel of a dark colour under RGB. Because a bit pattern has no self-describing label, the sender and receiver must agree on the encoding beforehand — via a file format, a header, a MIME type, an extension. Without that prior agreement the receiver is guessing, and a wrong guess produces mojibake, corrupted images, or distorted audio. ASCII maps 'A'=65, 'a'=97, '0'=48; Unicode extends this to every script and UTF-8 stores code points in 1–4 bytes, backward-compatible with ASCII." },
    { q: "What is a truth table, and why is it the definition of a boolean operation rather than just a summary?", a: "A truth table lists every possible input combination and the output the operation produces for each — two rows for one input, four for two inputs (2 × 2). It is the definition, not a summary, because boolean inputs are exhaustively 0 or 1, so the table specifies the operation's behaviour completely, leaving nothing unstated. Two operations are the same operation if and only if their truth tables are identical, which is why the table is the primary tool for proving boolean expressions equivalent: show they match on every row." },
    { q: "State what 'functionally complete' means for AND, OR, NOT, and sketch why it holds.", a: "Functionally complete means every possible boolean function — every truth table you can write, no matter how many inputs — can be expressed using only AND, OR, and NOT. The sketch is disjunctive normal form: for each row where the desired output is 1, write one AND expression that is true exactly for that row (using NOT to negate any input that is 0 in that row), then OR all those per-row expressions together. The result is true on exactly the 1-rows and false elsewhere, matching the target table. Stronger still, (AND, NOT), (OR, NOT), and even NAND alone are each complete — so an entire computer can be built from one gate type." },
    { q: "How does a half-adder turn gates into arithmetic, and which gate produces sum versus carry?", a: "A logic gate is hardware that performs one boolean operation; its output is a bit, the same type as its inputs, so gates compose freely into circuits. A half-adder adds two single bits and has two outputs. Adding A and B: the sum bit is 1 exactly when the inputs differ (0+1 or 1+0), which is XOR; the carry bit is 1 exactly when both are 1 (1+1 = binary 10), which is AND. So the half-adder is an XOR circuit for the sum and an AND gate for the carry, wired to the same inputs. A full-adder adds a carry-in (two half-adders plus an OR), and chaining full-adders adds multi-bit numbers — the arithmetic logic unit inside a CPU is gates all the way down." },
  ]}
/>

<Recap lang="en">
If you reconstructed each answer from memory, you hold the unit's spine: two states win on noise margin; place value turns bits into numbers and each bit doubles the range; an agreed encoding is what gives bits meaning; a truth table fully defines a boolean operation; AND, OR, NOT (or NAND alone) are functionally complete; and gates wired into a half-adder reproduce addition — the first rung from logic up to a working CPU.
</Recap>
