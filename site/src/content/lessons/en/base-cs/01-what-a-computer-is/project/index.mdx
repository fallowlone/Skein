---
concepts:
- place-value
- encoding
- truth-table
- functional-completeness
- half-adder
estMin: 210
lang: en
level: middle
order: 10
prereqs: []
slug: project
sources:
- https://www.nand2tetris.org/project01
- https://en.wikipedia.org/wiki/UTF-8
status: ready
summary: "Build a tiny 'bits to arithmetic' toolkit by hand and in code: convert numbers, decode an encoding, and simulate gates up to a half-adder."
title: 'What a computer is: build a bits-to-arithmetic toolkit'
track: base-cs
unit: 01-what-a-computer-is
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
You have read how bits become numbers, letters, truth values, and arithmetic. Now build the whole ladder yourself — small, by hand first and then in code — so the chain from a two-state switch to a working adder is something you have constructed, not just watched.
</Hook>

<Goal lang="en">
Turn the unit's mental model into a working toolkit: convert between binary and decimal, decode a byte under more than one encoding, and simulate boolean gates well enough to add two bits with a half-adder you built from AND, OR, and NOT.
</Goal>

<ProjectBrief
  id="01-what-a-computer-is-project"
  lessonSlug="01-what-a-computer-is"
  lang="en"
  objective="Build a small command-line toolkit (any language) that takes raw bits and walks them up the unit's ladder — number, then encoded character, then a gate-level half-adder — proving at each rung that you can produce the result by hand and confirm it in code."
  requirements={[
    "Binary to decimal and back: write two functions, one that reads an 8-bit string like '10110100' as a decimal number by summing place weights, and one that converts a decimal 0–255 back to an 8-bit string by repeated halving. Show they round-trip on at least five values.",
    "Decode a byte two ways: given an integer 0–127, print both its plain numeric value and its ASCII character. Verify the anchor codes 'A'=65, 'a'=97, '0'=48, and show that the same byte means two different things under the two readings.",
    "Gate primitives: implement and_gate, or_gate, and not_gate as functions on 0/1 inputs, each driven by its truth table. Print each gate's full truth table to prove the behaviour.",
    "Build XOR from your primitives: compose a function xor(a, b) using only and_gate, or_gate, not_gate — do not use a built-in XOR. Print its truth table and confirm it is 1 only when the inputs differ.",
    "Half-adder: write half_adder(a, b) that returns (sum, carry) using your xor for the sum and and_gate for the carry. Print its full input-to-output table for all four input pairs.",
    "Hand-check sheet: for at least three of the above (one conversion, one decode, the half-adder), write out the by-hand calculation in a short text file and confirm it matches your code's output.",
  ]}
  acceptance={[
    "binary-to-decimal and decimal-to-binary round-trip correctly on every test value (e.g. 0, 13, 90, 180, 255), shown as printed before/after pairs.",
    "The byte decoder prints, for example, 65 as both the number 65 and the character 'A', and a one-line note states that the meaning came from the chosen encoding, not the bits.",
    "All three gate truth tables and the XOR and half-adder tables print correctly and match the tables in the unit lessons exactly.",
    "The half-adder gives sum 0 / carry 1 for inputs 1 and 1, and the hand-check sheet shows the same result derived on paper.",
  ]}
  stretch={[
    "Extend the byte decoder to a third reading: treat three consecutive bytes as an RGB pixel and print the (R, G, B) triple, noting it is the same bits under yet another encoding.",
    "Chain two half-adders and an OR gate into a full-adder, then chain four full-adders to add two 4-bit numbers; verify a few sums against decimal arithmetic.",
    "Rebuild not_gate, and_gate, and or_gate using only a single nand(a, b) primitive, demonstrating that NAND alone is functionally complete.",
    "Add a tiny test harness that asserts every truth table and every round-trip automatically, so the whole toolkit passes or fails in one run.",
  ]}
/>

<Recap lang="en">
Building this toolkit makes the unit concrete: you converted bits to numbers by place value and back by halving, you saw one byte mean a number and a letter under different encodings, and you composed AND, OR, NOT into XOR and then a half-adder that adds two bits correctly. That is the entire arc — two-state switches, through encodings and boolean logic, up to arithmetic — assembled by your own hand.
</Recap>
