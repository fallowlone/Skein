---
slug: 03-primitive-types
lang: en
track: base-cs
unit: 05-values-and-types
order: 3
title: "Primitive types"
summary: "JavaScript/TypeScript has five primitive value categories you meet in everyday code: number, string, boolean, null, and undefined. The typeof operator reports which interpretation rule the runtime is applying to a value."
estMin: 22
status: ready
lessonType: coding
prereqs: ["02-types-interpret-bits"]
mathPrereqs: []
concepts: ["primitive-type", "js-number", "js-string", "js-boolean", "js-null", "js-undefined", "typeof"]
sources:
  - https://tc39.es/ecma262/#sec-ecmascript-language-types
  - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Data_structures
  - https://www.typescriptlang.org/docs/handbook/2/everyday-types.html
  - https://262.ecma-international.org/13.0/#sec-typeof-operator
---

import Hook from "../../../../../../components/lesson/Hook.astro";
import Goal from "../../../../../../components/lesson/Goal.astro";
import Idea from "../../../../../../components/lesson/Idea.astro";
import Code from "../../../../../../components/lesson/Code.astro";
import Trace from "../../../../../../components/lesson/Trace.astro";
import Check from "../../../../../../components/lesson/Check.astro";
import Recap from "../../../../../../components/lesson/Recap.astro";
import Inset from "../../../../../../components/lesson/Inset.astro";
import AnnotatedCode from "../../../../../../components/algo/AnnotatedCode.astro";
import AlgoTrace from "../../../../../../components/algo/AlgoTrace.astro";
import PracticeSet from "../../../../../../components/math/PracticeSet.astro";
import Quiz from "../../../../../../components/pedagogy/Quiz.astro";

<Hook>
You now know that a type is an interpretation rule for bits. The natural next question is:
what are the actual types available in the language you are learning?

In JavaScript and TypeScript, every value belongs to one of a small set of built-in
categories. The simplest category members — those that hold a single, indivisible piece of
data — are called **primitive types**. They are the bottom of the type hierarchy: numbers,
text characters, true/false decisions, "nothing here", "not yet set". You cannot break a
primitive value into smaller typed parts.

This lesson walks through each primitive type in JS/TS, grounds each in its bit-level
representation, and introduces `typeof` — the runtime's way of telling you which
interpretation rule it is applying to a value.
</Hook>

<Goal lang="en">
After this lesson you can name the five primitive type categories you meet in everyday JS/TS code, describe what
each represents at the bit level, predict the result of a `typeof` expression for any
primitive value, and explain what null and undefined mean as distinct absence values.
</Goal>

<Idea lang="en">
JavaScript and TypeScript define five primitive value categories you meet in everyday code:

1. **`number`** — any numeric value: integers, fractions, and the special IEEE 754
   values `Infinity`, `-Infinity`, and `NaN`. Internally stored as a 64-bit IEEE 754
   double-precision floating-point number (8 bytes). Even integers like `42` are stored
   this way — JS has no separate integer type.

2. **`string`** — a sequence of Unicode code points. In the V8 engine, a short string
   may be stored as a sequence of 16-bit UTF-16 code units in contiguous memory. A
   string value is not a single byte; it is a structured value with a length and a
   block of character data.

3. **`boolean`** — exactly two values: `true` and `false`. At the hardware level this
   maps to a 1-bit concept (though engines typically store it as a tagged word, not
   a single bit, for alignment reasons). The semantics: one of two mutually exclusive
   states.

4. **`null`** — the deliberate absence of any value. A programmer writes `null` to say
   "this slot holds nothing, intentionally." There is exactly one null value.

5. **`undefined`** — the unintentional or unset absence of a value. A declared variable
   that has not yet been assigned holds `undefined`. There is exactly one undefined
   value.

The TC39 spec actually defines **seven** primitive types: the five above plus `symbol`
(added in ES2015, for unique opaque identifiers) and `bigint` (added in ES2020, for
integers larger than `Number.MAX_SAFE_INTEGER`). This lesson covers the five you encounter
in everyday code; `symbol` and `bigint` serve specialised use cases you will meet later.

The **`typeof`** operator is how the runtime reports a value's type. Given any expression,
`typeof expr` returns a string naming the type. For primitives:

| Value | `typeof` result |
|---|---|
| `42` | `"number"` |
| `"hello"` | `"string"` |
| `true` | `"boolean"` |
| `undefined` | `"undefined"` |
| `null` | `"object"` ← historical quirk |

The `null` case is a well-known language quirk: `typeof null` returns `"object"` due to
a bug in the original JS implementation that could not be fixed for backwards
compatibility. In practice: `null` is a primitive, not an object.
</Idea>

<Code lang="en">

<AnnotatedCode
  code={`// 1. number — 64-bit IEEE 754 double in memory
const temperature = 36.6;
const count = 100;
const big = 9007199254740991;   // Number.MAX_SAFE_INTEGER

console.log(typeof temperature); // "number"
console.log(typeof count);       // "number"
console.log(typeof NaN);         // "number" — NaN is a numeric bit pattern

// 2. string — sequence of Unicode code points
const greeting = "hello";
const empty = "";

console.log(typeof greeting);    // "string"
console.log(greeting.length);    // 5 — length is part of the structured value

// 3. boolean — true or false only
const isReady = true;
const isOver = false;

console.log(typeof isReady);     // "boolean"

// 4. null — deliberate absence
const slot = null;
console.log(typeof slot);        // "object" — historical quirk, null IS a primitive
console.log(slot === null);      // true — test for null using ===, not typeof

// 5. undefined — unset / not-yet-assigned
let future;                      // declared but not assigned
console.log(typeof future);      // "undefined"
console.log(future === undefined);// true`}
  annotations={[
    { line: 2, text: "number: stores 36.6 as a 64-bit IEEE 754 double (8 bytes)" },
    { line: 7, text: "typeof returns the string name of the type" },
    { line: 8, text: "NaN is a special IEEE 754 bit pattern, still type 'number'" },
    { line: 12, text: "string: length property reports how many code points" },
    { line: 17, text: "boolean: exactly two values, true and false" },
    { line: 21, text: "null: typeof returns 'object' — known JS bug, not fixable for compat" },
    { line: 22, text: "Use === null to detect null, not typeof" },
    { line: 26, text: "undefined: variable declared but never assigned" },
  ]}
/>
</Code>

<Trace lang="en">
Watch how `typeof` responds to each kind of value. We trace a small program that assigns
five variables (one for each of the five everyday primitive types) and logs their `typeof` result.

<AlgoTrace
  id="trace-primitive-types"
  lang="en"
  code={[
    "const n = 42;",
    "const s = 'A';",
    "const b = true;",
    "const x = null;",
    "let u;",
    "console.log(typeof n, typeof s, typeof b, typeof x, typeof u);"
  ]}
  frames={[
    {
      caption: "Start: no variables exist yet.",
      line: 1,
      cells: []
    },
    {
      caption: "Line 1: n = 42. The runtime stores 42 as a 64-bit IEEE 754 double. typeof n = 'number'.",
      line: 1,
      cells: [
        { value: "42", mark: "active" },
      ]
    },
    {
      caption: "Line 2: s = 'A'. The runtime stores the string 'A'. typeof s = 'string'.",
      line: 2,
      cells: [
        { value: "42", mark: "done" },
        { value: "'A'", mark: "active" },
      ]
    },
    {
      caption: "Line 3: b = true. typeof b = 'boolean'.",
      line: 3,
      cells: [
        { value: "42", mark: "done" },
        { value: "'A'", mark: "done" },
        { value: "true", mark: "active" },
      ]
    },
    {
      caption: "Line 4: x = null. typeof x = 'object' (historical quirk). Value is null — deliberate absence.",
      line: 4,
      cells: [
        { value: "42", mark: "done" },
        { value: "'A'", mark: "done" },
        { value: "true", mark: "done" },
        { value: "null", mark: "active" },
      ]
    },
    {
      caption: "Line 5: u declared but not assigned. u = undefined. typeof u = 'undefined'.",
      line: 5,
      cells: [
        { value: "42", mark: "done" },
        { value: "'A'", mark: "done" },
        { value: "true", mark: "done" },
        { value: "null", mark: "done" },
        { value: "undef", mark: "active" },
      ]
    },
    {
      caption: "Line 6: console.log prints: 'number string boolean object undefined'. All five everyday primitives shown.",
      line: 6,
      cells: [
        { value: "42", mark: "done" },
        { value: "'A'", mark: "done" },
        { value: "true", mark: "done" },
        { value: "null", mark: "done" },
        { value: "undef", mark: "done" },
      ]
    }
  ]}
/>
</Trace>

<Inset kind="mistake" lang="en">
`null` and `undefined` look similar but mean different things. `null` is a deliberate
programmer choice: "I am explicitly saying this slot holds nothing." `undefined` is
automatic: "this variable was declared but never given a value, or this property does not
exist." Treat them as two distinct absence states. In TypeScript strict mode, the compiler
tracks them separately — a function that might return `null` is different from one that
might return `undefined`.
</Inset>

<PracticeSet
  id="primitive-types-practice"
  lessonSlug="03-primitive-types"
  lang="en"
  problems={[
    {
      prompt: "How many bytes does a JS number (IEEE 754 double) occupy in memory?",
      answer: 8,
      hint: "IEEE 754 double-precision is a 64-bit format. 64 bits ÷ 8 = 8 bytes.",
    },
    {
      prompt: "What does typeof 42 return? (Enter the ASCII code of the first character of the result string 'number', which starts with 'n'. ASCII 'n' = 110.)",
      answer: 110,
      hint: "typeof 42 returns the string 'number'. Its first character is 'n', ASCII code 110.",
    },
    {
      prompt: "A variable is declared with let x; and never assigned. What is typeof x in terms of a number — typeof 'undefined' has how many characters?",
      answer: 9,
      hint: "The string 'undefined' has 9 characters: u-n-d-e-f-i-n-e-d.",
    },
    {
      prompt: "How many distinct boolean values exist in JS? (true and false.)",
      answer: 2,
      hint: "Boolean has exactly two values: true and false.",
    },
    {
      prompt: "What does typeof null return as a string length? The string is 'object' — how many characters does 'object' have?",
      answer: 6,
      hint: "The string 'object' has 6 characters: o-b-j-e-c-t.",
    },
  ]}
/>

<Check lang="en">
  <Quiz
    id="primitive-types-check"
    pieceSlug="03-primitive-types"
    lang="en"
    question="Which of the following correctly describes what typeof null returns, and why?"
    choices={[
      {
        label: "'object' — a historical bug in the original JS implementation that cannot be changed for backwards compatibility",
        correct: true,
      },
      {
        label: "'null' — the runtime correctly identifies null as its own primitive type",
        misconception: "typeof null does not return 'null'. It returns 'object', which is a known quirk. To reliably test for null, use the strict equality check: x === null.",
      },
      {
        label: "'undefined' — null and undefined are the same absence value",
        misconception: "null and undefined are distinct absence values. null means deliberate absence (a programmer wrote null). undefined means uninitialised or not-yet-set. They are separate primitives with separate typeof results.",
      },
    ]}
  />
</Check>

<Recap lang="en">
JavaScript and TypeScript define five **primitive types you meet in everyday code**:
`number` (64-bit IEEE 754 double), `string` (Unicode sequence), `boolean` (true or false),
`null` (deliberate absence), and `undefined` (uninitialised or absent). (The full spec adds
`symbol` and `bigint`, bringing the total to seven — covered when you need them.) Each
primitive represents a specific interpretation rule applied to the bits the runtime stores.
The **`typeof`** operator returns the name of that rule as a string — with one historical
quirk: `typeof null` returns `"object"`, not `"null"`. Primitive values are indivisible:
you cannot read a sub-field out of `42` the way you can read a property out of an object.
They are the atomic building blocks from which all JS/TS values are made.
</Recap>
