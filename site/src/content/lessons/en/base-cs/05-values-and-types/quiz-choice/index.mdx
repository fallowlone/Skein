---
concepts:
- bits-have-no-meaning
- type-as-interpretation
- same-bits-different-types
- primitive-types
- static-vs-dynamic
- type-error
estMin: 13
lang: en
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://craftinginterpreters.com/values-and-types.html
- https://tc39.es/ecma262/#sec-ecmascript-language-types
- https://en.wikipedia.org/wiki/Data_type
status: ready
summary: "Multiple-choice synthesis across the unit: bits carry no meaning, a type is the reading rule, the same bits decode differently, and what type errors actually are."
title: 'Values and types: multiple-choice review'
track: base-cs
unit: 05-values-and-types
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. None of them is a definition to recite. Each asks you to apply the unit's one idea — bits mean nothing until a type says how to read them — to a concrete situation.
</Hook>

<Goal lang="en">
Confirm you can connect every thread of the unit: a value is raw bits, a type is the rule that interprets them, the same bits decode differently under different rules, and a type system exists to stop the wrong rule from being applied.
</Goal>

<Quiz
  id="05-values-and-types-choice-1"
  lessonSlug="05-values-and-types"
  lang="en"
  question="A memory cell holds the 8-bit pattern 01000001. Two programs read it: one prints 65, the other prints the letter 'A'. Which statement is correct?"
  choices={[
    { label: "Both are right. The bits are identical; each program applied a different type rule — u8 integer place-value gives 65, the ASCII table maps code 65 to 'A'. The reading, not the bits, decides the value.", correct: true },
    { label: "The byte really is 65; the program that printed 'A' has a bug.", misconception: "Neither value is the byte's 'true' meaning. The pattern 01000001 carries no inherent value. 65 and 'A' are both valid readings of the same bits under two different type rules." },
    { label: "Reading it as a character changed the bits, so the second program corrupted the cell.", misconception: "Applying a type is a read-only decode. It never alters the stored bits. Both programs read the same unchanged pattern; only their interpretation differed." },
    { label: "A cell can only hold one type, so the cell must secretly be either an integer or a character.", misconception: "Bits in a cell carry no permanent type label. The hardware stores eight off/on states. Which type rule to apply is the program's choice, tracked outside the cell." },
  ]}
/>

<Quiz
  id="05-values-and-types-choice-2"
  lessonSlug="05-values-and-types"
  lang="en"
  question="You must store a value and read it back correctly later. The starting address alone is not enough. What else must the reader know?"
  choices={[
    { label: "The size (how many bytes the value spans) and the interpretation (how to decode those bytes) — together, that is exactly what a type provides.", correct: true },
    { label: "Nothing — once you know the address, the memory hardware returns the value with its meaning attached.", misconception: "Memory returns raw bits, never meaning. The hardware knows addresses, not types. Size and interpretation live in the program, not in the cell." },
    { label: "Only the size; once you know it spans 4 bytes you can read the value.", misconception: "Size tells you which bytes to load but not what they mean. Four bytes could be a signed integer, a float, or four characters. Interpretation is a separate, equally necessary piece." },
    { label: "The variable's name, so the runtime can look up its meaning.", misconception: "A name is a label for the programmer; the machine has discarded it. What is needed at the bit level is size plus interpretation rule — the two halves of a type." },
  ]}
/>

<Quiz
  id="05-values-and-types-choice-3"
  lessonSlug="05-values-and-types"
  lang="en"
  question="In JavaScript, both 42 and 36.6 report typeof === 'number'. Why is there no separate integer type?"
  choices={[
    { label: "Every JS number is stored as a single 64-bit IEEE 754 double. Integers and fractions share that one 8-byte format, so 42 and 36.6 are the same type — there is no distinct integer category in everyday JS.", correct: true },
    { label: "JS rounds 36.6 to 37 internally, so really both are integers.", misconception: "JS does not round 36.6 to an integer. It stores the fraction in IEEE 754. Both values are the same type because they share the same 64-bit double format, not because one was rounded away." },
    { label: "typeof is too coarse to tell integers from floats, but internally they are different types.", misconception: "There is genuinely one number type. typeof reflects reality here: the runtime stores both 42 and 36.6 as doubles. The distinction integer-vs-float is not part of the JS primitive type set at all." },
    { label: "42 is an integer type and 36.6 is a float type; typeof merges them only for display.", misconception: "JS has a single number type. It does not maintain separate integer and float types under the hood and merge them for display — they are one type, one storage format." },
  ]}
/>

<Quiz
  id="05-values-and-types-choice-4"
  lessonSlug="05-values-and-types"
  lang="en"
  question="A teammate writes: 'null and undefined are the same thing — both mean empty.' What is the precise correction?"
  choices={[
    { label: "They are two distinct primitive absence values. null is deliberate absence (a programmer wrote it to mean 'nothing here, on purpose'); undefined is automatic absence (a variable declared but never assigned, or a missing property).", correct: true },
    { label: "They are identical; the language keeps two names only for historical reasons.", misconception: "They are genuinely distinct values with distinct meanings. null is an intentional 'nothing'; undefined is an unset/missing state the runtime supplies automatically. TypeScript strict mode even tracks them as separate types." },
    { label: "undefined is for numbers and null is for objects; they differ only by which type they belong to.", misconception: "Neither is tied to a specific value type. Any variable can be undefined (unassigned) and any slot can be deliberately null. The difference is intentional-vs-automatic absence, not number-vs-object." },
    { label: "null is undefined after garbage collection has run.", misconception: "Garbage collection does not turn one into the other. They are separate primitive values from the start: null is written deliberately, undefined arises from a missing assignment or property." },
  ]}
/>

<Quiz
  id="05-values-and-types-choice-5"
  lessonSlug="05-values-and-types"
  lang="en"
  question="What is the single underlying problem that every type system — static or dynamic — is built to catch?"
  choices={[
    { label: "Applying the wrong interpretation rule to a bit pattern: reading bits that were stored under one type as though they were a different type, producing wrong values, crashes, or memory corruption.", correct: true },
    { label: "Programs using too much memory because values occupy too many bytes.", misconception: "Memory footprint is a separate concern. A type system tracks which reading rule belongs to which value; it does not exist to limit how many bytes a value uses." },
    { label: "Code that is hard to read or poorly organised.", misconception: "Readability is a design and style goal. Type systems target a correctness problem: stopping bits from being decoded with the wrong rule. Clean code and type safety are different layers." },
    { label: "Slow programs that need optimisation.", misconception: "Performance is unrelated to the core purpose. The type system's job is to prevent misinterpretation of bits — applying the wrong rule — not to make code faster." },
  ]}
/>

<Quiz
  id="05-values-and-types-choice-6"
  lessonSlug="05-values-and-types"
  lang="en"
  question="The same type-mismatch bug can be caught at compile time or at run time. What is the practical difference between static (TypeScript) and dynamic (JavaScript) checking?"
  choices={[
    { label: "Static typing catches the mismatch before the program runs, eliminating the whole error class for type-correct code; dynamic typing catches it only when the bad line actually executes — so a rarely-run path can hide the bug for a long time.", correct: true },
    { label: "Static typing catches all bugs; dynamic typing catches none.", misconception: "Static typing catches only one class — type mismatches — and even then only before run time. Logic, algorithmic, and business-rule bugs slip past both. Dynamic typing also catches type errors, just later." },
    { label: "They catch different categories of bug entirely; there is no overlap.", misconception: "They target the same underlying problem — bits about to be read with the wrong rule. The difference is timing: compile time versus run time, not the category of error." },
    { label: "Dynamic typing is always safer because it checks at run time with full information.", misconception: "Run-time detection is late detection: the error surfaces only if and when the line runs, possibly in production. Earlier (static) detection removes the class before shipping, which is why TypeScript exists on top of JS." },
  ]}
/>

<Recap lang="en">
The unit's spine in one decision chain: a value is a fixed pattern of bits with no inherent meaning; a type supplies the two missing pieces — size and interpretation — that turn bits into a value; the same bits decode to different values under different type rules; JS exposes a small set of primitives (number as one 64-bit IEEE 754 double, string, boolean, and the two distinct absences null and undefined); and a type system exists to stop the wrong rule from being applied — statically before run time, or dynamically at the moment of execution.
</Recap>
