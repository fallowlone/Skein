---
concepts:
- one-to-one-translation
- abstraction
- compilation-vs-interpretation
- jit
- runtime-system
- source-to-running-program
estMin: 13
lang: en
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://en.wikipedia.org/wiki/Assembly_language
- https://developer.mozilla.org/en-US/docs/Glossary/Just_In_Time_Compilation
- https://en.wikipedia.org/wiki/Runtime_system
status: ready
summary: "Multiple-choice synthesis across the unit: the assembler's one-to-one rule, why high-level languages exist, compile vs interpret vs JIT tradeoffs, the runtime, and the full source-to-execution pipeline."
title: 'From machine code to a language: multiple-choice review'
track: base-cs
unit: 04-machine-code-to-language
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one asks you to connect ideas from different lessons — the assembler's one-to-one rule, what high-level languages buy you, how translation strategies differ, and what the runtime does — rather than to recite a single definition.
</Hook>

<Goal lang="en">
Confirm you can connect the rungs of the ladder: raw machine code, assembly, high-level languages, the translation strategies that bridge them, and the runtime and pipeline that carry your code all the way to the CPU.
</Goal>

<Quiz
  id="04-machine-code-to-language-choice-1"
  lessonSlug="04-machine-code-to-language"
  lang="en"
  question="You write one line of assembly, ADD R0 R1, and one line of TypeScript, total = price * qty + shipping. How many machine instructions does each produce, and what does that reveal?"
  choices={[
    { label: "The assembly line produces exactly one machine instruction (one-to-one is the defining property of assembly); the TypeScript line produces many (loads, a multiply, an add, a store). High-level languages trade one-to-one transparency for the productivity of one statement standing for many instructions.", correct: true },
    { label: "Both produce one machine instruction each — every source line becomes one instruction.", misconception: "Only assembly is one-to-one. A high-level statement typically expands to several machine instructions; the compiler chooses them. That many-to-one expansion is the whole productivity argument for high-level languages." },
    { label: "Both produce many instructions; assembly just hides the others.", misconception: "Assembly hides nothing — one mnemonic maps to exactly one machine instruction. There is no folding or expansion. The high-level line is the one that expands to many." },
    { label: "The TypeScript line produces one instruction because the CPU understands the whole expression at once.", misconception: "The CPU never understands a high-level expression. It runs only the individual machine instructions the compiler emitted; one expression becomes several of them." },
  ]}
/>

<Quiz
  id="04-machine-code-to-language-choice-2"
  lessonSlug="04-machine-code-to-language"
  lang="en"
  question="A team wants to ship the same program to both x86-64 Linux servers and ARM laptops. They consider distributing it as hand-written assembly. What goes wrong, and what fixes it?"
  choices={[
    { label: "Assembly is CPU-specific: x86-64 mnemonics encode x86-64 opcodes and will not assemble or run on ARM. A high-level language fixes it — the same source is translated by a separate compiler or interpreter for each target CPU, so the source is portable even though each binary is not.", correct: true },
    { label: "Nothing — assembly runs on any CPU because it is human-readable text.", misconception: "Human-readable text is not portable execution. Each CPU family has its own instruction set and encodings; x86-64 assembly must be rewritten in ARM assembly to run on ARM." },
    { label: "It works if they compile the assembly to bytecode first.", misconception: "Assembly is already CPU-specific machine-level text; there is no portable bytecode step in plain assembly. Portability comes from writing in a high-level language whose translator targets each CPU." },
    { label: "Assembly is fine; only the source text needs translating, not the instructions.", misconception: "The instructions are exactly the problem — assembly mnemonics name one CPU's opcodes. The portable layer is a high-level language, not assembly." },
  ]}
/>

<Quiz
  id="04-machine-code-to-language-choice-3"
  lessonSlug="04-machine-code-to-language"
  lang="en"
  question="A CPU-bound batch job runs for hours. The team can ship it as compiled C or as an interpreted Python script. Ignoring development effort, which runs the computation faster at run time, and why?"
  choices={[
    { label: "The compiled C version. The compiler translated and optimised the whole program ahead of time, so the CPU runs pre-built machine code with no per-statement translation overhead. The interpreter re-reads, decides, and executes each statement at run time, paying that overhead on every operation.", correct: true },
    { label: "The Python version, because interpreters skip the slow compile step.", misconception: "Skipping the compile step helps development speed, not run-time speed. At run time the interpreter pays translation overhead on every statement, so CPU-bound work runs slower than pre-compiled machine code." },
    { label: "They run at the same speed because both end up as machine code on the same CPU.", misconception: "Only the C program is pre-translated to optimised machine code. A pure interpreter never emits machine code for your program — it carries out each statement with its own code at run time, which is slower for CPU-bound loops." },
    { label: "Python, because it is a high-level language and high-level languages are always faster.", misconception: "Language level does not set run-time speed; the translation strategy does. Both are high-level relative to assembly, but compiled-ahead-of-time C runs the CPU-bound loop faster than an interpreter." },
  ]}
/>

<Quiz
  id="04-machine-code-to-language-choice-4"
  lessonSlug="04-machine-code-to-language"
  lang="en"
  question="A Node.js service is slow for the first few seconds under load, then speeds up and stays fast. Which mechanism explains this, and is it a bug?"
  choices={[
    { label: "JIT warm-up. V8 starts by interpreting; once a function is called enough to become a hot spot, the JIT compiles it to native machine code. Peak speed arrives after warm-up. It is expected behaviour, not a bug — JIT trades a short warm-up for near-compiled steady-state speed.", correct: true },
    { label: "A memory leak that the garbage collector eventually cleans up, restoring speed.", misconception: "A leak makes a service slower over time, not faster. The speed-up-then-stable shape is the JIT warm-up curve: hot functions get compiled to native code after enough calls." },
    { label: "The interpreter caches your source file to disk after the first run.", misconception: "Caching source text would not change the run-time speed of the computation. What changes is that the JIT compiles hot functions to native machine code at run time after warm-up." },
    { label: "The OS loader is still copying the binary into memory during the slow period.", misconception: "Loading happens once at process start in well under a second. The multi-second ramp is JIT warm-up: hot code paths being compiled to native code while the program runs." },
  ]}
/>

<Quiz
  id="04-machine-code-to-language-choice-5"
  lessonSlug="04-machine-code-to-language"
  lang="en"
  question="A junior says 'the runtime is just another name for the operating system.' Why is that wrong?"
  choices={[
    { label: "The OS provides generic services — processes, memory pages, file handles, system calls — and knows nothing about a language's types or object model. The runtime is the language-specific layer on top: garbage collector, call-stack and calling conventions, standard library, and the interpreter or VM. Python's and Java's runtimes differ even on the same OS.", correct: true },
    { label: "It is correct — every language's runtime is the OS kernel.", misconception: "The runtime sits between your code and the OS; it uses OS services but adds language-specific services (GC, standard library, VM) the kernel knows nothing about. Two languages on one OS have completely different runtimes." },
    { label: "Wrong because compiled languages have no runtime, only interpreted ones do.", misconception: "Even compiled C has a runtime: libc, program startup that sets up the stack and calls main, and exit cleanup. The runtime is thinner in C than in Python, but it is always present." },
    { label: "Wrong because the runtime is the code you write, not infrastructure.", misconception: "Your code runs at run time, but 'the runtime' as a noun means the support infrastructure shipped by the language implementation — GC, standard library, VM — not your own source." },
  ]}
/>

<Quiz
  id="04-machine-code-to-language-choice-6"
  lessonSlug="04-machine-code-to-language"
  lang="en"
  question="You click Run on a C program. Which ordering of the pipeline stages is correct?"
  choices={[
    { label: "Source text → translation (compile to object files) → linking (resolve library symbols into one executable) → loading (OS creates a process, copies bytes into memory, sets the program counter) → runtime startup → the CPU's fetch-decode-execute loop.", correct: true },
    { label: "Loading → translation → linking → fetch-decode-execute → runtime startup.", misconception: "Loading cannot come first — there is nothing to load until the source has been compiled and linked into an executable. Translation and linking precede loading; runtime startup precedes your code; fetch-decode-execute is the CPU running it." },
    { label: "Translation → loading → linking → runtime startup → fetch-decode-execute.", misconception: "Linking happens before loading, not after. The linker combines object files into one executable; only then can the OS loader read that executable from disk and place it in memory." },
    { label: "Linking → translation → loading → fetch-decode-execute → runtime startup.", misconception: "You cannot link before translating — the linker combines the object files the compiler produced, so translation must come first. And runtime startup runs before your code, not after the CPU loop." },
  ]}
/>

<Recap lang="en">
The unit is one ladder. Assembly names machine code one instruction at a time; high-level languages let one statement stand for many instructions and stay portable across CPUs; compilers translate the whole program ahead of time while interpreters translate statement by statement at run time, and JIT mixes both by compiling hot spots on the fly; the runtime (GC, call stack, standard library, VM) supports your code throughout; and the full pipeline — source → translate → link → load → runtime startup → fetch-decode-execute — carries every program to the CPU, which never knows what language it came from.
</Recap>
