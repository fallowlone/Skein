---
concepts:
- one-to-one-translation
- many-to-one-expansion
- compilation-vs-interpretation
- bytecode
- jit
estMin: 14
lang: en
level: middle
order: 9
prereqs: []
slug: quiz-code
sources:
- https://en.wikipedia.org/wiki/Assembly_language
- https://developer.mozilla.org/en-US/docs/Glossary/Just_In_Time_Compilation
status: ready
summary: Read assembly and high-level snippets, trace a source-to-machine-code mapping, and tell compilation from interpretation from JIT by what each one produces and when.
title: 'From machine code to a language: code and mapping reading'
track: base-cs
unit: 04-machine-code-to-language
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
The clearest way to tell these layers apart is to look at real code and ask: how many machine instructions does this become, and when does the translation happen? Read each snippet and pick the answer a careful engineer would give.
</Hook>

<Goal lang="en">
Practise reading the mapping between layers: assembly to machine code (one-to-one), a high-level statement to many instructions, and the difference compile, interpret, and JIT make to what is produced and when.
</Goal>

## Snippet 1 — assembly to machine code

```
        LOAD  R0, 200    ; opcode 00, R0=0, operand 200
        LOAD  R1, 201    ; opcode 00, R1=1, operand 201
        ADD   R0, R1     ; opcode 10
        STORE 202, R0    ; opcode 01, operand 202
```

<Quiz
  id="04-machine-code-to-language-code-1"
  lessonSlug="04-machine-code-to-language"
  lang="en"
  question="On the toy CPU each instruction is 2 bytes. How many machine instructions and how many bytes does the assembler emit for these four lines, and why?"
  choices={[
    { label: "Four machine instructions, 8 bytes. Assembly is one-to-one: each mnemonic line becomes exactly one machine instruction, so four lines become four instructions, two bytes each.", correct: true },
    { label: "More than four instructions, because ADD must also reload its operands.", misconception: "The assembler does not add hidden instructions. One-to-one means each line is exactly one machine instruction; the ADD line is a single ADD instruction, with the operands already in R0 and R1 from the LOADs." },
    { label: "Fewer than four, because the assembler optimises the LOADs together.", misconception: "An assembler does not optimise or fold instructions — that is a compiler's job. The assembler substitutes mnemonic for opcode one-to-one, so four lines stay four instructions." },
    { label: "Four instructions but only 4 bytes, because mnemonics are shorter than machine code.", misconception: "Byte count is set by the instruction encoding, not the length of the mnemonic text. Each instruction is 2 bytes here, so four instructions are 8 bytes." },
  ]}
/>

## Snippet 2 — one high-level statement

```ts
const total = price * quantity + shipping;
```

<Quiz
  id="04-machine-code-to-language-code-2"
  lessonSlug="04-machine-code-to-language"
  lang="en"
  question="The three variables are already in memory. Roughly how many machine instructions does this single TypeScript line become, and who decides which ones?"
  choices={[
    { label: "Several — load price, load quantity, multiply, load shipping, add, store the result — and the compiler decides which instructions, registers, and addresses to use. One high-level statement maps to many machine instructions; that many-to-one expansion is the productivity win.", correct: true },
    { label: "Exactly one, because it is one line of source.", misconception: "Source lines are not machine instructions. High-level statements are many-to-one: this expression becomes loads, a multiply, an add, and a store. Only assembly is one-to-one." },
    { label: "Several, but the programmer must choose the registers and addresses.", misconception: "The programmer chose none of them — that is the abstraction. The compiler selects the instructions, allocates registers, and resolves addresses; you wrote only the intent." },
    { label: "None — the CPU evaluates the whole expression in one step.", misconception: "The CPU has no instruction for an arbitrary expression. It runs the individual loads, multiply, add, and store the compiler emitted." },
  ]}
/>

## Snippet 3 — three ways to run the same program

```
A)  gcc add.c -o add   &&   ./add          # produces a native binary, then runs it
B)  python3 add.py                          # interpreter reads and runs the source
C)  node add.js                             # interpret first, JIT-compile hot functions
```

<Quiz
  id="04-machine-code-to-language-code-3"
  lessonSlug="04-machine-code-to-language"
  lang="en"
  question="Which statement correctly distinguishes A, B, and C by what gets produced and when?"
  choices={[
    { label: "A compiles the whole program to a native machine-code binary ahead of time, then runs it with no per-statement translation. B never produces a machine-code binary for your program — the interpreter carries out each statement at run time. C starts by interpreting and JIT-compiles hot functions to native code while running.", correct: true },
    { label: "All three produce a native binary on disk before running.", misconception: "Only A writes a native binary ahead of time. The interpreter in B produces no machine-code binary for your program; C produces native code only at run time, in memory, for hot functions." },
    { label: "B and C are identical — both just interpret the source.", misconception: "C is not a pure interpreter. It interprets first, then JIT-compiles frequently executed functions to native machine code at run time, reaching near-compiled speed after warm-up." },
    { label: "A is slowest because it must compile every time you run it.", misconception: "A compiles once; ./add then runs the existing binary with zero translation overhead. The interpreted path B is the one that pays translation cost on every run and every statement." },
  ]}
/>

## Snippet 4 — bytecode in between

```
javac App.java     ->  App.class   (bytecode, not native machine code)
java App           ->  the JVM loads App.class and runs the bytecode
```

<Quiz
  id="04-machine-code-to-language-code-4"
  lessonSlug="04-machine-code-to-language"
  lang="en"
  question="What is App.class, and why ship bytecode instead of either source text or a native binary?"
  choices={[
    { label: "Bytecode — a compact, portable intermediate format for the JVM, not native machine code. It separates portability (the same .class runs on any machine with a JVM) from performance (the JVM interprets or JIT-compiles it to native code at run time), so it is both portable and fast to start.", correct: true },
    { label: "Native x86-64 machine code, ready for the CPU to run directly.", misconception: "Bytecode is not native machine code — the CPU cannot run it directly. The JVM reads the bytecode and interprets or JIT-compiles it to native code. That indirection is what makes the same .class portable across CPUs." },
    { label: "Java source text in a compressed file.", misconception: "It is not source text; javac has already translated the source into bytecode, a compact binary for the JVM. The original Java text is not what the JVM loads." },
    { label: "A binary that only runs if the user installs the same CPU you compiled on.", misconception: "The opposite — bytecode is CPU-independent. Any machine with a JVM can run the same .class; the JVM bridges to that machine's native code." },
  ]}
/>

<Recap lang="en">
Reading the layers comes down to two questions. How many machine instructions? Assembly is one-to-one; a high-level statement is many-to-one, and the compiler picks the instructions. When does translation happen, and what is produced? A compiler emits a native binary ahead of time; a pure interpreter emits nothing for your program and translates each statement at run time; a JIT interprets first and compiles hot functions to native code on the fly; and bytecode is a portable middle layer the VM interprets or JIT-compiles. Match the snippet to the mechanism and the rest follows.
</Recap>
