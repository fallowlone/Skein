---
concepts:
- program-counter
- conditional-jump
- flags-register
- fall-through
- backward-jump
- infinite-loop
estMin: 13
lang: en
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://en.wikipedia.org/wiki/Control_flow
- https://www.cs.cmu.edu/afs/cs/academic/class/15213-f15/www/lectures/05-machine-basics.pdf
- https://www.nand2tetris.org/
status: ready
summary: "Multiple-choice synthesis across the control-flow unit: the program counter, conditional jumps and flags, fall-through, backward jumps, and why loops repeat or run forever."
title: 'Control flow: multiple-choice review'
track: base-cs
unit: 07-control-flow
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. None asks for a definition. Each asks you to reason about what the program counter does next — which is the single question every branch and every loop reduces to.
</Hook>

<Goal lang="en">
Confirm you can connect the four lessons into one model: the program counter drives flow, a compare sets flags, a conditional jump reads one flag bit, a backward jump repeats the body, and an if/else is just two destinations for the PC.
</Goal>

<Quiz
  id="07-control-flow-choice-1"
  lessonSlug="07-control-flow"
  lang="en"
  question="Two running machines hold identical instruction bytes at identical addresses, yet they execute completely different sequences of instructions. How is that possible?"
  choices={[
    { label: "Execution flow is set by the value the program counter holds at each fetch, not by the bytes. Steer the PC differently — different jump targets taken — and the same bytes run in a different order.", correct: true },
    { label: "Impossible: identical bytes at identical addresses always run in the same order.", misconception: "The bytes are fixed and inert; the flow is dynamic. The PC decides which byte is fetched next, so the same program can run different flows depending on which jumps are taken." },
    { label: "The CPU re-reads the source code, and the two machines have different source files.", misconception: "The CPU never sees source code — only bytes in memory. Both machines run the same bytes; the difference is which addresses the PC visits." },
    { label: "The general-purpose registers R0 and R1 hold the next instruction address.", misconception: "General-purpose registers hold data being computed. Only the program counter holds the address of the next instruction to fetch." },
  ]}
/>

<Quiz
  id="07-control-flow-choice-2"
  lessonSlug="07-control-flow"
  lang="en"
  question="A CMP R0, R1 executes, then a JNE (jump if not equal) is reached. Which single piece of state does the JNE actually inspect to decide jump-or-fall-through?"
  choices={[
    { label: "The Z (zero) flag set by the CMP. JNE jumps when Z=0 (the subtraction was non-zero, so the values differ) and falls through when Z=1.", correct: true },
    { label: "It re-subtracts R0 and R1 itself to recompute whether they are equal.", misconception: "The conditional jump does no arithmetic. The comparison already happened in CMP; the flag is its residue. JNE only reads a flag bit." },
    { label: "The current values of R0 and R1 directly.", misconception: "By the time JNE runs it inspects the flags register, not the operands. CMP recorded the relationship into the Z flag; the operand values are not re-read." },
    { label: "The program counter's low bit.", misconception: "The PC holds the address to fetch, not a comparison result. The decision comes from a flag (here Z) that CMP set." },
  ]}
/>

<Quiz
  id="07-control-flow-choice-3"
  lessonSlug="07-control-flow"
  lang="en"
  question="An if/else compiles to: CMP; conditional-jump-to-ELSE; if-true block; unconditional JUMP to END; ELSE: if-false block; END. Why is the unconditional JUMP after the if-true block necessary?"
  choices={[
    { label: "Without it, after the if-true block finishes the PC would advance straight into the else block and run both. The JUMP skips the else block to END.", correct: true },
    { label: "It is decorative; removing it changes nothing because the else block only runs when the condition is false.", misconception: "Fall-through is mechanical: the PC keeps advancing into the next bytes in memory, which are the else block. Without the skip, the if-true path also executes the else block." },
    { label: "It resets the program counter to 0 to restart the function.", misconception: "An unconditional JUMP here targets END, the instruction after the else block — not address 0. Its job is to skip the else block, not restart." },
    { label: "It re-runs the CMP so the else block can re-check the condition.", misconception: "The else block is reached only via the conditional jump when the condition is false. The trailing JUMP exists to skip the else block on the true path, not to re-test anything." },
  ]}
/>

<Quiz
  id="07-control-flow-choice-4"
  lessonSlug="07-control-flow"
  lang="en"
  question="At the machine level, what is the only structural difference between a compiled while loop and a compiled for loop?"
  choices={[
    { label: "None that the CPU can detect. A for loop adds an init block before the loop top and an update block just before the backward jump, but the test-then-body-then-backward-jump pattern is identical.", correct: true },
    { label: "A for loop uses a dedicated hardware loop-counter register; a while loop does not.", misconception: "General-purpose CPUs have no dedicated loop counter in the fetch-decode-execute cycle. Both loops use an ordinary register decremented or incremented by a normal instruction." },
    { label: "A while loop tests at the bottom, a for loop tests at the top.", misconception: "Both test at the top, so the body can run zero times. Testing at the bottom (body runs at least once) is a do-while, not a for loop." },
    { label: "A for loop is compiled to a single special FOR opcode.", misconception: "There is no FOR opcode. for is syntactic sugar that compiles to the same backward-jump structure as while." },
  ]}
/>

<Quiz
  id="07-control-flow-choice-5"
  lessonSlug="07-control-flow"
  lang="en"
  question="A countdown loop decrements a counter and should exit when it hits 0, but it never exits and pins the CPU at 100%. Which explanation is consistent with how the machine actually runs the loop?"
  choices={[
    { label: "The loop body never changes the value the test compares, so the exit flag never flips. The backward jump fires every pass and the PC keeps returning to the top.", correct: true },
    { label: "The CPU lost count of how many times it had already looped.", misconception: "The CPU counts nothing. It re-tests the condition each pass and makes one binary jump/fall-through decision; iteration count is an emergent property of the condition changing, not something the CPU tracks." },
    { label: "The loop body's instruction bytes got copied back to the top of memory each pass.", misconception: "Instructions never move. The bytes stay at fixed addresses; only the PC is rewound by the backward jump, so the same fixed bytes re-execute." },
    { label: "Infinite loops are a hardware fault the CPU should have detected.", misconception: "An infinite loop is the CPU doing exactly what the program says. It cannot tell a deliberate while(true) from a bug; only the OS or an interrupt stops it." },
  ]}
/>

<Quiz
  id="07-control-flow-choice-6"
  lessonSlug="07-control-flow"
  lang="en"
  question="In sumPositives over [3, -1, 4], the inner if skips the negative element. When nums[i] is non-positive, what does the machine do, and does the loop end early?"
  choices={[
    { label: "A conditional jump skips over sum += nums[i]; the i++ runs anyway because it sits outside the if; then the unconditional backward jump returns to the while test. The loop does not end early — only i reaching length ends it.", correct: true },
    { label: "The loop terminates as soon as a non-positive element is found.", misconception: "The while test checks i against the array length, not the sign of the element. A negative value only skips the inner if body; the loop keeps going." },
    { label: "i++ is skipped, so the same element is re-tested forever.", misconception: "i++ is outside the if block and always runs. Skipping it would re-test the same element and loop forever — but that is not what the code does." },
    { label: "Both the if-true and if-false paths run for the negative element.", misconception: "A conditional jump picks exactly one path from a single flag bit. The negative element takes the skip path only." },
  ]}
/>

<Recap lang="en">
The whole unit is one through-line: the program counter decides what runs next, and only a jump can change it. A compare sets a flag; a conditional jump reads that one flag and either jumps or falls through; an if/else is two PC destinations with a skip-JUMP guarding the boundary; a loop is a backward jump whose conditional test at the top decides whether to fall into the body again; and a loop runs forever when the tested value never changes. Every branch and loop you will ever read reduces to one question: where does the PC point next, and why.
</Recap>
