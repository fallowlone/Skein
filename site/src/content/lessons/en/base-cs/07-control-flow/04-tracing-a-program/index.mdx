---
concepts:
- program-trace
- combined-if-while
- loop-variable-state
- step-by-step-execution
estMin: 25
lang: en
lessonType: coding
mathPrereqs: []
order: 4
prereqs:
- 02-fetch-decode-execute
- 01-what-flow-means
- 02-conditionals-as-branches
- 03-loops-as-repeated-jumps
- 01-a-variable-is-a-named-cell
slug: 04-tracing-a-program
sources:
- https://www.nand2tetris.org/
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/while
- https://www.cs.cmu.edu/afs/cs/academic/class/15213-f15/www/lectures/05-machine-basics.pdf
status: ready
summary: Step through a TypeScript program that combines a conditional and a loop.
  Follow the program counter and variable state on each iteration to see exactly how
  if and while behave together at the instruction level.
title: Tracing a program
track: base-cs
unit: 07-control-flow
---



import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
You now know that an `if` is a conditional jump and a `while` is a backward jump. But
reading about them separately is different from watching them work together in a real
program, step by step, with the variable state changing in front of you.

This lesson does exactly that. You will read a short TypeScript function that loops over a
small array and adds up only the positive numbers. Then you will trace it frame by frame —
following the program counter and the value of every variable on each line — until the
function returns its result.

After this trace you will not just know what the code *says*; you will know what the CPU
*does*, one instruction at a time.
</Hook>

<Goal lang="en">
After this lesson you can read a program that combines a loop and a conditional, predict
each step of execution by tracking the program counter and variable values, explain how
the loop condition and the if-condition interact, and identify the exact frame at which
the loop exits.
</Goal>

<Idea lang="en">
The program to trace is a sum-of-positives function over a fixed array. It has three
control-flow moments:

1. **While test** — evaluate `i < nums.length` at the start of each iteration. The
   program counter either falls through into the loop body (condition true) or jumps
   forward past the loop (condition false, exits).
2. **If test** — evaluate `nums[i] > 0` inside the body. The program counter either
   executes `sum += nums[i]` (condition true, fall-through) or skips it (condition false,
   jump forward).
3. **Increment and backward jump** — `i++` runs every iteration (positive or not), then
   the unconditional backward jump returns to the while test.

The array is `[3, -1, 4]`. Initial state: `sum = 0`, `i = 0`. Expected result: `3 + 4 = 7`
(the value `-1` is skipped by the inner if).

Each frame in the trace below shows:
- The line of code about to execute.
- The current values of `i`, `sum`, and `nums[i]`.

Tracing this by hand before looking at the frames is highly recommended — it cements
the mental model of the program counter moving through code.
</Idea>

<Code lang="en">

<AnnotatedCode
  code={`function sumPositives(nums: number[]): number {
  let sum = 0;   // accumulator starts at zero
  let i = 0;     // loop counter starts at zero

  while (i < nums.length) {   // backward-jump loop
    if (nums[i] > 0) {        // conditional: skip negatives
      sum += nums[i];
    }
    i++;                      // always increment, positive or not
  }

  return sum;
}`}
  annotations={[
    { line: 2, text: "sum: accumulator — collects the running total" },
    { line: 3, text: "i: index into nums — advances every iteration" },
    { line: 5, text: "while test: CMP i, nums.length → conditional jump exits when i >= length" },
    { line: 6, text: "if test: CMP nums[i], 0 → conditional jump skips sum+= when nums[i] <= 0" },
    { line: 7, text: "sum += nums[i]: adds current element only when the if condition is true" },
    { line: 9, text: "i++: unconditional; runs even when the if was skipped" },
    { line: 10, text: "closing brace: unconditional backward jump back to the while test on line 5" },
  ]}
  caption="sumPositives: a while loop with an inner conditional. Input: [3, -1, 4]. Expected output: 7."
/>
</Code>

<Trace lang="en">
Step through `sumPositives([3, -1, 4])` frame by frame. Each cell shows the current value
of one variable. The active line is highlighted in the code panel above.

<AlgoTrace
  id="trace-sum-positives"
  lang="en"
  code={[
    "function sumPositives(nums: number[]): number {",
    "  let sum = 0;",
    "  let i = 0;",
    "",
    "  while (i < nums.length) {",
    "    if (nums[i] > 0) {",
    "    sum += nums[i];",
    "    }",
    "    i++;",
    "  }",
    "",
    "  return sum;",
    "}"
  ]}
  frames={[
    {
      caption: "Start: function called with nums=[3,-1,4]. No local variables exist yet.",
      line: 1,
      cells: []
    },
    {
      caption: "Line 2: sum = 0. Accumulator initialised to zero.",
      line: 2,
      cells: [
        { value: "sum=0", mark: "active" }
      ]
    },
    {
      caption: "Line 3: i = 0. Loop counter initialised to zero.",
      line: 3,
      cells: [
        { value: "sum=0", mark: "done" },
        { value: "i=0", mark: "active" }
      ]
    },
    {
      caption: "Line 5 (while test, iter 1): i=0 < nums.length=3 → true. Fall through into body.",
      line: 5,
      cells: [
        { value: "sum=0", mark: "done" },
        { value: "i=0", mark: "active" },
        { value: "nums[0]=3", mark: "target" }
      ]
    },
    {
      caption: "Line 6 (if test, iter 1): nums[0]=3 > 0 → true. Fall through to sum+=.",
      line: 6,
      cells: [
        { value: "sum=0", mark: "done" },
        { value: "i=0", mark: "done" },
        { value: "nums[0]=3", mark: "active" }
      ]
    },
    {
      caption: "Line 7: sum += nums[0] = 0 + 3 = 3. sum is now 3.",
      line: 7,
      cells: [
        { value: "sum=3", mark: "active" },
        { value: "i=0", mark: "done" },
        { value: "nums[0]=3", mark: "done" }
      ]
    },
    {
      caption: "Line 9: i++ → i = 1. Increment runs unconditionally.",
      line: 9,
      cells: [
        { value: "sum=3", mark: "done" },
        { value: "i=1", mark: "active" }
      ]
    },
    {
      caption: "Line 5 (while test, iter 2): i=1 < nums.length=3 → true. Fall through into body.",
      line: 5,
      cells: [
        { value: "sum=3", mark: "done" },
        { value: "i=1", mark: "active" },
        { value: "nums[1]=-1", mark: "target" }
      ]
    },
    {
      caption: "Line 6 (if test, iter 2): nums[1]=-1 > 0 → false. Conditional jump — skip sum+=.",
      line: 6,
      cells: [
        { value: "sum=3", mark: "done" },
        { value: "i=1", mark: "done" },
        { value: "nums[1]=-1", mark: "active" }
      ]
    },
    {
      caption: "Line 9: i++ → i = 2. The if body was skipped; sum stays 3.",
      line: 9,
      cells: [
        { value: "sum=3", mark: "done" },
        { value: "i=2", mark: "active" }
      ]
    },
    {
      caption: "Line 5 (while test, iter 3): i=2 < nums.length=3 → true. Fall through into body.",
      line: 5,
      cells: [
        { value: "sum=3", mark: "done" },
        { value: "i=2", mark: "active" },
        { value: "nums[2]=4", mark: "target" }
      ]
    },
    {
      caption: "Line 6 (if test, iter 3): nums[2]=4 > 0 → true. Fall through to sum+=.",
      line: 6,
      cells: [
        { value: "sum=3", mark: "done" },
        { value: "i=2", mark: "done" },
        { value: "nums[2]=4", mark: "active" }
      ]
    },
    {
      caption: "Line 7: sum += nums[2] = 3 + 4 = 7. sum is now 7.",
      line: 7,
      cells: [
        { value: "sum=7", mark: "active" },
        { value: "i=2", mark: "done" },
        { value: "nums[2]=4", mark: "done" }
      ]
    },
    {
      caption: "Line 9: i++ → i = 3.",
      line: 9,
      cells: [
        { value: "sum=7", mark: "done" },
        { value: "i=3", mark: "active" }
      ]
    },
    {
      caption: "Line 5 (while test, exit): i=3 < nums.length=3 → false. Conditional jump exits loop.",
      line: 5,
      cells: [
        { value: "sum=7", mark: "done" },
        { value: "i=3", mark: "active" }
      ]
    },
    {
      caption: "Line 12: return sum = 7. Function returns 7. Only 3 and 4 were added; -1 was skipped.",
      line: 12,
      cells: [
        { value: "sum=7", mark: "target" },
        { value: "i=3", mark: "done" }
      ]
    }
  ]}
/>
</Trace>

<Inset kind="edgecase" lang="en">
**What if the array is empty?** If `nums = []`, then `nums.length = 0` and the while test
on the very first check evaluates `i=0 < 0` → false. The loop body never runs. The
function immediately returns `sum = 0`. This is the correct result for an empty input and
is a natural consequence of testing the condition before the body — the same hardware
mechanism that makes a `while` test at the top.
</Inset>

<PracticeSet
  id="tracing-a-program-practice"
  lessonSlug="04-tracing-a-program"
  lang="en"
  problems={[
    {
      prompt: "After the first iteration of sumPositives([3,-1,4]), i has been incremented from 0. What value does i hold at the start of the second while test?",
      answer: 1,
      hint: "i starts at 0. i++ runs at the end of the first iteration: i = 0 + 1 = 1.",
    },
    {
      prompt: "During iteration 2 (nums[1] = -1), the if condition nums[1] > 0 is false. What value does sum hold after i++ runs at the end of that iteration?",
      answer: 3,
      hint: "The if body is skipped when nums[i] <= 0. sum is not changed during iteration 2. sum stays at 3.",
    },
    {
      prompt: "How many times does the line 'sum += nums[i]' execute when called with [3,-1,4]?",
      answer: 2,
      hint: "sum+= executes only when nums[i] > 0. nums[0]=3 (yes) and nums[2]=4 (yes) qualify; nums[1]=-1 does not. So it runs 2 times.",
    },
    {
      prompt: "At what value of i does the while loop exit (i.e., when the while condition becomes false)?",
      answer: 3,
      hint: "nums.length is 3. The while condition i < 3 becomes false when i = 3. At that point the backward jump is not taken and execution falls through past the loop.",
    },
    {
      prompt: "sumPositives([2, -5, -3, 6]) returns what value?",
      answer: 8,
      hint: "Positive values are 2 and 6. 2 + 6 = 8. The values -5 and -3 are skipped by the if.",
    },
  ]}
/>

<Check lang="en">
  <Quiz
    id="tracing-a-program-check"
    pieceSlug="04-tracing-a-program"
    lang="en"
    question="In sumPositives, what happens when nums[i] is negative or zero?"
    choices={[
      {
        label: "The if condition is false; a conditional jump skips over 'sum += nums[i]'; i++ still runs; then the backward jump returns to the while test",
        correct: true,
      },
      {
        label: "The loop terminates immediately when a non-positive value is found",
        misconception: "The loop does not exit when a negative value is encountered. The while condition checks i < nums.length, not the sign of the current element. A negative element causes the if body to be skipped, but the loop continues: i++ runs, the backward jump fires, and the while condition is tested again.",
      },
      {
        label: "i++ is skipped when the if condition is false, so the loop re-tests the same element",
        misconception: "i++ is outside the if block and always runs, regardless of whether the if condition was true or false. Look at the indentation in the code: i++ is at the same level as the if statement, not inside it. Skipping i++ would cause an infinite loop on negative values.",
      },
    ]}
  />
</Check>

<Recap lang="en">
Tracing a program step by step reveals the concrete machinery behind high-level syntax.
In `sumPositives`, the `while` loop is a test-first backward-jump structure: on each
iteration the condition `i < nums.length` is evaluated (a compare updates the Z or N
flag), a conditional jump either exits the loop or falls through into the body, and an
unconditional backward jump at the bottom resets the program counter to the test. The
inner `if` is a second CMP + conditional jump: when `nums[i] > 0` is false, the body
`sum += nums[i]` is skipped by a forward conditional jump; when true, it falls through.
The `i++` increment is unconditional — it sits outside the if block and runs regardless.
The loop exits when `i` reaches `nums.length`, at which point the while condition is
false, the conditional jump at the top fires (a forward jump past the loop), and the
program counter moves to `return sum`. The final value of `sum` is the total of all
positive elements encountered during the iteration.
</Recap>
