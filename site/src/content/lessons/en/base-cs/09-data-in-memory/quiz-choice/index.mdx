---
concepts:
- contiguous-memory
- array-indexing
- key-value-object
- references-and-indirection
- equal-size-cells
- graph-of-cells
estMin: 13
lang: en
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://see.stanford.edu/materials/icsppcs107/07-Arrays-The-Full-Story.pdf
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Data_structures
status: ready
summary: "Multiple-choice synthesis across the data-in-memory unit: contiguous layout, index arithmetic, named-field objects, and how references let a fixed-shape container hold variable-shape data."
title: 'Data in memory: multiple-choice review'
track: base-cs
unit: 09-data-in-memory
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. None of them asks you to recite a definition — each asks you to reason about how data actually sits in memory and why the layout behaves the way it does.
</Hook>

<Goal lang="en">
Confirm you can connect the four ideas the unit built: equal-size contiguous cells, the index-to-address formula, named-field objects, and references that let a fixed grid hold variable-size things.
</Goal>

<Quiz
  id="09-data-in-memory-choice-1"
  lessonSlug="09-data-in-memory"
  lang="en"
  question="An array of 8 numbers occupies one contiguous block of 32 bytes. Which statement about its layout is correct?"
  choices={[
    { label: "Each element is 4 bytes, the cells sit back-to-back with no gaps, and the block is one unbroken run of addresses — total 8 x 4 = 32 bytes.", correct: true },
    { label: "The 8 elements are stored at 8 unrelated addresses, and the array name keeps a separate location for each one.", misconception: "An array is contiguous: the elements occupy one continuous block of consecutive addresses, not scattered locations. The array name refers to the single block, specifically where it begins." },
    { label: "The cells can be different sizes as long as they add up to 32 bytes total.", misconception: "Every element of a plain array shares one type, so every cell is the same size. The equal-size grid is what defines an array; mixed-size cells would break the regular layout." },
    { label: "The 32 bytes include gaps between elements to keep them apart.", misconception: "There are no gaps. Contiguous means the cells sit directly back-to-back; the regularity of a gapless grid is exactly what makes index arithmetic land on the right cell." },
  ]}
/>

<Quiz
  id="09-data-in-memory-choice-2"
  lessonSlug="09-data-in-memory"
  lang="en"
  question="Reaching arr[3000] takes the same time as reaching arr[3]. Why is array access constant time?"
  choices={[
    { label: "The address is computed directly as base_address + index x element_size — one multiply and one add — so the work does not grow with the index or the array length.", correct: true },
    { label: "The program scans from element 0, counting cells until it reaches the index, but modern CPUs make scanning fast.", misconception: "Array access never scans. Scanning would make a large index slower than a small one. The address is computed in one arithmetic step, the same fixed work for every index." },
    { label: "The runtime keeps a lookup table mapping every index to its address, so any one is a table read.", misconception: "No per-element table is stored. The contiguous, equal-size layout lets the address be computed by arithmetic from the base and the index — no table needed." },
    { label: "The array length is small enough that any index is fast in practice.", misconception: "The length is irrelevant: the formula base + i x element_size does not use the length or touch the other elements, so access stays constant time even for ten million elements." },
  ]}
/>

<Quiz
  id="09-data-in-memory-choice-3"
  lessonSlug="09-data-in-memory"
  lang="en"
  question="Why is the first element of an array at index 0 rather than index 1?"
  choices={[
    { label: "The index is the offset measured in elements from the start. The first cell sits exactly at the base address, so its offset is 0 x element_size = 0 — and 0 is the only index whose offset is zero.", correct: true },
    { label: "It is an arbitrary convention chosen by language designers that you simply memorise.", misconception: "It is forced by the arithmetic, not arbitrary. The formula base + index x element_size makes the first cell's index exactly the one that yields a zero offset, which is 0." },
    { label: "Index 0 is a reserved slot for the array's length, so real data starts at the address after it.", misconception: "Index 0 holds the first real element, not metadata. Its offset is zero, placing it right at the base address; that is precisely why it is the first element." },
    { label: "Counting from 0 makes the array one element shorter, saving memory.", misconception: "Counting from 0 changes no sizes. It reflects that the index is an element-offset from the start: the first element is zero elements past the base." },
  ]}
/>

<Quiz
  id="09-data-in-memory-choice-4"
  lessonSlug="09-data-in-memory"
  lang="en"
  question="An array reaches a value by arr[2]; an object reaches a value by user.age. What is the core difference, and what follows from it?"
  choices={[
    { label: "An array reaches a value by numeric position and its order is the structure; an object reaches a value by a named key and its field order is meaningless — so { a: 1, b: 2 } and { b: 2, a: 1 } are the same object.", correct: true },
    { label: "An object is just an array whose elements happen to be labelled, and the labels are looked up by numeric index like array elements.", misconception: "An object's fields are reached by their key, a name, not by a numeric index. Name-based access is the whole point of an object; positional access is the array's property." },
    { label: "An object stores its fields in a strict order and you must read them first-field-first.", misconception: "Object fields have no meaningful order. You reach a field by its key, never by which one comes first. Ordered access is an array property; if order matters, you want an array." },
    { label: "There is no real difference — both are just sequences of values under one name.", misconception: "They group values differently: an array by position, an object by name. That choice decides whether order carries meaning and whether you index or name to read a value." },
  ]}
/>

<Quiz
  id="09-data-in-memory-choice-5"
  lessonSlug="09-data-in-memory"
  lang="en"
  question="You have an array of 100 user objects, each a different size. How does it sit in memory while keeping the array's equal-size contiguous grid?"
  choices={[
    { label: "Each array cell holds a reference — a fixed-size address — pointing to a user object stored elsewhere. The block stays a contiguous grid of equal-size reference cells; the objects live scattered in memory.", correct: true },
    { label: "The 100 whole objects are packed end-to-end inside one giant contiguous array block.", misconception: "Objects vary in size and can grow, so they cannot fill an equal-size grid. The cells hold references; the objects themselves live separately, wherever memory had room." },
    { label: "The array automatically resizes each cell to fit the object it holds, so cells differ in size.", misconception: "Cells in an array are uniform in size — that is what makes base + i x element_size valid. The cells hold equal-size references; only the referenced objects vary in size." },
    { label: "An array cannot hold objects at all; they must go in a different kind of structure.", misconception: "An array can hold objects by holding references to them. Each cell stores an object's address, and the array stays a normal contiguous array of equal-size cells." },
  ]}
/>

<Quiz
  id="09-data-in-memory-choice-6"
  lessonSlug="09-data-in-memory"
  lang="en"
  question="Reading users[1].name in an array of objects takes two hops. What are they, and why two?"
  choices={[
    { label: "Hop 1 is arithmetic: compute base + 1 x element_size to reach the cell and read the reference there. Hop 2 follows that reference to the object, then looks up the name field. Two hops because the cell holds an address, not the object.", correct: true },
    { label: "One hop: the array cell contains the whole user object, so reading the cell already gives you the name field.", misconception: "The array cell holds a reference, not the object. You must follow the address out to the object before you can read a field, which is the second hop." },
    { label: "Two hops because every array access scans the array once and then scans the object once.", misconception: "Neither step is a scan. The first hop is one arithmetic computation into the contiguous array; the second is a single reference follow to the object. Both are direct." },
    { label: "Three hops: one for the array, one for the reference, one to count the field's position.", misconception: "A field is reached by its key, not by counting positions. It is two hops: arithmetic into the array, then one reference follow to the object where the key locates the field." },
  ]}
/>

<Recap lang="en">
The through-line of the unit is one layered picture. An array is a contiguous run of equal-size cells, so any element's address is one multiply and one add (base + i x element_size) — constant time, with index 0 forced by a zero offset. An object groups values by name instead of position, so its field order carries no meaning. And because a fixed-size cell cannot hold a variable-size object, cells hold references — equal-size addresses — letting a uniform grid point at variable-shape data elsewhere. Carried all the way, nested data is a graph of cells: some holding values, some holding addresses, wired together.
</Recap>
