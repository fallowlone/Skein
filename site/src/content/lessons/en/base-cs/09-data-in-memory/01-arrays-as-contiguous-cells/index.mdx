---
slug: 01-arrays-as-contiguous-cells
lang: en
track: base-cs
unit: 09-data-in-memory
order: 1
title: "Arrays as contiguous cells"
summary: "An array is a run of equal-size cells placed back-to-back in memory. Because every element has the same type, every cell is the same size, and the whole array occupies one unbroken block of addresses."
estMin: 20
status: ready
lessonType: coding
prereqs:
  - "01-addressable-cells"
  - "02-the-byte"
  - "01-what-a-value-is"
  - "03-primitive-types"
mathPrereqs: []
concepts: ["array", "contiguous-memory", "array-element", "equal-size-cells", "array-block"]
sources:
  - https://www.nand2tetris.org/
  - https://see.stanford.edu/materials/icsppcs107/07-Arrays-The-Full-Story.pdf
  - https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array
---

import Hook from "../../../../../../components/lesson/Hook.astro";
import Goal from "../../../../../../components/lesson/Goal.astro";
import Idea from "../../../../../../components/lesson/Idea.astro";
import Code from "../../../../../../components/lesson/Code.astro";
import Trace from "../../../../../../components/lesson/Trace.astro";
import Check from "../../../../../../components/lesson/Check.astro";
import Recap from "../../../../../../components/lesson/Recap.astro";
import Inset from "../../../../../../components/lesson/Inset.astro";
import AnnotatedCode from "../../../../../../components/algo/AnnotatedCode.astro";
import AlgoTrace from "../../../../../../components/algo/AlgoTrace.astro";
import PracticeSet from "../../../../../../components/math/PracticeSet.astro";
import Quiz from "../../../../../../components/pedagogy/Quiz.astro";

<Hook>
So far every variable you have seen has held a single value: one number, one boolean, one
string. But a program almost always needs to hold *many* values of the same kind — the
scores of every player, the bytes of a file, the pixels of an image.

You could declare a hundred separate variables, but that is unworkable. Instead, languages
give you the **array**: one name that refers to a whole sequence of values laid out in
order. The first thing to understand about an array is not its syntax — it is its shape in
memory. An array is not scattered across memory at random. It is a single unbroken run of
cells, each the same size, placed directly back-to-back.

After this lesson you will be able to picture exactly where each element of an array sits
in memory, and why that picture is the foundation for everything arrays can do.
</Hook>

<Goal lang="en">
After this lesson you can define an array as a contiguous run of equal-size cells,
explain why every cell in an array is the same size, identify which addresses a small
array occupies, and trace the memory layout of an array literal element by element.
</Goal>

<Idea lang="en">
Recall two facts from earlier units. From Unit 02: memory is a long row of **addressable
cells**, each with its own numeric address, and the **byte** (8 bits) is the standard
unit a cell holds. From Unit 05: every value has a **type**, and the type fixes how many
bytes the value needs — a number of a given type always takes the same fixed number of
bytes.

An **array** is built directly on those two facts. An array is a sequence of values, all
of the **same type**, stored in memory as a run of cells that are:

1. **Equal in size.** Because every element has the same type, every element needs the
   same number of bytes. So every cell in the array is the same size. Call that size the
   **element size**.
2. **Contiguous.** The cells sit directly back-to-back, with no gaps between them. The
   array occupies one unbroken block of addresses. This property is called **contiguous
   memory**.

Each value stored in the array is called an **element**. If an array has 5 elements and
each element is 4 bytes, the whole array is one solid block of 5 × 4 = 20 bytes.

The two properties are linked. *Equal-size* is what makes *contiguous* useful: when
identical cells are packed back-to-back, the position of any cell is perfectly regular and
predictable. You will use that regularity in the next lesson to find any element instantly.
For now, the goal is just to see the shape: an array is a tidy, gapless strip of identical
cells.

The program below builds a small array of 5 numbers. The trace shows the array taking
shape one cell at a time.
</Idea>

<Code lang="en">

<AnnotatedCode
  code={`// An array literal: 5 numbers of the same type.
let scores: number[] = [90, 80, 70, 60, 50];

// All five values live in one contiguous block of memory.
// Element 0 is at the start of the block; the rest follow
// directly behind it, each in a cell of the same size.

// scores has 5 elements.
// If each number takes 4 bytes, the whole array is 20 bytes.`}
  annotations={[
    { line: 2, text: "scores is an array of 5 elements, every element a number — so every cell is the same size" },
    { line: 5, text: "Element 0 (value 90) sits first; element 1 (value 80) sits directly behind it, and so on" },
    { line: 6, text: "No gaps: the cells are back-to-back. This is contiguous memory." },
    { line: 9, text: "5 elements x 4 bytes each = one solid 20-byte block" },
  ]}
  caption="An array literal of 5 numbers. The trace below places each element in its memory cell."
/>
</Code>

<Trace lang="en">
Step through building `scores`. Each box is one cell of the array; the address below each
box is the byte address where that cell starts. Assume the array's block begins at address
1000 and each number takes 4 bytes, so the cells start at 1000, 1004, 1008, 1012, 1016.

<AlgoTrace
  id="trace-arrays-contiguous"
  lang="en"
  code={[
    "let scores: number[] =",
    "  [90, 80, 70, 60, 50];"
  ]}
  frames={[
    {
      caption: "Before: the array's block is reserved but no element values are placed yet.",
      line: 1,
      cells: [
        { value: "?" },
        { value: "?" },
        { value: "?" },
        { value: "?" },
        { value: "?" }
      ]
    },
    {
      caption: "Element 0 placed: value 90 stored in the first cell, at address 1000.",
      line: 2,
      cells: [
        { value: "90", mark: "active" },
        { value: "?" },
        { value: "?" },
        { value: "?" },
        { value: "?" }
      ]
    },
    {
      caption: "Element 1 placed: value 80 stored in the next cell, at address 1004 — directly behind cell 0.",
      line: 2,
      cells: [
        { value: "90", mark: "done" },
        { value: "80", mark: "active" },
        { value: "?" },
        { value: "?" },
        { value: "?" }
      ]
    },
    {
      caption: "Element 2 placed: value 70 stored at address 1008. Same size cell, no gap.",
      line: 2,
      cells: [
        { value: "90", mark: "done" },
        { value: "80", mark: "done" },
        { value: "70", mark: "active" },
        { value: "?" },
        { value: "?" }
      ]
    },
    {
      caption: "Element 3 placed: value 60 stored at address 1012.",
      line: 2,
      cells: [
        { value: "90", mark: "done" },
        { value: "80", mark: "done" },
        { value: "70", mark: "done" },
        { value: "60", mark: "active" },
        { value: "?" }
      ]
    },
    {
      caption: "Element 4 placed: value 50 stored at address 1016. The array is complete: one solid 20-byte block, addresses 1000 to 1019.",
      line: 2,
      cells: [
        { value: "90", mark: "done" },
        { value: "80", mark: "done" },
        { value: "70", mark: "done" },
        { value: "60", mark: "done" },
        { value: "50", mark: "active" }
      ]
    }
  ]}
/>
</Trace>

<Inset kind="why" lang="en">
**Why does the array need every element to be the same size?** If elements could be
different sizes, the cells could not be uniform, and you would have no way to know where
one element ends and the next begins without inspecting them all. By forcing one type — and
therefore one fixed size — the language guarantees the cells form a perfectly regular grid.
That regularity is exactly what lets a program jump straight to any element. A mixed-size
sequence is possible, but it is no longer this simple back-to-back grid; it needs extra
bookkeeping. The plain array trades flexibility for a clean, predictable shape.
</Inset>

<Inset kind="mistake" lang="en">
A common misconception is that an array's elements are stored "somewhere in memory" with
the array name acting as a list of separate locations. That is not the array model. The
elements occupy *one* block of consecutive addresses. The array name refers to that block —
specifically to where it begins. The elements are not scattered; they are a single
contiguous run.
</Inset>

<PracticeSet
  id="arrays-contiguous-practice"
  lessonSlug="01-arrays-as-contiguous-cells"
  lang="en"
  problems={[
    {
      prompt: "An array has 6 elements, and each element takes 4 bytes. How many bytes does the whole array block occupy?",
      answer: 24,
      hint: "The array is one contiguous block: number of elements x element size. 6 x 4 = 24.",
    },
    {
      prompt: "An array of 8 elements occupies a contiguous block of 16 bytes total. How many bytes is each element (the element size)?",
      answer: 2,
      hint: "Total block size divided by number of elements: 16 / 8 = 2 bytes per element.",
    },
    {
      prompt: "An array's block begins at address 1000 and each element is 4 bytes. At what address does element 1 (the second element) start?",
      answer: 1004,
      hint: "Element 0 starts at 1000. Element 1 sits directly behind it, 4 bytes later: 1000 + 4 = 1004.",
    },
    {
      prompt: "In a plain array, every element must have the same type so that every cell is the same size. If an array stores numbers, can one cell store a number and another cell store a 1-character string of a different byte size? Type 1 for yes, 0 for no.",
      answer: 0,
      hint: "A plain array requires one type for all elements, so all cells are equal size. Mixing types that need different sizes breaks the uniform grid. The answer is 0 (no).",
    },
    {
      prompt: "An array of 5 numbers, each 4 bytes, starts at address 2000. At what address does the last element (element 4) start?",
      answer: 2016,
      hint: "Element 4 is 4 cells past the start: 2000 + 4 elements x 4 bytes = 2000 + 16 = 2016.",
    },
  ]}
/>

<Check lang="en">
  <Quiz
    id="arrays-contiguous-check"
    pieceSlug="01-arrays-as-contiguous-cells"
    lang="en"
    question="What is the memory shape of an array?"
    choices={[
      {
        label: "One unbroken block of equal-size cells placed directly back-to-back, with no gaps between elements",
        correct: true,
      },
      {
        label: "A set of cells scattered across memory at unrelated addresses, with the array name holding a separate location for each element",
        misconception: "An array is contiguous: its elements occupy one continuous block of consecutive addresses. They are not scattered. The array name refers to the single block, not to a list of independent locations.",
      },
      {
        label: "A block of cells where each cell can be a different size depending on the value stored in it",
        misconception: "Every element of a plain array has the same type, so every cell is the same size. That uniform, equal-size grid is what defines an array. Different-size cells would break the regular layout.",
      },
    ]}
  />
</Check>

<Recap lang="en">
An **array** is a sequence of values of the same type, stored in memory as a run of
**equal-size cells** placed directly back-to-back. Because every element shares one type,
every element needs the same number of bytes — the **element size** — so every cell is
identical in size. Because the cells sit with no gaps between them, the array occupies one
unbroken block of addresses; this is **contiguous memory**. An array of N elements, each
of size S bytes, is therefore one solid block of N × S bytes. Each value in the array is
an **element**, and the array name refers to the start of the block. This tidy, gapless
grid of identical cells is the foundation of everything arrays can do — starting with the
instant element lookup you will see in the next lesson.
</Recap>
