---
concepts:
- object
- field
- key
- key-value-pair
- named-slot
estMin: 20
lang: en
lessonType: coding
mathPrereqs: []
order: 3
prereqs:
- 01-a-variable-is-a-named-cell
- 01-what-a-value-is
- 01-arrays-as-contiguous-cells
slug: 03-objects-as-key-value
sources:
- https://tc39.es/ecma262/multipage/ordinary-and-exotic-objects-behaviours.html
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Working_with_objects
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Data_structures
status: ready
summary: 'An object is a set of named fields: each key names a slot that holds a value.
  Where an array reaches an element by a numeric position, an object reaches a value
  by its name.'
title: Objects as key-value
track: base-cs
unit: 09-data-in-memory
---



import Hook from "~/components/lesson/Hook.astro";
import PracticeSet from "~/components/math/PracticeSet.astro";
import Goal from "~/components/lesson/Goal.astro";
import Idea from "~/components/lesson/Idea.astro";
import Code from "~/components/lesson/Code.astro";
import Trace from "~/components/lesson/Trace.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import AnnotatedCode from "~/components/algo/AnnotatedCode.astro";
import AlgoTrace from "~/components/algo/AlgoTrace.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
An array is the right shape when your values form a *sequence* — position 0, position 1,
position 2 — and the positions themselves carry no meaning beyond order. But much of the
data a program handles is not like that. A user has a *name*, an *age*, an *email*. These
are not "element 0, element 1, element 2". Each one has a meaning, and that meaning is a
word, not a number.

For data like this, languages give you the **object**: a value made of *named* fields.
Instead of reaching a value by counting positions, you reach it by its name. `user.age`
says "the age field of user" directly.

This lesson shows what an object is in memory — a set of named slots — and traces how the
program reaches a field by its name. You already understand the array; the object is its
counterpart, and seeing them side by side makes both clearer.
</Hook>

<Goal lang="en">
After this lesson you can define an object as a set of key-value pairs, explain what a
field, a key, and a value are, trace how the program reads a field by its name, and state
the core difference between an array and an object — positional index versus named key.
</Goal>

<Idea lang="en">
**An object is a set of named fields.** Recall from Unit 06 that a variable is a named
cell: a name bound to a slot in memory that holds a value. An **object** takes that idea
and bundles many named cells together under one value.

An object is made of **fields** (also called properties). Each field has two parts:

1. A **key** — the field's name, written as a word, like `name` or `age`.
2. A **value** — the data stored in that field, like `"Mara"` or `30`.

A key paired with its value is a **key-value pair**. An object is simply a collection of
key-value pairs. You can picture each field as a labelled slot: the label is the key, and
the slot holds the value. The object `{ name: "Mara", age: 30 }` has two fields — a slot
labelled `name` holding `"Mara"`, and a slot labelled `age` holding `30`.

**Reaching a value: name, not position.** To read a field you write `object.key` — for
example `user.age`. The program takes the key `age`, finds the slot in the object that
carries that label, and reads the value in it. The key *is* the address into the object.

**Array versus object — the core contrast.** Both group several values under one name. The
difference is *how you reach a value inside*:

- An **array** reaches a value by **position**: `arr[2]` means "the element at index 2".
  The positions are numbers, and their order is the whole structure. (Recall: the program
  finds it by computing an address from the index.)
- An **object** reaches a value by **name**: `obj.age` means "the field labelled age". The
  fields have word labels, and their order does not matter — `{ a: 1, b: 2 }` and
  `{ b: 2, a: 1 }` are the same object.

Use an array when your data is an ordered sequence of same-kind values. Use an object when
your data is a fixed set of differently-meaning, named pieces.

<DiagramFrame
  label="The object user as a two-column record. The left column lists three keys: name, age, active. The right column lists the matching values: Mara, 30, true. Each key points by an arrow to its own value. On the far left, a lookup user.age sends an arrow into the age row, showing that lookup finds a value by its key name, not by a number."
  hue="var(--d-hardware)"
  caption="An object is a set of key → value entries. Each key (a name) points to its stored value. A lookup like user.age finds the entry whose key is 'age' and reads 30 — reached by name, not by a numeric position."
>
  <svg viewBox="0 0 480 210" width="480" height="210" role="presentation" style="display:block;margin:0 auto;max-width:100%;height:auto;font-family:var(--font-body);">
    <defs>
      <marker id="obj-arrow-en" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
        <path d="M0 0 L10 5 L0 10 z" fill="var(--muted)" />
      </marker>
    </defs>
    {/* record outline + heading */}
    <text x="316" y="22" text-anchor="middle" font-size="12" fill="var(--ink)">object</text>
    <text x="316" y="38" text-anchor="middle" font-size="13" font-family="var(--font-mono)" fill="var(--d-hardware)" font-weight="600">user</text>
    <text x="198" y="52" text-anchor="middle" font-size="10" fill="var(--faint)">key</text>
    <text x="386" y="52" text-anchor="middle" font-size="10" fill="var(--faint)">value</text>
    {[
      { k: "name", v: "\"Mara\"" },
      { k: "age", v: "30" },
      { k: "active", v: "true" },
    ].map((row, i) => (
      <g>
        {/* key cell */}
        <rect x="150" y={60 + i * 44} width="96" height="32" rx="5" fill="color-mix(in srgb, var(--d-hardware) 12%, var(--card-2))" stroke="var(--hairline-strong)" stroke-width="0.8" />
        <text x="198" y={80 + i * 44} text-anchor="middle" font-size="13" font-family="var(--font-mono)" fill="var(--ink)">{row.k}</text>
        {/* key -> value arrow */}
        <path d={`M250 ${76 + i * 44} L300 ${76 + i * 44}`} fill="none" stroke="var(--muted)" stroke-width="1.4" marker-end="url(#obj-arrow-en)" />
        {/* value cell */}
        <rect x="304" y={60 + i * 44} width="124" height="32" rx="5" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
        <text x="366" y={80 + i * 44} text-anchor="middle" font-size="13" font-family="var(--font-mono)" fill="var(--ink)">{row.v}</text>
      </g>
    ))}
    {/* lookup by key name -> the age row */}
    <text x="62" y="98" text-anchor="middle" font-size="11" fill="var(--ink)">lookup</text>
    <text x="62" y="116" text-anchor="middle" font-size="13" font-family="var(--font-mono)" fill="var(--ink)">user.age</text>
    <path d="M104 112 L146 110" fill="none" stroke="var(--d-hardware)" stroke-width="1.6" marker-end="url(#obj-arrow-en)" />
    <text x="62" y="134" text-anchor="middle" font-size="10" fill="var(--faint)">by name</text>
  </svg>
</DiagramFrame>

The program below builds an object with three fields and reads two of them by name.
</Idea>

<Code lang="en">

<AnnotatedCode
  code={`// An object literal: three named fields (key-value pairs).
let user = {
  name: "Mara",   // key "name"  -> value "Mara"
  age: 30,        // key "age"   -> value 30
  active: true,   // key "active" -> value true
};

// Read a field BY ITS NAME, not by a position:
let a = user.age;       // find the slot labelled "age", read 30
let n = user.name;      // find the slot labelled "name", read "Mara"

// Compare with an array, which is reached BY POSITION:
let nums = [10, 20, 30];
let x = nums[1];        // element at index 1 -> 20`}
  annotations={[
    { line: 2, text: "An object: a set of key-value pairs grouped under the name user" },
    { line: 3, text: "Field 1 — key 'name' labels a slot holding the value 'Mara'" },
    { line: 4, text: "Field 2 — key 'age' labels a slot holding the value 30" },
    { line: 9, text: "user.age: look up the slot whose label is 'age', read its value (30)" },
    { line: 13, text: "An array reaches values by numeric position, not by name" },
    { line: 14, text: "nums[1]: position 1, computed as an offset from the base — see lesson 02" },
  ]}
  caption="An object with three named fields. Reading a field uses its key. The trace below shows the named lookups."
/>
</Code>

<Trace lang="en">
Step through building `user` and reading two fields. Each cell is one field of the object;
the label below each cell is that field's key.

<AlgoTrace
  id="trace-objects-keyvalue"
  lang="en"
  code={[
    "let user = {",
    "  name: \"Mara\",",
    "  age: 30,",
    "  active: true,",
    "};",
    "let a = user.age;",
    "let n = user.name;"
  ]}
  frames={[
    {
      caption: "Before: the object is being built. No fields placed yet.",
      line: 1,
      cells: []
    },
    {
      caption: "Line 2: field 'name' added. A slot labelled name holds the value \"Mara\".",
      line: 2,
      cells: [
        { value: "name:\nMara", mark: "active" }
      ]
    },
    {
      caption: "Line 3: field 'age' added. A slot labelled age holds the value 30.",
      line: 3,
      cells: [
        { value: "name:\nMara", mark: "done" },
        { value: "age:\n30", mark: "active" }
      ]
    },
    {
      caption: "Line 4: field 'active' added. The object now has three key-value pairs.",
      line: 4,
      cells: [
        { value: "name:\nMara", mark: "done" },
        { value: "age:\n30", mark: "done" },
        { value: "active:\ntrue", mark: "active" }
      ]
    },
    {
      caption: "Line 6: user.age. Find the slot labelled 'age', read its value: 30. a now holds 30.",
      line: 6,
      cells: [
        { value: "name:\nMara", mark: "done" },
        { value: "age:\n30", mark: "target" },
        { value: "active:\ntrue", mark: "done" }
      ]
    },
    {
      caption: "Line 7: user.name. Find the slot labelled 'name', read its value: \"Mara\". n now holds \"Mara\".",
      line: 7,
      cells: [
        { value: "name:\nMara", mark: "target" },
        { value: "age:\n30", mark: "done" },
        { value: "active:\ntrue", mark: "done" }
      ]
    }
  ]}
/>
</Trace>

<Inset kind="why" lang="en">
**Why use names instead of positions for this kind of data?** Numeric positions only carry
meaning when the data is genuinely a sequence. For a user record, position 0 being the
name and position 1 being the age would be an arbitrary convention you would have to
memorise and never break. A key is self-describing: `user.age` says exactly what it reads.
Names also free the data from order — adding a new field does not shift the others, the
way inserting an array element shifts every later index. Named fields trade the array's
tight positional layout for clarity and flexibility.
</Inset>

<Inset kind="mistake" lang="en">
A common mistake is to think an object's fields have a meaningful order, like an array's
elements. They do not. `{ a: 1, b: 2 }` and `{ b: 2, a: 1 }` describe the same object: the
same set of key-value pairs. You never reach a field by "which one comes first" — you
reach it by its key. If the order of items matters to your program, that is a signal you
want an array, not an object.
</Inset>

<PracticeSet
  id="objects-keyvalue-practice"
  lessonSlug="03-objects-as-key-value"
  lang="en"
  problems={[
    {
      prompt: "The object { name: \"Mara\", age: 30, active: true } has how many fields (key-value pairs)?",
      answer: 3,
      hint: "Count the keys: name, age, active. That is 3 key-value pairs.",
    },
    {
      prompt: "let p = { x: 7, y: 4 }; The program reads p.y. What value does it read?",
      answer: 4,
      hint: "p.y finds the slot labelled y and reads its value. The field y holds 4.",
    },
    {
      prompt: "let item = { price: 50, qty: 3 }; The program reads item.price. What value does it read?",
      answer: 50,
      hint: "item.price finds the slot whose key is price. That field holds 50.",
    },
    {
      prompt: "An object is reached by name; an array is reached by numeric position. To get the value of a field called 'score', do you use a numeric index? Type 1 for yes, 0 for no.",
      answer: 0,
      hint: "A field is reached by its key (the name 'score'), not by a number. Numeric position is how arrays work. The answer is 0 (no).",
    },
    {
      prompt: "let a = { v: 1, w: 1 }; let b = { w: 1, v: 1 }; The fields are the same key-value pairs in a different written order. Do a and b describe the same object's set of fields? Type 1 for yes, 0 for no.",
      answer: 1,
      hint: "An object is a set of key-value pairs; the written order of fields does not matter. Both have v:1 and w:1. The answer is 1 (yes).",
    },
  ]}
/>

<Check lang="en">
  <Quiz
    id="objects-keyvalue-check"
    pieceSlug="03-objects-as-key-value"
    lang="en"
    question="What is an object, and how is it different from an array?"
    choices={[
      {
        label: "An object is a set of key-value pairs — named fields reached by their key — whereas an array is a sequence of elements reached by numeric position",
        correct: true,
      },
      {
        label: "An object is just an array where the elements happen to be labelled, and the labels are reached by numeric index like array elements",
        misconception: "An object's fields are reached by their key (a name), not by a numeric index. The whole point of an object is name-based access. An array's elements are reached by position; an object's fields are reached by name.",
      },
      {
        label: "An object stores its fields in a strict order, and you must access them in that order, first field first",
        misconception: "An object's fields have no meaningful order. { a: 1, b: 2 } and { b: 2, a: 1 } are the same object. You reach a field by its key, never by its position. Ordered access is an array property, not an object property.",
      },
    ]}
  />
</Check>

<Recap lang="en">
An **object** is a value made of **fields** (also called properties), where each field is
a **key-value pair**: a **key** is the field's name, and the **value** is the data stored
under it. You can picture each field as a labelled slot — the key is the label, the slot
holds the value. To read a field you use its key: `user.age` finds the slot labelled `age`
and reads its value. The core difference from an array: an array reaches a value by
**numeric position** (`arr[2]`), while an object reaches a value by **name** (`obj.age`).
An object's fields therefore have no meaningful order — `{ a: 1, b: 2 }` and
`{ b: 2, a: 1 }` are the same object. Use an array for an ordered sequence of same-kind
values; use an object for a named set of differently-meaning pieces.
</Recap>
