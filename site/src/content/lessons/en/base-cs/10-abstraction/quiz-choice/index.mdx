---
concepts:
- interface-vs-implementation
- encapsulation
- module-boundary
- namespacing
- leaky-abstraction
- layer-stack
estMin: 13
lang: en
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.joelonsoftware.com/2002/11/11/the-law-of-leaky-abstractions/
- https://developer.mozilla.org/en-US/docs/Glossary/Abstraction
- https://sicp.sourceacademy.org/chapters/2.1.2.html
status: ready
summary: "Multiple-choice synthesis across the abstraction unit — interface vs implementation, encapsulation, module boundaries, namespacing, and why every non-trivial abstraction leaks."
title: 'Abstraction: multiple-choice review'
track: base-cs
unit: 10-abstraction
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one is a judgement call a working programmer makes — not a definition to recite, but the line between what an abstraction promises and what it actually hides.
</Hook>

<Goal lang="en">
Confirm you can connect the four ideas the unit built toward — interface vs implementation, encapsulation in a bundle, the module boundary and namespacing, and the leaky-abstraction tradeoff — and apply them to code you have never seen before.
</Goal>

<Quiz
  id="10-abstraction-choice-1"
  lessonSlug="10-abstraction"
  lang="en"
  question="A team rewrites the body of add(a, b) to compute the sum a faster way. The name, the two inputs, and the returned sum all stay the same. How many of the hundreds of call sites must change, and why?"
  choices={[
    { label: "Zero. Callers depend only on the interface — the name, the inputs, the returned sum. That contract held, so the implementation was free to change underneath every caller untouched.", correct: true },
    { label: "All of them — any change to a function's body forces its callers to be updated.", misconception: "Callers depend on the interface, not the body. As long as the name, inputs, and result are unchanged, the body can be rewritten completely and no caller changes by one character." },
    { label: "Only the callers that read add's local variables.", misconception: "Callers cannot read a function's local variables at all — those are part of the hidden implementation. Nothing outside the body depends on them, so there is nothing to update." },
    { label: "None of them, because rewriting a body is impossible once callers exist.", misconception: "Rewriting the body is exactly what abstraction makes safe. The reason zero callers change is the fixed interface, not any rule against editing the body." },
  ]}
/>

<Quiz
  id="10-abstraction-choice-2"
  lessonSlug="10-abstraction"
  lang="en"
  question="A counter object bundles a data field count with methods increment() and value(). What does encapsulation actually buy you here?"
  choices={[
    { label: "The methods are the interface and the field layout is the hidden implementation, so the one place that touches count is the methods — you can rename count or split it in two and every caller stays unchanged.", correct: true },
    { label: "It makes the object run faster because methods are compiled differently from plain functions.", misconception: "Encapsulation is about what is exposed versus hidden, not speed. A method is just a function stored as a field; bundling changes visibility and maintainability, not execution speed." },
    { label: "It lets callers read and write count directly without going through a method.", misconception: "That is the opposite of encapsulation. The whole point is that callers use the methods and never name the data field, so the layout stays hidden and changeable." },
    { label: "It deletes the count field from memory so only the methods remain.", misconception: "count still exists and still holds the state — the methods read and write it through this. Encapsulation hides the field from outside code, it does not remove it." },
  ]}
/>

<Quiz
  id="10-abstraction-choice-3"
  lessonSlug="10-abstraction"
  lang="en"
  question="A module defines six names and marks two of them as exports. Outside code tries to call one of the four unexported names directly. What happens, and why was the boundary worth enforcing?"
  choices={[
    { label: "It fails — the four private names are not visible across the boundary. Enforcing it, rather than trusting a convention, guarantees nothing outside can depend on a private detail, so the author can rewrite all four freely.", correct: true },
    { label: "It works, because every name a module defines is callable once you know its name.", misconception: "By default a module's names are private — visible only inside it. Only the two exported names cross the boundary; the other four cannot be named from outside at all." },
    { label: "It works only if the private name holds a function rather than a value.", misconception: "Privacy does not depend on whether the name holds a function or a value. Any name not marked as an export is invisible across the boundary, function or not." },
    { label: "It fails, but only because the names happen to collide with names in the calling module.", misconception: "It fails because the names are private, not because of any collision. Even with no name clash anywhere, an unexported name cannot be reached from outside." },
  ]}
/>

<Quiz
  id="10-abstraction-choice-4"
  lessonSlug="10-abstraction"
  lang="en"
  question="A large program needs two functions both sensibly called format — one for dates, one for currency. Why does putting each in its own module stop them from colliding?"
  choices={[
    { label: "Each module is its own namespace, so the two are reached as dateModule.format and currencyModule.format — distinct names because the module handle in front keeps them apart.", correct: true },
    { label: "The language renames the second format automatically to format2 behind the scenes.", misconception: "Nothing is silently renamed. The names stay format in each module; they do not clash because each is reached through its own module name, which gives it a separate home." },
    { label: "Only one of the two format functions is allowed to exist; modules force you to delete the other.", misconception: "Both can exist at once. Namespacing is precisely what lets identical short names coexist — each module is a fresh space, so neither has to be deleted." },
    { label: "Modules merge the two functions into one that handles both dates and currency.", misconception: "Modules keep the two functions entirely separate; they do not merge code. The boundary plus the module handle is what keeps the two format names apart." },
  ]}
/>

<Quiz
  id="10-abstraction-choice-5"
  lessonSlug="10-abstraction"
  lang="en"
  question="A function-call abstraction works fine for thousands of ordinary calls, then a runaway recursion crashes with a stack-overflow error that names stack frames. How should you read this through the unit?"
  choices={[
    { label: "The abstraction leaked: a detail of the layer below — the finite call stack — showed through when that layer hit its limit. It was genuinely useful for the ordinary calls and it leaked at the limit; both are true.", correct: true },
    { label: "The abstraction was broken from the start and should never have been used.", misconception: "It worked correctly for thousands of calls — that is real value. The Law of Leaky Abstractions says all non-trivial abstractions leak to some degree; a leak does not make the abstraction useless." },
    { label: "A good enough function-call abstraction would never leak like this.", misconception: "No abstraction over a real machine is leak-proof. Time, memory, and a finite stack are real properties of the layer below that no higher interface can fully hide." },
    { label: "The crash proves the function call never really hid the stack at all.", misconception: "It hid the stack for every ordinary call — you wrote f() thousands of times without thinking about frames. It only surfaced when the lower layer failed, which is exactly what a leak is." },
  ]}
/>

<Quiz
  id="10-abstraction-choice-6"
  lessonSlug="10-abstraction"
  lang="en"
  question="Because every non-trivial abstraction leaks, a colleague concludes you should stop learning the layers underneath the ones you work in. What is wrong with that conclusion?"
  choices={[
    { label: "An abstraction saves you time working but not time learning. Any layer can leak, so you must still understand each one for the day it does — which is exactly why the track taught the layers beneath the ones you mostly use.", correct: true },
    { label: "Nothing is wrong — once an abstraction exists, the lower layer genuinely never matters again.", misconception: "The lower layer still runs and can still leak. An abstraction removes detail from your attention while things work; it does not free you from understanding the layer for when it breaks." },
    { label: "The conclusion is fine, because leaks are so rare they can be ignored in practice.", misconception: "Leaks are not exotic — they surface whenever the lower layer fails or runs slow, which happens routinely in real systems. That is precisely why the underlying layers must be learned." },
    { label: "It is wrong because abstractions actually make programs slower, so the layers below speed them up.", misconception: "Abstraction is about managing complexity — what you must hold in mind — not raw speed. The flaw in the conclusion is that any layer can leak, so none can be safely ignored forever." },
  ]}
/>

<Recap lang="en">
The unit's through-line is one chain: an abstraction is a fixed interface over a hidden, replaceable implementation; a bundle applies that to data plus its operations and calls the hiding encapsulation; a module applies it to a whole group of code, with an enforced boundary and namespacing so private detail stays untouchable and short names stop colliding; and stacking these layers is how you manage complexity. The price is the leak — every non-trivial abstraction surfaces the layer below when that layer fails or runs slow, which is why you use the interface for the reasoning it buys and still learn the machinery underneath.
</Recap>
