---
concepts:
- address-vs-value
- byte-addressing
- pointer-indirection
- stack-lifo
- heap-lifetime
- dereference
estMin: 13
lang: en
level: middle
order: 5
prereqs: []
slug: quiz-choice
sources:
- https://en.wikipedia.org/wiki/Pointer_(computer_programming)
- https://en.wikipedia.org/wiki/Stack-based_memory_allocation
- https://en.wikipedia.org/wiki/Byte
status: ready
summary: "Multiple-choice synthesis across the memory unit: addresses vs values, byte-addressing, pointers and dereferencing, and stack vs heap lifetimes."
title: 'Memory: multiple-choice review'
track: base-cs
unit: 02-memory
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that pull the whole unit together. None of them is a definition to recite — each asks you to combine two or three ideas (address vs value, the byte, pointers, stack vs heap) and reason about what the machine actually does.
</Hook>

<Goal lang="en">
Confirm you can connect the unit's core ideas: a cell has both an address and a value, the byte is the addressable unit, a value can itself be an address (a pointer), and the stack and heap are two regions of the same memory with different lifetimes.
</Goal>

<Quiz
  id="02-memory-choice-1"
  lessonSlug="02-memory"
  lang="en"
  question="A cell at address 7 holds the value 7. Which statement is correct?"
  choices={[
    { label: "The address (7) is the cell's fixed position in the row; the value (7) is the data currently stored there. They are the same number by coincidence, not because they are the same kind of thing.", correct: true },
    { label: "The cell is special because its address equals its value.", misconception: "There is nothing special about a cell whose address happens to equal its value. Address and value are independent — one names the location, the other is the data. Their being equal here is pure coincidence." },
    { label: "Writing a new value would also change the address.", misconception: "A write changes only the value in the cell. The address is the cell's permanent position in the row and never changes when you overwrite the contents." },
    { label: "Because the numbers match, this cell is a pointer to itself.", misconception: "A cell is a pointer only when the program intentionally uses its value as an address. A value equal to the cell's own address does not make it a self-pointer; nothing here treats the 7 as an address." },
  ]}
/>

<Quiz
  id="02-memory-choice-2"
  lessonSlug="02-memory"
  lang="en"
  question="A program stores a 32-bit integer in byte-addressed memory starting at address 100. Which addresses does it occupy, and why?"
  choices={[
    { label: "Addresses 100, 101, 102, 103 — four consecutive bytes. Memory names one byte per address, so a 32-bit (4-byte) value spans four addresses; the CPU reads them together as one word.", correct: true },
    { label: "Only address 100 — one address holds the whole 32-bit value.", misconception: "Each address names exactly one byte (8 bits), not 32 bits. A 32-bit value needs four bytes, so it occupies four consecutive addresses; the CPU loads them together." },
    { label: "Addresses 100 through 131 — one address per bit.", misconception: "Memory is byte-addressed, not bit-addressed. An address names a whole byte, so a 32-bit value uses 32 / 8 = 4 addresses, not 32." },
    { label: "Addresses 100 and 101 — two bytes are enough for any integer.", misconception: "Two bytes hold only 16 bits. A 32-bit integer needs four bytes (four addresses). The byte count comes from bits divided by 8." },
  ]}
/>

<Quiz
  id="02-memory-choice-3"
  lessonSlug="02-memory"
  lang="en"
  question="Cell at address 5 holds value 12. Cell at address 12 holds value 99. A program dereferences the pointer at address 5. How many memory reads happen, and what is the final result?"
  choices={[
    { label: "Two reads: read address 5 to get 12 (an address), then read address 12 to get 99. The final result is 99 — dereferencing follows the stored address one step.", correct: true },
    { label: "One read: dereferencing address 5 returns 12, the value in the pointer cell.", misconception: "Dereferencing does not stop at the pointer cell. It uses the value found (12) as a second address and reads that cell too. The result is the value at address 12, which is 99." },
    { label: "Two reads, final result 5 — it returns to the original address.", misconception: "Dereferencing does not loop back. It reads the pointer (5 to get 12), then reads address 12 to get 99. The result is 99, not the starting address." },
    { label: "Zero reads — the pointer value 12 is the answer directly.", misconception: "The pointer value 12 is an address, not the data. You must perform a second read at address 12 to reach the actual value 99. Both reads are required." },
  ]}
/>

<Quiz
  id="02-memory-choice-4"
  lessonSlug="02-memory"
  lang="en"
  question="Why does indirection through a pointer appear in almost every non-trivial program, given that it costs an extra memory read?"
  choices={[
    { label: "It enables sharing without copying (many cells can hold the same address) and one-place updates (move the data, change only the pointer, and every reference follows). That flexibility is worth one extra read.", correct: true },
    { label: "Because pointers make memory access faster than reading the value directly.", misconception: "Indirection is slower for a single access — it adds a read. Pointers are used for sharing and flexible updates, not for raw speed of one lookup." },
    { label: "Because the hardware requires every value to be reached through a pointer.", misconception: "The hardware can read any cell directly by its address. Pointers are a software convention layered on top, used when sharing or indirection is needed — not a hardware requirement." },
    { label: "Because pointers reduce the number of bytes a value occupies.", misconception: "A pointer adds storage (the address-sized pointer plus the data it points to). The benefit is sharing and indirect updates, not smaller storage." },
  ]}
/>

<Quiz
  id="02-memory-choice-5"
  lessonSlug="02-memory"
  lang="en"
  question="Function main calls a, which calls b. While b is running, a creates a large object on the heap and keeps a pointer to it. b returns, then a returns. What is true about memory afterward?"
  choices={[
    { label: "b's stack frame is discarded when b returns and a's when a returns (LIFO). The heap object can outlive both function calls — it persists until it is explicitly freed or garbage collected, independent of the stack.", correct: true },
    { label: "The heap object is destroyed automatically when a returns, like a stack frame.", misconception: "Heap data is not tied to a function's lifetime. Unlike a stack frame, it survives until explicitly freed or garbage collected — that independence from call lifetime is the whole point of the heap." },
    { label: "b's frame survives until main returns because it was created deepest.", misconception: "The stack is LIFO: the most recently pushed frame (b's) is the first popped, when b returns. Frames never outlive the function they belong to." },
    { label: "The heap object lives on a separate memory chip, so stack returns cannot affect it.", misconception: "The stack and heap are regions of the same physical RAM. The object survives because of heap lifetime rules, not because it sits on different hardware." },
  ]}
/>

<Quiz
  id="02-memory-choice-6"
  lessonSlug="02-memory"
  lang="en"
  question="A recursive function with no base case keeps calling itself and the program crashes with a stack overflow. What actually happened, in terms of the unit's model?"
  choices={[
    { label: "Each call pushed a new stack frame and none was ever popped, so the stack region grew past its fixed size limit (typically 1–8 MB) and collided with its bound. It is a memory-region limit, not an arithmetic bug.", correct: true },
    { label: "The heap ran out of space because each call allocated a heap object.", misconception: "Stack overflow is about the stack region, not the heap. Each call pushes a frame onto the stack; with no base case, frames accumulate until the stack's fixed size is exceeded." },
    { label: "The CPU ran out of addresses for the byte values.", misconception: "The crash is the stack region exceeding its reserved size, not the machine running out of addresses. The address space is far larger than the stack's small fixed limit." },
    { label: "The values stored became too large for one byte each.", misconception: "Stack overflow is about the number of frames (depth of nesting), not the size of any single value. Unbounded recursion grows the frame count without bound." },
  ]}
/>

<Recap lang="en">
The through-line of this unit is one model: memory is a row of byte-addressed cells; every cell has a fixed address and a changeable value; a value can itself be an address, which is what makes a pointer, and following it (dereferencing) costs one extra read; and the same physical memory is split into a LIFO stack whose frames live exactly as long as their function and a heap whose objects can outlive any call. Every question above resolves back to telling address from value, and lifetime from location.
</Recap>
