infographic compare-binary-horizontal-underline-text-vs
data
  title Server cache vs client state — the shape question that matters most
  desc Fetched data has properties UI state never has; model it as client state and you hand-roll them all
  compares
    - label Server cache
      children
        - label Server cache — fetched data
        - label Goes stale, needs refetch
        - label Deduped across components
        - label Loading + error states, invalidation
        - label Tool: React Query / SWR / RTK Query
    - label Client state
      children
        - label Client state — UI-owned
        - label The server never knew it
        - label Tab, draft input, selection
        - label Shareable parts live in the URL
        - label Tool: useState / Context / Zustand
theme
  palette #6366f1 #14b8a6
