---
concepts:
- state-colocation
- derived-state
- single-source-of-truth
- normalization
- server-cache-state
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://react.dev/learn/choosing-the-state-structure
- https://kentcdodds.com/blog/state-colocation-will-make-your-react-app-faster
- https://redux.js.org/tutorials/essentials/part-6-performance-normalization
- https://frontendatlas.com/react/trivia/react-derived-state-anti-pattern
spiral: []
status: ready
summary: Most ''''state bugs'''' are shape bugs — a value stored that should be derived,
  or server cache treated as client state. The shape decides your re-render blast
  radius and which bugs are even possible.
title: 'State shape: the design decision before any library'
track: frontend
unit: 01-state-shape
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A filter UI ships. Users report the count badge is wrong: "127 results" but the list shows 9. The fix is not a render bug — someone stored `count` in `useState` next to the `results` array. One `setResults` updated; the other did not. Two values that should have been one. The badge had been lying for three weeks before anyone filed it.
</Hook>

<Crux>Most state bugs are shape bugs: a value stored that should be derived, or server cache treated as client state.</Crux>

<Explanation>

## Shape is a decision, not a default

Before you reach for Redux, Zustand, Jotai, or React Query, you make a quieter decision that matters more: what is a piece of state, where does it live, and is it the source of truth or a copy of one? Get the shape right and most state-management "problems" never appear. Get it wrong and no library saves you — you just spread the same drift across a nicer API.

Three questions decide the shape of any value:

1. **Is it state at all, or is it derived?** If you can compute it from existing state during render, it is not state. Storing it creates a second source of truth that will drift.
2. **Whose state is it — client or server?** A list of orders fetched from an API is *server cache*, not client state. Modeling it as client state is the most common architectural mistake in frontend apps.
3. **Who reads it?** That answer — one component, a subtree, or the whole app — sets where it lives and how big your re-render blast radius is.

<FlowDiagram
  label="A decision tree for placing a value: if derivable, derive it; otherwise if it is server data use a cache library, and if it is client state put shareable view-state in the URL and private state at the lowest common ancestor."
  perRow={3}
  hue="var(--d-frontend)"
  nodes={[
    { id: "value", label: "A value", sub: "where to put it?" },
    { id: "derive", label: "Derive in render", sub: "not state" },
    { id: "server", label: "Cache library", sub: "React Query/SWR" },
    { id: "url", label: "The URL", sub: "shareable" },
    { id: "colocate", label: "Lowest reader", sub: "colocate" },
  ]}
  edges={[
    { from: "value", to: "derive", label: "derivable?" },
    { from: "value", to: "server", label: "server data?" },
    { from: "value", to: "url", label: "client + shareable?" },
    { from: "value", to: "colocate", label: "client + private?" },
  ]}
  caption="Decide shape before library: derivable → compute during render; fetched → a server-cache library; shareable/bookmarkable client state → the URL; private client state → the lowest common ancestor of its readers."
/>

## The derived-state trap

The badge bug above is the canonical failure. `count` is `results.length`. The moment you store it separately, you own an invariant the framework was happy to maintain for free: every code path that changes `results` must also change `count`. Miss one path — an error handler, an optimistic update, a websocket patch — and the two drift. The UI shows a value that was true at some past render and never got corrected.

The senior reflex: **if you can derive it, derive it.** Compute during render; reach for `useMemo` only when the computation is measurably expensive, not by default. Memoization is a performance tool, not a correctness tool — adding it does not make derived-vs-stored a wash, because a stored copy is still a second source of truth even when memoized.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Value</th>
        <th class="border border-border px-3 py-2 text-left">Stored as state?</th>
        <th class="border border-border px-3 py-2 text-left">Why</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>results</code> from an API</td>
        <td class="border border-border px-3 py-2">No — server cache</td>
        <td class="border border-border px-3 py-2">Owned by the server; needs staleness + refetch, not <code>useState</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>count = results.length</code></td>
        <td class="border border-border px-3 py-2">No — derived</td>
        <td class="border border-border px-3 py-2">Computable during render; storing it invites drift</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>isModalOpen</code></td>
        <td class="border border-border px-3 py-2">Yes — local UI state</td>
        <td class="border border-border px-3 py-2">Pure client concern, read by one subtree → colocate</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>selectedId</code></td>
        <td class="border border-border px-3 py-2">Yes — but store the id, not the object</td>
        <td class="border border-border px-3 py-2">The object is derivable from id + the cache; the id is the real state</td>
      </tr>
    </tbody>
  </table>
</div>

## Server cache is not client state

The single biggest shape mistake is loading server data into `useState`/Redux and treating it as if you own it. You don't. Server data has properties client state never has: it goes stale, it can be refetched, it needs deduping across components, it has loading and error states, it can be invalidated by a mutation elsewhere. When you model it as plain client state, you hand-roll all of that — usually badly. This is exactly why React Query / TanStack Query, SWR, and RTK Query exist: they are *server-cache* libraries, not state libraries, and the distinction is the whole point.

The practical split a senior draws:

- **Server cache** → React Query / SWR / RTK Query. Keyed by request, with staleness, refetch, dedupe.
- **Client state** → `useState` (local), Context or Zustand/Jotai (shared). Things the server never knew: which tab is open, draft form input, selection.
- **URL** → the source of truth for anything shareable or bookmarkable: filters, pagination, the open detail id. Storing those only in `useState` means a refresh loses them and a copied link is dead.

## Colocation sets the blast radius

State lives at the lowest common ancestor of everything that reads it — and no higher. Hoisting a value to a global store "so it's available" is the most common cause of app-wide re-render storms: every component subscribed to that store re-renders when it changes, even ones that never read the changed slice. Kent C. Dodds' point is blunt — moving state *down* to where it's used makes the app faster, because React only re-renders the subtree that owns it.

The cost is real at scale. A global store holding high-frequency state (mouse position, a live-updating counter, form keystrokes) re-renders every subscriber on every change unless you carefully select slices. Atom libraries (Jotai, Recoil) exist precisely to shrink that blast radius to the components reading a specific atom. But the cheapest fix is usually structural: don't hoist what one subtree owns.

<Inset kind="why" lang="en">
"Lift state up" (the React docs phrase) is correct but routinely over-applied. Lift to the *lowest* common ancestor, not to the top. Lifting to the root is how a tooltip's open/closed state ends up re-rendering a dashboard. The mirror-image rule — "push state down" — is what colocation actually means in practice.
</Inset>

## Normalization: the tradeoff for relational client state

When you *do* hold relational data in a client store (an offline-first app, an editor with local entities), shape it the way a database would: a flat map keyed by id, not nested arrays. `{ users: { byId: {...}, allIds: [...] } }` instead of users nested inside posts nested inside threads.

The tradeoff is concrete. Nested shape makes reads ergonomic (`post.author.name`) but updates a nightmare: change one user's name and you must find and rewrite every copy embedded across the tree — and miss one, and you get drift again, now at scale. Normalized shape makes updates O(1) (write one entry in `byId`) at the cost of joining at read time (`byId[post.authorId].name`). For anything you mutate frequently, normalize. Redux Toolkit's `createEntityAdapter` exists to make this default. For read-mostly server data, don't bother — let the cache library hold the raw response.

<TradeoffMatrix
  id="01-state-shape-tradeoff-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="An editor holds threads → posts → users on the client, and users get renamed and edited constantly. Pick the client-side shape."
  options={[
    { name: "Normalized: a flat byId map per entity (users.byId, posts.byId) joined at read time", summary: "Database-style shape.", correct: true, justification: "Right for frequently-mutated relational data. Each entity is stored exactly once, so renaming a user is a single O(1) write to users.byId[id] — every view that joins through that id sees it instantly, with zero drift. The cost is a join at read time (byId[post.authorId].name) and a little ceremony, which createEntityAdapter removes. This is why Redux Toolkit ships normalization as a default helper." },
    { name: "Nested: keep users embedded inside posts inside threads, as the API returned them", summary: "Mirror the payload.", justification: "Ergonomic to read (post.author.name) and fine for a render-once snapshot, but a trap under mutation: one user is copied into every post they touched, so a rename means finding and rewriting every embedded copy. Miss one and you get drift at scale — the exact bug normalization exists to prevent." },
    { name: "Store the raw API response in React Query and never normalize", summary: "Cache-only.", justification: "Correct when the data is read-mostly server cache — let the cache library hold the raw response and refetch on invalidation. Wrong here: the prompt says entities are edited constantly on the client, which is client-owned mutable state, not a server snapshot to re-pull." },
    { name: "One big useState object you spread-update on every edit", summary: "Ad-hoc.", justification: "Spreading deep nested objects on every keystroke is both error-prone (easy to drop a branch) and a re-render hazard (the whole tree is a new reference). It reinvents normalization badly. Use a byId map and update one entry." },
  ]}
/>

<Quiz
  id="01-state-shape-quiz-1"
  lessonSlug="01-overview"
  lang="en"
  question="A list of products is fetched from an API and shown in three components. Where should it live?"
  choices={[
    { label: "In a server-cache library (React Query / SWR), keyed by the request — it's server data, not client state", correct: true },
    { label: "In a global Redux store via useState-style setters", misconception: "Server data needs staleness, refetch, dedupe, and invalidation — modeling it as plain client state means hand-rolling all of that, usually with bugs." },
    { label: "In useState in each of the three components", misconception: "Three independent copies means three fetches and three sources of truth that drift; the data is one thing, owned by the server." },
    { label: "In the URL", misconception: "The URL is for shareable view state (filters, ids), not for caching a fetched payload." },
  ]}
/>

<Quiz
  id="01-state-shape-quiz-2"
  lessonSlug="01-overview"
  lang="en"
  question="You have results: Item[] in state and want to show how many there are. What's the senior move?"
  choices={[
    { label: "Compute results.length during render — it's derived, not state", correct: true },
    { label: "Add a count to useState and update it in every setResults call", misconception: "This is the derived-state trap: a second source of truth that drifts the moment one update path forgets to set it." },
    { label: "Store count in a global store so all components agree", misconception: "Globalising a derived value doesn't fix the drift — it spreads it, and adds a re-render blast radius on top." },
    { label: "Memoize count with useMemo so it can't go stale", misconception: "useMemo is a performance tool; a memoized copy is still a second source of truth. Deriving during render is what removes the drift." },
  ]}
/>

<DragOrder
  id="01-state-shape-drag-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="Order the questions a senior asks to decide where a value belongs:"
  items={[
    { id: "a", label: "Is it derivable from existing state? If yes, don't store it — compute during render" },
    { id: "b", label: "If it is state: is it server-owned (fetched) or client-owned (UI/draft/selection)?" },
    { id: "c", label: "Server-owned → a cache library (React Query/SWR), keyed by request" },
    { id: "d", label: "Client-owned and shareable/bookmarkable → the URL" },
    { id: "e", label: "Client-owned and private → lowest common ancestor of its readers (colocate)" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Decide shape before library. Derive what's derivable, treat fetched data as server cache (not client state), keep shareable view-state in the URL, and colocate the rest at the lowest reader. Shape decides which bugs are possible.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-state-shape-retrieval"
  lang="en"
  questions={[
    { q: "Explain to a teammate why storing count alongside a results array is a bug waiting to happen, and what to do instead.", a: "count and results.length are the same fact, so storing count makes a second source of truth. Now every code path that changes results — the happy fetch, an error handler that clears the list, an optimistic add, a websocket patch — must also update count. The framework will not enforce that for you. Miss one path and the two drift: the badge says 127 while the list shows 9, and it stays wrong until someone notices. The fix is to not store it: compute results.length during render. It is impossible for a value to drift from itself. Reach for useMemo only if that computation is measurably expensive, and even then understand it is a performance optimisation, not a correctness fix." },
    { q: "Why is 'server cache vs client state' the most important shape question, and how does it change your tooling?", a: "Fetched data has properties client state never has: it goes stale, it can be refetched, it needs deduping when many components want the same resource, it has loading and error states, and a mutation elsewhere can invalidate it. If you load it into useState or a plain Redux slice, you end up hand-rolling all of that — and usually shipping subtle bugs (double fetches, stale views after a mutation, no dedupe). Recognising it as server cache means reaching for React Query / SWR / RTK Query, which are cache libraries keyed by request, not state libraries. That leaves your actual client state small: open/closed UI, draft input, selection — plus the URL for anything shareable. The split keeps each kind of state in the tool built for it." },
  ]}
/>

<Recap lang="en">
The shape of state is decided before any library, and it decides which bugs are possible. Anything derivable should be derived during render, never stored, or it becomes a second source of truth that drifts. Fetched data is server cache — give it to a cache library that handles staleness, refetch, dedupe, and invalidation, instead of modeling it as client state by hand. Shareable view-state belongs in the URL; the rest is colocated at the lowest common ancestor of its readers, which keeps the re-render blast radius small. When you genuinely hold relational data on the client, normalize it into a byId map so updates stay O(1) and can't drift. Get the shape right and most "state-management problems" simply never appear.
</Recap>
