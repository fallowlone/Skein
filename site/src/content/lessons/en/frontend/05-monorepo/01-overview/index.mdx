---
slug: 01-overview
lang: en
track: frontend
unit: 05-monorepo
order: 1
level: junior
lessonType: topic
status: ready
title: "Monorepo boundaries: the dependency graph decides your CI bill"
summary: "A monorepo is only fast if the build tool knows the dependency graph and runs only what changed. Get the boundaries wrong — one 'shared utils' package everyone imports — and every PR rebuilds the world."
prereqs: []
deepensInto: []
spiral: []
concepts: ["dependency-graph", "affected-graph", "remote-task-caching", "module-boundaries", "monorepo-vs-polyrepo"]
estMin: 16
sources:
  - https://nx.dev/docs/features/enforce-module-boundaries
  - https://turborepo.dev/docs/core-concepts/remote-caching
  - https://engineering.mercari.com/en/blog/entry/20260216-turborepo-remote-cache-accelerating-ci-to-move-fast/
  - https://vercel.com/blog/turborepo-remote-cache-nextjs-publish-times-80-percent
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
A two-line copy fix to a marketing page sits in CI for 41 minutes. The change touched one component, but the pipeline rebuilt and tested all 38 projects, because the import resolved through `@acme/utils` — a "shared utils" package that every app and library in the repo depends on. Touch anything that touches utils, and the affected graph is the whole repo. The team's verdict in retro: "the monorepo is slow." The monorepo wasn't slow. The boundary was wrong.
</Hook>

<Crux>A monorepo is only fast if the graph is right: build what changed and its dependents, not the world, every PR.</Crux>

<Explanation>

## The dependency graph is the product

A monorepo is not "all the code in one folder." That's just a folder. The thing that makes a monorepo fast or slow is the **dependency graph** — the directed graph of which package imports which. Turborepo and Nx build this graph by parsing your imports and `package.json` workspaces, then use it for two jobs: ordering (build a library before the app that consumes it) and scoping (figure out the blast radius of a change).

Every node is a package or project; every edge is a dependency. A healthy graph is wide and shallow: many leaf libraries, few things depending on any one of them. A sick graph has a **hub** — one node with a huge fan-in that sits on the path between everything. That hub is usually called `utils`, `common`, or `shared`, and it is the single most expensive mistake in a monorepo, because the affected-graph algorithm is only as good as the graph you give it.

## "Affected" vs build-everything

The naive CI pipeline runs `build && test` across every package on every PR. At 5 packages nobody notices. At 40, a full run is 30–45 minutes and your merge queue backs up. The fix is **affected** (Nx) / **filtered** (Turborepo): compute the diff against the base branch, find which projects changed, walk the graph to add every project that *depends* on a changed one, and run tasks only for that set.

The numbers are dramatic when the graph is clean. A 20-package workspace commonly drops from ~14 min (build everything) to 3–5 min (affected only). When a change is local to a leaf, the affected set can be a single project and CI finishes in under a minute. The whole bet of a monorepo rests on this: most PRs touch a small slice, so most PRs should pay for a small slice.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Strategy</th>
        <th class="border border-border px-3 py-2 text-left">What runs per PR</th>
        <th class="border border-border px-3 py-2 text-left">~40-pkg CI time</th>
        <th class="border border-border px-3 py-2 text-left">Failure mode</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Build everything</td>
        <td class="border border-border px-3 py-2">All projects, always</td>
        <td class="border border-border px-3 py-2">30–45 min</td>
        <td class="border border-border px-3 py-2">Scales with repo size, not change size</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Affected only</td>
        <td class="border border-border px-3 py-2">Changed projects + their dependents</td>
        <td class="border border-border px-3 py-2">1–5 min (typical PR)</td>
        <td class="border border-border px-3 py-2">A hub package makes "affected" = everything</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Affected + remote cache</td>
        <td class="border border-border px-3 py-2">Only tasks with no cached output</td>
        <td class="border border-border px-3 py-2">seconds–2 min on a warm cache</td>
        <td class="border border-border px-3 py-2">Cache key too broad → false misses</td>
      </tr>
    </tbody>
  </table>
</div>

## Remote caching: the second multiplier

Affected scoping says "don't run untouched projects." Remote caching says "don't even run touched projects if someone already produced this exact output." Turborepo and Nx hash every input to a task — source files, dependencies, env vars, the task config — and store the output (build artifacts, test results, logs) under that hash in a shared cache. If a teammate, or your last CI run, already built that hash, the task is a **cache hit**: the output is downloaded instead of recomputed, in milliseconds.

This is what closes the loop between developers and CI. Vercel measured an 80% drop in publish times for Next.js once SWC builds were shared through the remote cache. Mercari reported roughly a 50% reduction in Turbo task duration and a 30% reduction in total job duration after wiring Turborepo's remote cache into GitHub Actions. A common warm-cache figure is CI going from ~6 minutes to ~45 seconds. Cache hit rate is the metric to watch: in a many-package repo where most PRs are local, hit rates of 80–90% are achievable, and each point of hit rate is CI minutes (and money) you don't spend.

<Inset kind="why" lang="en">
The cache key is the whole game. If you forget to include an input — say, a shared `tsconfig` or an env var that changes output — you get a *false hit*: the cache serves a stale artifact and ships a bug. If you include too much — a volatile file that changes every run, or an over-broad glob — you get a *false miss*: the hash never repeats, the cache is useless, and you've paid the storage cost for nothing. Tuning cache hit rate is mostly tuning what counts as an input.
</Inset>

## Boundaries: stop the hub before it forms

The reason "shared utils" wrecks the affected graph is structural, not a tooling bug: if 30 projects import it, then by definition a change to it affects 30 projects, so the cache invalidates for all of them and CI rebuilds the world. The fix is to **enforce module boundaries** so the graph can't degenerate into a hub-and-spoke mess in the first place.

Nx does this with **tags** and an ESLint rule, `@nx/enforce-module-boundaries`. You tag each project (`type:feature`, `type:ui`, `scope:checkout`) and declare `depConstraints`: a feature may depend on ui, ui may depend only on util, nothing may depend on a feature. Imports that violate the rule fail lint — and the same rule catches **circular dependencies** ("Circular dependency between A and B detected"), which are the other way a graph turns to spaghetti. Turborepo leans on **package boundaries** and `package.json` `exports` to make a package's public surface explicit, so consumers can't reach into its internals. The senior move is to split the god-package: instead of one `utils` everyone imports, ship `@acme/format-date`, `@acme/http`, `@acme/result`, each with a narrow fan-in, so a change to date formatting invalidates only the handful of things that actually format dates.

<TradeoffMatrix
  id="05-monorepo-overview-tradeoff-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="CI hits 40+ min: every PR rebuilds the repo because everything imports one @acme/utils. Pick the fix that addresses the cause."
  options={[
    { name: "Split @acme/utils into narrow packages and enforce tags so most changes have a small fan-in", summary: "Fix the graph.", correct: true, justification: "This attacks the actual cause: the hub. With format-date, http, and result as separate leaf packages, a change to one invalidates only its real consumers, so the affected graph and cache invalidation shrink to a slice. Enforcing module boundaries (Nx tags / package exports) keeps the graph from re-collapsing into a hub. Everything else — caching, parallelism, bigger runners — only papers over a graph that is structurally everything-depends-on-everything." },
    { name: "Add a remote cache and call it done", summary: "Cache the world.", justification: "Remote caching is essential and you should have it, but it does not fix a hub. The whole point of a hub is that a change to it produces a new input hash for every dependent, so every dependent is a cache miss. You'd cache-hit on PRs that don't touch utils and still rebuild the world on every PR that does — which is the common case the prompt describes." },
    { name: "Buy bigger CI runners and crank up parallelism", summary: "Throw hardware.", justification: "More cores run the same 40 builds faster, but you are still building 40 projects for a one-line change — paying linearly with repo size, not change size. It buys a little headroom and a bigger bill, and the 41-minute job comes back as the repo grows. It treats the symptom, not the hub." },
    { name: "Split the monorepo into many polyrepos", summary: "Go polyrepo.", justification: "This can be right for truly independent teams, but it trades the CI problem for a coordination problem: shared code now ships through a package registry with version bumps and lockstep PRs across repos. For a codebase that genuinely shares this much (everyone imports utils), polyrepo usually makes change harder, not faster. Fix the boundary first." },
  ]}
/>

## Monorepo vs polyrepo, honestly

The monorepo's superpower is the **atomic change**: rename a function in a shared library and update every caller in one PR, with one CI run proving it all still compiles. No version bumps, no registry, no "deploy the lib, wait, bump consumers, deploy them." The cost is real: you need the graph-aware tooling (Nx/Turborepo/Bazel), remote caching, and enforced boundaries, or the repo collapses under its own CI. Polyrepo flips it: each repo is trivially fast and independent, but shared code travels through a registry, and a breaking change in a shared package means coordinated bumps, version drift, and "which service is on the old version" spelunking. The honest framing: monorepo trades **higher tooling complexity for lower coordination cost**; polyrepo trades **lower tooling complexity for higher coordination cost**. Pick monorepo when packages share a lot and change together; pick polyrepo when teams are truly independent and communicate only over APIs.

<Quiz
  id="05-monorepo-overview-quiz-1"
  lessonSlug="01-overview"
  lang="en"
  question="In a 40-package monorepo, what does 'affected' (Nx) / 'filtered' (Turborepo) actually compute for a PR?"
  choices={[
    { label: "The changed projects plus every project that transitively depends on them, by walking the dependency graph", correct: true },
    { label: "Every project in the repo, just in parallel", misconception: "That's build-everything. Affected exists precisely to avoid running untouched projects; running all of them in parallel still scales with repo size." },
    { label: "Only the exact files in the git diff, ignoring dependents", misconception: "Files aren't the unit — projects are. And you must include dependents: a change to a library can break the apps that import it, so they must be tested too." },
    { label: "Whatever isn't in the remote cache", misconception: "That's the cache's job, a separate step. Affected scoping decides which projects are candidates; caching then decides which of those can be skipped because their output already exists." },
  ]}
/>

<Quiz
  id="05-monorepo-overview-quiz-2"
  lessonSlug="01-overview"
  lang="en"
  question="Your remote cache hit rate is high, but one PR served a stale build that shipped a bug. Most likely cause?"
  choices={[
    { label: "A false hit: an input that affects the output (a shared tsconfig or env var) wasn't part of the cache key, so a changed build hashed to an old entry", correct: true },
    { label: "The cache was simply too small and evicted the right entry", misconception: "Eviction causes a miss (a rebuild), not a stale artifact. A stale-but-served artifact means the hash matched when it shouldn't have — a missing input." },
    { label: "Affected scoping skipped the project entirely", misconception: "If affected skipped it, nothing was served at all for that project. A stale served artifact means a task ran, hit the cache, and got the wrong entry — a key problem." },
    { label: "The hit rate was too high, which is inherently unsafe", misconception: "A high hit rate isn't the bug; an incorrect key is. Correct keys give correct hits. The goal is high hit rate with a key that captures every real input." },
  ]}
/>

<DragOrder
  id="05-monorepo-overview-drag-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="Order how a graph-aware tool decides what to run for a PR:"
  items={[
    { id: "a", label: "Build the dependency graph from imports + workspace package.json files" },
    { id: "b", label: "Diff the PR against the base branch to find directly changed projects" },
    { id: "c", label: "Walk the graph to add every project that depends on a changed one (the affected set)" },
    { id: "d", label: "Hash each affected task's inputs and check the remote cache for that hash" },
    { id: "e", label: "Run only the tasks that miss; download outputs for the tasks that hit" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>The dependency graph decides your CI bill. Run affected-only, share a remote cache, and enforce module boundaries so no "shared utils" hub makes every PR rebuild the world.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-monorepo-overview-retrieval"
  lang="en"
  questions={[
    { q: "Explain why a single 'shared utils' package can push monorepo CI from 4 minutes to 40, and what to do about it.", a: "The affected graph works by walking dependents: when a project changes, every project that imports it (directly or transitively) is in the affected set and must be rebuilt and retested. A 'shared utils' package is a hub — if 30 of your 38 projects import it, then any change to utils, by definition, affects 30 projects. Worse, because utils is an input to all of those builds, its hash change makes all of them cache-miss, so remote caching can't save you either: every dependent has to actually run. The result is that a one-line change anywhere near utils rebuilds nearly the whole repo, which is exactly the 40-minute job. The fix is structural, not a tooling tweak: split the god-package into narrow leaf packages (format-date, http, result) so each has a small fan-in, and enforce module boundaries (Nx tags with depConstraints, or package.json exports) so the graph can't re-collapse into a hub. After the split, a change to date formatting invalidates only the handful of projects that format dates, and both affected scoping and the cache do their job." },
    { q: "What's the difference between affected scoping and remote caching, and why do you need both?", a: "They solve different halves of the same problem. Affected scoping answers 'which projects could this change have broken?' — it diffs the PR against the base, finds changed projects, and walks the graph to add their dependents, so untouched projects never run at all. Remote caching answers 'of the projects that are in scope, which actually need to run?' — it hashes each task's inputs and, if that exact hash was already built (by you, a teammate, or a prior CI run), downloads the stored output instead of recomputing it. You need both because each leaves a gap the other fills. Affected alone still rebuilds an in-scope project every time, even if its inputs are identical to a previous run. Caching alone still considers every project a candidate, wasting time hashing and checking projects that the diff proves are irrelevant. Together: affected narrows the candidate set to the change's blast radius, then the cache turns most of those candidates into instant hits — which is how a clean monorepo serves an 80–90% cache hit rate and finishes most PRs in seconds." },
  ]}
/>

<Recap lang="en">
A monorepo is fast or slow depending on one thing: the dependency graph. Graph-aware tools (Nx, Turborepo) parse imports into a directed graph and use it to compute the affected set — the changed projects plus everything that depends on them — so most PRs build a slice instead of the world, often dropping a 40-package repo from 30–45 minutes to 1–5. Remote caching multiplies this by hashing each task's inputs and downloading stored outputs on a hit, which is how teams report 50–80% CI reductions and warm-cache runs in seconds; the catch is the cache key, where a missing input causes a dangerous false hit and an over-broad one causes useless false misses. None of it survives a bad graph: a single "shared utils" hub makes the affected set the whole repo and cache-misses every dependent, so the discipline that actually keeps a monorepo fast is enforced module boundaries — Nx tags, the enforce-module-boundaries lint rule, and explicit package exports — that split god-packages and ban circular and cross-domain imports. Against polyrepo, the trade is honest: monorepo buys atomic cross-package changes at the price of graph-aware tooling and boundary discipline; polyrepo buys trivial per-repo speed at the price of registry versioning and cross-repo coordination.
</Recap>
