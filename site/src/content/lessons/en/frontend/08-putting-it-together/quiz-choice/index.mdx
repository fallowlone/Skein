---
concepts:
- state-shape
- fetch-waterfall
- design-tokens
- monorepo-boundaries
- code-splitting
- failure-cascade
estMin: 14
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://kentcdodds.com/blog/state-colocation-will-make-your-react-app-faster
- https://web.dev/articles/lcp
- https://atlassian.design/foundations/tokens/design-tokens
status: ready
summary: "Cross-track multiple-choice synthesis: read a frontend symptom, locate the layer that actually owns the bug, and pick the fix that does not just mask it."
title: 'Putting it together: multiple-choice review'
track: frontend
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Every question here crosses two or three units. The skill the capstone tests is not recall of one layer — it is reading a symptom and naming the lowest layer that owns it, because that is where the durable fix lives.
</Hook>

<Goal lang="en">
Confirm you can trace a frontend symptom down the cascade — state shape, fetch graph, tokens, monorepo boundaries, splitting, pipeline — and reject fixes that optimise a layer above the real cause.
</Goal>

<Quiz
  id="08-putting-it-together-choice-1"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="A dashboard janks on every keystroke in its search box. A teammate files a ticket to code-split and lazy-load the charts. What is the senior read?"
  choices={[
    { label: "It is a state-shape bug: keystrokes are local UI state hoisted into a global store, so every subscriber re-renders on every character. Splitting changes the initial bundle, not the re-render that fires after the chunk loads — it fixes nothing. Colocate the keystroke state.", correct: true },
    { label: "Code-splitting the charts will fix it by reducing what re-renders.", misconception: "Splitting decides what is in the initial bundle and when chunks arrive; it does not change which subscribers re-render. The chart still re-renders after its chunk loads, because the cause is where the keystroke state lives, not bundle size." },
    { label: "Wrap the chart in React.memo and move on.", misconception: "Memoising one component can mute one symptom, but the global store still notifies every subscriber on every keystroke. You are patching a downstream node instead of fixing the state shape that set the blast radius." },
    { label: "Upgrade the bundler for faster builds.", misconception: "Build speed is unrelated to runtime re-renders. This optimises the cheapest layer (the pipeline) while the cause sits in the most expensive one (state shape)." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-2"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="A product page renders a skeleton, then JS hydrates, then it fetches the user, then the cart, then recommendations — each waiting on the previous. LCP is 4.8 s. Which layer owns this, and what is the fix?"
  choices={[
    { label: "The data-fetching layer: client fetch after JS plus a sequential waterfall adds several round-trips to the critical path. Move fetches to the server (or RSC) and parallelise independent ones with Promise.all so LCP collapses toward one round-trip — target under 2.5 s.", correct: true },
    { label: "The bundle is too big — code-split the page to drop LCP.", misconception: "A smaller bundle helps Time-to-Interactive, but it cannot remove serialised round-trips. Even with instant JS, three sequential client fetches still serialise; the waterfall, not the bundle, owns this LCP." },
    { label: "Add a loading spinner so the wait feels shorter.", misconception: "A spinner changes perceived UX, not LCP, which measures the largest content paint. The cause is a sequential fetch graph on the critical path, and that is what must change." },
    { label: "It is a state-shape problem — normalise the fetched data.", misconception: "Normalisation shapes how data is stored and read, not when it arrives. The latency here is the sequential round-trips before any data exists; normalising afterwards changes nothing about LCP." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-3"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="Marketing asks for a rebrand: a new brand color plus a dark mode. The codebase has the brand hex hardcoded across ~300 components. What does this reveal, and what is the structural fix?"
  choices={[
    { label: "The token layer is missing. A primitive then semantic then component structure makes a rebrand a swap of primitive values and dark mode a swap of semantics with components untouched. A documented dark-mode migration took two days with a semantic layer and would have been weeks of find-and-replace without it.", correct: true },
    { label: "Run a global find-and-replace of the hex value; it is a one-time cost.", misconception: "Find-and-replace handles one rebrand and leaves you exactly as fragile for the next request and for dark mode, where the same hex means different things in different contexts. The point of a semantic layer is that meaning, not raw value, is referenced." },
    { label: "It is a build-pipeline issue — add a PostCSS plugin to rewrite colors.", misconception: "Rewriting colors at build time still has no source of truth for what a color means; you cannot express text-danger versus surface-raised. The missing artifact is a semantic token layer, not a bundler transform." },
    { label: "Theme it with CSS overrides per page.", misconception: "Per-page overrides multiply the inconsistency tokens are meant to remove and do not give dark mode a single switch. Centralised semantic tokens are the source of truth; scattered overrides are the anti-pattern." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-4"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="CI takes 15 minutes for a one-line change in one of 12 packages because everything imports one giant shared package. What attacks the root cause?"
  choices={[
    { label: "Real package boundaries plus affected-only execution, so a one-package change builds one package; then a remote cache shares work across runs. Mercari cut Turborepo task time ~50%, and affected-only plus cache drops a 15-minute build to 2–3 minutes on a single-package change.", correct: true },
    { label: "Buy faster CI runners.", misconception: "Faster hardware rebuilds the whole dependency graph faster; it never stops rebuilding things that did not change. The 15 minutes come from a boundary problem — every leaf depends on the trunk — not from horsepower." },
    { label: "Turn on the bundler's tree-shaking.", misconception: "Tree-shaking trims shipped bytes per bundle; it does not change which packages CI decides to rebuild. The build-time explosion is a dependency-graph problem upstream of any bundling step." },
    { label: "Split the app into more route chunks.", misconception: "Route chunks affect what the browser downloads at runtime, not what CI rebuilds. The cause is that the dependency graph forces a full rebuild on any change." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-5"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="You are handed a new frontend to review. In which order does a senior evaluate the layers, and why that order?"
  choices={[
    { label: "Bottom-up by failure cost: state shape, then fetch graph, then tokens, then monorepo boundaries, then code splitting, then build pipeline. Lower layers constrain upper ones, so you find the lowest broken layer first and fix there, where the symptom and everything it spawned disappear at once.", correct: true },
    { label: "Top-down by file structure: folders, then components, then state, then build config.", misconception: "Reading by file structure hides the dependency direction. You can finish admiring a clean folder tree and still miss that a state-shape decision is causing the jank — the layers cascade by failure cost, not by where files sit." },
    { label: "Start with the build pipeline because its wins are the most measurable.", misconception: "The pipeline is the cheapest layer to fix later and the least likely to be the cause. Optimising bytes while the state shape is broken is waxing a car with a seized engine — the surface looks faster, the machine is not." },
    { label: "Order does not matter as long as every layer is eventually checked.", misconception: "Order is the whole lesson. A fix applied above the real cause is wasted work that also hides the bug, so reviewing in failure-cost order is what lets you fix the source instead of a symptom." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-6"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="A heavy charting library loads on first paint of every route even though most users never open the analytics tab, and a screen-reader user reports the date-picker inside it traps focus. Which two-layer fix is right?"
  choices={[
    { label: "Two independent layers: code-split the analytics route so the chart library lazy-loads only when that tab opens (cutting initial bundle and TTI), and fix the forms/a11y bug separately with proper focus management and a working escape path. Splitting never fixes the focus trap; a11y never fixes the bundle.", correct: true },
    { label: "Lazy-load the charts — that also resolves the focus trap by deferring the buggy component.", misconception: "Deferring the load only delays when the buggy component mounts; the focus trap returns the moment a user opens the tab. Accessibility and code-splitting are orthogonal layers — each needs its own fix." },
    { label: "Leave the chart eagerly loaded so the date-picker is always available to assistive tech.", misconception: "Eager loading bloats the initial bundle for every user to no benefit, and it does nothing for the focus trap, which is a focus-management bug regardless of when the component loads." },
    { label: "Move the chart into a separate monorepo package; that solves both.", misconception: "A package boundary changes build and ownership, not what ships to the browser on first paint or how focus behaves at runtime. You still need route-level splitting for the bundle and a focus fix for a11y." },
  ]}
/>

<Recap lang="en">
The through-line across the track is one decision tree. Where state lives sets the re-render blast radius; the fetch graph owns LCP; tokens decide whether a rebrand is a value swap or a file hunt; monorepo boundaries are the build-time multiplier; splitting and the pipeline are the cheapest layers. Every question above is the same move — read the symptom, drop to the lowest layer that owns it, and reject any fix that optimises a layer above the cause.
</Recap>
