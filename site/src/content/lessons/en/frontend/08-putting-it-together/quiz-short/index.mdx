---
concepts:
- state-shape
- fetch-waterfall
- design-tokens
- monorepo-boundaries
- code-splitting
- failure-cascade
estMin: 13
lang: en
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://kentcdodds.com/blog/state-colocation-will-make-your-react-app-faster
- https://web.dev/articles/lcp
- https://atlassian.design/foundations/tokens/design-tokens
- https://engineering.mercari.com/en/blog/entry/20260216-turborepo-remote-cache-accelerating-ci-to-move-fast/
status: ready
summary: "Free-recall prompts spanning the whole frontend track — state, fetch, tokens, monorepo, splitting, and the cascade. Answer from memory, then reveal the model answer."
title: 'Putting it together: free-recall review'
track: frontend
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. Each prompt spans several units at once — answer it fully from memory before revealing the model answer, because reconstructing the connection between layers is exactly the senior skill this capstone tests.
</Hook>

<Goal lang="en">
Reconstruct the whole track as one cascade — why state shape sits at the bottom, how the fetch graph owns LCP, what a semantic token layer buys, why boundaries are the build-time multiplier, and how to review it all in failure-cost order.
</Goal>

<RetrievalDrawer
  client:load
  id="08-putting-it-together-short-retrieval"
  lang="en"
  questions={[
    { q: "Why does a senior review a frontend bottom-up by failure cost rather than top-down by file structure, and which layers are most vs least expensive to get wrong?", a: "Because the layers form a dependency chain where lower layers constrain upper ones, so a fix applied above the real cause is wasted work that also hides the bug. State shape is the most expensive: it sets the re-render blast radius, and fetching, splitting, and the pipeline all inherit it. Next is the fetch graph (waterfalls serialise round-trips and own LCP, target under 2.5 s), then tokens (no semantic layer turns a one-line rebrand into a multi-week file hunt), then monorepo boundaries (the build-time multiplier — bad boundaries rebuild the whole graph). Code splitting and the build pipeline are last because they are the cheapest to fix — config changes, not architecture changes. Reviewing in failure-cost order means you find and fix the lowest broken layer first, so its symptom and every symptom it spawned disappear at once." },
    { q: "A dashboard janks on every keystroke and the team wants to code-split the charts. Walk through finding the real layer and why their fix fails.", a: "Profile before trusting the diagnosis — re-render jank looks like a bundle problem but rarely is. The profiler shows the whole subtree, charts included, re-rendering on every keystroke, which is a state-shape question: where do the keystrokes live? They have been hoisted into a global store, so every subscriber re-renders on every character. Code-splitting changes what is in the initial bundle and when chunks load; it does nothing to the re-render that fires after the chunk arrives, so the chart still janks and the ticket ships without fixing anything. The durable fix is a lower layer: colocate the keystroke state at the search box so only that subtree re-renders. That is the cascade rule — a symptom in an upper layer (rendering/bundle) whose cause is a lower layer (state shape)." },
    { q: "How does the data-fetching layer decide LCP, and what turns a slow page into a fast one?", a: "Where and how you fetch decides how many round-trips sit on the critical path before the largest content can paint. A client fetch after JS hydrates adds the JS download and parse, then two to four sequential trips if the fetch graph is a waterfall — each request waiting on the previous even when they share no data dependency. A server fetch (or RSC) collapses the critical path toward a single round-trip because the data arrives with the HTML. The two fixes are: move fetching to the server, and parallelise independent requests with Promise.all (or by removing false `enabled` gates) so they overlap instead of serialising. The target is LCP under 2.5 s, and waterfalls are the silent killer because each looks innocent in isolation." },
    { q: "Explain the three-layer token structure and what each layer buys when a rebrand or dark mode lands.", a: "Mature design systems structure tokens in three layers: primitives (the raw palette and scale, like a specific blue or an 8px step), semantics (what a value means in context — text-danger, surface-raised, the layer components actually reference), and component values (per-component overrides where needed). A rebrand swaps primitive values and everything above re-resolves automatically. Dark mode swaps the semantic mapping — surface-raised points to a dark value — while components, which only ever referenced semantics, stay untouched. The payoff is concrete: a documented 200-component dark-mode migration took two days with a semantic layer and would have been weeks of hunt-and-replace without one. The anti-pattern is hardcoding raw hex everywhere, which has no indirection and no source of truth for meaning." },
    { q: "Why are monorepo boundaries the build-time multiplier, and what two levers cut CI time the most?", a: "A bundler and CI obey your dependency graph. If packages have no real boundaries — everything imports one giant shared package — then any change invalidates the cache for the whole graph and CI rebuilds everything, so a one-line edit costs a full build. The biggest lever is affected-only execution: with clean boundaries a one-package change builds one package, so running 4 tasks instead of 45 beats any caching tweak. The second lever is a remote cache that shares work across runs and developers, eliminating 80–90% of total build time for a large team. Together they take a 15-minute build to 2–3 minutes on a single-package change; Mercari reported roughly a 50% cut in Turborepo task duration and ~30% in total CI time after adding remote caching. A faster bundler cannot rescue a graph where every leaf depends on the trunk — the fix is structural, not hardware." },
    { q: "When is code-splitting the right tool versus the wrong one, and how do splitting and the build pipeline relate to the layers below them?", a: "Code-splitting is right when a chunk of code is heavy and rarely needed on first paint — the analytics route, a heavy chart library, an admin-only editor — so a dynamic import() defers its download until the user actually needs it, cutting the initial bundle and Time-to-Interactive. It is the wrong tool when reached for to fix a runtime re-render or a slow fetch, because the chunk loads after the wasted render and changes nothing there. The build pipeline (tree-shaking, dead-code removal, minification, CI integration) is the cheapest layer of all — Chrome's Coverage panel routinely shows 20–30% of shipped JS never executes, real bandwidth to recover — but it is a config change, not an architecture change, so a senior tunes it last. Both layers assume the ones below them are sound; splitting a bundle or trimming bytes while the state shape or fetch graph is broken optimises the surface while the machine still stalls." },
  ]}
/>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the track's spine: state shape sets the re-render blast radius, the fetch graph owns LCP, a primitive-to-semantic token layer makes a rebrand a value swap, clean monorepo boundaries plus affected-only execution and a remote cache are the build-time multiplier, and code splitting and the pipeline are the cheapest layers to fix last. The recurring move is to read a symptom, drop to the lowest layer that owns it, and fix there — never patch a layer above the cause. Now when you see a janky dashboard or a slow CI, you know exactly which question to ask first — and which layer to open.
</Recap>
