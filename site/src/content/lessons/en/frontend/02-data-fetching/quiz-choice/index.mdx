---
concepts:
- fetch-location
- fetch-waterfall
- rsc-streaming
- swr-cache
- lcp-critical-path
- cache-invalidation
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://react.dev/reference/rsc/server-components
- https://tanstack.com/query/v5/docs/framework/react/guides/request-waterfalls
- https://web.dev/articles/optimize-lcp
status: ready
summary: "Multiple-choice synthesis across the data-fetching unit — fetch location, waterfalls, RSC streaming, the SWR cache, LCP, and multi-layer invalidation."
title: 'Data fetching: multiple-choice review'
track: frontend
unit: 02-data-fetching
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one mirrors a call you make in a real perf review — not a definition to recite, but a tradeoff to weigh against an LCP budget and a stale-data risk.
</Hook>

<Goal lang="en">
Confirm you can connect fetch location, waterfalls, RSC streaming, the client cache, and multi-layer invalidation into one decision: which round-trips are on the critical path, and what each fix actually buys.
</Goal>

<Quiz
  id="02-data-fetching-choice-1"
  lessonSlug="02-data-fetching"
  lang="en"
  question="A product page renders its title (the LCP element) from a client useEffect fetch. LCP is 2.9s. The team adds a loading skeleton and bumps the CDN cache TTL. LCP barely moves. What is the actual fix?"
  choices={[
    { label: "Move the title's fetch to the server (RSC or SSR) so the HTML arrives with the title already inside — this removes JS-download + mount + fetch from the LCP critical path, the only chain that sets LCP here.", correct: true },
    { label: "Add fetchpriority='high' to the title text node.", misconception: "fetchpriority is for resource discovery (images, fonts), not a JS-triggered API call. The title is gated behind bundle download and mount, which fetchpriority cannot touch." },
    { label: "Raise the CDN TTL further so the skeleton is cached.", misconception: "Caching the skeleton HTML changes nothing — the LCP element is the title, and it still waits for the client-side fetch after JS runs. CDN TTL on the shell does not collapse that chain." },
    { label: "Switch the API to HTTP/3 to cut connection overhead.", misconception: "HTTP/3 trims a little handshake latency but leaves the sequential bundle-then-mount-then-fetch ordering intact. The win is server-side fetch, not a faster transport." },
  ]}
/>

<Quiz
  id="02-data-fetching-choice-2"
  lessonSlug="02-data-fetching"
  lang="en"
  question="A profile page fetches user (200ms), posts (300ms), and followers (150ms) — all independent — via three nested useEffect components. Total is ~650ms. Why, and what bounds it to ~300ms?"
  choices={[
    { label: "React renders top-down: each child mounts only after its parent's effect data arrives, so the render cascade serialises independent fetches. Hoisting them to a parent Promise.all (or sibling RSC components) caps total at the slowest single fetch, ~300ms.", correct: true },
    { label: "The fetches share a connection pool that allows only one at a time.", misconception: "The browser allows several concurrent connections per origin; the serialisation here is the React render cascade, not connection limits. Promise.all fires all three at once regardless." },
    { label: "Total time is already max(200,300,150) = 300ms; the 650ms is measurement noise.", misconception: "Nested useEffect fetches genuinely sum because each child cannot mount until its parent re-renders with data. 650ms is the real waterfall, not noise." },
    { label: "Add a longer staleTime so the cache absorbs the latency.", misconception: "staleTime governs refetch timing on revisits, not the first-paint waterfall. On a cold load there is nothing cached; the fetches still serialise." },
  ]}
/>

<Quiz
  id="02-data-fetching-choice-3"
  lessonSlug="02-data-fetching"
  lang="en"
  question="A team marks every component 'use client' in a Next.js App Router app, then complains RSC gave them no bundle win. What happened?"
  choices={[
    { label: "The zero-JS benefit of Server Components only applies to components that stay server-side. Marking everything 'use client' ships the whole tree to the browser — the same bundle as a plain SPA, plus RSC complexity. The fix is Server Component by default, 'use client' only on interactive leaves.", correct: true },
    { label: "RSC never reduces bundle size; that is a marketing claim.", misconception: "RSC genuinely ships zero JS for server components — a real app can drop from ~240KB to ~60KB. The team defeated it by opting every component into the client." },
    { label: "They needed to also enable streaming to get the bundle win.", misconception: "Streaming lowers TTFB, not bundle size. The bundle win comes purely from keeping non-interactive components on the server, which 'use client' everywhere prevents." },
    { label: "The build tool failed to tree-shake the client components.", misconception: "Tree-shaking removes dead code, not legitimately-used interactive components. Every 'use client' leaf is real code the browser must run; that is why the bundle stayed large." },
  ]}
/>

<Quiz
  id="02-data-fetching-choice-4"
  lessonSlug="02-data-fetching"
  lang="en"
  question="An RSC page fetches data server-side, then a client component calls useQuery for the same data on mount — every page load does two round-trips for one dataset. What is the correct fix?"
  choices={[
    { label: "Dehydrate the server's query cache and rehydrate on the client: prefetchQuery on the server, embed dehydrate(queryClient) in the HTML, wrap the tree in HydrationBoundary. useQuery then finds the data in cache and skips the redundant fetch.", correct: true },
    { label: "Remove useQuery and rely only on the RSC fetch.", misconception: "The client component needs the query for post-mount interactivity (background revalidation, mutations). Dropping it loses that; the right move is to seed its cache, not delete it." },
    { label: "Set staleTime to 0 so the client always uses fresh data.", misconception: "staleTime=0 makes the client refetch immediately on mount — it guarantees the double fetch instead of preventing it. Dehydration is what removes the second trip." },
    { label: "Add a CDN cache in front of the API so the second fetch is cheap.", misconception: "A cheap second fetch is still a second round-trip and still costs the client a request. The dual-fetch trap is fixed by sharing the server's already-fetched data via hydration, not by caching the duplicate." },
  ]}
/>

<Quiz
  id="02-data-fetching-choice-5"
  lessonSlug="02-data-fetching"
  lang="en"
  question="A search-as-you-type box fires a fetch per keystroke. The user types 'react' but sees results for 'reac'. None of the queries error. Root cause and best fix?"
  choices={[
    { label: "Out-of-order responses: the 'reac' request resolved after 'react' and overwrote the UI. Fix with AbortController (cancel the in-flight request on each keystroke) or rely on TanStack Query, which cancels stale-key queries automatically when the queryKey changes.", correct: true },
    { label: "The server returned cached results for the wrong query.", misconception: "The server returned correct results for each query it received; the bug is client-side — the slower 'reac' response landed last and clobbered the newer 'react' state." },
    { label: "React batched the state updates in the wrong order.", misconception: "React batching affects re-render scheduling within a tick, not the arrival order of independent network responses. The race is between fetches, not within React's batcher." },
    { label: "Increase staleTime so old results are not shown.", misconception: "staleTime controls when cached data is refetched, not which of two in-flight responses wins. Only cancellation or queryKey versioning resolves the race." },
  ]}
/>

<Quiz
  id="02-data-fetching-choice-6"
  lessonSlug="02-data-fetching"
  lang="en"
  question="After a mutation updates a product, the server revalidates its cache but users still see the old price for several minutes. The Server Function called revalidateTag correctly. What was missed?"
  choices={[
    { label: "The client library cache (TanStack Query / SWR) was not invalidated. A Next.js 15 app has multiple cache layers — CDN, fetch cache, DB cache, HTTP cache, client cache — and a mutation must invalidate every relevant one. queryClient.invalidateQueries was skipped, so the client served stale data until gcTime/staleTime elapsed.", correct: true },
    { label: "revalidateTag does not work in Server Functions.", misconception: "revalidateTag works in Server Functions and did its job server-side. The stale data lived in the untouched client query cache, a separate layer." },
    { label: "The CDN TTL was too short, so it kept re-serving old data.", misconception: "A short TTL would refresh sooner, not pin stale data for minutes. The persistent staleness is the client cache holding the old response under its own lifetime." },
    { label: "The database query cache cannot be invalidated from the app.", misconception: "The DB/Redis cache is invalidated by application code and is one layer here, but the symptom — old data only in the browser — points to the client library cache, which revalidateTag never reaches." },
  ]}
/>

<Recap lang="en">
The through-line across the unit is one map of the critical path. Fetch location decides how many round-trips precede LCP — server fetch collapses them, client fetch after JS stacks them. Waterfalls re-serialise independent fetches through the render cascade; Promise.all and sibling RSC components cap them at the slowest. RSC ships zero JS only for components that stay server-side, and Suspense streaming buys a low TTFB. The client cache (SWR semantics, single-flight, optimistic snapshots) governs revisits and interactions, while AbortController kills races. And every mutation must invalidate every cache layer it touches — server revalidation alone leaves the browser stale.
</Recap>
