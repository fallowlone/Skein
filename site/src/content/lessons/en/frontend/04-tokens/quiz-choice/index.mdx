---
concepts:
- token-tiers
- semantic-tokens
- theming-dark-mode
- css-custom-properties
- token-pipeline
- token-drift
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.designtokens.org/tr/drafts/format/
- https://styledictionary.com/info/dtcg/
status: ready
summary: "Multiple-choice synthesis across the design-tokens unit: tier boundaries, semantic theming, the cross-platform pipeline, CSS-variable cascade, and how drift creeps back in."
title: 'Design tokens: multiple-choice review'
track: frontend
unit: 04-tokens
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each mirrors a real decision — where a token belongs, what a component is allowed to reference, when a pipeline pays off — not a definition to recite.
</Hook>

<Goal lang="en">
Confirm you can connect the three tiers, semantic theming, the cross-platform build, and the discipline that keeps drift from creeping back — the synthesis the lesson built toward.
</Goal>

<Quiz
  id="04-tokens-choice-1"
  lessonSlug="04-tokens"
  lang="en"
  question="A code review shows a button styled with background: var(--color-blue-600). The system has primitive, semantic, and component tiers. Why is this a defect even though the color is technically a token?"
  choices={[
    { label: "The button references a primitive directly, skipping the semantic tier. It is now locked to one palette value, so dark mode and a rebrand must touch this rule by hand — the exact fork tokens exist to remove.", correct: true },
    { label: "It is fine — blue-600 is a token, so the system is honoring single-source-of-truth.", misconception: "Naming the value is not the goal; routing intent through the semantic layer is. A component bound to a primitive cannot follow a theme override, so it has fallen out of the token system in practice." },
    { label: "The problem is the var() syntax; the button should inline the hex for performance.", misconception: "Inlining the hex is strictly worse — it is a literal, the original drift source. var() resolution is effectively free; the defect is the tier, not the syntax." },
    { label: "Component tokens are mandatory, so the button must define button.background first.", misconception: "Component tokens are optional. The button could correctly reference color.interactive.default directly; the bug is referencing a primitive, not the absence of a component token." },
  ]}
/>

<Quiz
  id="04-tokens-choice-2"
  lessonSlug="04-tokens"
  lang="en"
  question="Dark mode must recolor every component with no JavaScript and one repaint. Which approach delivers that on the web?"
  choices={[
    { label: "Redefine the same semantic token names under a [data-theme=dark] selector; the cascade re-resolves every var() that references them, so components are never touched.", correct: true },
    { label: "Read a JS theme object in each component via state and set inline styles on mount.", misconception: "That reinvents what the cascade does for free, adds re-render cost, and risks a flash before hydration. Custom properties recolor in one repaint with zero JS." },
    { label: "Ship a separate dark stylesheet that re-declares each component rule with dark hex values.", misconception: "That is a forked copy of every rule — the multi-tone-button failure mode. You maintain two sets of component styles that drift independently." },
    { label: "Override the primitive palette tokens only, and have components reference primitives so they pick up the new values.", misconception: "Components must reference semantics, not primitives. Overriding primitives can work, but the discipline is to redefine the semantic layer so intent is preserved and components stay theme-agnostic." },
  ]}
/>

<Quiz
  id="04-tokens-choice-3"
  lessonSlug="04-tokens"
  lang="en"
  question="The team authors tokens once in DTCG JSON and runs Style Dictionary. What is the load-bearing step that makes one source correct on web, iOS, and Android?"
  choices={[
    { label: "Per-platform transforms: the same space.4 becomes 1rem for CSS, a snake_case number for Android, camelCase for Swift; a color becomes hex for CSS, UIColor for iOS, 8-digit ARGB for Android — generated, not hand-copied.", correct: true },
    { label: "The JSON file itself — once values are named, every platform reads the same file directly at runtime.", misconception: "Platforms do not consume DTCG JSON at runtime. The build resolves aliases and emits native artifacts (CSS, colors.xml, a Swift file); the transform step is what makes each output correct." },
    { label: "The alias syntax {group.token} — aliases alone guarantee cross-platform correctness.", misconception: "Aliases handle reference resolution within the source, but a CSS px value is wrong for Android XML and a hex is wrong for UIColor. Transforms, not aliases, adapt values per platform." },
    { label: "A shared component library that imports the JSON into every app.", misconception: "Importing raw JSON still leaves each platform to interpret values itself — the manual fork. The pipeline's job is to emit platform-native files via transforms and formatters." },
  ]}
/>

<Quiz
  id="04-tokens-choice-4"
  lessonSlug="04-tokens"
  lang="en"
  question="A five-person team ships one web app, one theme, no native targets. They propose a full three-tier system with a Style Dictionary build. What is the senior read?"
  choices={[
    { label: "Over-engineered for now. Two tiers (primitive + semantic) as CSS variables is plenty; add component tokens and a pipeline only when a second output target or theme actually lands.", correct: true },
    { label: "Correct — every project should use the full pipeline from day one regardless of targets.", misconception: "Each tier is indirection a new engineer must trace and a place a bug can hide. With one target and one theme the pipeline cost outweighs the drift it prevents." },
    { label: "They should skip tokens entirely and hardcode hex until they have native apps.", misconception: "Even one web app benefits from primitive plus semantic tiers so a rebrand or future dark mode is one edit. The question is how many tiers, not whether to tokenize at all." },
    { label: "They need component tokens most of all, since those isolate each component.", misconception: "Component tokens are the most optional tier and add the most hops. You introduce them only when a specific component must diverge, not as a default." },
  ]}
/>

<Quiz
  id="04-tokens-choice-5"
  lessonSlug="04-tokens"
  lang="en"
  question="Three weeks after rolling out tokens, a grep finds fresh hardcoded hex values scattered through new components. The tier model is sound. What is the durable fix?"
  choices={[
    { label: "A lint gate — a stylelint rule that makes a raw hex outside the primitive file a build error. A convention without an enforced gate decays as soon as someone in a hurry pastes a literal.", correct: true },
    { label: "Re-train the team in a workshop and trust them to follow the convention.", misconception: "Training fades; the next deadline reintroduces literals. Drift is a process problem, and only an automated gate in CI holds the line reliably." },
    { label: "Add more semantic tokens so there is always one to reach for.", misconception: "Coverage helps, but it does not stop someone from pasting a hex anyway. Without a build-time error the system erodes regardless of how complete the palette is." },
    { label: "Switch to inline styles so the values live with the component.", misconception: "Inlining hex is the original drift, just relocated. The fix is to forbid raw values, not to make them more local." },
  ]}
/>

<Quiz
  id="04-tokens-choice-6"
  lessonSlug="04-tokens"
  lang="en"
  question="A designer asks why the button cannot just reference color.blue.600, since that is what it resolves to anyway. What is the precise reason for the extra semantic hop?"
  choices={[
    { label: "The name carries intent, not just a value. button → color.interactive → color.blue.600 means a theme can redefine 'interactive' to a different blue without touching the button, and a rebrand edits the primitive once while every intent mapping holds.", correct: true },
    { label: "It is purely a performance optimization — the extra var() resolves faster than a deep lookup.", misconception: "var() resolution cost is negligible and adding a hop does not speed anything up. The benefit is intent and theme-override capability, not performance." },
    { label: "There is no real reason; the semantic tier is convention for its own sake.", misconception: "The semantic tier is what lets dark mode and rebrands happen as one edit. Removing it re-couples every component to the raw palette — the unit's core failure mode." },
    { label: "DTCG forbids components from referencing primitives, so the hop is a spec requirement.", misconception: "DTCG defines the format, not which tier a consumer may reference. The hop is an architectural discipline that buys theming and rebrand-in-one-edit, not a spec rule." },
  ]}
/>

<Recap lang="en">
The through-line is one rule with consequences: a component references a semantic token, never a primitive and never a literal. That single discipline is what lets the cascade theme dark mode for free, lets one DTCG source generate correct CSS, Swift, and Android via transforms, and lets a rebrand be one edit to the primitive layer. Size the tiers to the targets you ship, and gate the rule with a linter — because a convention without an enforced gate drifts back to hardcoded hex within weeks. Now when you see a code-review thread debating which token a component should reference, you have the answer: trace the tier, find the semantic name for that intent, and make sure the gate will catch any backslide.
</Recap>
