---
slug: 01-overview
lang: en
track: frontend
unit: 03-forms-a11y
order: 1
level: junior
lessonType: topic
status: ready
title: "Accessible forms: the keyboard path is the real spec"
summary: "A form that works with a mouse can be totally unusable with a keyboard or screen reader. Labels, error announcement, and focus on the first invalid field are what make it pass WCAG 2.2 AA — and what most teams ship broken."
prereqs: []
deepensInto: []
spiral: []
concepts: ["label-association", "error-announcement", "focus-management", "constraint-validation", "accessible-name", "custom-widget-trap"]
estMin: 17
sources:
  - https://webaim.org/projects/million/2025
  - https://www.w3.org/WAI/WCAG22/Understanding/status-messages.html
  - https://www.w3.org/WAI/WCAG21/Understanding/error-identification.html
  - https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Reference/Roles/button_role
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
The checkout form passes QA. It works flawlessly — with a mouse. Then a screen-reader user files a ticket: they tab to the email field, hear nothing but "edit text," fill the whole form blind, hit the slick `<div>` "Pay" button — and Enter does nothing, because a div is not a button. They submit by accident, three fields are wrong, and the red error text that "clearly" appears is never announced. The form did not crash. It is simply unusable for everyone who does not point and click. That is a WCAG 2.2 AA failure, and on 1,000,000 home pages it is the most common one there is.
</Hook>

<Crux>A form usable with a mouse but not a keyboard or screen reader is broken — labels, announced errors, and focus on the first invalid field.</Crux>

<Explanation>

## The label is the input's name, and a third of the web has none

Every input needs a programmatically associated label — not placeholder text, not visually adjacent text, an actual association the accessibility tree can read. The WebAIM Million 2025 report scanned the top one million home pages and found **34.2% of all form inputs were not properly labeled**. With an average of 6.3 inputs per page, that is the single most common failure category on the web. A screen reader hitting an unlabeled field announces "edit text" and nothing else — the user has no idea what to type.

Two correct associations exist, and the difference matters:

- **Explicit**: `<label for="email">Email</label>` plus `<input id="email">`. The `for` (in React, `htmlFor`) matches the input's `id`. This is the robust default — the label can sit anywhere in the DOM.
- **Implicit**: wrap the input in the label — `<label>Email <input></label>`. No id needed, but the input must be a direct descendant, and some older AT and styling setups handle it less reliably.

Placeholders are not labels. They vanish on focus, fail color-contrast minimums, and several screen readers ignore them entirely. A "floating label" that animates a placeholder into a label is fine *only* if a real associated `<label>` exists underneath. This whole area is **WCAG 1.3.1 Info and Relationships (Level A)** plus 4.1.2 Name, Role, Value: the relationship between label and control must be exposed to assistive tech, not just visible on screen.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Approach</th>
        <th class="border border-border px-3 py-2 text-left">Accessible name?</th>
        <th class="border border-border px-3 py-2 text-left">Verdict</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>&lt;label for="x"&gt;</code> + <code>&lt;input id="x"&gt;</code></td>
        <td class="border border-border px-3 py-2">Yes — explicit</td>
        <td class="border border-border px-3 py-2">Default; label can live anywhere in the DOM</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>&lt;label&gt;Email &lt;input&gt;&lt;/label&gt;</code></td>
        <td class="border border-border px-3 py-2">Yes — implicit</td>
        <td class="border border-border px-3 py-2">Works, but input must be a direct child</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>placeholder="Email"</code> only</td>
        <td class="border border-border px-3 py-2">No</td>
        <td class="border border-border px-3 py-2">Vanishes on focus, low contrast, often ignored by AT</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>aria-label="Email"</code> on input</td>
        <td class="border border-border px-3 py-2">Yes — but no visible label</td>
        <td class="border border-border px-3 py-2">Last resort (icon-only fields); sighted users get nothing</td>
      </tr>
    </tbody>
  </table>
</div>

## Errors that nobody hears

You validate on submit, paint the invalid fields red, drop a message under each one. To a sighted mouse user, perfect. To a screen-reader user, silence — visually changing the DOM announces nothing. The fix is two-part, and both parts are easy to get subtly wrong.

First, **announce the error**. A message inserted into a container with `role="alert"` (or an `aria-live="assertive"` region) is read out the moment it appears — that is **WCAG 4.1.3 Status Messages (Level AA)**: the user learns of a status change without moving focus to find it. The trap: the live region must already exist in the DOM *before* you write text into it. If you mount the `<div role="alert">` and its message in the same render, many screen readers miss the change. Render the empty live region up front; only inject the text on error.

Second, **associate the message with its field** via `aria-describedby` pointing at the message's `id`, and mark the field `aria-invalid="true"`. Now when the user lands on that input, the screen reader reads label, value, "invalid," and the error text together. That is **WCAG 3.3.1 Error Identification (Level A)**. Go one better — say *how* to fix it ("enter a date as MM/DD/YYYY"), not just "invalid" — and you satisfy **3.3.3 Error Suggestion (Level AA)**.

<Inset kind="why" lang="en">
`role="alert"` is `aria-live="assertive"` plus `aria-atomic="true"` — it interrupts the screen reader to read the whole region immediately. Use it for errors. For non-urgent status ("Saved", "3 results") prefer `role="status"` (polite): it waits for a pause instead of barging in. Two assertive regions firing at once will stomp each other, so reserve assertive for things the user must hear *now*.
</Inset>

## Focus is the difference between usable and not

Validation fails. A sighted user sees the red fields and clicks the first one. A keyboard or screen-reader user, on submit, is still focused on the submit button — at the *bottom* of the form. They have no idea anything failed, and even if the page scrolled, they would have to tab backward through the whole form hunting for the broken field. The fix is one line of intent: **on submit error, move focus to the first invalid field** (`firstInvalid.focus()`). The user is dropped exactly where the work is, and because that field carries `aria-describedby` + `aria-invalid`, the screen reader immediately reads what is wrong and how to fix it.

This is the behavior browsers give you *for free* with native constraint validation — `required`, `type="email"`, `pattern`, `min`/`max` — where the browser focuses and scrolls to the first invalid control on submit and shows a built-in bubble. The moment you intercept with `e.preventDefault()` and roll your own validation (which most real apps do, for custom messages and async checks), **you inherit that focus responsibility**. Forget it, and you have built the classic "works with a mouse, dead with a keyboard" form.

<Quiz
  id="03-forms-a11y-quiz-1"
  lessonSlug="01-overview"
  lang="en"
  question="Custom JS validation runs on submit and finds three invalid fields. What's the senior move for keyboard/AT users?"
  choices={[
    { label: "Move focus to the first invalid field and have it announce the error via aria-describedby + aria-invalid", correct: true },
    { label: "Just paint the invalid fields red and rely on the user scrolling up to find them", misconception: "Color is invisible to a screen reader and useless to a colorblind user; focus stays on the submit button with no announced change. This is the core WCAG 2.2 AA failure." },
    { label: "Show a single alert() dialog listing the errors", misconception: "alert() interrupts everyone, loses the per-field association (aria-describedby), and doesn't put focus where the fix happens — it's a worse experience for keyboard and AT users." },
    { label: "Scroll the page to the first error but leave focus on the submit button", misconception: "Scrolling helps a sighted mouse user only. A keyboard user's focus is still at the bottom, and a screen reader announces nothing — you must move focus, not just the viewport." },
  ]}
/>

## Custom widgets are where it silently dies

The deepest failures are not missing labels — they are reinvented controls. A designer wants a styled dropdown, so an engineer builds `<div class="select">` with click handlers. It looks identical. To assistive tech it is *invisible as a control*: a div has no role, is not in the tab order, has no keyboard behavior. A screen reader reads it as plain text; a keyboard user cannot reach it, cannot open it, cannot pick an option. The same trap snares `<div onClick>` "buttons": MDN's button-role docs spell out that to make a div behave like a button you must add `role="button"`, `tabindex="0"`, a keydown handler for **both** Enter (keyCode 13) and Space (32) with `preventDefault`, plus the disabled and focus semantics you got free from `<button>`. People remember the click handler and forget every one of those.

The senior rule is blunt: **use the native element.** `<button>`, `<input>`, `<select>`, `<a href>` ship with role, focusability, keyboard handling, and OS-level form integration that you cannot fully replicate and will not maintain. Reach for ARIA-rebuilt widgets only when the native element genuinely cannot do the job (a true combobox with filtering, a multiselect) — and then follow the full APG keyboard pattern, because a half-built `role="combobox"` is worse than the plain `<select>` you replaced.

<TradeoffMatrix
  id="03-forms-a11y-tradeoff-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="Design hands you a custom-styled dropdown that the native <select> can't match visually. Pick the implementation."
  options={[
    { name: "Style the native <select> as far as CSS allows; accept the OS picker for the option list", summary: "Native first.", correct: true, justification: "The native <select> ships role, focusability, type-ahead, keyboard open/close, and OS-native option rendering — none of which you have to build or maintain. Modern CSS (and the emerging customizable-select work) covers most styling needs. You trade pixel-perfect option-list styling for a control that is correct in every browser and AT by default. This is almost always the right call." },
    { name: "Build a div-based dropdown with role=listbox, tabindex, and the full APG keyboard pattern", summary: "ARIA rebuild.", justification: "Sometimes necessary (filtering combobox, multiselect the native can't do), but you now own arrow-key navigation, type-ahead, Escape/Enter/Home/End, focus management, aria-activedescendant, and screen-reader testing across NVDA/JAWS/VoiceOver. Correct here only if the native control truly can't meet the requirement — and then you must finish the whole pattern, not half." },
    { name: "Use <div onClick> with matching CSS and skip ARIA — it looks the same", summary: "Visual only.", justification: "The classic silent failure. A div has no role, isn't in the tab order, and has no keyboard behavior, so it's invisible to screen readers and unreachable by keyboard. It works with a mouse and fails WCAG 1.3.1 / 4.1.2. Never ship this." },
    { name: "Keep the native <select> but hide it visually and overlay a fake div the mouse sees", summary: "Hidden native.", justification: "A common 'hack' that breaks: focus, value, and the visible widget drift apart, click targets misalign, and keyboard focus lands on an invisible control while the mouse sees a fake one. You get the maintenance cost of both with the correctness of neither." },
  ]}
/>

<DragOrder
  id="03-forms-a11y-drag-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="Order the steps to handle a failed submit accessibly:"
  items={[
    { id: "a", label: "Run validation on submit and collect the invalid fields" },
    { id: "b", label: "Set aria-invalid=\"true\" on each invalid field" },
    { id: "c", label: "Write each error message into a pre-existing live region / element the field's aria-describedby points to" },
    { id: "d", label: "Move keyboard focus to the first invalid field (firstInvalid.focus())" },
    { id: "e", label: "Let the screen reader read label + invalid + error text now that focus is on the field" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Associate every input with a real label, render error live regions before writing to them, set aria-invalid + aria-describedby, and move focus to the first invalid field on submit. Prefer native controls.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-forms-a11y-retrieval"
  lang="en"
  questions={[
    { q: "A screen-reader user submits your form, three fields are invalid, and they hear nothing. Walk through what's missing and how to fix each gap.", a: "Three things are likely broken at once. First, the errors aren't announced: visually changing the DOM says nothing to a screen reader, so each message must live in a region with role=\"alert\" (or aria-live), and crucially that region must already exist in the DOM before you inject text — mounting the region and its message in the same render is missed by many AT. That satisfies WCAG 4.1.3 Status Messages. Second, the errors aren't associated with their fields: set aria-invalid=\"true\" on each bad input and aria-describedby pointing at the message id, so landing on the field reads label, value, 'invalid,' and the message together (WCAG 3.3.1). Third — the big one — focus never moved: after preventDefault on submit, focus is still on the submit button at the bottom of the form, so the user has no idea anything failed. Call firstInvalid.focus() to drop them on the first broken field, where the describedby text is read immediately. Native constraint validation does all three for free; the moment you preventDefault and validate yourself, you own them." },
    { q: "Why is a <div role=\"button\"> dropdown a worse bug than a missing label, and what does 'use the native element' actually buy you?", a: "A missing label is one wrong attribute — annoying, but the control is still reachable, focusable, and operable. A div-based control is invisible as a control: a div has no implicit role, isn't in the tab order, and has no keyboard behavior, so a screen reader reads it as plain text and a keyboard user can't reach or operate it at all. To make a div behave like a button you must add role=\"button\", tabindex=\"0\", and a keydown handler for both Enter and Space with preventDefault, plus replicate disabled and focus semantics — and for a select/combobox the full APG pattern (arrow keys, type-ahead, Escape, aria-activedescendant). People remember the click handler and forget all of it, so the widget works with a mouse and is dead to keyboard and AT. The native element ships role, focusability, keyboard handling, and OS-level form integration that you can't fully replicate and won't maintain, which is why the senior default is to reach for <button>/<select>/<input> and only rebuild with ARIA when the native truly can't do the job." },
  ]}
/>

<Recap lang="en">
Accessible forms are decided by the keyboard-and-screen-reader path, not the mouse path. Every input needs a programmatically associated label (explicit htmlFor/id or implicit wrapping) — placeholders don't count, and 34.2% of the web's form inputs fail this. Errors must be announced through a live region (role="alert"/aria-live) that already exists in the DOM, associated to their fields with aria-invalid + aria-describedby, and stated as suggestions, not just "invalid" — covering WCAG 1.3.1, 3.3.1, 3.3.3, and 4.1.3. On a failed submit you must move focus to the first invalid field; native constraint validation does this for free, but the moment you preventDefault you own it. And the deepest trap is the reinvented control: a styled div is invisible to assistive tech, so reach for the native `<button>`/`<select>`/`<input>` first and only rebuild with the full ARIA pattern when the native element genuinely can't do the job. Get this right and the form that "works with a mouse" finally works for everyone.
</Recap>
