---
concepts:
- side-effects
- dev-prod-mode
- source-maps
- manual-chunks
- env-inlining
estMin: 14
lang: en
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://webpack.js.org/guides/tree-shaking/
- https://vite.dev/guide/build
- https://vite.dev/guide/env-and-mode
status: ready
summary: "Read real build configs and a package.json field, predict the bundler behaviour, and pick the highest-leverage fix for tree-shaking, mode, source maps, and env inlining."
title: 'Build pipelines: config and snippet reading'
track: frontend
unit: 07-build-pipelines
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Build problems are diagnosed in the config and the package manifest, not in the running app. Read each snippet, predict what the bundler does with it, and choose the fix a senior would make first.
</Hook>

<Goal lang="en">
Practise the loop you run on every build regression: read the config, predict where bytes leak or where dev and prod diverge, and reach for the precise field — sideEffects, mode, sourcemap, manualChunks, env — that fixes the root cause.
</Goal>

## Snippet 1 — the sideEffects declaration

```json
{
  "name": "@acme/ui",
  "module": "dist/index.js",
  "sideEffects": false,
  "exports": { ".": "./dist/index.js" }
}
```

The package ships a `dist/styles.css` that components import for their styling, e.g. `import "./button.css"` at the top of `Button.js`.

<Quiz
  id="07-build-pipelines-code-1"
  lessonSlug="07-build-pipelines"
  lang="en"
  question="A consumer reports that some component styles vanish in their production build. What does this sideEffects value cause, and what is the fix?"
  choices={[
    { label: "sideEffects: false promises every file is pure on import, so the bundler drops the CSS-importing module when its JS exports look unused — and the styles disappear. Fix: declare the impure files, e.g. sideEffects: ['*.css'].", correct: true },
    { label: "sideEffects: false only affects JS, so CSS is always preserved.", misconception: "The flag applies to every module, including the ones that import CSS for its side effect. Claiming false here lets the bundler eliminate the CSS-importing module." },
    { label: "The fix is to set sideEffects: true so nothing is dropped.", misconception: "true disables tree-shaking for the whole package and ships dead JS. The precise fix is to list only the genuinely impure files, like '*.css', and keep the rest shakeable." },
    { label: "CSS imports are never tree-shaken, so this is unrelated to sideEffects.", misconception: "A module whose only job is importing CSS is exactly what an over-broad sideEffects: false lets the bundler delete; the field is the root cause." },
  ]}
/>

## Snippet 2 — dev vs prod mode and NODE_ENV

```js
// webpack.config.js
module.exports = {
  mode: "development",
  // ...loaders, plugins...
};
```

The team runs this exact config in CI to produce the artifact they deploy to production.

<Quiz
  id="07-build-pipelines-code-2"
  lessonSlug="07-build-pipelines"
  lang="en"
  question="The deployed bundle is ~3× larger than expected and React logs development warnings in prod. What is wrong, and what is the highest-leverage fix?"
  choices={[
    { label: "mode: 'development' disables minification and sets process.env.NODE_ENV to development, so dead-code-elimination of dev-only branches (React's warnings) never runs. Build prod with mode: 'production' (or pass it per-command).", correct: true },
    { label: "The bundle is large because tree-shaking is off in all modes.", misconception: "Tree-shaking is not the toggle here; mode is. development mode skips minification and leaves NODE_ENV=development, so dev-only branches survive and the bundle stays large." },
    { label: "React always logs warnings; the size is unrelated to mode.", misconception: "React strips its dev warnings when NODE_ENV is production. They appear precisely because development mode left NODE_ENV at development." },
    { label: "Set devtool to false to shrink the bundle.", misconception: "devtool controls source maps, not the dev/prod code paths. The fix is the mode field, which drives minification and NODE_ENV." },
  ]}
/>

## Snippet 3 — source maps in production

```js
// vite.config.js
export default {
  build: {
    sourcemap: false,
  },
};
```

Production throws an error and the stack trace reads `index-a1b2c3.js:1:88431`. The error tracker shows the same unreadable frame.

<Quiz
  id="07-build-pipelines-code-3"
  lessonSlug="07-build-pipelines"
  lang="en"
  question="You need readable prod stack traces in your error tracker without exposing source to the public. What is the right setting?"
  choices={[
    { label: "Set sourcemap: 'hidden' so maps are generated and uploaded to the error tracker but no //# sourceMappingURL comment is emitted into the public bundle.", correct: true },
    { label: "Keep sourcemap: false and reconstruct traces from the minified output.", misconception: "Without a source map, the minified single-line frame cannot be mapped back to Button.tsx:42; every prod error stays unreadable. Generate the map, just do not expose it publicly." },
    { label: "Set sourcemap: true so the browser always downloads the map.", misconception: "true emits the sourceMappingURL into the public bundle, exposing your source to anyone with devtools. 'hidden' gives the tracker the map without publishing it." },
    { label: "Source maps change runtime behaviour, so enabling them risks the bug.", misconception: "Source maps are debugging metadata only; they never alter how the code runs. They affect whether a trace is readable, nothing more." },
  ]}
/>

## Snippet 4 — manualChunks and env inlining

```js
// vite.config.js
export default {
  build: {
    rollupOptions: {
      output: {
        manualChunks: { vendor: ["react", "react-dom"] },
      },
    },
  },
};
```

A reviewer also notices a client component reads `process.env.API_SECRET` to call an internal API.

<Quiz
  id="07-build-pipelines-code-4"
  lessonSlug="07-build-pipelines"
  lang="en"
  question="Which statement about this config is correct?"
  choices={[
    { label: "The manualChunks split is sound — vendor gets its own stable-hash chunk — but reading API_SECRET in a client component inlines the secret into the shipped bundle at build time; secrets must stay server-side, never in client env.", correct: true },
    { label: "Both the chunk split and the env read are fine; Vite hides process.env from the client.", misconception: "Vite statically inlines referenced env values into the client bundle at build time. A secret read in client code is baked into shipped JS — a real leak." },
    { label: "manualChunks here is wrong because vendor should share a chunk with app code.", misconception: "Sharing vendor with app code busts the vendor cache on every app deploy. Isolating react/react-dom into a stable vendor chunk is the correct move." },
    { label: "The secret is safe because it lives in an environment variable, not the source.", misconception: "Build-time inlining copies the env value into the output bundle regardless of where it originated. Only server-side code may read secrets." },
  ]}
/>

<Recap lang="en">
Every build regression is read in the config: sideEffects: false must list genuinely impure files (like CSS) or the bundler drops them; production must build with production mode so minification and NODE_ENV-gated dead-code-elimination run; source maps belong in 'hidden' mode so the error tracker gets readability without public exposure; vendor deserves its own manualChunks chunk for cache stability; and any env value referenced in client code is inlined at build time, so secrets never touch the client. Read the field, predict the output, fix the root cause.
</Recap>
