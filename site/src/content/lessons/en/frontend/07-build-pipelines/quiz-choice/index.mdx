---
concepts:
- tree-shaking
- side-effects
- dev-prod-parity
- content-hashing
- bundler-internals
- transform-vs-bundle
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://vite.dev/guide/why
- https://webpack.js.org/guides/tree-shaking/
- https://esbuild.github.io/faq/#why-is-esbuild-fast
status: ready
summary: "Multiple-choice synthesis across the build-pipeline unit — stage seams, tree-shaking defeats, dev/prod parity, content hashing, and tool-speed tradeoffs."
title: 'Build pipelines: multiple-choice review'
track: frontend
unit: 07-build-pipelines
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one mirrors a call you make while a bundle regresses, a dev-only bug ships, or a build step times out CI — not a definition to recite, but a tradeoff to weigh.
</Hook>

<Goal lang="en">
Confirm you can connect the five pipeline stages, why tree-shaking fails, why dev and prod diverge, and how content hashing and tool choice trade off — the synthesis the overview built toward.
</Goal>

<Quiz
  id="07-build-pipelines-choice-1"
  lessonSlug="07-build-pipelines"
  lang="en"
  question="A teammate swaps import debounce from 'lodash/debounce' for import _ from 'lodash' and the main bundle grows ~200 KB. Why did tree-shaking not drop the unused functions?"
  choices={[
    { label: "lodash's main build is CommonJS; its exports are assembled at runtime, so the bundler cannot statically prove which keys are unused and must keep the whole module. Use lodash-es or deep imports.", correct: true },
    { label: "Tree-shaking is opt-in and must be enabled per import.", misconception: "Tree-shaking is on by default in production bundles. The defeat here is module format — CommonJS exports are computed at runtime and cannot be statically analysed." },
    { label: "Minify ran before bundle, so the dead code was already locked in.", misconception: "Stage order is not the cause. Even with correct ordering the bundler keeps the whole CJS module because its exports are not statically analysable." },
    { label: "A default import disables shaking; a named import would have fixed it.", misconception: "Import style is not the root cause — module format is. ES modules shake regardless of default vs named; CommonJS does not shake either way." },
  ]}
/>

<Quiz
  id="07-build-pipelines-choice-2"
  lessonSlug="07-build-pipelines"
  lang="en"
  question="A clean ESM utility library still ships dead code after bundling, even though every import looks unused. The most likely cause?"
  choices={[
    { label: "The package omits or mis-declares the package.json sideEffects field, so the bundler conservatively assumes importing any file does observable work and keeps it.", correct: true },
    { label: "The library is too large for the bundler to analyse.", misconception: "Bundlers tree-shake arbitrarily large ESM graphs. Size is irrelevant; the missing sideEffects contract is what forces the bundler to keep modules." },
    { label: "Tree-shaking only removes whole files, never individual exports.", misconception: "Tree-shaking removes unused exports at binding granularity within a module, not just whole files. The sideEffects flag governs whether a file may be dropped at all." },
    { label: "Source maps are keeping references to the dead code alive.", misconception: "Source maps are debugging metadata emitted last; they never keep code in the bundle. The sideEffects declaration is the lever." },
  ]}
/>

<Quiz
  id="07-build-pipelines-choice-3"
  lessonSlug="07-build-pipelines"
  lang="en"
  question="A feature works in the Vite dev server but breaks after vite build. What is the most likely structural cause, not a one-off?"
  choices={[
    { label: "Dev serves native unbundled ESM via esbuild while prod bundles with Rollup. Different resolution, code-splitting, and side-effect handling can legitimately diverge — e.g. a circular import the browser tolerates but the bundler reorders.", correct: true },
    { label: "The dev server cached aggressively and served stale code.", misconception: "Stale cache makes old behaviour persist, the opposite symptom. A true dev/prod split is two different toolchains producing different output from the same source." },
    { label: "Production minification rewrote the program's behaviour.", misconception: "Minification can occasionally surface a latent bug, but the structural reason dev and prod diverge is that they run different bundlers on differently-shaped module graphs." },
    { label: "Environment variables were not inlined in dev.", misconception: "Both modes inline env vars; the divergence here is bundling architecture, not env handling. The fix is to test the real prod build, not to chase env injection." },
  ]}
/>

<Quiz
  id="07-build-pipelines-choice-4"
  lessonSlug="07-build-pipelines"
  lang="en"
  question="Why does swapping Babel for esbuild or SWC cut the transform stage by 90%-plus, while the bundle stage rarely sees the same speedup?"
  choices={[
    { label: "Transform is per-file and embarrassingly parallel, so compiled, multi-threaded tools scale across cores; bundle is a whole-graph, largely serial dependency walk that parallelises far less.", correct: true },
    { label: "esbuild simply skips work that Babel does, producing less correct output.", misconception: "esbuild produces correct output; the speedup comes from a compiled language, real threads, and a single shared AST — not from skipping necessary work." },
    { label: "Babel is slow only because of its config format, not its runtime.", misconception: "Config format is irrelevant to runtime cost. Babel is slow because it is single-threaded JavaScript re-parsing ASTs; the bundle stage stays serial regardless of transformer." },
    { label: "The bundle stage is already parallel, so any transformer matches it.", misconception: "Bundling is whole-graph and largely serial — that is exactly why transform-stage speedups do not carry over to it." },
  ]}
/>

<Quiz
  id="07-build-pipelines-choice-5"
  lessonSlug="07-build-pipelines"
  lang="en"
  question="Returning users re-download the large vendor chunk on every app deploy even though React and your deps never changed. The fix?"
  choices={[
    { label: "Split vendor code into its own content-hashed chunk separate from app code, so the vendor hash is stable across frequent app deploys and stays in the browser cache.", correct: true },
    { label: "Disable content hashing so filenames stop changing.", misconception: "Stable filenames break cache busting entirely — users would be served stale app code. The fix is chunk boundaries, not removing the hash." },
    { label: "Lower the Cache-Control max-age so the browser revalidates faster.", misconception: "Revalidating more often increases traffic, the opposite goal. With content hashing you want a long max-age plus immutable; the real issue is vendor sharing a chunk with app code." },
    { label: "Inline the vendor code into each app chunk to avoid an extra request.", misconception: "Inlining vendor into app chunks guarantees its hash changes on every app deploy, busting the cache every time — exactly the problem here." },
  ]}
/>

<Quiz
  id="07-build-pipelines-choice-6"
  lessonSlug="07-build-pipelines"
  lang="en"
  question="Source is clean ESM, yet the prod bundle still ships an entire dependency it should have shaken. The transform stage is the suspect — why?"
  choices={[
    { label: "@babel/preset-env can down-level import/export into require/module.exports before the bundler sees it, silently turning a shakeable graph opaque. Keep modules as ESM through transform.", correct: true },
    { label: "Tree-shaking happens during transform, so a slow transformer skips it.", misconception: "Tree-shaking happens during the bundle stage on the module graph, not during per-file transform. The transform-stage defeat is ESM-to-CJS down-leveling, which removes the static structure shaking needs." },
    { label: "The transformer minified the code, hiding the unused exports.", misconception: "Transform and minify are separate stages; transform does not minify. The real defeat is down-leveling ESM to CommonJS before bundling." },
    { label: "esbuild cannot tree-shake at all, so the transformer must.", misconception: "esbuild tree-shakes ESM well; the issue is that a Babel transform can convert ESM to CJS upstream, after which no bundler can shake it." },
  ]}
/>

<Recap lang="en">
The through-line: a build is resolve → transform → bundle → minify → emit, and the seams between stages are where bugs live. Tree-shaking needs static ESM, so it is defeated by CommonJS, by undeclared side effects, and by transforms that down-level ESM to CJS before the bundler looks. Transform is parallel (so compiled tools win there) while bundle stays serial. Dev and prod run different tools, so validate the real prod build. And content hashing only pays off if you split stable vendor code from churny app code.
</Recap>
