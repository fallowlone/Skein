infographic chart-column-simple
data
  title Nested lazy chunks stack round trips (ms, 150 ms RTT)
  desc A waterfall is sequential — chunk B is unknown until chunk A parses, so HTTP/2 can't parallelize it away
  values
    - label 1 level
      value 150
    - label 2 levels
      value 300
    - label 3 levels
      value 450
theme
  palette #6366f1 #8b5cf6 #f43f5e
