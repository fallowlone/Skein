---
concepts:
- route-based-splitting
- dynamic-import
- request-waterfall
- bundle-budget
- preload-prefetch
- vendor-chunk-invalidation
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://web.dev/articles/code-splitting-suspense
- https://web.dev/articles/granular-chunking-nextjs
- https://medium.com/webpack/webpack-http-2-7083ec3f3ce6
- https://nextjs.org/docs/pages/guides/lazy-loading
spiral: []
status: ready
summary: Splitting trades one big download for many small ones. Done by route it pays;
  done per-component on a high-latency phone it builds a request waterfall that loses
  to the bundle you were trying to beat.
title: 'Code splitting: the chunk graph is a latency budget, not a size win'
track: frontend
unit: 06-code-splitting
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A team "optimizes" a dashboard: every panel gets its own `React.lazy`. The bundle analyzer looks gorgeous — 40 tiny chunks, none over 12 KB. Then field data lands. LCP on 4G regressed from 2.3s to 4.1s, CLS jumped to 0.31, and the loading spinners flash on every panel. On a 150 ms RTT phone, the page now makes a chain of round trips it never made before: fetch the route, parse it, *then* discover it needs panel A, fetch that, discover panel B, fetch that. The "smaller" build is slower because chunks are requests, and requests on mobile are expensive.
</Hook>

<Crux>Splitting trades one download for many. Over-split and the request waterfall on a high-latency phone loses to the bundle you were fixing.</Crux>

<Explanation>

## Splitting is a latency trade, not a free size cut

Bundling everything into one file means the user downloads code for pages they may never visit. Code splitting fixes that by carving the graph into chunks loaded on demand — webpack/Rollup/Vite see a `dynamic import()` and emit a separate file. Less initial JavaScript means faster parse, faster Time-to-Interactive, faster Largest Contentful Paint.

But you did not delete code; you turned one download into several. Each chunk is a separate HTTP request, and a request is not free: connection scheduling, round-trip latency, decompression, and the parse that can only start after the bytes arrive. On a fast desktop connection that overhead disappears into the noise. On a 4G phone with a 100–200 ms round trip, a chain of dependent requests is the dominant cost. The senior question is never "is the bundle smaller?" — it is "how many *sequential* round trips did splitting add to the critical path?"

## Route-based splitting is the safe default; component-level is a scalpel

Route-based splitting is the highest-leverage, lowest-risk split: one chunk per page, loaded when you navigate. The initial route ships only its own code; the rest arrives as the user moves. Frameworks do this for free — Next.js, Remix, and React Router split per route automatically. The win is large and the waterfall is bounded, because each navigation is a deliberate user action with a natural loading moment.

Component-level splitting (`React.lazy` around a heavy widget) is a scalpel, and the rule is: split things that are **heavy and not on the first paint**. A 200 KB rich-text editor that opens behind a button — split it. A charting library used on one tab — split it. A 12 KB card that renders above the fold on load — *never* split it; you have added a round trip and a spinner to save almost nothing. The community heuristic is to only split components above roughly 30–50 KB; below that, the request overhead costs more than the bytes saved.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Candidate</th>
        <th class="border border-border px-3 py-2 text-left">Split?</th>
        <th class="border border-border px-3 py-2 text-left">Why</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">A different route / page</td>
        <td class="border border-border px-3 py-2">Yes — route-based</td>
        <td class="border border-border px-3 py-2">Highest leverage; framework default; waterfall bounded by navigation</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">200 KB editor behind a button</td>
        <td class="border border-border px-3 py-2">Yes — component-level</td>
        <td class="border border-border px-3 py-2">Heavy, below the fold, not on first paint → defer until interaction</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">12 KB card above the fold</td>
        <td class="border border-border px-3 py-2">No</td>
        <td class="border border-border px-3 py-2">Round trip + spinner cost more than the bytes; ship it in the route chunk</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Shared <code>node_modules</code> (React, date lib)</td>
        <td class="border border-border px-3 py-2">Vendor chunk</td>
        <td class="border border-border px-3 py-2">Stable across deploys → long-cached separately from app code</td>
      </tr>
    </tbody>
  </table>
</div>

<FlowDiagram
  label="The chunk graph for one route: the entry route chunk loads on first paint alongside a long-cached vendor chunk, while a lazy component chunk loads on interaction and the next route chunk loads on navigation; above-the-fold content stays inside the entry chunk."
  perRow={2}
  hue="var(--d-frontend)"
  nodes={[
    { id: "entry", label: "Entry route chunk", sub: "first paint" },
    { id: "vendor", label: "Vendor chunk", sub: "long-cached" },
    { id: "lazy", label: "Lazy component", sub: "heavy, deferred" },
    { id: "next", label: "Next route chunk", sub: "on nav" },
  ]}
  edges={[
    { from: "entry", to: "vendor", label: "shared deps" },
    { from: "entry", to: "lazy", label: "on interaction" },
    { from: "entry", to: "next", label: "on navigation" },
  ]}
  caption="The entry route chunk ships everything needed for first paint (including above-the-fold cards); only heavy, off-first-paint code becomes a separate chunk loaded on demand. Each extra chunk is a round trip — over-split into a nested chain and it waterfalls on a high-latency phone."
/>

## The request waterfall: why over-splitting loses on mobile

The failure mode that bites in production is the *waterfall*. With `React.lazy`, the parent route renders, React hits the `lazy` boundary, and only *then* requests the child chunk — sequentially. Nest two or three lazy boundaries and you get a dependent chain: chunk A must arrive and parse before the browser even learns chunk B exists. On a 150 ms RTT link, three sequential chunks add ~450 ms of pure latency before any of them executes, on top of download and parse.

HTTP/2 helps but does not save you. Multiplexing lets *independent* requests share one connection in parallel, so many small chunks issued at once download without the HTTP/1.1 head-of-line penalty. That is real — granular chunking on Next.js used `maxInitialRequests: 25` to ship more, smaller chunks safely. But multiplexing only parallelizes requests the browser knows about *at the same time*. A lazy waterfall is sequential by construction — the browser cannot fetch chunk B in parallel because it has not parsed chunk A yet. And tiny chunks compress worse: gzip/brotli need a window of repeated bytes, so splitting a 115 KB asset into many slivers can lose so much compression that even multiplexing cannot win it back (a classic sprite test: 10 KB combined vs 115 KB split).

<Inset kind="why" lang="en">
"HTTP/2 makes bundling obsolete" is a myth that cost teams real performance. Multiplexing removes the *connection* penalty of many requests, not the *sequential-discovery* penalty of a waterfall and not the *compression* penalty of tiny files. The webpack team's own conclusion: still bundle into n chunks where n is greater than 1 and far less than your module count — find the middle, do not shatter the graph.
</Inset>

## Bundle budgets and the deploy-cache trap

A senior sets a budget and enforces it in CI so a regression fails the build, not the field data. A common starting budget is roughly 200 KB JS and 50 KB CSS (compressed) per route, with the initial route held tightest because it sits on the LCP critical path. Above the split line goes everything needed for first paint and interactivity; below it goes everything gated behind a click, a route, or a scroll.

The subtler trap is the **vendor chunk and cache invalidation**. You isolate `node_modules` into a vendor chunk precisely because it changes rarely — so returning users keep it cached across deploys. But if your bundler hashes vendor and app code together, or inlines the webpack runtime/module-id map into the vendor file, then *every* deploy changes the vendor hash and every user re-downloads React on every release. The fix is granular chunking: a stable vendor chunk, the runtime in its own tiny file, and content-hashed filenames so only the chunks that actually changed get a new URL — Next.js's granular chunking shipped exactly this to stop one app change from invalidating all of vendor.

## Preload the critical chunk; prefetch the likely-next one

If you must split something on the critical path, do not let the browser discover it late. Two resource hints close the gap. `<link rel="preload">` (or `modulepreload` for ES modules) tells the browser to fetch a chunk *now*, in parallel with the document, flattening the waterfall before it forms — use it for a chunk the current page will definitely need. `<link rel="prefetch">` fetches at low priority for a *future* navigation — e.g. prefetch the dashboard chunk on hover over its link, so the click feels instant. Frameworks automate this: Next.js prefetches in-viewport `<Link>` targets, and writing `import()` inside `next/dynamic` lets it inject the preload before render.

The production accident this prevents is the above-the-fold lazy load. Lazy-load a hero or a primary panel and the user first sees the Suspense fallback (a spinner or blank box), then the real component pops in — shifting everything below it. That is a Cumulative Layout Shift hit; one real case went from CLS 0.85 down to 0.1 just by not lazy-loading above the fold and reserving space for what does load. The thresholds you are protecting: LCP under 2.5s, CLS under 0.1, INP under 200 ms. A spinner above the fold attacks all three at once.

<TradeoffMatrix
  id="06-code-splitting-tradeoff-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="A product page must show a 180 KB interactive 3D viewer that sits below the fold, opened only when the user scrolls to it. Pick the loading strategy for a 4G audience."
  options={[
    { name: "React.lazy the viewer, and prefetch its chunk on idle / when it nears the viewport", summary: "Split + prefetch.", correct: true, justification: "The viewer is heavy (180 KB) and below the fold, so it should not block the initial route or LCP — that is exactly what component-level splitting is for. But a naive lazy load would waterfall a spinner in when the user scrolls down. Prefetching the chunk at low priority on idle, or as it nears the viewport, means the bytes are usually already cached by the time it mounts: no critical-path cost, no scroll-time spinner. This is the textbook split-the-heavy-deferred-thing pattern with the waterfall pre-empted by a hint." },
    { name: "Ship the viewer in the main route chunk so it is always ready", summary: "Don't split.", justification: "Puts 180 KB of code most visitors never scroll to onto the LCP critical path, inflating initial parse and TTI for everyone to avoid one spinner. The whole point of splitting is to keep first paint lean; a heavy, below-the-fold, interaction-gated widget is the canonical thing to defer, not inline." },
    { name: "React.lazy every section of the page, including the above-the-fold header", summary: "Split everything.", justification: "Over-splitting. Splitting the above-the-fold header adds a round trip and a spinner to content needed for first paint, hurting LCP and risking CLS, and a chain of lazy boundaries builds a sequential waterfall that loses on a 4G round trip. Split the heavy deferred thing; do not shatter the page." },
    { name: "Load it in an iframe so the browser handles it separately", summary: "Iframe it.", justification: "An iframe is a separate document with its own JS context and overhead, not a code-splitting tool. It does not address chunk strategy, complicates state and styling, and adds its own load cost. The viewer is a component; split it as one." },
  ]}
/>

<Quiz
  id="06-code-splitting-quiz-1"
  lessonSlug="01-overview"
  lang="en"
  question="On a 150 ms RTT 4G phone, why can a build of 40 tiny lazy chunks be slower than one bundle?"
  choices={[
    { label: "Nested lazy boundaries fetch chunks sequentially, so dependent round trips stack onto the critical path and tiny files compress worse", correct: true },
    { label: "Tiny chunks always parse slower than one large file regardless of network", misconception: "Parse scales with total bytes, not file count; the dominant cost here is sequential round-trip latency on a high-RTT link, plus lost compression on slivers." },
    { label: "HTTP/2 cannot download more than one chunk at a time", misconception: "HTTP/2 multiplexes parallel requests fine; the problem is a waterfall is sequential by construction, so there is nothing to parallelize until each parent chunk parses." },
    { label: "Browsers cap JavaScript at one request per page", misconception: "No such cap exists. The issue is dependent discovery: chunk B is unknown until chunk A is fetched and parsed." },
  ]}
/>

<Quiz
  id="06-code-splitting-quiz-2"
  lessonSlug="01-overview"
  lang="en"
  question="Returning users re-download React on every deploy even though your dependencies never changed. What's the likely cause?"
  choices={[
    { label: "The webpack runtime / module-id map is inlined into the vendor chunk, so its content hash changes every build", correct: true },
    { label: "React must be re-fetched whenever any app code changes, by design", misconception: "Not by design — that is the symptom of bad chunking. A properly isolated, content-hashed vendor chunk stays cached across deploys when its bytes don't change." },
    { label: "Browser caches expire vendor files after every deploy automatically", misconception: "Caches key on URL; with content-hashed filenames an unchanged vendor chunk keeps the same URL and stays cached. The hash is changing because something volatile leaked into it." },
    { label: "HTTP/2 invalidates cached chunks on new connections", misconception: "HTTP/2 has nothing to do with cache invalidation; caching is governed by URLs and cache headers, not the transport." },
  ]}
/>

<DragOrder
  id="06-code-splitting-drag-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="Order the decisions a senior makes when adding code splitting to a route:"
  items={[
    { id: "a", label: "Split per route first — biggest win, framework default, waterfall bounded by navigation" },
    { id: "b", label: "Find heavy (~30–50 KB+) components that are below the fold or interaction-gated" },
    { id: "c", label: "React.lazy only those; keep above-the-fold content in the route chunk" },
    { id: "d", label: "Preload critical deferred chunks; prefetch likely-next routes on hover/idle" },
    { id: "e", label: "Isolate a content-hashed vendor chunk so deploys don't re-download dependencies" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Split per route by default; split a component only if it's heavy and off the first paint. Chunks are round trips — over-splitting waterfalls on mobile. Preload critical chunks, isolate a hashed vendor chunk.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-code-splitting-retrieval"
  lang="en"
  questions={[
    { q: "A teammate wants to wrap every component on a page in React.lazy 'to make the bundle smaller.' Explain why this can make a mobile page slower, not faster.", a: "Splitting does not delete code — it converts one download into many separate requests, and each request carries round-trip latency, decompression, and a parse that can only start once the bytes arrive. On a 4G phone with a 100–200 ms RTT, that overhead is the dominant cost, not the byte count. Worse, React.lazy boundaries are discovered sequentially: the parent route must render and hit a lazy boundary before it requests that child chunk, so nested lazy components form a request waterfall — chunk A must arrive and parse before the browser even learns chunk B exists. Three sequential chunks can add ~450 ms of pure latency to the critical path. Tiny chunks also compress worse, because gzip/brotli need a window of repeated bytes. HTTP/2 multiplexing helps independent parallel requests but cannot parallelize a waterfall and cannot recover lost compression. The fix is to split per route, then split only heavy (~30–50 KB+) components that are off the first paint, and to preload anything critical so it is fetched in parallel with the document instead of discovered late." },
    { q: "Why does isolating dependencies into a vendor chunk help caching, and how does it accidentally backfire on every deploy?", a: "node_modules code (React, a date library, a chart lib) changes far less often than your application code, so putting it in its own vendor chunk lets returning users keep it cached across deploys while only the app chunks re-download. With content-hashed filenames, an unchanged vendor chunk keeps the same URL and stays in cache. The backfire happens when something volatile leaks into that chunk's bytes — most commonly the webpack runtime and the module-id map, which can shift whenever you add or reorder modules. If that map is inlined into the vendor file, the vendor hash changes on every build, the URL changes, and every user re-downloads React on every release even though the dependency never changed. The fix is granular chunking: pull the runtime into its own tiny file, give vendor a stable boundary, and content-hash filenames so only chunks whose bytes actually changed get a new URL. Next.js's granular chunking shipped exactly this so a single app change no longer invalidates the entire vendor bundle." },
  ]}
/>

<Recap lang="en">
Code splitting is a latency trade, not a free size cut: it turns one download into many requests, and each request costs a round trip you feel most on a high-latency mobile link. Route-based splitting is the safe, high-leverage default — one chunk per page, waterfall bounded by navigation — and frameworks do it for you. Component-level splitting is a scalpel reserved for things that are both heavy (around 30–50 KB and up) and off the first paint; splitting a small above-the-fold component just buys a round trip and a spinner. The classic production failure is over-splitting into a sequential waterfall that loses to the bundle on 4G, or lazy-loading above the fold and shifting the layout — watch LCP under 2.5s, CLS under 0.1, INP under 200 ms. Set a bundle budget (roughly 200 KB JS per route) and enforce it in CI, preload critical deferred chunks and prefetch likely-next routes, and isolate a content-hashed vendor chunk with the runtime split out so a single app change does not make every user re-download React. The chunk graph is a budget you design, not a number to minimize.
</Recap>
