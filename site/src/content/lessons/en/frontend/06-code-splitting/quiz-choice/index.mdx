---
concepts:
- route-based-splitting
- request-waterfall
- vendor-chunk-invalidation
- preload-prefetch
- over-splitting
- bundle-budget
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://web.dev/articles/code-splitting-suspense
- https://web.dev/articles/granular-chunking-nextjs
- https://nextjs.org/docs/pages/guides/lazy-loading
status: ready
summary: "Multiple-choice synthesis across the code-splitting unit — the latency trade, route vs component splitting, the request waterfall, vendor-chunk caching, and resource hints."
title: 'Code splitting: multiple-choice review'
track: frontend
unit: 06-code-splitting
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. None ask for a definition — each mirrors a splitting decision you make under a real latency budget, where the wrong call regresses LCP or re-downloads React on every deploy.
</Hook>

<Goal lang="en">
Confirm you can connect the latency trade, the route-vs-component rule, the request waterfall, vendor-chunk caching, and resource hints — the synthesis the overview built toward.
</Goal>

<Quiz
  id="06-code-splitting-choice-1"
  lessonSlug="06-code-splitting"
  lang="en"
  question="A team wraps every panel of a dashboard in React.lazy. The analyzer shows 40 chunks, none over 12 KB, yet field LCP on 4G regresses from 2.3s to 4.1s. What is the dominant cause?"
  choices={[
    { label: "Splitting converts one download into many requests; nested lazy boundaries are discovered sequentially, so dependent round trips stack onto the critical path on a high-RTT link, and tiny chunks compress worse.", correct: true },
    { label: "The total bytes shipped went up, so the page simply downloads more code.", misconception: "Splitting did not add code — the same modules are now in more files. The regression is round-trip latency from a sequential waterfall plus lost compression, not extra bytes." },
    { label: "40 small files parse slower than one large file regardless of network.", misconception: "Parse cost scales with total bytes, not file count. The dominant cost on a 4G phone is sequential round-trip latency, which is why the same build is fine on desktop." },
    { label: "The bundle analyzer is misreporting; 40 tiny chunks are always faster.", misconception: "The analyzer is correct about size. Smaller-on-disk is not faster-on-network when each chunk is a request and the requests are dependent — that is the entire premise of the unit." },
  ]}
/>

<Quiz
  id="06-code-splitting-choice-2"
  lessonSlug="06-code-splitting"
  lang="en"
  question="You can split exactly one thing on a page. Which candidate is the highest-leverage, lowest-risk split?"
  choices={[
    { label: "A separate route / page — route-based splitting ships only the current route's code, the rest arrives on navigation, and the waterfall is bounded by a deliberate user action with a natural loading moment.", correct: true },
    { label: "A 12 KB card that renders above the fold on load.", misconception: "Splitting an above-the-fold component buys a round trip and a spinner to save almost nothing, hurting LCP and risking CLS. Ship small above-the-fold content in the route chunk." },
    { label: "Every leaf component, so each is as small as possible.", misconception: "Over-splitting shatters the graph into a sequential waterfall and destroys compression. The rule is split heavy, off-first-paint things — not everything." },
    { label: "The shared React runtime, so it loads first.", misconception: "React is needed for first paint, so deferring it just adds a round trip to the critical path. React belongs in a long-cached vendor chunk, not a lazy boundary." },
  ]}
/>

<Quiz
  id="06-code-splitting-choice-3"
  lessonSlug="06-code-splitting"
  lang="en"
  question="A reviewer claims 'HTTP/2 multiplexing makes bundling obsolete, so split as granularly as you like.' Where is this wrong?"
  choices={[
    { label: "Multiplexing removes the connection penalty of many parallel requests, but it cannot parallelize a sequential lazy waterfall (chunk B is unknown until chunk A parses) and cannot recover the compression lost when one asset is split into slivers.", correct: true },
    { label: "It is correct — with HTTP/2 the number of chunks no longer matters at all.", misconception: "Multiplexing only helps requests the browser knows about at the same time. A waterfall is sequential by construction, and tiny files compress worse — both costs survive HTTP/2." },
    { label: "HTTP/2 cannot download more than one chunk per connection.", misconception: "HTTP/2 does multiplex many parallel requests over one connection — that part of the claim is right. The flaw is sequential discovery and lost compression, not parallelism." },
    { label: "Bundling is always faster than splitting under any transport.", misconception: "Not always — route-based splitting still wins by keeping the initial route lean. The point is to find the middle: n chunks where n is far less than your module count, not to shatter or to ship one giant file." },
  ]}
/>

<Quiz
  id="06-code-splitting-choice-4"
  lessonSlug="06-code-splitting"
  lang="en"
  question="Returning users re-download React on every deploy even though dependencies never changed. Most likely cause?"
  choices={[
    { label: "The webpack runtime / module-id map is inlined into the vendor chunk, so a single app change shifts the map, changes the vendor content hash and URL, and busts the cache for everyone.", correct: true },
    { label: "React must be re-fetched whenever any app code changes, by design.", misconception: "That is the symptom of bad chunking, not a design rule. A properly isolated, content-hashed vendor chunk stays cached across deploys while its bytes are unchanged." },
    { label: "Browser caches expire vendor files automatically on each deploy.", misconception: "Caches key on URL. With content-hashed filenames an unchanged vendor chunk keeps the same URL and stays cached; the hash is changing because something volatile leaked into it." },
    { label: "The vendor chunk is too large, so the browser refuses to cache it.", misconception: "Size does not block caching here. The defect is hash volatility — pull the runtime into its own tiny file so vendor's bytes stay stable across deploys." },
  ]}
/>

<Quiz
  id="06-code-splitting-choice-5"
  lessonSlug="06-code-splitting"
  lang="en"
  question="A 180 KB interactive viewer sits below the fold and opens only when the user scrolls to it. Best loading strategy for a 4G audience?"
  choices={[
    { label: "React.lazy the viewer and prefetch its chunk at low priority on idle or as it nears the viewport, so the bytes are usually cached before it mounts — no critical-path cost, no scroll-time spinner.", correct: true },
    { label: "Ship it in the main route chunk so it is always ready.", misconception: "That puts 180 KB most visitors never scroll to onto the LCP critical path, inflating parse and TTI for everyone to avoid one spinner. A heavy, below-the-fold, interaction-gated widget is the canonical thing to defer." },
    { label: "React.lazy every section of the page including the header for consistency.", misconception: "Splitting the above-the-fold header adds a round trip and spinner to first-paint content and builds a sequential waterfall. Split the heavy deferred thing; do not shatter the page." },
    { label: "Load it in an iframe so the browser handles it separately.", misconception: "An iframe is a separate document with its own JS context and load cost, not a chunk-strategy tool. The viewer is a component — split it as one and pre-empt the waterfall with a prefetch." },
  ]}
/>

<Quiz
  id="06-code-splitting-choice-6"
  lessonSlug="06-code-splitting"
  lang="en"
  question="What is the practical difference between rel='preload' (or modulepreload) and rel='prefetch' for chunks?"
  choices={[
    { label: "preload fetches a chunk the current page will definitely need now, in parallel with the document, flattening the waterfall before it forms; prefetch fetches at low priority for a likely future navigation, e.g. on hover, so the next click feels instant.", correct: true },
    { label: "They are interchangeable — both just download a chunk earlier.", misconception: "Priority and intent differ. preload is high-priority for a chunk the current page needs; prefetch is idle-priority speculation for a future navigation. Using prefetch for a critical chunk leaves it discovered too late." },
    { label: "preload is for images and prefetch is for scripts.", misconception: "Both apply to script/module chunks (modulepreload for ES modules). The split is current-page-critical vs likely-future, not by asset type." },
    { label: "prefetch blocks rendering until the chunk arrives; preload does not.", misconception: "Neither blocks rendering — both are hints. prefetch is the low-priority speculative one; preload is the high-priority current-need one. Reversed from the claim." },
  ]}
/>

<Recap lang="en">
The through-line of the unit is one decision tree: splitting is a latency trade, not a free size cut, so the question is always how many sequential round trips you added to the critical path. Split per route by default (bounded waterfall, framework default); split a component only if it is heavy and off the first paint; never split small above-the-fold content. The waterfall — not byte count — is what loses on 4G, and HTTP/2 does not save it. Defend caching with a content-hashed vendor chunk and the runtime split out, and use preload for current-critical chunks, prefetch for likely-next navigations.
</Recap>
