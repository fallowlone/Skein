---
concepts:
- counting-principle
- permutation
- combination
- factorial
- order-matters
- model-real-counting
estMin: 200
lang: en
level: middle
order: 10
prereqs: []
slug: project
sources:
- https://openstax.org/books/contemporary-mathematics/pages/7-4-combinations
- https://www.khanacademy.org/math/statistics-probability/counting-permutations-and-combinations
status: ready
summary: "Hands-on project — build a one-page 'counting handbook' that solves real possibility-counting questions by choosing the right rule, showing the arithmetic, and proving the count is exact."
title: 'Combinatorics: build a counting handbook'
track: math
unit: 09-combinatorics
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Knowing the three rules is not the same as reaching for the right one under pressure. In this project you turn the unit into a reusable tool: a one-page handbook that takes any 'how many possibilities' question, routes it to the counting principle, a permutation, or a combination, and proves the count without listing.
</Hook>

<Goal lang="en">
Turn the unit's three rules into a repeatable method: read a counting situation, decide whether choices are independent and whether order matters, apply the matching rule, show the arithmetic, and verify the count by brute-force listing on a small version of the same problem.
</Goal>

<ProjectBrief
  id="09-combinatorics-project"
  lessonSlug="09-combinatorics"
  lang="en"
  objective="Build a one-page 'counting handbook' — a decision flow plus six fully worked real-world counting problems — that demonstrates you can pick and apply the right rule (counting principle, permutation, or combination) and prove each count is exact."
  requirements={[
    "Write a short decision flow in your own words: question 1 — are the choices separate and independent, or am I drawing from one set? Question 2 (only if one set) — does order matter? Map each path to its rule: multiply option counts, multiply shrinking factors (permutation), or shrinking factors divided by r! (combination).",
    "Collect six counting problems from real life or invent them — e.g. outfits from a wardrobe, a 4-digit PIN, the finishing order of a race, a committee chosen from a class, a starting line-up, lottery-style picks. At least two must use the counting principle, at least two a permutation, and at least two a combination.",
    "For each problem, write one sentence answering both decision questions before you compute anything, naming the rule you will use and why.",
    "Solve each problem showing every multiplication or division — e.g. 5 × 4 × 3 = 60, then ÷ 3! = 6 → 10 — not just the final number.",
    "For one permutation problem and one combination problem, verify the count by listing all outcomes on a deliberately tiny version (such as 3 items, not 8) and checking the list length equals your formula's answer.",
    "Include a 'traps I avoided' note: at least two specific mistakes the problems could have triggered (adding instead of multiplying, using full n! when only r positions are filled, forgetting to divide by r! when order does not matter) and how your decision flow caught each.",
  ]}
  acceptance={[
    "All six problems are solved with the correct rule and the correct final count, each preceded by the two-question routing sentence.",
    "Every answer shows its arithmetic step by step, not only the result.",
    "The two brute-force verifications list every outcome on the small version and the count matches the formula exactly.",
    "The decision flow at the top, applied to any of the six problems, leads to the rule that problem actually used — no problem contradicts its own routing.",
  ]}
  stretch={[
    "Add a seventh problem where the naive approach over- or under-counts (such as a round-table seating where rotations are the same, or a password that allows repeated characters) and explain how it bends one of the three rules.",
    "Add a tiny estimate-vs-exact panel: for one problem, write down a rough guess before computing, then compare it to the exact count and reflect on how fast factorials make intuition fail.",
    "Turn the handbook into a one-screen flowchart (boxes and arrows) so someone who has not taken the unit could route a new problem to the right rule.",
    "Pick one problem and compute both its permutation count and its combination count, then write one sentence on what real-world change (adding roles, ranking, or order) would flip the answer from one to the other.",
  ]}
/>

<Recap lang="en">
This is the method you will run on every real counting question: route with two questions — separate choices or one set, and does order matter — then apply the matching rule, show the arithmetic, and sanity-check the formula against a tiny brute-force list. Building the handbook once forces the routing to become automatic, so the next time someone asks 'how many possibilities are there', you reach for the right rule instead of trying to list them.
</Recap>
