---
concepts:
- fraction
- equivalent-fractions
- common-denominator
- decimal
- percent
- fraction-decimal-percent
estMin: 14
lang: en
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://openstax.org/books/prealgebra-2e/pages/4-1-visualize-fractions
- https://www.khanacademy.org/math/arithmetic-home/arith-review-fractions
status: ready
summary: "Free-recall prompts across the fractions unit — say a full answer from memory first, then reveal the model answer and compare."
title: 'Fractions: free-recall review'
track: math
unit: 03-fractions
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Recalling beats re-reading. For each prompt, say or write a full answer from memory before you open the model answer — the effort of pulling it back is what makes the idea stick.
</Hook>

<Goal lang="en">
Reconstruct the unit's core ideas — what makes a fraction, how equivalence and simplest form work, why adding needs a common denominator, and how decimals and percents name the same amount — without looking back at the lessons.
</Goal>

<RetrievalDrawer
  client:load
  id="03-fractions-short-retrieval"
  lang="en"
  questions={[
    { q: "What do the two numbers in a fraction mean, and why must the parts be equal?", a: "A fraction names a part of a whole. The bottom number, the denominator, says how many equal parts the whole was cut into; the top number, the numerator, says how many of those parts you have. So in 3/8 the bar is in 8 equal pieces and you have 3 of them. The parts must be equal because the denominator is a promise about size: '1 of 4 equal shares' is a known amount, but if the four pieces could be any size, 1/4 would tell you nothing about how much you hold. Equal parts is what gives a fraction a definite value." },
    { q: "How do you build an equivalent fraction and how do you simplify, and what is simplest form?", a: "Multiply the numerator and denominator by the same number to build an equivalent fraction: 1/2 = 3/6 (both times 3). Divide them by the same number to simplify: 6/8 = 3/4 (both divided by 2). It works because you do the same thing to both halves — cutting every part into more pieces and getting proportionally more of them leaves the total unchanged. A fraction is in simplest form when no number above 1 divides both the top and the bottom, so 1/2 is the simplest name for that amount. Never add to the parts and never change just one of them." },
    { q: "Why can't you add 1/2 + 1/3 directly, and what is the procedure?", a: "You can't add them directly because the parts are different sizes — half-sized pieces and third-sized pieces — and you cannot count unlike-sized pieces together. The fix is a common denominator: a number both denominators divide into. For 2 and 3 that is 6. Rewrite each fraction over 6 using equivalent fractions: 1/2 = 3/6 and 1/3 = 2/6. Now the parts match, so add only the numerators and keep the shared denominator: 3/6 + 2/6 = 5/6. Simplify the result if you can. Never add the denominators — a denominator is the size of a part, not a count." },
    { q: "What is a decimal in terms of place value, and why is 0.5 the same as 0.50?", a: "A decimal writes a fraction using place value, continuing the ones-tens-hundreds pattern downward past the decimal point. The first place after the point is tenths (each worth 1/10), the next is hundredths (1/100), each ten times smaller than the one before. So 0.7 is 7 tenths, which is 7/10. 0.5 equals 0.50 because 0.50 is 5 tenths and 0 hundredths — the extra 0 adds no hundredths, so the amount is unchanged. This differs from whole numbers, where a 0 on the right does matter; after the point a trailing 0 is just an empty smaller place." },
    { q: "What is a percent, and how do you convert between a percent, a decimal, and a fraction?", a: "A percent is a fraction whose denominator is always 100 — the sign % means 'per hundred'. So 50% is 50/100, which simplifies to 1/2 and equals the decimal 0.5. To go from a percent to a decimal, divide by 100 (move the point two places left): 25% = 0.25. To go from a decimal to a percent, multiply by 100: 0.6 = 60%. The fixed denominator is what makes percents directly comparable — 45% versus 52% is instant, where 9/20 versus 13/25 is not. 100% is 100/100 = 1, the whole thing." },
    { q: "How do you find a percent of a number, and what is the common mistake?", a: "'Percent of' means convert the percent to a fraction over 100 or a decimal, then multiply, because 'of' means multiply. So 20% of 50 is 20/100 × 50 = 0.2 × 50 = 10, and 25% of 80 is 1/4 of 80 = 20. The common mistake is treating the % as decoration and computing with the bare number: 20 × 50 = 1000, which is 100 times too big. Always convert the percent first. A useful check: 25% is a quarter, so 25% of 80 should be well under half of 80 (40) — and 20 fits." },
  ]}
/>

<Recap lang="en">
If you could rebuild each answer from memory, you hold the unit's spine: a fraction is equal parts named by numerator over denominator; equivalence comes from multiplying or dividing both numbers by the same value; adding needs a common denominator because you can only count same-sized parts; a decimal is the same amount written with place value; and a percent fixes that denominator at 100, so fraction, decimal, and percent are three interchangeable names — and 'percent of' is always convert, then multiply.
</Recap>
