---
slug: 03-adding-fractions
lang: en
track: math
unit: 03-fractions
order: 3
title: "Adding fractions"
summary: "Add fractions with the same bottom number by adding the tops — and when the bottoms differ, make them match first with equivalent fractions."
estMin: 16
status: ready
prereqs: ["01-what-is-a-fraction", "02-equivalent-fractions"]
concepts: ["adding-fractions", "common-denominator"]
sources:
  - https://openstax.org/books/prealgebra-2e/pages/4-5-add-and-subtract-fractions-with-common-denominators
  - https://www.khanacademy.org/math/arithmetic-home/arith-review-fractions
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Step from "~/components/lesson/Step.astro";
import WorkedExample from "~/components/lesson/WorkedExample.astro";
import Check from "~/components/lesson/Check.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import BarModel from "~/components/math/BarModel.astro";
import PracticeSet from "~/components/math/PracticeSet.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
You eat one quarter of a pizza, then another quarter. How much pizza did you eat? Two
quarters — `2/4`, which is half. The slices were the same size, so you just counted
them. That is the whole secret of adding fractions: the parts must be the same size.
</Hook>

<Goal lang="en">
After this lesson you can add and subtract fractions that have the same denominator,
explain why a shared denominator is required, find a common denominator for fractions
that differ, and add them by rewriting each one first.
</Goal>

<Step n={1}>
**When the denominators match, add the numerators and keep the denominator.** `1/4`
and `2/4` are both made of quarter-sized parts. One quarter plus two quarters is three
quarters: `1/4 + 2/4 = 3/4`. You are counting parts of the same size, so the
denominator — the size of one part — does not change. Only the count changes.
</Step>

<BarModel total={4} filled={3} label="1/4 + 2/4 = 3/4" />

<Step n={2}>
**Subtraction with matching denominators works the same way.** Subtract the numerators
and keep the denominator: `3/8 − 1/8 = 2/8`. Three eighth-sized parts, take away one,
leaves two. Again the denominator stays put because the parts are all the same size.
</Step>

<Step n={3}>
**When the denominators differ, you cannot add yet.** `1/2 + 1/3` mixes half-sized
parts with third-sized parts — different sizes, so you cannot just count them
together. First you must rewrite both fractions so they share one denominator. A
denominator that both can be rewritten to is called a **common denominator**.
</Step>

<Step n={4}>
**Find a common denominator, rewrite both fractions, then add.** A number that both
denominators divide into works. For `1/2` and `1/3`, the number 6 works: 2 and 3 both
divide into 6. Rewrite each fraction with 6 on the bottom using equivalent fractions —
then the denominators match and you add the numerators as in Step 1. Simplify the
result at the end if it can be simplified.
</Step>

<WorkedExample lang="en">
**Add 1/2 + 1/3.**

The denominators 2 and 3 differ, so find a common denominator: 6 works, because 2 and
3 both divide into 6.

Rewrite `1/2` with denominator 6: multiply top and bottom by 3 — `1/2 = 3/6`.

Rewrite `1/3` with denominator 6: multiply top and bottom by 2 — `1/3 = 2/6`.

Now the denominators match. Add the numerators: `3/6 + 2/6 = 5/6`. Since 5 and 6 share
no divider above 1, `5/6` is already in simplest form. So `1/2 + 1/3 = 5/6`.
</WorkedExample>

<Inset kind="why" lang="en">
Why must the denominators match before you add? Because a denominator is the *size* of
one part. Adding `1/2 + 1/3` directly would be like adding one large slice and one
small slice and calling the answer "2 slices" — but slices of what size? The common
denominator recuts both fractions into parts of one size, and only then can you count
them together honestly.
</Inset>

<Inset kind="mistake" lang="en">
The most common mistake is adding the denominators too: writing `1/2 + 1/3 = 2/5`. The
denominator is the size of a part, not a count — it must not be added. Once the
denominators match, add *only* the numerators and leave the shared denominator alone.
</Inset>

<PracticeSet
  id="adding-fractions-practice"
  lessonSlug="03-adding-fractions"
  lang="en"
  problems={[
    { prompt: "Add 1/5 + 2/5. The sum has denominator 5 — type its numerator.", answer: 3, hint: "Denominators match: add the numerators 1 + 2." },
    { prompt: "Subtract 3/8 − 1/8. The result has denominator 8 — type its numerator.", answer: 2, hint: "Denominators match: subtract the numerators." },
    { prompt: "To add 1/2 + 1/4, type a common denominator both 2 and 4 divide into.", answer: 4, hint: "4 works: both 2 and 4 divide into it." },
    { prompt: "1/2 + 1/4 rewritten over 4 is ?/4. Type the numerator of the sum.", answer: 3, hint: "1/2 = 2/4, and 2/4 + 1/4 adds the numerators." },
    { prompt: "To add 1/3 + 1/6, type the smallest common denominator.", answer: 6, hint: "Both 3 and 6 divide into 6." },
  ]}
/>

<Check lang="en">
  <Quiz
    id="adding-fractions-check"
    pieceSlug="03-adding-fractions"
    lang="en"
    question="Why can't you add 1/2 + 1/3 by just adding tops and bottoms to get 2/5?"
    choices={[
      { label: "The parts are different sizes; you must give both a common denominator first", correct: true },
      { label: "2/5 is too small to be a real fraction", misconception: "2/5 is a perfectly real fraction — it just is not the correct sum, because the parts were not the same size." },
      { label: "You can never add fractions at all", misconception: "Fractions add fine — once they share a denominator. The denominators here simply differ." },
    ]}
  />
</Check>

<Recap lang="en">
To add or subtract fractions with the same denominator, add or subtract the numerators
and keep the denominator — you are counting equal-sized parts. When the denominators
differ, you cannot add yet: find a common denominator, rewrite both fractions to it
with equivalent fractions, then add the numerators. Never add the denominators — a
denominator is the size of a part, not a count.
</Recap>
