---
concepts:
- function-rule
- function-notation
- domain-range
- linear-function
- slope-starting-value
- graph-point
estMin: 14
lang: en
level: middle
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://openstax.org/books/algebra-and-trigonometry-2e/pages/3-1-functions-and-function-notation
- https://www.khanacademy.org/math/algebra/x2f8bb11595b61c86:functions
status: ready
summary: "Multiple-choice synthesis across the functions unit: the one-output rule, function notation, evaluation, linear rate and starting value, and reading a graph."
title: 'Functions: multiple-choice review'
track: math
unit: 06-functions
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one tests whether you can use a function, not just recite its definition — feed inputs in, read outputs out, and recognise the rule behind the picture.
</Hook>

<Goal lang="en">
Confirm you can connect the four ideas the lessons built: a function is a one-output-per-input rule, notation names that rule, a linear function is a steady rate plus a starting value, and its graph is a straight line.
</Goal>

<Quiz
  id="06-functions-choice-1"
  lessonSlug="06-functions"
  lang="en"
  question="A machine returns 5 the first time you feed in 2, then returns 9 the next time you feed in 2. Is it a function, and why?"
  choices={[
    { label: "No. A function must give the same output every time for the same input; 2 produced 5 and then 9, so the one-output rule is broken.", correct: true },
    { label: "Yes, because both 5 and 9 are valid numbers it can output.", misconception: "Producing valid numbers is not enough. The defining rule is that one input maps to exactly one output, every time — two different outputs for input 2 disqualifies it." },
    { label: "Yes, because the inputs were the same each time.", misconception: "Same input is exactly the case the rule constrains: the same input must yield the same output. Here it did not, so it is not a function." },
    { label: "It cannot be decided without a formula.", misconception: "You do not need a formula. A function can be a table or a machine; the test is behavioural — same input, same output. That already failed here." },
  ]}
/>

<Quiz
  id="06-functions-choice-2"
  lessonSlug="06-functions"
  lang="en"
  question="A function follows the rule f(n) = 3n + 4. What is f(5)?"
  choices={[
    { label: "19 — substitute the input 5 for n: 3 × 5 = 15, then add 4 to get 19.", correct: true },
    { label: "27 — add the 3 and the 5 first, then multiply.", misconception: "The 3 is attached to n, so it multiplies the input: 3 × 5 = 15, then + 4. Adding 3 and 5 first treats the rate as if it stood alone." },
    { label: "12 — multiply the 3 and the 4, then ignore the input.", misconception: "The input is the whole point of evaluating a function. f(5) means feed 5 into the rule: 3 × 5 + 4. You cannot drop the input." },
    { label: "23 — add 4 to the input first, then multiply by 3.", misconception: "Order matters: the number attached to n (the rate) multiplies the input, then the lone number is added. That is 3 × 5 + 4 = 19, not 3 × (5 + 4)." },
  ]}
/>

<Quiz
  id="06-functions-choice-3"
  lessonSlug="06-functions"
  lang="en"
  question="A vending machine accepts only buttons A1, A2, and A3, and each drops one specific snack. What are its inputs (domain) and outputs (range)?"
  choices={[
    { label: "Inputs are the buttons it accepts — A1, A2, A3; outputs are the snacks those buttons can drop. The domain is what you may feed in; the range is what can come out.", correct: true },
    { label: "Inputs and outputs are the same set, since each button maps to one snack.", misconception: "A one-to-one mapping does not make the two sets identical. Buttons go in, snacks come out — different things. The domain is the allowed inputs; the range is the produced outputs." },
    { label: "The inputs are the snacks and the outputs are the buttons.", misconception: "It is reversed. You press a button (input) and a snack comes out (output). The thing you supply is the input; the thing produced is the output." },
    { label: "There are no inputs because the machine decides on its own.", misconception: "The button you press is the input — it is exactly what determines the output. A function always has inputs; here they are the accepted buttons." },
  ]}
/>

<Quiz
  id="06-functions-choice-4"
  lessonSlug="06-functions"
  lang="en"
  question="For the linear function y = 4x + 7, the input changes from 2 to 3. By how much does the output change, and why?"
  choices={[
    { label: "It rises by 4. The output changes by the slope for every 1-step change in the input, and 4 is the number attached to x.", correct: true },
    { label: "It rises by 7, the starting value.", misconception: "The starting value is the output at input 0, not the per-step change. The per-step change is the slope — the number multiplied by the input, which is 4." },
    { label: "It rises by 11, because 4 + 7 = 11.", misconception: "Adding the rate and the starting value mixes two different roles. The step per input is just the slope, 4; the 7 sits there unchanged as the input moves." },
    { label: "It cannot be known without computing both outputs.", misconception: "That is the whole point of linear: the step is constant. You already know it equals the slope, 4, without computing y at 2 and 3." },
  ]}
/>

<Quiz
  id="06-functions-choice-5"
  lessonSlug="06-functions"
  lang="en"
  question="On a function's graph there is a point at (6, 2). What does this point tell you?"
  choices={[
    { label: "Feeding input 6 into the function produced output 2 — across first to 6, then up to 2.", correct: true },
    { label: "Feeding input 2 produced output 6.", misconception: "The pair order is fixed: first number is the input (across), second is the output (up). (6, 2) is input 6, output 2 — not the reverse." },
    { label: "The function multiplies 6 by 2.", misconception: "A point is a pair, not a multiplication. It records one input-output result the function produced, here 6 in and 2 out." },
    { label: "The output is both 6 and 2 because both axes meet there.", misconception: "A single point names one input and one output, not two outputs. The first coordinate is the input on the horizontal axis, the second is the output on the vertical axis." },
  ]}
/>

<Quiz
  id="06-functions-choice-6"
  lessonSlug="06-functions"
  lang="en"
  question="You plot the outputs of f(n) = 2n + 1 for n = 0, 1, 2, 3 and they land at (0,1), (1,3), (2,5), (3,7). Without plotting more points, what shape will the full graph be, and why?"
  choices={[
    { label: "A straight line. The output rises by exactly 2 for every 1-step increase in input — a constant step — and a constant step can only draw a line, never a curve.", correct: true },
    { label: "A curve, because the outputs keep getting bigger.", misconception: "Getting bigger is not what bends a graph; a changing step is. Here the step is a fixed 2 each time, so the points stay perfectly straight." },
    { label: "A line only between the four points, then unknown.", misconception: "A linear rule applies the same constant step everywhere, not just between sampled points. The whole graph is one straight line." },
    { label: "It depends on whether the inputs are whole numbers.", misconception: "The constant step holds for every input, not just whole ones. The slope of 2 forces a straight line across the entire graph." },
  ]}
/>

<Recap lang="en">
The through-line of the unit is one machine: an input goes in and exactly one output comes out, every time. Function notation like f(5) just names the rule and the input you feed it; the domain is what you may feed in, the range is what comes out. A linear function adds structure — a steady rate (the slope, the per-step change) plus a starting value (the output at input 0) — and because that step never changes, its graph is always a straight line, with each point read as (input, output).
</Recap>
