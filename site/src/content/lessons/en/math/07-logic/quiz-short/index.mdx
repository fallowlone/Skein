---
concepts:
- statement
- truth-value
- logical-and
- logical-or
- logical-not
- union-intersection
estMin: 12
lang: en
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://openstax.org/books/contemporary-mathematics/pages/2-1-statements-and-quantifiers
- https://openstax.org/books/contemporary-mathematics/pages/1-1-basic-set-concepts
status: ready
summary: "Free-recall prompts across the logic-and-sets unit: statements, the AND/OR/NOT rules, set membership, and union versus intersection. Answer first, then check."
title: 'Logic and sets: free-recall review'
track: math
unit: 07-logic
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Recalling beats re-reading. For each prompt, say or write a full answer from memory first — then open the model answer and compare. The effort of pulling it back is what makes it stick.
</Hook>

<Goal lang="en">
Reconstruct the unit's core ideas from memory: what makes a sentence a statement, how AND, OR, and NOT decide a truth value, what membership means for a set, and how union and intersection differ.
</Goal>

<RetrievalDrawer
  client:load
  id="07-logic-short-retrieval"
  lang="en"
  questions={[
    { q: "What makes a sentence a statement, and what does its truth value mean?", a: "A statement is a sentence that is definitely true or definitely false — it lands firmly on one side. Questions ('What time is it?'), commands ('Close the door'), and opinions ('This soup is tasty') are not statements, because none of them has a single fixed answer. The truth value of a statement is which of the two it is, often written 1 for true and 0 for false, like a switch that is on or off. A statement carries exactly one truth value and never both. Crucially, a false statement is still a statement: '2 + 2 = 5' is a perfectly good statement with truth value 0." },
    { q: "State the rule for AND and the rule for OR, and the one case where each one fails.", a: "AND is true only when both parts are true; it fails the moment even one part is false. So 'sunny AND warm' holds only if both hold. OR is true when at least one part is true; it fails only when both parts are false. So 'sunny OR warm' holds if either one holds, and also if both do. The clean way to remember it: AND is the strict connective with a single way to be true (both true), and OR is the generous connective with a single way to be false (both false)." },
    { q: "What does NOT do, and how would you evaluate NOT (a false statement)?", a: "NOT takes one statement and reverses its truth value: NOT of a true statement is false, and NOT of a false statement is true. It is the switch that turns 1 into 0 and 0 into 1. So NOT (a false statement) is true. For example '2 &gt; 9' is false, so 'NOT (2 &gt; 9)' is true. NOT acts on a single statement, unlike AND and OR which combine two." },
    { q: "What is a set, and why does listing an element twice change nothing?", a: "A set is a collection of distinct things called its elements, written inside curly brackets, like {1, 2, 3}. Order does not matter and repeats do not matter, because a set only records WHETHER something belongs — a yes-or-no membership question, exactly like the true/false of a statement. So {1, 1, 2} and {1, 2} are the same set: both answer 'is 1 in?' with yes and 'is 2 in?' with yes. There is no count of copies to track; an element is simply in or out. The size of a set is how many distinct elements it has, and the empty set has size 0." },
    { q: "Explain the difference between the union and the intersection of two sets, with one example.", a: "The union of two sets gathers everything that is in either one, each element listed once; it is the generous combination, tied to 'or'. The intersection keeps only the elements that are in both; it is the strict combination, tied to 'and'. For A = {1, 2, 3} and B = {2, 3, 4}: the union is {1, 2, 3, 4} (size 4) and the intersection is {2, 3} (size 2). The union is always at least as big as the larger set, and the intersection is always at most as big as the smaller set." },
    { q: "How do the two set operations mirror the two logical connectives from earlier in the unit?", a: "Membership in a set is a yes-or-no question, the same shape as the true/false truth value of a statement. Building a set operation then mirrors building a compound statement. An element is in the union exactly when it is in A OR in B — union is the connective OR applied to membership. An element is in the intersection exactly when it is in A AND in B — intersection is the connective AND applied to membership. So union is the 'or' of sets and intersection is the 'and' of sets; the unit's two halves are the same machinery seen twice." },
  ]}
/>

<Recap lang="en">
If you could rebuild each answer from memory, you hold the unit's spine: a statement carries a fixed true/false value; AND needs both true, OR needs at least one, NOT flips; a set records yes-or-no membership with no count of repeats; and the two set operations are just the two connectives applied to membership — union is 'or', intersection is 'and'.
</Recap>
