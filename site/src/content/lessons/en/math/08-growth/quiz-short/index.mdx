---
concepts:
- linear-growth
- exponential-growth
- logarithm
- logarithm-base
- doubling
estMin: 14
lang: en
level: middle
order: 8
prereqs: []
slug: quiz-short
sources:
- https://openstax.org/books/college-algebra-2e/pages/6-1-exponential-functions
- https://openstax.org/books/college-algebra-2e/pages/6-3-logarithmic-functions
status: ready
summary: "Free-recall prompts across the growth unit. Answer each in your own words first, then reveal the model answer and compare."
title: 'Growth: free-recall review'
track: math
unit: 08-growth
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Recall sticks better than re-reading. For each prompt, say or write a full answer from memory first — then open the model answer and check it against yours. The effort of pulling it from memory is what makes it last.
</Hook>

<Goal lang="en">
Reconstruct the unit's core ideas from memory — the difference between adding and multiplying, why exponential growth overtakes linear, what a logarithm is, why the base matters, and why the log grows so slowly.
</Goal>

<RetrievalDrawer
  client:visible
  id="08-growth-short-retrieval"
  lang="en"
  questions={[
    { q: "What is the difference between linear growth and exponential growth?", a: "Linear growth adds the same fixed amount each step: start at 0 and add 10 a day, and you get 10, 20, 30, 40 — the step never changes. Exponential growth multiplies by the same fixed amount each step: start at 1 and double, and you get 1, 2, 4, 8, 16 — the step itself keeps growing, because each step acts on a bigger number than the last. The test is simple: ask whether the rule adds a fixed amount or multiplies by a fixed amount. Adding is linear; multiplying is exponential." },
    { q: "Why does exponential growth always overtake linear growth in the long run, even if it starts slower?", a: "Because a linear step is fixed forever, but an exponential step grows with the total. Early on, adding 10 a day can beat doubling from 1 — on day 2 the linear jar has 20 and the doubling jar only 4. But doubling compounds: each step is bigger than the last, so eventually the multiplying step is larger than any fixed amount, and from then on the linear plan can never catch up. Even a tiny multiplier like 1.01 eventually produces steps bigger than any fixed number. Fixed beats growing only for a while; growing wins in the end." },
    { q: "What is a logarithm, and how does it relate to an exponent?", a: "A logarithm is the reverse of an exponent. An exponent runs forward: 2³ = 8 means multiply three 2s to get 8. A logarithm runs that backward: it starts from 8 and recovers the exponent, so log₂(8) = 3. Just as the square root reverses squaring, the logarithm reverses raising to a power. Concretely, log₂(8) asks 'how many times must I multiply 2 to reach 8?' — the answer is the exponent, 3, because 2³ = 8. They are the same fact read in opposite directions." },
    { q: "Why is the base written with every logarithm — why does it matter?", a: "The base is the number you are multiplying, and 'how many times you multiply' only has a clear answer once you say multiply by what. log₂(8) = 3 because you multiply three 2s; but a logarithm with a different base asks a different question about the same number. The base names the step size of the multiplication, so changing the base changes the question and the answer. That is why it is always written right there with the log — log₂, log₁₀, and so on — rather than left implicit." },
    { q: "Why does a logarithm grow so slowly, and what makes that useful?", a: "A logarithm grows slowly because it is the reverse of exponential growth, which exploded. For log₂, each time the input doubles, the output rises by only 1 — so as the input runs from 1 all the way to 64, the output climbs only from 0 to 6. The logarithm of a number is roughly 'how many doublings it took to get here.' That slowness is exactly what makes it useful: it turns a runaway exponential quantity into a small, calm number you can compare and reason about, which is why logarithms appear wherever growth explodes." },
    { q: "Work out log₂(64) and log₁₀(1000), and explain what each answer counts.", a: "log₂(64) = 6, because 2 × 2 × 2 × 2 × 2 × 2 = 64, i.e. 2⁶ = 64 — six 2s multiplied, or equivalently six doublings from 1 to reach 64. log₁₀(1000) = 3, because 10 × 10 × 10 = 1000, i.e. 10³ = 1000 — three 10s multiplied, which matches 1000 being a 1 with three zeros. In both cases the logarithm counts how many times you multiply the base to reach the target: the exponent. The base just changes the step size — multiply by 2, or multiply by 10." },
  ]}
/>

<Recap lang="en">
If you could rebuild each answer from memory, you hold the unit's spine: linear growth adds a fixed amount, exponential growth multiplies by one, and multiplying always overtakes adding in the long run. A logarithm reverses an exponent — log₂(N) is the exponent on 2 that reaches N, written with its base because the base sets the question. The log grows by just 1 per doubling, which is why it stays a small, calm scale for measuring explosive growth.
</Recap>
