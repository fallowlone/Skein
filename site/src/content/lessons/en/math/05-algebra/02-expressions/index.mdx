---
slug: 02-expressions
lang: en
track: math
unit: 05-algebra
order: 2
title: "Expressions"
summary: "An expression combines numbers, variables, and operations into something with a value — evaluate it by substituting, simplify it by combining like terms."
estMin: 15
status: ready
prereqs: ["01-variables", "03-multiplication"]
concepts: ["expression", "like-terms"]
sources:
  - https://openstax.org/books/prealgebra-2e/pages/2-1-use-the-language-of-algebra
  - https://www.khanacademy.org/math/algebra-basics/alg-basics-foundations
---

import Hook from "../../../../../../components/lesson/Hook.astro";
import Goal from "../../../../../../components/lesson/Goal.astro";
import Step from "../../../../../../components/lesson/Step.astro";
import WorkedExample from "../../../../../../components/lesson/WorkedExample.astro";
import Check from "../../../../../../components/lesson/Check.astro";
import Recap from "../../../../../../components/lesson/Recap.astro";
import Inset from "../../../../../../components/lesson/Inset.astro";
import NumberLine from "../../../../../../components/math/NumberLine.astro";
import PracticeSet from "../../../../../../components/math/PracticeSet.astro";
import Quiz from "../../../../../../components/pedagogy/Quiz.astro";

<Hook>
"Three apples and two more" is a calculation: `3 + 2`. "Some apples and two more" is
also a calculation — `n + 2` — you just cannot finish it until you know `n`. That
unfinished-but-meaningful piece of maths is an expression.
</Hook>

<Goal lang="en">
After this lesson you can say what an expression is, see how it differs from an
equation, evaluate an expression by substituting a value, identify its terms, and
simplify it by combining like terms.
</Goal>

<Step n={1}>
**An expression combines numbers, variables, and operations.** `n + 2`, `3 × x`,
`4x + 1` are all **expressions**. An expression has a *value*, but it carries no
equals sign — it does not claim two things are equal. It is a recipe for a number, not
a statement about one.
</Step>

<Step n={2}>
**Evaluate an expression by substituting a value for the variable.** An expression
with a variable does not have a single value until you choose one. To **evaluate**
`4x + 1`, pick a value for `x` — say `x = 3` — put `3` everywhere `x` appears, then
compute: `4 × 3 + 1 = 13`. A different `x` gives a different value.
</Step>

<NumberLine min={0} max={20} highlight={[13]} />

<Step n={3}>
**An expression is built from terms.** A **term** is a piece of the expression
separated from the others by `+` or `−`. In `4x + 1`, the terms are `4x` and `1`. In
`2n + 3n + 5`, the terms are `2n`, `3n`, and `5`. The number stuck to a variable —
the `4` in `4x` — says how many of that variable the term holds.
</Step>

<Step n={4}>
**Combine like terms to simplify.** Terms that hold the *same* variable are **like
terms**, and they can be merged. `2n` and `3n` are like terms: two `n`s plus three
`n`s is five `n`s, so `2n + 3n = 5n`. But `2n` and `5` are not like terms — one counts
`n`s, the other is a plain number — so they cannot be merged.
</Step>

<WorkedExample lang="en">
**Simplify 2x + 3 + 4x, then evaluate the result at x = 5.**

First, **simplify** by combining like terms. The like terms are `2x` and `4x` — both
count `x`s. Two `x`s plus four `x`s is six `x`s: `2x + 4x = 6x`. The `3` is a plain
number with no like term, so it stays. The simplified expression is `6x + 3`.

Now **evaluate** at `x = 5`: substitute 5 for `x`. `6 × 5 + 3 = 30 + 3 = 33`.

So the expression is `6x + 3`, and at `x = 5` its value is `33`.
</WorkedExample>

<Inset kind="why" lang="en">
Why can you only combine like terms? Because `2x` means "two of whatever `x` is" and
`5` means "five ones". Adding them would be like adding two bags of an unknown weight
to five single coins and calling it "7" of something — seven of *what*? Only terms
measuring the same thing can be counted together. Like terms share a unit; unlike
terms do not.
</Inset>

<Inset kind="mistake" lang="en">
A common mistake is reading `2n` as `2 + n`. It is not — `2n` means `2 × n`, two times
the variable. A number written directly against a variable always means multiply. So
at `n = 4`, `2n` is `8`, not `6`. When in doubt, put the multiplication sign back in.
</Inset>

<PracticeSet
  id="expressions-practice"
  lessonSlug="02-expressions"
  lang="en"
  problems={[
    { prompt: "Evaluate the expression n + 7 at n = 5. Type the value.", answer: 12, hint: "Substitute 5 for n, then add." },
    { prompt: "Evaluate the expression 3n at n = 4. Type the value.", answer: 12, hint: "3n means 3 × n." },
    { prompt: "Evaluate the expression 2n + 1 at n = 6. Type the value.", answer: 13, hint: "2 × 6 first, then add 1." },
    { prompt: "Combine the like terms 2n + 3n. Type the number that ends up in front of n.", answer: 5, hint: "Two n's plus three n's." },
    { prompt: "Evaluate the expression 5x − 2 at x = 4. Type the value.", answer: 18, hint: "5 × 4 first, then subtract 2." },
  ]}
/>

<Check lang="en">
  <Quiz
    id="expressions-check"
    pieceSlug="02-expressions"
    lang="en"
    question="Why can 2x + 3x be combined into 5x, but 2x + 3 cannot be combined?"
    choices={[
      { label: "2x and 3x are like terms — both count x's — but 2x and 3 measure different things", correct: true },
      { label: "2x + 3 cannot be combined because 3 is too small", misconception: "Size has nothing to do with it. 3 cannot join 2x because 3 is a plain number, not a count of x's." },
      { label: "Any two terms can always be combined into one", misconception: "Only like terms — terms holding the same variable — can be combined. Unlike terms stay separate." },
    ]}
  />
</Check>

<Recap lang="en">
An expression combines numbers, variables, and operations into a recipe for a value,
and unlike an equation it carries no equals sign. Evaluate an expression by
substituting a number for the variable and computing. An expression is built from
terms, separated by + and −. Like terms — terms holding the same variable — can be
combined to simplify; unlike terms cannot, because they measure different things.
</Recap>
