infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label What limits scale?
    children
      - label I/O-bound — awaits DB or upstream
        children
          - label Loop never blocks — already async
          - label Do nothing: threads add only overhead
      - label CPU work + shared / large in-process data
        children
          - label worker_threads pool ≈ cores
          - label Pool it — never one worker per request
      - label CPU work in a stateless network server
        children
          - label cluster or N replicas — use every core
          - label Push state to a shared store (Redis/DB)
theme
  palette #6366f1 #14b8a6 #f59e0b
