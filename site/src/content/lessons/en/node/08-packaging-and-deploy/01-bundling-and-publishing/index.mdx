---
concepts:
- module-formats
- exports-map
- dual-package-hazard
- tree-shaking
- cli-bin
- publish-hygiene
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-bundling-and-publishing
sources:
- https://nodejs.org/api/packages.html
- https://nodejs.org/api/packages.html#exports
- https://docs.npmjs.com/cli/v10/configuring-npm/package-json
spiral: []
status: ready
summary: 'Publishing is package.json design. The exports map is the modern entry point: it picks ESM vs CJS vs types by condition and encapsulates internals. Dual-publish risks the dual-package hazard; ESM-only is often simpler.'
title: 'Bundling and publishing: ESM, CJS, and exports'
track: node
unit: 08-packaging-and-deploy
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A library team shipped `2.0` to fix a memory leak. Within an hour a major consumer's CI went red: their bundler had been deep-importing `their-lib/dist/internal/cache.js` to monkey-patch a class, and the refactor moved that file. The library authors had never published an `"exports"` map, so every file on disk was a public API by accident — and a "private" rename became a breaking change for thousands of installs. The fix was one field in `package.json`. The lesson: what you ship is not your source tree, it's the surface your `package.json` declares, and if you don't declare it, your whole `dist/` is the contract.
</Hook>

<Crux>What you publish is defined by `package.json`: the `exports` map picks ESM/CJS/types by condition and encapsulates everything else.</Crux>

<Explanation>

## Two module formats, and how Node chooses

Node runs your code as one of two formats, and the choice is made *before* any import resolves. A package is **ESM** if its `package.json` has `"type": "module"` (or the file ends in `.mjs`); otherwise it is **CommonJS** (or `.cjs`). This single field changes everything downstream: ESM uses static `import`/`export` and top-level `await`; CJS uses `require`/`module.exports`. You cannot `require()` an ESM-only package from CJS without `await import()`, and ESM consumers can import a CJS package but only get its default export cleanly.

The hard problem is publishing for *both* kinds of consumer — a **dual package**. The tempting answer is to ship a `dist/esm/` and a `dist/cjs/` build of the same code. It works until it doesn't: a consumer whose dependency graph pulls your CJS copy on one path and your ESM copy on another now has **two distinct module instances loaded at once**. Two copies means two class identities, so `instanceof` fails across the boundary, and any module-level state (a cache, a registry, a singleton) is silently duplicated. This is the **dual-package hazard**, and it is genuinely hard to fully avoid. For most new libraries the pragmatic answer is **ship ESM-only** — Node has supported ESM in production for years — or use a build tool that emits both *and* keep all shared identity/state in a single internal CJS core that both wrappers re-export.

```bash
# ESM-only is the simplest contract: one format, no hazard
# package.json
{
  "type": "module",
  "exports": "./dist/index.js"
}
```

## The exports map: entry points and encapsulation

If two packages share a boundary and one can reach arbitrarily deep into the other's internals, you lose the ability to refactor without breaking your consumers — and the fix is not tests, it's the `exports` map.

The `"exports"` field is the modern entry point and **supersedes `"main"`**. It does two jobs at once. First, **conditional resolution**: you map the same specifier to different files depending on *how* it's loaded — `"import"` for ESM, `"require"` for CJS, `"types"` for TypeScript, and `"default"` as the fallback. Order matters; Node reads conditions top-to-bottom and takes the first match, so `"types"` and the most specific conditions go first, `"default"` last. Second, **subpath exports**: `"./feature"` exposes a deep entry without leaking your folder layout.

The part seniors care about most is **encapsulation**. Once you define `"exports"`, *only the listed subpaths are importable*. A consumer can no longer reach `your-pkg/dist/internal/secret.js` — that import throws `ERR_PACKAGE_PATH_NOT_EXPORTED`. This is the field that would have saved the hook team: with an `exports` map, the internal file was never part of the contract, so moving it is free. Keep `"main"` as a fallback for ancient tooling that ignores `exports`, and **always provide a `"types"` condition** so TypeScript consumers get real types instead of `any`.

```jsonc
{
  "name": "their-lib",
  "type": "module",
  "main": "./dist/index.cjs",        // fallback for old tools
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",  // first: TS reads this
      "import": "./dist/index.js",   // ESM consumers
      "require": "./dist/index.cjs", // CJS consumers
      "default": "./dist/index.js"   // last-resort fallback
    },
    "./feature": "./dist/feature.js" // explicit subpath; nothing else is reachable
  }
}
```

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">package.json field</th>
        <th class="border border-border px-3 py-2 text-left">What it controls</th>
        <th class="border border-border px-3 py-2 text-left">Skip it and…</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>type</code></td>
        <td class="border border-border px-3 py-2">ESM (<code>module</code>) vs CJS for <code>.js</code> files</td>
        <td class="border border-border px-3 py-2">defaults to CJS; ESM syntax errors</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>exports</code></td>
        <td class="border border-border px-3 py-2">entry points + conditions + encapsulation</td>
        <td class="border border-border px-3 py-2">whole <code>dist/</code> is a public API</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>main</code></td>
        <td class="border border-border px-3 py-2">legacy entry; fallback when <code>exports</code> ignored</td>
        <td class="border border-border px-3 py-2">old tools can&#39;t resolve you</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>types</code></td>
        <td class="border border-border px-3 py-2">TS declarations (also a condition)</td>
        <td class="border border-border px-3 py-2">TS consumers get <code>any</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>bin</code></td>
        <td class="border border-border px-3 py-2">maps a CLI command to a file</td>
        <td class="border border-border px-3 py-2">no installed command</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>files</code></td>
        <td class="border border-border px-3 py-2">allowlist of what gets packed</td>
        <td class="border border-border px-3 py-2">you ship tests, src, secrets</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>sideEffects</code></td>
        <td class="border border-border px-3 py-2">tells bundlers modules are pure</td>
        <td class="border border-border px-3 py-2">weaker tree-shaking</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>engines</code></td>
        <td class="border border-border px-3 py-2">declares supported Node range</td>
        <td class="border border-border px-3 py-2">silent breakage on old Node</td>
      </tr>
    </tbody>
  </table>
</div>

Together these fields form a layered contract: `type` and `exports` define the module surface, `files` and `sideEffects` shape what lands in the tarball and how bundlers treat it, and `bin`/`engines` cover CLI and runtime compatibility. Skip any one of them and you ship a contract with an invisible hole — the kind that surfaces in someone else's CI three months later.

<Inset kind="why" lang="en">
Why does adding `"exports"` *break* existing consumers? Because encapsulation is retroactive and total. Before `exports`, every file in your package was reachable; the day you add the field, every deep import that isn't explicitly listed starts throwing `ERR_PACKAGE_PATH_NOT_EXPORTED`. This is intentional — it's the whole point — but it means adding `exports` to a mature package is a **semver-major** change. List every subpath your users legitimately rely on (`"./package.json"` is a common one tools expect), ship it as a major, and document the deep-import paths you're retiring.
</Inset>

## Tree-shaking, the CLI bin, and publish hygiene

ESM's static structure is what lets a bundler **tree-shake** — analyze imports at build time and drop code no one reaches. CJS resists this: `require` is a dynamic function call, so a bundler usually can't prove a branch is dead. You help the bundler with `"sideEffects": false`, a promise that importing your modules does nothing but define exports (no global mutation, no polyfill registration). With that flag, an unused export of yours can be pruned entirely; without it the bundler must keep modules around in case the import itself mattered.

A **CLI** is just a `"bin"` mapping from command name to a file. Three things must line up or the command silently fails to run: the target file needs a **shebang** `#!/usr/bin/env node` as its first line, it needs **execute permission** (`chmod +x`), and on install npm creates the symlink into `node_modules/.bin` for you. Forget the shebang and the shell tries to run JS as a shell script; forget the permission bit and you get "permission denied."

```jsonc
{
  "bin": { "their-cli": "./dist/cli.js" },
  "files": ["dist"],
  "engines": { "node": ">=18.17" },
  "sideEffects": false
}
```

Finally, **ship the build, not the source**. A bundler like tsup/esbuild/rollup emits `dist/` plus `.d.ts` declarations; use the `"files"` allowlist (or `.npmignore`) so the tarball contains only `dist` — not your `src/`, tests, or `.env`. Run `npm pack --dry-run` to *see the exact file list* before you publish. Bump versions by **semver** (a removed export or new `exports` map is major). And publish with `npm publish --provenance` from CI so npm records a signed attestation linking the artifact to the commit and workflow that built it — a supply-chain defense against tampered or typosquatted releases.

<TradeoffMatrix
  id="08-publishing-tradeoff-1"
  lessonSlug="01-bundling-and-publishing"
  lang="en"
  prompt="You're publishing a new general-purpose library in 2026 and must choose a distribution format. What's the senior default?"
  options={[
    { name: "Ship ESM-only with an exports map and a types condition", summary: "One format, no hazard.", correct: true, justification: "For a new library this is the pragmatic default. One build means there is exactly one module instance, so the dual-package hazard cannot occur — instanceof and module-level singletons just work. ESM is fully production-ready in Node and unlocks the strongest tree-shaking for your consumers. You still serve TS via the types condition. The only cost is dropping pure-CJS consumers who can't await import(), which is an increasingly small set." },
    { name: "Dual-publish ESM + CJS as two independent compiled copies", summary: "Maximum reach, real risk.", justification: "Maximizes compatibility but reintroduces the dual-package hazard: if a graph loads both copies, you get two class identities (instanceof fails) and duplicated module state. It's defensible for a foundational, widely-depended-on package, but only if you funnel all shared identity/state through a single internal core both wrappers re-export — extra build and test complexity most libraries don't need." },
    { name: "Ship CJS-only", summary: "Compatible, but weak.", justification: "Broad compatibility with old tooling, but you forfeit reliable tree-shaking (require is dynamic, so bundlers can't prove code is dead) and you push friction onto modern ESM consumers and bundlers. As an ecosystem default for a fresh library it's backward-looking; reserve it for code that must run on very old runtimes." },
    { name: "Publish your TypeScript source and let consumers compile it", summary: "No build, broken DX.", justification: "Shipping uncompiled .ts forces every consumer to transpile your code, leaks an internal toolchain assumption, and breaks plain-JS and many bundler setups outright. You also lose the encapsulation a built dist + exports map provides. Always emit a build and .d.ts; never make consumers your compiler." },
  ]}
/>

<Quiz
  id="08-publishing-quiz-1"
  lessonSlug="01-bundling-and-publishing"
  lang="en"
  question="What does adding an exports map buy you beyond picking ESM vs CJS files?"
  choices={[
    { label: "Encapsulation: only listed subpaths are importable, so deep imports of internal files throw ERR_PACKAGE_PATH_NOT_EXPORTED and you can refactor unlisted files freely", correct: true },
    { label: "It automatically minifies and tree-shakes your package at publish time", misconception: "exports is pure resolution metadata — it maps specifiers to files and conditions. It performs no minification or tree-shaking; that's the bundler's job, helped by ESM and sideEffects: false." },
    { label: "It lets consumers require() an ESM package from CommonJS without await import()", misconception: "The exports map can't change the format barrier. A CJS caller still can't synchronously require an ESM module; conditions only select which file is resolved, not how the runtime loads it." },
    { label: "It guarantees consumers always get the latest version", misconception: "Versioning is governed by the version field and the consumer's semver range / lockfile. exports has nothing to do with which version is installed." },
  ]}
/>

<Quiz
  id="08-publishing-quiz-2"
  lessonSlug="01-bundling-and-publishing"
  lang="en"
  question="A consumer reports that instanceof MyError returns false even for an error your dual-published library clearly threw. What's the most likely cause?"
  choices={[
    { label: "The dual-package hazard: the graph loaded both your ESM and CJS copies, so two distinct MyError classes exist and instanceof compares against the wrong one", correct: true },
    { label: "instanceof is unreliable across any package boundary in Node", misconception: "instanceof works fine across package boundaries when there's a single class identity. It fails here specifically because two copies of the module were loaded, creating two MyError constructors." },
    { label: "They forgot to set this.name on the error class", misconception: "this.name affects how the error prints and err.name, not instanceof. instanceof checks the prototype chain against a constructor, and the failure is two different constructors, not a missing name." },
    { label: "The error needs to be JSON-serialized before crossing modules", misconception: "Serialization is unrelated and would actually destroy the prototype, guaranteeing instanceof false. The root cause is duplicate module instances from dual-publishing." },
  ]}
/>

<DragOrder
  id="08-publishing-drag-1"
  lessonSlug="01-bundling-and-publishing"
  lang="en"
  prompt="Order the steps to publish a library + CLI safely, from build to attested release:"
  items={[
    { id: "a", label: "Build with tsup/esbuild → emit dist/ JS plus .d.ts declarations" },
    { id: "b", label: "Add the types condition so TS consumers resolve real declarations, not any" },
    { id: "c", label: "Write the exports map (entry + subpaths) so only intended paths are importable" },
    { id: "d", label: "Set files: [\"dist\"] (and bin + shebang + chmod +x for the CLI) so the tarball is minimal and the command runs" },
    { id: "e", label: "Publish from CI with npm publish --provenance to attest the artifact" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="From source tree to a consumer resolving the right file by condition"
  hue="var(--d-systems)"
  perRow={4}
  nodes={[
    { id: "s", label: "source", sub: "src/*.ts" },
    { id: "b", label: "build", sub: "tsup/esbuild" },
    { id: "e", label: "exports map", sub: "conditions" },
    { id: "c", label: "consumer", sub: "resolves" },
  ]}
  edges={[
    { from: "s", to: "b", label: "compile" },
    { from: "b", to: "e", label: "dist + .d.ts" },
    { from: "e", to: "c", label: "import/require/types" },
  ]}
  caption="Source compiles to a dist tree; the exports map turns that tree into a contract. A consumer's loader picks a file by condition — import → ESM, require → CJS, the TS compiler → types — and can reach nothing the map doesn't list."
/>

<KeyTakeaway>What you publish is your `package.json` surface, not your folder tree. The `exports` map picks ESM/CJS/types by condition and encapsulates the rest. Prefer ESM-only to dodge the dual-package hazard; always ship `types`.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="08-publishing-retrieval"
  lang="en"
  questions={[
    { q: "Why is adding an exports map to a mature package a breaking (semver-major) change, and how do you ship it safely?", a: "Because exports encapsulation is total and retroactive. Before the field exists, every file in the package is reachable, so consumers may legitimately deep-import paths like your-pkg/dist/internal/util.js. The moment you add exports, only the listed subpaths resolve — every unlisted deep import immediately throws ERR_PACKAGE_PATH_NOT_EXPORTED. That's the intended benefit (you can now refactor unlisted files freely), but it breaks anyone relying on the old reachability, so it must go out as a major version. To ship safely: enumerate every subpath your users genuinely depend on and list it explicitly (including ./package.json, which many tools read), keep main as a fallback for tooling that ignores exports, provide a types condition, document which deep paths you're retiring, and release it as a clearly-flagged major so consumers update deliberately." },
    { q: "What exactly is the dual-package hazard, and what's the simplest way to avoid it?", a: "It's the failure mode of shipping the same package as two independent builds — one ESM, one CJS. If a single dependency graph ends up loading both copies (one path requires the CJS build, another imports the ESM build), the runtime now holds two distinct module instances. Two instances means two separate class identities, so instanceof checks across the boundary return false, and any module-level state — a cache, a registry, a singleton connection — is silently duplicated, leading to subtle bugs where 'the same' object isn't. The simplest avoidance for a new library is to ship ESM-only: one format means exactly one instance, so the hazard can't arise, and you still serve TypeScript via the types condition. If you must dual-publish for reach, route all shared identity and state through a single internal core module that both the ESM and CJS wrappers re-export, so there is one source of truth despite two entry shims." },
  ]}
/>

<Recap lang="en">
Publishing a package is really designing its `package.json`: that file, not your source tree, defines the public surface. Node picks a module format from `"type"` — `"module"` for ESM, otherwise CommonJS — and the two don't mix freely, so serving both consumers (a *dual package*) risks the **dual-package hazard**, where two loaded copies produce two class identities (`instanceof` breaks) and duplicated module state; for new libraries shipping **ESM-only** sidesteps it entirely. The `"exports"` map is the modern entry point that supersedes `"main"`: it resolves by condition (`"types"` first, then `"import"`/`"require"`, `"default"` last) and, crucially, **encapsulates** — only listed subpaths are importable, so unlisted internals can be refactored freely (which is why adding `exports` to an existing package is a semver-major break). ESM plus `"sideEffects": false` unlocks real tree-shaking; a CLI needs `"bin"` plus a `#!/usr/bin/env node` shebang and execute permission; and ship hygiene means emitting a build with `.d.ts`, a `"files"` allowlist, an `"engines"` range, semver bumps, and `npm publish --provenance` from CI for a signed supply-chain attestation. Now when you see a consumer bug traced to a deep import — or an `instanceof` failure across a package boundary — you'll know exactly which fields were missing from the `package.json` and why.
</Recap>
