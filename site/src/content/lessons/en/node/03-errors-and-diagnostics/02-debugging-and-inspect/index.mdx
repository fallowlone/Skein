---
concepts:
- inspector-protocol
- inspect-brk
- source-maps
- node-debug
- util-debuglog
- inspector-security
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-debugging-and-inspect
sources:
- https://nodejs.org/en/learn/getting-started/debugging
- https://nodejs.org/api/inspector.html
- https://nodejs.org/api/util.html#utildebuglogsection-callback
spiral: []
status: ready
summary: '`node --inspect` opens a V8 Inspector WebSocket on 127.0.0.1:9229 that DevTools and VS Code attach to; `--inspect-brk` breaks before line one. Keep it on localhost — that port is remote code execution. For cheap logs use util.debuglog gated by NODE_DEBUG.'
title: 'Debugging Node with --inspect and DevTools'
track: node
unit: 03-errors-and-diagnostics
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A service crashed on boot — a config value was `undefined` before the first request ever landed. The on-call engineer added `node --inspect server.js`, opened DevTools, set a breakpoint on the failing line, and restarted. The breakpoint never hit: by the time DevTools finished attaching, the process had already thrown and exited. They added more `console.log` calls, redeployed, waited for the next crash, repeated — an hour gone. The fix was one flag they didn't know: `--inspect-brk`, which freezes the process before the first line of user code and waits for the debugger, so a startup bug can't outrun the attach.
</Hook>

<Crux>`node --inspect` opens a V8 Inspector on 127.0.0.1:9229; `--inspect-brk` breaks before line one. That port is RCE — keep it local.</Crux>

<Explanation>

## --inspect starts a WebSocket the debugger attaches to

`node --inspect app.js` doesn't run a debugger — it starts one *server*. Node enables the V8 Inspector agent, which opens a WebSocket and prints a line like `Debugger listening on ws://127.0.0.1:9229/<uuid>`. By default it binds to `127.0.0.1:9229` — localhost only, one well-known port. Anything that speaks the Chrome DevTools Protocol then *attaches* to that socket: Chrome via `chrome://inspect`, the standalone DevTools window, or VS Code's JavaScript debugger. The debugger is the client; your process is the server. That framing explains the next two facts that bite people.

First, attaching is asynchronous and takes real wall-clock time. `--inspect` lets your code run immediately, so a fast-failing startup bug is over before the client connects — exactly the hook. The fix is `--inspect-brk`, which enables the agent *and breaks before the first line of user code*, parking the process until a debugger attaches and tells it to continue. Use `--inspect` for a long-running server you'll attach to mid-flight; use `--inspect-brk` whenever the bug is in startup, module top-level, or anything that runs once. (`--inspect-wait` is the middle ground: wait for an attach but don't break.)

Second, the bind address is a deliberate choice. `--inspect=0.0.0.0:9229` or `--inspect=<public-ip>` exposes the agent on the network — keep reading, because that single change is the difference between a debugger and a remote shell.

```bash
# attach mid-flight to a running server (localhost:9229)
node --inspect server.js

# freeze before line 1 and wait for the debugger — for startup bugs
node --inspect-brk server.js

# custom bind (still localhost here; see the security section before going wider)
node --inspect=127.0.0.1:9230 server.js
```

## Breakpoints, the debugger statement, and source maps

Once attached, DevTools gives you the full toolkit: line breakpoints, conditional breakpoints (`hit only when userId === 42`), **logpoints** (print an expression without halting — a breakpoint that logs instead of pausing), step over/into/out, a live watch list, and a Console REPL evaluated *in the paused frame's scope*, so you can inspect and even mutate locals at the breakpoint. A bare `debugger;` statement in your source is a programmatic breakpoint: it pauses if a debugger is attached and is a no-op otherwise.

The trap for TypeScript and any compiled/bundled code: the debugger sees the *emitted* JavaScript, so breakpoints and stack frames land in `dist/app.js`, not your `src/app.ts`. Run with **`--enable-source-maps`** so Node consumes the `.js.map` files and remaps stack traces — and the debugger's line positions — back to the original source. Without it, a production stack trace points at minified line 1, column 9000, and your breakpoint sits in code you never wrote.

```bash
# emitted JS + source maps → stacks and breakpoints map back to .ts
node --enable-source-maps --inspect-brk dist/app.js
```

<Inset kind="why" lang="en">
Why does the debugger run in a separate client at all, instead of being baked into the runtime? Because the V8 Inspector exposes the same Chrome DevTools Protocol the browser uses — so the *exact* DevTools you already know (Sources, Console, Memory, Profiler) work unchanged against a server process, and tools like VS Code, WebStorm, and `chrome://inspect` all speak one protocol to one WebSocket. The cost of that power is the security surface in the next section: a protocol that can evaluate arbitrary expressions in your process is, by definition, a code-execution channel.
</Inset>

## util.debuglog: zero-cost logging without a debugger

Sometimes you don't want to halt — you want a trace you can switch on in one environment and pay nothing for everywhere else. That's `util.debuglog(section)`. It returns a logging function that is a **no-op unless the `NODE_DEBUG` environment variable names that section** — when disabled it does no string formatting and writes nothing, so it's effectively free to leave in hot paths. Enable it per-run with `NODE_DEBUG=mysection`, comma-separate several (`NODE_DEBUG=db,http`), or use wildcards (`NODE_DEBUG=db*`). Output goes to stderr tagged with the section and PID: `DB 3245: query took 14ms`. Node's own core uses this exact mechanism — `NODE_DEBUG=http,net,tls` lights up the runtime's internal traces.

```js
import { debuglog } from "node:util";
const log = debuglog("db"); // no-op unless NODE_DEBUG=db

async function query(sql) {
  const t = performance.now();
  const rows = await pool.query(sql);
  log("query took %dms (%d rows)", performance.now() - t, rows.length);
  return rows;
}
// run: NODE_DEBUG=db node app.js  → "DB 3245: query took 14ms (3 rows)"
// run: node app.js                → silent, zero overhead
```

The skill is matching the tool to the moment. The table below is the senior's decision grid.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Approach</th>
        <th class="border border-border px-3 py-2 text-left">What it gives</th>
        <th class="border border-border px-3 py-2 text-left">Cost</th>
        <th class="border border-border px-3 py-2 text-left">When</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>--inspect</code></td>
        <td class="border border-border px-3 py-2">Attach live; breakpoints, watch, REPL in scope</td>
        <td class="border border-border px-3 py-2">Manual attach; async — can miss fast failures</td>
        <td class="border border-border px-3 py-2">Long-running server, bug mid-flight</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>--inspect-brk</code></td>
        <td class="border border-border px-3 py-2">Same, but pauses before line 1</td>
        <td class="border border-border px-3 py-2">Blocks until a debugger attaches</td>
        <td class="border border-border px-3 py-2">Startup / module-top / one-shot bugs</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>NODE_DEBUG</code> + <code>util.debuglog</code></td>
        <td class="border border-border px-3 py-2">Section-gated stderr trace, no halt</td>
        <td class="border border-border px-3 py-2">No-op when off; near-zero cost</td>
        <td class="border border-border px-3 py-2">Conditional tracing, safe to ship</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>console.log</code></td>
        <td class="border border-border px-3 py-2">Always-on print, no setup</td>
        <td class="border border-border px-3 py-2">Always formats + writes; pollutes logs</td>
        <td class="border border-border px-3 py-2">Throwaway local probe only</td>
      </tr>
    </tbody>
  </table>
</div>

## The failure mode: the inspector port is a remote shell

Here is the rule that turns a debugging tool into an incident: **the inspector port is a remote-code-execution surface.** The Chrome DevTools Protocol includes `Runtime.evaluate`, which runs arbitrary expressions inside your process. The Node docs are blunt: if the debugger is bound to a public IP or `0.0.0.0`, *any* client that can reach that address can connect with no authentication and run arbitrary code on behalf of your process. There is no password, no token — reachability *is* authorization.

So two non-negotiables. Never bind the inspector to anything but localhost, and never leave `--inspect` on in production. The default `127.0.0.1` is safe precisely because only the local box can reach it; `--inspect=0.0.0.0` throws that away. To debug a remote box, don't widen the bind — tunnel localhost-to-localhost over SSH (`ssh -L 9221:localhost:9229 user@host`), so the port never touches the public network. A `--inspect` flag accidentally baked into a production container image, or a `0.0.0.0` bind on a host with a public interface, is a full RCE backdoor that no firewall rule inside the app can close.

<Quiz
  id="02-debugging-and-inspect-quiz-1"
  lessonSlug="02-debugging-and-inspect"
  lang="en"
  question="Your service throws and exits during startup, before the first request. Why does plain `node --inspect` fail to catch it, and what catches it?"
  choices={[
    { label: "--inspect lets code run immediately, and attaching a debugger takes wall-clock time, so a fast-failing startup bug is over before the client connects; --inspect-brk pauses before the first line of user code and waits for the debugger, so the bug can't outrun the attach", correct: true },
    { label: "--inspect only works for asynchronous errors, so you need a try/catch instead", misconception: "--inspect is about attaching a debugger, not about sync vs async errors. The real problem is timing: the process exits before the debugger has finished connecting. --inspect-brk fixes it by halting before line 1." },
    { label: "The inspector ignores startup code by design; restart the process after boot", misconception: "The inspector doesn't ignore any code. With --inspect the agent is up, but your code also runs immediately and can crash before you attach. --inspect-brk freezes execution until a debugger attaches." },
    { label: "You must set NODE_DEBUG=startup to capture boot-time errors", misconception: "NODE_DEBUG gates util.debuglog tracing; it has nothing to do with breakpoints or attach timing. The startup-bug fix is --inspect-brk, which breaks before the first line of user code." },
  ]}
/>

<Quiz
  id="02-debugging-and-inspect-quiz-2"
  lessonSlug="02-debugging-and-inspect"
  lang="en"
  question="By default, what does `node --inspect app.js` bind to, and why does that default matter?"
  choices={[
    { label: "127.0.0.1:9229 — localhost only, so only the local machine can reach the inspector; the port is an RCE surface (Runtime.evaluate runs arbitrary code), so binding it wider with 0.0.0.0 or a public IP exposes a no-auth remote shell", correct: true },
    { label: "0.0.0.0:9229 — all interfaces, so any teammate can attach over the network out of the box", misconception: "The default is 127.0.0.1, NOT 0.0.0.0. That's the whole point: only the local box can reach it. You'd have to explicitly pass 0.0.0.0 to expose it — which is exactly the dangerous misconfiguration." },
    { label: "A random high port chosen each run, so two debug sessions never collide", misconception: "The default port is the fixed, well-known 9229 (host 127.0.0.1). You change it explicitly with --inspect=host:port; it isn't randomized." },
    { label: "127.0.0.1:9229, but it's password-protected, so exposing it publicly is fine", misconception: "There is no password or token. Reachability is authorization: any client that can connect to the port can run arbitrary code. That's why the localhost default matters and why you must never bind it wider." },
  ]}
/>

<TradeoffMatrix
  id="02-debugging-and-inspect-tradeoff-1"
  lessonSlug="02-debugging-and-inspect"
  lang="en"
  prompt="A function in a long-running production-like staging server intermittently returns wrong totals, but only for certain users. You want to inspect locals at the moment it goes wrong, without halting every request. Which approach fits best?"
  options={[
    { name: "Attach with --inspect and set a conditional breakpoint (e.g. userId === 42) or a logpoint on the suspect line", summary: "Live, targeted, no full halt.", correct: true, justification: "This is the right tool. The server is long-running so --inspect can attach mid-flight, and a conditional breakpoint pauses only on the user that reproduces the bug — so you inspect (and can mutate) the exact locals in the failing frame without stopping every other request. A logpoint is the even-lighter variant when you only need values, not a pause. This is precisely what the inspector adds over print debugging." },
    { name: "Restart with --inspect-brk and step through from line 1", summary: "Right tool, wrong moment.", justification: "--inspect-brk is for startup/one-shot bugs that run before you can attach. Here the server already runs and the bug is request-scoped, so breaking before line 1 just stalls boot and forces you to step through unrelated initialization before you ever reach the buggy request. Use a conditional breakpoint on the live process instead." },
    { name: "Add console.log of the totals on every request and grep the logs", summary: "Always-on noise.", justification: "It works but it's the blunt instrument: it formats and writes on every request (not just the failing user), pollutes logs, can't show you the full local scope or let you step, and requires a redeploy to change what you print. For a known suspect line where you want locals at the moment of failure, a conditional breakpoint is faster and cleaner." },
    { name: "Set --inspect=0.0.0.0:9229 so the whole team can attach to staging", summary: "Opens an RCE backdoor.", justification: "Never. Binding to 0.0.0.0 exposes the inspector — an unauthenticated remote-code-execution channel via Runtime.evaluate — to anyone who can reach the host. It also doesn't even address the question (live conditional inspection). If others must attach, each tunnels over SSH to localhost:9229; the bind stays on 127.0.0.1." },
  ]}
/>

<DragOrder
  id="02-debugging-and-inspect-drag-1"
  lessonSlug="02-debugging-and-inspect"
  lang="en"
  prompt="Order the steps to attach DevTools to a running script and pause it on a startup bug, safely:"
  items={[
    { id: "a", label: "Start the process with the inspector agent breaking before line 1: node --inspect-brk app.js" },
    { id: "b", label: "Node opens the V8 Inspector WebSocket on 127.0.0.1:9229 and prints the ws:// URL, parked before user code" },
    { id: "c", label: "Open chrome://inspect (or VS Code / DevTools) and attach to the listed 127.0.0.1:9229 target" },
    { id: "d", label: "Set breakpoints (line / conditional / logpoint) in the source; with --enable-source-maps they land in your .ts" },
    { id: "e", label: "Resume execution; the process runs to your breakpoint, where you inspect locals in the paused frame" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="A Node process exposes the V8 Inspector as a WebSocket that DevTools clients attach to"
  hue="var(--d-systems)"
  perRow={4}
  nodes={[
    { id: "n", label: "node --inspect", sub: "agent on" },
    { id: "i", label: "V8 Inspector", sub: "ws :9229" },
    { id: "l", label: "loopback only", sub: "127.0.0.1" },
    { id: "d", label: "DevTools / VS Code", sub: "CDP client" },
  ]}
  edges={[
    { from: "n", to: "i", label: "opens" },
    { from: "i", to: "l", label: "binds" },
    { from: "l", to: "d", label: "attach" },
  ]}
  caption="--inspect turns on the agent, which opens a Chrome DevTools Protocol WebSocket on 127.0.0.1:9229. Clients attach over loopback; to reach a remote box, tunnel localhost-to-localhost over SSH rather than binding to 0.0.0.0, because that port can evaluate arbitrary code."
/>

<KeyTakeaway>`node --inspect` opens a V8 Inspector on `127.0.0.1:9229`; `--inspect-brk` breaks before line one; `--enable-source-maps` maps stacks to your `.ts`. The port is unauthenticated RCE — keep it on localhost, never ship it.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-debugging-and-inspect-retrieval"
  lang="en"
  questions={[
    { q: "Why is leaving `--inspect` enabled in production (or binding it to 0.0.0.0) a remote-code-execution risk, and how do you debug a remote box safely instead?", a: "The inspector speaks the Chrome DevTools Protocol, which includes Runtime.evaluate — a command that runs arbitrary expressions inside your live process, with full access to its execution environment. There is no authentication layer: per the Node docs, if the agent is bound to a public IP or 0.0.0.0, any client that can reach that address can connect and execute arbitrary code on behalf of your process. Reachability is authorization. The default 127.0.0.1 is safe only because just the local machine can reach it. So the two rules are: never bind wider than localhost, and never leave --inspect on in a production image. To debug a remote host, keep the bind on 127.0.0.1:9229 and forward it over an SSH tunnel — ssh -L 9221:localhost:9229 user@host — then point your debugger at localhost:9221. The inspector port never touches the public network, and SSH provides the auth the protocol lacks." },
    { q: "You're debugging a TypeScript service and your breakpoints land in dist/app.js, not your src .ts, and stack traces point at compiled line numbers. What's happening and what's the fix?", a: "The V8 Inspector debugs the JavaScript that actually runs, which is the emitted/compiled output, not your TypeScript source. Without source maps, Node has no way to relate a position in dist/app.js back to a line in src/app.ts, so breakpoints, the Sources panel, and stack traces all reference the compiled file (often a single minified line for bundled code). The fix is to emit source maps from your compiler (e.g. tsc with sourceMap, or your bundler) and run Node with --enable-source-maps, which makes Node consume the .js.map files and remap both stack traces and the debugger's line positions back to the original source. Then a breakpoint set in app.ts pauses at the right place and a production stack trace reads in terms of your real files instead of column 9000 of a minified bundle." },
  ]}
/>

<Recap lang="en">
`node --inspect` does not run a debugger — it starts the V8 Inspector agent, a WebSocket server bound by default to `127.0.0.1:9229`, and DevTools, VS Code, or `chrome://inspect` attach to it over the Chrome DevTools Protocol. Because attaching takes wall-clock time, plain `--inspect` can miss a fast-failing startup bug; `--inspect-brk` breaks before the first line of user code and waits for the debugger, so boot-time and one-shot bugs can't outrun the attach. Once paused you get line, conditional, and logpoint breakpoints, stepping, a watch list, and a Console evaluated in the paused frame's scope; `debugger;` is the same breakpoint in source. For compiled or TypeScript code, run with `--enable-source-maps` so stacks and breakpoints map back to the original `.ts` instead of `dist`. When you don't want to halt, `util.debuglog(section)` gives a near-zero-cost trace that is a no-op unless `NODE_DEBUG` names the section — the same mechanism core uses for `NODE_DEBUG=http,net`. And the one rule that turns a tool into an incident: the inspector port runs `Runtime.evaluate`, i.e. arbitrary code, with no authentication, so keep it on localhost, tunnel over SSH for remote work, and never bind to `0.0.0.0` or ship `--inspect` to production.
</Recap>
