---
concepts:
- error-objects
- throw-vs-reject
- error-first-callbacks
- error-cause
- custom-errors
- unhandled-rejection
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-error-handling
sources:
- https://nodejs.org/api/errors.html
- https://nodejs.org/api/process.html#event-uncaughtexception
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Error/cause
spiral: []
status: ready
summary: 'An error is an object with a stack and a code, not a string. Match throw,
  reject, or an error-first callback to the call style, chain the original with cause,
  and never swallow a rejection — an unhandled one crashes the process by default.'
title: 'Error handling: throw, reject, and never swallow'
track: node
unit: 03-errors-and-diagnostics
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A payment service logged `Error: [object Object]` for three weeks. The handler did `catch (e) { logger.error("payment failed: " + e) }`, and `e` was a rejected fetch wrapped by a library, so string concatenation flattened it to nothing useful — no stack, no status code, no upstream message. The bug that caused the failures was a one-line timeout misconfiguration, but it stayed invisible because the error had been thrown away at the first `catch`. The team wasn't missing a monitoring tool. They were destroying the one object that already carried the answer.
</Hook>

<Crux>An error carries a stack and a code — match throw/reject/callback to the call style, chain with `cause`, never swallow a rejection.</Crux>

<Explanation>

## An error is an object, not a string

When something fails in Node, the useful artifact is an `Error` object, and almost every bug in error handling comes from treating it like a string. An `Error` has a `message` (the human line), a `name` (`"Error"`, `"TypeError"`, …), and a `stack` — a captured trace of where it was created, which is the single most valuable field for debugging. Node adds one more that web code lacks: system and library errors carry a stable `code` string like `ENOENT`, `ECONNREFUSED`, or `ERR_INVALID_ARG_TYPE`. **You branch on `code`, never on `message`** — messages are prose and change between versions; codes are API.

```js
import { readFile } from "node:fs/promises";

try {
  await readFile("/etc/missing.conf");
} catch (err) {
  console.log(err instanceof Error); // true
  console.log(err.code);             // "ENOENT"  ← branch on this
  console.log(err.message);          // "ENOENT: no such file..."  ← log, don't parse
}
```

The moment you do `"failed: " + err` or `JSON.stringify(err)`, you lose it: concatenation calls `toString()` and yields `"Error: message"` with no stack, and `JSON.stringify` returns `{}` because `message` and `stack` are non-enumerable. Log the object itself (`logger.error({ err })`, or `console.error(err)`) so the stack survives. This is exactly the bug in the hook: the `Error` was real and complete, and the `+` operator threw away everything except a useless prefix.

## throw vs reject vs error-first callbacks

Node has three ways failure travels, and they are not interchangeable — each is bound to a *call style*, and mixing them is a top source of swallowed errors.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Call style</th>
        <th class="border border-border px-3 py-2 text-left">How failure surfaces</th>
        <th class="border border-border px-3 py-2 text-left">How you catch it</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Synchronous</td>
        <td class="border border-border px-3 py-2"><code>throw err</code></td>
        <td class="border border-border px-3 py-2"><code>try / catch</code> (same tick)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Promise / <code>async</code></td>
        <td class="border border-border px-3 py-2">returns a <em>rejected</em> promise</td>
        <td class="border border-border px-3 py-2"><code>await</code> in <code>try/catch</code>, or <code>.catch()</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Callback (older core APIs)</td>
        <td class="border border-border px-3 py-2"><code>cb(err, data)</code> — err is arg 1</td>
        <td class="border border-border px-3 py-2">check <code>if (err)</code> first, then return</td>
      </tr>
    </tbody>
  </table>
</div>

The trap is that `try/catch` only catches a *synchronous* `throw` in the same tick. A `throw` inside an asynchronous callback escapes the surrounding `try` entirely — by the time it runs, the `try` block has already returned. Likewise, calling an `async` function without `await` (or `.catch`) means its rejection is never observed by your `try`. The rule: inside `async` code, `await` everything you depend on so a rejection becomes a catchable throw; in error-first callbacks, handle `err` on the *first* line and `return` so you don't fall through into the success path with `data` undefined.

```js
// throw escapes — the callback runs after the try has exited
try {
  setTimeout(() => { throw new Error("boom"); }, 10); // becomes uncaughtException
} catch (e) { /* never runs */ }

// promise rejection is awaited into a catchable throw
try {
  await fetchUser(id); // rejects → caught here
} catch (e) { /* runs */ }

// error-first: check err first, then return
db.query(sql, (err, rows) => {
  if (err) return cb(err);   // stop here on failure
  cb(null, transform(rows));
});
```

<Inset kind="why" lang="en">
An `EventEmitter` is the fourth case, and the sharpest. If an emitter fires an `'error'` event and there is *no* `'error'` listener attached, Node does not silently drop it — it throws the error, which becomes an `uncaughtException` and crashes the process. This is deliberate: a stream or socket failing with nobody listening is a bug. Always attach an `'error'` handler to streams, sockets, and any emitter that can fail (`stream.on("error", …)`), even if it only logs.
</Inset>

## cause and custom error classes

Catching an error to add context is good; *replacing* it with a vaguer one is how you lose the stack. Since Node 16.9 the standard fix is the **`cause` option**: throw a new, higher-level error while chaining the original underneath, so the trace stays intact.

```js
class ConfigError extends Error {
  constructor(message, options) {
    super(message, options);   // forwards { cause }
    this.name = "ConfigError"; // so logs and instanceof read correctly
  }
}

try {
  await readFile(path);
} catch (err) {
  // wrap with context, keep the original ENOENT and its stack as .cause
  throw new ConfigError(`cannot load config at ${path}`, { cause: err });
}
```

Two senior habits live here. First, **set `this.name`** in a custom class — without it the class name is lost in serialized logs and `err.name` reads `"Error"`. Second, prefer a small set of typed errors (`ValidationError`, `NotFoundError`, `ConfigError`) that callers can branch on with `instanceof` or a `code` property, rather than parsing messages. A modern logger or `util.inspect` prints the full cause chain automatically, so wrapping costs nothing and buys you the full path from symptom to root.

<TradeoffMatrix
  id="03-error-handling-tradeoff-1"
  lessonSlug="01-error-handling"
  lang="en"
  prompt="A repository function catches a low-level ECONNREFUSED from the DB driver and wants the API layer to show a clean error without losing the root cause. What does it throw?"
  options={[
    { name: "A new typed error with the original attached: throw new RepositoryError('user lookup failed', { cause: err })", summary: "Wrap, keep cause.", correct: true, justification: "This is the senior default. The caller gets a clean, branchable RepositoryError (instanceof / code) for control flow, while the original ECONNREFUSED and its stack survive as .cause — so logs and util.inspect print the full chain from API symptom down to the driver root cause. You add context without destroying evidence, which is the whole point of catching to re-throw." },
    { name: "Re-throw the original error unchanged: throw err", summary: "No context.", justification: "Safe for the stack but leaks a low-level driver concern (ECONNREFUSED) up to the API layer with no domain context, so callers must know driver internals to handle it. Fine as a last resort, but wrapping with cause gives the same stack plus a meaningful type — strictly better here." },
    { name: "Throw a fresh error with just a message: throw new Error('user lookup failed')", summary: "Loses the root.", justification: "This is the classic stack-destroying move. The new error starts a brand-new stack at the catch site and discards the original ECONNREFUSED entirely — you keep a friendly message but throw away the one piece of evidence (the driver failure and its trace) that tells you why it broke. Use { cause: err } instead." },
    { name: "Log the error and return null", summary: "Swallows it.", justification: "Swallowing. The caller can't tell 'no such user' from 'database is down', null propagates into code that assumes a user, and the failure becomes a confusing downstream crash with no link to the real cause. Catch to add context and re-throw, or let it propagate — never convert a failure into a silent null." },
  ]}
/>

## The process-level safety net: uncaughtException and unhandledRejection

Some errors escape every local handler — a `throw` in a stray callback, a promise nobody awaited. Node surfaces these on the `process` object, and how you treat them separates a robust service from a zombie one.

An **unhandled promise rejection** — a rejected promise with no `.catch` or `await` anywhere — fires `process.on("unhandledRejection")`, and since Node 15 the **default action is to print the error and terminate the process** (exit code 1). This is the right default: a rejection you forgot to handle is a bug, and continuing in an unknown state hides it. Do not "fix" it by registering a handler that just logs and swallows — that re-creates the silent-failure problem at global scope.

An **uncaught synchronous exception** fires `process.on("uncaughtException")`. The critical, counter-intuitive rule from the Node docs: **after an uncaughtException the process is in an undefined state and you must not resume normal operation.** A handler is for last-rites only — flush logs, fire a metric, maybe close connections — and then `process.exit(1)`. Let a supervisor (systemd, Kubernetes, PM2) restart a clean process. The pattern below is the production shape: a thin top-level net that records and exits, paired with disciplined local handling everywhere else.

```js
process.on("unhandledRejection", (reason) => {
  console.error("unhandledRejection:", reason); // an Error or any value
  process.exit(1);
});
process.on("uncaughtException", (err) => {
  console.error("uncaughtException:", err);
  // state is unsafe — record, then die. Do NOT keep serving.
  process.exit(1);
});
```

<Quiz
  id="03-error-handling-quiz-1"
  lessonSlug="01-error-handling"
  lang="en"
  question="Why does try { setTimeout(() => { throw new Error('x') }) } catch (e) {} NOT catch the error?"
  choices={[
    { label: "The callback runs on a later tick, after the try block has already returned, so try/catch — which only catches synchronous throws in the same tick — is long gone; the throw becomes an uncaughtException", correct: true },
    { label: "setTimeout disables error handling", misconception: "setTimeout does nothing to error handling. The issue is timing: the callback executes asynchronously, after the synchronous try/catch has finished, so there is no active catch to receive the throw." },
    { label: "You need to await the setTimeout", misconception: "Plain setTimeout returns a timer, not a promise, so there's nothing to await. To catch an error from a deferred callback you must put the try/catch inside the callback, or promisify it." },
    { label: "The error is caught but e is undefined", misconception: "The catch block never runs at all — the throw escapes the try entirely and reaches the process as an uncaughtException. It isn't a matter of e being empty." },
  ]}
/>

<Quiz
  id="03-error-handling-quiz-2"
  lessonSlug="01-error-handling"
  lang="en"
  question="An unhandled promise rejection occurs in a modern Node service with no unhandledRejection handler registered. What happens by default?"
  choices={[
    { label: "Node prints the rejection reason and terminates the process with exit code 1 (the default since Node 15) — let a supervisor restart a clean process", correct: true },
    { label: "Nothing — the rejection is silently ignored", misconception: "That was old behaviour (a deprecation warning). Since Node 15 the default mode is 'throw': an unhandled rejection crashes the process, on purpose, so the bug can't hide." },
    { label: "The event loop pauses until the rejection is handled", misconception: "Promises don't pause the loop. An unhandled rejection is detected after a microtask drain and, by default, terminates the process — it does not block waiting for a handler." },
    { label: "It is automatically converted to a resolved promise", misconception: "Node never resolves a rejection for you. The rejection stands; with no handler the default action is to crash the process." },
  ]}
/>

<DragOrder
  id="03-error-handling-drag-1"
  lessonSlug="01-error-handling"
  lang="en"
  prompt="Order the lifecycle of a well-handled failure, from origin to clean recovery:"
  items={[
    { id: "a", label: "A low-level op fails and creates an Error with a stack and a code (e.g. ENOENT)" },
    { id: "b", label: "The nearest layer that has context catches it" },
    { id: "c", label: "It re-throws a typed error with { cause: original } to add meaning without losing the stack" },
    { id: "d", label: "A boundary (request handler / job runner) catches the typed error and maps it to a response or retry" },
    { id: "e", label: "Anything that still escapes hits the process net, which logs and exits for a supervisor to restart" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="An error propagating from where it is thrown up to a handler or the process"
  hue="var(--d-systems)"
  perRow={4}
  nodes={[
    { id: "t", label: "throw / reject", sub: "origin" },
    { id: "p", label: "propagates up", sub: "stack unwinds" },
    { id: "c", label: "catch + cause", sub: "add context" },
    { id: "h", label: "handle or crash", sub: "boundary / exit" },
  ]}
  edges={[
    { from: "t", to: "p" },
    { from: "p", to: "c", label: "unwinds" },
    { from: "c", to: "h", label: "re-throw / map" },
  ]}
  caption="A synchronous throw unwinds the call stack to the nearest try/catch; a rejection travels the promise chain to the nearest await/.catch. Wrap with cause on the way up, handle at a boundary, and let only true surprises reach the process-level net."
/>

<KeyTakeaway>An `Error` is an object with a `stack` and a `code`: log it, branch on `code`, never concat it. Match `throw`/`reject`/`callback` to the call style, wrap with `{ cause }`, and let unhandled rejections crash for a clean restart.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-error-handling-retrieval"
  lang="en"
  questions={[
    { q: "Why is concatenating an error into a string (or JSON.stringify-ing it) a bug, and what should you do instead?", a: "Both destroy the very fields that make an Error useful. String concatenation invokes err.toString(), which returns only 'Error: message' — the stack, the code, and any cause chain are gone, so your log line can't tell you where or why the failure happened. JSON.stringify is just as bad: message and stack are non-enumerable own properties, so it serializes an Error to '{}'. The fix is to log the object itself: console.error(err) or a structured logger.error({ err }), both of which print or capture the full stack and, with util.inspect under the hood, the cause chain too. The hook is exactly this bug — the Error was complete, and the + operator flattened it to a useless prefix, hiding a one-line timeout misconfig for three weeks." },
    { q: "What is the correct response to an uncaughtException, and why is it different from a normal catch?", a: "After an uncaughtException the process is, per the Node docs, in an undefined state — an exception tore through the stack without any code getting to run its cleanup, so module invariants, open transactions, and in-flight state may all be inconsistent. That means you must NOT resume normal operation. The uncaughtException handler is for last rites only: log the error, optionally flush metrics or close a few resources quickly, and then process.exit(1). You then rely on a supervisor — systemd, Kubernetes, PM2 — to start a fresh, clean process. This is the opposite of a normal try/catch, where you caught a specific, expected failure at a point where state is still known-good and can sensibly continue. The global net is a crash-cleanly device, not a keep-running-anyway device; the same logic applies to unhandledRejection, which since Node 15 already crashes by default for this reason." },
  ]}
/>

<Recap lang="en">
In Node, the unit of failure is an `Error` object, and most error-handling bugs are really data-destruction bugs: concatenating or `JSON.stringify`-ing an error throws away the `stack`, the `code`, and the `cause` chain, so log the object itself and branch on the stable `code`, never the prose `message`. Failure travels three ways bound to three call styles — a synchronous `throw` caught by `try/catch` in the same tick, a promise rejection you must `await` (or `.catch`) to make catchable, and an error-first `cb(err, data)` you check and `return` on the first line — plus the `EventEmitter` `'error'` event, which crashes the process if nobody is listening. When you catch to add context, re-throw a typed error with `{ cause: original }` and set `this.name`, so you gain a branchable type without losing the trace. And design the global net deliberately: an unhandled rejection crashes by default since Node 15, and after an `uncaughtException` the process state is undefined — log, then `process.exit(1)` and let a supervisor restart a clean one, rather than swallowing the failure and serving from a corrupted state.
</Recap>
