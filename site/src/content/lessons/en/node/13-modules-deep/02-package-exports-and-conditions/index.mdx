---
concepts:
- exports-field
- subpath-exports-and-patterns
- conditional-exports
- imports-field-private-specifiers
- dual-package-hazard
- export-encapsulation
deepensInto: []
estMin: 20
lang: en
lessonType: topic
level: senior
order: 2
prereqs:
- 01-module-resolution-algorithm
slug: 02-package-exports-and-conditions
sources:
- https://nodejs.org/api/packages.html#package-entry-points
- https://nodejs.org/api/packages.html#conditional-exports
- https://nodejs.org/api/packages.html#dual-commonjs-es-module-packages
spiral: []
status: ready
summary: 'The exports field is your package''s public API contract: it overrides main, blocks every path it does not list, and routes import vs require to different files via conditions — which is also how you create, and avoid, the dual-package hazard of two divergent module instances.'
title: 'Package exports and conditions: the public API contract'
track: node
unit: 13-modules-deep
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import conditionOrder from "./infographics/condition-order.svg?raw";

<Hook>
You upgrade a dependency by one minor version and your build explodes: `Error [ERR_PACKAGE_PATH_NOT_EXPORTED]: Package subpath './lib/parse' is not defined by "exports"`. The file `lib/parse.js` is still in `node_modules`, byte-for-byte unchanged. What changed is that the maintainer added an `exports` field — and the resolver now treats every path not listed in it as if it does not exist. The same field, used one way, also lets a library load itself twice with two divergent copies of its own state. The `exports` field is the most consequential nine lines in a modern `package.json`, and most people never read its rules.
</Hook>

<Crux>The exports field is a package's public API: it overrides main, hides unlisted paths, and routes import vs require via conditions.</Crux>

<Explanation>

## exports overrides main and seals the package

The classic `main` field names one entry point and leaves the rest of the package wide open: any consumer can `require("pkg/src/internal/helper.js")` because resolution just walks the filesystem. The `exports` field inverts that. The moment a package declares `exports`, three rules take effect at once:

- **It overrides `main`.** Node consults `exports` first; `main` is only the fallback for packages without `exports`.
- **It is an allow-list, not a hint.** Any subpath *not* listed becomes unreachable — `ERR_PACKAGE_PATH_NOT_EXPORTED` — even if the file physically exists. This is *encapsulation*: the package author decides the public surface, and internals can be refactored freely because no one could legally import them.
- **You must export `package.json` explicitly** if consumers (or tooling) read it: `"./package.json": "./package.json"`.

Together, these three rules flip the package from open-by-default to sealed-by-contract. Without the third rule, tooling silently breaks; without the second, you have no true API boundary — any deep `require("pkg/dist/internal")` that worked will keep working, and you cannot safely refactor internals.

```json
{
  "name": "mylib",
  "exports": {
    ".": "./dist/index.js",
    "./package.json": "./package.json"
  }
}
```

With this, `import "mylib"` resolves to `dist/index.js`, and `import "mylib/dist/anything-else.js"` throws — the deep-import door is shut. That is the senior reason to add `exports` deliberately: it is the only mechanism Node gives you to draw a real API boundary around a package, turning "every file is public" into "only what I publish is public."

## Subpath exports and patterns

The `.` key is the package root; additional keys define **subpath exports** — named entry points consumers reach as `pkg/<subpath>`:

```json
{
  "exports": {
    ".": "./dist/index.js",
    "./parser": "./dist/parser.js",
    "./utils/format": "./dist/utils/format.js"
  }
}
```

`import "mylib/parser"` now maps to `dist/parser.js` — note the *public* name (`/parser`) is decoupled from the *physical* path (`dist/parser.js`), so you can move files internally without breaking consumers as long as the map stays stable. For many subpaths, **patterns** with `*` avoid listing each by hand. The `*` is a non-greedy wildcard captured on the left and substituted on the right:

```json
{
  "exports": {
    ".": "./dist/index.js",
    "./features/*": "./dist/features/*.js"
  }
}
```

Here `import "mylib/features/auth"` resolves to `dist/features/auth.js`. Patterns expose a *whole directory* through one rule, so use them when you genuinely want that subtree public — and remember the encapsulation tradeoff: a broad `./*` pattern re-opens the package almost as wide as having no `exports` at all.

## Conditional exports: one specifier, many targets

A subpath's value can be a **conditions object** instead of a string: Node walks its keys *in order* and picks the first whose condition matches the current environment. This is how one package serves ESM to `import` and CommonJS to `require`:

```json
{
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.mjs",
      "require": "./dist/index.cjs",
      "default": "./dist/index.mjs"
    }
  }
}
```

Order is significant — **first match wins**, not most-specific — so the conventional ordering is `types` first (tooling reads it before anything runs), then the runtime conditions, then `default` last as the catch-all. The common conditions:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Condition</th>
        <th class="border border-border px-3 py-2 text-left">Matches when…</th>
        <th class="border border-border px-3 py-2 text-left">Typical target</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>import</code></td>
        <td class="border border-border px-3 py-2">Loaded via <code>import</code> / dynamic <code>import()</code></td>
        <td class="border border-border px-3 py-2">ESM build (<code>.mjs</code>)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>require</code></td>
        <td class="border border-border px-3 py-2">Loaded via <code>require()</code></td>
        <td class="border border-border px-3 py-2">CJS build (<code>.cjs</code>)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>node</code></td>
        <td class="border border-border px-3 py-2">Running on Node (any loader)</td>
        <td class="border border-border px-3 py-2">Node-specific code</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>types</code></td>
        <td class="border border-border px-3 py-2">TypeScript resolves the package</td>
        <td class="border border-border px-3 py-2"><code>.d.ts</code> declarations</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>default</code></td>
        <td class="border border-border px-3 py-2">Always (final fallback)</td>
        <td class="border border-border px-3 py-2">Safe universal entry</td>
      </tr>
    </tbody>
  </table>
</div>

<Infographic
  svg={conditionOrder}
  label="An ordered sequence of the canonical conditional-exports keys — types, then import, then require, then default — showing the order Node walks them and why each sits where it does."
  caption="Node takes the first matching key, not the most specific — so types goes first (TypeScript reads it before code runs) and default goes last, or a higher catch-all silently shadows every runtime condition below it."
  hue="var(--d-infra)"
/>

<Inset kind="why" lang="en">
Why does `types` go first when it is not even a runtime condition? Because TypeScript resolves the package at *check* time, before any code runs, and it stops at the first matching key just like Node does. If `import` or `require` sits above `types`, TypeScript may match the JavaScript target and never find the declarations — so put `types` at the top of each conditions block. The mirror trap is putting `default` anywhere but last: because matching is first-wins, a `default` above `require` would shadow it and every consumer would get the default target regardless of how they loaded the package.
</Inset>

## The imports field: private internal specifiers

Where `exports` defines the *outward* API, the sibling **`imports`** field defines *inward* aliases — private specifiers, always prefixed with `#`, visible only inside the package itself:

```json
{
  "imports": {
    "#db": {
      "node": "./src/db/postgres.js",
      "default": "./src/db/memory.js"
    },
    "#internal/*": "./src/internal/*.js"
  }
}
```

Inside the package you write `import db from "#db"` and Node resolves it through the map — with full condition support, so `#db` can point at a real database on Node and an in-memory stub elsewhere. Unlike a relative path, a `#` specifier is stable no matter how deep the importing file is nested, and unlike a bare specifier it never escapes to `node_modules`. It is the clean way to do internal aliasing without a bundler or `tsconfig` path hackery.

## The dual-package hazard

Conditional exports make it easy to ship both an ESM and a CJS build. The trap: if **both** builds carry state, an application can load **both** — ESM code reaches the `import` target, CJS code reaches the `require` target — and now two *separate* module instances of your package coexist, each with its own copy of every variable, registry, and `instanceof` identity. Singletons silently double; an object created by one half fails an `instanceof` check in the other. This is the **dual-package hazard**.

```js
// Application accidentally loading both halves:
import { registry } from "mylib";        // → dist/index.mjs instance A
const { registry: r2 } = require("mylib"); // → dist/index.cjs instance B
// registry !== r2  — two states, the hazard
```

The robust avoidances, in order of preference: (1) **ship ESM only** and let CJS consumers reach you via dynamic `import()`, so there is exactly one instance; or (2) keep all stateful logic in a single CJS (or single ESM) core and make the other condition a thin **wrapper** that re-exports the same instance rather than a second copy. The anti-pattern is two independent full builds that each own state — that is the configuration that bites.

<Quiz
  id="13-modules-deep-pec-quiz-encapsulation"
  lessonSlug="02-package-exports-and-conditions"
  lang="en"
  question="After a dependency adds an exports field, require('pkg/lib/parse') throws ERR_PACKAGE_PATH_NOT_EXPORTED, though lib/parse.js still exists. Why?"
  choices={[
    { label: "exports is an allow-list: any subpath not listed becomes unreachable, regardless of whether the file exists on disk — that is the encapsulation it provides", correct: true },
    { label: "The file was deleted in the upgrade", misconception: "The error is specifically ERR_PACKAGE_PATH_NOT_EXPORTED, not MODULE_NOT_FOUND — the file is present; the resolver is refusing to expose a path the exports map does not list." },
    { label: "exports only blocks ESM imports, not require", misconception: "The exports allow-list applies to both require and import — the encapsulation is module-system agnostic." },
    { label: "You must add a .js extension to deep imports", misconception: "Adding an extension does not help: the path is simply not in the exports map, so no spelling of it resolves. You must use an exported entry point (or the maintainer must export the subpath)." },
  ]}
/>

<Quiz
  id="13-modules-deep-pec-quiz-order"
  lessonSlug="02-package-exports-and-conditions"
  lang="en"
  question="In a conditions block, you place 'default' above 'require'. What happens to CommonJS consumers?"
  choices={[
    { label: "They get the 'default' target — condition matching is first-match-wins by key order, so 'default' above 'require' shadows it for everyone", correct: true },
    { label: "Nothing — Node picks the most specific condition regardless of order", misconception: "Node does NOT pick most-specific; it walks keys top to bottom and takes the first match. Order is the whole contract — default must come last." },
    { label: "require consumers get an error because default and require conflict", misconception: "There is no conflict error; default simply matches first and wins silently, which is worse — the misroute is invisible until something behaves wrong." },
    { label: "Only ESM consumers are affected", misconception: "default matches every loader, so placing it above require specifically robs require consumers of their intended CJS target." },
  ]}
/>

<TradeoffMatrix
  id="13-modules-deep-pec-tradeoff"
  lessonSlug="02-package-exports-and-conditions"
  lang="en"
  prompt="You publish a stateful library (it keeps a plugin registry) and want both import and require consumers to work without the dual-package hazard. What do you ship?"
  options={[
    { name: "ESM-only build; CJS consumers reach it via dynamic import(); exactly one instance exists", summary: "ESM-only, one instance.", correct: true, justification: "The hazard is two stateful instances; shipping one build makes a second impossible. Modern Node lets CJS callers await import() of an ESM package, so you keep a single source of truth for the registry and accept only that older sync-require consumers must adapt. For genuinely stateful libraries this is the cleanest guarantee that import and require see the same state." },
    { name: "Two full independent builds (.mjs and .cjs), each with its own registry, wired via import/require conditions", summary: "Two full builds.", justification: "This is the dual-package hazard itself: an app that loads both halves gets two registries and two instanceof identities, and the bug is invisible until a plugin registered through one half is missing in the other. Convenient to generate, dangerous for stateful packages." },
    { name: "Single CJS core holds the state; the ESM condition is a thin wrapper that re-exports the same instance", summary: "Core + wrapper.", justification: "A legitimate avoidance: only one module owns state and the other entry just re-exports it, so both conditions resolve to one instance. Slightly more build wiring than ESM-only and you must be disciplined that the wrapper never duplicates state, but it keeps sync require support — a reasonable choice when CJS consumers must not break." },
    { name: "Omit exports entirely and let consumers import whichever file they want", summary: "No exports map.", justification: "This dodges the conditions question but reopens the whole package as public API and still does nothing to prevent two instances — consumers can import the .mjs and require the .cjs directly. It trades a manageable problem for a worse one: no encapsulation and the hazard remains." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="How Node resolves a bare import against a package's exports field: match the subpath in the exports map, then walk the conditions object in order picking the first match, else throw ERR_PACKAGE_PATH_NOT_EXPORTED"
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "imp", label: "import 'pkg/sub'", sub: "bare + subpath" },
    { id: "map", label: "exports map", sub: "subpath listed?" },
    { id: "cond", label: "conditions", sub: "first match wins" },
    { id: "target", label: "resolved file", sub: ".mjs / .cjs" },
    { id: "block", label: "PATH_NOT_EXPORTED", sub: "unlisted subpath" },
  ]}
  edges={[
    { from: "imp", to: "map" },
    { from: "map", to: "cond", label: "listed" },
    { from: "map", to: "block", label: "unlisted" },
    { from: "cond", to: "target", label: "import / require / default" },
  ]}
  caption="The subpath must appear in the exports map (else PATH_NOT_EXPORTED); its conditions object is then walked top-to-bottom and the first matching key selects the file."
/>

<KeyTakeaway>exports is a package's API surface: overrides main, hides unlisted paths. Conditions route import/require/types first-match-wins — order types → runtime → default. Two stateful builds = dual-package hazard; ship one.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="13-modules-deep-pec-retrieval"
  lang="en"
  questions={[
    { q: "Explain what the exports field changes about resolution the moment a package adds it, and why a previously-working deep import can suddenly throw.", a: "Adding exports flips a package from open to sealed. Three things happen at once. First, exports overrides main: Node consults the exports map first and only falls back to main for packages that have no exports. Second, exports is an allow-list rather than a hint — any subpath not explicitly listed becomes unreachable and throws ERR_PACKAGE_PATH_NOT_EXPORTED, even though the file is physically present in node_modules. That is the encapsulation the field exists to provide: the author declares the public surface and can refactor internals freely because no consumer could legally import them. Third, even package.json must be exported explicitly (./package.json: ./package.json) if tooling reads it. So a deep import like require('pkg/lib/parse') that worked for years breaks after a minor upgrade not because the file moved or was deleted, but because the maintainer added an exports map that does not list ./lib/parse — the resolver now refuses to look at any path outside the contract. The fix is to use a path the package actually exports, or ask the maintainer to add the subpath." },
    { q: "What is the dual-package hazard, how do conditional exports cause it, and how do you avoid it?", a: "Conditional exports let one package ship both an ESM build (served to import) and a CJS build (served to require) by listing import and require conditions that point at different files. The hazard appears when both of those builds carry state and an application ends up loading both — its ESM code resolves the import target while some CJS code resolves the require target. Now two separate module instances of the same package coexist, each with its own copy of every variable, registry, and class identity. The symptoms are nasty and silent: a singleton is initialised twice, a value created by one half fails an instanceof check in the other, a plugin registered through import is missing when read through require. The robust avoidances, best first: ship ESM-only and let CJS consumers reach you through dynamic import(), so exactly one instance can ever exist; or keep all stateful logic in a single core (say CJS) and make the other condition a thin wrapper that re-exports that same instance instead of a second full build. The anti-pattern that triggers the hazard is two independent full builds that each own state — that is precisely the configuration to avoid for any stateful library." },
  ]}
/>

<Recap lang="en">
The exports field is the most consequential entry in a modern package.json because it converts a package from an open directory into a sealed public API. The moment it is present it overrides main, and it acts as an allow-list: any subpath not explicitly listed throws ERR_PACKAGE_PATH_NOT_EXPORTED even though the file still exists — which is the encapsulation that lets authors refactor internals safely and is why adding exports can break a long-standing deep import. Subpath exports decouple the public name from the physical path, and * patterns expose a whole subtree through one rule (at the cost of re-opening encapsulation if too broad). A subpath's value can be a conditions object that Node walks top-to-bottom, first-match-wins, to serve different files to import, require, node, types, and default — so order matters: types first so TypeScript finds declarations, default last as the catch-all. The sibling imports field defines private # specifiers that are internal-only, condition-aware, and stable regardless of file depth. Finally, conditional exports enable the dual-package hazard: two stateful builds loaded together become two divergent instances with separate state and broken instanceof identity. Avoid it by shipping a single instance — ESM-only with dynamic import() for CJS consumers, or a single stateful core that the other condition merely re-exports. Now when you see ERR_PACKAGE_PATH_NOT_EXPORTED after a minor upgrade, you know exactly why: the file is there, but the author added exports and did not list that path — check the exports map, find an exported entry point, and move on.
</Recap>
