---
concepts:
- integration-vs-unit
- supertest-request
- ephemeral-port
- test-database-strategy
- testcontainers
- test-isolation
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-integration-and-supertest
sources:
- https://github.com/ladjs/supertest
- https://testcontainers.com/
- https://nodejs.org/api/test.html
spiral: []
status: ready
summary: 'An integration test drives routing, middleware, handler and a real DB
  through the HTTP surface. supertest boots your app on an ephemeral port; isolation
  and a real engine decide whether the suite catches bugs or invents flakes.'
title: 'Integration testing an HTTP API'
track: node
unit: 06-testing
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import testPyramid from "./infographics/test-pyramid.svg?raw";

<Hook>
A team had 400 green unit tests and shipped a `POST /orders` route that 500'd on the first real request. The handler was perfect in isolation; the bug was that the auth middleware ran *after* the body parser, so `req.user` was undefined by the time validation read it — a wiring error no unit test could see, because every unit test called the handler directly with a fake `req`. Worse, their one integration test passed locally and failed in CI at random: it ran against the same shared dev Postgres as everyone else, so a colleague's seed row made the "expect 1 result" assertion flip between 1 and 2. They didn't have a missing test. They had tests that never exercised the seam where the bug actually lived.
</Hook>

<Crux>An integration test runs routing + middleware + handler + a real DB through HTTP; supertest boots an ephemeral port.</Crux>

<Explanation>

## What an integration test covers that a unit test can't

Ask yourself: when was the last time a bug in your unit-test-covered code still made it to production? Chances are it lived at a seam — middleware ordering, a wrong status code, auth wired to the wrong path. That is the gap integration tests close.

A unit test pins one function with its collaborators stubbed: fast, focused, and blind to wiring. An **integration test exercises the whole request path** — router → middleware chain → handler → (usually) the real database — through the actual HTTP surface. That is exactly where a class of bugs lives that unit tests structurally cannot reach: middleware ordering, a forgotten `await` in a route, a serializer that drops a field, a status code that's 200 when it should be 201, a content negotiation mismatch, an auth guard wired to the wrong path. The handler can be individually correct and the *assembly* still broken.

In the test pyramid, integration tests sit **above unit tests and below end-to-end**: more of them than E2E (which drives a real browser/network and is slow and flaky), fewer than units. The payoff per test is high — one integration test covers many units' worth of seams — but each costs more to run, so you don't write thousands. The classic failure is an "ice-cream cone": almost all tests at the slow end, a hollow middle, and contract bugs leaking to production.

<Infographic
  svg={testPyramid}
  label="A test pyramid with three tiers: the narrow apex is end-to-end tests (fewest, driving a real browser/network, slow and flaky); the middle is integration tests (the full request path over HTTP plus a real DB, catching wiring bugs); the wide base is unit tests (most, one function with collaborators stubbed, fast and focused)."
  caption="Integration tests are the middle tier — fewer than units, more than slow E2E. Inverting this into an ice-cream cone is the classic failure that lets contract bugs leak to prod."
  hue="var(--d-infra)"
/>

## supertest: real HTTP calls without managing a port

`supertest` wraps any Node `http` request handler — an Express or Fastify `app`, or a bare `(req, res) => …` — and on each call **boots it on an ephemeral port** (or dispatches in-process), fires a real HTTP request, and tears the listener down. You never pick a port, never risk `EADDRINUSE` from a hard-coded `3000`, and you assert on the real status line, headers, and parsed body.

```js
import { test } from "node:test";
import assert from "node:assert/strict";
import request from "supertest";
import { app } from "../src/app.js"; // do NOT call app.listen() here

test("POST /users creates a user", async () => {
  const res = await request(app)
    .post("/users")
    .send({ email: "a@b.com" })   // sets JSON body + content-type
    .expect("Content-Type", /json/)
    .expect(201);

  assert.equal(res.body.email, "a@b.com");
  assert.match(res.headers.location, /^\/users\/\d+$/);
});
```

Pass the `app` itself, not a running server — supertest manages the listener so the test owns the lifecycle. Because the call goes over real HTTP, it catches the wiring bug from the hook: middleware order, body parsing, and the status code are all in play, not stubbed away.

## A real database: four strategies, four tradeoffs

The database is where integration tests earn their keep — and where they go wrong. Four common approaches:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Strategy</th>
        <th class="border border-border px-3 py-2 text-left">Speed</th>
        <th class="border border-border px-3 py-2 text-left">Fidelity</th>
        <th class="border border-border px-3 py-2 text-left">Isolation</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Transaction per test + rollback</td>
        <td class="border border-border px-3 py-2">Fastest (no I/O to reset)</td>
        <td class="border border-border px-3 py-2">Real engine</td>
        <td class="border border-border px-3 py-2">Strong, automatic</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Truncate / reset tables</td>
        <td class="border border-border px-3 py-2">Slower (writes each test)</td>
        <td class="border border-border px-3 py-2">Real engine</td>
        <td class="border border-border px-3 py-2">Good, if you reset all</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Testcontainers (Docker Postgres/Redis)</td>
        <td class="border border-border px-3 py-2">Slow startup (~secs/suite)</td>
        <td class="border border-border px-3 py-2">Highest — matches prod</td>
        <td class="border border-border px-3 py-2">Per-suite container</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">In-memory SQLite</td>
        <td class="border border-border px-3 py-2">Very fast</td>
        <td class="border border-border px-3 py-2"><strong>Wrong engine</strong> — false confidence</td>
        <td class="border border-border px-3 py-2">Per-connection</td>
      </tr>
    </tbody>
  </table>
</div>

**Rollback** wraps each test in a transaction and aborts it at the end — nothing commits, so the next test sees a clean database with zero reset cost. It's the fastest with strong isolation, but it can't test code that itself commits or relies on transaction visibility (you can't roll back what your code already committed, and nested-transaction tricks change semantics). **Truncate/reset** is simpler and lets you test commit logic, at the cost of an explicit cleanup write between tests. **Testcontainers** spins up a real Postgres or Redis in a throwaway Docker container per suite: highest fidelity because it's the same engine, version, and SQL dialect as production — at the price of seconds of container startup. **In-memory SQLite** is fast and tempting, but it's a *different database*: it won't enforce your Postgres types, lacks `JSONB`, `RETURNING`, partial indexes, and many constraints, so a green SQLite suite can hide SQL that explodes on the real Postgres in prod. Prefer a real engine (Testcontainers) for fidelity; reach for rollback when suite speed dominates.

<Inset kind="why" lang="en">
Why is SQLite-for-Postgres specifically dangerous and not just "less accurate"? Because the failure is *silent and inverted*: the test is green and the production query is the thing that breaks. A migration using `ALTER TABLE ... ADD COLUMN ... GENERATED`, a query with `ON CONFLICT ... DO UPDATE`, a `citext` column, or a check constraint can all parse and pass on SQLite (which ignores or fakes them) while failing or behaving differently on Postgres. You ship with a green build and discover the divergence from a customer. Testing against the engine you deploy is the only way to make the test's verdict trustworthy.
</Inset>

## Isolation, and the open-handle hang

An integration suite is only trustworthy if every test is **independent**: no leftover rows from a previous test, no ordering dependence, no shared mutable state. Seed deterministic fixtures inside each test (or a fresh per-test transaction), point the suite at a **dedicated test database or schema** — never a shared dev DB — and give parallel workers **separate schemas or databases** so two workers don't fight over the same rows. The hook's flake is the canonical violation: tests against a shared DB become order-dependent, mutually destructive, and pass-or-fail by luck.

The second classic failure is the **process that won't exit**. If you open a server listener or a DB connection pool and never close them, the Node test runner finishes its assertions but the event loop still has live handles, so the process hangs (or warns about open handles / leaked timers). Always close everything in teardown:

```js
import { test, after } from "node:test";
import { pool } from "../src/db.js";

// node:test runs `after` hooks once the file's tests complete
after(async () => {
  await pool.end();   // close the DB connection pool
  // if you started a listener, server.close() too
});
```

With supertest you usually don't start a listener at all (it manages ephemeral ones), so the pool is the handle people forget. A hanging CI job that times out after ten minutes is almost always an unclosed pool or socket — not a slow test.

<TradeoffMatrix
  id="06-integration-tradeoff-1"
  lessonSlug="02-integration-and-supertest"
  lang="en"
  prompt="Your API uses Postgres-specific features (JSONB columns, ON CONFLICT upserts, partial indexes). You need an integration suite that catches SQL bugs before prod, and you can afford a few seconds of startup per suite. Which test-DB strategy?"
  options={[
    { name: "Testcontainers: spin up a real Postgres in Docker per suite", summary: "Real engine, same dialect.", correct: true, justification: "Fidelity is the hard requirement here: JSONB, ON CONFLICT, and partial indexes are Postgres-specific, so only the real engine, version, and dialect will tell you whether your SQL actually works. Testcontainers gives you a disposable, prod-matching Postgres per suite; the few seconds of startup is exactly the cost you said you can pay. This is the strategy that makes the suite's green build trustworthy." },
    { name: "In-memory SQLite for speed", summary: "Fast but wrong engine.", justification: "This is the trap. SQLite doesn't truly implement JSONB, ON CONFLICT semantics, or partial indexes — it ignores, fakes, or diverges on them — so the suite goes green while the real Postgres query fails in production. You'd be optimizing the one number (speed) that doesn't matter and sacrificing the one that does (fidelity), producing confident false negatives." },
    { name: "Mock the database layer entirely", summary: "No DB at all.", justification: "Mocking the DB turns an integration test back into a unit test: you're now asserting against your own stub, not against Postgres, so by definition it cannot catch a Postgres-specific SQL bug. It's useful for isolating logic, but it answers a different question than 'does this SQL run on prod's engine?'" },
    { name: "Run against the shared staging Postgres", summary: "Real engine, shared state.", justification: "It uses the real engine, but a shared database breaks isolation: other writers create order-dependence and flakes, you can't run workers in parallel safely, and a test could corrupt staging data. A throwaway per-suite container gives the same engine fidelity with clean, isolated state — strictly better for tests." },
  ]}
/>

<Quiz
  id="06-integration-quiz-1"
  lessonSlug="02-integration-and-supertest"
  lang="en"
  question="What does request(app) do with the network port when you call supertest?"
  choices={[
    { label: "It boots the app on an ephemeral (OS-chosen, free) port for the request and tears it down after — you never pick or manage a port", correct: true },
    { label: "It connects to whatever port app.listen(3000) opened, so you must start the server first", misconception: "You pass the app, not a running server, and you should NOT call app.listen() in the test. supertest manages an ephemeral listener itself, which is why hard-coded ports and EADDRINUSE conflicts disappear." },
    { label: "It sends the request to a live remote server over the public internet", misconception: "supertest makes a real HTTP call but against your own app booted locally on an ephemeral port (or dispatched in-process) — it is not hitting an external/remote server." },
    { label: "It bypasses HTTP entirely and calls the handler function directly with a fake req", misconception: "That's a unit test. supertest goes over real HTTP so routing, middleware order, and status codes are exercised — exactly the wiring a direct handler call would skip." },
  ]}
/>

<Quiz
  id="06-integration-quiz-2"
  lessonSlug="02-integration-and-supertest"
  lang="en"
  question="Your integration suite is green on in-memory SQLite but the API 500s in production on a query using ON CONFLICT and a JSONB column. Why did the tests miss it?"
  choices={[
    { label: "SQLite is a different engine that doesn't implement Postgres-specific SQL the same way, so it accepted (or faked) syntax that real Postgres rejects — the green build was false confidence", correct: true },
    { label: "The tests didn't have enough assertions", misconception: "Adding assertions wouldn't help: the test ran against the wrong database, so it never executed the real Postgres query path at all. The fix is fidelity — test on the engine you deploy (e.g. Testcontainers), not more asserts on SQLite." },
    { label: "supertest can't test database code", misconception: "supertest tests the HTTP path and works fine with a real DB behind it. The miss is the DB choice, not supertest — point the same suite at a real Postgres and it would have caught the failure." },
    { label: "Production was running a newer Node version", misconception: "The Node version isn't the issue; the SQL dialect is. SQLite and Postgres differ in types, upsert semantics, and JSONB support regardless of Node version, which is why an SQLite-backed suite can't vouch for Postgres SQL." },
  ]}
/>

<DragOrder
  id="06-integration-drag-1"
  lessonSlug="02-integration-and-supertest"
  lang="en"
  prompt="Order the lifecycle of one isolated integration test, from setup to clean exit:"
  items={[
    { id: "a", label: "Start an isolated DB (per-suite container or a per-test transaction) pointed at a dedicated test schema" },
    { id: "b", label: "Seed deterministic fixtures this test needs — and nothing more" },
    { id: "c", label: "Fire the request via request(app).post('/...').send(...) over an ephemeral port" },
    { id: "d", label: "Assert on status, headers, and parsed body" },
    { id: "e", label: "Clean up: rollback the transaction (or truncate), then close the pool/server in teardown" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<SequenceDiagram
  label="One integration test driving the app over HTTP into a real test database"
  hue="var(--d-systems)"
  actors={["test", "supertest", "app", "test DB"]}
  messages={[
    { from: "test", to: "supertest", label: "request(app).post('/users')" },
    { from: "supertest", to: "app", label: "real HTTP on ephemeral port" },
    { from: "app", to: "app", label: "router → middleware → handler" },
    { from: "app", to: "test DB", label: "INSERT (real engine)" },
    { from: "test DB", to: "app", label: "row + RETURNING id" },
    { from: "app", to: "supertest", label: "201 + JSON body" },
    { from: "supertest", to: "test", label: "res.status / res.body" },
  ]}
  caption="supertest boots the app on an ephemeral port and makes a real HTTP call, so routing, middleware order, and the handler all run; the request reaches a real test DB whose rows are isolated per test (transaction or per-suite container) and torn down afterward."
/>

<KeyTakeaway>Integration tests run routing, middleware, handler and a real DB through HTTP, catching wiring bugs units miss. Prefer a real engine over SQLite, isolate every test, and close the pool in teardown.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-integration-retrieval"
  lang="en"
  questions={[
    { q: "Why does an integration test catch bugs a unit test structurally cannot, and where does it sit in the test pyramid?", a: "A unit test calls one function with its collaborators stubbed, so it never executes the seams between components: middleware ordering, body parsing, status-code selection, serialization, auth-guard wiring, and the real DB query path. An integration test drives the full request through the actual HTTP surface — router → middleware → handler → real database — so it exercises exactly those seams. The hook's bug (auth middleware running after the body parser, leaving req.user undefined) is invisible to any unit test that calls the handler directly with a fake req, but a supertest POST hits it immediately. In the pyramid, integration tests sit above unit tests and below end-to-end: fewer than units (they cost more to run), more than E2E (which drives a real browser/network and is slow and flaky). Each integration test covers many units' worth of wiring, so the payoff per test is high; the anti-pattern is the ice-cream cone, where the middle is hollow and contract bugs leak to prod." },
    { q: "Compare the four test-DB strategies and say when each is right.", a: "(1) Transaction-per-test + rollback: wrap each test in a transaction and abort it, so nothing commits and the next test sees a clean DB at zero reset cost — fastest, strong automatic isolation, real engine; but it can't test code that itself commits or depends on transaction visibility. (2) Truncate/reset tables between tests: simpler and lets you test commit logic, at the cost of an explicit cleanup write each test, so it's slower. (3) Testcontainers: spin up a real Postgres/Redis in a throwaway Docker container per suite — highest fidelity because it's the same engine, version, and SQL dialect as production, at the price of seconds of startup. (4) In-memory SQLite: very fast but a different engine — it won't enforce Postgres types and lacks JSONB, RETURNING, ON CONFLICT semantics, partial indexes, so a green SQLite suite can hide SQL that fails on real Postgres (silent, inverted failure). Default to a real engine via Testcontainers for fidelity, and use rollback when suite speed dominates and you don't need to test commit behaviour." },
  ]}
/>

<Recap lang="en">
An integration test exercises routing, the middleware chain, the handler, and usually a real database together through the HTTP surface, catching the wiring and contract bugs — middleware order, a missing await, a wrong status code, a dropped field — that unit tests structurally cannot see because they call the handler in isolation. It belongs above unit tests and below E2E in the pyramid: fewer than units, more than the slow, flaky end-to-end layer. `supertest`'s `request(app)` boots your Express/Fastify/`http` app on an ephemeral, OS-chosen port (or in-process), makes a real HTTP call, and tears down — so you never manage a port and you assert on the true status, headers, and body. The database choice decides whether the suite tells the truth: transaction-per-test rollback is fastest with strong isolation but can't test commit logic; truncate/reset is simpler and slower; Testcontainers gives the highest fidelity by running the same Postgres/Redis as prod for a few seconds of startup; and in-memory SQLite is fast but a *different engine* whose green build can hide Postgres-specific SQL that breaks in production. Keep every test independent — deterministic fixtures, a dedicated test DB/schema, separate schemas per parallel worker — and always close the server and connection pool in teardown, or live handles will hang the runner. The hook's two failures, a wiring bug unit tests couldn't reach and a shared-DB flake, are the exact pair this discipline prevents. Now when you see an integration test fail in CI but pass locally, your first question is isolation: is it hitting a shared DB, a leaked handle, or a missing teardown?
</Recap>
