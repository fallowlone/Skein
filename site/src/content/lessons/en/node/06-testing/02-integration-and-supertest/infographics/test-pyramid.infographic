infographic sequence-pyramid-simple
data
  title Where integration tests sit
  desc Fewer than units, more than slow end-to-end
  sequences
    - label End-to-end
      desc Fewest — drives a real browser/network; slow and flaky
    - label Integration
      desc Mid layer — full request path over HTTP + a real DB; catches wiring bugs
    - label Unit
      desc Most — one function, collaborators stubbed; fast and focused
theme
  palette #f43f5e #f59e0b #14b8a6
