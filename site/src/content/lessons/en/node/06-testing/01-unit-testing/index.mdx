---
concepts:
- node-test-runner
- node-assert
- mocking-and-spies
- fake-timers
- vitest
- test-isolation
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-unit-testing
sources:
- https://nodejs.org/api/test.html
- https://nodejs.org/api/assert.html
- https://vitest.dev/guide/
spiral: []
status: ready
summary: 'Node ships a stable test runner (node:test) plus node:assert; Vitest adds
  Vite speed and rich matchers. Test behavior not implementation, keep each test independent,
  and fake the clock so time-dependent tests are fast and deterministic.'
title: 'Unit testing with node:test and Vitest'
track: node
unit: 06-testing
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A retry helper had a test that waited a real five seconds for a `setTimeout` backoff, and the suite was green — until CI got busy. Under load the timer fired a few milliseconds late, the assertion ran before the retry completed, and the test failed roughly one run in twenty. The team marked it `skip`, shipped, and three weeks later a real backoff regression slipped through because the only test that covered it had been muted. The failure was never in the retry code. It was a test coupled to the wall clock, and the fix was four lines: fake the timers, advance them by hand, and the same test became instant and 100% deterministic.
</Hook>

<Crux>Test behavior, not implementation: node:test for zero-dep speed or Vitest for rich matchers, isolate state, and fake the clock.</Crux>

<Explanation>

## The built-in runner: node:test and node:assert

Before you reach for a third-party runner, consider what you already have: since Node 18 (stable in Node 20) the built-in runner covers the whole unit-testing workflow with zero install cost, and that matters on a lean CI pipeline or a published library that can't afford extra transitive deps.

Since Node 20 the test runner is stable and built in — no dependency, no config. You write tests with `test()` (or `describe()` / `it()`), assert with `node:assert`, and run them with `node --test`, which discovers files matching `*.test.js`, `*-test.js`, and files under `test/` directories. Add `--watch` to re-run on change, and `--experimental-test-coverage` to get a coverage summary. Subtests nest with `await t.test(...)`, and hooks (`before`, `after`, `beforeEach`, `afterEach`) set up and tear down state.

```js
import { test, describe, it, beforeEach } from "node:test";
import assert from "node:assert/strict";
import { Cart } from "./cart.js";

describe("Cart", () => {
  let cart;
  beforeEach(() => { cart = new Cart(); }); // fresh state per test

  it("totals line items", () => {
    cart.add({ price: 10, qty: 2 });
    assert.strictEqual(cart.total(), 20);
  });

  it("rejects a negative quantity", () => {
    assert.throws(() => cart.add({ price: 10, qty: -1 }), /quantity/);
  });
});
```

The key with `node:assert` is to import the **strict** variant (`node:assert/strict`) so `assert.equal` means `===`, not the loose `==` that quietly treats `"2"` and `2` as equal. `assert.strictEqual` compares primitives; `assert.deepStrictEqual` walks objects and arrays structurally; `assert.match` checks a string against a regex. For failures you *expect*, assert them directly: `assert.throws(fn, /pattern/)` for synchronous throws and `await assert.rejects(promise, /pattern/)` for rejected promises — a test that asserts the absence of an error where one should fire is a test that passes for the wrong reason.

## Mocking at the boundaries

A unit test should exercise *your* logic, so you replace the slow or non-deterministic things at its edges — the network, the filesystem, the clock — and leave your own code real. `node:test` gives you `mock.fn()` for standalone spies, `t.mock.method(obj, "name")` to replace a method (auto-restored when the test ends), and `t.mock.module()` to swap a whole module. A spy records every call: `mock.calls` holds the arguments, return values, and call count, so you assert on *how* a collaborator was used.

```js
import { test, mock } from "node:test";
import assert from "node:assert/strict";

test("charges the gateway once with the order total", () => {
  const gateway = { charge: mock.fn(() => ({ id: "ch_1" })) };
  const order = { total: 4200, gateway };

  checkout(order);

  assert.strictEqual(gateway.charge.mock.callCount(), 1);
  assert.deepStrictEqual(gateway.charge.mock.calls[0].arguments, [4200]);
});
```

The discipline that matters: **mock at the boundary, not your own logic.** If you mock the function under test, the test asserts your mock, not your code. Stub the payment gateway's HTTP call; do not stub the `checkout()` you are trying to verify.

<Inset kind="why" lang="en">
Fake timers are the highest-leverage mock for backend code. `mock.timers.enable({ apis: ["setTimeout", "Date"] })` swaps the real clock for a controllable one; `mock.timers.tick(5000)` advances virtual time instantly, firing any timer due within that window without the wall clock moving at all. A five-second backoff test runs in microseconds and never flakes on a busy CI box, because there is no real time and therefore no race. Vitest's `vi.useFakeTimers()` / `vi.advanceTimersByTime()` does the same thing. This single technique turns the slowest, flakiest category of unit test into the fastest, most reliable one.
</Inset>

## Choosing the runner: node:test vs Vitest vs Jest

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Runner</th>
        <th class="border border-border px-3 py-2 text-left">Dependencies</th>
        <th class="border border-border px-3 py-2 text-left">Startup / speed</th>
        <th class="border border-border px-3 py-2 text-left">API &amp; env</th>
        <th class="border border-border px-3 py-2 text-left">Reach for it when</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>node:test</code></td>
        <td class="border border-border px-3 py-2">zero (built in)</td>
        <td class="border border-border px-3 py-2">fastest cold start; no bundler</td>
        <td class="border border-border px-3 py-2">core API + <code>node:assert</code>; no DOM</td>
        <td class="border border-border px-3 py-2">libraries, pure backend, minimal deps</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Vitest</td>
        <td class="border border-border px-3 py-2">one dev dep (Vite)</td>
        <td class="border border-border px-3 py-2">fast HMR watch; reuses Vite transform</td>
        <td class="border border-border px-3 py-2">Jest-like <code>expect</code>/<code>vi</code>; jsdom; TS built in</td>
        <td class="border border-border px-3 py-2">rich matchers, snapshots, DOM, monorepo</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Jest</td>
        <td class="border border-border px-3 py-2">several deps</td>
        <td class="border border-border px-3 py-2">slower start; heavy transform</td>
        <td class="border border-border px-3 py-2">mature <code>expect</code>; jsdom; huge ecosystem</td>
        <td class="border border-border px-3 py-2">existing Jest suite, legacy plugins</td>
      </tr>
    </tbody>
  </table>
</div>

`node:test` is the right default for a library or a backend with few dependencies: nothing to install, the fastest startup, and it speaks the core API. Reach for **Vitest** when you want Jest-style ergonomics — chainable `expect` matchers, inline snapshots, `vi.mock`, a `jsdom`/`happy-dom` environment for code that touches the DOM, TypeScript with no extra config, and a snappy HMR-powered watch that re-runs only the affected tests. Vitest is near-drop-in for Jest (`vi.fn` for `jest.fn`, `vi.mock` for `jest.mock`), so it is also the standard escape hatch from a slow Jest setup. Pick Jest itself mainly when you already have a Jest suite and plugins you do not want to migrate.

<TradeoffMatrix
  id="06-unit-testing-tradeoff-1"
  lessonSlug="01-unit-testing"
  lang="en"
  prompt="You are starting a new published npm library: pure TypeScript, no DOM, and you want the smallest dependency tree and fastest CI cold start. Which test runner is the senior default?"
  options={[
    { name: "node:test with node:assert/strict", summary: "Built in, zero deps.", correct: true, justification: "For a dependency-light library this is the strongest fit: nothing to install, the fastest cold start (no bundler transform), and a stable core API since Node 20. You get describe/it, hooks, spies via mock.fn, fake timers, and built-in coverage with --experimental-test-coverage. Fewer dependencies also means a smaller supply-chain surface for a package others install. You give up Jest-style matchers, but a library rarely needs them." },
    { name: "Vitest", summary: "Great DX, one dev dep.", justification: "Excellent for apps, snapshots, DOM work, and monorepos with its HMR watch and rich expect matchers — but it pulls in Vite as a dev dependency and a transform pipeline you do not need for pure TS with no DOM. It is a fine choice and near-Jest-compatible; it is simply heavier than node:test for the stated goal of the smallest tree and fastest cold start." },
    { name: "Jest", summary: "Mature but heavy.", justification: "Mature and battle-tested with a huge ecosystem, but it carries several dependencies and a heavier transform, giving slower startup than both alternatives. It shines when you inherit an existing Jest suite and plugins; for a brand-new minimal-deps library it is the wrong direction." },
    { name: "A hand-rolled assert script run with plain node", summary: "Reinvents the wheel.", justification: "You would re-implement test discovery, hooks, spies, fake timers, reporters, and coverage that node:test already provides for free and maintains. This is more code, more bugs, and no ecosystem — never the senior choice when a stable built-in runner exists." },
  ]}
/>

## Principles that keep tests honest

When you see a test suite getting muted or skipped rather than fixed, these three rules are what was violated. Three rules separate a suite you trust from one you mute. **Test behavior, not implementation:** assert the observable outcome (the return value, the call to a collaborator) rather than private internals, so a refactor that keeps behavior identical does not turn the suite red. A test that breaks every time you rename a private method is testing the wrong thing. **Arrange-Act-Assert:** set up inputs, perform the one action, assert the one outcome — one logical assertion per test makes a failure point straight at its cause. **Keep each test independent:** no shared mutable state, no reliance on run order. Reset state in `beforeEach`; if test B only passes because test A ran first and left a row in a module-level array, you have an order-dependent suite that goes flaky the moment the runner parallelizes or shuffles.

```js
// ❌ order-dependent: module-level state leaks between tests
const users = [];
test("a: adds a user", () => { users.push({ id: 1 }); assert.strictEqual(users.length, 1); });
test("b: starts empty", () => { assert.strictEqual(users.length, 0); }); // FAILS after a

// ✅ independent: fresh state each time
let store;
beforeEach(() => { store = new UserStore(); });
```

<Quiz
  id="06-unit-testing-quiz-1"
  lessonSlug="01-unit-testing"
  lang="en"
  question="A test mocks the function it is supposed to verify and asserts the mock was called. What is wrong with it?"
  choices={[
    { label: "It tests the mock, not the real code — by replacing the unit under test with a stub it can never catch a bug in that unit; you should mock at the boundary (network, fs, clock) and leave your own logic real", correct: true },
    { label: "Mocks are always forbidden in unit tests", misconception: "Mocks are essential — but at the boundaries. You stub the slow or non-deterministic collaborators (HTTP, filesystem, the clock). The mistake here is mocking the very function under test, not the use of mocks in general." },
    { label: "It needs an await because mocks are async", misconception: "mock.fn() is synchronous; nothing here requires await. The problem is conceptual: the test verifies a stub instead of the actual implementation, so it always passes regardless of real behavior." },
    { label: "It should use assert.deepStrictEqual instead of strictEqual", misconception: "The assertion choice is irrelevant. No matter how you assert, mocking the unit under test means the real code never runs, so the test proves nothing about it." },
  ]}
/>

<Quiz
  id="06-unit-testing-quiz-2"
  lessonSlug="01-unit-testing"
  lang="en"
  question="A test waits a real 5 seconds for a setTimeout-based retry, and it flakes under CI load. Why do fake timers fix both the slowness and the flakiness?"
  choices={[
    { label: "Fake timers replace the real clock with a controllable one: mock.timers.tick(5000) advances virtual time instantly and fires the due callback synchronously, so there is no real waiting and no race between the timer and the assertion", correct: true },
    { label: "Fake timers make the CPU run the timeout faster", misconception: "They do not speed up real time — they remove it. The clock is virtual, so advancing it by 5000ms is instantaneous and the callback fires deterministically; nothing about CPU speed is involved." },
    { label: "They retry the test until it passes", misconception: "That would hide flakiness, not fix it. Fake timers eliminate the source of the race — the dependency on wall-clock time — so the test is deterministic on the first run, not retried." },
    { label: "They increase the test's timeout so it has more time", misconception: "Raising the timeout only masks slowness and still depends on real time, so it can still race. Fake timers remove the real wait entirely by controlling virtual time." },
  ]}
/>

<DragOrder
  id="06-unit-testing-drag-1"
  lessonSlug="01-unit-testing"
  lang="en"
  prompt="Order the steps to write one deterministic unit test for a setTimeout-based retry:"
  items={[
    { id: "a", label: "Enable fake timers: mock.timers.enable({ apis: ['setTimeout'] })" },
    { id: "b", label: "Arrange: build the subject and stub its boundary (e.g. a failing-then-succeeding fetch spy)" },
    { id: "c", label: "Act: call the retry function (it schedules a setTimeout instead of waiting)" },
    { id: "d", label: "Advance virtual time: mock.timers.tick(5000) to fire the scheduled callback" },
    { id: "e", label: "Assert behavior: the spy was called the expected number of times and the result resolved" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="The lifecycle of one test run, from discovery to teardown"
  hue="var(--d-systems)"
  perRow={4}
  nodes={[
    { id: "d", label: "discover", sub: "node --test" },
    { id: "be", label: "beforeEach", sub: "fresh state" },
    { id: "aaa", label: "arrange/act/assert", sub: "the test body" },
    { id: "ae", label: "afterEach", sub: "restore mocks" },
  ]}
  edges={[
    { from: "d", to: "be", label: "per test" },
    { from: "be", to: "aaa" },
    { from: "aaa", to: "ae", label: "pass or fail" },
  ]}
  caption="node --test discovers files, then for each test runs beforeEach to build fresh state, the Arrange-Act-Assert body, and afterEach to tear down — restoring t.mock methods and the real clock so the next test starts clean and independent."
/>

<KeyTakeaway>Use node:test for zero-dependency, fast unit tests; reach for Vitest for rich matchers and HMR watch. Test behavior with Arrange-Act-Assert, keep tests independent, and fake timers so a 5s timeout runs instantly.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-unit-testing-retrieval"
  lang="en"
  questions={[
    { q: "Why does faking timers turn a slow, flaky timeout test into a fast, deterministic one — and how do you do it with node:test?", a: "A test that waits a real setTimeout depends on the wall clock, which makes it slow (a 5s backoff really takes 5s) and flaky: under CI load the timer can fire late and the assertion races ahead of the callback, so it passes most of the time and fails occasionally. Fake timers remove real time from the equation. With node:test you call mock.timers.enable({ apis: ['setTimeout', 'Date'] }) to swap the real clock for a controllable virtual one, run the code under test (which schedules a timer instead of waiting), then call mock.timers.tick(5000) to advance virtual time by 5000ms instantly. That synchronously fires every timer due within that window with no real waiting and no race, so the same assertion is now both instant (microseconds) and 100% deterministic. Vitest does the same with vi.useFakeTimers() and vi.advanceTimersByTime(5000). This is the single highest-leverage technique for the slowest, flakiest category of backend unit test." },
    { q: "When should you choose node:test over Vitest, and vice versa?", a: "Choose node:test when you want zero dependencies and the fastest cold start: it is built into Node (stable since Node 20), speaks the core API (test/describe/it, node:assert/strict, mock.fn, t.mock.method, fake timers, built-in coverage via --experimental-test-coverage), and adds no supply-chain surface — ideal for a published library or a backend with few dependencies. Choose Vitest when you want richer developer ergonomics: Jest-style chainable expect matchers, inline snapshots, vi.mock, a jsdom/happy-dom environment for DOM code, TypeScript with no extra config, and a fast HMR-powered watch that re-runs only affected tests — ideal for apps, frontend or DOM-touching code, and monorepos. Vitest is near-drop-in compatible with Jest (vi.fn↔jest.fn, vi.mock↔jest.mock), so it is also the standard way to escape a slow Jest setup. The decision is dependency weight and matcher/DOM needs, not raw correctness — both are capable runners." },
  ]}
/>

<Recap lang="en">
Node ships a stable, zero-dependency test runner: write tests with `test()` or `describe()`/`it()`, assert with `node:assert/strict` (use the strict import so `equal` means `===`), and run them with `node --test`, which discovers `*.test.js` files and supports `--watch` and `--experimental-test-coverage`; hooks (`before`/`after`/`beforeEach`/`afterEach`) build and tear down state. Assert the failures you expect with `assert.throws` and `await assert.rejects`, and mock at the boundaries only — `mock.fn`, `t.mock.method`, and especially fake timers (`mock.timers.tick`), which turn a real 5-second backoff test into an instant, deterministic one by advancing virtual time instead of waiting on the wall clock. Reach for Vitest when you want Jest-style `expect` matchers, snapshots, a DOM environment, TypeScript with no config, and a fast HMR watch — it is near-drop-in for Jest — and keep node:test as the default for libraries and dependency-light backends. Whichever you pick, the principles are the same: test behavior not implementation so refactors stay green, follow Arrange-Act-Assert with one outcome per test, and keep every test independent of shared mutable state and run order, or the suite goes flaky and gets muted — which is how a real regression slips past the one test that covered it. Now when you see a flaky test — or a test someone added `skip` to — you know the root is almost always one of these three rules broken, and you know how to fix it without touching a line of production code.
</Recap>
