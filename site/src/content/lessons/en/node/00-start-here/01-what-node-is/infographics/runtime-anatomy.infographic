infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label Node.js — a runtime, not a language
    children
      - label V8 — runs the code
        children
          - label Compiles JavaScript to machine code
          - label The same engine Chrome uses
          - label Alone: no files, no sockets
      - label libuv — handles the waiting
        children
          - label Event loop on one JS thread
          - label Non-blocking async I/O
          - label Thread pool — 4 threads by default
      - label Standard library — the interface
        children
          - label fs — files
          - label http / net — servers & sockets
          - label process, crypto, stream
theme
  palette #6366f1 #14b8a6 #f59e0b
