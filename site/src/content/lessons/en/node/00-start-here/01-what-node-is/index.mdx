---
concepts:
- javascript-runtime
- v8-engine
- libuv
- event-loop
- non-blocking-io
- standard-library
deepensInto: []
estMin: 14
lang: en
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-what-node-is
sources:
- https://nodejs.org/en/learn/getting-started/introduction-to-nodejs
- https://nodejs.org/docs/latest/api/
- https://nodejs.org/en/learn/asynchronous-work/event-loop-timers-and-nexttick
spiral: []
status: ready
summary: 'Node.js is JavaScript outside the browser: V8 runs your code, libuv gives it a single-threaded event loop with non-blocking I/O, and a built-in library exposes files, networking, and the process. Great at I/O, poor at heavy CPU.'
title: 'What Node.js actually is'
track: node
unit: 00-start-here
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
You learned JavaScript to make a button change colour in a web page. Then someone hands you `server.js` and says "run it with `node`." No browser, no `<button>`, no `document` — and yet it serves ten thousand API requests on one thread without melting. The same language you used to toggle a CSS class is now reading files off a disk and talking to a database. How? Node.js is not a new language. It is the *runtime* that takes the JavaScript you already know and runs it somewhere the browser never let it go: on a server.
</Hook>

<Crux>Node.js runs JavaScript outside the browser: V8 runs code, libuv adds an event loop and async I/O, the stdlib opens files and sockets.</Crux>

<Explanation>

## A runtime, not a language: V8 + libuv + a standard library

Before you write a single line of server code, it's worth knowing what "Node.js" actually is — because the mental model you form here will explain every quirk you hit later, from missing `window` to why one slow endpoint can freeze your entire API.

When people say "Node.js," they mean three things bolted together into one program.

The first is **V8** — the same engine Chrome uses to execute JavaScript. V8 takes your `.js` source, compiles it to machine code, and runs it fast. V8 alone, though, only *understands* JavaScript; it has no idea how to open a file or accept a network connection. The language spec has no `fs`, no `http`, no sockets.

The second piece fills that gap: **libuv**, a C library that gives Node an **event loop** plus asynchronous **I/O** (input/output — disk, network, timers). libuv is what lets one thread wait on thousands of connections at once instead of blocking on each. It also keeps a small **thread pool** in the background for the few operations the OS can't do asynchronously (some filesystem and crypto work).

The third is **Node's standard library** — the built-in modules that expose all of that to JavaScript: `fs` for files, `http`/`net` for servers and sockets, `path`, `process` for the running program itself, `crypto`, `stream`, and more. This is the part you actually `require` or `import`.

```js
// No browser in sight — this is the standard library at work.
import { readFile } from "node:fs/promises";
import http from "node:http";

const html = await readFile("./index.html", "utf8"); // libuv reads the disk
http.createServer((req, res) => res.end(html))       // V8 runs your callback
    .listen(3000);
console.log(`pid ${process.pid} serving on :3000`);   // process = this program
```

Why does this combination exist at all? Because before Node, JavaScript was trapped in the browser and servers were written in PHP, Python, Ruby, or Java. Node let teams write **one language across the whole stack** — frontend and backend — and tap the enormous **npm** package ecosystem. That single decision is most of why Node took over backend tooling.

## One thread, an event loop, and non-blocking I/O

Here is the idea that makes Node feel strange coming from other backends: **your JavaScript runs on a single thread.** There is no thread-per-request. So how does one thread serve thousands of clients?

The trick is **non-blocking I/O**. When your code asks to read a file or query a database, Node does *not* sit and wait for the answer. It hands the request to libuv (which uses the OS or the thread pool), registers a callback, and immediately moves on to other work. When the data is ready, libuv puts the callback on a queue, and the **event loop** — a loop that runs forever, pulling finished work off queues and executing the callbacks — runs it. The one thread is almost never idle and almost never blocked, so it can interleave thousands of in-flight operations.

The catch is the flip side. If you give that single thread **CPU-bound** work — a tight loop hashing a million passwords, resizing a huge image, parsing a giant JSON synchronously — there is no one else to take requests while it grinds. The event loop is **blocked**. Every other client waits. I/O is offloaded; computation is not.

<FlowDiagram
  label="A request enters Node, its I/O is offloaded to libuv while the single thread keeps serving others, and the event loop runs the callback when the data is ready"
  hue="var(--d-backend)"
  perRow="3"
  nodes={[
    { id: "req", label: "Request", sub: "incoming" },
    { id: "thread", label: "JS thread", sub: "single" },
    { id: "libuv", label: "libuv", sub: "async I/O" },
    { id: "next", label: "Serve others", sub: "no waiting" },
    { id: "loop", label: "Event loop", sub: "queue" },
    { id: "cb", label: "Callback", sub: "respond" },
  ]}
  edges={[
    { from: "req", to: "thread" },
    { from: "thread", to: "libuv", label: "read file / db" },
    { from: "thread", to: "next", label: "don't block" },
    { from: "libuv", to: "loop", label: "data ready" },
    { from: "loop", to: "cb", label: "run" },
  ]}
  caption="I/O is handed off so the one thread stays free. The event loop runs the callback only when libuv signals the data is ready — which is why CPU-heavy work, with no I/O to offload, blocks everyone."
/>

## Where Node shines, and where it doesn't

This architecture is not better or worse than thread-per-request backends — it's *shaped* for a particular kind of work. Knowing the shape tells you when to reach for Node.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Aspect</th>
        <th class="border border-border px-3 py-2 text-left">JavaScript in the browser</th>
        <th class="border border-border px-3 py-2 text-left">JavaScript in Node.js</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Where it runs</td>
        <td class="border border-border px-3 py-2">Inside a web page, in the user's browser tab</td>
        <td class="border border-border px-3 py-2">As a standalone process on a server (or your machine)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Engine</td>
        <td class="border border-border px-3 py-2">V8 (Chrome) / SpiderMonkey / JSC</td>
        <td class="border border-border px-3 py-2">V8 + libuv (event loop, async I/O)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Platform APIs</td>
        <td class="border border-border px-3 py-2">DOM, <code>fetch</code>, <code>localStorage</code>, Web APIs</td>
        <td class="border border-border px-3 py-2"><code>fs</code>, <code>net</code>/<code>http</code>, <code>crypto</code>, <code>child_process</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Global object</td>
        <td class="border border-border px-3 py-2"><code>window</code> / <code>document</code></td>
        <td class="border border-border px-3 py-2"><code>global</code> / <code>process</code> (env, argv, pid)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">No access to</td>
        <td class="border border-border px-3 py-2">The local filesystem, raw sockets</td>
        <td class="border border-border px-3 py-2">The DOM, the page (there is no page)</td>
      </tr>
    </tbody>
  </table>
</div>

Node is **excellent** at I/O-bound work, because that is exactly what non-blocking I/O and the event loop are built for: HTTP APIs and microservices that mostly wait on databases and other services, real-time systems (chat, live dashboards) over WebSockets, and developer tooling (bundlers, linters, CLIs) where reading and writing files dominates. It is **poor** at heavy CPU-bound compute — video transcoding, large numeric simulations, big synchronous crypto — because that work blocks the one thread. The escape hatches are real but deliberate: `worker_threads` to move computation off the main thread, or simply reaching for a language better suited to the number-crunching.

The ecosystem is the other half of the picture. **npm** is Node's package registry and installer — the largest software registry in the world. On top of it sit the frameworks you'll meet next: **Express** and **Fastify** for HTTP servers, **NestJS** for structured, opinionated backends. Treat those names as a map, not today's homework.

<Inset kind="why" lang="en">
"Single-threaded" is slightly imprecise, and it's worth knowing why. Your *JavaScript* runs on one thread, but Node as a process is not. libuv keeps a background thread pool (4 threads by default) for filesystem and crypto operations the OS can't do asynchronously, and you can spin up real `worker_threads` for parallel CPU work. The single-thread model describes the execution of *your* code and the event loop — not the whole runtime. The practical takeaway is unchanged: don't block that one JS thread with synchronous CPU work.
</Inset>

<Quiz
  id="01-what-node-is-quiz-1"
  lessonSlug="01-what-node-is"
  lang="en"
  question="A teammate says 'Node.js is a new programming language for the server.' What's the precise correction?"
  choices={[
    { label: "Node.js is a runtime — it runs the JavaScript language outside the browser, combining V8 (executes JS) with libuv (event loop + async I/O) and a standard library (fs, http, process)", correct: true },
    { label: "Node.js is JavaScript with extra keywords the browser doesn't have", misconception: "The language is the same JavaScript V8 runs in Chrome. Node adds capabilities through built-in modules (fs, http) and a runtime, not through new language syntax." },
    { label: "Node.js is a browser without a screen", misconception: "Node has no DOM, no window, no page at all. It shares V8 with the browser but pairs it with libuv and server-oriented APIs — a different platform, not a headless browser." },
    { label: "Node.js is a database that also runs JavaScript", misconception: "Node is a runtime, not a datastore. It talks to databases via libraries, but stores nothing itself by virtue of being Node." },
  ]}
/>

<Quiz
  id="01-what-node-is-quiz-2"
  lessonSlug="01-what-node-is"
  lang="en"
  question="One Node thread serves thousands of HTTP requests that each query a database. Then you add an endpoint that synchronously resizes huge images. What happens?"
  choices={[
    { label: "The image work is CPU-bound with no I/O to offload, so it blocks the single thread — every other request stalls until it finishes; move it to worker_threads or another service", correct: true },
    { label: "Nothing — Node spawns a new thread per request automatically", misconception: "Node does not use thread-per-request; your JS runs on one thread. I/O is offloaded to libuv, but CPU work has nothing to offload and blocks the event loop." },
    { label: "The image work is fine because libuv runs all callbacks in parallel", misconception: "libuv's thread pool handles async file/crypto ops, not your synchronous JS. A tight CPU loop runs on the main thread and blocks the event loop regardless." },
    { label: "The database queries break because Node can only do one I/O at a time", misconception: "Non-blocking I/O is exactly what lets one thread keep thousands of queries in flight. The problem is the CPU-bound resize, not the I/O." },
  ]}
/>

<TradeoffMatrix
  id="01-what-node-is-tradeoff-1"
  lessonSlug="01-what-node-is"
  lang="en"
  prompt="You're choosing whether Node.js is a good fit for a workload. Which of these is Node's sweet spot — and which fights its model?"
  options={[
    { name: "A real-time chat API: many concurrent WebSocket connections, each mostly idle, occasionally relaying small messages", summary: "I/O-bound, high concurrency.", correct: true, justification: "This is exactly what the event loop and non-blocking I/O are built for. Thousands of mostly-idle connections cost almost nothing because the single thread is never blocked waiting — it just runs a callback whenever a message arrives. High-concurrency, I/O-bound, low-per-request-CPU work is Node's core strength and why it dominates real-time and API workloads." },
    { name: "A batch job that decodes and re-encodes 4K video for hours of solid CPU compute", summary: "CPU-bound, blocking.", justification: "This fights Node's model head-on. Hours of CPU with no I/O to offload pins the single JS thread and starves every other task on the event loop. You'd reach for worker_threads to parallelise, native addons, or simply a language/tool built for the number-crunching. Node can orchestrate the job, but shouldn't grind it on the main thread." },
    { name: "A CLI tool that reads many small files, transforms them, and writes output (a bundler or linter)", summary: "I/O-heavy tooling.", justification: "A great fit and a huge part of why the JS toolchain is written in Node: the work is dominated by reading and writing files, which is I/O the event loop offloads cleanly. The per-file CPU is small. This is why bundlers, linters, and test runners ship as Node programs." },
    { name: "An HTTP API that mostly forwards requests to a database and other services", summary: "I/O-bound API.", justification: "Squarely in Node's wheelhouse: the request handler spends almost all its time waiting on the database and downstream services, which is offloaded I/O. One thread interleaves thousands of in-flight requests. This is the canonical Node backend shape — as long as no handler does heavy synchronous CPU work." },
  ]}
/>

</Explanation>

<KeyTakeaway>Node.js = V8 (runs JS) + libuv (event loop + async I/O) + stdlib (fs, http, process). One thread serves many connections by offloading I/O; heavy CPU work blocks all. Best for I/O-bound APIs, real-time, and tooling.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-what-node-is-retrieval"
  lang="en"
  questions={[
    { q: "Name the three parts that make up Node.js and what each one does.", a: "Node.js is three things bolted into one program. First, V8 — the same engine Chrome uses — which compiles and executes your JavaScript fast but, on its own, only understands the language: it has no notion of files or sockets. Second, libuv — a C library that supplies the event loop plus asynchronous I/O (disk, network, timers) and a small background thread pool for operations the OS can't do asynchronously. libuv is what lets one thread wait on thousands of connections at once. Third, Node's standard library — the built-in modules (fs, http/net, path, process, crypto, stream, …) that expose all of that to JavaScript; this is the part you actually import or require. Put differently: V8 runs the code, libuv handles the waiting, and the standard library is the interface to the machine." },
    { q: "Node runs your JS on one thread. Explain how it still serves thousands of clients, and the one kind of work that breaks this.", a: "It works through non-blocking I/O plus the event loop. When your code asks to read a file or query a database, Node doesn't sit and wait — it hands the request to libuv (which uses the OS or the thread pool), registers a callback, and immediately moves on to other work. When the data is ready, libuv queues the callback and the event loop, which runs forever pulling finished work off queues, executes it. Because the thread is almost never blocked, it interleaves thousands of in-flight operations on one thread. What breaks this is CPU-bound work: a synchronous tight loop (hashing, image resizing, huge JSON parsing) has no I/O to offload, so it pins the single thread and every other client waits — the event loop is blocked. The fix is to move computation off the main thread with worker_threads, or use a tool/language better suited to heavy compute. I/O is offloaded; computation is not." },
  ]}
/>

<Recap lang="en">
Node.js is not a new language — it is a runtime that runs the JavaScript you already know outside the browser. It bolts three things together: V8, which executes your JS; libuv, which provides the event loop, asynchronous I/O, and a small background thread pool; and a standard library (fs, http, net, path, process, crypto) that exposes the filesystem, networking, and the process to your code. It exists because it let teams write one language across the whole stack and tap the vast npm ecosystem. Its defining model is a single JavaScript thread driven by an event loop with non-blocking I/O: a request's slow waiting is offloaded to libuv so the one thread stays free to serve thousands of others, running each callback when its data is ready. That makes Node excellent at I/O-bound APIs, real-time systems, and developer tooling — and poor at heavy CPU compute, which blocks the thread and demands worker_threads or another language. Unlike browser JS there is no DOM or window; instead you get global, process, and the standard library. Now when you see an API that mysteriously slows to a crawl under load — even though it "only does database reads" — you'll have the vocabulary to ask the right question: is something blocking the event loop?
</Recap>
