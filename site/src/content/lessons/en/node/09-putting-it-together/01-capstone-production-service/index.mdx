---
concepts:
- error-boundary
- graceful-shutdown
- event-loop-discipline
- input-validation
- twelve-factor-config
- observability
deepensInto: []
estMin: 20
lang: en
lessonType: topic
level: senior
order: 1
prereqs: []
slug: 01-capstone-production-service
sources:
- https://12factor.net/
- https://github.com/goldbergyoni/nodebestpractices
- https://nodejs.org/en/learn/getting-started/nodejs-the-difference-between-development-and-production
spiral: []
status: ready
summary: 'A production Node service is the whole track composed at once: typed errors
  behind one boundary, an unblocked loop, an ordered middleware stack with timeouts
  and graceful shutdown, validated input, env config, and observability. Each skipped
  concern is a named incident.'
title: 'Capstone: designing and hardening a production Node service'
track: node
unit: 09-putting-it-together
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import skippedLayerIncident from "./infographics/skipped-layer-incident.svg?raw";

<Hook>
The prototype worked on the laptop, so it shipped. The first deploy under load wrote `200 OK` to half the in-flight checkout requests and then dropped them — the pod got `SIGTERM`, the container ran the app as PID 1 with no signal handler, and Kubernetes hard-killed it after 30 seconds mid-transaction. The next incident was tail latency: one endpoint did `JSON.parse` on a 4 MB body inside the request path and blocked the event loop, so every other request queued behind it and p99 went from 40 ms to 9 seconds. Then a crafted query string reached an unparameterized SQL string. None of these were exotic. Each was one unit of this track that the prototype had skipped, and each became a production incident the moment real traffic arrived.
</Hook>

<Crux>Production-ready = the whole track composed: one error boundary, an unblocked loop, ordered middleware, graceful shutdown.</Crux>

<Explanation>

## The checklist is the architecture

By the end of this lesson you will be able to trace a single request through a fully-hardened service and name the incident each skipped layer produces — so the next time a pull request cuts a corner, you'll recognise it before it reaches production.

Going from prototype to production is not a rewrite — it is applying every unit of this track to one small HTTP service and refusing to skip any of them. The useful lens is a request-lifecycle checklist: for each cross-cutting concern, name *what you do* and the concrete signal for *done when*. Skip a row and you don't get a smaller service; you get a future incident with a name. The `12factor` manifesto and the Node best-practices repo are both, underneath, exactly this list — config in the environment, dependencies declared and locked, processes disposable, logs as event streams.

<Infographic
  svg={skippedLayerIncident}
  label="A list pairing each cross-cutting concern omitted with its named production incident: no error boundary leaks stack traces, a blocked event loop pushes p99 from 40 ms to 9 s, no graceful shutdown drops in-flight requests on every deploy, unvalidated input reaches an unparameterized SQL string, and config baked into the image blocks promotion and leaks secrets."
  caption="Skip a row and you don't get a smaller service — you book a specific outage with a name."
  hue="var(--d-infra)"
/>

The senior move is to treat these as composing layers, not a feature backlog. Errors, the event loop, the middleware order, shutdown, validation, config, and observability are not independent checkboxes — they interlock. A graceful shutdown is worthless if the load balancer never learns you're draining (that's the readiness probe). An error boundary is worthless if a blocked loop means the boundary never runs. Below is the readiness checklist that this whole lesson defends.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Concern (unit)</th>
        <th class="border border-border px-3 py-2 text-left">What you do</th>
        <th class="border border-border px-3 py-2 text-left">Done when</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Errors (03)</td>
        <td class="border border-border px-3 py-2">Typed domain errors; one boundary maps type→status; process net on uncaught</td>
        <td class="border border-border px-3 py-2">Clients see safe JSON + codes; logs hold full stacks; a surprise crashes, not corrupts</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Event loop (04)</td>
        <td class="border border-border px-3 py-2">Never block; offload CPU to a worker pool; watch loop lag; scale across cores</td>
        <td class="border border-border px-3 py-2">p99 stays flat under load; loop lag &lt; ~50 ms; a profile is one command away</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">HTTP (05)</td>
        <td class="border border-border px-3 py-2">Ordered middleware; request + header + body timeouts; graceful <code>SIGTERM</code></td>
        <td class="border border-border px-3 py-2">No request runs unbounded; a deploy drains in-flight work with zero dropped requests</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Testing (06)</td>
        <td class="border border-border px-3 py-2">Unit tests for logic; integration tests (supertest + real test DB) for the wired API</td>
        <td class="border border-border px-3 py-2">CI gates merges on a green suite that exercises the real routes, not mocks</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Security (07)</td>
        <td class="border border-border px-3 py-2">Validate input at the boundary; parameterized queries; secrets from env; <code>npm ci</code> + audit</td>
        <td class="border border-border px-3 py-2">No unvalidated input reaches logic; no secret in git; lockfile-pinned, audited deps</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Packaging (08)</td>
        <td class="border border-border px-3 py-2">Multi-stage slim/distroless image, non-root, exec-form <code>CMD</code> + init, healthcheck</td>
        <td class="border border-border px-3 py-2">Small image runs as non-root and forwards signals; healthcheck reflects readiness</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Config &amp; ops (12-factor)</td>
        <td class="border border-border px-3 py-2">Config via env; liveness + readiness endpoints; metrics endpoint; SLOs</td>
        <td class="border border-border px-3 py-2">Same image runs every env via config; probes + metrics + traces exist; SLOs tracked</td>
      </tr>
    </tbody>
  </table>
</div>

## The request lifecycle, hardened end to end

Trace one request and you can see every unit doing its job. The ingress or load balancer routes to a process whose HTTP server already has timeouts set — `server.requestTimeout`, `headersTimeout`, and a per-route handler timeout — so no single client can pin a connection open forever (unit 05). The request enters an *ordered* middleware stack, and order is load-bearing: logging first (so you log even rejected requests), then a body parser with a size cap (so a 4 MB body can't blow up memory or block parse), then auth, then schema validation, then the handler. The handler calls a thin service layer, which calls a repository that issues **parameterized** queries — never string concatenation — and a domain failure becomes a typed error (unit 07, unit 03).

Threaded through all of it is request context. A `pino` structured logger carries a request/trace ID through `AsyncLocalStorage`, so every log line from deep in the stack is correlated to its request without passing the ID by hand. `diagnostics_channel` lets you publish instrumentation points (DB query start/end, cache hit) that exporters subscribe to without coupling. At the very end sits a single **error boundary**: error-handling middleware that maps error *type* to HTTP status (`ValidationError`→400, `NotFoundError`→404, anything unknown→500), sends a safe JSON body with no stack, and logs the full error object with its `cause` chain. That is unit 03's strategy expressed as exactly one place in the wiring.

<TradeoffMatrix
  id="09-capstone-tradeoff-1"
  lessonSlug="01-capstone-production-service"
  lang="en"
  prompt="A CPU-bound step (image resize, bcrypt at high cost, big JSON transform) sits inside a hot request handler and is spiking p99 under load. Where do you put it?"
  options={[
    { name: "Offload to a worker_threads pool; the handler awaits a message back", summary: "Free the loop.", correct: true, justification: "This is the senior fix. The CPU work runs off the main thread, so the event loop stays free to accept and dispatch other requests; the handler just awaits a result message. p99 stops collapsing because one expensive call no longer starves every concurrent request. A bounded pool also caps how much CPU the box spends on this, giving you backpressure instead of a thundering herd of threads." },
    { name: "Leave it inline but wrap it in a Promise / setImmediate", summary: "Still blocks.", justification: "A Promise or setImmediate only reschedules WHEN the work starts — it does not make synchronous CPU work yield. Once the bcrypt/resize runs, it holds the single thread until it finishes, blocking the loop exactly as before. Wrapping in a microtask hides the problem in a profiler without fixing tail latency." },
    { name: "Scale to more pods/replicas and keep it inline", summary: "Masks, costly.", justification: "More replicas raise total throughput but each instance still blocks its own loop on every such request, so p99 within an instance stays bad and you pay for capacity to paper over a per-request defect. Horizontal scale is for real load, not for hiding a blocking call — fix the block first, then scale the fixed service." },
    { name: "Cache the result and hope for hits", summary: "Partial, fragile.", justification: "Caching helps only for repeated identical inputs and does nothing for the cold path or unique inputs, which still block the loop. It's a worthwhile optimization on top of a worker pool, but as the primary fix it leaves every cache miss spiking p99 — you've reduced frequency, not removed the hazard." },
  ]}
/>

## Bootstrap and shutdown: the wiring that decides uptime

Two pieces of plumbing decide whether deploys are invisible or destructive: the bootstrap that installs the safety net before anything serves, and the shutdown that drains before exiting. When you look at a new service's `index.js` or `server.ts` entry point, ask yourself: does it install the process net *before* `listen`, and does it handle `SIGTERM` *before* the first request can arrive? On `SIGTERM` you do not slam the server shut — you flip readiness to *not ready* (so the load balancer stops sending new requests), stop accepting connections, let in-flight requests finish, close the DB pool, and arm a hard timeout so a stuck request can't block shutdown forever. Skipping this is the dropped-checkout incident from the hook.

```js
import { createServer } from "node:http";
import { app } from "./app.js";          // ordered middleware + error boundary
import { logger } from "./logger.js";
import { closePool } from "./db.js";

let ready = false;
const server = createServer(app);
server.requestTimeout = 30_000;          // unit 05: bound every request
server.headersTimeout = 10_000;

// unit 03: process net — record, then die; a supervisor restarts clean
process.on("uncaughtException", (err) => { logger.fatal({ err }); shutdown(1); });
process.on("unhandledRejection", (err) => { logger.fatal({ err }); shutdown(1); });

async function shutdown(code = 0) {
  ready = false;                         // readiness flips → LB drains us
  server.close(async () => {             // stop accepting; finish in-flight
    await closePool();
    process.exit(code);
  });
  setTimeout(() => process.exit(code), 10_000).unref(); // hard cap
}
process.on("SIGTERM", () => shutdown(0));

server.listen(3000, () => { ready = true; logger.info("listening"); });
// liveness = process is up; readiness = `ready` flag (12-factor disposability)
```

<Inset kind="why" lang="en">
Why does the readiness flip have to happen *before* `server.close`, not after? Because the load balancer learns you're leaving only by polling the readiness endpoint. If you close the server first, new requests that the LB hasn't yet stopped routing get a connection error instead of a clean drain. Flip readiness, give the probe interval a moment to notice, *then* close — that ordering is the difference between a zero-downtime deploy and a burst of 502s on every rollout.
</Inset>

<Quiz
  id="09-capstone-quiz-1"
  lessonSlug="01-capstone-production-service"
  lang="en"
  question="A rolling deploy drops a handful of in-flight requests with connection-reset errors on every rollout. Which skipped concern is the cause?"
  choices={[
    { label: "No graceful shutdown: the process exits on SIGTERM without flipping readiness, draining in-flight requests, and closing the server — so the orchestrator hard-kills it mid-request", correct: true },
    { label: "Missing input validation at the boundary", misconception: "Validation governs whether bad input reaches your logic; it has nothing to do with requests dropped during a deploy. The reset-on-rollout signature points squarely at shutdown handling, not the request body." },
    { label: "The event loop is blocked by CPU work", misconception: "A blocked loop shows up as tail-latency spikes under load, not as resets that appear only at rollout time. The timing — every deploy — is the tell that this is a shutdown problem." },
    { label: "Logs aren't structured, so you can't see the errors", misconception: "Unstructured logs make the incident harder to read, but they don't cause dropped requests. The requests are dropped because nothing drained them before the process died." },
  ]}
/>

<Quiz
  id="09-capstone-quiz-2"
  lessonSlug="01-capstone-production-service"
  lang="en"
  question="You want every log line from deep in the call stack to carry the same request/trace ID without threading it through every function signature. What do you reach for?"
  choices={[
    { label: "AsyncLocalStorage: set the trace ID once per request in early middleware and read it from the logger anywhere, since it persists across awaits within that request's async context", correct: true },
    { label: "A module-level global variable holding the current request ID", misconception: "A single global is shared across all concurrent requests, so under any real concurrency request B overwrites request A's ID and your correlation is wrong. AsyncLocalStorage exists precisely because a global can't isolate per-request context across awaits." },
    { label: "Pass the ID as an argument to every function in the chain", misconception: "This works but is exactly the manual threading you're trying to avoid; it pollutes every signature and breaks the moment one library call in the middle drops the argument. AsyncLocalStorage carries it implicitly." },
    { label: "Store it on the process object", misconception: "process is a single global object shared by all requests, so it has the same cross-request collision problem as a module global. Per-request context needs async-context isolation, which is what AsyncLocalStorage provides." },
  ]}
/>

<DragOrder
  id="09-capstone-drag-1"
  lessonSlug="01-capstone-production-service"
  lang="en"
  prompt="Order the middleware/handler pipeline for one inbound request, from edge to data and back through the boundary:"
  items={[
    { id: "a", label: "Logging + request-ID assignment (so even rejected requests are recorded)" },
    { id: "b", label: "Body parser with a size cap (reject oversized payloads early)" },
    { id: "c", label: "Authentication / authorization" },
    { id: "d", label: "Schema validation of the parsed input" },
    { id: "e", label: "Route handler → service → repository (parameterized query)" },
    { id: "f", label: "Error boundary maps any thrown error type → status + safe body" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f"]}
/>

</Explanation>

<FlowDiagram
  label="One hardened request flowing through the production service, edge to data"
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "lb", label: "LB / ingress", sub: "TLS, route" },
    { id: "srv", label: "HTTP server", sub: "timeouts set" },
    { id: "mw", label: "middleware", sub: "log→auth→validate" },
    { id: "h", label: "handler", sub: "thin" },
    { id: "svc", label: "service + repo", sub: "param query" },
    { id: "db", label: "database", sub: "data" },
  ]}
  edges={[
    { from: "lb", to: "srv" },
    { from: "srv", to: "mw", label: "ordered" },
    { from: "mw", to: "h", label: "validated" },
    { from: "h", to: "svc" },
    { from: "svc", to: "db", label: "parameterized" },
  ]}
  caption="Cross-cutting and invisible in the line above: an AsyncLocalStorage trace context tagging every log, a single error boundary mapping thrown types to status codes, and a metrics/diagnostics_channel feed — plus a SIGTERM path that flips readiness and drains this exact pipeline before exit."
/>

<KeyTakeaway>Production-ready composes the whole track: typed errors at one boundary, an unblocked loop, ordered middleware, timeouts, graceful shutdown, validated input, env config, observability. Each skipped row is an outage.</KeyTakeaway>


<RetrievalDrawer
  client:load
  id="09-capstone-retrieval"
  lang="en"
  questions={[
    { q: "Walk the SIGTERM shutdown sequence and explain why the order matters for a zero-downtime deploy.", a: "On SIGTERM: (1) flip the readiness flag to not-ready so the readiness probe fails and the load balancer stops routing NEW requests to this instance; (2) wait a probe interval so the LB actually notices; (3) call server.close() to stop accepting connections while letting in-flight requests finish; (4) close the DB pool and other resources; (5) arm an unref'd hard-timeout (e.g. 10 s) so a stuck request can't block shutdown forever, then process.exit. Order is load-bearing: if you close the server before flipping readiness, requests the LB hasn't yet stopped sending hit a closed socket and get connection resets — exactly the dropped-checkout incident. Flipping readiness first lets the LB drain you cleanly; closing first turns every rollout into a burst of 502s. Liveness says 'process is up' (don't fail it during drain or you get killed early); readiness says 'send me traffic', which is what you toggle." },
    { q: "Each row of the readiness checklist maps to a specific production incident if skipped. Name the incident for: no error boundary, blocked event loop, unvalidated input, and a root container that ignores SIGTERM.", a: "No error boundary → unmapped errors leak raw stack traces and DB internals to clients (info disclosure) and unexpected throws crash the request or process instead of becoming a clean 500. Blocked event loop → tail-latency collapse: one synchronous CPU call (parse, resize, bcrypt) on the single thread queues every concurrent request behind it, so p99 jumps from tens of ms to seconds even at modest load. Unvalidated input → injection and corruption: a crafted body or query string reaches logic and an unparameterized query, enabling SQL injection or invalid state. Root container ignoring SIGTERM → hard kills: PID 1 with no signal handler (shell-form CMD, no init) never receives or forwards SIGTERM, so the orchestrator SIGKILLs after the grace period, dropping in-flight work on every deploy. The pattern is the lesson: each concern you skip is not 'less code', it's a pre-booked outage." },
  ]}
/>

<Recap lang="en">
A production Node service is this entire track composed into one small HTTP service, defended by a readiness checklist where every row maps to a concrete incident if skipped. Errors (unit 03): typed domain errors propagate with `{ cause }`, a single error boundary maps type→status and logs the full object, and a process net on `uncaughtException`/`unhandledRejection` records then exits for a supervisor to restart clean. The event loop (unit 04) stays free — CPU work is offloaded to a worker pool, loop lag is monitored, and a CPU profile or heap snapshot is one command away when latency degrades. HTTP (unit 05) is a framework with an *ordered* middleware stack — logging → body-parser-with-size-cap → auth → validation → handler → error boundary — with request, header, and body timeouts and a graceful `SIGTERM` path that flips readiness, drains, closes, and hard-caps. Testing (unit 06) gates CI on unit tests plus integration tests against a real test DB. Security (unit 07) validates all input at the boundary, uses parameterized queries, loads secrets from the environment, and runs `npm ci` + audit with a pinned lockfile. Packaging (unit 08) ships a multi-stage slim/distroless image running non-root with an exec-form `CMD` + init so signals reach the process. And config/ops follows `12factor`: config via env, liveness and readiness probes, a metrics endpoint, logs/metrics/traces, and SLOs. The whole point: production-readiness is not extra features bolted on later — it is these layers interlocking. Now when you review a service and spot a missing `SIGTERM` handler, an inline bcrypt call, or a body parser with no size cap, you know exactly which incident that corner-cut has already booked.
</Recap>
