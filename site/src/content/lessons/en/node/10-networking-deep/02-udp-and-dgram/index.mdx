---
concepts:
- dgram-socket
- datagram-vs-stream
- no-delivery-guarantee
- message-size-mtu
- broadcast-multicast
- when-udp-wins
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-udp-and-dgram
sources:
- https://nodejs.org/api/dgram.html
- https://en.wikipedia.org/wiki/User_Datagram_Protocol
- https://github.com/statsd/statsd/blob/master/docs/metric_types.md
spiral: []
status: ready
summary: 'UDP via dgram sends independent datagrams with no connection, no ordering,
  and no delivery guarantee. Each send is one packet bounded by the MTU; you trade
  reliability for latency, and it wins for metrics, discovery, games, and DNS.'
title: 'UDP and dgram: datagrams, message boundaries, and when to drop reliability'
track: node
unit: 10-networking-deep
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import mtuCliff from "./infographics/mtu-cliff.svg?raw";

<Hook>
A team moves their app metrics from synchronous HTTP POSTs to a StatsD agent over UDP, and p99 request latency drops by 30 ms overnight. The reason is brutal and simple: the HTTP metric calls were waiting for a TCP handshake, a reliable delivery, and a response — on the hot path of every request. The UDP `send()` does none of that. It hands one datagram to the kernel and returns; if the packet is lost, the metric is lost, and that is *fine*. They consciously traded a tiny fraction of metric accuracy for never blocking a user request on telemetry again.
</Hook>

<Crux>UDP via `dgram` sends independent, unordered, best-effort datagrams — one `send()` is one packet — trading reliability for latency.</Crux>

<Explanation>

## Datagrams are messages, not a stream

TCP is a reliable, ordered byte stream; UDP is the opposite design point. The `dgram` module gives you a **connectionless** socket that sends and receives discrete **datagrams** — self-contained packets, each routed independently. There is no handshake, no connection state, no ordering, and no acknowledgement. A datagram either arrives whole or not at all.

This flips the framing problem from `net` on its head. In `net` you had to *add* message boundaries because TCP erases them; in `dgram` the boundaries are **preserved for free** — one `send()` produces exactly one `'message'` event on the receiver with exactly those bytes. You never get a partial datagram and you never get two coalesced.

```js
const dgram = require("node:dgram");

const server = dgram.createSocket("udp4");
server.on("message", (msg, rinfo) => {
  // msg is one whole datagram; rinfo = { address, port, size }
  console.log(`${rinfo.address}:${rinfo.port} -> ${msg.length} bytes`);
});
server.bind(8125);

const client = dgram.createSocket("udp4");
client.send(Buffer.from("requests:1|c"), 8125, "127.0.0.1");  // fire and forget
```

Note what is missing: no `connect`, no `accept`, no per-client socket object. The server `bind`s a port and receives from anyone; `rinfo` tells you who sent each datagram. `send()` is fire-and-forget — its callback fires when the datagram was handed to the OS, **not** when it was delivered, because UDP has no concept of delivery confirmation.

## No delivery, no order, no dedup — by design

Before you reach for UDP, ask yourself: does your protocol actually *need* every one of TCP's guarantees, or are you paying for reliability you will never use? The table below makes the tradeoff concrete.

UDP gives you **none** of TCP's guarantees, and that is the whole point.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Property</th>
        <th class="border border-border px-3 py-2 text-left">TCP (net)</th>
        <th class="border border-border px-3 py-2 text-left">UDP (dgram)</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Delivery</td>
        <td class="border border-border px-3 py-2">Guaranteed (retransmit)</td>
        <td class="border border-border px-3 py-2">Best-effort — may be dropped</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Ordering</td>
        <td class="border border-border px-3 py-2">In-order</td>
        <td class="border border-border px-3 py-2">Any order</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Duplicates</td>
        <td class="border border-border px-3 py-2">Removed</td>
        <td class="border border-border px-3 py-2">Possible</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Boundaries</td>
        <td class="border border-border px-3 py-2">Erased (you frame)</td>
        <td class="border border-border px-3 py-2">Preserved (1 send = 1 message)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Connection</td>
        <td class="border border-border px-3 py-2">Stateful handshake</td>
        <td class="border border-border px-3 py-2">Connectionless</td>
      </tr>
    </tbody>
  </table>
</div>

If your application needs reliability or ordering on top of UDP, *you* implement it (sequence numbers, ACKs, retransmit) — which is exactly what QUIC and reliable-UDP game protocols do. The reason to start from UDP and add only what you need is latency: there is no handshake round trip, no head-of-line blocking (a lost TCP segment stalls everything behind it; a lost datagram affects only itself), and no connection state to manage at scale.

## Message size and the MTU cliff

A datagram is bounded. The theoretical IPv4 UDP payload max is 65,507 bytes, but that is not the number that matters in production. The number that matters is the **path MTU** — typically ~1,500 bytes on Ethernet, often ~1,400 after tunnels/VPNs. A datagram larger than the path MTU must be **IP-fragmented** into multiple packets, and here is the cliff: if *any one* fragment is lost, the **entire datagram is discarded** — IP cannot reassemble a partial datagram. So a 4 KB datagram across a lossy path has a much higher effective loss rate than a 1 KB one, because it is three fragments that must all survive.

<Infographic
  svg={mtuCliff}
  label="Column chart of IP fragments per UDP datagram at a ~1,500 B path MTU: a 1.2 KB datagram is 1 fragment, 4 KB is 3 fragments, 8 KB is 6, and 16 KB is 11 — every fragment is another packet that must survive or the whole datagram is discarded."
  caption="Above the path MTU, each datagram becomes N fragments that ALL must arrive — so effective loss compounds with size. Staying under ~1,200 B keeps every datagram a single atomic packet."
  hue="var(--d-network)"
/>

The practical rule: keep datagrams under the path MTU — a safe portable target is **~1,200 bytes** of payload (the QUIC/DNS guidance). If you need to send more, fragment at the application layer into MTU-sized datagrams with your own sequencing, or use TCP. `socket.send()` does not split for you against your intent — it hands the whole buffer to the OS, which fragments at the IP layer with the fragility above.

<Inset kind="why" lang="en">
Why does one lost fragment kill the whole datagram? IP fragmentation splits a datagram across packets but only the first fragment carries the UDP header, and reassembly needs *every* fragment to rebuild the original. There is no per-fragment retransmit in UDP, so a single drop means the receiver holds incomplete pieces that time out and are thrown away. Staying under the MTU makes every datagram a single packet — atomic, no reassembly, no amplified loss.
</Inset>

## Broadcast and multicast: one send, many receivers

UDP can address **groups**, which TCP fundamentally cannot. **Broadcast** sends one datagram to every host on the local subnet (`socket.setBroadcast(true)`, send to the subnet broadcast address) — useful for "is anyone out there?" discovery on a LAN. **Multicast** is the disciplined version: receivers `addMembership(groupAddr)` to join a multicast group (224.0.0.0/4), and a single `send()` to the group address reaches exactly the joined members, with the network duplicating the packet only where paths diverge. `setMulticastTTL` bounds how many hops it travels. This is how service discovery (mDNS/Bonjour), some clustered-cache gossip, and IPTV-style fan-out work — one send, N receivers, no per-receiver connection.

<FlowDiagram
  label="A metrics agent fan-out: one app process sends UDP datagrams to a StatsD collector, while service discovery uses a multicast group to reach many nodes with one send"
  hue="var(--d-network)"
  perRow={3}
  nodes={[
    { id: "app", label: "App process", sub: "send()" },
    { id: "kernel", label: "Kernel UDP", sub: "no ACK" },
    { id: "statsd", label: "StatsD", sub: "':message'" },
    { id: "disco", label: "Discovery", sub: "send()" },
    { id: "group", label: "Multicast grp", sub: "224.x" },
    { id: "nodes", label: "N nodes", sub: "joined" }
  ]}
  edges={[
    { from: "app", to: "kernel", label: "1 datagram" },
    { from: "kernel", to: "statsd", label: "best-effort" },
    { from: "disco", to: "group", label: "1 send" },
    { from: "group", to: "nodes", label: "fan-out" }
  ]}
  caption="Unicast telemetry (top) is fire-and-forget: a lost datagram just loses one metric. Multicast (bottom) reaches every joined member with a single send — impossible over TCP."
/>

<TradeoffMatrix
  id="10-udp-and-dgram-tradeoff-1"
  lessonSlug="02-udp-and-dgram"
  lang="en"
  prompt="You are choosing a transport for high-frequency application metrics on the request hot path."
  options={[
    { name: "UDP datagrams to a local StatsD agent (fire-and-forget)", summary: "Latency over accuracy.", correct: true, justification: "Metrics are high-volume and individually disposable: losing one counter increment among thousands does not change the aggregate. UDP's fire-and-forget send hands one datagram to the kernel and returns immediately, never blocking the request on a handshake, delivery, or response. The occasional dropped packet is an acceptable, well-understood trade — this is exactly why StatsD uses UDP by default." },
    { name: "Synchronous HTTP POST per metric over TCP", summary: "Reliable but blocking.", justification: "TCP gives you guaranteed delivery you do not need for metrics, and pays for it with a handshake, reliable delivery, and a response on the hot path of every request — adding tens of milliseconds of tail latency. You are buying reliability for data that is fine to lose, at the cost of the user-facing latency you most want to protect." },
    { name: "Buffer metrics in memory and flush over TCP every 10 s", summary: "Batch over TCP.", justification: "Batching is a reasonable middle ground and is what many agents do internally, but rolling your own in-process buffer risks losing the whole batch on a crash and adds complexity. For the common case, a local UDP agent that itself batches upstream gives you non-blocking sends without that buffer-on-crash exposure." },
    { name: "Write metrics to a log file and tail it", summary: "Log and scrape.", justification: "Logging works for low-frequency events but turns high-frequency metrics into disk I/O and a parsing pipeline, with its own latency and back-pressure on the hot path. It is a different tool for a different job, not a transport for live counters." },
  ]}
/>

<Quiz
  id="10-udp-and-dgram-quiz-1"
  lessonSlug="02-udp-and-dgram"
  lang="en"
  question="You send a single 4 KB UDP datagram across the public internet and it frequently fails to arrive intact, while 1 KB datagrams mostly succeed. Why?"
  choices={[
    { label: "The 4 KB datagram exceeds the path MTU (~1,500 B), so IP fragments it into multiple packets; if any one fragment is lost the whole datagram is discarded — keep payloads under ~1,200 B", correct: true },
    { label: "UDP has a hard 1 KB datagram limit", misconception: "UDP's max payload is 65,507 bytes, not 1 KB. The practical limit is the path MTU; below it a datagram is one packet, above it fragmentation amplifies loss." },
    { label: "The kernel rate-limits large datagrams", misconception: "There is no size-based kernel rate limit at play. The fragility comes from IP fragmentation: one lost fragment kills the reassembly of the whole datagram." },
    { label: "TCP would have the same problem", misconception: "TCP segments to the MSS and retransmits lost segments, so it does not suffer the all-or-nothing fragment loss. This fragility is specific to large UDP datagrams." },
  ]}
/>

<Quiz
  id="10-udp-and-dgram-quiz-2"
  lessonSlug="02-udp-and-dgram"
  lang="en"
  question="In dgram, when does socket.send(buf, port, host, cb) invoke cb?"
  choices={[
    { label: "When the datagram has been handed to the OS for sending — not when it was delivered, because UDP has no delivery confirmation", correct: true },
    { label: "When the receiver acknowledges the datagram", misconception: "UDP has no acknowledgements. There is no signal of delivery to wait for; the callback only confirms the local hand-off to the kernel." },
    { label: "When the receiver's 'message' handler returns", misconception: "The sender has no visibility into the receiver's processing — there is no connection or response channel in plain UDP. The callback is purely local." },
    { label: "After a default 200 ms send timeout", misconception: "There is no send timeout in UDP. send() completes as soon as the OS accepts the datagram into its send path." },
  ]}
/>

</Explanation>

<KeyTakeaway>`dgram` sends connectionless, best-effort datagrams with preserved boundaries: 1 `send()` = 1 `'message'`, no delivery/order/dedup guarantees. Keep payloads under ~1,200 B to avoid fragment-loss, and reach for UDP when latency beats reliability — metrics, discovery, games, DNS.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="10-udp-and-dgram-retrieval"
  lang="en"
  questions={[
    { q: "Why does a UDP datagram larger than the path MTU lose so much more often than a small one?", a: "A datagram bigger than the path MTU (commonly ~1,500 bytes on Ethernet, less after tunnels) cannot fit in one IP packet, so IP fragments it into several packets. Reassembly is all-or-nothing: the receiver needs every fragment to rebuild the original datagram, only the first fragment carries the UDP header, and UDP has no per-fragment retransmit. So if any single fragment is dropped — and across a lossy path each extra packet is another chance to drop — the receiver holds incomplete pieces that eventually time out and are discarded, losing the entire datagram. A 4 KB datagram is three fragments that must all survive, giving it a far higher effective loss rate than a 1 KB single-packet datagram. The fix is to keep payloads under the MTU — a safe portable target is ~1,200 bytes — so every datagram is one atomic packet with no reassembly and no amplified loss; if you need more, fragment and sequence at the application layer or use TCP." },
    { q: "When is UDP the right choice over TCP, and what do you give up?", a: "UDP wins when low latency and the absence of head-of-line blocking matter more than guaranteed, ordered delivery. The classic fits are: application metrics/telemetry (StatsD), where individual data points are disposable and you must not block the request hot path; service discovery (mDNS/Bonjour) and gossip, where multicast lets one send reach many receivers with no per-peer connection; real-time multiplayer games and voice/video, where a late packet is worse than a lost one (you want the newest state, not a retransmit of stale state); and DNS, where a single small request/response is far cheaper without a handshake. What you give up is everything TCP provides for free: delivery is best-effort (packets can be lost), there is no ordering (datagrams can arrive out of order), duplicates are possible, and there is no connection or flow control. If your protocol needs any of those, you implement them yourself on top — sequence numbers, ACKs, retransmit — which is precisely what QUIC and reliable-UDP game netcode do, starting from UDP and adding back only the guarantees they actually need." },
  ]}
/>

<Recap lang="en">
UDP, exposed through Node's `dgram`, is the connectionless counterpart to `net`'s reliable stream. You create a socket, `bind` a port, and receive whole datagrams as `'message'` events with an `rinfo` describing the sender — no handshake, no per-client socket, no ordering, and no delivery guarantee. The framing problem inverts: where TCP erases message boundaries, UDP preserves them, so one `send()` is exactly one `'message'`, and `send()`'s callback fires on hand-off to the OS, not on delivery, because there is none to confirm. Datagrams are bounded by the path MTU, and a datagram larger than it is IP-fragmented into packets where a single lost fragment discards the whole thing — so keep payloads under ~1,200 bytes or fragment and sequence yourself. UDP's unique power is group addressing: broadcast to a subnet and multicast to joined members let one send reach many receivers, which TCP cannot do. Choose UDP when latency and no head-of-line blocking beat reliability — metrics, discovery, games, voice, DNS — and accept that any reliability you need, you build on top. Now when you see a team reaching for TCP on a high-frequency metrics or discovery path, you know the question to ask: does that protocol actually need delivery guarantees, or is it just paying for them by default?
</Recap>
