infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label Node stream types
    children
      - label Readable — a source you pull from
        children
          - label Examples: file read, HTTP request body, socket
          - label Reads in flowing or paused mode
      - label Writable — a sink you push to
        children
          - label Examples: file write, HTTP response, socket
          - label write() returns false past highWaterMark
      - label Duplex — both sides, read and write independent
        children
          - label Example: a TCP socket
          - label One buffer each direction
      - label Transform — a Duplex that maps input to output
        children
          - label Examples: gzip, cipher, CSV parser
          - label Streaming map: in one side, modified out the other
theme
  palette #6366f1 #14b8a6 #f59e0b #8b5cf6
