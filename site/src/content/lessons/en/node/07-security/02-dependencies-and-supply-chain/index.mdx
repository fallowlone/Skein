---
concepts:
- lifecycle-scripts
- lockfile-integrity
- vuln-scanning
- prototype-pollution
- typosquatting
- untrusted-code-isolation
deepensInto: []
estMin: 19
lang: en
lessonType: topic
level: senior
order: 2
prereqs: []
slug: 02-dependencies-and-supply-chain
sources:
- https://docs.npmjs.com/cli/v10/commands/npm-audit
- https://docs.npmjs.com/cli/v10/configuring-npm/package-lock-json
- https://owasp.org/www-project-top-ten/
spiral: []
status: ready
summary: 'Installing a package runs its code. Pin and verify the tree with a committed
  lockfile and `npm ci`, gate known CVEs with `npm audit`, block lifecycle scripts on
  untrusted installs, validate merge keys, and never treat `vm` as a sandbox.'
title: 'Dependencies and supply-chain risk'
track: node
unit: 07-security
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A team shipped a small CLI on a Friday. Over the weekend a maintainer two levels deep in the dependency tree pushed a patch release whose `postinstall` script base64-decoded a payload, read `process.env`, and POSTed every secret it found to a pastebin. The team never ran that code on purpose — it ran the instant their CI did `npm install`, before a single line of their own application loaded. The audit dashboard was green; there was no CVE yet. The breach wasn't in their code at all. It was in the act of installing, which on npm is just another way of saying *running arbitrary code from strangers*.
</Hook>

<Crux>Installing a package runs its code: pin with a committed lockfile + `npm ci`, block scripts on untrusted installs, never trust `vm`.</Crux>

<Explanation>

## Installing is running code

The single fact that reframes dependency security: `npm install` executes arbitrary code. Think about that the next time you reach for a new convenience library — every indirect dependency it pulls in has the same privilege. A package can declare **lifecycle scripts** — `preinstall`, `install`, `postinstall` — and npm runs them on your machine, with your user's permissions, the moment the package lands. There is no review gate, no sandbox, and no diff you read first. A `postinstall` has full access to the filesystem, the network, and `process.env`, which in CI is exactly where your registry tokens, cloud keys, and deploy credentials live. This is not a theoretical edge: it is the most direct supply-chain vector there is, because it fires *before* your application code — and any tests or scanners that might catch malicious behavior — ever runs.

The defensive posture is to stop treating install as inert. For untrusted installs and especially in CI, run scripts off by default:

```bash
# Install strictly from the lockfile AND skip all lifecycle scripts
npm ci --ignore-scripts

# Or make it the machine-wide default for an untrusted environment
npm config set ignore-scripts true
```

`--ignore-scripts` skips every package's lifecycle hooks. The tradeoff is real: a few legitimate packages (native addons that compile bindings, e.g. `node-gyp` builds) genuinely need a script to function, and silently blocking them produces a broken install rather than a malicious one. So you vet the small set that needs scripts and allow them deliberately, rather than granting blanket execution to your entire transitive tree. And the deeper lever is **count**: most of the code that ships in a Node service is third-party, often hundreds of transitive packages from authors you'll never audit. Every dependency you remove is attack surface you remove.

## The lockfile is your integrity boundary

`package.json` records the versions you *want* (often ranges like `^4.2.0`). `package-lock.json` records the exact versions you actually got — and, critically, an **SRI integrity hash** (sha512) for every package's tarball. That hash is the tamper-evidence: if the bytes fetched from the registry don't hash to the recorded value, the install fails. This is why the lockfile is not an annoyance to `.gitignore` — it *is* your reproducibility and integrity boundary, and it must be committed.

The command that enforces it is `npm ci`, not `npm install`:

```bash
# CI: deterministic, integrity-verified, lockfile-only
npm ci
```

`npm ci` installs strictly from `package-lock.json`, **fails loudly if `package.json` and the lockfile are out of sync** (rather than silently "fixing" the lock the way `npm install` does), deletes `node_modules` first for a clean tree, and verifies every integrity hash. `npm install`, by contrast, may resolve new versions, mutate the lockfile, and pull in a freshly-published malicious patch within your `^` range. The rule is mechanical: **`npm ci` in CI and reproducible builds, `npm install` only locally when you are deliberately changing dependencies** — and review the resulting lockfile diff like code.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Threat</th>
        <th class="border border-border px-3 py-2 text-left">Mechanism</th>
        <th class="border border-border px-3 py-2 text-left">Primary mitigation</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Lifecycle script</td>
        <td class="border border-border px-3 py-2"><code>postinstall</code> runs at install, reads <code>process.env</code></td>
        <td class="border border-border px-3 py-2"><code>npm ci --ignore-scripts</code>; vet the few that need scripts</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Lockfile drift</td>
        <td class="border border-border px-3 py-2"><code>npm install</code> resolves a new malicious patch in range</td>
        <td class="border border-border px-3 py-2">commit the lockfile; <code>npm ci</code>; review lock diffs</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Known CVE</td>
        <td class="border border-border px-3 py-2">a transitive dep has a published vulnerability</td>
        <td class="border border-border px-3 py-2"><code>npm audit --audit-level=high</code> gate; Dependabot/Renovate</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Prototype pollution</td>
        <td class="border border-border px-3 py-2"><code>__proto__</code> in input taints <code>Object.prototype</code></td>
        <td class="border border-border px-3 py-2">allowlist keys; <code>Object.create(null)</code>; <code>--disable-proto</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Typosquat / confusion</td>
        <td class="border border-border px-3 py-2">malicious name shadows a real / private package</td>
        <td class="border border-border px-3 py-2">scoped packages; pin registry/scope; verify names</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Untrusted code</td>
        <td class="border border-border px-3 py-2"><code>vm</code> realm is escapable via prototypes</td>
        <td class="border border-border px-3 py-2">separate process/worker, container, or <code>isolated-vm</code></td>
      </tr>
    </tbody>
  </table>
</div>

## Scanning for known vulnerabilities

A committed lockfile makes builds reproducible, but it also freezes *known* vulnerabilities in place — pinning is only safe if you keep watching what you pinned. `npm audit` cross-references your installed tree against the registry's advisory database and reports CVEs with severity and a fix path:

```bash
# Report vulnerabilities; fail CI only on high/critical to cut noise
npm audit --audit-level=high

# Apply safe (semver-compatible) fixes; --force may take breaking majors
npm audit fix
```

The mechanism is a tree-vs-advisory diff, so its weakness is the same as any signature scanner: it sees only *published, known* issues, it carries **false positives and noise** (a "critical" in a dev-only transitive dep you never call), and `npm audit fix --force` can silently bump a dependency across a major version and break you. So gate CI at `--audit-level=high` to reduce alert fatigue, automate the upgrade churn with **Dependabot or Renovate** (which open reviewable PRs), and layer a behavior-aware tool — **Snyk** or **Socket** — that inspects install scripts and package behavior, not just version numbers, to catch the brand-new malicious release that has no CVE yet. Audit is a floor, not a ceiling.

## Prototype pollution and untrusted input

Not every supply-chain failure is a stolen secret; some are logic corruption. **Prototype pollution** happens when attacker-controlled input containing the keys `__proto__` or `constructor.prototype` is merged into an object by vulnerable code — a naive recursive deep-merge, a permissive `Object.assign` loop, or a query-string parser. Because almost every object inherits from `Object.prototype`, writing through `__proto__` mutates that shared prototype, and the pollution leaks into *every* object in the process: a planted `isAdmin: true` can flip an authorization check that reads `user.isAdmin`, and in the worst cases a polluted property feeds a code path that reaches `child_process` or a template engine and becomes RCE.

```js
// VULNERABLE: a naive deep-merge follows __proto__ straight onto Object.prototype
function badMerge(target, src) {
  for (const k in src) {
    if (typeof src[k] === "object") badMerge(target[k] ??= {}, src[k]);
    else target[k] = src[k];        // k === "__proto__" pollutes everything
  }
}

// DEFENSE: reject dangerous keys and use a null-prototype map
function safeMerge(target, src) {
  for (const k of Object.keys(src)) {
    if (k === "__proto__" || k === "constructor" || k === "prototype") continue;
    target[k] = src[k];
  }
  return target;
}
const lookup = Object.create(null); // no prototype to pollute
```

Defenses stack: validate and allowlist input keys (a schema validator rejects `__proto__` outright), use `Object.create(null)` for objects used as maps so there is no prototype to corrupt, keep merge/clone libraries patched, and at the process level consider `Object.freeze(Object.prototype)` or running with the `--disable-proto=delete` flag to remove the `__proto__` accessor entirely.

<Inset kind="why" lang="en">
**Typosquatting and dependency confusion** are the naming attacks. A typosquat is a malicious package named one keystroke off a popular one (`cross-env` vs `crossenv`), hoping you mistype. Dependency confusion is sharper: if your internal package `@acme/auth` isn't published and your install can reach the public registry, an attacker publishes a *public* `@acme/auth` at a higher version, and a misconfigured resolver pulls the impostor over your private one. Defenses: always use **scoped** packages for internal code, pin each scope to its private registry in `.npmrc` (`@acme:registry=...`), verify exact package names before adding them, and rely on the lockfile so an unexpected name can't slip in unreviewed.
</Inset>

<TradeoffMatrix
  id="07-supply-chain-tradeoff-1"
  lessonSlug="02-dependencies-and-supply-chain"
  lang="en"
  prompt="Your CI pipeline installs dependencies before running tests, and the repo has a committed lockfile. Which install command should the pipeline use?"
  options={[
    { name: "npm ci (and --ignore-scripts for untrusted installs)", summary: "Deterministic + verified.", correct: true, justification: "npm ci installs strictly from package-lock.json, fails loudly if the lock and package.json disagree, wipes node_modules for a clean tree, and verifies every sha512 integrity hash — so CI is reproducible and tamper-evident. Adding --ignore-scripts removes the lifecycle-script execution vector for installs you don't fully trust. This is the senior default for any CI or reproducible build." },
    { name: "npm install", summary: "May mutate the lock.", justification: "npm install can resolve newer versions inside your semver ranges, rewrite the lockfile in place, and pull in a freshly-published malicious patch — none of which you want in CI, where you need the exact, reviewed tree. Reserve it for local sessions where you are deliberately changing dependencies and will review the lockfile diff." },
    { name: "npm install --no-save", summary: "Still resolves fresh.", justification: "--no-save only stops package.json from being edited; npm still resolves versions against your ranges and may fetch new releases, so it's neither deterministic nor integrity-strict the way ci is. It doesn't give you the lockfile-only, fail-on-drift guarantees CI needs." },
    { name: "Delete the lockfile and let CI regenerate it", summary: "Throws away integrity.", justification: "This discards the exact pinned versions and their sha512 integrity hashes — the very tamper-evidence that makes a build trustworthy — and lets every build float to whatever the registry currently serves. It maximizes supply-chain exposure instead of reducing it; the lockfile must be committed and used, not regenerated per build." },
  ]}
/>

## You cannot sandbox untrusted code with `vm`

When you need to *run* code you don't trust — a user-submitted formula, a plugin — the seductive wrong answer is Node's built-in `vm` module. **`vm` is not a security sandbox.** Its own documentation says so. It runs code in a separate V8 *context*, but that context shares prototypes and references with the host: untrusted code can climb from a passed-in object's constructor up to the host realm's `Function` and reconstruct full access, breaking out of the "sandbox" entirely.

```js
const vm = require("node:vm");
// Escape: reach the host Function constructor through a passed object's chain.
const escape = "this.constructor.constructor('return process')().env";
// vm.runInNewContext(escape, { /* "sandboxed" */ }) // → leaks process.env
```

Real isolation requires a real boundary. Run untrusted code in a **separate process or worker thread** with dropped privileges, inside a **container** or microVM with restricted syscalls and no network, or in a purpose-built isolate such as **`isolated-vm`** (a true separate V8 isolate) or a hardened SES/`vm2`-successor runtime. The mechanism that matters is that the untrusted code must not share an object graph or a process with your secrets — a different V8 context on the same heap is not that boundary.

<Quiz
  id="07-supply-chain-quiz-1"
  lessonSlug="02-dependencies-and-supply-chain"
  lang="en"
  question="Why should a CI pipeline use `npm ci` rather than `npm install` for a repo with a committed lockfile?"
  choices={[
    { label: "npm ci installs strictly from the lockfile, fails if package.json and the lock are out of sync, verifies sha512 integrity hashes, and won't resolve a new in-range version — giving reproducible, tamper-evident builds", correct: true },
    { label: "npm ci is just a faster alias for npm install with the same resolution behavior", misconception: "They are not aliases. npm ci is lockfile-only and refuses to mutate it; npm install may resolve newer in-range versions and rewrite the lockfile. The difference is determinism and integrity, not just speed." },
    { label: "npm ci automatically fixes all known vulnerabilities during install", misconception: "npm ci does not patch vulnerabilities — that's npm audit fix. ci's job is a deterministic, integrity-verified install strictly from the existing lockfile." },
    { label: "npm ci runs lifecycle scripts in a sandbox so postinstall can't do harm", misconception: "npm ci runs lifecycle scripts normally — there is no sandbox. To block them you add --ignore-scripts. The reproducibility/integrity win is separate from script execution." },
  ]}
/>

<Quiz
  id="07-supply-chain-quiz-2"
  lessonSlug="02-dependencies-and-supply-chain"
  lang="en"
  question="Why is Node's `vm` module unsafe for running untrusted user code as a sandbox?"
  choices={[
    { label: "The vm context shares prototypes and references with the host, so untrusted code can climb an object's constructor chain to the host Function/realm and escape — it isolates names, not security", correct: true },
    { label: "vm is safe; it fully isolates untrusted code in its own process", misconception: "vm does not spawn a process. It runs code in a separate V8 context on the same heap that shares prototypes with the host, and that sharing is exactly what makes it escapable. The Node docs explicitly say it is not a security mechanism." },
    { label: "vm is only unsafe because it is slow, not because code can escape", misconception: "Performance isn't the issue. The real problem is a security one: untrusted code can break out of the context to reach host objects and process.env. Use a separate process/worker, a container, or isolated-vm." },
    { label: "vm works as a sandbox if you pass an empty context object", misconception: "An empty context doesn't help — escapes go through the prototype chain of whatever the code can reach (its own constructors), not through what you hand in. Real isolation needs a process/container/isolate boundary." },
  ]}
/>

<DragOrder
  id="07-supply-chain-drag-1"
  lessonSlug="02-dependencies-and-supply-chain"
  lang="en"
  prompt="Order the steps to lock down a CI install against supply-chain attacks, from prerequisite to last line of defense:"
  items={[
    { id: "a", label: "Commit package-lock.json so exact versions and sha512 integrity hashes are pinned in the repo" },
    { id: "b", label: "Use `npm ci` (not `npm install`) so the build is lockfile-only and fails on drift" },
    { id: "c", label: "Add `--ignore-scripts` for installs you don't fully trust, vetting the few packages that truly need scripts" },
    { id: "d", label: "Gate the pipeline with `npm audit --audit-level=high` and automate upgrades via Dependabot/Renovate" },
    { id: "e", label: "Layer a behavior-aware scanner (Snyk/Socket) to catch malicious releases that have no CVE yet" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="The supply chain from registry to your runtime, and where each control sits"
  hue="var(--d-security)"
  perRow={3}
  nodes={[
    { id: "r", label: "registry", sub: "lockfile + ci" },
    { id: "d", label: "direct + transitive", sub: "audit / minimize" },
    { id: "s", label: "install scripts", sub: "ignore-scripts" },
    { id: "b", label: "your build", sub: "verified tree" },
    { id: "x", label: "runtime input", sub: "validate keys" },
    { id: "u", label: "untrusted code", sub: "process / isolate" },
  ]}
  edges={[
    { from: "r", to: "d", label: "resolve" },
    { from: "d", to: "s", label: "postinstall" },
    { from: "s", to: "b", label: "into build" },
    { from: "b", to: "x", label: "run" },
    { from: "x", to: "u", label: "eval?" },
  ]}
  caption="Code enters at the registry and runs at install (scripts), at build, and at runtime. Pin and verify with the lockfile + npm ci, block scripts for untrusted installs, scan the tree for CVEs, validate input keys against prototype pollution, and isolate any truly untrusted code in a separate process — vm is not that boundary."
/>

<KeyTakeaway>Installing runs code: commit the lockfile, use `npm ci` (`--ignore-scripts` when untrusted), gate CVEs with `npm audit`, validate keys against prototype pollution, isolate untrusted code in a real process — never `vm`.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-supply-chain-retrieval"
  lang="en"
  questions={[
    { q: "A maintainer publishes a malicious patch release of a transitive dependency whose postinstall reads process.env. Which two of your defenses block it, and how does each work?", a: "Two controls stop this independently. First, the lockfile + `npm ci`: because you committed package-lock.json with exact versions and sha512 integrity hashes, and CI runs `npm ci` (which installs strictly from the lock, fails on drift, and verifies integrity), the freshly-published malicious version is never resolved at all — CI keeps installing the exact, reviewed bytes you pinned, so the patch can't enter the tree until someone deliberately bumps it and reviews the lockfile diff. Second, `--ignore-scripts`: even if a bad version did enter, `npm ci --ignore-scripts` (or `npm config set ignore-scripts true`) skips every lifecycle hook, so the postinstall never executes and never reaches process.env. The deeper, slower control is minimizing dependency count and transitive depth, because most of your installed code is third-party — every package you remove is a maintainer who can no longer ship code into your CI. A behavior-aware scanner like Socket/Snyk is the last layer, flagging the new release's install-script behavior before any CVE exists." },
    { q: "Why isn't Node's vm module a security sandbox, and what is the correct way to run untrusted user code?", a: "vm runs code in a separate V8 *context* on the *same* heap as your process, and that context shares prototypes and object references with the host. Untrusted code can therefore climb the prototype/constructor chain of any object it can reach — e.g. `this.constructor.constructor` gives it the host Function constructor — and from there reconstruct full access to the host realm, including process and process.env, escaping the 'sandbox' entirely. The Node documentation explicitly states vm is not a security mechanism. Real isolation requires a real boundary so the untrusted code shares neither an object graph nor a process with your secrets: run it in a separate process or worker thread with dropped privileges, inside a container or microVM with no network and restricted syscalls, or in a purpose-built isolate such as isolated-vm (a true separate V8 isolate) or a hardened SES runtime. A different context on the same heap is name isolation, not security isolation." },
  ]}
/>

<Recap lang="en">
Dependency security starts from one fact: `npm install` runs arbitrary code, because packages can declare `preinstall`/`install`/`postinstall` lifecycle scripts that execute with your permissions and can read `process.env` before your own code or any scanner runs — so block them with `npm ci --ignore-scripts` (or `npm config set ignore-scripts true`) for untrusted installs, and shrink the attack surface by minimizing dependency count and transitive depth, since most of your code is third-party. The lockfile is your integrity boundary: `package-lock.json` pins exact versions and sha512 SRI hashes, and `npm ci` installs strictly from it, fails on package.json/lock drift, and verifies integrity — so commit it and use `npm ci` in CI, never `npm install`, which may mutate the lock and pull an in-range malicious patch. Layer detection on top: `npm audit --audit-level=high` gates known CVEs (noisy and signature-only), Dependabot/Renovate automate upgrades, and Snyk/Socket inspect behavior to catch releases with no CVE yet. Guard runtime input against prototype pollution — reject `__proto__`/`constructor`/`prototype` keys, use `Object.create(null)` maps, and consider `--disable-proto=delete` — and prefer scoped packages pinned to a private registry to defeat typosquatting and dependency confusion. Finally, never treat the `vm` module as a sandbox: it shares prototypes with the host and is escapable, so run genuinely untrusted code in a separate process, container, or `isolated-vm` where it can touch neither your object graph nor your secrets. Now when you review a dependency PR or set up a new CI job, you'll know which three commands (`npm ci`, `--ignore-scripts`, `npm audit`) form the non-negotiable baseline.
</Recap>
