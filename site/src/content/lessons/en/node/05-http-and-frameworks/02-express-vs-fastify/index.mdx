---
concepts:
- middleware-chain
- plugin-encapsulation
- schema-validation
- json-serialization
- throughput-benchmarks
- async-error-handling
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-express-vs-fastify
sources:
- https://expressjs.com/en/guide/using-middleware.html
- https://fastify.dev/docs/latest/Reference/
- https://fastify.dev/benchmarks/
spiral: []
status: ready
summary: 'Express is a linear (req, res, next) middleware chain — minimal and unopinionated
  with the largest ecosystem. Fastify trades that for a plugin model with JSON-Schema
  validation and fast-json-stringify serialization, the real throughput win. Both
  are production-grade.'
title: 'Express vs Fastify: middleware and throughput'
track: node
unit: 05-http-and-frameworks
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import latencyBudget from "./infographics/latency-budget.svg?raw";

<Hook>
A team migrated a JSON API to Fastify after a blog post promised "2x throughput," then watched their p99 latency stay exactly where it was. The benchmark was real — Fastify *does* serve roughly twice the requests/sec of Express on a tiny "return a JSON object" route — but their handler spent 40ms waiting on Postgres and 0.2ms in the framework. The framework was never the bottleneck. Worse, two routes silently broke: an Express 4 `async` handler that used to throw a 500 now leaked the rejection as an `unhandledRejection`, because nobody had wrapped it. They chased a number that didn't matter and shipped a bug that did.
</Hook>

By the end of this lesson you'll understand what each framework actually trades — so you can choose the right one for the right reasons, not chase a benchmark number.

<Crux>Express chains `(req, res, next)` middleware; Fastify adds schema validation and serialization for throughput — both ship-grade.</Crux>

<Explanation>

## Two philosophies: a chain vs an encapsulated plugin tree

Before picking a framework, ask yourself: do you need the widest possible ecosystem and the simplest mental model, or do you need schema-driven validation and serialization baked in? The answer usually decides the choice. Express is deliberately small: a request flows through a **linear chain of middleware**, each a `(req, res, next)` function that mutates `req`/`res` and calls `next()` to pass control along, or `next(err)` to jump to an error handler. There is almost no opinion baked in — you assemble routing, body parsing, auth, and logging from a vast ecosystem of `app.use()` middleware. That minimalism is its superpower: the largest middleware catalogue in the Node world, a decade of battle-testing, and a signature every Node developer already knows.

```js
import express from "express";
const app = express();

// global middleware: runs for every request, in registration order
app.use(express.json());
app.use((req, res, next) => { req.startedAt = Date.now(); next(); });

app.get("/users/:id", (req, res) => {
  res.json({ id: req.params.id });
});
```

Fastify makes a different bet: a request runs through **lifecycle hooks** (`onRequest`, `preParsing`, `preValidation`, `preHandler`, …) and routes are grouped into **plugins** registered with `fastify.register()`. The defining feature is **encapsulation** — a plugin gets its own scope, so decorators, hooks, and middleware registered inside it *do not* leak to siblings. This is a feature for modularity, but it surprises Express refugees who expect `app.use()` to be global everywhere.

```js
import Fastify from "fastify";
const app = Fastify({ logger: true }); // pino built in

await app.register(async (instance) => {
  // this hook only runs for routes inside this plugin scope
  instance.addHook("preHandler", async (req) => { req.startedAt = Date.now(); });
  instance.get("/users/:id", async (req) => ({ id: req.params.id }));
});
```

## The real differentiator: schema-driven validation and serialization

Express has no built-in validation or serialization story — you reach for `zod`, `joi`, or `express-validator`, and you serialize with `res.json()`, which calls `JSON.stringify`. Fastify builds both into the route definition through **JSON Schema**. A request schema validates *and coerces* incoming params/query/body before your handler runs. A **response schema** is the throughput lever: `fast-json-stringify` compiles that schema into a specialized serializer function ahead of time, so it never walks an unknown object shape at runtime the way generic `JSON.stringify` must.

```js
app.get("/users/:id", {
  schema: {
    params: { type: "object", properties: { id: { type: "string" } } },
    response: {
      200: {
        type: "object",
        properties: { id: { type: "string" }, name: { type: "string" } },
      },
    },
  },
}, async (req) => getUser(req.params.id));
```

That compiled serializer is the single most real-world-relevant win, because it speeds up *every* response, not just a synthetic empty route — and as a bonus, fields not in the response schema are stripped, so you can't accidentally leak a `passwordHash`.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Dimension</th>
        <th class="border border-border px-3 py-2 text-left">Express</th>
        <th class="border border-border px-3 py-2 text-left">Fastify</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Composition</td>
        <td class="border border-border px-3 py-2">linear middleware chain</td>
        <td class="border border-border px-3 py-2">plugins + lifecycle hooks</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Handler signature</td>
        <td class="border border-border px-3 py-2"><code>(req, res, next)</code></td>
        <td class="border border-border px-3 py-2"><code>async (req, reply)</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Validation</td>
        <td class="border border-border px-3 py-2">bring your own (zod/joi)</td>
        <td class="border border-border px-3 py-2">built-in JSON Schema</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Serialization</td>
        <td class="border border-border px-3 py-2"><code>JSON.stringify</code></td>
        <td class="border border-border px-3 py-2"><code>fast-json-stringify</code> (compiled)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Ecosystem</td>
        <td class="border border-border px-3 py-2">largest, oldest</td>
        <td class="border border-border px-3 py-2">large, plugin-based</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Throughput (own bench)</td>
        <td class="border border-border px-3 py-2">baseline</td>
        <td class="border border-border px-3 py-2">~2x on simple JSON</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Async handler errors</td>
        <td class="border border-border px-3 py-2">Express 4: not auto-caught</td>
        <td class="border border-border px-3 py-2">awaited &amp; routed to error hook</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="en">
Why is Fastify "~2x" in benchmarks but flat in production? Its own benchmark suite hammers a route that returns a small constant object, so the only work is HTTP parsing and JSON serialization — exactly where the compiled serializer and a lean router shine. A real handler adds a DB round-trip (often 5–40ms) and external calls that dwarf the framework's sub-millisecond overhead. Synthetic numbers measure the framework in isolation; your latency is dominated by IO. The throughput edge is genuine, but it matters most for high-RPS, response-heavy JSON gateways, not CRUD apps bound by a database.
</Inset>

<Infographic
  svg={latencyBudget}
  label="Horizontal bar chart of one request's time budget: the Postgres round-trip is 40ms while the framework's overhead is only 0.2ms — a barely-visible sliver beside it."
  caption="The same request, broken down: the framework is 0.2ms of a ~40ms request. That 200x gap is why a '2x throughput' framework swap leaves p99 untouched — the database, not the framework, is the bottleneck."
  hue="var(--d-infra)"
/>

## The Express 4 async-error trap

The sharpest failure mode is historical and still everywhere. **Express 4 does not catch rejected promises from `async` route handlers.** If an `async` handler throws (or `await`s something that rejects), Express 4 has no `await` around your function, so the rejection becomes an `unhandledRejection` — which since Node 15 crashes the process by default — instead of reaching your error-handling middleware.

```js
// Express 4 — BROKEN: rejection escapes to unhandledRejection
app.get("/u/:id", async (req, res) => {
  const user = await db.find(req.params.id); // if this rejects…
  res.json(user);                            // …Express 4 never catches it
});

// Express 4 — FIX: try/catch + next, or a wrapper like express-async-errors
app.get("/u/:id", async (req, res, next) => {
  try { res.json(await db.find(req.params.id)); }
  catch (err) { next(err); } // hand off to the error middleware
});
```

**Express 5 (now released) fixes this**: rejected promises from `async` handlers are forwarded to the error handler automatically, closing the gap. Fastify never had this problem — handlers are `async` by design, their rejections are awaited and routed to the `setErrorHandler` hook. If you are on Express 4, wrap every `async` handler (or adopt `express-async-errors`); the silent crash is the most common production bug in older Express apps.

<TradeoffMatrix
  id="05-frameworks-tradeoff-1"
  lessonSlug="02-express-vs-fastify"
  lang="en"
  prompt="You're building a new high-RPS public JSON API: many small read endpoints, strict input validation, a team comfortable picking up TypeScript, and a need to never leak internal fields in responses. Which framework fits best?"
  options={[
    { name: "Fastify, leaning on request + response JSON Schema per route", summary: "Schema-driven, fast serialization.", correct: true, justification: "This spec is Fastify's sweet spot. Request schemas give you validation and coercion for free at the boundary; response schemas compile a fast-json-stringify serializer that both speeds up every JSON response and strips any field not in the schema — directly satisfying 'never leak internal fields.' TypeScript-first ergonomics and built-in pino logging fit a new, type-comfortable team. The high-RPS, response-heavy shape is exactly where the throughput edge is real rather than synthetic." },
    { name: "Express 4 plus zod and a custom serializer", summary: "Familiar, manual everything.", justification: "Workable, but you rebuild by hand what Fastify gives natively: zod for validation, a hand-rolled response filter to avoid leaking fields, and an async-error wrapper to dodge Express 4's rejection trap. More glue code, more places to forget the field allowlist, and no compiled serializer. Choose this only if the team's deep Express familiarity outweighs the schema benefits — which the spec says it doesn't." },
    { name: "Express 5 with the same middleware you already know", summary: "Modern Express, no schema.", justification: "Express 5 fixes the async-error gap and is a fine, safe choice — but it still has no built-in validation or serialization, so 'strict validation' and 'never leak fields' remain your manual responsibility. For a brand-new high-RPS API where schemas are a stated need, Fastify's built-ins do this work for you. Good runner-up, not the best fit here." },
    { name: "Whichever the last project used, for consistency", summary: "Defer to precedent.", justification: "Consistency is a real value, but here the spec gives concrete technical requirements — schema validation, response-field safety, high RPS — that should drive the choice. Picking by precedent ignores that Fastify natively covers three of the four requirements. Let the requirements, not inertia, decide a greenfield service." },
  ]}
/>

<Quiz
  id="05-frameworks-quiz-1"
  lessonSlug="02-express-vs-fastify"
  lang="en"
  question="What does fast-json-stringify actually do, and why is it Fastify's most real-world-relevant performance feature?"
  choices={[
    { label: "It compiles the route's response JSON Schema into a specialized serializer ahead of time, so responses skip the generic object-walking that JSON.stringify must do — speeding up every JSON response, not just a synthetic one", correct: true },
    { label: "It validates incoming request bodies faster than zod", misconception: "That's request validation, a separate concern. fast-json-stringify is about the response path: it turns a response schema into a compiled serializer. Request validation in Fastify uses a different compiled validator (ajv)." },
    { label: "It caches each response so identical requests return instantly", misconception: "There is no response caching here. It compiles a serializer once from the schema, then serializes each (potentially different) response object quickly — it does not memoize response bodies." },
    { label: "It replaces the HTTP parser with a faster C++ one", misconception: "It only touches serialization of the response body. The HTTP parser is Node's, unchanged; the win is skipping generic JSON.stringify's runtime shape discovery via a schema-compiled function." },
  ]}
/>

<Quiz
  id="05-frameworks-quiz-2"
  lessonSlug="02-express-vs-fastify"
  lang="en"
  question="An async route handler in an Express 4 app awaits a DB call that rejects, and there is no try/catch. What happens?"
  choices={[
    { label: "The rejection is NOT caught by Express 4 — it becomes an unhandledRejection, which since Node 15 crashes the process by default, instead of reaching the error-handling middleware", correct: true },
    { label: "Express 4 automatically forwards it to the error-handling middleware", misconception: "That's Express 5 (and Fastify) behaviour. Express 4 doesn't await your handler, so a rejected promise escapes — you must try/catch and call next(err), or use a wrapper like express-async-errors." },
    { label: "Express returns a 500 with the error message in the body", misconception: "Only if you've wired error handling and the error reaches it. In Express 4 with a bare async handler, the rejection never reaches the error middleware at all — it surfaces as an unhandledRejection." },
    { label: "The request hangs forever with no response and no error", misconception: "It doesn't silently hang at the framework level — the rejection fires the process-level unhandledRejection (crashing by default since Node 15). The client may see a dropped connection, but the visible symptom is a process crash, not an indefinite hang." },
  ]}
/>

<DragOrder
  id="05-frameworks-drag-1"
  lessonSlug="02-express-vs-fastify"
  lang="en"
  prompt="Order a Fastify request's journey through the lifecycle, from arrival to response:"
  items={[
    { id: "a", label: "Request arrives; the router matches it to a route and its schema" },
    { id: "b", label: "onRequest / preParsing / preValidation hooks run for this scope" },
    { id: "c", label: "The request schema validates and coerces params, query, and body" },
    { id: "d", label: "preHandler hooks run, then your async handler executes" },
    { id: "e", label: "The response schema's fast-json-stringify serializer encodes the reply" },
    { id: "f", label: "The serialized response is sent back to the client" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f"]}
/>

</Explanation>

<FlowDiagram
  label="A request through a framework: middleware/hooks, handler, then serialize"
  hue="var(--d-systems)"
  perRow={4}
  nodes={[
    { id: "r", label: "request in", sub: "route match" },
    { id: "m", label: "hooks / middleware", sub: "validate" },
    { id: "h", label: "handler", sub: "your logic" },
    { id: "s", label: "serialize + send", sub: "JSON out" },
  ]}
  edges={[
    { from: "r", to: "m" },
    { from: "m", to: "h", label: "next / await" },
    { from: "h", to: "s", label: "schema" },
  ]}
  caption="Both frameworks share this shape: a request matches a route, flows through middleware (Express) or lifecycle hooks (Fastify), hits your handler, then is serialized and sent. Express serializes with generic JSON.stringify; Fastify uses a schema-compiled fast-json-stringify serializer — the throughput lever that also strips unlisted fields."
/>

<KeyTakeaway>Express is the unopinionated chain with the biggest ecosystem; Fastify is schema-driven, its fast-json-stringify the real throughput win. Choose by ecosystem vs schemas; the DB, not the framework, is the bottleneck.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="05-frameworks-retrieval"
  lang="en"
  questions={[
    { q: "Fastify reports ~2x the throughput of Express in its own benchmarks. Why is that often invisible in a real application, and when does it actually matter?", a: "The benchmark isolates the framework: it serves a tiny constant JSON object, so the only work measured is HTTP parsing, routing, and serialization — precisely where Fastify's lean router and compiled fast-json-stringify serializer win, roughly doubling requests/sec. A real handler, by contrast, does a database round-trip (commonly 5–40ms) and maybe external HTTP calls, which dwarf the framework's sub-millisecond overhead. So end-to-end p99 latency is dominated by IO, and swapping frameworks barely moves it. The throughput edge is real but matters most for high-RPS, response-heavy JSON gateways and APIs where serialization is a meaningful share of the work — not for CRUD apps bound by their database. The genuinely portable win is the compiled serializer speeding up every response (and stripping unlisted fields), not the headline 2x." },
    { q: "Why can an async route handler silently crash an Express 4 service, and what are the two ways to fix it?", a: "Express 4 invokes your route handler but does not await it, so if the handler is async and its promise rejects — for example an awaited DB call fails — there's no try/catch or await around it inside Express to capture the rejection. The rejection therefore escapes the request entirely and surfaces as a process-level unhandledRejection, which since Node 15 terminates the process by default rather than producing a 500 through your error-handling middleware. Two fixes: (1) wrap the body in try/catch and call next(err) so the error reaches the error middleware; or (2) adopt a wrapper such as express-async-errors (or a higher-order asyncHandler) that does this for you across all routes. The durable fix is upgrading to Express 5, which now forwards rejected promises from async handlers to the error handler automatically — the same behaviour Fastify has always had, since its handlers are async by design and their rejections are routed to setErrorHandler." },
  ]}
/>

<Recap lang="en">
Express and Fastify solve the same problem with opposite temperaments. Express is a minimal, unopinionated **linear middleware chain** of `(req, res, next)` functions with the largest ecosystem in Node and a decade of battle-testing — you assemble validation, serialization, and everything else from third-party middleware. Fastify is a **plugin-and-hooks** model whose plugins are encapsulated (so `app.use()`-style globals don't leak across scopes, which surprises Express refugees) and whose defining feature is **JSON Schema**: request schemas validate and coerce input, and a response schema compiles a `fast-json-stringify` serializer that speeds every JSON response and strips fields not in the schema. On performance, Fastify's own benchmarks show roughly **2x** the requests/sec of Express, but that measures the framework in isolation on a trivial route; in real apps a 5–40ms database round-trip dominates the sub-millisecond framework overhead, so the genuine, portable win is the compiled serializer, not the headline number. The classic failure mode is **Express 4 not catching rejections from `async` handlers** — they escape as an `unhandledRejection` and crash the process since Node 15, fixed by try/catch + `next(err)`, a wrapper like `express-async-errors`, or upgrading to Express 5 (which forwards them automatically, as Fastify always has). Pick Express for ecosystem breadth, team familiarity, and gradual adoption; pick Fastify for performance-sensitive JSON APIs, schema-driven validation/serialization, and a structured plugin model — both are production-grade. Now when you see a benchmark claiming "2x throughput," you'll know to ask what the handler actually does before reaching for a migration ticket.
</Recap>
