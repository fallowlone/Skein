---
concepts:
- package-json
- dependency-types
- semver-ranges
- lockfiles
- npm-ci-vs-install
- node-modules-resolution
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-packages-and-npm
sources:
- https://docs.npmjs.com/cli/v10/configuring-npm/package-json
- https://docs.npmjs.com/about-semantic-versioning
- https://docs.npmjs.com/cli/v10/commands/npm-ci
spiral: []
status: ready
summary: 'package.json declares intent with semver ranges; the lockfile pins the exact
  resolved tree. npm install may rewrite the lock, npm ci installs strictly from it
  — so commit the lockfile and use npm ci in CI.'
title: 'package.json, npm, semver and lockfiles'
track: node
unit: 01-modules-and-runtime
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The build is green on your laptop and red in CI, and nobody touched the code. The diff is one transitive dependency: a logging library you have never imported shipped a patch overnight, your `^` range silently swallowed it, and it broke on the CI image. Your `package.json` says `"pino": "^9.0.0"` — the same string it said yesterday — so the manifest "didn't change." What changed is what `^` *resolves to* today. The fix is one word in the pipeline: `npm ci`, which installs the exact tree someone already locked, instead of re-solving the ranges from scratch.
</Hook>

<Crux>package.json declares ranges; the lockfile pins the exact resolved tree. npm install may rewrite the lock; npm ci installs strictly from it.</Crux>

<Explanation>

## package.json: the manifest of intent

`package.json` is the contract for a Node project — identity, scripts, and what it depends on. The fields that earn their keep at the middle+ level:

```json
{
  "name": "billing-service",
  "version": "2.4.1",
  "type": "module",
  "engines": { "node": ">=20" },
  "exports": { ".": "./dist/index.js" },
  "scripts": { "build": "tsc", "test": "vitest run" },
  "dependencies": { "pino": "^9.0.0" },
  "devDependencies": { "vitest": "~2.1.0", "typescript": "5.6.3" },
  "peerDependencies": { "react": ">=18" },
  "optionalDependencies": { "fsevents": "^2.3.0" }
}
```

`"type": "module"` decides how Node interprets `.js` files (ESM vs CommonJS — the prior lesson). `"exports"` is the modern entry map: it defines exactly which paths consumers may import and *hides everything else*, so deep imports into your internals stop working — it supersedes the older single `"main"` field. `"engines"` declares the Node range you support; with a strict installer config it can hard-fail an install on the wrong runtime instead of failing mysteriously at runtime.

The four dependency buckets are not interchangeable:

- **`dependencies`** — needed at runtime by anyone who installs your package. Shipped.
- **`devDependencies`** — needed only to build and test (compilers, test runners, linters). Installed locally, skipped under `npm install --omit=dev` / `NODE_ENV=production`.
- **`peerDependencies`** — "I work *with* this, but the host app must provide it." A React plugin lists `react` here so it shares the host's single copy instead of bundling a second, duplicate React.
- **`optionalDependencies`** — install is attempted but a failure is non-fatal; your code must guard for absence. Used for platform-specific native addons like `fsevents` (macOS only).

## Semver: a range is a promise, not a pin

Versions are `MAJOR.MINOR.PATCH`, and per the npm spec each digit is a promise to consumers: **PATCH** = backward-compatible bug fixes (`1.0.0` → `1.0.1`), **MINOR** = backward-compatible new features (`1.0.0` → `1.1.0`), **MAJOR** = changes that break backward compatibility (`1.0.0` → `2.0.0`). A version may carry a prerelease tag like `1.0.0-beta.2`, which sorts *before* `1.0.0` and is excluded from normal ranges unless you ask for it.

What you write in `package.json` is usually a *range*, and the operator decides how much drift you accept on the next install:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Spec</th>
        <th class="border border-border px-3 py-2 text-left">Allows up to (for 1.2.3)</th>
        <th class="border border-border px-3 py-2 text-left">Lets in</th>
        <th class="border border-border px-3 py-2 text-left">Use when</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>^1.2.3</code></td>
        <td class="border border-border px-3 py-2"><code>&lt;2.0.0</code></td>
        <td class="border border-border px-3 py-2">minor + patch</td>
        <td class="border border-border px-3 py-2">default; you trust the lib's semver</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>~1.2.3</code></td>
        <td class="border border-border px-3 py-2"><code>&lt;1.3.0</code></td>
        <td class="border border-border px-3 py-2">patch only</td>
        <td class="border border-border px-3 py-2">cautious; want fixes, not features</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>1.2.3</code></td>
        <td class="border border-border px-3 py-2"><code>1.2.3</code></td>
        <td class="border border-border px-3 py-2">nothing</td>
        <td class="border border-border px-3 py-2">exact pin; max determinism</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>*</code></td>
        <td class="border border-border px-3 py-2">any version</td>
        <td class="border border-border px-3 py-2">everything, incl. majors</td>
        <td class="border border-border px-3 py-2">never in production</td>
      </tr>
    </tbody>
  </table>
</div>

`^` is what `npm install <pkg>` writes by default because it picks up bug fixes and new features automatically while *promising* to never cross a major. The risk is right there in the hook: that promise depends on the maintainer versioning honestly. A library that ships a breaking change as a minor — or just an accidental regression in a patch — flows straight into your tree on the next un-pinned install. `^` decides the *ceiling* of what may enter. Something else decides what *actually* entered last time.

<Inset kind="why" lang="en">
`^0.x` is a trap. Below `1.0.0` the caret special-cases to patch-only: `^0.2.3` means `>=0.2.3 <0.3.0`, not `<1.0.0`. The reasoning is that pre-1.0 packages are declaring themselves unstable, so every `0.MINOR` bump is treated as potentially breaking. Many real dependencies sit on `0.x` for years, so this rule bites more often than people expect.
</Inset>

## The lockfile: what actually got installed

`package.json` answers "what am I willing to accept?" `package-lock.json` answers "what did I get?" When npm resolves your ranges, it walks the whole dependency graph — your deps, their deps, all the way down — picks a concrete version for every node, and writes the entire flattened tree to the lockfile: exact versions, the resolved registry URL, and an **integrity** hash (a Subresource-Integrity SHA for the tarball). On the next install npm can rebuild byte-identical `node_modules` and verify each download against its hash.

This is the line between the two install commands, and it is the senior-level point of the lesson:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left"></th>
        <th class="border border-border px-3 py-2 text-left"><code>npm install</code></th>
        <th class="border border-border px-3 py-2 text-left"><code>npm ci</code></th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Source of truth</td>
        <td class="border border-border px-3 py-2">re-solves package.json ranges</td>
        <td class="border border-border px-3 py-2">installs strictly from the lockfile</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Mutates the lockfile?</td>
        <td class="border border-border px-3 py-2">yes — may add/update entries</td>
        <td class="border border-border px-3 py-2">no — never writes the lock</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Needs a lockfile?</td>
        <td class="border border-border px-3 py-2">no (creates one if missing)</td>
        <td class="border border-border px-3 py-2">yes — errors without one</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Lock vs manifest mismatch</td>
        <td class="border border-border px-3 py-2">updates the lock to reconcile</td>
        <td class="border border-border px-3 py-2">exits with an error</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">node_modules</td>
        <td class="border border-border px-3 py-2">patches in place</td>
        <td class="border border-border px-3 py-2">deletes it first, clean install</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Install one package?</td>
        <td class="border border-border px-3 py-2">yes (<code>npm i lodash</code>)</td>
        <td class="border border-border px-3 py-2">no — whole project only</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Use it for</td>
        <td class="border border-border px-3 py-2">local dev, adding/removing deps</td>
        <td class="border border-border px-3 py-2">CI, deploys, any reproducible build</td>
      </tr>
    </tbody>
  </table>
</div>

The senior rule falls out of the table. **Commit the lockfile**, and in CI run `npm ci`, not `npm install`. `npm ci` is deterministic (it ignores ranges and installs exactly what's locked), clean (it wipes `node_modules` first so there is no leftover state), fast (it skips the resolution step), and — crucially — it *fails loudly* if `package-lock.json` and `package.json` have drifted apart, instead of silently rewriting the lock the way `npm install` would. That failure is a feature: it catches a hand-edited manifest that nobody re-locked, before it ships.

```bash
# Local: change deps, which updates package.json AND package-lock.json
npm install lodash      # adds "^4.x" to deps, writes the resolved tree to the lock
git add package.json package-lock.json   # commit BOTH, together

# CI / Dockerfile: reproduce the locked tree exactly, fail if it drifted
npm ci                  # clean, deterministic, errors if lock is out of sync
```

## node_modules: flattening, hoisting, and phantoms

npm installs into `node_modules` as a *flat* tree where it can: instead of nesting every dependency inside its parent, it **hoists** shared, compatible versions up to the top level so many packages share one copy. This keeps the tree shallow and small — but it leaks. A package your app never declared can end up hoisted to the top of `node_modules`, and `require("that-package")` will *work* on your machine. That's a **phantom dependency**: you're using something that isn't in your `package.json`. The day a dependency upgrade stops hoisting it — or a fresh install resolves a different tree — your import vanishes and the build breaks, with nothing in your manifest to explain why.

This is one reason **pnpm** exists: it builds a strict, symlinked `node_modules` where a package can only see what it explicitly declared, making phantom dependencies a hard error rather than a silent crutch. **Yarn** is the other major alternative, with its own lockfile (`yarn.lock`) and, in modern versions, a Plug'n'Play mode that drops `node_modules` entirely. All three solve the same problem npm does; they differ in lockfile format and how aggressively they isolate the tree. The portable lesson is independent of the tool: a declared dependency plus a committed lockfile is the only combination that reproduces.

<Inset kind="why" lang="en">
The lockfile is also your first supply-chain control. Each entry's integrity hash means a tampered tarball on the registry fails verification on install, and pinning via the lock means an attacker can't slip a malicious patch in through your `^` range between builds. Run `npm audit` to surface known CVEs in the locked tree, and treat the lockfile as a reviewed, committed artifact — a surprise change to it in a PR deserves the same scrutiny as a code change, because it can swap out exactly what executes.
</Inset>

<FlowDiagram
  label="From a semver range in package.json through resolution and the lockfile to a reproducible node_modules"
  hue="var(--d-runtime)"
  perRow="3"
  nodes={[
    { id: "manifest", label: "package.json", sub: "^9.0.0" },
    { id: "resolve", label: "resolve range", sub: "pick versions" },
    { id: "lock", label: "package-lock", sub: "9.4.2 + hash" },
    { id: "ci", label: "npm ci", sub: "read lock" },
    { id: "verify", label: "verify hash", sub: "integrity" },
    { id: "tree", label: "node_modules", sub: "byte-identical" },
  ]}
  edges={[
    { from: "manifest", to: "resolve", label: "npm install" },
    { from: "resolve", to: "lock", label: "write" },
    { from: "lock", to: "ci", label: "commit" },
    { from: "ci", to: "verify" },
    { from: "verify", to: "tree", label: "reproduce" },
  ]}
  caption="npm install resolves ranges and writes the lockfile once; every later npm ci reads that lock, verifies integrity hashes, and rebuilds the same tree. The range sets the ceiling; the lockfile pins reality."
/>

<Quiz
  id="node-02-packages-quiz-1"
  lessonSlug="02-packages-and-npm"
  lang="en"
  question="CI is failing on a dependency you never changed. package.json still says the same ^ range. What is the most likely cause and fix?"
  choices={[
    { label: "A transitive dep released a new minor/patch that the ^ range and a re-solving install pulled in; switch CI to npm ci so it installs the exact locked tree", correct: true },
    { label: "The lockfile is corrupt — delete package-lock.json and run npm install", misconception: "Deleting the lock and re-installing re-solves all ranges from scratch — the exact thing that let the new version in. You'd freshly pull the drift, not fix it. Keep the lock and use npm ci." },
    { label: "package.json must have changed; check git blame on it", misconception: "The manifest genuinely didn't change — that's the whole trap. A ^ range is stable text whose resolved version moves under it. What changed is what the range resolves to, not the string." },
    { label: "Pin every dependency to an exact version to be safe", misconception: "Exact-pinning your direct deps doesn't pin their transitive deps, which is where this drift came from. The lockfile already pins the full transitive tree; using npm ci is the targeted fix." },
  ]}
/>

<Quiz
  id="node-02-packages-quiz-2"
  lessonSlug="02-packages-and-npm"
  lang="en"
  question="require('lodash') works locally but lodash is not in your package.json. What is going on?"
  choices={[
    { label: "A phantom dependency: lodash was hoisted to the top of node_modules by some other package, so the require resolves — until a future install stops hoisting it and the import breaks", correct: true },
    { label: "Node has lodash as a built-in module", misconception: "lodash is a third-party package, not a Node core module. The resolver found it in node_modules, not in the built-ins." },
    { label: "It's fine — if it resolves, it's effectively a dependency", misconception: "It resolves by accident of hoisting, not by declaration. Nothing guarantees it stays hoisted; the correct fix is to add it to package.json (or remove the usage)." },
    { label: "npm install always installs lodash by default", misconception: "npm installs only what's declared plus their transitive deps. lodash is present because a declared dependency pulled it in and it got hoisted, not because npm adds it automatically." },
  ]}
/>

<TradeoffMatrix
  id="node-02-packages-tradeoff-1"
  lessonSlug="02-packages-and-npm"
  lang="en"
  prompt="A team wants reproducible installs in CI and reliable, debuggable upgrades. Which install policy should they adopt?"
  options={[
    { name: "Commit package-lock.json; npm install locally to change deps, npm ci in CI and deploys", summary: "Lockfile is the source of truth.", correct: true, justification: "This is the standard senior policy. The committed lockfile pins the exact transitive tree with integrity hashes, so every CI run and deploy reproduces byte-identical node_modules. npm ci is deterministic, clean, fast, and fails loudly if the lock and manifest drift — catching un-relocked manifests before they ship. Developers still use npm install locally to add or update dependencies, which updates both files, and the lockfile change is reviewed in the PR like any other artifact." },
    { name: "gitignore the lockfile; run npm install everywhere", summary: "Always re-solve from ranges.", justification: "Without a committed lock, every machine re-solves ranges independently and can land on different versions — the exact non-reproducibility this lesson is about. A transitive minor released overnight flows into CI silently. The lockfile is the artifact that makes installs reproducible; ignoring it throws that away." },
    { name: "Commit the lockfile but run npm install in CI too", summary: "Lock present, but install in CI.", justification: "Better than no lock, but npm install in CI may still update the lock to reconcile drift and patch node_modules in place rather than doing a clean install — so it won't fail loudly on a stale manifest and can leave leftover state. npm ci exists precisely for the CI case: strict, clean, and erroring on mismatch." },
    { name: "Pin every direct dependency to an exact version and skip the lockfile", summary: "Exact pins, no lock.", justification: "Exact-pinning direct deps does nothing for their transitive deps, which still resolve via their own ranges on each install — so the tree still drifts and isn't reproducible. The lockfile is what pins the full transitive graph; exact direct pins are no substitute for it." },
  ]}
/>

</Explanation>

<KeyTakeaway>Ranges set the ceiling (`^` minor+patch, `~` patch, exact pins nothing); the lockfile pins the exact tree with hashes. Commit it; `npm install` to change deps, `npm ci` in CI — clean, deterministic, fails loudly on drift.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="node-02-packages-retrieval"
  lang="en"
  questions={[
    { q: "Explain the difference between npm install and npm ci, and why CI should use npm ci.", a: "package.json declares dependency ranges (intent); package-lock.json records the exact resolved tree that those ranges produced last time — every version, registry URL, and an integrity hash. npm install treats package.json as the source of truth: it re-solves the ranges, may add or update entries, and writes those changes back to the lockfile, patching node_modules in place. That's right for local development where you're adding or removing dependencies. npm ci treats the lockfile as the source of truth: it requires a lockfile to exist, ignores the ranges and installs exactly what's locked, deletes node_modules first for a clean install, never writes to the lock, and — critically — exits with an error if package.json and the lockfile have drifted out of sync. CI should use npm ci because it is deterministic (byte-identical tree every run), clean (no leftover state), fast (skips resolution), and it fails loudly on a stale or hand-edited manifest instead of silently rewriting the lock the way npm install would. The senior rule: commit the lockfile, npm install locally to change deps, npm ci everywhere reproducibility matters." },
    { q: "What's a phantom dependency, what causes it, and how do you prevent it?", a: "A phantom dependency is a package your code imports and uses but that is not declared in your package.json. It happens because npm installs a flat node_modules and hoists shared, compatible versions up to the top level so packages can share one copy. A package pulled in transitively by one of your real dependencies can get hoisted to the top, and Node's resolver will happily find it from there — so require('that-pkg') works on your machine even though you never declared it. It's fragile: the day a dependency upgrade changes how things hoist, or a fresh install resolves a different tree, the package stops being at the top level, the import fails, and nothing in your manifest explains it. Prevent it two ways. First, declare everything you import — if you use it directly, it belongs in your dependencies. Second, use a package manager with strict isolation: pnpm builds a symlinked node_modules where a package can only see what it explicitly declared, turning phantom dependencies into a hard error at install/build time rather than a silent crutch that breaks later." },
  ]}
/>

<Recap lang="en">
package.json is the manifest of intent: identity (name, version), how Node loads it (`type`, `exports`/`main`, `engines`), scripts, and four distinct dependency buckets — dependencies (shipped at runtime), devDependencies (build/test only), peerDependencies (provided by the host app), and optionalDependencies (non-fatal if missing). What you write for a dependency is usually a semver range, and the operator sets the ceiling: `^` allows minor and patch (the default, and the source of silent drift), `~` allows patch only, an exact version pins nothing in, and `*` is never safe in production; remember `^0.x` quietly degrades to patch-only. The range is only the ceiling — the lockfile pins reality: package-lock.json records the exact flattened tree with versions and integrity hashes so installs are reproducible and verifiable. That makes the two install commands different in kind: npm install re-solves ranges and may rewrite the lock (right for local dependency changes), while npm ci installs strictly from the lock, wipes node_modules for a clean install, never mutates the lock, and errors if it has drifted from package.json — so the senior policy is to commit the lockfile and use npm ci in CI and deploys. Finally, npm's flat, hoisted node_modules can produce phantom dependencies — packages you use but never declared, which break on the next reshuffle — so declare what you import, treat the lockfile as a reviewed supply-chain artifact (integrity hashes, `npm audit`), and reach for pnpm or yarn when you want stricter isolation than npm's flattening gives you.
</Recap>
