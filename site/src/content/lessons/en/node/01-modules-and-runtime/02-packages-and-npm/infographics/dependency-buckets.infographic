infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label package.json: 4 dependency buckets
    children
      - label dependencies — shipped at runtime to every consumer
      - label devDependencies — build & test only, omitted in prod
      - label peerDependencies — host app must provide it (shared copy)
      - label optionalDependencies — install may fail, code must guard
theme
  palette #6366f1 #14b8a6 #f59e0b #f43f5e
