---
concepts:
- commonjs-require
- esm-import-export
- static-vs-dynamic-resolution
- live-bindings
- module-detection
- cjs-esm-interop
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: middle
order: 1
prereqs:
- base-cs/10-abstraction/03-modules
- base-cs/04-machine-code-to-language/04-the-runtime
slug: 01-cjs-vs-esm
sources:
- https://nodejs.org/api/esm.html
- https://nodejs.org/api/modules.html
spiral: []
status: ready
summary: 'CommonJS resolves require() synchronously at runtime with value-copy exports; ESM is a static, hoisted, async graph with live read-only bindings and top-level await. Node picks one per file, and the interop seam is where teams bleed time.'
title: 'CommonJS vs ESM'
track: node
unit: 01-modules-and-runtime
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import moduleSystems from "./infographics/module-systems.svg?raw";

<Hook>
Think of a kitchen with two appliances that do the same job — a gas stove and an induction stove — but take incompatible cookware. Put a cast-iron pan on the gas one and it works; move it to the induction one and nothing heats, because each appliance expects pots built for it. Node has exactly two such "appliances" for loading your code into the program — *modules* (the files you split your code into) running inside the *runtime* (the engine that actually executes them) — and most of a day lost to Node setup is just a file built for one being fed to the other. Here is what that looks like. You add `"type": "module"` to `package.json` to use top-level await, and the next `node server.js` throws `ReferenceError: require is not defined`. You change the offending `require()` to `import`, and now a dependency explodes with `Error [ERR_REQUIRE_ESM]`. Then `__dirname is not defined` shows up where it never used to. None of these are bugs in your code — they are the friction of two module systems that resolve at different times, copy values differently, and disagree about how a file even names its imports.
</Hook>

<Crux>CommonJS resolves require() synchronously at runtime; ESM is a static, async graph with live bindings — Node picks one per file.</Crux>

Why you need this as a developer: every Node app, test runner, and library you publish is built from modules, and the day you mix the two systems — or install a package that picked the other one — these errors land on you. Knowing which system a file is, and why, turns an afternoon of guesswork into a one-line fix.

<Explanation>

## CommonJS: synchronous, runtime-resolved, value-copied

CommonJS (CJS) is Node's original module system. You pull a module in with `require()`, a plain function that runs *while your code executes*: it reads the target file from disk, evaluates it, and returns whatever that file assigned to `module.exports`. Because it is an ordinary function call, you can `require()` conditionally, inside an `if`, or build the path at runtime — resolution is dynamic and happens line by line.

```js
// math.cjs
function add(a, b) { return a + b; }
let calls = 0;
module.exports = { add, get calls() { return calls; }, bump() { calls++; } };

// app.cjs
const math = require("./math.cjs");   // synchronous: blocks until math.cjs is evaluated
console.log(math.add(2, 3));          // 5
const { add } = math;                 // value copy — `add` is a snapshot of the binding
```

Two properties matter. First, `require()` is **synchronous and blocking**: the calling line does not continue until the required file has fully run, which is why CJS startup is a depth-first walk of the dependency tree. Second, what you get back is a **value copy at the moment of require**. If you destructure `const { add } = require(...)`, you captured the reference that existed then; if the module later reassigns `module.exports.add`, your `add` does not change. CJS also hands every module two path globals for free — `__dirname` and `__filename` — because resolution is local and runtime.

## ESM: static, hoisted, asynchronous, with live bindings

ECMAScript Modules (ESM) are the standard. You declare dependencies with `import`/`export` statements that are **static**: they must sit at the top level (not inside an `if` or a function), so the engine can find every import *before any code runs*. That up-front analysis is the whole point — it builds the full module graph, hoists imports, and lets bundlers see exactly which exports are used so they can drop the rest (tree-shaking). Loading the graph is **asynchronous**, which is what unlocks top-level `await`.

```js
// math.mjs
let calls = 0;
export function add(a, b) { return a + b; }
export function bump() { calls++; }
export function getCalls() { return calls; }

// app.mjs
import { add, bump, getCalls } from "./math.mjs";   // hoisted, resolved before run
import { readFile } from "node:fs/promises";

const config = await readFile("config.json", "utf8"); // top-level await — no async wrapper
console.log(add(2, 3));   // 5
bump();
console.log(getCalls());  // 1 — live binding reflects the update
```

The exports you import are **live bindings**, not copies: `import { add }` is a read-only *view* onto the exporting module's variable. If that module mutates the value behind the name, your import sees the new value — and you may never reassign an imported name yourself (`add = ...` is a `TypeError`). There is no `__dirname`; instead you derive paths from `import.meta.url` (e.g. `new URL("./data.json", import.meta.url)`).

<Infographic
  svg={moduleSystems}
  label="Side-by-side comparison of the two module systems. CommonJS: synchronous require() that blocks until the file is evaluated, value-copy exports frozen at require time, dynamic line-by-line resolution, and no tree-shaking or top-level await. ESM: an async static graph with imports hoisted and resolved before any code runs, live read-only bindings that track the exporter's current value, static resolution, and tree-shaking plus top-level await."
  caption="One root difference drives everything else: CommonJS resolves dynamically at runtime, ESM is analysed statically before execution — and timing, binding semantics, and tree-shaking all follow from that."
  hue="var(--d-infra)"
/>

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Property</th>
        <th class="border border-border px-3 py-2 text-left">CommonJS</th>
        <th class="border border-border px-3 py-2 text-left">ESM</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Syntax</td>
        <td class="border border-border px-3 py-2"><code>require()</code> / <code>module.exports</code></td>
        <td class="border border-border px-3 py-2"><code>import</code> / <code>export</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Load timing</td>
        <td class="border border-border px-3 py-2">Synchronous, blocking</td>
        <td class="border border-border px-3 py-2">Asynchronous graph</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Resolution</td>
        <td class="border border-border px-3 py-2">Dynamic, at runtime</td>
        <td class="border border-border px-3 py-2">Static, before execution</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Import semantics</td>
        <td class="border border-border px-3 py-2">Value copy at require time</td>
        <td class="border border-border px-3 py-2">Live, read-only binding</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Path globals</td>
        <td class="border border-border px-3 py-2"><code>__dirname</code>, <code>__filename</code></td>
        <td class="border border-border px-3 py-2"><code>import.meta.url</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Top-level await</td>
        <td class="border border-border px-3 py-2">No</td>
        <td class="border border-border px-3 py-2">Yes</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Tree-shakeable</td>
        <td class="border border-border px-3 py-2">No (dynamic shape)</td>
        <td class="border border-border px-3 py-2">Yes (static shape)</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="en">
Why does "static" buy tree-shaking? Because `import &#123; add &#125; from "./math.mjs"` names exactly which binding you use, the bundler can prove `bump` is never referenced and delete it from the output — before running a line. CJS exports are an ordinary object built at runtime (`module.exports.add = ...`); a tool cannot know which keys survive without executing the program, so it must keep them all. Static structure is not a style preference — it is what makes the dependency graph analysable.
</Inset>

## How Node decides which system a file is

Node does not guess from the contents. It classifies each file as CJS or ESM by an explicit set of signals, in priority order:

- **Extension wins outright.** `.mjs` is always ESM; `.cjs` is always CJS. These override everything else and are the unambiguous escape hatch.
- **`"type"` in the nearest `package.json`.** For a plain `.js` file, Node walks up to the closest `package.json`: `"type": "module"` makes `.js` files ESM, `"type": "commonjs"` (or no field) makes them CJS. Flipping that one field reinterprets every `.js` file in the package — which is exactly why `require is not defined` appears the moment you add it.
- **The `"exports"` field** controls what `import "pkg"` and `require("pkg")` resolve to, and via **conditional exports** can serve a different file to each — the dual-package pattern.

```json
{
  "name": "mylib",
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.mjs",
      "require": "./dist/index.cjs",
      "types": "./dist/index.d.ts"
    },
    "./package.json": "./package.json"
  }
}
```

The `"exports"` map is more than dual publishing: it is your package's **public API boundary**. Anything not listed in `exports` is unreachable from outside — consumers can no longer deep-import `mylib/src/internal.js`, even if the file exists. That encapsulation is a senior reason to define `exports` deliberately rather than letting the whole directory leak.

<FlowDiagram
  label="How Node classifies a module file as CommonJS or ESM in priority order: extension first, then the nearest package.json type field, then exports conditions"
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "file", label: "Load a file", sub: "node x.js" },
    { id: "ext", label: ".mjs / .cjs?", sub: "extension" },
    { id: "type", label: '"type" field', sub: "package.json" },
    { id: "esm", label: "ESM", sub: "import/export" },
    { id: "cjs", label: "CommonJS", sub: "require()" },
    { id: "cond", label: '"exports"', sub: "import vs require" },
  ]}
  edges={[
    { from: "file", to: "ext" },
    { from: "ext", to: "esm", label: ".mjs" },
    { from: "ext", to: "type", label: ".js" },
    { from: "type", to: "esm", label: "module" },
    { from: "type", to: "cjs", label: "commonjs" },
    { from: "cond", to: "esm", label: "import" },
    { from: "cond", to: "cjs", label: "require" },
  ]}
  caption="Extension is the hard override; .js defers to the nearest package.json type; the exports field then routes import vs require to different files for dual packages."
/>

## The interop seam — where the time bleeds

The two systems do not compose symmetrically, and this asymmetry is the real pain of the migration era:

- **ESM can import CJS.** `import express from "express"` works; the CJS `module.exports` object becomes the ESM `default` export. **Named** imports from a CJS module are best-effort: Node statically analyses the CJS source to *guess* the named exports, but anything assigned dynamically (`module.exports[name] = ...`) is invisible, so `import &#123; Router &#125; from "express"` may fail where `import express from "express"; const &#123; Router &#125; = express;` works.
- **CJS cannot synchronously `require()` an ESM module** — historically it threw `ERR_REQUIRE_ESM`, because loading ESM is async and `require` is sync. The portable fix is **dynamic `import()`**, which returns a promise and works from either system: `const mod = await import("./esm-only.mjs")`. (Recent Node versions can `require()` a *synchronous* ESM graph, but dynamic `import()` is the rule that always holds.)

```js
// CJS file needing an ESM-only dependency
async function main() {
  const { default: chalk } = await import("chalk"); // chalk is ESM-only now
  console.log(chalk.green("works from CJS via dynamic import()"));
}
main();
```

<Quiz
  id="01-cjs-vs-esm-quiz-interop"
  lessonSlug="01-cjs-vs-esm"
  lang="en"
  question="A CommonJS file must use a dependency that ships as ESM-only. require('the-pkg') throws ERR_REQUIRE_ESM. What is the portable fix?"
  choices={[
    { label: "Use dynamic import(): const mod = await import('the-pkg') — it returns a promise and loads ESM from a CJS file", correct: true },
    { label: "Add \"type\": \"module\" to package.json so require works again", misconception: "Adding \"type\": \"module\" turns your own .js files into ESM (breaking your require() calls) — it does not let a synchronous require() pull an async ESM graph. The seam is sync-vs-async, not a config flag." },
    { label: "Rename the dependency's file to .cjs", misconception: "You cannot rewrite a third-party package's extensions, and renaming would not change that its source uses ESM syntax. Reach for dynamic import() on your side instead." },
    { label: "Wrap the require in a try/catch and retry", misconception: "ERR_REQUIRE_ESM is deterministic, not transient — retrying calls the same blocked path. The fix is to switch loading mechanism (dynamic import), not to retry the failing one." },
  ]}
/>

<Quiz
  id="01-cjs-vs-esm-quiz-bindings"
  lessonSlug="01-cjs-vs-esm"
  lang="en"
  question="A module increments an internal counter and exposes it. A consumer reads the counter after the increment. When does the consumer see the new value?"
  choices={[
    { label: "Under ESM, because imports are live bindings — a read-only view that reflects the exporter's current value", correct: true },
    { label: "Under CommonJS, because require returns a live reference to module.exports", misconception: "CJS hands back a value copy taken at require time; if you destructured the binding, it is a snapshot and will not track later reassignments. ESM imports, not CJS, are live." },
    { label: "Under both, the behaviour is identical", misconception: "They differ precisely here: ESM imports are live read-only bindings, while CJS destructured imports are value copies frozen at require time." },
    { label: "Never — module state is always isolated per importer", misconception: "Module instances are cached and shared, not copied per importer. The live-vs-copy difference is about binding semantics, not isolation." },
  ]}
/>

<TradeoffMatrix
  id="01-cjs-vs-esm-tradeoff-newcode"
  lessonSlug="01-cjs-vs-esm"
  lang="en"
  prompt="You are starting a new Node library in 2026 that others will install. Which module strategy do you publish?"
  options={[
    { name: "Author in ESM; expose a dual-package exports map (import + require conditions) with a deliberate public surface", summary: "ESM-first, dual exports.", correct: true, justification: "The senior default: write modern static ESM so consumers get tree-shaking and top-level await, and ship a conditional exports map so both import and require resolve correctly during the long interop era. Listing only your intended entry points in exports also locks the public API so consumers cannot deep-import internals. Most cost is build tooling, which is well-trodden." },
    { name: "Author in CommonJS only", summary: "CJS only.", justification: "Maximally compatible with old consumers and simple to build, but you forfeit tree-shaking and top-level await, and ESM consumers only get a default export with best-effort named guessing. For brand-new code this trades the platform's direction for short-term familiarity — reasonable only for a strictly internal CJS-only codebase." },
    { name: "Author in ESM only, no CJS condition", summary: "ESM only.", justification: "Cleanest source and forward-looking, but any CommonJS consumer must reach for dynamic import() and cannot require() you synchronously, which still breaks a meaningful slice of the ecosystem. Defensible for apps you control end-to-end, riskier for a widely-installed library where you cannot dictate the consumer's module system." },
    { name: "Skip the exports field; let the whole package directory be importable by path", summary: "No exports map.", justification: "Easiest to publish, but every internal file becomes part of your de-facto API — consumers deep-import internals and you can never refactor without breaking them. The exports field exists precisely to draw that boundary; omitting it is the encapsulation anti-pattern." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="The interop seam between CommonJS and ESM: ESM imports CJS as a default export, while CJS reaches an ESM module only through dynamic import()"
  hue="var(--d-systems)"
  perRow={2}
  nodes={[
    { id: "esm", label: "ESM file", sub: "import" },
    { id: "cjs", label: "CJS file", sub: "require" },
    { id: "cjsmod", label: "CJS module", sub: "module.exports" },
    { id: "esmmod", label: "ESM module", sub: "export" },
  ]}
  edges={[
    { from: "esm", to: "cjsmod", label: "import → default" },
    { from: "cjs", to: "esmmod", label: "await import()" },
  ]}
  caption="ESM can statically import a CJS module (its module.exports becomes default; named imports are best-effort). CJS cannot synchronously require ESM — it must use dynamic import(), which returns a promise."
/>

<KeyTakeaway>Prefer ESM for new code: static structure enables tree-shaking and top-level await. ESM can import CJS, but CJS cannot require ESM synchronously — use dynamic import(). Make exports your package's public API.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-cjs-vs-esm-retrieval"
  lang="en"
  questions={[
    { q: "Why are ESM imports tree-shakeable while CommonJS exports are not, and what does 'static' have to do with it?", a: "ESM import/export statements are static: they must appear at the top level and name exactly which bindings are pulled in, so the engine and bundlers analyse the full module graph before any code runs. That up-front analysis lets a tool prove which exports are actually referenced and delete the rest from the output — that is tree-shaking. CommonJS exports are the opposite: module.exports is an ordinary JavaScript object assembled at runtime (module.exports.add = ..., or even computed keys), and require() is a function you can call conditionally with a runtime path. A bundler cannot know which keys survive without executing the program, so it must conservatively keep them all. The asynchronous, statically-analysable ESM graph is also what enables top-level await, because the loader can pause a module's evaluation while a promise settles. So 'static' is not a stylistic label — it is the property that makes the dependency graph knowable ahead of execution, and tree-shaking plus top-level await are direct consequences." },
    { q: "Walk through the CJS↔ESM interop rules and how Node decides which system a file is.", a: "Node classifies each file in priority order: a .mjs extension is always ESM and .cjs always CJS (the hard override); for a plain .js file Node reads the nearest package.json, where \"type\": \"module\" makes it ESM and \"type\": \"commonjs\" or no field makes it CJS; and a package's exports field, with conditional import/require conditions, can route the two systems to different files (the dual-package pattern) while also locking the public API. Interop is asymmetric. ESM can import a CJS module: its module.exports becomes the default export, but named imports are only best-effort because Node statically guesses the CJS export names and misses anything assigned dynamically — so import default then destructure is the safe form. CJS cannot synchronously require() an ESM module, because loading ESM is asynchronous and require is synchronous; historically this threw ERR_REQUIRE_ESM. The portable fix that always works is dynamic import(), which returns a promise and can load an ESM module from inside a CJS file: const { default: x } = await import('esm-pkg')." },
  ]}
/>

<Recap lang="en">
CommonJS and ESM are two module systems that differ in timing, semantics, and structure. CommonJS uses require()/module.exports: resolution is dynamic and synchronous, the call blocks until the target file is evaluated, destructured imports are value copies frozen at require time, and every module gets __dirname/__filename for free. ESM uses import/export statements that are static and hoisted, so the engine builds the whole graph before execution — which enables tree-shaking, an asynchronous graph, top-level await, and live read-only bindings that track the exporter's current value; paths come from import.meta.url instead of __dirname. Node decides which system a file is in priority order: the .mjs/.cjs extension overrides everything, then the nearest package.json "type" field governs .js files, then the exports field (with conditional import/require) can serve different files to each and define the package's public API boundary. The interop seam is asymmetric: ESM can import a CJS module (its module.exports becomes default; named imports are best-effort), but CJS cannot synchronously require ESM — reach for dynamic import(), which returns a promise and works from either side. For new code prefer ESM, and treat the exports field as a deliberate API surface rather than an afterthought.
</Recap>
