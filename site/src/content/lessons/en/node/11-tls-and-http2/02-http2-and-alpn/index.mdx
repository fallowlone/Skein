---
concepts:
- http2-multiplexing
- alpn-negotiation
- head-of-line-blocking
- server-push-deprecation
- h2-vs-h2c
- flow-control
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: senior
order: 2
prereqs: []
slug: "02-http2-and-alpn"
sources:
- https://nodejs.org/api/http2.html
- https://www.rfc-editor.org/rfc/rfc9113
- https://developer.chrome.com/blog/removing-push
spiral: []
status: ready
summary: 'HTTP/2 multiplexes many streams over one TCP connection and negotiates h2 via ALPN in the TLS handshake — but one connection means transport HoL still bites under loss.'
title: 'HTTP/2 and ALPN: one connection, many streams, one failure mode'
track: node
unit: 11-tls-and-http2
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
The team flipped on HTTP/2 expecting a free win and watched p99 latency get *worse* for mobile users. The synthetic tests on the office fibre looked great — dozens of assets loaded over one connection, no more six-connection-per-origin limit. Then real users on lossy LTE started complaining. A single dropped TCP segment was now stalling *every* in-flight request at once, because all of them shared one connection and TCP delivers bytes strictly in order. Under HTTP/1.1 with six parallel connections, one lost packet froze one connection; the other five kept flowing. HTTP/2 had quietly traded six independent failure domains for one. The fix that actually helped wasn't a Node setting — it was HTTP/3 over QUIC.
</Hook>

<Crux>HTTP/2 multiplexes streams over one TCP connection (kills HTTP-layer HoL), negotiated `h2` via ALPN — transport HoL still bites under loss.</Crux>

<Explanation>

## Multiplexing: many streams, one connection

HTTP/1.1's defining limit is that a connection carries one request at a time. To load a page with 40 assets, the browser opens up to six TCP connections per origin and pipelines requests serially on each — and a slow response head-of-line-blocks everything queued behind it on that connection. HTTP/2 replaces this with **multiplexing**: a single TCP connection carries many independent **streams**, each an interleaved sequence of binary frames tagged with a stream ID. Request 7 and request 12 ride the same socket simultaneously, their `DATA` frames interleaved on the wire, reassembled by stream ID on the other end.

In Node, the `http2` core module exposes this directly. A connection is an `Http2Session`; each request/response pair is an `Http2Stream`.

```js
import http2 from "node:http2";
import { readFileSync } from "node:fs";

const server = http2.createSecureServer({
  key: readFileSync("key.pem"),
  cert: readFileSync("cert.pem"),
  // ALPN: offer h2 first, fall back to HTTP/1.1 for old clients
  ALPNProtocols: ["h2", "http/1.1"],
});

server.on("stream", (stream, headers) => {
  // Each `stream` is one Http2Stream multiplexed over the shared session.
  stream.respond({ ":status": 200, "content-type": "text/plain" });
  stream.end("hello over " + stream.session.alpnProtocol);
});

server.listen(8443);
```

The `stream` event fires once per request, and many streams belong to one `session`. That is the whole multiplexing model: the session owns the TCP socket and the connection-wide state (flow control, `SETTINGS`), while each stream is cheap and independent at the HTTP layer.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Mode</th>
        <th class="border border-border px-3 py-2 text-left">Transport</th>
        <th class="border border-border px-3 py-2 text-left">ALPN id</th>
        <th class="border border-border px-3 py-2 text-left">How a client starts it</th>
        <th class="border border-border px-3 py-2 text-left">Browser support</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>h2</code></td>
        <td class="border border-border px-3 py-2">HTTP/2 over TLS</td>
        <td class="border border-border px-3 py-2"><code>h2</code></td>
        <td class="border border-border px-3 py-2">ALPN in the TLS handshake</td>
        <td class="border border-border px-3 py-2">Yes (all modern browsers)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>h2c</code></td>
        <td class="border border-border px-3 py-2">HTTP/2 cleartext (no TLS)</td>
        <td class="border border-border px-3 py-2">none (no TLS handshake)</td>
        <td class="border border-border px-3 py-2">Prior-knowledge, or HTTP/1.1 <code>Upgrade</code></td>
        <td class="border border-border px-3 py-2">No browser supports it</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>http/1.1</code></td>
        <td class="border border-border px-3 py-2">HTTP/1.1 over TLS</td>
        <td class="border border-border px-3 py-2"><code>http/1.1</code></td>
        <td class="border border-border px-3 py-2">ALPN fallback</td>
        <td class="border border-border px-3 py-2">Yes</td>
      </tr>
    </tbody>
  </table>
</div>

Browsers only ever speak `h2` over TLS. `h2c` (cleartext HTTP/2) exists in the spec — established either by *prior knowledge* (the client just assumes the server speaks h2c) or by an HTTP/1.1 `Upgrade: h2c` dance — but no browser implements it, so in practice it shows up only between trusted backend services or behind a TLS-terminating proxy. For anything a browser touches, HTTP/2 means `h2`, which means TLS, which means ALPN.

## ALPN: agreeing on the protocol inside the handshake

How do client and server agree to speak HTTP/2 *before* sending a single HTTP byte? **ALPN** — Application-Layer Protocol Negotiation, a TLS extension. During the TLS `ClientHello`, the client advertises the protocols it can speak, ordered by preference, in the ALPN extension. The server picks one from that list and echoes it back in the `ServerHello`. By the time the handshake finishes, both sides already know whether this connection is `h2` or `http/1.1` — zero extra round trips, no `Upgrade` request.

<SequenceDiagram
  label="ALPN protocol negotiation folded into the TLS handshake: client offers h2 and http/1.1, server selects h2"
  hue="var(--d-network)"
  actors={["Client", "Server"]}
  messages={[
    { from: "Client", to: "Server", label: "ClientHello + ALPN: [h2, http/1.1]" },
    { from: "Server", to: "Client", label: "ServerHello + ALPN: h2" },
    { from: "Client", to: "Server", label: "Finished (now speaking h2)" },
    { from: "Server", to: "Client", label: "SETTINGS frame" },
  ]}
  caption="The server selects from the client's offered list; selection order is the server's policy. If the client offers no protocol the server accepts, behaviour depends on config — Node falls back per ALPNProtocols."
/>

In Node the `ALPNProtocols` option (on both `createSecureServer` and the TLS client) is the ordered list you offer. On the server it is conventionally `["h2", "http/1.1"]` — prefer HTTP/2, fall back to 1.1 for legacy clients. Order matters: the array communicates *your* preference, and Node's `http2` server selects the first mutually-supported entry. Read the negotiated result off `stream.session.alpnProtocol` (or `socket.alpnProtocol`) to confirm what was actually chosen.

<Inset kind="why" lang="en">
Multiplexing fixes **HTTP-layer** head-of-line blocking: no single slow response blocks the others, because each is its own stream. But it cannot fix **transport-layer** HoL. TCP guarantees in-order delivery of a *single* byte stream. When you put 40 HTTP/2 streams on one TCP connection and one segment is lost, TCP holds back *all* later bytes — for every stream — until the retransmit arrives, because the kernel won't deliver byte N+1 before byte N. So one lost packet stalls every multiplexed stream at once. HTTP/1.1's six connections accidentally isolated this: a loss froze one connection, not all six. The real fix is HTTP/3 over QUIC, which runs streams over UDP with per-stream loss recovery, so a lost packet stalls only its own stream. HTTP/2's multiplexing is genuinely better on a clean link and genuinely worse on a lossy one.
</Inset>

## Flow control, SETTINGS, and why Server Push died

A multiplexed connection needs coordination, and HTTP/2 carries it as control frames. At connect time each side sends a `SETTINGS` frame declaring limits — most importantly `SETTINGS_MAX_CONCURRENT_STREAMS` (how many streams the peer may open at once) and the initial flow-control window. **Flow control** is per-stream *and* per-connection: a receiver advertises a window of bytes it's willing to buffer, and `WINDOW_UPDATE` frames replenish it as data is consumed. This stops one fast stream from drowning a slow consumer, but it's also a footgun — a too-small window throttles throughput on high-bandwidth-delay-product links (the classic high-latency case where HTTP/2 should shine). Node exposes these via session options like `settings: { maxConcurrentStreams }` and `peerMaxConcurrentStreams`.

**Server Push** was HTTP/2's headline feature: the server could send a `PUSH_PROMISE` and proactively push resources (CSS, JS) it predicted the client would need, before the client asked. In practice it failed. Servers pushed assets the browser already had cached, wasting bandwidth on the very lossy links where bytes are most precious; push was hard to prioritise correctly and frequently *delayed* the critical HTML it was supposed to accelerate. Cache-hit rates were dismal. Chrome **removed** Server Push support (the change landed in Chrome 106, 2022), and the replacement is `103 Early Hints` — an informational response that tells the browser which resources to `preload`/`preconnect` while the server is still computing the real response, letting the *client* decide whether it actually needs them.

<Quiz
  id="02-http2-and-alpn-quiz-1"
  lessonSlug="02-http2-and-alpn"
  lang="en"
  question="A page over h2 stalls completely whenever the mobile network drops a packet, even though every asset is its own stream. Why?"
  choices={[
    { label: "All streams share one TCP connection, and TCP's in-order delivery holds back every later byte — across all streams — until the lost segment is retransmitted (transport-layer HoL)", correct: true },
    { label: "HTTP/2 multiplexing is broken under packet loss and should be disabled", misconception: "Multiplexing works as designed; it solves HTTP-layer HoL. The stall is TCP's in-order delivery, a transport property HTTP/2 cannot change. The fix is HTTP/3 over QUIC, not turning off h2." },
    { label: "ALPN re-negotiates the protocol on every lost packet, adding latency", misconception: "ALPN happens once, inside the initial TLS handshake. It plays no role after the connection is established and nothing to do with packet loss." },
    { label: "Flow control windows reset to zero on packet loss", misconception: "Flow control windows are managed by WINDOW_UPDATE frames at the HTTP/2 layer; they don't reset on TCP loss. The stall is the kernel withholding in-order bytes." },
  ]}
/>

<TradeoffMatrix
  id="02-http2-and-alpn-tradeoff-1"
  lessonSlug="02-http2-and-alpn"
  lang="en"
  prompt="You're deciding whether enabling HTTP/2 will actually help a given workload. Which case benefits most?"
  options={[
    { name: "A page with many small assets served to clients on high-latency links", summary: "Many small requests.", correct: true, justification: "This is HTTP/2's sweet spot. Multiplexing lets dozens of small requests share one connection without the six-connection cap or per-connection head-of-line blocking, and on a high-latency link avoiding extra connection setups (TCP + TLS handshakes) is a big win. The more small, parallelisable requests over one already-warm connection, the more multiplexing pays off — provided the link isn't badly lossy." },
    { name: "A single large file download (e.g. a 2GB video) over one stream", summary: "One big stream.", justification: "Multiplexing buys nothing here — there's only one stream, so there's nothing to interleave. Throughput is governed by TCP and the flow-control window, identical to HTTP/1.1. A too-small HTTP/2 flow-control window can even make it slightly worse. HTTP/2's advantage is parallelism across many requests, which a single download doesn't have." },
    { name: "Assets already served and multiplexed by a CDN over its own h2/h3 edge", summary: "CDN already does it.", justification: "If a CDN already terminates HTTP/2 or HTTP/3 at the edge and multiplexes to clients, enabling h2 on your origin behind it changes little for end users — the client-facing benefit is already captured at the edge. Origin-to-CDN is usually a small number of long-lived connections where h2's per-request multiplexing matters far less. You're optimising a hop users don't see." },
    { name: "A workload on a badly lossy network with one connection", summary: "Lossy single conn.", justification: "This is where HTTP/2 can lose to HTTP/1.1. One TCP connection means one lost segment head-of-line-blocks every multiplexed stream via TCP's in-order delivery, whereas HTTP/1.1's multiple connections isolate loss. On a lossy link the real answer is HTTP/3 over QUIC, not h2." },
  ]}
/>

</Explanation>

<KeyTakeaway>HTTP/2 multiplexes streams over one TLS connection, negotiated `h2` via ALPN. It kills HTTP-layer HoL but not transport HoL — a lost segment stalls all streams; QUIC/HTTP3 is the fix. Push is dead; use `103 Early Hints`.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-http2-and-alpn-retrieval"
  lang="en"
  questions={[
    { q: "Explain how a browser and a Node http2 server agree to speak HTTP/2, and where the ALPNProtocols option fits.", a: "They agree via ALPN, a TLS extension negotiated inside the handshake itself — before any HTTP bytes flow. In the ClientHello the browser advertises the protocols it supports, ordered by preference (typically h2 then http/1.1), in the ALPN extension. The server picks one mutually-supported protocol from that list and echoes the choice in the ServerHello. By the time the TLS handshake completes both sides already know whether the connection is h2 or http/1.1 — no extra round trips, no HTTP Upgrade request. In Node, ALPNProtocols on http2.createSecureServer is the ordered list the server offers, conventionally [\"h2\", \"http/1.1\"]: prefer HTTP/2, fall back to 1.1 for legacy clients. The negotiated result is readable on stream.session.alpnProtocol or socket.alpnProtocol. Because browsers only speak h2 over TLS, ALPN is effectively mandatory for browser-facing HTTP/2; cleartext h2c isn't browser-supported and uses prior-knowledge or an Upgrade handshake instead." },
    { q: "HTTP/2 fixed head-of-line blocking — so why does p99 get worse on lossy mobile networks, and what actually fixes it?", a: "Multiplexing fixed HTTP-layer head-of-line blocking: each request is an independent stream, so no single slow response blocks the others queued behind it. But it cannot fix transport-layer HoL. All those streams share one TCP connection, and TCP guarantees in-order delivery of a single byte stream — when one segment is lost, the kernel withholds every later byte, for every stream, until the retransmit arrives. So a single dropped packet stalls all multiplexed streams at once. HTTP/1.1's six parallel connections accidentally isolated this: a loss froze one connection while the others flowed. That's why moving to h2 can raise p99 on lossy LTE even though it helps on a clean link. The real fix is HTTP/3 over QUIC, which carries independent streams over UDP with per-stream loss recovery, so a lost packet stalls only its own stream rather than the whole connection." },
  ]}
/>

<Recap lang="en">
HTTP/2 carries many independent streams over a single TCP connection by interleaving binary frames tagged with stream IDs — in Node, one `Http2Session` owns the socket and connection-wide state, while each request is a cheap `Http2Stream`. This multiplexing eliminates HTTP-layer head-of-line blocking and the old six-connections-per-origin workaround. Client and server agree to speak HTTP/2 via ALPN, a TLS extension: the client offers an ordered protocol list (`ALPNProtocols`, e.g. `["h2", "http/1.1"]`) in the ClientHello, the server selects one, and the handshake ends with both sides knowing the protocol — no extra round trips. Browsers only ever speak `h2` over TLS; cleartext `h2c` has no browser support and lives between backend services. The catch is transport-layer HoL: because every stream shares one TCP connection and TCP delivers bytes in order, a single lost segment stalls all streams at once, which is why HTTP/2 can worsen p99 on lossy links — and why HTTP/3 over QUIC, with per-stream loss recovery, is the real fix. Connection coordination rides on `SETTINGS` frames (concurrency limits, flow-control windows) and `WINDOW_UPDATE` for per-stream and per-connection flow control. HTTP/2 helps most with many small assets over high-latency links and least for a single large download or assets a CDN already multiplexes. And Server Push is dead — Chrome removed it over poor cache-hit rates and wasted bandwidth; reach for `103 Early Hints` instead, letting the client decide what to preload.
</Recap>
