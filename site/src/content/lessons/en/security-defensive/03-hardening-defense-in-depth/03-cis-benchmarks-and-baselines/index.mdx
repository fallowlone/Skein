---
concepts:
- cis-benchmark
- configuration-baseline
- drift
- compliance-as-code
- hardening-guide
deepensInto: []
estMin: 15
lang: en
lessonType: topic
level: middle
order: 3
prereqs: []
slug: 03-cis-benchmarks-and-baselines
sources:
- https://www.cisecurity.org/cis-benchmarks
- https://csrc.nist.gov/pubs/sp/800/123/final
spiral: []
status: ready
summary: 'CIS Benchmarks give you a vetted hardening baseline so you do not invent
  one. The senior work is the loop after: capture that baseline as code, then detect
  and close configuration drift before an attacker finds the gap.'
title: CIS benchmarks and baselines
track: security-defensive
unit: 03-hardening-defense-in-depth
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The post-mortem is three pages long and the root cause is one line: `PermitRootLogin yes`. Six months ago someone debugged a stuck deploy by SSHing in as root, flipped that flag, and never reverted it. The hardened image you shipped had it set to `no`. The running host did not. No CVE, no exploit chain — just a setting that drifted away from the baseline you thought you had, on a box nobody re-checked. The attacker did not break in. They logged in. This is why hardening is not a one-time task: a baseline you set and never measure against is a baseline you have already lost.
</Hook>

By the end of this lesson you'll know why CIS Benchmarks are the baseline you should start from, how to encode that baseline so it survives contact with production, and how to detect the drift that quietly reopens the holes you closed.

<Crux>CIS Benchmarks give a vetted hardening baseline; the real work is detecting and closing config drift before it becomes an exploit.</Crux>

<Explanation>

## What a CIS Benchmark actually is

You do not want to invent "what does a hardened Ubuntu host look like?" from scratch — that is months of research per platform, and you will miss things. A **CIS Benchmark** is the answer someone already built: a consensus-developed, prescriptive configuration guide for a specific platform — a Linux distro, Windows Server, Kubernetes, PostgreSQL, nginx, an AWS account, a Docker daemon. There are over 100 of them, each maintained by a community of practitioners and revised against new platform versions and new attack techniques. Where a generic guide like NIST SP 800-123 tells you the *principles* of server hardening (minimize the attack surface, run least privilege, patch, log), a CIS Benchmark gives you the *exact settings* that implement those principles on the box in front of you, down to the file, line, and expected value.

Two structural features matter for how you use it. First, recommendations are split into **Level 1** (practical hardening that should not break normal function — the sane default for most fleets) and **Level 2** (defense-in-depth for high-security environments, accepting that some functionality or convenience is sacrificed). Second, each recommendation is tagged **Automated** or **Manual**: Automated items have a machine-checkable audit and remediation procedure, Manual items require human judgment. That split is the single most important thing a senior reads off the benchmark, because it tells you exactly which controls you can enforce continuously and which need a process instead.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Benchmark concept</th>
        <th class="border border-border px-3 py-2 text-left">What it means</th>
        <th class="border border-border px-3 py-2 text-left">Senior reading</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Level 1</td>
        <td class="border border-border px-3 py-2">Practical hardening, shouldn't break normal function</td>
        <td class="border border-border px-3 py-2">Baseline for the whole fleet; enforce by default</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Level 2</td>
        <td class="border border-border px-3 py-2">Defense-in-depth, may sacrifice function/convenience</td>
        <td class="border border-border px-3 py-2">Reserve for high-value hosts; test for breakage first</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Automated</td>
        <td class="border border-border px-3 py-2">Machine-checkable audit + remediation</td>
        <td class="border border-border px-3 py-2">Encode as code; check on every host, every day</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Manual</td>
        <td class="border border-border px-3 py-2">Needs human judgment to assess</td>
        <td class="border border-border px-3 py-2">Owns a process/review, not a scanner rule</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Profile</td>
        <td class="border border-border px-3 py-2">The set of recommendations you commit to</td>
        <td class="border border-border px-3 py-2">Your baseline = benchmark minus documented exceptions</td>
      </tr>
    </tbody>
  </table>
</div>

## From benchmark to *your* baseline

A benchmark is not a baseline. The benchmark is the menu; your **configuration baseline** is the meal you actually commit to — the specific set of recommendations, at the level you chose, *minus the exceptions your environment legitimately needs*, each exception written down with a reason and an owner. This distinction is where most hardening programs quietly fail. A team that treats "100% CIS compliant" as the goal hits a recommendation that breaks their app — say, disabling a kernel module their monitoring agent needs — and instead of recording a justified exception, they either ship a broken host or silently turn the whole check off. Now the baseline lies.

The senior move is to derive the baseline once, deliberately. Start from the appropriate level (Level 1 for the fleet), walk the recommendations, and for each one make an explicit decision: **apply**, or **except-with-reason**. The output is a baseline document — ideally machine-readable — where a deviation is never a surprise. The reason this matters for drift detection is causal: if you cannot say what "correct" is, you cannot detect "incorrect." A clean, exception-annotated baseline is the *reference* that every running host is measured against. No reference, no drift detection — just hope.

<Inset kind="why" lang="en">
Why not just aim for a 100% score? Because a compliance percentage is an average, and averages hide the controls that matter. A host can score 96% while the missing 4% is "SSH root login enabled" and "audit logging disabled" — the two settings an attacker most wants. CIS scoring weights every Automated check roughly equally, so the number rewards closing many trivial gaps over the one critical one. Use the score as a coarse trend, never as the goal. The goal is *this specific set of controls is enforced, and every deviation is a recorded decision* — which a single percentage can never express.
</Inset>

## Drift: how a hardened host un-hardens itself

You harden the golden image, you ship it, you move on. Then entropy arrives. **Configuration drift** is the gap that opens over time between the baseline a host was provisioned with and its actual running state. It is not usually malice — it is operations: an engineer disables a control to debug an incident at 2 a.m. and forgets to re-enable it; a package update resets a config file to its vendor default; a manual `hotfix` patches one box but not the other forty; two teams own overlapping config and each "corrects" the other. Each change is individually reasonable. The aggregate is a fleet where no two hosts are quite the same and the security posture you designed exists only in the image, not in production.

Drift is dangerous precisely because it is invisible by default. The host still runs. The app still serves traffic. Nothing alerts. The drifted setting only announces itself when an attacker — or an auditor — finds it. So the defender's job is to make drift *visible*: continuously re-audit every running host against the baseline and surface every deviation, so a control that flipped from `no` to `yes` raises a finding the next day instead of in the post-mortem.

<FlowDiagram
  label="The drift lifecycle: a host is provisioned from a hardened CIS baseline; over time operational changes (a debug tweak, a package update resetting a config) drift it away from that baseline; without re-auditing the gap stays invisible until an attacker or auditor finds it; a continuous audit against the baseline detects the deviation and remediation pulls the host back to the reference."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "base", label: "Hardened baseline", sub: "CIS-derived" },
    { id: "provision", label: "Host provisioned", sub: "matches baseline" },
    { id: "drift", label: "Operational change", sub: "control flips" },
    { id: "audit", label: "Continuous audit", sub: "vs baseline" },
    { id: "fix", label: "Remediate", sub: "back to reference" },
  ]}
  edges={[
    { from: "base", to: "provision" },
    { from: "provision", to: "drift", label: "entropy" },
    { from: "drift", to: "audit", label: "detect" },
    { from: "audit", to: "fix", label: "converge" },
  ]}
  caption="Drift is the default state of any long-lived host. The only thing that keeps a fleet at its baseline is a loop: audit every host against the reference, surface deviations, and remediate — provisioning hardened once is not enough."
/>

## Compliance as code: closing the loop

Detecting drift by hand does not scale past a handful of hosts. The durable pattern is **compliance as code**: express the baseline as machine-readable, version-controlled rules, and run them automatically against every host on a schedule and in the deploy pipeline. CIS ships its baselines in a structured format and provides tooling (CIS-CAT), and the open ecosystem encodes benchmarks as OpenSCAP/SCAP content or as policy in tools like Chef InSpec, Ansible, or OPA. The shape is the same regardless of tool: a control is a rule with an *audit* (how to read the current value) and often a *remediation* (how to set the correct value), checked into a repo and run by CI.

Encoding the baseline as code buys you four things at once. The baseline is now **reviewable** (a change to a control goes through a pull request, not a 2 a.m. SSH session), **versioned** (you can prove what "correct" was on any date — which auditors love and incidents require), **enforced uniformly** (the same rules run on host 1 and host 41, so drift between hosts is caught), and **fast to detect** (a scheduled scan turns a six-month-old silent regression into a next-morning finding). The remaining senior decision is what to do on a deviation: **alert-only** for controls where auto-fixing could break a workload (let a human reconcile), versus **auto-remediate** for controls where the correct value is unambiguous and safe to enforce — converging the host back to the reference without waiting for a ticket.

<TradeoffMatrix
  id="03-cis-baselines-tradeoff-1"
  lessonSlug="03-cis-benchmarks-and-baselines"
  lang="en"
  prompt="A scheduled compliance-as-code scan reports that `PermitRootLogin` drifted from `no` to `yes` on 1 of 40 production hosts. What's the right response design?"
  options={[
    { name: "Treat the versioned baseline as the source of truth: file a finding, and for an unambiguous control like this, auto-remediate the host back to `no` while recording the deviation and who/what changed it", summary: "Detect, remediate, record.", correct: true, justification: "This is the closed loop. The baseline-as-code is the reference, so a drifted host is a detected deviation, not a debate. `PermitRootLogin no` has an unambiguous correct value with no legitimate workload reason to be `yes`, so auto-remediation is safe and converges the host back to the reference immediately — and the recorded deviation feeds back into finding out why it drifted (the real root cause)." },
    { name: "Update the baseline to `PermitRootLogin yes` so the scan goes green again", summary: "Silence the alarm.", justification: "This makes the baseline match reality instead of making reality match the baseline — exactly backwards. The whole point of a baseline is that it encodes the *correct* state; editing it to match a drifted, insecure value launders a security regression into 'compliant.' The scan is doing its job; suppressing it reopens the hole on purpose." },
    { name: "Manually SSH to the host and fix the one box now; no need to change the process", summary: "One-off fix.", justification: "Fixing the symptom on one host leaves the mechanism intact: nothing stops the same control drifting again tomorrow, on this host or another, and nothing tells you why it flipped. A manual one-off is the same un-tracked change pattern that caused the drift in the first place. The fix has to be the loop, not a single box." },
    { name: "Auto-remediate every drifted control on every host immediately, including app-level configs, with no alerting", summary: "Auto-fix everything blindly.", justification: "Blanket auto-remediation is unsafe for controls where the 'correct' value can break a real workload — a kernel module a monitoring agent needs, a cipher an old client requires. Those deviations should alert a human to reconcile, possibly recording a justified exception. Auto-remediate only the unambiguous controls; alert-only for the rest. Silent blanket enforcement causes its own outages." },
  ]}
/>

<Quiz
  id="03-cis-baselines-quiz-1"
  lessonSlug="03-cis-benchmarks-and-baselines"
  lang="en"
  question="What is the difference between a CIS Benchmark and your configuration baseline?"
  choices={[
    { label: "The benchmark is the consensus menu of recommendations; your baseline is the specific set you commit to, minus documented exceptions your environment needs", correct: true },
    { label: "They're the same thing — 'baseline' is just CIS's marketing word for 'benchmark'", misconception: "They are distinct. The benchmark is platform-wide consensus guidance; the baseline is your tailored commitment to a subset of it with recorded exceptions. Conflating them is why teams either ship broken hosts or silently disable checks." },
    { label: "The baseline is stricter than the benchmark — it adds extra controls CIS didn't think of", misconception: "A baseline is usually the benchmark minus justified exceptions, not a superset. The point isn't to out-harden CIS; it's to commit to a known, exception-annotated reference you can measure drift against." },
    { label: "The benchmark applies to running hosts; the baseline applies only to the golden image", misconception: "Both describe desired configuration. The baseline is exactly what you measure *running* hosts against to detect drift — it isn't image-only." },
  ]}
/>

<Quiz
  id="03-cis-baselines-quiz-2"
  lessonSlug="03-cis-benchmarks-and-baselines"
  lang="en"
  question="A host scores 96% on a CIS scan. Why might a senior still treat it as a serious problem?"
  choices={[
    { label: "Because the score is an average that can hide critical controls — the missing 4% might be SSH root login and audit logging, the settings an attacker most wants", correct: true },
    { label: "Because anything below 100% means the host is fully compromised", misconception: "A sub-100% score doesn't mean compromise. The point is that the percentage is a coarse average; severity depends on *which* controls fail, not the raw number." },
    { label: "Because CIS requires 100% to issue a certificate", misconception: "This isn't about certification mechanics. It's that an average rewards closing many trivial gaps over the one critical gap, so the number can look healthy while a high-impact control is off." },
    { label: "Because a 96% score proves the scanner is misconfigured", misconception: "A 96% score is just a measurement, not evidence of a broken scanner. The senior concern is that the metric hides which specific controls are failing." },
  ]}
/>

<DragOrder
  id="03-cis-baselines-drag-1"
  lessonSlug="03-cis-benchmarks-and-baselines"
  lang="en"
  prompt="Order the hardening-and-drift lifecycle from start to steady state:"
  items={[
    { id: "a", label: "Pick the CIS Benchmark + level for the platform" },
    { id: "b", label: "Derive your baseline: apply or except-with-reason per control" },
    { id: "c", label: "Encode the baseline as version-controlled compliance-as-code" },
    { id: "d", label: "Continuously audit every running host against the baseline" },
    { id: "e", label: "Remediate (or alert + record) on each detected deviation" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Start hardening from a CIS Benchmark, not from scratch — but a benchmark isn't a baseline. Derive your baseline (chosen level, minus recorded exceptions), encode it as compliance-as-code, and continuously audit running hosts against it so drift becomes a next-day finding, not a post-mortem line.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-cis-baselines-retrieval"
  lang="en"
  questions={[
    { q: "Explain the difference between a CIS Benchmark, a configuration baseline, and configuration drift — and why all three are needed to actually stay hardened.", a: "A CIS Benchmark is consensus-developed, prescriptive hardening guidance for a specific platform (a distro, Windows Server, Kubernetes, PostgreSQL, an AWS account), giving the exact settings — file, line, expected value — that implement principles a generic guide like NIST SP 800-123 only states. It splits recommendations into Level 1 (practical fleet hardening) and Level 2 (defense-in-depth that may sacrifice function), and tags each as Automated (machine-checkable) or Manual (needs human judgment). Your configuration baseline is not the benchmark itself: it's the specific set of recommendations you commit to, at your chosen level, minus the exceptions your environment legitimately needs — each exception recorded with a reason and owner. The baseline is the reference. Configuration drift is the gap that opens over time between that baseline and a host's actual running state, caused by ordinary operations (a debug tweak left in, a package update resetting a config, a hotfix applied to one box not forty). All three are needed because: the benchmark saves you from inventing 'hardened' yourself; the baseline turns it into a concrete, exception-annotated reference you can measure against (no reference, no drift detection); and treating drift as the default state is what forces the continuous-audit loop that keeps the fleet at the baseline instead of only the golden image being correct." },
    { q: "What is 'compliance as code,' and how do you decide between alerting and auto-remediation when a scan detects drift?", a: "Compliance as code means expressing the baseline as machine-readable, version-controlled rules — each control is a rule with an audit (read the current value) and often a remediation (set the correct value) — run automatically against every host on a schedule and in the deploy pipeline, via tooling like CIS-CAT, OpenSCAP/SCAP, Chef InSpec, Ansible, or OPA. It buys four things: the baseline becomes reviewable (a control change is a pull request, not a 2 a.m. SSH), versioned (you can prove what 'correct' was on any date — what audits and incidents need), uniformly enforced (the same rules on host 1 and host 41, so host-to-host drift is caught), and fast to detect (a scheduled scan turns a six-month silent regression into a next-morning finding). On a detected deviation, the senior decision is alert-only versus auto-remediate. Auto-remediate controls with an unambiguous correct value and no legitimate workload reason to differ (e.g. PermitRootLogin no) — converge the host immediately. Alert-only for controls where forcibly setting the 'correct' value could break a real workload (a kernel module a monitoring agent needs, a cipher an old client requires) — surface it for a human to reconcile, possibly recording a justified exception. And never resolve a scan by editing the baseline to match a drifted insecure value: that launders a regression into 'compliant.' The recorded deviation should also drive finding the root cause of why it drifted." },
  ]}
/>

<Recap lang="en">
CIS Benchmarks are consensus, prescriptive hardening guides — over 100 of them, one per platform — that give you the exact settings to start from instead of inventing "hardened" yourself, where NIST SP 800-123 only gives the principles. Read two things off every benchmark: Level 1 vs Level 2 (fleet default vs high-security defense-in-depth) and Automated vs Manual (what you can enforce continuously vs what needs a process). But a benchmark is not a baseline: your configuration baseline is the specific set you commit to, minus exceptions recorded with a reason and owner, and that exception-annotated reference is the only thing you can measure drift against — no reference, no drift detection. Configuration drift is the silent, ordinary gap that opens between that baseline and a host's running state, and it's invisible until an attacker or auditor finds it. The fix is a loop, not a one-time hardening pass: encode the baseline as compliance-as-code (reviewable, versioned, uniform, fast), continuously audit every running host, and on each deviation either auto-remediate the unambiguous controls or alert a human for the ones that could break a workload. Now when you see a hardened image ship, your next question is: what re-checks the running host tomorrow — or did we just harden the image and call it done?
</Recap>
