---
concepts:
- host-hardening
- attack-surface-reduction
- secure-defaults
- service-minimization
- baseline
deepensInto: []
estMin: 15
lang: en
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-os-and-host-hardening
sources:
- https://www.cisecurity.org/cis-benchmarks
- https://csrc.nist.gov/pubs/sp/800/123/final
spiral: []
status: ready
summary: 'Every running service, open port, and shipped default is attack surface. Hardening shrinks a host to the job it actually does, swaps permissive defaults for safe ones, and pins a measurable baseline so drift becomes visible.'
title: 'OS and host hardening'
track: security-defensive
unit: 03-hardening-defense-in-depth
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A fresh Ubuntu cloud image boots, and before you SSH in for the first time, `ss -tlnp` already shows a handful of listeners you never asked for: a recursive resolver bound to a public interface, a print-spooler from the desktop meta-package, an SNMP daemon with the `public` community string still set. Each one is a service you don't use, didn't audit, and won't patch on time — and each is a doorway. Nobody attacked you yet. The image just shipped with the doors open, and "default" is the most dangerous word in infrastructure.
</Hook>

By the end of this lesson you'll know how to shrink a host to the exact job it does, replace permissive defaults with safe ones, and pin a baseline so configuration drift becomes a thing you can *see* instead of a thing you discover during an incident.

<Crux>Every running service is attack surface; shrink a host to its job, ship secure defaults, and pin a baseline so drift is visible.</Crux>

<Explanation>

## Attack surface is the sum of what's reachable

Before you can harden a host you need a precise mental model of what an attacker can actually touch. A host's **attack surface** is the union of every interface where untrusted input meets your code: listening network ports, running services and their RPC endpoints, local setuid binaries, kernel modules, exposed filesystems, scheduled jobs, and the credentials each of those runs as. Every item is a potential entry or a potential escalation. The defender's leverage is brutally simple and brutally effective: *you cannot be exploited through a service that isn't running.* Removed code has no CVEs.

This is why **attack-surface reduction** is the highest-leverage move in host security and the cheapest. A patch fixes one known bug in a service; removing the service fixes every bug it will ever have, including the ones not yet discovered. When the 2014 Shellshock and 2021 PrintNightmare classes of bugs landed, the hosts that shrugged weren't the ones that patched fastest — they were the ones that had never installed the vulnerable component in the first place. Reduction beats reaction.

## Service minimization: subtract before you add

Service minimization is the disciplined practice of running the smallest set of processes a host needs to do its one job, and nothing else. A box that serves HTTP does not need a mail transfer agent, a desktop DNS resolver, Avahi/mDNS, CUPS, or an X server. The operational test for every listener is one sentence: *name the specific function this serves and the team that owns it.* If you can't, it shouldn't be running.

In practice you walk the surface and cut:

- **Audit listeners** — `ss -tulpn` (or `ss -tlnp` for TCP) maps every open port to the process and PID holding it. Anything you can't name gets stopped.
- **Disable, then remove** — `systemctl disable --now <svc>` stops it surviving reboot; `apt purge` / `dnf remove` deletes the binary so it can't be re-enabled or exploited at all.
- **Bind to the narrowest interface** — a service that only talks to localhost should listen on `127.0.0.1`, never `0.0.0.0`. A database bound to `0.0.0.0` with a default password is how you end up in a ransom note.
- **Default-deny the firewall** — drop all inbound, then allow only the named ports. The firewall is your backstop for the service you forgot to disable.

<FlowDiagram
  label="The host-hardening pipeline: start from the broad default attack surface, enumerate every listening service, minimize to only what the host's job requires, apply secure defaults to what remains, capture the result as a measured baseline, and continuously detect drift away from it; the loop is what keeps a host hardened over time rather than just on day one."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "surface", label: "Default surface", sub: "ports + services open" },
    { id: "enumerate", label: "Enumerate", sub: "ss -tulpn" },
    { id: "minimize", label: "Minimize", sub: "disable + purge" },
    { id: "defaults", label: "Secure defaults", sub: "deny-by-default" },
    { id: "baseline", label: "Baseline", sub: "CIS / measured" },
    { id: "drift", label: "Detect drift", sub: "re-scan, alert" },
  ]}
  edges={[
    { from: "surface", to: "enumerate" },
    { from: "enumerate", to: "minimize", label: "name or kill" },
    { from: "minimize", to: "defaults" },
    { from: "defaults", to: "baseline", label: "pin it" },
    { from: "baseline", to: "drift", label: "watch" },
  ]}
  caption="Hardening is a loop, not a one-time checklist: minimize the surface, lock the survivors with safe defaults, freeze the result as a baseline, then continuously detect drift back toward 'default'."
/>

## Secure defaults: the survivors must be safe out of the box

Whatever services survive minimization, their defaults are tuned for *getting started*, not for *production safety* — and those are opposite goals. Shipped defaults optimize for "works on first run with zero config," which means broad binds, verbose errors, permissive permissions, and sample credentials. Hardening flips every one of those to **deny by default**: a config starts from the most restrictive setting and opens only what's named.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Surface</th>
        <th class="border border-border px-3 py-2 text-left">Risky default</th>
        <th class="border border-border px-3 py-2 text-left">Hardened setting</th>
        <th class="border border-border px-3 py-2 text-left">Why it matters</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">SSH</td>
        <td class="border border-border px-3 py-2">Password auth + root login</td>
        <td class="border border-border px-3 py-2">Keys only, <code>PermitRootLogin no</code></td>
        <td class="border border-border px-3 py-2">Kills credential-stuffing and brute force</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">DB bind</td>
        <td class="border border-border px-3 py-2"><code>0.0.0.0</code>, sample password</td>
        <td class="border border-border px-3 py-2"><code>127.0.0.1</code>, rotated secret</td>
        <td class="border border-border px-3 py-2">Database not reachable from the internet</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Firewall</td>
        <td class="border border-border px-3 py-2">Allow-all inbound</td>
        <td class="border border-border px-3 py-2">Default-drop, allow named ports</td>
        <td class="border border-border px-3 py-2">Backstop for the service you forgot</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Services</td>
        <td class="border border-border px-3 py-2">Run as root</td>
        <td class="border border-border px-3 py-2">Unprivileged user, dropped caps</td>
        <td class="border border-border px-3 py-2">Compromise stays contained, not host-wide</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Errors</td>
        <td class="border border-border px-3 py-2">Verbose stack traces, banners</td>
        <td class="border border-border px-3 py-2">Generic errors, version hidden</td>
        <td class="border border-border px-3 py-2">Stops free recon for the attacker</td>
      </tr>
    </tbody>
  </table>
</div>

The two highest-value defaults to fix are authentication and privilege. SSH with password auth and root login enabled is the single most-brute-forced surface on the public internet; switching to key-only auth with `PermitRootLogin no` removes the entire credential-guessing class of attack. And the principle of **least privilege** applies to every process: a web server compromised while running as root is a compromised *host*; the same exploit against a service running as an unprivileged user with dropped Linux capabilities is a compromised *process* — the difference between an incident and a catastrophe.

<Inset kind="why" lang="en">
Why is removing a service strictly better than patching it? A patch is a promise about *known* bugs — it closes the CVEs disclosed so far, on the day you apply it, assuming you apply it on time (and the industry median time-to-patch is measured in weeks, not hours). A removed service has no attack surface at all: no zero-days, no patch lag, no agent to keep current, no config to drift. Patching is a race you run forever; removal ends the race. That's why "do we even need this?" is the first hardening question, asked before "is it patched?"
</Inset>

## Baselines: make the hardened state measurable and durable

A host you hardened by hand is hardened *once*. Three months and forty deploys later, someone re-enabled a service to debug an outage, a config-management run reverted a setting, an image rebuild dropped your firewall rules — and you have no idea, because "hardened" lived only in one engineer's memory. The fix is a **baseline**: a written, machine-checkable definition of the host's known-good secure state, plus continuous comparison against it.

This is exactly what the **CIS Benchmarks** provide — consensus-built, prescriptive configuration baselines per OS and platform, with each control specifying the audit check and the remediation. NIST SP 800-123 frames the same idea at the program level: harden to a documented standard, then *maintain* it. The baseline turns hardening from a heroic one-time effort into an enforced property:

- **Pin it as code** — encode the baseline in Ansible/Chef/Puppet or a golden image so every host is born hardened, not hardened afterward.
- **Audit continuously** — tools like OpenSCAP, `Lynis`, or a CIS-CAT scan grade a live host against the benchmark and emit a score plus a list of failed controls.
- **Alert on drift** — when a live host diverges from the baseline, that delta is a security event, not a footnote. The drift is often the first visible sign of either a sloppy change or an active intrusion.

<TradeoffMatrix
  id="03-os-host-hardening-tradeoff-1"
  lessonSlug="01-os-and-host-hardening"
  lang="en"
  prompt="A scan finds a CUPS print-spooler listening on a production web host that has no printers and serves only HTTP. Pick the best hardening response."
  options={[
    { name: "Disable and purge it: `systemctl disable --now cups` then remove the package, since the host has no printing function", summary: "Remove it.", correct: true, justification: "This is attack-surface reduction at its purest. The service serves no named function on this host, so the correct move is to delete it entirely — that closes every current and future CVE in CUPS for this box, with no agent to patch and no config to drift. Service minimization means: if you can't name the function and owner, it shouldn't run." },
    { name: "Leave it running but make sure it's on the latest patched version", summary: "Patch it.", justification: "Patching an unused service is strictly worse than removing it: you keep the attack surface, sign up for a perpetual patch race against future CVEs, and gain nothing — the host doesn't print. Removal closes bugs that haven't even been disclosed yet; patching only closes today's." },
    { name: "Add a firewall rule to block the CUPS port from external traffic and move on", summary: "Firewall it.", justification: "A firewall rule is a useful backstop, but it's a band-aid over a service that should not exist. It leaves the service reachable from localhost and any internal pivot, still running as a process that can be exploited, and still drifting. Default-deny firewalling complements minimization; it doesn't replace it." },
    { name: "Change the CUPS admin password to something strong and log access to it", summary: "Credential it.", justification: "Hardening the credentials of a service you don't need is effort spent in the wrong place. The service is still attack surface regardless of its password, and a strong password does nothing against an unauthenticated RCE in the daemon. The right answer is to remove the thing, not to guard it." },
  ]}
/>

<Quiz
  id="03-os-host-hardening-quiz-1"
  lessonSlug="01-os-and-host-hardening"
  lang="en"
  question="Why is removing an unused service generally a stronger control than keeping it patched?"
  choices={[
    { label: "A removed service has zero attack surface — no future CVEs, no patch lag, no agent or config to maintain", correct: true },
    { label: "Removing the service makes the host boot faster, which is the main security benefit", misconception: "Boot time isn't the security argument. The point is that removed code can't be exploited at all — it eliminates the entire future stream of vulnerabilities, not just today's." },
    { label: "Patches always introduce new bugs, so patching is never worth doing", misconception: "Patching is essential for services you keep. The claim isn't 'never patch' — it's that for a service you don't need, removal is strictly better than the perpetual patch race." },
    { label: "Firewalling the port is equivalent to removing the service", misconception: "A firewall hides a port from one direction; the service still runs, can still be reached via localhost or an internal pivot, and still accrues CVEs. Removal eliminates the surface entirely." },
  ]}
/>

<Quiz
  id="03-os-host-hardening-quiz-2"
  lessonSlug="01-os-and-host-hardening"
  lang="en"
  question="You harden a host by hand and it passes a CIS scan with a high score. Six weeks later, what is the most likely reason it's no longer compliant?"
  choices={[
    { label: "Configuration drift — a deploy, debug session, or config-management run silently reverted a hardened setting", correct: true },
    { label: "The CIS Benchmark expired and the host was automatically downgraded", misconception: "Benchmarks don't expire and hosts aren't auto-downgraded. The real enemy of a hardened host over time is drift: changes that quietly move it away from the baseline." },
    { label: "Hardening is permanent once applied, so it can't have changed", misconception: "That's exactly the dangerous assumption. Hardening applied by hand is a point-in-time state; without a pinned baseline and drift detection it decays with every change." },
    { label: "A new CVE was published, which lowers the CIS score automatically", misconception: "A CIS score grades configuration against the benchmark, not the CVE feed. A new CVE is a patching concern; the compliance drop here is about config settings changing." },
  ]}
/>

<DragOrder
  id="03-os-host-hardening-drag-1"
  lessonSlug="01-os-and-host-hardening"
  lang="en"
  prompt="Order the host-hardening workflow from first step to last:"
  items={[
    { id: "a", label: "Enumerate listeners and services (ss -tulpn) to map the real surface" },
    { id: "b", label: "Minimize: disable and purge every service you can't name" },
    { id: "c", label: "Apply secure defaults to the survivors (deny-by-default, least privilege)" },
    { id: "d", label: "Pin the hardened state as a CIS-aligned baseline (as code)" },
    { id: "e", label: "Continuously scan for drift and alert on divergence" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Host hardening is subtraction first: enumerate every listener, remove what you can't name, then flip the survivors to deny-by-default and least privilege. Removed code has no CVEs. Finally, pin the result as a CIS-aligned baseline and detect drift — because "hardened by hand" decays the moment someone deploys.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-os-host-hardening-retrieval"
  lang="en"
  questions={[
    { q: "What is a host's attack surface, and why is reducing it higher-leverage than patching?", a: "A host's attack surface is the union of every interface where untrusted input can reach your code: listening network ports, running services and their RPC endpoints, setuid binaries, loaded kernel modules, exposed filesystems, scheduled jobs, and the privileges each runs as. Every item is a potential entry point or escalation path. Reducing that surface — service minimization — is higher-leverage than patching because of an asymmetry: a patch fixes one known bug in a service on the day you apply it (and median time-to-patch is weeks), whereas removing the service eliminates every bug it will ever have, including undisclosed zero-days, with no agent to keep current and no config to drift. The hosts that survived classes like Shellshock and PrintNightmare untouched weren't the fastest patchers — they were the ones that never installed the vulnerable component. The first hardening question is 'do we even need this?', asked before 'is it patched?'." },
    { q: "What is a baseline, and why does hardening without one decay over time?", a: "A baseline is a written, machine-checkable definition of a host's known-good secure state — for example a CIS Benchmark, which gives consensus-built, prescriptive per-control settings each with an audit check and a remediation. Hardening done by hand is a point-in-time state that lives only in one engineer's memory; across dozens of deploys, debug sessions, config-management runs, and image rebuilds, settings silently revert and the host drifts back toward 'default' with nobody noticing until an incident. A baseline fixes this by making the hardened state durable and enforceable: pin it as code (Ansible/Chef/Puppet or a golden image) so hosts are born hardened, audit continuously with tools like OpenSCAP, Lynis, or CIS-CAT to grade live hosts against the benchmark, and treat any divergence from the baseline as a security event — drift is frequently the first visible sign of either a sloppy change or an active intrusion. NIST SP 800-123 frames the same loop: harden to a documented standard, then maintain it." },
  ]}
/>

<Recap lang="en">
Host hardening is the discipline of shrinking a machine to exactly the job it does and keeping it there. Start by mapping the attack surface — every listening port, service, setuid binary, and the privileges each runs as — because you can't be exploited through a service that isn't running. Minimize first: enumerate listeners with `ss -tulpn`, then disable and purge anything whose function and owner you can't name, since removed code has no CVEs and removal beats the perpetual patch race. Then make the survivors safe out of the box by flipping permissive defaults to deny-by-default and least privilege: key-only SSH with no root login, databases bound to localhost, default-drop firewalls, services running unprivileged with dropped capabilities. Finally, make it durable: pin the hardened state as a CIS-aligned baseline expressed as code, audit live hosts continuously with OpenSCAP or CIS-CAT, and treat any drift from the baseline as a security event — because a host hardened by hand is hardened exactly once, and decays with the next deploy. Now when you boot a fresh image and `ss -tlnp` shows listeners you never asked for, your first question isn't 'is it patched?' — it's 'can I name what this does, and if not, why is it running?'.
</Recap>
