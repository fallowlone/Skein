---
concepts:
- network-hardening
- egress-filtering
- segmentation
- secure-protocols
- default-deny
deepensInto: []
estMin: 15
lang: en
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-network-hardening
sources:
- https://www.cisecurity.org/cis-benchmarks
- https://csrc.nist.gov/pubs/sp/800/123/final
spiral: []
status: ready
summary: 'Default-deny networking blocks everything and allows only named flows — including egress, the direction most teams forget. Combined with segmentation and killing plaintext protocols, it turns one compromised box from a launchpad into a dead end.'
title: 'Network hardening'
track: security-defensive
unit: 03-hardening-defense-in-depth
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
An attacker pops a single image-resize worker through a dependency CVE. It's an internal box — no public ingress, nobody panics. Within ninety seconds it has connected outbound to `45.9.x.x:443`, pulled a second-stage payload, scanned the `10.0.0.0/8` flat network, found an unauthenticated Redis on a neighboring host, and dumped session tokens. Every one of those steps crossed a network boundary that allowed it. The ingress firewall everyone obsessed over never fired once: the worker initiated all of it. This is what an *un-hardened* network costs — not the breach of one box, but the free movement after it.
</Hook>

By the end of this lesson you'll know why default-deny has to cover egress, how segmentation contains the blast radius, and why killing plaintext protocols is non-negotiable.

<Crux>Default-deny networking blocks all traffic, then allows only named flows — including egress — and kills plaintext protocols.</Crux>

<Explanation>

## Default-deny is the only firewall posture that scales

A firewall rule set is a policy about what *may* happen on the wire. There are exactly two ways to write it. **Default-allow** permits everything and enumerates what to block — you are listing the attacks you've thought of, and every attack you haven't thought of gets through. **Default-deny** drops everything and enumerates what to *allow* — you are listing the flows your application actually needs, and everything else, known and unknown, is denied by the absence of a rule.

The asymmetry is the whole point. A default-allow list is a blocklist, and a blocklist is only ever as complete as your imagination on the day you wrote it. A default-deny list is an allowlist, and an allowlist fails *closed*: a new service, a new port, a forgotten debug endpoint, a freshly published CVE that opens an unexpected listener — none of them work until someone deliberately writes a rule. This is the same `deny-by-default` reflex that governs application authorization, applied one layer down at the network. CIS Benchmarks and NIST SP 800-123 both make the implicit-deny final rule the baseline for exactly this reason: it converts "did we remember to block this?" into "did we remember to allow this?", and the second question is one a reviewer can actually answer by reading the rule set.

The reason teams still ship default-allow is operational, not architectural: deny-by-default breaks things loudly until every legitimate flow is enumerated, and that enumeration is real work. But that pain is the feature — it forces you to know your own traffic.

## Egress is the half of the firewall everyone forgets

Walk into most networks and the ingress rules are tight and the egress rules are a single line: `allow all outbound`. That single line is what made the Hook scenario possible. Almost every modern attack chain depends on *outbound* connections the defender permitted: pulling a second-stage payload, beaconing to a command-and-control server, and exfiltrating the stolen data back out. An ingress-only firewall is a door with a deadbolt on the outside and the inside knob removed — it stops people walking in, and does nothing about someone already inside walking out with the silverware.

**Egress filtering** applies default-deny to the outbound direction: a workload may only initiate connections to the specific destinations its job requires. A payment worker that talks to one bank API and your database has no business opening a TLS connection to an arbitrary internet IP — so it can't, and the moment it tries, the attempt is denied *and logged*, which is often your earliest signal of compromise. The single highest-value egress rule on a cloud host is blocking outbound access to the instance metadata endpoint (`169.254.169.254`) from anything that doesn't need it: that one link-local address is what turns an SSRF or a popped container into stolen cloud credentials.

<FlowDiagram
  label="Two network postures after the same single-box compromise. Top path (default-allow egress, flat network): the popped worker freely connects out to C2, pulls a payload, scans the flat subnet, and reaches a neighboring Redis — full lateral movement. Bottom path (default-deny egress, segmented network): the same compromise hits a denied-and-logged egress attempt and a segment boundary, so it dead-ends at the one box."
  perRow={4}
  hue="var(--d-crypto)"
  nodes={[
    { id: "pop", label: "1 box popped", sub: "dependency CVE" },
    { id: "egress", label: "Egress attempt", sub: "out to C2" },
    { id: "allow", label: "Allow-all egress", sub: "succeeds" },
    { id: "spread", label: "Lateral movement", sub: "flat /8 + Redis" },
    { id: "deny", label: "Default-deny egress", sub: "denied + logged" },
    { id: "segment", label: "Segment boundary", sub: "blocks east-west" },
    { id: "dead", label: "Dead end", sub: "blast radius = 1" },
  ]}
  edges={[
    { from: "pop", to: "egress" },
    { from: "egress", to: "allow", label: "no egress rule" },
    { from: "allow", to: "spread", label: "free movement" },
    { from: "egress", to: "deny", label: "no matching allow" },
    { from: "deny", to: "segment" },
    { from: "segment", to: "dead", label: "contained" },
  ]}
  caption="Same compromise, two outcomes. Allow-all egress plus a flat network turns one box into a launchpad; default-deny egress plus segmentation turns it into a dead end. The breach is identical — the network hardening decides the blast radius."
/>

## Segmentation decides the blast radius

A **flat network** — one big `10.0.0.0/8` where every host can reach every other host on every port — means the blast radius of *any* compromise is the entire estate. Segmentation breaks the network into zones (subnets, VLANs, security groups, Kubernetes NetworkPolicies, service-mesh authorization) and enforces default-deny *between* them. East-west traffic — server-to-server, the direction lateral movement travels — now has to cross a boundary that, by default, says no.

The win is concrete: put the database tier in its own segment that only the application tier may reach on port 5432, and a compromised front-end web box cannot touch the database directly even though both live in "the network." Put each microservice in its own policy and a popped image-resizer cannot scan its neighbors. The classic worst case — one machine compromised, then unauthenticated internal services (a Redis, an Elasticsearch, an admin panel) reachable flat across the whole subnet — is precisely what segmentation removes. You are not preventing the first breach here; you are making sure the first breach is also the last box.

<Inset kind="why" lang="en">
Why isn't "we have a perimeter firewall" enough? Because the perimeter is a single eggshell: hard on the outside, soft everywhere within. The moment one thing inside is compromised — a phished laptop, a vulnerable dependency, a leaked credential — a flat interior offers zero friction to lateral movement. Segmentation plus internal default-deny is what *zero trust* operationalizes: stop treating "inside the network" as a trust boundary, and authenticate and authorize every hop as if it came from the open internet. The perimeter still matters; it's just no longer allowed to be the only control.
</Inset>

## Killing plaintext protocols

Every plaintext protocol on your network is a credential leak and a tamper vector waiting for anyone who can see the wire — and on a shared or cloud network, "see the wire" is cheaper than teams assume. The hardening rule is blunt: **no cleartext for anything that carries credentials, data, or control.** Replace the unencrypted protocol with its authenticated, encrypted successor, and then *disable the old one at the server* — not just "prefer" the new one, because a protocol still listening is a protocol an attacker can downgrade you onto.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Kill this</th>
        <th class="border border-border px-3 py-2 text-left">Port</th>
        <th class="border border-border px-3 py-2 text-left">Why it's dangerous</th>
        <th class="border border-border px-3 py-2 text-left">Use instead</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Telnet</td>
        <td class="border border-border px-3 py-2">23</td>
        <td class="border border-border px-3 py-2">Login + every keystroke in cleartext</td>
        <td class="border border-border px-3 py-2">SSH (22), key-based auth</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">FTP</td>
        <td class="border border-border px-3 py-2">21</td>
        <td class="border border-border px-3 py-2">Credentials + files in cleartext</td>
        <td class="border border-border px-3 py-2">SFTP / FTPS</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">HTTP (internal)</td>
        <td class="border border-border px-3 py-2">80</td>
        <td class="border border-border px-3 py-2">Tokens, cookies, data readable + tamperable</td>
        <td class="border border-border px-3 py-2">HTTPS / mTLS, HSTS</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">SNMP v1/v2c</td>
        <td class="border border-border px-3 py-2">161</td>
        <td class="border border-border px-3 py-2">Community string is a plaintext password</td>
        <td class="border border-border px-3 py-2">SNMPv3 (auth + privacy)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">LDAP</td>
        <td class="border border-border px-3 py-2">389</td>
        <td class="border border-border px-3 py-2">Bind credentials in cleartext</td>
        <td class="border border-border px-3 py-2">LDAPS / StartTLS</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">TLS 1.0 / 1.1, SSLv3</td>
        <td class="border border-border px-3 py-2">—</td>
        <td class="border border-border px-3 py-2">Known-broken ciphers, downgrade-able</td>
        <td class="border border-border px-3 py-2">TLS 1.2+ (prefer 1.3)</td>
      </tr>
    </tbody>
  </table>
</div>

"Internal HTTP is fine, it never leaves our network" is the dangerous half-truth: once you accept that an attacker can already be *inside* the network (the entire premise of segmentation), internal cleartext is exactly as exposed as external cleartext. Encrypt east-west traffic too — mutual TLS between services is the service-mesh answer — so that a foothold on one box doesn't hand the attacker a tap on everyone else's traffic.

<TradeoffMatrix
  id="02-network-hardening-tradeoff-1"
  lessonSlug="02-network-hardening"
  lang="en"
  prompt="A payment-processing service runs in a flat cloud VPC with allow-all egress. You can ship one change this sprint to most reduce the blast radius if that service is compromised. Pick the highest-leverage move."
  options={[
    { name: "Add default-deny egress: allow outbound only to the bank API and the database, deny + log everything else, and block the metadata endpoint", summary: "Cut the attacker's outbound.", correct: true, justification: "Almost every post-compromise step — pulling a second stage, beaconing to C2, exfiltrating data, stealing cloud credentials via 169.254.169.254 — needs an outbound connection the defender currently permits. Constraining egress to the two destinations this service actually uses denies all of that by absence of a rule and turns the first denied attempt into an alert. It directly attacks the free-movement phase that does the real damage." },
    { name: "Add a WAF in front of the service to inspect inbound requests for known attack signatures", summary: "Harden ingress only.", justification: "A WAF guards the front door against known inbound patterns, but the scenario's damage is all outbound and east-west after a compromise the WAF didn't stop (e.g. a dependency CVE). It does nothing about the attacker beaconing out or scanning neighbors, which is where an un-hardened network bleeds." },
    { name: "Rotate the service's credentials more frequently and lengthen them", summary: "Better secrets hygiene.", justification: "Good hygiene, but orthogonal to blast radius. Once the box is popped the attacker uses the live credentials and the live network from inside; shorter-lived secrets help marginally but don't stop outbound C2, lateral scanning, or metadata theft. It's not the highest-leverage network move." },
    { name: "Enable verbose flow logging on the VPC so you can investigate thoroughly after an incident", summary: "Observe, don't prevent.", justification: "Logging is necessary for detection and forensics, but on its own it's a passive control: it tells you the attacker moved freely, after they did. It pairs with default-deny (which is what generates the high-signal 'denied egress' events) — it is not a substitute for the boundary that actually contains the breach." },
  ]}
/>

<Quiz
  id="02-network-hardening-quiz-1"
  lessonSlug="02-network-hardening"
  lang="en"
  question="Why is a default-deny (allowlist) firewall fundamentally safer than a default-allow (blocklist) one?"
  choices={[
    { label: "It fails closed: any new port, service, or unforeseen attack is denied by the absence of an allow rule, instead of getting through because nobody wrote a block rule yet", correct: true },
    { label: "It is faster because the firewall evaluates fewer rules", misconception: "Performance isn't the point and isn't reliably better. The security difference is that an allowlist denies the unknown by default, whereas a blocklist only stops what you explicitly enumerated." },
    { label: "It makes encryption unnecessary because nothing unexpected can connect", misconception: "Default-deny controls which flows are allowed; it doesn't encrypt the allowed ones. You still need to kill plaintext protocols on the flows you do permit." },
    { label: "It blocks all outbound traffic permanently, so malware can never communicate", misconception: "Default-deny doesn't block everything forever — it denies by default and then explicitly allows the flows the app needs. The value is that unlisted flows (including malware C2) are denied, not that all egress is gone." },
  ]}
/>

<Quiz
  id="02-network-hardening-quiz-2"
  lessonSlug="02-network-hardening"
  lang="en"
  question="A service still has TLS 1.0 enabled 'for an old client' alongside TLS 1.3. Why is leaving the old version listening a real risk, not just legacy clutter?"
  choices={[
    { label: "An attacker can force a downgrade onto the weak version, so a protocol that is still offered is still attackable — preferring 1.3 isn't enough; you must disable the old one", correct: true },
    { label: "TLS 1.0 uses more CPU, so it's a performance problem more than a security one", misconception: "The issue is security, not CPU. A reachable weak protocol can be negotiated down to, exposing broken ciphers regardless of what the strong default is." },
    { label: "It's fine as long as TLS 1.3 is the default — the client picks the strongest both support", misconception: "An on-path attacker influences negotiation; 'both support' includes the weak one, and downgrade attacks exploit exactly that. Hardening means removing the weak option at the server, not just preferring the strong one." },
    { label: "TLS 1.0 can't be logged, so you lose audit trail", misconception: "Loggability isn't the concern. The concern is that an enabled weak protocol remains negotiable and therefore exploitable via downgrade." },
  ]}
/>

<DragOrder
  id="02-network-hardening-drag-1"
  lessonSlug="02-network-hardening"
  lang="en"
  prompt="Order these network-hardening moves from broadest blast-radius reduction to most local, as you'd phase them in:"
  items={[
    { id: "a", label: "Default-deny ingress AND egress as the firewall baseline (allow only named flows)" },
    { id: "b", label: "Segment the network into zones with default-deny between them (contain east-west)" },
    { id: "c", label: "Lock down the cloud metadata endpoint and per-service egress destinations" },
    { id: "d", label: "Disable plaintext protocols and weak TLS on each service's listeners" },
  ]}
  correctOrder={["a", "b", "c", "d"]}
/>

</Explanation>

<KeyTakeaway>Default-deny is the only firewall posture that fails closed — and it must cover egress, not just ingress, because post-compromise movement is outbound. Segment the network so one popped box can't reach the rest, lock down the metadata endpoint, and disable every plaintext protocol and weak TLS version at the server. The breach is inevitable; the blast radius is your design choice.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-network-hardening-retrieval"
  lang="en"
  questions={[
    { q: "Explain why egress filtering matters as much as ingress filtering, and give the single highest-value egress rule on a cloud host.", a: "Ingress filtering stops attackers from connecting in, but almost every post-compromise step is an outbound connection the defender permitted: pulling a second-stage payload, beaconing to a command-and-control server, and exfiltrating stolen data. A firewall with tight ingress and a blanket 'allow all outbound' rule does nothing to stop any of that — the compromised workload initiates the connections from the inside. Egress filtering applies default-deny to the outbound direction so a workload may only connect to the specific destinations its job needs; everything else is denied by the absence of a rule, and the denied attempt is logged, which is often the earliest signal of compromise. The single highest-value egress rule on a cloud host is blocking outbound access to the instance metadata endpoint (169.254.169.254) from anything that doesn't legitimately need it, because that link-local address is what turns an SSRF or a popped container into stolen cloud credentials." },
    { q: "How do segmentation and killing plaintext protocols together contain a single-box compromise, and why is 'internal HTTP is fine' wrong?", a: "Segmentation breaks a flat network — where any host can reach any other on any port, so any compromise has the whole estate as its blast radius — into zones (subnets, VLANs, security groups, Kubernetes NetworkPolicies, service-mesh authz) with default-deny enforced between them. East-west, server-to-server traffic, which is the direction lateral movement travels, now has to cross a boundary that says no by default, so a popped web box can't reach the database tier and a popped image-resizer can't scan its neighbors: the first breach becomes the last box. Killing plaintext protocols closes the complementary gap. 'Internal HTTP is fine, it never leaves our network' is wrong precisely because segmentation assumes an attacker can already be inside the network; once that's true, internal cleartext is as exposed as external cleartext — credentials, tokens, and data are readable and tamperable by anyone with a foothold on a neighboring box. The fix is to encrypt east-west traffic too (mutual TLS between services) and disable old protocols (Telnet, FTP, SNMPv1/2c, plain LDAP) and weak TLS versions at the server, since a protocol still listening can be downgraded onto." },
  ]}
/>

<Recap lang="en">
Network hardening is the discipline of deciding the blast radius of a compromise before it happens. The foundation is default-deny: a firewall that drops everything and allows only the named flows your application needs, so unknown and future attacks are denied by the absence of a rule rather than slipping past an incomplete blocklist. Crucially, default-deny has to cover egress, not just ingress — post-compromise movement (second-stage payloads, C2 beaconing, data exfiltration, stealing cloud credentials from 169.254.169.254) is all outbound, so a blanket "allow all outbound" rule is the gap most teams leave open. Segmentation enforces default-deny between zones so east-west lateral movement hits a boundary, turning one popped box into a dead end instead of a launchpad across a flat network. And every plaintext protocol — Telnet, FTP, internal HTTP, SNMPv1/2c, plain LDAP — plus weak TLS versions must be disabled at the server, not merely deprecated, because a protocol still listening can be downgraded onto. Now when you see a service with allow-all egress on a flat network, your first question is: when this box is popped, can it reach anything but the two destinations it actually needs?
</Recap>
