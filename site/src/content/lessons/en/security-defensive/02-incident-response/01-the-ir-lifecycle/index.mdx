---
concepts:
- incident-response
- nist-ir
- preparation
- detection-analysis
- containment-eradication-recovery
deepensInto: []
estMin: 15
lang: en
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-the-ir-lifecycle
sources:
- https://csrc.nist.gov/pubs/sp/800/61/r2/final
- https://attack.mitre.org/
spiral: []
status: ready
summary: 'NIST splits incident response into four phases, but the one nobody is
  watching during an outage — Preparation — quietly decides the outcome of the
  other three. This is why the work you do before the pager fires is the work
  that saves you.'
title: The IR lifecycle
track: security-defensive
unit: 02-incident-response
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
2:47 a.m. An alert fires: a service account is reading rows it has never touched, fast. The on-call engineer is awake, competent, and completely stuck. Who declares an incident? Where is the runbook? Does anyone have the cloud permissions to pull the IAM logs, and is anyone even *allowed* to power off a production node? They burn forty minutes paging people who don't answer, find the wiki page is a year stale, and watch the attacker finish exfiltration while the team argues about whether this is "really" an incident. Nothing that happened at 2:47 was a technical failure. Every one of those forty minutes was lost in a decision that should have been made — and written down — months earlier, on a calm afternoon, by someone who is now asleep.
</Hook>

By the end of this lesson you'll know the four NIST incident-response phases, how they feed back into each other, and why the phase that runs *before* any incident is the one that decides whether the other three go well or badly.

<Crux>NIST splits IR into four phases — but the work done in Preparation, before anything fires, quietly decides how the other three go.</Crux>

<Explanation>

## The model: four phases, not a straight line

When people picture incident response they picture the middle of it — the war room, the bridge call, the frantic typing. NIST SP 800-61 (the field's reference, *Computer Security Incident Handling Guide*) frames it more usefully as a **lifecycle of four phases that loop**, not a script you run once top to bottom:

1. **Preparation** — everything you build *before* an incident: the response team and its authority, runbooks, log pipelines, tooling, retained backups, contact lists, and the practice runs that make all of it real.
2. **Detection & Analysis** — noticing that something happened and figuring out what: triaging an alert, confirming it's a true incident, scoping how far it reaches, and assigning severity.
3. **Containment, Eradication & Recovery** — stopping the bleeding, removing the attacker's access and artifacts, then restoring service to a known-good state and confirming it holds.
4. **Post-Incident Activity** — the "lessons learned" pass: a blameless review that turns what you just lived through into improvements.

The arrow that matters most is the one that loops the last phase back to the first. **Post-Incident Activity feeds Preparation.** A finding from one incident — "we had no log of service-account reads" — becomes a Preparation task that makes the *next* Detection phase faster. An IR program that skips the loop relives the same incident every quarter; one that closes it gets measurably harder to breach over time.

<FlowDiagram
  label="The NIST SP 800-61 incident-response lifecycle as a loop: Preparation feeds Detection and Analysis, which feeds Containment, Eradication and Recovery, which feeds Post-Incident Activity; the critical feedback arrow runs from Post-Incident Activity back into Preparation, so every incident makes the next response faster."
  perRow={4}
  hue="var(--d-crypto)"
  nodes={[
    { id: "prep", label: "Preparation", sub: "before anything" },
    { id: "detect", label: "Detect & Analyze", sub: "is it real?" },
    { id: "contain", label: "Contain/Erad/Recover", sub: "stop & restore" },
    { id: "post", label: "Post-incident", sub: "lessons learned" },
  ]}
  edges={[
    { from: "prep", to: "detect" },
    { from: "detect", to: "contain" },
    { from: "contain", to: "post" },
    { from: "post", to: "prep", label: "feeds back" },
  ]}
  caption="The lifecycle is a loop, not a line. The highest-leverage edge is the dashed feedback from Post-Incident Activity into Preparation: each incident's findings become Preparation tasks that make the next Detection and Containment faster. Programs that skip this edge relive the same incident; programs that close it harden over time."
/>

## Why Preparation decides the outcome

Here is the senior insight that the four-box diagram hides: **the phases are not equal in leverage.** During an actual incident, the clock is running and your options are fixed — you can only respond with the access, logs, tooling, and authority that already exist. You cannot decide *during* the breach to start retaining the logs you'll need to scope it. You cannot grant yourself the cloud permission to isolate a node while the attacker is using it. You cannot write, socialize, and rehearse a runbook at 2:47 a.m. Every capability you reach for under pressure was either built in Preparation or it simply isn't there.

This is why mature teams measure two numbers: **MTTD** (mean time to detect) and **MTTR** (mean time to respond/recover). Both are dominated by Preparation. MTTD is short only if you were already logging the right events with the right context and had detections watching them — that's the previous unit's work, cashed in here. MTTR is short only if the runbook exists, the responder has standing authority to act, and the recovery path (clean backups, infrastructure-as-code to rebuild) was tested before the day you need it. The breach in the Hook lost forty minutes not to a hard technical problem but to *missing Preparation*: no clear incident commander, a stale runbook, no pre-granted permissions. The attack succeeded in the gaps the team never closed on a calm afternoon.

<Inset kind="why" lang="en">
Why does NIST put **practice** inside Preparation, not treat it as optional polish? Because an untested plan is a hypothesis, and incidents are a terrible time to test hypotheses. Teams run *tabletop exercises* (talk through a scenario around a table) and *game days* (inject a real failure and respond live) precisely to discover the gaps cheaply: the contact list is wrong, the runbook assumes a tool that was decommissioned, nobody actually has the IAM role the playbook requires. Each gap found in a drill is one you didn't find at 2:47 a.m. with an attacker on the keyboard. A plan that has never been exercised should be assumed broken in at least one load-bearing place.
</Inset>

## Detection, Containment, and the tradeoffs inside them

The middle phases carry their own senior judgment calls. **Detection & Analysis** is where you resist two opposite failures: declaring an incident on every noisy alert (alert fatigue, wasted response), or dismissing a real one as noise. The discipline is triage — confirm it's a true positive, then *scope* it (what's affected, how far, since when) before you act, because containment decisions are only as good as your understanding of blast radius. ATT&CK is the shared vocabulary that makes scoping concrete: naming the adversary's techniques (credential access, lateral movement, exfiltration) tells you where else to look before you declare the incident bounded.

**Containment** hides the sharpest tradeoff in the whole lifecycle: *containment versus evidence*. The instinct is to pull the plug — power off the compromised box the instant you find it. But a hard power-off destroys volatile evidence (memory, live network connections, the malware's running state) and tips the attacker off, who may have other footholds you haven't found yet. Isolating the host on the network instead preserves forensics and may let you watch the attacker's other moves. Yet waiting too long lets exfiltration continue. There is no universal right answer — only a judgment weighing active damage against the value of evidence and the risk of a wider, undetected presence.

<TradeoffMatrix
  id="01-the-ir-lifecycle-tradeoff-1"
  lessonSlug="01-the-ir-lifecycle"
  lang="en"
  prompt="You find one compromised host actively exfiltrating data, and ATT&CK-mapped signals suggest the attacker used valid credentials and may have moved laterally. Pick the best first containment move."
  options={[
    { name: "Network-isolate the host (cut its egress / quarantine it) while preserving it powered-on, and rotate the credentials you believe were used", summary: "Isolate, don't destroy.", correct: true, justification: "This stops the active exfiltration (cuts egress) without destroying volatile evidence — memory, live connections, running malware state — that you need to scope a lateral-movement attacker. Rotating the implicated credentials closes the access path that ATT&CK signals pointed at, addressing the likelihood that other footholds share that path. It buys containment and keeps your options open to find the rest of the intrusion." },
    { name: "Immediately hard power-off the compromised host to stop everything fast", summary: "Pull the plug.", justification: "A hard power-off does stop that one host's exfiltration, but it destroys volatile evidence (RAM, live connections, malware runtime state), tips off an attacker who likely has other footholds (the lateral-movement signal), and leaves those other footholds untouched. You've blinded your own investigation and only contained one node of a multi-node intrusion." },
    { name: "Leave the host running and untouched while you spend the next few hours fully scoping the entire intrusion first", summary: "Observe, act later.", justification: "Scoping matters, but data is actively leaving right now. Letting confirmed exfiltration continue for hours to perfect your picture trades real, ongoing damage for analytical completeness. You can cut egress (stopping the bleed) and continue scoping in parallel — you don't have to choose passivity to preserve evidence." },
    { name: "Reimage the host from a known-good template immediately to restore it to service", summary: "Rebuild now.", justification: "Reimaging is a Recovery step, and doing it first is the worst of both worlds: it destroys all evidence like a power-off does, AND it can restore the host straight back into a still-compromised environment (the lateral-movement path is still open), so it gets reinfected. Recovery comes after containment and eradication, not instead of them." },
  ]}
/>

<Quiz
  id="01-the-ir-lifecycle-quiz-1"
  lessonSlug="01-the-ir-lifecycle"
  lang="en"
  question="Two teams have identical tooling and engineers. Team A rehearses its IR plan quarterly with tabletop exercises and game days; Team B wrote a plan two years ago and filed it. Why does Team A respond dramatically faster to a real incident?"
  choices={[
    { label: "Practice is part of Preparation — drills surface the broken contact lists, stale runbooks, and missing permissions before the incident, so none of them cost time during it", correct: true },
    { label: "Because rehearsing makes the attacker's job harder, lowering the incident's severity", misconception: "Drills don't change the attacker or the attack. They change your readiness — they find and fix the gaps in your own response capability before the incident, which is why Preparation work shortens MTTR." },
    { label: "Because a recently-written plan is automatically more technically correct than an old one", misconception: "Recency isn't the mechanism — an unexercised new plan can be just as broken. It's the act of testing the plan that surfaces the load-bearing gaps, not how recently it was typed up." },
    { label: "Because Team B skipped the Detection phase entirely by not rehearsing", misconception: "Both teams still have detection tooling. The difference is that Team A's Preparation (including practice) makes detection and response faster; rehearsal doesn't add or remove a phase." },
  ]}
/>

<Quiz
  id="01-the-ir-lifecycle-quiz-2"
  lessonSlug="01-the-ir-lifecycle"
  lang="en"
  question="During a post-incident review, the team finds the breach went undetected for days because service-account database reads were never logged. In the NIST lifecycle, where does the fix belong?"
  choices={[
    { label: "Preparation — Post-Incident Activity feeds back into Preparation; adding that logging is a Preparation task that shortens the next Detection phase", correct: true },
    { label: "Detection & Analysis — it's a detection problem, so the fix lives in that phase", misconception: "The problem surfaced in Detection, but you can't detect what you never logged. The durable fix — building the missing log pipeline — is Preparation work; that's exactly the feedback loop from Post-Incident Activity into Preparation." },
    { label: "Containment — better logging would have contained the incident sooner", misconception: "Logging isn't a containment action; it's a capability you build beforehand. Containment is stopping an in-progress incident, which can't happen until detection fires, which can't happen without the logs Preparation provides." },
    { label: "It doesn't belong anywhere — logging is an operations concern, not part of IR", misconception: "Log pipelines for security events are core Preparation. The whole point of the post-incident loop is to turn findings like this into Preparation improvements." },
  ]}
/>

<DragOrder
  id="01-the-ir-lifecycle-drag-1"
  lessonSlug="01-the-ir-lifecycle"
  lang="en"
  prompt="Order the NIST SP 800-61 incident-response phases as they run, ending with the phase that loops back to the first:"
  items={[
    { id: "a", label: "Preparation — build the team, runbooks, logs, and tooling before anything fires" },
    { id: "b", label: "Detection & Analysis — confirm it's a real incident and scope the blast radius" },
    { id: "c", label: "Containment, Eradication & Recovery — stop it, remove access, restore to known-good" },
    { id: "d", label: "Post-Incident Activity — blameless review that feeds back into Preparation" },
  ]}
  correctOrder={["a", "b", "c", "d"]}
/>

</Explanation>

<KeyTakeaway>The NIST IR lifecycle is four looping phases: Preparation, Detection & Analysis, Containment/Eradication/Recovery, and Post-Incident Activity. Preparation has the most leverage — during an incident you can only use the access, logs, runbooks, and authority that already exist. Close the loop: each incident's lessons become Preparation tasks that make the next response faster.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-the-ir-lifecycle-retrieval"
  lang="en"
  questions={[
    { q: "Name the four NIST SP 800-61 incident-response phases, what each does, and explain the feedback loop between them.", a: "The four phases are: (1) Preparation — everything built before an incident: the response team and its authority, runbooks, log pipelines, tooling, tested backups, contact lists, and the tabletop exercises and game days that make all of it real. (2) Detection & Analysis — noticing something happened and confirming it: triaging an alert to a true positive, then scoping it (what's affected, how far, since when) and assigning severity before acting. (3) Containment, Eradication & Recovery — stopping the active damage, removing the attacker's access and artifacts, then restoring service to a known-good state and confirming it holds. (4) Post-Incident Activity — a blameless lessons-learned review that turns the experience into improvements. The critical feedback loop runs from Post-Incident Activity back into Preparation: a finding like 'we never logged service-account reads' becomes a Preparation task that shortens the next Detection phase. The lifecycle is a loop, not a one-way script — a program that closes that loop gets measurably harder to breach over time, while one that skips it relives the same incident repeatedly." },
    { q: "Why is Preparation the highest-leverage phase, and how does it show up in MTTD and MTTR? Include the containment-versus-evidence tradeoff.", a: "Preparation has the most leverage because during an actual incident the clock is running and your options are fixed: you can only respond with the access, logs, tooling, and authority that already exist. You cannot start retaining the logs you need to scope the breach during the breach, grant yourself the cloud permission to isolate a node while the attacker is using it, or write and rehearse a runbook at 2:47 a.m. This is why MTTD (mean time to detect) and MTTR (mean time to respond/recover) are both dominated by Preparation: MTTD is short only if you were already logging the right events with the right context and had detections watching them; MTTR is short only if the runbook exists, the responder has standing authority, and the recovery path (clean backups, infrastructure-as-code) was tested in advance. Practice (tabletop exercises, game days) lives inside Preparation because an unexercised plan is a hypothesis that surfaces its broken contact lists, stale runbooks, and missing permissions at the worst possible time. The containment-versus-evidence tradeoff sits inside the middle phase: a hard power-off of a compromised host stops it fast but destroys volatile evidence (memory, live connections, malware runtime state) and tips off an attacker who may hold other footholds; network-isolating the host instead preserves forensics and your ability to scope a lateral-movement intrusion while still cutting egress. There's no universal answer — you weigh active damage against the value of evidence and the risk of a wider, undetected presence." },
  ]}
/>

<Recap lang="en">
NIST SP 800-61 frames incident response as a lifecycle of four phases that loop, not a one-way script: Preparation (the team, runbooks, logs, tooling, backups, and rehearsals you build beforehand), Detection & Analysis (confirm it's real, then scope the blast radius before acting), Containment/Eradication/Recovery (stop the bleeding, remove access, restore to known-good), and Post-Incident Activity (a blameless review). The highest-leverage edge is the feedback arrow from Post-Incident Activity back into Preparation — each incident's findings become Preparation tasks that make the next response faster. Preparation dominates because during an incident you can only use the access, logs, authority, and tooling that already exist; both MTTD and MTTR are decided long before the pager fires. Practice is part of Preparation, not optional, because an unexercised plan is a hypothesis. And inside containment lives the sharpest tradeoff: a hard power-off stops one host but destroys evidence and tips off an attacker with other footholds, while network isolation preserves forensics and cuts egress at once. Next time you read a green incident-response plan, the senior question is: has anyone ever actually run it — and on the day it fires, will the responder already have the access and the authority to act?
</Recap>
