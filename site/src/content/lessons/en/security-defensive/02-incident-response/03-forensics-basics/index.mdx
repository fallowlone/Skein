---
concepts:
- forensics
- chain-of-custody
- disk-memory-forensics
- timeline
- indicators-of-compromise
deepensInto: []
estMin: 15
lang: en
lessonType: topic
level: middle
order: 3
prereqs: []
slug: 03-forensics-basics
sources:
- https://csrc.nist.gov/pubs/sp/800/61/r2/final
- https://attack.mitre.org/
spiral: []
status: ready
summary: 'Evidence is only as good as how you handled it. Collect volatile data before it evaporates, hash and log every artifact for chain of custody, extract IOCs, and stitch a timeline that turns scattered logs into a defensible story.'
title: Forensics basics
track: security-defensive
unit: 02-incident-response
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
3 a.m. page: a payments pod is making outbound connections to an IP nobody recognizes. Your first instinct — the wrong one — is to SSH in and start `grep`-ing through logs to see what's happening. You just wrote to the filesystem, bumped the access time on every file you `cat`-ed, and spawned processes that overwrote pages of RAM that held the attacker's now-vanished reverse shell. The intrusion is still there; the *evidence* of how it got in just got quieter. Forensics is the discipline of answering "what happened, how, and can we prove it" — and almost every fatal mistake is made in the first ten minutes, by a well-meaning engineer touching the wrong thing first.
</Hook>

By the end of this lesson you'll know the order to collect evidence in, how chain of custody keeps it trustworthy, what an IOC is, and how a timeline turns a pile of artifacts into a story you can defend.

<Crux>Preserve volatile data first, hash and log every artifact for chain of custody, then build a timeline that turns IOCs into a story.</Crux>

<Explanation>

## Order of volatility: collect the fragile stuff first

The single most important rule in evidence collection is **order of volatility**: capture the data that disappears fastest, first. RAM is gone the instant the box reboots or the pod is rescheduled. Network connection state, running processes, and open file handles vanish within seconds to minutes. Disk survives a reboot; an off-site log or backup survives the disk. So you collect *up* the volatility ladder — memory before disk, live state before stored state — because every minute you spend on the durable stuff is a minute the fragile stuff is decaying.

This is exactly why "just SSH in and look around" is so destructive. Reading files updates access timestamps. Running tools allocates memory, overwriting the unallocated RAM where deleted-but-not-yet-reclaimed artifacts (an attacker's in-memory payload, a freed buffer with a credential) still live. Every command is an observer effect: the act of looking changes what you can later prove. The senior move is the opposite reflex — **acquire before you analyze**. Snapshot the volatile state to *somewhere else*, then do your poking on the copy.

In cloud and Kubernetes the rule still holds, the mechanics just shift. You don't image a physical disk; you snapshot the EBS volume, dump the container's memory via the node before the pod is killed, and — critically — set the compromised instance's auto-scaling and restart policy to *not* terminate it, because a rescheduled pod is a wiped crime scene. Cordon the node, snapshot, then investigate the snapshot.

<FlowDiagram
  label="The forensic acquisition pipeline ordered by volatility: capture RAM first because it dies on reboot, then live network and process state, then snapshot the disk which survives a reboot, then pull durable off-host logs; every artifact is SHA-256 hashed and recorded in the chain-of-custody log before any analysis begins on the copies."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "ram", label: "Memory", sub: "dies on reboot" },
    { id: "live", label: "Live state", sub: "conns, procs" },
    { id: "disk", label: "Disk image", sub: "snapshot" },
    { id: "logs", label: "Off-host logs", sub: "durable" },
    { id: "hash", label: "Hash + log", sub: "SHA-256" },
  ]}
  edges={[
    { from: "ram", to: "live", label: "then" },
    { from: "live", to: "disk", label: "then" },
    { from: "disk", to: "logs", label: "then" },
    { from: "logs", to: "hash", label: "for each" },
  ]}
  caption="Acquire up the volatility ladder — memory, then live state, then a disk snapshot, then durable off-host logs — and hash every artifact into the chain-of-custody log before you analyze the copies. Analysis happens on copies; the originals stay sealed."
/>

## Chain of custody: evidence you can defend

An artifact is only as trustworthy as the record of who touched it and when. **Chain of custody** is the unbroken, documented trail proving an artifact wasn't altered between collection and conclusion: who collected it, when, with what tool, where it was stored, and every hand it passed through. The technical anchor is a **cryptographic hash** — you SHA-256 the memory dump or disk image at acquisition, write that hash down, and anyone can re-hash the copy later and confirm it matches bit-for-bit. A broken hash means the evidence was modified; a matching hash means it's pristine.

The discipline that protects the hash is **work on copies, never originals**. Image the disk, hash the image, then do all analysis against a mounted *read-only* copy. The original (the snapshot, the sealed image) never gets touched again. This isn't bureaucratic theater — if the incident becomes a legal matter, a regulatory disclosure, or even just an internal dispute about blame, evidence with a broken or missing custody chain is worthless. You'll know *what* happened and be unable to *prove* it, which in a breach-notification or insurance context is the same as not knowing.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Artifact</th>
        <th class="border border-border px-3 py-2 text-left">Volatility</th>
        <th class="border border-border px-3 py-2 text-left">What it tells you</th>
        <th class="border border-border px-3 py-2 text-left">Collection note</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">RAM / process memory</td>
        <td class="border border-border px-3 py-2">Seconds — gone on reboot</td>
        <td class="border border-border px-3 py-2">In-memory payloads, decrypted keys, live shells</td>
        <td class="border border-border px-3 py-2">Dump first; never reboot the box</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Network + process state</td>
        <td class="border border-border px-3 py-2">Seconds–minutes</td>
        <td class="border border-border px-3 py-2">C2 connections, listening ports, parent PIDs</td>
        <td class="border border-border px-3 py-2">Capture before killing the pod</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Disk image</td>
        <td class="border border-border px-3 py-2">Survives reboot</td>
        <td class="border border-border px-3 py-2">Dropped files, persistence, deleted artifacts</td>
        <td class="border border-border px-3 py-2">Snapshot, hash, mount read-only</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Off-host logs</td>
        <td class="border border-border px-3 py-2">Durable (if shipped)</td>
        <td class="border border-border px-3 py-2">Auth events, API calls, the timeline spine</td>
        <td class="border border-border px-3 py-2">Attacker can't edit what already left the host</td>
      </tr>
    </tbody>
  </table>
</div>

## IOCs: the fingerprints you search for and share

An **Indicator of Compromise (IOC)** is an observable artifact that signals an intrusion: a malicious IP or domain, a file hash, a suspicious registry key or cron entry, a user-agent string, a mutex name. IOCs do two jobs. First, **scoping**: once you have one — say the SHA-256 of a dropped binary — you sweep every host for that hash to find the full blast radius, not just the box that paged you. Second, **sharing**: IOCs are the lingua franca of threat intel, so an IP you extract can be fed to detection rules and shared with peers.

The senior caveat: IOCs sit at the *bottom* of the **Pyramid of Pain**. Hashes and IPs are trivial for an attacker to change — a recompile gives a new hash, a new VPS gives a new IP. They're cheap to collect and cheap to evade. The durable signal is higher up: **TTPs** — the tactics, techniques, and procedures the adversary uses, the thing MITRE ATT&CK catalogs. Mapping your findings to an ATT&CK technique (say T1059, command-and-scripting interpreter) survives the attacker swapping infrastructure, because changing *how* they operate is genuinely expensive. So extract IOCs to scope *this* incident, but reach for the behavior to build detection that catches the *next* one.

<Inset kind="why" lang="en">
Why hash the disk image *before* you do anything with it, instead of just trusting your own copy? Because the value of evidence is in being able to prove it's unchanged, and you can only prove that against a number recorded at the moment of acquisition. If you analyze first and hash later, your hash certifies a *possibly-already-mutated* file — it proves nothing about the original state. The hash is a seal applied at collection time; apply it late and the seal is meaningless. This is the same logic as signing a contract before edits, not after.
</Inset>

## The timeline: turning artifacts into a story

A pile of artifacts isn't an answer. The deliverable of forensics is a **timeline**: a single, time-ordered sequence that fuses events from every source — auth logs, process-creation events, file modification times (the MAC times: modified, accessed, changed/created), network connections, deploy logs — into one narrative of what the attacker did, in order. This is where **supertimelining** tools earn their keep: they normalize timestamps from dozens of artifact types onto one axis so you can read the intrusion top to bottom.

The two hard problems are time and gaps. **Time:** correlating across sources demands a common reference — normalize everything to UTC, and watch for clock skew between hosts and for an attacker who *tampered with timestamps* to hide (timestomping; a file whose modified time predates its inode-change time is a tell). **Gaps:** the absence of a log is itself evidence — a deleted shell-history file or a window where audit logging was off tells you where the attacker worked and that they tried to cover it. A good timeline doesn't just say *what* happened; lined up against MITRE ATT&CK it shows the *progression* — initial access, execution, persistence, lateral movement — and that progression is what tells you whether you've actually scoped the whole intrusion or only seen its tail.

<TradeoffMatrix
  id="03-forensics-basics-tradeoff-1"
  lessonSlug="03-forensics-basics"
  lang="en"
  prompt="A production pod is compromised and actively beaconing out. You need to investigate. What do you do first?"
  options={[
    { name: "Cordon the node and snapshot the pod's memory and disk before anything else, then analyze the copies", summary: "Acquire before analyze.", correct: true, justification: "This respects order of volatility: memory and live state are captured before they can be lost to a reschedule or reboot, and the disk snapshot is preserved. Analysis then happens on copies, leaving the originals sealed and hashable for chain of custody. You lose nothing and can prove everything." },
    { name: "SSH in and start grepping logs and running ps/netstat to see what's happening live", summary: "Look around first.", justification: "Every command you run allocates memory (overwriting volatile artifacts) and updates access timestamps, corrupting the very evidence you're trying to read. This is the classic first-ten-minutes mistake: the act of looking changes what you can later prove. Acquire a memory dump first, then investigate the copy." },
    { name: "Immediately kill and restart the pod to stop the beaconing, then look at what's left", summary: "Kill it fast.", justification: "Killing the pod stops the bleeding but destroys all volatile evidence — the in-memory payload, live connections, and process tree vanish, and a rescheduled pod is a wiped crime scene. Containment matters, but isolate at the network layer (cordon, block egress) so you stop the attack without erasing how it happened." },
    { name: "Analyze the disk first to find the dropped files, then hash the image once you know what you're looking at", summary: "Hash later.", justification: "Hashing after analysis certifies a possibly-already-modified file, which proves nothing about the original state. The hash must be applied at acquisition to be a valid seal — and you'd also be skipping the more volatile memory and live state entirely. Order of volatility and hash-at-collection are both violated." },
  ]}
/>

<Quiz
  id="03-forensics-basics-quiz-1"
  lessonSlug="03-forensics-basics"
  lang="en"
  question="Why is RAM collected before the disk image during evidence acquisition?"
  choices={[
    { label: "Order of volatility — memory is lost on reboot/reschedule, while disk survives, so you capture the most fragile data first", correct: true },
    { label: "Because RAM is smaller and faster to copy than a disk", misconception: "Speed isn't the reason. The principle is order of volatility: you collect what decays fastest first, regardless of size. Memory must be captured before any action that could lose it." },
    { label: "Because the disk image doesn't need to be hashed but memory does", misconception: "Both get hashed for chain of custody. The ordering is about volatility — memory vanishes on reboot, disk doesn't — not about which artifacts need integrity protection." },
    { label: "Because disk evidence is never useful in cloud environments", misconception: "Disk evidence is very useful — dropped files, persistence, deleted artifacts. In the cloud you snapshot the volume rather than image a physical disk, but you still collect it, just after the volatile state." },
  ]}
/>

<Quiz
  id="03-forensics-basics-quiz-2"
  lessonSlug="03-forensics-basics"
  lang="en"
  question="You extract the IP and file hash of an attacker's tool. Why isn't building detection solely on those the strongest move?"
  choices={[
    { label: "IOCs like IPs and hashes sit at the bottom of the Pyramid of Pain — trivial to change; mapping the behavior (TTP / ATT&CK technique) is the durable signal", correct: true },
    { label: "IP and hash IOCs are illegal to store without a warrant", misconception: "There's no legal bar to recording observable indicators from your own systems. The real limitation is that hashes and IPs are cheap for an attacker to rotate, so detection built only on them is brittle." },
    { label: "Hashes and IPs can't be shared with other teams or tools", misconception: "They're highly shareable — IOCs are the common currency of threat intel. The issue is durability, not shareability: an attacker recompiles or changes VPS and your indicator is dead." },
    { label: "Because timelines make IOCs unnecessary", misconception: "Timelines and IOCs do different jobs; IOCs scope the blast radius and feed detection. The point is to also capture the higher-value TTPs, which survive infrastructure swaps." },
  ]}
/>

<DragOrder
  id="03-forensics-basics-drag-1"
  lessonSlug="03-forensics-basics"
  lang="en"
  prompt="Order an evidence-collection sequence correctly, from first to last:"
  items={[
    { id: "a", label: "Dump process / system memory (lost on reboot)" },
    { id: "b", label: "Capture live network connections and running processes" },
    { id: "c", label: "Snapshot and image the disk (survives reboot)" },
    { id: "d", label: "Collect durable off-host logs already shipped away" },
    { id: "e", label: "Hash each artifact and record the chain of custody" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Acquire before you analyze: collect up the volatility ladder (memory → live state → disk → off-host logs), hash every artifact at collection and work only on copies for chain of custody, extract IOCs to scope the blast radius, and fuse everything into a UTC-normalized timeline mapped to ATT&CK.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-forensics-basics-retrieval"
  lang="en"
  questions={[
    { q: "Explain order of volatility and why 'just SSH in and look around' corrupts evidence.", a: "Order of volatility says collect the data that disappears fastest, first: RAM is lost the instant a host reboots or a pod is rescheduled, live network and process state vanish in seconds to minutes, disk survives a reboot, and off-host logs survive the disk. You acquire up that ladder — memory, then live state, then a disk snapshot, then durable logs — because every minute spent on durable artifacts is a minute the fragile ones decay. 'Just SSH in and look around' is destructive because the act of investigating changes the evidence: reading files updates their access timestamps, and running any tool allocates memory that overwrites the unallocated RAM where an attacker's in-memory payload or a freed credential buffer still lives. The senior reflex is 'acquire before analyze' — snapshot the volatile state somewhere else first, then do all poking on the copy. In Kubernetes the same logic means cordon the node and dump memory before the pod is killed, and set the policy so the compromised instance is not terminated, because a rescheduled pod is a wiped crime scene." },
    { q: "What is chain of custody, how does hashing enforce it, and why does it matter when the incident is over?", a: "Chain of custody is the unbroken, documented trail proving an artifact wasn't altered between collection and conclusion — who collected it, when, with what tool, where it was stored, and every hand it passed through. The technical anchor is a cryptographic hash: you SHA-256 the memory dump or disk image at the moment of acquisition and record that value, so anyone can later re-hash the copy and confirm it matches bit-for-bit. A matching hash proves the evidence is pristine; a broken hash proves it was modified. The discipline that protects the seal is working on copies, never originals — image the disk, hash the image, then analyze a mounted read-only copy while the sealed original is never touched again — and the hash must be applied at collection time, because hashing after analysis only certifies a possibly-already-mutated file. It matters after the incident because if it becomes a legal matter, a regulatory breach disclosure, an insurance claim, or even an internal blame dispute, evidence with a broken or missing custody chain is worthless: you'll know what happened but be unable to prove it, which in those contexts is the same as not knowing." },
  ]}
/>

<Recap lang="en">
Forensics answers "what happened, how, and can we prove it" — and most fatal errors happen in the first ten minutes when a well-meaning engineer touches the wrong thing. The discipline is acquire-before-analyze, ordered by volatility: dump memory before it's lost to a reboot or reschedule, capture live network and process state, snapshot the disk, and pull durable off-host logs that the attacker can't edit. Every artifact is SHA-256 hashed at collection and analyzed only as a read-only copy, so chain of custody — the documented trail of who touched what, when — stays unbroken and the evidence is defensible if the incident turns legal or regulatory. IOCs (IPs, hashes, domains) let you sweep every host to scope the blast radius and feed detection, but they sit at the bottom of the Pyramid of Pain and are trivial to rotate, so map findings to durable TTPs in MITRE ATT&CK too. Finally, fuse it all into a UTC-normalized timeline: correlate across sources, watch for clock skew and timestomping, treat missing logs as evidence of where the attacker worked, and read the progression against ATT&CK to confirm you've scoped the whole intrusion, not just its tail. Next time you're paged, your first move isn't to look — it's to acquire.
</Recap>
