---
concepts:
- sbom
- software-supply-chain
- dependency-risk
- provenance
- slsa
deepensInto: []
estMin: 15
lang: en
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-sbom-and-supply-chain
sources:
- https://csrc.nist.gov/pubs/sp/800/40/r4/final
- https://www.first.org/cvss/
spiral: []
status: ready
summary: 'A new dependency CVE drops and the first question is "are we affected?" An SBOM — the per-build inventory of everything you ship — is what lets you answer in minutes instead of days. Provenance and SLSA add the missing half: trusting that inventory.'
title: 'SBOM and supply chain'
track: security-defensive
unit: 04-secops-and-governance
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
December 2021, late afternoon: a CVE lands for Log4j 2 — `CVE-2021-44228`, a remote-code-execution bug reachable by a single crafted log line, scored 10.0. Every security channel lights up. Your CTO asks the one question that matters: *are we affected?* And the room goes quiet. Nobody can say. Log4j is not in your `package.json` or your `pom.xml` top level — it's a transitive dependency four layers deep, pulled in by a logging adapter that a framework needed. It might be in three services or thirty. The teams that answered in twenty minutes had a Software Bill of Materials: a queryable, per-build list of every component they shipped. The teams that answered in nine days grepped source repos by hand. Same bug, same patch — the gap was entirely about whether they knew what they were running.
</Hook>

By the end of this lesson you'll know what an SBOM actually contains, why "are we affected?" is unanswerable without one, and why knowing your dependencies is only half the job — the other half is trusting them.

<Crux>An SBOM is the per-build inventory of everything you ship; without it, a fresh CVE means you can't even answer "are we affected?"</Crux>

<Explanation>

## What an SBOM actually is

A Software Bill of Materials is a formal, machine-readable inventory of every component in a piece of software — direct dependencies, transitive dependencies, and often the OS packages baked into your container image. The mental model is the ingredient label on food: not marketing copy, but the literal list of what's inside, in a format a machine can parse and a regulator can audit. Two standards dominate in practice: **SPDX** (an ISO standard, `ISO/IEC 5962`) and **CycloneDX** (from OWASP, built security-first). Both express the same core facts — component name, version, and a unique identifier called a **purl** (package URL, e.g. `pkg:maven/org.apache.logging.log4j/log4j-core@2.14.1`) — plus a cryptographic hash so you can prove the artifact wasn't swapped.

The single property that makes an SBOM worth generating is that it captures the **transitive** graph, not just the top level. When you `npm install express`, you pull in dozens of packages you never named; a real application's dependency tree routinely runs to several hundred or a thousand nodes. The vulnerabilities that hurt almost always live in that buried majority — Log4j was four levels deep — which is exactly where a human reading `package.json` cannot see them. An SBOM flattens that whole graph into a list you can query: *"does any version of `log4j-core` appear anywhere in this build?"* becomes a one-line lookup instead of a multi-day archaeology dig.

The non-obvious rule: an SBOM is **generated at build time, per artifact**, not written by hand. A hand-maintained dependency list is stale the moment someone bumps a lockfile, and stale is worse than nothing because it lies with confidence. The SBOM is an output of the build, pinned to a specific image digest, so it describes exactly the bytes you deployed — not what the README claims you deployed.

<FlowDiagram
  label="The SBOM lifecycle: the build generates an SBOM per artifact, it is stored alongside the image digest, a vulnerability scanner continuously matches the SBOM's purls against new CVE feeds, and when a fresh CVE lands you query 'are we affected?' in minutes instead of grepping repos by hand for days."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "build", label: "Build", sub: "compile + resolve deps" },
    { id: "gen", label: "Generate SBOM", sub: "per artifact, all transitive" },
    { id: "store", label: "Store", sub: "pinned to image digest" },
    { id: "scan", label: "Continuous scan", sub: "purls vs CVE feed" },
    { id: "answer", label: "Are we affected?", sub: "minutes, not days" },
  ]}
  edges={[
    { from: "build", to: "gen" },
    { from: "gen", to: "store", label: "machine-readable" },
    { from: "store", to: "scan", label: "queryable" },
    { from: "scan", to: "answer", label: "new CVE" },
  ]}
  caption="An SBOM only pays off when it is generated at build time, stored with the artifact, and continuously re-scanned against fresh CVE feeds — a static document filed once and forgotten answers nothing six months later."
/>

## Why "are we affected?" is the whole point

An SBOM has no value sitting in a bucket. Its value is realized when a new vulnerability is published and you feed it into a scanner that matches each component's purl against a CVE feed. This is the difference between **inventory** and **detection**: the inventory tells you what you have; the scan tells you which of it is now known to be dangerous. Because new CVEs are published every day against components you froze months ago, the match has to be **continuous** — a vulnerability that was clean at build time becomes critical the day its CVE drops, and your stored SBOM is what lets you reach back and find it without rebuilding or re-grepping anything.

This is also where **prioritization** enters, because a real scan against a thousand-node tree returns dozens of hits and you cannot patch them all this week. CVSS — the Common Vulnerability Scoring System — gives each CVE a 0–10 **base score**, a vendor-neutral severity that's comparable across teams. But the base score is deliberately worst-case and context-free: it assumes the vulnerable code is reachable and the asset is exposed. The senior move is to layer CVSS's **environmental and temporal** metrics — *is this component actually on a reachable path? is the asset internet-facing? does an exploit exist in the wild?* — on top of the base score. A 9.8 base CVE in a dev-only build tool you never ship is lower real risk than a 7.5 in your internet-facing auth service. The SBOM tells you *where* the component is, which is precisely the context that turns a raw score into a real priority.

<Inset kind="why" lang="en">
Why isn't grepping your repos "good enough" instead of generating an SBOM? Because source ≠ artifact. Your repo shows what you *wrote*; the SBOM shows what you *shipped*. A transitive dependency resolved by your lockfile, a base-image OS package, a vendored binary, a version pinned differently in CI than locally — none of those are visible from `grep` over your source tree, and all of them ship in the container. The Log4j teams who grepped found their direct usages and missed the framework adapter that pulled it in transitively. The SBOM is generated from the resolved build, so it sees the bytes, not the intentions.
</Inset>

## The other half: provenance and trust

Knowing *what* you ship is only half the supply-chain problem. The other half is trusting that the component you pulled is the one its author actually published — and that question got urgent after attacks like **SolarWinds** (2020), where malicious code was injected into the *build pipeline* so the signed, official artifact itself was poisoned, and **Codecov** and the `event-stream` npm hijack, where a legitimate package was taken over and a malicious version pushed. In every case the dependency name and version looked correct. An SBOM would have told you that you were running `that` package; it would not have told you the package had been tampered with upstream. That gap is what **provenance** closes.

Provenance is verifiable, tamper-evident metadata answering *where did this artifact come from, from which source commit, built by which pipeline?* — cryptographically signed so it can't be forged after the fact. **SLSA** (Supply-chain Levels for Software Artifacts, pronounced "salsa") is the framework that grades how trustworthy that provenance is, in levels: roughly, L1 means provenance exists, L2 means it's signed by the build service, and L3 means the build ran in a hardened, isolated environment that the producer can't tamper with. The combined posture is the goal: an **SBOM** so you know your components, **provenance** so you trust they're authentic, and a **patch SLA** — like the timelines NIST SP 800-40 prescribes — so a known-bad component gets remediated on a clock instead of whenever someone notices.

<TradeoffMatrix
  id="04-sbom-supply-chain-tradeoff-1"
  lessonSlug="02-sbom-and-supply-chain"
  lang="en"
  prompt="A critical RCE CVE just dropped for a popular logging library. Your CTO asks: 'Are we affected, and where?' Which capability actually answers that in minutes?"
  options={[
    { name: "A continuously-scanned SBOM generated per build, with each component's purl matched against the live CVE feed", summary: "Query the inventory.", correct: true, justification: "This is exactly what an SBOM is for. Because it captures the full transitive graph per artifact and is re-scanned against fresh CVE feeds, the question becomes a one-line lookup: does any version of this component's purl appear in any build we deployed, and on which services? You get a precise, complete answer in minutes — including the four-levels-deep transitive uses a human would never spot." },
    { name: "Grep every source repository for the library name and version string", summary: "Search the source.", justification: "Source is not the artifact. Grep finds your direct, written usages and misses transitive dependencies resolved by the lockfile, base-image OS packages, and vendored binaries — which is precisely where the dangerous copy usually hides. This is the slow, incomplete path that took teams days during Log4j and still missed instances." },
    { name: "Check the top-level package.json / pom.xml of each service for the library", summary: "Read the manifest.", justification: "The top-level manifest only lists direct dependencies. The components that hurt are almost always transitive — Log4j was pulled in four layers deep by a framework adapter — so reading the manifest gives a confident answer that is wrong wherever the dependency was indirect." },
    { name: "Add cryptographic signing (provenance/SLSA) to your build pipeline", summary: "Sign the builds.", justification: "Provenance is essential, but it answers a different question — 'is this artifact authentic and untampered?' — not 'do we run a vulnerable version, and where?' Signing won't enumerate which of your services contain the CVE. SBOM is for inventory and detection; provenance is for trust. You need both, but only one answers 'are we affected?'" },
  ]}
/>

<Quiz
  id="04-sbom-supply-chain-quiz-1"
  lessonSlug="02-sbom-and-supply-chain"
  lang="en"
  question="Why must an SBOM be generated at build time per artifact rather than maintained as a hand-written document?"
  choices={[
    { label: "A hand-maintained list goes stale the moment a lockfile changes and describes intentions, not the actual shipped bytes — a build-time SBOM is pinned to the real image digest", correct: true },
    { label: "Hand-written SBOMs can't use the SPDX or CycloneDX format", misconception: "You could write SPDX or CycloneDX by hand in principle; the problem isn't the format, it's that a hand-maintained inventory drifts out of sync with the actual resolved build and lies with confidence." },
    { label: "Regulators only accept SBOMs produced by a CI server", misconception: "The driver is accuracy, not who signs off. A hand-written SBOM fails because it can't track transitive resolution and base-image packages, not because of a regulatory rule about the producer." },
    { label: "Build-time generation makes the SBOM smaller and faster to scan", misconception: "Size isn't the issue — a build-time SBOM is typically larger because it captures the full transitive graph. The point is fidelity to what actually shipped, not file size." },
  ]}
/>

<Quiz
  id="04-sbom-supply-chain-quiz-2"
  lessonSlug="02-sbom-and-supply-chain"
  lang="en"
  question="An attacker compromises a build pipeline and ships a poisoned version of an official, correctly-named package (SolarWinds-style). Which control would have caught this, and which would not?"
  choices={[
    { label: "Provenance/SLSA would catch it (the artifact wouldn't match a trusted, signed build); an SBOM alone would not, since the name and version still look correct", correct: true },
    { label: "An SBOM alone catches it, because the component would show up as a new entry", misconception: "The SBOM would list the exact name and version the attacker intended — it looks legitimate. SBOM answers 'what do we run?', not 'was it tampered with upstream?' That trust question is provenance's job." },
    { label: "CVSS scoring would catch it by flagging the high severity", misconception: "CVSS scores known, published CVEs. A fresh supply-chain injection has no CVE yet, so there's nothing to score; this is a provenance/integrity problem, not a severity-scoring one." },
    { label: "Neither — a poisoned official build is undetectable by design", misconception: "It's exactly what provenance and SLSA exist to catch: verifiable, signed metadata proving the artifact came from a specific source commit and a hardened build, so a pipeline-injected version fails verification." },
  ]}
/>

<DragOrder
  id="04-sbom-supply-chain-drag-1"
  lessonSlug="02-sbom-and-supply-chain"
  lang="en"
  prompt="Order the supply-chain workflow from build to answering 'are we affected?':"
  items={[
    { id: "a", label: "Build resolves all dependencies (direct + transitive + base image)" },
    { id: "b", label: "Generate the SBOM per artifact, pinned to the image digest" },
    { id: "c", label: "Store the SBOM alongside the deployed artifact" },
    { id: "d", label: "Continuously match each purl against fresh CVE feeds" },
    { id: "e", label: "On a new CVE, query the SBOM and prioritize hits with CVSS + reachability" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>An SBOM is the per-build, machine-readable inventory of every component you ship — including transitive ones — and it's what turns "are we affected?" from a multi-day grep into a one-line query. But inventory isn't trust: pair it with provenance/SLSA to prove components are authentic, and a patch SLA to fix the bad ones on a clock.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-sbom-supply-chain-retrieval"
  lang="en"
  questions={[
    { q: "What exactly is in an SBOM, and why does generating it at build time per artifact matter?", a: "A Software Bill of Materials is a formal, machine-readable inventory of every component in a build: direct dependencies, transitive dependencies, and usually the OS packages in the container image. Each entry carries a name, a version, a unique identifier (a purl / package URL like pkg:maven/org.apache.logging.log4j/log4j-core@2.14.1), and a cryptographic hash so the artifact can't be silently swapped. The two dominant formats are SPDX (an ISO standard) and CycloneDX (OWASP, security-first). The property that makes it worth generating is that it flattens the full transitive graph — which routinely runs to hundreds or a thousand nodes — into a list you can query, so a buried dependency four levels deep (where Log4j lived) becomes visible. It must be generated at build time and pinned to the specific image digest, not hand-written: a hand-maintained list goes stale the instant a lockfile changes and describes intentions rather than the actual shipped bytes, and a stale inventory is worse than none because it lies with confidence." },
    { q: "Knowing your dependencies is only half the supply-chain problem — what's the other half, and what closes it?", a: "The other half is trust: knowing you're running a package named X version Y does not tell you the artifact is the one its author actually published. Attacks like SolarWinds (malicious code injected into the build pipeline so the signed official artifact was poisoned), Codecov, and the event-stream npm hijack all shipped correct-looking names and versions — an SBOM would confirm you run that package but not that it was tampered with upstream. Provenance closes that gap: verifiable, cryptographically-signed, tamper-evident metadata answering where the artifact came from, from which source commit, built by which pipeline. SLSA (Supply-chain Levels for Software Artifacts) grades how trustworthy that provenance is — roughly L1 provenance exists, L2 it's signed by the build service, L3 it ran in a hardened, isolated build the producer can't tamper with. The full posture is SBOM (know your components) + provenance/SLSA (trust they're authentic) + a patch SLA (remediate known-bad components on a clock, e.g. the timelines in NIST SP 800-40), and CVSS plus reachability to prioritize which CVEs get patched first." },
  ]}
/>

<Recap lang="en">
An SBOM is a formal, machine-readable inventory of every component you ship — direct, transitive, and base-image — expressed in SPDX or CycloneDX with a purl and a hash per component. Its whole reason to exist is the question a fresh CVE forces: "are we affected, and where?" Because the dangerous components are almost always buried deep in the transitive graph (Log4j was four levels down), a manifest read or a source grep gives a confident-but-wrong answer, while a build-time SBOM that's continuously scanned against CVE feeds answers it in minutes — then CVSS base scores plus reachability and exposure context turn the dozens of hits into a real patch priority. But inventory is not trust: SolarWinds-style attacks poison correctly-named official artifacts, which an SBOM alone can't catch, so you pair it with provenance and SLSA to prove components are authentic and a patch SLA to remediate the bad ones on a clock. Now when a critical dependency CVE drops, your first move isn't to grep — it's to query the SBOM and ask which of those hits is actually reachable and exposed.
</Recap>
