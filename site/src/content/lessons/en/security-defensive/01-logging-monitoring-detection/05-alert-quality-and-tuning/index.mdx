---
concepts:
- alert-fatigue
- tuning
- signal-to-noise
- precision-recall
- triage
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: senior
order: 5
prereqs: []
slug: 05-alert-quality-and-tuning
sources:
- https://attack.mitre.org/
- https://csrc.nist.gov/pubs/sp/800/92/final
spiral: []
status: ready
summary: 'Most alerts are noise, and a wall of noise hides the one real intrusion. Senior tuning raises precision — killing low-value rules, scoping the rest — without quietly dropping the true positive you exist to catch.'
title: Alert quality and tuning
track: security-defensive
unit: 01-logging-monitoring-detection
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The Target breach is the parable every detection engineer learns the hard way: the FireEye sensor *did* fire. The malware was named, the alert was raised, and it landed in a console that was already drowning. It sat next to thousands of other alerts that week — most of them benign, repetitive, ignorable — so the one that mattered was triaged as just more weather. Forty million cards later, the lesson wasn't "buy a better sensor." The sensor worked. The pipeline that was supposed to turn its output into a *decision* had been trained, alert by ignored alert, to assume everything was noise. That is alert fatigue, and it is not a human failing you can fix with a memo. It is a math problem: when 99% of what reaches an analyst is false, the analyst's rational move is to stop reading.
</Hook>

By the end of this lesson you'll know why most alerts are noise, how to tune detections for precision without silently dropping the one true positive, and how to measure whether your tuning helped or just hid the breach.

<Crux>Most alerts are noise hiding the real intrusion. Tune for precision — kill weak rules, scope the rest — without losing the true one.</Crux>

<Explanation>

## Why the wall of alerts is the actual vulnerability

A detection rule does not exist to fire; it exists to *cause a correct action*. Between the rule and the action sits a human with a finite attention budget, and that budget is the scarcest resource in the whole stack. Once you accept that, "we have 600 rules and a glowing dashboard" stops being reassurance and starts being a warning: every low-value rule you add spends down the attention you need for the high-value one.

The trap is the base rate. In a real environment the overwhelming majority of activity is legitimate. Suppose a rule flags 1 in 1,000 events as suspicious and your environment generates 10 million events a day — that's 10,000 alerts. Now suppose the rule is genuinely good, with a 99% true-positive rate on actual attacks, but actual attacks are rare: maybe 5 real malicious events hidden in those 10 million. The rule catches all 5 (great recall) but also flags, say, 0.1% of the benign 10 million as suspicious — 10,000 false positives. Your analyst now faces 10,005 alerts to find 5 real ones. **Precision is 5 / 10,005 ≈ 0.05%.** Recall is perfect; the system is still useless, because no human reads 10,005 alerts looking for 5 needles before deciding the haystack isn't worth searching.

This is the base-rate fallacy in operational form, and it is why "we caught it, the alert fired" is cold comfort. Coverage without precision doesn't protect you — it *buries* you, and the breach hides in the rubble of your own detections.

## Precision, recall, and the lever you actually pull

Two numbers govern every tuning decision, and conflating them is the most common senior mistake:

- **Recall** (sensitivity) — of the real attacks, what fraction did you catch? Missing a true attack is a *false negative*. This is the number that ends companies.
- **Precision** — of the alerts you fired, what fraction were real? Crying wolf is a *false positive*. This is the number that ends *analysts* — and a burned-out, desensitized analyst causes the false negative downstream.

The two are coupled by your threshold. Loosen a rule (fire on weaker signal) and recall rises but precision falls — more noise. Tighten it and precision rises but recall falls — you risk missing the real one. **Tuning is not "make alerts go away." Tuning is moving the operating point along that curve deliberately, with eyes open about which error you're trading for which.** The cardinal sin is tightening a rule until the noise stops *and not noticing you tightened it past the true positive* — buying a quiet console by going blind.

<FlowDiagram
  label="The signal-to-noise pipeline: raw events are scored by a detection rule, which splits them into true positives, false positives, and missed true positives (false negatives). False positives drive alert fatigue, which desensitizes the analyst and turns a real true positive into an ignored alert — the Target failure. Tuning for precision suppresses the false positives without moving the threshold past the true positive."
  perRow={4}
  hue="var(--d-crypto)"
  nodes={[
    { id: "events", label: "Raw events", sub: "10M / day" },
    { id: "rule", label: "Detection rule", sub: "threshold" },
    { id: "fp", label: "False positives", sub: "noise" },
    { id: "tp", label: "True positive", sub: "the real one" },
    { id: "fatigue", label: "Alert fatigue", sub: "analyst tunes out" },
    { id: "ignored", label: "Ignored alert", sub: "Target failure" },
    { id: "tune", label: "Tune precision", sub: "suppress FPs" },
    { id: "action", label: "Correct action", sub: "contained" },
  ]}
  edges={[
    { from: "events", to: "rule" },
    { from: "rule", to: "fp", label: "low signal" },
    { from: "rule", to: "tp", label: "high signal" },
    { from: "fp", to: "fatigue" },
    { from: "fatigue", to: "ignored", label: "tunes out" },
    { from: "tune", to: "fp", label: "kill noise" },
    { from: "tp", to: "action", label: "if seen" },
  ]}
  caption="The breach doesn't hide in a missing rule — it hides behind the false positives. Tuning for precision removes the noise so the true positive is visible, without sliding the threshold past it (which would convert it to a silent false negative)."
/>

## How a senior actually tunes: kill, scope, enrich, tier

You don't tune by globally raising a threshold until it's quiet. That's the move that loses the true positive. You tune rule by rule, and the order matters:

1. **Kill the rules that can never cause an action.** Every alert should map to a decision: *if this fires, what would I do?* A rule whose honest answer is "nothing, we always close it as benign" is pure tax — it spends attention and trains the team to auto-dismiss. Deleting it raises precision with zero recall cost, because it never caught a real attack to begin with. This is the cheapest precision you will ever buy.
2. **Scope, don't silence.** The vuln scanner that hammers your network at 02:00 trips your port-scan rule nightly. The wrong fix is to disable port-scan detection. The right fix is a *scoped suppression*: exclude that source IP and that window, and alert if the pattern appears from anywhere else or at any other time. You kept recall for the attack you care about and removed the noise from the source you trust. The danger of broad suppressions is that they rot — the trusted IP gets reassigned, the window drifts — so every suppression needs an owner and an expiry, or it becomes a permanent blind spot an attacker can hide inside.
3. **Enrich so the analyst decides in seconds, not minutes.** A bare "failed login from 10.2.3.4" forces a five-minute investigation. The same alert enriched with *whose* account, geo/ASN, "this user normally logs in from Berlin, this is from a hosting provider in another country," and a link to recent activity is a five-second decision. Enrichment raises *effective* precision: a true positive that was drowning in ambiguity is now obviously actionable.
4. **Tier by severity and route accordingly.** Not every detection deserves a page at 03:00. Map detections to severity (ATT&CK tactic helps: a credential-dumping technique outranks a reconnaissance ping) and route low-severity, high-volume signals to a dashboard or a daily digest, reserving the pager for the handful that genuinely demand a human now. Tiering protects the attention budget without dropping recall — the low-signal events are still *recorded*, just not *interrupting*.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Move</th>
        <th class="border border-border px-3 py-2 text-left">Effect on precision</th>
        <th class="border border-border px-3 py-2 text-left">Effect on recall</th>
        <th class="border border-border px-3 py-2 text-left">The failure mode to watch</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Delete an always-benign rule</td>
        <td class="border border-border px-3 py-2">Up</td>
        <td class="border border-border px-3 py-2">Unchanged (never caught anything)</td>
        <td class="border border-border px-3 py-2">Deleting a rule that *was* catching real ones — confirm it never fired a true positive first</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Globally raise the threshold</td>
        <td class="border border-border px-3 py-2">Up</td>
        <td class="border border-border px-3 py-2">Down — risks the true positive</td>
        <td class="border border-border px-3 py-2">Buying a quiet console by going blind to the real attack</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Scoped suppression (source + window)</td>
        <td class="border border-border px-3 py-2">Up</td>
        <td class="border border-border px-3 py-2">Preserved for everything else</td>
        <td class="border border-border px-3 py-2">Suppression rots: stale IP/window becomes a permanent blind spot</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Enrich the alert</td>
        <td class="border border-border px-3 py-2">Up (effective)</td>
        <td class="border border-border px-3 py-2">Unchanged</td>
        <td class="border border-border px-3 py-2">Enrichment source is slow/unavailable and stalls triage</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Tier and route by severity</td>
        <td class="border border-border px-3 py-2">Up (per-channel)</td>
        <td class="border border-border px-3 py-2">Unchanged (still recorded)</td>
        <td class="border border-border px-3 py-2">A mis-tiered critical lands in a digest nobody reads</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="en">
Why not just buy a model that scores everything and only surface the top 1%? Because a pure-precision optimizer with no recall floor will quietly learn to never alert on the rare thing it has the least data about — which is exactly the novel intrusion you most need to catch. Precision and recall are a *constrained* optimization: maximize precision **subject to** a recall floor on the techniques that would actually hurt you. That's why tuning is anchored to a threat model (which ATT&CK techniques must I never miss?) and not to "reduce ticket volume" as a raw KPI. Optimize ticket volume alone and the cheapest path the system finds is to stop detecting the attacks that are hardest to detect.
</Inset>

## Measuring whether the tuning worked — not just whether it's quieter

A quieter console is not evidence of a better one; the Target console could have been made quieter by deleting the very rule that fired. So you measure the trade explicitly:

- **Precision proxy** — what fraction of fired alerts get actioned vs auto-closed as benign? A rule auto-closed 99% of the time is a precision problem you can see without waiting for an attack.
- **Recall, deliberately probed** — you cannot measure false negatives by waiting (you don't know what you missed). You manufacture known true positives: **purple-team exercises and detection unit tests** that replay a real technique (per ATT&CK) and assert the rule fires. A tuning change that drops a purple-team detection is caught *before* an attacker exploits the same gap.
- **Mean time to detect/triage** — enrichment and tiering should pull this down; if MTTD climbs after a tuning pass, you traded clarity for quiet.

<TradeoffMatrix
  id="05-alert-quality-tuning-tradeoff-1"
  lessonSlug="05-alert-quality-and-tuning"
  lang="en"
  prompt="A high-volume port-scan rule fires every night because the internal vuln scanner at 10.0.0.9 runs at 02:00. Analysts have started ignoring all port-scan alerts. Pick the best tuning move."
  options={[
    { name: "Scoped suppression: exclude source 10.0.0.9 during the 02:00 scan window, and keep alerting on port scans from any other source or time", summary: "Scope, don't silence.", correct: true, justification: "This removes the known-benign noise that is desensitizing the team while preserving recall for the attack you actually care about — a port scan from an unexpected host or at an unexpected time. Precision goes up, the real-attack detection stays intact. Pair it with an owner and an expiry so the suppression can't silently rot into a permanent blind spot when 10.0.0.9 gets reassigned." },
    { name: "Disable the port-scan rule entirely — it's clearly too noisy", summary: "Delete the detection.", justification: "This buys quiet by going blind. Port scanning is a real reconnaissance technique; deleting the rule drops recall to zero for an attacker doing exactly what the scanner does. The noise came from one trusted source, not from the rule being worthless — kill the noise, not the detection." },
    { name: "Globally raise the rule's threshold so it only fires on very aggressive scans", summary: "Raise the threshold.", justification: "A global threshold raise trades recall blindly: a careful attacker scans slowly and quietly, precisely under an aggressive-only threshold, so you lose the stealthy recon you most want to catch while still alerting on the loud benign scanner if it's aggressive. You moved the operating point without targeting the actual noise source." },
    { name: "Send all port-scan alerts straight to a daily digest nobody pages on", summary: "Bury in a digest.", justification: "Tiering is a real tool, but routing a reconnaissance signal that could precede a breach into an unread digest is mis-tiering — you've hidden the true positive instead of suppressing the false ones. The benign source is the problem; scope it out, and tier by genuine severity, not by which channel is quietest." },
  ]}
/>

<Quiz
  id="05-alert-quality-tuning-quiz-1"
  lessonSlug="05-alert-quality-and-tuning"
  lang="en"
  question="A rule catches 100% of real attacks but, in a 10M-event/day environment, also fires 10,000 false positives a day against ~5 real events. Why is it still a failed detection?"
  choices={[
    { label: "Recall is perfect but precision is ~0.05%, so analysts drown in noise and rationally stop reading — the real one gets buried", correct: true },
    { label: "Because the recall is too low to catch the attacks", misconception: "Recall is perfect here — it catches all 5 real events. The failure is precision: 10,000 false positives bury the 5 true ones, which is a base-rate problem, not a recall problem." },
    { label: "Because 10 million events per day is too many to log at all", misconception: "Log volume isn't the issue; you can store all of it. The issue is the alert-to-true-positive ratio reaching the analyst — precision, not storage." },
    { label: "Because false positives are harmless as long as recall is 100%", misconception: "False positives are the core harm here: they cause alert fatigue, which desensitizes analysts and converts the real true positive into an ignored alert. Perfect recall is wasted if no one reads the alert." },
  ]}
/>

<Quiz
  id="05-alert-quality-tuning-quiz-2"
  lessonSlug="05-alert-quality-and-tuning"
  lang="en"
  question="You can't measure false negatives by waiting (you don't know what you missed). How does a senior team actually probe recall before an attacker does?"
  choices={[
    { label: "Manufacture known true positives — purple-team exercises and detection unit tests that replay a real ATT&CK technique and assert the rule fires", correct: true },
    { label: "Count how many alerts fired this week and assume more alerts means better recall", misconception: "Alert count is a precision/volume signal, not recall. A flood of false positives inflates the count while telling you nothing about the attacks you missed." },
    { label: "Wait for the next breach and see whether the rule fired", misconception: "That's measuring recall by getting breached — far too late and far too costly. The point is to surface false negatives proactively, before an adversary exploits the gap." },
    { label: "Lower every threshold until the console is busy again", misconception: "Loosening thresholds raises noise, not measured recall, and reintroduces the alert fatigue you were tuning away. You probe recall with replayed known-true techniques, not by re-adding noise." },
  ]}
/>

<DragOrder
  id="05-alert-quality-tuning-drag-1"
  lessonSlug="05-alert-quality-and-tuning"
  lang="en"
  prompt="Order a precision-tuning pass on a noisy rule from safest-first to last, so you never drop the true positive:"
  items={[
    { id: "a", label: "Confirm the rule has never fired a real true positive (check history)" },
    { id: "b", label: "Identify the specific benign source/pattern causing the noise" },
    { id: "c", label: "Apply a scoped suppression (source + window) with an owner and expiry" },
    { id: "d", label: "Enrich the surviving alerts so the true positive is obviously actionable" },
    { id: "e", label: "Re-run the purple-team test to confirm the real technique still fires" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Most alerts are noise, and the noise — not a missing rule — is what hides the real intrusion. Tune for precision by deleting never-actioned rules and applying scoped, owned, expiring suppressions; never globally raise a threshold until the console is quiet, because that's how you go blind to the true positive. Probe recall with purple-team replays so you can prove the tuning didn't drop the one alert you exist to catch.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="05-alert-quality-tuning-retrieval"
  lang="en"
  questions={[
    { q: "Explain, using the base rate, why a detection rule with perfect recall can still be a failed detection — and what that implies about coverage vs precision.", a: "Real environments are dominated by benign activity, so even a small false-positive rate produces an enormous number of false alerts relative to the handful of real attacks. Take a 10M-event/day environment with ~5 genuinely malicious events. A rule with 100% recall catches all 5 — but if it also flags just 0.1% of the 10M benign events, that's 10,000 false positives. The analyst now sees 10,005 alerts to find 5 real ones, so precision is about 0.05%. No human reads 10,005 alerts hunting 5 needles before rationally concluding the haystack isn't worth searching — which is the base-rate fallacy in operational form and exactly the Target failure, where the sensor fired but the alert was triaged as noise. The implication: coverage without precision doesn't protect you, it buries you. Adding low-value rules spends the analyst's finite attention budget, and the real intrusion hides not in a missing rule but behind the wall of false positives the existing rules generate. The job of tuning is to raise precision (remove the noise) while protecting recall on the techniques that would actually hurt you." },
    { q: "Walk through how a senior tunes a noisy rule for precision without dropping the true positive, and how they prove they didn't.", a: "Tune rule by rule, in order of safety. First, kill rules that can never cause an action: if the honest answer to 'if this fires, what would I do?' is 'nothing, we always close it benign,' delete it — that raises precision at zero recall cost because it never caught a real attack (confirm against history first). Second, scope rather than silence: when a trusted source (say a nightly internal vuln scanner) trips a rule, don't disable the detection — apply a scoped suppression that excludes that source and time window while still alerting on the same pattern from anywhere else, and give the suppression an owner and an expiry so it can't rot into a permanent blind spot when the IP gets reassigned. Third, enrich surviving alerts (whose account, geo/ASN, normal baseline, recent activity) so a true positive that was drowning in ambiguity becomes a five-second decision — raising effective precision. Fourth, tier by severity and route: page only on the few high-severity techniques, send high-volume low-severity signals to a dashboard or digest, so the attention budget is protected without dropping recall (the events are still recorded). The thing you never do is globally raise a threshold until the console goes quiet, because that silently slides past the true positive and converts it into a false negative. To prove the tuning didn't blind you, you don't wait — you manufacture known true positives via purple-team exercises and detection unit tests that replay a real ATT&CK technique and assert the rule still fires, and you watch precision proxies (action rate vs auto-close) and mean-time-to-detect. A tuning change that breaks a purple-team detection is caught before an attacker exploits the same gap." },
  ]}
/>

<Recap lang="en">
Most security alerts are noise, and the breach hides not in a rule you forgot to write but behind the wall of false positives your existing rules generate — the base-rate fallacy in operational form, and the real lesson of Target, where the sensor fired into a console already trained to ignore it. Recall (did you catch the real attack?) and precision (were your alerts real?) are coupled by your threshold, and the cardinal sin is tightening a rule until it's quiet without noticing you tightened it past the true positive. A senior tunes rule by rule: delete never-actioned rules (free precision), apply scoped suppressions with an owner and expiry instead of silencing whole detections, enrich alerts so a true positive is obviously actionable, and tier by severity so only the genuinely urgent few interrupt a human. And because you can't measure false negatives by waiting, you probe recall deliberately — purple-team replays and detection unit tests that fire a known ATT&CK technique and assert the rule catches it. So next time someone celebrates a quieter dashboard, your first question is: quieter because we removed noise, or quieter because we just went blind to the one that matters?
</Recap>
