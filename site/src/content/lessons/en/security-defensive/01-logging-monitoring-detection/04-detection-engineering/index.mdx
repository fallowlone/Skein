---
concepts:
- detection-engineering
- detection-as-code
- false-positive
- true-positive
- sigma
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: senior
order: 4
prereqs: []
slug: 04-detection-engineering
sources:
- https://attack.mitre.org/
- https://csrc.nist.gov/pubs/sp/800/92/final
spiral: []
status: ready
summary: 'A rule clicked into a SIEM console has no history, no test, no reviewer.
  Detection engineering treats detections as code — Sigma rules in Git, tested against
  sample logs, reviewed in a PR — so tuning becomes an auditable diff, not a click
  no one remembers.'
title: Detection engineering
track: security-defensive
unit: 01-logging-monitoring-detection
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
3 a.m., the on-call analyst is drowning. A detection named "Suspicious PowerShell" has fired 4,000 times overnight — every one of them a backup job that someone added to a server eight months ago. The analyst does what every exhausted analyst does: opens the SIEM console, finds the rule, adds an exception for that host, clicks save. The alerts stop. Nobody writes it down. Six weeks later a different engineer, staring at the same noisy rule, *deletes* the condition entirely to make it quiet — and silently blows a hole through which the exact technique the rule was built to catch now walks unseen. There is no commit, no diff, no review, no test that would have caught it. The rule was never code. It was a setting in a box, and settings in a box have no memory.
</Hook>

By the end of this lesson you'll know how to write, test, and version detections as code — Sigma rules in Git, validated against sample logs, reviewed in a pull request — so tuning a noisy rule is an auditable engineering change instead of a forgotten click.

<Crux>Detection-as-code keeps Sigma rules in Git, tested and reviewed, so tuning a false positive is a diff — not a forgotten click.</Crux>

<Explanation>

## What detection engineering actually is

You already know that SIEM rules turn logs into alerts. Detection engineering is the discipline that asks a harder question: *how do you build, change, and trust those rules over years, across a team, without the rule set rotting into noise?* The answer the field converged on is **detection-as-code** — treat every detection the way you treat application code. It lives in a Git repository, not a vendor console. It has an author, a commit message, and a reviewer. It ships through CI. And critically, it has tests: sample log events that *should* fire it (the true positives) and sample events that *should not* (the negatives), run on every change.

The console-clicking model fails for the same reasons editing production by hand in a database GUI fails. There's no history, so you can't answer "who changed this rule and why?" There's no review, so a tired analyst's 3 a.m. exception ships with zero second pair of eyes. There's no test, so a one-character edit to a regex can quietly stop matching the thing it was built to catch and *nobody finds out until the breach*. Detection-as-code closes all three gaps by borrowing the muscles you already have from software delivery: version control, code review, and automated testing.

## Sigma: the rule format that travels

The mechanism that makes this portable is **Sigma** — a generic, YAML-based signature format for log-based detections, the "write once, target many" layer for the SIEM world. You describe the *logic* of a detection in Sigma's vendor-neutral schema, then a converter (the reference tool is `sigma-cli` with its `pySigma` backends) compiles that one rule into the query language of whatever backend you run: Splunk SPL, Elastic's query DSL, a Microsoft Sentinel KQL query, and so on. Write the logic once; let the toolchain emit the dialect.

A Sigma rule's heart is two fields. `detection.selection` lists the field/value conditions that must match, and `condition` combines selections with boolean logic. Here is a real-shaped rule for the very alert that flooded our analyst:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Sigma field</th>
        <th class="border border-border px-3 py-2 text-left">Example value</th>
        <th class="border border-border px-3 py-2 text-left">What it does for you</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>title</code></td>
        <td class="border border-border px-3 py-2">Encoded PowerShell command line</td>
        <td class="border border-border px-3 py-2">Human name — what shows up in the alert</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>logsource</code></td>
        <td class="border border-border px-3 py-2">product: windows, category: process_creation</td>
        <td class="border border-border px-3 py-2">Which log stream the rule runs against</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>detection.selection</code></td>
        <td class="border border-border px-3 py-2">Image endswith \powershell.exe; CommandLine contains '-enc'</td>
        <td class="border border-border px-3 py-2">The match conditions</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>detection.filter</code></td>
        <td class="border border-border px-3 py-2">CommandLine contains 'BackupAgent\\run.ps1'</td>
        <td class="border border-border px-3 py-2">The tuning carve-out — in the rule, in Git</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>condition</code></td>
        <td class="border border-border px-3 py-2">selection and not filter</td>
        <td class="border border-border px-3 py-2">Boolean glue — the actual logic</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>tags</code></td>
        <td class="border border-border px-3 py-2">attack.execution, attack.t1059.001</td>
        <td class="border border-border px-3 py-2">ATT&CK mapping — feeds your coverage grid</td>
      </tr>
    </tbody>
  </table>
</div>

Notice the tuning lives in `filter`, expressed in the `condition` as `selection and not filter`. That is the same exception our 3 a.m. analyst made by hand — but now it is a line in a YAML file, in a commit, with a message saying *why* the backup agent is excluded, reviewable by anyone who later wonders whether that carve-out is too broad. The fix didn't move into a hidden console setting; it moved into the diff.

## The lifecycle: a detection is software

The reason this matters is that detections are never "done." An adversary changes a flag, a vendor renames a log field, an application starts emitting a benign pattern that looks malicious — and the rule must change. Detection-as-code gives that change a pipeline.

<FlowDiagram
  label="The detection-as-code lifecycle: an analyst writes a Sigma rule mapped to an ATT&CK technique, commits it to Git, CI converts it and runs it against sample true-positive and negative logs, a peer reviews the pull request, the rule deploys to the SIEM, and production false positives feed a tuning commit that re-enters the same loop."
  perRow={4}
  hue="var(--d-crypto)"
  nodes={[
    { id: "write", label: "Write rule", sub: "Sigma + ATT&CK tag" },
    { id: "commit", label: "Commit to Git", sub: "author + message" },
    { id: "ci", label: "CI tests", sub: "TP fires / FP quiet" },
    { id: "review", label: "PR review", sub: "second pair of eyes" },
    { id: "deploy", label: "Deploy to SIEM", sub: "converted query" },
    { id: "tune", label: "Tune on FPs", sub: "filter in the diff" },
  ]}
  edges={[
    { from: "write", to: "commit" },
    { from: "commit", to: "ci" },
    { from: "ci", to: "review", label: "green" },
    { from: "review", to: "deploy", label: "approved" },
    { from: "deploy", to: "tune", label: "FPs in prod" },
    { from: "tune", to: "ci", label: "loop" },
  ]}
  caption="A detection moves like a feature: authored, committed, tested in CI, reviewed, deployed, and re-tuned. The tuning step loops back through tests and review — so a noise fix can never silently break the true-positive case."
/>

The test step is the load-bearing one and the part teams skip first. Every Sigma rule should ship with fixtures: at least one log event that represents the real attack technique (assert it fires) and a set of known-benign events that look superficially similar (assert they stay quiet). When the 3 a.m. analyst adds `filter: BackupAgent\run.ps1`, CI re-runs the malicious fixture and proves the rule *still catches the encoded-PowerShell attack* even with the new carve-out. The six-weeks-later engineer who tries to delete the whole condition watches the true-positive test go red in the pull request. The test is what converts "make it quiet" from a gamble into a safe operation.

<Inset kind="why" lang="en">
Why is a false positive treated as a first-class bug and not just an annoyance? Because alert fatigue is itself an attack surface. An analyst facing 4,000 false alarms a night doesn't carefully triage the 4,001st — they reflexively dismiss the queue, and that's exactly when the real one slips past. The infamous 2013 Target breach is the canonical case: the malware *was* flagged, but the alert drowned in noise and went unactioned. So a detection's false-positive rate isn't a cosmetic metric; it's the difference between a control that works and one that has trained its operators to ignore it. Tuning is not optional polish — it's keeping the control alive.
</Inset>

## The signal you are actually tuning

Every detection change is a trade between two error types, and saying which one you're optimizing is the whole skill. A **true positive** is the rule firing on real malicious activity — the thing you want. A **false positive** is the rule firing on benign activity — the backup job. Tighten the logic to kill false positives and you risk creating *false negatives*: real attacks the now-narrower rule no longer catches. Loosen it to catch more variants and your false positives climb until the analyst tunes you out. There is no setting that makes both zero; detection engineering is the practice of choosing the point on that curve deliberately, per technique, and recording the choice where the next engineer can see it.

<TradeoffMatrix
  id="04-detection-engineering-tradeoff-1"
  lessonSlug="04-detection-engineering"
  lang="en"
  prompt="A detection 'Encoded PowerShell' fires 4,000 times a night, all from one backup host running a known script. You need it quiet without going blind to the attack. Pick the senior move."
  options={[
    { name: "Add a scoped `filter` to the Sigma rule (`condition: selection and not filter`) excluding that script path, commit it with a rationale, and let CI re-run the true-positive fixture", summary: "Tune in the diff, prove the TP survives.", correct: true, justification: "This is detection-as-code working as designed. The exception is narrow (one known script on one host, not all PowerShell), it lives in Git with a message explaining why, it's reviewable, and CI re-runs the malicious fixture to prove the rule still catches the real encoded-PowerShell technique after the carve-out. You've killed the noise without creating a false negative — and the next engineer can see exactly what was excluded and reverse it if the backup job ever goes away." },
    { name: "Open the SIEM console and add a per-host suppression for the backup server", summary: "Click an exception in the box.", justification: "This is exactly the 3 a.m. failure the discipline exists to prevent. It works tonight, but there's no commit, no rationale, no review, and no test — so nobody can later answer what was suppressed or why, and the suppression can silently rot into a blind spot when that host gets repurposed. It's an undocumented setting in a box, not an engineering change." },
    { name: "Delete the `-enc` condition from the rule so it stops matching the noisy pattern", summary: "Remove the noisy condition.", justification: "This makes the alerts stop by making the rule stop detecting the technique. Encoded command lines are a core obfuscation signal; removing that condition creates a wide false negative — the attack the rule was built for now passes unseen. A true-positive test fixture would turn red here, which is precisely why tests are mandatory." },
    { name: "Raise the alert threshold so it only fires after 5,000 events per hour", summary: "Bury it under a threshold.", justification: "A volume threshold tuned to the noise floor means a low-and-slow attacker who runs the technique a handful of times sits comfortably under the bar. You've optimized for the benign high-volume case and blinded yourself to the realistic adversary, who is quiet by design. Threshold tuning addresses symptom volume, not the benign-vs-malicious distinction the filter does." },
  ]}
/>

<Quiz
  id="04-detection-engineering-quiz-1"
  lessonSlug="04-detection-engineering"
  lang="en"
  question="Why does detection-as-code keep the tuning exception inside the Sigma rule's `filter` rather than as a suppression in the SIEM console?"
  choices={[
    { label: "So the carve-out is versioned, reviewed, and re-tested — visible in the diff with a rationale, instead of an invisible console setting", correct: true },
    { label: "Because SIEM consoles can't suppress alerts at all", misconception: "Consoles absolutely can suppress — that's the problem, not a limitation. The point is that a console suppression has no history, review, or test, whereas a `filter` in the rule does." },
    { label: "Because Sigma filters run faster than console suppressions", misconception: "Performance isn't the reason. After conversion both end up as backend query logic; the value of the filter-in-rule approach is auditability and testing, not speed." },
    { label: "Because filters are encrypted and console settings are not", misconception: "There's no encryption involved. The benefit is that the exception lives in version control where it can be reviewed and tested, not anything about secrecy." },
  ]}
/>

<Quiz
  id="04-detection-engineering-quiz-2"
  lessonSlug="04-detection-engineering"
  lang="en"
  question="An engineer tightens a rule to silence false positives, and it works — alerts drop to near zero. What's the senior's first worry?"
  choices={[
    { label: "That the tighter logic introduced false negatives — real attacks the narrower rule no longer catches — which a true-positive test fixture should verify", correct: true },
    { label: "That the rule now runs too slowly against the log volume", misconception: "Performance might matter, but the core risk of tightening for FPs is going blind to real attacks (false negatives). That's the trade the discipline is built around, and what the TP fixture exists to catch." },
    { label: "Nothing — fewer alerts is unambiguously the goal", misconception: "Fewer alerts can mean a healthier rule or a blind one. 'Quiet' is only good if the rule still fires on the real technique; otherwise you've traded noise for a false negative." },
    { label: "That the SIEM license bills per rule and the change adds cost", misconception: "Licensing isn't the engineering concern here. The substantive risk is that killing false positives created false negatives, verified by re-running the malicious fixture." },
  ]}
/>

<DragOrder
  id="04-detection-engineering-drag-1"
  lessonSlug="04-detection-engineering"
  lang="en"
  prompt="Order the detection-as-code lifecycle for a new rule, from authoring to production tuning:"
  items={[
    { id: "a", label: "Write the Sigma rule and tag it with its ATT&CK technique" },
    { id: "b", label: "Commit to Git with an author and a rationale message" },
    { id: "c", label: "CI converts the rule and runs it against TP and negative fixtures" },
    { id: "d", label: "A peer reviews and approves the pull request" },
    { id: "e", label: "Deploy the converted query to the SIEM; tune later FPs back through the same loop" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Detection engineering treats detections as code: Sigma rules in Git, mapped to ATT&CK, tested against true-positive and benign fixtures, and reviewed in a PR. Tuning a false positive becomes a scoped `filter` in the diff — auditable and re-tested — never a forgotten console click that can silently go blind to the attack.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-detection-engineering-retrieval"
  lang="en"
  questions={[
    { q: "Explain detection-as-code and why it beats clicking rules into a SIEM console.", a: "Detection-as-code treats every detection the way you treat application code: the rule lives in a Git repository, not a vendor console; it has an author, a commit message, and a peer reviewer on a pull request; it ships through CI; and it carries tests — sample log events that should fire it (true positives) and benign events that should stay quiet (negatives) — run on every change. The console-clicking model fails for the same reasons hand-editing production in a database GUI fails: no history (you can't answer who changed a rule or why), no review (a tired analyst's 3 a.m. exception ships unseen by anyone else), and no test (a one-character edit can quietly stop matching the attack and nobody learns until a breach). Detection-as-code closes all three gaps by reusing the software-delivery muscles a team already has — version control, code review, and automated testing — so a noisy-rule fix becomes an auditable diff instead of a forgotten setting in a box." },
    { q: "What is Sigma, what are its key fields, and how does the false-positive / true-positive trade-off shape how you tune a rule?", a: "Sigma is a generic, YAML-based, vendor-neutral signature format for log-based detections: you write the detection logic once and a converter (sigma-cli with pySigma backends) compiles it into each SIEM's query language — Splunk SPL, Elastic DSL, Sentinel KQL. Its core fields are logsource (which log stream to run against), detection.selection (the match conditions), an optional detection.filter (tuning carve-outs), condition (the boolean glue, e.g. 'selection and not filter'), and tags (the ATT&CK mapping that feeds the coverage grid). Tuning is a deliberate trade between two error types: a true positive is the rule firing on real malicious activity (what you want); a false positive is it firing on benign activity (the backup job). Tighten the logic to kill false positives and you risk false negatives — real attacks the narrower rule no longer catches; loosen it for more coverage and false positives climb until the analyst tunes the rule out entirely (alert fatigue, itself an attack surface, as in the 2013 Target breach where the real alert drowned in noise). So you put the exception in the rule's filter, scoped narrowly, committed with a rationale, and let CI re-run the true-positive fixture to prove the rule still catches the attack after the carve-out — choosing the point on the FP/FN curve on purpose and recording it where the next engineer can see it." },
  ]}
/>

<Recap lang="en">
Detection engineering is the discipline of building, changing, and trusting detections over years without the rule set rotting into noise — and its core practice is detection-as-code: rules live in Git, not a SIEM console, with an author, a commit message, a peer review, CI, and tests. Sigma is the vendor-neutral YAML format that makes this portable: write the logic once (logsource, selection, optional filter, condition, ATT&CK tags) and a converter emits Splunk, Elastic, or Sentinel queries. Tuning a false positive becomes a narrowly scoped `filter` expressed as `selection and not filter`, committed with a rationale and re-validated by CI against a true-positive fixture — so killing noise can never silently create a false negative. The whole thing turns on treating a false positive as a first-class bug, because alert fatigue is an attack surface: a rule that floods its operators has trained them to ignore the one alert that mattered. So the next time you face a noisy detection, your reflex is no longer "click an exception in the box" — it's "scope a filter, write the rationale, and prove with a test that the attack still fires."
</Recap>
