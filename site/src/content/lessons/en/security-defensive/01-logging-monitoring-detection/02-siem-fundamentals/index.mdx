---
concepts:
- siem
- log-aggregation
- correlation
- normalization
- alerting
deepensInto: []
estMin: 15
lang: en
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-siem-fundamentals
sources:
- https://attack.mitre.org/
- https://csrc.nist.gov/pubs/sp/800/92/final
spiral: []
status: ready
summary: 'A SIEM ingests logs from every host, normalizes them into one schema,
  and correlates that firehose into a handful of alerts. The hard part isn''t storage
  — it''s turning millions of events an hour into the three that mean something.'
title: SIEM fundamentals
track: security-defensive
unit: 01-logging-monitoring-detection
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
3 a.m. An attacker logs in with a stolen password, the auth service emits a clean `200`, and the load balancer, the database, the Linux box, and the Kubernetes audit log all dutifully write their lines — each in a different format, each in a different place. Every record needed to catch the intrusion already exists. The breach isn't that nobody logged it; it's that the proof was scattered across forty hosts in four formats, and nobody was watching the one sequence — *new geo, then a failed MFA, then a sudo, then a 4 GB outbound transfer* — that, read together, screams compromise. A SIEM exists to read those forty logs as one story.
</Hook>

By the end of this lesson you'll know how a SIEM turns a firehose of raw logs from every host into a normalized, correlated stream — and why the genuinely hard part is alerting, not storage.

<Crux>A SIEM ingests logs from everywhere, normalizes them to one schema, then correlates that stream into the few alerts worth a human.</Crux>

<Explanation>

## What a SIEM actually is

SIEM stands for **Security Information and Event Management** — but the acronym hides the real job. A SIEM is the place where every log in your estate is collected, made comparable, and watched for patterns no single log could reveal on its own. NIST SP 800-92, the canonical guide to log management, frames the core problem precisely: logs are generated by dozens of sources in incompatible formats and volumes that no human can read, so the value is entirely in *centralizing and analyzing* them, not in the act of writing them.

Think of it as a four-stage pipeline. Each stage is a place where real systems fail in production, so it's worth walking them in order.

<FlowDiagram
  label="The SIEM pipeline in four stages: collection pulls raw logs from every source, normalization parses each format into one shared schema, correlation joins normalized events across sources over a time window into rules, and alerting fires only the high-fidelity matches to a human while the rest is retained for search and forensics."
  perRow={4}
  hue="var(--d-crypto)"
  nodes={[
    { id: "collect", label: "1. Collect", sub: "agents, syslog, API" },
    { id: "normalize", label: "2. Normalize", sub: "parse → one schema" },
    { id: "correlate", label: "3. Correlate", sub: "join over a window" },
    { id: "alert", label: "4. Alert", sub: "high-fidelity → human" },
    { id: "store", label: "Retain", sub: "search + forensics" },
  ]}
  edges={[
    { from: "collect", to: "normalize", label: "raw lines" },
    { from: "normalize", to: "correlate", label: "typed fields" },
    { from: "correlate", to: "alert", label: "rule match" },
    { from: "normalize", to: "store", label: "everything" },
  ]}
  caption="Collection and normalization handle volume; correlation and alerting handle signal. Note the second arrow out of normalize: every normalized event is retained for search even when no rule fires — you can't investigate what you didn't keep."
/>

## Stage 1–2: collection and normalization — the unglamorous half

**Collection** is the plumbing: ship every log to one place. Agents (a daemon on each host tailing files), syslog over the network, cloud provider APIs (CloudTrail, GCP Audit Logs), and direct integrations all feed the SIEM. The failure modes here are mundane and constant — a host whose agent died three weeks ago is a blind spot you don't know you have, and a misconfigured firewall silently dropping UDP syslog loses records with no error anywhere. The senior reflex is to **monitor the absence of logs**: a source that suddenly goes quiet is itself an alert, because attackers turn logging off.

**Normalization** is where the real engineering lives. A Linux `sshd` line, an AWS CloudTrail JSON event, and an nginx access log all describe "someone did something from somewhere," but they spell it completely differently. Normalization parses each format and maps its fields onto one **common schema** — so `srcip`, `192.168.1.5`, and `client_addr` all become a single field like `source.ip`. Without this, correlation is impossible: you cannot join a failed SSH login to a suspicious database query if "the user" is `acct`, `user_name`, and `principalId` in three different logs.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Raw source</th>
        <th class="border border-border px-3 py-2 text-left">Native field for "who"</th>
        <th class="border border-border px-3 py-2 text-left">Native field for "from where"</th>
        <th class="border border-border px-3 py-2 text-left">Normalized schema</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Linux sshd</td>
        <td class="border border-border px-3 py-2"><code>user</code></td>
        <td class="border border-border px-3 py-2"><code>rhost</code></td>
        <td class="border border-border px-3 py-2"><code>user.name</code> / <code>source.ip</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">AWS CloudTrail</td>
        <td class="border border-border px-3 py-2"><code>userIdentity.principalId</code></td>
        <td class="border border-border px-3 py-2"><code>sourceIPAddress</code></td>
        <td class="border border-border px-3 py-2"><code>user.name</code> / <code>source.ip</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">nginx access</td>
        <td class="border border-border px-3 py-2">(none — derive from auth)</td>
        <td class="border border-border px-3 py-2"><code>$remote_addr</code></td>
        <td class="border border-border px-3 py-2"><code>user.name</code> / <code>source.ip</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">K8s audit</td>
        <td class="border border-border px-3 py-2"><code>user.username</code></td>
        <td class="border border-border px-3 py-2"><code>sourceIPs[]</code></td>
        <td class="border border-border px-3 py-2"><code>user.name</code> / <code>source.ip</code></td>
      </tr>
    </tbody>
  </table>
</div>

Four sources, four schemas, one query language after normalization. This is why mature SIEMs lean on a shared field model — the Elastic Common Schema (ECS) and the OCSF (Open Cybersecurity Schema Framework) exist precisely so that `source.ip` means the same thing everywhere and a single rule can span every source.

## Stage 3: correlation — reading logs as a story

Correlation is the SIEM's reason to exist. A single failed login is noise. A failed login on a service account that has *never* failed, from a country you've never seen, followed within ninety seconds by a successful login and a privilege escalation, is an attack — and you can only see it by **joining events across sources over a time window**. That join is exactly what normalization made possible.

Correlation rules come in two broad shapes, and seniors mix them deliberately:

- **Signature / known-bad rules** map directly to attacker behavior. Frameworks like **MITRE ATT&CK** catalog techniques — say, `T1110` Brute Force or `T1078` Valid Accounts — and you write rules that detect their telemetry: "10 failed auths then a success from one IP within 5 minutes" maps to credential stuffing. These are precise but only catch what you've named.
- **Anomaly / behavioral rules** flag deviation from a baseline: a user pulling 50× their normal data volume, a server opening an outbound connection it has never made. These catch the unknown but pay for it in false positives, because "unusual" and "malicious" overlap only partially.

<Inset kind="why" lang="en">
Why not just alert on every individual suspicious line and skip correlation? Because volume destroys you. A mid-size estate emits millions of events an hour, and a meaningful fraction look mildly suspicious in isolation — every failed login, every new IP, every large download. Alert on each and you bury the real attack under ten thousand benign ones; within a week the on-call mutes the channel. Correlation is the act of *raising the bar*: it demands that several weak signals line up in a specific sequence and window before anything reaches a human, trading raw recall for the precision that keeps alerts credible.
</Inset>

## Stage 4: alerting — where SIEMs live or die

The whole pipeline exists to produce **a small number of high-fidelity alerts**. This is the part teams underestimate. Storage is a solved, boring problem — you can buy more. Tuning is the eternal one. Every rule sits on a tradeoff between **false positives** (crying wolf until analysts stop listening — *alert fatigue*, the documented failure mode behind real breaches that were technically detected and then ignored) and **false negatives** (a real attack that never trips a rule). A rule tuned tight enough to be quiet may miss the novel attack; a rule loose enough to catch everything drowns the team. The senior measure of a SIEM is not how much it ingests but its **signal-to-noise ratio**: alerts per day a human can actually triage, and the fraction of those that are real.

<TradeoffMatrix
  id="02-siem-fundamentals-tradeoff-1"
  lessonSlug="02-siem-fundamentals"
  lang="en"
  prompt="Your SIEM fires 800 'suspicious login' alerts a day; analysts have quietly muted the channel and a real account-takeover slipped through last month. Pick the response that best fixes the root problem."
  options={[
    { name: "Make the rule correlative: require a new-geo login AND a failed-MFA AND a privilege change within one session before alerting, and route the rest to searchable storage", summary: "Raise the bar with correlation.", correct: true, justification: "The disease is low signal-to-noise causing alert fatigue — the exact mechanism that let a detected attack be ignored. The fix is to stop alerting on single weak signals and demand that several line up in a specific sequence and window, which is what correlation is for. Everything still lands in retention so you can search it during an investigation; you've cut alert volume without going blind. This raises precision where it matters: the human queue." },
    { name: "Increase log retention from 30 to 365 days and buy more storage", summary: "Store more.", justification: "Storage is the easy, solved half of the problem and has nothing to do with why the alert was ignored. More retention helps forensics after the fact but does not reduce the 800 daily false positives or restore the team's trust in the channel. You'd be spending money on the stage that wasn't broken." },
    { name: "Turn off the noisy login rule entirely until you have time to fix it", summary: "Disable the rule.", justification: "This trades false positives for a guaranteed false negative — you now detect zero account takeovers, the very attack that already slipped through. Muting a category of detection because it's noisy is how breaches go unseen; the goal is to make the rule precise, not blind." },
    { name: "Forward all 800 alerts to a second team so each team sees fewer", summary: "Split the queue.", justification: "Splitting noise across more people doesn't raise signal-to-noise; it just spreads the fatigue. The same low-fidelity rule still fires 800 times, no individual alert becomes more trustworthy, and you've added handoff ambiguity about who owns a real hit." },
  ]}
/>

<Quiz
  id="02-siem-fundamentals-quiz-1"
  lessonSlug="02-siem-fundamentals"
  lang="en"
  question="Why is normalization a prerequisite for correlation, rather than an optional nicety?"
  choices={[
    { label: "Because correlation joins events across sources, and you can't join fields that are named and shaped differently in each log", correct: true },
    { label: "Because normalization compresses logs so they take less storage", misconception: "Normalization is about a shared schema, not compression. Its purpose is to make fields comparable across sources; storage savings are not the point and aren't guaranteed." },
    { label: "Because raw logs are encrypted and normalization decrypts them", misconception: "Normalization parses and maps fields to a common schema; it has nothing to do with encryption. Logs aren't generally encrypted in a way that blocks parsing." },
    { label: "Because alerting can only read JSON, so everything must be converted to JSON", misconception: "The issue isn't a single wire format like JSON — it's a shared *field model* (e.g. source.ip meaning the same thing everywhere) so a rule can span sources." },
  ]}
/>

<Quiz
  id="02-siem-fundamentals-quiz-2"
  lessonSlug="02-siem-fundamentals"
  lang="en"
  question="A SIEM ingests 40 million events/hour and stores them perfectly, but the on-call team has muted its alert channel. What's actually broken?"
  choices={[
    { label: "Signal-to-noise: the alerting stage produces too many low-fidelity alerts, so real ones get ignored (alert fatigue)", correct: true },
    { label: "Collection: 40 million events/hour means logs are being dropped", misconception: "High volume that's being stored perfectly is a sign collection is working, not failing. The failure is downstream, at alerting — too much noise, not too little data." },
    { label: "Storage: they need a bigger cluster to hold 40 million events/hour", misconception: "The events are stored fine; storage is the solved, boring half. The unsolved problem is turning that volume into a few alerts worth a human's attention." },
    { label: "Nothing — a muted channel just means a quiet, secure period", misconception: "A muted channel is the opposite of secure: it means real alerts are being ignored. This is alert fatigue, the documented path by which detected breaches go unactioned." },
  ]}
/>

<DragOrder
  id="02-siem-fundamentals-drag-1"
  lessonSlug="02-siem-fundamentals"
  lang="en"
  prompt="Order the four stages of the SIEM pipeline, from raw log to human:"
  items={[
    { id: "a", label: "Collect — ship every log to one place (agents, syslog, cloud APIs)" },
    { id: "b", label: "Normalize — parse each format into one shared schema" },
    { id: "c", label: "Correlate — join normalized events across sources over a time window" },
    { id: "d", label: "Alert — fire only high-fidelity matches to a human" },
  ]}
  correctOrder={["a", "b", "c", "d"]}
/>

</Explanation>

<KeyTakeaway>A SIEM is a four-stage pipeline: collect every log, normalize it to one schema, correlate events across sources over a time window, and alert on the few high-fidelity matches. Storage is easy; the hard, senior part is signal-to-noise — turning millions of events into the three that mean something.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-siem-fundamentals-retrieval"
  lang="en"
  questions={[
    { q: "Walk through the four stages of a SIEM pipeline and name a real production failure at each.", a: "A SIEM runs collect → normalize → correlate → alert. (1) Collection ships every log to one place via agents, syslog, and cloud APIs (CloudTrail, K8s audit). Its failure mode is silent blind spots — a dead agent or a dropped UDP syslog stream loses records with no error, which is why the absence of logs from a source must itself be an alert, since attackers disable logging. (2) Normalization parses each format and maps its fields onto one common schema (e.g. ECS/OCSF), so a Linux sshd 'user'/'rhost', a CloudTrail 'principalId'/'sourceIPAddress', and a K8s 'user.username'/'sourceIPs' all become user.name/source.ip. Its failure is brittle or missing parsers, which leave events un-joinable. (3) Correlation joins normalized events across sources over a time window — that join is only possible because normalization unified the fields — turning a sequence like new-geo login → failed MFA → sudo → large outbound transfer into one detection. (4) Alerting emits the few high-fidelity matches to a human. Its failure is alert fatigue: too many low-fidelity alerts cause the team to mute the channel and miss the real one. Storage is the easy half; signal-to-noise is the hard half." },
    { q: "A SIEM ingests 40M events/hour flawlessly but a real account takeover was missed. Where's the problem and how do you fix it?", a: "The problem is not collection or storage — flawless ingest of high volume means those stages work. The problem is at alerting: low signal-to-noise. If the SIEM fires hundreds of low-fidelity 'suspicious login' alerts a day, analysts develop alert fatigue and mute the channel, so a real account takeover that did trip a rule gets ignored — the documented pattern where a breach is technically detected and then unactioned. The fix is to raise the bar with correlation instead of alerting on single weak signals: require several signals to line up in a specific sequence and window (e.g. new-geo login AND failed MFA AND a privilege change within one session) before anything reaches a human, while everything else still flows to searchable retention for investigation. You're trading raw recall on individual events for the precision that keeps the human queue credible. Crucially you do NOT 'fix' it by disabling the noisy rule (that guarantees a false negative on the exact attack), by buying more storage (the unbroken stage), or by splitting the same noise across two teams (which spreads fatigue without raising fidelity). The senior metric is alerts/day a human can triage and the fraction that are real, not events/hour ingested." },
  ]}
/>

<Recap lang="en">
A SIEM — Security Information and Event Management — exists because every record needed to catch an intrusion already exists, just scattered across dozens of hosts in incompatible formats with nobody reading them as one story. It works as a four-stage pipeline. Collection ships every log to one place (agents, syslog, cloud APIs) and treats a source going silent as its own alert. Normalization parses each format onto a shared schema (ECS/OCSF) so that source.ip means the same thing in an sshd line, a CloudTrail event, and a K8s audit log — without which correlation is impossible. Correlation joins those normalized events across sources over a time window, mixing signature rules mapped to MITRE ATT&CK techniques with anomaly rules against a baseline, so a sequence of weak signals becomes one detection. Alerting emits only the high-fidelity matches to a human, and this is where SIEMs live or die: storage is easy, but the eternal tradeoff between false positives (alert fatigue) and false negatives (a missed attack) makes signal-to-noise the real senior measure. Next time you see a SIEM praised for ingesting 40 million events an hour, your first question is the right one: how many alerts a day does a human actually act on, and how many of those are real?
</Recap>
