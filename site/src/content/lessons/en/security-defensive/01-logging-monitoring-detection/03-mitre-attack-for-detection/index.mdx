---
concepts:
- mitre-attack
- ttp
- detection-coverage
- technique
- threat-informed-defense
deepensInto: []
estMin: 15
lang: en
lessonType: topic
level: middle
order: 3
prereqs: []
slug: 03-mitre-attack-for-detection
sources:
- https://attack.mitre.org/
- https://csrc.nist.gov/pubs/sp/800/92/final
spiral: []
status: ready
summary: 'ATT&CK is the shared catalog of how real adversaries operate. Map each
  detection rule to a technique and your coverage stops being a feeling and becomes
  a grid — one that names exactly which attacker moves you would never see.'
title: MITRE ATT&CK for detection
track: security-defensive
unit: 01-logging-monitoring-detection
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A board member asks your security lead one question: "are we covered?" The honest answer used to be a shrug — 600 detection rules, a vendor dashboard glowing green, and no way to say what they actually catch. Then someone laid the rules over a grid of known adversary techniques and the green turned patchy: solid columns for credential attacks and brute force, near-empty columns for the steps that come *after* a foothold — discovery, lateral movement, the quiet living-off-the-land commands that look exactly like a busy admin. Six hundred rules, and a real intruder could walk three full stages of an attack without tripping one. The grid didn't add a single rule. It just made the holes impossible to keep ignoring.
</Hook>

By the end of this lesson you'll know how to map your detections onto MITRE ATT&CK so coverage becomes a measurable grid, and how to read that grid to find the gaps an attacker would exploit first.

<Crux>ATT&CK catalogs real adversary techniques; tagging each detection to one turns "are we covered?" into a grid that names your gaps.</Crux>

<Explanation>

## What ATT&CK actually is

The previous lesson named two ATT&CK technique IDs almost in passing — `T1110` Brute Force, `T1078` Valid Accounts — as targets for correlation rules. That casualness hides how much structure sits behind those IDs. **MITRE ATT&CK** (Adversarial Tactics, Techniques, and Common Knowledge) is a curated, public knowledge base of how real intrusions unfold, built from observed incidents rather than theory. It is the closest thing the field has to a shared vocabulary for attacker behavior — when one team says "we're weak on `T1021` Remote Services," every other team knows exactly which move they mean.

The model has three nested levels, and the distinction is the whole point:

- **Tactic — the *why*.** The adversary's objective at a step: *Initial Access*, *Persistence*, *Privilege Escalation*, *Lateral Movement*, *Exfiltration*. There are roughly fourteen tactics in the Enterprise matrix, and they read left-to-right as the rough order of an attack — the "kill chain" columns.
- **Technique — the *how*.** A specific way to achieve a tactic: under *Credential Access*, `T1110` Brute Force or `T1003` OS Credential Dumping. Techniques (and their finer sub-techniques) are what you actually write detections against.
- **Procedure — the *exact* how.** The concrete implementation a specific group used in the wild — *this* PowerShell one-liner, *this* tool. Procedures are the ground truth that makes a technique real.

This stack is the meaning of **TTP** — Tactics, Techniques, and Procedures, the term you'll hear constantly. The reason TTPs matter for *detection* is durability: an attacker swaps an IP or a file hash in seconds, but the *behavior* — dumping LSASS to steal credentials, then using them to move laterally — is expensive to change because it's tied to how the objective is actually accomplished.

<Inset kind="why" lang="en">
Why build detections on techniques instead of indicators? Because of the **Pyramid of Pain** — a model of how much it hurts an attacker when you block a given artifact. Hashes and IPs sit at the bottom: trivial for the adversary to rotate, so a detection built on them is dead the next campaign. TTPs sit at the apex: detecting the *behavior* forces the attacker to fundamentally rethink their operation, not just regenerate an indicator. Indicator feeds aren't useless — they're cheap, fast wins — but a detection program anchored only in hashes is permanently one step behind. ATT&CK is the field's agreement to fight at the top of the pyramid.
</Inset>

## Mapping detections to the matrix

Here's the move that turns ATT&CK from a poster into an engineering tool. Take every detection rule you own and tag it with the technique IDs it covers. Now your detection inventory and the ATT&CK matrix share a key, and you can *join* them — exactly the way normalization let a SIEM join logs across sources. The result is a **coverage grid**: the matrix of tactics × techniques, with each cell colored by whether — and how well — you can detect that technique.

<FlowDiagram
  label="How detections become a coverage grid: each detection rule is tagged with the ATT&CK technique IDs it covers, that tag joins the rule inventory to the ATT&CK matrix, projecting onto a coverage grid of tactics by techniques; the unmapped cells are the visible detection gaps that feed back into which new rules to build next."
  perRow={5}
  hue="var(--d-crypto)"
  nodes={[
    { id: "rules", label: "Detection rules", sub: "your SIEM content" },
    { id: "tag", label: "Tag w/ technique", sub: "rule → T1110" },
    { id: "matrix", label: "ATT&CK matrix", sub: "tactics × tech" },
    { id: "grid", label: "Coverage grid", sub: "cells colored" },
    { id: "gaps", label: "Named gaps", sub: "empty columns" },
  ]}
  edges={[
    { from: "rules", to: "tag" },
    { from: "tag", to: "matrix", label: "join key" },
    { from: "matrix", to: "grid", label: "project" },
    { from: "grid", to: "gaps", label: "what's empty" },
  ]}
  caption="Mapping is a join: the technique ID is the shared key between your rule inventory and the ATT&CK matrix. Projecting that join onto the matrix produces a coverage grid whose empty cells are your detection gaps — feeding back into the next rules you build. MITRE ships ATT&CK Navigator to render exactly this grid."
/>

MITRE ships a free tool, **ATT&CK Navigator**, that renders this grid as a colored heatmap — green where you have layered detection, yellow where you have one fragile rule, red where you have nothing. The value isn't the pretty picture; it's that "are we covered?" stops being a vibe and becomes a number you can defend, prioritize, and track over time.

One discipline separates real mapping from theater: **the tag must reflect what the rule genuinely detects, not what it's named.** A rule called "Brute Force Detector" that only fires on 100 failed logins from one IP covers a narrow slice of `T1110` — it misses password spraying (`T1110.003`: a few attempts across many accounts, which never trips a per-IP threshold) entirely. Overclaiming coverage is worse than admitting a gap, because it paints a cell green that an attacker walks straight through.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Tactic (the why)</th>
        <th class="border border-border px-3 py-2 text-left">Example technique</th>
        <th class="border border-border px-3 py-2 text-left">A detection that covers it</th>
        <th class="border border-border px-3 py-2 text-left">Typical real coverage</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Credential Access</td>
        <td class="border border-border px-3 py-2"><code>T1110</code> Brute Force</td>
        <td class="border border-border px-3 py-2">Failed-auth threshold + success correlation</td>
        <td class="border border-border px-3 py-2">Strong — everyone builds this</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Credential Access</td>
        <td class="border border-border px-3 py-2"><code>T1110.003</code> Password Spraying</td>
        <td class="border border-border px-3 py-2">Low-rate failures across <em>many</em> accounts</td>
        <td class="border border-border px-3 py-2">Often missed — per-IP rules don't fire</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Discovery</td>
        <td class="border border-border px-3 py-2"><code>T1087</code> Account Discovery</td>
        <td class="border border-border px-3 py-2">Bursts of enumeration commands per host</td>
        <td class="border border-border px-3 py-2">Weak — looks like normal admin work</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Lateral Movement</td>
        <td class="border border-border px-3 py-2"><code>T1021</code> Remote Services</td>
        <td class="border border-border px-3 py-2">Unusual host-to-host auth (new pairs)</td>
        <td class="border border-border px-3 py-2">Thin — east-west traffic rarely logged</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Exfiltration</td>
        <td class="border border-border px-3 py-2"><code>T1048</code> Exfil over alt protocol</td>
        <td class="border border-border px-3 py-2">Outbound volume / DNS-tunnel anomaly</td>
        <td class="border border-border px-3 py-2">Patchy — needs egress visibility</td>
      </tr>
    </tbody>
  </table>
</div>

Read that "typical real coverage" column top to bottom and a pattern jumps out: teams are strong at the *left* of the matrix — the loud, early, well-understood steps like brute force — and thin toward the *middle and right*, where the attacker is already inside and the activity blends into legitimate operations. That left-heavy profile is the single most common detection-coverage shape in the industry, and it's exactly the shape an intruder counts on.

## Reading the grid: threat-informed defense

A naïve reading of the coverage grid is "color everything green." That's a trap — the Enterprise matrix has hundreds of techniques and sub-techniques, full coverage is neither achievable nor worth it, and chasing it spends your scarce detection-engineering budget on techniques no adversary who targets *you* will ever use. The senior move is **threat-informed defense**: prioritize coverage by the techniques the groups that actually target your sector are known to use.

ATT&CK supports this directly. It catalogs threat *groups* (e.g. APT29, FIN7) and maps each to the techniques it has been observed using. So you don't color the whole matrix — you overlay the technique sets of the two or three groups relevant to your industry, and *that* overlay becomes your priority list. A fintech weights credential access and financial-fraud TTPs; a hospital weights ransomware-precursor techniques like `T1486` Data Encrypted for Impact. Coverage becomes a question of *expected value*: detect the moves your real adversaries make, in the order they make them, before you spend a dollar on the exotic.

<TradeoffMatrix
  id="03-mitre-attack-for-detection-tradeoff-1"
  lessonSlug="03-mitre-attack-for-detection"
  lang="en"
  prompt="Your ATT&CK coverage grid is dense green on the left (Initial Access, Credential Access) and nearly empty in the middle (Discovery, Lateral Movement). You have budget for one quarter of detection engineering. Pick the best use of it."
  options={[
    { name: "Build detections for the empty Discovery and Lateral Movement columns, prioritized by the techniques the groups targeting your sector are known to use", summary: "Fill the gaps, threat-informed.", correct: true, justification: "The grid is telling you an intruder who gets past the strong left side then operates freely through the middle — exactly the post-foothold stages where most dwell time and damage accrue. Threat-informed defense says don't try to color the whole matrix; prioritize the empty cells that the adversaries actually targeting your sector traverse. That's the highest expected-value spend: it closes the path a real attacker would take, in the order they'd take it." },
    { name: "Add a second and third brute-force rule to make the already-green Credential Access column even darker green", summary: "Deepen what's strong.", justification: "This is coverage theater. You're spending budget where you already detect well, turning a green cell greener while the intruder walks untouched through Discovery and Lateral Movement. Layering more rules on a covered technique has steeply diminishing returns; the marginal value is in the empty cells, not the dark ones." },
    { name: "Aim to turn every cell in the matrix green this quarter for complete coverage", summary: "Cover everything.", justification: "The Enterprise matrix has hundreds of techniques and sub-techniques; 'all green' is neither achievable in a quarter nor worth it. Chasing total coverage spreads a finite budget across techniques no adversary targeting you will use, and you'll run out of quarter before you've meaningfully closed the gaps that matter. Prioritize by real threat, not by completeness." },
    { name: "Buy a threat-intel feed of malicious IPs and hashes to block at the firewall", summary: "Buy indicators.", justification: "Indicators sit at the bottom of the Pyramid of Pain — cheap and useful, but trivially rotated, and they do nothing to fill a behavioral gap in Discovery or Lateral Movement. An attacker already inside isn't stopped by a hash blocklist; the missing thing is detection of their behavior, which is what the empty columns represent." },
  ]}
/>

<Quiz
  id="03-mitre-attack-for-detection-quiz-1"
  lessonSlug="03-mitre-attack-for-detection"
  lang="en"
  question="Why are detections built on ATT&CK techniques (TTPs) more durable than detections built on IP addresses and file hashes?"
  choices={[
    { label: "Behavior is tied to how the objective is achieved and is expensive to change, while an IP or hash is rotated in seconds (the Pyramid of Pain)", correct: true },
    { label: "Because technique IDs never change, so the rule never needs maintenance", misconception: "ATT&CK does evolve and rules still need tuning. The durability point is about the attacker's cost to evade — behavior is costly to change — not that the rule is maintenance-free." },
    { label: "Because hashes and IPs are encrypted and can't be read by a SIEM", misconception: "Hashes and IPs are perfectly readable; the issue is that they're trivial for an attacker to rotate, so a detection built on them dies the next campaign." },
    { label: "Because techniques cover the whole matrix automatically once you tag one rule", misconception: "Tagging one rule covers only the techniques that rule genuinely detects. Coverage of the matrix comes from many mapped rules, and overclaiming is a known failure mode." },
  ]}
/>

<Quiz
  id="03-mitre-attack-for-detection-quiz-2"
  lessonSlug="03-mitre-attack-for-detection"
  lang="en"
  question="A rule named 'Brute Force Detector' fires only on 100+ failed logins from a single IP. On the coverage grid, how should you tag T1110 Brute Force?"
  choices={[
    { label: "Partial — it covers classic single-source brute force but misses password spraying (T1110.003), so don't paint the cell fully green", correct: true },
    { label: "Fully covered — the rule's name matches the technique, so T1110 is green", misconception: "Tagging by name, not behavior, is exactly the theater to avoid. A per-IP threshold misses low-rate spraying across many accounts; painting it fully green hides a path the attacker walks." },
    { label: "Not covered at all — single-IP detection doesn't count as brute force", misconception: "It genuinely detects the classic single-source case, so it's real partial coverage. Marking it zero is as inaccurate as marking it full; the honest tag is partial." },
    { label: "Covered, plus it automatically covers Credential Access entirely", misconception: "One rule covers one slice of one technique. A tactic column contains many techniques (dumping, spraying, forced auth); one rule can't cover the column." },
  ]}
/>

<DragOrder
  id="03-mitre-attack-for-detection-drag-1"
  lessonSlug="03-mitre-attack-for-detection"
  lang="en"
  prompt="Order these ATT&CK tactics in the rough left-to-right sequence of an intrusion:"
  items={[
    { id: "a", label: "Initial Access — get a foothold" },
    { id: "b", label: "Credential Access — steal usernames and passwords" },
    { id: "c", label: "Discovery — map the environment from inside" },
    { id: "d", label: "Lateral Movement — pivot to other hosts" },
    { id: "e", label: "Exfiltration — get the data out" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>ATT&CK is the shared catalog of real adversary techniques (TTPs). Tag each detection with the technique IDs it truly covers, project that join onto the matrix, and "are we covered?" becomes a grid that names your gaps. Tag by behavior, not by rule name, and prioritize the empty cells by threat-informed defense — the techniques the groups targeting *you* actually use — not by chasing an all-green matrix.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-mitre-attack-for-detection-retrieval"
  lang="en"
  questions={[
    { q: "Explain the tactic / technique / procedure hierarchy in ATT&CK and why detecting at the TTP level (not IPs and hashes) makes a detection durable.", a: "ATT&CK structures adversary behavior into three nested levels. A tactic is the adversary's objective at a step — the 'why' — such as Initial Access, Credential Access, Lateral Movement, or Exfiltration; there are roughly fourteen Enterprise tactics that read left-to-right as the rough order of an attack. A technique is a specific way to achieve a tactic — the 'how' — for example T1110 Brute Force or T1003 OS Credential Dumping under Credential Access, often refined into sub-techniques (T1110.003 Password Spraying). A procedure is the exact in-the-wild implementation a particular group used — the specific tool or command. Together these are TTPs. Detecting at the TTP/behavior level is durable because of the Pyramid of Pain: IPs and file hashes sit at the bottom and an attacker rotates them in seconds, so a detection built on them is dead the next campaign; behavior sits at the apex because it's tied to how the objective is actually accomplished (you must dump credentials to steal them, then use them to move laterally), so forcing the attacker to change behavior is expensive. Indicator feeds are still cheap fast wins, but a program anchored only in hashes is permanently one campaign behind." },
    { q: "How does mapping detections to ATT&CK turn 'are we covered?' into a measurable grid, and how do you prioritize what to build with threat-informed defense?", a: "You tag every detection rule with the ATT&CK technique IDs it genuinely covers. The technique ID is then a shared key between your rule inventory and the ATT&CK matrix, so you can join them — the same move normalization enabled for joining logs across sources — and project the result onto a coverage grid of tactics × techniques, rendered as a heatmap (MITRE's free ATT&CK Navigator does this): green where detection is layered, yellow where it's one fragile rule, red where there's nothing. The discipline that makes this honest is tagging by what the rule actually detects, not its name — a per-IP 'brute force' rule covers classic T1110 but misses password spraying T1110.003, so it's partial, not green; overclaiming paints a cell an attacker walks straight through. The common real-world shape is dense green on the left (loud early steps like brute force) and thin in the middle and right (Discovery, Lateral Movement), exactly where an intruder is already inside and blends in. You do NOT try to color the whole matrix — it has hundreds of techniques and full coverage isn't worth it. Threat-informed defense prioritizes by expected value: ATT&CK maps threat groups (APT29, FIN7) to the techniques they're observed using, so you overlay the two or three groups that target your sector and let that overlay be your build queue — close the path real adversaries take, in the order they take it, before spending on the exotic." },
  ]}
/>

<Recap lang="en">
MITRE ATT&CK is the field's shared catalog of how real intrusions unfold, structured as tactics (the why — Initial Access, Lateral Movement, Exfiltration), techniques (the how — T1110 Brute Force, T1003 Credential Dumping), and procedures (the exact in-the-wild implementation) — together, TTPs. Detecting at the TTP level is durable because of the Pyramid of Pain: an attacker rotates an IP or hash in seconds, but the behavior is expensive to change. The engineering move is to tag each detection rule with the technique IDs it genuinely covers, join your rule inventory to the matrix on that key, and project it onto a coverage grid — MITRE's ATT&CK Navigator renders the heatmap — so "are we covered?" becomes a number you can defend. Tag by behavior, not rule name: a per-IP brute-force rule is only partial coverage of T1110 because it misses password spraying. The usual shape is strong on the left and empty in the middle, where a real intruder operates. Don't chase an all-green matrix; use threat-informed defense to prioritize the empty cells by the techniques the groups targeting your sector actually use. Next time someone shows you a green security dashboard, your first question is the senior one: which techniques does it cover — and which attacker moves would never appear on it at all?
</Recap>
