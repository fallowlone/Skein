---
concepts:
- review-latency
- small-pr-economics
- defect-density
- automate-style
- knowledge-sharing
- review-as-gatekeeping
deepensInto:
- 02-pr-size-and-latency
estMin: 16
lang: en
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://google.github.io/eng-practices/review/reviewer/standard.html
- https://static0.smartbear.co/support/media/resources/cc/book/code-review-cisco-case-study.pdf
- https://vitalii4reva.medium.com/the-hidden-cost-of-slow-code-reviews-data-from-8-million-prs-9926849f1428
- https://abseil.io/resources/swe-book/html/ch09.html
spiral:
- feedback-loops
- cost-of-delay
status: ready
summary: 'Review exists to catch design and intent, not style — automate style. The
  dominant cost is latency: a PR waiting a day blocks the author. And defect detection
  collapses past a few hundred lines, so big diffs get rubber-stamped.'
title: 'Code review: what humans catch, what tooling should, and why latency is the
  real cost'
track: engineering-practice
unit: 03-code-review
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A 1,800-line PR lands Friday at 5pm: a new billing flow, a schema migration, and a refactor, all in one diff. Two reviewers leave four comments — a rename, a missing semicolon, a "nit: prefer const" — and approve. Three weeks later the migration double-charges a cohort of customers because a retry path wasn't idempotent. Nobody saw it. The diff was too big to actually read, so the reviewers reviewed what was easy to see: trivia. The real flaw was in the design, and the design is exactly what a giant LGTM'd PR hides.
</Hook>

<Crux>Review catches design and intent; tooling catches style. The real cost is latency, and big diffs get rubber-stamped.</Crux>

<Explanation>

## Review catches what tooling can't

A senior reviewer's scarce attention should go to the things a machine cannot judge: is this the right design, does it actually do what the ticket intended, will the next engineer understand it, is the error path correct, does it open a security hole. Google's own standard puts design first — "the most important thing to cover in a review" — because design choices compound and are expensive to undo later, while a misplaced brace costs nothing.

Everything a tool can decide deterministically should never reach a human. Formatting, import order, naming conventions, lint rules, unused variables — run a formatter and a linter in CI and make them blocking. The moment a person types "nit: spacing," you've spent the most expensive review resource you have (senior judgement) on the cheapest possible problem, and you've taught the author that review is about surface. Google's engineering culture is explicit: automate style enforcement so style never reaches review, then train reviewers to spend their attention on correctness, security, and architecture.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Concern</th>
        <th class="border border-border px-3 py-2 text-left">Who should catch it</th>
        <th class="border border-border px-3 py-2 text-left">Why</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Formatting, import order, spacing</td>
        <td class="border border-border px-3 py-2">Formatter (CI)</td>
        <td class="border border-border px-3 py-2">Deterministic; zero judgement; should never reach a human</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Unused vars, obvious bugs, lint rules</td>
        <td class="border border-border px-3 py-2">Linter / static analysis (CI)</td>
        <td class="border border-border px-3 py-2">Machine-checkable; blocking gate before review starts</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Is this the right design?</td>
        <td class="border border-border px-3 py-2">Human reviewer</td>
        <td class="border border-border px-3 py-2">Needs context, intent, system knowledge — no tool can judge it</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Does it match the ticket's intent?</td>
        <td class="border border-border px-3 py-2">Human reviewer</td>
        <td class="border border-border px-3 py-2">Correct code that solves the wrong problem still ships a bug</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Maintainability, naming-for-meaning</td>
        <td class="border border-border px-3 py-2">Human reviewer</td>
        <td class="border border-border px-3 py-2">"Will the next dev understand this?" is a judgement call</td>
      </tr>
    </tbody>
  </table>
</div>

## Latency is the cost nobody measures

The number that matters most isn't defect count — it's turnaround. A PR is blocked work: the author can't merge, often can't start the next thing cleanly, and every hour it sits is an hour of someone's progress frozen. The data is stark. Analysis across roughly 8 million pull requests found elite teams close the full review cycle in under 26 hours, while lagging teams let PRs sit a week before review even *starts*. The single biggest chunk of that time isn't reviewing — it's the initial pickup, the gap before anyone looks. Distributed teams pay another 8–16 hours per round just to timezone gaps.

That idle time has a price. The same analysis put the cost of review-bottleneck waiting at roughly 5.8 hours per developer per week, around $238K/year of frozen time for a ten-person team — and that's *waiting only*, not the review itself. This is why DORA treats lead time as a core health metric: slow review directly degrades throughput, and a team optimising purely for "thorough review" while ignoring latency is trading a small defect win for a large flow loss.

<Inset kind="why" lang="en">
The fastest fix for review latency is almost never "review faster" — it's "make PRs smaller." A 50-line PR is picked up in minutes because it's cheap to context-switch into; a 1,500-line PR sits because nobody has a free hour. Small PRs attack both problems at once: faster pickup *and* higher defect detection. Latency and quality are usually the same lever, not a tradeoff.
</Inset>

## Small-PR economics: defect detection falls off a cliff

SmartBear's study at Cisco — 2,500 reviews over 3.2 million lines of code, the largest of its kind — found the sweet spot is 200–400 lines of code per review. Inside that band, with the review spread over no more than 60–90 minutes, you get around a 70%+ defect yield. Push past 400 lines and detection drops sharply; the human stops reading and starts skimming. Pace matters too: reviewers going slower than ~400 LOC/hour were above average at finding defects, but past ~500 LOC/hour, defect density came in below average in 87% of cases.

This is the mechanism behind the rubber stamp. A reviewer faced with a 1,800-line diff doesn't have the budget to hold it all in their head, so they do the only thing that fits: they comment on the things that are locally visible (a name, a style nit) and approve. The giant PR doesn't get *more* scrutiny for being big — it gets *less*, because attention doesn't scale with size and the reviewer's cognitive limit is fixed. Big diffs are where real design flaws hide precisely because they're the diffs nobody can fully read.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">PR size</th>
        <th class="border border-border px-3 py-2 text-left">What actually happens</th>
        <th class="border border-border px-3 py-2 text-left">Defect detection</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>LOC &lt; 200</code></td>
        <td class="border border-border px-3 py-2">Read fully, fast pickup, real design discussion</td>
        <td class="border border-border px-3 py-2">High</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>200–400 LOC</code></td>
        <td class="border border-border px-3 py-2">Sweet spot: 60–90 min, full attention</td>
        <td class="border border-border px-3 py-2">70–90% yield</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>400–800 LOC</code></td>
        <td class="border border-border px-3 py-2">Attention thins; skimming begins</td>
        <td class="border border-border px-3 py-2">Falling</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>LOC &gt; 800</code></td>
        <td class="border border-border px-3 py-2">Rubber stamp: nits + LGTM, design unread</td>
        <td class="border border-border px-3 py-2">Collapses</td>
      </tr>
    </tbody>
  </table>
</div>

## The senior tradeoff: thoroughness vs throughput, sharing vs gatekeeping

Every review is a bet on where to spend a fixed budget. Crank thoroughness to the maximum and you slow the whole team's flow; optimise purely for throughput and defects leak. The senior move is to spend depth where the risk is — the design, the data migration, the auth boundary, the money path — and to let low-risk diffs through fast. Google's standard codifies this with a deliberately humane rule: approve once the change *definitely improves overall code health*, even if it isn't perfect. "Not perfect" is not a reason to block; "makes the codebase worse" is.

The second axis is cultural: review as knowledge-sharing versus review as gatekeeping. Done well, review spreads context — the author learns the system, the reviewer learns the change, and the bus factor goes up. Done as a status game, it becomes bikeshedding (Parkinson's Law of Triviality: people argue endlessly about the colour of the bike shed because it's the only part they understand) and gatekeeping, where a reviewer blocks to assert control rather than to improve the code. The tell is a thread with twelve comments about naming on a PR whose actual design flaw shipped untouched. A review that nitpicks trivia while waving through a flawed design has inverted its entire purpose.

<TradeoffMatrix
  id="03-code-review-tradeoff-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="A teammate opens a 1,400-line PR bundling a feature, a refactor, and a migration. It's been blocking their next task for two days. As reviewer, what's the senior call?"
  options={[
    { name: "Ask them to split it into a few small PRs (feature / refactor / migration), reviewed independently", summary: "Split it.", correct: true, justification: "This attacks the real problem at the root. At 1,400 lines you are past the 200–400 LOC band where detection is 70–90% — you will skim and rubber-stamp the design while leaving nits. Splitting restores per-PR attention, gives each piece fast pickup (cutting the latency that's already cost two days), and isolates the risky migration so it gets real scrutiny instead of being buried. Smaller PRs is the single change that improves both quality and flow." },
    { name: "Approve with a few style nits so they're unblocked — speed matters and they've waited two days", summary: "Rubber stamp.", justification: "This is exactly the failure mode. Optimising purely for throughput on a diff too big to read means the design and the migration go unreviewed; the nits create the illusion of scrutiny. The two days of latency is real pain, but the fix is smaller PRs next time, not waving through an unreadable one — that's how an un-idempotent retry path double-charges customers in three weeks." },
    { name: "Block hard and demand a full design doc before re-reviewing a single line", summary: "Gatekeep.", justification: "Over-correcting into gatekeeping. A design doc may help for the migration, but freezing all 1,400 lines behind a heavyweight process punishes the author for a structural problem (one giant PR) you can fix cheaply by splitting. Review should improve code health and share knowledge, not assert control or maximise ceremony." },
    { name: "Read only the migration carefully, skim the rest, approve", summary: "Spot-check.", justification: "Better instinct than a blind LGTM — spending depth on the riskiest part is correct senior triage. But it's a patch on a bad shape: the refactor and feature still ride through under-reviewed, and the next 1,400-line PR repeats the problem. Use it as a stopgap, then ask for the split so attention isn't rationed by accident." },
  ]}
/>

<Quiz
  id="03-code-review-quiz-1"
  lessonSlug="01-overview"
  lang="en"
  question="A reviewer leaves six comments on import ordering and variable spacing, then approves. What's the core problem?"
  choices={[
    { label: "Style is machine-checkable — a formatter/linter should catch it in CI so human attention goes to design and correctness", correct: true },
    { label: "The reviewer should have left more style comments to be thorough", misconception: "More style nits is the opposite of thoroughness — it spends senior judgement on the cheapest, most automatable problem while design goes unreviewed." },
    { label: "Style review is the most valuable thing humans do", misconception: "Style is exactly what tooling does best; deterministic rules belong in CI. Humans are scarce judgement for design, intent, and correctness." },
    { label: "Nothing — comments mean a careful review", misconception: "Comment count isn't review quality. A wall of nits often signals the reviewer engaged with surface trivia and never with the design." },
  ]}
/>

<Quiz
  id="03-code-review-quiz-2"
  lessonSlug="01-overview"
  lang="en"
  question="Per the SmartBear/Cisco study, what happens to defect detection as a single review grows past ~400 LOC?"
  choices={[
    { label: "It drops sharply — the reviewer stops reading and starts skimming; the 200–400 LOC band yields 70–90%", correct: true },
    { label: "It improves, because a bigger diff gives more context", misconception: "Attention doesn't scale with size; the reviewer's cognitive budget is fixed, so a bigger diff gets proportionally less real scrutiny, not more." },
    { label: "It stays flat regardless of size", misconception: "The study found a clear falloff past ~400 LOC and below-average defect density past ~450 LOC/hour in 87% of cases — size strongly affects detection." },
    { label: "It only depends on reviewer seniority, not size", misconception: "Seniority helps, but the cognitive limit is human and fixed; even a strong reviewer skims a 1,800-line diff because it can't be held in working memory." },
  ]}
/>

<DragOrder
  id="03-code-review-drag-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="Order a healthy review pipeline so human attention lands where it's worth most:"
  items={[
    { id: "a", label: "Formatter + linter run in CI and block — style/format never reaches a human" },
    { id: "b", label: "Keep the PR small (aim ~200–400 LOC) so it's readable in one sitting" },
    { id: "c", label: "Pick it up fast — initial latency is the biggest part of turnaround" },
    { id: "d", label: "Spend reviewer depth on the highest-risk parts: design, migration, auth, money path" },
    { id: "e", label: "Approve once it definitely improves code health — 'not perfect' isn't a block" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="Code review lifecycle"
  hue="var(--d-systems)"
  perRow={5}
  nodes={[
    {id:"open", label:"Open PR", sub:"small diff"},
    {id:"ci", label:"CI gate", sub:"lint·format·types"},
    {id:"review", label:"Human review", sub:"design·intent"},
    {id:"changes", label:"Changes", sub:"author revises"},
    {id:"approve", label:"Approve", sub:"LGTM"},
    {id:"merge", label:"Merge", sub:"code ships"},
  ]}
  edges={[
    {from:"open", to:"ci"},
    {from:"ci", to:"review", label:"green"},
    {from:"review", to:"changes", label:"feedback"},
    {from:"changes", to:"review", label:"re-review"},
    {from:"review", to:"approve", label:"health up"},
    {from:"approve", to:"merge"},
  ]}
  caption="CI filters the mechanical so reviewers spend their budget on design; changes loop back for another pass until approved."
/>

<KeyTakeaway>Automate style so humans review design, intent, and maintainability. Keep PRs ~200–400 LOC — detection collapses past that and big diffs get rubber-stamped. Latency is the real cost: a PR waiting blocks the author. Approve once it improves code health.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-code-review-retrieval"
  lang="en"
  questions={[
    { q: "A teammate says 'thorough review means catching everything, so bigger reviews should catch more bugs.' Explain why the data says the opposite.", a: "Attention is a fixed budget, not a function of diff size. The SmartBear/Cisco study of 2,500 reviews over 3.2 million lines found the sweet spot is 200–400 LOC per review, yielding 70–90% defect detection when spread over 60–90 minutes; past ~400 lines detection drops sharply, and past ~450 LOC/hour defect density was below average in 87% of cases. The mechanism is cognitive: a reviewer can hold a few hundred lines in working memory and reason about the design, but a 1,800-line diff exceeds that, so they skim. What's left is the locally visible stuff — a name, a spacing nit — and an approval. So a bigger PR gets less effective scrutiny, not more, which is exactly why real design flaws hide in giant diffs. The fix isn't 'review harder', it's smaller PRs, which also speed up pickup — quality and latency are the same lever." },
    { q: "Why is review latency, not defect count, often the metric a senior watches first — and what's the cheapest way to improve it?", a: "A PR in review is blocked work: the author can't merge and often can't cleanly start the next task, so every hour it sits is frozen progress for the whole pipeline. Data across ~8 million PRs showed elite teams close the full cycle in under 26 hours while lagging teams let PRs sit a week before review even starts — and the biggest chunk of that delay is the initial pickup, the gap before anyone looks, with distributed teams paying another 8–16 hours per round to timezone gaps. The waiting alone costs roughly 5.8 hours per developer per week, about $238K/year of idle time for a ten-person team. DORA treats lead time as a core health metric for this reason. The cheapest improvement is almost never 'review faster' — it's smaller PRs, which are cheap to context-switch into and so get picked up in minutes instead of sitting for a free hour nobody has." },
  ]}
/>

<Recap lang="en">
Code review earns its keep on the things tooling can't judge — design, intent, maintainability, the correct error and money paths — so push everything deterministic (format, lint, style) into a blocking CI gate and never spend a human on it. The cost nobody measures is latency: a PR in review is blocked work, elite teams close the cycle in under 26 hours while laggards let it sit a week, and the waiting alone burns ~5.8 hours per developer per week. Defect detection peaks at 200–400 LOC and collapses past ~400, which is why giant PRs get a nits-and-LGTM rubber stamp while their real design flaws ship untouched. The senior balance is to spend depth where the risk lives, approve once the change definitely improves code health rather than chasing perfection, and treat review as knowledge-sharing rather than a gatekeeping status game. Smaller PRs are the one move that improves quality and flow at the same time.
</Recap>
