---
concepts:
- automated-gates
- static-analysis
- formatter
- shift-left
- false-positive-rate
- human-judgement
deepensInto:
- 05-anti-patterns-and-scaling
estMin: 17
lang: en
lessonType: topic
level: senior
order: 4
prereqs:
- 03-giving-and-receiving-review
slug: 04-automate-the-mechanical
sources:
- https://google.github.io/eng-practices/review/reviewer/standard.html
- https://research.google/pubs/tricorder-building-a-program-analysis-ecosystem/
- https://static0.smartbear.co/support/media/resources/cc/book/code-review-cisco-case-study.pdf
- https://martinfowler.com/bliki/CodeReview.html
spiral:
- feedback-loops
- shift-left
status: ready
summary: Every deterministic check a human performs in review is wasted senior attention.
  Push format, lint, types, and security scanning into a blocking CI gate that runs
  before review, so the human budget lands on design, intent, and correctness — the
  things no tool can decide.
title: Automate the mechanical, reserve humans for judgement
track: engineering-practice
unit: 03-code-review
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A team's PRs averaged eleven comments each, and roughly seven of those eleven were about formatting, import order, or naming case. The two senior reviewers were spending most of a day a week typing "nit: spacing" and "use camelCase here." Then someone added Prettier and ESLint as a blocking CI check that ran before the review request even fired. Overnight the average dropped to four comments, and all four were about behavior: an off-by-one in pagination, a missing await, a dropped error case, a question about a money rounding rule. Nothing changed about how careful the reviewers were. The machine simply stopped handing them the machine's job.
</Hook>

<Crux>Any check a machine can decide deterministically should never reach a human — push it into a blocking gate so review lands on judgement.</Crux>

<Explanation>

## The dividing line is decidability, not importance

The rule isn't "automate the unimportant stuff." Some automated checks — a security scanner catching a SQL injection, a type checker catching a null deref — catch things far more important than most human comments. The real dividing line is **decidability**: can a rule be evaluated mechanically with a stable answer? Formatting, import order, naming conventions, unused variables, lint rules, type errors, known-vulnerable dependencies, and a large class of bug patterns are all decidable — a tool gives the same verdict every time, with no context required. Everything decidable belongs in CI, running *before* the human ever opens the diff, and blocking merge when it fails.

What's left for the human is the **undecidable** residue, which is precisely what review is for: Is this the right design? Does it actually do what the ticket intended? Will the next engineer understand it in six months? Is the error path correct, the money path exact, the auth boundary sound? Google's Standard of Code Review puts design first — "the most important thing to cover" — because design choices compound and are expensive to undo, while a misplaced brace costs nothing and a formatter fixes it for free. Every minute a senior spends on a decidable check is a minute stolen from the undecidable ones only they can do, and it's a *recurring* theft: the same nit, every PR, forever, until you automate it once.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Gate</th>
        <th class="border border-border px-3 py-2 text-left">Catches</th>
        <th class="border border-border px-3 py-2 text-left">Decidable?</th>
        <th class="border border-border px-3 py-2 text-left">When it runs</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Formatter (Prettier, gofmt)</td>
        <td class="border border-border px-3 py-2">Layout, spacing, quotes</td>
        <td class="border border-border px-3 py-2">Yes — auto-fixes</td>
        <td class="border border-border px-3 py-2">Pre-commit + CI block</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Linter / type checker</td>
        <td class="border border-border px-3 py-2">Unused vars, null derefs, lint rules</td>
        <td class="border border-border px-3 py-2">Yes</td>
        <td class="border border-border px-3 py-2">CI block, before review</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">SAST / dependency scan</td>
        <td class="border border-border px-3 py-2">Injection, known CVEs, secrets</td>
        <td class="border border-border px-3 py-2">Mostly — needs tuning</td>
        <td class="border border-border px-3 py-2">CI block, before review</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Human reviewer</td>
        <td class="border border-border px-3 py-2">Design, intent, maintainability, correctness</td>
        <td class="border border-border px-3 py-2">No — needs judgement</td>
        <td class="border border-border px-3 py-2">After gates are green</td>
      </tr>
    </tbody>
  </table>
</div>

## Shift left: the gate runs before the human, and it must be cheap to obey

Sequencing is half the value. A linter that only runs *after* a human approves is theatre — the nit was already typed. The gate must run **before the review request**, ideally also pre-commit on the author's machine, so the author fixes mechanical issues themselves and the reviewer receives a diff where every decidable check is already green. This is the same shift-left logic as trunk-based CI: catch the cheap class of problem at the earliest, cheapest point, and free the expensive resource (human judgement, end-to-end testing) for the problems that can only be caught late. The author-preparation effect from the SmartBear study reinforces it — a clean, pre-checked diff is one the author has already had to look at carefully, which itself drives defect density down.

But automation has a failure mode that will quietly destroy it: **false positives**. A static-analysis tool that flags ten non-issues for every real one trains engineers to ignore it, and then it catches nothing because everyone reflexively dismisses its output — the same alert-fatigue dynamic as a pager that cries wolf. Google's Tricorder program treated this as the central design constraint, holding analyzers to a strict **effective-false-positive rate under 10%** and giving developers a one-click "not useful" feedback channel to kill noisy checks. The lesson is that "turn on more static analysis" is not free: a noisy analyzer is worse than none, because it spends trust you can't easily rebuild. Tune for signal, make the fix obvious, and let developers prune checks that don't earn their place.

<Inset kind="why" lang="en">
The deeper reason to automate the mechanical isn't reviewer time — it's calibration of the whole team's model of what review means. When a junior's first PRs come back full of spacing nits, they learn that review is about surface, and they review others the same way, and the culture ossifies around trivia while design flaws sail through. When the machine owns all of that and every human comment is about behavior or design, the team learns that review is for the things that matter. Automation doesn't just save attention; it teaches everyone where attention is supposed to go.
</Inset>

## Automation redirects review, it doesn't replace it

The seductive overreach is to conclude that if tools catch bugs, tools can replace review. They can't — they shift the boundary, they don't erase it. A linter has no concept of whether a feature solves the user's actual problem; a SAST tool can't tell you the retry path isn't idempotent; an AI reviewer can summarize a diff but still can't certify that the design will survive next quarter's requirements. What automation does is **redirect** the human: by clearing the decidable layer, it concentrates scarce judgement on the undecidable layer where the expensive defects live. A team that wires up formatters, linters, type checks, and tuned SAST as a blocking pre-review gate isn't reviewing less — it's reviewing *only the things worth a human*, which is the entire point of the practice.

The end state is a tiered pipeline. Tier 0 is the author's own pre-commit checks and annotated diff. Tier 1 is the blocking CI gate — format, lint, types, tests, dependency and security scans — that a PR must pass before a human is even asked. Tier 2 is human review, now spending its full, undiluted budget on design, intent, correctness, and knowledge transfer. Each tier catches what's cheapest to catch at that layer and passes the irreducible residue up. The single highest-leverage move most teams haven't fully made is simply ensuring no human ever types a comment a machine could have made.

<TradeoffMatrix
  id="04-automate-the-mechanical-tradeoff-1"
  lessonSlug="04-automate-the-mechanical"
  lang="en"
  prompt="Your PRs are dominated by style and formatting comments, and seniors are burning hours on nits. What's the right structural fix?"
  options={[
    { name: "Add formatter + linter + type check as a blocking CI gate that runs before the review request (and pre-commit locally)", summary: "Gate before review.", correct: true, justification: "This removes the entire decidable class from human review at the earliest point. The author fixes mechanical issues themselves, the reviewer gets a diff that's already green on everything a machine can decide, and senior attention redirects to design and correctness. It's a one-time setup that ends a recurring per-PR tax — exactly the right layer for the problem." },
    { name: "Write a long style-guide doc and ask reviewers to enforce it manually", summary: "Document the nits.", justification: "A doc still routes every style decision through a human on every PR — the recurring tax stays, just better specified. Decidable rules belong in a tool that applies them automatically and identically; a document that humans enforce is the slow, inconsistent version of a linter." },
    { name: "Turn on every available static-analysis rule for maximum coverage", summary: "Max analysis.", justification: "Untuned analysis floods reviews with false positives, and a tool that cries wolf gets reflexively ignored until it catches nothing — Google held Tricorder under a 10% effective-false-positive rate for exactly this reason. More rules isn't more signal; a noisy analyzer is worse than none. Tune for signal and let devs prune." },
    { name: "Tell reviewers to just ignore style and only comment on design", summary: "Ignore by convention.", justification: "Closer to the goal, but convention without a gate is fragile — style issues still land in the diff, still distract, and a tired reviewer still types the nit. The robust fix is to make the decidable class structurally impossible to reach review by blocking on it in CI, not to rely on everyone's discipline." },
  ]}
/>

<Quiz
  id="04-automate-the-mechanical-quiz-1"
  lessonSlug="04-automate-the-mechanical"
  lang="en"
  question="What's the actual dividing line for what to automate versus leave to a human reviewer?"
  choices={[
    { label: "Decidability — if a rule can be evaluated mechanically with a stable answer, it belongs in a gate; the undecidable residue (design, intent, correctness) is for humans", correct: true },
    { label: "Importance — automate the trivial stuff, humans handle the important stuff", misconception: "Importance isn't the line: a SAST scanner catching SQL injection is automated yet hugely important. The line is whether a tool can decide it deterministically, not how much it matters." },
    { label: "Speed — automate whatever is slow for humans", misconception: "Some human judgement is slow but irreplaceable (design review). The criterion is decidability — can a machine give a stable verdict — not how long a human would take." },
    { label: "Cost — automate whatever is expensive to fix", misconception: "Fix cost doesn't determine ownership. A design flaw is expensive to fix yet undecidable, so it stays human; formatting is cheap to fix yet decidable, so it's automated." },
  ]}
/>

<Quiz
  id="04-automate-the-mechanical-quiz-2"
  lessonSlug="04-automate-the-mechanical"
  lang="en"
  question="Why did Google's Tricorder hold analyzers to an effective-false-positive rate under 10%?"
  choices={[
    { label: "A tool that flags many non-issues trains developers to ignore it, so it ends up catching nothing — noisy analysis is worse than none", correct: true },
    { label: "False positives slow down the CI build", misconception: "The cost isn't build time — it's trust. Once developers learn an analyzer cries wolf, they reflexively dismiss all of its output, including the true positives, so it stops protecting anything." },
    { label: "10% is an arbitrary number with no real effect", misconception: "It's a deliberate threshold tied to whether developers keep paying attention. Past a certain noise level the tool loses credibility and is mentally filtered out, which is the failure mode the limit prevents." },
    { label: "Below 10% the tool can replace human review entirely", misconception: "Low false positives don't make a tool able to judge design or intent. Tuning keeps the tool trusted, but automation redirects human review to the undecidable layer — it never replaces it." },
  ]}
/>

<DragOrder
  id="04-automate-the-mechanical-drag-1"
  lessonSlug="04-automate-the-mechanical"
  lang="en"
  prompt="Order a tiered pipeline so human attention only ever sees the undecidable layer:"
  items={[
    { id: "a", label: "Author runs pre-commit format/lint locally and annotates the diff" },
    { id: "b", label: "Blocking CI gate runs format, lint, types, tests, tuned SAST — must be green" },
    { id: "c", label: "Review request fires only after every decidable check passes" },
    { id: "d", label: "Human spends full budget on design, intent, correctness, maintainability" },
    { id: "e", label: "Noisy checks that draw 'not useful' feedback are pruned to protect trust" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="Tiered review pipeline — decidable gates run before humans"
  hue="var(--d-systems)"
  perRow={4}
  nodes={[
    {id:"author", label:"Author", sub:"pre-commit check"},
    {id:"fmt", label:"Formatter", sub:"auto-fix"},
    {id:"lint", label:"Lint + types", sub:"CI block"},
    {id:"sast", label:"SAST + deps", sub:"CI block"},
    {id:"human", label:"Human review", sub:"design·intent"},
    {id:"merge", label:"Merge", sub:"only judgement"},
  ]}
  edges={[
    {from:"author", to:"fmt"},
    {from:"fmt", to:"lint"},
    {from:"lint", to:"sast"},
    {from:"sast", to:"human", label:"all green"},
    {from:"human", to:"merge", label:"LGTM"},
  ]}
  caption="Each gate catches what's cheapest at that layer. The human never sees a decidable issue — full budget goes to design and correctness."
/>

<KeyTakeaway>The line is decidability, not importance: anything a machine can verify with a stable answer — format, lint, types, security scans — belongs in a blocking CI gate that runs before review, so human attention lands only on the undecidable residue: design, intent, correctness. Tune for a low false-positive rate or the tool gets ignored, and remember automation redirects review, it never replaces it.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-automate-the-mechanical-retrieval"
  lang="en"
  questions={[
    { q: "A teammate says 'automate the unimportant checks, humans do the important ones.' Why is 'importance' the wrong dividing line, and what's the right one?", a: "Importance is wrong because plenty of automated checks catch things far more important than most human comments — a SAST scanner catching SQL injection, a type checker catching a null deref, a dependency scan catching a known CVE. The right line is decidability: can a rule be evaluated mechanically with a stable answer that needs no context? Formatting, import order, naming, unused variables, lint rules, type errors, known-vulnerable dependencies, and a large class of bug patterns are all decidable, so they belong in CI, running before the human opens the diff and blocking merge on failure. What's left for the human is the undecidable residue, which is exactly what review is for: is this the right design, does it match the ticket's intent, will the next engineer understand it, is the error and money and auth path correct. Google's Standard puts design first because design compounds and is expensive to undo, while a brace costs nothing and a formatter fixes it free. Every minute a senior spends on a decidable check is stolen from the undecidable ones only they can do, and it's a recurring theft — the same nit on every PR until you automate it once." },
    { q: "What are the two failure modes to avoid when leaning on automated gates, and what does the tiered end state look like?", a: "First, sequencing: a linter that runs after a human approves is theatre, because the nit was already typed. The gate must run before the review request — ideally pre-commit on the author's machine too — so the author fixes mechanical issues and the reviewer receives a diff already green on every decidable check; this is shift-left, the same logic as trunk-based CI, and it compounds with the author-preparation effect that drives defect density down. Second, false positives: a tool that flags ten non-issues per real one trains everyone to ignore it, and then it catches nothing — the alert-fatigue dynamic of a pager that cries wolf. Google's Tricorder treated this as the central constraint, holding analyzers under a 10% effective-false-positive rate with a one-click 'not useful' channel to prune noisy checks; a noisy analyzer is worse than none because it spends trust you can't rebuild. The end state is tiered: Tier 0 is the author's pre-commit checks and annotated diff; Tier 1 is the blocking CI gate (format, lint, types, tests, tuned SAST) a PR must pass before a human is asked; Tier 2 is human review spending its undiluted budget on design, intent, correctness, and knowledge transfer. Automation redirects review to the undecidable layer; it never replaces it." },
  ]}
/>

<Recap lang="en">
The reason to automate the mechanical parts of review isn't merely to save reviewer time — it's to put the right class of problem at the right layer and to teach the whole team where attention belongs. The dividing line is decidability, not importance: any rule a machine can evaluate with a stable, context-free answer — formatting, import order, naming, unused variables, lint, types, known-vulnerable dependencies, and a wide band of bug patterns — belongs in a blocking CI gate, ideally mirrored pre-commit, that runs before the review request so the author clears it and the reviewer never sees it. That leaves the human with the undecidable residue that review actually exists for: is the design right, does it match intent, will it be maintainable, are the error, money, and auth paths correct. Sequencing matters — a gate that runs after approval is theatre — and so does signal: an untuned analyzer that cries wolf gets ignored and protects nothing, which is why Google's Tricorder held false positives under 10% with a one-click prune channel. The seductive overreach is to think tools can replace review; they only redirect it, concentrating scarce judgement where the expensive defects hide. The mature end state is tiered — author pre-checks, then a blocking CI gate, then human review on design and correctness alone — and the single highest-leverage rule is that no human should ever type a comment a machine could have made.
</Recap>
