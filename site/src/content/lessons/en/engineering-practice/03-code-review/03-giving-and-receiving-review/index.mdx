---
concepts:
- severity-tagging
- conventional-comments
- actionable-feedback
- author-annotation
- ego-effect
- nitpick-vs-blocker
deepensInto:
- 04-automate-the-mechanical
estMin: 16
lang: en
lessonType: topic
level: middle
order: 3
prereqs:
- 02-pr-size-and-latency
slug: 03-giving-and-receiving-review
sources:
- https://conventionalcomments.org/
- https://google.github.io/eng-practices/review/reviewer/comments.html
- https://static0.smartbear.co/support/media/resources/cc/book/code-review-cisco-case-study.pdf
- https://google.github.io/eng-practices/review/developer/handling-comments.html
spiral:
- feedback-loops
- psychological-safety
status: ready
summary: A review comment that doesn''''t say its severity or its fix is a guess the
  author has to decode. Tag blocker vs nit, make it actionable, attack the code not
  the person. And the author''''s annotations do more for defects than any reviewer
  — preparation finds bugs before review s
title: Giving and receiving review without the friction
track: engineering-practice
unit: 03-code-review
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
Diego, two weeks into a new team, opens his first PR and gets back a comment that just says: "Why is this a Map?" He spends an hour rewriting it as an object, re-pushes, and gets: "No, the Map was fine — I was asking why not a Set." The reviewer meant a thought, not a change; Diego read it as a blocker. Down the hall, Lena gets "this whole approach is wrong" with no alternative on a PR she shipped under deadline, and quietly stops asking that reviewer for early feedback. Neither comment was malicious. Both were ambiguous about two things every comment must answer: how important is this, and what do you want me to do?
</Hook>

<Crux>A comment must say its severity and its action — otherwise the author guesses, and a wrong guess costs a round-trip and some goodwill.</Crux>

<Explanation>

## Every comment carries an implicit severity — make it explicit

The single biggest source of review friction is unlabeled severity. A reviewer drops "consider extracting this" and the author can't tell if it's a hard blocker, a suggestion, or idle musing — so they either over-react (rewrite something that was fine) or under-react (ignore a real concern). The fix is a tiny, durable convention: prefix every comment with its weight. **Conventional Comments** formalizes this — labels like `nit:`, `suggestion:`, `issue:`, `question:`, `praise:`, and `thought:`, optionally marked `(blocking)` or `(non-blocking)`. The label is the contract: a `nit:` is explicitly droppable, an `issue: (blocking)` must be resolved before merge, a `question:` wants an answer not a change. Google's guidance lands in the same place: prefix genuine nits with "Nit:" so the author knows it's polish, not a gate.

This matters because the two things a comment must answer — *how important* and *what to do* — are exactly what reviewers leave implicit under time pressure. Severity tagging makes the first explicit; making the comment **actionable** makes the second explicit. "This is confusing" is a verdict; "extract `validateOrder` so the early-return path is obvious — happy to pair if useful" is something the author can act on without a round-trip. When the reasoning isn't obvious, state it: "this allocates on every request; cache it on the module" teaches, where "don't allocate here" just commands.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Label</th>
        <th class="border border-border px-3 py-2 text-left">Means</th>
        <th class="border border-border px-3 py-2 text-left">Author should</th>
        <th class="border border-border px-3 py-2 text-left">Blocks merge?</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>issue: (blocking)</code></td>
        <td class="border border-border px-3 py-2">Real defect / correctness / security</td>
        <td class="border border-border px-3 py-2">Fix or discuss before merge</td>
        <td class="border border-border px-3 py-2">Yes</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>suggestion:</code></td>
        <td class="border border-border px-3 py-2">A concrete better option</td>
        <td class="border border-border px-3 py-2">Consider; apply if it improves health</td>
        <td class="border border-border px-3 py-2">Usually no</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>question:</code></td>
        <td class="border border-border px-3 py-2">Reviewer doesn't understand yet</td>
        <td class="border border-border px-3 py-2">Answer — may need no code change</td>
        <td class="border border-border px-3 py-2">Until answered</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>nit:</code></td>
        <td class="border border-border px-3 py-2">Trivial polish, reviewer's taste</td>
        <td class="border border-border px-3 py-2">Take it or leave it</td>
        <td class="border border-border px-3 py-2">No</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>praise:</code></td>
        <td class="border border-border px-3 py-2">Genuinely good work</td>
        <td class="border border-border px-3 py-2">Nothing — it's a signal</td>
        <td class="border border-border px-3 py-2">No</td>
      </tr>
    </tbody>
  </table>
</div>

## Attack the code, not the author — and the author's prep beats every reviewer

Review is a social act, and the failure modes are social. "This whole approach is wrong" attacks the person's judgement; "this approach breaks under concurrent writes — here's the race" attacks the code and hands over a fact. The difference isn't politeness theatre; it's whether the author can engage with the substance or has to defend their competence first. Psychological safety is load-bearing: when people fear that a PR is a competence exam, they ship smaller, hide uncertainty, and stop asking for the early feedback that catches the biggest mistakes — exactly what happened to Lena. A reviewer who blocks to assert status rather than to improve the code (Parkinson's bikeshedding — twelve comments on a variable name while the design flaw ships untouched) has inverted the purpose.

The most under-appreciated finding in the SmartBear study flips the usual focus from reviewer to author. **Author preparation** — annotating your own diff with explanations before anyone reviews it — had a dramatic effect: across all reviews with at least one author-preparation comment, defect density was *never above 30, and most commonly zero*. The mechanism is that explaining your change forces you to re-think it, and you catch your own bugs in the act of justifying them — the review's value starts before the reviewer arrives. The related **ego effect** compounds it: knowing a peer will read your code makes you write it cleaner in the first place. So the highest-leverage reviewer-side habit (clear, tagged, actionable comments) is matched by an author-side one (annotate the diff, narrate the non-obvious decisions) that finds defects no reviewer ever sees.

<Inset kind="why" lang="en">
Receiving review well is a skill, not a personality trait, and it's the other half of the loop. Assume good intent, separate your ego from the diff, and treat every comment as information about the code rather than a verdict on you. Push back when you disagree — "I chose a Map because lookups dominate; a Set loses the value" — because a review is a conversation, not a compliance check. But pick the hill: relitigating a `nit:` burns the goodwill you'll want when an `issue: (blocking)` is genuinely wrong. The authors who grow fastest are the ones who mine review comments for the model behind them, not just the line-level fix.
</Inset>

## Resolve disagreement on facts, escalate on judgement

Most review threads die quietly when both sides ground the discussion in the code: a benchmark, a failing test, a concrete race condition. "I think it's cleaner" versus "I think mine is cleaner" is unresolvable and corrosive; "here's a test that fails on your version" ends it. When a thread can't be settled on facts — it's a genuine judgement call about design direction — the senior move is to *escalate, don't entrench*: pull in a third opinion or the area owner, timebox the debate, and let the change land if it improves health. Google's standard is explicit that "not perfect" is never a reason to block; "makes the codebase worse" is. A reviewer who holds a PR hostage over taste is optimizing for being right over the team shipping.

The throughput cost of getting this wrong is measurable. Excessive nitpicking — unlabeled, non-actionable, taste-driven comments — has been estimated to cause 20–40% velocity losses, and worse, it *obscures* the serious problems by burying the one blocking comment under ten cosmetic ones. So the discipline isn't only kindness; it's signal hygiene. Tag severity so the author can triage. Make comments actionable so they don't bounce. Praise real work so the channel isn't pure negativity. And keep the human attention — yours and the author's — pointed at design, correctness, and intent, which is the only thing review is actually for.

<TradeoffMatrix
  id="03-giving-and-receiving-review-tradeoff-1"
  lessonSlug="03-giving-and-receiving-review"
  lang="en"
  prompt="You're reviewing a PR with one real concurrency bug and several style/taste preferences. How do you write the review?"
  options={[
    { name: "Tag the bug 'issue: (blocking)' with the concrete failing scenario, mark taste items 'nit:', and skip or fold cosmetic ones the linter should own", summary: "Tag and triage.", correct: true, justification: "This makes both required answers explicit — severity (one blocker, the rest droppable) and action (a concrete scenario the author can fix). The author triages instantly: fix the race, take or leave the nits. It keeps the one serious comment from being buried under cosmetics and it doesn't spend the channel on things a linter should enforce." },
    { name: "Leave every comment unlabeled so the author decides what matters", summary: "Unlabeled.", justification: "This is the core friction generator. Without severity the author can't tell the blocking race from a taste preference, so they either over-react and rewrite fine code or under-react and ship the bug. Unlabeled comments are exactly what causes round-trips and the 20–40% velocity loss from nitpick noise." },
    { name: "Block the PR and write 'this whole approach is wrong, redo it'", summary: "Attack the approach.", justification: "It attacks judgement instead of code, gives no actionable path, and provides no severity beyond 'blocked'. The author can't engage with substance because there isn't any — just a verdict. This is how you lose people's willingness to seek early feedback, the feedback that catches the biggest mistakes." },
    { name: "Approve and mention the bug in chat later so you don't slow them down", summary: "Defer the blocker.", justification: "Speed is good, but a real concurrency bug is precisely what review exists to stop; approving it defeats the purpose. Out-of-band mentions get lost and aren't tied to the line. Tag it 'blocking' in the review — that's fast and unambiguous, not slow." },
  ]}
/>

<Quiz
  id="03-giving-and-receiving-review-quiz-1"
  lessonSlug="03-giving-and-receiving-review"
  lang="en"
  question="Why does prefixing comments with severity labels (nit/suggestion/issue/question) reduce review friction?"
  choices={[
    { label: "It makes explicit the two things every comment must answer — how important it is and what to do — so the author doesn't have to guess and round-trip", correct: true },
    { label: "It makes reviewers leave more comments overall", misconception: "The goal isn't more comments — it's less ambiguity per comment. Labeling lets the author triage and often reduces total back-and-forth by preventing wrong guesses." },
    { label: "It lets reviewers skip explaining their reasoning", misconception: "Labels set severity, not reasoning. Actionable comments still need the 'why' when it isn't obvious; the label complements the explanation, it doesn't replace it." },
    { label: "It guarantees the author will fix every comment", misconception: "The opposite — a 'nit:' is explicitly droppable. Labels tell the author what's optional versus blocking, which is the point: not everything must be fixed." },
  ]}
/>

<Quiz
  id="03-giving-and-receiving-review-quiz-2"
  lessonSlug="03-giving-and-receiving-review"
  lang="en"
  question="What did the SmartBear study find about author preparation (annotating your own diff)?"
  choices={[
    { label: "Reviews with author-preparation comments had defect density never above 30 and most commonly zero — explaining your change makes you catch your own bugs first", correct: true },
    { label: "Author annotations slow review down with no defect benefit", misconception: "The data is the opposite: annotated diffs had dramatically lower defect density. Re-thinking the change to explain it surfaces bugs before the reviewer even arrives." },
    { label: "Only reviewer effort affects defect detection, not author effort", misconception: "Author preparation was one of the strongest single factors in the study — the author's act of explaining catches defects no reviewer sees, shifting value upstream." },
    { label: "Annotations only help in large PRs", misconception: "The effect held across reviews regardless of size; the mechanism is cognitive (explaining forces re-examination), not size-dependent." },
  ]}
/>

<DragOrder
  id="03-giving-and-receiving-review-drag-1"
  lessonSlug="03-giving-and-receiving-review"
  lang="en"
  prompt="Order a low-friction review exchange that keeps attention on substance:"
  items={[
    { id: "a", label: "Author annotates the diff, narrating the non-obvious decisions before requesting review" },
    { id: "b", label: "Reviewer tags each comment's severity (issue/suggestion/nit) and makes it actionable" },
    { id: "c", label: "Reviewer attacks the code with a concrete fact, not the author's judgement" },
    { id: "d", label: "Author triages: fixes blockers, answers questions, takes-or-leaves nits, pushes back with facts" },
    { id: "e", label: "A genuine judgement split is escalated to a third opinion, not entrenched" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<StackDiagram
  label="Comment severity — top must be resolved, bottom is optional"
  hue="var(--d-systems)"
  layers={[
    {label:"issue: (blocking)", note:"defect·security — fix before merge"},
    {label:"question:", note:"reviewer needs answer — may need no code change"},
    {label:"suggestion:", note:"concrete better option — usually non-blocking"},
    {label:"nit:", note:"polish·taste — take it or leave it"},
    {label:"praise:", note:"genuinely good work — signal only"},
  ]}
  caption="Every comment belongs to exactly one tier. Labeling the tier makes severity explicit and lets the author triage without guessing."
/>

<KeyTakeaway>Every comment must answer two things: how important (tag blocker vs suggestion vs nit) and what to do (make it actionable, give the why). Attack the code with a fact, not the author's judgement — psychological safety keeps people seeking the early feedback that catches the biggest bugs. And the author's own annotations find more defects than any reviewer: preparation drove density to near zero.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-giving-and-receiving-review-retrieval"
  lang="en"
  questions={[
    { q: "A reviewer writes 'consider extracting this' on a function and the author can't tell if it's required. What two things does every comment need to make explicit, and how do you encode them?", a: "Every comment must answer how important it is and what the author should do; leaving either implicit is the single biggest source of review friction, because the author then over-reacts (rewrites fine code) or under-reacts (ignores a real concern) and you eat a round-trip and some goodwill. Encode severity with a label — Conventional Comments uses nit:, suggestion:, issue:, question:, praise:, thought:, optionally marked (blocking) or (non-blocking), and Google's guidance matches with prefixing genuine nits 'Nit:'. The label is a contract: a nit: is explicitly droppable, an issue: (blocking) must be resolved before merge, a question: wants an answer not a change. Encode the action by making the comment actionable and stating the why when it isn't obvious: 'extract validateOrder so the early-return path is obvious' beats 'this is confusing', and 'this allocates on every request, cache it on the module' teaches where 'don't allocate here' only commands. Excessive unlabeled nitpicking is estimated to cost 20–40% velocity and buries the one blocking comment under cosmetic ones." },
    { q: "What did SmartBear find about the author's role in defect detection, and why does psychological safety matter for review?", a: "The study's most under-appreciated result is that author preparation — annotating your own diff with explanations before review — drove defect density to never above 30 and most commonly zero. The mechanism is that explaining your change forces you to re-think it, so you catch your own bugs in the act of justifying them; the review's value starts before the reviewer arrives. The related ego effect compounds it: knowing a peer will read the code makes you write it cleaner up front. Psychological safety is load-bearing on the other side: review is a social act, and 'this whole approach is wrong' attacks the person while 'this breaks under concurrent writes, here's the race' attacks the code and hands over a fact the author can engage with. When people fear a PR is a competence exam, they ship smaller, hide uncertainty, and stop asking for the early feedback that catches the biggest mistakes. So the highest-leverage habits are paired: reviewer-side, tag severity and make comments actionable and aimed at the code; author-side, annotate the diff and narrate the non-obvious decisions. Disagreements get resolved on facts (a failing test, a benchmark) and escalated, not entrenched, when they're genuine judgement calls — 'not perfect' is never a block; 'makes the codebase worse' is." },
  ]}
/>

<Recap lang="en">
Review friction is mostly a communication failure, and two questions fix most of it: how important is this comment, and what do you want me to do. Tag severity with a small durable convention — nit, suggestion, issue, question, praise, optionally blocking or non-blocking — so the author can triage instead of guessing, and make every comment actionable with the reasoning attached so it doesn't bounce back as a round-trip. Aim at the code, not the author: 'this breaks under concurrent writes, here's the race' lets the author engage with a fact, where 'this whole approach is wrong' makes them defend their competence and quietly stop seeking the early feedback that catches the biggest mistakes. The most overlooked lever lives on the author's side — the SmartBear study found that annotating your own diff drove defect density to near zero, because explaining a change forces you to re-examine it, and the ego effect means you write it cleaner just knowing it'll be read. Resolve disagreements on facts, escalate genuine judgement splits rather than entrenching, never block over taste, and keep the channel clean — tagged, actionable, with real praise — so the one serious comment isn't buried under ten cosmetic ones. Done this way, review spreads knowledge and raises the bus factor instead of becoming a status game.
</Recap>
