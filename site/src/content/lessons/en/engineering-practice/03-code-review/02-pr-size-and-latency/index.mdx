---
concepts:
- pr-size
- review-latency
- defect-detection
- batch-size
- stacked-diffs
- rubber-stamp
deepensInto:
- 03-giving-and-receiving-review
estMin: 16
lang: en
lessonType: topic
level: middle
order: 2
prereqs:
- 01-overview
slug: 02-pr-size-and-latency
sources:
- https://google.github.io/eng-practices/review/developer/small-cls.html
- https://static0.smartbear.co/support/media/resources/cc/book/code-review-cisco-case-study.pdf
- https://google.github.io/eng-practices/review/reviewer/speed.html
- https://martinfowler.com/articles/branching-patterns.html
spiral:
- feedback-loops
- cost-of-delay
- batch-size
status: ready
summary: Big PRs don''''t get more scrutiny — they get less, because attention is
  a fixed budget. Detection peaks at 200-400 LOC and collapses past it, while large
  diffs sit longest waiting for a free hour. Small PRs are the one lever that cuts
  latency and raises quality together.
title: PR size sets review latency and detection at once
track: engineering-practice
unit: 03-code-review
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
Priya opens two PRs the same morning. The first is a 40-line bug fix; Sam picks it up between meetings, reads every line, leaves one real question, approves — done before lunch. The second is a 1,600-line "refactor + new export pipeline" she'd been polishing on a branch for nine days. It sits. Nobody has a free 90-minute block, so it waits two days for a reviewer, then gets eleven comments — ten about naming, one about a log line — and an LGTM. The pipeline's batching logic, the actual risk, is never discussed. Three weeks later it silently drops every event that arrives during a retry. The small PR got more real review than the big one, in a tenth of the time.
</Hook>

<Crux>Attention is a fixed budget, so a bigger diff gets less scrutiny and waits longer — small PRs cut latency and raise detection together.</Crux>

<Explanation>

## Detection peaks in a narrow band, then falls off a cliff

The SmartBear study at Cisco — 2,500 reviews over 3.2 million lines, still the largest of its kind — found the sweet spot is **200–400 lines of code per review**, examined over no more than **60–90 minutes**, yielding a **70–90% defect yield**: of ten defects present, you'd find seven to nine. Two thresholds bound that band. Past ~400 lines, detection drops sharply because the reviewer can no longer hold the change in working memory. And pace matters independently: reviewers slower than ~400 LOC/hour were above average at finding defects, but past ~450–500 LOC/hour, defect density came in *below average in 87% of cases*. Detection also collapses after 60–90 minutes of continuous reviewing — concentration is a depleting resource, not a constant.

The non-intuitive consequence: a big PR doesn't earn proportionally more scrutiny for being big. Attention doesn't scale with diff size; the human's cognitive budget is fixed at a few hundred lines. So the 1,600-line PR gets *less effective* review than the 160-line one, not more. This is the mechanism behind the rubber stamp — the reviewer comments on what's locally visible (a name, a log line) and approves, because that's the only thing that fits the budget. Real design flaws hide in giant diffs precisely because giant diffs are the ones nobody can fully read.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">PR size</th>
        <th class="border border-border px-3 py-2 text-left">What the reviewer actually does</th>
        <th class="border border-border px-3 py-2 text-left">Detection</th>
        <th class="border border-border px-3 py-2 text-left">Typical pickup</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>&lt; 200 LOC</code></td>
        <td class="border border-border px-3 py-2">Reads fully in a 5-min gap; real questions</td>
        <td class="border border-border px-3 py-2">High</td>
        <td class="border border-border px-3 py-2">Minutes</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>200–400 LOC</code></td>
        <td class="border border-border px-3 py-2">Sweet spot: 60–90 min, full attention</td>
        <td class="border border-border px-3 py-2">70–90% yield</td>
        <td class="border border-border px-3 py-2">~1 hour</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>400–800 LOC</code></td>
        <td class="border border-border px-3 py-2">Attention thins; skimming begins</td>
        <td class="border border-border px-3 py-2">Falling</td>
        <td class="border border-border px-3 py-2">Hours</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>&gt; 800 LOC</code></td>
        <td class="border border-border px-3 py-2">Rubber stamp: nits + LGTM, design unread</td>
        <td class="border border-border px-3 py-2">Collapses</td>
        <td class="border border-border px-3 py-2">Days (waits for a free block)</td>
      </tr>
    </tbody>
  </table>
</div>

## Latency is a queueing problem, and big PRs starve at the front

A PR in review is blocked work — the author can't merge, often can't cleanly start the next thing, and every hour it sits is frozen progress. The dominant component of turnaround usually isn't the reviewing itself; it's the **pickup latency**, the gap before anyone looks. And pickup is a queueing phenomenon driven by *task size*. Google's own data is the cleanest illustration: developers wait a **median under one hour** for small changes but around **five hours** for very large ones, because a reviewer can find a five-minute slot to read a small CL between meetings, but a 1,500-line change needs a contiguous block that almost never appears on a busy calendar. The big PR doesn't just review worse — it queues longer, because its service time exceeds the gaps in everyone's day.

This is why "review faster" is almost always the wrong instruction. You can't will a free 90-minute block into existence, and pressuring reviewers to make one means they rush and skim — trading the latency win for a detection loss. The variable you actually control is the service time: a 200-line PR fits the slots that already exist. Shrinking PR size attacks both halves of turnaround — shorter queue *and* shorter review — without asking anyone to work faster or skip steps.

<Inset kind="why" lang="en">
The deep point is that PR size couples two things people usually treat as a tradeoff. "Thorough but slow" versus "fast but shallow" feels like a dial you have to position. It isn't — for PR size, both move the same way. A smaller diff is reviewed *faster* (it fits the gaps in a calendar) *and* more *thoroughly* (it fits working memory). The only real cost is the author's discipline to decompose the work, which is exactly the skill trunk-based development demands too. Quality and flow are the same lever here, not opposite ends of one.
</Inset>

## Decompose the batch: stacking, not big-banging

The objection is real: some changes are genuinely large. A migration plus the code that uses it, a refactor that touches forty call sites — you can't always ship 200 lines. The answer is to **decompose the batch**, not to give up and merge a monolith. Split along seams that each stand alone and improve code health independently: land the pure refactor first (behavior-preserving, easy to verify), then the new feature on top of the clean base, then the wiring. **Stacked diffs** make this practical — a chain of small, dependent PRs each reviewed in its own readable window, so the reviewer never faces more than a few hundred lines at once even though the whole change is large.

This is the same batch-size principle from lean that drives trunk-based development: small batches surface problems early and cheaply; large batches hide them until they detonate together. A 1,600-line PR is one enormous batch that can't be partially approved — it's all-or-nothing, and "all" means rubber-stamped. Four 400-line PRs are four small batches, each of which gets real attention, fast pickup, and the option to be rejected or fixed in isolation. The author pays a little decomposition cost up front; the team avoids the much larger cost of an unreadable diff shipping an unreviewed flaw.

<TradeoffMatrix
  id="02-pr-size-and-latency-tradeoff-1"
  lessonSlug="02-pr-size-and-latency"
  lang="en"
  prompt="Your team's median PR pickup time is creeping toward a day, and post-merge defects are rising. The diffs are getting larger. What's the highest-leverage fix?"
  options={[
    { name: "Push for smaller PRs — decompose work into ~200–400 LOC changes, using stacked diffs for genuinely large work", summary: "Shrink the batch.", correct: true, justification: "PR size is the single variable that moves both metrics at once. Small diffs fit the five-minute gaps in a calendar (pickup drops) and fit working memory (detection rises to the 70–90% band). Stacking lets even a large change be reviewed as a chain of readable windows. You attack latency and quality together without asking anyone to rush." },
    { name: "Set a 24-hour review SLA so reviewers can't sit on PRs", summary: "Pressure the queue.", justification: "An SLA pressures the visible metric while harming the real one. It can't conjure a free 90-minute block for a 1,500-line diff; it just makes the reviewer rush and skim to beat the clock, trading the latency win for a detection loss. The constraint is service time, and a timer doesn't shrink it." },
    { name: "Add more reviewers per PR so big diffs get covered", summary: "Throw bodies at it.", justification: "Two reviewers on a 1,600-line diff still each face 1,600 lines past anyone's cognitive budget, so you get two skims instead of one and double the pickup wait. Reviewer count doesn't change the per-person attention limit; diff size does." },
    { name: "Buy an AI reviewer to pre-summarize large PRs for humans", summary: "Tooling over size.", justification: "A summary can help, but it doesn't change that the human still must reason about 1,600 lines of design to catch the real flaw, and the diff still queues for hours waiting for a block. Useful as a supplement, but it treats the symptom; the cause is batch size." },
  ]}
/>

<Quiz
  id="02-pr-size-and-latency-quiz-1"
  lessonSlug="02-pr-size-and-latency"
  lang="en"
  question="Why does a 1,600-line PR typically get LESS effective review than a 160-line one?"
  choices={[
    { label: "Attention is a fixed budget that doesn't scale with diff size — past a few hundred lines the reviewer skims and comments only on locally visible trivia", correct: true },
    { label: "Bigger PRs give the reviewer more context, so they review more carefully", misconception: "Context doesn't outrun the cognitive limit. Past ~400 LOC the reviewer can't hold the change in working memory, so detection drops — the data shows below-average defect density past ~450 LOC/hour in 87% of cases." },
    { label: "Reviewers always assign more people to big PRs, which dilutes responsibility", misconception: "Even if more reviewers are assigned, each still faces the same unreadable diff past their own budget. The problem is per-person attention, not how blame is shared." },
    { label: "Git surfaces fewer comments on large diffs", misconception: "The tool isn't limiting comments. The reviewer is skimming because the diff exceeds working memory, so the substantive design comments never get written in the first place." },
  ]}
/>

<Quiz
  id="02-pr-size-and-latency-quiz-2"
  lessonSlug="02-pr-size-and-latency"
  lang="en"
  question="Per Google's data, why do small changes get picked up in under an hour while large ones wait ~5 hours?"
  choices={[
    { label: "A small CL fits a five-minute gap between meetings; a large one needs a contiguous block that rarely appears on a busy calendar", correct: true },
    { label: "Large CLs are deprioritized by policy until small ones clear", misconception: "It's not a priority rule — it's queueing physics. The large CL's service time exceeds the free gaps in people's days, so it waits for a block that's scarce, not because anyone deprioritized it." },
    { label: "Reviewers are lazy about big changes", misconception: "It's a structural constraint, not motivation. Even a diligent reviewer can't read 1,500 lines in a 5-minute window, so the large CL physically can't be served until a long block opens up." },
    { label: "Small CLs skip review entirely", misconception: "Small CLs are reviewed — often more thoroughly. They're just cheap to context-switch into, so they're picked up fast rather than skipped." },
  ]}
/>

<DragOrder
  id="02-pr-size-and-latency-drag-1"
  lessonSlug="02-pr-size-and-latency"
  lang="en"
  prompt="Order how a too-large PR turns into a rubber stamp:"
  items={[
    { id: "a", label: "Author batches a feature + refactor + migration into one 1,600-line PR" },
    { id: "b", label: "No reviewer has a free 90-min block, so it queues for two days" },
    { id: "c", label: "Reviewer finally opens it; the diff exceeds working memory" },
    { id: "d", label: "They comment on locally visible trivia (names, a log line) and approve" },
    { id: "e", label: "The real design flaw ships unreviewed and surfaces weeks later" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Attention is fixed, not proportional to diff size, so big PRs get less real review and queue longer waiting for a block nobody has. Detection peaks at 200–400 LOC; latency is dominated by pickup, which scales with size. Small PRs — stacked when work is genuinely large — cut latency and raise detection at once.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-pr-size-and-latency-retrieval"
  lang="en"
  questions={[
    { q: "A teammate argues 'a bigger PR gives the reviewer more context, so it should catch more bugs.' Explain why the data says the opposite.", a: "Attention is a fixed budget, not a function of diff size. The SmartBear/Cisco study of 2,500 reviews over 3.2 million lines found the sweet spot is 200–400 LOC per review, yielding a 70–90% defect yield when spread over 60–90 minutes; past ~400 lines detection drops sharply, and past ~450–500 LOC/hour defect density was below average in 87% of cases, with concentration itself collapsing after 60–90 minutes of continuous reviewing. The mechanism is cognitive: a reviewer can hold a few hundred lines in working memory and reason about the design, but a 1,600-line diff exceeds that, so they skim and comment only on what's locally visible — a name, a log line — then approve. So a bigger PR gets less effective scrutiny, not more, which is exactly why real design flaws hide in giant diffs. More context doesn't help if the reader can't hold it; the fix is decomposition, not effort." },
    { q: "Why is 'review faster' usually the wrong instruction, and what variable should you change instead?", a: "Turnaround is dominated by pickup latency — the gap before anyone looks — and pickup is a queueing problem driven by task size. Google's data shows small CLs wait a median under an hour while very large ones wait around five, because a reviewer can find a five-minute slot for a small change between meetings but a 1,500-line diff needs a contiguous block that rarely appears on a busy calendar. You can't will that block into existence; pressuring reviewers to make one (an SLA, say) just makes them rush and skim, trading the latency win for a detection loss. The variable you actually control is service time: a 200-line PR fits the slots that already exist, so it's both picked up faster and reviewed more thoroughly. For genuinely large work, decompose it into stacked diffs — a chain of small dependent PRs — so the reviewer never faces more than a few hundred lines at once. Shrink the batch, not the clock." },
  ]}
/>

<Recap lang="en">
PR size is the master variable of code review because it sets latency and detection at the same time, and in the same direction. Detection peaks at 200–400 LOC reviewed over 60–90 minutes — a 70–90% yield — and falls off a cliff past ~400 lines and past ~450–500 LOC/hour, because a reviewer's attention is a fixed cognitive budget that doesn't scale with diff size; a 1,600-line PR therefore gets less real scrutiny than a 160-line one, and the rubber stamp (nits plus LGTM) is the predictable result. Latency tells the same story from the queue's side: pickup dominates turnaround and scales with size, so Google sees small CLs picked up in under an hour and very large ones waiting around five, because a small diff fits a five-minute gap while a big one needs a block nobody has. That's why "review faster" and SLAs backfire — they pressure the symptom and force skimming. The real lever is service time: keep PRs small, and decompose genuinely large work into stacked diffs so even a big change is reviewed as a sequence of readable windows. Small PRs cut latency and raise quality together; the only cost is the author's discipline to decompose.
</Recap>
