---
concepts:
- review-purpose
- pr-size-detection
- review-latency
- severity-and-action
- author-preparation
- scaling-review
estMin: 13
lang: en
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://google.github.io/eng-practices/review/reviewer/standard.html
- https://static0.smartbear.co/support/media/resources/cc/book/code-review-cisco-case-study.pdf
- https://google.github.io/eng-practices/review/developer/small-cls.html
status: ready
summary: "Free-recall prompts across the code-review unit — what review is for, why PR size sets latency and detection, how to make feedback triageable, and how to scale the queue. Answer first, then reveal."
title: 'Code review: free-recall review'
track: engineering-practice
unit: 03-code-review
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory before you open the model answer — the effort of pulling it back is what makes the unit's operating model stick.
</Hook>

<Goal lang="en">
Reconstruct the unit's spine — review's purpose, the PR-size lever, the latency story, severity-tagged actionable feedback, author preparation, and how review scales — without looking back at the lessons.
</Goal>

<RetrievalDrawer
  client:load
  id="03-code-review-short-retrieval"
  lang="en"
  questions={[
    { q: "What is the real dividing line between what a human reviewer should catch and what tooling should, and why is it not 'importance'?", a: "The line is decidability, not importance. A check belongs in an automated gate if a tool can evaluate it with a stable, context-free verdict — formatting, import order, naming, unused variables, lint rules, type errors, known-vulnerable dependencies, and a wide band of bug patterns. Importance is the wrong criterion because plenty of automated checks catch things more important than most human comments: a SAST scanner finding SQL injection, a type checker catching a null deref. What's left for the human is the undecidable residue, which is exactly what review is for — is this the right design, does it match the ticket's intent, will the next engineer understand it, are the error, money, and auth paths correct. Google's Standard puts design first because design compounds and is expensive to undo, while a brace costs nothing and a formatter fixes it free. Every minute a senior spends on a decidable check is stolen from the undecidable ones only they can do — and it's a recurring theft, the same nit on every PR until you automate it once." },
    { q: "Why does a 1,600-line PR get LESS effective review than a 160-line one, and what's the supporting data?", a: "Attention is a fixed cognitive budget that doesn't scale with diff size. The SmartBear/Cisco study of 2,500 reviews over 3.2 million lines found the sweet spot is 200–400 LOC per review, examined over 60–90 minutes, for a 70–90% defect yield; past ~400 lines detection drops sharply, and past ~450–500 LOC/hour defect density was below average in 87% of cases, with concentration itself collapsing after 60–90 minutes of continuous reviewing. The mechanism is cognitive: a reviewer can hold a few hundred lines in working memory and reason about the design, but a 1,600-line diff exceeds that, so they skim and comment only on what's locally visible — a name, a log line — then approve. That's the rubber stamp, and it's why real design flaws hide in giant diffs: they're the diffs nobody can fully read. More context doesn't help if the reader can't hold it; the fix is decomposition, not effort." },
    { q: "Why is 'review faster' usually the wrong instruction for latency, and what variable should you change instead?", a: "Turnaround is dominated by pickup latency — the gap before anyone looks — and pickup is a queueing problem driven by task size. Across ~8 million PRs, elite teams close the full cycle in under 26 hours while laggards let PRs sit a week before review even starts; Google's data shows small CLs picked up in a median under an hour while very large ones wait around five, because a reviewer can find a five-minute slot for a small change but a 1,500-line diff needs a contiguous block that rarely appears on a busy calendar. The waiting alone costs roughly 5.8 hours per developer per week, about $238K/year of frozen time for a ten-person team, which is why DORA treats lead time as a core metric. You can't will a free block into existence; pressuring reviewers (an SLA on completion) just makes them rush and skim, trading the latency win for a detection loss. The variable you control is service time — keep PRs small (stacked when genuinely large), so they fit the gaps that already exist and get both faster pickup and more thorough review." },
    { q: "What two things must every review comment make explicit, how do you encode them, and what does unlabeled nitpicking cost?", a: "Every comment must answer how important it is and what the author should do; leaving either implicit is the single biggest source of review friction, because the author then over-reacts (rewrites fine code) or under-reacts (ships the bug) and you eat a round-trip and some goodwill. Encode severity with a label — Conventional Comments uses nit:, suggestion:, issue:, question:, praise:, thought:, optionally (blocking)/(non-blocking), and Google matches it by prefixing genuine nits 'Nit:'. The label is a contract: a nit: is explicitly droppable, an issue: (blocking) must be resolved before merge, a question: wants an answer not a change. Encode the action by making the comment actionable and stating the why when it isn't obvious — 'extract validateOrder so the early-return path is obvious' beats 'this is confusing', and 'this allocates on every request, cache it' teaches where 'don't allocate here' only commands. Aim at the code, not the author. Excessive unlabeled nitpicking is estimated to cost 20–40% velocity and buries the one blocking comment under cosmetic ones." },
    { q: "What did SmartBear find about author preparation, and why does psychological safety matter for review?", a: "The study's most under-appreciated result is that author preparation — annotating your own diff with explanations before review — drove defect density to never above 30 and most commonly zero. The mechanism is that explaining your change forces you to re-think it, so you catch your own bugs in the act of justifying them; the review's value starts before the reviewer arrives. The related ego effect compounds it: knowing a peer will read the code makes you write it cleaner up front. Psychological safety is load-bearing on the other side because review is a social act — 'this whole approach is wrong' attacks the person, while 'this breaks under concurrent writes, here's the race' attacks the code and hands over a fact the author can engage with. When people fear a PR is a competence exam, they ship smaller, hide uncertainty, and stop asking for the early feedback that catches the biggest mistakes. So the highest-leverage habits are paired: reviewer-side, tag severity and make comments actionable and aimed at the code; author-side, annotate the diff and narrate the non-obvious decisions." },
    { q: "How do you scale code review across a growing org, and when do you change the shape of review rather than just speed it up?", a: "Scaling pressure pushes service time and queue length up while the human's attention budget stays fixed, so the answers all reduce what any one human must serve. Route with CODEOWNERS so PRs auto-assign to competent owners — but list a team, not a person, with round-robin or load balancing, or you build a bottleneck by construction (one staff engineer with 40 reviews a day skims and stamps). Send the routine 80% to any competent reviewer and reserve the bottleneck-prone expert for the architecturally significant and security-sensitive minority, pairing a junior with them there so review skill propagates instead of concentrating. Time-box pickup with an SLA on first response (Google's norm is one business day) plus a review-buddy rotation, and make load visible on a dashboard so the org rebalances before anyone drowns — but an SLA on completion of a big diff just forces a skim, so SLAs work on pickup, not quality. When stakes are high, change the shape: pair/mob programming is continuous review with zero async latency and far higher feedback bandwidth, best on the hard 20%; post-commit (ship-then-review) removes pickup latency from the merge path but is safe only on a high-trust base with a strong automated floor, for low-stakes changes, and is the wrong default for money or security paths. Measure outcomes (latency, escaped defects), never activity (comment count) — that's a Goodhart trap." },
  ]}
/>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the unit's spine: review exists for the undecidable, so automate the decidable into a blocking gate; PR size is the master variable that sets latency and detection together, and small or stacked PRs move both; latency is dominated by pickup, so shrink service time rather than pressure the clock; feedback works when it states severity and a fix and aims at the code; the author's own annotation finds defects no reviewer sees; and at scale you route to a team, time-box pickup not completion, balance load, and change the shape — pairing or post-commit on a tested base — measuring outcomes, never activity.
</Recap>
