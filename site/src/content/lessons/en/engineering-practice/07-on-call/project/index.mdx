---
concepts:
- slo-burn-rate-alerting
- alert-actionability-audit
- runbooks
- escalation
- on-call-metrics
estMin: 240
lang: en
level: senior
order: 10
prereqs: []
slug: project
sources:
- https://sre.google/workbook/alerting-on-slos/
- https://sre.google/sre-book/being-on-call/
- https://prometheus.io/docs/alerting/latest/configuration/
status: ready
summary: "Hands-on project — audit a noisy alert set, replace cause alerts with SLO burn-rate pages, write runbooks and escalation, and prove the rotation got quieter with before/after metrics."
title: 'On-call: rebuild a noisy rotation'
track: engineering-practice
unit: 07-on-call
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Reading that "every page must be actionable" is not the same as making a real rotation trustworthy. Take a noisy alert set, audit it, replace cause alerts with SLO burn-rate pages, wire runbooks and escalation, and prove the pager got quieter — with numbers, not vibes.
</Hook>

<Goal lang="en">
Turn the unit's one rule into an operating system for a rotation: classify every alert by actionability, page only on symptoms and burn rate, demote the rest, give each remaining page a runbook and a timed escalation path, and measure the change in page volume, % actionable, and MTTA/MTTR.
</Goal>

<ProjectBrief
  id="07-on-call-project"
  lessonSlug="07-on-call"
  lang="en"
  objective="Take a service with a noisy alert set (your own, or build a small HTTP service plus a Prometheus/Alertmanager stack with deliberately cause-based alerts) and rebuild its on-call so that every page is actionable, proving the rotation got quieter and faster with before/after metrics."
  requirements={[
    "Define at least one SLO for the service — e.g. 99.9% availability and p99 latency under 300ms over 30 days — and compute the resulting error budget in absolute failed-requests terms at your traffic level.",
    "Inventory the existing alert set and classify each alert as symptom or cause, actionable or not. Record the current page volume per shift, the false-positive rate (self-resolved or no-action pages), and current % actionable as the baseline.",
    "Replace cause-based pages (CPU, disk, connection-pool thresholds) with symptom/SLO alerts: implement a multi-window, multi-burn-rate rule (e.g. a fast 14.4× burn confirmed across 1h and 5m, plus a slower burn at a lower multiplier) in Prometheus or your alerting system.",
    "Demote every non-actionable alert off the pager to a ticket or dashboard, and use Alertmanager group_by, inhibit_rules, and group_wait to collapse storms so a related-alert flood becomes one notification.",
    "Write a runbook for every remaining page: the linked dashboard, an ordered triage checklist, the highest-probability mitigation first (mitigate before diagnose), and a verification step. An alert with no runbook does not page.",
    "Configure a timed escalation path — primary, then secondary, then service owner / incident commander on a timer — so an unacknowledged or stuck page never sits silent.",
    "Run a realistic incident drill (inject a deploy bug or dependency failure that burns the budget) and capture MTTA and MTTR end-to-end, including whether the runbook and escalation worked as written.",
  ]}
  acceptance={[
    "A before/after table: page volume per shift, % actionable, false-positive rate, MTTA, and MTTR — measured or drilled, not estimated.",
    "Every alert still on the pager is symptom/SLO-based and links a runbook; a written rationale lists each demoted or deleted alert and why it was not actionable.",
    "The burn-rate rule demonstrably pages on a real fast burn during the drill and stays silent for a brief flap that does not threaten the budget.",
    "The escalation path fires on the timer when the primary does not ack, and the drill MTTR comes in under the rotation's target (aim for under 1 hour, DORA-elite).",
  ]}
  stretch={[
    "Add a toil log for one rotation: record every manual, repetitive mitigation, then automate the top one (auto-rollback on burn, auto-scale on saturation) and show the page that used to require a human is now self-healing or demoted.",
    "Wire a follow-the-sun handoff between two time zones with a written handoff checklist, so no routine page lands at 3 a.m. local for anyone.",
    "Build an alert-quality CI gate: every new alert rule must declare a runbook link and a symptom/SLO justification, and the build fails if a rule is tagged severity: page without one.",
    "Instrument a weekly on-call review that auto-surfaces the lowest-% -actionable alerts from the page log and proposes the next deletion candidate, closing the postmortem-to-deletion feedback loop.",
  ]}
/>

<Recap lang="en">
This is the loop you will run on every real rotation: define the SLO and budget, audit every alert for actionability, page only on symptoms and burn rate, demote or delete the rest, give each surviving page a runbook and a timed escalation, then prove with before/after numbers that page volume fell, % actionable rose, and MTTA/MTTR held under target. Reduce toil by automating the repeated mitigations. Do it once on a real or toy stack and the production version becomes muscle memory — a pager people can still trust at 4 a.m.
</Recap>
