---
concepts:
- symptom-vs-cause-alerting
- alert-fatigue
- error-budget-burn-rate
- on-call-load-caps
- runbooks-escalation
- toil-reduction
estMin: 13
lang: en
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://sre.google/sre-book/being-on-call/
- https://sre.google/workbook/alerting-on-slos/
- https://sre.google/sre-book/monitoring-distributed-systems/
status: ready
summary: "Free-recall prompts across the on-call unit. Answer each in your own words first, then reveal the model answer and compare — symptom alerting, burn rate, fatigue, load caps, escalation, and toil."
title: 'On-call: free-recall review'
track: engineering-practice
unit: 07-on-call
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory before you open the model answer — the effort of recall is what makes the on-call discipline stick when you are actually holding the pager.
</Hook>

<Goal lang="en">
Reconstruct the unit's core mechanisms — why symptoms beat causes, what burn-rate alerting does, the anatomy of alert fatigue, the load caps, the page lifecycle, and how toil reduction keeps a rotation sustainable — without looking back.
</Goal>

<RetrievalDrawer
  client:load
  id="07-on-call-short-retrieval"
  lang="en"
  questions={[
    { q: "Why is symptom-based alerting higher-leverage than cause-based alerting, and what is the one exception?", a: "Symptoms describe what a user actually experiences and what your SLO promised — p99 latency over threshold, error rate burning the budget. Causes describe internal mechanics — CPU over 80%, disk over 80%, connection pool near full. Causes fire constantly and most are non-events: high CPU is frequently fine and self-corrects, so a cause alert pages on things that don't harm anyone while still missing real symptoms that don't move that particular gauge. Symptom alerts fire only when something real is breaking, which is exactly what makes them actionable. The four golden signals — latency, traffic, errors, saturation — catch nearly every meaningful failure as a symptom. The one exception: it is worth alerting on a cause that is definite and imminent, e.g. a disk that will be full within the hour, because that cause reliably becomes user harm soon." },
    { q: "Explain burn-rate alerting and why multi-window, multi-burn-rate beats a raw error-rate threshold.", a: "Burn rate is how fast you are consuming the error budget relative to your SLO — a 1× burn exhausts the monthly budget exactly at month end, a 6× burn exhausts it in days. A burn-rate alert pages when the budget is being spent fast enough to threaten the reliability promise, so urgency matches the actual threat rather than a fixed threshold. Multi-window, multi-burn-rate means you confirm a fast short-window burn against a longer window before paging: the short window gives quick detection, the long window suppresses false pages from a brief flap that doesn't actually endanger the budget. A raw error-rate threshold can't tell a transient spike from a sustained budget-killer, so it either pages on noise or detects real burns too late. Burn-rate alerting lets you act before the budget is gone, not after a full breach when the buffer is already spent." },
    { q: "Walk through the mechanism of alert fatigue and why you can't fix it by telling the responder to be more careful.", a: "The chain is: too many low-value pages arrive (industry false-positive rates run 60–80%, median ~42 pages/week) → the responder learns, correctly, that a given class self-resolves and that swiping it away costs nothing → they pattern-match and reflexively dismiss → the one night the alert is real, it looks identical to the noise and gets the same dismissal. It actively trains the human to ignore signal, and even when they do engage, the recognition tax adds 2–5 minutes of MTTA per page. The human cost compounds it: ~62% report weekly sleep disruption, ~41% have considered leaving over alert load, and attrition removes operational knowledge. You can't discipline your way out of a 60–80% false-positive rate at 3 a.m.; the only durable fix is structural — delete or demote any alert that isn't actionable so the pager is trustworthy again." },
    { q: "State Google SRE's on-call load caps and the reasoning that makes each cap a reliability lever, not just a humane one.", a: "SREs spend at most 50% of their time on operational work (on-call, tickets, manual toil), and of that no more than 25% on on-call itself; per shift the target is a maximum of two incidents. Handling one incident properly — triage, mitigation, root-cause, postmortem, follow-up fixes — is roughly six hours of real work, so two already fill a workday. The reasoning: a third incident means something gets cut, and what gets cut is almost always the postmortem and follow-up, the exact work that prevents the next page. The 50% ops ceiling reserves the other half for engineering that makes the system quieter next quarter. Ignore the caps and you get a doom loop — more ops work crowds out the engineering that reduces ops work, page volume climbs, responders burn out, attrition takes knowledge with it, and the survivors carry a heavier rotation. The caps keep on-call sustainable, which is a reliability property, not just a kindness." },
    { q: "Describe the lifecycle of a well-run page from definition to closure, and where each on-call mechanism plugs in.", a: "First, before the alert ever reaches the pager, you decide it is actionable and symptom/SLO-based — non-actionable alerts are demoted to tickets or dashboards. When it fires, Alertmanager grouping and inhibition collapse a storm of related alerts into one signal and group_wait lets flaps self-resolve, so the responder gets one notification, not a barrage. The responder acknowledges (this is the MTTA clock) and opens the runbook linked from the page, which turns a 3 a.m. cold start into a procedure. They mitigate to restore service (the MTTR clock); if they are stuck, escalation routes onward on a timer — primary → secondary → service owner / incident commander — so the page never sits silent. After service is restored they write the postmortem and delete or tune any alert that fired without being actionable. That last step is the feedback loop that pushes % actionable toward 100%." },
    { q: "What is toil, why does reducing it matter for on-call specifically, and how do the metrics MTTA, MTTR, page volume, and % actionable steer that reduction?", a: "Toil is manual, repetitive, automatable operational work that scales with service growth and produces no lasting value — restarting a stuck process by hand, clearing a queue, applying the same mitigation every week. It matters for on-call because toil is what consumes the operational budget; every hour spent on toil is an hour not spent making the system need less hand-holding, and unbounded toil is what drives a rotation into the doom loop. The metrics steer the reduction: rising MTTA is the early signature of fatigue or bad routing; MTTR (DORA 2024 elite is under one hour) tells you whether runbooks and tooling are good enough for the median responder; page volume per shift trending up means the rotation is degrading and the ≤2-incident target is at risk; and % actionable is the north star — every non-actionable page is a candidate for deletion or automation. Push % actionable toward 100% and automate the repeated mitigations, and MTTA, MTTR, page volume, and retention all improve together." },
  ]}
/>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the unit's spine: page on symptoms and SLO burn rate, not causes; burn-rate alerting matches urgency to real budget threat; alert fatigue is a structural failure cured by deletion, not discipline; the load caps (≈2 incidents/shift, ≤50% ops, ≤25% on-call) protect the engineering that keeps the rotation quiet; every page runs a defined lifecycle with a runbook and timed escalation; and toil reduction, steered by MTTA, MTTR, page volume, and % actionable, is what keeps it all sustainable.
</Recap>
