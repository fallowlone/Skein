---
concepts:
- test-pyramid
- end-to-end-testing
- integration-environment
- flaky-tests
- combinatorial-explosion
- service-boundary
deepensInto:
- 02-consumer-driven-contracts
estMin: 15
lang: en
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://martinfowler.com/articles/microservice-testing/
- https://www.infoq.com/news/2020/12/end-to-end-testing-microservices/
- https://pactflow.io/blog/contract-testing-vs-integration-testing/
- https://www.tweag.io/blog/2025-01-23-contract-testing/
spiral:
- feedback-loops
- blast-radius
status: ready
summary: 'Spinning every service up together works for two services and collapses
  for twenty: e2e is slow, flaky on any hop, and grows ~N² so teams mute it and catch
  nothing. The test pyramid breaks at the service boundary, where unit tests mock
  the very thing that breaks.'
title: The integration-testing dilemma
track: engineering-practice
unit: 02-contract-testing
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
At 02:14 the `orders` service starts throwing 500s. A provider team renamed a field from `total_cents` to `amount_cents` and shipped it — a clean, well-tested change, green on their own suite. Nothing in their CI knew that three downstream consumers read `total_cents`. The team had a "full integration environment," but it had been red for nine days and everyone had stopped looking at it. The first real signal anyone got was a pager, in production, on a Friday. The fix took ten minutes. *Finding out it had broken* cost an outage — because the only test that could have caught it was the one nobody trusted anymore.
</Hook>

<Crux>Booting every service together to test integration works for two services and collapses for twenty — e2e is slow, flaky, and grows ~N².</Crux>

<Explanation>

## The intuitive answer doesn't scale

Ask "will these two services still talk to each other?" and the obvious answer is: run them both, send a real request, check the response. That instinct is correct — for two services. The trouble is what happens as the system grows. With N services that call each other, the number of interaction pairs you'd want to exercise grows roughly with **N²**, and an end-to-end test of any one journey needs *every* hop in that journey to be healthy at the same moment. A request that fans out through `gateway → orders → pricing → inventory → payments` only passes its e2e test when all five services, plus their databases and queues, are simultaneously up, migrated, and seeded with the right data.

That "all healthy at once" requirement is the quiet killer. Each service has, say, a 98% chance of being green in the shared environment at any moment. String five together and the journey is green only ~90% of the time; string twelve together and you're under 80%. The environment isn't broken because of *your* change — it's broken because someone *else's* migration is half-applied, or a seed script failed, or a dependency is mid-deploy. You inherit all of that noise on every run.

## Slow, flaky, and therefore ignored

Those two forces — combinatorial setup and shared fragility — produce the three symptoms every team on a big e2e suite recognizes. **Slow:** booting a half-dozen services, a database, and a message broker before a single assertion runs pushes suites to 40+ minutes, so they move out of the inner loop and into a nightly job nobody watches. **Flaky:** because any hop can time out, a large fraction of failures — often cited around **1-in-5 runs** — have nothing to do with the change under test. **Ignored:** a test that cries wolf four times for every real bug gets muted, retried-until-green, or quarantined. A muted test catches nothing, which is strictly worse than no test, because it still costs you the runtime and gives false comfort.

This is why mature platforms invert the classic advice. Netflix and Spotify famously reshaped the test "pyramid" into a **honeycomb or diamond** at the service layer: a thin cap of true end-to-end journeys (the guidance is roughly **5-10%** of total tests), with the bulk of cross-service confidence pushed down into something faster and more isolated. The question this whole unit answers is: *what is that faster, more isolated thing?*

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Test layer</th>
        <th class="border border-border px-3 py-2 text-left">What it boots</th>
        <th class="border border-border px-3 py-2 text-left">Speed / determinism</th>
        <th class="border border-border px-3 py-2 text-left">Catches the 02:14 rename?</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Unit test</td>
        <td class="border border-border px-3 py-2">Nothing — the boundary is mocked</td>
        <td class="border border-border px-3 py-2">Milliseconds, deterministic</td>
        <td class="border border-border px-3 py-2">No — the mock returns the old shape</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">End-to-end</td>
        <td class="border border-border px-3 py-2">All services + DB + queue together</td>
        <td class="border border-border px-3 py-2">Minutes, flaky on any hop</td>
        <td class="border border-border px-3 py-2">Yes — if the env is green, which it isn't</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">The missing layer</td>
        <td class="border border-border px-3 py-2">One side, against a recorded agreement</td>
        <td class="border border-border px-3 py-2">Seconds, deterministic</td>
        <td class="border border-border px-3 py-2">Yes — and at the author's desk</td>
      </tr>
    </tbody>
  </table>
</div>

## The boundary is exactly where the pyramid has a hole

Here is the subtle part. The classic test pyramid says "lots of unit tests, fewer integration tests, very few e2e." Inside a monolith that works, because a unit test exercises real in-process calls between modules. Across a network boundary it springs a leak: a unit test of the `orders` consumer **mocks** the `pricing` provider. The mock returns whatever shape the consumer's author *believed* `pricing` returns. The day `pricing` renames a field, the consumer's unit tests stay green — they're asserting against the author's stale belief, not against reality. The thing most likely to break (the wire contract between services) is the one thing unit tests deliberately stub out.

So you're squeezed from both sides. Unit tests are fast and reliable but blind to the boundary by construction. End-to-end tests can see the boundary but are too slow and too flaky to gate every deploy. The renamed-field outage falls straight through the gap. What's needed is a test that checks the boundary specifically — the shape and semantics of the requests and responses two services exchange — **without** booting both services together. That is the shape of the problem the rest of this unit solves. When you spot a cross-service failure that unit tests missed and e2e was too broken to catch, you're already staring at this gap.

<Inset kind="why" lang="en">
"Just keep the integration environment green" sounds like a discipline problem, but it's a structural one. A shared environment's uptime is the *product* of every service's individual uptime, so it degrades multiplicatively as you add services — and its health is owned by everyone, which means it's owned by no one. Telling teams to try harder doesn't change the math. The only durable fix is to stop requiring all services to be simultaneously healthy to learn whether two of them agree.
</Inset>

<TradeoffMatrix
  id="02-contract-testing-tradeoff-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="A platform has 18 services with dense HTTP dependencies. The e2e suite takes 45 minutes and fails ~20% of runs for reasons unrelated to the change. The team wants reliable cross-service compatibility feedback. What's the most sound direction?"
  options={[
    { name: "Find a per-edge test that checks the boundary without booting both services, and keep only a thin e2e smoke for critical journeys", summary: "Push compatibility down a layer.", correct: true, justification: "This attacks the root cause: the combinatorial requirement that all services be healthy at once. A per-edge test scoped to two services scales linearly and runs deterministically in seconds, so it can gate every deploy; the thin e2e cap (5-10%) still covers real journeys and timing the per-edge tests can't see. You keep the boundary coverage and drop the flake." },
    { name: "Invest in the e2e suite: more parallelism, automatic retries, and a bigger shared environment", summary: "Scale up e2e.", justification: "Retries hide flake instead of fixing it, and parallelism doesn't change the N² shape — you still need one shared environment healthy all at once. You spend money making a slow, brittle layer marginally faster while it keeps failing for reasons unrelated to the change. This is the trap teams fall into right before they look for something better." },
    { name: "Lean harder on unit tests with good mocks of every dependency, and drop integration testing", summary: "Unit + mocks only.", justification: "Unit tests mock the boundary, so by construction they assert against the author's belief about a dependency, not its real shape. A provider renaming a field leaves every consumer's unit tests green — that's exactly the 02:14 outage. Unit tests are necessary but structurally blind to the wire contract." },
    { name: "Accept that integration bugs reach prod and invest in fast detection and rollback instead", summary: "Observe in prod.", justification: "Detection-and-rollback is a real layer of defense, but as the *only* gate for compatibility it makes the customer your test and an outage your feedback. The whole point is that a field rename is cheaply catchable before deploy; choosing to catch it after is choosing the most expensive place to learn." },
  ]}
/>

<Quiz
  id="02-contract-testing-quiz-1"
  lessonSlug="01-overview"
  lang="en"
  question="Why does an end-to-end suite's reliability degrade as you add more services to a journey?"
  choices={[
    { label: "The journey passes only when every hop is simultaneously healthy, so its success rate is the product of each service's uptime and falls multiplicatively as services are added", correct: true },
    { label: "Each added service makes the test code longer and more error-prone to write", misconception: "Test length isn't the driver. The fragility comes from the runtime requirement that all services be green at once — the math of multiplying uptimes — not from how much test code you wrote." },
    { label: "Newer services are inherently buggier than older ones", misconception: "Service age isn't the point. Even if every service were individually rock-solid at 98% green, stringing twelve together still drops the journey below 80% because the probabilities multiply." },
    { label: "The CI server runs out of memory with more services", misconception: "Resource limits are a separate, fixable concern. The structural problem is that a shared environment's health is the product of all its parts, so it degrades as you add parts regardless of hardware." },
  ]}
/>

<Quiz
  id="02-contract-testing-quiz-2"
  lessonSlug="01-overview"
  lang="en"
  question="Why do unit tests fail to catch a provider renaming a field that a consumer reads?"
  choices={[
    { label: "Unit tests mock the boundary, so they assert against the author's belief about the provider's shape — a mock keeps returning the old field even after the real provider changes", correct: true },
    { label: "Unit tests don't run often enough to notice the change", misconception: "Frequency isn't the issue — they could run on every commit and still pass. The mock encodes a stale assumption about the provider, so it returns the old shape no matter when it runs." },
    { label: "Unit tests only check private methods, not API responses", misconception: "Unit tests routinely cover code that handles API responses. The gap is that they handle a *mocked* response, which by design reflects the author's belief, not the provider's real, possibly-changed output." },
    { label: "The rename is a textual change git would have flagged", misconception: "git has no idea another service reads that field; it sees a normal edit. The breakage is a cross-service semantic mismatch that no source-control diff in the provider's repo can detect." },
  ]}
/>

<DragOrder
  id="02-contract-testing-drag-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="Order how an e2e-only strategy decays into the 02:14 outage:"
  items={[
    { id: "a", label: "Two services integrate; a shared e2e environment is stood up to test them" },
    { id: "b", label: "More services join; the journey needs all of them healthy at once" },
    { id: "c", label: "Suite slows past 40 minutes and flakes ~1-in-5 for unrelated reasons" },
    { id: "d", label: "The team mutes or stops watching the env; it sits red for days" },
    { id: "e", label: "A provider renames a field; the only test that would catch it is muted; prod pages at 02:14" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="Where a field rename falls through the testing gap"
  caption="Unit tests mock the boundary so they stay green; e2e sees it but gets muted. The rename falls through the gap between both layers."
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "unit", label: "Unit tests", sub: "mock boundary" },
    { id: "gap", label: "The gap", sub: "rename falls here" },
    { id: "e2e", label: "E2e tests", sub: "flaky, muted" },
    { id: "rename", label: "Field rename", sub: "provider ships it" },
    { id: "prod", label: "Prod outage", sub: "02:14 pager" },
    { id: "contract", label: "Contract layer", sub: "checks boundary" },
  ]}
  edges={[
    { from: "unit", to: "gap", label: "blind to wire" },
    { from: "e2e", to: "gap", label: "ignored" },
    { from: "rename", to: "gap", label: "slips through" },
    { from: "gap", to: "prod", label: "reaches prod" },
    { from: "contract", to: "gap", label: "fills the gap" },
  ]}
/>

<KeyTakeaway>End-to-end integration testing collapses at scale: it grows ~N², needs every hop healthy at once, and flakes until it's muted. Unit tests are fast but mock the boundary, so they're blind to the wire contract. The renamed field falls through the gap — the missing layer must check two services' agreement without booting both.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-contract-testing-retrieval"
  lang="en"
  questions={[
    { q: "A colleague says 'our integration environment just needs more discipline to stay green.' Explain why that's a structural problem, not a discipline one.", a: "A shared end-to-end environment is healthy only when every service in a journey is simultaneously up, migrated, and seeded — so its uptime is the *product* of each service's individual uptime. If each of twelve services is green 98% of the time, the whole journey is green only about 78% of the time, and that gets worse as you add services. The degradation is multiplicative and built into the architecture; no amount of trying harder changes the math. On top of that, the environment's health is owned by everyone, which in practice means no one, so a half-applied migration or a failed seed from another team breaks your run. That combination is why big e2e suites slow past 40 minutes, flake on roughly 1-in-5 runs for reasons unrelated to the change, and eventually get muted — and a muted test catches nothing while still costing runtime and giving false comfort. The durable fix isn't discipline; it's to stop requiring all services to be healthy at once just to learn whether two of them still agree." },
    { q: "Where exactly does the test pyramid 'break' at a service boundary, and why does that let a field rename reach production?", a: "Inside a monolith a unit test exercises real in-process calls, so the pyramid's 'lots of fast unit tests' advice works. Across a network boundary it springs a leak: a unit test of the consumer mocks the provider, and the mock returns whatever shape the consumer's author believed the provider returns. That belief is frozen at the time the mock was written. When the provider renames total_cents to amount_cents, the consumer's unit tests stay green because they're asserting against the stale mock, not against the real provider — the one thing most likely to break, the wire contract, is the one thing unit tests deliberately stub out. End-to-end tests *can* see the boundary but are too slow and flaky to gate every deploy, so they get muted. The rename therefore falls straight through the gap between a fast-but-blind layer and a sees-it-but-untrusted layer, and the first signal is a production pager. The missing layer is one that checks the boundary specifically — the shape and semantics of requests and responses between two services — without booting both together." },
  ]}
/>

<Recap lang="en">
The instinct to test integration by running every service together is right for two services and wrong for twenty. End-to-end suites grow combinatorially: with N interacting services the pairs to exercise scale with N², and any one journey passes only when every hop is healthy at the same instant, so a shared environment's reliability is the product of its parts and degrades multiplicatively as you add services. The result is the familiar trio — slow (40+ minute boots), flaky (~1-in-5 failures unrelated to the change), and therefore muted, and a muted test catches nothing while still costing runtime. The classic pyramid doesn't save you, because at a network boundary unit tests mock the provider and assert against the author's stale belief about its shape, leaving them green the day the provider renames a field. So you're squeezed between a fast-but-blind layer and a sees-it-but-untrusted layer, and a cross-service rename falls through the gap into a 2 a.m. pager. What's needed is a layer that checks the boundary itself — the shape and semantics two services exchange — without requiring both to be booted together. That layer is contract testing, and building it up is what the rest of this unit does. Now when you inherit a muted integration environment and a colleague asks "why can't we just fix it?", you'll know it's a structural math problem, not a discipline one — and you'll know what to reach for instead.
</Recap>
