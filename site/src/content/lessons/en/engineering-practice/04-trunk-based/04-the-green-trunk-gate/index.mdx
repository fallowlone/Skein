---
slug: 04-the-green-trunk-gate
lang: en
track: engineering-practice
unit: 04-trunk-based
order: 4
level: senior
lessonType: topic
status: ready
title: "The green-trunk gate is the whole bargain"
summary: "A shared trunk means one break halts everyone, so trunk-based needs a fast, blocking CI gate. At scale a naive pre-merge gate races itself, so teams adopt merge queues that test against the projected trunk — but one FIFO queue bottlenecks past ~20-30 devs."
prereqs: ["03-feature-flags-dark-launch"]
deepensInto: ["05-flag-debt-and-rollout-discipline"]
spiral: ["blast-radius", "feedback-loops"]
concepts: ["ci-gate", "green-trunk", "merge-queue", "batch-testing", "flaky-tests", "build-speed", "git-bisect"]
estMin: 17
sources:
  - https://dora.dev/capabilities/continuous-integration/
  - https://github.blog/engineering/engineering-principles/how-github-uses-merge-queue-to-ship-hundreds-of-changes-every-day/
  - https://graphite.com/guides/merge-queue-comparison-github-gitlab-graphite
  - https://martinfowler.com/articles/continuousIntegration.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
A 40-engineer team goes trunk-based and within a month declares it a failure: trunk is red half the day, and when it's red nobody can pull a clean base or merge on green, so the whole team stalls. The diagnosis isn't "trunk-based doesn't scale" — it's that they adopted the *risk* (everyone on one branch) without the *mitigation* (a gate that keeps that branch green). Trunk-based without a fast, blocking CI gate isn't trunk-based; it's a shared branch with no brakes.
</Hook>

<Crux>One branch means a broken trunk halts the team, so trunk-based needs a fast, blocking CI gate — no green, no merge.</Crux>

<Explanation>

## The shared trunk concentrates risk

Trunk-based development's strength — everyone integrating into one branch — is also its single point of failure. With long-lived branches, one person's broken branch is one person's problem. With a shared trunk, a broken trunk is *everyone's* problem: nobody can pull a clean base to start work, nobody can merge because their PR builds on red, and a "quick fix" lands on top of breakage that makes the failure harder to diagnose. The practice deliberately trades isolated, deferred pain for shared, immediate risk — and that trade only pays off if the trunk is kept green.

So the gate is not an add-on; it's the other half of the bargain. The rule is **no green, no merge**: every change runs the full build and test suite *before* it lands on trunk, and a red result blocks the merge. This is why trunk-based and continuous integration are the same practice described from two angles — "merge to trunk daily" is only safe because "an automated gate keeps trunk always releasable" holds at the same time. DORA's data shows the strength of trunk-based runs *through* CI; adopt the branching model without the gate and you get the red-trunk failure mode instead of the benefit.

## The gate must be fast, or people route around it

A gate's effectiveness decays with its latency. If the build takes 45 minutes, developers start batching changes to amortize the wait — which lengthens branches, which reabsorbs the drift from lesson 1. They cut corners, skip running tests locally, or pressure to merge red "just this once." A slow gate quietly destroys the very behavior it exists to enforce. The target is minutes: a build fast enough that integrating small and often is the path of least resistance. Speed comes from test parallelization, tiering (fast unit tests block the merge; slow end-to-end suites run post-merge or nightly), caching, and ruthlessly cutting the suite's slowest offenders.

When trunk does go red despite the gate — a flaky test slipped through, an environment-only failure — the senior reflex is **stop the line**: fixing or reverting the trunk takes priority over new feature work, because every minute it's red taxes the whole team. `git bisect` makes finding the offending commit cheap by binary-searching the history, and a small, frequently-integrated history is exactly what makes bisect fast and the revert clean.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Gate design</th>
        <th class="border border-border px-3 py-2 text-left">When tests run</th>
        <th class="border border-border px-3 py-2 text-left">Can trunk break?</th>
        <th class="border border-border px-3 py-2 text-left">Scaling limit</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Post-merge CI only</td>
        <td class="border border-border px-3 py-2">After landing on trunk</td>
        <td class="border border-border px-3 py-2">Yes — detected late</td>
        <td class="border border-border px-3 py-2">Breaks constantly past a few devs</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Pre-merge (test the PR branch)</td>
        <td class="border border-border px-3 py-2">Before merge, vs stale base</td>
        <td class="border border-border px-3 py-2">Yes — two green PRs can conflict semantically</td>
        <td class="border border-border px-3 py-2">Races itself at high merge rate</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Merge queue (test vs projected trunk)</td>
        <td class="border border-border px-3 py-2">Against trunk + PRs ahead in queue</td>
        <td class="border border-border px-3 py-2">No — only green-against-final-state lands</td>
        <td class="border border-border px-3 py-2">FIFO bottleneck ~20-30 devs; flaky tests stall it</td>
      </tr>
    </tbody>
  </table>
</div>

## At scale the gate races itself — enter merge queues

A naive pre-merge gate has a subtle hole. You test PR-A against trunk: green. I test PR-B against the *same* trunk: green. We both merge. But A and B were never tested *together* — they can conflict semantically and break trunk even though each passed. At a few merges a day this is rare; at dozens it's constant. The fix is a **merge queue**: instead of merging directly, PRs enter a queue and CI tests each against the *projected* state of trunk — trunk plus all PRs ahead of it in line. A PR only lands if it's green against the exact state it will create. To keep throughput up, queues **batch**: test several queued PRs together as one combined candidate and merge the whole batch if green, falling back to bisecting the batch if it fails.

This is how large teams keep a single trunk green at high volume — GitHub's own merge queue scaled the monorepo from about a thousand merges a month in 2016 to over thirty thousand by 2023, with hundreds of engineers merging thousands of PRs a month and roughly a third lower average deploy time. But the queue has limits a senior must plan for. A single FIFO queue becomes a bottleneck past roughly **20-30 active developers**: unrelated changes compete for one lane, and slow CI stalls everything behind it. Worse, a single **flaky test** is catastrophic in a queue — at 50+ PRs a day it can stall the line for hours, because a nondeterministic failure rejects batches that were actually fine. This is why flake quarantine and queue parallelism (independent lanes for independent code) become first-order infrastructure concerns at scale, not nice-to-haves.

<Inset kind="why" lang="en">
Notice the gate's quality requirement is symmetrical with the flag discipline of the last lesson. A flaky test in a merge queue is the gate's version of a stale flag: a piece of the system that's supposed to give a clear signal but instead emits noise, and the noise compounds at scale until it halts everyone. Trunk-based at scale is less about the branching model and more about keeping every signal in the system — the gate, the queue, the flags — trustworthy. A signal you can't trust is worse than no signal, because people route around it.
</Inset>

<TradeoffMatrix
  id="04-green-trunk-gate-tradeoff-1"
  lessonSlug="04-the-green-trunk-gate"
  lang="en"
  prompt="At 50 PRs/day on one trunk, trunk breaks a few times a week even though every PR was green before merge. What's the right fix?"
  options={[
    { name: "Adopt a merge queue that tests each PR against the projected trunk (trunk + PRs ahead), with batching for throughput", correct: true, justification: "Two PRs each green against the same stale base can still break trunk when combined — a naive pre-merge gate races itself at this volume. A merge queue tests against the exact state each PR will create, so only changes that are green against the final state land. Batching keeps throughput high; bisecting a failed batch isolates the culprit." },
    { name: "Make CI stricter and require two approvals per PR", summary: "More review, same race.", justification: "Reviews and stricter checks don't address the race: each PR is still validated against a stale base, so two independently-green PRs can still combine to break trunk. The problem is what trunk state you test against, not how carefully you review." },
    { name: "Move to post-merge CI and revert fast when trunk breaks", summary: "Detect-and-revert.", justification: "Post-merge testing detects breakage only after it's already blocking the team, and at 50 PRs/day the revert churn is constant. The point of the gate is to keep trunk green, not to get good at cleaning it up after it's red." },
    { name: "Split into long-lived team branches that integrate weekly", summary: "Re-introduce drift.", justification: "This abandons trunk-based to dodge a solvable infrastructure problem, trading the queue's bottleneck for weekly big-bang merges and reabsorbed drift. The right move is to scale the gate (queue + parallel lanes + flake control), not the branches." },
  ]}
/>

<Quiz
  id="04-green-trunk-gate-quiz-1"
  lessonSlug="04-the-green-trunk-gate"
  lang="en"
  question="Why is trunk-based development inseparable from a fast, blocking CI gate?"
  choices={[
    { label: "Everyone shares one branch, so a broken trunk blocks the whole team from a clean base and from merging — the gate is the mitigation for that concentrated risk", correct: true },
    { label: "Because CI servers are required to create branches", misconception: "CI has nothing to do with creating branches. The point is that a shared trunk concentrates risk — one break stops everyone — so a gate that keeps trunk green is the necessary other half of the bargain." },
    { label: "Because the gate replaces the need for code review", misconception: "The gate and review are separate; the gate runs tests, review checks design and intent. The gate is required because a shared trunk makes a single break everyone's problem, not because it substitutes for review." },
    { label: "Because trunk-based forbids running tests locally", misconception: "Trunk-based encourages local testing; the gate is the authoritative check before landing. It's required because of concentrated risk on the shared branch, not any prohibition on local tests." },
  ]}
/>

<Quiz
  id="04-green-trunk-gate-quiz-2"
  lessonSlug="04-the-green-trunk-gate"
  lang="en"
  question="Two PRs each pass CI against the current trunk, then both merge — and trunk breaks. What happened, and what prevents it?"
  choices={[
    { label: "They were tested against the same base but never together; a merge queue testing each PR against the projected trunk (trunk + PRs ahead) prevents it", correct: true },
    { label: "One developer skipped the tests; stricter enforcement prevents it", misconception: "Both PRs genuinely passed — the failure is that neither was tested against the other's changes. Enforcement doesn't help; you need to test against the projected combined state, which is what a merge queue does." },
    { label: "The CI cache was stale; clearing it prevents it", misconception: "It's not a caching issue. Each PR was correctly green against the base it saw; the gap is that the combined state was never validated. A merge queue closes that gap." },
    { label: "Git merged them in the wrong order; rebasing prevents it", misconception: "Order isn't the root cause — even correctly ordered, two PRs validated only against a stale base can conflict semantically. The fix is testing against the projected trunk, not reordering." },
  ]}
/>

<DragOrder
  id="04-green-trunk-gate-drag-1"
  lessonSlug="04-the-green-trunk-gate"
  lang="en"
  prompt="Order how a merge queue lands a change safely at scale:"
  items={[
    { id: "a", label: "PR passes its own CI and is approved" },
    { id: "b", label: "PR enters the merge queue rather than merging directly" },
    { id: "c", label: "CI tests it (often batched) against trunk + the PRs ahead in line" },
    { id: "d", label: "Green against the projected final state → the batch merges to trunk" },
    { id: "e", label: "On failure, bisect the batch to eject the offending PR and re-run" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>A shared trunk concentrates risk — one break halts everyone — so trunk-based is inseparable from a fast, blocking CI gate: no green, no merge. The gate must be fast (minutes) or people batch and reabsorb drift. At scale a naive pre-merge gate races itself (two PRs green against the same base can still break trunk together), so merge queues test against the projected trunk and batch for throughput — but a single FIFO queue bottlenecks past ~20-30 devs and one flaky test can stall it for hours.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-green-trunk-gate-retrieval"
  lang="en"
  questions={[
    { q: "Why does a team that 'went trunk-based' end up with a permanently red trunk, and what's the missing piece?", a: "They adopted the risk without the mitigation. Trunk-based concentrates everyone on one branch, which is its strength for integration but also a single point of failure: when trunk is red, nobody can pull a clean base or merge on green, so one break halts the entire team — and a 'quick fix' on top of breakage makes diagnosis worse. The missing piece is a fast, blocking CI gate: every change runs the full build and test suite before it lands, and red blocks the merge — no green, no merge. That's why trunk-based and continuous integration are the same practice from two angles: 'merge to trunk daily' is only safe because 'an automated gate keeps trunk always releasable' holds simultaneously. The gate must also be fast, minutes not tens of minutes, or developers batch changes to amortize the wait and reabsorb the drift the practice exists to avoid. Adopt the branching model without the gate and you inherit the red-trunk failure mode instead of the elite-delivery benefit." },
    { q: "Explain why a naive pre-merge gate races itself at scale and how merge queues fix it, including the limits of queues.", a: "A pre-merge gate tests each PR against the current trunk. PR-A is green against trunk; PR-B is green against the same trunk; both merge — but they were never tested together, so they can conflict semantically and break trunk even though each passed. At a few merges a day this is rare; at dozens it's constant. A merge queue fixes the race: PRs enter a queue and CI tests each against the projected state of trunk — trunk plus all PRs ahead in line — so a change only lands if it's green against the exact state it will create. To keep throughput up, queues batch several PRs as one combined candidate and merge the batch if green, bisecting the batch on failure to eject the culprit. This is how GitHub kept one monorepo green from ~1,000 merges/month in 2016 to 30,000+ by 2023. But queues have limits: a single FIFO queue bottlenecks past roughly 20-30 developers because unrelated changes compete for one lane and slow CI stalls everything behind it, and a single flaky test is catastrophic — at 50+ PRs/day it can stall the line for hours by rejecting batches that were actually fine. So at scale, flake quarantine and parallel independent lanes become first-order infrastructure, not nice-to-haves." },
  ]}
/>

<Recap lang="en">
The shared trunk is trunk-based development's strength and its single point of failure: when everyone integrates into one branch, a broken trunk blocks the whole team from a clean base and from merging, so the practice is inseparable from a fast, blocking CI gate — no green, no merge — that keeps trunk always releasable. The gate must run in minutes, because a slow gate pushes developers to batch changes and reabsorb the drift trunk-based exists to remove, and when trunk does go red the senior reflex is to stop the line and fix or revert before new work, using git bisect on the small, frequent history to find the culprit fast. At scale a naive pre-merge gate races itself — two PRs each green against the same base can still break trunk when combined — so merge queues test each PR against the projected trunk and batch for throughput, the technique that let GitHub keep one monorepo green from a thousand to thirty thousand merges a month. But queues are not free: a single FIFO queue bottlenecks past about 20-30 developers, and one flaky test can stall it for hours, making flake control and parallel lanes core infrastructure. The through-line: trunk-based at scale is the discipline of keeping every signal — gate, queue, flags — trustworthy, because a signal people can't trust gets routed around.
</Recap>
