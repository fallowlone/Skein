---
concepts:
- integration-frequency
- merge-conflict-cost
- branch-drift
- semantic-conflicts
- continuous-integration
- batch-size
deepensInto:
- 02-short-lived-branches-vs-gitflow
estMin: 15
lang: en
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-integration-frequency
sources:
- https://dora.dev/capabilities/trunk-based-development/
- https://www.atlassian.com/continuous-delivery/continuous-integration/trunk-based-development
- https://martinfowler.com/articles/branching-patterns.html
- https://trunkbaseddevelopment.com/
spiral:
- feedback-loops
- cost-of-delay
status: ready
summary: Merge cost is set by how far trunk drifted since you forked, and drift compounds
  as everyone keeps committing. Integrating daily doesn''''t avoid conflicts — it
  keeps each one small enough to resolve in minutes with full context, before it becomes
  a big-bang nobody can review.
title: Integration frequency is the lever
track: engineering-practice
unit: 04-trunk-based
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Two engineers fork the same `OrderService` on Monday. Alice ships a one-line fix Monday afternoon: her branch reconciles half a day of other people's commits, merges in thirty seconds, done. Bob keeps his "checkout rewrite" on a branch for three weeks. On merge day, trunk has moved 600 commits under him — the validation helper he calls was renamed, the money type changed from cents-as-int to a `Money` class, a migration reshaped the table. His feature was finished in week two. The other week and a half is the integration tax, and he pays all of it at once.
</Hook>

<Crux>Merge cost grows with how far trunk drifts while your branch lives, and drift compounds — so integrate daily to keep each conflict small.</Crux>

<Explanation>

## Merge cost is a function of drift, not diff size

The intuitive model — "a big feature is hard to merge because it's big" — is wrong in a way that matters. The cost of merging a branch is set by **divergence**: how far trunk has moved away from the point where you forked. A 2,000-line feature merged the same hour you started has near-zero merge cost, because trunk hasn't moved. A 50-line fix merged after three weeks can be agony, because the 50 lines now sit on top of a foundation that was rebuilt underneath them.

Divergence compounds because integration is a *shared* system. While your branch is alive, every other engineer is also merging to trunk. A branch that lives one day reconciles one day of everyone else's work. A branch that lives three weeks reconciles three weeks of it — and worse, you've lost the mental context for code that changed while you weren't looking. The cost curve bends upward with branch age, not linearly. This is why Microsoft's internal study found teams that went more than three days without integrating spent roughly **12× longer** resolving conflicts than teams merging daily, and why merge-conflict work consumes an estimated **10–25%** of developer time in branch-heavy shops.

## Textual conflicts are the cheap ones

Git's merge will flag a *textual* conflict when two branches edit the same lines — those are loud, and the tool stops you. The expensive failures are **semantic conflicts**: both sides apply cleanly, git is happy, and the code is now wrong. Alice renames `validate(order)` to `validateOrder(order)` and updates all 40 call sites on trunk. Bob's branch added a 41st call site to the old name. Git merges both with zero conflict markers; the build breaks, or worse, it compiles because of an overload and silently does the wrong thing.

Semantic conflicts scale with the *same* drift variable. The more trunk changed while you were away, the more invisible assumptions your branch is built on have quietly become false. Frequent integration is the only cheap defense: a one-day-old branch can only have inherited one day of broken assumptions, and your CI gate catches them while the change that caused them is still fresh in someone's memory.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Branch age at merge</th>
        <th class="border border-border px-3 py-2 text-left">Trunk drift to reconcile</th>
        <th class="border border-border px-3 py-2 text-left">Conflict character</th>
        <th class="border border-border px-3 py-2 text-left">Who still has context</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Hours</td>
        <td class="border border-border px-3 py-2">A few commits</td>
        <td class="border border-border px-3 py-2">Tiny, mostly textual</td>
        <td class="border border-border px-3 py-2">Everyone — it just happened</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">3+ days</td>
        <td class="border border-border px-3 py-2">Dozens of commits</td>
        <td class="border border-border px-3 py-2">~12× resolution time</td>
        <td class="border border-border px-3 py-2">Fading</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Weeks</td>
        <td class="border border-border px-3 py-2">Hundreds of commits</td>
        <td class="border border-border px-3 py-2">Semantic + textual, big-bang</td>
        <td class="border border-border px-3 py-2">Nobody — code changed under you</td>
      </tr>
    </tbody>
  </table>
</div>

## Frequency is the lever; tooling is a rounding error

Teams in merge pain reach for a better merge tool first. It's the wrong knob. A smarter three-way diff lowers the cost of resolving a *given* conflict, but it cannot lower the conflict's *size* — that's fixed by how much trunk drifted, which is fixed by how long you waited. Three weeks of divergence is three weeks of divergence whether you resolve it in vim or in a fancy GUI. The lever that actually moves the cost is the one that controls drift: **integrate more often.**

This reframes "continuous integration" away from "we have a CI server." CI is a *practice* — every developer merging small changes to the shared trunk at least daily — and the server is just the gate that makes the practice safe. The batch-size principle from lean shows up here exactly: small batches expose problems early and cheaply; large batches hide them until they detonate together. A daily merge is a small batch. A three-week branch is one enormous batch you can't unship.

<Inset kind="why" lang="en">
"Integrate daily" sounds like a productivity nag, but it's really a statement about feedback latency. Every hour your branch diverges from trunk is an hour of decisions you're making on stale information — calling functions that no longer exist, assuming invariants that were just broken. Shrinking the branch lifetime shrinks the window in which you can be wrong without knowing it. That's the whole game.
</Inset>

<TradeoffMatrix
  id="01-integration-frequency-tradeoff-1"
  lessonSlug="01-integration-frequency"
  lang="en"
  prompt="Your team's merges are consistently painful. A three-week branch just took two engineers a full day to land. What's the highest-leverage change?"
  options={[
    { name: "Shrink branch lifetime — break the work into changes that integrate to trunk at least daily", summary: "Attack drift at the source.", correct: true, justification: "Merge cost is a function of how far trunk drifts while a branch lives, and drift compounds because everyone keeps committing. The only lever that lowers the cost rather than the per-conflict effort is reducing the lifetime — integrate daily and each conflict is one day's worth of drift, small and resolvable with full context." },
    { name: "Adopt a better merge/diff tool with smarter conflict resolution", summary: "Tooling over frequency.", justification: "A better tool reduces the effort to resolve a given conflict but not its size, which is set by drift. Three weeks of divergence stays three weeks regardless of the tool — this treats the symptom, not the cause." },
    { name: "Add a senior reviewer who's good at untangling big merges", summary: "Heroics over frequency.", justification: "Routing all painful merges through one expert creates a bottleneck and a bus factor, and still doesn't shrink the diffs. The pain recurs every branch; you've hidden it behind a person instead of removing it." },
    { name: "Schedule a weekly 'integration day' where everyone merges together", summary: "Batching the batches.", justification: "A weekly sync is still a one-week batch — you've just synchronized the big-bang. It reduces surprise timing but not drift; daily integration means each branch only ever reconciles a day of work." },
  ]}
/>

<Quiz
  id="01-integration-frequency-quiz-1"
  lessonSlug="01-integration-frequency"
  lang="en"
  question="Why can a tiny 50-line change be harder to merge than a large change committed the same hour you forked?"
  choices={[
    { label: "Merge cost is set by how far trunk drifted since you forked, not by your diff size — a small change on top of weeks of drift reconciles weeks of other people's work", correct: true },
    { label: "Git charges more to merge older branches regardless of content", misconception: "Git's algorithm doesn't penalize age directly. What grows with age is the volume of divergent commits it must reconcile and the lost human context — not a time-based cost in the tool." },
    { label: "Small changes are riskier because they're under-reviewed", misconception: "Review depth isn't the issue here. The point is that the same small diff is cheap to merge when trunk hasn't moved and expensive when it has drifted for weeks underneath it." },
    { label: "Large same-hour merges secretly defer their cost to later", misconception: "There's no deferred cost when trunk hasn't moved — divergence is near zero, so the merge is near-free. The cost only appears as trunk drifts away from your fork point." },
  ]}
/>

<Quiz
  id="01-integration-frequency-quiz-2"
  lessonSlug="01-integration-frequency"
  lang="en"
  question="What makes a semantic conflict more dangerous than a textual one?"
  choices={[
    { label: "Both sides merge cleanly with no conflict markers, so git and the merge tool stay silent while the code is now subtly wrong", correct: true },
    { label: "Semantic conflicts produce more conflict markers to resolve", misconception: "It's the opposite — semantic conflicts produce zero markers. The merge looks successful; the breakage shows up only at build or run time, which is what makes it dangerous." },
    { label: "Git refuses to merge when it detects semantic drift", misconception: "Git has no understanding of meaning. It reconciles text. A rename plus a new call site to the old name can merge cleanly and break — git can't see the contradiction." },
    { label: "They only happen in dynamically typed languages", misconception: "They happen everywhere. A type system catches some at compile time, but changed invariants, altered semantics of a function, or behavioral assumptions slip through regardless of typing." },
  ]}
/>

<DragOrder
  id="01-integration-frequency-drag-1"
  lessonSlug="01-integration-frequency"
  lang="en"
  prompt="Order how merge cost compounds on a long-lived branch:"
  items={[
    { id: "a", label: "You fork from trunk; divergence is zero, merge would be free" },
    { id: "b", label: "Days pass; teammates merge dozens of commits to trunk" },
    { id: "c", label: "Code you depend on is renamed or has its invariants changed" },
    { id: "d", label: "Your branch now carries stale assumptions you can't see" },
    { id: "e", label: "Merge day: textual + semantic conflicts detonate together, nobody has context" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="Integration frequency vs merge cost"
  hue="var(--d-systems)"
  perRow={4}
  nodes={[
    {id:"da",label:"Daily merge",sub:"1 day drift"},
    {id:"db",label:"Small conflict",sub:"minutes"},
    {id:"wa",label:"3-week branch",sub:"600 commits drift"},
    {id:"wb",label:"Big-bang merge",sub:"~12× longer"}
  ]}
  edges={[
    {from:"da",to:"db",label:"integrate"},
    {from:"wa",to:"wb",label:"integrate"},
  ]}
  caption="Frequent integration caps drift at one day; long-lived branches detonate all drift at once."
/>

<KeyTakeaway>Merge cost is a function of drift — how far trunk moved since you forked — and drift compounds because everyone keeps committing. Daily integration doesn't prevent conflicts; it caps each one at a day's worth of drift, small and resolvable with full context. Frequency is the lever; better tooling barely moves the cost.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-integration-frequency-retrieval"
  lang="en"
  questions={[
    { q: "A colleague says 'big features are hard to merge because they're big.' Correct the model and explain what actually drives merge cost.", a: "Diff size isn't the driver — divergence is. Merge cost is set by how far trunk has moved away from the point where you forked, because every conflict, rename, and changed invariant you must reconcile comes from trunk's drift, not from your own lines. A 2,000-line feature merged the same hour you forked has near-zero cost because trunk hasn't moved; a 50-line fix merged three weeks later can be agony because it sits on a foundation rebuilt underneath it. And drift compounds, because integration is shared: while your branch lives, everyone else keeps committing, so a one-day branch reconciles one day of others' work and a three-week branch reconciles three weeks of it plus the context you've lost. Microsoft measured ~12× longer conflict resolution past three days without integrating. The fix is to shrink branch lifetime — integrate daily — so each merge only ever reconciles a small, fresh slice of drift." },
    { q: "Why is 'integrate daily' really a statement about feedback latency, and how do semantic conflicts fit in?", a: "Every hour your branch diverges from trunk is an hour you spend making decisions on stale information: calling functions that may have been renamed, assuming invariants that may have just been broken. The danger isn't only textual conflicts, which git flags loudly and stops you on — it's semantic conflicts, where both sides merge cleanly with no markers but the code is now wrong (someone renamed a function and you added a call to the old name; the build breaks, or an overload silently misbehaves). Semantic conflicts scale with the same drift variable: the more trunk changed while you were away, the more of your branch's hidden assumptions have quietly become false. Integrating daily caps that exposure at a single day — your CI gate catches the broken assumption while the change that caused it is still fresh in someone's memory, instead of letting weeks of invisible drift detonate at once. Shrinking branch lifetime shrinks the window in which you can be wrong without knowing it." },
  ]}
/>

<Recap lang="en">
The cost of merging a branch is governed by drift — how far trunk has moved since you forked — not by how big your own change is. Drift compounds because integration is shared: every other engineer keeps committing while your branch lives, so a day-old branch reconciles a day of work and a three-week branch reconciles three weeks plus the context you've lost, which is why conflict-resolution time rises roughly 12× past three days and can eat a quarter of developer time. Textual conflicts are the cheap, loud ones; the expensive failures are semantic conflicts that merge cleanly and break silently, and they scale with the same drift. Better merge tooling lowers the effort per conflict but never the conflict's size, so the only real lever is frequency: integrate to trunk at least daily, keep branches alive hours not weeks, and treat small batches as the mechanism that surfaces problems early instead of detonating them together. Continuous integration is that practice — the server is just the gate that makes it safe.
</Recap>
