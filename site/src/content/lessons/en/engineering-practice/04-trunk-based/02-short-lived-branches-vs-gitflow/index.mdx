---
concepts:
- short-lived-branches
- gitflow
- github-flow
- branch-by-abstraction
- release-from-trunk
- three-branch-rule
deepensInto:
- 03-feature-flags-dark-launch
estMin: 16
lang: en
lessonType: topic
level: middle
order: 2
prereqs:
- 01-integration-frequency
slug: 02-short-lived-branches-vs-gitflow
sources:
- https://dora.dev/capabilities/trunk-based-development/
- https://trunkbaseddevelopment.com/branch-for-release/
- https://martinfowler.com/bliki/BranchByAbstraction.html
- https://nvie.com/posts/a-successful-git-branching-model/
spiral:
- cost-of-delay
- incremental-change
status: ready
summary: Trunk-based means short-lived branches — three or fewer, gone within a day,
  no integration phase — not no branches. Gitflow suits scheduled shrink-wrapped releases
  but institutionalizes drift for continuous deploy. Big refactors stay on trunk via
  branch by abstraction.
title: Short-lived branches vs gitflow
track: engineering-practice
unit: 04-trunk-based
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A team reads "trunk-based development" and hears "commit straight to main, no branches, no PRs." They turn off branch protection. Within a week the trunk is red twice a day, an unreviewed change leaks a credential, and they conclude trunk-based "doesn't scale." They threw out the wrong thing. Trunk-based was never about deleting branches — it's about keeping them so short-lived that they never accumulate drift. The branch lives for an hour, behind a green gate and a review, then it's gone.
</Hook>

<Crux>Trunk-based means short-lived branches — three or fewer, gone within a day, no integration phase — not the absence of branches.</Crux>

<Explanation>

## What "short-lived" actually specifies

Before you can tell whether your team is trunk-based, you need a precise definition — because without one, "we use short branches" and "we use gitflow" can look the same from the outside. DORA's definition of trunk-based is concrete, not vibes. Keep **three or fewer active branches** in the repository at any time. Branches should last **no more than a day** before merging into trunk — often only a few hours. There are **no code freezes and no integration phases**. Each branch is one small, scoped change that opens a PR, passes a fast CI gate, gets a quick review, and merges the same day. The branch is a staging area for review and gating, not a place where work *lives*.

This is sometimes run as pure "commit to trunk" (no branches at all, common in small co-located teams with pairing) and more often as "short-lived feature branches" (the version most teams use, because it keeps code review and the PR gate). Both are trunk-based. The dividing line isn't "branch or no branch" — it's **lifetime**. A branch that lives hours is trunk-based; a `feature/*` branch that lives two weeks is the long-lived branch the practice exists to eliminate, no matter what you call your workflow.

## Why gitflow optimizes for the opposite world

Gitflow gives you permanent `main` and `develop` branches, plus `feature/*`, `release/*`, and `hotfix/*` branches with formal phases: features integrate into `develop`, a `release` branch stabilizes, then merges to `main` and tags. It's a coherent model — for the world it was designed for: **shrink-wrapped, versioned software with scheduled releases**, multiple versions supported in the field, and no continuous deployment. In that world, an explicit stabilization phase and parallel release lines earn their keep.

For a continuously deployed web service, gitflow institutionalizes exactly the drift the previous lesson warned about. Long-lived `feature/*` branches accumulate divergence; the `develop`-to-`release`-to-`main` promotion is a built-in integration phase; "done" features sit in `develop` rotting while they wait for the next release train. You inherit big-bang merges and code freezes by design. DORA's research explicitly flags integration phases and code freezes as anti-patterns for delivery performance — gitflow makes them load-bearing.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Dimension</th>
        <th class="border border-border px-3 py-2 text-left">Gitflow</th>
        <th class="border border-border px-3 py-2 text-left">GitHub flow</th>
        <th class="border border-border px-3 py-2 text-left">Trunk-based</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Long-lived branches</td>
        <td class="border border-border px-3 py-2">main + develop (+ release)</td>
        <td class="border border-border px-3 py-2">main only</td>
        <td class="border border-border px-3 py-2">trunk only</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Feature branch life</td>
        <td class="border border-border px-3 py-2">Days to weeks</td>
        <td class="border border-border px-3 py-2">Until PR merges (variable)</td>
        <td class="border border-border px-3 py-2">Hours, &lt; 1 day</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Integration phase</td>
        <td class="border border-border px-3 py-2">Explicit (release branch)</td>
        <td class="border border-border px-3 py-2">None</td>
        <td class="border border-border px-3 py-2">None</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Built for</td>
        <td class="border border-border px-3 py-2">Versioned, scheduled releases</td>
        <td class="border border-border px-3 py-2">Web apps, continuous deploy</td>
        <td class="border border-border px-3 py-2">Continuous deploy at scale</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Release</td>
        <td class="border border-border px-3 py-2">Promote develop → release → main</td>
        <td class="border border-border px-3 py-2">Deploy merged main</td>
        <td class="border border-border px-3 py-2">Tag/branch from trunk at release point</td>
      </tr>
    </tbody>
  </table>
</div>

## Releasing without long-lived branches

The objection: "if there's only trunk, where do releases come from, and how do I patch an old version?" Trunk-based has a specific answer — **branch for release, not for feature**. When you cut a release, you create a short-lived `release/x.y` branch *from* trunk at that commit. Day-to-day development never happens on it. If a critical fix is needed, you fix it on trunk first (so the next release has it too) and **cherry-pick** the single commit onto the release branch. The release branch is a read-mostly snapshot, not a parallel development line, so it never accumulates drift. Teams doing continuous deployment often skip even this and just tag trunk.

For big changes that genuinely can't ship in a day — replacing an ORM, swapping a payment provider — the trunk-based tool is **branch by abstraction**, not a long-lived branch. You introduce an abstraction layer over the thing you're replacing, build the new implementation behind it incrementally on trunk (each step merged daily, dark until ready), switch the abstraction to the new implementation, then remove the old one and the abstraction. The large refactor lives on trunk the entire time as a sequence of small green commits — never as a three-week branch.

<Inset kind="why" lang="en">
The naming trap is real: a `feature/*` branch in a gitflow repo and a short-lived branch in a trunk-based repo can look identical in git. The difference is invisible in the branch and visible only in its lifetime and the discipline around it. "We use feature branches" tells you nothing about whether you're trunk-based — "our branches are merged or deleted within a day" tells you everything.
</Inset>

<TradeoffMatrix
  id="02-short-lived-branches-tradeoff-1"
  lessonSlug="02-short-lived-branches-vs-gitflow"
  lang="en"
  prompt="You're replacing the ORM across a continuously deployed service. The migration will take six weeks. How do you do it trunk-based?"
  options={[
    { name: "Branch by abstraction: introduce a layer over data access, build the new ORM behind it in small daily-merged steps, flip the abstraction, then delete the old path", correct: true, justification: "This keeps the six-week change on trunk the whole time as a sequence of small green commits, each integrating daily, dark until the flip. You never accumulate drift, the team keeps building on a releasable trunk, and you can pause or reverse at the abstraction boundary. It's the trunk-based answer to 'too big to ship in a day.'" },
    { name: "Cut a long-lived migration/orm branch and merge it when the migration is done", summary: "The long-lived branch.", justification: "Six weeks of divergence on a continuously deployed service is exactly the big-bang merge and semantic-conflict detonation trunk-based exists to avoid. The rest of the team builds on a trunk your branch knows nothing about." },
    { name: "Switch the repo to gitflow with a dedicated release branch for the migration", summary: "Wrong model for the world.", justification: "Gitflow's stabilization phases suit scheduled shrink-wrapped releases, not a continuously deployed service. It institutionalizes the integration phase and long-lived branch you're trying to avoid." },
    { name: "Freeze other work on the affected modules until the migration lands", summary: "Code freeze.", justification: "Freezes are a DORA anti-pattern: they stall the team and just move the integration pain to thaw day. Branch by abstraction lets normal work continue on trunk alongside the migration." },
  ]}
/>

<Quiz
  id="02-short-lived-branches-quiz-1"
  lessonSlug="02-short-lived-branches-vs-gitflow"
  lang="en"
  question="What is the actual dividing line between trunk-based and a long-lived-branch workflow?"
  choices={[
    { label: "Branch lifetime — trunk-based branches are merged or deleted within a day; the model is about drift, not whether branches exist", correct: true },
    { label: "Whether you use branches at all — trunk-based forbids them", misconception: "Most trunk-based teams use short-lived feature branches to keep code review and the PR gate. The practice forbids long-lived branches, not branches as such." },
    { label: "Whether you use pull requests", misconception: "PRs are orthogonal. You can be trunk-based with PRs (the common case) or with direct commits to trunk (paired teams). Lifetime is the distinguishing variable, not the PR mechanism." },
    { label: "Whether the repo has a develop branch", misconception: "A develop branch is a gitflow artifact and its absence is suggestive, but the real test is lifetime — a single feature branch left alive for two weeks is non-trunk-based regardless of whether develop exists." },
  ]}
/>

<Quiz
  id="02-short-lived-branches-quiz-2"
  lessonSlug="02-short-lived-branches-vs-gitflow"
  lang="en"
  question="In trunk-based development, how is a critical fix to an already-released version handled?"
  choices={[
    { label: "Fix it on trunk first, then cherry-pick that single commit onto the short-lived release branch cut from trunk", correct: true },
    { label: "Develop the fix on the release branch, then merge the release branch back into trunk", misconception: "That turns the release branch into a parallel development line that accumulates drift — the thing trunk-based avoids. Fix on trunk first so the next release inherits it, then cherry-pick down." },
    { label: "Create a long-lived hotfix branch as in gitflow", misconception: "That's the gitflow model. Trunk-based branches for release are short-lived, read-mostly snapshots; fixes flow from trunk down via cherry-pick, not from a long-lived hotfix line." },
    { label: "Roll trunk back to the released commit and re-apply later work", misconception: "Rolling back trunk to patch an old release would discard everyone's subsequent work. The release branch exists precisely so you can patch the old point without disturbing trunk." },
  ]}
/>

<DragOrder
  id="02-short-lived-branches-drag-1"
  lessonSlug="02-short-lived-branches-vs-gitflow"
  lang="en"
  prompt="Order a branch-by-abstraction migration on trunk:"
  items={[
    { id: "a", label: "Introduce an abstraction layer over the component you're replacing" },
    { id: "b", label: "Build the new implementation behind the abstraction in small daily-merged commits, dark" },
    { id: "c", label: "Switch the abstraction to point at the new implementation" },
    { id: "d", label: "Verify in production, then delete the old implementation" },
    { id: "e", label: "Remove the now-redundant abstraction layer" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="Short-lived branches vs Gitflow"
  hue="var(--d-systems)"
  perRow={4}
  nodes={[
    {id:"trunk",label:"trunk",sub:"always releasable"},
    {id:"sb",label:"Short branch",sub:"hours, < 1 day"},
    {id:"merge",label:"Merge to trunk",sub:"CI gate passes"},
    {id:"develop",label:"develop branch",sub:"days–weeks drift"},
    {id:"release",label:"release branch",sub:"integration phase"},
    {id:"main",label:"main",sub:"scheduled release"}
  ]}
  edges={[
    {from:"trunk",to:"sb",label:"fork"},
    {from:"sb",to:"merge",label:"PR + CI"},
    {from:"merge",to:"trunk",label:"lands"},
    {from:"develop",to:"release",label:"stabilise"},
    {from:"release",to:"main",label:"ship"}
  ]}
  caption="Trunk-based: branch lives hours, merges back same day. Gitflow: features accumulate on develop, integration is a phase."
/>

<KeyTakeaway>Trunk-based forbids long-lived branches, not branches: keep three or fewer active, gone within a day, with no integration phase. Gitflow's develop/release/hotfix structure is right for scheduled shrink-wrapped software but institutionalizes drift for continuously deployed services. Release by branching from trunk and cherry-picking down; handle big refactors with branch by abstraction so they live on trunk as small green commits.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-short-lived-branches-retrieval"
  lang="en"
  questions={[
    { q: "A team turned off all branch protection because they read that trunk-based means 'no branches.' What did they misunderstand, and what does trunk-based actually specify?", a: "They confused 'no long-lived branches' with 'no branches.' Trunk-based is about branch lifetime and drift, not the existence of branches. DORA's definition is concrete: three or fewer active branches, each living no more than a day (often hours), with no code freezes and no integration phases. Most teams run this as short-lived feature branches precisely so they keep code review and the PR/CI gate — turning those off throws away the safety mechanisms while keeping none of the benefit. Pure commit-to-trunk exists too (small paired teams) but still relies on a fast green gate. The dividing line between trunk-based and a long-lived-branch workflow is lifetime: a branch that merges within a day is trunk-based; a feature branch alive for two weeks is the long-lived branch the practice exists to eliminate, whatever you name it." },
    { q: "If there's only trunk, how do you cut releases and do a six-week refactor without a long-lived branch?", a: "Two distinct tools. For releases: branch for release, not for feature. Cut a short-lived release/x.y branch from trunk at the release commit; never develop on it. A critical fix goes onto trunk first (so the next release inherits it) and is cherry-picked down to the release branch — the release branch stays a read-mostly snapshot that never accumulates drift, and continuous-deploy teams often just tag trunk instead. For a six-week refactor like swapping an ORM: branch by abstraction, not a long-lived branch. Put an abstraction layer over the thing you're replacing, build the new implementation behind it in small commits merged to trunk daily and kept dark, flip the abstraction to the new implementation, verify, then delete the old path and the abstraction. The large change lives on trunk the whole time as a sequence of small green commits, so the team keeps building on a releasable trunk and you never pay a big-bang merge." },
  ]}
/>

<Recap lang="en">
Trunk-based development forbids long-lived branches, not branches themselves — DORA specifies three or fewer active branches, a lifetime under a day, and no integration phases or code freezes. Whether you commit directly to trunk or use short-lived feature branches with PRs, the distinguishing variable is lifetime, not the presence of a branch. Gitflow's main/develop/release/hotfix structure is coherent for shrink-wrapped, scheduled, multi-version software, but for a continuously deployed service it institutionalizes the very drift, big-bang merges, and integration phases that trunk-based exists to remove. Releases come from short-lived branches cut from trunk, with fixes made on trunk first and cherry-picked down so the release branch never becomes a parallel development line. And changes too big to ship in a day use branch by abstraction — an abstraction layer, the new implementation built behind it in small daily commits, a flip, then cleanup — so even a six-week refactor lives on trunk as small green steps instead of a long-lived branch. Now when you see a workflow called "feature branches," you know the only question that matters is: how long does each branch live?
</Recap>
