---
concepts:
- feature-flags
- dark-launch
- decouple-deploy-from-release
- canary-release
- kill-switch
- flag-taxonomy
deepensInto:
- 04-the-green-trunk-gate
estMin: 16
lang: en
lessonType: topic
level: middle
order: 3
prereqs:
- 02-short-lived-branches-vs-gitflow
slug: 03-feature-flags-dark-launch
sources:
- https://martinfowler.com/articles/feature-toggles.html
- https://launchdarkly.com/blog/why-decouple-deployments-from-releases/
- https://www.getunleash.io/blog/using-feature-flags-to-enable-trunk-based-development
- https://trunkbaseddevelopment.com/feature-flags/
spiral:
- incremental-change
- blast-radius
status: ready
summary: 'Daily integration of long features needs deploy and release split apart.
  A flag ships unfinished work to production dark — integrated but unreachable — so
  release is a runtime ramp you can roll back in seconds. Flag types differ in lifetime:
  release, ops, experiment, permission.'
title: Feature flags decouple deploy from release
track: engineering-practice
unit: 04-trunk-based
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
"I can't merge daily — my feature isn't finished. I can't put half a checkout flow on trunk." This is the objection that sinks most trunk-based adoptions, and it rests on a hidden assumption: that putting code in production means *exposing it to users*. Separate those two and the objection dissolves. The half-finished checkout flow ships to production today, runs through CI, sits there fully integrated — and no user can reach it, because it's behind a flag that's off. You deployed the code. You didn't release the feature.
</Hook>

<Crux>A feature flag splits deploy from release, so unfinished work rides on trunk dark and release becomes a runtime decision.</Crux>

<Explanation>

## Deploy and release are different events

Bundling "deploy" and "release" into one act is what forces long-lived branches: if shipping code is the same as exposing the feature, then incomplete work must stay off trunk until it's done. Feature flags break the bundle. **Deploying** is moving code to production servers. **Releasing** is making a feature reachable by users. A flag is the runtime switch between them: wrap the new path in a conditional that reads a flag, ship it off, and the code is deployed, integrated, and tested but **dark** — present and unreachable.

This is what makes daily integration compatible with multi-week features. The work merges to trunk every day as small green commits, each hidden behind the off flag, so you never accumulate drift (lesson 1) and never need a long-lived branch (lesson 2). When the feature is complete, you don't deploy anything — you flip a flag. Release stops being a risky big push and becomes a configuration change you can make at 10 a.m. on a Tuesday with the author watching dashboards.

## Dark launch and progressive exposure

Once release is a runtime decision, you get capabilities a deploy can't give you. **Dark launching** means running the new code path in production *before* showing its results to users — e.g. send real traffic to the new recommendation service, compare its output to the old one and measure latency, but discard the new output. You load-test and validate against production reality with zero user-visible risk. Then you **progressively expose**: turn the feature on for internal users, then 1%, then 5%, 25%, 100%, watching error rates and latency at each step. This is a canary by flag — blast radius grows only as confidence grows.

The same switch is your fastest recovery tool. If the ramped feature misbehaves at 5%, you flip the flag off — a **kill switch** — and every user is back on the old path in seconds, no rollback deploy, no revert PR, no waiting for CI. Decoupling deploy from release turns "we shipped a bug, start the incident" into "we turned it off, now let's debug calmly."

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Flag type</th>
        <th class="border border-border px-3 py-2 text-left">Purpose</th>
        <th class="border border-border px-3 py-2 text-left">Expected lifetime</th>
        <th class="border border-border px-3 py-2 text-left">Who flips it</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Release</td>
        <td class="border border-border px-3 py-2">Hide unfinished work; ramp a rollout</td>
        <td class="border border-border px-3 py-2">Days to weeks — delete after 100%</td>
        <td class="border border-border px-3 py-2">Dev / release owner</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Ops / kill switch</td>
        <td class="border border-border px-3 py-2">Disable a feature under load/incident</td>
        <td class="border border-border px-3 py-2">Long-lived by design</td>
        <td class="border border-border px-3 py-2">On-call / ops</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Experiment</td>
        <td class="border border-border px-3 py-2">A/B test, measure a hypothesis</td>
        <td class="border border-border px-3 py-2">One experiment cycle</td>
        <td class="border border-border px-3 py-2">Product / data</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Permission / entitlement</td>
        <td class="border border-border px-3 py-2">Gate features by plan/role</td>
        <td class="border border-border px-3 py-2">Permanent (it's a product rule)</td>
        <td class="border border-border px-3 py-2">Product / runtime</td>
      </tr>
    </tbody>
  </table>
</div>

## Not every flag is the same animal

The single word "flag" hides four very different things, and conflating them is how flag systems rot. A **release flag** is temporary scaffolding — it exists to hide unfinished work and ramp a rollout, and it must be deleted once the feature is at 100%. An **ops flag** (kill switch) is meant to live forever, so you can shed load or disable a misbehaving subsystem during an incident. An **experiment flag** lives for one A/B cycle. A **permission flag** is permanent because it encodes a product rule (this feature is for Pro plans). The dangerous category is the release flag, because it *looks* permanent if you forget it — and a forgotten release flag is the flag-debt problem of the next lesson.

<Inset kind="why" lang="en">
The reason "decouple deploy from release" is the unlock for trunk-based isn't the flags themselves — it's that it removes the only honest reason to keep work off trunk. Without flags, "my feature isn't done" is a real argument for a long-lived branch. With flags, unfinished work has a safe home in production, so there's no longer any excuse to let a branch accumulate drift. The flag is what makes "integrate daily" achievable for work that takes a month.
</Inset>

<TradeoffMatrix
  id="03-feature-flags-tradeoff-1"
  lessonSlug="03-feature-flags-dark-launch"
  lang="en"
  prompt="You're rolling out a rewritten search ranking that touches every query. How do you release it with the smallest blast radius?"
  options={[
    { name: "Dark launch first (run new ranking in prod, compare to old, discard its output), then ramp the flag 1% → 25% → 100% watching metrics, with a kill switch ready", correct: true, justification: "Dark launch validates the new path against real production traffic with zero user impact; the progressive ramp grows blast radius only as confidence grows; and the kill switch makes recovery a seconds-long flag flip instead of a rollback deploy. This is the full decouple-deploy-from-release toolkit applied to a high-risk change." },
    { name: "Deploy it to 100% of users at once and watch error rates closely", summary: "Big-bang release.", justification: "Watching closely doesn't shrink blast radius — a ranking regression hits every user immediately and recovery means a rollback deploy. The point of flags is to make exposure gradual and recovery instant." },
    { name: "Keep it on a branch until you're fully confident, then merge and deploy", summary: "Confidence on a branch.", justification: "You can't gain production confidence on a branch — the new ranking only proves itself against real traffic, which is exactly what dark launching gives you. The branch also accumulates drift while you wait." },
    { name: "Ship behind a flag but skip the dark launch and ramp straight to 100%", summary: "Flag without the ramp.", justification: "A flag you flip straight to 100% gives you a fast kill switch but throws away the gradual-exposure benefit. For a change touching every query, validate dark and ramp — don't expose everyone in one flip." },
  ]}
/>

<Quiz
  id="03-feature-flags-quiz-1"
  lessonSlug="03-feature-flags-dark-launch"
  lang="en"
  question="What hidden assumption makes 'I can't merge daily, my feature isn't done' feel true — and how does a flag dissolve it?"
  choices={[
    { label: "The assumption that deploying code equals exposing it to users; a flag separates them, so unfinished code ships dark and unreachable until released", correct: true },
    { label: "The assumption that CI is too slow; a flag speeds up the build", misconception: "Flags don't change build speed. The assumption being dissolved is that putting code in production means users can reach it — flags break that link, letting incomplete work ship hidden." },
    { label: "The assumption that code review is required; a flag bypasses review", misconception: "Flags don't bypass review — flagged code still goes through the gate. What they change is exposure: deployed but dark, so 'not done' is no longer a reason to keep it off trunk." },
    { label: "The assumption that you need branches; a flag is a kind of branch", misconception: "A flag is a runtime conditional, not a VCS branch, and the point is the opposite — flags remove the need for long-lived branches by giving unfinished work a home in production." },
  ]}
/>

<Quiz
  id="03-feature-flags-quiz-2"
  lessonSlug="03-feature-flags-dark-launch"
  lang="en"
  question="Which flag type is temporary scaffolding that must be deleted after the feature reaches 100%?"
  choices={[
    { label: "A release flag — it exists only to hide unfinished work and ramp a rollout, and becomes flag debt if left behind", correct: true },
    { label: "An ops/kill-switch flag", misconception: "Kill switches are meant to live forever so on-call can disable a subsystem during an incident. The temporary, must-delete category is the release flag." },
    { label: "A permission/entitlement flag", misconception: "Permission flags are permanent — they encode a product rule like 'Pro-plan only.' The temporary one that must be removed at 100% is the release flag." },
    { label: "An experiment flag", misconception: "Experiment flags live for one A/B cycle and are cleaned up after, but the flag type specifically tied to hiding unfinished rollout work is the release flag." },
  ]}
/>

<DragOrder
  id="03-feature-flags-drag-1"
  lessonSlug="03-feature-flags-dark-launch"
  lang="en"
  prompt="Order a safe release of a risky feature using flags:"
  items={[
    { id: "a", label: "Merge the work to trunk daily behind a release flag that's off — deployed but dark" },
    { id: "b", label: "Dark launch: run the new path in prod, compare output, discard it" },
    { id: "c", label: "Enable for internal users, then ramp 1% → 25% → 100% watching metrics" },
    { id: "d", label: "Keep a kill switch ready to flip off in seconds if metrics regress" },
    { id: "e", label: "At stable 100%, delete the release flag and the dead old path" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Deploying code and releasing a feature are different events; a flag is the runtime switch between them. That lets unfinished work ride on trunk dark, makes release a progressive ramp you can dark-launch and roll back in seconds via a kill switch, and removes the only honest reason to keep work off trunk. But "flag" hides four animals — release, ops, experiment, permission — with different lifetimes; only release flags are temporary scaffolding that must be deleted.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-feature-flags-retrieval"
  lang="en"
  questions={[
    { q: "Explain why decoupling deploy from release is the specific thing that makes trunk-based work for multi-week features.", a: "Bundling deploy and release into one act is what forces long-lived branches: if shipping code is the same as exposing the feature, incomplete work has to stay off trunk until it's done. A feature flag breaks the bundle — deploying is moving code to production, releasing is making it reachable by users, and the flag is the runtime switch between them. So a month-long feature merges to trunk every day as small green commits, each hidden behind an off flag: deployed, integrated, and tested but dark. That's what removes the only honest reason to keep work off trunk — 'it isn't done' no longer requires a branch, because unfinished work has a safe home in production. You never accumulate drift and never need a long-lived branch. Release then stops being a risky push and becomes a configuration change: flip the flag when the feature is complete." },
    { q: "Beyond hiding unfinished work, what operational capabilities does a flag give you, and why does flag taxonomy matter?", a: "Once release is a runtime decision you get three things a deploy can't give. Dark launching: run the new code path in production before showing results to users — send real traffic to the new service, compare output and measure latency, discard the new output — so you validate against production reality with zero user risk. Progressive exposure: turn it on for internal users, then 1%, 5%, 25%, 100%, watching error rates and latency so blast radius grows only with confidence. And a kill switch: if the ramp misbehaves, flip the flag off and everyone is back on the old path in seconds — no rollback deploy. Taxonomy matters because 'flag' hides four different lifetimes: release flags are temporary scaffolding deleted at 100%; ops/kill-switch flags live forever for incident response; experiment flags live one A/B cycle; permission flags are permanent product rules. Conflating them is how flag systems rot — the dangerous case is a release flag left behind, which looks permanent and becomes flag debt." },
  ]}
/>

<Recap lang="en">
Daily integration of multi-week work is only possible because deploying code and releasing a feature are separable events, and a feature flag is the runtime switch between them: wrap the new path in a conditional that's off, and the code ships to production dark — integrated and tested but unreachable. That gives unfinished work a safe home on trunk so you never need a long-lived branch, and it turns release from a risky deploy into a configuration change. It also unlocks operational power: dark launching validates new code against real traffic with no user impact, progressive exposure ramps blast radius with confidence, and a kill switch makes recovery a seconds-long flip instead of a rollback. But "flag" conflates four animals with different lifetimes — temporary release flags, permanent ops kill switches, one-cycle experiment flags, and permanent permission flags — and the release flag is the one that must be deleted at 100%, or it quietly becomes the flag debt of the next lesson.
</Recap>
