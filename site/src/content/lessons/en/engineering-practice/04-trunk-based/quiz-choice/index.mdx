---
concepts:
- integration-frequency
- short-lived-branches
- feature-flags
- ci-gate
- merge-queue
- flag-debt
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://dora.dev/capabilities/trunk-based-development/
- https://martinfowler.com/articles/branching-patterns.html
- https://github.blog/engineering/engineering-principles/how-github-uses-merge-queue-to-ship-hundreds-of-changes-every-day/
status: ready
summary: "Multiple-choice synthesis across the trunk-based unit — drift, short-lived branches, feature flags, the green CI gate, merge queues at scale, and flag debt."
title: 'Trunk-based: multiple-choice review'
track: engineering-practice
unit: 04-trunk-based
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one mirrors a call you make in a real workflow argument — not a definition to recite, but a tradeoff to defend when someone says "trunk-based doesn't scale."
</Hook>

<Goal lang="en">
Confirm you can connect the unit's spine: drift sets merge cost, short-lived branches cap it, flags decouple deploy from release, the green CI gate makes the shared trunk safe, merge queues hold it at scale, and cleanup is what makes the whole trade net-positive.
</Goal>

<Quiz
  id="04-trunk-based-choice-1"
  lessonSlug="04-trunk-based"
  lang="en"
  question="One engineer merges a 2,000-line feature the same hour they forked; another merges a 50-line fix from a three-week branch. Which merge is more expensive, and why?"
  choices={[
    { label: "The 50-line fix. Merge cost is set by how far trunk drifted since you forked, not by diff size — the small change sits on three weeks of other people's commits, renames, and changed invariants it must reconcile.", correct: true },
    { label: "The 2,000-line feature, because larger diffs always produce more conflicts.", misconception: "Diff size doesn't drive merge cost; divergence does. A big change merged the same hour reconciles near-zero drift because trunk hasn't moved — it's near-free." },
    { label: "Equal, since git resolves both with the same three-way merge algorithm.", misconception: "Same algorithm, very different inputs. The cost is the volume of divergent commits and lost context to reconcile, set by branch lifetime, not the merge tool." },
    { label: "The 50-line fix, because small changes are under-reviewed and riskier.", misconception: "Review depth isn't the point. The same 50 lines are cheap on a fresh trunk and expensive on three weeks of drift — lifetime, not review, is the variable." },
  ]}
/>

<Quiz
  id="04-trunk-based-choice-2"
  lessonSlug="04-trunk-based"
  lang="en"
  question="A team reads 'trunk-based development,' turns off branch protection, and commits straight to main. A week later trunk is red twice a day and a credential leaked. What did they actually get wrong?"
  choices={[
    { label: "They confused 'no long-lived branches' with 'no branches.' Trunk-based is about branch lifetime and a green gate — most teams keep short-lived branches with PRs and CI precisely to preserve review and gating.", correct: true },
    { label: "Nothing — committing straight to main is the only true trunk-based workflow.", misconception: "Pure commit-to-trunk exists for small paired teams but still relies on a fast green gate. The model forbids long-lived branches, not the PR/CI safety mechanisms." },
    { label: "Trunk-based genuinely doesn't scale past a few engineers.", misconception: "The failure was dropping the gate and review, not the branching model. DORA data shows trunk-based correlates with elite delivery when the gate and discipline are present." },
    { label: "They needed gitflow's develop branch to stage changes safely.", misconception: "Adding a develop branch re-introduces the integration phase and drift trunk-based removes. The fix is a fast blocking gate plus short-lived branches, not a long-lived staging branch." },
  ]}
/>

<Quiz
  id="04-trunk-based-choice-3"
  lessonSlug="04-trunk-based"
  lang="en"
  question="An engineer insists they can't integrate daily because their checkout rewrite isn't finished and 'you can't put half a flow on trunk.' What dissolves the objection?"
  choices={[
    { label: "Deploying code and releasing a feature are different events. A feature flag is the runtime switch between them, so the half-finished flow ships to production dark — integrated and tested but unreachable — until you flip the flag.", correct: true },
    { label: "They should keep it on a long-lived branch until it's complete, then merge once.", misconception: "That is exactly the drift-accumulating long-lived branch the unit exists to eliminate; the flag gives unfinished work a safe home on trunk instead." },
    { label: "They should make CI faster so the half-finished code passes the build.", misconception: "Build speed isn't the blocker. The hidden assumption is that deploying equals exposing to users — a flag breaks that link so incomplete code can ship hidden." },
    { label: "Code review can be skipped for incomplete work behind a flag.", misconception: "Flagged code still goes through the gate and review. The flag changes exposure, not the requirement to review and pass CI." },
  ]}
/>

<Quiz
  id="04-trunk-based-choice-4"
  lessonSlug="04-trunk-based"
  lang="en"
  question="You must replace the ORM across a continuously deployed service; the migration will take six weeks. What's the trunk-based way to do it?"
  choices={[
    { label: "Branch by abstraction: put a layer over data access, build the new ORM behind it in small daily-merged commits kept dark, flip the abstraction, then delete the old path. The change lives on trunk as small green steps.", correct: true },
    { label: "Cut a long-lived migration branch and merge it once the migration is done.", misconception: "Six weeks of divergence on a continuously deployed service is the big-bang merge and semantic-conflict detonation trunk-based avoids. The team builds on a trunk that branch knows nothing about." },
    { label: "Switch the repo to gitflow with a dedicated release branch for the migration.", misconception: "Gitflow's stabilization phases suit scheduled shrink-wrapped releases, not continuous deploy. It institutionalizes the integration phase and long-lived branch you're trying to avoid." },
    { label: "Freeze other work on the affected modules until the migration lands.", misconception: "Code freezes are a DORA anti-pattern — they stall the team and move the integration pain to thaw day. Branch by abstraction lets normal work continue on trunk." },
  ]}
/>

<Quiz
  id="04-trunk-based-choice-5"
  lessonSlug="04-trunk-based"
  lang="en"
  question="At 50 PRs/day on one trunk, every PR is green before merge yet trunk still breaks a few times a week. What's the right fix?"
  choices={[
    { label: "A merge queue that tests each PR against the projected trunk — trunk plus the PRs ahead in line — with batching for throughput, so only changes green against the final state land.", correct: true },
    { label: "Require two approvals and make CI checks stricter.", misconception: "Each PR is still validated against a stale base, so two independently-green PRs can combine to break trunk. The problem is the state you test against, not review depth." },
    { label: "Move to post-merge CI and just revert fast when trunk goes red.", misconception: "Post-merge testing detects breakage only after it blocks the team, and at this volume the revert churn is constant. The gate exists to keep trunk green, not to clean it up after." },
    { label: "Split into long-lived team branches that integrate weekly.", misconception: "That abandons trunk-based to dodge a solvable infra problem, trading the queue bottleneck for weekly big-bang merges and reabsorbed drift. Scale the gate, not the branches." },
  ]}
/>

<Quiz
  id="04-trunk-based-choice-6"
  lessonSlug="04-trunk-based"
  lang="en"
  question="Two years in, a trunk-based team has 400 flags, a 'new checkout' flag at 100% for a year with the old path still behind the else, and an incident from two flags whose combination was never tried. What's the root failure?"
  choices={[
    { label: "Skipped cleanup. A release flag left on after rollout is a long-lived branch hiding in an if; N stale flags create 2^N untested runtime configs, so interaction bugs become inevitable. The fix is deleting flags as aggressively as branches.", correct: true },
    { label: "Trunk-based simply trades branch pain for unavoidable flag pain.", misconception: "Flag debt is avoidable with lifecycle discipline — owners, expiries, and deleting the flag and dead path as the closing step of rollout. The debt only piles up when cleanup is skipped." },
    { label: "They should have used fewer flags by keeping work on branches instead.", misconception: "Branches re-introduce drift and big-bang merges. Flags are the right tool; the discipline is to remove release flags at 100%, not to avoid flags." },
    { label: "The flag system is too slow at runtime with 400 flags.", misconception: "The cost isn't runtime latency — it's the combinatorial space of untested configurations and the rotting old paths behind stale release flags." },
  ]}
/>

<Recap lang="en">
The through-line of the unit is one disciplined system, not a branching trick. Drift sets merge cost, so short-lived branches (three or fewer, gone within a day) cap it; feature flags decouple deploy from release so unfinished work rides on trunk dark and big refactors use branch by abstraction; the fast blocking green gate makes the shared trunk safe and merge queues hold it green at scale; and cleanup — delete the branch on merge, the flag on full rollout — is what stops branch debt from reappearing as 2^N flag debt. Adopt the branch model alone and you get the failure modes; adopt the whole system and the DORA numbers follow.
</Recap>
