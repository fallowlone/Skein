---
concepts:
- red-green-refactor
- test-doubles-boundary
- property-based-testing
- when-tdd-hurts
- mutation-testing
- behavior-vs-implementation
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://martinfowler.com/articles/mocksArentStubs.html
- https://hypothesis.works/articles/quickcheck-in-every-language/
- https://stryker-mutator.io/docs/mutation-testing-elements/mutant-states-and-metrics/
status: ready
summary: "Multiple-choice synthesis across the TDD unit — design pressure, the boundary rule for doubles, property shapes, the spec-stability tradeoff, and mutation score as the honest metric."
title: 'TDD: multiple-choice review'
track: engineering-practice
unit: 01-tdd-property
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one is a decision a senior makes under real constraints — a fragile suite, an unknown vendor, a green CI sitting on a 67% mutation score — not a definition to recite.
</Hook>

<Goal lang="en">
Confirm you can connect the unit's spine: test-first as design pressure, the boundary rule that tames doubles, properties over examples, when TDD's tax is worth paying, and mutation score as the metric coverage can't fake.
</Goal>

<Quiz
  id="01-tdd-property-choice-1"
  lessonSlug="01-tdd-property"
  lang="en"
  question="You write a test first and find you must construct four collaborators — DB, payment, email, flags — just to exercise one cancel-order rule. What is the test actually telling you?"
  choices={[
    { label: "The object has too many responsibilities — test-first surfaced the over-broad design before cancel() gained call sites. Extract the rule into a focused unit; the friction is the signal, not an obstacle to mock around.", correct: true },
    { label: "Wire up four mocks so the test compiles, then move on.", misconception: "That silences the exact signal TDD just gave you. A test needing four mocks is testing a god object; the mocks paper over the design problem and lock the bad shape in. The friction was the point." },
    { label: "Skip the test — this code is clearly too hard to test.", misconception: "Hard-to-test almost always means hard-to-change. Declaring it untestable ships the god object untouched and unguarded; the difficulty is information about the design, not a reason to drop the discipline." },
    { label: "Higher line coverage is the real goal here, so make it compile and count the coverage.", misconception: "Coverage is a side effect, not the goal. Test-first's distinctive value is the design feedback from consuming your own API as the first caller — which this hard-to-construct object just delivered." },
  ]}
/>

<Quiz
  id="01-tdd-property-choice-2"
  lessonSlug="01-tdd-property"
  lang="en"
  question="A PriceCalculator uses a pure TaxRule you own and a third-party CurrencyApi over HTTP. How should you double each for a robust unit test?"
  choices={[
    { label: "Use the real TaxRule, stub or fake the CurrencyApi, and assert the final computed price. You exercise the real tax math, eliminate network flakiness, and the test fails only when the price is wrong.", correct: true },
    { label: "Mock both and assert each was called with the right arguments.", misconception: "Mocking the pure TaxRule you own is over-mocking — there is nothing to isolate, and asserting its calls pins the test to internal structure. Worse, stubbing the tax result means the real math is never verified and a pricing bug can ship green." },
    { label: "Use real objects for both, including a live call to CurrencyApi.", misconception: "A live network boundary makes the test slow, flaky, and dependent on a third party's uptime and a changing rate. Network boundaries are exactly what you must double — this loses determinism for no benefit." },
    { label: "Stub TaxRule to a fixed tax, use the real CurrencyApi.", misconception: "This inverts the boundary rule. You throw away the math you most want to test and reintroduce the flakiness you should have removed — doubling what you own and trusting what you don't." },
  ]}
/>

<Quiz
  id="01-tdd-property-choice-3"
  lessonSlug="01-tdd-property"
  lang="en"
  question="A money formatter has 94% coverage and eight months of green CI, then double-charges a customer on a value ending in .005. Why did every example test miss it, and what would have caught it?"
  choices={[
    { label: "Example tests only exercise inputs a human imagined, so the suite's blind spots are the author's. A round-trip property — parse(format(x)) === x over generated decimals — attacks boundary values like 0.005 and shrinks the failure to a minimal counterexample.", correct: true },
    { label: "The 6% of uncovered lines is where the bug lived — push coverage to 100%.", misconception: "The bug lived in covered lines: every line ran with round numbers. Bugs hide in untried inputs through fully-covered code, which is exactly what coverage cannot see." },
    { label: "Add more round-number example tests until one happens to hit it.", misconception: "More examples in the same style re-test the cases already imagined. The gap is defined by what nobody thought to type, so only generated input over an invariant closes it." },
    { label: "Switch the suite from example tests to mutation testing.", misconception: "Mutation testing attacks the output side — missing assertions — not the input side. The .005 gap is an un-enumerated input, which is precisely what property generation covers." },
  ]}
/>

<Quiz
  id="01-tdd-property-choice-4"
  lessonSlug="01-tdd-property"
  lang="en"
  question="A senior insists on full TDD for a two-day spike to evaluate an unfamiliar vendor API and decide whether to sign. Why is this the wrong call?"
  choices={[
    { label: "TDD's return scales with spec stability and code lifetime; a spike is low on both. You pay the 15–22% tax to freeze guesses about a response shape you don't understand yet, on code whose job is to be thrown away. Spike first, then rebuild the keeper with TDD.", correct: true },
    { label: "TDD is always correct discipline; the team simply needs to write the tests faster.", misconception: "Speed is not the issue. The spec is unknowable up front, so test-first specifies behavior you're about to learn is wrong — the discipline is being applied to the one situation it punishes." },
    { label: "The vendor API can't be tested at all, so tests are pointless here.", misconception: "It can be tested once you know its real shape. The point isn't untestability — it's that pinning a spec before you understand it is wasted front-loading on disposable code." },
    { label: "Write live integration tests against the vendor and iterate on those.", misconception: "Live tests against an unfamiliar third party are slow, flaky, and rate-limited, and you still don't know the correct behavior to assert. You'd fight test infrastructure instead of answering the sign/don't-sign question." },
  ]}
/>

<Quiz
  id="01-tdd-property-choice-5"
  lessonSlug="01-tdd-property"
  lang="en"
  question="A billing module shows 100% line coverage but a 67% mutation score, with a surviving '>' → '>=' boundary mutant. How do you read this?"
  choices={[
    { label: "Coverage proves the line ran; the survivor proves no test fed the boundary value where '>' and '>=' differ. It's a named missing assertion — add a test at the exact boundary, then work the survivor list on critical code.", correct: true },
    { label: "The numbers contradict each other; the coverage tool is miscounting.", misconception: "They measure different things — execution versus detection. A line can run in every test while no assertion would fail on a wrong result, so 100% coverage and a 67% mutation score are fully consistent." },
    { label: "Coverage is already 100%, so the suite is done — ignore the mutation score.", misconception: "That discards the one metric that just proved the gate is hollow. Coverage can't see the missing boundary assertion; turning off the honest metric to preserve the misleading one is backwards." },
    { label: "Push the mutation score to 100% across the whole repo before shipping.", misconception: "Equivalent mutants make sub-100% expected and full-repo runs take hours. The goal is killing meaningful survivors on critical modules, not a flawless number everywhere." },
  ]}
/>

<Quiz
  id="01-tdd-property-choice-6"
  lessonSlug="01-tdd-property"
  lang="en"
  question="Across the unit, what is the through-line that distinguishes a test suite that is an asset from one that is a liability?"
  choices={[
    { label: "Assertions are coupled to observable behavior, not internal structure — so the suite breaks exactly when behavior breaks, survives refactors, and earns the team's trust to act on a red.", correct: true },
    { label: "The suite has the highest possible line coverage on every module.", misconception: "Coverage measures execution, not detection — you can hit 100% and still miss a third of injected bugs. A trustworthy suite is defined by what it asserts, not how many lines it runs." },
    { label: "Every collaborator is mocked so each unit is perfectly isolated.", misconception: "Over-mocking pins tests to call structure: they break on harmless refactors and stay green on real bugs because stubbed results are never checked. Isolation for its own sake manufactures fragility." },
    { label: "Test-induced design damage is accepted as the price of testability.", misconception: "Indirection that serves the test rather than the user is damage, not a virtue. The skill is heeding pressure that reveals a real coupling smell and refusing ceremony added solely to satisfy a test." },
  ]}
/>

<Recap lang="en">
The unit's spine is one judgment loop. Test-first is design pressure — a hard-to-construct object is the design review, not an obstacle to mock around. The boundary rule tames doubles: mock what you can't run, use real objects for code you own, assert state. Example tests cover only inputs you imagined, so properties generate the rest and shrink failures to a minimal counterexample. TDD's tax (15–22%) pays off in proportion to spec stability and code lifetime — spike the unknown, then rebuild. And mutation score is the honest metric: coverage proves a line ran, a survived mutant proves a test would have missed the bug. Through all of it, couple to behavior, never to structure.
</Recap>
