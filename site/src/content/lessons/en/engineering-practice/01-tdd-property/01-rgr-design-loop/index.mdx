---
concepts:
- red-green-refactor
- test-first-design
- behavior-vs-implementation
- refactor-step
- fragile-tests
- arrange-act-assert
deepensInto:
- 02-test-doubles-london-detroit
estMin: 15
lang: en
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-rgr-design-loop
sources:
- https://martinfowler.com/bliki/TestDrivenDevelopment.html
- http://xunitpatterns.com/Fragile%20Test.html
- https://enterprisecraftsmanship.com/posts/tdd-not-tdd/
- https://arxiv.org/pdf/1711.05082
spiral:
- feedback-loops
- design-pressure
status: ready
summary: Writing the test first is design pressure, not coverage hygiene. You call
  your own API before it exists, so a bad interface shows up in seconds instead of
  after fifteen call sites. The payoff lives in the refactor step teams skip under
  deadline.
title: Red-green-refactor is a design loop, not a testing ritual
track: engineering-practice
unit: 01-tdd-property
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Priya is asked to add a "cancel order" feature. She writes the test first and immediately hits a wall: to assert the outcome she has to construct an `OrderService` that needs a database connection, a payment gateway, an email client, and a feature-flag service — four collaborators just to test one boolean rule. The test is impossible to write cleanly. Her teammate would have noticed this only after wiring `cancel()` into a controller, two jobs, and an admin script. The unwritable test is the design review: `OrderService` is doing too much, and TDD said so before a single line of production code existed.
</Hook>

<Crux>Writing the test first makes you the first consumer of your own API — a bad interface fails the test before it has call sites.</Crux>

<Explanation>

## The first step is design pressure, not coverage

The loop is deliberately tiny: write the smallest failing test (**red**), write the least code that makes it pass (**green**), then improve the code without changing behavior (**refactor**). The value most people miss lives entirely in the *red* step. Writing the test before the implementation forces you to call your own code from the outside before it exists — you choose the function's name, its arguments, its return shape, and its failure mode while you are still a *consumer*, not an author. Code that is awkward to call is awkward to test first, so a bad interface surfaces in seconds instead of after it has been wired into fifteen call sites that all now have to change.

This reframes the whole practice. People argue about TDD as if the question were "does writing tests first find more bugs?" That misses it. The mechanism is feedback latency on *design*: a hard-to-construct object, a method that needs six mocks to exercise, a function whose return value you can't assert without reaching into internals — these are design smells, and test-first surfaces them at the cheapest possible moment, before the design has metastasized into the codebase. The arXiv comparative case study found the design effect was the consistent signal across teams; the bug-count effect was noisier.

## Skipping refactor is how the loop rots

The most common failure is stopping at green. Under deadline pressure the ritual collapses to "test passes, ship it," and the third step — refactor — quietly disappears. That is the step where duplication gets removed and tangled names get fixed *while the tests still guard behavior*. Skip it for a quarter and you have a green CI sitting on top of a codebase nobody wants to touch. Green is not health. The tests passing tells you the behavior is preserved; it tells you nothing about whether the code is still shaped like something a human can change.

There is a real cost here, and pretending otherwise is dishonest. Industry data puts test-first at roughly **15–22% more upfront time** than test-after on the same feature. The trade is front-loaded design feedback and a regression net against later rework — which is why TDD's return is highest on long-lived, frequently-changed code and weakest on throwaway scripts. We resolve that tension fully in lesson 04; for now the point is that the refactor step is where most of the long-term payoff actually lands, and it is the first thing teams cut.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Loop step</th>
        <th class="border border-border px-3 py-2 text-left">What you actually do</th>
        <th class="border border-border px-3 py-2 text-left">What it buys you</th>
        <th class="border border-border px-3 py-2 text-left">Cost of skipping it</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Red</td>
        <td class="border border-border px-3 py-2">Write the smallest failing test, calling your API from outside</td>
        <td class="border border-border px-3 py-2">Design feedback before code exists; a bad interface fails here</td>
        <td class="border border-border px-3 py-2">You discover the bad API after 15 call sites depend on it</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Green</td>
        <td class="border border-border px-3 py-2">Write the least code that passes — even something crude</td>
        <td class="border border-border px-3 py-2">Forces you to confront the real requirement, not a guess</td>
        <td class="border border-border px-3 py-2">Over-engineering for cases no test demands</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Refactor</td>
        <td class="border border-border px-3 py-2">Clean names and duplication while tests stay green</td>
        <td class="border border-border px-3 py-2">Most of the long-term payoff; safe restructuring</td>
        <td class="border border-border px-3 py-2">Green CI over rotting code nobody will touch</td>
      </tr>
    </tbody>
  </table>
</div>

## Couple to behavior, or the suite becomes a liability

The quiet killer is *what* you assert. A test coupled to **behavior** — "a refund for a fully-shipped order is rejected" — survives any refactor that preserves the outcome, so it protects you while you improve the code. A test coupled to **implementation** — "`RefundService` calls `inventoryClient.check()` exactly once, then `ledger.post()`" — breaks the instant you reorganize internals, even when nothing the customer sees has changed. The team learns that red is usually noise, and starts ignoring it. That is precisely how a suite slides from asset to liability: the fragile-test anti-pattern documented in xUnit Test Patterns.

The mirror failure is just as dangerous: an implementation-coupled test can stay *green* while the real behavior is broken, because it only verifies that some internal dance happened, not that the right outcome was produced. Assert through the public surface, assert observable results, and mock only true external boundaries. A suite that breaks exactly when behavior breaks — and only then — is the one developers trust enough to act on.

<Inset kind="why" lang="en">
"Test first" sounds like a discipline about correctness, but its real product is a *usable interface*. When you are forced to consume your own API before writing it, you feel the friction your callers will feel — the four constructor dependencies, the method that can't be called without a live database, the return value you can't inspect. Those frictions are design defects, and the test is the first place they become concrete and cheap to fix. You are not writing a test; you are running a design review with yourself as the harshest user.
</Inset>

<TradeoffMatrix
  id="01-rgr-design-loop-tradeoff-1"
  lessonSlug="01-rgr-design-loop"
  lang="en"
  prompt="You write a test first and find you need to construct four collaborators (DB, payment, email, flags) just to exercise one cancel-order rule. What is the test telling you?"
  options={[
    { name: "The object has too many responsibilities — extract the cancel rule so it can be tested in isolation", summary: "Listen to the design smell.", correct: true, justification: "An object that needs four collaborators to exercise one rule is doing four things. The hard-to-write test is the design review TDD exists to give you: it surfaces the over-broad responsibility before cancel() has call sites. Extracting the rule into a focused unit makes both the test and the production code simpler — that friction is the signal, not an obstacle to route around." },
    { name: "Wire up four mocks so the test compiles, then move on", summary: "Mock past the smell.", justification: "You can make the test pass this way, but you have silenced the exact signal TDD gave you. A test that needs four mocks is testing a god object; the mocks paper over the design problem and lock the bad shape in. The friction was the point — mocking it away throws away the feedback." },
    { name: "Skip the test for this one — it's clearly too hard to test", summary: "Abandon test-first.", justification: "Hard-to-test almost always means hard-to-change. Declaring the code untestable and moving on ships the god object untouched and unguarded. The difficulty is information about the design, not a reason to drop the discipline that surfaced it." },
    { name: "Write an end-to-end test that drives the whole stack instead", summary: "Escalate to E2E.", justification: "An E2E test can confirm cancel works through the full stack, but it is slow, flaky, and gives zero design feedback on the unit. It also doesn't fix the responsibility problem — you've just moved the assertion far away from the rule and made failures harder to localize." },
  ]}
/>

<Quiz
  id="01-rgr-design-loop-quiz-1"
  lessonSlug="01-rgr-design-loop"
  lang="en"
  question="What is the primary thing the 'red' step of red-green-refactor buys a senior engineer?"
  choices={[
    { label: "Design feedback — you consume your own API before it exists, so a bad interface fails the test before it gains call sites", correct: true },
    { label: "Higher line coverage, which is the main goal of writing tests first", misconception: "Coverage is a side effect, not the goal. Test-first's distinctive value is forcing you to use the interface as a consumer before authoring it, which surfaces design problems — coverage you could also get by writing tests after." },
    { label: "Proof that the feature has no bugs once the test goes green", misconception: "A single passing example proves one case, not the absence of bugs. The red step's value is design feedback; bug-finding is limited and is exactly why properties and mutation testing exist later in this unit." },
    { label: "A guarantee the code won't need refactoring later", misconception: "It guarantees the opposite expectation — refactor is the explicit third step. Test-first doesn't remove the need to restructure; it makes restructuring safe by leaving a behavior net in place." },
  ]}
/>

<Quiz
  id="01-rgr-design-loop-quiz-2"
  lessonSlug="01-rgr-design-loop"
  lang="en"
  question="A test asserts RefundService calls inventoryClient.check() once then ledger.post() once. Why is this the fragile-test anti-pattern?"
  choices={[
    { label: "It's coupled to implementation detail — it breaks on harmless refactors and can stay green while the refund outcome is actually wrong", correct: true },
    { label: "It's an ideal behavior test because it verifies the exact internal steps", misconception: "Internal call order is implementation, not behavior. The customer cares that a fully-shipped order's refund is rejected, not which methods fired in which sequence — asserting the sequence couples you to internals." },
    { label: "It's fine as long as the mock expectations are configured correctly", misconception: "Correct mocks don't fix the root problem: the test asserts how the code works internally, so it breaks on any refactor and never confirms the actual customer-visible outcome." },
    { label: "It only fails to find bugs in statically typed languages", misconception: "The fragility is independent of typing. An implementation-coupled test can break on refactor and stay green on real breakage in any language; types don't change what the test is asserting." },
  ]}
/>

<DragOrder
  id="01-rgr-design-loop-drag-1"
  lessonSlug="01-rgr-design-loop"
  lang="en"
  prompt="Order the red-green-refactor loop the way a senior runs it for a new rule:"
  items={[
    { id: "a", label: "Red: write the smallest failing test, calling the not-yet-built API from outside" },
    { id: "b", label: "Feel the friction — if it's hard to construct, fix the design before continuing" },
    { id: "c", label: "Green: write the least code that makes the test pass" },
    { id: "d", label: "Refactor: clean names and remove duplication while tests stay green" },
    { id: "e", label: "Assert behavior, not internals, so the test survives the next refactor" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="TDD design loop: red, green, refactor cycle"
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    {id:"red",label:"red",sub:"write failing test"},
    {id:"green",label:"green",sub:"least code to pass"},
    {id:"refactor",label:"refactor",sub:"clean, tests guard"}
  ]}
  edges={[
    {from:"red",to:"green",label:"implement"},
    {from:"green",to:"refactor",label:"pass"},
    {from:"refactor",to:"red",label:"next rule"}
  ]}
  caption="Three-step cycle: write the smallest failing test (red), write the least code to pass (green), then clean without changing behavior (refactor) — and loop back to red for the next rule."
/>

<KeyTakeaway>Test-first is design pressure: you consume your own API before it exists, so a bad interface fails the test before it has call sites to protect. The payoff is in the refactor step teams cut under deadline — and only if you assert behavior, never internals.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-rgr-design-loop-retrieval"
  lang="en"
  questions={[
    { q: "A colleague says 'TDD is just about getting coverage up — I'll write the tests after, same result.' What's the senior counter?", a: "Coverage you can get either way; the distinctive thing test-first gives you is design feedback, and you only get it before the code exists. When you write the test first you become the first consumer of your own API: you choose the name, the arguments, the return shape, and the failure mode while you're still on the outside. A bad interface — an object you can't construct without four collaborators, a method you can't call without a live database, a return value you can't assert without reaching into internals — fails the test immediately, before it has fifteen call sites that would all have to change. Write the test after and the design is already set; the test just ratifies whatever shape you happened to build. The arXiv comparative case study found the design effect was the consistent signal across teams while bug-count effects were noisy. So the answer is: test-after gives you the same coverage and none of the design pressure, which is the whole point of doing it first." },
    { q: "Why does 'assert behavior, not implementation' decide whether a suite is an asset or a liability?", a: "A behavior-coupled test — 'a refund for a fully-shipped order is rejected' — keeps passing through any refactor that preserves the outcome, so it protects you while you improve the code. An implementation-coupled test — 'RefundService calls inventoryClient.check() then ledger.post()' — fails every time you reorganize internals even though nothing the customer sees changed, so the team learns that red is usually noise and starts ignoring failures. That's how a suite becomes a liability: it cries wolf on harmless changes, which is the fragile-test anti-pattern from xUnit Test Patterns. The mirror failure is just as bad — an implementation test can stay green while the real behavior is broken, because it only checks that some internal dance happened, not that the right outcome was produced. The fix is to test through the public surface, assert observable results, and mock only true external boundaries, so the suite breaks exactly when behavior breaks and only then." },
  ]}
/>

<Recap lang="en">
Red-green-refactor is a design loop, not a coverage ritual. The red step is the load-bearing one: writing the test first makes you the first consumer of your own API, so a bad interface — an object that needs four collaborators, a method that can't run without a database — fails the test in seconds instead of after fifteen call sites depend on it. That is design feedback at the cheapest possible moment, and the comparative case study found it the most consistent effect of TDD. The green step forces you to confront the real requirement; the refactor step is where most of the long-term payoff lands, because it removes duplication and fixes names while the tests still guard behavior — and it is the first step teams cut under deadline, leaving green CI over code nobody will touch. Test-first costs roughly 15–22% more upfront, which is why its return is highest on long-lived code and weakest on throwaway work. Above all, couple tests to behavior, never to implementation: implementation-coupled tests break on harmless refactors and can stay green while the real outcome is broken, training the team to ignore red. Assert observable outcomes through the public surface and the suite breaks exactly when behavior breaks.
</Recap>
