---
concepts:
- test-doubles
- mockist-vs-classicist
- london-detroit-schools
- over-mocking
- interaction-vs-state
- test-boundaries
deepensInto:
- 03-property-based-testing
estMin: 16
lang: en
lessonType: topic
level: middle
order: 2
prereqs:
- 01-rgr-design-loop
slug: 02-test-doubles-london-detroit
sources:
- https://martinfowler.com/articles/mocksArentStubs.html
- https://medium.com/@adrianbooth/test-driven-development-wars-detroit-vs-london-classicist-vs-mockist-9956c78ae95f
- http://xunitpatterns.com/Test%20Double.html
- https://martinfowler.com/bliki/UnitTest.html
spiral:
- feedback-loops
- design-pressure
status: ready
summary: 'Mockist (London) tests assert interactions; classicist (Detroit) tests assert
  state with real collaborators. Mock the boundaries — network, clock, payment — and
  use real objects inside. Over-mocking couples tests to call structure: they break
  on refactor and stay green on bugs.'
title: 'Test doubles: London vs Detroit, and the over-mocking trap'
track: engineering-practice
unit: 01-tdd-property
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A team mocks everything. Every collaborator in every unit test is a mock with `expect(...).toHaveBeenCalledWith(...)`. Their suite is 2,400 tests, all green, 91% coverage. Then a refactor that splits one service into two — pure internal reorganization, identical behavior — turns **600 tests red** in a single afternoon. None of them caught a bug; they caught the fact that the internal calls moved. Worse, a month earlier a real pricing bug shipped to production with the suite fully green, because the mocks returned canned values and nobody had asserted the actual computed total. The suite was measuring the wrong thing the whole time.
</Hook>

<Crux>Mock boundaries you can't run — network, clock, payment; use real objects inside. Mocking collaborators you own couples tests to structure.</Crux>

<Explanation>

## Five kinds of double, and the one that bites

"Mock" gets used as a catch-all, but xUnit Test Patterns names five distinct **test doubles**, and the distinction decides whether your test is robust or brittle. A **dummy** is a placeholder passed but never used. A **stub** returns canned answers to calls — it feeds state in. A **fake** is a working lightweight implementation (an in-memory repository standing in for Postgres). A **spy** records how it was called so you can inspect it after. A **mock** is pre-programmed with *expectations*: it asserts that specific calls happened, in a specific way, and fails the test if they didn't.

The first four feed inputs or observe outputs; the **mock** is the one that asserts *interactions*, and that is where brittleness comes from. A mock bakes the production code's call structure into the test's pass/fail condition. The moment you change *how* the code achieves a result — even with identical observable behavior — the mock's expectations no longer match and the test fails. Stubs and fakes don't do this; they just supply data and let you assert the final state. Reaching for a mock when a stub would do is the single most common way teams manufacture fragile tests.

## London (mockist) vs Detroit (classicist)

The two schools are a real, named disagreement. The **London / mockist** school works outside-in: a unit is one class, you mock all its collaborators, and you test by asserting the *interactions* between objects — the right messages were sent in the right order. The **Detroit / classicist** (also Chicago) school works inside-out: a unit is a behavior that may span several real objects, you use real collaborators wherever you can, mock only what you must, and assert *final state* rather than calls.

The practical consequence is what survives a refactor. Classicist tests assert outcomes, so they afford ruthless refactoring — you can restructure internals freely and the test stays green as long as the result is right. Mockist tests assert call structure, so they pin the implementation: they catch design intent precisely but break whenever the internal collaboration changes. London's strength is fast outside-in design feedback and tiny isolated units; its failure mode is exactly the 600-red-tests afternoon — a suite so coupled to structure that refactoring becomes prohibitively expensive.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Aspect</th>
        <th class="border border-border px-3 py-2 text-left">London / mockist</th>
        <th class="border border-border px-3 py-2 text-left">Detroit / classicist</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Unit = </td>
        <td class="border border-border px-3 py-2">A single class</td>
        <td class="border border-border px-3 py-2">A behavior across real collaborators</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Collaborators</td>
        <td class="border border-border px-3 py-2">Mocked</td>
        <td class="border border-border px-3 py-2">Real wherever possible</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Asserts</td>
        <td class="border border-border px-3 py-2">Interactions (which calls happened)</td>
        <td class="border border-border px-3 py-2">Final state / output</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Survives refactor?</td>
        <td class="border border-border px-3 py-2">Often breaks — pinned to call structure</td>
        <td class="border border-border px-3 py-2">Yes — only breaks on behavior change</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Failure mode</td>
        <td class="border border-border px-3 py-2">Brittle suite, costly refactors</td>
        <td class="border border-border px-3 py-2">Harder to localize a failure</td>
      </tr>
    </tbody>
  </table>
</div>

## The boundary rule resolves it

The senior synthesis isn't "pick a school." It's a boundary rule: **mock what you can't run, use real objects for what you can.** Mock the things that are slow, nondeterministic, or have side effects you can't undo — the network, the clock, the payment processor, the email sender, the third-party API. For collaborators you *own* and that run fast and pure, use the real thing or a fake; asserting final state over real internal objects gives you a test that breaks only when behavior breaks. This is mostly the classicist position with London's discipline applied at the system edges, and it dissolves the over-mocking trap: you never mock a value object or a pure helper, because there is nothing to isolate from.

The decisive question for any double is: *if I refactor internals without changing behavior, should this test fail?* If the answer is no, you are using a mock where a stub or a real object belongs. Over-mocking is when that answer is "yes" for tests of code you control — and it is precisely what turns a green suite into 600 red tests that caught no bug, while the real pricing error slipped through because the mocks returned canned numbers no one ever checked against a real computation.

<Inset kind="why" lang="en">
The reason over-mocking feels productive is that it makes every test fast, isolated, and trivially deterministic — you control every input. But that control is the trap: a test where you stub every collaborator is, in the limit, a test that the code calls the methods you told it to call. It can't catch an integration bug between two real objects, and it can't catch a wrong result if you stubbed the result. The mocks turn the test into a mirror of the implementation, so it reflects every structural change as a failure and reflects none of the behavioral truth. Real collaborators are slower and harder to set up, but they are the only way the test can disagree with the code.
</Inset>

<TradeoffMatrix
  id="02-test-doubles-london-detroit-tradeoff-1"
  lessonSlug="02-test-doubles-london-detroit"
  lang="en"
  prompt="You're testing a PriceCalculator that uses a TaxRule object (pure, owned by you) and a CurrencyApi (third-party HTTP). How should you double each?"
  options={[
    { name: "Use the real TaxRule, stub/fake the CurrencyApi, assert the final computed price", summary: "Boundary rule.", correct: true, justification: "TaxRule is pure code you own and runs fast — use it for real so the test actually exercises the tax math and survives refactors. CurrencyApi is a network boundary that's slow and nondeterministic — stub it with a fixed rate. Then assert the final computed price (state), not which methods fired. The test fails only when the price is wrong, which is exactly the behavior you care about." },
    { name: "Mock both TaxRule and CurrencyApi, assert each was called with the right arguments", summary: "Mock everything.", justification: "Mocking TaxRule is over-mocking: it's pure code you own, so there's nothing to isolate, and asserting its calls pins the test to PriceCalculator's internal structure. Refactor how the calc composes tax and the test goes red with no bug. Worse, stubbing the tax result means you never verify the actual math — a real pricing bug can ship green." },
    { name: "Use real objects for both, including a live call to CurrencyApi", summary: "No doubles.", justification: "TaxRule real is correct, but a live CurrencyApi makes the test slow, flaky, and dependent on a third party's uptime and on a rate that changes — the test can fail for reasons unrelated to your code. Network boundaries are exactly what you must double; this loses determinism for no benefit." },
    { name: "Stub TaxRule to return a fixed tax, use the real CurrencyApi", summary: "Backwards.", justification: "This is the boundary rule inverted. Stubbing the pure tax rule throws away the math you most want to test, while running the real network call reintroduces the flakiness and slowness you should have eliminated. You've doubled the thing you own and trusted the thing you don't." },
  ]}
/>

<Quiz
  id="02-test-doubles-london-detroit-quiz-1"
  lessonSlug="02-test-doubles-london-detroit"
  lang="en"
  question="What distinguishes a mock from a stub, and why does it matter for brittleness?"
  choices={[
    { label: "A mock asserts that specific interactions happened (and fails if not); a stub just returns canned data — so mocks pin the test to the code's call structure", correct: true },
    { label: "A mock is faster than a stub because it's pre-compiled", misconception: "Speed isn't the distinction. The difference is behavioral: a stub feeds inputs, a mock asserts that calls occurred a certain way — and that assertion on calls is what couples the test to implementation." },
    { label: "They're interchangeable terms for the same thing", misconception: "xUnit Test Patterns names five distinct doubles. Conflating mock and stub is exactly how teams reach for interaction assertions when state assertions would be robust." },
    { label: "A stub fails the test on unexpected calls; a mock ignores them", misconception: "It's the reverse. A mock has pre-programmed expectations and fails on the wrong calls; a stub simply returns its canned answer regardless of how it's called." },
  ]}
/>

<Quiz
  id="02-test-doubles-london-detroit-quiz-2"
  lessonSlug="02-test-doubles-london-detroit"
  lang="en"
  question="A pure-refactor split of one service into two turns 600 mock-heavy tests red, none catching a bug. What's the root cause?"
  choices={[
    { label: "The tests assert interactions, so they're pinned to call structure and break when internal collaboration changes even though behavior didn't", correct: true },
    { label: "The refactor must have introduced a behavior change the tests correctly caught", misconception: "The premise says behavior is identical and none caught a bug. The reds are structural — the mocks expected calls that now happen differently after the split, not a real regression." },
    { label: "The tests had too little coverage to be reliable", misconception: "Coverage was high (91%); that's not the issue. The problem is the kind of assertion: interaction-based mocks break on refactor regardless of how much code they cover." },
    { label: "Mocks are always wrong and should never be used", misconception: "Mocks are correct at true boundaries (network, clock, payment). The failure here is over-mocking collaborators the team owns, not mocking per se." },
  ]}
/>

<DragOrder
  id="02-test-doubles-london-detroit-drag-1"
  lessonSlug="02-test-doubles-london-detroit"
  lang="en"
  prompt="Order how a green-but-useless over-mocked suite degrades:"
  items={[
    { id: "a", label: "Every collaborator is mocked, including pure objects you own" },
    { id: "b", label: "Tests assert which calls happened, not the final result" },
    { id: "c", label: "Stubbed return values mean the real computation is never checked" },
    { id: "d", label: "A real bug ships to prod with the suite fully green" },
    { id: "e", label: "A behavior-preserving refactor turns hundreds of tests red for no bug" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="London mocks interactions; Detroit uses real collaborators and asserts state"
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    {id:"sut",label:"SUT",sub:"code under test"},
    {id:"mock",label:"mock",sub:"London: interactions"},
    {id:"real",label:"real collab",sub:"Detroit: state"},
    {id:"interact",label:"assert calls",sub:"pinned to structure"},
    {id:"state",label:"assert state",sub:"survives refactor"},
    {id:"boundary",label:"boundary rule",sub:"mock what can't run"}
  ]}
  edges={[
    {from:"sut",to:"mock",label:"London"},
    {from:"sut",to:"real",label:"Detroit"},
    {from:"mock",to:"interact"},
    {from:"real",to:"state"},
    {from:"interact",to:"boundary"},
    {from:"state",to:"boundary"}
  ]}
  caption="London (mockist) mocks all collaborators and asserts interactions — pinned to call structure. Detroit (classicist) uses real collaborators and asserts final state — survives refactors. The boundary rule synthesizes both: mock the network/clock/payment boundary, use real objects for code you own."
/>

<KeyTakeaway>Mocks assert interactions; stubs and fakes feed state. The boundary rule resolves London vs Detroit: mock what you can't run (network, clock, payment), use real objects for code you own, and assert final state. Over-mocking pins tests to call structure — they break on refactor and stay green on real bugs.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-test-doubles-london-detroit-retrieval"
  lang="en"
  questions={[
    { q: "Explain London vs Detroit TDD and how the boundary rule resolves the disagreement.", a: "London (mockist) works outside-in: a unit is a single class, you mock all its collaborators, and you assert interactions — that the right messages were sent. Detroit/Chicago (classicist) works inside-out: a unit is a behavior that may span several real objects, you use real collaborators wherever you can, mock only what you must, and assert final state. The practical difference is refactor survival: classicist tests assert outcomes so they afford ruthless refactoring, while mockist tests assert call structure so they pin the implementation and break when internal collaboration changes. The senior synthesis isn't picking a school — it's a boundary rule: mock what you can't run (slow, nondeterministic, irreversible side effects — network, clock, payment, email, third-party APIs) and use real objects or fakes for fast, pure code you own. The decisive test for any double is: if I refactor internals without changing behavior, should this test fail? If no, you're using a mock where a stub or real object belongs. That's mostly classicist with London's discipline applied at the system edges, and it dissolves over-mocking." },
    { q: "How can an over-mocked suite be 91% green and still let a real bug ship while breaking on a no-op refactor?", a: "Because mocks make the test a mirror of the implementation rather than a check on behavior. When every collaborator is stubbed, the test only verifies that the code called the methods you told it to call — it can't catch a wrong result if you stubbed the result, and it can't catch an integration bug between two real objects because there are no real objects. So a pricing bug ships green: the mocks returned canned totals and nobody asserted the actual computed value against a real calculation. At the same time, the mocks bake the call structure into the pass condition, so a behavior-preserving refactor — splitting one service into two — changes which calls happen and turns hundreds of tests red without a single bug among them. The fix is the boundary rule: use real objects for code you own and assert final state, so the suite can actually disagree with the code and breaks only when behavior breaks." },
  ]}
/>

<Recap lang="en">
"Mock" is a catch-all, but the five test doubles differ in ways that decide robustness: dummies, stubs, fakes, and spies feed inputs or observe outputs, while a mock asserts that specific interactions happened — and that assertion on calls is the source of brittleness. The London (mockist) school treats a unit as one class, mocks every collaborator, and asserts interactions; the Detroit/Chicago (classicist) school treats a unit as a behavior across real objects, mocks only what it must, and asserts final state. Classicist tests survive refactors because they check outcomes; mockist tests pin the implementation and break when collaboration changes, which is how a behavior-preserving refactor turns 600 tests red with no bug among them while a real pricing error ships green behind canned stub values. The senior resolution is the boundary rule: mock what you can't run — network, clock, payment, email, third-party APIs — and use real objects or fakes for fast, pure code you own, asserting final state. The decisive question for any double is whether a behavior-preserving refactor should make this test fail; if it shouldn't, you've reached for a mock where a stub or a real object belongs.
</Recap>
