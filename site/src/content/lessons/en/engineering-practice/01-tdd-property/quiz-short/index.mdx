---
concepts:
- design-pressure
- boundary-rule
- property-shapes
- spec-stability
- mutation-score
estMin: 14
lang: en
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://martinfowler.com/articles/mocksArentStubs.html
- https://hypothesis.works/articles/quickcheck-in-every-language/
- https://stryker-mutator.io/docs/mutation-testing-elements/mutant-states-and-metrics/
status: ready
summary: "Free-recall prompts across the TDD unit. Answer each in your own words first, then reveal the model answer and compare."
title: 'TDD: free-recall review'
track: engineering-practice
unit: 01-tdd-property
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory before you open the model answer — the effort of recall is what makes the material stick.
</Hook>

<Goal lang="en">
Reconstruct the unit's core arguments — test-first as design pressure, the boundary rule for doubles, the four property shapes, the spec-stability tradeoff, and why mutation score is the honest metric — without looking back at the lessons.
</Goal>

<RetrievalDrawer
  client:load
  id="01-tdd-property-short-retrieval"
  lang="en"
  questions={[
    { q: "A colleague says 'TDD is just about getting coverage up — I'll write the tests after, same result.' What's the senior counter?", a: "Coverage you can get either way; the distinctive thing test-first gives you is design feedback, and you only get it before the code exists. Writing the test first makes you the first consumer of your own API — you choose the name, the arguments, the return shape, and the failure mode while still on the outside. A bad interface (an object you can't construct without four collaborators, a method you can't call without a live database, a return you can't assert without reaching into internals) fails the test immediately, before it has fifteen call sites that would all have to change. Write the test after and the design is already set; the test just ratifies whatever shape you built. The comparative case study found the design effect was the consistent signal across teams while bug-count effects were noisy. So test-after gives the same coverage and none of the design pressure — which is the whole point of doing it first." },
    { q: "State the boundary rule for test doubles and the decisive question behind it.", a: "Mock what you can't run; use real objects for code you own. Double the things that are slow, nondeterministic, or have irreversible side effects — network, clock, payment, email, third-party APIs — with a stub or fake, and use the real object (or a fake) for fast, pure collaborators you control, asserting final state rather than calls. This resolves London (mockist) vs Detroit (classicist): it's mostly the classicist position with London's discipline applied at the system edges. The decisive question for any double is: if I refactor internals without changing behavior, should this test fail? If the answer is no, you're using a mock where a stub or a real object belongs. Over-mocking is when that answer is 'yes' for code you own — which is how a behavior-preserving refactor turns 600 tests red with no bug, while a real pricing error ships green behind canned stub values." },
    { q: "Name the four property shapes and give the situation each fits.", a: "Round-trip: decode(encode(x)) === x — any serialize/parse, compress/decompress, save/load pair, and the shape that catches the .005 money-formatter bug. Invariant: a fact that always holds after the operation — sort(xs).length === xs.length, output is ordered, a total is non-negative. Oracle: a trusted reference agrees with the new code — a rewrite versus the old function, the fast path versus the slow path; the highest-leverage shape for any rewrite because the proven code defines correctness. Metamorphic: a relation between two related runs — search(q) results are a superset of search(q AND x). A weak property like 'result is a number' looks thorough but tests almost nothing; stating a useful invariant is itself a design act. The framework generates hundreds of inputs to attack the property, then shrinks any failure to a minimal counterexample and prints a seed so it replays deterministically." },
    { q: "Why does property testing find bugs an example suite with 94% coverage can't, and what makes generated tests trustworthy in CI?", a: "Coverage measures which lines ran, not which inputs you tried, so an example suite only exercises the cases a human imagined — its blind spots are exactly your blind spots. You can run every line with round numbers and never hit the value that breaks, the way the money formatter stayed green eight months because the half-even/half-up disagreement only appeared on values ending in .005 that nobody typed. A property asserts an invariant over generated input, so the framework throws boundary values like 0.005 and 2.675 at it within a handful of runs. The risk is flakiness: a property that secretly depends on a clock, an unseeded random, or external state can pass 999 runs and fail the 1000th. The discipline that defuses it is twofold — keep the property pure and let the framework own all generation, and pin the printed seed the moment a flake appears so the failure is reproducible rather than a coin toss. A property test you can't reproduce on demand isn't a test; it's a rumor." },
    { q: "What two variables govern whether TDD pays off, and how does that distinguish design pressure from test-induced design damage?", a: "Spec stability — can you state correct behavior before writing the code — and code lifetime — will it live for months or be deleted next week. High on both (billing, auth, a long-lived domain core) is peak payoff: design feedback now, a regression net forever. Low on both (an exploratory spike, a vendor probe, a one-off script) and the 15–22% front-loaded cost buys certainty you don't have about disposable code — so you spike first to convert unknowns into a knowable spec, throw the spike away, then rebuild the keeper with TDD. The same force produces design pressure or design damage. Pressure is valuable when a hard-to-write test surfaces a real coupling problem (lesson 01's god object). Damage is when the drive to make code unit-testable adds indirection that serves only the test — an extra layer, an interface and a mock, a cohesive thing split apart for isolation. The tell: does fixing what the test complains about make the code better for a reader who never sees the test? If yes it's pressure; if the change exists only to satisfy the test it's damage." },
    { q: "How can a module have 100% line coverage and a 67% mutation score, and how should a senior adopt mutation testing given its costs?", a: "Coverage asks 'did a test run this line?'; mutation score asks 'would a test fail if this line were wrong?' They come apart constantly — the tool mutates 'amount > limit' to 'amount >= limit', the line still runs in every test, but no test fed the exact boundary value where the operators differ, so the mutant survives. The 67% is the honest measurement of how much covered code the suite would fail to protect; the 100% measured the wrong thing. Adoption: treat the survived-mutant list, not the score, as the product — each survivor is a named missing assertion ('>' → '>=' means add a boundary test, '&&' → '||' means a condition is under-tested, a deleted statement means a side effect is never asserted). Manage the two costs deliberately: runtime, since the suite reruns once per mutant and full sweeps take hours, so scope it to changed files in CI (Stryker and PIT both do diff-based runs) or run nightly on critical modules; and equivalent mutants, behaviorally identical mutations no test can kill, which make a perfect score unattainable and must be triaged by hand. The goal is killing meaningful survivors on critical code — money, permissions, state transitions — not a flawless number everywhere." },
  ]}
/>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the unit's spine: test-first is design pressure because you consume your own API first; the boundary rule (mock what you can't run, real objects for what you own, assert state) tames doubles and the over-mocking trap; the four property shapes — round-trip, invariant, oracle, metamorphic — find the inputs your examples missed, with shrinking and seeds making failures debuggable; TDD's 15–22% tax pays off in proportion to spec stability and code lifetime, so spike the unknown and tell pressure from damage; and mutation score is the honest metric because, unlike coverage, it can't be satisfied by execution without assertion.
</Recap>
