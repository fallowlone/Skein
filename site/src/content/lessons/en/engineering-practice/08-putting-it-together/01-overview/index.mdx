---
slug: 01-overview
lang: en
track: engineering-practice
unit: 08-putting-it-together
order: 1
level: junior
lessonType: topic
status: ready
title: "Putting it together: practices are one feedback loop, not a checklist"
summary: "TDD, contract tests, review, trunk-based dev, flags, on-call, and postmortems are not independent rituals — they form one loop where each enables the next. Adopt one without its dependencies and it backfires. Optimize the whole loop with DORA, not one ceremony."
prereqs: []
deepensInto: []
spiral: []
concepts: ["delivery-feedback-loop", "dora-metrics", "trunk-based-development", "feature-flag-debt", "blameless-postmortem", "practice-dependencies"]
estMin: 17
sources:
  - https://getdx.com/blog/2024-dora-report/
  - https://docs.getunleash.io/guides/trunk-based-development
  - https://docs.devcycle.com/best-practices/tech-debt/
  - https://www.atlassian.com/continuous-delivery/continuous-integration/trunk-based-development
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
A team reads the DORA report, picks the headline practice — "elite teams deploy on every commit to trunk" — and mandates it on Monday. By Wednesday the trunk is red half the day: there is no fast test suite to gate merges, so every push lands broken work on top of broken work. The "fix" is a Friday afternoon hotfix behind a feature flag. Six months later there are 340 flags, none cleaned up, and a stale one fires in prod during Black Friday. They adopted the ceremony and skipped everything that made it safe.
</Hook>

<Crux>Engineering practices are one feedback loop, not a checklist — adopt one without its dependencies and it backfires.</Crux>

<Explanation>

## The practices form a loop, not a list

The earlier lessons each looked like a standalone discipline: TDD and property tests, contract testing, code review, trunk-based development, feature flags, on-call, blameless postmortems. The senior insight is that they are not seven independent boxes to tick. They are one closed feedback system, and the order matters because each link feeds the next.

Tests and contract checks produce a CI signal fast and trustworthy enough that merging to trunk many times a day is safe. Safe frequent merging *is* trunk-based development. Trunk-based development plus feature flags lets you deploy unfinished or risky code dark, decoupling deploy from release. Frequent small deploys mean that whatever still slips through is a tiny blast radius, caught by on-call. On-call feeds the incident into a blameless postmortem, whose action items become new tests, new contract assertions, or a new flag default — closing the loop back at the start. Pull one link out and the chain does not just weaken; the downstream practice starts actively hurting you.

## The seam failures: one practice without its dependencies

The failures are not subtle, and they are predictable from the dependency graph. Each practice has a precondition; skip the precondition and the practice inverts from asset to liability.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Practice adopted</th>
        <th class="border border-border px-3 py-2 text-left">Required dependency</th>
        <th class="border border-border px-3 py-2 text-left">Failure if skipped</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Trunk-based development</td>
        <td class="border border-border px-3 py-2">Fast, trustworthy CI (tests + contracts)</td>
        <td class="border border-border px-3 py-2">Broken trunk: everyone integrates on top of red, builds stall daily</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Feature flags</td>
        <td class="border border-border px-3 py-2">A cleanup/lifecycle discipline (TTL)</td>
        <td class="border border-border px-3 py-2">Flag debt: hundreds of dead toggles, combinatorial test matrix, a stale flag fires in prod</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Frequent deploys</td>
        <td class="border border-border px-3 py-2">On-call + fast rollback/flag-kill</td>
        <td class="border border-border px-3 py-2">High deploy frequency with no recovery path → high MTTR, every deploy a gamble</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Postmortems</td>
        <td class="border border-border px-3 py-2">Owned, tracked action items</td>
        <td class="border border-border px-3 py-2">Theater: a doc nobody acts on, the same outage recurs next quarter</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Code review</td>
        <td class="border border-border px-3 py-2">Small batches (from trunk-based + CI)</td>
        <td class="border border-border px-3 py-2">2000-line PRs get a rubber-stamp `LGTM`; review catches nothing</td>
      </tr>
    </tbody>
  </table>
</div>

Flag debt is the most quietly expensive. A flag is supposed to be short-lived: Unleash models a lifecycle of `initial → pre-live → live → completed → archived`, where `completed` means "rollout done, code removal pending." Skip the removal step and toggles accumulate. Every live flag doubles a path through the code, so N flags imply up to 2^N reachable states — a test matrix you can never fully cover. The classic outage is a "release toggle" treated as a permanent "permission toggle": it never gets removed, sits forgotten for a year, and a config change flips it during peak traffic.

## Optimize the loop, not the ceremony

The trap is local optimization: a team proudly reports "we deploy 40 times a day" while change-fail rate quietly climbs to 30%. DORA exists to stop exactly this — it measures the *whole loop* with four metrics that must move together, not one in isolation:

- **Lead time for changes** — commit to running in production.
- **Deployment frequency** — how often you ship to prod.
- **Change failure rate** — share of deploys that cause a degraded service or need remediation.
- **MTTR / failed-deployment recovery time** — how fast you restore service after a bad change.

The 2024 DORA report's spread between elite and low performers is the argument: elites deploy roughly **182x more often**, with lead times about **127x faster**, recover from incidents about **2,293x faster**, and have change-fail rates about **8x lower**. Notice these are not in tension — speed and stability rise together, because the loop that makes you fast (small batches, strong CI, flags, fast rollback) is the same loop that makes you safe. The 2024 report even saw a "high" cluster whose CFR was *worse* than a slower-but-careful "medium" cluster — proof that chasing deploy frequency alone, with no stability metric to govern it, regresses the system.

<Inset kind="why" lang="en">
Speed and stability being a tradeoff is the most stubborn myth here. The data says the opposite: the same capabilities that shrink lead time — tiny batches, trustworthy automated tests, deploy/release decoupling via flags — also shrink change-fail rate and MTTR, because small changes are easier to review, easier to reason about, and trivial to roll back. A team that is fast *and* unstable has not over-optimized speed; it has skipped the dependencies (CI, flags, recovery) that make speed safe.
</Inset>

## Reading the loop in an incident

Walk a real incident through the loop and the dependencies become concrete. A bad change ships. Because batches are small (trunk-based + CI), the change is one PR, not fifty, so the on-call engineer bisects to it in minutes, not hours — that is low MTTR. Because the risky path was behind a flag, recovery is flipping the flag off, not a code rollback and redeploy. The blameless postmortem asks not "who pushed it" but "which link let it through": Was the CI gap a missing contract test? Then the action item is a contract assertion. Was it an untested flag combination? Then the action item is a flag-cleanup policy plus a default. Each answer feeds a specific upstream practice. A postmortem with no owned action item is the one seam that silently breaks the whole loop, because it is the link that turns an incident into a permanent improvement.

<TradeoffMatrix
  id="08-putting-it-together-tradeoff-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="Leadership wants 'elite DORA numbers in one quarter.' A team currently deploys weekly with a slow flaky test suite and no flags. What's the senior sequencing?"
  options={[
    { name: "First make CI fast and trustworthy, then move to trunk-based + flags, then raise deploy frequency", summary: "Dependencies first.", correct: true, justification: "The loop has an order: a fast, reliable test+contract signal is the precondition for safe frequent merging (trunk-based), which with flags lets you decouple deploy from release and raise frequency without raising change-fail rate. Build the gate before you open the floodgates. Each link is only safe once the link before it exists, which is exactly why elite teams show speed AND stability rising together." },
    { name: "Mandate daily deploys to trunk immediately; the pressure will force the team to fix CI", summary: "Speed-first forcing function.", justification: "This is the Hook's failure. Pushing many times a day onto a trunk with no trustworthy CI means integrating broken work on top of broken work — a red trunk and stalled builds. You cannot use deploy frequency as a forcing function for the dependency that was supposed to make deploys safe; you just spike change-fail rate and MTTR." },
    { name: "Add feature flags everywhere first so risky code can ship, then worry about tests later", summary: "Flags as a substitute for CI.", justification: "Flags decouple deploy from release; they do not validate correctness. Shipping risky code behind flags with no CI just hides bugs behind toggles, and with no cleanup discipline you get flag debt — a combinatorial state space and a stale flag waiting to fire in prod. Flags are a release tool, not a quality gate." },
    { name: "Optimize one metric — deploy frequency — and report it; the others will follow on their own", summary: "Single-metric chase.", justification: "Local optimization is the exact anti-pattern DORA warns about. Driving deploy frequency in isolation, with no change-fail-rate or MTTR governor, produces the 2024 'high-but-unstable' cluster: lots of deploys, worse failure rate. The four metrics are a balanced set; you optimize the loop, not one ceremony." },
  ]}
/>

<Quiz
  id="08-putting-it-together-quiz-1"
  lessonSlug="01-overview"
  lang="en"
  question="A team adopts trunk-based development but still has a 25-minute, frequently-flaky test suite. What is the predictable outcome?"
  choices={[
    { label: "A frequently broken trunk — people merge on top of red because the CI signal is too slow and untrusted to gate merges", correct: true },
    { label: "Faster, safer delivery, because trunk-based development is good on its own", misconception: "Trunk-based development is only safe when fast, trustworthy CI gates each merge. Without that dependency it produces a broken trunk, not faster delivery." },
    { label: "Lower change-fail rate automatically", misconception: "Change-fail rate falls from small batches plus a reliable gate. A flaky, slow suite is not a reliable gate, so failures still slip to trunk." },
    { label: "Nothing changes; the test suite is unrelated to branching strategy", misconception: "CI quality is the precondition for trunk-based development. The branching strategy depends directly on the test signal being fast and trustworthy." },
  ]}
/>

<Quiz
  id="08-putting-it-together-quiz-2"
  lessonSlug="01-overview"
  lang="en"
  question="Your team deploys 40x/day and brags about it, but change-fail rate is 28% and MTTR is creeping up. What does DORA say you've done wrong?"
  choices={[
    { label: "Optimized one metric (deploy frequency) in isolation instead of the whole loop — the four metrics must move together", correct: true },
    { label: "Deployed too often — you should slow down to be stable again", misconception: "Speed and stability are not a tradeoff in the data; elite teams have both. Slowing down treats the symptom, not the missing dependencies (CI, flags, fast recovery) that make frequency safe." },
    { label: "Picked the wrong headline metric; deploy frequency doesn't matter", misconception: "Deploy frequency is one of the four valid DORA metrics. The error is reporting it alone, with no stability metric (CFR, MTTR) to govern it." },
    { label: "Nothing — high deploy frequency is the goal regardless of failure rate", misconception: "The 2024 report's 'high-but-unstable' cluster is exactly this failure: lots of deploys, worse CFR. Frequency without a stability governor regresses the system." },
  ]}
/>

<DragOrder
  id="08-putting-it-together-drag-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="Order the feedback loop so each link enables the next:"
  items={[
    { id: "a", label: "Tests + contract checks give a fast, trustworthy CI signal" },
    { id: "b", label: "Trustworthy CI makes merging to trunk many times a day safe (trunk-based)" },
    { id: "c", label: "Trunk-based + feature flags decouple deploy from release; small risky changes ship dark" },
    { id: "d", label: "Small frequent deploys mean what slips through has a tiny blast radius, caught by on-call" },
    { id: "e", label: "Blameless postmortem turns the incident into action items: new tests, contracts, flag defaults — closing the loop" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Practices are one feedback loop: CI enables trunk-based, flags enable safe deploy, on-call catches escapes, postmortems feed fixes back. Skip a dependency and the practice backfires. Optimize the whole loop with DORA, not one ceremony.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="08-putting-it-together-retrieval"
  lang="en"
  questions={[
    { q: "Explain why mandating trunk-based development before fixing CI predictably backfires, and what the correct sequencing is.", a: "Trunk-based development means everyone merges to one shared branch many times a day. The only thing that makes that safe is a fast, trustworthy CI signal — tests and contract checks that catch breakage before it lands. If the suite is slow or flaky, the signal is too weak to gate merges, so people merge anyway and integrate broken work on top of broken work: a trunk that is red half the day and stalled builds for everyone. You have skipped the dependency that the practice rests on. The correct sequencing follows the loop's order: first make CI fast and trustworthy, then adopt trunk-based development on top of that gate, then add feature flags so you can decouple deploy from release, and only then raise deploy frequency. Build the gate before you open the floodgates — each link is only safe once the link before it exists." },
    { q: "Why does DORA insist on four metrics moving together, and what goes wrong when a team optimizes deploy frequency alone?", a: "DORA's four metrics — lead time, deployment frequency, change-fail rate, and MTTR — are a balanced set that describes the whole delivery loop: two for throughput, two for stability. The 2024 report shows elite teams winning on all four at once (roughly 182x more deploys, 127x faster lead time, 2,293x faster recovery, 8x lower change-fail rate), because the same capabilities that make you fast — small batches, strong automated tests, deploy/release decoupling via flags, fast rollback — also make you safe. When a team optimizes deploy frequency in isolation, it removes the stability governor: it ships 40 times a day while change-fail rate climbs to 28% and MTTR creeps up. The 2024 report literally found a 'high-but-unstable' cluster whose change-fail rate was worse than a slower, careful cluster. That is the proof that you optimize the loop, not one ceremony — a single metric driven alone regresses the system." },
  ]}
/>

<Recap lang="en">
The seven practices of this track are not a checklist of independent rituals — they are one closed feedback loop, and the order is load-bearing. Tests and contract checks produce a CI signal trustworthy enough that merging to trunk many times a day is safe; trunk-based development plus feature flags decouples deploy from release; small frequent deploys keep the blast radius of any escape tiny, where on-call catches it; and a blameless postmortem with owned action items turns that incident into new tests, contracts, and flag defaults, closing the loop. Pull a dependency out and the downstream practice inverts: trunk-based without CI gives a broken trunk, flags without cleanup give flag debt and a stale toggle firing in prod, frequent deploys without a recovery path give high MTTR, postmortems without action items give repeat outages. The capstone discipline is to stop optimizing single ceremonies and instead optimize the whole loop, governed by the four DORA metrics together — because the data is unambiguous that speed and stability rise as one when the loop is intact, and regress together when a link is missing.
</Recap>
