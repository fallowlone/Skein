---
concepts:
- delivery-feedback-loop
- dora-metrics
- trunk-based-development
- feature-flag-debt
- blameless-postmortem
- ci-as-gate
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://getdx.com/blog/2024-dora-report/
- https://docs.getunleash.io/guides/trunk-based-development
- https://www.atlassian.com/continuous-delivery/continuous-integration/trunk-based-development
status: ready
summary: "Multiple-choice synthesis across the engineering-practice track — how CI, trunk-based dev, flags, review, on-call, and postmortems form one loop where each link enables the next."
title: 'Putting it together: multiple-choice review'
track: engineering-practice
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole track. Each one is a sequencing or dependency decision a tech lead actually makes — not a definition to recite, but which link of the delivery loop is load-bearing and what happens when you pull it out.
</Hook>

<Goal lang="en">
Confirm you can connect TDD, contract testing, code review, trunk-based development, feature flags, on-call, and postmortems into a single feedback loop — and predict the seam failure when a practice is adopted without its dependency.
</Goal>

<Quiz
  id="08-putting-it-together-choice-1"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="Leadership mandates daily deploys to trunk on Monday. The team has a 25-minute, flaky test suite and no feature flags. What is the predictable outcome, and what was the missing dependency?"
  choices={[
    { label: "A red trunk half the day. Frequent merging is only safe on top of a fast, trustworthy CI signal; without that gate, people integrate broken work on top of broken work. The missing dependency is reliable tests + contract checks, which must come before trunk-based development.", correct: true },
    { label: "Faster, safer delivery — trunk-based development reduces conflicts on its own.", misconception: "Trunk-based development keeps conflicts small only because integration is frequent AND each merge is gated by a trustworthy build. A slow flaky suite is no gate, so frequency just multiplies broken integrations." },
    { label: "Nothing changes — branching strategy is independent of test quality.", misconception: "CI quality is the precondition for trunk-based development. The branching strategy depends directly on the signal being fast and trustworthy enough to gate every merge." },
    { label: "Lower change-fail rate automatically, since more deploys means more practice.", misconception: "Change-fail rate falls from small batches plus a reliable gate. Without the gate, failures keep slipping to trunk no matter how often you deploy." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-2"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="A team proudly deploys 40x/day, but change-fail rate is 28% and MTTR is creeping up. In DORA terms, what have they done wrong?"
  choices={[
    { label: "Optimized one throughput metric (deployment frequency) in isolation, with no stability governor. DORA's four metrics are a balanced set — two throughput, two stability — and must move together; the 2024 report found a 'high-but-unstable' cluster whose change-fail rate was worse than a slower, careful one.", correct: true },
    { label: "Deployed too often — they should slow down to regain stability.", misconception: "Speed and stability are not a tradeoff in the data; elite teams win on all four metrics at once. Slowing down treats the symptom, not the missing dependencies (CI, flags, fast recovery) that make frequency safe." },
    { label: "Picked the wrong headline metric — deploy frequency does not matter.", misconception: "Deployment frequency is a valid DORA metric. The error is reporting it alone, with no change-fail-rate or MTTR metric to govern it." },
    { label: "Nothing — high deploy frequency is the goal regardless of failure rate.", misconception: "Frequency without a stability metric regresses the system; that is precisely the failure DORA's balanced set is designed to expose." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-3"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="A service has 340 feature flags, none removed in a year. A config change flips a forgotten 'release toggle' during peak traffic and takes down checkout. Which link of the loop was actually missing?"
  choices={[
    { label: "A flag lifecycle / cleanup discipline. A flag is meant to be short-lived (initial → live → completed → archived); skipping removal turns a release toggle into a permanent landmine, and N live flags imply up to 2^N reachable states no test matrix covers.", correct: true },
    { label: "Feature flags themselves were the mistake; the team should ship code directly.", misconception: "Flags are what let you decouple deploy from release and kill bad code instantly — the Knight Capital lesson. The defect is missing TTL/cleanup, not the existence of flags." },
    { label: "On-call should have caught the toggle before it fired.", misconception: "On-call catches the incident after the blast; it cannot prevent a stale toggle that should have been removed months earlier. The prevention lives in flag lifecycle, an owned postmortem action item." },
    { label: "The config system is the root cause and should be the only fix.", misconception: "Blameless analysis looks for contributing factors, not one root cause. The deep contributing factor is flag debt: a release toggle treated as permanent, with no removal step." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-4"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="An on-call engineer bisects a checkout outage to a single PR in four minutes and recovers by flipping a flag off — no rollback build. Which two upstream practices made that fast recovery possible?"
  choices={[
    { label: "Small batches from trunk-based development + CI made the change one reviewable PR, so bisection was minutes not hours; and shipping the risky path behind a feature flag made recovery a toggle, not a code rollback and redeploy. Low MTTR is downstream of those two links.", correct: true },
    { label: "A large, infrequent release train and a manual change-approval board.", misconception: "Big infrequent releases make blast radius large and bisection slow — the opposite of what enabled the four-minute recovery. The lever was small batches plus a kill-switch." },
    { label: "Faster hardware and more on-call staff.", misconception: "MTTR here was set by batch size and the flag kill-switch, not raw compute or headcount. The same incident on a giant release would still take hours to bisect." },
    { label: "A longer manual QA phase before deploy.", misconception: "More upfront QA lengthens lead time and does nothing for recovery speed once a change is live. Fast recovery comes from small batches and the ability to flip a flag." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-5"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="The same outage recurs three quarters running, each time with a fresh postmortem doc on the wiki. Which single seam of the loop is broken?"
  choices={[
    { label: "Postmortems without owned, tracked action items. A blameless postmortem only closes the loop when its findings become a new test, a contract assertion, or a flag default that someone owns and ships; a doc nobody acts on is theater, and the same incident returns.", correct: true },
    { label: "The team needs to assign individual blame so people are more careful.", misconception: "Blame makes people hide incidents, so you lose the learning. Recurrence comes from action items never shipping, not from insufficient blame." },
    { label: "They are writing too many postmortems; fewer would help.", misconception: "The count of docs is not the problem — their lack of follow-through is. One postmortem with an owned, shipped action item beats three that nobody acts on." },
    { label: "On-call rotations are too short.", misconception: "Rotation length does not determine whether a fix ships. The broken link is the absence of tracked action items feeding back into tests, contracts, and flag defaults." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-6"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="A 20-service org runs full end-to-end integration tests on every merge; the suite is slow and flaky, so engineers mute failures and bugs reach prod. What is the senior fix that keeps the delivery loop intact?"
  choices={[
    { label: "Replace most cross-service e2e with consumer-driven contract tests at each boundary, keeping a thin e2e smoke layer. Contracts are fast and deterministic, so CI stays a trustworthy gate — the precondition that makes trunk-based merging and frequent deploys safe.", correct: true },
    { label: "Run the full e2e suite nightly instead of per-merge and ship on green from the morning.", misconception: "Deferring e2e to nightly removes the gate from the merge entirely; broken work lands on trunk all day and you discover it the next morning. The loop needs a fast per-merge signal, which contracts provide." },
    { label: "Keep the e2e suite and just add retries until it passes.", misconception: "Retrying flaky e2e hides real failures behind green and trains the team to ignore the gate — exactly the muting behaviour that let bugs through. The fix is a faster, deterministic signal, not more retries." },
    { label: "Drop integration testing and rely on feature flags to catch issues in prod.", misconception: "Flags decouple deploy from release; they do not validate correctness. Shipping unvalidated code behind flags just hides bugs and grows flag debt. Contracts validate the boundary that unit mocks fake away." },
  ]}
/>

<Recap lang="en">
The through-line across the whole track is one closed loop with a load-bearing order: tests and contract checks make CI a trustworthy gate; that gate makes trunk-based merging safe; trunk-based plus feature flags decouples deploy from release; small frequent deploys keep any escape's blast radius tiny, where on-call catches it; and a blameless postmortem with owned action items turns the incident into new tests, contracts, and flag defaults — closing the loop. Pull a link out and the downstream practice inverts. And you govern the whole thing with DORA's four metrics together, never one ceremony in isolation. Now when you see a team's incident debrief produce no owned action items, or a CI step labeled "allowed to fail," you know exactly which link to name — and which conversation to start.
</Recap>
