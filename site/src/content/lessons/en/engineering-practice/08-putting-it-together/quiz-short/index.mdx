---
concepts:
- delivery-feedback-loop
- dora-metrics
- contract-vs-e2e
- feature-flag-debt
- blameless-postmortem
- review-batch-size
estMin: 14
lang: en
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://getdx.com/blog/2024-dora-report/
- https://www.atlassian.com/continuous-delivery/continuous-integration/trunk-based-development
- https://docs.getunleash.io/guides/trunk-based-development
status: ready
summary: "Free-recall prompts spanning the whole engineering-practice track. Answer each in your own words first, then reveal the model answer and compare."
title: 'Putting it together: free-recall review'
track: engineering-practice
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, reconstruct a full answer from memory before opening the model answer — and make sure your answer connects the practice to the link before and after it in the loop, not just to itself.
</Hook>

<Goal lang="en">
Reconstruct the spine of the whole track — why the practices form one loop, the order that makes each link safe, and the seam failure when a dependency is skipped — without looking back at the lessons.
</Goal>

<RetrievalDrawer
  client:load
  id="08-putting-it-together-short-retrieval"
  lang="en"
  questions={[
    { q: "Explain why the seven practices of this track are one feedback loop rather than a checklist of independent rituals, and trace the order of the links.", a: "They are one closed loop because each practice produces the precondition the next one needs. Tests and contract checks produce a fast, trustworthy CI signal; that signal is what makes merging to trunk many times a day safe, which is trunk-based development. Trunk-based development plus feature flags lets you ship unfinished or risky code dark, decoupling deploy from release. Small frequent deploys mean any escape has a tiny blast radius, which on-call catches quickly. On-call feeds the incident into a blameless postmortem, whose owned action items become new tests, contract assertions, or flag defaults — closing the loop back at the start. The order is load-bearing: each link is only safe once the link before it exists. Pull one link out and the downstream practice does not just weaken, it inverts from asset to liability." },
    { q: "Pick three practices and state, for each, the dependency it rests on and the failure mode if that dependency is skipped.", a: "Trunk-based development rests on fast, trustworthy CI; skip it and you get a red trunk where everyone integrates broken work on top of broken work. Feature flags rest on a cleanup/lifecycle discipline (a TTL); skip it and you get flag debt — hundreds of dead toggles, a combinatorial 2^N test matrix, and a stale release toggle that fires in prod during peak. Code review rests on small batches (which come from trunk-based + CI); skip it and 2000-line PRs get a rubber-stamp LGTM because defect detection collapses past a few hundred lines. (Postmortems rest on owned, tracked action items; skip them and the same outage recurs. Frequent deploys rest on on-call plus fast rollback/flag-kill; skip them and you get high MTTR.) Each precondition is what keeps the practice an asset instead of a liability." },
    { q: "Why does DORA insist on its four metrics moving together, and what specifically goes wrong when a team optimizes deployment frequency alone?", a: "DORA's four metrics — lead time, deployment frequency, change-fail rate, and MTTR — are a balanced set: two for throughput, two for stability. They are designed to be read together because the same capabilities that make you fast (small batches, strong automated tests, deploy/release decoupling via flags, fast rollback) also make you safe, so elite teams win on all four at once rather than trading one for another. When a team optimizes deploy frequency in isolation it removes the stability governor: it ships 40 times a day while change-fail rate climbs to ~28% and MTTR creeps up. The 2024 DORA report even found a 'high-but-unstable' cluster whose change-fail rate was worse than a slower, careful one. That is the proof you optimize the whole loop, not one ceremony — a single metric driven alone regresses the system." },
    { q: "A 20-service org's full end-to-end suite is slow and flaky, so engineers mute it and bugs reach prod. Diagnose the failure in loop terms and give the senior fix.", a: "The broken link is the CI gate. The merge signal is the precondition for safe trunk-based merging and frequent deploys; when it is slow and flaky, engineers stop trusting it and mute failures, so the gate stops gating and broken work flows to trunk and prod. End-to-end tests grow roughly N-squared with services and are flaky on any hop, so they are the wrong tool to put on the merge gate. The senior fix is to replace most cross-service e2e with consumer-driven contract tests at each boundary — fast and deterministic — keeping only a thin e2e smoke layer for one critical path on the gate, and pushing the full e2e suite to an async/nightly lane. That restores a trustworthy, fast gate, which is what the rest of the loop depends on." },
    { q: "Why is a feature flag a release tool rather than a quality gate, and what is the most quietly expensive way flags break the loop?", a: "A flag decouples deploy from release: it lets you ship code dark, roll out gradually, and kill a bad path instantly. But it does not validate correctness — shipping unvalidated code behind a flag just hides bugs behind a toggle instead of catching them. So flags complement CI; they never substitute for it. The most quietly expensive failure is flag debt. A flag is meant to be short-lived (Unleash models initial → live → completed → archived, where 'completed' means rollout done, code removal pending). Skip the removal step and toggles accumulate; every live flag doubles a path through the code, so N flags imply up to 2^N reachable states that no test matrix can cover. The classic outage is a release toggle treated as a permanent permission toggle — forgotten for a year, then flipped by a config change during peak traffic." },
    { q: "Walk a single bad deploy through the intact loop and name which link gives each property: fast detection, fast recovery, and no recurrence.", a: "A bad change ships. Because batches are small (trunk-based + CI), the change is one PR not fifty, so the on-call engineer bisects to it in minutes — that fast detection comes from small batches plus on-call. Because the risky path was behind a feature flag, recovery is flipping the flag off rather than a code rollback and redeploy — that fast recovery (low MTTR) comes from flags plus on-call's runbook. Then the blameless postmortem asks not 'who pushed it' but 'which link let it through': a missing contract test becomes a new contract assertion, an untested flag combination becomes a flag-cleanup policy and a default. Those owned, tracked action items feed back upstream into CI and flag lifecycle, which is what gives no recurrence — the postmortem with owned action items is the link that turns an incident into a permanent improvement." },
  ]}
/>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the spine of the whole track: the practices are one closed loop with a load-bearing order, each link rests on a specific dependency whose absence inverts the practice into a liability, DORA's four metrics govern the loop together rather than one ceremony in isolation, contracts beat brittle e2e at the merge gate, flags are a release tool and not a quality gate, and a postmortem only closes the loop when its action items are owned, tracked, and fed back upstream.
</Recap>
