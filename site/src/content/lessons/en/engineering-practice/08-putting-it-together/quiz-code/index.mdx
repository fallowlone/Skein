---
concepts:
- test-as-design-pressure
- feature-flag-lifecycle
- ci-quality-gate
- postmortem-action-item
- consumer-driven-contract
estMin: 14
lang: en
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://getdx.com/blog/2024-dora-report/
- https://docs.getunleash.io/guides/trunk-based-development
- https://docs.devcycle.com/best-practices/tech-debt/
status: ready
summary: "Read real artifacts from the delivery loop — a test, a feature flag, a CI gate config, and a postmortem action item — and pick the change a senior engineer makes first."
title: 'Putting it together: code and config reading'
track: engineering-practice
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
The loop is not an abstraction — it shows up as a test, a flag, a CI gate, and a postmortem action item. Read each artifact, find which link of the delivery loop it strengthens or breaks, and choose the fix a senior engineer would make first.
</Hook>

<Goal lang="en">
Practise reading the concrete artifacts each practice produces — a test, a feature flag, a pipeline gate, a postmortem item — and judging whether each one actually closes the loop or quietly leaves a seam open.
</Goal>

## Snippet 1 — a flag check on the hot path

```ts
// shipped 14 months ago for the "new-checkout" rollout, never removed
export function priceCart(cart: Cart): Money {
  if (flags.isEnabled("new-checkout-pricing")) {
    return newPricing(cart);   // the rollout "completed" months ago
  }
  return legacyPricing(cart);  // still here, still reachable
}
```

<Quiz
  id="08-putting-it-together-code-1"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="The new-checkout rollout finished over a year ago. What is the senior action on this flag, and why does leaving it cost more than it looks?"
  choices={[
    { label: "Remove the flag and the legacyPricing branch: the rollout is complete, so the toggle is now permanent debt. Every live flag is a branch in prod — N flags imply up to 2^N reachable states — and a forgotten release toggle is exactly what gets flipped during peak and fires the dead path.", correct: true },
    { label: "Leave it — a working flag is harmless and might be needed for a future rollback.", misconception: "A completed flag is not a rollback tool; it is a live branch nobody tests. The lifecycle (initial → live → completed → archived) exists because skipping removal is how a release toggle becomes the next Knight-Capital-style incident." },
    { label: "Convert it to a permanent config setting so ops can toggle pricing.", misconception: "Silently re-purposing a release toggle as a permission toggle is the classic flag-debt trap: a 'temporary' switch becomes load-bearing forever, with no owner and no tests on the legacy branch." },
    { label: "Add a unit test for the legacy branch so it stays covered.", misconception: "Testing a branch you intend to delete is wasted work that legitimizes keeping dead code. The fix is removal, which also removes the test burden — not adding coverage to debt." },
  ]}
/>

## Snippet 2 — a test written first

```ts
// written before any implementation exists
test("refund splits across original tender lines", () => {
  const order = orderWith([cash(30), card(70)]);
  const refund = refundService.refund(order, money(50));
  expect(refund.allocations).toEqual([cash(15), card(35)]);  // proportional
});
```

<Quiz
  id="08-putting-it-together-code-2"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="The team writes this test before refundService exists. What is the primary value of writing it first, in the loop's terms?"
  choices={[
    { label: "It is design pressure: calling refund() and asserting the allocation shape forces the interface to be decided before fifteen call sites depend on it, and it produces the fast, trustworthy signal that later gates trunk merges. Coverage is a by-product, not the point.", correct: true },
    { label: "It guarantees higher code coverage, which is the main reason to write tests first.", misconception: "Coverage is a by-product. Test-first's payoff is design feedback — a bad interface shows up in seconds — plus a reliable gate; chasing coverage as the goal produces brittle tests that assert implementation, not behaviour." },
    { label: "It lets you skip code review because the test proves correctness.", misconception: "Tests catch regressions against a known spec; review catches design and intent the test author never thought to assert. They are different links — a passing test does not replace a reviewer reading the change." },
    { label: "Writing it first only matters for end-to-end tests, not unit tests.", misconception: "The design-pressure benefit is strongest at the unit/API level, where you are shaping the interface. The loop wants the fast unit/contract signal as the merge gate; e2e stays a thin smoke layer." },
  ]}
/>

## Snippet 3 — a CI pipeline gate

```yaml
# .ci/merge-gate.yml — required for merge to trunk
on: pull_request
jobs:
  gate:
    steps:
      - run: make unit            # ~90s, deterministic
      - run: make contract        # consumer + provider verification
      - run: make e2e-smoke       # 1 critical path only
      - run: make e2e-full || true # full suite, allowed to fail
```

<Quiz
  id="08-putting-it-together-code-3"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="This gate is required for merge. What is the highest-leverage fix to the config, and why?"
  choices={[
    { label: "Remove the `|| true` from e2e-full or move it out of the merge gate entirely. A step that is 'allowed to fail' is not a gate — it trains the team to ignore red, which silently breaks the CI signal that makes trunk-based merging safe. Keep the fast deterministic steps (unit, contract, smoke) as the real gate.", correct: true },
    { label: "Add more e2e coverage to the merge gate so nothing slips through.", misconception: "Piling slow flaky e2e onto the merge gate is what makes the signal slow and untrusted, so people mute it — the failure this gate already shows. The loop wants a fast deterministic gate; e2e-full belongs in a nightly or async lane." },
    { label: "Make every step `|| true` so flaky tests never block a merge.", misconception: "That deletes the gate entirely: broken work lands on trunk freely and you lose the precondition for safe frequent merging. The fix is fewer, deterministic, blocking checks — not making everything non-blocking." },
    { label: "Nothing — `|| true` on the slow suite is a reasonable way to keep merges fast.", misconception: "Keeping a step that can never fail in the required gate is theater: it looks like coverage but gates nothing, and engineers learn the suite's red is meaningless. Either it blocks or it leaves the gate." },
  ]}
/>

## Snippet 4 — a postmortem action item

```md
## Action items
- [ ] Improve communication during incidents          owner: ?        due: ?
- [x] Add contract test asserting `cart.total` is non-negative   owner: @ana   due: 2026-06-02   ticket: PAY-1183
- [ ] Be more careful when changing pricing config     owner: team     due: ongoing
```

<Quiz
  id="08-putting-it-together-code-4"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="Reading these three action items from a blameless postmortem, which one actually closes the loop, and why do the other two fail?"
  choices={[
    { label: "Only the contract-test item closes the loop: it has an owner, a due date, a ticket, and it feeds a concrete assertion back into CI so the same defect cannot recur. The other two are unowned, undated, and unverifiable — 'be more careful' is not an action item, it is a wish that lets the incident repeat.", correct: true },
    { label: "All three are valid; soft items like 'be more careful' set the right culture.", misconception: "An action item must be specific, owned, and trackable, or it is theater. 'Be more careful' assigns no work and changes no system, so the outage recurs — the exact seam that breaks the loop." },
    { label: "The 'communication' item is the most important because incidents are mostly a comms problem.", misconception: "It may matter, but with no owner and no due date it ships nothing. An unowned item is indistinguishable from no item; the loop only closes when work is owned and tracked." },
    { label: "None of them matter — the value of a postmortem is the timeline, not the action items.", misconception: "The timeline is for understanding; the owned action items are what turn the incident into a system you fixed. Without them the postmortem is a doc nobody acts on and the same incident returns next quarter." },
  ]}
/>

<Recap lang="en">
Each practice leaves a concrete artifact, and reading it tells you whether the loop is intact: a completed flag still on the hot path is debt to remove, not a rollback tool; a test written first is design pressure and the merge signal, with coverage as a by-product; a CI step that is 'allowed to fail' is not a gate and quietly trains the team to ignore red; and a postmortem item only closes the loop when it is owned, dated, tracked, and feeds a concrete assertion back into CI. Read the artifact, find the link it touches, fix the seam — do not admire the ceremony.
</Recap>
