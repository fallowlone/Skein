---
concepts:
- deploy-release-decoupling
- flag-types
- percentage-rollout
- kill-switch
- flag-debt
- flag-lifecycle
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://specbranch.com/posts/knight-capital/
- https://en.wikipedia.org/wiki/Knight_Capital_Group
- https://docs.getunleash.io/topics/feature-flags/feature-flag-best-practices
- https://launchdarkly.com/docs/guides/flags/technical-debt
spiral: []
status: ready
summary: 'Flags ship code dark and release by toggle, buying gradual rollout and instant
  kill-switch. The senior cost: every flag is a live branch in prod, and a forgotten
  one took $440M from Knight Capital in 45 minutes.'
title: 'Feature flags: decoupling deploy from release without drowning in flag debt'
track: engineering-practice
unit: 05-feature-flags
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
On August 1, 2012, Knight Capital deployed new code to 7 of its 8 trading servers. The new code reused an old flag named `Power Peg` — a 2003 test feature that had been dead for years. On the eighth, unpatched server, flipping that flag woke the dead code. In 45 minutes it fired 4 million erroneous trades across 154 stocks and lost roughly $440 million — more than the firm's market cap. A stale flag, left in the codebase and reused, ended a 1,400-person company.
</Hook>

<Crux>Flags decouple deploy from release: ship code dark, release by toggle — but every live flag is a branch in production you now own.</Crux>

<Explanation>

## Deploy is not release

A senior's first reframe: deploying code and releasing a feature are two different events. Without flags they are welded together — the merge that hits `main` is the moment users see the change, so every risky launch becomes a high-stakes deploy at 2am with the whole team watching. A feature flag splits them. You deploy the code "dark" (present in production, wrapped in `if (flags.newCheckout) {...}` and returning `false` for everyone), then release later by flipping the flag — no rebuild, no redeploy.

When you next plan a risky launch, ask yourself: am I about to weld deploy and release together? If yes, a flag is the lever that separates them.

That split changes how teams ship. Code merges continuously and small (trunk-based development stops depending on long-lived branches), incomplete features sit safely behind an off flag, and the release becomes a config change an SDK picks up in seconds. The Unleash client spec polls flag state on a default 15-second interval; LaunchDarkly streams updates over SSE so a toggle propagates to running servers in well under a second. Release stops being a deploy and becomes a decision.

## The four flag types — and why their lifecycles differ

"Feature flag" hides four distinct kinds, and the senior mistake is treating them all the same. The type dictates how long the flag should live and who owns it.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Type</th>
        <th class="border border-border px-3 py-2 text-left">Purpose</th>
        <th class="border border-border px-3 py-2 text-left">Lifespan</th>
        <th class="border border-border px-3 py-2 text-left">Owner</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>release</code></td>
        <td class="border border-border px-3 py-2">Gate an in-progress feature; gradual rollout</td>
        <td class="border border-border px-3 py-2">Days to weeks — delete after 100%</td>
        <td class="border border-border px-3 py-2">Dev team</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>ops</code> / kill-switch</td>
        <td class="border border-border px-3 py-2">Disable a subsystem under load or incident</td>
        <td class="border border-border px-3 py-2">Permanent — kept on purpose</td>
        <td class="border border-border px-3 py-2">SRE / on-call</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>experiment</code></td>
        <td class="border border-border px-3 py-2">Serve A/B variants, measure a metric</td>
        <td class="border border-border px-3 py-2">One experiment cycle — then delete</td>
        <td class="border border-border px-3 py-2">Product / data</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>permission</code></td>
        <td class="border border-border px-3 py-2">Gate by plan, role, or entitlement</td>
        <td class="border border-border px-3 py-2">Long-lived — tied to the product model</td>
        <td class="border border-border px-3 py-2">Product</td>
      </tr>
    </tbody>
  </table>
</div>

A release flag that never gets deleted has quietly become flag debt. A kill-switch that someone "cleans up" because it looked stale has removed your safety net. Same mechanism, opposite correct fate — which is exactly why the type has to be recorded, not guessed.

## Gradual rollout and the instant rollback

The release flag's superpower is the percentage rollout. Instead of 0% → 100%, you ramp: 1% → 5% → 25% → 100%, watching error rate and latency at each step. If the new path breaks at 5%, you flip the flag off and the blast radius was 5% of traffic, recovered in seconds — no commit revert, no hotfix pipeline, no redeploy. That is the real argument for flags: rollback stops being an engineering event and becomes a config toggle.

The mechanism matters for correctness. A good rollout is *sticky*: the same user must keep getting the same variant across requests, or your UI flickers and your experiment data is garbage. SDKs do this by hashing a stable key (`userId` plus a `groupId`) into a 0–99 bucket; "25% rollout" means buckets 0–24 are on. The hash is deterministic and computed locally, so evaluation is sub-millisecond and needs no network call per check — the SDK holds the whole ruleset in memory and refreshes it in the background.

<Inset kind="why" lang="en">
Why hash locally instead of asking the server per evaluation? At scale a flag is checked thousands of times per request path. A network round-trip per check would add latency and a hard dependency: if the flag service is down, your app is down. Local in-memory evaluation with background sync means a flag check is a hashmap lookup, and a flag-service outage degrades to "last known config" rather than an outage of yours.
</Inset>

## Every flag is a branch in production

Here is the cost a senior weighs against all that velocity. Each live flag is an `if`/`else` that both paths of are running in production simultaneously. Ten independent boolean flags is 2^10 = 1,024 possible runtime configurations — you cannot test them all, and the combination a user actually hits in prod may be one no test ever exercised. Flags multiply the state space of your system. They also rot: a flag left at 100% for months is dead config that still gets evaluated, still clutters the code with a branch nobody reads, and — the Knight Capital lesson — can be *reused*, waking code everyone forgot was there.

This is flag debt, and it is not hypothetical. LaunchDarkly's own guidance defines a stale flag as one serving the same variation to everyone for over ~30 days, and recommends archiving on a schedule; tools like Uber's Piranha exist specifically to AST-parse codebases and auto-generate the pull request that deletes a flag and its dead branch. The discipline is the whole game: a release flag must carry an expiry and a Jira ticket to remove it, kill-switches must be labeled permanent so nobody "cleans" them, and removal is part of the feature's definition of done — not a someday-maybe.

<TradeoffMatrix
  id="05-feature-flags-tradeoff-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="A new checkout path is built and tested in staging. You want to ship it to production today but de-risk the launch. Pick the rollout approach."
  options={[
    { name: "Deploy dark behind a release flag, ramp 1% → 5% → 25% → 100% watching error rate, with the flag as instant kill-switch", summary: "Gradual flagged rollout.", correct: true, justification: "This is the textbook use of a release flag: code ships to production but stays off, then you release to a small slice and widen only while the metrics stay clean. If the new path breaks at 5%, you flip the flag off and the damage was 5% of traffic recovered in seconds — no revert, no redeploy. The flag must be sticky by userId so users don't flap between checkouts, and it must carry an expiry so it gets deleted once at 100%." },
    { name: "Merge to main and deploy to 100% of users at once; you tested it in staging so it should be fine", summary: "Big-bang deploy.", justification: "Staging never reproduces production's traffic mix, data, and concurrency. A big-bang to 100% means any bug hits every user at once, and rollback is a full revert-and-redeploy cycle measured in minutes-to-hours while users suffer. This is exactly the high-stakes deploy that flags exist to eliminate." },
    { name: "Add the flag but leave it on permanently after launch so you can re-enable the old path if needed someday", summary: "Keep the flag forever.", justification: "This converts a release flag into permanent flag debt. The old path is now dead code that still gets evaluated and can be accidentally re-enabled — the Knight Capital failure mode. A release flag's job ends at 100%; delete it and the dead branch. Only ops/kill-switch flags are intentionally permanent." },
    { name: "Use a long-lived feature branch, QA it for two weeks, then merge and deploy when fully done", summary: "Branch-based delay.", justification: "Long-lived branches drift from main, accumulate painful merge conflicts, and still produce a big-bang release at the end. Flags exist precisely so code can merge continuously and small while staying dark — you get trunk-based development and a controlled release instead of a risky merge day." },
  ]}
/>

<Quiz
  id="05-feature-flags-quiz-1"
  lessonSlug="01-overview"
  lang="en"
  question="What does decoupling deploy from release actually mean?"
  choices={[
    { label: "Code is deployed to production turned off behind a flag, and the feature is released later by flipping the flag — no redeploy", correct: true },
    { label: "You deploy to a separate release server that users hit only after launch", misconception: "Decoupling is about the flag, not a separate environment. The same production binary holds the code dark and active; the flag — not a redeploy or a new server — decides exposure." },
    { label: "Releases happen automatically whenever a deploy succeeds", misconception: "That is the opposite — coupling deploy and release. Decoupling means a deploy is silent and the release is a separate, deliberate toggle." },
    { label: "Only QA-approved builds are allowed to deploy to production", misconception: "That is a deploy gate, not deploy/release decoupling. Decoupling is about controlling user-facing exposure of already-deployed code via a runtime toggle." },
  ]}
/>

<Quiz
  id="05-feature-flags-quiz-2"
  lessonSlug="01-overview"
  lang="en"
  question="A release flag has been at 100% rollout for three months and nobody has touched it. What's the senior call?"
  choices={[
    { label: "It's stale flag debt — delete the flag and its now-dead else branch from the code", correct: true },
    { label: "Leave it; it's serving 100% so it's harmless", misconception: "It still gets evaluated, still clutters the branch surface, and — as Knight Capital showed — a forgotten flag can be reused and wake dead code. Stale flags are a liability, not free." },
    { label: "Flip it to a permanent kill-switch just in case", misconception: "Don't silently repurpose a release flag into a permanent one without intent and labeling. A release flag's job ends at 100%; converting it to ops requires a deliberate decision and an owner, not inertia." },
    { label: "Set it back to 50% so you can A/B test the old path again", misconception: "Re-enabling a path users were migrated off three months ago resurrects dead code against stale assumptions — the exact shape of the Knight Capital incident." },
  ]}
/>

<DragOrder
  id="05-feature-flags-drag-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="Order the lifecycle of a release flag from creation to retirement:"
  items={[
    { id: "a", label: "Create the flag, default off; deploy the code dark to production" },
    { id: "b", label: "Release to 1–5% of users, sticky by userId; watch error rate and latency" },
    { id: "c", label: "Widen the rollout to 25% → 100% while metrics stay clean (or kill-switch off if not)" },
    { id: "d", label: "Once stable at 100%, remove the flag and delete the now-dead else branch" },
    { id: "e", label: "Confirm no references remain in code or config; close the cleanup ticket" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="Release Flag: Percentage Rollout Ramp"
  hue="var(--d-systems)"
  perRow={5}
  nodes={[
    {id:"req", label:"Request", sub:"userId + ctx"},
    {id:"eval", label:"Flag eval", sub:"hash bucket"},
    {id:"r1", label:"1% rollout", sub:"buckets 0–0"},
    {id:"r5", label:"5% rollout", sub:"buckets 0–4"},
    {id:"r25", label:"25% rollout", sub:"buckets 0–24"},
    {id:"r100", label:"100% rollout", sub:"all buckets"},
    {id:"new", label:"New variant", sub:"feature ON"},
    {id:"old", label:"Old variant", sub:"feature OFF"},
    {id:"kill", label:"Kill-switch", sub:"flag OFF"},
    {id:"del", label:"Delete flag", sub:"cleanup done"},
  ]}
  edges={[
    {from:"req", to:"eval"},
    {from:"eval", to:"r1", label:"start"},
    {from:"r1", to:"r5", label:"metrics OK"},
    {from:"r5", to:"r25", label:"metrics OK"},
    {from:"r25", to:"r100", label:"metrics OK"},
    {from:"r100", to:"del", label:"stable"},
    {from:"eval", to:"old", label:"bucket OUT"},
    {from:"eval", to:"new", label:"bucket IN"},
    {from:"r1", to:"kill", label:"metrics bad"},
    {from:"r5", to:"kill", label:"metrics bad"},
    {from:"r25", to:"kill", label:"metrics bad"},
  ]}
  caption="Flag evaluation hashes userId into a 0–99 bucket locally; rollout widens the bucket window each step. Any bad metric flips the kill-switch in seconds — no redeploy. At 100%, delete the flag and its dead else-branch."
/>

<KeyTakeaway>Flags split deploy from release: ship dark, ramp by percentage, kill-switch in seconds. The price is a live branch per flag — combinatorial test surface and flag debt. Give release flags an expiry; a stale one is a loaded gun.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-feature-flags-retrieval"
  lang="en"
  questions={[
    { q: "Explain how feature flags decouple deploy from release, and why that changes the way a team ships.", a: "Deploying code and releasing a feature are normally the same event: the merge that hits production is the moment users see the change, so every risky launch is a high-stakes deploy. A feature flag splits the two. You deploy the code dark — it's present in production but wrapped in a flag check that returns false for everyone — and then release later by flipping the flag, with no rebuild or redeploy. SDKs pick the change up fast: Unleash polls on a 15-second default, LaunchDarkly streams over SSE in sub-second. This lets code merge continuously and small (trunk-based development instead of long-lived branches), lets incomplete features sit safely behind an off flag, and turns release into a config decision rather than an engineering event. Rollout becomes a percentage ramp (1% → 5% → 25% → 100%) and rollback becomes flipping the flag off in seconds instead of reverting commits and redeploying." },
    { q: "What is flag debt, why is the Knight Capital incident the canonical example, and what discipline prevents it?", a: "Flag debt is the accumulation of flags that have outlived their purpose but still live in the code. Every flag is an if/else whose both branches run in production, so N flags create 2^N possible configurations — a combinatorial test surface you can't fully cover. A flag left at 100% for months is dead config that still evaluates, clutters the code, and can be reused. Knight Capital is the canonical disaster: in 2012 a deploy reused an old flag named Power Peg — a dead 2003 test feature — and on one un-updated server flipping it woke the dead code, firing 4 million bad trades and losing ~$440 million in 45 minutes, more than the firm's value. The discipline: record each flag's type (release flags are temporary, kill-switches are permanent), give release flags an expiry and a removal ticket, make deletion part of definition-of-done, and treat a stale flag — one serving the same value to everyone for ~30+ days — as a liability to archive on schedule. Tools like Uber's Piranha automate the cleanup PR." },
  ]}
/>

<Recap lang="en">
Feature flags decouple deploy from release: code ships dark to production and a runtime toggle — picked up by the SDK in seconds — decides exposure, so release becomes a decision rather than a high-stakes deploy. That buys gradual percentage rollout (1% → 5% → 25% → 100%, sticky by userId so variants don't flicker) and instant kill-switch rollback, with evaluation done locally as a sub-millisecond hash lookup so a flag-service outage degrades gracefully. But the four flag types — release, ops/kill-switch, experiment, permission — have opposite correct lifespans, and every live flag is a branch in production, so N flags mean 2^N configurations you can't fully test. The failure mode is flag debt: stale or forgotten flags that still evaluate, clutter the code, and can be reused — the exact mechanism that cost Knight Capital ~$440M in 45 minutes in 2012. The senior discipline is lifecycle: type every flag, give release flags an expiry and a removal ticket, label kill-switches permanent so nobody deletes the safety net, and make flag cleanup part of done. Now when you see a flag that has been sitting at 100% for months — or one named after a long-dead feature — you know exactly what to do: open a PR, delete the flag and its dead branch, and close the cleanup ticket.
</Recap>
