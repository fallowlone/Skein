infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label Hydration problem → its fix
    children
      - label Browser-only render (window / locale)
        children
          - label Fix: render null, then swap in useEffect
      - label Lone divergence (Date.now timestamp)
        children
          - label Fix: suppressHydrationWarning / useId
      - label Too much client JS → INP > 200 ms
        children
          - label Fix: server component (ships no JS)
      - label Monolithic hydration blocks a tap
        children
          - label Fix: wrap in a Suspense boundary
theme
  palette #6366f1 #14b8a6 #f59e0b #f43f5e
