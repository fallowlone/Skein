infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label Everything the response reads is in the key
    children
      - label Full Route Cache — keys on route path
        children
          - label Personalized props bake into the shared RSC payload
      - label Data Cache — keys on fetch URL + options
        children
          - label A module-token fetch hides which user it read
      - label unstable_cache — keys on keyParts + arguments
        children
          - label Closure-captured session is invisible to the key
      - label CDN — keys on URL + Vary
        children
          - label Missing Vary or a cached Set-Cookie leaks across users
theme
  palette #6366f1 #14b8a6 #f59e0b #f43f5e
