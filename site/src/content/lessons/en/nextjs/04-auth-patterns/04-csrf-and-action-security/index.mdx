---
concepts:
- nextjs-action-security
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: senior
order: 4
prereqs: []
slug: "04-csrf-and-action-security"
sources:
- https://nextjs.org/docs/app/api-reference/functions/server-actions
- https://nextjs.org/docs/app/building-your-application/authentication
spiral: []
status: ready
summary: 'Every exported server action is a public POST endpoint. Next compares Origin to Host — that stops cross-site forgery and nothing else, and route handlers get no such check. Authentication, per-row authorization, zod validation, and rate limiting belong inside every action.'
title: 'CSRF and action security: every server action is a public POST endpoint'
track: "nextjs"
unit: "04-auth-patterns"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Day two of the pentest, the tester stops clicking the UI and opens the network tab. Every server action in the app is a POST to the current URL with a `Next-Action` header carrying an opaque id — and the ids are right there in the public JS bundle. First attempt: a forged form on an external host posting to the app. Blocked — Next compared the `Origin` header to the `Host` and rejected the mismatch; the framework's CSRF check earns its keep. Second attempt: replay the "rename project" action from the tester's own logged-in session, with the `projectId` argument swapped to another tenant's project. It lands. The action authenticated the caller and never asked whether *this caller* may touch *this row* — unit 02 called this the IDOR pattern, and it survives every framework protection because it is not a forgery: it is a perfectly authentic request doing something it should never have been allowed to do. The report's summary line writes itself: "The framework stopped the attacks aimed at the framework. Nothing stopped the attacks aimed at the application."
</Hook>

<Crux>Every server action is a public POST endpoint. Origin checks stop CSRF only; authorization and validation live inside the action.</Crux>

<Explanation>

## An action is a public POST route with a hash for a name

Strip the RPC illusion: when you export a function from a `'use server'` file, Next.js creates an HTTP endpoint. The client invokes it by POSTing to the page URL with a `Next-Action` header containing the action's id; arguments travel serialized in the body. Three properties follow, each with security weight. **Reachability is not gated by UI**: the action is invocable whether or not any button renders it — "we removed the delete button for non-admins" changes pixels, not the endpoint. **Every exported action from every `'use server'` file is an endpoint**, including the one whose UI you deleted last sprint; Next mitigates with dead-code elimination of unreferenced ids, but an action still referenced anywhere stays live, so treat *exported* as *public*. **Action ids are not secrets** — they are non-deterministic per build, but they sit in the JS bundle every visitor downloads. Security through unguessable ids lasts exactly until someone opens devtools.

One nuance works in your favor: **closed-over values are encrypted**. If an action defined inside a component closes over a variable from the render scope, Next encrypts those values with a per-build key before they round-trip through the client. But arguments — everything the client passes when calling the action — are attacker-controlled plaintext, exactly like a request body, because that is what they are.

## The CSRF protection you get, and its exact edges

Classic CSRF: `evil.com` auto-submits a form to your origin from a victim's browser, riding the victim's cookies. Next.js layers two defenses for actions. First, actions only accept POST — and session cookies set with `SameSite=Lax` (lesson 01) are not sent on cross-site POSTs at all, so on modern browsers the forged request usually arrives anonymous. Second, Next **compares the `Origin` header to the `Host` (`X-Forwarded-Host`) header** on every action invocation and rejects mismatches. This covers gaps the cookie layer leaves — older `SameSite=None` cookies, subtle same-site cases: notably, a forged POST from a sibling subdomain (`status.example.com` → `app.example.com`) is *same-site*, so `SameSite=Lax` cookies ride along — but `Origin` still differs from `Host`, and the action check rejects it.

Now the edges, because they are where incidents live. **Edge one: the check protects actions only.** Your route handlers — `app/api/*/route.ts` — get no automatic CSRF protection whatsoever; a POST route handler relying on cookie auth with `SameSite=None` is plainly forgeable, and even with `Lax` you should treat handler CSRF as your own problem. **Edge two: proxies can break or loosen it.** If your app sits behind a reverse proxy that rewrites `Host`, legitimate requests fail the comparison — and the documented escape hatch, `serverActions.allowedOrigins`, is a list that pentests find stuffed with wildcards added during some 3 a.m. debugging session. Every entry is an origin you have declared trusted to forge requests. **Edge three — the one from the Hook: CSRF protection is about *where the request came from*, not *what it is allowed to do*.** An authenticated user attacking your authorization model sails through every Origin check, because nothing about their request is forged.

<SequenceDiagram
  label="The pentester's two attempts: the cross-site forgery dies at the Origin check, the authenticated IDOR replay sails through it"
  hue="var(--d-ai)"
  actors={["evil.com", "Browser", "Action"]}
  messages={[
    { from: "evil.com", to: "Browser", label: "auto-submit form" },
    { from: "Browser", to: "Action", label: "Origin mismatch" },
    { from: "Action", to: "Browser", label: "rejected — CSRF" },
    { from: "Browser", to: "Action", label: "own session + id swap" },
    { from: "Action", to: "Browser", label: "200 — IDOR lands" },
  ]}
  caption="The framework's Origin/Host comparison stops the forged cross-site POST. The second request is perfectly authentic — the attacker's own session, a real action, a swapped projectId — and only the ownership check inside the action can stop it."
/>

<Quiz
  id="04-csrf-and-action-security-quiz-1"
  lessonSlug="04-csrf-and-action-security"
  lang="en"
  question="An app uses server actions for in-app mutations and a route handler POST /api/export for a legacy integration, with cookie auth. Which CSRF surface does the framework actually cover?"
  choices={[
    { label: "Only the server actions: Next checks Origin against Host on action POSTs, but route handlers get no automatic CSRF check — /api/export's forgery resistance rests entirely on cookie SameSite settings and your own defenses", correct: true },
    { label: "Both, since the Origin/Host comparison applies to every POST that reaches the server", misconception: "The comparison is implemented in the action-invocation path, not as a global POST filter. Route handlers are deliberately raw HTTP — bring-your-own CSRF: token, Origin check, or strict SameSite, but it is yours to bring." },
    { label: "Neither needs anything, because SameSite=Lax cookies already make CSRF impossible", misconception: "Lax closes the classic cross-site POST, but not SameSite=None cookies (set for embeds/iframes), not stale browsers, and not same-site attackers on sibling subdomains — for actions that last gap is exactly what the Origin/Host check covers, and route handlers get none of it." },
    { label: "Both are covered as long as action ids stay secret, since an attacker cannot name the endpoint", misconception: "Action ids ship in the public JS bundle — every visitor has them. They are opaque, not secret, and the route handler has a perfectly guessable URL anyway. Neither surface is protected by obscurity." },
  ]}
/>

## Authorize inside, validate at the boundary, rate-limit the mutation

Everything the framework cannot know — who may do what to which row, what a valid payload looks like, how often a mutation is plausible — must live inside the action, every action, no exceptions for "internal" ones. The discipline compresses to a pipeline, and unit 02's server-actions lesson showed its skeleton; this is the full armored version:

```ts
'use server';
import { z } from 'zod';
import { notFound } from 'next/navigation';
import { revalidatePath } from 'next/cache';
import { verifySession } from '~/lib/dal';
import { ratelimit } from '~/lib/ratelimit';
import { db } from '~/lib/db';

const RenameInput = z.object({
  projectId: z.string().uuid(),
  name: z.string().min(1).max(120),
});

export async function renameProject(raw: unknown) {
  const session = await verifySession();                     // 1. authenticate
  const { success } = await ratelimit.limit(session.userId); // 2. e.g. 20 writes / 10 s
  if (!success) throw new Error('Too many requests');
  const input = RenameInput.parse(raw);                      // 3. the wire is untyped
  const project = await db.project.findUnique({ where: { id: input.projectId } });
  if (project?.ownerId !== session.userId) notFound();       // 4. authorize THIS row
  await db.project.update({ where: { id: input.projectId }, data: { name: input.name } });
  revalidatePath(`/projects/${input.projectId}`);            // 5. tell the caches
}
```

Step 3 deserves its own sentence: the `raw: unknown` type is the honest one. Your TypeScript signature is compile-time fiction — at runtime the body is whatever bytes the caller sent, and `RenameInput.parse` is the moment fiction becomes checked fact. Validation also closes **mass assignment**: an action that spreads `...data` into an update lets a crafted payload set `role: 'admin'` unless the schema allowlists fields. Step 4 is the IDOR check from the Hook — ownership verified against the actual row, not against which buttons rendered. Step 2 acknowledges that mutations are the expensive, abusable surface: a login action without rate limiting is a free credential-stuffing oracle (typical policy: 5 attempts per 15 minutes per IP+identifier), and a costly mutation without limits is a denial-of-wallet invitation; sliding-window limits in KV cost ~1 ms and run fine in actions. The same shared `verifySession`/wrapper pattern from the DAL lesson applies: encode the pipeline once, make the safe path the lazy path, and let reviewers hunt only for mutations that bypass the wrapper.

<Inset kind="why" lang="en">
Why does the framework not authorize for you? Because authorization is a domain statement, not a transport property. Next.js can see headers, so it checks headers — Origin against Host. It cannot know that projects have owners, that owners may rename but viewers may not, or that role is not a client-settable field. Every framework draws this same line; what changes with server actions is the temptation to forget it, because calling await renameProject(id) feels like a local function call, and ten years of instincts say local calls are trusted. The wire disagrees: it is a POST from a hostile network, wearing your function's name.
</Inset>

<Quiz
  id="04-csrf-and-action-security-quiz-2"
  lessonSlug="04-csrf-and-action-security"
  lang="en"
  question="A deleteDocument(docId) action calls verifySession(), then db.document.delete({ where: { id: docId } }). The delete button only renders for document owners. A pentester deletes another user's document. How?"
  choices={[
    { label: "They invoked the action from their own authenticated session with a different docId — the action authenticated the caller but never compared the document's owner to the session user, and UI-level button gating does not execute on the request path", correct: true },
    { label: "They forged the request from an external site, riding the victim's cookies", misconception: "That is the attack Next's Origin/Host check (plus SameSite=Lax cookies) actually blocks. No forgery was needed — the attacker used their own valid session and simply pointed the action at a row they do not own. CSRF defenses are silent about authorization." },
    { label: "They extracted the action's encrypted closure variables to escalate privileges", misconception: "Closure encryption is real but irrelevant here: docId is an argument, not a closed-over value. Arguments arrive as attacker-controlled plaintext by design — they are the request body. The vulnerability is the missing ownership check, not broken cryptography." },
    { label: "They guessed the action id, which should have been kept server-side", misconception: "Action ids ship in the public bundle and are not a secret to guess — and hiding them would not help, since the attacker's own pages legitimately reference the action. Endpoint obscurity is not an authorization model." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="What the framework checks versus what only your code can check, in pipeline order"
  hue="var(--d-ai)"
  perRow={3}
  nodes={[
    { id: "post", label: "POST + Next-Action", sub: "args = attacker-controlled" },
    { id: "samesite", label: "SameSite=Lax", sub: "cross-site POST drops cookie" },
    { id: "origin", label: "Origin vs Host", sub: "framework: stops CSRF only" },
    { id: "authn", label: "verifySession", sub: "yours: who is calling" },
    { id: "validate", label: "zod + rate limit", sub: "yours: shape and frequency" },
    { id: "authz", label: "Row ownership", sub: "yours: may THEY touch THIS" },
  ]}
  edges={[
    { from: "post", to: "samesite", label: "cookie layer" },
    { from: "samesite", to: "origin", label: "header layer" },
    { from: "origin", to: "authn", label: "framework ends" },
    { from: "authn", to: "validate", label: "parse the wire" },
    { from: "validate", to: "authz", label: "then the row" },
  ]}
  caption="Next.js ends its protection at the Origin/Host comparison — and route handlers do not even get that. Everything after the dashed line is application code: authenticate, rate-limit, validate the untyped payload, and authorize against the actual row before mutating and revalidating."
/>

<KeyTakeaway>An exported server action is a public POST endpoint: UI gating, unused buttons, and opaque ids protect nothing. Next checks Origin against Host — that stops cross-site forgery for actions only; route handlers bring their own CSRF defense, and allowedOrigins entries are declared trust. Inside every action: verifySession, rate limit, zod-parse the unknown payload, and check row ownership — IDOR rides authenticated requests through every framework check.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-csrf-and-action-security-retrieval"
  lang="en"
  questions={[
    { q: "What CSRF protection do server actions get, and what are its three edges?", a: "Two layers: SameSite=Lax session cookies are omitted from cross-site POSTs, and Next compares the Origin header to Host (X-Forwarded-Host) on every action invocation, rejecting mismatches — which also covers same-site gaps like sibling-subdomain forgeries, where Lax cookies still ride. Edges: (1) it covers actions only — route handlers under app/api get no automatic CSRF check; (2) proxies that rewrite Host break the comparison, and the serverActions.allowedOrigins escape hatch is a list of origins you have declared trusted to forge — audit it; (3) it verifies where the request came from, never what it may do — an authenticated attacker passes untouched." },
    { q: "Recite the five-step pipeline every mutating action needs, and the attack each step kills.", a: "1. verifySession — authentication; kills anonymous invocation, since the action is reachable without any UI. 2. Rate limit (e.g. 20 writes/10 s per user, 5 login attempts/15 min per IP) — kills credential stuffing and denial-of-wallet on expensive mutations. 3. Schema-parse the raw: unknown payload with zod — the TS signature is compile-time fiction; parsing kills type confusion and mass assignment (role: admin via spread). 4. Per-row authorization — compare ownerId to session.userId on the actual row; kills IDOR, which UI gating and CSRF checks never see. 5. revalidatePath/Tag — not security, but the cache contract that makes the mutation visible." },
  ]}
/>

<Recap lang="en">
Exporting a function from a 'use server' file creates an HTTP endpoint: the client POSTs to the page URL with a Next-Action id from the public bundle, and the arguments arrive as attacker-controlled plaintext — only closed-over values get per-build encryption. Reachability has nothing to do with UI: removed buttons, admin-only rendering, and unused-but-exported actions are all live endpoints, so exported means public. The framework's CSRF story for actions is two genuine layers — SameSite=Lax cookies that stay home on cross-site POSTs, and an Origin-versus-Host comparison on every invocation that even catches sibling-subdomain forgeries where Lax cookies still ride — and three exact edges: route handlers get none of it and bring their own CSRF defense; Host-rewriting proxies break the comparison, and every serverActions.allowedOrigins entry is an origin you have declared trusted to forge; and the check authenticates provenance, not permission — an authenticated attacker is invisible to it. That last edge is the pentest finding that keeps recurring: IDOR, a valid session pointing a real action at someone else's row, which unit 02 introduced and this unit armors against. The armor is a five-step pipeline inside every mutating action: verifySession to authenticate; a rate limit because login actions without one are credential-stuffing oracles and costly mutations are denial-of-wallet invitations; zod-parse of a payload honestly typed as unknown, which also closes mass assignment by allowlisting fields; per-row ownership comparison before the write, because that is the only line that stops IDOR; then revalidate. Wrap the pipeline once so the safe path is the lazy path — the framework stops the attacks aimed at the framework, and the pipeline stops the ones aimed at you.
</Recap>
