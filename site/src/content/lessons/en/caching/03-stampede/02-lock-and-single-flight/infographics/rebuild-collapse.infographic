infographic chart-bar-plain-text
data
  title DB rebuilds at a TTL boundary (50 nodes, 100,000 misses)
  desc Each mitigation layer narrows the coordination scope — per-process, then per-fleet
  values
    - label No mitigation
      value 100000
    - label Single-flight only (1 per node)
      value 50
    - label Single-flight + Redis lock
      value 1
theme
  palette #f43f5e #f59e0b #14b8a6
