---
concepts:
- no-store-vs-no-cache
- s-maxage-override
- private-vs-public
- immutable-content-hash
- stale-while-revalidate
- vary-cache-key
estMin: 13
lang: en
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.rfc-editor.org/rfc/rfc9111.html
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Headers/Cache-Control
- https://simonhearne.com/2022/caching-header-best-practices/
status: ready
summary: "Free-recall prompts across the Cache-Control unit. Answer each in your own words first, then reveal the model answer and compare."
title: 'Cache-Control: free-recall review'
track: caching
unit: 05-cache-control
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory before you open the model answer — the effort of recall is what makes the directive semantics stick.
</Hook>

<Goal lang="en">
Reconstruct the unit's core distinctions — store vs revalidate, browser vs shared TTL, public vs private, immutable assets, and stale serving — without looking back at the lesson.
</Goal>

<RetrievalDrawer
  client:visible
  id="05-cache-control-short-retrieval"
  lang="en"
  questions={[
    { q: "Explain the difference between no-store and no-cache, and why conflating them causes real incidents.", a: "They answer two different questions. no-store answers 'may a cache store this at all?' with no — nothing is written to disk or memory in the browser or any shared cache, and it is the directive for genuinely sensitive per-request data like a bank balance or a password-reset page. no-cache answers 'may a cache reuse a stored copy without checking?' — it permits storage and only requires revalidation with the origin (a conditional If-None-Match / If-Modified-Since) before serving the stored copy. The incident pattern: a developer reaches for no-cache expecting no storage, the sensitive response lands in the browser and bfcache, and on a back-button press the bfcache serves it without any revalidation — leaking one user's data to the next. The name is the trap; the directive that means 'do not store' is no-store." },
    { q: "What is the relationship between max-age and s-maxage, and what breaks when you forget s-maxage behind a CDN?", a: "max-age sets freshness for any cache; s-maxage sets it for shared caches (CDNs, reverse proxies) only and overrides max-age for them, while private (browser) caches ignore s-maxage entirely. They are independent levers because the two tiers have different jobs: the browser caches for one user, the CDN for everyone. Forgetting s-maxage hands the CDN whatever max-age says. If max-age=0, the CDN revalidates on every request — zero offload, origin hammered. If max-age is long, the CDN caches the page for that whole duration, which is catastrophic for anything personalized. The senior move is to set them deliberately and separately: a short browser TTL with a longer shared TTL, e.g. max-age=60, s-maxage=300." },
    { q: "Why is the absence of private on an authenticated route a classic data-leak, and what is the senior reflex?", a: "public lets any cache store the response, including a CDN; private says only a single user's browser may store it and excludes shared caches. When an authenticated, personalized response (an account page, a signed-in homepage, a current-user API) goes out without private, the CDN stores the first user's copy and serves it to every subsequent visitor within the freshness window — textbook authenticated-response cache poisoning, and it usually traces to a default Cache-Control left on a route that became personalized later. The senior reflex: any response that varies by who is asking is private (and ideally no-store), you assume a CDN sits in front whether you can see it or not, and you add Vary: Cookie/Authorization only as a secondary signal — the real safety lives in private/no-store because some CDNs ignore Vary." },
    { q: "Walk through why content-hashed filenames make public, max-age=31536000, immutable both aggressive and correct, and what must NOT be cached that way.", a: "One year (the conventional value for immutable assets; the RFC imposes no cap) plus immutable tells every cache to keep the file and never revalidate it, not even on a hard reload — saving a conditional round trip per asset, a real win on a page pulling many files. The danger is that a one-year cache becomes a one-year-stale bug if the file ever changes, since clients won't re-fetch a URL they consider immutable. Content hashing dissolves this: the filename embeds a hash of the bytes (app.4f3a9c.js), so any change produces a new filename and therefore a new URL. The old URL stays cached forever and harmless; the new one is fetched fresh because it is a different resource. The rule that ties it together is hash the assets, never the HTML — the HTML document is no-cache (or max-age=0, must-revalidate) so the browser always re-checks it and picks up the new hashed asset URLs." },
    { q: "What do stale-while-revalidate and stale-if-error do, and how do they differ from must-revalidate?", a: "must-revalidate is the strict stance: once a copy is stale, a cache MUST revalidate with the origin and may not serve the stale copy even if the origin is unreachable. stale-while-revalidate=N is the opposite opt-in: after expiry, a cache may serve the stale copy instantly for up to N seconds while it revalidates in the background — trading a slightly-stale response for zero request-time latency. stale-if-error=N says if the origin is down (or errors) during revalidation, serve the stale copy for up to N seconds rather than show an error — a resilience lever. A realistic stacked header for a CDN-fronted API is max-age=60, s-maxage=300, stale-while-revalidate=3600: 60s fresh in browsers, 5 min in the CDN, then up to an hour of instant-but-stale while a background refresh runs." },
    { q: "What does Vary do, why is it a cache key rather than a safety directive, and where does it fail you?", a: "Vary names the request headers a cache must include in its cache key, so it stores a separate variant per distinct value of those headers — Vary: Accept-Encoding correctly stores a gzip and a br variant. It governs keying, not lifetime and not whether sharing is allowed. It fails in two ways. First, high-cardinality values (Vary: Cookie, Vary: User-Agent) fragment the cache so badly that every distinct value gets its own entry and the hit rate collapses to near zero. Second, some CDNs (Cloudflare) ignore Vary on most responses, so a Vary you rely on for correctness may not be honored at all — meaning a personalized page could still be shared. The takeaway: keep per-user safety in private/no-store and treat Vary as a hint, not a guarantee." },
  ]}
/>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the unit's spine: no-store forbids storage while no-cache only forces revalidation, so the name of no-cache is the trap; max-age governs every cache and s-maxage overrides it for shared caches only, so forgetting s-maxage hands the CDN the wrong TTL; private keeps per-user responses off the CDN and its absence is the classic leak; content-hashed filenames make immutable one-year caching correct because a change means a new URL; stale-while-revalidate and stale-if-error trade staleness for latency and resilience against must-revalidate's strictness; and Vary keys variants rather than excluding sharing, so safety must live in private/no-store. Now when you design the caching policy for a new route, run these six questions in order before you ship the header — that's the difference between a deliberate policy and a silent incident waiting to happen.
</Recap>
