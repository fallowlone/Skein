infographic chart-column-simple
data
  title Concurrent DB connections at the expiry instant
  desc A 200ms query: idle behind a warm cache, ~5000 at the TTL tick — far past a 100-connection pool
  values
    - label Steady state (warm cache)
      value 1
    - label Postgres pool ceiling
      value 100
    - label Herd at TTL expiry
      value 5000
theme
  palette #14b8a6 #f59e0b #f43f5e
