---
concepts:
- dogpile-effect
- single-flight
- distributed-lock
- lock-ttl
- lease-renewal
- probabilistic-early-expiration
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://cseweb.ucsd.edu/~avattani/papers/cache_stampede.pdf
- https://pkg.go.dev/golang.org/x/sync/singleflight
- https://redis.io/docs/latest/develop/use/patterns/distributed-locks/
status: ready
summary: "Multiple-choice synthesis across the dogpile unit — the expiry-instant collision, single-flight coalescing, distributed locks with TTL and lease renewal, and why jitter does not help one hot key."
title: 'Dogpile: multiple-choice review'
track: caching
unit: 07-dogpile
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one mirrors a decision you make mid-incident when one hot key takes the origin down — not a definition to recite, but a tradeoff to weigh on the lock, the lease, and the recompute window.
</Hook>

<Goal lang="en">
Confirm you can connect the expiry-instant collision, single-flight coalescing, distributed locking with TTL and lease renewal, and probabilistic early expiry — the synthesis the lesson built toward.
</Goal>

<Quiz
  id="07-dogpile-choice-1"
  lessonSlug="07-dogpile"
  lang="en"
  question="A single hot key (200ms recompute) takes the DB down every time its TTL expires under ~5000 req/s. Which fix most directly collapses the herd, and what is the one detail that makes it safe?"
  choices={[
    { label: "Single-flight / request coalescing: the first miss recomputes and every other concurrent miss is deduplicated onto that in-flight call. Make it safe by giving the lock a timeout so a crashed recomputer cannot deadlock all readers.", correct: true },
    { label: "Lower the TTL so the value is fresher and the herd is smaller.", misconception: "Backwards. A shorter TTL means the key expires more often, so the dogpile fires more frequently. TTL controls staleness, not the concurrency collision at the expiry instant." },
    { label: "Add TTL jitter to the feed key so the expiry instant moves around.", misconception: "Jitter staggers a batch of keys cached together. There is exactly one hot key here, so exactly one expiry instant and one herd — randomizing when that instant occurs does not reduce the herd that piles on at it." },
    { label: "Scale the connection pool up to 5000 so every recompute gets a connection.", misconception: "That just moves the meltdown onto the database itself — 5000 simultaneous 200ms aggregations. The leverage is collapsing N recomputes to one, not provisioning for N." },
  ]}
/>

<Quiz
  id="07-dogpile-choice-2"
  lessonSlug="07-dogpile"
  lang="en"
  question="A local single-flight (in-process mutex) cut the herd but the DB still sees ~20 concurrent recomputes per expiry across the fleet. Why, and what closes the gap?"
  choices={[
    { label: "A local mutex only coalesces within one process; with 20 app instances each runs its own single-flight, so the origin still sees up to 20 recomputes. A distributed lock (e.g. Redis SET key val NX PX 30000) coordinates one recompute across the fleet.", correct: true },
    { label: "The mutex is leaking goroutines, so old recomputes never release.", misconception: "A leak would grow unbounded; ~20 maps exactly to the instance count. The pattern is per-process coalescing, not a leak — the boundary is the process, not a bug." },
    { label: "20 recomputes means the lock timeout is too short and keeps expiring.", misconception: "Timeout tuning affects deadlock recovery, not the fleet-wide fan-out. Even a perfect local lock cannot see the other 19 instances; only a shared lock store can." },
    { label: "Redis is rate-limiting the writes, capping it at 20.", misconception: "The 20 is the instance count, not a Redis limit. The issue is that each process coordinates only with itself until you introduce a cross-process lock." },
  ]}
/>

<Quiz
  id="07-dogpile-choice-3"
  lessonSlug="07-dogpile"
  lang="en"
  question="You add a distributed recompute lock but skip the lock TTL (no expiry). What is the senior-level failure?"
  choices={[
    { label: "If the single recomputer crashes or hangs while holding the lock, it is never released and every reader blocks forever — a total outage with a healthy, idle database.", correct: true },
    { label: "The lock makes the recompute itself slower because of the network round-trip.", misconception: "A round-trip adds milliseconds but is not the failure mode of a missing TTL. The hazard is a dead holder that never releases the lock, deadlocking all readers." },
    { label: "Readers will silently get stale data.", misconception: "A lock-and-wait pattern serves the fresh value once written. Staleness is the stale-while-revalidate tradeoff, not the consequence of an absent lock TTL." },
    { label: "The cache TTL will be ignored once the lock is held.", misconception: "Cache TTL and lock TTL are independent timers. Omitting the lock TTL does not disable the cache TTL — it just removes the deadlock escape hatch." },
  ]}
/>

<Quiz
  id="07-dogpile-choice-4"
  lessonSlug="07-dogpile"
  lang="en"
  question="The recompute usually takes 2s but occasionally 25s. You set the distributed lock TTL to 5s. What goes wrong, and what is the principle?"
  choices={[
    { label: "On a slow 25s recompute the 5s lock expires mid-flight, a second worker acquires it and starts recomputing too — the dogpile reopens, and two writers may race. Principle: the lock TTL must exceed the worst-case recompute, or the holder must renew (extend) the lease while it is alive.", correct: true },
    { label: "Nothing — 5s is fine because most recomputes finish in 2s.", misconception: "The tail is the whole problem. A TTL tuned to the median lets the p99 recompute lose its lock mid-flight, which is exactly when reopening the dogpile hurts most." },
    { label: "The lock will never release because 5s is shorter than the recompute.", misconception: "5s is longer than the typical 2s, so the normal case releases fine on completion. The defect surfaces only on the 25s tail, where the TTL fires before the work finishes." },
    { label: "Readers will deadlock because the TTL is too short to acquire.", misconception: "A short TTL does not block acquisition; it causes premature release. The risk is duplicate recomputes after expiry, not an inability to take the lock." },
  ]}
/>

<Quiz
  id="07-dogpile-choice-5"
  lessonSlug="07-dogpile"
  lang="en"
  question="Rather than guess a lock TTL, you have the holder renew a short lease (a heartbeat that re-extends the lock every second while it works). What does this buy, and what new failure must you handle?"
  choices={[
    { label: "The lease tracks the real recompute: a live holder keeps extending so it never loses the lock mid-flight, while a crashed holder stops renewing and the short TTL releases it fast. New failure: a paused holder (GC/STW pause, network partition) can believe it still holds the lock after the lease lapsed — so the writer must fence (check a lock token before writing) to avoid a stale overwrite.", correct: true },
    { label: "It removes the need for any TTL because renewal keeps the lock alive.", misconception: "Renewal complements the TTL; it does not replace it. The short TTL is exactly what releases the lock when a crashed holder stops renewing — without it a dead holder still deadlocks everyone." },
    { label: "It guarantees exactly one writer under all conditions including network partitions.", misconception: "A pause or partition can make a holder think it still owns a lapsed lease while another worker has acquired it. You need fencing tokens or a check-and-set write; renewal alone does not give exactly-once." },
    { label: "It eliminates the recompute cost by spreading it across heartbeats.", misconception: "Heartbeats only extend the lock; the recompute still runs once in full. The lease changes who may write, not how expensive the work is." },
  ]}
/>

<Quiz
  id="07-dogpile-choice-6"
  lessonSlug="07-dogpile"
  lang="en"
  question="A teammate argues a lock is always the wrong tool because it serializes readers, and wants probabilistic early expiry (XFetch) everywhere instead. When is each actually the right call?"
  choices={[
    { label: "Locking collapses N misses to one but can serialize readers behind a slow holder (a latency cliff) unless waiters serve stale; XFetch makes the key never expire under load by having each reader independently recompute slightly early, so the first lucky reader refreshes alone — great for a single very hot key, but it needs the recompute-cost delta stored and tolerates a little extra recompute work. Match the tool to the shape, not a blanket rule.", correct: true },
    { label: "Locks are always wrong; XFetch strictly dominates because it never blocks.", misconception: "XFetch adds a little redundant recompute and needs the delta stored and beta tuned; it shines for one hot key but is not a universal replacement. A lock with stale-on-wait fits better when correctness needs a single coordinated write." },
    { label: "XFetch is always wrong because probabilistic means non-deterministic herds.", misconception: "XFetch is proven near-optimal (beta=1) precisely because each reader rolls independently, so one refreshes early and alone before the herd. It is deliberately probabilistic, not accidentally unreliable." },
    { label: "They are interchangeable; pick whichever your library ships.", misconception: "They attack different points: locking coordinates the miss window, XFetch prevents the expiry from coinciding with peak load. The choice turns on staleness tolerance, instance count, and recompute cost — not library convenience." },
  ]}
/>

<Recap lang="en">
The through-line is one decision: the dogpile is the collision at a single hot key's expiry instant, and every fix targets a different point in that window. Single-flight collapses N concurrent misses into one recompute — but a local mutex only coalesces per process, a distributed lock needs a TTL longer than the worst-case recompute (or a renewed lease) so a crashed holder cannot deadlock all readers, and a lease still needs fencing against a paused holder. XFetch dissolves the collision by making the hot key recompute early and alone before it ever expires under load. Jitter staggers a batch of keys and does nothing for one hot key. Most dogpile postmortems are about the fix deadlocking, serializing, or double-writing — not the original miss. Now when you are paged for a latency cliff that fires on a 5-minute cadence, you know exactly which questions to ask: is it one hot key or a batch, do you have multiple instances, and can you tolerate a few seconds of staleness — the answers tell you which fix to reach for without wasting an hour on the wrong lever.
</Recap>
