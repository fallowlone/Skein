---
concepts:
- access-latency
- hit-ratio
- os-page-cache
- wrong-layer-caching
- double-caching
- cache-invalidation
estMin: 14
lang: en
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://gist.github.com/jboner/2841832
- https://redis.io/blog/why-your-cache-hit-ratio-strategy-needs-an-update/
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Cache-Control
status: ready
summary: "Free-recall prompts across the cache-layers unit. Answer each in your own words first, then reveal the model answer and compare."
title: 'Cache layers: free-recall review'
track: caching
unit: 01-layers
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory before you open the model answer — the effort of recall is what makes the latency ladder and the failure modes stick.
</Hook>

<Goal lang="en">
Reconstruct the unit's spine — the latency ladder, hit-ratio break-even, the OS page cache, where to cache, and the wrong-layer and double-caching failures — without looking back at the lesson.
</Goal>

<RetrievalDrawer
  client:load
  id="01-layers-short-retrieval"
  lang="en"
  questions={[
    { q: "Walk the caching latency ladder from fastest to slowest with rough numbers, and explain why the orders of magnitude matter more than the exact figures.", a: "Roughly: L1 CPU cache ~1 ns, L2 ~4 ns, L3 ~10–40 ns, RAM and OS page cache ~100 ns, SSD ~100 µs, an in-region Redis network round trip ~1 ms, and a far CDN hop 100+ ms. Each rung is bigger, cheaper, and slower than the one above. The exact numbers drift with hardware, but the orders of magnitude are the decision tool: L1 is ~200× faster than RAM, RAM ~1000× faster than SSD, an SSD ~100× faster than a cross-continent hop. When you add a cache you are betting the layer you front is slower than the cache by a wide margin. If it is not — if you front a RAM-speed origin with a network-speed cache — you have added latency, not removed it. A senior carries the magnitudes in their head because they decide whether a cache helps at all." },
    { q: "Why can adding a cache make latency worse, and what is the average-latency formula that proves it?", a: "A cache only removes latency if the layer it fronts is much slower than the cache itself, and a miss pays BOTH costs — the cache lookup and the full origin fetch. So average latency is hit_rate × cache_latency + miss_rate × (cache_latency + origin_latency). Two ways this goes wrong: first, if the origin is already fast — a warm Postgres primary-key lookup served from the buffer pool at ~0.3 ms — then even a hit (a ~1 ms Redis round trip) is slower than just asking the origin. Second, with a low hit ratio against any origin, the miss tax dominates: you pay cache+origin on most requests for little benefit. A cache earns its place only when origin_latency is large, the hit ratio is high, and the data tolerates being slightly stale. Miss any one and you have added a failure domain and an invalidation problem for nothing." },
    { q: "What is the OS page cache, and why does it change the 'should I add Redis?' decision?", a: "When you read a file, the kernel keeps its pages in free RAM, so the second read is a memory hit, not a disk hit. Combined with a database's own buffer pool, this means a 'disk' read on hot data is usually a RAM read — which is why databases are fast on their working set without you doing anything. The implication: if the working set fits in RAM, the database is already a RAM-speed cache, so wrapping it in Redis only inserts a network round trip and can make hot reads slower. The page cache is per-machine and local, so it cannot be shared across instances — that sharing is the real reason to reach for Redis. Before adding an application cache, measure the origin's actual latency; if it is already RAM-speed because of the page cache and buffer pool, the cache will not help." },
    { q: "Given a candidate read path, how do you decide whether to cache and at which layer?", a: "Three gates first: is the origin genuinely slow, is the hit ratio high (hot, reused keys), and does the data tolerate staleness? If any is no, do not cache. If yes, choose the layer by the data's nature and volatility. Static or near-static responses shared across users go at the CDN, keyed by URL, exploiting geography. A frequently-invalidated query result shared across users goes in an application cache (Redis) keyed by inputs, with a short TTL plus write-time invalidation — this fits an 80 ms join called 50×/sec on read-mostly data. CPU-bound results must be cached as computed values, not pushed to lower layers (the page cache speeds reads but cannot skip recomputation). Personalized or per-request data must be cached close to the user — per-session app cache or not at all — never at a shared URL-keyed layer. The rule: cache as close to the consumer as the data's volatility allows." },
    { q: "What is double-caching, why does it produce stale-on-stale bugs, and what makes them hard to debug?", a: "Double-caching is when the same fact is cached at two independent layers — for example a value in Redis and the rendered response at a reverse proxy or CDN — each with its own TTL and its own invalidation path, with no coordination between them. A write clears Redis but the CDN keeps serving the old HTML until its TTL expires; or the CDN is purged but a proxy in front keeps its own stale copy — stale-on-stale, two layers out of sync. It is hard to debug because each layer reports only itself: CF-Cache-Status describes the CDN, X-Cache the proxy, and a reverse proxy can silently override origin Cache-Control (origin says no-cache, the proxy keeps a 5-minute TTL). You have to walk the request layer by layer — Age, CF-Cache-Status, X-Cache, proxy logs, Redis hit/miss, then the DB — to find which copy is lying. The structural fix is one owner per cached fact and one invalidation path." },
    { q: "What does a low hit ratio actually tell you, and why is 'increase the hit ratio' not always the goal?", a: "Hit ratio is the number that justifies a cache: below the break-even ratio for a given origin and cache latency, the miss tax (cache lookup + full origin) makes the cache net-negative versus no cache. A low ratio is usually a workload or key-design signal — one-hit-wonder keys, too-short TTL, or poor key cardinality — not a stability bug. But raising the raw ratio is not always right: caching cold, rarely-reused data to bump the number can evict the genuinely hot keys to make room for one-hit wonders, raising overall latency. The expensive caches in production are the ones at a 40% hit ratio that everyone is afraid to remove. The real target is whether the cache lowers overall latency and origin load enough to justify the new failure domain and invalidation cost — not a hit-ratio vanity metric." },
  ]}
/>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the unit's spine: caching is a latency ladder where orders of magnitude decide whether a cache helps; a miss pays cache+origin so a low hit ratio or a fast origin makes the cache worse than none; the OS page cache and buffer pool often make the origin already RAM-fast; you cache only when the origin is slow, the hit ratio is high, and staleness is tolerable, at the layer the data's volatility allows; and the recurring senior failures are wrong-layer caching and double-caching one fact at two layers — fixed by one owner and one invalidation path per cached fact.
</Recap>
