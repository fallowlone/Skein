---
concepts:
- cache-hierarchy
- access-latency
- hit-ratio
- wrong-layer-caching
- double-caching
- cache-invalidation
estMin: 12
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://gist.github.com/jboner/2841832
- https://redis.io/blog/why-your-cache-hit-ratio-strategy-needs-an-update/
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Cache-Control
status: ready
summary: "Multiple-choice synthesis across the cache-layers unit — the latency ladder, where to cache, hit-ratio break-even, and the wrong-layer and double-caching failure modes."
title: 'Cache layers: multiple-choice review'
track: caching
unit: 01-layers
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one mirrors a decision you make in a real system — not a definition to recite, but a tradeoff to weigh the moment someone says "just add a cache."
</Hook>

<Goal lang="en">
Confirm you can connect the latency ladder, where to put a cache, the hit-ratio break-even, and the wrong-layer and double-caching failure modes — the synthesis the lesson built toward.
</Goal>

<Quiz
  id="01-layers-choice-1"
  lessonSlug="01-layers"
  lang="en"
  question="A team fronts a Postgres primary-key lookup (warm, served from the buffer pool in ~0.3 ms) with an in-region Redis cache (~1 ms round trip). p99 read latency rises. Why?"
  choices={[
    { label: "The origin was already RAM-speed, so the cache only adds a network hop. A buffer-pool / page-cache hit is faster than the Redis round trip you put in front of it — you wrapped a fast origin in a slower cache.", correct: true },
    { label: "Redis is single-threaded, so it serializes all the lookups.", misconception: "Redis is fast and effectively non-blocking for simple GETs; throughput is not the issue. Latency rose because the network round trip to Redis is slower than the warm buffer-pool read it fronts." },
    { label: "Postgres got slower because the cache stole its memory.", misconception: "Redis runs as a separate process / instance and does not shrink Postgres's buffer pool. The regression is the added network hop on a path that was already a RAM hit." },
    { label: "The cache key was hashed, which is expensive at p99.", misconception: "Key hashing is nanoseconds — irrelevant next to the ~1 ms network round trip. The structural mistake is fronting a RAM-speed origin with a network-speed cache." },
  ]}
/>

<Quiz
  id="01-layers-choice-2"
  lessonSlug="01-layers"
  lang="en"
  question="A read endpoint runs a 3-table join taking 80 ms, called 50×/sec, on data that changes a few times a day. Where does the cache belong?"
  choices={[
    { label: "An application cache (Redis) keyed by the query inputs, with a short TTL plus invalidation on write. The origin is genuinely slow, the access is hot so the hit ratio is high, and the data is read-mostly — the ~1 ms round trip is two orders of magnitude cheaper than the join.", correct: true },
    { label: "Add RAM and trust the DB buffer pool / OS page cache to keep it hot.", misconception: "Lower layers help I/O-bound reads, but the 80 ms is CPU work — joining and aggregating, not disk reads. The page cache makes the rows fast to fetch; it cannot skip recomputing the join. You must cache the computed result." },
    { label: "Cache the rendered HTML at the CDN with a long TTL.", misconception: "Wrong layer for a data endpoint. The CDN is keyed by URL and is for static or near-static responses; a long edge TTL means a data change takes until expiry to appear globally, and personalized variants leak across users." },
    { label: "No cache — 80 ms is fine, keep it simple.", misconception: "50 calls/sec of 80 ms work is real origin load and real tail latency, and the data tolerates staleness. This is a clean cache fit; skipping it trades a cheap, well-understood win for needless origin pressure." },
  ]}
/>

<Quiz
  id="01-layers-choice-3"
  lessonSlug="01-layers"
  lang="en"
  question="A cache with a 35% hit ratio fronts an 80 ms origin behind a 1 ms lookup. Average latency is 0.35×1 + 0.65×(1+80) ≈ 53 ms — better than 80. So why is a 35% hit ratio still a red flag?"
  choices={[
    { label: "Every miss now pays the cache lookup on top of the origin (81 ms vs 80), you have added a new failure domain and an invalidation problem, and a 35% ratio means the cache mostly is not earning its complexity — a small traffic-mix shift can push it net-negative.", correct: true },
    { label: "It is not a red flag — 53 ms beats 80 ms, ship it.", misconception: "The average improves only because the origin is slow enough to absorb the miss tax, but 35% reuse is poor: misses still pay cache+origin, and you have bought a new moving part and invalidation path for a thin win that is fragile to traffic changes." },
    { label: "Hit ratio is irrelevant once average latency drops.", misconception: "Hit ratio is precisely the number that justifies a cache. Below a break-even ratio the miss tax (cache lookup plus full origin) makes the cache strictly worse than no cache." },
    { label: "A low hit ratio means Redis is misconfigured and will crash.", misconception: "A low hit ratio is a workload / key-design signal — one-hit-wonder keys, too-short TTL, poor key cardinality — not a stability bug. It tells you the cache may not be worth its cost here." },
  ]}
/>

<Quiz
  id="01-layers-choice-4"
  lessonSlug="01-layers"
  lang="en"
  question="Why is the OS page cache the most underrated layer when deciding whether to add Redis?"
  choices={[
    { label: "The kernel keeps recently-read file pages in free RAM, so a 'disk' read on hot data is actually a RAM read. Combined with the DB buffer pool, a working set that fits in RAM means the database is already a RAM-speed cache — adding Redis only inserts a network hop.", correct: true },
    { label: "The page cache compresses data so it fits more in RAM than Redis can.", misconception: "The page cache stores raw file pages, not compressed data, and that is not the point. Its value is that hot reads are served from RAM with zero work from you, so the origin may already be RAM-speed." },
    { label: "The page cache replicates across machines like Redis does.", misconception: "The page cache is per-machine and local — it cannot be shared across instances, which is exactly what an application cache adds. Its relevance here is that it makes a single node's hot reads RAM-fast." },
    { label: "It guarantees data is never stale.", misconception: "The page cache is about read latency, not freshness, and is kept coherent through the filesystem. The reason it matters here is that it makes the origin fast, so a network cache in front may add latency instead of removing it." },
  ]}
/>

<Quiz
  id="01-layers-choice-5"
  lessonSlug="01-layers"
  lang="en"
  question="A value is cached in Redis (invalidated on write) AND its rendered response is cached at the CDN with a 5-minute TTL. After a write, users still see old data for minutes. What is the failure and the structural fix?"
  choices={[
    { label: "Double-caching: the same fact lives at two layers with independent lifetimes. The write clears Redis but the CDN keeps serving stale HTML until its TTL expires. Fix: one owner per cached fact and one invalidation path — purge the CDN on write, or do not cache that response at the edge at all.", correct: true },
    { label: "Redis invalidation failed; just lower the Redis TTL.", misconception: "Redis is fresh — it was invalidated correctly. The stale copy is at the CDN. Lowering the Redis TTL changes nothing because the CDN layer is the one lying; the problem is two uncoordinated layers, not one TTL." },
    { label: "The CDN TTL is too long; set it to 30 seconds and the bug is gone.", misconception: "Shortening the edge TTL only shrinks the staleness window — the layers are still uncoordinated and a write is still invisible until expiry. The real fix is purge-on-write or a single owning layer, not a smaller window." },
    { label: "This is unavoidable with any CDN — caching always risks staleness.", misconception: "Stale-on-stale here is caused by two independent layers caching the same fact, which is avoidable: give each fact one owner and one invalidation path. A single, correctly-purged cache layer does not drift against itself." },
  ]}
/>

<Quiz
  id="01-layers-choice-6"
  lessonSlug="01-layers"
  lang="en"
  question="An engineer caches a per-user 'recommended for you' fragment at the CDN to cut origin load. Some users start seeing other users' recommendations. Root cause?"
  choices={[
    { label: "The CDN keys cache entries by URL (and configured headers), not by identity. Personalized content at a shared, URL-keyed layer collapses distinct users onto one entry, leaking data. Personalize as close to the user as the data allows — per-session app cache or no shared cache — not at the edge.", correct: true },
    { label: "A bug in the recommendation model mixed up users.", misconception: "The model is fine — the fragments are served from a shared CDN entry keyed by URL, so user A's cached fragment is returned to user B. It is a caching-layer mistake, not a model bug." },
    { label: "Cookies were stripped, so the origin got confused.", misconception: "Even if the origin reads the cookie correctly, a URL-keyed CDN hit never reaches the origin — it serves whatever it cached first. The defect is caching identity-dependent data at a layer that cannot tell users apart." },
    { label: "The CDN TTL was too long, so old recommendations leaked.", misconception: "This is a cross-user leak, not staleness. A shorter TTL still serves one user's fragment to another within the window. Personalized data simply must not be cached at a shared URL-keyed layer." },
  ]}
/>

<Recap lang="en">
The through-line: caching is a latency ladder — L1, RAM, page cache, Redis, CDN — and a cache only helps when it fronts a genuinely slower origin, the hit ratio is high, and the data tolerates staleness. The page cache and DB buffer pool often make the origin RAM-fast already, so check real origin latency before reaching for Redis. The senior failures all repeat: fronting a fast origin, justifying a cache on a weak hit ratio, caching personalized data at a URL-keyed CDN, and double-caching one fact at two layers with independent TTLs. Cache as close to the consumer as the data's volatility allows; give every fact one owner and one invalidation path.
</Recap>
