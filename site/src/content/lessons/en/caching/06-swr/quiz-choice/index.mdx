---
concepts:
- stale-while-revalidate
- stale-if-error
- background-revalidation
- request-coalescing
- bounded-staleness
- swr-auth-hazard
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://datatracker.ietf.org/doc/html/rfc5861
- https://swr.vercel.app/docs/api
- https://www.fastly.com/documentation/guides/concepts/edge-state/cache/stale/
status: ready
summary: "Multiple-choice synthesis across the SWR unit — the two windows, single-flight, stale-if-error, client revalidation, and the auth hazard."
title: 'SWR: multiple-choice review'
track: caching
unit: 06-swr
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each mirrors a header you write or an incident you triage — not a directive to memorise, but a freshness-versus-latency trade to weigh under real traffic.
</Hook>

<Goal lang="en">
Confirm you can connect the freshness window, the SWR window, stale-if-error, single-flight, and the client revalidation model — and that you know the one place SWR must never go.
</Goal>

<Quiz
  id="06-swr-choice-1"
  lessonSlug="06-swr"
  lang="en"
  question="A request arrives 90 seconds after caching, with Cache-Control: max-age=60, stale-while-revalidate=300. What does the user get and why?"
  choices={[
    { label: "The stale value immediately, and a background refresh is kicked off for subsequent requests — the entry is past max-age but still inside the SWR window, so it is serveable as stale.", correct: true },
    { label: "A blocking wait while the cache revalidates with the origin, then the fresh value.", misconception: "That is plain max-age behaviour. Inside the SWR window the entire point is that the user is not blocked — stale now, refresh out of band." },
    { label: "A hard miss, because 90s is past the 60s max-age.", misconception: "It only becomes a hard miss after max-age + SWR window (60+300=360s). Between 60s and 360s it is stale-but-serveable." },
    { label: "Fresh data, because stale-while-revalidate revalidates synchronously before responding.", misconception: "The 'while' means the refresh runs while the stale response is already on its way. Synchronous revalidation is exactly what SWR removes." },
  ]}
/>

<Quiz
  id="06-swr-choice-2"
  lessonSlug="06-swr"
  lang="en"
  question="Why does max-age alone produce a per-TTL p99 sawtooth, and how does stale-while-revalidate flatten it?"
  choices={[
    { label: "With max-age, the first request after expiry blocks on a full origin round-trip to refill the entry; at high request rates many users hit each boundary and are sampled into p99. SWR serves those requests stale instantly and refreshes in the background, so the foreground miss — and its spike — disappears.", correct: true },
    { label: "max-age is slow because conditional revalidation runs on every request; SWR caches the response so it does not.", misconception: "max-age does NOT revalidate every request — it serves from cache until expiry. The spike comes only at the TTL boundary, where the refill blocks a live user. That is what SWR moves out of the request path." },
    { label: "The sawtooth is origin jitter; SWR smooths it by averaging several origin responses.", misconception: "The spike is not origin variance — it is the synchronous refill landing in a user's request. SWR does not average anything; it just stops a human from waiting on the refresh." },
    { label: "SWR lowers the spike by shortening max-age so each miss is cheaper.", misconception: "SWR does not change max-age or per-miss cost; it changes WHO pays the miss — a background fetch instead of a live user. The origin round-trip is the same size, just nobody waits on it." },
  ]}
/>

<Quiz
  id="06-swr-choice-3"
  lessonSlug="06-swr"
  lang="en"
  question="A popular key expires under 10k req/s and the edge fires one background revalidation per stale-hit. What breaks and what is the fix?"
  choices={[
    { label: "A thundering herd of background refreshes hits the origin at once — a self-inflicted stampede. Fix with single-flight: the first stale request triggers exactly one refresh, everyone else is served stale until it lands; add jitter so keys do not revalidate in lockstep.", correct: true },
    { label: "Nothing — background refreshes are free because no user is waiting.", misconception: "The user is not waiting, but the origin still receives every refresh. Thousands of concurrent background fetches can melt the origin just like a foreground stampede." },
    { label: "The stale value is served forever; fix it by removing the SWR window.", misconception: "Removing SWR brings back the synchronous-revalidation spike. The herd is about coalescing concurrent refreshes, not about the staleness window." },
    { label: "Users start getting 503s; fix it by raising max-age.", misconception: "Users get stale-but-fast responses, not errors. Raising max-age does not stop concurrent background fetches stampeding the origin." },
  ]}
/>

<Quiz
  id="06-swr-choice-4"
  lessonSlug="06-swr"
  lang="en"
  question="What does stale-if-error add on top of stale-while-revalidate, and why is pairing them the senior default?"
  choices={[
    { label: "stale-if-error keeps serving the last good copy when the origin returns 5xx or is unreachable, for its own window — the failure-path tool. SWR alone covers the happy path but still hands the user an error once the origin breaks and the SWR window lapses, so pairing them turns an outage into degraded-but-up.", correct: true },
    { label: "They are the same directive under two names, so you only need one.", misconception: "They are independent and mirror images: stale-while-revalidate is for a healthy origin you do not want users to wait on; stale-if-error is for a sick origin where stale beats a 503." },
    { label: "stale-if-error makes revalidation synchronous so errors are caught before responding.", misconception: "Neither directive blocks the user. stale-if-error simply changes what happens when the background refresh FAILS — keep serving stale instead of propagating the error." },
    { label: "stale-if-error shortens the SWR window to reduce maximum staleness.", misconception: "It adds a separate, usually longer, failure-only window. It does not touch the SWR window or normal staleness — it is a floor under outages." },
  ]}
/>

<Quiz
  id="06-swr-choice-5"
  lessonSlug="06-swr"
  lang="en"
  question="Two components call useSWR('/api/user') in the same render tick. What does the SWR client library do, and what governs it?"
  choices={[
    { label: "It makes one request and both components receive the same result — request coalescing / single-flight. Repeat calls for the same key inside dedupingInterval (default 2000ms) collapse onto the in-flight promise. React Query does the same; its analogous staleTime defaults to 0.", correct: true },
    { label: "It fires two requests and uses whichever returns first.", misconception: "Duplicate calls for the same key are coalesced onto one in-flight request inside dedupingInterval — that is the whole point of the client cache layer." },
    { label: "It blocks the second component until the first request resolves, then refetches.", misconception: "There is no blocking and no second fetch. Both components subscribe to the single in-flight promise and get the same cached result." },
    { label: "It serves a spinner to both because the key has never been fetched.", misconception: "The question is about deduplication of concurrent calls, not first-load UI. SWR coalesces them into one request regardless of whether the key was seen before." },
  ]}
/>

<Quiz
  id="06-swr-choice-6"
  lessonSlug="06-swr"
  lang="en"
  question="For which response is stale-while-revalidate the wrong tool, and why?"
  choices={[
    { label: "An authorization or permission decision (or a feature flag that gates access). Serving a revoked user a few-minutes-stale 'you still have access' is a shipped security regression — security-sensitive, per-user responses need short synchronous validated freshness, or no caching at all.", correct: true },
    { label: "A product-listing API where catalogue data can be a few minutes stale.", misconception: "That is the textbook SWR fit — bounded staleness is harmless and the p99 sawtooth is the real cost. SWR is exactly right here." },
    { label: "An analytics tile refreshed every few minutes on a dashboard.", misconception: "Slightly-stale analytics is fine; SWR serves it instantly and self-corrects in the background. Nothing about it is a correctness or security hazard." },
    { label: "A public blog post or article body.", misconception: "Static-ish content tolerates staleness perfectly; SWR is an ideal fit. The wrong target is data where stale equals a security or correctness bug, like auth." },
  ]}
/>

<Recap lang="en">
The through-line is one contract: return what you have now, fetch what is correct next, never block the human on the refresh. max-age sets freshness, the SWR window kills the synchronous-revalidation sawtooth, stale-if-error turns an outage into degraded-but-up, single-flight plus jitter stops the background-refresh herd, and the client libraries reproduce the same model with dedupingInterval and focus/reconnect revalidation. The one hard boundary: never apply SWR to auth, permissions, or anything where stale data is a security bug.
</Recap>
