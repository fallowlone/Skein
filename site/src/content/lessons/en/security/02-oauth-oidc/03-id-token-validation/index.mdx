---
slug: 03-id-token-validation
lang: en
track: security
unit: 02-oauth-oidc
order: 3
title: "ID token validation and JWKS cache management"
summary: "The eight mandatory checks every OIDC client must perform on id_tokens, why JWKS cache staleness causes auth outages, and the on-cache-miss refresh pattern."
estMin: 9
status: ready
lessonType: topic
level: middle
prereqs:
  - security/02-oauth-oidc/02-auth-code-pkce
deepensInto:
  - security/02-oauth-oidc/04-refresh-tokens-and-scopes
  - security/02-oauth-oidc/05-sender-constrained-tokens
spiral: ["statefulness"]
concepts: ["id-token", "jwks", "jwt-validation", "kid", "iss", "aud", "nonce", "clock-skew"]
sources:
  - https://openid.net/specs/openid-connect-core-1_0.html
  - https://datatracker.ietf.org/doc/html/rfc7636
  - https://auth0.com/docs/get-started/authentication-and-authorization-flow/authorization-code-flow-with-pkce
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";

<Hook>
An IdP rotates its signing key. Your app's JWKS cache has a 6-hour TTL. For the next 4 hours, every new user gets a 401 — their id_token is signed with the new key, which your cache does not know about. Signature validation fails on a token that is completely valid.
</Hook>

<Crux>Skip one of eight mandatory id_token checks and you have a CVE. JWKS cache staleness is the most common production outage.</Crux>

<Explanation>

## The eight mandatory id_token checks

When your app receives an id_token from an OIDC authorization server, it must validate all eight of the following before trusting the user's identity. Skipping any one is a documented CVE category.

**1. Parse the JWT.** Split `header.payload.signature`. Base64url-decode each part.

**2. Read `kid` from the JWT header.** The `kid` (Key ID) field names which public key signed this token. Without it you cannot look up the right key.

**3. Fetch the public key from JWKS.** Call the IdP's `/.well-known/jwks.json` endpoint (cached, TTL 5–15 min). Find the key whose `kid` matches. On cache miss, refresh immediately before failing.

**4. Verify the signature.** Use the algorithm declared in the header (must be RS256 or ES256 — never accept HS256 with an asymmetric key, never accept `alg: none`). Signature failure → reject. **Pin the algorithm server-side; never trust the token's own `alg` claim.**

**5. Validate `iss`.** Must exactly match the expected issuer URL from your configuration. An attacker could issue a token from their own IdP with a valid signature.

**6. Validate `aud`.** Must contain your `client_id`. Without this check, a token issued for a different application is accepted by yours.

**7. Validate `exp` and `iat`.** `exp` must be in the future; `iat` must be in the past. Allow ~5 minutes of clock skew (300 seconds is the typical tolerance). Both checks use the server's clock, not the client's.

**8. Validate `nonce`.** Must match the nonce the client sent in the `/authorize` request. Without this check, a captured id_token can be replayed into a different session.

<div data-lesson-visual class="overflow-x-auto my-6 rounded-lg border border-border bg-surface-1 p-4 text-sm">
  <div class="mb-3 text-ink-2 text-xs uppercase tracking-wider">id_token validation sequence</div>
  <div class="space-y-1">
    {[
      ["Parse JWT", "Split header.payload.signature"],
      ["kid lookup", "Read key ID from header → find key in JWKS"],
      ["Signature", "Verify with public key; reject alg=none or wrong type"],
      ["iss", "Must match expected issuer (exact string)"],
      ["aud", "Must contain YOUR client_id"],
      ["exp / iat", "exp in future, iat in past — 300s clock skew allowed"],
      ["nonce", "Must match nonce sent in /authorize"],
    ].map(([step, detail], i) => (
      <div class="flex gap-2 items-start">
        <span class={`shrink-0 w-6 h-6 rounded-full text-white text-xs flex items-center justify-center font-bold ${i < 3 ? 'bg-blue-500' : i < 6 ? 'bg-orange-500' : 'bg-green-500'}`}>{i + 1}</span>
        <div><span class="font-medium text-ink-1">{step}</span><span class="text-ink-2 ml-2">{detail}</span></div>
      </div>
    ))}
  </div>
</div>

## JWKS cache staleness: the production failure mode

The IdP's JWKS endpoint lists its current signing public keys. Your app caches this list (fetching it on every request would be too slow). When the IdP rotates signing keys, it publishes the new key — but your cache still has the old one.

**What breaks:** new tokens are signed with the new key. Your validator reads the `kid` from the token header, looks in the cache — not found. Signature validation fails. Users cannot log in, even though their tokens are completely valid.

**The fix — on-cache-miss refresh:** When a `kid` is not found in the cache, immediately fetch fresh JWKS from `/.well-known/jwks.json` and retry the lookup. RFC 7517 explicitly allows this. Only fail if the `kid` is still absent after the refresh.

**Long-term mitigation:** Short cache TTL (5–15 minutes) plus on-miss refresh as a backstop. Mature IdPs (Auth0, Okta) publish new keys 24–48 hours before activation so clients can pre-warm caches. If your IdP does not, short TTL + on-miss refresh is the only reliable defense.

<Quiz
  id="03-q1"
  pieceSlug="03-id-token-validation"
  lang="en"
  question="An app validates id_token signature and iss correctly but skips the aud check. What attack is enabled?"
  choices={[
    { label: "PKCE downgrade", misconception: "PKCE downgrade is about code exchange, unrelated to aud." },
    { label: "Audience confusion: a token issued for a different application is accepted, letting an attacker who obtained a token for any other client access this app as the victim", correct: true },
    { label: "JWT algorithm confusion", misconception: "Algorithm confusion is about alg header manipulation, not aud." },
    { label: "Nonce replay", misconception: "Nonce replay requires skipping the nonce check, not the aud check." },
  ]}
/>

<Quiz
  id="03-q2"
  pieceSlug="03-id-token-validation"
  lang="en"
  question="Why should you never trust the 'alg' claim in the JWT header?"
  choices={[
    { label: "The header is not base64-encoded", misconception: "The header is base64url-encoded and readable. The issue is trusting it." },
    { label: "Algorithm confusion attacks change the alg to 'none' or HS256, making the validator use a wrong verification path — pinning alg server-side prevents this", correct: true },
    { label: "The alg claim is always empty", misconception: "The alg claim is present; the problem is letting the token dictate which algorithm to use." },
    { label: "GDPR forbids reading JWT headers", misconception: "GDPR does not govern JWT header trust." },
  ]}
/>

<DragOrder
  id="03-drag-1"
  pieceSlug="03-id-token-validation"
  lang="en"
  prompt="Put the id_token validation steps in the correct order:"
  items={[
    "Parse the JWT — split header.payload.signature",
    "Read the kid (key ID) from the JWT header",
    "Fetch the matching public key from the issuer's JWKS endpoint (cached, refresh on miss)",
    "Verify the signature using the public key and the pinned expected algorithm",
    "Validate the iss claim matches the expected issuer",
    "Validate the aud claim contains the application's client_id",
    "Validate exp is in the future and iat is in the past (allow ~300s clock skew)",
    "Validate the nonce claim matches the nonce sent in /authorize",
  ]}
/>

</Explanation>

<KeyTakeaway>Eight checks, in order — skip one and you have a CVE. JWKS staleness causes the most common production outage: short TTL (5–15 min) plus on-cache-miss refresh solves it. Pin the expected algorithm server-side; never read alg from the token.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-id-token-validation-retrieval"
  lang="en"
  questions={[
    { q: "What is the on-cache-miss JWKS refresh pattern, and why is it required?", a: "When a kid in the JWT header is not found in the local JWKS cache, immediately fetch fresh JWKS from /.well-known/jwks.json and retry. This handles IdP key rotation: the new key exists on the IdP but not yet in your cache. Without this pattern, the first wave of tokens signed by the new key cause a 401 storm until the cache TTL expires." },
    { q: "Why must the aud claim contain YOUR client_id, not just any client_id?", a: "A token is minted for a specific audience. If your validator accepts any valid aud, an attacker who obtained a token for application X can present it to application Y. The aud check must verify the token was intended for this specific resource server — validated against a hard-coded expected value, never derived from the request." },
    { q: "An app skips the exp/iat check for performance. What is the blast radius of a stolen id_token?", a: "Without exp/iat checks, a stolen id_token is valid indefinitely — no expiry enforcement. The attacker replays it days or months later. The blast radius is the user's entire account at the app for as long as the IdP's signing key remains valid (typically years). Short-lived id_tokens (TTL 1–24 hours) combined with exp validation limit the window to the token's lifetime." },
  ]}
/>

<Recap lang="en">
OIDC id_token validation has eight mandatory steps. Signature verification requires the correct public key from JWKS, selected by `kid`, with a server-side pinned algorithm — never one read from the token. The `iss` and `aud` claims prevent cross-issuer and cross-audience attacks. The `exp`/`iat` checks enforce freshness. The `nonce` check prevents replay. JWKS cache staleness during key rotation is the most common production failure: on-cache-miss refresh plus a short TTL (5–15 minutes) is the standard mitigation.
</Recap>
