---
concepts:
- pkce
- authorization-code
- code-verifier
- code-challenge
- state
- nonce
- redirect-uri
deepensInto:
- security/02-oauth-oidc/03-id-token-validation
- security/02-oauth-oidc/04-refresh-tokens-and-scopes
estMin: 10
lang: en
lessonType: topic
level: middle
order: 2
prereqs:
- security/02-oauth-oidc/01-what-is-oauth
slug: 02-auth-code-pkce
sources:
- https://datatracker.ietf.org/doc/html/rfc7636
- https://datatracker.ietf.org/doc/html/rfc9700
- https://auth0.com/docs/get-started/authentication-and-authorization-flow/authorization-code-flow-with-pkce
spiral:
- statefulness
- encapsulation
status: ready
summary: How OAuth 2.1''''s mandatory PKCE prevents code interception, why exact-match
  redirect URIs block open-redirect exfiltration, and what state and nonce defend
  against.
title: Authorization code flow with PKCE
track: security
unit: 02-oauth-oidc
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";

<Hook>
An attacker monitors a mobile app's outgoing requests and captures the one-time authorization code in the redirect URL. Without PKCE, they exchange the code for tokens and own the account. PKCE is the lock that makes a stolen code worthless.
</Hook>

<Crux>PKCE binds a code to its client. Stolen code is useless without the verifier that never leaves the client.</Crux>

<Explanation>

## How PKCE works — step by step

PKCE (Proof Key for Code Exchange, RFC 7636) adds a cryptographic challenge to the authorization code flow. OAuth 2.1 mandates it for every client, including confidential server-side apps.

**Before the redirect:**
1. Client generates a `code_verifier` — a random 43–128 character URL-safe string (cryptographically random).
2. Client computes `code_challenge = base64url(SHA-256(code_verifier))`.
3. Client sends the `code_challenge` (but NOT the `code_verifier`) to the authorization server in the `/authorize` request.

**After user consent:**
4. Authorization server returns a one-time `code` in the redirect. It stores the `code_challenge` alongside the code.

**Token exchange:**
5. Client POSTs to `/token` with the `code` AND the original `code_verifier`.
6. Authorization server hashes the `code_verifier`, compares to the stored `code_challenge`. Match → tokens issued. No match → request rejected.

An attacker who intercepts the code in step 4 cannot redeem it — they need `code_verifier`, which was never sent over the wire.

<div data-lesson-visual class="overflow-x-auto my-6 rounded-lg border border-border bg-surface-1 p-4 text-sm font-mono">
  <div class="mb-3 text-ink-2 text-xs uppercase tracking-wider">PKCE code binding</div>
  <div class="space-y-2">
    <div class="flex gap-2 items-start">
      <span class="shrink-0 w-6 h-6 rounded-full bg-blue-500 text-white text-xs flex items-center justify-center font-bold">1</span>
      <span>Client generates <code>code_verifier</code> (random 43–128 chars, stays local)</span>
    </div>
    <div class="flex gap-2 items-start">
      <span class="shrink-0 w-6 h-6 rounded-full bg-blue-500 text-white text-xs flex items-center justify-center font-bold">2</span>
      <span>Client computes <code>code_challenge = base64url(SHA-256(verifier))</code></span>
    </div>
    <div class="flex gap-2 items-start">
      <span class="shrink-0 w-6 h-6 rounded-full bg-blue-500 text-white text-xs flex items-center justify-center font-bold">3</span>
      <span><code>/authorize</code> request carries <code>code_challenge</code> (hash only, verifier stays secret)</span>
    </div>
    <div class="flex gap-2 items-start">
      <span class="shrink-0 w-6 h-6 rounded-full bg-green-500 text-white text-xs flex items-center justify-center font-bold">4</span>
      <span>Auth server stores challenge → issues code in redirect URL</span>
    </div>
    <div class="flex gap-2 items-start">
      <span class="shrink-0 w-6 h-6 rounded-full bg-orange-500 text-white text-xs flex items-center justify-center font-bold">5</span>
      <span><code>/token</code> request carries code + <code>code_verifier</code> — auth server hashes, compares, mints tokens</span>
    </div>
  </div>
  <div class="mt-3 text-ink-2 text-xs border-t border-border pt-2">Attacker gets the code in step 4 but not the verifier → /token call rejected.</div>
</div>

## The exact-match redirect URI rule

OAuth 2.1 requires the `redirect_uri` in the token request to exactly match a pre-registered URI — no wildcards, no path prefixes, no partial matches.

Without this rule: an attacker can trigger a flow with `redirect_uri=https://yourapp.com/comments?next=https://attacker.com`. If your app has any open-redirect on `/comments`, the authorization code lands on the attacker's server. The exact-match rule shuts this down: the URI must be character-for-character identical to a registered value.

For native apps where you cannot reliably register a URL (deep links, custom URI schemes), OAuth 2.1 + RFC 8252 require either `http://127.0.0.1:RANDOM_PORT` (loopback) or a private-use URI scheme registered with the OS.

## State and nonce: two separate anti-attack tokens

**State** is OAuth's anti-CSRF: the client generates a random value at `/authorize` time, the server echoes it back in the redirect, the client verifies it matches. Without state, an attacker can send a victim a crafted `/authorize` URL pointing to the attacker's redirect — on click, the victim's browser silently completes the flow under the attacker's account.

**Nonce** is OIDC's anti-replay: the client sends a random value in `/authorize`; the IdP embeds it inside the `id_token`. The client verifies the nonce in the token matches what it sent. Without nonce, an attacker who captures an `id_token` from another channel can replay it to a different session.

Both are random per-request. Both are validated server-side. Neither is optional in modern flows.

<Quiz
  id="02-q1"
  pieceSlug="02-auth-code-pkce"
  lang="en"
  question="Why does OAuth 2.1 mandate PKCE for ALL clients, including confidential server-side clients?"
  choices={[
    { label: "PKCE replaces the client_secret", misconception: "PKCE is in addition to client_secret for confidential clients, not a replacement." },
    { label: "It defends against authorization-code interception even when client_secret is absent or leaked", correct: true },
    { label: "Required by GDPR", misconception: "GDPR does not require PKCE. OAuth 2.1 mandates it for defense in depth." },
    { label: "Makes tokens shorter", misconception: "PKCE adds ~50 bytes per request; it is not a size optimization." },
  ]}
/>

<Quiz
  id="02-q2"
  pieceSlug="02-auth-code-pkce"
  lang="en"
  question="A client validates the id_token signature and iss, but skips the nonce check. What attack is enabled?"
  choices={[
    { label: "Open redirect", misconception: "Open redirect is about redirect_uri, not nonce." },
    { label: "ID-token replay: an attacker who captures an id_token can reuse it at a different session, impersonating the victim", correct: true },
    { label: "PKCE downgrade", misconception: "PKCE downgrade is about challenge methods, unrelated to nonce." },
    { label: "Token introspection bypass", misconception: "Introspection is a server-side trust mechanism, unrelated to nonce." },
  ]}
/>

<DragOrder
  id="02-drag-1"
  pieceSlug="02-auth-code-pkce"
  lang="en"
  prompt="Put the full authorization code + PKCE flow in order:"
  items={[
    "Client generates code_verifier and code_challenge (SHA-256 of verifier)",
    "Client redirects browser to /authorize with code_challenge, state, nonce, client_id, redirect_uri",
    "User authenticates at the authorization server and approves scopes",
    "Authorization server redirects to redirect_uri with code and state",
    "Client verifies state matches; POSTs to /token with code + code_verifier",
    "Authorization server hashes verifier, compares to stored challenge, issues access + ID tokens",
    "Client validates ID token (signature, iss, aud, exp, nonce) and reads user identity",
  ]}
/>

</Explanation>

<KeyTakeaway>PKCE binds a code to the client that generated it — interception is useless without the verifier. Exact-match redirect URIs block open-redirect code exfiltration. State prevents CSRF; nonce prevents ID-token replay. OAuth 2.1 requires all three.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-auth-code-pkce-retrieval"
  lang="en"
  questions={[
    { q: "What does PKCE prevent, and how?", a: "PKCE prevents authorization-code interception attacks. The client generates a secret code_verifier, sends only its SHA-256 hash (code_challenge) to the authorization server, then proves ownership at /token by sending the original verifier. An attacker who steals the code cannot redeem it without the verifier, which was never sent over the network." },
    { q: "Why does OAuth 2.1 require exact-match redirect URIs?", a: "Without exact match, an attacker can chain the authorization flow into an open-redirect bug on the app's domain — redirecting the code to an attacker-controlled URL. Exact match ensures the authorization server will only redirect to a URI the developer explicitly registered, character for character." },
    { q: "What is the difference between the state parameter and the nonce parameter?", a: "State is an anti-CSRF value for the OAuth flow: the client generates it, the server echoes it in the redirect, the client verifies it matches. Nonce is an OIDC anti-replay value: the client generates it, the IdP embeds it in the id_token, the client verifies the id_token's nonce matches what it sent. State protects the redirect flow; nonce protects the id_token." },
  ]}
/>

<Recap lang="en">
The authorization code flow with PKCE is the only grant type allowed for user-facing apps in OAuth 2.1. PKCE prevents code interception: the verifier never leaves the client, so a stolen code cannot be redeemed. Exact-match redirect URIs prevent open-redirect code exfiltration. The state and nonce parameters close two independent attack windows — CSRF on the flow and replay on the ID token. All three are mandatory in OAuth 2.1; skipping any one is a documented CVE vector.
</Recap>
