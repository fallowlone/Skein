---
concepts:
- ambient-authority
- samesite-cookies
- synchronizer-token
- double-submit-cookie
- origin-referer-check
- defense-in-depth
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Guides/Cookies
- https://web.dev/articles/samesite-cookies-explained
- https://portswigger.net/web-security/csrf/bypassing-samesite-restrictions
spiral: []
status: ready
summary: The browser auto-attaches cookies to cross-site requests, so a malicious
  page can trigger authenticated state changes. SameSite=Lax (Chrome''''s 2020 default)
  shrank the surface — but GET state-changes, SameSite=None, and the top-level-navigation
  gap keep CSRF alive.
title: 'CSRF in the SameSite era: why the default helped but didn''''''''t kill it'
track: security
unit: 04-csrf
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";
import PacketDot from "~/components/diagram/PacketDot.astro";

<Hook>
A bank ships a "transfer funds" endpoint and feels safe — sessions are cookie-based, HTTPS everywhere, login is solid. Months later a user clicks a link in a forum post. The linked page silently auto-submits a hidden form to `POST /transfer` with `amount=5000&to=attacker`. The browser does what browsers do: it attaches the user's session cookie to the cross-site request. The server sees a valid, authenticated session and moves the money. No password was stolen, no session was hijacked. The attacker never saw the response — they didn't need to. The side effect already happened.
</Hook>

<Crux>CSRF rides the browser's auto-attached cookies; SameSite=Lax shrank the attack surface in 2020 but didn't close it.</Crux>

<Explanation>

## The mechanism: ambient authority

In ten minutes you will know exactly why a valid, fully-authenticated request can arrive at your server without the user ever intending to send it — and which defenses actually stop that.

CSRF exists because of one browser behavior: cookies are *ambient authority*. The browser attaches a site's cookies to every request to that site's origin — no matter who initiated the request. A form on `evil.com` that posts to `bank.com` still gets the bank's session cookie attached, because the browser keys cookies by destination, not by who started the navigation. The attacker doesn't need to read the cookie or steal it (HttpOnly stops that, and is irrelevant here). They only need to make the victim's browser *send* a request. The authentication comes along for free.

That's the whole trick, and it's why CSRF is sometimes called "the confused deputy": the browser is a deputy acting with the user's authority, confused into using that authority on the attacker's behalf. Crucially, the attacker cannot read the response — the same-origin policy blocks that. CSRF is a *write* attack, not a read attack. It works on any endpoint where the side effect alone is the goal: transfer money, change email, delete account, promote a user to admin.

<SequenceDiagram
  label="The CSRF attack in three steps: the attacker page auto-submits a forged request through the victim's browser, the browser automatically attaches the victim's session cookie, and the target server executes the state change as the victim."
  hue="var(--d-crypto)"
  actors={["Attacker page", "Victim browser", "Target server"]}
  messages={[
    { from: "Attacker page", to: "Victim browser", label: "auto-submit hidden form" },
    { from: "Victim browser", to: "Target server", label: "POST /transfer + session cookie" },
    { from: "Target server", to: "Victim browser", label: "executes transfer as victim" },
  ]}
  caption="The victim only loaded a page. The browser auto-attaches the bank's cookie to the forged POST, so the server sees a valid authenticated request and moves the money — the attacker never reads the response, the side effect is the goal."
/>

The cookie is never stolen — it simply rides along. The animation below shows the victim's session cookie auto-attaching to the forged request as it travels from the attacker's page to the target server:

<DiagramFrame
  label="The forged request travels from the attacker page through the victim's browser to the target server; a moving dot represents the session cookie being auto-attached by the browser and carried along to the server."
  hue="var(--d-crypto)"
  caption="The browser attaches the cookie by destination, not by who started the request — so the attacker's forged POST arrives fully authenticated."
>
  <svg viewBox="0 0 460 120" width="460" height="120" role="presentation" style="display:block;margin:0 auto;max-width:100%;height:auto;font-family:var(--font-body);">
    {/* path the cookie+request travels */}
    <path data-path d="M70 60 L230 60 L390 60" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" stroke-dasharray="4 4" />
    {/* attacker page */}
    <rect x="8" y="36" width="118" height="48" rx="7" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="67" y="58" text-anchor="middle" font-size="12" fill="var(--ink)">Attacker page</text>
    <text x="67" y="73" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--muted)">evil.com</text>
    {/* victim browser */}
    <rect x="171" y="36" width="118" height="48" rx="7" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="230" y="58" text-anchor="middle" font-size="12" fill="var(--ink)">Victim browser</text>
    <text x="230" y="73" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--muted)">+ cookie</text>
    {/* target server */}
    <rect x="334" y="36" width="118" height="48" rx="7" fill="var(--card-2)" stroke="var(--hairline-strong)" stroke-width="0.8" />
    <text x="393" y="58" text-anchor="middle" font-size="12" fill="var(--ink)">Target server</text>
    <text x="393" y="73" text-anchor="middle" font-size="10" font-family="var(--font-mono)" fill="var(--muted)">bank.com</text>
    <text x="230" y="22" text-anchor="middle" font-size="10.5" font-family="var(--font-mono)" fill="var(--d)">forged POST + session cookie →</text>
    <PacketDot color="var(--d-crypto)" durationMs={2200} size={9} />
  </svg>
</DiagramFrame>

## SameSite=Lax: the 2020 default that changed everything

In February 2020, Chrome 80 made `SameSite=Lax` the default for any cookie that doesn't specify the attribute. Firefox and Edge followed. This was the single biggest blow to classic CSRF in the web's history. A `Lax` cookie is **not** sent on cross-site subrequests — the `evil.com` form posting to `bank.com` no longer gets the cookie attached, so the attack above is dead by default for a `POST`.

But "Lax" is not "Strict," and the gap is deliberate. `Lax` *does* send the cookie on **top-level navigations using safe methods** — when the user clicks a link and the URL bar changes to your site, the cookie comes along (otherwise every inbound link would log you out). Two consequences follow directly, and both are live CSRF vectors.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Request</th>
        <th class="border border-border px-3 py-2 text-left">SameSite=Lax sends cookie?</th>
        <th class="border border-border px-3 py-2 text-left">CSRF risk</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Cross-site <code>POST</code> form / fetch</td>
        <td class="border border-border px-3 py-2">No</td>
        <td class="border border-border px-3 py-2">Blocked by default — the big win</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Top-level link click → cross-site <code>GET</code></td>
        <td class="border border-border px-3 py-2">Yes</td>
        <td class="border border-border px-3 py-2">Open if any state-change is reachable via <code>GET</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Cookie set <code>SameSite=None; Secure</code></td>
        <td class="border border-border px-3 py-2">Yes, everywhere</td>
        <td class="border border-border px-3 py-2">All SameSite protection disabled by design</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>POST</code> within 120s of cookie set (Chrome Lax+POST)</td>
        <td class="border border-border px-3 py-2">Yes (default-Lax only)</td>
        <td class="border border-border px-3 py-2">~2-minute window after login</td>
      </tr>
    </tbody>
  </table>
</div>

## Why CSRF is not dead

**GET-based state changes.** If `GET /account/delete` or `GET /transfer?to=x&amount=5000` actually mutates data, `Lax` happily sends the cookie on a top-level navigation. An attacker just needs the victim to click a link (or land on a page that redirects). This is why "never mutate on `GET`" is a real security rule, not a REST style preference.

**SameSite=None.** Any site that legitimately needs cross-site cookies — embedded widgets, third-party SSO, an API consumed from another origin — must set `SameSite=None; Secure`, which turns *all* SameSite protection off for that cookie. The moment you opt into cross-site, you're back to pre-2020 and need a real token defense.

**The Lax+POST window.** Chrome's *default*-Lax (not explicit `SameSite=Lax`) keeps a compatibility exception: a brand-new cookie is sent on a top-level cross-site `POST` for the first 120 seconds, to avoid breaking some SSO redirects. That's a ~2-minute CSRF window right after the cookie is set. Setting `SameSite=Lax` explicitly removes this exception.

**Inconsistent enforcement.** Default-Lax is enforced by the *browser*. Older browsers, embedded WebViews, and some non-Chromium clients don't all behave identically, and you cannot assume the server is protected by a client-side default. SameSite is a defense-in-depth layer, never the only lock.

<Inset kind="why" lang="en">
There's a subtle distinction that bites people: the protection differs between a cookie with **no** SameSite attribute (browser applies *default*-Lax, which includes the 120s Lax+POST exception) and a cookie with an **explicit** `SameSite=Lax` (no exception, stricter). If you rely on SameSite at all, set it explicitly. Letting the browser's default carry your security means inheriting its compatibility hacks.
</Inset>

## The defenses that still matter

When you audit a cookie-session app, what do you actually put in front of a state-changing route? SameSite shrinks the surface; it does not replace a token defense for any endpoint that matters. Two server-side patterns remain the backbone:

- **Synchronizer token pattern.** The server generates a per-session (or per-request) cryptographically random token, stores it server-side, and embeds it in every form / sends it in a custom header. The attacker's cross-site page can't read it (same-origin policy) and can't guess it. The server rejects any state-changing request whose token doesn't match. Stateful, strongest, the OWASP default for server-rendered apps.
- **Double-submit cookie.** Stateless: the server sets a random value in a cookie *and* expects the same value echoed in a header / form field. The attacker can't read your cookie to copy it into the header, so the two won't match. Cheaper (no server storage) but weaker — vulnerable if an attacker can write cookies for your domain (e.g. a subdomain XSS), which is why OWASP now recommends the **HMAC signed** variant that binds the token to the session.

Add a **defense-in-depth** layer on sensitive endpoints: verify the `Origin` header (and fall back to `Referer`) matches your own origin, rejecting requests from foreign origins. And for JSON APIs, requiring a custom header (e.g. `X-Requested-With`) plus a non-simple content type forces a CORS *preflight* — which the attacker's cross-site `fetch` cannot satisfy without your CORS policy allowing it. That's a partial mitigation, not a full token defense, because top-level form posts and some content types skip preflight.

Together, these layers mean no single failure opens the door: SameSite cuts the surface, the token provides positive proof of intent, and Origin/Referer stops anything that slips through the cracks. Without the token, SameSite alone lets GET side effects and None-cookies through; without SameSite, every POST is exposed until the token check runs.

<TradeoffMatrix
  id="04-csrf-overview-tradeoff-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="A server-rendered banking app with cookie sessions needs CSRF protection for its state-changing forms. Pick the primary defense."
  options={[
    { name: "Synchronizer token pattern (per-session random token, stored server-side, embedded in every form) plus explicit SameSite=Lax and an Origin check", summary: "Token + defense-in-depth.", correct: true, justification: "This is the OWASP-recommended backbone for server-rendered apps. The token is unguessable and unreadable cross-site (same-origin policy), so a forged request can't include a valid one; SameSite=Lax cuts the surface before the token check even runs; the Origin check catches anything that slips through. Layered, and no single failure unlocks the endpoint." },
    { name: "Rely on default SameSite=Lax alone — Chrome made it the default in 2020, so cross-site POSTs are blocked", summary: "Browser default only.", justification: "SameSite is defense-in-depth, not a complete defense. Default-Lax still sends cookies on top-level GET navigations (so any GET state-change is exposed), keeps the 120s Lax+POST window, and is only enforced by the browser — older clients and WebViews vary. Never let a client-side default be your only lock." },
    { name: "Double-submit cookie alone — it's stateless and easy to deploy", summary: "Stateless token.", justification: "Reasonable for stateless APIs, but weaker as a sole defense: if an attacker can set a cookie for your domain (e.g. a subdomain XSS or cookie injection), they can make the two values match. OWASP now recommends the HMAC-signed variant bound to the session. For a stateful banking app, the synchronizer token is stronger." },
    { name: "Check the Referer header only and reject anything missing it", summary: "Referer-only.", justification: "Origin/Referer checks are a good defense-in-depth layer but a poor sole defense: Referer can be stripped by privacy settings, proxies, or a referrer-policy, so legitimate requests get blocked while you've added no positive proof of intent. Use it alongside a token, not instead of one." },
  ]}
/>

<Quiz
  id="04-csrf-overview-quiz-1"
  lessonSlug="01-overview"
  lang="en"
  question="Why does HttpOnly NOT protect against CSRF?"
  choices={[
    { label: "CSRF doesn't need to read the cookie — the browser auto-attaches it to the request; HttpOnly only blocks JavaScript from reading it", correct: true },
    { label: "HttpOnly is deprecated in modern browsers", misconception: "HttpOnly is alive and useful — but for XSS (stopping script from stealing the cookie), a different attack class than CSRF." },
    { label: "HttpOnly only works over HTTPS", misconception: "That's the Secure flag. HttpOnly governs JavaScript access to the cookie, not the transport, and neither stops the browser from auto-sending the cookie cross-site." },
    { label: "It does protect — HttpOnly stops the forged request entirely", misconception: "CSRF is a confused-deputy write attack: the attacker makes the browser send a request with the cookie already attached. Reading the cookie was never required." },
  ]}
/>

<Quiz
  id="04-csrf-overview-quiz-2"
  lessonSlug="01-overview"
  lang="en"
  question="Your cookie sessions are SameSite=Lax. Which endpoint is still exposed to CSRF?"
  choices={[
    { label: "GET /account/close that actually closes the account — Lax sends cookies on top-level GET navigations", correct: true },
    { label: "A cross-site POST /transfer from a hidden form on evil.com", misconception: "This is exactly what Lax blocks — cookies aren't sent on cross-site POST subrequests, so the forged POST arrives without a session." },
    { label: "A cross-site fetch() with a JSON body", misconception: "Lax blocks the cookie on cross-site fetch, and a non-simple JSON request triggers a CORS preflight the attacker can't satisfy. The GET state-change is the real hole." },
    { label: "An image tag pointing at a cross-site POST endpoint", misconception: "Image loads are GETs and are subrequests, not top-level navigations; Lax won't attach the cookie. The exposed case is a GET that mutates on top-level navigation." },
  ]}
/>

<DragOrder
  id="04-csrf-overview-drag-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="Order the layers of a modern CSRF defense, from the broadest cheap filter to the strongest positive proof:"
  items={[
    { id: "a", label: "Never mutate state on GET — keep all side effects on POST/PUT/PATCH/DELETE" },
    { id: "b", label: "Set cookies explicitly SameSite=Lax (or Strict) — cuts the cross-site surface" },
    { id: "c", label: "Verify the Origin header (fall back to Referer) matches your own origin" },
    { id: "d", label: "Require a synchronizer or HMAC double-submit CSRF token on every state-change" },
  ]}
  correctOrder={["a", "b", "c", "d"]}
/>

</Explanation>

<KeyTakeaway>SameSite=Lax (Chrome's 2020 default) blocks cross-site POSTs but leaves GET state-changes, SameSite=None, and the 120s Lax+POST gap. Layer it with an explicit CSRF token and an Origin check.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-csrf-overview-retrieval"
  lang="en"
  questions={[
    { q: "Explain why CSRF works even though the attacker never sees the response and never reads the session cookie.", a: "CSRF exploits ambient authority: the browser automatically attaches a site's cookies to every request bound for that site's origin, regardless of who initiated the request. So a hidden form or auto-submitting page on evil.com that posts to bank.com gets the victim's session cookie attached for free — the attacker never has to read or steal it (HttpOnly is irrelevant). The same-origin policy stops the attacker from reading bank.com's response, but they don't care: CSRF is a write attack, not a read attack. The whole goal is the side effect — money transferred, email changed, account deleted. That already happened the moment the authenticated request reached the server. This is the confused-deputy problem: the browser is a deputy acting with the user's authority, tricked into using it on the attacker's behalf." },
    { q: "Chrome defaults cookies to SameSite=Lax since 2020. Why is that not enough on its own, and what do you add?", a: "Lax was a huge win — it stops cookies from riding cross-site POST subrequests, killing the classic hidden-form attack by default. But it deliberately keeps three gaps. First, Lax sends cookies on top-level GET navigations, so any state-change reachable via GET is still forgeable by getting the victim to click a link — which is why 'never mutate on GET' is a security rule. Second, any cookie set SameSite=None (required for legitimate cross-site flows like third-party SSO or embedded widgets) turns all SameSite protection off. Third, Chrome's default-Lax keeps a 120-second Lax+POST compatibility window where a fresh cookie is sent on a top-level cross-site POST. And SameSite is browser-enforced, so older clients and WebViews vary. The fix is defense-in-depth: keep all mutations off GET, set SameSite explicitly, verify the Origin/Referer header, and require an unguessable CSRF token (synchronizer pattern, or HMAC-signed double-submit) on every state-changing request — so no single failure unlocks the endpoint." },
  ]}
/>

<Recap lang="en">
CSRF works because cookies are ambient authority: the browser attaches your session cookie to any request bound for your origin, even one a malicious page started, so a forged write executes with full authentication while the attacker never reads the response. Chrome's 2020 switch to SameSite=Lax as the default was the biggest single mitigation in web history — cross-site POSTs no longer carry the cookie — but it is not a complete defense. Lax still sends cookies on top-level GET navigations (so any state-change on GET is exposed), SameSite=None disables protection entirely for cross-site flows, and default-Lax keeps a ~120-second Lax+POST window. So the senior posture is defense-in-depth: never mutate on GET, set SameSite explicitly, verify the Origin/Referer header, and back it with a real CSRF token — the synchronizer pattern for server-rendered apps, or the HMAC-signed double-submit cookie for stateless APIs — so no single layer failing opens the door. Now when you review a cookie-session app, your first question is not "is Lax set?" but "what happens to a state-changing request that arrives with no CSRF token?"
</Recap>
