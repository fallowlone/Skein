---
concepts:
- ambient-authority
- samesite-cookies
- get-side-effects
- double-submit-cookie
- origin-referer-check
- cors-not-csrf
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://cheatsheetseries.owasp.org/cheatsheets/Cross-Site_Request_Forgery_Prevention_Cheat_Sheet.html
- https://web.dev/articles/samesite-cookies-explained
- https://portswigger.net/web-security/csrf/bypassing-samesite-restrictions
status: ready
summary: "Multiple-choice synthesis across the CSRF unit — ambient authority, SameSite gaps, GET side effects, token patterns, Origin checks, and why CORS is not CSRF protection."
title: 'CSRF: multiple-choice review'
track: security
unit: 04-csrf
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one mirrors a decision you make reviewing a real auth design — not a definition to recite, but a tradeoff to weigh when an endpoint changes state.
</Hook>

<Goal lang="en">
Confirm you can connect ambient authority, SameSite's deliberate gaps, the token patterns, and Origin checks — the synthesis the lesson built toward, and the trap of mistaking CORS for a CSRF defense.
</Goal>

<Quiz
  id="04-csrf-choice-1"
  lessonSlug="04-csrf"
  lang="en"
  question={"A reviewer claims 'our session cookie is HttpOnly and Secure, so we're safe from CSRF.' Why is that reasoning wrong?"}
  choices={[
    { label: "CSRF never reads the cookie — the browser auto-attaches it to any request bound for your origin. HttpOnly only blocks JavaScript from reading the value (an XSS defense); Secure only forces HTTPS. Neither stops the browser from sending the cookie on a forged cross-site write.", correct: true },
    { label: "HttpOnly is deprecated, so it provides no protection anymore.", misconception: "HttpOnly is alive and useful — but against XSS cookie theft, a different attack class. The reasoning is wrong because CSRF never needed to read the cookie in the first place." },
    { label: "They're right — HttpOnly does stop the forged request from carrying the cookie.", misconception: "HttpOnly governs JavaScript access to the cookie value, not whether the browser attaches it to a request. The browser still auto-sends it cross-site; that is exactly the ambient authority CSRF rides." },
    { label: "Secure is the relevant flag — over HTTPS the cookie can't be forged.", misconception: "Secure only forces the cookie onto HTTPS connections. The forged request is made over HTTPS too, with the cookie attached for free. Transport encryption is orthogonal to CSRF." },
  ]}
/>

<Quiz
  id="04-csrf-choice-2"
  lessonSlug="04-csrf"
  lang="en"
  question={"Your session cookies are SameSite=Lax. A hidden form on evil.com auto-submits a cross-site POST /transfer. What happens, and which endpoint is actually still exposed?"}
  choices={[
    { label: "The POST arrives without the session cookie — Lax does not send cookies on cross-site POST subrequests, so that classic attack is dead. The live hole is any state-change reachable via GET (e.g. GET /account/close), because Lax does send the cookie on top-level GET navigations.", correct: true },
    { label: "The POST succeeds with the cookie attached, so SameSite=Lax did nothing.", misconception: "Lax specifically strips the cookie from cross-site POST subrequests; that is its main win. The remaining exposure is GET-based state changes on top-level navigation, not the POST." },
    { label: "Both the POST and any GET are fully blocked — Lax behaves like Strict for cross-site.", misconception: "Lax is not Strict. It deliberately sends the cookie on top-level navigations using safe methods, so a GET that mutates state is still forgeable by getting the victim to follow a link." },
    { label: "Nothing is exposed; SameSite=Lax is a complete CSRF defense by itself.", misconception: "Lax leaves three gaps: GET side effects, SameSite=None cookies, and the default-Lax 120s Lax+POST window. It is defense-in-depth, never the only lock." },
  ]}
/>

<Quiz
  id="04-csrf-choice-3"
  lessonSlug="04-csrf"
  lang="en"
  question={"Why is 'never perform a state change on a GET request' a real security rule and not just REST style preference?"}
  choices={[
    { label: "SameSite=Lax sends the cookie on top-level GET navigations, so a GET that mutates data is forgeable by getting the victim to click a link or land on a redirecting page — no form, no script needed. Keeping all side effects on POST/PUT/PATCH/DELETE removes that vector entirely.", correct: true },
    { label: "GET requests are slower, so mutations on GET hurt performance.", misconception: "Performance is irrelevant here. The security point is that Lax still attaches cookies to top-level GET navigations, so a mutating GET is directly CSRF-able." },
    { label: "Browsers cache GET responses, so the mutation would be lost.", misconception: "Caching is a correctness concern, not the security reason. The real issue is that a GET side effect is reachable via a forged top-level navigation that carries the Lax cookie." },
    { label: "It doesn't matter — a token check protects GET and POST equally, so the method is a free choice.", misconception: "In practice GET endpoints are routinely exempted from token checks and are triggerable by plain links and image tags. Keeping mutations off GET closes the cheapest CSRF vector before any token logic runs." },
  ]}
/>

<Quiz
  id="04-csrf-choice-4"
  lessonSlug="04-csrf"
  lang="en"
  question={"A stateless JSON API uses the plain double-submit cookie pattern: a random value in a cookie that must be echoed in a header. An attacker controls a subdomain with an XSS. Why can they bypass it, and what's the fix?"}
  choices={[
    { label: "Cookies are shared up the domain tree, so a subdomain can set a cookie for the parent domain. The attacker writes a CSRF cookie value AND echoes the same value in the header — the two match, defeating the check. Fix: the HMAC-signed variant binding the token to the session, so a value the attacker can plant won't verify.", correct: true },
    { label: "Double-submit is impossible to deploy statelessly, so it must have been misconfigured.", misconception: "Double-submit is genuinely stateless — that's its appeal. The weakness is that it assumes the attacker can't write your cookies; a subdomain XSS or cookie injection breaks that assumption." },
    { label: "The header can be read cross-site, so the attacker just copies it.", misconception: "The same-origin policy stops cross-site reads of your headers and cookie value. The break here is cookie writability via a subdomain, not readability." },
    { label: "There is no fix — double-submit is fundamentally broken and you must go stateful.", misconception: "The HMAC-signed double-submit binds the token to the session so a planted value won't validate, keeping the pattern stateless. The synchronizer token (stateful) is stronger but not the only option." },
  ]}
/>

<Quiz
  id="04-csrf-choice-5"
  lessonSlug="04-csrf"
  lang="en"
  question={"A team adds a strict CORS policy (no foreign origins allowed) and concludes their state-changing endpoints are now CSRF-safe. Where does this reasoning fail?"}
  choices={[
    { label: "CORS governs whether JavaScript may READ a cross-origin response; it does not stop the request from being SENT with cookies attached. A top-level form POST or a simple request reaches the server and executes its side effect regardless of CORS — the attacker never needed to read the response.", correct: true },
    { label: "It's correct — a strict CORS policy blocks all cross-origin requests, including forged ones.", misconception: "CORS does not block requests from being sent or executed; it controls the browser's exposure of the response to script. The forged write still lands. CORS is not a CSRF defense." },
    { label: "CORS only works on GET, so they're protected on POST but not GET.", misconception: "CORS applies across methods, but in all cases it gates response readability, not request execution. The mutation happens before CORS matters." },
    { label: "The failure is that CORS requires a preflight on every request, which they didn't enable.", misconception: "Preflight only fires for non-simple requests. Simple requests and top-level form posts skip it entirely and execute their side effect — which is precisely why CORS can't be the CSRF defense." },
  ]}
/>

<Quiz
  id="04-csrf-choice-6"
  lessonSlug="04-csrf"
  lang="en"
  question={"An attacker logs the victim's browser into the ATTACKER's account (login CSRF), so the victim's later activity is recorded under the attacker's identity. Which defense addresses this, and why is the usual reasoning incomplete?"}
  choices={[
    { label: "The login form itself needs CSRF protection — a pre-session (anonymous) synchronizer token tied to the visitor, validated on the login POST. Teams forget this because the login endpoint has no authenticated session yet, but login CSRF forges the act of authenticating, so the token must cover the unauthenticated flow too.", correct: true },
    { label: "Login CSRF is harmless — the attacker only logs the victim into the attacker's own account.", misconception: "It is not harmless: the victim then enters data, payment details, or search history that lands in the attacker's account where the attacker can later read it. Login CSRF is a real, named threat." },
    { label: "SameSite=Strict on the session cookie fully prevents login CSRF.", misconception: "SameSite governs the session cookie, but login CSRF forges the login POST before any session cookie exists. You need a pre-session token on the login form, not just SameSite on the post-login cookie." },
    { label: "HTTPS prevents login CSRF because the credentials are encrypted in transit.", misconception: "Encryption protects confidentiality in transit; it does nothing about a forged top-level login submission. The defense is a CSRF token covering the unauthenticated login flow." },
  ]}
/>

<Recap lang="en">
The through-line of the unit is one chain: cookies are ambient authority, so the browser sends your session on any request bound for your origin — including a forged write. SameSite=Lax cut the cross-site POST surface in 2020 but left GET side effects, SameSite=None, and the 120s Lax+POST window, so it is defense-in-depth, not the lock. A real defense layers an unguessable token (synchronizer for stateful apps, HMAC-signed double-submit for stateless APIs), an Origin/Referer check, and the rule that mutations never ride GET. Two traps recur: HttpOnly/Secure and CORS both feel like CSRF defenses but address reading, not the forged write that has already executed. Now when you hear "we have HttpOnly and a strict CORS policy, so we're safe," you know exactly which question to ask next: what stops the forged write from landing?
</Recap>
