---
concepts:
- broken-access-control
- deny-by-default
- cryptographic-failures
- insecure-design
- ssrf
- vulnerable-components
estMin: 14
lang: en
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://owasp.org/Top10/2021/
- https://owasp.org/Top10/2021/A01_2021-Broken_Access_Control/
- https://owasp.org/Top10/2021/A04_2021-Insecure_Design/
status: ready
summary: "Free-recall prompts across the OWASP Top 10 (2021). Answer each in your own words first, then reveal the model answer and compare."
title: 'OWASP Top 10: free-recall review'
track: security
unit: 01-owasp-modern
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory before you open the model answer — the effort of recall is what makes the material stick when you are reviewing real code.
</Hook>

<Goal lang="en">
Reconstruct the unit's spine — why Broken Access Control is #1, what deny-by-default means in code, the cause-over-symptom reframing, the design-era categories, and how SSRF is defended — without looking back at the lesson.
</Goal>

<RetrievalDrawer
  client:load
  id="01-owasp-modern-short-retrieval"
  lang="en"
  questions={[
    { q: "Why did Broken Access Control jump to #1 in 2021, and what does 'deny by default' mean in code?", a: "It rose from #5 to #1 because OWASP's 2021 data showed it was both the most common class — present in 94% of all applications tested, with the highest number of occurrences in the dataset across 34 mapped CWEs — and among the most damaging, since it lets a user read or change data that is not theirs. The two dominant production shapes are IDOR (the server fetches a record by a client-supplied id without checking ownership) and missing function-level authorization (a privileged endpoint is reachable just because the UI button was hidden). Deny by default means every resource access starts from a refusal: the server computes, on each request and on the actual record and action, whether the authenticated caller is authorized — using server-side state, not a hidden UI element or a forgeable client claim. In code that is scoping queries to the owner (WHERE id = ? AND owner_id = ?) and gating privileged routes with an explicit role/permission check." },
    { q: "Explain the cause-over-symptom reframing in 2021 using Sensitive Data Exposure as the example.", a: "The single biggest 2021 change was philosophical: the list reorganized around root causes instead of symptoms, re-derived against roughly 400 mapped CWEs (up from about 30). 'Sensitive Data Exposure' described what you saw after a breach — data got out. Its replacement, 'Cryptographic Failures,' names what actually went wrong: weak or missing crypto, absent TLS, hardcoded keys, broken cipher choices. The benefit is actionability — a category that names a failure mode you can design against (enforce TLS, rotate keys, drop weak ciphers) is more useful than one that names an outcome you only notice too late. The same logic shaped the rest of the list, which is why it reads as a map of how apps break rather than a catalog of breach symptoms." },
    { q: "What is Insecure Design (A04), how is it different from a coding bug, and why can't you patch it away?", a: "Insecure Design is the 2021 recognition that some vulnerabilities are baked in before a single line is written — a password-reset flow with no rate limit, a checkout that trusts a client-sent price, a feature with no abuse case considered. It is a missing security requirement, not a defect in otherwise-correct code: the implementation faithfully does the wrong thing. That is why no input filter, sanitizer, or patch closes it — the control was never specified. The fix is shift-left: threat-model the feature, write abuse cases alongside user stories, and bake the control into the architecture (server-side price, rate limits, state machines that cannot be skipped). It marks the field maturing past 'scrub the input' toward 'design for least privilege.'" },
    { q: "What is SSRF (A10), why did it rank despite thin data, and how do you defend against it?", a: "SSRF is when a server can be tricked into making a request to a URL the attacker controls — for example a 'fetch this image' feature pointed at http://169.254.169.254/ to read cloud metadata credentials. It maps roughly one CWE and had almost no historical incidence data, yet it ranked because OWASP reserves two of its ten slots for community-survey-voted risks, and practitioners voted it in. Defenses are layered: validate and allowlist the destination (scheme, host, port) rather than blocklisting; resolve the hostname and reject private, loopback, and link-local ranges (including after redirects and DNS rebinding); disable unneeded URL schemes; require IMDSv2 / drop the metadata endpoint; and isolate the fetcher on a network segment that cannot reach internal services." },
    { q: "Why are 'Vulnerable & Outdated Components' (A06) your problem, and what process keeps the category from drifting?", a: "A06 rose to prominence because a dependency's CVE becomes your CVE the moment it runs in your production — the attacker does not care that you did not write the vulnerable code. Modern apps are mostly third-party code, so the attack surface is dominated by transitive dependencies you may not even know you ship. The category drifts (silently accumulates risk) when there is no upgrade discipline. The process that holds it: maintain an SBOM so you know what you ship, run dependency/CVE scanning in CI (and continuously, since new CVEs land against old versions), remove unused dependencies, prefer maintained libraries, and have a fast patch path so a critical CVE can ship the same day rather than waiting for the next release train." },
    { q: "How should a senior actually read the Top 10 — as a checklist, or as something else?", a: "Not as gospel or a pass/fail checklist, but as a prioritization map built from data on 500,000+ real applications. A senior reads it as 'where is my time best spent,' and access control wins on expected value: it is the most common class and among the most damaging, so it earns the most rigor — server-side, deny-by-default checks on every object and every function. The rest is a coverage map: crypto in transit and at rest, parameterized data access, threat-modeled designs, a dependency-update process, hardened configs, and structured security logging so you can detect the breach you did not prevent. The ranking tells you where the field is heading (SSRF earned its spot by vote), so treat it as guidance to weight effort, not a box to tick." },
  ]}
/>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the unit's spine: Broken Access Control is #1 because it is both the most common and among the most damaging, fixed with server-side deny-by-default checks; the 2021 list reorganized around root causes (Cryptographic Failures, not Sensitive Data Exposure); Insecure Design names flaws no patch can close; SSRF earned its slot by practitioner vote and is defended by allowlisting and metadata lockdown; vulnerable components are your CVEs and need an SBOM plus CI scanning; and the whole list is a prioritization map, not a checklist. Now when you open the next Top 10 edition or step into a security review, you will instinctively ask: what is the root cause — and am I fixing that, or only its symptom?
</Recap>
