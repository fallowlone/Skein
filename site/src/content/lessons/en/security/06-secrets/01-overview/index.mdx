---
concepts:
- hardcoded-credentials
- secret-rotation
- secrets-manager
- dynamic-secrets
- least-privilege
- secret-scanning
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://cheatsheetseries.owasp.org/cheatsheets/Secrets_Management_Cheat_Sheet.html
- https://www.helpnetsecurity.com/2024/12/02/revoke-exposed-aws-keys/
- https://developer.hashicorp.com/vault/tutorials/get-started/understand-static-dynamic-secrets
- https://snyk.io/articles/state-of-secrets/
spiral: []
status: ready
summary: A credential in source code is a leaked credential. Git history keeps it
  after you delete it, scanners find public keys in about a minute, and the only real
  fix is rotation, not removal. The maturity ladder runs from hardcoded keys to a
  managed, audited, short-lived secret.
title: 'Secrets management: why a key in git is already a breach'
track: security
unit: 06-secrets
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A junior pushes a quick fix to a public repo. Buried in the diff is an AWS access key that was "just for testing." Within a minute an automated scanner watching GitHub's public event stream has it; within eleven minutes someone spins up a fleet of GPU instances for crypto mining on the company's account. The dev panics and force-pushes a commit deleting the line. The key still works — it is still live in git history, and rotating it, not deleting it, is the only thing that ends the breach.
</Hook>

<Crux>A secret committed to git is already leaked: history keeps it after deletion, so only rotation — not removal — ends the exposure.</Crux>

<Explanation>

## A hardcoded credential is a leaked credential

The instinct is to treat a key in source code as untidy — something to clean up before the demo. It is not untidy; it is exposed. The moment a credential lives in a repo, everyone with read access has it, every clone carries it, every CI log can print it, and if the repo is ever made public it is broadcast to the internet. GitHub recorded over 39 million leaked secrets in 2024, a 67% jump year over year, and GitGuardian counted 28.65 million new hardcoded secrets pushed to public repos in 2025 alone. This is not a rare slip — it is the single most common way real systems get breached, which is why OWASP treats secrets management as a first-class concern.

The numbers on exploitation are the part that scares people into changing. Researchers planting honeypot AWS keys saw automated bots find and abuse them in about one minute, and security firms have measured scanners grabbing exposed keys within eleven minutes and immediately launching GPU instances. You do not get a grace period. By the time you notice the bad commit, the attacker has already used the key.

## Git history is append-only, so deletion is a lie

The trap that catches most engineers: they `git rm` the file or delete the line, push, and believe the secret is gone. It is not. Git's data model is append-only — every commit is preserved, and the secret sits in the history of every clone and fork forever. Anyone who runs `git log -p` or any scanner that walks full history (and they all do) reads it as easily as the current code.

So the cleanup has two parts, and only one of them actually matters. You can rewrite history with `git filter-repo` to scrub the blob from every commit — but that does not help anyone who already cloned, and you can never be sure no copy escaped. The step that ends the breach is **rotation**: revoke the leaked credential at its source so the value an attacker holds becomes worthless. Treat every secret that has ever touched a repo as compromised and rotate it. History rewriting is hygiene; rotation is the fix.

<Inset kind="why" lang="en">
Hardcoding is not the only leak path. The same secret can be baked into a Docker image layer (anyone who pulls the image runs `docker history` and reads it), printed into application logs or a verbose error page, or bundled into a client-side JavaScript build and shipped to every browser. A "backend" secret in a Next.js component that isn't server-only is one careless import away from the public bundle. Every one of these is the same failure: the secret left the trust boundary.
</Inset>

## The maturity ladder: hardcoded → dotenv → managed

There is a clear progression in how teams handle secrets, and most outages happen because a team stalled on a lower rung.

The visual below is the ladder a senior climbs deliberately:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Rung</th>
        <th class="border border-border px-3 py-2 text-left">What it gives you</th>
        <th class="border border-border px-3 py-2 text-left">What still hurts</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Hardcoded in source</td>
        <td class="border border-border px-3 py-2">Nothing — convenience only</td>
        <td class="border border-border px-3 py-2">In git history forever; leaks on every clone, log, public push</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>.env</code> + <code>.gitignore</code></td>
        <td class="border border-border px-3 py-2">Out of git, out of the image if done right</td>
        <td class="border border-border px-3 py-2">Plaintext on disk and in CI; lives in process env; no access control or audit; one bad <code>.gitignore</code> from disaster</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Secrets manager (Vault, AWS Secrets Manager, SOPS)</td>
        <td class="border border-border px-3 py-2">Encryption at rest, access control, audit log of who read what</td>
        <td class="border border-border px-3 py-2">Operational cost; still long-lived if you only store static values</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Dynamic, short-lived secrets</td>
        <td class="border border-border px-3 py-2">Per-request creds with a TTL, auto-revoked, per-service auditable</td>
        <td class="border border-border px-3 py-2">Most setup; needs an integration the app understands</td>
      </tr>
    </tbody>
  </table>
</div>

The big jump is from `.env` to a secrets manager. A `.env` file is genuinely better than hardcoding — it keeps the value out of the commit — but it is still plaintext sitting on every developer's laptop and CI runner, readable by anyone with the box and invisible to any audit. A manager like HashiCorp Vault, AWS Secrets Manager, or SOPS adds the three things a `.env` can never give you: **encryption at rest**, **access control** (this service may read this secret and no other), and an **audit trail** of every read. When an incident happens, the audit log is what tells you whose credential to rotate and what it touched.

## Short-lived beats long-lived: the TTL is the blast radius

The top rung is where senior teams aim, and it changes the math of a leak. A static, long-lived credential is the most common attack vector precisely because it stays valid until a human remembers to rotate it — which, after a leak, is often never. A dynamic secret is generated on demand with a built-in TTL and revoked automatically when it expires. Vault's database engine, for example, mints a unique database user per request with a one-hour TTL in production; a leaked credential is dead within sixty minutes whether or not anyone noticed.

Two more properties fall out of this. Because each consumer gets a **unique** credential, the audit log can name the exact service instance that did something — you are not staring at one shared username used by everything. And dynamic creds make **least privilege** enforceable: each lease is scoped to exactly the permissions that role needs, so a stolen key for the read-only reporting service cannot delete the production database. The senior tradeoff is real — dynamic secrets cost the most to set up and need the app to fetch and renew leases — but for anything protecting production data, a one-hour blast radius beats an indefinite one.

<FlowDiagram
  label="The managed-secret lifecycle: the secret lives in a secrets manager or vault, never in the repo; it is injected at deploy or runtime into the app that consumes it; and it is rotated on a TTL so a leaked value auto-expires, with the repo deliberately bypassed by the whole flow."
  perRow={3}
  hue="var(--d-crypto)"
  nodes={[
    { id: "vault", label: "Secrets manager", sub: "Vault, KMS" },
    { id: "inject", label: "Inject at runtime", sub: "deploy / lease" },
    { id: "app", label: "App consumes", sub: "in process env" },
    { id: "repo", label: "Repo", sub: "never committed" },
    { id: "rotate", label: "Rotate on TTL", sub: "auto-revoke" },
  ]}
  edges={[
    { from: "vault", to: "inject" },
    { from: "inject", to: "app" },
    { from: "rotate", to: "vault", label: "renew" },
    { from: "repo", to: "vault", label: "bypassed" },
  ]}
  caption="The secret never touches the repo. It flows from the manager into the running app at deploy/runtime, and rotation on a TTL bounds the blast radius — a leaked dynamic credential auto-expires (e.g. in an hour) whether or not anyone noticed."
/>

<TradeoffMatrix
  id="06-secrets-tradeoff-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="A backend service needs database credentials in production. The team can spend a sprint on this. Pick the approach a senior defends."
  options={[
    { name: "Dynamic secrets from Vault: a unique DB user minted per request with a 1-hour TTL, auto-revoked", summary: "Short-lived, scoped.", correct: true, justification: "The leak blast radius is the TTL. A credential that leaks is useless within an hour without anyone noticing, each service instance gets a unique username so the audit log can name exactly what acted, and the lease is scoped to least privilege. The cost is the integration to fetch and renew leases — affordable in a sprint, and the only option that bounds the damage of a leak in advance." },
    { name: "A long-lived DB password stored in AWS Secrets Manager with access control and audit", summary: "Managed but static.", justification: "A real improvement over .env — encryption at rest, access control, and an audit of reads. But the value is still long-lived, so a leak stays valid until a human rotates it, and after an incident that is often never. Good as a step; not the senior endpoint when dynamic secrets are on the table." },
    { name: "A .env file deployed to the server, kept out of git via .gitignore", summary: "Plaintext on disk.", justification: "Better than hardcoding because it's out of the commit, but it's plaintext on the box and in CI, readable by anyone with access, with no access control and no audit. One mis-edited .gitignore and it's back in git. Fine for a hobby project, not for production data." },
    { name: "Hardcode it in a config module and add the file to .gitignore so it won't be committed", summary: "Still in code.", justification: "A config module is still source code on disk and almost certainly already tracked or about to be — .gitignore only helps untracked files, and one git add -f or a careless commit puts the secret in history forever. This is the rung the whole lesson is about leaving." },
  ]}
/>

<Quiz
  id="06-secrets-quiz-1"
  lessonSlug="01-overview"
  lang="en"
  question="You discover an API key was committed to your repo last month. You delete the line and force-push. Is the secret safe now?"
  choices={[
    { label: "No — the key is still in git history and on every clone; you must rotate (revoke) it at the source", correct: true },
    { label: "Yes — force-push rewrote history, so the line no longer exists anywhere", misconception: "Force-push only changes your remote's current tip; every existing clone and fork still has the old commit, and you can never confirm no copy leaked. Deletion is not revocation." },
    { label: "Yes, as long as the repo is private", misconception: "Private cuts the audience but not the risk — anyone with read access, any CI log, and any future visibility flip still exposes it. The fix is rotation regardless of visibility." },
    { label: "Yes — git rm removes the file from history", misconception: "git rm removes the file from the working tree and future commits, not from past commits. Git history is append-only; the blob stays until you rewrite history, and even then rotation is what matters." },
  ]}
/>

<Quiz
  id="06-secrets-quiz-2"
  lessonSlug="01-overview"
  lang="en"
  question="Why does a dynamic, short-lived database credential beat a long-lived one for production?"
  choices={[
    { label: "Its TTL bounds the blast radius — a leaked credential auto-expires (e.g. in an hour) and each consumer gets a unique, scoped, auditable identity", correct: true },
    { label: "It encrypts the credential so it can never be read", misconception: "The app still reads the credential in plaintext to use it; the win is the short lifetime and per-consumer scope, not that the value is unreadable." },
    { label: "It removes the need for an audit log", misconception: "It's the opposite: unique per-consumer credentials make the audit log more precise, naming the exact instance that acted instead of one shared user." },
    { label: "It means you never have to rotate anything again", misconception: "Dynamic secrets automate rotation rather than abolish it — the underlying engine and root credentials still need periodic rotation; the app just stops handling long-lived keys." },
  ]}
/>

<DragOrder
  id="06-secrets-drag-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="You just found a live cloud key committed to a public repo. Order the response from first to last:"
  items={[
    { id: "a", label: "Rotate the credential now — revoke the leaked key at the source so it stops working" },
    { id: "b", label: "Check the provider's audit/billing logs for what the key already did" },
    { id: "c", label: "Rewrite git history (git filter-repo) to scrub the blob from every commit" },
    { id: "d", label: "Add a pre-commit secret scanner (gitleaks/trufflehog) so it can't happen again" },
    { id: "e", label: "Move the secret into a manager with access control and short-lived leases" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>A secret in git is leaked, not messy — scanners abuse public keys in about a minute. Deletion can't fix it (history is append-only); rotate at the source. Climb from hardcoded to a managed, short-lived secret.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-secrets-retrieval"
  lang="en"
  questions={[
    { q: "A teammate committed a database password, then deleted the line and pushed. Explain why that does not fix the problem and what the actual remediation is.", a: "Git's history is append-only, so deleting the line in a new commit does not remove the secret from the previous commit — it still lives in the full history of the repo and in every clone and fork anyone made. Any scanner or anyone running git log -p reads it trivially, so from a security standpoint the password is leaked the moment it was pushed, and leaked keys on public repos get abused in about a minute. The deletion is cosmetic. The real remediation is rotation: revoke the password at the database so the value the attacker holds stops working, then check the provider's audit and billing logs to see what was done while it was live. As hygiene you can also rewrite history with git filter-repo to scrub the blob, but that does not retroactively secure copies people already cloned, which is exactly why rotation — not removal — is the fix. Finally add a pre-commit secret scanner so the next one is caught before it ever leaves the laptop." },
    { q: "Walk a teammate up the secrets maturity ladder and explain why each rung is better, ending with why short-lived dynamic secrets are the goal.", a: "Rung one is hardcoding, which is just a leaked credential sitting in git history forever. Rung two is a .env file kept out of git with .gitignore: better, because the value is out of the commit and the image, but it is still plaintext on every laptop and CI runner, readable by anyone with the box, with no access control and no audit, and one bad .gitignore from disaster. Rung three is a secrets manager — HashiCorp Vault, AWS Secrets Manager, or SOPS — which adds the three things a .env can't: encryption at rest, access control so only the right service can read a given secret, and an audit trail of every read, which is what tells you whose credential to rotate after an incident. Rung four is dynamic, short-lived secrets: instead of storing a static value, the manager mints a unique credential per request with a TTL — Vault's database engine commonly uses a one-hour TTL in production — and revokes it automatically when it expires. That makes the TTL the blast radius of any leak, gives each consumer a unique, scoped, least-privilege identity that the audit log can name precisely, and removes long-lived keys from the application entirely. It costs the most to set up, but for anything guarding production data a one-hour exposure window beats an indefinite one." },
  ]}
/>

<Recap lang="en">
A credential in source code is not a tidiness issue — it is a leaked credential, and leaked keys on public repos are abused in roughly a minute by automated scanners that immediately spin up resources on your account. The defining gotcha is that git history is append-only: deleting the line or even force-pushing does not remove the secret from past commits or from anyone's clone, so the only thing that ends the exposure is rotation — revoking the value at its source — backed up by checking audit and billing logs for what already happened. From there a senior climbs the maturity ladder: off hardcoding, past a .env file (out of git but still plaintext, uncontrolled, and unaudited), into a secrets manager that adds encryption at rest, access control, and an audit trail, and finally to dynamic, short-lived secrets whose TTL bounds the blast radius of any leak while enforcing least privilege per consumer. The safety net beneath all of it is a pre-commit secret scanner so the next mistake is caught before it ever leaves the machine.
</Recap>
