---
concepts:
- dependency-confusion
- lockfile-integrity
- slsa-provenance
- typosquatting
- ci-token-scope
- ignore-scripts
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://medium.com/@alex.birsan/dependency-confusion-how-i-hacked-into-apple-microsoft-and-dozens-of-other-companies-4a5d60fec610
- https://slsa.dev/spec/v1.0/levels
- https://en.wikipedia.org/wiki/XZ_Utils_backdoor
status: ready
summary: "Multiple-choice synthesis across the supply-chain unit — confusion, typosquatting, lockfile integrity, provenance, signing, and CI pipeline blast radius."
title: 'Supply-chain security: multiple-choice review'
track: security
unit: 07-supply-chain
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one mirrors a decision you make in a real incident — not a definition to recite, but a tradeoff to weigh when the install step is the attack surface.
</Hook>

<Goal lang="en">
Confirm you can connect the documented attack patterns to the defense that actually stops each one — lockfile integrity, registry precedence, provenance, and signing — and judge which layer a given control protects.
</Goal>

<Quiz
  id="07-supply-chain-choice-1"
  lessonSlug="07-supply-chain"
  lang="en"
  question="Your team pins exact versions in package.json (no caret ranges) and feels safe from supply-chain attacks. What does that pin actually fail to guarantee?"
  choices={[
    { label: "That the bytes behind the version are unchanged. A version is a label the publisher controls; if a release is republished with new content or the registry is compromised, a version-only pin installs it silently. Only a committed lockfile with integrity hashes verified by `npm ci` catches byte-level tampering.", correct: true },
    { label: "Nothing — an exact pin is a complete defense.", misconception: "An exact pin only stops a caret range from auto-jumping to a new release. It says nothing about the actual tarball bytes, build provenance, or registry compromise — you still need integrity hashes and `npm ci`." },
    { label: "That transitive dependencies are pinned too.", misconception: "Transitive pinning is a real gap, but the lockfile already pins the full transitive tree to versions and hashes. The deeper gap a pin misses is byte integrity, not transitive coverage." },
    { label: "That you applied the latest security patches.", misconception: "Patch currency is a separate concern. The question is integrity: even the exact pinned version can be a different artifact than you audited if only the version, not the hash, is enforced." },
  ]}
/>

<Quiz
  id="07-supply-chain-choice-2"
  lessonSlug="07-supply-chain"
  lang="en"
  question="A build at a large company silently resolved a public package named after an internal-only module, at version 9.9.9, and ran the attacker's code. No credentials were stolen. What enabled this, and what is the primary fix?"
  choices={[
    { label: "Dependency confusion: the package manager consulted both the public and private registries and preferred the higher version. Fix: own a scoped namespace publicly and give the private registry strict precedence for that scope.", correct: true },
    { label: "A leaked registry token let the attacker publish to the private registry.", misconception: "No token was needed. The attacker published to the PUBLIC registry under the internal name; resolution order — preferring the higher version across registries — did the rest." },
    { label: "Typosquatting on a misspelled package name.", misconception: "Typosquatting relies on a developer fat-fingering a name. Confusion needs no typo — the real internal name resolves to a public impostor at a higher version." },
    { label: "A compromised lockfile hash matched the attacker's package.", misconception: "An enforced lockfile is a defense here, not the cause. Confusion bites builds that resolve fresh across multiple registries without strict precedence." },
  ]}
/>

<Quiz
  id="07-supply-chain-choice-3"
  lessonSlug="07-supply-chain"
  lang="en"
  question="The xz-utils backdoor (CVE-2024-3094) had clean source in Git but a malicious released tarball. Which control would have caught it, and why do lockfiles and audit not?"
  choices={[
    { label: "Reproducible builds plus SLSA provenance: provenance ties the artifact to the exact source and build, so a tarball that does not match the committed source fails verification. Lockfiles only check the bytes you fetch match a recorded hash — of the already-tampered artifact; audit only knows published advisories, and this was a zero-day.", correct: true },
    { label: "A lockfile with integrity hashes would have caught it.", misconception: "A hash pins the bytes you receive, but those bytes WERE the backdoored tarball. Hashing the tampered artifact just locks in the tamper — it never compares against the clean source." },
    { label: "An audit run would have flagged the advisory before install.", misconception: "It was a zero-day backdoor with no advisory, and xz is a C library shipped via distros, not npm. Audit only catches known, disclosed CVEs after the fact." },
    { label: "Pinning the exact version would have stopped it.", misconception: "Everyone who installed got the same pinned, backdoored version. Pinning a malicious artifact just guarantees you keep getting the malicious one." },
  ]}
/>

<Quiz
  id="07-supply-chain-choice-4"
  lessonSlug="07-supply-chain"
  lang="en"
  question="Why does adding `--ignore-scripts` to installs matter, and what does it specifically defend against?"
  choices={[
    { label: "It stops a package's lifecycle scripts (preinstall/postinstall) from running arbitrary code during the fetch — the moment with your registry tokens, CI secrets, and network access. Most install-time payloads execute exactly there, so blocking scripts shrinks the blast radius of a malicious or confused package.", correct: true },
    { label: "It prevents the lockfile from being mutated during install.", misconception: "That is what `npm ci` does. `--ignore-scripts` is orthogonal: it blocks arbitrary code execution from lifecycle hooks, not lockfile drift." },
    { label: "It verifies integrity hashes more strictly.", misconception: "Hash verification is independent of scripts. `--ignore-scripts` neither strengthens nor weakens integrity checks; it removes the install-time code-execution vector." },
    { label: "It blocks the package from reaching the public registry.", misconception: "Registry precedence and scoping control where packages resolve from. `--ignore-scripts` only governs whether fetched packages can run code during install." },
  ]}
/>

<Quiz
  id="07-supply-chain-choice-5"
  lessonSlug="07-supply-chain"
  lang="en"
  question="A CI workflow grants its build job a broad, write-scoped token and runs on pull_request from forks. What is the supply-chain risk, and the first fix?"
  choices={[
    { label: "Untrusted PR code runs in a job that can read secrets and push artifacts — a single malicious dependency or PR can exfiltrate the token and publish poisoned releases. First fix: least-privilege, read-only default token scope, and do not expose publish secrets to fork-triggered runs.", correct: true },
    { label: "There is no risk — CI is isolated from production.", misconception: "CI is squarely in the supply chain: it builds and signs the artifacts that reach production. A token leaked there can publish poisoned releases that are downstream-trusted." },
    { label: "The fix is to add an audit step to the workflow.", misconception: "Audit catches known advisories; it does nothing about an over-scoped token that untrusted code can steal. The fix is least-privilege scoping and isolating secrets from fork runs." },
    { label: "Switch the runner to a larger machine.", misconception: "Resources are unrelated to blast radius. The problem is that untrusted code executes with credentials it should never see — a permissions and trigger-isolation problem." },
  ]}
/>

<Quiz
  id="07-supply-chain-choice-6"
  lessonSlug="07-supply-chain"
  lang="en"
  question="A CVE drops in a deep transitive dependency. Leadership asks 'are we affected, and where?' What artifact answers this in seconds rather than a frantic grep across repos?"
  choices={[
    { label: "An SBOM (CycloneDX/SPDX) generated at build time: a machine-readable inventory of every component and version, so you query the component name and get an exact list of affected builds and services.", correct: true },
    { label: "The lockfile alone is enough.", misconception: "A lockfile lists one repo's resolved tree, but across many services and images there is no consolidated, queryable inventory. The SBOM is the standardized, aggregatable artifact built for exactly this question." },
    { label: "SLSA provenance answers it.", misconception: "Provenance certifies HOW an artifact was built and that it was not tampered with — not WHAT components are inside. The composition question is the SBOM's job." },
    { label: "A signed artifact answers it.", misconception: "A signature proves authenticity and integrity of the artifact, not its ingredient list. To know whether a vulnerable component is present, you read the SBOM." },
  ]}
/>

<Recap lang="en">
Across the unit the through-line is one layered model: the install step is the attack surface, and each control answers a different question. Lockfile + `npm ci` + integrity hashes answer "did I get the exact bytes I expected"; exact pins and `--ignore-scripts` shrink the auto-upgrade and code-execution windows; scoped names with registry precedence kill dependency confusion; SBOMs answer "what is inside"; and SLSA provenance plus signing answer "how was it built and is it untampered" — the gap xz exploited. CI itself is in the chain, so least-privilege tokens matter as much as the dependencies they handle.
</Recap>
