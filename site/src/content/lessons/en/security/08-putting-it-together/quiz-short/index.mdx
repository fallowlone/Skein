---
concepts:
- defense-in-depth
- authn-vs-authz
- token-composition
- offline-cracking
- secret-rotation
- supply-chain-trust
estMin: 14
lang: en
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://owasp.org/Top10/2021/A01_2021-Broken_Access_Control/
- https://www.rfc-editor.org/rfc/rfc8725.html
- https://cheatsheetseries.owasp.org/cheatsheets/Secrets_Management_Cheat_Sheet.html
status: ready
summary: "Free-recall prompts spanning the whole security track. Answer each in your own words first, then reveal the model answer and compare against the full threat model."
title: 'Security capstone: free-recall review'
track: security
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, reconstruct a full answer from memory before opening the model answer — and notice that every one forces you to connect two or more units of the track, because that is where real systems break.
</Hook>

<Goal lang="en">
Reconstruct the track's spine without looking back: defense in depth, AuthN vs AuthZ, how token controls compose, why fast hashes lose, why rotation (not deletion) ends a secret leak, and why your dependencies are your attack surface.
</Goal>

<RetrievalDrawer
  client:visible
  id="08-putting-it-together-short-retrieval"
  lang="en"
  questions={[
    { q: "State the central thesis of the whole security track in your own words, using one concrete cross-layer example.", a: "No single control is sufficient: defense in depth assumes every control eventually fails, so you stack independent layers — transport, authentication, authorization, input handling, secrets, supply chain — where one catches what another misses. The dangerous failures are not a missing layer (those get caught in review) but two correct layers that don't compose. The canonical example: authentication flawlessly proves a user is logged in, and an endpoint like GET /accounts/{id}/statements trusts that valid token to mean the caller may read any id it names. Authentication never claimed that — it answered 'who', not 'what may they touch'. The breach lives in the authorization seam, which is exactly why OWASP ranks Broken Access Control A01. You threat-model the whole flow, not each control alone, and apply least privilege so that when one layer falls, the blast radius is small." },
    { q: "Explain the difference between an OIDC ID token and an OAuth access token, and the validation an API must perform before trusting either.", a: "The ID token (OIDC layer) proves who signed in — it's for the client to establish identity, carries claims like sub/email/name, and is never sent to an API. The access token authorizes API calls — the client sends it in Authorization: Bearer, and its audience is the resource server. Authorizing an API call by reading ID-token claims is wrong because the API would be trusting a token never minted for it. Before trusting an access token the resource server must verify the signature against the issuer's published key, pin the algorithm (an allowlist, never the header's alg, to block alg:none and RS256→HS256 confusion), and check iss, aud (this API), and exp. Even then the token only establishes identity and scopes — the endpoint still enforces object-level authorization (owns(subject, object)) on top." },
    { q: "Walk through the JWT-in-localStorage seam: why a perfectly minted token still gets stolen, and what composing controls looks like.", a: "Token strength is about whether it can be forged; theft is about where it lives. In localStorage any successful XSS runs localStorage.getItem('token') and exfiltrates it — the signature and short expiry are irrelevant once JavaScript can read the value. Moving it to an HttpOnly cookie blocks that, because JS can't touch the cookie, but cookies are ambient authority the browser auto-sends cross-site, which reopens CSRF. You close CSRF with SameSite (Lax/Strict) plus a CSRF token or origin check, and ensure no state-changing action rides a GET (SameSite=Lax still sends on top-level GET navigations). Each fix solves one layer and exposes the next seam, which is the point: compose HttpOnly + Secure + SameSite + short TTL, and still kill XSS at the source with output encoding and CSP — because an attacker running script in your origin has already moved past the storage question." },
    { q: "A breach dumps the users table. Explain why the storage choice made years earlier decides how bad Monday is.", a: "Once an attacker has the hash column they're no longer rate-limited by your login endpoint — they crack offline, as fast as their hardware allows. A fast general-purpose hash like SHA-256 is the wrong tool: a single consumer GPU does ~22 billion guesses/second, so against standard wordlists every dictionary word, name-plus-year, and common pattern falls in hours, even with a salt (the salt only defeats rainbow tables and forces per-account work). The fix is a deliberately slow, memory-hard, per-user-salted KDF — Argon2id (or bcrypt cost 12+) tuned to a few hundred ms per hash — so one login is cheap but a billion offline guesses become economically impossible. Encryption is wrong because it's reversible and the key usually leaks with the breach. The decision lives in one line of signup code, written long before the breach." },
    { q: "Why is rotating a leaked secret the only real fix, and how does this connect to least privilege and blast radius across the track?", a: "Git history is append-only, so deleting the line or git-rm'ing the file doesn't remove the secret — it stays in the history of every clone and fork forever, readable by anyone running git log -p or any scanner that walks history. Honeypot keys are abused within ~1 minute, so by the time you notice, the value is already in use. History rewriting (git filter-repo) is hygiene; rotation — revoking the credential at its source so the attacker's copy becomes worthless — is the fix. This is the same blast-radius logic as the rest of the track: a vault credential should grant only the rows and operations the endpoint needs, an access token should be short-lived so a stolen one expires in minutes, and a dependency runs inside your trust boundary. Least privilege everywhere decides how far damage spreads when (not if) a layer fails." },
    { q: "Your application code is flawless. Explain why you can still be fully compromised, and the defenses that address it.", a: "A modern app is a thin layer of your logic on hundreds of mostly-transitive dependencies written by strangers and updated automatically — so the attack surface moved to the install step. Supply-chain attacks skip your code: the moment CI fetches a package and runs its install scripts, they execute with your credentials, registry tokens, and network. The documented shapes: dependency confusion (the resolver prefers a higher-versioned public package over your internal one — Birsan hit Apple, Microsoft, Tesla), typosquatting (lodahs next to lodash), compromised legit packages (event-stream, ua-parser-js), and long-game maintainer takeover (the xz-utils backdoor, where trust in a maintainer was not trust in the build). Defenses: scope/namespace internal packages and pin a single trusted registry precedence, enforce a lockfile with integrity hashes for reproducible installs, track an SBOM and provenance (SLSA), and gate dependency updates rather than auto-merging them." },
  ]}
/>

<Recap lang="en">
If you reconstructed each answer from memory you hold the track's spine: defense in depth assumes every control fails, so the breach lives in the seam — AuthN proves who, AuthZ decides what they may touch (A01). Tokens compose: validate the access token (signature, pinned alg, iss/aud/exp), keep it off localStorage, and trade XSS theft for CSRF closed by SameSite plus a token. Passwords need a slow, salted, memory-hard KDF because offline cracking is GPU-fast. A leaked secret ends only at rotation, never deletion. And your dependencies are your real attack surface — scope, pin, lock, and verify provenance. Now when you threat-model a new flow, you run the stack end to end — not stopping when each layer passes its own test, but asking which seam between two correct controls is the one an attacker walks through.
</Recap>
