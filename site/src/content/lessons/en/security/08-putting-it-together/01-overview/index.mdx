---
concepts:
- defense-in-depth
- broken-access-control
- threat-modeling
- least-privilege
- token-storage
- control-composition
deepensInto: []
estMin: 17
lang: en
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-overview
sources:
- https://owasp.org/Top10/2021/A01_2021-Broken_Access_Control/
- https://devguide.owasp.org/en/02-foundations/03-security-principles/
- https://cheatsheetseries.owasp.org/cheatsheets/Password_Storage_Cheat_Sheet.html
- https://pages.nist.gov/800-63-4/sp800-63b.html
spiral: []
status: ready
summary: No single security control is sufficient. Each layer — TLS, auth, authz,
  input handling, secrets, dependencies — can be individually correct and the system
  still falls, because the breach lives in the seam where two correct controls meet.
title: 'Defense in depth: the breach lives in the seam'
track: security
unit: 08-putting-it-together
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
The login flow is textbook. Passwords are Argon2id-hashed, the session is a short-lived JWT, TLS is enforced with HSTS, the API key sits in a vault. Then a user changes the `id` in `GET /accounts/{id}/statements` and reads someone else's bank statements. No control was broken. Authentication verified *who* the caller was — flawlessly — and then the statements endpoint simply never asked *whether this caller may read this record*. Every layer passed its own test. The breach lived in the gap between them.
</Hook>

<Crux>No single control is sufficient: layers each look correct, yet the breach lives in the seam where two correct controls meet.</Crux>

<Explanation>

## One request, six layers

Trace a single sensitive action — a logged-in user transfers money — and you pass through every control the security track has covered. Each is a distinct layer with its own job, and a senior reasons about them as a *stack*, not a checklist:

1. **Transport** — TLS plus HSTS so the request can't be read or downgraded to plaintext on the wire.
2. **Authentication** — prove *who* is calling. A short-lived access token (minutes, not days), passwords stored as Argon2id or bcrypt hashes, never the password itself.
3. **Authorization** — decide *whether this caller may do this to this object*. Deny-by-default, least privilege. This is OWASP's #1 risk for a reason.
4. **Input handling** — treat every field as hostile: validate, parameterize queries, encode output.
5. **Secrets** — keys and DB credentials live in a vault or env, never in the repo, never in the JWT.
6. **Supply chain** — the dependencies executing all of the above are themselves vetted and pinned.

The thesis of the whole track lands here: **defense in depth assumes every control eventually fails.** You stack them so that when the first barrier goes down, the second slows the attacker and the third raises an alert. The mistake juniors make is treating one strong control as the answer. Together these six layers mean a breach has to punch through each in sequence — and the dangerous gap is never inside one layer but in the seam where two meet without composing.

<StackDiagram
  label="The six defense-in-depth layers a single sensitive request passes through, outermost to innermost: Transport, Authentication, Authorization, Input handling, Secrets, and Supply chain."
  hue="var(--d-crypto)"
  layers={[
    { label: "Transport", note: "TLS + HSTS" },
    { label: "Authentication", note: "who is calling" },
    { label: "Authorization", note: "deny by default" },
    { label: "Input handling", note: "validate, parameterize" },
    { label: "Secrets", note: "vault, not the repo" },
    { label: "Supply chain", note: "vetted, pinned deps" },
  ]}
  caption="Six layers, each with its own job. Defense in depth assumes every one eventually fails, so they stack — and the dangerous breach lives not in a missing layer but in the seam where two correct layers (e.g. Authentication and Authorization) fail to compose."
/>

## The breach lives in the seam

The dangerous failures are not a missing layer — those get caught in review. They are two correct layers that don't compose. The hook is the canonical example: authentication answered "yes, this is a valid logged-in user," and the endpoint trusted that answer to mean "...so let them read the record," which authentication never claimed. **AuthN is not AuthZ.** Knowing who someone is tells you nothing about what they may touch.

OWASP ranks Broken Access Control as A01 — the most common serious flaw — precisely because it lives in these seams. The fix is structural, not a patch on one route: enforce authorization in trusted server-side code, deny by default for every non-public resource, and check record *ownership* on the object the request names, not just the route. An attacker who flips `{id}` in `GET /accounts/{id}/statements` is doing IDOR; the only thing that stops it is the server asking "does *this* subject own *this* object?" on every request.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Layer</th>
        <th class="border border-border px-3 py-2 text-left">Looks correct alone</th>
        <th class="border border-border px-3 py-2 text-left">The seam that still breaks it</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Auth (JWT)</td>
        <td class="border border-border px-3 py-2">Signed, short-lived, verified</td>
        <td class="border border-border px-3 py-2">Endpoint never checks <code>owns(subject, object)</code> → IDOR</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Password hashing</td>
        <td class="border border-border px-3 py-2">Argon2id, salted, ~250ms</td>
        <td class="border border-border px-3 py-2">Reset endpoint has no rate limit → account takeover</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Token storage</td>
        <td class="border border-border px-3 py-2">Strong, valid JWT</td>
        <td class="border border-border px-3 py-2">Kept in <code>localStorage</code> → one XSS reads it all</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">CSRF defense</td>
        <td class="border border-border px-3 py-2"><code>SameSite=Lax</code> cookie</td>
        <td class="border border-border px-3 py-2">A state-changing <code>GET</code> route bypasses it entirely</td>
      </tr>
    </tbody>
  </table>
</div>

## The JWT-in-localStorage seam

When you hear "we use JWTs," ask yourself: where do those tokens live and what can reach them? The token-storage decision is a pure composition problem, so it deserves a close look. A perfectly minted JWT — signed, short expiry, correct claims — is defeated by *where you put it*. Store it in `localStorage` and any successful XSS runs `localStorage.getItem('token')` and exfiltrates it to the attacker's server; the token's own strength is irrelevant once JavaScript can read it. The mitigation is an `HttpOnly` cookie, which JavaScript cannot touch, so XSS can't steal it — but cookies are sent automatically cross-site, which reopens CSRF. You close *that* with `SameSite` plus a CSRF token. Notice the shape: each fix solves one layer and exposes the seam with the next. There is no single setting that wins; you compose `HttpOnly` + `Secure` + `SameSite` + short TTL, and you still have to kill XSS at the source with output encoding and CSP, because an attacker who can run script in your origin has already moved past the storage question.

<Inset kind="why" lang="en">
"We use JWTs, so we're stateless and safe" conflates two unrelated things. A JWT proves authentication and can carry claims; it says nothing about authorization on a specific object, and it does nothing to defend its own storage. Statelessness is an architecture choice. Resistance to IDOR and XSS are separate controls you still have to build — the token format buys you neither.
</Inset>

## Least privilege everywhere, not just at the door

Ask yourself: if this service were compromised right now, how far could an attacker reach? Defense in depth is also about blast radius. When a layer does fail — and the model assumes one will — least privilege decides how far the damage spreads. The vault credential the API uses should grant only the rows and operations that endpoint needs, so a compromised service can't drain the whole database. The access token should be short-lived so a stolen one expires in minutes, not weeks. The dependency you pulled in should run with the same scrutiny, because a malicious transitive package executes inside your trust boundary. NIST SP 800-63B and the OWASP password guidance set the floor for the auth layer — Argon2id tuned to roughly 250–500ms per hash, or bcrypt at cost factor 12+, with a unique per-user salt — but a strong hash behind an unrate-limited reset endpoint still falls. The number on one layer never excuses a gap in the next.

<TradeoffMatrix
  id="08-security-overview-tradeoff-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="A logged-in user calls `GET /accounts/{id}/statements` with a valid, signed JWT but an id that isn't theirs. What actually stops the breach?"
  options={[
    { name: "Server-side authorization that checks the subject owns the named object on every request (deny-by-default)", summary: "Check ownership, not just identity.", correct: true, justification: "This is the seam in the hook. Authentication already confirmed a valid user; the missing control is authorization on the specific object — does this subject own account {id}? Enforcing owns(subject, object) in trusted server-side code, denying by default, is the only thing that stops IDOR. OWASP ranks Broken Access Control as A01 precisely because teams verify identity and forget object-level authorization." },
    { name: "A stronger signing algorithm and shorter token expiry on the JWT", summary: "Harden the token.", justification: "The token is already valid and the caller is genuinely logged in — the attack uses a real session, not a forged one. Hardening authentication does nothing for an authorization gap. This is the classic mistake of treating AuthN as if it were AuthZ." },
    { name: "Argon2id password hashing and TLS with HSTS", summary: "Lock the lower layers.", justification: "Both are correct and necessary, but they sit on different layers. Transport secrecy and password storage don't decide whether a logged-in caller may read a record they don't own. Each layer passing its own test is exactly how the seam breach happens." },
    { name: "Move the token from localStorage into an HttpOnly cookie", summary: "Fix storage.", justification: "Good hygiene against XSS-based theft, but irrelevant here: the attacker is using their own legitimate token, not a stolen one. The flaw is missing object-level authorization, which no storage choice addresses." },
  ]}
/>

<Quiz
  id="08-security-overview-quiz-1"
  lessonSlug="01-overview"
  lang="en"
  question="A strong, signed JWT is stored in localStorage. What is the realistic way it gets stolen?"
  choices={[
    { label: "An XSS payload runs localStorage.getItem('token') and ships it to an attacker server — JavaScript can read localStorage", correct: true },
    { label: "An attacker brute-forces the JWT signature over the network", misconception: "A properly signed JWT with a strong secret isn't brute-forced in practice; the realistic theft path is reading it from storage via injected script, not breaking the crypto." },
    { label: "TLS interception on the wire", misconception: "TLS plus HSTS protects the token in transit; the localStorage exposure is in the browser, where HttpOnly cookies — not TLS — are the relevant control." },
    { label: "CSRF forces the browser to send the token", misconception: "CSRF rides cookies sent automatically; a token in localStorage is attached by your own JS, so CSRF isn't the vector — XSS reading the value is." },
  ]}
/>

<Quiz
  id="08-security-overview-quiz-2"
  lessonSlug="01-overview"
  lang="en"
  question="What does 'defense in depth' fundamentally assume about your controls?"
  choices={[
    { label: "Every control eventually fails, so you stack independent layers where one catches what another misses", correct: true },
    { label: "One sufficiently strong control (like a perfect JWT) makes the others optional", misconception: "This is the single-control fallacy. A strong token doesn't provide authorization, input validation, or storage safety — depth exists because no one layer covers the others." },
    { label: "If each layer passes its own test, the system is secure", misconception: "Each layer passing in isolation is exactly the condition under which a seam breach occurs — the gap is between correct layers, not inside any one of them." },
    { label: "More layers always slow the app too much to be worth it", misconception: "Layers are about blast radius and redundancy, not raw cost; least privilege and short token lifetimes are cheap and limit damage when a layer falls." },
  ]}
/>

<DragOrder
  id="08-security-overview-drag-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="Order the layers a single sensitive request passes through, outermost to innermost:"
  items={[
    { id: "a", label: "Transport: TLS + HSTS so the request can't be read or downgraded" },
    { id: "b", label: "Authentication: prove who is calling (short-lived token, hashed password)" },
    { id: "c", label: "Authorization: deny-by-default, check this subject owns this object" },
    { id: "d", label: "Input handling: validate, parameterize, encode every field" },
    { id: "e", label: "Secrets + dependencies: keys in a vault, packages vetted and pinned" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Security is layered: TLS, auth, authz, input, secrets, dependencies. Each can be correct alone yet the system falls in the seam between two — AuthN is not AuthZ. Assume every control fails; stack them deny-by-default.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="08-security-overview-retrieval"
  lang="en"
  questions={[
    { q: "A teammate says the login flow is secure because passwords are Argon2id-hashed and the JWT is strong. Explain why that isn't enough and where the real breach hides.", a: "Argon2id hashing and a strong JWT are two layers — authentication and credential storage — and both can be flawless while the system still falls. Authentication answers only 'who is calling', and a JWT carries identity, not permission on a specific object. The breach hides in the authorization seam: an endpoint like GET /accounts/{id}/statements that trusts a valid token to mean the caller may read any id it asks for. That is IDOR, and OWASP ranks Broken Access Control as A01 because teams verify identity correctly and then forget object-level checks. The fix is structural — enforce owns(subject, object) in trusted server-side code, deny by default for non-public resources, and never let a passed lower layer excuse a missing upper one. Defense in depth assumes each control eventually fails, so a strong hash behind an unrate-limited reset endpoint, or a strong token read out of localStorage by XSS, still loses." },
    { q: "Walk through why a perfectly valid JWT can still be stolen, and what composing controls looks like in practice.", a: "The token's strength is about whether it can be forged, but theft is about where it lives. In localStorage, any successful XSS runs localStorage.getItem('token') and exfiltrates it — the signature and short expiry are irrelevant once JavaScript can read the value. Moving it to an HttpOnly cookie blocks that, because JS can't touch the cookie, but cookies are sent automatically cross-site, reopening CSRF; you close that with SameSite plus a CSRF token. Each fix solves one layer and exposes the seam with the next, which is the whole point: there is no single winning setting. In practice you compose HttpOnly + Secure + SameSite + short TTL, and you still eliminate XSS at the source with output encoding and a Content Security Policy, because an attacker who can run script in your origin has already moved past the storage question. That is defense in depth — independent controls so that when one fails, the next still limits the blast radius." },
  ]}
/>

<Recap lang="en">
A single sensitive request passes through layers — transport, authentication, authorization, input handling, secrets, supply chain — and the security track has covered each. The lesson of putting them together is that no one control is sufficient, and the dangerous failures are not missing layers but two correct layers that don't compose. Authentication proves who is calling and says nothing about whether they may touch a specific object, which is why Broken Access Control is OWASP's #1 risk: the breach lives in the seam, an endpoint trusting a valid token to mean ownership. A strong Argon2id hash falls behind an unrate-limited reset; a strong JWT is exfiltrated from localStorage by one XSS. Defense in depth assumes every control eventually fails, so you stack independent layers, deny by default, apply least privilege to credentials and token lifetimes, and enforce authorization on the object — not just the identity — on every request. Now when you see a "secure" login flow, your first question isn't "is the JWT strong?" — it's "which seam is this flow not composing?" Run the threat model across layers, not control by control, and find the gap before an attacker finds it for you.
</Recap>
