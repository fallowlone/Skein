---
slug: "01-windows-vs-group-by"
lang: en
track: "sql-postgres"
unit: "04-window-functions"
order: 1
title: "Windows vs GROUP BY"
summary: "A window function computes an aggregate-like value per row without collapsing the result set. GROUP BY collapses; OVER() keeps every row. Reach for a window when you need detail rows and an aggregate in the same query."
estMin: 14
status: ready
lessonType: topic
level: middle
concepts:
  - window-function
  - over-clause
  - group-by-collapse
  - per-row-aggregate
prereqs:
  - 00-start-here/02-how-postgres-executes-a-query
sources:
  - https://www.postgresql.org/docs/current/tutorial-window.html
  - https://www.postgresql.org/docs/current/functions-window.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import resultShape from "./infographics/result-shape.svg?raw";

<Hook>
The dashboard ticket reads: "show every sale, and next to each one, what percent of its region's total it was." A junior writes a `GROUP BY region`, gets four rows back, and realizes with dread that the detail rows are gone. The senior writes one `OVER (PARTITION BY region)` and ships it in a line. Same arithmetic, opposite result shape — and the difference is the most useful trick in analytic SQL. In the next ten minutes you'll understand exactly why `GROUP BY` loses the rows and how `OVER` keeps them, so the next time a report demands "each row plus a group aggregate" you'll reach for the right tool immediately.
</Hook>

<Crux>A window function returns an aggregate per row without collapsing — GROUP BY destroys detail rows, OVER() keeps them all.</Crux>

<Explanation>

## GROUP BY collapses, the window keeps every row

`GROUP BY` is a *reducer*. You hand it many rows, it returns one row per group. After `GROUP BY region` you have four rows — one per region — and there is no way to also see the individual sales, because they were folded into the aggregate. That is the whole point of grouping: collapse detail into a summary.

A **window function** runs the *same* aggregate math but emits the result alongside **every original row**. Nothing collapses. The `OVER (...)` clause is what makes a function a window function: it tells Postgres "compute this over a set of related rows — the *window* — but attach the answer to each row instead of folding rows together."

```sql
-- GROUP BY: 4 rows out, detail gone
SELECT region, SUM(amount) AS region_total
FROM sales
GROUP BY region;

-- Window: every sale row kept, region total attached to each
SELECT region, salesperson, sale_date, amount,
       SUM(amount) OVER (PARTITION BY region) AS region_total
FROM sales;
```

The first query answers "what did each region sell?" The second answers "show me each sale *and* its region's total on the same line" — which is exactly what the dashboard ticket needed. Same `SUM`, same `region` grouping logic; the only structural difference is `OVER (PARTITION BY region)` instead of `GROUP BY region`.

<FlowDiagram
  label="Eight raw sale rows enter. GROUP BY collapses them to one row per region — four rows out, detail lost. A window function with OVER attaches the same region total to all eight rows — eight rows out, detail kept."
  perRow={3}
  hue="var(--d-data)"
  nodes={[
    { id: "rows", label: "8 sale rows", sub: "raw detail" },
    { id: "group", label: "GROUP BY region", sub: "reducer" },
    { id: "g-out", label: "4 rows", sub: "detail GONE" },
    { id: "rows2", label: "8 sale rows", sub: "same input" },
    { id: "win", label: "SUM() OVER", sub: "per-row" },
    { id: "w-out", label: "8 rows", sub: "detail KEPT" },
  ]}
  edges={[
    { from: "rows", to: "group" },
    { from: "group", to: "g-out" },
    { from: "rows2", to: "win" },
    { from: "win", to: "w-out" },
  ]}
  caption="Top path: GROUP BY is a reducer — fewer rows out. Bottom path: a window keeps row count identical and just adds a column. That row-count invariant is the mental model."
/>

## The percent-of-total pattern, in one query

Because the window keeps every row, you can mix a detail value and an aggregate in the *same* `SELECT` and divide them — no self-join, no subquery:

```sql
SELECT region, salesperson, amount,
       ROUND(
         100.0 * amount / SUM(amount) OVER (PARTITION BY region),
         1
       ) AS pct_of_region
FROM sales
ORDER BY region, pct_of_region DESC;
```

`amount` is the row's own value; `SUM(amount) OVER (PARTITION BY region)` is its region's total computed across all rows in the same region. Divide and you get each sale's share of its region — per row. With plain `GROUP BY` this requires computing the totals separately and joining them back, which is more code and a second pass over the data.

## When to use which — the decision rule

Ask yourself: does the requirement mention "each row" or "per row" next to an aggregate? That single question determines the tool.

The choice is not about which is "better"; it is about the **shape of the answer you need**:

- Need a **summary only** (one row per group)? Use `GROUP BY`. Reports, counts per status, daily totals.
- Need **detail rows *and* an aggregate** on the same row? Use a window. Percent-of-total, rank within group, running balance, "how does this row compare to its group's average."

A reliable production rule: *if the word "each" or "per row" appears in the requirement next to an aggregate, you want a window.* "Show **each** order with its customer's lifetime total" — window. "Show the total **per** customer" — `GROUP BY`. Mixing them up is the classic junior bug: reaching for `GROUP BY` and then discovering the detail rows you needed are gone, or trying to put a non-aggregated column in a `GROUP BY` query and hitting `column must appear in the GROUP BY clause`.

<Infographic
  svg={resultShape}
  label="Side-by-side of the same SUM two ways. GROUP BY is a reducer: one row per group, detail rows destroyed, 8 sales in becomes 4 rows out, used for a summary like reports and counts. A window via OVER() keeps every input row, the aggregate rides alongside, 8 sales in stays 8 rows out, used when you need detail plus an aggregate such as percent-of-total, rank, or a running balance."
  caption="Same SUM, opposite result shape: GROUP BY collapses to a summary; OVER() keeps every row and rides the aggregate alongside. Pick by the shape of the answer you need."
  hue="var(--d-infra)"
/>

<Inset kind="why" lang="en">
Why can't you just `SELECT salesperson, SUM(amount) ... GROUP BY region` and get both? Because once rows are grouped, `salesperson` is ambiguous — a region has many salespeople, so which one's name would the single output row carry? SQL refuses, demanding every selected column be either grouped or aggregated. Windows sidestep this entirely: rows are never collapsed, so `salesperson` stays unambiguous on its own row while `SUM(amount) OVER (...)` rides alongside. The aggregation logic in unit `03-aggregation` and the windowing logic here are two answers to "summarize," distinguished only by whether detail survives.
</Inset>

<Quiz
  id="01-windows-vs-group-by-quiz-1"
  lessonSlug="01-windows-vs-group-by"
  lang="en"
  question="You run SELECT region, salesperson, SUM(amount) OVER (PARTITION BY region) FROM sales; against a table of 8 sales across 4 regions. How many rows come back?"
  choices={[
    { label: "8 — a window function attaches the aggregate to every original row; nothing collapses", correct: true },
    { label: "4 — one row per region, like GROUP BY", misconception: "That would be GROUP BY. OVER() does NOT collapse rows; it keeps all 8 and adds the region total as a column on each." },
    { label: "1 — a single grand total", misconception: "PARTITION BY region computes per region, and a window never reduces to one row anyway; the result keeps all input rows." },
    { label: "It errors — salesperson is not in a GROUP BY", misconception: "There is no GROUP BY here, so the rule doesn't apply. Windows leave rows intact, so non-aggregated columns are always allowed." },
  ]}
/>

<Quiz
  id="01-windows-vs-group-by-quiz-2"
  lessonSlug="01-windows-vs-group-by"
  lang="en"
  question="A requirement says: 'list every event with the running count of events for that user.' Which tool fits and why?"
  choices={[
    { label: "A window function — you need each detail row kept AND an aggregate alongside it", correct: true },
    { label: "GROUP BY user_id — it aggregates per user", misconception: "GROUP BY would return one row per user and discard the individual events, but the requirement says 'list every event' — detail must survive, so it's a window." },
    { label: "A correlated subquery per row — the only way to keep detail", misconception: "A correlated subquery works but re-scans per row and is far slower; the window computes it in one pass. Detail-plus-aggregate is the window's job." },
    { label: "DISTINCT — to count uniques", misconception: "DISTINCT removes duplicate rows; it neither keeps all detail rows nor computes a per-user running count." },
  ]}
/>

<DragOrder
  id="01-windows-vs-group-by-drag-1"
  lessonSlug="01-windows-vs-group-by"
  lang="en"
  prompt="Order these steps to add 'percent of region total' to every sale row:"
  items={[
    { id: "a", label: "Start from the detail query: SELECT region, salesperson, amount FROM sales" },
    { id: "b", label: "Add SUM(amount) OVER (PARTITION BY region) — the region total, kept per row" },
    { id: "c", label: "Divide the row's amount by that windowed total" },
    { id: "d", label: "Multiply by 100.0 and ROUND to get a readable percent" },
    { id: "e", label: "ORDER BY region, pct DESC to present it" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>`GROUP BY` is a reducer: many rows in, one row per group out, detail destroyed. A window function with `OVER (...)` runs the same aggregate math but keeps every input row, attaching the result as a new column. Use `GROUP BY` for a summary; use a window whenever you need detail rows *and* an aggregate together — the percent-of-total, rank-in-group, running-total family of problems.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-windows-vs-group-by-retrieval"
  lang="en"
  questions={[
    { q: "In one sentence, what is the structural difference between GROUP BY and a window function?", a: "GROUP BY collapses many rows into one row per group (detail is lost), while a window function with OVER() computes an aggregate-like value but keeps every input row, attaching the result as an extra column." },
    { q: "You need every order shown next to its customer's lifetime total. GROUP BY or window — and why?", a: "A window: SUM(amount) OVER (PARTITION BY customer_id). The requirement says 'every order' so detail rows must survive; GROUP BY customer_id would collapse to one row per customer and discard the individual orders." },
    { q: "Why does SELECT salesperson, SUM(amount) ... GROUP BY region fail, but the same with OVER (PARTITION BY region) works?", a: "Under GROUP BY, each output row represents a whole region, so a single salesperson value is ambiguous and SQL rejects it. The window version never collapses rows, so salesperson stays valid on its own row while the windowed SUM rides alongside it." },
  ]}
/>

<Recap lang="en">
`GROUP BY` and window functions answer the same "summarize" question with opposite result shapes. `GROUP BY` is a **reducer** — it folds detail rows into one row per group, so afterward the individual rows are gone. A **window function** adds `OVER (...)` to an aggregate so the same math is computed over a window of related rows but the answer is attached to **every** original row; the row count is unchanged. That row-count invariant is the decision rule: if the requirement needs both detail rows and an aggregate — percent of total, rank within group, running balance, comparison to the group average — you want a window. If it needs only a summary, `GROUP BY` is simpler. Now when you see a ticket that says "show each X with its group's Y," reach for `OVER (PARTITION BY ...)` before you write a single `GROUP BY` — the detail rows will still be there. The next lesson opens up the two clauses that shape a window: `PARTITION BY`, which restarts the window per group, and `ORDER BY` inside `OVER`, which orders rows within it — and quietly changes what the window contains.
</Recap>
