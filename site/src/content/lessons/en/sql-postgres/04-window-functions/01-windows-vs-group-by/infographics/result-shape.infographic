infographic compare-binary-horizontal-underline-text-vs
data
  title Same SUM, opposite result shape
  desc The choice is the shape of the answer you need, not which tool wins
  compares
    - label GROUP BY
      children
        - label GROUP BY
          desc The reducer
        - label One row per group
          desc Detail rows destroyed
        - label 8 sales in, 4 rows out
          desc One row per region
        - label Use for a summary
          desc Reports, counts, daily totals
    - label Window
      children
        - label OVER()
          desc The per-row window
        - label Every input row kept
          desc Aggregate rides alongside
        - label 8 sales in, 8 rows out
          desc Row count unchanged
        - label Use for detail plus aggregate
          desc Percent-of-total, rank, running balance
theme
  palette #f43f5e #14b8a6
