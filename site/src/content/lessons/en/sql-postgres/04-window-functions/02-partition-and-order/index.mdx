---
slug: "02-partition-and-order"
lang: en
track: "sql-postgres"
unit: "04-window-functions"
order: 2
title: "PARTITION BY and ORDER BY"
summary: "PARTITION BY splits rows into independent windows; ORDER BY inside OVER orders rows within each one. The twist: adding ORDER BY silently changes the default frame from the whole partition to a running range."
estMin: 15
status: ready
lessonType: topic
level: middle
concepts:
  - partition-by
  - order-by-in-over
  - default-frame
  - independent-windows
prereqs:
  - 01-windows-vs-group-by
sources:
  - https://www.postgresql.org/docs/current/sql-expressions.html#SYNTAX-WINDOW-FUNCTIONS
  - https://www.postgresql.org/docs/current/tutorial-window.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
A revenue report ships. Finance flags it the next morning: the "region total" column is wrong — it's showing a *running* total that grows down the page instead of the same flat total per region. The query had `SUM(amount) OVER (PARTITION BY region ORDER BY sale_date)`. Nobody asked for a running total. But adding `ORDER BY` inside `OVER` silently turned the aggregate into one. This lesson is about the two clauses that shape a window, and the trap hiding in the second one.
</Hook>

<Crux>PARTITION BY splits rows into independent windows; ORDER BY orders rows within one — and silently makes the default frame a running range.</Crux>

<Explanation>

## PARTITION BY: independent windows side by side

`PARTITION BY` is the spatial dimension of a window. It chops the result set into groups and computes the window function **independently inside each group**, resetting at every boundary. Think of each partition as its own private window that knows nothing about the others.

```sql
SELECT region, salesperson, amount,
       SUM(amount) OVER (PARTITION BY region) AS region_total,
       AVG(amount) OVER (PARTITION BY region) AS region_avg
FROM sales;
```

Every `West` row sees only `West` rows; every `East` row sees only `East`. The `region_total` for a `West` sale is the sum of all `West` amounts and nothing else. This is exactly the role `GROUP BY region` plays in a grouped query — but here the rows are never collapsed. Omit `PARTITION BY` and the whole result set is one giant partition (a grand total over all rows).

<StackDiagram
  label="The sales result set split by PARTITION BY region into three independent windows — West, East, North. Each window computes its own aggregate over only its own rows; partitions never see each other."
  hue="var(--d-data)"
  layers={[
    { label: "Partition: West", note: "SUM/AVG over West rows only" },
    { label: "Partition: East", note: "restarts — East rows only" },
    { label: "Partition: North", note: "restarts — North rows only" },
  ]}
  caption="PARTITION BY = independent windows stacked side by side. The function resets at every partition boundary; a row in one partition never influences another."
/>

## ORDER BY inside OVER: order within the partition

`ORDER BY` *inside* the `OVER (...)` clause is the temporal dimension — it defines the order of rows **within each partition**. This is completely separate from the query's outer `ORDER BY`, which only sorts the final output. The window's `ORDER BY` is what makes "running" and "row-relative" computations possible: it gives the rows a sequence, so a function can say "everything up to here," "the previous row," "the rank so far."

```sql
SELECT region, sale_date, amount,
       ROW_NUMBER() OVER (PARTITION BY region ORDER BY sale_date) AS nth_sale,
       SUM(amount)  OVER (PARTITION BY region ORDER BY sale_date) AS running_total
FROM sales;
```

`ROW_NUMBER()` needs the order to number 1, 2, 3 within each region by date. `SUM` with that same window now produces a **running total** — and that is the surprise.

## The default-frame trap

Here is the rule that bites people. When `OVER` has **no** `ORDER BY`, the window's *frame* — the slice of rows the function actually aggregates — defaults to the **entire partition**. So `SUM(amount) OVER (PARTITION BY region)` sums every row in the region: one flat number, repeated.

The instant you add `ORDER BY` inside `OVER`, the default frame silently changes to `RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW` — "from the first row of the partition up to the current row." That is a **running** aggregate, not a partition-wide one. Same `SUM`, same `PARTITION BY`, but adding an inner `ORDER BY` flips a flat total into a cumulative one.

```sql
-- Flat region total (no inner ORDER BY): same value on every West row
SUM(amount) OVER (PARTITION BY region)

-- Running total (inner ORDER BY): grows down the partition
SUM(amount) OVER (PARTITION BY region ORDER BY sale_date)
```

That is the bug from the Hook. The author wanted a flat region total but added `ORDER BY sale_date` (perhaps copied from a neighboring `ROW_NUMBER`), and the frame quietly became cumulative. The fix is to remove the inner `ORDER BY` for a flat total, or — if some columns need ordering and others don't — name the windows explicitly so each gets the frame it wants.

<Inset kind="mistake" lang="en">
The deepest part of this trap is that it is *silent* — no error, no warning, just a number that's subtly wrong and passes code review because the SQL looks reasonable. Two senior habits defend against it. First: if you want a flat aggregate, never put `ORDER BY` in that window. Second: when a query mixes flat and running aggregates, define named windows with `WINDOW w_flat AS (PARTITION BY region)` and `WINDOW w_run AS (PARTITION BY region ORDER BY sale_date)`, then reference `OVER w_flat` / `OVER w_run`. The next lesson, on frames, makes the running-vs-flat behavior fully explicit.
</Inset>

<Quiz
  id="02-partition-and-order-quiz-1"
  lessonSlug="02-partition-and-order"
  lang="en"
  question="What does PARTITION BY region do inside an OVER clause?"
  choices={[
    { label: "Splits rows into independent windows by region; the function resets at each region and sees only that region's rows", correct: true },
    { label: "Sorts the final output by region", misconception: "That's the outer ORDER BY. PARTITION BY defines which rows share a window, not the output sort order." },
    { label: "Collapses the result to one row per region", misconception: "That's GROUP BY. PARTITION BY keeps every row; it only scopes which rows the window function aggregates over." },
    { label: "Filters out rows from other regions", misconception: "It filters nothing. All rows remain; PARTITION BY just isolates each region into its own window for the calculation." },
  ]}
/>

<Quiz
  id="02-partition-and-order-quiz-2"
  lessonSlug="02-partition-and-order"
  lang="en"
  question="You want a FLAT region total on every row, but you wrote SUM(amount) OVER (PARTITION BY region ORDER BY sale_date). What goes wrong?"
  choices={[
    { label: "You get a running total instead — the inner ORDER BY changes the default frame to UNBOUNDED PRECEDING .. CURRENT ROW", correct: true },
    { label: "Nothing — ORDER BY inside OVER only sorts and doesn't affect the SUM", misconception: "It does affect it. With an inner ORDER BY the default frame becomes 'start of partition to current row', making SUM cumulative, not flat." },
    { label: "It errors because ORDER BY isn't allowed with SUM", misconception: "ORDER BY is perfectly legal in OVER with SUM; the issue is the silent frame change it triggers, not an error." },
    { label: "It sorts the whole output by sale_date", misconception: "The inner ORDER BY orders rows within the window for the frame, not the final result; the outer query's ORDER BY controls output order." },
  ]}
/>

<MetaphorComplete
  id="02-partition-and-order-metaphor-1"
  lessonSlug="02-partition-and-order"
  lang="en"
  setup="Fill in the blank: with no inner ORDER BY, the window's frame is the whole partition (a flat total). Adding ORDER BY inside OVER silently changes the default frame to a _______ range, turning a flat aggregate into a cumulative one."
  accepted={["running", "cumulative", "growing"]}
  canonical="running"
  explanation="No inner ORDER BY → frame = entire partition → flat aggregate. Inner ORDER BY → default frame = RANGE UNBOUNDED PRECEDING to CURRENT ROW → a running (cumulative) aggregate. The change is implicit, which is exactly why it surprises people."
/>

</Explanation>

<KeyTakeaway>`PARTITION BY` is the window's spatial split — independent sub-windows that reset at each boundary and never see one another. `ORDER BY` inside `OVER` is its temporal order — and it does double duty: it sequences rows *and* silently switches the default frame from the whole partition to `UNBOUNDED PRECEDING .. CURRENT ROW`, turning a flat aggregate into a running one. Add the inner `ORDER BY` only when you want that running behavior.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-partition-and-order-retrieval"
  lang="en"
  questions={[
    { q: "What is the difference between PARTITION BY and ORDER BY inside an OVER clause?", a: "PARTITION BY splits rows into independent windows (the function resets per group and sees only that group's rows). ORDER BY inside OVER defines the order of rows within each partition, enabling running and row-relative computations — and it is distinct from the outer query ORDER BY that sorts final output." },
    { q: "Why does SUM(amount) OVER (PARTITION BY region ORDER BY sale_date) give a running total instead of a flat region total?", a: "Adding ORDER BY inside OVER changes the default frame from the entire partition to RANGE BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW. So the SUM now covers only rows from the start of the partition up to the current row — a cumulative total that grows down the partition." },
    { q: "You need both a flat region total and a running total in the same query. How do you avoid the frame trap?", a: "Use two distinct windows: SUM(amount) OVER (PARTITION BY region) for the flat total (no inner ORDER BY), and SUM(amount) OVER (PARTITION BY region ORDER BY sale_date) for the running total. Naming them with a WINDOW clause (w_flat, w_run) makes the intent explicit and avoids accidentally ordering the flat one." },
  ]}
/>

<Recap lang="en">
A window is shaped by two clauses inside `OVER (...)`. `PARTITION BY` is spatial: it carves the rows into independent windows that reset at each boundary, so a function only ever sees rows in its own partition — the same scoping `GROUP BY` does, minus the collapse. `ORDER BY` inside `OVER` is temporal: it orders rows within each partition so functions can compute "up to here," "previous row," or "rank so far." The trap is that this inner `ORDER BY` is not just a sort — it silently changes the default frame from the entire partition to `RANGE UNBOUNDED PRECEDING .. CURRENT ROW`, flipping a flat aggregate into a running one with no error. The defense is intentional windows: omit the inner `ORDER BY` for flat totals, add it only for running ones, and name your windows when a query needs both. Now when you see a window query return a running total where you expected a flat one, check the inner `ORDER BY` first — nine times out of ten, that is the culprit. The next lesson makes frames fully explicit — `ROWS` vs `RANGE` vs `GROUPS`, and the bound syntax — so you control exactly which rows each function aggregates.
</Recap>
