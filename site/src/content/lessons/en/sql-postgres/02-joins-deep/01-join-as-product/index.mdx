---
slug: "01-join-as-product"
lang: en
track: "sql-postgres"
unit: "02-joins-deep"
order: 1
title: "A join is a filtered product"
summary: "Every join starts as the Cartesian product of two tables — every row of A paired with every row of B — and the ON predicate is just a filter on that product. Internalize this and join behavior stops being magic."
estMin: 14
status: ready
lessonType: topic
level: middle
concepts:
  - cartesian-product
  - cross-join
  - join-predicate
  - row-cardinality
  - set-combination
prereqs:
  - 00-start-here/02-how-postgres-executes-a-query
sources:
  - https://www.postgresql.org/docs/current/queries-table-expressions.html
  - https://www.postgresql.org/docs/current/tutorial-join.html
  - https://use-the-index-luke.com/sql/join
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A reporting query that ran fine in staging returned 4.2 billion rows in production and pinned a CPU for nine minutes before someone killed it. The cause was one missing word in the `ON` clause. The author thought of a join as "matching rows up". The engine thinks of it as something much blunter: pair *everything* with *everything*, then throw away what doesn't match. Once you see joins the engine's way, that nine-minute incident becomes obvious in advance.
</Hook>

<Crux>Every join is a filtered Cartesian product: pair every row of A with every row of B, then keep only the pairs the ON predicate accepts.</Crux>

<Explanation>

## The Cartesian product is the foundation

Forget "matching" for a moment. The most basic combination of two tables is the **Cartesian product** (the `CROSS JOIN`): take every row of `users` and pair it with every single row of `orders`. If `users` has 3 rows and `orders` has 4, the product has exactly 3 × 4 = 12 rows. No condition, no matching — just every possible pairing.

```sql
-- The raw product: every user paired with every order
SELECT u.id AS user_id, o.id AS order_id
FROM users u
CROSS JOIN orders o;
-- 3 users × 4 orders = 12 rows, regardless of who placed what
```

The row count of a product is `|A| × |B|` — it *multiplies*. That single fact is the whole reason a join can explode. Two tables of 100k rows have a product of 10 billion rows. The engine will never materialize all of those if it can help it, but the *logical* meaning of your query is still "start from that product."

<FlowDiagram
  label="A 3-by-4 grid where every row of A is paired with every row of B, producing 12 cells; an ON predicate then keeps only the matching cells."
  perRow={4}
  hue="var(--d-data)"
  nodes={[
    { id: "a1b1", label: "u1 × o1", sub: "keep" },
    { id: "a1b2", label: "u1 × o2", sub: "drop" },
    { id: "a1b3", label: "u1 × o3", sub: "drop" },
    { id: "a1b4", label: "u1 × o4", sub: "drop" },
    { id: "a2b1", label: "u2 × o1", sub: "drop" },
    { id: "a2b2", label: "u2 × o2", sub: "keep" },
    { id: "a2b3", label: "u2 × o3", sub: "keep" },
    { id: "a2b4", label: "u2 × o4", sub: "drop" },
    { id: "a3b1", label: "u3 × o1", sub: "drop" },
    { id: "a3b2", label: "u3 × o2", sub: "drop" },
    { id: "a3b3", label: "u3 × o3", sub: "drop" },
    { id: "a3b4", label: "u3 × o4", sub: "keep" },
  ]}
  edges={[]}
  caption="The full 3×4 product is 12 cells. The ON predicate (here u.id = o.user_id) accepts only the cells where the keys line up — 4 of them. A join is exactly this grid with a filter applied."
/>

## An INNER JOIN is that product, filtered

An `INNER JOIN ... ON <predicate>` is *definitionally* the Cartesian product with the predicate applied as a filter. These two queries are logically identical:

```sql
-- Written as a join (what you'll always write)
SELECT u.id, o.id, o.total
FROM users u
JOIN orders o ON o.user_id = u.id;

-- Written as the product + filter (what it MEANS)
SELECT u.id, o.id, o.total
FROM users u
CROSS JOIN orders o
WHERE o.user_id = u.id;
```

Both ask: of all 12 possible pairings, keep the ones where `o.user_id = u.id`. The `ON` clause is not special syntax — it is a filter on the product. The planner never literally builds the 12-row grid (it uses hash or merge or nested-loop strategies to reach the same answer cheaply — that's the `databases` track's join-algorithms lesson), but the *result* is guaranteed to equal "product, then filter."

This reframing pays off immediately. The output row count of an inner join is **not** `|A|` and **not** `|B|`. It is "how many pairs survive the filter," which depends entirely on the relationship. If every order has exactly one matching user, the result has `|orders|` rows. If the join key is non-unique on both sides, you can get *more* rows than either table — a fan-out, the subject of this unit's last lesson.

## Why CROSS JOIN behavior is a feature, not just a footgun

The incident in the hook was an accidental product: someone wrote a join, forgot the `ON` predicate (or wrote `ON true`), and got `|A| × |B|`. SQL doesn't stop you, because a product is sometimes exactly what you want — a calendar grid, an "every product × every size" combination matrix, filling gaps. The fourth lesson covers deliberate cross joins. The discipline: an `ON` clause that doesn't actually constrain the relationship is silently a product, and a product on two large tables is an outage.

<Inset kind="why" lang="en">
Why does thinking "product then filter" matter when the planner never builds the product? Because it makes cardinality predictable, and cardinality is what governs cost. When you can answer "how many rows does this join emit?" before running it, you can spot fan-outs, accidental cross joins, and missing predicates by inspection. The planner's whole job is to *avoid* materializing the product while returning the product-then-filter answer — but the answer is defined by the product, so your mental model has to start there. The `databases` execution-plans chapter shows how nested-loop, hash, and merge joins each reach that answer with different cost profiles.
</Inset>

<Quiz
  id="01-join-as-product-quiz-1"
  lessonSlug="01-join-as-product"
  lang="en"
  question="users has 5 rows, orders has 8 rows. You run SELECT * FROM users CROSS JOIN orders. How many rows come back?"
  choices={[
    { label: "40 — the Cartesian product is |users| × |orders| = 5 × 8", correct: true },
    { label: "13 — the rows are added together", misconception: "A product multiplies, it does not add. CROSS JOIN pairs every left row with every right row, so it is 5 × 8 = 40, not 5 + 8." },
    { label: "8 — one row per order", misconception: "That would be the result of a one-to-one join on a key, not a cross join. Without a predicate, every user pairs with every order." },
    { label: "5 — one row per user", misconception: "A cross join does not collapse to the left table; each user is paired with all 8 orders, giving 40 rows." },
  ]}
/>

<Quiz
  id="01-join-as-product-quiz-2"
  lessonSlug="01-join-as-product"
  lang="en"
  question="Which statement is logically equivalent to: FROM users u JOIN orders o ON o.user_id = u.id ?"
  choices={[
    { label: "FROM users u CROSS JOIN orders o WHERE o.user_id = u.id — the product, then filtered by the predicate", correct: true },
    { label: "FROM users u, orders o with no WHERE — that already restricts to matches", misconception: "Comma without a WHERE is a bare cross join; it does NOT filter. You would get the full |u| × |o| product." },
    { label: "Two separate SELECTs whose results are concatenated", misconception: "Concatenation is UNION, which stacks rows vertically. A join combines columns horizontally from a filtered product." },
    { label: "It cannot be expressed without the JOIN keyword", misconception: "An inner join is exactly a cross join plus a WHERE filter; the JOIN ... ON form is sugar for that product-then-filter." },
  ]}
/>

<MetaphorComplete
  id="01-join-as-product-metaphor-1"
  lessonSlug="01-join-as-product"
  lang="en"
  setup="Fill in the blank: an INNER JOIN is the Cartesian _______ of two tables with the ON predicate applied as a filter — every left row paired with every right row, then the non-matching pairs discarded."
  accepted={["product"]}
  canonical="product"
  explanation="The Cartesian product is the set of all possible pairs (|A| × |B| rows). A join keeps only the pairs that satisfy ON. This is why a join's output size depends on the relationship, not on either table's row count alone — and why a missing predicate yields the full product."
/>

</Explanation>

<KeyTakeaway>A join is a filtered Cartesian product. The product pairs every row of A with every row of B (|A| × |B| rows, multiplying), and the ON predicate keeps only the pairs that match. The output row count is "surviving pairs", never simply |A| or |B| — so a missing or too-loose predicate silently produces an explosive product.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-join-as-product-retrieval"
  lang="en"
  questions={[
    { q: "Define a join in terms of the Cartesian product and a predicate.", a: "An inner join is the Cartesian product of the two tables (every row of A paired with every row of B, giving |A| × |B| candidate pairs) with the ON predicate applied as a filter: only the pairs that satisfy the predicate are kept. The ON clause is just a filter on the product, not special matching syntax." },
    { q: "users has 3 rows, orders has 4. What is the row count of a CROSS JOIN, and what does the inner join on o.user_id = u.id return instead?", a: "The CROSS JOIN returns 3 × 4 = 12 rows — the full product. The inner join returns only the pairs where o.user_id = u.id; if every order belongs to exactly one of these users, that is one row per matching order, which is a subset of the 12." },
    { q: "Why is 'product then filter' the right mental model even though Postgres never materializes the full product?", a: "Because the result is defined by it: the answer equals product-then-filter regardless of which physical algorithm (nested loop, hash, merge) the planner uses to compute it. Holding that model lets you predict cardinality — spotting fan-outs, accidental cross joins, and missing predicates by inspection before they become an outage." },
  ]}
/>

<Recap lang="en">
The foundation of every join is the **Cartesian product**: pair every row of A with every row of B, giving `|A| × |B|` rows — a count that *multiplies*, which is why joins can explode. A `CROSS JOIN` is that raw product; an `INNER JOIN ... ON p` is exactly the product with `p` applied as a filter, identical to `CROSS JOIN ... WHERE p`. Because of this, a join's output size is "how many pairs survive the predicate," never simply the size of either table — so a missing or too-loose `ON` clause silently degrades into a full product and an outage. The planner never builds the literal grid; it uses hash, merge, or nested-loop strategies (the `databases` join-algorithms lesson) to reach the same product-then-filter answer cheaply. Hold this model and the rest of the unit — outer joins, semi/anti joins, lateral, and fan-out traps — are all just variations on "which pairs do we keep, and what do we do with the rows that have no partner."
</Recap>
