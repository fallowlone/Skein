---
slug: "04-self-and-cross-joins"
lang: en
track: "sql-postgres"
unit: "02-joins-deep"
order: 4
title: "Self and cross joins"
summary: "A self-join joins a table to itself to walk hierarchies or compare rows pairwise; dedup pairs with a.id < b.id. An explicit CROSS JOIN builds deliberate products — calendar grids, gap-fills, all-combinations — distinct from an accidental comma join."
estMin: 14
status: ready
lessonType: topic
level: middle
concepts:
  - self-join
  - adjacency-hierarchy
  - pairwise-comparison
  - cross-join
  - generate-series
prereqs:
  - 03-semi-and-anti-joins
sources:
  - https://www.postgresql.org/docs/current/queries-table-expressions.html
  - https://www.postgresql.org/docs/current/tutorial-join.html
  - https://www.postgresql.org/docs/current/functions-srf.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A "find duplicate products" query joined the `products` table to itself, matched on equal name, and returned 1.4 million pairs from a table of 1,200 dupes. Every duplicate was reported twice (A–B and B–A) and every row also matched *itself* (A–A). The logic was right; the dedup was missing. A self-join is the most error-prone join precisely because both sides are the same table — and the fix is one comparison operator most people forget.
</Hook>

<Crux>A self-join joins a table to itself for hierarchies and pairs; an ordering predicate dedups them. CROSS JOIN makes deliberate products.</Crux>

<Explanation>

## Self-join: a table joined to itself

A self-join is just a join where both sides are the *same* table, given two different aliases. There's nothing special in the engine — it's the same filtered-product machinery — but it unlocks two whole classes of query.

**Walking a hierarchy (adjacency list).** A common pattern stores a tree by having each row point to its parent: `users` could have a `referred_by` column, or an `employees`-style table has `manager_id` referencing the same table's `id`. To pair each employee with their manager's name, join the table to itself:

```sql
-- Each employee alongside their manager's name (one level of hierarchy)
SELECT e.id, e.name AS employee, m.name AS manager
FROM employees e
LEFT JOIN employees m ON m.id = e.manager_id;
-- LEFT JOIN so the CEO (manager_id IS NULL) still appears, manager = NULL
```

The `LEFT JOIN` matters: the top of the tree has no parent, so an inner self-join would silently drop the root. (One self-join climbs exactly one level; arbitrary-depth ancestry needs a recursive CTE — unit 05.)

<FlowDiagram
  label="A self-join on employees: each employee row's manager_id points back into the same table to find the manager row, so e (employee alias) and m (manager alias) are the same physical table."
  perRow={3}
  hue="var(--d-data)"
  nodes={[
    { id: "ceo", label: "1 CEO", sub: "manager_id = NULL" },
    { id: "vp", label: "2 VP", sub: "manager_id = 1" },
    { id: "eng", label: "3 Eng", sub: "manager_id = 2" },
  ]}
  edges={[
    { from: "vp", to: "ceo", label: "manager_id → id" },
    { from: "eng", to: "vp", label: "manager_id → id" },
  ]}
  caption="The same table aliased twice. e.manager_id matches m.id, so each non-root row finds its manager. A LEFT JOIN keeps the CEO (no manager); an inner join would drop the root."
/>

**Pairwise comparison.** The other use is comparing rows of a table against other rows of the same table: find duplicate products, users in the same city, orders within an hour of each other. This is where the hook's bug lives.

```sql
-- Pairs of products with the same name — deduped and self-excluded
SELECT a.id AS id_a, b.id AS id_b, a.name
FROM products a
JOIN products b ON a.name = b.name
WHERE a.id < b.id;     -- the critical line
```

Without `WHERE a.id < b.id`, you get three kinds of garbage: each row matches itself (`a.id = b.id`), and each genuine pair appears twice in both orders (A–B and B–A). The predicate `a.id < b.id` solves both at once — it excludes self-matches (an id is never less than itself) and keeps only one ordering of each pair. Use `<` for unordered pairs; use `<>` only if you genuinely want both directions.

## Cross join: a deliberate product

The last lesson framed the Cartesian product as a footgun. But sometimes the product is exactly the result you want, and you write `CROSS JOIN` to say so on purpose. The canonical case is a **dense grid** — every combination of two dimensions, including the empty cells.

```sql
-- Every (day, product) cell for a 7-day report, even days with zero sales
SELECT d.day, p.id AS product_id, COALESCE(s.qty, 0) AS qty
FROM generate_series(DATE '2026-06-01', DATE '2026-06-07', INTERVAL '1 day') AS d(day)
CROSS JOIN products p
LEFT JOIN sales s ON s.product_id = p.id AND s.sold_on = d.day;
```

Here `generate_series` produces the 7 days, `CROSS JOIN products` makes every day-product combination (the scaffold), and the `LEFT JOIN` to actual sales fills in quantities, defaulting to 0 for empty cells. Without the cross join, days with no sales would be missing rows entirely — a classic reporting gap. Other deliberate uses: an "all sizes × all colors" variant matrix, or pairing each row with a fixed set of buckets.

The distinction the lesson title makes: an **explicit** `CROSS JOIN` reads as intent — "yes, I want the product." A product that sneaks in through a comma join (`FROM a, b` with no `WHERE` linking them) or a too-loose `ON` is an **accident** and the outage from lesson 1. Write `CROSS JOIN` when you mean it; never let a product happen silently.

<Inset kind="why" lang="en">
Why is the explicit CROSS JOIN keyword worth using over the old comma syntax? Readability and safety. <code>FROM a, b, c WHERE ...</code> hides whether the product is intended; a reviewer can't tell a deliberate grid from a forgotten join condition. <code>a CROSS JOIN b</code> states the product is on purpose, and reserving the comma/JOIN forms for related tables makes a missing predicate visually obvious. Many teams lint against comma joins entirely for exactly this reason — the syntax that makes accidental products easy is the syntax worth banning.
</Inset>

<Quiz
  id="04-self-and-cross-joins-quiz-1"
  lessonSlug="04-self-and-cross-joins"
  lang="en"
  question="A self-join finds pairs of users in the same city. Why add WHERE a.id < b.id?"
  choices={[
    { label: "It excludes each row matching itself AND keeps only one ordering of each pair, removing duplicates", correct: true },
    { label: "It sorts the output by id", misconception: "It is a filter, not an ORDER BY. It removes self-matches and mirror-duplicate pairs; sorting is a separate concern." },
    { label: "It makes the join run faster by using an index", misconception: "Its purpose is correctness (dedup and self-exclusion), not performance; any speed effect is incidental." },
    { label: "It is required syntax for any self-join", misconception: "Self-joins for hierarchies (e.manager_id = m.id) need no such predicate; it's specific to pairwise-comparison joins to dedup." },
  ]}
/>

<Quiz
  id="04-self-and-cross-joins-quiz-2"
  lessonSlug="04-self-and-cross-joins"
  lang="en"
  question="You build a daily report and want every (day, product) cell to appear, including days with zero sales. Which structure guarantees the empty cells exist?"
  choices={[
    { label: "generate_series(days) CROSS JOIN products, then LEFT JOIN the actual sales", correct: true },
    { label: "INNER JOIN sales to products grouped by day", misconception: "An inner join only emits days that actually had sales; zero-sale days vanish, which is exactly the gap you're trying to avoid." },
    { label: "SELECT from sales with GROUP BY day", misconception: "Grouping only existing sales rows can't invent a row for a day with no sales — the scaffold must come from a cross join of the full dimensions." },
    { label: "A self-join on the sales table", misconception: "A self-join compares sales rows to each other; it cannot manufacture rows for days that have no sales at all." },
  ]}
/>

<MetaphorComplete
  id="04-self-and-cross-joins-metaphor-1"
  lessonSlug="04-self-and-cross-joins"
  lang="en"
  setup="Fill in the blank: an explicit CROSS JOIN says the Cartesian product is _______ — written on purpose to build a grid — whereas a product that sneaks in via a comma join with no linking condition is an accident."
  accepted={["intentional", "deliberate", "intended", "on purpose"]}
  canonical="intentional"
  explanation="The whole point of the CROSS JOIN keyword is to make intent explicit. A reviewer reading CROSS JOIN knows the product is wanted (a grid, a scaffold, an all-combinations matrix); a comma join hides whether the product was meant or a join condition was forgotten — which is why many teams ban comma joins."
/>

</Explanation>

<KeyTakeaway>A self-join joins a table to itself via two aliases — for one-level hierarchies (LEFT JOIN so the root survives) or pairwise comparison. For comparison, `WHERE a.id < b.id` is essential: it excludes self-matches and keeps one ordering of each pair. An explicit CROSS JOIN builds deliberate products (calendar grids via generate_series, all-combinations matrices); reserve it for intended products and never let one happen via a comma join.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-self-and-cross-joins-retrieval"
  lang="en"
  questions={[
    { q: "What two problems does WHERE a.id < b.id solve in a pairwise self-join, and what would happen without it?", a: "It excludes a row matching itself (since a.id is never less than itself) and it keeps only one ordering of each genuine pair (A–B but not the mirror B–A). Without it, every row matches itself and every real pair appears twice in both orders, inflating the result — the 1.4-million-pairs bug. Use < for unordered pairs; <> only if you actually want both directions." },
    { q: "Why use a LEFT JOIN (not INNER) when self-joining employees to their managers, and what does one self-join NOT do?", a: "The root of the hierarchy (e.g. the CEO) has manager_id = NULL, so an inner self-join would find no matching manager row and silently drop the root. A LEFT JOIN keeps it with manager = NULL. A single self-join climbs exactly one level of the tree; walking arbitrary-depth ancestry requires a recursive CTE (unit 05), not repeated self-joins." },
    { q: "Give a legitimate use of an explicit CROSS JOIN and explain why it beats an inner join here.", a: "Building a dense report grid: generate_series of days CROSS JOIN products produces every (day, product) cell as a scaffold, then a LEFT JOIN to actual sales fills quantities with COALESCE(...,0) for empty cells. An inner join would emit only day-product combinations that had sales, dropping zero-sale days and leaving gaps in the report. The cross join deliberately manufactures the complete grid so no combination is missing." },
  ]}
/>

<Recap lang="en">
A **self-join** joins a table to itself under two aliases, reusing the ordinary filtered-product machinery for two jobs. For **hierarchies** (adjacency lists like `employees.manager_id → id`), join the table to itself with a `LEFT JOIN` so the root — whose parent is `NULL` — survives; note a single self-join climbs only one level, and arbitrary depth needs a recursive CTE (unit 05). For **pairwise comparison** (duplicates, same-city users), the indispensable line is `WHERE a.id < b.id`: it removes self-matches and the mirror-image duplicate of every pair in one stroke. A **CROSS JOIN** is the deliberate Cartesian product, the legitimate twin of lesson 1's accidental one: `generate_series(...) CROSS JOIN products LEFT JOIN sales` builds a dense report grid where even zero-sale days appear, an all-combinations matrix the data alone can't produce. Write `CROSS JOIN` to signal intent, and keep comma joins out of your codebase so a forgotten predicate can never masquerade as a grid. Now when you write a pairwise self-join, you'll reach for `WHERE a.id < b.id` before running it — because without it, every real pair silently appears twice and every row matches itself.
</Recap>
