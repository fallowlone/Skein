---
slug: "03-null-and-three-valued-logic"
lang: en
track: "sql-postgres"
unit: "01-sql-foundations"
order: 3
title: "NULL and three-valued logic"
summary: "NULL means 'unknown', not 'empty'. That turns SQL's logic three-valued (TRUE/FALSE/UNKNOWN) and produces the silent NOT IN bug that returns zero rows the day one NULL appears."
estMin: 15
status: ready
lessonType: topic
level: middle
concepts:
  - null-semantics
  - three-valued-logic
  - is-null-vs-equals
  - not-in-null-trap
  - coalesce-nullif
prereqs:
  - 02-filtering-rows
sources:
  - https://www.postgresql.org/docs/current/functions-comparison.html
  - https://www.postgresql.org/docs/current/functions-conditional.html
  - https://www.postgresql.org/docs/current/sql-select.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
A fraud filter ran clean for a year: `WHERE user_id NOT IN (SELECT user_id FROM blocked_users)`. One night it returned **zero** flagged accounts — not a few, zero — and the on-call assumed the fraud ring had gone quiet. It hadn't. Someone had inserted one `blocked_users` row with a `NULL` user_id, and that single `NULL` silently turned the entire `NOT IN` into "match nothing". No error, no warning. This is the most expensive NULL bug in SQL, and it follows directly from one fact: in SQL, `NULL` means *unknown*.
</Hook>

<Crux>NULL means "unknown", so x = NULL is never TRUE — it's UNKNOWN. That third truth value is what breaks NOT IN and quietly drops rows.</Crux>

<Explanation>

## NULL is "unknown", not "empty" or "zero"

The single idea everything else hangs on: `NULL` is not a value, it's the *absence* of a known value — "we don't know". It is not zero, not an empty string, not false. Two `NULL`s are not equal to each other, because "unknown" can't be confirmed equal to "unknown".

That means comparisons against `NULL` don't return `TRUE` or `FALSE` — they return a third truth value, **UNKNOWN**. `5 = NULL` is UNKNOWN. `NULL = NULL` is UNKNOWN. `NULL <> 3` is UNKNOWN. SQL is therefore a **three-valued logic**: TRUE, FALSE, and UNKNOWN.

The hard rule that follows: `WHERE` keeps a row only when its predicate evaluates to **TRUE**. UNKNOWN is *not* TRUE, so a row whose predicate is UNKNOWN is dropped — just like FALSE. That symmetry is the source of nearly every NULL surprise.

```sql
-- users(id, email, deleted_at)  -- deleted_at is NULL for active users
SELECT count(*) FROM users WHERE deleted_at = NULL;   -- always 0 rows!
SELECT count(*) FROM users WHERE deleted_at IS NULL;  -- the correct test
```

`= NULL` is never the test you want. To ask "is this unknown?" you must use the dedicated `IS NULL` / `IS NOT NULL` operators, which return real booleans.

## The three-valued truth tables

Each connective has to define what it does when an operand is UNKNOWN. The key entries:

<StackDiagram
  label="Three-valued logic results. TRUE OR UNKNOWN is TRUE because the OR is already satisfied. FALSE AND UNKNOWN is FALSE because the AND is already doomed. But TRUE AND UNKNOWN is UNKNOWN, FALSE OR UNKNOWN is UNKNOWN, and NOT UNKNOWN stays UNKNOWN."
  hue="var(--d-data)"
  layers={[
    { label: "TRUE  OR  UNKNOWN", note: "= TRUE" },
    { label: "FALSE AND UNKNOWN", note: "= FALSE" },
    { label: "TRUE  AND UNKNOWN", note: "= UNKNOWN" },
    { label: "FALSE OR  UNKNOWN", note: "= UNKNOWN" },
    { label: "NOT UNKNOWN", note: "= UNKNOWN" },
    { label: "WHERE keeps a row only if", note: "predicate is TRUE" },
  ]}
  caption="UNKNOWN behaves like 'could be either'. OR with a TRUE is already TRUE; AND with a FALSE is already FALSE; everything else involving UNKNOWN stays UNKNOWN — and UNKNOWN rows are dropped by WHERE, just like FALSE."
/>

The two short-circuit rows matter in practice: `TRUE OR <anything>` is TRUE and `FALSE AND <anything>` is FALSE, even when "anything" is UNKNOWN — because the result is already determined. Every other combination touching UNKNOWN yields UNKNOWN.

## The NOT IN trap, decoded

Now the opening incident. `x NOT IN (a, b, c)` is defined as `x <> a AND x <> b AND x <> c`. Suppose the list is `(1, 2, NULL)` and `x = 5`:

```text
5 <> 1  → TRUE
5 <> 2  → TRUE
5 <> NULL → UNKNOWN      -- because comparing to NULL is always UNKNOWN
TRUE AND TRUE AND UNKNOWN → UNKNOWN
```

The whole expression collapses to UNKNOWN, so the row is dropped. With even one `NULL` in the `NOT IN` list, **no row can ever be TRUE** — the query returns zero rows, silently, for every value. That's exactly how a single `NULL` in `blocked_users` zeroed out the fraud filter.

The fixes, in order of preference:

- **Use `NOT EXISTS` instead of `NOT IN`** for subqueries. `NOT EXISTS` is NULL-safe (it checks row existence, not value equality) and usually plans better as an anti-join:

```sql
SELECT * FROM users u
WHERE NOT EXISTS (
  SELECT 1 FROM blocked_users b WHERE b.user_id = u.id
);
```

- Or exclude NULLs from the subquery: `... NOT IN (SELECT user_id FROM blocked_users WHERE user_id IS NOT NULL)`.
- Better still, **make the column `NOT NULL`** so the bug is impossible by construction.

All three fixes address the same root: once a NULL can reach the comparison, the logic collapses. The strongest fix (`NOT NULL` constraint) eliminates the possibility at the schema level; `NOT EXISTS` is the safest drop-in replacement for existing queries without a schema change.

## IS DISTINCT FROM, and the NULL-aware toolkit

Sometimes you genuinely want "are these two values different, treating NULL as a normal comparable value?" Plain `<>` can't, because it goes UNKNOWN on NULL. Postgres gives you `IS DISTINCT FROM`: it returns a real boolean, treating two NULLs as *not* distinct (equal) and a NULL-vs-value as distinct.

```sql
-- detect a changed column even when either side may be NULL:
SELECT * FROM staging s JOIN target t USING (id)
WHERE s.email IS DISTINCT FROM t.email;   -- TRUE/FALSE, never UNKNOWN
```

The everyday helpers:

- **COALESCE(a, b, …)** returns the first non-NULL argument — your default-value tool: `COALESCE(nickname, email, 'anon')`.
- **NULLIF(a, b)** returns NULL when `a = b`, else `a` — handy to dodge divide-by-zero: `total / NULLIF(qty, 0)`.

And two aggregate facts that trip people: aggregates **skip NULLs**. `COUNT(col)` counts only non-NULL values of `col`, while `COUNT(*)` counts rows regardless. `AVG(col)` averages over non-NULL values only — so an `AVG` can differ from `SUM/COUNT(*)` when NULLs are present.

<Inset kind="edgecase" lang="en">
`NULL` and `UNIQUE` disagree with intuition too. A `UNIQUE` constraint treats `NULL`s as distinct, so by default a unique column allows *many* NULL rows — they don't violate uniqueness because no two NULLs are "equal". If you need at most one NULL, Postgres 15+ offers `UNIQUE NULLS NOT DISTINCT`, which treats NULLs as equal for the constraint. Same root cause: `NULL = NULL` is UNKNOWN, so the default uniqueness check never fires on NULLs.
</Inset>

<Quiz
  id="03-null-quiz-1"
  lessonSlug="03-null-and-three-valued-logic"
  lang="en"
  question="Why does WHERE deleted_at = NULL return zero rows even when many rows have a NULL deleted_at?"
  choices={[
    { label: "Comparing anything to NULL yields UNKNOWN, and WHERE keeps a row only when the predicate is TRUE — so every row is dropped; use IS NULL", correct: true },
    { label: "NULL equals NULL, so it matches every row and Postgres rejects the ambiguous result", misconception: "NULL does NOT equal NULL — the comparison is UNKNOWN, not TRUE, so no row qualifies." },
    { label: "= NULL is a syntax error that returns an empty set", misconception: "It's syntactically valid; it just always evaluates to UNKNOWN, which WHERE treats as not-kept." },
    { label: "deleted_at must be indexed before = NULL works", misconception: "Indexing is irrelevant; the issue is three-valued logic — = NULL is never TRUE regardless of indexes." },
  ]}
/>

<Quiz
  id="03-null-quiz-2"
  lessonSlug="03-null-and-three-valued-logic"
  lang="en"
  question="A NOT IN (subquery) suddenly returns zero rows after a deploy. Most likely cause and best fix?"
  choices={[
    { label: "The subquery now yields a NULL; NOT IN with any NULL collapses to UNKNOWN for every row. Switch to NOT EXISTS (NULL-safe)", correct: true },
    { label: "The subquery returns too many rows for NOT IN to handle", misconception: "Row count isn't the issue; a single NULL in the list makes the whole NOT IN UNKNOWN. NOT EXISTS or filtering NULLs fixes it." },
    { label: "NOT IN requires an ORDER BY in the subquery", misconception: "NOT IN has no ordering requirement; the regression is the NULL-in-list semantics, not ordering." },
    { label: "The outer table lost its primary key", misconception: "The PK is unrelated; the cause is NULL three-valued logic inside NOT IN." },
  ]}
/>

<MetaphorComplete
  id="03-null-metaphor-1"
  lessonSlug="03-null-and-three-valued-logic"
  lang="en"
  setup="Fill in the blank: in SQL a NULL means the value is _______ — not zero and not empty — which is why x = NULL evaluates to UNKNOWN rather than TRUE or FALSE."
  accepted={["unknown", "not known", "missing", "absent"]}
  canonical="unknown"
  explanation="NULL models 'we don't know this value', so it can't be confirmed equal to anything, even another NULL. That 'unknown' is the third truth value (UNKNOWN), and because WHERE keeps only TRUE rows, predicates over NULL silently drop rows. Test for it with IS NULL, and use NOT EXISTS / COALESCE / IS DISTINCT FROM to stay NULL-safe."
/>

</Explanation>

<KeyTakeaway>`NULL` means *unknown*, so any comparison to it is `UNKNOWN`, the third truth value — and `WHERE` keeps a row only when the predicate is `TRUE`, so UNKNOWN rows vanish silently. Test nullness with `IS NULL`, never `= NULL`. The headline bug: `NOT IN` with a single `NULL` in the list returns zero rows for everything — prefer `NOT EXISTS`. Reach for `COALESCE`, `NULLIF`, and `IS DISTINCT FROM`, and remember aggregates skip NULLs (`COUNT(col)` vs `COUNT(*)`).</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-null-retrieval"
  lang="en"
  questions={[
    { q: "Why is = NULL never the right test, and what is the third truth value involved?", a: "Because NULL means 'unknown', any comparison to it — including = NULL — evaluates to UNKNOWN, the third truth value alongside TRUE and FALSE. WHERE keeps a row only when its predicate is TRUE, so a predicate that is UNKNOWN drops the row, the same as FALSE. The correct nullness test is IS NULL / IS NOT NULL, which return real booleans." },
    { q: "Explain precisely why NOT IN with a NULL in the list returns zero rows.", a: "x NOT IN (a, b, NULL) expands to x <> a AND x <> b AND x <> NULL. The last term x <> NULL is always UNKNOWN, and TRUE AND TRUE AND UNKNOWN is UNKNOWN, so the whole predicate can never be TRUE for any x — every row is dropped. Fix with NOT EXISTS (which tests row existence, is NULL-safe, and plans as an anti-join) or by filtering NULLs out of the subquery, or make the column NOT NULL." },
    { q: "Contrast COUNT(col) with COUNT(*), and state what IS DISTINCT FROM is for.", a: "COUNT(*) counts all rows; COUNT(col) counts only rows where col IS NOT NULL, because aggregates skip NULLs (AVG, SUM, etc. ignore NULLs too). IS DISTINCT FROM is a NULL-safe inequality: it returns a real TRUE/FALSE, treating two NULLs as equal (not distinct) and NULL-vs-value as distinct — useful for change detection where plain <> would go UNKNOWN." },
  ]}
/>

<Recap lang="en">
`NULL` is the absence of a known value — "unknown" — not zero and not empty. Because you can't confirm "unknown = anything", comparisons against `NULL` produce a third truth value, **UNKNOWN**, making SQL a three-valued logic. `WHERE` (and `HAVING`, and `JOIN` conditions) keep a row only when the predicate is `TRUE`, so UNKNOWN rows are dropped just like FALSE — which is why `= NULL` always yields nothing and you must use `IS NULL`. The marquee failure mode is `NOT IN` with a `NULL` in its list: it collapses to UNKNOWN for every row and returns zero results silently, so prefer the NULL-safe `NOT EXISTS` (or filter NULLs, or make the column `NOT NULL`). Round out the toolkit with `COALESCE` for defaults, `NULLIF` to dodge divide-by-zero, and `IS DISTINCT FROM` for NULL-aware inequality — and remember aggregates skip NULLs, so `COUNT(col)` and `COUNT(*)` differ. Now when you see a filter that should match rows but returns nothing, ask: is there a `NULL` somewhere in the comparison path? That single question catches `= NULL`, `NOT IN (subquery with NULLs)`, and `<>` on a nullable column — all the same root cause.
</Recap>
