infographic chart-column-simple
data
  title The dedup tax: UNION vs UNION ALL
  desc Same 9M-row report, one keyword changed — wall-clock milliseconds
  values
    - label UNION (sort/hash 9M rows)
      value 11000
    - label UNION ALL (concat)
      value 600
theme
  palette #f43f5e #14b8a6
