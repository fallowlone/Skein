infographic chart-column-simple
data
  title Sargable vs not: 2 ms vs 1.9 s on 20M rows
  desc Wrapping the indexed column in lower() turns a 2 ms index scan into a 1.9 s full scan — the lesson's ~900x regression
  values
    - label email = $1 · index scan
      value 2
    - label lower(email) = $1 · seq scan
      value 1900
theme
  palette #14b8a6 #f43f5e
