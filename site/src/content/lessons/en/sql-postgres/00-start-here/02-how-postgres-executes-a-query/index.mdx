---
slug: "02-how-postgres-executes-a-query"
lang: en
track: "sql-postgres"
unit: "00-start-here"
order: 2
title: "How Postgres executes a query"
summary: "A query passes through five stages — parse, analyze, rewrite, plan, execute — before a single row comes back. Knowing the pipeline tells you where time goes and which problems live at which stage."
estMin: 14
status: ready
lessonType: topic
level: junior
concepts:
  - query-pipeline
  - parser
  - planner
  - executor
  - logical-vs-physical-plan
prereqs:
  - 01-why-sql-runs-everything
sources:
  - https://www.postgresql.org/docs/current/query-path.html
  - https://www.postgresql.org/docs/current/overview.html
  - https://www.interdb.jp/pg/pgsql03.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import SpiralCue from "~/components/prose/SpiralCue.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
You type `SELECT * FROM orders WHERE id = 42;` and a row comes back in a millisecond. It feels atomic — like the database just *looked it up*. It isn't atomic at all: your text went through five distinct stages, and each one is where a different class of bug or slowness lives. When a query is slow, the first senior question is always "slow at which stage?"
</Hook>

<Crux>One query crosses five stages: parse, analyze, rewrite, plan, execute. Each stage owns a different failure mode — and a different fix.</Crux>

<Explanation>

## The five stages

Your SQL string is not run directly. The Postgres backend that owns your connection pushes it through a pipeline:

1. **Parse** — the raw text is checked for *syntax* and turned into a parse tree. A missing comma or `SELCT` dies here. No tables are touched yet; the parser doesn't even know if `orders` exists.
2. **Analyze (semantic)** — names are resolved against the catalog. Does `orders` exist? Does it have a column `id`? What types are involved? A "column does not exist" error comes from here, not the parser.
3. **Rewrite** — rule-based transformations apply. The big one: a query against a **view** is rewritten to hit the underlying tables; row-level security policies and rules are injected here too.
4. **Plan / optimize** — the **planner** takes the analyzed query tree and produces a **physical plan**: which scan (sequential? index?), which join algorithm (nested loop? hash? merge?), in which order. It uses table **statistics** to estimate row counts and picks the cheapest plan by an internal cost model.
5. **Execute** — the executor walks the chosen plan tree, pulling rows through it node by node, and streams the result back to you.

<FlowDiagram
  label="The query pipeline: raw SQL text becomes a parse tree, then a resolved query tree, then a plan tree, then rows. Syntax errors die at parse; bad row estimates hurt at plan; the work happens at execute."
  perRow={5}
  hue="var(--d-data)"
  nodes={[
    { id: "sql", label: "SQL text", sub: "your string" },
    { id: "parse", label: "Parse", sub: "syntax → tree" },
    { id: "analyze", label: "Analyze", sub: "resolve names" },
    { id: "rewrite", label: "Rewrite", sub: "views, RLS" },
    { id: "plan", label: "Plan", sub: "cost-based" },
    { id: "exec", label: "Execute", sub: "→ rows" },
  ]}
  edges={[
    { from: "sql", to: "parse" },
    { from: "parse", to: "analyze" },
    { from: "analyze", to: "rewrite" },
    { from: "rewrite", to: "plan" },
    { from: "plan", to: "exec" },
  ]}
  caption="Five stages, each with a distinct job. 'Why is it slow?' almost always means 'the plan stage chose badly' or 'the execute stage is doing too much I/O' — rarely parse or analyze."
/>

## Why the stages matter for debugging

The pipeline is a diagnostic map. Match the symptom to the stage:

- **Syntax error** → parse stage. Pure text problem; the schema is irrelevant.
- **"relation/column does not exist"** → analyze stage. The text is valid SQL but doesn't match the catalog.
- **Query suddenly slow after a data load** → plan stage. The planner's row estimates went stale, so it picked a bad plan. The fix is usually `ANALYZE` to refresh statistics — not rewriting the query.
- **Query always slow, plan looks right** → execute stage. The plan is fine but it's genuinely reading too many pages; the fix is an index or less data, which changes what plans are *available*.

This is why the senior reflex is `EXPLAIN`. `EXPLAIN <query>` shows you the **plan** the planner chose without running it; `EXPLAIN ANALYZE <query>` runs it and shows estimated *vs* actual rows at each node. The gap between estimate and actual is the single most useful number in Postgres performance work — it tells you whether the planner was lied to by stale statistics.

```sql
-- See the plan only (fast, no execution):
EXPLAIN SELECT * FROM orders WHERE id = 42;

-- Run it and compare estimate vs reality:
EXPLAIN ANALYZE SELECT * FROM orders WHERE id = 42;
```

<SpiralCue thread="latency" lang="en" />

You won't fully read a plan yet — that's an entire unit later (08-internals-and-tuning) and the `databases` track covers the planner and execution-plan internals in depth. For now, internalize only the shape: text in, five stages, rows out, and `EXPLAIN` is your window into stage four.

<Inset kind="why" lang="en">
Why separate planning from execution at all? Because planning is *expensive* — for a query joining six tables, the number of possible join orders explodes, and the planner does real combinatorial work to find a cheap one. Postgres pays that cost once and (with prepared statements) can reuse the plan across many executions. It also means the same parsed query can be re-planned later under better statistics. Bundling plan and execute together would forfeit both wins. The `databases` track's execution-plans chapter goes deep on the cost model that drives stage four.
</Inset>

<Quiz
  id="00-pipeline-quiz-1"
  lessonSlug="02-how-postgres-executes-a-query"
  lang="en"
  question={"You run a query and get 'column \"emial\" does not exist'. Which stage produced this error?"}
  choices={[
    { label: "Analyze (semantic) — the text parsed fine but the name doesn't resolve against the catalog", correct: true },
    { label: "Parse — it's a syntax error", misconception: "The text is syntactically valid SQL; a misspelled column name is caught when names are resolved, in the analyze stage." },
    { label: "Execute — it failed while reading rows", misconception: "Execution never starts; the query is rejected before planning because the column can't be resolved." },
    { label: "Rewrite — a view rule broke", misconception: "Rewrite handles views and RLS, not name resolution of a base-table column." },
  ]}
/>

<Quiz
  id="00-pipeline-quiz-2"
  lessonSlug="02-how-postgres-executes-a-query"
  lang="en"
  question="A query was fast yesterday; today, after a big bulk INSERT, it's 50× slower with the same text. Best first move?"
  choices={[
    { label: "Run ANALYZE to refresh table statistics so the planner estimates rows correctly again", correct: true },
    { label: "Rewrite the SQL to be 'more efficient'", misconception: "The text didn't change and was fine yesterday; the regression is in plan selection driven by stale stats, not the query shape." },
    { label: "Add RAM to the server", misconception: "Hardware rarely explains a sudden 50× regression from a data load; stale statistics misleading the planner do." },
    { label: "Drop and recreate the table", misconception: "Destructive and unnecessary; the data is fine — only the planner's statistics are stale." },
  ]}
/>

<MetaphorComplete
  id="00-pipeline-metaphor-1"
  lessonSlug="02-how-postgres-executes-a-query"
  lang="en"
  setup="Fill in the blank: EXPLAIN shows you the planner's chosen plan _______ running the query, while EXPLAIN ANALYZE runs it and reports actual timings and row counts."
  accepted={["without", "before", "instead of"]}
  canonical="without"
  explanation="Plain EXPLAIN is a dry run: it asks the planner 'what plan would you use?' and prints it without touching the heap. EXPLAIN ANALYZE actually executes, so you can compare the planner's estimate to reality — the core skill of unit 08."
/>

</Explanation>

<KeyTakeaway>Every query crosses five stages: parse (syntax), analyze (resolve names), rewrite (views/RLS), plan (cost-based physical plan), execute (produce rows). Each stage owns a distinct failure mode, so "slow at which stage?" is the first diagnostic question. `EXPLAIN` reveals the plan; `EXPLAIN ANALYZE` compares estimate to actual — the gap is gold.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="00-pipeline-retrieval"
  lang="en"
  questions={[
    { q: "List the five stages a query passes through, in order, with one word on what each does.", a: "Parse (syntax → tree), Analyze (resolve names against the catalog), Rewrite (apply view/RLS rules), Plan (cost-based choice of scans/joins/order → physical plan), Execute (walk the plan, produce rows)." },
    { q: "A query is suddenly slow after a bulk load even though its text is unchanged. Which stage is the culprit and what's the fix?", a: "The plan stage. The bulk load made the planner's row-count statistics stale, so it now estimates badly and picks a worse plan. The fix is to refresh statistics with ANALYZE (or autovacuum's analyze), not to rewrite the query — the SQL was never the problem." },
    { q: "What is the difference between EXPLAIN and EXPLAIN ANALYZE, and why does the estimate-vs-actual gap matter?", a: "EXPLAIN prints the plan the planner would choose without executing it. EXPLAIN ANALYZE actually runs the query and annotates each plan node with estimated rows vs actual rows and timing. A large gap between estimated and actual rows means the planner was working from wrong statistics — that mismatch is the root cause of most bad plans." },
  ]}
/>

<Recap lang="en">
A query is not looked up atomically — it crosses a five-stage pipeline: **parse** (syntax to tree), **analyze** (resolve names against the catalog), **rewrite** (expand views, inject row-level security), **plan** (the cost-based optimizer turns the query tree into a physical plan of scans and joins), and **execute** (the executor walks that plan and streams rows). The value of knowing the pipeline is diagnostic: a syntax error is parse, a missing-column error is analyze, a sudden post-load slowdown is the plan stage starved of fresh statistics (fix with `ANALYZE`), and a chronically slow-but-correct plan is the execute stage doing too much I/O (fix with an index). `EXPLAIN` shows the chosen plan; `EXPLAIN ANALYZE` runs it and exposes the estimate-vs-actual gap. You'll learn to read those plans deeply in unit 08 and in the `databases` execution-plans chapter — here you only need the map.
</Recap>
