---
concepts:
- dependency-confusion
- typosquatting
- scope-registry-pinning
- lockfile-integrity
- install-scripts
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: senior
order: 3
prereqs: []
slug: "03-dependency-confusion-and-typosquatting"
sources:
- https://owasp.org/www-community/attacks/Dependency_Confusion
- https://docs.npmjs.com/cli/v10/configuring-npm/npmrc
spiral: []
status: ready
summary: 'Two name-resolution attacks that turn your install step into RCE: dependency confusion (a public package shares your internal name at a higher version) and typosquatting (a near-miss of a popular name). Pin the scope to your registry, install with npm ci, block scripts.'
title: 'Dependency confusion and typosquatting'
track: ci-cd
unit: 05-supply-chain
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The build was green for years. Then one morning CI started failing a fraud check downstream, and the on-call engineer pulled the runner logs to find an `install` step phoning a host nobody recognized. The package was named exactly like one of the company's internal libraries — the kind of name that only appears inside the monorepo and in stack traces. Except this copy came from the public registry, carried a version number an order of magnitude higher than anything the team had ever shipped, and ran a postinstall script the moment `npm install` resolved it. Nobody added a dependency. Nobody changed a version. An attacker had simply published a package with the company's own internal name, set the version to 99.9.9, and waited for a build machine to prefer it. This lesson is about the two attacks that weaponize how a package manager turns a name into an artifact: dependency confusion and typosquatting.
</Hook>

<Crux>Resolution attacks abuse "highest version wins" in a shared namespace: pin the scope to your registry, install with npm ci, block scripts.</Crux>

<Explanation>

## Dependency confusion: highest version wins, across a shared namespace

When a package manager resolves a name, it asks a registry for the versions available and, by default, prefers the highest one that satisfies the range. The trap is that "the registry" is often *plural*: many setups treat the public registry (npm, PyPI) as a fallback in the **same namespace** as your private one. So if your internal package `acme-internal-utils` exists privately at `1.4.0`, and an attacker publishes a **public** package with the identical name at `99.0.0`, the resolver compares them in one namespace and picks the higher version — the attacker's.

The attacker's only prerequisite is your internal *name*. Those names leak constantly: a `package.json` committed to a public repo, a dependency tree printed in a CI log, a path in a stack trace pasted into an issue. None of that is secret, and none of it feels like a vulnerability — until it becomes the seed of an attack.

```bash
# What the resolver effectively does for an UNSCOPED internal name:
#   query private registry  -> acme-internal-utils@1.4.0   (yours)
#   query public  registry  -> acme-internal-utils@99.0.0  (attacker's, higher)
#   "highest version wins"  -> installs 99.0.0 from public
# The install/postinstall script then runs with the CI job's secrets and network.
```

The payload runs because package managers execute lifecycle scripts (`postinstall`, `preinstall`) during installation by design — that is how native modules build themselves. On a CI runner that means the attacker's code executes with whatever the job holds: registry tokens, cloud credentials, the ability to reach internal hosts. The root cause is structural, not a bug: a **single resolution namespace** where the public registry is a silent fallback and **highest version wins**, while your internal names are not reserved publicly.

## The fix that actually closes it: scope + registry pinning

The decisive defense is to make your internal names **unresolvable** from the public registry, so the comparison above never happens. You do that by putting internal packages under a **scope** (`@acme/...`) and pinning that scope to your private registry in `.npmrc`:

```ini
# .npmrc — the scope @acme NEVER falls through to the public registry
@acme:registry=https://registry.internal.acme.com
//registry.internal.acme.com/:_authToken=${INTERNAL_NPM_TOKEN}
```

Now a request for `@acme/utils` is routed *only* to the internal registry. The resolver does not even query the public registry for that scope, so there is no public `@acme/utils@99.0.0` to win — the version-comparison trap is structurally removed. For legacy **unscoped** internal names, the equivalent is to ensure the resolver never falls through to public for them (a proxy that fails closed, or migrating them under a scope). Privacy of the registry is not the same as **isolation of the namespace**; only the pin gives you isolation.

<Inset kind="why" lang="en">
Why are you still vulnerable just by *having* an internal package, if you don't pin the scope to your registry? Because most resolvers treat the public registry as a fallback in the SAME namespace, and "highest version wins" — so a public package with your internal name and a higher version is preferred over your private one. Making the registry private protects its *contents*; it does nothing about the *name*. The resolver still happily queries public for a name it can't satisfy privately, and an attacker who knows the name publishes a higher version there. Only explicitly pinning the scope to your registry (so the resolver never queries public for that scope) makes the name unforgeable. Reserving the public name or scope — publishing a harmless placeholder so no one else can claim it — is the belt-and-suspenders second layer.
</Inset>

## Lockfiles, integrity, and install scripts: the layers under the pin

The pin closes the namespace; three more layers limit blast radius if anything slips through.

**Commit the lockfile and install with `npm ci`, not `npm install`.** `npm install` is allowed to *resolve* — it can pull a new higher version and rewrite the lockfile, which is exactly the behavior dependency confusion exploits. `npm ci` installs **only** what the lockfile pins, and verifies each artifact against the `integrity` hash (a subresource-integrity SHA recorded at lock time). If a registry serves a swapped artifact for the same version, the hash check fails and the install aborts.

```bash
# npm install: may resolve a higher version and REWRITE the lockfile (the attack window)
npm install

# npm ci: installs EXACTLY the locked versions, verifies each integrity hash, fails on mismatch
npm ci --ignore-scripts   # also refuse lifecycle scripts where the build allows it
```

**Disable install scripts where you can** (`--ignore-scripts`). A malicious package's leverage is almost always its `postinstall`; if scripts don't run, a swapped or confused package is inert code on disk rather than executing code. You re-enable scripts deliberately for the few dependencies that genuinely need to compile.

## Typosquatting: the sibling attack on the same install step

Dependency confusion targets *your* names; typosquatting targets *popular* names. The attacker publishes packages a fat finger away from something well-known — `expresss`, `lodahs`, `crossenv` — and waits for a developer to mistype an install command or copy a typo from a blog. The squatted package then runs the same kind of malicious install script. The defense layers differently because there is no scope to pin: verify the exact name before installing, run installs through a **private proxy registry** (Artifactory, Verdaccio) that serves only vetted, allow-listed packages, scan dependencies with tooling (Socket, `npm audit`), and pin everything via the lockfile so a typo can't silently enter a reproducible build.

<TradeoffMatrix
  id="ci-cd-05-supply-chain-03-dependency-confusion-and-typosquatting-tradeoff-1"
  lessonSlug="03-dependency-confusion-and-typosquatting"
  lang="en"
  prompt="You must stop dependency confusion for your internal @acme packages — a public package with the same name and a higher version must never reach your build. Which approach actually closes the hole?"
  options={[
    { name: "Scope internal packages under @acme, pin @acme to your private registry in .npmrc, reserve the public @acme scope, and install with npm ci against a committed lockfile", summary: "Pin the namespace.", correct: true, justification: "This removes the version-comparison trap structurally: with @acme:registry pinned in .npmrc, the resolver never queries the public registry for that scope, so there is no public @acme/utils@99.0.0 to win. Reserving the public scope means an attacker can't even claim the name. npm ci against a committed lockfile then installs exactly the locked versions with their integrity hashes, so even a swapped artifact for a pinned version fails the hash check. Pin + reserve + lockfile is the layered, senior answer." },
    { name: "Rely on the registry being private — internal packages aren't published publicly, so they're safe", summary: "Privacy isn't isolation.", justification: "A private registry protects its contents, not the namespace. The resolver still treats the public registry as a fallback for any name it can't satisfy privately, and an attacker who learns the internal name (from a public package.json, a CI log, a stack trace) publishes a higher version there. 'Highest version wins' then prefers the public package. Privacy of the registry is not isolation of the name — only an explicit scope-to-registry pin gives you that." },
    { name: "Use npm install and run npm audit afterwards to catch anything bad", summary: "Audits the wrong window.", justification: "npm install is allowed to resolve a new higher version and rewrite the lockfile — that IS the dependency-confusion mechanism, so this leaves the door open. And npm audit checks for KNOWN-vulnerable versions in advisories; a freshly published confusion/typosquat package isn't in any advisory database yet, so audit reports nothing while the malicious postinstall has already run during install. After-the-fact scanning can't undo code that executed at install time." },
    { name: "Rename internal packages to long unguessable strings so an attacker can't guess the name", summary: "Security by obscurity.", justification: "Obscurity is not isolation. Internal names leak constantly — committed lockfiles, CI logs, error traces, SBOMs — so 'unguessable' becomes 'published in a log' fast, and the moment the name is known the higher-version public package wins exactly as before. It also makes every internal package harder to use while providing no structural guarantee. The pin removes the attack regardless of whether the name is known; renaming just delays it." },
  ]}
/>

<Quiz
  id="ci-cd-05-supply-chain-03-dependency-confusion-and-typosquatting-quiz-1"
  lessonSlug="03-dependency-confusion-and-typosquatting"
  lang="en"
  question="How does a dependency-confusion attacker get their code running inside your CI build?"
  choices={[
    { label: "They publish a PUBLIC package with the same name as your internal one and a higher version; the resolver prefers it in the shared namespace and its install script runs in CI", correct: true },
    { label: "They guess your registry token and push a malicious version into your private registry directly", misconception: "That would be a credential compromise, a different attack. Dependency confusion needs no access to your registry at all — the attacker only needs to know the internal NAME, then publishes a same-named package to the PUBLIC registry with a higher version. The resolver's 'highest version wins' across a shared namespace does the rest, with no token theft involved." },
    { label: "They exploit a buffer overflow in npm's resolver to redirect the download", misconception: "No memory-safety bug is involved. The mechanism is entirely by-design behavior: the public registry is a fallback in the same namespace and the resolver prefers the highest version. The attacker just publishes a higher-versioned public package with your internal name — the 'exploit' is the resolution policy itself, not a flaw in the resolver's code." },
    { label: "They submit a pull request that adds the malicious dependency to your package.json", misconception: "That would require getting a PR merged and would show up in review. Dependency confusion changes nothing in your repo — your package.json still names the internal package you always used. The substitution happens at RESOLUTION time, when the public higher version is silently preferred, which is precisely why it slips past code review." },
  ]}
/>

<Quiz
  id="ci-cd-05-supply-chain-03-dependency-confusion-and-typosquatting-quiz-2"
  lessonSlug="03-dependency-confusion-and-typosquatting"
  lang="en"
  question="Why is `npm ci` against a committed lockfile safer than `npm install` for resisting this attack?"
  choices={[
    { label: "npm ci installs exactly the locked versions and verifies each integrity hash; it won't silently resolve to a malicious higher version, whereas npm install can re-resolve and rewrite the lockfile", correct: true },
    { label: "npm ci runs the install offline, so it never contacts any registry", misconception: "npm ci does contact the registry to download the locked artifacts; it isn't an offline mode. Its safety comes from installing ONLY the versions the lockfile pins and checking each one against its recorded integrity hash — not from skipping the network. npm install, by contrast, is free to resolve a new higher version and rewrite the lockfile." },
    { label: "npm ci automatically blocks all postinstall scripts by default", misconception: "npm ci runs lifecycle scripts just like npm install unless you add --ignore-scripts. Blocking scripts is a separate, complementary layer. What npm ci gives you over npm install is exact, lockfile-pinned, integrity-verified installs with no silent re-resolution — that's the dependency-confusion defense here." },
    { label: "npm ci only installs packages from scoped names, ignoring unscoped ones", misconception: "npm ci installs everything in the lockfile, scoped or not — it doesn't filter by scope. Scoping plus a registry pin is the separate defense that isolates the namespace. npm ci's contribution is reproducibility: exact locked versions with verified integrity hashes, no upgrade to an attacker's higher version." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="How an install request for an internal package resolves, with and without a scope-to-registry pin. The request enters the resolver. On the pinned path, the .npmrc maps @acme to the private registry only, so the resolver queries just that registry, gets your version, and installs your code — the public registry is never consulted for the scope. On the unpinned path, the resolver treats the public registry as a fallback in the same namespace, finds the attacker's higher version there, picks it under highest-version-wins, and runs its install script inside CI with the job's secrets. The pin is the gate that decides which path runs."
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "req", label: "install @acme/utils", sub: "resolver entry" },
    { id: "pin", label: "pinned scope", sub: ".npmrc -> private only" },
    { id: "safe", label: "your version", sub: "public never queried" },
    { id: "unpin", label: "unpinned", sub: "public is fallback" },
    { id: "high", label: "attacker 99.0.0", sub: "highest wins" },
    { id: "rce", label: "install script runs", sub: "CI secrets exposed" },
  ]}
  edges={[
    { from: "req", to: "pin", label: "scope pinned" },
    { from: "pin", to: "safe", label: "private only" },
    { from: "req", to: "unpin", label: "no pin" },
    { from: "unpin", to: "high", label: "public fallback" },
    { from: "high", to: "rce", label: "postinstall" },
  ]}
  caption="The pin is the gate: pinned, @acme resolves only against your private registry and public is never consulted; unpinned, the public higher version wins the shared namespace and its install script runs in CI."
/>

<KeyTakeaway>Internal names leak; secrecy isn't a defense. Pin the scope to your private registry in .npmrc so public is never queried; install with npm ci (integrity-checked); block scripts. Typosquats: proxy through an allow-list.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="ci-cd-05-supply-chain-03-dependency-confusion-and-typosquatting-retrieval"
  lang="en"
  questions={[
    { q: "Explain the dependency-confusion mechanism end to end: what the attacker needs, why the resolver picks their package, and where their code runs.", a: "The attacker needs only your internal package NAME — not your registry, not a token. Those names leak routinely: a package.json in a public repo, a dependency tree in a CI log, a path in a pasted stack trace. The attacker publishes a package with that exact name to the PUBLIC registry (npm/PyPI) and sets a very high version, e.g. 99.0.0. The vulnerability is structural: many setups treat the public registry as a fallback in the SAME namespace as the private one, and the resolver prefers the highest version that satisfies the range. So it compares your private acme-internal-utils@1.4.0 against the public acme-internal-utils@99.0.0 in one namespace and picks the attacker's higher version. The code runs because package managers execute lifecycle scripts (preinstall/postinstall) during installation by design — on a CI runner that means the attacker's code executes with whatever the job holds: registry tokens, cloud credentials, reach into internal hosts. Root cause: a single resolution namespace with a silent public fallback, highest-version-wins, and internal names not reserved publicly." },
    { q: "Lay out the layered defenses and why each is necessary: scope+registry pin, reserving the name, lockfile + npm ci + integrity, ignore-scripts, and the typosquatting variant.", a: "The decisive layer is scope + registry PINNING: put internal packages under a scope (@acme/...) and pin @acme:registry=https://registry.internal in .npmrc. Now the resolver routes @acme/* ONLY to the private registry and never queries public for that scope, so there is no public @acme/utils@99.0.0 to win — the version-comparison trap is removed structurally. Privacy of the registry isn't isolation of the namespace; only the pin gives isolation. RESERVING the public scope/name (publishing a harmless placeholder) is belt-and-suspenders so no attacker can claim it. LOCKFILE + npm ci + INTEGRITY: commit the lockfile and install with npm ci, which installs exactly the locked versions and verifies each against its integrity (subresource-integrity) hash — npm install, by contrast, may resolve a higher version and rewrite the lockfile, which is the very behavior the attack exploits; a swapped artifact for a locked version fails the hash check. IGNORE-SCRIPTS (npm ci --ignore-scripts) blocks the postinstall that carries the payload, turning a confused/swapped package into inert code rather than executing code; re-enable scripts deliberately for deps that must compile. TYPOSQUATTING is the sibling attack on popular names (expresss, lodahs, crossenv) hoping for a fat-finger; since there is no scope to pin, defend by verifying exact names, routing installs through a private proxy registry (Artifactory/Verdaccio) that serves only vetted allow-listed packages, scanning with tooling (Socket, npm audit), and pinning via the lockfile." },
  ]}
/>

<Recap lang="en">
Two name-resolution attacks turn a routine install step into remote code execution. Dependency confusion abuses how a resolver turns a name into an artifact: many setups treat the public registry as a fallback in the SAME namespace as your private one, and "highest version wins" — so an attacker who learns your internal name (leaked in a public package.json, a CI log, or a stack trace) publishes a same-named PUBLIC package at version 99.0.0, the resolver prefers it over your private 1.4.0, and its install/postinstall script runs in CI with the job's secrets and network. The decisive fix is scope + registry pinning: put internal packages under a scope (@acme/...) and pin that scope to your private registry in .npmrc, so the resolver never even queries public for @acme/* and there is no higher public version to win — privacy of the registry is not isolation of the namespace, only the pin is. Reserve the public scope as a second layer. Underneath, commit the lockfile and install with npm ci (not npm install): npm ci installs exactly the locked versions and verifies each integrity hash, refusing a swapped artifact, whereas npm install may re-resolve to a malicious higher version and rewrite the lockfile. Block install scripts with --ignore-scripts where the build allows, so a confused or swapped package is inert rather than executing. Typosquatting is the sibling attack on popular names (expresss, lodahs, crossenv) betting on a fat-finger; with no scope to pin, defend by verifying names, routing installs through a private proxy registry that serves only an allow-list, scanning dependencies, and pinning everything via the lockfile. Now when you see a new internal package being added without a scope prefix, that's your cue to ask: is this name reserved on the public registry, and is its scope pinned in .npmrc?
</Recap>
