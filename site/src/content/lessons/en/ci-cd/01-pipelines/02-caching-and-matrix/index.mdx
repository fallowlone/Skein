---
concepts:
- dependency-caching
- cache-key-design
- restore-keys-fallback
- matrix-builds
- artifacts-between-jobs
- artifacts-vs-cache
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-caching-and-matrix
sources:
- https://docs.github.com/en/actions/using-workflows/caching-dependencies-to-speed-up-workflows
- https://docs.github.com/en/actions/using-jobs/using-a-matrix-for-your-jobs
- https://docs.github.com/en/actions/using-workflows/storing-workflow-data-as-artifacts
spiral: []
status: ready
summary: 'Cache keyed on a lockfile hash restores dependencies across runs; a matrix
  fans one job across versions and OSes in parallel; artifacts hand build output
  between jobs. A stale key is worse than no cache.'
title: 'Caching, matrix builds and artifacts'
track: ci-cd
unit: 01-pipelines
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Every CI run on a fresh runner reinstalls 1,200 npm packages from the network — three minutes, every push, before a single test runs. Someone adds `actions/cache` with `key: deps`, a constant. The first run saves `node_modules`; every run after restores it. Builds drop to forty seconds. Two weeks later a dependency bump lands, tests pass green in CI, and break in production — because the constant key kept restoring the *old* `node_modules` and the new lockfile was never installed. The cache was faster and wrong. A cache that never changes its key is a time bomb.
</Hook>

<Crux>Cache on a lockfile hash restores deps across runs; a matrix fans a job over versions in parallel; artifacts pass output between jobs.</Crux>

<Explanation>

## Caching: a lockfile hash is the only safe key

Each CI job runs on a clean machine, so without help it redoes every expensive, deterministic step — chiefly the dependency install — from zero on every run. `actions/cache` fixes this by saving a directory after the first run and restoring it on later runs, keyed on a string you choose. The cardinal rule lives entirely in that key: **it must change exactly when the cached contents should change, and not before.** For dependencies, the input that determines `node_modules` is the lockfile, so you hash it.

```yaml
- name: Cache node_modules
  uses: actions/cache@v4
  with:
    path: node_modules
    key: ${{ runner.os }}-npm-${{ hashFiles('**/package-lock.json') }}
    restore-keys: |
      ${{ runner.os }}-npm-
```

`hashFiles('**/package-lock.json')` produces a checksum that changes the instant any dependency changes. On a hit, the cache restores and the install is near-instant; on a miss — a new lockfile — the key is new, so the old entry is *not* reused, you install fresh, and the run saves a new entry under the new key. That is why a constant `key: deps` is dangerous: it can never miss, so it restores stale dependencies forever and your new lockfile is silently ignored. Always bind the key to the content.

`restore-keys` is the fallback ladder. When the exact key misses, GitHub tries each prefix in turn and restores the most recent partial match — so a lockfile bump still seeds the cache from the previous `runner.os-npm-` entry instead of starting empty, and the package manager only downloads the *delta*. It is a warm start, not a correctness shortcut: the exact `key` still governs whether the entry is treated as current.

<Inset kind="why" lang="en">
`setup-node` has caching built in: `with: { node-version: 22, cache: 'npm' }` hashes your lockfile and manages the key for you — prefer it over a hand-rolled `actions/cache` for the package-manager cache. Two scope facts matter at senior level. Caches are scoped per branch with read-down inheritance: a branch can restore from its base and from `main`, but not sideways from an unrelated branch — which is also why a cache written by an untrusted fork PR cannot poison `main`. And there is a repository eviction budget (10 GB); once exceeded, GitHub evicts least-recently-used entries, so unused caches quietly disappear.
</Inset>

## Matrix: one job definition, many parallel runs

A `strategy.matrix` runs the same job once per combination of the dimensions you list — the standard way to prove your code works across Node versions and operating systems without copy-pasting jobs. GitHub expands the cartesian product and schedules every combination in parallel.

```yaml
test:
  strategy:
    fail-fast: false
    max-parallel: 4
    matrix:
      node: [18, 20, 22]
      os: [ubuntu-latest, macos-latest]
      include:
        - { node: 22, os: ubuntu-latest, coverage: true }
      exclude:
        - { node: 18, os: macos-latest }
  runs-on: ${{ matrix.os }}
  steps:
    - uses: actions/setup-node@v4
      with: { node-version: ${{ matrix.node }} }
```

That `node × os` grid is 3 × 2 = 6 jobs; the `exclude` drops one, the `include` adds an extra field to one existing combination (and, with a new value, could add a whole job). `fail-fast: true` (the default) cancels every sibling the moment one fails — fast feedback, but you lose the full picture; set it `false` when you need to see *which* combinations broke. `max-parallel` caps how many run at once, throttling concurrent runner usage. Mind the explosion: dimensions multiply, so adding a third 3-value axis turns 6 jobs into 18, each consuming runner minutes — a matrix is a cost dial, not a free coverage win.

## Artifacts: hand output from one job to the next

Because every job is a fresh machine, a build produced in one job does not exist in the next — the filesystem is gone when the job ends. The cache is the wrong tool here: it is a keyed, best-effort, possibly-evicted speedup, not a guaranteed handoff. Artifacts are the right tool — explicit, durable-for-the-run outputs you upload and later download, and that you can also publish as the run's deliverables (a built bundle, test reports, coverage).

```yaml
build:
  steps:
    - run: npm run build
    - uses: actions/upload-artifact@v4
      with: { name: dist, path: dist/ }
deploy:
  needs: build
  steps:
    - uses: actions/download-artifact@v4
      with: { name: dist }
    - run: ./deploy.sh dist/
```

Build once, deploy the exact same bytes you tested — no rebuild drift between jobs. Artifacts have a retention period (default 90 days, configurable) and count against storage, so name them deliberately and keep large ones short-lived.

<FlowDiagram
  label="A two-stage CI pipeline: a build job uploads a dist artifact, a matrix test job restores a lockfile-keyed cache, and a deploy job downloads the same artifact"
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "build", label: "build job", sub: "npm run build" },
    { id: "upload", label: "upload-artifact", sub: "name: dist" },
    { id: "test", label: "test (matrix)", sub: "18/20/22 × os" },
    { id: "cache", label: "actions/cache", sub: "key: hash(lock)" },
    { id: "download", label: "download-artifact", sub: "name: dist" },
    { id: "deploy", label: "deploy job", sub: "needs: build" },
  ]}
  edges={[
    { from: "build", to: "upload", label: "writes dist/" },
    { from: "upload", to: "download", label: "same bytes" },
    { from: "test", to: "cache", label: "restore deps" },
    { from: "download", to: "deploy", label: "ship" },
  ]}
  caption="Cache is a keyed speedup that may be evicted; an artifact is a durable handoff of the exact output you tested. Different jobs, different tools, different guarantees."
/>

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Dimension</th>
        <th class="border border-border px-3 py-2 text-left">Cache (<code>actions/cache</code>)</th>
        <th class="border border-border px-3 py-2 text-left">Artifact (<code>upload/download-artifact</code>)</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Purpose</td>
        <td class="border border-border px-3 py-2">Speed up a reproducible step (re-derivable inputs)</td>
        <td class="border border-border px-3 py-2">Pass / keep an output you cannot re-derive cheaply</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Lifetime</td>
        <td class="border border-border px-3 py-2">Best-effort; LRU-evicted under the 10 GB repo budget</td>
        <td class="border border-border px-3 py-2">Retained for a fixed period (default 90 days)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Keyed on</td>
        <td class="border border-border px-3 py-2">A key string you design (e.g. lockfile hash)</td>
        <td class="border border-border px-3 py-2">A plain name you choose (<code>dist</code>, <code>coverage</code>)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Cross-job?</td>
        <td class="border border-border px-3 py-2">Yes, but as a hint — a miss is normal and safe</td>
        <td class="border border-border px-3 py-2">Yes, and guaranteed within the run via <code>needs</code></td>
      </tr>
    </tbody>
  </table>
</div>

<Quiz
  id="02-caching-and-matrix-quiz-1"
  lessonSlug="02-caching-and-matrix"
  lang="en"
  question="You set the cache key to a constant ('deps'). Builds are fast, but a dependency bump that passes CI breaks in prod. Why?"
  choices={[
    { label: "A constant key never misses, so it keeps restoring the old node_modules and the new lockfile is never installed — fast but stale", correct: true },
    { label: "actions/cache cannot store node_modules, only ~/.npm", misconception: "It can cache any path. The failure is the key design: a constant key can never invalidate, so it serves stale contents regardless of what path you cache." },
    { label: "You forgot restore-keys, so the cache was empty", misconception: "An empty cache would make builds slow, not fast-and-stale. The symptom (fast + wrong) points to a cache that always hits with old contents — a constant key." },
    { label: "The matrix re-used a cache across Node versions", misconception: "That would be a scoping issue, but the described symptom is staleness across one toolchain over time, caused by a key that never tracks the lockfile." },
  ]}
/>

<Quiz
  id="02-caching-and-matrix-quiz-2"
  lessonSlug="02-caching-and-matrix"
  lang="en"
  question="A build job compiles dist/; a deploy job that 'needs: build' finds dist/ missing. What is the correct mechanism to pass it?"
  choices={[
    { label: "upload-artifact in build, download-artifact in deploy — each job is a fresh machine, so a guaranteed handoff is required, not a cache", correct: true },
    { label: "actions/cache with key: dist — caching is the standard way to share files between jobs", misconception: "Cache is a best-effort, evictable speedup; a miss is normal. A deploy must get the exact bytes every time, which is what artifacts guarantee within a run." },
    { label: "Nothing — jobs in the same workflow share a filesystem", misconception: "They do not. Each job runs on its own clean runner; the build's filesystem is gone when that job ends, which is exactly why the handoff is needed." },
    { label: "Re-run npm run build in the deploy job", misconception: "That works but rebuilds, risking drift from what you tested and wasting minutes. Artifacts ship the identical tested bytes." },
  ]}
/>

<TradeoffMatrix
  id="02-caching-and-matrix-tradeoff-1"
  lessonSlug="02-caching-and-matrix"
  lang="en"
  prompt="A library must verify it works on Node 18/20/22 across Ubuntu and macOS, give clear signal on failures, and not blow up CI minutes. Pick the matrix strategy."
  options={[
    { name: "matrix node:[18,20,22] os:[ubuntu,macos], fail-fast:false, max-parallel set, exclude rare combos", summary: "Full grid, full signal, throttled.", correct: true, justification: "This is the senior default: the cartesian product covers every supported combination, fail-fast:false lets you see exactly which cells broke instead of one cancelling the rest, max-parallel throttles concurrent runner usage so the grid doesn't saturate your minutes, and exclude trims combinations you don't support to fight the multiplicative explosion. Coverage and clear signal, cost kept in hand." },
    { name: "Single job on node 22 / ubuntu only", summary: "Cheap, one cell.", justification: "Cheapest and fastest, but it proves nothing about Node 18/20 or macOS — exactly the compatibility the library must guarantee. Fine for a quick smoke check, not for a cross-version/OS support claim." },
    { name: "Full grid with fail-fast:true (default)", summary: "Grid but cancels on first fail.", justification: "You get the combinations, but the first failure cancels every sibling, so you learn one broken cell and lose visibility into the others — you fix it, rerun, and discover the next. fail-fast:true favours fast-stop CI; for a compatibility matrix where you want the whole failure picture, set it false." },
    { name: "Add a third axis (e.g. 3 package managers) to the grid for completeness", summary: "More axes, more coverage.", justification: "Dimensions multiply: a third 3-value axis turns 18 jobs into 54, each burning runner minutes for marginal signal. A matrix is a cost dial; add an axis only when the combination genuinely fails differently, not for completeness theatre." },
  ]}
/>

</Explanation>

<KeyTakeaway>Key the cache on the lockfile hash so it invalidates when deps change — a constant key serves stale forever. Use a matrix to test versions in parallel, and artifacts (not cache) to hand tested output between jobs.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-caching-and-matrix-retrieval"
  lang="en"
  questions={[
    { q: "Why is a cache key bound to a lockfile hash safe while a constant key is dangerous, and what does restore-keys add?", a: "actions/cache restores a saved directory only when its key matches, and the cardinal rule is that the key must change exactly when the cached contents should. hashFiles('**/package-lock.json') gives a checksum that changes the instant any dependency changes, so a lockfile bump produces a new key: the old entry is not reused, you install fresh against the new lockfile, and the run saves a new entry. A constant key like 'deps' can never miss, so it restores the same old node_modules on every run forever — your new lockfile is silently ignored, which is fast and wrong (it can pass CI and break in prod). restore-keys is a prefix-fallback ladder: when the exact key misses, GitHub restores the most recent partial match (e.g. runner.os-npm-) so a lockfile change still warm-starts from the previous cache and the package manager only fetches the delta. It is a warm start, not a correctness mechanism — the exact key still decides whether an entry counts as current." },
    { q: "Cache and artifacts both move files between jobs. When do you use each, and why not cache for a build-to-deploy handoff?", a: "They have different guarantees because each CI job runs on a fresh machine, so anything one job produces is gone in the next. A cache is a keyed, best-effort speedup for reproducible work whose inputs you still have: a miss is normal and safe because you can just re-derive the result (re-run the install). It can also be evicted — the repo has a ~10 GB budget and LRU eviction. An artifact is an explicit, durable-for-the-run output you upload and download by name, guaranteed available to a downstream job via needs, and usable as the run's published deliverable (bundle, test reports, coverage). For build-to-deploy you must ship the exact bytes you tested: cache is wrong because a miss would leave deploy with nothing and re-deriving risks rebuild drift, whereas upload-artifact in build + download-artifact in deploy guarantees the identical tested output. Rule of thumb: cache = reproducible speedup you can afford to lose; artifact = an output you must keep or pass on." },
  ]}
/>

<Recap lang="en">
Caching and matrices are the two big levers on a CI pipeline, and artifacts are how jobs talk to each other. Each job runs on a clean machine, so actions/cache saves and restores a directory keyed on a string you design — and the whole game is in that key: bind it to hashFiles('**/package-lock.json') so it invalidates the moment dependencies change, because a constant key never misses and serves stale node_modules forever, which is fast and wrong. restore-keys gives a prefix-fallback warm start after a real change, and setup-node's built-in cache: 'npm' handles the key for you; remember caches are branch-scoped with read-down inheritance and LRU-evicted under a 10 GB budget. A strategy.matrix fans one job definition across versions and OSes as the cartesian product, run in parallel, with include/exclude to shape combinations, fail-fast to choose fast-stop versus full-failure visibility, and max-parallel to throttle — but dimensions multiply, so a matrix is a cost dial, not free coverage. Finally, because the build's filesystem vanishes with the job, you hand output forward with upload-artifact/download-artifact: artifacts are durable, named, guaranteed-within-the-run outputs you keep or publish, whereas cache is a best-effort, evictable speedup. Use cache for reproducible work you can afford to lose, and artifacts for the exact bytes you must pass on.
</Recap>
