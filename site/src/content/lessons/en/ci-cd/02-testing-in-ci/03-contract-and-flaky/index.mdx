---
concepts:
- consumer-driven-contracts
- pact-broker
- can-i-deploy
- flaky-test-causes
- test-determinism
- quarantine-and-retries
deepensInto: []
estMin: 17
lang: en
lessonType: topic
level: senior
order: 3
prereqs: []
slug: 03-contract-and-flaky
sources:
- https://docs.pact.io/
- https://martinfowler.com/articles/contract-test.html
- https://docs.pact.io/pact_broker/can_i_deploy
- https://martinfowler.com/articles/practical-test-pyramid.html
spiral: []
status: ready
summary: 'Independently deployed services break each other through APIs; consumer-driven
  contracts catch it before deploy without a shared integration env. And flaky tests
  rot trust until green stops meaning "safe to merge" — make them deterministic, not retried.'
title: 'Contract testing and taming flaky tests'
track: ci-cd
unit: 02-testing-in-ci
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
The checkout team renames a JSON field from `total_cents` to `amount_cents` and ships at 11:00. Their tests are green, their service is healthy. At 11:04 the orders service starts throwing — it still reads `total_cents`, now `undefined`, and writes `NaN` to the ledger. Nobody ran a test that exercised both services together, because the only one that did lived in a nightly integration suite that takes 40 minutes and fails so often everyone ignores its email. Two separate CI failures here: there was no fast check that the orders service depended on that field, and the one slow check that might have caught it had already lost all credibility to flakiness.
</Hook>

<Crux>Independently deployed services break each other via APIs; consumer-driven contracts catch it in CI; flaky tests rot trust in green.</Crux>

<Explanation>

## Why a shared integration env is the wrong tool

When two services deploy on the same release train, an end-to-end test that spins up both is honest: it tests exactly what ships. The moment they deploy independently — the normal state of any microservice estate — that honesty evaporates. A full integration environment must run *every* service at once, which makes it slow to provision, expensive to keep current, and a flake magnet: any one of N services being mid-deploy fails the suite for reasons unrelated to your change. Worse, it tests the *wrong question*. You don't care whether all services are mutually compatible in some staging snapshot; you care whether *the version you're about to deploy* won't break anyone who depends on it, and won't be broken by anyone it depends on.

Consumer-driven contract testing answers exactly that question without ever running two services in the same process. The insight: the only part of a provider's API that matters is the part its consumers actually use. So let each **consumer** record, as a byproduct of its own unit tests, the concrete request/response pairs it relies on — that recording is the **contract**. Each request/response pair is a single interaction: "when I GET `/orders/42`, I expect a 200 with a body containing `amount_cents` as a number." The consumer's tests run against a mock provider that replays those responses, so they stay fast and isolated. Then the **provider** fetches every consumer's contract and, in its own pipeline, verifies it can satisfy each interaction against the real handler. The checkout rename above would have failed checkout's provider verification the instant it ran orders' contract — in checkout's own CI, before merge, with no orders service deployed anywhere.

## The broker and can-i-deploy

The piece that turns two pipelines into a coordinated gate is the **broker** (the Pact Broker, or its hosted form PactFlow). The consumer publishes its contract there, tagged with its version and the branch/environment it's heading to. The provider's pipeline pulls the relevant contracts from the broker, verifies them, and publishes the *verification result* back. The broker thus holds a matrix: for every consumer version and provider version, has this pair been verified to be compatible?

That matrix is what powers **can-i-deploy** — the actual deploy gate. Before you release version X of a service to an environment, you ask the broker `can-i-deploy --pacticipant orders --version X --to-environment production`, and it answers yes only if every service `orders` will talk to in production has a recorded, passing verification against orders' contract at version X (and vice versa). It is a query over already-collected results, not a fresh test run, so it is instant. This is the difference from a shared env: instead of *hoping* a staging snapshot reflects production, the broker *knows* which exact versions are compatible because each side proved it in its own fast pipeline.

<SequenceDiagram
  label="Consumer-driven contract flow: the consumer generates a contract from its own tests and publishes it to the broker; the provider pulls that contract, verifies it against its real handler in its own pipeline, and publishes the result back; can-i-deploy then queries the broker before either side releases."
  hue="var(--d-systems)"
  actors={["Consumer", "Broker", "Provider"]}
  messages={[
    { from: "Consumer", to: "Consumer", label: "run unit tests vs mock provider" },
    { from: "Consumer", to: "Broker", label: "publish contract @ v.X" },
    { from: "Provider", to: "Broker", label: "pull consumer contracts" },
    { from: "Provider", to: "Provider", label: "verify each interaction vs real handler" },
    { from: "Provider", to: "Broker", label: "publish verification result" },
    { from: "Provider", to: "Broker", label: "can-i-deploy --to production?" },
    { from: "Broker", to: "Provider", label: "yes: every pair verified compatible" },
  ]}
  caption="No shared integration environment and the two services never run together. Each side proves compatibility in its own fast pipeline; the broker's verification matrix answers can-i-deploy instantly."
/>

Contract tests are not a replacement for schema or OpenAPI checks — they answer a narrower, sharper question. An OpenAPI diff tells you the *spec* changed in a breaking way; a contract tells you whether a breaking change actually breaks *a real consumer*. Renaming a field nobody reads is a breaking schema change but breaks zero contracts, so it deploys freely. Tightening a field three consumers parse passes the schema (still valid JSON) but fails their contracts. Use schema linting as a cheap first filter; use contracts to gate on real coupling.

<Inset kind="why" lang="en">
Contract testing is socio-technical, not just a tool. The contract is a negotiation between two teams, so a provider that "fixes" a failing verification by quietly editing the consumer's contract has defeated the point — the contract must be owned by the consumer and changed only by agreement. Bi-directional contract testing (PactFlow) relaxes this by letting a provider supply its OpenAPI spec instead of running consumer-generated tests, trading some fidelity for less coupling between the teams' test suites.
</Inset>

## Flaky tests: the trust-killer

A flaky test passes and fails on the *same code* with no change between runs. It is more dangerous than a consistently failing test, because a red that means nothing trains engineers to ignore red. Once "just re-run it" is the reflex, your pipeline has stopped being a gate: a real regression hides among the noise, people merge on a hunch, and the whole value of CI — that green means *safe to merge* — is gone. Treating flakiness as a quality problem in its own right, with a budget and an owner, is the senior move; tolerating it is how a test suite dies.

The causes are a short, recurring list, and naming the cause dictates the fix.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Cause</th>
        <th class="border border-border px-3 py-2 text-left">Symptom</th>
        <th class="border border-border px-3 py-2 text-left">Deterministic fix</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Timing / race</td>
        <td class="border border-border px-3 py-2">Fails under CI load, passes locally; <code>sleep(100)</code> "fixes" it</td>
        <td class="border border-border px-3 py-2">Wait on a condition (poll for the element/state), never a fixed sleep; control async ordering</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Real clock / date</td>
        <td class="border border-border px-3 py-2">Breaks at midnight, month-end, or across DST</td>
        <td class="border border-border px-3 py-2">Inject a fake clock; freeze time in the test</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Shared state / order dependence</td>
        <td class="border border-border px-3 py-2">Passes alone, fails when the suite is shuffled or run in parallel</td>
        <td class="border border-border px-3 py-2">Isolate per-test state (fresh DB/txn rollback); randomize order to surface it</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Real network / external service</td>
        <td class="border border-border px-3 py-2">Fails when a third party is slow or down</td>
        <td class="border border-border px-3 py-2">Stub the boundary; pull the integration to a contract test</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Test pollution</td>
        <td class="border border-border px-3 py-2">Test B fails only after Test A ran (leaked global/singleton/file)</td>
        <td class="border border-border px-3 py-2">Reset globals in teardown; no cross-test fixtures</td>
      </tr>
    </tbody>
  </table>
</div>

The workflow around these is three moves. **Detect**: re-run the suite on a schedule and track pass/fail per test over time, so flakiness shows up as a dashboard, not a vibe. **Quarantine**: when a test flakes, mark it as quarantined so it still runs and reports but does *not* gate the merge — and file a ticket. This protects the pipeline's signal while you fix the test, without the worse alternative of deleting it. **Fix**: drive it to determinism using the table above; quarantine is a holding pen, not a graveyard, and a quarantine list that only grows is itself a failure.

Bounded retries are the most misused tool here. Auto-retrying a failed test until it passes does not fix flakiness — it *hides* it, and it will hide real intermittent bugs (a genuine race in production code) just as readily. The only defensible use is a small, bounded retry (e.g., 2 attempts) on **end-to-end tests only**, where some environmental flakiness is irreducible, and only when every retry is logged and counted so the flake rate stays visible. Never retry a unit or integration test: those should be deterministic, and a retry there is a confession you skipped the fix.

<TradeoffMatrix
  id="contract-flaky-tradeoff-1"
  lessonSlug="03-contract-and-flaky"
  lang="en"
  prompt="A unit test for an order-discount calculator passes ~95% of CI runs and fails the rest, on unchanged code. What do you do?"
  options={[
    { name: "Find the nondeterminism (clock, shared state, ordering) and make the test deterministic", summary: "Fix the root cause.", correct: true, justification: "A unit test should be fully deterministic, so a 95% pass rate is a defect in the test, not background noise. The fix is to name the cause — almost always a real clock, leaked shared state, or order dependence — and remove it: inject a fake clock, isolate per-test state, control async ordering. This is the only option that restores the test's signal; everything else preserves or hides the rot." },
    { name: "Quarantine it immediately and move on, leaving it quarantined", summary: "Mark, then forget.", justification: "Quarantine is the right first move to protect the pipeline's signal, but it is a holding pen, not a destination. Leaving it quarantined forever is how a suite quietly loses coverage — quarantine must come with a ticket and a fix. As the sole answer it stops the bleeding but never heals the wound." },
    { name: "Add an automatic retry (3 attempts) so it goes green", summary: "Retry until pass.", justification: "Retrying a unit test hides the flakiness instead of fixing it, and it will hide a real intermittent bug — a genuine race in the discount logic — just as readily as an environmental hiccup. Bounded retries are defensible only on end-to-end tests where some flakiness is irreducible, never on a unit test that should be deterministic." },
    { name: "Delete the test — 95% is good enough and it keeps blocking merges", summary: "Drop it.", justification: "Deleting removes coverage of real logic to silence a test problem, the worst trade of all: you lose the regression net and the next discount bug ships unnoticed. If it must stop gating merges right now, quarantine it (still runs, still reported) and fix it — don't throw away the assertion." },
  ]}
/>

<Quiz
  id="contract-flaky-quiz-1"
  lessonSlug="03-contract-and-flaky"
  lang="en"
  question="A provider renames a JSON field that one consumer reads. Which check is most likely to catch it before deploy, and why?"
  choices={[
    { label: "The provider's contract verification — it replays the consumer's recorded interaction against the real handler and fails because the expected field is gone", correct: true },
    { label: "The consumer's own unit tests", misconception: "The consumer's tests run against a mock that replays the OLD recorded responses, so they stay green — the consumer doesn't know the provider changed. The break surfaces on the PROVIDER side, when it verifies the consumer's contract against its new handler." },
    { label: "A shared nightly integration environment", misconception: "It might catch it, but a full integration env is slow, flaky, and runs after the fact — exactly the brittle, low-trust check contract testing exists to replace. And it doesn't gate the deploy of a specific version." },
    { label: "An OpenAPI schema lint", misconception: "Schema linting can flag that the spec changed, but it can't tell whether the change breaks a REAL consumer — a renamed field nobody reads is a breaking schema diff that breaks zero contracts. Contracts gate on actual coupling, not on spec shape alone." },
  ]}
/>

<Quiz
  id="contract-flaky-quiz-2"
  lessonSlug="03-contract-and-flaky"
  lang="en"
  question="A unit test fails ~1 in 20 runs on unchanged code. Your teammate wires up an automatic 3x retry so the build goes green. What's the senior objection?"
  choices={[
    { label: "Retrying hides the flakiness rather than fixing it, and will mask a real intermittent bug just as readily; a unit test must be deterministic, so the cause (clock/shared state/ordering) should be removed", correct: true },
    { label: "Three retries is too few — it should be five", misconception: "More retries make the masking worse, not better. The number isn't the problem; retrying a unit test at all is. The fix is determinism, not more attempts to roll a passing run." },
    { label: "Retries are fine here; unit tests are inherently flaky", misconception: "Unit tests are NOT inherently flaky — they run in-process with no network or real clock, so flakiness there is always a removable defect (a real clock, shared state, or order dependence). Bounded retries belong on e2e tests, never units." },
    { label: "The build should be configured to ignore that test's result", misconception: "Ignoring the result is just a permanent, invisible quarantine with no ticket and no fix — it silently drops coverage. Quarantine should keep the test running and reported while you make it deterministic." },
  ]}
/>

</Explanation>

<KeyTakeaway>Each consumer records the pairs it uses as a contract; the provider verifies it and can-i-deploy gates the release — no shared env. Flaky tests rot trust in green: fix the cause, quarantine, retry e2e only.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="contract-flaky-retrieval"
  lang="en"
  questions={[
    { q: "Walk through consumer-driven contract testing end to end, and say why it beats a shared integration environment.", a: "Each consumer, as a byproduct of its own unit tests, records the concrete request/response pairs it actually relies on — those interactions are the contract. The consumer's tests run against a mock provider that replays those responses, so they stay fast and isolated. The consumer publishes the contract to a broker (Pact Broker or PactFlow), tagged with its version and target environment. The provider's pipeline then pulls every consumer's contract and verifies, against its real handler, that it can satisfy each interaction; it publishes the verification result back to the broker. The broker thus holds a matrix of which consumer-version/provider-version pairs are proven compatible, and can-i-deploy queries that matrix instantly before a release. This beats a shared integration env on three counts: it never runs two services together, so it's fast and not a flake magnet; each side proves compatibility in its own pipeline before merge rather than after the fact in staging; and it answers the right question — is THIS version safe to deploy against everyone it talks to — instead of hoping a staging snapshot mirrors production. It complements rather than replaces schema/OpenAPI checks: a schema diff tells you the spec changed in a breaking way, a contract tells you whether the change breaks a real consumer." },
    { q: "Why are flaky tests dangerous, and what is the right response — including when retries are acceptable?", a: "A flaky test passes and fails on identical code, which is worse than a consistently red test because a red that means nothing trains the team to ignore red. Once 'just re-run it' is the reflex, the pipeline stops being a gate — a real regression hides in the noise and a green build no longer means 'safe to merge.' The right response has three moves. Detect: re-run on a schedule and track per-test pass/fail so flakiness is a dashboard, not a vibe. Quarantine: mark the flaky test so it still runs and reports but doesn't gate the merge, and file a ticket — this protects the pipeline's signal without the worse option of deleting the test. Fix: drive it to determinism by naming the cause — timing/race (wait on a condition, never a fixed sleep), real clock (inject a fake clock), shared state or order dependence (isolate per-test state, randomize order to surface it), real network (stub the boundary), test pollution (reset globals in teardown). Bounded retries are acceptable only on end-to-end tests, where some environmental flakiness is irreducible, and only when every retry is logged and counted so the flake rate stays visible; never retry a unit or integration test, because there a retry just hides a removable defect and can mask a genuine intermittent bug." },
  ]}
/>

<Recap lang="en">
Independently deployed services break each other across APIs, and a full integration environment is the wrong tool to catch it: slow, flake-prone, and it tests whether some staging snapshot is mutually compatible rather than whether the version you're about to ship is safe. Consumer-driven contract testing answers the sharp question instead — each consumer records the request/response pairs it actually uses as a contract, runs its own tests against a mock, and publishes the contract to a broker; the provider pulls every contract and verifies it against its real handler in its own pipeline, publishing the result back. The broker's verification matrix powers can-i-deploy, an instant query that gates a release only when every relevant pair is proven compatible. Contracts complement schema/OpenAPI linting: the schema tells you the spec changed, the contract tells you whether it breaks a real consumer. The second half is trust: a flaky test that passes and fails on the same code trains people to ignore red, and once "just re-run it" is the reflex, green no longer means "safe to merge." Detect flakiness with scheduled re-runs and a dashboard, quarantine a flaky test so it still reports but doesn't gate while you file a ticket and fix it, and make it deterministic by killing the cause — fake clock, isolated state, controlled ordering, stubbed boundaries. Reserve bounded retries for end-to-end tests alone, always logged; a retry on a unit test only hides a removable defect, and can hide a real bug with it.
</Recap>
