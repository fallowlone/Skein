---
concepts:
- ci-cd-gha-events
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: senior
order: 1
prereqs: []
slug: "01-events-and-triggers"
sources:
- https://docs.github.com/en/actions/using-workflows/events-that-trigger-workflows
- https://docs.github.com/en/actions/using-jobs/using-concurrency
- https://docs.github.com/en/actions/security-guides/security-hardening-for-github-actions
spiral: []
status: ready
title: "Events and triggers: what fires a workflow, and the pull_request_target trap"
summary: "A workflow is event-driven: the `on:` block, filters, and concurrency decide what runs and what gets cancelled. pull_request runs untrusted code without secrets; pull_request_target runs trusted code with secrets against a fork — the classic exfiltration hole."
track: "ci-cd"
unit: "08-workflows-deep"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The leak post-mortem read like a magic trick. A drive-by contributor opened a pull request from a fork that touched only the test config — nothing suspicious. CI ran, went green, and twenty minutes later the repo's `NPM_TOKEN` and a deploy key were posted to a pastebin. Nobody had merged anything. The trick was the trigger: someone had switched the lint workflow from `pull_request` to `pull_request_target` months earlier to "fix" a secrets-not-available complaint. That one word changed the security model completely — `pull_request_target` runs the workflow definition from the *base* branch but with the *fork's* code checked out, and crucially with full access to the repo's secrets. The fork's malicious `pretest` script ran with the token in the environment. The fix wasn't a patch; it was understanding that the event you pick decides whether attacker-controlled code meets your secrets.
</Hook>

<Crux>A workflow is event-driven: `on:` and filters decide what runs; pull_request_target exposes secrets to untrusted code.</Crux>

<Explanation>

## The `on:` block is the whole entry point

A workflow does nothing until an event matches its `on:` block. There are three broad event families, and the distinction governs both behavior and security:

```yaml
on:
  push:
    branches: [main, "release/**"]
    paths-ignore: ["docs/**"]
  pull_request:
    types: [opened, synchronize, reopened]
  schedule:
    - cron: "17 3 * * 1-5"   # 03:17 UTC, Mon–Fri — offset to dodge the top-of-hour stampede
  workflow_dispatch:          # manual, with typed inputs
  workflow_call:              # invoked as a reusable workflow by another
```

Filters compose with **AND across keys, OR within a list**: `branches: [main]` *and* `paths: [src/**]` both must hold, but any branch in the list matches. `paths` and `paths-ignore` are mutually exclusive on one event; mixing positive and negative globs in a single `paths` list is the usual source of "why didn't my workflow run." A subtlety that bites teams using required status checks: if a `paths` filter skips a workflow, the required check is reported as *skipped*, not *passed* — and a branch protection rule that requires it will block the merge forever unless you make the check pass-through. `schedule` cron is UTC-only and is best-effort: GitHub explicitly drops scheduled runs under high load, and runs on the top of the hour are the most likely to be delayed or dropped, which is why production schedules use an offset like `17 3`.

## pull_request vs pull_request_target — the security fork in the road

For a PR from a fork, `pull_request` runs the workflow **from the PR's head ref**, in a sandbox with a read-only `GITHUB_TOKEN` and **no access to secrets**. That is the safe default: untrusted code runs, but it can't reach anything valuable. `pull_request_target` runs the workflow **from the base branch** (so the workflow file is trusted) but in the context of the base repo **with full secrets and a read-write token** — while the fork's code is what would be checked out if you naively `actions/checkout` the PR head. That combination is the exfiltration vector from the Hook: trusted trigger, untrusted code, live secrets.

```yaml
# DANGEROUS: secrets in scope, then checks out the fork's code and runs it
on: pull_request_target
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}  # ← attacker's code
      - run: npm ci && npm test   # ← runs with NPM_TOKEN in env
```

The rule of thumb: use `pull_request_target` **only** to do trusted work that never executes fork code (label a PR, post a comment, run a check that reads metadata). The moment you need to build or test the contributor's actual code, use `pull_request` and accept that secrets are off the table — or gate the privileged step behind a manual approval environment.

<Quiz
  id="01-events-and-triggers-quiz-1"
  lessonSlug="01-events-and-triggers"
  lang="en"
  question="Why is a workflow triggered by pull_request_target that checks out and tests the PR's head code a security hole, while the same steps under pull_request are safe?"
  choices={[
    { label: "pull_request_target grants secrets and a write token to a run that then executes attacker-controlled fork code; pull_request runs that same fork code with no secrets and a read-only token", correct: true },
    { label: "pull_request_target runs on a faster runner that bypasses the sandbox", misconception: "Both run on the same hosted runners with the same isolation. The danger is not the runner — it is that pull_request_target injects secrets and a write-capable token into the same job where fork code executes." },
    { label: "pull_request cannot check out the PR code at all, so nothing dangerous can run", misconception: "pull_request checks out the PR head by default and does run the fork's code — that is exactly why it is the safe trigger: it does so without secrets and with a read-only token, so executing the code leaks nothing." },
    { label: "The two triggers are identical; the hole is purely in the checkout action version", misconception: "The triggers differ fundamentally in token permissions and secret availability. No checkout version makes pull_request_target safe when it runs untrusted code with secrets in scope." },
  ]}
/>

<Inset kind="why" lang="en">
Why does GitHub even offer `pull_request_target`? Because legitimate fork PRs need *some* trusted automation: auto-labeling, "needs-review" bots, posting coverage comments — work that must read or write repo state but should never run the contributor's code. The event exists so that trusted metadata work can have credentials while the untrusted-code path (`pull_request`) stays locked down. The failure mode is collapsing the two: doing untrusted *code execution* under the *trusted* trigger. Keep them separate and each is safe.
</Inset>

## Concurrency: cancel the stale run before it wastes a runner

When a contributor pushes three commits in five minutes, three runs queue. Without concurrency control all three execute, and the first two are obsolete the moment the third starts. The `concurrency` block fixes this:

```yaml
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true
```

`cancel-in-progress: true` cancels any in-progress run in the same group when a new one starts — the canonical PR pattern, keying on `github.ref` so each branch is its own group. This routinely cuts wasted minutes by a large fraction on busy branches. The inverse pattern matters too: for deploy workflows you want `cancel-in-progress: false` so a deploy is never killed mid-flight, and the group serializes deploys to one-at-a-time instead. Concurrency is a queue gate, not a trigger — the event still fires; concurrency just decides which runs survive.

<Quiz
  id="01-events-and-triggers-quiz-2"
  lessonSlug="01-events-and-triggers"
  lang="en"
  question="A deploy-to-production workflow uses concurrency with cancel-in-progress: true keyed on the environment. A second deploy starts while the first is halfway through. What happens, and is it what you want?"
  choices={[
    { label: "The first deploy is cancelled mid-flight — usually NOT what you want for deploys, where false serializes them so each finishes before the next starts", correct: true },
    { label: "Both deploys run in parallel to the same environment", misconception: "A shared concurrency group never runs two members at once — that is the point of the group. The question is whether the in-progress one is cancelled or the new one waits." },
    { label: "The second deploy is rejected and never runs", misconception: "It is not rejected — with cancel-in-progress: true it cancels the running one and proceeds; with false it would queue and wait. Either way it eventually runs." },
    { label: "Cancel-in-progress only affects pull_request events, so a deploy is unaffected", misconception: "Concurrency applies to any event. cancel-in-progress: true will absolutely cancel an in-flight deploy, which is why production deploys set it to false." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="An event enters the on-filter, passes concurrency, and fans out to a job DAG whose security context depends on the trigger"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "event", label: "Event", sub: "push / PR / schedule" },
    { id: "filter", label: "on: filters", sub: "branches + paths (AND/OR)" },
    { id: "conc", label: "Concurrency", sub: "cancel-in-progress?" },
    { id: "ctx", label: "Trigger context", sub: "secrets? token perms?" },
    { id: "jobs", label: "Job DAG", sub: "needs: edges" },
    { id: "skip", label: "Skipped", sub: "filter / cancelled" },
  ]}
  edges={[
    { from: "event", to: "filter", label: "match" },
    { from: "filter", to: "conc", label: "queued" },
    { from: "conc", to: "ctx", label: "survives" },
    { from: "ctx", to: "jobs", label: "dispatch" },
    { from: "filter", to: "skip", label: "no match" },
    { from: "conc", to: "skip", label: "cancelled" },
  ]}
  caption="pull_request gives the job a read-only token and no secrets; pull_request_target gives it secrets and a write token from the base branch — the security branch point sits at the trigger, before any job runs."
/>

<KeyTakeaway>The `on:` block is the workflow's entire entry point: filters compose AND-across-keys / OR-within-lists, `schedule` is best-effort UTC cron that GitHub drops under load, and a `paths`-skipped required check blocks merges as "skipped," not "passed." The security model lives in the trigger: `pull_request` runs untrusted fork code with no secrets and a read-only token; `pull_request_target` runs the trusted base-branch workflow with full secrets — fatal if you then check out and execute the fork's code. Concurrency with `cancel-in-progress: true` kills stale PR runs to save minutes; deploys set it `false` to serialize instead.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-events-and-triggers-retrieval"
  lang="en"
  questions={[
    { q: "Explain the exact difference between pull_request and pull_request_target, and why one is a known exfiltration vector.", a: "pull_request runs the workflow from the PR head ref in a restricted context: the GITHUB_TOKEN is read-only and no secrets are available, so even though the fork's code runs, it has nothing to steal. pull_request_target runs the workflow file from the BASE branch (trusted), but with the base repo's full secrets and a read-write token in scope. If such a workflow then checks out the PR head SHA and runs build/test scripts, attacker-controlled code from the fork executes with live secrets in the environment — the exfiltration vector. The safe rule: use pull_request_target only for trusted metadata work (labels, comments, checks) that never executes fork code; build and test untrusted code under pull_request without secrets, or gate privileged steps behind a manual-approval environment." },
    { q: "How do on: filters compose, what makes schedule unreliable, and why can a path filter break a merge?", a: "Filters AND across different keys (branches AND paths both apply) but OR within a list (any listed branch matches). paths and paths-ignore are mutually exclusive on one event. schedule cron is UTC-only and best-effort — GitHub drops scheduled runs under high load, especially on-the-hour, so production schedules use an offset minute. A path filter that skips a workflow reports the status check as skipped, not passed; if branch protection lists that check as required, the PR is blocked indefinitely because skipped never satisfies the requirement — you must restructure so the required check still reports success on filtered paths." },
  ]}
/>

<Recap lang="en">
A GitHub Actions workflow is event-driven from the first line: the `on:` block names the events — `push`, `pull_request`, `schedule`, `workflow_dispatch`, `workflow_call` — and its filters compose AND across keys and OR within a list, with `paths`/`paths-ignore` mutually exclusive and a `paths`-skipped required check blocking merges as "skipped" rather than "passed." `schedule` is UTC-only and best-effort, so GitHub drops runs under load and on-the-hour jobs are the most likely casualties; offset the minute. The security model is decided at the trigger, not in the jobs: `pull_request` runs untrusted fork code with a read-only token and no secrets, so executing it is safe; `pull_request_target` runs the trusted base-branch workflow with full secrets and a write token, which becomes an exfiltration hole the instant you check out and run the fork's head code — reserve it for trusted metadata work and push untrusted code execution to `pull_request` or behind a manual-approval environment. Concurrency is a queue gate layered after the trigger: `cancel-in-progress: true` keyed on `github.ref` kills stale PR runs and reclaims minutes, while deploys flip it to `false` so the group serializes and no deploy dies mid-flight. Pick the event deliberately — it determines what runs, what gets cancelled, and whether attacker code ever meets your secrets.
</Recap>
