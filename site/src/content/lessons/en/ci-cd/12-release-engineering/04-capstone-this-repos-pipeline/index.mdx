---
concepts:
- ci-cd-gha-capstone
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: senior
order: 4
prereqs: []
slug: "04-capstone-this-repos-pipeline"
sources:
- https://docs.github.com/en/actions/using-jobs/using-concurrency
- https://docs.github.com/en/actions/use-cases-and-examples/deploying/deploying-to-your-cloud-provider
- https://docs.github.com/en/actions/how-tos/manage-workflow-runs/control-deployments-with-environments
spiral: []
status: ready
title: "Capstone: reading a real deploy.yml as a release-engineering case study"
track: "ci-cd"
unit: "12-release-engineering"
summary: "Read one real deploy workflow end to end — concurrency cancel-in-progress, a single artifact-free build-deploy job, cache-keyed state, a ~12-minute build — and judge it against the release-engineering bar: versioning, changelog, gates, and reversibility. Critique, not recall."
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The pipeline you are about to read is real — it is the shape of the workflow that deploys a large static curriculum site, and it earned its current form the hard way. An earlier version sharded the build across six parallel jobs and handed ~420 MB artifacts between them on every run; that blew a free-tier artifact-storage quota, and once the quota was hit it blocked every upload — even a 163 KB file — until the platform recalculated usage six to twelve hours later, wedging every deploy. The fix was not a bigger quota; it was a structural rewrite to a single artifact-free `build-deploy` job that keeps state only in a build cache. That history is the point of this capstone: a release pipeline is judged not by whether it is green today but by how it fails, how it recovers, and whether you can answer "what shipped and how do I undo it" at 3 a.m. Read this workflow the way a senior reviews a colleague's pipeline — find what it does well, what it leaves on the table, and what you would change before it carries something that moves money.
</Hook>

<Crux>A release pipeline is judged by how it fails and reverts, not by being green — read this real deploy.yml against the four release pillars.</Crux>

<Explanation>

## The workflow under review

Here is the shape of the pipeline, condensed to the load-bearing parts:

```yaml
name: build-deploy
on:
  push:
    branches: [main]

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: true        # a newer push aborts an in-flight deploy

permissions:
  contents: read

jobs:
  build-deploy:                    # one job: plan → build → lint → deploy
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: actions/cache@v4      # state lives here, not in artifacts
        with:
          key: build-${{ hashFiles('content-hash') }}
          path: dist/
      - run: bun install --frozen-lockfile
      - run: bun run build          # full render ~12 min (incremental shorter)
      - run: cat dist/lint-report.json   # build-time linter gate
      - run: ./deploy.sh dist/       # upload static output to the CDN/Pages host
```

What it does well, in this unit's vocabulary:

- **`concurrency` with `cancel-in-progress: true`** keyed on the ref means a newer push to `main` cancels an older, still-running deploy of the same branch. You never get two deploys racing to publish, and you never waste a 12-minute build on a commit that has already been superseded. This is the right default for a single-target static deploy where only the latest state matters.
- **A single artifact-free job** is the scar tissue from the Hook: plan, build, lint, and deploy run in one job and pass state through `actions/cache` (a separate quota from artifacts), so there is no 420 MB hand-off to wedge. Keeping the whole pipeline in one job also means there is no inter-job artifact to expire or corrupt — the cost is that the steps cannot scale out, which for a ~12-minute build is an acceptable trade.
- **`permissions: contents: read`** is least privilege: the job's token can read the repo and nothing else, so a compromised step cannot push commits or publish packages.
- **The build-time linter** (`lint-report.json`) is a release gate inside the build — content that violates the rules fails the run before anything ships.

<Quiz
  id="04-capstone-this-repos-pipeline-quiz-1"
  lessonSlug="04-capstone-this-repos-pipeline"
  lang="en"
  question="Why is `concurrency: { group: deploy-${{ github.ref }}, cancel-in-progress: true }` the right choice for this single-target static-site deploy, and where would it be dangerous?"
  choices={[
    { label: "For a single-target deploy where only the latest main state matters, cancelling a superseded in-flight run prevents two deploys racing and wastes no build on an already-replaced commit — but for a deploy that performs non-idempotent steps (migrations, partial writes) cancelling mid-run could leave a half-applied state", correct: true },
    { label: "It is always the safest setting because cancelling a run can never cause harm", misconception: "Cancelling is safe HERE because the deploy publishes a complete static snapshot and only the latest matters, so an aborted older run leaves nothing half-done. But a pipeline that runs a database migration or other non-idempotent, partial-progress step could be cancelled mid-write and leave inconsistent state — cancel-in-progress is not universally safe." },
    { label: "It speeds up the build by running the cancelled and new runs in parallel", misconception: "cancel-in-progress does the opposite of parallelizing — it ABORTS the older run so only one proceeds. It saves wasted compute on a superseded commit; it does not run things in parallel, and the build time of the surviving run is unchanged." },
    { label: "It is required for `actions/cache` to work", misconception: "Concurrency control and caching are independent features. The cache works regardless of the concurrency setting; cancel-in-progress is about not racing two deploys of the same ref, not about cache correctness." },
  ]}
/>

## Judge it against the release-engineering bar

The workflow is operationally sound, but read it against the four pillars of this unit and the gaps are clear — and instructive, because they are the difference between "deploys a static site" and "governs a release."

- **Versioning and tags**: this pipeline deploys on every push to `main` with **no version, no tag, and no immutable ref**. For a static site that is publishing latest-wins, that is a defensible choice — but it means there is no named release to point at, and "roll back to the last good version" has nothing to name. The mitigation that fits this design is the deploy host's own immutable deployment history (each upload is a distinct, addressable deployment you can instantly re-promote), which substitutes for a git tag as the reversibility anchor.
- **Changelog and release record**: there is no generated changelog and no release object, so "what changed in this deploy" is answerable only by reading the commit that triggered it. Acceptable for an internal site; a gap the moment external consumers or auditors need a per-release record.
- **Environments and gates**: the deploy runs **straight to production on push with no environment, no required reviewer, and no wait timer**. Every merged commit ships instantly with no human at release time and no soak. This is the single biggest gap against the unit: for anything beyond a low-stakes static site, you would put the deploy behind a `production` environment with a required reviewer and a short wait timer, and likely split build (on push) from deploy (gated).
- **Reversibility**: there is no explicit rollback path in the workflow. Recovery depends entirely on the deploy host's ability to re-promote a previous deployment. That works for a static CDN host, but it is implicit — a senior would make "re-deploy the previous known-good deployment" an explicit, tested, one-command path rather than a property they are assuming the platform provides.

<Quiz
  id="04-capstone-this-repos-pipeline-quiz-2"
  lessonSlug="04-capstone-this-repos-pipeline"
  lang="en"
  question="A reviewer says this pipeline 'has no rollback story.' Given it deploys a static site to a CDN host with an immutable per-upload deployment history, what is the most accurate critique and fix?"
  choices={[
    { label: "Rollback is possible via the host's re-promote-previous-deployment feature, but it is implicit and untested — make it an explicit, documented, one-command path with a known-good deployment to return to, rather than an assumed platform property", correct: true },
    { label: "The reviewer is wrong — static sites cannot be rolled back, so there is nothing to fix", misconception: "Static sites are very rollback-friendly: a CDN host with immutable per-upload deployments lets you instantly re-promote a prior known-good deployment. The reviewer's real point stands — the capability exists but the workflow does not make it an explicit, tested path, which is the gap to fix." },
    { label: "Add a database migration rollback step to the deploy job", misconception: "A static-site deploy has no database or migrations, so a migration-rollback step is irrelevant here. The reversibility concern is about re-promoting a prior static deployment, not about schema changes the pipeline does not make." },
    { label: "The fix is to delete the concurrency setting so old deploys are never cancelled", misconception: "Concurrency cancellation is orthogonal to rollback and is correct for this single-target deploy. Removing it would let deploys race and would not give you a way to return to a known-good deployment, which is what the rollback critique is actually about." },
  ]}
/>

<Inset kind="why" lang="en">
Why does a perfectly green, well-built pipeline still fall short of the unit's bar? Because "builds and deploys correctly" and "governs a release" are different jobs. This workflow nails the operational layer — concurrency, least privilege, a lint gate, and a failure-hardened single-job structure earned from a real outage. But release engineering adds a governance layer on top: a named, immutable thing each deploy produces (a version/tag or an addressable deployment), a human-readable record of what changed, a deliberate human gate before production, and an explicit, tested way back. The static-site context makes some of these optional — latest-wins publishing genuinely does not need semver — but the moment the same pipeline carries something a user depends on or an auditor inspects, each omitted pillar becomes the gap an incident finds. Reading a real pipeline well means naming which omissions are justified by context and which are latent incidents.
</Inset>

</Explanation>

<FlowDiagram
  label="The real pipeline as a chain — and where the release-engineering pillars are present or missing"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "push", label: "push to main", sub: "concurrency: cancel old" },
    { id: "cache", label: "Restore cache", sub: "state, not artifacts" },
    { id: "build", label: "Build + lint", sub: "~12 min, gated" },
    { id: "deploy", label: "Deploy", sub: "straight to prod" },
    { id: "gapgate", label: "Missing: gate", sub: "no env / reviewer / soak" },
    { id: "gaprev", label: "Implicit: rollback", sub: "host re-promote only" },
  ]}
  edges={[
    { from: "push", to: "cache", label: "least privilege" },
    { from: "cache", to: "build", label: "lint gate" },
    { from: "build", to: "deploy", label: "no approval" },
    { from: "deploy", to: "gapgate", label: "ships instantly" },
    { from: "deploy", to: "gaprev", label: "no explicit revert" },
  ]}
  caption="Strong operational layer (concurrency, least privilege, lint gate, failure-hardened single job); thin governance layer (no version/tag, no changelog, no production gate, implicit rollback). Context justifies some omissions, not all."
/>

<KeyTakeaway>This real pipeline is operationally excellent — ref-keyed concurrency that cancels superseded deploys, a single artifact-free job that is scar tissue from a quota-wedging outage, least-privilege permissions, and a build-time lint gate. But judged against the unit's four pillars it is thin on governance: no version, tag, or immutable ref; no generated changelog or release record; no production environment, required reviewer, or wait timer (it ships straight to prod on push); and only an implicit, platform-provided rollback. The static-site context justifies some of these, but every omitted pillar is the gap an incident finds the moment the pipeline carries something a user or auditor depends on. Reading a pipeline well is judging how it fails and reverts, not whether it is green.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-capstone-this-repos-pipeline-retrieval"
  lang="en"
  questions={[
    { q: "Summarize what this pipeline does well operationally and why each choice is sound for a single-target static deploy.", a: "Four operational strengths. (1) `concurrency` keyed on the ref with `cancel-in-progress: true` cancels an older in-flight deploy when a newer push to main arrives, so two deploys never race to publish and no 12-minute build is wasted on a superseded commit — correct because only the latest state matters and the deploy is idempotent (publishing a complete static snapshot). (2) A single artifact-free job passing state through `actions/cache` instead of handing 420 MB artifacts between sharded jobs — the structural fix for the real outage where artifact-storage quota exhaustion wedged every upload for 6–12 hours. (3) `permissions: contents: read` is least privilege, so a compromised step cannot push or publish. (4) The build-time linter is a release gate inside the build that fails non-conforming content before anything ships. Together they make the operational layer robust and failure-hardened." },
    { q: "Walk through the four release-engineering pillars and judge this pipeline on each, distinguishing context-justified omissions from latent incidents.", a: "Versioning/tags: it deploys on every push with no version, tag, or immutable ref — defensible for latest-wins static publishing, but it leaves nothing to name in 'roll back to the last good version'; the deploy host's immutable per-upload history substitutes as the reversibility anchor. Changelog/release record: none — 'what changed' is only the triggering commit, fine internally but a gap once auditors or external consumers need a per-release record. Environments/gates: the biggest gap — it ships straight to production on push with no environment, required reviewer, or wait timer, so every merge releases instantly with no human at release time and no soak; for anything higher-stakes you would gate the deploy behind a `production` environment and likely split build from deploy. Reversibility: no explicit rollback path; recovery relies implicitly on the host re-promoting a previous deployment, which a senior would make an explicit, tested one-command path. The static-site context justifies the versioning/changelog omissions; the missing production gate and implicit rollback are the latent incidents." },
  ]}
/>

<Recap lang="en">
The capstone reads one real deploy workflow as a case study and judges it the way a senior judges a colleague's pipeline. Operationally it is strong and its shape is earned: ref-keyed `concurrency` with `cancel-in-progress` cancels a superseded deploy so two never race and no long build is wasted; a single artifact-free `build-deploy` job passing state through `actions/cache` is the structural fix for a real outage where 420 MB inter-job artifacts exhausted a storage quota and wedged every upload for hours; `permissions: contents: read` is least privilege; and a build-time linter gates non-conforming content before it ships. Judged against this unit's four pillars, though, the governance layer is thin. There is no version, tag, or immutable ref — acceptable for latest-wins static publishing, with the host's immutable per-upload deployment history as the reversibility anchor. There is no generated changelog or release object, so "what changed" is only the triggering commit. The largest gap is gates: the pipeline ships straight to production on push with no environment, required reviewer, or wait timer, so every merge releases instantly with no human at release time and no soak — for anything beyond a low-stakes site you would gate the deploy behind a `production` environment and probably split build from deploy. And rollback is implicit, leaning on the platform's re-promote feature rather than an explicit, tested one-command path. The lesson of reading a real pipeline is that being green today is not the bar: a release pipeline is judged by how it fails, how it reverts, and whether you can answer "what shipped and how do I undo it" under pressure — and the honest review names which omissions the context justifies and which are simply the next incident waiting.
</Recap>
