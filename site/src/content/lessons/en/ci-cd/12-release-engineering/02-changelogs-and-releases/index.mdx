---
concepts:
- ci-cd-gha-changelogs
deepensInto: []
estMin: 15
lang: en
lessonType: topic
level: senior
order: 2
prereqs: []
slug: "02-changelogs-and-releases"
sources:
- https://www.conventionalcommits.org/en/v1.0.0/
- https://docs.github.com/en/repositories/releasing-projects-on-github/automatically-generated-release-notes
- https://keepachangelog.com/en/1.1.0/
spiral: []
status: ready
title: "Changelogs and releases: generated notes, PR labels, and the human-facing record"
track: "ci-cd"
unit: "12-release-engineering"
summary: "A changelog is the human-readable diff between two tags. Generate it from conventional commits or PR labels so it cannot drift from what shipped, attach it to an immutable GitHub Release tied to the tag and artifact, and the record becomes the auditable answer to what changed."
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The audit request was one line: "show us every change that touched the payment-rounding logic in the last quarter, and who approved each." The team had a `CHANGELOG.md` — hand-maintained, which meant it was a work of fiction. Half the entries said "various fixes and improvements." Three releases had no entry at all because someone forgot. One entry described a feature that was reverted before it shipped. The actual changes lived in 1,400 commits and a wall of merged PRs, and reconstructing the audit trail by hand took two engineers three days. The postmortem action item was not "write better changelogs." It was "stop writing them": derive the release notes mechanically from the same commits and PRs that produced the build, so the record can never disagree with what shipped — and attach that record to an immutable release the auditor can read directly.
</Hook>

<Crux>A changelog is the generated diff between two tags — derived from commits or PR labels so it cannot drift from what shipped.</Crux>

<Explanation>

## The changelog is a derived artifact, not a hand-written one

Ask yourself how many release notes you have written under pressure — and how many of them were accurate three months later. A changelog answers two questions for a human: *what changed between the last release and this one*, and *do I need to do anything before upgrading*. The Hook's failure was treating it as prose someone writes — which guarantees it drifts from reality, because the writer is a separate, fallible step from the shipping. The fix is to make it a **function of the same inputs that produced the build**. Two sources dominate:

- **Conventional commits**: a release tool groups commits since the last tag by type — `feat:` under *Features*, `fix:` under *Bug Fixes*, anything with `BREAKING CHANGE:` under a prominent *Breaking Changes* section — and emits a Markdown section per release. Because the commits are the same ones that built the artifact, the notes cannot describe something that did not ship.
- **PR labels / titles**: GitHub's automatic release notes (and tools like `release-drafter`) categorize **merged PRs** by label — `type: feature`, `type: fix`, `breaking` — into configured sections, and link each line to its PR and author. This is the model when you squash-merge and the PR title, not individual commits, is the unit of change.

Either way the changelog is generated in CI at tag time, appended to `CHANGELOG.md`, and used as the body of the release. The discipline shifts from "write good entries" to "label PRs and write good commit/PR subjects" — work that already happens on every change, enforced by a label/commit-lint check, so the record is a byproduct, not a chore.

<Quiz
  id="02-changelogs-and-releases-quiz-1"
  lessonSlug="02-changelogs-and-releases"
  lang="en"
  question="Why is a changelog generated from conventional commits or PR labels more trustworthy for an audit than a hand-maintained CHANGELOG.md?"
  choices={[
    { label: "It is derived from the same commits/PRs that produced the build, so it cannot describe a change that did not ship or omit one that did", correct: true },
    { label: "Because generated Markdown is formatted more consistently than hand-written entries", misconception: "Consistent formatting is a nice side effect, but it is not why the generated log is trustworthy. The trust comes from the SOURCE: it is derived from the exact commits/PRs that built the artifact, so it cannot drift — a hand-written log can be beautifully formatted and still be fiction." },
    { label: "Because it is stored in git and hand-written changelogs are not", misconception: "Both can live in git. The difference is not where it is stored but how it is produced: a hand-written entry is an independent, forgettable step that can disagree with what shipped, while a generated one is a function of the shipped commits/PRs." },
    { label: "Because generated notes never include breaking changes, which simplifies the audit", misconception: "The opposite is true and desirable — generated notes specifically surface breaking changes in a prominent section from `feat!`/`BREAKING CHANGE:` or a `breaking` label. Hiding breaks would make the changelog less useful, not more auditable." },
  ]}
/>

## The GitHub Release is the record, not the file

`CHANGELOG.md` is convenient for browsing in the repo, but the load-bearing artifact is the **GitHub Release**: an object bound to a specific tag, carrying the generated notes as its body, the build artifacts (or links to the published image/package), and — through the tag — the immutable ref and digest the release shipped. A release is the join point of everything in this unit: the semver name, the immutable tag, the digest of what ran, the human-readable changelog, and (optionally) the provenance attestation that proves how it was built. That join is exactly what the auditor wanted and the hand-maintained file could not give — a single, per-version record tying *what changed* to *what shipped* to *who approved it*. Treat the published release as immutable in spirit: you edit notes for clarity, but the version, tag, and artifact it points at do not change; a follow-up fix is a new release, not an edit of the old one. Pre-releases (`v2.5.0-rc.1` marked "pre-release") let release candidates carry generated notes and artifacts without appearing as the "Latest" release, so consumers who pin to latest are not pulled onto an unfinished build.

<Quiz
  id="02-changelogs-and-releases-quiz-2"
  lessonSlug="02-changelogs-and-releases"
  lang="en"
  question="A team wants the single most useful place to answer 'what shipped in v2.5.0, and how do I get exactly those bytes back?' Which artifact ties it all together, and why?"
  choices={[
    { label: "A GitHub Release bound to the v2.5.0 tag — it carries the generated notes plus the immutable tag and the artifact/digest, joining what changed to what shipped", correct: true },
    { label: "The CHANGELOG.md file, because it lists every version in one scrollable place", misconception: "CHANGELOG.md is handy for browsing, but it is just text — it does not bind the notes to the immutable tag, the digest, or the actual artifact. The release record is the object that joins the human-readable change to the exact bytes that shipped, which is what 'get those bytes back' requires." },
    { label: "The git commit history, since every change is already there", misconception: "The raw commit log has the changes but not the human-readable grouping, the version boundary, the artifact, or the approval — reconstructing 'what shipped in v2.5.0' from commits by hand is exactly the multi-day archaeology the lesson is trying to eliminate." },
    { label: "The CI run logs, because they show the build that produced the release", misconception: "Run logs are ephemeral and not version-addressed; they may be expired or rotated, and they do not present a per-version human record. The release object is the durable, version-keyed join of notes, tag, and artifact." },
  ]}
/>

<Inset kind="why" lang="en">
Why bind the changelog to the release object instead of just keeping the Markdown file? Because the file is detached from the artifact: nothing stops `CHANGELOG.md` from saying one thing while the deployed image is another, which is how the Hook's file drifted into fiction. The release object is anchored to a tag, and the tag resolves to a digest, so the notes, the version, and the exact shipped bytes are one indivisible record. When the auditor — or the on-call deciding what to roll back to — opens the release, they see the change description and the artifact identity in the same place, with no chance the two disagree.
</Inset>

</Explanation>

<FlowDiagram
  label="Commits and labeled PRs feed a generated changelog that becomes the body of an immutable release record"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "commits", label: "Commits + PRs", sub: "typed / labeled" },
    { id: "group", label: "Group by type", sub: "feat / fix / breaking" },
    { id: "notes", label: "Generated notes", sub: "Markdown sections" },
    { id: "tag", label: "Immutable tag", sub: "version boundary" },
    { id: "release", label: "GitHub Release", sub: "notes + artifact" },
    { id: "audit", label: "Audit / rollback", sub: "one lookup" },
  ]}
  edges={[
    { from: "commits", to: "group", label: "scan since tag" },
    { from: "group", to: "notes", label: "render" },
    { from: "notes", to: "release", label: "release body" },
    { from: "tag", to: "release", label: "bind + digest" },
    { from: "release", to: "audit", label: "what + how-to-undo" },
  ]}
  caption="The changelog is a function of the shipped commits/PRs, so it cannot drift. The release object joins notes, immutable tag, and artifact digest into one auditable record."
/>

<KeyTakeaway>A changelog is the human-readable diff between two tags, and it is trustworthy only when it is generated — from conventional commits grouped by type, or from labeled merged PRs — because then it is a function of the exact commits/PRs that built the artifact and cannot describe what did not ship. The load-bearing record is the GitHub Release: bound to an immutable tag, it joins the generated notes, the version name, and the artifact digest into one per-version object, which is what makes auditing what changed and rolling back to those exact bytes a single lookup instead of multi-day archaeology.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-changelogs-and-releases-retrieval"
  lang="en"
  questions={[
    { q: "Contrast generating release notes from conventional commits versus from PR labels — when does each fit, and what do they share?", a: "Both derive the changelog from the inputs that produced the build, so neither can drift from what shipped. Conventional commits group individual commits by type (`feat:` → Features, `fix:` → Bug Fixes, `BREAKING CHANGE:` → a prominent section); this fits teams that keep meaningful commit history and merge without squashing. PR labels (GitHub auto-generated notes or release-drafter) categorize merged PRs by label and link each line to its PR and author; this fits squash-merge workflows where the PR title, not the commit stream, is the unit of change. Either way the notes are generated in CI at tag time and become the release body, and the team's job shifts from writing entries to enforcing honest commit types or PR labels via a lint check." },
    { q: "Why is the GitHub Release object, not CHANGELOG.md, the artifact that makes auditing and rollback a single lookup?", a: "The Markdown file is detached text — nothing binds it to the deployed artifact, so it can say one thing while production runs another, which is how a hand-maintained log becomes fiction. The release object is bound to a specific immutable tag, and the tag resolves to a content digest, so it joins the human-readable notes, the semver name, the artifact, and optionally the provenance into one per-version record. An auditor or on-call opens one release and sees both what changed and the exact bytes that shipped, with no chance the description and the artifact disagree; rollback is re-deploying the digest that release names. The release is the join point of the whole unit: name, tag, digest, changelog, and approval in one place." },
  ]}
/>

<Recap lang="en">
A changelog answers two human questions — what changed since the last release and what you must do before upgrading — and it is trustworthy only when it stops being prose someone writes and becomes a function of the build's inputs. Generate it from conventional commits, grouping `feat:` under Features, `fix:` under Bug Fixes, and `BREAKING CHANGE:` under a prominent section, or from labeled merged PRs via GitHub's auto-generated notes or release-drafter, linking each line to its PR and author; either way it is emitted in CI at tag time and cannot describe a change that did not ship, because it is derived from the same commits/PRs that produced the artifact. The discipline moves from writing entries to enforcing honest commit types and PR labels with a lint check — work that already happens on every change. The load-bearing record is not CHANGELOG.md but the GitHub Release: an immutable object bound to a tag, carrying the generated notes, the artifacts or links, and through the tag the digest of what ran, plus optionally the provenance attestation. That release is the join point of the whole unit — semver name, immutable tag, artifact digest, human changelog, and approval in one per-version record — which turns "show every change and who approved it" and "restore exactly these bytes" from multi-day archaeology into a single lookup. Edit notes for clarity, but never the version or artifact a published release points at; a follow-up is a new release, and a release candidate ships as a marked pre-release so latest-pinned consumers are never pulled onto an unfinished build. Now when you see a hand-maintained changelog in a repo, you know exactly how far it has drifted from reality — and what to replace it with.
</Recap>
