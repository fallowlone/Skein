---
concepts:
- ci-cd-gha-environments
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: senior
order: 3
prereqs: []
slug: "03-environments-and-gates"
sources:
- https://docs.github.com/en/actions/how-tos/manage-workflow-runs/control-deployments-with-environments
- https://docs.github.com/en/actions/concepts/use-cases/deploy-to-third-party-platforms
- https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches/about-protected-branches
spiral: []
status: ready
title: "Environments and gates: required reviewers, wait timers, and branch protection"
track: "ci-cd"
unit: "12-release-engineering"
summary: "A GitHub Environment scopes secrets and wraps a deploy job in protection rules: required reviewers force human approval, a wait timer creates a soak window, and deployment-branch rules plus branch protection ensure only reviewed code from main can ever reach production."
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
At 2 a.m. an engineer pushed a hotfix branch and, to save time, pointed the deploy workflow straight at it. The job had the production deploy token in scope, so it shipped — straight to prod, from an unreviewed branch, with no second pair of eyes and no soak. The hotfix fixed the symptom and introduced a worse bug, and because nothing stood between "push" and "production" it reached 100% of users in ninety seconds. The retro found the real defect was not the bad code; it was that production was reachable by anyone who could trigger a workflow, from any branch, instantly. The fix was to put production behind a gate it could not skip: a named environment that holds the deploy secret, demands a human approval the deployer cannot self-grant, enforces a short wait so a bad release can be caught before it widens, and refuses to deploy anything that is not reviewed code on `main`.
</Hook>

<Crux>A GitHub Environment scopes the deploy secret and gates it with reviewers, a wait timer, and branch rules, so only approved main code ships.</Crux>

<Explanation>

## The environment is a scoped, gated boundary

A GitHub **Environment** (`production`, `staging`) is two things at once: a scope for secrets and a set of protection rules around any job that targets it. A job opts in with `environment: production`, and at that moment three things happen. First, the job gains access to **environment-scoped secrets** — the prod deploy token lives on the environment, not loose in the repo, so only a job that successfully enters `production` can ever see it. Second, the run **pauses** before the job starts until every protection rule is satisfied. Third, the deployment is recorded against the environment, giving the audit trail of who deployed what, when. The Hook's incident is impossible the moment the prod token is environment-scoped and the environment is gated, because there is no longer a path from "trigger a workflow" to "hold the prod credential" that does not pass through the rules.

```yaml
jobs:
  deploy-prod:
    runs-on: ubuntu-latest
    environment: production      # scopes secrets + triggers protection rules
    steps:
      - run: ./deploy.sh
        env:
          TOKEN: ${{ secrets.PROD_DEPLOY_TOKEN }}   # only readable inside `production`
```

## The three protection rules

Each rule closes a different kind of gap. When you see the 2 a.m. incident in the Hook, notice which specific rule would have stopped it — and notice that removing any single rule recreates a version of that gap. The environment's rules are what convert "a job that can deploy" into "a governed deploy":

- **Required reviewers**: the run blocks until a named person or team approves the deployment in the GitHub UI. Critically, the approval is **separate from the code review** — it gates the *act of deploying this run to this environment*, and a reviewer who is the deployer typically cannot self-approve, so shipping always needs a second human. This is the rule that the 2 a.m. push bypassed.
- **Wait timer**: a configurable delay (0–43,200 minutes, i.e. up to 30 days) inserted before the job runs *after* approval. A short timer — say 10 minutes — creates a deliberate soak/abort window: an obviously-bad release can be cancelled before it touches production, and a canary in front of it has time to report.
- **Deployment branches**: restrict which branches may deploy to the environment — typically `main` only (or a release-tag pattern). Combined with **branch protection** on `main` (required PR review, required status checks, no direct pushes), this guarantees the only code that can reach production is code that was reviewed and passed CI on the protected branch. The hotfix branch from the Hook would be refused outright.

Together these three rules mean production is not just password-protected — it is structurally unreachable unless code quality, human judgment, and timing all clear. Remove any one and a gap opens — direct-push code, a self-approved deploy, an instant blast — which is precisely the gap the incident fell through.

<Quiz
  id="03-environments-and-gates-quiz-1"
  lessonSlug="03-environments-and-gates"
  lang="en"
  question="A deploy job sets `environment: production`, which has required reviewers configured. Why does scoping the prod deploy token to that environment (rather than storing it as a repo secret) close the 2 a.m. hotfix hole even before anyone reviews the code?"
  choices={[
    { label: "An environment-scoped secret is only readable by a job that successfully enters the environment, so the job cannot obtain the prod token until the protection rules (including approval) are satisfied", correct: true },
    { label: "Environment secrets are encrypted more strongly than repo secrets, so they cannot be exfiltrated", misconception: "Both repo and environment secrets are encrypted the same way; the protection is not cryptographic strength. The point is ACCESS SCOPE: an environment secret is unreadable until the job enters the environment and passes its rules, so an unreviewed, unapproved job never holds the credential in the first place." },
    { label: "Repo secrets are disabled automatically once any environment exists", misconception: "Creating an environment does not disable repo secrets; they coexist. The fix is to deliberately MOVE the prod token onto the environment so only a gated job can read it — a repo-scoped token would still be readable by any job and would not close the hole." },
    { label: "Because environment secrets are only available on the main branch", misconception: "Branch restriction is a separate protection rule (deployment branches), not a property of where the secret is stored. The reason the scoping helps is that entering the environment — and thus satisfying its rules — is the precondition for reading the secret at all." },
  ]}
/>

## Required reviewers vs branch protection: two different gates

A common confusion is treating branch protection and required reviewers as redundant. They guard different things. **Branch protection** governs *what code is allowed onto `main`* — it is about the artifact's trustworthiness and fires at merge time. **Required reviewers on the environment** governs *whether this specific run may deploy now* — it is about the act of releasing and fires at deploy time. A change can be perfectly reviewed and merged (branch protection satisfied) and still need a deliberate, separately-approved decision to push it to production during business hours with someone watching (environment reviewer satisfied). You want both because they fail differently: branch protection alone lets any green main commit auto-deploy with no human in the loop at release time; environment review alone lets a human approve deploying code that was never reviewed. The wait timer then adds time to the human gate, and deployment-branch rules bind the two together by ensuring the run being approved actually originates from the protected branch.

<Quiz
  id="03-environments-and-gates-quiz-2"
  lessonSlug="03-environments-and-gates"
  lang="en"
  question="Branch protection on `main` already requires PR review before merge. Why also configure required reviewers on the `production` environment — isn't that the same approval twice?"
  choices={[
    { label: "They gate different acts: branch protection approves the code entering main at merge time; the environment reviewer approves the act of deploying a specific run to prod at release time — a release can need a fresh decision even on already-reviewed code", correct: true },
    { label: "Yes, it is redundant; one of the two should be removed to simplify the pipeline", misconception: "They are not redundant — they protect different things and fail differently. Branch protection alone would let any green main commit auto-deploy with no human deciding to release; environment review alone would let a human approve deploying code that never got a code review. Keeping both closes both gaps." },
    { label: "The environment reviewer replaces branch protection, which can then be disabled", misconception: "Disabling branch protection would let unreviewed code reach main, and the environment reviewer does not re-check the diff — it approves the deploy ACT, trusting that main is already protected. Remove branch protection and the environment reviewer is approving the deploy of code nobody reviewed." },
    { label: "Required reviewers only matter for staging, not production", misconception: "It is the reverse — required reviewers matter MOST for production. Staging is often left ungated for fast iteration; production is exactly where you want a separate human approval of the release act on top of branch protection." },
  ]}
/>

<Inset kind="why" lang="en">
Why separate the deploy approval from the code review at all — wouldn't one approval be simpler? Because they answer different questions at different times. The PR review asks "is this change correct?" when the code is proposed; the deploy approval asks "should we release this, now, to these users?" when the artifact is about to ship — a decision that depends on the current state of production, the on-call schedule, an ongoing incident, or a change freeze, none of which the original reviewer could know. Collapsing them means either you lose the release-time decision (code auto-ships the moment it merges) or you lose the merge-time decision (no diff review). The two-gate design is what lets a team merge freely into a protected main yet still hold a deliberate, human, abortable moment before each production release.
</Inset>

</Explanation>

<FlowDiagram
  label="A deploy run passes through branch protection, then the environment's reviewer, wait timer, and branch rule before the secret unlocks"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "main", label: "Protected main", sub: "PR review + CI" },
    { id: "env", label: "environment: production", sub: "scopes secret" },
    { id: "branch", label: "Branch rule", sub: "only main / tag" },
    { id: "review", label: "Required reviewer", sub: "human, no self-approve" },
    { id: "wait", label: "Wait timer", sub: "soak / abort window" },
    { id: "deploy", label: "Deploy", sub: "token now readable" },
  ]}
  edges={[
    { from: "main", to: "env", label: "run targets env" },
    { from: "env", to: "branch", label: "rule 1" },
    { from: "branch", to: "review", label: "rule 2" },
    { from: "review", to: "wait", label: "rule 3" },
    { from: "wait", to: "deploy", label: "all satisfied" },
  ]}
  caption="The prod token is unreadable until every rule passes, so there is no path from 'trigger a workflow' to 'hold the prod credential' that skips approval — the gap the 2 a.m. hotfix fell through."
/>

<KeyTakeaway>A GitHub Environment scopes the deploy secret and wraps the job in protection rules, so production is reachable only through a gate. The prod token lives on the environment and is unreadable until the job enters it, required reviewers force a human approval of the deploy act that is separate from the code review and cannot be self-granted, a wait timer creates a soak/abort window, and deployment-branch rules plus branch protection on main ensure only reviewed, CI-passed code from the protected branch can ever deploy. The four rules compose into a chain with no skippable gap.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-environments-and-gates-retrieval"
  lang="en"
  questions={[
    { q: "Explain how scoping the prod deploy secret to a GitHub Environment, combined with required reviewers, makes an unreviewed instant deploy impossible.", a: "When a job declares `environment: production`, the environment's secrets are only readable by that job once it has actually entered the environment — and entering is blocked until every protection rule passes. So the prod deploy token does not live loose in the repo where any triggered job could read it; it lives on the environment. A job that has not satisfied required reviewers (a human approval of the deploy act, separate from code review and not self-grantable), the wait timer, and the deployment-branch rule never enters the environment and therefore never obtains the token. There is no path from 'trigger a workflow' to 'hold the prod credential' that skips the gate, which is exactly why the 2 a.m. hotfix-straight-to-prod incident becomes structurally impossible." },
    { q: "Why are branch protection and environment required-reviewers not redundant, and what does each protect?", a: "They gate different acts at different times. Branch protection governs what code may enter `main` — required PR review, required status checks, no direct pushes — and fires at merge time, ensuring the artifact is trustworthy. Required reviewers on the `production` environment govern whether a specific run may deploy now, and fire at release time, ensuring a human deliberately approves the act of releasing. They fail differently: branch protection alone would auto-deploy any green main commit with no release-time human; environment review alone would let a human approve deploying code that was never diff-reviewed. The wait timer adds a soak/abort window to the deploy gate, and deployment-branch rules bind the two by ensuring the approved run actually came from the protected branch — together a chain where removing any link opens a real gap." },
  ]}
/>

<Recap lang="en">
A GitHub Environment is simultaneously a secret scope and a gate: a job opts in with `environment: production`, and only then can it read the environment-scoped deploy token, only after the run pauses for every protection rule, and the deployment is recorded for audit. Scoping the prod token to the environment is itself a control — there is no path from triggering a workflow to holding the prod credential that does not pass the rules, which is what makes the 2 a.m. unreviewed-hotfix-straight-to-prod incident structurally impossible. The three rules compose a chain. Required reviewers force a human approval of the deploy act, separate from the PR code review and not self-grantable, so every release needs a second person at release time. A wait timer — up to 30 days, but in practice a short soak like 10 minutes — inserts an abort window after approval so a bad release can be cancelled and a canary can report before it widens. Deployment-branch rules restrict which branches may deploy, typically main or a release tag, and combined with branch protection on main (required review, required checks, no direct push) guarantee only reviewed, CI-passed code reaches production. Branch protection and environment review are not redundant: one governs what code enters main at merge time, the other governs whether a run may deploy now at release time, and they fail in different ways, so you keep both. Remove any link — direct-push code, a self-approved deploy, an instant blast with no soak — and the exact gap the incident fell through reopens. Now when you review a colleague's pipeline and see a loose repo secret wired directly into a deploy job, you know exactly what is missing and why it matters at 2 a.m.
</Recap>
