---
concepts:
- ci-cd-gha-arc
deepensInto: []
estMin: 17
lang: en
lessonType: topic
level: senior
order: 2
prereqs: []
slug: "02-autoscaling-arc"
sources:
- https://docs.github.com/en/actions/hosting-your-own-runners/managing-self-hosted-runners-with-actions-runner-controller/about-actions-runner-controller
- https://docs.github.com/en/actions/hosting-your-own-runners/managing-self-hosted-runners-with-actions-runner-controller/deploying-runner-scale-sets-with-actions-runner-controller
- https://docs.github.com/en/actions/hosting-your-own-runners/about-self-hosted-runners
spiral: []
status: ready
title: "Autoscaling with Actions Runner Controller: ephemeral pods that scale to the queue"
summary: "Actions Runner Controller (ARC) runs self-hosted runners as ephemeral Kubernetes pods. A runner scale set scales pods up to maxRunners, each servicing one job, giving hosted-like isolation on your cluster with capacity that tracks the job queue instead of a fixed pool."
track: "ci-cd"
unit: "11-runners-and-scale"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The pool was eight static self-hosted runners on EC2, always on, and at 9 a.m. on a Monday it became a bottleneck: forty engineers pushed before standup, the queue grew to 60 jobs, and the median wait time hit eleven minutes for a test suite that ran in four. The runners were never idle and never enough. The platform team's first instinct was to add static capacity — but eight machines billed 24/7 were already burning money overnight when the queue was empty, and doubling them would double the idle waste to fix a two-hour-a-day spike. They moved to Actions Runner Controller instead: runners became ephemeral Kubernetes pods that scaled from zero to forty when the queue filled and back to zero when it drained. Monday's wait time dropped to under a minute, and the overnight bill dropped to nothing because no pods ran when no jobs were queued. The fixed pool had been paying for peak capacity around the clock to serve a spike that lasted two hours.
</Hook>

<Crux>ARC turns runners into ephemeral pods that scale to the job queue: capacity follows demand, not a fixed always-on pool.</Crux>

<Explanation>

## What ARC is

If you have ever asked "why am I paying for idle runners overnight to handle a two-hour morning spike?", ARC is the answer — but only if you understand what actually drives its scaling decisions.

Actions Runner Controller is a Kubernetes operator that manages self-hosted runners as pods. You install the controller and define a **runner scale set** — a Helm release bound to a repository, organization, or enterprise with a registration token. The scale set's listener holds a long poll to GitHub's Actions service; when jobs are assigned to that scale set's runner group, the listener learns how many are queued and the controller creates that many **ephemeral runner pods**. Each pod registers, runs **exactly one job**, then terminates and is replaced. There is no persistent runner accumulating state between jobs — the same one-job-one-runner isolation that makes hosted runners safe, now on your own cluster.

The scaling is bounded by two values you set: **minRunners** (a warm floor of idle pods to absorb the cold-start latency of pod scheduling and image pull) and **maxRunners** (the ceiling that caps both your blast radius and your cloud bill). Between them, pod count tracks the queue: jobs queued drives pods up toward maxRunners, jobs draining lets pods finish and not be replaced, falling back toward minRunners (which can be zero — scale-to-zero when idle).

<Quiz
  id="02-autoscaling-arc-quiz-1"
  lessonSlug="02-autoscaling-arc"
  lang="en"
  question="In an ARC runner scale set, how many jobs does a single ephemeral runner pod handle before it is replaced, and why does that matter?"
  choices={[
    { label: "Exactly one — the pod runs a single job then terminates, giving each job a clean isolated environment with no state carried over", correct: true },
    { label: "As many as fit in the pod's lifetime — pods are reused across jobs to amortize startup cost", correct: false, misconception: "ARC pods are ephemeral by design: one job per pod, then terminate. Reuse across jobs would reintroduce the persistent-runner state leakage that ephemeral runners exist to prevent." },
    { label: "It depends on minRunners — warm pods handle multiple jobs while cold ones handle one", correct: false, misconception: "minRunners only sets how many idle pods stay warm to reduce cold-start latency; it does not change the one-job-per-pod rule. Every pod, warm or freshly scheduled, runs a single job and is replaced." },
    { label: "Up to maxRunners jobs per pod before the ceiling forces a new one", correct: false, misconception: "maxRunners is the cap on concurrent pods, not jobs per pod. Each pod still runs exactly one job; maxRunners just limits how many such pods exist at once." },
  ]}
/>

## Cold start, warm floor, and the queue-wait tradeoff

The cost of ephemerality is **cold start**: a freshly created pod must be scheduled onto a node (which may itself need the cluster autoscaler to add a node — tens of seconds to minutes), pull the runner image, register with GitHub, and only then pick up the job. For a four-minute test suite, a 90-second cold start is a 38% overhead on the first job of a spike. `minRunners` is the lever: keep a few pods warm and idle so the front of a burst lands on a ready runner while the controller scales the rest behind it. The tradeoff is direct — warm pods cost money while idle, so `minRunners` is a dial between **queue-wait latency** (set it low, pay less, wait more at the start of a burst) and **responsiveness** (set it higher, pay for idle warmth, start faster). Right-size it to your actual spike shape, not to peak.

<Inset kind="connect" lang="en">
ARC layers on the cluster autoscaler, and the two cold starts compound. When the queue spikes past what current nodes can hold, ARC creates pending pods that no node can schedule; the Kubernetes cluster autoscaler sees the unschedulable pods and provisions new nodes. So a cold burst can pay *two* latencies: node provisioning, then pod startup. Teams that need fast spike response keep both a warm `minRunners` floor and a small headroom of spare node capacity, accepting steady idle cost to avoid the stacked cold-start delay — the same fixed-cost-versus-latency tradeoff as the earlier static pool, now tuned with two knobs instead of one.
</Inset>

## Sizing maxRunners and the runner image

`maxRunners` is your safety ceiling. Set it from two constraints: your **GitHub Actions concurrency limit** (a plan-level cap on concurrent jobs — exceeding it just queues, so a maxRunners above it wastes nodes) and your **cluster capacity and budget** (each pod consumes CPU/memory requests; maxRunners times the per-pod request must fit your nodes, and uncapped scaling can run up a cloud bill or starve other workloads on a shared cluster). The pod image matters too: a lean custom runner image with your toolchain pre-baked cuts the per-pod cold start dramatically versus pulling a fat image and installing tools at job start — the same logic as a well-built CI base image, applied to the runner pod itself.

<Quiz
  id="02-autoscaling-arc-quiz-2"
  lessonSlug="02-autoscaling-arc"
  lang="en"
  question="A team sets minRunners: 0 to minimize cost and reports that the first job after an idle period waits about two minutes before it even starts. What is the most likely cause and the direct fix?"
  choices={[
    { label: "Cold start: with no warm pods, the first job waits for a pod (and possibly a new node) to be scheduled and the image pulled; raise minRunners above 0 to keep a warm floor", correct: true },
    { label: "The GitHub Actions concurrency limit is throttling the job; lower maxRunners to stay under it", correct: false, misconception: "Hitting the concurrency limit queues jobs when many run at once — it does not add a fixed two-minute delay to the first job after idle. The signature here (idle, then a slow first job) is cold start, fixed by a warm minRunners floor." },
    { label: "The runner pods are being reused and the previous job's cleanup is blocking; disable ephemeral mode", correct: false, misconception: "ARC pods are ephemeral and not reused, so there is no previous-job cleanup to block on. Disabling ephemeral mode would reintroduce state-leak risk and is the wrong fix; the delay is pod/node cold start." },
    { label: "The listener lost its long poll to GitHub; restart the controller", correct: false, misconception: "A broken listener would stop scaling entirely, not add a consistent two-minute startup to jobs that do eventually run. The pattern matches cold start of pods scaling from zero, addressed by minRunners." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="The scale set listener reads queued jobs from GitHub and the controller scales ephemeral runner pods between a warm floor and a hard ceiling"
  hue="var(--d-infra)"
  perRow={3}
  nodes={[
    { id: "github", label: "Actions queue", sub: "jobs assigned" },
    { id: "listener", label: "Listener", sub: "long poll" },
    { id: "controller", label: "Controller", sub: "reconciles pods" },
    { id: "pods", label: "Ephemeral pods", sub: "1 job each" },
    { id: "floor", label: "minRunners", sub: "warm floor" },
    { id: "ceiling", label: "maxRunners", sub: "hard ceiling" },
  ]}
  edges={[
    { from: "github", to: "listener", label: "queued count" },
    { from: "listener", to: "controller", label: "desired" },
    { from: "controller", to: "pods", label: "create / reap" },
    { from: "floor", to: "pods", label: "keep warm" },
    { from: "ceiling", to: "pods", label: "cap scaling" },
  ]}
  caption="Pod count tracks the queue between minRunners and maxRunners; each pod runs one job then dies. The warm floor trades idle cost for lower first-job latency; the ceiling caps both blast radius and cloud spend, and should sit under the GitHub concurrency limit and cluster capacity."
/>

<KeyTakeaway>Actions Runner Controller runs self-hosted runners as ephemeral Kubernetes pods driven by a runner scale set: a listener long-polls GitHub for queued jobs and the controller scales pods between minRunners (a warm floor that buys lower first-job latency at the price of idle cost) and maxRunners (a ceiling that caps blast radius and spend). Each pod runs exactly one job then terminates, giving hosted-like isolation on your cluster, with capacity that follows the queue instead of a fixed always-on pool — at the cost of cold start that you tune with the warm floor, a lean runner image, and spare node headroom.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-autoscaling-arc-retrieval"
  lang="en"
  questions={[
    { q: "Describe the ARC scaling loop and the roles of minRunners and maxRunners.", a: "A runner scale set's listener holds a long poll to GitHub's Actions service and learns how many jobs are queued for its runner group. The ARC controller reconciles the desired pod count to match: it creates ephemeral runner pods, each of which registers, runs exactly one job, and then terminates without being reused. minRunners is a warm floor of idle pods kept ready so the front of a burst lands on a scheduled runner instead of paying cold-start latency; it can be zero for scale-to-zero when idle. maxRunners is the hard ceiling that bounds both blast radius and cloud spend, and should be set under the GitHub Actions concurrency limit (above it only queues) and within cluster capacity. Between the two, pod count tracks the queue — rising as jobs are assigned, falling as they drain." },
    { q: "Why does scale-to-zero (minRunners: 0) introduce first-job latency, and what compounds it?", a: "With no warm pods, the first job of a burst must wait for a pod to be created and scheduled onto a node, the runner image pulled, and registration with GitHub completed before the job even starts — a cold start of tens of seconds to a minute or more. It compounds with the cluster autoscaler: if no existing node has room, ARC's pending pods are unschedulable, the Kubernetes cluster autoscaler must provision a new node first, and only then can the pod start — two stacked latencies, node provisioning plus pod startup. Mitigations are a non-zero minRunners warm floor, a lean pre-baked runner image to shorten the pull, and a small amount of spare node headroom so pods can schedule without waiting on a new node. Each mitigation trades steady idle cost for faster spike response." },
  ]}
/>

<Recap lang="en">
Actions Runner Controller is a Kubernetes operator that replaces a fixed pool of always-on self-hosted runners with ephemeral pods that scale to the job queue. You define a runner scale set bound to a repo, org, or enterprise; its listener long-polls GitHub's Actions service for queued jobs, and the controller reconciles the pod count to match. Each pod registers, runs exactly one job, and terminates — the same one-job-one-runner isolation that makes hosted runners safe, brought onto your own cluster. Scaling is bounded by minRunners, a warm floor of idle pods that lowers first-job latency at the cost of paying for idle warmth, and maxRunners, a hard ceiling that caps blast radius and cloud spend. The price of ephemerality is cold start: a fresh pod must be scheduled, pull its image, and register before running, and when the cluster autoscaler must add a node first, the two latencies stack. You tune that with a non-zero warm floor, a lean pre-baked runner image, and a little spare node headroom — each trading steady idle cost for faster spikes. Size maxRunners under your GitHub Actions concurrency limit (above it just queues) and within cluster capacity and budget, since every pod consumes CPU and memory requests and uncapped scaling can starve neighbours or run up a bill. The result is capacity that follows demand — zero pods when the queue is empty, scaling to the ceiling when it fills — instead of paying for peak capacity around the clock to serve a spike that lasts a couple of hours. Now when you see a team complaining about slow CI on Monday mornings and zero queue by noon, you know the fix: ARC with a calibrated warm floor, not more static runners.
</Recap>
