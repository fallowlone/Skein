---
concepts:
- deploy-vs-release
- canary
- blue-green
- feature-flags
- automated-rollback
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: senior
order: 3
prereqs: []
slug: "03-progressive-delivery"
sources:
- https://martinfowler.com/bliki/CanaryRelease.html
- https://martinfowler.com/bliki/BlueGreenDeployment.html
- https://docs.github.com/en/actions/deployment
spiral: []
status: ready
summary: 'Deploying code is not releasing it. Decouple them: ship dark, then expose gradually — canary at 1-5% with metric-gated auto-rollback, blue-green for instant flip-back, feature flags as a redeploy-free kill switch. Contain the blast radius instead of betting on staging.'
title: 'Progressive delivery: canary, blue-green and flags'
track: ci-cd
unit: 04-release-automation
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The tag was cut, the pipeline was green, the artifact was published. The team did everything the last two lessons taught — clean SemVer, a reviewed tag, a signed release — and then they pushed the new version to 100% of production in one shot, because that is what the deploy script had always done. Within fifteen seconds the error rate went vertical. A serialization bug that only triggers under real concurrent writes — invisible in staging's single-threaded smoke test — was now failing for every single user. They had no gate, no slice to pull back, just one lever, and it was already all the way down. The rollback was a frantic redeploy of the previous version while the incident channel screamed: minutes of a full outage, 100% of users hit. The bug was never the real failure. Shipping it to everyone at once was.
</Hook>

<Crux>Deploying puts code on servers; releasing sends users to it. Decouple them: ship dark, expose gradually, watch signals, pull back fast.</Crux>

<Explanation>

## Deploy is not release

This is the framing the whole lesson hangs on. **Deploying** is putting new code on servers — the binary is running, the container is up. **Releasing** is directing user traffic to that code — deciding who actually hits it. The previous lessons got you to a published artifact; they did not say a single user should see it yet. The senior move is to treat those as two separate switches. Ship the code *dark* — deployed but receiving no traffic — and then turn the traffic dial up gradually while you watch the signals, ready to turn it back down the instant they degrade. Everything below is a different mechanism for that one idea: separate "the code is here" from "users are on it", and put a blast-radius limiter on the second switch.

## The four rollout strategies

```yaml
# big-bang (recreate): stop everything, start the new version — downtime + 100% blast radius
strategy:
  type: Recreate          # old pods die, new pods start: a gap where nothing serves

# rolling: replace instances batch by batch — no downtime, but two versions run at once
strategy:
  type: RollingUpdate
  rollingUpdate:
    maxSurge: 25%
    maxUnavailable: 0      # k8s default-ish: incremental, but NO explicit traffic gate
```

- **Big-bang (recreate / all-at-once):** stop the old version, start the new one. There is a downtime gap, and the moment it is up the blast radius is 100% — every user is on untested-in-prod code at once. This is the Hook's failure. Acceptable only for tiny or internal apps where a blip is fine.
- **Rolling:** replace instances batch by batch (the Kubernetes default). No downtime, which is why it feels safe — but two versions run simultaneously, so they must be backward/forward compatible, and a bad version still reaches users incrementally with *no explicit gate* watching whether it is healthy. Rolling controls *availability*, not *risk*.
- **Blue-green:** stand up two full environments. Deploy to GREEN (idle), smoke-test it in isolation, then flip the load balancer / router from BLUE to GREEN atomically. The cutover is instant, and so is rollback — flip the router back to BLUE. The cost is roughly `2x` infra during the switch, and the hard part is shared state: both versions usually talk to one database, so a schema change must be compatible with both BLUE and GREEN across the flip.
- **Canary:** route a *small* percentage of real traffic (1-5%) to the new version, watch its error rate, latency, and business metrics against the old version, then ramp `5 → 25 → 50 → 100%` if it stays healthy, or abort if it does not. The canary catches problems *with real production traffic* at a tiny blast radius — which is exactly the bug class staging missed.

<Inset kind="why" lang="en">
Why decouple deploy from release with flags and canaries instead of just testing harder in staging? Because staging structurally cannot reproduce production's real traffic mix, concurrency, data shape, and scale — and those are precisely where the expensive bugs live. Race conditions, hot-key contention, a serializer that only breaks under simultaneous writes, the one malformed payload that exists in a real users table — none of them show up against eight seeded rows and a single test thread. Progressive delivery accepts this and treats production as the final test, but with a limiter bolted on: expose the change to a thin slice, measure it against the baseline, and if it is bad you have contained the damage to that slice instead of betting your entire user base on staging having been representative. Testing harder is good; it just runs out before the bugs that take you down.
</Inset>

## Feature flags: the release switch that needs no redeploy

A canary moves traffic at the *infrastructure* layer. A **feature flag** moves the release decision into the *application* layer: wrap the new behaviour in a conditional, deploy it with the flag OFF, and the code ships dark while users keep getting the old path. Then you flip the flag — for 1% of users, then 10%, then everyone — *independently of any deploy*. The new code was already on every server; you are just changing a config value.

```ts
// the new code is deployed everywhere, but dark until the flag opens it
if (flags.isEnabled("new-checkout-pipeline", { userId })) {
  return runNewCheckout(order);   // exposed to 1% -> 10% -> 100% via config, no redeploy
}
return runLegacyCheckout(order);  // everyone else, unchanged
```

Two properties make this the senior's favourite tool. First, it is an **instant kill switch**: if `new-checkout-pipeline` misbehaves, you flip the flag back to OFF and every user is on the old path *in seconds* — no pipeline run, no image rebuild, no redeploy. Second, it does **per-user / percentage targeting**, which is what powers A/B tests and dark launches (run the new path, discard its output, just measure its load). When you own a flag that's been "temporarily enabled" for six months, you're already in the cost column. The cost is real: **flag debt**. Every flag is a branch in your code and a row in your config; stale flags that were never cleaned up become a combinatorial mess nobody dares delete. Flags are a release lever, not a permanent home — open them, learn, then remove them.

## Automated rollback: the gate decides, not a human

Everything above is just safer *if a human is watching*. The senior version is **automated**. A progressive-delivery controller — Argo Rollouts, Flagger, or logic in your deploy pipeline — ramps the canary on a schedule and, at each step, runs an **analysis query** against your metrics: error rate, p99 latency, SLO burn rate. If the query breaches a threshold, the controller **auto-aborts and rolls back** — shifts traffic back to the stable version — with no human in the loop. That is the key number to internalise: the rollback decision is **gated on metrics, not on vibes** and not on someone happening to be staring at a dashboard at 4pm.

```yaml
# Argo Rollouts canary with a metric-gated analysis step (sketch)
strategy:
  canary:
    steps:
      - setWeight: 5            # 5% of traffic to the new version
      - analysis:               # query Prometheus; auto-abort if it fails
          templates: [{ templateName: error-rate }]
      - setWeight: 25
      - analysis: { templates: [{ templateName: error-rate }] }
      - setWeight: 50
      - setWeight: 100          # full promotion only if every gate passed
```

Run the Hook's incident through this. The bad version goes to 5% of traffic. The serialization bug fires under real concurrency and the analysis query sees the error rate on the canary spike past its threshold within seconds. The controller aborts at 5% and shifts traffic back to stable — automatically. Blast radius: 5% of users for a few seconds, instead of 100% of users for minutes. The bug still happened; progressive delivery just made it a non-event.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Strategy</th>
        <th class="border border-border px-3 py-2 text-left">Downtime</th>
        <th class="border border-border px-3 py-2 text-left">Blast radius of a bad version</th>
        <th class="border border-border px-3 py-2 text-left">Rollback</th>
        <th class="border border-border px-3 py-2 text-left">Cost / catch</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Big-bang</strong></td>
        <td class="border border-border px-3 py-2">Yes (gap)</td>
        <td class="border border-border px-3 py-2">100% instantly</td>
        <td class="border border-border px-3 py-2">Redeploy old (slow)</td>
        <td class="border border-border px-3 py-2">No gate; only ok for tiny/internal</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Rolling</strong></td>
        <td class="border border-border px-3 py-2">No</td>
        <td class="border border-border px-3 py-2">Grows incrementally, no gate</td>
        <td class="border border-border px-3 py-2">Roll back the batches</td>
        <td class="border border-border px-3 py-2">Two versions live; must be compatible</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Blue-green</strong></td>
        <td class="border border-border px-3 py-2">No</td>
        <td class="border border-border px-3 py-2">100% at the flip (but smoke-tested first)</td>
        <td class="border border-border px-3 py-2">Instant: flip router back</td>
        <td class="border border-border px-3 py-2"><code>2x</code> infra; shared DB is the hard part</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Canary</strong></td>
        <td class="border border-border px-3 py-2">No</td>
        <td class="border border-border px-3 py-2">1-5%, contained by the gate</td>
        <td class="border border-border px-3 py-2">Auto-abort on metric breach</td>
        <td class="border border-border px-3 py-2">Needs good metrics + automation</td>
      </tr>
    </tbody>
  </table>
</div>

<TradeoffMatrix
  id="ci-cd-04-release-automation-03-progressive-delivery-tradeoff-1"
  lessonSlug="03-progressive-delivery"
  lang="en"
  prompt="You must ship a risky change to a high-traffic service with the minimum possible blast radius and instant rollback. How do you roll it out?"
  options={[
    { name: "Canary 1 -> 5 -> 25 -> 100% with automated metric-gated rollback, behind a feature flag kill switch", summary: "Gated ramp + kill switch.", correct: true, justification: "This is the senior answer because it minimises blast radius on two independent axes. The canary exposes the change to real production traffic at 1-5% first, and an analysis query on error rate / p99 / SLO burn auto-aborts and shifts back to stable the instant the new version misbehaves — so a bug hits ~5% of users for seconds, not 100% for minutes, with no human needed to notice. Wrapping the change in a feature flag adds a second, redeploy-free kill switch and lets you ramp per-user independently of the deploy. The decision to promote is gated on metrics, not vibes. Cost is real metrics + automation, which a high-traffic service already needs." },
    { name: "Big-bang to 100% after running extra tests in staging", summary: "Bet on staging.", justification: "This is exactly the Hook's failure dressed up as diligence. Staging structurally cannot reproduce production's concurrency, data shape, traffic mix, and scale, so the bug classes that take you down — race conditions, hot-key contention, real payload variety — survive any amount of extra staging testing and then hit 100% of users at once with no slice to pull back. More staging tests are good but they run out before these bugs; they are not a substitute for a blast-radius limiter on the release." },
    { name: "Blue-green flip with no canary analysis", summary: "Instant flip, no gate.", justification: "Blue-green buys you an instant, atomic cutover and instant flip-back, which is genuinely valuable — but with no canary analysis the flip still sends 100% of traffic to the new version in one move. A smoke test on GREEN catches gross breakage, not a subtle bug that only appears under real concurrent load, so you can still flip the whole user base onto a broken version and only learn from the resulting incident. Strong on rollback speed, weak on blast-radius containment for this risk profile." },
    { name: "Rolling update with no explicit health gate", summary: "Incremental, ungated.", justification: "A rolling update avoids downtime and replaces instances batch by batch, but it controls availability, not risk: there is no gate evaluating whether the new version is actually healthy before the next batch. A bad version reaches users incrementally and keeps rolling forward until something or someone notices, by which point a large fraction may already be on it. Better than big-bang, but it is not a metric-gated containment strategy." },
  ]}
/>

<Quiz
  id="ci-cd-04-release-automation-03-progressive-delivery-quiz-1"
  lessonSlug="03-progressive-delivery"
  lang="en"
  question="In progressive delivery, what is the difference between deploying and releasing, and what does a feature flag decouple?"
  choices={[
    { label: "Deploying puts the code on servers; releasing directs user traffic to it. A feature flag decouples the release (turning behaviour on for users) from the deploy, so you can expose or kill it via config with no redeploy.", correct: true },
    { label: "They are the same thing — once code is deployed it is released, and a feature flag just toggles environments", misconception: "Deploy and release are deliberately separate switches. Deploying means the binary is running on servers; releasing means user traffic is being directed to it. They are decoupled precisely so you can ship code dark and expose it later. A feature flag toggles behaviour for users, not environments." },
    { label: "Deploying directs traffic; releasing builds the artifact. A feature flag decouples the build from the test stage.", misconception: "This inverts the terms. Releasing directs user traffic; deploying puts the code on servers. Building the artifact happened earlier in CI. A feature flag decouples turning behaviour on for users from the deploy, acting as a redeploy-free release switch and kill switch." },
    { label: "A feature flag decouples nothing at runtime; it only changes which branch CI builds", misconception: "A feature flag is a runtime decision evaluated in the application: the new code is deployed everywhere but stays dark until the flag opens it, per-user or per-percentage, with no rebuild. That is the whole point — it decouples release from deploy so you flip behaviour via config." },
  ]}
/>

<Quiz
  id="ci-cd-04-release-automation-03-progressive-delivery-quiz-2"
  lessonSlug="03-progressive-delivery"
  lang="en"
  question="A serialization bug only manifests under real concurrent writes; a 100% big-bang deploy of it took the whole site down. Which rollout would have contained it, and on what signal?"
  choices={[
    { label: "A canary at a small percent (1-5%) with an automated analysis query that aborts and rolls back on an error-rate / SLO-burn breach", correct: true },
    { label: "A bigger staging test suite run before the same 100% deploy", misconception: "Staging cannot reproduce the real concurrency that triggers this bug, so more staging tests would still pass and the 100% deploy would still take everyone down. The fix is not to test harder before a full deploy — it is to limit the blast radius of the release itself with a canary and a metric gate." },
    { label: "A rolling update, which would have caught it because it replaces instances in batches", misconception: "A rolling update has no explicit health gate watching error rate before each batch, so the bad version reaches users incrementally and keeps rolling forward. It controls availability, not risk. Containment needs a canary analysis that aborts on a metric breach, which a plain rolling update does not provide." },
    { label: "Blue-green, because the smoke test on GREEN would have caught the concurrency bug before the flip", misconception: "A smoke test on the idle GREEN environment runs without real production concurrency, so it would not surface a bug that only fires under simultaneous writes — and the blue-green flip then sends 100% of traffic over at once. You need a slice of real traffic plus a metric gate, i.e. a canary." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="The metric-gated canary rollout. Step 1: deploy the new version dark — it is running on servers but receiving no user traffic. Step 2: shift 5% of real production traffic to the canary. Step 3: an analysis query compares the canary's error rate and p99 latency against the threshold. Branch on the result: if healthy, ramp the traffic weight 25 then 50 then 100%, fully promoting the new version; if the metric breaches the threshold, abort and roll back, shifting all traffic to the stable version with no human in the loop. The abort branch is what bounds the blast radius to the 5% slice."
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "dark", label: "deploy dark", sub: "0% traffic" },
    { id: "canary", label: "canary 5%", sub: "real traffic" },
    { id: "gate", label: "analyze", sub: "err / p99 vs threshold" },
    { id: "ramp", label: "ramp 25/50/100", sub: "healthy" },
    { id: "abort", label: "abort + rollback", sub: "breach" },
  ]}
  edges={[
    { from: "dark", to: "canary", label: "shift 5%" },
    { from: "canary", to: "gate", label: "measure" },
    { from: "gate", to: "ramp", label: "healthy" },
    { from: "gate", to: "abort", label: "breach" },
  ]}
  caption="Deploy dark, then send 5% of real traffic to the canary and let an analysis query decide: healthy ramps to 100%, a metric breach auto-aborts back to stable. The gate is the whole point — the blast radius of a bad version is bounded to the 5% slice, no human required."
/>

<KeyTakeaway>Deploy is not release: ship dark, expose gradually. Canary 1-5% with metric-gated auto-rollback bounds a bad version to a thin slice; blue-green gives instant flip-back; a feature flag is a redeploy-free kill switch.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="ci-cd-04-release-automation-03-progressive-delivery-retrieval"
  lang="en"
  questions={[
    { q: "Explain the deploy-vs-release framing and contrast the four rollout strategies (big-bang, rolling, blue-green, canary) by downtime, blast radius, and rollback.", a: "Deploying is putting new code on servers — the binary is running; releasing is directing user traffic to that code. Decoupling them is the whole game: ship the code dark (deployed, no traffic), then turn the traffic dial up gradually while watching signals, ready to turn it down. Big-bang (recreate) stops the old version and starts the new one: there is a downtime gap and 100% blast radius instantly, with rollback being a slow redeploy of the old version — acceptable only for tiny/internal apps. Rolling replaces instances batch by batch (k8s default): no downtime, but two versions run at once so they must be compatible, and a bad version still reaches users incrementally with no explicit health gate — it controls availability, not risk. Blue-green stands up two full environments, deploys to the idle GREEN, smoke-tests it, then flips the router from BLUE to GREEN atomically: instant cutover and instant flip-back, at the cost of ~2x infra during the switch and the hard problem of a shared database both versions must be compatible with. Canary routes a small percent (1-5%) of real traffic to the new version, watches error rate / latency / business metrics versus the baseline, and ramps 5 to 25 to 50 to 100% if healthy or aborts if not — it catches problems with real production traffic at a tiny blast radius, which is the bug class staging misses." },
    { q: "What does a feature flag decouple and what is its cost, and how does automated metric-gated rollback turn the Hook's incident into a non-event?", a: "A feature flag moves the release decision into the application layer: you wrap the new behaviour in a conditional, deploy it with the flag OFF so the code ships dark, then flip the flag for 1% then 10% then 100% of users independently of any deploy — the code was already on every server, you are only changing a config value. That gives two properties: an instant kill switch (flip back to OFF and every user is on the old path in seconds, no redeploy) and per-user / percentage targeting that powers A/B tests and dark launches. The cost is flag debt: every flag is a code branch plus a config row, and stale uncleaned flags become a combinatorial mess — flags are a temporary lever you must remove after learning. Automated rollback is the senior layer: a controller (Argo Rollouts, Flagger, or pipeline logic) ramps the canary and at each step runs an analysis query against metrics — error rate, p99, SLO burn — and if it breaches a threshold it auto-aborts and shifts traffic back to stable with no human in the loop; the decision is gated on metrics, not vibes. Run the Hook's serialization bug through it: the bad version goes to 5%, the bug fires under real concurrency, the analysis query sees the canary's error rate spike within seconds and aborts back to stable. Blast radius: 5% of users for seconds instead of 100% for minutes — the bug still happened, progressive delivery just made it a non-event." },
  ]}
/>

<Recap lang="en">
Deploying code is not releasing it: deploying puts the binary on servers, releasing directs user traffic to it, and the senior move is to treat them as two switches — ship dark, then expose gradually while watching signals, ready to pull back fast. The four rollout strategies trade off downtime against blast radius: big-bang stops the old and starts the new with a downtime gap and 100% blast radius (only ok for tiny/internal apps); rolling replaces instances batch by batch with no downtime but two compatible versions live and no explicit gate, controlling availability not risk; blue-green stands up two full environments and flips the router from BLUE to GREEN atomically for instant cutover and instant flip-back, at ~2x infra and the hard problem of a shared database; canary routes 1-5% of real traffic to the new version, watches error rate / p99 / business metrics against the baseline, and ramps 5/25/50/100% if healthy or aborts if not, catching the bugs staging cannot reproduce at a tiny blast radius. Feature flags decouple release from deploy in the application layer — deploy with the flag OFF, then flip it per-user or per-percentage with no redeploy — giving an instant kill switch and A/B/dark-launch targeting, at the cost of flag debt you must clean up. The senior version is automated: a controller ramps the canary and runs an analysis query on error rate / p99 / SLO burn, auto-aborting and rolling back to stable on a metric breach with no human in the loop — the decision is gated on metrics, not vibes. A 5% canary with that gate would have caught the Hook's serialization bug at 5% blast radius for seconds, instead of a 100% big-bang taking the whole site down for minutes. Now when you see a deploy script that pushes to 100% in one shot, you'll know which lever is missing — and what it would have cost to have it.
</Recap>
