---
concepts:
- build-once-promote-many
- immutable-artifacts
- digest-pinning
- environment-promotion
- config-per-environment
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: senior
order: 5
prereqs: []
slug: "05-artifact-promotion-and-environments"
sources:
- https://12factor.net/build-release-run
- https://docs.github.com/en/actions/deployment/targeting-different-environments
spiral: []
status: ready
summary: 'Build the artifact exactly once and promote that same immutable, digest-pinned image through dev to staging to prod — never rebuild per environment. A rebuild from the same commit is not the same artifact; config is injected at runtime, and prod is gated behind approval.'
title: 'Build once, promote everywhere: artifacts and environments'
track: ci-cd
unit: 04-release-automation
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Staging was green for three days. The release engineer kicked off the prod deploy, which — as the pipeline had always done — ran its own `docker build` from the very same commit SHA that staging had built. Same Dockerfile, same commit, same green tests. Prod fell over within ninety seconds. The postmortem found the difference: in the hours between the staging build and the prod build, a transitive dependency had published a new patch release, and the prod image resolved it where staging had pinned the older one. The two images were built from identical source and were not the same binary. "It passed staging" was a sentence about an artifact that no longer existed. The second twist of the knife came during rollback: the deploy targeted `:latest`, so rolling back "to the previous image" pulled a `:latest` that had since been re-pushed — not the previous image at all. This lesson is about the one discipline that makes both of those impossible: build the artifact once, and promote that exact artifact everywhere.
</Hook>

<Crux>Build the artifact once; promote that same digest-pinned image through environments. Inject config at runtime — never rebuild per env.</Crux>

<Explanation>

## Build once, promote many

In ten minutes you'll see exactly why the opening incident was inevitable — and why one line in your pipeline prevents it forever.

The principle is from the 12-factor build/release/run separation: a **build** turns source into one immutable artifact (a container image, a package), a **release** binds that artifact to a specific config, and **run** executes the release. The artifact is produced **exactly once**, then the *same bytes* move through environments — dev, then staging, then prod. You never run a second `build` for another environment.

The reason is the whole point of having a staging environment. If prod rebuilds, then the binary you tested in staging and the binary you ship to prod are two different builds — and "it passed staging" is now a claim about a different artifact. Promotion makes the guarantee literal: the bytes you tested are the bytes you run.

```bash
# WRONG — each environment rebuilds, so each runs a (potentially) different binary
# staging job
docker build -t app:staging .      # build #1
# prod job (hours later)
docker build -t app:prod .         # build #2 — NOT the same image as build #1
```

```bash
# RIGHT — build ONCE, capture the digest, promote that exact image everywhere
# build job (runs one time)
docker build -t registry/app:$GIT_SHA .
docker push registry/app:$GIT_SHA
DIGEST=$(docker inspect --format='{{index .RepoDigests 0}}' registry/app:$GIT_SHA)
# DIGEST is now registry/app@sha256:abc123...  — promote THIS, never rebuild
```

## Pin by digest, not by tag

When you write `image: registry/app:latest` in a deploy manifest, ask yourself: what, exactly, is that? The answer changes every time someone re-pushes the tag — and that mutability is the second failure in the opening story.

Promotion is only meaningful if "the same artifact" is unambiguous. A **tag** like `:latest` or even `:v1.2.3` is a *mutable pointer*: it can be re-pushed to point at a different image at any time. `:latest` is the classic footgun — two deploys of `:latest` minutes apart can run two different images, and there is no record that anything changed. Even a semver tag can be force-pushed over.

The fix is to reference the artifact by its **immutable content digest** — `registry/app@sha256:...` — which is a hash of the image content itself. A digest cannot be re-pointed: `sha256:abc` *is* that image, byte for byte, forever. Deploy the digest, record the digest in the release, and a given release is reproducible and auditable down to the bit.

```yaml
# Deploy manifest references the IMMUTABLE digest, not a floating tag
spec:
  containers:
    - name: app
      # NOT image: registry/app:latest   (mutable — can be re-pushed under you)
      # NOT image: registry/app:v1.2.3    (still a re-pushable tag)
      image: registry/app@sha256:abc123def456...   # immutable: this exact image, always
```

## Config per environment, not artifact per environment

The same artifact runs in every environment; only the **config** differs. Database URLs, feature flags, secrets, and tenant settings are injected at **runtime** — environment variables, mounted secrets, config maps — not baked into the image. This is the "config in the environment" rule of 12-factor, and it is what makes one artifact serve all environments.

Baking environment config into the image breaks promotion two ways: it forces a rebuild per environment (because the prod image must differ from the staging image), and it leaks prod secrets into an artifact that lives in a registry. The same digest plus a per-environment config is the only shape that keeps the artifact immutable and the secrets out of it.

<Inset kind="why" lang="en">
Why is "rebuild from the same git commit for prod" not equivalent to promoting the staging artifact? Because a build is not a pure function of the source commit. Transitive dependency versions, base-image digests, build-tool versions, embedded timestamps, and network-fetched layers can all differ between two builds of the same commit minutes apart — exactly the drift that crashed prod in the opening story. So a rebuild can produce a different binary than the one you tested, defeating the entire purpose of staging. Promoting the exact artifact by digest ships the tested bytes; rebuilding ships a hopeful approximation that happened to share a commit hash.
</Inset>

## Promotion and gates

The artifact moves through a gated path: **dev** auto-deploys on build, **staging** auto-deploys and runs integration tests, **prod** requires a manual approval. GitHub Actions models this with *environments* that carry required reviewers and wait timers — the prod environment simply won't deploy until a named approver signs off. On approval, the pipeline promotes the already-tested artifact; it does not rebuild.

GitOps tools (Argo CD, Flux) make the promotion itself a **git commit**: a deploy is a change to the environment's manifest that moves the pinned digest. That makes every promotion declarative, reviewable in a PR, auditable in git history, and revertible by reverting the commit — the rollback is `git revert`, which restores the *exact* previous digest, sidestepping the `:latest` rollback trap entirely.

<FlowDiagram
  label="The build-once, promote-many pipeline. A single build job produces one immutable image identified by its content digest, registry/app@sha256. That exact same digest is then promoted through three environments in sequence: dev deploys automatically, staging deploys automatically and runs the integration test suite, then a manual approval gate (required reviewer) guards prod, which deploys the identical digest. No environment rebuilds the artifact; each only re-points its manifest at the same sha256 and supplies its own runtime config — env vars, secrets, config maps — so the one tested binary runs everywhere with environment-specific settings injected at run time."
  hue="var(--d-systems)"
  perRow={3}
  nodes={[
    { id: "build", label: "build once", sub: "app@sha256" },
    { id: "dev", label: "dev", sub: "auto deploy" },
    { id: "staging", label: "staging", sub: "auto + tests" },
    { id: "gate", label: "approval gate", sub: "required reviewer" },
    { id: "prod", label: "prod", sub: "same digest" },
  ]}
  edges={[
    { from: "build", to: "dev", label: "promote digest" },
    { from: "dev", to: "staging", label: "same sha256" },
    { from: "staging", to: "gate", label: "tests pass" },
    { from: "gate", to: "prod", label: "on approval" },
  ]}
  caption="One artifact (app@sha256), N environments. The same immutable digest is promoted dev to staging to prod; config is injected per environment at runtime; prod is gated behind a manual approval — no environment ever rebuilds."
/>

<TradeoffMatrix
  id="ci-cd-04-release-automation-05-artifact-promotion-and-environments-tradeoff-1"
  lessonSlug="05-artifact-promotion-and-environments"
  lang="en"
  prompt="You must move a tested release from staging to prod with confidence that prod runs the exact same code that passed staging. How do you ship it?"
  options={[
    { name: "Promote the same immutable artifact by digest (registry/app@sha256:...), inject config per environment at runtime, and gate prod behind a required-reviewer approval", summary: "Promote the tested bytes.", correct: true, justification: "This is the build-once, promote-many discipline. The artifact is built exactly once and referenced by its immutable content digest, so the bytes that passed staging are byte-for-byte the bytes that run in prod — the digest cannot be re-pointed. Per-environment config is injected at runtime, so one image serves all environments and no secret is baked in. The prod approval gate (GitHub Actions environment with a required reviewer) means a human signs off on promoting the already-tested artifact rather than triggering a rebuild. This is the only option that makes 'it passed staging' a true statement about prod." },
    { name: "Rebuild the image from the same git commit for the prod deploy", summary: "Same commit, different artifact.", justification: "This is exactly the opening incident. A build is not a pure function of the commit: transitive dependency patches, base-image digests, build-tool versions, and network-fetched layers can all differ between two builds of the same SHA minutes apart. The prod rebuild can resolve a dependency staging never saw, so you ship an untested binary that merely shares a commit hash with the one you tested. 'Same commit' is not 'same artifact' — promote the staging image by digest instead." },
    { name: "Deploy registry/app:latest to both staging and prod", summary: "Mutable tag, nondeterministic.", justification: "`:latest` is a mutable pointer that can be re-pushed at any time, so two deploys of `:latest` can run two different images with no record of the change — and a rollback 'to the previous image' pulls whatever `:latest` now points at, not the previous image. Staging and prod can silently diverge, and rollback is unreliable. Pin the immutable @sha256: digest so a release is reproducible and revertible." },
    { name: "Copy the staging image but retag it and rebuild it for prod hardening", summary: "Retag + rebuild breaks immutability.", justification: "Retagging is harmless, but rebuilding for prod re-introduces the exact drift the discipline prevents: the rebuilt image is a new artifact that was never tested in staging. Any 'hardening' that changes bytes must happen before the single build, not after promotion. If you change the bytes, you no longer shipped the tested artifact — promote the original digest untouched and inject any environment differences as config." },
  ]}
/>

<Quiz
  id="ci-cd-04-release-automation-05-artifact-promotion-and-environments-quiz-1"
  lessonSlug="05-artifact-promotion-and-environments"
  lang="en"
  question="Why build the artifact once and promote it through environments rather than rebuild it per environment?"
  choices={[
    { label: "Because a rebuild can differ from the tested binary (dependency drift, base-image changes), so only promoting the exact artifact ships the bytes you actually tested", correct: true },
    { label: "Because rebuilding per environment is slower, and the only cost of rebuilding is extra CI minutes", misconception: "Speed is a side benefit, not the point. The real reason is correctness: a build is not a pure function of the commit, so a prod rebuild can resolve different transitive dependencies or a new base-image digest than staging did. Even if rebuilds were instant, they would still ship a binary you never tested. Promotion is about guaranteeing the tested bytes are the run bytes, not about saving minutes." },
    { label: "Because Docker forbids building the same image twice and will refuse the second build", misconception: "Docker does no such thing — you can rebuild a Dockerfile as many times as you like, and that is precisely the danger: each rebuild is a fresh, potentially different artifact. Nothing stops you mechanically; the discipline of building once and promoting is what you impose to keep prod running the tested binary." },
    { label: "Because promotion lets each environment compile with environment-specific optimizations baked into separate images", misconception: "That is the opposite of promotion — baking environment-specific differences into separate images forces a rebuild per environment and means each runs a different binary. Promotion ships ONE artifact everywhere and injects environment differences as runtime config, never as separate builds." },
  ]}
/>

<Quiz
  id="ci-cd-04-release-automation-05-artifact-promotion-and-environments-quiz-2"
  lessonSlug="05-artifact-promotion-and-environments"
  lang="en"
  question="Why is deploying registry/app:latest dangerous, and what should you reference instead?"
  choices={[
    { label: "A tag is a mutable pointer that can be re-pushed, so :latest is nondeterministic; reference the immutable @sha256: content digest instead", correct: true },
    { label: ":latest is fine for prod as long as you also push a :v1.2.3 semver tag alongside it", misconception: "A semver tag like :v1.2.3 is still a mutable pointer — it can be force-pushed to point at a different image, so it carries the same nondeterminism as :latest. The only reference that cannot be re-pointed is the content digest, @sha256:. Pushing a semver tag is good for humans, but deploys and rollbacks should pin the digest." },
    { label: ":latest is dangerous only because it is a long string that is easy to typo; use a short tag instead", misconception: "The danger has nothing to do with string length. :latest is dangerous because it is mutable: two deploys of :latest can run different images and a rollback pulls whatever it now points at. A shorter tag is just as mutable. Immutability — the @sha256: digest — is what fixes it, not brevity." },
    { label: "It is dangerous because :latest always pulls the newest image, which is what you want in prod", misconception: "Wanting prod to auto-pull 'the newest image' is the bug, not the feature: it means prod silently changes whenever someone re-pushes :latest, with no review and no record. Prod should run a specific, pinned, immutable digest that only changes through a reviewed promotion — never a tag that drifts on its own." },
  ]}
/>

</Explanation>

<KeyTakeaway>Build once, promote that same image — pinned by @sha256: digest, not :latest — through dev, staging, prod. A rebuild from the same commit is not the same artifact. Inject config at runtime; gate prod.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="ci-cd-04-release-automation-05-artifact-promotion-and-environments-retrieval"
  lang="en"
  questions={[
    { q: "State the build-once, promote-many principle and explain why a rebuild from the same git commit is not equivalent to promoting the tested artifact.", a: "Build-once, promote-many comes from the 12-factor build/release/run separation: source is turned into one immutable artifact (a container image or package) exactly once, and that same artifact — the same bytes — is then promoted through environments dev to staging to prod. You never run a second build for another environment. The reason it's not equivalent to rebuilding from the same commit is that a build is NOT a pure function of the source commit: transitive dependency versions, base-image digests, build-tool versions, embedded timestamps, and network-fetched layers can all differ between two builds of the same SHA minutes apart. So a prod rebuild can resolve a dependency or a base image that staging never tested, producing a different binary that merely shares a commit hash. That defeats the entire purpose of staging — 'it passed staging' becomes a claim about an artifact that no longer runs anywhere. Promoting the exact artifact by digest ships the tested bytes; rebuilding ships a hopeful approximation. The discipline makes the guarantee literal: the bytes you tested are the bytes you run." },
    { q: "Explain digest pinning versus tags, where environment config lives, and how prod gets gated in a build-once promotion pipeline.", a: "A tag like :latest or :v1.2.3 is a mutable pointer — it can be re-pushed to point at a different image at any time, so two deploys of :latest can run different images with no record, and a rollback 'to the previous image' pulls whatever :latest now points at rather than the actual previous image. The fix is to reference the artifact by its immutable content digest, registry/app@sha256:..., which is a hash of the image content and cannot be re-pointed: sha256:abc IS that image, byte for byte, so a release is reproducible and auditable. Environment config does NOT live in the artifact: the same image runs everywhere and database URLs, flags, and secrets are injected at runtime as env vars, mounted secrets, or config maps (12-factor 'config in the environment'); baking config into the image would force a rebuild per environment and leak prod secrets into the registry. Prod is gated by a manual approval: GitHub Actions environments carry required reviewers and wait timers so the prod deploy won't proceed until an approver signs off, and on approval the pipeline promotes the already-tested digest rather than rebuilding. With GitOps (Argo CD/Flux) the promotion is itself a git commit moving the pinned digest in the env manifest — declarative, reviewable, auditable, and revertible by reverting the commit, which restores the exact previous digest and avoids the :latest rollback trap." },
  ]}
/>

<Recap lang="en">
The discipline is build once, promote many: a build turns source into one immutable artifact exactly once, and that same artifact — the same bytes — is promoted through dev, staging, then prod, never rebuilt per environment. Rebuilding is the trap, because a build is not a pure function of the commit: transitive dependency patches, base-image digests, and build-tool versions can differ between two builds of the same SHA, so a prod rebuild can ship a binary staging never tested even though it shares a commit hash. Promotion is only meaningful if the artifact is unambiguous, so you pin by immutable content digest (registry/app@sha256:...) rather than a mutable tag like :latest or :v1.2.3, which can be re-pushed to point at a different image and silently breaks both deploys and rollbacks. The same artifact serves every environment because config — database URLs, flags, secrets — is injected at runtime (12-factor config in the environment), never baked into the image, which would force per-environment rebuilds and leak secrets into the registry. The artifact moves through a gated path: dev auto-deploys, staging auto-deploys and runs integration tests, and prod sits behind a manual approval (GitHub Actions environments with required reviewers and wait timers) that promotes the already-tested digest rather than rebuilding. GitOps makes each promotion a git commit that moves the pinned digest in the environment manifest, so every deploy is declarative, reviewable, auditable, and revertible by reverting the commit — which restores the exact previous digest and sidesteps the :latest rollback trap entirely. Now when you see a pipeline with two `docker build` steps — one for staging, one for prod — you'll know why "it passed staging" is no longer a guarantee, and exactly which single build step to remove.
</Recap>
