---
concepts:
- semver
- conventional-commits
- changesets
- release-please
- automated-changelog
- breaking-change
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-semver-and-changelogs
sources:
- https://semver.org/
- https://www.conventionalcommits.org/en/v1.0.0/
- https://github.com/changesets/changesets
- https://github.com/googleapis/release-please
spiral: []
status: ready
summary: SemVer encodes a contract — MAJOR breaks consumers, MINOR adds compatibly,
  PATCH fixes compatibly. Conventional commits make that intent machine-readable so
  tools compute the bump and generate the changelog. Forget to mark a break and it
  ships as a MINOR.
title: 'SemVer, conventional commits, and automated changelogs'
track: ci-cd
unit: 04-release-automation
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A library ships `2.4.0` on a Friday. It is a one-word rename — `parse()` now returns `null` instead of throwing on bad input, "a tiny improvement." Over the weekend, three downstream services that wrapped `parse()` in a `try/catch` silently start writing `null` rows to the database, because the throw that their error path depended on never fires. The change went out as a MINOR, so every consumer's `^2.0.0` range auto-upgraded into it. The author's defence: "I didn't remove anything." But behaviour *is* the contract. A break that wasn't marked as a break shipped to everyone who trusted the version number.
</Hook>

<Crux>SemVer is a breakage contract; conventional commits encode each change's intent so a tool computes the bump and writes the changelog.</Crux>

<Explanation>

## SemVer is a promise about what can break

Why does the version number exist at all? Because your consumers automate upgrades — `^2.0.0` silently pulls every MINOR and PATCH. If you mislabel a break, it ships to everyone who trusted you. That contract is the whole point of this section.

A version `MAJOR.MINOR.PATCH` is not a changelog of effort — it is a compatibility contract aimed at the consumer. SemVer 2.0.0 fixes the meaning of each field precisely:

- **MAJOR** — you made a *backward-incompatible* change. Any consumer-observable break counts: a removed function, a renamed field, a narrowed input range, a changed default, or — as in the hook — a changed *behaviour* their code relied on. If existing correct usage can now break, it is MAJOR.
- **MINOR** — you added functionality in a backward-compatible way. New endpoint, new optional argument, new return field. Old code keeps working untouched.
- **PATCH** — backward-compatible bug fix only. The contract is unchanged; you made it honour what it already promised.

The subtle, senior point is that "break" means *consumer-observable contract*, not "deleted code." Tightening validation, changing an error type, altering output ordering a consumer parsed — all can be breaking even though nothing was removed. The author who says "I didn't remove anything" has the wrong mental model.

Two corners matter in practice. **`0.y.z` is initial development**: under SemVer the public API is explicitly *not* stable, so anything MAY change in any release — pre-1.0 you cannot lean on MINOR/PATCH meaning anything, which is why "0ver" projects (versions that never reach `1.0.0` precisely to dodge the MAJOR promise) frustrate consumers. And **pre-release / build metadata** extends the format: `1.4.0-rc.1` (a release candidate that sorts *before* `1.4.0`) and `1.4.0+build.42` (build metadata, ignored in precedence) let you publish previews without spending the real number.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Change you made</th>
        <th class="border border-border px-3 py-2 text-left">Conventional commit</th>
        <th class="border border-border px-3 py-2 text-left">Bump (from 2.4.1)</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Fix a crash on empty input</td>
        <td class="border border-border px-3 py-2"><code>fix: handle empty input</code></td>
        <td class="border border-border px-3 py-2">PATCH → <code>2.4.2</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Add an optional <code>timeout</code> arg</td>
        <td class="border border-border px-3 py-2"><code>feat: add timeout option</code></td>
        <td class="border border-border px-3 py-2">MINOR → <code>2.5.0</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Rename a public field</td>
        <td class="border border-border px-3 py-2"><code>feat!: rename id to uuid</code></td>
        <td class="border border-border px-3 py-2">MAJOR → <code>3.0.0</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Change <code>parse()</code> to return null vs throw</td>
        <td class="border border-border px-3 py-2"><code>fix: ...</code> + <code>BREAKING CHANGE:</code> footer</td>
        <td class="border border-border px-3 py-2">MAJOR → <code>3.0.0</code></td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Reword a doc comment</td>
        <td class="border border-border px-3 py-2"><code>docs: clarify return type</code></td>
        <td class="border border-border px-3 py-2">no release</td>
      </tr>
    </tbody>
  </table>
</div>

## Conventional commits: the machine-readable signal

The gap between "I know this is breaking" and "the tooling knows" is bridged by [Conventional Commits](https://www.conventionalcommits.org/en/v1.0.0/) — a tiny grammar on the commit subject and footer that a parser can read. The contract:

```text
fix: a bug fix                          → PATCH
feat: a new feature                     → MINOR
feat!: a feature that breaks the API    → MAJOR   (the ! marks the break)

# or, the same break expressed in a footer:
refactor: drop the legacy code path

BREAKING CHANGE: parse() now returns null instead of throwing.
                 Wrap callers that relied on the throw.
```

Two ways to signal MAJOR, and both must be present in the message for the tool to see them: a `!` after the type/scope (`feat!:`, `fix(api)!:`) **or** a `BREAKING CHANGE:` footer (note the literal token &#123;BREAKING CHANGE&#125; with the space, not a hyphen). Types that touch no shipped behaviour — `docs:`, `chore:`, `test:`, `style:`, `ci:` — produce no version bump at all. This is the load-bearing convention of the whole unit: the bump is *computed from the commit type*, so an unmarked break is invisible to the machine and ships at the wrong level.

<Inset kind="why" lang="en">
Why a footer *and* a `!`? The `!` is a fast visual flag in the subject line and works even when the body is stripped; the `BREAKING CHANGE:` footer gives you room to *describe* the migration. Convention is to use both for anything load-bearing — `feat!:` in the subject so a one-line squash still carries the signal, and a footer so the generated changelog has a real "what to change in your code" paragraph. The footer alone is the trap: it lives in the commit body, and a squash-merge that keeps only the subject line silently drops it.
</Inset>

## Two tools, two philosophies: declare vs infer

Once commits carry intent, a tool turns history into a release. The two dominant choices sit at opposite ends of an explicit-vs-automatic axis.

[**changesets**](https://github.com/changesets/changesets) makes intent *explicit and per-PR*. When you finish work you run `changeset add`, pick the affected packages, choose the bump, and write a human summary; it drops a markdown file into `.changeset/` that travels with your branch and goes through code review. At release time `changeset version` consumes every pending file, computes each package's new version, and writes the changelog; `changeset publish` ships them. Because the contributor *declares* the bump in a reviewed file, it is excellent for monorepos — it understands cross-package dependency bumps — and it never has to guess from commit text.

```markdown
---
"@acme/parser": major
"@acme/cli": patch
---

parse() now returns null on bad input instead of throwing.
Callers that relied on the throw must add an explicit null check.
```

[**release-please**](https://github.com/googleapis/release-please) makes intent *inferred and automatic*. A bot parses your conventional commits on `main`, computes the next version, and maintains a standing "release PR" whose diff is the bumped version files plus a generated `CHANGELOG.md`. The PR stays continuously up to date as more commits land; when you merge it, release-please tags the release and publishes. You write no intent files — the commit *is* the intent — which is frictionless until a commit is mislabelled, because the bot believes the commit, not your intentions.

<TradeoffMatrix
  id="01-semver-and-changelogs-tradeoff-1"
  lessonSlug="01-semver-and-changelogs"
  lang="en"
  prompt="A monorepo of ~12 published packages needs automated, reviewable releases. Several packages depend on each other, and contributors squash-merge PRs. Pick the release mechanism."
  options={[
    { name: "changesets: each PR adds a reviewed .changeset/ markdown declaring the per-package bump", summary: "Explicit intent, monorepo-aware.", correct: true, justification: "Right for a multi-package repo with inter-package dependencies and squash-merge. The contributor declares the bump in a markdown file that goes through code review, so intent is captured at PR time and survives squash (it lives in a tracked file, not the commit message). changesets understands the dependency graph and bumps dependents correctly, and the declared bump cannot be silently mis-inferred from a stripped commit body. The cost is one manual step per PR — changeset add — and a CI check that fails PRs missing a changeset, which is exactly the discipline a 12-package release needs." },
    { name: "release-please: a bot parses conventional commits on main and maintains a release PR per package", summary: "Automatic, zero intent files.", justification: "Excellent ergonomics — the commit is the intent, no extra files — and it does support a manifest mode for monorepos. But it depends entirely on every merged commit being correctly typed, which is brittle precisely under squash-merge: a single-line squash can drop a BREAKING CHANGE footer and ship a MAJOR as a MINOR. For a small app with disciplined commits it is a great default; for a 12-package repo where a mislabel silently corrupts a version, the explicit-declaration model is safer." },
    { name: "Manual: a maintainer edits package.json and hand-writes CHANGELOG.md each release", summary: "Full control, no tooling.", justification: "Maximum control and zero tooling to learn, but it does not scale and it drifts: hand-written changelogs fall out of sync with what actually shipped, and a tired maintainer picks the wrong bump or forgets a package. Fine for a single tiny library released rarely; a liability across 12 interdependent packages where consistency is the whole point." },
    { name: "Auto-bump PATCH on every merge to main, ignore commit types entirely", summary: "Always +0.0.1.", justification: "This throws away the entire signal SemVer exists to carry. Shipping every change — including breaking ones — as a PATCH tells consumers nothing can break, so their ^ ranges auto-upgrade straight into incompatibilities. It is the hook's failure mode made into policy: the version number stops meaning anything." },
  ]}
/>

## The failure mode: an unmarked break ships as a MINOR

Everything above exists to prevent one specific incident, and it is the most common one in release automation. A contributor makes a breaking change but writes `feat: improve parse()` — no `!`, no footer. The tool dutifully computes a MINOR, because that is what the commit *says*. The new version flows out, and every consumer pinned to a caret range (`^2.0.0`, the npm default) auto-upgrades into the break without a single human deciding to. The version number lied, and the contract that consumers trusted to gate breaking changes never fired.

Squash-merge is the amplifier. If the break was signalled *only* in a `BREAKING CHANGE:` footer (in the commit body) and the PR is squash-merged with just the subject line kept, the footer evaporates and the tool sees a plain `feat:` — MINOR again. The defences are concrete: enforce the convention with `commitlint` in CI so an unparseable or untyped commit fails the check; require a `feat!:`-style subject marker (not only a footer) for breaks so a squash still carries it; or use changesets, where the bump lives in a reviewed file that a squash cannot strip. And generate the changelog from the same signal — a human-written `CHANGELOG.md` drifts from reality the moment someone forgets to update it; a generated one is, by construction, exactly what the commits said shipped.

<Quiz
  id="01-semver-and-changelogs-quiz-1"
  lessonSlug="01-semver-and-changelogs"
  lang="en"
  question="You change a function so it returns null on bad input instead of throwing — existing callers relied on the throw. From 2.4.1, what is the correct bump, and how do you signal it?"
  choices={[
    { label: "MAJOR → 3.0.0; mark it with feat!: / fix!: or a BREAKING CHANGE: footer, because changed behaviour existing callers depended on is a consumer-observable break", correct: true },
    { label: "MINOR → 2.5.0; it adds a new behaviour without deleting any code", misconception: "MINOR is for backward-compatible additions only. You didn't add an option — you changed behaviour that correct existing callers depended on (the throw), which breaks them. 'I didn't delete anything' is the wrong test; the test is whether existing correct usage can now break." },
    { label: "PATCH → 2.4.2; returning null instead of crashing is a bug fix", misconception: "PATCH must be backward-compatible. Callers wrapping the function in try/catch now silently get null instead of an error, corrupting their flow — that is a contract break, not a transparent fix, regardless of intent." },
    { label: "No bump; behaviour changes aren't covered by SemVer", misconception: "SemVer is about the consumer-observable contract, and behaviour is part of that contract. A behavioural change that breaks correct existing usage is exactly what MAJOR is for." },
  ]}
/>

<Quiz
  id="01-semver-and-changelogs-quiz-2"
  lessonSlug="01-semver-and-changelogs"
  lang="en"
  question="A breaking change was signalled only in a BREAKING CHANGE: footer in the commit body. The PR is squash-merged keeping just the subject 'feat: improve parser'. release-please bumps MINOR. Why, and what prevents it?"
  choices={[
    { label: "The squash kept only the subject and dropped the body, so the footer the tool reads for breaks vanished; put the marker in the subject (feat!:) and/or enforce commitlint, or use changesets where the bump is a tracked file", correct: true },
    { label: "release-please ignores BREAKING CHANGE footers entirely and only reads the ! marker", misconception: "release-please does honour the BREAKING CHANGE: footer — when it's present. The problem here is upstream: the squash-merge discarded the commit body, so by the time the bot parses main, the footer no longer exists to be read." },
    { label: "MINOR is correct; a footer-only break is just a feature", misconception: "A break is a break regardless of how it's signalled; MINOR shipping it auto-upgrades caret-pinned consumers into the break. The signalling failure (footer lost in squash) is the bug, not a reason the MINOR is right." },
    { label: "Nothing prevents it; squash-merge is fundamentally incompatible with conventional commits", misconception: "Squash and conventional commits coexist fine if the breaking signal lives where the squash keeps it — the subject line (feat!:) — or in a reviewed changeset file. The fix is to not rely on a body-only footer surviving a squash." },
  ]}
/>

<DragOrder
  id="01-semver-and-changelogs-drag-1"
  lessonSlug="01-semver-and-changelogs"
  lang="en"
  prompt="Order the steps of a commit-driven automated release (from the developer's keystroke to a tagged release):"
  items={[
    { id: "a", label: "Developer writes a conventional commit (feat: / fix: / feat!:) capturing the intent" },
    { id: "b", label: "Tool analyses commits since the last release and classifies each by type" },
    { id: "c", label: "Tool computes the next version: highest of PATCH (fix) / MINOR (feat) / MAJOR (break)" },
    { id: "d", label: "Tool generates the CHANGELOG entry from the classified commits" },
    { id: "e", label: "Maintainer merges the release PR → version is tagged and the release published" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<FlowDiagram
  label="A commit-driven release pipeline: a conventional commit is analysed, classified into a version bump, turned into a changelog, and surfaced as a release PR or tag"
  hue="var(--d-systems)"
  perRow="5"
  nodes={[
    { id: "commit", label: "Commit", sub: "feat / fix / feat!" },
    { id: "analyze", label: "Analyse", sub: "parse types" },
    { id: "bump", label: "Compute bump", sub: "PATCH/MINOR/MAJOR" },
    { id: "changelog", label: "Changelog", sub: "generated" },
    { id: "release", label: "Release PR / tag", sub: "merge → publish" },
  ]}
  edges={[
    { from: "commit", to: "analyze", label: "read msg" },
    { from: "analyze", to: "bump", label: "classify" },
    { from: "bump", to: "changelog", label: "summarise" },
    { from: "changelog", to: "release", label: "open PR" },
  ]}
  caption="The commit's type is the signal that drives everything downstream: analyse classifies each commit, the bump is the highest level seen (a single break forces MAJOR), the changelog is generated from the same classification, and merging the release PR tags and publishes. Mislabel the commit and the wrong version ships — the chain only ever knows what the commit said."
/>

<KeyTakeaway>SemVer is a consumer contract: MAJOR breaks, MINOR adds compatibly, PATCH fixes — changed *behaviour* counts as a break. Conventional commits (`feat!:`) make that machine-readable; an unmarked break ships as MINOR.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-semver-and-changelogs-retrieval"
  lang="en"
  questions={[
    { q: "Map fix / feat / feat! to their SemVer bumps, and explain why 'I didn't delete anything' is the wrong test for whether a change is breaking.", a: "fix: is a backward-compatible bug fix → PATCH (2.4.1 → 2.4.2). feat: is a backward-compatible new feature → MINOR (2.4.1 → 2.5.0). feat!: (or any type with a ! after the type/scope, or a BREAKING CHANGE: footer) is a backward-incompatible change → MAJOR (2.4.1 → 3.0.0). The wrong test is 'did I remove code', because SemVer is about the consumer-observable contract, not your diff. Tightening input validation, changing an error type, altering output ordering a consumer parsed, changing a default, or — classically — making a function return null instead of throwing are all breaks even though nothing was deleted: existing correct usage can now fail. The correct test is 'can code that used the old version correctly now break'. If yes, it is MAJOR, and it must be signalled so a tool bumps MAJOR and consumers' caret ranges do not auto-upgrade into it." },
    { q: "Contrast changesets and release-please, and describe the squash-merge failure they each survive or fall to.", a: "changesets is explicit and per-PR: the contributor runs changeset add and commits a reviewed markdown file in .changeset/ declaring each package's bump and a summary; at release time changeset version consumes them, computes versions (handling cross-package deps in a monorepo), and writes the changelog. release-please is automatic and inferred: a bot parses conventional commits on main, computes the next version, and maintains a standing release PR with a generated CHANGELOG; merging it tags and publishes. The squash-merge trap hits release-please hardest: if a break was signalled only in a BREAKING CHANGE: footer in the commit body and the PR is squashed to just its subject, the footer is lost and the bot sees a plain feat:, shipping a MAJOR as a MINOR. changesets survives this because the bump lives in a tracked, reviewed file that the squash cannot strip. Defences for the commit-driven path: put the marker in the subject (feat!:) so a squash keeps it, and enforce commitlint in CI." },
  ]}
/>

<Recap lang="en">
A version number is a contract aimed at the consumer, not a log of effort: MAJOR means you made a backward-incompatible change, MINOR a backward-compatible addition, PATCH a backward-compatible fix — and the senior point is that "break" means any consumer-observable contract change, including changed behaviour that correct existing callers relied on, not just deleted code. Remember the corners: `0.y.z` is initial development where nothing is promised, and `-rc.1` / `+build` extend the format for previews. Conventional commits make that intent machine-readable — `fix:` → PATCH, `feat:` → MINOR, `feat!:` or a `BREAKING CHANGE:` footer → MAJOR — so a tool can compute the bump and generate the changelog instead of a human guessing. changesets captures intent explicitly in a reviewed `.changeset/` file per PR, which is monorepo-aware and survives squash-merge; release-please infers it from commits and maintains a release PR, which is frictionless but only as correct as the commit labels. The incident the whole unit guards against is an unmarked break shipping as a MINOR — auto-upgrading every caret-pinned consumer into it — and its amplifier, a squash-merge that drops a body-only `BREAKING CHANGE:` footer. Put the break marker in the subject line, enforce it in CI, and generate the changelog from the same signal so it never drifts from what actually shipped. Now when you see a `feat:` commit touching error-handling behaviour, you'll pause and ask whether existing callers depended on the old behaviour — because that's exactly how an unmarked MAJOR ships as MINOR.
</Recap>
