---
concepts:
- ci-cd-gha-js-actions
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: senior
order: 2
prereqs: []
slug: 02-javascript-actions
sources:
- https://docs.github.com/en/actions/sharing-automations/creating-actions/creating-a-javascript-action
- https://docs.github.com/en/actions/sharing-automations/creating-actions/metadata-syntax-for-github-actions
- https://github.com/actions/toolkit
spiral: []
status: ready
summary: 'A JavaScript action runs as a node20 process on the runner with the @actions/toolkit for inputs, outputs, and the GitHub API. Its dependencies must be committed — bundled with ncc into one file — because runners do not run npm install for an action.'
title: 'JavaScript actions: the node20 runtime, the toolkit, and the ncc bundle'
track: ci-cd
unit: 09-actions-authoring
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The action worked on the author's laptop and in the test workflow, then failed for every consumer with `Cannot find module '@actions/core'`. The author had a correct `action.yml` pointing at `main: index.js`, an `index.js` that imported the toolkit, and a `package.json` listing the dependency — everything you would expect. What they did not have was `node_modules` committed to the repo, and they assumed the runner would `npm install` it the way their laptop did. It does not. A JavaScript action is handed to a node20 runtime and run *exactly as committed* — no install step, no package resolution. The fix was not more dependencies but fewer files that ship: run `ncc build` to compile `index.js` and its entire dependency tree into one `dist/index.js`, point `action.yml` at that, and commit it. The action that had silently depended on a build step nobody ran now carried its own runtime inside a single bundled file.
</Hook>

<Crux>A JavaScript action runs as a node20 process on @actions/toolkit; its deps must be committed, so authors bundle them into one ncc file.</Crux>

<Explanation>

## The node20 runtime and the toolkit

A JavaScript action declares `runs.using: "node20"` and `main:` pointing at an entry file. At call time GitHub Actions spawns a Node.js 20 process (the supported LTS runtime; older `node16`/`node12` are deprecated) and executes that file on the host runner — no container, so it runs on Linux, macOS, and Windows alike, and it boots in well under a second versus a Docker action's image pull. The contract between your code and the runner is the **`@actions/toolkit`** — a set of npm packages you call instead of poking at magic environment variables:

```js
import * as core from "@actions/core";
import * as github from "@actions/github";

async function run() {
  const token = core.getInput("github-token", { required: true });
  core.setSecret(token);                 // mask it in logs
  const label = core.getInput("label");

  const octokit = github.getOctokit(token);
  const { owner, repo } = github.context.repo;
  const pr = github.context.payload.pull_request;

  await octokit.rest.issues.addLabels({
    owner, repo, issue_number: pr.number, labels: [label],
  });

  core.setOutput("labeled", "true");     // available to later steps
}

run().catch((err) => core.setFailed(err.message));
```

`core.getInput` reads `action.yml` inputs (which arrive as `INPUT_*` env vars, but never read those directly); `core.setOutput` and `core.setFailed` are the right way to emit outputs and fail — `setFailed` sets the exit code so the step turns red. `github.context` hands you the parsed event payload, and `github.getOctokit` is a pre-authenticated API client. Reaching past the toolkit to parse `process.env.GITHUB_EVENT_PATH` yourself is the classic junior smell.

<Quiz
  id="02-javascript-actions-quiz-1"
  lessonSlug="02-javascript-actions"
  lang="en"
  question="A JavaScript action's index.js imports @actions/core. package.json lists it as a dependency, but the action fails on consumers' runners with 'Cannot find module @actions/core'. Why?"
  choices={[
    { label: "The runner runs the action exactly as committed and never runs `npm install` — node_modules isn't in the repo, so the import resolves to nothing", correct: true },
    { label: "@actions/core must be a devDependency, not a dependency", misconception: "The dependency type is irrelevant to the failure — the runner installs nothing either way. The real cause is that no node_modules is present and the runner does not install one; the dependency tree must ship in the repo." },
    { label: "node20 cannot import scoped packages like @actions/core", misconception: "node20 imports scoped packages fine — the toolkit is scoped and is the standard. The failure is purely that the package was never installed on the runner because runners skip npm install for actions." },
    { label: "The action.yml is missing `runs.using: \"docker\"`", misconception: "Switching to Docker is not the fix and changes the action's whole runtime. A JavaScript action just needs its dependencies committed — typically bundled with ncc — so the import resolves." },
  ]}
/>

## Why you bundle: the ncc problem

The runner does not run `npm install` for an action — it runs your `main:` file as-is. So the dependency tree has to be present in the repository. Committing a raw `node_modules` works but is ugly: hundreds of folders, thousands of files, a noisy diff on every dependency bump, and a real supply-chain surface to review. The standard answer is **`@vercel/ncc`**: it compiles your entry file and *all* of its `node_modules` into a single `dist/index.js`, tree-shaking unused code along the way. You point `action.yml` at `dist/index.js`, commit that one file, and `.gitignore` `node_modules`. A toolkit-based action that pulls in `@actions/core` plus `@actions/github` (which carries Octokit) bundles to a few hundred kilobytes to low single-digit megabytes — one file, deterministic, reviewable as a diff.

The discipline this imposes is real and a common bug source: **the committed bundle can drift from the source**. If you edit `index.js`, run tests, and forget `ncc build`, CI keeps running the *old* `dist/index.js` and your change silently does nothing. Teams guard this with a CI check that runs `ncc build` and fails if `git diff --exit-code dist/` is dirty — proving the committed bundle matches source. Two more senior notes: pin the runtime to `node20` (don't leave a deprecated version that GitHub will sunset), and for **supply-chain safety, consumers should pin your action by full commit SHA, not a tag** — `uses: org/labeler@<40-char-sha>` — because a tag like `@v1` is mutable and a compromised release could ship malicious code inside that bundled `dist/index.js` to everyone tracking the tag.

<Quiz
  id="02-javascript-actions-quiz-2"
  lessonSlug="02-javascript-actions"
  lang="en"
  question="A team edits a JavaScript action's source index.js, adds tests, and ships. The action's behavior in CI is unchanged — the new code never runs. The dist/ bundle is committed and action.yml points at it. What most likely happened?"
  choices={[
    { label: "They forgot to re-run `ncc build`, so the committed dist/index.js still holds the old compiled code that the runner actually executes", correct: true },
    { label: "node20 caches the previous bundle across runs and served the stale copy", misconception: "There is no cross-run node bundle cache doing this. The runner executes whatever dist file is committed; if ncc wasn't re-run, the committed bundle is simply stale relative to source." },
    { label: "The runner ran npm install and pulled an older published version", misconception: "Runners do not run npm install for actions at all — that is exactly why the bundle must be committed. The stale behavior comes from an un-rebuilt dist/, not a package fetch." },
    { label: "action.yml inputs changed and silently disabled the new logic", misconception: "Input changes would surface as missing/changed values, not as the old code running. The signature here — committed dist + unchanged behavior after a source edit — is the classic forgot-to-rebuild drift." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="A JavaScript action's authoring path: source through bundling to the runtime"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "src", label: "index.js + deps", sub: "@actions/toolkit" },
    { id: "ncc", label: "ncc build", sub: "tree-shake to 1 file" },
    { id: "dist", label: "dist/index.js", sub: "committed, ~KB-MB" },
    { id: "yml", label: "action.yml", sub: "using: node20, main:" },
    { id: "runner", label: "node20 process", sub: "<1s, any OS" },
    { id: "drift", label: "Stale dist", sub: "CI diff guard" },
  ]}
  edges={[
    { from: "src", to: "ncc", label: "bundle" },
    { from: "ncc", to: "dist", label: "commit" },
    { from: "dist", to: "yml", label: "main points here" },
    { from: "yml", to: "runner", label: "spawn" },
    { from: "src", to: "drift", label: "forgot rebuild" },
  ]}
  caption="Runners never run npm install, so the dependency tree must be committed — ncc bundles it into one file. The drift edge is the trap: edit source, skip ncc build, and the runner executes the old committed bundle."
/>

<KeyTakeaway>A JavaScript action runs as a `node20` process on the host runner — cross-OS, sub-second startup, no container — and talks to the runner through the `@actions/toolkit` (`core` for inputs/outputs/failure, `github` for the event context and a pre-auth Octokit) rather than raw env vars. Because runners never run `npm install` for an action, the dependency tree must ship committed; the standard practice is `ncc build` to compile entry plus deps into one tree-shaken `dist/index.js` (hundreds of KB to a few MB), guarded by a CI check that the committed bundle matches source so an un-rebuilt `dist/` can't silently run stale code. Pin the runtime to a supported Node, and have consumers pin your action by commit SHA so a mutable tag can't ship a poisoned bundle.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-javascript-actions-retrieval"
  lang="en"
  questions={[
    { q: "Why must a JavaScript action's dependencies be committed to the repo, and what tool solves it cleanly?", a: "Because the runner executes the action's `main:` file exactly as committed and never runs `npm install` for it — there is no dependency resolution step. So if `node_modules` isn't present, every `import` of a third-party package (including `@actions/core`) throws 'Cannot find module' on the consumer's runner. Committing raw `node_modules` works but is a huge, noisy, hard-to-review tree. The clean solution is `@vercel/ncc`: it compiles the entry file and its entire dependency tree into a single tree-shaken `dist/index.js` (typically a few hundred KB to low single-digit MB). You point `action.yml`'s `main:` at that file, commit just it, and gitignore `node_modules`. A CI check that re-runs `ncc build` and fails on a dirty `git diff dist/` keeps the committed bundle from drifting away from source." },
    { q: "What does the @actions/toolkit give you, and what is the anti-pattern it replaces?", a: "`@actions/toolkit` is the supported interface between your action code and the runner. `@actions/core` reads inputs (`core.getInput`, which pulls from the `INPUT_*` env vars without you touching them), writes outputs (`core.setOutput`), masks secrets (`core.setSecret`), and fails the step with the right exit code (`core.setFailed`). `@actions/github` gives you the parsed event payload via `github.context` and a pre-authenticated API client via `github.getOctokit(token)`. The anti-pattern it replaces is poking at the underlying environment directly — reading `process.env.INPUT_*`, parsing `GITHUB_EVENT_PATH` JSON by hand, or constructing your own authenticated HTTP client — which is fragile, undocumented, and breaks when GitHub changes the wiring." },
  ]}
/>

<Recap lang="en">
A JavaScript action declares `runs.using: "node20"` and a `main:` entry file, and GitHub Actions runs it as a Node.js 20 process directly on the host runner — no container, so it works across Linux, macOS, and Windows and boots in under a second, far cheaper than a Docker action's image pull. Your code reaches the runner through the `@actions/toolkit`: `@actions/core` for inputs, outputs, secret masking, and `setFailed`; `@actions/github` for the parsed `context` event payload and a pre-authenticated Octokit — using these instead of reading `INPUT_*` env vars or parsing the event JSON by hand is the dividing line between a robust action and a fragile one. The defining constraint is that runners never run `npm install` for an action: it executes your `main:` file exactly as committed, so the dependency tree must be in the repo. The standard solution is `@vercel/ncc`, which compiles the entry file and all of `node_modules` into a single tree-shaken `dist/index.js` of a few hundred kilobytes to a few megabytes; you commit that one file and gitignore `node_modules`. The recurring bug is bundle drift — editing source but forgetting `ncc build` leaves the runner executing the old `dist/`, so guard it with a CI check that the rebuilt bundle matches the committed one. Pin the runtime to a supported Node version, and have consumers pin your action by full commit SHA rather than a mutable tag, so a compromised release cannot ship a malicious bundle to everyone tracking `@v1`.
</Recap>
