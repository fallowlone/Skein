---
concepts:
- ci-cd-gha-docker-actions
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: senior
order: 3
prereqs: []
slug: 03-docker-actions
sources:
- https://docs.github.com/en/actions/sharing-automations/creating-actions/creating-a-docker-container-action
- https://docs.github.com/en/actions/sharing-automations/creating-actions/dockerfile-support-for-github-actions
- https://docs.github.com/en/actions/sharing-automations/creating-actions/metadata-syntax-for-github-actions
spiral: []
status: ready
summary: 'A Docker action packages its entire environment in a container — any language, any system dependency — but runs only on Linux runners and pays a 30-90s image pull or build per run, versus a composite or JS action measured in milliseconds.'
title: 'Docker actions: total environment control at the cost of Linux-only and image overhead'
track: ci-cd
unit: 09-actions-authoring
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The team needed a release action that ran a Go binary, `cosign`, and a pinned version of `skopeo` — a toolchain that did not exist as npm packages and would have meant scripting fragile downloads in a composite action. So they reached for a Docker action: a `Dockerfile` that `FROM`-ed a known base, installed the exact tools, and ran an entrypoint. It worked perfectly. Then they tried to add the action to a workflow that ran on `macos-latest` for a separate signing requirement, and the job failed before the entrypoint ever ran: *Docker container actions are only supported on Linux runners.* And on the Linux jobs, the action that had felt fast in testing now added a flat 40-70 seconds to every run — the runner was building the image from the `Dockerfile` on each invocation because nothing pre-built it. The container had bought them an arbitrary environment, and charged them an arbitrary-environment's startup tax and OS lock-in on every single run.
</Hook>

<Crux>A Docker action ships its whole environment in a container — but runs only on Linux and pays a 30-90s image pull or build each run.</Crux>

<Explanation>

## Total environment control

A Docker action declares `runs.using: "docker"` and either `image: "Dockerfile"` (built on the runner) or `image: "docker://ghcr.io/org/img:tag"` (pulled pre-built). Inside the container you control *everything* — base OS, system libraries, any language runtime, exact tool versions — which is the one thing composite and JavaScript actions cannot give you. If your action needs `cosign`, a specific `glibc`, a compiled Go or Rust binary, or a tool with no npm equivalent, the container is the only type that packages it reproducibly:

```dockerfile
FROM alpine:3.20
RUN apk add --no-cache cosign skopeo
COPY entrypoint.sh /entrypoint.sh
RUN chmod +x /entrypoint.sh
ENTRYPOINT ["/entrypoint.sh"]
```

```yaml
# action.yml
runs:
  using: "docker"
  image: "Dockerfile"
  args:
    - ${{ inputs.tag }}
  env:
    REGISTRY_TOKEN: ${{ inputs.token }}
```

The runtime contract differs from a JavaScript action. **Inputs arrive as `INPUT_*` environment variables** in the container (an input `tag` becomes `INPUT_TAG`), and you can also pass them positionally via `args:`. **Outputs are written by appending to the file named by `$GITHUB_OUTPUT`** — `echo "result=ok" >> "$GITHUB_OUTPUT"` from inside the entrypoint — and the workspace is bind-mounted at `$GITHUB_WORKSPACE`. There is no toolkit; you speak the protocol directly through env vars and these files.

<Quiz
  id="03-docker-actions-quiz-1"
  lessonSlug="03-docker-actions"
  lang="en"
  question="A team adds a working Docker container action to a job that runs on `runs-on: macos-latest`. The job fails before the action's entrypoint executes. What is the cause?"
  choices={[
    { label: "Docker container actions are only supported on Linux runners — they cannot run on macOS or Windows runners", correct: true },
    { label: "The Dockerfile must declare `FROM macos` to run on a macOS runner", misconception: "There is no macOS Docker base image for this, and the runner OS isn't switched by the base image. Docker container actions simply don't run on macOS runners at all — only Linux." },
    { label: "macOS runners need `runs.using: \"docker-mac\"` in action.yml", misconception: "No such runtime exists. The constraint is hard: Docker container actions run only on Linux runners; for cross-OS you need a composite or JavaScript action." },
    { label: "The image needs to be pre-pushed to a registry before macOS can pull it", misconception: "Pre-pushing changes pull-vs-build latency, not OS support. Even a pre-pushed image cannot run as a Docker action on a macOS runner — the platform doesn't support it." },
  ]}
/>

## The cost: Linux-only and image overhead

The container's power has two prices, both paid on every run. First, **Docker actions run only on Linux runners** — not macOS, not Windows. An action you want usable everywhere cannot be a Docker action; that alone pushes many reusable actions toward JavaScript. Second, **the container has to materialize before your code runs**. With `image: "Dockerfile"`, the runner *builds* the image on every invocation unless layers are cached — commonly 40-90 seconds. With `image: "docker://..."`, it *pulls* a pre-built image — faster, but still tens of seconds for a cold pull of a non-trivial image. Compare that to a composite (milliseconds) or a JavaScript action (sub-second node boot): the Docker action adds a flat, per-run tax that is invisible in a long job and brutal in a fast one triggered hundreds of times a day.

The mitigation is to **publish a pre-built image to a registry** (GHCR) and reference it with `docker://`, turning a per-run *build* into a per-run *pull* and letting layer caching help. But that adds a release pipeline for the image itself. Two security notes inherit from the container world and matter more here than for the other types: **pin the base image by digest** (`FROM alpine:3.20@sha256:...`) so the build is reproducible and a re-tagged upstream base can't change your action silently; and pin any actions *your workflow uses to invoke this one* by commit SHA, the same supply-chain hygiene as the other action types. The decision rule is blunt: choose Docker only when you genuinely need an environment composite and JavaScript cannot provide, and accept Linux-only plus the startup tax as the cost of that control.

<Quiz
  id="03-docker-actions-quiz-2"
  lessonSlug="03-docker-actions"
  lang="en"
  question={"A Docker action using `image: \"Dockerfile\"` adds ~60 seconds to every run of a workflow that fires hundreds of times a day. The container's logic is fast. What is the most effective fix that keeps it a Docker action?"}
  choices={[
    { label: "Pre-build and publish the image to a registry and reference it with `image: \"docker://...\"`, turning a per-run build into a faster per-run pull that benefits from layer caching", correct: true },
    { label: "Switch `runs.using` to `node20` to remove the container overhead", misconception: "That stops being a Docker action entirely — it abandons the arbitrary environment that was the reason to use Docker. The question asks to keep it a Docker action; the fix is pre-building the image, not changing type." },
    { label: "Add more `RUN` layers so Docker caches them and the build is instant", misconception: "More layers don't make a from-scratch per-run build instant, and on a fresh runner the layer cache is cold. The real lever is pulling a pre-built published image instead of building on every run." },
    { label: "Set `args:` to skip the image build step", misconception: "args: passes positional inputs to the entrypoint — it has nothing to do with whether the image is built. The build happens regardless; the fix is referencing a pre-built docker:// image." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Choosing an action type by the environment it needs and the cost it pays"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "need", label: "Need arbitrary env?", sub: "non-npm tools, any OS image" },
    { id: "docker", label: "Docker action", sub: "using: docker" },
    { id: "build", label: "Build Dockerfile", sub: "~40-90s per run" },
    { id: "pull", label: "Pull docker://", sub: "~tens of s, cold" },
    { id: "linux", label: "Linux runners only", sub: "no macOS/Windows" },
    { id: "else", label: "Composite / JS", sub: "ms / sub-second, cross-OS" },
  ]}
  edges={[
    { from: "need", to: "docker", label: "yes" },
    { from: "need", to: "else", label: "no" },
    { from: "docker", to: "build", label: "image: Dockerfile" },
    { from: "docker", to: "pull", label: "image: docker://" },
    { from: "docker", to: "linux", label: "platform limit" },
  ]}
  caption="Docker is the type you pick only when you need an environment the others can't give — and the price is Linux-only plus a per-run image build or pull, versus the millisecond-to-sub-second startup of composite and JS actions."
/>

<KeyTakeaway>A Docker action (`runs.using: "docker"`) packages an entire environment — any base OS, system library, language runtime, or non-npm tool — in a container, which is control composite and JavaScript actions cannot offer. The price is paid every run: it runs **only on Linux runners**, and the image must materialize first — a per-run build from `image: "Dockerfile"` (commonly 40-90s) or a pull from `image: "docker://..."` (tens of seconds cold), versus composite's milliseconds and a JS action's sub-second boot. Inputs arrive as `INPUT_*` env vars (no toolkit), outputs append to `$GITHUB_OUTPUT`. Mitigate the tax by publishing a pre-built image and digest-pinning the base; choose Docker only when you truly need its environment.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-docker-actions-retrieval"
  lang="en"
  questions={[
    { q: "What is the unique capability of a Docker action, and the two recurring costs that come with it?", a: "Its unique capability is total environment control: the action ships its own container, so it can use any base OS, any system library, any language runtime, and tools that have no npm equivalent (cosign, a specific glibc, a compiled Go/Rust binary) — reproducibly. Composite and JavaScript actions cannot package an arbitrary environment like that. The two costs, paid on every run: (1) it runs only on Linux runners — never macOS or Windows — so an action that must work everywhere can't be Docker; and (2) the image must materialize before the code runs — a per-run build from `image: \"Dockerfile\"` (commonly 40-90 seconds) or a pull from `image: \"docker://...\"` (tens of seconds for a cold non-trivial image), against composite's milliseconds and a JS action's sub-second node boot." },
    { q: "How do inputs and outputs work for a Docker action, and how do you reduce its per-run startup cost?", a: "Unlike a JavaScript action, there is no @actions/toolkit. Inputs arrive as environment variables named `INPUT_<NAME>` inside the container (input `tag` -> `INPUT_TAG`), and can also be passed positionally via the action.yml `args:` list. Outputs are written by appending `name=value` lines to the file path in `$GITHUB_OUTPUT` from the entrypoint (e.g. `echo \"result=ok\" >> \"$GITHUB_OUTPUT\"`); the repo is bind-mounted at `$GITHUB_WORKSPACE`. To cut the startup cost, publish a pre-built image to a registry like GHCR and reference it with `image: \"docker://ghcr.io/org/img:tag\"` so each run pulls instead of building, and benefits from layer caching — and pin the base image by digest (`FROM alpine:3.20@sha256:...`) so the build stays reproducible and a re-tagged upstream base can't change the action silently." },
  ]}
/>

<Recap lang="en">
A Docker action sets `runs.using: "docker"` and points `image:` at either a `Dockerfile` (built on the runner) or a `docker://` reference to a pre-built image, and inside that container you control the entire environment: base OS, system libraries, any language runtime, exact tool versions, and tools with no npm equivalent. That is the one capability composite and JavaScript actions structurally cannot provide, and it is the only reason to choose Docker. The cost is charged on every single run and comes in two parts. First, Docker container actions run only on Linux runners — not macOS, not Windows — so an action meant to be usable everywhere cannot be a Docker action, which alone pushes most general-purpose reusable actions toward JavaScript. Second, the image has to materialize before your entrypoint runs: building from `image: "Dockerfile"` adds roughly 40-90 seconds per run unless cached, and even a pre-built `docker://` image costs tens of seconds on a cold pull — a flat tax that disappears inside a long job and dominates a fast one triggered hundreds of times a day, against composite's milliseconds and a JS action's sub-second boot. The runtime protocol is bare: inputs arrive as `INPUT_*` environment variables (and optional positional `args:`), outputs are appended to `$GITHUB_OUTPUT`, the repo is mounted at `$GITHUB_WORKSPACE`, and there is no toolkit. Mitigate the startup tax by publishing a pre-built image to a registry and referencing it with `docker://`, turning a per-run build into a per-run pull with layer caching, and pin the base image by digest so the build is reproducible and a re-tagged upstream base cannot silently change your action. The decision rule: choose Docker only when you genuinely need an environment the other types can't give, and accept Linux-only plus the image overhead as the deliberate price of that control.
</Recap>
