---
concepts:
- affected-builds
- remote-cache
- nx
- turborepo
- task-graph
- sharding
deepensInto: []
estMin: 17
lang: en
lessonType: topic
level: senior
order: 1
prereqs: []
slug: 01-monorepo-and-affected
sources:
- https://nx.dev/ci/features/affected
- https://nx.dev/ci/features/remote-cache
- https://turborepo.com/docs/core-concepts/remote-caching
- https://docs.github.com/en/actions/using-jobs/using-a-matrix-for-your-jobs
spiral: []
status: ready
summary: In a large monorepo, "build & test everything on every PR" stops scaling. Run only the
  affected subset from the git diff, restore prior outputs from a content-hashed remote cache,
  and shard what's left — but a cache key that misses an input ships a stale build.
title: 'Monorepo CI: affected-only builds and remote caching'
track: ci-cd
unit: 06-scaling-ci
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A one-line copy fix in a docs package opens a PR. CI spins up, and forty minutes later it goes green — having rebuilt and tested all 180 projects in the monorepo, including three services and a mobile app that import nothing from the package you touched. The pipeline does this on every PR. The team has quietly accepted a forty-minute feedback loop as the cost of a monorepo, and one contributor has started batching unrelated changes into a single PR just to amortise the wait. Nothing is broken. Everything is slow. The build is answering a question nobody asked: *what could have changed?* — when the only question that matters is *what actually did?*
</Hook>

<Crux>Run only the projects a diff affects, restore the rest from a content-hashed cache, and CI scales with the change, not the repo.</Crux>

<Explanation>

## Affected: from a git diff to a subset of the task graph

By the end of this lesson you'll know exactly why the 40-minute build keeps happening and the three levers that eliminate it — or at least shrink it to match the diff, not the repo.

"Build and test everything on every PR" is correct and, in a large monorepo, ruinous. Its cost grows with the size of the repository while the value of any single PR depends only on what that PR changed. Affected-only builds break that coupling. The tooling — Nx, Turborepo, Bazel — models the repo not as a flat list of packages but as a **project graph**: nodes are projects, edges are `import`/dependency relationships. On top of it sits a **task graph**: `build`, `test`, and `lint` for each project, wired so a project's `build` depends on its dependencies' `build`.

Computing the affected set is two steps. First, the tool asks git which files changed between two commits — a `--base` and a `--head` — and maps each touched file back to the project that owns it. That gives the *directly* changed projects. Second, it walks the project graph **in reverse**: any project that transitively *depends on* a changed project is also affected, because a change upstream can break it. The union is the affected set, and the tool runs the requested tasks for exactly that subset.

```bash
# In CI, base = last successful main SHA, head = the PR tip.
# nrwl/nx-set-shas exports these so you don't diff against a stale point.
nx affected -t build,test,lint --base=origin/main --head=HEAD

# Turborepo expresses the same idea via the package graph + git:
turbo run build test lint --filter='...[origin/main]'
```

The base matters more than it looks. Set it to the **last successful commit on main**, not a fixed branch point — otherwise a long-lived PR diffs against an old base, recomputes a huge affected set, and you lose the win. In CI you wire this with a helper like `nrwl/nx-set-shas`.

## Remote cache: hash the inputs, restore the outputs

Affected tells you *what to run*. The cache tells you whether you even need to. Before executing a task, the tool computes a hash over **everything that can influence the output**: the project's source files, the resolved hashes of its dependencies, the task's configuration, the tool versions, and any declared environment variables. That hash is the cache key. If a task with that exact key was run before — by anyone, on any machine — its recorded **output** (the `dist/`, the test result, the logs) is downloaded and replayed instead of recomputed.

The leverage is that the cache is **shared and content-addressed**. Turborepo splits this into a *global hash* and a per-task *task hash*; if either changes, the task misses. A teammate who already built `main` locally, or a previous CI run, populates a remote cache (Nx Cloud, Turborepo Remote Cache) that your run reads from. A cache hit that turns a multi-minute `build` into a sub-second artifact restore is the difference between a CI run that scales with the diff and one that scales with the repo.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Strategy</th>
        <th class="border border-border px-3 py-2 text-left">What runs on a small PR</th>
        <th class="border border-border px-3 py-2 text-left">Wall-clock</th>
        <th class="border border-border px-3 py-2 text-left">Main risk</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Build &amp; test everything</td>
        <td class="border border-border px-3 py-2">All 180 projects</td>
        <td class="border border-border px-3 py-2">~40 min, every PR</td>
        <td class="border border-border px-3 py-2">None — just slow and costly</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Affected only</td>
        <td class="border border-border px-3 py-2">Changed + dependents (often 5–25%)</td>
        <td class="border border-border px-3 py-2">Minutes</td>
        <td class="border border-border px-3 py-2">A root file marks everything affected</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Affected + remote cache</td>
        <td class="border border-border px-3 py-2">Affected minus cache hits</td>
        <td class="border border-border px-3 py-2">Seconds–minutes</td>
        <td class="border border-border px-3 py-2">A missing input → a wrong "cache hit"</td>
      </tr>
    </tbody>
  </table>
</div>

<Inset kind="why" lang="en">
The cache is only as correct as its inputs. The hash must capture *every* input — and the one people forget is **environment variables**. Turborepo is blunt about this: a value read at build time but absent from the hash is a silent poison. You split env vars into `env`/`globalEnv` (affect the hash) and `passThroughEnv`/`globalPassThroughEnv` (available at runtime, do *not* change the key). Put `NEXT_PUBLIC_API_URL` in the wrong bucket and two builds with different API URLs share a key: the second gets a "hit" and ships the first one's baked-in URL. The bug looks impossible — same commit, wrong build — because the input that differed was never hashed.
</Inset>

## Sharding: split what's left across runners

Affected and caching shrink *which* tasks run. Sharding shrinks how long the survivors take by running them **in parallel across N runners**. The classic form is a CI matrix: split a 2,000-case test suite into shards `1/4 … 4/4`, give each its own runner, and a 40-minute suite finishes in ~10 (four-way ≈ 75% faster); a small suite can drop from 3 minutes to 30 seconds. Nx Cloud and Turborepo go further and *distribute the task graph itself* across agents, respecting dependency order so a downstream `build` waits for its upstream.

```yaml
# GitHub Actions matrix: 4 parallel test shards.
strategy:
  matrix:
    shard: [1, 2, 3, 4]
steps:
  - run: npx jest --shard=${{ matrix.shard }}/4   # or: nx affected -t test --parallel
```

One rule separates a fast matrix from a slow one: **balance by execution time, not test count.** Four shards of equal file count but lopsided runtime leave three runners idle while one grinds — the slowest shard sets your wall-clock, so the win is only as good as your worst shard.

<TradeoffMatrix
  id="01-monorepo-and-affected-tradeoff-1"
  lessonSlug="01-monorepo-and-affected"
  lang="en"
  prompt="A 180-project monorepo runs ~40 min of build+test on every PR, where most PRs touch 1–3 projects. Pick the primary scaling strategy."
  options={[
    { name: "Affected-only builds + a shared remote cache (Nx Cloud / Turborepo)", summary: "Run the changed subset, restore the rest.", correct: true, justification: "This attacks the actual cost: the pipeline scales with the diff, not the repo. The affected set runs only changed projects plus their transitive dependents, and the remote cache turns previously-built tasks into sub-second artifact restores shared across CI and developer machines. On a repo where most PRs touch a few projects this is the highest-leverage change — minutes instead of 40, on most PRs. You still pay for correctness discipline (every input in the hash, a tight base SHA, and watching for root files that mark everything affected), but that's the work, and it's bounded." },
    { name: "Sharding only: keep building everything, split the test suite across 8 runners", summary: "Parallelise the brute force.", justification: "Sharding helps, but applied alone it parallelises waste — you still build and test 177 projects that didn't change. It cuts the 40 minutes by maybe half at 8x the runner cost, and the win caps at your slowest shard. Sharding is the right tool for the tasks that genuinely must run; it's the wrong place to start when the real problem is running far too many tasks in the first place. Combine it with affected, don't substitute it." },
    { name: "Buy bigger / more CI runners and keep build-everything", summary: "Throw hardware at it.", justification: "Faster runners shave the constant but not the scaling: cost grows linearly with the repo and you re-pay it on every PR, including the docs-typo PR. It needs zero config, which is its only virtue. As the monorepo grows the bill and the wall-clock both keep climbing because you never stopped doing redundant work — you just made redundant work faster." },
    { name: "Split the monorepo into many polyrepos so each CI only sees one project", summary: "Delete the problem.", justification: "This does make each pipeline small, but it trades a CI-scaling problem for a dependency-versioning and cross-cutting-change nightmare: atomic changes across packages now span many repos and PRs, and you lose the single source of truth that motivated the monorepo. It's a strategic reorg, not a CI fix, and most teams that try it for this reason regret it. Solve scaling inside the monorepo with affected + cache first." },
  ]}
/>

The honest tradeoff: affected + remote cache is the highest-leverage move, but its entire value rests on the hash being *complete* and the affected computation being *tight*. Miss an input and you ship stale; let a root file balloon the affected set and you've paid the setup cost for nothing.

## The failure modes that erase the win

Three failures recur, and each turns a scaling win into a liability. When you see any of them in your repo, the fix is always the same: tighten the input set, not the tooling.

**The root-file blast radius.** Change `tsconfig.base.json`, `nx.json`, or — the classic — the lockfile, and the tool marks **everything** affected. The lockfile case is a deliberate failsafe: a dependency bump *could* affect any project, so by default Nx plays it safe and rebuilds all. The result is that a routine `package-lock.json` update detonates the full 40-minute build, and a team that doesn't understand why concludes "affected doesn't work." It works; you tripped the global-input wire. You tune it with `namedInputs` so only genuinely global files are global — but mis-scope a `{workspaceRoot}` glob and you re-create the blast radius yourself.

**The stale hit from an unhashed input.** Covered above: an env var, a tool version, or a config file that influences the output but isn't in the hash. The cache returns a build that is *wrong* for the current inputs, and because a hit is silent and fast, it can pass review and reach production. This is the most dangerous failure because it doesn't fail — it succeeds with the wrong answer.

**Remote-cache poisoning / trust.** A remote cache is a shared write surface. If an untrusted actor (a fork PR, a compromised token) can *write* artifacts, they can plant a malicious `dist/` under a key a trusted build will later read, and your "cache hit" runs their code. This is why Turborepo signs artifacts with **HMAC-SHA256** and verifies them on download, treating any artifact that fails the signature as a miss — and why you scope cache *write* access tightly and never let untrusted PRs populate the cache trusted runs consume.

<Quiz
  id="01-monorepo-and-affected-quiz-1"
  lessonSlug="01-monorepo-and-affected"
  lang="en"
  question="A PR changes only a leaf docs package, yet nx affected rebuilds all 180 projects. What is the most likely cause?"
  choices={[
    { label: "The PR also bumped a dependency, changing package-lock.json — and Nx by default marks every project affected on a lockfile change as a failsafe", correct: true },
    { label: "The remote cache is disabled, so nothing can be skipped", misconception: "A disabled cache means tasks recompute instead of restoring from artifacts, but it does not expand the affected set. Affected is computed from the git diff and the project graph, independently of whether a cache is present. The blast radius here comes from a global input, not a missing cache." },
    { label: "nx affected always rebuilds everything; the subset only applies to test, not build", misconception: "Affected applies uniformly to whatever targets you pass (-t build,test,lint). It does not silently fall back to building everything for build. A full rebuild signals a global input was touched — most often the lockfile or a root config file." },
    { label: "The --base SHA was set to HEAD, so the diff is empty and Nx rebuilds as a fallback", misconception: "An empty diff yields an empty affected set, so Nx would run nothing, not everything. A stale base causes too-large or wrong sets, but base=HEAD doesn't trigger a build-all fallback. The all-affected result points to a changed global file." },
  ]}
/>

<Quiz
  id="01-monorepo-and-affected-quiz-2"
  lessonSlug="01-monorepo-and-affected"
  lang="en"
  question="Two builds of the same commit produce different bundles, but the second is reported as a remote cache HIT and ships the wrong API URL. What went wrong?"
  choices={[
    { label: "A build-time env var that changes the output was not included in the cache key — so two different inputs hashed to the same key and the second restored a stale artifact", correct: true },
    { label: "The cache was poisoned by a malicious external writer", misconception: "Poisoning is a real risk, but the symptom here is a value the team's own build set, not injected code. The signature would still verify because the artifact is legitimately one of theirs. The defect is an incomplete hash: an input that affects output wasn't hashed, so a valid-but-stale artifact was reused." },
    { label: "Cache hits are inherently nondeterministic and occasionally return the wrong artifact", misconception: "Content-addressed caches are deterministic by construction: same key produces the same artifact. A 'wrong' hit always means the key failed to capture an input that actually mattered. The fix is to add that input (here, the env var) to env/globalEnv, not to distrust hitting." },
    { label: "The affected computation skipped the project, so it was never rebuilt", misconception: "Being skipped by affected would mean no build attempt at all, not a hit returning a wrong bundle. The project clearly built and the cache returned an artifact — the question is why the artifact was stale, which is a hashing-completeness problem, not an affected-scoping one." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Monorepo CI pipeline: a git diff is mapped onto the project graph to compute the affected set, each affected task's inputs are hashed and looked up in the remote cache, and only the cache misses actually run"
  hue="var(--d-systems)"
  nodes={[
    { id: "diff", label: "git diff base..head", sub: "touched files" },
    { id: "graph", label: "Project / task graph", sub: "+ reverse deps" },
    { id: "affected", label: "Affected set", sub: "changed + dependents" },
    { id: "cache", label: "Cache lookup", sub: "hash inputs to key" },
    { id: "run", label: "Run only misses", sub: "shard across runners" },
  ]}
  edges={[
    { from: "diff", to: "graph", label: "map files to projects" },
    { from: "graph", to: "affected", label: "walk dependents" },
    { from: "affected", to: "cache", label: "per task" },
    { from: "cache", to: "run", label: "miss" },
    { from: "cache", to: "run", label: "hit: restore output" },
  ]}
  caption="Affected narrows what could run to what's relevant; the content-hash cache narrows that to what hasn't been built before; sharding parallelises the remainder. A wrong base widens the affected set, and an incomplete input hash turns a 'hit' into a stale build."
/>

<KeyTakeaway>Scale CI with the diff: the affected set bounds what runs, a content-hashed cache restores prior builds, sharding splits the rest. Traps: a root file marks everything affected; an unhashed input makes a "hit" stale.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-monorepo-and-affected-retrieval"
  lang="en"
  questions={[
    { q: "Walk through how a tool computes the affected set from a PR, and why a stale --base or a lockfile change breaks it.", a: "The tool models the repo as a project graph (nodes = projects, edges = dependencies) with a task graph layered on top. To compute affected it (1) asks git for the files changed between --base and --head, (2) maps each file to the project that owns it to get the directly changed projects, then (3) walks the graph in reverse to add every project that transitively depends on a changed one, because an upstream change can break a downstream consumer. The union is the affected set, and only its tasks run. Two things break this. A stale --base — e.g. a fixed branch point instead of the last successful main SHA — makes a long-lived PR diff against an old commit, inflating the affected set with changes that already merged, so you recompute work for nothing; you fix it with a helper like nrwl/nx-set-shas that exports the right base/head. A lockfile change is worse: by default the tool marks every project affected on a package-lock.json change as a deliberate failsafe, because a dependency bump could touch anything. So a routine dependency update detonates the full build, and teams misread it as 'affected is broken.' You scope it with namedInputs so only genuinely global files are global — but a mis-written {workspaceRoot} glob re-creates the same blast radius." },
    { q: "What exactly goes into a task's cache key, and how does an incomplete key produce a stale 'cache hit' that ships to production?", a: "A task's cache key is a hash over everything that can influence its output: the project's own source files, the resolved hashes of its dependency tasks, the task configuration, the tool/toolchain versions, and any declared environment variables. Turborepo splits this into a global hash and a per-task task hash; if either changes the task misses, and if both match a prior run anywhere — another machine, an earlier CI run — the recorded output is restored instead of recomputed, which is the whole speed win. The danger is that the hash is only correct if it captures every real input. The classic miss is an environment variable read at build time but not declared in env/globalEnv — instead left in passThroughEnv or forgotten entirely. Now two builds with different values (say two API URLs) hash to the same key: the second build gets a 'hit' and restores the first build's artifact, baking in the wrong URL. Because a hit is silent and fast and the commit is identical, the wrong build passes review and can reach production. It's the most dangerous cache failure precisely because it doesn't fail — it succeeds with the wrong answer. The fix is to make the hash complete (move output-affecting env vars into env/globalEnv) and, for remote caches, sign artifacts (HMAC-SHA256) and verify on download so a poisoned or tampered artifact is treated as a miss." },
  ]}
/>

<Recap lang="en">
"Build and test everything on every PR" is correct and, past a certain repo size, unaffordable — its cost scales with the monorepo while a PR's value scales only with its diff. Affected-only builds break that coupling: the tool models the repo as a project graph and a task graph, reads the files changed between a --base and --head, maps them to the projects that own them, then walks the graph in reverse to add every transitive dependent — and runs tasks only for that affected set. A content-hashed remote cache (Nx Cloud, Turborepo) goes further, hashing each task's full inputs — source, dependency hashes, config, tool versions, env vars — into a key and restoring a prior output instead of recomputing, shared across CI and developer machines so a multi-minute build becomes a sub-second restore. Sharding then parallelises the survivors across N runners (balance by runtime, not test count) for roughly 75% off at four-way. Three failures erase the win: a root file (lockfile, tsconfig.base.json) marking everything affected — by design for the lockfile, tunable via namedInputs; an input missing from the hash turning a 'cache hit' into a silently stale build that succeeds with the wrong answer; and remote-cache poisoning, which is why artifacts are HMAC-SHA256-signed and verified on download and why write access stays tightly scoped. Scale with the change, not the repo — and keep the hash complete and the base tight, because that is where the correctness lives. Now when you see a PR that rebuilds everything, your first question is: which global input was touched — the lockfile, a root config, or a mis-scoped glob?
</Recap>
