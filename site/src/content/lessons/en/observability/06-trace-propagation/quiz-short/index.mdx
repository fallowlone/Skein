---
concepts:
- traceparent
- consistent-sampling
- tail-sampling-ram
- async-context
- span-links
- propagation-observability
estMin: 14
lang: en
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://www.w3.org/TR/trace-context/
- https://opentelemetry.io/docs/concepts/context-propagation/
- https://opentelemetry.io/docs/concepts/sampling/
status: ready
summary: "Free-recall prompts across the trace-propagation unit. Answer each in your own words first, then reveal the model answer and compare."
title: 'Trace propagation: free-recall review'
track: observability
unit: 06-trace-propagation
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory before you open the model answer — the effort of recall is what makes propagation reasoning stick.
</Hook>

<Goal lang="en">
Reconstruct the unit's spine from memory — the W3C header, the missing-header fallback, consistent sampling, the tail-sampler RAM model, async-boundary fixes, and the propagation-health metrics — without looking back at the lessons.
</Goal>

<RetrievalDrawer
  client:load
  id="06-trace-propagation-short-retrieval"
  lang="en"
  questions={[
    { q: "Why is broken trace propagation worse than no tracing at all, and what single metric exposes it?", a: "With no tracing you know you are blind and fall back to logs. With broken propagation every service still emits spans, but none link — each trace is one span deep. The dashboard renders traces and claims end-to-end visibility, so operators trust it and chase the wrong service for hours. The slow downstream is invisible because its span sits in a separate orphan trace with no causal link to the entry point. The failure is silent: GitHub ran a 50% orphan rate for a quarter while dashboards looked normal. The only thing that exposes it is the orphan-span rate by service.name — entry-points legitimately root traces, but a sustained orphan rate above ~5% on an internal service is a propagation regression. It must be a dashboarded metric and ideally an alert, because OTel never surfaces it by default." },
    { q: "Walk through the four traceparent fields and what a service must do with an invalid header.", a: "traceparent is exactly 55 bytes: version (2 hex, currently 00, ff forbidden), trace-id (32 hex / 128 bits, uniformly random, all-zeros invalid), parent-id (16 hex / 64 bits, the caller's span-id, all-zeros invalid), and trace-flags (2 hex, only bit 0 — the sampled flag — defined). The fixed width lets a sidecar parse it by splitting on dashes with no regex. If a header is invalid — unknown version, wrong field length, all-zero trace-id or parent-id — the spec mandates the receiver discard it and start a brand-new trace, ideally logging invalid_traceparent_received=true so operators can find the broken upstream caller. This graceful fallback stops one misbehaving caller from corrupting the backend trace store, which keys all spans by trace-id." },
    { q: "How does consistent sampling let 12 services agree on keep/drop with no coordination, and why are partial traces unacceptable?", a: "Every service computes the same deterministic function: hash(trace-id) mod 100 < sample_percentage. Because the trace-id is identical on every hop (propagated by traceparent) and the hash is deterministic, all services independently reach the same boolean verdict without communicating; the head decision is also cached in the sampled flag so downstreams can short-circuit. W3C Trace Context Level 2 adds a random-trace-id flag asserting the id is uniformly random, which the hash distribution relies on. Partial traces are unacceptable because a trace missing service B's span looks like A called C directly — latency attribution is wrong, percentile math is biased toward short spans, and on-call engineers make confident wrong diagnoses. Consistency means the whole trace is kept or none of it, never a fragment." },
    { q: "Give the tail-sampler RAM model and the three independent OOM levers.", a: "A tail-sampling collector holds all spans of every active trace in RAM until the decision window closes. RAM = active_traces × avg_spans_per_trace × bytes_per_span, where active_traces ≈ in_flight_request_rate × decision_window_seconds. At 10k req/s, 30s window, 10 spans, 1 KB/span that is roughly 3 GB per instance. Three independent levers: (1) active_traces — bounded by a hard num_traces cap that evicts the oldest incomplete trace, plus scaling replicas behind the load-balancing exporter; (2) spans_per_trace — bounded by breaking long-running and batch workflows into span-linked sub-traces so no single trace carries thousands of spans; (3) bytes_per_span — bounded by attribute-value size limits. The load-balancing exporter must hash by trace-id, never round-robin, or each replica sees only fragments and decides wrongly." },
    { q: "Name the async boundaries that drop context and the correct fix for each, including the in-process vs cross-process distinction.", a: "In-process boundaries lose the runtime's current-context substrate (Node AsyncLocalStorage, Python contextvars, Java ThreadLocal+Scope, Go explicit ctx). setTimeout, setImmediate, queueMicrotask, manually spawned threads, and Loom virtual threads created outside a Scope all start with no context — fix by capturing context.active() and wrapping the callback with context.bind (or context.with), copying contextvars across Python threads, creating virtual threads inside the Scope, and threading ctx everywhere in Go. Cross-process boundaries (Kafka, RabbitMQ, SQS) have no shared memory, so context.bind cannot help — the producer must propagator.inject() the traceparent into the message headers and the consumer must propagator.extract() it back. The service mesh transparently propagates headers for HTTP and gRPC only; it never touches queue wire formats, so queue propagation is always application-level." },
    { q: "When does the parent-child tree break down, and how do span-links and the 30-minute problem resolve it?", a: "Parent-child assumes linear causality (A calls B calls C) and forces every span to have exactly one parent. It breaks in three cases: batch processing (one consumer pulls 1,000 messages — 1,000 incoming contexts, no single meaningful parent), fan-in (parallel sub-jobs converge at a join point with many contributors), and async follow-ups (the originating request returns to the user, then spawns work hours later under a now-closed context). Span-links resolve all three: a span declares additional SpanContexts it is causally related to but does not descend from, drawn as dotted lines in the backend. The 30-minute problem is the related collector issue: a batch job running longer than the 30s–5min decision window emits late spans that look like orphans to the tail sampler. The senior fix is to break any trace longer than ~30s or wider than ~100 spans into span-linked sub-traces, each fitting the decision window; the retrofit is backend late-span support (Tempo, Honeycomb, Datadog accept spans up to 24h) with head sampling at 100% for batch." },
  ]}
/>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the unit's spine: the 55-byte W3C traceparent stitches services, an invalid header means start fresh, and a uniformly random trace-id makes consistent sampling work without coordination. The tail-sampler trades RAM for outcome-aware selection — bound it with num_traces, replicas, and span-linked sub-traces. Async boundaries drop context (context.bind in-process, inject/extract cross-process), and because propagation fails silently, the orphan-span rate is the metric that proves the dashboard is telling the truth.
</Recap>
