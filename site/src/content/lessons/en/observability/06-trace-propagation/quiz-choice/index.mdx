---
concepts:
- traceparent
- consistent-sampling
- tail-sampling
- async-context
- orphan-span-rate
- span-links
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.w3.org/TR/trace-context/
- https://opentelemetry.io/docs/concepts/context-propagation/
- https://opentelemetry.io/docs/concepts/sampling/
status: ready
summary: "Multiple-choice synthesis across the trace-propagation unit — the W3C header, consistent sampling, tail-sampling collector limits, async boundaries, and silent production failures."
title: 'Trace propagation: multiple-choice review'
track: observability
unit: 06-trace-propagation
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one mirrors a call you make in a real incident — not a definition to recite, but a propagation decision to weigh while a dashboard lies to you about how much you can see.
</Hook>

<Goal lang="en">
Confirm you can connect the W3C header format, consistent sampling, tail-sampling collector economics, async-boundary failures, and propagation observability — the synthesis the individual lessons built toward.
</Goal>

<Quiz
  id="06-trace-propagation-choice-1"
  lessonSlug="06-trace-propagation"
  lang="en"
  question="A service receives 'traceparent: 99-aaaaaaaa-bbbbbbbb-01'. Per the W3C spec, what must it do, and why does this rule matter for the whole fleet?"
  choices={[
    { label: "Discard it as invalid (unknown version 99, trace-id and parent-id wrong length) and start a fresh trace. The graceful fallback stops one misbehaving caller from cascading corruption into every downstream trace store.", correct: true },
    { label: "Reject the request with a 400 so the caller fixes its header.", misconception: "traceparent is a hint, not access control. The spec mandates ignoring an invalid header and starting a fresh trace; rejecting the request would break the user flow over a telemetry detail." },
    { label: "Use the header as-is — any value is better than none for stitching.", misconception: "Using a malformed trace-id silently merges or corrupts traces in the backend, which keys all spans by trace-id. A wrong id is worse than a fresh valid one." },
    { label: "Pad the short fields with zeros to reach the required widths.", misconception: "The spec defines no repair behaviour; an all-zero trace-id or parent-id is itself invalid. The only correct action is to discard and regenerate." },
  ]}
/>

<Quiz
  id="06-trace-propagation-choice-2"
  lessonSlug="06-trace-propagation"
  lang="en"
  question="A team runs probabilistic head sampling at 1% across 12 services with no central coordinator. How do all 12 independently agree to keep or drop the same trace?"
  choices={[
    { label: "Each computes the same deterministic hash(trace-id) mod 100 against the rate. Because the trace-id is identical on every hop and the hash is deterministic, all services reach the same keep/drop verdict with zero coordination — consistent sampling, never a fragment.", correct: true },
    { label: "The collector reconciles the per-service decisions after the fact and drops mismatched spans.", misconception: "Head sampling decides before spans leave the service; there is no after-the-fact reconciliation. Consistency comes from deterministic trace-id hashing at each service, not from the collector." },
    { label: "Each service samples its own 1% independently, so on average the trace is roughly complete.", misconception: "Independent per-service dice would keep service A's span and drop service B's for the same trace — exactly the partial trace consistent sampling exists to prevent." },
    { label: "The root service writes the verdict and downstreams ignore the sampled flag entirely.", misconception: "Downstreams honour the sampled flag in trace-flags — but the deeper guarantee is that even re-hashing the same trace-id yields the same answer, so the system is consistent by construction." },
  ]}
/>

<Quiz
  id="06-trace-propagation-choice-3"
  lessonSlug="06-trace-propagation"
  lang="en"
  question="A tail-sampling collector OOMs every few hours. Trace count is flat but spans-per-trace is climbing. What is the cause, and which 'fix' makes it worse?"
  choices={[
    { label: "A long-running trace (batch job or recursive worker) is accumulating thousands of spans, all held in RAM until the decision window closes. Lengthening the decision window to 'be safe' inflates the buffer further and accelerates the OOM.", correct: true },
    { label: "Duplicate spans are arriving; deduplicate at ingest.", misconception: "Duplicates would raise span count across many traces, not concentrate it on a few. The signature here is a few traces each carrying thousands of spans — a long-running-trace problem." },
    { label: "Too few collector replicas; the durable fix is simply more replicas.", misconception: "Replicas behind the load-balancing exporter split active traces across instances, but one fat trace still lands wholly on one replica. The real fix is breaking the work into span-linked sub-traces." },
    { label: "The trace-id hash is uneven; switch the exporter to round-robin.", misconception: "Round-robin scatters one trace's spans across replicas so no instance sees the whole trace — that breaks tail sampling outright. trace-id hashing is mandatory; it is not the cause." },
  ]}
/>

<Quiz
  id="06-trace-propagation-choice-4"
  lessonSlug="06-trace-propagation"
  lang="en"
  question="A Node service uses OTel HTTP auto-instrumentation. Work deferred via setTimeout, and messages it publishes to Kafka, both show up as orphan traces. What is the correct two-part fix?"
  choices={[
    { label: "Wrap the setTimeout callback with context.bind(ctx, fn) for the in-process boundary, and call propagator.inject() into the message headers on publish (extract on the consumer) for the cross-process boundary. They are different mechanisms.", correct: true },
    { label: "context.bind fixes both — it keeps the active context alive through any boundary.", misconception: "context.bind only revives in-process context. Kafka crosses a process boundary with no shared memory, so the trace-id must be written into the message headers via inject and read back via extract." },
    { label: "inject/extract fixes both — Kafka instrumentation also covers the timer.", misconception: "inject/extract is for serialised carriers like message headers. A setTimeout callback runs in the same process on a new async stack; it needs context.bind, not header injection." },
    { label: "Switch to manual spans everywhere; auto-instrumentation cannot handle async.", misconception: "Auto-instrumentation handles HTTP fine; the gaps are specifically unwrapped async callbacks and queue boundaries. The fix is targeted binding and inject/extract, not abandoning auto-instrumentation." },
  ]}
/>

<Quiz
  id="06-trace-propagation-choice-5"
  lessonSlug="06-trace-propagation"
  lang="en"
  question="An internal service's orphan-span rate sits at 5% for a quarter while dashboards show full-looking traces. The team adds tail sampling hoping to clean it up. What is the core misunderstanding?"
  choices={[
    { label: "Tail sampling only chooses which complete traces to KEEP — it cannot reconstruct a missing parent-id. Orphans are created at emission time by broken propagation and must be fixed there; sampling only filters, it never repairs lineage.", correct: true },
    { label: "A 5% orphan rate is normal for internal services, so nothing is wrong.", misconception: "Entry-point services legitimately root traces, but a healthy internal service should sit near 0%. A sustained 5% on an internal service is a propagation regression, not a baseline." },
    { label: "The dashboards prove propagation is fine; the metric is miscounting.", misconception: "This is exactly the silent-failure trap from the unit: dashboards keep rendering traces while half are disconnected. The orphan-rate metric is the truth; the rendered traces are the illusion." },
    { label: "Switching everything to head sampling at 100% would eliminate the orphans.", misconception: "Sampling rate governs whether spans are exported, not whether they carry a correct parent-id. Exporting more orphans does not connect them." },
  ]}
/>

<Quiz
  id="06-trace-propagation-choice-6"
  lessonSlug="06-trace-propagation"
  lang="en"
  question="A consumer pulls 1,000 Kafka messages and processes them in one batch. Modelling this as one parent span with 1,000 children OOMs the collector. What is the idiomatic fix and what does it preserve?"
  choices={[
    { label: "Use span-links: create one batch span that links to all 1,000 incoming SpanContexts without making them children. The trace stays small enough for the decision window while the causal lineage to every source message is preserved.", correct: true },
    { label: "Raise num_traces so the collector can hold the wide trace.", misconception: "num_traces caps active trace count, not spans-per-trace. A 1,000-child trace still floods one buffer; raising the cap defers the OOM instead of removing the fan-in." },
    { label: "Head-sample the batch service at 0% so it stops emitting.", misconception: "Dropping all batch traces also drops the error and slow ones the team needs. span-links keep the batch observable while bounding its size." },
    { label: "Give each message its own independent trace with no relationship to the batch.", misconception: "Fully independent traces lose the fan-in causality — you can no longer see that one batch operation processed those specific messages. span-links exist precisely to keep that link." },
  ]}
/>

<Recap lang="en">
The unit's through-line is one chain: the 55-byte W3C traceparent stitches services together, an invalid header means start fresh, and a uniformly random trace-id lets every service hash the same keep/drop verdict for consistent sampling. The tail-sampling collector trades RAM (active-traces × spans × bytes × window) for outcome-aware selection, so long-running traces and fan-in must be broken up with span-links. Async boundaries (setTimeout needs context.bind, queues need inject/extract) are the leading source of orphans — and because propagation fails silently, the orphan-span rate is the only metric that tells you the dashboard is lying.
</Recap>
