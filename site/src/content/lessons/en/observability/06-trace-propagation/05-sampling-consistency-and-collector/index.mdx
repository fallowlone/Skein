---
concepts:
- consistent-sampling
- trace-id-hashing
- tail-sampling-collector
- load-balancing-exporter
- oom-prevention
- num-traces-cap
deepensInto: []
estMin: 14
lang: en
lessonType: topic
level: senior
order: 5
prereqs:
- observability/06-trace-propagation/04-head-and-tail-sampling
slug: 05-sampling-consistency-and-collector
sources:
- https://opentelemetry.io/blog/2022/tail-sampling/
- https://opentelemetry.io/docs/concepts/sampling/
- https://www.w3.org/TR/trace-context-2/
spiral: []
status: ready
summary: Consistent sampling uses deterministic trace-id hashing so all services agree
  on keep/drop without coordination. The tail-sampling Collector tier requires a load-balancing
  exporter, a RAM budget model, and an explicit cap to prevent OOMs from long-running
  traces.
title: Sampling consistency and the tail-sampling Collector tier
track: observability
unit: 06-trace-propagation
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A tail-sampling Collector OOMs every few hours during peak traffic. The team bumps the memory limit and it OOMs again. Nobody asked: why does memory grow, what controls it, and what caps it?
</Hook>

<Crux>Trace-id hash: all services agree on keep/drop, no coordination. Collector RAM = traces × spans × size × window — each is a lever or OOM.</Crux>

<Explanation>

## Sampling consistency across services

If you already have head and tail sampling, why does consistent sampling matter as a separate concern? Because without it, service A might keep a trace while service B drops it — and the backend stitches together a fragment that looks like a complete trace but is missing half the story. "Consistent sampling" means all spans of one trace are either all-sampled or all-dropped. No partial traces — either the backend has a complete trace or it has nothing.

The mechanism: probabilistic head-samplers use a **deterministic hash of the trace-id modulo 100** to decide which traces to keep. Because the hash is deterministic and the trace-id is the same across all services (propagated by traceparent), every service independently arrives at the same keep/drop decision. No coordination is needed.

W3C Trace Context Level 2 (2024) formalises this with the `random-trace-id` flag (bit 1, value `02`): when set, the trace-id is guaranteed to be uniformly random, making the hash-based consistent sampling safe to rely on. Consistent hash-based samplers read this flag as a prerequisite; if it is absent, they may fall back to a simpler approach.

**Why partial traces are worse than no traces:**
- A trace missing service B's span looks like service A called service C directly. Latency attribution is wrong.
- Percentile calculations over partial traces produce systematic bias — short spans are over-represented.
- On-call engineers diagnose based on what they see; partial traces lead to confident wrong diagnoses.

## The tail-sampler memory model

The tail-sampling Collector holds all spans of every active trace in memory until the decision window closes. Memory usage:

```
RAM = active_traces × avg_spans_per_trace × bytes_per_span
    = (in_flight_request_rate × decision_window_seconds) × avg_spans × span_size
```

At 10k req/s, 30s window, 10 spans/trace, 1 KB/span:
`10,000 × 30 × 10 × 1,024 = ~3 GB per Collector instance`

Every factor is an independent OOM lever:

| Factor | What inflates it | Mitigation |
|---|---|---|
| active_traces | Traffic spike, too-long window | Scale replicas, shorten window |
| spans_per_trace | Long-running batch jobs, recursive instrumentation | Break long traces with span-links |
| bytes_per_span | Large attribute values, excessive metadata | Attribute value size limits |
| decision_window | Conservatively set too long | Tune per-service SLA; 30s is typical |

Each lever is independent — you can fix a span explosion without scaling replicas, and you can fix a traffic spike without changing span structure. When the collector OOMs, inspect which of these four is growing before reaching for a larger instance.

## The load-balancing exporter requirement

Multiple Collector replicas are standard in production (5–20 is common). Without routing, spans for one trace scatter randomly across replicas — no instance sees the full trace and none can make a correct policy decision.

The OTel Collector ships a `loadbalancing` exporter that hashes by trace-id and routes to a fixed replica. Discovery mechanisms: DNS round-robin, static list, or Kubernetes endpoint API.

**Operational gotchas:**
- **Scale events:** when a Collector replica is added or removed, the hash ring re-balances. Spans in flight for traces that hash to the rebalanced shard may arrive at the new replica before earlier spans — the tail sampler sees a partial trace and may decide prematurely. Mitigation: short decision windows (30s), graceful drain on scale-down.
- **Replica loss:** if a Collector replica dies mid-decision-window, its in-flight traces are lost. Tail sampling does not persist to disk by default. This is acceptable: losing traces during a Collector failure is a known operational trade-off.

<FlowDiagram
  label="trace-id routing → one collector sees the whole trace"
  perRow={4}
  hue="var(--d-systems)"
  nodes={[
    { id: "a1", label: "Span A1", sub: "trace=ab" },
    { id: "a2", label: "Span A2", sub: "trace=ab" },
    { id: "a3", label: "Span A3", sub: "trace=ab" },
    { id: "b1", label: "Span B1", sub: "trace=cd" },
    { id: "lb", label: "LB exporter", sub: "hash(trace-id)" },
    { id: "c2", label: "Collector 2", sub: "shard for ab" },
    { id: "c1", label: "Collector 1", sub: "shard for cd" },
    { id: "whole", label: "Full trace ab", sub: "A1+A2+A3" },
    { id: "decide", label: "Tail decision", sub: "keep / drop" },
  ]}
  edges={[
    { from: "a1", to: "lb" },
    { from: "a2", to: "lb" },
    { from: "a3", to: "lb" },
    { from: "b1", to: "lb" },
    { from: "lb", to: "c2", label: "hash(ab)" },
    { from: "lb", to: "c1", label: "hash(cd)" },
    { from: "c2", to: "whole" },
    { from: "whole", to: "decide" },
  ]}
  caption="The load-balancing exporter hashes by trace-id, so every span of trace ab (A1, A2, A3) lands on the same Collector (2); a different trace-id (cd) routes elsewhere. Only when one Collector holds the complete trace can the tail-sampler decide keep/drop correctly."
/>

## Preventing tail-sampler OOMs

A tail-sampler without a cap will buffer indefinitely until the process OOMs. The `tail_sampling` processor exposes `num_traces` as a hard cap on the number of active traces. When the cap is reached, the sampler begins evicting the oldest in-progress traces (incomplete traces are discarded).

Slack's 2023 incident: tail-sampling Collectors took down their tracing pipeline during a major incident because `num_traces` was uncapped. Postmortem added the cap, added a separate high-priority always-keep tier for critical traces, and added an alert on `otelcol_processor_dropped_spans_rate`.

**The full configuration pattern:**

```yaml
processors:
  tail_sampling:
    decision_wait: 30s
    num_traces: 100000           # hard cap — evict oldest if exceeded
    policies:
      - name: errors
        type: status_code
        status_code:
          status_codes: [ERROR]
      - name: slow-traces
        type: latency
        latency:
          threshold_ms: 2000
      - name: probabilistic
        type: probabilistic
        probabilistic:
          sampling_percentage: 1
```

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Alert metric</th>
        <th class="border border-border px-3 py-2 text-left">What it means</th>
        <th class="border border-border px-3 py-2 text-left">Threshold</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2 font-mono text-xs">otelcol_processor_tail_sampling_count_traces_on_memory</td>
        <td class="border border-border px-3 py-2">Active in-flight traces</td>
        <td class="border border-border px-3 py-2">Alert at 80% of num_traces cap</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2 font-mono text-xs">otelcol_processor_dropped_spans</td>
        <td class="border border-border px-3 py-2">Spans dropped due to cap</td>
        <td class="border border-border px-3 py-2">Alert at any non-zero rate</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2 font-mono text-xs">container_memory_working_set_bytes (Collector pod)</td>
        <td class="border border-border px-3 py-2">Actual RAM used</td>
        <td class="border border-border px-3 py-2">Alert at 85% of memory limit</td>
      </tr>
    </tbody>
  </table>
</div>

<TraceScenario
  id="05-consistency-trace-1"
  pieceSlug="05-sampling-consistency-and-collector"
  lang="en"
  scenario="Tail-sampling Collector OOMs every few hours. Trace the root cause."
  steps={[
    { prompt: "Step 1: tail-sampler OOM — what is the memory model?", reveal: "Tail sampler must hold all spans of every active trace in RAM until the decision window closes (typically 30s). Memory = active-traces × spans-per-trace × bytes-per-span. If any factor balloons, RAM goes. Start by looking at the Collector's own metrics to isolate which factor is growing." },
    { prompt: "Step 2: check the metrics — which factor is growing?", reveal: "Inspect otelcol_processor_tail_sampling_count_traces_on_memory and otelcol_processor_tail_sampling_count_spans_on_memory. If trace count is steady but spans-per-trace grows: one trace is collecting too many spans (long-running, recursive, batch). If trace count grows: traffic spike or decision window too long." },
    { prompt: "Step 3: spans-per-trace blew up — typical cause?", reveal: "A long-running trace (a batch job, a worker that processes 10,000 records under one trace-id) accumulates thousands of spans, each held until decision. Decision window 30s × 1000 spans/sec = 30,000 spans for one trace. Fix: don't make 10k-span traces; break the work into sub-traces with span-links, or use a separate trace-id per item." },
    { prompt: "Step 4: trace count blew up — typical cause?", reveal: "Either traffic spike (incident) or upstream is generating a unique trace-id per request without parent (no traceparent propagation, so each request looks new). Fix: scale collector replicas behind a load-balancing exporter that hashes by trace-id, or shorten the decision window if 30s is overkill." },
    { prompt: "Step 5: durable fix?", reveal: "Set a hard num_traces cap on the tail-sampling processor; configure load-balancing exporter (trace-id hash) so traces don't fan-in to one collector; alert on otelcol_processor_dropped_spans rate. Long-term: budget collector capacity per service / per region; treat collectors as standalone production infra with their own SLO." },
  ]}
/>

<NumbersCard
  id="05-consistency-numbers"
  title="Tail-sampling Collector reference numbers"
  stats={[
    { label: "W3C Trace Context Level 1", value: "Recommendation 2020-02" },
    { label: "W3C Trace Context Level 2 (random-trace-id flag)", value: "Recommendation 2024" },
    { label: "Typical tail decision window", value: "30s" },
    { label: "Collector RAM at 50k traces × 100 spans × 1 KB", value: "~5 GB" },
    { label: "Typical production Collector replicas", value: "5–20" },
    { label: "Routing key for load-balancing exporter", value: "trace-id (deterministic hash)" },
  ]}
/>

<Quiz
  id="05-consistency-quiz-1"
  lessonSlug="05-sampling-consistency-and-collector"
  lang="en"
  question="A service emits 5% orphan spans for internal (non-entry-point) services. The team adds tail sampling. The orphan rate stays. What's misunderstood?"
  choices={[
    { label: "Tail sampling decides which traces to KEEP — it can't repair broken propagation. Orphans are caused by missing parent_id at emission time; sampling only filters, it doesn't reconstruct.", correct: true },
    { label: "Tail sampling rejects orphans", misconception: "It can be configured to reject, but that doesn't fix the underlying broken propagation; it just hides the symptom." },
    { label: "5% is normal for internal services", misconception: "Healthy internal services should have an orphan rate near 0%; 5% indicates a real propagation bug." },
    { label: "OTel doesn't support orphan repair", misconception: "No tracing system can repair orphans; they must be prevented at propagation time." },
  ]}
/>

<Quiz
  id="05-consistency-quiz-2"
  lessonSlug="05-sampling-consistency-and-collector"
  lang="en"
  question="A Collector replica is removed during a scale-down event. What happens to traces whose decision window has not yet closed on that replica?"
  choices={[
    { label: "Those traces are lost — tail sampling does not persist to disk by default, and this is an accepted operational trade-off", correct: true },
    { label: "The load-balancing exporter re-routes their spans to a surviving replica", misconception: "The load-balancing exporter routes new spans, but does not migrate in-progress trace buffers from a dying replica to another." },
    { label: "The Collector flushes all in-progress decisions before shutdown", misconception: "Graceful drain flushes the export queue, but tail-sampling decision buffers are in RAM and are lost when the process exits." },
    { label: "The tracing backend reconstructs the trace from partial spans", misconception: "Tracing backends join spans by trace-id, but they can only join spans they received. If the Collector discarded them, the backend never saw them." },
  ]}
/>

</Explanation>

<KeyTakeaway>Consistent sampling uses deterministic trace-id hashing so all services independently agree on keep/drop without coordination — partial traces are never acceptable. The tail-sampling Collector's RAM is bounded by active-traces × spans × bytes × window; a hard num_traces cap prevents OOM. The load-balancing exporter is mandatory: without trace-id routing, tail sampling silently produces fragment-based decisions.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-consistency-retrieval"
  lang="en"
  questions={[
    { q: "How does deterministic trace-id hashing achieve consistent sampling without coordination between services?", a: "Every service independently computes the same deterministic function: hash(trace-id) mod 100 < sample_percentage. Because the trace-id is fixed across all services (propagated by traceparent) and the hash function is deterministic, all services produce the same boolean result without communicating. If the root service decides to drop a trace (hash result = false), every downstream service will also produce false for the same trace-id. The OTel SDK encodes the head decision in the sampled flag (traceparent trace-flags bit 0), so downstream services can short-circuit the hash computation and just read the flag — but even if they re-hash, they get the same answer. The W3C Trace Context Level 2 random-trace-id flag (bit 1) asserts that the trace-id is uniformly random, which is a prerequisite for the hash distribution to be uniform." },
    { q: "Explain the tail-sampler OOM failure mode and the three independent mitigations.", a: "OOM root cause: tail sampling holds all spans for every active trace in RAM until decision_wait expires. RAM = active_traces × avg_spans × bytes_per_span. Any factor growing unbounded → OOM. Three independent mitigations: 1. num_traces cap — the tail_sampling processor evicts the oldest incomplete trace when the cap is reached. This bounds the numerator (active trace count). Set the alert at 80% of cap so scaling can happen before eviction starts. 2. Break long traces — a batch job processing 10,000 records under one trace-id accumulates thousands of spans before decision. Use span-links to split long workflows into sub-traces, each fitting in one decision window. This bounds spans_per_trace. 3. Scale collector replicas behind the load-balancing exporter — distributes RAM load horizontally. Each replica handles a disjoint subset of trace-ids, so adding replicas halves the per-instance active-trace count. This distributes active_traces across more instances." },
    { q: "Why must the load-balancing exporter use trace-id hashing rather than round-robin or least-connections?", a: "Round-robin and least-connections distribute spans evenly by count or load, but they scatter the spans of one trace across different replicas. The tail sampler on each replica sees a fragment — some spans for the trace-id but not all. Fragment-based decisions are wrong: an error span might land on replica A, the root span might land on replica B. Replica A keeps the trace (error policy), replica B drops it (no error seen). The 'keep' decision from A produces partial spans; the 'drop' decision from B discards the rest. The resulting stored trace is a fragment. Trace-id hashing is deterministic: all spans for one trace-id always map to the same replica, regardless of timing. The tail sampler on that replica accumulates the complete set and makes a correct policy decision. The routing key must be trace-id — no other routing strategy produces correct tail-sampling semantics." },
  ]}
/>

<Recap lang="en">
Consistent sampling uses deterministic hash(trace-id) mod 100 so all services independently agree on keep/drop — partial traces never occur. The W3C Level 2 `random-trace-id` flag makes this safe by asserting uniform trace-id distribution. The tail-sampling Collector's RAM is `active_traces × spans/trace × bytes/span × decision_window`; every factor is an independent OOM lever. A hard `num_traces` cap prevents unbounded growth; alerts on `otelcol_processor_dropped_spans` catch when the cap fires. The load-balancing exporter must use trace-id hashing — round-robin scatters a trace's spans across replicas, making policy decisions wrong. Long-running traces (batch jobs accumulating thousands of spans) must be broken into sub-traces linked via span-links to stay within the decision window. Now when your tail-sampling collector starts climbing in memory, you know exactly which metric to check first and which configuration knob to turn — because you understand the formula, not just the symptom.
</Recap>
