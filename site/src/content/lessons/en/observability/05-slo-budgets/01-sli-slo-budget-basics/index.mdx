---
concepts:
- sli
- slo
- error-budget
- burn-rate
deepensInto: []
estMin: 12
lang: en
lessonType: topic
level: junior
order: 1
prereqs:
- observability/04-red-use/06-histograms-slo-and-advanced
slug: 01-sli-slo-budget-basics
sources:
- https://sre.google/workbook/implementing-slos/
- https://sre.google/sre-book/embracing-risk/
- https://sre.google/workbook/error-budget-policy/
spiral:
- statefulness
status: ready
summary: An SLO is a promise about how reliable your service will be, and an error
  budget is the leftover failure allowance you can spend before that promise breaks
  — together they end arguments about ''''how reliable is reliable enough?
title: 'SLI, SLO, and the error budget: reliability by the numbers'
track: observability
unit: 05-slo-budgets
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";

<Hook>
A product manager asks: "Can we ship the new checkout flow this week?" Without an SLO, the answer is a feeling. With one, it is arithmetic — the error budget either has headroom or it does not.
</Hook>

<Crux>SLI = good/total. SLO = target SLI. Error budget = 1 − SLO. Burn rate = how fast you're spending it.</Crux>

<Explanation>

## What the three terms actually mean

**SLI (Service Level Indicator)** is a ratio: the fraction of requests (or events) that were "good" out of everything that was attempted. For a request-driven service, "good" typically means: returned a successful status code, within an acceptable latency. The formula is always the same shape — good events / total events — and the result is a number between 0% and 100%.

**SLO (Service Level Objective)** is the target you commit to for that SLI. If you pick 99.9%, you are saying: at least 99.9% of requests must be good. Everything above the target is surplus. Everything below is a failure.

**Error budget** is what remains under the target: 1 − SLO. A 99.9% SLO leaves a 0.1% budget of failures you are *allowed* to produce before the SLO is breached.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Term</th>
        <th class="border border-border px-3 py-2 text-left">Formula</th>
        <th class="border border-border px-3 py-2 text-left">Example (99.9% SLO, 30 days)</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2 font-semibold">SLI</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">good_events / total_events</td>
        <td class="border border-border px-3 py-2">999,000 / 1,000,000 = 99.9%</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2 font-semibold">SLO</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">target for the SLI</td>
        <td class="border border-border px-3 py-2">&gt;= 99.9% of requests must succeed</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2 font-semibold">Error budget</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">1 − SLO</td>
        <td class="border border-border px-3 py-2">0.1% = 1,000 failures per million, or ~43 minutes of downtime</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2 font-semibold">Burn rate</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">actual_error_rate / (1 − SLO)</td>
        <td class="border border-border px-3 py-2">1.0% error rate = 10x burn (exhausts budget in 3 days)</td>
      </tr>
    </tbody>
  </table>
</div>

## The data-plan metaphor

Think of the error budget like a monthly mobile data plan. A 99.9% SLO over 30 days has the same shape as "10 GB per month": you start the period with a fixed allowance, you spend it as events happen, and if you run out before the period ends, you slow down — no new feature deploys — until the next billing cycle resets.

A small incident is like a 30-second video: barely a dent. A two-hour outage is like streaming HD all weekend: most of the month's quota gone. Teams that are heavy spenders ration carefully; light spenders ship freely. The budget is a real resource, not a metaphor in the system — when it goes negative, real consequences follow.

## Burn rate: the derived quantity that matters most

Burn rate normalises the error rate against the SLO target:

`burn_rate = actual_error_rate / (1 − SLO)`

At the 99.9% SLO (budget = 0.1% = 0.001):
- 0.1% error rate → burn rate 1.0: you would exhaust the budget exactly by month-end
- 1.44% error rate → burn rate 14.4: you would exhaust the 30-day budget in about 2 days

A burn rate of 1 means "sustainable." Above 1 means you are on pace to miss the SLO. The burn rate is what alerts and dashboards show, because it is the same number regardless of the SLO target or traffic volume — 14.4x means the same thing on any service.

## A concrete scenario

A SaaS team sets a 99.5% SLO. Over a month they serve 10 million requests; 0.5% = 50,000 errors allowed. A config bug in week one burns 30,000 errors in 20 minutes — 60% of the monthly budget in one incident. The burn-rate alert fires; the postmortem uses the burned budget as the severity metric. The remaining 40% covers the next 20 days only at the baseline error rate — no room for a risky deploy. The team ships the planned feature behind a feature flag instead.

<Inset kind="why" lang="en">
Without an SLO, "should we ship?" is a political argument — whoever has more authority wins. With an SLO and an error budget, the answer is arithmetic: the budget either has headroom or it does not. This is the single most leveraged cultural shift the framework creates. It turns engineering reliability into a language product managers, SREs, and executives can all read.
</Inset>

<Quiz
  id="01-sli-slo-budget-basics-quiz-1"
  lessonSlug="01-sli-slo-budget-basics"
  lang="en"
  question="A service has a 99.9% availability SLO over 30 days. What is the error budget?"
  choices={[
    { label: "0.1% of requests — roughly 43 minutes of downtime per month", correct: true },
    { label: "99.9% of requests — the SLO target itself", misconception: "The budget is what remains *below* the target: 1 − SLO. The SLO is the goal, not the allowance." },
    { label: "Zero — 99.9% means almost no errors allowed", misconception: "99.9% still allows 0.1%, which is 1,000 errors per million requests — meaningful at scale." },
    { label: "It depends on the SLA contract with customers", misconception: "The error budget derives from the SLO (internal engineering target), not the SLA (customer contract). They are different concepts." },
  ]}
/>

<Quiz
  id="01-sli-slo-budget-basics-quiz-2"
  lessonSlug="01-sli-slo-budget-basics"
  lang="en"
  question="A service has a 99.9% SLO and is currently running at a 1.44% error rate. What is the burn rate?"
  choices={[
    { label: "14.4x — the team would exhaust the 30-day budget in about 2 days at this rate", correct: true },
    { label: "1.44x — the burn rate equals the error rate percentage", misconception: "Burn rate = error_rate / (1 − SLO). With SLO = 0.999, budget = 0.001, burn rate = 0.0144 / 0.001 = 14.4x." },
    { label: "0.144x — below 1, so the budget is fine", misconception: "Dividing in the wrong direction. A 1.44% error rate at a 99.9% SLO is 14.4 times the budget rate." },
    { label: "Burn rate is not computable without traffic volume", misconception: "Burn rate is a ratio of rates — traffic volume cancels out. It is always computable from error rate and SLO." },
  ]}
/>

<DragOrder
  id="01-sli-slo-budget-basics-drag-1"
  lessonSlug="01-sli-slo-budget-basics"
  lang="en"
  prompt="Order the steps of building an SLO from scratch:"
  items={[
    { id: "a", label: "Identify the user journey that matters (checkout, search, login)" },
    { id: "b", label: "Pick an SLI: a measurable good/total ratio (e.g. successful_requests / total_requests)" },
    { id: "c", label: "Set the SLO: a target percentage for that ratio (e.g. 99.9%)" },
    { id: "d", label: "Compute the error budget: 1 − SLO, over a rolling window (typically 28 days)" },
    { id: "e", label: "Instrument multi-window multi-burn-rate alerts on top of the SLO" },
    { id: "f", label: "Write the error budget policy: what happens if the budget is exhausted" },
    { id: "g", label: "Review the SLO quarterly: tighten, loosen, or change the SLI based on real user impact" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g"]}
/>

<MetaphorComplete
  id="01-sli-slo-budget-basics-metaphor-1"
  pieceSlug="01-sli-slo-budget-basics"
  lang="en"
  setup="Fill in the blank: the error _______ is the failure allowance you can spend before a deployment freeze is triggered."
  accepted={["budget", "budgets"]}
  canonical="budget (often called the error budget)"
  explanation="The phrase 'error budget' is the standard SRE term for the allowance that remains under the SLO target. Treating it like a budget lets teams choose between shipping risk and preserving headroom, using a number everyone agrees on."
/>

</Explanation>

<KeyTakeaway>SLI = good/total. SLO = the target. Error budget = 1 − SLO. Burn rate = how fast the budget drains. When the budget runs out, feature deploys halt — so the budget is what the team trades for reliability.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-sli-slo-budget-basics-retrieval"
  lang="en"
  questions={[
    { q: "In one sentence each, what is an SLI, an SLO, and an error budget?", a: "An SLI is a measurable good-over-total ratio that quantifies how well a service is serving users. An SLO is the target value for that SLI — a percentage the team commits to. An error budget is what remains below the target: the share of failures the team is allowed to produce before the SLO is breached." },
    { q: "Why is burn rate more useful than raw error rate for alerting?", a: "Burn rate normalises the error rate against the SLO budget, so 14.4x means the same thing on any service regardless of traffic volume or SLO target. A raw error rate of 0.5% is alarming on a 99.9% SLO (500x the budget rate) but fine on a 95% SLO. Burn rate makes both cases read '500x' and '0.1x' respectively — unambiguous on a single axis." },
    { q: "What happens when the error budget reaches zero?", a: "The error budget policy kicks in: feature deployments halt (except P0 bug fixes and security patches), and the team focuses on reliability work until the budget regenerates above the policy threshold — typically 50% remaining. The freeze enforces the contract that was agreed up front, turning 'should we deploy?' from a political argument back into arithmetic." },
  ]}
/>

<Recap lang="en">
An SLI is the measurable ratio — good events over total events — that tells you how the service is performing from the user's perspective. The SLO sets the target for that ratio: a 99.9% SLO means 99.9% of events must be good. The error budget is the 0.1% of failures you are allowed before the SLO is breached, converted into concrete numbers: at 1 million requests per month, that is 1,000 failures, or roughly 43 minutes of downtime. Burn rate normalises the current error rate against the budget rate — a burn rate of 14.4x means the 30-day budget would be gone in 2 days — making alerts and dashboards comparable across any service. When the budget is exhausted, the error budget policy halts feature deploys until it regenerates.
</Recap>
