---
concepts:
- error-budget
- burn-rate
- mwmbr
- composite-slo
- error-budget-policy
- sla-vs-slo
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://sre.google/workbook/implementing-slos/
- https://sre.google/workbook/alerting-on-slos/
- https://sre.google/workbook/error-budget-policy/
status: ready
summary: "Multiple-choice synthesis across the SLO unit — budget arithmetic, burn-rate derivation, MWMBR AND logic, composite ceilings, and the organisational failure modes."
title: 'SLO and error budgets: multiple-choice review'
track: observability
unit: 05-slo-budgets
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one is a decision you make in a real reliability review — not a definition to recite, but a number to compute and a tradeoff to defend.
</Hook>

<Goal lang="en">
Confirm you can connect SLI selection, budget arithmetic, burn-rate derivation, MWMBR alerting, composite-journey math, and the organisational layer — the synthesis the eight lessons built toward.
</Goal>

<Quiz
  id="05-slo-budgets-choice-1"
  lessonSlug="05-slo-budgets"
  lang="en"
  question="A service runs a 99.9% availability SLO over a 28-day window at 1M requests/day. Fourteen days in it has served 14M requests with 21,000 failures. How healthy is the budget?"
  choices={[
    { label: "Budget mostly gone: the 28-day budget is 0.001 × 28M = 28,000 failures, and 21,000 is 75% spent at the halfway mark — burning at ~1.5x, on track to breach before the window closes.", correct: true },
    { label: "Fine — 21,000 is below the 28,000 total, so there is headroom.", misconception: "Position in the window matters. At day 14 the expected spend is half the budget (14,000). 21,000 spent is 75% of the budget with 50% of the window remaining — a ~1.5x burn that exhausts the budget early." },
    { label: "Trigger an immediate freeze — the budget is over halfway gone.", misconception: "Most policies freeze at zero remaining, not at 75% spent. The team should slow risky deploys and watch the burn, but the freeze trigger has not been reached yet." },
    { label: "Cannot tell without the SLA contract value.", misconception: "The error budget derives from the SLO and the traffic, not the SLA. Budget = (1 − SLO) × total_events; the SLA is a separate, looser customer commitment." },
  ]}
/>

<Quiz
  id="05-slo-budgets-choice-2"
  lessonSlug="05-slo-budgets"
  lang="en"
  question="An on-call team copies a 1h+5m page rule from a 99.9% SLO service — expr (1 − ratio_rate1h) &gt; (14.4 × 0.001) — onto a service whose SLO is 99.5%, leaving the expression unchanged. What breaks?"
  choices={[
    { label: "The threshold is now wrong: at a 99.5% SLO the budget rate is 0.005, so 14.4x burn means an error rate of 14.4 × 0.005 = 7.2%. The hard-coded 0.001 makes the alert fire at 1.44% — far too sensitive, paging on normal variation.", correct: true },
    { label: "Nothing — the 14.4x burn-rate threshold is independent of the SLO target.", misconception: "The burn multiplier (14.4x) is the same, but the error-rate threshold it maps to is burn × (1 − SLO). The copied 0.001 encodes a 99.9% budget; for 99.5% it must become 0.005, or the alert fires ~5x too eagerly." },
    { label: "The alert never fires because 99.5% is a looser target.", misconception: "It fires more, not less. The leftover 0.001 sets the firing point at a 1.44% error rate, which a 99.5%-SLO service exceeds routinely — the opposite of never firing." },
    { label: "Only the short (5m) window needs updating; the 1h window inherits the target automatically.", misconception: "Both windows compare against the same hard-coded budget rate. This is exactly why hand-rolled PromQL is error-prone and platforms like Sloth generate it from the declared target." },
  ]}
/>

<Quiz
  id="05-slo-budgets-choice-3"
  lessonSlug="05-slo-budgets"
  lang="en"
  question="An MWMBR page rule fires when the 1h burn AND the 5m burn both exceed 14.4x. During an incident the 1h burn is 18x but the 5m burn has dropped to 9x. Should it page, and what does each window tell you?"
  choices={[
    { label: "No page: the AND is false. The 1h window confirms a severe sustained burn just happened, but the 5m window says it is no longer ongoing — the incident is clearing, so waking someone adds no value.", correct: true },
    { label: "Yes — the 1h burn at 18x is well over threshold and that is the authoritative signal.", misconception: "MWMBR deliberately requires BOTH windows. The short window is the 'is it still happening now?' gate; with it below threshold the AND is false and the page suppresses, avoiding a wake-up for an already-recovering incident." },
    { label: "Yes — 9x on the 5m window is still a high burn rate.", misconception: "9x is high in absolute terms but below the 14.4x page threshold. The AND needs both windows over the line; the falling short window is the recovery signal that keeps the page quiet." },
    { label: "It depends on the Alertmanager 'for' duration, not the windows.", misconception: "The AND between the two windows lives in the PromQL expression itself. 'for' is a separate debounce mechanism and does not implement the dual-window logic." },
  ]}
/>

<Quiz
  id="05-slo-budgets-choice-4"
  lessonSlug="05-slo-budgets"
  lang="en"
  question="A checkout journey traverses API gateway, auth, inventory, payment, and database, each holding an independent 99.9% SLO, all green. The user-facing checkout success rate sits at ~99.5%. What is the correct reading and fix?"
  choices={[
    { label: "Composite SLOs multiply: a request is good only if all five succeed, so the ceiling is 0.999^5 ≈ 99.5%, not 99.9%. Add a journey-level SLI at the gateway (successful checkouts / attempts) and rank per-service failure rates to find the dominant 1-2 contributors.", correct: true },
    { label: "One of the per-service dashboards is lying — 99.9% across 5 should still be ~99.9%.", misconception: "Per-service SLOs compose as a product, not an average or a max. Five independent 99.9% services give 0.999^5 ≈ 99.5%; the dashboards are all correct, they just measure the wrong layer for the user journey." },
    { label: "Tighten every service to 99.99% so the journey reaches 99.9%.", misconception: "That works mathematically (0.9999^5 ≈ 99.95%) but is the expensive lever — each extra nine costs 3-10x. The cheaper fix is a journey-level SLI plus targeting the Pareto-dominant contributors, or idempotent retries / hedging on the worst hop." },
    { label: "The 28-day window is hiding the failures; shorten it to 7 days.", misconception: "Window length changes detection speed, not the composite ceiling. The journey is structurally capped at ~99.5% regardless of window; only journey-level measurement and architectural levers raise it." },
  ]}
/>

<Quiz
  id="05-slo-budgets-choice-5"
  lessonSlug="05-slo-budgets"
  lang="en"
  question="A checkout availability SLI counts any 2xx as 'good'. It is steadily green, yet customers report duplicate charges and 30-second 'successful' checkouts. Why does the SLO miss this, and what is the senior fix?"
  choices={[
    { label: "It is an iceberg SLI: a duplicate charge and a 30s success both return 2xx, so HTTP status hides correctness and latency failures. Fix: derive SLIs from bad user outcomes — add a latency SLI and a uniqueness/correctness SLI from reconciliation — and join them worst-of.", correct: true },
    { label: "The SLO target is too lenient; tightening to 99.99% would surface the duplicates.", misconception: "No target catches a failure the SLI never observes. A 99.999% SLO with a 2xx-only SLI still scores duplicate charges as 'good'. The defect is the indicator, not the objective." },
    { label: "The burn-rate alert thresholds are mis-set; 14.4x should be 6x.", misconception: "Thresholds are irrelevant when the SLI does not count the failure. If correctness errors are invisible to the indicator, no alert tuning will ever surface them." },
    { label: "The composite math is wrong — the five services should be measured separately.", misconception: "Composition is a different problem. Here a single SLI fails to track correctness and latency; the fix is multiple user-outcome SLIs joined worst-of, not re-slicing per service." },
  ]}
/>

<Quiz
  id="05-slo-budgets-choice-6"
  lessonSlug="05-slo-budgets"
  lang="en"
  question="Six months after rolling SLOs to 80 services with correctly generated MWMBR alerts and a signed error budget policy, half the teams ignore the pages and budgets go negative without freezes. What is the most likely root cause?"
  choices={[
    { label: "Organisational, not technical: the SLIs do not correlate with user pain (alerts fire on signals users never feel, so teams learn the page is noise) and the policy is being overridden without enforcement — the GitHub-2023 / unenforced-policy failure pattern.", correct: true },
    { label: "The burn-rate thresholds are too low; raising 14.4x to 20x would cut the noise.", misconception: "If the SLI is wrong, no threshold fixes it — a perfectly calibrated burn rate on a meaningless indicator still pages for non-issues, and raising thresholds delays real detection. The fault is the SLI definition and policy enforcement." },
    { label: "Migrating from Sloth to a commercial platform like Nobl9 would fix adoption.", misconception: "Better tooling does not fix an organisational bottleneck. Teams that switch tools without fixing the SLI and the policy get the same dashboards nobody acts on. Fix the contract first, then the tools." },
    { label: "The 28-day window is too long; a 7-day window would force action.", misconception: "Shortening the window changes freeze cadence, not whether alerts are relevant or whether the policy has teeth. Negative budgets with no freeze is an enforcement gap, not a window-length problem." },
  ]}
/>

<Recap lang="en">
The unit's through-line is one chain of arithmetic made organisational: SLI = good/total (and it must track user pain, not 2xx), SLO is the target, error budget = (1 − SLO) × events, and burn rate normalises the spend. Alerting derives from burn = (budget_fraction × period) / window, fired with a long-AND-short window so it is both noise-resistant and fast to reset; remember to rebase the budget rate when the SLO target changes. Journeys multiply (0.999^5 ≈ 99.5%), iceberg SLIs hide correctness behind 2xx, SLO must sit tighter than SLA, and the framework only delivers value when a signed, enforced policy turns the number into a decision.
</Recap>
