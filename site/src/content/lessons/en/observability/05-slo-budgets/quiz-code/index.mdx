---
concepts:
- promql-burn-rate
- mwmbr-rule
- latency-bucket-sli
- nan-denominator
- sloth-declaration
estMin: 14
lang: en
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://sre.google/workbook/alerting-on-slos/
- https://prometheus.io/docs/practices/histograms/
- https://sloth.dev/
status: ready
summary: "Read real PromQL recording rules, MWMBR alert expressions, a latency-bucket SLI, and a Sloth YAML, then predict the behaviour and pick the highest-leverage fix."
title: 'SLO and error budgets: PromQL and rule reading'
track: observability
unit: 05-slo-budgets
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
SLO bugs hide in the PromQL, not in the slides. A wrong bucket, a hard-coded budget rate, a NaN denominator — each silently corrupts the budget and the alert. Read the rule and choose the fix a senior engineer would make first.
</Hook>

<Goal lang="en">
Practise reading the artefacts SLOs are actually built from: recording rules, burn-rate alert expressions, latency-bucket SLIs, and the YAML a platform generates them from — then spot the defect.
</Goal>

## Snippet 1 — the burn-rate page rule

```yaml
# 99.9% SLO. Intended: page when 1h AND 5m burn both exceed 14.4x.
alert: SLOAvailabilityBurnFast
expr: |
  (1 - job:slo_availability:ratio_rate1h) > (14.4 * 0.001)
  or
  (1 - job:slo_availability:ratio_rate5m) > (14.4 * 0.001)
labels:
  severity: page
```

<Quiz
  id="05-slo-budgets-code-1"
  lessonSlug="05-slo-budgets"
  lang="en"
  question="The author wanted MWMBR behaviour. What is the bug and what does it cause in production?"
  choices={[
    { label: "The operator is `or`, not `and`. With OR, the 5m window alone fires the page on every transient spike (noise), and the 1h window alone keeps it firing for ~55 minutes after the fix — you get the worst of both. Change `or` to `and`.", correct: true },
    { label: "The budget rate 0.001 is wrong for a 99.9% SLO.", misconception: "0.001 is exactly the budget rate for a 99.9% SLO (1 − 0.999). The defect is the OR operator: MWMBR requires AND so that the long window confirms severity while the short window confirms it is still ongoing." },
    { label: "The 5m window should use a different burn threshold than the 1h window.", misconception: "In canonical MWMBR the short window uses the same burn threshold as its paired long window — it is the 'still happening now?' gate, not a different severity. The real bug is OR vs AND." },
    { label: "Nothing — OR is the standard MWMBR composition.", misconception: "MWMBR is defined by AND between a long and a short window. OR is the failed Approach 4 from the SRE workbook precisely because it combines the noise of the short window with the slow reset of the long window." },
  ]}
/>

## Snippet 2 — the latency SLI

```promql
# Latency SLO target: 99% of requests under 200ms.
# Histogram buckets defined as le: 0.1, 0.25, 0.5, 1, 2.5
latency_sli =
  sum(rate(http_request_duration_seconds_bucket{le="0.25"}[1h]))
  /
  sum(rate(http_request_duration_seconds_count[1h]))
```

<Quiz
  id="05-slo-budgets-code-2"
  lessonSlug="05-slo-budgets"
  lang="en"
  question="The SLO threshold is 200ms but the nearest bucket is le=0.25. What is wrong, and what is the fix?"
  choices={[
    { label: "There is no bucket boundary at 0.2s, so this counts requests under 250ms, not 200ms — the SLI overstates fastness and drains the budget too slowly. Fix: add a le=0.2 bucket exactly at the SLO threshold so the counter is exact, with no histogram_quantile estimate.", correct: true },
    { label: "The expression should use histogram_quantile(0.99, ...) to get the real p99.", misconception: "A latency SLO is a counter ratio (fast / total), not a percentile estimate. histogram_quantile interpolates within a bucket and introduces error that corrupts the budget. The fix is a bucket boundary exactly at the threshold, then count directly." },
    { label: "The [1h] range is too long for a latency SLI.", misconception: "The range affects smoothing, not correctness. The actual defect is the missing bucket at 0.2s, forcing the SLI to use the 0.25 boundary and misclassify 200-250ms requests as 'fast'." },
    { label: "Nothing — le=0.25 is close enough to 200ms.", misconception: "'Close enough' silently biases the budget: every request between 200ms and 250ms counts as good when it should be a budget burn. The SLO threshold and a histogram bucket boundary must coincide exactly." },
  ]}
/>

## Snippet 3 — the gctrace-free NaN

```promql
# Recording rule for a low-traffic internal service.
record: job:slo_availability:ratio_rate5m
expr: |
  sum(rate(http_requests_total{status!~"5..",job="reports"}[5m]))
  /
  sum(rate(http_requests_total{job="reports"}[5m]))
# During a quiet window, this series evaluates to NaN.
```

<Quiz
  id="05-slo-budgets-code-3"
  lessonSlug="05-slo-budgets"
  lang="en"
  question="The ratio goes NaN during quiet periods on this low-traffic service. What does that mean for the SLO, and what is the senior remedy?"
  choices={[
    { label: "Zero traffic makes the denominator zero, so the ratio is NaN — the burn-rate alert then neither fires nor clears, silently breaking the SLO. Remedy: add synthetic probes to keep the denominator non-zero, and alert separately when sum(rate(... total ...)) == 0.", correct: true },
    { label: "NaN means the service hit 100% availability with no errors.", misconception: "NaN is not 100% — it is 0/0 from an absent denominator. The alert evaluates against NaN and stays in an indeterminate state, which is worse than a false negative because nobody notices." },
    { label: "The fix is to lower the SLO target so the ratio stays defined.", misconception: "The target is unrelated to a zero denominator. The structural fix for low-traffic NaN is synthetic probes (stabilise the denominator), aggregation across related services, or a longer evaluation window — plus a separate no-traffic alert." },
    { label: "Switch from rate() to increase() and the NaN disappears.", misconception: "increase() over a window of no traffic still yields zero, so the ratio is still 0/0. The root cause is no traffic in the denominator, not the choice of rate vs increase." },
  ]}
/>

## Snippet 4 — the Sloth declaration

```yaml
version: "prometheus/v1"
service: checkout
slos:
  - name: availability
    objective: 99.5
    sli:
      events:
        error_query: sum(rate(http_requests_total{status=~"5..",job="checkout"}[{{.window}}]))
        total_query: sum(rate(http_requests_total{job="checkout"}[{{.window}}]))
    alerting:
      page_alert:   { labels: { severity: page } }
      ticket_alert: { labels: { severity: ticket } }
```

<Quiz
  id="05-slo-budgets-code-4"
  lessonSlug="05-slo-budgets"
  lang="en"
  question="A teammate argues this is risky because you must hand-maintain six recording rules and three MWMBR alerts per service. Why is that argument wrong here?"
  choices={[
    { label: "Sloth generates all six ratio_rate recording rules (one per window) and the three MWMBR alerts with correct AND logic and burn thresholds rebased for the 99.5% objective — from this one declaration. You declare the SLI and target once; the math cannot drift per service.", correct: true },
    { label: "It is still risky — Sloth cannot rebase the burn thresholds for a non-99.9% objective like 99.5%.", misconception: "Rebasing is exactly what the generator does: it derives the per-objective budget rate (here 0.005) and emits correct alert expressions. Hand-rolling is what gets the 0.005 wrong; Sloth does not." },
    { label: "The teammate is right; you should write the PromQL by hand for auditability.", misconception: "Hand-written PromQL across many services is the documented source of silent SLO bugs (wrong window, wrong budget rate). Declarative generation gives auditability via the YAML plus consistent, correct output." },
    { label: "Sloth only generates dashboards, not alerts, so the rules are still manual.", misconception: "Sloth emits Prometheus recording rules AND MWMBR alert rules (plus optional Grafana dashboards) from the declaration. The whole alerting stack is generated, not just dashboards." },
  ]}
/>

<Recap lang="en">
Every SLO defect is read in the rules: MWMBR needs `and` between a long and short window (OR is the failed Approach 4); a latency SLI needs a histogram bucket exactly at the SLO threshold or it silently miscounts and biases the budget; a zero denominator on low-traffic services yields NaN that neither fires nor clears, so add synthetic probes plus a no-traffic alert; and a platform like Sloth generates all six recording rules and three MWMBR alerts — with the budget rate correctly rebased to the objective — from one YAML, which is exactly why hand-rolled PromQL is the bug source.
</Recap>
