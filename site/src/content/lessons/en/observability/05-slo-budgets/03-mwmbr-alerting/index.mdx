---
concepts:
- mwmbr
- burn-rate-alert
- slo-alerting
- dual-window
deepensInto: []
estMin: 15
lang: en
lessonType: topic
level: middle
order: 3
prereqs:
- observability/05-slo-budgets/02-sli-selection-and-slo-targets
slug: 03-mwmbr-alerting
sources:
- https://sre.google/workbook/alerting-on-slos/
- https://grafana.com/blog/how-to-implement-multi-window-multi-burn-rate-alerts-with-grafana-cloud/
- https://www.datadoghq.com/blog/burn-rate-is-better-error-rate/
spiral:
- latency
status: ready
summary: Single-window SLO alerts are either too noisy (short window) or too slow
  to clear (long window) — the MWMBR pattern''''s AND between a long and a short window
  gets both fast detection and fast reset.
title: 'Multi-window multi-burn-rate alerting: why AND beats OR'
track: observability
unit: 05-slo-budgets
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
An on-call team uses a single alert: "fire if the error rate over the past hour exceeds 1.44%." The pager fires at 2 AM. The engineer logs in. The issue cleared itself 55 minutes ago — but the 1-hour window still contains the bad data. The engineer stays awake for nothing.
</Hook>

<Crux>MWMBR fires only when burn rate is high AND still ongoing — long window confirms severity, short window confirms the fire is live.</Crux>

<Explanation>

## Why naive SLO alerts fail in two ways

Before you tune alert thresholds, ask a harder question: does your alert structure even allow fast recovery? Most teams find out the answer at 2 AM. The SRE workbook classifies six approaches to SLO alerting. Five fail in predictable ways.

**Approach 1 (raw error rate &gt; SLO threshold):** fires hundreds of times per day on tiny blips. A single bad request batch triggers the page; teams learn to ignore the pager — the classic alert fatigue death spiral.

**Approach 5 (single burn-rate threshold, e.g. 14.4x over 1h):** better — catches real outages — but the reset is slow. After the incident is resolved, the 1-hour window still contains 55 minutes of bad data. The alert keeps firing for nearly an hour post-fix. Engineers cannot tell if their change helped.

**Approach 6 (MWMBR)** is the only one that balances detection latency, noise resistance, and recovery latency. It is the production default in Google, Datadog, Grafana, Splunk, Honeycomb, Sloth, and Pyrra.

## The dual-window trick

The key insight: combine a **long window** (noise resistance) AND a **short window** (recovery resolution):

- Long window (e.g. 1h): confirms the burn is sustained, not a transient spike
- Short window (e.g. 5m): confirms the burn is **still happening right now**

The AND logic:
- If only the long window is high: the incident probably ended; the short window cleared
- If only the short window is high: a brief spike that has not yet accumulated into a sustained burn
- If **both** are high: a real outage that is both severe and still active — page

When the incident resolves, the short window clears within 5 minutes. The alert resets within 5 minutes instead of within 55 minutes. Detection time and reset time are both bounded.

<DiagramFrame
  label="Two burn-rate window checks feed an AND gate. The fast window (1h at over 14.4x burn) is tripped and the slow window (6h at over 6x burn) is tripped; because both are true, the AND gate outputs a live PAGE. If either window were below its threshold, the AND would be false and no alert would fire."
  hue="var(--d-systems)"
  caption="Both windows must trip for the AND to fire: the slow 6h window confirms the burn is severe and sustained, the fast 1h window confirms it is still live. Either window alone leaves the AND false, suppressing flapping and noise."
>
  <svg viewBox="0 0 560 260" font-family="var(--font-body)">

    <rect x="24" y="36" width="184" height="78" rx="6" fill="var(--card-2)" stroke="var(--accent)" stroke-width="1.4" />
    <text x="116" y="60" text-anchor="middle" font-size="12.5" fill="var(--ink)">Fast window</text>
    <text x="116" y="80" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--muted)">1h · burn &gt; 14.4x</text>
    <text x="116" y="100" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--accent)">TRIPPED (true)</text>


    <rect x="24" y="146" width="184" height="78" rx="6" fill="var(--card-2)" stroke="var(--accent)" stroke-width="1.4" />
    <text x="116" y="170" text-anchor="middle" font-size="12.5" fill="var(--ink)">Slow window</text>
    <text x="116" y="190" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--muted)">6h · burn &gt; 6x</text>
    <text x="116" y="210" text-anchor="middle" font-size="11" font-family="var(--font-mono)" fill="var(--accent)">TRIPPED (true)</text>


    <path d="M208 75 H300 V112 H336" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" />
    <path d="M208 185 H300 V148 H336" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" />


    <path d="M336 96 H372 A32 32 0 0 1 372 164 H336 Z" fill="var(--card)" stroke="var(--ink)" stroke-width="1.6" />
    <text x="356" y="134" text-anchor="middle" font-size="13" fill="var(--ink)">AND</text>


    <path d="M404 130 H452" fill="none" stroke="var(--hairline-strong)" stroke-width="1.4" />


    <rect x="452" y="104" width="92" height="52" rx="6" fill="var(--accent)" />
    <text x="498" y="128" text-anchor="middle" font-size="12.5" fill="var(--card)">PAGE</text>
    <text x="498" y="146" text-anchor="middle" font-size="10" fill="var(--card)">fires now</text>

    <text x="356" y="196" text-anchor="middle" font-size="10" fill="var(--muted)">both true → fire</text>
  </svg>
</DiagramFrame>

## The canonical MWMBR configuration (99.9% SLO)

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Severity</th>
        <th class="border border-border px-3 py-2 text-left">Long window</th>
        <th class="border border-border px-3 py-2 text-left">Short window</th>
        <th class="border border-border px-3 py-2 text-left">Burn rate</th>
        <th class="border border-border px-3 py-2 text-left">Budget consumed if sustained</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2 font-semibold text-red-600">Page</td>
        <td class="border border-border px-3 py-2">1h</td>
        <td class="border border-border px-3 py-2">5m</td>
        <td class="border border-border px-3 py-2 font-mono">&gt;14.4x</td>
        <td class="border border-border px-3 py-2">2% in 1h — fast outage, urgent</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2 font-semibold text-orange-500">Page</td>
        <td class="border border-border px-3 py-2">6h</td>
        <td class="border border-border px-3 py-2">30m</td>
        <td class="border border-border px-3 py-2 font-mono">&gt;6x</td>
        <td class="border border-border px-3 py-2">5% in 6h — moderate sustained burn</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2 font-semibold text-yellow-600">Ticket</td>
        <td class="border border-border px-3 py-2">3d</td>
        <td class="border border-border px-3 py-2">6h</td>
        <td class="border border-border px-3 py-2 font-mono">&gt;1x</td>
        <td class="border border-border px-3 py-2">10% in 3d — slow burn, needs attention</td>
      </tr>
    </tbody>
  </table>
</div>

<NumbersCard
  id="03-mwmbr-numbers"
  title="MWMBR canonical numbers at 99.9% SLO"
  stats={[
    { label: "14.4x burn rate = error rate of", value: "1.44% (at 99.9% SLO)" },
    { label: "14.4x for 1h = budget consumed", value: "~2% of the 28-day budget" },
    { label: "6x for 6h = budget consumed", value: "~5% of the 28-day budget" },
    { label: "1x for 3d = budget consumed", value: "~10% of the 28-day budget" },
    { label: "Alert reset time (5m short window)", value: "&lt;5 minutes after fix" },
    { label: "Alert reset time (single 1h window)", value: "up to 55 minutes after fix" },
  ]}
/>

## Prometheus implementation

```yaml
# Recording rule: fast-request ratio over a 1h window
record: job:slo_latency_fast:ratio_rate1h
expr: |
  sum(rate(http_request_duration_seconds_bucket{le="0.2"}[1h]))
  /
  sum(rate(http_request_duration_seconds_count[1h]))

# Page alert: 1h AND 5m both at >14.4x burn
alert: SLOLatencyBurnFast
expr: |
  (
    (1 - job:slo_latency_fast:ratio_rate1h) > (14.4 * 0.001)
    and
    (1 - job:slo_latency_fast:ratio_rate5m) > (14.4 * 0.001)
  )
labels:
  severity: page
annotations:
  summary: "Latency SLO burning at >14.4x over 1h AND 5m"
```

The `14.4 * 0.001` is `burn_rate × (1 − SLO)` = `14.4 × 0.001 = 0.0144` — the error rate threshold corresponding to 14.4x burn at 99.9% SLO.

<Inset kind="why" lang="en">
The thresholds 14.4, 6, and 1 are not arbitrary. They come from solving: "what burn rate would consume X% of the budget in window W?" With a 30-day period, 2% budget in 1h: burn = (0.02 × 720h) / 1h = 14.4. With 5% budget in 6h: burn = (0.05 × 720h) / 6h = 6. With 10% budget in 3 days (72h): burn = (0.10 × 720h) / 72h = 1. These thresholds are derived from first principles, not empirical tuning — any team can recompute them for a different SLO window length.
</Inset>

<FadedExample
  client:load
  id="03-faded-mwmbr"
  pieceSlug="03-mwmbr-alerting"
  lang="en"
  title="Complete the 6h+30m page alert for the same 99.9% SLO"
  steps={{
    solved: (
      <div class="space-y-3 text-sm">
        <p><strong>Full 1h+5m page alert (solved):</strong></p>
        <pre class="text-xs bg-zinc-100 p-2 rounded overflow-x-auto"><code>{`alert: SLOBurnFast
expr: |
  (1 - ratio_rate1h) > (14.4 * 0.001)
  and
  (1 - ratio_rate5m) > (14.4 * 0.001)
labels:
  severity: page`}</code></pre>
        <p>Fires when the 1-hour burn AND the 5-minute burn are both above 14.4x. The 5m window clears the alert within 5 minutes of fix.</p>
      </div>
    ),
    semi: {
      prompt: "Fill in the burn rate threshold for the 6h+30m page alert at the same 99.9% SLO:",
      blanks: [
        { id: "br", expected: "6x", placeholder: "burn rate (e.g. 1x, 6x, 14.4x)" },
      ],
    },
    blank: {
      prompt: "Why does the 6h+30m alert use a lower burn rate than the 1h+5m alert?",
      reveal: (
        <div class="space-y-2 text-sm">
          <p>A lower burn rate over a longer window catches <em>sustained moderate burns</em> that the 1h+5m alert would miss — outages that eat budget slowly but steadily over several hours. The math: burn_rate × window / period = budget_consumed. To consume 5% in 6 hours: burn = (0.05 × 720) / 6 = 6x. The 1h alert needs 14.4x to fire because it has only 1 hour to observe the burn. The 6h alert has more data, so it can be confident at a lower burn rate. The two page-severity rows are complementary: the first catches big fast outages, the second catches moderate sustained burns.</p>
        </div>
      ),
    },
  }}
  misconceptions={{
    "br:1x": "1x is the slow-burn ticket threshold. For a page at the 6h+30m window, you need 6x — consuming 5% of the budget in 6 hours.",
    "br:14.4x": "14.4x is correct for the 1h+5m page. The 6h+30m page uses 6x — a lower threshold that catches sustained moderate burns the 1h alert would miss.",
  }}
/>

<Quiz
  id="03-mwmbr-quiz-1"
  lessonSlug="03-mwmbr-alerting"
  lang="en"
  question="A single-window alert fires at 14.4x burn over 1 hour. The incident resolves at 12:00. When does the alert clear?"
  choices={[
    { label: "Around 12:55 — the 1-hour window still contains 55 minutes of bad data after resolution", correct: true },
    { label: "Immediately at 12:00 — the alert evaluates the current burn rate", misconception: "The 1-hour window is a rolling average. At 12:00, the window contains data from 11:00–12:00, most of which is bad. The burn rate stays elevated for another ~55 minutes." },
    { label: "At 13:00 — when the next full evaluation window runs", misconception: "Prometheus evaluates alerts every 15–30 seconds. The window clearing is not tied to evaluation intervals but to how much bad data remains in the window." },
    { label: "Within 5 minutes — the exporter scrape interval catches the fix quickly", misconception: "Scrape interval is irrelevant. The alert uses a 1-hour rate — the numerator drains slowly as old bad data rolls out of the window." },
  ]}
/>

<Quiz
  id="03-mwmbr-quiz-2"
  lessonSlug="03-mwmbr-alerting"
  lang="en"
  question="An MWMBR alert fires when the 1h burn exceeds 14.4x AND the 5m burn exceeds 14.4x. The 1h burn is 15x and the 5m burn is 12x (just under threshold). Does the alert fire?"
  choices={[
    { label: "No — both conditions must be true. The 5m burn is below threshold, so the AND is false.", correct: true },
    { label: "Yes — the 1h burn at 15x is severe enough on its own", misconception: "MWMBR requires BOTH the long and short window to exceed the threshold. The short window check confirms the burn is still happening right now." },
    { label: "Yes — 12x on the 5m window is still very high", misconception: "12x is high, but the threshold is 14.4x. The AND requires both to exceed the threshold. This is intentional: if the 5m burn is below 14.4x, the situation may be self-resolving." },
    { label: "It depends on whether Alertmanager is configured with 'for: 5m'", misconception: "The 'for' duration is a separate mechanism. The AND logic between two windows is in the PromQL expression itself, not in the alert duration." },
  ]}
/>

</Explanation>

<KeyTakeaway>MWMBR alerting combines a long window (noise resistance) AND a short window (recovery resolution) per severity level. The AND means: both windows must exceed the burn-rate threshold simultaneously. Result: fast detection, &lt;5-minute reset after fix, no noise. The thresholds 14.4x/6x/1x come from "what burn rate consumes 2%/5%/10% of the budget in 1h/6h/3d?"</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-mwmbr-retrieval"
  lang="en"
  questions={[
    { q: "What are the two failure modes of single-window SLO alerting?", a: "Short window: too noisy — random blips fire the page; alert fatigue causes teams to ignore it. Long window: too slow to clear — after the incident resolves, the window still contains historical bad data, so the alert keeps firing for nearly the full window length, making it impossible to tell if the fix worked." },
    { q: "Why does the MWMBR pattern use AND between windows instead of OR?", a: "OR would fire either on a short spike OR on a sustained burn — too sensitive. AND requires both: the long window confirms the burn is real and sustained (noise-resistant); the short window confirms the burn is still ongoing right now (recovery-fast). Pages clear within 5 minutes of fix because the short window is the fast-recovery signal. With just the long window, the alert fires for nearly the window length after the issue ends." },
    { q: "Derive the 14.4x burn rate threshold for the 1h+5m page alert at a 99.9% SLO with a 30-day window.", a: "Goal: consume 2% of the budget in 1 hour. Math: burn_rate × window / period = budget_consumed. Solve for burn_rate: (0.02 × 720h) / 1h = 14.4. The 5-minute confirmation window uses the same 14.4x threshold — it does not change the burn rate, it just adds 'is this still happening now?'" },
  ]}
/>

<Recap lang="en">
Naive SLO alerts fail in one of two ways: short windows are noisy and fire on every transient spike, long windows reset slowly and keep the on-call awake after the incident clears. Multi-window multi-burn-rate alerting solves both by combining a long window (noise resistance) with a short window (recovery resolution) using AND logic: the page fires only when the burn is both sustained AND currently ongoing. The canonical thresholds — 14.4x/6x/1x across 1h/6h/3d windows — derive from "what burn consumes 2%/5%/10% of the budget in each window?" At a 99.9% SLO, 14.4x means a 1.44% error rate; the 5-minute short window clears the alert within 5 minutes of fix. Hand-rolling MWMBR PromQL per service is error-prone; use Sloth or Pyrra to generate it declaratively. Now when you see a single-window SLO alert staying fired 45 minutes after an incident, you know why — and what to change.
</Recap>
