infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label trace-id: one 128-bit hex string, the four-signal join key
    children
      - label Logs
        children
          - label Carries it as a structured field (trace_id)
          - label Join payoff: every log line for the request
      - label Metrics
        children
          - label Carries it as a histogram exemplar on a bucket sample
          - label Join payoff: jump from a latency spike to one trace
      - label Traces
        children
          - label Keyed by it natively (the primary key)
          - label Join payoff: the full causal span tree
      - label Profiles
        children
          - label Carries it on each CPU/allocation stack sample
          - label Join payoff: flame graph for that exact request
theme
  palette #6366f1 #14b8a6 #f59e0b #f43f5e
