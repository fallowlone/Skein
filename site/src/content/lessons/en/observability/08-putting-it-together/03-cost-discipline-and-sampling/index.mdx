---
concepts:
- observability-cost
- cardinality-limits
- tiered-retention
- sampling-budget
- mttd-mttr
deepensInto:
- observability/08-putting-it-together/04-incident-loop-and-culture
estMin: 13
lang: en
lessonType: topic
level: middle
order: 3
prereqs:
- observability/08-putting-it-together/02-otel-architecture-four-signals
- observability/04-red-use/05-cardinality-and-cost
- observability/05-slo-budgets/01-sli-slo-budget-basics
slug: 03-cost-discipline-and-sampling
sources:
- https://opentelemetry.io/docs/
- https://sre.google/workbook/implementing-slos/
- https://www.brendangregg.com/usemethod.html
spiral:
- latency
status: ready
summary: Three levers — cardinality limits on metrics, tail sampling on traces, and
  tiered log retention — keep observability cost at $1–5/million requests even as
  traffic grows 10x.
title: 'Cost discipline: keeping observability under 5% of infra spend'
track: observability
unit: 08-putting-it-together
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import savingsByLever from "./infographics/savings-by-lever.svg?raw";

<Hook>
A 200-service engineering org sends you the observability bill: $680k/year, growing faster than traffic. The platform team has two quarters to cut it in half without losing debugging power. No new tooling. No vendor change. The difference between a well-tuned and an untuned org is not the backend — it is three levers and the discipline to apply them.
</Hook>

<Crux>Cardinality limits, tail sampling, tiered retention: three levers that hold the observability bill under 5% of infra at 10x growth.</Crux>

<Explanation>

## Why observability scales superlinearly without discipline

Production observability scales linearly with traffic for the baseline signal volume, but superlinearly with cardinality. One new unbounded label (e.g. `user_id` added to a metric) can multiply the metric series count by 10–100x overnight. Trace volume grows with request count. Log volume grows with request count and can spike on errors. Without governance, a 10x traffic increase can mean a 50–100x cost increase.

The industry benchmark for a well-tuned org: **$1–5 per million requests** for full four-signal observability. For an untuned org: **$10–50 per million requests**. The difference is engineering discipline, not vendor choice.

## The three cost levers

### Lever 1: Cardinality limits on metrics

Every unique combination of label values creates one time-series. A metric `http_requests_total{service, route, status_code}` with 200 services × 500 routes × 600 status codes = 60 million series. The realistic cardinality budget is 5–10k active series per service.

Rules that prevent cardinality explosions:
- Use **route templates**, not full URLs (`/users/{id}` not `/users/17384`).
- Use **status classes**, not exact codes (`2xx`, `4xx`, `5xx` not `200`, `201`, `404`).
- Never use `user_id`, `request_id`, `email`, or any unbounded value as a label.
- Enforce limits in CI: a lint step rejects any new label whose values are unbounded.

Together these rules mean a single engineer adding one innocent-looking label can multiply your entire metric bill tenfold overnight. Without CI enforcement you will catch it only when the invoice arrives.

### Lever 2: Tail sampling on traces

Without tail sampling a 200-service org at 10k requests/second generates ~10k trace trees per second. Keeping all of them at 30-day retention is the biggest cost driver. Tail sampling policy:

- **100%** of traces with any ERROR span.
- **100%** of traces with total duration above the slow threshold (2 s typical).
- **5% random baseline** of everything else.

Expected result: 80–90% trace volume reduction with zero debugging-power loss. Errors and slow traces are the only traces you ever navigate; the 5% baseline provides context for rate/pattern analysis.

### Lever 3: Tiered retention on logs and profiles

Not all data needs to be queryable at full resolution forever.

| Tier | Retention | Cost level | Use case |
|---|---|---|---|
| Hot (full resolution) | 7–14 days | High | Incident investigation |
| Summary / rollup | 30–90 days | Medium | Trend analysis, SLO review |
| Archival (object storage) | 90 days–7 years | Low | Compliance, rare deep dives |

Logs in mature orgs: hot 14 days, summary 30 days, archival 90 days. Profiles: hot 30 days, downsampled 90 days. Tiered retention makes cost grow logarithmically with time rather than linearly.

<FlowDiagram
  label="Telemetry firehose through the cost levers"
  perRow={6}
  hue="var(--d-systems)"
  nodes={[
    { id: "firehose", label: "Raw firehose", sub: "all telemetry" },
    { id: "cardinality", label: "Cardinality", sub: "drop labels" },
    { id: "sampling", label: "Tail sampling", sub: "−80–90%" },
    { id: "rollup", label: "Aggregation", sub: "rollups" },
    { id: "retention", label: "Retention", sub: "hot→archive" },
    { id: "store", label: "Stored", sub: "$1–5/M req" },
  ]}
  edges={[
    { from: "firehose", to: "cardinality", label: "100%" },
    { from: "cardinality", to: "sampling", label: "fewer series" },
    { from: "sampling", to: "rollup", label: "errors+slow+5%" },
    { from: "rollup", to: "retention", label: "summarised" },
    { from: "retention", to: "store", label: "tiered" },
  ]}
  caption="Each lever shrinks the stream before it reaches the expensive store: cardinality limits cut series, tail sampling drops 80–90% of traces, rollups summarise, and tiered retention ages data into cheap storage."
/>

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Signal</th>
        <th class="border border-border px-3 py-2 text-left">% of typical bill</th>
        <th class="border border-border px-3 py-2 text-left">Primary cost lever</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Logs</td>
        <td class="border border-border px-3 py-2">~40%</td>
        <td class="border border-border px-3 py-2">Tiered retention + log-level audit</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Metrics</td>
        <td class="border border-border px-3 py-2">~25%</td>
        <td class="border border-border px-3 py-2">Cardinality limits enforced in CI</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Traces</td>
        <td class="border border-border px-3 py-2">~25%</td>
        <td class="border border-border px-3 py-2">Tail sampling (80–90% volume drop)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Profiles</td>
        <td class="border border-border px-3 py-2">~10%</td>
        <td class="border border-border px-3 py-2">Per-service sample rate (default 99 Hz)</td>
      </tr>
    </tbody>
  </table>
</div>

## The cost-cutting playbook applied

Back to the $680k/year platform team asked to cut costs by 50% in two quarters.

**Step 1: Pareto analysis.** Pull the bill broken down by service and signal type. Top 5 services account for ~60% of cost. Top 1 signal type (usually logs) accounts for ~50% of cost.

**Step 2: Logs at $300k/year.** Look for: verbose log levels in production (INFO or DEBUG instead of WARN); error messages logged on every retry; per-request log lines that duplicate trace span events; repeated identical messages. Fixes: log-level audit, dedup, move per-request info into span attributes, rate-limit noisy events.

**Step 3: Metrics at $200k.** Find the top 20 metrics by series count. Most will have unbounded labels. Drop them via collector relabeling; route the dimension into traces or logs instead. Expected: 50–70% series reduction.

**Step 4: Traces at $150k with no tail sampling.** Deploy tail-sampling policy (errors 100% + slow 100% + 5% baseline). Expected: 80–90% trace volume reduction.

**Step 5: Profiles at $30k.** Leave them. Profiling is ~10% of total cost; cutting it saves little and destroys CPU debugging power. Better to turn it on for services that lack it — saves more by preventing future incidents.

**Expected results:** Logs −40% ($120k saved), metrics −50% ($100k saved), traces −85% ($127k saved). Total: $347k saved. Cost from $680k to $333k — 49% reduction in two quarters.

<Infographic
  svg={savingsByLever}
  label="Column chart of the dollar savings each lever delivers in the $680k playbook: logs −40% saves $120k, metrics −50% saves $100k, traces −85% saves $127k."
  caption="Tail sampling on traces is the single highest-leverage move: it saves $127k even though traces are only a quarter of the bill, because the 85% volume cut dwarfs the 40% logs cut on a larger base."
  hue="var(--d-infra)"
/>

## MTTD vs MTTR: both matter, MTTD is the bigger lever

**Mean Time To Detect (MTTD):** how long between something breaking and an alert firing. Multi-window burn-rate alerts (unit 05) drive MTTD to 1–5 minutes.

**Mean Time To Resolve (MTTR):** how long between alert and fix. Funnel discipline drives MTTR to 3–10 minutes.

A 30-second MTTD turns a 10-minute incident into a 5-minute one for users. A 5-minute MTTD turns the same incident into 15 minutes. MTTD compounds faster than MTTR because it affects every request sent during the failure window. Measure both separately; show both on the team reliability scorecard.

<Misconception id="mc-vendor-cost">
  <p><strong>"A cheaper vendor will cut our bill 50%."</strong> No. Vendor pricing differs by 20–40% at similar cardinality. Untuned cardinality with no tail sampling at a cheaper vendor still costs more than a tuned setup at an expensive one. The bill follows data volume and cardinality, not brand. Fix the data first.</p>
</Misconception>

<NumbersCard
  id="card-cost-numbers"
  title="Production observability: typical cost parameters"
  stats={[
    { label: "Well-tuned org cost / million requests", value: "$1–5" },
    { label: "Untuned org cost / million requests", value: "$10–50" },
    { label: "Observability / total infra cost target", value: "3–7%" },
    { label: "Cardinality budget per service", value: "5–10k active series" },
    { label: "Trace volume reduction (tail sampling)", value: "80–90%" },
    { label: "Typical MTTD with burn-rate alerts", value: "1–5 minutes" },
    { label: "Typical MTTR with funnel discipline", value: "3–10 minutes" },
  ]}
  data-lesson-visual
/>

<Quiz
  id="03-cost-discipline-and-sampling-quiz-1"
  lessonSlug="03-cost-discipline-and-sampling"
  lang="en"
  question="A platform team is asked to cut observability cost by 30% without losing debugging power. Which set of moves is most likely to deliver?"
  choices={[
    { label: "Drop high-cardinality labels (user_id, request_id) from metrics, switch traces to tail sampling on errors/slow + 1% baseline, tier logs (7 days fine + 90 days archival)", correct: true },
    { label: "Disable all profiling", misconception: "Profiling is ~10% of total cost; cutting it gives marginal savings and destroys debugging power." },
    { label: "Use a cheaper vendor", misconception: "Vendor swap does not change cardinality. The bill follows the data, not the brand." },
    { label: "Keep everything but reduce retention uniformly", misconception: "Uniform retention cuts hurt long-running trend analysis. Tiered retention is the production pattern." },
  ]}
/>

<Quiz
  id="03-cost-discipline-and-sampling-quiz-2"
  lessonSlug="03-cost-discipline-and-sampling"
  lang="en"
  question="A team adds a new metric: http_requests_total{service, user_id, endpoint}. The service handles 50k unique users and 200 endpoints. How many new time series does this create?"
  choices={[
    { label: "50k × 200 = 10M series — the user_id label turns a 200-series metric into a 10-million-series one", correct: true },
    { label: "200 series — one per endpoint", misconception: "Each unique combination of all label values creates one series. user_id multiplies the series count by the number of unique users." },
    { label: "50k series — one per user", misconception: "Each combination matters: service × user_id × endpoint = series per service × 50k × 200." },
    { label: "1 series — metrics aggregate automatically", misconception: "Metrics are not automatically aggregated. Each label-value combination creates a separate series." },
  ]}
/>

</Explanation>

<KeyTakeaway>Three levers — cardinality limits (CI-enforced, no unbounded labels), tail sampling (80–90% trace volume drop keeping 100% errors/slow), and tiered retention (hot 7–14 d, summary 30–90 d, archival) — keep the observability bill at 3–7% of infra across 10x growth. Vendor choice is secondary; data discipline is primary.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-cost-discipline-and-sampling-retrieval"
  lang="en"
  questions={[
    { q: "Name the three primary cost levers and state what each one controls.", a: "1. Cardinality limits on metrics: enforced in CI, reject unbounded labels (user_id, email, request_id), use route templates not URLs, status classes not exact codes. Controls the number of active time-series (budget: 5–10k per service). 2. Tail sampling on traces: buffer spans until trace close, keep 100% of errors and slow traces, keep 5% random baseline, discard the rest. Controls trace volume — typically 80–90% reduction from an unsampled baseline. 3. Tiered retention on logs (and profiles): 7–14 days hot at full resolution, 30–90 days summary rollup, 90 days–7 years archival in object storage. Controls long-tail storage cost." },
    { q: "Walk the cost-cutting playbook for a $680k/year observability spend that needs to reach $340k in two quarters.", a: "Step 1: Pareto pull — the bill by service and signal. Top 5 services = ~60% of cost; top signal (usually logs) = ~50%. Step 2: Logs ($300k) — log-level audit (INFO/DEBUG → WARN in prod), dedup repeated messages, move per-request info into span attributes, rate-limit noisy event types. Expected: −40% ($120k saved). Step 3: Metrics ($200k) — find top-20 metrics by series count, drop unbounded labels via collector relabeling. Expected: −50% ($100k saved). Step 4: Traces ($150k, no tail sampling) — deploy tail-sampling policy (errors 100% + slow 100% + 5% baseline). Expected: −85% ($127k saved). Step 5: Profiles ($30k) — leave them; marginal savings vs full debugging power lost. Total saved: $347k. Cost: $680k → $333k, 49% reduction, two-quarter target met." },
    { q: "Why is MTTD often a bigger lever than MTTR for user-facing impact?", a: "MTTD (mean time to detect) measures how long users experience the failure before the team even knows about it. MTTR (mean time to resolve) measures how long from knowing to fixed. A 5-minute MTTD means users suffer the failure for at least 5 minutes even with a 1-second MTTR. A 1-minute MTTD with a 10-minute MTTR means users suffer 11 minutes total — but a 5-minute MTTD with the same MTTR means 15 minutes. Multi-window burn-rate alerting drives MTTD to 1–5 minutes (the alert fires within one short window after the burn starts). Funnel discipline drives MTTR to 3–10 minutes. Both compound, but MTTD affects every request sent during the failure window, not just the investigation time." },
  ]}
/>

<Recap lang="en">
Production observability scales superlinearly with cardinality without discipline: one unbounded label can multiply a metric's series count 100x overnight. The industry benchmark is $1–5/million requests for a well-tuned org versus $10–50 for an untuned one — a 10x difference driven by data discipline, not vendor choice. Three levers control the bill: cardinality limits enforced in CI (no unbounded labels, route templates not URLs), tail sampling that keeps 100% of errors and slow traces while discarding 90%+ of baseline volume, and tiered retention that moves data from hot storage to cheap object storage as it ages. The cost-cutting playbook runs Pareto first (top 5 services drive 60% of cost), then addresses logs (−40%), metrics (−50%), and traces (−85%) in descending cost order. Profiling at ~10% of total cost is the last thing to touch. MTTD from burn-rate alerting and MTTR from funnel discipline compound to keep most incidents under 10 minutes total for users. Now when you see a metric label proposal in a code review, your first question is: "What is the cardinality bound on this value?" — if no one can answer it, the label does not merge.
</Recap>
