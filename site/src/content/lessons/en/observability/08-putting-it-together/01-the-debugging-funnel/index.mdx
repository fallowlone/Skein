---
concepts:
- debugging-funnel
- mttr
- funnel-discipline
- slo-burn-alert
deepensInto:
- observability/08-putting-it-together/02-otel-architecture-four-signals
estMin: 10
lang: en
lessonType: topic
level: junior
order: 1
prereqs:
- observability/04-red-use/01-red-and-use-overview
- observability/05-slo-budgets/01-sli-slo-budget-basics
- observability/06-trace-propagation/01-what-is-propagation
- observability/07-profiling/01-flame-graph-basics
slug: 01-the-debugging-funnel
sources:
- https://opentelemetry.io/docs/concepts/observability-primer/
- https://sre.google/workbook/implementing-slos/
- https://grafana.com/blog/the-red-method-how-to-instrument-your-services/
spiral:
- latency
- encapsulation
status: ready
summary: A fixed funnel order — SLO burn, then RED, then trace, then profile, then
  git blame — cuts MTTR by 50–80% by eliminating random dashboard navigation.
title: 'The debugging funnel: SLO → RED → trace → profile'
track: observability
unit: 08-putting-it-together
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import mttrReduction from "./infographics/mttr-reduction.svg?raw";

<Hook>
It is 2 am. The SLO burn alert fires. You open four dashboards at random, ask in Slack who deployed last, then spend 40 minutes hunting. Your colleague who joined the same call follows a five-step checklist and closes the incident in 8 minutes. The difference is not tooling — it is order.
</Hook>

<Crux>RED says symptom. SLO sets the budget. Traces find the span. Profiles name the function. One loop, three minutes, every incident.</Crux>

<Explanation>

## The funnel mental model

By the end of this lesson you will know the five-step sequence that takes you from "something is on fire" to "here is the commit" in under three minutes — and why skipping any step costs you time, not saves it.

The debugging funnel is a fixed narrowing sequence. Each layer's output is the next layer's input filter. The funnel has five layers:

1. **SLO burn-rate alert** — confirms user-facing impact is happening and names the service. Without a SLO burn the team does not yet know whether the symptom is real or a flaky test.
2. **RED dashboard** — Rate, Errors, Duration for the named service. One of the three moved; the shape (spike vs drift vs plateau) hints at the cause class.
3. **Trace view** — filtered to the burn window. Finds which service and which span owns the latency or the errors. Narrows from "something is wrong in checkout" to "inventory.lookup is taking 1.3s."
4. **Profile** — filtered to a trace-id from step 3. Flame graph shows which function inside the span consumed the CPU or the IO. Narrows from "inventory.lookup is slow" to "json.Marshal on a new schema."
5. **git blame** — on the function and line from the profile. Names the commit, the author, and the deploy window.

Together, these five steps mean: every click is determined by the previous one, and no layer is optional — skip SLO burn and you may spend twenty minutes on a service that was never the source of impact. The funnel is fixed. Tools change every two years; the funnel has not changed in fifteen.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Layer</th>
        <th class="border border-border px-3 py-2 text-left">Tool</th>
        <th class="border border-border px-3 py-2 text-left">Output (narrows to)</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">1. SLO burn alert</td>
        <td class="border border-border px-3 py-2">Alerting + SLO dashboard</td>
        <td class="border border-border px-3 py-2">Which service has user-facing impact</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">2. RED dashboard</td>
        <td class="border border-border px-3 py-2">Grafana / Datadog</td>
        <td class="border border-border px-3 py-2">Which of Rate / Errors / Duration moved</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">3. Trace view</td>
        <td class="border border-border px-3 py-2">Jaeger / Tempo / Honeycomb</td>
        <td class="border border-border px-3 py-2">Which service and span owns the time</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">4. Profile</td>
        <td class="border border-border px-3 py-2">Pyroscope / Parca</td>
        <td class="border border-border px-3 py-2">Which function inside the span ate the CPU</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">5. git blame</td>
        <td class="border border-border px-3 py-2">git</td>
        <td class="border border-border px-3 py-2">Which commit, author, deploy window</td>
      </tr>
    </tbody>
  </table>
</div>

<FlowDiagram
  label="The four signals as a narrowing funnel"
  perRow={1}
  hue="var(--d-systems)"
  nodes={[
    { id: "metric", label: "Metric", sub: "something is wrong" },
    { id: "trace", label: "Trace", sub: "which request / service" },
    { id: "log", label: "Log", sub: "what exactly happened" },
    { id: "profile", label: "Profile", sub: "which code / line" },
  ]}
  edges={[
    { from: "metric", to: "trace", label: "narrow" },
    { from: "trace", to: "log", label: "narrow" },
    { from: "log", to: "profile", label: "narrow" },
  ]}
  caption="Each signal answers a narrower question than the one above: the metric alert says something broke, the trace isolates the request and service, the log states what happened, and the profile names the exact code."
/>

## A concrete walk-through

A startup adopts the full chapter's stack: RED dashboards, SLOs with multi-window burn-rate alerts, OTel traces, continuous profiling. An on-call engineer gets paged: "checkout SLO burn 14x."

She does not open every dashboard. She follows the funnel.

- **RED on checkout:** p99 jumped from 200 ms to 1.5 s; Rate flat; Errors flat. Duration-only spike — not a crash, not a capacity problem.
- **Trace view filtered to the burn window:** one span dominates in every slow trace — `inventory.lookup`, 1.3 s of the 1.5 s total.
- **Profile for inventory service, filtered to that trace-id:** 1.1 s in `json.Marshal` — a new code path serialising too much.
- **git blame on `json.Marshal` call site:** commit landed 30 minutes ago, author is the inventory team, deploy matches the burn start time.

Total time from page to git blame: 90 seconds. The funnel did the work.

Six months in, the team's MTTR dropped from 45 minutes to 8. Not faster individual clicks — no random order.

## Why tool upgrades alone do not cut MTTR

Tooling typically gives 10–20% MTTR improvement. Funnel discipline gives 50–80% because most of the loss was navigation: opening whichever dashboard the on-call last touched, asking in Slack, switching signals without direction. The funnel eliminates deliberation. Each click is forced by the previous one. Senior engineers who appear 10x faster during incidents are usually not running different queries; they are running the same queries in the right order while junior engineers run them randomly.

<Infographic
  svg={mttrReduction}
  label="Column chart comparing MTTR reduction by source: new tooling alone yields only 10–20%, while funnel discipline yields 50–80% — roughly four times the cut."
  caption="Better tools shorten any single step; a fixed funnel order shortens the whole journey. That is why discipline, not new tooling, is where most of the MTTR win comes from."
  hue="var(--d-infra)"
/>

<Inset kind="why" lang="en">
Think of a hospital triage room. A patient arrives in pain. Step 1: vital signs (RED) — pulse, BP, temperature. Step 2: known constraints (SLO) — stable or critical? Step 3: imaging (traces) — where in the body exactly? Step 4: biopsy (profile) — what is the tissue made of? Each step uses different tools but they all answer one question: where do we cut? Production observability is the same.
</Inset>

<NumbersCard
  id="card-funnel-numbers"
  title="Funnel discipline: what the numbers say"
  stats={[
    { label: "MTTR reduction from funnel discipline", value: "50–80%" },
    { label: "MTTR reduction from new tooling alone", value: "10–20%" },
    { label: "Sample MTTR drop (startup, 6 months)", value: "45 min → 8 min" },
    { label: "Typical MTTD with burn-rate alerts", value: "1–5 minutes" },
    { label: "Typical MTTR with funnel discipline", value: "3–10 minutes" },
  ]}
  data-lesson-visual
/>

<Quiz
  id="01-the-debugging-funnel-quiz-1"
  lessonSlug="01-the-debugging-funnel"
  lang="en"
  question="In the funnel for production debugging, which signal is the FIRST one an on-call should look at?"
  choices={[
    { label: "The SLO burn-rate alert (or the RED dashboard it points at) — that names the user-facing symptom; everything else is a drill-down from there", correct: true },
    { label: "Continuous profile flame graph", misconception: "Profile is the last step; it requires already knowing which service and span to look at." },
    { label: "Raw logs", misconception: "Logs are a last resort for specific event lookups; the funnel reaches them only when traces are not enough." },
    { label: "Whatever dashboard the on-call last opened", misconception: "Random order is what makes incident response slow. The funnel exists to prevent that." },
  ]}
/>

<DragOrder
  id="01-the-debugging-funnel-drag-1"
  lessonSlug="01-the-debugging-funnel"
  lang="en"
  prompt="Order the production-debugging funnel from broadest to narrowest:"
  items={[
    { id: "a", label: "SLO burn-rate alert fires — user impact is happening, budget is being spent fast" },
    { id: "b", label: "RED dashboard for the affected service — which of Rate / Errors / Duration moved?" },
    { id: "c", label: "Trace view filtered to the burn window — which service and span owns the latency?" },
    { id: "d", label: "Profile filtered to the trace-id — which function inside the span ate the time?" },
    { id: "e", label: "git blame on the hot function — which commit and author introduced it?" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

<MetaphorComplete
  id="01-the-debugging-funnel-metaphor-1"
  lessonSlug="01-the-debugging-funnel"
  lang="en"
  setup="Fill in the blank: the funnel order is SLO → RED → trace → _______ → code."
  accepted={["profile", "profiles", "profiling"]}
  canonical="profile (or profiles / profiling)"
  explanation="Profiles sit between the trace span (which tells you where time went) and the code (which tells you why). Every layer narrows the search by one level."
/>

<Quiz
  id="01-the-debugging-funnel-quiz-2"
  lessonSlug="01-the-debugging-funnel"
  lang="en"
  question="What does OpenTelemetry's promise of 'one SDK, one wire format' mean for an engineering org?"
  choices={[
    { label: "Switching observability backends becomes a config change instead of a re-instrumentation project — the SDK stays, only the collector exporter changes", correct: true },
    { label: "It makes monitoring free", misconception: "OTel does not change vendor pricing, just portability." },
    { label: "It removes the need for dashboards", misconception: "Dashboards still exist; OTel changes what feeds them." },
    { label: "It auto-magically debugs your code", misconception: "OTel collects data; humans still debug." },
  ]}
/>

</Explanation>

<KeyTakeaway>A fixed funnel — SLO → RED → trace → profile → git blame — cuts MTTR by 50–80% by eliminating navigation waste. Tool upgrades give 10–20%; funnel discipline gives the rest. The discipline is harder to install than tools, but it pays off forever.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-the-debugging-funnel-retrieval"
  lang="en"
  questions={[
    { q: "Why does following a fixed funnel order cut MTTR more than buying better tools?", a: "Better tools shorten any single step; a fixed funnel order shortens the whole journey by eliminating wasted searches. Without a funnel, engineers open whichever dashboard they remember first, hunt for clues, switch tools, get distracted by side observations. Each detour adds minutes. With a funnel, the next click is always determined by the current click: SLO points at a service, RED points at a metric, trace points at a span, profile points at a function. No deliberation, no random clicks. Tool upgrades give 10–20% MTTR cuts; funnel discipline gives 50–80% because most of the loss was navigation, not analysis." },
    { q: "In the checkout SLO burn scenario, what did RED show and what did that shape rule out?", a: "RED on checkout showed p99 Duration jumped from 200 ms to 1.5 s while Rate and Errors stayed flat. A Duration-only spike with no Error change rules out a crash or logic regression. Flat Rate rules out capacity exhaustion. The shape says: slow downstream or new code path consuming extra time. That narrows the next step: open the trace view filtered to slow Duration examples to find which span owns the 1.3 s overage." },
    { q: "Name the five funnel layers in order and state what each layer's output feeds as input to the next.", a: "1. SLO burn alert — output: which service has user-facing impact. 2. RED dashboard — output: which of Rate / Errors / Duration moved and the shape (spike, drift, plateau). 3. Trace view — output: which service and span owns the latency; the slow trace-id. 4. Profile (filtered by the trace-id from step 3) — output: the specific function and file:line that consumed the time. 5. git blame (on the function from step 4) — output: commit hash, author, and deploy window confirming the cause. Each layer's output is the filter input for the next layer." },
  ]}
/>

<Recap lang="en">
The production debugging funnel is a five-layer narrowing sequence: SLO burn names the service, RED names the signal that moved, trace names the span, profile names the function, git blame names the commit. Each layer's output filters the next layer's query, so no deliberation is needed. Tool upgrades account for 10–20% of MTTR improvement; funnel discipline accounts for 50–80% because most incident time is wasted in navigation, not analysis. A startup that adopted the full observability stack with funnel discipline saw MTTR drop from 45 minutes to 8 over six months — not from faster tools, but from removing random order. The funnel is fixed; tools change every two years, the funnel has not changed in fifteen. Now when you get paged, resist the urge to open the dashboard you last looked at — start at SLO burn, follow the output, and let the funnel drive every click from there.
</Recap>
