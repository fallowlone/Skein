---
concepts:
- debugging-funnel
- four-signal-join
- tail-sampling
- cardinality-cost
- mttd-vs-mttr
- observability-roi
estMin: 14
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://opentelemetry.io/docs/
- https://sre.google/workbook/implementing-slos/
- https://grafana.com/blog/the-red-method-how-to-instrument-your-services/
status: ready
summary: "Cross-track synthesis MCQs spanning the whole observability chapter — funnel order, the trace-id join, tail sampling, cardinality cost, MTTD vs MTTR, and ROI."
title: 'Observability capstone: multiple-choice synthesis'
track: observability
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that force you to wire the whole track together — three pillars, OTel, RED/USE, SLO budgets, trace propagation, profiling. Each one is a decision you make mid-incident or mid-design-review, not a definition to recite.
</Hook>

<Goal lang="en">
Confirm you can compose the chapter into one system: the funnel that drives MTTR, the trace-id that joins four signals, the sampling and cardinality levers that hold the bill, and the arithmetic that justifies the spend.
</Goal>

<Quiz
  id="08-putting-it-together-choice-1"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="An on-call is paged 'checkout SLO burn 14x'. RED shows p99 Duration up, Rate and Errors flat. What is the correct next move in the funnel, and why that one?"
  choices={[
    { label: "Open the trace view filtered to the burn window and find the span that owns the latency — a Duration-only spike means time is being spent somewhere downstream, and only a trace localises which service and span.", correct: true },
    { label: "Open a continuous profile flame graph for checkout right away.", misconception: "A profile needs a target — a service and ideally a trace-id — before it is useful. Jumping to the profile before the trace skips the step that tells you which service and span to profile." },
    { label: "Grep the logs for ERROR across all services.", misconception: "Errors are flat; this is a latency incident, not an error incident. Logs are the funnel's last resort for specific event lookups, not the second step." },
    { label: "Roll back the most recent deploy immediately.", misconception: "You have not yet localised the cause. Rolling back blind may revert an unrelated change and still leave the latency; the funnel localises first, then resolves." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-2"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="A request slows down. RED Duration shows it, the trace shows inventory.lookup owns 1.3s, the profile shows json.Marshal ate the CPU, and the logs hold the error detail. What single property makes all four signals answer one query about THIS request?"
  choices={[
    { label: "A consistent trace-id propagated by the W3C traceparent header through every hop — logs attach it as a field, metrics as histogram exemplars, traces are keyed by it, profiles tag each sample with it.", correct: true },
    { label: "A shared timestamp; you correlate the four signals by aligning their clocks.", misconception: "Timestamp + service name is exactly the manual correlation the trace-id replaces. Clock skew and concurrent requests make timestamp-only joins ambiguous; the trace-id is an exact key." },
    { label: "All four signals living in the same vendor backend.", misconception: "Co-locating signals in one backend helps UX but does not create the join. Tempo, Loki, Prometheus and Pyroscope are separate stores yet still join — because each row carries the same trace-id." },
    { label: "Identical label names (semantic conventions) on every signal.", misconception: "Semantic conventions make cross-service dashboards consistent, but the per-request join across signal TYPES is the trace-id specifically — conventions are about attribute keys, not request identity." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-3"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="A team head-samples traces at 5% at the SDK to cut cost. During an incident they cannot find traces for the failing requests. What went wrong, and what is the fix?"
  choices={[
    { label: "Head sampling decides at trace start, before the error or slow tail exists, so 95% of the interesting traces were discarded up front. Fix: emit 100% at the SDK and move the keep/discard decision to a tail-sampling collector that keeps 100% of errors and slow traces plus a small baseline.", correct: true },
    { label: "The SDK was misconfigured; 5% head sampling should still capture all errors.", misconception: "Head sampling is probabilistic at trace start and has no knowledge of the eventual outcome — it cannot preferentially keep errors it has not seen yet. This is the intrinsic limitation, not a misconfiguration." },
    { label: "Traces are the wrong signal for incidents; rely on logs instead.", misconception: "Traces are precisely the per-request causal signal you want in an incident. The problem is the sampling strategy, not the signal." },
    { label: "Raise head sampling to 100% permanently.", misconception: "100% head sampling defeats the cost goal — you store every baseline trace forever. Tail sampling keeps 100% of what matters and drops 90%+ of the boring baseline, getting both cost and coverage." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-4"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="The observability bill doubled in a week with no traffic change. One team added user_id as a metric label and started logging full request bodies at INFO. Which two distinct failure modes is this, and which signal does each hit?"
  choices={[
    { label: "Cardinality explosion on metrics (an unbounded label multiplies the active time-series count by the number of unique users) and PII leakage on logs (request bodies may carry emails, addresses, credentials) — two separate problems on two separate signals.", correct: true },
    { label: "Both are trace-volume problems solved by tail sampling.", misconception: "Neither touches traces. user_id hits metric cardinality; request bodies hit log content and compliance. Tail sampling addresses neither." },
    { label: "Both are retention problems solved by tiering.", misconception: "Tiering controls how long data lives, not its width or its PII content. An unbounded label still explodes series on day one; tiering does not stop the leak." },
    { label: "Only the metric label matters; encrypted-at-rest logs are safe.", misconception: "Encryption at rest does not stop an authorised query (or an attacker with access) from reading the PII. Both risks are real and documented — Datadog 2021 (cardinality) and Slack 2022 (PII in logs)." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-5"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="Two teams share identical tooling. Team A detects an outage in 30s and resolves in 20min; Team B detects in 8min and resolves in 12min. For the same severity, which team's users suffer less total impact, and what does that say about where to invest?"
  choices={[
    { label: "Team A: total user-facing impact is dominated by detection delay, since every request during the undetected window fails unmitigated. A fast burn-rate alert (MTTD around 30s) caps the damage even with a slower fix; MTTD often outweighs MTTR.", correct: true },
    { label: "Team B: a 12-minute MTTR beats a 20-minute MTTR, so B's users suffer less.", misconception: "MTTR alone ignores the failure window before detection. B's users absorbed 8 minutes of unflagged failure before the clock even started; total impact is roughly MTTD + MTTR per affected request." },
    { label: "They are equal because total time (detect + resolve) is about 20 min each.", misconception: "Impact is not symmetric across the two phases — during the detection delay the failure is fully live and unmitigated. A 30s MTTD with a 20min MTTR still exposed users for far less of the dangerous window than 8min undetected." },
    { label: "It cannot be compared without the vendor names.", misconception: "The tooling is identical by stipulation; the entire difference is the MTTD vs MTTR shape, which is a process and alerting question, not a vendor question." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-6"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="A CFO asks why observability costs $150k/year. The org runs ~30 incidents/year, each now resolved ~25 min faster than the pre-instrumentation baseline, at roughly $5k/min of revenue exposure. What is the strongest answer?"
  choices={[
    { label: "Compute avoided cost: 30 × 25 min × $5k/min is about $3.75M/year against $150k spend — roughly 25x ROI — and show it from real incident-by-incident MTTR deltas, not estimates.", correct: true },
    { label: "'It is industry standard and our competitors all run it.'", misconception: "Industry-standard is not a business case. The defensible answer is measurable avoided-outage cost derived from your own incident history." },
    { label: "'Engineering cannot debug production without it.'", misconception: "True but not a quantified justification — it is the kind of answer that gets the budget cut in a downturn. Tie the spend to revenue protected or risk reduced." },
    { label: "'Our SLAs contractually require this level of monitoring.'", misconception: "SLAs are downstream of observability; the business argument is the avoided-cost arithmetic, and it is computable from MTTR logs, incident counts, and revenue rate." },
  ]}
/>

<Recap lang="en">
The chapter composes into one system. The funnel (SLO burn → RED → trace → profile → git blame) drives diagnosis, and each step is forced by the previous one. The trace-id propagated by W3C traceparent is what makes four signals answer one per-request query. Cost discipline is three independent levers — cardinality limits, tail sampling, tiered retention — each hitting a different signal. MTTD usually outweighs MTTR for user impact because the detection window is fully unmitigated. And the spend is justified by arithmetic: avoided-outage cost from real MTTR deltas, typically 10–30x. Every answer here is a drill-down from those through-lines.
</Recap>
