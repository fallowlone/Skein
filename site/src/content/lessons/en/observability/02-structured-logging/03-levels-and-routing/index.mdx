---
slug: 03-levels-and-routing
lang: en
track: observability
unit: 02-structured-logging
order: 3
title: "Log levels and alert routing"
summary: "The TRACE–FATAL ladder controls what gets stored and who gets paged. Misclassified levels pollute either the alert or the bill."
estMin: 10
status: ready
lessonType: topic
level: middle
prereqs: ["observability/02-structured-logging/02-log-schema"]
deepensInto: []
spiral: ["latency"]
concepts: ["log-levels", "alert-routing", "severity", "debug-flag", "level-discipline"]
sources:
  - https://signoz.io/blog/logging-levels/
  - https://betterstack.com/community/guides/logging/log-levels-explained/
  - https://opentelemetry.io/docs/specs/otel/logs/data-model/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";

<Hook>
An on-call engineer's Slack is flooded with ERROR alerts. Half are retry attempts that succeeded. The alerts are noise, real errors get buried, and the oncall tunes out. The bug is not the service — it is the level assigned to the log line.
</Hook>

<Crux>Log levels serve two purposes: controlling stored volume and notifying humans. A misclassified level pollutes either the alert or the bill.</Crux>

<Explanation>

## The level ladder

The production ladder has six rungs: TRACE / DEBUG / INFO / WARN / ERROR / FATAL. Each rung is a contract about what action the log line implies.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Level</th>
        <th class="border border-border px-3 py-2 text-left">Meaning</th>
        <th class="border border-border px-3 py-2 text-left">Production default</th>
        <th class="border border-border px-3 py-2 text-left">Alert routing</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2 font-mono">TRACE</td>
        <td class="border border-border px-3 py-2">Very verbose, framework internals</td>
        <td class="border border-border px-3 py-2">Off (almost never)</td>
        <td class="border border-border px-3 py-2">None</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2 font-mono">DEBUG</td>
        <td class="border border-border px-3 py-2">Developer investigation detail</td>
        <td class="border border-border px-3 py-2">Off (dynamic flag only)</td>
        <td class="border border-border px-3 py-2">None</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2 font-mono">INFO</td>
        <td class="border border-border px-3 py-2">State changes on the success path</td>
        <td class="border border-border px-3 py-2">On (default)</td>
        <td class="border border-border px-3 py-2">Query during investigation</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2 font-mono">WARN</td>
        <td class="border border-border px-3 py-2">Recoverable issues, degraded path taken</td>
        <td class="border border-border px-3 py-2">On</td>
        <td class="border border-border px-3 py-2">Slack, capacity dashboards</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2 font-mono">ERROR</td>
        <td class="border border-border px-3 py-2">Unrecoverable, work left undone</td>
        <td class="border border-border px-3 py-2">On</td>
        <td class="border border-border px-3 py-2">Page on-call</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2 font-mono">FATAL</td>
        <td class="border border-border px-3 py-2">Process is about to die</td>
        <td class="border border-border px-3 py-2">On</td>
        <td class="border border-border px-3 py-2">Page on-call immediately</td>
      </tr>
    </tbody>
  </table>
</div>

## The production default: INFO and above

The 2026 production default is INFO and above. DEBUG is enabled only via a dynamic flag during active investigation — never on by default. TRACE rarely exists outside of framework internals.

INFO represents the **success path**: state changes that let a human reconstruct what the service did — `request_received`, `request_completed`, `kafka_message_processed`, `transaction_committed`. Without INFO lines you cannot reconstruct normal behaviour before an error.

WARN is for **recoverable issues**: retry succeeded after 3 attempts, fallback path taken, deprecation hit, rate limit approached. The system continued; action may be needed eventually.

ERROR is for **unrecoverable issues that left work undone**: the request returned 5xx, a message went to the dead-letter queue, a write rolled back. Someone should investigate.

FATAL is for **"the process is about to die"**: assertion failure, out-of-memory imminent. The process typically exits or is restarted immediately after.

## Alert routing follows the level ladder

The level contract implies the alert routing:

- **ERROR + FATAL** feed pages and incident channels. A page is an implicit promise that action is required.
- **WARN** feeds Slack and capacity dashboards. No immediate action required; trend is worth watching.
- **INFO** is queried during investigation, never alerted on directly. An alert on INFO volume would fire constantly on normal traffic.
- **DEBUG / TRACE** are filtered out in production storage entirely.

## Why misclassified levels hurt

**INFO used where WARN belongs**: the issue gets buried in the INFO stream. WARN routing does not fire. The slow degradation is invisible until it becomes an ERROR.

**ERROR used where WARN belongs**: every retry, every cache miss, every expected timeout fires a page. The on-call tunes out. When a real ERROR arrives, it is buried in the noise. Alert fatigue is one of the most dangerous observability failures — and it is almost always caused by over-classified levels.

**DEBUG left on in production**: runaway cost. A service emitting DEBUG at 1000 req/s logs internal request details on every call. At pino's ~140k msg/sec throughput this is still feasible, but the volume — 100x the INFO stream — hits the backend at full ingest cost. Teams have received 10x monthly log bills from a single PR that left a debug flag enabled.

<Inset kind="why" lang="en">
Dynamic DEBUG enabling is the production-safe pattern: the DEBUG level is set to OFF by default via an environment variable or a feature flag, and can be toggled at runtime (typically by updating a config map or calling a management endpoint) for a specific service instance during active investigation. After 30-60 minutes the toggle expires or is manually turned off. This pattern lets you get DEBUG detail when you need it and guarantees you never pay for it in steady state.
</Inset>

<Quiz
  id="03-levels-and-routing-quiz-1"
  lessonSlug="03-levels-and-routing"
  lang="en"
  question="A retry-attempt loop emits an ERROR on each of its 5 attempts, including the 3 that succeed. What is wrong with this classification?"
  choices={[
    { label: "Nothing — retrying is an error condition", misconception: "A retry that succeeds is recoverable — the system handled it. Emitting ERROR means a page fires on normal retry behaviour." },
    { label: "The 3 successful retries should be WARN (recoverable), only a final failure that leaves work undone should be ERROR", correct: true },
    { label: "All retries should be DEBUG", misconception: "Retry outcomes are operationally relevant and should be visible in production — DEBUG would filter them out." },
    { label: "Retries should not be logged at all", misconception: "Retry patterns are important for capacity and reliability monitoring — they should be logged, just at the right level." },
  ]}
/>

<Quiz
  id="03-levels-and-routing-quiz-2"
  lessonSlug="03-levels-and-routing"
  lang="en"
  question="In production, which log level is the right default to emit for normal request handling (e.g., request_completed with status 200)?"
  choices={[
    { label: "DEBUG — captures everything, never miss a thing", misconception: "DEBUG in production is a runaway cost; it is safe in dev/staging, dangerous in prod." },
    { label: "INFO — represents the success path, gives a baseline of state changes without being chatty", correct: true },
    { label: "WARN — keeps logs small", misconception: "WARN is for recoverable issues; without INFO lines you cannot reconstruct normal behaviour." },
    { label: "ERROR — only log when something breaks", misconception: "Without state-change INFO lines you cannot tell who did what before the error." },
  ]}
/>

<DragOrder
  id="03-levels-and-routing-drag-1"
  lessonSlug="03-levels-and-routing"
  lang="en"
  prompt="Map each scenario to the correct log level (order from TRACE to FATAL):"
  items={[
    { id: "a", label: "A specific SQL query's bind parameters during debugging" },
    { id: "b", label: "A checkout request completed successfully (status 200)" },
    { id: "c", label: "A retry attempt succeeded on attempt 3 of 5" },
    { id: "d", label: "A request returned 503 because the upstream payment gateway timed out and work is undone" },
    { id: "e", label: "The application caught an unhandled exception and is about to exit" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>The six-level ladder (TRACE/DEBUG/INFO/WARN/ERROR/FATAL) controls both storage volume and alert routing. DEBUG is off in production by default (dynamic flag only). INFO is the success-path default. WARN routes to dashboards; ERROR and FATAL route to pages. Misclassified levels create either alert fatigue or invisible degradation.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-levels-and-routing-retrieval"
  lang="en"
  questions={[
    { q: "What is the difference between WARN and ERROR, and why does it matter for alert routing?", a: "WARN is for recoverable issues — the system handled the situation and continued (retry succeeded, fallback path taken, degraded mode). ERROR is for unrecoverable issues that left work undone — the request returned 5xx, a message went to the dead-letter queue, a transaction rolled back. The difference drives alert routing: WARN feeds dashboards and Slack for trend monitoring; ERROR fires a page requiring immediate action. Misclassifying a recoverable issue as ERROR floods on-call with false positives and causes alert fatigue — the most dangerous outcome because real errors then get buried in noise." },
    { q: "Why is leaving DEBUG on in production a cost risk, not just a noise risk?", a: "DEBUG logs are emitted at every code path the developer wanted to inspect — typically 10-100x the volume of INFO. At production traffic levels, even a modest service emitting DEBUG at 1000 req/s can generate 100-200 GB/day of additional log volume versus 1-2 GB/day at INFO. At $0.10/GB ingest, that is an additional $10-$20 per day per service — before indexed-event costs. Teams have received 10x monthly log bills from a single PR that left a debug flag enabled." },
    { q: "What is the dynamic DEBUG flag pattern, and why is it preferable to a process restart?", a: "Dynamic DEBUG enabling sets DEBUG to OFF by default via an environment variable or feature flag, toggleable at runtime for a specific service instance without restarting the process. The toggle can be a config map update, a management API call, or a feature-flag system change. It expires after 30-60 minutes or is manually turned off. This is preferable to a process restart because restarting loses the in-flight request context you are trying to debug, and in distributed systems restarting one replica is not guaranteed to be the replica handling the problematic request." },
  ]}
/>

<Recap lang="en">
The six-level ladder — TRACE, DEBUG, INFO, WARN, ERROR, FATAL — serves two purposes: volume control (what gets stored) and alert routing (who gets notified). The production default is INFO and above; DEBUG is off by default and enabled only via a dynamic flag during investigation. INFO logs the success path, WARN logs recoverable degradations, ERROR logs unrecoverable failures that left work undone, and FATAL logs imminent process death. Alert routing follows directly: ERROR and FATAL page on-call, WARN feeds dashboards, INFO is queried during investigation. Misclassified levels create two failure modes: over-classification (recoverable issue as ERROR) causes alert fatigue; under-classification (unrecoverable issue as WARN) makes real failures invisible.
</Recap>
