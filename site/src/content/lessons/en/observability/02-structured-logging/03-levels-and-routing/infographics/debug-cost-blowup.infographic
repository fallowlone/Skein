infographic chart-column-simple
data
  title Daily log volume at 1000 req/s: INFO vs DEBUG (GB/day)
  desc Leaving DEBUG on in prod is ~100x the INFO stream — a cost risk, not just noise
  values
    - label INFO default ~2 GB/day
      value 2
    - label DEBUG left on ~200 GB/day
      value 200
theme
  palette #14b8a6 #f43f5e
