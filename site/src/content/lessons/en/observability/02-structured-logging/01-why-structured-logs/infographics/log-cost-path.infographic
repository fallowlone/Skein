infographic sequence-snake-steps-underline-text
data
  title Where a log line's cost is paid
  desc Structured logs are not free — the bill lands in three places, then pays back at query time
  sequences
    - label Write time
      desc CPU + RAM to serialize the event to JSON
    - label In transit
      desc Network egress shipping the line to the backend
    - label At the backend
      desc Ingest GB + indexed-event count + retention bytes
theme
  palette #6366f1 #14b8a6 #f59e0b
