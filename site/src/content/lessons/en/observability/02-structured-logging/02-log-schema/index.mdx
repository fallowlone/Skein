---
slug: 02-log-schema
lang: en
track: observability
unit: 02-structured-logging
order: 2
title: "The production log schema: fields every line must carry"
summary: "What a production-grade structured log line contains in 2026 — the OTel Logs Data Model fields, resource attributes, and why the schema is the API your on-call tooling expects."
estMin: 12
status: ready
lessonType: topic
level: middle
prereqs: ["observability/02-structured-logging/01-why-structured-logs"]
deepensInto: []
spiral: ["latency", "statefulness"]
concepts: ["log-schema", "otel-logs", "trace-id", "severity", "resource-attributes", "semantic-conventions"]
sources:
  - https://opentelemetry.io/docs/specs/otel/logs/data-model/
  - https://github.com/open-telemetry/opentelemetry-specification/blob/main/specification/logs/data-model.md
  - https://opentelemetry.io/docs/concepts/signals/logs/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook>
Two services on the same team call the field "status_code" in one and "http_status" in the other. Every dashboard that joins them breaks. The fix is not a regex — it is a shared schema enforced before the code ships.
</Hook>

<Crux>A production log schema is a contract: every line carries typed fields so any service or on-call query can read it without configuration.</Crux>

<Explanation>

## What every production log line carries

A production-grade structured log line in 2026 has at minimum these fields, in roughly this priority order:

1. **`timestamp`** — ISO-8601 UTC with sub-second precision: `2026-04-12T14:02:11.482Z`. The primary sort key for every backend.
2. **`level`** — string enum: TRACE / DEBUG / INFO / WARN / ERROR / FATAL. Controls what gets stored and who gets paged.
3. **`service.name`** — the OTel-canonical key identifying the emitter. This is the join key back to metrics and traces.
4. **`trace_id`** and **`span_id`** — when the log is emitted inside a traced request, these are the join keys to the tracing backend. Without them the log-to-trace pivot does not work.
5. **`message`** — a short human-readable summary. The only field humans skim; the rest is for machines.
6. **Event-specific fields** — a flat key-value tree using OTel Semantic Conventions: `http.route`, `http.response.status_code`, `db.system`, `error.type`, etc.
7. **Resource attributes** — set once at service start: `host.name`, `cloud.region`, `service.version`, `deployment.environment`. These describe the emitter, not the event.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Field group</th>
        <th class="border border-border px-3 py-2 text-left">Example fields</th>
        <th class="border border-border px-3 py-2 text-left">Set by</th>
        <th class="border border-border px-3 py-2 text-left">Frequency</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Core</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">timestamp, level, message</td>
        <td class="border border-border px-3 py-2">Logger SDK</td>
        <td class="border border-border px-3 py-2">Every line</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Service identity</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">service.name, service.version</td>
        <td class="border border-border px-3 py-2">Logger SDK + config</td>
        <td class="border border-border px-3 py-2">Every line</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Trace context</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">trace_id, span_id, trace_flags</td>
        <td class="border border-border px-3 py-2">Active span mixin</td>
        <td class="border border-border px-3 py-2">Inside traced requests</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Event-specific</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">http.route, http.response.status_code, error.type</td>
        <td class="border border-border px-3 py-2">Application code</td>
        <td class="border border-border px-3 py-2">Per event type</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Resource</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">host.name, cloud.region, deployment.environment</td>
        <td class="border border-border px-3 py-2">Platform / config</td>
        <td class="border border-border px-3 py-2">Once at startup</td>
      </tr>
    </tbody>
  </table>
</div>

## The OTel Logs Data Model

The OpenTelemetry Logs specification (stable API late 2023, SDKs late 2024) formalises this split. A log record contains:

- **Timestamp** — when the event actually occurred (set by the application).
- **ObservedTimestamp** — when the collector saw the record. Under clock skew or backpressure these two diverge; senior teams alert on the divergence as a pipeline-health metric.
- **SeverityNumber** — a numeric ladder (TRACE=1-4, DEBUG=5-8, INFO=9-12, WARN=13-16, ERROR=17-20, FATAL=21-24) so backends can compare severity across libraries that use different text labels.
- **Body** — the human-readable message.
- **Attributes** — the flat key-value map of event-specific data.
- **Resource** — set once per emitter (service.name, host.name, cloud.region).
- **TraceId / SpanId / TraceFlags** — W3C traceparent context inherited from the active span at emit time.

Adopting this shape — even on a non-OTel backend — buys forward compatibility: you can swap backends without rewriting instrumentation, and every dashboard and on-call run-book can speak the same field names.

## OTel Semantic Conventions: one field name across all services

The Semantic Conventions define the canonical names for common fields: `http.route` (not `route`, not `path`, not `url`), `http.response.status_code` (not `status`, not `http_status`), `db.system` (not `db_type`), `error.type`. Using these names means:

- A dashboard querying `http.response.status_code` works uniformly across Node and Go services.
- A backend alert rule referencing `error.type:timeout` works without per-service configuration.
- A new service that onboards the schema inherits every existing query.

The cost of not following conventions shows up in incident response: when the checkout service calls the field `status` and the payment service calls it `http_status`, the join query fails and someone has to know the mapping under pressure at 03:00.

<Inset kind="why" lang="en">
The schema is not bureaucracy — it is load-bearing. Every query, every dashboard, every alert rule, every run-book step that references a field name assumes that field name is stable and consistent across services. Schema drift breaks them silently: the query returns fewer results, the alert fires on only some services, the run-book step requires manual translation. Centralising the schema in a per-org wrapper logger (one module each service imports) makes drift a build-time error instead of a 03:00 discovery.
</Inset>

<NumbersCard
  id="02-log-schema-numbers"
  title="Log schema numbers"
  stats={[
    { label: "OTel Logs API stability (GA)", value: "Late 2023" },
    { label: "OTel Logs SDK stability (most langs)", value: "Late 2024" },
    { label: "Typical structured log line size", value: "~0.5–2 KB" },
    { label: "SeverityNumber range (TRACE to FATAL)", value: "1–24" },
    { label: "OTLP/Logs over gRPC, compressed", value: "~30–50% smaller than JSON" },
  ]}
/>

<Quiz
  id="02-log-schema-quiz-1"
  lessonSlug="02-log-schema"
  lang="en"
  question="A log line carries Timestamp = 14:00:00 and ObservedTimestamp = 14:05:00. What does this gap indicate?"
  choices={[
    { label: "Clock drift on the application host — the application clock is 5 minutes ahead", misconception: "If Timestamp is ahead of ObservedTimestamp the app clock is fast. Here Timestamp is BEHIND ObservedTimestamp, which indicates a delay in the pipeline." },
    { label: "A 5-minute backpressure or buffering delay in the collection pipeline — the log was emitted at 14:00 but the collector only saw it at 14:05", correct: true },
    { label: "The log backend is applying a 5-minute TTL", misconception: "Backends do not rewrite timestamps; the gap shows when the collector saw the record versus when it was created." },
    { label: "The service is in a different timezone", misconception: "Both timestamps should be UTC; a timezone difference would not produce this kind of gap." },
  ]}
/>

<Quiz
  id="02-log-schema-quiz-2"
  lessonSlug="02-log-schema"
  lang="en"
  question="Why does the OTel Logs Data Model define a numeric SeverityNumber (1-24) in addition to the text SeverityText?"
  choices={[
    { label: "To save bandwidth — numbers are smaller than strings", misconception: "Bandwidth is not the reason; the field exists for semantic comparability." },
    { label: "So backends can compare severity across libraries that use different text labels (DEBUG vs Debug vs debug vs trace), enabling numeric range queries like severity_number >= 9 for INFO and above", correct: true },
    { label: "To support 24 distinct log levels instead of 6", misconception: "The 24 values are grouped into 6 ranges of 4, not 24 independent levels — each range has 4 numbers for within-level granularity." },
    { label: "Because the SeverityText field is deprecated", misconception: "Both fields coexist; SeverityText is human-readable, SeverityNumber is machine-comparable." },
  ]}
/>

<DragOrder
  id="02-log-schema-drag-1"
  lessonSlug="02-log-schema"
  lang="en"
  prompt="Order the OTel Logs Data Model fields from most fundamental to most event-specific:"
  items={[
    { id: "a", label: "Timestamp — when the event occurred" },
    { id: "b", label: "SeverityNumber — numeric severity (1-24)" },
    { id: "c", label: "Resource — per-emitter attributes (service.name, host.name)" },
    { id: "d", label: "TraceId / SpanId — W3C trace context" },
    { id: "e", label: "Body — human-readable message" },
    { id: "f", label: "Attributes — event-specific flat key-value (http.route, error.type, ...)" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f"]}
/>

</Explanation>

<KeyTakeaway>A production log schema has a fixed core (timestamp, level, service.name, trace_id, message), event-specific OTel Semantic Convention fields, and resource attributes set once at startup. Schema consistency across services is what makes queries, dashboards, and run-books work without per-service translation.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-log-schema-retrieval"
  lang="en"
  questions={[
    { q: "What are the two timestamps in the OTel Logs Data Model, and why do both matter?", a: "Timestamp is when the event actually occurred, set by the application at emit time. ObservedTimestamp is when the collector saw the record. Under normal conditions they are nearly identical. When they diverge by more than a few seconds it indicates either clock skew on the application host or backpressure in the collection pipeline — the log was buffered. Senior teams emit an alert when (ObservedTimestamp - Timestamp) p99 exceeds 60 seconds, because time-windowed queries break when logs arrive in the wrong time bucket." },
    { q: "Why should you use OTel Semantic Convention field names (http.route, http.response.status_code) instead of your own names?", a: "Convention field names are shared across services, languages, and vendors. A dashboard or alert rule written against http.response.status_code works uniformly across every service that follows conventions, without per-service configuration. When teams invent their own names (status, http_status, statusCode) every query and run-book step has to know the mapping for each service — discovered under pressure at 03:00 during an incident." },
    { q: "Which fields should be set once at startup versus emitted on every log line?", a: "Resource attributes (host.name, cloud.region, service.version, deployment.environment) describe the emitter and never change during a process lifetime — they are set once at startup and attached to every log record by the SDK. Core fields (timestamp, level, message) and trace context (trace_id, span_id) change per event and are set by the logger on every emit. Event-specific fields (http.route, error.type) vary per event type and are set in application code at each call site." },
  ]}
/>

<Recap lang="en">
A production log schema in 2026 follows the OTel Logs Data Model: Timestamp (event time), ObservedTimestamp (collector time), SeverityNumber (1-24) plus SeverityText, Body, Resource (per-emitter: service.name, host.name, cloud.region), Attributes (event-specific: http.route, error.type), and TraceId/SpanId (W3C trace context). Resource attributes are set once at startup; trace context is pulled from the active span at emit time; event-specific attributes are written in application code. Using OTel Semantic Convention field names uniformly across services is what makes cross-service queries, dashboards, and run-books work without per-service translation. The gap between Timestamp and ObservedTimestamp is a pipeline health signal — alert on p99 over 60 seconds.
</Recap>
