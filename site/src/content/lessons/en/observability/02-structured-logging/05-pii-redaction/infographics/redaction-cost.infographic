infographic chart-column-simple
data
  title Redaction overhead per log line (µs)
  desc Static field deny-list is cheap enough for the hot path; regex scrubbing is an order of magnitude dearer
  values
    - label Static deny-list (source)
      value 2
    - label Dynamic regex (source)
      value 8
    - label Collector regex scrubber
      value 20
theme
  palette #6366f1 #f59e0b #f43f5e
