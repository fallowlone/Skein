---
slug: 04-sampling-and-cost
lang: en
track: observability
unit: 02-structured-logging
order: 4
title: "Sampling strategies and log cost"
summary: "Success-path, pattern-based, and tail sampling are the three levers that keep the log bill proportional to incidents, not to traffic."
estMin: 12
status: ready
lessonType: topic
level: middle
prereqs: ["observability/02-structured-logging/03-levels-and-routing"]
deepensInto: []
spiral: ["latency"]
concepts: ["log-sampling", "success-path-sampling", "tail-sampling", "log-cost", "retention-tiering", "log-pipeline"]
sources:
  - https://opentelemetry.io/docs/concepts/signals/logs/
  - https://betterstack.com/community/guides/logging/log-levels-explained/
  - https://signoz.io/blog/logging-levels/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";

<Hook>
A team turns on structured logging. Six weeks later their log bill is three times higher than their compute bill. Every service, every request, every INFO line — indexed and billed. Logging costs more than serving traffic. The service is fine. The sampling policy is missing.
</Hook>

<Crux>Sampling keeps log cost proportional to incidents, not traffic. Success-path, pattern-based, and tail sampling each cut a different slice.</Crux>

<Explanation>

## The cost equation

The cost of a log line is paid in three places: at write time (CPU + RAM for serialization), in transit (network egress + collector capacity), and at the backend (ingest GB + indexed-event count + retention bytes).

A modest service emitting one log line per request handles 1 MB/s at 1000 req/s — about 86 GB/day. At hosted log pricing ($0.10/GB ingest plus indexed-event cost), the bill compounds fast across dozens of services.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Service load</th>
        <th class="border border-border px-3 py-2 text-left">Daily volume</th>
        <th class="border border-border px-3 py-2 text-left">Monthly ingest cost</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">1000 req/s, 1 KB/log</td>
        <td class="border border-border px-3 py-2">&#126;86 GB</td>
        <td class="border border-border px-3 py-2">&#126;$260 (one service)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Same, with 1-in-10 INFO sampling</td>
        <td class="border border-border px-3 py-2">&#126;10 GB</td>
        <td class="border border-border px-3 py-2">&#126;$30</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">10 services at 1000 req/s</td>
        <td class="border border-border px-3 py-2">&#126;860 GB</td>
        <td class="border border-border px-3 py-2">&#126;$2600/month raw</td>
      </tr>
    </tbody>
  </table>
</div>

## Three sampling strategies

**Success-path sampling** keeps 1-in-N INFO lines for high-volume successful events while keeping 100% of WARN and ERROR. Typical N is 10 to 100. This is the first lever to pull: it cuts INFO volume by 90% without touching failure forensics.

**Pattern-based sampling** keeps every distinct log pattern at full rate and samples the duplicates. Vector and Fluent Bit ship sampling filters that hash the message template — so a "retry attempt 1 of 5" pattern is kept at 1-in-100 while "payment_declined" (rare) stays at 100%.

**Tail sampling for logs** mirrors the trace pattern: buffer logs for a request window, then decide based on outcome — keep all logs for failed requests, sample the successful ones. This is the most powerful strategy but requires a stateful buffer at the collector tier. It guarantees zero loss of failure context while discarding up to 99% of success-path volume.

<Inset kind="why" lang="en">
The pipeline tier (collector / agent) is the right place for sampling — not the application. Sampling at the collector keeps application code simple and lets the platform team manage the policy centrally. The anti-pattern is baking sampling into each service individually, which fragments the policy and makes it hard to change consistently across the fleet.
</Inset>

## The shipping pipeline

Logs travel through three stages: emit (the application writes JSON to stdout or a logger SDK), collect (a sidecar agent or DaemonSet reads stdout, parses JSON, batches, applies sampling), ship (OTLP-HTTP or native protocol to the backend).

The collector layer — Fluent Bit, Vector, OTel Collector with the filelog receiver — does three things you do not want in the application: backpressure (buffer on disk if the backend is slow), enrichment (attach resource attributes from pod metadata), and redaction (strip PII patterns before they leave the host).

Production rule: emit JSON to stdout, let the platform handle everything after that.

## Retention tiering: hot, warm, cold

Indexed log storage at $0.10-$1.00/GB-month is too expensive for multi-month retention at scale. Mature stacks tier:

- **Hot** (last 7-15 days, fully indexed, sub-second query — Datadog Standard, Loki recent)
- **Warm** (30-90 days, partially indexed or scan-only — Datadog Flex, Splunk Frozen-Searchable)
- **Cold** (compliance retention in S3 or equivalent at $0.023/GB-month, restorable but not directly queryable)

An incident under 7 days old runs against hot tier with full query power. An investigation into "what happened 6 months ago" needs warm-tier queries that may take minutes per scan and may not have every dimension indexed.

<NumbersCard
  id="card-log-cost-shape-lesson"
  title="Structured logs: cost and capacity numbers"
  stats={[
    { label: "Pino throughput (Node 24, 1 core)", value: "~140k msg/sec" },
    { label: "Winston throughput (same workload)", value: "~20k msg/sec" },
    { label: "Typical structured log size", value: "~0.5-2 KB" },
    { label: "Service @ 1000 req/s, 1 log/req", value: "~86 GB / day" },
    { label: "Datadog log ingest", value: "~$0.10 / GB" },
    { label: "Datadog indexed events (standard tier)", value: "~$1.27 / million" },
    { label: "Hot tier retention typical", value: "7-15 days" },
    { label: "Cold tier (S3) cost", value: "~$0.023 / GB-month" },
  ]}
/>

<Quiz
  id="04-sampling-and-cost-quiz-1"
  lessonSlug="04-sampling-and-cost"
  lang="en"
  question="A team applies 1-in-10 sampling to all log lines including ERROR. What is the problem?"
  choices={[
    { label: "No problem — uniform sampling is simpler to reason about", misconception: "Uniform sampling loses 90% of ERROR lines, making failure forensics impossible. Sampling must preserve 100% of WARN and ERROR." },
    { label: "Sampling ERROR means 90% of failure events are silently discarded, breaking incident investigations", correct: true },
    { label: "The sampling ratio should be 1-in-100, not 1-in-10", misconception: "The ratio is not the issue; applying sampling to ERROR at all is the problem." },
    { label: "Sampling should only be applied at the application, not the collector", misconception: "The collector is the preferred place for sampling; the problem is sampling ERROR, not where sampling runs." },
  ]}
/>

<Quiz
  id="04-sampling-and-cost-quiz-2"
  lessonSlug="04-sampling-and-cost"
  lang="en"
  question="What is tail sampling for logs, and what makes it different from success-path sampling?"
  choices={[
    { label: "Tail sampling discards the last lines in a log file to save storage", misconception: "Nothing to do with file position. Tail sampling buffers a complete request's logs and decides based on outcome." },
    { label: "Tail sampling buffers all logs for a request window, then keeps everything for failed requests and samples success-path logs — guaranteeing zero loss of failure context", correct: true },
    { label: "Tail sampling keeps the first N lines per minute and discards the rest", misconception: "Time-window head sampling, not tail sampling. Tail sampling decides per-request based on outcome." },
    { label: "Tail sampling is only possible in application code, not at the collector", misconception: "Tail sampling requires a stateful buffer but works at the collector tier (e.g., OTel Collector tail-sampling processor)." },
  ]}
/>

<DragOrder
  id="04-sampling-and-cost-drag-1"
  lessonSlug="04-sampling-and-cost"
  lang="en"
  prompt="Order these log cost-control levers from cheapest to most complex to implement:"
  items={[
    { id: "a", label: "Set INFO as production default, turn off DEBUG globally" },
    { id: "b", label: "Apply success-path sampling (1-in-10 INFO, 100% WARN/ERROR) at the collector" },
    { id: "c", label: "Add pattern-based sampling to collapse chatty duplicate patterns" },
    { id: "d", label: "Configure retention tiering: hot 15d, warm 90d, cold S3" },
    { id: "e", label: "Implement tail sampling with per-request buffering at a central collector gateway" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Sampling is a first-class engineering concern, not an afterthought. Keep 100% of WARN and ERROR always. Sample INFO at 1-in-10 to 1-in-100 on the success path. Use pattern-based sampling for chatty duplicate patterns. Use tail sampling when you need zero failure loss. Apply all of this at the collector tier, not in application code.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-sampling-and-cost-retrieval"
  lang="en"
  questions={[
    { q: "A service emits 1 KB JSON logs at 1000 req/s. What is the rough monthly ingest bill, and what is the standard cost-control lever to cut it by 90% without losing failure forensics?", a: "Volume: 1000 req/s × 1 KB = 1 MB/s = 86 GB/day = ~2.6 TB/month. At $0.10/GB ingest that is ~$260/month for just this one service. The standard lever: success-path sampling at 1-in-10 for INFO (keep 100% of WARN/ERROR). This cuts INFO volume by 90%, dropping the bill to ~$30-$50/month depending on error rate, without losing the forensic guarantee that all failures are retained. Pair with retention tiering (hot 7d, cold S3) to keep the audit trail without indexed-storage cost." },
    { q: "Why does sampling belong at the collector tier rather than in the application?", a: "Sampling at the collector keeps application code simple — services emit everything and let the platform decide what to store. It also allows centralized policy management: a single config file in the platform team's repo controls sampling ratios across the entire fleet. Application-level sampling fragments the policy across dozens of services with no central visibility, making it hard to audit, change, or enforce consistently. The collector also enables tail sampling (which requires buffering across a request window) that is impractical to implement in every application separately." },
    { q: "What is the retention tiering model and why does the hot/warm/cold split matter for incident response?", a: "Indexed log storage at $0.10-$1.00/GB-month is too expensive for multi-month retention. The standard tiers: hot (7-15 days, fully indexed, sub-second query), warm (30-90 days, partial index or scan-only), cold (S3 or equivalent at $0.023/GB-month, restorable but not directly queryable). The split matters for incident response because an incident under 7 days old gets full hot-tier query power with millisecond response. An investigation into events 3 months ago needs warm-tier queries that may take minutes per scan and may lack some indexed dimensions. Teams need to decide per service which questions they must be able to ask at what age and configure retention accordingly." },
  ]}
/>

<Recap lang="en">
Log cost compounds because every structured line is indexed and billed per event and per GB. A service at 1000 req/s emits ~86 GB/day — and most fleets have dozens of services. The three sampling levers: success-path sampling (1-in-10 INFO, 100% WARN/ERROR) cuts volume by 90% with zero loss of failure context; pattern-based sampling collapses chatty duplicate patterns at the collector; tail sampling buffers per-request logs and keeps everything for failures, sampling only successes. All three belong at the collector tier for centralized policy control. Pair sampling with retention tiering — hot (7-15d), warm (30-90d), cold (S3) — to keep the audit trail without the full indexed-storage bill.
</Recap>
