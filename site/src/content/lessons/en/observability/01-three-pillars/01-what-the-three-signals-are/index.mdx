---
concepts:
- metrics
- logs
- traces
- telemetry
- triage
- three-pillars
deepensInto: []
estMin: 10
lang: en
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-what-the-three-signals-are
sources:
- https://www.oreilly.com/library/view/distributed-systems-observability/9781492033431/
- https://opentelemetry.io/docs/concepts/signals/
- https://prometheus.io/docs/practices/instrumentation/
spiral:
- latency
- statefulness
status: ready
summary: The three telemetry shapes production systems emit, why each exists, and
  how a five-minute triage works by picking the right one.
title: 'What the three signals are: logs, metrics, and traces'
track: observability
unit: 01-three-pillars
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import PersonaTag from "~/components/pedagogy/PersonaTag.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
An API's p99 jumps from 80 ms to 1.2 s at 14:02 UTC. You have three different signals you can reach for. Choosing the wrong one first costs you an extra hour. Choosing the right one first costs you five minutes.
</Hook>

<Crux>Logs, metrics, traces — each is the cheapest answer to a different question. The question picks the signal, not the other way around.</Crux>

<Explanation>

## The three signals

Cindy Sridharan's 2018 O'Reilly book *Distributed Systems Observability* crystallised the vocabulary: production systems emit three telemetry shapes.

- **Metrics** — numeric measurements aggregated over time with a small fixed set of labels. Pre-aggregated, cheap to query across years of history, blind to any dimension you did not label in advance.
- **Logs** — event records (timestamp + payload). Preserve every field you wrote, at the cost of ingestion bytes and query scan time.
- **Traces** — the causal chain of one request across services. Preserve causality and timing per span, at the cost of sampling — storing every trace would multiply storage by an order of magnitude over the original traffic.

Together, each signal fills the blind spot of the other two: metrics see the trend but not the record, logs see the record but not the path, traces see the path but not the population. The three are not redundant. Each preserves something different and pays a different cost. A team with only one signal cannot triage end-to-end.

<StackDiagram
  label="The three observability signals: what each answers"
  hue="var(--d-systems)"
  layers={[
    { label: "Metrics — aggregates & counters", note: "is anything wrong?" },
    { label: "Logs — discrete events", note: "what exactly happened?" },
    { label: "Traces — request spans", note: "where did time go?" },
  ]}
  caption="Three complementary shapes: metrics raise the alarm, logs give the diagnostic, traces follow one request across services."
/>

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Signal</th>
        <th class="border border-border px-3 py-2 text-left">What it preserves</th>
        <th class="border border-border px-3 py-2 text-left">What it discards</th>
        <th class="border border-border px-3 py-2 text-left">Cost axis</th>
      </tr>
    </thead>
    <tbody>
      <tr><td class="border border-border px-3 py-2">Metrics</td><td class="border border-border px-3 py-2">Aggregate trends, percentiles</td><td class="border border-border px-3 py-2">Any unlabelled dimension</td><td class="border border-border px-3 py-2">Cardinality (series count)</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">Logs</td><td class="border border-border px-3 py-2">Every field in every event</td><td class="border border-border px-3 py-2">Nothing — that is the problem</td><td class="border border-border px-3 py-2">Ingestion bytes &amp; retention</td></tr>
      <tr><td class="border border-border px-3 py-2">Traces</td><td class="border border-border px-3 py-2">Causal chain of one request</td><td class="border border-border px-3 py-2">Unsampled requests</td><td class="border border-border px-3 py-2">Storage per span × traffic</td></tr>
    </tbody>
  </table>
</div>

## The kitchen metaphor

A restaurant kitchen maps the three signals clearly. **Metrics** are wall dials — covers per hour, average ticket time, error rate. Cheap to read, always-on, but they only show what you labelled in advance. **Logs** are order tickets on the rail — every dish, every modifier, every table. Bulky, but when a customer complains you go to their ticket. **Traces** are the story of one specific order: prep at station A, sauté at B, plate-up, runner, table. Sampled — tracing every dish would bury you — but a cold dish lights the whole path.

## The triage walk

<PersonaTag id="bea" lang="en" /> gets paged: checkout error rate jumped to 3%. She opens metrics — sees the spike at 14:02 UTC. The metric says "yes, errors are real" but not "why." <PersonaTag id="sven" lang="en" /> pulls logs filtered to `status=5xx` — sees a stack trace pointing at a payment-gateway timeout. Logs gave the cause. To know whether the request retried or wrote stale state, they pick a `trace_id` from the log line, open the span tree, and see the gateway timing out after 30 s with no retry. Three signals, three jobs, seconds each.

The rule: **metrics are the alarm**, **logs are the diagnostic**, **traces are the deep dive**.

<Inset kind="why" lang="en">
The three pillars exist because no single storage shape is cheap on all three of: long retention, high cardinality, and full request fidelity. Metrics excel at long retention; logs excel at high cardinality; traces excel at full request fidelity. Each one optimises for the questions its cost model handles cheaply, and offloads the rest to the other two.
</Inset>

<DragOrder
  id="01-what-the-three-signals-are-drag-1"
  lessonSlug="01-what-the-three-signals-are"
  lang="en"
  prompt="Order the typical incident triage steps from cheapest signal to deepest:"
  items={[
    { id: "a", label: "Glance at metrics dashboards (RED, USE) — confirm the symptom is real" },
    { id: "b", label: "Open the alert source — identify the affected service and time window" },
    { id: "c", label: "Filter logs to errors in the window — read messages and stack traces" },
    { id: "d", label: "Pick one failed trace_id from a log line" },
    { id: "e", label: "Open the trace — see every span and where time was spent" },
    { id: "f", label: "If still unresolved, reach for profiles or replay the request" },
  ]}
  correctOrder={["b", "a", "c", "d", "e", "f"]}
/>

<Quiz
  id="01-what-the-three-signals-are-quiz-1"
  lessonSlug="01-what-the-three-signals-are"
  lang="en"
  question="A service emits a counter labelled by route, method, and status_class. Which of the three signals is this?"
  choices={[
    { label: "Metrics — numeric measurement with bounded labels, aggregated over time", correct: true },
    { label: "Logs — text events with timestamps", misconception: "Logs are unstructured or structured event records, not numeric counters aggregated into time series." },
    { label: "Traces — causal chain of spans across services", misconception: "Traces describe a single request's path across services, not a numeric aggregate." },
    { label: "Profiles — sampled stack traces", misconception: "Profiles measure CPU time per function, not request counts." },
  ]}
/>

<Quiz
  id="01-what-the-three-signals-are-quiz-2"
  lessonSlug="01-what-the-three-signals-are"
  lang="en"
  question="Why do production teams sample traces instead of storing every one?"
  choices={[
    { label: "Sampling makes traces more accurate", misconception: "Sampling reduces volume; accuracy is preserved by smart sampling, not improved by sampling itself." },
    { label: "A trace can carry hundreds of spans; storing every trace at full traffic would multiply storage cost by 10x to 100x relative to original request volume", correct: true },
    { label: "Traces are less important than metrics", misconception: "Traces and metrics answer different questions; neither is universally more important." },
    { label: "Traces only work on sampled requests by protocol", misconception: "Traces work on every request technically — the limit is cost, not protocol." },
  ]}
/>

<MetaphorComplete
  id="01-what-the-three-signals-are-metaphor-1"
  pieceSlug="01-what-the-three-signals-are"
  lang="en"
  setup="Fill in the blank: _______ are the wall dials — cheap to read, always-on, but they only show what you labelled in advance."
  accepted={["metrics", "metric"]}
  canonical="metrics"
  explanation="Metrics are pre-aggregated numeric measurements with a fixed set of labels. They are cheap to read because they are pre-rolled-up into time series, but they can only answer questions about dimensions you decided to record before the question was asked."
/>

</Explanation>

<KeyTakeaway>Three signals, three cost shapes: metrics aggregate cheaply but discard unlabelled dimensions; logs preserve every field but pay in bytes; traces preserve causality but must sample. The signal you reach for first should be the cheapest answer to the specific question at hand.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-what-the-three-signals-are-retrieval"
  lang="en"
  questions={[
    { q: "In two sentences, why are the three signals not interchangeable?", a: "Each signal preserves something different and pays a different cost. Metrics throw away every dimension except the labels you set, logs preserve the full event at the cost of bytes, and traces preserve the causal chain at the cost of sampling — a team with only one cannot do triage end-to-end." },
    { q: "What is the triage order for an unknown production incident, and why?", a: "Start with metrics: they confirm the symptom is real and scope it to a service and time window at zero marginal cost. Then logs: filter to errors in that window to read the actual message. Then traces: pick a trace_id from a log line and open the span tree to see where time was spent. Metrics are fastest but least specific; traces are most specific but sampled and expensive to query." },
    { q: "Why does the kitchen metaphor work for the three signals?", a: "Metrics are wall dials — always-on but only show pre-labelled data. Logs are order tickets — every detail, but you must search through them. Traces are the story of one specific order — complete picture of one path, but you can only afford to trace a fraction of orders." },
  ]}
/>

<Recap lang="en">
The three production telemetry signals are metrics (aggregated numeric measurements with fixed labels), logs (timestamped event records preserving every field), and traces (causal chains of spans for individual requests). Each exists because no single storage shape is cheap on long retention, high cardinality, and full request fidelity simultaneously. In incident triage, metrics confirm the symptom, logs give the diagnostic message, and traces identify the exact span where time was lost. Now when you see an alert fire, you will instinctively reach for metrics first to confirm the symptom, then logs to read the cause, then a trace to see where time was spent — rather than spending an hour in the wrong signal.
</Recap>
