---
concepts:
- join-keys
- semantic-conventions
- opentelemetry
- exemplars
- otlp
- otel-collector
- service-name
deepensInto: []
estMin: 12
lang: en
lessonType: topic
level: middle
order: 5
prereqs:
- observability/01-three-pillars/04-traces-and-sampling
slug: 05-join-keys-and-exemplars
sources:
- https://opentelemetry.io/docs/concepts/signals/
- https://www.honeycomb.io/blog/opentelemetry-is-not-three-pillars
- https://prometheus.io/docs/practices/instrumentation/
spiral:
- latency
status: ready
summary: How shared attribute names connect metrics, logs, and traces into a navigable
  surface, and how exemplars bridge aggregate metrics to individual traces.
title: 'Join keys and exemplars: making the three signals compose'
track: observability
unit: 01-three-pillars
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A metric shows checkout p99 spiking. You open the log search — but your metric uses `route="/checkout"`, the log uses `http_path="/checkout"`, and the trace uses `http.route="/checkout"`. Three different field names, three disconnected searches. The pivot that should take 10 seconds takes 20 minutes.
</Hook>

<Crux>Join keys — shared attribute names in metrics, logs, and traces — make triage pivots a single click instead of a 20-minute manual search.</Crux>

<Explanation>

## The join key problem

Three signals on three different backends are useless if you cannot navigate between them. The bridge is a small set of shared attribute keys that appear identically in metrics, logs, and traces. When they diverge — even subtly — every dashboard needs ad-hoc query translation and every on-call run-book is fragile.

The standard set, formalised in **OpenTelemetry Semantic Conventions**:

- `service.name` — which service emitted this
- `trace_id` + `span_id` — which request, which step
- `http.route` — the templated route (e.g. `/orders/{id}`, not the full URL `/orders/42`)
- `http.request.method`, `http.response.status_code`
- `db.system`, `db.operation`, `db.namespace`
- `messaging.system`, `messaging.destination.name`

When metrics carry the same `service.name` and `http.route` as log lines, and log lines carry the `trace_id` of the request that emitted them, and traces carry the same `http.route` attribute as the metric — that is when "click from metric spike to log line to trace" works in under 30 seconds. Without those join keys you have three disconnected dashboards and no narrative.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Join key</th>
        <th class="border border-border px-3 py-2 text-left">In metrics</th>
        <th class="border border-border px-3 py-2 text-left">In logs</th>
        <th class="border border-border px-3 py-2 text-left">In traces</th>
      </tr>
    </thead>
    <tbody>
      <tr><td class="border border-border px-3 py-2">service.name</td><td class="border border-border px-3 py-2">label</td><td class="border border-border px-3 py-2">field</td><td class="border border-border px-3 py-2">resource attribute</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">trace_id</td><td class="border border-border px-3 py-2">exemplar (sampled)</td><td class="border border-border px-3 py-2">mandatory field</td><td class="border border-border px-3 py-2">root identifier</td></tr>
      <tr><td class="border border-border px-3 py-2">http.route</td><td class="border border-border px-3 py-2">label (templated)</td><td class="border border-border px-3 py-2">field</td><td class="border border-border px-3 py-2">span attribute</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">http.response.status_code</td><td class="border border-border px-3 py-2">label (class)</td><td class="border border-border px-3 py-2">field (full code)</td><td class="border border-border px-3 py-2">span attribute (full code)</td></tr>
    </tbody>
  </table>
</div>

## Exemplars: the metric-to-trace bridge

An exemplar is a sampled `trace_id` attached to a histogram bucket observation. When `http_request_duration_seconds` records a 1.5 s observation in the bucket above 1 s, the histogram client optionally attaches one `trace_id` that produced that observation. Grafana renders these as dots on the histogram heatmap; clicking a dot opens the corresponding trace.

Exemplars (standardised in Prometheus 2.32, early 2022, now standard in OTel exporters) eliminate the gap that defined the pre-exemplar era: "metrics say something is slow but I have no example request to drill into." The pivot is:

1. Metric histogram spike → click exemplar dot
2. Exemplar opens the trace for the specific slow request
3. Trace span shows `db.query` took 1.3 s
4. Log filtered to `trace_id=<same>` shows the full query text

No manual timestamp correlation. No searching across dashboards. One click chain. Steps 1–4 only work because each signal carries the same `trace_id`; without that shared join key, step 2 would require manually guessing which trace to look at from a timestamp window — and you would be wrong half the time.

## OpenTelemetry: the integration layer

The OpenTelemetry project (CNCF, born 2019 from a merger of OpenCensus and OpenTracing) standardises three things:

- **API** — the surface engineers write code against: `tracer.start_span()`, `meter.create_counter()`.
- **SDK** — the per-language implementation that builds OTLP messages.
- **OTLP** — OpenTelemetry Protocol: binary protobuf over gRPC or HTTP, roughly 50–70% smaller than JSON over HTTP.

The pitch: **instrument once, route anywhere**. Application code calls the OTel API, the SDK builds OTLP messages, the OTel Collector forwards them to any backend — Honeycomb, Datadog, Jaeger, ClickHouse, Loki, Prometheus — anything that speaks OTLP or has a contrib exporter. Without OTel Semantic Conventions, every team invents their own field names and joins fail at integration time.

<Inset kind="why" lang="en">
The OTel Semantic Conventions document is the single most load-bearing piece of the observability stack. It is not the SDK or the wire format — those are plumbing. The field names are the semantics. Adopting them across all three signals — even if your storage backends are Prometheus + Loki + Jaeger — is the highest-leverage investment in observability hygiene because it is what makes the three pillars compose rather than fight.
</Inset>

<FlowDiagram
  label="One trace_id joins all three signals; an exemplar bridges a histogram bucket to a sample trace"
  hue="var(--d-systems)"
  nodes={[
    { id: "metric", label: "Metric", sub: "service.name + http.route", col: 0, row: 0 },
    { id: "log", label: "Log line", sub: "trace_id field", col: 0, row: 2 },
    { id: "tid", label: "trace_id", sub: "shared join key", col: 1, row: 1 },
    { id: "trace", label: "Trace", sub: "root span id", col: 2, row: 1 },
    { id: "exemplar", label: "Exemplar", sub: "sampled trace_id", col: 3, row: 0 },
    { id: "bucket", label: "Histogram", sub: "bucket > 1s", col: 4, row: 0 },
  ]}
  edges={[
    { from: "metric", to: "tid", label: "carries" },
    { from: "log", to: "tid", label: "carries" },
    { from: "trace", to: "tid", label: "is" },
    { from: "bucket", to: "exemplar", label: "attaches" },
    { from: "exemplar", to: "trace", label: "opens" },
  ]}
  caption="The three signals each carry the same trace_id, so a metric spike, its log lines, and its trace are one click apart. An exemplar attaches a sampled trace_id to a slow histogram bucket, opening the exact trace behind the spike."
/>

<DragOrder
  id="05-join-keys-and-exemplars-drag-1"
  lessonSlug="05-join-keys-and-exemplars"
  lang="en"
  prompt="Walk the three-pillar triage for 'p99 checkout latency spiked in us-east-1 only':"
  items={[
    { id: "a", label: "Open the RED histogram filtered by region — confirm us-east-1 p99=1.4s, eu-west-1=90ms" },
    { id: "b", label: "Click the exemplar dot on the >1s histogram bucket" },
    { id: "c", label: "Open the trace — see 1.1s spent in the payment-gateway span" },
    { id: "d", label: "Filter logs to service=payment-proxy, region=us-east-1, around the trace timestamp" },
    { id: "e", label: "Read the log line: 'gateway upstream connect timeout, retrying'" },
    { id: "f", label: "Fix: add region as a metric label, add gateway.upstream.host as a span attribute" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f"]}
/>

<Quiz
  id="05-join-keys-and-exemplars-quiz-1"
  lessonSlug="05-join-keys-and-exemplars"
  lang="en"
  question="A team's metrics use route='/checkout', logs use http_path='/checkout', and traces use http.route='/checkout'. What is the consequence?"
  choices={[
    { label: "Dashboards work fine; field names are cosmetic", misconception: "If the field names differ, automated pivots from metric to log to trace cannot use a shared key — every query must manually translate between naming conventions." },
    { label: "Pivoting from a metric spike to the matching log line or trace requires manual query translation; the 30-second triage walk becomes a 20-minute multi-tab search", correct: true },
    { label: "The OTel collector fails to forward data", misconception: "The collector forwards data regardless of naming; the problem is operability during triage, not data pipeline failure." },
    { label: "Cardinality increases because field names differ", misconception: "Cardinality is a metrics concern about label-value counts, not field naming consistency." },
  ]}
/>

<Quiz
  id="05-join-keys-and-exemplars-quiz-2"
  lessonSlug="05-join-keys-and-exemplars"
  lang="en"
  question="Why is OpenTelemetry Semantic Conventions load-bearing even for teams that stay on 1.0 backends (Prometheus, Loki, Jaeger)?"
  choices={[
    { label: "It reduces memory usage in the OTel collector", misconception: "Semantic conventions are about field names, not collector memory optimisation." },
    { label: "It forces teams to use the OTel SDK", misconception: "Semantic conventions can be adopted regardless of which SDK or agent you use." },
    { label: "It defines the canonical field names (service.name, http.route, trace_id, etc.) so that the three backends can be navigated with shared keys — without which each dashboard requires ad-hoc translation and on-call run-books are fragile", correct: true },
    { label: "It eliminates the need for exemplars", misconception: "Exemplars are a complementary mechanism; semantic conventions define naming but do not replace the metric-to-trace bridge." },
  ]}
/>

</Explanation>

<KeyTakeaway>Shared join keys — service.name, trace_id, http.route — are what transform three disconnected backends into one navigable surface. OpenTelemetry Semantic Conventions defines the canonical names. Exemplars bridge the gap from an aggregate metric spike to a concrete individual trace, eliminating manual cross-signal correlation during triage.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-join-keys-and-exemplars-retrieval"
  lang="en"
  questions={[
    { q: "Name the five most important join keys for three-signal triage and where each appears.", a: "service.name: metric label, log field, trace resource attribute — identifies which service emitted the data. trace_id: metric exemplar (sampled), mandatory log field, trace root identifier — the cross-pillar join key for a specific request. http.route: metric label (templated), log field, trace span attribute — the templated route used for all three. http.response.status_code: metric label (as status class), log field (full code), trace span attribute (full code). db.system / db.operation: log fields and trace span attributes for database calls." },
    { q: "Explain the exemplar click-through chain from a slow metric to a log line.", a: "1. Metric histogram shows a spike in the >1 s bucket. 2. Click the exemplar dot — Grafana opens the trace_id attached to that histogram observation. 3. The trace waterfall shows which span was slow (e.g., db.query = 1.3 s). 4. Filter logs to trace_id=<same> and timestamp around the trace — reads the full query text or error message. The entire pivot takes under 30 s because every signal shares the trace_id join key." },
    { q: "Why did OpenTelemetry emerge from a merger of OpenCensus and OpenTracing, and what does it standardise that neither predecessor did alone?", a: "OpenCensus (Google) standardised metric and trace instrumentation for specific languages; OpenTracing standardised the trace API across languages but not metrics. Their merger into OpenTelemetry produced a single project covering: the API (language-neutral surface), the SDK (per-language implementation), and OTLP (a single binary wire format). The critical addition is Semantic Conventions — the canonical field names that make the signals compose across any backend." },
  ]}
/>

<Recap lang="en">
Three observability signals on separate backends are only useful when they share join keys — field names that appear identically in metric labels, log fields, and trace span attributes. OpenTelemetry Semantic Conventions defines these canonical names: `service.name`, `trace_id`, `http.route`, `db.system`, and others. Without them, pivoting from a metric spike to the matching log line requires manual translation; with them it is a single click. Exemplars extend this: a `trace_id` attached to a histogram bucket observation bridges an aggregate metric spike directly to one concrete slow request's trace. OpenTelemetry (API + SDK + OTLP wire format) is the substrate that makes all three signals share these join keys in practice across any backend combination. Now when you instrument a new service, you will check that its metric labels, log fields, and span attributes all use the same OTel semantic convention names — because you know a single inconsistency turns a 30-second triage into a 20-minute search.
</Recap>
