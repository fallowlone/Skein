---
concepts:
- signal-selection
- cardinality
- log-volume
- trace-sampling
- join-keys
- observability-2.0
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://opentelemetry.io/docs/concepts/signals/
- https://prometheus.io/docs/practices/instrumentation/
- https://www.honeycomb.io/blog/opentelemetry-is-not-three-pillars
status: ready
summary: "Multiple-choice synthesis across the three-pillars unit — signal selection, cardinality, log volume, sampling, join keys, and the 2.0 cost shift."
title: 'Three pillars: multiple-choice review'
track: observability
unit: 01-three-pillars
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one mirrors a decision you make mid-incident or at instrumentation time — not a definition to recite, but a cost tradeoff to weigh.
</Hook>

<Goal lang="en">
Confirm you can pick the right signal for a question, reason about its cost axis, and connect the three pillars through join keys — the synthesis the individual lessons built toward.
</Goal>

<Quiz
  id="01-three-pillars-choice-1"
  lessonSlug="01-three-pillars"
  lang="en"
  question="A product manager wants per-customer error rates broken down by individual customer_id across 200k active customers, queryable for the last 30 days. In a 1.0 stack, which signal carries this data, and why not the others?"
  choices={[
    { label: "Logs (or traces) — high cardinality is free at write time there; the cost is ingestion bytes and query scan, not series-count memory. Pivot to a specific customer via a bounded customer_segment metric label plus an exemplar.", correct: true },
    { label: "Metrics — just add customer_id as a label so the dashboard can group by it.", misconception: "customer_id is unbounded; 200k values multiply the series count by 200k and OOM the TSDB. Per-customer identity belongs in logs or traces, never in a metric label." },
    { label: "Metrics with a Summary type, which pre-computes the per-customer percentile.", misconception: "A Summary computes percentiles per replica and still needs a label per customer to separate them — the cardinality problem is unchanged, and summaries cannot aggregate across replicas." },
    { label: "None — per-customer breakdown is impossible in a 1.0 stack.", misconception: "It is entirely possible in logs and traces, which preserve every field. Only the metrics pillar cannot afford unbounded per-customer labels." },
  ]}
/>

<Quiz
  id="01-three-pillars-choice-2"
  lessonSlug="01-three-pillars"
  lang="en"
  question="Two services share a heavy endpoint. Service A holds a stable label set (route × method × status_class = 400 series) but logs every request. Service B has tiny log volume but added user_id to one counter. Which threatens its observability backend first, and on which axis?"
  choices={[
    { label: "Service B — an unbounded user_id label multiplies series count into the millions and OOMs the TSDB, while A's log volume only grows the ingest bill linearly and degrades gracefully.", correct: true },
    { label: "Service A — high log volume always crashes the backend faster than a metric label.", misconception: "Log volume drives a linear ingest bill and query latency, not a hard memory cliff. It is a cost problem, not an OOM-in-minutes problem like a cardinality bomb." },
    { label: "Both fail identically — cardinality and log volume are the same cost.", misconception: "They are different cost axes: cardinality is series-count memory in the TSDB (a cliff); log volume is ingestion bytes and scan time (linear). Conflating them is the core confusion the unit dismantles." },
    { label: "Neither — modern backends auto-shard both away.", misconception: "Auto-sharding helps log ingest but does not save a Prometheus head block from millions of series; an unbounded label still OOMs the TSDB." },
  ]}
/>

<Quiz
  id="01-three-pillars-choice-3"
  lessonSlug="01-three-pillars"
  lang="en"
  question="A team runs tail-based sampling at the collector keeping 100% of errors and 2% of successes. Their trace bill barely drops versus 100% retention, even though stored volume fell sharply. Why?"
  choices={[
    { label: "Tail sampling buffers every span through the collector until the trace completes, so collector CPU, memory, and ingress scale with raw traffic — only the downstream storage shrinks. The savings show up in storage, not collector cost.", correct: true },
    { label: "Tail sampling is broken; it should reduce all costs proportionally.", misconception: "It works as designed. Storage volume drops, but the collector must still receive and buffer every span to make a context-aware decision — that cost is unavoidable in tail sampling." },
    { label: "They should switch to 100% head-based sampling to save money.", misconception: "100% head sampling saves nothing; head sampling saves by dropping at trace start. The point of tail sampling is keeping rare errors and slow tails that head sampling would randomly miss." },
    { label: "The error traces are the entire cost, so sampling successes never helps.", misconception: "Errors are usually a small fraction of traffic. The dominant remaining cost here is collector buffering of all spans, not the kept error traces." },
  ]}
/>

<Quiz
  id="01-three-pillars-choice-4"
  lessonSlug="01-three-pillars"
  lang="en"
  question="Metrics use route='/checkout', logs use http_path='/checkout', and traces use http.route='/checkout'. The data is all correct and flowing. What actually breaks, and what is the fix?"
  choices={[
    { label: "Cross-signal triage breaks: with no shared key name, pivoting metric to log to trace needs manual query translation per backend, turning a 30-second walk into a 20-minute multi-tab hunt. Fix: adopt OpenTelemetry Semantic Conventions so all three emit http.route.", correct: true },
    { label: "Nothing — field names are cosmetic as long as the values match.", misconception: "Automated pivots key on the field NAME, not just the value. Divergent names mean every dashboard and run-book carries fragile ad-hoc translation. Naming consistency is the entire join-key thesis." },
    { label: "The OTel Collector refuses to forward the data.", misconception: "The collector forwards regardless of attribute naming. The failure is operational friction during triage, not a broken pipeline." },
    { label: "Metric cardinality increases because the names differ.", misconception: "Cardinality is about label-VALUE counts within one metric, not whether three backends agree on a field name. Naming divergence is a join problem, not a cardinality problem." },
  ]}
/>

<Quiz
  id="01-three-pillars-choice-5"
  lessonSlug="01-three-pillars"
  lang="en"
  question="A 40-service team spends ~2 engineer-weeks per quarter policing cardinality budgets and ~3 hours per incident on cross-pillar pivots. They also keep 5 years of low-cardinality SLO metrics for compliance. What is the senior recommendation?"
  choices={[
    { label: "Run both: a 2.0 wide-event backend for incident response and ad-hoc high-cardinality questions, and keep a 1.0 TSDB tier for the 5-year low-cardinality SLO trends where pre-aggregation is cheapest. The decision is economic, not ideological.", correct: true },
    { label: "Migrate everything to 2.0 — three pillars are obsolete.", misconception: "2.0 wins for incident response and high-cardinality queries, but raw wide events over 5 years cost more than pre-aggregated metric rollups. 'Always better' is the misconception lesson 6 explicitly refutes." },
    { label: "Stay fully on 1.0 — it is the lowest infra cost.", misconception: "Lowest infra cost ignores the hidden engineering cost: 2 weeks/quarter on label hygiene plus 3 hours/incident on pivots is the real bill driving the 2.0 case." },
    { label: "Build a custom in-house wide-event store to avoid SaaS lock-in.", misconception: "Reaching feature parity with a SaaS wide-event backend takes 1–2 engineer-years; that is only justified for very large orgs with a dedicated observability team, not a 40-service shop." },
  ]}
/>

<Quiz
  id="01-three-pillars-choice-6"
  lessonSlug="01-three-pillars"
  lang="en"
  question="During an incident the on-call finds the trace they need was never sampled — the SDK uses ParentBased(TraceIdRatioBased) but the trace_id is derived from a hash of the request path. What is the root cause, and what is the durable fix?"
  choices={[
    { label: "A path-derived trace_id makes the sampling decision correlate with the endpoint, so a whole endpoint cohort is always sampled or always dropped. Fix: use a cryptographically random trace_id, and layer tail-based policies at the collector to keep 100% of errors and slow traces.", correct: true },
    { label: "ParentBased ignores the incoming traceparent flag, so downstream services re-decide randomly.", misconception: "ParentBased explicitly honours the incoming sampled flag — that is its job. The defect is the non-random trace_id seed, flagged in the Elastic 2024 post-mortem." },
    { label: "Head-based sampling is always wrong; only tail sampling should ever be used.", misconception: "Head sampling is the cheap baseline and is fine when the trace_id is truly random. The fix is a random seed plus tail policies for the interesting cases, not abolishing head sampling." },
    { label: "The sampling rate is simply too low; raise it to 100%.", misconception: "100% sampling removes the cost savings that justified sampling and still does not fix the correlation bug. A random seed plus tail policies keeps cost bounded and never loses error traces." },
  ]}
/>

<Recap lang="en">
The through-line of the unit is one decision: the question picks the signal at its cheapest cost axis. Metrics are cheap-to-query but die on cardinality; logs preserve everything but pay in ingestion bytes; traces preserve causality but must sample (and tail sampling shifts cost from storage to collector). Join keys — service.name, trace_id, http.route via OpenTelemetry Semantic Conventions — are what make the three compose into one navigable surface. The 2.0 wide-event model collapses all three when the engineering cost of cardinality discipline and cross-pillar pivots exceeds the bill of a unified backend — an economic call, not an ideological one.
</Recap>
