---
concepts:
- logs
- structured-logging
- log-level
- ingestion
- retention
- log-pipeline
- tiered-storage
deepensInto: []
estMin: 12
lang: en
lessonType: topic
level: middle
order: 3
prereqs:
- observability/01-three-pillars/02-metrics-and-cardinality
slug: 03-logs-and-volume
sources:
- https://www.oreilly.com/library/view/distributed-systems-observability/9781492033431/
- https://opentelemetry.io/docs/concepts/signals/
- https://grafana.com/blog/how-to-manage-high-cardinality-metrics-in-prometheus-and-kubernetes/
spiral:
- latency
status: ready
summary: How structured logs work, why ingestion bytes dominate the bill, and how
  level discipline and tiered retention control the cost.
title: 'Logs and volume: the cost model of structured logging'
track: observability
unit: 01-three-pillars
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A team's logging bill quadrupled in two months while traffic is flat. One refactor quietly changed a retry count from 3 to 20 — and with it, an INFO log line emitted per attempt. Sixty GB per day became 600 GB per day from one service.
</Hook>

<Crux>Ingestion bytes per day is the dominant log cost driver. One chatty log line in a hot path can multiply the bill by 10x overnight.</Crux>

<Explanation>

## What a structured log is

A log line is an event record: a timestamp plus a payload that describes what happened. Production observability in 2026 is **structured-or-nothing**: a JSON event with consistent keys is the only form that supports filter-aggregate queries at scale.

Mandatory fields per log line:
- `timestamp` (ISO-8601 UTC)
- `level` (`debug` / `info` / `warn` / `error`)
- `service.name`
- `trace_id` (the join key to traces)
- `span_id`
- `message`
- Plus event-specific fields (`user_id`, `order_id`, `status_code`, etc.)

The join key `trace_id` is what makes it possible to navigate from a metric spike to a log line to a trace with a single click. Without it, you have three disconnected dashboards and no narrative.

## The storage model

The pipeline is append-only ingest with indexed search:

1. Application emits JSON to stdout or a local agent (Fluent Bit, Vector, OTel Collector).
2. Agent buffers and ships to a log backend (Loki, ClickHouse, Elasticsearch, Datadog, Splunk).
3. Backend indexes a subset of fields for fast filtering; stores the rest for raw scan.

Logs preserve everything you wrote — high cardinality is **free at write time** but **expensive at query time**, since the backend must scan or index every field. Unlike metrics, there is no cardinality explosion from adding `user_id` to a log line; the cost is paid in ingestion bytes and query scan time, not in series-count memory.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Log level</th>
        <th class="border border-border px-3 py-2 text-left">Meaning</th>
        <th class="border border-border px-3 py-2 text-left">Production default</th>
      </tr>
    </thead>
    <tbody>
      <tr><td class="border border-border px-3 py-2">debug</td><td class="border border-border px-3 py-2">Verbose trace of internal state</td><td class="border border-border px-3 py-2">Off</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">info</td><td class="border border-border px-3 py-2">State-change events (request completed, order created)</td><td class="border border-border px-3 py-2">On, but sample high-volume paths at 1%</td></tr>
      <tr><td class="border border-border px-3 py-2">warn</td><td class="border border-border px-3 py-2">Recoverable failure (retry succeeded after N attempts)</td><td class="border border-border px-3 py-2">100%</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">error</td><td class="border border-border px-3 py-2">Unrecoverable, actionable failure</td><td class="border border-border px-3 py-2">100%</td></tr>
    </tbody>
  </table>
</div>

## Ingestion volume: the cost math

A modest service at 1 000 req/s emitting 1 KB of structured JSON per request produces:
- 1 MB/s = 86 GB/day = 2.6 TB/month per service

Multiply by 50 services in a typical fleet and the bill at $0.10/GB ingest plus indexed-event cost reaches tens of thousands per month before retention costs.

Standard cost-control patterns:

1. **Log-level discipline.** DEBUG off in production. INFO for state-change events only. WARN for recoverable failures. ERROR for actionable failures.
2. **Sampling on success paths.** 1-in-100 for high-volume successful INFO events; 100% for errors and warns.
3. **Tiered retention.** 15-day hot for queries; archive to cheap object storage for compliance. Most log queries happen in the first 3 days.
4. **Redirect chatty tiers.** Datadog Flex Logs and Splunk tiered indexes let you route high-volume/low-value events out of the expensive indexed tier into a cheaper scan tier.

<Inset kind="why" lang="en">
The retry-log antipattern is the most common cause of surprise log bills: a refactor raises retries from 3 to 20, each attempt emits an INFO line, and the service that was responsible for 5 GB/day suddenly produces 60 GB/day. The fix is to emit a single structured log at the end of the retry sequence — `{attempts: 20, final_outcome: "timeout"}` — instead of one line per attempt. A metric counter `payment_retries_total{outcome}` captures the volume cheaply.
</Inset>

<FlowDiagram
  label="Log volume control: where bytes get cut before the expensive store"
  perRow={5}
  hue="var(--d-systems)"
  nodes={[
    { id: "emit", label: "App emits", sub: "JSON event" },
    { id: "level", label: "Level filter", sub: "DEBUG dropped" },
    { id: "sample", label: "Sampling", sub: "INFO 1%" },
    { id: "ship", label: "Agent ships", sub: "Vector / OTel" },
    { id: "store", label: "Store $$", sub: "index + retain" },
  ]}
  edges={[
    { from: "emit", to: "level", label: "all lines" },
    { from: "level", to: "sample", label: "WARN/ERROR 100%" },
    { from: "sample", to: "ship", label: "reduced volume" },
    { from: "ship", to: "store", label: "GB/day billed" },
  ]}
  caption="Volume is cut at the level filter (DEBUG off) and the sampler (INFO at 1%, WARN/ERROR kept 100%) before reaching the store, where indexing and retention dominate the bill. Cutting bytes upstream is cheaper than storing them."
/>

<DragOrder
  id="03-logs-and-volume-drag-1"
  lessonSlug="03-logs-and-volume"
  lang="en"
  prompt="A logging bill quadrupled while traffic is flat. Order the diagnosis steps:"
  items={[
    { id: "a", label: "Open vendor dashboard — find ingest GB/day per service" },
    { id: "b", label: "Identify the service whose volume jumped (e.g., from 5 GB/day to 60 GB/day)" },
    { id: "c", label: "Aggregate log volume by log_pattern inside that service" },
    { id: "d", label: "Find the chatty log line (e.g., one per retry attempt)" },
    { id: "e", label: "Assess the correct log level (retry attempt = DEBUG or one WARN on final failure)" },
    { id: "f", label: "Apply short-term mitigation: route to sampled tier or raise level to WARN" },
    { id: "g", label: "Durable fix: emit one summary log at end of retry sequence + add a metric counter" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g"]}
/>

<Quiz
  id="03-logs-and-volume-quiz-1"
  lessonSlug="03-logs-and-volume"
  lang="en"
  question="A service emits an INFO log line for every retry attempt. After a refactor raises retries from 3 to 20, the logging bill 4x in two months. What is the durable fix?"
  choices={[
    { label: "Switch to plaintext logs to save bytes", misconception: "Plaintext logs are harder to query and filter; the real problem is volume, not JSON encoding overhead." },
    { label: "Emit one structured log at the end of the retry sequence (attempts_total, final_outcome) instead of one per attempt, and add a metric counter for retry volume", correct: true },
    { label: "Raise the retry count back to 3", misconception: "Reducing retries changes application behaviour; the real fix is log discipline, not business logic." },
    { label: "Turn off logging for the payment service entirely", misconception: "That removes a critical diagnostic signal for failures." },
  ]}
/>

<Quiz
  id="03-logs-and-volume-quiz-2"
  lessonSlug="03-logs-and-volume"
  lang="en"
  question="Why does adding user_id to a log line not cause the same explosion as adding it as a metric label?"
  choices={[
    { label: "Log lines are compressed, so the extra field is negligible", misconception: "Compression helps but is not the reason; the architectural difference is write-path vs series-count." },
    { label: "Log backends store each event as raw bytes; there is no per-value series that multiplies in memory — the cost is ingestion volume and query scan time, not in-memory series count", correct: true },
    { label: "Logs do not support high-cardinality fields", misconception: "Logs support arbitrary fields including high-cardinality ones; that is precisely their strength over metrics." },
    { label: "The user_id field is automatically deduplicated in log storage", misconception: "No automatic deduplication happens; each event is stored as a separate record." },
  ]}
/>

<NumbersCard
  id="03-logs-volume-numbers"
  title="Log volume cost numbers"
  stats={[
    { label: "Typical log volume per service at 1k req/s with 1 KB per request", value: "~86 GB / day" },
    { label: "Datadog log ingest", value: "~$0.10 / GB" },
    { label: "Datadog indexed log events", value: "~$1.27 / million" },
    { label: "Hot retention typical (query tier)", value: "15 days" },
    { label: "Recommended success-path log sampling", value: "1% INFO, 100% WARN+ERROR" },
    { label: "Retry-antipattern volume multiplier (3→20 retries)", value: "~6-7x bill increase" },
  ]}
/>

</Explanation>

<KeyTakeaway>Logs preserve every field in every event — which is their superpower and their cost. Ingestion volume (GB/day per service) is the bill driver. Level discipline (DEBUG off, INFO sampled, WARN/ERROR 100%) and tiered retention are the standard controls. The trace_id in every log line is the join key that makes three-signal pivots possible.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-logs-and-volume-retrieval"
  lang="en"
  questions={[
    { q: "What are the mandatory fields in a production-quality structured log line, and why is trace_id particularly important?", a: "Mandatory: timestamp (ISO-8601 UTC), level, service.name, trace_id, span_id, message, plus event-specific fields. trace_id is the join key that lets you navigate from a log line directly to the corresponding distributed trace — click from a log grep result to the full span waterfall without manually correlating timestamps across services." },
    { q: "A service produces 86 GB/day of logs. Name three controls to reduce this without losing actionable data.", a: "1. Level discipline: turn DEBUG off in production, sample INFO at 1% for high-volume success paths, keep 100% of WARN and ERROR. 2. Sampling at the ingest pipeline: route high-volume/low-value events to a cheaper scan tier (Datadog Flex Logs, Splunk tiered index). 3. Fix chatty antipatterns: emit one summary event per logical operation instead of one line per iteration or retry attempt." },
    { q: "Why is structured (JSON) logging required for filter-aggregate queries at scale, while plaintext is not?", a: "Structured logging means every event has consistent key names; the backend can index specific fields and answer 'show me all events where user_id=42 and status=error' without parsing every byte of free text. Plaintext grep requires full-text scan, which scales linearly with ingested volume and cannot use field-level indexes, making aggregations like 'how many 5xx errors per route' impractical at TB scale." },
  ]}
/>

<Recap lang="en">
A structured log is a timestamped JSON event with consistent keys; it is the only form that supports filter-aggregate queries at production scale. The backend pipeline is append-only ingest with indexed search, storing every field you write. Unlike metrics, logs have no cardinality explosion from high-cardinality fields — but they pay in ingestion bytes: 86 GB/day per service is typical at 1 000 req/s. Level discipline (DEBUG off, INFO sampled, WARN/ERROR 100%) and tiered retention control the bill. The mandatory `trace_id` field is the join key that connects log lines to traces, enabling single-click pivots from a log search to a full span waterfall in the tracing backend.
</Recap>
