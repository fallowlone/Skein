---
concepts:
- metrics
- time-series
- counter
- gauge
- histogram
- cardinality
- label
- prometheus
- exemplar
deepensInto: []
estMin: 14
lang: en
lessonType: topic
level: middle
order: 2
prereqs:
- observability/01-three-pillars/01-what-the-three-signals-are
slug: 02-metrics-and-cardinality
sources:
- https://prometheus.io/docs/practices/instrumentation/
- https://grafana.com/blog/how-to-manage-high-cardinality-metrics-in-prometheus-and-kubernetes/
- https://opentelemetry.io/docs/concepts/signals/
spiral:
- latency
status: ready
summary: How Prometheus-model metrics work, why cardinality is the central cost driver,
  and what happens when a single label explodes series count.
title: 'Metrics and cardinality: the cost model of a time-series database'
track: observability
unit: 01-three-pillars
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";

<Hook>
A team adds a `customer_email` label to their `request_total` counter to track per-customer error rates. Within two hours the Prometheus server runs out of memory and 15 minutes of alerts go missing. The label looked harmless.
</Hook>

<Crux>Cardinality — unique label combinations — is the dominant metrics cost driver. One unbounded label multiplies series count by millions.</Crux>

<Explanation>

## How a metric is stored

The canonical model is Prometheus. A metric is identified by a `(metric_name, label_set)` pair, where the label set is a small fixed collection of `key=value` pairs. Every unique combination of label values creates one **time series** — a stream of `(timestamp, value)` samples, typically scraped every 15 s.

Four standard metric types:

- **Counter** — monotonically increasing integer. Only a rate-of-change (per second) is meaningful. Example: `http_requests_total`.
- **Gauge** — instantaneous reading that can go up or down. Example: `memory_used_bytes`, `queue_depth`.
- **Histogram** — distributes observations across pre-defined buckets for percentile queries. Example: `http_request_duration_seconds` with buckets `[0.01, 0.05, 0.1, 0.5, 1.0, 5.0]`.
- **Summary** — pre-computed per-replica percentile. Rarely used for new instrumentation because it cannot be aggregated across replicas.

The compute model is **pre-aggregation at the source**: the application increments counters in-process, the scraper ships them to the TSDB, and the TSDB stores deltas and serves queries by walking pre-rolled-up buckets. This is why metric queries return in tens of milliseconds across years of history: the heavy lifting happened at write time, not read time.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Type</th>
        <th class="border border-border px-3 py-2 text-left">Goes up/down</th>
        <th class="border border-border px-3 py-2 text-left">Use for</th>
        <th class="border border-border px-3 py-2 text-left">Example</th>
      </tr>
    </thead>
    <tbody>
      <tr><td class="border border-border px-3 py-2">Counter</td><td class="border border-border px-3 py-2">Up only</td><td class="border border-border px-3 py-2">Rate of events</td><td class="border border-border px-3 py-2"><code>http_requests_total</code></td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">Gauge</td><td class="border border-border px-3 py-2">Both</td><td class="border border-border px-3 py-2">Current state</td><td class="border border-border px-3 py-2"><code>memory_used_bytes</code></td></tr>
      <tr><td class="border border-border px-3 py-2">Histogram</td><td class="border border-border px-3 py-2">Up only (buckets)</td><td class="border border-border px-3 py-2">Latency distribution, percentiles</td><td class="border border-border px-3 py-2"><code>http_request_duration_seconds</code></td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">Summary</td><td class="border border-border px-3 py-2">Pre-computed</td><td class="border border-border px-3 py-2">Percentile at source (not aggregatable)</td><td class="border border-border px-3 py-2">Legacy; prefer histogram</td></tr>
    </tbody>
  </table>
</div>

## Cardinality: the central cost driver

Each unique combination of label values creates a separate time series in the TSDB. The math is multiplicative:

- `route` (20 templates) × `method` (5 verbs) × `status_class` (4 classes) = **400 series** — fine.
- Same metric + `user_id` (100 k active users) = 100 k × 20 × 5 × 4 = **40 million series** — Prometheus OOM on 16 GB.

Prometheus stores roughly **3 KB per active series** in the head block. At 40 M series that is 120 GB, far beyond any commodity machine's memory.

Both Grafana's published guidance and the Prometheus instrumentation documentation say the same thing: **label only with values from a small fixed set** — route templates not full URLs, status classes not status codes, customer segments not customer IDs. Anything with unbounded values belongs in logs (free cardinality) or traces (sampled), not metrics.

## The exemplar bridge

When a histogram records a slow observation, the client can attach one **exemplar** — a `trace_id` from the request that produced the observation. Grafana renders exemplars as dots on the histogram heatmap; clicking a dot opens the corresponding trace.

Exemplars (standardised in Prometheus 2.32, early 2022) solve the gap that defined the pre-exemplar era: "metrics say something is slow, but I have no example request to drill into." They make it possible to keep `user_id` out of metric labels — cardinality stays bounded — while still being able to navigate from an aggregate spike to one concrete request via the trace.

<Misconception id="mc-metrics-user-id">
  <p><strong>"Add user_id as a label to track per-customer error rates."</strong> No. 100k users × label dimensions = millions of series; Prometheus OOM in hours. Per-customer data belongs in logs or traces. Use bounded labels like <code>customer_segment</code> (3 values) and exemplars to pivot to a specific request.</p>
</Misconception>

<NumbersCard
  id="02-metrics-cardinality-numbers"
  title="Prometheus cardinality and cost numbers"
  stats={[
    { label: "Prometheus bytes per active series (head block)", value: "~3 KB" },
    { label: "Safe-zone series count on 16 GB box", value: "~5 M" },
    { label: "Cardinality multiplier per high-card label", value: "100x to 10 000x" },
    { label: "Typical scrape interval", value: "15 s" },
    { label: "Series created by route × method × status_class (20×5×4)", value: "400" },
    { label: "Additional series if user_id (100 k) is added", value: "40 M" },
  ]}
/>

<Quiz
  id="02-metrics-and-cardinality-quiz-1"
  lessonSlug="02-metrics-and-cardinality"
  lang="en"
  question="A team adds customer_email to their request_total counter. The Prometheus server runs out of memory within hours. Why?"
  choices={[
    { label: "Counters use too much CPU to increment", misconception: "Counter increments are essentially free — in-process atomic increments." },
    { label: "Each unique customer_email creates a separate time series; with 100k+ active customers, the series count grows by orders of magnitude and exhausts head-block memory", correct: true },
    { label: "Prometheus does not support strings as labels", misconception: "Strings are fully supported as label values; the issue is unbounded cardinality, not type." },
    { label: "The scrape interval must be increased", misconception: "Scrape interval affects data resolution; it does not reduce series-count memory consumption." },
  ]}
/>

<Quiz
  id="02-metrics-and-cardinality-quiz-2"
  lessonSlug="02-metrics-and-cardinality"
  lang="en"
  question="What does an exemplar on a histogram metric do?"
  choices={[
    { label: "An example bucket boundary", misconception: "Histogram buckets are defined by the metric configuration, not by exemplars." },
    { label: "A trace_id attached to one sampled observation in a bucket, so a metric spike can be clicked through to one concrete slow request", correct: true },
    { label: "The most common observed value in the bucket", misconception: "Exemplars are not statistical modes; they are linked references to individual trace records." },
    { label: "A canonical example PromQL query for the dashboard", misconception: "Exemplars are runtime data attached to metric samples, not query templates." },
  ]}
/>

<Quiz
  id="02-metrics-and-cardinality-quiz-3"
  lessonSlug="02-metrics-and-cardinality"
  lang="en"
  question="Which metric type should you use for 'checkout request duration, so you can query p99 across all replicas'?"
  choices={[
    { label: "Counter — it is the most common type", misconception: "A counter only counts events; you cannot compute a percentile from a counter." },
    { label: "Gauge — read the latency at scrape time", misconception: "A gauge records an instantaneous value; you cannot retroactively compute a distribution from it." },
    { label: "Histogram — distributes observations into buckets; histogram_quantile aggregates across replicas", correct: true },
    { label: "Summary — pre-computes the percentile at the source", misconception: "A summary computes percentiles per-replica and cannot be aggregated across replicas without losing accuracy." },
  ]}
/>

</Explanation>

<KeyTakeaway>Metrics are pre-aggregated time series: fast to query for years of history, but blind to any dimension not in the label set. Cardinality is the cost cliff — each unique label-value combination is a separate series, so unbounded labels like user_id destroy the TSDB. Exemplars bridge metrics to traces without paying the cardinality bill.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-metrics-and-cardinality-retrieval"
  lang="en"
  questions={[
    { q: "Explain in your own words why metrics are cheap to query but blind, while logs are powerful but expensive.", a: "Metrics are pre-aggregated at write time: the TSDB stores only running totals per (metric, label_set, timestamp), so read time is just walking pre-rolled buckets — fast across years. The cost is that you can only ask questions about dimensions you labelled before the question arose. Logs are the opposite: every field is preserved, so any question is answerable post-hoc, but the backend must scan or index every field, paying in bytes and query latency." },
    { q: "A metric has labels: route (30 templates), method (5), status_class (4), region (3). Is this safe? What would make it unsafe?", a: "30 × 5 × 4 × 3 = 1 800 series — safe. It becomes unsafe when you add an unbounded label like user_id (100k+), which multiplies the series count by 100 000, pushing the total into the hundreds of millions and exhausting TSDB memory." },
    { q: "Why use an exemplar instead of just adding user_id as a metric label?", a: "Adding user_id as a label multiplies series count by the number of active users, causing TSDB memory exhaustion. An exemplar instead attaches a sampled trace_id to a histogram bucket observation — so you can click from a metric spike to one concrete request's trace without ever paying the cardinality cost of labelling every request with its user." },
  ]}
/>

<Recap lang="en">
A Prometheus metric is identified by a (name, label_set) pair; each unique combination of label values creates one time series stored at roughly 3 KB in the head block. Pre-aggregation at the source is what makes queries fast across years, but it means metrics can only answer questions about dimensions you labelled in advance. Cardinality — the product of all label-value counts — is the dominant cost driver: a single unbounded label like customer_email on a busy metric can multiply series count from thousands to tens of millions, OOM-ing the server. The four metric types are counter (rate-of-change), gauge (instantaneous), histogram (percentile-queryable distribution), and summary (pre-computed, non-aggregatable). Exemplars bridge the aggregate metric to a concrete trace, eliminating the need to label with high-cardinality identifiers.
</Recap>
