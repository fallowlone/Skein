---
concepts:
- profile-security
- otel-profile-signal
- continuous-profiling-design
- differential-profile-ci
- profiling-war-stories
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: senior
order: 7
prereqs:
- observability/07-profiling/06-advanced-profiling
slug: 07-profiling-in-production
sources:
- https://opentelemetry.io/docs/specs/otel/profiles/
- https://docs.kernel.org/admin-guide/perf-security.html
- https://github.com/grafana/pyroscope
- https://github.com/parca-dev/parca
spiral:
- latency
status: ready
summary: Profiles expose function names and call patterns — treat them like debugger
  output. Five war stories show what continuous profiling catches and what dashboards
  miss. OTel profile signal is the fourth pillar closing the SLO → trace → profile
  loop.
title: 'Profiling in production: security, war stories, OTel profiles, and the infrastructure
  design'
track: observability
unit: 07-profiling
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Stripe's continuous profiler caught a regression two days after a deploy that no dashboard showed. A new feature flag was reading from disk on every request instead of from in-memory cache. CPU profile looked normal; off-CPU profile showed the disk wait. The fix was one line. The detection would have taken weeks without continuous profiling.
</Hook>

<Crux>Profiles expose code internals — treat as security-sensitive. Continuous profiling is the fourth pillar: SLO → trace → profile.</Crux>

<Explanation>

## Profiles are security-sensitive artefacts

A profile contains function names (often private), call patterns, and sometimes allocation arguments — enough to reverse-engineer business logic. Some profilers capture argument values at allocation sites; poorly configured allocation profilers have leaked credentials.

<Misconception id="mc-symbols-private">
  <p><strong>"Profiles are operational artefacts; safe to share widely."</strong> No. A profile contains function names (often private), call patterns, and sometimes allocation arguments — enough to reverse-engineer business logic. Treat profiles as security-sensitive; restrict access, retention, and external sharing.</p>
</Misconception>

In hostile contexts, a profile from a competitor's binary can reveal proprietary algorithms — function names alone often telegraph what a service does. eBPF profilers running on shared kernels can in principle observe other tenants' execution; this is why eBPF requires explicit capabilities and is namespace-scoped on modern kernels.

**Production discipline:**
- Profiles are RBAC-gated by team (Pyroscope tenancy model).
- Retention limited to 30-90 days; exports require approval.
- Never shipped outside the organisation.
- eBPF agent runs with `CAP_PERFMON` only, not full root.
- Audit log of who pulled which profile.

Together these controls treat a profile the same way you would treat a heap dump or a debugger session: useful internally, dangerous externally. Skip any one of them and you have created an unaudited window into your codebase's internals.

## Production war stories

**Discord 2020:** a chat service ran at 80% CPU with mysterious tail latency. CPU profile pointed at JSON serialisation. Switching to a faster JSON library dropped CPU to 30% and tail latency to baseline.

**GitHub 2021:** Ruby workers were OOMing on certain endpoints. Allocation profile showed a single template-rendering function allocating 200 MB per request because of an unbounded loop concatenating strings.

**Stripe 2022:** continuous profiling caught a regression two days after deploy. A new feature flag read from disk on every request instead of from in-memory cache. CPU profile looked normal; off-CPU profile showed the disk wait. Fix was one line.

**Cloudflare 2023:** a Worker runtime regression appeared in eBPF profiles as time spent in V8's GC. The team rolled back a V8 update that introduced more aggressive collection.

**Slack 2024:** PHP service was spending 30% of CPU on autoloader. Profiler-guided opcache tuning cut it to 5%.

**The shared pattern:** every major engineering org has a profiling war story. The common thread: dashboards showed normal, but the profile showed the bottleneck. The fix was obvious from the flame graph; impossible to find without one.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Company / Year</th>
        <th class="border border-border px-3 py-2 text-left">Symptom</th>
        <th class="border border-border px-3 py-2 text-left">Profile type</th>
        <th class="border border-border px-3 py-2 text-left">Root cause</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Discord 2020</td>
        <td class="border border-border px-3 py-2">80% CPU, tail latency</td>
        <td class="border border-border px-3 py-2">CPU flame graph</td>
        <td class="border border-border px-3 py-2">JSON serialisation hotspot</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">GitHub 2021</td>
        <td class="border border-border px-3 py-2">OOM on endpoints</td>
        <td class="border border-border px-3 py-2">Allocation profile</td>
        <td class="border border-border px-3 py-2">String concat loop, 200 MB/req</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Stripe 2022</td>
        <td class="border border-border px-3 py-2">Post-deploy regression</td>
        <td class="border border-border px-3 py-2">Off-CPU profile</td>
        <td class="border border-border px-3 py-2">Feature flag disk read on every req</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Cloudflare 2023</td>
        <td class="border border-border px-3 py-2">Worker runtime regression</td>
        <td class="border border-border px-3 py-2">eBPF CPU profile</td>
        <td class="border border-border px-3 py-2">V8 GC update, more aggressive collection</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Slack 2024</td>
        <td class="border border-border px-3 py-2">High PHP CPU</td>
        <td class="border border-border px-3 py-2">CPU flame graph</td>
        <td class="border border-border px-3 py-2">Autoloader: 30% CPU, fixed with opcache</td>
      </tr>
    </tbody>
  </table>
</div>

## OTel profile signal: the fourth pillar

OpenTelemetry is standardising profiles as a fourth signal (after logs, metrics, traces). The spec defines:
- A profile data model: samples with stacks, labels, and time ranges.
- A transport: OTLP profile signal (added in 2024).
- Integration with context propagation: trace-id tagging on every sample.

**Adoption status**: Datadog, Grafana, Honeycomb, Splunk are implementing OTel profile ingestion. Agents (OTel Collector + profiler side) emit OTel-formatted profiles. The OTel profile spec is in beta as of 2026 — most production deployments still use vendor-specific formats (pprof, JFR, Pyroscope-native). Choosing a tool today commits to a format for 2-3 years; the OTel trajectory is worth tracking.

The promise: cross-vendor portability and a unified collector pipeline — the same architecture as logs, metrics, and traces. The catch: the spec is young and implementations diverge at the edges.

## Designing continuous profiling infrastructure

A 200-service polyglot platform (Go, Java, Node, Python) with the requirement to surface deploy regressions in 1 hour and enable trace-to-profile drill in under 30 seconds:

**Layer 1 — Collection:** eBPF DaemonSet on every node (Parca-style or Pyroscope eBPF) as the universal baseline — covers all languages, one agent per node. Per-language agents as supplements: pprof for Go, async-profiler for Java, py-spy for Python. The eBPF agent is the catchall; per-language agents provide allocation and mutex profiles.

**Layer 2 — Backend:** self-hosted Pyroscope 2.0 cluster. Object storage (S3 / GCS) with 30-day fine-grained retention and 90-day downsampled. Symbol deduplication keeps per-service storage under 10 GB/month.

**Layer 3 — Trace correlation:** profiles carry trace-id and span-id labels. Grafana links trace span → Pyroscope filtered by trace-id. Sub-30-second drill.

**Layer 4 — Regression detection:** CI job on every deploy: capture 5-minute profile of new version under canary traffic, diff against previous version's profile, post flame-graph diff as PR comment, fail CI if a new function appears in top 5 by self-CPU. Hourly production diff against same-hour-yesterday baseline; Slack alert on shape changes.

**Layer 5 — Cost controls:** sample rate per service configurable in service.yaml (default 99 Hz; drop to 19 Hz for cheap baseline services). Budget alert at 80% of monthly cost ceiling.

<NumbersCard
  id="card-production-design"
  title="Profiling infrastructure: design targets"
  stats={[
    { label: "Trace-to-profile drill time", value: "&lt;30 seconds" },
    { label: "Deploy regression detection window", value: "&lt;1 hour" },
    { label: "Pager-to-git-blame MTTR", value: "&lt;90 seconds" },
    { label: "Storage per service per month", value: "&lt;10 GB (Pyroscope 2.0)" },
    { label: "eBPF capability required", value: "CAP_PERFMON only" },
    { label: "Profile RBAC", value: "Per-team tenancy" },
  ]}
/>

<Quiz
  id="07-profiling-in-production-quiz-1"
  lessonSlug="07-profiling-in-production"
  lang="en"
  question="A profile from your service leaks to a vendor's support team. What is the security concern?"
  choices={[
    { label: "Function names expose internal code structure, undocumented APIs, and call patterns — enough to reverse-engineer business logic or identify exploitable code paths", correct: true },
    { label: "Profile files are large and will fill the vendor's inbox", misconception: "File size is not the concern; information exposure is." },
    { label: "No concern — profiles are operational data, not secrets", misconception: "This is exactly the misconception. Profiles contain function names that reveal implementation details." },
    { label: "Only allocation profiles are sensitive; CPU profiles are safe", misconception: "CPU profiles expose function names and call patterns. Allocation profiles additionally may expose argument values." },
  ]}
/>

<Quiz
  id="07-profiling-in-production-quiz-2"
  lessonSlug="07-profiling-in-production"
  lang="en"
  question="The OTel profile signal is in beta as of 2026. What is the practical implication for teams choosing a profiling tool today?"
  choices={[
    { label: "The tool's format (pprof, JFR, Pyroscope-native) commits the team to a backend for 2-3 years; worth choosing a tool with OTel export support to ease future migration", correct: true },
    { label: "Wait for OTel profiles to stabilise before profiling at all", misconception: "Production teams cannot wait years for a spec to stabilise. Use existing tools and plan for OTel migration." },
    { label: "OTel profile signal replaces all existing profilers immediately", misconception: "OTel defines the wire format; existing profilers add OTel export as an option, not replacement." },
    { label: "No practical implication; format does not matter", misconception: "Format affects which backends can ingest data and whether switching backends requires data re-export or re-capture." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Safe production profiling pipeline"
  perRow={4}
  hue="var(--d-systems)"
  nodes={[
    { id: "prod", label: "Prod processes", sub: "200 services" },
    { id: "sampler", label: "Sampler", sub: "100 Hz, <1% cap" },
    { id: "store", label: "Tagged store", sub: "version + commit" },
    { id: "compare", label: "Compare", sub: "across deploys" },
  ]}
  edges={[
    { from: "prod", to: "sampler", label: "low-freq sample" },
    { from: "sampler", to: "store", label: "overhead budget" },
    { from: "store", to: "compare", label: "diff baseline" },
  ]}
  caption="Low-frequency sampling (≈100 Hz) under a per-host overhead budget keeps cost below 1%; each profile is tagged with version and commit so regressions are attributable when compared across deploys."
/>

<KeyTakeaway>Profiles are security-sensitive — function names reveal code internals, treat them like debugger output with RBAC and retention limits. Five industry war stories show the same pattern: dashboards hide what profiles reveal. The OTel profile signal makes profiling the fourth observability pillar; choosing a tool today means choosing a format for 2-3 years.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-profiling-in-production-retrieval"
  lang="en"
  questions={[
    { q: "Why are profiles treated as security-sensitive artefacts rather than just operational data?", a: "A profile contains function names — often including private APIs, internal libraries, and undocumented code paths. Function names alone can telegraph proprietary algorithms: 'hashCreditCardToken', 'fraudModelV3Score', 'secretKeyDerive' expose what a service does without access to source code. Some profilers capture allocation arguments (string contents at malloc sites), which can include credentials if the service handles them. eBPF profilers on shared kernels in principle observe other tenants' execution patterns. The Linux kernel's perf security documentation (linked in the sources) explicitly models profilers as equivalent to debuggers from a threat perspective. Production discipline: RBAC per team, audit log for access, 30-90 day retention limits, no external sharing without approval." },
    { q: "Design the profiling CI gate for a 50-service platform to catch CPU regressions at deploy time.", a: "Each deploy triggers a CI job that: (1) deploys the new version to a canary pool (5-10% traffic or synthetic load); (2) lets the continuous profiler collect 5 minutes of profiles from both the canary and the current stable version under comparable load; (3) runs a differential flame graph diff between them; (4) posts the diff as a PR comment with the top-5 changed functions; (5) fails the CI job if any of these conditions are true: a new function appears in the top 5 by self-CPU that was not in the previous version's top 20; any existing top-5 function grew by more than 15%; total CPU usage under the same load increased by more than 10%. The thresholds are tunable per service (latency-critical services have tighter bounds). This catches regressions before any SLO alert fires because it tests under controlled comparable load rather than waiting for production traffic shape to reveal the problem. Teams that skip this gate typically detect regressions from customer reports or SLO burns, averaging 2-7 days after deploy." },
    { q: "What is the OTel profile signal and what does it standardise?", a: "The OpenTelemetry profile signal is a specification for profiles as a fourth observability pillar, complementing logs, metrics, and traces. It defines: a profile data model (samples with stack frames, labels including trace-id and span-id, time ranges, value types); a wire format (OTLP extended for profiles); and integration with OTel's context propagation so trace-id is a first-class label on every sample. The spec reached beta in 2024-2025. Adoption: Datadog, Grafana, Honeycomb, Splunk are adding OTel profile ingestion. The practical consequence: if a team's profiler emits OTel-format profiles, they can switch tracing/monitoring backends without recapturing or re-instrumenting. The catch: the spec is still evolving and most production deployments use vendor-specific formats (pprof, JFR, Pyroscope-native). Choosing OTel-capable tooling today reduces migration friction in 2-3 years when the spec stabilises." },
  ]}
/>

<Recap lang="en">
Profiles contain function names, call patterns, and sometimes allocation argument values — treat them as security-sensitive artefacts with RBAC, audit logs, and retention limits, never shared externally without approval. Five industry war stories (Discord, GitHub, Stripe, Cloudflare, Slack) follow the same pattern: dashboards showed normal, the profile showed the bottleneck, the fix was obvious from the flame graph. The OTel profile signal standardises profiles as the fourth observability pillar with a data model, OTLP transport, and trace-id integration; it is in beta as of 2026 but worth tracking when choosing tooling. Production profiling infrastructure for a 200-service polyglot fleet combines an eBPF DaemonSet (universal baseline), per-language native agents (depth), Pyroscope 2.0 self-hosted (storage), trace-id correlation (30-second drill), and CI differential profiles (1-hour regression detection). The cultural shift: senior on-call engineers in 2026 open the profile dashboard the same reflexive way they opened traces two years ago.
</Recap>
