---
concepts:
- profiling
- flame-graph
- call-stack
- sampling-profiler
- cpu-time
deepensInto: []
estMin: 12
lang: en
lessonType: topic
level: junior
order: 1
prereqs:
- observability/06-trace-propagation/07-production-failures-and-design
slug: 01-flame-graph-basics
sources:
- https://www.brendangregg.com/flamegraphs.html
- https://queue.acm.org/detail.cfm?id=2927301
- https://pkg.go.dev/runtime/pprof
spiral:
- latency
status: ready
summary: A profiler samples your call stack 100 times per second and draws a flame
  graph — the widest frame at any level is the function eating the most CPU, discoverable
  in one glance.
title: 'Flame graphs: reading the picture that shows where time goes'
track: observability
unit: 07-profiling
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import samplingPipeline from "./infographics/sampling-pipeline.svg?raw";

<Hook>
A trace says "1.2 seconds in inventory." You have logs, metrics, dashboards — but none of them tell you which function inside inventory ate the time. Profiling answers that question in 60 seconds without a debugger or a guess. In the next ten minutes you will know exactly how to read the picture that shows where your time goes.
</Hook>

<Crux>Profiling = sampled stacks. Wide frame = hot function. Find the bottleneck in one glance, not a grep session.</Crux>

<Explanation>

## What a profiler does

A profiler interrupts your program 100 times per second and captures the current **call stack** — the chain of functions from `main` down to whatever is running right now. After 30 seconds it has 3,000 snapshots. The function that appears most often across those snapshots is the one consuming the most CPU.

<Infographic
  svg={samplingPipeline}
  label="Five-stage pipeline turning periodic interrupts into a hot-path verdict: interrupt the program 100 times per second, snapshot the call stack each time, accumulate about 3,000 stacks over 30 seconds, rank functions by how often they appear, then draw the flame graph where the widest frame is the hot path."
  caption="Sampling is statistical, not exhaustive: ~3,000 snapshots in 30s are enough that the most-sampled function is provably the CPU hog — which is why a 2-5% profiler can run always-on."
  hue="var(--d-infra)"
/>

**Flame graphs** visualise this: stacks are sorted alphabetically along the x-axis, width is proportional to sample count, depth on the y-axis goes from the entry point at the bottom to the leaf function at the top. The widest frame at any level is the busiest path.

<DiagramFrame
  label="A flame graph: callers stacked beneath callees, width = sampled CPU time"
  hue="var(--d-systems)"
  caption="Each row is one call-stack level (main at the bottom, the on-CPU leaf at the top). A frame's width is the share of samples spent in it — never less than the sum of its children. json.Marshal is the widest leaf, so it is the hot path; gc is a thin sliver."
>
  <svg viewBox="0 0 600 200" width="600" height="200" role="img" aria-label="Flame graph with main at the base widening up to json.Marshal as the hottest leaf frame" style="max-width:100%;height:auto;font-family:var(--font-mono, monospace);">

    <rect x="20" y="156" width="560" height="28" rx="3" fill="#e25822" />
    <text x="300" y="174" text-anchor="middle" font-size="13" fill="#ffffff">main  (100%)</text>


    <rect x="20" y="120" width="480" height="28" rx="3" fill="#ef7d3a" />
    <text x="260" y="138" text-anchor="middle" font-size="13" fill="#ffffff">handleRequest  (86%)</text>
    <rect x="510" y="120" width="70" height="28" rx="3" fill="#c8c8c8" />
    <text x="545" y="138" text-anchor="middle" font-size="11" fill="#333333">gc</text>


    <rect x="20" y="84" width="440" height="28" rx="3" fill="#f59b4b" />
    <text x="240" y="102" text-anchor="middle" font-size="13" fill="#333333">serializeResponse  (79%)</text>
    <rect x="466" y="84" width="34" height="28" rx="3" fill="#c8c8c8" />
    <text x="483" y="102" text-anchor="middle" font-size="10" fill="#333333">db</text>


    <rect x="20" y="48" width="420" height="28" rx="3" fill="#ffc234" stroke="#e25822" stroke-width="2" />
    <text x="230" y="66" text-anchor="middle" font-size="13" fill="#333333">json.Marshal  (75%)  ← hot leaf</text>


    <text x="588" y="170" text-anchor="end" font-size="10" fill="#888888">depth ↑</text>
    <line x1="20" y1="192" x2="580" y2="192" stroke="#bbbbbb" stroke-width="1" />
    <text x="300" y="200" text-anchor="middle" font-size="10" fill="#888888">width = sampled CPU time (not wall-clock order)</text>
  </svg>
</DiagramFrame>

## The stadium metaphor

Imagine a stadium with 100,000 people doing different things. A helicopter flies overhead 100 times per second and photographs who is doing what. After a minute you have 6,000 photos. Count which activity appears most across the photos — that is where the crowd's "CPU time" goes.

The flame graph is the bar chart of those counts, with callers stacked beneath callees. Wide bars = popular activities. The helicopter is the profiler; the photos are stack samples; the chart is the flame graph.

## Reading a flame graph in practice

Bea is on-call. Inventory service, p99 = 1.5 s. She opens the continuous-profile dashboard, filters by trace-id, and sees a flame graph with one massive 1.1-second-wide block: `json.Marshal` inside `serializeResponse`. The fix is obvious: cache the marshalled response or pre-encode at write-time. Without profiling, the team would have guessed — DB? Cache? Network? With the flame graph there is no guessing.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Axis</th>
        <th class="border border-border px-3 py-2 text-left">Meaning</th>
        <th class="border border-border px-3 py-2 text-left">Common misreading</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Width (x)</td>
        <td class="border border-border px-3 py-2">Sample count — CPU time share</td>
        <td class="border border-border px-3 py-2">People read left-to-right as "time order" — it is NOT</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Position (x)</td>
        <td class="border border-border px-3 py-2">Alphabetical grouping by parent</td>
        <td class="border border-border px-3 py-2">Left frame does NOT run before right frame</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Height (y)</td>
        <td class="border border-border px-3 py-2">Call depth — main at bottom, leaf at top</td>
        <td class="border border-border px-3 py-2">Taller stack = deeper nesting, not slower</td>
      </tr>
    </tbody>
  </table>
</div>

## How to capture a CPU profile with pprof

A Node API has a p99 jump. Tracing finds a slow span. The continuous-profile dashboard, filtered by trace-id, shows a flame graph dominated by a regex compile in a handler. A library upgrade introduced an O(n²) regex; fix is to precompile it outside the handler. Detection: 60 seconds.

For Go services, pprof is built-in:

```go
// 1. Expose pprof handlers (registers /debug/pprof/* routes)
import _ "net/http/pprof"

// Start the debug server
go func() {
  http.ListenAndServe("localhost:6060", nil)
}()

// 2. Capture a 30-second CPU profile under load:
//    go tool pprof -http=:9090 \
//      http://localhost:6060/debug/pprof/profile?seconds=30
//
// 3. The flame graph view opens at :9090.
//    Widest top-level leaf = hot path.
```

You must run the profile under **representative load** — on an idle system, almost everything in the samples is the runtime's idle loop, useless for finding hot paths.

<Quiz
  id="01-flame-graph-basics-quiz-1"
  lessonSlug="01-flame-graph-basics"
  lang="en"
  question="What does the WIDTH of a frame on a flame graph represent?"
  choices={[
    { label: "How often that function appeared on the call stack in the sampled snapshots — wider means more CPU time", correct: true },
    { label: "How long the function took to compile", misconception: "Compile time is unrelated to flame graph width." },
    { label: "Number of lines in the function", misconception: "Code size is irrelevant; width is sample frequency." },
    { label: "Wall-clock time on the x-axis", misconception: "The x-axis is alphabetical sort of stacks, not time. Width is sample count." },
  ]}
/>

<Quiz
  id="01-flame-graph-basics-quiz-2"
  lessonSlug="01-flame-graph-basics"
  lang="en"
  question="A continuous profiler runs in production at 2-5% CPU overhead. Why doesn't it ruin performance?"
  choices={[
    { label: "It samples — not every call is recorded; only ~100 stack snapshots per second. The cost is bounded by the sample rate.", correct: true },
    { label: "It pauses the program while running", misconception: "It does not pause; it samples without stopping execution." },
    { label: "It only runs at night", misconception: "Continuous profiling runs continuously by definition." },
    { label: "It only profiles errors", misconception: "Profilers capture all CPU activity, not error paths only." },
  ]}
/>

<DragOrder
  id="01-flame-graph-basics-drag-1"
  lessonSlug="01-flame-graph-basics"
  lang="en"
  prompt="Order the steps of CPU profiling a slow function with pprof:"
  items={[
    { id: "a", label: "Identify the suspicious workload (slow span, high CPU, slow endpoint)" },
    { id: "b", label: "Start profiling (pprof.StartCPUProfile or /debug/pprof/profile endpoint)" },
    { id: "c", label: "Run the suspicious workload for 30 seconds under load" },
    { id: "d", label: "Stop profiling and save the profile file" },
    { id: "e", label: "Open the profile in a flame graph viewer (go tool pprof, speedscope, Pyroscope)" },
    { id: "f", label: "Find the widest frame at the leaf level — that is the hot function" },
    { id: "g", label: "Walk up the parents to see who is calling the hot path, then apply the fix" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g"]}
/>

<MetaphorComplete
  id="01-flame-graph-basics-metaphor-1"
  pieceSlug="01-flame-graph-basics"
  lang="en"
  setup="Fill in the blank: a flame graph's vertical axis shows the call _______ — main is at the bottom, the function on the CPU is at the top."
  accepted={["stack", "depth", "stack depth"]}
  canonical="stack (also called call depth)"
  explanation="The y-axis is the call stack: the function at the bottom called the one above it, which called the one above that, all the way up. The top of any column is what was on CPU at sample time."
/>

</Explanation>

<KeyTakeaway>A profiler samples the call stack ~100 times per second. The flame graph turns thousands of samples into one picture: widest frame = hottest function. Profile under representative load — idle captures the wrong thing.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-flame-graph-basics-retrieval"
  lang="en"
  questions={[
    { q: "In one paragraph: why is a flame graph almost always faster than a debugger or print statements for finding the slow part of a program?", a: "A debugger or print statements require you to guess where to look — set a breakpoint, watch a line, measure timing manually. With a 1000-line function and three nested call chains, the guess is wrong most of the time. A flame graph shows you the answer: the widest frame is the hottest path, no guessing. The profiler has already inspected every call across thousands of samples; you read the result in one glance. The only requirement is that the slow function actually runs on CPU often enough to be sampled — for very short or rare slow paths, profiling needs longer runs to gather data. For everything else (and that is almost everything in production), the flame graph is the fastest path from 'something is slow' to 'this function is slow.'" },
    { q: "What is the most common misreading of a flame graph and what does the x-axis actually mean?", a: "The most common misread is treating the x-axis as time order — assuming a frame to the left runs before one to the right. In reality the x-axis is alphabetical grouping: all children of the same parent are sorted lexicographically and placed next to each other. Width is sample count (CPU time share), not wall-clock position. A frame appearing to the right of another tells you nothing about call order — only about the lexicographic order of their full stack strings. If you need time order, use a trace view (Gantt-style spans), not a flame graph." },
    { q: "Why must you run the profile under representative load, not on a quiet system?", a: "A sampling profiler only sees what is running. On an idle system, almost everything in the samples is the runtime's idle loop or the GC — useless for finding hot paths. Under representative load, the application's real hot paths dominate the samples and the flame graph points at the actual bottleneck. Production-grade profiling captures during real traffic; staging profiling captures under synthetic load that mimics production. Profiling without load is profiling the wrong thing." },
  ]}
/>

<Recap lang="en">
A profiler interrupts the program ~100 times per second, captures the call stack, and after many samples draws a flame graph where width equals CPU share. The widest frame at any level is the hottest code path — no guessing required. The x-axis is alphabetical grouping of stacks, not time; misreading it as time order is the single most common rookie mistake. You must profile under representative load; an idle system only shows the runtime's idle loop. With a continuous profiler always running at 2-5% overhead, the flame graph for any SLO-burning incident is already saved the moment the pager fires. Now when you see a wide frame dominating a flame graph, you know the exact question to ask: who calls it, and can its work be reduced or cached?
</Recap>
