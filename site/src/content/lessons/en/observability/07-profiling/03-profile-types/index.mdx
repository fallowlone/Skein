---
slug: 03-profile-types
lang: en
track: observability
unit: 07-profiling
order: 3
title: "Profile types: CPU, memory, off-CPU, mutex — which one to reach for"
summary: "CPU profiling only sees code that is running; off-CPU, block, and mutex profiles cover the 96% of a request that can be spent waiting — you need all four to diagnose any slow service."
estMin: 15
status: ready
lessonType: topic
level: middle
prereqs: ["observability/07-profiling/02-sampling-vs-instrumenting"]
deepensInto: []
spiral: ["latency"]
concepts: ["cpu-profile", "heap-profile", "off-cpu-profile", "block-profile", "mutex-profile", "allocation-profile"]
sources:
  - https://pkg.go.dev/runtime/pprof
  - https://www.brendangregg.com/offcpuanalysis.html
  - https://github.com/grafana/pyroscope
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Misconception from "~/components/layout/Misconception.astro";

<Hook>
A span takes 500 ms. You open the CPU profile — the service used 20 ms of CPU. Where did the other 480 ms go? CPU profiling is blind to waiting. If you stop there, you will never find the answer.
</Hook>

<Crux>CPU profile = only running code. Off-CPU + block + mutex profiles cover waiting time. Together they cover the full request lifecycle.</Crux>

<Explanation>

## CPU profiles: what they see and what they miss

CPU profiling samples the call stack while the thread is **on the CPU** — running instructions. A request that spends 20 ms computing and 480 ms waiting on a database query will show 20 ms in the CPU profile and leave 480 ms invisible.

This is the most important constraint in profiling: CPU profiles only see functions that are consuming the processor. Anything the program waits for — I/O, network, locks, scheduling — is off-CPU and invisible to a CPU profiler.

<Misconception id="mc-mean-hides">
  <p><strong>"CPU profiling shows everything that makes my request slow."</strong> No. CPU profiling only sees functions actually running. A request waiting 400 ms on Postgres uses zero CPU during the wait — invisible to the CPU profile. Use an off-CPU profile, block profile, or trace spans for the off-CPU portion.</p>
</Misconception>

## Memory and allocation profiles

Heap profilers sample allocations, not CPU. Go's heap profile samples one allocation per ~512 KiB (Poisson distributed) and records the stack at each sample. The result is a flame graph where width is **allocated bytes**, not CPU time. This finds memory hotspots: a function allocating 100 MB/s shows up wide.

Memory leak detection with heap profiles:
1. Take a heap profile.
2. Wait 30-60 minutes.
3. Take another heap profile.
4. Diff them (`go tool pprof -base baseline.heap current.heap`).
5. Functions whose allocation grew are leaking.

Allocation profiles capture short-lived allocations that GC reclaims; heap profiles snapshot live memory. Both are useful. JVM equivalents: async-profiler with `-e alloc`, JFR allocation events. Python: `tracemalloc`, `memray`.

## Off-CPU profiles

Brendan Gregg's 2013 work on off-CPU analysis identified the gap: CPU profiles miss everything a process waits for. eBPF implementations hook into the kernel scheduler's switch events. When the scheduler removes a thread from the CPU (it blocks on I/O, sleeps, waits on a lock), the kernel captures the thread's stack. That stack represents where the wait started. When the thread comes back, elapsed time is attributed to that stack.

The off-CPU flame graph shows **wait time** the same way a CPU flame graph shows running time. For an I/O-bound service, the off-CPU profile is the only profile that explains anything — the CPU profile just says "service was idle."

## Block and mutex profiles

**Block profile** (Go: `runtime.SetBlockProfileRate`): time spent waiting on synchronisation primitives — channels, condition variables, WaitGroups. More focused than off-CPU because it targets language-level synchronisation.

**Mutex profile** (`runtime.SetMutexProfileFraction`): lock contention specifically. Reports which code held a lock while others were waiting for it, attributed at unlock time.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Profile type</th>
        <th class="border border-border px-3 py-2 text-left">Width measures</th>
        <th class="border border-border px-3 py-2 text-left">When to reach for it</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">CPU</td>
        <td class="border border-border px-3 py-2">CPU time consumed</td>
        <td class="border border-border px-3 py-2">CPU usage is high, slow response</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Heap / Allocation</td>
        <td class="border border-border px-3 py-2">Bytes allocated</td>
        <td class="border border-border px-3 py-2">GC pressure, OOM, memory growth</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Off-CPU</td>
        <td class="border border-border px-3 py-2">Time spent waiting (all causes)</td>
        <td class="border border-border px-3 py-2">Slow request but low CPU usage</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Block</td>
        <td class="border border-border px-3 py-2">Time on sync primitives</td>
        <td class="border border-border px-3 py-2">Go goroutine contention suspected</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Mutex</td>
        <td class="border border-border px-3 py-2">Lock contention time</td>
        <td class="border border-border px-3 py-2">High lock contention suspected</td>
      </tr>
    </tbody>
  </table>
</div>

## Choosing the right profile from the CPU/wall-time ratio

The diagnostic shortcut: look at CPU time vs wall-clock time for the slow request.

- CPU/wall ≈ 100%: computation bottleneck — CPU profile.
- CPU/wall &lt; 30%: the bottleneck is off-CPU — off-CPU / block profile or trace spans.
- Memory growing steadily: heap / allocation profile.
- Threads contending on a lock: mutex profile.

A Java service GC-thrashing is a classic allocation-profile case. The symptom is high heap allocation rate with frequent old-gen GC. The allocation flame graph will show the widest frame as the function allocating at highest rate — often string concatenation in logging code that is not using parameterized formatting.

<Quiz
  id="03-profile-types-quiz-1"
  lessonSlug="03-profile-types"
  lang="en"
  question="A request spends 50 ms on CPU and 450 ms waiting on a DB query. Which profile type would show you the DB wait?"
  choices={[
    { label: "Off-CPU profile (or block profile, or a trace span — anything that captures waiting time)", correct: true },
    { label: "CPU profile", misconception: "CPU profile shows only running code; waiting is invisible to it." },
    { label: "Heap profile", misconception: "Heap shows allocations, not waiting time." },
    { label: "Mutex profile", misconception: "Mutex shows lock contention, not DB query wait." },
  ]}
/>

<Quiz
  id="03-profile-types-quiz-2"
  lessonSlug="03-profile-types"
  lang="en"
  question="A Java service is OOMing on certain endpoints. Its CPU profile looks normal. Which profile type to reach for?"
  choices={[
    { label: "Allocation profile (async-profiler -e alloc, or JFR allocation events) — find the stack that allocates the most bytes per second", correct: true },
    { label: "Off-CPU profile", misconception: "Off-CPU shows waiting time, not memory allocation patterns." },
    { label: "CPU profile with longer capture", misconception: "A normal CPU profile means the issue is not a compute hotspot — the OOM is almost certainly an allocation issue." },
    { label: "Mutex profile", misconception: "Mutex contention can degrade performance but is not the typical OOM cause." },
  ]}
/>

</Explanation>

<KeyTakeaway>CPU profiling only captures code that is running — waiting is invisible. For a request that is slow but uses little CPU, reach for off-CPU, block, or mutex profiles. For memory pressure and OOM, use a heap or allocation profile. The CPU/wall-time ratio tells you which type to grab first.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-profile-types-retrieval"
  lang="en"
  questions={[
    { q: "Why does Go's heap profiler sample one allocation per ~512 KiB instead of recording every allocation?", a: "Recording every allocation would cost CPU proportional to the allocation rate — for a service allocating 100 GB/s, that is 200 million stack walks per second, completely infeasible. Sampling at 1-per-512 KiB drops the cost by roughly 5 orders of magnitude while still gathering enough samples to identify allocation hotspots: a function allocating 100 MB/s will hit ~200 samples per second, plenty for statistical estimation. The Poisson distribution means each KiB has an independent probability of triggering a sample, so over time the sample-weighted aggregate is unbiased — multiplied back by the sample rate, the reported 'bytes allocated' reflects the true distribution." },
    { q: "Explain why a CPU flame graph is not enough to diagnose an I/O-bound service.", a: "CPU profiling captures only what is running on the CPU. An I/O-bound service spends most of its wall-clock time waiting — for disk reads, database responses, network packets, lock acquisitions. During those waits the thread is descheduled; it consumes zero CPU; the profiler never sees it. The flame graph for such a service shows ~5% CPU work and 95% of the request time as invisible. Three orthogonal profile types fill the gap: off-CPU profile (captures the stack at the moment a thread leaves CPU and attributes wait time to it), block profile (time on synchronisation primitives), and mutex profile (lock contention). The pattern: start with the trace to see where wall-clock time went, then CPU profile for compute, then off-CPU or block for waiting. Each answers a different question." },
    { q: "What is the procedure to detect a memory leak with heap profiles?", a: "Take a baseline heap profile, wait 30-60 minutes of production traffic, take another heap profile, diff them with go tool pprof -base baseline.heap current.heap. Functions whose cumulative allocation grew indicate where live objects are accumulating. The key is that heap profiles show live memory (objects the GC has not freed), not just total allocations — so growing live memory is genuinely a leak. Confirm by repeating the diff after another hour: if the same functions continue to grow, you have found the leak site. Apply the fix, capture another heap profile post-fix to verify the growth is gone." },
  ]}
/>

<Recap lang="en">
Four profile types cover the full request lifecycle: CPU (what is running), heap or allocation (what is being allocated), off-CPU (what is waiting on I/O or scheduling), and block or mutex (what is waiting on locks). CPU profiling only sees code that is actively on the processor — a request waiting 480 ms on a DB query will show only the 20 ms of compute in a CPU profile. The CPU/wall-time ratio is the diagnostic signal: under 30% means the bottleneck is off-CPU. Go's heap profiler samples at 1-per-512 KiB to make always-on memory profiling affordable. Combining all four types gives a complete picture; using only CPU profiling in an I/O-bound service guarantees you find the wrong bottleneck.
</Recap>
