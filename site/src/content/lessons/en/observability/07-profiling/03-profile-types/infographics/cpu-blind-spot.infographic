infographic chart-pie-donut-plain-text
data
  title A 500 ms request: what the CPU profile sees
  desc CPU profiling captures only on-CPU time — the waiting is invisible
  values
    - label On-CPU compute — visible to CPU profile (20 ms)
      value 20
    - label Off-CPU waiting on DB — invisible to CPU profile (480 ms)
      value 480
theme
  palette #6366f1 #f43f5e
