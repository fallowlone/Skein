infographic chart-pie-donut-plain-text
data
  title I/O-bound service: where the time actually goes
  desc A CPU profile sees only ~5%; off-CPU profiling reveals the 95% spent waiting
  values
    - label Off-CPU waiting (I/O, locks, sleep)
      value 95
    - label On-CPU (visible to a CPU profile)
      value 5
theme
  palette #6366f1 #f59e0b
