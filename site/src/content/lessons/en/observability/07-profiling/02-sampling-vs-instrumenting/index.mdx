---
concepts:
- sampling-profiler
- instrumentation-profiler
- sample-rate
- overhead
- pprof
- perf
deepensInto: []
estMin: 13
lang: en
lessonType: topic
level: middle
order: 2
prereqs:
- observability/07-profiling/01-flame-graph-basics
slug: 02-sampling-vs-instrumenting
sources:
- https://www.brendangregg.com/flamegraphs.html
- https://pkg.go.dev/runtime/pprof
- https://github.com/grafana/pyroscope
spiral:
- latency
status: ready
summary: Instrumentation wraps every function call for exact data but collapses under
  production load; sampling captures one stack every 10 ms at bounded overhead, making
  it the only viable production profiling strategy.
title: 'Sampling vs instrumentation profiling: why 99 Hz wins in production'
track: observability
unit: 07-profiling
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Your staging server runs under profiling with no problem. The same profiler in production raises latency by 40%. The profiler didn't change — the load did. Understanding why is the difference between a tool you can run always and one you can only use in emergencies.
</Hook>

<Crux>Instrumentation = exact data, prohibitive cost at scale. Sampling = bounded cost, statistical accuracy sufficient for production.</Crux>

<Explanation>

## Two ways to profile

**Instrumentation profiling** works by wrapping every function entry and exit with timing code. The runtime measures exactly how many times each function was called and exactly how long it ran. The cost is proportional to function call frequency — at 10 million calls per second, you pay 10 million timing measurements per second. On a dev machine with light load this is fine; under production traffic it can add 20-100% overhead. Instrumentation is great for small, isolated benchmarks; it is not viable for always-on production profiling.

**Sampling profiling** works differently: an external clock fires N times per second and captures the current call stack. The cost is exactly N stack walks per second regardless of how many function calls happen between them. At 100 Hz that is 100 stack walks per second — each costing ~5-20 microseconds — giving an overhead of roughly 0.5-2% on a busy CPU. Modern profilers (Go pprof, Linux perf, Java async-profiler, py-spy, eBPF-based profilers) are all sampling-based. The cost is bounded.

<FlowDiagram
  label="Sampling vs instrumentation: cost follows different inputs"
  perRow={2}
  hue="var(--d-systems)"
  nodes={[
    { id: "running", label: "Running program", sub: "fns called N×/s", col: 0, row: 0 },
    { id: "clock", label: "Sampling", sub: "clock @ 99 Hz", col: 0, row: 1 },
    { id: "instr", label: "Instrumenting", sub: "wrap entry/exit", col: 1, row: 1 },
    { id: "snapshot", label: "Stack snapshot", sub: "fixed rate", col: 0, row: 2 },
    { id: "count", label: "Every call timed", sub: "scales w/ calls", col: 1, row: 2 },
    { id: "stat", label: "Statistical", sub: "~0.5-2% over", col: 0, row: 3 },
    { id: "exact", label: "Exact counts", sub: "20-100% over", col: 1, row: 3 },
  ]}
  edges={[
    { from: "running", to: "clock", label: "sample" },
    { from: "running", to: "instr", label: "instrument" },
    { from: "clock", to: "snapshot" },
    { from: "instr", to: "count" },
    { from: "snapshot", to: "stat", label: "bounded cost" },
    { from: "count", to: "exact", label: "cost ∝ call rate" },
  ]}
  caption="Sampling's cost is fixed by sample rate (low, statistical); instrumentation's cost scales with call frequency (high, exact) — the same program, two opposite overhead profiles."
/>

## The key statistical property

At sample rate R Hz, a function that uses X% of CPU will appear in X% of samples — regardless of how many times it was called. The statistic reported is "fraction of CPU time," not "call count." This is exactly the right metric for finding bottlenecks: you want to know which function is on the CPU most often, not which function is called most often.

The consequence: sampling at 100 Hz is sufficient to find functions consuming more than a few percent of CPU. A function using 10% of CPU will hit ~10 samples per second. A 30-second profile gives 300 samples of that function — plenty for a reliable estimate.

## Sample rate choices in the wild

Linux `perf` defaults to 99 Hz — not 100 Hz. This is deliberate: 100 Hz can accidentally synchronise with periodic kernel timers and produce misleading results. 99 Hz avoids the resonance. Go pprof defaults to 100 Hz. eBPF-based continuous profilers typically use 19 or 49 Hz to minimise impact on very busy containers.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Approach</th>
        <th class="border border-border px-3 py-2 text-left">Cost model</th>
        <th class="border border-border px-3 py-2 text-left">Typical overhead</th>
        <th class="border border-border px-3 py-2 text-left">Use case</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Instrumentation</td>
        <td class="border border-border px-3 py-2">Per function call</td>
        <td class="border border-border px-3 py-2">20-100%</td>
        <td class="border border-border px-3 py-2">Dev benchmarks, targeted micro-benchmarks</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Sampling (Go pprof)</td>
        <td class="border border-border px-3 py-2">Per sample (100/s)</td>
        <td class="border border-border px-3 py-2">0.5-2%</td>
        <td class="border border-border px-3 py-2">Production on-demand or continuous</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Sampling (eBPF)</td>
        <td class="border border-border px-3 py-2">Per sample (19-49/s)</td>
        <td class="border border-border px-3 py-2">1-3%</td>
        <td class="border border-border px-3 py-2">Continuous, polyglot fleets</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Continuous profiler (full)</td>
        <td class="border border-border px-3 py-2">Sampling + batching + shipping</td>
        <td class="border border-border px-3 py-2">2-5%</td>
        <td class="border border-border px-3 py-2">Always-on production fleet</td>
      </tr>
    </tbody>
  </table>
</div>

<NumbersCard
  id="card-sample-rates"
  title="Sample rates and overhead"
  stats={[
    { label: "Go pprof default sample rate", value: "100 Hz" },
    { label: "Linux perf default rate", value: "99 Hz (avoids timer sync)" },
    { label: "eBPF profiler typical rate", value: "19 or 49 Hz" },
    { label: "Stack walk cost per sample", value: "~5-20 μs" },
    { label: "Overhead at 100 Hz, busy CPU", value: "~0.5-2%" },
    { label: "Continuous profiler (full pipeline)", value: "2-5% CPU" },
  ]}
/>

## When the claimed 2-5% overhead becomes 12%

A continuous profiler claims 2% overhead; you measure 12% in production. The most common causes:

- Sample rate was accidentally set 10x higher than default.
- Stack walking is expensive because the language runtime uses JIT-compiled code requiring symbol resolution at sample time (Python, JVM without native hooks).
- The agent is doing heavy symbol decompression or compression on the application thread instead of async.
- Average stack depth is 120+ frames (deep middleware chains) — each sample costs proportionally more to walk.

Always check profiler config before assuming the tool is misbehaving.

<Quiz
  id="02-sampling-vs-instrumenting-quiz-1"
  lessonSlug="02-sampling-vs-instrumenting"
  lang="en"
  question="A service makes 5 million function calls per second. An instrumentation profiler adds 1 μs per call. A sampling profiler runs at 100 Hz and costs 10 μs per sample. Which adds less overhead?"
  choices={[
    { label: "Sampling profiler: 100 × 10 μs = 1 ms overhead per second vs 5 million × 1 μs = 5 seconds per second for instrumentation — sampling wins by 5000x", correct: true },
    { label: "Instrumentation profiler because 1 μs is tiny", misconception: "1 μs is tiny per call but multiplied by 5 million calls per second it vastly exceeds sampling overhead." },
    { label: "They are equivalent", misconception: "The per-call vs per-sample cost models diverge dramatically at high call rates." },
    { label: "Neither matters at modern CPU speeds", misconception: "Both add real, measurable overhead; sampling is designed to keep it bounded." },
  ]}
/>

<Quiz
  id="02-sampling-vs-instrumenting-quiz-2"
  lessonSlug="02-sampling-vs-instrumenting"
  lang="en"
  question="Linux perf defaults to 99 Hz, not 100 Hz. Why?"
  choices={[
    { label: "To avoid accidentally synchronising with 100 Hz periodic kernel timers, which would make certain code paths appear in every sample regardless of their real CPU usage", correct: true },
    { label: "Because 99 is a prime number and primes are lucky", misconception: "The reason is technical synchronisation avoidance, not numerology." },
    { label: "Because 100 Hz requires special kernel patches", misconception: "100 Hz is fully supported; the choice is deliberate statistical safety." },
    { label: "To save 1% overhead", misconception: "The overhead difference between 99 and 100 Hz is negligible; the motivation is timer resonance avoidance." },
  ]}
/>

</Explanation>

<KeyTakeaway>Sampling captures one stack every 10 ms at bounded overhead regardless of call rate; instrumentation wraps every call and collapses under load. A function consuming X% CPU appears in X% of samples — the statistic is CPU share, not call count. The 2-5% overhead claimed by continuous profilers is real and verifiable; spikes above that indicate misconfiguration.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-sampling-vs-instrumenting-retrieval"
  lang="en"
  questions={[
    { q: "Why is instrumentation profiling impractical for always-on production profiling?", a: "Instrumentation wraps every function entry and exit with timing code, so cost scales with call frequency. A service processing 5 million calls per second at 1 μs per call adds 5 CPU-seconds of overhead per second — impossible to sustain. Sampling caps cost at N stack walks per second where N is the sample rate (typically 100), giving a fixed overhead of ~1 ms per second regardless of call rate. At production load, this difference is the gap between viable and catastrophic." },
    { q: "A sampling profiler reports a function at 8% of samples. What does this mean in terms of CPU usage?", a: "It means the function was on the CPU in 8% of the samples taken. At the profiler's statistical resolution, this represents approximately 8% of CPU time consumed by that function during the profiling window. The exact value has a confidence interval (wider for short captures, narrower for longer ones), but 8% is a reliable enough signal to act on. The statistic is CPU time share, not call count — a function called 10 million times for 1 μs each shows the same 10% as a function called 100 times for 100 μs each, if total CPU is comparable." },
    { q: "Why does Linux perf use 99 Hz instead of 100 Hz?", a: "Linux has periodic kernel timers that fire at multiples of a configurable base rate. If a sampling profiler fires at the same frequency as one of those timers, the profiler will always see that timer's code on the CPU — inflating its apparent CPU share artificially. 99 Hz is a prime number relative to common timer frequencies (100, 250, 1000 Hz), breaking any accidental lock-step. Brendan Gregg documented this pattern; most production profilers follow it. The overhead difference between 99 and 100 Hz is negligible; the statistical correctness difference can be significant." },
  ]}
/>

<Recap lang="en">
Instrumentation profiling wraps every function call, giving exact data but collapsing under production load as overhead scales with call frequency. Sampling profiling fires a clock N times per second and captures the current stack — bounded overhead regardless of how many functions are called between samples. At 100 Hz and ~10 μs per stack walk, the overhead is 0.5-2%; a full continuous profiler pipeline (with batching and shipping) adds to 2-5%. The statistical property that makes sampling powerful: a function consuming X% of CPU appears in X% of samples, so you get CPU share estimates without touching every call. Linux perf defaults to 99 Hz to avoid synchronising with periodic kernel timers — a subtle correctness detail every senior engineer should know. Now when you see a profiler claiming 2-5% overhead and then measuring 12%, you know where to look first: sample rate, stack depth, and whether JIT symbol resolution is happening on the application thread.
</Recap>
