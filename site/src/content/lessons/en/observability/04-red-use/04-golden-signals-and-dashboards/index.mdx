---
concepts:
- golden-signals
- service-mesh
- red-dashboard
- alerting-severity
- cross-protocol-red
deepensInto: []
estMin: 12
lang: en
lessonType: topic
level: middle
order: 4
prereqs:
- observability/04-red-use/01-red-and-use-overview
- observability/04-red-use/02-red-instrumentation
- observability/04-red-use/03-use-on-linux
slug: 04-golden-signals-and-dashboards
sources:
- https://sre.google/sre-book/monitoring-distributed-systems/
- https://grafana.com/blog/the-red-method-how-to-instrument-your-services/
- https://thenewstack.io/monitoring-microservices-red-method/
spiral:
- latency
status: ready
summary: How Google''''s four golden signals extend RED, what a senior RED+USE dashboard
  looks like, how service meshes emit RED without application code, and how RED transfers
  to non-HTTP protocols.
title: Golden signals, dashboard layout, and service mesh auto-RED
track: observability
unit: 04-red-use
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import alertSeveritySplit from "./infographics/alert-severity-split.svg?raw";

<Hook>
A team has five services. Each RED dashboard looks different — different metric names, different panel layouts, different label structures. When an incident crosses services, the on-call spends 30 minutes reading documentation before starting triage. A single consistent dashboard pattern across all services would reduce that to seconds.
</Hook>

<Crux>Golden signals add Saturation to RED. One dashboard, whole fleet. Meshes give RED free. Symptom (RED) before cause (USE).</Crux>

<Explanation>

## The four golden signals

Google's SRE book (predating RED and USE in spirit, published later) names four signals: **Latency, Traffic, Errors, Saturation**. The overlap with RED is direct: Latency = Duration, Traffic = Rate, Errors = Errors. The novel piece is **Saturation** — "how full" the service is relative to a known capacity limit.

Unlike USE's Saturation (which is about physical resources), the golden-signal Saturation is at the **service level**: in-flight request count, queue length, active session count, connection pool occupancy. A service can be at 50% CPU but have its connection pool at 100% — the service is full, users are queueing, and USE on the host shows nothing.

Many production teams treat RED + Saturation as the practical evolution of the SRE-book signals:

| Signal | RED | Golden signals |
|--------|-----|---------------|
| Request rate | Rate | Traffic |
| Failed requests | Errors | Errors |
| Response latency | Duration | Latency |
| Capacity headroom | _(implicit in Rate plateau)_ | Saturation |

## The senior dashboard layout

If your dashboards look different across services, you are paying a hidden tax: every new incident requires re-learning the layout instead of reading the signal. The layout below is the same for every service, every environment — the only thing that changes is the data.

A well-structured service dashboard reads top-to-bottom during an incident:

```
Row 1: RED  — Rate panel | Errors panel | Duration p50/p95/p99 panel
Row 2: USE  — CPU util+PSI | Memory util+PSI | Disk %util+queue
Row 3: Downstreams — DB response time + errors | Cache hit rate | Queue depth
```

Reading rhythm:
1. Which RED metric moved?
2. Which USE row matches the timeline?
3. Which downstream dependency followed the same timeline?
4. Click the exemplar to confirm in a trace.

The key discipline: RED and USE share the same time axis in the same dashboard. Cross-service correlation requires consistent label keys (`service`, `route`, `region`) so panels can be filtered by the same label set.

<StackDiagram
  label="The four golden signals as dashboard rows"
  hue="var(--d-systems)"
  layers={[
    { label: "Latency — top-line SLO panel", note: "Duration p50 / p95 / p99" },
    { label: "Traffic — how much demand arrives", note: "Rate · req/s" },
    { label: "Errors — fraction of requests failing", note: "Errors · 5xx %" },
    { label: "Saturation — how full vs capacity", note: "in-flight · queue · pool %" },
  ]}
  caption="A golden-signals dashboard reads top-to-bottom: latency/SLO first (what the user feels), then traffic, errors, and saturation — each signal one row."
/>

## Alerting severity split

The mature pattern is to **alert on user-facing symptoms (RED) at page-grade** and on **internal cause signals (USE) at warning-grade**:

| Alert | Metric | Severity |
|-------|--------|----------|
| Latency SLO breach | p99 Duration &gt; 200 ms for 5 min | Page |
| Error rate spike | Errors &gt; 1% for 2 min | Page |
| CPU saturation | PSI cpu some &gt; 20% for 10 min | Warning / Slack |
| Memory pressure | PSI memory full &gt; 0 for 5 min | Warning / Slack |
| Disk headroom | Disk &gt; 90% for 5 min | Warning / Slack |

The split is deliberate. RED alerts wake humans because users are affected. USE alerts notify a slower channel so capacity teams plan ahead — not so on-call wakes up every time CPU touches 70%. This one architectural choice is among the strongest levers against alert fatigue.

<Infographic
  svg={alertSeveritySplit}
  label="Two-column comparison of the alerting severity split: the RED column pages on-call for user-facing symptoms — latency SLO breach (p99 > 200 ms for 5 min) and error rate spike (Errors > 1% for 2 min); the USE column routes internal-cause signals to Slack or a ticket — CPU saturation, memory pressure, and disk headroom at warning-grade."
  caption="RED pages because the user is already unhappy; USE warns so the capacity team plans before it becomes an incident. Routing symptom-vs-cause to different severities is the single strongest lever against alert fatigue."
  hue="var(--d-infra)"
/>

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Layer</th>
        <th class="border border-border px-3 py-2 text-left">Channel</th>
        <th class="border border-border px-3 py-2 text-left">Reason</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">RED alert (user impact)</td>
        <td class="border border-border px-3 py-2">PagerDuty / on-call page</td>
        <td class="border border-border px-3 py-2">User is already unhappy</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">USE alert (resource headroom)</td>
        <td class="border border-border px-3 py-2">Slack / ticket</td>
        <td class="border border-border px-3 py-2">Plan capacity before it becomes a RED incident</td>
      </tr>
    </tbody>
  </table>
</div>

## Service mesh auto-RED

Service meshes (Envoy, Linkerd, Istio) emit RED metrics at the sidecar without application code changes. Envoy's `downstream_rq_total`, `downstream_rq_xx`, and `downstream_rq_time` give Rate, Errors-by-status, and Duration histogram per cluster, labelled consistently across the fleet.

**Advantage**: a polyglot estate (Node, Go, Python, JVM, Rust) gets one RED dashboard pattern even though languages disagree on Prometheus client conventions.

**Catch**: the sidecar sees only what the network sees. A request that succeeds at the sidecar but is mis-served by buggy application code shows as 2xx in mesh metrics.

**Production pattern**: run both layers — sidecar RED for breadth and consistency, application-emitted RED for business-logic fidelity (e.g. a payment-success counter labelled by gateway that the sidecar cannot see). Keep label keys aligned across both layers so dashboards join them.

## RED transfers across protocols

RED's request-centric shape transfers to every protocol; only the definition of "request" and "error" changes:

| Protocol | Rate | Errors | Duration |
|----------|------|--------|----------|
| HTTP | req/s | HTTP 5xx | p99 response time |
| gRPC | RPCs/s | status != OK | end-to-end for unary, first-byte for streaming |
| Queues (Kafka, SQS) | messages/s consumed | dead-letter rate | publish-to-consume wait time |
| Batch jobs | jobs/min | failed jobs | wall-clock per job |
| Serverless | invocations/s | error rate + throttle rate | includes cold-start tail |

For async and queue-based services, queue depth (backlog) is the **Saturation** signal that pure RED misses. A queue with Duration p99 of 10 s per job may have a 10-minute backlog — the user waits 10 min + 10 s. RED captures the job-processing time; Saturation captures the queue-wait before processing begins.

<Inset kind="why" lang="en">
Batch and async systems need a separate queue-depth metric that RED alone cannot provide. The pattern is to emit `queue_depth_seconds` — the age of the oldest unprocessed item in wall-clock seconds. If this grows, users are waiting longer than the job Duration suggests. This is the Saturation signal at the service level, complementing USE's resource-level Saturation.
</Inset>

<Quiz
  id="04-golden-signals-quiz-1"
  lessonSlug="04-golden-signals-and-dashboards"
  lang="en"
  question="What does 'Saturation' mean in the four golden signals, and how does it differ from USE's Saturation?"
  choices={[
    { label: "It means the same thing — how full a physical resource like CPU or disk is", misconception: "Golden-signal Saturation extends to service-level capacity: connection pool occupancy, in-flight count, queue length — not just physical resources." },
    { label: "Golden-signal Saturation measures how full the service is relative to a logical capacity limit (connection pool, queue), while USE Saturation measures resource queue depth (CPU run-queue, disk I/O queue).", correct: true },
    { label: "Golden-signal Saturation is only about network bandwidth", misconception: "Saturation in the SRE book is explicitly service-level — it covers any capacity limit, not just network." },
    { label: "They are identical — both come from Brendan Gregg's USE method", misconception: "Golden signals are from Google's SRE book; USE is Brendan Gregg's. Saturation in the SRE book deliberately extends USE's concept to the service level." },
  ]}
/>

<Quiz
  id="04-golden-signals-quiz-2"
  lessonSlug="04-golden-signals-and-dashboards"
  lang="en"
  question="An on-call engineer sees RED Duration p99 climb but Rate and Errors are flat. Which USE row should they check first?"
  choices={[
    { label: "Disk errors", misconception: "Disk errors (USE-E) would show up as EIO events, not explain a Duration spike alone." },
    { label: "Network interface utilization", misconception: "Network saturation usually shows in retransmits, not a Duration drift with stable R and E." },
    { label: "CPU saturation on the service hosts, or the downstream database's response time and utilization — whatever sits between the service and its slowest dependency", correct: true },
    { label: "Memory utilization alone", misconception: "Memory utilization alone rarely causes Duration spikes without PSI memory saturation (si/so non-zero or PSI full)." },
  ]}
/>

<DragOrder
  id="04-golden-signals-drag-1"
  lessonSlug="04-golden-signals-and-dashboards"
  lang="en"
  prompt="Order the RED + USE incident response steps in production:"
  items={[
    { id: "a", label: "Page fires — symptom description includes service name and severity" },
    { id: "b", label: "Open the service's RED dashboard, identify which of R / E / D moved" },
    { id: "c", label: "Read the time series — single spike or sustained drift?" },
    { id: "d", label: "Cross-reference USE on the same hosts and on direct downstream dependencies" },
    { id: "e", label: "If RED-D moved with USE-CPU saturation: capacity issue → scale" },
    { id: "f", label: "If RED-E moved with USE-Errors on a dependency: dependency failure → failover" },
    { id: "g", label: "If both RED and USE look fine but pager fired: revisit the alert source, suspect false positive" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g"]}
/>

</Explanation>

<KeyTakeaway>Golden signals add Saturation to RED's three dimensions. One consistent dashboard layout (RED top, USE middle, downstreams bottom) scales to the whole fleet. Alert RED at page-grade, USE at warning-grade — this single decision prevents the alert-fatigue spiral that ruins on-call rotations.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-golden-signals-retrieval"
  lang="en"
  questions={[
    { q: "What does Google's SRE book add to RED that RED itself does not cover?", a: "Saturation — how full the service is relative to a known capacity limit. This extends beyond physical resource saturation (USE) to service-level signals: connection pool occupancy, in-flight request count, queue depth. A service can be at 50% CPU but have its connection pool at 100%, and USE on the host shows nothing — golden-signal Saturation catches this." },
    { q: "What is the practical advantage and limitation of service-mesh auto-RED?", a: "Advantage: a polyglot fleet (Node, Go, Python, JVM, Rust) gets one consistent RED metric shape without changing any application code, because the sidecar instruments all traffic at the network level. Limitation: the sidecar only sees what the network sees — a request that succeeds at the network but is mis-served by buggy application code (e.g. returns wrong data with a 200 status) is invisible to mesh RED. Application-emitted RED for business logic must complement the mesh layer." },
    { q: "For a Kafka consumer, what are Rate, Errors, and Duration, and what extra signal is needed?", a: "Rate: messages consumed per second. Errors: dead-letter rate (messages that failed processing and were discarded or moved to DLQ). Duration: wall-clock time from message consumption to commit (processing time per message). Extra signal: queue depth / consumer lag in seconds (the age of the oldest unprocessed message) — this is the Saturation signal, capturing how long messages wait before processing begins, which pure RED Duration does not cover." },
  ]}
/>

<Recap lang="en">
Google's four golden signals — Latency, Traffic, Errors, Saturation — extend RED by adding a service-level Saturation dimension: how full the service is relative to its logical capacity (connection pool, in-flight count, queue depth), not just physical resources. A mature RED+USE dashboard puts RED panels in the top row, USE panels in the middle, and downstream dependency RED+USE in the bottom row, all on the same time axis with consistent label keys. Alerting discipline is the other half: RED alerts page on-call (user is affected), USE alerts go to Slack or a ticket (capacity team plans ahead). Service meshes emit RED for free at the sidecar level — consistent across languages — but only see what the network sees; application-emitted RED is needed for business-logic fidelity. RED's shape transfers to gRPC, queues, batch jobs, and serverless with minimal adjustment — only the definition of "request" and "error" changes. Now when you build a new service dashboard, you will resist the urge to make it unique — same layout, same label keys, and your future on-call self will find the answer in seconds instead of minutes.
</Recap>
