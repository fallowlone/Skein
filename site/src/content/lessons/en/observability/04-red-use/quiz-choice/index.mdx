---
concepts:
- red-method
- use-method
- golden-signals
- cardinality
- saturation
- slo-burn-rate
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.brendangregg.com/usemethod.html
- https://grafana.com/blog/the-red-method-how-to-instrument-your-services/
- https://sre.google/sre-book/monitoring-distributed-systems/
status: ready
summary: "Multiple-choice synthesis across the RED+USE unit — symptom vs cause triage, saturation, histogram aggregation, cardinality, and alert-severity discipline."
title: 'RED and USE: multiple-choice review'
track: observability
unit: 04-red-use
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. None is a definition to recite — each mirrors a decision you make mid-incident, when the pager is loud and you have to pick the right dashboard, the right signal, and the right alert grade.
</Hook>

<Goal lang="en">
Confirm you can fuse the two checklists into one triage discipline: RED names the symptom from the caller's side, USE finds the cause from the kernel's side, and the supporting decisions — saturation, histogram aggregation, cardinality, alert severity — all serve that reading rhythm.
</Goal>

<Quiz
  id="04-red-use-choice-1"
  lessonSlug="04-red-use"
  lang="en"
  question="The pager fires on checkout p99 (80 ms to 1.2 s). RED shows Rate steady, Errors under 0.1%, Duration p99 15x worse. What is the correct next move, and why?"
  choices={[
    { label: "Switch to the USE dashboard for the resources under the service. RED has named the symptom (slow but succeeding requests); USE finds the cause — look for a resource whose utilization or saturation jumped on the same timeline.", correct: true },
    { label: "Start reading traces and logs immediately to find the slow request.", misconception: "Traces and logs are step 5, after RED and USE narrow the search space. Jumping to per-request data first means searching the whole haystack instead of the resource USE points you to." },
    { label: "Roll back the last deploy — a 15x latency spike is always a bad release.", misconception: "RED has not implicated a deploy. Rate is steady and errors are flat, which fits a resource crunch (CPU saturation, downstream slowdown) at least as well as a code regression. USE tells you which before you act." },
    { label: "Page the database team — Duration spikes are always downstream.", misconception: "You do not yet know the bottleneck is the database. Duration drift with flat R and E can be CPU saturation on the service hosts, a saturated connection pool, or a downstream — USE on the hosts and direct dependencies is what discriminates." },
  ]}
/>

<Quiz
  id="04-red-use-choice-2"
  lessonSlug="04-red-use"
  lang="en"
  question="A disk sits at 80% utilization with I/O queue depth 50; another disk sits at 95% utilization with queue depth 1. Which is the worse signal, and which USE dimension tells you?"
  choices={[
    { label: "The 80%/queue-50 disk is far worse. Saturation — the queue of work waiting — is the leading indicator of latency; every queued job pays the wait. A resource at 95% utilization with queue depth 1 is just keeping pace with no backlog.", correct: true },
    { label: "The 95%-utilization disk, because higher utilization always means more latency.", misconception: "Utilization is average busy-time, not waiting work. 95% busy with queue depth 1 means the disk keeps up; 80% busy with queue depth 50 means jobs pile up. Saturation, not utilization, predicts user-visible latency." },
    { label: "They are equally bad — both disks are heavily used.", misconception: "They are not equal. The queue depth differs by 50x. The whole point of USE's S is that two resources at similar utilization can have wildly different waiting-work, and the queued one is the incident." },
    { label: "Neither matters until utilization hits 100%.", misconception: "Waiting for 100% is too late. Saturation rises before utilization saturates and is the earlier, more diagnostic signal — a queue forms well before the resource is pinned." },
  ]}
/>

<Quiz
  id="04-red-use-choice-3"
  lessonSlug="04-red-use"
  lang="en"
  question="A node's memory utilization is moderate and MemAvailable still shows 500 MB free, yet a worker stalls for minutes. Which signal would have caught it, and why do free-RAM dashboards miss it?"
  choices={[
    { label: "PSI memory 'full' going non-zero: all non-idle tasks were stalled waiting for the kernel to free pages. Free-RAM shows what is available right now, not the reclaim pressure the kernel paid to keep it available.", correct: true },
    { label: "node_memory_MemAvailable crossing below 10% — the standard memory alert.", misconception: "MemAvailable still showed 500 MB; the free-RAM threshold never tripped. That is exactly the gap PSI closes — pressure can be severe while available memory still reads a healthy number." },
    { label: "CPU utilization above 80%, since the stall must be a CPU problem.", misconception: "The stall is memory reclaim, not CPU. PSI cpu would be the wrong resource; PSI memory 'full' is the signal that fires when the kernel is thrashing reclaim." },
    { label: "Nothing — 500 MB free proves there was no memory pressure.", misconception: "This is the precise mistake the unit warns against. PSI memory 'full' can be high while MemAvailable looks plentiful; the kernel blocks allocating tasks while it struggles to reclaim pages." },
  ]}
/>

<Quiz
  id="04-red-use-choice-4"
  lessonSlug="04-red-use"
  lang="en"
  question="A team computes fleet-wide p99 by averaging each replica's pre-computed p99 (Prometheus summaries). Why is this wrong, and what is the correct method?"
  choices={[
    { label: "Percentiles are not additive — the p99 of merged distributions is not the average of per-replica p99s, and can be off by 2x or more. Correct: emit histograms, aggregate bucket counts with sum by (le) (rate(...)), then run histogram_quantile over the merged counts.", correct: true },
    { label: "Summaries are simply less accurate than histograms per replica, so per-replica numbers are already wrong.", misconception: "Per-replica summary accuracy is fine. The defect is aggregation: averaging percentiles across replicas is mathematically invalid even when each replica's p99 is exact." },
    { label: "It is correct as long as every replica serves roughly equal traffic.", misconception: "Equal traffic does not save it. If latency distributions differ across replicas, the average of their p99s still is not the p99 of the combined distribution." },
    { label: "Summaries cannot be queried in PromQL, so the query returns nothing.", misconception: "Summaries are queryable; the problem is the mathematics of cross-replica percentile aggregation, not query support." },
  ]}
/>

<Quiz
  id="04-red-use-choice-5"
  lessonSlug="04-red-use"
  lang="en"
  question="To jump from a p99 histogram spike to the exact slow request, an engineer wants to add trace_id as a metric label. What breaks, and what is the designed alternative?"
  choices={[
    { label: "trace_id is unbounded, so each value multiplies the series count and can crash the TSDB or explode the bill. Use exemplars: a sampled trace_id is attached to an individual histogram observation, so Grafana jumps to the trace without adding a cardinality-multiplying label.", correct: true },
    { label: "Nothing breaks — trace_id is a normal label and Prometheus handles it fine.", misconception: "trace_id is effectively unique per request, the canonical unbounded-cardinality label. Every existing series gets a new variant per trace; the head block and the bill grow without bound." },
    { label: "Add a second counter labelled by trace_id to hold the references instead.", misconception: "That just moves the same unbounded cardinality to a different metric — identical TSDB and cost blow-up. Exemplars exist precisely to avoid storing trace_id as a label anywhere." },
    { label: "Reduce the histogram's bucket count so the query is cheaper to correlate.", misconception: "Fewer buckets lowers p99 accuracy and does nothing for trace correlation. The bridge between a metric spike and a specific trace is the exemplar, not bucket count." },
  ]}
/>

<Quiz
  id="04-red-use-choice-6"
  lessonSlug="04-red-use"
  lang="en"
  question="Designing alerts for a service, how should RED and USE signals map to severity, and why is the split the strongest lever against alert fatigue?"
  choices={[
    { label: "Page on RED (user-facing symptoms: p99 SLO breach, error-rate spike); route USE (PSI cpu/memory, disk headroom) to Slack or a ticket. Users are already affected when RED fires; USE is a capacity heads-up to plan before it becomes a RED incident.", correct: true },
    { label: "Page on every USE alert too, so the team always knows when CPU passes 70%.", misconception: "Paging on resource thresholds is the classic fatigue spiral — most CPU/memory ticks never reach the user. USE belongs on a slower channel so on-call is woken only by actual user impact." },
    { label: "Page on USE and treat RED as informational, since resource health is the root cause.", misconception: "This inverts the discipline. The user feels RED, not USE; a saturated resource that never degrades a request is not an incident. Page on the symptom, investigate the cause." },
    { label: "Put everything at the same severity so nothing is missed.", misconception: "Flat severity is how teams drown. The deliberate RED-page / USE-warning split is among the strongest single levers against alert fatigue precisely because it separates 'user is hurting now' from 'plan capacity soon'." },
  ]}
/>

<Recap lang="en">
The through-line of the unit is one decision tree: RED names the symptom from the caller's side (Rate, Errors, Duration), USE finds the cause from the kernel's side (Utilization, Saturation, Errors), and saturation — queue depth, PSI — is the most diagnostic dimension because waiting work, not average busy-time, is what users feel. Around that core sit the supporting disciplines: Duration must be a histogram aggregated with sum by (le) (never an averaged percentile); cardinality is bounded by labelling only route/method/status_class and bridging to traces via exemplars; and alerts page on RED, warn on USE. Every production failure in the unit reduces to one missed signal in that tree.
</Recap>
