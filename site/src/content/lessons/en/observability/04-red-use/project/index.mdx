---
concepts:
- red-method
- use-method
- golden-signals
- cardinality
- slo-burn-rate
estMin: 240
lang: en
level: senior
order: 10
prereqs: []
slug: project
sources:
- https://grafana.com/blog/the-red-method-how-to-instrument-your-services/
- https://www.brendangregg.com/usemethod.html
- https://sre.google/sre-book/monitoring-distributed-systems/
status: ready
summary: "Hands-on project — instrument a service with bounded RED, wire USE/PSI for its hosts, build the layered dashboard, then drive an incident and triage it RED-first, USE-second with evidence."
title: 'RED and USE: build the dashboard and triage an incident'
track: observability
unit: 04-red-use
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Reading about RED+USE triage is not the same as running it under load. Instrument a real service the way a senior engineer would, build the layered dashboard, deliberately break it, and prove you can name the symptom from RED and the cause from USE — with screenshots and numbers at every step.
</Hook>

<Goal lang="en">
Turn the unit's mental model into a working observability stack and a triage you can defend: bounded RED metrics, USE plus PSI for the hosts, a single layered dashboard, an alert split that pages on symptoms, and a documented RED-first / USE-second incident walkthrough.
</Goal>

<ProjectBrief
  id="04-red-use-project"
  lessonSlug="04-red-use"
  lang="en"
  objective="Instrument a small HTTP service with correct RED, wire USE and PSI for its hosts, assemble one layered RED-over-USE dashboard, then induce an incident and triage it RED-first / USE-second — proving each conclusion with a panel, a query, or a number."
  requirements={[
    "Build or take a small HTTP service (Go, Node, JVM, or Python) with at least one route whose latency you can manipulate, and one downstream dependency (a DB or cache) you can slow down.",
    "Instrument RED: a requests counter (Rate), a 5xx errors counter (Errors), and a duration histogram (Duration). Label only route template, method, and status_class — no user_id, request_id, or error content.",
    "Choose histogram buckets that bracket your SLO target with at least three boundaries on each side and adjacent boundaries within ~2x near the target (or use native / exponential histograms). Write down the SLO you are targeting.",
    "Deploy node_exporter (or equivalent) and collect USE per host: CPU utilization + PSI cpu, memory utilization + PSI memory, disk %util + queue depth, network throughput + retransmits.",
    "Build one dashboard with the layered layout: Row 1 RED (Rate | Errors | Duration p50/p95/p99 via sum by (le)), Row 2 USE (CPU+PSI | Memory+PSI | Disk %util+queue), Row 3 the downstream's RED — all on the same time axis with shared label keys.",
    "Wire alerts with the severity split: page on RED (p99 SLO breach, error-rate spike); send USE/PSI (PSI cpu high, PSI memory full above zero, disk headroom) to a warning channel. Bonus: a multi-window burn-rate alert on the latency SLO.",
    "Induce two distinct incidents under load — one CPU-saturation slowdown (flat Errors, climbing Duration, PSI cpu rising) and one downstream failure or slowdown — and triage each RED-first then USE-second, capturing the panels that named the symptom and the cause.",
  ]}
  acceptance={[
    "A screenshot of the layered dashboard with all three rows populated under load, p99 computed via histogram_quantile with sum by (le).",
    "A short cardinality audit: list every label on the RED metrics, its bounded value set, and the resulting series count — and confirm no unbounded or PII-bearing label is present.",
    "Two written triage walkthroughs, one per induced incident, each naming which RED signal moved, which USE/PSI signal explained it, and the order you read them — backed by the captured panels.",
    "Evidence the alert split works: the RED alert paged on the user-facing incident and the USE/PSI signal stayed on the warning channel (or fired there), not the page channel.",
  ]}
  stretch={[
    "Add an on-call runbook: the RED-first / USE-second reading rhythm, the four USE resources with their saturation metric, the alert-severity table, and a checklist for confirming a false positive.",
    "Add exemplars to the duration histogram and demonstrate clicking a p99 spike to jump straight to the slow request's trace.",
    "Add a service-mesh sidecar (Envoy/Linkerd) and compare its auto-RED against your application-emitted RED — find one case the sidecar cannot see (e.g. a 200 that returned wrong data).",
    "Add an async or queue-based path and instrument its Saturation signal (consumer lag / queue-depth-in-seconds), showing a backlog the per-job Duration alone would miss.",
  ]}
/>

<Recap lang="en">
This is the loop you run on every real incident: instrument RED with bounded labels, wire USE plus PSI for the resources underneath, put them on one layered dashboard sharing a time axis, page on the symptom and warn on the cause, then triage RED-first to name what the user felt and USE-second to find why. Doing it once on a service you broke on purpose — with screenshots, a cardinality audit, and two written walkthroughs — turns the discipline into reflex for the production version.
</Recap>
