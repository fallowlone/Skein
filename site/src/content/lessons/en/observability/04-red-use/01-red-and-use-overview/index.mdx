---
concepts:
- red-method
- use-method
- incident-triage
- golden-signals
deepensInto: []
estMin: 10
lang: en
lessonType: topic
level: junior
order: 1
prereqs:
- observability/03-otel/01-what-is-otel
slug: 01-red-and-use-overview
sources:
- https://www.brendangregg.com/usemethod.html
- https://grafana.com/blog/the-red-method-how-to-instrument-your-services/
- https://sre.google/sre-book/monitoring-distributed-systems/
spiral:
- latency
status: ready
summary: RED (Rate, Errors, Duration) describes what the user felt. USE (Utilization,
  Saturation, Errors) describes which resource caused it. Running both in sequence
  is the senior engineer''''s first reflex when the pager fires.
title: 'RED and USE: two checklists, one triage discipline'
track: observability
unit: 04-red-use
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";

<Hook>
The pager fires. p99 latency on the checkout service spiked from 80 ms to 1.2 s. You have seconds to find the right dashboard before the incident commander is asking for updates. The wrong move is opening every panel. The right move is two short checklists.
</Hook>

<Crux>RED = Rate, Errors, Duration (services). USE = Utilization, Saturation, Errors (resources). Two checklists, no debate.</Crux>

<Explanation>

## What RED is

Tom Wilkie introduced RED at a London Prometheus meetup in 2015. RED describes a service from the **caller's perspective**:

- **Rate** — requests per second arriving at the service.
- **Errors** — failed requests per second (HTTP 5xx, gRPC non-OK, timeouts).
- **Duration** — the latency distribution (p50 / p95 / p99) of the requests that did complete.

If RED looks sick, the user is sick. Rate tells you whether traffic is normal. Errors tell you whether requests are failing. Duration tells you whether surviving requests are slow.

## What USE is

Brendan Gregg named USE in 2012 as an emergency checklist for performance debugging. USE describes **every resource** (CPU, memory, disk, network, locks, thread pools) from three angles:

- **Utilization** — average percentage of time the resource was busy.
- **Saturation** — amount of queued work the resource cannot service yet (run-queue length, wait time).
- **Errors** — count of error events on the resource (ECC errors, disk EIO, NIC CRC, ENOSPC).

If USE looks sick on a box, that resource is a candidate cause for the RED symptom.

## The layered mental model

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Method</th>
        <th class="border border-border px-3 py-2 text-left">What it measures</th>
        <th class="border border-border px-3 py-2 text-left">Answers</th>
        <th class="border border-border px-3 py-2 text-left">Origin</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2 font-mono text-xs">RED</td>
        <td class="border border-border px-3 py-2">Services (request-driven)</td>
        <td class="border border-border px-3 py-2">Is the user unhappy? Which symptom?</td>
        <td class="border border-border px-3 py-2">Tom Wilkie, 2015</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2 font-mono text-xs">USE</td>
        <td class="border border-border px-3 py-2">Resources (CPU, memory, disk, …)</td>
        <td class="border border-border px-3 py-2">Which resource caused it?</td>
        <td class="border border-border px-3 py-2">Brendan Gregg, 2012</td>
      </tr>
    </tbody>
  </table>
</div>

The reading rhythm during an incident is always **RED first, USE second**:

1. Open the RED dashboard for the affected service.
2. Identify which of R / E / D is abnormal — that names the symptom.
3. Switch to the USE dashboard for the resources under that service.
4. Find the resource where utilization or saturation jumped — that names the candidate cause.
5. Drill into traces, logs, or profiling only after RED and USE have narrowed the search space.

## The hospital metaphor

Think of a hospital. RED is the patient's vital signs — pulse, blood pressure, temperature — measured from the outside. USE is the ICU monitoring of each life-support machine — oxygen flow, pump pressure, error LEDs — measured at the equipment itself. You need both. If the patient's vitals crash you act fast, but to know _why_ you walk over and check the machines. Doctors who watch only one side miss obvious problems.

## A concrete triage

On-call engineer Bea gets paged: p99 latency on the checkout service spiked from 80 ms to 1.2 s. The RED dashboard shows Rate steady at 400 req/s, Errors under 0.1%, Duration p99 15× worse. That is the RED triage — requests still arrive and mostly succeed, but they are slow. Bea switches to USE on the boxes: CPU at 92%, run queue jumped from 0 to 14. The boxes are CPU-saturated; threads queue for cycles. Fix: scale out. Diagnosis took under a minute.

<Inset kind="why" lang="en">
USE's Saturation is the most diagnostic signal of the three. Utilization tells you how busy a resource was on average — a CPU at 100% utilization is fine if no work is waiting (it is just keeping pace). What matters is the run-queue length. A disk at 80% utilization with queue depth 50 is far worse than a disk at 95% utilization with queue depth 1, because the queue depth is the leading indicator of latency: every job in line pays the queueing delay.
</Inset>

<Quiz
  id="01-red-and-use-overview-quiz-1"
  lessonSlug="01-red-and-use-overview"
  lang="en"
  question="A monitoring dashboard shows the service Rate, Errors, and Duration. Which methodology is this?"
  choices={[
    { label: "RED — Rate, Errors, Duration; measures the service from the caller's perspective", correct: true },
    { label: "USE — Utilization, Saturation, Errors", misconception: "USE covers resource health (CPU, memory, disk), not service-level signals." },
    { label: "The four golden signals", misconception: "Golden signals are Latency, Traffic, Errors, Saturation — overlapping but distinct from RED." },
    { label: "PSI — Pressure Stall Information", misconception: "PSI is a Linux kernel feature reporting resource pressure, not a service methodology." },
  ]}
/>

<Quiz
  id="01-red-and-use-overview-quiz-2"
  lessonSlug="01-red-and-use-overview"
  lang="en"
  question="USE method says: for every resource, check Utilization, Saturation, and Errors. What does 'saturation' mean in USE?"
  choices={[
    { label: "The percentage of memory used", misconception: "That is utilization, not saturation." },
    { label: "The number of errors in the past minute", misconception: "That is the E in USE, not the S." },
    { label: "The amount of extra work queued waiting for the resource — e.g. threads in the run queue waiting for CPU", correct: true },
    { label: "Whether the system has reached 100% CPU", misconception: "100% CPU is a utilization fact; saturation is the queue of waiting work behind it." },
  ]}
/>

<MetaphorComplete
  id="01-red-and-use-overview-metaphor-1"
  pieceSlug="01-red-and-use-overview"
  lang="en"
  setup="Fill in the blank: RED is the methodology for measuring _______, while USE is the methodology for measuring resources."
  accepted={["services", "service", "requests", "request-driven services"]}
  canonical="services (specifically request-driven services like HTTP APIs, RPCs, queues)"
  explanation="Tom Wilkie introduced RED specifically because the USE method, designed for hardware resources, did not capture how a service looks to its callers. RED measures the request flow; USE measures the resource pool serving it."
/>

<DragOrder
  id="01-red-and-use-overview-drag-1"
  lessonSlug="01-red-and-use-overview"
  lang="en"
  prompt="Order the steps of a typical RED+USE incident response:"
  items={[
    { id: "a", label: "Pager fires — a symptom is reported (latency, errors, capacity)" },
    { id: "b", label: "Open the RED dashboard for the affected service" },
    { id: "c", label: "Identify which of R / E / D is abnormal — that names the symptom" },
    { id: "d", label: "Switch to the USE dashboard for the resources under that service" },
    { id: "e", label: "Find the resource where utilization or saturation jumped — that names the cause" },
    { id: "f", label: "Drill into traces, logs, or profiling only after RED and USE have narrowed scope" },
    { id: "g", label: "Apply the fix (scale up, restart, throttle, rollback) and watch RED return to baseline" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g"]}
/>

</Explanation>

<KeyTakeaway>RED says which service is sick and how (symptom). USE says which resource caused it (cause). Run RED first, USE second — every time — before reaching for logs, traces, or profiling.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-red-and-use-overview-retrieval"
  lang="en"
  questions={[
    { q: "In two sentences, why is running RED alone or USE alone usually not enough for incident response?", a: "RED alone tells you the user is unhappy (slow, errors, missing traffic) but not why — a 100% CPU box, a saturated disk, a leaking memory allocator are all invisible at the service edge. USE alone tells you a resource is stressed, but not whether the user notices — a CPU at 95% on a batch worker is fine; on the request path it is an incident." },
    { q: "What are the three letters of USE and what does each measure?", a: "U = Utilization: average percentage of time the resource was busy. S = Saturation: amount of queued work the resource cannot service yet (run-queue length, wait time). E = Errors: count of error events on that resource (disk EIO, NIC CRC, ECC)." },
    { q: "Who introduced RED and when? Who introduced USE and when?", a: "Tom Wilkie introduced RED at a London Prometheus meetup in 2015, while working at Weaveworks. Brendan Gregg named USE in 2012 as an emergency performance-debugging checklist." },
  ]}
/>

<Recap lang="en">
RED and USE are two short checklists written two years apart that together cover both the service the user touches and the resources that service stands on. RED (Rate, Errors, Duration) measures request flow from the caller's perspective — if RED looks sick, the user is sick. USE (Utilization, Saturation, Errors) measures every physical and logical resource from three angles — if USE looks sick on a box, that is a candidate cause. The senior engineer's reflex on any incident is RED first (name the symptom), USE second (find the cause), and everything else — logs, traces, profiles — only after those two checklists have narrowed the space. Saturation, not utilization, is the most diagnostic dimension of USE: a queue of waiting work is the leading indicator of user-visible latency even when average utilization is moderate.
</Recap>
