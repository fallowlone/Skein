---
concepts:
- otel-auto-instrumentation
- java-agent
- manual-spans
- platform-wrapper
- span-attributes
- span-events
deepensInto: []
estMin: 11
lang: en
lessonType: topic
level: middle
order: 3
prereqs:
- observability/03-otel/02-signals-and-wire-format
slug: 03-auto-and-manual-instrumentation
sources:
- https://opentelemetry.io/docs/concepts/components/
- https://opentelemetry.io/docs/platforms/kubernetes/operator/automatic/
- https://opentelemetry.io/docs/specs/otel/overview/
spiral:
- latency
status: ready
summary: Auto-instrumentation covers 70-80% of production traffic for free — HTTP,
  DB, queues. The remaining 20-30% is manual spans with business attributes, and that
  is where the real diagnostic value lives.
title: 'Auto-instrumentation and manual spans: the 80/20 of OTel'
track: observability
unit: 03-otel
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";

<Hook>
Auto-instrumentation tells you "this HTTP request took 800 ms and made 5 DB queries." Manual instrumentation tells you "the slow part was the fraud-check loop, which fanned out to 12 calls on the rules engine, of which 3 timed out." Both are necessary. Neither alone is enough.
</Hook>

<Crux>Auto-instrumentation gives breadth free. Manual spans give depth for business questions. Platform owns the wrapper; app teams own spans.</Crux>

<Explanation>

## Auto-instrumentation: the 70-80% you get for free

Most production traffic flows through a small set of frameworks — HTTP servers, HTTP clients, database drivers, message queue clients — that OTel's contrib libraries instrument out of the box.

**Java**: The OTel Java Agent (`-javaagent` JAR) rewrites bytecode at JVM startup and instruments ~200 libraries automatically — no code changes required. Add one `-javaagent` flag to the JVM arguments, set `OTEL_SERVICE_NAME` and `OTEL_EXPORTER_OTLP_ENDPOINT` env vars, and every HTTP request, JDBC query, and Kafka message is traced.

**Python**: `opentelemetry-instrument` wraps the process and monkey-patches common libraries (Flask, Django, FastAPI, SQLAlchemy, Redis, Celery, etc.).

**Node.js**: `@opentelemetry/sdk-node` plus a set of instrumentations (express, http, pg, redis, kafkajs) registered before any other `require` hook framework methods automatically.

**Go**: Auto-instrumentation is less mature because Go has no monkey-patching. eBPF-based auto-instrumentation (using uprobes) is the emerging approach; the Go OTel contrib libraries require explicit middleware setup per framework.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Language</th>
        <th class="border border-border px-3 py-2 text-left">Auto-instrumentation mechanism</th>
        <th class="border border-border px-3 py-2 text-left">Coverage</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Java</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">-javaagent JAR, bytecode rewriting at startup</td>
        <td class="border border-border px-3 py-2">~200 libraries, HTTP + DB + queues + caches</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Python</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">opentelemetry-instrument, monkey-patching</td>
        <td class="border border-border px-3 py-2">Flask, Django, FastAPI, SQLAlchemy, Redis, Celery</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Node.js</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">require hook before other imports</td>
        <td class="border border-border px-3 py-2">express, http, pg, redis, kafkajs, grpc</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Go</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">Middleware setup per framework (no monkey-patching)</td>
        <td class="border border-border px-3 py-2">net/http, gRPC, database/sql — requires manual wiring</td>
      </tr>
    </tbody>
  </table>
</div>

The 70-80% is the easy part: HTTP routes, DB queries, cache calls, queue interactions. Auto-instrumentation tells you *what* happened. Manual instrumentation tells you *why*.

## Manual instrumentation: the value bridge

The remaining 20-30% — business-specific spans, custom attributes, and cross-cutting concerns — is where the real diagnostic value lives, because it is where the business questions live.

**Pattern**: open a span around a business operation, attach attributes that describe the operation, emit span events for noteworthy moments.

```typescript
const span = tracer.startSpan("fraud.check", {
  attributes: {
    "customer.segment": customer.segment,
    "order.value_cents": order.valueCents,
    "feature_flag.fraud_model": flags.fraudModel,
  },
});

try {
  for (const rule of rules) {
    const result = await rules.engine.evaluate(rule, order);
    span.addEvent("rule.evaluated", {
      "rule.id": rule.id,
      "rule.result": result.outcome,
      "rule.latency_ms": result.latencyMs,
    });
  }
} catch (err) {
  span.recordException(err);
  span.setStatus({ code: SpanStatusCode.ERROR });
} finally {
  span.end();
}
```

This span tells you customer segment, order value, the feature flag state of the fraud model, and the result of every rule evaluation — data that auto-instrumentation cannot see because it does not know your business domain.

## The platform wrapper pattern

Manual instrumentation is the same pattern in every service, so platform teams publish a thin helper library that wraps the OTel API with org-specific conventions:

- **Typed business attributes** — pre-defined attribute names for common concepts (customer segment, feature flag state, batch size) so teams cannot misspell them.
- **Redaction helpers** — strip PII before attaching to spans (email → hash, SSN → masked).
- **Span event helpers** — typed helpers for common events (cache miss, retry, fallback).
- **Resource pre-configuration** — service name, version, and deployment environment from a standardised env var set.

New services import the wrapper; CI lint rejects raw OTel SDK usage in new code (to prevent ungoverned attribute naming). Override hooks let teams add service-specific attributes without modifying the wrapper.

<Inset kind="why" lang="en">
Why not one span per function? Every span has overhead: the SDK allocates the span struct, records attributes, runs the sampler, and when the span ends, runs the batch processor, serializes to OTLP, and possibly queues for the exporter. At ~1-5 μs per span, 100 spans per request at 10k RPS is 1-5 ms of CPU overhead per core per second. More importantly, a flat list of 100 function-level spans is harder to read than 8 spans at the right abstraction level. The guideline: one span per service-boundary crossing plus one per significant subsystem call (one span for the fraud check, one for the DB query inside it, not one span per method inside the fraud check).
</Inset>

<Quiz
  id="03-auto-manual-quiz-1"
  lessonSlug="03-auto-and-manual-instrumentation"
  lang="en"
  question="What does auto-instrumentation cover that manual instrumentation cannot, and what does manual instrumentation cover that auto-instrumentation cannot?"
  choices={[
    { label: "Auto covers business logic; manual covers network calls", misconception: "The opposite: auto covers standard framework calls (network, DB); manual covers business logic." },
    { label: "Auto covers standard framework calls (HTTP, DB, queues) for free; manual covers business-domain spans with typed attributes that explain why something was slow or failed", correct: true },
    { label: "Auto covers tracing; manual covers metrics", misconception: "Both cover tracing; the distinction is breadth vs depth, not signal type." },
    { label: "Auto is for production; manual is for development only", misconception: "Both run in production; manual spans are especially valuable in production for incident diagnosis." },
  ]}
/>

<Quiz
  id="03-auto-manual-quiz-2"
  lessonSlug="03-auto-and-manual-instrumentation"
  lang="en"
  question="Why does a platform team publish a per-language wrapper around the OTel SDK instead of letting application teams call the OTel SDK directly?"
  choices={[
    { label: "The OTel SDK API is too complex for application engineers", misconception: "The OTel SDK API is straightforward; complexity is not the reason." },
    { label: "To enforce Semantic Conventions, redaction, typed attribute names, and pre-configured resource attributes — preventing inconsistent naming drift that breaks cross-team dashboards and compliance", correct: true },
    { label: "To reduce OTel SDK version dependency conflicts", misconception: "Dependency management is a concern but is not the primary reason for the wrapper pattern." },
    { label: "The SDK cannot be used without a wrapper in production", misconception: "The SDK works standalone; the wrapper adds governance, not capability." },
  ]}
/>

<DragOrder
  id="03-auto-manual-drag-1"
  lessonSlug="03-auto-and-manual-instrumentation"
  lang="en"
  prompt="Order the steps to add a well-instrumented business span to a service:"
  items={[
    { id: "a", label: "Open a span with a descriptive operation name (e.g., fraud.check)" },
    { id: "b", label: "Attach structured attributes: customer segment, order value, feature flag state" },
    { id: "c", label: "Execute the business logic" },
    { id: "d", label: "Emit span events for significant moments: cache miss, retry, rule evaluation result" },
    { id: "e", label: "On error: call span.recordException(err) and set status to ERROR" },
    { id: "f", label: "Always call span.end() in a finally block to ensure the span is flushed" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f"]}
/>

</Explanation>

<KeyTakeaway>Auto-instrumentation covers 70-80% of traces for free (HTTP, DB, queues) via bytecode rewriting or monkey-patching. The remaining 20-30% — business spans with typed attributes — is where incident diagnosis happens. The platform team owns the SDK wrapper (Semantic Conventions, redaction, resource config); application teams own the business spans and attributes.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-auto-manual-retrieval"
  lang="en"
  questions={[
    { q: "Why is the OTel Java Agent the most powerful auto-instrumentation option across languages?", a: "The Java Agent uses bytecode rewriting at JVM startup — it modifies the loaded bytecode of ~200 libraries before any application code runs. This requires no code changes and no framework middleware setup. Python and Node.js use monkey-patching (runtime method replacement), which requires the instrumentation to be registered before the library is used. Go has neither bytecode rewriting nor monkey-patching, so auto-instrumentation requires explicit middleware calls per framework — much more friction." },
    { q: "What is the right granularity for manual spans, and why?", a: "One span per service-boundary crossing, plus one per significant subsystem call. Not one span per function — function-level spans produce unreadable 100-span traces and add measurable CPU overhead (~1-5 μs each at high RPS). The right level is the business operation level: one span for the fraud check overall, one for the DB query it makes, not one for each method inside the fraud check. The goal is answering 'which subsystem is slow and why' — not mapping the call graph." },
    { q: "What does the platform wrapper pattern solve, and what does it cost?", a: "It enforces Semantic Conventions (typed, spelled-correctly attribute names), redaction (PII stripped before hitting the span), span event helpers, and pre-configured resource attributes across all services. Without it, each team invents their own attribute names — http_route vs http.route vs httpRoute — and cross-team dashboards break silently. The cost: 1-2 weeks of initial platform work per language, plus ongoing maintenance when OTel SDK versions or org conventions change." },
  ]}
/>

<Recap lang="en">
Auto-instrumentation instruments 70-80% of production traffic for free: the OTel Java Agent rewrites bytecode to cover ~200 libraries; Python's opentelemetry-instrument monkey-patches common frameworks; Node.js uses a require-hook before other imports. Go requires manual middleware setup (no monkey-patching). The remaining 20-30% is manual spans: open a span around a business operation, attach typed attributes (customer segment, feature flag state, batch size), emit span events for noteworthy moments, record exceptions with span.recordException(). The overhead is ~1-5 μs per span — keep granularity at the business-operation level, not the function level. Platform teams publish a per-language wrapper that enforces Semantic Conventions, redaction, and resource configuration, so application teams can add business spans without inventing new attribute names.
</Recap>
