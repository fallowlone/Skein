---
concepts:
- otel-signals
- otlp-grpc
- otlp-http
- semantic-conventions
- otel-resource
- trace-context
deepensInto: []
estMin: 12
lang: en
lessonType: topic
level: middle
order: 2
prereqs:
- observability/03-otel/01-what-is-otel
slug: 02-signals-and-wire-format
sources:
- https://opentelemetry.io/docs/specs/otlp/
- https://opentelemetry.io/docs/concepts/semantic-conventions/
- https://opentelemetry.io/docs/specs/otel/overview/
spiral:
- encapsulation
- latency
status: ready
summary: Traces, metrics, and logs each have their own SDK pipeline and OTLP message
  shape but share the Resource and trace context. OTLP/gRPC is 50-70% smaller than
  JSON and is the vendor-neutral edge contract.
title: OTel signals, Semantic Conventions, and the OTLP wire format
track: observability
unit: 03-otel
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import otlpTransport from "./infographics/otlp-transport.svg?raw";

<Hook>
Your metrics backend runs on vendor-A. Your logs go to vendor-B. Your traces go to vendor-C. A request fails — and no two backends agree on the field name for the service. Cross-signal correlation is a spreadsheet exercise. OTel Semantic Conventions are the fix.
</Hook>

<Crux>OTel's three signals share one Resource and one trace context — the join keys for cross-signal correlation without manual field mapping.</Crux>

<Explanation>

## Three signals, one data model

By the end of this lesson you will know why correlating a metrics alert to a failing trace is a one-click operation in OTel — and why it is a spreadsheet nightmare without Semantic Conventions.

OTel covers three signal types in stable form. Each has its own API surface, SDK pipeline, and OTLP message shape, but they share two concepts: **Resource** (per-emitter attributes — `service.name`, `host.name`, `cloud.region`, set once at service start) and **Trace Context** (`trace_id`, `span_id`) that lets you jump from a log line to the corresponding trace.

**Traces** — GA since 2021. The workhorse. Every backend speaks it. A trace is a tree of spans, each with a start time, duration, status, and attributes. The join key across services is the `trace_id` propagated via the W3C `traceparent` header.

**Metrics** — GA since 2023. The API supports counter, gauge, histogram, and exponential histogram (the new default for high-resolution latency, replacing fixed buckets). Metrics aggregate inside the SDK before export; the wire format carries resource-metrics batches.

**Logs** — API stable late 2023, SDKs stable late 2024. Bridge libraries (the "OTel log bridge" for slf4j, structlog, pino) let existing code emit OTel logs without rewrites. The wire format carries resource-logs batches with the same `trace_id`/`span_id` fields as traces.

**Profiles** — experimental in late 2024/2025, GA expected in 2026 as a fourth signal.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Signal</th>
        <th class="border border-border px-3 py-2 text-left">GA</th>
        <th class="border border-border px-3 py-2 text-left">OTLP message</th>
        <th class="border border-border px-3 py-2 text-left">Cross-signal join key</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Traces</td>
        <td class="border border-border px-3 py-2">2021</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">ExportTraceServiceRequest</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">trace_id</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Metrics</td>
        <td class="border border-border px-3 py-2">2023</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">ExportMetricsServiceRequest</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">service.name via Resource</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Logs</td>
        <td class="border border-border px-3 py-2">API 2023 / SDK 2024</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">ExportLogsServiceRequest</td>
        <td class="border border-border px-3 py-2 font-mono text-xs">trace_id, span_id</td>
      </tr>
    </tbody>
  </table>
</div>

## Semantic Conventions: the shared vocabulary

Ask yourself: if every team in a 50-service fleet names the HTTP route attribute differently, how many cross-team queries will silently return zero results? That is the problem Semantic Conventions solve.

Independent of the four-piece architecture, OTel publishes **Semantic Conventions** — a normative set of attribute names for common operations.

- **Resource**: `service.name`, `service.version`, `deployment.environment`, `host.name`, `cloud.region`
- **HTTP**: `http.request.method`, `http.route`, `http.response.status_code`, `url.full`
- **Database**: `db.system`, `db.operation`, `db.namespace`
- **Messaging**: `messaging.system`, `messaging.destination.name`, `messaging.operation`

These are the join keys that make metrics, logs, and traces compose. Adopting Semantic Conventions is the highest-leverage discipline in OTel — every backend's auto-derived dashboards and every cross-team query depends on consistent population. Mature platform teams ship a per-language wrapper around the OTel SDK that pre-configures attribute extraction so it cannot be forgotten.

## OTLP: the wire format that ties everything

OTLP is a protobuf-defined wire protocol with two transports:

- **OTLP/gRPC** — binary protobuf over HTTP/2. Preferred. ~50-70% smaller payload than JSON, ~2-5× faster to encode. Supports streaming.
- **OTLP/HTTP** — protobuf or JSON over HTTP/1.1. Exists for environments that block gRPC (some firewalls) or for browser-side telemetry.

<Infographic
  svg={otlpTransport}
  label="Side-by-side comparison of the two OTLP transports: OTLP/gRPC (binary protobuf over HTTP/2, 50-70% smaller than JSON, 2-5x faster to encode, supports streaming, the preferred default) versus OTLP/HTTP with JSON (larger and slower, no streaming, used only when gRPC is blocked or for browser telemetry)."
  caption="gRPC is the production default; HTTP/JSON is the fallback for firewalled or browser environments — both carry the same OTLP messages, so the choice is transport, not vendor lock-in."
  hue="var(--d-infra)"
/>

The three OTLP service messages are `ExportTraceServiceRequest`, `ExportMetricsServiceRequest`, and `ExportLogsServiceRequest`. Every OTel-compatible backend exposes an OTLP endpoint. The wire format has been stable for years and evolves backward-compatibly.

**The vendor-neutrality contract**: if your edge (the application or its sidecar) emits OTLP, you are vendor-neutral. If your edge emits a proprietary format, you are not — even if a gateway later converts.

<StackDiagram
  label="One OTLP envelope, three signal payloads"
  hue="var(--d-systems)"
  layers={[
    { label: "Resource + Attributes", note: "service.name, host.name, trace_id" },
    { label: "Traces payload", note: "ExportTraceServiceRequest" },
    { label: "Metrics payload", note: "ExportMetricsServiceRequest" },
    { label: "Logs payload", note: "ExportLogsServiceRequest" },
    { label: "OTLP encoding", note: "protobuf" },
    { label: "Transport", note: "gRPC / HTTP" },
  ]}
  caption="All three signals wrap the same Resource + Attributes envelope, then serialize as protobuf over OTLP/gRPC (or HTTP) — the shared keys are what make cross-signal correlation work."
/>

<Inset kind="why" lang="en">
Why exponential histograms? Fixed-bucket histograms (e.g., latency buckets at 0-10ms, 10-50ms, 50-100ms, 100-500ms, 500ms+) require you to pick bucket boundaries at instrumentation time. If your service's latency distribution changes — say a slow path suddenly dominates the 1-5s range — you need to redeploy with different buckets. Exponential histograms (also called "native histograms" in Prometheus) use a logarithmic scale and dynamically adjust resolution. They give you full latency distribution without guessing bucket boundaries upfront. The OTel SDK defaults to exponential histograms for new counters.
</Inset>

<Quiz
  id="02-signals-quiz-1"
  lessonSlug="02-signals-and-wire-format"
  lang="en"
  question="Which OTLP transport is preferred for service-to-Collector communication, and why?"
  choices={[
    { label: "OTLP/HTTP — more widely supported, works everywhere", misconception: "OTLP/HTTP is the fallback for constrained environments, not the preferred choice for service-to-Collector." },
    { label: "OTLP/gRPC — binary protobuf over HTTP/2 is 50-70% smaller and 2-5× faster to encode than JSON, and supports streaming", correct: true },
    { label: "OTLP/JSON — human-readable and easier to debug", misconception: "OTLP/JSON over HTTP is the slowest and largest of the three; it exists for debugging and browser use, not production throughput." },
    { label: "Vendor-specific binary protocol — lowest overhead per vendor", misconception: "Using a vendor-specific protocol reintroduces lock-in; OTLP/gRPC has nearly the same overhead with full portability." },
  ]}
/>

<Quiz
  id="02-signals-quiz-2"
  lessonSlug="02-signals-and-wire-format"
  lang="en"
  question="A team uses `http_route` (underscore) instead of `http.route` (dot) for the HTTP route attribute across their services. What breaks?"
  choices={[
    { label: "The OTLP export fails because the field name is invalid", misconception: "OTLP accepts arbitrary attribute names; invalid names do not block export." },
    { label: "Nothing breaks immediately — but cross-team dashboards and backend auto-discovery of HTTP endpoints fail because the non-Semantic-Convention name is not recognized", correct: true },
    { label: "The OTel SDK raises a validation error at instrumentation time", misconception: "The SDK does not enforce Semantic Conventions at write time." },
    { label: "Only the Collector's transform processor rejects the non-standard name", misconception: "The Collector's transform processor can rename fields, but it does not reject non-standard names by default." },
  ]}
/>

<DragOrder
  id="02-signals-drag-1"
  lessonSlug="02-signals-and-wire-format"
  lang="en"
  prompt="Order the OTel signals from most mature to least mature (by GA date):"
  items={[
    { id: "a", label: "Traces (GA 2021)" },
    { id: "b", label: "Metrics (GA 2023)" },
    { id: "c", label: "Logs API (GA late 2023), SDK (GA late 2024)" },
    { id: "d", label: "Profiles (experimental, GA expected 2026)" },
  ]}
  correctOrder={["a", "b", "c", "d"]}
/>

</Explanation>

<KeyTakeaway>OTel's three signals (traces GA 2021, metrics GA 2023, logs API/SDK GA 2023/2024) share one Resource and one trace context — the join keys for cross-signal correlation. Semantic Conventions are the shared attribute vocabulary that makes dashboards compose. OTLP/gRPC is 50-70% smaller than JSON and is the vendor-neutrality contract: emit it at the edge and backends become config.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-signals-retrieval"
  lang="en"
  questions={[
    { q: "What two shared concepts make cross-signal correlation possible in OTel?", a: "Resource (per-emitter attributes like service.name, host.name, cloud.region set once at service start) and Trace Context (trace_id, span_id) that let you jump from a log line or metric exemplar to the corresponding trace. Without these shared join keys, correlating a metrics alert to a trace requires timestamp-guessing across backends." },
    { q: "Why are Semantic Conventions the highest-leverage discipline in OTel adoption?", a: "Semantic Conventions are normative attribute names (http.route, db.system, service.name, etc.) that every backend's auto-discovery and every cross-team query depends on. A service that uses http_route instead of http.route is invisible to every dashboard and alert that uses the standard name. The SDK and Collector do not enforce them, so governance — usually a per-language wrapper that pre-configures extraction — is the only way to ensure consistency at scale." },
    { q: "What is the vendor-neutrality contract, and where exactly does it live?", a: "The contract is: the application edge emits OTLP. As long as the SDK in the application emits OTLP (over gRPC or HTTP) to the next hop (Collector or backend), the application is portable — the rest of the stack can be swapped without touching application code. If the application emits a vendor-proprietary protocol (dd-trace wire format, New Relic agent wire format), the edge is not OTLP and portability is lost at the source, regardless of what happens downstream." },
  ]}
/>

<Recap lang="en">
OTel defines three stable signals — traces (GA 2021), metrics (GA 2023), logs (API stable late 2023, SDK late 2024) — each with its own SDK pipeline and OTLP message type, but all sharing the Resource concept (per-emitter attributes like service.name) and Trace Context (trace_id, span_id) for cross-signal correlation. Semantic Conventions give every attribute a normative name so dashboards, alerts, and cross-team queries compose without manual field mapping. The wire format is OTLP: protobuf over gRPC (preferred, 50-70% smaller than JSON, 2-5× faster) or HTTP (for firewalled or browser environments). Emit OTLP at the application edge and every backend choice becomes a Collector config change. Now when you see a metrics alert fire, your instinct should be to reach for the trace_id in the exemplar — OTel's shared Resource and Trace Context make that jump instant rather than a manual timestamp hunt.
</Recap>
