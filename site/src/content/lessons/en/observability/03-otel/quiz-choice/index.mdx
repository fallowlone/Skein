---
concepts:
- otel-architecture
- semantic-conventions
- collector-pipeline
- sampling-strategy
- vendor-neutrality
- collector-operations
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://opentelemetry.io/docs/concepts/components/
- https://opentelemetry.io/docs/collector/
- https://opentelemetry.io/docs/concepts/semantic-conventions/
status: ready
summary: "Multiple-choice synthesis across the OTel unit — the four-piece architecture, signals and OTLP, Collector pipeline, sampling, vendor-neutrality, and operations."
title: 'OTel: multiple-choice review'
track: observability
unit: 03-otel
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each mirrors a real architecture or incident decision — not a definition to recite, but where to make a change and why it lands there.
</Hook>

<Goal lang="en">
Confirm you can connect the four pieces — API, SDK, Collector, OTLP — to the decisions they govern: where portability lives, what makes signals correlate, how sampling and the Collector pipeline compose, and how the whole thing fails in production.
</Goal>

<Quiz
  id="03-otel-choice-1"
  lessonSlug="03-otel"
  lang="en"
  question="A platform team claims full OTel vendor-neutrality. Two facts surface in review: application code imports the vendor's dd-trace SDK directly, and a Collector sits in front of every service converting to OTLP. Are they neutral, and why?"
  choices={[
    { label: "No. Neutrality is defined at the application edge: the SDK in the app must emit OTLP. Here the edge is vendor-format, so the migration cost lives in every service's code — the downstream Collector conversion does not undo that.", correct: true },
    { label: "Yes — a Collector in the path normalises everything to OTLP, so the backend is swappable.", misconception: "The Collector can reshape data, but the migration cost was already incurred upstream: every service imports the vendor SDK. You would rewrite instrumentation in every service to actually move, so the edge — not the Collector — sets neutrality." },
    { label: "Yes — using any Collector at all makes a stack vendor-neutral by definition.", misconception: "A Collector is necessary infrastructure but not sufficient for neutrality. If the application edge is not OTLP, the lock-in lives in application code regardless of what the Collector does." },
    { label: "No — using a vendor Collector distro is itself full lock-in.", misconception: "Vendor distros that accept and emit OTLP are acceptable; the silent erosion is vendor SDKs in app code and proprietary Collector-only processors, not the distro itself." },
  ]}
/>

<Quiz
  id="03-otel-choice-2"
  lessonSlug="03-otel"
  lang="en"
  question="A team needs tail sampling that keeps 100% of error traces. They run a single agent Collector as a DaemonSet (one per node) exporting straight to the backend. Why does this not work, and what is the minimal fix?"
  choices={[
    { label: "Tail sampling needs the full trace, but each DaemonSet agent only sees the spans from its own node — a distributed trace is split across nodes, so no agent owns the complete trace. Fix: add a gateway tier and route by trace_id (loadbalancing exporter) so all spans of a trace land on one replica.", correct: true },
    { label: "DaemonSet agents cannot run the tail_sampling processor at all; only gateways have it compiled in.", misconception: "The processor binary is the same; the problem is data locality. An agent only receives the local node's spans, so it never has the full trace context tail sampling requires — not a missing feature." },
    { label: "Head sampling at the SDK already keeps errors, so tail sampling is redundant.", misconception: "Head sampling decides at root-span start, before any error has occurred — it is blind to errors. Only tail sampling, seeing the completed trace, can keep 100% of error traces." },
    { label: "Raise the agent's num_traces buffer so it can hold every trace.", misconception: "Buffer size is irrelevant when the agent never receives the other nodes' spans of a trace. The issue is that the full trace is not co-located, not that the buffer is too small." },
  ]}
/>

<Quiz
  id="03-otel-choice-3"
  lessonSlug="03-otel"
  lang="en"
  question="Two services emit HTTP telemetry. Service A tags the route as http.route; service B tags it as http_route. OTLP export succeeds for both and no SDK error fires. What actually breaks, and where?"
  choices={[
    { label: "Nothing fails at export — OTLP carries arbitrary attribute names. But every cross-team dashboard, alert, and backend auto-discovery keyed on the Semantic Convention name http.route silently omits service B. The break is at query time, fleet-wide, and invisible until someone notices B is missing.", correct: true },
    { label: "The OTLP exporter rejects http_route as an invalid attribute name and drops service B's spans.", misconception: "OTLP places no constraints on attribute names; both export fine. Semantic Conventions are a naming agreement enforced by governance, not by the wire protocol." },
    { label: "The SDK raises a validation error at instrumentation time for the non-standard name.", misconception: "Neither the SDK nor the Collector enforces Semantic Conventions at write time. That is exactly why the drift is silent and why a platform wrapper plus CI lint is the only durable fix." },
    { label: "Both services are fine because the Collector's transform processor auto-corrects field names.", misconception: "transform/attributes processors can rename fields, but only if you write a rule for it — there is no automatic correction. By default the non-standard name flows straight through." },
  ]}
/>

<Quiz
  id="03-otel-choice-4"
  lessonSlug="03-otel"
  lang="en"
  question="In a production traces pipeline you have memory_limiter, batch, and tail_sampling. Why does memory_limiter belong first, and what is the consequence of putting tail_sampling first?"
  choices={[
    { label: "memory_limiter drops records cheaply under RAM pressure before any expensive processing runs. Put it after tail_sampling and the Collector spends CPU and RAM buffering traces for the decision window before the limiter can shed load — defeating its purpose and risking OOM, which loses all buffered traces at once.", correct: true },
    { label: "Order does not matter — the Collector reorders processors internally for efficiency.", misconception: "The Collector runs processors in the exact order you declare them. Ordering is a correctness and protection decision, not a hint." },
    { label: "tail_sampling first is correct because it reduces volume before the limiter has to act.", misconception: "tail_sampling does not reduce in-flight memory — it buffers every span for 30-60s before deciding. Running it before memory_limiter means the limiter cannot protect against the very buffering that causes the OOM." },
    { label: "batch must be first; memory_limiter only works after spans are batched.", misconception: "memory_limiter checks process RAM independently of batching and must come first so it can refuse new records before the costlier processors consume more memory." },
  ]}
/>

<Quiz
  id="03-otel-choice-5"
  lessonSlug="03-otel"
  lang="en"
  question="A metrics backend's cardinality explodes after a fleet rollout of OTel auto-instrumentation. The new dimension driving it is the HTTP client's url.full attribute. What is the root cause and the correct fix?"
  choices={[
    { label: "url.full carries the raw URL including path IDs and query strings, so every distinct request becomes a new time series — unbounded cardinality. Fix: use the templated http.route (e.g. /users/{id}) instead of url.full, or strip query strings via an attributes processor at the Collector.", correct: true },
    { label: "The metrics backend is undersized — add storage and the problem resolves.", misconception: "Scaling storage treats the symptom and the cost keeps climbing. The cause is an unbounded-cardinality attribute; the durable fix is to template or drop it, not to absorb infinite series." },
    { label: "Tail sampling is keeping too many traces and inflating metric series.", misconception: "Tail sampling acts on traces, not on metric series cardinality. The exploding dimension is a per-request attribute on metrics — url.full — independent of sampling." },
    { label: "Switch from exponential to fixed-bucket histograms to cap cardinality.", misconception: "Histogram type controls latency-bucket resolution, not label cardinality. The cardinality is in the url.full label dimension, which neither histogram type changes." },
  ]}
/>

<Quiz
  id="03-otel-choice-6"
  lessonSlug="03-otel"
  lang="en"
  question="Your single OTel Collector gateway crashes during an incident. The on-call dashboards stay green and no alerts fire, yet user-facing errors are spiking. What is the lesson, and the structural fix?"
  choices={[
    { label: "The Collector is critical-path observability infrastructure: when it fails, visibility fails — and dashboards that depend on it lie by omission. Fix: 3+ gateway replicas, a persistent disk-backed queue, health probes, and meta-monitoring (scrape the Collector's own /metrics, alert on dropped/refused/failed spans) so a Collector failure is itself observable.", correct: true },
    { label: "Green dashboards prove the system is healthy; the user errors are unrelated.", misconception: "The dashboards are green because their data source — the Collector — is down, not because the system is healthy. Absence of telemetry is being read as absence of problems." },
    { label: "Add more application-side logging so errors show up without the Collector.", misconception: "More logging still flows through the same failed Collector. The fix is making the Collector itself highly available and self-monitored, not adding upstream volume to a broken pipe." },
    { label: "The SDK should export directly to the backend to bypass the Collector entirely.", misconception: "Direct export loses buffering, central policy, and uniform processing, and creates a new single point of failure per service. The answer is to make the Collector tier HA and observable, not to remove it." },
  ]}
/>

<Recap lang="en">
The through-line: portability is decided at the application edge (emit OTLP there or nowhere), correlation is decided by Semantic Conventions (consistent names or invisible services), and the Collector is where policy lives — processor order protects it, sampling composes head-cheap with tail-curated, and the gateway must be HA and self-monitored because when the Collector fails, your visibility fails silently. Every production failure in this unit — eroded neutrality, split traces, naming drift, OOM, cardinality leaks, blind dashboards — resolves back to one of those four pieces.
</Recap>
