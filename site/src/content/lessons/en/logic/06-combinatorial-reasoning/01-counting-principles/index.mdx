---
concepts:
- logic-counting
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: zero
order: 1
prereqs: []
slug: "01-counting-principles"
sources:
- https://en.wikipedia.org/wiki/Rule_of_product
- https://en.wikipedia.org/wiki/Rule_of_sum
- https://en.wikipedia.org/wiki/Permutation
- https://en.wikipedia.org/wiki/Combination
spiral: []
status: ready
summary: "All counting stands on two laws: independent sequential choices multiply (4 shirts × 3 pants = 12 outfits), disjoint cases add (3 pastas + 4 pizzas = 7 dinners). Factorials, permutations, combinations and the size of your CI matrix are these two rules applied over and over."
title: "Counting principles: multiply the and-thens, add the eithers"
track: "logic"
unit: "06-combinatorial-reasoning"
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A reviewer once asked an innocent question about a CI config: the matrix listed 3 browsers, 4 operating systems, and 2 locales — so how many jobs is that per push? Half the room said 9, adding the numbers up. The pipeline ran 24. Then mobile Safari joined the browser list and the count quietly became 32; a third locale pushed it to 48; a `node: [18, 20]` axis doubled everything again to 96. Nobody ever decided to burn an hour of compute per commit — the matrix multiplied while everyone was merely adding one small thing at a time. The gap between the guessed 9 and the real 24 is not a CI quirk; it is the gap between the two laws that govern all counting. Knowing which law applies — and spotting the moment neither does — is a skill you will reuse on test matrices, password policies, nested loops, and feature-flag explosions for the rest of your career.
</Hook>

<Crux>Independent sequential choices multiply; disjoint cases add. Every counting formula — factorials, permutations — is these two laws repeated.</Crux>

<Explanation>

## And-then choices multiply

You own 4 shirts and 3 pairs of pants. How many outfits? Picture the **choice tree**: from the root grow 4 branches, one per shirt; from *each* of those grow 3 branches, one per pair of pants. Count the leaves: 4 groups of 3, so 12. That is the **multiplication rule** (the rule of product): when a task splits into sequential stages — make this choice, *and then* that one — and the *number* of options at each stage does not depend on what was chosen earlier, the total is the product of the per-stage counts.

The fine print earns its keep: the options themselves may change with earlier choices — only their *count* must not. Choosing 2 distinct letters for a code gives 26 options for the first, then 25 for the second (whatever you picked is gone; the leftovers differ each time, but there are always 25 of them) — 26 · 25 = 650 ordered pairs. The rule still applies, because every branch of the tree fans out into the same number of sub-branches.

Code already speaks this rule fluently — every nested loop is a multiplication:

```js
const browsers = ["chrome", "firefox", "safari"];        // 3 options
const oses = ["ubuntu", "macos", "windows", "alpine"];   // 4 options
const locales = ["en", "ru"];                            // 2 options

let jobs = 0;
for (const b of browsers)      // pick a browser, AND THEN…
  for (const o of oses)        //   …pick an OS, AND THEN…
    for (const l of locales)   //     …pick a locale
      jobs++;

console.log(jobs);             // 3 · 4 · 2 = 24 — one job per leaf of the tree
```

The same rule prices a password space. An 8-character lowercase password is 8 sequential choices from 26 letters: `26^8`, about 2.1 × 10¹¹ strings. Widen the alphabet to 62 symbols (upper, lower, digits) and stretch to 12 characters: `62^12`, about 3.2 × 10²¹ — ten billion times more. Each added *position* multiplies the whole space by the alphabet size again, which is why password length beats clever character substitutions: length adds factors, substitutions only fatten one factor slightly.

<Inset kind="why" lang="en">
Why does the rule insist on independence of counts? Because one dependent stage breaks the clean product. A real CI matrix usually carries `exclude:` entries — say, safari on alpine is excluded. Now the OS stage offers 4 options for two of the browsers but only 3 for the third: the choice tree is lopsided, and no single product describes it. You size it as the full product minus the excluded leaves: 3 · 4 · 2 − 1 · 1 · 2 = 22 jobs. Multiply first, then subtract the dead branches — that is how every real matrix with exclusions is counted, and forgetting the correction is how CI bills surprise teams in both directions.
</Inset>

## Either-or cases add — and the overlap tax

Dinner is one dish. The menu has 3 pastas and 4 pizzas. You are not choosing a pasta *and then* a pizza — you choose one dish from two **disjoint cases** (no dish is both a pasta and a pizza), so the counts add: 3 + 4 = 7 possible dinners. That is the **addition rule** (the rule of sum): when the outcomes split into cases with no element in common, the total is the sum of the case counts.

The load-bearing word is *disjoint*. Count files that are large *or* recently modified: 80 large plus 50 recent is not 130 when 20 files are both — those 20 were counted twice, once in each case. Subtract the double count once: 80 + 50 − 20 = 110. You met this correction in unit 03 as inclusion–exclusion on sets: the size of a union is the sum of the sizes minus the size of the intersection. The addition rule is simply the happy special case where the intersection is empty.

Real problems chain both rules. A meal deal is one dish *and then* one of 2 drinks: the dish count comes from addition over disjoint cases (3 + 4 = 7), and the drink is an independent next stage, so the deals multiply: 7 · 2 = 14.

<Quiz
  id="01-counting-principles-quiz-1"
  lessonSlug="01-counting-principles"
  lang="en"
  question="A menu offers 3 pastas and 4 pizzas; a meal deal is one dish plus one of 2 drinks. How many distinct meal deals exist?"
  choices={[
    { label: "(3 + 4) · 2 = 14 — pasta and pizza are disjoint cases of one dish choice, so they add; the drink is an independent next stage, so it multiplies", correct: true },
    { label: "3 · 4 · 2 = 24 — three choices means three factors", misconception: "Multiplication is for and-then stages. You never pick a pasta AND THEN a pizza — you pick one dish, and the two kinds are either-or cases of that single choice. Cases add (3 + 4 = 7); only then does the genuinely sequential drink choice multiply the result." },
    { label: "3 + 4 + 2 = 9 — just add up all the options", misconception: "The drink is not a third case of the dish choice — you take a dish AND a drink, not a dish OR a drink. Sequential independent choices multiply, so the 7 dishes pair with each of 2 drinks: 7 · 2 = 14. Adding everything treats two different laws as one." },
    { label: "It cannot be determined without listing every combination", misconception: "The two rules exist precisely so you never enumerate. The structure of the choice — either-or cases inside, an and-then stage outside — fully determines the count: (3 + 4) · 2 = 14. Enumeration would just redraw the same choice tree leaf by leaf." },
  ]}
/>

## Permutations and combinations: built, not memorized

How many ways can n distinct items stand in a line? Apply the multiplication rule n times: n choices for the first slot, *and then* n − 1 for the second (one item is used up), *and then* n − 2, down to 1. The product n · (n−1) · … · 1 is written **n!** and pronounced "n factorial". Five services to deploy in sequence: 5! = 120 possible orders — which is why "just try every deploy order" stops being a plan around n = 5.

Stop the product early and you get ordered selections: a podium (gold, silver, bronze) chosen from 10 runners is 10 · 9 · 8 = 720 — three stages, then stop. This is the **permutation count** of 3 out of 10, and order genuinely matters: gold-Ann-silver-Bo is a different podium from gold-Bo-silver-Ann.

Now the one genuinely new move. Choose an unordered *team* of 3 from 10 people. The 720 ordered selections overcount it: the team of Ann, Bo and Cy appears once for every way to order those three people — and orderings of 3 items number 3! = 6. Every team is counted exactly 6 times, no exceptions, so the number of teams is 720 / 6 = 120. That division argument — group the ordered selections by which set they contain, observe that every group has exactly k! members, divide — is the entire theory of **combinations**. C(n, k) is not a formula to memorize; it is "count ordered with the product rule, then divide out the orderings you never cared about."

Here is the misconception this lesson dismantles: *order always matters, so the bigger permutation number is the safe default.* A lottery draw of 6 numbers from 49 is a set — the machine's draw order is noise nobody records. Count it with permutations and you get 49 · 48 · 47 · 46 · 45 · 44, about 10 billion; the real ticket space is that divided by 6! = 720, which is 13,983,816. Mix the two up and your odds are off by three orders of magnitude. The question is never "does order matter in general" but "do two selections that differ only in order count as the same outcome *here*?" Podium: no — keep the product. Lottery ticket, on-call team, hand of cards: yes — divide by k!.

<Quiz
  id="01-counting-principles-quiz-2"
  lessonSlug="01-counting-principles"
  lang="en"
  question="From 10 engineers you pick an unordered on-call trio. Which count is right, and why?"
  choices={[
    { label: "10 · 9 · 8 / 3! = 120 — the 720 ordered picks count every trio exactly 6 times, so divide the orderings back out", correct: true },
    { label: "10 · 9 · 8 = 720 — three shrinking stages, multiply and stop", misconception: "That counts ordered podiums. An on-call trio of Ann, Bo and Cy is the same trio in any pick order, yet the product counts it 3! = 6 separate times. When order does not distinguish outcomes, the ordered count must be divided by k! — here 720 / 6 = 120." },
    { label: "10^3 = 1000 — each of three slots can hold any of ten people", misconception: "10 · 10 · 10 allows the same person in two slots, which a real trio forbids — selection without repetition shrinks the options each stage: 10, then 9, then 8. And after fixing that, the picks are still ordered, so a further division by 3! is needed: 120." },
    { label: "10! / 3! = 604800 — factorials divided look like the textbook formula", misconception: "10! counts full orderings of all ten people, which was never the task. The correct ordered count stops after three stages (10 · 9 · 8 = 720); dividing by 3! then forgets order. The textbook C(n,k) = n! / (k!(n−k)!) hides exactly this: the (n−k)! cancels the stages you never ran." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Routing a counting task: stages multiply, cases add, overlap gets subtracted"
  hue="var(--d-network)"
  perRow={3}
  nodes={[
    { id: "task", label: "Counting task", sub: "how many ways?", col: 0, row: 0 },
    { id: "stages", label: "AND-then stages", sub: "shirt, then pants", col: 1, row: 0 },
    { id: "mult", label: "Multiply", sub: "4 · 3 = 12", col: 2, row: 0 },
    { id: "cases", label: "EITHER-or cases", sub: "pasta or pizza", col: 1, row: 1 },
    { id: "add", label: "Add", sub: "3 + 4 = 7", col: 2, row: 1 },
    { id: "overlap", label: "Subtract overlap", sub: "|A|+|B|−|A∩B|", col: 2, row: 2 },
  ]}
  edges={[
    { from: "task", to: "stages", label: "sequential" },
    { from: "task", to: "cases", label: "one of" },
    { from: "stages", to: "mult", label: "counts independent" },
    { from: "cases", to: "add", label: "disjoint" },
    { from: "cases", to: "overlap", label: "overlapping" },
  ]}
  caption="Two questions route every counting task: do the choices happen one after another (multiply the per-stage counts), or is it one choice among cases (add them — and if the cases overlap, subtract the intersection once)? Mixed problems chain the rules: a meal deal adds the dishes, then multiplies by the drinks."
/>

<KeyTakeaway>Multiply and-then stages whose option counts are independent; add disjoint either-or cases, subtracting any overlap once. n! is the product rule applied n times, the permutation count stops after k stages, and C(n, k) divides by k! because every unordered choice was counted exactly k! times. When a number explodes — CI matrices, password spaces — look for the hidden multiplication.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-counting-principles-retrieval"
  lang="en"
  questions={[
    { q: "A test matrix has 3 browsers, 4 OSes and 2 locales; a menu has 3 pastas and 4 pizzas. Why does the first count multiply to 24 while the second adds to 7?", a: "The matrix is a chain of and-then stages: every job picks a browser, AND THEN an OS, AND THEN a locale, and the number of options at each stage does not depend on the earlier picks. The choice tree fans out 3 × 4 × 2 ways, so the counts multiply: 24 jobs — exactly what three nested loops execute. The menu is a single choice split into either-or cases: a dinner is one dish, and pasta versus pizza are disjoint kinds of that one dish (no dish is both), so the case counts add: 3 + 4 = 7. The two structures also fail differently. Multiplication breaks when a stage's option count depends on earlier choices — a CI exclude entry like safari-on-alpine makes the tree lopsided, and you count the full product minus the dead leaves: 24 − 2 = 22. Addition breaks when the cases overlap — files that are large or recent double-count the files that are both, so you subtract the intersection once, which is inclusion–exclusion from the sets unit. Route the task first, then apply the law: sequential means product, one-of means sum." },
    { q: "Rebuild C(10, 3) = 120 from the two counting laws, citing no formula.", a: "Step one, count ordered selections with the product rule: pick a first person (10 options), and then a second (9 remain), and then a third (8 remain) — 10 · 9 · 8 = 720 ordered triples. The counts per stage are independent of which people were picked, so the multiplication is legitimate. Step two, repair the overcount: a team is an unordered set, and the 720 list mentions the same set repeatedly — once per ordering of its three members. Orderings of 3 distinct items number 3 · 2 · 1 = 6 (the product rule again). Crucially, this is exactly 6 for every team, with no exceptions, so the 720 ordered triples partition into groups of 6 that each describe one team. The number of teams is therefore 720 / 6 = 120. That is all C(n, k) ever is: the ordered count n · (n−1) · … · (n−k+1), divided by k! because each unordered outcome was counted exactly k! times. The textbook n! / (k!(n−k)!) is the same quantity with the never-run stages (n−k)! cancelled. If order does distinguish outcomes — a podium, a ranked shortlist — you stop after step one and keep 720." },
  ]}
/>

<Recap lang="en">
Counting runs on two laws. The multiplication rule covers and-then structure: a task built from sequential stages, where the number of options per stage does not depend on earlier picks, has as many outcomes as the product of the stage counts — 4 shirts and 3 pants make 12 outfits, three nested loops run 3 · 4 · 2 times, an 8-character lowercase password lives in a space of 26^8. The addition rule covers either-or structure: one choice split into disjoint cases has as many outcomes as the sum of the case counts — 3 pastas plus 4 pizzas is 7 dinners — and when the cases overlap, the intersection was counted twice and gets subtracted once, which is inclusion–exclusion. Everything classical is these laws repeated: n! orders n items by multiplying n shrinking stages; the permutation count of k from n stops the product after k factors (10 · 9 · 8 = 720 podiums); and combinations divide that by k!, because an unordered team was counted once per ordering of its members — exactly k! times each, so 720 / 6 = 120 teams. The misconception to retire: order does not always matter — a lottery ticket is a set, and counting it as ordered inflates the space 720-fold. Ask instead whether two selections differing only in order are the same outcome here; multiply while they are not, divide by k! the moment they are.
</Recap>
