---
concepts:
- leader-election
- split-brain
- fencing-token
- lease-leadership
- election-timeout
- raft-terms
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://martin.kleppmann.com/2016/02/08/how-to-do-distributed-locking.html
- https://raft.github.io/raft.pdf
- https://etcd.io/docs/latest/learning/api/
status: ready
summary: "Multiple-choice synthesis across the leader-election unit — terms and timeouts, leases vs clocks, split-brain, and the fencing token that the resource must enforce."
title: 'Leader election: multiple-choice review'
track: distributed
unit: 04-leader-election
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each mirrors a call you make in a real incident — not a definition to recite, but a safety argument to defend when a leader pauses, a partition heals, or two nodes both claim the crown.
</Hook>

<Goal lang="en">
Confirm you can connect why we elect a leader, how Raft elects one safely, why a lease is a claim about untrustworthy clocks, and why only a resource-enforced fencing token closes the split-brain window.
</Goal>

<Quiz
  id="04-leader-election-choice-1"
  lessonSlug="04-leader-election"
  lang="en"
  question="A leader holds a 10s lease and writes to object storage; you have observed multi-second GC pauses. Which mitigation actually closes the stale-write window, not just narrows it?"
  choices={[
    { label: "Issue a strictly increasing fencing token on every lease grant and have the storage layer reject any write whose token is below the highest it has accepted.", correct: true },
    { label: "Shorten the lease TTL to 2s so a deposed leader gives up faster.", misconception: "A frozen process executes no code, so it cannot 'give up' a lease it does not know expired. A shorter TTL only makes false expirations more frequent and shrinks — never closes — the window." },
    { label: "Have the leader re-check its lease expiry immediately before each write.", misconception: "A GC pause can land in the gap between the check and the write — a classic time-of-check/time-of-use hole. Safety cannot depend on the suspect node staying responsive." },
    { label: "Switch to a faster consensus algorithm so a new leader is elected sooner.", misconception: "Election speed is orthogonal. The bug is that the OLD leader still writes after it is deposed; no election speed stops a frozen process from waking and writing stale data." },
  ]}
/>

<Quiz
  id="04-leader-election-choice-2"
  lessonSlug="04-leader-election"
  lang="en"
  question="Why does Raft draw each follower's election timeout randomly from roughly 150–300 ms instead of using one fixed value?"
  choices={[
    { label: "It desynchronizes candidates: one timer almost always fires first, so that node campaigns and collects a majority before peers wake — avoiding repeated split votes.", correct: true },
    { label: "It rotates leadership fairly so every node leads about equally often.", misconception: "Randomization is not about fairness of who leads; it is about staggering candidacy so a majority can form in one round instead of everyone campaigning at once." },
    { label: "It compensates for clock skew between nodes.", misconception: "The range exists to stagger candidacy, not to correct clocks. Raft's safety does not rely on synchronized clocks at all." },
    { label: "A fixed timeout would violate the quorum requirement.", misconception: "A fixed timeout breaks no quorum math; it just causes synchronized candidacies that split the vote and force endless retries." },
  ]}
/>

<Quiz
  id="04-leader-election-choice-3"
  lessonSlug="04-leader-election"
  lang="en"
  question="A senior says 'a lease does not bound how long a paused leader keeps believing it leads.' What is the precise reason, and the consequence?"
  choices={[
    { label: "A lease bounds how long the COORDINATOR waits before declaring a node dead — a different clock from the leader's. A frozen leader runs no code, so wall time and its lease expire underneath it while it still thinks it leads: the birth of split-brain.", correct: true },
    { label: "The lease is bounded on both sides as long as clocks are NTP-synced to within a few ms.", misconception: "NTP reduces skew but never removes the deeper problem: a paused or swapped-out process is not executing, so no clock accuracy helps it notice expiry mid-pause." },
    { label: "It is only unbounded if the lease TTL is set too long; a short TTL makes both sides agree.", misconception: "TTL length changes how often expirations happen, not the fact that a stopped process cannot observe its own expiry. The two-clock gap survives any TTL." },
    { label: "The statement is wrong — a lease is a hard guarantee enforced by the coordinator on the leader.", misconception: "The coordinator can revoke its own side, but it cannot reach into a frozen process and stop its in-flight write. The guarantee is one-sided." },
  ]}
/>

<Quiz
  id="04-leader-election-choice-4"
  lessonSlug="04-leader-election"
  lang="en"
  question="Your team adds fencing tokens: the lock service hands out strictly increasing numbers and the leader stamps every write with its token. Incidents continue. What is the most likely gap?"
  choices={[
    { label: "The protected resource never validates the token — it accepts every write regardless of token order. Fencing only works when the RESOURCE rejects any token below the highest it has seen.", correct: true },
    { label: "The tokens are not increasing fast enough to overtake a stale writer.", misconception: "Strict monotonicity is all that matters, not the size of the increment. A token of 34 beats 33 no matter the gap; the failure is the resource not checking." },
    { label: "The lock service should reject the stale writer instead of the resource.", misconception: "The stale leader is paused and bypasses the lock service entirely when it wakes and writes directly to storage. Only the resource sees that write, so the resource must enforce." },
    { label: "Clocks are not synchronized, so token comparison is unreliable.", misconception: "Tokens are compared as integers, not timestamps. The whole point of fencing is to be correct WITHOUT trusting clocks." },
  ]}
/>

<Quiz
  id="04-leader-election-choice-5"
  lessonSlug="04-leader-election"
  lang="en"
  question="A 5-node cluster splits 3-2 by a network partition. How does a correct quorum-based election protocol behave, and why?"
  choices={[
    { label: "Only the 3-node majority side can elect or keep a leader; the 2-node side cannot reach a quorum, so it cannot elect one and any leader stranded there can no longer commit. Quorum is what prevents two writing leaders.", correct: true },
    { label: "Both sides elect their own leader and reconcile by last-write-wins when the partition heals.", misconception: "That IS split-brain with data loss. A majority-quorum protocol forbids it precisely so writes never diverge — the minority side is barred from electing." },
    { label: "Neither side can make progress until the partition heals, because a leader needs all 5 nodes.", misconception: "A leader needs a MAJORITY (3 of 5), not all nodes. The majority side stays available; tolerating one minority partition is the entire point." },
    { label: "The side holding the previous leader keeps leading regardless of which side has the majority.", misconception: "If the old leader is stranded in the minority it loses the ability to commit because it cannot reach a quorum. Leadership follows the quorum, not history." },
  ]}
/>

<Quiz
  id="04-leader-election-choice-6"
  lessonSlug="04-leader-election"
  lang="en"
  question="You need 'run this cron on exactly one host' and reach for a distributed lock. A reviewer warns the lock alone is not enough for correctness. What is the missing piece?"
  choices={[
    { label: "The lock gives mutual-exclusion liveness but not safety against a paused holder; the job's writes must also carry a fencing token that the downstream resource checks, or two runs can still both write.", correct: true },
    { label: "Add a second redundant lock service so the lock is highly available.", misconception: "More lock replicas improve availability, not safety. They do nothing about a holder that pauses and wakes to write after its lock expired." },
    { label: "Make the cron idempotent and you can drop the lock entirely.", misconception: "Idempotency helps for some workloads, but it is a different property; many side effects (sending money, appending to a file) are not naturally idempotent, and the question is about lock correctness." },
    { label: "Run the lock acquire and the job in a single database transaction.", misconception: "The job's external writes (object storage, another service) are outside that transaction, so the transaction cannot fence them. The token check must live at each protected resource." },
  ]}
/>

<Recap lang="en">
The through-line of the unit is one decision tree: elect a single leader to serialize writes (Raft uses terms plus randomized 150–300 ms timeouts to elect one safely), hold leadership as a lease that the coordinator can expire — but never trust that a paused leader knows it lost the lease. A partition is handled by quorum (only the majority leads); a pause is handled by a monotonic fencing token that the protected resource itself enforces. Now when you review a distributed lock design, ask one question before approving it: does the protected resource reject writes with a lower token than the highest it has seen? If not, the lock is incomplete — no TTL tuning will save it.
</Recap>
