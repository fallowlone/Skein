infographic chart-column-simple
data
  title Where the WAL lives decides cluster stability
  desc fsync per committed entry (microseconds) — cloud volumes are 20-40x the NVMe path
  values
    - label NVMe + battery cache
      value 75
    - label EBS gp3 / GCP PD
      value 2000
theme
  palette #10b981 #f43f5e
