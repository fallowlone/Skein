---
concepts:
- pre-vote
- learner
- install-snapshot
- readindex
- leader-lease
- log-compaction
deepensInto:
- distributed/02-raft-outline/06-raft-production-ops
estMin: 16
lang: en
lessonType: topic
level: senior
order: 5
prereqs:
- distributed/02-raft-outline/04-failure-modes-and-recovery
slug: 05-advanced-extensions
sources:
- https://raft.github.io/raft.pdf
- https://raft.github.io/
- https://www.pingcap.com/article/understanding-raft-consensus-in-distributed-systems-with-tidb/
- https://developer.hashicorp.com/consul/docs/concept/consensus
spiral:
- statefulness
status: ready
summary: Four production-critical Raft extensions — pre-vote to prevent term inflation,
  learners for safe scale-out, snapshots for log compaction, and ReadIndex/lease reads
  for fast linearizable queries.
title: 'Raft extensions: pre-vote, learners, snapshots, and linearizable reads'
track: distributed
unit: 02-raft-outline
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Misconception from "~/components/layout/Misconception.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import readLatency from "./infographics/read-latency.svg?raw";

<Hook>
Your etcd cluster has 50,000 reads per second but only 200 writes per second. If every read triggers a consensus round, the cluster spends 250x more work on reads than writes. Yet if you serve reads from a stale follower, you lose linearizability. How do production Raft implementations serve reads at sub-millisecond latency without sacrificing correctness?
</Hook>

<Crux>Pre-vote, learners, snapshots, and ReadIndex/lease reads turn textbook Raft into a production system.</Crux>

<Explanation>

## Pre-vote: preventing spurious elections

When a node returns from a long partition, it has been incrementing its term counter the whole time. Its RequestVote arrives with a very high term, forcing healthy followers to update their terms and step down the current leader — even though no actual failure occurred. The cluster suffers an unnecessary election.

**Pre-vote** adds a non-binding dry run before any real election. Before incrementing its term, a candidate sends a `PreVote` message. Nodes respond "I would vote for you" or "I would not" without changing any persistent state. Only if a majority would vote yes does the candidate actually increment and run a real election.

Cost: one extra RPC round when an election is genuinely needed. Benefit: zero spurious leader changes from rejoining nodes. Pre-vote is now standard in etcd, TiKV, and Hashicorp Raft.

## Learners: safe cluster scale-out

Adding a node directly as a voter is dangerous: the new node starts with an empty log and its slow catch-up degrades commit latency for every write until it is current. In a 3-node cluster adding a 4th voter also temporarily drops fault-tolerance (quorum is now 3 of 4 instead of 2 of 3).

**Learners** (non-voting members) solve this. A learner receives AppendEntries and replicates the log like a follower, but does not count toward quorums and does not vote in elections. The operator promotes the learner to voter only once its log lag is below a threshold (typically under 1000 entries behind). The promotion is then a single-server membership change — safe, fast, and transparent.

Learners are also how etcd, Consul, and Yugabyte handle node replacement after a failure: the new node learns, catches up, gets promoted, then the dead node is removed. Availability never drops below the original quorum during the replacement.

## Snapshots and log compaction

A Raft log grows indefinitely. A one-year-old cluster with 10k writes/s has written 315 billion entries — the log would be terabytes, and replaying it after a crash would take days.

**Snapshots** compact the log: periodically, each node serialises its full state machine state plus the `(lastIncludedIndex, lastIncludedTerm)` tuple to disk, then deletes all log entries up to that index. If a follower falls so far behind that the leader has already compacted the entries it needs, the leader sends an `InstallSnapshot` RPC instead of the missing AppendEntries — the follower loads the snapshot as its starting state, then replays only the recent log tail.

<div data-lesson-visual class="overflow-x-auto my-6">

| System | Default snapshot interval |
|--------|--------------------------|
| etcd | Every 10,000 entries |
| CockroachDB | Every few seconds per range |
| TiKV | Configurable, default 200 MB |

</div>

Snapshot frequency is a tunable tradeoff: too often amplifies fsync; too rare grows the log and slows recovery. The snapshot must include the latest membership configuration — forgetting this is a well-known bug in DIY Raft implementations.

## ReadIndex: linearizable reads without writing to the log

A naive approach to linearizable reads: the leader commits a no-op log entry just for the read, then returns data. This serialises reads into the log — expensive (one full consensus round per read).

**ReadIndex** eliminates the log write. When a read arrives:

1. Leader records its current `commitIndex`.
2. Leader sends a heartbeat to a majority of followers to confirm it is still the active leader (prevents a stale leader from serving reads after a partition).
3. Once its state machine has applied entries up to `commitIndex`, the leader returns the result.

Cost: one heartbeat round-trip (typically 1–5 ms intra-region). No new log entry. Reads scale independently of write throughput.

## Leader lease reads: sub-millisecond latency

ReadIndex still pays a network round-trip per read. **Lease reads** go further: the leader holds a **lease** — a time window during which it is guaranteed to be the only leader (because no election can complete within the lease duration). During the lease, the leader serves reads from its current state machine without any RPC.

<Infographic
  svg={readLatency}
  label="Column chart comparing intra-region linearizable read latency: ReadIndex costs up to 5 ms for one heartbeat round-trip, while a lease read serves locally with no RPC in under 1 ms."
  caption="ReadIndex's heartbeat round-trip is 1–5 ms intra-region; a lease read drops the RPC entirely and serves in under 1 ms — the speed-up that makes bounded clock skew worth caring about."
  hue="var(--d-infra)"
/>

Lease duration is typically `election_timeout * 0.9` (e.g., 9 ms for a 10 ms election timeout), leaving a 10% buffer for clock skew.

**The correctness condition:** lease reads are safe only if the clock skew between leader and followers is bounded. If the leader's clock runs significantly faster than any follower's, the lease may expire on a follower before the leader thinks it has — a follower could start a new election and elect a new leader while the old leader is still serving reads. This is why **NTP/PTP synchronisation is a correctness requirement** for lease reads, not just a hygiene practice.

<Misconception id="mc-raft-bft">
  <p><strong>"Raft makes my system hack-proof."</strong> No. Raft is CFT, not BFT. A compromised node can lie or send corrupted logs — Raft does not cover this. For adversarial participants use PBFT, HotStuff, or Tendermint. For typical datacenter workloads (crashes, not malicious operators), CFT is sufficient.</p>
</Misconception>

<NumbersCard
  id="card-raft-sr-numbers"
  title="Raft extension numbers"
  stats={[
    { label: "fsync latency, NVMe + BBU", value: "50–100 µs" },
    { label: "fsync latency, cloud SSD (EBS gp3)", value: "1–3 ms" },
    { label: "ReadIndex latency (intra-region)", value: "1–5 ms" },
    { label: "Lease read latency", value: "under 1 ms" },
    { label: "Etcd snapshot interval (default)", value: "10,000 entries" },
    { label: "TimeoutNow leadership transfer", value: "under 10 ms unavailability" },
  ]}
/>

<Quiz
  id="raft-sr-q1"
  lessonSlug="05-advanced-extensions"
  lang="en"
  question="Why is leader lease read considered correctness-sensitive while ReadIndex is not?"
  choices={[
    { label: "Lease reads use an unsafe RPC path", misconception: "Both use the same channels. The difference is time-based vs network-round-trip-based confirmation." },
    { label: "Lease reads depend on bounded clock skew — wall-clock sync (NTP/PTP) becomes a correctness requirement", correct: true },
    { label: "ReadIndex skips the state machine entirely", misconception: "ReadIndex waits for the state machine to apply up to commitIndex. It does not skip anything." },
    { label: "Lease reads are slower", misconception: "Lease reads are faster (sub-ms). That speed is exactly what makes them attractive — and the correctness tradeoff real." },
  ]}
/>

<Quiz
  id="raft-sr-q2"
  lessonSlug="05-advanced-extensions"
  lang="en"
  question="A new node is added to a 3-node Raft cluster directly as a voter. Why might this temporarily degrade commit latency?"
  choices={[
    { label: "The new node's empty log causes all AppendEntries to fail until it catches up", misconception: "Log catch-up happens independently; the new node can ack entries as it receives them, but it starts very behind." },
    { label: "The new node counts toward quorum (now 3 of 4 required), and its slow catch-up means it can be a bottleneck for quorum acknowledgements", correct: true },
    { label: "Adding a voter always requires a cluster restart", misconception: "Raft membership changes are online operations. No restart is required." },
    { label: "The new node triggers a leader election on join", misconception: "Joining as a voter does not trigger an election. The existing leader continues." },
  ]}
/>

</Explanation>

<StackDiagram
  label="Three ways to serve a linearizable read, from heaviest to lightest"
  hue="var(--d-systems)"
  layers={[
    { label: "No-op log entry", note: "full consensus round" },
    { label: "ReadIndex", note: "one heartbeat RTT, no log write" },
    { label: "Lease read", note: "no RPC, under 1 ms" },
  ]}
  caption="Each layer down strips cost while keeping linearizability: the naive no-op writes to the log, ReadIndex confirms leadership with one heartbeat round-trip, and a lease read serves locally within a clock-bounded lease window."
/>

<KeyTakeaway>Pre-vote stops spurious elections from rejoining nodes; learners let new nodes catch up before counting toward quorum; snapshots bound log size and speed recovery; ReadIndex and lease reads enable linearizable reads without consensus round-trips — lease reads are sub-millisecond but require bounded NTP clock skew as a correctness condition.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="raft-sr-retrieval"
  lang="en"
  questions={[
    { q: "A node rejoins after 30 minutes offline. Without pre-vote, what happens to the cluster?", a: "The returning node has been incrementing its term counter throughout the partition (every failed RequestVote forced an increment). Its RequestVote arrives with a very high term. Healthy followers update their terms to match, which forces the current leader to step down, triggering an unnecessary election. With pre-vote, the node sends a non-binding PreVote first; followers reply 'I would not vote for you — your log is stale.' The node does not increment its term, and the current leader is not disturbed." },
    { q: "What information must a Raft snapshot include, beyond the serialised state machine?", a: "At minimum: (1) lastIncludedIndex — the log index of the last entry applied to the snapshot; (2) lastIncludedTerm — the term of that entry; (3) the current cluster membership configuration. Omitting the membership config means a node that installs the snapshot from another cluster version will have the wrong voter set — a classic source of membership change bugs in DIY Raft implementations." },
    { q: "ReadIndex is described as requiring 'one heartbeat round-trip per read.' Why cannot the leader skip this and serve reads immediately from its current state?", a: "After a partition heals, there may be a brief window where the old leader does not yet know it has been replaced. Without a fresh heartbeat majority confirmation, the stale leader could serve reads from its local state machine — which might be behind the new leader's committed state. The heartbeat round confirms the leader is still authoritative before it serves the read, preventing stale data from being returned as fresh." },
  ]}
/>

<Recap lang="en">
Four extensions complete the gap between textbook Raft and production systems. Pre-vote eliminates spurious elections by requiring a dry-run before term increment. Learners allow safe scale-out by letting new nodes catch up before they count toward quorum. Snapshots bound log growth by periodically checkpointing state machine state and allowing InstallSnapshot for lagging followers. ReadIndex enables linearizable reads with one heartbeat round-trip instead of a full log write; lease reads take this to sub-millisecond by time-bounding leader authority, but require NTP synchronisation as a correctness precondition. All four are standard in etcd, TiKV, and Hashicorp Raft. Now when you evaluate a Raft deployment, ask four questions: Is pre-vote on? Are new nodes added as learners first? How often does snapshotting run? Are reads going through ReadIndex or a stale follower? The answers tell you whether you have textbook Raft or a production-ready one.
</Recap>
