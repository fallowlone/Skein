infographic chart-column-simple
data
  title Linearizable read latency by path (intra-region)
  desc ReadIndex pays one heartbeat round-trip; a lease read serves locally with no RPC
  values
    - label ReadIndex (heartbeat RTT)
      value 5
    - label Lease read (no RPC)
      value 1
theme
  palette #6366f1 #14b8a6
