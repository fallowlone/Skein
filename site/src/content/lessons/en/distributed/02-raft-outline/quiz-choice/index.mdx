---
concepts:
- quorum-overlap
- commit-safety
- leader-completeness
- pre-vote
- joint-consensus
- cp-tradeoff
estMin: 14
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://raft.github.io/raft.pdf
- https://developer.hashicorp.com/consul/docs/concept/consensus
- https://github.com/hashicorp/raft/blob/main/membership.md
status: ready
summary: "Multiple-choice synthesis across the Raft unit — quorum overlap, commit safety, election rules, the CP tradeoff, pre-vote, and safe membership change."
title: 'Raft: multiple-choice review'
track: distributed
unit: 02-raft-outline
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one is a decision you make while operating a real Raft cluster — etcd, TiKV, Consul — not a definition to recite. Pick the answer a senior engineer defends on the incident call.
</Hook>

<Goal lang="en">
Confirm you can connect the pieces the lessons built separately: why majority quorum is a safety requirement, what "committed" really means, how the voting rule preserves history, why Raft halts under partition, and the production discipline that keeps the cluster alive.
</Goal>

<Quiz
  id="02-raft-outline-choice-1"
  lessonSlug="02-raft-outline"
  lang="en"
  question="A 5-node cluster commits entry 101 with a quorum of {A, B, C}. Leader A then crashes, and the new leader is elected by the quorum {C, D, E}. Why is entry 101 guaranteed to survive into the new term?"
  choices={[
    { label: "The two quorums {A,B,C} and {C,D,E} must overlap — C is in both. The RequestVote rule refuses any candidate whose log is less up-to-date than a voter's, so the winner has at least C's log, which contains 101.", correct: true },
    { label: "The new leader replays entry 101 from a transaction log shipped by the old leader before it crashed.", misconception: "There is no out-of-band log shipping in Raft. Durability comes purely from quorum overlap plus the log-completeness voting rule — the new leader already had 101 because it shares a node with the commit quorum." },
    { label: "Raft re-commits every entry from the previous term automatically at the start of each new term.", misconception: "Raft never blindly re-commits prior-term entries — that is actually unsafe (the figure-8 problem). A committed entry survives because every election quorum overlaps the commit quorum, not because of re-commit." },
    { label: "Entry 101 is only safe if all five nodes acknowledged it.", misconception: "Requiring all nodes would destroy availability — one slow node would block every commit. A majority is sufficient precisely because any two majorities intersect." },
  ]}
/>

<Quiz
  id="02-raft-outline-choice-2"
  lessonSlug="02-raft-outline"
  lang="en"
  question="A leader appends entry 200 to its own disk and fsyncs it, then crashes before any AppendEntries leaves the box. What is the correct statement about entry 200?"
  choices={[
    { label: "It was never committed — only one node held it. A new leader's log ends before 200, and when the old leader returns the consistency check forces it to truncate 200. No client was acknowledged, so nothing is lost.", correct: true },
    { label: "It is committed because it was fsynced to durable storage on the leader.", misconception: "fsync on one node is durability, not commitment. Commitment in Raft means a majority has persisted the entry; a single-node fsync can be discarded on leader change." },
    { label: "The new leader must preserve entry 200 because Leader Append-Only forbids deleting entries.", misconception: "Leader Append-Only constrains a leader's own log, not a follower's. The returning old leader is now a follower and must truncate its uncommitted tail to match the new leader." },
    { label: "The cluster is now inconsistent and needs manual repair.", misconception: "Discarding an unacknowledged, uncommitted entry is normal, automatic, and safe — exactly what Log Matching handles. No manual repair is needed." },
  ]}
/>

<Quiz
  id="02-raft-outline-choice-3"
  lessonSlug="02-raft-outline"
  lang="en"
  question="A node returns from a 30-minute partition. During that time it kept timing out and incrementing its term, so it now has a much higher term than the live leader but a stale log. Without pre-vote, what happens — and what does pre-vote change?"
  choices={[
    { label: "Its high-term RequestVote forces healthy nodes to bump their term and the leader to step down, causing a needless election. Pre-vote runs a non-binding dry run first; voters reject the stale log, so the node never increments its term and the leader is undisturbed.", correct: true },
    { label: "It immediately becomes leader because it has the highest term.", misconception: "A high term alone never wins an election — the log-completeness rule makes voters reject a candidate whose log is less up-to-date than theirs. The damage without pre-vote is the spurious election it triggers, not a takeover." },
    { label: "Nothing happens; the cluster ignores any node with a stale log.", misconception: "Without pre-vote the cluster cannot ignore it: the high term in its RequestVote forces every receiver to adopt that term and step the leader down before the log check even matters." },
    { label: "Pre-vote speeds up the election once the node rejoins.", misconception: "Pre-vote adds one RPC round when an election is genuinely needed; its purpose is to prevent spurious elections, not to make legitimate ones faster." },
  ]}
/>

<Quiz
  id="02-raft-outline-choice-4"
  lessonSlug="02-raft-outline"
  lang="en"
  question="A network partition puts the current leader on the minority side, reaching only 1 of 4 other nodes. Clients on the leader's side get write timeouts; the majority side elects a new leader. Is this a bug?"
  choices={[
    { label: "No — this is Raft's CP guarantee working as designed. The minority side cannot reach quorum, so it refuses to commit rather than risk a second leader committing conflicting entries. On heal, the old leader sees the higher term, steps down, and truncates its uncommitted tail.", correct: true },
    { label: "Yes — a correct system should let both sides keep serving writes and merge later.", misconception: "That is AP behaviour (Dynamo/Cassandra). Raft has no merge; it deliberately sacrifices availability on the minority side to guarantee a single consistent history." },
    { label: "Yes — the cluster should freeze entirely until the partition heals.", misconception: "Only the minority side halts. The majority side has quorum and continues serving normally — freezing everything would throw away the availability Raft can safely provide." },
    { label: "No bug, but the two leaders mean the cluster briefly has split brain.", misconception: "There is no split brain: only the majority-side leader can actually commit. The stale minority leader cannot reach quorum, so its writes never complete." },
  ]}
/>

<Quiz
  id="02-raft-outline-choice-5"
  lessonSlug="02-raft-outline"
  lang="en"
  question="An operator needs to grow a 3-node cluster to 5. They push the new 5-node config to every node in one atomic step, skipping joint consensus. What is the actual risk?"
  choices={[
    { label: "During the config-change window, nodes on the new config and nodes still on the old config can form two non-overlapping majorities — 2-of-3 (old) and 3-of-5 (new) — electing two leaders at once. Joint consensus requires a majority of BOTH configs, so no two quorums can be disjoint.", correct: true },
    { label: "The two new nodes start with empty logs and slow commits until they catch up.", misconception: "Slow catch-up is a performance issue, solved by adding nodes as learners first — not the safety hazard. The single-step swap risks two disjoint majorities, i.e. split brain." },
    { label: "etcd does not support adding more than one node, so the call simply fails.", misconception: "Single-server-change is a safety discipline layered on joint consensus, not a hard library limit. The danger of bypassing it is real split brain, documented in production postmortems." },
    { label: "New nodes cannot vote until their first heartbeat, so the election stalls.", misconception: "Vote eligibility is not the issue. The issue is the quorum-intersection property breaking during the config discontinuity window." },
  ]}
/>

<Quiz
  id="02-raft-outline-choice-6"
  lessonSlug="02-raft-outline"
  lang="en"
  question="A cluster shows leader_changes_total climbing to several per minute, and clients see periodic 200–400 ms write stalls. wal_fsync_duration_seconds p99 is 1.8 s. What is the diagnosis and first fix?"
  choices={[
    { label: "The leader's fsync exceeds the heartbeat interval, so followers time out mid-fsync and start elections; the new leader hits the same slow disk and the cycle repeats. First fix: move the Raft WAL to dedicated local NVMe.", correct: true },
    { label: "Raise the heap / give the process more memory so the runtime stops pausing.", misconception: "This is a disk-latency problem in the consensus WAL, not a memory problem. No heap tuning changes fsync latency on a shared cloud volume." },
    { label: "Grow the cluster from 5 to 7 nodes to spread the load.", misconception: "More voters increases the quorum size and the fsync fan-out — it makes a slow-disk problem worse, not better. Fix the disk first." },
    { label: "The high term-growth rate means a malicious node is attacking the cluster.", misconception: "Raft is crash-fault-tolerant, not Byzantine. Flapping leadership with high fsync p99 is a slow-disk signature, not an attack." },
  ]}
/>

<Recap lang="en">
The unit's spine is one safety argument and one ops discipline. Safety: majority quorum guarantees any two quorums overlap, so a committed entry — persisted by a majority — is always inherited by the next leader, while the log-completeness voting rule blocks a stale candidate; together that is Leader Completeness, and "committed" only ever means majority-persisted. Discipline: Raft is CP, so the minority halts under partition by design; pre-vote stops rejoining nodes from triggering spurious elections; joint consensus keeps membership changes from splitting the cluster; and the WAL belongs on dedicated NVMe so fsync never starves the heartbeat. Almost every production incident traces back to one of those three — bypassed membership protocol, disabled pre-vote, or a slow disk.
</Recap>
