---
concepts:
- election-timeout
- request-vote
- leader-completeness
- safety-properties
- quorum-overlap
deepensInto:
- distributed/02-raft-outline/04-failure-modes-and-recovery
estMin: 14
lang: en
lessonType: topic
level: middle
order: 3
prereqs:
- distributed/02-raft-outline/02-log-replication-and-commit
slug: 03-leader-election-in-depth
sources:
- https://raft.github.io/raft.pdf
- https://raft.github.io/
- https://en.wikipedia.org/wiki/Raft_(algorithm)
spiral:
- statefulness
status: ready
summary: How randomised timeouts prevent repeated split votes, why the candidate log-completeness
  rule preserves committed entries across leader changes, and the four invariants
  that make Raft correct.
title: 'Raft leader election: timeouts, voting rules, and the four safety properties'
track: distributed
unit: 02-raft-outline
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import safetyInvariants from "./infographics/safety-invariants.svg?raw";

<Hook>
Five followers all notice the leader is silent at the same instant. All five start an election simultaneously. Each votes for itself. Nobody wins. They all start again. This is why Raft elections use randomised timeouts — and why the voting rule is more nuanced than "first come, first served."
</Hook>

<Crux>Randomised timeouts break election ties; the log-completeness voting rule ensures the new leader has every previously committed entry.</Crux>

<Explanation>

## The election timeout and heartbeats

A leader asserts its authority by sending `AppendEntries` heartbeats to every follower at a fixed interval — typically 50 ms. Each follower has an **election timeout** that resets on every valid heartbeat. If the timeout fires (no heartbeat received), the follower assumes the leader is dead and starts an election.

The timeout is **randomised** — typically in a range like 150–300 ms. Without randomisation, all followers would time out simultaneously, each vote for itself, and the vote would split. With a wide-enough range, one follower fires first with high probability, sends `RequestVote` to all others, and collects a majority before anyone else times out. Ties are still possible but become rare; any tie just starts a fresh term and a new random timeout resolves it quickly.

<FlowDiagram
  data-lesson-visual
  label="Randomised election timeout breaks split-vote ties: the first follower to fire sends RequestVote, collects a majority before others time out, and becomes leader."
  hue="var(--d-systems)"
  perRow={4}
  nodes={[
    { id: "heartbeat", label: "Leader silent", sub: "no heartbeat received" },
    { id: "timeout", label: "Random timeout fires", sub: "150–300 ms, one node first" },
    { id: "candidate", label: "Follower → Candidate", sub: "increments term, votes for self" },
    { id: "leader", label: "Majority → Leader", sub: "wins before others time out" },
  ]}
  edges={[
    { from: "heartbeat", to: "timeout" },
    { from: "timeout", to: "candidate" },
    { from: "candidate", to: "leader", label: "RequestVote granted by quorum" },
  ]}
  caption="Fixed timeouts cause every follower to fire at once, splitting votes. A random range (150–300 ms) makes one node fire first with high probability, collecting a majority before anyone else starts."
/>

## The RequestVote voting rule

A node grants a vote in `RequestVote` **only if**:

1. It has not already voted in this term (one vote per term per node).
2. The candidate's log is **at least as up-to-date** as the voter's own log.

"At least as up-to-date" is compared by: higher `lastLogTerm` wins; if terms are equal, higher `lastLogIndex` wins. This rule is the key to safety.

## Why the log-completeness rule matters

Without the up-to-date check, a candidate with a stale log could win an election and become leader despite missing committed entries. Those entries would then be overwritten, violating the guarantee that a committed entry is durable forever.

The quorum-overlap argument shows why the rule works: every committed entry was acknowledged by a majority. Any election quorum overlaps with that commit quorum in at least one node. Through that shared node, the candidate must have the committed entry (or a voter refuses). Combined, the quorum overlap and the log-completeness rule give **Leader Completeness**: every leader in term T+1 has all entries committed in terms 1–T.

## The four safety properties

Raft's correctness proof reduces to four invariants:

1. **Election Safety** — at most one leader per term. Follows from "one vote per node per term + majority required."
2. **Leader Append-Only** — a leader never overwrites or deletes its own log entries; it only appends.
3. **Log Matching** — if two logs share an entry at index i with term t, they are identical for all indices up to i. Follows from the AppendEntries consistency check.
4. **Leader Completeness** — any entry committed in some term is in the log of every leader of higher terms. Follows from quorum overlap + the voting rule.

These four invariants work as a chain: Safety prevents two leaders (1), Append-Only keeps a leader's own log intact (2), Log Matching makes followers converge to it (3), and Completeness ensures no new leader starts with a gap (4). Without any single link, the chain breaks — which is why implementations like etcd treat each invariant as a non-negotiable protocol requirement, not a best-effort guideline.

<Infographic
  svg={safetyInvariants}
  label="Hierarchy tree of Raft's four correctness invariants — Election Safety, Leader Append-Only, Log Matching, Leader Completeness — each annotated with the single mechanism it follows from and the failure that occurs if that mechanism is dropped."
  caption="Each invariant is not a standalone rule but the consequence of one concrete mechanism; remove the mechanism and the named failure follows. That is the difference between memorising four properties and seeing why the proof holds."
  hue="var(--d-infra)"
/>

**State Machine Safety** (derived): no two nodes ever apply different commands at the same index.

<Quiz
  id="raft-mid-q2"
  lessonSlug="03-leader-election-in-depth"
  lang="en"
  question="Why does Raft randomise the election timeout (150–300 ms) rather than using a fixed value?"
  choices={[
    { label: "To make elections faster on average", misconception: "Randomisation does not speed up elections; if anything, it slows the worst case. The purpose is to avoid repeated split votes." },
    { label: "To probabilistically ensure one follower fires first, avoiding simultaneous candidate collisions", correct: true },
    { label: "Because the network protocol requires it", misconception: "No transport constraint requires randomisation. It is a deliberate protocol design choice." },
    { label: "To fairly rotate leadership across nodes", misconception: "Raft does not aim for fairness — it aims for liveness. The first node to time out and collect a majority wins." },
  ]}
/>

<Quiz
  id="raft-mid-q3"
  lessonSlug="03-leader-election-in-depth"
  lang="en"
  question="Why does the RequestVote voting rule require the candidate's log to be at least as up-to-date as the voter's?"
  choices={[
    { label: "To speed up the election", misconception: "The log check adds a comparison step — it makes elections slightly slower, not faster." },
    { label: "To ensure the new leader has all previously committed entries, preventing loss of acknowledged writes", correct: true },
    { label: "To prevent followers from voting for themselves", misconception: "A candidate always votes for itself; the up-to-date check governs grants from other nodes." },
    { label: "To avoid term inflation", misconception: "Term inflation is mitigated by pre-vote (a separate extension). The log check is about entry completeness." },
  ]}
/>

<TraceScenario
  id="raft-mid-trace1"
  lessonSlug="03-leader-election-in-depth"
  lang="en"
  scenario="Trace a clean leader election after the leader crashes."
  steps={[
    { prompt: "Setup: 5-node cluster A, B, C, D, E. A is leader at term 7. A crashes.", reveal: "No more heartbeats from A. Each follower's election timer counts down independently (randomised in the 150–300 ms range)." },
    { prompt: "B's timer fires first at 187 ms. What does B do?", reveal: "B transitions to candidate, increments term to 8, votes for itself, sends RequestVote(term=8, lastLogIndex=100, lastLogTerm=7) to C, D, E." },
    { prompt: "C, D, E receive RequestVote. They check: did I already vote in term 8? Is B's log as up-to-date as mine?", reveal: "None have voted in term 8. B's log equals theirs (lastLogIndex=100, lastLogTerm=7). Each grants the vote and updates currentTerm to 8." },
    { prompt: "B collects 3 votes (itself + C + D). What happens next?", reveal: "B has a majority of 5. B becomes leader for term 8 and immediately sends heartbeats (empty AppendEntries) to all nodes. C, D, E reset their election timers." },
    { prompt: "A comes back online with stale term 7.", reveal: "A's AppendEntries fail (followers reject lower term). A receives term 8 in the reply, steps down to follower, updates its term to 8, and starts following B." },
  ]}
/>

</Explanation>

<SequenceDiagram
  label="A RequestVote round where candidate B wins a majority and becomes leader"
  hue="var(--d-systems)"
  actors={["Candidate", "C", "D", "E"]}
  messages={[
    { from: "Candidate", to: "C", label: "RequestVote(term=8, lastLogIdx=100)" },
    { from: "Candidate", to: "D", label: "RequestVote(term=8, lastLogIdx=100)" },
    { from: "Candidate", to: "E", label: "RequestVote(term=8, lastLogIdx=100)" },
    { from: "C", to: "Candidate", label: "vote granted" },
    { from: "D", to: "Candidate", label: "vote granted" },
    { from: "Candidate", to: "E", label: "heartbeat (now leader)" },
  ]}
  caption="The candidate gets votes from C and D — with its own vote that is 3 of 5, a majority — so it becomes leader for term 8 and starts sending heartbeats. A voter grants only if it has not voted this term and the candidate's log is at least as up-to-date."
/>

<KeyTakeaway>Randomised election timeouts make split votes rare. The log-completeness voting rule ensures every new leader has all previously committed entries. Together, these give Leader Completeness — the invariant that no acknowledged write is ever lost across a leader transition.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="raft-mid-retrieval2"
  lang="en"
  questions={[
    { q: "Why does Raft need a majority for elections, not just any 2-of-5?", a: "Because any two majorities of a 5-node set must share at least one node (pigeonhole). That shared node carries the full committed history from the old majority to the new. With a non-majority quorum (e.g., 2-of-5), two disjoint groups could both believe they are leaders, commit conflicting entries, and create split brain." },
    { q: "A node just returned from 10 minutes offline. It has lastLogTerm=5, lastLogIndex=200. The cluster is at term 12, with the leader having lastLogIndex=9500. Can this returning node win an election?", a: "No. The returning node's log is far behind: lastLogTerm=5 is lower than the cluster's current lastLogTerm (which is at least 12). Any voter with a higher lastLogTerm will refuse. Even if terms were equal, lastLogIndex=200 vs 9500 loses. The returning node steps down to follower and catches up via AppendEntries before it could ever be a viable candidate." },
    { q: "What is the difference between Election Safety and Leader Completeness?", a: "Election Safety says at most one leader per term — guaranteed by one-vote-per-node-per-term plus majority. Leader Completeness says every leader in term T+1 has all entries committed in terms 1 through T — guaranteed by quorum overlap plus the log-completeness voting rule. Election Safety prevents two concurrent leaders; Leader Completeness prevents a leader that is missing history." },
  ]}
/>

<Recap lang="en">
Raft prevents repeated split votes by randomising the election timeout — the first follower to fire likely collects a majority before others even start. The RequestVote voting rule requires candidates to have logs at least as up-to-date as any voter's, which combined with quorum overlap ensures the winning leader has every previously committed entry (Leader Completeness). The four safety properties — Election Safety, Leader Append-Only, Log Matching, and Leader Completeness — collectively guarantee that no two nodes ever apply different commands at the same index. A typical leader election on a healthy cluster resolves in 100–500 ms. Now when you see a candidate lose an election despite having a higher node ID or longer uptime, you will know why: it is the log completeness check, not any notion of seniority, that decides who wins.
</Recap>
