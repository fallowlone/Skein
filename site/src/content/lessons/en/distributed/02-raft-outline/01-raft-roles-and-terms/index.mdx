---
concepts:
- raft
- consensus
- leader-election
- quorum
- term
deepensInto:
- distributed/02-raft-outline/02-log-replication-and-commit
estMin: 10
lang: en
lessonType: topic
level: junior
order: 1
prereqs:
- distributed/01-cap-practice
slug: 01-raft-roles-and-terms
sources:
- https://raft.github.io/raft.pdf
- https://raft.github.io/
- https://en.wikipedia.org/wiki/Raft_(algorithm)
spiral:
- statefulness
status: ready
summary: Raft''''s three node roles, the monotonic term counter, and the quorum rule
  that makes it impossible for two leaders to exist at once.
title: Raft roles, terms, and why majority quorums prevent split brain
track: distributed
unit: 02-raft-outline
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import failureTolerance from "./infographics/failure-tolerance.svg?raw";

<Hook>
Your Kubernetes cluster is backed by a 5-node etcd cluster. One node loses power mid-morning. You run `kubectl get pods` and it works fine — no error, no stall. How is there still agreement with a node down?
</Hook>

<Crux>Raft keeps a cluster consistent by electing one leader per term and requiring a majority quorum for every decision.</Crux>

<Explanation>

## The job: one machine from many

A distributed system's hardest problem is agreement. If five nodes each accept writes independently, you get five conflicting histories. Raft's job is to make those five nodes behave like one: same order of changes, same state, no writes lost. It does this by electing exactly one leader at a time and routing all changes through that leader.

## Three roles

Understanding the three roles is what lets you read a Raft status page — or a post-mortem — and immediately know which invariant held and which one broke.

Every Raft node is in exactly one of three states:

- **Follower** — the default state. Receives and stores log entries from the leader. Does not accept client writes directly.
- **Candidate** — a follower that has stopped hearing from a leader and is now running for leadership. Temporary state, lasts until the election resolves.
- **Leader** — the node clients send writes to. Drives replication to all followers. At most one leader per term exists in a healthy cluster.

A node starts as a follower. It becomes a candidate when its election timeout fires. It becomes a leader if it wins a majority vote.

## The term: a monotonic logical clock

Raft tracks time not with wall clocks but with **terms** — monotonically increasing integers. Each term begins with an election. If a leader wins, it leads for the whole term. If no leader emerges (split vote), the term ends and a new one starts.

The term has two jobs:

1. **Deduplication.** When a message arrives, nodes compare the sender's term to their own. A higher term always wins — the receiver updates its term and steps down to follower if needed. This resolves stale-leader confusion instantly.
2. **Ordering.** Every log entry is tagged with the term it was written in. This tag is used later to detect log divergence.

Together, these two jobs mean the term is the single shared clock that makes "who is in charge right now?" always answerable without any wall-clock agreement — drop either job and you get either a zombie leader or an undetectable log split.

<div data-lesson-visual class="overflow-x-auto my-6">

| Term | What happened |
|------|--------------|
| 1 | Node A elected leader. Served 30 s. |
| 2 | A lost network briefly. B won election. |
| 3 | B crashed. C won election. |
| 4 | C still leader — no new election needed. |

</div>

Terms are never reused. If you see term 7, every message from term 6 is stale.

## Majority quorum: the split-brain barrier

Raft requires a **majority** (more than half the cluster) for two operations: **elections** and **commits**. In a 5-node cluster the majority is 3.

Why majority specifically? The key property is overlap: any two majorities of the same set share at least one node. In a 5-node cluster, if one set of 3 commits an entry and a different set of 3 elects a new leader, those two sets cannot be disjoint — they share a node. Through that shared node, the new leader is guaranteed to have seen the committed entry.

If Raft used simple plurality (2 of 5) instead of majority, two separate groups of 2 could each believe they are authoritative — split brain. Majority prevents this.

**Failure tolerance:** a cluster of N nodes tolerates `floor((N-1)/2)` simultaneous failures. 5 nodes → 2 failures. 3 nodes → 1 failure. This is why Raft clusters are 3, 5, or 7 nodes — odd numbers maximize tolerance for a given size.

<Infographic
  svg={failureTolerance}
  label="Column chart of failures tolerated by Raft cluster size: a 3-node cluster survives 1 failure, a 5-node cluster survives 2, and a 7-node cluster survives 3 — the floor((N-1)/2) rule."
  caption="Each step of 2 added nodes buys exactly one more tolerated failure — the reason production Raft clusters are 3, 5, or 7 nodes, never even."
  hue="var(--d-infra)"
/>

<Quiz
  id="raft-jr-q1"
  lessonSlug="01-raft-roles-and-terms"
  lang="en"
  question="A 5-node Raft cluster is split: DC A has the leader and 2 followers (3 nodes), DC B has 2 followers. The link between DCs is cut. What happens?"
  choices={[
    { label: "Both sides keep accepting writes — split brain", misconception: "Raft refuses to commit on any side that cannot reach a majority. No split brain by design." },
    { label: "DC A (3 of 5 nodes = majority) keeps accepting writes; DC B's 2 nodes cannot elect a leader", correct: true },
    { label: "The whole cluster freezes until the link is restored", misconception: "The majority side continues. Only the minority side stalls." },
    { label: "DC B elects a new leader after timeout", misconception: "2 nodes cannot form a majority of 5. Their elections never complete." },
  ]}
/>

<Quiz
  id="raft-jr-q2"
  lessonSlug="01-raft-roles-and-terms"
  lang="en"
  question="Why does Raft require a majority (3 of 5), not just any 2 of 5, for both elections and commits?"
  choices={[
    { label: "To make voting faster", misconception: "Bigger quorums are slower. Majority is required for correctness, not speed." },
    { label: "To guarantee that any two majorities share at least one node, so a new leader always inherits the history of committed entries", correct: true },
    { label: "To make the cluster tolerate 3 simultaneous failures", misconception: "A 5-node cluster tolerates only 2 failures — 3 of 5 must remain alive." },
    { label: "Because odd cluster sizes require it", misconception: "Majority is a protocol correctness requirement, not a consequence of cluster size parity." },
  ]}
/>

<DragOrder
  id="raft-jr-drag1"
  lessonSlug="01-raft-roles-and-terms"
  lang="en"
  prompt="Put the Raft leader-election steps in order:"
  items={[
    "Followers stop receiving heartbeats for longer than the election timeout",
    "A follower transitions to candidate, increments its term, and votes for itself",
    "The candidate sends RequestVote RPCs to all other nodes",
    "Each node grants its vote at most once per term, to the first eligible candidate",
    "The candidate collects a majority of votes and becomes leader for the new term",
    "The new leader starts sending heartbeats to assert its authority",
  ]}
/>

<MetaphorComplete
  id="raft-jr-metaphor1"
  lessonSlug="01-raft-roles-and-terms"
  lang="en"
  setup="Fill in the blank: Raft uses a council of N members where only one member at a time holds the _______ and proposes new laws."
  accepted={["gavel", "speaker role", "leadership", "mandate"]}
  canonical="gavel (or speaker role / leadership)"
  explanation="At most one speaker at a time, elected by majority vote, replaced when silent too long. Raft formalises exactly this discipline for computers."
/>

</Explanation>

<FlowDiagram
  label="The three Raft roles and the transitions between them"
  perRow={3}
  hue="var(--d-systems)"
  nodes={[
    { id: "follower", label: "Follower", sub: "stores log" },
    { id: "candidate", label: "Candidate", sub: "runs election" },
    { id: "leader", label: "Leader", sub: "drives writes" },
  ]}
  edges={[
    { from: "follower", to: "candidate", label: "timeout fires" },
    { from: "candidate", to: "leader", label: "wins majority" },
    { from: "leader", to: "follower", label: "sees higher term" },
    { from: "candidate", to: "follower", label: "split vote" },
  ]}
  caption="A node starts as Follower, becomes Candidate when its election timeout fires, and becomes Leader only after winning a majority vote. A higher term seen by a leader forces it back to Follower."
/>

<KeyTakeaway>Raft keeps consensus by electing one leader per term. Majority quorum (more than half) ensures any two quorums overlap — preventing two leaders from both committing, which is split brain.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="raft-jr-retrieval"
  lang="en"
  questions={[
    { q: "Why does a 5-node Raft cluster survive 2 simultaneous failures but not 3?", a: "Because commits and elections both require a majority — at least 3 of 5 nodes alive. With 3 failures only 2 nodes remain, which is below the majority threshold. Those 2 nodes cannot agree on anything, cannot elect a leader, and cannot commit new entries. The cluster waits for at least one failed node to return." },
    { q: "What is a Raft term and why does it replace wall-clock time?", a: "A term is a monotonically increasing integer that starts with each election. It deduplicates stale messages: any message with a lower term than the receiver's is rejected. Wall clocks differ between machines and can drift; terms are a shared logical counter that never goes backward and is always unambiguous." },
    { q: "A node in a Raft cluster has been offline for 10 minutes. It comes back with term 4, but the cluster is now on term 9. What happens when it sends a message?", a: "The receiver sees term 4, which is lower than its current term 9. The message is rejected, and the reply carries term 9. The returning node updates its term to 9 and steps down to follower. It then starts catching up from the leader via AppendEntries." },
  ]}
/>

<Recap lang="en">
Raft assigns each node one of three roles — follower, candidate, or leader — and exactly one leader exists per term. The term is a monotonic logical clock that resolves stale-leader confusion: higher term always wins. Both elections and commits require a majority quorum, which guarantees that any two quorums share at least one node — making it impossible for two separate leaders to both commit conflicting entries. A 5-node cluster tolerates 2 simultaneous failures; 3 failures drop the surviving 2 below the majority threshold and halt progress until recovery. The next lesson covers how the leader actually replicates writes to followers. Now when you see etcd report "leader changed" or "no leader elected," you know to check which of these three invariants — one leader per term, majority quorum, term monotonicity — was violated and why.
</Recap>
