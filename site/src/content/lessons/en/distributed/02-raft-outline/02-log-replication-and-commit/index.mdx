---
concepts:
- append-entries
- commit-index
- log-matching
- state-machine
- fsync
deepensInto:
- distributed/02-raft-outline/03-leader-election-in-depth
estMin: 14
lang: en
lessonType: topic
level: middle
order: 2
prereqs:
- distributed/02-raft-outline/01-raft-roles-and-terms
slug: 02-log-replication-and-commit
sources:
- https://raft.github.io/raft.pdf
- https://raft.github.io/
- https://www.pingcap.com/article/understanding-raft-consensus-in-distributed-systems-with-tidb/
spiral:
- statefulness
status: ready
summary: The AppendEntries RPC, the consistency check that keeps logs identical, the
  commit index, and the replicated state machine pattern built on top.
title: How Raft replicates a log entry and decides it is safe to commit
track: distributed
unit: 02-raft-outline
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
A client sends `SET x = 42` to a Raft cluster. The leader appends it to its log. But the leader alone cannot commit that write — it needs confirmation from enough followers that they have it too. If the leader crashes right after appending but before any follower responds, what happens to the write?
</Hook>

<Crux>A log entry is committed only when a majority has persisted it — uncommitted entries can vanish on leader crash, committed ones cannot.</Crux>

<Explanation>

## The log and AppendEntries

The Raft leader maintains an ordered log of entries. Each entry is a tuple: **(term, index, command)**. When a client sends a write:

1. Leader appends the entry to its own log and fsyncs to disk.
2. Leader sends `AppendEntries` RPC to every follower, carrying: the new entry, plus **prevIndex** and **prevTerm** — the index and term of the entry immediately before the new one.
3. Each follower checks: does my log have an entry at `prevIndex` with `prevTerm`? If yes, append and fsync. If no, reject with a mismatch reply.
4. Once the leader hears acknowledgement from a majority (including itself), it marks the entry **committed**.
5. The leader piggybacks its **commit index** on the next heartbeat. Followers apply entries up to that index to their state machines.

When you trace this sequence, notice that steps 1–3 are a two-phase process — write then confirm — and the commit only happens at step 4. Without that two-phase split, any leader crash between append and majority acknowledgement would silently lose the write.

<div data-lesson-visual class="overflow-x-auto my-6">

| Step | Who acts | What happens |
|------|----------|-------------|
| 1 | Leader | Appends (term=7, idx=101, cmd="SET x=42"), fsyncs |
| 2 | Leader | Sends AppendEntries to B, C, D, E with prevIdx=100, prevTerm=7 |
| 3 | Followers B, C, D, E | Each checks log at idx=100 has term=7. Appends, fsyncs, replies success |
| 4 | Leader | Receives 2 successes (plus own = 3 of 5). Marks idx=101 committed |
| 5 | Leader | Replies success to client |
| 6 | Next heartbeat | Carries commitIndex=101 — followers apply "SET x=42" to state machine |

</div>

## The consistency check: Log Matching

The `prevIndex`/`prevTerm` check is not bureaucracy — it is the mechanism that keeps every follower's log identical to the leader's. If a follower's log diverges (because a previous leader wrote different entries before crashing), the mismatch reply tells the leader to back up and retry with an earlier `prevIndex`. The leader keeps decrementing until it finds the last point of agreement, then overwrites the follower's divergent tail with its own.

This converges to identical logs because: any entry that was committed in a prior term was stored by a majority of nodes. The new leader's log (elected by majority) overlaps with that prior majority, so the new leader has those committed entries. Uncommitted entries on the old leader's disk get overwritten — they were never acknowledged to any client, so no data is lost.

## The replicated state machine pattern

Raft does not care what commands mean. Your application defines the **state machine** — a key-value map, a config tree, a relation — and the commands that mutate it. Raft guarantees: every node applies every committed entry in the same order. Since the commands are deterministic, every state machine ends up in the same state. This is the **replicated state machine** pattern: ordered replicated log + deterministic state machine = consistent distributed service.

Important consequences:
- Do not use `NOW()`, `random()`, or external API calls inside command handlers — these break determinism across replicas.
- The state machine is application-specific; Raft is agnostic to it.
- Reads for linearizability must go through the leader (covered in a later lesson); follower reads may return stale data.

<SequenceDiagram
  label="AppendEntries replication: the leader ships entry 101 to followers; only after a majority fsyncs it does the leader mark it committed and reply to the client."
  hue="var(--d-systems)"
  actors={["Leader", "F1", "F2", "F3 (slow)"]}
  messages={[
    { from: "Leader", to: "F1", label: "AppendEntries(prevIdx=100, entry 101)" },
    { from: "Leader", to: "F2", label: "AppendEntries(prevIdx=100, entry 101)" },
    { from: "Leader", to: "F3 (slow)", label: "AppendEntries(prevIdx=100, entry 101)" },
    { from: "F1", to: "Leader", label: "success (fsynced)" },
    { from: "F2", to: "Leader", label: "success (fsynced)" },
    { from: "Leader", to: "F1", label: "heartbeat(commitIndex=101)" },
    { from: "Leader", to: "F2", label: "heartbeat(commitIndex=101)" },
  ]}
  caption="Leader + F1 + F2 = 3 of 5 — quorum reached. Entry 101 is committed before F3 even replies. The next heartbeat carries commitIndex=101 so followers apply the command to their state machines."
/>

<Quiz
  id="raft-mid-q1"
  lessonSlug="02-log-replication-and-commit"
  lang="en"
  question="A Raft follower receives AppendEntries with prevIndex=50, prevTerm=4, but its own log at index 50 has term 3. What does the follower do?"
  choices={[
    { label: "Append the entry and trust the leader", misconception: "Appending without the prevTerm check would let logs diverge — exactly what Log Matching prevents." },
    { label: "Reject with a mismatch reply; the leader will retry with a lower prevIndex", correct: true },
    { label: "Promote itself to candidate", misconception: "Term comparison drives role changes, not log-index mismatches. The follower stays a follower." },
    { label: "Truncate its entire log and restart from scratch", misconception: "Truncation is incremental — the leader backs up one index at a time until it finds the matching point." },
  ]}
/>

<FadedExample
  client:load
  id="raft-mid-fe1"
  lessonSlug="02-log-replication-and-commit"
  lang="en"
  title="Trace a client write through a 5-node Raft cluster"
  steps={{
    solved: (
      <div class="space-y-3 text-sm">
        <p><strong>Setup:</strong> 5-node cluster (A, B, C, D, E). A is leader at term 7. All followers caught up to log index 100.</p>
        <p><strong>Client writes:</strong> "SET x = 42".</p>
        <ul class="list-disc list-inside ml-2 space-y-1">
          <li>A appends entry (term=7, index=101, cmd="SET x=42") and fsyncs.</li>
          <li>A sends AppendEntries to B, C, D, E: prevIndex=100, prevTerm=7, new entry.</li>
          <li>B and C check: log at 100 has term 7. Append, fsync, reply success.</li>
          <li>A counts: own + B + C = 3 of 5. Quorum reached. Marks 101 committed.</li>
          <li>A replies success to the client. Next heartbeat piggybacks commitIndex=101.</li>
        </ul>
      </div>
    ),
    semi: {
      prompt: "A crashes after fsyncing entry 101 to its own disk but before sending any AppendEntries. After election, what term does the new leader have?",
      blanks: [
        { id: "new_term", expected: "8", placeholder: "term number" },
      ],
    },
    blank: {
      prompt: "Same crash. Entry 101 was on A's disk only. Does the new leader B include entry 101 in the cluster history?",
      reveal: (
        <div class="space-y-2 text-sm">
          <p><strong>No.</strong> Entry 101 was never replicated and never committed. When B wins term 8, its log ends at index 100. B never sees 101. When A returns, B tells A: log diverges at 101 — truncate it. A obeys.</p>
          <p>The client never received a success for 101 (A crashed before replying), so the client either retries or treats it as a failure. No committed entry was lost; only an uncommitted attempt was discarded. This is Log Matching and Leader Append-Only at work.</p>
        </div>
      ),
    },
  }}
  misconceptions={{
    "new_term:7": "Term 7 belonged to A. B increments to start a new election, so the new leader is at term 8 minimum.",
  }}
/>

<DragOrder
  id="raft-mid-drag1"
  lessonSlug="02-log-replication-and-commit"
  lang="en"
  prompt="Put the AppendEntries consistency-check steps in order:"
  items={[
    "Leader picks the next log index to send to a follower",
    "Leader sends AppendEntries with prevIndex, prevTerm, and the new entry",
    "Follower checks: does my log have an entry at prevIndex with prevTerm?",
    "If yes, follower appends and replies success",
    "If no, follower replies mismatch with its conflicting index/term",
    "Leader decrements nextIndex for this follower and retries with an earlier prevIndex",
    "Eventually leader finds the last matching point; follower truncates its tail and accepts the leader's version",
  ]}
/>

</Explanation>

<SequenceDiagram
  label="AppendEntries round-trip that commits one entry by majority"
  hue="var(--d-systems)"
  actors={["Leader", "F1", "F2"]}
  messages={[
    { from: "Leader", to: "F1", label: "AppendEntries(prevIdx=100, entry 101)" },
    { from: "Leader", to: "F2", label: "AppendEntries(prevIdx=100, entry 101)" },
    { from: "F1", to: "Leader", label: "success" },
    { from: "F2", to: "Leader", label: "success" },
    { from: "Leader", to: "F1", label: "heartbeat(commitIndex=101)" },
    { from: "Leader", to: "F2", label: "heartbeat(commitIndex=101)" },
  ]}
  caption="The leader plus F1 and F2 acking equals 3 of 5 — a majority — so entry 101 is committed. The next heartbeat carries commitIndex=101 and followers apply the entry to their state machines."
/>

<KeyTakeaway>AppendEntries replicates one entry at a time, gated on a prevIndex/prevTerm check that keeps every follower's log identical to the leader's. An entry is committed — and safe forever — only once a majority has fsynced it. Uncommitted entries on a crashed leader's disk are silently discarded.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="raft-mid-retrieval"
  lang="en"
  questions={[
    { q: "Why is a Raft log entry not committed the instant the leader writes it to disk?", a: "Because 'committed' in Raft means a majority of nodes have persisted the entry. If only the leader has it, a crash and a new election would elect a leader that lacks the entry, and it would be overwritten. Only once a majority has fsynced does the entry survive any possible leader change. The leader signals commitment by updating its commit index, which it piggybacks on the next AppendEntries or heartbeat." },
    { q: "A follower was offline for 2 minutes and missed 500 log entries. It comes back. How does it catch up?", a: "The leader tracks a nextIndex for each follower — the next log index it should send. On reconnect the follower starts rejecting AppendEntries until the leader backs up to the last matching prevIndex/prevTerm. Once found, the leader sends the missing entries in order. If the follower has fallen so far behind that the leader has already compacted those entries into a snapshot, the leader sends InstallSnapshot instead." },
    { q: "Why must command handlers in a Raft state machine be deterministic?", a: "Every node applies every committed entry independently and must end up in the same state. If a command calls NOW(), random(), or makes a network request, different nodes return different values for the same command, and state diverges across replicas. All non-deterministic values must be computed by the leader and embedded in the log entry as constants before replication." },
  ]}
/>

<Recap lang="en">
The leader replicates writes via AppendEntries, which carries a prevIndex/prevTerm consistency check that forces every follower's log to converge to the leader's. An entry is committed when a majority has persisted it; the leader then broadcasts the commit index so followers can apply the entry to their state machines. This is the replicated state machine pattern: identical ordered logs plus deterministic handlers equals identical state on every replica. Uncommitted entries from a crashed leader are safe to discard — they were never acknowledged. The fsync cost on every commit is the dominant operational expense, which is why Raft workloads need dedicated NVMe, not shared cloud volumes. Now when you see a write hang or a "unable to commit" error, ask: did the majority receive it, or did the leader crash before the quorum ack — those are different recoveries.
</Recap>
