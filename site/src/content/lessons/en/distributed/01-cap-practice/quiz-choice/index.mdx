---
concepts:
- cap-theorem
- cp-ap-tradeoff
- pacelc-theorem
- linearizability
- logical-partition
- conflict-resolution
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://en.wikipedia.org/wiki/CAP_theorem
- https://en.wikipedia.org/wiki/PACELC_theorem
- https://www.infoq.com/articles/cap-twelve-years-later-how-the-rules-have-changed/
status: ready
summary: "Multiple-choice synthesis across the CAP unit — formal C/A definitions, the CP/AP partition choice, PACELC's healthy-state tradeoff, logical partitions, and AP conflict resolution."
title: 'CAP in practice: multiple-choice review'
track: distributed
unit: 01-cap-practice
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each mirrors a decision you make in a real on-call incident — not an acronym to recite, but a tradeoff to defend while the network is silent.
</Hook>

<Goal lang="en">
Confirm you can connect the formal CAP definitions, the binary CP/AP partition choice, PACELC's healthy-state latency tradeoff, and the production failure modes — the synthesis the lesson built toward.
</Goal>

<Quiz
  id="01-cap-practice-choice-1"
  lessonSlug="01-cap-practice"
  lang="en"
  question="A vendor markets its database as 'always available, fully consistent, partition tolerant — CAP-complete.' Why is this claim incoherent under the Gilbert-Lynch proof?"
  choices={[
    { label: "During a partition you provably cannot have both linearizability and a non-error response on every non-failing node — one side must either block/error (sacrificing A) or serve possibly-stale data (sacrificing C). 'All three at once' contradicts the theorem.", correct: true },
    { label: "The claim is fine — modern consensus like Raft gives you all three simultaneously.", misconception: "Raft is CP: when a partition isolates the leader from the majority, the minority rejects reads and writes to preserve linearizability. It buys consistency by giving up availability on the isolated side, not by escaping the theorem." },
    { label: "It is incoherent only because no real network is ever partitioned.", misconception: "Partitions are physically inevitable — cut fibers, BGP flaps, hypervisor pauses. P is mandatory, which is exactly why C-vs-A becomes a forced choice rather than a free lunch." },
    { label: "It is fine as long as the cluster keeps 99.999% uptime.", misconception: "Uptime is an operational SLA, not the per-request liveness property CAP defines. A 'five nines' cluster can still be formally CP and reject requests during a partition." },
  ]}
/>

<Quiz
  id="01-cap-practice-choice-2"
  lessonSlug="01-cap-practice"
  lang="en"
  question="A 5-node etcd cluster splits 2-3 by a partition. The 2-node side rejects all reads and writes with errors; the 3-node side keeps serving. How do you classify this behaviour?"
  choices={[
    { label: "CP. The minority refuses to act to preserve linearizability — it would rather return an error than risk a stale or split-brain write. Formally that minority is unavailable, which is the deliberate CP choice.", correct: true },
    { label: "AP, because the 3-node majority stayed available.", misconception: "Availability in CAP requires every non-failing node to respond successfully. The 2-node minority returns errors, so the system as a whole sacrifices A — this is CP, not AP." },
    { label: "It is neither CP nor AP — it is a bug, the minority should keep serving.", misconception: "A minority that kept serving writes would split-brain and break linearizability. Refusing is the correct, intended CP behaviour for a quorum system like etcd." },
    { label: "AP, because no data was lost.", misconception: "No-data-loss is a durability property, unrelated to the CAP A/C axis. The minority being unable to respond successfully is precisely what makes this CP." },
  ]}
/>

<Quiz
  id="01-cap-practice-choice-3"
  lessonSlug="01-cap-practice"
  lang="en"
  question="Two engineers both pick 'CP'. One says reads in another region now add 80 ms; the other insists CP changes nothing when the network is healthy. Who is right, and why?"
  choices={[
    { label: "The first. CAP only describes partitions; PACELC adds the healthy-state axis: choosing consistency normally (EC) forces synchronous cross-replica coordination, so the round-trip latency is paid on every request even with zero partitions.", correct: true },
    { label: "The second — without a partition, consistency is free.", misconception: "PACELC's whole point is that strong consistency costs latency even when healthy: a linearizable read/write must coordinate synchronously across replicas, adding RTT. PC/EC systems pay this constantly." },
    { label: "Both are wrong — latency depends only on disk speed.", misconception: "Cross-region coordination latency is network RTT between replicas, not disk. Synchronous quorum acknowledgement across datacenters is the dominant cost for EC systems." },
    { label: "The first, but only because the cluster is misconfigured.", misconception: "The 80 ms is the inherent cost of synchronous coordination (EC), not a misconfiguration. PACELC predicts exactly this latency-for-consistency trade in the healthy 'Else' state." },
  ]}
/>

<Quiz
  id="01-cap-practice-choice-4"
  lessonSlug="01-cap-practice"
  lang="en"
  question="Classify Google Spanner and Amazon DynamoDB (default) on the PACELC axis, and say what that predicts about each."
  choices={[
    { label: "Spanner is PC/EC: it rejects writes when a quorum is unreachable and uses synchronous Paxos/TrueTime when healthy, trading latency for consistency. DynamoDB is PA/EL: replicas accept writes independently during partitions and default to eventually-consistent reads for sub-10 ms latency.", correct: true },
    { label: "Both are PA/EL — they are cloud databases, so they optimise for availability.", misconception: "Being cloud-hosted says nothing about the CAP/PACELC stance. Spanner deliberately chooses consistency (PC/EC) and accepts the coordination latency; only DynamoDB defaults to PA/EL." },
    { label: "Spanner is PA/EL because Google has no downtime; DynamoDB is PC/EC.", misconception: "This is reversed. Spanner blocks rather than serve stale data (PC/EC); DynamoDB stays available and eventually consistent by default (PA/EL)." },
    { label: "PACELC types are marketing labels with no behavioural meaning.", misconception: "They predict concrete behaviour: PC/EC rejects writes under partition and adds RTT when healthy; PA/EL keeps serving and can return stale reads. The labels map to observable decisions." },
  ]}
/>

<Quiz
  id="01-cap-practice-choice-5"
  lessonSlug="01-cap-practice"
  lang="en"
  question="A healthy CP leader survives the network fine but suffers a 10-second stop-the-world GC pause; peers immediately trigger a leader re-election. What is the root cause class?"
  choices={[
    { label: "A logical (not physical) partition: the paused node stops answering heartbeats, so peers conclude it failed and re-elect — even though no link was ever cut. Slow is indistinguishable from dead to a failure detector.", correct: true },
    { label: "A clock-skew bug — Raft needs synchronized wall clocks and GC broke them.", misconception: "Raft uses logical terms and sequence numbers, not wall-clock synchronization. The trigger is missed heartbeats from the paused node, not clock skew." },
    { label: "GC physically disabled the network card.", misconception: "A GC pause stalls application threads; it never touches the NIC or OS network stack. The node is reachable but unresponsive — a logical partition." },
    { label: "Nothing is wrong — re-election on a healthy leader is harmless.", misconception: "It is disruptive: the cluster loses its leader, blocks writes during the election, and the old leader's in-flight work is wasted. This is why pre-vote and tuned timeouts matter." },
  ]}
/>

<Quiz
  id="01-cap-practice-choice-6"
  lessonSlug="01-cap-practice"
  lang="en"
  question="You run an AP store (Cassandra/DynamoDB style) and resolve concurrent writes with Last-Write-Wins on wall-clock timestamps. What is the hidden failure mode?"
  choices={[
    { label: "Slightly unsynchronized node clocks make LWW silently drop a legitimate later write whose timestamp happens to be lower — irreversible data loss with no error surfaced. CRDTs or vector clocks avoid this at the cost of complexity.", correct: true },
    { label: "LWW is safe because the most recent write always wins.", misconception: "'Most recent' is decided by clock timestamps, and clocks drift. A genuinely-later write on a lagging node loses to an earlier write with a higher timestamp — LWW silently discards real data." },
    { label: "LWW only fails if you lose a whole datacenter.", misconception: "It fails on ordinary concurrent writes across partitions with skewed clocks — no datacenter loss required. This is the everyday AP conflict-resolution tax." },
    { label: "The fix is to switch to a CP database, there is no AP-side option.", misconception: "You can stay AP and fix conflict resolution with CRDTs or vector clocks, which merge or order writes without a single wall-clock winner. Switching to CP is one option, not the only one." },
  ]}
/>

<Recap lang="en">
The through-line: P is not a choice, so a partition forces the binary CP-vs-AP decision — block/error to keep linearizability, or stay responsive and serve divergence. PACELC extends this to the healthy state, where strong consistency still costs latency. The production traps — vendor 'CAP-complete' claims, logical partitions from GC pauses, and LWW silently dropping writes under clock skew — all resolve back to the same discipline: use the formal definitions, classify your system honestly on PC/EC vs PA/EL, and tune to the workload's real correctness needs.
</Recap>
