---
concepts:
- read-write-quorum
- rw-overlap-invariant
- tunable-consistency
- sloppy-quorum
- hinted-handoff
- read-repair
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://cassandra.apache.org/doc/latest/cassandra/architecture/dynamo.html
- https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/HowItWorks.ReadConsistency.html
- https://www.allthingsdistributed.com/2007/10/amazons_dynamo.html
status: ready
summary: "Multiple-choice synthesis across the quorum unit: R + W > N arithmetic, tunable consistency, sloppy quorum and hinted handoff, and the silent production failure modes."
title: 'Quorums: multiple-choice review'
track: distributed
unit: 03-quorum
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each is a decision you make when you size a cluster or debug a stale-read ticket — not a definition to recite, but R, W, and N arithmetic under real failures.
</Hook>

<Goal lang="en">
Confirm you can derive the overlap guarantee from R + W > N, place a config on the consistency/availability/latency triangle, and predict exactly which guarantee sloppy quorum forfeits during a partition.
</Goal>

<Quiz
  id="03-quorum-choice-1"
  lessonSlug="03-quorum"
  lang="en"
  question="A cluster runs RF=5. You need every read guaranteed to see the latest successful write while tolerating as many node failures as possible. What is the minimal (R, W) that still gives strong consistency, and how many simultaneous node failures does it survive on each path?"
  choices={[
    { label: "R=3, W=3 — sum 6 > 5, so the sets must overlap; each path needs 3 of 5, so both reads and writes tolerate 2 nodes down. This is QUORUM at N=5.", correct: true },
    { label: "R=2, W=3 — sum 5, a majority write plus a small read is enough.", misconception: "5 is not strictly greater than 5. R + W must be > N, not >= N. With R=2, W=3 a read can pick 2 replicas that both missed the write and return stale data." },
    { label: "R=1, W=5 — write to ALL guarantees any single read is current.", misconception: "Sum 6 > 5 does guarantee overlap, but W=5 means a single node down fails every write — it tolerates 0 write failures, the opposite of 'as many failures as possible.' QUORUM R=3,W=3 tolerates 2 on each path." },
    { label: "R=5, W=1 — write fast, read ALL to catch the latest.", misconception: "Sum 6 > 5 gives overlap, but R=5 means one slow or down node blocks every read, tolerating 0 read failures, and W=1 risks losing the ack if the lone node dies. QUORUM balances both paths." },
  ]}
/>

<Quiz
  id="03-quorum-choice-2"
  lessonSlug="03-quorum"
  lang="en"
  question="A payments service at RF=3 runs writes at W=1 'for latency.' One night a node acks a payout-account update, then crashes during a deploy before replicating. The next read serves the old account number. What actually went wrong?"
  choices={[
    { label: "Two compounding failures: W=1 acked the write on a single replica that died before replicating, so it lived on zero live nodes — a lost write; and with R + W not greater than N the read was free to contact replicas that never had it. Neither errored, because the config defined the loss away.", correct: true },
    { label: "A bug in the coordinator's replication code dropped the write.", misconception: "Replication behaved exactly as configured. W=1 means 'ack after one replica' — the runtime never promised the other two would receive it before acking. The loss is a configuration choice, not a code defect." },
    { label: "The read should have used R=3 to force consistency.", misconception: "R=3 with W=1 sums to 4 > 3 and would have caught a surviving copy — but here the only copy died, so even R=3 finds nothing. The root cause is W=1 durability, not the read setting." },
    { label: "The deploy was the problem; with no deploy the write is safe.", misconception: "A deploy is just one trigger. Any crash, GC pause, or disk failure in the window after a W=1 ack and before replication loses the write. W=1 makes single-replica failure a data-loss event." },
  ]}
/>

<Quiz
  id="03-quorum-choice-3"
  lessonSlug="03-quorum"
  lang="en"
  question="Same RF=3 cluster, configured strict W=2, R=2. During a network partition a write lands via sloppy quorum on hint-holding substitute nodes, and a strict read on the home replicas returns stale data. Why does W=2, R=2 not save you here?"
  choices={[
    { label: "Sloppy quorum keeps W=2 but satisfies it with substitute nodes outside the normal read set, so the read's 2 home replicas can both miss the hinted write. R + W > N overlap holds only against the designated replicas, and during the failure the write isn't on them.", correct: true },
    { label: "Sloppy quorum silently lowers W to 1 during partitions.", misconception: "It keeps your W count — two nodes still ack. What changes is WHICH nodes can satisfy it: healthy substitutes via hinted handoff, not the designated replicas. The counts are unchanged; the membership is." },
    { label: "Read repair was off, so the stale replica never got fixed.", misconception: "Read repair can only push a newer version it can see. During the partition the latest write is on hint-holders the read never contacts, so there is no fresher copy at the read for repair to propagate." },
    { label: "R=2 is too low; R=3 would have found the write.", misconception: "R=3 reads all three home replicas, but the hinted write is on substitute nodes outside that set, so R=3 still misses it mid-partition. The hole is the suspended overlap, not the R value." },
  ]}
/>

<Quiz
  id="03-quorum-choice-4"
  lessonSlug="03-quorum"
  lang="en"
  question="A team argues 'R=1, W=2 is fine at N=3 because the write is already on a majority.' What is wrong with the reasoning?"
  choices={[
    { label: "R + W = 3, which is not strictly greater than 3, so the single read replica can be the one node that lagged the write. A majority on the write path does not help if the lone read replica is outside that majority — overlap needs > N, not a majority write alone.", correct: true },
    { label: "Nothing — a majority write plus any read is always consistent.", misconception: "A majority write guarantees the value is on 2 of 3, but R=1 can pick the remaining 1 that missed it. Consistency comes from forced intersection (R + W > N), not from the write being on a majority." },
    { label: "It is wrong because W=2 is itself too weak at N=3.", misconception: "W=2 is exactly QUORUM and is fine — paired with R=2. The flaw is R=1, which drops the sum to 3 and breaks overlap. Fix the read, not the write." },
    { label: "It is fine as long as reads always hit the coordinator node.", misconception: "There is no guarantee the coordinator holds the key or the latest version; coordinators route, they are not necessarily replicas. Overlap must come from the R + W > N math, not from routing luck." },
  ]}
/>

<Quiz
  id="03-quorum-choice-5"
  lessonSlug="03-quorum"
  lang="en"
  question="At RF=3 a QUORUM read (R=2) shows p99 latency far worse than its p50, while the cluster's per-node latencies look healthy. What is the mechanism, and what mitigation targets it directly?"
  choices={[
    { label: "A QUORUM read waits for 2 of 3 replicas, so its latency is the second-fastest of three — one slow replica (GC pause, hot partition, throttled disk) drags p99 even when the cluster is fine. Request hedging — fire a duplicate after a short delay and take the first to return — clips that tail.", correct: true },
    { label: "QUORUM reads are CPU-bound; add cores to the coordinator.", misconception: "The latency is dominated by waiting on the slowest required replica, not coordinator CPU. More cores do nothing for a read blocked on a GC-paused replica; hedging or lowering R does." },
    { label: "The p99 is from read repair writing back on every read.", misconception: "Read repair fires only when replicas disagree and is usually cheap. The structural p99 driver is 'wait for the 2nd-fastest of 3' — a tail-latency property of R itself." },
    { label: "Lower R to 1 to remove the wait — it has no consistency cost.", misconception: "R=1 does cut the wait, but at N=3 with W=2 it drops R + W to 3 and breaks the overlap guarantee. Hedging clips the tail WITHOUT sacrificing consistency; dropping R trades correctness for latency." },
  ]}
/>

<Quiz
  id="03-quorum-choice-6"
  lessonSlug="03-quorum"
  lang="en"
  question="Overlap (R + W > N) guarantees a quorum read sees the latest committed write. Which of these does it NOT guarantee?"
  choices={[
    { label: "Ordering of concurrent writes. Two clients writing the same key at W=2 can both succeed against different majorities at the same logical time; quorum stores both as siblings or resolves by last-write-wins. Overlap is about visibility of a completed write, not serialising racing writes — that needs LWT/Paxos.", correct: true },
    { label: "That a completed write is visible to a subsequent quorum read.", misconception: "This is precisely what overlap DOES guarantee: the read set and write set share a node carrying the latest value. The thing quorum does not give you is ordering of concurrent writes." },
    { label: "Tolerance of a single node failure on both paths at W=2, R=2.", misconception: "QUORUM does provide single-node-failure tolerance on both paths — that is why it beats ALL. The missing guarantee is serialisation of concurrent writes to the same key." },
    { label: "That R and W can be tuned per operation.", misconception: "Tunable consistency genuinely lets you set R and W per query. The guarantee overlap does NOT provide is ordering concurrent writes — visibility is not serialisation." },
  ]}
/>

<Recap lang="en">
The through-line is one inequality: R + W > N forces the read and write replica sets to intersect, so a quorum read sees the latest committed write — and the moment you tune below it (R=1,W=2 summing to N, or W=1), stale reads and lost writes become legal, not bugs. QUORUM (W=2,R=2 at N=3; W=3,R=3 at N=5) is the production default because it keeps overlap while tolerating one (or two at N=5) node down on both paths, unlike ALL. R is also a p99 knob — a quorum read waits on its slowest required replica, which is why hedging exists. And sloppy quorum buys write availability during partitions by parking writes on hint-holders outside the read set, suspending overlap exactly when a node is down; read repair and Merkle-tree anti-entropy converge it afterward, usually.
</Recap>
