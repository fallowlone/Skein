---
concepts:
- clock-skew
- lamport-timestamps
- vector-clocks
- happens-before
- last-write-wins
- truetime
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://aphyr.com/posts/299-the-trouble-with-timestamps
- https://research.google.com/pubs/archive/39966.pdf
- https://en.wikipedia.org/wiki/Vector_clock
status: ready
summary: "Multiple-choice synthesis across the clocks unit: wall-clock skew, Lamport vs vector clocks, happens-before, concurrency detection, and TrueTime."
title: 'Clocks: multiple-choice review'
track: distributed
unit: 05-clocks
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each mirrors a real decision designing or debugging a distributed store — not a definition to recite, but a choice about how to order events when the wall clock lies.
</Hook>

<Goal lang="en">
Confirm you can connect clock skew, the choice between physical and logical clocks, Lamport vs vector semantics, happens-before, and TrueTime — the synthesis the lesson built toward.
</Goal>

<Quiz
  id="05-clocks-choice-1"
  lessonSlug="05-clocks"
  lang="en"
  question="A Cassandra cluster uses last-write-wins on client timestamps. One node's clock is 1.5s behind. A user's later update routed through that node vanishes after a refresh, with a 200 OK returned. What is the root cause?"
  choices={[
    { label: "LWW keeps the row with the highest timestamp. The skewed node stamped the genuinely newer write with a smaller (past) timestamp, so conflict resolution discarded it as stale — which is not an error condition, hence the success response.", correct: true },
    { label: "The write failed quorum and was silently rolled back.", misconception: "The write was accepted and acknowledged; nothing was rolled back. It was lost later during timestamp-based conflict resolution — a silent drop, not a failed write." },
    { label: "NTP would have prevented this entirely if it were running.", misconception: "NTP shrinks skew on average but LWW fails on the worst case. A step can move a clock backward hundreds of ms, and a stalled ntpd or paused VM leaves seconds of skew — exactly the window that loses data." },
    { label: "Cassandra read stale data from a lagging replica; read-repair will fix it.", misconception: "The newer write was permanently dropped by the LWW rule, not merely lagging. Read-repair propagates the surviving (wrong) value; it does not resurrect the discarded one." },
  ]}
/>

<Quiz
  id="05-clocks-choice-2"
  lessonSlug="05-clocks"
  lang="en"
  question="You need conflict resolution in a replicated KV store that must never silently drop a concurrent update. Lamport timestamps or vector clocks?"
  choices={[
    { label: "Vector clocks — they can detect concurrency. Comparing two vectors tells you whether one happened-before the other or whether neither dominates (concurrent), so the store keeps both as siblings to merge.", correct: true },
    { label: "Lamport timestamps — they give a total order, which is exactly what conflict resolution needs.", misconception: "A total order is the problem here, not the solution: Lamport imposes an arbitrary order on concurrent writes and cannot tell them apart from a causal chain, so the store deterministically picks one and drops the other." },
    { label: "Either works; the difference is only space cost.", misconception: "The semantics differ fundamentally. Lamport cannot detect concurrency at all; vector clocks can. Picking Lamport here means silently dropping concurrent writes, regardless of space." },
    { label: "Neither — use wall-clock timestamps with tighter NTP.", misconception: "Wall-clock LWW is the exact failure being avoided; tighter NTP only narrows the window of loss. The requirement is to DETECT concurrency, which needs logical clocks." },
  ]}
/>

<Quiz
  id="05-clocks-choice-3"
  lessonSlug="05-clocks"
  lang="en"
  question="Lamport's clock guarantees that if A happens-before B then L(A) &lt; L(B). Why can't you use L(A) &lt; L(B) to conclude that A caused B?"
  choices={[
    { label: "The implication runs one way only. Two events on nodes that never communicated are concurrent, yet Lamport still assigns them some order — so a smaller timestamp can simply be the arbitrary side of a concurrent pair, not a causal ancestor.", correct: true },
    { label: "Because Lamport timestamps can collide, making the comparison undefined.", misconception: "Ties are broken deterministically (e.g. by node id) to get a total order. The real gap is that the order is total even over events with no causal relation — concurrency is invisible." },
    { label: "Because the counter overflows on long-running systems.", misconception: "Overflow is an implementation footnote, not the semantic gap. The fundamental issue is that Lamport encodes a total order that cannot distinguish causality from concurrency." },
    { label: "You can — a smaller Lamport timestamp always means a causal ancestor.", misconception: "That is the exact misconception. Lamport gives consistency with causality (A before B implies a smaller timestamp) but not the converse; concurrent events get ordered anyway." },
  ]}
/>

<Quiz
  id="05-clocks-choice-4"
  lessonSlug="05-clocks"
  lang="en"
  question="Your vector clocks are correct, but a 200-node cluster now ships 200 counters of metadata with every write, and it grows as you add nodes. What is the real tradeoff you accepted?"
  choices={[
    { label: "O(n) metadata per stamped value that grows with the cluster, in exchange for the ability to detect concurrency. Real systems prune or cap the vectors (e.g. scope per actor, dotted version vectors) to bound the bloat.", correct: true },
    { label: "Vector clocks are wrong at this scale; switch back to Lamport.", misconception: "Lamport is O(1) but cannot detect concurrency — switching back reintroduces silent conflict drops. The cost is metadata size, not correctness; the answer is to prune, not abandon detection." },
    { label: "The metadata makes ordering slower; it has no correctness benefit.", misconception: "The metadata is exactly what buys concurrency detection — the correctness benefit you needed. The tradeoff is space, not wasted cost." },
    { label: "Nothing — vector clocks are O(1) like Lamport.", misconception: "Vector clocks are O(n) in the number of nodes by construction; that growth is the whole reason production systems prune or scope the vectors." },
  ]}
/>

<Quiz
  id="05-clocks-choice-5"
  lessonSlug="05-clocks"
  lang="en"
  question="What does Spanner's TrueTime expose, and what does commit-wait do with it?"
  choices={[
    { label: "TT.now() returns an interval [earliest, latest] whose width is the uncertainty ε (about 1ms). Commit-wait sleeps until TT.now().earliest exceeds the commit timestamp — until the timestamp is certainly in the past everywhere — buying external consistency.", correct: true },
    { label: "TrueTime returns one exact instant from GPS, and commit-wait waits for all replicas to flush to disk.", misconception: "TrueTime never claims an exact instant; it returns a bounded interval. Flushing to disk is durability, a separate concern — commit-wait specifically outlasts the ε uncertainty interval." },
    { label: "Commit-wait blocks new transactions until the previous one's locks release.", misconception: "That is lock contention, not commit-wait. Commit-wait is about clock uncertainty and overlaps with Paxos replication, so it often adds little real latency." },
    { label: "Commit-wait re-reads the clock until two consecutive reads agree exactly.", misconception: "There is no exact reading to converge on — TrueTime is an interval. Commit-wait sleeps out the interval rather than chasing agreement between reads." },
  ]}
/>

<Quiz
  id="05-clocks-choice-6"
  lessonSlug="05-clocks"
  lang="en"
  question="A delete in an LWW store occasionally suppresses every real write to a key for days, even writes that arrive long after the delete. What is the mechanism?"
  choices={[
    { label: "A tombstone stamped with a far-future timestamp (a buggy client or a node whose clock jumped forward) outranks every later real write under LWW until compaction garbage-collects the tombstone — which can be days away.", correct: true },
    { label: "The delete acquired an exclusive lock on the key that never released.", misconception: "LWW stores resolve by timestamp, not locks. The suppression comes from a future-dated tombstone winning every comparison, not a held lock." },
    { label: "Replication lag is hiding the new writes until the cluster catches up.", misconception: "These writes are not lagging — they are stored and then lose the LWW comparison to a future-timestamped tombstone. Catching up does not help; the tombstone still wins." },
    { label: "The key was never indexed, so reads miss the new writes.", misconception: "Indexing is unrelated. The writes are found and compared; they lose to the tombstone's larger timestamp until it is compacted away." },
  ]}
/>

<Recap lang="en">
The through-line: wall-clock time is not an ordering primitive, so LWW turns skew into silent data loss (and future-dated tombstones into days-long key suppression). Logical clocks order by causality instead — Lamport gives a cheap O(1) total order but cannot detect concurrency, so vector clocks add a per-node counter to flag concurrent writes at O(n) metadata that must be pruned. TrueTime takes the third path: quantify clock uncertainty as a bounded interval ε and commit-wait it away for real external consistency. Now when you see a conflict-resolution design in review, ask one question first: does this mechanism need to detect concurrency, or just impose an order? That answer tells you which of these three paths belongs in the code.
</Recap>
