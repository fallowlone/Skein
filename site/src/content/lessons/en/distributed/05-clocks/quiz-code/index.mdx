---
concepts:
- lamport-timestamps
- vector-clocks
- happens-before
- concurrency-detection
- clock-skew
estMin: 14
lang: en
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://en.wikipedia.org/wiki/Vector_clock
- https://aphyr.com/posts/299-the-trouble-with-timestamps
status: ready
summary: "Read clock-update code and event traces — Lamport increment, vector-clock merge, happens-before comparison, and concurrency detection — and predict the result."
title: 'Clocks: code and trace reading'
track: distributed
unit: 05-clocks
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Logical clocks are tiny pieces of code with subtle correctness rules. Read each update routine and event trace, then pick the answer a senior engineer would defend in review — where the bug, the order, or the concurrency verdict actually lives.
</Hook>

<Goal lang="en">
Practise the core operations of logical clocks in code: a Lamport increment on send and receive, a vector-clock merge, the happens-before comparison, and detecting that two events are concurrent rather than ordered.
</Goal>

## Snippet 1 — the Lamport receive

```python
class LamportClock:
    def __init__(self):
        self.t = 0

    def on_local_event(self):
        self.t += 1
        return self.t

    def on_send(self):
        self.t += 1
        return self.t            # piggyback self.t on the message

    def on_receive(self, msg_t):
        self.t = self.t          # <-- BUG candidate
        self.t += 1
        return self.t
```

<Quiz
  id="05-clocks-code-1"
  lessonSlug="05-clocks"
  lang="en"
  question="The receive handler ignores msg_t. What invariant does this break, and what is the fix?"
  choices={[
    { label: "It breaks the happens-before guarantee: a send must causally precede its receive, so the receiver's clock must jump past the sender's. Fix: self.t = max(self.t, msg_t) + 1, so a message from a higher clock pulls the receiver forward.", correct: true },
    { label: "Nothing breaks — incrementing by one on receive is enough for a Lamport clock.", misconception: "Plain increment ignores the sender's progress. If the sender's clock was far ahead, the receive event can end up with a SMALLER timestamp than the send, violating that send happens-before receive." },
    { label: "The bug is that on_receive should reset self.t to msg_t exactly.", misconception: "Resetting to msg_t can move your clock backward and drops your own local progress. The rule is max of the two plus one — never just adopt the incoming value." },
    { label: "It double-counts the event; on_receive should not increment at all.", misconception: "The receive IS an event and must advance the clock. The defect is dropping msg_t, not the increment; the fix takes the max first, then increments." },
  ]}
/>

## Snippet 2 — the vector-clock merge

```python
def merge_receive(local, msg_vec, my_id):
    # local and msg_vec are dicts: node_id -> counter
    merged = {}
    for node in set(local) | set(msg_vec):
        merged[node] = max(local.get(node, 0), msg_vec.get(node, 0))
    merged[my_id] += 1
    return merged
```

<Quiz
  id="05-clocks-code-2"
  lessonSlug="05-clocks"
  lang="en"
  question="This receive merge is correct. Which statement captures why the elementwise max plus a single self-increment is exactly right?"
  choices={[
    { label: "Taking the elementwise max absorbs everything the sender has seen (including events this node hadn't); incrementing only my_id records that the receive is one new local event. Together they preserve happens-before across the message.", correct: true },
    { label: "It should increment every component, not just my_id, to keep the vector in sync.", misconception: "Incrementing every component would falsely claim this node observed new events on every other node. Only my own counter advances on a local/receive event; the rest are merged via max." },
    { label: "It should sum the two vectors componentwise instead of taking the max.", misconception: "Summing double-counts events both sides already share and breaks the comparison semantics. The max keeps each component at the latest count each party has actually seen." },
    { label: "It is buggy because it does not increment the sender's component.", misconception: "The receiver must never advance the sender's counter — only the sender owns it. The receiver bumps its own component and maxes the rest." },
  ]}
/>

## Snippet 3 — comparing two vector clocks

```python
def compare(a, b):
    # returns 'before', 'after', or 'concurrent'
    le = all(a.get(k, 0) <= b.get(k, 0) for k in set(a) | set(b))
    ge = all(a.get(k, 0) >= b.get(k, 0) for k in set(a) | set(b))
    if le and not ge: return 'before'      # a happens-before b
    if ge and not le: return 'after'
    if le and ge:     return 'equal'
    return 'concurrent'
```

```
A = {n1: 2, n2: 1, n3: 0}
B = {n1: 1, n2: 1, n3: 4}
```

<Quiz
  id="05-clocks-code-3"
  lessonSlug="05-clocks"
  lang="en"
  question="What does compare(A, B) return for these two vectors, and what does it mean for conflict resolution?"
  choices={[
    { label: "'concurrent' — A leads on n1 (2 vs 1) while B leads on n3 (4 vs 0), so neither dominates. The store must keep both versions as siblings rather than pick a winner.", correct: true },
    { label: "'before' — B's total (6) is larger than A's (3), so A happened-before B.", misconception: "Vector clocks are compared componentwise, never by summing. A leads on n1 and B leads on n3, so neither is uniformly less-or-equal — that is the definition of concurrent." },
    { label: "'after' — A has the highest single component (n1 = 2).", misconception: "A single larger component does not make A 'after'; you need EVERY component of A to be greater-or-equal. Here B beats A on n3, so they are concurrent, not ordered." },
    { label: "'equal' — both are valid clock states so they are interchangeable.", misconception: "Equal requires identical counts on every node. These differ on n1 and n3 in opposite directions, which is precisely the concurrent case the merge function must surface." },
  ]}
/>

## Snippet 4 — the event trace

```
P1: a(send m1)            Lamport: a=1
P2: b(recv m1) -> c(send m2)
P3: d(recv m2)
P1: e(local)              Lamport: e=2
```

<Quiz
  id="05-clocks-code-4"
  lessonSlug="05-clocks"
  lang="en"
  question="Using happens-before over this trace, what is the relationship between e on P1 and d on P3?"
  choices={[
    { label: "Concurrent. The causal chain is a -> b -> c -> d (via m1 then m2); e is a later local event on P1 that never communicated after a, so no message links e to d in either direction.", correct: true },
    { label: "e happens-before d, because e has Lamport timestamp 2 and the chain to d is longer.", misconception: "Lamport timestamps give a total order, but a smaller-or-larger Lamport value does NOT imply causality. There is no message path from e to d, so they are concurrent regardless of the numbers." },
    { label: "d happens-before e, because d is on P3 which started the message chain.", misconception: "P3 receives m2; it does not start the chain (P1's a does), and no message flows from d back to P1's e. Without a connecting message path, the two events are concurrent." },
    { label: "They are the same event observed on two nodes.", misconception: "e and d are distinct events on different processes. Happens-before asks whether a message path connects them; here none does, so they are concurrent." },
  ]}
/>

<Recap lang="en">
The clock operations are small but unforgiving: a Lamport receive must take max(local, msg) + 1 or it can stamp a receive earlier than its send; a vector-clock receive takes the elementwise max and bumps only its own component; comparing vectors is componentwise, and when each side leads on some node the events are concurrent and must be kept as siblings. And no Lamport number ordering implies causality without a connecting message path. Read the code and the trace, then decide order vs concurrency from the causal structure, not the raw integers.
</Recap>
