---
concepts:
- retry-amplification
- retry-storm
- exponential-backoff-jitter
- retry-budget
- circuit-breaker
- metastable-failure
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://aws.amazon.com/blogs/architecture/exponential-backoff-and-jitter/
- https://sre.google/sre-book/addressing-cascading-failures/
- https://www.usenix.org/publications/loginonline/metastable-failures-wild
status: ready
summary: "Multiple-choice synthesis across the retry-amplification unit — fan-out math, metastable loops, jitter, retry budgets, and circuit breakers under load."
title: 'Retry amplification: multiple-choice review'
track: distributed
unit: 07-retry-amplification
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one mirrors a call you make mid-incident — not a definition to recite, but a fan-out number to compute and a fix to rank while the backend is on fire.
</Hook>

<Goal lang="en">
Confirm you can connect retry fan-out math, the metastable sustaining loop, and the defense ladder — jitter, retry budgets, circuit breakers, idempotency, deadline propagation — the synthesis the unit built toward.
</Goal>

<Quiz
  id="07-retry-amplification-choice-1"
  lessonSlug="07-retry-amplification"
  lang="en"
  question="A request crosses 5 service layers and every layer retries 3 times on failure. In the worst case, how many calls hit the deepest dependency for one failing request, and why does this ambush people?"
  choices={[
    { label: "3⁵ = 243 — retries compose by multiplication because each layer re-triggers the full retry behaviour of every layer below it. It ambushes people because the factor is geometric in depth and only kicks in during an incident, when capacity is already gone.", correct: true },
    { label: "3 × 5 = 15 — one set of retries per layer, summed.", misconception: "Retries multiply, they do not add. Each layer's retry re-runs the whole retry chain beneath it, so amplification is retries^depth (3⁵ = 243), not retries × depth (15)." },
    { label: "3 — only the leaf layer actually retries the dependency.", misconception: "Every layer with its own retry policy contributes a factor. The leaf's 3 is just the innermost term of 3 × 3 × 3 × 3 × 3." },
    { label: "15, but only if all five layers fail at the same instant.", misconception: "Amplification is about one failing path being re-attempted at every level on its way down, not simultaneous independent failures. A single failing request alone produces 3⁵." },
  ]}
/>

<Quiz
  id="07-retry-amplification-choice-2"
  lessonSlug="07-retry-amplification"
  lang="en"
  question="The 8-second database failover that triggered the incident finished 30 minutes ago, but the fleet is still down under a retry storm. What is actually keeping it down?"
  choices={[
    { label: "A metastable sustaining loop: the retry traffic is now its own load source, independent of the trigger. Backend slows, requests time out, timeouts fire retries, retries add load, backend slows more. The original failover is irrelevant; removing it changes nothing.", correct: true },
    { label: "The failover must secretly still be in progress — keep hunting the trigger.", misconception: "The defining property of metastable failure is that it persists after the trigger clears. Hunting the already-healed trigger is the classic time sink; the self-sustaining retry loop is the real cause." },
    { label: "A second, unrelated bug that happened to start at the same moment.", misconception: "No new bug is required. The retry feedback loop alone (timeouts to retries to more load to more timeouts) holds the system in the degraded stable state on its own." },
    { label: "Nothing — just wait, it will drain and self-heal.", misconception: "Metastable failures generally do not self-heal because the load that sustains them does not drop. Operators have to shed load, open breakers, or restart tiers to force the system back to its healthy state." },
  ]}
/>

<Quiz
  id="07-retry-amplification-choice-3"
  lessonSlug="07-retry-amplification"
  lang="en"
  question="A team adds fixed-interval retries (wait 200 ms, retry, wait 200 ms, retry) at every layer to 'be gentle.' During the next dependency blip the outage is worse than before. Why?"
  choices={[
    { label: "Fixed intervals re-synchronise the herd every round: all clients timed out together, so they all retry together, again and again, arriving as repeated synchronized spikes. The fix is exponential backoff with jitter to de-correlate clients across the window.", correct: true },
    { label: "200 ms is simply too short — making it 2 s would have been gentle enough.", misconception: "A longer fixed interval still re-synchronises the herd; everyone just stamps together on a slower beat. The defect is the lack of randomness (jitter), not the specific interval length." },
    { label: "Adding any retry policy always worsens blips; they should have removed retries.", misconception: "Jittered, budgeted retries genuinely help — most transient blips succeed on a second attempt. The problem here is unjittered fixed timing, not the existence of retries." },
    { label: "The retries are fine; the blip itself just lasted longer this time.", misconception: "The synchronized spike is self-inflicted by fixed intervals. Jitter spreads attempts and demonstrably cuts both total calls and time-to-recovery versus fixed or plain exponential backoff." },
  ]}
/>

<Quiz
  id="07-retry-amplification-choice-4"
  lessonSlug="07-retry-amplification"
  lang="en"
  question="You have already shipped exponential backoff with full jitter at every layer, yet a full dependency outage still drives the backend to roughly 80x its normal load. What did jitter not buy you, and what do you add?"
  choices={[
    { label: "Jitter smooths the timing of retries, not their volume — 81 calls spread over time are still 81 calls. Add a retry budget that caps retries at ~10% of request volume (Google SRE / Finagle) so amplification has a hard ceiling even in a full outage.", correct: true },
    { label: "Jitter should already cap the count; the implementation must be buggy.", misconception: "Jitter only de-correlates clients in time. It never bounds how many total retries occur — that is precisely what a retry budget is for." },
    { label: "Raise the per-request retry limit so each client gives up sooner.", misconception: "Raising the per-request limit increases attempts, not decreases them. Bounding amplification needs a fleet-wide budget (retries as a fraction of traffic), not a per-request count." },
    { label: "Nothing — 80x is unavoidable during a full outage.", misconception: "A ~10% retry budget turns unbounded geometric growth into at most ~10% extra load even in a full outage. 80x is exactly what the budget exists to prevent." },
  ]}
/>

<Quiz
  id="07-retry-amplification-choice-5"
  lessonSlug="07-retry-amplification"
  lang="en"
  question="A circuit breaker opens after the failure rate to a dependency crosses its threshold. What does this accomplish that a retry budget alone does not?"
  choices={[
    { label: "An open breaker fails calls instantly without touching the network, handing the backend a window of zero retry traffic to recover; a half-open probe then tests one request before letting traffic back in. It breaks the sustaining loop from the dependency end, not just caps the volume.", correct: true },
    { label: "It permanently blocks the dependency until an operator resets it manually.", misconception: "A breaker is not a permanent kill switch. After a cooldown it goes half-open: one probe succeeds and it closes; the probe fails and it re-opens. Recovery is automatic." },
    { label: "It speeds up the dependency by reducing its CPU load directly.", misconception: "The breaker does not touch the dependency's internals. It stops the caller from sending doomed requests, which indirectly lets the backend recover by removing retry load." },
    { label: "It makes retries unnecessary, so you can delete backoff and budgets.", misconception: "Breaker, budget, and jittered backoff are complementary layers: jitter shapes timing, the budget caps volume, the breaker grants a zero-traffic recovery window. You want all three, not one instead of the others." },
  ]}
/>

<Quiz
  id="07-retry-amplification-choice-6"
  lessonSlug="07-retry-amplification"
  lang="en"
  question="Reviewing a service's retry config, you find it retries every failure up to 3 times — including 400s, 409 conflicts, and non-idempotent POSTs — with no deadline propagation. Which two rules is it violating, and why do they matter most under load?"
  choices={[
    { label: "Only retry idempotent operations and retryable errors (never a 400, never a blind POST), and propagate the deadline so no layer retries a request whose caller already gave up. Under load these stop the purest wasted, amplifying work: re-sending requests that cannot succeed or are already dead.", correct: true },
    { label: "It should retry more aggressively — 5 times — to ride out longer blips.", misconception: "More retries on non-retryable errors and dead requests just multiplies amplification. The fix is fewer, smarter retries: only retryable errors, only within the live deadline." },
    { label: "Retrying a 400 is fine because the gateway might fix it on the way back.", misconception: "A 400 is a deterministic client error; retrying it always fails again and wastes a backend call. Only transient, retryable errors (timeouts, 503s, connection resets) should be retried." },
    { label: "Deadline propagation only matters for tracing, not for load.", misconception: "Without deadline propagation a layer happily retries a request whose caller already timed out — pure wasted work that produces nothing but more load, which is exactly what feeds a metastable storm." },
  ]}
/>

<Recap lang="en">
The through-line is one decision tree: retries compose by multiplication (retries^depth, so 3⁵ = 243), and a tiny error rate becomes a self-inflicted DDoS; the storm then sustains itself as a metastable failure long after the trigger clears. The defenses layer from broadest to finest — jitter de-synchronizes the herd in time, a ~10% retry budget caps the volume, a circuit breaker grants a zero-retry recovery window — and the non-negotiable rules are: retry only idempotent operations and retryable errors, and propagate the deadline so no layer ever retries a request that is already dead.
</Recap>
