---
concepts:
- allocation-profiling
- gc-observability
- gomemlimit
- allocation-reduction
estMin: 240
lang: en
level: senior
order: 10
prereqs: []
slug: project
sources:
- https://go.dev/doc/gc-guide
- https://go.dev/blog/pprof
status: ready
summary: Hands-on project — instrument, diagnose, and tame a GC death-spiral in a small allocation-heavy service, then prove the fix with before/after numbers.
title: 'GC: tame a death-spiral'
track: performance
unit: 04-gc
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Reading about death-spirals is not the same as pulling a service out of one. Build a small allocation-heavy server, drive it into GC trouble, and apply the unit's fix ladder until the numbers come back — with evidence at every step.
</Hook>

<Goal lang="en">
Turn the unit's mental model into a reproducible engineering loop: instrument allocation and GC, diagnose the hotspot from a profile, reduce allocations, defend the memory bound, and verify the fix with before/after metrics.
</Goal>

<ProjectBrief
  id="04-gc-project"
  lessonSlug="04-gc"
  lang="en"
  objective="Take a deliberately allocation-heavy HTTP service (your own or the starter below) and bring its GC CPU share under 5% and its p99 under target — without switching collectors — proving each step with measurements."
  requirements={[
    "Build or take a small HTTP service in Go (or JVM/Node) with at least one allocation-heavy endpoint: per-request fmt.Sprintf/JSON building into a zero-capacity buffer, or a handler that returns unbounded query results.",
    "Wire allocation + GC observability: alloc rate (bytes/s), GC pause distribution (p50/p99/max), GC CPU share, and heap-size-vs-live-set — four panels, or the equivalent logged from runtime/metrics + GODEBUG=gctrace=1.",
    "Load-test the endpoint hard enough to push GC CPU above ~15% (or visibly grow concurrent-mark time). Capture the baseline numbers and the gctrace death-spiral signature.",
    "Capture an allocation profile (/debug/pprof/allocs or equivalent) and identify the top-3 allocation hotspots by cumulative bytes.",
    "Apply fixes from the top of the ladder: eliminate or pre-size the allocation, drop Sprintf on the hot path, pool only what the profile flags. Document which lever you used for each hotspot.",
    "Set GOMEMLIMIT (Go) / max-heap (JVM) / --max-old-space-size (Node) to a sensible bound (~90% of the container limit) and show the runtime defends it under the same load.",
    "Re-run the identical load test and record after numbers next to before; allocation rate should drop ≥50% and GC CPU should fall under 5%.",
  ]}
  acceptance={[
    "A before/after table: alloc rate, GC CPU %, p99 pause, and p99 request latency — measured under the same load, not estimated.",
    "The allocation profile clearly shows the top hotspots shrinking after the fix (re-profiled, not assumed).",
    "GC CPU share holds under ~5% and the death-spiral signature is gone from gctrace at sustained load.",
    "A one-paragraph write-up naming the lever used for each hotspot and why it ranked above tuning the collector.",
  ]}
  stretch={[
    "Add a one-page on-call runbook: quick triage from the four panels, common allocation causes for your runtime, the fix-priority ladder, and a verification checklist.",
    "Add an allocation-driven DoS guard — request-body size limit and result-size cap — and show the service stays bounded under an oversized-payload flood.",
    "Add a CI gate that load-tests a canary, diffs the allocation profile against main, and fails the build if any function's allocation share grows more than 20%.",
    "Repeat the experiment on a second runtime (e.g. add a JVM or Node version) and compare how the same allocation pattern manifests under a different collector.",
  ]}
/>

<Recap lang="en">
This is the loop you will run in every real GC incident: instrument first, diagnose from a profile, fix at the top of the ladder (eliminate before pool before tune before switch), defend the memory bound with GOMEMLIMIT, and verify with before/after numbers under identical load. Doing it once on a toy service makes the production version muscle memory.
</Recap>
