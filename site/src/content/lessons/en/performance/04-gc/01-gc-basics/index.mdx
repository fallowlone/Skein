---
concepts:
- garbage-collection
- allocation-rate
- stop-the-world
- concurrent-gc
- gc-cycle
deepensInto: []
estMin: 12
lang: en
lessonType: topic
level: junior
order: 1
prereqs:
- performance/02-hot-paths/01-what-makes-a-hot-path
slug: 01-gc-basics
sources:
- https://go.dev/blog/ismmkeynote
- https://v8.dev/blog/concurrent-marking
- https://shipilev.net/jvm/anatomy-quarks/
spiral:
- latency
status: ready
summary: Garbage collection frees heap memory automatically — but the work shows up
  as pauses and CPU. Allocation rate, not heap size, drives GC-driven tail latency.
title: 'GC basics: what the runtime taxes you for'
track: performance
unit: 04-gc
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A service's p99 latency spikes to 800 ms every few seconds. CPU looks fine. No slow queries. Opening the GC log reveals pauses of 600–700 ms every 4 seconds — the service allocates 1 GB/sec and the runtime is stopping the world to clean up.
</Hook>

<Crux>GC reclaims unreachable memory. Pause time, throughput, and heap size are a 3-way tradeoff. Allocation pressure is what you control.</Crux>

<Explanation>

## What a garbage collector does

A garbage collector trades CPU for memory safety: it walks the heap, marks what is reachable from roots (CPU registers, thread stacks, global variables), and reclaims the rest. You do not call `free()`; the runtime does it for you. The cost is that the runtime must spend CPU time on bookkeeping — and it sometimes pauses your application to do so.

Three knobs are always in tension:

- **Pause time** — how long the application stops while GC works.
- **Throughput** — how much CPU the collector takes away from your code.
- **Heap size** — how much overhead memory the collector needs to work efficiently.

You cannot minimise all three at once.

## The kitchen metaphor

Think of your service as a restaurant kitchen. Cooks (your code) produce dirty dishes (allocations). A dishwasher (GC) cleans them. If cooks generate dishes faster than the dishwasher can clean, dishes pile up — the kitchen must stop and clear the sink. A modern concurrent dishwasher cleans while cooks work; an old stop-the-world dishwasher makes everyone wait. Even the best dishwasher costs water and electricity (CPU). The fast kitchen does not come from a magical dishwasher — it comes from cooks reusing plates.

## Why allocation rate matters more than heap size

Total heap size says how much memory the program holds at any moment. Allocation rate says how often new memory is requested. GC cycles are triggered proportionally to how fast garbage accumulates — which is the allocation rate, not the heap size.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Scenario</th>
        <th class="border border-border px-3 py-2 text-left">Heap size</th>
        <th class="border border-border px-3 py-2 text-left">Alloc rate</th>
        <th class="border border-border px-3 py-2 text-left">GC pause frequency</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Large steady cache</td>
        <td class="border border-border px-3 py-2">4 GB</td>
        <td class="border border-border px-3 py-2">50 MB/s</td>
        <td class="border border-border px-3 py-2">Every ~80 s, short pauses</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">High-throughput API</td>
        <td class="border border-border px-3 py-2">100 MB</td>
        <td class="border border-border px-3 py-2">1 GB/s</td>
        <td class="border border-border px-3 py-2">Every ~0.1 s, frequent spikes</td>
      </tr>
    </tbody>
  </table>
</div>

A service that holds 4 GB but allocates slowly may barely see GC pauses. A service that holds 100 MB but allocates 1 GB/s churns GC constantly. The lever for tail latency is allocation rate, not heap size.

## The mark-sweep cycle

Every tracing GC follows the same skeleton:

1. **Root scan** — briefly pause, identify roots (registers, stacks, globals).
2. **Mark** — walk the object reference graph from roots; mark every reachable object.
3. **Sweep** — reclaim memory for every object not marked.
4. **Optionally compact** — move live objects together to defragment.
5. **Update references** — fix pointers to moved objects.
6. **Resume** — application runs at full speed until the next cycle is needed.

The naive version pauses the application for all of steps 1–5. A 32 GB heap might take seconds — unusable for any latency-sensitive service. Modern collectors reduce or eliminate most STW phases.

## Concurrent vs stop-the-world GC

A **stop-the-world (STW)** collector pauses all application threads while it works. Simple to implement; pauses grow with heap size.

A **concurrent** collector runs most of its work alongside your application threads, so user-visible pauses are short (sub-millisecond) instead of long (tens to hundreds of milliseconds). It needs **write barriers** — small code snippets that run on every reference write to keep the collector informed. The barrier costs ~2–10% CPU; the payoff is short pauses.

<Inset kind="why" lang="en">
All modern concurrent GCs still have *some* stop-the-world phases — root scanning, weak-reference processing, remap. Concurrent collectors minimise STW but cannot eliminate it. Always look at GC logs for actual pause distributions, not vendor headlines.
</Inset>

<Quiz
  id="01-gc-basics-quiz-1"
  lessonSlug="01-gc-basics"
  lang="en"
  question="A service's p99 latency spikes correlate with GC pauses. What is the FIRST place to look for the fix?"
  choices={[
    { label: "The allocation profile — what code is making the most garbage. Reducing allocations reduces the work GC has to do.", correct: true },
    { label: "Switch to a different GC algorithm immediately", misconception: "Switching collectors helps less than reducing allocations; the same allocations still need to be reclaimed by whatever collector runs." },
    { label: "Add more RAM to the server", misconception: "More heap helps a little but does not fix the underlying allocation pressure; pauses scale with garbage, not heap alone." },
    { label: "Disable GC entirely", misconception: "You cannot — the runtime needs it. Even native languages have allocator costs." },
  ]}
/>

<Quiz
  id="01-gc-basics-quiz-2"
  lessonSlug="01-gc-basics"
  lang="en"
  question="Why is a concurrent GC preferred over a stop-the-world GC for production services?"
  choices={[
    { label: "Concurrent collectors do most of their work alongside your code, so user-visible pauses are short (sub-millisecond) instead of long (tens to hundreds of ms).", correct: true },
    { label: "Concurrent GCs use less memory", misconception: "Concurrent GCs typically use MORE memory (write barriers, snapshots); they trade memory for pause time." },
    { label: "Concurrent GCs never pause", misconception: "All modern concurrent GCs still have short stop-the-world phases; they are minimised, not eliminated." },
    { label: "Concurrent GCs are simpler to implement", misconception: "They are more complex — the simplicity argument runs the other way." },
  ]}
/>

<DragOrder
  id="01-gc-basics-drag-1"
  lessonSlug="01-gc-basics"
  lang="en"
  prompt="Order the conceptual stages of a typical garbage-collection cycle:"
  items={[
    { id: "a", label: "Identify roots — registers, thread stacks, global variables" },
    { id: "b", label: "Mark — walk from roots, mark every reachable object" },
    { id: "c", label: "Sweep — reclaim memory of objects not marked" },
    { id: "d", label: "Optionally compact — move live objects together to defragment" },
    { id: "e", label: "Update references to moved objects so the application still works" },
    { id: "f", label: "Resume application full-speed until the next cycle is needed" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f"]}
/>

<MetaphorComplete
  id="01-gc-basics-metaphor-1"
  pieceSlug="01-gc-basics"
  lang="en"
  setup="Fill in the blank: garbage collection trades CPU for memory _______ — you do not have to track every allocation yourself, but the runtime pays for the bookkeeping with cycles that could have gone to your code."
  accepted={["safety", "management", "correctness", "reclamation"]}
  canonical="safety (so use-after-free and double-free bugs become structurally impossible)"
  explanation="GC eliminates whole classes of bugs (use-after-free, double-free, dangling pointers) by removing manual memory management. The cost is CPU time and runtime complexity. Manual memory management gives you control over both, with the bugs included."
/>

</Explanation>

<FlowDiagram
  label="The mark-sweep cycle"
  perRow={6}
  hue="var(--d-hardware)"
  nodes={[{ id: "roots", label: "Root scan", sub: "stacks, globals" }, { id: "mark", label: "Mark", sub: "reachable" }, { id: "sweep", label: "Sweep", sub: "free unmarked" }, { id: "compact", label: "Compact", sub: "optional" }, { id: "update", label: "Update refs" }, { id: "resume", label: "Resume" }]}
  edges={[{ from: "roots", to: "mark" }, { from: "mark", to: "sweep" }, { from: "sweep", to: "compact" }, { from: "compact", to: "update" }, { from: "update", to: "resume" }]}
  caption="A tracing GC scans roots, marks reachable objects, reclaims the rest, optionally compacts, then resumes the application until the next cycle."
/>

<KeyTakeaway>GC reclaims unreachable heap memory so you do not have to. Allocation rate drives GC frequency; pause time is the symptom. Reduce allocations first — every byte you do not allocate is GC work the runtime never has to do.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-gc-basics-retrieval"
  lang="en"
  questions={[
    { q: "In one paragraph: why does allocation rate matter more for GC-driven tail latency than total heap size?", a: "Total heap size says how much memory the program holds at any moment; allocation rate says how often new memory is requested. GC runs cycles proportional to how fast garbage accumulates — which is the allocation rate, not the heap size. A service with a 4 GB heap and 50 MB/s allocation rate may cycle every minute with tiny pauses. The same service holding 100 MB total but allocating 1 GB/s churns GC constantly — pauses every second, mark work dominates, p99 suffers. The lever for latency is allocation rate; the lever for memory footprint is heap size. Reducing allocations (pool buffers, reuse slices, struct-of-arrays, in-place mutation) is the durable fix for GC-driven tail latency." },
    { q: "Name the three-way tradeoff every GC must navigate, and give one example of a collector that optimises for each extreme.", a: "The three axes are: (1) pause time — how long the application stops; (2) throughput — how much CPU the collector takes; (3) heap size — how much overhead memory the runtime needs. JVM ParallelGC optimises throughput at the cost of long pauses. JVM ZGC optimises pause time at the cost of ~10–15% throughput and more memory. Go's concurrent collector sits between, with sub-ms pauses and moderate throughput cost. No collector can minimise all three; you pick the one that fits the workload." },
  ]}
/>

<Recap lang="en">
A garbage collector marks reachable objects and reclaims the rest, trading CPU cycles for memory safety. The three knobs — pause time, throughput, and heap size — cannot all be minimised at once. Allocation rate drives GC cycle frequency more than heap size does: a small service that allocates 1 GB/s sees far more GC pressure than a large cache-heavy service at 50 MB/s. Modern concurrent GCs do most of their work alongside application threads, keeping pauses under 1 ms, but still have short stop-the-world phases for root scanning. The first lever for GC-driven tail latency is always the allocation profile, not the collector choice.
</Recap>
