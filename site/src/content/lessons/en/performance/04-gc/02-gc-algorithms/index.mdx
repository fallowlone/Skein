---
concepts:
- generational-gc
- generational-hypothesis
- concurrent-marking
- write-barrier
- mark-sweep
- gc-per-runtime
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: middle
order: 2
prereqs:
- performance/04-gc/01-gc-basics
slug: 02-gc-algorithms
sources:
- https://go.dev/blog/ismmkeynote
- https://v8.dev/blog/concurrent-marking
- https://openjdk.org/jeps/333
- https://openjdk.org/jeps/377
- https://devblogs.microsoft.com/dotnet/server-gc-2/
- https://docs.python.org/3/c-api/refcounting.html
spiral:
- latency
status: ready
summary: The generational hypothesis (most objects die young) shapes every major production
  GC. Knowing which algorithm your runtime ships — and why it makes different tradeoffs
  than others — is the prerequisite for diagnosing any pause.
title: 'GC algorithms: generational, concurrent, and per-runtime'
track: performance
unit: 04-gc
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";

<Hook>
A Go service and a JVM service are both running at 200 MB/s allocation rate. The Go service has flat 0.5 ms pauses; the JVM service has 50 ms pauses every 10 seconds. Same allocation rate, 100x pause difference — because the collectors work entirely differently.
</Hook>

<Crux>Most objects die young. Generational collectors exploit this, visiting the young heap often. Concurrent marking hides the remaining pause.</Crux>

<Explanation>

## The generational hypothesis

The single most important empirical observation in GC research: **most objects die young**. A typical request handler allocates many temporary objects — parsed body, intermediate strings, response builders — serves the response, and drops them all. The objects that survive long enough to be useful are few: caches, configuration, connection pools.

Generational collectors exploit this by splitting the heap:

- **Young generation** — small (a few hundred MB to a few GB), collected frequently. All allocations land here.
- **Old generation** — large, collected rarely. Objects that survive several young-gen cycles get "promoted" (tenured) here.

A young-gen collection walks only the small young heap. If 95% of objects die in young, one cheap pass reclaims 95% of allocated bytes. Old-gen collections are expensive but rare because most objects never reach old.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Generation</th>
        <th class="border border-border px-3 py-2 text-left">Size</th>
        <th class="border border-border px-3 py-2 text-left">Collection frequency</th>
        <th class="border border-border px-3 py-2 text-left">Contents</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Young (Eden + Survivor)</td>
        <td class="border border-border px-3 py-2">~256 MB – 4 GB</td>
        <td class="border border-border px-3 py-2">Every few seconds</td>
        <td class="border border-border px-3 py-2">New allocations; mostly short-lived</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Old (Tenured)</td>
        <td class="border border-border px-3 py-2">Remaining heap</td>
        <td class="border border-border px-3 py-2">Every few minutes</td>
        <td class="border border-border px-3 py-2">Promoted survivors; mostly long-lived</td>
      </tr>
    </tbody>
  </table>
</div>

## Concurrent marking

To keep pauses below 10 ms on multi-GB heaps, marking must happen **while the application runs**. The GC uses a background thread that walks the object graph alongside mutator (application) threads.

The challenge: the application may modify references while marking is in progress, so the GC needs a **write barrier** — a small piece of code that runs on every reference write to keep the collector informed. Two main strategies:

- **Snapshot-at-the-beginning (SATB)** — marks the *old* reference about to be overwritten so the collector treats it as live for this cycle. Used by G1, Shenandoah, ZGC.
- **Incremental-update** — marks the *new* reference being written so it is not missed. Used by CMS, classic V8 mark-compact.

Write barrier overhead: **~2–10% CPU** — the price of concurrent marking.

## Per-runtime tour

**Go** — concurrent tri-color non-generational mark-sweep (no compaction; allocator is a tcmalloc variant). Triggered by allocation rate; the pacer keeps STW pauses under 1 ms. `GOGC=100` means GC runs when the heap doubles since the last cycle; `GOMEMLIMIT` (Go 1.19+) adds a soft memory cap. Go is the *counterexample* to generational: the team bet that simpler runtime + escape analysis reducing heap pressure made the generational complexity not worth it. Empirically validated — sub-ms pauses on most workloads.

**JVM (modern)** — G1 is the default for most servers: region-based, generational, concurrent marking, typical pauses 10–50 ms. ZGC (JEP 333 experimental JDK 11, production JDK 15; JEP 377) targets sub-ms pauses on heaps up to 16 TB using colored pointers + load barriers. Shenandoah (Red Hat) uses Brooks pointers for similar goals. Tunable via `-XX:MaxGCPauseMillis` and heap-size flags.

**V8 (Node.js)** — generational Scavenger for young (semi-space copying), Mark-Compact for old. The Orinoco project (2017+) added concurrent marking + parallel compaction, targeting ≤10 ms pauses. Heap capped per-isolate (~1.5 GB default in Node, configurable via `--max-old-space-size`).

**.NET** — workstation/server GC, generational (gen 0/1/2 + LOH), background concurrent marking. Tunable via `GCSettings.LatencyMode`.

**CPython** — reference counting (drops object when refcount=0, no major pause) + cycle collector for reference cycles. The GIL serialises mutation but reference counts cost ~10–20% throughput. No major STW pauses; cycle collection is incremental.

<Inset kind="why" lang="en">
The generational hypothesis still informs production even in non-generational runtimes like Go. Code that allocates many short-lived objects in tight loops — zero-capacity slices that grow, fmt.Sprintf on every request, JSON encoding without pooling — creates more GC pressure than long-lived state regardless of the collector's algorithm. The lever is always the same: reduce short-lived allocations.
</Inset>

<Quiz
  id="02-gc-algorithms-quiz-1"
  lessonSlug="02-gc-algorithms"
  lang="en"
  question="A JVM service has 50 ms p99 from G1 pauses. Doubling the heap (-Xmx 8g → 16g) might help — why, and what is the risk?"
  choices={[
    { label: "More heap means fewer GC cycles for the same allocation rate, so fewer pauses per second. Risk: larger heap means longer pause when a cycle does happen, because there is more live data to mark.", correct: true },
    { label: "Doubling heap reduces individual pause length", misconception: "Pause length scales with live data the collector must mark; a larger heap with more live data makes single pauses LONGER, not shorter." },
    { label: "More heap eliminates GC", misconception: "Even a 1 TB heap eventually fills with garbage; allocation rate sets the cycle frequency, not absolute heap size." },
    { label: "Heap size has no effect on GC", misconception: "Heap size is one of the three primary GC tradeoffs (pause/throughput/heap)." },
  ]}
/>

<Quiz
  id="02-gc-algorithms-quiz-2"
  lessonSlug="02-gc-algorithms"
  lang="en"
  question="A V8 service running in Node.js grows to 1.4 GB then crashes. Most likely cause?"
  choices={[
    { label: "Hit V8's default old-space limit (~1.5 GB in Node). Either raise it with --max-old-space-size, or find the retained objects with a heap snapshot and fix the leak.", correct: true },
    { label: "GC stopped working", misconception: "GC does not stop working; it is hitting a hard limit imposed by V8's max old-space-size." },
    { label: "Memory leaks always cause restarts", misconception: "True only when they hit a limit; not all leaks crash, some just degrade performance." },
    { label: "Node.js has no GC", misconception: "Node uses V8's full GC; the limit is configurable, not absent." },
  ]}
/>

<Quiz
  id="02-gc-algorithms-quiz-3"
  lessonSlug="02-gc-algorithms"
  lang="en"
  question="A Python service is single-threaded due to the GIL, but still has GC overhead. What is the dominant cost?"
  choices={[
    { label: "Reference counting on every PyObject assignment — every variable bind, every function call, every attribute access increments or decrements a counter. The cycle collector is a small extra cost on top.", correct: true },
    { label: "Tracing mark-sweep on every object", misconception: "CPython's primary mechanism is refcounting; tracing is only for cycles that refcounting cannot detect." },
    { label: "Stop-the-world pauses", misconception: "CPython does not have major STW pauses like JVM or Go; the refcount cost is incremental per operation." },
    { label: "Parallel GC threads", misconception: "Cycle collection is single-threaded and infrequent." },
  ]}
/>

<DragOrder
  id="02-gc-algorithms-drag-1"
  lessonSlug="02-gc-algorithms"
  lang="en"
  prompt="Order the priority of GC-pressure fix levers, from highest leverage to lowest:"
  items={[
    { id: "a", label: "Eliminate the allocation (in-place mutation, struct-of-arrays, primitives)" },
    { id: "b", label: "Pool / reuse the allocation (sync.Pool, ObjectPool, bytes.Buffer reset)" },
    { id: "c", label: "Let escape analysis stack-allocate (smaller object, scope-local)" },
    { id: "d", label: "Shrink the allocation (smaller struct, smaller buffer, pre-sized container)" },
    { id: "e", label: "Move the allocation off the hot path (cache the result, compute once)" },
    { id: "f", label: "Tune the collector (GOGC, MaxGCPauseMillis, max-old-space-size)" },
    { id: "g", label: "Switch the collector algorithm (ParallelGC → G1 → ZGC)" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g"]}
/>

</Explanation>

<KeyTakeaway>Generational collectors split the heap young/old and collect the cheap small young gen often. Concurrent marking runs alongside the application at the cost of a write-barrier (~2–10% CPU). Go bets on non-generational + escape analysis; JVM, V8, and .NET all generational. Know which algorithm your runtime ships before tuning anything.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-gc-algorithms-retrieval"
  lang="en"
  questions={[
    { q: "Walk through the generational hypothesis and why it shapes most production GCs. Include one counterexample.", a: "The generational hypothesis is the empirical observation that most allocated objects die young. A typical request handler allocates many short-lived objects and very few long-lived ones. Generational collectors exploit this by splitting the heap: young gen (small, collected often) and old gen (large, collected rarely). Allocations land in young; survivors get promoted to old. Young-gen collection is cheap because the heap is small; old-gen collection is rare because most objects never survive there. If 95% of objects die young, one cheap young-gen pass reclaims 95% of allocated bytes. Counterexample: Go's GC is non-generational — a single heap, concurrent tri-color mark. The Go team argued that runtime simplicity and allocation patterns (escape analysis, stack-allocate everything possible) reduced heap pressure enough that generational complexity was not worth it. Empirically, Go achieves sub-ms pauses on most workloads. JVM, V8, and .NET all use generational collectors; Go and Erlang's BEAM do not." },
    { q: "What is a write barrier in concurrent GC and why is it needed?", a: "A write barrier is a small piece of code that runs on every reference write while concurrent marking is in progress. Without it, the collector could miss an object: if a mutator thread writes a reference from an already-scanned (black) object to an unscanned (white) object after the black object was marked, the collector would finish without visiting the white object and reclaim live memory — silently corrupting the program. The barrier intercepts the write and informs the collector. Two strategies: SATB (marks the old reference being overwritten, used by G1/Shenandoah/ZGC) and incremental-update (marks the new reference, used by CMS/classic V8). The overhead is ~2–10% CPU on write-heavy workloads — the price of concurrent marking instead of stop-the-world." },
  ]}
/>

<Recap lang="en">
The generational hypothesis — most objects die young — is the empirical foundation of most production GC algorithms. Generational collectors split the heap into a small, frequently-collected young generation and a large, rarely-collected old generation; most garbage is reclaimed cheaply in the young gen. Concurrent marking runs alongside the application by adding a write barrier (~2–10% CPU) to intercept reference writes. Go is the main counterexample: non-generational, concurrent tri-color, betting that escape analysis reduces heap pressure enough. JVM G1 and ZGC, V8 Orinoco, and .NET server GC are all generational + concurrent. CPython is reference-counting with a supplementary cycle collector — no major STW pauses but per-operation refcount overhead.
</Recap>
