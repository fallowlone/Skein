---
slug: 03-gc-tradeoffs
lang: en
track: performance
unit: 04-gc
order: 3
title: "GC tradeoffs: pause, throughput, heap — and object pooling"
summary: "Every collector picks two of three: short pauses, high throughput, or low memory. Allocation pressure is the upstream cause of all three; reducing it is more durable than tuning the collector. Object pooling works only when the profile says so."
estMin: 14
status: ready
lessonType: topic
level: middle
prereqs:
  - performance/04-gc/02-gc-algorithms
deepensInto: []
spiral: ["latency"]
concepts: ["gc-tradeoff", "allocation-pressure", "object-pooling", "sync-pool", "gc-numbers"]
sources:
  - https://go.dev/blog/ismmkeynote
  - https://openjdk.org/jeps/333
  - https://openjdk.org/jeps/377
  - https://shipilev.net/jvm/anatomy-quarks/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";

<Hook>
Two teams both switch from G1 to ZGC. Team A sees p99 drop from 60 ms to 0.8 ms — a clear win. Team B sees pauses drop, but throughput falls 12% and memory use rises 18%. Both outcomes are correct. The tradeoff is real; knowing which axis your workload needs decides whether ZGC is the right call.
</Hook>

<Crux>Short pauses, high throughput, low memory: pick two. Reducing allocation pressure is the only lever that improves all three at once.</Crux>

<Explanation>

## The three-way tradeoff

Every garbage collector picks two of three axes to optimise:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Collector</th>
        <th class="border border-border px-3 py-2 text-left">Pause time</th>
        <th class="border border-border px-3 py-2 text-left">Throughput</th>
        <th class="border border-border px-3 py-2 text-left">Memory overhead</th>
        <th class="border border-border px-3 py-2 text-left">Typical use</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">JVM ParallelGC</td>
        <td class="border border-border px-3 py-2">Poor (100s of ms)</td>
        <td class="border border-border px-3 py-2">Excellent</td>
        <td class="border border-border px-3 py-2">Low</td>
        <td class="border border-border px-3 py-2">Batch jobs</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">JVM G1</td>
        <td class="border border-border px-3 py-2">Good (10–50 ms)</td>
        <td class="border border-border px-3 py-2">Good</td>
        <td class="border border-border px-3 py-2">Medium</td>
        <td class="border border-border px-3 py-2">Most services</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">JVM ZGC</td>
        <td class="border border-border px-3 py-2">Excellent (&lt;1 ms)</td>
        <td class="border border-border px-3 py-2">~10–15% lower</td>
        <td class="border border-border px-3 py-2">Higher</td>
        <td class="border border-border px-3 py-2">Latency-sensitive, large heaps</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Go concurrent</td>
        <td class="border border-border px-3 py-2">Excellent (~0.1–1 ms)</td>
        <td class="border border-border px-3 py-2">Slightly below peak</td>
        <td class="border border-border px-3 py-2">Medium</td>
        <td class="border border-border px-3 py-2">Go services</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">V8 Orinoco</td>
        <td class="border border-border px-3 py-2">Good (5–15 ms)</td>
        <td class="border border-border px-3 py-2">Good</td>
        <td class="border border-border px-3 py-2">Medium</td>
        <td class="border border-border px-3 py-2">Node.js services</td>
      </tr>
    </tbody>
  </table>
</div>

The wrong choice for a workload can cost 30–50% throughput or 10x p99 latency.

## GC numbers to know

- Go concurrent GC typical STW: **~100 µs – 1 ms**
- JVM G1 default pause target: **200 ms**
- JVM ZGC pause: **&lt;1 ms on multi-TB heaps**
- V8 Orinoco typical pause: **5–15 ms**
- Write barrier overhead: **~2–10% CPU**
- Allocation rate before GC dominates: **≥500 MB/s/core**
- Go `GOGC` default: **100 (heap doubles between cycles)**
- GC frame share in a healthy service: **&lt;5% CPU**

## Allocation pressure: the upstream cause

If your service allocates 1 GB/s, GC is going to be busy regardless of which collector you ship. Allocation pressure is the upstream cause; pause time is the symptom. Reducing allocation pressure has **unbounded upside** — every byte you do not allocate is GC work that never has to happen. Tuning the collector has **bounded upside** — you are optimising the cost of work; the work does not disappear.

The senior pattern: when GC is wide in a CPU profile, look at the allocation profile next. The widest leaf in the alloc profile is your target. Fix levers in priority order:

1. Eliminate the allocation (in-place mutation, struct-of-arrays)
2. Pool/reuse (sync.Pool in Go, object pools in Java/JS)
3. Escape-analyse — let the compiler stack-allocate (Go, .NET, partially JVM)
4. Shrink the allocation (smaller struct, smaller buffer, pre-sized container)
5. Move the allocation off the hot path (compute once, reuse)
6. Tune the collector
7. Switch the collector

## Object pooling: when it works and when it does not

`sync.Pool` in Go, Apache Commons Pool in Java, object pools in .NET — patterns for reusing allocations to reduce GC pressure. Work well when objects are expensive to create and used briefly on hot paths: `bytes.Buffer`s, JSON encoders, regexp objects, scratch slices.

**Fail or hurt when:**
- Objects are cheap to create (savings smaller than pool overhead)
- Objects live long (pool holds memory without freeing)
- Thread-coordination overhead exceeds allocation savings

The senior rule: **pool only what the profile flags as hot allocation**. Do not pre-pool. Go's `sync.Pool` has an advantage: the GC can drain the pool between cycles — memory is not lost forever. JVM/.NET object pools hold memory until explicit release.

<Inset kind="why" lang="en">
Why does reducing allocations help BOTH p99 latency AND throughput, even though they are often presented as a tradeoff? Allocations cost twice: once when the runtime allocates, and once when the GC reclaims. Reducing allocations reduces both costs. p99 improves because GC runs less and pauses are shorter or less frequent. Throughput improves because the CPU previously spent on GC machinery (mark, scan, write barriers) is now available for application work. The "throughput vs latency tradeoff" applies to GC tuning (longer pauses → higher throughput); it does not apply to allocation reduction, which moves both metrics in the same direction.
</Inset>

<Quiz
  id="03-gc-tradeoffs-quiz-1"
  lessonSlug="03-gc-tradeoffs"
  lang="en"
  question="A latency-sensitive Java microservice with a 16 GB heap sees G1 pauses occasionally hitting 200 ms. The highest-leverage first step is:"
  choices={[
    { label: "Profile allocations and reduce allocation rate — allocation reduction lowers GC work proportionally, shrinking pauses AND increasing throughput simultaneously.", correct: true },
    { label: "Migrate to ZGC immediately", misconception: "ZGC delivers sub-ms pauses but pays ~10–15% throughput and ~10% memory. Right move after allocation reduction is exhausted; wrong as a first step — it masks allocation pressure without fixing it." },
    { label: "Double the heap to 32 GB", misconception: "Doubling heap roughly halves cycle frequency but makes each pause longer because there is more live data to mark. Useful as a temporary stabiliser, not a fix." },
    { label: "Set -XX:MaxGCPauseMillis=50", misconception: "G1 will try harder to meet 50 ms by collecting more often in smaller chunks, which drops throughput. Sometimes right after allocation reduction is exhausted; usually premature." },
  ]}
/>

<Quiz
  id="03-gc-tradeoffs-quiz-2"
  lessonSlug="03-gc-tradeoffs"
  lang="en"
  question="A Go service switches from sync.Pool for a small struct to always allocating fresh. The struct costs 80 ns to allocate. The pool costs 120 ns to access (lock + reset). What should you do?"
  choices={[
    { label: "Remove the pool — it adds net overhead. Pool only when the allocation hotspot shows up in the alloc profile AND pool access is cheaper than the allocation.", correct: true },
    { label: "Keep the pool to reduce GC pressure regardless", misconception: "If pool access costs more than allocation, the pool makes things slower, not faster. Profile first." },
    { label: "Use a larger pool", misconception: "The issue is per-access overhead, not pool capacity. Larger pool does not change the access cost." },
    { label: "Convert to a global variable", misconception: "Globals create contention and state issues; they are not an equivalent replacement." },
  ]}
/>

<DragOrder
  id="03-gc-tradeoffs-drag-1"
  lessonSlug="03-gc-tradeoffs"
  lang="en"
  prompt="Order the fix levers for GC-driven allocation pressure, from highest to lowest leverage:"
  items={[
    { id: "a", label: "Eliminate the allocation (in-place mutation, struct-of-arrays, primitives)" },
    { id: "b", label: "Pool / reuse (sync.Pool, ObjectPool, bytes.Buffer reset)" },
    { id: "c", label: "Let escape analysis stack-allocate (smaller object, scope-local)" },
    { id: "d", label: "Shrink the allocation (smaller struct, smaller buffer, pre-sized container)" },
    { id: "e", label: "Move off the hot path (cache the result, compute once)" },
    { id: "f", label: "Tune the collector (GOGC, MaxGCPauseMillis, max-old-space-size)" },
    { id: "g", label: "Switch the collector algorithm (G1 → ZGC)" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g"]}
/>

</Explanation>

<KeyTakeaway>Every collector picks two of: short pauses, high throughput, low memory. Allocation reduction is the only lever that moves all three simultaneously. Pool allocations only after the profile confirms the hotspot is hot enough to justify the complexity.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-gc-tradeoffs-retrieval"
  lang="en"
  questions={[
    { q: "Why is reducing allocation rate a more durable fix than tuning GC parameters?", a: "Tuning GC parameters (heap size, pause target, generation ratios) optimises the cost of the work the GC has to do; reducing allocations eliminates the work itself. Tuning is bounded — you can shrink a 200 ms pause to 50 ms but not to 0; with the same allocation rate, the collector still runs and still pauses. Reducing allocations is unbounded: every byte not allocated is a byte the GC never has to manage, mark, or reclaim. The leverage scales with the service lifetime: a long-running service that handles a billion requests pays the allocation cost a billion times; even small reductions accumulate. Additionally, allocation reduction improves both throughput and latency simultaneously because it removes GC work entirely, while tuning trades one axis for another." },
    { q: "Name three conditions where object pooling helps, and three where it hurts.", a: "Pooling helps when: (1) objects are expensive to create — db connections, large buffers, compiled regexp; (2) the object is used briefly and returned quickly — so the pool does not just hold memory long-term; (3) the alloc profile shows the object in the top-5 hotspots — confirming the save is real. Pooling hurts when: (1) the object is cheap to create — pool access overhead exceeds allocation cost; (2) objects are long-lived — pool holds memory without freeing, worsening footprint; (3) thread-coordination overhead dominates — highly contended pools under GOMAXPROCS=1 or in single-threaded contexts. The diagnostic: benchmark pool vs no-pool with the real workload; if allocation rate in the profile does not drop meaningfully, remove the pool." },
  ]}
/>

<Recap lang="en">
Every GC algorithm picks two of three axes: short pauses, high throughput, low memory. ZGC delivers sub-ms pauses at the cost of 10–15% throughput and more memory. ParallelGC maximises throughput with poor pauses. G1 balances all three. Regardless of which collector runs, allocation pressure is the upstream cause of GC overhead: reducing allocations moves pause time, throughput, and memory footprint in the right direction simultaneously — the only lever with unbounded upside. Object pooling is a powerful tool when the alloc profile confirms the hotspot, but adds complexity and can hurt when objects are cheap to create or long-lived.
</Recap>
