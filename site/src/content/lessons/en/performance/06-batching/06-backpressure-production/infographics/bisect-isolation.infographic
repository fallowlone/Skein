infographic chart-column-simple
data
  title Isolating one poison record in a 500-batch
  desc Split-and-retry bisects the batch — O(log N) ≈ 9 retries, not 500
  values
    - label Test each record (linear)
      value 500
    - label Split-and-retry (bisect)
      value 9
theme
  palette #f43f5e #14b8a6
