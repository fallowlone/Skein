---
concepts:
- fixed-vs-variable-cost
- batching-window
- break-even
- backpressure
- poison-message
- decompression-bomb
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://kafka.apache.org/documentation/#producerconfigs_linger.ms
- https://www.postgresql.org/docs/current/populate.html
- https://nvd.nist.gov/vuln/detail/CVE-2023-34455
status: ready
summary: Multiple-choice synthesis across the batching unit — the fixed-cost model, the size/wait window, break-even math, backpressure policies, and production failure modes.
title: 'Batching: multiple-choice review'
track: performance
unit: 06-batching
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. None is a definition to recite — each mirrors a call you make under load: when to batch, how wide the window goes, and what breaks when the happy path ends.
</Hook>

<Goal lang="en">
Confirm you can connect the fixed-cost model, the two-trigger window, break-even math, and production failure modes — the synthesis the individual lessons built toward.
</Goal>

<Quiz
  id="06-batching-choice-1"
  lessonSlug="06-batching"
  lang="en"
  question="A payment service writes one durable ledger row per transaction at ~30 writes/s; the SLA is 'durable before we return success.' A teammate proposes 50 ms COPY batches to cut DB load. What does a senior decide, and why?"
  choices={[
    { label: "Reject batching here. At 30 writes/s there is almost no queue depth to amortize, so the fixed-cost win is tiny, while every transaction now waits up to 50 ms and a crash before flush loses acknowledged payments — the durability SLA is incompatible with partial-batch loss.", correct: true },
    { label: "Batch into 50 ms COPY windows — fewer commits is always cheaper at the database.", misconception: "It optimizes a load nobody is paying: 30 writes/s is not a throughput problem. It buys headroom you don't need while breaking the SLA, since buffered rows aren't durable and vanish on crash." },
    { label: "Batch, but acknowledge the user immediately, before the flush, to hide the added latency.", misconception: "Worse: you've told the user the payment succeeded while it sits in a volatile buffer. That converts a latency problem into data loss plus a lie — exactly the failure the durability SLA forbids." },
    { label: "Batch with a 5 ms window so the latency penalty is negligible.", misconception: "A smaller window shrinks the latency tax but doesn't fix the two real problems: there's still no queue depth to amortize at 30 writes/s, and any buffered-but-unflushed window can still lose an acknowledged payment on crash." },
  ]}
/>

<Quiz
  id="06-batching-choice-2"
  lessonSlug="06-batching"
  lang="en"
  question="A batching system is configured with max-size only (no max-wait timer). Traffic drops overnight. What is the failure mode, and what does adding the timer fix?"
  choices={[
    { label: "The first item of a half-full batch waits unboundedly, because the buffer never reaches max-size at low load — a head-of-line stall whose latency scales inversely with traffic. The max-wait timer says 'ship what you have' and bounds that latency.", correct: true },
    { label: "Batches grow too large and overflow downstream capacity; the timer caps their size.", misconception: "That is the max-wait-only failure under HIGH load. A size-only batcher can't overflow — it can only fail to flush. The timer protects latency, not size." },
    { label: "Throughput collapses because each item is effectively sent on its own.", misconception: "Size-only batching still batches; items accumulate until the buffer fills. The defect is unbounded latency for the waiting items, not lost batching." },
    { label: "Nothing — at low load there's nothing to optimize, so a size-only window is fine.", misconception: "Low load is exactly when size-only is dangerous: the buffer never fills, so the oldest item can sit for seconds. That's a latency bug, and it's worst precisely when the system looks idle." },
  ]}
/>

<Quiz
  id="06-batching-choice-3"
  lessonSlug="06-batching"
  lang="en"
  question="For 4 KB payloads where the fixed per-call cost is ~50 µs and per-KB transfer is ~40 µs, will a large batch give a big speedup?"
  choices={[
    { label: "No. Variable cost (4 × 40 µs = 160 µs) already exceeds fixed cost (50 µs), so F < V·n: you're past break-even into the speedup→1 regime, and batching mostly just adds latency.", correct: true },
    { label: "Yes — batching always multiplies throughput by the batch size N.", misconception: "speedup → N only when fixed cost dominates (F > V·n). Here variable cost dominates, so there's almost no fixed overhead left to amortize." },
    { label: "Yes — larger payloads make batching more effective.", misconception: "The reverse: larger payloads push you toward speedup → 1, because the fixed cost becomes a smaller fraction of total work. Batching tiny messages is the big win." },
    { label: "Yes — at 4 KB you're still well under typical batch.size, so it amortizes freely.", misconception: "Break-even is per item, not per batch. Once one item's variable cost exceeds the fixed cost, no batch size rescues you — fitting under batch.size is irrelevant." },
  ]}
/>

<Quiz
  id="06-batching-choice-4"
  lessonSlug="06-batching"
  lang="en"
  question="A Kafka cluster's brokers sit under 20% disk and network, yet producers can't push past a fraction of capacity. The producers run Kafka 3.x defaults. What's the first fix?"
  choices={[
    { label: "Raise linger.ms to 5–100 ms and batch.size to 64–512 KB. The 3.x default linger.ms=0 ships tiny batches under steady load, capping throughput far below the hardware; deliberately waiting makes requests fewer and fatter.", correct: true },
    { label: "Add more partitions and more producer instances to drive parallelism up.", misconception: "More clients each sending tiny batches doesn't help — you multiply small requests instead of making each one larger. The fix is fatter batches, not more of them." },
    { label: "Turn on compression.type=zstd and leave linger.ms at 0.", misconception: "Compression runs per batch; with linger.ms=0 batches stay tiny, so the codec has little repetition to exploit. Compression amplifies batching — it can't substitute for it." },
    { label: "Switch the brokers to faster disks.", misconception: "The brokers are bored at under 20% utilization — disk isn't the bottleneck. The producer that refuses to wait is, so faster broker disks change nothing." },
  ]}
/>

<Quiz
  id="06-batching-choice-5"
  lessonSlug="06-batching"
  lang="en"
  question="A Kafka consumer polls 500 records; record 47 throws on deserialization. The pipeline retries the whole batch on any error. Consumer lag climbs to millions and rises, but the consumer looks healthy. What's happening, and what's the fix?"
  choices={[
    { label: "A poison-message stall: record 47 throws every retry, so the offset never advances and the partition freezes behind one bad record. Fix with split-and-retry — bisect to isolate the poison item in O(log N) requests, dead-letter it, commit the rest.", correct: true },
    { label: "The consumer is under-provisioned; add more consumer instances to clear the lag.", misconception: "More consumers all hit the same poison record and stall identically. Lag is rising because the offset can't advance past record 47, not because of throughput." },
    { label: "Skip the whole batch on error so the consumer can advance past the bad record.", misconception: "That discards 499 good records to drop one — data loss, which for orders or payments can be a compliance incident. Split-and-retry isolates the one bad item instead." },
    { label: "Increase the poll timeout so the deserializer has more time to succeed.", misconception: "Record 47 is structurally corrupt — more time doesn't make it parse. The problem is failure isolation, not timing." },
  ]}
/>

<Quiz
  id="06-batching-choice-6"
  lessonSlug="06-batching"
  lang="en"
  question="A broker enforces a wire-level max message size, yet a 1 MB compressed batch OOM-kills it (the CVE-2023-34455 class). What is the correct defense?"
  choices={[
    { label: "Enforce a cap on the post-decompression size, validated as the frame expands. The exploit is the expansion ratio — a tiny compressed frame can declare a ~2 GB chunk — so a wire-size limit says nothing about what it expands to.", correct: true },
    { label: "Lower the on-the-wire max message size further.", misconception: "The attack lives entirely below the wire limit: 1 MB compressed already passed it. Shrinking the wire cap doesn't bound the decompressed size, so it offers no protection." },
    { label: "Disable compression on the broker entirely.", misconception: "That throws away the throughput and storage gains compression buys, and isn't necessary — a post-decompression bound (the snappy-java 1.1.10.1 fix) closes the hole while keeping compression." },
    { label: "Add per-item idempotency keys to reject the malicious records.", misconception: "Idempotency stops replay, a different attack. A decompression bomb needs no duplicate payload — it OOMs the broker during expansion, before any per-item logic runs." },
  ]}
/>

<Recap lang="en">
Across the unit the through-line is one decision sequence: confirm the fixed cost dominates the variable cost, confirm there's queue depth and latency slack to spend, then size the window — max-size for throughput, max-wait for latency, whichever fires first — to the largest value under your SLO. The break-even is per item at F = V·n. Production then turns every efficiency property into a failure multiplier: a poison item kills N (split-and-retry plus DLQ), a fast producer floods a queue (bound it, pick block/drop/spill on purpose), and a compressed batch is an opaque envelope you must validate post-decompression and per-item. The recurring senior trap is optimizing throughput nobody is paying for while breaking a latency or durability contract.
</Recap>
