---
concepts:
- latency
- throughput
- profiling
- bottleneck
- percentile
- the-tail
- big-o-vs-real-cost
- measure-dont-guess
- optimization
deepensInto: []
estMin: 10
lang: en
lessonType: topic
level: zero
order: 1
prereqs: []
slug: 01-overview
sources:
- https://www.brendangregg.com/usemethod.html
- https://web.dev/articles/rail
- https://developer.mozilla.org/en-US/docs/Web/Performance/How_browsers_work
- https://queue.acm.org/detail.cfm?id=2209336
spiral: []
status: ready
summary: Performance engineering is one discipline with one rule — measure first,
  fix the proven bottleneck. This is the from-zero map and the eight words every
  senior performance unit assumes you already know.
title: 'Start from zero: what performance work actually is'
track: performance
unit: 00-start-here
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import EditorialDiagram from "~/components/diagram/EditorialDiagram.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import tailLatency from "./infographics/tail-latency.svg?raw";

<Hook>
Your app is slow. A user clicked a button and nothing happened for two seconds. You have a theory: probably the database, or maybe too much JavaScript, or perhaps that loop you wrote last week. So you spend a day rewriting the loop — and the button is still slow. It turns out the database was doing a full-table scan the whole time. You optimised the wrong thing. This lesson is the map that prevents that mistake: before you change a single line for speed, you find out exactly where the time is actually going.
</Hook>

<Crux>Performance work has one rule: measure first, fix the proven bottleneck. Guessing wastes time and misses the real slowness.</Crux>

<Explanation>

## The one mistake performance work prevents

When something is slow, the instinct is to guess — "it must be the database," "probably too much JavaScript," "I bet it's that loop." Guesses feel reasonable because they come from experience. They are also wrong roughly half the time, and acting on a wrong guess makes you faster at something that was not the problem. Meanwhile the real bottleneck — the one place where time actually piles up — sits unchanged and keeps slowing every user. Performance engineering is the discipline of not guessing: you measure where the time goes, you find the one place that matters most, you fix only that, and then you measure again to confirm you actually helped.

Get this right and a small targeted change can halve response time. Get it wrong and you spend weeks micro-optimising code that the profiler would have shown you to be irrelevant in thirty seconds.

## The eight words the rest of the track assumes

The senior units that follow drop these terms without stopping to define them. Before you can debug a production slowdown or read a flame graph, these eight words need to already be in your head — fluent, not memorised. Here they are, one sentence each.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Word</th>
        <th class="border border-border px-3 py-2 text-left">What it is</th>
        <th class="border border-border px-3 py-2 text-left">Why it exists</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Latency</strong></td>
        <td class="border border-border px-3 py-2">How long one operation takes — the delay from "sent" to "answered."</td>
        <td class="border border-border px-3 py-2">Because users feel latency directly; a slow response is a latency problem.</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Throughput</strong></td>
        <td class="border border-border px-3 py-2">How many operations a system completes per unit of time.</td>
        <td class="border border-border px-3 py-2">Because a system can be fast per request but still collapse under high load.</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Profiling</strong></td>
        <td class="border border-border px-3 py-2">Running a tool that records exactly where your program spends its time.</td>
        <td class="border border-border px-3 py-2">So you replace guessing with evidence before touching a single line of code.</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Bottleneck</strong></td>
        <td class="border border-border px-3 py-2">The one slowest step that limits the whole system's speed.</td>
        <td class="border border-border px-3 py-2">Because speeding up anything else cannot make the overall system faster.</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Percentile (p50 / p99)</strong></td>
        <td class="border border-border px-3 py-2">p50 = the median request; p99 = the slowest 1 in 100.</td>
        <td class="border border-border px-3 py-2">Because averages hide the tail — a slow 1% of users is still many real people.</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>The tail</strong></td>
        <td class="border border-border px-3 py-2">The slowest outlier requests at high percentiles (p99, p999).</td>
        <td class="border border-border px-3 py-2">Because in microservices, one slow tail call can stall the whole user request.</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><strong>Big-O vs real cost</strong></td>
        <td class="border border-border px-3 py-2">Big-O describes how cost grows with input size; real cost includes constants, cache misses, and I/O.</td>
        <td class="border border-border px-3 py-2">Because an O(n²) algorithm on a tiny dataset can beat an O(n log n) one with poor cache behaviour.</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><strong>Measure-don't-guess</strong></td>
        <td class="border border-border px-3 py-2">The principle that every optimization decision must start from profiler data, not intuition.</td>
        <td class="border border-border px-3 py-2">Because premature optimization — fixing things before measuring — wastes engineering time and adds complexity.</td>
      </tr>
    </tbody>
  </table>
</div>

## How they fit together

The words tell one story in order: you **profile** a running system to find where time actually goes, then identify the single **bottleneck** limiting overall speed. You fix only that, then profile again. You judge the result not by the average but by **percentiles** — especially p99, because the **tail** is what real users on slow connections or unlucky timing actually experience. You keep **latency** and **throughput** in mind as separate axes: a change that lowers latency might not raise throughput, and vice versa. You remember that **big-O** is a growth model, not a promise — real cost includes memory layout and I/O that asymptotic notation ignores. And you never skip the first step: **measure-don't-guess** is not a slogan but the one habit that separates productive performance work from wasted effort.

<Inset kind="why" lang="en">
Why does premature optimization get such a bad reputation? Because it optimizes before the evidence exists. You spend a week making the wrong code faster, and now that code is also harder to read and change. The real bottleneck, untouched, still owns the response time. Profiling first takes thirty seconds and tells you exactly which thirty lines of code deserve a week of attention. That is the entire return on "measure first."
</Inset>

## You do not need to memorise this

Two honest notes before the climb. First: nobody carries all of this on day one — you will meet each word again in its own unit, in context, and it will stick then. This page is a coat-hook for the details, not a test. Second: averages lie in comfortable ways. When a dashboard says "average response time: 120 ms," that sounds fine — but if p99 is 4 seconds, one in a hundred users is waiting four seconds, and in a high-traffic system that is thousands of real people per hour. The senior instinct is to look at the tail first.

<Infographic
  svg={tailLatency}
  label="Column chart of the same system's response time read two ways: a 120 ms average bar dwarfed by a 4000 ms p99 bar — the slowest 1 in 100 requests waits about 33 times longer than the average."
  caption="The average bar looks fine; the p99 bar towering beside it is what the slow 1% of users actually feel. That gap is why seniors read the tail, not the mean."
  hue="var(--d-performance, var(--accent))"
/>

<Quiz
  id="00-start-here-performance-quiz-1"
  lessonSlug="01-overview"
  lang="en"
  question="Why is measuring before optimising more important than engineering intuition?"
  choices={[
    { label: "Because intuition is often wrong about where time goes — a profiler shows the actual bottleneck, so you fix the right thing instead of wasting effort on irrelevant code", correct: true },
    { label: "Because profilers are required by law in production systems", misconception: "There is no such requirement. Profiling matters because guessing which path is slow is unreliable — not because of any rule." },
    { label: "Because making code faster always makes it simpler", misconception: "Optimisations often add complexity (caches, indexes, pre-computation). That cost is only worth paying when profiler data shows the code is actually the bottleneck." },
    { label: "Because averages are always an accurate representation of user experience", misconception: "Averages hide the tail. A 120 ms average can coexist with a 4-second p99, meaning many real users wait far longer than the average suggests." },
  ]}
/>

<DragOrder
  id="00-start-here-performance-drag-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="Order the correct performance engineering loop:"
  items={[
    { id: "a", label: "Profile the running system to find where time actually goes" },
    { id: "b", label: "Identify the single bottleneck limiting overall speed" },
    { id: "c", label: "Fix only that bottleneck" },
    { id: "d", label: "Profile again and check percentiles to confirm improvement" },
  ]}
  correctOrder={["a", "b", "c", "d"]}
/>

</Explanation>

<EditorialDiagram
  kind="timeline"
  genre="the loop"
  labels={["measure", "profile", "fix hotspot", "measure again"]}
  note="never guess"
  label="The performance loop: measure first, let a profiler find the real bottleneck, fix that one hotspot, then measure again to confirm."
  caption="Every performance unit is one turn of this loop — judged by percentiles, not averages."
  hue="var(--d-performance, var(--accent))"
/>

<KeyTakeaway>Measure before you fix. Use a profiler to find the real bottleneck — never guess. Judge by percentiles: p99 reveals what the average hides. Optimise the one proven hotspot, then measure again.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="00-start-here-performance-retrieval"
  lang="en"
  questions={[
    { q: "In one breath: what is the core rule of performance work, and why does guessing fail?", a: "The core rule is measure-don't-guess: before changing a single line for speed, run a profiler to find out exactly where the time is going, identify the one real bottleneck, fix only that, then profile again to confirm. Guessing fails because intuition about which code is slow is wrong roughly half the time — and acting on a wrong guess makes you faster at something that was not the problem, while the real bottleneck sits unchanged and keeps every user waiting. The extra danger is that premature optimisations add complexity: the code becomes harder to read and change, so you pay a maintenance cost for a speed gain that did not address the actual constraint." },
    { q: "Why do senior engineers judge performance by p99 rather than average latency?", a: "Averages compress all requests into one number and hide the shape of the distribution. A 120 ms average can coexist with a p99 of 4 seconds — meaning one in a hundred requests takes four seconds. In a system handling ten thousand requests per minute, that is a hundred users per minute experiencing a four-second stall. The tail (high-percentile latency) is what those users actually feel. In microservices the problem compounds: if a single downstream call has a 1% chance of being slow, a request that fans out to ten services has roughly a 10% chance of hitting at least one slow tail call, making the tail the dominant experience for a meaningful fraction of users." },
  ]}
/>

<Recap lang="en">
Performance engineering is one rule with a lot of vocabulary hung off it: measure first, find the real bottleneck, fix only that, then measure again. The measuring half relies on a profiler — a tool that records where your program actually spends its time, replacing intuition with evidence. The judging half relies on percentiles rather than averages: p50 is the median experience, but p99 is what the slowest one in a hundred users actually waits through, and the tail matters more than the average in any system under real load. Latency and throughput are separate axes — a change can help one without helping the other. Big-O describes growth rate, not real cost; cache behaviour and I/O dominate in practice. The one habit that holds all of this together is measure-don't-guess: premature optimisation wastes effort and adds complexity before you know what to fix. You will meet each of these words in depth in its own unit. Now when you see a slow production system, your first move is not a fix — it is a profiler run to find out where the time actually goes.
</Recap>
