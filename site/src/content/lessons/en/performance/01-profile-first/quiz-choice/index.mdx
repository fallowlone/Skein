---
concepts:
- amdahl-law
- self-vs-cum-time
- measurement-scopes
- observer-effect
- statistical-baseline
- flame-graph-shapes
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://dl.acm.org/doi/10.1145/356635.356640
- https://www.brendangregg.com/methodology.html
- https://en.wikipedia.org/wiki/Amdahl%27s_law
status: ready
summary: Multiple-choice synthesis across the profile-first unit — Amdahl ceilings, self vs cum-time, measurement scopes, the observer effect, statistical baselines, and flame-graph shape reading.
title: 'Profile first: multiple-choice review'
track: performance
unit: 01-profile-first
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one mirrors a decision you make in a real "the service is slow" investigation — not a definition to recite, but a judgement call to defend with numbers.
</Hook>

<Goal lang="en">
Confirm you can connect the measurement loop, Amdahl's ceiling, scope selection, the observer effect, statistical reporting, and flame-graph reading — the synthesis the individual lessons built toward.
</Goal>

<Quiz
  id="01-profile-first-choice-1"
  lessonSlug="01-profile-first"
  lang="en"
  question="A profile shows your top function at 12% of total CPU. A teammate proposes a heroic rewrite that would make it 8x faster. Before approving, what is the decisive number and what does it say?"
  choices={[
    { label: "The Amdahl ceiling: 1 / ((1 − 0.12) + 0.12/8) ≈ 1.12x. Even an 8x local win caps total speedup near 12% — likely below the bar for a risky rewrite. The function's SHARE, not its local speedup, decides.", correct: true },
    { label: "The 8x local speedup — an 8x win on the hottest function is always worth shipping.", misconception: "Local speedup is meaningless without the share. Amdahl bounds total improvement by p; an 8x win on a 12% function buys ~12% total, not 8x." },
    { label: "Nothing measurable yet — you must ship it and watch production p99 to know.", misconception: "Amdahl lets you predict the ceiling before writing a line. Shipping a risky rewrite to discover a 1.12x ceiling you could have computed is exactly the waste profile-first prevents." },
    { label: "The function's self-time — if self-time is high the rewrite is justified regardless of share.", misconception: "Self-time tells you WHERE the work is, not whether the win is worth it. A function can be 100% self-time and still only 12% of total — Amdahl still caps the win at ~12%." },
  ]}
/>

<Quiz
  id="01-profile-first-choice-2"
  lessonSlug="01-profile-first"
  lang="en"
  question="A flame graph shows one function with cum-time 80% but self-time 2%, sitting above a single wide leaf. What is it, and where does the fix live?"
  choices={[
    { label: "It is a routing/dispatch frame — its own code costs ~nothing (2% self), and the 80% belongs to the callee below it. Walk down to the wide leaf with large self-time; the fix lives there, not in the dispatcher.", correct: true },
    { label: "It is the bottleneck — 80% cum-time means rewriting that function is the highest-leverage fix.", misconception: "Cum-time includes callees. With 2% self-time the function's own instructions barely cost anything; rewriting it saves at most 2%. The real cost is in what it calls." },
    { label: "The profiler is miscounting — cum and self should be close for a real hotspot.", misconception: "Huge cum-time with tiny self-time is the normal, expected signature of HTTP handlers, middleware, and dispatch layers. It is a reading instruction, not an error." },
    { label: "The horizontal width means it ran first, so fix the earliest-executing code.", misconception: "Flame-graph x-position is alphabetical grouping, not time order. Left-vs-right says nothing about execution sequence; width is sample count." },
  ]}
/>

<Quiz
  id="01-profile-first-choice-3"
  lessonSlug="01-profile-first"
  lang="en"
  question="A microbenchmark says a new hash is 10x faster. You want to know if your API will get faster. Which measurement answers that honestly, and why?"
  choices={[
    { label: "A production CPU profile to measure hashing's SHARE (p) of total time. The microbench gives the local speedup s; only the profile gives p, and Amdahl needs both to predict the API win before you ship.", correct: true },
    { label: "Run the microbenchmark 1000x and report the mean — high iteration count makes it production-representative.", misconception: "Iterations fix variance, not scope. A microbench measures the function in isolation and can never tell you its share of API wall-clock — that requires a production profile." },
    { label: "Trust the 10x and ship — a 10x win on a common bottleneck always moves the API.", misconception: "If hashing is 3% of API time, Amdahl caps the gain at ~1.03x — invisible to users. Reasoning by category ('hashing is a common bottleneck') instead of measuring share is the original sin profile-first prevents." },
    { label: "A staging macrobenchmark alone is the honest answer.", misconception: "A macrobench validates end-to-end but depends on synthetic load matching real traffic; it is a final check, not the share measurement. The production profile is the honest source of p." },
  ]}
/>

<Quiz
  id="01-profile-first-choice-4"
  lessonSlug="01-profile-first"
  lang="en"
  question="An instrumentation profiler reports a hot Java method at 40 ms/call; a sampling profiler reports 8 ms/call for the same method. Which reflects production, and what causes the gap?"
  choices={[
    { label: "The sampling profiler (~8 ms). Instrumentation wraps every entry/exit (20–100% overhead in JIT runtimes) and blocks the inlining the production JIT would apply, so it inflates and distorts the timing. Sampling perturbs under 2% and stays representative.", correct: true },
    { label: "The instrumentation profiler — it times every call exactly, so it is the ground truth.", misconception: "Exact call counts only help when measurement cost is negligible. In a JIT runtime, instrumentation disables inlining and adds 20–100% overhead, so it measures a different (slower) program than production runs." },
    { label: "Both equally valid — they just report different units.", misconception: "Same method, same units (ms/call), wildly different numbers because of different perturbation levels. The observer effect makes them measure different systems; sampling is the production-representative one." },
    { label: "Neither — you need hardware counters to time a method correctly.", misconception: "Hardware counters explain WHY a function is hot (memory- vs compute-bound); they do not resolve the instrumentation-overhead gap. Sampling does." },
  ]}
/>

<Quiz
  id="01-profile-first-choice-5"
  lessonSlug="01-profile-first"
  lang="en"
  question="A PR claims '15% faster' from one staging run. A second engineer reruns the same code unchanged and sees a 7% swing. How do you read the original claim?"
  choices={[
    { label: "Indistinguishable from noise. The natural run-to-run noise floor is ~5–10% (GC timing, scheduler, cache warmth, contention). A single-run 15% is not a measurement; demand median + p95 + p99 across at least 5 runs with a confidence interval.", correct: true },
    { label: "The 15% is real — staging is a controlled environment, so its single run is trustworthy.", misconception: "Controlled does not mean noise-free. Staging still has GC pauses, scheduler decisions, and cache-state variation producing 5–10% swings between identical runs, as the rerun just demonstrated." },
    { label: "Report the mean across the two runs (≈11%) and ship.", misconception: "Two runs cannot establish a distribution, and mean is the wrong statistic — it is dominated by outliers and hides the tail. You need the median, p95/p99, and a CI across at least 5 runs." },
    { label: "The 7% rerun swing proves the change is a regression.", misconception: "The swing proves the measurement is noisy, not that the change is bad. Neither run is trustworthy alone; you cannot judge sign or magnitude without a distribution." },
  ]}
/>

<Quiz
  id="01-profile-first-choice-6"
  lessonSlug="01-profile-first"
  lang="en"
  question="A flame graph shows thin spikes scattered across the full width with no dominant leaf, yet p99 latency is terrible. What does the shape tell you and what do you capture next?"
  choices={[
    { label: "No CPU hotspot exists — the latency is off-CPU (waiting on I/O, locks, GC, or network). A CPU profile cannot explain it; capture an off-CPU / block / wait profile (e.g. eBPF off-CPU or Go's block profile) and diff that instead.", correct: true },
    { label: "The profiler is broken — a real bottleneck always shows a wide leaf.", misconception: "Scattered thin spikes are a recognisable, valid shape meaning the time is not on-CPU. It is a diagnosis, not a malfunction." },
    { label: "The service is already optimal — flat CPU means there is nothing to fix.", misconception: "Flat CPU means the bottleneck is off-CPU, not absent. With bad p99 the time is going somewhere — into waiting, which a CPU profile is blind to." },
    { label: "Raise the sampling rate until a hotspot appears.", misconception: "A higher rate renders the same scattered shape more densely; it cannot consolidate off-CPU time into a CPU hotspot because that time was never on-CPU." },
  ]}
/>

<Recap lang="en">
The unit's through-line is one investigation: quantify the complaint, reproduce under realistic load, capture a baseline, read the SHAPE before the names, and let the numbers — not intuition — name the hotspot. Amdahl's ceiling decides whether a fix is worth it (share beats local speedup); self vs cum-time decides where to look; scope selection and the observer effect decide which measurement to trust; statistical baselines decide whether a "win" is real; and a scattered flame graph tells you the bottleneck is off-CPU. Every wrong answer above is a real way engineers fool themselves into optimising the wrong code.
</Recap>
