---
concepts:
- measurement-loop
- amdahl-prediction
- flame-graph-reading
- statistical-baseline
- observer-effect
estMin: 240
lang: en
level: senior
order: 10
prereqs: []
slug: project
sources:
- https://www.brendangregg.com/methodology.html
- https://pkg.go.dev/runtime/pprof
status: ready
summary: "Hands-on project — run the full profile-first loop on a deliberately misleading slow service: quantify, profile, predict with Amdahl, fix the real hotspot, and prove the win with statistically valid before/after numbers."
title: 'Profile first: run the loop on a misleading service'
track: performance
unit: 01-profile-first
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Reading about profile-first is not the same as catching intuition lying to you. Build a service whose obvious-looking bottleneck is a decoy, then run the full measurement loop until the profile — not your gut — names the real cost, and prove every step with numbers.
</Hook>

<Goal lang="en">
Turn the unit's mental model into a reproducible engineering loop: quantify the complaint, reproduce under realistic load, read the flame graph, predict the win with Amdahl, fix the real hotspot, and verify with statistically valid before/after metrics — confirming intuition was wrong before you trust the measurement.
</Goal>

<ProjectBrief
  id="01-profile-first-project"
  lessonSlug="01-profile-first"
  lang="en"
  objective="Take an HTTP service with a deliberately misleading slow endpoint (your own or the starter described below) and run the complete profile-first loop — quantify, reproduce, profile, predict, fix, verify — driving p99 to target while proving with a profile that the obvious suspect was NOT the bottleneck."
  requirements={[
    "Build or take a small HTTP service (Go / JVM / Node / Python) with one slow endpoint whose apparent cause is a decoy — e.g. a plausible-looking DB query or a 'heavy' computation that is actually cheap, while the real cost sits in a synchronous log serializer, a per-request JSON marshal, or an off-CPU wait that runs before the suspect.",
    "Write down the complaint as a measurable target FIRST: 'p99 of endpoint X is currently N ms; the bar is M ms.' No optimisation may start before this number exists.",
    "Reproduce under realistic load with a macrobench (k6 / vegeta / JMeter / locust) that mirrors request mix and body sizes — not a single curl. Verify the profiler's overhead by capturing the headline metric with the profiler off and on; confirm they agree within ~5% (observer-effect check).",
    "Capture a baseline CPU profile of statistically valid duration (≥30 s at 100 Hz) with the language-appropriate sampling profiler (pprof / async-profiler / py-spy / --prof). Read the flame graph by SHAPE first, then names; record self-time vs cum-time for the top frames.",
    "State your pre-profile intuition in writing, then show the profile contradicting it — name the real hotspot with its share of total CPU, and explicitly note that the obvious suspect is a small fraction.",
    "If the flame graph shows scattered thin spikes (no CPU hotspot), capture an off-CPU / block / wait profile and identify the off-CPU cost — the decoy endpoint should force this at least once.",
    "Compute the Amdahl ceiling for fixing the real hotspot from its measured share, form ONE hypothesis with a predicted speedup, then apply only that change in isolation.",
    "Re-profile under the identical load, diff against baseline, and confirm the hotspot frame shrank by roughly the predicted amount; run the macrobench ≥5 times and report median + p95 + p99 with a confidence interval, before and after.",
  ]}
  acceptance={[
    "A before/after table: p50, p95, p99 endpoint latency across ≥5 runs each, with a 95% CI — measured under identical load, not estimated, and showing p99 met the stated target.",
    "A short write-up stating the pre-profile intuition, the profile that contradicted it, the real hotspot with its CPU (or off-CPU) share, and the Amdahl-predicted vs actual total speedup.",
    "The profile diff clearly shows the real hotspot frame shrinking after the fix (re-profiled, not assumed), and the decoy suspect confirmed as a small share throughout.",
    "Evidence of the observer-effect check: the headline metric with profiler off vs on, within ~5%.",
  ]}
  stretch={[
    "Add a one-page triage runbook: how to quantify a 'slow' complaint, pick the measurement scope (micro/macro/prod), read flame-graph shapes, and verify a win statistically — the loop a new on-call should follow.",
    "Wire a profile-diff CI gate: deploy the PR branch to a canary, run a 5-minute load test, capture CPU + off-CPU profiles, diff against a weekly-refreshed main baseline, and fail the build if any function's CPU share grows ≥10% absolute or ≥30% relative.",
    "Add hardware-counter analysis (perf stat -e cycles,instructions,cache-misses) on the real hotspot; report its IPC and decide from the number whether the next fix should target data layout or the algorithm.",
    "Add a cold-start profile (first 30–60 s after launch) alongside the steady-state one and show that the hotspots differ — then name a cold-start-specific fix (eager warmup, connection pre-warming, or AOT) the steady-state profile would never have suggested.",
  ]}
/>

<Recap lang="en">
This is the loop you will run in every real performance investigation: quantify the complaint into a target, reproduce under realistic load, verify the profiler is not lying (observer effect), read the flame graph by shape before names, let the measurement contradict your intuition, predict the ceiling with Amdahl, fix the one real hotspot, and confirm with median/p95/p99 across multiple runs. Building it once on a service whose obvious bottleneck is a decoy is what makes "profile first, then change code" muscle memory instead of a slogan.
</Recap>
