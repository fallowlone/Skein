---
slug: 04-reading-flame-graphs
lang: en
track: performance
unit: 01-profile-first
order: 4
title: "Reading flame graphs: shapes, per-language profilers, and the 60-second scan"
summary: "Flame graphs have five recognisable shapes that a senior engineer reads before zooming into names. Per-language profiler picks ensure representative measurements, not debug-build artefacts."
estMin: 16
status: ready
lessonType: topic
level: middle
prereqs: ["performance/01-profile-first/03-measurement-loop"]
deepensInto: []
spiral: ["latency"]
concepts: ["flame-graph", "call-stack", "profiler", "pprof", "async-profiler", "eBPF", "per-language-profiling"]
sources:
  - https://www.brendangregg.com/flamegraphs.html
  - https://pkg.go.dev/runtime/pprof
  - https://github.com/grafana/pyroscope
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";

<Hook>
A senior engineer opens a flame graph and says "volcano pattern — the fix is not in the wide leaf, it is in the dispatch layer two levels up." They have not read a single function name yet. Shape literacy cuts the time from profile to hypothesis from minutes to seconds.
</Hook>

<Crux>Flame graph x-axis is alphabetical order, not time. Width = sample count. Read shapes before names; the shape tells you where the fix lives.</Crux>

<Explanation>

## Reading a flame graph in 60 seconds

Brendan Gregg's flame graph format is the canonical readout.

- **Y-axis**: call stack depth. Entry point (`main`, HTTP handler) at the bottom; the leaf function on CPU at the top.
- **X-axis**: alphabetical order of sibling stacks. Position left/right tells you nothing about call order.
- **Width**: proportional to sample count = CPU time share.

Reading workflow:

1. Scan the **top** of the graph for the widest leaf frames — those are the functions on CPU when sampled.
2. Walk **down** from a wide leaf to see which parent called it, and whether the path is the same across most samples or fragments across many callers.
3. If most samples converge on the leaf through **one path**: that is the hot path; fix it there.
4. If the leaf is called from **many parents** each contributing a thin slice: the fix is the leaf itself, not its callers.

The most common rookie mistake: reading horizontal position as time order. It is not. Two frames side-by-side tell you nothing about which executed first.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Axis</th>
        <th class="border border-border px-3 py-2 text-left">Meaning</th>
        <th class="border border-border px-3 py-2 text-left">Common misreading</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Width (x)</td>
        <td class="border border-border px-3 py-2">Sample count — CPU time share</td>
        <td class="border border-border px-3 py-2">People read left-to-right as "time order" — it is NOT</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Position (x)</td>
        <td class="border border-border px-3 py-2">Alphabetical grouping by parent</td>
        <td class="border border-border px-3 py-2">Left frame does NOT run before right frame</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Height (y)</td>
        <td class="border border-border px-3 py-2">Call depth — entry at bottom, leaf at top</td>
        <td class="border border-border px-3 py-2">Taller stack = deeper nesting, not slower</td>
      </tr>
    </tbody>
  </table>
</div>

## Five flame graph shapes and what they mean

An experienced eye reads a flame graph by shape in seconds, before zooming into names.

**1. Tall narrow column running to the top through one path** — recursion or a deep middleware chain. One fix on the leaf or near it.

**2. Wide plateau at one level with dozens of thin leaves** — a dispatcher fanning out to many handlers. The fix is usually in the dispatcher or the shared path above it, not in any one handler.

**3. Two adjacent wide columns with no shared parent** — multiple hot paths needing separate fixes. Amdahl applies to each independently.

**4. "Volcano" widening from leaf to root** — large cum-time at one entry-point with work distributed below. The fix depends on which level stops being real work and becomes dispatch; walk the shape.

**5. Thin spikes scattered across the width** — no real hotspot. The latency problem is elsewhere (off-CPU, coordination, network). A CPU profile alone will not show it; try an off-CPU or allocation profile.

These shapes are the vocabulary senior engineers use to describe what a profile looks like before naming functions.

## Per-language profiler picks

Every modern language has a recommended sampling profiler. Using the right one avoids debug-build artefacts and JVM stack-walk errors.

- **Go**: built-in `pprof` via `runtime/pprof` and `net/http/pprof` — industry standard. Read by `go tool pprof`, exports pprof.proto.
- **Java**: JFR (Java Flight Recorder, default since JDK 11) and async-profiler — production standard because it walks Java stacks correctly via AsyncGetCallTrace, avoiding safepoint bias.
- **Python**: py-spy (external sampler, no app changes required) and scalene. `cProfile` is instrumentation-based — dev-only, not production.
- **Node.js**: built-in `--prof` + clinic.js for flame graphs; 0x for production.
- **Rust**: pprof-rs; `perf` with addr2line for symbol resolution.
- **Ruby**: stackprof and rbspy.
- **PHP**: tideways, Excimer.
- **C/C++**: `perf` + perf-tools; Intel VTune for microarchitectural analysis.

**Continuous-profile backends** (Pyroscope, Parca, Polar Signals, Datadog Continuous Profiler) ingest pprof or JFR from any of these. eBPF agents capture stacks kernel-side and work language-agnostic via DWARF unwinding for compiled binaries or JIT-emitted perf maps for V8 and JVM.

The senior choice: one primary profiler per language, one universal eBPF baseline across infrastructure. A polyglot team should normalise all profiles into one backend and read them in one tool.

<Inset kind="why" lang="en">
async-profiler for Java is the canonical production choice rather than JVMTI-based profilers precisely because of AsyncGetCallTrace: it can walk Java stacks at any point, not only at safepoints. Safepoint-biased profilers over-count time spent on methods that have safepoints (typically hot loops) and under-count time in native methods. The bias can point you at the wrong hotspot in GC-heavy workloads.
</Inset>

<DragOrder
  id="04-reading-flame-graphs-drag-1"
  lessonSlug="04-reading-flame-graphs"
  lang="en"
  prompt="Order the 60-second flame graph reading workflow:"
  items={[
    { id: "a", label: "Scan the top edge — find the widest leaf frames (these are the functions on CPU)" },
    { id: "b", label: "Identify the shape before reading names (tall column? wide plateau? scattered spikes?)" },
    { id: "c", label: "Walk down from the widest leaf to see which parent called it most often" },
    { id: "d", label: "Check whether most samples converge through one path or fan out from many callers" },
    { id: "e", label: "If one path: the hot path is there; if many callers: the fix is in the leaf itself" },
    { id: "f", label: "Read the function names and look up their self-time vs cum-time in the profile" },
  ]}
  correctOrder={["b", "a", "c", "d", "e", "f"]}
/>

<Quiz
  id="04-reading-flame-graphs-quiz-1"
  lessonSlug="04-reading-flame-graphs"
  lang="en"
  question="A flame graph shows thin spikes scattered across the full width with no dominant leaf frame. What does this tell you about the latency problem?"
  choices={[
    { label: "The latency is not on-CPU — it is off-CPU (waiting on I/O, locks, GC, network). A CPU profile alone cannot explain it; an off-CPU or wait profile is needed.", correct: true },
    { label: "The profiler is broken", misconception: "Scattered thin spikes are a recognisable pattern meaning no CPU hotspot." },
    { label: "The application is already optimised", misconception: "Absence of a CPU hotspot means the bottleneck is elsewhere, not that the application is fast." },
    { label: "The sampling rate is too low", misconception: "A higher rate would show the same pattern more densely; it would not consolidate the spikes." },
  ]}
/>

<Quiz
  id="04-reading-flame-graphs-quiz-2"
  lessonSlug="04-reading-flame-graphs"
  lang="en"
  question="Why is async-profiler the preferred production profiler for JVM applications rather than a JVMTI-based sampler?"
  choices={[
    { label: "It uses AsyncGetCallTrace to walk stacks at any point, avoiding safepoint bias that over-counts hot loop methods and under-counts native methods.", correct: true },
    { label: "It is faster to install", misconception: "Installation ease is not the technical reason; the bias correction is." },
    { label: "It works with all Java versions", misconception: "Version compatibility is a practical concern, not the primary reason async-profiler is recommended." },
    { label: "It uses less memory", misconception: "Memory overhead is not the distinguishing factor; unbiased stack walking is." },
  ]}
/>

</Explanation>

<KeyTakeaway>Read the flame graph shape before reading names: tall column = recursion/chain; wide plateau = dispatcher; scattered spikes = off-CPU problem. Per-language profiler choice matters — async-profiler for JVM avoids safepoint bias; py-spy for Python avoids GIL interference.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-reading-flame-graphs-retrieval"
  lang="en"
  questions={[
    { q: "What are the five flame graph shapes and what fix does each suggest?", a: "1) Tall narrow column through one path: recursion or deep chain; fix the leaf or break the chain. 2) Wide plateau with thin leaves: dispatcher fanning to many handlers; fix the dispatcher or shared path above it. 3) Two adjacent wide columns with no shared parent: two independent hot paths; fix each with separate Amdahl estimates. 4) Volcano widening leaf-to-root: work distributed below a single entry-point; find where the shape transitions from real work to dispatch and fix there. 5) Thin scattered spikes: no CPU hotspot; problem is off-CPU (I/O, locks, GC, network); switch to an off-CPU or allocation profile." },
    { q: "What is safepoint bias in JVM profilers and why does it matter for reading flame graphs?", a: "JVMTI-based sampling profilers can only walk the call stack at safepoints — specific points in bytecode where the JVM stops for GC or deoptimisation. Methods that happen to have safepoints at their hot inner loops are over-counted; methods that spend most time in native code (with no safepoints) are under-counted. The result is a flame graph that over-represents some methods and under-represents others — potentially pointing you at the wrong hotspot. async-profiler uses AsyncGetCallTrace to walk stacks at any point, not only safepoints, giving an unbiased sample distribution that reflects true CPU time." },
  ]}
/>

<Recap lang="en">
A flame graph's x-axis is alphabetical grouping, not time — the most common misread. Width is sample count (CPU time share); the widest leaf is the hottest function. Shape reading before name reading: tall column means recursion or chain fix; wide plateau means dispatcher fix; scattered spikes mean off-CPU problem not visible in a CPU profile. Per-language profiler choice is not aesthetic — async-profiler avoids JVM safepoint bias; py-spy avoids Python GIL interference; pprof is Go's built-in standard. Continuous profiling backends (Pyroscope, Parca) normalise all formats into one view across a polyglot stack.
</Recap>
