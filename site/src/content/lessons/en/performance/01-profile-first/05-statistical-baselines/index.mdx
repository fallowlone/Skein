---
concepts:
- statistical-baseline
- noise
- median
- p99
- confidence-interval
- performance-reporting
deepensInto: []
estMin: 12
lang: en
lessonType: topic
level: middle
order: 5
prereqs:
- performance/01-profile-first/03-measurement-loop
slug: 05-statistical-baselines
sources:
- https://www.brendangregg.com/methodology.html
- https://www.brendangregg.com/systems-performance-2nd-edition-book.html
- https://en.wikipedia.org/wiki/Amdahl%27s_law
spiral:
- latency
status: ready
summary: A single benchmark run cannot distinguish a real 15% speedup from natural
  noise. Report median, p95, p99 across multiple runs with confidence intervals —
  anything less is a guess wearing a number.
title: 'Statistical baselines: why one run is not a measurement'
track: performance
unit: 01-profile-first
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
"This PR makes the service 15% faster" — based on one staging run, posted to the review. Is it real? The noise floor in most systems is 5-10% between identical runs. Without more runs and percentiles, you cannot tell.
</Hook>

<Crux>A single run cannot distinguish signal from noise. Report median + p95 + p99 across ≥5 runs — anything less is fiction.</Crux>

<Explanation>

## Why a single run is unreliable

The same workload run twice may differ by 5-10% from sources unrelated to the code:

- GC timing differences (stop-the-world windows shift between runs)
- OS scheduler decisions (which CPU core, when preempted)
- Cache state at run start (L3 cold vs warm)
- Transient network or disk contention
- CPU frequency scaling on laptops (throttling under thermal load)

A "15% speedup" from one run cannot be distinguished from this noise. It might be a real win, it might be a coincidence, you cannot tell.

## What to report instead

- **Minimum runs**: 5-10 for macrobench, 30+ for short microbench.
- **Report**: median, p95, and p99 — not mean. Mean is dominated by outliers; median is robust to them.
- **Include spread**: standard deviation or confidence interval.
- **What a defensible result looks like**: "p99 latency dropped from 480 ms to 320 ms across five 5-minute runs, p50 from 60 ms to 55 ms, with 95% CI of ±15 ms on each measurement."

A "10% speedup" is real only if median improved by ≥10% AND p95/p99 did not regress.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Reported as</th>
        <th class="border border-border px-3 py-2 text-left">Problem</th>
        <th class="border border-border px-3 py-2 text-left">Better alternative</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Single run, single number</td>
        <td class="border border-border px-3 py-2">Indistinguishable from 5-10% natural noise</td>
        <td class="border border-border px-3 py-2">5+ runs, report median + p99 + CI</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Mean latency</td>
        <td class="border border-border px-3 py-2">One outlier skews the average</td>
        <td class="border border-border px-3 py-2">Median + p95/p99 percentiles</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Staging-only measurement</td>
        <td class="border border-border px-3 py-2">Synthetic load may not match production</td>
        <td class="border border-border px-3 py-2">Staging + production canary confirmation</td>
      </tr>
    </tbody>
  </table>
</div>

## Why mean is the wrong metric

Latency distributions have a long tail. A service that handles 99% of requests in 50 ms but 1% in 2000 ms has a mean around 70 ms — which hides the 2000 ms tail that users experience. Mean is dominated by outliers in either direction. Median (p50) is robust to outliers; p99 captures the tail experience.

The discipline: report numbers like a scientist, with distributions and uncertainty — not like a marketer, with single percentages.

<Inset kind="why" lang="en">
The discipline pays off most when a claimed "win" turns out to be noise. A team that catches a noise win before merging saves: the PR churn of reverting a "regression" later when production shows no improvement, the time spent writing a postmortem for a deploy that did nothing, and the loss of trust when the team learns perf numbers cannot be believed without methodology.
</Inset>

<Quiz
  id="05-statistical-baselines-quiz-1"
  lessonSlug="05-statistical-baselines"
  lang="en"
  question="A team reports 'this change made the service 15% faster' based on one staging run. What is the most important follow-up question?"
  choices={[
    { label: "Across how many runs and at what percentile? A single run cannot distinguish a real 15% win from natural noise (typically 5-10% between runs).", correct: true },
    { label: "Did you deploy yet?", misconception: "Important operationally but does not validate the claim itself." },
    { label: "Did you write tests?", misconception: "Correctness is a separate axis; the question here is whether the perf claim is statistically sound." },
    { label: "Did you document the change?", misconception: "Documentation is independent of statistical validity." },
  ]}
/>

<Quiz
  id="05-statistical-baselines-quiz-2"
  lessonSlug="05-statistical-baselines"
  lang="en"
  question="Why is mean latency the wrong metric for reporting service performance?"
  choices={[
    { label: "Mean is dominated by outliers — one 2000 ms request can push the mean far above the typical experience, while median and p99 accurately describe the distribution.", correct: true },
    { label: "Mean is too slow to compute", misconception: "Mean is computationally trivial; the problem is statistical, not computational." },
    { label: "Percentiles are required by the SLO spec", misconception: "The reason for percentiles is statistical validity, not spec compliance." },
    { label: "Mean only works for normally distributed data", misconception: "Technically true but not the engineering reason; the deeper issue is that tails are invisible in the mean." },
  ]}
/>

<Quiz
  id="05-statistical-baselines-quiz-3"
  lessonSlug="05-statistical-baselines"
  lang="en"
  question="You run a microbenchmark 1000 times and compute the mean. It shows a 12% improvement. What additional information do you need before claiming the improvement is real?"
  choices={[
    { label: "The standard deviation or confidence interval, and whether the p99 regressed — a 12% mean improvement with high variance and a p99 regression may be net negative.", correct: true },
    { label: "More iterations until the mean converges", misconception: "More iterations reduce variance but the distribution shape matters as much as convergence." },
    { label: "The improvement expressed in absolute time, not percent", misconception: "Absolute vs relative is a communication choice; it does not resolve the statistical validity question." },
    { label: "Nothing; 1000 iterations is always sufficient", misconception: "Sample count alone does not tell you the spread; a tight CI requires both sufficient samples and low variance." },
  ]}
/>

</Explanation>

<KeyTakeaway>Never report a single performance number. Report median + p95 + p99 across ≥5 runs with confidence intervals. A single run is indistinguishable from the 5-10% natural noise floor; anything less than a distribution is a guess wearing a number's clothes.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-statistical-baselines-retrieval"
  lang="en"
  questions={[
    { q: "Why is reporting a single 'X% speedup' number from one benchmark run a red flag, and what should be reported instead?", a: "Performance numbers are inherently noisy. Two consecutive runs of the same workload typically vary by 5-10% from sources unrelated to the code: GC timing differences, OS scheduler decisions, cache state at start, transient network or disk contention. A single-run '15% speedup' cannot be distinguished from this noise. What should be reported: median, p95, and p99 across multiple runs (5-10 for macrobench, 30+ for short microbench), with the spread (standard deviation or confidence interval) explicit. A defensible result looks like: 'median latency dropped from 240 ms to 195 ms across eight 5-minute runs, p99 from 820 ms to 640 ms, with 95% CI of ±10 ms on each measurement.'" },
    { q: "What are the four main sources of noise in performance measurements, and which can be controlled?", a: "GC timing: the garbage collector may pause at different times across runs; controllable by running enough samples that GC pauses average out, or by triggering GC before each run. OS scheduler: thread preemption timing varies; partially controllable by binding to specific CPUs and using high-priority processes. Cache state: L3 cache warmth at run start varies; controllable by running a warmup pass before measurement. Transient contention: network or disk activity from other processes; partially controllable by running on isolated hardware, but production measurements cannot fully control this. The statistical approach is to gather enough runs that these effects average out and are captured in the confidence interval." },
  ]}
/>

<Recap lang="en">
Performance measurements are distributions, not single numbers. The natural noise floor is 5-10% between identical runs of the same code, from GC timing, OS scheduler, cache state, and transient contention. A single-run "15% speedup" is statistically indistinguishable from noise. Defensible reporting requires median, p95, and p99 across at least five runs with explicit confidence intervals. Mean latency is the wrong metric because it is dominated by outliers; median and tail percentiles correctly describe user experience. The discipline of reporting ranges instead of single values catches noise wins before they are merged and builds the team's trust in performance claims.
</Recap>
