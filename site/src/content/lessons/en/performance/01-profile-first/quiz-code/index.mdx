---
concepts:
- microbenchmark-pitfalls
- dead-code-elimination
- pprof-diff
- hardware-counters
- amdahl-arithmetic
estMin: 14
lang: en
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://www.brendangregg.com/methodology.html
- https://pkg.go.dev/runtime/pprof
- https://man7.org/linux/man-pages/man2/perf_event_open.2.html
status: ready
summary: Read a microbenchmark, a profiler diff, a perf hardware-counter readout, and an Amdahl calculation, then predict the behaviour and pick the highest-leverage fix.
title: 'Profile first: code and trace reading'
track: performance
unit: 01-profile-first
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Benchmarks, profiler diffs, and counter readouts are where profile-first lives or dies. Read each artefact and choose the move a senior engineer makes first — before touching a tuning knob or trusting a headline number.
</Hook>

<Goal lang="en">
Practise the loop you run in every performance investigation: read the benchmark or trace, spot the lie or the signal, and reach for the highest-leverage interpretation before changing anything.
</Goal>

## Snippet 1 — the benchmark that is too fast

```go
func BenchmarkHash(b *testing.B) {
    data := []byte("fixed-input-string")
    for i := 0; i < b.N; i++ {
        _ = fnv32(data)   // result discarded
    }
}
// reported: 0.31 ns/op — faster than a single memory load
```

<Quiz
  id="01-profile-first-code-1"
  lessonSlug="01-profile-first"
  lang="en"
  question="0.31 ns/op is below the cost of one L1 load. What happened, and what is the fix?"
  choices={[
    { label: "Dead-code elimination: the result is discarded, so the optimiser deleted the call (and likely the loop body). The number is fiction. Fix: consume the result via a sink — assign to a package-level var or use b.Run with a sink, and parameterise the input so it is not a compile-time constant.", correct: true },
    { label: "fnv32 really is that fast on modern CPUs.", misconception: "0.31 ns/op is below a single L1 cache load (~1 ns); no real hash of a byte slice runs that fast. The compiler removed the work because the result is unused — the benchmark measures nothing." },
    { label: "b.N is too small, so the timer is imprecise.", misconception: "testing.B grows b.N until the run is long enough; precision is not the issue. The issue is dead-code elimination removing the body entirely, plus a constant input the optimiser can fold." },
    { label: "Switch to a sampling profiler to get the real number.", misconception: "A profiler does not fix a benchmark that measures eliminated code. The defect is in the benchmark: consume the result and use a non-constant input so the work actually runs." },
  ]}
/>

## Snippet 2 — the profiler diff vs the production metric

```
# Production (5-min window, after deploy)
checkout_p99_ms        580   (prev 820)   # 29% faster
cpu_pct                 62   (prev 58)    # CPU went UP

# go tool pprof -diff_base baseline.cpu prod.cpu
Showing nodes accounting for -3.20s, 1.15% of -278.5s total
      flat  flat%        cum   cum%
    -1.80s  0.64%     -1.80s  0.64%  net/http.(*conn).serve
    -1.40s  0.50%     -1.40s  0.50%  encoding/json.Marshal
```

<Quiz
  id="01-profile-first-code-2"
  lessonSlug="01-profile-first"
  lang="en"
  question="p99 dropped 29% but the CPU profile diff shows only ~1% net CPU change and CPU% rose. How do you reconcile this, and what do you capture next?"
  choices={[
    { label: "The wall-clock win came from off-CPU time, not CPU. The deploy cut waiting (faster DB/lock/network), so the service spends more of its wall-clock computing — hence slightly higher CPU%. A CPU profile is blind to this; capture an off-CPU / block profile and diff that to confirm the shrinking wait frame.", correct: true },
    { label: "The metrics are wrong — a 29% p99 win must show as a large CPU drop.", misconception: "Wall-clock latency is not only CPU. For an I/O- or lock-bound service the saving is off-CPU wait time, which never appears in a CPU profile. The metrics and the tiny CPU diff are perfectly consistent." },
    { label: "CPU went up, so the deploy is a regression — roll back.", misconception: "Higher CPU after a fix is often a good signal: the service does real work instead of waiting. p99 improved 29% — judge against the SLO, not the CPU number in isolation." },
    { label: "Raise the CPU sampling rate to find the missing time.", misconception: "The missing time was never on-CPU, so no sampling rate will surface it in a CPU profile. You need an off-CPU profile to see DB-wait or mutex-wait frames." },
  ]}
/>

## Snippet 3 — the hardware-counter readout

```
$ perf stat -e cycles,instructions,cache-misses ./svc bench-json
   142,310,884,001  cycles
    61,994,210,773  instructions     #  0.44  insn per cycle
     1,902,544,118  cache-misses
# flame graph: parseJSON is 35% of CPU, single wide leaf
```

<Quiz
  id="01-profile-first-code-3"
  lessonSlug="01-profile-first"
  lang="en"
  question="parseJSON is 35% of CPU. IPC is 0.44 with a huge cache-miss count. What fix is most likely to help, and which one will not?"
  choices={[
    { label: "Memory-stalled: IPC under 1.0 plus high cache-misses means the CPU is waiting on RAM, not computing. Fix the data layout — smaller structs, struct-of-arrays, arena allocation, cache-friendly traversal. A faster parser algorithm or SIMD will NOT help while the bottleneck is memory access.", correct: true },
    { label: "Swap in a faster JSON parser — 35% of CPU is a clear algorithmic hotspot.", misconception: "IPC 0.44 says the function is memory-stalled, not compute-bound. A different parser that still chases the same scattered pointers sees the same stalls. The lever is data layout, not the algorithm." },
    { label: "Add SIMD vectorisation to the parse loop.", misconception: "Vectorisation helps compute-bound code (high IPC). At IPC 0.44 the CPU is idle waiting for memory; SIMD has nothing to accelerate until the cache-misses drop." },
    { label: "Reduce branch mispredictions with branchless code.", misconception: "The readout flags cache-misses, not branch-misses. Low IPC plus high cache-miss points squarely at memory stalls; check the branch-miss counter separately before assuming it." },
  ]}
/>

## Snippet 4 — the Amdahl decision

```
# Request total: 200 ms. Profile shows:
#   funcA  100 ms (50%)   -- option 1: rewrite for 2x  -> 50 ms saved
#   funcB   40 ms (20%)   -- option 2: rewrite B and C for 4x each
#   funcC   20 ms (10%)   --            -> 45 ms saved combined
```

<Quiz
  id="01-profile-first-code-4"
  lessonSlug="01-profile-first"
  lang="en"
  question="You can do option 1 OR option 2, not both. Which delivers more total speedup, and what is the general rule?"
  choices={[
    { label: "Option 1. A 2x on a 50% function saves 50 ms (200→150, 1.33x); a 4x on the combined 30% saves 45 ms (200→155, 1.29x). Rule: weigh fraction-of-total before magnitude-of-local-speedup — a modest win on a large share beats a large win on a small share.", correct: true },
    { label: "Option 2 — a 4x speedup is bigger than a 2x, so it wins.", misconception: "Magnitude of local speedup is not the deciding factor; share is. The 4x lands on only 30% of time, saving 45 ms, while the 2x on 50% saves 50 ms. Amdahl, not the multiplier, decides." },
    { label: "They tie — both touch fast functions.", misconception: "They are close but not equal: 50 ms vs 45 ms saved, 1.33x vs 1.29x. The point is precisely that you must do the Amdahl arithmetic instead of eyeballing the multipliers." },
    { label: "Neither — you must microbenchmark each function first.", misconception: "The profile already gave you the shares (p) and the proposed local speedups (s); that is everything Amdahl needs. A microbenchmark would only re-measure s in isolation and could not improve this decision." },
  ]}
/>

<Recap lang="en">
Every artefact here hides a trap or a signal. A benchmark whose result is discarded measures eliminated code, not work. A 29% p99 win with a flat CPU diff means the saving was off-CPU — capture a wait profile to see it. IPC under 1.0 with heavy cache-misses means memory-stalled: fix the data layout, not the algorithm. And when two optimisations compete, Amdahl's arithmetic on shares — not the size of the local multiplier — picks the winner. Read the number, find the lie, then act on the share.
</Recap>
