infographic sequence-ascending-steps
data
  title JIT warmup gap: a 1k-call bench never reaches production code
  desc HotSpot compiles in tiers — C1 near ~10k calls, C2 near ~100k; a naive 1k-iteration bench measures the interpreter
  order asc
  sequences
    - label Naive bench ~1k calls - still the interpreter
    - label C1 baseline JIT ~10k calls - 10x more invocations
    - label C2 optimised JIT ~100k calls - what production runs
theme
  palette #f43f5e #f59e0b #14b8a6
