---
concepts:
- continuous-profiling
- pyroscope
- profile-trace-correlation
- CI-profile-gate
- eBPF
- OTel-profiles
- anti-patterns
deepensInto: []
estMin: 20
lang: en
lessonType: topic
level: senior
order: 8
prereqs:
- performance/01-profile-first/07-hardware-counters-and-cold-start
slug: 08-continuous-profiling-at-scale
sources:
- https://github.com/grafana/pyroscope
- https://research.google/pubs/google-wide-profiling-a-continuous-profiling-infrastructure-for-data-centers/
- https://www.brendangregg.com/systems-performance-2nd-edition-book.html
spiral:
- latency
status: ready
summary: Always-on profiling at 2-5% overhead is standard, but the storage compounds
  across a fleet. Profile-trace correlation turns incident triage from guessing into
  a 30-second drill. Profile-diff CI gates catch regressions before merge.
title: 'Continuous profiling at scale: costs, CI gates, trace correlation, and anti-patterns'
track: performance
unit: 01-profile-first
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DebugLog from "~/components/pedagogy/DebugLog.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import DesignPrompt from "~/components/pedagogy/DesignPrompt.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import profilingStorageFunnel from "./infographics/profiling-storage-funnel.svg?raw";

<Hook>
An SLO burn alert fires. In 2020, the on-call opens a terminal, runs a manual profile capture, waits 60 seconds, and stares at a flame graph that may or may not represent the traffic causing the burn. In 2026, they click the alert, click the slow trace, click the span — and the flame graph filtered to that exact trace-id is already there.
</Hook>

<Crux>Continuous profiling makes profile-first infrastructure. Trace correlation delivers the flame graph the moment the pager fires.</Crux>

<Explanation>

## Continuous profiling: costs and tradeoffs

Before you propose adding continuous profiling to your organisation, you need to know what it actually costs at scale — and when the cost is not worth it.

Always-on profiling at 2-5% overhead is the modern default. The cost is non-zero and compounds across a fleet.

**CPU overhead:** 200-service fleet at 100 Hz, 2% overhead = the equivalent of running ~4 extra services worth of CPU permanently.

**Storage overhead:** 30-second profiles at ~200 KB compressed × 240 per hour × 24 hours × 200 services ≈ 230 GB/day raw. Pyroscope 2.0's symbol deduplication cuts this 3x. Tiered retention (7 days fine, 30 days downsampled, 90 days summary) cuts another 5x in aggregate. Net cost on object storage: ~$25-100/month for 200 services.

<Infographic
  svg={profilingStorageFunnel}
  label="Funnel narrowing a 200-service fleet's daily profiling storage from 230 GB/day raw, to ~77 GB/day after Pyroscope symbol deduplication (3x), to ~15 GB/day after tiered retention (another 5x), ending at a $25-100/month object-storage bill."
  caption="A scary-looking 230 GB/day of raw fleet profiles collapses ~15x through symbol dedup and tiered retention — landing at $25-100/month, smaller than any bottleneck it finds."
  hue="var(--d-infra)"
/>

**The rollout pattern:** pay the cost only when the team has the discipline to use the data. A small team starting out may get more value from on-demand profiling (`perf record`, pprof endpoint) and adopt continuous profiling later. The graduation criterion: reaching-for-the-profile is already a reflex. If the team has to remember to look, continuous profiling becomes decoration.

## Profile-trace correlation

The integrated triage workflow:

SLO burn alert → click → traces filtered to the burn window → pick a slow trace → click span → **profile filtered to that span's trace-id** → flame graph for the specific request.

Sub-30-second drill from page-out to git blame.

**The bridge:** trace-id stamping inside profile samples. Each stack sample carries the active trace-id at the moment of sampling. At query time the backend joins profile samples to the trace. OpenTelemetry's profile signal (beta as of 2026) standardises this stamping.

Production implementations:
- Grafana Tempo + Pyroscope
- Datadog APM + Continuous Profiler
- Honeycomb + OTel profile signal

Without trace-id stamping, the on-call must guess which profile sample corresponds to the slow request. With it, the answer is one filter away.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Triage step</th>
        <th class="border border-border px-3 py-2 text-left">Without trace-profile join</th>
        <th class="border border-border px-3 py-2 text-left">With trace-profile join</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Find the slow span</td>
        <td class="border border-border px-3 py-2">Trace view, ~30s</td>
        <td class="border border-border px-3 py-2">Trace view, ~30s</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Get the flame graph for that request</td>
        <td class="border border-border px-3 py-2">Manual capture, 2-5 minutes</td>
        <td class="border border-border px-3 py-2">Click the span, &lt;5 seconds</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Total drill to function</td>
        <td class="border border-border px-3 py-2">3-10 minutes</td>
        <td class="border border-border px-3 py-2">Under 30 seconds</td>
      </tr>
    </tbody>
  </table>
</div>

## Profile-driven CI gates

Continuous profiling enables pre-merge regression detection.

**Pattern:** every PR triggers a canary deploy. A 5-minute load test under representative traffic captures a CPU profile and an allocation profile. The CI job diffs against main's baseline. If any function's CPU share grows by more than a configured threshold (typically 10% absolute or 30% relative), the PR is flagged for review.

**Implementations:** Pyroscope's compare API, Datadog's deploy comparison, custom pipelines using Go's `pprof` diff or async-profiler's JFR diff.

**Payoff:** regressions that would have hit production at p99 are caught in CI. Mean time to detect on perf regressions drops from days (production monitoring eventually flags) to minutes (CI fails the PR).

**Operational challenges:** (1) Noisy baselines — main itself is changing; refresh the baseline weekly, not nightly. (2) Variance thresholds — start loose (≥30% absolute) and tighten over months. (3) Synthetic load mismatch — extend the load profile based on missed regressions caught in production. (4) Engineer pushback — provide a `/perf-override` escape hatch requiring manager sign-off and logged for audit.

## Production failures: profile-first paid off

**Stripe (2021):** service at 80% CPU. Team guessed retries. Profile showed 60% of CPU in a JSON parser called on every health-check 100 times/second. Fix: cache the parsed config. CPU dropped to 30% in 8 minutes.

**GitHub (2020):** Ruby workers OOMing. Allocation profile pointed at template-rendering allocating 200 MB per request.

**Discord (2020):** chat tail latency. JSON serialisation switch dropped p99 tail.

**Counter-example (2019 startup):** two months rewriting a Postgres query for a "slow" admin page. Profile finally showed 90% in a third-party widget. SQL change moved nothing.

Pattern: with the profile, the bottleneck is named in minutes. Without it, teams burn weeks on the wrong code.

## Anti-patterns

Five common anti-patterns in performance work:

1. **Optimising the cold path** — a function called once at startup gets a 100x rewrite while the per-request hot path is ignored. Catch: "calls per second × per-call cost", not just per-call cost.
2. **Microbench-driven optimisation** — function X is 10x faster in isolation; production is 1.03x. Catch: production profile share before any microbench rewrite.
3. **"We cannot profile production, it is too expensive"** — an excuse to skip the only honest measurement. Catch: continuous profiling overhead is 2-5%, well below the gain from finding any real bottleneck.
4. **Single-run reporting** — "this PR is 12% faster" with one measurement. Catch: insist on distributions.
5. **Regression-by-feature-flag** — a perf improvement is gated behind a flag and never enabled. Catch: the CI profile gate must test flag-enabled runs; otherwise the improvement is permanent dead code.

<DebugLog
  id="sr-debug-1"
  pieceSlug="08-continuous-profiling-at-scale"
  lang="en"
  title="Diagnose a profile-vs-production-metric disagreement"
  output={`# Production metrics (5-minute window)
checkout_p99_ms          580
checkout_p99_ms_prev     820  # before deploy
cpu_pct                  62
cpu_pct_prev             58

# go tool pprof -diff_base baseline.cpu prod.cpu
File: checkout
Type: cpu
Showing nodes accounting for -3.20s, 1.15% of -278.5s total

      flat  flat%   sum%        cum   cum%
    -1.80s  0.64%  0.64%    -1.80s  0.64%  net/http.(*conn).serve
    -1.40s  0.50%  1.15%    -1.40s  0.50%  encoding/json.Marshal
    +0.05s 0.018%  1.13%    +0.05s 0.018%  myapp/handlers.Checkout
    ... (other shifts < 0.5s)`}
  outputLang="log"
  question="Production p99 dropped 29% (820→580ms) after a deploy. CPU went UP slightly (58→62%). The CPU profile diff shows only ~1% net CPU reduction. How do you reconcile the headline win with the small CPU shift?"
  hint="Wall-clock improvement does not have to come from CPU."
  answer="Diagnosis: the p99 improvement is real (production metrics confirm), but the CPU profile alone cannot explain it because the wall-clock saving came from off-CPU time, not CPU time. Likely cause: the deploy reduced waiting time (faster DB query, lower lock contention, less network round-trip). CPU usage went UP because the service now spends more of its wall-clock executing real work and less waiting — same wall-clock budget, more compute throughput, slightly higher CPU%. To fully diagnose: capture an off-CPU profile (eBPF off-CPU or block profile in Go) and diff against baseline — the win should show up as a shrinking off-CPU frame on DB-wait or mutex-wait paths. Lessons: (1) CPU profiles only show running time; for I/O-bound or lock-bound services, off-CPU is mandatory. (2) Higher CPU after a fix is often a good signal — the service is doing work instead of waiting. (3) Always pair CPU profile with off-CPU + a representative trace span. (4) The CI profile gate should accept both CPU and off-CPU profiles into the regression check."
/>

<TradeoffMatrix
  id="sr-tradeoff-1"
  pieceSlug="08-continuous-profiling-at-scale"
  lang="en"
  prompt="Pick the right measurement scope for the question 'is this 10x microbench speedup of HashMap going to make my API faster?'"
  options={[
    { name: "Production CPU profile of the API to measure HashMap's share of total time", summary: "Honest, decisive answer.", correct: true, justification: "Amdahl says total speedup = 1 / ((1 - p) * (1 - 1/s)). The microbench gives s (local speedup); only the production profile gives p (the share). With both, you predict the actual API improvement before changing code." },
    { name: "Trust the microbench and ship", summary: "Fastest, often wrong.", justification: "A 10x microbench tells you nothing about the API's wall-clock unless HashMap dominates. If HashMap is 3% of API time, total speedup ≈ 1.03x — invisible to users." },
    { name: "Run a macrobench under synthetic load and compare end-to-end", summary: "Slower; better than microbench but less honest than prod profile.", justification: "Useful as a final check, but synthetic load may not match production traffic shape." },
    { name: "Skip measurement; HashMap is a common bottleneck and 10x is huge", summary: "Pure guesswork.", justification: "Reasoning by category instead of measurement is the original sin profile-first prevents." },
  ]}
/>

<DesignPrompt
  id="sr-design-1"
  pieceSlug="08-continuous-profiling-at-scale"
  lang="en"
  prompt="Design the 'profile-first' programme for a 50-engineer platform team running 30 services in production. Within six months: every perf PR must cite a profile; every production incident runbook must produce a profile within 5 minutes; profile-diff CI gates must run on critical-path services."
  constraints={[
    "Polyglot mix: Go, Node, Python, JVM.",
    "Existing stack: Prometheus + Grafana + Tempo + Loki; no continuous-profiling backend yet.",
    "Budget ceiling: $3k/month for new tooling.",
    "No formal performance engineer; the practice must be self-sustaining.",
  ]}
  canonical="Phase 1 (weeks 1-4): Stand up Pyroscope 2.0 self-hosted (3 replicas + S3, ~$300/month) or Grafana Cloud Profiles (≤$1500/month). Deploy language-appropriate agents: pprof scrape for Go and Node, async-profiler javaagent for JVM, py-spy sidecar for Python. eBPF DaemonSet on every K8s node as universal baseline. Target ≤3% CPU overhead per service. Phase 2 (weeks 5-8): Integrate profiles into every incident runbook with a 'profile' step linking to Pyroscope filtered by service + 5-minute incident window. Add a Slack /profile command. Monthly tabletop drills: on-call must open a profile within 5 minutes. Phase 3 (weeks 9-16): Update perf PR template to require: (a) baseline profile link with measured share of target function, (b) Amdahl-predicted total speedup, (c) post-fix profile diff. Reviewers reject PRs missing these fields. Phase 4 (weeks 17-26): Pick three critical-path services. CI job: deploy PR branch to canary, run 5-minute k6 load test, capture CPU + allocation profiles, diff against main baseline. Thresholds: function CPU share growing ≥10% absolute or ≥30% relative flags the PR. Tune for 4-6 weeks to manage false-positive rate. Quarterly: top-10 services by CPU consumption ranked; owning teams present hotspot explanations. Retention: 7 days fine-grained + 30 days downsampled, ≤$500/month storage. Outcome targets: (1) mean time to root cause ≤5 minutes for perf incidents; (2) ≥80% of perf PRs cite a profile; (3) ≥1 regression per quarter caught by CI gate; (4) total profiling cost ≤$2.5k/month."
  keyPoints={[
    "Stand up continuous profiling backend within 4 weeks.",
    "Polyglot collection: native agents + eBPF DaemonSet baseline.",
    "Runbook integration: every incident page links to a profile within 5 minutes.",
    "PR template: baseline profile + Amdahl prediction + post-fix diff mandatory.",
    "CI profile-diff gates on critical-path services; tune thresholds.",
    "Quarterly profile reviews on top-10 services by CPU.",
    "Cost ≤$3k/month; RBAC + CAP_PERFMON for security.",
  ]}
/>

</Explanation>

<FlowDiagram
  label="Continuous profiling pipeline: an always-on low-overhead agent on every service emits trace-id-stamped samples, a backend aggregates them across the fleet, and an engineer queries a flame graph filtered to one trace-id"
  hue="var(--d-hardware)"
  perRow={4}
  nodes={[
    { id: "agent", label: "Always-on agent", sub: "2-5% overhead" },
    { id: "samples", label: "Stamped samples", sub: "+ trace-id" },
    { id: "aggregate", label: "Fleet backend", sub: "store + dedupe" },
    { id: "query", label: "Query flame graph", sub: "filter by trace-id" },
  ]}
  edges={[
    { from: "agent", to: "samples" },
    { from: "samples", to: "aggregate" },
    { from: "aggregate", to: "query" },
  ]}
  caption="Trace-id stamping in each sample lets the on-call jump from an SLO burn alert to the request's flame graph in one click."
/>

<KeyTakeaway>Continuous profiling at 2-5% overhead plus trace-id stamping turns incident triage from a manual 5-minute capture into a one-click 30-second drill. Profile-diff CI gates catch regressions before merge. Anti-pattern: "cannot profile production" — continuous profiling overhead is smaller than any real bottleneck's cost.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="08-continuous-profiling-at-scale-retrieval"
  lang="en"
  questions={[
    { q: "How does profile-trace correlation work technically, and what does the triage workflow look like with it in place?", a: "Each stack sample in a continuous profiler carries the active trace-id at the moment of sampling. The continuous-profile backend stores these stamped samples and allows filtering by trace-id. At triage time: SLO burn alert fires → filter traces to the burn window → pick a slow trace → click the span → the profile backend returns flame graph samples filtered to that trace-id. The flame graph shows exactly what was on CPU during the slow request. Without trace-id stamping, the on-call must manually trigger a profile capture during the incident and hope the slow requests are still occurring. With stamping, the profile for any sampled request is already saved the moment the alert fires. OpenTelemetry's profile signal (beta 2026) standardises the trace-id-in-sample format." },
    { q: "Walk through how you would set up a profile-diff CI gate for a critical-path Go service, including thresholds and the failure modes you expect in the first month.", a: "Architecture: every PR triggers a canary deploy. k6 or vegeta runs a 5-minute load test mirroring production traffic. /debug/pprof/profile?seconds=300 is captured; heap profile captured in parallel. Both uploaded to S3 and posted to the PR as Pyroscope-compatible links. A diff job runs: go tool pprof -diff_base main-baseline.cpu pr.cpu, parsed for functions whose self-time share changed beyond threshold. Thresholds: ≥10% absolute or ≥30% relative flags the PR. First-month failure modes: (1) Flaky baseline — main changes daily; fix by refreshing baseline weekly. (2) Noisy variance — even identical code differs 5-10%; start with loose thresholds and tighten. (3) Synthetic load mismatch — load test does not capture all patterns; extend incrementally. (4) Engineer pushback — add a /perf-override escape hatch requiring manager sign-off. (5) Cost creep — gate canary deploys by PR label (only critical-path code paths). Expect 1-3 caught regressions and 5-10 false positives per week in month one; by month three, ≤2 false positives per week." },
  ]}
/>

<Recap lang="en">
Continuous profiling at 2-5% overhead is the 2026 standard, but the storage and CPU cost compounds across a fleet — 200 services at 2% overhead equals four permanent extra services. Tiered retention and symbol deduplication bring net storage cost to $25-100/month. Profile-trace correlation via trace-id stamping in each stack sample reduces incident triage from minutes of manual capture to a 30-second click-through. Profile-diff CI gates catch regressions before merge, cutting mean-time-to-detect from days to minutes. Five common anti-patterns: optimising cold paths, microbench-driven rewrites without production share, refusing to profile production, single-run reporting, and feature-flagged improvements that never get enabled. The chapter's foundation: every subsequent optimisation technique — hot paths, GC, N+1, batching, bundle budgets — is an answer to a question profiling raised. Now when an SLO alert fires, your first move is not a terminal command — it is clicking the trace, then the span, and reading the flame graph that is already there.
</Recap>
