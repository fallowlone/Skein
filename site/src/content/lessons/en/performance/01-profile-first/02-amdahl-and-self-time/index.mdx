---
concepts:
- amdahl-law
- self-time
- cumulative-time
- wall-clock-time
- cpu-time
- bottleneck
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: middle
order: 2
prereqs:
- performance/01-profile-first/01-why-profile-first
slug: 02-amdahl-and-self-time
sources:
- https://dl.acm.org/doi/10.1145/356635.356640
- https://en.wikipedia.org/wiki/Amdahl%27s_law
- https://www.brendangregg.com/systems-performance-2nd-edition-book.html
spiral:
- latency
status: ready
summary: Amdahl''''s law converts ''''should I optimise this?'''' into a number. Self-time
  vs cum-time tells you whether to fix a function or walk down to what it calls.
title: 'Amdahl''''''''s law and self-time: the ceiling on every speedup you can ship'
track: performance
unit: 01-profile-first
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
A team spends three weeks rewriting a hash function for a 10x local speedup. The deployed service is 1.05x faster. Amdahl's law would have told them the answer before they started — if they had checked the profile first.
</Hook>

<Crux>Amdahl caps every speedup. Profile gives p — the hot fraction. Without p the math is unsolvable.</Crux>

<Explanation>

## Amdahl's law: the ceiling on every speedup

If a section of code takes fraction **p** of total execution time, and you make that section **s** times faster, the total application speedup is:

```
total_speedup = 1 / ((1 − p) + p/s)
```

The ceiling case — making the section infinitely fast — gives:

```
max_speedup = 1 / (1 − p)
```

A function that is 10% of total time can never give more than **1.11x** total speedup, even if you replace it with a no-op. A function that is 80% of total time gives **5x** if you halve it.

This law converts "should I optimise this?" into a quantitative question. Profile, see that function X is 12% of execution, and accept up-front that even a perfect rewrite caps your win at 13.6%. If the ceiling is below your bar, do not start. If function Y is 70% of execution, a 2x speedup of Y gives 1.54x total — worth the engineering time.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Fraction of total time (p)</th>
        <th class="border border-border px-3 py-2 text-left">Max total speedup (p → 0)</th>
        <th class="border border-border px-3 py-2 text-left">Speedup if made 2x faster</th>
      </tr>
    </thead>
    <tbody>
      <tr><td class="border border-border px-3 py-2">10%</td><td class="border border-border px-3 py-2">1.11x</td><td class="border border-border px-3 py-2">1.05x</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">50%</td><td class="border border-border px-3 py-2">2.00x</td><td class="border border-border px-3 py-2">1.33x</td></tr>
      <tr><td class="border border-border px-3 py-2">80%</td><td class="border border-border px-3 py-2">5.00x</td><td class="border border-border px-3 py-2">1.67x</td></tr>
      <tr class="bg-surface-2"><td class="border border-border px-3 py-2">90%</td><td class="border border-border px-3 py-2">10.0x</td><td class="border border-border px-3 py-2">1.82x</td></tr>
    </tbody>
  </table>
</div>

## Wall-clock vs CPU time vs self-time

A profile reports several different time measurements and confusing them is the most common reading error.

- **Wall-clock time** — what the user feels: how long the request took from start to end. A request at 500 ms wall-clock that only used 50 ms of CPU is 90% waiting (on disk, network, locks, GC).
- **CPU time** — how much of wall-clock was spent actually computing.
- **Self-time** — time spent in a function's own code, not including time spent in functions it called.
- **Cumulative time (cum-time)** — includes time in callees.

Reading a profile: if **cum-time is large but self-time is small**, the function is a routing/dispatch layer — the slow work is in something it calls; walk down. If **self-time is large**, the function is itself doing the work; that is where the fix lives.

Confusing the two leads to optimising a wrapper while the real cost sits in a callee — or vice versa.

<Inset kind="why" lang="en">
"Optimising 10% of code to zero" is the classic example of an Amdahl trap. Even eliminating that code entirely gives only 1.11x — less than most teams' minimum threshold for a breaking change. The profile identifies which fractions are large enough to be worth attacking. Without it, you risk spending a week on 1.05x wins.
</Inset>

<NumbersCard
  id="card-amdahl-numbers"
  title="Amdahl's law and profile-first economics"
  stats={[
    { label: "Amdahl's law max speedup", value: "1 / (1 − p) where p = fraction sped up" },
    { label: "Optimising 10% of code to free", value: "1.11x total max" },
    { label: "Optimising 50% of code to free", value: "2.0x total max" },
    { label: "Optimising 90% of code to free", value: "10.0x total max" },
    { label: "Typical sampling profiler overhead", value: "0.5-5% CPU" },
  ]}
/>

<FadedExample
  client:load
  id="fe-amdahl-1"
  pieceSlug="02-amdahl-and-self-time"
  lang="en"
  title="Apply Amdahl's law to decide which optimisation is worth doing"
  steps={{
    solved: (
      <div class="space-y-3 text-sm">
        <p><strong>Setup.</strong> A request takes 200 ms total. A profile shows three hot regions: function A consumes 100 ms (50%), function B 40 ms (20%), function C 20 ms (10%). The team can either rewrite A for a 2x speedup (50 ms saved) or rewrite B and C together for 4x speedup on both (45 ms saved). Which gives more total improvement?</p>
        <pre class="text-xs bg-zinc-100 p-2 rounded overflow-x-auto"><code>{`# Option 1: Rewrite A (50% of time) for 2x speedup
#   New A time = 100 ms / 2 = 50 ms
#   New total  = 50 + 40 + 20 + 40 (rest) = 150 ms
#   Speedup    = 200 / 150 = 1.33x
#   Saved      = 50 ms (25%)

# Option 2: Rewrite B and C (30% of time) for 4x speedup
#   New B time = 40 ms / 4 = 10 ms
#   New C time = 20 ms / 4 = 5 ms
#   New total  = 100 + 10 + 5 + 40 = 155 ms
#   Speedup    = 200 / 155 = 1.29x
#   Saved      = 45 ms (22.5%)

# Amdahl's law winner: Option 1 — modest speedup on the larger fraction
# beats large speedup on the smaller fractions.`}</code></pre>
        <p><strong>The pattern.</strong> Always weigh "fraction of total time" before "magnitude of local speedup". A 2x on 50% beats a 4x on 30%. The profile gives you the fractions; without them you cannot do the math.</p>
      </div>
    ),
    semi: {
      prompt: "Function X is 40% of execution. You can make it 10x faster. What is the maximum total speedup according to Amdahl?",
      blanks: [
        { id: "speedup", expected: "1.61", placeholder: "speedup factor" },
      ],
    },
    blank: {
      prompt: "Why is Amdahl's law called a 'ceiling' rather than a prediction?",
      reveal: (
        <div class="space-y-2 text-sm">
          <p>The law gives the maximum possible speedup if the optimised section becomes infinitely fast — which is unachievable. The real speedup is always lower, because any rewrite still costs some time. The law is a ceiling: if the ceiling is too low to justify the work, you should not start.</p>
        </div>
      ),
    },
  }}
  misconceptions={{
    "speedup:1.5": "Close — but the exact Amdahl formula gives 1 / (0.6 + 0.04) ≈ 1.56. Round to 1.6 or 1.61.",
    "speedup:10": "That's the local speedup on the section, not the total. Total is bounded by Amdahl at 1 / (1 - 0.4) = 1.67 even if the section becomes infinitely fast.",
  }}
/>

<Misconception id="mc-guess-default">
  <p><strong>"My intuition for where it's slow is good enough."</strong> Wrong more often than right — too many layers (framework, runtime, OS, caches, libraries) to model in your head. Treat intuition as a hypothesis; profile to verify. It rarely survives contact with measurement.</p>
</Misconception>

<Quiz
  id="02-amdahl-and-self-time-quiz-1"
  lessonSlug="02-amdahl-and-self-time"
  lang="en"
  question="A profile shows function X has CUM-time 80% but SELF-time 2%. What does this mean and where do you look for the fix?"
  choices={[
    { label: "X is a routing/dispatch layer that calls slow children. The fix is not in X itself but in whatever it calls — walk down the flame graph to find the wide leaf frame.", correct: true },
    { label: "X is the bottleneck; rewrite X.", misconception: "Self-time of 2% means X's own code barely costs anything. The 80% cum-time belongs to its callees." },
    { label: "The profiler is wrong.", misconception: "This pattern is normal: HTTP handlers, middleware, dispatch functions all have huge cum-time and tiny self-time." },
    { label: "The profile is over-sampled.", misconception: "Sampling rate does not change the self-vs-cum distinction; it is about where in the call tree the work happens." },
  ]}
/>

<Quiz
  id="02-amdahl-and-self-time-quiz-2"
  lessonSlug="02-amdahl-and-self-time"
  lang="en"
  question="A microbenchmark says a new hash function is 10x faster than the old one. When integrated, the application is only 1.05x faster. Most likely explanation?"
  choices={[
    { label: "Hashing was a small fraction of total time — Amdahl's law caps the application speedup at the hash function's share, no matter how fast hashing becomes.", correct: true },
    { label: "The microbenchmark is fundamentally broken", misconception: "It may still be correct in isolation; the issue is the local win does not dominate total time." },
    { label: "The new hash is slower in real workloads", misconception: "Possible but rarer than the Amdahl explanation; profile to verify before assuming." },
    { label: "The hardware does not support it", misconception: "Hardware support is binary — works or does not. It would not yield a 1.05x partial improvement." },
  ]}
/>

</Explanation>

<StackDiagram
  label="A handler's cumulative time decomposes into its small self-time plus the time spent in the functions it calls"
  hue="var(--d-hardware)"
  layers={[
    { label: "handler() cum-time", note: "100% (all attributed)" },
    { label: "handler() self-time", note: "2% — fix NOT here" },
    { label: "└ parseBody() (callee)", note: "18%" },
    { label: "└ query() (callee)", note: "80% — walk down to here" },
  ]}
  caption="cum-time = self-time + Σ callees. Tiny self-time but huge cum-time means the fix lives in a callee, not the handler."
/>

<KeyTakeaway>Amdahl's law: total_speedup = 1 / ((1 − p) + p/s). Profile gives p; without p the math is impossible. Self-time large means fix the function; cum-time large but self-time small means walk down to what it calls.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-amdahl-and-self-time-retrieval"
  lang="en"
  questions={[
    { q: "Walk through Amdahl's law with an example: function X takes 30% of execution time. You make X 5x faster. What is the total application speedup and what fraction of execution does X now consume?", a: "total_speedup = 1 / ((1 − p) + p/s) = 1 / (0.7 + 0.3/5) = 1 / (0.7 + 0.06) = 1 / 0.76 ≈ 1.32x. After the rewrite, X consumes (0.3/5) / (1 − 0.3 + 0.3/5) = 0.06 / 0.76 ≈ 7.9% of the new execution time. X has gone from 30% to ~8%. The next-largest function is now the dominant share and the next profiling target. This iterative pattern repeats: every successful optimisation shifts the hotspot and you re-profile to find it." },
    { q: "What is the difference between self-time and cum-time, and what action does each reading suggest?", a: "Self-time is time spent in a function's own instructions, not counting time in functions it called. Cum-time (cumulative) includes all time attributed to the function and its entire call subtree. If self-time is large, the function itself is doing costly work — fix it there. If cum-time is large but self-time is small, the function is a thin routing or dispatch layer; the real work is in one of its callees. Walk down the call tree to find the callee with large self-time." },
  ]}
/>

<Recap lang="en">
Amdahl's law gives a ceiling: total_speedup = 1 / ((1 − p) + p/s). The profile provides p — the fraction of total time the target function occupies. Without that number, you cannot decide whether an optimisation is worth engineering time. A function at 10% of total time can never deliver more than 1.11x speedup even if made free; one at 80% can deliver up to 5x. Self-time vs cum-time resolves where to look: large self-time means fix the function directly; large cum-time with tiny self-time means walk down to the callee where the real cost sits.
</Recap>
