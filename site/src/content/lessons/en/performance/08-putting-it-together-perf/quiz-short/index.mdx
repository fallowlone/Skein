---
concepts:
- profile-first
- amdahl-cap
- cost-shapes
- right-layer-fix
- verify-and-enforce
- batching-window
estMin: 14
lang: en
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://web.dev/articles/vitals
- https://go.dev/doc/gc-guide
status: ready
summary: Free-recall prompts spanning the whole track — the measurement loop, Amdahl's cap, the five cost shapes, fixing at the right layer, and verify-then-enforce.
title: 'Performance capstone: free-recall synthesis'
track: performance
unit: 08-putting-it-together-perf
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, reconstruct a full answer from memory across the whole track before you open the model answer — the effort of pulling the connections together is what turns seven units into one discipline.
</Hook>

<Goal lang="en">
Reconstruct the track's spine without looking back: the measurement loop, why Amdahl caps every speedup, the cost shapes you classify into, how to fix at the right layer, and why verify-then-enforce is what makes performance durable.
</Goal>

<RetrievalDrawer
  client:load
  id="08-putting-it-together-perf-short-retrieval"
  lang="en"
  questions={[
    { q: "State the five-step loop the whole track reduces to, and what each step prevents you from doing wrong.", a: "Profile → classify → fix → verify → enforce. Profile first stops you acting on intuition, which is wrong more than half the time. Classify forces you to name the dominant cost's shape (CPU, allocation, cache, lock/round-trip, bytes) so you reach for the matching fix instead of a familiar one. Fix at the right layer stops you patching a symptom one level removed from the cause. Verify under representative load stops you trusting a laptop number that omits production concurrency and data volume. Enforce — a CI budget, benchmark, or alert — stops the gain from silently decaying as the next feature lands. The loop, not any single fix, is what makes performance a durable team property." },
    { q: "Why does Amdahl's law make 'profile first' non-negotiable, with a concrete number?", a: "Amdahl's law caps the whole-system speedup at 1 / ((1 − p) + p/s), where p is the fraction of time spent in the part you speed up by factor s. If a component is 5% of wall time, even making it infinitely fast (s → ∞) yields at most a 1/0.95 ≈ 5.3% improvement. So optimising anything that is not the dominant cost has a hard ceiling set by how much time it actually consumes — and you cannot know that fraction without profiling. This is exactly why the named-suspect rewrite is a trap: a 3% serializer caps the page at a 3% win no matter how clever the rewrite." },
    { q: "Name the cost 'shapes' a hot path can take and the matching first move for each.", a: "A hot path the profiler keeps surfacing usually falls into one of: CPU-bound (the function does heavy math/work itself — reduce work, better algorithm, SIMD); allocation-bound (time in alloc + GC — eliminate or pre-size allocations, pool only what the profile flags); cache-bound (stalls on memory access despite low instruction count — improve locality, contiguous layout, smaller working set); lock/round-trip-bound (waiting on contention or sequential I/O — reduce critical-section size, batch, or remove N+1 round-trips); and syscall/IO-bound (kernel transitions or network — batch, buffer, amortize fixed cost). Hardware counters (TMA) resolve the ambiguous cases. The shape, read from the profile, dictates the layer you fix at." },
    { q: "A profile shows time in GC. Why is the fix usually not in the collector, and how does this generalise across the track?", a: "GC pause is the symptom; allocation rate is the cause. Tuning the collector (GOGC, pause target) or switching it (G1 → ZGC) only optimises the cost of allocation work that still happens — the bottom of the fix ladder. The durable fix is at the allocation: eliminate it, pre-size it, or pool it. This generalises to the whole track: always fix at the layer where the cost is created, not one level downstream. An N+1's fix is in the access pattern, not a bigger DB; a bundle's fix is fewer bytes, not a faster CDN; a per-op-overhead problem's fix is batching, not more workers. Treat the symptom layer last." },
    { q: "Explain the core tradeoff of batching and how the same per-op-cost reasoning shows up elsewhere in the track.", a: "Batching amortizes a fixed per-operation cost (connection setup, auth, syscall, round-trip) across many items, so throughput rises as the fixed cost is divided by batch size. The price is tail latency: an item waits for the batch window to fill or a max-wait timer to fire, so you tune batch size and max-wait against the latency SLO. The same 'fixed cost per operation' reasoning drives N+1 fixes (one batched query replaces N round-trips, each carrying fixed round-trip cost) and is the inverse of the bundle problem (there the per-load fixed cost is bytes shipped to every user, amortized by shipping fewer). Recognising fixed-per-op cost is the unifying lens." },
    { q: "What does it mean to verify and enforce a performance fix, and why is shipping a green local benchmark not enough?", a: "Verify means re-measuring under the same representative load you used for the baseline — identical dataset size, concurrency, cache state, and environment — and comparing before/after, not estimating. A laptop benchmark omits production concurrency, contention, data volume, and cold caches, so a local win can vanish or even reverse in production. Enforce means making the gain stick: a per-route bundle budget that fails CI, a benchmark in the pipeline that flags regressions, or an alert on the SLI. Without enforcement the gain decays the moment a new dependency, N+1, or allocation creeps in — invisible until users feel it. Verify proves the fix; enforce keeps it." },
  ]}
/>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the track's spine: one loop — profile, classify, fix, verify, enforce — backed by Amdahl's cap on optimising the wrong thing, a small vocabulary of cost shapes that map symptom to fix, the discipline of fixing at the cost's own layer rather than a downstream knob, and the habit of proving the gain under real load and locking it in CI. Performance is the loop, not any one fix. Now when you face a slow system, you will catch yourself reaching for the profiler before the nearest fix — that reflex is the whole track in one move.
</Recap>
