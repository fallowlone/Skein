---
concepts:
- parent-chain
- fan-in
- fan-out
- self-time
- cum-time
- fix-layer
- bottom-up
- top-down
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: middle
order: 3
prereqs:
- performance/02-hot-paths/02-five-shapes-of-hotspot
slug: 03-reading-parent-and-child-chains
sources:
- https://github.com/google/pprof/blob/main/doc/README.md
- https://www.brendangregg.com/Articles/Netflix_Tech_Blog_Performance.pdf
- https://github.com/grafana/pyroscope
spiral:
- latency
status: ready
summary: A wide leaf with one parent means fix the caller. A wide leaf with twenty
  parents means fix the leaf. Self-time vs cum-time boundary locates the work vs dispatch
  split.
title: 'Reading parent and child chains: where to apply the fix'
track: performance
unit: 02-hot-paths
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";

<Hook>
A senior engineer opens pprof and looks at json.Marshal at 28% CPU. They do not reach for a faster JSON library. They check the parent chain. One wide parent: a request logger. The fix is not the library — it is the logger calling Marshal on every request regardless of log level.
</Hook>

<Crux>Parent chain: fix the leaf or its callers? Child chain: fix the callee? Both reads are required before picking a fix location.</Crux>

<Explanation>

## Reading the parent chain

The parent chain answers: "where does this wide leaf get called from?"

**Concentrated fan-in (one wide parent path):** the leaf is called repeatedly from a single location. Fix the caller — call less often, cache results, batch, or eliminate the call.

**Distributed fan-in (many narrow parents):** the leaf is shared infrastructure used by many callers. Fix the leaf itself, because optimising any one caller leaves the other nineteen unchanged.

The classic example: `json.Marshal` wide with one wide path from a logger means the logger is the bug — don't switch JSON libraries, fix the logger. The same `json.Marshal` wide and called from twenty handlers means JSON is the bottleneck — switch libraries or pre-encode.

Production-grade pprof and Pyroscope let you switch between leaf-up and caller-down views with one click; using both is the senior reading habit.

## Reading the child chain (self-time vs cum-time)

The child chain answers: "does this function do the work, or does it delegate to a callee?"

**Self-time-heavy leaf with narrow children:** the function is doing the work itself. Fix is inside the function.

**Cum-time-heavy parent with thin self-time:** this function is a dispatcher. The cost sits one level down in a callee. Fix the callee, not this layer.

The reading rule: walk from the leaf with high self-time down the parent chain. Find the level where cum-time drops sharply as you move up. That level is the "boundary of meaningful work" — above it is dispatch, below it is execution. Apply the fix inside that boundary.

Misreading this boundary is how teams end up rewriting middleware that wraps a slow database call, or optimising HTTP routers when the cost is in the handler body.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Signal</th>
        <th class="border border-border px-3 py-2 text-left">What it means</th>
        <th class="border border-border px-3 py-2 text-left">Fix layer</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Wide leaf, one dominant parent</td>
        <td class="border border-border px-3 py-2">Concentrated call site</td>
        <td class="border border-border px-3 py-2">Caller (call less often / batch / cache)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Wide leaf, many thin parents</td>
        <td class="border border-border px-3 py-2">Shared infrastructure</td>
        <td class="border border-border px-3 py-2">Leaf itself</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">High self-time, narrow children</td>
        <td class="border border-border px-3 py-2">Function does the work</td>
        <td class="border border-border px-3 py-2">Inside the function</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Thin self-time, wide cum-time</td>
        <td class="border border-border px-3 py-2">Dispatcher</td>
        <td class="border border-border px-3 py-2">Inside the callee (one level down)</td>
      </tr>
    </tbody>
  </table>
</div>

## Fan-in amplification

When one function is called from many callers, optimising the leaf compounds across each caller. A 30% local improvement in a shared function used by twenty paths gives 30% on every one.

The inverse — fan-out: one caller hitting many callees — makes optimising each callee a single-path local saving.

Senior move: look at fan-in/fan-out shape before deciding leaf vs caller fix. High fan-in (shared infra) means leaf fix amplifies. High fan-out (one caller, many callees) means caller fix (batching, caching, eliminating the call) amplifies. This math often outweighs the fix-family choice: even a small fix on a high fan-in leaf can outscore a large fix on a single-caller path.

## Bottom-up vs top-down views

pprof, Pyroscope, and Intel VTune all support two complementary views.

**Bottom-up:** rank functions by self-time, leaf-first. Best for finding which single function eats the most CPU.

**Top-down:** rank functions by cum-time from root, parent-first. Best for finding which subsystem is responsible for the most time.

Senior reading habit: open bottom-up first to name the hot leaf, then switch to top-down to see which entry-point owns it. A leaf that is hot under one entry-point is a localised fix; a leaf that is hot under many is shared infrastructure.

<Inset kind="why" lang="en">
Intel's Top-Down Microarchitecture Analysis (TMA) extends this further, classifying each hot frame into front-end bound, back-end bound, retiring, or bad speculation. TMA is covered in the hardware counters lesson. The parent/child chain reading here is a prerequisite — TMA adds the CPU-level layer on top.
</Inset>

<TraceScenario
  id="mid-trace-1"
  pieceSlug="03-reading-parent-and-child-chains"
  lang="en"
  scenario="A Go API has p99 of 600 ms. Profile shows runtime.mallocgc at 18% and runtime.scanobject at 14%. Trace the diagnosis and fix."
  steps={[
    { prompt: "Step 1: runtime.mallocgc + runtime.scanobject = 32% of CPU in GC machinery. What is the category?", reveal: "Allocation-bound. The application code itself is not shown in those frames — the GC is paying for allocations the application is causing. Switch to the allocation profile to find which application functions are responsible." },
    { prompt: "Step 2: switch to /debug/pprof/allocs. Wide leaf: json.Encode called from middleware/RequestLog. What does this say?", reveal: "Every request is being JSON-encoded twice — once for the response, once by the request logger writing it to a log line. The logger is the unintended allocator. Same payload, allocated twice per request." },
    { prompt: "Step 3: fix candidates?", reveal: "(a) Drop the JSON-encoded request body from the log line entirely — log a hash or summary instead. (b) Reuse the response encoder's output (encode once, log the same buffer). (c) Switch to a logger that defers serialisation until level-filter passes (zerolog, zap with lazy fields). Pick (a) if the body in logs is not needed; (c) if it is needed but only for filtered levels." },
    { prompt: "Step 4: applied fix (a). Predicted impact?", reveal: "Allocation profile share of json.Encode should drop ~50% (one of two calls eliminated). GC frames (mallocgc + scanobject) should drop from 32% to ~16%. Service CPU improves by Amdahl ≈ 1 / (1 − 0.16) ≈ 1.19x throughput. p99 should drop because GC pauses are thinner." },
    { prompt: "Step 5: post-fix profile shows GC at 18%, not the predicted 16%. p99 dropped from 600 to 480 ms. Diagnosis?", reveal: "Fix landed (GC dropped from 32% to 18%, close to prediction). The remaining 18% is from other allocators — possibly the response encoder itself, or middleware buffers. The win is real; next iteration attacks the next-widest allocator. Loop closed: diagnose, fix, verify, find the next." },
  ]}
/>

<TraceScenario
  id="mid-trace-2"
  pieceSlug="03-reading-parent-and-child-chains"
  lang="en"
  scenario="A Node service has a wide leaf JSON.parse at 24% CPU. Trace why naively swapping parsers might not help."
  steps={[
    { prompt: "Step 1: JSON.parse is wide. First check?", reveal: "How big are the parsed payloads, and how often is parse called per request? A small parse called many times versus a giant parse called once need different fixes. Read the parent chain and count call sites." },
    { prompt: "Step 2: parent chain shows parse called from one middleware: requestValidator, once per request. Mean payload ~50 KB. What does this imply?", reveal: "Each request pays one large parse. The cost is dominated by walking the payload, not by parse call overhead. A faster parser might help, but the dominant cost is the work of parsing 50 KB itself — that floor is bounded by memory bandwidth." },
    { prompt: "Step 3: what other options exist before switching libraries?", reveal: "(a) Stop parsing fields the validator does not use (lazy or streaming parse that extracts only required fields). (b) Move validation to a layer that can use a schema and skip irrelevant data. (c) Push down to a binary format (Protobuf, MessagePack) so parse cost is 10x cheaper. (d) Pre-validate at the edge with a faster filter before reaching the handler. Switching JSON libraries is option (e) — worth measuring but lower upside than (a)-(d)." },
    { prompt: "Step 4: team picks (a) — switch to lazy field extraction. Predicted impact?", reveal: "Lazy extraction skips fields the validator does not use. Predicted CPU share: from 24% to 4–6%. Predicted p99 improvement: 1 / (1 − 0.20) ≈ 1.25x throughput. Predicted wall-clock per request: -18-20 ms." },
    { prompt: "Step 5: verify?", reveal: "Re-profile. The JSON.parse frame should be replaced by the new parser's frame at much narrower width. Headline p99 should drop by the predicted amount. If not, look for new hot frames — the previous hotspot may have masked one." },
  ]}
/>

<DragOrder
  id="03-reading-parent-and-child-chains-drag-1"
  lessonSlug="03-reading-parent-and-child-chains"
  lang="en"
  prompt="Order the steps of the hot-path attack loop, in the order a senior engineer runs them:"
  items={[
    { id: "a", label: "Open the profile and identify the widest leaf by self-time" },
    { id: "b", label: "Read the parent chain (one caller or many?) and the children (is real cost one level down?)" },
    { id: "c", label: "Classify the hotspot: CPU, allocation, cache, lock, syscall, or JIT deopt" },
    { id: "d", label: "Pick the categorical fix family that matches the classification" },
    { id: "e", label: "Write only the predicted change, no scope creep" },
    { id: "f", label: "Capture a fresh profile under the same load" },
    { id: "g", label: "Diff: local frame shrank AND headline metric improved? Both must hold to ship" },
    { id: "h", label: "If headline did not move, find the new top hotspot — it was masked by the first" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g", "h"]}
/>

<Quiz
  id="03-reading-parent-and-child-chains-quiz-1"
  lessonSlug="03-reading-parent-and-child-chains"
  lang="en"
  question="A wide leaf function is called from twenty different parent paths, each contributing a thin slice. Where do you fix?"
  choices={[
    { label: "Fix the leaf — it is shared infrastructure used by many callers. Even small improvements there compound across all twenty paths.", correct: true },
    { label: "Pick one caller and optimise its path only", misconception: "Optimising one caller leaves the other nineteen unchanged; the leaf's total cost dominates." },
    { label: "Inline the leaf into each caller", misconception: "Inlining at this scale duplicates code and rarely helps the original cost; the cost is in the function's work, not call overhead." },
    { label: "Add caching at one caller", misconception: "Same problem — partial fix across one of twenty paths." },
  ]}
/>

</Explanation>

<KeyTakeaway>Parent chain: one dominant parent means fix the caller; many thin parents means fix the leaf. Child chain: high self-time means work is here; thin self-time with wide cum-time means work is one level down. Both reads together locate the fix at the right layer.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-reading-parent-and-child-chains-retrieval"
  lang="en"
  questions={[
    { q: "Why is it important to read both the parent chain AND the children when diagnosing a wide leaf, and what does each direction tell you?", a: "The parent chain tells you about call-site concentration. A wide leaf reached through one dominant parent path means the leaf is called repeatedly from a single place — fix the caller (call less often, batch, cache, eliminate the call). The same wide leaf reached through twenty narrow parent paths means the leaf is shared infrastructure used by many — fix the leaf itself, because optimising any one caller leaves the other nineteen unchanged. The children chain tells you whether the function does its own work or delegates. If self-time is large but children's cum-time is small, the function is doing the work — fix is inside the function. If self-time is small but a callee's cum-time is large, the real cost is one level down — fix the callee, not the leaf. Together, parent and child reads form the diagnosis: where the calls come from (caller fix vs leaf fix) and where the cost actually accumulates (this function vs its callee)." },
    { q: "What does 'boundary of meaningful work' mean in pprof, and how do you find it?", a: "The boundary of meaningful work is the level in the call tree where cum-time drops sharply as you move upward from the hot leaf. Below that level is execution — functions doing real work (compute, I/O, memory access). Above it is dispatch — functions that route, select, or wrap without doing significant work themselves. To find it: start at the leaf with high self-time and walk up the parent chain. At each parent level, compare that parent's self-time to its cum-time. When self-time drops near zero and cum-time still equals the leaf's, you are in dispatcher territory. The last level with significant self-time is the top of the 'work zone'. The fix should target something inside that zone. Fixing above the boundary (rewriting dispatch code) wastes time; fixing below the widest leaf usually means the problem is in a callee already identified by the profile." },
  ]}
/>

<Recap lang="en">
The parent chain and child chain are two lenses on the same wide leaf. The parent chain reveals fan-in shape: one caller means fix the call site, twenty callers means fix the leaf. The child chain reveals whether the function itself carries the cost or is a pass-through to a callee. The boundary between dispatch and execution is where the effective fix location lives. Bottom-up and top-down profile views make both reads mechanical: bottom-up names the hot leaf, top-down shows which subsystem owns it. Together these two reads eliminate the most common fix-layer error in performance work.
</Recap>
