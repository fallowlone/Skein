---
concepts:
- hot-path
- flame-graph
- symptom-vs-cause
- self-time
- parent-chain
deepensInto: []
estMin: 12
lang: en
lessonType: topic
level: junior
order: 1
prereqs:
- performance/01-profile-first/04-reading-flame-graphs
slug: 01-what-makes-a-hot-path
sources:
- https://www.brendangregg.com/usemethod.html
- https://www.brendangregg.com/Articles/Netflix_Tech_Blog_Performance.pdf
- https://pkg.go.dev/runtime/pprof
spiral:
- latency
status: ready
summary: A wide flame-graph frame names where time accumulates, not why. The same
  leaf can hide five different bottlenecks — each demanding a different fix.
title: 'What makes a hot path: symptom vs cause'
track: performance
unit: 02-hot-paths
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import PersonaTag from "~/components/pedagogy/PersonaTag.astro";

<Hook>
The profile is done. One flame-graph frame is wide. Two engineers want to switch template engines. A third engineer asks: "Wide from what? CPU work, allocations, lock contention, or a syscall?" Only one of those four has "switch template engines" as the right fix.
</Hook>

<Crux>Wide frame = hot. But hot from what? CPU, alloc, cache, lock, syscall — different shapes, different fixes. Diagnose first.</Crux>

<Explanation>

## What a hot path is

A hot path is a sequence of calls the program spends most of its time in. The profile shows it as a stack of wide frames climbing from a leaf back to a top-level entry. The leaf names a function; the question is why that function is expensive.

Modern hardware turns the same "1 second of CPU" into very different problems depending on what the CPU was actually doing: executing instructions, waiting for memory, waiting for a lock, waiting for a syscall to return. The diagnosis decides which family of fix applies.

Applying the wrong fix to the right hotspot is the second most common waste in performance work — after optimising the wrong hotspot entirely (covered in the profile-first unit).

## The waiting room metaphor

A doctor's waiting room is full. That tells you the room is busy — not why. Are patients waiting for the doctor, the lab, paperwork, or parking? Each has a different fix: more doctors, faster lab, fewer forms, more parking.

A wide flame-graph frame is the same: the room is full; ask what people are waiting on inside.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Wide frame shows</th>
        <th class="border border-border px-3 py-2 text-left">What it actually means</th>
        <th class="border border-border px-3 py-2 text-left">Where to look next</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">High self-time in user function</td>
        <td class="border border-border px-3 py-2">Function does real CPU work</td>
        <td class="border border-border px-3 py-2">Inspect the algorithm or data layout</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Wide GC frames near leaf</td>
        <td class="border border-border px-3 py-2">Caller allocates a lot</td>
        <td class="border border-border px-3 py-2">Switch to allocation profile</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Wide in wall-clock, narrow in CPU</td>
        <td class="border border-border px-3 py-2">Function waits — lock or syscall</td>
        <td class="border border-border px-3 py-2">Capture off-CPU or mutex profile</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Interpreter frame where JIT should be</td>
        <td class="border border-border px-3 py-2">JIT deoptimised — fell back to baseline</td>
        <td class="border border-border px-3 py-2">Stabilise object shapes / types</td>
      </tr>
    </tbody>
  </table>
</div>

## Bea and Sven: one frame, two readings

<PersonaTag id="bea" lang="en" /> finds `processOrder` at 35% CPU and wants to rewrite the loop. <PersonaTag id="sven" lang="en" /> looks closer: most of that 35% is in `runtime.scanobject` (the GC) called from inside the loop. The loop allocates a lot. The fix is `sync.Pool`, not a new algorithm.

The flame graph showed the symptom. The cause was one level deeper.

## A scenario: regex on every request

A search endpoint shows `regex.test` as a wide leaf. Two engineers want to switch regex engines. A third looks at the parent: the regex is compiled on every request because the pattern is built from a template string. The fix is to compile once at startup. The leaf pointed at the right area; the bug was in the caller's pattern, not in the leaf itself.

<Inset kind="why" lang="en">
The leaf is the dashboard warning light: it says "something is wrong here." The fix may be inside the function (rewrite the algorithm), in the caller (don't call so often), in a callee (real cost one level down), or in the surrounding context (allocate less, lock less, fewer syscalls). Senior engineers read the whole neighbourhood, not just the leaf.
</Inset>

<Quiz
  id="01-what-makes-a-hot-path-quiz-1"
  lessonSlug="01-what-makes-a-hot-path"
  lang="en"
  question="A flame graph shows a wide leaf frame. What is the FIRST question to ask?"
  choices={[
    { label: "Why is this function expensive — is it doing CPU work, allocating, waiting on a lock, or waiting on a syscall? The shape decides the fix.", correct: true },
    { label: "Can I delete this function?", misconception: "Deletion is rarely an option; the function probably exists for a reason." },
    { label: "Can I rewrite it in a faster language?", misconception: "Premature; you do not yet know if CPU instructions are the bottleneck at all." },
    { label: "Can I cache its result?", misconception: "Sometimes correct, but only after diagnosing what makes it slow — caching a function whose cost is GC pressure does not help." },
  ]}
/>

<Quiz
  id="01-what-makes-a-hot-path-quiz-2"
  lessonSlug="01-what-makes-a-hot-path"
  lang="en"
  question="Why is 'wide frame = bottleneck' an incomplete reading of a flame graph?"
  choices={[
    { label: "A wide frame names the SYMPTOM (where time accumulates); the CAUSE may be in a callee, a caller, or in what the function is doing (compute, allocation, waiting). You need to read context.", correct: true },
    { label: "Flame graphs are unreliable tools", misconception: "Flame graphs are accurate; the issue is interpretation, not the tool." },
    { label: "Wide frames are usually noise in production data", misconception: "Wide frames are wide because samples landed there — that is real signal." },
    { label: "Flame graphs only work for toy programs", misconception: "Production profilers show production data; flame graphs are the standard format." },
  ]}
/>

<DragOrder
  id="01-what-makes-a-hot-path-drag-1"
  lessonSlug="01-what-makes-a-hot-path"
  lang="en"
  prompt="Order the steps of attacking a hot path the senior way:"
  items={[
    { id: "a", label: "Open the profile and find the widest leaf frame by self-time" },
    { id: "b", label: "Read the parent chain — is the leaf called from one path or many?" },
    { id: "c", label: "Classify the work: CPU instructions, allocation, cache miss, lock wait, syscall, or JIT deopt" },
    { id: "d", label: "Form one hypothesis about the fix that matches the classification" },
    { id: "e", label: "Apply ONLY that change in isolation" },
    { id: "f", label: "Capture a new profile under the same load and diff against baseline" },
    { id: "g", label: "Verify both the local hotspot shrank AND the headline metric improved" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g"]}
/>

<MetaphorComplete
  id="01-what-makes-a-hot-path-metaphor-1"
  pieceSlug="01-what-makes-a-hot-path"
  lang="en"
  setup="Fill in the blank: a wide flame-graph frame names the _______; the cause may sit one level above (in the caller), one level below (in a callee), or in what the function is actually doing."
  accepted={["symptom", "where", "location", "place", "site"]}
  canonical="symptom (the location where time accumulates, not necessarily the cause that produces it)"
  explanation="A wide frame is the dashboard light: it says 'something is wrong here'. The fix may be inside the function (rewrite the algorithm), in the caller (don't call so often), in a callee (the real cost is one level down), or in the surrounding context (allocate less, lock less, system-call less). Senior engineers read the whole neighbourhood, not just the leaf."
/>

</Explanation>

<KeyTakeaway>A wide leaf names the symptom — where time accumulates. The cause may be in the caller (called too often), a callee (cost one level down), or the work pattern (alloc, lock, syscall). Read the neighbourhood before picking a fix.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-what-makes-a-hot-path-retrieval"
  lang="en"
  questions={[
    { q: "In one paragraph: why is naming the hot function not enough — what else do you need to read from the profile before you can fix it?", a: "Naming a hot function tells you where time accumulates; it does not tell you what kind of work is consuming the time. The same wide frame can mean five different problems: the function is doing real CPU work and needs a better algorithm; the function is allocating a lot and triggering GC; the function is touching uncached memory and stalling on cache misses; the function is waiting on a lock; the function is making expensive syscalls. Each problem has a different fix — rewriting the algorithm helps the first but hurts the second; reducing allocations helps the second but does nothing for the third; restructuring data helps the third but is wasted on the fourth and fifth. To choose the right fix, read the parent chain (where is this called from?), read the children (is the cost actually in a callee?), and use a second profile of the right type. The wide frame opens the investigation; the rest of the diagnosis closes it." },
    { q: "Give two concrete examples where the fix is in the caller rather than in the wide leaf itself.", a: "1) A search endpoint shows regex.test as a wide leaf. Switching regex engines seems logical, but the parent reveals the regex is compiled on every request because the pattern is built from a template string. The fix is compiling once at startup — the leaf pointed at the area, but the bug was in the caller. 2) json.Marshal shows wide with one dominant parent path coming from a request logger. Switching JSON libraries looks like the fix, but the logger is calling Marshal on every request to write a request body to a log line — log the body at debug level only or drop it, and the leaf shrinks by 50% without touching the library." },
  ]}
/>

<Recap lang="en">
A hot path is the sequence of calls where the program spends most of its time. The flame graph's wide leaf names the function, but the cause may be in the caller (too many calls), a callee (real cost one level down), or in what the function does (CPU work vs allocation vs waiting). The diagnosis question — which of the five shapes is this hotspot — must precede the fix choice. The next lessons cover each of the five shapes and their fix families.
</Recap>
