infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label Wide frame = 1 symptom
    children
      - label CPU work (high self-time)
        children
          - label Fix: rewrite the algorithm
      - label Allocation (GC pressure)
        children
          - label Fix: reduce allocations / pool
      - label Cache miss (memory stall)
        children
          - label Fix: restructure data layout
      - label Lock wait (off-CPU)
        children
          - label Fix: shrink the critical section
      - label Syscall (kernel wait)
        children
          - label Fix: batch / fewer syscalls
theme
  palette #6366f1 #14b8a6 #f59e0b #f43f5e
