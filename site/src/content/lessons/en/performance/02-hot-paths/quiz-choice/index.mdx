---
concepts:
- five-shapes
- symptom-vs-cause
- parent-chain
- ipc-diagnosis
- false-sharing
- constant-time
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.brendangregg.com/usemethod.html
- https://easyperf.net/blog/2019/02/09/Top-Down-performance-analysis-methodology
- https://www.intel.com/content/www/us/en/docs/vtune-profiler/user-guide/2024-2/cpu-microarchitecture-analysis.html
status: ready
summary: Multiple-choice synthesis across the hot-paths unit — the five shapes, symptom-vs-cause reading, parent/child chains, IPC and TMA, false sharing, and the security gate.
title: 'Hot paths: multiple-choice review'
track: performance
unit: 02-hot-paths
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one mirrors a call you make mid-incident with a profile open — not a definition to recite, but a diagnosis to commit to before you spend a sprint on the wrong fix.
</Hook>

<Goal lang="en">
Confirm you can chain the unit together: read a wide leaf as a symptom, classify it into one of the shapes, locate the fix via the parent/child chain, and respect the constraints production adds — counters, tail latency, and the security gate.
</Goal>

<Quiz
  id="02-hot-paths-choice-1"
  lessonSlug="02-hot-paths"
  lang="en"
  question="A flame graph shows renderTemplate at 28% self-time. The allocation profile then shows fmt.Sprintf at 62% of alloc bytes inside it. Two engineers want to switch template engines. What is the correct read?"
  choices={[
    { label: "Allocation-bound, not CPU-bound. The self-time is being spent in per-iteration Sprintf + string concat, which the alloc profile confirms. Fix the allocation (strings.Builder, pre-sized buffer); switching engines moves the metric far less.", correct: true },
    { label: "CPU-bound — 28% self-time means the function is executing instructions, so rewrite the algorithm.", misconception: "Self-time only says the samples landed here; it does not say whether the CPU was executing or feeding the allocator. The alloc profile reclassifies it as allocation-bound, where an algorithm rewrite barely helps." },
    { label: "The engines are the bottleneck — both engineers are right, just measure which is faster.", misconception: "The cost is Sprintf + concat in the caller's loop, not the engine internals. A faster engine still pays for the same allocations the loop creates." },
    { label: "Lock-bound — high self-time under load implies contention.", misconception: "Lock contention shows wide in the off-CPU/mutex profile and narrow on-CPU. This frame is wide on-CPU with a clear allocation signature." },
  ]}
/>

<Quiz
  id="02-hot-paths-choice-2"
  lessonSlug="02-hot-paths"
  lang="en"
  question="Two wide leaves look identical on the flame graph. perf stat shows leaf A at IPC 3.0 with a 1% cache-miss rate, and leaf B at IPC 0.4 with an 18% cache-miss rate. How do the fixes differ?"
  choices={[
    { label: "A is compute-bound — better algorithm, SIMD, specialisation. B is memory-bound — data-layout change (SoA, contiguous arrays, iteration order). Same width, opposite toolboxes; the counters, not the flame graph, decide.", correct: true },
    { label: "Both need a better algorithm since both are wide on CPU.", misconception: "B's IPC of 0.4 means the CPU is stalled ~60% of the time waiting for RAM. A new algorithm that touches the same scattered memory still stalls — it is memory-bound, not compute-bound." },
    { label: "B is the bigger problem because its cache-miss rate is higher, so fix B first.", misconception: "Miss rate classifies the shape; it does not rank impact. Priority comes from each leaf's share of total time (Amdahl), not from its miss rate." },
    { label: "Both are allocation-bound — high cache-miss rate means GC pressure.", misconception: "Allocation-bound shows as wide GC frames (mallocgc, scanobject), not as low IPC on the leaf itself. Cache misses are a memory-access pattern, not a GC signal." },
  ]}
/>

<Quiz
  id="02-hot-paths-choice-3"
  lessonSlug="02-hot-paths"
  lang="en"
  question="json.Marshal is at 28% CPU. In one service it has a single dominant parent (a request logger); in another it has twenty thin parents (handlers). Where does each fix belong?"
  choices={[
    { label: "Concentrated fan-in → fix the caller: the logger is marshalling every request's body regardless of log level; stop doing that. Distributed fan-in → fix the leaf: it is shared infrastructure, so switch library or pre-encode because optimising one of twenty callers changes almost nothing.", correct: true },
    { label: "Both cases: switch to a faster JSON library — that is what 28% on Marshal means.", misconception: "The single-parent case is a caller bug (encoding per request for logs); swapping libraries there leaves the redundant encode in place. Read the parent chain before reaching for the library." },
    { label: "Both cases: fix the leaf, because the cost is always inside the wide function.", misconception: "With one dominant parent the cheapest win is in the caller (call it less / not at all). Fan-in shape, not the leaf width, decides leaf-fix vs caller-fix." },
    { label: "Both cases: inline Marshal into each caller to remove call overhead.", misconception: "The cost is the marshalling work, not call dispatch. Inlining duplicates code and does nothing for the actual serialisation cost." },
  ]}
/>

<Quiz
  id="02-hot-paths-choice-4"
  lessonSlug="02-hot-paths"
  lang="en"
  question="A lock-free counter array uses atomic adds with per-thread indices, yet under load IPC collapses to 0.42, cache-miss rate hits 76%, and throughput gets worse as you add threads. Diagnosis and fix?"
  choices={[
    { label: "False sharing. Eight uint64 counters share one 64-byte line, so each thread's atomic write invalidates the line in every other core's cache via MESI — the hardware serialises them as if locked. Fix: pad each counter to its own cache line (e.g. value + [56]byte).", correct: true },
    { label: "Lock contention — the atomics are effectively a spinlock, so move to a real mutex.", misconception: "Atomics are not locks, and contention would show as off-CPU wait, not on-CPU IPC collapse. XSNP_HITM confirms cache-line bouncing, not scheduler blocking." },
    { label: "The increment algorithm has bad complexity — rewrite it.", misconception: "An atomic add is O(1). The cost is memory-system coherency traffic, which no algorithmic change to a single-instruction add can fix." },
    { label: "The array exceeds L1 capacity — shrink it.", misconception: "16 uint64 is 128 bytes and fits L1 easily. The problem is multiple cores writing the same 64-byte line, not capacity." },
  ]}
/>

<Quiz
  id="02-hot-paths-choice-5"
  lessonSlug="02-hot-paths"
  lang="en"
  question="A Node flame graph shows InterpreterCallStub frames dominating a function that should be hot, and latency spikes that do not correlate with traffic. After a 'better algorithm' rewrite, nothing improves. Why, and what is the actual fix?"
  choices={[
    { label: "JIT deopt: V8 optimised the function, a type assumption broke, and it fell back to the interpreter/baseline tier — every call now pays interpreter cost. An algorithm rewrite is wasted until types are stabilised (≤4 hidden classes, no late property addition in the hot loop).", correct: true },
    { label: "It is CPU-bound and the rewrite was simply not aggressive enough.", misconception: "CPU-bound code shows optimised TurboFan frames, not interpreter frames. The interpreter frame is itself the deopt signal; no algorithm fixes a runtime that refuses to optimise the code." },
    { label: "It is allocation-bound — pool the objects.", misconception: "Allocation-bound shows wide GC frames, not interpreter frames. Pooling does not bring TurboFan back; type stabilisation does." },
    { label: "The profiler is sampling that one function too aggressively.", misconception: "Sample rate affects all functions uniformly; it cannot selectively manufacture interpreter frames for a single function. The deopt is real." },
  ]}
/>

<Quiz
  id="02-hot-paths-choice-6"
  lessonSlug="02-hot-paths"
  lang="en"
  question="An engineer makes a token-comparison hot path 3x faster by adding an early-exit branch on the first mismatching byte. The flame frame shrank and p50 improved. Why is this still wrong?"
  choices={[
    { label: "It breaks the constant-time invariant: response latency now depends on how long a prefix matched, turning a 32-byte secret search from ~2^256 into ~32×256 guesses for an attacker timing responses. Constant-time, security-sensitive paths require a security-review gate before any speed change.", correct: true },
    { label: "Nothing is wrong — faster token validation is strictly better.", misconception: "Timing side channels are a real attack class; the constant-time, branch-free form exists precisely to deny the attacker this signal. A local speedup that leaks the secret is a regression." },
    { label: "The early exit introduces a data race on the token buffer.", misconception: "The broken property is timing, not concurrency. Even fully serialised, the data-dependent exit time leaks the matching-prefix length." },
    { label: "It only matters if the median moved; since p50 improved, the change is safe.", misconception: "p50 improving is irrelevant to security. The leak is in the relationship between input and time, not in any latency percentile." },
  ]}
/>

<Recap lang="en">
The unit is one decision tree: a wide leaf names the symptom, not the cause; classify it into a shape (CPU, allocation, cache, lock, syscall, JIT deopt, or the hardware-only cases — false sharing, native-bridge); locate the fix via the parent chain (one caller vs many) and the child chain (work here vs one level down); reach for hardware counters and TMA when CPU-bound and memory-bound look identical; and gate any change to constant-time or security-sensitive paths through review. Diagnose first, fix one thing, prove it with a profile diff — and watch tail latency, not just the mean.
</Recap>
