---
concepts:
- cpu-bound
- allocation-bound
- cache-bound
- lock-bound
- syscall-bound
- IPC
- fix-family
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: middle
order: 2
prereqs:
- performance/02-hot-paths/01-what-makes-a-hot-path
slug: 02-five-shapes-of-hotspot
sources:
- https://www.brendangregg.com/usemethod.html
- https://easyperf.net/blog/2019/02/09/Top-Down-performance-analysis-methodology
- https://www.intel.com/content/www/us/en/docs/vtune-profiler/user-guide/2024-2/cpu-microarchitecture-analysis.html
spiral:
- latency
status: ready
summary: Each of the five hotspot categories has a tell-tale signature in the profile
  and a categorical fix. Picking the wrong fix family wastes the entire engineering
  effort.
title: 'Five shapes of hotspot: CPU, alloc, cache, lock, syscall'
track: performance
unit: 02-hot-paths
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
Two wide leaves on a flame graph look identical at first glance. One needs a better algorithm. The other needs buffer reuse. Applying a better algorithm to an allocation-bound path moves the metric by 1.05x instead of the predicted 3x. Two hours of work for five percent. The category maps the toolbox.
</Hook>

<Crux>Five shapes — CPU, alloc, cache, lock, syscall — each with its own profile signature and fix family. Diagnose the shape first.</Crux>

<Explanation>

## The five categories

By the end of this lesson you will be able to classify any hot leaf in under five minutes and select the fix family that actually moves the metric.

A wide leaf fits one of five categories. Reading the profile's second layer — not just "which function" but "what is the function doing" — gives the classification.

### 1. CPU-bound algorithmic

The function executes a lot of instructions. The CPU is running the algorithm.

**Signature:** large self-time, narrow children, high instructions-per-second, IPC in the range 2–4. In a CPU flame graph the leaf occupies real width with no GC or kernel frames nearby.

**Fix family:** better algorithm, vectorisation (SIMD), inline pragma, hot-path specialisation for the common case.

### 2. Allocation-bound

The function (or its caller) allocates so much that garbage collection dominates wall-time.

**Signature:** `runtime.scanobject`, `gc`, `mallocgc`, or `malloc` appears wide near the hot leaf. The CPU profile blames GC machinery, not application logic. Switch to an allocation profile to name the application-side allocator.

**Fix family:** object pooling, buffer reuse (`sync.Pool`), in-place mutation, struct-of-arrays, pre-size containers to avoid repeated growth.

### 3. Cache-bound

The function touches memory in a pattern the hardware prefetcher cannot predict. The CPU stalls waiting for data from RAM instead of L1/L2.

**Signature:** low IPC (&lt;1), high cache-miss rate (15%+), low instructions-per-second despite a wide CPU frame. Hardware counters confirm the stall type (L3 miss, DRAM stall).

**Fix family:** data-layout change (contiguous arrays instead of pointer-chased linked lists, struct-of-arrays instead of array-of-structs), iteration-order change to improve spatial locality, prefetch hints.

### 4. Lock-bound

The function spends time waiting on a mutex or channel.

**Signature:** wide in the mutex/block/off-CPU profile, narrow in the CPU profile. The function is off-CPU, not running. Wall-clock time is high; CPU time is low.

**Fix family:** lock-free data structures, finer-grained locks, sharded state, read-write locks for read-heavy paths, eventual consistency to eliminate the shared state.

### 5. Syscall-bound

The function spends time inside the kernel — reading, writing, network I/O, or waiting on `futex`.

**Signature:** kernel frames (`read`, `write`, `recv`, `futex`) visible in a flame graph with kernel-symbol support. Off-CPU time dominates. May appear as frequent narrow kernel entries rather than one wide leaf.

**Fix family:** batch syscalls (one `writev` instead of ten `write` calls), larger I/O buffers, `io_uring` for async kernel I/O, memory-mapped I/O, eliminate the syscall entirely where data can stay in user space.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Category</th>
        <th class="border border-border px-3 py-2 text-left">Profile signature</th>
        <th class="border border-border px-3 py-2 text-left">Fix family</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">CPU-bound</td>
        <td class="border border-border px-3 py-2">High self-time, IPC 2–4</td>
        <td class="border border-border px-3 py-2">Better algorithm, SIMD, specialisation</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Allocation-bound</td>
        <td class="border border-border px-3 py-2">GC frames wide (mallocgc, scanobject)</td>
        <td class="border border-border px-3 py-2">Pooling, buffer reuse, SoA</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Cache-bound</td>
        <td class="border border-border px-3 py-2">IPC &lt;1, high cache-miss rate</td>
        <td class="border border-border px-3 py-2">Data layout change, contiguous arrays</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Lock-bound</td>
        <td class="border border-border px-3 py-2">Wide off-CPU, narrow on-CPU</td>
        <td class="border border-border px-3 py-2">Lock-free, sharding, finer granularity</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Syscall-bound</td>
        <td class="border border-border px-3 py-2">Kernel frames in flame graph</td>
        <td class="border border-border px-3 py-2">Batch syscalls, io_uring, larger buffers</td>
      </tr>
    </tbody>
  </table>
</div>

<NumbersCard
  id="card-hot-numbers"
  title="Hot-path diagnosis numbers"
  stats={[
    { label: "Typical IPC of compute-bound code", value: "2–4 instructions/cycle" },
    { label: "Typical IPC of memory-bound code", value: "0.3–0.8 instructions/cycle" },
    { label: "L1 cache miss penalty", value: "~5 cycles" },
    { label: "L3 cache miss to DRAM penalty", value: "~150–300 cycles" },
    { label: "Branch mispredict penalty", value: "~15–25 cycles" },
    { label: "Cost of one deopt + recompile (V8)", value: "~10–100 μs" },
    { label: "syscall round-trip cost", value: "~1–5 μs" },
    { label: "futex lock contention wakeup", value: "~5–50 μs" },
  ]}
/>

<Misconception id="mc-leaf-blame">
  <p><strong>"The wide leaf IS the bottleneck — rewrite it."</strong> The leaf is the symptom; the cause may be the caller (called too often), a callee (cost one level down), or the work pattern (alloc, lock, syscall). Read the neighbourhood first.</p>
</Misconception>

## Classifying a hotspot in practice

<FadedExample
  client:load
  id="fe-hot-1"
  pieceSlug="02-five-shapes-of-hotspot"
  lang="en"
  title="Classify a hotspot before picking the fix"
  steps={{
    solved: (
      <div class="space-y-3 text-sm">
        <p><strong>Setup.</strong> CPU profile shows function <code>renderTemplate</code> at 28% self-time. Bea wants to switch template engines. Sven looks first.</p>
        <pre class="text-xs bg-zinc-100 p-2 rounded overflow-x-auto"><code>{`# 1. Read parent chain — pprof: top + tree
(pprof) top 5 --cum
    flat  flat%   cum%   function
   0.8s   3.5%  28.0%   handleRequest    # cum: 28% via renderTemplate
   6.2s  28.0%  28.0%   renderTemplate   # self-time leader

# Single wide caller (handleRequest) — concentrated call site.
# Walk DOWN: is the cost in renderTemplate itself or in a callee?

(pprof) list renderTemplate
  renderTemplate:
    line 42: for k, v := range data {
    line 43:    out = out + fmt.Sprintf("%s=%v;", k, v)  # 5.4s self-time
    line 44: }

# Line 43 is hot. Profile says self-time, but the work is
# string concatenation + Sprintf — ALLOCATION heavy, not CPU-bound.
# Switch to allocation profile to confirm.

# 2. Allocation profile (go tool pprof -alloc_space)
(pprof) top 5
   flat  flat%   function
   180MB  62%   fmt.Sprintf       # allocation hotspot confirmed
    60MB  21%   strings.<concat>

# Diagnosis: CATEGORY = allocation-bound, not CPU-bound algorithmic.
# Fix family: pre-allocate buffer, use strings.Builder / bytes.Buffer.
# Switching template engines would help far less than removing Sprintf+concat.`}</code></pre>
        <p><strong>The fix:</strong> rewrite line 43 with <code>strings.Builder</code>, pre-sized for the average output length. Re-profile shows <code>renderTemplate</code> self-time drops to 6%, <code>fmt.Sprintf</code> disappears from the allocation profile, GC frame width drops 60%, and p99 drops from 380 ms to 220 ms. Switching template engines (the original instinct) would have moved 28% by 2x at best (Amdahl ≤1.16x); the actual fix moved it 4.7x (Amdahl 1.30x) and shrank GC.</p>
      </div>
    ),
    semi: {
      prompt: "If the hot function showed wide in the off-CPU profile (not the CPU profile), which classification is it most likely to be?",
      blanks: [
        { id: "cat", expected: "lock", placeholder: "category (one word)" },
      ],
    },
    blank: {
      prompt: "Why does diagnosing the category before picking a fix save real engineering time?",
      reveal: (
        <div class="space-y-2 text-sm">
          <p>Each category has a different fix family, and the wrong fix family wastes the work entirely. A team that rewrites an algorithm when the bottleneck is allocation gets a small win and a code-review fight; a team that switches data layout when the bottleneck is a lock gets nothing. Diagnosing first costs minutes (one more profile capture, one read of the right view); not diagnosing costs days (the wrong fix lands, does not move the metric, the team starts over). The category map turns hot-path work into a decision tree instead of a guessing game.</p>
        </div>
      ),
    },
  }}
  misconceptions={{
    "cat:cpu": "If it shows in the OFF-CPU profile, the function is waiting, not running on CPU. Wait time on a mutex/channel = lock-bound; on disk/network = syscall-bound.",
    "cat:io": "Closer — but the specific category in the five-way taxonomy is either 'lock' (mutex/channel wait) or 'syscall' (kernel-side I/O wait). 'io' is a broader bucket.",
  }}
/>

<Quiz
  id="02-five-shapes-of-hotspot-quiz-1"
  lessonSlug="02-five-shapes-of-hotspot"
  lang="en"
  question="A function shows IPC of 0.4 and cache-miss rate of 15%. What is the category, and what fix family does it suggest?"
  choices={[
    { label: "Memory-bound (cache-bound). Fix family: data-layout change (contiguous arrays, SoA), iteration-order change, prefetch hints. Algorithmic rewrite alone will not help if the new algorithm still touches the same scattered memory.", correct: true },
    { label: "CPU-bound. Fix: better algorithm.", misconception: "Low IPC means the CPU is stalled waiting for memory, not executing instructions — algorithm changes that do not fix the memory access pattern still stall." },
    { label: "Lock-bound. Fix: lock-free data structures.", misconception: "Lock wait would not show as low IPC on the function itself; it would show in the off-CPU/block profile." },
    { label: "Allocation-bound. Fix: pool buffers.", misconception: "Allocation would appear as wide GC frames, not as low IPC on the function itself." },
  ]}
/>

<Quiz
  id="02-five-shapes-of-hotspot-quiz-2"
  lessonSlug="02-five-shapes-of-hotspot"
  lang="en"
  question="After a fix, the local hotspot shrank by 60% but the service's p99 is unchanged. What does this most likely mean?"
  choices={[
    { label: "The local win revealed (or was always paired with) a second bottleneck that was being masked. Capture a new profile and look at what is now widest.", correct: true },
    { label: "The fix did not actually deploy", misconception: "Possible, but the profile already shows the fix landed locally; the question is why the headline did not move." },
    { label: "Performance is random at scale", misconception: "Performance is deterministic at scale; one profile shift not moving the headline has a specific systemic cause." },
    { label: "Production traffic changed", misconception: "Possible, but ruled out by capturing under the same load as the baseline." },
  ]}
/>

</Explanation>

<StackDiagram
  label="The five shapes of hotspot, each with its profile signature"
  hue="var(--d-hardware)"
  caption="A wide leaf fits one of five categories — the profile signature names which one, and the category selects the fix family."
  layers={[
    { label: "1. CPU-bound", note: "high self-time, IPC 2–4" },
    { label: "2. Allocation-bound", note: "wide GC frames (mallocgc)" },
    { label: "3. Cache-bound", note: "IPC <1, high cache-miss" },
    { label: "4. Lock-bound", note: "wide off-CPU, narrow on-CPU" },
    { label: "5. Syscall-bound", note: "kernel frames (read/futex)" },
  ]}
/>

<KeyTakeaway>The five categories — CPU-bound, allocation-bound, cache-bound, lock-bound, syscall-bound — each have a distinct profile signature and a matching fix family. Diagnosing the category in five minutes prevents days of wasted engineering on the wrong toolbox.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-five-shapes-of-hotspot-retrieval"
  lang="en"
  questions={[
    { q: "Walk through the five hot-path categories with one tell-tale signature for each in a profile, and the fix family that matches.", a: "CPU-bound algorithmic: wide self-time leaf, high IPC (2–4 instructions/cycle), narrow children. Fix family: better algorithm, vectorisation, inline pragma, hot-path specialisation. Allocation-bound: GC frames (mallocgc, scanobject, malloc, free) wide near the hot leaf. Fix family: pool buffers, in-place mutation, struct-of-arrays, pre-size containers. Cache-bound: low IPC (<1), high cache-miss rate, low instructions-per-second despite wide CPU frame. Fix family: data-layout change (contiguous, SoA), iteration-order change, prefetch hints. Lock-bound: wide in mutex/block/off-CPU profile, narrow in CPU profile; the function is waiting, not running. Fix family: lock-free structures, finer-grained locks, sharded state, eventual consistency. Syscall-bound: kernel frames visible in flame graph (read, write, recv, futex), often dominant in off-CPU. Fix family: batch syscalls, larger buffers, io_uring, mmap'd I/O, or eliminate the syscall." },
    { q: "A Go API shows runtime.mallocgc at 18% and runtime.scanobject at 14% in the CPU profile. What is the category and what should the next diagnostic step be?", a: "Allocation-bound. The application code itself is not shown in those GC frames — the GC is paying for allocations the application code is causing. The next step is to switch to the allocation profile (/debug/pprof/allocs) to find which application functions are responsible for those allocations. The CPU profile names the symptom (GC consuming 32% of CPU); the allocation profile names the cause (which caller is allocating). Without the allocation profile, you might target the wrong function." },
  ]}
/>

<Recap lang="en">
The five hotspot categories — CPU, allocation, cache, lock, syscall — each have a distinct profile signature: IPC and self-time for CPU-bound, GC frames for allocation-bound, low IPC with high cache-miss for cache-bound, wide off-CPU but narrow on-CPU for lock-bound, kernel frames for syscall-bound. The diagnosis takes minutes (capture the right secondary profile, read the IPC or miss-rate); the fix family follows mechanically. Picking the wrong family wastes the work entirely. The next lesson covers how to read the parent and child chains to locate the fix at the right layer of the call tree. Now when you see GC frames climbing wide alongside a hot leaf, you will reach for the allocation profile before touching a single line of application logic.
</Recap>
