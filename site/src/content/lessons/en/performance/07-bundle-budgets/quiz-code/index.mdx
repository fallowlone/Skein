---
concepts:
- bundle-analyzer
- dynamic-import
- size-limit
- tree-shaking-pitfalls
- http3-priorities
estMin: 14
lang: en
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://web.dev/articles/granular-chunking-nextgen
- https://nextjs.org/docs/app/building-your-application/optimizing/bundle-analyzer
status: ready
summary: Read real bundler config, import patterns, a size-limit CI snippet, and a Priority header, predict the effect on shipped bytes, and pick the highest-leverage fix.
title: 'Bundle budgets: config and output reading'
track: performance
unit: 07-bundle-budgets
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Bundle problems are diagnosed in config files, import lines, the analyzer treemap, and the CI log. Read each snippet the way a senior would, then pick the fix that actually moves shipped bytes — not the one that only moves the wire.
</Hook>

<Goal lang="en">
Practise the loop you run in every bundle review: read the import or config, predict whether it ships dead weight, and reach for the highest-leverage fix — split or shake the bytes before you reach for compression or a knob.
</Goal>

## Snippet 1 — the barrel re-export

```js
// components/index.ts
export * from './Button';
export * from './Modal';
export * from './RichTextEditor';   // pulls in a 180 KB editor lib

// consumer
import { Button } from '@/components';   // only Button is used here
```

<Quiz
  id="07-bundle-budgets-code-1"
  lessonSlug="07-bundle-budgets"
  lang="en"
  question="The route only uses Button, yet the analyzer shows the 180 KB editor in this chunk. What is happening, and what is the fix?"
  choices={[
    { label: "`export *` is a barrel that re-exports everything; if any consumer imports from it, the bundler tends to keep the whole module graph it touches — including RichTextEditor's editor lib. Fix: explicit named re-exports, or import directly from './Button'.", correct: true },
    { label: "Button transitively imports the editor.", misconception: "Button does not import the editor. The barrel's `export *` is what drags the editor into the graph for any consumer of the barrel." },
    { label: "Nothing is wrong — the editor will be tree-shaken because it is unused.", misconception: "`export *` barrels frequently defeat tree shaking: the bundler keeps the re-exported modules when even one barrel export is consumed. The editor ships." },
    { label: "Set sideEffects: false to drop the editor.", misconception: "sideEffects: false helps with module-scope side effects, not with a barrel that statically re-exports a used module's siblings. Fix the re-export." },
  ]}
/>

## Snippet 2 — the dynamic import

```js
// loads a chart panel that is not on first paint
function openPanel(name) {
  return import(`./panels/${name}`);   // variable in the specifier
}

// elsewhere, a true lazy boundary
const Editor = React.lazy(() => import('./Editor'));
```

<Quiz
  id="07-bundle-budgets-code-2"
  lessonSlug="07-bundle-budgets"
  lang="en"
  question="Both lines look like code splitting. Which one actually produces a clean, individually-shakeable chunk, and why?"
  choices={[
    { label: "Only `import('./Editor')` with a string literal. The variable specifier in `import('./panels/' + name)` forces the bundler to bundle every file under ./panels into one catch-all chunk — it cannot know at build time which module is requested.", correct: true },
    { label: "Both are equivalent — dynamic import always splits cleanly.", misconception: "A runtime-variable specifier makes the target unknowable at build time, so the bundler must include the whole matched directory in one chunk. Only the string literal splits cleanly." },
    { label: "Only the variable form, because it is more flexible.", misconception: "Flexibility is the cost here: the variable form bundles the entire ./panels folder. The string-literal `import('./Editor')` is the one that yields a precise chunk." },
    { label: "Neither — React.lazy disables code splitting.", misconception: "React.lazy is built on dynamic import() and is exactly how you split a component. The string-literal import is the clean one." },
  ]}
/>

## Snippet 3 — the size-limit CI gate

```json
// package.json
"size-limit": [
  { "name": "Homepage JS", "path": ".next/static/**/_app-*.js", "limit": "100 KB" },
  { "name": "All pages total", "path": ".next/static/**/*.js", "limit": "500 KB" }
]
```

```
$ npx size-limit
  Homepage JS:     132 KB  (limit 100 KB)   FAIL  +32 KB
  All pages total: 471 KB  (limit 500 KB)   PASS
```

<Quiz
  id="07-bundle-budgets-code-3"
  lessonSlug="07-bundle-budgets"
  lang="en"
  question="A PR triggers this output. The author asks to merge and 'fix it next sprint'. What is the correct senior response?"
  choices={[
    { label: "Block the merge. The homepage gate exists precisely to stop this regression at the source. The author either lazy-loads/splits the addition back under 100 KB or opens a budget-raise PR with sign-off — the limit is never bypassed silently.", correct: true },
    { label: "Merge it — the 'All pages total' gate still passes, so the budget is met.", misconception: "Each named limit is independent; the homepage limit failed. A passing aggregate does not excuse a per-route regression." },
    { label: "Raise the homepage limit to 132 KB in this same PR to make CI green.", misconception: "Self-approving a raise in the offending PR defeats the gate. A raise requires the budget-raise protocol: documented justification and performance-lead sign-off." },
    { label: "Add a skip flag to bypass size-limit for this PR.", misconception: "Bypassing the gate destroys its value — budgets erode one bypass at a time. The point is that every change is visible and explicit." },
  ]}
/>

## Snippet 4 — the HTTP/3 priority

```
# Browser request for the critical app chunk
GET /_next/static/app.abc123.js
Priority: u=1

# Browser request for a large deferred vendor chunk
GET /_next/static/vendor.def456.js
Priority: u=5

# CDN config (misconfigured): ignores client Priority, serves FIFO
```

<Quiz
  id="07-bundle-budgets-code-4"
  lessonSlug="07-bundle-budgets"
  lang="en"
  question="The app is correctly code-split and the browser sends sane RFC 9218 priorities, but the CDN ignores them and serves first-come-first-served. What is the likely impact and where do you verify it?"
  choices={[
    { label: "The large u=5 vendor chunk competes equally for bandwidth with the u=1 critical chunk, so the critical chunk can arrive late — erasing up to 20–30% of the split benefit. Verify in Chrome's Network panel 'Priority' column and the CDN's priority support.", correct: true },
    { label: "No impact — HTTP/3 is faster than HTTP/2 regardless of priorities.", misconception: "HTTP/3's transport gains don't help if priority hints are ignored: bandwidth is shared equally and the critical chunk loses its head start." },
    { label: "The vendor chunk loads last because it is larger.", misconception: "Without honored priorities, ordering is non-deterministic / FIFO — size does not push it last. Correct priorities are what would order the small critical chunk first." },
    { label: "Re-merge the chunks into one bundle to avoid the contention.", misconception: "Un-splitting reintroduces the full parse cost on the critical path. The fix is to make the CDN honor priorities, not to abandon splitting." },
  ]}
/>

<Recap lang="en">
Every bundle regression is read in config and output: `export *` barrels and runtime-variable dynamic imports silently defeat splitting and tree shaking; a size-limit failure is a hard gate, not a suggestion to defer; and even a perfectly split bundle underdelivers if the CDN ignores HTTP/3 priorities. Diagnose from the analyzer and the CI log, fix by removing bytes from the critical path first, then confirm delivery order — re-measure rather than assume.
</Recap>
