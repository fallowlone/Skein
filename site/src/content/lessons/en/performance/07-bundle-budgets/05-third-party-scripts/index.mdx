---
concepts:
- third-party-scripts
- partytown
- interaction-gating
- async-defer
- csp
deepensInto:
- 07-bundle-budgets/06-ci-enforcement
estMin: 14
lang: en
lessonType: topic
level: middle
order: 5
prereqs:
- 07-bundle-budgets/04-tree-shaking-and-compression
slug: 05-third-party-scripts
sources:
- https://web.dev/articles/vitals
- https://web.dev/articles/preload-critical-assets
spiral:
- latency
status: ready
summary: Analytics, ads, chat widgets, and A/B testing tools routinely add 500 KB
  to 1 MB across a page — outside your control but inside the user''''s cost. Three
  patterns mitigate them.
title: 'Third-party scripts: the silent budget killer'
track: performance
unit: 07-bundle-budgets
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A team spends a sprint cutting their app bundle from 800 KB to 350 KB. Lighthouse score improves by 12 points. A week later the data team adds Segment, Hotjar, and a new Intercom widget — collectively 450 KB. The sprint's work is erased. No one noticed because third-party scripts are not in the app repository.
</Hook>

<Crux>Third-party scripts: outside your repo, inside user cost. Interaction gating, Worker isolation, server-side measurement neutralize them.</Crux>

<Explanation>

## The third-party tax

Most production web apps load 5-15 third-party scripts per page. Common ones and their approximate sizes:

- Segment analytics: ~150 KB
- Hotjar recording: ~200 KB
- Intercom chat widget: ~200 KB
- Google Tag Manager (with several tags): ~500 KB+
- Full Optimizely A/B testing: ~400 KB
- A single poorly optimised ad tag: ~300 KB

Ten average scripts × 100 KB = 1 MB of third-party JS before your own code. None of this appears in your bundle analyzer because the files load from external domains at runtime. None of it can be tree-shaken because you do not control the source. And most of it runs on the main thread, blocking paint and interaction.

The problem compounds: business teams often add scripts through Google Tag Manager without engineering review. Each tag manager addition escapes code review entirely.

## Pattern 1: Interaction gating

Load the script only when the user takes an action that requires it. The chat widget loads when the user clicks "Help". The analytics SDK loads after the first meaningful interaction. The newsletter modal script loads when the user scrolls to 80% of the page.

```html
<!-- Defer until user clicks -->
<button id="chat-btn">Chat with us</button>
<script>
  document.getElementById('chat-btn').addEventListener('click', () => {
    const script = document.createElement('script');
    script.src = 'https://widget.intercom.io/widget/APP_ID';
    document.body.appendChild(script);
  });
</script>
```

Tradeoff: the first interaction that triggers the load has a visible delay as the chunk downloads and parses. Mitigate with `prefetch` when the trigger element becomes visible (`IntersectionObserver` on the button) or on `mouseenter`.

## Pattern 2: Web Worker isolation via PartyTown

PartyTown is an open-source library (Builder.io) that runs third-party scripts in a Web Worker via a service-worker proxy. DOM access from the Worker is bridged via synchronous postMessage. The main thread is freed from the analytics, ad, and tracking scripts entirely.

```html
<script>
  partytown = { forward: ['dataLayer.push', 'fbq', 'gtag'] };
</script>
<script src="/~partytown/partytown.js"></script>
<script type="text/partytown" src="https://www.googletagmanager.com/gtm.js?id=GTM-XXXX"></script>
```

Tradeoff: scripts that rely on synchronous DOM reads are incompatible (rare for analytics). postMessage round-trips add ~5-15 ms per call. Some scripts break when proxied. Test each vendor before deploying.

## Pattern 3: Server-side measurement

Move analytics events to the server. Instead of shipping a 150 KB Segment client to every user, fire Segment events from your API server on relevant actions. Users get the tracking; browsers get nothing.

Similar options: Google Analytics Measurement Protocol, Sentry server-side error capture for SSR errors, custom event pipeline via a lightweight beacon endpoint.

Tradeoff: you lose browser-only context — user agent details, real-time client errors, click heatmaps. Suitable for core funnel analytics; combine with a lean client SDK for browser-specific signals.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Pattern</th>
        <th class="border border-border px-3 py-2 text-left">Main-thread cost</th>
        <th class="border border-border px-3 py-2 text-left">Tradeoff</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Interaction gating</td>
        <td class="border border-border px-3 py-2">Zero until triggered</td>
        <td class="border border-border px-3 py-2">First-use latency; mitigate with prefetch</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">PartyTown Web Worker</td>
        <td class="border border-border px-3 py-2">Near-zero (proxied to Worker)</td>
        <td class="border border-border px-3 py-2">DOM-sync incompatible scripts; postMessage overhead</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Server-side measurement</td>
        <td class="border border-border px-3 py-2">Zero (nothing on client)</td>
        <td class="border border-border px-3 py-2">Loses browser-specific context</td>
      </tr>
    </tbody>
  </table>
</div>

## Governance: a separate third-party budget

Senior teams maintain a separate third-party budget alongside the app bundle budget. Concretely: a `third-party-budget.json` file in the repo that lists every permitted third-party domain, its approved size, and the owning team. Any new script added via GTM or `<script src>` that is not in the file fails CI.

Complementary enforcement: CSP (Content Security Policy) `script-src` header lists allowed origins. Scripts from unlisted origins are blocked by the browser. This prevents accidental or malicious third-party script injection. Quarterly audit reviews every entry, removes unused vendors, and checks vendor track records.

<Inset kind="why" lang="en">
Why do third-party scripts disproportionately hurt? (1) Vendors prioritise features over bytes — a chat SDK supports every use case even if the app uses 5%. (2) No tree shaking — you cannot remove unused vendor code. (3) Cumulative effect — 10 scripts average 100 KB each = 1 MB before your own code. (4) Main-thread blocking — most run synchronously on page load unless explicitly deferred.
</Inset>

<Quiz
  id="05-quiz-1"
  lessonSlug="05-third-party-scripts"
  lang="en"
  question="Marketing wants a 200 KB chat widget on every page and says it must load immediately. Senior compromise?"
  choices={[
    { label: "Load the widget on first click of the chat button. The widget is 'on every page' via the button — but costs zero bytes on initial load. Prefetch the script on button hover.", correct: true },
    { label: "Remove the widget.", misconception: "If the business requires it, removal is not the right tradeoff. Deferral achieves the business goal without the performance cost." },
    { label: "Move the widget to load last after the page is interactive.", misconception: "'Load last' still blocks the main thread when it runs. Interaction gating avoids the cost entirely until triggered." },
    { label: "Add async to the script tag.", misconception: "async delays the script until after HTML parse, but still executes on the main thread and adds ~200 KB of parse+execute cost on page load." },
  ]}
/>

<Quiz
  id="05-quiz-2"
  lessonSlug="05-third-party-scripts"
  lang="en"
  question="A team runs five analytics scripts through GTM totalling 600 KB. They migrate funnel events to server-side Measurement Protocol. What is the expected client-side impact?"
  choices={[
    { label: "The five scripts no longer ship to the browser at all — 600 KB removed from every page load. LCP and INP improve proportionally.", correct: true },
    { label: "The events are lost — server-side is unreliable.", misconception: "Server-side Measurement Protocol is used at scale by large companies and is reliable. Events fire from your server on relevant API actions." },
    { label: "Partial improvement — only the server-initiated events are removed.", misconception: "All five scripts are replaced by server calls. The full 600 KB is removed from the client." },
    { label: "No impact — GTM loads scripts lazily anyway.", misconception: "GTM loads its container synchronously on page load. The tags inside it also execute on the main thread." },
  ]}
/>

<Quiz
  id="05-quiz-3"
  lessonSlug="05-third-party-scripts"
  lang="en"
  question="Why is a Content Security Policy (CSP) script-src header relevant to third-party budget governance?"
  choices={[
    { label: "CSP blocks scripts from domains not in the allow-list. Adding a new vendor without engineering approval fails at runtime — the browser refuses to load the script.", correct: true },
    { label: "CSP compresses third-party scripts automatically.", misconception: "CSP is a security and governance mechanism, not a compression tool." },
    { label: "CSP prevents GTM from loading any scripts.", misconception: "GTM itself is allowed in CSP if listed. CSP restricts what GTM can then load from unlisted domains." },
    { label: "CSP is irrelevant to performance — it is security only.", misconception: "CSP doubles as a governance tool: it enforces the third-party allow-list at the browser level, preventing unreviewed additions from going live." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="A third-party script blocks the main thread unless one of three patterns contains it"
  caption="By default a third-party script runs synchronously on the main thread. Interaction gating, PartyTown Web Worker isolation, and server-side measurement each remove some or all of that cost."
  hue="var(--d-hardware)"
  nodes={[
    { id: "script", label: "Third-party script", sub: "unbudgeted bytes", col: 0, row: 1 },
    { id: "block", label: "Blocks main thread", sub: "default: sync", col: 1, row: 0 },
    { id: "gate", label: "Interaction gating", sub: "load on trigger", col: 1, row: 1 },
    { id: "party", label: "PartyTown Worker", sub: "off main thread", col: 1, row: 2 },
    { id: "server", label: "Server-side measure", sub: "nothing on client", col: 1, row: 3 },
    { id: "free", label: "Main thread freed", sub: "LCP / INP recover", col: 2, row: 2 },
  ]}
  edges={[
    { from: "script", to: "block", label: "default" },
    { from: "script", to: "gate" },
    { from: "script", to: "party" },
    { from: "script", to: "server" },
    { from: "gate", to: "free" },
    { from: "party", to: "free" },
    { from: "server", to: "free" },
  ]}
/>

<KeyTakeaway>Third-party scripts are the most common route to blowing a bundle budget — they are outside the repo, outside code review, and run on the main thread. Interaction gating, PartyTown, and server-side measurement each remove some or all of their cost. A governance layer (approved-vendor list + CSP) prevents unreviewed additions.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-retrieval"
  lang="en"
  questions={[
    { q: "Why do third-party scripts disproportionately inflate bundle cost compared to first-party code?", a: "Vendors ship complete feature sets regardless of which features the app uses — no tree shaking is possible. Cumulative effect: 10 scripts at 100 KB average = 1 MB. They run on the main thread unless deferred. They are added through GTM without engineering code review. First-party code goes through PR, bundle analysis, and can be split or shaken." },
    { q: "Describe the interaction gating pattern and its tradeoff.", a: "Load the third-party script only when the user performs an action that requires it (click, scroll trigger, form focus). Cost on initial load: zero. Tradeoff: first use has download+parse latency. Mitigate by prefetching the script on hover or intersection, so it is cached before the click fires." },
  ]}
/>

<Recap lang="en">
Third-party scripts regularly add 500 KB to 1 MB to every page, outside the app repo and outside code review. Three structural mitigations: interaction gating (load on trigger, zero initial cost), PartyTown Web Worker isolation (main thread freed), and server-side measurement (nothing on client). Governance via a third-party budget file and CSP enforce the allow-list at CI and browser level respectively. The next lesson covers CI enforcement: how to make the budget a hard constraint rather than a guideline.
</Recap>
