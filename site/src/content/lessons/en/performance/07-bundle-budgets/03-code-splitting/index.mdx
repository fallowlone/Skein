---
concepts:
- code-splitting
- dynamic-import
- lazy-loading
- vendor-splitting
- tree-shaking
deepensInto:
- 07-bundle-budgets/04-tree-shaking-and-compression
estMin: 16
lang: en
lessonType: topic
level: middle
order: 3
prereqs:
- 07-bundle-budgets/02-core-web-vitals
slug: 03-code-splitting
sources:
- https://web.dev/articles/granular-chunking-nextgen
- https://nextjs.org/docs/app/building-your-application/optimizing/bundle-analyzer
- https://web.dev/articles/preload-critical-assets
spiral:
- latency
status: ready
summary: 'Code splitting is the highest-leverage bundle reduction: each route downloads
  only what it needs, components load when triggered, vendor chunks cache separately
  across deploys.'
title: 'Code splitting: route-level, component-level, vendor splitting'
track: performance
unit: 07-bundle-budgets
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A dashboard app ships 900 KB of JS to every page because all routes share one bundle. The homepage needs React, a chart library, a date picker, and the admin panel — even though the homepage never shows a chart, uses the admin panel, or renders the date picker. Three routes of unneeded code travel to the user on every visit to every page.
</Hook>

<Crux>Code splitting: route chunks deliver only what the page needs. Component and vendor splits cut the rest.</Crux>

<Explanation>

## Route-level splitting

The most impactful change. Each route gets its own JavaScript chunk; navigating to `/settings` does not download `/editor`'s bundle. Every modern bundler supports this out of the box.

- **Webpack** — `splitChunks` configuration, or React Router 6 with `lazy()`.
- **Vite** — automatic per-route splitting when using a router plugin.
- **Next.js** — automatic per-page chunks in both Pages and App Router. Every file in `/pages` or `/(routes)` is a separate chunk automatically.
- **Remix** — route modules are automatically code-split; parallel data loading is built-in.

The senior pattern: every route has its own chunk. If the bundle analyzer shows that 40% of the homepage bundle is from `/admin` routes, that is route splitting not working.

## Component-level splitting

Components that are not on the critical path — modals, sidebars, drawers, admin tools, complex charts — should load only when they are needed. The primitive is `dynamic import()`.

```js
// React
const DatePicker = React.lazy(() => import('./DatePicker'));
// Render only inside a <Suspense> boundary

// Vue
const DatePicker = defineAsyncComponent(() => import('./DatePicker.vue'));
```

The rule of thumb: any component over 30 KB that is not rendered on first paint should be lazy-loaded. Common candidates: rich text editors, data visualisation libraries, map components, video players, admin panels.

A lazy-loaded component defers its download until the first render. When the user triggers it (opens a modal, navigates to a sub-route), the chunk downloads and parses on the spot. To avoid the visible delay on that first interaction, **prefetch** the chunk when the user hovers or when the trigger is nearby:

```html
<link rel="prefetch" href="/chunk-datepicker.js" />
```

Or use `React.lazy` with `import(/* webpackPrefetch: true */ './DatePicker')`.

## Vendor splitting

Third-party libraries (React, React DOM, charting libraries, utility packages) change infrequently compared to your own application code. If they are bundled with your app code, every new deploy invalidates the user's cached copy of both — the user re-downloads React even though React did not change.

Vendor splitting puts third-party libraries in a separate chunk with its own content hash. The user caches `vendor.abc123.js` indefinitely. A deploy that only changes your app code invalidates `app.def456.js` but the vendor chunk stays cached. Second visits after a deploy download only the small changed chunk.

**How to configure**: Webpack's `splitChunks.cacheGroups`, Vite's `manualChunks`, Next.js's automatic framework chunk (`framework-*.js` in `_next/static/chunks/`).

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Strategy</th>
        <th class="border border-border px-3 py-2 text-left">Mechanism</th>
        <th class="border border-border px-3 py-2 text-left">Best for</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Route-level</td>
        <td class="border border-border px-3 py-2">Each route in its own chunk via bundler or router</td>
        <td class="border border-border px-3 py-2">SaaS apps with distinct routes sharing little code</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Component-level</td>
        <td class="border border-border px-3 py-2">dynamic import() + React.lazy / defineAsyncComponent</td>
        <td class="border border-border px-3 py-2">Modals, drawers, charts, editors not on critical path</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Vendor splitting</td>
        <td class="border border-border px-3 py-2">Third-party libs in separate content-hashed chunk</td>
        <td class="border border-border px-3 py-2">Apps that deploy frequently; libraries change rarely</td>
      </tr>
    </tbody>
  </table>
</div>

## The critical path boundary

Not all lazy loading is free. Deferring a component means the first time the user opens it, the chunk downloads and parses in the foreground. This improves LCP (smaller initial bundle) but can hurt INP for that specific interaction.

The rule: keep on the critical path anything the user sees within the first 2-3 seconds of a page load. Defer everything else. If a component is below the fold, behind a click, or on a route the user visits second, it belongs in a lazy chunk.

<Inset kind="why" lang="en">
Why does vendor splitting help even if the vendor chunk is large? Cache headers. Static assets are served with `Cache-Control: public, max-age=31536000, immutable` because the filename includes a content hash. Once the user has downloaded `vendor.abc123.js`, it never downloads again unless the hash changes. Only your app code changes on every deploy.
</Inset>

<DragOrder
  id="03-drag-1"
  lessonSlug="03-code-splitting"
  lang="en"
  prompt="Order the steps of bringing an existing 1.2 MB bundle under a 200 KB per-route budget:"
  items={[
    "Open bundle analyzer and sort modules by size",
    "Identify which modules are not used on the current route",
    "Set up route-level splitting via the framework router",
    "Lazy-load large components not on the critical first-paint path",
    "Move third-party libraries to a vendor chunk with a content hash",
    "Re-run bundle analyzer and verify each route is under budget",
    "Add a CI size-limit gate to enforce the budget on future PRs",
  ]}
/>

<Quiz
  id="03-quiz-1"
  lessonSlug="03-code-splitting"
  lang="en"
  question="After lazy-loading a 100 KB chart component, LCP improves but a user reports a visible delay when they first click 'Open Chart'. What is the cause and the fix?"
  choices={[
    { label: "The chunk downloads and parses on first click. Fix: prefetch the chunk on hover or intersection so it is ready before the click fires.", correct: true },
    { label: "Lazy loading is broken for charts.", misconception: "Lazy loading works correctly — LCP improved. The issue is the first-use download cost." },
    { label: "The chart needs to be on the critical path.", misconception: "Putting it back on the critical path would restore the LCP regression. The right fix is prefetch, not eager loading." },
    { label: "The server is slow to respond with the chunk.", misconception: "Chunk delivery from CDN is fast. The delay is parse+compile on the client's CPU after download." },
  ]}
/>

<Quiz
  id="03-quiz-2"
  lessonSlug="03-code-splitting"
  lang="en"
  question="A daily-deploying Next.js app has one large bundle with React, app code, and libraries mixed. After vendor splitting, users on a second visit after a deploy report the page loads faster. Why?"
  choices={[
    { label: "The vendor chunk (React, stable libraries) is cached from before the deploy. Only the changed app chunk re-downloads. Total bytes transferred on second visit drops by 60-80%.", correct: true },
    { label: "The server got faster after deploy.", misconception: "Server speed is unchanged; the win is in what the browser already has cached." },
    { label: "The CDN revalidated more efficiently.", misconception: "CDN delivers the new chunk fine regardless. The win is that the user already has the vendor chunk in browser cache." },
    { label: "Brotli compression improved after splitting.", misconception: "Compression ratios are similar; the win is that the cached vendor chunk avoids any download at all." },
  ]}
/>

<Quiz
  id="03-quiz-3"
  lessonSlug="03-code-splitting"
  lang="en"
  question="A product engineer wants to add a rich text editor (180 KB) to the homepage. What is the senior recommendation?"
  choices={[
    { label: "Lazy-load the editor with React.lazy() and a Suspense boundary. The 180 KB downloads only when the user opens the editor. Optionally prefetch on interaction-start.", correct: true },
    { label: "Add it to the critical path — users need it.", misconception: "If users need it after they are engaged, it is not on the critical path for first paint. Lazy load and prefetch." },
    { label: "Use a lighter editor library.", misconception: "Switching libraries may be warranted, but the senior recommendation is lazy loading first regardless of library weight." },
    { label: "Block the feature until bundle size is reduced.", misconception: "The feature can ship safely with lazy loading — no bundle regression needed." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="One monolithic bundle split into an initial chunk plus on-demand chunks"
  caption="Splitting turns one all-routes bundle into a small initial route chunk loaded now, while lazy components and the vendor chunk load on demand or stay cached across deploys."
  hue="var(--d-hardware)"
  nodes={[
    { id: "mono", label: "One big bundle", sub: "900 KB all routes", col: 0, row: 1 },
    { id: "route", label: "Initial route chunk", sub: "loaded now", col: 1, row: 0 },
    { id: "lazy", label: "Lazy component chunks", sub: "on demand", col: 1, row: 1 },
    { id: "vendor", label: "Vendor chunk", sub: "cached across deploys", col: 1, row: 2 },
  ]}
  edges={[
    { from: "mono", to: "route", label: "split" },
    { from: "mono", to: "lazy" },
    { from: "mono", to: "vendor" },
  ]}
/>

<KeyTakeaway>Route-level splitting is the highest-leverage bundle change: each page only downloads what it needs. Component-level splitting defers large non-critical components. Vendor splitting makes third-party libraries cache across deploys. All three stack — a well-split app uses all three.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-retrieval"
  lang="en"
  questions={[
    { q: "Explain route-level, component-level, and vendor splitting. When is each the right choice?", a: "Route-level: each route in its own chunk — right when routes share little code and the user navigates between them. Component-level: dynamic import for large components not on the critical paint path — right for modals, editors, charts behind a click. Vendor splitting: third-party libs in a separate content-hashed chunk — right when app deploys frequently and libraries are stable, to preserve the user's cached vendor chunk across deploys." },
    { q: "A lazy-loaded component causes visible delay on first interaction. What is the fix?", a: "Prefetch the chunk: either via <link rel='prefetch'>, webpack magic comments (/* webpackPrefetch: true */), or by triggering the import() on hover/intersection before the click. The chunk will be cached by the time the user clicks." },
  ]}
/>

<Recap lang="en">
Code splitting is the most impactful bundle optimisation: route-level ensures each page pays only for its own code, component-level defers large non-critical components, vendor splitting preserves cached library chunks across frequent deploys. The primitive for the latter two is dynamic `import()`, which every modern bundler and framework wraps into `React.lazy`, `defineAsyncComponent`, or equivalent. All three strategies stack. The next lesson covers tree shaking and compression — complementary techniques that reduce bytes within each chunk.
</Recap>
