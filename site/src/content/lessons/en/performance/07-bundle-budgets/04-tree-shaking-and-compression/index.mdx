---
slug: 04-tree-shaking-and-compression
lang: en
track: performance
unit: 07-bundle-budgets
order: 4
title: "Tree shaking and compression: removing what you don't use"
summary: "Tree shaking strips unused exports at build time — but four common pitfalls silently defeat it. Brotli and gzip reduce wire bytes without touching parse cost."
estMin: 14
status: ready
lessonType: topic
level: middle
prereqs: ["07-bundle-budgets/03-code-splitting"]
deepensInto: ["07-bundle-budgets/05-third-party-scripts"]
spiral: ["latency"]
concepts: ["tree-shaking", "dead-code-elimination", "brotli", "gzip", "esm", "side-effects"]
sources:
  - https://web.dev/articles/granular-chunking-nextgen
  - https://v8.dev/blog/cost-of-javascript-2019
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
A developer imports one function from lodash: `import { debounce } from 'lodash'`. The bundle analyzer shows 70 KB of lodash in the output. They expected 2 KB. The import looked right — but the module format was wrong.
</Hook>

<Crux>Tree shaking drops unused exports: requires ESM, no side effects, correct config. Compression saves 60-80% wire bytes but not parse cost.</Crux>

<Explanation>

## How tree shaking works

Tree shaking is the bundler's ability to detect and remove exports that are never imported. If a module exports 200 functions and the app imports only one, the other 199 are "dead code" — they should not appear in the bundle.

This works because ES module syntax (`import` / `export`) is statically analysable: the bundler can trace import chains at build time without executing code. CommonJS (`require()`) computes exports at runtime; the bundler cannot know statically which ones are used, so it includes everything.

```js
// Tree-shakeable (ESM) — bundler sees at build time which exports are used
import { debounce } from 'lodash-es';  // Only debounce in the bundle

// NOT tree-shakeable (CJS) — bundler must include all exports
import { debounce } from 'lodash';      // All 70 KB of lodash in the bundle
```

To enable tree shaking: use the library's ESM build (often listed as `module` in `package.json`), configure the bundler to use it (`resolve.mainFields: ['module', 'main']` in Webpack, automatic in Vite), and ensure your own code uses named exports.

## Four pitfalls that defeat tree shaking

**Pitfall 1 — side effects at module scope.** A module that sets `window.foo = bar` or registers a global when imported cannot be tree-shaken — the bundler must preserve the side effect even if no export is consumed. Fix: annotate the package as `"sideEffects": false` in `package.json` if it has no module-scope side effects, or use `/* #__PURE__ */` annotations on specific calls.

**Pitfall 2 — CommonJS re-export.** Even if your code is ESM, a library that compiles to CJS cannot be tree-shaken. The bundler wraps the whole module. Check the library's `package.json`: if there is no `"module"` or `"exports"` field pointing to an ESM build, the whole library ships. Switch to an ESM-first alternative or use cherry-picked imports (`lodash/debounce` instead of `lodash`).

**Pitfall 3 — `export *` re-exports.** `export * from './module'` re-exports everything. If even one consumer imports one export from this barrel, the entire module is kept. Fix: use explicit named re-exports in barrel files.

**Pitfall 4 — dynamic import with a variable.** `await import('./module')` is tree-shakeable; `await import(name)` where `name` is a runtime variable is not — the bundler cannot know which module will be requested at build time.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Pitfall</th>
        <th class="border border-border px-3 py-2 text-left">Why it defeats tree shaking</th>
        <th class="border border-border px-3 py-2 text-left">Fix</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Side effects at module scope</td>
        <td class="border border-border px-3 py-2">Bundler must preserve global mutation</td>
        <td class="border border-border px-3 py-2">sideEffects: false or #__PURE__ annotation</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">CJS library</td>
        <td class="border border-border px-3 py-2">Exports computed at runtime</td>
        <td class="border border-border px-3 py-2">Use ESM build or cherry-pick imports</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">export * barrel files</td>
        <td class="border border-border px-3 py-2">One consumer keeps entire module</td>
        <td class="border border-border px-3 py-2">Explicit named re-exports</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Dynamic import with variable</td>
        <td class="border border-border px-3 py-2">Module unknown at build time</td>
        <td class="border border-border px-3 py-2">Use string literals in import()</td>
      </tr>
    </tbody>
  </table>
</div>

## Compression: gzip vs brotli

Compression reduces the bytes transferred over the wire. Text-based assets (JS, CSS, JSON, SVG) compress 60-80% with gzip and 70-90% with brotli. The same 500 KB uncompressed JS file becomes roughly 200 KB gzip'd or 160 KB brotli'd.

**Important constraint**: compression reduces download cost only. Parse, compile, and execute cost is determined by the **uncompressed** size of the code. Switching from gzip to brotli saves ~10-25% more on the wire — worth doing, but it does not address the CPU bottleneck.

**Static vs dynamic compression**: Static compression generates the compressed file at build time and serves it directly with `Content-Encoding: br`. Cost at request time: zero. Dynamic compression compresses on the fly per request: ~5-30 ms for brotli, ~1-5 ms for gzip. Senior pattern: static brotli for all static assets (JS, CSS); dynamic gzip for HTML responses that vary per user.

```bash
# Verify brotli is served
curl -sI -H "Accept-Encoding: br" https://your-app.com/app.js | grep content-encoding
# Expected: content-encoding: br
```

<Inset kind="why" lang="en">
Why does the uncompressed size determine parse cost? The browser decompresses the file before parsing. The JS engine receives and parses the original bytes, not the compressed form. Gzip and brotli are transport-layer optimisations — they save network bytes but the CPU work is the same.
</Inset>

<Quiz
  id="04-quiz-1"
  lessonSlug="04-tree-shaking-and-compression"
  lang="en"
  question="A library ships only a CJS build. The team imports one utility function. What happens in the bundle?"
  choices={[
    { label: "The entire library ships — CJS exports are computed at runtime so the bundler cannot eliminate unused exports.", correct: true },
    { label: "Only the imported function ships — bundlers are smart enough to handle CJS.", misconception: "Modern bundlers do partial tree shaking on some CJS patterns, but it is unreliable. ESM is required for reliable dead-code elimination." },
    { label: "The library is excluded entirely — it is a peer dep.", misconception: "Peer deps are still bundled unless explicitly externalised; and even then they must be loaded by the runtime." },
    { label: "The library is minified to just the imported function.", misconception: "Minification removes whitespace and renames variables; it does not remove unused exports from a CJS module." },
  ]}
/>

<Quiz
  id="04-quiz-2"
  lessonSlug="04-tree-shaking-and-compression"
  lang="en"
  question="A team switches from gzip to static brotli for their JS assets. Transfer size drops 15%. LCP improves 40 ms on mobile. Is the parse cost affected?"
  choices={[
    { label: "No. Parse cost is determined by the uncompressed size. Brotli reduces wire bytes but the browser decompresses before parsing — CPU cost is unchanged.", correct: true },
    { label: "Yes — smaller transferred bytes means faster parse.", misconception: "Compression saves network bytes, not CPU bytes. The engine parses decompressed code." },
    { label: "Yes — brotli uses a dictionary that speeds up V8 parse.", misconception: "Brotli is a transport encoding; V8 receives and parses the raw decompressed bytes regardless of how they were transferred." },
    { label: "No — parse cost is determined by gzip size.", misconception: "Parse cost is determined by the uncompressed (original) size, not any compressed form." },
  ]}
/>

<Quiz
  id="04-quiz-3"
  lessonSlug="04-tree-shaking-and-compression"
  lang="en"
  question="A module has `window.analytics = createAnalytics()` at module scope and is marked sideEffects: true. The app only imports one function from it. What happens?"
  choices={[
    { label: "The entire module is included. The side-effect code at module scope must run, so the bundler keeps the whole thing to preserve correctness.", correct: true },
    { label: "The side-effect code is removed and only the imported function ships.", misconception: "Bundlers preserve side effects to ensure correctness. Removing a global assignment could break code that depends on it." },
    { label: "The module is automatically split into the side-effect portion and the export portion.", misconception: "Bundlers do not split CJS or side-effectful modules this way — it is the developer's responsibility to refactor." },
    { label: "The sideEffects: true marks the module as safe to shake.", misconception: "sideEffects: true means the module HAS side effects and must NOT be shaken away. sideEffects: false signals it is safe to remove if unused." },
  ]}
/>

</Explanation>

<KeyTakeaway>Tree shaking removes unused ESM exports at build time — defeated by CJS imports, module-scope side effects, barrel-file export*, and dynamic variable imports. Brotli saves 10-25% more wire bytes than gzip but does not reduce parse cost. Both are useful; neither replaces code splitting.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-retrieval"
  lang="en"
  questions={[
    { q: "Why does importing from a CJS library ship the whole library even if you only use one function?", a: "CJS exports are computed at runtime via require(). The bundler cannot statically determine which exports are used at build time, so it must include the entire module to guarantee correctness. ESM static imports let the bundler trace exactly which exports are consumed." },
    { q: "A library is marked sideEffects: false but still appears fully in the bundle. What are two likely causes?", a: "1) The library ships only a CJS build — sideEffects: false only helps with ESM modules that are also tree-shakeable by the bundler. 2) A barrel file re-exports everything with 'export *' and one of the exports is used — the whole module is kept. Fix: explicit named re-exports in barrel files, and ensure the library has an ESM build." },
    { q: "What is the difference in effect between gzip and code splitting on parse cost?", a: "Gzip reduces transfer bytes but not parse cost — the browser decompresses first, then parses the original size. Code splitting reduces the actual code sent to the route, reducing both transfer and parse cost. Code splitting addresses the CPU bottleneck; compression addresses only the network bottleneck." },
  ]}
/>

<Recap lang="en">
Tree shaking relies on ESM static analysis to remove unused exports. The four pitfalls — CJS modules, module-scope side effects, barrel export*, and dynamic variable imports — silently defeat it and are worth auditing in every build. Compression (brotli preferred over gzip) reduces wire bytes but leaves parse cost unchanged. Together, tree shaking and compression complement code splitting: splitting reduces what a route downloads; tree shaking removes dead code within each chunk; compression shrinks the transfer. The next lesson covers third-party scripts — the category that most commonly blows through carefully set budgets.
</Recap>
