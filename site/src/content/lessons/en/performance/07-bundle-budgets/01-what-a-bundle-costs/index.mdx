---
concepts:
- bundle-budget
- js-parse-cost
- mobile-cpu-gap
- time-to-interactive
deepensInto:
- 07-bundle-budgets/02-core-web-vitals
estMin: 12
lang: en
lessonType: topic
level: junior
order: 1
prereqs:
- performance/01-profile-first/01-why-profile-first
- performance/02-hot-paths/01-what-makes-a-hot-path
slug: 01-what-a-bundle-costs
sources:
- https://v8.dev/blog/cost-of-javascript-2019
- https://web.dev/articles/vitals
- https://developer.chrome.com/docs/lighthouse/performance/
spiral:
- latency
status: ready
summary: A 1 MB JS bundle is not a 1 MB cost — it is four chained CPU phases that
  hit mid-range mobile 4-8x harder than your laptop.
title: 'What a bundle actually costs: download, parse, compile, execute'
track: performance
unit: 07-bundle-budgets
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A developer ships a new feature on a Friday. On their MacBook the page loads in 800 ms — feels fine. By Monday, bounce rate is up 20%. A colleague tests on a mid-range Android phone and watches the page sit blank for 8 seconds. The feature was fine. The bundle was not.
</Hook>

<Crux>JS bytes = parse + compile + execute on user's CPU. Mobile is 4-8x slower than dev laptop. Budget or pay in bounce rate.</Crux>

<Explanation>

## Four phases, one cost

When a browser downloads a JavaScript file, it does not just "load" the code. It runs four sequential phases before a single user interaction is possible.

**Download** — the bytes travel over the network. On a 4G connection, 200 KB gzip'd takes 150-500 ms depending on signal quality.

**Parse** — the JS engine walks every byte and builds an abstract syntax tree (AST). V8 parses at roughly 1-2 MB/sec on a fast laptop CPU, but only 200-400 KB/sec on a median Android device. A 500 KB uncompressed bundle takes about 1.5-2 seconds to parse on mid-range mobile.

**Compile** — the engine converts the AST to bytecode, then to optimised machine code. V8 uses a tiered pipeline (Ignition → Sparkplug → TurboFan). Compile cost grows roughly linearly with how much code actually runs at startup.

**Execute** — the bundle's top-level code runs synchronously: framework initialisation, store creation, route registration. For a typical React + Redux app this blocks the main thread for 100-500 ms.

The full chain on mid-range mobile for a 500 KB uncompressed bundle: download 800 ms + parse 1.5 s + compile 300 ms + execute 400 ms = roughly 3 seconds before the user can click anything. Each phase is a separate lever; none of them helps unless you address it directly.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Phase</th>
        <th class="border border-border px-3 py-2 text-left">Bottleneck</th>
        <th class="border border-border px-3 py-2 text-left">Mid-range mobile cost (500 KB uncompressed)</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Download</td>
        <td class="border border-border px-3 py-2">Network bandwidth</td>
        <td class="border border-border px-3 py-2">~800 ms on 4G</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Parse</td>
        <td class="border border-border px-3 py-2">CPU — V8 walks every byte</td>
        <td class="border border-border px-3 py-2">~1.5-2 s</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Compile</td>
        <td class="border border-border px-3 py-2">CPU — JIT tiers</td>
        <td class="border border-border px-3 py-2">~300 ms</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Execute</td>
        <td class="border border-border px-3 py-2">CPU — single-threaded startup</td>
        <td class="border border-border px-3 py-2">~400 ms</td>
      </tr>
    </tbody>
  </table>
</div>

## Why "works on my laptop" fails

A developer's MacBook M-series has a single-thread CPU speed roughly 4-8x faster than a median Android phone (Snapdragon 6 or 7-series equivalent). A 5G or Wi-Fi connection helps with download — but parse, compile, and execute happen entirely on the user's CPU. Faster network does not move that needle at all.

Most users in global markets are on mid-range or older hardware. Teams that test only on flagship phones or development machines systematically underestimate real-user experience. Chrome DevTools has a 4x CPU throttling mode that approximates mid-range Android; using it for every performance check catches the gap before it reaches production.

## The bundle-budget discipline

A **bundle budget** is a maximum number of bytes (typically gzip'd JS, CSS, images split separately) that a route is allowed to ship, enforced at CI time. Without a budget, bundle size grows by accretion: each engineer adds a library, each library adds a few KB, no single PR is to blame, but after six months the bundle has doubled. With a budget, every PR that pushes past the cap fails CI — the team either justifies the addition or finds a smaller alternative.

The budget converts performance from an intention into a constraint. It does for client-side bytes what an SLO does for server-side latency: makes it measurable, enforced, and owned.

<Inset kind="why" lang="en">
Why does a 1 MB JS bundle cost more than a 1 MB image? An image is downloaded, decoded by the GPU, and painted. JS is downloaded, then parsed, compiled, and executed on the CPU — three extra phases that are slow, sequential, and block the main thread. Per byte, JS is roughly 5-10x more expensive than an image on mid-range mobile.
</Inset>

<Quiz
  id="01-quiz-1"
  lessonSlug="01-what-a-bundle-costs"
  lang="en"
  question="A 500 KB uncompressed JS bundle loads fine in 400 ms on a dev laptop. Estimate the likely Time to Interactive on a mid-range Android on 4G."
  choices={[
    { label: "2-4 seconds — parse and compile add 4-8x the laptop cost on top of slower download.", correct: true },
    { label: "About the same — mobile CPUs have caught up.", misconception: "Mid-range mobile single-thread speed is still 4-8x slower than an M-series laptop as of 2024." },
    { label: "Longer only because of slower network.", misconception: "Network affects download only; parse, compile, and execute are CPU-bound and 4-8x slower on mobile." },
    { label: "2x slower at most.", misconception: "The gap is larger — parse alone on mid-range mobile for 500 KB uncompressed is 1.5-2 seconds." },
  ]}
/>

<DragOrder
  id="01-drag-1"
  lessonSlug="01-what-a-bundle-costs"
  lang="en"
  prompt="Order the JS bundle lifecycle from network request to first user interaction:"
  items={[
    "Browser requests the JS file from the server",
    "Server returns the file (often gzip or brotli compressed)",
    "Browser decompresses and receives bytes",
    "JS engine parses bytes into an abstract syntax tree (AST)",
    "JS engine compiles the AST into bytecode and then optimised machine code",
    "Browser executes top-level code: imports, side effects, framework init",
    "App reaches interactive state — user can click",
  ]}
/>

<Quiz
  id="01-quiz-2"
  lessonSlug="01-what-a-bundle-costs"
  lang="en"
  question="A team switches from gzip to brotli compression and the bundle goes from 180 KB to 150 KB transferred. LCP improves by 50 ms. Is the bundle problem solved?"
  choices={[
    { label: "No. Brotli reduces download only. Parse, compile, and execute cost is set by the uncompressed size — that is unchanged. The team needs to reduce actual code shipped.", correct: true },
    { label: "Yes — smaller download means faster page.", misconception: "Download is only one of four phases. The uncompressed size on disk determines parse and compile cost." },
    { label: "Yes — brotli is the best optimisation available.", misconception: "Brotli is useful but saves 10-25% on top of gzip. Code splitting saves 50-90% of what a given route downloads." },
    { label: "Partially — the 50 ms improvement is all that was possible.", misconception: "50 ms is the download win. Parse+compile+execute savings come from reducing shipped bytes, not compressing them." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="The four sequential phases a JS bundle costs before a page is interactive"
  caption="One network phase, then three CPU phases on the user's device. Only download depends on the network; parse, compile, and execute run on the main thread."
  hue="var(--d-hardware)"
  perRow={4}
  nodes={[
    { id: "dl", label: "Download", sub: "network ~800 ms" },
    { id: "parse", label: "Parse", sub: "CPU ~1.5-2 s" },
    { id: "compile", label: "Compile", sub: "CPU ~300 ms" },
    { id: "exec", label: "Execute", sub: "CPU ~400 ms" },
  ]}
  edges={[
    { from: "dl", to: "parse" },
    { from: "parse", to: "compile" },
    { from: "compile", to: "exec", label: "interactive" },
  ]}
/>

<KeyTakeaway>A JS bundle costs four CPU phases on the user's device. Mid-range mobile is 4-8x slower than a dev laptop — parse alone for 500 KB takes 1.5 s. Bundle budgets enforce a per-route byte cap at CI to prevent size from drifting upward unnoticed.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-retrieval"
  lang="en"
  questions={[
    { q: "Name the four phases a JS bundle goes through before a page is interactive.", a: "Download (network), parse (V8 walks every byte into an AST), compile (JIT converts AST to bytecode/machine code), execute (top-level code runs on the main thread)." },
    { q: "Why does a 4G upgrade not fix parse and compile times?", a: "Parse and compile happen on the user's CPU after bytes arrive. Network speed affects download only; parse and compile are CPU-bound and 4-8x slower on mid-range mobile than a dev laptop regardless of network." },
    { q: "What is a bundle budget and what does it prevent?", a: "A maximum byte cap per route enforced at CI. It prevents bundle size from growing by accretion — each PR that exceeds the cap fails, forcing the team to split, defer, or justify the cost explicitly." },
  ]}
/>

<Recap lang="en">
A JavaScript bundle triggers four sequential CPU phases: download, parse, compile, execute. The first is network-bound; the other three are CPU-bound and run 4-8x slower on a median Android than on a dev laptop. A 500 KB uncompressed bundle can cost 3 seconds of blocked main thread on mid-range mobile — invisible in development. Bundle budgets make the cost a per-route constraint enforced at CI, catching drift before it reaches users. The next lesson connects these costs to Google's Core Web Vitals and the concrete thresholds that define "good" vs "poor" for LCP, INP, and CLS.
</Recap>
