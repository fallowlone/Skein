---
concepts:
- LCP
- INP
- CLS
- core-web-vitals
- rum
- lighthouse
deepensInto:
- 07-bundle-budgets/03-code-splitting
estMin: 14
lang: en
lessonType: topic
level: middle
order: 2
prereqs:
- 07-bundle-budgets/01-what-a-bundle-costs
slug: 02-core-web-vitals
sources:
- https://web.dev/articles/vitals
- https://web.dev/articles/inp
- https://web.dev/articles/optimize-vitals-lighthouse
- https://developer.chrome.com/docs/lighthouse/performance/
spiral:
- latency
status: ready
summary: Google''''s three field metrics quantify what users feel. Bundle size is
  the dominant lever for LCP and INP — this lesson maps bytes to thresholds.
title: 'Core Web Vitals: LCP, INP, and CLS'
track: performance
unit: 07-bundle-budgets
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import interactiveBudget from "./infographics/interactive-budget.svg?raw";

<Hook>
A team spends two sprints optimising their API response times. Server P99 drops from 400 ms to 90 ms. Google Search Console shows LCP is still "Poor" for 40% of mobile users. The API was not the bottleneck. The 800 KB JS bundle blocking the main thread was.
</Hook>

<Crux>LCP, INP, CLS: Google's field metrics for user perception. Heavy JS tanks LCP and INP. Bundle budget is the lever.</Crux>

<Explanation>

## The three metrics and what they measure

Before you can fix a performance problem, you need a number that tells you it exists — and whether the fix actually worked. Core Web Vitals are those numbers: they are field metrics — collected from real Chrome users via the CrUX dataset — that quantify distinct dimensions of user experience.

**LCP (Largest Contentful Paint)** — the time from navigation start to when the largest visible element above the fold is rendered. This is usually a hero image, an H1, or a background block. Target: under 2.5 s is "Good"; 2.5-4.0 s is "Needs Improvement"; above 4.0 s is "Poor". Heavy JavaScript that blocks the main thread delays when the browser can paint — LCP suffers directly.

**INP (Interaction to Next Paint)** — introduced in March 2024, replacing FID. INP measures the worst-case latency from any user input (click, keypress, tap) to the next visual update during the full page lifetime. Target: under 200 ms is "Good"; 200-500 ms is "Needs Improvement"; above 500 ms is "Poor". Long JS execution tasks on the main thread — including large bundle parse and execution — drive INP up.

**CLS (Cumulative Layout Shift)** — how much visible content shifts unexpectedly during load. Target: under 0.1 is "Good"; 0.1-0.25 is "Needs Improvement"; above 0.25 is "Poor". Bundle size does not directly cause CLS; layout shifts come from unsized images, late-loading fonts, and JS that inserts above-fold content after initial paint.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Metric</th>
        <th class="border border-border px-3 py-2 text-left">Good</th>
        <th class="border border-border px-3 py-2 text-left">Needs Improvement</th>
        <th class="border border-border px-3 py-2 text-left">Poor</th>
        <th class="border border-border px-3 py-2 text-left">Bundle lever?</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">LCP</td>
        <td class="border border-border px-3 py-2">&lt; 2.5 s</td>
        <td class="border border-border px-3 py-2">2.5 – 4.0 s</td>
        <td class="border border-border px-3 py-2">&gt; 4.0 s</td>
        <td class="border border-border px-3 py-2">Strong — JS parse/execute blocks paint</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">INP</td>
        <td class="border border-border px-3 py-2">&lt; 200 ms</td>
        <td class="border border-border px-3 py-2">200 – 500 ms</td>
        <td class="border border-border px-3 py-2">&gt; 500 ms</td>
        <td class="border border-border px-3 py-2">Strong — long tasks from large bundles</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">CLS</td>
        <td class="border border-border px-3 py-2">&lt; 0.1</td>
        <td class="border border-border px-3 py-2">0.1 – 0.25</td>
        <td class="border border-border px-3 py-2">&gt; 0.25</td>
        <td class="border border-border px-3 py-2">Indirect — JS that inserts above-fold content</td>
      </tr>
    </tbody>
  </table>
</div>

## The 100-170 KB rule of thumb

Google's Web Fundamentals guidance recommends keeping the initial JS under 100-170 KB gzip'd for fast first interactive on mid-range mobile. The math: 170 KB gzip'd decompresses to roughly 500 KB. V8 parses at 200-400 KB/sec on mid-range Android, so parse alone takes 1.5 s. Add compile and execute and the total is 2-2.5 s CPU cost. Add a 4G download at ~500 ms and you arrive at roughly 3 s to interactive — the upper bound that keeps LCP under 2.5 s. Every additional 100 KB beyond that adds roughly 300-500 ms to LCP on the median device.

<Infographic
  svg={interactiveBudget}
  label="Column chart of the time-to-interactive budget for a 170 KB gzip'd JS bundle on mid-range mobile: 4G download 0.5 s, parse 1.5 s, compile and execute 0.75 s — the CPU stages dominate."
  caption="The CPU cost of parsing and executing JS (~2.25 s) dwarfs the 0.5 s download — which is why bundle size, not the network, sets the LCP floor."
  hue="var(--d-infra)"
/>

Different routes can carry different budgets: a marketing homepage should be tight (50-100 KB), a dashboard medium (150-250 KB), admin tools more relaxed (300-500 KB). The homepage carries the tightest cap because it is the conversion entry point.

## Three tools: Lighthouse, PageSpeed Insights, RUM

These tools measure the same metrics but from different vantage points.

**Lighthouse** runs a synthetic test — DevTools or CLI — with CPU throttling on the tester's machine. Reliable for catching regressions in PRs. Not field data; it represents a single synthetic run.

**PageSpeed Insights** runs a Google-managed Lighthouse test plus overlays field data from the CrUX dataset — real aggregated metrics from Chrome users on your domain. Useful for public-facing analysis. Updates once per day.

**Real User Monitoring (RUM)** — your own sampling of real users via the Web Vitals JS library, Sentry, Datadog RUM, or similar. Most accurate, but requires setup and budget. Senior pattern: Lighthouse in CI for catching regressions before merge; RUM in production for regressions that CI misses; PageSpeed Insights quarterly for cross-reference. Each catches a different class of problem; none replaces the others.

<Inset kind="why" lang="en">
INP replaced FID (First Input Delay) in March 2024. FID measured only the first interaction; INP measures the worst across the full session. A page where the first click is fast but the fifth is sluggish scored "Good" on FID and "Poor" on INP. The change makes it harder to game the metric with a fast handler for the first event while hiding slow handlers everywhere else.
</Inset>

<Quiz
  id="02-quiz-1"
  lessonSlug="02-core-web-vitals"
  lang="en"
  question="A team's P75 LCP is 3.2 s on mobile. Server TTFB is 80 ms. Most likely cause?"
  choices={[
    { label: "Client-side JS parse and execute blocking the main thread — TTFB is fast, so the problem is on the client. Reducing the JS bundle is the primary lever.", correct: true },
    { label: "Slow server response.", misconception: "TTFB is 80 ms which is fast. The server is not the bottleneck." },
    { label: "Images are too large.", misconception: "Images affect LCP if the largest element is an image — but the most common cause of LCP past 2.5 s with fast TTFB is JS blocking paint." },
    { label: "CDN is misconfigured.", misconception: "With TTFB at 80 ms, CDN delivery is working. The client-side processing chain is the bottleneck." },
  ]}
/>

<Quiz
  id="02-quiz-2"
  lessonSlug="02-core-web-vitals"
  lang="en"
  question="A route's INP is 380 ms. The bundle is 900 KB uncompressed. After code splitting the bundle to 200 KB for this route, INP drops to 150 ms. What improved and why?"
  choices={[
    { label: "Reduced JS execution time on the main thread. Fewer bytes to parse and compile means shorter long tasks — each task under 50 ms keeps the main thread responsive to input.", correct: true },
    { label: "The server started responding faster.", misconception: "INP is entirely client-side — it measures input to next paint, not server round-trips." },
    { label: "The images were smaller.", misconception: "INP is about JS execution on the main thread, not image size." },
    { label: "Brotli compression reduced parse time.", misconception: "Compression reduces download bytes; parse time is determined by uncompressed size. Code splitting reduced actual bytes executed at route load." },
  ]}
/>

<Quiz
  id="02-quiz-3"
  lessonSlug="02-core-web-vitals"
  lang="en"
  question="Why does CLS (Cumulative Layout Shift) NOT improve much from reducing JS bundle size?"
  choices={[
    { label: "CLS is caused by unsized images, late-loading fonts, and content inserted dynamically above the fold — not by JS parse time. Reserve space for images, use font-display: swap, avoid above-fold JS insertion.", correct: true },
    { label: "CLS and JS bundle size are directly correlated.", misconception: "CLS is about layout shifts, not CPU parse cost. Different root cause, different fix." },
    { label: "CLS can only be fixed server-side.", misconception: "CLS is fixed by client-side layout discipline — sized images, reserved space, font-display." },
    { label: "CLS measures paint time, not shift.", misconception: "CLS measures the magnitude of unexpected layout shifts, scored by fraction of viewport shifted times distance moved." },
  ]}
/>

</Explanation>

<StackDiagram
  label="The three Core Web Vitals and their 'Good' thresholds"
  caption="LCP and INP are driven by JS bundle size; CLS has separate layout causes."
  hue="var(--d-hardware)"
  layers={[
    { label: "LCP — largest contentful paint", note: "Good < 2.5 s" },
    { label: "INP — interaction to next paint", note: "Good < 200 ms" },
    { label: "CLS — cumulative layout shift", note: "Good < 0.1" },
  ]}
/>

<KeyTakeaway>LCP under 2.5 s and INP under 200 ms are the "Good" thresholds. Each 100 KB added to a route's JS adds ~300-500 ms to LCP on mid-range mobile. CLS is a layout problem, not a bundle problem. Lighthouse catches regressions in CI; RUM catches what CI misses in production.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-retrieval"
  lang="en"
  questions={[
    { q: "What do LCP, INP, and CLS each measure? Give the 'Good' threshold for each.", a: "LCP: time to render the largest above-fold element. Good: under 2.5 s. INP: worst-case latency from user input to next paint. Good: under 200 ms. CLS: cumulative score of unexpected layout shifts. Good: under 0.1." },
    { q: "Why does a fast server TTFB (80 ms) not guarantee a good LCP?", a: "TTFB covers server processing and first byte delivery. LCP is measured to when the largest element is rendered. Heavy JS that blocks the main thread after bytes arrive can delay paint by seconds, independently of TTFB." },
    { q: "What is the difference between Lighthouse and RUM for catching CWV regressions?", a: "Lighthouse is a synthetic test run locally with throttling — good for PR-time checks but not real users. RUM samples actual Chrome users on real devices and networks. Lighthouse can miss regressions that only appear on specific device classes or geographies; RUM catches them after deploy." },
  ]}
/>

<Recap lang="en">
Core Web Vitals are field metrics collected from real Chrome users. LCP and INP both improve as JS bundle size decreases — parse and execute time set the floor for how fast a page can paint and respond. The 100-170 KB gzip'd rule of thumb maps to the math: each 100 KB beyond that adds 300-500 ms to LCP on mid-range mobile. CLS has its own separate root causes (unsized images, fonts, dynamic insertion). Lighthouse catches regressions pre-merge; RUM catches production drift. The next lesson covers code splitting — the primary technique for reducing per-route bundle size. Now when you see LCP stuck in the "Poor" band despite fast server TTFB, you know exactly where to look — and which metric tells you when you've fixed it.
</Recap>
