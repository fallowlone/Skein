---
concepts:
- four-phase-cost
- splitting-strategies
- tree-shaking
- third-party-mitigation
- ci-and-rum
estMin: 14
lang: en
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://v8.dev/blog/cost-of-javascript-2019
- https://web.dev/articles/vitals
- https://web.dev/articles/granular-chunking-nextgen
status: ready
summary: Free-recall prompts across the bundle-budgets unit. Answer each in your own words first, then reveal the model answer and compare.
title: 'Bundle budgets: free-recall review'
track: performance
unit: 07-bundle-budgets
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory before you open the model answer — the effort of recall is what makes the material stick.
</Hook>

<Goal lang="en">
Reconstruct the unit's core mechanisms — the four-phase cost model, the splitting hierarchy, tree-shaking pitfalls, third-party mitigations, and the CI-plus-RUM enforcement loop — without looking back at the lessons.
</Goal>

<RetrievalDrawer
  client:visible
  id="07-bundle-budgets-short-retrieval"
  lang="en"
  questions={[
    { q: "Name the four phases a JS bundle goes through before interactive, and say which are CPU-bound. Why does 'works on my laptop' fail?", a: "Download (network), parse (the engine walks every byte into an AST), compile (the JIT turns the AST into bytecode then machine code), and execute (top-level code runs on the main thread). Parse, compile, and execute are CPU-bound; only download is network-bound. A dev M-series laptop has 4–8× the single-thread speed of a mid-range Android, so the three CPU phases that dominate a large bundle are invisible in development. A 500 KB uncompressed bundle can parse in ~1.5 s and cost ~3 s to interactive on mid-range mobile — fine on the laptop, an 8 s blank screen for the user. Always test with 4× CPU throttling." },
    { q: "Give the 'Good' thresholds for LCP, INP, and CLS, and say which the bundle is a lever for.", a: "LCP (largest above-fold paint) Good < 2.5 s; INP (worst input-to-next-paint over the session, replaced FID in March 2024) Good < 200 ms; CLS (cumulative unexpected layout shift) Good < 0.1. Bundle size is a strong lever for LCP and INP — both are gated by main-thread JS parse and execute, and each extra 100 KB adds roughly 300–500 ms to LCP on mid-range mobile. It is NOT a lever for CLS: layout shift comes from unsized images, late-loading fonts, and above-fold dynamic insertion, fixed by reserving space, sized media, and font-display: swap." },
    { q: "Explain route-level, component-level, and vendor splitting, and when each is the right choice.", a: "Route-level: each route gets its own chunk so navigating to /settings doesn't download /editor — the highest-leverage change, right when routes share little code. Component-level: dynamic import() (React.lazy, defineAsyncComponent) for large items not on the critical first-paint path — modals, charts, editors behind a click; rule of thumb, lazy-load anything > 30 KB not rendered on first paint, and prefetch on hover to hide first-use latency. Vendor splitting: stable third-party libs in a separate content-hashed chunk so a deploy that only changes app code leaves the user's cached vendor chunk intact — right for frequently-deploying apps. All three stack." },
    { q: "Why does importing one function from a CommonJS library ship the whole library, and what are the four pitfalls that defeat tree shaking?", a: "CJS exports are computed at runtime via require(), so the bundler cannot statically determine which are used and must include the whole module for correctness; ESM's static import/export lets it trace exactly what is consumed. The four pitfalls: (1) module-scope side effects (e.g. window.x = ...), which the bundler must preserve — fix with sideEffects: false or /* #__PURE__ */; (2) a CJS-only library — use its ESM build or cherry-pick (lodash-es, or lodash/debounce); (3) `export *` barrel files, where one used export keeps the whole module — use explicit named re-exports; (4) dynamic import with a runtime-variable specifier, which the bundler cannot resolve at build time — use string literals." },
    { q: "Why are third-party scripts the most common way to blow a bundle budget, and what are the three structural mitigations?", a: "They are outside the repo and outside code review (often added via GTM by non-engineering teams), cannot be tree-shaken because you don't control the source (vendors ship full feature sets), accumulate fast (10 scripts × ~100 KB = 1 MB), and run on the main thread by default — and a first-party size-limit gate never sees them because they load from external domains at runtime. Three mitigations: interaction gating (load on click/scroll trigger, zero initial cost, prefetch on hover to hide first-use latency); Web Worker isolation via PartyTown (runs them off the main thread through a service-worker proxy); and server-side measurement (fire events from the API, ship nothing to the browser). Govern with a separate third-party budget file plus a CSP script-src allow-list." },
    { q: "Why is a bundle budget without a CI gate just an aspiration, and what is the full PR-merge-production enforcement stack? Include the anti-drift mechanism.", a: "Without a gate, the budget erodes one reasonable-looking PR at a time and is back to its old size in months with no single PR to blame. The stack: at PR time, size-limit (or bundlewatch) runs after build and fails the PR if any per-route gzip'd limit is exceeded, posting a delta comment; on merge to main, Lighthouse CI runs a full synthetic CWV audit; in production, RUM tracks P75 LCP/INP per route per device class and alerts on > 20% regression — catching device classes and slow accretion that synthetic tests miss. Anti-drift: a budget-raise protocol (documented justification + performance-lead sign-off, recorded in perf-budgets.json) plus a quarterly ratchet that tightens the budget ~10%, forcing old code out when new code comes in." },
  ]}
/>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the unit's spine: bytes are four CPU phases that hit mobile 4–8× harder than your laptop; LCP and INP move with shipped JS while CLS has its own causes; the reduction hierarchy is split, then shake, with compression a transport-only finisher; third-party scripts are the silent killer that needs its own budget and CSP; and none of it survives without a CI gate, RUM in production, and a ratcheting budget-raise protocol against drift.
</Recap>
