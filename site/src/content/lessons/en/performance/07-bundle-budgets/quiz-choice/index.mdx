---
concepts:
- four-phase-cost
- core-web-vitals
- code-splitting
- tree-shaking
- third-party-scripts
- ci-enforcement
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://v8.dev/blog/cost-of-javascript-2019
- https://web.dev/articles/vitals
- https://web.dev/articles/granular-chunking-nextgen
status: ready
summary: Multiple-choice synthesis across the bundle-budgets unit — the four-phase cost model, Core Web Vitals levers, splitting vs compression, third-party tax, and CI enforcement.
title: 'Bundle budgets: multiple-choice review'
track: performance
unit: 07-bundle-budgets
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each mirrors a call you make in a real perf review — not a definition to recite, but which lever actually moves the number under load.
</Hook>

<Goal lang="en">
Confirm you can connect the four-phase cost model, Core Web Vitals thresholds, the splitting/tree-shaking/compression hierarchy, the third-party tax, and CI enforcement — the synthesis the individual lessons built toward.
</Goal>

<Quiz
  id="07-bundle-budgets-choice-1"
  lessonSlug="07-bundle-budgets"
  lang="en"
  question="Team A ships brotli instead of gzip and cuts transfer from 180 KB to 150 KB. Team B code-splits the same route from 600 KB to 200 KB uncompressed. Both report LCP wins. Which team actually reduced CPU cost, and why?"
  choices={[
    { label: "Team B. Parse, compile, and execute scale with uncompressed size; the browser decompresses before parsing, so brotli only shrinks the download phase. Splitting removes bytes the CPU never has to parse.", correct: true },
    { label: "Both equally — fewer bytes on the wire always means less CPU work.", misconception: "Compression is a transport-layer win only. The engine parses the decompressed bytes, so the CPU cost is set by uncompressed size — unchanged by brotli." },
    { label: "Team A — brotli's dictionary speeds up V8's parser.", misconception: "Brotli is an encoding; V8 receives raw decompressed bytes and parses them identically regardless of how they arrived. No parse speedup." },
    { label: "Neither — LCP is driven by network, not CPU.", misconception: "On a fast TTFB, LCP past 2.5 s is usually JS parse/execute blocking paint. CPU is exactly the bottleneck the unit targets." },
  ]}
/>

<Quiz
  id="07-bundle-budgets-choice-2"
  lessonSlug="07-bundle-budgets"
  lang="en"
  question="A page loads in 800 ms on the developer's M-series laptop but sits blank for ~8 s on a mid-range Android. TTFB is 70 ms on both. What is the dominant cause?"
  choices={[
    { label: "Parse + compile + execute on the user's CPU. Mid-range mobile single-thread is 4–8× slower than the laptop, and those three phases are entirely CPU-bound — a faster network would not move them.", correct: true },
    { label: "The Android device is on a slower network.", misconception: "TTFB is 70 ms on both, so the network delivered the first byte fast. The gap is CPU-bound parse/compile/execute, not download." },
    { label: "The server is slower to respond to mobile user agents.", misconception: "TTFB is identical at 70 ms. The server is not the variable; the client CPU is." },
    { label: "The bundle is not gzip'd for mobile.", misconception: "Compression affects download bytes. An 8 s blank screen on fast TTFB is the CPU parsing/executing a large uncompressed bundle." },
  ]}
/>

<Quiz
  id="07-bundle-budgets-choice-3"
  lessonSlug="07-bundle-budgets"
  lang="en"
  question="A team's P75 LCP is Poor but P75 CLS is Good. They cut the route bundle from 700 KB to 200 KB. LCP and INP improve; CLS barely moves. Why is that the expected outcome?"
  choices={[
    { label: "LCP and INP are gated by main-thread JS parse/execute, so cutting bytes helps both. CLS comes from unsized images, late fonts, and above-fold dynamic insertion — a layout problem with a separate root cause and separate fix.", correct: true },
    { label: "CLS should have improved proportionally — it tracks bundle size like LCP.", misconception: "CLS measures visual stability (viewport fraction × shift distance), not CPU cost. Reducing bytes does not reserve layout space." },
    { label: "The CLS measurement is broken if it did not change.", misconception: "Unchanged CLS is correct: the fix for CLS is sized images, reserved space, and font-display: swap — none of which a bundle cut touches." },
    { label: "CLS only improves from server-side rendering.", misconception: "CLS is a client-side layout discipline issue; SSR alone does not fix unsized media or late fonts." },
  ]}
/>

<Quiz
  id="07-bundle-budgets-choice-4"
  lessonSlug="07-bundle-budgets"
  lang="en"
  question="A developer writes `import { debounce } from 'lodash'` and the analyzer shows 70 KB, not 2 KB. They already set `sideEffects: false` on their own package. What is the actual cause and fix?"
  choices={[
    { label: "`lodash` (not `lodash-es`) ships a CommonJS build; its exports are computed at runtime so the bundler cannot dead-code-eliminate them. Fix: import from `lodash-es`, or cherry-pick `lodash/debounce`.", correct: true },
    { label: "Their own `sideEffects: false` is wrong and is forcing the full library in.", misconception: "`sideEffects: false` on your package only marks your code shakeable; it cannot make a third-party CJS module statically analysable." },
    { label: "Minification failed — minifying would drop the unused 68 KB.", misconception: "Minification renames and strips whitespace; it does not remove unreferenced CJS exports. Tree shaking requires statically-analysable ESM." },
    { label: "Brotli would compress the unused code away.", misconception: "Compression shrinks the transfer of those bytes but they still ship, still parse, and still execute. The fix is to not include them." },
  ]}
/>

<Quiz
  id="07-bundle-budgets-choice-5"
  lessonSlug="07-bundle-budgets"
  lang="en"
  question="An app spends a sprint cutting its first-party bundle from 800 KB to 350 KB; a month later the data team adds Segment, Hotjar, and Intercom (~450 KB) via GTM and the win is erased. Which control would have caught this before it shipped?"
  choices={[
    { label: "A separate third-party budget (approved-vendor list) plus a CSP script-src allow-list — the new origins fail CI / are refused by the browser because they were never reviewed. A first-party size-limit gate never sees GTM-loaded scripts.", correct: true },
    { label: "A stricter first-party size-limit budget would have blocked them.", misconception: "size-limit measures chunks in your build output. GTM-injected scripts load from external domains at runtime and never appear in your bundle, so the gate is blind to them." },
    { label: "Tree shaking would have removed the unused vendor code.", misconception: "You do not control vendor source and cannot shake it; vendors ship full feature sets. Tree shaking is a first-party, ESM-only mechanism." },
    { label: "Brotli on the third-party scripts would have neutralised the cost.", misconception: "Compression cuts transfer, not the main-thread parse/execute the vendor scripts impose. The control needed is governance, not compression." },
  ]}
/>

<Quiz
  id="07-bundle-budgets-choice-6"
  lessonSlug="07-bundle-budgets"
  lang="en"
  question="Lighthouse CI on every PR reports P75 LCP 2.1 s; production RUM shows 4.8 s P75 on mobile for the same route. A senior reads this as:"
  choices={[
    { label: "Expected divergence. Lighthouse is one synthetic run with fixed throttling; RUM is the real distribution across device classes, networks, and geographies. Keep the CI gate to catch regressions and trust RUM for the real-user number — neither replaces the other.", correct: true },
    { label: "RUM is misconfigured — a 2× gap means bad data.", misconception: "A 2×+ synthetic-vs-field gap is normal and reflects real device/network diversity, not misconfiguration." },
    { label: "Drop Lighthouse CI since it disagrees with reality.", misconception: "Lighthouse catches code-introduced regressions pre-merge cheaply; RUM catches what synthetic misses post-deploy. They cover different failure classes." },
    { label: "Raise the budget — the real number is what users see, so 4.8 s is the new baseline.", misconception: "RUM revealing a worse real number is a reason to cut bytes, not to accept the regression as baseline." },
  ]}
/>

<Recap lang="en">
The unit's through-line is one decision tree: bytes equal four CPU phases (download is network, parse/compile/execute are the user's CPU), so the real levers reduce shipped bytes — splitting first, then tree shaking, with compression as a transport-only finisher. Core Web Vitals quantify the cost (LCP/INP move with JS, CLS does not). Third-party scripts are the silent budget killer that first-party gates cannot see, so they need their own budget plus CSP. And none of it holds without enforcement: size-limit in CI, RUM in production, a ratcheting budget-raise protocol against drift.
</Recap>
