---
concepts:
- bundle-budget-ci
- size-limit
- lighthouse-ci
- rum
- real-user-monitoring
deepensInto:
- 07-bundle-budgets/07-v8-pipeline-and-priorities
estMin: 15
lang: en
lessonType: topic
level: middle
order: 6
prereqs:
- 07-bundle-budgets/05-third-party-scripts
slug: 06-ci-enforcement
sources:
- https://web.dev/articles/optimize-vitals-lighthouse
- https://nextjs.org/docs/app/building-your-application/optimizing/bundle-analyzer
- https://developer.chrome.com/docs/lighthouse/performance/
spiral:
- latency
- statefulness
status: ready
summary: Without a CI gate, a bundle budget is an aspiration. With size-limit in CI
  and RUM in production, every PR that blows the budget fails, and every production
  regression pages the owning team.
title: 'CI enforcement and RUM: making budgets stick'
track: performance
unit: 07-bundle-budgets
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A team spends a week cutting the homepage bundle from 900 KB to 180 KB. Three months later it is back at 820 KB. No one violated any policy. Each engineer made a reasonable-looking import. Without a CI gate that fails PRs, the budget is aspirational — it erodes one PR at a time.
</Hook>

<Crux>Bundle budget without CI gate is a suggestion. size-limit fails over-budget PRs; RUM catches production drift before business metrics slip.</Crux>

<Explanation>

## The CI gate: size-limit

`size-limit` is an open-source tool that reads per-route bundle targets from `package.json` or a config file and fails the build if any limit is exceeded. It runs after the build, reads gzip'd chunk sizes, and exits non-zero if anything is over budget.

```json
// package.json
"size-limit": [
  { "name": "Homepage JS", "path": ".next/static/**/_app-*.js", "limit": "100 KB" },
  { "name": "Dashboard JS", "path": ".next/static/**/dashboard-*.js", "limit": "250 KB" },
  { "name": "All pages total", "path": ".next/static/**/*.js", "limit": "500 KB" }
]
```

```yaml
# .github/workflows/perf.yml
- name: Build
  run: bun run build
- name: Check bundle size
  run: npx size-limit
  # Fails PR if any limit exceeded
```

The PR comment integration (`size-limit-action`) posts a table of current vs budgeted size on every PR, making the impact visible before merge. Engineers who add a library see the delta immediately; they either justify the addition (and the team explicitly raises the budget) or find a smaller alternative.

## Alternative: bundlewatch, Lighthouse CI

`bundlewatch` is similar to size-limit with a different config format and GitHub check integration. Lighthouse CI runs a full Lighthouse audit on every PR — more comprehensive, slower. All three tools answer the same question: "does this PR blow the performance budget?"

Senior teams use size-limit for fast per-PR feedback (runs in 5-10 s) and Lighthouse CI on main branch merges for a full CWV snapshot.

## The bundle analyzer workflow

Before CI can enforce a budget, the team must understand the current bundle. The standard workflow:

1. Enable the bundle analyzer: `ANALYZE=true bun run build` (Next.js: `@next/bundle-analyzer`; Vite: `rollup-plugin-visualizer`; Webpack: `webpack-bundle-analyzer`).
2. Sort by size. Identify the top 5-10 contributors.
3. For each: ask "is this needed on the critical path of this route?"
4. Common findings: `moment.js` (220 KB, replace with date-fns); `lodash` full (80 KB, cherry-pick); admin panel imported globally (170 KB, lazy-load); all icon variations (110 KB, cherry-pick).
5. Set the initial budget at current size + 20% (room to grow, but not unlimited).

Together these steps turn an unknown bundle into a measured one with a named owner. Without step 4 — the common-findings audit — teams set arbitrary caps and then immediately discover they are over budget with no idea why.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Tool</th>
        <th class="border border-border px-3 py-2 text-left">Purpose</th>
        <th class="border border-border px-3 py-2 text-left">When to use</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">size-limit</td>
        <td class="border border-border px-3 py-2">Fails PR if bundle &gt; budget</td>
        <td class="border border-border px-3 py-2">Every PR</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Bundle analyzer</td>
        <td class="border border-border px-3 py-2">Visual treemap of modules</td>
        <td class="border border-border px-3 py-2">Before and after optimisations</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Lighthouse CI</td>
        <td class="border border-border px-3 py-2">Full synthetic CWV audit</td>
        <td class="border border-border px-3 py-2">On merge to main</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">RUM</td>
        <td class="border border-border px-3 py-2">Real-user LCP/INP/CLS metrics</td>
        <td class="border border-border px-3 py-2">Continuous production monitoring</td>
      </tr>
    </tbody>
  </table>
</div>

## RUM in production

CI catches regressions before merge. RUM catches what CI misses in production: device classes that perform worse than the throttled test, geographies with slow networks, and slow regressions that accumulate over many small PRs each within budget.

Implementations: Web Vitals JS library (Google, free), Sentry Performance, Datadog RUM, New Relic Browser, Vercel Speed Insights, Cloudflare Web Analytics.

Senior RUM dashboard: P75 LCP per route, per device class (mobile vs desktop), per geography. Alert when P75 LCP for any route degrades more than 20% for 24 hours.

Connecting RUM to bundle size: some teams ship the gzip'd bundle size as a custom RUM metric alongside LCP, so when LCP degrades the first dashboard to check shows the bundle size delta for the same deploy. Makes the correlation visible without inference.

## The budget raise protocol

When a PR legitimately needs to exceed a budget: the engineer opens a budget-raise PR. It requires sign-off from the performance-owning team lead, documents the justification (new feature, vendor requirement, user value), and records the old and new limits in `perf-budgets.json`. Without this protocol, budgets drift upward one implicit approval at a time.

<Inset kind="why" lang="en">
Why does the initial budget include a +20% buffer? Setting the budget at the current measured size means the next feature addition immediately fails CI, which is demoralising and counterproductive. A 20% buffer acknowledges that features grow bundles and forces explicit justification only when growth exceeds the buffer. Tighten the budget 10% per quarter as a ratchet — continuous improvement without a cliff.
</Inset>

<Quiz
  id="06-quiz-1"
  lessonSlug="06-ci-enforcement"
  lang="en"
  question="A PR adds a 60 KB chart library to the homepage. The CI gate fails. The engineer says the chart is important. What is the correct process?"
  choices={[
    { label: "Open a budget-raise PR with documented justification. The performance lead approves or suggests an alternative (lazy-load the chart, use a lighter library). The budget is explicitly raised if approved.", correct: true },
    { label: "Bypass the CI gate with a flag.", misconception: "Bypassing the gate destroys its value. The point is that every budget change is visible and explicit." },
    { label: "Merge the PR; fix the performance later.", misconception: "Later never comes. The point of the gate is to stop the regression at the source." },
    { label: "Remove the chart from the feature.", misconception: "Removing is one valid option, but lazy-loading is often better — delivers the feature without the initial bundle cost." },
  ]}
/>

<Quiz
  id="06-quiz-2"
  lessonSlug="06-ci-enforcement"
  lang="en"
  question="Lighthouse CI runs on every PR and reports P75 LCP as 2.1 s. Production RUM shows P75 LCP as 4.8 s on mobile. Why the discrepancy?"
  choices={[
    { label: "Lighthouse uses synthetic throttling, not real devices. Real users on mid-range hardware with variable network conditions are significantly slower. RUM shows the real distribution.", correct: true },
    { label: "RUM is misconfigured and reporting wrong data.", misconception: "A 2x+ discrepancy between synthetic and field data is common and expected — it reflects real device diversity, not misconfiguration." },
    { label: "Lighthouse includes third-party scripts; RUM does not.", misconception: "Both measure the same page; Lighthouse synthetic tests often miss third-party scripts that only load in production environments." },
    { label: "Lighthouse runs on desktop; RUM is mobile-only.", misconception: "Lighthouse can be configured for mobile emulation. The gap is device and network diversity, not desktop vs mobile." },
  ]}
/>

<Quiz
  id="06-quiz-3"
  lessonSlug="06-ci-enforcement"
  lang="en"
  question="A team sets a size-limit budget of 100 KB for homepage JS. A month later the budget is raised to 150 KB, then 180 KB, then 200 KB — each time for a 'critical' feature. What structural problem is this and how do you fix it?"
  choices={[
    { label: "Budget inflation — each raise feels justified in isolation but the total is unconstrained. Fix: quarterly ratchet that tightens the budget 10% per quarter, forcing the team to remove old code when adding new.", correct: true },
    { label: "The budget was set too low initially.", misconception: "Even a well-set budget will drift if there is no downward pressure. The budget-raise protocol must include periodic tightening." },
    { label: "size-limit is the wrong tool — use Lighthouse CI instead.", misconception: "The tool is not the issue; any tool can be bypassed by repeatedly raising the limit. The governance process is what needs fixing." },
    { label: "Features should not require bundle increases.", misconception: "Features legitimately require bytes. The issue is that the ratchet never tightens — old features are never cleaned up." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="The CI size-budget gate: measure each PR's bundle and fail it if over budget"
  caption="size-limit runs after the build, reads gzip'd chunk sizes, and compares to the per-route budget. Under budget merges; over budget fails the PR until split, justified, or the budget is explicitly raised."
  hue="var(--d-hardware)"
  nodes={[
    { id: "build", label: "Build", sub: "PR pipeline", col: 0, row: 1 },
    { id: "measure", label: "Measure size", sub: "gzip'd chunks", col: 1, row: 1 },
    { id: "compare", label: "Compare to budget", sub: "size-limit", col: 2, row: 1 },
    { id: "pass", label: "Pass → merge", sub: "under budget", col: 3, row: 0 },
    { id: "fail", label: "Fail PR", sub: "over budget", col: 3, row: 2 },
  ]}
  edges={[
    { from: "build", to: "measure" },
    { from: "measure", to: "compare" },
    { from: "compare", to: "pass", label: "≤ limit" },
    { from: "compare", to: "fail", label: "> limit" },
  ]}
/>

<KeyTakeaway>size-limit in CI makes every over-budget PR fail — converting the budget from aspirational to enforced. Bundle analyzer shows what is inside each chunk. RUM alerts on production drift that CI misses. The budget-raise protocol makes every limit change explicit and reviewed.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-retrieval"
  lang="en"
  questions={[
    { q: "Describe the complete bundle-budget enforcement stack: PR time, merge, and production.", a: "PR time: size-limit runs after build, fails PR if any per-route limit exceeded. Bundle analyzer available for investigation. Merge: Lighthouse CI runs a full synthetic CWV audit on main. Production: RUM tracks P75 LCP/INP per route, per device class; alerts on regression > 20% for 24 hours. Together: CI catches regressions before merge; RUM catches what synthetic misses in real production traffic." },
    { q: "Why does Lighthouse CI sometimes show Good LCP while production RUM shows Poor LCP for the same route?", a: "Lighthouse runs a synthetic test with fixed throttling on the test machine. Production users span many device classes (low-end, mid-range, high-end), network types (3G, 4G, 5G, WiFi), and geographies. Real P75 LCP reflects the entire distribution; synthetic shows one point. Lighthouse catches regressions introduced by code changes; RUM catches the full real-user impact." },
  ]}
/>

<Recap lang="en">
A bundle budget without a CI gate is a suggestion. size-limit, bundlewatch, or Lighthouse CI makes every over-budget PR fail automatically. The bundle analyzer provides the visibility to understand what is inside each chunk and where to cut. RUM completes the loop in production, catching slow regressions and device-class issues that synthetic tests miss. The budget-raise protocol ensures every limit change is explicit and reviewed. The next and final lesson covers the senior layer: V8's JIT pipeline and how HTTP/3 resource priorities interact with bundle delivery. Now when you see bundle size creeping up week over week, you have the full stack to catch it: CI before merge, RUM after deploy, and a raise protocol that keeps every increase deliberate.
</Recap>
