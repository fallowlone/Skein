---
concepts:
- v8-jit-tiers
- http3-priorities
- rfc-9218
- 103-early-hints
- sri
- csp
- bundle-observability
deepensInto: []
estMin: 18
lang: en
lessonType: topic
level: senior
order: 7
prereqs:
- 07-bundle-budgets/06-ci-enforcement
slug: 07-v8-pipeline-and-priorities
sources:
- https://v8.dev/blog/cost-of-javascript-2019
- https://web.dev/articles/preload-critical-assets
- https://web.dev/articles/optimize-vitals-lighthouse
spiral:
- latency
- statefulness
status: ready
summary: V8''''s four compilation tiers, HTTP/3 Extensible Priorities (RFC 9218),
  103 Early Hints, and Subresource Integrity form the senior layer of bundle delivery
  — the mechanisms that explain why a smaller bundle still varies by 2-3x depending
  on how it is served.
title: V8 JIT pipeline, HTTP priorities, and bundle security
track: performance
unit: 07-bundle-budgets
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Two apps have the same 100 KB homepage bundle. One has LCP 1.4 s; the other 2.3 s. The bundle sizes are identical. The difference is in how the bundle is delivered — resource priorities, Early Hints, and whether critical functions reach V8's optimised tiers before the page is interactive.
</Hook>

<Crux>Bundle size sets the floor. V8 JIT tiers, HTTP/3 priorities, 103 Early Hints decide if you're at the floor or 2x above it.</Crux>

<Explanation>

## V8's tiered compilation pipeline

V8 compiles JavaScript through four progressive tiers, trading compile cost for execution speed.

**Ignition** — the bytecode interpreter. Every function starts here. Compile cost is minimal; execution is ~5-10x slower than optimised native code. Code that runs only once (module-level initialisation) typically stays in Ignition.

**Sparkplug** (Chrome 91, 2021) — a non-optimising baseline JIT. Compiles bytecode to machine code with no inlining or type speculation. Compile cost is similar to Ignition; execution is ~5x faster. Functions automatically promote to Sparkplug after running roughly 100 times.

**Maglev** (Chrome 117, 2023) — a mid-tier optimising JIT with some inlining and type speculation. Promotes after more iterations.

**TurboFan** — the top-tier optimising compiler. Full inlining, escape analysis, type speculation, loop unrolling. Compile cost is high (10-100 ms per hot function) but execution approaches native speed. Functions reach TurboFan only after proven hot.

**Implications for bundle budgets**: total parse cost is proportional to bundle bytes — every byte must be parsed once regardless of tier. Initial compile cost is proportional to code that actually runs at startup. Code that never runs is parsed but never compiled (V8's lazy compilation). Bundle size matters most for parse and initial compile; execution speed of hot functions depends more on code shape (type stability, allocation patterns) than on bundle size.

**Deoptimisation traps**: V8 speculatively optimises functions based on observed types. If a function that was compiled assuming argument is always `number` receives a `string`, V8 deoptimises it — throws away the TurboFan-compiled version and falls back to Sparkplug or Ignition. A deopt loop is a function that optimises, deoptimises, and re-optimises repeatedly — it can burn CPU even with a small bundle. Diagnose with Chrome DevTools Performance tab; look for "Deoptimize" events.

## HTTP/3 Extensible Priorities (RFC 9218)

HTTP/1.1 and HTTP/2 have no reliable client-to-server priority signalling. HTTP/3 (over QUIC) implements RFC 9218: browsers send a `Priority` header with each request. Servers and CDNs that implement it serve critical resources first even if they were requested last.

```
Priority: u=1          # High urgency (0=highest, 7=lowest)
Priority: u=3, i       # Medium urgency, incremental delivery
```

Browsers automatically assign priorities: CSS in `<head>` gets urgency 0; lazy-loaded images get urgency 5-6. A small critical bundle competing with a large deferred vendor chunk for bandwidth is served first when the CDN honours priorities.

**Misconfiguration risk**: misconfigured HTTP/3 priority can erase 20-30% of code-splitting benefits if the CDN sends the large deferred chunk first despite browser priority hints. Verify in Chrome's Network panel under the "Priority" column.

## 103 Early Hints (RFC 8297)

A server that knows it will serve `index.html` also knows the critical resources that page will need — the critical JS chunk, the main font, the hero image. With `103 Early Hints`, the server sends an informational `103` response with `Link: ...; rel=preload` headers before finishing HTML generation. The browser starts fetching critical resources while the server is still building the response.

```
HTTP/2 103 Early Hints
Link: </static/app.abc123.js>; rel=preload; as=script
Link: </fonts/inter.woff2>; rel=preload; as=font; crossorigin
```

Typical gain: 100-500 ms on critical resource delivery, depending on the server's HTML generation time and network latency. Cloudflare, Fastly, and Vercel support 103. Most production teams do not use Early Hints yet; it is a senior-level optimisation that shows up in 95th-percentile LCP improvements.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Technique</th>
        <th class="border border-border px-3 py-2 text-left">Mechanism</th>
        <th class="border border-border px-3 py-2 text-left">Typical gain</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">HTTP/3 RFC 9218 priorities</td>
        <td class="border border-border px-3 py-2">Critical chunk served before deferred chunks</td>
        <td class="border border-border px-3 py-2">Prevents 20-30% split benefit loss</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">103 Early Hints</td>
        <td class="border border-border px-3 py-2">Preload headers before HTML is ready</td>
        <td class="border border-border px-3 py-2">100-500 ms LCP improvement</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">modulepreload</td>
        <td class="border border-border px-3 py-2">Download + parse-compile ESM module early</td>
        <td class="border border-border px-3 py-2">Removes parse cost from critical path</td>
      </tr>
    </tbody>
  </table>
</div>

## Subresource Integrity and CSP

**Subresource Integrity (SRI)**: the `integrity` attribute on `<script>` and `<link>` tags includes a hash of the expected file content. If a CDN or third-party host is compromised and serves a modified script, the browser computes the hash and rejects the file.

```html
<script src="https://cdn.example.com/lib.js"
        integrity="sha384-oqVuAfXRKap7fdgcCY5uykM6+R9GqQ8K/uxy9rx7HNQlGYl1kPzQho1wx4JwY8wC"
        crossorigin="anonymous"></script>
```

Without SRI, a CDN compromise ships malicious JS to all users silently. With SRI, the browser refuses the script. Production-grade: SRI on all third-party script tags; build tooling generates SRI hashes for first-party assets automatically.

**CSP + bundle delivery**: `script-src 'self' https://cdn.example.com` restricts which origins can serve scripts. Common misconfiguration: `'unsafe-inline'` allows inline scripts, defeating the protection against XSS-injected scripts. Use nonces or hashes for intentional inline scripts instead.

## Production observability: bundle size as a RUM metric

Beyond LCP/INP/CLS, senior teams track gzip'd bundle size per route as a custom RUM metric. When LCP degrades, the first correlation to check is whether the bundle size changed in the same deploy. Some platforms (Vercel Analytics, Sentry Performance) expose this automatically; others require a custom beacon.

Production failure patterns: Pinterest 2017 (single large bundle → 12 s main-thread block, rewrite to chunks → bounce -40%); Tinder 2019 (admin panel on consumer routes → 200 KB removed → conversion +5%); Stripe 2020 (Stripe.js 80 KB on every checkout → small loader + lazy → TTI -35%). The pattern is always structural: split, lazy, replace — not compress-more.

<Inset kind="why" lang="en">
Why does a deopt loop matter for bundle budgets? A deoptimisation happens when V8 detects that its type assumptions were wrong and falls back to slower tiers. If the deopt is in a frequently-called function (say, a render loop), the CPU overhead is multiplicative — the function re-JITs repeatedly. A 50 KB bundle with a deopt loop in a hot function can feel slower than a 200 KB bundle without one. Bundle analysis and --trace-deopt are different diagnostic axes; senior performance work uses both.
</Inset>

<Quiz
  id="07-quiz-1"
  lessonSlug="07-v8-pipeline-and-priorities"
  lang="en"
  question="A team profiles V8 and finds a render function deoptimises 50 times per second. The bundle is 90 KB. What is the fix, and does reducing bundle size help?"
  choices={[
    { label: "Fix the type instability in the hot function — stabilise the object shape or ensure consistent argument types. Bundle size reduction does not help; the cost is per-call CPU from deopt loops, not parse cost.", correct: true },
    { label: "Reduce the bundle size — smaller bundles deopt less.", misconception: "Deoptimisations are driven by type instability in hot functions, not bundle size. A 10 KB bundle can deopt as readily as a 1 MB bundle." },
    { label: "Switch to a lighter framework.", misconception: "Framework size is irrelevant to a specific function's deopt pattern. Diagnose and fix the type instability in the function itself." },
    { label: "Use brotli compression.", misconception: "Compression affects wire bytes and download time. Deopt is a runtime CPU issue — compression is irrelevant." },
  ]}
/>

<Quiz
  id="07-quiz-2"
  lessonSlug="07-v8-pipeline-and-priorities"
  lang="en"
  question="A team enables code splitting but their CDN is misconfigured with equal HTTP/3 priorities for all chunks. What is the likely performance impact?"
  choices={[
    { label: "The deferred large vendor chunk competes equally for bandwidth with the small critical app chunk. The critical chunk may arrive late despite being smaller — erasing up to 20-30% of the split benefit.", correct: true },
    { label: "No impact — HTTP/3 is inherently faster than HTTP/2.", misconception: "HTTP/3 is faster for certain patterns, but misconfigured priorities mean the browser's hints are ignored and bandwidth is shared equally." },
    { label: "The vendor chunk downloads first because it is larger.", misconception: "Without priorities, ordering is non-deterministic. With correct priorities, the small critical chunk is sent first. With wrong priorities, either order can occur." },
    { label: "Code splitting does not interact with HTTP/3 priorities.", misconception: "Code splitting creates multiple chunks competing for bandwidth. Priority ordering determines which arrive first — directly affecting LCP." },
  ]}
/>

<Quiz
  id="07-quiz-3"
  lessonSlug="07-v8-pipeline-and-priorities"
  lang="en"
  question="A script tag has no integrity attribute and loads from a third-party CDN. The CDN is compromised and serves a modified script. What happens without SRI, and what happens with it?"
  choices={[
    { label: "Without SRI: the modified script runs, potentially exfiltrating user data. With SRI: the browser computes the hash of the served file, finds it does not match the expected hash, and refuses to execute the script.", correct: true },
    { label: "Without SRI: the browser detects tampering and blocks it.", misconception: "Without SRI, the browser has no way to verify file integrity. It executes whatever the CDN serves." },
    { label: "With SRI: the script is slower to execute.", misconception: "SRI hash verification is a cryptographic check on download — negligible overhead. It does not affect execution speed." },
    { label: "SRI only works for first-party scripts.", misconception: "SRI is specifically designed for third-party scripts served from external CDNs. First-party scripts served from your own origin are implicitly trusted." },
  ]}
/>

</Explanation>

<KeyTakeaway>V8's four JIT tiers (Ignition → Sparkplug → Maglev → TurboFan) compile hot functions progressively — bundle size sets parse cost, but deopt loops in hot functions can dominate execution cost independently. HTTP/3 RFC 9218 priorities ensure split bundles arrive in the right order. 103 Early Hints gives the browser a 100-500 ms head start on critical resources. SRI + CSP secure the delivery against CDN compromise.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-retrieval"
  lang="en"
  questions={[
    { q: "Describe V8's four compilation tiers and what each buys.", a: "Ignition: bytecode interpreter, minimal compile cost, slow execution (~5-10x below native). Sparkplug: baseline non-optimising JIT, fast to compile, ~5x faster than Ignition. Maglev: mid-tier optimising JIT, some inlining and type speculation. TurboFan: top-tier, full inlining and speculative optimisation, 10-100 ms compile per function, near-native execution. Functions promote up tiers as they run more. Bundle size affects parse and Ignition compile; execution of hot functions depends on reaching TurboFan and staying there." },
    { q: "What does RFC 9218 define, and why does it matter for code-split bundles?", a: "RFC 9218 defines the Extensible Prioritization Scheme for HTTP — a standard Priority header that lets browsers signal urgency (0=highest, 7=lowest) and incremental flag per resource request. Servers and CDNs that implement it serve high-urgency resources (critical JS chunk) before low-urgency ones (large deferred vendor chunk) even if all were requested at the same time. Without it, bandwidth is shared equally and the critical chunk may arrive late, erasing split benefits." },
    { q: "A PR introduces a function that handles both strings and numbers. After a deploy, RUM shows INP regression on a specific route. What is the likely diagnosis and fix?", a: "Likely a V8 deoptimisation. The function was optimised for one type (say number), then called with a string, triggering a deopt and TurboFan recompile. If this function is on the interaction hot path (called on every click), each call in the deopt period is slow. Fix: stabilise the type — handle the two types in separate functions, or normalise input before the hot function. Diagnose with Chrome Performance tab, look for Deoptimize events correlated with the slow interactions." },
  ]}
/>

<Recap lang="en">
Bundle size sets the parse cost floor, but the ceiling is determined by how the bundle is delivered and compiled. V8's four JIT tiers compile hot functions progressively from Ignition through TurboFan — bundle bytes affect parse, but type-unstable hot functions cause deopt loops that dominate execution time independently of size. HTTP/3 RFC 9218 priorities ensure code-split chunks arrive in the right order; a misconfigured CDN erases up to 30% of split benefits. 103 Early Hints gives the browser a 100-500 ms head start on critical resources before HTML even arrives. Subresource Integrity and CSP secure the delivery against CDN compromise. Together these form the senior performance layer: the difference between a theoretically correct bundle budget and one that actually delivers the performance the measurements promise.
</Recap>
