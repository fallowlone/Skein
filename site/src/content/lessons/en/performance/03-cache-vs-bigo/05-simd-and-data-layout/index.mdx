---
concepts:
- simd
- soa
- aos
- memory-bandwidth
- numa
- auto-vectorisation
- inlining-icache
deepensInto: []
estMin: 15
lang: en
lessonType: topic
level: middle
order: 5
prereqs:
- performance/03-cache-vs-bigo/03-cache-lines-and-false-sharing
slug: 05-simd-and-data-layout
sources:
- https://en.algorithmica.org/hpc/pipelining/branching/
- https://learn.arm.com/learning-paths/servers-and-cloud-computing/memory-subsystem/cache-hierarchy/
- https://en.wikipedia.org/wiki/CPU_cache
spiral:
- statefulness
status: ready
summary: Array-of-Structures (AoS) prevents SIMD; Structure-of-Arrays (SoA) enables
  it. Memory bandwidth and NUMA are the two constraints that profiling cache-miss
  rate misses.
title: SIMD, SoA vs AoS, and memory bandwidth
track: performance
unit: 03-cache-vs-bigo
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import Misconception from "~/components/layout/Misconception.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
A hot ML inference loop is 5x slower than the reference C++ implementation, despite identical algorithm and the same number of multiplications. The profiler shows IPC 0.8 and 25% L3 miss rate. Switching to SIMD intrinsics is suggested — but the fix that actually works is changing how the data is stored, not how it is computed.
</Hook>

<Crux>AoS interleaves types — SIMD gathers at 10x cost. SoA stores one type per array — 8 values/cycle. Layout decides if SIMD works.</Crux>

<Explanation>

## SIMD: one instruction, multiple values

Modern CPUs carry wide vector registers: 256-bit (AVX2, standard on x86 since 2013), 512-bit (AVX-512, available on server Intel and recent Ryzen), 128-bit NEON on ARM. One SIMD instruction can add, multiply, or compare 4–8 floats simultaneously.

The critical requirement: the values must be **consecutive in memory**. A single load instruction fills the vector register from a contiguous block; the CPU then operates on all 4–8 values in parallel.

## Array of Structures vs Structure of Arrays

**AoS (Array of Structures):**
```c
struct Point { float x; float y; float z; };
Point points[N];  // layout: x0 y0 z0 x1 y1 z1 x2 y2 z2 ...
```

To add all `x` values with SIMD, you need `points[0].x`, `points[1].x`, `points[2].x`, `points[3].x` — but they are at offsets 0, 12, 24, 36 bytes. The SIMD load at address 0 gives `x0 y0 z0 x1` — mixed types. You must use scatter/gather operations to pull out just the `x` values, which costs ~10x a contiguous load.

**SoA (Structure of Arrays):**
```c
float xs[N];  // layout: x0 x1 x2 x3 x4 x5 x6 x7 ...
float ys[N];
float zs[N];
```

To add all `x` values with SIMD: load 8 consecutive floats from `xs[0]`, done. One instruction, 8 results. The loop body becomes 8x more productive per instruction.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Layout</th>
        <th class="border border-border px-3 py-2 text-left">SIMD compatibility</th>
        <th class="border border-border px-3 py-2 text-left">Cache efficiency</th>
        <th class="border border-border px-3 py-2 text-left">Best use case</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">AoS</td>
        <td class="border border-border px-3 py-2">Requires gather/scatter (slow)</td>
        <td class="border border-border px-3 py-2">Good when all fields used</td>
        <td class="border border-border px-3 py-2">Single-element operations, OOP</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">SoA</td>
        <td class="border border-border px-3 py-2">Native contiguous load</td>
        <td class="border border-border px-3 py-2">Excellent when one field used</td>
        <td class="border border-border px-3 py-2">Batch processing, ML, game physics</td>
      </tr>
    </tbody>
  </table>
</div>

Game engines (Unity ECS, Bevy), ML inference engines, audio processing, and database column stores all use SoA. V8 (Chrome's JavaScript engine) uses SoA-like TypedArrays for hot loops. The previous lesson's ML example: changing from AoS `(x,y,z,w)` structs to `xs[], ys[], zs[], ws[]` drops L3 miss rate from 25% to 5% and raises IPC from 0.8 to 3.0.

## Auto-vectorisation

Compilers automatically convert simple loops to SIMD when the data layout allows. Conditions for auto-vectorisation success:
- No pointer aliasing (two pointers don't point to overlapping memory — use `restrict` in C, Rust handles this via borrow checker).
- Contiguous (not strided or gather/scatter).
- Predictable trip count.
- No data-dependent inner branches.

Check what the compiler emitted: `-fopt-info-vec` (GCC) or `-Rpass=loop-vectorize` (Clang) prints when loops are vectorised and why they failed. For hot loops that fail auto-vec, manual SIMD intrinsics or libraries (Intel Highway, simd-everywhere) close the gap.

## Memory bandwidth: the other constraint

Cache hit rate is one axis of performance; **memory bandwidth** is another. A workload that streams through 100 GB of data will hit the RAM bandwidth ceiling (~50–100 GB/s on DDR5) regardless of cache behaviour. Bandwidth-bound code is fixed by:
- Reducing data volume: more compact types (float32 instead of float64 when precision allows), on-the-fly computation instead of materialised intermediate tables.
- Compression at rest, decompression on-the-fly.
- Non-temporal stores (covered in the senior lesson): bypass cache for write-once data.

`perf stat -e cache-misses,mem-loads-retired.l3-miss` separates cache-miss-bound from bandwidth-bound: if L3 misses are high but total data volume is huge, you are bandwidth-bound; if L3 misses are high but data volume is small (fits in L3 but pattern is random), you are cache-miss-bound.

## NUMA: multi-socket memory access

Servers with 2+ CPU sockets are Non-Uniform Memory Access. Each socket has local RAM (~70 ns) and remote RAM (~120–150 ns). A thread that allocates memory on socket 0 but runs on socket 1 pays the remote-access tax on every load. This is a 1.7x latency penalty that `perf stat` will misreport as "L3 misses" because the access goes through the interconnect.

Mitigations:
- Pin threads to sockets (`taskset`, `hwloc`).
- Allocate on the local NUMA node (`numactl --membind`, `jemalloc` NUMA-aware mode).
- Distribute work so each thread touches data from its local socket.

<NumbersCard
  id="card-simd-numbers"
  title="SIMD and data layout numbers"
  stats={[
    { label: "AVX2 float throughput", value: "8 floats per instruction" },
    { label: "AVX-512 float throughput", value: "16 floats per instruction" },
    { label: "NEON (ARM) throughput", value: "4 floats per instruction" },
    { label: "Gather/scatter vs contiguous load", value: "~10x slower" },
    { label: "DDR5-6000 bandwidth", value: "~50 GB/s per channel" },
    { label: "NUMA local vs remote latency", value: "70 ns vs 120–150 ns" },
    { label: "ML loop AoS→SoA IPC gain", value: "0.8 → 3.0 (example)" },
  ]}
/>

<Inset kind="why" lang="en">
Inlining affects the instruction cache (I-cache), not data cache. Aggressive inlining of a function called from 100 sites adds 100 copies of its body to the binary. If those copies pollute I-cache they evict other hot functions. The fix: inline tiny functions (1–3 lines) freely; inline larger functions only on the hottest call sites. PGO makes these decisions based on real call frequencies. Monitoring `L1-icache-load-misses` in perf stat catches instruction-cache pressure after layout changes.
</Inset>

<Misconception id="mc-simd-auto">
  <p><strong>"Just add SIMD intrinsics to make my loop fast."</strong> SIMD only helps when data is contiguous and the loop is compute-bound. If it is cache-miss-bound (low IPC, high L3 miss), SIMD on AoS data gives zero benefit. Fix layout to SoA first, confirm compute-bound, then apply SIMD.</p>
</Misconception>

<Quiz
  id="05-simd-quiz-1"
  lessonSlug="05-simd-and-data-layout"
  lang="en"
  question="A hot ML inference loop is 5x slower than a reference C++ port despite identical algorithm. perf stat shows IPC 0.8 and 25% L3 miss rate. What is the primary diagnosis?"
  choices={[
    { label: "Memory layout: the slow version stores activations as AoS (array of structs) where SIMD requires gather/scatter. The reference uses SoA. Fix: restructure to SoA — IPC should rise to ~3.0 and L3 miss rate drop to ~5%.", correct: true },
    { label: "Missing compiler flags like -O3 or -march=native", misconception: "Compiler flags are worth checking, but IPC 0.8 combined with 25% L3 miss rate is a clear memory-bound signature — not a compute/flag issue. Compiler flags do not fix AoS layout." },
    { label: "The algorithm has O(N²) complexity that should be O(N)", misconception: "Both versions use the same algorithm. The 5x gap is a constant factor, not an asymptotic difference. Memory layout is the cause of constant-factor differences of this magnitude." },
    { label: "Thread contention on a shared data structure", misconception: "Thread contention shows as off-CPU wait time and low throughput at high concurrency — not as IPC 0.8 with high L3 miss rate in a compute loop." },
  ]}
/>

<Quiz
  id="05-simd-quiz-2"
  lessonSlug="05-simd-and-data-layout"
  lang="en"
  question="A backend service runs on a 2-socket NUMA server. A thread allocates a large working set on socket 0, then migrates to socket 1 under load. What perf signature does this produce?"
  choices={[
    { label: "High cache-miss rate and high latency that looks like L3 misses, but data volume is small enough to fit in L3 — the real cause is remote NUMA access (~120–150 ns vs 70 ns local). Fix: pin thread to socket and allocate locally.", correct: true },
    { label: "High branch-miss rate from cross-socket interrupts", misconception: "NUMA remote access affects memory latency, not branch prediction. Branch-miss rate is about code structure, not NUMA topology." },
    { label: "No performance impact — the OS migrates pages automatically", misconception: "The OS can migrate pages with NUMA balancing (Linux autonuma) but does so slowly and does not eliminate the problem under sustained load. Application-level pinning is required for perf-sensitive code." },
    { label: "Lower latency — the remote socket has fresher cache lines", misconception: "Remote NUMA memory is always slower (~1.7x latency penalty) regardless of cache state. There is no scenario where accessing remote socket memory is faster." },
  ]}
/>

</Explanation>

<DiagramFrame
  label="AoS scatters x values; SoA makes them one SIMD load"
  hue="var(--d-hardware)"
  caption="To sum x with SIMD: AoS forces a gather (x values 12 bytes apart, ~10x slower); SoA loads 4–8 consecutive x in one instruction."
>
  <svg viewBox="0 0 470 220" width="470" height="220" role="img" aria-label="AoS interleaves x y z; SoA stores each field contiguously, enabling a single SIMD load" style="display:block;margin:0 auto;max-width:100%;height:auto;font-family:var(--font-mono);">
    {/* AoS row */}
    <text x="20" y="28" font-size="12.5" font-family="var(--font-body)" fill="var(--ink)">AoS: struct Point {'{ x y z }'} points[N]</text>
    {["x0","y0","z0","x1","y1","z1","x2","y2"].map((t, i) => (
      <g>
        <rect x={20 + i*52} y="40" width="48" height="34" rx="4"
          fill={t.startsWith("x") ? "var(--accent)" : "var(--card-2)"} opacity={t.startsWith("x") ? "0.45" : "1"}
          stroke="var(--hairline-strong)" stroke-width="0.8" />
        <text x={44 + i*52} y="62" text-anchor="middle" font-size="11.5" fill="var(--ink)">{t}</text>
      </g>
    ))}
    <text x="235" y="98" text-anchor="middle" font-size="10.5" fill="var(--muted)">x values 12 B apart → gather (~10x slower)</text>

    {/* SoA row */}
    <text x="20" y="142" font-size="12.5" font-family="var(--font-body)" fill="var(--ink)">SoA: float xs[N], ys[N], zs[N]</text>
    {["x0","x1","x2","x3","x4","x5","x6","x7"].map((t, i) => (
      <g>
        <rect x={20 + i*52} y="154" width="48" height="34" rx="4"
          fill="var(--accent)" opacity="0.45" stroke="var(--hairline-strong)" stroke-width="0.8" />
        <text x={44 + i*52} y="176" text-anchor="middle" font-size="11.5" fill="var(--ink)">{t}</text>
      </g>
    ))}
    <rect x="17" y="151" width="212" height="40" rx="5" fill="none" stroke="var(--d-hardware, var(--accent))" stroke-width="1.6" />
    <text x="235" y="208" text-anchor="middle" font-size="10.5" fill="var(--muted)">one SIMD load → 4–8 x per instruction</text>
  </svg>
</DiagramFrame>

<KeyTakeaway>SIMD requires contiguous same-type values; AoS interleaves types forcing scatter/gather (10x slower); SoA stores each field in its own array, enabling native SIMD loads. Memory bandwidth and NUMA are separate axes — cache hit rate alone does not tell you whether you are bandwidth-bound or topology-bound. Fix layout before adding SIMD intrinsics.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-simd-retrieval"
  lang="en"
  questions={[
    { q: "Explain the AoS vs SoA trade-off for a hot loop that accesses only one field of a multi-field struct.", a: "AoS (Array of Structures) packs all fields of each element together: struct Point { float x, y, z } points[N]. A loop that sums only x values loads all three floats per element from cache — 2/3 of every cache line is wasted on y and z. Worse, SIMD cannot load 8 consecutive x values because they are 12 bytes apart (AoS stride), forcing gather instructions (~10x slower than contiguous load). SoA (Structure of Arrays) stores each field separately: float xs[N], ys[N], zs[N]. A loop summing x values loads 8 consecutive floats from xs in one SIMD instruction; y and z are never loaded, saving 2/3 of memory bandwidth. The trade-off: AoS is better when you access all fields together (e.g. constructing a Point to pass to one function); SoA is better for bulk operations on one field at a time. Game engines, ML inference, and physics simulations use SoA as the default; OOP code prefers AoS for encapsulation. The migration pattern: keep AoS for the API surface but convert to SoA internally in hot loops via a SoA-view transformation." },
    { q: "A perf stat run shows high L3 miss rate, but the working set is only 50 MB (well within L3 capacity). What alternative explanation should you investigate, and how?", a: "Three alternatives: 1) NUMA remote access — if the service runs on a multi-socket server, the thread may be accessing memory allocated on the remote NUMA node. Check with numastat or perf stat --metric-groups NUMA; fix by pinning the thread to its NUMA node with numactl --cpunodebind or taskset. 2) Cache associativity conflict misses — if hot data addresses map to the same cache set, lines evict each other even though overall capacity is available. Check with cachegrind (D1mr/D1mw per source line); fix by padding arrays by a cache line to break alignment patterns. 3) Pointer-chasing access pattern — the data fits in L3 but is accessed via linked structures with unpredictable addresses; each dereference misses even though the data is 'in L3' in the sense that individual nodes eventually reside there but are not pre-loaded. Check by looking at the access pattern in the hot function; fix by packing into contiguous arrays or using an arena allocator." },
  ]}
/>

<Recap lang="en">
SIMD instructions operate on 4–16 consecutive values per cycle; AoS layout interleaves field types forcing 10x-slower gather operations, while SoA stores each field in a flat array enabling native SIMD loads. The ML benchmark's 5x gap came entirely from changing AoS to SoA — no algorithm change, just layout. Beyond cache locality, two separate constraints limit throughput: memory bandwidth (how many GB/s you can stream from RAM) and NUMA topology (remote-socket memory costs 1.7x more than local). `perf stat` exposes both via L3-miss rate and NUMA counters. Fix layout to SoA first, verify the loop becomes compute-bound, then apply SIMD intrinsics or rely on auto-vectorisation.
</Recap>
