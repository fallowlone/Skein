---
concepts:
- hardware-prefetcher
- tlb
- huge-pages
- mlp
- non-temporal-stores
- cache-associativity
- software-prefetch
deepensInto: []
estMin: 15
lang: en
lessonType: topic
level: senior
order: 6
prereqs:
- performance/03-cache-vs-bigo/03-cache-lines-and-false-sharing
- performance/03-cache-vs-bigo/05-simd-and-data-layout
slug: 06-hardware-prefetcher-and-tlb
sources:
- https://en.algorithmica.org/hpc/pipelining/branching/
- https://learn.arm.com/learning-paths/servers-and-cloud-computing/memory-subsystem/cache-hierarchy/
- https://en.wikipedia.org/wiki/CPU_cache
- https://blog.cloudflare.com/branch-predictor/
spiral:
- latency
status: ready
summary: The hardware prefetcher is a lubricant — it helps predictable patterns but
  cannot infer pointer-chasing. TLB misses add 5–50 cycles before any cache check.
  MLP lets the CPU stack multiple outstanding misses.
title: Hardware prefetcher, TLB, and memory-level parallelism
track: performance
unit: 03-cache-vs-bigo
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A graph traversal loop shows high L3 miss rate even though the graph fits in L2 cache. The prefetcher is silent. Separately, a database scan that streams through 50 GB is bandwidth-bound despite cache being clean and hits being high. Two different mechanisms — same symptom.
</Hook>

<Crux>Prefetcher sees strides, not pointer chains. TLB miss = 5–50 cycles. Non-temporal stores and MLP exist outside the basic cache model.</Crux>

<Explanation>

## Hardware prefetcher: what it can and cannot do

When you see a loop with high L3 miss rate even though the working set fits comfortably in cache, the culprit is almost always a pattern the prefetcher cannot infer — understanding its limits tells you exactly which fix to apply.

Modern CPUs have multiple hardware prefetchers per core:
- **Sequential prefetcher**: detects forward-sequential access and prefetches up to 16 lines ahead. Activates after 2–3 consecutive misses in the same direction.
- **Stride detector**: detects "every N-th byte" patterns (e.g. column access at a fixed stride). Works up to strides of ~256 bytes.
- **Stream prefetcher (some chips)**: detects multiple independent sequential streams simultaneously.

The prefetcher **cannot** infer the next address when it depends on the current data value — i.e. pointer chasing. A graph traversal where `next = node->next_ptr` requires loading the pointer value before knowing where to prefetch next. The prefetcher watches address patterns, not data values.

**What this means in practice:**
- Sequential array iterations: prefetcher fills L1 continuously, essentially free.
- Regular strides (every 32 bytes, every 128 bytes): stride detector fires, most misses hidden.
- Pointer-chasing (linked lists, trees, graphs): every dereference is a cold miss unless data happens to fit entirely in L2/L3.
- Graph traversal on a graph that fits in L2: still misses because access order is data-dependent, not because data is absent.

**Software prefetch** is the manual escape hatch. Insert `__builtin_prefetch(addr, 0, 1)` (GCC/Clang) or `_mm_prefetch` a few iterations ahead of when the data is needed. Useful when you know the next address at the code level but the hardware cannot infer it. Used in database hash-join implementations, some graph processing libraries.

## TLB: the address-translation cache

Before any L1/L2/L3 check, the CPU must translate the virtual address to a physical address. The Translation Lookaside Buffer (TLB) caches recent virtual-to-physical translations:
- TLB hit: 0–1 cycle overhead.
- TLB miss: the CPU must walk the page table — 5–50 cycles (sometimes more with deep table nesting).

**Default page size: 4 KB.** A workload touching 1 GB of data touches 262 144 different pages. The TLB holds 64–1024 entries (depending on level and CPU). Most entries evict frequently — TLB thrashing adds 5–50 cycles per access on top of the L3/RAM latency.

**Huge pages (2 MB on x86, optional 1 GB):**
- 2 MB pages: 512x fewer TLB entries needed to cover the same address space (2 MB / 4 KB = 512).
- 1 GB pages: 262 144x reduction.
- For code touching hundreds of MB (databases, ML inference, image processing), huge pages can provide 10–20% throughput improvement independently of cache locality.

Enabling: Linux `madvise(MADV_HUGEPAGE)`, `hugetlbfs`, or `LD_PRELOAD` with jemalloc/mimalloc configured for transparent huge pages.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Access path</th>
        <th class="border border-border px-3 py-2 text-left">TLB state</th>
        <th class="border border-border px-3 py-2 text-left">Total latency</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">L1 hit</td>
        <td class="border border-border px-3 py-2">TLB hit</td>
        <td class="border border-border px-3 py-2">~4 cycles total</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">L1 hit</td>
        <td class="border border-border px-3 py-2">TLB miss (page walk)</td>
        <td class="border border-border px-3 py-2">~50 cycles total</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">RAM miss</td>
        <td class="border border-border px-3 py-2">TLB hit</td>
        <td class="border border-border px-3 py-2">~300 cycles total</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">RAM miss</td>
        <td class="border border-border px-3 py-2">TLB miss (page walk)</td>
        <td class="border border-border px-3 py-2">~350 cycles total</td>
      </tr>
    </tbody>
  </table>
</div>

## Memory-level parallelism (MLP)

Modern CPUs can have 8–16 cache misses outstanding simultaneously. When code issues independent loads, the CPU queues them in its load buffer and executes them in parallel — 10 independent misses cost ~150 cycles (one miss latency + dispatch overhead), not 10 × 300 cycles.

**Dependent-load chains defeat MLP entirely:**
```c
// Pointer chasing: each load depends on the previous result
x = arr[0];         // must wait for arr[0]
y = arr[x];         // must wait for x → cannot start until arr[0] completes
z = arr[y];         // must wait for y → cannot start until arr[x] completes
// Three misses in serial: 3 × 300 ns = 900 ns
```

**Independent loads benefit from MLP:**
```c
// Multiple independent loads: CPU can issue all in parallel
a = arr[0]; b = arr[1000]; c = arr[2000]; d = arr[3000];
// Four misses in parallel: ~300 ns total (+ small overhead)
```

Optimising for MLP: transform dependent pointer-chains into index-based arrays where future indices can be prefetched while current data is processed.

## Non-temporal stores

Streaming through gigabytes of data (DB scans, log processing, memcpy) produces data that is read once and never reused. Writing this data through the cache wastes L3 capacity evicting hot data. Non-temporal store instructions bypass the cache and write directly to memory:

```c
// Standard store: goes through L1 → L2 → L3 → RAM
store[i] = value;

// Non-temporal store: bypasses cache, writes directly to RAM
// Frees L3 for hot data; improves sustained bandwidth 20–30%
_mm_stream_si64((long long*)&store[i], value);  // x86 AVX
__builtin_nontemporal_store(value, &store[i]);   // GCC/Clang
```

Use when: write-once-then-discard pattern is confirmed by profiling (`perf stat mem-loads-retired.l3-miss` shows writes dominate).

## Cache associativity and conflict misses

L1 and L2 caches are not fully associative — each address maps to a specific "set" (typically 8–16 ways associative). If many hot addresses hash to the same set, they evict each other even though cache capacity is available elsewhere.

Classic case: two large arrays accessed at identical offsets that are multiples of the cache size (e.g. two arrays of size 1 MB accessed at stride 1 MB). On a 512 KB L2, offsets 0 and 512 KB map to the same set — they evict each other.

Detection: `cachegrind` reports D1mr/D1mw and DLmr/DLmw per source line. Conflict misses appear as high miss rate on small data that "should" fit in cache.
Fix: pad arrays by a cache-line amount to break the alignment pattern.

<NumbersCard
  id="card-senior-numbers"
  title="Senior-tier cache and CPU numbers"
  stats={[
    { label: "TLB miss page walk", value: "5–50 cycles" },
    { label: "Huge page (x86)", value: "2 MB / 1 GB" },
    { label: "L1 cache associativity", value: "typically 8-way" },
    { label: "MLP outstanding misses", value: "8–16 typical" },
    { label: "Non-temporal store bandwidth gain", value: "20–30% streaming" },
    { label: "Apple M3 pipeline depth", value: "~10 stages" },
    { label: "DDR5-6000 bandwidth", value: "~50 GB/s per channel" },
  ]}
/>

<Misconception id="mc-prefetch-magic">
  <p><strong>"The hardware prefetcher handles everything; cache locality is legacy."</strong> The prefetcher helps only with predictable patterns (sequential, strides). Pointer-chasing and irregular indexing defeat it. Layout still matters — prefetcher amplifies good layout but cannot rescue bad.</p>
</Misconception>

<Quiz
  id="06-hw-prefetch-quiz-1"
  lessonSlug="06-hardware-prefetcher-and-tlb"
  lang="en"
  question="Why does the hardware prefetcher fail on graph traversals even when the graph fits entirely in L2 cache?"
  choices={[
    { label: "Each node visit's next address is data-dependent (requires loading a pointer value first); the prefetcher cannot predict the next address without doing the load itself, so it offers no benefit.", correct: true },
    { label: "The prefetcher only works on sequential (stride-1) access", misconception: "Modern prefetchers handle regular strides (stride detector). The block is specifically data-dependency — the next address is inside the data being loaded." },
    { label: "Graphs are too large for the prefetcher to track", misconception: "The question states the graph fits in L2; size is not the issue. The issue is access order unpredictability." },
    { label: "The L2 cache does not support prefetching", misconception: "All cache levels benefit from prefetching; the sequential prefetcher and stride detector work across L1/L2/L3." },
  ]}
/>

<Quiz
  id="06-hw-prefetch-quiz-2"
  lessonSlug="06-hardware-prefetcher-and-tlb"
  lang="en"
  question="A database scan streams through 80 GB of data. Increasing L3 cache from 32 MB to 64 MB provides no speedup. Why, and what would actually help?"
  choices={[
    { label: "The scan is bandwidth-bound: data is read once and discarded, so caching it wastes capacity. Larger L3 does not raise RAM bandwidth. Fixes: use non-temporal stores to bypass cache and avoid polluting L3; reduce data volume (compression, projection); or upgrade to higher-bandwidth memory.", correct: true },
    { label: "The scan is branch-prediction-bound due to filter comparisons", misconception: "A raw streaming scan is overwhelmingly memory-bandwidth-bound; branch prediction plays a minor role. Doubling L3 while failing to help confirms bandwidth, not branch, is the bottleneck." },
    { label: "Larger L3 introduces higher associativity conflicts", misconception: "Larger L3 generally reduces conflict misses; this is not the cause of the non-speedup on a streaming workload." },
    { label: "The CPU's NUMA topology limits cross-socket bandwidth", misconception: "NUMA is relevant for multi-socket servers with thread pinning issues, but the description is a straightforward streaming scan — the primary issue is RAM bandwidth saturation, not NUMA topology." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Address translation + access path"
  hue="var(--d-hardware)"
  perRow={4}
  nodes={[
    { id: "vaddr", label: "Virtual address", sub: "load instruction" },
    { id: "tlb", label: "TLB lookup", sub: "virtual → physical" },
    { id: "walk", label: "Page walk", sub: "miss: 5–50 cycles" },
    { id: "cache", label: "Cache lookup", sub: "L1 → L2 → L3 → RAM" },
    { id: "prefetch", label: "Prefetcher", sub: "sequential / stride only" },
  ]}
  edges={[
    { from: "vaddr", to: "tlb" },
    { from: "tlb", to: "cache", label: "hit: 0–1 cyc" },
    { from: "tlb", to: "walk", label: "miss" },
    { from: "walk", to: "cache" },
    { from: "prefetch", to: "cache", label: "pre-loads lines" },
  ]}
  caption="Every access pays TLB translation before any cache check. The prefetcher fills cache lines ahead for strided patterns — but cannot infer pointer-chasing."
/>

<KeyTakeaway>Hardware prefetchers handle sequential and strided patterns but cannot infer pointer-dependent next-addresses. TLB misses add 5–50 cycles before any cache check — huge pages (2 MB) cut TLB pressure 512x for large working sets. MLP lets 8–16 independent misses run in parallel; dependent pointer-chains serialize them. Non-temporal stores bypass cache for write-once data, freeing L3 for hot data and improving streaming bandwidth 20–30%.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-hw-prefetch-retrieval"
  lang="en"
  questions={[
    { q: "A perf-critical graph algorithm shows high L3 miss rate even though the graph fits in L2 cache. What is happening and what are the fixes?", a: "The issue is pointer-chasing with data-dependent access patterns. Even though all graph nodes reside in L2, the order in which they are accessed depends on the values stored inside nodes (the `next` or `neighbor` pointer). The hardware prefetcher watches address patterns — it cannot predict address B before it has read the pointer value at address A that points to B. Each dereference is effectively a serial cache miss, even from L2 (~3 ns each). At 1M dereferences, 3 ns × 1M = 3 ms vs what a sequential scan would cost at 0.3 ms. Fixes in order of impact: (1) Pack graph into a contiguous arena allocator so logically close nodes are physically close — BFS/DFS locality improves dramatically. (2) Use BFS instead of DFS where possible; BFS has better spatial locality because nodes at the same level were inserted near each other. (3) Use software prefetch (__builtin_prefetch) for known-ahead nodes — in BFS, prefetch queue[i+8] while processing queue[i]. (4) Switch to an adjacency-array representation instead of pointer-linked nodes — vertex IDs become array indices, not pointer values, enabling the stride detector." },
    { q: "Explain Memory-Level Parallelism (MLP) and give a concrete code example of how dependent pointer chains defeat it.", a: "MLP is the CPU's ability to have multiple cache misses in flight simultaneously. The load buffer can hold 8–16 outstanding misses (depending on microarchitecture). When the CPU encounters independent load instructions, it issues all of them without waiting for individual results — 8 misses might cost only 350 cycles total instead of 8 × 300 = 2400 cycles. Dependent pointer chains serialize misses because each load instruction requires the result of the previous: `p2 = p1->next` cannot start until p1 is loaded (to get the address of next), and `p3 = p2->next` cannot start until p2 is loaded. Each dereference is a serial round-trip to wherever that node lives. In contrast, an array access with an index array like `arr[idx[0]], arr[idx[1]], ..., arr[idx[7]]` can issue 8 loads in parallel if idx values are already in registers — the CPU knows all 8 addresses immediately and queues them concurrently. The optimization: whenever possible, transform pointer-chasing into index-based access using a contiguous node arena and index arrays so the CPU can exploit MLP. This is why high-performance graph engines (e.g. GraphBLAS, SSSP in database engines) use CSR (Compressed Sparse Row) format with integer adjacency arrays rather than pointer-linked node objects." },
  ]}
/>

<Recap lang="en">
The hardware prefetcher accelerates sequential and strided access — it cannot infer the next address when it depends on data values (pointer-chasing in linked structures, graphs). Beyond L1/L2/L3, every access also pays a TLB translation cost; a miss adds 5–50 cycles before any cache level is checked. Huge pages (2 MB on x86) cut TLB pressure 512x for large working sets. Memory-level parallelism lets the CPU queue 8–16 independent misses simultaneously — dependent pointer-chains serialize all misses into sequential round-trips. Non-temporal stores bypass cache for write-once streaming workloads, protecting L3 for hot data. Cache associativity conflict misses occur when hot addresses hash to the same cache set; fix by padding arrays by a cache line to break alignment patterns. Now when you see a graph or tree traversal that stays slow even after you confirm the data fits in cache, remember: the prefetcher cannot help — the fix is to restructure access order or use software prefetch hints a few steps ahead.
</Recap>
