---
concepts:
- memory-hierarchy
- cache-line
- false-sharing
- branch-prediction
- soa-vs-aos
- perf-stat-diagnosis
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://en.wikipedia.org/wiki/CPU_cache
- https://en.algorithmica.org/hpc/pipelining/branching/
- https://www.cs.cornell.edu/courses/cs3110/2012sp/lectures/lec25-locality/lec25.html
status: ready
summary: Multiple-choice synthesis across the cache-vs-big-O unit — memory hierarchy, cache lines, false sharing, branch prediction, data layout, and reading a perf profile.
title: 'Cache vs big-O: multiple-choice review'
track: performance
unit: 03-cache-vs-bigo
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. None is a definition to recite — each is the judgement call you make when a "fast" algorithm loses to a "slow" one and big-O has nothing to say about it.
</Hook>

<Goal lang="en">
Confirm you can connect the memory hierarchy, cache-line behaviour, false sharing, branch prediction, and data layout into one diagnostic instinct: read the profile, name the constant-factor cause, pick the matching fix.
</Goal>

<Quiz
  id="03-cache-vs-bigo-choice-1"
  lessonSlug="03-cache-vs-bigo"
  lang="en"
  question="A linked list of 1M nodes iterates ~17x slower than an array of 1M ints, though both are O(N). Which statement best explains the gap?"
  choices={[
    { label: "Array elements are contiguous, so the prefetcher fills cache lines ahead of the loop and most reads hit L1 (~1 ns). Each list node sits at a random heap address, so every `.next` dereference is a cold miss to RAM (~70–100 ns). Big-O hides this 100x constant.", correct: true },
    { label: "Big-O is simply wrong for linked structures.", misconception: "Big-O is asymptotic and correct — both are O(N). What it omits is the per-operation constant, which the memory hierarchy sets: ~1 ns for a cache hit vs ~100 ns for a RAM miss." },
    { label: "The linked list executes more instructions per node.", misconception: "Instruction count per node is comparable; the CPU stalls waiting on memory, not on extra arithmetic. The cost is miss latency, not op count." },
    { label: "The array fits in cache and the list does not — purely a capacity issue.", misconception: "Even when both working sets exceed cache, the array still wins: sequential access is prefetched while pointer-chasing is data-dependent and unpredictable. It is access pattern, not just capacity." },
  ]}
/>

<Quiz
  id="03-cache-vs-bigo-choice-2"
  lessonSlug="03-cache-vs-bigo"
  lang="en"
  question="A team flips a matrix-multiply inner loop from row-major to column-major order on a 10 000×10 000 double matrix and sees a ~9x slowdown. The arithmetic is identical. Root cause?"
  choices={[
    { label: "Column-major stepping advances 10 000×8 = 80 000 bytes per access — far past the 64-byte cache line just loaded. Almost every read lands in a fresh, un-prefetched line and pays full RAM latency. Same loads, ~100x cost each.", correct: true },
    { label: "The big-O complexity got worse than O(N²).", misconception: "Both loop orders are O(N²) with identical operation counts. The regression lives entirely in the constant factor — cache-miss rate — not the asymptotic class." },
    { label: "Column access needs a different compiler flag to vectorise.", misconception: "No flag fixes an 80 000-byte stride. The access pattern itself defeats the prefetcher and SIMD; the fix is loop reordering or cache-oblivious tiling, not a flag." },
    { label: "Columns are longer than rows in a tall matrix.", misconception: "It is a square 10k×10k matrix — rows and columns are equal length. Memory layout (row-major storage), not dimension, drives the gap." },
  ]}
/>

<Quiz
  id="03-cache-vs-bigo-choice-3"
  lessonSlug="03-cache-vs-bigo"
  lang="en"
  question="A counter array is made lock-free with atomics and gets slower than the locked version as threads scale. perf shows IPC 0.4 and a 72% cache-miss rate. Diagnosis?"
  choices={[
    { label: "False sharing. Several per-thread counters share one 64-byte line; each atomic write forces a MESI invalidation of that line in every other core, so the line ping-pongs at L3 latency. Fix: pad each counter to its own cache line.", correct: true },
    { label: "Lock contention — the atomics are effectively a spinlock.", misconception: "Atomics are not locks. Lock contention shows as off-CPU wait time; this signature — high CPU, IPC collapse, high cache-miss rate — is memory-bound coherency traffic, i.e. false sharing." },
    { label: "The counter array overflowed L3 capacity.", misconception: "A handful of uint64 counters is ~128 bytes — trivially L1-resident. The defect is multiple cores writing the same 64-byte line, not capacity." },
    { label: "Branch mispredictions in the atomic increment.", misconception: "An atomic add has no data-dependent branch. Branch misses raise the branch-miss counter, not the cache-miss counter; the 72% figure points squarely at memory." },
  ]}
/>

<Quiz
  id="03-cache-vs-bigo-choice-4"
  lessonSlug="03-cache-vs-bigo"
  lang="en"
  question="Sorting an array before summing the elements above a threshold yields a 2–3x speedup, despite the extra O(N log N) sort. Why does it pay back?"
  choices={[
    { label: "Sorting groups all sub-threshold elements together, then all above. The data-dependent branch becomes a long run of 'not-taken' then 'taken', so the predictor reaches ~99% accuracy and avoids the 10–30 cycle pipeline flush it paid on ~50% of iterations.", correct: true },
    { label: "Sorting reduces how many elements must be compared.", misconception: "Every element is still compared to the threshold after sorting — the comparison count is unchanged. The win is branch predictability, not fewer comparisons." },
    { label: "Sorted data is more cache-friendly to scan.", misconception: "A linear scan of an array is already cache-friendly whether sorted or not. The dominant effect here is branch prediction, not memory locality." },
    { label: "The compiler auto-vectorises only sorted loops.", misconception: "Auto-vectorisation does not depend on sort order. The documented, architecture-independent speedup comes from turning an unpredictable branch into a predictable one." },
  ]}
/>

<Quiz
  id="03-cache-vs-bigo-choice-5"
  lessonSlug="03-cache-vs-bigo"
  lang="en"
  question="A hot ML loop runs 5x slower than a reference C++ port; perf shows IPC 0.8 and 25% L3 miss rate. Someone proposes adding SIMD intrinsics. What is the correct first move?"
  choices={[
    { label: "Fix the layout first: the slow version stores activations as AoS, where fields are interleaved, so SIMD would need 10x-slower gather/scatter. Convert to SoA (one flat array per field); IPC rises toward ~3.0 and L3 misses drop, and only then does SIMD help.", correct: true },
    { label: "Add AVX intrinsics directly to the existing AoS loop.", misconception: "SIMD on AoS data forces gather/scatter (~10x a contiguous load) and does nothing for a memory-bound loop. IPC 0.8 + 25% L3 miss is a layout problem; SIMD on bad layout buys nothing." },
    { label: "Add -O3 -march=native and stop.", misconception: "Flags are worth checking, but they cannot turn an interleaved AoS layout into contiguous SIMD-friendly data. The signature is memory-bound, not under-optimised compute." },
    { label: "Rewrite the algorithm — it must be a hidden O(N²).", misconception: "Both versions run the same algorithm; a 5x gap with high L3 miss rate is a constant-factor layout cost, not an asymptotic difference." },
  ]}
/>

<Quiz
  id="03-cache-vs-bigo-choice-6"
  lessonSlug="03-cache-vs-bigo"
  lang="en"
  question="perf stat on a hot loop reports IPC 0.40, branch-miss 25%, and LLC-miss 47%. You can fix one thing first. Which, and why?"
  choices={[
    { label: "The 47% LLC miss — nearly half of L3 lookups go to RAM, the single largest stall source. Restructure data (SoA, pack hot fields, tile so the working set fits L3) for the biggest single win; then attack the 25% branch miss. IPC is the downstream symptom of both.", correct: true },
    { label: "The branch miss — pipeline flushes always dominate.", misconception: "A 10–30 cycle flush is real, but a RAM miss is ~300 cycles and 47% of L3 lookups miss. The memory stall is the larger lever here; fix layout first, then branches." },
    { label: "Raise IPC directly by issuing more instructions.", misconception: "IPC is not a knob — it is the result of stalls. It is 0.40 because the CPU waits on memory and mispredicted branches. Remove those causes and IPC rises on its own." },
    { label: "Switch to a lower-big-O algorithm.", misconception: "Nothing in the profile points to the asymptotic class; it points to constant factors — layout and branch behaviour. A 'better' big-O with worse locality could easily be slower." },
  ]}
/>

<Recap lang="en">
The unit's through-line is one decision tree: big-O sizes how cost grows with N, but the memory hierarchy sets the constant — and that constant swings ~100x with layout. Contiguous beats pointer-chasing; loop order must match storage order; per-thread writes need their own cache line; unpredictable branches want sorting or branchless code; one-field-at-a-time work wants SoA. When a profile is in front of you, read miss rates and IPC first, name the constant-factor cause, and apply the matching fix — measure, never guess.
</Recap>
