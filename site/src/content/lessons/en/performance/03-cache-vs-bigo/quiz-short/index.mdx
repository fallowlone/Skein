---
concepts:
- memory-hierarchy
- false-sharing
- branch-prediction
- soa-vs-aos
- tlb-and-prefetcher
- perf-stat-loop
estMin: 14
lang: en
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://en.wikipedia.org/wiki/CPU_cache
- https://en.algorithmica.org/hpc/pipelining/branching/
- https://www.cs.cornell.edu/courses/cs3110/2012sp/lectures/lec25-locality/lec25.html
status: ready
summary: Free-recall prompts across the cache-vs-big-O unit. Answer each from memory first, then reveal the model answer and compare.
title: 'Cache vs big-O: free-recall review'
track: performance
unit: 03-cache-vs-bigo
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory before you open the model answer — the effort of recall is what fixes the mental model, not the recognition of a right-looking option.
</Hook>

<Goal lang="en">
Reconstruct the unit's spine without looking back: why allocation pattern beats operation count, how the cache line creates false sharing, when sorting beats branchless code, why SoA enables SIMD, and the perf-stat loop that ties it together.
</Goal>

<RetrievalDrawer
  client:visible
  id="03-cache-vs-bigo-short-retrieval"
  lang="en"
  questions={[
    { q: "Why can an O(N) scan of a contiguous array beat an O(log N) tree traversal in production, and where does that stop being true?", a: "Big-O counts operations but is silent on the constant in front of N, which the memory hierarchy sets: an L1 hit is ~1 ns, a RAM miss is ~70–100 ns — a 100x gap. An array scan is sequential, so the hardware prefetcher loads cache lines ahead and most reads hit L1; a balanced tree scatters nodes across the heap, so each level is a pointer dereference to a likely-cold line, costing a full RAM round-trip. With ~17 misses per lookup vs a tightly prefetched scan, the O(N) array can win for N into the millions. It stops being true once the array no longer fits in cache AND N is large enough that log N savings outweigh the per-step constant — so the crossover is workload- and size-dependent. The rule: measure at the real N on the real hardware before trusting the asymptotic ranking." },
    { q: "Explain false sharing: how the MESI protocol turns independent per-thread writes into a performance bug, and how you detect and fix it.", a: "Cache lines are 64-byte ownership units. Under MESI, when one core writes any byte of a line it takes the line Modified and marks every other core's copy Invalid; those cores must re-fetch before their next access. False sharing is when two threads write to different variables that happen to share one line: logically independent, but every write invalidates the other thread's line, so it ping-pongs between cores at ~30–50 cycles per bounce. Lock-free code can end up slower than locked. Detect it with `perf c2c` (cache-line contention) or the signature of low IPC (0.3–0.6) plus a high cache-miss rate that worsens as thread count rises — distinct from lock contention, which shows as off-CPU wait. Fix by padding each per-thread field to its own 64-byte line (Go: 56 bytes of padding after an 8-byte value; C++ `alignas(64)`; Java `@Contended`; Rust `CachePadded`)." },
    { q: "When does sorting (O(N log N)) beat a branchless rewrite for a data-dependent branch, and when is branchless the better tool?", a: "A 50/50 data-dependent branch mispredicts ~half the time, each miss flushing the pipeline for 10–30 cycles. Sorting groups the outcomes so the predictor sees a long not-taken run then a long taken run, reaching ~99% accuracy — a 2–3x speedup that pays for the sort when N is large or the data is reused across many passes. Sorting is the right tool when you can afford the sort once and query/scan many times, or when ordering has independent value. Branchless code (arithmetic select, CMOV) is better when you cannot reorder the data (it arrives in arbitrary order, single pass), the branch is genuinely unpredictable, and both arms are cheap to compute — branchless always computes both arms, so it loses when the branch is predictable (95%+) or one arm is expensive. The workflow: read `branch-misses / branches` in perf stat; above ~5% on a hot branch, choose sort or branchless by whether the data can be reordered." },
    { q: "Why does SoA enable SIMD where AoS forces gather/scatter, and what is the trade-off?", a: "SIMD loads a vector register from a contiguous block and operates on 4–16 lanes at once. In AoS (struct Point {x,y,z}; points[N]) the x values are 12 bytes apart, so a single load returns mixed fields — you must use gather instructions to collect just the x lanes, ~10x slower than a contiguous load, and you also drag y and z into cache unused. In SoA (float xs[N], ys[N], zs[N]) the x values are contiguous, so one SIMD load fills the register with 8 x-values and y/z are never touched, saving two-thirds of the bandwidth for an x-only loop. The trade-off: AoS is better when you access all fields of one element together (encapsulation, single-object APIs); SoA is better for bulk operations over one field at a time (ML inference, game ECS, columnar databases). Common pattern: keep AoS at the API surface, convert to SoA internally for the hot loop." },
    { q: "Beyond cache hit rate, what two constraints limit a memory-bound loop, and how do you tell them apart from cache misses?", a: "Memory bandwidth and NUMA topology. (1) Bandwidth: a loop streaming through tens of GB saturates the RAM bandwidth ceiling (~50–100 GB/s on DDR5) regardless of cache behaviour — a larger L3 gives no speedup because the data is read once and discarded. The tell: high L3 miss rate AND huge data volume; doubling L3 does nothing. Fixes: shrink data (smaller types, projection, compression), non-temporal stores to bypass cache for write-once data. (2) NUMA: on a multi-socket server, a thread accessing memory on a remote socket pays ~120–150 ns vs ~70 ns local — a ~1.7x penalty that perf often misreports as L3 misses. The tell: high apparent miss rate on a small working set (fits in L3) on multi-socket hardware; confirm with numastat. Fix: pin threads and allocate on the local node (numactl, taskset). Both are separate axes from a true capacity/locality cache miss, which you confirm by checking data volume and socket topology." },
    { q: "State the performance-engineering loop for this unit and which metric points to which class of fix.", a: "Measure → identify → matching fix → re-measure. Run `perf stat -e cache-misses,cache-references,branch-misses,branches,cycles,instructions`. Read it: IPC below ~1.0 means the CPU is stalled (memory or branches); LLC miss rate above ~20% means a layout/locality problem (working set exceeds L3, AoS, or pointer-chasing) → fix with SoA, struct splitting, contiguous arenas, or cache-oblivious tiling; branch-miss rate above ~5% on a hot branch means an unpredictable data-dependent branch → fix with sorting or branchless code; high LLC miss with huge data volume means bandwidth-bound → shrink data or use non-temporal stores; healthy miss rates but low IPC from instruction issue means a SIMD opportunity → SoA + auto-vectorise. Then re-measure to confirm the fix and surface the next bottleneck. The discipline: start from measurement, not from algorithm choice — big-O is the scalability model, the profile is the performance model." },
  ]}
/>

<Recap lang="en">
If you reconstructed each answer from memory, you hold the unit's spine: the memory hierarchy sets a ~100x constant that big-O ignores; cache lines are 64-byte ownership units that turn neighbouring writes into false sharing; sorting and branchless are two tools for the same unpredictable-branch problem; SoA unlocks SIMD and bandwidth that AoS blocks; and the perf-stat loop maps each metric to its matching fix. Measure first, name the constant-factor cause, fix layout or branches, re-measure.
</Recap>
