---
concepts:
- cache-hierarchy
- cache-line
- hardware-prefetcher
- spatial-locality
- array-vs-linked-list
deepensInto: []
estMin: 14
lang: en
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-memory-hierarchy
sources:
- https://en.wikipedia.org/wiki/CPU_cache
- https://www.geeksforgeeks.org/dsa/why-arrays-have-better-cache-locality-than-linked-list/
- https://www.cs.cornell.edu/courses/cs3110/2012sp/lectures/lec25-locality/lec25.html
spiral:
- latency
status: ready
summary: Modern CPUs have five storage levels separated by 100x latency gaps — the
  level where your data lives decides wall-clock cost more than operation count does.
title: 'Memory hierarchy: why the same O(N) loop can be 17x slower'
track: performance
unit: 03-cache-vs-bigo
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";

<Hook>
Two engineers benchmark a list of 1 million numbers. Both loops are O(N). One takes 2 ms, the other 35 ms. Big-O predicts them equal. The CPU disagrees — by 17x.
</Hook>

<Crux>Big-O counts operations; the memory hierarchy sets the price. L1 = 1 ns, RAM = 100 ns — a 100x gap that swamps algorithmic differences.</Crux>

<Explanation>

## The memory hierarchy

A modern CPU is not a uniform memory machine. Data does not sit in one place — it flows through five levels, each faster and smaller than the one below it.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Level</th>
        <th class="border border-border px-3 py-2 text-left">Size (typical)</th>
        <th class="border border-border px-3 py-2 text-left">Latency</th>
        <th class="border border-border px-3 py-2 text-left">Who controls it</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Register</td>
        <td class="border border-border px-3 py-2">~16 × 8 bytes</td>
        <td class="border border-border px-3 py-2">&lt;1 cycle</td>
        <td class="border border-border px-3 py-2">Compiler / CPU</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">L1 cache</td>
        <td class="border border-border px-3 py-2">32–64 KB per core</td>
        <td class="border border-border px-3 py-2">~1 ns (3–5 cycles)</td>
        <td class="border border-border px-3 py-2">Hardware</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">L2 cache</td>
        <td class="border border-border px-3 py-2">256 KB–2 MB per core</td>
        <td class="border border-border px-3 py-2">~3 ns (10–15 cycles)</td>
        <td class="border border-border px-3 py-2">Hardware</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">L3 cache</td>
        <td class="border border-border px-3 py-2">4–64 MB shared</td>
        <td class="border border-border px-3 py-2">~10 ns (30–50 cycles)</td>
        <td class="border border-border px-3 py-2">Hardware</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Main memory (DDR5)</td>
        <td class="border border-border px-3 py-2">GBs</td>
        <td class="border border-border px-3 py-2">~70–100 ns</td>
        <td class="border border-border px-3 py-2">OS + allocator</td>
      </tr>
    </tbody>
  </table>
</div>

The number that matters most: L1 is 100x faster than RAM. A single cache miss to main memory costs as much as ~30 floating-point multiplications would take in registers.

## The library metaphor

Think of a library with five storage zones. 10 books are on your desk (L1), 100 on a nearby shelf (L2), 10 000 in the same room (L3), and 10 million in a warehouse two blocks away (RAM). A "fast" algorithm that needs to walk to the warehouse for every step loses to a "slow" algorithm that processes every book on the desk in order. Distance matters more than operation count.

## Cache lines: the unit of transfer

Data does not move between RAM and cache one byte at a time. It moves in cache lines — 64 bytes on x86 and most ARM chips. Reading one byte at address X loads bytes X through X+63 into cache. Every subsequent read of any byte in that range is a free L1 hit.

This is why sequential access patterns are fast: after the first load, the next seven or eight reads cost almost nothing. The hardware fires proactively — before you ask for byte 64, the prefetcher already loaded that line.

## Array vs linked list: the canonical case

Both data structures hold a million integers. Both require N iterations. Big-O says equal. The benchmark says 17x different.

- An array is one contiguous allocation. Elements sit at fixed offsets: address, address+8, address+16, ... The prefetcher reads cache lines ahead of your loop. Each element is in L1 by the time the loop reaches it. Cost per element: ~1 ns.
- A linked list allocates each node separately on the heap. The `.next` pointer in each node points to a random location. The prefetcher cannot predict this. Every pointer dereference likely hits a cold cache line at L3 or RAM. Cost per node: ~100 ns.

1 000 000 × 100 ns = 100 ms. The array case: ~1 ms. The 17x benchmark gap is conservative — it assumes some hits from recently-touched nodes. At larger sizes the gap widens.

<Inset kind="why" lang="en">
Big-O is defined as asymptotic — it tells you how cost grows with N, not the absolute cost. The hidden constant in front of the N is what the memory hierarchy controls. A well-placed O(N) can have a constant of 1 ns; a pointer-chasing O(log N) can have a constant of 100 ns per step, making the O(N) win until N is in the millions.
</Inset>

<Quiz
  id="01-memory-hierarchy-quiz-1"
  lessonSlug="01-memory-hierarchy"
  lang="en"
  question="Why can an O(N) array iteration beat an O(log N) tree traversal in real benchmarks?"
  choices={[
    { label: "Arrays sit in contiguous memory, keeping data in cache. Trees scatter nodes via pointers, causing a cache miss on every step. The constant per-operation cost of pointer-chasing can exceed the log-N savings.", correct: true },
    { label: "Big-O is wrong for linked structures", misconception: "Big-O is asymptotic and correct; the issue is constant factors hidden inside each operation." },
    { label: "Compilers optimise arrays better", misconception: "Compilers help both; the underlying difference is memory layout, not compiler tricks." },
    { label: "Trees use more energy", misconception: "Energy is correlated but not the cause; the cause is cache miss latency." },
  ]}
/>

<Quiz
  id="01-memory-hierarchy-quiz-2"
  lessonSlug="01-memory-hierarchy"
  lang="en"
  question="What is a cache line?"
  choices={[
    { label: "The unit of data transfer between RAM and CPU cache — typically 64 bytes; reading one byte loads the whole line into cache", correct: true },
    { label: "A graphical row in CPU diagrams", misconception: "Not a UI concept; cache lines are hardware units." },
    { label: "A network packet size", misconception: "Cache lines are memory units; unrelated to networking." },
    { label: "A debugging tool for cache misses", misconception: "Cache lines are the granularity at which hardware moves data, not a tool." },
  ]}
/>

<DragOrder
  id="01-memory-hierarchy-drag-1"
  lessonSlug="01-memory-hierarchy"
  lang="en"
  prompt="Order the levels of memory the CPU accesses, from fastest to slowest:"
  items={[
    { id: "a", label: "Register (inside the CPU core)" },
    { id: "b", label: "L1 cache (per core, ~1 ns)" },
    { id: "c", label: "L2 cache (per core, ~3 ns)" },
    { id: "d", label: "L3 cache (shared, ~10 ns)" },
    { id: "e", label: "Main memory / RAM (~70–100 ns)" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

<MetaphorComplete
  id="01-memory-hierarchy-metaphor-1"
  pieceSlug="01-memory-hierarchy"
  lang="en"
  setup="Fill in the blank: when memory accesses are sequential, the CPU's hardware _______ predicts and pre-loads the next bytes before you ask for them."
  accepted={["prefetcher", "prefetch unit", "prefetch"]}
  canonical="prefetcher (the hardware unit that watches access patterns and pre-loads future cache lines)"
  explanation="The hardware prefetcher detects sequential or strided access patterns. After two or three consecutive misses in the same direction, it starts pre-loading lines ahead of the current position. Sequential array iteration feeds it perfectly. Random pointer-chasing defeats it — every dereference jumps to an address the prefetcher has no way to predict."
/>

</Explanation>

<KeyTakeaway>L1 is 100x faster than RAM. Array iteration stays in cache; pointer-chasing misses on every step. Same O(N), 17x difference in wall-clock time — the data layout, not the algorithm, decides production speed.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-memory-hierarchy-retrieval"
  lang="en"
  questions={[
    { q: "In one paragraph: why is the iteration cost of a linked list of 1 million nodes 17x slower than an array of 1 million numbers, when both are O(N)?", a: "Because each linked list node is allocated separately on the heap and contains a pointer to the next node. Following each pointer jumps to a random memory location — likely cold (not in cache), triggering a full RAM round-trip of 70–100 nanoseconds per node. Array elements sit in contiguous memory; the prefetcher loads cache lines proactively, and most accesses hit L1 at ~1 ns. Multiply by 1 million: 100 ns × 1M = 100 ms for the linked list vs 1 ns × 1M = 1 ms for the array. The 17x ratio in benchmarks accounts for some cache hits in the linked list case, but the order of magnitude is real and widens with dataset size." },
    { q: "What is a cache line, and why does it make sequential access patterns much cheaper than random access patterns?", a: "A cache line is the unit of transfer between main memory and cache — 64 bytes on x86 and most ARM processors. When the CPU loads one byte from address X, the hardware fetches the entire 64-byte line containing X into the L1 cache. Every subsequent read of any byte in that range (X through X+63) is a free L1 hit at ~1 cycle. Sequential access exploits this: consecutive elements in a flat array all fall within recently-loaded cache lines, so the cost per element approaches 1 cycle. Random pointer access lands at unpredictable addresses; the chance of the required line already being in L1 is low, so each access pays the full 70–100 ns RAM penalty. The difference — 1 cycle vs ~300 cycles per access — is what the hardware prefetcher amplifies by pre-loading lines before the code requests them." },
  ]}
/>

<Recap lang="en">
A modern CPU has five memory levels separated by up to 100x latency gaps; data in L1 costs 1 ns, data in RAM costs 70–100 ns. The hardware works in 64-byte cache lines: one read fills a line, making sequential accesses cheap and random accesses expensive. An array of 1 million integers iterates 17x faster than a linked list of the same size because array elements live in contiguous memory — the prefetcher fills cache lines ahead of the loop — while linked list nodes scatter across the heap and each pointer dereference lands in cold memory. Big-O counts operations; the memory hierarchy decides the price per operation. Data layout is the first lever in production performance work.
</Recap>
