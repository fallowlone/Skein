---
concepts:
- row-major
- column-major
- cache-oblivious-blocking
- spatial-locality
- access-pattern
deepensInto: []
estMin: 12
lang: en
lessonType: topic
level: junior
order: 2
prereqs:
- performance/03-cache-vs-bigo/01-memory-hierarchy
slug: 02-row-major-vs-column-major
sources:
- https://en.wikipedia.org/wiki/CPU_cache
- https://www.cs.cornell.edu/courses/cs3110/2012sp/lectures/lec25-locality/lec25.html
- https://ivoroshilin.wordpress.com/2013/02/06/know-your-locality-of-reference-some-techniques-for-keeping-data-in-the-cpu-cache/
spiral:
- latency
status: ready
summary: Changing loop order on the same matrix can produce a 9x wall-clock difference
  — because the step from row to column flips every access from a cache hit to a cache
  miss.
title: 'Row-major vs column-major: access order and the 9x gap'
track: performance
unit: 03-cache-vs-bigo
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import wallClockGap from "./infographics/wall-clock-gap.svg?raw";

<Hook>
Two matrix-multiply loops. Same algorithm. Same 10 000 × 10 000 matrix. Same number of additions and multiplications. One finishes in 300 ms. The other in 2 800 ms. The only difference: the order of two `for` loops.
</Hook>

<Crux>Row-major walks contiguous memory; column-major jumps 80 000 bytes. Same O(N²) — 9x wall-clock gap from cache miss rate.</Crux>

<Explanation>

## How 2D arrays are laid out in memory

Before you tune a single inner loop, you need to know how the language stores your matrix in memory — because that choice determines whether each step is a 1 ns cache hit or a 100 ns round-trip to RAM.

In C, Java, Rust, Python (NumPy default), and most other languages, a 2D array `arr[rows][cols]` is stored in **row-major** order: all elements of row 0 are contiguous, then all of row 1, and so on.

For a 10 000 × 10 000 matrix of 8-byte doubles, the layout is:

```
arr[0][0], arr[0][1], ..., arr[0][9999],   ← row 0: bytes 0–79999
arr[1][0], arr[1][1], ..., arr[1][9999],   ← row 1: bytes 80000–159999
...
```

## What happens inside each loop order

**Row-major iteration** (`for i: for j: arr[i][j]`):
- Accesses `arr[i][0]`, `arr[i][1]`, `arr[i][2]`, ...
- Each step moves 8 bytes forward in memory.
- A cache line is 64 bytes (8 doubles). The first read loads 8 elements at once; the next 7 reads are free L1 hits.
- The prefetcher detects the sequential pattern and pre-loads the next line ahead.
- Cost per element: ~1 ns.

**Column-major iteration** (`for j: for i: arr[i][j]`):
- Accesses `arr[0][j]`, `arr[1][j]`, `arr[2][j]`, ...
- Each step moves 10 000 × 8 = **80 000 bytes** forward — well past any loaded cache line.
- Every access is in a fresh cache line that was not prefetched.
- Cost per element: ~70–100 ns (full RAM latency).

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Loop order</th>
        <th class="border border-border px-3 py-2 text-left">Step size in memory</th>
        <th class="border border-border px-3 py-2 text-left">Cache behaviour</th>
        <th class="border border-border px-3 py-2 text-left">Time (10k×10k doubles)</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>for i: for j: arr[i][j]</code></td>
        <td class="border border-border px-3 py-2">8 bytes (sequential)</td>
        <td class="border border-border px-3 py-2">7 of 8 accesses hit L1</td>
        <td class="border border-border px-3 py-2">~300 ms</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>for j: for i: arr[i][j]</code></td>
        <td class="border border-border px-3 py-2">80 000 bytes (random)</td>
        <td class="border border-border px-3 py-2">Almost every access misses</td>
        <td class="border border-border px-3 py-2">~2 800 ms</td>
      </tr>
    </tbody>
  </table>
</div>

The instruction count is identical. The arithmetic is identical. The 9x difference is entirely from cache-miss latency.

<Infographic
  svg={wallClockGap}
  label="Column chart comparing wall-clock time for one pass over a 10 000 × 10 000 matrix of doubles: row-major iteration takes about 300 ms, column-major about 2800 ms — a roughly 9x gap with no change to the algorithm."
  caption="Same O(N²), same arithmetic — the bar height is pure cache-miss latency. Column-major's 80 000-byte stride turns nearly every access into a RAM round-trip."
  hue="var(--d-hardware)"
/>

## Fortran exception and language conventions

Fortran stores arrays in **column-major** order — all elements of column 0 first, then column 1. NumPy has a Fortran-order option (`order='F'`). If your code or library uses Fortran layout, the loop that is "obviously right" in C is the worst case in Fortran. Always verify your language's memory layout before tuning inner loops.

## Cache-oblivious blocking

Sometimes both loop orders are necessary — for example in matrix transposition or certain linear algebra routines. The fix is **cache-oblivious blocking** (also called tiling): divide the matrix into small tiles (~64 rows × 64 columns, so each tile fits in L2), and process each tile before moving to the next. Within a tile, accesses are in order; the whole tile fits in cache while being processed, and both row and column dimensions see mostly cache hits.

<Inset kind="why" lang="en">
The 9x example uses a 10 000 × 10 000 matrix because it guarantees the working set far exceeds L3 cache. On smaller matrices (100 × 100) the entire matrix fits in L1 and both loop orders are fast. The performance cliff only appears when the matrix outgrows the cache — which is the common production scenario for real data processing workloads.
</Inset>

<Quiz
  id="02-row-major-quiz-1"
  lessonSlug="02-row-major-vs-column-major"
  lang="en"
  question="A 10 000 × 10 000 matrix of doubles is stored row-major. The inner loop iterates columns (arr[i][j], j from 0 to N). Why is this fast?"
  choices={[
    { label: "Consecutive j values access consecutive memory addresses. A single 64-byte cache line holds 8 doubles. The prefetcher loads the next line ahead. Most accesses hit L1 at 1 ns.", correct: true },
    { label: "Columns are smaller than rows", misconception: "For a square matrix, rows and columns have the same length. Layout — not size — is the factor." },
    { label: "The CPU cores share L3, which covers the matrix", misconception: "For a 10k×10k matrix of doubles that is 800 MB — far beyond any L3. Locality, not L3 size, is the reason." },
    { label: "Column iteration uses SIMD automatically", misconception: "SIMD can help, but it requires contiguous data regardless. The benefit of row-major iteration is contiguous access, which allows SIMD — but the question is about cache, not SIMD." },
  ]}
/>

<Quiz
  id="02-row-major-quiz-2"
  lessonSlug="02-row-major-vs-column-major"
  lang="en"
  question="A team switches a matrix multiply from row-major to column-major loop order and observes a 9x slowdown with no algorithm change. The root cause is:"
  choices={[
    { label: "Column-major iteration jumps 80 000 bytes per step on a 10k-column matrix. Every step lands in a new cache line. Every access pays full RAM latency (~100 ns). The same number of loads, but each costs 100× more.", correct: true },
    { label: "The CPU executes fewer instructions per second for column access", misconception: "Instruction count is the same; IPC drops because the CPU stalls waiting for memory, not because fewer instructions are issued." },
    { label: "The algorithm's big-O complexity changed", misconception: "Both loop orders have identical O(N²) complexity — the algorithm is unchanged. The cost is in the constant factor, not the asymptotic class." },
    { label: "Column-major requires a different compiler optimisation flag", misconception: "Compiler flags can help with vectorisation, but the fundamental issue is the memory access stride, which no flag fixes." },
  ]}
/>

<DragOrder
  id="02-row-major-drag-1"
  lessonSlug="02-row-major-vs-column-major"
  lang="en"
  prompt="Trace what happens when row-major code accesses arr[i][0] through arr[i][7] (8 consecutive doubles, 64 bytes total):"
  items={[
    { id: "a", label: "CPU requests arr[i][0] (address X)" },
    { id: "b", label: "Cache miss: load 64-byte line at X into L1 (costs ~100 ns once)" },
    { id: "c", label: "arr[i][0] through arr[i][7] are all in the loaded line" },
    { id: "d", label: "Accesses arr[i][1] through arr[i][7] each hit L1 (~1 cycle each)" },
    { id: "e", label: "Prefetcher fires and loads the next 64-byte line before it is needed" },
    { id: "f", label: "arr[i][8] through arr[i][15] arrive with zero wait" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f"]}
/>

</Explanation>

<DiagramFrame
  label="Same row-major grid, two traversal orders"
  hue="var(--d-hardware)"
  caption="Row-major walks one cache line (8 bytes/step); column-major jumps a whole row (80 000 bytes/step), missing every time."
>
  <svg viewBox="0 0 460 230" width="460" height="230" role="img" aria-label="Row-major vs column-major traversal on a row-major grid" style="display:block;margin:0 auto;max-width:100%;height:auto;font-family:var(--font-mono);">
    {/* Row-major: walk along a row */}
    <text x="100" y="18" text-anchor="middle" font-size="12" font-family="var(--font-body)" fill="var(--ink)">Row-major: for i: for j</text>
    {[0,1,2,3].map((r) => [0,1,2,3].map((c) => (
      <rect x={26 + c*38} y={30 + r*38} width="34" height="34" rx="4"
        fill={r===0 ? "var(--accent)" : "var(--card-2)"} opacity={r===0 ? "0.35" : "1"}
        stroke="var(--hairline-strong)" stroke-width="0.8" />
    )))}
    <line x1="43" y1="47" x2="195" y2="47" stroke="var(--accent)" stroke-width="2" />
    <text x="100" y="200" text-anchor="middle" font-size="10.5" fill="var(--muted)">+8 B/step — 7 of 8 hit L1</text>

    {/* Column-major: walk down a column */}
    <text x="358" y="18" text-anchor="middle" font-size="12" font-family="var(--font-body)" fill="var(--ink)">Column-major: for j: for i</text>
    {[0,1,2,3].map((r) => [0,1,2,3].map((c) => (
      <rect x={284 + c*38} y={30 + r*38} width="34" height="34" rx="4"
        fill={c===0 ? "var(--d-hardware, var(--accent))" : "var(--card-2)"} opacity={c===0 ? "0.35" : "1"}
        stroke="var(--hairline-strong)" stroke-width="0.8" />
    )))}
    <line x1="301" y1="47" x2="301" y2="199" stroke="var(--ink)" stroke-width="2" />
    <text x="358" y="218" text-anchor="middle" font-size="10.5" fill="var(--muted)">+80 000 B/step — miss every step</text>
  </svg>
</DiagramFrame>

<KeyTakeaway>Row-major iteration on a row-major matrix steps 8 bytes at a time — 7 of 8 reads hit L1. Column-major iteration steps 80 000 bytes — every read misses cache. Same O(N²), 9x wall-clock difference. Know your language's memory layout and orient loops to match it.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-row-major-retrieval"
  lang="en"
  questions={[
    { q: "Why is row-major matrix iteration 9x faster than column-major on a 10 000×10 000 matrix, when both perform the same number of element accesses?", a: "Because of cache line behaviour. The matrix is stored row by row in memory: all of row 0, then all of row 1, etc. A cache line is 64 bytes (8 doubles). Row-major iteration touches arr[i][0], arr[i][1], ..., arr[i][N-1] consecutively — every 8 reads transfers one cache line; the prefetcher loads the next line ahead of time, so hits dominate. Column-major iteration touches arr[0][j], arr[1][j], ..., arr[N-1][j] — each step jumps N×8 = 80 000 bytes, into a fresh cache line that was not loaded. Every read is a miss; each costs 30–100 cycles. The total number of arithmetic operations is identical; wall-clock time differs by 9x because column-major pays full RAM latency on essentially every load. Fix: change loop order, or use cache-oblivious tiling so each tile fits in L2." },
    { q: "What is cache-oblivious blocking and when should you use it?", a: "Cache-oblivious blocking (tiling) divides a matrix into small tiles — typically sized so each tile fits in L2 cache (~256 KB to 1 MB) — and processes each tile completely before moving to the next. This means that within a tile, all accesses see mostly L2 hits regardless of whether you are iterating rows or columns. The algorithm does not need to know the specific cache sizes; the natural recursion ensures that at some level of recursion the sub-problem fits in some cache level. Use it when your algorithm needs to access a matrix in two dimensions (e.g. matrix transposition, matrix multiplication, FFT), especially when the full matrix exceeds L3 cache. For simple single-direction scans where loop order can be chosen to match memory layout, a single loop reorder is simpler and just as effective." },
  ]}
/>

<Recap lang="en">
In a row-major language (C, Java, Rust, Python/NumPy), elements of the same row occupy consecutive addresses. A 64-byte cache line holds 8 doubles; sequential (row-major) iteration reads 8 elements per cache miss. Column-major iteration on the same array steps 80 000 bytes per element on a 10 000-column matrix, landing in a fresh cache line every time and paying full RAM latency on every load. The result is a 9x wall-clock difference with no algorithmic change. Always know your language's memory layout and align loop order to match it; use cache-oblivious blocking when both access directions are unavoidable. Now when you see a matrix-processing function that is suspiciously slow, flip the loop order before reaching for any other optimization — it takes one line to fix and one benchmark to confirm.
</Recap>
