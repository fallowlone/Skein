infographic compare-quadrant-quarter-simple-card
data
  title When denormalisation is the right answer
  desc Latency SLO vs read:write ratio — only one corner earns it
  compares
    - label SLO <50ms, read-heavy: denormalise
    - label SLO <50ms, balanced: eager load
    - label SLO >100ms, read-heavy: eager load
    - label SLO >100ms, balanced: eager load
theme
  palette #f43f5e #6366f1 #14b8a6 #0ea5e9
