---
concepts:
- nplus1
- round-trip
- lazy-loading
- orm
- batch-loading
deepensInto: []
estMin: 10
lang: en
lessonType: topic
level: junior
order: 1
prereqs:
- performance/02-hot-paths/01-what-makes-a-hot-path
slug: 01-what-is-nplus1
sources:
- https://www.prisma.io/docs/orm/prisma-client/queries/relation-queries
- https://guides.rubyonrails.org/active_record_querying.html
- https://docs.djangoproject.com/en/5.0/ref/models/querysets/
spiral:
- latency
status: ready
summary: One screen renders into 50 database queries because the ORM loads each related
  row on demand. The fix is not a faster query — it is fewer queries.
title: 'N+1: one logical operation, many round-trips'
track: performance
unit: 05-n-plus-one
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import queryExplosion from "./infographics/query-explosion.svg?raw";

<Hook>
A page is slow at 800 ms. The CPU profile looks fine — no wide functions. Sven opens the database query log and counts 51 queries for one page load. The profiler never lied; the bottleneck is not in any single function. It is spread across 51 small round-trips. By the end of this lesson you will know exactly how that happens and what one word fixes it.
</Hook>

<Crux>One trip = fast. N+1 trips = slow. Each round-trip pays RTT; one query of 100 rows beats 100 queries of one row.</Crux>

<Explanation>

## What the N+1 problem is

The user requests "show me a list of orders with their customer names." The ORM executes one query for orders, then one query per order to fetch the customer — 1 + N queries. With 50 orders on the page that is 51 database round-trips for one render.

Each round-trip pays network latency (1–5 ms intra-DC, 10–50 ms cross-region) plus query overhead. A page that should take 20 ms takes 500 ms — and the engineer cannot find the problem in a CPU profile, because the cost is distributed across many fast calls. No single function is slow.

## Why ORMs produce N+1 by default

Object-relational mappers expose a model where each object has relationships accessed like plain properties. `order.customer` reads as if it is a plain attribute; under the hood the ORM triggers a query if the customer is not yet loaded. This is called **lazy loading** — convenient because you do not declare what you need up front, but it is the source of N+1.

Loop over 50 orders, read `order.customer.name` on each one, and the ORM fires 50 queries without you writing 50 queries.

## The pantry metaphor

Cooking pasta and need garlic, oil, salt, and basil:

- **Option A (batch):** walk to the pantry once, grab all four, walk back. One trip.
- **Option B (N+1):** walk for garlic, return; walk for oil, return; walk for salt, return; walk for basil, return. Four trips.

Same work — four trips versus one. The N+1 pattern is option B. The fix is option A: reduce the walks.

The unit of cost here is the round-trip: the time from when your code asks for data to when the data is in your hands. RTT, query parse, planner overhead, lock acquisition, result encoding — all paid per round-trip. One query of 100 rows pays it once; 100 queries pay it 100 times.

## A concrete example

```
// ORM code that looks innocent:
const orders = await Order.findAll({ where: { userId } });
for (const order of orders) {
  const customer = await order.getCustomer(); // fires one query per order
  render(order, customer);
}

// Query log:
// SELECT * FROM orders WHERE user_id = 42 LIMIT 50      — 1 query
// SELECT * FROM customers WHERE id = 1                   — 1 query
// SELECT * FROM customers WHERE id = 2                   — 1 query
// ...
// SELECT * FROM customers WHERE id = 50                  — 1 query
// Total: 51 queries × ~15 ms RTT = 765 ms
```

The fix in Prisma or most ORMs is one word — `include`:

```
const orders = await prisma.order.findMany({
  where: { userId },
  include: { customer: true },
});
// Now: 2 queries — one for orders, one for all customers via IN (...)
// Total: ~20 ms
```

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Approach</th>
        <th class="border border-border px-3 py-2 text-left">Queries</th>
        <th class="border border-border px-3 py-2 text-left">Time (intra-DC, 15 ms RTT)</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Lazy (N+1)</td>
        <td class="border border-border px-3 py-2">51</td>
        <td class="border border-border px-3 py-2">~765 ms</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Eager / include</td>
        <td class="border border-border px-3 py-2">2</td>
        <td class="border border-border px-3 py-2">~30 ms</td>
      </tr>
    </tbody>
  </table>
</div>

## Nested N+1: the problem compounds

A dashboard renders teams → projects → members. Naive ORM:

- 1 query for teams
- N queries for projects (one per team)
- N×M queries for members (one per project)

With 10 teams × 5 projects × 8 members: 1 + 10 + 50 = 61 queries. Fix with nested eager loading: 3 queries total. Render time drops from 1.5 s to 80 ms.

<Infographic
  svg={queryExplosion}
  label="Column chart of database queries per page render. The simple page drops from 51 lazy queries to 2 with eager loading; the nested dashboard drops from 61 to 3 — the eager bars collapse to almost nothing."
  caption="The same render, lazy vs eager: the fix is not a faster query — it is far fewer of them. Both the simple (51→2) and nested (61→3) cases collapse to one or two batched round-trips."
  hue="var(--d-infra)"
/>

<Inset kind="why" lang="en">
The N+1 name reflects the worst case: 1 query for the parent collection plus 1 per parent = 1 + N. The term entered mainstream programming around 2003–2005 with the rise of ORMs like Active Record (Rails 2004) and Hibernate (Java). The shape reappears in every new data-access framework: Prisma added `include` in 2020; every modern ORM ships it.
</Inset>

<Quiz
  id="01-what-is-nplus1-quiz-1"
  lessonSlug="01-what-is-nplus1"
  lang="en"
  question="A page is slow but the CPU profile shows no wide functions. The database query log shows hundreds of small queries. What is the most likely cause?"
  choices={[
    { label: "N+1 query pattern — one logical operation is making many small round-trips. The cost is in the round-trips, not in any single function's compute, so the CPU profile does not surface it.", correct: true },
    { label: "The database is slow", misconception: "Each individual query is fast; the problem is the COUNT of queries, not their speed." },
    { label: "There is a memory leak", misconception: "Memory leaks cause growing RSS, not hundreds of fast queries per request." },
    { label: "The profiler is broken", misconception: "The profiler is correctly showing no CPU hotspot. The bottleneck is round-trip latency, which a CPU profile does not measure." },
  ]}
/>

<Quiz
  id="01-what-is-nplus1-quiz-2"
  lessonSlug="01-what-is-nplus1"
  lang="en"
  question="Why does fetching 100 rows in ONE query beat fetching 100 rows in 100 separate queries?"
  choices={[
    { label: "Each round-trip has a fixed cost: network RTT plus query parse plus planner overhead. One query pays it once; 100 queries pay it 100 times, even if each is individually fast.", correct: true },
    { label: "One query uses less memory", misconception: "Memory is not the primary factor. Round-trip count is." },
    { label: "The database cannot handle 100 queries", misconception: "Databases handle many queries. The problem is per-round-trip overhead multiplied by count." },
    { label: "ORMs forbid multiple queries", misconception: "ORMs allow N queries — that is exactly the N+1 problem." },
  ]}
/>

<DragOrder
  id="01-what-is-nplus1-drag-1"
  lessonSlug="01-what-is-nplus1"
  lang="en"
  prompt="Order the four common fix patterns for N+1, from simplest to most flexible:"
  items={[
    { id: "a", label: "JOIN — fetch parent and child in one query with a SQL join" },
    { id: "b", label: "IN (...) — first query gets parent IDs, second fetches all children WHERE child_id IN (parents)" },
    { id: "c", label: "Eager loading / preload — ORM directive that turns lazy access into one batched query" },
    { id: "d", label: "Batch loader (DataLoader) — collect all child IDs across the request, fire one batched query when the event loop yields" },
  ]}
  correctOrder={["a", "b", "c", "d"]}
/>

<MetaphorComplete
  id="01-what-is-nplus1-metaphor-1"
  pieceSlug="01-what-is-nplus1"
  lang="en"
  setup="Fill in the blank: an N+1 query pattern is N+1 _______ — each one fast on its own, but the total of all the trips is what makes the page slow."
  accepted={["round-trips", "trips", "calls", "queries", "requests"]}
  canonical="round-trips (each paying network latency + query parse + planner overhead)"
  explanation="The unit of cost here is the round-trip: time from your code asking for data to time the data is in your hands. RTT, query parse, planner, lock acquisition, result encoding — all paid per query. One query of 100 rows pays them once; 100 queries pay them 100 times. The optimisation is reducing trips, not making individual trips faster."
/>

</Explanation>

<SequenceDiagram
  label="N+1: one list query, then one query per row"
  caption="50 orders → 1 query for the list + 50 lazy customer queries = 1 + N = 51 round-trips, each paying RTT."
  hue="var(--d-hardware)"
  actors={["App", "DB"]}
  messages={[
    { from: "App", to: "DB", label: "SELECT orders → 50 rows" },
    { from: "App", to: "DB", label: "SELECT customer id=1" },
    { from: "App", to: "DB", label: "SELECT customer id=2" },
    { from: "App", to: "DB", label: "... (47 more)" },
    { from: "App", to: "DB", label: "SELECT customer id=50" },
  ]}
/>

<KeyTakeaway>N+1 happens when one logical operation fires many small round-trips instead of one batch. Lazy loading in any ORM causes it by default. The fix is always structural: declare what you need up front so the ORM fetches in one or two queries.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-what-is-nplus1-retrieval"
  lang="en"
  questions={[
    { q: "In one paragraph: explain why the N+1 problem is hard to find with a CPU profile and where to look instead.", a: "A CPU profile measures where the program spends compute time. The N+1 problem does not burn CPU in any one function — each query is dispatched quickly, the program waits for the database off-CPU, and the next query dispatches. The wait is off-CPU; the CPU profile sees fast little blips and nothing wide. To find N+1, look at the database query log (Postgres log_min_duration_statement, MySQL slow query log, ORM logger), the application request trace (Tempo, Jaeger, Datadog APM showing the full waterfall of queries), or a wall-clock off-CPU profile that captures wait time. The signature is unmistakable: dozens to hundreds of similar queries per request, often the same SELECT differing only in the WHERE id = ? parameter." },
    { q: "What is lazy loading and why does it produce N+1 by default?", a: "Lazy loading is the ORM behavior where a related object is fetched from the database the first time you access its property. This is convenient because you do not declare relationships up front. But when you loop over a collection and access a related property on each item, the ORM fires one query per item — N queries for N items, plus the original query for the collection itself. That is 1 + N queries: the N+1 pattern. The fix is eager loading: tell the ORM in advance what relationships you need, so it fetches them in one or two queries instead." },
  ]}
/>

<Recap lang="en">
The N+1 problem turns one logical operation into 1 + N database round-trips by loading each related row lazily. With 50 items, that is 51 queries; at 15 ms RTT each, the page takes 765 ms instead of 30 ms. The root cause is lazy loading — the ORM default that fires a query each time a related property is accessed. The fix is structural: use an eager-load directive (Rails `.includes`, Django `.select_related`, SQLAlchemy `selectinload`, Prisma `include`) to tell the ORM what you need up front. Now when you see a slow page with a flat CPU profile, open the query log first — if you count more than 5–10 similar SELECTs per request, you have found your N+1.
</Recap>
