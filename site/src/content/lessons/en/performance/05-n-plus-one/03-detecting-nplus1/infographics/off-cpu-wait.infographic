infographic chart-column-simple
data
  title Where the slow 980 ms request goes
  desc 73% is off-CPU database wait across 300+ N+1 queries — a CPU profiler sees none of it
  values
    - label ActiveRecord (DB wait, off-CPU)
      value 689
    - label Views (render, on-CPU)
      value 250
theme
  palette #f43f5e #6366f1
