---
concepts:
- round-trip-count
- cardinality-fix-choice
- dataloader
- off-cpu-detection
- pool-exhaustion
- query-amplification-dos
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.prisma.io/docs/orm/prisma-client/queries/relation-queries
- https://github.com/graphql/dataloader
- https://docs.sqlalchemy.org/en/20/orm/queryguide/relationships.html
status: ready
summary: Multiple-choice synthesis across the N+1 unit — round-trip cost, fix-family choice by cardinality, DataLoader, off-CPU detection, pool exhaustion, and DoS amplification.
title: 'N+1: multiple-choice review'
track: performance
unit: 05-n-plus-one
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one mirrors a decision you make in a real incident — not a definition to recite, but a fix to pick when the query log is in front of you and the page is slow.
</Hook>

<Goal lang="en">
Confirm you can connect the round-trip cost model, the four fix families, the detection instruments, and the second-order failures — the synthesis the individual lessons built toward.
</Goal>

<Quiz
  id="05-n-plus-one-choice-1"
  lessonSlug="05-n-plus-one"
  lang="en"
  question="A list page fires 51 queries at 0.4 ms each but the wall-clock is 765 ms. The DBA insists every query is fast and the indexes are fine. How do you reconcile this?"
  choices={[
    { label: "Both are right. Each query is fast on the server, but the client pays full round-trip latency (~15 ms intra-DC) per query — RTT plus parse plus planner — serially. 51 round-trips dominate; the cost is in the trip count, not the per-query work.", correct: true },
    { label: "The DBA is wrong — slow queries are hiding behind the index.", misconception: "The queries genuinely are fast server-side. The 765 ms is round-trip latency × 51, not slow execution. Speeding up each query does almost nothing here." },
    { label: "Add an index to the child table to drop the 765 ms.", misconception: "An index lowers per-query execution time, already at 0.4 ms. The bottleneck is the 51 round-trips' latency, which an index does not touch. You need fewer trips, not faster ones." },
    { label: "The application server is CPU-bound between queries.", misconception: "The time is spent off-CPU waiting on the database socket between trips, not burning CPU. A CPU profile would look flat — the classic N+1 signature." },
  ]}
/>

<Quiz
  id="05-n-plus-one-choice-2"
  lessonSlug="05-n-plus-one"
  lang="en"
  question="A page loads 50 orders, each with ~20 line items. You fix the N+1 with a JOIN and query count drops 51 → 2, but p99 gets 30% worse. What happened and what is the fix?"
  choices={[
    { label: "The JOIN duplicates the 50 parent order columns once per line item — ~1000 rows where 950 repeat order data. The extra payload and ORM parsing outweigh the saved round-trip. Switch to IN-based batching (selectinload / two queries), which returns each row once.", correct: true },
    { label: "Eager loading is inherently slower than lazy loading.", misconception: "Eager loading almost always wins by collapsing round-trips. The regression here is the JOIN strategy for heavy one-to-many fan-out, not eager loading itself." },
    { label: "The IN list overflowed the planner.", misconception: "This is a JOIN, not an IN query — there is no IN list. The cost is row duplication from the JOIN, which IN-based loading would avoid." },
    { label: "Roll back to N+1 — 2 queries clearly can't beat 51.", misconception: "Two queries beat 51 round-trips on latency; the regression is payload bloat from JOIN duplication. The fix is IN-based loading, not reverting to N+1." },
  ]}
/>

<Quiz
  id="05-n-plus-one-choice-3"
  lessonSlug="05-n-plus-one"
  lang="en"
  question="A GraphQL query me { posts { author { name } } } over 50 posts fires 1 + 50 author lookups. Why can't ORM eager loading (.includes / select_related) fix this, and what can?"
  choices={[
    { label: "There is no single query construction site to declare relationships at — 50 independent author resolvers each fetch one record. DataLoader fixes it by queuing IDs at request scope and firing one SELECT ... WHERE id IN (...) per type when the event loop ticks.", correct: true },
    { label: "Eager loading works fine; just add .includes(:author) in the resolver.", misconception: "Each resolver runs independently per parent object with no shared query site. There is nowhere to attach .includes that batches across the 50 separate author resolvers — that is exactly why DataLoader exists." },
    { label: "GraphQL forbids JOINs, so eager loading is impossible.", misconception: "GraphQL has no opinion on SQL. The blocker is the scattered, per-resolver nature of the lookups, not any prohibition on JOINs." },
    { label: "Cap posts at 10 to keep the lookup count down.", misconception: "That is a workaround that shrinks the symptom; the structural 1+N pattern remains. DataLoader removes the pattern regardless of fan-out size." },
  ]}
/>

<Quiz
  id="05-n-plus-one-choice-4"
  lessonSlug="05-n-plus-one"
  lang="en"
  question="A REST aggregator calls 8 downstream services serially at 30 ms each (240 ms p99). A SQL N+1 on a list page also costs ~240 ms. Are these the same problem, and do they share a fix?"
  choices={[
    { label: "Same shape, different fix axis. Both pay N serial round-trips. SQL N+1 batches into one query (JOIN/IN/preload/DataLoader). Independent service calls have no shared backend to batch, so you parallelise — Promise.all / errgroup / asyncio.gather — turning sum(latencies) into max(latencies), ~35 ms.", correct: true },
    { label: "Unrelated problems — one is a database issue, one is a network issue.", misconception: "Both are the same N-round-trips shape one protocol level apart. The unit's whole point is that N+1 is protocol-agnostic; only the batching mechanism differs." },
    { label: "Parallelise the SQL queries with Promise.all too.", misconception: "Parallel SQL still pays N round-trips and can saturate the connection pool. SQL to the same DB should be batched into one query, not fanned out in parallel." },
    { label: "Both need a faster RPC protocol.", misconception: "Protocol speed gives an incremental win. The structural fix is collapsing trips: batch the SQL, parallelise the independent service calls." },
  ]}
/>

<Quiz
  id="05-n-plus-one-choice-5"
  lessonSlug="05-n-plus-one"
  lang="en"
  question="A service with a 25-connection pool starts returning 503s under load. Individual query latency is stable, the DB is healthy, but pool saturation is pinned near 100% and the request queue is growing. Root cause and first fix?"
  choices={[
    { label: "An N+1 endpoint holds a connection across ~50 serial queries (~100 ms per request), so pool throughput caps near pool_size / hold_time ≈ 250 req/s. Fix the N+1 to cut hold-time to ~4 ms; the same pool then sustains ~10× the traffic.", correct: true },
    { label: "The pool is too small — just raise it to 200.", misconception: "Enlarging the pool defers the wall but shifts contention onto the database and masks the cause. The real lever is shorter connection hold-time, achieved by killing the N+1." },
    { label: "Connections are leaking and never released.", misconception: "A leak makes active count climb and never recover. Here connections are released — just slowly, because each request runs ~50 serial queries before releasing." },
    { label: "The database is overloaded by concurrent connections.", misconception: "Individual query latency is stable, so the DB is not the bottleneck. The application is holding connections too long across serial queries." },
  ]}
/>

<Quiz
  id="05-n-plus-one-choice-6"
  lessonSlug="05-n-plus-one"
  lang="en"
  question="A public GraphQL endpoint lets clients control nesting depth and page size. A single crafted request spikes the DB to thousands of queries. How should a senior frame this, and what is the durable guard?"
  choices={[
    { label: "It is query amplification — a user-controlled parameter multiplying query count is a DoS vector. Guard with depth limits, query-complexity analysis, and a per-request query budget — plus a CI gate so no future change reintroduces unbounded fan-out.", correct: true },
    { label: "It is just a performance bug; fix the N+1 and move on.", misconception: "Fixing one path helps, but on a public endpoint with user-controlled fan-out the issue is a security/availability vector. It needs depth/complexity bounds, not only a one-path fix." },
    { label: "Rate-limit the client and it's solved.", misconception: "Rate limiting blunts volume but one expensive request can still amplify thousands of queries. You must bound per-request cost via depth and complexity limits." },
    { label: "Add more read replicas to absorb the spike.", misconception: "Replicas raise capacity but the attacker scales fan-out with the request; you'd be paying to serve an attack. Bound the per-request query cost instead." },
  ]}
/>

<Recap lang="en">
The through-line of the unit is one decision tree: count the round-trips first (the cost is trip count, not per-query speed), then pick the fix by cardinality — JOIN for one-to-one, IN/selectinload for heavy one-to-many, preload when the shape is known at the query site, DataLoader when lookups are scattered across resolvers, and parallel dispatch when independent services share no backend. Detect it off-CPU via query logs and APM waterfalls, gate it in CI, and remember the second-order failures: JOIN payload bloat, connection-pool exhaustion, query-plan flips, and DoS amplification.
</Recap>
