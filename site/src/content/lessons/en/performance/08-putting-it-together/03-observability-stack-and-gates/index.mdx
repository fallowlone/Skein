---
slug: 03-observability-stack-and-gates
lang: en
track: performance
unit: 08-putting-it-together-perf
order: 3
title: "Observability stack and CI gates: catching regressions before they ship"
summary: "Five integrated data streams — metrics, logs, traces, profiles, RUM — plus pre-merge CI gates that fail regressions before they reach users. The cheapest fix is the one that never ships."
estMin: 14
status: ready
lessonType: topic
level: middle
prereqs:
  - performance/08-putting-it-together/02-classify-and-fix-families
  - performance/01-profile-first/08-continuous-profiling-at-scale
deepensInto:
  - performance/08-putting-it-together/04-incident-to-enforcement
spiral: ["latency", "statefulness"]
concepts: ["observability-stack", "ci-gates", "continuous-profiling", "rum", "slo-budget"]
sources:
  - https://opentelemetry.io/docs/specs/otel/profiles/
  - https://github.com/grafana/pyroscope
  - https://web.dev/articles/vitals
  - https://sre.google/sre-book/eliminating-toil/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";

<Hook>
An on-call engineer gets a p99 SLO burn alert at 2 am. She has continuous profiling, distributed traces, and RUM. She finds the root cause in 4 minutes. Her colleague at a different company gets the same alert but has only server logs. She is still triaging at 6 am. The difference is not the incident — it is the observability stack she had before the incident.
</Hook>

<Crux>Five signals — metrics, traces, profiles, RUM — plus CI gates: catch regressions before ship, fix those that slip in minutes.</Crux>

<Explanation>

## The five production signals

Production-grade performance work needs five integrated data streams. Each answers a different question; together they give a complete picture.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Signal</th>
        <th class="border border-border px-3 py-2 text-left">Tool examples</th>
        <th class="border border-border px-3 py-2 text-left">Question answered</th>
        <th class="border border-border px-3 py-2 text-left">Refresh / retention</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Metrics</td>
        <td class="border border-border px-3 py-2">Prometheus + Grafana</td>
        <td class="border border-border px-3 py-2">Is the service healthy? p50/p99 latency, RPS, error rate</td>
        <td class="border border-border px-3 py-2">15–60 s / 13 months</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Logs</td>
        <td class="border border-border px-3 py-2">Loki / ELK / Datadog Logs</td>
        <td class="border border-border px-3 py-2">What happened for this request?</td>
        <td class="border border-border px-3 py-2">On-demand / 7–90 days</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Traces</td>
        <td class="border border-border px-3 py-2">Tempo / Jaeger / Honeycomb</td>
        <td class="border border-border px-3 py-2">Which span owns the latency?</td>
        <td class="border border-border px-3 py-2">Per-request / 7–30 days</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Profiles</td>
        <td class="border border-border px-3 py-2">Pyroscope / Parca / Polar Signals</td>
        <td class="border border-border px-3 py-2">Which function ate the CPU / allocations?</td>
        <td class="border border-border px-3 py-2">Continuous / 7–30 days</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">RUM</td>
        <td class="border border-border px-3 py-2">Sentry / Datadog RUM / Vercel Speed Insights</td>
        <td class="border border-border px-3 py-2">What did real users experience? Core Web Vitals</td>
        <td class="border border-border px-3 py-2">Per-session / 30–90 days</td>
      </tr>
    </tbody>
  </table>
</div>

## Linking the signals: the unified dashboard

The power is not in each signal in isolation — it is in the links between them. An SLO burn alert links to the RED dashboard. The RED dashboard links to the trace view filtered to the burn window. The trace links to the profile filtered by the trace-id. The profile links to the source code.

A well-instrumented service lets you travel this chain in under five minutes:

1. SLO alert fires (Prometheus → alertmanager).
2. Open RED dashboard: which of p99/error-rate/RPS moved?
3. Jump to traces filtered to the last 10 minutes: which span is slow?
4. Click "profile this trace" in Pyroscope: which function is hot?
5. Click "git blame" on the function: which commit and deploy?

Without the links (cross-signal join by trace-id), each step requires manual correlation — copying a timestamp, searching in a different tool, guessing which deployment window. The median MTTR without signal linking is 30 to 90 minutes; with it, 3 to 10 minutes.

OpenTelemetry standardises the wire format (OTLP) and SDK across all five signals. Teams that instrument with OTel can swap backends — from Datadog to a self-hosted stack — without reinstrumenting. The profile signal (standardised in OTel 2024-2026) joins metrics, logs, and traces in the same collector pipeline.

## Pre-merge CI gates: catch it before it ships

The cheapest fix is the one that never reaches production. Four gates run on every PR, take 1 to 5 minutes, and catch 90%+ of would-be regressions:

**Bundle-size gate** — fails the PR if any route's JS bundle exceeds the per-route budget. Set the budget at the post-fix level after a bundle optimisation; any PR that grows the bundle beyond it needs explicit sign-off. Tools: bundlewatch, Lighthouse CI, Next.js bundle analyzer in CI.

**Query-count gate** — fails the PR if any endpoint introduces N+1 queries. Implementation: a middleware in the test suite that counts DB queries per request and asserts the count is under a threshold. Catches the most common backend regression class before merge.

**Allocation-rate diff** — fails the PR if the allocation rate for a critical path benchmark regresses beyond a threshold. A benchmarking harness (go test -bench, criterion for Rust, JMH for Java) runs on the hot path and asserts alloc/op is within budget.

**Load-test diff** — runs a brief load scenario against the PR and fails if p99 on the critical path regressed beyond X%. Heavier than the others (5 to 15 minutes) but catches architectural regressions that microbenchmarks miss.

<Inset kind="why" lang="en">
The mental model for gates: each gate enforces the lesson from one past incident. Bundle gate = the lesson from the time a third-party script ballooned LCP to 4s. Query gate = the lesson from the N+1 that took /orders from 30ms to 1.5s. Allocation gate = the lesson from the logger allocation that spiked GC. Every incident retro should end with "what gate would have caught this?" and that gate should be added. The gate set is never finished — it grows with the system's failure history.
</Inset>

<Misconception id="mc-gates-done">
  <p><strong>"We have all the CI gates — we are done."</strong> Gates catch known failure classes. New code patterns and external changes introduce failure modes no existing gate covers. After every incident, add the gate that would have caught it. The gate set evolves.</p>
</Misconception>

<NumbersCard
  id="03-observability-numbers"
  title="Observability investment numbers"
  stats={[
    { label: "MTTR with full stack + signal linking", value: "3–10 minutes" },
    { label: "MTTR without continuous profiling", value: "30–90 minutes" },
    { label: "Pre-merge gate catch rate for known regression classes", value: "90%+" },
    { label: "Pyroscope OSS infra cost per month (self-hosted)", value: "~$500" },
    { label: "Sentry RUM per year (small team)", value: "~$30k" },
    { label: "Engineer-time for performance firefighting without discipline", value: "20–40%" },
    { label: "Engineer-time with discipline (steady state)", value: "5–10%" },
  ]}
  data-lesson-visual
/>

<Quiz
  id="03-gates-quiz-1"
  lessonSlug="03-observability-stack-and-gates"
  lang="en"
  question="A team has CI gates for bundle, query count, and allocation rate. They still get a production p99 regression once per quarter. Most likely gap?"
  choices={[
    { label: "Existing gates catch known-class regressions. New failure modes are not covered by existing gates. Fix: add a gate after each incident, and do not assume the current set is complete.", correct: true },
    { label: "The gates are incorrectly configured", misconception: "Gates that catch known issues are correct; the gap is in coverage, not correctness." },
    { label: "Production traffic is unmeasurable in CI", misconception: "CI gates use synthetic load; RUM and SLO alerts catch what slips through. The gap is the missing gate for the new failure mode." },
    { label: "Need more CI compute", misconception: "More compute speeds up gates; it does not extend their coverage to new failure classes." },
  ]}
/>

<Quiz
  id="03-gates-quiz-2"
  lessonSlug="03-observability-stack-and-gates"
  lang="en"
  question="Why is the trace-id the critical join key between signals in a unified observability stack?"
  choices={[
    { label: "The trace-id propagates through every service and every layer for a single request. By filtering metrics, logs, and profiles to the same trace-id, you see exactly what one slow request experienced — without guessing.", correct: true },
    { label: "It is required by the HTTP spec", misconception: "Trace-id is optional and vendor-specific. Its value is the correlated cross-signal view, not compliance." },
    { label: "It makes the logs easier to read", misconception: "Trace-id is for correlation across systems, not for individual log readability." },
    { label: "It reduces alert noise", misconception: "Alert filtering uses metrics thresholds; trace-id is about cross-signal drill-down, not alert volume." },
  ]}
/>

<DragOrder
  id="03-gates-drag-1"
  lessonSlug="03-observability-stack-and-gates"
  lang="en"
  prompt="Order the five production signals from broadest (whole service view) to narrowest (single function in single request):"
  items={[
    { id: "a", label: "Metrics — p99 latency, RPS, error rate across all requests" },
    { id: "b", label: "RUM — Core Web Vitals from real user devices per route" },
    { id: "c", label: "Traces — per-request waterfall showing which span is slow" },
    { id: "d", label: "Logs — structured event detail for a specific request" },
    { id: "e", label: "Profiles — which function inside the span consumed CPU or allocations" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>Five signals — metrics, logs, traces, profiles, RUM — linked by trace-id cut MTTR from hours to minutes. Pre-merge CI gates enforce the lessons from past incidents and catch 90% of regressions before they ship. Build both; neither alone is enough.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-observability-retrieval"
  lang="en"
  questions={[
    { q: "What is the role of the trace-id in linking the five observability signals?", a: "Every request gets a trace-id that propagates through every service and layer. Metrics aggregate across all requests; when the p99 spikes, you filter traces to the burn window to find the slow trace-ids. The trace links to profile samples captured at the same trace-id. The profile names the function. Without the trace-id join, you would have to manually match timestamps across three different tools and guess which samples correspond to the slow request. The trace-id makes it a deterministic query: show me all profile samples for trace abc123." },
    { q: "Name the four CI gate types described, what each catches, and how it is implemented.", a: "Bundle-size gate: catches client-side bloat regressions. Implementation: bundlewatch or Lighthouse CI checks each route's JS size against a per-route budget set at the post-optimisation level; PRs that exceed the budget fail. Query-count gate: catches N+1 regressions in backend endpoints. Implementation: a test-suite middleware counts DB queries per request and asserts the count is under a per-endpoint threshold. Allocation-rate diff: catches GC regression in hot paths. Implementation: a benchmarking harness (go test -bench, criterion, JMH) runs on the hot path and asserts alloc/op is within budget compared to the baseline. Load-test diff: catches architectural regressions that microbenchmarks miss. Implementation: a brief synthetic load scenario against the PR's service, asserting p99 on the critical path has not regressed beyond a percentage threshold." },
    { q: "Why do CI gates and observability complement each other rather than substitute for each other?", a: "CI gates run on synthetic workloads in a controlled environment before merge; they catch changes that would introduce a known regression class. But they cannot catch regressions introduced by external service changes, unusual production traffic patterns, or failure modes that have no matching gate yet. Observability catches regressions that slip through CI and reach production — continuous profiling and RUM give early warning before the SLO burns. The two together give defence in depth: gates stop 90% of regressions at merge time, observability catches the remaining 10% within minutes of deploy. Neither alone is sufficient." },
  ]}
/>

<Recap lang="en">
Production-grade performance work runs on five integrated data streams: metrics (aggregate health), logs (per-request events), traces (per-request span waterfall), profiles (per-function CPU and allocation), and RUM (real user Core Web Vitals). The trace-id is the join key that links them: an SLO alert links to a trace, the trace links to a profile, the profile names the function. With the full stack and signal linking, MTTR drops from 30–90 minutes to 3–10. Before signals, four CI gates catch 90% of regressions before they ship: bundle-size, query-count, allocation-rate diff, and load-test diff. Each gate encodes the lesson from a past incident. The gate set is never complete — it grows after every production regression via incident retros that answer "what gate would have caught this?"
</Recap>
