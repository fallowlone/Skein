infographic compare-binary-horizontal-underline-text-vs
data
  title The discipline pays for itself
  desc Same 2 am alert, two different nights — minutes vs hours to root cause
  compares
    - label Without the stack
      children
        - label MTTR 30–90 min
          desc Hand-correlate timestamps across separate tools
        - label 20–40% engineer time firefighting
          desc Perf work with no gates and no signal links
        - label Regressions reach users
          desc Caught only after the SLO budget burns
        - label Still triaging at 6 am
          desc Only server logs to drill into
    - label With stack + CI gates
      children
        - label MTTR 3–10 min
          desc trace-id links metric to trace to profile to code
        - label 5–10% engineer time
          desc Steady state, not constant firefighting
        - label 90%+ caught pre-merge
          desc Bundle, query-count, alloc, load-test gates
        - label Root cause in 4 min
          desc Five linked signals, gates stop the rest
theme
  palette #f43f5e #10b981
