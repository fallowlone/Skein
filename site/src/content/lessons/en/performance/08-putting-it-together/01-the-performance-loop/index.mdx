---
concepts:
- performance-loop
- discipline-vs-project
- regression-prevention
- ci-gates
deepensInto:
- performance/08-putting-it-together/02-classify-and-fix-families
estMin: 10
lang: en
lessonType: topic
level: junior
order: 1
prereqs:
- performance/01-profile-first/01-why-profile-first
- performance/02-hot-paths/01-what-makes-a-hot-path
slug: 01-the-performance-loop
sources:
- https://sre.google/sre-book/eliminating-toil/
- https://www.brendangregg.com/methodology.html
- https://web.dev/articles/vitals
spiral:
- latency
- statefulness
status: ready
summary: Performance regresses by default. The eight-step loop — observe, profile,
  classify, predict, fix, verify, enforce, repeat — is the discipline that keeps a
  service fast year over year.
title: 'The performance loop: discipline, not a project'
track: performance
unit: 08-putting-it-together-perf
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import disciplineVsProject from "./infographics/discipline-vs-project.svg?raw";

<Hook>
A team fixed their p99 from 1.2 s to 200 ms. They called it done, shipped, and moved on. Six months later p99 is 900 ms again. No single regression — just new features, new libraries, a bigger JSON response from an upstream service. The fix was right. The discipline was missing.
</Hook>

<Crux>Performance is a loop, not a project. Profile, classify, fix, verify, enforce — then repeat at the next bottleneck.</Crux>

By the end of this lesson you will know the exact mechanism that causes a team's hard-won fix to quietly disappear — and the one step that prevents it.

<Explanation>

## Why performance regresses by default

Every new feature adds bytes, queries, or allocations. Every dependency upgrade ships new code paths. Every schema change can turn a fast query into a slow one. Without a mechanism to catch these additions, performance degrades continuously.

A one-time optimisation has an effective half-life of three to six months. After that window, the accumulated changes from new feature work undo the gains. Teams that treat performance as a project get there — then drift back. Teams that treat performance as a discipline stay there.

The difference is one mechanism: **the loop**.

<Infographic
  svg={disciplineVsProject}
  label="Two-column comparison of treating performance as a loop versus a one-time project. The loop costs 5–10% of engineering time, holds metrics steady year over year, and gives a fix an indefinite half-life because a CI gate fails any regressing PR. The project costs 20–40% of time in crisis mode, hits a crisis every 6–18 months, and a fix decays in 3–6 months as new features drift performance back."
  caption="The discipline is not more expensive — it is cheaper. It frontloads the cost into CI gates and observability instead of paying 20–40% in recurring firefights."
  hue="var(--d-infra)"
/>

## The eight-step performance loop

When you internalise these eight steps, any slowdown stops being a mystery and becomes a structured search. Here is the sequence every senior engineer runs:

1. **Observe** — a symptom surfaces: SLO burn, RUM regression, user complaint, dashboard alert. This tells you something is wrong, not what.
2. **Profile** — capture data appropriate to the symptom. CPU flame graph for CPU spikes, allocation profile for memory growth, network waterfall for slow page loads, bundle analyzer for client-side bloat.
3. **Classify** — name the bottleneck by family: CPU-algorithmic, allocation-bound, cache-bound, lock-bound, I/O-bound (N+1), syscall-bound (batching), JIT-deopt, bundle-bound. Each family has a known fix set.
4. **Predict** — use Amdahl's law to estimate how much the headline metric will improve if you fix this hotspot. If the prediction is below your SLO target, this is not the right hotspot; return to step 2.
5. **Fix** — from the family's playbook, pick the technique that matches the specific shape of the hotspot. Apply only the predicted change; no scope creep.
6. **Verify** — re-profile under the same load. Confirm both the local hotspot shrank AND the headline metric improved.
7. **Enforce** — add a CI gate, alert, or runbook entry that prevents this exact regression from returning.
8. **Move on** — find the next bottleneck. The loop never ends; it shifts between layers.

Together these eight steps form a closed feedback system: observe turns a vague symptom into a specific signal, enforce converts a one-time fix into permanent protection, and move-on keeps the cycle alive. Skip enforce, and step 5 is work you will repeat in six months.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Step</th>
        <th class="border border-border px-3 py-2 text-left">Action</th>
        <th class="border border-border px-3 py-2 text-left">Output feeds</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">1. Observe</td>
        <td class="border border-border px-3 py-2">Notice the symptom</td>
        <td class="border border-border px-3 py-2">Which service / metric to profile</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">2. Profile</td>
        <td class="border border-border px-3 py-2">Capture the right data stream</td>
        <td class="border border-border px-3 py-2">Hot function / span name</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">3. Classify</td>
        <td class="border border-border px-3 py-2">Name the bottleneck family</td>
        <td class="border border-border px-3 py-2">Fix playbook to pull from</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">4. Predict</td>
        <td class="border border-border px-3 py-2">Amdahl estimate of headline gain</td>
        <td class="border border-border px-3 py-2">Go/no-go on this hotspot</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">5. Fix</td>
        <td class="border border-border px-3 py-2">Apply the matching technique</td>
        <td class="border border-border px-3 py-2">Changed code / config</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">6. Verify</td>
        <td class="border border-border px-3 py-2">Re-profile under same load</td>
        <td class="border border-border px-3 py-2">Confirmed or reverted</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">7. Enforce</td>
        <td class="border border-border px-3 py-2">CI gate / alert / runbook</td>
        <td class="border border-border px-3 py-2">Regression-proof deploy</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">8. Move on</td>
        <td class="border border-border px-3 py-2">Find next bottleneck</td>
        <td class="border border-border px-3 py-2">Next iteration of step 1</td>
      </tr>
    </tbody>
  </table>
</div>

## The kitchen metaphor

Performance is like cleaning a kitchen, not painting a room. Painting once is fine. A kitchen cleaned once gets dirty as cooking happens; you clean continuously.

Each of the seven pieces in this chapter is a tool: profiler, hot-paths classifier, GC fixer, N+1 detector, batcher, bundle analyzer. None alone keeps the kitchen clean; the loop does.

<Inset kind="why" lang="en">
Teams without the loop end up with "why is the site slow now?" meetings every six months, each consuming 5 to 20 engineer-days. Teams with the loop have steady metrics year over year. The difference in total engineer-time is small — the discipline just frontloads the investment into CI gates and observability rather than deferring it to incident response.
</Inset>

## Bea and Sven's quarter

Bea joins a team where the service was fast a year ago. Now p99 is 1.2 s, up from 200 ms. Sven walks her through the loop: profile shows GC pressure at 18%, an N+1 in /orders adds 50 queries per request, /dashboard bundle grew 800 KB over six months. No single crisis — three separate slow accumulations.

They run the loop on each bottleneck one at a time: logger allocation fix (week 1), query deduplification (week 2-3), bundle code-split (week 4). After a month, p99 is 280 ms. CI gates keep the work alive through the next quarter of feature shipping.

<Quiz
  id="01-the-performance-loop-quiz-1"
  lessonSlug="01-the-performance-loop"
  lang="en"
  question="A team applied a performance fix and shipped. Six months later, performance is worse than before the fix. Most likely cause?"
  choices={[
    { label: "No CI gate enforced the win. New features added bytes, queries, and allocations that undid the fix invisibly. Regression is the default without enforcement.", correct: true },
    { label: "The fix was wrong", misconception: "Possible but unlikely — the fix worked initially. The regression came from new code, not the old fix breaking." },
    { label: "Production traffic changed", misconception: "Some effect is possible, but the typical cause is internal drift from new features without enforcement." },
    { label: "Hardware aged", misconception: "Hardware doesn't slow over months; software grows in the absence of gates." },
  ]}
/>

<DragOrder
  id="01-the-performance-loop-drag-1"
  lessonSlug="01-the-performance-loop"
  lang="en"
  prompt="Order the eight steps of the performance loop a senior engineer runs every time:"
  items={[
    { id: "a", label: "Notice the symptom — SLO burn, RUM regression, profile alert" },
    { id: "b", label: "Open the profile — identify the hot path with concrete numbers" },
    { id: "c", label: "Classify the hotspot: CPU, allocation, cache, lock, I/O, syscall, JIT, bundle" },
    { id: "d", label: "Predict headline metric impact using Amdahl" },
    { id: "e", label: "Apply only the predicted change; no scope creep" },
    { id: "f", label: "Re-profile under same load; verify both local frame and headline metric improved" },
    { id: "g", label: "Add a CI gate or alert so this regression cannot return invisibly" },
    { id: "h", label: "Document and move to the next bottleneck" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f", "g", "h"]}
/>

<MetaphorComplete
  id="01-the-performance-loop-metaphor-1"
  lessonSlug="01-the-performance-loop"
  lang="en"
  setup="Fill in the blank: performance is the _______ of the codebase — measured continuously, enforced at every commit, owned by every engineer."
  accepted={["health", "fitness", "quality", "discipline", "responsibility"]}
  canonical="health (a property that needs continuous care, not a one-time achievement)"
  explanation="Like physical health, performance health degrades silently without continuous attention. The seven pieces are the diagnostics and treatments; the loop is the regimen. Teams without the regimen end up with crisis-mode triage every six months; teams with it have steady metrics year over year."
/>

<Quiz
  id="01-the-performance-loop-quiz-2"
  lessonSlug="01-the-performance-loop"
  lang="en"
  question="What does it mean to treat performance as a 'loop' rather than a 'project'?"
  choices={[
    { label: "Performance work is continuous: measure, fix, verify, enforce, then repeat at the next regression. Each pass uses the same toolkit; the discipline is to keep running it.", correct: true },
    { label: "Always be optimising everything at all times", misconception: "Premature optimisation is the opposite of the loop. The loop starts with measure first; only optimise what is actually slow." },
    { label: "Hire a dedicated performance team", misconception: "A dedicated team helps, but the loop is a methodology any team applies with the right tools." },
    { label: "Rewrite the service every year", misconception: "Rewriting is rarely the right answer. The loop applies incremental, measured fixes." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="The eight-step performance loop: observe, profile, classify, predict, fix, verify, enforce, then move on to the next bottleneck"
  hue="var(--d-hardware)"
  perRow={4}
  nodes={[
    { id: "observe", label: "Observe", sub: "symptom surfaces" },
    { id: "profile", label: "Profile", sub: "capture data" },
    { id: "classify", label: "Classify", sub: "name the family" },
    { id: "predict", label: "Predict", sub: "Amdahl ceiling" },
    { id: "fix", label: "Fix", sub: "apply technique" },
    { id: "verify", label: "Verify", sub: "re-profile" },
    { id: "enforce", label: "Enforce", sub: "CI gate / alert" },
    { id: "moveon", label: "Move on", sub: "next bottleneck" },
  ]}
  edges={[
    { from: "observe", to: "profile" },
    { from: "profile", to: "classify" },
    { from: "classify", to: "predict" },
    { from: "predict", to: "fix" },
    { from: "fix", to: "verify" },
    { from: "verify", to: "enforce" },
    { from: "enforce", to: "moveon" },
    { from: "moveon", to: "observe", label: "repeat" },
  ]}
  caption="The loop never ends — it shifts between layers. Enforcement is the step that converts a one-time fix into a durable property."
/>

<KeyTakeaway>Performance regresses by default. The eight-step loop — observe, profile, classify, predict, fix, verify, enforce, move on — is what converts a one-time fix into a durable property. Enforcement is the step that separates discipline from a project.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-the-performance-loop-retrieval"
  lang="en"
  questions={[
    { q: "Why does a one-time performance fix have a half-life of three to six months?", a: "Every new feature, dependency upgrade, and schema change adds bytes, queries, or allocations. Without a mechanism to catch these additions — CI gates, RUM alerts, allocation-rate thresholds — each one goes unnoticed. The aggregate of dozens of small additions undoes the original fix. The optimisation half-life is the time it takes for accumulated unguarded changes to push performance back to the pre-fix level. Adding CI gates after the fix extends the half-life to indefinite: any PR that would reintroduce the same class of regression fails before merge." },
    { q: "What is the role of the 'enforce' step, and why is it the most important of the eight?", a: "Enforcement converts a local fix into a systemic protection. Without it, the team will fix the same class of bug again in three to six months when someone else makes a similar change. With it, every future PR is checked against the lesson learned. The fix is one-time work; the gate is permanent. This is what makes performance a discipline rather than a recurring project. Concrete forms: bundle-size CI check, query-count middleware gate, allocation-rate benchmark, RUM alert tied to SLO budget. Any one of these running on every PR catches the specific regression class before it ships." },
    { q: "In Bea and Sven's scenario, three separate bottlenecks accumulated over six months. What prevented the team from noticing each one as it appeared?", a: "No observability and no CI gates. GC pressure grew gradually as the logger added more allocation per request; no allocation-rate alert fired. The N+1 appeared in a new endpoint and was never flagged in CI because there was no query-count gate. The bundle grew 800 KB as features shipped new dependencies; no per-route bundle budget check ran on PRs. Each individual change was small enough to slip past code review. Continuous profiling, RUM alerts, and CI gates would have surfaced each regression within days of the commit that introduced it — instead of letting them compound over six months." },
  ]}
/>

<Recap lang="en">
Performance regresses by default. Every new feature, dependency, and deploy adds bytes, queries, or allocations without anyone noticing. A one-time optimisation has a half-life of three to six months before accumulated changes undo the gains. The performance loop — observe, profile, classify, predict, fix, verify, enforce, repeat — converts the one-time fix into a durable property. The critical step is enforcement: CI gates that fail any PR reintroducing the same regression class. Teams without the loop reach a performance crisis every six to eighteen months and rebuild from scratch; teams with it maintain steady metrics year over year at a cost of five to ten percent of engineering time, versus twenty to forty percent in crisis mode. Now when you see p99 slowly climbing again after a fix, you will know exactly which step of the loop is missing — and what gate to add.
</Recap>
