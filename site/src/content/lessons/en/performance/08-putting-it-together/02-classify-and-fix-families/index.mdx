---
concepts:
- bottleneck-classification
- amdahl-law
- fix-families
- cross-layer
deepensInto:
- performance/08-putting-it-together/03-observability-stack-and-gates
estMin: 12
lang: en
lessonType: topic
level: middle
order: 2
prereqs:
- performance/08-putting-it-together/01-the-performance-loop
- performance/02-hot-paths/02-five-shapes-of-hotspot
- performance/04-gc/01-gc-pause-anatomy
slug: 02-classify-and-fix-families
sources:
- https://www.brendangregg.com/methodology.html
- https://go.dev/blog/ismmkeynote
- https://aws.amazon.com/builders-library/avoiding-overload-in-distributed-systems-by-putting-the-smaller-service-in-control/
spiral:
- latency
- statefulness
status: ready
summary: Each bottleneck belongs to one of eight families. Naming the family takes
  seconds from the profile; picking the wrong family wastes days. Amdahl sets the
  ceiling on any fix before you write a line of code.
title: 'Classify and fix: matching bottleneck families to remedies'
track: performance
unit: 08-putting-it-together-perf
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import amdahlCeiling from "./infographics/amdahl-ceiling.svg?raw";

<Hook>
Two engineers look at the same flame graph. One sees "GC is hot" and rewrites the allocator. The other sees "GC is hot because of a bulk upstream payload" and rolls back one deploy. The first spends a week; the second spends 35 minutes. The difference is classification — naming the bottleneck family before touching the code.
</Hook>

<Crux>Name the bottleneck family from the profile. Family names the fix set. Amdahl sets the ceiling before writing a line.</Crux>

<Explanation>

## The eight bottleneck families

Why does naming the family matter before writing a single line? Because each family has a distinct fix set with zero overlap — applying the wrong one wastes days without moving the metric. The seven pieces of this chapter map exactly to this vocabulary. When you classify a hotspot correctly, the fix set follows immediately.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Family</th>
        <th class="border border-border px-3 py-2 text-left">Profile signal</th>
        <th class="border border-border px-3 py-2 text-left">Chapter piece</th>
        <th class="border border-border px-3 py-2 text-left">Primary fix</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">CPU-algorithmic</td>
        <td class="border border-border px-3 py-2">High self-time, low off-CPU</td>
        <td class="border border-border px-3 py-2">02 hot-paths</td>
        <td class="border border-border px-3 py-2">Better algorithm or data structure</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Allocation-bound</td>
        <td class="border border-border px-3 py-2">mallocgc / scanobject high</td>
        <td class="border border-border px-3 py-2">04 GC</td>
        <td class="border border-border px-3 py-2">Reduce allocations, reuse, pools</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Cache-bound</td>
        <td class="border border-border px-3 py-2">High LLC-miss perf counters</td>
        <td class="border border-border px-3 py-2">03 cache-vs-bigo</td>
        <td class="border border-border px-3 py-2">Data layout, access pattern</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Lock-bound</td>
        <td class="border border-border px-3 py-2">High off-CPU on sync wait</td>
        <td class="border border-border px-3 py-2">02 hot-paths</td>
        <td class="border border-border px-3 py-2">Lock-free structures, sharding</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">I/O-bound (N+1)</td>
        <td class="border border-border px-3 py-2">Many short spans in trace</td>
        <td class="border border-border px-3 py-2">05 N+1</td>
        <td class="border border-border px-3 py-2">Batch queries, eager-load</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Syscall-bound</td>
        <td class="border border-border px-3 py-2">High syscall overhead in profile</td>
        <td class="border border-border px-3 py-2">06 batching</td>
        <td class="border border-border px-3 py-2">Batch writes, vectorised I/O</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">JIT-deopt</td>
        <td class="border border-border px-3 py-2">Deopt frames in JS/JVM profile</td>
        <td class="border border-border px-3 py-2">02 hot-paths</td>
        <td class="border border-border px-3 py-2">Monomorphic shapes, typed arrays</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Bundle-bound</td>
        <td class="border border-border px-3 py-2">RUM shows parse/compile time</td>
        <td class="border border-border px-3 py-2">07 bundle-budgets</td>
        <td class="border border-border px-3 py-2">Code-split, lazy-load, tree-shake</td>
      </tr>
    </tbody>
  </table>
</div>

## Amdahl before you code

Before you touch code, ask yourself: even if this fix is perfect, does it actually reach the SLO target? That is what Amdahl's law answers in 30 seconds.

Amdahl's law: if fraction `f` of execution time is spent in the hotspot, the maximum speedup from fixing it is `1 / (1 - f)`.

If the hotspot is 40% of CPU (`f = 0.4`), the best possible speedup is `1 / 0.6 = 1.67x`. If your SLO needs a 3x improvement, this is the wrong hotspot. Return to step 2 and profile again.

This calculation takes 30 seconds. It prevents weeks of work on the wrong target.

<Infographic
  svg={amdahlCeiling}
  label="Line chart of Amdahl's ceiling: the maximum speedup 1/(1-f) rises non-linearly with the hotspot fraction f — 25% caps at 1.33x, 40% at 1.67x, 50% at 2x, 68% at 3.1x, and 80% at 5x."
  caption="The ceiling accelerates as the hotspot grows: a fix to a 25% hotspot can never beat 1.33x, so reaching a 3x SLO needs a hotspot of roughly 68% or more — otherwise you must combine families."
  hue="var(--d-infra)"
/>

**Example:** A checkout service has p99 = 800 ms and a target of under 200 ms. The profile shows:
- `json.Marshal`: 28% CPU
- `runtime.scanobject`: 22% CPU
- `pgx.Query`: 18% CPU (via trace spans — actually I/O)

Amdahl on Marshal alone: `1 / (1 - 0.28) = 1.39x`. That brings 800 ms to ~575 ms — still above target.
Amdahl on Marshal + scanobject combined (50%): `1 / 0.5 = 2x`. That brings to ~400 ms — still above target.
Adding the I/O path (68% total): `1 / 0.32 = 3.1x`. That brings to ~258 ms — close.

Real root cause: all three hot frames share an upstream that suddenly returns 10x more data. Fix the upstream; all three frames shrink together. Total gain: 6x. The Amdahl calculation made it clear that fixing any single family in isolation was insufficient.

## Cross-layer compounding

Real bottlenecks rarely sit in one layer. A slow checkout page might be 30% JS bundle parse on the client, 20% N+1 queries in the backend, 15% GC allocation pressure, and 35% backend compute. Fixing any one layer in isolation gives only Amdahl-bounded wins on the total.

The compound effect is the real win. Two engineers working in parallel — one on bundle, one on queries and GC — each delivering a 2x improvement, give a combined 4x on the headline metric. Without cross-layer reasoning, the conversation stalls: "we already optimised our part."

The chapter's classification vocabulary lets engineers from frontend, backend, and infra describe their bottleneck in a way the other layers understand. Without the shared vocabulary, "the backend is slow" and "no, the frontend is slow" is the whole conversation.

<Inset kind="why" lang="en">
The fix-family table above is not a ranking. Cache-bound bottlenecks in tight compute loops (piece 03) can give 10x improvements on CPU workloads. Allocation-bound regressions (piece 04) are often the sneakiest because GC pauses add tail latency variance that Amdahl underestimates. When classifying, check whether the bottleneck contributes to p50 (average user) or p99 (worst-case user); the fix priority differs.
</Inset>

<NumbersCard
  id="02-classify-numbers"
  title="Classification pays off"
  stats={[
    { label: "Time to classify from a flame graph", value: "30–90 seconds" },
    { label: "Time lost fixing the wrong family", value: "1–5 days" },
    { label: "Amdahl ceiling if bottleneck is 25% of CPU", value: "1.33x max" },
    { label: "Amdahl ceiling if bottleneck is 80% of CPU", value: "5x max" },
    { label: "Typical multi-family compound gain", value: "4–8x" },
    { label: "Single-family fix typical gain", value: "1.3–2.5x" },
  ]}
  data-lesson-visual
/>

<Quiz
  id="02-classify-quiz-1"
  lessonSlug="02-classify-and-fix-families"
  lang="en"
  question="A service is allocation-bound (GC pressure 25%) AND has an 800 KB JS bundle. Which should be fixed first?"
  choices={[
    { label: "Measure each layer's contribution to the headline metric. Whichever contributes more — fix first. Both matter; the question is leverage per engineer-week.", correct: true },
    { label: "Bundle first, it is smaller", misconception: "Smaller does not mean less impactful. The bundle is client-side; GC is server-side. Contribution to the user-facing metric depends on the split." },
    { label: "GC first, it is faster to fix", misconception: "Speed to fix is one factor. Leverage on the headline metric is the deciding factor." },
    { label: "Both simultaneously", misconception: "Parallel work is fine, but prioritising the bigger lever first maximises early gains." },
  ]}
/>

<Quiz
  id="02-classify-quiz-2"
  lessonSlug="02-classify-and-fix-families"
  lang="en"
  question="A flame graph shows runtime.scanobject at 22% and runtime.mallocgc at 11%. Amdahl on the combined GC frames gives a maximum 1.47x speedup. The SLO requires 3x. What is the correct next step?"
  choices={[
    { label: "Return to step 2. The GC family contributes too little to reach the SLO alone. Profile again to find additional contributors — likely I/O, another CPU path, or a cross-layer cause.", correct: true },
    { label: "Fix the GC path anyway and hope it compounds", misconception: "Amdahl is a hard ceiling, not an estimate. You will not get 3x from a 33% contributor." },
    { label: "Change the SLO", misconception: "The SLO is the user-facing requirement. Change it only with product/business sign-off, not as a technical workaround." },
    { label: "Add more machines", misconception: "Horizontal scaling improves throughput, not per-request latency. The p99 will remain the same on each instance." },
  ]}
/>

<DragOrder
  id="02-classify-drag-1"
  lessonSlug="02-classify-and-fix-families"
  lang="en"
  prompt="Order the steps for classifying and fixing a bottleneck from profile to verified result:"
  items={[
    { id: "a", label: "Open the profile — identify the hottest frame by self-time" },
    { id: "b", label: "Name the family: CPU, allocation, cache, lock, I/O, syscall, JIT, bundle" },
    { id: "c", label: "Apply Amdahl to check whether fixing this family can reach the SLO target" },
    { id: "d", label: "If Amdahl ceiling is insufficient, return to profile and find additional contributors" },
    { id: "e", label: "Pick the fix technique from the family's playbook" },
    { id: "f", label: "Re-profile under same load; confirm both local frame and headline metric improved" },
  ]}
  correctOrder={["a", "b", "c", "d", "e", "f"]}
/>

</Explanation>

<FlowDiagram
  label="A profile signal classifies into one bottleneck family, and each family names its own fix set; Amdahl then sets the ceiling before any code is written"
  hue="var(--d-hardware)"
  nodes={[
    { id: "signal", label: "Profile signal", sub: "hottest frame", col: 0, row: 1 },
    { id: "classify", label: "Classify family", sub: "name in seconds", col: 1, row: 1 },
    { id: "alloc", label: "Allocation-bound", sub: "mallocgc high", col: 2, row: 0 },
    { id: "io", label: "I/O-bound (N+1)", sub: "many short spans", col: 2, row: 1 },
    { id: "bundle", label: "Bundle-bound", sub: "parse/compile", col: 2, row: 2 },
    { id: "fixalloc", label: "Reuse / pools", col: 3, row: 0 },
    { id: "fixio", label: "Batch / eager-load", col: 3, row: 1 },
    { id: "fixbundle", label: "Code-split", col: 3, row: 2 },
    { id: "amdahl", label: "Amdahl check", sub: "1/(1-f) ceiling", col: 4, row: 1 },
  ]}
  edges={[
    { from: "signal", to: "classify" },
    { from: "classify", to: "alloc" },
    { from: "classify", to: "io" },
    { from: "classify", to: "bundle" },
    { from: "alloc", to: "fixalloc" },
    { from: "io", to: "fixio" },
    { from: "bundle", to: "fixbundle" },
    { from: "fixio", to: "amdahl" },
  ]}
  caption="Eight families (three shown) each map to a distinct fix playbook; Amdahl tells you whether one family's fix can reach the SLO or whether you must combine layers."
/>

<KeyTakeaway>Classify first, then apply Amdahl. The family names the fix set; Amdahl sets the ceiling before you write a line. Real bottlenecks span layers — compound cross-layer gains are the real win.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-classify-retrieval"
  lang="en"
  questions={[
    { q: "Why does naming the bottleneck family matter before writing any fix?", a: "Each family has a distinct fix set with no overlap. An allocation-bound hotspot requires reducing allocations, reusing objects, or using memory pools — entirely different techniques from fixing a cache-bound hotspot (data layout) or an I/O-bound one (batching queries). Applying the wrong technique wastes days and may not change the profile at all. Naming the family from the profile takes 30 to 90 seconds and directs you to the correct playbook immediately." },
    { q: "A checkout service has three hot families: allocation 28%, I/O 22%, and CPU-algorithmic 18%. What does Amdahl say about each, and what does that imply?", a: "Amdahl on allocation alone (28%): 1 / (1 - 0.28) = 1.39x. On I/O alone (22%): 1 / 0.78 = 1.28x. On CPU alone (18%): 1 / 0.82 = 1.22x. Combined all three (68%): 1 / 0.32 = 3.1x. No single family gets you to 3x. You need all three, but they can be parallelised across engineers. Without Amdahl, a team might fix only allocation, see 1.4x, and declare the effort done — still well above the SLO target." },
    { q: "What is cross-layer compounding, and why does the shared classification vocabulary enable it?", a: "Cross-layer compounding is when fixes in multiple independent layers multiply rather than add. A 2x fix in bundle + a 2x fix in GC gives a combined 4x on the headline metric if they are truly independent layers. Without a shared vocabulary, frontend engineers say 'the backend is slow' and backend engineers say 'the frontend is slow', and neither team can scope the other's contribution. With the eight-family classification, a frontend engineer can say 'bundle-bound — 30% parse time' and a backend engineer can say 'allocation-bound — 25% GC overhead', both quantified, and the compound improvement can be planned and executed in parallel." },
  ]}
/>

<Recap lang="en">
Every bottleneck belongs to one of eight families: CPU-algorithmic, allocation, cache, lock, I/O (N+1), syscall (batching), JIT-deopt, or bundle. Naming the family from the profile takes under two minutes; it directs you to the correct fix playbook immediately. Amdahl's law — maximum speedup equals 1 divided by (1 minus the hotspot fraction) — sets a hard ceiling on any single fix before you write a line of code. If the Amdahl ceiling is below the SLO target, the hotspot is not sufficient to fix alone; return to the profile and find additional contributors. Real production bottlenecks typically span multiple layers; the compound gain from fixing two or three families in parallel is four to eight times larger than fixing any one family in isolation. The shared vocabulary of families lets frontend, backend, and infra engineers describe and quantify their layer's contribution so parallel work can be coordinated without confusion. Now when you open a flame graph and feel the urge to start coding immediately, run the Amdahl calculation first — 30 seconds that can save a week of work on the wrong target.
</Recap>
