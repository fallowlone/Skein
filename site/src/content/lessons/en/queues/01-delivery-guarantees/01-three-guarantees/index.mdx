---
slug: 01-three-guarantees
lang: en
track: queues
unit: 01-delivery-guarantees
order: 1
title: "At-most-once, at-least-once, exactly-once: the three delivery contracts"
summary: "What the three delivery guarantees mean, why exactly-once delivery is impossible, and why at-least-once is the production default."
estMin: 8
status: ready
lessonType: topic
level: junior
prereqs: []
deepensInto:
  - queues/01-delivery-guarantees/02-failure-modes
  - queues/01-delivery-guarantees/03-consumer-dedup
spiral: ["statefulness", "encapsulation"]
concepts: ["at-most-once", "at-least-once", "exactly-once", "delivery-guarantee", "idempotency"]
sources:
  - https://docs.confluent.io/kafka/design/delivery-semantics.html
  - https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-visibility-timeout.html
  - https://blog.bulloak.io/post/20200917-the-impossibility-of-exactly-once/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";

<Hook>
A payment worker processes a "charge $50" message and crashes before telling the queue it is done. The queue redelivers. The customer is charged twice. This is not a bug in your code — it is the default behaviour of every production queue in the world.
</Hook>

<Crux>Exactly-once delivery is impossible. The production fix is at-least-once delivery plus an idempotent consumer that ignores duplicates.</Crux>

<Explanation>

## The three contracts

Every message queue offers a delivery contract — a guarantee about how many times a message reaches a consumer when failures happen.

**At-most-once.** The broker sends the message once and never retries. If the network drops the message, it is gone. No duplicates, but possible loss. Use case: debug logs, non-critical telemetry. Never use for money, orders, or any event where loss has business consequences.

**At-least-once.** The broker retries until the consumer sends an explicit acknowledgement (ack). If the ack is lost, the broker redelivers. The message arrives at least one time, but possibly more. At-least-once is the default in SQS, RabbitMQ (with ack), and Kafka with default settings.

**Exactly-once.** Every message is processed exactly one time regardless of retries. This is what you want. The problem: pure exactly-once delivery over an unreliable network is mathematically impossible — the Two Generals' Problem proves that two parties over an unreliable channel can never be certain they both agree on the same fact. You cannot guarantee both sides know a message was delivered exactly once.

What you *can* achieve is **exactly-once processing** — the consumer's side effect (charge a card, send an email, update a row) happens exactly once, even if the broker delivers the message more than once. The trick is an idempotent consumer.

<div data-lesson-visual class="overflow-x-auto my-6 rounded-lg border border-border bg-surface-1 p-4 text-sm font-mono">
  <div class="mb-3 text-ink-2 text-xs uppercase tracking-wider">Three delivery contracts at a glance</div>
  <div class="flex flex-col gap-3">
    <div class="flex items-start gap-3">
      <span class="shrink-0 w-24 rounded bg-yellow-100 dark:bg-yellow-900/40 text-yellow-800 dark:text-yellow-200 text-xs font-bold px-2 py-1 text-center">at-most-once</span>
      <span>Send once, no retry. Can lose messages. Best for metrics, logs.</span>
    </div>
    <div class="flex items-start gap-3">
      <span class="shrink-0 w-24 rounded bg-blue-100 dark:bg-blue-900/40 text-blue-800 dark:text-blue-200 text-xs font-bold px-2 py-1 text-center">at-least-once</span>
      <span>Retry until ack. Can deliver 2+ times. Production default. Requires idempotent consumer.</span>
    </div>
    <div class="flex items-start gap-3">
      <span class="shrink-0 w-24 rounded bg-green-100 dark:bg-green-900/40 text-green-800 dark:text-green-200 text-xs font-bold px-2 py-1 text-center">exactly-once</span>
      <span>Delivery impossible. Processing achievable via consumer dedup. 20–30% throughput cost in Kafka.</span>
    </div>
  </div>
</div>

## Why at-least-once is the safe default

At-most-once accepts loss. For a payment system, a lost "charge customer" message means a missed transaction — unacceptable. At-least-once accepts duplicates. A duplicate "charge customer" message means charging twice — also bad, but *fixable* on the consumer side. Exactly-once delivery is not achievable without adding expensive coordination.

The industry convergence: use at-least-once delivery + make the consumer idempotent. The consumer checks "have I already processed this message ID?" before acting. Duplicates are ignored. The queue can retry freely without causing harm.

<Quiz
  id="01-q1"
  pieceSlug="01-three-guarantees"
  lang="en"
  question="Which delivery guarantee is the default for SQS and Kafka?"
  choices={[
    { label: "Exactly-once — the broker handles all deduplication", misconception: "Exactly-once requires opt-in mechanisms (transactions, idempotent producer); the default is at-least-once." },
    { label: "At-least-once — retries until the consumer acks, accepting duplicates as the cost of durability", correct: true },
    { label: "At-most-once — never retries to keep latency low", misconception: "At-most-once is opt-in for latency-critical use cases; the default is at-least-once for durability." },
    { label: "It varies per message randomly", misconception: "Delivery semantics are deterministic contracts; defaults are documented per system." },
  ]}
/>

<Quiz
  id="01-q2"
  pieceSlug="01-three-guarantees"
  lang="en"
  question="Why is exactly-once processing achievable while exactly-once delivery is not?"
  choices={[
    { label: "Delivery requires both producer and consumer to agree on the same fact over an unreliable channel — mathematically impossible (Two Generals). Processing only requires one side (the consumer) to check, which is achievable via a dedup log.", correct: true },
    { label: "Hardware limits the network", misconception: "The issue is the unreliable-channel impossibility proof, not hardware." },
    { label: "Brokers can guarantee one delivery if you pay for premium tiers", misconception: "No paid tier solves the underlying impossibility; the fix is always consumer-side dedup." },
    { label: "Disabling retries achieves exactly-once", misconception: "Disabling retries gives at-most-once, not exactly-once — messages can still be lost." },
  ]}
/>

<DragOrder
  id="01-drag-1"
  pieceSlug="01-three-guarantees"
  lang="en"
  prompt="Order the steps of an at-least-once payment flow with consumer-side dedup:"
  items={[
    "Producer generates a unique message ID and sends 'charge $50' to the broker",
    "Broker stores the message durably and acks the producer",
    "Consumer receives the message and reads the ID 'msg-7a3f'",
    "Consumer checks the dedup store: 'have I processed msg-7a3f before?'",
    "Dedup store says 'no' — consumer calls the payment API and records the charge",
    "Consumer writes 'msg-7a3f processed' to the dedup store",
    "Consumer acks the broker — message removed from the queue",
  ]}
/>

</Explanation>

<KeyTakeaway>At-most-once loses messages; at-least-once duplicates them; exactly-once delivery is impossible. The production standard: at-least-once + idempotent consumer = effectively exactly-once processing.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-three-guarantees-retrieval"
  lang="en"
  questions={[
    { q: "Name the three delivery guarantees and one use case each.", a: "At-most-once: metrics/logs (loss acceptable). At-least-once: payment events, order processing (loss unacceptable, duplicates fixable). Exactly-once: stream-to-stream inside Kafka using transactions (expensive, 20–30% overhead)." },
    { q: "Why is at-least-once the production default instead of exactly-once?", a: "Pure exactly-once delivery is mathematically impossible (Two Generals' Problem). Kafka-style transactional exactly-once costs 20–30% throughput and ties the system to one broker ecosystem. At-least-once + idempotent consumer achieves the same correctness across any broker at much lower cost." },
    { q: "What property must a consumer have to make at-least-once delivery safe?", a: "Idempotency — processing the same message multiple times produces the same result as processing it once. Implemented via a dedup store keyed by message ID that the consumer checks before acting." },
  ]}
/>

<Recap lang="en">
Every queue offers a delivery contract: at-most-once accepts loss, at-least-once accepts duplicates, exactly-once delivery is impossible because the Two Generals' Problem proves no finite protocol over an unreliable channel can give both parties certainty of agreement. Exactly-once *processing* is achievable: the consumer maintains a dedup log keyed by message ID and skips any message it has already handled. This converts at-least-once delivery into exactly-once processing at the cost of a dedup store, which is why at-least-once is the production default in SQS, Kafka, and RabbitMQ.
</Recap>
