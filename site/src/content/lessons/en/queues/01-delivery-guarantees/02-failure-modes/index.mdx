---
concepts:
- visibility-timeout
- ack
- duplicate-charge
- failure-leg
- at-least-once
deepensInto:
- queues/01-delivery-guarantees/03-consumer-dedup
- queues/01-delivery-guarantees/05-sqs-dlq
estMin: 10
lang: en
lessonType: topic
level: middle
order: 2
prereqs:
- queues/01-delivery-guarantees/01-three-guarantees
slug: 02-failure-modes
sources:
- https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-visibility-timeout.html
- https://docs.confluent.io/kafka/design/delivery-semantics.html
spiral:
- statefulness
status: ready
summary: How producer-to-broker, broker durability, and broker-to-consumer failures
  each produce duplicates or loss, traced through a real double-charge incident.
title: The three failure legs — where duplicates and losses actually happen
track: queues
unit: 01-delivery-guarantees
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
An on-call engineer is staring at a graph showing 1,200 Stripe charges for 900 orders. The queue has been running for six months without issue. Tonight it charged customers twice. Nothing in the code changed. The failure was always there — waiting for the right combination of slow Lambda and SQS visibility timeout.
</Hook>

<Crux>Three failure legs; at-least-once can duplicate at any one; only the consumer side reliably catches every case.</Crux>

<Explanation>

## The three failure legs

A message travels across three distinct communication boundaries. Each boundary can fail in ways that produce either loss or duplicates.

**Leg 1 — Producer to broker.** The producer sends a message and waits for a broker ack. If the network drops the send, the message never arrives. If the network drops the *ack*, the producer never learns the message was stored — it retries, and the broker receives the message a second time. At-least-once producers retry on timeout; this is a feature, not a bug, but it means the broker can see the same logical message more than once.

**Leg 2 — Broker durability.** The broker acks the producer but must persist the message before it crashes. If replication lag exists when the broker crashes, followers may not have the message. Proper configuration (Kafka `acks=all`, SQS durability by default) covers this leg, but misconfiguration silently drops messages here.

**Leg 3 — Broker to consumer.** This is where most production duplicates happen. The broker delivers a message to the consumer. The consumer processes it (calls Stripe, updates the DB), then sends an ack. If the consumer crashes *after processing but before acking*, the broker has no record of success — it redelivers the message to the next consumer. The side effect runs twice.

<div data-lesson-visual class="overflow-x-auto my-6 rounded-lg border border-border bg-surface-1 p-4 text-sm font-mono">
  <div class="mb-2 text-ink-2 text-xs uppercase tracking-wider">Three failure legs</div>
  <div class="flex flex-col gap-2 text-xs">
    <div class="flex items-center gap-2">
      <span class="shrink-0 w-32 font-bold text-yellow-700 dark:text-yellow-300">Producer → Broker</span>
      <span>Ack lost → producer retries → broker sees message twice</span>
    </div>
    <div class="flex items-center gap-2">
      <span class="shrink-0 w-32 font-bold text-orange-700 dark:text-orange-300">Broker durability</span>
      <span>Crash before replicate → message lost (config failure)</span>
    </div>
    <div class="flex items-center gap-2">
      <span class="shrink-0 w-32 font-bold text-red-700 dark:text-red-300">Broker → Consumer</span>
      <span>Consumer processes then crashes before ack → broker redelivers</span>
    </div>
  </div>
</div>

Leg 3 is worth tracing one level deeper, because the *order* of process and ack decides whether a fault becomes a duplicate or a loss. The diagram below follows the consume→process→ack lifecycle and marks where each failure strikes.

<FlowDiagram
  label="Consume → process → ack: where loss and duplicate strike"
  hue="var(--d-network)"
  nodes={[
    { id: "deliver", label: "Deliver", sub: "broker → consumer", col: 0, row: 0 },
    { id: "process", label: "Process", sub: "charge / DB write", col: 1, row: 0 },
    { id: "ack", label: "Ack", sub: "delete message", col: 2, row: 0 },
    { id: "done", label: "Done", sub: "exactly once", col: 3, row: 0 },
    { id: "dup", label: "Duplicate", sub: "side effect 2x", col: 1, row: 1 },
    { id: "loss", label: "Loss", sub: "work never ran", col: 2, row: 1 },
  ]}
  edges={[
    { from: "deliver", to: "process" },
    { from: "process", to: "ack" },
    { from: "ack", to: "done" },
    { from: "process", to: "dup", label: "timeout expires mid-process → redeliver" },
    { from: "ack", to: "dup", label: "crash after process, before ack → redeliver" },
    { from: "ack", to: "loss", label: "ack before process, then crash" },
  ]}
  caption="Happy path runs left to right. Crash after processing but before acking, or a visibility timeout firing mid-process, both redeliver → duplicate. Acking before processing (at-most-once ordering) then crashing loses the work entirely."
/>

## Tracing the double-charge incident

Here is the exact SQS visibility-timeout scenario that trips engineers every six months.

1. **T=0s:** Lambda receives `msg-7a3f` ("charge $50 for order O-123"). SQS hides the message from other consumers for the visibility timeout (default 30s).
2. **T=0.5s:** Lambda calls Stripe. Stripe charges the card. `charge_id=ch_abc123` returned.
3. **T=29s:** Lambda is still running (slow downstream DB write). It has not yet called `DeleteMessage`.
4. **T=30s:** SQS visibility timeout fires. `msg-7a3f` becomes visible to *all* consumers again — it has no idea the first Lambda succeeded.
5. **T=30.1s:** A second Lambda picks up `msg-7a3f`. Calls Stripe. Second charge: `ch_xyz789`.
6. **T=31s:** First Lambda finally calls `DeleteMessage`. Message gone. But the damage is done: customer charged $100 for a $50 order.

The failure is not a bug. It is the defined behaviour of at-least-once delivery when visibility timeout is shorter than processing time.

<Quiz
  id="02-q1"
  pieceSlug="02-failure-modes"
  lang="en"
  question="A consumer reads a message, processes it successfully, but crashes before acking. What does at-least-once delivery do?"
  choices={[
    { label: "Loses the message — it was already delivered once", misconception: "That is at-most-once. At-least-once retries until ack." },
    { label: "Redelivers the message to another consumer — the side effect runs twice unless the consumer is idempotent", correct: true },
    { label: "Holds the message in limbo indefinitely", misconception: "After the visibility timeout or ack deadline expires, the message is redelivered, not held." },
    { label: "Asks the producer to resend", misconception: "The broker already has the message; it does not contact the producer." },
  ]}
/>

<Quiz
  id="02-q2"
  pieceSlug="02-failure-modes"
  lang="en"
  question="An SQS consumer's visibility timeout is 10s but average processing time is 30s. What is the failure mode?"
  choices={[
    { label: "Messages are silently dropped", misconception: "Messages are not dropped — they are redelivered, which is the actual problem." },
    { label: "Each message is redelivered while the original consumer is still processing — concurrent duplicate side effects", correct: true },
    { label: "The queue throttles the consumer", misconception: "SQS does not throttle on visibility-timeout misconfiguration; it redelivers literally." },
    { label: "Messages go to DLQ immediately", misconception: "DLQ is triggered by maxReceiveCount failures, not timeout duration." },
  ]}
/>

<DragOrder
  id="02-drag-1"
  pieceSlug="02-failure-modes"
  lang="en"
  prompt="Order the events in the double-charge incident trace:"
  items={[
    "Lambda receives msg-7a3f; SQS sets visibility timeout 30s",
    "Lambda calls Stripe successfully — charge ch_abc123 created",
    "Visibility timeout expires; msg-7a3f becomes visible to all consumers",
    "Second Lambda receives msg-7a3f; calls Stripe again — charge ch_xyz789 created",
    "First Lambda finally calls DeleteMessage — message removed, but two charges exist",
  ]}
/>

</Explanation>

<KeyTakeaway>At-least-once delivery duplicates at Leg 3 whenever processing time exceeds the ack window. Rule: visibility timeout = 6x average processing time. Fix: idempotent consumer so duplicates are harmless.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-failure-modes-retrieval"
  lang="en"
  questions={[
    { q: "What happens at Leg 3 when a consumer crashes after processing but before acking?", a: "The broker never receives the ack, so after the visibility timeout or commit deadline the message becomes available again and is redelivered. The side effect (charge, email, DB write) runs a second time." },
    { q: "What is the SQS visibility timeout rule of thumb and why?", a: "Set visibility timeout to 6x average processing time. This ensures the timeout does not fire while the first consumer is still legitimately working. For variable processing times, extend the timeout via ChangeMessageVisibility (heartbeat pattern)." },
    { q: "Why does losing the ack at Leg 1 cause duplicates, not loss?", a: "The producer retries when it does not receive an ack. The broker may have persisted the message already and now receives a second copy. Without dedup on the broker side (Kafka idempotent producer) or consumer side, the message is delivered twice." },
  ]}
/>

<Recap lang="en">
Every message queue has three failure legs: producer-to-broker (lost ack causes producer retry), broker durability (replication misconfiguration causes loss), and broker-to-consumer (processing succeeds but ack lost causes redelivery). The most common production duplicate is at Leg 3: consumer processes a Stripe charge, crashes before acking SQS, visibility timeout fires, a second consumer picks up the same message and charges again. The structural fix is an idempotent consumer with a dedup store, plus a visibility timeout set to at least 6x average processing time to prevent concurrent duplicate processing.
</Recap>
