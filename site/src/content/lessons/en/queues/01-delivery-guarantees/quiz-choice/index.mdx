---
concepts:
- at-least-once
- idempotent-consumer
- visibility-timeout
- kafka-transactions
- outbox-pattern
- two-generals
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://docs.confluent.io/kafka/design/delivery-semantics.html
- https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/sqs-visibility-timeout.html
- https://en.wikipedia.org/wiki/Two_Generals%27_Problem
status: ready
summary: "Multiple-choice synthesis across the delivery-guarantees unit — the impossibility proof, the three failure legs, idempotent consumers, Kafka EOS, SQS timeouts, and the outbox."
title: 'Delivery guarantees: multiple-choice review'
track: queues
unit: 01-delivery-guarantees
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each mirrors a decision you make in a real incident — not a definition to recite, but a tradeoff to weigh while customers are being double-charged.
</Hook>

<Goal lang="en">
Confirm you can connect the impossibility proof, the failure legs, consumer idempotency, Kafka transactions, SQS mechanics, and the outbox into one coherent end-to-end correctness story.
</Goal>

<Quiz
  id="01-delivery-guarantees-choice-1"
  lessonSlug="01-delivery-guarantees"
  lang="en"
  question="A vendor's queue advertises 'exactly-once delivery'. A senior reviewer pushes back. What is the precise, correct objection?"
  choices={[
    { label: "Exactly-once delivery is impossible over an unreliable channel (Two Generals): both sides can never reach mutual certainty about a single delivery. What is achievable is exactly-once processing — one-sided certainty via a consumer dedup log. Read the badge as at-least-once plus dedup somewhere.", correct: true },
    { label: "The objection is wrong — modern brokers solved this with stronger replication.", misconception: "More replicas improve durability, not two-sided agreement. No amount of replication lets producer and consumer share certainty over a lossy channel; the impossibility is information-theoretic, not a hardware limit." },
    { label: "It is fine as long as you also enable TCP, which deduplicates at the packet level.", misconception: "TCP dedups packets within a connection; it says nothing about application-level redelivery after a consumer crash or reconnect. Delivery-guarantee duplicates live above the transport layer." },
    { label: "Exactly-once is achievable but only with synchronous two-phase commit on every message.", misconception: "Two-phase commit across producer, broker, and consumer is impractical at queue throughput and still cannot defeat the Two Generals asymmetry. The production answer is one-sided consumer dedup, not distributed agreement." },
  ]}
/>

<Quiz
  id="01-delivery-guarantees-choice-2"
  lessonSlug="01-delivery-guarantees"
  lang="en"
  question="Across the three failure legs, which one produces the duplicates you actually fight in production, and why does fixing it land on the consumer?"
  choices={[
    { label: "Leg 3, broker to consumer: the consumer performs the side effect, then crashes before acking, so after the timeout the broker redelivers and the side effect runs again. The broker cannot know the side effect already happened — only the consumer can record that, so the durable fix is consumer idempotency.", correct: true },
    { label: "Leg 1, producer to broker — a lost producer ack is the dominant duplicate source.", misconception: "Leg 1 lost-ack retries do cause duplicates, but Kafka's idempotent producer neutralises them at ~3% cost. The recurring production pain is Leg 3, where processing already had a visible side effect before the crash." },
    { label: "Leg 2, broker durability — replication lag duplicates messages on failover.", misconception: "Leg 2 is a loss risk under misconfiguration (acks not all), not a duplicate source. Correctly configured durability does not generate duplicates." },
    { label: "None — duplicates are a myth if you set the timeout high enough.", misconception: "A high timeout reduces concurrent-duplicate windows but never eliminates the post-processing-pre-ack crash. At-least-once means duplicates are a structural certainty you design around, not tune away." },
  ]}
/>

<Quiz
  id="01-delivery-guarantees-choice-3"
  lessonSlug="01-delivery-guarantees"
  lang="en"
  question="A team implements consumer dedup as: SELECT the msg_id; if absent, call Stripe and INSERT it. Under a consumer-group rebalance they still see double charges. What is the structural defect?"
  choices={[
    { label: "SELECT-then-act is not atomic. Two consumers can both SELECT 'absent' for the same message during a rebalance, both call Stripe, then both INSERT. The fix is INSERT-first inside one transaction: a UNIQUE violation rolls the whole unit back so the side effect never runs twice.", correct: true },
    { label: "They forgot to raise the visibility timeout; the SELECT pattern itself is sound.", misconception: "A longer timeout shrinks the concurrent window but the SELECT/INSERT race is a correctness bug independent of timing — a rebalance or any concurrent delivery re-opens it. The pattern must be made atomic, not just slower to race." },
    { label: "Stripe charges twice because the Idempotency-Key only works for 24 hours.", misconception: "The TTL is not the issue here; queues redeliver within seconds. The double charge comes from the non-atomic local dedup letting two side-effect calls slip through before either INSERT lands." },
    { label: "The processed table needs an index, not a transaction.", misconception: "An index speeds the lookup but does not serialise two concurrent transactions; only a UNIQUE constraint hit inside the same transaction as the side effect prevents the duplicate." },
  ]}
/>

<Quiz
  id="01-delivery-guarantees-choice-4"
  lessonSlug="01-delivery-guarantees"
  lang="en"
  question="A Kafka Streams job has enable.idempotence=true and transactional.id set, writing to an output topic AND a Postgres table. Ops still see duplicate Postgres rows after a broker restart. Why?"
  choices={[
    { label: "Kafka transactions are atomic only within Kafka — the output-topic write plus the offset commit. The Postgres write is outside that boundary, so a crash between the Kafka commit and the DB write replays the DB write. Postgres needs its own idempotency (ON CONFLICT DO NOTHING keyed by offset/msg_id).", correct: true },
    { label: "transactional.id was not unique, so the coordinator merged the streams.", misconception: "A non-unique transactional.id causes fencing problems, but even with a perfect config Kafka transactions never span Postgres. The duplicate is structural: the DB write is outside the Kafka transaction scope." },
    { label: "The consumer was missing isolation.level=read_committed, which would have deduplicated the DB rows.", misconception: "read_committed only hides aborted Kafka records from the consumer; it has no effect on a write your code makes to Postgres. The DB still needs its own dedup." },
    { label: "Idempotent producer at ~3% was not enough; they needed ~20–30% transactions to cover the DB.", misconception: "They already have transactions. No Kafka layer reaches into Postgres — adding more Kafka EOS cost cannot fix a cross-system write. Consumer-side DB dedup does." },
  ]}
/>

<Quiz
  id="01-delivery-guarantees-choice-5"
  lessonSlug="01-delivery-guarantees"
  lang="en"
  question="An SQS consumer's average processing is 30s; the queue default visibility timeout is 30s. Engineers report sporadic concurrent double-processing. First fix, and what does it buy?"
  choices={[
    { label: "Raise the queue default to ~6x average processing (about 180s), or use a ChangeMessageVisibility heartbeat every timeout/3 for variable work. A timeout that equals processing time means the message reappears to other consumers while the first is still legitimately working — that is the concurrent-duplicate window.", correct: true },
    { label: "Lower maxReceiveCount to 1 so duplicates go straight to the DLQ.", misconception: "maxReceiveCount=1 sends every transient failure to the DLQ on first miss; it does nothing about the timeout-too-short window and turns normal retries into poison-pill investigations." },
    { label: "Switch SQS to exactly-once delivery mode.", misconception: "SQS standard queues are at-least-once and there is no 'exactly-once delivery' toggle that defeats Two Generals. The correctness fix is timeout sizing plus an idempotent consumer." },
    { label: "Set the timeout on each delivery via ChangeMessageVisibility and rely on it persisting across redeliveries.", misconception: "The extended value is scoped to that receipt handle; on redelivery the timeout resets to the queue default. You must raise the queue default itself or re-heartbeat every delivery." },
  ]}
/>

<Quiz
  id="01-delivery-guarantees-choice-6"
  lessonSlug="01-delivery-guarantees"
  lang="en"
  question="A service updates Postgres and publishes a Kafka event as two separate steps. Under load, downstream consumers occasionally never see an event whose DB row clearly committed. Which pattern fixes the root cause?"
  choices={[
    { label: "The outbox pattern: INSERT the event into an outbox table in the SAME transaction as the business update, then a separate sender publishes pending rows. This is the dual-write problem — the DB commit and the publish are independent, so a publish failure silently drops the event. The outbox makes the event durable if and only if the update committed.", correct: true },
    { label: "Add producer retries with enable.idempotence=true and the gap closes.", misconception: "Idempotent producer prevents duplicate publishes; it cannot publish an event that was never sent because the process died after the DB commit. The loss is the dual-write gap, which only a transactional outbox (or CDC) closes." },
    { label: "Wrap the Kafka publish and the Postgres commit in a distributed two-phase commit.", misconception: "XA/2PC across Postgres and Kafka is brittle, slow, and rarely supported in practice. The outbox achieves the same atomicity using only the DB's local transaction plus an async sender." },
    { label: "Increase the SQS visibility timeout so the event has time to publish.", misconception: "This is a Kafka producer-side dual-write, not an SQS consumer timing issue. Visibility timeout is irrelevant to a missing publish on the producer side." },
  ]}
/>

<Recap lang="en">
The through-line of the unit is one chain: exactly-once delivery is impossible (Two Generals), so you run at-least-once and move correctness to the consumer. Duplicates concentrate at Leg 3; the cheapest defence is an INSERT-first transactional dedup, extended by a Stripe-style Idempotency-Key for external calls. Kafka's idempotent producer (~3%) and transactions (~20–30%) only harden the within-Kafka path — cross-system writes still need consumer dedup. SQS visibility timeout must be ~6x processing (or heartbeated), and the outbox pattern closes the producer-side dual-write gap. Broker guarantees are necessary but never sufficient.
</Recap>
