---
concepts:
- exchange-types
- routing-key-binding
- smart-broker-push
- prefetch-qos
- dead-letter-exchange
- rabbitmq-vs-kafka
estMin: 12
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.rabbitmq.com/docs/exchanges
status: ready
summary: "Multiple-choice synthesis across the exchanges unit — direct/fanout/topic/headers routing, binding keys, smart-broker push, prefetch, and dead-letter failure modes."
title: 'RabbitMQ exchanges: multiple-choice review'
track: queues
unit: 03-rabbit-exchanges
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one mirrors a topology decision you make in a real design review — not a definition to recite, but a routing tradeoff to weigh under production load.
</Hook>

<Goal lang="en">
Confirm you can connect exchange type, binding keys, the smart-broker push model, and the production failure modes — the synthesis the lesson built toward.
</Goal>

<Quiz
  id="03-rabbit-exchanges-choice-1"
  lessonSlug="03-rabbit-exchanges"
  lang="en"
  question="You must deliver order events to three services today, expect more later, and one service only cares about EU orders. Which exchange type fits, and why?"
  choices={[
    { label: "topic — producers publish keys like order.created.eu; each consumer binds a pattern (order.#, order.*.eu) and a new subscriber is an ops change, not a producer deploy.", correct: true },
    { label: "fanout — broadcast everything and let each service filter in code.", misconception: "Fanout copies every message to every bound queue, so the EU service receives all US orders just to discard them — wasted bandwidth and a silent-data-loss risk if the in-consumer filter has a bug. Use fanout only when all consumers genuinely want everything." },
    { label: "direct — one binding key per service.", misconception: "Direct is exact-match, so 'all EU order events' is not one binding; you would enumerate every concrete key and re-bind on each new event type. Fine for fixed point-to-point routing, brittle for an evolving event taxonomy." },
    { label: "headers — match on a region header with x-match=any.", misconception: "Headers can express it but is slower (it matches an attribute map instead of a routing key) and rarely worth it when a dotted topic key like order.created.eu carries the same intent more cheaply." },
  ]}
/>

<Quiz
  id="03-rabbit-exchanges-choice-2"
  lessonSlug="03-rabbit-exchanges"
  lang="en"
  question="A new consumer's queue receives nothing — no errors, no exceptions, and the producer's publish calls all return success. What is the most likely cause?"
  choices={[
    { label: "The queue is not bound to the exchange with a matching key, so the broker routes the message to nowhere and drops it silently; by default basic.publish does not report unroutability.", correct: true },
    { label: "Kafka-style retention expired the messages before the consumer read them.", misconception: "RabbitMQ has no replayable log — routing is decided at publish time by bindings, not by a retention window. A late consumer cannot miss retained messages because there are none to retain." },
    { label: "The consumer's prefetch count is too low, so delivery stalled.", misconception: "A low prefetch slows throughput but still delivers; an empty queue with zero errors points at a missing or non-matching binding, not QoS." },
    { label: "The producer published to a queue instead of the exchange.", misconception: "RabbitMQ producers can only publish to an exchange — you cannot publish to a queue directly — so this is not an expressible failure mode." },
  ]}
/>

<Quiz
  id="03-rabbit-exchanges-choice-3"
  lessonSlug="03-rabbit-exchanges"
  lang="en"
  question="A topic binding uses the key order.*.eu. Which routing key matches?"
  choices={[
    { label: "order.created.eu — * matches exactly one word, so the three-word key with eu as the last word matches.", correct: true },
    { label: "order.eu — the * still matches because eu is present.", misconception: "* matches exactly one word and is mandatory; order.eu has only two words, so the middle position the * requires is absent and it does not match. Use # to match zero-or-more words." },
    { label: "order.created.payment.eu — eu is the final word.", misconception: "* matches exactly one word, not several; order.*.eu requires exactly three words, so a four-word key does not match. You would need order.#.eu to catch it." },
    { label: "order.created.us — the prefix order.created matches.", misconception: "A topic binding must match every position; the final word eu does not equal us, so the whole key fails to match regardless of the matching prefix." },
  ]}
/>

<Quiz
  id="03-rabbit-exchanges-choice-4"
  lessonSlug="03-rabbit-exchanges"
  lang="en"
  question="A teammate says RabbitMQ and Kafka are interchangeable queues, so a late-joining consumer can replay last week's events from either. Where is the error?"
  choices={[
    { label: "RabbitMQ is a smart broker with ack-and-delete: a consumed-and-acked message is gone, there is no log to rewind. Only Kafka's append-only log retains records for a new consumer group to read from offset zero.", correct: true },
    { label: "RabbitMQ can replay if you use a fanout exchange.", misconception: "Fanout copies live messages to currently-bound queues at publish time; it does not retain anything. A consumer added later still cannot see past events because RabbitMQ deleted each on ack." },
    { label: "RabbitMQ can replay if you raise the prefetch count.", misconception: "Prefetch governs in-flight unacked delivery, not retention or replay. Acked messages are deleted regardless of prefetch." },
    { label: "RabbitMQ can replay with quorum queues.", misconception: "Quorum queues add Raft replication for durability across node failures, not replay — an acked message is still deleted and cannot be re-read by a new consumer." },
  ]}
/>

<Quiz
  id="03-rabbit-exchanges-choice-5"
  lessonSlug="03-rabbit-exchanges"
  lang="en"
  question="One consumer holds a huge backlog of unacked messages while fast consumers sit idle, and broker memory keeps climbing toward the high-watermark. What is misconfigured, and what is the fix?"
  choices={[
    { label: "The prefetch count (basic.qos) is too high, so the broker hands one slow consumer a large reserved batch — starving others and pinning unacked messages in broker memory. Drop prefetch low (start at 1) and tune up with metrics.", correct: true },
    { label: "The exchange type should be changed from topic to direct.", misconception: "Exchange type governs which queues get a copy, not how many in-flight messages one consumer holds. Starvation plus rising unacked memory is a QoS/prefetch problem, not a routing problem." },
    { label: "The queue needs a dead-letter exchange.", misconception: "A dead-letter exchange quarantines rejected or expired messages; it does nothing about a slow consumer hogging a large prefetch batch. The fix is lowering prefetch, not dead-lettering." },
    { label: "Publisher confirms are disabled, so messages pile up.", misconception: "Publisher confirms tell the producer a message was persisted; they do not control consumer-side in-flight limits. The backlog and memory growth come from a too-high prefetch." },
  ]}
/>

<Quiz
  id="03-rabbit-exchanges-choice-6"
  lessonSlug="03-rabbit-exchanges"
  lang="en"
  question="A consumer nacks-with-requeue on a malformed payload it can never process, and one worker is now pinned redelivering the same message forever. What is the durable fix?"
  choices={[
    { label: "Configure a dead-letter exchange: the queue dead-letters messages that are rejected or expire, routing them to a quarantine queue you can inspect and reprocess, so a poison message leaves the hot path instead of looping.", correct: true },
    { label: "Switch the queue to a fanout exchange so the message spreads out.", misconception: "Fanout copies the same poison message to more queues — it multiplies the problem rather than removing the message from the redelivery loop. The fix is dead-lettering it out, not broadcasting it." },
    { label: "Raise the prefetch count so the worker can move past it.", misconception: "A higher prefetch lets the worker hold more messages, but the poison one is still requeued and redelivered; it never drains. Only dead-lettering removes it from the loop." },
    { label: "Enable the mandatory flag on publish.", misconception: "The mandatory flag returns unroutable messages to the producer at publish time; it does nothing for a message that routed correctly but fails repeatedly in the consumer. That case needs a dead-letter exchange." },
  ]}
/>

<Recap lang="en">
The through-line is one routing decision tree: exchange type is the algorithm (direct exact-match, fanout broadcast, topic dotted-pattern with * and #, headers attribute-match), bindings plus routing keys decide which queues get an independent copy, and the smart-broker push model means an acked message is deleted — no replay, unlike Kafka. The production failures all sit one layer down: an unbound exchange drops silently unless you set mandatory or a catch-all binding, a too-high prefetch starves fast consumers and grows unacked memory, and a poison message with no dead-letter exchange loops forever. Now when you review a RabbitMQ design, the first three questions are: which exchange type, are bindings correct, and is there a dead-letter route — everything downstream depends on those three being right.
</Recap>
