---
concepts:
- optimistic-ui
- read-your-own-writes
- pending-states
- idempotency-key
- conflict-resolution
- consistency-window
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://www.nngroup.com/articles/response-times-3-important-limits/
- https://tanstack.com/query/latest/docs/framework/react/guides/optimistic-updates
- https://dzone.com/articles/conflict-resolution-using-last-write-wins-vs-crdts
status: ready
summary: "Multiple-choice synthesis across the eventual-UX unit — optimistic UI, read-your-own-writes, pending states, idempotent retries, and conflict resolution under an async backend."
title: 'Eventual-consistency UX: multiple-choice review'
track: queues
unit: 07-eventual-ux
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one mirrors a UX decision you make over a real async backend — not a definition to recite, but a tradeoff to weigh while the consistency window stretches under load.
</Hook>

<Goal lang="en">
Confirm you can connect the consistency window, optimistic UI, read-your-own-writes, honest pending states, idempotent retries, and conflict resolution — the synthesis the lesson built toward.
</Goal>

<Quiz
  id="07-eventual-ux-choice-1"
  lessonSlug="07-eventual-ux"
  lang="en"
  question="A POST drops work on a queue and returns 202 Accepted in 40ms; a consumer writes the row ~700ms later. The UI POSTs then immediately refetches the list. What does the user see, and why?"
  choices={[
    { label: "Their new item is missing — the refetch raced the consumer and won, reading inside the consistency window. That is a read-your-own-writes violation: the UI trusts a read that happened before the write landed.", correct: true },
    { label: "Their new item appears instantly because 202 means the write committed.", misconception: "202 Accepted means the request was queued, not that the row exists. The data is not readable until the consumer processes the message ~700ms later." },
    { label: "A 409 Conflict, because the row does not exist yet.", misconception: "There is no conflict — a conflict needs two competing writes. The read simply happens before the write lands and returns stale, item-absent data." },
    { label: "Nothing changes because refetches are cached and never update.", misconception: "The refetch does return fresh data — just data from before the consumer wrote. Reading inside the window, not caching, is the problem." },
  ]}
/>

<Quiz
  id="07-eventual-ux-choice-2"
  lessonSlug="07-eventual-ux"
  lang="en"
  question="You are wiring an optimistic update in TanStack Query useMutation. Which step do juniors most often skip, and what breaks without it?"
  choices={[
    { label: "Snapshotting the cache in onMutate and restoring it in onError. Without the rollback path, a failed mutation leaves the predicted value on screen forever — a confident lie that never reconciles to server truth.", correct: true },
    { label: "Calling invalidateQueries in onSuccess only, never onSettled.", misconception: "Invalidating on settle (success OR error) is the more robust choice, but the step actually skipped is taking the snapshot so onError can roll back. Skipping reconciliation is bad; skipping rollback corrupts state." },
    { label: "Cancelling in-flight refetches in onMutate.", misconception: "Cancelling refetches matters so they cannot clobber the optimistic value, but the most-skipped and most-damaging omission is the snapshot-and-rollback pair — without it a failure is invisible." },
    { label: "Awaiting the mutation before rendering anything.", misconception: "Awaiting before rendering defeats the entire point of optimistic UI — it reintroduces the latency you were hiding. The missing piece is rollback, not blocking." },
  ]}
/>

<Quiz
  id="07-eventual-ux-choice-3"
  lessonSlug="07-eventual-ux"
  lang="en"
  question="A 'Pay' button shows a spinner. The event confirming the charge never arrives because the consumer crashed. With no extra handling, what happens?"
  choices={[
    { label: "Infinite spinner — the UI waits forever for an event that will not come. Every pending state needs a timeout plus a reconciliation path: after N seconds, poll the authoritative endpoint or surface 'still processing, check back.'", correct: true },
    { label: "The spinner auto-resolves once the HTTP request times out.", misconception: "The HTTP POST already returned 202; the spinner is waiting on a separate async event that no HTTP timeout governs. The request succeeded — the work did not." },
    { label: "TanStack Query rolls it back automatically after 30 seconds.", misconception: "No client library invents a server outcome. A timeout and reconciliation are something you design; nothing rolls back an event that simply never fired." },
    { label: "The user is safe to click again with no risk.", misconception: "Re-clicking without an idempotency key risks a double charge — the first request may still be sitting on the queue, about to process." },
  ]}
/>

<Quiz
  id="07-eventual-ux-choice-4"
  lessonSlug="07-eventual-ux"
  lang="en"
  question="A predictable action (toggle a checkbox that goes through a queue, consumer writes ~500ms later) is shown with a blocking spinner until the consumer confirms. What is wrong with this UX choice?"
  choices={[
    { label: "It needlessly blocks on truth for a result you could predict. You make the user sit through the full async round-trip (often over 1s, past the flow threshold) when optimistic UI would give ~0ms perceived latency. Reserve blocking for outcomes you genuinely cannot predict.", correct: true },
    { label: "Nothing — blocking on the real result is always the most correct choice.", misconception: "Correctness and good UX diverge here: the outcome is fully predictable client-side, so blocking trades perceived latency for a guarantee you did not need. Predict and reconcile instead." },
    { label: "The spinner should be a progress bar with an ETA instead.", misconception: "A richer indicator does not fix the core mistake of blocking on a predictable result. The fix is to apply optimistically, not to decorate the wait." },
    { label: "Checkbox toggles can never be made optimistic.", misconception: "A toggle is the canonical optimistic action — its outcome is fully determined client-side. Flip it now, send in the background, roll back on failure." },
  ]}
/>

<Quiz
  id="07-eventual-ux-choice-5"
  lessonSlug="07-eventual-ux"
  lang="en"
  question="Two users edit the same shared document body concurrently; both writes go through a queue. The backend resolves with last-write-wins (LWW). What is the senior critique?"
  choices={[
    { label: "LWW silently drops the loser's edit and trusts synchronized clocks — clock skew can let an older edit win. For free-form shared text it destroys data; reach for a CRDT, which lets replicas merge in any order to the same result with no coordination.", correct: true },
    { label: "LWW is fine here because the latest edit is always the one the user wanted.", misconception: "'Latest' depends on clock timestamps that can skew between nodes, and even a correct timestamp discards the other user's real work. For shared text that is data loss, not resolution." },
    { label: "You should always use CRDTs and never LWW anywhere.", misconception: "LWW is perfectly fine for a simple field like a rename, where dropping the loser is acceptable. CRDTs carry metadata overhead and can grow unbounded without GC — match the strategy to the data." },
    { label: "Conflict resolution is a backend concern; the frontend has no role.", misconception: "The frontend's job is to make the chosen strategy legible — if LWW dropped a change, say so and offer to reapply; surface a 409 as a real choice rather than a silent overwrite." },
  ]}
/>

<Quiz
  id="07-eventual-ux-choice-6"
  lessonSlug="07-eventual-ux"
  lang="en"
  question="A user clicks 'Submit', sees no feedback because the pending state was never built, and clicks again. Two messages land on the queue. What is the durable root-cause fix?"
  choices={[
    { label: "Attach an idempotency key per intent so the same key yields an at-most-once effect — the second message is deduped at the boundary. Disabling the button is a useful guard but does not survive a refresh, a retry, or a second tab; the key is the durable fix.", correct: true },
    { label: "Just disable the button after the first click.", misconception: "Disabling helps but is not sufficient — a page refresh, a network retry, or a second tab all re-enable the path. Only an idempotency key makes the effect at-most-once across all of them." },
    { label: "Add a longer spinner so the user does not click again.", misconception: "A spinner reduces the chance of a double-click but does nothing for automated retries or refreshes. You still need an idempotency key to dedupe at the boundary." },
    { label: "Retry the POST on the client until it succeeds.", misconception: "Blind client retries without an idempotency key are exactly how one intent becomes many effects. Retries are safe only when the key makes them idempotent." },
  ]}
/>

<Recap lang="en">
Across the unit the through-line is one rule: an async write is not instantly readable, so the consistency window becomes the frontend's problem. When the client can predict the result, use optimistic UI — snapshot, predict, reconcile or roll back — to buy ~0ms perceived latency against the 100ms/1s/10s thresholds. When the server owns the outcome, show an honest pending state with a timeout so a never-arriving event cannot spin forever, and never collapse a 202 into a success toast. Echo writes locally instead of trusting a racing refetch, key retries so a double-click cannot double-charge, and resolve conflicts deliberately — LWW for simple fields, CRDTs for shared text — making the outcome legible rather than silently overwriting the user.
</Recap>
