infographic chart-column-simple
data
  title Nielsen perception thresholds (ms)
  desc Why async pipelines need optimistic UI: render the predicted result at ~0ms instead of waiting out the >1s round-trip
  values
    - label Optimistic render (perceived)
      value 0
    - label ~100ms feels instantaneous
      value 100
    - label ~1s keeps user in flow
      value 1000
    - label ~10s attention breaks
      value 10000
theme
  palette #14b8a6 #6366f1 #f59e0b #f43f5e
