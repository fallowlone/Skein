---
concepts:
- total-order
- per-partition-fifo
- partition-key
- producer-reorder
- repartition-hazard
- idempotency
estMin: 12
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://kafka.apache.org/41/configuration/producer-configs/
- https://docs.aws.amazon.com/AWSSimpleQueueService/latest/SQSDeveloperGuide/FIFO-queues-understanding-logic.html
- https://cwiki.apache.org/confluence/display/KAFKA/KIP-185:+Make+exactly+once+in+order+delivery+per+partition+the+default+producer+setting
status: ready
summary: "Multiple-choice synthesis across the ordering unit — the throughput cost of total order, per-partition FIFO, partition-key choice, producer reordering, and where order silently breaks in production."
title: 'Message ordering: multiple-choice review'
track: queues
unit: 04-ordering
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one mirrors a decision you make in a real incident — not a definition to recite, but a tradeoff to weigh when ordering and throughput pull against each other.
</Hook>

<Goal lang="en">
Confirm you can connect total-order cost, per-partition FIFO, partition-key choice, producer guarantees, and the production failure modes — the synthesis the lesson built toward.
</Goal>

<Quiz
  id="04-ordering-choice-1"
  lessonSlug="04-ordering"
  lang="en"
  question="Why is global total order across all messages fundamentally a throughput tax, not just a config flag you can flip on?"
  choices={[
    { label: "A single global sequence requires a single serialization point — one partition drained by one consumer at a time. The moment two consumers run in parallel there is no longer one timeline, so total order and parallelism are physically opposed: the ordering you want is a queue of one.", correct: true },
    { label: "Total order is free; it only costs more memory to buffer messages in sequence.", misconception: "It is not a buffering cost — it is a concurrency cost. Ordering everything means serializing everything, which caps you at one consumer's throughput no matter how much memory you add." },
    { label: "Total order just needs a faster broker; throughput scales with hardware.", misconception: "Faster hardware speeds up the single serialization point but cannot parallelize it. The cap is structural — one partition, one consumer — not a hardware bottleneck." },
    { label: "Total order and partial order have the same throughput; the difference is only durability.", misconception: "They differ precisely in throughput. Partial order parallelizes across keys; total order serializes everything through one point. Durability is an orthogonal concern." },
  ]}
/>

<Quiz
  id="04-ordering-choice-2"
  lessonSlug="04-ordering"
  lang="en"
  question={"A wallet service emits credit then debit per account. It worked in staging with one consumer; in prod with eight consumers the debit sometimes lands first. What actually changed?"}
  choices={[
    { label: "Staging's single consumer accidentally serialized everything. With eight consumers, events on different partitions process in parallel and there is no single global order — the queue never promised order across consumers, only within a partition.", correct: true },
    { label: "The eight consumers each read the whole topic, so every message is processed eight times in random order.", misconception: "In a consumer group each partition is assigned to exactly one consumer; messages are not all read by everyone. The issue is parallelism across partitions, not duplicate reads." },
    { label: "Kafka silently dropped the credit event during the scale-up.", misconception: "Nothing was dropped. Both events were delivered; they were processed out of order because they landed on partitions handled by different consumers running concurrently." },
    { label: "Adding consumers increased network latency, which shuffled arrival order.", misconception: "The reorder is structural — parallel processing of independent partitions — not a latency artifact. Removing the accidental single-threading is the root cause." },
  ]}
/>

<Quiz
  id="04-ordering-choice-3"
  lessonSlug="04-ordering"
  lang="en"
  question="You need per-account ordering at high throughput on Kafka. What is the correct partition-key choice and why?"
  choices={[
    { label: "Use the accountId as the partition key. Kafka hashes it so all of one account's events land on the same partition in order, while different accounts spread across partitions and consumers — order where it matters, parallelism everywhere else.", correct: true },
    { label: "Use a random key per message so load spreads evenly across partitions.", misconception: "A random key maximizes parallelism but destroys per-account order — one account's events scatter across partitions and race. Even load is worthless if it breaks the ordering you needed." },
    { label: "Use a single fixed key so everything is totally ordered and safe.", misconception: "One key routes every message to one partition: a single-threaded pipeline. That guarantees total order but throws away all parallelism to solve a per-account problem." },
    { label: "Use a timestamp as the key so messages sort chronologically.", misconception: "Hashing a timestamp scatters an account's events across partitions by time, not by entity. Partitioning is by key hash, not sort order; the key must be the consistency boundary." },
  ]}
/>

<Quiz
  id="04-ordering-choice-4"
  lessonSlug="04-ordering"
  lang="en"
  question="Which Kafka producer configuration can silently reorder two messages on the same partition?"
  choices={[
    { label: "enable.idempotence=false with max.in.flight.requests.per.connection greater than 1 — a failed batch can retry and land after a later batch that already succeeded, swapping two messages on the partition.", correct: true },
    { label: "enable.idempotence=true with up to 5 in-flight requests — the broker rejects out-of-sequence batches.", misconception: "This is the safe default since KIP-185: the broker tracks a per-producer, per-partition sequence number and refuses out-of-sequence batches, preserving order even with 5 in-flight requests." },
    { label: "acks=all with a single in-flight request.", misconception: "One in-flight request means no later batch can overtake a retrying one, so order holds regardless of the idempotence setting." },
    { label: "Any producer config — Kafka cannot guarantee per-partition order at all.", misconception: "Per-partition order is a core Kafka guarantee. It is broken only by the specific non-idempotent, multiple-in-flight retry case." },
  ]}
/>

<Quiz
  id="04-ordering-choice-5"
  lessonSlug="04-ordering"
  lang="en"
  question="A team grows a Kafka topic from 6 to 12 partitions to add throughput and immediately sees per-key ordering corruption. Why, and what is the standard avoidance?"
  choices={[
    { label: "Kafka maps a key to a partition by hashing modulo the partition count, so changing the count remaps keys. For a window, in-flight events drain from the old partition while new ones pile into the new one — two partitions, two consumers, same key. Teams over-provision partitions up front to avoid repartitioning.", correct: true },
    { label: "Adding partitions deletes the old ones, losing in-flight messages.", misconception: "Existing partitions are not deleted; their data stays. The corruption is that a key now hashes to a different partition than its already-queued events, so two partitions briefly hold the same key." },
    { label: "More partitions always reduce ordering guarantees permanently.", misconception: "Per-partition order still holds after the dust settles; the corruption is a transient window during the remap. The durable fix is to avoid frequent repartitioning, not to use fewer partitions forever." },
    { label: "Consumers cannot handle 12 partitions, so they shuffle messages.", misconception: "Consumers handle many partitions fine. The reorder comes from the key-to-partition remap during the resize, not from consumer capacity." },
  ]}
/>

<Quiz
  id="04-ordering-choice-6"
  lessonSlug="04-ordering"
  lang="en"
  question="A profile service consumes user.updated on an at-least-once queue with concurrent consumers; updates for one user occasionally arrive out of order or twice. What is the strongest design?"
  choices={[
    { label: "Partition by userId for per-key FIFO AND stamp each event with a monotonic version, discarding any version less than or equal to the last applied. Partitioning handles the common case; the version guard catches redeliveries and late DLQ replays that partial order does not.", correct: true },
    { label: "Make the handler idempotent by message id and assume order is then fine.", misconception: "Idempotency stops double-applying the same message, but it does not order two different messages. Two distinct updates for one user can still apply in the wrong order; you also need a version check." },
    { label: "Switch the queue to exactly-once delivery and rely on it for ordering.", misconception: "Exactly-once addresses duplicates, not cross-message ordering across concurrent consumers. Treating it as an ordering guarantee is exactly the misunderstanding that ships the bug." },
    { label: "Force a single consumer and a single partition so everything is totally ordered.", misconception: "That guarantees order by serializing unrelated users against each other — capped throughput to solve a per-user problem. Correct only for a genuinely global ledger, which a profile service is not." },
  ]}
/>

<Recap lang="en">
The through-line is one decision tree: total order needs a single serialization point and caps throughput, so you almost always want partial order — per-key FIFO. Choose the partition key as the entity that must stay consistent (accountId, userId). Order then breaks in three predictable places: a non-idempotent producer with multiple in-flight requests, a repartition that remaps keys, and at-least-once redelivery or DLQ replay. The durable design partitions by key and version-guards against the residual disorder.
</Recap>
