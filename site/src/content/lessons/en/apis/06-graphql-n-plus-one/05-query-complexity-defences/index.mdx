---
concepts:
- depth-limit
- complexity-scoring
- persisted-queries
- alias-bomb
- introspection
- trusted-documents
deepensInto:
- apis/06-graphql-n-plus-one/06-graphql-api-senior
estMin: 14
lang: en
lessonType: topic
level: middle
order: 5
prereqs:
- apis/06-graphql-n-plus-one/02-dataloader-mechanics
slug: 05-query-complexity-defences
sources:
- https://graphql.org/learn/security/
- https://cheatsheetseries.owasp.org/cheatsheets/GraphQL_Cheat_Sheet.html
- https://escape.tech/blog/graphql-batch-attacks-cause-dos/
- https://www.apollographql.com/docs/graphos/schema-design/guides/handling-n-plus-one
spiral:
- statefulness
status: ready
summary: DataLoader fixes database trips. Depth limits, complexity scoring, persisted
  queries, and alias caps stop attackers from sending query shapes that kill the server
  before any resolver fires.
title: 'Query complexity defences: depth, cost, persisted queries'
track: apis
unit: 06-graphql-n-plus-one
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import defenceStrategy from "./infographics/defence-strategy.svg?raw";

<Hook>
DataLoader is live and the page loads in 80 ms. Then a security researcher sends `{ user { friends { friends { friends { ... } } } } }` 12 levels deep. The server fires for 28 seconds and returns a 502. DataLoader did its job. The problem is the query shape itself — and DataLoader cannot help with that.
</Hook>

<Crux>DataLoader fixes DB trips; depth limits, complexity scoring, and persisted queries stop attack-shaped queries before any resolver runs.</Crux>

DataLoader is live, but a single crafted query can still bring the server down before any resolver fires. This lesson covers the defences that sit in front of execution — and the numbers that separate working production configs from defaults that invite abuse.

<Explanation>

## Depth limiting

Why does query depth matter so much if DataLoader already handles batching? Because at high depth the leaf count grows exponentially — DataLoader cannot batch its way out of 10^22 IDs. A depth limiter walks the query AST and rejects documents whose nesting level exceeds a configured maximum. Typical production limit: 7–10 levels. The check runs during the validation phase — before any resolver fires, before any database is touched.

```
{ user { friends { friends { friends { ... } } } } }
  depth 1   depth 2   depth 3   depth 4
```

At depth 12 with a branching factor of 100 (each user has 100 friends), the leaf count is 100^12. Even with DataLoader batching, that is 12 batched queries each on a set of 10^22 IDs. Depth limiting kills this at parse time.

**List-depth is stricter than scalar depth.** A 10-level query where some levels return scalars is different from a 10-level query where every level returns a list. Some libraries (e.g. `graphile/depth-limit`) provide separate `maxListDepth` (typically 3–4).

## Complexity scoring

Complexity scoring assigns a cost to each field and sums across the AST. Two styles:

- **Static weights:** each field has a `@cost(value: Int!)` directive. The analyser walks the AST, sums costs, multiplies list-field costs by their `limit` arguments. Reject if sum exceeds budget (typical: 1000–10000 units).
- **Multiplicative (GitHub model):** `cost(parent) = cost(parent_fields) + sum(child.limit × cost(child_fields))`. A query that requests 100 items at each of 5 levels costs 100^5 by this formula — far over budget, rejected at AST parse time.

GitHub caps per-query cost at 1000 and publishes the cost in `extensions.cost`. Shopify Storefront caps at 1000 cost/query and 1000 cost/second/IP.

## Persisted queries (trusted documents)

Persisted queries replace the inline query string with a SHA-256 hash. The client registers known queries at build time; at request time it sends only the hash and variables. The server executes the stored document.

This closes the entire inline-query attack surface: arbitrary client-supplied queries are impossible. Introspection-driven enumeration, complexity attacks, alias bombs, and depth bombs are blocked at the gate.

**Tradeoff:** every client deploy requires a registration step. Ad-hoc tooling (Postman, browser console) no longer works against production. Public APIs that cannot constrain clients (GitHub, Shopify) keep inline queries open but add complexity scoring instead.

<Infographic
  svg={defenceStrategy}
  label="Two-column comparison of GraphQL query-shape defence strategies: persisted queries close the entire inline surface, break ad-hoc tooling, and fit when you own every client; open inline plus complexity scoring is required for public third-party APIs and relies on a per-query cost cap (~1000 units), a depth limit of 7-10, and an alias cap of 20."
  caption="The strategic fork: if you control every client, persisted queries close the inline attack surface outright; if you serve public clients like GitHub or Shopify, you keep inline open and lean on depth and complexity limits instead."
  hue="var(--d-infra)"
/>

## Alias bombs and operation batching

A single document can declare hundreds of top-level aliases for the same resolver:

```graphql
q1: user(id: 1) { email }
q2: user(id: 2) { email }
...
q1000: user(id: 1000) { email }
```

This is one valid document, but it executes 1000 resolver calls. DataLoader collapses the database trips, but resolver-execution count is still the attacker's leverage. Production caps: ≤20 root aliases per document, ≤5–10 operations per batch request.

<div data-lesson-visual class="overflow-x-auto my-6">

| Defence | What it stops | When it runs |
|---|---|---|
| Depth limit | Recursive/deep query bombs | Validation (before resolvers) |
| Complexity scoring | Cost-budget overruns | Validation |
| Persisted queries | All arbitrary inline queries | Before parsing |
| Alias cap | Alias bombs | Validation |
| Operation batch cap | Batch-request amplification | Before parsing |
| DataLoader | DB trip amplification | During resolution |

</div>

<NumbersCard
  id="card-defences"
  title="GraphQL N+1 and defence: numbers"
  stats={[
    { label: "Typical depth limit", value: "7–10 levels" },
    { label: "List-depth recommendation", value: "3–4" },
    { label: "Typical complexity budget", value: "1000–10000 units" },
    { label: "GitHub per-query cost cap", value: "1000" },
    { label: "Shopify Storefront per-query cap", value: "1000 cost units" },
    { label: "Alias-bomb cap (typical)", value: "≤20 root aliases" },
    { label: "Operation-batch cap (typical)", value: "≤5–10 operations" },
  ]}
/>

<Misconception id="mc-introspection-only">
  <p><strong>"Disabling introspection hides the schema, so attackers can't find expensive fields."</strong> Wrong. Tools like clairvoyance reconstruct the type system from error messages. Disabling introspection slows reconnaissance; it does not defend execution. Complexity scoring and persisted queries do.</p>
</Misconception>

<Quiz
  id="defences-q1"
  lessonSlug="05-query-complexity-defences"
  lang="en"
  question="Which is the strongest single line of defence for a public GraphQL API against query-complexity attacks?"
  choices={[
    { label: "Disable introspection in production", misconception: "Hiding the schema slows attackers down but does not stop complexity attacks — the schema can be reconstructed from response shapes." },
    { label: "Persisted queries (trusted documents) — the server only executes pre-registered query hashes", correct: true },
    { label: "Add HTTPS", misconception: "Transport encryption is unrelated to query-shape attacks." },
    { label: "Use REST instead of GraphQL", misconception: "Not a defence — and the question presumes the API is GraphQL." },
  ]}
/>

<DragOrder
  id="defences-drag1"
  lessonSlug="05-query-complexity-defences"
  lang="en"
  prompt="Order the safety checks a production GraphQL server runs on an incoming query:"
  items={[
    "Hash lookup: is this a known persisted query? If yes, accept and execute the stored document",
    "Parse and validate the document against the schema",
    "Depth analysis: reject if document depth exceeds the configured maximum",
    "Complexity scoring: walk the AST, sum field costs, reject if budget exceeded",
    "Authorise: check that the requesting client has the right scopes for the operation",
    "Execute resolvers via DataLoader-batched fetchers",
  ]}
/>

<Quiz
  id="defences-q2"
  lessonSlug="05-query-complexity-defences"
  lang="en"
  question="An API team enables persisted queries but leaves the inline-query endpoint open for debugging. Why is this only marginally safer than no persisted queries?"
  choices={[
    { label: "The inline endpoint is the actual attack surface; an attacker hits the inline endpoint and all persisted-query protections are bypassed", correct: true },
    { label: "Persisted queries only work when the client is authenticated", misconception: "Persisted queries are not tied to authentication; they are a query-shape constraint." },
    { label: "Debugging requires inline queries so the system is not less safe", misconception: "The safety gain of persisted queries comes entirely from closing the inline path. Leaving it open removes that gain." },
    { label: "The inline endpoint uses a different rate limit", misconception: "Rate limits are separate from query-shape defences. The inline endpoint allows arbitrary document shapes regardless of rate limits." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Cost gates run at validation — before any resolver fires"
  hue="var(--d-backend)"
  perRow={4}
  nodes={[
    { id: "query", label: "incoming query", sub: "AST", col: 0, row: 0 },
    { id: "depth", label: "depth limit", sub: "≤ 7–10", col: 1, row: 0 },
    { id: "cost", label: "complexity score", sub: "≤ budget", col: 2, row: 0 },
    { id: "exec", label: "execute", sub: "resolvers + DataLoader", col: 3, row: 0 },
    { id: "reject", label: "reject", sub: "before DB touched", col: 2, row: 1 },
  ]}
  edges={[
    { from: "query", to: "depth" },
    { from: "depth", to: "cost", label: "pass" },
    { from: "cost", to: "exec", label: "pass" },
    { from: "depth", to: "reject", label: "too deep" },
    { from: "cost", to: "reject", label: "over budget" },
  ]}
  caption="Depth and complexity are checked at validation time; an over-budget query is rejected before any resolver or DB call runs."
/>

<KeyTakeaway>Layer defences: DataLoader for DB trips, depth + complexity limits for AST-level bombs, persisted queries to close arbitrary client documents, alias and batch caps for amplification. Each layer fails closed.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="defences-retrieval"
  lang="en"
  questions={[
    { q: "What does complexity scoring do that depth limiting does not?", a: "Depth limiting counts nesting levels. Complexity scoring counts the weighted cost of fields and multiplies by list-argument sizes. A query with low depth but many high-cost list fields with large limit arguments can pass depth limits while exceeding a complexity budget. Multiplicative complexity scoring sees the 100^5 row expansion that field-count depth checks miss." },
    { q: "Persisted queries block complexity attacks. What is their operational tradeoff?", a: "Every client deploy requires registering query hashes with the server before the client goes live. Ad-hoc tooling (Postman, curl, browser devtools) cannot send arbitrary queries against production. Public APIs with third-party clients cannot fully use persisted queries without providing a separate, constrained inline-query endpoint with its own complexity scoring." },
  ]}
/>

<Recap lang="en">
DataLoader fixes the N+1 problem within resolver execution. It does nothing about query shape. Depth limits (7–10 levels) reject recursive bombs at validation time. Complexity scoring (1000–10000 budget) rejects cost-overrun documents before any resolver fires. Persisted queries close the entire inline-document attack surface by allowing only pre-registered hashes. Alias caps (≤20) and operation-batch caps (≤5–10) stop amplification attacks that defeat naive per-request rate limits. Use all layers together; each one fails closed. Now when you ship a GraphQL API, walk through the table above and check which rows are enabled — missing depth limits and alias caps are the two most common gaps that turn up in production security reviews.
</Recap>
