---
concepts:
- graphql-nplus1
- resolver-isolation
- lazy-loading
- query-planner
deepensInto:
- apis/06-graphql-n-plus-one/02-dataloader-mechanics
estMin: 10
lang: en
lessonType: topic
level: junior
order: 1
prereqs:
- performance/05-n-plus-one/01-what-is-nplus1
slug: 01-why-graphql-gets-nplus1
sources:
- https://www.apollographql.com/docs/graphos/schema-design/guides/handling-n-plus-one
- https://graphql.org/learn/execution/
- https://shopify.engineering/solving-the-n-1-problem-for-graphql-through-batching
spiral:
- statefulness
status: ready
summary: GraphQL''''s independent-resolver model hands the client flexibility and
  hands the server an accidental query storm — one HTTP request turns into hundreds
  of DB calls.
title: Why GraphQL gets N+1
track: apis
unit: 06-graphql-n-plus-one
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";

<Hook>
Bea asks for `posts { title, author { name } }` — one HTTP request for 50 posts. Sven watches the database log light up: 51 queries. The schema is correct. No bug was introduced. This is GraphQL working exactly as designed, and it is costing 600 ms instead of 80 ms.
</Hook>

<Crux>One GraphQL query of 1+N rows fires 1+N DB trips. REST avoids this: developers write JOINs. Resolvers are isolated — no global plan.</Crux>

In ten minutes you'll know exactly why isolation causes the storm — and what structural change collapses 51 queries back to 2.

<Explanation>

## How GraphQL executes a query

GraphQL resolves a document by walking it depth-first and breadth-first at each level. For every field the client selected, the server calls the resolver registered for that `Type.field` pair. Resolvers receive `(parent, args, context, info)` and return a value or a Promise. They are deliberately isolated: each resolver does not see its siblings, does not know how the parent fetched its data, and cannot inspect what other resolvers will run next.

This isolation is what makes GraphQL composable — you can add a field to a type without touching any other resolver. But it is also the direct cause of N+1.

## Why the isolation causes N+1

Consider the query `posts { title, author { name } }`. The execution engine:

1. Calls `Query.posts` → runs `SELECT * FROM posts LIMIT 50` → returns 50 post objects.
2. For each of the 50 posts, calls `Post.author(parent=post, ...)` → runs `SELECT * FROM users WHERE id = post.author_id` → 50 separate queries.

Total: 51 database round-trips for one HTTP request.

A REST controller writing the same endpoint would naturally write `SELECT posts JOIN users ON users.id = posts.author_id` — one query. The developer sees the whole response shape in one place and can optimize the SQL. In GraphQL, the `Post.author` resolver sees only one post at a time. There is no global planning point.

## The waiter metaphor

A restaurant where each table orders items one at a time. A table of six sends the waiter back six times — once per item. The kitchen is fast, but the waiter spends the shift walking. The kitchen's speed does not matter when the walk is the cost.

DataLoader is the manager who says: hold tickets until I clap, then bring them all at once. The clap is one event-loop tick. This lesson explains the problem; the next two explain the fix.

<div data-lesson-visual class="overflow-x-auto my-6">

| Without DataLoader | With DataLoader |
|---|---|
| `Query.posts` → 1 SQL | `Query.posts` → 1 SQL |
| `Post.author(post1)` → 1 SQL | `Post.author` × 50 → queue 50 IDs |
| `Post.author(post2)` → 1 SQL | DataLoader fires 1 SQL for all 50 |
| ... (50 rows) | Total: **2 SQL** |
| Total: **51 SQL** | |

</div>

<NumbersCard
  id="card-nplus1-basics"
  title="GraphQL N+1 at a glance"
  stats={[
    { label: "Naive trips for 50 posts × 1 author", value: "51 (1 + 50)" },
    { label: "With DataLoader", value: "2 (1 + 1)" },
    { label: "Reported DataLoader speedup", value: "64–84% (relational)" },
    { label: "DataLoader scope", value: "per-request, not global" },
  ]}
/>

<Quiz
  id="nplus1-basics-q1"
  lessonSlug="01-why-graphql-gets-nplus1"
  lang="en"
  question="What does 'N+1' mean in the GraphQL context?"
  choices={[
    { label: "The query depth is exactly N+1 levels", misconception: "N+1 names the call count, not the nesting depth. Depth is a separate limit." },
    { label: "One query to fetch a list of N items, then N more queries — one per item — to fetch nested fields", correct: true },
    { label: "The schema has N+1 types and one root query", misconception: "Type counts are unrelated. N+1 is purely a resolution-time pattern." },
    { label: "GraphQL caches N requests and invalidates one", misconception: "There is no automatic caching at the spec level." },
  ]}
/>

<Quiz
  id="nplus1-basics-q2"
  lessonSlug="01-why-graphql-gets-nplus1"
  lang="en"
  question="Why does GraphQL get N+1 problems while REST often does not?"
  choices={[
    { label: "REST is faster than GraphQL", misconception: "Speed is not the cause. REST avoids it because the server controls the response shape per endpoint." },
    { label: "REST endpoints are hand-written per use case, so the developer joins the data up front; GraphQL resolvers run independently per field and have no global plan", correct: true },
    { label: "GraphQL servers are single-threaded", misconception: "Most GraphQL servers are async; threading is not the issue." },
    { label: "REST uses HTTP and GraphQL does not", misconception: "GraphQL also runs over HTTP. The difference is in resolution, not transport." },
  ]}
/>

<DragOrder
  id="nplus1-basics-drag1"
  lessonSlug="01-why-graphql-gets-nplus1"
  lang="en"
  prompt="Put the steps of a naive GraphQL resolution in order:"
  items={[
    "Client sends one GraphQL query asking for posts and their authors",
    "Server resolves the top-level field 'posts' and gets back 50 posts",
    "For each post, the Post.author resolver fires — 50 independent resolver calls",
    "Each resolver runs SELECT * FROM users WHERE id = post.author_id",
    "Database executes 50 separate queries, one per post",
    "GraphQL assembles the final response and sends it to the client",
  ]}
/>

</Explanation>

<SequenceDiagram
  label="N+1 fan-out: one list query triggers N per-author queries"
  hue="var(--d-backend)"
  actors={["Resolver", "Database"]}
  messages={[
    { from: "Resolver", to: "Database", label: "SELECT * FROM posts LIMIT 50" },
    { from: "Database", to: "Resolver", label: "→ 50 post rows" },
    { from: "Resolver", to: "Database", label: "Post.author(post1): id = author_id" },
    { from: "Resolver", to: "Database", label: "Post.author(post2): id = author_id" },
    { from: "Resolver", to: "Database", label: "… 48 more per-post author queries" },
  ]}
  caption="1 list query + 50 isolated author resolvers = 51 round-trips. No global plan to join them."
/>

<KeyTakeaway>GraphQL resolvers are isolated by design — no global query plan. That isolation causes N+1: one list query triggers N per-item queries. DataLoader is the structural fix.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="nplus1-basics-retrieval"
  lang="en"
  questions={[
    { q: "Why does GraphQL get N+1 while a REST controller on the same data usually does not?", a: "A REST controller sees the whole response shape in one place and the developer naturally writes a JOIN. GraphQL resolvers are isolated — each resolver knows only its own field and parent object, so there is no natural place to write the JOIN. The engine calls each resolver independently, producing one query per parent." },
    { q: "In one sentence: what is the fix for GraphQL N+1?", a: "DataLoader batches all .load(id) calls from the same event-loop tick into one SQL query, collapsing N per-item queries into one." },
  ]}
/>

<Recap lang="en">
GraphQL execution is field-by-field and resolver-by-resolver. When a list of N items each require a nested fetch, the server fires 1+N database queries by default — not because of a bug, but because resolver isolation is how GraphQL stays composable. A page with 50 posts and their authors costs 51 round-trips without intervention. DataLoader, covered in the next lesson, collapses those 50 per-author trips into one batch, reducing the total from 51 to 2. Now when you see resolver logs lighting up with repeated single-row queries, you'll recognise the N+1 pattern immediately — and know that the fix is structural, not a one-off JOIN.
</Recap>
