---
concepts:
- batch-function
- order-preservation
- one-to-many-loader
- per-key-error
- maxBatchSize
deepensInto:
- apis/06-graphql-n-plus-one/06-graphql-api-senior
estMin: 12
lang: en
lessonType: topic
level: middle
order: 3
prereqs:
- apis/06-graphql-n-plus-one/02-dataloader-mechanics
slug: 03-batch-function-contracts
sources:
- https://github.com/graphql/dataloader
- https://www.apollographql.com/docs/apollo-server/data/fetching-data
spiral:
- statefulness
status: ready
summary: The batchLoadFn must return values in the same order as the input keys, handle
  missing rows and per-key errors explicitly, and support one-to-one, one-to-many,
  and count shapes.
title: 'Batch function contracts: ordering, shapes, errors'
track: apis
unit: 06-graphql-n-plus-one
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Sven wires DataLoader and the page is fast — for most requests. Occasionally posts show the wrong author. The SQL query ran correctly and returned the right rows. The bug is in the batch function: it returned rows in the order Postgres chose, not the order DataLoader expected.
</Hook>

<Crux>batchLoadFn(keys) must return same-length, same-order values — one entry per key. Wrong order or missing slots silently corrupt resolvers.</Crux>

This lesson covers the three contract rules that DataLoader trusts you to uphold — break any one of them and the bugs show up as wrong data in production, not as errors.

<Explanation>

## The ordering contract

`batchLoadFn(keys: Array<K>): Promise<Array<V | Error>>` must return an array of the same length as `keys`, with values at matching positions. If `keys = [7, 9, 3]` then the return must be `[row7, row9, row3]` — in that order. When you write a batch function, ask yourself: am I returning database rows in input-key order, or in whatever order the DB chose?

Postgres (and most databases) do not guarantee row order from a `WHERE id IN (...)` query. If the query returns `[row9, row7, row3]`, and you return that array directly, DataLoader resolves:
- key 7 → row9 (wrong)
- key 9 → row7 (wrong)
- key 3 → row3 (correct by coincidence)

This is a silent bug: no error is thrown, tests may pass, and production serves wrong authors on intermittent queries.

**The fix:** build a Map from the results, then walk the input keys.

```javascript
async function batchAuthors(ids) {
  const rows = await db.query(
    'SELECT id, name FROM users WHERE id = ANY($1)', [ids]
  );
  const map = new Map(rows.map(r => [r.id, r]));
  // Walk ids in input order; return null for missing rows
  return ids.map(id => map.get(id) ?? null);
}
```

## Handling missing rows

If a key has no matching row in the database, `map.get(id)` returns `undefined`. Returning `undefined` in a slot resolves the Promise to `undefined`, which propagates to the resolver as the field value. For non-nullable GraphQL fields this causes a validation error at runtime.

The correct approach depends on the field's nullability:
- Nullable field: return `null` for missing rows.
- Non-nullable field that should always exist: return `new Error('User not found: ' + id)`.

Returning an `Error` in a slot causes DataLoader to reject only that specific `.load()` Promise, not all pending loads. This is the opposite of throwing from the batch function, which rejects **all** pending Promises.

## One-to-many shape

For a field like `Post.tags`, the loader key is the post ID and the value is an array of tags. Empty arrays are mandatory for posts with no tags — if you return `undefined` or skip the slot, the next post's tags fill in.

```javascript
async function batchTags(postIds) {
  const rows = await db.query(
    'SELECT post_id, tag FROM tags WHERE post_id = ANY($1)', [postIds]
  );
  // Pre-fill every key with an empty array
  const map = new Map(postIds.map(id => [id, []]));
  rows.forEach(r => map.get(r.post_id).push(r.tag));
  return postIds.map(id => map.get(id));
}
```

## Count shape

For `Post.likeCount`, the batch runs `GROUP BY post_id COUNT(*)` and maps back to a count (defaulting to 0 for posts with no likes).

<div data-lesson-visual class="overflow-x-auto my-6">

| Shape | Key | Return per key | Pattern |
|---|---|---|---|
| One-to-one | entity ID | row or null | authorLoader |
| One-to-many | parent ID | array (possibly empty) | tagsLoader |
| Count | parent ID | integer (possibly 0) | likeCountLoader |

</div>

## maxBatchSize

When a single GraphQL document references 5000 unique authors, a single `WHERE id IN (...)` with 5000 IDs is slower than five queries of 1000 due to Postgres planner overhead. Set `options.maxBatchSize` (typical: 500–1000) to split large batches automatically.

<Inset kind="why" lang="en">
Why does DataLoader let you return `Error` per slot instead of throwing? Because throwing fails all pending `.load()` Promises — every resolver in the request gets a rejection, even those whose rows returned successfully. Per-slot errors give surgical failure: the one field with a missing row returns an error, all others resolve normally. Apollo Server translates a resolver error to a partial GraphQL response with one `null` field and one error entry.
</Inset>

<Quiz
  id="batch-contracts-q1"
  lessonSlug="03-batch-function-contracts"
  lang="en"
  question="A batch function returns rows in the order the database returned them, without building a lookup map. What is the symptom?"
  choices={[
    { label: "The batch function throws a DataLoader error", misconception: "DataLoader does not validate the ordering of the returned array. It trusts the batchLoadFn." },
    { label: "Some resolvers silently receive the wrong row because database row order does not match input key order", correct: true },
    { label: "The query is slower because extra sorting is added", misconception: "No extra sorting is added. The bug is in correctness, not speed." },
    { label: "Only the first resolver call gets the correct value", misconception: "Any resolver may get the wrong value depending on the database's arbitrary row order." },
  ]}
/>

<Quiz
  id="batch-contracts-q2"
  lessonSlug="03-batch-function-contracts"
  lang="en"
  question="A one-to-many batch function for Post.tags omits empty arrays for posts with no tags. What breaks?"
  choices={[
    { label: "The loader throws an error for the missing posts", misconception: "DataLoader does not throw on missing slots — it resolves with whatever value is in that position of the returned array." },
    { label: "Later posts receive tags intended for earlier posts, because missing slots shift all subsequent positions", correct: true },
    { label: "The server returns an empty response for the entire request", misconception: "The failure is local to the resolver — other fields resolve normally." },
    { label: "DataLoader retries the batch function automatically", misconception: "DataLoader does not retry. The contract is to return one value per key." },
  ]}
/>

<Quiz
  id="batch-contracts-q3"
  lessonSlug="03-batch-function-contracts"
  lang="en"
  question="The batch function throws an unhandled exception. Which .load() Promises are rejected?"
  choices={[
    { label: "Only the .load() call that caused the exception", misconception: "Throwing rejects all pending Promises for the batch, not just one." },
    { label: "All .load() Promises in the current batch", correct: true },
    { label: "None — DataLoader catches exceptions and returns null for each slot", misconception: "DataLoader propagates throws as batch-wide rejections." },
    { label: "Only Promises for keys that were already in the cache", misconception: "Cached Promises are already resolved. The throw affects only the current uncached batch." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="The batch contract: results aligned by index to input keys"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "k0", label: "keys[0] = 7", col: 0, row: 0 },
    { id: "k1", label: "keys[1] = 9", col: 1, row: 0 },
    { id: "k2", label: "keys[2] = 3", col: 2, row: 0 },
    { id: "fn", label: "batchFn", sub: "Map reorder", col: 1, row: 1 },
    { id: "r0", label: "results[0]", sub: "row 7", col: 0, row: 2 },
    { id: "r1", label: "results[1]", sub: "row 9", col: 1, row: 2 },
    { id: "r2", label: "results[2]", sub: "row 3", col: 2, row: 2 },
  ]}
  edges={[
    { from: "k0", to: "fn" },
    { from: "k1", to: "fn" },
    { from: "k2", to: "fn" },
    { from: "fn", to: "r0", label: "id 7" },
    { from: "fn", to: "r1", label: "id 9" },
    { from: "fn", to: "r2", label: "id 3" },
  ]}
  caption="DB rows come back unordered; the Map re-keys them so results[i] matches keys[i]. Same length, same order."
/>

<KeyTakeaway>batchLoadFn must return one entry per input key, in the same order, using a Map to reorder database results. Return null or Error per slot for missing/failed rows; throw only for batch-wide failures.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="batch-contracts-retrieval"
  lang="en"
  questions={[
    { q: "Why must batchLoadFn return values in the same order as the input keys?", a: "DataLoader maps the i-th value in the returned array to the i-th key in the input array. If the order differs, each .load() Promise resolves with the wrong value. Databases do not guarantee row order from WHERE id IN (...), so you must build a Map keyed by ID and walk the input keys in order." },
    { q: "What is the difference between returning an Error in a slot vs throwing from batchLoadFn?", a: "Returning an Error in a slot rejects only the .load() Promise for that specific key — all other Promises resolve normally. Throwing from batchLoadFn rejects all pending Promises for the entire batch. Use per-slot errors for known failures (not found, no permission); throw only for catastrophic failures (database down)." },
    { q: "For a one-to-many loader, what must you return for a parent key that has no children?", a: "An empty array — not undefined, null, or an omitted slot. If you skip the slot, the next parent's children fill its position, silently giving wrong data to every resolver downstream." },
  ]}
/>

<Recap lang="en">
The batch function contract has three rules: same length as input keys, values in the same order as input keys, and a value (or Error) for every key. Violating order silently returns wrong rows because DataLoader trusts position. Violating length shifts all downstream resolvers. Skipping empty arrays in one-to-many batches causes data from one parent to appear on another. Return per-slot Errors for known-bad rows so other resolvers in the same batch succeed; throw only when the entire batch cannot proceed. Now when you write or review a batch function, run through the three rules mentally before merging — a missing Map reorder or a skipped empty array will not show up in tests until a specific key ordering happens to expose it.
</Recap>
