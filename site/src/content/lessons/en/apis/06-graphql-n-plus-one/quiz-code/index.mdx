---
concepts:
- resolver-nplus1
- batch-function
- order-preservation
- depth-limit
- dataloader-scope
estMin: 14
lang: en
level: senior
order: 9
prereqs: []
slug: quiz-code
sources:
- https://github.com/graphql/dataloader
- https://www.apollographql.com/docs/apollo-server/data/fetching-data
- https://graphql.org/learn/security/
status: ready
summary: "Read real GraphQL resolver and DataLoader code, predict the N+1 or correctness behaviour, and pick the highest-leverage fix."
title: 'GraphQL N+1: code reading'
track: apis
unit: 06-graphql-n-plus-one
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
N+1 problems are diagnosed in resolver code, batch functions, and the validation layer — not in prose. Read each snippet, predict what it does under 50 posts or a hostile client, then choose the fix a senior engineer makes first.
</Hook>

<Goal lang="en">
Practise the loop you run on every GraphQL review: spot the per-field fetch, audit the batch contract, and confirm the query-shape gate — before the database log lights up in production.
</Goal>

## Snippet 1 — the resolver that fires N+1

```javascript
const resolvers = {
  Query: {
    posts: () => db.query('SELECT * FROM posts LIMIT 50'),
  },
  Post: {
    // called once per post, in isolation
    author: (post) => db.query(
      'SELECT * FROM users WHERE id = $1', [post.authorId]
    ),
  },
};
```

<Quiz
  id="06-graphql-n-plus-one-code-1"
  lessonSlug="06-graphql-n-plus-one"
  lang="en"
  question="For posts { title author { name } } over 50 posts, how many DB queries fire, and what is the structural fix?"
  choices={[
    { label: "51 — one for the post list plus one per post in Post.author, because resolvers are isolated and Post.author sees one post at a time. Fix: route author through a per-request DataLoader so the 50 loads batch into one WHERE id = ANY(...) query.", correct: true },
    { label: "2 — the GraphQL engine automatically batches sibling resolver calls.", misconception: "The engine does not batch; each Post.author call runs independently. You get 1 + 50 = 51 queries until you add DataLoader." },
    { label: "51 — fix it by raising the connection-pool size so the queries run in parallel.", misconception: "Parallelism does not remove the 50 queries; it only spreads the load. The fix is to collapse them into one batch with DataLoader, not to run them faster." },
    { label: "1 — Post.author reuses the rows already loaded by Query.posts.", misconception: "Query.posts returns posts, not users; Post.author must fetch each author separately. That separate fetch per post is exactly the N+1." },
  ]}
/>

## Snippet 2 — the batch function

```javascript
async function batchAuthors(ids) {
  const rows = await db.query(
    'SELECT id, name FROM users WHERE id = ANY($1)', [ids]
  );
  // returns rows in whatever order Postgres chose
  return rows;
}
```

<Quiz
  id="06-graphql-n-plus-one-code-2"
  lessonSlug="06-graphql-n-plus-one"
  lang="en"
  question="This batch function returns the raw rows. What is the latent bug, and how do you fix it?"
  choices={[
    { label: "It violates the order contract: DataLoader maps the i-th returned value to the i-th input key, but Postgres does not guarantee row order from ANY(...). Build a Map from id to row and return ids.map(id => map.get(id) ?? null) so values line up with keys.", correct: true },
    { label: "No bug — ANY($1) preserves the order of the input array.", misconception: "It does not. Row order from IN/ANY is unspecified, so values can land on the wrong keys. Reorder with a Map keyed by id." },
    { label: "The bug is that it should throw when a row is missing.", misconception: "Throwing rejects every pending .load() in the batch. For a missing row, return null (or an Error in that slot) — but the primary defect here is ordering." },
    { label: "The query should use IN (...) instead of ANY($1) to keep order.", misconception: "IN and ANY behave the same for ordering — neither guarantees it. Ordering is fixed in the batch function with a Map, not in the SQL syntax." },
  ]}
/>

## Snippet 3 — the one-to-many loader

```javascript
async function batchTags(postIds) {
  const rows = await db.query(
    'SELECT post_id, tag FROM tags WHERE post_id = ANY($1)', [postIds]
  );
  const map = new Map();
  rows.forEach(r => {
    if (!map.has(r.post_id)) map.set(r.post_id, []);
    map.get(r.post_id).push(r.tag);
  });
  return postIds.map(id => map.get(id));   // posts with no tags?
}
```

<Quiz
  id="06-graphql-n-plus-one-code-3"
  lessonSlug="06-graphql-n-plus-one"
  lang="en"
  question="A post with zero tags has no rows, so map.get(id) is undefined. What goes wrong, and what is the fix?"
  choices={[
    { label: "A one-to-many loader must return an empty array for keys with no children. Returning undefined resolves Post.tags to undefined (a validation error on a non-null [Tag!]!). Fix: map.get(id) ?? [] so tagless posts return [].", correct: true },
    { label: "Nothing — undefined and an empty array are equivalent for a list field.", misconception: "They are not. undefined breaks a non-null list field at validation, while [] is a valid empty list. Default missing keys to []." },
    { label: "The post inherits the previous post's tags.", misconception: "Because the function maps over postIds (not the raw rows), positions do not shift — but undefined slots still break the field. Return [] for tagless posts." },
    { label: "DataLoader retries the batch for the missing keys.", misconception: "DataLoader never retries; it resolves with whatever value sits in that slot. undefined must be replaced with an empty array by your code." },
  ]}
/>

## Snippet 4 — the validation gate

```javascript
const server = new ApolloServer({
  schema,
  validationRules: [depthLimit(10)],   // only depth is capped
});
```

<Quiz
  id="06-graphql-n-plus-one-code-4"
  lessonSlug="06-graphql-n-plus-one"
  lang="en"
  question="This server caps depth at 10 but nothing else. A client sends a 5-level query with first: 100 at every level. What gets through, and what is the missing layer?"
  choices={[
    { label: "It passes — depth is only 5 — yet it reads roughly 100^5 rows conceptually. Depth counts nesting, not row expansion. Add multiplicative complexity scoring (cost multiplied by the first/last list-argument) and a per-document alias cap.", correct: true },
    { label: "It is rejected — depth 10 covers all expensive queries.", misconception: "A 5-level query is under the depth cap yet expands to 100^5 rows. Depth limiting cannot see list-argument multiplication; complexity scoring can." },
    { label: "DataLoader will batch the expansion away, so no extra gate is needed.", misconception: "DataLoader collapses DB trips per level, but 100^5 rows are still read and materialised. Query-shape defences must reject the document before resolvers run." },
    { label: "Disabling introspection would have blocked this query.", misconception: "Introspection only hides the schema for reconnaissance; it does not stop an expensive query shape. Complexity scoring is the defence." },
  ]}
/>

<Recap lang="en">
Every GraphQL N+1 incident is read in code: a per-field resolver that fetches one row per parent (51 queries → DataLoader), a batch function that trusts database row order (corruption → Map by key), a one-to-many loader that drops empty arrays (validation error → default to []), and a validation layer that caps depth but not row expansion (100^5 leak → multiplicative complexity plus alias caps). Read the resolver, audit the batch contract, confirm the gate — then re-trace resolver counts to verify the fix held.
</Recap>
