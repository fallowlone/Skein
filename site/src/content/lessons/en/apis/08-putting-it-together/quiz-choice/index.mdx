---
concepts:
- resource-modeling
- status-code-retry-contract
- cursor-pagination
- openapi-breaking-change
- grpc-vs-rest
- rate-limit-headers
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Status/429
- https://www.rfc-editor.org/rfc/rfc8594.html
- https://stripe.com/docs/api/idempotent_requests
status: ready
summary: "Multiple-choice synthesis across the APIs track — resource modeling, the status-code retry contract, cursor pagination, OpenAPI break diffs, gRPC-vs-REST, and rate-limit headers as one connected design."
title: 'APIs: multiple-choice review'
track: apis
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole track. Each one mirrors a review decision on a real public API — not a definition to recite, but a place where one weak link load-bears the next.
</Hook>

<Goal lang="en">
Confirm you can trace the cascade end to end: how resource modeling sets up honest status codes, how status codes drive client retries, how pagination and the OpenAPI contract keep both correct under load, and where protocol and rate limits fit.
</Goal>

<Quiz
  id="08-putting-it-together-choice-1"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="A team modeled POST /chargeCard instead of a payments resource. Months later they get duplicate charges. What is the root link in the cascade?"
  choices={[
    { label: "The verb-shaped endpoint has nothing addressable to return, so it answers 200 for everything including duplicates — and 200 means 'done, do not retry,' which turns a dropped response into a double charge. Modeling a noun (POST /payments → 201/Location, 409 on retry) is what would have made the status codes honest.", correct: true },
    { label: "The real bug is purely the missing rate limit; modeling is cosmetic.", misconception: "The rate limit is the last guardrail, not the root. Even with a perfect limit, a verb-shaped endpoint returning 200 for duplicates still double-charges on the first retry. The cascade starts at modeling." },
    { label: "POST should never be used for payments; it should be PUT for idempotency.", misconception: "PUT is idempotent only for full replacement of a known resource id; creating a payment is a POST. Idempotency for creation comes from an Idempotency-Key, not from swapping the method." },
    { label: "The client should simply never retry, so the verb shape does not matter.", misconception: "Mobile networks fail constantly; banning retries trades double-writes for lost writes. The fix is an addressable resource plus an idempotency key that makes the retry safe." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-2"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="Which statement best captures why the HTTP status code is a contract, not decoration?"
  choices={[
    { label: "Clients act on the code automatically during an incident: 2xx means stop, 4xx means do not retry unchanged, 429/503 means back off and read Retry-After, 5xx means maybe retry only if the operation is idempotent. The code drives machine behavior, so a wrong code corrupts client logic.", correct: true },
    { label: "Status codes are mostly for logging dashboards; the response body is what clients actually read.", misconception: "Reverse priority. Generic clients and SDKs branch on the status code and headers without parsing your body; the code is the machine-readable part of the contract." },
    { label: "Any 2xx is fine as long as the body has an error field for failures.", misconception: "Returning 200 with an in-body error is the classic anti-pattern: retry logic, caches, and monitoring all read the 200 as success and never see the failure. The code must reflect the outcome." },
    { label: "5xx is always safe to retry, so servers can return it freely.", misconception: "5xx is safe to retry only when the operation is idempotent. Retrying a non-idempotent POST on a 5xx that landed after the write is exactly how you double-charge." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-3"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="An endpoint returns a 2M-row list, browsed live, using OFFSET/LIMIT, and deep pages are getting slow and occasionally skip rows. Best structural fix?"
  choices={[
    { label: "Switch to cursor (keyset) pagination: WHERE id > :last ORDER BY id LIMIT n. Cost stays O(page size) at any depth and pages stay stable under concurrent inserts, at the cost of no random jump-to-page.", correct: true },
    { label: "Add an index and keep OFFSET — the index makes deep pages fast.", misconception: "An index helps the ORDER BY, but OFFSET 100000 still forces the DB to scan and discard 100k rows on every deep page. Cost grows with depth regardless of the index." },
    { label: "Cache each page for five minutes so deep pages stop hitting the DB.", misconception: "Caching a live paginated list serves stale, drifting pages and does nothing for the long tail of unique deep pages. Keyset removes the slow path structurally instead of papering over it." },
    { label: "Cap the API at 100 pages so nobody reaches the slow part.", misconception: "That hides the symptom and breaks legitimate deep browsing. Cursor pagination eliminates the slow part rather than fencing it off." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-4"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="A deploy renamed the error envelope field from `message` to `detail` and tightened a status enum. CI was green and clients broke in production. What was missing, and which changes were the problem?"
  choices={[
    { label: "A spec-first OpenAPI break diff in CI. Renaming a field and tightening an enum are breaking changes — an old client depends on the exact old shape — so they belong behind a version bump. The green tests were written against the old shape and never caught the drift; the diff would have failed the build.", correct: true },
    { label: "Nothing was wrong with the changes; the clients were just poorly written.", misconception: "Backward compatibility means an unmodified old client still works. Renaming a field and tightening an enum break that by definition — the producer broke the contract, not the consumer." },
    { label: "Both changes were additive and safe; the failure was a flaky deploy.", misconception: "Renaming (removing the old name) and tightening validation are breaking, not additive. Additive means adding an optional field or a new enum value the client can ignore." },
    { label: "The fix is to stop writing tests and rely on manual QA instead.", misconception: "Tests against the old shape stayed green precisely because they were not regenerated from the spec. The fix is a machine diff of the new spec against the old, not removing tests." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-5"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="A teammate wants the public API to be gRPC because benchmarks show ~77% lower latency, ~10x smaller messages, and higher throughput. How should a senior reason about it?"
  choices={[
    { label: "Protocol is an axis independent of modeling, versioning, and the contract. Those numbers answer 'internal service-to-service at high volume,' not 'public contract.' A public API lives on discoverability, curl/browser reachability, and SDK ergonomics — REST + OpenAPI's strengths. Use REST at the edge and gRPC between internal services if volume justifies it.", correct: true },
    { label: "Adopt gRPC everywhere because raw latency is the metric that matters most.", misconception: "Raw latency is not the constraint a public API is optimized for. Over the public edge, gRPC kills discoverability, breaks curl/browser access, and raises onboarding friction for external developers." },
    { label: "Use REST everywhere including the 40k req/s internal hop to keep one stack.", misconception: "At 40k req/s with tiny payloads and streaming, JSON-over-HTTP burns extra CPU, memory, and bandwidth and lacks first-class streaming. The internal hop is the one place gRPC's numbers genuinely matter." },
    { label: "Switch to GraphQL since it subsumes both REST and gRPC.", misconception: "GraphQL solves client-shaped over/under-fetching; it does not address the high-throughput internal streaming hop and adds query-complexity and caching headaches at the public edge. It is a different tool, not a drop-in." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-6"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="A retry storm at peak turns into an outage: tens of thousands of duplicate writes per second hit the database. Which control would have contained it, and how should it behave?"
  choices={[
    { label: "A documented rate limit returning 429 with a Retry-After header (and ideally X-RateLimit-Remaining/Reset), so over-limit clients back off instead of hammering. Combined with Idempotency-Key on the writes, the storm is both throttled and de-duplicated.", correct: true },
    { label: "Return 503 with no headers so clients know the server is down.", misconception: "A bare 503 with no Retry-After leaves clients to guess; many retry immediately and amplify the storm. The throttle must tell clients when to come back via Retry-After." },
    { label: "Silently drop excess requests so the client never knows.", misconception: "Silently dropping looks like a network failure, which triggers more retries and hides the limit from the client entirely. A 429 with Retry-After is an explicit, machine-readable backoff signal." },
    { label: "Return 429 but with no Retry-After, since clients should know the default backoff.", misconception: "Without Retry-After clients fall back to their own (often aggressive or fixed) backoff, and the documented quota becomes guesswork. The header is what makes the backoff cooperative." },
  ]}
/>

<Recap lang="en">
The track is one chain, not a checklist. Resource modeling (nouns, non-CRUD actions as sub-resources) decides whether status codes can be honest; status codes are a machine-readable retry contract (201/409/429, Idempotency-Key on unsafe writes, Retry-After on throttles); cursor pagination keeps large/live reads flat and stable; spec-first OpenAPI with a CI break diff stops silent drift; versioning evolves additively and bumps only on real breaks with a Sunset policy; protocol is an orthogonal axis (REST at the edge, gRPC internal); and rate limits with 429 + Retry-After keep a retry storm from becoming an outage. Review the cascade, because each link load-bears the next.
</Recap>
