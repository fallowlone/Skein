---
concepts:
- spec-first-vs-code-first
- contract-drift
- breaking-change-detection
- codegen
- request-validation
- openapi-3-1
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://spec.openapis.org/oas/v3.1.0.html
- https://www.oasdiff.com/docs/breaking-changes
- https://learn.openapis.org/best-practices.html
status: ready
summary: "Multiple-choice synthesis across the OpenAPI unit — spec-first vs code-first, contract drift, breaking-change gates, codegen, validation, and the 3.1 schema shift."
title: 'OpenAPI: multiple-choice review'
track: apis
unit: 04-openapi
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions across the whole unit. Each one is a call you make on a real API — not a definition to recite, but a decision about where the truth lives and how the pipeline keeps it honest.
</Hook>

<Goal lang="en">
Confirm you can connect spec-first vs code-first, contract drift, breaking-change gates, codegen, and edge validation into one enforcement story — the synthesis the lesson built toward.
</Goal>

<Quiz
  id="04-openapi-choice-1"
  lessonSlug="04-openapi"
  lang="en"
  question="A team has a hand-written OpenAPI spec rendered to a nice docs portal. A teammate says 'we are covered against breaking changes.' What is actually missing?"
  choices={[
    { label: "A CI gate that diffs the new spec against the previous version and fails the PR on a breaking change. A spec is a file; it prevents nothing on its own until a diff tool like oasdiff blocks the merge.", correct: true },
    { label: "Nothing — having the spec means clients can see any change before they integrate.", misconception: "Clients integrate over weeks, not on your deploy day. A published spec describes the change but does not stop an engineer adding a required field and breaking every existing client in production." },
    { label: "More $ref components to deduplicate the schemas.", misconception: "$ref reduces duplication inside a spec; it does nothing about detecting incompatible changes between spec versions. That is what a breaking-change diff is for." },
    { label: "Switching from code-first to spec-first — that alone prevents breaking changes.", misconception: "How the spec is born does not gate anything. Spec-first still needs the same oasdiff gate; otherwise a reviewed-but-incompatible change merges just as easily." },
  ]}
/>

<Quiz
  id="04-openapi-choice-2"
  lessonSlug="04-openapi"
  lang="en"
  question="Your hand-written docs mark a field optional, but the running server now rejects requests that omit it. What is this, and what prevents it?"
  choices={[
    { label: "Contract drift — the spec and the implementation silently diverged. Prevented by making the spec load-bearing: validate requests against it at the edge and diff it in CI so it cannot disagree with the server unnoticed.", correct: true },
    { label: "A server bug — fix it by making the handler accept the missing field.", misconception: "The server may be correct; the defect is that the contract and the code disagree. Bending the server to match stale docs hides the real failure — nothing keeps spec and code in sync." },
    { label: "Normal API evolution — clients should retry.", misconception: "A request missing a now-required field fails identically on every retry. Drift is a process failure, not a transient one." },
    { label: "A versioning problem — bump the API to v2.", misconception: "A new version does not reconcile a spec that already lies about the current version. You first need the spec to track the server; versioning manages intended changes, not accidental drift." },
  ]}
/>

<Quiz
  id="04-openapi-choice-3"
  lessonSlug="04-openapi"
  lang="en"
  question="A public API is consumed by mobile apps you cannot force-update and three external partners. Which enforcement strategy fits?"
  choices={[
    { label: "Spec-first source of truth, an oasdiff breaking-change gate in CI, and generated SDKs for consumers. The contract is reviewed before code and a silently-incompatible change cannot merge.", correct: true },
    { label: "Generate the spec from code annotations and publish it to a docs portal.", misconception: "Code-first docs describe the current code but never compare today's contract to yesterday's. A newly-required field is faithfully documented and still breaks every old client. Code-first is fine, but it needs the same diff gate on top." },
    { label: "Write the spec by hand and review it manually in PRs.", misconception: "Human review routinely misses breaking changes — narrowing a type or adding a required field looks innocuous in a 400-line diff. The reason oasdiff exists is that 'someone will catch it' does not survive a Friday deploy." },
    { label: "Skip the spec and keep an API changelog teams read before integrating.", misconception: "A changelog is a wiki page by another name: it drifts, it is written after the fact, and it enforces nothing. The clients that broke would break identically with a changelog nobody re-read." },
  ]}
/>

<Quiz
  id="04-openapi-choice-4"
  lessonSlug="04-openapi"
  lang="en"
  question="You generate a TypeScript client from the spec and the result is full of any and loose untyped objects. What is the root cause?"
  choices={[
    { label: "The spec has loose schemas — additionalProperties left open, missing required arrays, untyped object blobs. A generator encodes the contract you feed it, so a loose contract yields a loose client. Tighten the schemas with explicit types, explicit required, and reused components.", correct: true },
    { label: "The generator is buggy — switch tools.", misconception: "Every conforming generator produces loose output from a loose schema; that is faithful, not buggy. The fix is upstream in the contract, not the tool." },
    { label: "TypeScript cannot express OpenAPI schemas precisely.", misconception: "TypeScript expresses these shapes fine when the schema is tight. The any-ridden output reflects an under-specified contract, not a language limit." },
    { label: "Code-first generation always produces stronger types than spec-first.", misconception: "Type quality tracks schema tightness, not which direction the spec was generated. A sloppy code-first spec generates an equally sloppy client." },
  ]}
/>

<Quiz
  id="04-openapi-choice-5"
  lessonSlug="04-openapi"
  lang="en"
  question="You upgrade the spec from OpenAPI 3.0 to 3.1. Which change is real and why does it matter?"
  choices={[
    { label: "3.1 aligns schemas with JSON Schema 2020-12: nullable: true is gone in favour of type: ['string', 'null'], and exclusiveMinimum/Maximum take a numeric value, not a boolean. Validation keywords are now real JSON Schema, but many tools still default to 3.0 — check generator support first.", correct: true },
    { label: "3.1 removes $ref, so you must inline every schema.", misconception: "$ref is unchanged and still the way you reuse components. 3.1's schema changes are about nullability and JSON Schema alignment, not references." },
    { label: "3.1 makes all request validation automatic with no middleware.", misconception: "3.1 changes the schema language, not the runtime. You still need a validator middleware at the edge to enforce requests against the spec." },
    { label: "3.1 is purely cosmetic and changes nothing in your schemas.", misconception: "The nullability rewrite is a concrete syntactic change — a removed 'null' from a type array even registers in a diff as a type change rather than a nullable change." },
  ]}
/>

<Quiz
  id="04-openapi-choice-6"
  lessonSlug="04-openapi"
  lang="en"
  question="Which change to a request/response contract is backward compatible and safe to ship without a major version bump?"
  choices={[
    { label: "Adding a new optional request field, or adding a new field to a response. Existing clients keep sending what they sent and ignore unknown response fields, so neither breaks them.", correct: true },
    { label: "Adding a new required request field.", misconception: "Every existing client that does not send it is now rejected. Adding a required field is a classic breaking change — the exact case a diff gate must catch." },
    { label: "Removing a field from a response body.", misconception: "A client may read that field; removing it breaks consumers silently. Removing response fields is breaking." },
    { label: "Narrowing a string field to an enum of three values.", misconception: "Narrowing a type rejects previously-valid values. Tightening an existing field is breaking even though it looks like a harmless clarification." },
  ]}
/>

<Recap lang="en">
The unit's through-line is one enforcement story: a spec is a contract only when CI makes it load-bearing. Spec-first vs code-first decides where the truth lives and whether the contract is reviewed before code, but neither escapes the gate — validate requests at the edge, diff every PR with oasdiff so breaking changes (new required field, removed response field, narrowed type) fail the build, and feed tight schemas to the generator so codegen yields strong types. OpenAPI 3.1 moves schemas to JSON Schema 2020-12, replacing nullable with type arrays. Unenforced, the spec drifts into fiction and breaks clients.
</Recap>
