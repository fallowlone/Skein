---
concepts:
- status-code-classes
- 4xx-vs-5xx-ownership
- error-body-truth
- idempotency-retry
- 429-503-retry-after
- 401-vs-403
estMin: 14
lang: en
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://datatracker.ietf.org/doc/html/rfc9110
- https://developer.mozilla.org/en-US/docs/Web/HTTP/Reference/Status
status: ready
summary: "Free-recall prompts across the status-code unit. Answer each from memory first, then reveal the model answer and compare."
title: 'Status codes: free-recall review'
track: apis
unit: 02-status-codes-real
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory before you open the model answer — the effort of recall is what makes the distinctions stick when you are choosing a code under pressure.
</Hook>

<Goal lang="en">
Reconstruct the unit's spine without looking back: the class-as-machine-instruction model, the 4xx/5xx ownership split, why 200-with-error-body is the cardinal sin, the idempotency retry trap, and the Retry-After ordering.
</Goal>

<RetrievalDrawer
  client:load
  id="02-status-codes-real-short-retrieval"
  lang="en"
  questions={[
    { q: "Why is the first digit of a status code a machine instruction, and which machines branch on it before any human reads the response?", a: "The status class is read by infrastructure that never looks at your response body: caches branch on it to decide whether the response is storable, load balancers and reverse proxies branch to decide routing and health, APM/monitoring agents branch to count success vs error rate, and client retry middleware branches to decide whether to resend. They all act on the class immediately. 2xx = success, cacheable per headers; 3xx = go elsewhere, follow Location or use the cache; 4xx = the client sent something wrong, do not retry as-is; 5xx = the server failed, the request may have been valid so a retry might work. Because these decisions happen on the class alone, getting it wrong makes every downstream machine make the wrong call — the cache stores a failure, the retry loop hammers a doomed request, the dashboard miscounts the outage." },
    { q: "Explain the 4xx-vs-5xx ownership split and why it is the load-bearing distinction for retries.", a: "4xx means the fault is the client's: the request itself is wrong — bad syntax, failed business rule, missing permission — so resending the identical bytes produces the identical failure. 5xx means the fault is the server's: the request may have been perfectly valid and the failure transient, so a retry can succeed. That split is exactly the retry decision: do not retry a 4xx (it will fail identically), but a 5xx is a candidate for retry. The one explicit exception is 429 (rate limited), which lives in the 4xx range but is retryable after a delay. Get the class wrong — say a 500 for a bad input — and you tell every retry loop to hammer a request that can never succeed; emit a 200 for a real server failure and the retry that would have recovered never fires." },
    { q: "Why is returning 200 OK with an error object in the body the cardinal sin, and what specifically breaks?", a: "It makes the status code lie to every machine that branches on it. Caches may store the failed response and serve it to other users as valid. Monitoring counts the request as a success, so error-rate dashboards stay green during a real outage — the canonical case is a gateway that caught upstream failures and returned 200 with an error body, so a total outage looked like 100% health for forty minutes. Client retry logic keyed off 5xx/429 never fires, because it saw a 2xx, so transient failures a retry would have fixed surface to the user as broken. And every client must now parse the body to discover whether 'success' was real, coupling them to your bespoke error envelope. The fix is to make the status tell the truth — correct 4xx/5xx — and put the human-readable detail in the body of that correct status." },
    { q: "Walk through the retry decision including the idempotency trap on a timed-out write.", a: "Start from the class. A 4xx other than 429 means the request is wrong; do not retry, fix it. A 5xx or 429 means the server failed or rate-limited you and the request may have been valid, so a retry can succeed — but only if the operation is safe to repeat. GET, PUT, and DELETE are idempotent: running them twice has the same effect as once, so retrying is safe. POST is not. The dangerous case is a 504 (gateway timeout) on a POST: the backend may have completed the work before the gateway gave up, so the outcome is genuinely unknown. Blindly retrying risks a duplicate (e.g. a double charge); the correct move is to not retry the write and reconcile by reading state, or to send an idempotency key (as Stripe and Square require) so the server deduplicates retries. When you do retry, honour Retry-After if present, else exponential backoff with jitter." },
    { q: "Give the Retry-After ordering for 429 and 503, and why instant retries are dangerous.", a: "Both 429 (rate limited) and 503 (service unavailable / shedding load) should carry a Retry-After header — either a number of seconds or an HTTP date — because the server knows its own window better than the client. The client's ordering is: honour Retry-After first; only when it is absent, fall back to exponential backoff with jitter (1s, 2s, 4s, 8s…). Instant, no-delay retries are dangerous because a fleet of clients all retrying immediately synchronises into a thundering herd that keeps an already-overloaded server down — the retries become the outage. Jitter de-synchronises the clients so the load spreads out instead of arriving in a spike. Google Cloud Storage, OpenAI, and Stripe all document this exact ordering: read Retry-After, then back off with jitter." },
    { q: "Distinguish 401 vs 403 and 400 vs 422, and say what the client should fix in each case.", a: "401 Unauthorized means 'I don't know who you are' — no, expired, or invalid credentials; the fix is to authenticate (log in or refresh the token) and retry. 403 Forbidden means 'I know exactly who you are, and you still cannot do this' — no credential change helps; the client must stop. Returning 401 for an authorization failure sends a valid-token client into a pointless re-login loop. 400 Bad Request means the request is malformed — unparseable JSON, a missing required field — so the client must fix the request structure. 422 Unprocessable Content means it parsed fine but the values fail business rules (negative quantity, bad email) — so the client must fix the data and the server should return field-level errors. The pattern: 401/400 are about the request's identity/shape, 403/422 are about what is allowed/valid given a well-formed request." },
  ]}
/>

<Recap lang="en">
If you reconstructed each answer from memory, you hold the unit's spine: the class is a machine instruction read by caches, dashboards, and retry loops; 4xx is the client's fault (don't retry) and 5xx is the server's (maybe retry), with 429 the retryable 4xx exception; 200-with-error-body is the cardinal sin because it blinds every machine; the idempotency trap turns a retried 504 POST into a double charge unless you reconcile or send an idempotency key; Retry-After is honoured before backoff with jitter; and 401/400 versus 403/422 tell the client whether to re-auth/fix-shape or stop/fix-data. Now when you choose a status code under pressure, you are not guessing — you are making a routing decision for every machine that will read it before any human does.
</Recap>
