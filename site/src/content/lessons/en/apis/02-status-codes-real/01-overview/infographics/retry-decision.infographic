infographic compare-quadrant-quarter-simple-card
data
  title Retry decision: method idempotency x failure class
  compares
    - label GET/PUT + 5xx/429: retry safely
    - label GET/PUT + 4xx: stop, fix request
    - label POST + 5xx/429: idempotency key
    - label POST + 4xx: stop, fix request
theme
  palette #10b981 #f43f5e #f59e0b #f43f5e
