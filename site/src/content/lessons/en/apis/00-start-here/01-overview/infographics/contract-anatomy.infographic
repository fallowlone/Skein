infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label API = the contract
    children
      - label Request (client → server)
        children
          - label HTTP method (GET / POST / PUT)
          - label Endpoint (a URL)
          - label Resource (the noun named)
          - label Request body (optional data)
      - label Response (server → client)
        children
          - label Status code (200 / 404 / 500)
          - label Response body (the result)
          - label JSON (shared data format)
theme
  palette #6366f1 #14b8a6 #f59e0b
