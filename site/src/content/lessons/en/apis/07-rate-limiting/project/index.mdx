---
concepts:
- distributed-rate-limiting
- token-bucket
- atomic-lua
- retry-after
- per-key-quota
estMin: 240
lang: en
level: senior
order: 10
prereqs: []
slug: project
sources:
- https://redis.io/docs/latest/develop/use-cases/rate-limiter/
- https://blog.arcjet.com/rate-limiting-algorithms-token-bucket-vs-sliding-window-vs-fixed-window/
- https://datatracker.ietf.org/doc/html/draft-ietf-httpapi-ratelimit-headers
status: ready
summary: "Hands-on project — build a distributed, atomic token-bucket limiter in Redis, prove it holds the real limit across multiple nodes, and ship a correct 429 contract."
title: 'Rate limiting: build a distributed limiter that actually holds'
track: apis
unit: 07-rate-limiting
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Reading about the per-node-counter lie is not the same as watching your limit silently triple in production. Build a limiter, run it behind a load balancer across several nodes, then attack it the way a real client does — across the window boundary and across every node at once — and prove the limit holds.
</Hook>

<Goal lang="en">
Turn the unit's mental model into a working artifact: a distributed token-bucket limiter whose counter lives in Redis, whose check is atomic, and whose 429 response is a correct contract — verified with a load test that defeats a naive per-node limiter.
</Goal>

<ProjectBrief
  id="07-rate-limiting-project"
  lessonSlug="07-rate-limiting"
  lang="en"
  objective="Build a rate-limiting middleware that enforces a real per-API-key limit across at least 3 app nodes behind a load balancer, using an atomic Redis token bucket, and prove with a load test that the global limit holds and the 429 contract is correct."
  requirements={[
    "Stand up a small HTTP service with a rate-limit middleware, run at least 3 instances behind a load balancer (nginx, Caddy, or a Docker Compose round-robin). The limiter must key per API key, e.g. 100 req/min with a burst capacity of 100.",
    "First implement a deliberately naive per-node in-memory counter and load-test it across all nodes to demonstrate the failure: record the effective limit and show it is roughly N times configured.",
    "Replace it with a token bucket whose state (tokens + last-refill timestamp) lives in Redis, evaluated by a single Lua script so the read-decide-update is atomic. No app-side GET-then-SET.",
    "On rejection, return 429 Too Many Requests with Retry-After (delta-seconds until the next token frees) and the IETF RateLimit-Limit / RateLimit-Remaining / RateLimit-Reset headers; RateLimit-Reset must be delta-seconds, not a UNIX timestamp.",
    "Support at least two per-key dimensions — per-API-key and per-IP (or per-endpoint) — and document the fairness tradeoff of each (per-IP punishes everyone behind a corporate NAT; per-key is fair to paying customers).",
    "Add jitter to Retry-After so throttled clients return staggered, and explain how this and the continuously-refilling bucket avoid the reset-edge thundering herd.",
    "Write a load test that (a) sends a steady stream across all nodes and confirms the global accepted count tracks the configured limit, and (b) fires a boundary-crossing burst and shows the token bucket does not leak a 2x burst the way a fixed window would.",
  ]}
  acceptance={[
    "A before/after result: the naive per-node limiter admits ~N times the configured limit under the cross-node load test; the Redis token bucket admits within a small tolerance of the configured limit under the identical test.",
    "A captured 429 response showing status, Retry-After, and the three RateLimit-* headers with RateLimit-Reset expressed in delta-seconds.",
    "Evidence the check is atomic: a concurrency test (many parallel requests for one key) shows no over-admission beyond capacity, and the Lua script is shown handling the whole read-decide-update.",
    "A short write-up: which algorithm and per-key dimension you chose and why, the measured global limit across nodes, and how jitter plus continuous refill defend against the thundering herd.",
  ]}
  stretch={[
    "Add a sliding-window-log variant for one sensitive endpoint (e.g. login) and compare its memory footprint per key against the token bucket under the same load.",
    "Make the limiter fail open vs fail closed when Redis is unreachable, measure the latency cost of the Redis round trip on the request path, and add a local in-process fallback with a documented tradeoff.",
    "Implement tiered per-key quotas (free vs paid) read from a config or a header, and show a paid key gets a larger burst and rate under the same load test.",
    "Add an allocation-style abuse guard: a per-key daily quota on top of the per-minute rate, and show a client that exhausts the daily quota is held even while under the per-minute rate.",
  ]}
/>

<Recap lang="en">
This is the limiter you will actually ship: the counter lives in Redis so all nodes share it, the check is one atomic Lua script so concurrent requests cannot double-spend, the algorithm (token bucket) matches bursty real traffic, and the rejection is a contract — 429 with Retry-After in delta-seconds, the RateLimit-* headers, and jitter so the reset edge does not stampede. Building it once, and watching the naive version fail the cross-node load test, turns the per-node-counter lie into something you will never ship by accident.
</Recap>
