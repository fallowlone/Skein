---
concepts:
- field-number-contract
- schema-evolution
- http2-multiplexing
- rpc-streaming
- deadlines
- grpc-vs-rest
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://protobuf.dev/programming-guides/proto3/
- https://grpc.io/docs/what-is-grpc/core-concepts/
status: ready
summary: "Multiple-choice synthesis across the gRPC/Protobuf unit — field-number compatibility, schema evolution, streaming shapes, deadlines, and the gRPC-vs-REST edge decision."
title: 'gRPC and Protobuf: multiple-choice review'
track: apis
unit: 05-grpc-protobuf
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each mirrors a real decision — a schema change you have to sign off, a streaming shape to pick, a deadline to set — not a definition to recite. The wire carries numbers, not names, and most of these traps spring from forgetting that.
</Hook>

<Goal lang="en">
Confirm you can connect the protobuf wire format, field-number discipline, the four call shapes, deadlines, and the gRPC-vs-REST boundary — the synthesis the lesson built toward.
</Goal>

<Quiz
  id="05-grpc-protobuf-choice-1"
  lessonSlug="05-grpc-protobuf"
  lang="en"
  question="A teammate renames a protobuf field from discount_pct to discountPercent and, in the same commit, renumbers it from 3 to 4 to keep numbers contiguous. Which change is safe and which corrupts data?"
  choices={[
    { label: "The rename is free (names never travel on the wire); the renumber is catastrophic — peers still on the old schema decode the value into whatever they call field 4, silently corrupting state.", correct: true },
    { label: "Both are safe — protobuf tracks fields by name, so renaming and renumbering are equivalent edits.", misconception: "Protobuf serializes only field number, wire type, and value — names live in the .proto at compile time and never travel. The number is the identity, so renumbering reassigns meaning on the wire." },
    { label: "Both are dangerous — renaming breaks old clients because they look up fields by string key.", misconception: "There is no string key on the wire. Renaming changes only a local source-code label; old and new binaries interoperate perfectly across a rename." },
    { label: "The renumber is fine as long as you recompile every service; the rename is the risky one.", misconception: "Recompiling does not help: each side compiles a self-consistent schema, so the compiler never sees the conflict. Stragglers on the old number are the ones that corrupt." },
  ]}
/>

<Quiz
  id="05-grpc-protobuf-choice-2"
  lessonSlug="05-grpc-protobuf"
  lang="en"
  question="You need to remove an obsolete field number 3 from a message that is live in production. What is the senior move?"
  choices={[
    { label: "Delete the field and add reserved 3; plus reserved for its name, so neither the number nor the name can ever be reclaimed by a future edit.", correct: true },
    { label: "Just delete the line — the compiler will remember 3 is gone and stop anyone reusing it.", misconception: "The compiler only checks the current file's self-consistency. A later innocent edit can reassign 3 to a new field, and old clients still sending the original field 3 will poison it. reserved is what blocks that." },
    { label: "Renumber every field after 3 down by one so the numbering stays gap-free.", misconception: "Renumbering is exactly the catastrophe to avoid — peers on the old schema decode values into the wrong fields. Gaps in numbering are completely fine and cost nothing on the wire." },
    { label: "Change the field's type to bytes and keep the number, so old payloads still parse.", misconception: "Type changes do not make removal safe and can themselves break wire compatibility. The issue is the number's identity, which only reserved protects." },
  ]}
/>

<Quiz
  id="05-grpc-protobuf-choice-3"
  lessonSlug="05-grpc-protobuf"
  lang="en"
  question="A new producer build adds field 7 and sends it to consumers still running the old schema; meanwhile an old producer omits field 7 entirely when talking to new consumers. What happens in each direction?"
  choices={[
    { label: "Forward compat: old consumers skip the unknown number 7 (and preserve it on pass-through). Backward compat: new consumers see field 7 missing and apply the proto3 default (0 / empty). Both directions work — that is why producers and consumers deploy independently.", correct: true },
    { label: "Both directions error out — protobuf rejects any message whose fields do not exactly match the local schema.", misconception: "Protobuf is built for partial matches: unknown fields are skipped, missing fields default. Strict exact-match rejection would make independent deployment impossible." },
    { label: "Old consumers crash on the unknown field 7 because there is no handler for it.", misconception: "Unknown field numbers are skipped using their wire type to know their length — and preserved for re-serialization. No crash; this is forward compatibility." },
    { label: "New consumers read field 7 as null and must null-check it; protobuf has no defaults.", misconception: "proto3 has well-defined zero defaults (0, empty string, false, empty) for missing scalar fields — there is no null for them. Message fields are the exception, exposed via presence." },
  ]}
/>

<Quiz
  id="05-grpc-protobuf-choice-4"
  lessonSlug="05-grpc-protobuf"
  lang="en"
  question="A dashboard needs the server to push live price updates to each client for as long as the client stays subscribed, with no per-update request from the client. Which RPC shape fits, and what is the common mistake?"
  choices={[
    { label: "Server streaming — one request opens a stream of responses. The common mistake is reaching for bidirectional streaming when the client never sends after the initial subscribe, paying for a more complex shape you do not use.", correct: true },
    { label: "Unary, polled in a loop — call GetQuote repeatedly on a timer.", misconception: "Polling reintroduces the request-per-update overhead that server streaming exists to eliminate, and adds latency equal to the poll interval. Server streaming pushes as data is ready over one open stream." },
    { label: "Client streaming — the client sends many messages, the server replies once.", misconception: "That is the inverse of what is needed. Client streaming suits uploads/telemetry batching; here the server is the one producing the stream." },
    { label: "Bidirectional streaming is the only shape that can push from the server.", misconception: "Server streaming already pushes a response stream from one request. Bidi is for when both sides stream independently (chat, control channels); using it here is over-engineering." },
  ]}
/>

<Quiz
  id="05-grpc-protobuf-choice-5"
  lessonSlug="05-grpc-protobuf"
  lang="en"
  question="A client call returns DEADLINE_EXCEEDED, yet the server logs show the handler completed and committed a write. How do you read this, and what is the durable fix?"
  choices={[
    { label: "The work happened but the deadline expired before the response arrived, so the caller saw an error for a side effect that did land. Fix: make the operation idempotent (idempotency key / safe retries) and set deadlines that account for real downstream latency — do not assume failure means nothing happened.", correct: true },
    { label: "Impossible — DEADLINE_EXCEEDED guarantees the server never ran the handler.", misconception: "A deadline cancels the RPC on both ends, but it cannot un-run work already in flight on the server. A succeeded-server / failed-client outcome is the classic deadline race." },
    { label: "The server clock is wrong; sync NTP and the error goes away.", misconception: "Deadlines propagate as a relative duration in metadata, not an absolute wall-clock time, so clock skew is not the cause. The cause is the response losing the race to the deadline." },
    { label: "Remove the deadline so the call can never time out.", misconception: "Dropping deadlines is how a mesh cascades into resource exhaustion — slow downstreams pin callers forever. The fix is idempotency plus realistic deadlines, not removing them." },
  ]}
/>

<Quiz
  id="05-grpc-protobuf-choice-6"
  lessonSlug="05-grpc-protobuf"
  lang="en"
  question="A public REST endpoint hit directly from a React SPA is slow under load. A teammate proposes converting it to native gRPC so the browser calls it directly. What is the senior call?"
  choices={[
    { label: "Keep REST/JSON (or Connect-RPC) at the browser edge and reserve gRPC for internal service-to-service hops. Browsers cannot control HTTP/2 framing/trailers, so native gRPC at the edge forces grpc-web plus a translating proxy and still bars client/bidi streaming.", correct: true },
    { label: "Convert it to native gRPC — the browser will call it directly and get the 4-10x win.", misconception: "Browsers cannot make native gRPC calls at all. You would be forced into grpc-web and a proxy hop, lose readable payloads in DevTools, and gain little if the payload is string-heavy." },
    { label: "Convert every tier — edge and internal — to gRPC for one consistent protocol.", misconception: "Edge and internal tiers have opposite constraints. Forcing one protocol drags the proxy requirement and binary opacity to the browser, where they cost most, for uniformity you do not need." },
    { label: "Stream raw JSON over a WebSocket and skip the schema.", misconception: "That throws away the typed contract and schema-evolution guarantees that are gRPC's whole point, and reinvents framing, deadlines, and backpressure by hand — usually worse than either option." },
  ]}
/>

<Recap lang="en">
The unit's through-line is one fact: the wire carries field numbers and values, never names. That makes renaming free and renumbering or reusing a number a silent corruption; add-only evolution with reserved is the only safe path, and it is exactly what gives protobuf two-way backward/forward compatibility. On top of HTTP/2 multiplexing, the four call shapes map to who streams (unary, server, client, bidi), deadlines propagate and can fail a caller whose work actually succeeded, and the edge decision stays the same: gRPC between your services, REST/JSON or Connect-RPC at the browser. Now when you see a teammate's diff that renumbers a field or proposes native gRPC for a browser endpoint, you have the exact words to explain why it won't work.
</Recap>
