---
concepts:
- schema-evolution
- streaming-shapes
- deadlines
- backward-forward-compat
- grpc-vs-rest
estMin: 240
lang: en
level: senior
order: 10
prereqs: []
slug: project
sources:
- https://protobuf.dev/programming-guides/proto3/
- https://grpc.io/docs/what-is-grpc/core-concepts/
status: ready
summary: "Hands-on project — design a gRPC service, evolve its schema across two incompatible versions without breaking live peers, add a streaming RPC, and prove deadline propagation under failure."
title: 'gRPC and Protobuf: build a contract that survives evolution'
track: apis
unit: 05-grpc-protobuf
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Reading about field-number corruption is not the same as keeping a live contract intact across a deploy. Build a small gRPC service, then evolve its schema the way a real system forces you to — old clients still in flight, new fields landing, a removed field that must never come back — and prove with running peers that nothing decoded into the wrong slot.
</Hook>

<Goal lang="en">
Turn the unit's mental model into a working contract: define a proto, run two service versions side by side, evolve the schema add-only with reserved, add a streaming RPC for the right reason, and demonstrate that deadlines propagate and cancel work across the call graph.
</Goal>

<ProjectBrief
  id="05-grpc-protobuf-project"
  lessonSlug="05-grpc-protobuf"
  lang="en"
  objective="Build a small gRPC service (Go, or any language with a mature gRPC stack), evolve its protobuf schema across two versions that must interoperate in both directions, add one streaming RPC, and prove — with two running peers and captured evidence — that schema evolution stays safe and deadlines propagate."
  requirements={[
    "Define a v1 .proto with at least one message and a unary RPC (e.g. an OrderService with GetOrder/CreateOrder). Generate code and stand up a working client and server that complete a unary call end to end.",
    "Evolve to a v2 .proto add-only: append at least two new fields with fresh numbers, rename one existing field (same number), and remove one field — replacing it with reserved on both its number and its name. Keep a v1 client binary around.",
    "Run a compatibility matrix: v1 client against v2 server and v2 client against v1 server. Capture output showing new fields default for old writers (backward compat) and unknown fields are skipped, not errored (forward compat). No value may decode into the wrong field.",
    "Deliberately break it once: in a throwaway branch, renumber an existing field (or reuse a reserved number) and show the cross-version call now corrupts a value. Capture the wrong-field output, then revert — this is the proof of why the rules exist.",
    "Add one streaming RPC where it genuinely fits (server streaming for a feed/tail, or client streaming for batched upload). Justify in a sentence why that shape, not unary-in-a-loop or bidi.",
    "Set a client deadline on a call into a deliberately slow downstream and show the RPC is cancelled on both ends when it expires; capture a DEADLINE_EXCEEDED at the client and evidence the server stopped (or would stop) the downstream work.",
    "Write a short ADR-style note: which schema changes you classified safe vs unsafe and why, and where you would put this service relative to a browser (gRPC internal vs REST/Connect at the edge).",
  ]}
  acceptance={[
    "A reproducible compatibility matrix (commands + captured output) proving v1-v2 interop in both directions with zero misdecoded fields.",
    "The deliberate-break branch's captured output showing a renumber/reuse corrupting a value, plus the reverted main where reserved blocks the reuse at compile time.",
    "The streaming RPC works against a real client and the one-sentence justification names who streams and why the simpler shape was rejected.",
    "A captured DEADLINE_EXCEEDED at the client for the slow-downstream call, with evidence the deadline propagated rather than each hop timing out independently.",
  ]}
  stretch={[
    "Add a CI gate using buf breaking (or protolock) that diffs the proto against the committed baseline and fails the build on any wire-incompatible change — renumber, type change, or unreserved removal.",
    "Add a grpc-web or Connect path for a browser client and document exactly what the proxy/translation layer adds and which streaming shapes it cannot support.",
    "Add a FieldMask (or optional fields) to support true PATCH semantics, and show the server distinguishing 'cleared to empty' from 'not sent' on a scalar field.",
    "Benchmark the same payload as protobuf vs JSON for a numeric-heavy and a string-heavy message, and report the size/latency delta to confirm where the wire-format win is real.",
  ]}
/>

<Recap lang="en">
This is the loop you run whenever a gRPC contract has to change under live traffic: evolve add-only, reserve every removal, and verify with two running peers that both directions still decode correctly — then prove to yourself, once, that a renumber really does corrupt so the discipline is muscle memory. Pick streaming shapes by who actually streams, set deadlines that propagate and cancel downstream work, and keep gRPC where it belongs — between your services, with REST/Connect at the browser edge.
</Recap>
