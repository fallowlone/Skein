infographic chart-pie-donut-plain-text
data
  title One 100 ms CFS period — GOMAXPROCS=64 on limits.cpu 2
  desc 64 threads burn the 200 ms quota in ~3 ms, then the cgroup is frozen
  values
    - label Parked (CFS throttled)
      value 97
    - label Running (quota granted)
      value 3
theme
  palette #f43f5e #14b8a6
