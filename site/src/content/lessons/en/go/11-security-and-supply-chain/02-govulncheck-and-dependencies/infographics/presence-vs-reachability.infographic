infographic compare-binary-horizontal-underline-text-vs
data
  title Presence vs reachability: the 10:1 cut
  desc Same Go service, same vuln DB — two different questions, two different worklists
  compares
    - label Image scanner — presence
      children
        - label 23 CVEs flagged
          desc Module versions intersect a CVE range
        - label Direct and transitive in scope
          desc Anything in the artifact gets matched
        - label No proof of use
          desc Can't say if the function is ever called
        - label Friday fire drill
          desc Argue a CVSS score, cancel the weekend
    - label govulncheck — reachability
      children
        - label 2 actionable findings
          desc A call path reaches the vulnerable symbol
        - label 21 demoted to informational
          desc In the build, but never on a call path
        - label Trace per finding
          desc Stack from your code to the vuln symbol
        - label Monday chore
          desc Bump on the next release train
theme
  palette #f43f5e #14b8a6
