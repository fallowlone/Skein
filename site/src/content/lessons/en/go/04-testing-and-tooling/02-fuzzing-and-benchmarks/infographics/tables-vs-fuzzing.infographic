infographic compare-binary-horizontal-underline-text-vs
data
  title Why fuzzing finds what tables cannot
  desc Same duration parser: tables ran two years; the fuzzer found the crash in eleven seconds
  compares
    - label Table-driven tests
      children
        - label Table-driven tests
          desc only inputs an author imagined
        - label 31 hand-written rows
          desc two years in production
        - label Missed the +. crash
          desc a sign with no digits
        - label Static set of cases
          desc no new inputs without new code
    - label Coverage-guided fuzzing
      children
        - label Coverage-guided fuzzing
          desc explores inputs you never wrote
        - label ~94,000 execs/sec
          desc mutation steered by coverage
        - label Crash found in 11s
          desc the +. panic, over lunch
        - label Minimized to a regression
          desc committed in testdata/fuzz
theme
  palette #f43f5e #14b8a6
