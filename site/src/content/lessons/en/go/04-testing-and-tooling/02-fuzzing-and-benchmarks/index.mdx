---
concepts:
- go-fuzz-bench
deepensInto: []
estMin: 19
lang: en
lessonType: topic
level: senior
order: 2
prereqs: []
slug: "02-fuzzing-and-benchmarks"
sources:
- https://go.dev/doc/tutorial/fuzz
- https://pkg.go.dev/testing
- https://pkg.go.dev/golang.org/x/perf/cmd/benchstat
spiral: []
status: ready
summary: 'Native fuzzing mutates inputs coverage-guided from seeds and minimizes crashers into testdata/fuzz as permanent regressions — finding what tables cannot. Benchmarks: b.Loop (1.24) over b.N, ns/op and allocs/op via b.ReportAllocs, benchstat over -count=10 runs for honesty.'
title: 'Fuzzing and benchmarks: coverage-guided inputs, b.Loop, and benchstat honesty'
track: "go"
unit: "04-testing-and-tooling"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
The homegrown duration parser had 31 table rows and two years of production behind it — every format anyone had ever thought of: 90s, 1.5h, 2h45m. After a public endpoint started accepting user-supplied retry intervals, someone spent ten minutes wiring a FuzzParseInterval with three seed strings and left it running over lunch. Eleven seconds in, at around 94,000 execs per second, the fuzzer printed a panic: slice bounds out of range, input "+." — a leading sign with no digits sent the index past the end during suffix scanning. No table row had imagined it, because every table row was written by someone who knew what a duration looks like. The minimized crasher landed in testdata/fuzz as a file, instantly a regression test that plain go test runs forever after. One crash from a public parser is a 500; the same crash from a goroutine with no recover is a process restart. The fuzzer found in seconds what two years of examples never would.
</Hook>

<Crux>Fuzzing mutates inputs you never imagined and keeps crashers as regressions; benchmarks lie without b.Loop discipline and benchstat.</Crux>

<Explanation>

## Native fuzzing: seeds, mutation, and the corpus

Since Go 1.18 fuzzing is built into `go test`. A fuzz target is a function `FuzzXxx(f *testing.F)`: you register seed inputs with `f.Add`, then hand `f.Fuzz` a property that must hold for *any* input:

```go
func FuzzParseInterval(f *testing.F) {
	f.Add("90s")
	f.Add("2h45m")
	f.Add("")
	f.Fuzz(func(t *testing.T, s string) {
		d, err := ParseInterval(s)
		if err != nil {
			return // invalid input may be rejected — it must not panic
		}
		// round-trip property: format what we parsed, parse it again
		s2 := FormatInterval(d)
		d2, err := ParseInterval(s2)
		if err != nil || d2 != d {
			t.Fatalf("round-trip broke: %q -> %v -> %q -> %v (%v)", s, d, s2, d2, err)
		}
	})
}
```

Without `-fuzz`, `go test` just runs the seeds plus the committed corpus — cheap, deterministic, CI-friendly. With `go test -fuzz=FuzzParseInterval`, the engine mutates inputs **coverage-guided**: an input that reaches a new branch is kept and mutated further, so the search concentrates on code it has not seen, typically tens of thousands of execs per second per worker. The mechanism is why fuzzing finds what tables cannot: a table encodes examples its author *imagined*; the fuzzer explores the input space your code actually branches on — sign-without-digits, lone UTF-8 continuation bytes, 1e309. When an input fails, the engine **minimizes** it (shrinking while the failure reproduces) and writes it to `testdata/fuzz/FuzzParseInterval/` — commit that file and the crash is a permanent regression test, run by ordinary `go test` with no flags. The craft is in the property: panics come free, but "no panic" is weak; round-trips (`parse∘format = id`), comparisons against a reference implementation, and invariants (output length, sortedness) are where fuzzing earns senior-grade value.

<Quiz
  id="02-fuzzing-and-benchmarks-quiz-1"
  lessonSlug="02-fuzzing-and-benchmarks"
  lang="en"
  question="go test -fuzz found a crashing input for your parser and wrote a file under testdata/fuzz/FuzzParse/. What happens on the next plain go test run in CI, with no -fuzz flag?"
  choices={[
    { label: "The committed crasher is executed as a regular test case along with the f.Add seeds — the crash is now a permanent regression test", correct: true },
    { label: "Nothing — corpus files only matter when -fuzz is active", misconception: "Seeds and the committed corpus run on every plain go test precisely so that found crashers keep guarding the code. Only the mutation engine needs -fuzz; replaying known inputs is cheap and deterministic." },
    { label: "CI starts fuzzing from that corpus until it times out", misconception: "Plain go test never mutates — open-ended fuzzing needs an explicit -fuzz=Pattern and runs indefinitely until stopped or a -fuzztime budget expires, which is why it lives in nightly jobs, not the PR gate." },
    { label: "The file is ignored until a developer converts it into a table row by hand", misconception: "No conversion step exists or is needed; the corpus file is the test case. f.Fuzz replays it through the same property function. Deleting the file is what would silently drop the regression." },
  ]}
/>

## Benchmarks: b.Loop, ns/op, allocs/op

A benchmark is `BenchmarkXxx(b *testing.B)`, run via `go test -bench=.`. Since **Go 1.24** the loop is `b.Loop()`:

```go
func BenchmarkParseInterval(b *testing.B) {
	b.ReportAllocs()
	input := strings.Repeat("1h30m", 1) // setup, outside the loop
	for b.Loop() {
		d, err := ParseInterval(input)
		if err != nil {
			b.Fatal(err)
		}
		_ = d
	}
}
```

`b.Loop` fixes two chronic lies of the older `for i := 0; i < b.N; i++` style. First, **dead-code elimination**: the compiler may notice a result is never used and delete the call — old benchmarks needed package-level sink variables to defeat it; `b.Loop` keeps the loop body's arguments and results alive automatically. A benchmark reporting 0.25 ns/op is reporting the cost of an empty loop, not your function. Second, **setup amortization**: the b.N style re-ran the whole function with growing N (1, 100, 10000…) until the run exceeded `-benchtime` (default 1 s), re-executing setup each round unless you remembered `b.ResetTimer`; `b.Loop` runs setup exactly once and times only the loop. The numbers: `ns/op` is wall time per iteration; `-benchmem` or `b.ReportAllocs()` adds `B/op` and `allocs/op` — allocation counts are *exact* (from the runtime's allocator counters) and usually the most stable, most actionable number a benchmark produces: cutting allocs/op from 7 to 1 is a real win regardless of timer noise.

## benchstat: statistical honesty

A single before/after run proving your optimization made things "4% faster" proves nothing. Wall-clock benchmarks on real machines wobble — thermal throttling, core migration, cache and ASLR layout, a browser tab — easily ±2–5% on a laptop, worse on shared CI runners. The honest protocol: run each side ~10 times and let `benchstat` do the statistics:

```go
// go test -bench=ParseInterval -count=10 > old.txt
// ... apply the change ...
// go test -bench=ParseInterval -count=10 > new.txt
// benchstat old.txt new.txt
//
//             │   old.txt   │             new.txt              │
// ParseInterval  412.5n ± 2%   369.0n ± 1%   -10.55% (p=0.000 n=10)
// (an insignificant change prints: ~ , p=0.483)
```

`benchstat` reports the median, the spread, and a p-value from a Mann-Whitney U test; if the distributions are statistically indistinguishable it prints `~` instead of a delta. That tilde is the feature: it stops you from shipping noise as a win. Discipline that keeps the numbers real: quiet machine or dedicated runner, `-count=10`, compare allocs/op first (exact), and benchmark the level users feel — a 30% win in a function that is 0.1% of the profile is a rounding error, which is why benchmarks pair with pprof, not replace it.

<FlowDiagram
  label="From a single benchmark to an honest claim: b.Loop measures, ten runs per side feed benchstat, and the p-value decides between a real delta and a tilde"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "loop", label: "b.Loop()", sub: "setup once, results alive" },
    { id: "nums", label: "ns/op + allocs/op", sub: "allocs are exact" },
    { id: "runs", label: "-count=10", sub: "old.txt vs new.txt" },
    { id: "stat", label: "benchstat", sub: "Mann-Whitney U" },
    { id: "win", label: "Significant delta", sub: "-10.55% (p=0.000)" },
    { id: "noise", label: "Tilde: noise", sub: "do not ship the claim" },
  ]}
  edges={[
    { from: "loop", to: "nums", label: "measures" },
    { from: "nums", to: "runs", label: "repeat ×10" },
    { from: "runs", to: "stat", label: "compare" },
    { from: "stat", to: "win", label: "p small" },
    { from: "stat", to: "noise", label: "indistinct" },
  ]}
  caption="A lone before/after run wobbles ±2–5% on a laptop — thermal throttling, core migration, cache layout. benchstat prints a delta only when the distributions genuinely differ; the tilde is the feature that stops noise shipping as a win."
/>

<Quiz
  id="02-fuzzing-and-benchmarks-quiz-2"
  lessonSlug="02-fuzzing-and-benchmarks"
  lang="en"
  question="An old-style benchmark loop runs sum := compute(x) but never uses sum, and reports 0.3 ns/op. What is actually being measured?"
  choices={[
    { label: "An empty loop — the compiler proved the result unused and eliminated the call; the fix is b.Loop (Go 1.24), which keeps body results alive, or a sink variable", correct: true },
    { label: "compute is genuinely that fast because it was inlined", misconception: "Inlining removes call overhead, not the work itself. 0.3 ns is about one CPU cycle — no real computation fits there. A too-good-to-be-true ns/op is the classic signature of dead-code elimination." },
    { label: "The timer missed the call because b.ResetTimer was not invoked", misconception: "ResetTimer excludes setup before the loop; it cannot remove work inside the loop from measurement. The loop body itself was deleted by the optimizer, which is a compile-time event, not a timing one." },
    { label: "Benchmarks measure per-iteration overhead only; the function cost is reported separately", misconception: "ns/op is total loop time divided by iterations — function cost included. There is no separate report; if the body is optimized away, ns/op honestly measures the nothing that remains." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="The fuzz engine mutates coverage-guided from seeds, minimizes any crasher into testdata, and the committed corpus runs as regressions under plain go test"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "seeds", label: "f.Add seeds", sub: "known-shape inputs" },
    { id: "mutate", label: "Mutation engine", sub: "coverage-guided" },
    { id: "prop", label: "Property fn", sub: "no panic, round-trip" },
    { id: "crash", label: "Failing input", sub: "minimized" },
    { id: "corpus", label: "testdata/fuzz/", sub: "committed corpus" },
    { id: "ci", label: "plain go test", sub: "replays corpus" },
  ]}
  edges={[
    { from: "seeds", to: "mutate", label: "-fuzz" },
    { from: "mutate", to: "prop", label: "new coverage" },
    { from: "prop", to: "crash", label: "fails" },
    { from: "crash", to: "corpus", label: "minimize + write" },
    { from: "corpus", to: "ci", label: "regression" },
  ]}
  caption="Coverage feedback steers mutation toward unexplored branches — that is why fuzzing finds inputs no table author imagined. The committed crasher closes the loop: every future go test replays it with zero flags."
/>

<KeyTakeaway>Fuzz targets assert properties — no panic, round-trip, reference parity — and the coverage-guided engine explores inputs tables never encode; minimized crashers in testdata/fuzz become permanent regressions under plain go test. Benchmarks: use b.Loop (Go 1.24) to defeat dead-code elimination and setup re-runs, read allocs/op as the exact number, and never claim a win without -count=10 plus benchstat — its p-value and tilde exist to stop you shipping noise.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-fuzzing-and-benchmarks-retrieval"
  lang="en"
  questions={[
    { q: "Walk through what happens from go test -fuzz=FuzzParse to a committed regression test, and explain why fuzzing finds bugs that a 31-row table missed.", a: "f.Add registers seed inputs; with -fuzz the engine mutates them coverage-guided — any input reaching a new branch is kept and mutated further, at tens of thousands of execs/sec. The property function must hold for arbitrary input: return early on expected errors, but panics or violated invariants (round-trip parse-format-parse, reference-implementation parity) fail the run. A failing input is automatically minimized while still reproducing, then written to testdata/fuzz/FuzzParse/; committed, it replays on every plain go test with no flags — a permanent regression. Tables miss these bugs structurally: every row is an input its author imagined, while the fuzzer searches the input space the code actually branches on — sign-without-digits, truncated UTF-8, overflow boundaries." },
    { q: "Your teammate posts a single before/after benchmark run showing -4% ns/op. What is wrong with the claim and what is the honest protocol?", a: "Single runs wobble ±2–5% on laptops (thermal throttling, core migration, cache/ASLR layout) — a lone 4% delta is indistinguishable from noise. Protocol: go test -bench -count=10 on each side, then benchstat old.txt new.txt; it reports medians with spread and a Mann-Whitney p-value, printing ~ when the difference is insignificant. Also check the loop itself: pre-1.24 b.N loops with unused results get dead-code-eliminated (impossibly small ns/op); b.Loop keeps results alive and runs setup once. Prefer allocs/op for the first read — it comes from exact allocator counters, not the timer — and verify the function matters in the pprof profile before celebrating: 30% off 0.1% of CPU is nothing." },
  ]}
/>

<Recap lang="en">
Go's native fuzzing turns a property function into a search: f.Add seeds the corpus, the engine mutates inputs with coverage feedback so effort concentrates on unexplored branches, and any failing input is minimized and written into testdata/fuzz where, once committed, plain go test replays it forever — the crasher becomes the regression test. The value over tables is structural, not incremental: tables hold inputs an author imagined; the fuzzer finds the "+." that panicked a two-year-old parser in eleven seconds because no human writes that row. Strong targets assert more than survival — round-trips, reference parity, invariants. Benchmarks earn the same skepticism. b.Loop in Go 1.24 closes the two classic holes of the b.N style: dead-code elimination that turned unused results into 0.3 ns/op fantasies, and setup re-executed across the auto-scaling rounds unless b.ResetTimer was remembered. Read allocs/op (exact, from allocator counters) before ns/op (noisy, from the wall clock), and never compare single runs: -count=10 per side and benchstat, whose Mann-Whitney p-value prints a tilde for statistically indistinguishable results — the tilde that keeps a 4%-faster story out of the changelog when it is actually thermal noise. Fuzzing guards correctness on inputs you never wrote; benchstat guards honesty on numbers you want to believe.
</Recap>
