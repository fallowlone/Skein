---
concepts:
- go-modules-tooling
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: middle
order: 4
prereqs: []
slug: "04-modules-and-tooling"
sources:
- https://go.dev/ref/mod
- https://go.dev/blog/using-go-modules
- https://pkg.go.dev/cmd/vet
spiral: []
status: ready
summary: 'go.mod requires are minimums and MVS picks the maximum of them — deterministic, no solver, no surprise upgrades; go.sum is a checksum ledger, not a lockfile. replace works only in the main module; go.work wires multi-module dev. CI: vet, staticcheck, -race, drift checks.'
title: 'Modules and tooling: MVS, go.sum, workspaces, and the CI toolchain'
track: "go"
unit: "04-testing-and-tooling"
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import mvsVsSemver from "./infographics/mvs-vs-semver.svg?raw";

<Hook>
Friday afternoon, every pipeline in the org went red at once: verifying github.com/acme/sigutil@v1.4.2: checksum mismatch. Security incident? Almost the opposite. The library's author had found a bug in the tag, deleted v1.4.2, and re-tagged the fixed commit under the same version — a quiet little rewrite of history. Go refused to play along: go.sum in every consuming repo held the hash of the *original* v1.4.2, the public checksum database agreed, and the new bytes under an old name failed verification everywhere. The npm-trained half of the team read the error as an outage; the other half read it as the system working — somebody had changed published code without changing the version, and the build said no. The author cut v1.4.3, one line of go get later everything was green, and the postmortem's only action item was a link to the MVS docs: in Go, nothing about your dependency graph moves unless you move it.
</Hook>

<Crux>MVS picks the minimum version satisfying everyone — builds reproducible by design. go.sum verifies content; vet and -race guard CI.</Crux>

<Explanation>

## MVS: minimal version selection, not a solver

Why does your Go build reproduce identically on a colleague's machine six months later, while the same npm project needs a lockfile and still sometimes differs? The answer is a single algorithm that fits on a napkin.

A `require` line in go.mod is a *minimum*, not a range. To build the final version list, Go walks the module graph, collects every module's required minimum for each dependency, and picks **the maximum of the minimums** — the smallest version that satisfies everyone. That is the whole algorithm:

```go
// your module requires:  A v1.2.0,  B v1.1.0
// A requires:            C v1.3.0
// B requires:            C v1.5.0
//
// MVS selects C v1.5.0 — the max of the required minimums.
// NOT C's latest release v1.9.2: MVS never jumps ahead of
// what something in the graph explicitly asked for.
//
// Upgrades are always explicit:
//   go get C@v1.6.0    // raise your own minimum
//   go get -u ./...    // raise everything (deliberately)
//   go mod tidy        // sync go.mod with actual imports
```

Contrast with npm's model: semver ranges (`^1.3.0`) resolved at install time mean the same package.json yields different trees on different days, so a lockfile must be bolted on to restore determinism. MVS needs no lockfile for *version selection* — the build list is a pure function of the go.mod files in the graph, computable by hand, identical on every machine, forever. The tradeoff is honest: you do not get automatic bugfix pickup; a security patch in C v1.5.1 reaches you only when you or a dependency asks for it — hence `govulncheck` and scheduled `go get -u` PRs as the counterweight. What go.sum adds is not version pinning but **content verification**: a ledger of cryptographic hashes for every module version ever used, cross-checked against the sum.golang.org transparency log on first download. Versions come from go.mod; go.sum guarantees the named version is byte-identical to what the rest of the world got — that is what fired in the Hook.

<Infographic
  svg={mvsVsSemver}
  label="Side-by-side comparison: Go MVS treats each require as a minimum and picks the maximum of the minimums, so the build list is a pure function of go.mod with no lockfile and no automatic upgrades; npm semver ranges like ^1.3.0 resolve to the latest compatible release at install time, yielding different trees on different days and needing a lockfile bolted on to recover determinism."
  caption="The whole determinism gap in one frame: Go selects versions from go.mod by construction, while npm resolves ranges at install time and pins after the fact with a lockfile."
  hue="var(--d-infra)"
/>

<Quiz
  id="04-modules-and-tooling-quiz-1"
  lessonSlug="04-modules-and-tooling"
  lang="en"
  question="Your app requires A and B. A requires zap v1.21.0, B requires zap v1.24.0, and zap's latest release is v1.27.0. Which version builds, and when does it change?"
  choices={[
    { label: "v1.24.0 — the maximum of the required minimums; it changes only when you run go get or a dependency raises its requirement in a version you adopt", correct: true },
    { label: "v1.27.0 — Go resolves to the latest compatible release like npm's caret ranges", misconception: "MVS never selects beyond what the graph explicitly requires. Latest-compatible is npm's model and is exactly what makes installs time-dependent; Go trades automatic freshness for a build list that is a pure function of go.mod files." },
    { label: "v1.21.0 — minimal version selection means the smallest version in the graph wins", misconception: "Minimal means the smallest version satisfying every requirement, and v1.21.0 fails B's stated minimum of v1.24.0. MVS takes the maximum of the minimums — minimal relative to ranges-and-latest solvers, not absolutely." },
    { label: "Whatever go.sum pins — go.sum is Go's lockfile", misconception: "go.sum holds content hashes for verification, not version choices; deleting it changes no versions (Go re-fetches and re-verifies hashes). Version selection lives entirely in go.mod via MVS." },
  ]}
/>

## replace and go.work: local wiring that must not leak

Two tools redirect where module source comes from, with very different blast radii. A `replace` directive in go.mod (`replace github.com/acme/lib => ../lib`) applies **only when that module is the main module** — replaces in your dependencies' go.mod files are ignored entirely. That makes replace safe for forks and emergency pins, but also a classic landmine: a committed `=> ../lib` builds on your laptop and dies in Docker and in every consumer, because the relative path exists only on your disk. Since Go 1.18 the better tool for multi-module development is the **workspace**: a go.work file listing `use ./app` and `use ./lib` makes the toolchain resolve lib from local source across all listed modules — no go.mod edits, and go.work is conventionally untracked (developer-local wiring, like an IDE config), so nothing leaks into the published module. CI builds without a workspace see exactly what consumers will see; `GOFLAGS=-mod=readonly` (the default behavior since 1.16) plus `go.work` absence in CI is the honest configuration.

## The CI toolchain: vet, staticcheck, -race, generate

The compiler accepts plenty of wrong programs; the toolchain narrows the gap in layers. `go vet` runs analyzers for bugs the type system misses — printf argument mismatches, copied locks (`copylocks`), unreachable code; a vet subset already runs automatically during `go test`, but CI should run the full `go vet ./...`. **staticcheck** goes an order of magnitude further with its SA-series checks (nil-map writes, time.Format misuse, deferred Close on a nil response body) and is the de-facto second linter of serious Go shops. The **race detector** is dynamic instrumentation: `go test -race` catches only races your tests actually execute, at a real cost — roughly 5–10× CPU and 5–10× memory — which is why it belongs on the CI test run (always) and possibly a canary replica, but not on every production pod. `go generate` is a build-time contract with humans, not the build: it never runs automatically, so generated code (mocks, protobufs, stringer) is committed, and CI guards drift by regenerating and failing on a non-empty `git diff` — same trick as `go mod tidy -diff`. For reproducible release binaries: a pinned `toolchain go1.24.x` directive in go.mod, `-trimpath` so file paths do not embed the builder's home directory, and the module proxy plus go.sum doing the rest — same inputs, same bytes.

<Quiz
  id="04-modules-and-tooling-quiz-2"
  lessonSlug="04-modules-and-tooling"
  lang="en"
  question="A teammate deletes go.sum to fix a checksum mismatch error and the build goes green. What did the project actually lose?"
  choices={[
    { label: "Content verification — versions stay identical (go.mod decides those), but the freshly written go.sum now trusts whatever bytes the proxy served today, accepting exactly the rewrite the error was reporting", correct: true },
    { label: "Version pinning — without go.sum, MVS floats to the latest compatible releases", misconception: "go.sum plays no role in version selection; MVS computes the same build list from go.mod with or without it. Treating go.sum as the lockfile is the npm habit this design specifically departs from." },
    { label: "Nothing — go.sum is a derived cache that Go regenerates with equivalent guarantees", misconception: "Regeneration records the hashes of what is fetched now, not what was audited before. The mismatch was the guarantee in action: published bytes changed under an existing version. Deleting the ledger converts an alarm into silent acceptance." },
    { label: "Build speed — go.sum lets Go skip downloading modules that have not changed", misconception: "Caching is the module cache's job (GOMODCACHE); go.sum is consulted to verify downloads, not avoid them. Its loss costs no speed — it costs the proof that the dependency tree is byte-identical to what everyone else verified." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="go.mod requirements flow through MVS into a deterministic build list, verified by go.sum and the checksum database, then gated by vet, staticcheck, and race-enabled tests in CI"
  hue="var(--d-backend)"
  perRow={3}
  nodes={[
    { id: "gomod", label: "go.mod graph", sub: "requires = minimums" },
    { id: "mvs", label: "MVS", sub: "max of minimums" },
    { id: "sum", label: "go.sum + sumdb", sub: "content hashes" },
    { id: "vet", label: "vet + staticcheck", sub: "static analysis" },
    { id: "race", label: "go test -race", sub: "5-10x cost, CI only" },
    { id: "bin", label: "Release binary", sub: "-trimpath, pinned toolchain" },
  ]}
  edges={[
    { from: "gomod", to: "mvs", label: "compute" },
    { from: "mvs", to: "sum", label: "verify bytes" },
    { from: "sum", to: "vet", label: "build list ready" },
    { from: "vet", to: "race", label: "then test" },
    { from: "race", to: "bin", label: "ship" },
  ]}
  caption="Version selection is a pure function of go.mod files — no solver, no lockfile, no time dependence. go.sum verifies content, never picks versions. The dynamic race detector runs where tests run; reproducibility comes from pinned toolchain plus trimpath."
/>

<KeyTakeaway>MVS makes the build list a pure function of go.mod files: requires are minimums, the maximum of minimums wins, and nothing upgrades until you ask — go.sum then verifies bytes against the transparency log, which is a ledger, not a lockfile. Keep replace out of commits (main-module-only, breaks consumers); use untracked go.work for multi-module dev. CI runs go vet, staticcheck, tests with -race (5–10× cost, catches only executed races), and drift checks for tidy and generate.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-modules-and-tooling-retrieval"
  lang="en"
  questions={[
    { q: "Explain how MVS picks versions, why Go needs no lockfile for that, and the precise division of labor between go.mod and go.sum.", a: "Every require is a minimum. Go walks the module graph, gathers each dependency's required minimums, and selects the maximum of them — the smallest version satisfying everyone, never anything newer than something explicitly asked for. The build list is therefore a pure function of the go.mod files: same graph, same result, any machine, any day — which is what npm's install-time range resolution lacks and why it needs a lockfile bolted on. Upgrades happen only via go get (targeted or -u). go.mod owns version selection; go.sum owns content verification — a hash ledger for every module version used, cross-checked against the sum.golang.org transparency log, so a re-tagged v1.4.2 with different bytes fails everywhere. Deleting go.sum changes no versions; it deletes the proof." },
    { q: "Design the Go toolchain gates for a service repo's CI: what runs, what each catches, and what stays out of production.", a: "PR gate: go vet ./... (printf mismatches, copylocks — the test-time subset is not the full set), staticcheck (SA checks: nil-map writes, misused time.Format), go test -race ./... — dynamic detector, 5–10× CPU/memory, only catches races the tests execute, so it lives in CI and maybe a canary, never on every prod pod. Drift gates: go mod tidy -diff fails on unsynced go.mod; regenerate go generate outputs and fail on git diff, because generate never runs automatically and generated code is committed. Hygiene: no replace directives in committed go.mod (main-module-only, relative paths die in Docker); go.work stays untracked so CI sees what consumers see. Release: pinned toolchain directive plus -trimpath for reproducible binaries; govulncheck and scheduled go get -u PRs to offset MVS's no-auto-upgrade tradeoff." },
  ]}
/>

<Recap lang="en">
Go's dependency story is deterministic by construction. Require lines are minimums; MVS gathers every module's minimums and picks the maximum of them, never jumping to latest — so the build list is a pure function of go.mod files, reproducible on any machine without a lockfile, and nothing moves until go get moves it. The cost is no automatic bugfix pickup, offset by govulncheck and scheduled upgrade PRs. go.sum is the other half and the other job: a ledger of content hashes cross-checked against the public transparency log, which is why a silently re-tagged version fails every consumer's build — the Friday incident from the Hook was the mechanism succeeding. Local wiring has sharp edges: replace applies only in the main module and a committed relative-path replace breaks Docker and every consumer, while go.work gives multi-module development the same effect as developer-local, conventionally untracked configuration. The toolchain then layers what the compiler cannot promise: go vet and staticcheck for statically detectable bugs, go test -race for dynamically executed races at 5–10× cost (CI yes, prod fleet no), go generate as a human-triggered step whose outputs are committed and drift-checked, and go mod tidy -diff keeping the manifest honest. Reproducible binaries close the loop: pinned toolchain, -trimpath, verified modules — same inputs, same bytes. Now when you see a checksum mismatch in CI or a colleague deletes go.sum to "fix" it, you will know exactly what the error was telling you — and why the fix is one version bump, not a ledger deletion.
</Recap>
