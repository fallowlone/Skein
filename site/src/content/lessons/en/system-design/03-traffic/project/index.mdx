---
slug: "project"
lang: en
track: "system-design"
unit: "03-traffic"
order: 8
title: "Traffic & edge: a load balancer and edge plan"
summary: "Hands-on project: stand up a real load balancer in front of a small pool, prove that a load-aware algorithm beats round-robin under an uneven node, then design and measure a CDN cache policy — hit ratio, TTL, and what must never hit the shared edge."
estMin: 240
status: ready
lessonType: topic
level: senior
concepts:
  - load-balancing-algorithms
  - health-checks
  - cache-hit-ratio
  - cache-control-ttl
  - cache-invalidation
  - single-point-of-failure
prereqs:
  - 03-traffic/01-load-balancing
  - 03-traffic/02-reverse-proxy-and-gateway
  - 03-traffic/03-cdn
sources:
  - https://docs.nginx.com/nginx/admin-guide/load-balancer/http-load-balancer/
  - https://www.cloudflare.com/learning/cdn/what-is-caching/
  - https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Reading that a load-aware algorithm protects your tail and that a CDN's hit ratio is non-linear is not the same as proving it on a running system. Stand up a real load balancer in front of a small pool, inject one slow node, and watch round-robin versus least-response-time diverge — then design a cache policy for a mixed app and measure the hit ratio you actually get.
</Hook>

<Crux>Run a balancer over a pool, prove load-aware beats round-robin under a slow node, then design and measure a CDN cache policy.</Crux>

<Explanation>

This project makes the whole unit operational: you'll configure a real balancer, demonstrate the tail-latency difference between a blind and a load-aware algorithm, verify that a meaningful health check ejects a broken node, and then design a per-response cache policy and measure its hit ratio — including proving what must never reach the shared edge.

<ProjectBrief
  id="03-traffic-project"
  lessonSlug="project"
  lang="en"
  objective="Build a small but real traffic edge: a load balancer (nginx/HAProxy/Envoy or a cloud LB) fronting a pool of identical app instances, demonstrate empirically that a load-aware algorithm protects tail latency where round-robin does not, verify health-check ejection, and then design and measure a CDN-style cache policy for a mixed app — closing the loop between the unit's claims and observed behavior."
  requirements={[
    "Stand up a load balancer in front of at least three identical app instances (containers are fine), each serving a trivial endpoint with a configurable per-request delay. Confirm the pool serves traffic through one virtual address.",
    "Write a load generator that holds a fixed offered rate (open-loop) and records throughput and p50/p99 latency, so you can compare algorithms fairly without coordinated omission.",
    "Inject an uneven node: make one instance respond ~50–100× slower (simulating GC pauses or a degraded host) while it still accepts connections, then run the load test once under round-robin and once under least-connections or least-response-time/EWMA.",
    "Compare the two runs: show that round-robin keeps the service p99 pinned near the slow node's latency (it gets its even share) while the load-aware algorithm routes around it and holds the p99 near the healthy nodes'.",
    "Configure a meaningful health check (an HTTP endpoint that probes a dependency, not a bare TCP/static-200 check), then break that dependency on one node and show the balancer ejects it and traffic continues on the rest.",
    "Design a per-response CDN cache policy for a mixed app with: fingerprinted static assets, a public page (same for everyone), a per-user authenticated page, a must-be-correct value, and a write endpoint. State the Cache-Control/TTL/Vary for each and which must never hit a shared cache.",
    "Stand up a caching reverse proxy (or use a CDN) in front of the public content and measure the cache hit ratio under load; then improve it (TTL tuning and cache-key normalization) and record the before/after hit ratio and the implied origin-load reduction.",
    "Write up the failover capacity math: with N nodes at your measured peak utilization, compute whether the survivors can absorb one lost node's share without crossing the queueing knee, and state the N−1 headroom you'd provision.",
  ]}
  acceptance={[
    "A running balancer + pool reachable through one address, with the algorithm and health-check configuration shown.",
    "Two load-test results (round-robin vs load-aware) under the same injected slow node, with p50/p99 and throughput, visibly showing round-robin's p99 tracking the slow node and the load-aware algorithm holding the tail near the healthy nodes.",
    "A demonstration that breaking a node's probed dependency causes the balancer to eject it (logs or metrics), with traffic continuing on the remaining nodes and recovering when the node is healed.",
    "A one-page cache-policy table covering all five content types with explicit Cache-Control/TTL/Vary, and a one-line justification per row, including which responses are never cacheable on a shared edge and why (leak / correctness / write).",
    "A measured before/after cache hit ratio for the public content with the change you made, plus the implied origin-RPS reduction (showing you understand the non-linear miss-fraction economics).",
    "A short failover write-up: the N−1 headroom calculation and the recommended pool size, tying back to the thundering-herd risk.",
  ]}
  stretch={[
    "Add consistent hashing: put a keyed cache tier behind the balancer, show that hashing on the key gives a high hit ratio while least-connections collapses it, and measure how many keys remap when you add/remove a node (~1/N).",
    "Add a thundering-herd scenario: expire a hot object's TTL across many edge/proxy caches at once, observe the synchronized origin spike, then enable request coalescing (and/or an origin shield) and show the origin sees one fetch instead of many.",
    "Make the gateway redundant: run two balancer/gateway instances behind a higher-level balancer, kill one mid-load, and show no full outage — contrasting it with the single-instance SPOF.",
    "Add jittered retries to the load generator and show how synchronized (un-jittered) retries during a node failure accelerate the cascade, while jitter spreads the recovery load and avoids the brownout-to-outage feedback loop.",
  ]}
/>

</Explanation>

<KeyTakeaway>A traffic edge plan makes the unit operational: prove load-aware beats round-robin under a slow node, verify health-check ejection, and design plus measure a CDN cache policy and its hit ratio.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-traffic-project-retrieval"
  lang="en"
  questions={[
    { q: "How do you empirically show a load-aware algorithm protects tail latency?", a: "Front an identical pool with a balancer, add a load generator that holds a fixed open-loop rate and records p50/p99, then inject one node that responds ~50–100× slower while still accepting connections. Run once under round-robin and once under least-connections/least-response-time. Round-robin keeps the service p99 near the slow node's latency (it gets its even 1/N share); the load-aware algorithm routes around the slow node (its connection backlog or measured latency grows) and holds p99 near the healthy nodes. That divergence is the proof." },
    { q: "What does a sound CDN cache-policy design specify per response type?", a: "Cache-Control, TTL, and Vary for each: fingerprinted assets get long max-age + immutable; the public page gets a short-to-moderate s-maxage plus purge-on-publish; the per-user authenticated page is private or no-store with Vary on auth (never shared); the must-be-correct value is no-store (or a tiny micro-TTL); the write endpoint is not cacheable. The rule: cache what's the same for everyone and safe when stale; never cache per-user, must-be-correct, or write responses on a shared edge." },
    { q: "Why measure the hit ratio and compute N−1 headroom rather than trust the defaults?", a: "Hit-ratio economics are non-linear — improving 90% → 95% halves origin traffic — so measuring before/after a TTL/cache-key change quantifies the real origin-load reduction instead of guessing. And N−1 headroom is the defense against the thundering herd on failover: at your measured peak utilization, the survivors must absorb a lost node's share without crossing the queueing knee, so you compute the spare capacity and size the pool for it rather than discovering the cascade in production." },
  ]}
/>

<Recap lang="en">
This project turns the traffic-and-edge unit into a repeatable workflow: stand up a **load balancer** over an identical pool, then **prove** the unit's central claim by injecting one slow node and showing **round-robin** pins the service p99 to it while a **load-aware** algorithm (least-connections/least-response-time) routes around it. Verify that a **meaningful health check** — one that probes a real dependency, not a static 200 — **ejects** a broken node and that traffic continues. Then design a **per-response CDN cache policy** for a mixed app (fingerprinted assets, public page, per-user page, must-be-correct value, write endpoint), measure the **hit ratio** before and after a TTL/cache-key improvement, and translate it into the non-linear origin-load reduction. Finally, do the **N−1 failover capacity math** so the survivors can absorb a lost node without a thundering-herd cascade. Doing the configuration and measurement, not just reading the claims, is the whole discipline — an engineer who has built this once chooses layers, algorithms, gateways, and cache policies deliberately instead of discovering their failure modes during an incident.
</Recap>
