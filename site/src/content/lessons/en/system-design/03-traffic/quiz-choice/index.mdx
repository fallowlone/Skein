---
slug: "quiz-choice"
lang: en
track: "system-design"
unit: "03-traffic"
order: 5
title: "Traffic & edge: multiple-choice review"
summary: "Multiple-choice synthesis across the traffic unit: L4 vs L7 and load-balancing algorithms, reverse proxy vs gateway and the SPOF it concentrates, the mesh split, and CDN hit-ratio, TTL, and what not to cache."
estMin: 13
status: ready
lessonType: topic
level: senior
concepts:
  - l4-vs-l7
  - load-balancing-algorithms
  - api-gateway
  - service-mesh
  - cache-hit-ratio
  - cache-invalidation
prereqs:
  - 03-traffic/01-load-balancing
  - 03-traffic/02-reverse-proxy-and-gateway
  - 03-traffic/03-cdn
sources:
  - https://www.cloudflare.com/learning/performance/types-of-load-balancing-algorithms/
  - https://www.cloudflare.com/learning/cdn/what-is-a-cdn/
  - https://docs.aws.amazon.com/whitepapers/latest/microservices-on-aws/api-gateway.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each is a decision you make when you place a load balancer, choose an algorithm, decide whether a gateway earns its keep, or set a CDN cache policy — not a definition to recite, but the tradeoffs of L4/L7, the algorithms, the SPOF, the mesh, and hit-ratio economics under real load.
</Hook>

<Crux>Six decisions across the unit: a layer, an algorithm, a gateway's blast radius, the mesh split, a hit-ratio number, and what not to cache.</Crux>

<Explanation>

Confirm you can pick the right layer and algorithm for a balancer, see why a single gateway is a SPOF and when a mesh fits instead, run the hit-ratio arithmetic, and reject caching a per-user response on a shared edge.

<Quiz
  id="03-traffic-choice-1"
  lessonSlug="quiz-choice"
  lang="en"
  question="You must route requests by URL path and read a routing cookie, and you also want per-request retries. Which balancer layer is required, and what does it cost?"
  choices={[
    { label: "Layer 7 — it terminates and parses each HTTP request to see the path and cookie and to retry at the request level, at the cost of CPU/latency (it proxies two connections per request).", correct: true },
    { label: "Layer 4 — it's faster and can still read the path.", misconception: "L4 routes only by IP/port and never parses the HTTP bytes, so it can't see a path or cookie or retry a request. Content-based routing requires L7." },
    { label: "Either layer; path routing doesn't depend on the layer.", misconception: "Path and cookie live inside the HTTP message; an L4 balancer forwards the connection without reading it. You must be at L7 to route on content." },
    { label: "Layer 7, and it's free because parsing is cheap.", misconception: "The capability is real but not free: L7 terminates TLS/TCP, parses each request, and proxies two connections, costing CPU and latency. That tradeoff is why L4 exists." },
  ]}
/>

<Quiz
  id="03-traffic-choice-2"
  lessonSlug="quiz-choice"
  lang="en"
  question="In a round-robin pool of ten, one node degrades to 5 s/request (others ~50 ms) but keeps accepting connections. What happens to the service p99, and which algorithm fixes it?"
  choices={[
    { label: "Round-robin keeps sending it 1/10 of traffic, so ~10% of requests get 5 s and the service p99 ≈ 5 s. A load-aware algorithm — least-connections or least-response-time/EWMA — routes around it.", correct: true },
    { label: "Round-robin detects the slow node and skips it automatically.", misconception: "Round-robin counts requests, not response times; it keeps feeding the slow node its even share. You need a load-aware algorithm or a latency-aware passive health check." },
    { label: "One slow node in ten can't move the tail.", misconception: "It dominates the tail: a tenth of requests get the 5 s response, so the service p99 tracks the slow node. Tail latency follows the worst server you keep routing to." },
    { label: "The node is ejected instantly for being slow.", misconception: "Slow isn't failing — a basic health check sees it accept connections and respond (late) and keeps it in rotation. Only a load-aware algorithm or latency-aware probe reacts." },
  ]}
/>

<Quiz
  id="03-traffic-choice-3"
  lessonSlug="quiz-choice"
  lang="en"
  question="A single API gateway in front of 15 services crashes on a bad config push; every service is healthy but all 15 are unreachable. What's the property and the fix?"
  choices={[
    { label: "The gateway is a single point of failure (all traffic funnels through it). Fix: run a redundant, horizontally-scaled gateway pool behind its own balancer with health checks, and stage config changes (canary).", correct: true },
    { label: "The services failed; restart them.", misconception: "The services were healthy — the gateway couldn't route to them. The concentrated failure is the gateway SPOF; restarting healthy services doesn't address it." },
    { label: "Remove the gateway; gateways are always a liability.", misconception: "The centralization (auth/rate-limit/routing once) is valuable. The fix is eliminating the single-instance SPOF with a redundant pool and staged rollouts, not removal." },
    { label: "Push auth back into every service.", misconception: "That re-creates the duplication the gateway solved and still leaves the routing SPOF. Make the gateway tier redundant and roll config safely instead." },
  ]}
/>

<Quiz
  id="03-traffic-choice-4"
  lessonSlug="quiz-choice"
  lang="en"
  question="Forty services have heavy service-to-service traffic with missing retries, inconsistent mTLS, and no tracing. The public API also needs one auth/rate-limit front door. What's the right shape?"
  choices={[
    { label: "A gateway for north-south (clients → platform: auth, public rate limits, public API, TLS) AND a service mesh for east-west (per-instance sidecars: mTLS, retries, timeouts, tracing) — two planes, each owning its concern.", correct: true },
    { label: "Put everything in the gateway — auth, plus all the retries and mTLS for internal calls.", misconception: "Funneling east-west traffic through the gateway creates a god-gateway and a central bottleneck/SPOF. East-west concerns belong in a mesh's per-instance sidecars, not the central tier." },
    { label: "A service mesh alone; it can present the public API too.", misconception: "A mesh handles east-west (service-to-service) concerns; it isn't the public front door. You still want a gateway for north-south auth, public rate limits, and the public API surface." },
    { label: "Neither — just add retries in each service's code.", misconception: "That's the duplication problem again (drifting copies, the original incidents). A mesh standardizes east-west retries/mTLS/tracing centrally-configured but locally-executed; a gateway owns the public API." },
  ]}
/>

<Quiz
  id="03-traffic-choice-5"
  lessonSlug="quiz-choice"
  lang="en"
  question="A CDN at 90% hit ratio sends 20,000 RPS of misses to the origin. A cache-key cleanup raises it to 95%. Roughly what happens to origin load?"
  choices={[
    { label: "Origin misses roughly halve: 10% → 5% of traffic, so ~20,000 → ~10,000 RPS. Hit-ratio economics are non-linear — each step toward 100% halves the residual origin traffic.", correct: true },
    { label: "Origin load drops ~5%, matching the 5-point ratio gain.", misconception: "It's linear in the MISS fraction (10% → 5%, i.e. halved), not in the ratio. Origin load roughly halves. That non-linearity is why small hit-ratio gains pay off." },
    { label: "Nothing — hit ratio only affects latency.", misconception: "Hit ratio is exactly origin offload: a hit is a request the origin never sees. 90% → 95% halves the miss fraction and roughly halves origin RPS." },
    { label: "Origin load doubles because more is cached.", misconception: "Backwards — more cached means fewer misses reach the origin. 90% → 95% halves the miss fraction, so origin load roughly halves." },
  ]}
/>

<Quiz
  id="03-traffic-choice-6"
  lessonSlug="quiz-choice"
  lang="en"
  question="An engineer wants to cache the authenticated account page (shows the logged-in user's orders) on the shared CDN to make it fast. What's the verdict and why?"
  choices={[
    { label: "Reject it: a shared edge keying on the URL can serve User A's orders to User B — a data leak. Mark it Cache-Control: private (browser-only) or no-store, and Vary on auth; only cache per-user content in a per-user (browser) cache.", correct: true },
    { label: "Fine — caching makes it faster and the CDN keeps users separate automatically.", misconception: "A shared cache keys on the URL, not the user; without private/no-store it can serve one user's page to another. Personalized responses must never live in a shared cache." },
    { label: "Fine if you set a 1-second TTL.", misconception: "A short TTL still serves the cached copy to whoever requests that URL within the window — including a different user. The issue isn't freshness, it's that per-user data must not be shared." },
    { label: "Reject it, because authenticated pages can't be cached anywhere.", misconception: "Correct verdict, wrong reason: they can be cached in the user's own browser (Cache-Control: private). The rule is no SHARED caching of per-user content, not no caching at all." },
  ]}
/>

</Explanation>

<KeyTakeaway>Traffic decisions are tradeoffs made before load: the right layer and algorithm, a redundant gateway so it isn't a SPOF, a mesh for east-west, hit-ratio math, and never caching per-user data on a shared edge.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-traffic-choice-retrieval"
  lang="en"
  questions={[
    { q: "Why does a load-aware algorithm protect tail latency where round-robin doesn't?", a: "Round-robin counts requests and feeds a slow node its even share regardless of how slow it is, so the service p99 tracks the worst node. Least-connections and least-response-time/EWMA are load-aware: a slow node accumulates open connections or high measured latency and stops receiving its share, so traffic routes around it and the tail stays controlled." },
    { q: "How do you reject caching a per-user response on a shared CDN in one sentence?", a: "A shared edge keys on the URL, not the user, so without Cache-Control: private/no-store (and Vary on auth) it can serve User A's authenticated page to User B at the same URL — a data leak; per-user content may only be cached in the user's own browser, never on the shared edge." },
  ]}
/>

<Recap lang="en">
The through-line is that traffic and edge are tradeoffs you settle before load arrives. A **balancer** needs the right layer (L4 fast and protocol-blind, L7 content-aware at a CPU cost) and the right **algorithm** (load-aware least-connections/EWMA protect the tail; consistent hashing preserves cache locality). A **gateway** centralizes auth/rate-limit/routing — a consistency win that becomes a single point of failure, so it runs redundant and stays thin, while a **service mesh** handles east-west concerns in per-instance sidecars. A **CDN** lives or dies on its **hit ratio**, whose economics are non-linear (90% → 95% halves origin traffic), and the discipline is to cache what's the same for everyone and safe when stale and **never cache per-user or must-be-correct data on a shared edge**.
</Recap>
