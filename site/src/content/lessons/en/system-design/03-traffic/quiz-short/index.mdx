---
slug: "quiz-short"
lang: en
track: "system-design"
unit: "03-traffic"
order: 6
title: "Traffic & edge: free-recall review"
summary: "Free-recall prompts across the traffic unit. Answer each in your own words first — including the tradeoffs and the arithmetic — then reveal the model answer and compare."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - l4-vs-l7
  - load-balancing-algorithms
  - api-gateway
  - service-mesh
  - cache-hit-ratio
  - cache-invalidation
prereqs:
  - 03-traffic/01-load-balancing
  - 03-traffic/02-reverse-proxy-and-gateway
  - 03-traffic/03-cdn
sources:
  - https://www.cloudflare.com/learning/performance/types-of-load-balancing-algorithms/
  - https://www.cloudflare.com/learning/cdn/what-is-caching/
  - https://docs.nginx.com/nginx/admin-guide/load-balancer/http-load-balancer/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory — including the tradeoffs and the arithmetic — before you open the model answer. The effort of reconstructing the L4/L7 split, the algorithms, the gateway's blast radius, and the hit-ratio economics is what makes them stick.
</Hook>

<Crux>Reconstruct the unit's spine: L4 vs L7, the algorithms, reverse proxy vs gateway, the mesh split, and CDN hit-ratio, TTL, and invalidation.</Crux>

<Explanation>

Reconstruct the unit's spine without looking back: where a balancer reads the request and which algorithm protects the tail, what a gateway adds and concentrates, when a mesh fits, and how a CDN's hit ratio, TTL, and invalidation work.

<RetrievalDrawer
  client:visible
  id="03-traffic-short-retrieval"
  lang="en"
  questions={[
    { q: "Contrast L4 and L7 load balancing, and name the algorithms from blind to load-aware.", a: "L4 (transport) routes by TCP/UDP IP+port without reading the payload: fast, protocol-agnostic, blind to content. L7 (application) terminates and parses the HTTP request to route on path/host/header/cookie and to retry/terminate TLS, at a CPU/latency cost (two connections per request). Algorithms: round-robin (even by count, blind) → weighted RR (heterogeneous fleet) → least-connections (proxy for least-busy) → least-response-time/EWMA (tracks recent latency, prefers the fastest) → consistent hashing (key → same node, for cache/shard locality, remapping ~1/N on change). The load-aware ones protect tail latency by routing around a slow node instead of feeding it an even share." },
    { q: "What is consistent hashing for, and why not plain hash-modulo-N?", a: "It maps keys and servers onto a ring so a given key (cache key, shard key, user) lands on the same server while membership is stable — needed for cache/shard locality. On a join or leave, only the keys in the affected arc (~1/N) remap, so a deploy or autoscale event doesn't cold-start the whole cache. Plain hash-modulo-N remaps nearly every key when N changes, flushing the entire cache at once and stampeding the origin. Virtual nodes smooth out uneven arc sizes." },
    { q: "Distinguish forward proxy, reverse proxy, and API gateway, and state the gateway's core tradeoff.", a: "A forward proxy fronts clients (represents them outbound). A reverse proxy fronts servers (clients think it's the server) — every load balancer is one. An API gateway is a reverse proxy that absorbs application-level cross-cutting concerns: auth, rate limiting, routing/versioning, aggregation, TLS termination, observability. Tradeoff: that one place is a consistency win (policy enforced once) but also a single point of failure, a throughput bottleneck, a latency tax, and a deployment chokepoint — so run it redundant behind its own balancer and keep it thin (no business logic)." },
    { q: "When do you reach for a service mesh, and how does north-south vs east-west split?", a: "A mesh moves east-west (service-to-service) concerns — mTLS, retries, timeouts, circuit breaking, internal load balancing, tracing — into a sidecar proxy per service instance, configured centrally but executed locally, so there's no central tier all internal traffic funnels through. A gateway owns north-south (clients → platform): auth, public rate limits, the public API surface, TLS termination. Use a mesh when internal service-to-service traffic is where reliability/security problems live; a gateway when the problem is presenting one secure public API. They commonly coexist; don't overload one god-gateway with east-west concerns." },
    { q: "Walk a CDN's two benefits, the hit-ratio economics, push vs pull, and what not to cache.", a: "Benefits: lower latency (nearby edge ~ms vs far origin ~150 ms cross-region) and origin offload (a hit is a request the origin never sees, so 95% hits ≈ a 20× smaller origin). Hit-ratio economics are non-linear: 90% → 95% halves origin traffic (10% → 5% miss), 95% → 99% halves it again. Pull (lazy, demand-driven) is the default for web traffic; push (eager pre-warm) suits large predictable assets. Invalidation is the hard part — purge is active, TTL is passive, content-hashed URLs avoid it entirely. Never cache per-user/sensitive responses, must-be-correct data, or writes on a shared edge (use private/no-store/Vary), and use origin shielding + request coalescing against the thundering herd." },
  ]}
/>

</Explanation>

<KeyTakeaway>Reconstruct each answer from memory — L4 vs L7 and the algorithms, the gateway's win and SPOF, the mesh split, and the CDN hit-ratio math — and you hold the unit's spine.</KeyTakeaway>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the unit's spine: a **balancer** reads requests at **L4** (fast, protocol-blind) or **L7** (content-aware, at a CPU cost), and its **algorithm** decides the tail — load-aware least-connections/EWMA route around a slow node, while **consistent hashing** preserves cache/shard locality. A **gateway** is a reverse proxy that centralizes auth/rate-limit/routing/TLS, a consistency win that becomes a **SPOF/bottleneck** unless run redundant and thin; a **service mesh** handles east-west concerns in per-instance sidecars. A **CDN** trades on **hit ratio** (non-linear: 90% → 95% halves origin load), gets content via **pull** (default) or **push**, and its hard problem is **invalidation** (purge active, TTL passive, content-hashed URLs avoiding it), with **origin shielding** against the herd and a hard rule never to cache per-user or must-be-correct data on a shared edge. The thread: at this depth, traffic and edge are reasoning you do up front, not behavior you discover in production.
</Recap>
