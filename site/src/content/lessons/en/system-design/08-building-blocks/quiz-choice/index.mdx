---
slug: "quiz-choice"
lang: en
track: "system-design"
unit: "08-building-blocks"
order: 6
title: "Building blocks: multiple-choice review"
summary: "Multiple-choice synthesis across the building-blocks unit: the rate-limiter race, time-ordered vs random IDs, the bloom filter's one-sided guarantee, the geohash boundary problem, and the split-brain that fencing tokens defeat."
estMin: 13
status: ready
lessonType: topic
level: senior
concepts:
  - distributed-rate-limiting
  - snowflake-id
  - false-positive-rate
  - boundary-problem
  - fencing-token
  - split-brain
prereqs:
  - 08-building-blocks/01-rate-limiter
  - 08-building-blocks/02-unique-id-generation
  - 08-building-blocks/03-bloom-filters
  - 08-building-blocks/04-geohashing
  - 08-building-blocks/05-leader-election
sources:
  - https://stripe.com/blog/rate-limiters
  - https://en.wikipedia.org/wiki/Bloom_filter
  - https://martin.kleppmann.com/2016/02/08/how-to-do-distributed-locking.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Five questions that cut across the whole unit. Each is a decision you make when you reach for an off-the-shelf mechanism — a limiter, an ID scheme, a membership filter, a spatial index, a coordinator — and get the subtle part right instead of the obvious part.
</Hook>

<Crux>Five decisions: the limiter race, the right ID, the filter's guarantee, the geohash boundary, and the fencing token that beats split-brain.</Crux>

<Explanation>

Confirm you can spot the distributed-counter race, pick a time-ordered vs random ID for the situation, reason from the bloom filter's no-false-negatives guarantee, defeat the geohash boundary problem, and make split-brain harmless with a fencing token.

<Quiz
  id="08-building-blocks-choice-1"
  lessonSlug="quiz-choice"
  lang="en"
  question="A rate limiter runs on 40 servers, each doing GET-counter → check < limit → SET counter+1 against shared Redis. Clients exceed their limit under load. What's the minimal correct fix?"
  choices={[
    { label: "Make increment-and-check a single atomic operation (Redis INCR, or an atomic Lua script) so two requests can't both read the same stale count.", correct: true },
    { label: "Give each of the 40 servers 1/40th of the limit.", misconception: "Static splits waste capacity under uneven balancing and still drift. The bug is the read-modify-write race on the shared counter; the fix is an atomic increment-and-check, not partitioning the limit." },
    { label: "Acquire a distributed lock around the counter on every request.", misconception: "A per-request lock serialises the whole fleet and becomes the bottleneck. You need an atomic primitive (INCR/Lua), which gives correctness without serialising every request." },
    { label: "Increase the limit so the overshoot is within tolerance.", misconception: "That hides the bug instead of fixing it and defeats the limiter's purpose. The overshoot is a lost-update race; close it with atomicity." },
  ]}
/>

<Quiz
  id="08-building-blocks-choice-2"
  lessonSlug="quiz-choice"
  lang="en"
  question="A clustered-index OLTP table ingests millions of rows/hour and you need decentralized ID generation across regions without fragmenting the index. Which ID scheme fits best?"
  choices={[
    { label: "A time-ordered ID (UUIDv7 or Snowflake): each node mints locally with no coordinator, and the high-bit timestamp keeps inserts at the index's right edge instead of scattering.", correct: true },
    { label: "Auto-increment from a single central sequence server.", misconception: "A central sequence is a write-path bottleneck and SPOF — the opposite of decentralized. Use a coordination-free, time-ordered ID so each region mints its own without fragmenting the index." },
    { label: "Random UUIDv4 — it's decentralized, so it's ideal here.", misconception: "UUIDv4 is decentralized but its randomness fragments a clustered B-tree (page splits, lost cache locality), which is exactly the throughput problem you're trying to avoid. UUIDv7 keeps decentralization AND right-edge inserts." },
    { label: "Per-shard local auto-increment counters.", misconception: "Local counters collide across shards (two shards both emit id 1), so IDs aren't globally unique. A time-ordered distributed ID avoids both the collision and the fragmentation." },
  ]}
/>

<Quiz
  id="08-building-blocks-choice-3"
  lessonSlug="quiz-choice"
  lang="en"
  question="You want to guard an expensive database read with a bloom filter, but a teammate worries a wrong answer could cause harm. When is the filter safe to use here?"
  choices={[
    { label: "It's safe because a false positive just triggers a fallback read that returns empty — harmless. The filter never has false negatives, so 'definitely absent' is always correct and can skip the read.", correct: true },
    { label: "It's unsafe; bloom filters have false negatives, so they might skip a key that's really there.", misconception: "Bloom filters have NO false negatives — 'definitely absent' is always correct. The only error is a false positive (a harmless extra lookup). That's exactly why they're safe as a read guard." },
    { label: "It's safe only if you also enable deletions on the filter.", misconception: "Deletion is irrelevant to read-guard safety (and a plain filter can't delete anyway). Safety comes from the one-sided guarantee: a false positive only causes a harmless fallback read." },
    { label: "It's never safe; bloom filters can't be trusted for any decision.", misconception: "They're safe wherever a false positive is cheap (a fallback to ground truth). They're only unsafe as the SOLE authority for a harmful 'yes' — a read guard is the safe case." },
  ]}
/>

<Quiz
  id="08-building-blocks-choice-4"
  lessonSlug="quiz-choice"
  lang="en"
  question="A 'drivers near me' query selects only the rider's own geohash cell and misses a driver parked 20 m away across the street. What's the fix?"
  choices={[
    { label: "Query the rider's cell plus its 8 surrounding cells (the 3×3 grid), then rank by exact distance — the close driver is just over a cell boundary and shares no prefix.", correct: true },
    { label: "Use a longer geohash so the cell is smaller and more precise.", misconception: "Precision doesn't fix it — a driver just across ANY cell boundary still has a different code. The boundary problem is solved by expanding to the 8 neighbour cells, not by changing precision." },
    { label: "Switch from a string index to a special spatial index.", misconception: "An ordinary string/B-tree index handles geohash prefix scans fine. The bug is querying one cell; the fix is including the 8 neighbours, regardless of index type." },
    { label: "Recompute all drivers' distances on every query (full scan).", misconception: "That throws away the whole point of geohashing (an indexed prefix scan). Keep the index; just expand to the rider's cell plus its 8 neighbours and then rank by exact distance." },
  ]}
/>

<Quiz
  id="08-building-blocks-choice-5"
  lessonSlug="quiz-choice"
  lang="en"
  question="A leader takes a lock, suffers a long GC pause past its lease, a new leader is elected, and the old one wakes up still acting as leader. Both write. What actually prevents corruption?"
  choices={[
    { label: "A fencing token: each grant carries a monotonically increasing number, and the resource rejects any operation with a token lower than the highest it has seen — so the woken-up leader's stale writes are refused.", correct: true },
    { label: "A much longer lease, so the GC pause fits inside it.", misconception: "No timeout distinguishes 'dead' from 'frozen,' and a longer lease only slows failover while still allowing arbitrarily long pauses. The real fix is a fencing token enforced at the resource." },
    { label: "Adding more nodes to reach a larger quorum.", misconception: "More nodes don't stop a stale leader from acting after its lease expired. The fix is fencing tokens so the resource rejects stale-token operations." },
    { label: "Nothing — the old leader will notice and stop on its own.", misconception: "It acts before it notices; that's the danger. You must fence its stale writes at the resource with a monotonically increasing token, not trust it to self-correct." },
  ]}
/>

</Explanation>

<KeyTakeaway>Each block has a subtle core: the limiter race needs atomicity, IDs a time-ordered scheme, the bloom filter only where a false positive is cheap, geohash its 8 neighbours, split-brain a fencing token.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="08-building-blocks-choice-retrieval"
  lang="en"
  questions={[
    { q: "Why does a distributed rate limiter overshoot, and what's the one-line fix?", a: "Across a fleet, the naïve read-modify-write races: two requests both read the same count below the limit, both allow, the client exceeds it (a lost update). Fix: make increment-and-check a single atomic operation — Redis INCR or an atomic Lua script — so no two requests act on the same stale read." },
    { q: "When is a bloom filter safe, and when is it not?", a: "Safe wherever a false positive is cheap, because it only triggers a fallback to ground truth (e.g. guarding a DB read — a false positive is one wasted empty lookup, and there are never false negatives). Unsafe as the SOLE authority for a harmful 'yes' (already-used coupon, duplicate transaction), where a false positive wrongly rejects a legitimate action." },
    { q: "State the fencing-token rule that defeats split-brain.", a: "The coordination service hands out a monotonically increasing number with each leadership grant; every operation carries its token, and the protected resource rejects any token lower than the highest it has seen. A revived stale leader's old token is refused, so its writes are harmless regardless of timeouts — correctness lives at the resource, not in the lease length." },
  ]}
/>

<Recap lang="en">
The through-line of the unit is that each building block has an obvious surface and a subtle correctness core. A **rate limiter** is trivial on one node but races on a fleet — the fix is an **atomic** increment-and-check on one shared counter (and fail open if it's down). **ID generation** is trivial with one writer but needs a **time-ordered** scheme (Snowflake/UUIDv7) to stay coordination-free without fragmenting the index. A **bloom filter** is a fast membership pre-check that is safe only where a **false positive is cheap**, because it guarantees no false negatives but not no false positives. **Geohashing** turns proximity into a prefix scan but must query the cell **plus its 8 neighbours** to beat the boundary problem. And **leader election** is easy until **split-brain**, which no timeout can prevent — only a **fencing token** enforced at the resource makes a stale leader harmless. Get the subtle part, not just the obvious part.
</Recap>
