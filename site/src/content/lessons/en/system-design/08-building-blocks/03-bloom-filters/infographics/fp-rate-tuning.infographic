infographic chart-column-simple
data
  title Tuning a bloom filter: bits per element buys accuracy
  desc Each extra ~5 bits per element cuts the false-positive rate by ~10x
  values
    - label 5 bits/elem → ~10% false positives
      value 5
    - label 10 bits/elem → ~1% false positives
      value 10
    - label 15 bits/elem → ~0.1% false positives
      value 15
theme
  palette #f59e0b #6366f1 #14b8a6
