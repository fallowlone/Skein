---
slug: "quiz-choice"
lang: en
track: "system-design"
unit: "07-storage-choices"
order: 4
title: "Storage choices: multiple-choice review"
summary: "Multiple-choice synthesis across the storage unit: SQL vs NoSQL and the ACID/BASE trade, access-pattern-first design, blobs vs object storage with presigned URLs, durability vs availability, specialized stores, and the dual-write problem."
estMin: 13
status: ready
lessonType: topic
level: senior
concepts:
  - acid-vs-base
  - access-pattern-design
  - object-storage
  - durability-vs-availability
  - dual-write-consistency
  - polyglot-persistence
prereqs:
  - 07-storage-choices/01-sql-vs-nosql
  - 07-storage-choices/02-blob-and-object
  - 07-storage-choices/03-time-series-and-search
sources:
  - https://aws.amazon.com/nosql/
  - https://docs.aws.amazon.com/AmazonS3/latest/userguide/Welcome.html
  - https://www.elastic.co/guide/en/elasticsearch/reference/current/documents-indices.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each is a decision you make at a design review or on a whiteboard — which store fits a workload, where the bytes live, what consistency you're trading, and how two stores stay in sync — not a definition to recite.
</Hook>

<Crux>Six decisions: the ACID workload, the access-pattern trap, where bytes live, durability vs availability, the specialized store, dual writes.</Crux>

<Explanation>

Confirm you can place a workload on the SQL/NoSQL axis by the consistency it needs, spot when access-pattern-first design will bite, keep bytes out of the database, separate durability from availability, pick a specialized store on a measured limit, and avoid the silent dual-write divergence.

<Quiz
  id="07-storage-choice-1"
  lessonSlug="quiz-choice"
  lang="en"
  question="A feature must atomically move money between two rows with no intermediate state visible to concurrent transactions. Which property is non-negotiable, and what store class typically provides it?"
  choices={[
    { label: "ACID atomicity + isolation, enforced by the engine — typically a relational store. The all-or-nothing multi-row invariant can't be left to application code.", correct: true },
    { label: "BASE eventual consistency — it's faster and the balances converge.", misconception: "Convergence 'eventually' is exactly the window you can't allow for money: one row debited, the other not yet, visible to a concurrent read. This needs ACID atomicity + isolation, not BASE." },
    { label: "Horizontal write scale via a wide-column store.", misconception: "Write scale doesn't address a correctness invariant across two rows. A wide-column store tuned for write throughput typically relaxes the multi-row transaction you need here." },
    { label: "A flexible schema to add fields without migrations.", misconception: "Schema flexibility is irrelevant to atomic money movement. The non-negotiable is atomic, isolated multi-row transactions — an ACID guarantee." },
  ]}
/>

<Quiz
  id="07-storage-choice-2"
  lessonSlug="quiz-choice"
  lang="en"
  question="You modeled a key-value table around 'get item by id.' Six months later product needs 'list all items for a customer, newest first,' and customer isn't in the key. What does the access-pattern-first design predict?"
  choices={[
    { label: "The query has no key to seek on, so on the existing table it degrades to a full scan; the real options are a secondary index (more cost, eventual consistency), a key redesign, or a migration — none free.", correct: true },
    { label: "Nothing — NoSQL queries any field as fast as a relational WHERE.", misconception: "It doesn't. Without a matching key, the store can only scan. Access-pattern-first design is fast for planned patterns and punishes unplanned ones — the opposite of relational's ad-hoc flexibility." },
    { label: "Just add a B-tree index like in SQL and it's solved cheaply.", misconception: "NoSQL secondary indexes aren't free relational B-trees: they add cost and are typically eventually consistent. The point stands — the new pattern wasn't designed in, so it's expensive." },
    { label: "Switch the whole system to a graph database.", misconception: "A list-by-customer query isn't a traversal; a graph store doesn't fit. The lesson is that the key design didn't anticipate this pattern, not that the family is wrong." },
  ]}
/>

<Quiz
  id="07-storage-choice-3"
  lessonSlug="quiz-choice"
  lang="en"
  question="A video app stores multi-GB uploads as BYTEA in Postgres 'to keep bytes and metadata in one transaction.' What breaks at scale, and what's the fix?"
  choices={[
    { label: "Backups balloon, the buffer cache is poisoned by blob bytes, WAL/replication bloat, and the connection pool starves. Fix: bytes in object storage under a key, a small metadata row in the DB, served via presigned URLs.", correct: true },
    { label: "Nothing — keeping bytes and metadata transactional is the clean design.", misconception: "That single 'benefit' is dwarfed by the costs: ballooning backups, a poisoned buffer cache, WAL bloat, and pool starvation. Databases are for small structured rows; large bytes belong in object storage." },
    { label: "Just add read replicas to spread the blob reads.", misconception: "Replicas inherit the same bloat (and you ship every blob down the replication stream). The structural fix is removing blobs from the DB entirely — metadata in DB, bytes in object storage." },
    { label: "Store the bytes on the app server's local disk instead.", misconception: "Local disk attaches to one machine, isn't durable across machine death, and breaks the moment you add a second app node. Object storage is the machine-independent, durable home for large bytes." },
  ]}
/>

<Quiz
  id="07-storage-choice-4"
  lessonSlug="quiz-choice"
  lang="en"
  question="'S3 is eleven-nines durable, so our images survive any outage and we don't need cross-region replication.' What's the precise correction?"
  choices={[
    { label: "Durability (bytes still exist, ~11 nines) is not availability (bytes reachable now, often ~3 nines). A region outage leaves data perfectly durable but unreachable; surviving it needs cross-region replication.", correct: true },
    { label: "Correct — eleven nines means always reachable too.", misconception: "Durability and availability are different guarantees. Eleven nines is about not LOSING data; reachability is a separate, lower number, and a region outage breaks reachability without losing a byte." },
    { label: "Durability is irrelevant; only latency matters for images.", misconception: "Durability matters (it's why uploads aren't lost), but the claim conflated it with availability. The fix is to separate 'still exists' from 'reachable now,' then add cross-region replication for outages." },
    { label: "Move the images into the database for outage safety.", misconception: "That reintroduces every blob-in-DB failure and doesn't improve regional availability. The right answer is cross-region replication of the object store." },
  ]}
/>

<Quiz
  id="07-storage-choice-5"
  lessonSlug="quiz-choice"
  lang="en"
  question="A search box runs `WHERE description LIKE '%word%'` on a 3M-row table with a B-tree index on description, and it times out under load. What's the verdict and the right tool?"
  choices={[
    { label: "The leading wildcard makes the B-tree useless, forcing a full scan of all 3M rows per query. Use a search engine's inverted index (or Postgres tsvector): term → posting list, plus stemming and relevance LIKE can't do.", correct: true },
    { label: "Add more B-tree indexes on description and it'll use them.", misconception: "No B-tree helps a leading-wildcard LIKE — there's no prefix to seek on, so it scans regardless. The fix is an inverted index that maps terms to documents, not another B-tree." },
    { label: "It's fine; LIKE is real search, just needs a bigger instance.", misconception: "A bigger box delays the wall, not removes it: it's still O(rows) per query and offers no ranking, stemming, or typo tolerance. LIKE isn't search; an inverted index is." },
    { label: "Store the descriptions in object storage to offload the DB.", misconception: "Object storage has no query capability at all — no WHERE, no text matching. The workload needs a search engine's inverted index, not blob storage." },
  ]}
/>

<Quiz
  id="07-storage-choice-6"
  lessonSlug="quiz-choice"
  lang="en"
  question="You write an order to Postgres, then best-effort index it in Elasticsearch (log and ignore failures) to keep the request fast. Months later search is missing orders unpredictably. Root cause and fix?"
  choices={[
    { label: "A dual write with no shared transaction: a crash or failed index call silently diverges the stores. Make Postgres authoritative and derive the index from its commit via CDC or a transactional outbox.", correct: true },
    { label: "The search engine is buggy; switch vendors.", misconception: "No vendor fixes this — it's architectural. Two independent writes with no shared transaction diverge on any crash or failure. Derive the index from the DB commit (CDC/outbox)." },
    { label: "Add aggressive retries to the index call in the handler.", misconception: "Retries can't survive a crash between the two writes and risk double-indexing, while slowing the request you wanted fast. Derive the index from the commit log so committing the data guarantees indexing." },
    { label: "Wrap both writes in one database transaction.", misconception: "No transaction spans Postgres and Elasticsearch — separate systems, no shared commit. You need an outbox row committed atomically with the order, or CDC off the WAL." },
  ]}
/>

</Explanation>

<KeyTakeaway>Storage choice fits a workload to a store's guarantees: ACID for money, access-pattern-first when patterns are known, bytes in object storage, durability ≠ availability, specialize on a limit, dual writes from a commit.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-storage-choice-retrieval"
  lang="en"
  questions={[
    { q: "Why is 'eleven nines durable' not 'always available,' and what closes the gap?", a: "Durability (~11 nines) is the probability the bytes still exist and aren't corrupted — achieved by cross-disk/facility replication. Availability is the probability you can reach them right now, a much lower number (~3 nines), because a regional outage can make perfectly durable data unreachable without losing a byte. The gap is closed by cross-region replication plus a failover read path — a separate, billable choice." },
    { q: "How do you keep a search index consistent with the database without a dual write?", a: "Never best-effort two independent writes — they silently diverge on a crash or a failed second write. Make the database the single source of truth and derive the index from its commit: a transactional outbox (write an 'index this' row in the same transaction, a relay pushes it) or Change Data Capture (tail the WAL/binlog and stream committed changes). Either way the single DB commit is the event that guarantees indexing, and the index becomes an eventually-consistent derived projection rebuildable from the primary." },
  ]}
/>

<Recap lang="en">
The through-line is that storage choice is matching a workload to the guarantees a store actually provides. Money needs **ACID** atomicity and isolation (relational), not BASE — convergence "eventually" is a window you can't allow. **Access-pattern-first** NoSQL design is fast for planned patterns and brutal for unplanned ones, so a new query with no matching key degrades to a scan. Large bytes never go in the database (ballooning backups, poisoned buffer cache, WAL bloat, pool starvation) — keep **metadata in the DB, bytes in object storage**, served by **presigned URLs**. **Durability is not availability**: eleven nines means you won't lose bytes, not that a region can't go dark, which is what cross-region replication is for. A specialized store (time-series, search) earns its place on a **measured limit** and is treated as a **derived index**, with the **dual-write problem** solved by deriving that index from one authoritative commit via CDC or a transactional outbox — never two independent writes.
</Recap>
