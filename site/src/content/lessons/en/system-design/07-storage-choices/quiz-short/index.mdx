---
slug: "quiz-short"
lang: en
track: "system-design"
unit: "07-storage-choices"
order: 5
title: "Storage choices: free-recall review"
summary: "Free-recall prompts across the storage unit. Answer each in your own words first — the SQL/NoSQL trade, access-pattern design, the metadata/bytes split, durability vs availability, time-series and search internals, and the dual-write fix — then reveal and compare."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - acid-vs-base
  - access-pattern-design
  - metadata-bytes-split
  - durability-vs-availability
  - inverted-index
  - dual-write-consistency
prereqs:
  - 07-storage-choices/01-sql-vs-nosql
  - 07-storage-choices/02-blob-and-object
  - 07-storage-choices/03-time-series-and-search
sources:
  - https://aws.amazon.com/nosql/
  - https://docs.aws.amazon.com/AmazonS3/latest/userguide/Welcome.html
  - https://www.elastic.co/guide/en/elasticsearch/reference/current/documents-indices.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory before you open the model answer. The effort of reconstructing the SQL/NoSQL trade, the metadata/bytes split, and the dual-write fix is what makes them stick.
</Hook>

<Crux>Reconstruct the spine: SQL vs NoSQL, access-pattern design, bytes vs DB, durability vs availability, time-series and search, dual writes.</Crux>

<Explanation>

Reconstruct the unit's spine without looking back: when relational vs each NoSQL family wins, how access-pattern-first design cuts both ways, why blobs never live in the database, how durability differs from availability, what makes time-series and search stores specialized, and how to keep two stores in sync.

<RetrievalDrawer
  client:load
  id="07-storage-short-retrieval"
  lang="en"
  questions={[
    { q: "Name the four NoSQL families and the access shape each is built for, and the ACID/BASE trade.", a: "Document (MongoDB) — denormalized self-contained docs read as a whole, single lookup. Key-value (DynamoDB simple mode, Redis) — hash map, lookup by known id. Wide-column (Cassandra, Bigtable) — partition-keyed rows for huge write throughput with no single primary. Graph (Neptune, Neo4j) — nodes+edges so traversals are cheap. All scale out and are fast for a known pattern, trading relational's ad-hoc query flexibility. ACID (relational) enforces atomic/isolated invariants for every code path; BASE (many distributed NoSQL) is Basically Available, Soft state, Eventually consistent — CAP forces it: an available-during-partition store can't also be strongly consistent. Choose by whether the data tolerates staleness — a like count yes, a balance no." },
    { q: "Why does access-pattern-first design cut both ways, and what is polyglot persistence?", a: "Modeling queries first and shaping storage so each is one cheap pre-joined lookup is how NoSQL serves feeds at single-digit-ms latency to hundreds of millions of users — but it only works if patterns are known up front, and a new unplanned query degrades to a scan, a secondary index, or a migration. The 'avoid schemas' failure is related: schema-on-read just relocates enforcement to every reader. Polyglot persistence is the mature answer — multiple stores, each for what it's best at (relational for money, KV for sessions, wide-column for events, search for text, graph for edges) — accepting more operational surface and the cross-store dual-write problem for the right tool per workload." },
    { q: "Why never store blobs in the DB, and state the metadata/bytes split with presigned URLs.", a: "Blobs in the DB balloon backups (GB schema → TB backup), poison the buffer cache (image bytes evict hot index pages), bloat WAL/replication (each write ships megabytes), and starve the connection pool. The split: bytes in object storage under a key; a small metadata row (owner, key, content-type, size, status) in the relational DB that points at the object, so list/query/permission logic never touches a byte. Presigned URLs grant the client time-limited, signed access to one object so bytes flow client ↔ store directly (upload and download) and never through the stateless app tier, which therefore is never a bandwidth bottleneck." },
    { q: "Distinguish durability from availability, and explain lifecycle tiering.", a: "Durability = probability the bytes still exist/aren't corrupted (S3 ~11 nines via cross-disk/facility replication) — protects from data loss. Availability = probability you can reach them now (often ~3 nines) — a regional outage breaks reachability without losing a byte, so cross-region replication is a separate billable choice. Lifecycle tiering: object stores offer storage classes at different price/latency points (hot standard → infrequent access → cold archive); a lifecycle policy auto-migrates objects by age (30d → IA, 365d → archive, 7y → delete) so cost tracks how cold the data is. Archive retrieval is slow and metered, so set rules at design time." },
    { q: "What makes time-series and search stores specialized, and how do you fix the dual-write problem?", a: "Time-series: LSM-tree storage turns writes into fast sequential appends (vs in-place B-tree updates), plus downsampling (coarser old buckets) and retention (auto-drop aged data) to bound storage — matching append-only, write-heavy, time-windowed metrics. Search: an inverted index maps term → posting list of documents, so full-text queries intersect pre-built lists (work proportional to matching docs, not corpus) with stemming and relevance, where SQL LIKE with a leading wildcard forces a scan. Add a specialized store only on a measured limit and treat it as a derived index with the primary as source of truth. Fix dual writes by deriving the index from the DB commit via Change Data Capture or a transactional outbox — never two independent best-effort writes, which silently diverge on any crash." },
  ]}
/>

</Explanation>

<KeyTakeaway>Reconstruct each answer from memory — SQL/NoSQL and ACID/BASE, access-pattern design, the metadata/bytes split, durability vs availability, dual writes — and you hold the spine: fit a workload to its store's guarantees.</KeyTakeaway>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the unit's spine. **Relational** (ACID) is the default for invariants the engine must enforce; the four **NoSQL** families (document, key-value, wide-column, graph) scale out and are fast for a *known* access pattern, trading ad-hoc flexibility and, under **BASE/CAP**, strong consistency. **Access-pattern-first** design is powerful when patterns are known and brutal when they change, which is why mature systems go **polyglot**. Large **bytes never live in the database** — metadata in the DB, bytes in object storage, served by **presigned URLs** so the app tier is never a pipe — and **durability** (bytes exist, ~11 nines) is not **availability** (reachable now), with **lifecycle tiering** tracking cost to coldness. **Time-series** stores (LSM, downsampling, retention) and **search** engines (inverted index) are specialized for write shape and text, added on a *measured* limit and treated as **derived indexes** whose **dual-write** consistency is solved by deriving from one authoritative commit (CDC or outbox), never two independent writes. The thread: storage design is choosing which guarantees you need and which costs you can pay.
</Recap>
