---
slug: "project"
lang: en
track: "system-design"
unit: "07-storage-choices"
order: 7
title: "Storage choices: a polyglot media service"
summary: "Hands-on project: build a small media-sharing backend that puts each workload on the right store — relational metadata, object-storage bytes with presigned URLs, a search index — and then deliberately solve the dual-write consistency between the database and the search index."
estMin: 240
status: ready
lessonType: topic
level: senior
concepts:
  - polyglot-persistence
  - metadata-bytes-split
  - presigned-urls
  - inverted-index
  - dual-write-consistency
  - acid-vs-base
prereqs:
  - 07-storage-choices/01-sql-vs-nosql
  - 07-storage-choices/02-blob-and-object
  - 07-storage-choices/03-time-series-and-search
sources:
  - https://docs.aws.amazon.com/AmazonS3/latest/userguide/PresignedUrlUploadObject.html
  - https://docs.aws.amazon.com/AmazonS3/latest/userguide/Welcome.html
  - https://www.elastic.co/guide/en/elasticsearch/reference/current/documents-indices.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Reading that you should split metadata from bytes and derive a search index from the DB commit is not the same as building a system that actually does it — and stays consistent when a process crashes between two writes. Build a small media-sharing backend that puts each workload on the right store, serves bytes through presigned URLs, and then prove your search index can't silently drift from the database, even when you kill the relay mid-write.
</Hook>

<Crux>Build a polyglot media backend — DB metadata, object-storage bytes via presigned URLs, a derived search index — and prove it can't drift.</Crux>

<Explanation>

This project makes the whole unit operational: you'll place each workload on the store whose guarantees fit it, keep large bytes off the database, and then face the dual-write problem head-on by deriving the search index from the database's commit and proving — by deliberately crashing — that it converges.

<ProjectBrief
  id="07-storage-project"
  lessonSlug="project"
  lang="en"
  objective="Build a small media-sharing backend (any language) that stores file metadata in a relational database, file bytes in object storage served via presigned URLs, and a full-text search index over titles/captions — then make the search index a derived, eventually-consistent projection of the database that provably cannot silently diverge from it."
  requirements={[
    "Model the relational schema for media items: id, owner_id, title, caption, content_type, size_bytes, object_key, status, created_at. Store NO file bytes in the database — only the object_key that points at them. Justify each column staying in the relational store (queryable, joinable, permission-checked).",
    "Implement a direct-to-object-store upload: an endpoint that authorizes the user, writes a metadata row with status='pending', and returns a short-lived presigned PUT URL scoped to one object key. The client uploads bytes directly to the store; no byte streams through the app server.",
    "Implement upload confirmation: on completion (a store event or a client callback), flip the row to status='complete'. Add a sweeper that deletes rows stuck 'pending' past a timeout plus any orphaned objects, reconciling the metadata↔bytes gap.",
    "Serve downloads via presigned GET URLs, enforcing per-user access at signing time; verify (e.g. with logs or metrics) that file bytes never pass through the app tier.",
    "Add full-text search over title + caption using a search engine (Elasticsearch/OpenSearch) or Postgres tsvector/GIN. Confirm a leading-wildcard LIKE would full-scan, and that your inverted-index query does not.",
    "Make the search index a DERIVED projection of the database, NOT a second source of truth: feed it from the DB commit via a transactional outbox (an 'index this item' row written in the same transaction as the media row, drained by a relay) or Change Data Capture.",
    "Write a consistency test: kill the relay (or the process) between the DB commit and the index push, then restart it, and show the index converges to match the database with no manually lost items.",
    "Document the store-by-store decision: for each workload (metadata, bytes, search) state the store chosen and the specific property that justified it, and name what consistency model each piece offers (ACID for the metadata, eventual for the derived index).",
  ]}
  acceptance={[
    "A relational schema holding only metadata (no file bytes), with each column justified and an object_key column pointing at the bytes in object storage.",
    "A working upload flow where the client PUTs bytes directly to the object store via a presigned URL, the app writes pending→complete metadata, and a sweeper reconciles failed uploads — demonstrated end to end with a multi-megabyte file that never streams through the app tier.",
    "A full-text search endpoint backed by an inverted index, with a short note showing why the equivalent LIKE query would full-scan and the inverted-index query does not.",
    "The search index fed from the DB commit (transactional outbox or CDC), with the dual write removed from request handlers — no best-effort 'index and ignore failures' anywhere.",
    "A reproducible consistency test that crashes the relay/process mid-write and shows the index converges to the database after restart, plus a one-paragraph explanation of why it can't silently drift.",
    "A short write-up mapping each workload to its store and consistency model (ACID metadata, eventually-consistent derived search), stated at composition altitude.",
  ]}
  stretch={[
    "Add lifecycle tiering: a policy (real or simulated) that moves objects to an infrequent-access/archive class by age, and measure the storage-cost change versus keeping everything hot.",
    "Add durability-vs-availability handling: configure (or simulate) cross-region replication of the object store and describe the failover read path that keeps bytes reachable during a regional outage.",
    "Add a second derived store: a time-series store for per-item view counts/metrics, fed the same way (from the commit log), and show downsampling + retention bounding its storage.",
    "Add a graph or recommendation query ('items also liked by people who liked this') and discuss which store family would serve it and why a relational self-join would not scale — staying at composition altitude.",
  ]}
/>

</Explanation>

<KeyTakeaway>A polyglot media backend, end to end: metadata in the DB, bytes in object storage via presigned URLs, search in a derived index fed from the DB commit — removing the dual write and, by crashing the relay, proving it.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-storage-project-retrieval"
  lang="en"
  questions={[
    { q: "Why split metadata from bytes, and how does the upload flow keep bytes off the app tier?", a: "Bytes in the database balloon backups, poison the buffer cache, bloat WAL/replication, and starve the connection pool — so bytes go in object storage under a key, and a small metadata row (owner, title, content_type, size, object_key, status) lives in the relational DB where it's queryable, joinable, and permission-checked. The upload flow keeps bytes off the app tier with a presigned PUT URL: the app authorizes the request, writes a pending metadata row, and returns a short-lived signed URL scoped to one key; the client uploads directly to the store, then a confirm event flips the row to complete and a sweeper reconciles failures. No byte streams through an app server." },
    { q: "How do you make the search index consistent with the database without a dual write, and how do you prove it?", a: "Make the database the single source of truth and derive the index from its commit — never a best-effort second write in the handler. A transactional outbox writes an 'index this item' row in the SAME transaction as the media row (atomic commit), and a relay drains the outbox to the search engine, retrying until success; alternatively Change Data Capture tails the WAL/binlog. You prove it with a crash test: kill the relay or process between the DB commit and the index push, restart, and show the index converges to match the database with no manually lost items — because the durable outbox row (or the WAL) still carries the pending change." },
    { q: "What consistency model does each piece of this system offer, and why is that the right composition?", a: "The metadata in the relational store is ACID — strong, transactional, the source of truth for what exists and who owns it, which is right for permissions and the pending→complete invariant. The file bytes in object storage are highly durable (and you add cross-region replication for availability). The search index is an eventually-consistent derived projection — a new item is searchable a moment after commit, which is acceptable because search tolerates a brief lag and the index can always be rebuilt from the primary. Composing strong consistency where it's required and eventual consistency where it's tolerable is exactly the polyglot tradeoff, paid for with operational surface and the deliberate dual-write handling." },
  ]}
/>

<Recap lang="en">
This project turns the storage unit into a working system. You place each workload on the store whose guarantees fit it: **metadata in a relational database** (ACID, queryable, the source of truth for what exists and who owns it), **bytes in object storage** under a key (never in the DB), served by **presigned URLs** so the client uploads and downloads directly and the stateless app tier never proxies a byte — with a pending→complete status and a sweeper reconciling the metadata↔bytes gap. Search runs over a **derived inverted index** (a search engine or `tsvector`), confirmed to avoid the full scan a leading-wildcard `LIKE` would force. The heart of the project is the **dual-write problem**: you remove the best-effort index write from request handlers and instead derive the index from the database's commit via a **transactional outbox** or **CDC**, then *prove* convergence by deliberately crashing the relay mid-write and watching the index catch up after restart. The deliverable is a system that composes **strong consistency where required and eventual consistency where tolerable** — the polyglot tradeoff — and an engineer who has built it once reaches for the right store per workload and never trusts two independent writes to stay in sync.
</Recap>
