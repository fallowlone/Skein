---
slug: "01-sql-vs-nosql"
lang: en
track: "system-design"
unit: "07-storage-choices"
order: 1
title: "SQL vs NoSQL"
summary: "Relational stores enforce relationships with ACID and a schema; NoSQL families (document, key-value, wide-column, graph) trade joins and strong consistency for an access-pattern-first layout that scales out. The senior call is which costs you can pay, not which is modern."
estMin: 18
status: ready
lessonType: topic
level: middle
concepts:
  - relational-model
  - document-store
  - wide-column
  - acid-vs-base
  - access-pattern-design
  - polyglot-persistence
prereqs:
  - 04-data-distribution/02-sharding-and-partitioning
  - 04-data-distribution/04-cap-and-pacelc
sources:
  - https://aws.amazon.com/nosql/
  - https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/SQLtoNoSQL.html
  - https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/bp-modeling-nosql-B.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Inset from "~/components/lesson/Inset.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import nosqlFamilies from "./infographics/nosql-families.svg?raw";

<Hook>
A team launched a social product on a document database with one stated reason: "we don't want to deal with migrations." For a year it was bliss — ship a feature, add a field, no `ALTER TABLE`. Then product asked a question the launch never anticipated: *which users follow each other, and what's trending among their follows?* That's a graph traversal and an aggregation across documents, and the store could do neither without dragging half the dataset into the application and joining it by hand. The schema they "avoided" hadn't disappeared — it had moved out of the database and into a thousand fragile code paths that each had to remember the shape. They didn't pick a database; they deferred a decision, and the bill came due with interest.
</Hook>

<Crux>Relational enforces ACID; NoSQL trades joins and strong consistency for access-pattern-first scale-out. Pick by cost, not fashion.</Crux>

<Explanation>

## The relational default and what it buys

By the end of this lesson you'll know exactly which database property makes that deferred decision a lawsuit risk — and which one you can trade away safely.

A **relational database** (Postgres, MySQL) stores data as tables of typed rows, and the relationships between them are first-class: you **normalize** to remove duplication, then reassemble with **joins** at query time. The engine enforces a **schema** (columns, types, constraints) and gives you **ACID** transactions — atomic all-or-nothing writes, consistency against your constraints, isolation between concurrent transactions, durable commits. This is not legacy baggage; it is a contract. The database guarantees that a transfer debiting one account and crediting another either fully happens or fully doesn't, that a foreign key never dangles, that two concurrent bookings can't both grab the last seat. That contract is why relational is the correct *default* for anything with money, inventory, or relationships you care about — you write the invariant once and the engine holds it for every code path forever.

The cost is that the relational model assumes you'll ask *flexible, ad-hoc* questions — `JOIN` whatever you need at read time — and that flexibility has a price at scale: joins across a sharded dataset are expensive (the rows live on different machines), and a single primary handling all writes has a ceiling (the lesson from **04-data-distribution/02-sharding-and-partitioning**). Relational is excellent until your write volume or your data size pushes past what one primary plus read replicas can hold.

## The NoSQL families: four different bets

Before you reach for a NoSQL store, ask yourself: what is the *specific access shape* that makes the relational engine the wrong tool here? The answer determines which family you need — and they are not interchangeable.

"NoSQL" is not one thing — it's four families, each optimized for a different access shape:

- **Document** (MongoDB, DocumentDB) — stores self-contained JSON-ish documents. You **denormalize**: embed related data inside one document so a read is a single lookup, no join. Great when an entity is read as a whole (a product page, a user profile).
- **Key-value** (DynamoDB in its simplest mode, Redis) — a giant hash map: give a key, get a blob. The simplest, fastest model; no querying *inside* the value. Great for sessions, carts, lookups by a known id.
- **Wide-column** (Cassandra, Bigtable) — rows keyed by a partition key, with sparse, flexible columns; built to absorb enormous write throughput across a cluster with no single primary. Great for time-stamped event firehoses and feeds.
- **Graph** (Neptune, Neo4j) — nodes and edges as first-class citizens, so traversals ("friends of friends who like X") are cheap. Great for social graphs, fraud rings, recommendation paths — exactly the question the hook's team couldn't answer.

When you see this list, notice what all four share: each one wins on a specific, known read shape and loses whenever you ask a question it wasn't designed for.

<Infographic
  svg={nosqlFamilies}
  label="A mind map rooted at NoSQL branching into its four families — Document (MongoDB, read a whole denormalized doc in one lookup), Key-value (Redis/DynamoDB, a hash map keyed by id), Wide-column (Cassandra/Bigtable, huge write throughput with no single primary), and Graph (Neptune/Neo4j, cheap traversals) — each labeled with the one access shape it is built for."
  caption="NoSQL is not one thing: four distinct bets, each fast on its own known access shape and weak on the questions it wasn't designed for."
  hue="var(--d-infra)"
/>

The unifying theme: NoSQL stores are built to **scale out** on commodity clusters and to be **fast for a known access pattern**, by giving up the relational engine's ability to answer questions you didn't plan for. AWS frames the family the same way — purpose-built engines, flexible schemas, horizontal scale — in exchange for relaxing some of the consistency guarantees a relational engine enforces.

## ACID vs BASE: the consistency you're trading

The deepest difference isn't the data shape — it's the consistency contract. Relational systems aim for **ACID**. Many distributed NoSQL stores instead offer **BASE**: **B**asically **A**vailable, **S**oft state, **E**ventually consistent. Under BASE, a write may not be visible to every reader immediately; replicas converge "eventually" (usually within milliseconds). This is not sloppiness — it's the direct consequence of **CAP/PACELC** (the lesson from **04-data-distribution/04-cap-and-pacelc**): a store that stays available during a network partition *cannot* also be strongly consistent. NoSQL stores that prioritize availability and partition tolerance choose eventual consistency on purpose, and many (DynamoDB, Cassandra) let you opt into stronger consistency per request at a latency and cost premium.

So the SQL-vs-NoSQL question is really: *can your domain tolerate eventual consistency on this data?* A "like" count that's stale for 200 ms is fine. An account balance that's stale for 200 ms during a withdrawal is a lawsuit.

<Inset kind="why" lang="en">
Why does "we picked Mongo to avoid schemas" so reliably backfire? Because schema-on-write (relational) and schema-on-read (document) don't remove the schema — they relocate *who enforces it*. With schema-on-write, the database rejects malformed data at the door, once. With schema-on-read, every reader must defensively handle every historical shape the data has ever taken: the document written in v1 with no `country` field, the v2 one where `country` was a string, the v3 one where it became an object. Five years and forty deploys later, your "schema-less" collection has a dozen implicit shapes, and the validation logic is smeared across the codebase instead of declared in one place. Flexibility at write time is a loan against read time, and the interest is paid by every engineer who touches the data afterward.
</Inset>

## Access-pattern-first design: the real NoSQL skill

Using NoSQL well is not "relational without joins" — it's an inverted design process. In relational modeling you design the *data* (normalize into clean entities), then write whatever queries you need. In NoSQL modeling you design the *queries first*: list every access pattern the application will have, then shape the storage so each one is a single, cheap lookup. AWS's DynamoDB guidance is explicit about this — you model the table around access patterns, often collapsing many entity types into one table with carefully chosen partition and sort keys, so that a query returns exactly what one screen needs, pre-joined.

This is genuinely powerful — it's how a store serves a personalized feed to hundreds of millions of users at single-digit-millisecond latency. But it has a hard edge: it only works if you *know the access patterns up front*, and it punishes you brutally when a new one appears. A new query that doesn't match your key design forces either an expensive full scan, a secondary index (more cost, more eventual consistency), or a data migration. Relational's flexibility is exactly what NoSQL trades away — which is why NoSQL shines for well-understood, high-scale patterns and bites hard for evolving, exploratory products.

<FlowDiagram
  label="The same online-store data modeled two ways. Relational normalizes into separate Users, Orders, and Items tables joined at read time; the document model denormalizes the whole order — a customer snapshot and line items — into one self-contained document fetched in a single read."
  perRow={3}
  hue="var(--d-data)"
  nodes={[
    { id: "rel", label: "RELATIONAL", sub: "normalized + join at read" },
    { id: "users", label: "users table", sub: "id, name, email" },
    { id: "orders", label: "orders table", sub: "FK → user, FK → items" },
    { id: "doc", label: "DOCUMENT", sub: "denormalized, 1 read" },
    { id: "orderdoc", label: "order document", sub: "embeds user + line items" },
    { id: "read", label: "1 lookup", sub: "no join, pre-shaped" },
  ]}
  edges={[
    { from: "rel", to: "users" },
    { from: "rel", to: "orders" },
    { from: "users", to: "orders", label: "JOIN" },
    { from: "doc", to: "orderdoc" },
    { from: "orderdoc", to: "read" },
  ]}
  caption="Relational trades read-time work (the join) for flexibility and no duplication; document trades duplication and update-everywhere cost for a single fast read. Neither is free — you're choosing which cost to pay, and on which side of the read/write split."
/>

## Polyglot persistence: stop picking one

The mature answer to "SQL or NoSQL?" is usually *both*. **Polyglot persistence** means using more than one store in a single system, each for what it's best at: the relational primary for orders and money (ACID), a key-value cache for sessions, a wide-column store for the event firehose, a search engine for full-text, a graph store for the social edges. The cost is operational — more systems to run, monitor, and keep in sync — and the dangerous part is keeping data consistent *across* stores (the dual-write problem, covered in **03-time-series-and-search**). But the alternative — forcing every workload through one engine — guarantees you're using the wrong tool for at least half of them. Senior design is about composing stores, not crowning one.

<TradeoffMatrix
  id="01-sql-vs-nosql-tradeoff"
  lessonSlug="01-sql-vs-nosql"
  lang="en"
  prompt="A fintech startup is building a ledger that records every debit and credit between accounts. The schema is fixed and well-understood. They need atomic multi-row transactions (debit A, credit B) and strong consistency on every read. Which storage choice fits?"
  options={[
    { name: "Relational database (Postgres)", summary: "ACID transactions enforce the debit-credit invariant in the engine, not in application code.", justification: "A relational database gives you atomic multi-row transactions natively: the debit and credit either both commit or both roll back, and no concurrent reader ever sees a partial state. The schema is a first-class constraint — a dangling foreign key or a negative balance violates a CHECK constraint before the write lands. Strong consistency is the default: every read sees the latest committed write. For a fixed, well-understood schema with correctness invariants that must hold for every code path, relational is exactly the right tool — the engine enforces the contract once so no future developer can accidentally bypass it.", correct: true },
    { name: "Wide-column store (Cassandra)", summary: "Huge write throughput, but multi-row atomic transactions and strong consistency require extra care.", justification: "Cassandra is optimized for enormous write throughput across a commodity cluster with no single primary, making it the right choice for event firehoses, time-series, and append-heavy feeds. However, atomicity across multiple partition keys (debit account A on one partition, credit account B on another) requires lightweight transactions (LWT) or application-level saga patterns — both are complex and expensive. Cassandra's default consistency is eventual; strongly-consistent reads require quorum flags that add latency. This is the wrong primary store for a ledger where correctness is the top concern." },
    { name: "Document store (MongoDB)", summary: "Flexible schema and single-document atomicity — but cross-document transactions are possible yet complex.", justification: "MongoDB added multi-document transactions in v4.0, so it can express debit-credit atomicity. However, the document model's strength is reading a self-contained entity in one lookup, not enforcing referential integrity across entities. Schema enforcement is opt-in (JSON Schema validators) rather than engine-enforced by default, meaning a deploy that accidentally omits a field succeeds silently. For a fixed schema with hard correctness invariants, the relational model's write-time enforcement is simpler and harder to accidentally bypass than document validation layers." },
    { name: "Key-value store (Redis)", summary: "Sub-millisecond latency for lookups — but no native multi-key atomic transactions and in-memory durability limits.", justification: "Redis is a hash map: get a key, get a blob, with sub-millisecond latency. MULTI/EXEC gives optimistic multi-key atomicity, but Redis is primarily an in-memory store — durability depends on fsync tuning and AOF/RDB snapshots, which introduce recovery windows where recent data can be lost. For a ledger where every debit and credit must survive a crash, a disk-first ACID database is the correct foundation; Redis is the right layer for sessions, caches, and leaderboards where some data loss is acceptable." },
  ]}
/>

<Inset kind="mistake" lang="en">
The most expensive SQL-vs-NoSQL mistake is choosing on *scale you don't have yet*. A team reads that Cassandra serves millions of writes per second and reaches for it on day one — when they have 50 writes per second and no idea what their access patterns are. They've bought the operational burden and the access-pattern rigidity of a horizontally-scaled store to solve a problem they won't have for years, while giving up the joins, transactions, and query flexibility that an early-stage product needs *most* (its requirements change weekly). The honest default is a single relational database until a *measured* bottleneck — a real write ceiling, a real latency wall — names the specific NoSQL store that solves it. Premature NoSQL is premature optimization wearing a buzzword.
</Inset>

<Quiz
  id="07-sql-vs-nosql-quiz-1"
  lessonSlug="01-sql-vs-nosql"
  lang="en"
  question="A payments feature must guarantee that debiting one account and crediting another either both happen or neither does, with no intermediate state visible to other transactions. Which store property is non-negotiable here?"
  choices={[
    { label: "ACID transactions (atomicity + isolation) — typically a relational store, because the all-or-nothing multi-row invariant must be enforced by the engine, not hoped for in application code.", correct: true },
    { label: "Eventual consistency (BASE) — it's faster and the balances will converge.", misconception: "Convergence 'eventually' is exactly what you cannot tolerate for money: a window where one account is debited but the other isn't, visible to a concurrent read, is a real bug with legal consequences. This invariant needs ACID atomicity + isolation." },
    { label: "Horizontal write scale — pick a wide-column store so it never bottlenecks.", misconception: "Write scale doesn't address the requirement, which is a correctness invariant across two rows. A wide-column store optimized for write throughput typically relaxes the multi-row transactional guarantee you need here." },
    { label: "A flexible schema so you can add fields without migrations.", misconception: "Schema flexibility is irrelevant to the requirement. The non-negotiable property is atomic, isolated multi-row transactions — an ACID guarantee, not a schema one." },
  ]}
/>

<Quiz
  id="07-sql-vs-nosql-quiz-2"
  lessonSlug="01-sql-vs-nosql"
  lang="en"
  question="A team chose a document database 'to avoid schemas.' Two years in, every read path is littered with checks for missing and differently-typed fields. What actually happened?"
  choices={[
    { label: "The schema didn't disappear — it moved from schema-on-write (enforced once by the DB) to schema-on-read (re-enforced defensively by every reader), so validation is now smeared across the codebase.", correct: true },
    { label: "They picked the wrong document database; a different one would have stayed clean.", misconception: "No document store removes the schema problem — it's inherent to schema-on-read. Without write-time enforcement, accumulated historical shapes must be handled at read time regardless of vendor." },
    { label: "Document databases simply can't store consistent data.", misconception: "They can; many offer optional schema validation. The issue here is that the team opted out of write-time enforcement, so the implicit schema drifted and every reader pays for it. The schema relocated, it didn't vanish." },
    { label: "The team should have denormalized harder.", misconception: "Denormalization is about read shape, not about enforcing field presence or types. More embedding wouldn't fix drifting field shapes; write-time validation (or a relational schema) would." },
  ]}
/>

<MetaphorComplete
  id="07-sql-vs-nosql-metaphor-1"
  lessonSlug="01-sql-vs-nosql"
  lang="en"
  setup="In relational modeling you design the data first and query freely; in NoSQL modeling you invert it — you design the _______ first, then shape the storage so each one is a single cheap lookup, which is why a new, unplanned one is expensive."
  accepted={["access patterns", "access pattern", "queries", "access-patterns", "query"]}
  canonical="access patterns"
  explanation="NoSQL modeling is access-pattern-first: enumerate every query the app will make, then choose partition/sort keys so each is one cheap lookup with the data pre-joined. The power (single-digit-ms reads at scale) comes from knowing the patterns up front; the cost is that a new pattern not matching the key design forces a scan, a secondary index, or a migration."
/>

</Explanation>

<KeyTakeaway>Relational is the default: ACID + schema enforce invariants for every code path. NoSQL trades joins and strong consistency for access-pattern scale-out — great for known patterns, brutal when they change. Go polyglot.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-sql-vs-nosql-retrieval"
  lang="en"
  questions={[
    { q: "Name the four NoSQL families and the access shape each is built for.", a: "Document (MongoDB) — self-contained denormalized documents read as a whole, single lookup, no join. Key-value (DynamoDB simple mode, Redis) — a hash map, lookup by known id, fastest, no querying inside the value. Wide-column (Cassandra, Bigtable) — partition-keyed rows built to absorb huge write throughput across a cluster with no single primary; ideal for event firehoses and feeds. Graph (Neptune, Neo4j) — nodes and edges as first-class citizens so traversals (friends-of-friends) are cheap. All four scale out and are fast for a known pattern, at the cost of relational's ad-hoc query flexibility." },
    { q: "What is the ACID-vs-BASE trade, and how does CAP force it?", a: "ACID (relational) gives atomic, consistent, isolated, durable transactions — the engine enforces invariants for every code path. BASE (many distributed NoSQL) is Basically Available, Soft state, Eventually consistent: a write isn't instantly visible everywhere; replicas converge after a short delay. CAP/PACELC forces the choice: a store that stays available during a network partition cannot also be strongly consistent, so availability-first stores choose eventual consistency (often opt-in stronger consistency per request at a cost). The design question is whether the data tolerates staleness — a like count yes, an account balance no." },
    { q: "Why does access-pattern-first design cut both ways, and what is polyglot persistence?", a: "Inverting the design (model queries first, shape storage so each is one cheap pre-joined lookup) is how NoSQL serves feeds at single-digit-ms latency to hundreds of millions of users — but it only works if patterns are known up front and punishes a new, unplanned query with a scan, a secondary index, or a migration. Polyglot persistence is the mature answer: use multiple stores, each for what it's best at (relational for money, KV for sessions, wide-column for events, search for text, graph for edges), accepting more operational surface and the cross-store dual-write consistency problem in exchange for using the right tool per workload." },
  ]}
/>

<Recap lang="en">
**Relational** is the correct default: a schema and **ACID** transactions let you declare an invariant once and have the engine enforce it for every code path forever — essential for money, inventory, and relationships you care about. Its cost is that joins across a sharded dataset are expensive and a single write primary has a ceiling. **NoSQL** is four distinct bets — **document** (denormalized, read-as-a-whole), **key-value** (hash map, lookup by id), **wide-column** (huge write throughput, no single primary), and **graph** (cheap traversals) — all built to **scale out** and be fast for a *known* access pattern by trading away ad-hoc query flexibility and, usually, strong consistency (**BASE**, forced by CAP). The "we picked Mongo to avoid schemas" failure is the tell: the schema doesn't vanish, it moves from one enforced write-time check to defensive re-validation in every reader. NoSQL's real skill is **access-pattern-first** design — powerful at known scale, brutal when patterns change. The senior answer is rarely one store: **polyglot persistence** composes the right engine per workload, paying for it in operational surface and the cross-store dual-write problem — chosen by *measured* cost, not by which database is in fashion. Now when you face a "SQL or NoSQL?" question in a design review, your first move is to name the specific access pattern and consistency tolerance — and let that answer the question for you.
</Recap>
