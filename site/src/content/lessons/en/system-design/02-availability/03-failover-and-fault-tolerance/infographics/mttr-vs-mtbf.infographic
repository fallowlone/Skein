infographic chart-column-simple
data
  title Annual downtime: raise MTBF vs lower MTTR
  desc Same baseline (MTBF 1000h, MTTR 10h). Recovering 10x faster beats failing half as often.
  values
    - label Baseline
      value 86.7
    - label Fail half as often (MTBF 2x)
      value 43.6
    - label Recover 10x faster (MTTR /10)
      value 8.8
theme
  palette #f43f5e #f59e0b #14b8a6
