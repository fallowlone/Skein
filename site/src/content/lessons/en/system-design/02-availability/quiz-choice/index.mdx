---
slug: "quiz-choice"
lang: en
track: "system-design"
unit: "02-availability"
order: 4
title: "Availability: multiple-choice review"
summary: "Multiple-choice synthesis across the availability unit: SLI/SLO/SLA and the nines, error budgets, redundancy sizing and correlated failure, and the failover/split-brain/retry-storm traps that turn recovery into outage."
estMin: 13
status: ready
lessonType: topic
level: senior
concepts:
  - sli-slo-sla
  - error-budget
  - redundancy
  - correlated-failure
  - split-brain
  - mttr-mtbf
prereqs:
  - 02-availability/01-sla-slo-sli
  - 02-availability/02-redundancy-and-spof
  - 02-availability/03-failover-and-fault-tolerance
sources:
  - https://sre.google/sre-book/service-level-objectives/
  - https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/
  - https://aws.amazon.com/builders-library/static-stability-using-availability-zones/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each is a judgement you make when you write an SLO, size redundancy, review a failover design, or read a postmortem — not a definition to recite, but the reasoning behind where to set a target, what counts as redundant, and why a failover turned into an outage.
</Hook>

<Crux>Six decisions across the unit: an SLO target, the nines math, an error budget, a redundancy sizing, a correlated failure, a split-brain.</Crux>

<Explanation>

Confirm you can place an SLO under an SLA, do the nines and availability-composition arithmetic, spend an error budget, size redundancy to N+1 against peak, spot a correlated failure behind "redundant" copies, and recognize split-brain and the retry storm.

<Quiz
  id="02-availability-choice-1"
  lessonSlug="quiz-choice"
  lang="en"
  question="A customer SLA promises 99.9% monthly availability with credits for misses. Where do you set the internal SLO, and why?"
  choices={[
    { label: "Tighter than the SLA (e.g. 99.95%) so an internal breach alerts you with room to react before you breach the contract and owe credits — the SLO is the smoke detector, the SLA is the fire.", correct: true },
    { label: "Exactly at the SLA's 99.9%, so the two numbers agree.", misconception: "If the SLO equals the SLA, you only learn you're in trouble once the contract is already breached and money is owed. The SLO must sit tighter to function as an early warning." },
    { label: "Looser than the SLA (e.g. 99.5%) so it's easier to meet internally.", misconception: "Backwards: a looser SLO would routinely let you breach the SLA while the internal dashboard still looks green. The SLO has to trip BEFORE the contract, so it's set tighter." },
    { label: "There's no need for a separate SLO once an SLA exists.", misconception: "The SLA has no early warning; the SLO is the internal target whose breach alerts you in time to act. Without it you discover the miss at payout time." },
  ]}
/>

<Quiz
  id="02-availability-choice-2"
  lessonSlug="quiz-choice"
  lang="en"
  question="Your SLO is 99.99% successful requests over 28 days. Roughly how much downtime/failure does that allow per month, and what does the leftover represent?"
  choices={[
    { label: "≈4.3 minutes per month; the 0.01% you're allowed to fail is your error budget — a resource you may spend on risky launches while it lasts and that freezes risk when it's gone.", correct: true },
    { label: "≈43 minutes per month, and the leftover is the SLA.", misconception: "43 minutes is three nines (99.9%), not four. Four nines (99.99%) is ~4.3 min/month. And the leftover (100% − SLO) is the error budget, not the SLA — the SLA is the external contract." },
    { label: "≈26 seconds per month, with no spendable budget.", misconception: "26 seconds is five nines (99.999%). Four nines is ~4.3 min/month, and the 0.01% allowed to fail IS a spendable error budget — that's the whole point of expressing the SLO this way." },
    { label: "Zero — 99.99% means essentially never failing.", misconception: "99.99% explicitly allows 0.01% to fail — ~4.3 min/month. Treating it as 'never fail' discards the error budget, the resource that lets you ship features without breaching the objective." },
  ]}
/>

<Quiz
  id="02-availability-choice-3"
  lessonSlug="quiz-choice"
  lang="en"
  question="A request path is four independent serial components, each at 99.9% availability. What's the path's availability, and the design lesson?"
  choices={[
    { label: "≈99.6% (0.999⁴) — availability multiplies across serial dependencies, so the chain is worse than any link; to hit four nines end to end each link must beat four nines or be made redundant.", correct: true },
    { label: "99.9% — the path inherits its components' availability.", misconception: "Availability composes by multiplication, not by copying one component's number. Four serial 99.9% links give 0.999⁴ ≈ 99.6%, lower than any single link." },
    { label: "≈99.99% — more components add reliability.", misconception: "Adding serial dependencies LOWERS availability — each is another thing that can fail the request. 0.999⁴ ≈ 99.6%, not 99.99%." },
    { label: "Cannot be computed without latency numbers.", misconception: "Serial-availability composition needs only each link's availability: multiply them, 0.999⁴ ≈ 99.6%. Latency is a separate axis." },
  ]}
/>

<Quiz
  id="02-availability-choice-4"
  lessonSlug="quiz-choice"
  lang="en"
  question="A service needs 4 servers to carry peak and runs exactly 4, calling itself redundant. One dies at peak. What's the verdict?"
  choices={[
    { label: "That's N, not N+1 — three servers can't carry a four-server peak, so the survivors overload and can collapse under the queueing curve. True redundancy needs a spare (N+1) so any subset still meets peak.", correct: true },
    { label: "Fine — the load balancer redistributes load to the three survivors.", misconception: "Redistribution doesn't create capacity. Peak needs 4; with 3 the survivors are overloaded and tip toward collapse. You needed N+1 (a 5th) to tolerate one loss." },
    { label: "Fine — running more than one server is what redundant means.", misconception: "Redundancy means surviving a loss while still meeting peak, which requires spare capacity (N+1). Exactly N puts you under capacity on the first failure." },
    { label: "Only latency is affected; capacity is unchanged.", misconception: "Capacity is exactly what's hit — 25% of it gone at peak — and the survivors can't absorb it. The latency spike is the symptom of the resulting queueing collapse." },
  ]}
/>

<Quiz
  id="02-availability-choice-5"
  lessonSlug="quiz-choice"
  lang="en"
  question="Two database replicas are run 'for redundancy,' but both sit in the same availability zone. The AZ loses power. What does this expose?"
  choices={[
    { label: "Correlated failure: the replicas share a failure domain (the AZ), so one event takes both — they were never truly redundant. Real redundancy needs copies in independently-failing domains (separate AZs/regions).", correct: true },
    { label: "Nothing — two replicas is redundant by definition; this was bad luck.", misconception: "Two copies in one failure domain aren't redundant; a single event kills both. Independence is the whole point, and same-AZ placement correlates the failures." },
    { label: "Independence-based math still holds: two 99.9% replicas → ~99.9999%.", misconception: "That multiplication assumes INDEPENDENT failures. A shared AZ correlates them, so real availability is governed by the AZ, not the product of the replicas." },
    { label: "Add a third replica in the same AZ.", misconception: "More copies inside the same shared dependency don't help — the AZ event still takes all of them. Spread across independent failure domains instead." },
  ]}
/>

<Quiz
  id="02-availability-choice-6"
  lessonSlug="quiz-choice"
  lang="en"
  question="A network partition isolates a primary from its standby; the standby promotes itself while the primary is still alive and taking writes. What is this and what prevents it?"
  choices={[
    { label: "Split-brain — two primaries take writes and diverge with no automatic merge. Prevent it with quorum (only the majority may promote) or fencing (forcibly cut off the old primary first) so a wrong 'dead' call is harmless.", correct: true },
    { label: "Normal failover; the data reconciles automatically afterward.", misconception: "It's split-brain, and there's no automatic reconcile — two conflicting write histories. The failover misfired because it couldn't tell 'dead' from 'unreachable.' Quorum/fencing prevents it." },
    { label: "Fixable purely by setting a longer detection timeout.", misconception: "A longer timeout trades risks — fewer false positives but slower response to a REAL death — and never resolves the dead-vs-unreachable ambiguity. The durable fix is quorum or fencing." },
    { label: "A throughput problem solved by a bigger standby.", misconception: "It's a correctness problem, not capacity: two nodes both believe they're primary. A bigger box doesn't help; ensuring only one can win (quorum/fencing) does." },
  ]}
/>

</Explanation>

<KeyTakeaway>Availability is decisions made before the incident: SLO under SLA, the nines and multiplied-availability math, a spendable error budget, N+1 redundancy in independent domains, and failover that survives wrong detection.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-availability-choice-retrieval"
  lang="en"
  questions={[
    { q: "Why does 'two replicas, both in one AZ' fail the redundancy test?", a: "Because redundancy requires INDEPENDENT failure. Two copies in the same availability zone share a failure domain — one power/cooling/network event takes both at once (correlated failure), so the real availability is the AZ's, not the product of the replicas'. Independence-based math (two 99.9% → 99.9999%) only holds when failures are uncorrelated, which is why you spread replicas across AZs or regions engineered to fail independently." },
    { q: "What single design move prevents a split-brain during a network partition?", a: "Ensure at most one node can win. Quorum/consensus requires a majority to agree before promoting, so a partitioned old primary in the minority can't keep acting as primary (hence odd node counts). Or fencing (STONITH) forcibly cuts off the old primary — revoking its storage lease, killing it, or blocking it at the network — before promoting the standby. Both make a wrong 'the primary is dead' call harmless, which is the only durable fix because detection can't distinguish dead from unreachable." },
  ]}
/>

<Recap lang="en">
The through-line of the unit is that availability is reasoning you do before the incident. You set the **SLO under the SLA** so an internal breach warns you before you owe credits; you do the **nines** arithmetic (99.9% ≈ 43 min/month, 99.99% ≈ 4.3 min, 99.999% ≈ 26 s) and remember availability **multiplies** across serial dependencies, so a four-link 99.9% path is only ~99.6%. The **error budget** (100% − SLO) turns reliability into a spendable resource. You size **redundancy to N+1 against peak**, not average, and you place the copies in **independent failure domains** because two replicas sharing an AZ is **correlated failure**, not redundancy. And you design **failover** for a wrong detection: quorum or fencing against **split-brain**, bounded timeouts and capped retries with backoff-and-jitter against the **retry storm**, so recovery doesn't become the next outage — with **availability ≈ MTBF/(MTBF+MTTR)** reminding you that fast, rehearsed recovery (MTTR) usually beats chasing a higher MTBF.
</Recap>
