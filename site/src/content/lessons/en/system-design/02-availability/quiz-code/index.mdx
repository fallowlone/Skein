---
slug: "quiz-code"
lang: en
track: "system-design"
unit: "02-availability"
order: 6
title: "Availability: numbers and config reading"
summary: "Read real availability math, config, and code, then do the arithmetic: an error-budget calculation, a serial-availability composition, a redundancy-sizing check, and a retry config that turns a slow dependency into an outage."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - error-budget
  - availability-math
  - redundancy
  - retry-storm
  - timeouts
  - split-brain
prereqs:
  - 02-availability/01-sla-slo-sli
  - 02-availability/02-redundancy-and-spof
  - 02-availability/03-failover-and-fault-tolerance
sources:
  - https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/
  - https://sre.google/sre-book/service-level-objectives/
  - https://aws.amazon.com/builders-library/implementing-health-checks/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Availability incidents live in the numbers, not the prose: an SLO budget, a chain of dependencies multiplied together, a pool sized to N instead of N+1, a retry policy with no backoff. Read each snippet, do the arithmetic in your head, and pick the answer a senior on-call engineer would commit to.
</Hook>

<Crux>Find the numbers in the config — budget, availability product, N-vs-N+1, retries — do the arithmetic, and pick the fix the math supports.</Crux>

<Explanation>

Practise the loop you run during an availability review or an incident: locate the numbers, apply the nines/error-budget/composition math or the retry-and-timeout rules, and choose the change the arithmetic actually supports — not the one that feels reassuring.

## Snippet 1 — the error-budget helper

```python
def error_budget_minutes(slo: float, window_days: int = 28) -> float:
    # downtime allowed = (1 - SLO) * window
    return (1 - slo) * window_days * 24 * 60

print(round(error_budget_minutes(0.999), 1))   # what prints?
```

<Quiz
  id="02-availability-code-1"
  lessonSlug="quiz-code"
  lang="en"
  question="What does error_budget_minutes(0.999) return over a 28-day window, and what does it mean?"
  choices={[
    { label: "≈40.3 minutes — a 99.9% SLO allows 0.1% downtime, and 0.001 × 28 × 24 × 60 ≈ 40.3 min; that's the error budget you may spend before breaching the objective.", correct: true },
    { label: "≈4 minutes — 99.9% allows about 4 minutes a month.", misconception: "~4.3 min is FOUR nines (99.99%) over ~30 days. Three nines (99.9%) over 28 days is 0.001 × 28 × 24 × 60 ≈ 40.3 min. Watch which nine you're computing." },
    { label: "≈403 minutes — 1% downtime.", misconception: "That's two nines (99%, i.e. 0.01). A 99.9% SLO uses 0.001, giving ≈40.3 min over 28 days, not 403. You're off by a factor of ten — one whole nine." },
    { label: "0 — a 99.9% SLO means no allowed downtime.", misconception: "99.9% explicitly allows 0.1% to fail. The budget is 0.001 × 28 × 24 × 60 ≈ 40.3 min — the spendable error budget, not zero. Treating it as zero discards the whole point of an SLO." },
  ]}
/>

## Snippet 2 — the dependency chain

```python
# a request must pass through all four to succeed
chain = {"lb": 0.999, "app": 0.999, "cache": 0.999, "db": 0.999}

avail = 1.0
for a in chain.values():
    avail *= a
print(round(avail, 4))   # what prints, and is it >= the 99.9% SLO?
```

<Quiz
  id="02-availability-code-2"
  lessonSlug="quiz-code"
  lang="en"
  question="What does this print, and does the path meet a 99.9% SLO?"
  choices={[
    { label: "≈0.996 (99.6%) — availability multiplies across the four serial dependencies, so 0.999⁴ ≈ 99.6%, which is BELOW the 99.9% SLO; you'd add redundancy or remove a hop.", correct: true },
    { label: "0.999 (99.9%) — the path is as available as each component, so it just meets the SLO.", misconception: "Serial availability multiplies, it doesn't copy one link's value. 0.999⁴ ≈ 99.6%, below the 99.9% SLO. Each added dependency lowers the path's availability." },
    { label: "≈0.9996 — four nines, comfortably above the SLO.", misconception: "Adding serial components LOWERS availability, not raises it. The product is 0.999⁴ ≈ 99.6%, not 99.96%. You can't reach four nines from four three-nines links in series." },
    { label: "Can't tell — depends on the order of the dependencies.", misconception: "For a serial path the product is order-independent: 0.999⁴ ≈ 99.6% regardless of sequence. The point is it's below the SLO, so the chain needs redundancy or fewer hops." },
  ]}
/>

## Snippet 3 — the redundancy config

```yaml
# autoscaler / capacity config
peak_required_instances: 6     # instances needed to carry peak load
desired_instances: 6           # what we actually run
# note: "we run multiple instances, so the tier is redundant"
```

<Quiz
  id="02-availability-code-3"
  lessonSlug="quiz-code"
  lang="en"
  question="The team calls this tier redundant because it runs multiple instances. Using the redundancy lesson, what's wrong and what's the fix?"
  choices={[
    { label: "desired equals peak_required, so this is N, not N+1 — losing one instance at peak drops you below capacity and the survivors can collapse under the queueing curve. Set desired_instances to 7 (N+1), or more for N+2.", correct: true },
    { label: "Nothing — running 6 instances is redundant because there's more than one.", misconception: "Redundancy means surviving a loss while still meeting PEAK, which needs a spare. At desired = peak = 6, losing one leaves 5 for a 6-instance peak: under capacity. That's N, not N+1." },
    { label: "Lower desired_instances to 5 to save cost; redundancy is about having a backup, not capacity.", misconception: "That makes it worse — now you're under peak even with zero failures. Redundancy IS about spare capacity sized to peak: you need N+1 = 7, not fewer than peak." },
    { label: "Set desired_instances to 6 but add a second load balancer; that makes the tier N+1.", misconception: "A second LB removes a different SPOF (the balancer) but doesn't fix instance redundancy: 6 instances for a 6-instance peak is still N. You need a 7th instance for N+1 in this tier." },
  ]}
/>

## Snippet 4 — the retry policy

```python
# client calling a downstream service
RETRY_CONFIG = {
    "max_attempts": 5,
    "timeout_seconds": 30,     # per attempt
    "backoff": "none",         # retry immediately
    "jitter": False,
}
# the downstream service has just started returning slow responses
```

<Quiz
  id="02-availability-code-4"
  lessonSlug="quiz-code"
  lang="en"
  question="The downstream service slows down. What does this retry policy do to it, and what's the correct configuration?"
  choices={[
    { label: "It causes a retry storm: 30 s timeouts pin threads while 5 immediate, un-jittered retries multiply load on the struggling service in lockstep, driving brownout into outage. Fix with short bounded timeouts, a small retry budget, exponential backoff, and jitter — and retry only idempotent calls.", correct: true },
    { label: "It makes the call more reliable — 5 attempts maximize the chance of success.", misconception: "During overload, hammering 5× immediately multiplies load exactly when the dependency can least afford it (a retry storm). Reliability comes from capped, backed-off, jittered retries, not from retrying harder." },
    { label: "Harmless — the retries just use spare capacity on the downstream.", misconception: "An overloaded dependency has no spare capacity; the retries are added load on a failing system, accelerating collapse. That's why you need a retry budget plus backoff and jitter." },
    { label: "The only problem is max_attempts; lower it to 3 and it's fine.", misconception: "Fewer attempts helps a little, but the 30 s timeout (thread exhaustion) and the no-backoff/no-jitter lockstep retries are the bigger issues. Fix timeouts, add exponential backoff WITH jitter, and a retry budget." },
  ]}
/>

</Explanation>

<KeyTakeaway>Every availability decision reduces to config arithmetic: (1−SLO)×window for the error budget, a product of links for path availability, desired-vs-peak for N-vs-N+1, timeout × retries for the storm. Read the numbers.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-availability-code-retrieval"
  lang="en"
  questions={[
    { q: "How do you compute an error budget from an SLO, and what's the common nine-off-by-one error?", a: "Budget downtime = (1 − SLO) × window. For 99.9% over 28 days: 0.001 × 28 × 24 × 60 ≈ 40.3 min; for 99.99% it's 0.0001 × that ≈ 4 min. The classic mistake is being off by one nine — using 0.01 (two nines) when you meant 0.001 (three nines), a 10× error. Always line up the number of nines with the decimal: 99% → 0.01, 99.9% → 0.001, 99.99% → 0.0001." },
    { q: "Why does a four-link 99.9% serial path miss a 99.9% SLO, and what are the fixes?", a: "Availability multiplies across serial dependencies: 0.999⁴ ≈ 0.996 (99.6%), below the 99.9% SLO, because each added dependency is another thing that can fail the request. Fixes: remove hops from the critical path, make a link redundant so its failure doesn't fail the request (turning a serial dependency into a parallel/tolerated one), or push a slow/optional dependency off the synchronous path (cache it, make it async, degrade gracefully). You can't reach the SLO by improving prose — only by changing the dependency graph or its redundancy." },
    { q: "What turns a slow downstream into an outage in a retry config, and what's the safe configuration?", a: "Long per-attempt timeouts pin threads until the pool is exhausted (one slow backend → full outage), and immediate, un-jittered retries multiply load on the struggling service in lockstep — a retry storm that drives brownout into outage. The safe config is short, deliberate timeouts; a retry budget that caps retries as a fraction of traffic (not a fixed per-request count); exponential backoff so attempts space out; jitter so clients don't synchronize; circuit breakers to stop hammering a dead dependency; and retries only on idempotent operations. This is exactly the AWS timeouts-retries-backoff-with-jitter prescription." },
  ]}
/>

<Recap lang="en">
Every availability decision in this unit reduces to arithmetic you can read straight off the config. The **error budget** is `(1 − SLO) × window` — 99.9% over 28 days ≈ 40 minutes — and the classic bug is being off by one nine (0.01 vs 0.001, a 10× error). Path availability is the **product** of its serial links, so a four-link 99.9% chain is only ~99.6% and misses a 99.9% SLO; you fix it by removing hops or adding redundancy, not by editing prose. **Redundancy** is N+1, so `desired_instances == peak_required` is N — not redundant — and losing one at peak tips into the queueing collapse; you provision a spare. And a **retry policy** with long timeouts and immediate, un-jittered retries turns a slow dependency into a retry storm and an outage — the fix is short bounded timeouts, a retry budget, exponential backoff with jitter, circuit breakers, and idempotent-only retries. The senior habit is the same as the scalability unit: find the numbers, do the math, and choose the fix the arithmetic supports rather than the one that sounds safe.
</Recap>
