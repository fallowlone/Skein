---
slug: "project"
lang: en
track: "system-design"
unit: "02-availability"
order: 7
title: "Availability: an SLO and failover plan from scratch"
summary: "Hands-on project: take a product brief, derive its SLIs/SLOs and error budget, hunt every single point of failure and design the redundancy and failover to remove them, then prove your failover and degradation work with a small chaos test that injects failures."
estMin: 240
status: ready
lessonType: topic
level: senior
concepts:
  - sli-slo-sla
  - error-budget
  - redundancy
  - failover
  - split-brain
  - graceful-degradation
prereqs:
  - 02-availability/01-sla-slo-sli
  - 02-availability/02-redundancy-and-spof
  - 02-availability/03-failover-and-fault-tolerance
sources:
  - https://sre.google/workbook/implementing-slos/
  - https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/
  - https://aws.amazon.com/builders-library/static-stability-using-availability-zones/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Reading that you should define SLOs and remove single points of failure is not the same as producing an availability design a staff engineer would sign off on. Take a real-shaped product brief, write its SLIs and SLOs with an error budget, map and eliminate every SPOF with the right redundancy and failover, and then prove the two riskiest claims — that failover actually recovers and that the system degrades instead of going dark — with a small chaos test you write yourself.
</Hook>

<Crux>Turn a brief into SLOs and an error budget, eliminate every SPOF with redundancy and failover, then chaos-test recovery and degradation.</Crux>

<Explanation>

This project makes the whole unit operational: you'll choose user-centric SLIs and set SLOs under an SLA, derive the error budget, hunt SPOFs and design redundancy and failover that hold under correlated failure, and then measure — inject failures and confirm the system recovers within your target and degrades gracefully instead of failing whole.

<ProjectBrief
  id="02-availability-project"
  lessonSlug="project"
  lang="en"
  objective="Produce a complete availability design for a chosen product (e.g. a URL shortener, a payments API, or a chat backend): define its SLIs, SLOs, and error budget; map and eliminate every single point of failure with appropriately-sized redundancy and a failover plan; then build a small chaos test that injects failures and empirically validates that failover recovers within your target and that the system degrades gracefully — closing the loop between the design and the failures it claims to survive."
  requirements={[
    "Pick a product and write a one-paragraph brief: what it does, the critical user journey (e.g. 'shorten a URL and redirect'), and any contractual SLA you'll assume (e.g. 99.9% monthly availability with credits). State the assumptions explicitly.",
    "Define 2-3 SLIs that reflect what a USER feels, each as a good-events-over-valid-events ratio measured close to the user (e.g. 'fraction of redirect requests served < 200 ms with a 2xx/3xx'), and say exactly where you'd measure each.",
    "Set an SLO for each SLI, choose the measurement window, and set it deliberately TIGHTER than the assumed SLA; compute the resulting error budget in minutes/requests per window and state what you'd do when it's half-spent and when it's exhausted.",
    "Draw the architecture and run the SPOF thought experiment over every box and line ('if this dies now, do we keep serving?'); produce a table of every SPOF found, including shared infrastructure (single LB, DB primary, DNS, NAT) and non-machine ones (a vendor, a sole owner).",
    "For each SPOF, specify the removal: redundancy sized to N+1 against PEAK, placed in INDEPENDENT failure domains (separate AZs), and the arrangement (active-active vs active-passive) with the cost/recovery-time tradeoff justified.",
    "Write the failover plan for the stateful tier: detection mechanism (health check/heartbeat) and its timeout tradeoff, the promote/redirect steps, and the split-brain defense (quorum or fencing). Specify timeouts, a retry budget, and backoff-with-jitter so failover doesn't trigger a retry storm.",
    "List the graceful-degradation behaviors: for each dependency, what the system does when it's down (e.g. serve cached/popular results, read-only mode) so a dependency's death is a missing feature, not a blank page.",
    "Build a tiny service (any language) with a redundant component you can kill (e.g. two backends behind a proxy, or a primary/standby pair), plus a chaos script that injects failures: kill a node, add latency, partition the network, and force the dependency down.",
    "Validate recovery: kill the active node under load, measure the recovery time (time to the first successful request after the kill), and compare it to your failover target; show the error-budget burn the incident caused.",
    "Validate degradation and the retry behavior: force a non-critical dependency down and show the system serves a degraded-but-working response; then show that your timeout/retry/backoff config prevents a retry storm during the failure (compare with and without backoff-and-jitter).",
  ]}
  acceptance={[
    "An SLI/SLO/error-budget sheet: 2-3 user-centric SLIs with where each is measured, an SLO per SLI tighter than the SLA, the computed error budget per window, and the policy for half-spent and exhausted budget — readable in two minutes.",
    "A SPOF table that covers the whole architecture (including shared infra and non-machine SPOFs), each row with the removal: redundancy level (N+1/N+2), failure-domain placement, and active-active vs active-passive with the tradeoff stated.",
    "A failover plan naming the detection mechanism and its timeout tradeoff, the promote/redirect steps, the split-brain defense (quorum or fencing), and the timeout/retry-budget/backoff-with-jitter settings that keep recovery from becoming a retry storm.",
    "A working chaos test whose measured recovery time after killing the active node is reported against the failover target, plus the error-budget burn the simulated incident consumed.",
    "A demonstration of graceful degradation (a forced-down dependency yields a degraded-but-working response) and of retry safety (the same failure with vs without backoff-and-jitter, showing the storm is averted).",
    "A short write-up: one paragraph on which SPOF was least obvious and how you found it, and one on where the chaos test's measured recovery differed from the design's assumption and why.",
  ]}
  stretch={[
    "Add a correlated-failure test: place both replicas in the same simulated failure domain, take the domain down, and show the 'redundant' pair fails together; then re-place them in independent domains and show it survives — quantifying the difference in availability.",
    "Add a split-brain demonstration: partition a primary from its standby without fencing/quorum and show the divergence; then enable fencing or quorum and show the wrong 'dead' call becomes harmless.",
    "Model the nines: from your measured MTBF (injected failure rate) and MTTR (measured recovery), compute availability ≈ MTBF/(MTBF+MTTR), and show which investment — failing less often vs recovering faster — moves your achievable SLO more.",
    "Add an open-loop retry storm reproduction: hold the dependency slow, run clients with naive immediate retries vs a retry budget with backoff-and-jitter, and plot the load amplification each produces on the struggling dependency.",
  ]}
/>

</Explanation>

<KeyTakeaway>An availability design makes the unit operational: SLOs under the SLA with an error budget, every SPOF removed by N+1 redundancy in independent domains, a split-brain-safe failover, plus degradation — proven by a test.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-availability-project-retrieval"
  lang="en"
  questions={[
    { q: "What does the availability design produce before any chaos test, and how does each piece connect?", a: "It produces: user-centric SLIs (good/valid ratios measured close to the user), SLOs set tighter than the SLA with a computed error budget and a spend policy, a SPOF table covering the whole architecture (including shared infra and non-machine SPOFs), a removal per SPOF (N+1 redundancy in independent failure domains, active-active vs active-passive with the tradeoff), a failover plan (detection + timeout tradeoff, promote/redirect, split-brain defense via quorum/fencing, storm-safe timeouts/retries/backoff-with-jitter), and graceful-degradation behaviors. Each connects: the SLO sets the bar, the error budget is the slack, the SPOF removal raises the floor, and degradation contains what's left." },
    { q: "How do you validate failover and graceful degradation in a chaos test, and what do you measure?", a: "Failover: kill the active node under load and measure recovery time (time to first successful request after the kill) against your target, plus the error-budget burn the incident caused. Degradation: force a non-critical dependency down and show a degraded-but-working response instead of a blank page. Retry safety: reproduce the failure with and without backoff-and-jitter and show the storm is averted. Stretch: place replicas in the same vs independent failure domains to demonstrate correlated failure, and partition with/without fencing to demonstrate split-brain becoming harmless." },
    { q: "Why design SLOs and SPOF removal first, then chaos-test, rather than just chaos-testing?", a: "The design is reasoning that eliminates whole classes of outage on paper — a SPOF table and the right redundancy/failover prevent failures you'd otherwise discover in production, and the SLO/error-budget tells you how much failure is even acceptable. Chaos testing is slower and only tells you about what you already built; doing the design first makes the test targeted — you verify the two riskiest claims (that failover recovers within target and that the system degrades instead of going dark) rather than poking randomly. The discipline is design-then-validate: the paper design says what should survive, the chaos test confirms it does." },
  ]}
/>

<Recap lang="en">
This project turns the availability unit into a repeatable workflow: take a product brief, define **user-centric SLIs** (good/valid ratios measured close to the user), set **SLOs tighter than the SLA**, and compute the **error budget** with a spend policy. Then **hunt every SPOF** with the thought experiment over every box and line — including shared infrastructure and non-machine SPOFs — and remove each with **redundancy sized to N+1 against peak, placed in independent failure domains**, choosing active-active or active-passive with the tradeoff stated. Write the **failover plan** (detection and its timeout tradeoff, promote/redirect, a **split-brain** defense via quorum or fencing, and storm-safe timeouts/retries/backoff-with-jitter) and the **graceful-degradation** behaviors so a dependency's death is a missing feature, not a blank page. Then **close the loop with a chaos test**: kill the active node and measure recovery against the target, force a dependency down and confirm degradation, and reproduce the retry storm with and without backoff-and-jitter. Doing the design first and the chaos test second is the whole discipline — the paper design says what should survive, the test confirms it does. An engineer who has built this once designs for failure deliberately instead of discovering their SPOFs and their split-brain during a 3 a.m. incident.
</Recap>
