---
slug: "quiz-short"
lang: en
track: "system-design"
unit: "02-availability"
order: 5
title: "Availability: free-recall review"
summary: "Free-recall prompts across the availability unit. Answer each in your own words first — including the arithmetic — then reveal the model answer and compare: SLO/SLA, the nines and error budgets, redundancy and correlated failure, failover and split-brain."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - sli-slo-sla
  - error-budget
  - redundancy
  - correlated-failure
  - split-brain
  - mttr-mtbf
prereqs:
  - 02-availability/01-sla-slo-sli
  - 02-availability/02-redundancy-and-spof
  - 02-availability/03-failover-and-fault-tolerance
sources:
  - https://sre.google/sre-book/service-level-objectives/
  - https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/
  - https://aws.amazon.com/builders-library/static-stability-using-availability-zones/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory — including the arithmetic — before you open the model answer. The effort of reconstructing the SLO math, the redundancy sizing, and why a failover causes split-brain is what makes them stick.
</Hook>

<Crux>Reconstruct the unit's spine: SLI/SLO/SLA, the nines and error budget, redundancy and correlated failure, failover, split-brain, and MTTR.</Crux>

<Explanation>

Reconstruct the unit's spine without looking back: how the three service-level terms relate and where to set each, what the nines cost and how availability composes, what an error budget buys you, how to size redundancy and why same-AZ replicas aren't redundant, and how a failover can cause the very outage it was meant to prevent.

<RetrievalDrawer
  client:visible
  id="02-availability-short-retrieval"
  lang="en"
  questions={[
    { q: "Define SLI, SLO, and SLA, state how they relate, and where you set the SLO.", a: "SLI (indicator) is the measured number — a ratio of good events to valid events, e.g. fraction of requests served correctly under 300 ms. SLO (objective) is the internal target on that SLI, e.g. 99.9% over 28 days. SLA (agreement) is the customer contract with penalties for missing, e.g. credits below 99.5%. You measure the SLI, target the SLO, promise the SLA — and you set the SLO tighter than the SLA so an internal breach is an early warning with room to react before you breach the contract and owe credits. And the SLI must reflect what a user feels, measured close to the user, or the other two measure nothing." },
    { q: "Give the nines table for downtime per month and explain why each nine costs ~10× more.", a: "99% ≈ 7.2 h/month, 99.9% ≈ 43 min, 99.99% ≈ 4.3 min, 99.999% ≈ 26 s — each nine cuts allowed downtime by 10×. The cost climbs an order of magnitude per nine because availability composes by MULTIPLICATION across dependencies (four serial 99.9% links → 0.999⁴ ≈ 99.6%, worse than any link), so reaching four nines end to end means every link must beat four nines or be made redundant with automated failover faster than a human can react. Three nines tolerates a manual rollback; four needs automation; five removes humans from the loop entirely — that automation and multi-region redundancy is the cost." },
    { q: "What is an error budget and how does it change a team's behavior?", a: "The error budget is 100% − SLO — the amount of failure allowed before breaching the objective (0.1% of requests for a 99.9% SLO; ~4.3 min/month for 99.99%). It reframes reliability from 'never fail' to 'stay within budget' and makes reliability a spendable resource: while budget remains, ship features and take risks; once exhausted, policy freezes risky launches until it refills. A number, not opinion, settles the velocity-vs-stability fight, and a perpetually unspent budget signals over-investment in reliability users didn't ask for." },
    { q: "Explain redundancy sizing (N/N+1/N+2), active-active vs active-passive, and why same-AZ replicas aren't redundant.", a: "N is exactly peak capacity, no spare (not redundant); N+1 survives one loss while still meeting PEAK (size against peak, not average); N+2 survives a loss while another node is already down. Active-active: all copies serve behind a balancer — instant failover but you pay for every copy and resolve write conflicts. Active-passive: one serves, a standby waits — cheaper and write-simple but failover takes time and an unexercised standby may be silently broken. Two replicas in the same AZ aren't redundant because they share a failure domain: one event takes both (correlated failure), so independence-based math is invalid. Spread copies across AZs/regions engineered to fail independently, and shrink blast radius to bound the failures you can't prevent." },
    { q: "Distinguish failover from fault tolerance, explain split-brain and its fix, and the retry-storm trap.", a: "Failover is reactive recovery with a short window (high availability); fault tolerance masks the fault with no window via live redundancy (e.g. active-active), at higher cost. Split-brain is a failover promoting a new primary while the old one is alive (network partition), so two primaries take writes and diverge with no auto-merge — it loses correctness, not availability. Better detection can't fix it ('dead' and 'unreachable' are indistinguishable by waiting); quorum (only the majority promotes) or fencing/STONITH (cut off the old primary first) makes a wrong call harmless. The retry storm: at failover every request times out and retries in lockstep onto a warming standby, so bound timeouts, cap retries (retry budget), use exponential backoff WITH jitter, retry only idempotent ops, and lean on graceful degradation. Availability ≈ MTBF/(MTBF+MTTR), so invest in fast rehearsed recovery (MTTR)." },
  ]}
/>

</Explanation>

<KeyTakeaway>Reconstruct each answer from memory — SLI/SLO/SLA, the nines and error budget, redundancy and correlated failure, failover and split-brain — and you hold the unit's spine: availability is reasoning done up front.</KeyTakeaway>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the unit's spine. The three service-level terms relate one way — measure the **SLI**, target the **SLO**, promise the **SLA** — and the SLO sits tighter than the SLA as an early warning. The **nines** (99.9% ≈ 43 min/month → 99.999% ≈ 26 s) cost an order of magnitude each because availability **multiplies** across dependencies, and the **error budget** (100% − SLO) turns reliability into a spendable resource. **Redundancy** is sized to N+1 against peak and placed in independent failure domains, because same-AZ replicas are **correlated failure**, not redundancy. **Failover** (reactive, with a recovery window) differs from **fault tolerance** (masked, no window), and a failover that can't tell dead from unreachable causes **split-brain** — fixed by quorum or fencing, not better timeouts — while the **retry storm** at failover is tamed by bounded timeouts and capped retries with backoff and jitter. The unifying idea: **availability ≈ MTBF/(MTBF+MTTR)**, so since failures are inevitable, the better investment is usually fast, rehearsed recovery — and all of it is reasoning you do before the incident, not behavior you discover during it.
</Recap>
