---
slug: "quiz-choice"
lang: en
track: "system-design"
unit: "09-interview-framework"
order: 5
title: "Interview framework: multiple-choice review"
summary: "Multiple-choice synthesis across the interview-framework unit: clarifying requirements and access patterns, estimation that changes the design, the HLD-and-deep-dive split, and finding the bottleneck while naming tradeoffs."
estMin: 13
status: ready
lessonType: topic
level: senior
concepts:
  - functional-requirements
  - access-patterns
  - estimation-in-interview
  - high-level-design
  - bottleneck-identification
  - tradeoffs
prereqs:
  - 09-interview-framework/01-requirements
  - 09-interview-framework/02-estimation
  - 09-interview-framework/03-hld-and-deep-dive
  - 09-interview-framework/04-bottlenecks-and-tradeoffs
sources:
  - https://github.com/donnemartin/system-design-primer
  - https://sre.google/sre-book/service-level-objectives/
  - https://aws.amazon.com/builders-library/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole interview framework. Each is a decision you make at the whiteboard — what to ask before drawing, which number to estimate, what to deep-dive, where the bottleneck is — not a definition to recite. Answer as if the interviewer is watching for the senior signal.
</Hook>

<Crux>Six whiteboard decisions: the load-bearing requirement, the design-changing number, the deep-dive, the bottleneck, the named tradeoff.</Crux>

<Explanation>

Confirm you can run the framework end to end: clarify the requirement that actually changes the design, estimate only the number that matters, split a clean HLD from a real deep-dive, find the single binding bottleneck, and state a tradeoff with its price.

<Quiz
  id="09-interview-choice-1"
  lessonSlug="quiz-choice"
  lang="en"
  question="Given 'design a chat app' and nothing else, which question most changes the architecture and should come first?"
  choices={[
    { label: "The consistency/latency target — strict real-time ordering vs a sub-second eventual window — because it decides whether you can fan out and cache or must coordinate.", correct: true },
    { label: "Which programming language the backend uses.", misconception: "Language is an implementation detail that doesn't move the high-level architecture. The non-functional target (consistency/latency) is what pushes you toward fan-out-and-cache vs strict coordination — ask it first." },
    { label: "How many tables the schema will have.", misconception: "Table count is downstream of the data model, which is downstream of entities and access patterns. You can't size the schema before pinning the consistency/latency targets that shape the design." },
    { label: "The cloud provider for deployment.", misconception: "Cloud choice rarely changes the architecture at this stage. The load-bearing early question is the non-functional target — consistency and latency." },
  ]}
/>

<Quiz
  id="09-interview-choice-2"
  lessonSlug="quiz-choice"
  lang="en"
  question="A social timeline reads ~100:1 over writes. What does this access pattern most directly justify before any box is drawn?"
  choices={[
    { label: "A precomputed, cached read path (fan-out-on-write into per-user timelines) — optimizing the dominant read path is the leverage when reads far outnumber writes.", correct: true },
    { label: "A write-optimized append-only log with no read index.", misconception: "That optimizes the rare operation. With reads outnumbering writes 100×, you precompute and cache the read path, not speed up the infrequent writes." },
    { label: "Synchronous replication on every write.", misconception: "The 100:1 ratio says nothing about strict write replication, which would only slow writes. It points at optimizing reads — caching and fan-out — first." },
    { label: "Nothing — the ratio doesn't affect storage choice.", misconception: "It's one of the most relevant facts you have: a read-heavy pattern justifies caching and precomputed reads. The access pattern, not the entity, picks the read path." },
  ]}
/>

<Quiz
  id="09-interview-choice-3"
  lessonSlug="quiz-choice"
  lang="en"
  question="Estimating to one significant figure: 50M DAU each make 2 posts/day, reads are ~50× posts. Which number first changes the design, and to what?"
  choices={[
    { label: "Read QPS ≈ 50,000/sec average (~150K peak) forces a cache and a fan-out/precomputed read path — a single DB won't serve it.", correct: true },
    { label: "Write QPS ≈ 1,000/sec, which forces immediate sharding.", misconception: "~1,000 writes/sec (≈3K peak) sits comfortably under a single primary's low-thousands ceiling — it does NOT force sharding yet. The design-changing number here is the ~150K peak read QPS, which forces a cache." },
    { label: "Storage, which is trivial for text and changes nothing.", misconception: "Text storage may be modest, but if there's media it dominates by orders of magnitude — and regardless, the FIRST design-changing number is the ~150K peak read QPS forcing a cache/fan-out path." },
    { label: "Nothing changes until you know the exact response byte size.", misconception: "Response size is for bandwidth, computed later. Read QPS comes straight from activity ÷ seconds and already forces the cache/fan-out decision before you size bandwidth." },
  ]}
/>

<Quiz
  id="09-interview-choice-4"
  lessonSlug="quiz-choice"
  lang="en"
  question="You've drawn a clean five-box HLD with 30 minutes left. What is the highest-value next move?"
  choices={[
    { label: "Deep-dive the one or two genuinely hard components (e.g. the hot read path the estimate flagged) — data structures, failure modes, hot keys, numbers — announcing the choice so the interviewer can redirect.", correct: true },
    { label: "Add more boxes — a service mesh, a second queue — to show breadth.", misconception: "Breadth is table stakes and extra boxes are liabilities you must defend. The interview is won in the depth: engineer the hard component instead of inflating the HLD." },
    { label: "Re-derive the QPS estimate to more significant figures.", misconception: "The estimate already flagged the hard part. Re-deriving burns the budget that should go to the deep-dive — the part that proves you can engineer, not just estimate." },
    { label: "List operational concerns — monitoring, CI, rate limiting — to be thorough.", misconception: "That generic rattle reads as shallow and signals you've run out to say about the real problem. Go deep on the hard component; mention ops briefly at the end if at all." },
  ]}
/>

<Quiz
  id="09-interview-choice-5"
  lessonSlug="quiz-choice"
  lang="en"
  question="A DB cluster shows 40% average CPU, yet users hit timeouts on requests touching one popular account. What's the bottleneck, and why does the average mislead?"
  choices={[
    { label: "A hot shard/key — one partition runs near 100% while others idle, so the average hides the saturated resource. The bottleneck is the single hottest resource, not the mean.", correct: true },
    { label: "Nothing's wrong — 40% average means plenty of headroom.", misconception: "The average is lying. A 40% cluster average can hide one shard at 99% (a popular account's data). The system stalls at the hottest resource regardless of the mean — that's the bottleneck the timeouts point to." },
    { label: "The service tier needs more CPU.", misconception: "The symptom (DB timeouts, low average DB CPU) points at uneven load inside the database — a hot shard — not the service tier. Adding service CPU widens a resource that isn't binding." },
    { label: "The network is saturated, since CPU is low.", misconception: "Low average CPU with localized timeouts most directly indicates a hot shard/key skewing load, not network saturation. The lesson: the average misleads — find the single hottest resource." },
  ]}
/>

<Quiz
  id="09-interview-choice-6"
  lessonSlug="quiz-choice"
  lang="en"
  question="Asked 'what if traffic grows 10×?', which response shows the senior bottleneck reasoning the unit teaches?"
  choices={[
    { label: "Recompute at 10×, walk the path to the resource that now breaks first (the stateless tier scales out, so it's a stateful one — the write primary or a cache that no longer fits RAM), name the targeted fix and its new tradeoff, and say where the bottleneck moves next.", correct: true },
    { label: "'We'd add more servers to handle the load.'", misconception: "The mid-level reflex: true but it never names WHICH resource breaks first. The stateless tier scales out trivially; the real bottleneck moves to a stateful resource. Naming the specific binding constraint and its fix is the senior signal." },
    { label: "'The design already scales infinitely, so nothing changes.'", misconception: "No design scales infinitely — there's always a next bottleneck (write primary, hot shard, cache outgrowing RAM). Claiming nothing changes shows you haven't found where the constraint moves at 10×." },
    { label: "'We'd rewrite it in a faster language.'", misconception: "Language rarely addresses the binding constraint, which at 10× is almost always a stateful resource, not service-tier CPU. The senior move is to locate that resource and fix it specifically." },
  ]}
/>

</Explanation>

<KeyTakeaway>The framework is one procedure: clarify the requirement that changes the design, estimate the number that matters, split a clean HLD from a real deep-dive, find the binding bottleneck, name each tradeoff with its price.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="09-interview-choice-retrieval"
  lang="en"
  questions={[
    { q: "Why is the non-functional target, not the feature list, the load-bearing early question?", a: "Because two systems with identical features need wildly different architectures once the targets move: a lossy, eventually-consistent store is a cache, while a linearizable, durable one is a consensus database. The consistency/latency target decides whether you can fan out and cache or must coordinate — so it's the first question that actually changes the design, while the features are usually shared by every candidate." },
    { q: "How do you handle '10× traffic' as a procedure rather than a reflex?", a: "Recompute the key estimate at 10× and walk the path to find the resource that now breaks first — the stateless tier scales out trivially, so it's almost always a stateful resource (the write primary, a hot shard, a cache that no longer fits one node's RAM). Name the targeted fix and the new tradeoff it introduces (sharding the write tier makes cross-shard queries expensive → denormalize), then say where the bottleneck moves next and what cascades if you do nothing (queueing curve goes vertical, retries turn a brownout into an outage)." },
  ]}
/>

<Recap lang="en">
The through-line is that the system-design interview is one repeatable procedure, and these six questions test each step. **Clarify** the non-functional target that actually changes the design (consistency/latency decides fan-out-and-cache vs coordinate), and read the **access pattern** (100:1 read-heavy → precomputed cached read path). **Estimate** only the number that matters — the ~150K peak read QPS that forces a cache, not the modest write rate or the trivia. Draw a clean **HLD** fast, then spend the budget on a real **deep-dive** of the genuinely hard component rather than inflating the box count. Find the **bottleneck** — the single hottest resource, which the average utilization hides (a 40% cluster melting on one shard) — and handle **"10×"** by recomputing, naming the new binding resource, its fix, and its tradeoff. The seniority signal under all of it: name the specific resource and the price of each choice, never just "add more servers."
</Recap>
