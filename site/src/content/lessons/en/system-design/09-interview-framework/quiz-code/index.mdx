---
slug: "quiz-code"
lang: en
track: "system-design"
unit: "09-interview-framework"
order: 7
title: "Interview framework: applied numbers and config"
summary: "Read the artifacts of a real interview — an estimation helper, a requirements note, a capacity calc, a config that encodes a tradeoff — and do the reasoning a senior engineer would: which number changes the design, what the config gives up, where the bottleneck moves."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - estimation-in-interview
  - capacity-planning
  - access-patterns
  - bottleneck-identification
  - tradeoffs
  - high-level-design
prereqs:
  - 09-interview-framework/01-requirements
  - 09-interview-framework/02-estimation
  - 09-interview-framework/03-hld-and-deep-dive
  - 09-interview-framework/04-bottlenecks-and-tradeoffs
sources:
  - https://github.com/donnemartin/system-design-primer
  - https://aws.amazon.com/builders-library/
  - https://sre.google/sre-book/service-level-objectives/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
The framework lives in the numbers and the config, not the prose. An estimate that converts to a server count, a requirements note that hides a missing target, a config that silently encodes a tradeoff. Read each artifact, do the arithmetic and the reasoning in your head, and pick the answer a senior engineer would commit to at the whiteboard.
</Hook>

<Crux>Read the estimate, the requirements note, the capacity calc, and the config — then pick the design decision the numbers actually support.</Crux>

<Explanation>

Practise the loop you run at the whiteboard: find the design-changing number, spot the missing requirement, convert load to capacity, and read what a config gives up — choosing the move the arithmetic and the tradeoff support.

## Snippet 1 — the estimation helper

```python
def design_changing_numbers(dau, posts_per_day, read_write_ratio):
    writes_per_sec = dau * posts_per_day / 86_400
    reads_per_sec  = writes_per_sec * read_write_ratio
    peak = lambda x: round(x * 3)          # 3x peak multiplier
    return {"write_avg": round(writes_per_sec), "read_avg": round(reads_per_sec),
            "write_peak": peak(writes_per_sec), "read_peak": peak(reads_per_sec)}

# 50M DAU, 2 posts/day, reads 50x writes
print(design_changing_numbers(50_000_000, 2, 50))
```

<Quiz
  id="09-interview-code-1"
  lessonSlug="quiz-code"
  lang="en"
  question="Reading to one significant figure, which output number first changes the design, and to what?"
  choices={[
    { label: "read_peak ≈ 175,000 (~150–200K). A single DB can't serve that, so it forces a cache and a fan-out/precomputed read path — the first design-changing number.", correct: true },
    { label: "write_avg ≈ 1,000, which forces sharding the write tier immediately.", misconception: "~1,000 writes/sec (≈3K peak) is comfortably under a single primary's low-thousands ceiling — no sharding forced yet. The design-changing number is the ~150–200K peak read QPS, which forces a cache." },
    { label: "Nothing — the helper returns guesses, so no number can change the design.", misconception: "Order-of-magnitude guesses are exactly what changes the design: ~150–200K peak reads versus a single DB's capacity is a clear threshold crossing that forces a cache/fan-out path. The estimate's whole job is to surface that." },
    { label: "write_peak ≈ 3,000, which is past a single primary.", misconception: "~3,000 writes/sec is right at, not past, a single primary's comfortable low-thousands range — borderline, worth watching, but not the first thing that changes the design. The ~150–200K peak read QPS does, by forcing a cache." },
  ]}
/>

## Snippet 2 — the requirements note

```yaml
# requirements captured before drawing
functional:
  - users post messages
  - users read a timeline
non_functional:
  scale: 50M DAU
  latency_p99_read_ms: 150
  # consistency: <-- not captured
  availability_slo: "99.9%"
```

<Quiz
  id="09-interview-code-2"
  lessonSlug="quiz-code"
  lang="en"
  question="What's the most consequential gap in this requirements note, and why does it matter before you draw?"
  choices={[
    { label: "The consistency target is missing — whether the timeline must be strictly fresh or tolerates seconds of staleness decides whether you can fan out and cache or must coordinate. It changes the architecture, so capture it first.", correct: true },
    { label: "Nothing important is missing — scale, latency, and an SLO are enough to design.", misconception: "The consistency target is missing, and it's load-bearing: staleness-tolerant lets you fan out and cache; strict freshness forces coordination. Two different architectures hinge on it, so it must be captured before drawing." },
    { label: "The programming language is missing.", misconception: "Language doesn't change the high-level architecture. The consequential gap is the consistency target, which decides fan-out-and-cache vs coordinate — capture that, not the language." },
    { label: "The exact number of microservices is missing.", misconception: "Service count is downstream of the design, not an input requirement. The missing input that changes the design is the consistency target." },
  ]}
/>

## Snippet 3 — the capacity calc

```python
peak_read_qps   = 200_000
per_node_qps    = 8_000          # bend point of the queueing curve
cache_hit_rate  = 0.90

origin_qps = peak_read_qps * (1 - cache_hit_rate)
nodes      = -(-origin_qps // per_node_qps)     # ceil division
print(origin_qps, nodes)         # what prints, and what does it justify?
```

<Quiz
  id="09-interview-code-3"
  lessonSlug="quiz-code"
  lang="en"
  question="What does this print, and what design decision does it justify?"
  choices={[
    { label: "20000 and 3. Only 10% of reads (20K/sec) reach origin, so ~3 nodes (add headroom). The order-of-magnitude drop from a ~25-node fleet justifies the cache itself.", correct: true },
    { label: "200000 and 25 — the cache doesn't reduce origin load.", misconception: "The calc applies a 90% hit rate: origin sees 200K × 0.10 = 20K/sec, not 200K. That's ~3 nodes — and the gap from ~25 nodes without the cache is exactly what justifies the cache." },
    { label: "20000 and 3, but you should still provision for the full 200K to be safe.", misconception: "Provisioning origin for the full 200K defeats the cache's purpose and wastes the fleet. Size origin for the miss traffic (20K → ~3 nodes plus headroom); separately ensure graceful degradation if the cache fails." },
    { label: "Can't tell — the hit rate is just a guess.", misconception: "It's an order-of-magnitude guess, which is enough: at ~90% hit rate origin sees ~20K/sec → ~3 nodes. The point is the cache turns a ~25-node fleet into a handful, a design-changing difference." },
  ]}
/>

## Snippet 4 — the config that encodes a tradeoff

```yaml
# timeline read path
read_from: cache
cache_ttl_seconds: 30
on_cache_miss: read_replica       # async-replicated, may lag primary
write_path: fan_out_on_write_async
```

<Quiz
  id="09-interview-code-4"
  lessonSlug="quiz-code"
  lang="en"
  question="What tradeoff does this config encode, and how should you state it to the interviewer?"
  choices={[
    { label: "It chooses eventual consistency for the timeline (30s cache TTL + async replica + async fan-out) — buying read latency and availability at the cost of up to ~30s+ staleness. State it: 'reads can lag by tens of seconds, which is fine for a timeline but I'd change it for anything transactional.'", correct: true },
    { label: "It guarantees strict consistency because it reads from a cache.", misconception: "A cache with a 30s TTL plus an async-replicated replica and async fan-out is the opposite of strict consistency — it deliberately allows staleness. The honest statement names that staleness window as the price of the read latency and availability it buys." },
    { label: "It has no tradeoff — caching is strictly better.", misconception: "Every cache encodes a tradeoff. This one buys read latency and availability but costs freshness (a ~30s+ staleness window) plus invalidation and thundering-herd concerns. Presenting it as strictly better is the surest sign you haven't thought it through." },
    { label: "It makes writes slower to make reads faster, with no other cost.", misconception: "Async fan-out doesn't slow the write path (it's async); the real cost is staleness on reads — the timeline can lag by tens of seconds. State the consistency tradeoff, which is the one that matters here." },
  ]}
/>

</Explanation>

<KeyTakeaway>The framework reduces to reading artifacts: which estimate crosses a threshold, which requirement is missing, how a calc converts load to nodes, what a config gives up. Read the numbers, name the decision and its price.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="09-interview-code-retrieval"
  lang="en"
  questions={[
    { q: "Given an estimation helper's output, how do you spot the number that changes the design?", a: "Look for the figure that crosses a capacity threshold. Peak read QPS in the hundreds of thousands crosses 'a single DB can't serve this' → forces a cache/fan-out path; that's usually the first design-changing number. A write rate around a single primary's low-thousands ceiling is borderline (watch it), while one in the tens of thousands forces sharding. A number that crosses no threshold (a modest write rate, exact metadata bytes) changes nothing and shouldn't drive the design. Read to one significant figure and size for the peak." },
    { q: "How do you convert a cache hit rate into an origin node count, and what does the result justify?", a: "origin_qps = peak_read_qps × (1 − hit_rate); nodes = ceil(origin_qps / per_node_qps), plus headroom (target ~70%, below the queueing bend). Example: 200K peak × 10% miss = 20K to origin / 8K per node ≈ 3 nodes. The result justifies the cache itself: without it you'd need ~25 nodes, so the order-of-magnitude drop is the design-changing fact. Separately size the cache cluster's RAM for the hot set, and ensure the origin degrades gracefully if the cache fails so the cache isn't a new SPOF." },
    { q: "How do you read a tradeoff out of a config, and how do you state it?", a: "Find what the config gives up. A read path with a cache TTL, an async-replicated replica on miss, and async fan-out is choosing eventual consistency: it buys read latency and availability at the cost of a staleness window (here tens of seconds). State it with the price and the boundary: 'reads can lag by ~30s+, acceptable for a timeline but I'd flip to synchronous/strongly-consistent for anything transactional like a balance.' Naming the cost and when you'd change the decision is the senior signal — every component buys something and costs something." },
  ]}
/>

<Recap lang="en">
Every step of the framework shows up in the artifacts you read at the whiteboard. The **estimation helper** outputs a number that either crosses a threshold (a ~150–200K peak read QPS forces a cache/fan-out path) or doesn't (a modest write rate changes nothing) — read to one significant figure and find the one that moves the design. The **requirements note** is judged by what's missing: a dropped consistency target is load-bearing, because staleness-tolerance decides fan-out-and-cache vs coordinate. The **capacity calc** converts load to capacity — a 90% cache hit rate turns a ~25-node origin fleet into ~3, which is exactly what justifies the cache — and you still size the cache's RAM and guard against it becoming a SPOF. The **config** silently encodes a tradeoff (a TTL + async replica + async fan-out is eventual consistency), and the senior move is to name the price (a staleness window) and the boundary where you'd flip it (transactional data). Read the numbers, do the math, name the decision and its cost — that's the framework made operational.
</Recap>
