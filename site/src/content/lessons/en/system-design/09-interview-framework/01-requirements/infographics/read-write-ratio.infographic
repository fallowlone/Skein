infographic chart-column-simple
data
  title Access pattern picks the store: reads per write
  desc Same prompt, different read/write skew — Timeline 100:1 wants a cached read path; Message ~1:1 wants an append log
  values
    - label Timeline (read >> write)
      value 100
    - label Message (write ~ read)
      value 1
theme
  palette #f59e0b #14b8a6
