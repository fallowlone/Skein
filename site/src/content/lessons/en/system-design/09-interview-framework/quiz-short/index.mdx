---
slug: "quiz-short"
lang: en
track: "system-design"
unit: "09-interview-framework"
order: 6
title: "Interview framework: free-recall review"
summary: "Free-recall prompts across the interview-framework unit. Reconstruct each step of the procedure in your own words — requirements, estimation, HLD and deep-dive, bottlenecks and tradeoffs — before revealing the model answer."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - functional-requirements
  - access-patterns
  - estimation-in-interview
  - high-level-design
  - bottleneck-identification
  - tradeoffs
prereqs:
  - 09-interview-framework/01-requirements
  - 09-interview-framework/02-estimation
  - 09-interview-framework/03-hld-and-deep-dive
  - 09-interview-framework/04-bottlenecks-and-tradeoffs
sources:
  - https://github.com/donnemartin/system-design-primer
  - https://sre.google/sre-book/service-level-objectives/
  - https://aws.amazon.com/builders-library/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory — the step of the procedure and the reasoning behind it — before you open the model answer. Reconstructing the framework is what turns it from notes you've read into a procedure you can run live at the whiteboard.
</Hook>

<Crux>Reconstruct the framework's spine: requirements, estimation, HLD-and-deep-dive, bottlenecks, and the tradeoffs that signal seniority.</Crux>

<Explanation>

Reconstruct the whole framework without looking back: what you clarify before drawing, which number you estimate and why, how you split a clean HLD from a real deep-dive, how you find the binding bottleneck, and how you name a tradeoff with its price.

<RetrievalDrawer
  client:load
  id="09-interview-short-retrieval"
  lang="en"
  questions={[
    { q: "What do you clarify before drawing any box, and why are the non-functional requirements where the design is decided?", a: "Split requirements into functional (the features, as user verbs) and non-functional (scale, p99 latency, availability SLO, consistency, durability, cost — stated as numbers). The non-functional targets decide the architecture because two systems with identical features need wildly different designs once the targets change: a lossy, eventually-consistent get/put is a cache; a linearizable, zero-loss one is a consensus database. Then scope the over-broad prompt (a scoped design taken deep beats a complete one taken nowhere), name the 3–6 core entities, and characterize their read/write access patterns — because the access pattern, not the entity, picks the store and the read path." },
    { q: "Which numbers do you estimate in the interview, and what's the rule for when a number belongs on the whiteboard?", a: "Estimate peak read QPS (single primary vs cache/fan-out), peak write QPS (single primary vs sharding), storage over the retention window split by data class (the dominant class picks relational vs object store + CDN), hot-set size (one node's RAM vs sharded cache), and peak bandwidth. The rule: a number belongs on the board only if it would CHANGE the design — each answers 'one box or a fleet?', and a figure that flips no decision is wasted time. Size for the peak (3× average, retries in mind), round to one significant figure, and read whether the interviewer wants the derivation or just the conclusion." },
    { q: "What does the HLD prove, what does the deep-dive prove, and how do you choose what to deep-dive?", a: "The HLD proves you can compose: the standard skeleton — client → CDN/LB → stateless service tier → data tier, with a cache/queue/CDN added only where a number or failure mode justified it — every box defensible in a sentence. The deep-dive proves you can engineer: take the one or two genuinely hard components (the hot read path, the sharding scheme, a strong-consistency write, a celebrity fan-out — usually the one the estimate flagged) and work the data structures, failure modes, hot keys, and numbers. Announce the choice out loud so the interviewer can redirect. Breadth is table stakes; depth wins. Going broad (a shallow tour of ten components, or an ops buzzword rattle) is the failure mode." },
    { q: "How do you find the bottleneck, and why does average utilization lie about it?", a: "Walk the request path and, at each hop, ask the component's ceiling and how close the load is, using the earlier estimate (the bottleneck is the resource your number is closest to exceeding). Candidates: CPU (easy — scale out the stateless tier), the database (usually the real one), a hot key/shard, network, or a downstream dependency. The average lies because the bottleneck is set by the worst-loaded resource: a cluster at 40% average CPU can be melting on one shard at 99% (a celebrity's data), exactly as the mean latency hid the tail. Look for the single hottest resource, not the busy tier." },
    { q: "Name the recurring tradeoff axes, how you handle '10×', and what separates a senior answer from a mid one.", a: "Axes: consistency vs availability (CAP/PACELC — 'a like count can be stale, a balance can't'), latency vs cost, simplicity vs scale, read- vs write-optimization. For '10×': recompute the estimate, walk the path to the resource that now breaks first (almost always stateful), name the targeted fix and its new tradeoff, and say where the bottleneck moves next and what cascades if you do nothing (queueing curve vertical, retries → outage). Senior vs mid: the mid presents the design as correct choices; the senior presents it as deliberate tradeoffs, each with its price named and its alternative acknowledged. Certainty is a junior tell; calibrated tradeoff-awareness — volunteering the downside of your own decision — is the senior one." },
  ]}
/>

</Explanation>

<KeyTakeaway>Reconstruct each step from memory — requirements, estimation, HLD-and-deep-dive, bottleneck, tradeoff — and you hold the framework as a procedure you can run live at the whiteboard, not notes you once read.</KeyTakeaway>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the framework as a runnable procedure. **Requirements**: split functional from non-functional (the latter, as numbers, decide the design), scope the prompt, name entities and their access patterns. **Estimation**: compute only the numbers that change the design — peak read/write QPS, storage by class, hot set, bandwidth — sized for the peak, read to one significant figure, calibrated to whether the interviewer wants math or conclusion. **HLD and deep-dive**: compose the justified skeleton fast, then engineer the one or two genuinely hard components deeply (announce the choice; depth beats breadth). **Bottlenecks and tradeoffs**: find the single hottest resource the average hides, handle "10×" by recomputing and naming the new binding resource, and state every choice as a deliberate tradeoff with its price. The thread tying them together is the seniority signal: name the specific resource and the cost of each decision, never just "add more servers."
</Recap>
