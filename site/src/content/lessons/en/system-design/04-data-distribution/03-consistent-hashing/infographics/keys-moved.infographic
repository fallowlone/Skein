infographic compare-binary-horizontal-underline-text-vs
data
  title Add one node: how many keys move?
  desc Going from 20 to 21 nodes — the lesson's cache example
  compares
    - label hash(key) mod N
      children
        - label Keys moved on +1 node
          desc Nearly all (about 95%)
        - label Cache effect
          desc Hit rate collapses to near zero
        - label Change is
          desc Global reshuffle
    - label Consistent hashing
      children
        - label Keys moved on +1 node
          desc Only one arc (about 1/N, 5%)
        - label Cache effect
          desc Hit rate stays near intact
        - label Change is
          desc Local arc handoff
theme
  palette #f43f5e #14b8a6
