---
slug: "project"
lang: en
track: "system-design"
unit: "04-data-distribution"
order: 8
title: "Data distribution: shard, replicate, and break it"
summary: "Hands-on project: take a dataset, design its partition key and replication, build a tiny sharded + consistent-hashed store, then deliberately trigger a hot shard, a lost write on failover, and a quorum-staleness read — and prove the mitigations work."
estMin: 240
status: ready
lessonType: topic
level: senior
concepts:
  - partition-key
  - consistent-hashing
  - replication-lag
  - quorum
  - hot-shard
  - cap-theorem
prereqs:
  - 04-data-distribution/01-replication
  - 04-data-distribution/02-sharding-and-partitioning
  - 04-data-distribution/03-consistent-hashing
  - 04-data-distribution/04-cap-and-pacelc
sources:
  - https://www.allthingsdistributed.com/files/amazon-dynamo-sosdp2007.pdf
  - https://jepsen.io/consistency
  - https://aws.amazon.com/builders-library/challenges-with-distributed-systems/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Reading that async failover loses writes and that mod-N wrecks a reshard is not the same as watching it happen on a store you built. Take a dataset, design its partition key and replication, implement a tiny sharded and consistent-hashed key-value store, and then deliberately reproduce the unit's three signature failures — the hot shard, the lost write, and the stale quorum read — and prove your mitigations actually close each one.
</Hook>

<Crux>Build a tiny sharded, consistent-hashed, replicated store, then reproduce and fix the hot shard, the lost write, and the stale quorum read.</Crux>

<Explanation>

This project makes the whole unit operational: you'll commit to a partition key and a replication mode, route keys with a hash ring, and then trigger each failure on purpose so the mitigation is something you measured, not something you read.

<ProjectBrief
  id="04-data-distribution-project"
  lessonSlug="project"
  lang="en"
  objective="Design and build a tiny distributed key-value store (any language, in-process or multi-process) that shards data by a chosen partition key, routes keys with consistent hashing, and replicates each shard — then deliberately reproduce a hot shard, a lost write on failover, and a stale quorum read, and demonstrate the mitigation for each."
  requirements={[
    "Pick a dataset and access pattern (e.g. multi-tenant events, social posts, or a URL store). Write a one-paragraph brief stating the dominant query, the read:write ratio, and the cardinality of candidate keys — then justify your chosen partition key from the access pattern (not convenience).",
    "Implement consistent-hashing routing: a ring with virtual nodes (e.g. ~100 per physical node). Show that adding and removing a node moves only ~1/N of keys by measuring the fraction of keys that change owner, and compare against a hash(key) mod N baseline that remaps nearly all.",
    "Replicate each shard with a leader and followers, supporting both synchronous (ack after one follower confirms) and asynchronous (ack immediately, ship in background) modes, with a configurable replication delay.",
    "Reproduce a hot shard: send a skewed workload where one celebrity key takes a large share of traffic, and show one shard saturating while others idle. Then apply a mitigation (split the key with a suffix and fan-in, cache it, or isolate it) and show the load spread.",
    "Reproduce a lost write: in async mode, acknowledge a write, drop it before it ships, kill the leader, promote a follower, and show the acknowledged write is gone. Then switch to semi-sync (one synchronous replica) and show the same scenario no longer loses the write.",
    "Implement leaderless quorum reads/writes with configurable N, W, R. Reproduce a stale read with R + W ≤ N (read set disjoint from write set), then set R + W > N and show the read always sees the latest acknowledged write.",
    "Write an architecture-decisions note: the partition key and why, the replication mode and its durability/latency trade, the quorum (N, W, R) and what it guarantees, and which CAP/PACELC point each data class sits at.",
  ]}
  acceptance={[
    "A one-paragraph brief with the dominant query, read:write ratio, and key cardinality, plus a justified partition key chosen from the access pattern.",
    "A measurement showing consistent hashing moves ~1/N of keys on a membership change versus a mod-N baseline that moves nearly all — with the actual fractions reported.",
    "A reproduced hot shard (one shard's load far above the rest under skew) and an after measurement showing the mitigation spread the load.",
    "A reproduced lost write under async failover, and a re-run under semi-sync where the same acknowledged write survives — with the exact sequence that produced each outcome.",
    "A quorum demonstration: a stale read at R + W ≤ N and a guaranteed-fresh read at R + W > N, with the N, W, R values used.",
    "A short architecture-decisions note tying each choice (key, replication mode, quorum, CAP/PACELC point) to the specific guarantee or trade it buys.",
  ]}
  stretch={[
    "Add split-brain: partition the cluster, let both sides accept writes, then heal and show the divergent histories — and implement fencing or quorum promotion that prevents the second leader from accepting writes.",
    "Add a resharding path: grow the ring from N to N+2 nodes under live writes using double-writes or change-data-capture, and show no acknowledged write is lost during the cutover.",
    "Add bounded-load consistent hashing (cap each node at c×average and spill clockwise) and show it tames the hot shard for request routing while preserving the ~1/N movement property.",
    "Add a multi-leader mode with two regions accepting writes, inject a conflicting concurrent write, and implement a conflict resolution (last-write-wins vs a version vector) — discussing which silently loses data.",
  ]}
/>

</Explanation>

<KeyTakeaway>A built store makes the unit operational: choose a partition key, route on a ring, replicate each shard — then reproduce the hot shard, the lost write, and the stale quorum read, and prove each fix by measurement.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-data-distribution-project-retrieval"
  lang="en"
  questions={[
    { q: "How do you demonstrate that consistent hashing beats mod-N on a membership change?", a: "Route keys both ways, then add (or remove) one node and measure the fraction of keys whose owner changed. With consistent hashing on a ring with virtual nodes, only ~1/N of keys change owner (one arc handed off); with hash(key) mod N, the divisor changes so nearly all keys remap. Reporting the two fractions side by side (e.g. ~6% vs ~94% for N≈16) makes the property concrete — and the same measurement explains why the mod-N cache wipes its hit rate on every scale event." },
    { q: "What's the exact sequence that produces a lost write under async failover, and how does semi-sync fix it?", a: "In async mode: accept a write and ack it to the client, but drop/delay it before it ships to any follower; kill the leader; promote the most-caught-up follower (which never received the write); read the key and observe the acknowledged write is gone. Under semi-sync (sync_replicas = 1), the ack is withheld until one follower confirms, so the acknowledged write is on two nodes before the client hears success — repeat the same kill-and-promote and the write survives because the promoted follower already had it. The takeaway: an ack is only as durable as the replication that backed it." },
    { q: "How do you show a quorum guarantees (or fails to guarantee) a fresh read?", a: "Set N replicas with configurable W and R. With R + W ≤ N (e.g. N=5, W=2, R=2), construct a case where the W nodes that took the write and the R nodes that answer the read are disjoint — the read returns stale data, proving non-overlap. Then set R + W > N (e.g. W=3, R=3, sum 6 > 5): now any read set must intersect any write set in at least one node, so the read always sees the latest acknowledged write. The demonstration is the inequality made physical: overlap is the guarantee." },
  ]}
/>

<Recap lang="en">
This project turns the data-distribution unit into something you've operated, not just read. You **choose a partition key** from the dominant access pattern and cardinality, **route keys on a consistent-hashing ring** with virtual nodes (and measure the ~1/N movement against a mod-N baseline that remaps nearly all), and **replicate each shard** in both synchronous and asynchronous modes. Then you reproduce the unit's three signature failures on purpose and close each: a **hot shard** under skew (mitigated by splitting, caching, or isolating the celebrity key), a **lost write** under async failover (fixed by semi-sync so an ack means 'on two nodes'), and a **stale quorum read** at `R + W ≤ N` (fixed by `R + W > N` so the read and write sets must overlap). The architecture-decisions note ties every choice — key, replication mode, quorum, CAP/PACELC point — to the specific guarantee it buys. An engineer who has built and broken this store once distributes data deliberately, choosing which disagreement to tolerate up front, instead of discovering a lost write or a wiped cache during a 3 a.m. failover.
</Recap>
