---
slug: "quiz-choice"
lang: en
track: "system-design"
unit: "04-data-distribution"
order: 5
title: "Data distribution: multiple-choice review"
summary: "Multiple-choice synthesis across the data-distribution unit: replication lag and lost writes, partitioning strategies and the hot shard, consistent hashing vs mod-N, and the CAP/PACELC choice per use case."
estMin: 13
status: ready
lessonType: topic
level: senior
concepts:
  - replication-lag
  - hot-shard
  - consistent-hashing
  - cap-theorem
  - pacelc
  - quorum
prereqs:
  - 04-data-distribution/01-replication
  - 04-data-distribution/02-sharding-and-partitioning
  - 04-data-distribution/03-consistent-hashing
  - 04-data-distribution/04-cap-and-pacelc
sources:
  - https://www.allthingsdistributed.com/files/amazon-dynamo-sosdp2007.pdf
  - https://jepsen.io/consistency
  - https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/HowItWorks.ReadConsistency.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each is a decision you make when you choose a replication mode, pick a partition key, route keys to nodes, or label a data store's consistency — not a definition to recite, but the trade-off a senior engineer commits to under a real failure.
</Hook>

<Crux>Six decisions: a lost write on failover, a hot shard, a reshard, the ring, a quorum, and a CAP/PACELC label per use case.</Crux>

<Explanation>

Confirm you can trace a lost write through an async failover, diagnose a hot shard from a partition key, pick consistent hashing over mod-N, compute a quorum overlap, and place a workload on the consistency spectrum with CAP and PACELC straight.

<Quiz
  id="04-data-distribution-choice-1"
  lessonSlug="quiz-choice"
  lang="en"
  question="An async-replicated database acknowledges writes #51–53 to clients, then the leader crashes having shipped only up to #50. A follower at #50 is promoted. What is the correct description?"
  choices={[
    { label: "Writes #51–53 are lost: acknowledged to clients but never replicated, so the new leader has no record of them. An ack under async replication is not a durable write; a synchronous replica would have made the ack mean 'on two nodes'.", correct: true },
    { label: "The new leader will request #51–53 from the clients.", misconception: "Clients don't hold a replayable log of acknowledged writes, and the database never asked them to. The writes existed only on the dead leader and are gone. Durability needs a synchronous replica, not client replay." },
    { label: "Nothing is lost — the success responses guarantee durability.", misconception: "That's the trap: with async replication the ack is sent before the write is durable anywhere but the leader. A crash in that window loses acknowledged writes silently." },
    { label: "The writes are safe because reads are eventually consistent.", misconception: "Eventual consistency is about reads converging, not about recovering writes that were never replicated. #51–53 left no copy to converge to; they're lost." },
  ]}
/>

<Quiz
  id="04-data-distribution-choice-2"
  lessonSlug="quiz-choice"
  lang="en"
  question="You shard users by user_id, evenly by count. One shard pins at 100% CPU while the rest idle, right after a celebrity account went viral. What's happening, and the cleanest mitigation?"
  choices={[
    { label: "A hot shard from skew: even key counts don't mean even load, and all of one celebrity key's traffic lands on its one shard. Split the hot key (suffix + fan-in), cache it, or isolate it on a dedicated shard.", correct: true },
    { label: "The shards are unbalanced by row count; rebalance row counts.", misconception: "Counts are already even — the problem is LOAD concentrated on one hot key, not row distribution. Rebalancing counts won't move the celebrity's traffic off its shard." },
    { label: "Replication lag is overloading a follower; add followers.", misconception: "This is a hot-key load problem on one shard, not replication. More followers scale reads broadly but don't relieve the single key whose traffic all routes to one shard." },
    { label: "Switch from hash to range partitioning to spread the load.", misconception: "Range wouldn't help — the celebrity's single key still lands on one shard regardless of scheme. You must spread that key specifically: split it, cache it, or isolate it." },
  ]}
/>

<Quiz
  id="04-data-distribution-choice-3"
  lessonSlug="quiz-choice"
  lang="en"
  question="A Redis cache fleet routes keys with hash(key) mod N. Adding one node (N: 16 → 17) collapses the hit rate to near zero. What's the fix and why does it work?"
  choices={[
    { label: "Use consistent hashing: place nodes and keys on a ring and route a key to the next node clockwise. Adding a node then moves only ~1/N of keys instead of remapping nearly all of them as a changed mod-N divisor does.", correct: true },
    { label: "Pre-warm the new node; only its share of keys missed.", misconception: "With mod-N, changing the divisor remaps NEARLY ALL keys, so essentially every lookup misses — not just the new node's share. Pre-warming one node can't fix a fleet-wide remap; consistent hashing prevents it." },
    { label: "Add more replicas of each cache node.", misconception: "Replicas don't change the routing function; the collapse is from mod-N remapping every key on the N change. The structural fix is consistent hashing, which decouples routing from N." },
    { label: "Increase the cache TTLs so keys live longer.", misconception: "Longer TTLs don't help when every key now hashes to the wrong node — the entries exist but are looked up on the wrong node. Consistent hashing keeps keys on their node when membership changes." },
  ]}
/>

<Quiz
  id="04-data-distribution-choice-4"
  lessonSlug="quiz-choice"
  lang="en"
  question="A leaderless store has N = 5 replicas. You want a read to be guaranteed to see the latest acknowledged write. Which (W, R) pair achieves this with the most write availability?"
  choices={[
    { label: "W = 3, R = 3 (R + W = 6 > 5): the read and write sets must overlap in at least one node, guaranteeing the read sees the latest write, while W = 3 tolerates more node loss on writes than W = 4 or 5.", correct: true },
    { label: "W = 1, R = 1 (R + W = 2): maximally available.", misconception: "R + W = 2 is not > N = 5, so the read and write sets can be disjoint — the read may miss the latest write entirely. The overlap guarantee requires R + W > N." },
    { label: "W = 5, R = 5: strongest possible.", misconception: "R + W = 10 > 5 does guarantee overlap, but W = 5 requires every replica for a write — the least write availability, the opposite of the goal. W = 3, R = 3 also overlaps with more write headroom." },
    { label: "W = 2, R = 2 (R + W = 4): good enough.", misconception: "R + W = 4 is not > 5, so the sets may not overlap and a read can miss the latest write. You need the sum strictly greater than N; the minimum here is 6, e.g. W = 3, R = 3." },
  ]}
/>

<Quiz
  id="04-data-distribution-choice-5"
  lessonSlug="quiz-choice"
  lang="en"
  question="A teammate says 'we run Cassandra, so we're AP — we gave up consistency.' Using CAP and PACELC precisely, what's the most accurate correction?"
  choices={[
    { label: "AP only describes behavior DURING a partition (serve possibly-stale data rather than refuse). The constant, everyday trade-off — PACELC's 'else' branch — is latency vs consistency: with a healthy network, a stronger read still costs coordination latency. Cassandra is PA/EL.", correct: true },
    { label: "Correct — Cassandra has no consistency at all.", misconception: "Cassandra offers tunable consistency (quorum levels per query); it isn't 'no consistency'. And AP is only the partition-time stance — the everyday trade-off is latency vs consistency (PACELC's else), which the statement omits." },
    { label: "Wrong — Cassandra is actually CP.", misconception: "Cassandra leans AP under partition (it stays available, serving possibly-stale reads). The correction isn't to flip the letter but to note that AP describes only the partition case, and PACELC's EL describes the rest." },
    { label: "It doesn't matter; CAP and PACELC are the same thing.", misconception: "They're not: CAP covers only the partition-time C-vs-A choice; PACELC adds the else-branch (latency vs consistency in normal operation), which is exactly what the teammate's statement ignores." },
  ]}
/>

<Quiz
  id="04-data-distribution-choice-6"
  lessonSlug="quiz-choice"
  lang="en"
  question="You're choosing a consistency level per data type for one product. Which assignment reflects the unit's 'pick per use case' principle?"
  choices={[
    { label: "Account balance / inventory decrement → linearizable (CP/EC); a like count or activity feed → eventual (AP/EL); a user editing their own profile → read-your-writes. Match each datum to its actual correctness need.", correct: true },
    { label: "Make everything linearizable to be safe.", misconception: "Demanding linearizability on a like count or feed buys the latency and reduced availability of CP/EC for data that doesn't need it, making the whole product slower and more fragile under partition for no benefit." },
    { label: "Make everything eventual for maximum scale.", misconception: "Running a balance or inventory on an eventually-consistent store invites double-spends and oversells during a partition. Money and inventory need linearizability; only low-stakes data should be eventual." },
    { label: "Use read-your-writes everywhere as a compromise.", misconception: "Read-your-writes is a per-client guarantee, too weak for a balance (which needs global linearizability) and overkill for a public like count (eventual is fine). One level for everything is the mistake." },
  ]}
/>

</Explanation>

<KeyTakeaway>Distributing data is choosing which disagreement you tolerate: async failover loses acked writes, a skewed key makes a hot shard, mod-N dooms a reshard, R + W > N guarantees overlap, CAP/PACELC pick consistency per case.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-data-distribution-choice-retrieval"
  lang="en"
  questions={[
    { q: "Why can an async-replicated failover lose acknowledged writes, and what prevents it?", a: "The leader can acknowledge writes it hasn't yet shipped to any follower; if it crashes in that window, the promoted follower is behind and those acknowledged writes simply don't exist (and if the old leader rejoins, they may conflict with newer writes and be discarded). The rule: under async replication an ack is NOT a durable write. Prevention: at least one synchronous replica so an ack means 'on two nodes', plus failover tooling that won't promote a too-far-behind follower and fences the old leader against split-brain." },
    { q: "Give the minimum quorum for a guaranteed-fresh read with N = 5 and the most write availability.", a: "Need R + W > N = 5, so the read and write sets overlap in at least one node and the read sees the latest acknowledged write. The minimum sum is 6; W = 3, R = 3 satisfies it while keeping the most write availability (W = 4 or 5 would tolerate fewer node failures on writes). W = 1/R = 1 fails (sum 2, sets can be disjoint, read may miss the write)." },
  ]}
/>

<Recap lang="en">
The through-line is that distributing data is a series of choices about which disagreement you can tolerate. **Replication** copies data for reads and survival, but async replication acks before durability, so a **failover** can lose acknowledged writes — keep a synchronous replica. **Sharding** splits data by a partition key, and an even key count still produces a **hot shard** under skew (the celebrity), fixed by splitting, caching, or isolating the hot key. **Consistent hashing** routes keys on a ring so adding a node moves only ~1/N of keys, where `hash mod N` would remap nearly all of them and wipe a cache. **Leaderless quorums** guarantee a fresh read when **R + W > N** (the read and write sets overlap). And **CAP** is only the partition-time C-vs-A choice while **PACELC** adds the everyday latency-vs-consistency trade — so you **pick per use case**: linearizable for money and inventory, eventual for like counts and feeds, read-your-writes for a user's own edits.
</Recap>
