---
slug: "quiz-short"
lang: en
track: "system-design"
unit: "05-caching-at-scale"
order: 6
title: "Caching at scale: free-recall review"
summary: "Free-recall prompts across the caching unit. Answer each in your own words first — strategy and consistency, eviction and hit-ratio math, sharding and the stampede, invalidation and the source-of-truth rule — then reveal the model answer and compare."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - cache-aside
  - eviction-policy
  - hit-ratio
  - consistent-hashing
  - cache-stampede
  - cache-invalidation
prereqs:
  - 05-caching-at-scale/01-caching-strategies
  - 05-caching-at-scale/02-eviction-and-ttl
  - 05-caching-at-scale/03-distributed-cache
  - 05-caching-at-scale/04-cache-invalidation
sources:
  - https://docs.aws.amazon.com/AmazonElastiCache/latest/mem-ug/Strategies.html
  - https://redis.io/docs/latest/develop/reference/eviction/
  - https://aws.amazon.com/builders-library/caching-challenges-and-strategies/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory — including the arithmetic — before you open the model answer. The effort of reconstructing the strategy tradeoff, the hit-ratio math, the stampede fix, and the source-of-truth rule is what makes them stick.
</Hook>

<Crux>Reconstruct the unit's spine: read/write strategies, eviction and TTL, hit ratio, sharding, the stampede, and invalidation.</Crux>

<Explanation>

Reconstruct the unit's spine without looking back: what each caching strategy promises, what eviction and TTL trade, why hit ratio (not size) is the metric, how to shard and survive node loss, what a stampede is and how to stop it, and why the cache is never the source of truth.

<RetrievalDrawer
  client:visible
  id="05-caching-short-retrieval"
  lang="en"
  questions={[
    { q: "Name the five caching strategies, split into read-path and write-path, and state what each promises.", a: "Read-path: cache-aside (lazy loading) — the app loads from the DB on a miss and populates the cache, caching only requested data and surviving empty nodes, but permitting stale reads; read-through — the same load-on-miss logic lives in the cache layer for consistency, at the cost of coupling. Write-path: write-through — write cache and DB synchronously, never stale but two trips and churn (pair with TTL); write-back — write the cache now and flush the DB async, lowest latency and coalesces bursts but loses un-flushed writes on a crash; write-around — write only the DB, value enters the cache lazily on the next read, no churn but a guaranteed first-read miss. You compose one read pattern and one write pattern per data class — there's no fast-fresh-durable option." },
    { q: "What do eviction and TTL each handle, what does each eviction policy bet, and why approximate LRU?", a: "Eviction handles space (memory full → delete something); TTL handles time (bound staleness). LRU bets recency predicts reuse (default, fits temporal locality); LFU bets popularity (survives one-off floods); FIFO evicts oldest-inserted regardless of use (can drop your hottest early item); random is cheap and scan-resistant. Real caches approximate LRU/LFU by sampling a few keys per eviction rather than maintaining a perfect global order — accuracy traded for hot-path speed. And never run a cache on noeviction (writes fail under pressure); use allkeys-lru/lfu." },
    { q: "Why is hit ratio the metric, and how do you compute the DB load from a hit-ratio change?", a: "The DB feels the MISS rate, not the hit rate, and the store is sized assuming the cache absorbs most reads. Miss rate = 100% − hit ratio; DB reads = total reads × miss rate. So 99%→90% hits is 1%→10% misses = a 10× increase in DB load, even though the hit ratio only moved 9 points. That asymmetry is why a small hit-ratio regression (a bad deploy, an eviction or expiry storm) can melt a database — and why you watch the hit ratio rather than the cache size in GB." },
    { q: "Why shard a cache with consistent hashing, what does replication add, and what is a cache stampede + its fixes?", a: "Consistent hashing places keys/nodes on a ring so a node change remaps only ~1/N keys (one arc), with virtual nodes evening the load — because for a cache a remap is a miss storm, and modulo (hash % N) would remap almost everything on a rescale and stampede the DB. Replication keeps per-shard copies so a node loss doesn't zero the hit ratio and reads fan out, but it's async, so replicas lag (the cache is not the source of truth). A stampede (thundering herd) is N concurrent reads recomputing the same expired hot key at once; fixes are request coalescing/single-flight (one recompute, rest wait), probabilistic early recompute (refresh ahead of expiry), and locks/leases. Hot keys saturate their owning shard (replicate or near-cache them); cold start leaves a fresh cache empty (warm + gradual restarts)." },
    { q: "State the three invalidation approaches, the write-time race and its fix, and the source-of-truth rule.", a: "Approaches, trading staleness vs coupling: TTL (write path does nothing, self-heals, bounded-stale), explicit invalidation (delete on write, near-fresh but the write path must know every derived key — a missed site stays stale forever), versioned keys (bump a version per write, race-free but leaves orphans). The write-time race: a slow read loads the old value, a writer updates the DB and deletes the key, the slow read finishes after the delete and writes the old value back — versioning avoids it because the stale write lands on an orphaned old version no new read requests; the practical default is explicit invalidation with a TTL backstop. Source-of-truth rule: the cache is a disposable copy of an authoritative durable store — you must be able to flush the whole cache anytime and recover correct answers. Also: negative-cache absence with short TTLs (invalidate on create), and don't delete-on-change a hot key (it stampedes — version or update-in-place instead)." },
  ]}
/>

</Explanation>

<KeyTakeaway>Reconstruct each answer from memory — strategy promises, the miss-rate math, consistent hashing, the stampede fix, invalidation and the source-of-truth rule — and you hold the unit's spine: caching is tradeoffs.</KeyTakeaway>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the unit's spine. The five **strategies** split read-path (cache-aside, read-through) from write-path (write-through, write-back, write-around), each promising a different mix of freshness, miss cost, and durability — composed one-of-each per data class. **Eviction** handles space (LRU is the default access-aware bet, never `noeviction` for a cache) and **TTL** handles time (jitter it). The metric is the **hit ratio**, because the DB feels the *miss* rate — 99%→90% hits is a 10× DB load spike. Scaling out needs **consistent hashing** (only ~1/N keys move on rescale) and **replication** (async, slightly stale), and must defend the **stampede** with single-flight while handling hot keys and cold start. **Invalidation** trades staleness against coupling across TTL, explicit delete, and versioned keys — versioning beats delete because it escapes the write-time race — and the deepest rule is that the cache is a **disposable copy**, never the source of truth. The thread tying it together: at this depth, caching is reasoning about tradeoffs and failure modes you choose up front, not behavior you discover in production.
</Recap>
