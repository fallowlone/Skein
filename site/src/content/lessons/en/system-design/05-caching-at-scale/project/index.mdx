---
slug: "project"
lang: en
track: "system-design"
unit: "05-caching-at-scale"
order: 8
title: "Caching at scale: build and break a cache"
summary: "Hands-on project: put a cache in front of a slow store with a chosen strategy, measure its hit ratio and the DB load behind it, then deliberately trigger and fix a stampede, a stale read, and a synchronized-expiry storm."
estMin: 240
status: ready
lessonType: topic
level: senior
concepts:
  - cache-aside
  - hit-ratio
  - cache-stampede
  - cache-invalidation
  - ttl
  - request-coalescing
prereqs:
  - 05-caching-at-scale/01-caching-strategies
  - 05-caching-at-scale/02-eviction-and-ttl
  - 05-caching-at-scale/03-distributed-cache
  - 05-caching-at-scale/04-cache-invalidation
sources:
  - https://docs.aws.amazon.com/AmazonElastiCache/latest/mem-ug/Strategies.html
  - https://redis.io/docs/latest/develop/reference/eviction/
  - https://aws.amazon.com/builders-library/caching-challenges-and-strategies/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Reading that a stampede multiplies DB load and that delete-on-write races is not the same as watching your own database fall over, then fixing it. Put a real cache in front of a deliberately slow store, measure the hit ratio and the load it hides, and then break it on purpose — a stampede, a stale read, a synchronized-expiry storm — and fix each with the technique from the unit.
</Hook>

<Crux>Build a cache over a slow store, measure hit ratio and DB load, then trigger and fix a stampede, a stale read, and an expiry storm.</Crux>

<Explanation>

This project makes the whole unit operational: you'll choose a strategy and prove the hit-ratio math against a measured database-load number, then reproduce the three signature failures — the stampede, the stale-read race, and the TTL-cliff storm — and demonstrate that the unit's fixes actually eliminate them.

<ProjectBrief
  id="05-caching-project"
  lessonSlug="project"
  lang="en"
  objective="Build a cache layer in front of a slow backing store, instrument its hit ratio and the backend load behind it, and then deliberately reproduce and fix a cache stampede, a write-time stale-read race, and a synchronized-expiry (TTL-cliff) storm — closing the loop between the unit's theory and observed behavior."
  requirements={[
    "Stand up a slow backing store (any DB, or a fake store with an artificial per-read delay you control, e.g. 50-300ms) and a cache (Redis/Memcached or an in-process map) in front of it, reading via cache-aside. State your read strategy and write strategy explicitly.",
    "Instrument and expose three counters: cache hits, cache misses, and backend reads per second. Compute and log the hit ratio continuously.",
    "Drive load with a generator that reads a realistic key distribution (a small hot set plus a long cold tail). Record the steady-state hit ratio and the backend reads/sec, and confirm backend_reads ≈ total_reads × (1 − hit_ratio).",
    "Reproduce a cache stampede: make one key hot, give it a short TTL, and ramp concurrent readers until its expiry produces a burst of simultaneous backend recomputes; capture the backend-reads spike at each expiry.",
    "Fix the stampede with request coalescing (single-flight): ensure that when N concurrent requests miss the same key, only one recomputes and the rest await its result. Show the backend spike collapses to ~1 recompute per expiry.",
    "Reproduce a write-time stale-read race: with delete-on-write invalidation and a concurrent slow read, produce a case where the old value is written back after the delete and persists. Then fix it with versioned keys (and/or a TTL backstop) and show the stale value no longer survives.",
    "Reproduce a synchronized-expiry storm: warm many keys with an identical TTL and observe the periodic backend spike when they all expire together. Then add TTL jitter and show the spike flattens into a smooth refresh.",
    "Write up each failure: the symptom you measured, the mechanism, the fix applied, and the before/after backend-load numbers proving the fix worked.",
  ]}
  acceptance={[
    "A running cache-in-front-of-store with live counters for hits, misses, and backend reads/sec, and a logged steady-state hit ratio that matches the measured backend load via backend_reads ≈ total_reads × (1 − hit_ratio).",
    "A reproduced stampede with a captured backend-reads spike at each hot-key expiry, and an after-fix measurement showing single-flight collapses it to ~1 recompute per expiry.",
    "A reproduced write-time stale-read (old value re-poisoning a key after a delete) and a fixed version using versioned keys (or a TTL backstop) where the stale value no longer persists, demonstrated by a test.",
    "A reproduced synchronized-expiry storm (periodic backend spike at the shared TTL boundary) and an after-jitter measurement showing the spike flattened.",
    "A short write-up tying each observed failure to the unit's mechanism and fix, with the before/after backend-load numbers for all three.",
  ]}
  stretch={[
    "Shard the cache across multiple nodes and compare modulo (hash % N) vs consistent hashing on a node add: measure the fraction of keys that miss immediately after the topology change and show modulo mass-misses while consistent hashing only disturbs ~1/N.",
    "Add a hot-key saturation scenario: route one extremely hot key and show a single shard saturating, then mitigate with key replication or an in-process near-cache and measure the load redistribution.",
    "Add a cold-start scenario: flush the cache under load and measure the backend-load spike and recovery time, then compare warming the cache first vs taking traffic cold.",
    "Implement probabilistic early recompute (refresh a hot key ahead of expiry) and compare it against single-flight on tail latency at the expiry boundary.",
  ]}
/>

</Explanation>

<KeyTakeaway>Build-and-break makes the unit operational: measure the hit ratio and the DB load it hides, then reproduce and fix the three failures — stampede, stale-read race, expiry storm — proving each with before/after numbers.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-caching-project-retrieval"
  lang="en"
  questions={[
    { q: "How do you prove the hit-ratio/DB-load relationship empirically?", a: "Instrument three counters — hits, misses, and backend reads/sec — and drive load with a realistic key distribution (small hot set + long cold tail). At steady state, confirm backend_reads ≈ total_reads × (1 − hit_ratio): e.g. at 99% hits and 80k reads/sec you should measure ~800 backend reads/sec, and forcing the hit ratio to 90% should measure ~8,000 — a 10× jump. Measuring it makes the asymmetry concrete: a small hit-ratio drop is a large DB-load spike." },
    { q: "How do you reproduce and fix a stampede in the load test?", a: "Make one key hot with a short TTL and ramp concurrent readers; at each expiry, capture the burst of simultaneous backend recomputes (the backend-reads spike). Then add request coalescing/single-flight so that when N requests miss the same key, only one recomputes and the rest await its result. Re-measure: the spike collapses to ~1 recompute per expiry. Optionally compare probabilistic early recompute, which refreshes the hot key before it ever goes absent." },
    { q: "How do you demonstrate the stale-read race and the synchronized-expiry storm, and their fixes?", a: "Stale-read race: with delete-on-write and a concurrent slow read, engineer the interleave where the slow read writes the old value back AFTER the delete and (with no TTL) it persists; fix with versioned keys (the stale write lands on an orphaned old version) and/or a TTL backstop, then show via a test that the stale value no longer survives. Synchronized-expiry storm: warm many keys with one identical TTL and watch the periodic backend spike when they all expire together; add TTL jitter and show the spike flattens into a smooth, spread-out refresh." },
  ]}
/>

<Recap lang="en">
This project turns the caching unit into a repeatable build-and-break workflow. First you **make the hit-ratio math real**: put a cache in front of a deliberately slow store via cache-aside, instrument hits, misses, and backend reads/sec, and confirm with measured numbers that `backend_reads ≈ total_reads × (1 − hit_ratio)` — so a 99%→90% hit-ratio drop shows up as a 10× backend-load spike. Then you **reproduce and fix the three signature failures**: the **stampede** (a hot key's expiry triggers a burst of simultaneous recomputes, collapsed to ~1 by single-flight), the **write-time stale-read race** (an old value written back after a delete, eliminated by versioned keys and a TTL backstop), and the **synchronized-expiry storm** (a periodic spike when uniformly-TTL'd keys expire together, flattened by jitter). Each failure is tied to the unit's mechanism and proven fixed with before/after backend-load numbers. The stretch goals push into distribution — modulo vs consistent hashing on a rescale, hot-key saturation, and cold start. An engineer who has built this once stops treating a cache as a magic speed-up and starts treating it as a system with measurable behavior and predictable failure modes you design against up front.
</Recap>
