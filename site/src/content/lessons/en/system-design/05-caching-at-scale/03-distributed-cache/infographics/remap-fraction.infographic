infographic chart-column-simple
data
  title Keys remapped on a 4 to 5 node scale-up
  desc For a cache a remap is a cold miss, so this is the share that instantly stampedes the DB
  values
    - label Modulo hash(key) % N
      value 80
    - label Consistent hashing (~1/N)
      value 20
theme
  palette #f43f5e #14b8a6
