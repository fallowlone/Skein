---
slug: "quiz-short"
lang: en
track: "system-design"
unit: "06-async-messaging"
order: 6
title: "Async & messaging: free-recall review"
summary: "Free-recall prompts across the async unit. Answer each in your own words first — including the mechanism and the failure mode — then reveal the model answer and compare."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - delivery-semantics
  - log-vs-broker
  - choreography-vs-orchestration
  - outbox-pattern
  - backpressure
  - idempotency
prereqs:
  - 06-async-messaging/01-message-queues
  - 06-async-messaging/02-pub-sub
  - 06-async-messaging/03-event-driven
  - 06-async-messaging/04-backpressure
sources:
  - https://aws.amazon.com/builders-library/avoiding-insurmountable-queue-backlogs/
  - https://docs.aws.amazon.com/prescriptive-guidance/latest/cloud-design-patterns/transactional-outbox.html
  - https://kafka.apache.org/documentation/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory — the mechanism and the failure it prevents — before you open the model answer. The effort of reconstruction is what makes delivery semantics, the outbox, and the backlog trap stick.
</Hook>

<Crux>Reconstruct the unit's spine: delivery semantics, queue vs log, choreography vs orchestration, the outbox, and backpressure.</Crux>

<Explanation>

Reconstruct the unit's spine without looking back: what a queue decouples and what it costs, why exactly-once is effectively-once, how a log differs from a queue, the two coordination styles, how the outbox defeats the dual write, and why an unbounded queue is a latent disaster.

<RetrievalDrawer
  client:visible
  id="06-async-short-retrieval"
  lang="en"
  questions={[
    { q: "What does a message queue decouple, and what does the decoupling cost you?", a: "It decouples producer and consumer in TIME: the producer enqueues and returns; a consumer processes later; each message goes to one consumer and is removed. This converts a synchronous availability dependency (the request fails if the downstream is down) into an asynchronous latency one (the work just arrives later), buying load levelling, failure isolation, and independent scaling. The cost: you now must reason about delivery semantics (duplicates, ordering), added end-to-end latency, eventual consistency, and a new component to operate." },
    { q: "Explain at-least-once vs at-most-once and why exactly-once is really effectively-once.", a: "The semantics come from WHEN you ack. Ack-before-process = at-most-once (no duplicates, possible loss on crash). Ack-after-process = at-least-once (no loss, possible duplicates). There's no third honest option because process and ack can't be atomic over an unreliable network. 'Exactly-once' = effectively-once: the message may be delivered more than once but its EFFECT happens once, via idempotency (applying twice == once) plus dedup on a stable business key. It's a property of your consumer, not a broker setting. Mechanics that make at-least-once run: visibility timeout (engine of redelivery — heartbeat long jobs), DLQ (sideline poison messages, alarm on depth), per-key ordering." },
    { q: "How does a log differ from a queue, and what does that enable?", a: "A queue is a delivery pipe: messages flow through and are deleted on consume; state is 'what work remains.' A log is an append-only, retained record: messages stay and each consumer tracks its own offset; state is 'where is each reader.' Because the log retains data, REPLAY (reset an offset) and BACKFILL (a new consumer starts at offset 0) are first-class — impossible on a delete-on-consume broker. Pub/sub on a log: consumer GROUPS give fan-out across groups and load-sharing within a group (a partition per consumer); ordering holds WITHIN a partition (key by entity), parallelism across partitions." },
    { q: "Define choreography vs orchestration and the rule of thumb, and what event sourcing/CQRS add.", a: "Choreography: no central brain; each service reacts to events and emits its own, so the logic is the sum of reactions — maximally decoupled, great for simple flows, but for complex processes the end-to-end logic and process state live nowhere. Orchestration: a central saga drives steps and runs compensating actions on failure — the flow is in one readable place, ideal for complex/must-not-half-complete processes. Rule: choreography for simple few-step reactions; orchestration when there are compensations or someone must answer 'where is this in the flow?'. Event sourcing stores events as the source of truth (replayed to derive state) on a retained log; CQRS splits write and read models — both worth their complexity only when audit or independent scaling justifies it." },
    { q: "What is the dual-write problem, how does the outbox solve it, and why is eventual consistency a UX problem?", a: "Dual-write: a handler must update its DB AND publish an event, two systems with no shared transaction, so a crash in the gap loses the event or emits a phantom one. The transactional outbox writes the business row and an outbox row in ONE local transaction (both or neither), then a separate relay publishes from the outbox — recovering atomicity inside the one system that has transactions. The relay is at-least-once, so it guarantees 'never lost,' not 'never duplicated' — consumers must be idempotent. Eventual consistency (the rest of the system learns the fact after it happened) leaks into the UX: design optimistic UI, processing states, and read-your-own-writes rather than pretending the system is synchronous." },
    { q: "Why is an unbounded queue a latent disaster, and what restores the feedback loop?", a: "Queue systems are bimodal: near-empty = fast mode; once arrival > service for a sustained period the backlog grows without bound and end-to-end latency climbs — and nothing looks wrong until you cross the line. Recovery is brutal: falling an hour behind needs ~2× capacity for an hour, a 10× spike takes 10× to drain, data may go stale, and an in-memory queue can OOM. It launders a brief failure into a long one. Fixes: bound every queue (full = fail fast = the backpressure signal), shed low-value load cheaply at the front door, rate-limit/isolate per tenant (shuffle-sharding), pull-based flow control with an alarm on consumer lag, and guard retries (backoff + jitter, retry budget, circuit breakers) so a retry storm doesn't amplify the overload." },
  ]}
/>

</Explanation>

<KeyTakeaway>Reconstruct each answer from memory — delivery semantics, queue vs log, the coordination styles, the outbox, and backpressure — and you hold the unit's spine: asynchrony buys decoupling and is paid for in semantics.</KeyTakeaway>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the unit's spine. A **queue** decouples producer and consumer in time, converting a synchronous availability dependency into an asynchronous latency one — paid for in **delivery semantics**: at-least-once (no loss, duplicates) is the safe default, and **exactly-once is effectively-once** via idempotency + dedup, run by the visibility timeout, DLQ, and per-key ordering. **Pub/sub** fans out, and a **log** differs from a **queue** by retaining messages with per-consumer offsets, making **replay/backfill** first-class. Multi-step flows are **choreography** (decoupled, simple) or **orchestration** (a saga with process state and compensations, for complex flows), with **event sourcing/CQRS** riding a retained log. The **outbox** defeats the **dual-write problem** by making the state change and the event one local transaction plus an async relay (never lost, possibly duplicated → idempotent consumers), and the resulting **eventual consistency** is a UX problem. Finally, every queue needs a **bound**, or it launders a brief stall into a long backlog; you bound, shed, rate-limit, and alarm on lag. The thread: at this depth, you name the tradeoff before adding the component.
</Recap>
