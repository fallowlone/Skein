---
slug: "project"
lang: en
track: "system-design"
unit: "06-async-messaging"
order: 8
title: "Async & messaging: an event-driven order pipeline"
summary: "Hands-on project: build a small event-driven order pipeline with an idempotent at-least-once consumer, a transactional outbox, and a bounded queue, then break it on purpose — kill the relay, redeliver, and overload it — to prove your guarantees hold."
estMin: 240
status: ready
lessonType: topic
level: senior
concepts:
  - delivery-semantics
  - outbox-pattern
  - idempotency
  - backpressure
  - dead-letter-queue
  - eventual-consistency
prereqs:
  - 06-async-messaging/01-message-queues
  - 06-async-messaging/02-pub-sub
  - 06-async-messaging/03-event-driven
  - 06-async-messaging/04-backpressure
sources:
  - https://aws.amazon.com/builders-library/avoiding-insurmountable-queue-backlogs/
  - https://docs.aws.amazon.com/prescriptive-guidance/latest/cloud-design-patterns/transactional-outbox.html
  - https://kafka.apache.org/documentation/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Reading that the outbox defeats the dual-write problem is not the same as watching your pipeline survive a relay crash, a redelivery storm, and an overload — and stay correct. Build a small event-driven order pipeline, then deliberately break it: kill the process between commit and publish, redeliver every message twice, and bury the consumer in load. The point isn't a polished service; it's seeing your guarantees hold (or fail) under the exact crashes this unit warned you about.
</Hook>

<Crux>Build an event-driven order pipeline (idempotent consumer + outbox + bounded queue), then break it on purpose and prove the guarantees hold.</Crux>

<Explanation>

This project makes the whole unit operational: you'll implement the patterns — at-least-once with idempotency, the transactional outbox, a bounded queue with backpressure — and then run the failure injections that separate code that *looks* correct from code that *is* correct under crashes.

<ProjectBrief
  id="06-async-project"
  lessonSlug="project"
  lang="en"
  objective="Build a minimal event-driven order pipeline — an order service that writes an order and emits an OrderPlaced event via a transactional outbox, a queue/log carrying the event, and one or more idempotent consumers (e.g. inventory, email) — then inject the failures from this unit (relay crash, redelivery, overload) and demonstrate that no event is lost, no effect is duplicated, and the system degrades gracefully under load."
  requirements={[
    "Stand up a broker or log (any: a local Kafka/Redpanda, RabbitMQ, SQS-compatible, or even a database-backed queue) and an order service with its own database. State which delivery semantic the broker gives by default.",
    "Implement the transactional outbox in the order service: on placing an order, write the order row AND an outbox row in ONE local transaction; a SEPARATE relay process reads unpublished outbox rows and publishes OrderPlaced to the broker, marking each row published only after the broker acknowledges.",
    "Implement at least one consumer (e.g. inventory decrement, or 'send confirmation') that is idempotent: dedup on a stable key (order_id) using a unique constraint or a seen-keys store, so a redelivered message is skipped and re-acked rather than reprocessed.",
    "Give the consumer's intake a BOUNDED queue (or, on a log, document and alarm on consumer lag): define the max depth/lag and what happens when it's exceeded.",
    "Add a dead-letter path: after N failed processing attempts, route a message to a DLQ (or DLQ topic) and alarm on its depth; include one deliberately 'poison' message to exercise it.",
    "Instrument: log/emit the visibility-timeout (or offset-commit) behaviour, the dedup hits, the queue depth / consumer lag, and the DLQ count, so each failure injection is observable.",
    "FAILURE INJECTION A — dual-write crash: kill the order service process AFTER the local transaction commits but BEFORE (or while) the relay publishes. Restart, and show the relay still publishes the outbox row — the event is never lost.",
    "FAILURE INJECTION B — redelivery: force each message to be delivered twice (e.g. crash a consumer after processing but before ack, or duplicate the publish). Show the idempotent consumer processes the effect exactly once.",
    "FAILURE INJECTION C — overload: drive the producer far above the consumer's drain rate. Show the bounded queue applying backpressure (fail-fast / shedding) rather than an unbounded backlog, and capture the backlog/lag curve.",
  ]}
  acceptance={[
    "A working pipeline where placing an order reliably results in exactly-effectively-once downstream effects, demonstrated end to end.",
    "Failure injection A: a recorded run (logs/screens) where the process is killed in the commit→publish gap and, after restart, the relay publishes the pending outbox row — proving 'never lost' and that the outbox, not luck, provides it.",
    "Failure injection B: a recorded run where every message is delivered twice and the consumer's dedup store shows the second delivery skipped — proving the effect happened once despite at-least-once delivery.",
    "Failure injection C: a recorded run where sustained overload triggers backpressure (rejections/shedding or bounded blocking) instead of an ever-growing backlog, with a queue-depth/lag plot and the chosen bound stated.",
    "A short write-up: the delivery semantic you started from, exactly where the outbox restores atomicity, why the consumer is idempotent, and what your bound + DLQ + lag alarm protect against — plus one paragraph on the eventual-consistency window a user would experience and how you'd surface it in a UI.",
  ]}
  stretch={[
    "Add a second consumer group (analytics) on the same stream and demonstrate fan-out — both groups see every event independently — and, on a log, replay the analytics consumer from offset 0 to backfill.",
    "Add a retry storm: enable aggressive client retries during overload and show the queueing collapse accelerate, then tame it with exponential backoff + jitter and a circuit breaker, comparing the two backlog curves.",
    "Turn the flow into a small orchestration (saga): coordinate order → charge → reserve with a coordinator that runs a compensating action (refund/release) when a later step fails, and show the process state is queryable ('where is order #X?').",
    "Add multi-tenant fairness: shuffle-shard tenants across a small pool of queues and show that one tenant's spike no longer starves the others.",
  ]}
/>

</Explanation>

<KeyTakeaway>A pipeline plus three failure injections makes the unit operational: the outbox proves 'never lost' across a commit→publish crash, idempotency proves 'once-effect' across redelivery, a bounded queue degrades gracefully.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="06-async-project-retrieval"
  lang="en"
  questions={[
    { q: "What does each of the three failure injections prove, and why inject them?", a: "A — killing the process in the commit→publish gap proves the OUTBOX guarantees 'never lost': after restart the relay still publishes the committed outbox row (the event exists iff the order did). B — delivering every message twice proves IDEMPOTENCY turns at-least-once delivery into once-effect: the dedup store shows the second delivery skipped. C — sustained overload proves a BOUNDED queue degrades gracefully (backpressure / shedding) instead of an unbounded backlog. You inject them because code that looks correct on the happy path only proves its guarantees under the exact crashes this unit warned about." },
    { q: "Where exactly does the outbox restore atomicity, and what must the consumer still do?", a: "The order row and the outbox row are written in ONE local database transaction, so they commit or roll back together — atomicity recovered inside the one system that has transactions. A SEPARATE relay then publishes from the outbox at-least-once (it may crash after publishing but before marking the row done, re-publishing on restart). So the guarantee is 'never lost,' not 'never duplicated' — the consumer must still be idempotent (dedup on order_id) to make the inevitable duplicates harmless." },
  ]}
/>

<Recap lang="en">
This project turns the async unit into a repeatable workflow: build a minimal **event-driven order pipeline** — an order service that writes the order and an outbox row in **one local transaction**, a **relay** that publishes `OrderPlaced` from the outbox, a broker/log, and an **idempotent at-least-once consumer** that dedups on `order_id` — then make every guarantee earn its keep by breaking the system on purpose. **Injection A** kills the process in the commit→publish gap and shows the relay still publishes after restart (the **outbox** = never lost). **Injection B** delivers every message twice and shows the dedup store skip the second (idempotency = once-effect). **Injection C** overloads the consumer and shows a **bounded queue** apply backpressure instead of an unbounded backlog, with a **DLQ** for poison messages and a **lag alarm** as early warning. The write-up ties it together: the delivery semantic you started from, where atomicity is restored, why the consumer is idempotent, and how you'd surface the **eventual-consistency** window in a UI. An engineer who has built and broken this once designs async systems for the crash, not just the happy path — which is the entire point of this unit.
</Recap>
