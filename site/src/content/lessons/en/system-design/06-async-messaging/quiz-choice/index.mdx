---
slug: "quiz-choice"
lang: en
track: "system-design"
unit: "06-async-messaging"
order: 5
title: "Async & messaging: multiple-choice review"
summary: "Multiple-choice synthesis across the async unit: delivery semantics and effectively-once, queue vs log and replay, choreography vs orchestration, the outbox against dual writes, and the unbounded-queue backlog trap."
estMin: 13
status: ready
lessonType: topic
level: senior
concepts:
  - delivery-semantics
  - log-vs-broker
  - choreography-vs-orchestration
  - outbox-pattern
  - backpressure
  - idempotency
prereqs:
  - 06-async-messaging/01-message-queues
  - 06-async-messaging/02-pub-sub
  - 06-async-messaging/03-event-driven
  - 06-async-messaging/04-backpressure
sources:
  - https://aws.amazon.com/builders-library/avoiding-insurmountable-queue-backlogs/
  - https://docs.aws.amazon.com/prescriptive-guidance/latest/cloud-design-patterns/transactional-outbox.html
  - https://kafka.apache.org/documentation/
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each is a decision you make when you put a queue in a design, choose a coordination style, or review an async pipeline — not a definition to recite, but the judgement calls behind delivery semantics, the outbox, and the backlog trap.
</Hook>

<Crux>Six decisions across the unit: a delivery guarantee, queue vs log, a coordination style, the outbox, and an unbounded-queue disaster.</Crux>

<Explanation>

Confirm you can pick a delivery semantic and make it effectively-once, choose queue vs log from a replay requirement, place a process on the choreography/orchestration axis, recognize and fix a dual write with the outbox, and reject an unbounded queue from its backlog behaviour.

<Quiz
  id="06-async-choice-1"
  lessonSlug="quiz-choice"
  lang="en"
  question="A vendor advertises 'exactly-once delivery' for its queue. What's the accurate way to read that, and what must your consumer still do?"
  choices={[
    { label: "Read it as effectively-once: the message may be delivered more than once, but its effect can happen once if your consumer is idempotent (dedup on a stable business key). You must still design for duplicates.", correct: true },
    { label: "Take it literally — turn off your dedup logic since the broker guarantees no duplicates.", misconception: "No broker can make delivery exactly-once over an unreliable network, because process and ack can't be atomic. Removing dedup means a redelivery double-acts. 'Exactly-once' is effectively-once, enforced in your consumer." },
    { label: "It means at-most-once, so you may lose messages but never duplicate.", misconception: "Exactly-once marketing means the EFFECT happens once, not that messages are dropped. At-most-once (lossy) is a different, weaker guarantee. The mechanism is idempotency + dedup over at-least-once delivery." },
    { label: "It's impossible, so the feature is fake and useless.", misconception: "It's real and useful — it bundles broker-side dedup with delivery to approximate once-effect — but the guarantee is effectively-once, and your consumer must still be idempotent to be safe. Don't dismiss it; just read it correctly." },
  ]}
/>

<Quiz
  id="06-async-choice-2"
  lessonSlug="quiz-choice"
  lang="en"
  question="You must onboard a new consumer that reprocesses the last 14 days of events to build a fresh view. Which messaging substrate supports this directly?"
  choices={[
    { label: "A log-based system (Kafka/Kinesis) with ≥14-day retention: the new consumer group starts at an old offset and replays, because the log retains messages independent of consumption.", correct: true },
    { label: "A classic broker (RabbitMQ/SNS): pub/sub keeps every message for every future subscriber.", misconception: "A broker deletes each copy on ack; retention isn't its job, and a subscription that didn't exist 14 days ago has nothing to replay. Backfill needs a retained, replayable log." },
    { label: "Any queue — just re-enqueue the old events from the producer.", misconception: "The producer no longer holds 14 days of past events; they were transient. Re-deriving and re-publishing history is exactly the manual pain a log avoids. Use a log with enough retention." },
    { label: "Neither — historical reprocessing isn't possible in messaging.", misconception: "It's first-class on a log: a new consumer reads from offset 0 (or a timestamp) and replays. The log model exists precisely to make backfill and reprocessing easy." },
  ]}
/>

<Quiz
  id="06-async-choice-3"
  lessonSlug="quiz-choice"
  lang="en"
  question="A 7-step order process needs compensating actions on failure (refund, release stock), and on-call must answer 'where is order #X right now?'. Which coordination style fits?"
  choices={[
    { label: "Orchestration: a central saga drives the steps, holds the process state (so 'where is it?' is answerable), and runs compensations on failure — right for a complex, must-not-half-complete flow.", correct: true },
    { label: "Choreography, because more decoupling is always better.", misconception: "Decoupling isn't free here: across 7 reacting services the end-to-end logic and process state live nowhere, and compensations scatter. That's exactly the case orchestration exists to handle." },
    { label: "Choreography, then reconstruct the flow from logs across all 7 services when asked.", misconception: "Per-query log archaeology across 7 services is the symptom of missing orchestration. A coordinator holds the process state directly instead of forcing you to rebuild it each time." },
    { label: "Synchronous request/response, so the flow is traceable.", misconception: "Synchronous chaining reintroduces the availability coupling async removed (one slow step blocks all) and still needs compensations. Orchestration keeps async decoupling while centralizing the flow." },
  ]}
/>

<Quiz
  id="06-async-choice-4"
  lessonSlug="quiz-choice"
  lang="en"
  question="A handler does `db.commit(order)` then `broker.publish(event)`; occasionally the order is saved but the event never fires. What's the bug and the standard fix?"
  choices={[
    { label: "The dual-write problem — two systems, no shared transaction, a crash in the gap loses the event. Fix with the transactional outbox: insert the event into an outbox table in the SAME local transaction as the order, then a separate relay publishes it.", correct: true },
    { label: "A flaky network; wrap publish in retries.", misconception: "If the process dies before publish runs at all, there's nothing to retry — the order is already committed. You need the event persisted atomically with the order (outbox), not just retried." },
    { label: "Publish before committing instead; then it always fires.", misconception: "That flips it to a phantom event — a crash or rollback after publishing emits an event for an order that doesn't exist. Neither order is safe; the outbox makes both writes one transaction." },
    { label: "Use a two-phase commit across the DB and broker.", misconception: "Most brokers don't support 2PC with your DB, and 2PC is slow and fragile (coordinator failure blocks participants). The outbox is the standard lighter answer." },
  ]}
/>

<Quiz
  id="06-async-choice-5"
  lessonSlug="quiz-choice"
  lang="en"
  question="A pipeline uses an unbounded queue 'so we never drop events.' A downstream stalls for 30 minutes at peak. What's the most likely result, and the root fix?"
  choices={[
    { label: "A huge backlog silently accumulates, then takes hours to drain (recovery needs ~2× capacity for as long as you were behind) and may be stale/useless — a brief incident becomes a long one. Root fix: bound the queue (full = fail fast = backpressure) and shed low-value load.", correct: true },
    { label: "Nothing — the queue protects you; events just arrive slightly late.", misconception: "Under sustained arrival > service the backlog grows without bound, and a 10× backlog takes 10× the spike's duration to drain. 'Slightly late' becomes hours late and possibly worthless. The unbounded queue manufactures a longer outage." },
    { label: "Producers automatically slow down when the queue is full.", misconception: "An unbounded queue is never full, so it never pushes back — that's the bug. Backpressure only happens when the queue is bounded and the enqueue fails fast." },
    { label: "Just add consumers after the incident to catch up.", misconception: "That helps drain but doesn't prevent the disaster: during the stall the unbounded queue already hid the overload and ballooned the backlog. You need a bound + load shedding to keep the failure small." },
  ]}
/>

<Quiz
  id="06-async-choice-6"
  lessonSlug="quiz-choice"
  lang="en"
  question="On a Kafka topic with 6 partitions, you need each account's events processed in order while parallelizing across accounts. How do you key and consume?"
  choices={[
    { label: "Partition by account_id so each account's events stay ordered within one partition, and run a consumer group with up to 6 consumers so different partitions process in parallel.", correct: true },
    { label: "Random partition per message to balance load, and reorder by timestamp.", misconception: "Random partitioning scatters an account's events across partitions with no cross-partition order, and timestamp reordering is fragile and stateful. Key by account_id to keep per-account order." },
    { label: "One partition for guaranteed global order.", misconception: "One partition gives total order but kills parallelism — only one consumer can process it. Key by account_id over multiple partitions: order within an account, parallelism across accounts." },
    { label: "Add more consumers than partitions for more parallelism.", misconception: "Consumers beyond the partition count sit idle — each partition is owned by exactly one consumer in the group. Parallelism is capped at the partition count; add partitions, not surplus consumers." },
  ]}
/>

</Explanation>

<KeyTakeaway>Async design is a sequence of honest tradeoffs: at-least-once + idempotency for effectively-once, a log for replay, orchestration for complex flows, the outbox against dual writes, and a bound on every queue.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="06-async-choice-retrieval"
  lang="en"
  questions={[
    { q: "Why is 'exactly-once delivery' really effectively-once, and what does that demand of you?", a: "Process and ack can't be made atomic over an unreliable network, so a crash always risks a duplicate or a loss — no broker can deliver exactly-once. 'Exactly-once' means the EFFECT happens once: you accept at-least-once delivery and make your consumer idempotent (dedup on a stable business key) so redeliveries are harmless. The duplicates are guaranteed eventually; your code is where once-effect is enforced." },
    { q: "How does the outbox defeat the dual-write problem, and what guarantee does it give?", a: "A handler must update its DB and publish an event, but they're separate systems with no shared transaction — a crash in the gap loses the event or emits a phantom one. The transactional outbox writes the business row and an outbox row in ONE local transaction (both or neither), then a separate relay publishes from the outbox. It recovers atomicity by keeping both writes in the system that has transactions. The relay publishes at-least-once, so the guarantee is 'never lost,' not 'never duplicated' — consumers must still be idempotent." },
  ]}
/>

<Recap lang="en">
The through-line of this unit is that asynchrony buys decoupling and pays in semantics you must design around. A queue turns a synchronous availability dependency into an asynchronous latency one, but delivery is honestly **at-least-once** (or lossy at-most-once), so **exactly-once is effectively-once** — idempotent consumers with a dedup key. **Pub/sub** fans out across subscribers, and the deep split is **broker vs log**: a log retains messages and tracks per-consumer offsets, making **replay and backfill** first-class while a delete-on-consume broker can't. Coordinating multi-step processes is **choreography** (decoupled, simple flows) vs **orchestration** (a saga that holds process state and compensations — for complex flows). The **transactional outbox** defeats the **dual-write problem** by making the state change and the event one local transaction with an async relay, guaranteeing "never lost, possibly duplicated." And every queue needs a **bound**: an unbounded queue is a latent disaster that launders a brief stall into a long backlog, so you bound, shed load, rate-limit per tenant, and alarm on lag. At this depth, the senior move is to name the tradeoff before adding the component.
</Recap>
