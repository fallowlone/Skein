---
slug: "quiz-choice"
lang: en
track: "system-design"
unit: "01-scalability"
order: 5
title: "Scalability: multiple-choice review"
summary: "Multiple-choice synthesis across the scalability unit: Little's Law and the queueing curve, vertical vs horizontal tradeoffs and the USL, back-of-envelope arithmetic, and the latency numbers used to reject designs."
estMin: 13
status: ready
lessonType: topic
level: senior
concepts:
  - littles-law
  - queueing-delay
  - universal-scalability-law
  - back-of-envelope
  - latency-hierarchy
  - capacity-planning
prereqs:
  - 01-scalability/01-latency-vs-throughput
  - 01-scalability/02-vertical-vs-horizontal
  - 01-scalability/03-back-of-envelope
  - 01-scalability/04-numbers-to-know
sources:
  - https://highscalability.com/google-pro-tip-use-back-of-the-envelope-calculations-to-choo/
  - https://aws.amazon.com/compare/the-difference-between-throughput-and-latency/
  - https://www.allthingsdistributed.com/2006/03/a_word_on_scalability.html
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each is a decision you make when you size a service, review a design doc, or estimate a system on a whiteboard — not a definition to recite, but the arithmetic of Little's Law, the USL, and the latency numbers under real load.
</Hook>

<Crux>Six decisions across the unit: a throughput ceiling, the queueing knee, the USL curve, an estimate, and a design rejected on latency.</Crux>

<Explanation>

Confirm you can apply Little's Law to find a throughput ceiling, read the queueing curve, place a workload on the vertical/horizontal axis with the USL in mind, run a back-of-envelope estimate to one significant figure, and reject a design from the latency numbers alone.

<Quiz
  id="01-scalability-choice-1"
  lessonSlug="quiz-choice"
  lang="en"
  question="A service is capped at 150 concurrent in-flight requests by its connection pool, and each request averages 30 ms end to end. By Little's Law, what is its maximum sustainable throughput?"
  choices={[
    { label: "5,000 RPS — λ = L / W = 150 / 0.030 s = 5,000.", correct: true },
    { label: "150 RPS — one request per pool slot per second.", misconception: "That ignores W. Each slot turns over every 30 ms, serving ~33 requests/sec, not 1. λ = L / W = 150 / 0.030 = 5,000 RPS." },
    { label: "4,500 RPS — 150 × 30.", misconception: "You multiplied concurrency by latency in ms. Little's Law divides, with W in seconds: 150 / 0.030 = 5,000 RPS." },
    { label: "Can't tell without the CPU count.", misconception: "Little's Law needs only L and W — it's an accounting identity with no hardware assumption. λ = L / W = 5,000 RPS." },
  ]}
/>

<Quiz
  id="01-scalability-choice-2"
  lessonSlug="quiz-choice"
  lang="en"
  question="At 90% utilization your p99 latency is ~10× the unloaded service time, and the team wants to push to 98% to 'use the hardware fully.' What does the queueing curve predict?"
  choices={[
    { label: "Latency goes vertical: W = 1/(μ−λ), so at 98% the denominator is ~0.02μ and the tail is ~50× the unloaded time — the last slice of throughput costs unbounded latency. Keep headroom (60–70%), don't squeeze to 98%.", correct: true },
    { label: "Latency rises linearly, so 98% costs only ~9% more than 90%.", misconception: "Queueing delay is non-linear: W = 1/(μ−λ). Going from 90% to 98% roughly quintuples the tail (10× → ~50× unloaded), not a linear 9% bump. That's the knee of the curve." },
    { label: "Nothing changes — utilization doesn't affect latency until 100%.", misconception: "The tail grows steeply well before 100%. At 90% it's already ~10× unloaded; at 98% ~50×. The whole point of the curve is that latency explodes near the limit, not at it." },
    { label: "Throughput will rise 8%, which is worth it.", misconception: "You may gain a little throughput, but at a latency cost that's non-linear and often violates SLOs. Mature systems trade that last slice for headroom precisely because the tail goes vertical." },
  ]}
/>

<Quiz
  id="01-scalability-choice-3"
  lessonSlug="quiz-choice"
  lang="en"
  question="A stateless API tier scales out linearly to ~30 nodes, then total throughput plateaus and finally dips slightly past ~45 nodes. By the Universal Scalability Law, what's happening?"
  choices={[
    { label: "The coherency term β (cross-node coordination, growing as N(N−1)) is overtaking the work each new node adds, making the curve retrograde — past the peak, more nodes lower total throughput.", correct: true },
    { label: "Pure contention (α) — Amdahl's serial fraction.", misconception: "Contention (α) makes throughput plateau at a ceiling, but it never goes DOWN. A throughput that falls as you add nodes is the signature of the coherency term β, which grows quadratically." },
    { label: "The load balancer is misconfigured; throughput can't fall when you add capacity.", misconception: "It can, and that's the USL's β term: past a peak, coordination cost exceeds the work each node adds, so the cluster slows. A 50-node cluster can trail a 30-node one." },
    { label: "Latency rose, which always lowers throughput.", misconception: "More nodes don't inherently raise per-request latency; the symptom is falling AGGREGATE throughput, which the USL attributes to coherency β, not to latency." },
  ]}
/>

<Quiz
  id="01-scalability-choice-4"
  lessonSlug="quiz-choice"
  lang="en"
  question="Estimating to one significant figure: 50M daily active users each make 20 requests/day. What is the average QPS, and what peak should you provision for?"
  choices={[
    { label: "~10,000 QPS average (10^9 requests / ~10^5 seconds); provision ~30,000 for a 3× peak.", correct: true },
    { label: "~1,000,000 QPS — 50M × 20.", misconception: "That's requests per DAY (10^9), not per second. Divide by ~86,400 s/day (≈10^5): 10^9 / 10^5 = ~10,000 QPS average. Then apply a peak multiplier." },
    { label: "~10,000 QPS, and provision for exactly that.", misconception: "The average is right, but sizing for the average is the classic error — traffic is bursty. Provision for the peak (~3× average) so you survive the evening spike." },
    { label: "~580 QPS — divide by seconds and by 20.", misconception: "You divided twice. Requests/day = 50M × 20 = 10^9; ÷ 86,400 ≈ 10,000 QPS. Don't divide by the per-user rate again — it's already in the 10^9." },
  ]}
/>

<Quiz
  id="01-scalability-choice-5"
  lessonSlug="quiz-choice"
  lang="en"
  question="A design doc proposes serving 50,000 reads/second by random-seeking records from a spinning HDD. Using the latency numbers, what is the verdict, with the binding limit?"
  choices={[
    { label: "Reject it: an HDD does ~100 random seeks/sec, so 50,000/sec needs ~500 disks — the binding limit is seek RATE, not capacity. Serve from RAM/SSD or add a cache.", correct: true },
    { label: "Fine — sequential HDD throughput is ~150 MB/s, plenty for 50K reads.", misconception: "The design says RANDOM seeks, which are bound by the ~10 ms seek time (~100/sec), not sequential bandwidth. The sequential-vs-random gap is the trap; the design needs ~500 disks." },
    { label: "Fine if records are small enough.", misconception: "Record size affects bandwidth, but the binding limit is the seek RATE: ~100 random seeks/sec/HDD regardless of size. 50K/sec fails by ~500×." },
    { label: "Reject it, because RAM is ~10× faster than disk.", misconception: "Correct verdict, wrong ratio: RAM random reads are ~10^5× faster than an HDD seek, and the disk's limit is ~100 random seeks/sec. The factor is enormous, not 10×." },
  ]}
/>

<Quiz
  id="01-scalability-choice-6"
  lessonSlug="quiz-choice"
  lang="en"
  question="Your single write-primary database is at 70% CPU on the second-largest instance type, and traffic grows 8%/month. Which plan reflects the vertical-scaling lesson?"
  choices={[
    { label: "Bump once as a stopgap, but start the scale-out work now (read replicas, then sharding) — you can already see the top of the instance ladder, and the migration takes longer than the runway the last bump buys.", correct: true },
    { label: "Just keep bumping the instance each quarter; there's always a bigger box.", misconception: "There isn't — you're one step from the largest instance, with nowhere to go after that. Discovering the ceiling during an outage is the exact failure the vertical-scaling lesson warns against." },
    { label: "Switch entirely to horizontal scaling for the write primary today, mid-incident.", misconception: "Sharding a write primary is a major migration you start deliberately, not under fire. The right move is a stopgap bump WHILE you begin scale-out, before you hit the ceiling." },
    { label: "Do nothing; 70% CPU is fine.", misconception: "At 8%/month the remaining headroom is ~4–6 months, and you're near the top instance. Waiting guarantees you hit the ceiling with no runway for the migration." },
  ]}
/>

</Explanation>

<KeyTakeaway>Scaling is arithmetic done before you build: Little's Law for the ceiling, the queueing curve for headroom, the USL for the coordination tax, an estimate for the bottleneck, latency numbers to reject designs.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="01-scalability-choice-retrieval"
  lang="en"
  questions={[
    { q: "Why keep utilization at 60–70% instead of pushing to 98%?", a: "Queueing delay is non-linear: W = 1/(μ−λ). At 90% the tail is ~10× the unloaded time; at 98% the denominator is ~0.02μ and the tail is ~50×. The last slice of throughput costs unbounded latency, so mature systems trade it for 30–40% headroom rather than squeeze the hardware." },
    { q: "How do you reject a 50K-reads/sec spinning-disk design in one sentence?", a: "An HDD is bound by its ~10 ms seek to ~100 random seeks/sec, so 50,000/sec needs ~500 disks — the binding limit is seek RATE, not capacity or sequential bandwidth. Serve from RAM/SSD or add a cache. No benchmark needed; the latency number kills it." },
  ]}
/>

<Recap lang="en">
The through-line is that scaling is arithmetic you do before you build. Little's Law (λ = L/W) gives a throughput ceiling from concurrency and latency; the queueing curve (W = 1/(μ−λ)) makes the tail go vertical near the limit, so you keep 30–40% headroom instead of squeezing to 98%. Vertical scaling is simple but capped, superlinear in cost, and a SPOF — so you bump as a stopgap while starting scale-out before the ceiling; horizontal scaling is elastic but pays the USL coordination tax (α plateaus, β makes the curve retrograde). Back-of-envelope estimates (requests/day ÷ ~10^5, sized for a 3× peak, rounded to one significant figure) tell you which resource breaks first, and the latency numbers (~100 random seeks/sec on HDD, ~150 ms cross-region) let you reject a bad design in a sentence — no benchmark required.
</Recap>
