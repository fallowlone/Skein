infographic sequence-ascending-steps
data
  title If a nanosecond were a second
  desc Latency stretched x10^9 — each tier dwarfs the one below
  order asc
  sequences
    - label L1 cache ~1 ns - 1 second
    - label RAM ~100 ns - ~2 minutes
    - label SSD read ~16 us - ~4.5 hours
    - label DC round trip ~0.5 ms - ~6 days
    - label Disk seek ~10 ms - ~4 months
    - label Cross-region ~150 ms - ~5 years
theme
  palette #14b8a6 #6366f1 #f59e0b #f43f5e
