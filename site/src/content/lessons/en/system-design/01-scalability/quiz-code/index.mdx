---
slug: "quiz-code"
lang: en
track: "system-design"
unit: "01-scalability"
order: 7
title: "Scalability: numbers and config reading"
summary: "Read real capacity math, config, and code, then do the arithmetic: a Little's Law ceiling, a queueing budget, an estimation helper, and a latency check that rejects a design."
estMin: 14
status: ready
lessonType: topic
level: senior
concepts:
  - littles-law
  - queueing-delay
  - back-of-envelope
  - latency-hierarchy
  - capacity-planning
  - significant-figures
prereqs:
  - 01-scalability/01-latency-vs-throughput
  - 01-scalability/03-back-of-envelope
  - 01-scalability/04-numbers-to-know
sources:
  - https://highscalability.com/google-pro-tip-use-back-of-the-envelope-calculations-to-choo/
  - https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/
  - https://github.com/donnemartin/system-design-primer
---
import Hook from "~/components/lesson/Hook.astro";
import Crux from "~/components/prose/Crux.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Scaling bugs live in the numbers, not the prose: a pool size, a utilization target, a rounding step, a per-request call to a far-away region. Read each snippet, do the arithmetic in your head, and pick the answer a senior engineer would commit to.
</Hook>

<Crux>Read the config, find L/W/μ/λ or the access pattern, do the arithmetic, and pick the fix that respects the math — not the one that hides it.</Crux>

<Explanation>

Practise the loop you run during a design or capacity review: locate the numbers in the code, apply Little's Law or the queueing curve or the latency hierarchy, and choose the change that the arithmetic actually supports.

## Snippet 1 — the pool sizing helper

```python
def max_throughput_rps(pool_size: int, avg_latency_ms: float) -> float:
    # Little's Law: λ = L / W, with W in seconds
    return pool_size / (avg_latency_ms / 1000)

print(max_throughput_rps(pool_size=200, avg_latency_ms=40))   # what prints?
```

<Quiz
  id="01-scalability-code-1"
  lessonSlug="quiz-code"
  lang="en"
  question="What does max_throughput_rps(200, 40) return, and what does it mean?"
  choices={[
    { label: "5,000 — the ceiling is λ = L / W = 200 / 0.040 s = 5,000 RPS; beyond this the pool saturates and requests queue.", correct: true },
    { label: "8,000 — 200 × 40.", misconception: "That multiplies; Little's Law divides. λ = L / W = 200 / (40/1000) = 200 / 0.04 = 5,000 RPS. Multiplying concurrency by latency has no meaning here." },
    { label: "5 — 200 / 40, treating latency as seconds.", misconception: "The latency is in milliseconds; the helper divides by 1000 to get seconds. 200 / 0.04 = 5,000 RPS, not 5. Watch the units — that's the whole point of the /1000." },
    { label: "200 — the pool size is the throughput limit.", misconception: "The pool size is L (concurrency), not throughput. Each slot turns over every 40 ms, so λ = L / W = 5,000 RPS. The pool serves far more than one request per slot per second." },
  ]}
/>

## Snippet 2 — the utilization target

```yaml
# autoscaler config
target_cpu_utilization: 0.95   # "use the hardware fully"
# unloaded service time ~20 ms; SLO is p99 <= 200 ms
```

<Quiz
  id="01-scalability-code-2"
  lessonSlug="quiz-code"
  lang="en"
  question="The team set the autoscaler target to 95% CPU to save cost. Using the queueing model, what's the risk and the fix?"
  choices={[
    { label: "At 95% utilization, W = 1/(μ−λ) makes latency ~20× the unloaded time → ~400 ms, blowing the 200 ms p99 SLO. Lower the target to ~60–70% so the autoscaler adds capacity before the queueing knee.", correct: true },
    { label: "No risk — 95% is efficient and latency only matters at 100%.", misconception: "Latency goes vertical well before 100%. At 95%, the M/M/1 tail is ~20× the unloaded 20 ms ≈ 400 ms, past the 200 ms SLO. Headroom isn't waste; it's how you hold the tail." },
    { label: "Raise the target to 99% to be even more efficient.", misconception: "That makes it far worse: at 99% the tail is ~100× unloaded ≈ 2 s. The fix is the opposite direction — target ~60–70% so scaling kicks in before the curve goes vertical." },
    { label: "The SLO is wrong; relax it to 500 ms.", misconception: "Relaxing the SLO to fit a bad utilization target is backwards. Set the target to keep headroom (~60–70%) and meet the SLO you actually need; don't move the goalpost to excuse the queueing knee." },
  ]}
/>

## Snippet 3 — the estimation helper

```python
def daily_to_qps(daily_requests: float, peak_multiplier: float = 3) -> dict:
    avg = daily_requests / 86_400
    return {"avg_qps": round(avg), "peak_qps": round(avg * peak_multiplier)}

# 1.5 billion requests/day
print(daily_to_qps(1_500_000_000))
```

<Quiz
  id="01-scalability-code-3"
  lessonSlug="quiz-code"
  lang="en"
  question="Roughly what does daily_to_qps(1_500_000_000) return, and what's the right way to read it?"
  choices={[
    { label: "~17,000 avg and ~52,000 peak. Read it to one significant figure: ~20K average, ~50K peak — size the system for the ~50K peak, not the average.", correct: true },
    { label: "~17,361 avg and ~52,083 peak — provision for exactly 17,361.", misconception: "Two errors: false precision (the input is a guess, so carry one significant figure — ~20K/~50K) and sizing for the average. You provision for the ~50K PEAK, not the average." },
    { label: "~1,500,000,000 QPS — that's the request rate.", misconception: "That's requests per DAY. The helper divides by 86,400 to get per second: ~17,000 QPS average. Always convert daily totals to per-second before reasoning about load." },
    { label: "~17,000 QPS, and the peak_multiplier is unnecessary.", misconception: "The peak multiplier is the most important part: traffic is bursty, so you size for the ~50K peak. Dropping it is exactly the average-vs-peak mistake that takes systems down at the spike." },
  ]}
/>

## Snippet 4 — the per-request call

```python
# service runs in us-east-1 (Virginia); this store is in eu-central-1 (Frankfurt)
def handle_request(req):
    user = remote_store.get(req.user_id)      # Virginia -> Frankfurt -> Virginia
    prefs = remote_store.get(req.prefs_id)    # Virginia -> Frankfurt -> Virginia
    return render(user, prefs)                # two synchronous cross-region calls
```

<Quiz
  id="01-scalability-code-4"
  lessonSlug="quiz-code"
  lang="en"
  question="Using the latency numbers, what is this handler's latency floor, and what's the right fix?"
  choices={[
    { label: "~300 ms floor — each cross-region round trip is ~150 ms, and there are two in series. Fix by co-locating a replica in us-east-1 (or batching into one call and moving the data closer); caching the result can't beat the speed of light on a miss.", correct: true },
    { label: "~1 ms — the calls are just reads, which are fast.", misconception: "The reads themselves are cheap, but each is a ~150 ms transatlantic round trip. Two in series ≈ 300 ms floor, set by the speed of light, not by how 'fast' the read is." },
    { label: "~300 ms, fixed by adding more CPU to the service.", misconception: "The floor is network propagation across an ocean, not CPU. Faster CPUs don't shorten the path. You must move the data into the service's region or stop calling cross-region per request." },
    { label: "~150 ms — only the first call counts.", misconception: "The two calls are sequential, so their round trips add: ~150 ms + ~150 ms ≈ 300 ms. Batching them into one round trip would cut it to ~150 ms; co-locating removes the cross-region cost entirely." },
  ]}
/>

</Explanation>

<KeyTakeaway>Every scaling decision reduces to arithmetic in the config: λ = L/W for the ceiling, W = 1/(μ−λ) for the headroom, daily ÷ 86,400 × peak for the load, and ~150 ms × hops for cross-region. Read the numbers, not the prose.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-scalability-code-retrieval"
  lang="en"
  questions={[
    { q: "Given a pool size and average latency, how do you compute the throughput ceiling, and what's the unit trap?", a: "λ = L / W (Little's Law), with W in SECONDS. The classic bug is leaving W in milliseconds: 200 / 40 = 5 is wrong; 200 / 0.040 = 5,000 RPS is right. Always convert latency to seconds before dividing. The pool size is L (concurrency), not the throughput — each slot turns over every W seconds." },
    { q: "Why is a 95% CPU autoscaler target dangerous, and what target holds an SLO?", a: "Queueing delay is non-linear: W = 1/(μ−λ). At 95% the tail is ~20× the unloaded time (e.g. 20 ms → ~400 ms), past a 200 ms p99 SLO; at 99% it's ~100× (~2 s). Target ~60–70% so the autoscaler adds capacity before the curve goes vertical. Headroom is how you meet the tail SLO, not wasted hardware." },
    { q: "What's the latency floor of two synchronous cross-region calls in series, and the fix?", a: "Each Virginia↔Frankfurt round trip is ~150 ms (speed of light over ~9,000 km each way), and two in series add to ~300 ms — a floor caching can't beat on a miss. Fix by co-locating a replica in the service's region, or batch into one round trip (~150 ms) and ideally make non-critical calls async. The numbers reject the per-request cross-region pattern on sight." },
  ]}
/>

<Recap lang="en">
Every scaling decision in this unit reduces to arithmetic you can read straight off the config. **Little's Law** (λ = L/W, with W in seconds) turns a pool size and latency into a throughput ceiling — and the unit trap is forgetting to convert milliseconds to seconds. The **queueing curve** (W = 1/(μ−λ)) says a 95% utilization target blows a tight p99 SLO because the tail is ~20× unloaded there, so you set ~60–70%. **Back-of-envelope** code (daily ÷ 86,400, then × a peak multiplier, read to one significant figure) gives the load you size for — the peak, never the average. And the **latency numbers** (~150 ms per cross-region round trip, ~100 random seeks/sec on HDD) let you reject a per-request cross-region call or a disk-per-request read in a sentence. The senior habit is to find the numbers, do the math, and choose the fix the arithmetic supports — not the one that papers over it.
</Recap>
