infographic compare-quadrant-quarter-simple-card
data
  title Two attack axes, two controls
  desc Query depth (shallow vs deep) x width (narrow vs wide) — which gate catches each
  compares
    - label Deep, narrow: depth limit rejects
    - label Shallow, wide: cost budget rejects
    - label Deep, wide: both gates reject
    - label Shallow, narrow: allowed, cheap
theme
  palette #14b8a6 #6366f1 #8b5cf6 #f59e0b
