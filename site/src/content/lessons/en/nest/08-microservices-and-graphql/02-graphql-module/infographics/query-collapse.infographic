infographic chart-column-simple
data
  title Database queries for 200 authors with their posts
  desc The GraphQL N+1 collapse — per-parent field resolver vs one batched DataLoader call
  values
    - label Naive resolver (1 + N)
      value 201
    - label DataLoader (1 + 1)
      value 2
theme
  palette #f43f5e #14b8a6
