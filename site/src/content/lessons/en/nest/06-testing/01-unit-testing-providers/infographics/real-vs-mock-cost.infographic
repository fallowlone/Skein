infographic compare-binary-horizontal-underline-text-vs
data
  title Real-DB suite vs mocked unit test
  desc Same UsersService.register logic — the testing module buys back the time and the determinism
  compares
    - label Real-DB suite
      children
        - label ~9 min per CI run
        - label 1 in 5 runs fails
          desc connection-pool timeout
        - label Boots AppModule, Postgres, mail API
        - label Tests Postgres/TypeORM, not logic
    - label Mocked unit test
      children
        - label Runs in milliseconds
        - label 0 flaky runs, deterministic
        - label No DB, no network
        - label Exercises register() logic
theme
  palette #f43f5e #14b8a6
