infographic chart-column-simple
data
  title Calling forRoot twice doubles the connection pool
  desc 5 pods x poolSize 10 — one forRoot = 50 idle conns; a second forRoot = 100, hitting Postgres max_connections
  values
    - label forRoot once
      value 50
    - label forRoot twice
      value 100
    - label max_connections
      value 100
theme
  palette #14b8a6 #f43f5e #f59e0b
