---
concepts:
- dto-classes
- class-validator
- validation-pipe
- whitelist-forbid
- transform-coercion
- built-in-pipes
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-dto-validation
sources:
- https://docs.nestjs.com/techniques/validation
- https://docs.nestjs.com/pipes
spiral: []
status: ready
summary: 'A DTO is a runtime class whose class-validator decorators describe a valid
  request; ValidationPipe reads that metadata before the handler, rejects bad input
  with 400, strips unknown props, and coerces types — so the handler trusts its args.'
title: 'DTOs, class-validator and pipes'
track: nest
unit: 02-validation-and-pipes
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import SequenceDiagram from "~/components/diagram/SequenceDiagram.astro";
import Infographic from "~/components/diagram/Infographic.astro";
import pipeOptions from "./infographics/pipe-options.svg?raw";

<Hook>
A POST to `/users` arrives with `{ "email": 42, "isAdmin": true }`. There is no validation, so the controller takes the body as-is, the service spreads it into a Prisma `create`, and now you have a user whose email is the number 42 and who quietly granted themselves admin — a field your API never meant to expose. The handler trusted the wire. It should never have had to: the contract was supposed to be enforced at the door, before a single line of business logic ran.
</Hook>

<Crux>A DTO is a runtime class; class-validator decorators describe a valid request, and ValidationPipe enforces it before the handler runs.</Crux>

<Explanation>

## A DTO is a class, not an interface — because metadata must survive to runtime

A DTO (Data Transfer Object) describes the shape of a request body, query, or param. In Nest you write it as a **class**, never a TypeScript `interface`. The reason is concrete: interfaces are erased during compilation — they do not exist at runtime — and Nest's validation works by reading decorator *metadata* off the object at runtime. A `@IsEmail()` decorator on an interface property has nothing to attach to once the types are stripped. A class survives transpilation as a real value, so the decorators (and the emitted design-type metadata) are there when the pipe asks.

You annotate each field with [class-validator](https://github.com/typestack/class-validator) decorators that declare its constraints: `@IsString`, `@IsEmail`, `@IsInt`, `@Min`, `@IsOptional`, `@ValidateNested`, and dozens more. The DTO becomes a single, declarative source of truth for "what does a valid request look like" — readable, reusable, and testable in isolation.

```ts
import { IsEmail, IsString, IsInt, Min, IsOptional } from 'class-validator';

export class CreateUserDto {
  @IsEmail()
  email: string;

  @IsString()
  @Min(2) // length handled by @MinLength in real code; @Min shown for numbers below
  name: string;

  @IsInt()
  @Min(18)
  age: number;

  @IsOptional()
  @IsString()
  bio?: string;
}
```

## The pipe runs before the handler — and decides what the handler ever sees

A **pipe** is a class with a `transform()` method that Nest runs on a route argument *before* the handler receives it. Pipes do two jobs: **transformation** (reshape or coerce the value) and **validation** (check it, throw if it fails). The built-in `ValidationPipe` is the one that ties DTOs together: it reads the class-validator metadata off your DTO, validates the incoming payload against it, and on failure throws a `BadRequestException` — an automatic **400** with the list of what was wrong. The handler only ever runs on input that already passed.

You enable it once, globally, in `main.ts`:

```ts
import { ValidationPipe } from '@nestjs/common';
import { NestFactory } from '@nestjs/core';
import { AppModule } from './app.module';

async function bootstrap() {
  const app = await NestFactory.create(AppModule);
  app.useGlobalPipes(
    new ValidationPipe({
      whitelist: true,            // strip properties not in the DTO
      forbidNonWhitelisted: true, // ...or reject the request if they appear
      transform: true,            // coerce the plain object into a DTO instance + coerce primitives
    }),
  );
  await app.listen(3000);
}
bootstrap();
```

The controller then just declares the DTO as the type of its `@Body()` argument. No validation code lives in the controller — that is the point:

```ts
import { Controller, Post, Body, Get, Param, ParseIntPipe } from '@nestjs/common';
import { CreateUserDto } from './dto/create-user.dto';

@Controller('users')
export class UsersController {
  @Post()
  create(@Body() dto: CreateUserDto) {
    // dto is already validated AND an instance of CreateUserDto.
    // Unknown fields like `isAdmin` were stripped (or the request was rejected).
    return this.users.create(dto);
  }

  @Get(':id')
  findOne(@Param('id', ParseIntPipe) id: number) {
    // ParseIntPipe — a built-in pipe — coerces the string param to a number
    // and 400s if it isn't a valid integer. id is a real number here.
    return this.users.findOne(id);
  }
}
```

## The three options that matter: whitelist, forbidNonWhitelisted, transform

These options are not cosmetic — two of them are a real security control.

- **`whitelist: true`** strips any property of the payload that has no validation decorator in the DTO. The extra `isAdmin: true` from the hook simply never reaches your service.
- **`forbidNonWhitelisted: true`** (use *with* `whitelist`) goes further: instead of silently stripping unknown props, it rejects the whole request with a 400 listing them. This is the stronger stance — it surfaces clients sending fields you do not accept rather than quietly dropping them.
- **`transform: true`** does two things. It instantiates the DTO **class** from the plain JSON object (via class-transformer), so the value the handler gets is a real `CreateUserDto` instance, not a bare object — methods and `instanceof` work. It also coerces primitive types: a query param `?age=18` arriving as the string `"18"` becomes the number `18` when the DTO field is typed `number`.

Together, `whitelist` + `forbidNonWhitelisted` are your defence against **mass-assignment / over-posting** — the class of bug where a client sets a field (role, balance, `isAdmin`) the server never intended to be client-settable. Validating at the edge is what lets the rest of the application *trust its inputs*: once the pipe has run, every layer below it can treat the DTO as known-good.

<Infographic
  svg={pipeOptions}
  label="Taxonomy of ValidationPipe's three options grouped by their two jobs: whitelist and forbidNonWhitelisted both stop mass-assignment, while transform does two things — it turns the plain JSON into a real DTO instance and coerces a string like 18 into the number 18."
  caption="The three options split into two jobs: whitelist + forbidNonWhitelisted are the security control against mass-assignment; transform is the typing control that hands the handler a real, coerced DTO instance."
  hue="var(--d-infra)"
/>

<Inset kind="why" lang="en">
Why a *global* pipe rather than per-route? Registering `ValidationPipe` once in `main.ts` makes validation the default for every endpoint, so a new controller is safe by omission — you cannot forget to add it. The cost is that `whitelist`/`forbid` now apply everywhere, which is exactly what you want for a public API. For the rare endpoint that needs different rules, you override locally with a `@UsePipes(new ValidationPipe({ ... }))` on that handler. Beware one gotcha: `transform: true` only coerces primitives reliably when the design-type metadata is present, which is why `tsconfig` must have `emitDecoratorMetadata` and `experimentalDecorators` enabled.
</Inset>

The flow below is what every request now goes through: the pipe stands between the wire and your handler.

<SequenceDiagram
  label="An HTTP request passes through ValidationPipe, which validates and transforms the body against the DTO before the controller handler runs."
  hue="var(--d-systems)"
  actors={["Client", "ValidationPipe", "Handler"]}
  messages={[
    { from: "Client", to: "ValidationPipe", label: "POST /users { email, isAdmin }" },
    { from: "ValidationPipe", to: "ValidationPipe", label: "read DTO class-validator metadata" },
    { from: "ValidationPipe", to: "ValidationPipe", label: "whitelist: strip isAdmin" },
    { from: "ValidationPipe", to: "Client", label: "invalid -> throw 400 BadRequest" },
    { from: "ValidationPipe", to: "Handler", label: "valid -> CreateUserDto instance" },
    { from: "Handler", to: "Client", label: "201 Created (trusted input)" },
  ]}
  caption="Time flows top to bottom. The pipe runs before the handler: invalid input never reaches your business logic, and what does reach it is a clean, typed DTO instance."
/>

The table makes the difference concrete — the same noisy payload, with and without the pipe:

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Aspect</th>
        <th class="border border-border px-3 py-2 text-left">No ValidationPipe</th>
        <th class="border border-border px-3 py-2 text-left">ValidationPipe (whitelist + forbid + transform)</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">What reaches the handler</td>
        <td class="border border-border px-3 py-2">The raw parsed JSON, untouched</td>
        <td class="border border-border px-3 py-2">A validated <code>CreateUserDto</code> instance</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Unknown prop <code>isAdmin</code></td>
        <td class="border border-border px-3 py-2">Passes straight through to the service</td>
        <td class="border border-border px-3 py-2">Stripped (whitelist) or request rejected (forbid)</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Type coercion of <code>?age=18</code></td>
        <td class="border border-border px-3 py-2">Stays the string <code>"18"</code></td>
        <td class="border border-border px-3 py-2">Coerced to the number <code>18</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Invalid body (<code>email: 42</code>)</td>
        <td class="border border-border px-3 py-2">Saved as-is; corrupt data downstream</td>
        <td class="border border-border px-3 py-2">Automatic <code>400</code> with a per-field error list</td>
      </tr>
    </tbody>
  </table>
</div>

Beyond `ValidationPipe`, Nest ships small **built-in pipes** for single scalar args — `ParseIntPipe`, `ParseUUIDPipe`, `ParseBoolPipe`, `ParseArrayPipe` — and you can write a **custom pipe** by implementing `PipeTransform`'s `transform(value, metadata)`. But for request bodies the rule holds: put the constraints in the DTO, not in the controller, and let one global `ValidationPipe` enforce them.

<Quiz
  id="01-dto-validation-quiz-1"
  lessonSlug="01-dto-validation"
  lang="en"
  question="Why must a Nest DTO be a class and not a TypeScript interface?"
  choices={[
    { label: "Interfaces are erased at compile time, so the class-validator decorators and design-type metadata the pipe reads at runtime would not exist", correct: true },
    { label: "Classes are faster to instantiate than interfaces", misconception: "Interfaces are a type-system construct with no runtime cost or instance at all — there is nothing to instantiate. Speed is not the issue; existence at runtime is." },
    { label: "Nest forbids interfaces in DTOs by a lint rule", misconception: "It is not a style rule. An interface simply has no runtime representation, so there is no object for decorators to attach to and nothing for ValidationPipe to inspect." },
    { label: "Interfaces cannot describe optional fields", misconception: "Interfaces handle optional fields fine (foo?: string). The blocker is that decorators and metadata need a runtime class to live on." },
  ]}
/>

<Quiz
  id="01-dto-validation-quiz-2"
  lessonSlug="01-dto-validation"
  lang="en"
  question="A client POSTs { email, name, isAdmin: true } but the DTO has no isAdmin field. ValidationPipe runs with whitelist: true. What happens?"
  choices={[
    { label: "isAdmin is stripped from the payload, so the handler receives only the decorated fields and never sees isAdmin", correct: true },
    { label: "The whole request is rejected with a 400", misconception: "That is forbidNonWhitelisted: true. Plain whitelist silently strips unknown props; you need forbidNonWhitelisted to reject the request and list them." },
    { label: "isAdmin passes through but is set to null", misconception: "whitelist removes the property entirely; it does not null it out. The handler's DTO has no isAdmin key at all." },
    { label: "Nothing — whitelist only affects type coercion", misconception: "Type coercion is the transform option. whitelist is specifically about stripping properties with no validation decorator in the DTO." },
  ]}
/>

<TradeoffMatrix
  id="01-dto-validation-tradeoff-1"
  lessonSlug="01-dto-validation"
  lang="en"
  prompt="A public POST endpoint must never let a client set a field the server didn't intend (mass-assignment), and bad bodies should fail loudly. Pick the ValidationPipe configuration."
  options={[
    { name: "whitelist: true, forbidNonWhitelisted: true, transform: true", summary: "Strip + reject + coerce.", correct: true, justification: "This is the hardened default for a public API. whitelist strips undecorated props, forbidNonWhitelisted turns an unexpected field into a loud 400 rather than a silent drop (so clients sending what you don't accept are surfaced), and transform gives the handler a real DTO instance with coerced primitives. Together whitelist+forbid close the mass-assignment hole, which is exactly the requirement." },
    { name: "whitelist: true only", summary: "Strip silently.", justification: "Safe against mass-assignment because unknown props are stripped, but it fails silently — a client sending isAdmin gets a 201 and never learns the field was ignored, which hides integration bugs. The brief asks bad bodies to fail loudly, so add forbidNonWhitelisted." },
    { name: "transform: true only (no whitelist)", summary: "Coerce, don't strip.", justification: "You get DTO instances and primitive coercion, but with no whitelist undecorated properties pass straight through to your service — the mass-assignment hole is wide open. transform solves typing, not over-posting." },
    { name: "No global pipe; validate by hand in each controller", summary: "Manual checks.", justification: "Re-implements what ValidationPipe does, scatters validation across controllers, and is the bug from the hook waiting to happen — one forgotten endpoint accepts anything. Validation belongs declaratively in the DTO, enforced by one global pipe, not hand-rolled per route." },
  ]}
/>

</Explanation>

<KeyTakeaway>DTOs are classes so decorator metadata survives to runtime. A global `ValidationPipe` with `whitelist` + `forbidNonWhitelisted` kills mass-assignment; `transform` gives typed instances. Keep constraints in the DTO.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-dto-validation-retrieval"
  lang="en"
  questions={[
    { q: "Walk through what ValidationPipe does to a request body, and where it sits relative to the handler.", a: "ValidationPipe is a pipe, so Nest runs it on the route argument before the controller handler is ever called. It reads the class-validator decorator metadata off the DTO class you typed the @Body() as, then validates the incoming plain object against those constraints. If anything fails it throws a BadRequestException, which Nest turns into an automatic 400 response carrying a per-field list of what was wrong — the handler never runs. If it passes, the options shape what the handler receives: whitelist strips any property with no validation decorator, forbidNonWhitelisted instead rejects the whole request if unknown props are present, and transform instantiates the DTO class from the plain object (so you get a real CreateUserDto instance) and coerces primitive types like a string \"18\" to the number 18. So the pipe sits squarely between the wire and your business logic, and the handler only ever sees clean, typed, known-good input." },
    { q: "Why does whitelist + forbidNonWhitelisted matter for security, and why must the DTO be a class?", a: "Together they defend against mass-assignment (over-posting): the bug where a client sends a field the server never meant to be client-settable — role, balance, isAdmin — and it gets blindly persisted. whitelist strips any property the DTO doesn't declare with a validation decorator, so an injected isAdmin never reaches the service; forbidNonWhitelisted is the stronger stance, rejecting the request with a 400 rather than silently dropping the field, which surfaces clients sending things you don't accept. This only works because the DTO is a class: TypeScript interfaces are erased at compile time and have no runtime existence, so there would be no object for the class-validator decorators or the emitted design-type metadata to live on, and the pipe would have nothing to read. A class survives transpilation as a real value, carrying its decorator metadata to runtime where ValidationPipe inspects it." },
  ]}
/>

<Recap lang="en">
Validate input at the boundary so the rest of the app can trust it. A DTO is a runtime class — never an interface, because the class-validator decorators and design-type metadata the validator reads must survive compilation, and interfaces are erased. You annotate each field (`@IsEmail`, `@IsInt`, `@Min`, `@IsOptional`, `@ValidateNested`) to declare a valid request declaratively. A pipe runs before the handler to transform and validate an argument; the built-in `ValidationPipe`, enabled once with `app.useGlobalPipes(new ValidationPipe({ ... }))`, reads the DTO metadata, rejects bad input with an automatic 400 plus a per-field error list, and otherwise hands the handler clean input. Its three load-bearing options: `whitelist` strips undecorated properties, `forbidNonWhitelisted` rejects requests that carry them (together closing the mass-assignment hole), and `transform` instantiates the DTO class and coerces primitives like `"18"` to `18`. Beyond it, built-in pipes (`ParseIntPipe` and friends) and custom `PipeTransform` classes handle scalar args. The discipline that ties it together: keep constraints in the DTO, not the controller, and let one global pipe enforce them everywhere. Now when you see a controller method that opens with manual type checks or trusts its own `req.body` directly, you know exactly what to reach for.
</Recap>
