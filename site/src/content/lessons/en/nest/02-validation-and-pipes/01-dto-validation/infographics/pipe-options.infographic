infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label ValidationPipe — 3 options, 2 jobs
    children
      - label Stop mass-assignment (security)
        children
          - label whitelist — strip props with no DTO decorator
          - label forbidNonWhitelisted — reject request, 400 lists extras
      - label Give the handler typed input
        children
          - label transform — plain JSON to real DTO instance
          - label transform — coerce string 18 to number 18
theme
  palette #6366f1 #f43f5e #14b8a6
