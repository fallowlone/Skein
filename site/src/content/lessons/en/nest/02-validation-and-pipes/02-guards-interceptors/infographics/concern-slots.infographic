infographic hierarchy-tree-curved-line-rounded-rect-node
data
  root
    label One slot per cross-cutting concern
    children
      - label Guard (CanActivate)
        children
          - label Authn / authz — a yes/no gate
          - label Reads @Roles via Reflector, before pipes
      - label Pipe (ValidationPipe)
        children
          - label Validate + transform the input
          - label Throws on invalid input, just before the handler
      - label Interceptor (RxJS)
        children
          - label Logging, caching, transform, timeout
          - label Only stage that runs after the handler too
      - label Filter (@Catch)
        children
          - label Shape the error response, once
          - label Single sink for every throw in the chain
theme
  palette #6366f1 #14b8a6 #f59e0b #f43f5e
