---
slug: 01-overview
lang: en
track: deployment
unit: 06-lb-levels
order: 1
level: junior
lessonType: topic
status: ready
title: "Load balancing L4 vs L7: what each layer can and can't see"
summary: "An L4 balancer routes by IP and port without reading the payload — fast but blind to URLs. An L7 balancer terminates the connection and reads HTTP, enabling path routing and retries at a CPU cost. Pick the wrong layer and path routing is simply impossible."
prereqs: []
deepensInto: []
spiral: []
concepts: ["l4-load-balancing", "l7-load-balancing", "tls-termination", "health-checks", "connection-draining", "session-affinity"]
estMin: 16
sources:
  - https://docs.aws.amazon.com/elasticloadbalancing/latest/application/load-balancer-target-groups.html
  - https://aws.amazon.com/compare/the-difference-between-the-difference-between-application-network-and-gateway-load-balancing/
  - https://docs.aws.amazon.com/elasticloadbalancing/latest/application/edit-target-group-attributes.html
  - https://www.nginx.com/resources/glossary/layer-4-load-balancing/
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
A team splits a monolith: `/api/*` should go to the new service, everything else to the old one. They already run an AWS NLB for "performance", so they add a rule to route by path — and discover there is no such rule. The NLB cannot see the path. It forwards raw TCP; the URL only exists once HTTP is parsed, and an L4 balancer never parses HTTP. The migration stalls until someone swaps in an ALB. The lesson cost a day: the layer you pick decides which routing is even *possible*.
</Hook>

<Crux>An L4 balancer routes by IP/port without reading the payload; an L7 balancer reads HTTP and routes by path, host, header, or cookie.</Crux>

<Explanation>

## L4 forwards bytes; L7 reads requests

A Layer-4 balancer operates at the transport layer. It sees the TCP/UDP connection — source IP, destination IP, ports — and forwards packets to a backend without ever decoding what is inside. It is essentially a fast, connection-aware NAT. Because it never parses the payload, it is protocol-agnostic (it balances any TCP or UDP service, not just HTTP) and extremely cheap per packet — AWS's NLB handles millions of requests per second at microsecond-level per-packet overhead and hands out static IPs.

A Layer-7 balancer operates at the application layer. It *terminates* the client connection, reads the full HTTP request — method, host, path, headers, cookies — and then opens its own connection to a chosen backend. That terminate-and-re-originate is the whole source of its power and its cost. Once the balancer holds the parsed request, it can route on `/api/*` vs `/`, on `Host:` header, on a cookie, or on a custom header for canaries. The price: it must buffer and parse every request, and if TLS is in play it decrypts and re-encrypts — real CPU per request, and it only speaks the protocols it understands (HTTP/1.1, HTTP/2, gRPC).

The decision is not "which is better" but "what do I need to see." If you only need to spread raw TCP across identical backends, L4 is faster and simpler. The moment routing depends on anything *inside* the request, you need L7 — and no amount of L4 tuning will conjure a URL it never decoded.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Capability</th>
        <th class="border border-border px-3 py-2 text-left">L4 (transport)</th>
        <th class="border border-border px-3 py-2 text-left">L7 (application)</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Routes by</td>
        <td class="border border-border px-3 py-2">IP + port</td>
        <td class="border border-border px-3 py-2">path, host, header, cookie</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Reads payload?</td>
        <td class="border border-border px-3 py-2">No — forwards bytes</td>
        <td class="border border-border px-3 py-2">Yes — parses HTTP</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Protocols</td>
        <td class="border border-border px-3 py-2">any TCP/UDP</td>
        <td class="border border-border px-3 py-2">HTTP/1.1, HTTP/2, gRPC</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">TLS termination</td>
        <td class="border border-border px-3 py-2">passthrough (usually)</td>
        <td class="border border-border px-3 py-2">terminates + can re-encrypt</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">HTTP-aware retries</td>
        <td class="border border-border px-3 py-2">No</td>
        <td class="border border-border px-3 py-2">Yes (idempotent reqs)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Per-request cost</td>
        <td class="border border-border px-3 py-2">very low</td>
        <td class="border border-border px-3 py-2">parse + buffer + crypto</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">AWS example</td>
        <td class="border border-border px-3 py-2">NLB</td>
        <td class="border border-border px-3 py-2">ALB</td>
      </tr>
    </tbody>
  </table>
</div>

## Where TLS terminates changes everything

TLS termination is the cleanest lens on the L4/L7 split. An L7 balancer ends the encrypted session at the edge: it holds the certificate, decrypts the request, reads the plaintext HTTP, makes its routing decision, and forwards — either as plaintext inside the trusted network or re-encrypted to the backend. That decryption is exactly what lets it route on path or header. It also centralises certificate management and offloads crypto from your app servers.

An L4 balancer normally cannot terminate TLS, because terminating means decrypting, and decrypting means becoming HTTP-aware. So it does TLS *passthrough*: the encrypted bytes flow straight to the backend, which holds the cert and terminates. That keeps the balancer blind by design — great for end-to-end encryption and for non-HTTP protocols, useless if you wanted path routing. This is the concrete reason "just use the fast one" backfires: the fast one literally cannot read what you need to route on.

<Inset kind="why" lang="en">
"L4 is always faster" is half-true and misleading. Per packet, yes — no parsing, no crypto. But an L7 balancer that terminates TLS once at the edge can be *cheaper overall* than passing encryption through to every backend, because each app server no longer pays the handshake and decryption cost. The right question is where the work is best done, not which layer wins a microbenchmark.
</Inset>

## Algorithms, health checks, and pulling dead backends

Both layers still have to *choose* a backend. Round-robin spreads connections evenly and is fine when requests cost roughly the same. Least-connections sends the next request to the backend with the fewest active connections — far better when request durations vary widely, because one slow endpoint won't keep getting piled on. Hashing (by client IP, or consistent hashing) pins a given key to a given backend, which is how an L4 balancer fakes stickiness without cookies.

Health checks are what keep the pool honest. Active checks probe each backend on an interval (say every few seconds) and pull it from rotation after N consecutive failures; passive checks mark a backend down after it returns errors to real traffic. Open-source nginx leans on passive checks; ALB/NLB and nginx Plus do active probing. The failure to internalise: a backend that crashes is only removed *after* the health check notices — so a too-slow interval means a window where live traffic keeps hitting a dead box and 502s leak to users.

<TradeoffMatrix
  id="06-lb-levels-tradeoff-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="You must route /api/* to a new service and / to the old one, terminate TLS at the edge, and run canary by a request header. Pick the balancer."
  options={[
    { name: "L7 application balancer (ALB / nginx HTTP / Envoy)", summary: "Reads HTTP.", correct: true, justification: "Every requirement lives inside the HTTP request: path-based routing (/api/* vs /), TLS termination (which requires decrypting), and header-based canary routing all need the balancer to parse the request. Only an L7 balancer terminates the connection and reads method, host, path, headers and cookies, so it is the only layer that can satisfy any of these. The cost — per-request parsing and crypto — is the price of being able to route on what the request says." },
    { name: "L4 network balancer (NLB) with extra config", summary: "Forward TCP fast.", justification: "No amount of configuration makes an L4 balancer see a URL or header — it forwards TCP/UDP by IP and port and never decodes the payload. Path routing, header canaries and TLS termination are structurally impossible at L4. This is the classic trap: choosing L4 for raw speed, then discovering the routing you need doesn't exist at that layer." },
    { name: "L4 balancer with TLS passthrough to backends that route internally", summary: "Push routing down.", justification: "Technically possible but it just moves the L7 work into every backend: each app server now terminates TLS and re-implements path/header routing, multiplying cert management and crypto cost and scattering routing logic. You've rebuilt an L7 balancer badly across N boxes instead of running one at the edge." },
    { name: "DNS round-robin between two hostnames", summary: "Resolve to IPs.", justification: "DNS hands out IPs; it cannot inspect a path or header, has no health-aware failover within a record's TTL, and clients cache results. It's a coarse distribution mechanism, not a request-aware router, and solves none of the stated requirements." },
  ]}
/>

## Sticky sessions and connection draining: two ways deploys break

Sticky sessions (session affinity) pin a client to one backend — via an L4 IP hash or an L7 cookie — so in-memory session state stays put. It works, and it quietly hurts: load stops distributing evenly (one whale client hammers one box), and that backend can't be drained cleanly because it owns sessions nobody else has. The senior reflex is to make backends stateless (sessions in Redis or a JWT) so any backend can serve any request; reach for stickiness only when you genuinely can't externalise state.

The deploy-killer is missing connection draining. When you remove a backend during a rollout, in-flight requests are still being served on it. If the balancer rips it out immediately, those requests die — users see resets mid-checkout. Connection draining (AWS calls it *deregistration delay*) tells the balancer to stop sending *new* connections to the backend but let existing ones finish for a grace window. On AWS the default is **300 seconds**, tunable from 0 to 3600. Set it shorter than your longest request and you'll still cut live requests; forget to enable it and every deploy is a small outage. Pair it with health checks: drain, wait, then terminate.

<Quiz
  id="06-lb-levels-quiz-1"
  lessonSlug="01-overview"
  lang="en"
  question="Your L4 (NLB) load balancer needs to send /admin/* to a separate backend pool. How do you configure the path rule?"
  choices={[
    { label: "You can't — an L4 balancer never parses HTTP, so the path doesn't exist at that layer; you need an L7 balancer", correct: true },
    { label: "Add a path-matching listener rule on the NLB", misconception: "L4 listeners match on port, not path. The URL only exists after HTTP is parsed, and an L4 balancer forwards raw TCP without parsing — there is no path rule to add." },
    { label: "Enable deep packet inspection on the NLB", misconception: "Inspecting the HTTP payload to read a path is exactly what makes a balancer L7. A network balancer that did this would no longer be operating at layer 4." },
    { label: "Use IP-hash so /admin/* clients stick to the right pool", misconception: "IP-hash pins a client to a backend regardless of path; it can't separate /admin/* from / because it never sees the path. Affinity is not routing." },
  ]}
/>

<Quiz
  id="06-lb-levels-quiz-2"
  lessonSlug="01-overview"
  lang="en"
  question="During a rolling deploy, users report connection resets mid-request whenever an old instance is removed. What's the fix?"
  choices={[
    { label: "Enable connection draining / deregistration delay so in-flight requests finish before the backend is terminated", correct: true },
    { label: "Switch from least-connections to round-robin", misconception: "The algorithm chooses among healthy backends; it does nothing about killing in-flight requests on a backend you're removing. The resets come from no graceful drain." },
    { label: "Add more backends to the pool", misconception: "More capacity doesn't change what happens to the requests already running on the instance you remove — without draining they're still cut the moment it's deregistered." },
    { label: "Lower the health-check interval", misconception: "Faster health checks detect a dead backend sooner, but the problem is removing a healthy one mid-request without letting its connections finish — that's draining, not detection." },
  ]}
/>

<DragOrder
  id="06-lb-levels-drag-1"
  lessonSlug="01-overview"
  lang="en"
  prompt="Order the steps to gracefully remove a backend during a deploy:"
  items={[
    { id: "a", label: "Mark the backend for deregistration so the balancer stops sending it new connections" },
    { id: "b", label: "Let connection draining run: existing in-flight requests keep being served" },
    { id: "c", label: "Wait out the drain window (e.g. AWS deregistration delay, default 300s)" },
    { id: "d", label: "Once connections finish or the window elapses, terminate the instance" },
    { id: "e", label: "Health checks confirm the remaining pool is serving traffic" },
  ]}
  correctOrder={["a", "b", "c", "d", "e"]}
/>

</Explanation>

<KeyTakeaway>L4 routes by IP/port and forwards bytes — fast but blind to URLs. L7 reads HTTP and routes by path/host/header, at a CPU cost. Always enable connection draining before removing a backend.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-lb-levels-retrieval"
  lang="en"
  questions={[
    { q: "A colleague wants path-based routing but the team already runs an L4 (NLB) balancer 'because it's faster'. Explain precisely why path routing is impossible there and what changes if you move to L7.", a: "A Layer-4 balancer operates at the transport layer: it sees the TCP/UDP connection — source and destination IP and port — and forwards the raw bytes to a backend without ever decoding the payload. The URL path is part of the HTTP request, which only exists once someone parses the application-layer protocol, and an L4 balancer by definition never does that. So there is no path rule to add and no setting to flip; the information you'd route on was never decoded. Moving to L7 changes the model entirely: an application balancer terminates the client connection, parses the full HTTP request (method, host, path, headers, cookies), makes a routing decision on any of those fields, then opens its own connection to the chosen backend. That terminate-and-read is what enables /api/* vs / routing, host routing, header-based canaries, and HTTP-aware retries. The tradeoff you accept is per-request CPU: parsing, buffering, and — if TLS terminates at the edge — decryption and re-encryption. You pick the layer by what you need to see, not by which wins a microbenchmark." },
    { q: "Walk through what connection draining does during a deploy and what breaks without it. Include the AWS default.", a: "During a rolling deploy you remove old backends from the pool, but at the instant you remove one it may still be serving in-flight requests. Without connection draining the balancer deregisters it immediately and those open connections are severed — users see resets or errors mid-request, e.g. mid-checkout. Connection draining (AWS calls it deregistration delay) fixes this: when a backend is marked for removal the balancer stops routing new connections to it but lets existing ones run to completion for a grace window. On AWS the default deregistration delay is 300 seconds, configurable from 0 to 3600. The window must be at least as long as your longest legitimate request, or you'll still cut live traffic; set too long and deploys drag. The correct sequence is: mark for deregistration, let in-flight requests drain, wait out the window (or until connections finish), then terminate — with health checks confirming the remaining pool is healthy. Forgetting to enable draining turns every deploy into a small, self-inflicted outage." },
  ]}
/>

<Recap lang="en">
The layer you pick decides what the balancer can route on, because it decides what it can see. An L4 balancer works at the transport layer — IP, port, raw TCP/UDP — and forwards bytes without parsing, making it fast, protocol-agnostic, and capable of TLS passthrough, but structurally blind to URLs, headers, and cookies. An L7 balancer terminates the connection, parses HTTP, and routes on path, host, header, or cookie, which is what enables path-based routing, edge TLS termination, header canaries, and HTTP-aware retries — paid for in per-request CPU. On top of the layer choice sit the operational details that decide reliability: round-robin, least-connections, and hashing balance differently under uneven load; active and passive health checks pull dead backends out, but only as fast as their interval; sticky sessions keep state in place at the cost of even distribution and clean draining; and connection draining (AWS deregistration delay, default 300 seconds) lets in-flight requests finish before a backend is removed. The two production failures fall straight out of this: needing path routing on an L4 balancer, which is impossible, and removing a backend with no draining, which kills live requests on every deploy.
</Recap>
