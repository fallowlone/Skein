---
concepts:
- service-clusterip
- selector-to-endpoints
- service-types
- headless-service
- ingress-routing
- empty-endpoints-failure
deepensInto: []
estMin: 17
lang: en
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-services-and-ingress
sources:
- https://kubernetes.io/docs/concepts/services-networking/service/
- https://kubernetes.io/docs/concepts/services-networking/ingress/
spiral: []
status: ready
summary: 'Pods are ephemeral with changing IPs, so you reach them through a Service:
  a stable virtual IP that load-balances over pods matching a label selector, with
  Ingress as the shared HTTP router in front. Selector mismatch means zero endpoints.'
title: 'Services and ingress'
track: deployment
unit: 10-k8s-deep
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A rollout goes green. Pods are `Running`, the Deployment reports all replicas ready, and yet every request to the app hangs and then times out. `kubectl get endpoints api` prints a single chilling line: `api   <none>`. The Service is pointing at nothing. Someone renamed the pod label from `app: api` to `app: api-svc` and never touched the Service's selector — so the Service matches zero pods, builds an empty endpoint list, and quietly black-holes traffic. The pods are fine. The wiring between them and the Service is broken.
</Hook>

<Crux>A Service is a stable virtual IP load-balancing over pods matching a label selector; Ingress is the shared HTTP router in front.</Crux>

<Explanation>

## Why you can't address a pod directly

When you deploy to Kubernetes, the first instinct is to grab the pod's IP and call it directly — and that instinct is exactly what gets you into the failure in the hook above. Here is why it can never work reliably.

A pod is the cheapest, most disposable thing in Kubernetes. The scheduler kills it, reschedules it, scales it up and down, and moves it to another node on a whim — and every time it comes back it gets a **new IP from the pod network**. There is no stable address you can hardcode, no DNS name that survives a rollout, no guarantee the IP you resolved a second ago still belongs to a live pod. Directly dialing a pod IP is a bug waiting for the next reschedule.

The **Service** is the fix. It is an API object that gives a set of pods one stable identity: a virtual IP (the ClusterIP) and a DNS name (`<service>.<namespace>.svc.cluster.local`) that live for the lifetime of the Service, independent of which pods exist behind it right now. Clients talk to the Service; the Service load-balances each connection across the healthy pods. Pods churn underneath; the address in front never moves.

## The selector-to-endpoints mechanism

The link between a Service and its pods is **labels**, not IPs. A Service declares a `selector` — a set of label key/values — and Kubernetes continuously watches for pods that (a) match every label in the selector and (b) are **Ready** (passing their readiness probe). For each such pod it records the pod IP and port into an **EndpointSlice** (the modern, sharded replacement for the older single Endpoints object). That EndpointSlice *is* the load-balancing target list; kube-proxy on every node programs the data plane (iptables or IPVS rules) from it.

```yaml
apiVersion: v1
kind: Service
metadata:
  name: api
spec:
  selector:
    app: api            # must match the pods' labels exactly
  ports:
    - name: http
      port: 80          # the Service's virtual port
      targetPort: 8080  # the container port on the pod
```

Two things follow from this. First, the matching is dynamic: scale the Deployment up and new Ready pods are added to the slice within moments; a pod that fails its readiness probe is removed, so traffic stops going to it without you deleting anything. Second — and this is the failure that bites everyone — **if the selector matches no Ready pods, the EndpointSlice is empty, and the Service routes to nowhere.** It still has a ClusterIP, still resolves in DNS, still accepts connections — and then refuses or times out, because there is no backend to forward to.

<Inset kind="why" lang="en">
"Ready" is doing real work here. A pod can be `Running` but not `Ready` — it is in the EndpointSlice only once its readiness probe passes. This is deliberate: during a rolling update, new pods are kept out of the Service until they can actually serve, and old pods are pulled from the slice before termination. Get the readiness probe wrong and you can ship a Service that has running pods but zero endpoints — same black hole, subtler cause.
</Inset>

## Service types: how far the address reaches

Every Service has a type that decides who can reach it. They stack: each builds on the one before.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Type</th>
        <th class="border border-border px-3 py-2 text-left">Scope</th>
        <th class="border border-border px-3 py-2 text-left">External?</th>
        <th class="border border-border px-3 py-2 text-left">Cost</th>
        <th class="border border-border px-3 py-2 text-left">Use case</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>ClusterIP</code> (default)</td>
        <td class="border border-border px-3 py-2">Internal virtual IP only</td>
        <td class="border border-border px-3 py-2">No</td>
        <td class="border border-border px-3 py-2">Free</td>
        <td class="border border-border px-3 py-2">Service-to-service inside the cluster</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>NodePort</code></td>
        <td class="border border-border px-3 py-2">A port (30000–32767) on every node</td>
        <td class="border border-border px-3 py-2">Yes, crude</td>
        <td class="border border-border px-3 py-2">Free</td>
        <td class="border border-border px-3 py-2">Basic external access, dev, behind your own LB</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>LoadBalancer</code></td>
        <td class="border border-border px-3 py-2">Provisions a cloud load balancer</td>
        <td class="border border-border px-3 py-2">Yes</td>
        <td class="border border-border px-3 py-2">One paid LB per Service</td>
        <td class="border border-border px-3 py-2">A single L4 entry point for one Service</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Headless (<code>clusterIP: None</code>)</td>
        <td class="border border-border px-3 py-2">DNS returns pod IPs, no virtual IP</td>
        <td class="border border-border px-3 py-2">No</td>
        <td class="border border-border px-3 py-2">Free</td>
        <td class="border border-border px-3 py-2">StatefulSets, per-pod addressing, client-side LB</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Ingress (not a Service type)</td>
        <td class="border border-border px-3 py-2">L7 HTTP(S) router over many Services</td>
        <td class="border border-border px-3 py-2">Yes</td>
        <td class="border border-border px-3 py-2">One LB shared by all routes</td>
        <td class="border border-border px-3 py-2">Host/path routing + TLS for the whole cluster</td>
      </tr>
    </tbody>
  </table>
</div>

Together these types form a single decision: how far outside the cluster do you need the traffic to reach? Start with ClusterIP for everything internal; reach for Ingress (not LoadBalancer) the moment you need HTTP routing to the public internet.

**ClusterIP** is the default and the workhorse — it is how your API talks to your database talks to your cache, all by DNS name, all internal. **NodePort** opens the same port on *every* node and forwards it to the Service; it is the crude knob for getting traffic in, useful in dev or when you front it with your own load balancer, but you would not hand a raw `node-ip:31000` URL to users. **LoadBalancer** asks the cloud provider to provision a real external load balancer pointing at the Service — clean, but you pay for and manage one LB per Service, which gets expensive and unwieldy fast. **Headless** opts out of the virtual IP entirely: DNS returns the individual pod IPs, which is exactly what a StatefulSet (database, broker) needs so clients can address a specific replica.

## Ingress: one front door for many Services

If LoadBalancer gives you one cloud LB per Service, ten public services cost ten load balancers — and you still have no HTTP-aware routing. **Ingress** solves both. It is an L7 (HTTP/HTTPS) router that sits in front of your ClusterIP Services and dispatches requests by **host and path** to the right Service, terminates **TLS** centrally, and does it all behind a *single* shared load balancer. One entry point, many backends, far cheaper than an LB per service.

```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: site
spec:
  ingressClassName: nginx
  rules:
    - host: app.example.com
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: api      # routes /api -> the api Service
                port:
                  number: 80
          - path: /
            pathType: Prefix
            backend:
              service:
                name: web      # routes everything else -> the web Service
                port:
                  number: 80
```

The crucial catch: **the Ingress object is just rules. It does nothing on its own.** You must run an **ingress controller** — nginx, Traefik, HAProxy, a cloud provider's — which actually watches Ingress objects and turns them into a running reverse proxy. No controller, no routing; the YAML applies cleanly and traffic still goes nowhere. The `ingressClassName` tells Kubernetes which controller owns this Ingress.

<TradeoffMatrix
  id="01-services-and-ingress-tradeoff-1"
  lessonSlug="01-services-and-ingress"
  lang="en"
  prompt="You have eight public HTTP services that need host/path routing and TLS. How do you expose them?"
  options={[
    { name: "One Ingress (with an ingress controller) routing host/path to eight ClusterIP Services behind a single LB", summary: "Shared L7 front door.", correct: true, justification: "This is the standard pattern. The eight services stay as cheap internal ClusterIP Services; one ingress controller behind one cloud load balancer terminates TLS once and routes by host and path to each. You pay for a single LB instead of eight, get HTTP-aware routing (paths, rewrites, headers), and manage certificates in one place. The only requirement is that an ingress controller is actually running to enforce the rules." },
    { name: "Eight LoadBalancer Services, one per service", summary: "An LB each.", justification: "It works but it is the expensive, unwieldy option: eight cloud load balancers to pay for and manage, eight external IPs, and still no HTTP-level routing — LoadBalancer is L4, so it cannot route by path or host or terminate TLS per-route. Reserve LoadBalancer for the one or two cases that genuinely need a dedicated L4 entry point." },
    { name: "Eight NodePort Services exposed directly to users", summary: "Raw node ports.", justification: "NodePort opens a high port (30000-32767) on every node; handing users node-ip:31234 URLs is brittle, ugly, and unroutable through a normal domain without something in front. NodePort is a building block for dev or for your own external LB, not a public-facing answer for eight services." },
    { name: "One ClusterIP Service per app and tell clients to call the ClusterIP", summary: "Internal IPs only.", justification: "ClusterIP is internal-only by definition — it is a virtual IP reachable from inside the cluster, with no route from the public internet. External users cannot reach a ClusterIP at all, so this exposes nothing." },
  ]}
/>

## The classic failure: zero endpoints

Return to the hook. The number-one Kubernetes networking bug is a **Service whose selector does not match its pods' labels.** Nothing errors at apply time — the Service is valid YAML, gets a ClusterIP, resolves in DNS. But its selector matches no Ready pods, so the EndpointSlice is empty, and every connection is refused or times out. The same empty-endpoints symptom also comes from pods that match the labels but are not Ready (a failing or too-strict readiness probe), or a `targetPort` that doesn't match the container's actual port.

The diagnosis is always the same one command: `kubectl get endpoints <svc>` (or `kubectl get endpointslices`). If it shows `<none>` or no addresses, the Service is talking to no one — stop debugging the app and go check that the Service's `selector` exactly equals the pods' labels and that the pods are Ready.

<Quiz
  id="01-services-and-ingress-quiz-1"
  lessonSlug="01-services-and-ingress"
  lang="en"
  question="A Service has a ClusterIP and resolves in DNS, but every request times out. kubectl get endpoints shows <none>. The pods are Running. What is the most likely cause?"
  choices={[
    { label: "The Service's selector doesn't match the pods' labels (or the pods aren't Ready), so the EndpointSlice is empty and the Service forwards to nothing", correct: true },
    { label: "The ClusterIP is wrong and needs to be set manually", misconception: "You almost never set a ClusterIP by hand — Kubernetes assigns it, and the Service resolving in DNS proves the IP is fine. Empty endpoints is a selector/label or readiness problem, not an addressing one." },
    { label: "ClusterIP can't be reached from inside the cluster; you need NodePort", misconception: "ClusterIP is precisely the internal type — it's reachable from other pods by design. The timeout here is an empty backend list, not the Service type." },
    { label: "You forgot to install an ingress controller", misconception: "An ingress controller only matters for Ingress objects. This is a Service with empty endpoints — no Ingress is involved, and the fix is matching the selector to the pods' labels." },
  ]}
/>

<FlowDiagram
  label="How traffic reaches pods in Kubernetes: an external request enters through Ingress, which routes by host and path to a ClusterIP Service, which load-balances over the EndpointSlice of Ready pods built from the label selector."
  hue="var(--d-systems)"
  perRow={4}
  nodes={[
    { id: "client", label: "Client", sub: "app.example.com" },
    { id: "ingress", label: "Ingress", sub: "host/path + TLS" },
    { id: "svc", label: "Service", sub: "ClusterIP" },
    { id: "ep", label: "EndpointSlice", sub: "Ready pod IPs" },
    { id: "pod1", label: "Pod", sub: "app: api" },
    { id: "pod2", label: "Pod", sub: "app: api" },
    { id: "pod3", label: "Pod", sub: "app: api" },
  ]}
  edges={[
    { from: "client", to: "ingress", label: "HTTPS" },
    { from: "ingress", to: "svc", label: "routes /api" },
    { from: "svc", to: "ep", label: "selector match" },
    { from: "ep", to: "pod1", label: "balance" },
    { from: "ep", to: "pod2", label: "balance" },
    { from: "ep", to: "pod3", label: "balance" },
  ]}
  caption="Ingress is the L7 front door; the Service is the stable virtual IP; the EndpointSlice is the live list of Ready pods built from the selector. Break the selector-to-label match and the EndpointSlice goes empty — the Service still resolves, but routes to nothing."
/>

</Explanation>

<KeyTakeaway>Reach ephemeral pods via a Service: a stable virtual IP load-balancing over an EndpointSlice of Ready pods matching its `selector`. ClusterIP is internal, LoadBalancer external; Ingress is one shared L7 router over many. Selector ≠ labels means zero endpoints.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-services-and-ingress-retrieval"
  lang="en"
  questions={[
    { q: "Walk through the selector-to-endpoints mechanism: how does a Service decide which pods to load-balance over, and what makes the endpoint list empty?", a: "A Service does not store pod IPs directly — it declares a label selector, a set of key/value labels. Kubernetes continuously watches every pod and, for those that match all of the selector's labels AND are Ready (passing their readiness probe), records the pod IP and target port into an EndpointSlice (the sharded successor to the older Endpoints object). That EndpointSlice is the load-balancing target list: kube-proxy on each node programs iptables or IPVS rules from it, so a connection to the Service's ClusterIP is forwarded to one of the Ready pods. Because the watch is continuous, scaling up adds new Ready pods to the slice within moments and a pod failing its readiness probe is removed automatically. The list goes empty in three classic ways: the selector matches no pods at all (a label was renamed on the pods or the Service), the matching pods are Running but never become Ready (broken or too-strict readiness probe), or the targetPort points at a port the container isn't listening on. In every case the Service still has a ClusterIP and still resolves in DNS, but with an empty EndpointSlice it forwards to nothing — connections are refused or time out. The one-line diagnosis is kubectl get endpoints <svc>: <none> means the Service is wired to no pods." },
    { q: "Why use an Ingress instead of giving each service its own LoadBalancer, and what must be running for an Ingress to actually work?", a: "A LoadBalancer Service provisions one cloud load balancer per Service and operates at L4 — so ten public services mean ten paid load balancers, ten external IPs, and still no HTTP-aware routing or per-route TLS. An Ingress is an L7 (HTTP/HTTPS) router that sits in front of many internal ClusterIP Services behind a single shared load balancer: it dispatches by host and path to the right Service, terminates TLS centrally, and lets you manage certificates and routing rules in one object. One entry point, many backends, far cheaper. The critical caveat is that an Ingress object is only a set of rules — it does nothing by itself. You must run an ingress controller (nginx, Traefik, HAProxy, or a cloud provider's) that watches Ingress objects and turns them into a running reverse proxy; the ingressClassName field selects which controller owns the Ingress. With no controller installed, the Ingress applies as valid YAML but no proxy enforces it, so traffic still goes nowhere." },
  ]}
/>

<Recap lang="en">
Pods are ephemeral and get a new IP on every reschedule, so you never address them directly — you put a Service in front. Now when you see `kubectl get endpoints <svc>` print `<none>`, you know exactly where to look: the selector, the labels, and the readiness probe — in that order. A Service is an API object with a stable ClusterIP and DNS name that load-balances over a set of pods chosen by a label selector: Kubernetes records the IPs of pods that match the selector AND are Ready into an EndpointSlice, and kube-proxy routes Service traffic across that live list. The type controls reach: ClusterIP is internal-only service-to-service, NodePort opens a port on every node for crude external access, LoadBalancer provisions one paid cloud LB per Service, and a headless Service drops the virtual IP so DNS returns pod IPs directly for StatefulSets. Because an LB-per-service gets expensive and offers no HTTP routing, an Ingress sits in front as a single L7 front door: it routes by host and path to many ClusterIP Services and terminates TLS centrally behind one shared load balancer — but it is only rules, and needs an ingress controller (nginx, Traefik) running to enforce them. The failure that bites hardest is the simplest: if the Service's selector doesn't match the pods' labels (or the pods aren't Ready), the EndpointSlice is empty, the Service still resolves but routes to nothing, and requests silently time out — so the first thing to check is always `kubectl get endpoints`.
</Recap>
