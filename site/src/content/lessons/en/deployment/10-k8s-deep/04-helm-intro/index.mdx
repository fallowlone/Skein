---
concepts:
- helm-package-manager
- chart-structure
- values-override
- releases-and-rollback
- chart-repositories-and-subcharts
- diff-and-dry-run
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: middle
order: 4
prereqs: []
slug: 04-helm-intro
sources:
- https://helm.sh/docs/topics/charts/
- https://helm.sh/docs/chart_template_guide/values_files/
spiral: []
status: ready
summary: 'Helm is the Kubernetes package manager. A chart templates manifests with
  Go-template placeholders; values override them per environment; and install/upgrade/rollback
  track a named, versioned release that raw kubectl apply never gives you.'
title: 'Helm: charts and values'
track: deployment
unit: 10-k8s-deep
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
You have one service to run in three environments. The dev deployment wants 1 replica and the `:dev` image; staging wants 2 and `:rc`; prod wants 6, a different resource budget, and the pinned `:1.8.3` tag. Everything else in the manifest is identical. So the team copies `deployment.yaml` into three folders, and now a change to the readiness probe has to be made in three places by hand — and someone always misses one. Worse, when the prod rollout goes bad at 2am, `kubectl apply` has no "undo." There is no record of what the previous good manifest was, no single command to put it back. You are diffing YAML against your shell history.
</Hook>

<Crux>Helm templates manifests from per-env values and tracks each deploy as a named, versioned release you can upgrade and roll back.</Crux>

<Explanation>

## The problem: raw YAML duplicates, and `kubectl apply` has no undo

Plain Kubernetes YAML has two structural gaps once you run the same app in more than one place. First, **duplication**: environments differ by a handful of values — replica count, image tag, resource limits, a hostname — but YAML has no parameters, so you copy the whole manifest set per environment and keep them in sync by hand. Drift is inevitable. Second, **no release unit**: `kubectl apply -f` pushes the current files to the cluster and forgets. It does not record "this is version 4 of my-api," it cannot bundle a Deployment + Service + ConfigMap + Ingress as one atomic thing, and it has no concept of rolling back to the previous version. If you want to undo, you have to reconstruct the old YAML yourself.

Helm — the de-facto **package manager for Kubernetes** — closes both gaps. It introduces a packaging unit (the chart), a parameterisation mechanism (values), and a stateful, versioned deploy unit (the release).

## A chart is a package: templates + default values + metadata

A **chart** is a directory (or a packaged `.tgz`) with a fixed layout. The three parts that matter day one:

```text
my-api/
  Chart.yaml          # metadata: name, version, appVersion, dependencies
  values.yaml         # default values (the public API of the chart)
  templates/
    deployment.yaml   # K8s manifests, but with Go-template placeholders
    service.yaml
    _helpers.tpl      # reusable template snippets
```

`templates/` holds ordinary Kubernetes manifests with **Go-template placeholders** punched into the spots that vary. Helm renders these templates against a values tree to produce final, plain YAML, then sends that to the cluster.

```yaml
# templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-api
spec:
  replicas: {{ .Values.replicaCount }}
  template:
    spec:
      containers:
        - name: api
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          resources:
            limits:
              memory: {{ .Values.resources.limits.memory }}
```

`values.yaml` ships the **defaults** — and doubles as the chart's documented public interface:

```yaml
# values.yaml (defaults)
replicaCount: 1
image:
  repository: registry.example.com/my-api
  tag: dev
resources:
  limits:
    memory: 256Mi
```

## Values override the templates per environment

The same chart renders different manifests because you feed it different **values**. You keep one small file per environment and pass it at install/upgrade time with `-f`, or override single keys with `--set`. Precedence is layered: the chart's `values.yaml` is the base, each `-f` file overrides it left-to-right, and `--set` flags win over everything.

```yaml
# values-prod.yaml — only what differs from the defaults
replicaCount: 6
image:
  tag: "1.8.3"
resources:
  limits:
    memory: 1Gi
```

```bash
# one chart, three environments — values are the only difference
helm upgrade --install my-api ./my-api -f values-prod.yaml
helm upgrade --install my-api ./my-api -f values-staging.yaml --set image.tag=rc-42
```

This is the load-bearing idea: **values are the environment boundary.** The template encodes the *shape* of your deployment once; the values carry the per-environment *facts*. There is exactly one readiness-probe definition to maintain, and dev/staging/prod can no longer drift apart in the parts that should be identical.

<Inset kind="why" lang="en">
Keep environment-specific values files thin — only the keys that genuinely differ from `values.yaml`. If you copy the whole defaults file per environment, you have just reinvented the duplication problem Helm exists to solve, only now it is hidden one level down. The discipline is: the chart's `values.yaml` is the single source of shape and sane defaults; each per-environment values file is a short diff against it.
</Inset>

## Releases: install, upgrade, rollback as one versioned unit

When you `helm install` (or `helm upgrade --install`) a chart, Helm creates a named **release** and records a numbered **revision** of exactly what it deployed, stored as a Secret in the cluster. Every `helm upgrade` bumps the revision. Because Helm remembers each revision, `helm rollback my-api 4` is a single command that re-applies a known-good past state — the "undo" `kubectl apply` never had. `helm uninstall` removes the whole release's resources together. This turns a deploy from "a pile of YAML I shoved at the API server" into an atomic, versioned, revertible transaction over a named bundle.

```bash
helm upgrade --install my-api ./my-api -f values-prod.yaml  # -> revision 5
helm history my-api                                         # see all revisions
helm rollback my-api 4                                      # atomic undo to a known-good state
```

<FlowDiagram
  label="Helm render and release pipeline: chart templates plus values are rendered into plain manifests, applied to the cluster, and recorded as a numbered release revision"
  hue="var(--d-systems)"
  perRow={5}
  nodes={[
    { id: "chart", label: "Chart", sub: "templates/" },
    { id: "values", label: "Values", sub: "-f / --set" },
    { id: "render", label: "Render", sub: "Go-template" },
    { id: "manifests", label: "Manifests", sub: "plain YAML" },
    { id: "release", label: "Release", sub: "rev N" },
  ]}
  edges={[
    { from: "chart", to: "render", label: "templates" },
    { from: "values", to: "render", label: "fill" },
    { from: "render", to: "manifests", label: "output" },
    { from: "manifests", to: "release", label: "apply + record" },
  ]}
  caption="Chart + values render to plain manifests, which Helm applies and records as a numbered, revertible revision. Swap the values file and the same chart renders a different environment; the revision history is what makes helm rollback possible."
/>

## kubectl apply vs Helm, at a glance

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Capability</th>
        <th class="border border-border px-3 py-2 text-left">Raw <code>kubectl apply -f</code></th>
        <th class="border border-border px-3 py-2 text-left">Helm</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Templating / parameters</td>
        <td class="border border-border px-3 py-2">None — static YAML, copy per env</td>
        <td class="border border-border px-3 py-2">Go-template placeholders filled from values</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Multi-environment</td>
        <td class="border border-border px-3 py-2">Duplicate manifests, manual sync, drift</td>
        <td class="border border-border px-3 py-2">One chart, thin <code>values-&lt;env&gt;.yaml</code> per env</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Release / rollback</td>
        <td class="border border-border px-3 py-2">No release unit, no built-in undo</td>
        <td class="border border-border px-3 py-2">Named release, numbered revisions, <code>helm rollback</code></td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Packaging / sharing</td>
        <td class="border border-border px-3 py-2">Ad-hoc files in a repo</td>
        <td class="border border-border px-3 py-2"><code>.tgz</code> chart, versioned, pushed to a chart repo</td>
      </tr>
    </tbody>
  </table>
</div>

## Sharing and reuse: repositories and subcharts

Why write a Postgres StatefulSet from scratch when production-grade charts already exist, are battle-tested, and carry sensible defaults? This is where the chart ecosystem pays off.

Charts are distributable artifacts. Public **chart repositories** — Bitnami being the best-known — publish production-grade charts for off-the-shelf infrastructure (Postgres, Redis, nginx), so you do not hand-write a stateful-set for a database. You can also compose: a chart can declare **dependencies** (subcharts) in `Chart.yaml`, and a parent's values can override a subchart's values, so an umbrella chart wires your app together with its dependencies as one release.

<Inset kind="warn" lang="en">
Helm's `--reuse-values` flag carries forward the previous release's values during an upgrade, which sounds convenient but silently masks changes you made to `values.yaml` and to defaults in the new chart version. Prefer passing your `-f` files explicitly on every upgrade so the rendered output is a pure function of the chart + the values you can see, not of accumulated hidden state from prior revisions.
</Inset>

## Senior notes: never upgrade blind

Two practices separate a safe Helm workflow from a dangerous one. **Preview before you apply.** `helm upgrade --dry-run` renders the manifests without touching the cluster; the widely-used `helm diff upgrade` plugin shows the exact change against the *live* release, so you see "this will delete a PVC" before it happens, not after. **Version everything.** A chart carries two versions in `Chart.yaml`: `version` (the chart's own version, bump on any chart change) and `appVersion` (the app it ships). Treat charts like code — pin the chart version your environments use, and bump it deliberately. And mind the **upgrade-vs-recreate gotcha**: editing an immutable field (a Job's spec, a Service's `clusterIP`, certain volume fields) cannot be patched in place; the upgrade fails or, depending on the resource, forces a recreate that can mean downtime. Read the diff, know which fields are immutable, and plan the rollout — `helm rollback` is your safety net, not your strategy.

<TradeoffMatrix
  id="04-helm-intro-tradeoff-1"
  lessonSlug="04-helm-intro"
  lang="en"
  prompt="You run one service across dev, staging, and prod, differing only in replica count, image tag, and memory limit. You need painless multi-env config and a real rollback path. Pick the approach."
  options={[
    { name: "One Helm chart; a thin values-<env>.yaml per environment; deploy with helm upgrade --install", summary: "Template once, parameterise.", correct: true, justification: "This is exactly the problem Helm is built for. The template encodes the deployment's shape once, so a probe or label change is made in one place; each environment carries only the handful of values that genuinely differ, so dev/staging/prod cannot drift in the parts that should match. And every upgrade creates a numbered revision, giving you helm rollback as a one-command undo and helm history as an audit trail — the release semantics raw kubectl never provides." },
    { name: "Three copied folders of plain YAML, one per environment, applied with kubectl apply -f", summary: "Copy per env.", justification: "This is the starting pain itself. The manifests duplicate everything that is identical, so a single probe change must be edited in three files and one will eventually be missed — drift by construction. And kubectl apply has no release or rollback concept, so an undo means reconstructing the previous YAML by hand from git or memory. It works for one trivial app and collapses the moment you have environments to keep in sync." },
    { name: "A single YAML set with environment chosen by branching kubectl logic in a shell script", summary: "Script the differences.", justification: "You end up reimplementing templating and a release ledger badly in bash. There is no values precedence, no rendered-output preview, no revision history, and no atomic rollback — just a script that is itself untested infrastructure. Helm already solves this with a tested engine; a hand-rolled script trades a known tool for bespoke fragility." },
    { name: "--set every differing key on the kubectl/Helm command line, no values files", summary: "All overrides inline.", justification: "--set is fine for a one-off override, but encoding every per-environment difference as inline flags makes deploys long, error-prone, and unreviewable — there is no file to diff in a PR and no record of what prod's config actually is. Per-environment values files are the reviewable, version-controlled home for these facts; reserve --set for a single ad-hoc tweak on top." },
  ]}
/>

<Quiz
  id="04-helm-intro-quiz-1"
  lessonSlug="04-helm-intro"
  lang="en"
  question="A prod helm upgrade ships a bad config and the service starts erroring. What is the fastest correct recovery, and why is it possible?"
  choices={[
    { label: "helm rollback my-api <previous-revision> — Helm recorded each upgrade as a numbered revision, so it can re-apply a known-good past state in one atomic command", correct: true },
    { label: "kubectl apply the old YAML you reconstruct from git history", misconception: "That can work but it is slower and error-prone: you must find and rebuild the exact previous manifests by hand. The point of Helm is that it already stored the previous revision, so helm rollback restores it directly without you reconstructing anything." },
    { label: "Nothing — once an upgrade is applied it cannot be undone", misconception: "Untrue for Helm specifically. Unlike raw kubectl apply, every helm upgrade saves a numbered revision, and helm rollback re-applies any previous one. That release ledger is the whole reason to use Helm over plain apply." },
    { label: "helm uninstall my-api and reinstall the old chart", misconception: "uninstall deletes the entire release's resources, which is downtime and risks losing state (PVCs depending on policy). Rollback restores the previous revision in place without tearing the release down." },
  ]}
/>

<Quiz
  id="04-helm-intro-quiz-2"
  lessonSlug="04-helm-intro"
  lang="en"
  question="In a chart, where does the per-environment difference between dev and prod (replicas, image tag) belong?"
  choices={[
    { label: "In a thin values-<env>.yaml passed with -f at upgrade time; the templates stay identical and read those keys via .Values", correct: true },
    { label: "Duplicated into separate template files per environment", misconception: "That recreates the duplication Helm exists to remove — the templates would drift exactly like copied raw YAML. Templates encode shape once; only the values vary per environment." },
    { label: "Hard-coded into Chart.yaml metadata", misconception: "Chart.yaml is metadata (name, version, appVersion, dependencies), not runtime configuration. Per-environment values go in values files, not metadata." },
    { label: "Edited directly in the rendered manifests after helm template", misconception: "Editing rendered output bypasses the chart entirely, so the change is lost on the next render and there is no source of truth. The whole point is that values feed the template; you change values, not the output." },
  ]}
/>

</Explanation>

<KeyTakeaway>Helm packages manifests as a chart, parameterises them with values (the per-environment boundary), and tracks each deploy as a named, numbered release. That buys you one source of shape, no cross-env drift, and `helm rollback` — a real undo `kubectl apply` lacks. Preview with `--dry-run`/`helm diff` and version your charts.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-helm-intro-retrieval"
  lang="en"
  questions={[
    { q: "Explain how one Helm chart produces different manifests for dev, staging, and prod, naming the three parts of a chart and how values precedence works.", a: "A chart is a package with three parts that matter here: Chart.yaml (metadata — name, version, appVersion, dependencies), templates/ (Kubernetes manifests with Go-template placeholders like the replica count or image tag punched into the spots that vary), and values.yaml (the default values and the chart's documented public interface). One chart renders different manifests because you feed it different values. The base is the chart's own values.yaml; you then layer environment files with -f (values-prod.yaml, values-staging.yaml), which override the defaults left-to-right; and --set flags override everything on top. So the template encodes the deployment's shape exactly once, and each environment carries only a thin file of the handful of keys that genuinely differ — replicas, image tag, memory limit. Helm renders template + merged values into plain YAML and applies that. The result is one readiness-probe definition to maintain and no drift between environments in the parts that should be identical; values are the environment boundary." },
    { q: "Why can Helm roll back a bad deploy when kubectl apply cannot, and what should you run before an upgrade to avoid needing the rollback?", a: "kubectl apply -f pushes the current files to the cluster and forgets — there is no release object, no bundling of related resources, and no record of the previous manifest, so undoing means reconstructing old YAML by hand. Helm instead creates a named release and stores a numbered revision of exactly what it deployed (as a Secret in the cluster) on every install and upgrade. Because each revision is recorded, helm rollback my-api <revision> re-applies a known-good past state in one atomic command, and helm history shows the full ledger. To avoid needing it, preview before applying: helm upgrade --dry-run renders the manifests without touching the cluster, and the helm diff upgrade plugin shows the exact change against the live release — so you catch a destructive or immutable-field change (a Service clusterIP, a Job spec, certain volume fields that force a recreate and downtime) before it happens. Read the diff, know which fields are immutable, and keep rollback as the safety net rather than the plan." },
  ]}
/>

<Recap lang="en">
Raw Kubernetes YAML has two gaps once an app runs in more than one place: it duplicates everything that is identical across environments, and kubectl apply has no release or rollback unit. Now when you inherit a cluster where someone has been doing `kubectl apply -f` across three environments with copied manifests, you know the path out: one chart, one thin values file per environment, and a release ledger that makes `helm rollback` your 2am lifeline. Helm — the Kubernetes package manager — closes both. A chart is a package: Chart.yaml metadata, templates/ holding manifests with Go-template placeholders, and values.yaml carrying the defaults and the chart's public interface. The same chart renders different manifests because values override the templates per environment — a thin per-environment values file passed with `-f`, or single keys via `--set`, layered base-to-override-to-flag. Values are the environment boundary: shape lives in the template once, per-env facts live in small values files, so dev/staging/prod cannot drift. Each deploy is a named, numbered release, so helm upgrade bumps a revision Helm stores and helm rollback re-applies a known-good one atomically — the undo apply never had. Reuse off-the-shelf infra from public chart repos like Bitnami and compose with subchart dependencies. And never upgrade blind: helm --dry-run and helm diff preview the change, version your charts via Chart.yaml's version and appVersion, and watch the upgrade-vs-recreate gotcha where immutable fields force a disruptive recreate.
</Recap>
