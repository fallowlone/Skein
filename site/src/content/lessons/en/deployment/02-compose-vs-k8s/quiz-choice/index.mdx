---
concepts:
- orchestration-scope
- reconciliation-loop
- self-healing
- rolling-update
- complexity-tax
- middle-ground
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://docs.docker.com/compose/intro/features-uses/
- https://kubernetes.io/docs/concepts/overview/
- https://encore.cloud/resources/do-you-need-kubernetes
status: ready
summary: "Multiple-choice synthesis across the Compose-vs-Kubernetes unit: orchestration scope, reconciliation, self-healing, gated rollouts, and the complexity tax weighed against real scale."
title: 'Compose vs Kubernetes: multiple-choice review'
track: deployment
unit: 02-compose-vs-k8s
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one mirrors an orchestration decision you make for real — not a definition to recite, but a tradeoff to weigh against the scale you actually have.
</Hook>

<Goal lang="en">
Confirm you can connect orchestration scope, the reconciliation loop, self-healing, gated rollouts, and the complexity tax — and decide which weight a given workload earns.
</Goal>

<Quiz
  id="02-compose-vs-k8s-choice-1"
  lessonSlug="02-compose-vs-k8s"
  lang="en"
  question="A team runs an API, Postgres, Redis, and a worker on one rented box with modest traffic. They want resilience to a crashed container and a single declarative file. Which orchestration weight fits, and why?"
  choices={[
    { label: "Docker Compose on the single host. One docker-compose.yml declares all four services, networks, and volumes; restart: always covers crashed containers; ~50MB overhead, debuggable with docker compose logs. The workload exercises no multi-node need, so a heavier orchestrator is pure tax.", correct: true },
    { label: "Self-managed Kubernetes now, to be ready to scale.", misconception: "This is the classic too-early mistake. A small team would own a control plane, CNI, RBAC, ingress, and YAML sprawl to self-heal across machines they don't have. Migrate when a real ceiling is in sight — not a hypothetical one." },
    { label: "No orchestrator — docker run each container by hand.", misconception: "That loses Compose's single declarative file, named networks, dependency ordering, and one-command up/down for zero benefit. Compose is the floor for multi-container apps; dropping below it is just manual drift." },
    { label: "A managed Kubernetes service (EKS/GKE/AKS), since it removes the ops burden.", misconception: "Managed k8s removes control-plane operations but not the conceptual complexity — you still own pods, networking, RBAC, and the YAML. It is far heavier than this single-host workload needs." },
  ]}
/>

<Quiz
  id="02-compose-vs-k8s-choice-2"
  lessonSlug="02-compose-vs-k8s"
  lang="en"
  question="What does the Kubernetes reconciliation loop give you that Compose's restart: always fundamentally cannot?"
  choices={[
    { label: "It continuously drives actual state toward a declared desired state across the cluster — when a whole node dies, controllers reschedule its pods onto healthy nodes. That is self-healing across machines; restart: always only restarts a container on the same host.", correct: true },
    { label: "It restarts a crashed container faster on the same host.", misconception: "Speed is not the point. restart: always already handles same-host restarts; the loop's value is surviving a lost node, which Compose cannot do because it has no multi-node scheduling." },
    { label: "It makes the YAML shorter and simpler.", misconception: "Kubernetes YAML is famously larger, not shorter — pods, services, deployments, ingress. That sprawl is part of the complexity tax you pay for reconciliation, not a benefit." },
    { label: "It removes the need for health checks.", misconception: "Reconciliation relies on readiness and liveness probes to know what healthy means — it adds health gating, it does not remove it." },
  ]}
/>

<Quiz
  id="02-compose-vs-k8s-choice-3"
  lessonSlug="02-compose-vs-k8s"
  lang="en"
  question="A service must roll out a new release with zero downtime and automatic rollback if the new version is unhealthy. How do Compose and a Kubernetes Deployment differ here?"
  choices={[
    { label: "A Deployment brings up new pods, waits for their readiness probe to pass, shifts traffic, then tears down the old ones — so a bad release never serves traffic and rollback is automatic. Compose restarts the service in place; for the window between stop and healthy-start it is simply down, with no health gate.", correct: true },
    { label: "Both gate the rollout on a readiness probe; the difference is only speed.", misconception: "Compose has no readiness-gated rollout at all. It restarts in place with a downtime window — the health gate and automatic rollback are exactly what only the reconciliation loop provides." },
    { label: "Compose does zero-downtime deploys natively; Kubernetes needs a service mesh for it.", misconception: "It is the reverse. Rolling updates gated on readiness are built into a Deployment; Compose restarts in place. No mesh is required for basic rolling updates." },
    { label: "Neither can roll back automatically; both require a manual redeploy.", misconception: "A Kubernetes Deployment keeps rollout history and can roll back automatically when new pods fail readiness. Compose has no such mechanism." },
  ]}
/>

<Quiz
  id="02-compose-vs-k8s-choice-4"
  lessonSlug="02-compose-vs-k8s"
  lang="en"
  question="What are the honest signals that a workload has genuinely outgrown Docker Compose?"
  choices={[
    { label: "The single-host ceiling (must spread load across machines, not just a bigger box), availability (a lost host is no longer acceptable), and operational needs Compose lacks (health-gated zero-downtime rollout, horizontal autoscaling). Hit one for real and the orchestration tax starts paying for itself.", correct: true },
    { label: "The docker-compose.yml has grown past a dozen services.", misconception: "File size is not the signal. Compose handles many services on one host fine; what forces the move is needing capabilities Compose lacks — multi-node scheduling, self-healing across machines — not line count." },
    { label: "You want to use container images instead of bare processes.", misconception: "Both Compose and Kubernetes run container images; that shared verb hides the real difference. Image usage is no reason to switch orchestrators." },
    { label: "The team wants nicer dashboards and metrics.", misconception: "Observability tooling is orthogonal to orchestration weight — you can ship dashboards on a single Compose host. The move to k8s is driven by scheduling and availability needs, not dashboards." },
  ]}
/>

<Quiz
  id="02-compose-vs-k8s-choice-5"
  lessonSlug="02-compose-vs-k8s"
  lang="en"
  question="Surveys repeatedly rank operational complexity as the top Kubernetes pain (~70% of users), and clusters routinely sit at 30–50% utilization. What is the senior reading of these numbers?"
  choices={[
    { label: "Kubernetes' capabilities arrive bundled with a permanent complexity tax — control plane, CNI, RBAC, ingress, YAML sprawl, on-call burden, low-five-figure monthly bills — and a lot of provisioned capacity is waste. The tax only pays off when you genuinely need multi-node scheduling, gated rollout under real traffic, or autoscaling.", correct: true },
    { label: "It means most teams configure Kubernetes wrong; a correct setup removes the complexity.", misconception: "The complexity is intrinsic to running a distributed control plane, not a misconfiguration. Even well-run clusters carry the conceptual and operational weight; the question is whether your scale justifies it." },
    { label: "Low utilization proves Kubernetes wastes memory at runtime, like a leaky process.", misconception: "30–50% utilization is about provisioned-versus-used cluster capacity and headroom, not a runtime memory leak. It reflects over-provisioning relative to actual load." },
    { label: "The numbers are irrelevant once you use a managed service.", misconception: "Managed k8s removes control-plane operations but not the conceptual complexity or the utilization economics — you still own pods, networking, and right-sizing the cluster." },
  ]}
/>

<Quiz
  id="02-compose-vs-k8s-choice-6"
  lessonSlug="02-compose-vs-k8s"
  lang="en"
  question="A team needs multi-node resilience and zero-downtime deploys but does not want to own a control plane or learn the full Kubernetes surface. What is the senior framing of their options?"
  choices={[
    { label: "It is not binary. Between one Compose host and self-managed Kubernetes sit Docker Swarm, HashiCorp Nomad, and managed PaaS / serverless containers (Cloud Run, App Runner, ECS, Container Apps) that handle scaling and rollout without you owning a control plane. Pick the lightest option that meets the need.", correct: true },
    { label: "Their only real choices are Compose or full self-managed Kubernetes.", misconception: "That false binary is exactly what drives premature k8s adoption. Swarm, Nomad, and managed PaaS give multi-node resilience and gated rollout well before self-managed Kubernetes is warranted." },
    { label: "A managed Kubernetes service is always the lightest middle ground.", misconception: "Managed k8s removes control-plane ops but keeps the full conceptual surface — pods, networking, RBAC, YAML. A managed PaaS or Swarm is often lighter for teams that just want scaling and rollout." },
    { label: "Stay on Compose and add a load balancer in front of multiple hosts manually.", misconception: "Hand-rolling multi-host Compose behind a load balancer gives no self-healing, no rescheduling on node loss, and no gated rollout — you rebuild a fragile orchestrator by hand. A purpose-built middle-ground tool is the better answer." },
  ]}
/>

<Recap lang="en">
The through-line is one decision: capability versus complexity tax, weighed against your actual scale. Orchestration scope (single host vs many nodes) and the reconciliation loop are what separate the tools; self-healing, gated rollouts, and autoscaling are what the loop buys you — at the cost of a control plane, CNI, RBAC, and YAML sprawl. You move off Compose when you hit a real single-host ceiling, a real availability need, or a real rollout/autoscaling need — and even then the answer is often a middle ground (Swarm, Nomad, managed PaaS), not self-managed Kubernetes. Buy the weight your traffic earns.
</Recap>
