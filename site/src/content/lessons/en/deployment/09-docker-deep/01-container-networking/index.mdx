---
concepts:
- network-drivers
- bridge-network
- user-defined-bridge-dns
- port-publishing
- localhost-trap
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: middle
order: 1
prereqs: []
slug: 01-container-networking
sources:
- https://docs.docker.com/network/
- https://docs.docker.com/network/drivers/bridge/
- https://docs.docker.com/reference/cli/docker/container/run/
spiral: []
status: ready
summary: 'Containers talk over network drivers — bridge by default. A user-defined bridge adds DNS by container name; the default bridge does not. Publish ports only for outside access, and never trust localhost inside a container.'
title: 'Container networking: bridges, DNS by name, and the localhost trap'
track: deployment
unit: 09-docker-deep
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Two containers, `api` and `db`, both running, both healthy. The `api` logs `ECONNREFUSED 127.0.0.1:5432` on every request. The developer swears the database is up — `docker ps` proves it — and burns an afternoon checking Postgres config, firewalls, and credentials. The bug is none of those. The connection string says `localhost`, and inside the `api` container `localhost` means the `api` container itself, where nothing listens on 5432. The database is one network hop away, reachable by the name `db` — but only if the two containers share the right kind of network.
</Hook>

<Crux>Containers talk over a driver — bridge by default. A user-defined bridge resolves container names to IPs; the default one does not.</Crux>

<Explanation>

## Drivers: how a container gets a network at all

A container starts in its own network namespace — its own interfaces, routing table, and `localhost`. The **network driver** decides how that namespace connects to anything else. You pick the driver when you create the network or run the container.

The **bridge** driver is the default. Docker creates a private virtual switch on the host (`docker0` for the default network) and connects each container to it with a virtual ethernet pair. Containers get internal IPs on a private subnet (often `172.17.0.0/16`), talk to each other over that bridge, and reach the outside world through NAT — the host masquerades their traffic behind its own IP. From outside the host, those container IPs are invisible until you publish a port.

The **host** driver removes the isolation entirely: the container shares the host's network namespace. There is no separate container IP, no NAT, and no port mapping — if the app binds `:8080`, it is on the host's `:8080` directly. That is the fastest option (no bridge, no NAT hop) and the reason to use it for latency-sensitive or high-throughput workloads, but you give up isolation and you cannot run two containers that both want the same port.

The **none** driver gives the container a namespace with only loopback — no external connectivity at all, for fully sandboxed work. **overlay** spans multiple hosts (Swarm services, multi-host clusters) by tunnelling container traffic between daemons. **macvlan** hands the container its own MAC address so it appears as a physical device on the LAN, for legacy systems that expect that. The everyday workhorse is bridge; the rest are situational.

```bash
docker run -d --name web nginx                 # default bridge
docker run -d --network host nginx             # share the host's stack, no -p needed
docker run -d --network none alpine sleep 1d   # loopback only, isolated
docker network ls                              # bridge, host, none + any you created
```

## The default bridge vs a user-defined bridge

Here is the senior point that the hook turns on. There are two kinds of bridge network, and they are not equivalent.

The **default bridge** (the one named `bridge` that every container joins unless told otherwise) has no built-in DNS. Containers on it can reach each other only by raw IP address — which is fragile, because IPs are assigned dynamically and change across restarts. The old workaround was the deprecated `--link` flag.

A **user-defined bridge** — any network you create yourself — adds an embedded DNS server. Containers on it resolve each other **by container name** automatically. Start `db` and `api` on the same user-defined network and `api` can connect to `db:5432` with no IP, no link, no config. This is service discovery, and it is the single reason to always put related containers on a network you created rather than the default bridge.

```bash
docker network create appnet                              # user-defined bridge
docker run -d --name db  --network appnet postgres:16
docker run -d --name api --network appnet my/api          # api reaches db by the name "db"
```

This is exactly what `docker compose` does for you: every Compose project gets its own user-defined bridge, so services reach each other by their service name out of the box. You rarely create the network by hand because Compose already did.

<Inset kind="why" lang="en">
On a user-defined bridge, name resolution is dynamic: if `db` restarts and gets a new IP, the embedded DNS returns the new address on the next lookup, so `api` keeps working without a restart. The default bridge has no such DNS, which is why hardcoding a container IP there breaks on the next restart. Docker's DNS also forwards names it does not know to the host's resolver, so external hostnames still resolve normally from inside the container.
</Inset>

## Publishing ports vs talking inside the network

Two containers on the same user-defined network talk **directly on the container's own port** — `api` hits `db:5432` whether or not 5432 is ever published. Publishing is only about reaching a container **from outside the Docker network**, typically from the host or the internet.

`-p host:container` (`--publish`) maps a host port onto a container port through the bridge's NAT. `-p 8080:80` means "send the host's :8080 to this container's :80." You publish the public-facing service (the web server, the API gateway) and leave internal services like the database unpublished — they do not need a host port because their clients are on the same network.

A common confusion: **`EXPOSE` in a Dockerfile does not publish anything.** It is documentation — metadata declaring which port the app listens on. It opens no host port and changes no firewall. Only `-p` (or Compose's `ports:`) actually publishes; `EXPOSE` just tells readers and tools the intent.

```bash
docker run -d --network appnet -p 8080:80 --name web my/web   # host:8080 → container:80
# db has NO -p: it is unreachable from the host, but web reaches it at db:5432 internally
```

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Driver</th>
        <th class="border border-border px-3 py-2 text-left">Isolation</th>
        <th class="border border-border px-3 py-2 text-left">DNS by name</th>
        <th class="border border-border px-3 py-2 text-left">Port mapping</th>
        <th class="border border-border px-3 py-2 text-left">Use it for</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2"><code>bridge</code> (user-defined)</td>
        <td class="border border-border px-3 py-2">Own namespace + IP</td>
        <td class="border border-border px-3 py-2">Yes (by container name)</td>
        <td class="border border-border px-3 py-2"><code>-p</code> for outside access</td>
        <td class="border border-border px-3 py-2">Default for multi-container apps</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>bridge</code> (default)</td>
        <td class="border border-border px-3 py-2">Own namespace + IP</td>
        <td class="border border-border px-3 py-2">No (IP only)</td>
        <td class="border border-border px-3 py-2"><code>-p</code> for outside access</td>
        <td class="border border-border px-3 py-2">Quick one-offs; avoid for app wiring</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>host</code></td>
        <td class="border border-border px-3 py-2">None (shares host stack)</td>
        <td class="border border-border px-3 py-2">N/A (host resolver)</td>
        <td class="border border-border px-3 py-2">None &#123;no <code>-p</code>&#125;</td>
        <td class="border border-border px-3 py-2">Lowest latency, single port owner</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2"><code>none</code></td>
        <td class="border border-border px-3 py-2">Total (loopback only)</td>
        <td class="border border-border px-3 py-2">No</td>
        <td class="border border-border px-3 py-2">No connectivity</td>
        <td class="border border-border px-3 py-2">Fully sandboxed jobs</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2"><code>overlay</code></td>
        <td class="border border-border px-3 py-2">Own namespace + IP</td>
        <td class="border border-border px-3 py-2">Yes (Swarm services)</td>
        <td class="border border-border px-3 py-2">Published per service</td>
        <td class="border border-border px-3 py-2">Multi-host / Swarm clusters</td>
      </tr>
    </tbody>
  </table>
</div>

## The localhost trap

Now the hook resolves. Inside a container, `localhost` (`127.0.0.1`) is **the container's own loopback** — not the host, and not any other container. Each container has its own loopback interface in its own namespace. So `api` connecting to `localhost:5432` is asking *itself* for Postgres, finds nothing listening, and gets `ECONNREFUSED`.

The fixes follow from everything above:

- **To reach another container**, use its name on a shared user-defined network: `db:5432`. Never `localhost`, never a hardcoded IP.
- **To reach a service on the host machine** from inside a container (a database running natively on your laptop, say), use `host.docker.internal`, which Docker resolves to the host's address. (`host` networking is the other route — then `localhost` *is* the host, because there is no separate namespace.)
- **To reach the container from the host**, publish a port with `-p` and connect to `localhost:<hostPort>` on the host.

`localhost` only means "this same container." The instant you cross a container boundary, you need a name — `db`, `api`, or `host.docker.internal`.

<Quiz
  id="01-container-networking-quiz-1"
  lessonSlug="01-container-networking"
  lang="en"
  question="The api container connects to localhost:5432 to reach the db container, both on the same user-defined network, and gets ECONNREFUSED. What is wrong?"
  choices={[
    { label: "Inside the api container localhost is api's own loopback, not db — it should connect to db:5432, the container name the user-defined network's DNS resolves", correct: true },
    { label: "The db container needs -p 5432:5432 published for api to reach it", misconception: "Publishing is only for access from outside the Docker network. Containers on the same user-defined network reach each other on the container port directly — db:5432 works with no -p at all." },
    { label: "User-defined bridges do not support container-to-container traffic", misconception: "They are the recommended way to wire containers together — they add automatic DNS by name on top of normal bridge connectivity. The problem is the address localhost, not the network." },
    { label: "The two containers need to share the host network driver", misconception: "Host networking would make localhost mean the host, which still is not where db listens unless db is also on host. The clean fix is name resolution on the shared user-defined bridge: db:5432." },
  ]}
/>

<Quiz
  id="01-container-networking-quiz-2"
  lessonSlug="01-container-networking"
  lang="en"
  question="A Dockerfile has EXPOSE 80 but the container is started with no -p flag. Can you reach the app from your host browser?"
  choices={[
    { label: "No — EXPOSE is only documentation; without -p (or Compose ports:) no host port is published, so nothing on the host routes to the container", correct: true },
    { label: "Yes, EXPOSE opens the port on the host automatically", misconception: "EXPOSE publishes nothing. It is metadata declaring the listen port for humans and tools; only -p / --publish or Compose ports: actually maps a host port to the container." },
    { label: "Yes, but only on localhost inside the container", misconception: "The app may indeed listen inside the container, but with no published port the host has no route in. EXPOSE does not change that — you still need -p." },
    { label: "No, because EXPOSE blocks external access for security", misconception: "EXPOSE neither opens nor blocks anything — it is inert documentation. The reason you cannot reach it is simply the absence of a published port." },
  ]}
/>

<TradeoffMatrix
  id="01-container-networking-tradeoff-1"
  lessonSlug="01-container-networking"
  lang="en"
  prompt="A multi-container app (web + api + db) runs on one host and the web tier must be reachable from the internet. Pick the networking setup."
  options={[
    { name: "A user-defined bridge for all three; publish only the web port with -p; services talk by name (api, db)", summary: "DNS by name, minimal exposure.", correct: true, justification: "This is the standard senior choice. The user-defined bridge gives automatic DNS so web reaches api and api reaches db by name with no hardcoded IPs and no fragility across restarts. Publishing only the web port keeps the database and internal api off the host and off the internet — they are reachable solely from inside the network. This is exactly the topology docker compose builds for you." },
    { name: "The default bridge for all three; connect by container IP looked up with docker inspect", summary: "Works until a restart.", justification: "The default bridge has no DNS, so you fall back to raw IPs, which Docker assigns dynamically and reshuffles on restart — your wiring breaks the next time a container bounces. It also lacks the isolation niceties of a user-defined network. Create your own network and use names instead." },
    { name: "Host networking for all three so they share the host stack and reach each other on localhost", summary: "No isolation, port clashes.", justification: "Host networking drops isolation and removes per-container ports, so three services fighting over the host's port space collide, and every listening port is exposed on the host with no -p gate. It can suit a single latency-critical service, but it is the wrong fit for a multi-service app that needs internal-only tiers." },
    { name: "Put each container on its own separate user-defined network", summary: "Isolated to the point of broken.", justification: "DNS-by-name and direct connectivity only work between containers on the SAME network. Isolating each one means web cannot resolve or reach api or db at all. Segmenting networks is useful for security boundaries, but the tiers that must talk have to share a network." },
  ]}
/>

</Explanation>

<FlowDiagram
  label="Two containers on a user-defined bridge reach each other by name, while the bridge NATs outbound traffic and a published port lets the host in"
  hue="var(--d-systems)"
  perRow="3"
  nodes={[
    { id: "host", label: "Host", sub: ":8080 published", col: 0, row: 0 },
    { id: "bridge", label: "User bridge", sub: "appnet + DNS", col: 1, row: 0 },
    { id: "web", label: "web", sub: ":80", col: 2, row: 0 },
    { id: "db", label: "db", sub: ":5432", col: 2, row: 1 },
    { id: "net", label: "Internet", sub: "via NAT", col: 0, row: 1 },
  ]}
  edges={[
    { from: "host", to: "bridge", label: "-p 8080:80" },
    { from: "bridge", to: "web", label: "" },
    { from: "web", to: "db", label: "db:5432 by name" },
    { from: "bridge", to: "net", label: "NAT out" },
  ]}
  caption="Containers on the same user-defined bridge reach each other by name (web → db:5432) with no published port. -p maps a host port in; outbound traffic leaves through the bridge's NAT. Inside web, localhost would mean web itself — never db."
/>

<KeyTakeaway>A user-defined bridge adds DNS so containers reach each other by name — always wire app containers on one. Publish (`-p`) only for outside access; `EXPOSE` never publishes. And `localhost` is that container, not a peer.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-container-networking-retrieval"
  lang="en"
  questions={[
    { q: "Why does a user-defined bridge let containers reach each other by name when the default bridge does not, and what does that change in practice?", a: "Every Docker network uses a driver; bridge is the default and builds a private virtual switch on the host that connects containers by virtual ethernet pairs, giving each an internal IP on a private subnet and NAT to the outside. The crucial difference is DNS. The default network named bridge has no embedded DNS, so containers on it can address each other only by raw IP — which Docker assigns dynamically and changes across restarts, making the wiring fragile (the old fix was the deprecated --link). Any network you create yourself — a user-defined bridge — runs an embedded DNS server that resolves container names to their current IPs automatically. So if db and api share a user-defined network, api connects to db:5432 with no IP, no link, and no config, and it keeps working even if db restarts with a new IP because the next DNS lookup returns the new address. In practice this is service discovery, and it is why you always put related containers on a network you created — which is exactly what docker compose does: each project gets its own user-defined bridge so services resolve each other by service name out of the box." },
    { q: "Inside a container, what does localhost mean, and how do you reach (a) another container, (b) a service on the host machine, and (c) the container from the host?", a: "Each container has its own network namespace with its own loopback, so inside a container localhost (127.0.0.1) is that container itself — not the host and not any peer. Connecting to localhost:5432 from the api container asks api for Postgres, finds nothing, and returns ECONNREFUSED; that is the classic bug. To reach (a) another container, use its name on a shared user-defined network — db:5432 — never localhost or a hardcoded IP, since the embedded DNS resolves the name to the live address. To reach (b) a service running natively on the host machine from inside a container, use host.docker.internal, which Docker resolves to the host's address; alternatively run the container with host networking, in which case there is no separate namespace and localhost does mean the host. To reach (c) the container from the host, publish a port with -p hostPort:containerPort and connect to localhost:hostPort on the host — note that publishing is only for crossing into the Docker network from outside; containers on the same network already talk on the container port directly, and EXPOSE alone publishes nothing." },
  ]}
/>

<Recap lang="en">
A container starts in its own network namespace, and the network driver decides how it connects. Bridge is the default: a private virtual switch on the host gives each container an internal IP and NATs its outbound traffic, while host networking shares the host's stack with no isolation and no port mapping, none gives loopback only, and overlay and macvlan cover multi-host and LAN-presence cases. The senior distinction is between the default bridge, which has no DNS and forces fragile IP addressing, and a user-defined bridge, whose embedded DNS lets containers resolve each other by container name — automatic service discovery, and the reason docker compose creates a network per project so services reach each other by name. Publishing with -p maps a host port to a container port and is needed only for access from outside the Docker network; containers on the same user-defined network already talk on the container port directly, and EXPOSE is mere documentation that publishes nothing. Finally, localhost inside a container is that container's own loopback, so reach a peer by name (db:5432), the host by host.docker.internal, and the container from the host via a published port — never assume localhost crosses a container boundary.
</Recap>
