---
concepts:
- writable-layer-ephemerality
- named-volumes
- bind-mounts
- tmpfs-mounts
- volume-lifecycle
- volumes-are-host-local
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: middle
order: 2
prereqs: []
slug: 02-volumes-and-persistence
sources:
- https://docs.docker.com/storage/volumes/
- https://docs.docker.com/storage/
spiral: []
status: ready
summary: 'A container''s writable layer dies with the container, so persistent data
  needs a mount: a Docker-managed named volume for databases, a bind mount for dev
  source, tmpfs for in-memory secrets — each with its own lifecycle and traps.'
title: 'Volumes and persistence'
track: deployment
unit: 09-docker-deep
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import StackDiagram from "~/components/diagram/StackDiagram.astro";

<Hook>
A `docker compose down && docker compose up` on the staging box wiped the database. Two weeks of seeded test data, gone — no `rm`, no `DROP`, no human error in the usual sense. Postgres had been writing to the container's own writable layer the whole time, because nobody declared a volume. Removing the container removed the layer, and the layer *was* the data. The fix was one line in the compose file. The lesson was that container storage is ephemeral by default, and you opt into persistence on purpose.
</Hook>

<Crux>A container's writable layer dies with it. To keep data, mount external storage: a named volume, a bind mount, or in-memory tmpfs.</Crux>

<Explanation>

## The writable layer is scratch space, not storage

Before you add a database to any Docker setup, ask yourself: where does a restart leave your data? The answer determines whether you come in on Monday to find an empty database.

A running container is its read-only image layers plus one thin **writable layer** on top, stitched together by a union filesystem. Every file your process creates or modifies at runtime — a Postgres data directory, an uploaded avatar, a log file — lands in that writable layer. It feels like a normal disk, which is exactly the trap.

That layer is bound to the container's lifecycle. Remove the container (`docker rm`, `compose down`, a redeploy that recreates it) and the writable layer is deleted with it. The data is not "somewhere on the host you can dig out" — it lived inside the container's own diff and is gone. Worse, while the container *is* alive, everything you write there inflates the container's size and goes through the storage driver's union-filesystem abstraction, which is slower than writing to the host directly. The writable layer is for ephemeral scratch — temp files, a process's runtime state — never for anything you'd be upset to lose.

To persist data you mount storage that lives **outside** the writable layer. Docker gives you three mount types, and choosing the right one is the whole skill.

## Named volumes: the default for data you keep

A **named volume** is storage Docker creates and manages for you, living on the host under `/var/lib/docker/volumes/<name>/_data`. You never touch that path directly — you refer to the volume by name, and Docker handles the rest. This is the recommended mechanism for anything stateful: database files, message-queue data, uploaded user content.

```bash
# Create on demand and mount it; -v <name>:<container-path>
docker run -d --name db \
  -v pgdata:/var/lib/postgresql/data \
  postgres:16

# The explicit, self-documenting form
docker run -d --name db \
  --mount type=volume,src=pgdata,dst=/var/lib/postgresql/data \
  postgres:16
```

```yaml
# docker-compose.yml — the one line that saves the staging box
services:
  db:
    image: postgres:16
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
```

Named volumes win for stateful data on three counts. They are **decoupled from host layout** — you reference `pgdata`, not `/home/deploy/whatever`, so the same compose file runs on your laptop and the server. They are **portable and backup-able** — Docker can manage, inspect, and back them up as first-class objects, and they survive container removal by design. And they **write to the host filesystem directly** rather than through the union layer, so they are faster for the write-heavy workloads databases generate.

## Bind mounts: a host path mapped straight in

A **bind mount** maps a specific directory or file from the host machine into the container. Unlike a volume, *you* own the host path and its layout; Docker just wires it through, and the host and container see the same files live.

```bash
# Mount the current project source into the container for live reload
docker run -it \
  --mount type=bind,src="$(pwd)",dst=/app \
  node:22 bash

# Short form (-v with an absolute host path is a bind mount, not a volume)
docker run -it -v "$(pwd)":/app node:22 bash
```

The killer use case is development: bind-mount your source tree into the container and your dev server hot-reloads on every save, no rebuild. But bind mounts couple you to the host's directory layout (the path must exist and look the same everywhere the container runs), and they carry a sharp edge — **a bind mount over a path the image already populated hides that path's original contents** for the life of the mount. Bind an empty host folder onto `/app` after `node_modules` was installed there at build time, and the container now sees an empty `/app` — the install is shadowed, not merged. That is one of the most common "it worked in the image but not when I run it" mysteries.

<Inset kind="why" lang="en">
Why does a bind mount *shadow* rather than *merge*? The mount replaces what the container sees at that path with the host's view of it, exactly like mounting a USB stick over a folder on your laptop: the folder's original contents are still on disk underneath but invisible while the mount is active. Named volumes behave differently in one helpful way — when you mount an *empty* named volume onto a path that the image populated, Docker copies the image's existing files into the fresh volume first, so the data is seeded rather than hidden. A bind mount never does this copy-up.
</Inset>

## tmpfs: in-memory, never persisted

A **tmpfs mount** lives only in the host's RAM. Nothing is ever written to disk, and the moment the container stops the data evaporates. That is the point: it is for data you specifically do *not* want to persist — decrypted secrets, session scratch, intermediate caches you would rather not leave on a disk.

```bash
# Mount an in-memory filesystem at /run/secrets (Linux only)
docker run -it --tmpfs /run/secrets myapp

# Explicit form with options
docker run -it \
  --mount type=tmpfs,dst=/run/secrets,tmpfs-size=64m \
  myapp
```

Reach for tmpfs when a secret must exist as a file for one process to read but should never touch durable storage, or for hot scratch space where RAM speed matters and durability does not. Never use it for anything you need after a restart — by design, there is nothing to recover.

<div data-lesson-visual class="overflow-x-auto my-6">
  <table class="w-full text-sm border-collapse">
    <thead>
      <tr class="bg-surface-2">
        <th class="border border-border px-3 py-2 text-left">Property</th>
        <th class="border border-border px-3 py-2 text-left">Named volume</th>
        <th class="border border-border px-3 py-2 text-left">Bind mount</th>
        <th class="border border-border px-3 py-2 text-left">tmpfs</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class="border border-border px-3 py-2">Managed by</td>
        <td class="border border-border px-3 py-2">Docker (under <code>/var/lib/docker/volumes</code>)</td>
        <td class="border border-border px-3 py-2">You (any host path)</td>
        <td class="border border-border px-3 py-2">Kernel (host RAM)</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Persists?</td>
        <td class="border border-border px-3 py-2">Yes — survives container removal</td>
        <td class="border border-border px-3 py-2">Yes — lives on the host</td>
        <td class="border border-border px-3 py-2">No — gone on stop</td>
      </tr>
      <tr>
        <td class="border border-border px-3 py-2">Use case</td>
        <td class="border border-border px-3 py-2">Database / stateful data</td>
        <td class="border border-border px-3 py-2">Dev source live-reload</td>
        <td class="border border-border px-3 py-2">Secrets / in-memory scratch</td>
      </tr>
      <tr class="bg-surface-2">
        <td class="border border-border px-3 py-2">Portability</td>
        <td class="border border-border px-3 py-2">High — name, not a path</td>
        <td class="border border-border px-3 py-2">Low — couples to host layout</td>
        <td class="border border-border px-3 py-2">N/A — nothing stored</td>
      </tr>
    </tbody>
  </table>
</div>

## Volume lifecycle: the "where did my disk go" trap

Volumes have a lifecycle that is deliberately *independent* of containers, and that independence cuts both ways. A volume is created on demand the first time you reference it, and it **survives container removal** — that is the durability you want. But it also means a volume is never cleaned up automatically. Delete the container and the named volume stays, occupying disk, until you explicitly remove it.

```bash
docker volume ls                 # list volumes
docker volume inspect pgdata     # see the Mountpoint on the host
docker volume rm pgdata          # remove one volume (must be unused)
docker volume prune              # remove ALL volumes not used by a container
```

The senior trap lives in `docker volume prune`. It deletes every volume not currently attached to a container — and a volume whose container you removed last week is, by that definition, "unused." Run a careless prune to reclaim disk and you can erase a database you fully intended to keep. The flip side, **stale volumes silently eating disk**, is the more common day-to-day surprise: months of dead anonymous volumes from removed containers pile up under `/var/lib/docker/volumes` until the host runs out of space.

<TradeoffMatrix
  id="09-docker-deep-volumes-tradeoff-1"
  lessonSlug="02-volumes-and-persistence"
  lang="en"
  prompt="A Postgres container must keep its data across redeploys that recreate the container. Where should the data directory live?"
  options={[
    { name: "A named volume mounted at /var/lib/postgresql/data", summary: "Docker-managed, durable.", correct: true, justification: "This is the standard senior choice for database data. The named volume lives outside the container's writable layer, so recreating or removing the container leaves the data intact, and it writes to the host filesystem directly for the performance a write-heavy database needs. It is decoupled from host paths (the compose file is portable across machines) and is a first-class object Docker can inspect and back up. The only discipline it demands is remembering it must be removed explicitly — it will not vanish on its own." },
    { name: "The container's writable layer (no mount declared)", summary: "Default, ephemeral.", justification: "This is exactly the bug from the hook. The writable layer is bound to the container lifecycle, so any redeploy that recreates the container deletes the data with the layer. It also inflates container size and routes writes through the slower union filesystem. The writable layer is scratch space, never durable storage." },
    { name: "A bind mount to a fixed host path like /opt/pgdata", summary: "Works, but couples.", justification: "It does persist, and some teams do this deliberately for direct host access to the files. But it couples the deployment to a specific host directory layout, sidesteps Docker's volume tooling (inspect/backup as first-class objects), and is easy to get wrong with permissions and the shadowing trap. For pure database persistence a named volume is the cleaner, more portable default." },
    { name: "A tmpfs mount at the data directory", summary: "In-memory, lost on stop.", justification: "tmpfs lives only in RAM and is wiped the instant the container stops — the opposite of what a database needs. It is for secrets and in-memory scratch you explicitly do not want persisted, never for durable data." },
  ]}
/>

## The senior cases: host-local is not the network

Three rules separate someone who can mount a volume from someone who can run stateful workloads. First, **database data MUST live on a named volume, not the container layer** — the hook is this rule learned the hard way. Second, **never bind-mount over a path the image populated** unless you specifically mean to shadow it; that is how `node_modules` and other build-time content silently vanish at runtime. Third, and the one that bites at scale: **volumes are host-local.** A named volume lives on one machine's disk. In a multi-node cluster (Swarm, Kubernetes, several VMs behind a load balancer), a volume on node A is invisible to a container scheduled onto node B. For multi-node persistence you need networked or object storage — a managed database, S3-compatible object storage, or a CSI/network-volume driver — not a plain local volume. Treating a local volume as if it were shared storage is the classic mistake when a single-host compose setup graduates to a cluster. Together these three rules mean that storage decisions made at compose-file time determine your resilience at cluster scale — skipping the second rule loses data silently, skipping the third loses an entire node's data at scale.

<Quiz
  id="09-docker-deep-volumes-quiz-1"
  lessonSlug="02-volumes-and-persistence"
  lang="en"
  question="A Postgres container with no volume declared is removed and recreated during a redeploy. What happens to the data?"
  choices={[
    { label: "It is gone — the data lived in the container's writable layer, which is deleted with the container; declare a named volume to persist it", correct: true },
    { label: "It is safe in /var/lib/docker/volumes until you prune", misconception: "Nothing was written to a volume — no volume was declared. The data went to the container's writable layer, which is part of the container and is deleted when the container is removed. /var/lib/docker/volumes only holds named/anonymous volumes." },
    { label: "Docker automatically snapshots the writable layer before removal", misconception: "Docker does not snapshot or preserve the writable layer on removal. The layer is the container's own diff and is destroyed with it. Persistence is opt-in via a mount, not automatic." },
    { label: "It survives because the image is unchanged", misconception: "The image is read-only and never held your runtime data anyway. Runtime writes go to the writable layer on top of the image, and that layer dies with the container regardless of the image." },
  ]}
/>

<Quiz
  id="09-docker-deep-volumes-quiz-2"
  lessonSlug="02-volumes-and-persistence"
  lang="en"
  question="Your image installs node_modules at /app during build. You then bind-mount an empty host folder onto /app at runtime. What does the container see?"
  choices={[
    { label: "An empty /app — the bind mount shadows the image's contents at that path, hiding the installed node_modules for the life of the mount", correct: true },
    { label: "The image's node_modules merged with the empty host folder", misconception: "Bind mounts do not merge. The mount replaces the container's view at that path with the host's view, so the image's files are hidden, not combined. Only an empty NAMED volume copies the image's existing files up into the volume." },
    { label: "An error, because the path already has files", misconception: "There is no error — the mount silently succeeds and obscures the existing files. That silence is exactly why this bug is so confusing to diagnose." },
    { label: "The node_modules, because the image layer takes priority", misconception: "The mount sits on top of the image's view at that path and wins; the image layer's files are underneath and invisible while the bind mount is active." },
  ]}
/>

</Explanation>

<StackDiagram
  label="Container storage from the read-only image at the bottom up through the ephemeral writable layer, with three external mount types attached for persistence"
  hue="var(--d-systems)"
  layers={[
    { label: "tmpfs mount", note: "in RAM — never persisted (secrets/scratch)" },
    { label: "Bind mount", note: "host path mapped in — dev source" },
    { label: "Named volume", note: "/var/lib/docker/volumes — DB data, survives" },
    { label: "Writable layer", note: "ephemeral — dies with the container" },
    { label: "Image layers", note: "read-only — never hold runtime data" },
  ]}
  caption="The image and writable layer travel with the container; the writable layer is deleted on removal. Persistence comes from mounts attached from outside: a named volume for durable data, a bind mount for host files, tmpfs for in-memory scratch."
/>

<KeyTakeaway>The writable layer dies with the container, so persistence is opt-in: database data on a named volume, dev source on a bind mount, secrets on tmpfs. Volumes are durable but host-local — not networked storage.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="09-docker-deep-volumes-retrieval"
  lang="en"
  questions={[
    { q: "Why is data written inside a container ephemeral by default, and what are the three mount types you use to persist (or deliberately not persist) it?", a: "A container is its read-only image layers plus one thin writable layer on top, joined by a union filesystem. Every file a process creates or modifies at runtime — a database directory, an upload, a log — lands in that writable layer, which is bound to the container's lifecycle. Remove the container (docker rm, compose down, a redeploy that recreates it) and the writable layer is deleted with it, so the data is genuinely gone, not stashed on the host. It also inflates the container and writes through the slower union abstraction, so it is scratch space, not storage. To persist data you mount storage from outside the writable layer, and there are three types. A named volume is Docker-managed storage under /var/lib/docker/volumes referenced by name — the recommended choice for databases and stateful data because it is decoupled from host paths, portable, backup-able, writes to the host filesystem directly, and survives container removal. A bind mount maps a specific host path straight into the container — ideal for dev live-reload of source, but it couples to host layout and shadows any contents the image already put at that path. A tmpfs mount lives only in RAM and is wiped when the container stops — for secrets and in-memory scratch you never want on disk." },
    { q: "Why is `docker volume prune` dangerous, and why are local volumes not enough for a multi-node deployment?", a: "Volumes have a lifecycle independent of containers: a volume is created on demand, survives container removal by design, and is never cleaned up automatically — it sits on disk until you explicitly remove it with docker volume rm or docker volume prune. That independence is the durability you want, but it makes prune dangerous: docker volume prune deletes every volume not currently attached to a running container, and a volume whose container you removed last week counts as unused. A careless prune to reclaim disk can therefore erase a database you fully intended to keep. The complementary surprise is stale volumes silently filling the disk under /var/lib/docker/volumes. Separately, volumes are host-local: a named volume lives on one machine's filesystem, so in a multi-node cluster (Swarm, Kubernetes, multiple VMs) a volume on node A is invisible to a container scheduled on node B. Real multi-node persistence needs networked or object storage — a managed database, S3-compatible object store, or a CSI/network-volume driver — not a plain local volume. Treating a local volume as shared storage is the classic failure when a single-host setup graduates to a cluster." },
  ]}
/>

<Recap lang="en">
A running container is its read-only image layers plus one thin writable layer, and that writable layer is bound to the container's lifecycle — remove the container and everything written there is deleted with it, which is why a volume-less Postgres loses its data on the next redeploy. Persistence is opt-in, through one of three mounts attached from outside the writable layer. A named volume is Docker-managed storage under /var/lib/docker/volumes, referenced by name; it is decoupled from host paths, portable, backup-able, writes to the host filesystem directly, and survives container removal, which makes it the default for databases and any stateful data. A bind mount maps a specific host path straight in — perfect for dev live-reload, but it couples to host layout and silently shadows whatever the image already populated at that path, so never bind-mount over a directory like a build-time node_modules unless you mean to hide it. A tmpfs mount lives only in RAM and vanishes on stop, for secrets and scratch you never want on disk. Volumes have an independent lifecycle: created on demand, surviving container removal, and removed only explicitly — which is why `docker volume prune` can wipe a database you meant to keep, and why dead volumes quietly eat disk. Finally, volumes are host-local: in a multi-node cluster you need networked or object storage, not a plain local volume. Now when you open a compose file, check whether every stateful container has a named volume declared — that one line is the difference between a staging wipe and a Monday morning you can recover from.
</Recap>
