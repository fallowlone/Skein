---
slug: 01-what-v8-is
lang: en
track: browser
unit: 03-v8-internals
order: 1
title: "What V8 is and why performance varies 100×"
summary: "V8 turns JS source into machine code through four compilation tiers — the same function can run 100× faster or slower depending on whether V8 can keep it on the hot path."
estMin: 10
status: ready
lessonType: topic
level: junior
prereqs: []
deepensInto: ["02-jit-pipeline", "04-inline-caches"]
spiral: ["statefulness"]
concepts: ["V8", "JIT compilation", "compilation tiers", "Ignition", "TurboFan", "monomorphic", "hidden class"]
sources:
  - https://v8.dev/blog/maglev
  - https://v8.dev/blog/sparkplug
  - https://en.wikipedia.org/wiki/V8_(JavaScript_engine)
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import PersonaTag from "~/components/pedagogy/PersonaTag.astro";

<Hook>
React renders 1000 rows. With stable object shapes it is nanosecond lookups and 60fps. With mixed shapes it is 10–50× slower — same code, same function, different speed because of what V8 observed.
</Hook>

<Crux>V8 has four JIT tiers: code starts slow in the interpreter and climbs to fast machine code only when object shapes stay consistent.</Crux>

<Explanation>

## What V8 is

V8 is the JavaScript engine inside Chrome, Edge, and Node.js. It takes JS source code and turns it into machine code the CPU executes. The same JS function can run 100× faster or slower depending on whether V8 can optimise it. A few habits — consistent object shapes, monomorphic call sites — make the difference between a 60fps animation and a janky page.

## The kitchen metaphor

V8 is a kitchen with four cooks:

- **Trainee (Ignition)** reads every recipe slowly but logs what was ordered.
- **Line cook (Sparkplug)** cranks out fast generic versions.
- **Sous-chef (Maglev)** makes specialised versions for popular dishes.
- **Head chef (TurboFan)** hand-crafts a perfect dish for the most-ordered — but only if orders stay consistent. Change the order mid-bite and TurboFan tosses the dish.

<NumbersCard
  id="card-v8-overview"
  title="V8 tiers at a glance"
  data-lesson-visual
  stats={[
    { label: "Tiers", value: "4 (Ignition / Sparkplug / Maglev / TurboFan)" },
    { label: "Ignition vs TurboFan speed", value: "~100× slower" },
    { label: "Megamorphic slowdown", value: "10–50× vs monomorphic" },
    { label: "IC monomorphic access", value: "5–10 CPU cycles" },
  ]}
/>

## A concrete walk-through

<PersonaTag id="bea" lang="en" /> browser runs JS. Ignition interprets first, logging types. After thousands of calls V8 promotes to TurboFan with assumptions "arg always a number, object always has .x". If <PersonaTag id="sven" lang="en" /> server one day sends a string, assumptions break — deopt, fall back. The "fast" function is slow again until V8 re-learns.

React renders 1000 rows. Stable keys = monomorphic IC, nanosecond lookups. Mixed keys = polymorphic, 2–3× slower. 10+ shapes = megamorphic, 10–50× slower. Same code, vastly different speed.

<DragOrder
  id="jr-drag-1"
  pieceSlug="what-v8-is"
  lang="en"
  prompt="Order the journey of a hot function through V8's tiers:"
  items={[
    "JS source is parsed into an AST",
    "AST is converted to bytecode by the Ignition compiler",
    "Ignition interprets the bytecode and records type feedback per call site",
    "After thousands of calls, Sparkplug emits baseline machine code with no specialisation",
    "If still hot, Maglev compiles a mid-tier optimised version using observed types",
    "For the very hottest paths, TurboFan does aggressive speculative optimisation",
    "If an assumption breaks at runtime, V8 deoptimizes and falls back to a lower tier",
  ]}
  correctOrder={["0", "1", "2", "3", "4", "5", "6"]}
/>

<Quiz
  id="jr-quiz-1"
  pieceSlug="what-v8-is"
  lang="en"
  question="Why does V8 use multiple compilation tiers instead of just one?"
  choices={[
    { label: "To support more browsers", misconception: "V8 ships in Chrome and Node — the tiers are about performance balance, not browser portability." },
    { label: "Different tiers trade compile-time for run-time speed: Ignition starts immediately but runs slow; TurboFan compiles slowly but runs blazing fast", correct: true },
    { label: "Tiers are for different JS versions", misconception: "All tiers run the same ECMAScript; they differ in optimisation strategy." },
    { label: "Only one tier is real, the others are decoration", misconception: "All four tiers are real, working compilers shipping in current Chrome." },
  ]}
/>

<Quiz
  id="jr-quiz-2"
  pieceSlug="what-v8-is"
  lang="en"
  question="What makes one piece of JS run 100× faster than another that does 'the same thing'?"
  choices={[
    { label: "More CPU cores", misconception: "JS in V8 is single-threaded; cores do not change per-line performance." },
    { label: "Different parsers", misconception: "Parsing happens once and is fast either way." },
    { label: "Whether V8 can keep the function on its fast tier (consistent types, monomorphic call sites) or has to fall back to the generic interpreter (polluted ICs, deopt)", correct: true },
    { label: "Whether the file is .mjs or .js", misconception: "Module type does not affect V8's per-function performance." },
  ]}
/>

<MetaphorComplete
  id="jr-metaphor-1"
  pieceSlug="what-v8-is"
  lang="en"
  setup="Fill in the blank: a call site that has seen exactly one object shape is _______ and V8 generates the fastest possible machine code for it."
  accepted={["monomorphic", "mono"]}
  canonical="monomorphic (one shape — the fastest IC state)"
  explanation="Monomorphic is the gold-standard IC state. V8 emits machine code with the property offset hard-coded. Polymorphic (2-4 shapes) adds branches; megamorphic (5+ shapes) falls back to the generic lookup that is 10-50x slower than monomorphic."
/>

<Inset kind="why" lang="en">
Why does a dynamic language's engine need to know types? Machine code is typed — an ADD instruction for integers is different from one for floats. V8 observes what types a call site actually sees and generates specialised code for those types. When the type changes at runtime, V8 has to throw away the specialised code and start over. That is the deoptimization cost.
</Inset>

</Explanation>

<KeyTakeaway>V8 climbs from slow interpreter to fast machine code only when types are consistent. Monomorphic call sites run in 5-10 cycles; megamorphic ones are 10-50× slower.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="what-v8-is-retrieval"
  lang="en"
  questions={[
    { q: "Name V8's four compilation tiers in order from slowest to fastest.", a: "Ignition (bytecode interpreter) → Sparkplug (baseline JIT) → Maglev (mid-tier optimising) → TurboFan (heavy optimising). Ignition is ~100× slower than TurboFan per instruction, but has zero compile cost. TurboFan compiles in tens to hundreds of ms but emits the fastest code." },
    { q: "Why does adding a new property to an object mid-way through code slow down downstream functions?", a: "Adding a property changes the object's hidden class. Every function that touches that object via an inline cache had specialised on the OLD hidden class. The next call sees a different hidden class, the IC transitions monomorphic → polymorphic. The fix: construct objects with all properties up front in a constructor so the hidden class is stable from creation." },
    { q: "What is a monomorphic inline cache and why is it the fastest IC state?", a: "A per-call-site cache where V8 records the single hidden class it has seen and the exact property offset for that class. Next time the same line runs with the same hidden class, V8 reads the property at the precomputed offset in 5-10 cycles — no hashtable lookup, no type check, one MOV instruction. It is fastest because there is zero branching — V8 hard-codes the offset at compile time." },
  ]}
/>

<Recap lang="en">
V8 is the JS engine in Chrome and Node.js. It runs code through four tiers: Ignition interprets slowly while collecting type feedback, Sparkplug emits cheap baseline machine code, Maglev optimises with observed types, and TurboFan makes aggressively optimised code for the hottest functions. A function stays on the fast tier only while object shapes — hidden classes — remain stable. One extra property added at the wrong moment changes the hidden class, degrades the inline cache from monomorphic to polymorphic or megamorphic, and costs 10–50× in throughput on that call site. The same JS function can run at 5 cycles or 500 cycles per property access depending on how consistent the shapes are.
</Recap>
