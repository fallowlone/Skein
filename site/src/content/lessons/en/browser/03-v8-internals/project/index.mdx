---
concepts:
- ic-profiling
- hidden-class-stability
- deopt-diagnosis
- typed-array-layout
- v8-observability
estMin: 240
lang: en
level: senior
order: 10
prereqs: []
slug: project
sources:
- https://mathiasbynens.be/notes/shapes-ics
- https://github.com/thlorenz/v8-perf/blob/master/compiler.md
status: ready
summary: "Hands-on project — drive a hot JS path into a megamorphic IC and a deopt-loop, diagnose it with d8 and --trace-deopt, then fix the shape and numeric type and prove the win with before/after numbers."
title: 'V8 internals: hunt a deopt and a megamorphic IC'
track: browser
unit: 03-v8-internals
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Reading about megamorphic ICs and deopt-loops is not the same as pulling a hot path out of one. Build a small allocation-and-compute-heavy workload, deliberately drive it into V8 trouble, then use d8 and --trace-deopt to diagnose and fix it — with evidence at every step.
</Hook>

<Goal lang="en">
Turn the unit's mental model into a reproducible loop: instrument tier and IC state, reproduce a megamorphic site and a deopt-loop, fix the shape and numeric type upstream, and verify the recovery with before/after measurements.
</Goal>

<ProjectBrief
  id="03-v8-internals-project"
  lessonSlug="03-v8-internals"
  lang="en"
  objective="Take a deliberately V8-hostile hot path (your own or the starter below) and bring its hot site back to a monomorphic IC and a stable TurboFan tier with zero deopt-loop — proving each step with d8 natives-syntax output and --trace-deopt, not by guessing."
  requirements={[
    "Build a small Node script with one hot function called millions of times: a property-access site fed objects from multiple constructors / conditionally-added properties, plus a numeric accumulator that can overflow the Smi range.",
    "Wire V8 observability: run under node --allow-natives-syntax and use %GetOptimizationStatus(fn) to read the tier, %HasFastProperties(obj) to detect dictionary mode, and %DebugPrintFeedback(fn) (or d8) to dump the FeedbackVector and read IC slot states.",
    "Reproduce a megamorphic IC: feed the hot site 5+ distinct hidden classes and capture the FeedbackVector slot showing the MegamorphicSentinel, plus the before throughput.",
    "Reproduce a deopt-loop: run under --trace-opt --trace-deopt and capture the repeating optimize/deoptimize pair on one source line, with its reason (wrong map or not a Smi).",
    "Fix the shape upstream: unify every object reaching the hot site to one hidden class (declare all properties in a constructor, never delete, add absent fields as null). Re-dump the FeedbackVector and confirm the slot is monomorphic.",
    "Fix the numeric type: clamp the accumulator into Smi/int32 range (| 0) or move numeric arrays to a TypedArray, and confirm --trace-deopt is now silent for the function under the same input.",
    "Re-run the identical workload and record after numbers next to before; the hot site should be monomorphic, the function should hold an optimised tier, and throughput should improve markedly (often 5–50× on the hot loop).",
  ]}
  acceptance={[
    "A before/after table: IC slot state (mega → mono), optimisation tier (%GetOptimizationStatus), deopt count from --trace-deopt, and hot-loop throughput — all measured, not estimated.",
    "%DebugPrintFeedback clearly shows the target slot moving from MegamorphicSentinel to a single hidden class after the fix.",
    "--trace-deopt produces no deopt for the hot function under representative input after the numeric-type fix; the deopt-loop signature is gone.",
    "A one-paragraph write-up naming, for each problem, which lever you used (constructor shape, drop delete, null-fill, | 0, or TypedArray) and why it beat tuning any flag.",
  ]}
  stretch={[
    "Add a parallel-TypedArray variant of the hot loop (Float32Array per field) and compare its throughput and per-frame GC against the fixed-object version — quantify the IC-bypass and zero-allocation win.",
    "Add a perf-budget test that runs the hot path under representative inputs and asserts --trace-deopt is empty and %GetOptimizationStatus reports an optimised tier; fail CI on any regression.",
    "Reproduce one of the unit's real-world failures in miniature (Discord-style polymorphic render IC, Slack-style periodic NaN deopt, or Maps-style optional-field megamorphism) and write the one-line root cause and the upstream fix.",
    "Take a heap snapshot before and after a closure-leak variant (a per-frame closure captured by a long-lived callback) and show old-gen growth, then fix by hoisting the closure and re-snapshot to confirm.",
  ]}
/>

<Recap lang="en">
This is the loop you run in every real V8 incident: instrument the tier and IC state with d8 natives syntax and --trace-deopt, reproduce the megamorphic site and the deopt-loop so you can see them, fix the shape and the numeric type upstream in the data model, and verify with before/after numbers under identical input. Doing it once on a toy script makes the production diagnosis — where the regression hides in a small refactor — muscle memory.
</Recap>
