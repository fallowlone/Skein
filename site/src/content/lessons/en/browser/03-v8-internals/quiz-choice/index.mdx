---
concepts:
- hidden-class
- inline-cache
- jit-tiers
- deopt-loop
- megamorphic
- orinoco-gc
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://v8.dev/blog/maglev
- https://mathiasbynens.be/notes/shapes-ics
- https://v8.dev/blog/concurrent-marking
status: ready
summary: "Multiple-choice synthesis across the V8 unit — hidden-class transitions, IC states, the four JIT tiers, deopt-loops, megamorphic call sites, and Orinoco GC interplay."
title: 'V8 internals: multiple-choice review'
track: browser
unit: 03-v8-internals
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one is a decision you make reading a real performance trace — not a definition to recite, but a mechanism to reason about under production load.
</Hook>

<Goal lang="en">
Confirm you can connect hidden-class shape, IC state, tier promotion, deopt behaviour, and GC barriers — the synthesis the individual lessons built toward.
</Goal>

<Quiz
  id="03-v8-internals-choice-1"
  lessonSlug="03-v8-internals"
  lang="en"
  question="A hot render function ran at 5µs in tests but 250µs in production. A heap snapshot shows objects reaching one property-access site come from 7 distinct constructors. What is the V8-level cause and the real fix?"
  choices={[
    { label: "The IC at that site has gone megamorphic — past 4 hidden classes V8 abandons per-class tracking and falls back to a generic ~50–100 cycle lookup. Fix upstream: unify the shape so the site sees one hidden class, or split the loop per-shape.", correct: true },
    { label: "TurboFan was disabled in the production build, so the function never optimised.", misconception: "TurboFan cannot be selectively disabled per environment. The function did optimise; its IC degraded to megamorphic because production traffic carries shape diversity the tests never exercised." },
    { label: "The production machine is slower, so the same monomorphic code just runs at a lower clock.", misconception: "IC state depends on observed hidden classes, not clock speed. 7 constructors at one site is a megamorphic signature — a 10–50× algorithmic penalty, not a hardware delta." },
    { label: "GC pressure increased, adding pause time to every call.", misconception: "GC affects pause frequency, not the per-call throughput of one function. A 50× per-call slowdown localised to one site is IC degradation, not GC." },
  ]}
/>

<Quiz
  id="03-v8-internals-choice-2"
  lessonSlug="03-v8-internals"
  lang="en"
  question="Two objects are built as {x:1, y:2} and {y:2, x:1}. A function reads .x on both in a hot loop. What IC state does that access site reach, and why?"
  choices={[
    { label: "Polymorphic — the hidden class encodes property-addition ORDER, so the two objects sit at different leaf nodes of the transition tree and present as two distinct shapes at the site.", correct: true },
    { label: "Monomorphic — both objects have the same property set, so V8 treats them as one shape.", misconception: "V8 keys the IC off the hidden class, which records the order properties were added — not just the final set. Different order means different leaf node, hence two shapes." },
    { label: "Megamorphic — any two different object literals immediately blow past the cache.", misconception: "Megamorphic requires 5+ distinct hidden classes at the site. Two shapes is polymorphic — a short branch chain, still fast." },
    { label: "Uninitialized — V8 cannot track a site that sees more than one object.", misconception: "V8 tracks the hidden class at every site across all invocations; it transitions uninitialized → monomorphic → polymorphic → megamorphic as shapes accumulate." },
  ]}
/>

<Quiz
  id="03-v8-internals-choice-3"
  lessonSlug="03-v8-internals"
  lang="en"
  question="Why does V8 ship four JIT tiers (Ignition, Sparkplug, Maglev, TurboFan) instead of one good optimiser?"
  choices={[
    { label: "JavaScript is dynamically typed, so compile-time type knowledge is incomplete; V8 must observe runtime types before it can specialise — and the warm-up must itself be cheap, so Ignition and Sparkplug front-load fast startup while Maglev and TurboFan earn their compile cost only on hot, stable code.", correct: true },
    { label: "Each tier targets a different browser or ECMAScript version.", misconception: "All tiers ship together in one engine and run the same ECMAScript. They differ in the compile-cost-vs-runtime-speed tradeoff, not in language or browser support." },
    { label: "More tiers mean more parallelism, so the JS runs on four cores at once.", misconception: "JS execution stays single-threaded; tiers are layered optimisation levels, not parallel workers. Concurrent compilation builds the next tier off-thread, but execution is on one tier at a time." },
    { label: "TurboFan is too buggy to run alone, so the lower tiers are fallbacks.", misconception: "Lower tiers exist for startup latency and to gather type feedback, not because TurboFan is unreliable. TurboFan needs the FeedbackVector that Ignition fills before it can specialise well." },
  ]}
/>

<Quiz
  id="03-v8-internals-choice-4"
  lessonSlug="03-v8-internals"
  lang="en"
  question="A perf-critical function shows repeated 'optimized' then 'deoptimized (not a Smi)' pairs in --trace-deopt within the same second. What is happening, and what is the highest-leverage fix?"
  choices={[
    { label: "A deopt-loop: TurboFan keeps speculating the value is a Smi, a rare large or NaN value fails the guard every call, V8 deopts and re-promotes (~100 ms compile each time). Fix: route pathological values to a slow path before the hot loop so the fast path stays Smi-stable.", correct: true },
    { label: "Normal behaviour — every optimised function deopts a few times as it warms up.", misconception: "A one-time deopt during warm-up is normal; a repeating deopt/re-opt pair on the same line every call is a death-spiral that costs more than never optimising." },
    { label: "The function is broken and will never optimise again; rewrite it in WebAssembly.", misconception: "Deopt drops a tier, it does not permanently disable optimisation — the function re-tiers once feedback stabilises. The fix is type-stabilising the input, not a rewrite." },
    { label: "Raise the TurboFan promotion threshold so it stops recompiling.", misconception: "Deferring promotion only delays the loop; the guard still fails whenever the value appears. The durable fix removes the pathological value from the hot path, not the optimiser." },
  ]}
/>

<Quiz
  id="03-v8-internals-choice-5"
  lessonSlug="03-v8-internals"
  lang="en"
  question="Why does Orinoco's concurrent marking need a write barrier, and what breaks without one?"
  choices={[
    { label: "While the background marker traces the heap, the JS main thread can overwrite a reference to a still-white (unvisited) object. The snapshot-at-the-beginning barrier shades the overwritten referent grey so the marker still visits it; without it a live object is never marked and gets falsely collected — silent corruption.", correct: true },
    { label: "The barrier exists to stop two GC worker threads from visiting the same object twice.", misconception: "Duplicate visits are handled by work-stealing and the grey/black colouring. The write barrier is about the mutator (JS) rewiring references mid-mark, not about thread coordination." },
    { label: "It speeds up marking by parallelising the heap walk.", misconception: "The barrier adds ~3–5 cycles per reference write — it is a correctness mechanism, not a speed-up. Parallelism comes from worker threads, not from the barrier." },
    { label: "It tracks old-gen-to-young-gen pointers for the scavenger.", misconception: "That is the remembered set / card table, a separate mechanism. The write barrier here preserves the tri-color invariant during concurrent old-gen marking." },
  ]}
/>

<Quiz
  id="03-v8-internals-choice-6"
  lessonSlug="03-v8-internals"
  lang="en"
  question="A numeric-heavy animation loop processing 10M entities per frame keeps drifting off 60fps as it warms up, with deopts on arithmetic and rising minor-GC frequency. Which data layout fixes both the IC and GC problems at once?"
  choices={[
    { label: "Parallel TypedArrays (Float32Array of x, y, vx, vy) pre-allocated up front. TypedArray access bypasses the hidden-class IC layer entirely and the buffers allocate once, so there is no per-frame garbage to scavenge.", correct: true },
    { label: "An array of plain objects whose shape varies per entity to keep the code flexible.", misconception: "Varying shapes drive the per-property IC megamorphic (10–50× slower) and allocate a fresh object per entity, raising young-gen GC frequency. This worsens both problems the question names." },
    { label: "A Map() keyed by entity id, mutated in place each frame.", misconception: "Map carries hashing and generic-iteration overhead and does not sidestep the IC for value access. For fixed numeric fields in a hot loop it is slower than TypedArrays and still allocates entries." },
    { label: "Keep the objects but raise the young-generation heap size so minor GC runs less often.", misconception: "A bigger young gen defers scavenges but does nothing for the megamorphic IC or the Smi-overflow deopts. It treats a symptom; TypedArrays remove both root causes." },
  ]}
/>

<Recap lang="en">
The unit's through-line is one chain: property-addition order sets the hidden class, the hidden class sets the IC state, the IC state (with type stability) decides whether a function climbs to TurboFan or deopt-loops, and Orinoco's barriers keep the heap correct while all of this churns. Every production regression in the unit — megamorphic render paths, Smi-overflow deopt-loops, closure leaks into old gen — resolves back to the same lever: stabilise the shape and the numeric type upstream, in the data model, before reaching for any flag.
</Recap>
