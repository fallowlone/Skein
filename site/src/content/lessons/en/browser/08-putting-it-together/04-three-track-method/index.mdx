---
concepts:
- three-track-method
- DevTools-performance
- RUM-attribution
- web-vitals-library
- CI-gate
- LoAF
- capstone-discipline
- measure-before-touch
deepensInto: []
estMin: 20
lang: en
lessonType: topic
level: senior
order: 4
prereqs:
- browser/08-putting-it-together/03-five-canonical-breaks
- browser/07-core-web-vitals/06-tradeoffs-and-observability
slug: 04-three-track-method
sources:
- https://developer.chrome.com/docs/devtools/performance
- https://web.dev/articles/vitals
- https://web.dev/articles/optimize-inp
- https://web.dev/articles/lcp
- https://developer.chrome.com/docs/lighthouse/overview
spiral:
- latency
- statefulness
- encapsulation
status: ready
summary: The senior diagnostic discipline — find the symptom, locate the bottleneck
  track (network/main-thread/GPU), fix the dominant cause, re-measure. Plus the layered
  telemetry stack that turns whole-chain understanding into a production monitoring
  system.
title: 'The three-track method: reading traces and building a monitored system'
track: browser
unit: 08-putting-it-together
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A junior engineer optimises the "Add to cart" handler — it already takes 12 ms, well under budget. INP stays at 480 ms. A second engineer finds a 900 ms hydration long task that starts before every early tap. Fixing the handler optimised something that was not the constraint. The constraint was the task the tap was queuing behind. Reading a trace is not knowing that things are slow — it is knowing which track, at which moment, was the actual bottleneck.
</Hook>

<Crux>Three moves: measure before touching, find the constraint track at the constraint moment, fix the dominant cause then re-measure.</Crux>

<Explanation>

**The three-track method.**

A DevTools performance trace of the product page shows three overlapping tracks on one timeline: the network track, the main-thread track, and the GPU/compositor track. The senior method for diagnosing any slow page:

1. **Find the user-facing symptom.** A late LCP marker. A long interaction in the Interactions track. A red layout-shift marker.
2. **Drop to the moment that symptom occurred and ask which track was the constraint.**
   - Late LCP + hero still on the network track at LCP time → network or discovery problem (break 1).
   - Late LCP + hero finished early + main thread solid → render-blocking JS, element-render-delay.
   - Slow interaction + long main-thread task overlapping it → hydration or heavy handler (breaks 3 and 5).
3. **Name the constraint as: "this track, at this moment."** That is a fixable statement. "The page is slow" is not.

This converts "the page is slow" into "the main thread was blocked by the hydration long task from 1.6 s to 2.5 s, and the user's tap at 2.0 s queued behind it." That is something you can fix.

<NumbersCard
  id="card-three-tracks"
  title="Three-track trace structure"
  stats={[
    { label: "Network track", value: "HTML → CSS → JS → images, in parallel" },
    { label: "Main-thread track", value: "Parse → compile → paint → hydrate → handle" },
    { label: "GPU track", value: "Composite → present frames" },
    { label: "Bottleneck pattern: pre-paint", value: "Network-bound (hero download, render-blocking JS)" },
    { label: "Bottleneck pattern: post-paint", value: "Main-thread-bound (hydration long task, heavy handler)" },
  ]}
  data-lesson-visual
/>

**The hydration deep-dive — where five pieces meet.**

Hydration is the hinge of the whole trace, so it is worth one more pass. It is the React reconciler (lesson 05) running in a special mode: instead of `createRoot` building fresh DOM, `hydrateRoot` walks the component tree and *adopts* the existing server DOM. The render phase runs — your components execute, the fiber tree is built — but the commit phase, instead of inserting nodes, attaches event listeners and runs effects.

Hydration is gated by the JavaScript bundle: nothing hydrates until V8 (lesson 03) has parsed, compiled, and begun executing that bundle. It is one or more tasks on the event loop (lesson 01), and a large eager hydration is one long task that blocks input — which Core Web Vitals (lesson 07) records as poor INP for any interaction in that window. And the entire cost exists only because the page was server-rendered (lesson 06) in the first place. Hydration is where five of the seven browser lessons meet.

**The first-paint deep-dive — another dense intersection.**

The other dense intersection is the first-paint sequence. The render pipeline (lesson 02) needs the DOM and CSSOM; the DOM comes from the HTML parser, the CSSOM from CSS parsing — both main-thread tasks (lesson 01). The JavaScript that may modify the DOM before first paint must first be parsed and compiled by V8 (lesson 03), and if that JS is render-blocking, the paint waits for it. The composite stage hands layers to the GPU. LCP (lesson 07) fires when the largest element of that paint lands.

So "first paint" is not one mechanism — it is the render pipeline, the parser, V8, and the compositor, coordinated, with a Core Web Vital watching the result. No layer is an island; every real page is the whole stack at once.

**The capstone discipline — three moves.**

Step back from the page and look at the method itself. The senior skill this whole chapter builds toward is not memorising eight layers — it is a discipline of diagnosis. It has three moves:

1. **Measure before you touch.** A real trace and field RUM, never a guess, because the symptom routinely names the wrong layer. The junior who optimised the 12 ms handler measured nothing — they guessed.

2. **Find the constraint, not a constraint.** On the overlapped timeline there is always one track, at one moment, that is the actual bottleneck — optimising anything else moves a number that was not the problem. The 900 ms hydration long task was the constraint; the handler was not.

3. **Fix the dominant cause, then re-measure.** Because the layers interact, a fix in one layer changes the budget in others, so the trace after the fix is a new diagnosis, not a confirmation. Shrinking the bundle changes the V8 cost, which changes the hydration long task, which changes the INP profile — the new trace may reveal a secondary bottleneck.

A junior engineer optimises what they know how to optimise; a senior engineer optimises what the trace says is the constraint. The eight layers are the map; the discipline is knowing the map does not tell you where the problem is — the measurement does.

<FlowDiagram
  label="The three-move capstone discipline as a loop: measure before touching, find the one constraint track at the constraint moment, fix the dominant cause, then re-measure — which feeds a fresh diagnosis."
  perRow={3}
  hue="var(--d-frontend)"
  nodes={[
    { id: "measure", label: "1. Measure", sub: "trace + RUM" },
    { id: "constraint", label: "2. Find the constraint", sub: "track + moment" },
    { id: "fix", label: "3. Fix + re-measure", sub: "dominant cause" },
  ]}
  edges={[
    { from: "measure", to: "constraint" },
    { from: "constraint", to: "fix" },
    { from: "fix", to: "constraint", label: "new diagnosis" },
  ]}
  caption="Never guess the layer — measure first. Find the one track at the one moment that is the actual constraint, not just something slow. A fix changes the whole system, so re-measuring is a new diagnosis, not a confirmation: the loop repeats until the next constraint emerges."
/>

**Production observability — turning understanding into a system.**

The end-to-end mental model becomes a production practice through layered telemetry, each layer watching the boundary it owns:

- **RUM with `web-vitals`** captures LCP, INP, and CLS with attribution — the LCP element, the INP script, the CLS source — so a regression names its own layer.
- **Long Animation Frames** (`PerformanceObserver` `long-animation-frame` entries) attribute slow frames to specific scripts, catching the hydration long task and heavy handlers.
- **Server-side tracing** watches TTFB and the data-fetch path.
- **Service-worker telemetry** watches cache hit rates and `fetch`-handler duration.
- **Synthetic CI gate** — a throttled Lighthouse or Playwright trace on every PR — catches the obvious regressions before merge.

Because a slow page is a chain problem, the monitoring must cover every link. And each metric must carry enough attribution to point back to the layer that broke. That is how whole-chain understanding stops being a debugging skill and becomes a system that catches the break for you.

<Quiz
  id="sr-quiz-1"
  pieceSlug="04-three-track-method"
  lang="en"
  question="A DevTools trace shows a 900 ms main-thread long task starting at 1.6 s. A user's tap at 2.0 s has an INP of 700 ms. What is the correct diagnosis?"
  choices={[
    { label: "The tap queued behind the hydration long task — the input delay accounts for most of the 700 ms INP; the constraint is the main-thread track, not the handler", correct: true },
    { label: "The 'Add to cart' handler itself is 700 ms — optimise the handler", misconception: "Handler processing time and input delay are different components of INP. A 700 ms INP with a 900 ms main-thread long task and a tap at 2.0 s means the tap spent ~500 ms waiting in the queue — that is input delay, not handler time." },
    { label: "The GPU track was the bottleneck", misconception: "A long main-thread task is a main-thread-track problem. The GPU/compositor track runs after the main thread commits — it is not the source of input delay." },
    { label: "The network was still downloading at the tap", misconception: "A 900 ms long task on the main-thread track is not a network event. Network fetches do not block the main thread as a long task — they show on the network track while the main thread is idle or doing other work." },
  ]}
/>

<Quiz
  id="sr-quiz-2"
  pieceSlug="04-three-track-method"
  lang="en"
  question="Why is 'fix the dominant cause, then re-measure' a better approach than 'fix everything at once and check'?"
  choices={[
    { label: "Layers interact — a fix in one layer changes the budget in others, so a post-fix trace is a new diagnosis, not a confirmation. Batch fixes make it impossible to attribute improvements.", correct: true },
    { label: "Re-measuring is only needed if the first fix did not work", misconception: "Re-measuring is needed even when the fix worked — because the fix changes the system, and a new bottleneck may have emerged that was previously hidden behind the dominant one." },
    { label: "Fixing everything at once is faster", misconception: "Fixing everything at once is only faster if you are correct about all the fixes. If any fix is wrong or overcorrects, you cannot isolate which change caused the regression." },
    { label: "'Fix everything' is the senior approach", misconception: "The senior approach is sequential diagnosis — measure, fix the dominant cause, re-measure. Batch changes without measurement is the junior approach." },
  ]}
/>

<Quiz
  id="sr-quiz-3"
  pieceSlug="04-three-track-method"
  lang="en"
  question="Why does 'hydration is where five pieces meet' matter for production diagnosis?"
  choices={[
    { label: "Hydration cost depends on V8 parse speed, event loop task structure, render strategy, and React fiber — so a hydration INP regression can have a cause in any of those layers", correct: true },
    { label: "Hydration is always the bottleneck", misconception: "Hydration is often the bottleneck in server-rendered apps with large bundles, but not always. The trace must show it before you fix it." },
    { label: "It means you need to know all five pieces to work on hydration", misconception: "The point is diagnostic, not prerequisite. You need to know that hydration touches all five pieces so you look in the right place when INP is poor after a paint-fast page." },
    { label: "It means React is the primary source of performance problems", misconception: "React is one piece. The hydration cost includes V8 parse+compile (layer 4), the event loop task structure (layer 1), the render strategy choice (layer 6), and the reconciler (layer 5) — not just React." },
  ]}
/>

<Quiz
  id="sr-quiz-4"
  pieceSlug="04-three-track-method"
  lang="en"
  question="Why does a production observability stack need both RUM and a synthetic CI gate — why is neither alone sufficient?"
  choices={[
    { label: "RUM catches device-class and interaction regressions lab tests cannot simulate; the CI gate catches regressions before they reach real users. Each fills the other's blind spot.", correct: true },
    { label: "RUM is more accurate, so the CI gate is optional", misconception: "RUM is the real verdict, but it requires regressions to reach production first. The CI gate exists to catch regressions before they land — RUM alone means every regression ships to real users." },
    { label: "The CI gate is more reliable, so RUM is optional", misconception: "Lab tests cannot simulate real device class distributions, real user interactions, or real slow paths. RUM catches regressions that a fast simulated device would miss." },
    { label: "Both are doing the same thing on different schedules", misconception: "They measure fundamentally different things: RUM captures real user sessions with real device constraints; CI captures a synthetic session on a controlled device. They are complementary, not redundant." },
  ]}
/>

<Inset kind="why" lang="en">
The capstone discipline mirrors a classical systems-thinking insight: the constraint governs the throughput of the whole system, and optimising anything that is not the constraint does not move the system's output. This is Goldratt's Theory of Constraints applied to page performance: find the one track, at the one moment, that is blocking the result — then focus exclusively on that until it is no longer the constraint, which exposes the next constraint. The three-move discipline (measure, find the constraint, fix and re-measure) is just this principle made concrete for browser performance work.
</Inset>

</Explanation>

<KeyTakeaway>Three moves: measure before touching (never guess the layer), find the constraint track at the constraint moment (not just a slow thing), fix the dominant cause then re-measure (layers interact — a fix creates a new system). RUM + CI gate turns this discipline into a system that catches breaks automatically.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-three-track-method-retrieval"
  lang="en"
  questions={[
    { q: "Explain the three-track trace-reading method, and why 'the symptom and the cause live in different layers' is the central lesson of this unit.", a: "A DevTools trace shows network, main-thread, and GPU tracks on one timeline. The method: (1) find the user-facing symptom — late LCP marker, long interaction, layout-shift marker; (2) drop to the moment the symptom occurred and ask which track was the constraint; (3) name it as 'this track, at this moment' — that is fixable. The central lesson: symptoms routinely name the wrong layer. 'Slow image' was a discovery bug in the parse layer, not a network size bug. 'React is slow' was a megamorphic V8 shape bug, not a reconciler bug. Someone who knows only one layer fixes the layer the symptom names. Seeing all three tracks on one timeline is what lets you find a cause that lives in a different layer than the symptom." },
    { q: "What makes hydration 'the hinge of the whole trace' — which five lessons does it touch, and why?", a: "Hydration is where five lessons' concerns intersect in one task. (1) Event loop (lesson 01): hydration is a task on the main thread — possibly one long task that blocks input. (2) V8 (lesson 03): hydration cannot begin until V8 has parsed and compiled the JS bundle. (3) React fiber (lesson 05): hydrateRoot runs the reconciler in adopt mode, executing all components and attaching handlers. (4) Render strategies (lesson 06): hydration only exists because the page was server-rendered — SSG with no interactivity needs no hydration. (5) Core Web Vitals (lesson 07): any interaction during the hydration long task is recorded as poor INP. This convergence means a hydration INP regression may have its root cause in V8 (too much to compile), the render strategy (too much to hydrate), the reconciler (expensive component trees), or the event loop (long task not yielded). The trace is what tells you which." },
    { q: "Describe the production observability stack that turns the three-track discipline into a system that catches breaks automatically.", a: "Four layers. (1) RUM with web-vitals: the PerformanceObserver-based library captures LCP, INP, and CLS with attribution — LCP element plus phase split, INP interaction target plus LoAF script, CLS shifting nodes — tagged by route, device class, country, and release. This is the real verdict; it catches device-class and interaction regressions the lab never will. (2) Long Animation Frames: LoAF entries attribute slow frames to specific scripts with source URLs and durations, turning 'INP is 340 ms' into 'search.js:88 is the dominant script.' (3) Server-side tracing: TTFB and data-fetch path, so a slow deploy or a new slow query shows in the same dashboard. (4) Synthetic CI gate: throttled Lighthouse or Playwright on every PR, budgeting LCP, TBT, CLS — catches obvious regressions before they reach production. Neither RUM nor CI alone is sufficient: RUM without CI ships every regression to real users first; CI without RUM misses device-class and interaction regressions." },
  ]}
/>

<Recap lang="en">
The three-track method turns "the page is slow" into a fixable statement: find the user-facing symptom (late LCP, bad INP, CLS spike), drop to the moment it occurred, and ask which track — network, main thread, or GPU — was the constraint at that instant. Hydration is the hinge of the trace because it touches the event loop, V8, the React reconciler, the render strategy, and Core Web Vitals simultaneously; a hydration INP regression may have its root cause in any of those layers. The capstone discipline has three moves: measure before touching (never guess), find the constraint track at the constraint moment (not just something slow), fix the dominant cause then re-measure (the fix changes the system and may expose a new bottleneck). Layered production telemetry — RUM with web-vitals and LoAF attribution, server-side tracing, service-worker monitoring, and a synthetic CI gate — turns this diagnostic discipline into a system that catches breaks before they reach users. The eight layers are the map; the measurement is what tells you where the problem actually lives.
</Recap>
