---
concepts:
- whole-chain-diagnosis
- three-track-method
- core-web-vitals-remediation
- rum-attribution
- ci-performance-gate
- hydration-cost-reduction
estMin: 240
lang: en
level: senior
order: 10
prereqs: []
slug: project
sources:
- https://web.dev/articles/optimize-lcp
- https://web.dev/articles/optimize-inp
- https://developer.chrome.com/docs/devtools/performance
status: ready
summary: "Capstone — diagnose and fix a slow, janky server-rendered page end-to-end across the rendering path, then prove every fix with before/after field and lab numbers."
title: 'Putting it together: diagnose and fix a janky page end-to-end'
track: browser
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Reading about the relay is not the same as pulling a real page out of the uncanny valley. Take a slow, janky server-rendered page, walk the whole chain with a trace and RUM, fix the dominant cause at each layer in leverage order, and prove every move with before/after numbers — the loop you will run in every real performance incident.
</Hook>

<Goal lang="en">
Turn the capstone discipline into a reproducible engineering loop: measure before touching, find the constraint track at the constraint moment, fix the dominant cause across the rendering path, re-measure, and wire the understanding into a monitored system that catches the next regression.
</Goal>

<ProjectBrief
  id="08-putting-it-together-project"
  lessonSlug="08-putting-it-together"
  lang="en"
  objective="Take a deliberately slow, janky server-rendered page (your own or the starter pattern below) with poor LCP, INP, and CLS, and bring all three Core Web Vitals to 'good' at field p75 — proving each step with a DevTools trace and RUM, without abandoning SSR."
  requirements={[
    "Build or take a server-rendered React (or framework-equivalent) page with at least: a hero image referenced as a CSS background-image or with loading='lazy'; an eagerly-hydrated bundle large enough to produce a 300 ms+ hydration long task; a component that renders a locale- or browser-only value (price, timestamp) identically on server and client; and an 'Add to cart' handler doing synchronous heavy work before paint.",
    "Capture a baseline DevTools performance trace (throttled to mid-range mobile, 4G) and read it as three tracks — network, main thread, GPU — annotating which track is the constraint at the LCP moment and at the first-tap moment.",
    "Wire field-style RUM with the web-vitals library: log LCP (element + phase), INP (interaction target + LoAF script), and CLS (shifting node) with attribution. Record the baseline p75 numbers.",
    "Diagnose each of the page's breaks to a specific layer using the chain-walk: late LCP still-downloading vs render-blocking; INP input-delay vs processing; CLS source. Write the symptom-to-layer mapping down.",
    "Fix in leverage order, naming the layer and metric for each: (1) TTFB / service-worker shell for repeat visits; (2) LCP discovery — plain eager img, fetchpriority high, AVIF/WebP, explicit dimensions; (3) hydration cost — code-split, convert display-only subtrees to Server Components, make below-the-fold an island that hydrates on scroll; (4) CLS — reserved dimensions plus deferring client-only values to a post-hydration useEffect; (5) the handler — minimal sync work then scheduler.yield(), heavy compute to a worker.",
    "Re-run the identical trace and RUM after each dominant fix, not all at once, so each improvement is attributable to one change.",
    "Add a CI performance gate: a throttled Lighthouse or Playwright trace on every PR that budgets LCP, total blocking time / INP proxy, and CLS, and fails the build on regression.",
  ]}
  acceptance={[
    "A before/after table at field p75: LCP, INP, CLS, plus TTFB and the hydration long-task duration — measured under the same throttling, not estimated.",
    "All three Core Web Vitals reach 'good' at p75: LCP ≤ 2.5 s, INP ≤ 200 ms, CLS ≤ 0.1.",
    "An annotated trace pair (before/after) showing the constraint track at the LCP and first-tap moments, and the hydration long task visibly shorter after the fix.",
    "A one-paragraph write-up per fix: the layer it targeted, the metric it moved, and why it ranked where it did in the leverage order — TTFB and hydration cost before handler micro-optimisation.",
    "The CI gate is green on the fixed branch and demonstrably fails when a regression (e.g. re-adding the lazy hero or an eager heavy island) is introduced.",
  ]}
  stretch={[
    "Add an on-call runbook: chain-walk triage from a trace, the symptom-to-layer table, the five canonical breaks with their fixes, and a verification checklist.",
    "Stream the shell with React Server Components and Suspense so first paint precedes full data, and measure the LCP and INP impact versus the all-at-once hydration baseline.",
    "Harden the service worker: content-hashed assets, version-tagged caches purged in activate, network-first navigation, and a kill switch — then simulate a bad deploy and show repeat visitors are not trapped.",
    "Add Long Animation Frame observation in RUM and confirm the dominant INP script named in the field matches the long task you fixed in the lab — closing the loop between field and lab.",
  ]}
/>

<Recap lang="en">
This is the loop you will run in every real frontend performance incident: capture a trace and field RUM first, read it as three tracks to find the constraint at the moment that matters, fix the dominant cause across the rendering path in leverage order — TTFB, then LCP discovery, then hydration cost, then CLS determinism, then the handler — re-measure after each move because the layers interact, and wire RUM plus a CI gate so the next regression is caught before users feel it. Doing it once on a deliberately broken page is what makes the production version muscle memory.
</Recap>
