---
concepts:
- relay-race-chain
- three-track-method
- hydration-hinge
- canonical-breaks
- ttfb-floor
- observability-stack
estMin: 14
lang: en
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://web.dev/articles/howbrowserswork
- https://web.dev/articles/optimize-inp
- https://developer.chrome.com/docs/devtools/performance
status: ready
summary: "Free-recall prompts spanning the whole browser chapter — the eight layers, the three-track method, why hydration is the hinge, the five breaks, and the observability stack."
title: 'Putting it together: free-recall review'
track: browser
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, reconstruct a full answer from memory — naming the layers and the metrics — before you reveal the model answer. The effort of recall is what welds the seven browser lessons into one mental model.
</Hook>

<Goal lang="en">
Rebuild the capstone's spine without looking back: the relay chain, the three-track diagnosis method, why hydration is the hinge that touches five lessons, the five canonical breaks, and the telemetry stack that catches them.
</Goal>

<RetrievalDrawer
  client:visible
  id="08-putting-it-together-short-retrieval"
  lang="en"
  questions={[
    { q: "Trace the eight layers of a server-rendered React product page from the user's tap to a working button, and name the metric that lands at each key moment.", a: "Layer 1, network: DNS, TLS, HTTP; the service worker gets first look and the first byte arrives as TTFB — the floor under everything. Layer 2, HTML parse: the DOM is built and the preload scanner discovers the hero image early. Layer 3, CSS: stylesheets parse into the CSSOM, which is render-blocking; DOM plus CSSOM is the render tree. Layer 4, V8: the JS bundle is parsed and compiled to bytecode (~150 ms before any line runs). Layer 5, first paint: style, layout, paint, composite; the hero paints and LCP fires — the page looks done. Layer 6, hydration: hydrateRoot runs the reconciler in adopt mode, attaching handlers — one long main-thread task. Layer 7, interaction: a tap is handled (INP measured tap-to-paint); if it lands inside hydration it queues as input delay. Layer 8, second navigation: the worker serves the app shell from cache (~30 ms TTFB)." },
    { q: "State the three-track method and why 'find the constraint track at the constraint moment' beats 'the page is slow'.", a: "A DevTools trace shows three overlapping tracks: network (HTML, CSS, JS, images), main thread (parse, compile, paint, hydrate, handle), and GPU/compositor. The method: (1) find the user-facing symptom — a late LCP marker, a long interaction, a layout-shift marker; (2) drop to the moment it occurred and ask which track was the constraint then; (3) name it as 'this track, at this moment.' That is a fixable statement — 'the main thread was blocked by the hydration long task from 1.6 to 2.5 s and the tap at 2.0 s queued behind it' — whereas 'the page is slow' is not. The pattern: pre-paint bottlenecks are network-bound (hero download, render-blocking JS); post-paint bottlenecks are main-thread-bound (hydration, heavy handler)." },
    { q: "Why is hydration called the hinge of the whole trace, and which lessons converge there?", a: "Hydration is where five of the seven lessons meet in one task. Event loop (01): it is a task on the main thread, often one long task that blocks input. V8 (03): it cannot begin until the bundle is parsed and compiled. React fiber (05): hydrateRoot runs the reconciler in adopt mode, executing every component and attaching handlers. Render strategy (06): the cost exists only because the page was server-rendered — SSG with no interactivity needs no hydration. Core Web Vitals (07): any interaction during the long task is recorded as poor INP. So a hydration INP regression can have its root cause in V8 (too much to compile), the render strategy (too much to hydrate), the reconciler (expensive trees), or the event loop (a task not yielded) — the trace tells you which." },
    { q: "Name the five canonical breaks, the layer and metric each one hits, and the fix.", a: "Break 1, late hero (layers 2+5, LCP): the hero is a CSS background-image or lazy-loaded so the preload scanner misses it — fix with a plain eager img, fetchpriority high, AVIF/WebP. Break 2, hydration mismatch (layer 6, CLS plus correctness): a component renders a value differing server vs client (locale price, timestamp), React re-renders and shifts — fix by deferring client-only values to a post-hydration useEffect. Break 3, megamorphic render path (layers 4+7, INP): a hot function has seen too many object shapes so V8 cannot use an inline cache — fix by stabilising object shapes. Break 4, stale service worker cache (layers 1+8, correctness): the worker serves an old bundle against new HTML or traps users on a broken shell — fix with content-hashed names, version-tagged caches purged in activate, network-first navigation, a kill switch. Break 5, synchronous handler work (layer 7, INP): the handler does heavy work before yielding — fix by doing the minimum then yielding with scheduler.yield() and offloading pure compute to a worker." },
    { q: "Why is TTFB the highest-leverage first fix when LCP, INP, and CLS are all also poor?", a: "TTFB is the floor under every metric that depends on the HTML arriving. Nothing downstream — FCP, LCP, the start of V8 parse, the start of hydration — can begin before the first byte. At a 2980 ms TTFB, FCP and LCP are already delayed by nearly 3 seconds before any other layer does a thing. Fixing TTFB (CDN, origin caching, a service-worker shell for repeat visits) drops FCP and LCP together for free. Leverage is not about ease but about how many downstream metrics depend on the fix — and FCP and LCP both sit directly on top of TTFB, so it cascades. INP and CLS are separate failures attacked afterward in parallel." },
    { q: "Describe the capstone diagnostic discipline and the observability stack that turns it into a system.", a: "The discipline is three moves: (1) measure before you touch — a real trace and field RUM, never a guess, because the symptom routinely names the wrong layer; (2) find the constraint, not a constraint — there is one track at one moment that is the actual bottleneck, and optimising anything else moves a number that was not the problem; (3) fix the dominant cause then re-measure — because layers interact, a fix changes the budget elsewhere, so the new trace is a fresh diagnosis that may expose a secondary bottleneck. The stack that automates it: RUM with the web-vitals library capturing LCP/INP/CLS with attribution (LCP element, INP target plus LoAF script, CLS node); Long Animation Frames attributing slow frames to specific scripts; server-side tracing for TTFB and the data path; service-worker telemetry for cache hit rate; and a synthetic CI gate (throttled Lighthouse or Playwright) on every PR. Neither RUM nor CI alone suffices — RUM without CI ships every regression to users first; CI without RUM misses real device-class and interaction regressions." },
  ]}
/>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the capstone's spine: the page load is a relay across eight layers on three overlapping tracks; hydration is the hinge where five lessons converge and where early INP is decided; the five canonical breaks each own a layer but routinely present as a different one; TTFB is the floor whose fix cascades to FCP and LCP; and the discipline — measure, find the constraint, fix and re-measure — plus a layered RUM-and-CI telemetry stack is what turns whole-chain understanding into a system that catches the break for you.
</Recap>
