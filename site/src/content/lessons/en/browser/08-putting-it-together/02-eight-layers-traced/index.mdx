---
slug: 02-eight-layers-traced
lang: en
track: browser
unit: 08-putting-it-together
order: 2
title: "Eight layers traced: from the service worker to the second navigation"
summary: "One e-commerce product page followed through all eight layers — network, HTML parse, CSS, V8, first paint, hydration, the interaction, and the second navigation — with the budget numbers for each."
estMin: 18
status: ready
lessonType: topic
level: middle
prereqs:
  - browser/08-putting-it-together/01-the-full-picture
  - browser/01-event-loop/02-queues-and-scheduling
  - browser/02-render-pipeline/02-stages-and-threads
  - browser/03-v8-internals/02-jit-pipeline
  - browser/04-workers/04-service-worker-lifecycle
  - browser/05-react-fiber/03-render-and-commit-phases
  - browser/06-ssr-vs-ssg/03-hydration-cost
  - browser/07-core-web-vitals/01-what-vitals-measure
deepensInto:
  - browser/08-putting-it-together/03-five-canonical-breaks
  - browser/08-putting-it-together/04-three-track-method
spiral: ["latency", "statefulness", "encapsulation"]
concepts: ["TTFB", "preload-scanner", "CSSOM", "V8-parse-compile", "hydration-long-task", "INP", "LCP", "service-worker-cache", "three-track-timeline"]
sources:
  - https://web.dev/articles/howbrowserswork
  - https://developer.chrome.com/docs/devtools/performance
  - https://web.dev/articles/rendering-on-the-web
  - https://web.dev/articles/lcp
  - https://web.dev/articles/optimize-inp
  - https://html.spec.whatwg.org/multipage/parsing.html
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumberDrill from "~/components/pedagogy/NumberDrill.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook>
One e-commerce product page: server-rendered, React-based, hero image, "Add to cart" button, a registered service worker. A mid-range Android phone, 4G. What exactly happens — in which order, across which threads, spending how much time — from the moment the user taps the link to the moment the cart badge updates?
</Hook>

<Crux>Eight layers overlap on three tracks — reading a slow page means finding which track was the bottleneck at the moment that mattered.</Crux>

<Explanation>

**The page we are tracing.**

One concrete page: an e-commerce product page, server-rendered, React-based, with a hero image, a price, an "Add to cart" button, a reviews section below the fold, and a service worker registered from a previous visit. The user is on a mid-range Android phone on 4G. We follow this exact page from the moment they tap a link to the moment they successfully add the item to the cart.

**Layer 1 — Network.**

The tap triggers a navigation. DNS resolves the host, a TLS handshake secures the connection, and an HTTP request goes out. But there is a twist: a service worker from a prior visit is registered. The browser gives the service worker's `fetch` handler the first look — it can serve the navigation from cache or pass it through to the network. Either way, the first byte of HTML arrives. This is TTFB, and it is the floor under everything: nothing downstream can start before the bytes are here.

**Layer 2 — HTML parse and the preload scanner.**

The HTML parser begins turning bytes into a DOM tree, top to bottom. Running ahead of it is the preload scanner, spotting `<img>`, `<script>`, and `<link>` references and kicking off their downloads early. This is why the hero image, sitting in the initial HTML as a plain `<img>`, starts downloading immediately — the LCP clock is already ticking and discovery happens as early as possible. A render-blocking `<script>` in the `<head>` would pause the main parser here; a `defer`'d one would not.

**Layer 3 — CSS and the CSSOM.**

As `<link rel="stylesheet">` resources arrive, the browser parses them into the CSSOM. CSS is render-blocking by design: the browser will not paint until it has the CSSOM, because painting with incomplete styles would mean a flash of unstyled content. The DOM and CSSOM together form the render tree. If the CSS is large or served slowly, every paint downstream waits.

**Layer 4 — JavaScript: V8 parse and compile.**

The JavaScript bundle arrives and V8 goes to work — but not by running it immediately. V8 first parses the source and compiles it to bytecode (the Ignition interpreter's input). For a large bundle this parse-and-compile step alone is tens to hundreds of milliseconds of main-thread time on a mid-range phone, before a single line has executed. This is the hidden cost inside "the JavaScript is too big": the bytes are not just a download, they are CPU work.

**Layer 5 — First paint.**

With the render tree ready, the render pipeline runs: style, layout, paint, composite. The product photo, price, and layout appear. The hero image, if it has finished downloading, paints — and this is very likely the LCP moment, the largest contentful element arriving. The page now *looks done*. But it is a picture: no handler is attached, no state is live.

**Layer 6 — Hydration.**

Now React runs `hydrateRoot`. It walks the same component tree the server walked, and instead of creating DOM nodes it adopts the existing server DOM, attaching event handlers and wiring up state and effects — the reconciler in hydration mode. This is one long task: V8 executes the component tree, the reconciler does its render and commit. While it runs, the main thread is blocked. The page looked done at layer 5; it becomes *actually* interactive only when hydration reaches each component.

**Layer 7 — The interaction.**

The user taps "Add to cart." If they tap before hydration reached that button, the input is delayed — queued behind the hydration long task — and INP records a large latency. If they tap after, the handler runs: it updates cart state, the reconciler re-renders the cart badge, the render pipeline paints the change. The time from tap to that paint is one INP sample.

**Layer 8 — The second navigation and the worker.**

The user navigates to checkout. Now the service worker earns its keep: it serves the app shell from cache, so the second navigation's TTFB is tens of milliseconds instead of a network round trip.

<NumbersCard
  id="card-trace-numbers"
  title="The trace, in numbers (mid-range phone, 4G)"
  stats={[
    { label: "TTFB — network served", value: "~300 ms" },
    { label: "TTFB — service worker cache", value: "~30 ms" },
    { label: "JS parse + compile (V8)", value: "~150 ms" },
    { label: "Hydration long task", value: "300 ms – 1 s+" },
    { label: "Good LCP / INP / CLS", value: "2.5 s / 200 ms / 0.1" },
    { label: "Canonical break count", value: "5, one per domain" },
  ]}
  data-lesson-visual
/>

**The overlapped timeline — three tracks, not one sequence.**

The crucial insight is that these layers are not a tidy sequence — they overlap on a timeline across three resources:

- The **network track** is busy fetching HTML, then CSS, then the JS bundle, then the image, in parallel where the protocol allows.
- The **main thread track** is busy parsing HTML, then compiling JS, then painting, then running the hydration long task.
- The **GPU track** does the final composite.

A DevTools performance trace shows exactly these three tracks. Reading a slow page means finding which track is the bottleneck at the moment that matters — network-bound before first paint, main-thread-bound during hydration. The skill is not knowing the layers; it is reading where, on the overlapped timeline, the time actually went.

**Parallelism: why the layers are not sequential.**

The browser does not do one step, wait, do the next — it keeps all three resources busy whenever there is work. While the network downloads the JS bundle, the main thread is already parsing the HTML that arrived earlier. While the main thread compiles the JS, the network is already fetching the image. The preload scanner exists precisely for this parallelism — it finds resources and throws them onto the network while the main parser is busy with something else.

The diagnostic consequence: a "slow page" is almost never one slow step. It is the moment where parallelism broke down, where one track became the constraint and the other two sat idle waiting for it. A render-blocking script is the classic example: it stops the main parser, and while it downloads, the main thread is idle even though it could be parsing the rest of the HTML.

<Quiz
  id="mid-quiz-1"
  pieceSlug="02-eight-layers-traced"
  lang="en"
  question="On the second navigation (to checkout), TTFB drops from ~300 ms to ~30 ms. Which layer is responsible?"
  choices={[
    { label: "The service worker — it serves the app shell from cache, skipping the network round trip", correct: true },
    { label: "The DNS cache", misconception: "DNS caching saves the lookup, but not the whole round trip to the server. A ~30 ms TTFB means the response came from a local cache — the service worker." },
    { label: "Hydration finished, so the page is faster", misconception: "Hydration affects interactivity, not the TTFB of a new navigation. TTFB this low means a cache served the document — the service worker." },
    { label: "The render pipeline got faster", misconception: "The render pipeline does not affect TTFB at all — TTFB is the byte arriving, before any rendering. The service worker cache is the cause." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="02-eight-layers-traced"
  lang="en"
  question="On the trace, V8 spends ~150 ms before a single line of the bundle has executed. What is it doing?"
  choices={[
    { label: "Parsing the JavaScript source and compiling it to bytecode — CPU work that scales with bundle size, before execution begins", correct: true },
    { label: "Downloading the bundle", misconception: "Downloading is the network track. The ~150 ms is main-thread CPU work — V8 parsing and compiling the bytes it already received." },
    { label: "Waiting for the server", misconception: "Waiting for the server is TTFB, much earlier. This 150 ms is V8 actively working on the main thread, turning source into bytecode." },
    { label: "Running the hydration", misconception: "Hydration is execution — it comes after parse and compile. The 150 ms here is specifically the parse+compile step before any execution." },
  ]}
/>

<TraceScenario
  id="mid-trace-1"
  pieceSlug="02-eight-layers-traced"
  lang="en"
  scenario="A DevTools trace of the product page: LCP fires at 1.5 s (good). But there is a 900 ms main-thread long task starting at 1.6 s, and the user's tap on 'Add to cart' at 2.0 s shows an INP of 700 ms. What is the long task, and why is INP bad despite a good LCP?"
  steps={[
    { prompt: "The 900 ms long task is hydration — V8 executing the component tree and the reconciler adopting the server DOM; the 2.0 s tap landed inside it, so INP recorded the wait", reveal: "Correct. LCP measures when the largest content painted — 1.5 s, good, because the server HTML painted fast. But the page was not interactive yet: the 900 ms long task starting at 1.6 s is hydration. The tap at 2.0 s arrived 400 ms into that task, and the input was queued behind the remaining ~500 ms of hydration — that queued wait is the bulk of the 700 ms INP. Good LCP and bad INP coexisting is the signature of the hydration uncanny valley: painted fast, interactive slow. The fix is to shrink hydration — code-split, Server Components, fewer hydrated islands." },
    { prompt: "The long task is the network fetching images", reveal: "Network fetches are not main-thread long tasks — they show on the network track as the main thread sits idle or does other work. A 900 ms main-thread task this early is hydration (or a heavy script execution), not a download." },
    { prompt: "INP is bad because LCP was bad", reveal: "LCP was 1.5 s — good. INP and LCP are independent metrics measuring different things (paint vs interactivity). A good LCP does not protect INP; here hydration ruins INP while LCP is fine." },
  ]}
/>

<Quiz
  id="mid-quiz-3"
  pieceSlug="02-eight-layers-traced"
  lang="en"
  question="Why is reading a DevTools trace described as finding the bottleneck 'track', not the bottleneck 'layer'?"
  choices={[
    { label: "The layers overlap on a timeline across network, main-thread, and GPU tracks — the bottleneck is whichever track is busy at the moment that matters", correct: true },
    { label: "The layers run in a strict sequence with no overlap", misconception: "They overlap heavily — the network fetches resources while the main thread parses and compiles. A trace shows parallel tracks, not a single sequence." },
    { label: "DevTools only shows one track at a time", misconception: "DevTools shows network, main-thread, GPU, and more simultaneously. Reading the trace is about comparing tracks to find the constraint." },
    { label: "Tracks and layers are unrelated concepts", misconception: "They are related: each layer's work runs on one of the tracks. The point is that the work overlaps, so you locate the bottleneck by track and time." },
  ]}
/>

<DragOrder
  id="mid-drag-1"
  pieceSlug="02-eight-layers-traced"
  lang="en"
  prompt="Order the eight layers of the page's life, from the user's tap to the second navigation."
  items={[
    { id: "network", label: "Network — DNS, TLS, HTTP; service worker gets first look" },
    { id: "htmlparse", label: "HTML parse — DOM tree, preload scanner discovers resources" },
    { id: "css", label: "CSS — CSSOM built, render tree formed" },
    { id: "v8", label: "V8 — JS bundle parsed and compiled to bytecode" },
    { id: "paint", label: "First paint — layout, paint, composite; LCP fires" },
    { id: "hydrate", label: "Hydration — reconciler adopts server DOM, attaches handlers" },
    { id: "interact", label: "Interaction — tap handled, re-render, INP measured" },
    { id: "secondnav", label: "Second navigation — service worker serves the app shell" },
  ]}
  correctOrder={["network", "htmlparse", "css", "v8", "paint", "hydrate", "interact", "secondnav"]}
/>

<MetaphorComplete
  id="mid-metaphor-1"
  pieceSlug="02-eight-layers-traced"
  lang="en"
  setup="A good LCP and a bad INP coexisting — the page paints fast but cannot be tapped — has a name. It describes the window where the page is a convincing picture that does not respond. What is that window called?"
  accepted={["uncanny valley", "the uncanny valley", "hydration uncanny valley", "uncanny valley of hydration"]}
  canonical="uncanny valley"
  explanation="The hydration uncanny valley is the window between first paint and full interactivity: the page looks completely done — text, images, buttons all visible — but the JavaScript has not finished hydrating, so taps are dropped or delayed. It is 'uncanny' because the page looks alive but is not. A good LCP with a poor INP is the precise measurable signature of a page stuck in this valley."
/>

<NumberDrill
  id="mid-drill-1"
  pieceSlug="02-eight-layers-traced"
  lang="en"
  question="On the trace, the JS bundle parse-and-compile in V8 takes about how many milliseconds for a typical bundle on a mid-range phone — the cost before any line executes?"
  expected={150}
  tolerance={50}
  unit="ms"
  explanation="Roughly 150 ms (it scales with bundle size and is far worse on low-end devices). This is pure V8 parse-and-compile — turning source bytes into bytecode — before a single line runs. It is the hidden CPU cost inside 'the bundle is too big': the bytes are not only a download, they are main-thread work that delays everything after them, including hydration."
/>

<Inset kind="why" lang="en">
The eight layers are not an arbitrary list — they are the shape the web acquired over thirty years of evolution. Early pages were layers 1–3 and nothing else: network, parse, paint — a static document. JavaScript added execution, and layer 4 (V8) became significant. AJAX and SPAs pushed rendering into the browser, killing layer 5 as "first paint of something useful." SSR brought server rendering back for fast first paint and SEO — but it also introduced layer 6, hydration, and with it the uncanny valley. Service workers (layer 8) were added to win back the instant repeat loads lost when sites moved away from static files. Each layer is a solution to a past tradeoff that became a new tradeoff.
</Inset>

</Explanation>

<KeyTakeaway>Eight layers run on three overlapping tracks (network, main thread, GPU). TTFB is the floor; V8 parse+compile adds ~150 ms before execution; hydration is one long main-thread task that blocks early taps. The uncanny valley — good LCP, bad early INP — is the structural signature of server-rendered apps.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="02-eight-layers-traced-retrieval"
  lang="en"
  questions={[
    { q: "Trace the e-commerce product page from the user's tap to a working 'Add to cart' button, naming each of the eight layers.", a: "Layer 1, network: DNS, TLS, HTTP — service worker gets first look (cache or pass-through), first byte arrives as TTFB. Layer 2, HTML parse: DOM tree built; preload scanner discovers hero img and starts its download. Layer 3, CSS: stylesheets parse into CSSOM; DOM + CSSOM = render tree; CSS is render-blocking. Layer 4, V8: JS bundle parsed and compiled to bytecode (~150 ms before execution). Layer 5, first paint: layout, paint, composite; hero paints and LCP fires — page looks done. Layer 6, hydration: hydrateRoot runs reconciler in adopt mode, attaches handlers — one long main-thread task. Layer 7, interaction: tap; if hydration reached the button the handler runs and cart re-renders (INP sample); if not, tap queues behind hydration (bad INP). Layer 8, second navigation: service worker serves app shell from cache (~30 ms TTFB)." },
    { q: "What is the three-track structure of a DevTools trace, and why does 'finding the bottleneck track' matter more than 'finding the slow layer'?", a: "Three tracks: network (fetching HTML, CSS, JS, images), main thread (parsing, compiling, painting, hydrating, handling), GPU/compositor. Layers overlap across these tracks in parallel. Finding the bottleneck track at the moment that matters — not just which layer was involved — is what makes the diagnosis fixable. If LCP is late and the hero is still on the network track at LCP time: a discovery or network problem. If LCP is late but the image finished early and the main thread is solid: render-blocking JS. The constraint track points at the actual fix." },
    { q: "Why does V8 spend ~150 ms before a single line of the bundle executes, and what does this cost affect?", a: "V8 must parse the JavaScript source into an AST and then compile it to bytecode for the Ignition interpreter before any line runs. For a typical bundle on a mid-range phone this is ~150 ms of main-thread work. This delays everything that comes after it: the first execution, which leads to hydration, which determines when the page becomes interactive (INP). It is the hidden CPU cost inside 'the bundle is too big' — the bytes are not just a download, they are also a parse-and-compile task." },
  ]}
/>

<Recap lang="en">
A server-rendered, React-based product page traverses eight layers: network (TTFB ~300 ms, or ~30 ms from service worker cache), HTML parse with preload scanner starting the hero image early, CSS building the CSSOM (render-blocking), V8 parse-and-compile (~150 ms of main-thread work before execution), first paint and LCP, a hydration long task (300 ms to 1 s+) that blocks early taps, the interaction (INP measured tap-to-paint), and a second navigation served from cache. These eight layers run on three overlapping tracks — network, main thread, and GPU — not a single sequence. Parallelism is the norm; a bottleneck is the moment one track stalls and the others wait. The hydration uncanny valley — good LCP, bad early INP — is the structural failure of server-rendered apps: the page looks done before it works, and every tap inside the hydration long task is a delayed interaction the user feels.
</Recap>
