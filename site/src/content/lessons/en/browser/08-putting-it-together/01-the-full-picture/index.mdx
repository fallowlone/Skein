---
concepts:
- relay-race-metaphor
- hydration-gap
- chain-problem
- uncanny-valley
deepensInto:
- browser/08-putting-it-together/02-eight-layers-traced
estMin: 10
lang: en
lessonType: topic
level: junior
order: 1
prereqs:
- browser/01-event-loop/01-loop-model
- browser/02-render-pipeline/01-the-six-stages
- browser/06-ssr-vs-ssg/03-hydration-cost
slug: 01-the-full-picture
sources:
- https://web.dev/articles/howbrowserswork
- https://web.dev/articles/vitals
- https://web.dev/articles/rendering-on-the-web
spiral:
- latency
- statefulness
status: ready
summary: A page load is a relay race across network, parse, paint, and hydration stages
  — one slow runner makes every later runner late, and the user only sees the finish
  line.
title: 'The full picture: URL to LCP to INP as a relay race'
track: browser
unit: 08-putting-it-together
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumberDrill from "~/components/pedagogy/NumberDrill.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Bea taps a product link. The page paints the photo and price after 1.4 seconds — it looks done. She taps "Add to cart" at 2 seconds and nothing happens. The button is not broken; it is not wired up yet. Hydration has not finished. By the end of this lesson you will know exactly which stage of a page load creates that gap — and why fixing the wrong runner changes nothing.
</Hook>

<Crux>A page load is a relay race: the race is only as fast as the whole chain, and "looks done" is not the same as "works."</Crux>

<Explanation>

**The relay-race metaphor.**

When you look at a slow page, which runner do you blame? Before you can answer that, you need to know who the runners are and in what order they pass the baton.

Think of a page load as a relay race with four runners, each passing a baton to the next:

1. **Network** — fetches the HTML bytes from the server.
2. **Parse** — builds the DOM from HTML and the CSSOM from CSS.
3. **Paint** — lays out the page and draws the first frame. At this point the page *looks done*.
4. **Hydrate** — downloads and runs the JavaScript that attaches event handlers and state to the painted page. Only now does the page *work*.

The race is only as fast as the whole chain. If any runner stumbles, every runner after them is late. The user only sees the finish line — when the page looks done and when it actually responds to a tap. Together these four stages mean that "the page is slow" could be a network problem, a CSS problem, a JavaScript size problem, or a hydration problem — and without the relay frame, you are guessing.

**Why care.**

Almost no real performance problem lives in one place. A slow page is usually a chain problem: the server was a little slow, the bundle was a little big, the hero image was a little late — and together they push the page over the line into "slow." Tracing the whole chain beats guessing at one step.

<FlowDiagram
  label="A page load as a relay race: Network hands to Parse, Parse to Paint, Paint to Hydrate, Hydrate to Interact; the page looks done after Paint but only works after Hydrate."
  perRow={5}
  hue="var(--d-frontend)"
  nodes={[
    { id: "network", label: "Network", sub: "fetch HTML" },
    { id: "parse", label: "Parse", sub: "DOM + CSSOM" },
    { id: "paint", label: "Paint", sub: "looks done" },
    { id: "hydrate", label: "Hydrate", sub: "run JS" },
    { id: "interact", label: "Interact", sub: "works" },
  ]}
  edges={[
    { from: "network", to: "parse" },
    { from: "parse", to: "paint" },
    { from: "paint", to: "hydrate", label: "the gap" },
    { from: "hydrate", to: "interact" },
  ]}
  caption="The baton passes one way only. The page looks done after Paint, but a tap gets no response until Hydrate finishes — that gap between 'looks done' and 'works' is where dropped taps happen."
/>

<NumbersCard
  id="card-relay-numbers"
  title="A real relay trace (mid-range phone, 4G)"
  stats={[
    { label: "Page looks done (LCP)", value: "1.4 s" },
    { label: "Page becomes interactive (hydration done)", value: "3.0 s" },
    { label: "Gap: looks done but is not", value: "1.6 s" },
    { label: "Good LCP threshold", value: "≤ 2.5 s" },
    { label: "Good INP threshold", value: "≤ 200 ms" },
  ]}
  data-lesson-visual
/>

<DragOrder
  id="jr-drag-1"
  pieceSlug="01-the-full-picture"
  lang="en"
  prompt="Trace a page from the user pressing Enter to a working button. Drag the stages into order."
  items={[
    { id: "network", label: "Network: fetch the HTML bytes from the server" },
    { id: "parse", label: "Parse: build the DOM from HTML, the CSSOM from CSS" },
    { id: "paint", label: "Paint: lay out and draw the first frame — page looks done" },
    { id: "hydrate", label: "Hydrate: run the JavaScript, wire up the buttons" },
    { id: "interact", label: "Interact: a tap now gets a response" },
  ]}
  correctOrder={["network", "parse", "paint", "hydrate", "interact"]}
/>

<Quiz
  id="jr-quiz-1"
  pieceSlug="01-the-full-picture"
  lang="en"
  question="A page 'looks done' but a button does not work yet. Which stage of the relay is still running?"
  choices={[
    { label: "Hydration — the JavaScript that wires up the buttons has not finished running", correct: true },
    { label: "The network is still fetching the HTML", misconception: "The HTML already arrived — that is why the page looks done. The unfinished stage is hydration, which comes after paint." },
    { label: "The page is fully done; the button is just broken", misconception: "The button is not broken — it is not wired up yet. Once hydration finishes it will work. This is a timing gap, not a bug." },
    { label: "The CSS is still loading", misconception: "If the page looks done, the CSS painted. An unresponsive button is a JavaScript/hydration timing issue, not CSS." },
  ]}
/>

<Quiz
  id="jr-quiz-2"
  pieceSlug="01-the-full-picture"
  lang="en"
  question="Why is a slow page usually a 'chain problem'?"
  choices={[
    { label: "Several stages are each a little slow, and together they push the page over the line — no single stage is the whole cause", correct: true },
    { label: "Because there is always exactly one slow stage to find and fix", misconception: "Real slow pages rarely have a single villain. The slowness is usually spread across several stages that add up." },
    { label: "Because the network is always the problem", misconception: "The network is one stage of many. Blaming it by default is exactly the guessing that tracing the whole chain avoids." },
    { label: "Because JavaScript is always too big", misconception: "Bundle size is one common factor, but not the only one. A slow page is a chain — server, bytes, images, and JS all contribute." },
  ]}
/>

<MetaphorComplete
  id="jr-metaphor-1"
  pieceSlug="01-the-full-picture"
  lang="en"
  setup="In the relay-race metaphor, the page 'looks done' when the picture is painted, but it is not truly done until one more runner finishes — the one who wires up the buttons. What is that final runner's job called?"
  accepted={["hydration", "hydrate", "hydrating"]}
  canonical="hydration"
  explanation="Hydration is the final runner: after the HTML is fetched, parsed, and painted, the JavaScript still has to download and run to attach event handlers and state to the painted page. Until that runner crosses the line, the page is a picture — it looks done but does not respond. The gap between 'painted' and 'hydrated' is the single most important thing to understand about modern page loads."
/>

<NumberDrill
  id="jr-drill-1"
  pieceSlug="01-the-full-picture"
  lang="en"
  question="In the read-aloud trace, the page looks done at 1.4 s but is only interactive at 3.0 s. How many seconds is the 'looks done but is not' gap?"
  expected={1.6}
  tolerance={0}
  unit="seconds"
  explanation="3.0 s minus 1.4 s is a 1.6-second gap. For 1.6 seconds the page is a convincing picture that does not respond to taps. This gap — painted but not yet hydrated — is where the user taps a dead button, and it is the single most common source of 'this site feels broken' complaints on modern server-rendered sites."
/>

<Inset kind="why" lang="en">
The gap between "painted" and "interactive" has a name in the performance literature: the hydration uncanny valley. The page looks alive — text, images, buttons all visible — but it is not, because the JavaScript has not finished hydrating. It is "uncanny" because appearances deceive. A good LCP (fast paint) with a poor INP (slow first interaction) is the measurable signature of a page stuck in this valley.
</Inset>

</Explanation>

<KeyTakeaway>A page load is a relay: network, parse, paint, hydrate. "Looks done" happens at paint; "actually done" at hydration. The gap between them is where taps are dropped — and most slow pages are chain problems, not single-step failures.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-the-full-picture-retrieval"
  lang="en"
  questions={[
    { q: "What are the four relay runners of a page load, in order?", a: "Network (fetch HTML bytes), Parse (build DOM from HTML and CSSOM from CSS), Paint (lay out and draw the first frame — page looks done), Hydrate (download and run JS, wire up buttons — page now works). Only after Hydrate does a tap get a response." },
    { q: "Why is 'looks done' not the same as 'works'?", a: "Paint produces a visual picture but no JavaScript is running yet — no event handlers are attached and no state is live. Hydration runs afterward and is what actually wires the page up. The gap between painted and hydrated is where the user can tap a button and get no response." },
    { q: "What makes a slow page a 'chain problem'?", a: "Each stage consumes time from a shared budget. Several stages can each be slightly over, and together they add up to a page that crosses the 'slow' threshold. No single stage is the whole cause — server latency + bundle size + image load delay can each be tolerable alone but unacceptable together. Guessing at one stage and fixing it may not move the overall number." },
  ]}
/>

<Recap lang="en">
A page load is a relay race across four stages: network, parse, paint, and hydrate. The race is only as fast as the whole chain — one slow runner delays every runner after. The user sees two moments: when the page looks done (after paint, LCP territory) and when it actually works (after hydration, INP territory). The gap between them — up to 1.6 seconds in the trace above — is where taps are dropped and users feel the site is broken. Most real performance problems are chain problems, not single-step failures, which is why tracing the whole chain beats guessing at one place. Now when you see a button that does not respond on a painted page, you know to look at the hydration runner — not the button.
</Recap>
