---
concepts:
- relay-race-chain
- hydration-uncanny-valley
- three-track-bottleneck
- canonical-breaks
- ttfb-leverage
- measure-before-touch
estMin: 14
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://web.dev/articles/howbrowserswork
- https://web.dev/articles/optimize-inp
- https://web.dev/articles/optimize-lcp
status: ready
summary: "Cross-track synthesis MCQs over the whole browser chapter — relay-race chain, the hydration uncanny valley, three-track bottleneck reading, and the five canonical breaks."
title: 'Putting it together: multiple-choice review'
track: browser
unit: 08-putting-it-together
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole browser track — event loop, render pipeline, V8, workers, fiber, SSR/SSG, and Core Web Vitals collapsed into one page. Each one is a decision you make staring at a real trace, not a definition to recite.
</Hook>

<Goal lang="en">
Confirm you can connect every layer of the page-load relay into one chain: why the symptom routinely names the wrong layer, why hydration is the hinge, and which track is the constraint at the moment that matters.
</Goal>

<Quiz
  id="08-putting-it-together-choice-1"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="A server-rendered product page hits LCP at 1.4 s (good) but the first tap on 'Add to cart' at 2.0 s records INP 700 ms. What is the structural explanation?"
  choices={[
    { label: "The hydration uncanny valley: paint (layer 5) finished fast so the page looks done, but the hydration long task (layer 6) is still running, so the tap queues behind it as input delay — good LCP and bad early INP coexist by design in SSR apps.", correct: true },
    { label: "LCP and INP measure the same thing, so a 700 ms INP means LCP was actually bad too.", misconception: "LCP measures when the largest element paints; INP measures interaction latency. They are independent — a fast paint over server HTML does not protect interactivity while hydration blocks the main thread." },
    { label: "The 'Add to cart' handler itself takes 700 ms and must be optimised.", misconception: "With a tap inside the hydration window, most of the 700 ms is input delay — time queued behind the long task — not handler processing time. Optimising a handler that was never the constraint moves nothing." },
    { label: "The network was still downloading the page at 2.0 s.", misconception: "The page already painted at 1.4 s, so the bytes arrived. A post-paint interaction delay is a main-thread problem (hydration), not a network one." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-2"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="A DevTools trace shows LCP firing late while the hero image is still on the network track at the LCP moment. Which layer is the actual cause, and which is paying for it?"
  choices={[
    { label: "Discovery (layer 2, the preload scanner) is the cause — the hero is a CSS background-image or lazy-loaded, so it was found and fetched late; paint (layer 5) pays for it with a late LCP. Fix: a plain eager img with fetchpriority high in the initial HTML.", correct: true },
    { label: "The render pipeline is too slow at compositing the image.", misconception: "If the image is still downloading at LCP time, the compositor never got it. The pipeline is fine; the failure is that discovery happened too late on the parse layer." },
    { label: "V8 is render-blocking the paint.", misconception: "Render-blocking JS is the other branch — hero finished early but the main thread is busy. Here the hero is still on the network track, so the constraint is discovery, not V8." },
    { label: "The GPU track is the bottleneck.", misconception: "A resource still downloading at LCP time is a network-track symptom rooted in discovery. The GPU composites only what already arrived and was painted." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-3"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="The 'Add to cart' re-render that should cost ~5 ms costs ~80 ms and tanks INP, even though the bundle is small and the handler is short. What is the most likely cross-layer cause?"
  choices={[
    { label: "A megamorphic hot function on the render path: a property accessor or comparator has seen too many object shapes, so V8 (layer 3) cannot use an inline cache and falls back to slow lookups — the reconciler (layer 5) is fine, the V8 shapes are not. Fix: stabilise object shapes to keep ICs monomorphic.", correct: true },
    { label: "React's reconciler is inherently slow and must be replaced.", misconception: "The reconciler walked the same tree fast elsewhere. An 80 ms re-render with a small handler points at a megamorphic V8 path, not the reconciler — this is the classic 'React is slow' misattribution." },
    { label: "The network re-fetched data during the re-render.", misconception: "A synchronous re-render cost on the main thread is not a network event. A data fetch would show on the network track while the main thread is free, not as scripting time inside the interaction." },
    { label: "CLS — the layout shifted during the interaction.", misconception: "CLS measures visual stability, not interaction latency. An 80 ms scripting bar inside the tap is an INP problem caused by slow execution, not a layout shift." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-4"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="A deploy ships new HTML and a new JS bundle, but a fraction of returning users hit runtime errors and some are stuck on a broken page even after the fix deploys. What is the canonical break and the layer?"
  choices={[
    { label: "Stale service worker cache (layers 1 and 8): the worker serves last week's cached app.js cache-first against this week's HTML — a module-version mismatch — and a cached broken shell traps repeat visitors, bypassing the network where the fix lives. Fix: content-hashed filenames, version-tagged caches purged in activate, network-first navigation, a kill switch.", correct: true },
    { label: "A hydration mismatch between server and client HTML.", misconception: "A mismatch flashes content and spikes CLS within a session; it does not survive a deploy or trap users on old code. Persisting across deploys and bypassing the fix is the signature of a stale worker cache." },
    { label: "The CDN cached the new bundle too aggressively.", misconception: "The new bundle deployed fine; the problem is a client-side cache (the service worker) serving the OLD bundle against new HTML. A CDN flush would not free users trapped behind their own worker." },
    { label: "A megamorphic V8 path slowed the new bundle.", misconception: "Megamorphism hurts INP, not correctness across deploys. Runtime errors from a version skew plus users trapped on a broken shell is statefulness — the cache outlived its correctness." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-5"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="A junior optimises the 'Add to cart' handler from 12 ms down to 6 ms; field INP stays at 480 ms. What did the three-track method say they should have done first?"
  choices={[
    { label: "Measure before touching, then find the constraint — not a constraint. The trace shows a 900 ms hydration long task before every early tap; the input delay, not handler processing, is the constraint. The handler was never on the critical path; shrinking the bundle to cut the long task is the real lever.", correct: true },
    { label: "Optimise the handler further — 6 ms is still too slow.", misconception: "Handler processing time was never the constraint. INP held at 480 ms because the tap queues behind the hydration long task; no amount of handler optimisation touches that input delay." },
    { label: "Switch the page from SSR to client-side rendering to remove hydration.", misconception: "CSR removes hydration but reintroduces a blank first paint and a worse LCP and SEO — trading one problem for two. The fix is shrinking what hydrates (code-split, Server Components, islands), not abandoning SSR." },
    { label: "Add more CPU cores so hydration runs in parallel.", misconception: "Hydration is largely a single long main-thread task; extra cores do not split it. The lever is less to parse, compile, and execute — a smaller bundle and fewer hydrated islands." },
  ]}
/>

<Quiz
  id="08-putting-it-together-choice-6"
  lessonSlug="08-putting-it-together"
  lang="en"
  question="One RUM session is poor on every metric: TTFB 2980 ms, LCP 6800 ms (hero is a CSS background-image), INP 540 ms (LoAF names the hydration script), CLS 0.21 (price re-rendered). Which single fix has the most leverage to start with, and why?"
  choices={[
    { label: "Fix TTFB first. It is the floor: nothing downstream — FCP, LCP, the start of V8 parse, the start of hydration — can begin before the first byte. Cutting ~3 s of TTFB (CDN, origin caching, service-worker shell for repeat visits) drops FCP and LCP together for free, then LCP discovery, hydration cost, and the mismatch get attacked in parallel.", correct: true },
    { label: "Fix CLS first — it is the most visible to users.", misconception: "CLS is a real bug, but fixing the price mismatch leaves a ~3 s TTFB floor untouched. Leverage is about how many downstream metrics depend on a fix, and FCP and LCP both sit on top of TTFB." },
    { label: "Add a skeleton loader so the page feels faster.", misconception: "A skeleton hides the wait but moves no metric — the main thread is still blocked and the bytes still arrive at 2980 ms. It treats perception, not the constraint." },
    { label: "Optimise INP first because it is the interaction metric.", misconception: "INP matters, but its root here is the hydration long task downstream of the bundle. With a 2980 ms TTFB, paint cannot even start for ~3 s; fixing the floor cascades to FCP and LCP, so TTFB is the higher-leverage first move." },
  ]}
/>

<Recap lang="en">
The through-line across the track is one chain: TTFB is the floor, discovery feeds LCP, V8 parse plus the reconciler feed the hydration long task, and that long task is the hinge that determines early INP. The symptom routinely names the wrong layer — 'slow image' is discovery, 'React is slow' is a megamorphic V8 shape, 'broken after deploy' is a stale worker cache. The discipline is always the same: measure before touching, find the constraint track at the constraint moment, fix the dominant cause (TTFB and hydration cost first), then re-measure.
</Recap>
