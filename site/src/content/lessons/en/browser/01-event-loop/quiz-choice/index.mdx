---
concepts:
- microtask-vs-task
- rendering-step
- requestAnimationFrame
- microtask-starvation
- scheduler-yield
- node-nexttick
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://html.spec.whatwg.org/multipage/webappapis.html#event-loops
- https://web.dev/articles/inp
- https://nodejs.org/en/learn/asynchronous-work/event-loop-timers-and-nexttick
status: ready
summary: "Multiple-choice synthesis across the event-loop unit — queue semantics, the render step, timer throttling, starvation, scheduler.yield, and the Node loop."
title: 'Event loop: multiple-choice review'
track: browser
unit: 01-event-loop
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one mirrors a call you make in a real performance incident — not a definition to recite, but which queue a primitive lands in, and what that costs the next frame.
</Hook>

<Goal lang="en">
Confirm you can connect queue semantics, the render step, timer throttling, starvation, and cross-runtime differences — the synthesis the individual lessons built toward.
</Goal>

<Quiz
  id="01-event-loop-choice-1"
  lessonSlug="01-event-loop"
  lang="en"
  question="Inside a click handler you call setTimeout(t, 0) then queueMicrotask(m), then the handler returns. In what order do t and m run relative to the next paint?"
  choices={[
    { label: "m runs first (microtasks drain to empty right after the handler, before any frame), then the browser may paint, then t runs as a task on a later iteration.", correct: true },
    { label: "t runs first because setTimeout(0) means zero delay, then m as a microtask.", misconception: "setTimeout(0) queues a task; the microtask checkpoint drains before the loop pulls another task. queueMicrotask always runs before any setTimeout callback queued in the same turn." },
    { label: "They run in registration order: t then m, both before the next paint.", misconception: "Queue, not registration order, decides timing. m is a microtask (pre-render, immediate); t is a task (post-render, after the checkpoint and possibly a frame)." },
    { label: "Both run after the next paint because the handler is a task.", misconception: "Microtasks drain before the render step, not after it. m runs before any paint; only t waits for a later iteration." },
  ]}
/>

<Quiz
  id="01-event-loop-choice-2"
  lessonSlug="01-event-loop"
  lang="en"
  question="A click handler runs for 400 ms. Which of these keeps working during the block, and why?"
  choices={[
    { label: "A CSS transform animation on a compositor-promoted element — it runs on the GPU/compositor thread and does not need the main thread the handler is holding.", correct: true },
    { label: "A pending promise's .then callback, because microtasks have priority over tasks.", misconception: "Microtasks still need the main thread. They queue behind the running task and cannot run until the 400 ms handler returns." },
    { label: "A setInterval callback, because timers fire on their own clock.", misconception: "Timers only queue a task; the callback still needs the blocked main thread. Backed-up interval callbacks burst when the thread frees." },
    { label: "A new click from the user, because input has a separate thread.", misconception: "The input thread records the event but the handler runs on the main thread. The click queues and waits — the user sees a frozen UI." },
  ]}
/>

<Quiz
  id="01-event-loop-choice-3"
  lessonSlug="01-event-loop"
  lang="en"
  question="You must do 200 ms of work without freezing input. You try await Promise.resolve() between chunks and the page still locks up. Why, and what actually yields?"
  choices={[
    { label: "await Promise.resolve() queues a microtask, so the loop never reaches the render or input step between chunks — your work just extends the current task. A task-level yield (scheduler.yield(), setTimeout(0), MessageChannel) is what lets rendering and input run.", correct: true },
    { label: "Promise.resolve() is too slow; allocating promises in a loop is the freeze. Use await Promise.all instead.", misconception: "Allocation cost is negligible here. The freeze is structural: microtasks drain before the render step, so no yield to input ever happens." },
    { label: "await only yields if the promise is not already resolved; awaiting a resolved one is a no-op that runs synchronously.", misconception: "It still suspends the function and resumes via the microtask queue — but a microtask, by definition, runs before any frame, so it does not yield to the renderer." },
    { label: "requestAnimationFrame is the only correct yield; setTimeout still blocks.", misconception: "rAF only runs on a frame boundary, so it is a poor general yield. setTimeout(0) and scheduler.yield() both queue tasks that let input and rendering interleave." },
  ]}
/>

<Quiz
  id="01-event-loop-choice-4"
  lessonSlug="01-event-loop"
  lang="en"
  question="A background tab runs setInterval(poll, 1000). After several minutes hidden, how often does the callback fire, and what compounds with this?"
  choices={[
    { label: "Roughly once per minute — hidden tabs clamp timers to ≥1 s immediately and to ≥1 min after minutes hidden; rAF stops entirely, and the same logic still drains microtasks normally.", correct: true },
    { label: "Every second — setInterval is exempt from background throttling because it self-reschedules.", misconception: "setInterval is throttled exactly like setTimeout in hidden tabs. The 1 s minimum applies immediately, the 1 min minimum after a longer hidden period." },
    { label: "Every 4 ms — the nesting clamp is the floor and overrides visibility.", misconception: "The 4 ms nesting clamp applies to deep setTimeout/setInterval chains; in a hidden tab the much larger background throttle wins." },
    { label: "Never — the browser suspends all timers in background tabs.", misconception: "Timers fire at a throttled rate, not zero. Full suspension only happens if the Page Lifecycle API freezes the tab — a stronger, later signal." },
  ]}
/>

<Quiz
  id="01-event-loop-choice-5"
  lessonSlug="01-event-loop"
  lang="en"
  question="A search input shows INP of 410 ms. The Long Tasks API logs a 312 ms task but only attributes it to 'same-origin'. What is the right next instrument and why?"
  choices={[
    { label: "LoAF (PerformanceLongAnimationFrameTiming): it fires per slow frame with a per-script attribution array, so you can resolve the heaviest script's URL and char position through a sourcemap to a file:line — Long Tasks gives only browsing-context attribution, good for counting, not root-cause.", correct: true },
    { label: "Switch to the Long Tasks API's attribution field harder — it does expose the function once you enable buffered:true.", misconception: "buffered:true only backfills entries from before the observer attached; it does not add script-level attribution. Long Tasks attribution stays at browsing-context granularity." },
    { label: "Reproduce locally in DevTools — production attribution is never trustworthy.", misconception: "Local repro often misses production payload size and device class. LoAF plus sourcemap resolution is the correct production attribution path." },
    { label: "Use the User Timing API marks instead; LoAF and Long Tasks both lack timing detail.", misconception: "User Timing needs you to already know where to place marks. LoAF auto-attributes the heaviest script per slow frame without manual instrumentation." },
  ]}
/>

<Quiz
  id="01-event-loop-choice-6"
  lessonSlug="01-event-loop"
  lang="en"
  question="A library tested only in Node chains process.nextTick(a) then Promise.resolve().then(b) and relies on a running before b. Ported to the browser via a nextTick polyfill, the ordering flips. Why?"
  choices={[
    { label: "In Node, process.nextTick has its own queue that drains before the microtask queue, so a precedes b. The browser has no nextTick; a polyfill maps it to queueMicrotask, giving a and b equal microtask priority — order becomes registration order, which can differ.", correct: true },
    { label: "The browser runs Promise.then on a separate worker thread, so b races ahead.", misconception: "All microtasks run on the main thread. The flip is about queue priority: Node's nextTick outranks microtasks; the polyfilled version does not." },
    { label: "ECMA-262 defines nextTick, so conforming browsers must honour the same ordering.", misconception: "process.nextTick is Node-specific and not in ECMA-262. The spec only defines the microtask (jobs) queue, which is why the polyfill collapses to queueMicrotask." },
    { label: "setTimeout(0) in the polyfill demotes a to a task, so it runs after b.", misconception: "A correctness-minded polyfill uses queueMicrotask, not setTimeout — but either way the point stands: nextTick's pre-microtask priority does not exist in the browser." },
  ]}
/>

<Recap lang="en">
The through-line across the unit is one decision: which queue does this primitive land in, and what does that cost the next frame. Microtasks (promise callbacks, queueMicrotask, MutationObserver) drain before rendering and starve it if they self-schedule; tasks (setTimeout, MessageChannel, input dispatch) yield to rendering but pay clamps and throttling; rAF runs only on a frame boundary. Diagnose user-perceived latency with INP, attribute it with LoAF plus sourcemaps, and remember that Node reorders all of this with process.nextTick sitting above microtasks.
</Recap>
