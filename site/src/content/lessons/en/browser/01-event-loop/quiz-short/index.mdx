---
concepts:
- microtask-checkpoint
- render-step
- timer-throttling
- microtask-starvation
- loaf-inp
- node-loop-lag
estMin: 14
lang: en
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://html.spec.whatwg.org/multipage/webappapis.html#event-loops
- https://developer.mozilla.org/en-US/docs/Web/API/PerformanceLongAnimationFrameTiming
- https://web.dev/articles/inp
status: ready
summary: "Free-recall prompts across the event-loop unit. Answer each in your own words first, then reveal the model answer and compare."
title: 'Event loop: free-recall review'
track: browser
unit: 01-event-loop
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory before you open the model answer — the effort of recall is what makes the loop's timing model stick.
</Hook>

<Goal lang="en">
Reconstruct the unit's core mechanisms — the five-step iteration, the microtask checkpoint, timer throttling, the starvation failure mode, the INP-to-LoAF pipeline, and Node's reordering — without looking back at the lessons.
</Goal>

<RetrievalDrawer
  client:visible
  id="01-event-loop-short-retrieval"
  lang="en"
  questions={[
    { q: "Walk through one full iteration of the browser event loop, naming what runs and when each step is skipped.", a: "Step 1: select one task from a non-empty task queue (skipped only if all queues are empty — then the loop idles). Step 2: run that task to completion — no preemption. Step 3: the microtask checkpoint drains the microtask queue to empty, including microtasks scheduled by other microtasks. Step 4: update rendering — run requestAnimationFrame callbacks, then style, layout, paint, composite — but only when the browser decides a frame is due (roughly every 16.67 ms at 60 Hz); otherwise skipped. Step 5: loop back to step 1. The key consequence: microtasks always run before the render step, so they behave as an extension of the running task from the renderer's perspective." },
    { q: "Why does await Promise.resolve() fail to 'yield to the browser' even though it suspends the calling function, and what actually yields?", a: "await is sugar for Promise.then, so awaiting a resolved promise queues the function's resume as a microtask. The microtask checkpoint drains to empty before the loop ever reaches the render step or pulls another task, so the function pauses but the renderer and input never get a turn between the pause and the resume. To genuinely yield you must queue a task: setTimeout(fn, 0) (coarse, 4 ms clamped after nesting depth 5), MessageChannel.postMessage (no clamp — what React 18 uses), or scheduler.yield() (Chrome 115+, resumes the task with preserved priority)." },
    { q: "Name the three throttling layers that compound on setTimeout accuracy, and the one rule that ties them together.", a: "Layer 1: the 4 ms nesting clamp — after 5 levels of setTimeout/setInterval nesting the minimum delay is clamped to 4 ms, so a setTimeout(0) loop runs at most ~250 times per second. Layer 2: background-tab throttling — hidden tabs clamp timers to ≥1 s, then ≥1 min after several minutes hidden, and rAF stops entirely. Layer 3: timer coalescing — fire times are aligned to a shared grid to let the CPU wake once for many timers. The unifying rule: setTimeout(fn, delay) guarantees only a lower bound — the callback fires no sooner than delay, never exactly at it. For sub-frame precision use the AudioContext clock or performance.now() deltas inside a rAF loop." },
    { q: "Describe the microtask-starvation failure mode, how it looks in DevTools, and one production scenario where it appears by accident.", a: "A microtask that schedules another microtask before returning keeps the microtask queue permanently non-empty, so the loop never escapes the step-3 checkpoint to reach rendering or input — the page freezes with zero frames painted and INP over 1 s. In DevTools Performance it shows as a single uninterrupted yellow scripting bar with no dominant function in the flame chart. A production example: a reactive state library that fires an update inside a reaction handler, where the reaction mutates state that re-triggers the reaction — a microtask cycle that never settles. The cure is to insert one task-level yield (setTimeout, MessageChannel, scheduler.postTask) somewhere in the chain so the loop can breathe." },
    { q: "You see an INP regression in production. Walk through the LoAF-to-fix pipeline that turns the metric into a deployable change.", a: "INP only tells you an interaction was slow. Subscribe a PerformanceObserver to long-animation-frame entries: each LoAF entry carries a scripts array with per-script attribution (sourceURL, sourceFunctionName, sourceCharPosition) and the frame's duration. Correlate the LoAF that overlapped the slow interaction, take the heaviest script, and resolve its URL and char position through the production sourcemap to a file:line in the original source. Classify the work (a synchronous Redux reducer filtering a large list, JSON.parse of a big response, derived-state recompute), then fix at the source — memoised selector, Worker offload, stream-parse — and gate it with a CPU-throttled synthetic INP test so the regression cannot return silently." },
    { q: "How does Node's event loop reorder things relative to the browser, and what is the headless equivalent of INP?", a: "Node's libuv loop runs six ordered phases — timers, pending callbacks, idle/prepare, poll (block on I/O), check (setImmediate), close — and between every phase it drains two queues: the Node-specific process.nextTick queue first, then the standard microtask queue. So process.nextTick sits above microtasks, making it the highest-priority async primitive and a starvation footgun if used recursively. setImmediate fires in the check phase of the current iteration; setTimeout(0) fires in the next iteration's timers phase, so inside an I/O callback setImmediate consistently wins. Node has no frames or input, so the equivalent of INP is event-loop lag — schedule a timer for T ms, measure how late it fired; perf_hooks.monitorEventLoopDelay() gives a histogram, and a p99 in the hundreds of ms means a handler is monopolising the single thread." },
  ]}
/>

<Recap lang="en">
If you could reconstruct each answer from memory, you hold the unit's spine: one task runs to completion, then the microtask checkpoint drains fully before any frame, then rendering runs only on a frame boundary. Microtasks never yield to the renderer — only tasks do — which is why self-scheduling microtasks starve the page. Timers are lower bounds throttled by three compounding layers. INP measures the user-perceived cost, LoAF plus sourcemaps attribute it, and Node reruns the whole model with process.nextTick promoted above microtasks and event-loop lag standing in for INP.
</Recap>
