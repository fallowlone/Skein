---
concepts:
- timer-clamp
- background-throttle
- timer-coalescing
- requestIdleCallback
- promise-combinators
- page-lifecycle
deepensInto: []
estMin: 14
lang: en
lessonType: topic
level: middle
order: 3
prereqs:
- 01-loop-model
- 02-queues-and-scheduling
slug: 03-timer-accuracy
sources:
- https://html.spec.whatwg.org/multipage/webappapis.html#timers
- https://developer.mozilla.org/en-US/docs/Web/API/requestIdleCallback
- https://developer.mozilla.org/en-US/docs/Web/API/Page_Visibility_API
spiral:
- latency
status: ready
summary: Why setTimeout(fn, 100) rarely fires at 100 ms — three throttling layers,
  background-tab coalescing, requestIdleCallback, and Promise combinator semantics.
title: Timer accuracy, throttling, and idle work
track: browser
unit: 01-event-loop
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumberDrill from "~/components/pedagogy/NumberDrill.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
Your polling loop drifts. A background tab's `setInterval` fires once a minute instead of every second. A diagnostic timer meant to measure 50 ms measures 120 ms. Timers in the browser are a promise with asterisks, and the asterisks multiply the moment something else is busy.
</Hook>

<Crux>A timer fires no sooner than its delay, but can fire much later — three throttling layers compound on the main thread.</Crux>

<Explanation>

## Why setTimeout is a lower bound, not a guarantee

A `setTimeout(fn, 100)` does not promise to run `fn` in exactly 100 ms — it promises to queue the task **no sooner** than 100 ms. The actual run time depends on when the main thread is next free. If a 300 ms task is in flight when the timer fires, your callback waits the full 300 ms plus its queue position.

Three throttling layers compound this:

**Layer 1 — the 4 ms nesting clamp.** After 5 levels of `setTimeout` nesting (or `setInterval`), browsers clamp the delay to a minimum of 4 ms. This is a spec rule, not a quirk: it prevents `setTimeout(0)` chains from busy-looping the main thread. A 100-iteration loop using `setTimeout(0)` takes at least 400 ms regardless of how fast the work is.

**Layer 2 — background-tab throttling.** When a tab is hidden (`document.visibilityState === 'hidden'`), browsers clamp timers to a 1-second minimum. After the tab has been hidden for several minutes, Chrome further reduces to once per minute. Your background polling loop quietly slows to a crawl.

**Layer 3 — timer alignment / coalescing.** To save battery, browsers round timer fire times to a shared grid so the CPU can wake once for many timers instead of many times. The practical consequence: never use `setTimeout`/`setInterval` for anything needing sub-frame precision. For animation use `requestAnimationFrame`; for precise scheduling use the `AudioContext` clock or `performance.now()` deltas inside a rAF loop.

<FlowDiagram
  label="The life of setTimeout(fn, d): the requested delay is clamped to a minimum (about 4 ms once nested 5 deep), the task is queued, and fn runs only when both the delay has elapsed and the call stack is empty."
  perRow={4}
  hue="var(--d-frontend)"
  nodes={[
    { id: "request", label: "setTimeout(fn, d)", sub: "requested delay" },
    { id: "clamp", label: "Clamp to min", sub: "~4 ms nested" },
    { id: "queue", label: "Queue task", sub: "timer source" },
    { id: "run", label: "Run fn", sub: "stack empty" },
  ]}
  edges={[
    { from: "request", to: "clamp", label: "floor delay" },
    { from: "clamp", to: "queue", label: "after delay" },
    { from: "queue", to: "run", label: "stack empty" },
  ]}
  caption="d is only a lower bound. fn cannot run until BOTH the (clamped) delay has elapsed AND the call stack is empty — a long task in flight pushes the real fire time well past d."
/>

<NumbersCard
  id="card-timer-layers"
  title="Three throttling layers"
  data-lesson-visual
  stats={[
    { label: "Normal minimum delay", value: "0 ms (but queue-position-dependent)" },
    { label: "Nesting clamp (depth ≥5)", value: "4 ms minimum" },
    { label: "Background tab", value: "≥1 000 ms" },
    { label: "Background tab (many minutes)", value: "≥60 000 ms" },
    { label: "Battery-saving coalescing", value: "grid alignment, ~1 ms jitter" },
  ]}
/>

<Quiz
  id="mid-timer-quiz-1"
  pieceSlug="03-timer-accuracy"
  lang="en"
  question="A page calls `setTimeout(loop, 0)` where `loop` re-schedules itself. After 5 nesting levels, what happens to the effective delay?"
  choices={[
    { label: "It is clamped to 4 ms minimum, so the loop runs at most 250 times per second", correct: true },
    { label: "Stays at 0 ms; the clamp is for setInterval, not setTimeout", misconception: "The clamp is for setTimeout (and setInterval) once nesting reaches 5. It is in the HTML spec specifically to prevent setTimeout(0) chains from busy-looping the page." },
    { label: "Becomes negative (the browser's way of marking a starvation pattern)", misconception: "Negative delays are clamped to 0, then to 4 ms after nesting. There is no special negative marking." },
    { label: "Microtasks take over and the timer queue is bypassed", misconception: "setTimeout always queues a task. Microtasks are unrelated to the clamp." },
  ]}
/>

## Page visibility and the event loop lifecycle

The event loop does not stop when a tab goes to the background, but it changes mode. `document.visibilityState` switches to `hidden`, `requestAnimationFrame` stops firing entirely (no drawing = no rAF), and timers throttle as above.

The `visibilitychange` event is the right place to stop polling, pause video, flush unsaved state. The modern replacement for the unreliable `unload` event is `pagehide` plus the Page Lifecycle API with `frozen` and `terminated` states: the browser can fully freeze a background tab (stopping its loop entirely) to free memory, then thaw it on return. Code holding open connections or timers must listen to `freeze`/`resume`, or after thaw it is running with stale state — a closed WebSocket, an expired token, a detached observer.

<Quiz
  id="mid-timer-quiz-2"
  pieceSlug="03-timer-accuracy"
  lang="en"
  question="A background tab runs a 1-second polling setInterval. After 5 minutes in the background, how often does Chrome typically fire the callback?"
  choices={[
    { label: "Once per minute — Chrome clamps background timers to 1-minute minimum after several minutes hidden", correct: true },
    { label: "Once per second — setInterval is exempt from background throttling", misconception: "setInterval is subject to the same background throttling as setTimeout. The 1-second minimum kicks in immediately; the 1-minute minimum applies after a longer period." },
    { label: "Every 4 ms — the nesting clamp overrides background throttle", misconception: "The nesting clamp and background throttle stack: background throttle always wins over the 4 ms clamp in a hidden tab." },
    { label: "Not at all — Chrome suspends all timer callbacks in background tabs", misconception: "Timers do fire in background tabs, but at a throttled rate. They are not suspended entirely." },
  ]}
/>

## requestIdleCallback — scheduling non-urgent work

Not all work is urgent. Analytics beacons, prefetching, cache warming, and log flushing can wait until the loop has nothing better to do. `requestIdleCallback(fn)` queues a callback that the browser runs only when an iteration finishes with spare time before the next frame deadline. The callback receives a `deadline` object whose `timeRemaining()` tells you how many ms you may safely use (capped around 50 ms).

The discipline: do a small slice, check `timeRemaining()`, and if it is near zero, re-schedule the rest with another `requestIdleCallback`. The optional `{ timeout }` parameter forces the callback to run anyway after the deadline so the work is not starved forever.

`requestIdleCallback` is the cooperative opposite of `requestAnimationFrame`: rAF says "run me right before the next paint," ridle says "run me only if nobody else needs the thread." Used together they let you keep urgent work on rAF and defer everything else off the critical path.

<NumberDrill
  id="mid-timer-drill-1"
  pieceSlug="03-timer-accuracy"
  lang="en"
  question="requestIdleCallback's `deadline.timeRemaining()` is capped at roughly this many milliseconds — the same as the long-task threshold."
  expected={50}
  tolerance={0}
  unit="ms"
  explanation="The browser caps idle time at ~50 ms to prevent a single idle callback from monopolising a long idle gap. This matches the long-task threshold: if your idle callback runs for more than 50 ms it becomes a long task from the perspective of the Long Tasks API. The cap means you should always check timeRemaining() and slice your work accordingly."
/>

## Promise combinators and scheduling

When you reach for `Promise.all` on a dashboard, do you know what happens to the other four widgets if one API call throws? The answer depends on which combinator you picked — and the wrong choice is a correctness bug, not a style choice.

`Promise.all`, `Promise.allSettled`, `Promise.race`, and `Promise.any` change **what** you wait for, not how the loop schedules. All four start every passed promise immediately — the parallelism comes from kicking off async operations before awaiting.

- `Promise.all` — rejects on the first failure (fail-fast). Right when any failure makes the whole result useless.
- `Promise.allSettled` — never rejects; resolves with a status array. Right when you want partial results (five independent dashboard widgets where one failing should not blank the others).
- `Promise.race` — settles on the first to settle, success or failure. Classic use: timeout: `Promise.race([fetchData(), rejectAfter(5000)])`.
- `Promise.any` — resolves on the first **success**, ignoring rejections until all fail. Right for redundancy, like racing three CDN mirrors.

Picking the wrong combinator is a correctness bug, not a perf bug: `Promise.all` for a dashboard means one slow widget's failure blanks the page.

A common bug worth naming: writing `const a = await fetchA(); const b = await fetchB();` when both fetches are independent. The two requests run sequentially because the second `await` does not start until the first resolves. `const [a, b] = await Promise.all([fetchA(), fetchB()])` kicks off both immediately. The cost difference is one full RTT. The clue in DevTools is two network requests with non-overlapping timing where both could have run in parallel.

<Quiz
  id="mid-timer-quiz-3"
  pieceSlug="03-timer-accuracy"
  lang="en"
  question="You have five independent API calls for a dashboard. One of them may fail. Which combinator fits?"
  choices={[
    { label: "Promise.allSettled — it never rejects; you get the result or error for each call independently", correct: true },
    { label: "Promise.all — fail-fast if any call fails, show error state", misconception: "Promise.all rejects on the first failure, which would blank the entire dashboard. allSettled lets you render the four successful widgets even if one fails." },
    { label: "Promise.race — settle as soon as the fastest call resolves", misconception: "Promise.race settles on the first call to settle, success or failure. You would only get one result, not all five." },
    { label: "Promise.any — settle on the first success", misconception: "Promise.any resolves on the first success and ignores the rest. You would only get one result." },
  ]}
/>

<Quiz
  id="mid-timer-quiz-4"
  pieceSlug="03-timer-accuracy"
  lang="en"
  question="A background tab's `visibilitychange` event fires. The code holds an open WebSocket and a `setInterval` polling loop. What should it do?"
  choices={[
    { label: "Pause the polling loop; keep the WebSocket open but stop sending keep-alives; resume on `visibilitychange` back to visible", correct: true },
    { label: "Close the WebSocket immediately to save resources", misconception: "Closing the WebSocket means a full reconnect when the user returns. Keeping it open (without excessive keep-alives) is cheaper than reconnecting." },
    { label: "Do nothing — the browser handles background throttling automatically", misconception: "The browser throttles timers but does not stop your polling logic from running. The WebSocket will keep-aliving, draining battery and server connections. You must explicitly pause." },
    { label: "Use the `freeze` event instead — visibilitychange is unreliable", misconception: "visibilitychange is reliable and fires when a tab goes to background. freeze fires only when the browser fully freezes the tab — a stronger signal that happens later, if at all." },
  ]}
/>

</Explanation>

<KeyTakeaway>Timers fire no sooner than their delay but can fire much later. Three throttling layers — nesting clamp (4 ms), background-tab throttle (1 s → 1 min), and coalescing — compound on each other. For sub-frame precision, use AudioContext or rAF; for non-urgent work, use requestIdleCallback.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-timer-accuracy-retrieval"
  lang="en"
  questions={[
    { q: "Name the three throttling layers that compound on setTimeout accuracy.", a: "1. Nesting clamp: after 5 levels of setTimeout/setInterval nesting, the minimum delay is clamped to 4 ms. 2. Background-tab throttling: hidden tabs get timers clamped to ≥1 s, eventually ≥1 min. 3. Timer coalescing: browsers align timer fire times to a shared grid to reduce CPU wake-ups and save battery." },
    { q: "When should you use requestIdleCallback instead of setTimeout(fn, 0)?", a: "When the work is truly non-urgent and can wait for genuine idle time — analytics, prefetch, cache warming. requestIdleCallback only runs when the loop has spare capacity before the next frame deadline, and timeRemaining() tells you how many ms you have. setTimeout(fn, 0) runs as soon as the main thread is free regardless of frame budget, which can still interfere with rendering." },
    { q: "Why is `const a = await fetchA(); const b = await fetchB();` a bug when both fetches are independent?", a: "Sequential await means fetchB does not start until fetchA resolves. If each fetch takes one RTT, you wait two RTTs total. Promise.all([fetchA(), fetchB()]) kicks both off immediately and waits for both, spending only one RTT. In DevTools the bug is visible as two non-overlapping network bars." },
  ]}
/>

<Recap lang="en">
`setTimeout(fn, delay)` queues the task no sooner than `delay` milliseconds, but the actual fire time depends on main-thread availability and three throttling layers: the 4 ms nesting clamp (prevents setTimeout(0) busy-loops), background-tab throttling (1 s → 1 min), and timer coalescing for battery savings. For precise timing in animations, use `requestAnimationFrame` or the `AudioContext` clock — both are driven by the rendering pipeline, not the timer queue. For non-urgent work, `requestIdleCallback` runs only during genuine idle gaps and gives you a `timeRemaining()` budget to slice work safely. Promise combinators do not change loop scheduling but do change correctness: `Promise.all` for fail-fast, `Promise.allSettled` for partial results, `Promise.race` for timeouts, `Promise.any` for first-success redundancy. Now when you see a polling loop drift or a background-tab feature that stops working, you know which throttling layer to blame — and how to handle it cleanly with `visibilitychange`.
</Recap>
