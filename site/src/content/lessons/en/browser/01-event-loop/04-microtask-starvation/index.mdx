---
concepts:
- microtask-starvation
- PerformanceLongTaskTiming
- LoAF
- scheduler-yield
- scheduler-postTask
- INP
- sourcemap
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: senior
order: 4
prereqs:
- 01-loop-model
- 02-queues-and-scheduling
slug: 04-microtask-starvation
sources:
- https://html.spec.whatwg.org/multipage/webappapis.html#event-loops
- https://developer.mozilla.org/en-US/docs/Web/API/PerformanceLongAnimationFrameTiming
- https://developer.chrome.com/blog/introducing-scheduler-yield-origin-trial
- https://web.dev/articles/inp
spiral:
- statefulness
status: ready
summary: The starvation failure mode, how to detect it in production with PerformanceLongTaskTiming
  and LoAF, and scheduler.yield() / scheduler.postTask() as the cure.
title: Microtask starvation, Long Tasks, and LoAF
track: browser
unit: 01-event-loop
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import RFCQuiz from "~/components/pedagogy/RFCQuiz.astro";
import DesignPrompt from "~/components/pedagogy/DesignPrompt.astro";
import DebugLog from "~/components/pedagogy/DebugLog.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";
import Pulse from "~/components/diagram/Pulse.astro";

<Hook>
A user reports the page "froze completely" for several seconds. DevTools Performance shows one uninterrupted yellow scripting bar. No function call dominates the flame chart — the work keeps re-scheduling itself as microtasks and the loop never escapes.
</Hook>

<Crux>Microtasks that schedule microtasks block the render step — the page freezes until the chain breaks.</Crux>

<Explanation>

## Failure mode: microtask starvation

Microtasks drain to empty between tasks. If a microtask schedules another microtask before returning, the loop never escapes the microtask checkpoint — no rendering, no input, no further tasks. The pathological example:

```javascript
function loop() {
  Promise.resolve().then(loop);  // schedules itself as a microtask
}
loop();
```

This freezes the page indefinitely: the microtask queue is never empty, step 4 (rendering) never runs, the user sees a frozen tab. DevTools Performance shows it as a single uninterrupted yellow scripting bar. The same pattern appears in production as accidental recursion through async chains: a `.then` that resubscribes to the same observable, a router middleware that re-resolves on every change, a state library that re-fires reactions inside a reaction.

**Detection:** long-task entries with no clear single function, scripting bar that grows without bound, INP &gt; 1 s.

**Cure:** insert at least one task-level yield (`setTimeout`, `MessageChannel`, `scheduler.postTask`) into the chain.

<FlowDiagram
  label="The microtask starvation cycle: running a microtask enqueues another microtask, so the microtask queue stays non-empty and the loop loops back to run it again — never reaching the macrotask or render step, which are starved."
  perRow={3}
  hue="var(--d-frontend)"
  nodes={[
    { id: "run", label: "Run microtask", sub: "checkpoint" },
    { id: "enqueue", label: "Enqueue microtask", sub: ".then(loop)" },
    { id: "nonempty", label: "Queue non-empty", sub: "drain again" },
    { id: "starved", label: "Macrotask + Render", sub: "STARVED — never" },
  ]}
  edges={[
    { from: "run", to: "enqueue", label: "schedules self" },
    { from: "enqueue", to: "nonempty", label: "queued" },
    { from: "nonempty", to: "run", label: "loop back" },
    { from: "nonempty", to: "starved", label: "never reached" },
  ]}
  caption="The three top nodes form a closed loop: run → enqueue → non-empty → run. Because the microtask queue is never empty, the loop never exits the checkpoint, so the macrotask and render step (bottom) are never reached."
/>

The hot path is the self-loop: <Pulse>run → enqueue → non-empty → run</Pulse>. While it spins, zero frames paint.

<NumbersCard
  id="card-starvation-signals"
  title="Microtask starvation signals in DevTools"
  data-lesson-visual
  stats={[
    { label: "Yellow scripting bar", value: "continuous, never breaks" },
    { label: "Long Tasks API entries", value: "duration grows without bound" },
    { label: "INP", value: "> 1 000 ms" },
    { label: "Frame counter", value: "0 fps during starvation" },
    { label: "Fix", value: "insert a task-level yield in the chain" },
  ]}
/>

<Quiz
  id="sr-starvation-quiz-1"
  pieceSlug="04-microtask-starvation"
  lang="en"
  question="A `MutationObserver` callback fires synchronously after a DOM mutation. What kind of work is it queued as?"
  choices={[
    { label: "Microtask — fires at the next microtask checkpoint, before the next task or frame", correct: true },
    { label: "Task on the DOM-manipulation task source", misconception: "MutationObserver is explicitly defined to use the microtask queue precisely so that observers see all mutations from a synchronous block at once, not interleaved with rendering." },
    { label: "Synchronous — fires the moment the mutation completes", misconception: "Synchronous would defeat the batching purpose of MutationObserver. It accumulates records, then fires once at the next microtask checkpoint." },
    { label: "Task on the user-interaction task source", misconception: "Programmatic DOM mutations have nothing to do with user interaction. The queue is microtask, regardless of who triggered the mutation." },
  ]}
/>

## Long Tasks API and Long Animation Frames

You've confirmed there's a starvation problem — now how do you find it in a production bundle you didn't write, on a device you don't own? That's what these two APIs are for: they give you the observability layer that DevTools can't provide in the field.

**PerformanceLongTaskTiming.** Shipped in 2017, fires a `PerformanceObserver` entry for any task that exceeds 50 ms on the main thread. Each entry includes start time, duration, and an `attribution` array with the originating browsing context. Limitation: the attribution rarely points to a specific function; it tells you "something inside iframe A took 230 ms" — useful for SLO dashboards, less useful for root-cause. Use the Long Tasks API to **count** long tasks per session, not to debug them.

**PerformanceLongAnimationFrameTiming (LoAF).** Shipped 2023–2024 in Chromium. Fires for any frame whose render time exceeds 50 ms. A LoAF entry includes per-script attribution: an array of which scripts ran in the frame, what they did (event-handler, classic-script, module-script, user-callback), how long each took. This is the production diagnostic that long tasks should have been from the start. Combine with INP: when INP regresses, query LoAF entries from the same session, find the offending frame, get the script attribution, deploy a fix. The full pipeline: from user-perceived metric (INP) to specific script (LoAF) to specific function (sourcemap on the script URL).

<DebugLog
  id="sr-debug-1"
  pieceSlug="04-microtask-starvation"
  lang="en"
  label="DevTools Performance — long-task summary"
  output={`[Long Task] 312 ms
  attribution: same-origin
  startTime: 2456.3
  duration: 312.4

[Long Task] 287 ms
  attribution: same-origin
  startTime: 5102.7
  duration: 287.1

[INP candidate] 410 ms
  type: pointerdown -> click -> next paint
  startTime: 2401.0
  processingStart: 2456.3
  processingEnd: 2768.7
  presentationTime: 2811.0
  attribution: handleSearch (search.js:142)`}
  outputLang="log"
  question="A search input shows 410 ms INP and the long-task log points to handleSearch. The handler does: validate query → call setSearchTerm (Redux) → trigger debounced fetch → wait for results. Where is the 312 ms?"
  hint="The input event timestamp is 2401, processingStart is 2456, processingEnd is 2768. processingEnd - processingStart is the long task. What runs between input and processingStart?"
  answer="The 55 ms between input timestamp (2401) and processingStart (2456) is the wait for the main thread to free up — another task held it. Then 312 ms (processingStart to processingEnd) is the handler itself. The 42 ms between processingEnd and presentationTime (2811) is the rendering work that follows. The 312 ms is almost certainly a synchronous Redux reducer running on the full state tree (a common React anti-pattern: deriving large filtered lists in the reducer instead of in a memoised selector). Fix: move the filter into a selector with reselect/createSelector so it only recomputes when its inputs change, and run the heavy filter on a Worker if the dataset is large enough that even an optimised selector exceeds 50 ms."
/>

## scheduler.yield() and the Scheduler API

**What scheduler.yield() does.** The Scheduler API (Chrome 115+, partial in Edge, Firefox/Safari behind flag as of late 2025) gives the platform a first-class yield primitive. Awaiting `scheduler.yield()` suspends the current task, drains the microtask queue, allows input and rendering to run, and resumes the suspended task as the **next** task — with priority that prevents low-priority work from cutting in front. Previously, yielding via `setTimeout(0)` worked for rendering but lost queue position (any task scheduled in the meantime would run first). With `scheduler.yield()`, you can split a 200 ms task into four 50 ms chunks that are still effectively one logical operation from the user's perspective.

**scheduler.postTask() with priorities.** The same API exposes `scheduler.postTask(callback, { priority })` with three priorities: `user-blocking` (input-response work), `user-visible` (default), `background` (non-urgent). Practical pattern: route input handlers through `user-blocking` so they jump the queue ahead of background work like analytics flushes; route background analytics through `background` so they yield to anything urgent. The scheduler has access to system signals (battery, thermal throttling, page visibility) that JS code does not.

<FadedExample
  client:visible
  id="sr-faded-1"
  pieceSlug="04-microtask-starvation"
  lang="en"
  title="Yield a long task without losing logical continuity"
  steps={{
    solved: (
      <pre><code>{`async function processItems(items) {
  let lastYield = performance.now();
  const slice = 50; // ms target chunk size
  for (const item of items) {
    doWork(item);
    if (performance.now() - lastYield > slice) {
      // Yields to the renderer and to input, then resumes
      await (scheduler.yield?.() ?? new Promise(r => setTimeout(r, 0)));
      lastYield = performance.now();
    }
  }
}`}</code></pre>
    ),
    semi: {
      prompt: (
        <p>Fill in the missing condition and the missing yield expression to chunk this work into 50 ms slices.</p>
      ),
      blanks: [
        { id: "condition", expected: /performance\.now/i, placeholder: "JS expression for elapsed-time check" },
        { id: "yield", expected: /scheduler\.yield|setTimeout/i, placeholder: "yield primitive" },
      ],
    },
    blank: {
      prompt: (
        <p>You have a function that processes 10 000 items and takes 500 ms — this regresses INP whenever a user types in the same page. Rewrite it so that no single task on the loop exceeds 50 ms. What is the structural change?</p>
      ),
      reveal: (
        <p>Track elapsed time inside the loop. After each ~50 ms of work, yield via <code>scheduler.yield()</code> (or <code>setTimeout(0)</code> as fallback) before continuing. The work is now spread across ~10 tasks of ~50 ms each, with rendering and input interleaved between them. INP returns to acceptable levels because input handlers can run between chunks. The cost is overall wall-clock — a 500 ms job becomes a ~600 ms job — but the user perceives it as smooth because every interaction is acknowledged within one frame.</p>
      ),
    },
  }}
/>

<RFCQuiz
  id="sr-rfc-1"
  pieceSlug="04-microtask-starvation"
  lang="en"
  question="Which specification defines the event loop, task queues, microtask queue, and the rendering steps in step-by-step detail?"
  choices={[
    { rfc: "HTML Living Standard § event loops", title: "Defines event loop, task sources, microtask checkpoint, update-the-rendering algorithm", correct: true },
    { rfc: "ECMA-262 (JavaScript)", title: "Defines microtasks (jobs queue) but not tasks, rendering, or browser-specific scheduling" },
    { rfc: "WHATWG Fetch", title: "Defines fetch and CORS, not the event loop" },
    { rfc: "W3C DOM", title: "Defines DOM tree and events, but the loop processing model lives in HTML" },
  ]}
/>

<DesignPrompt
  id="sr-design-1"
  pieceSlug="04-microtask-starvation"
  lang="en"
  prompt="Design the input pipeline for a search bar that filters 50 000 client-side items and must keep INP under 200 ms p75 on mid-range Android."
  constraints={[
    "Frame budget: ~10 ms after browser overhead.",
    "INP target: ≤200 ms p75.",
    "No long tasks > 50 ms on the main thread during typing.",
    "Filter results must update visibly within 200 ms of the most recent keystroke.",
    "Off-screen results may render lazily but on-screen results must be correct.",
    "Browser support: Chrome, Safari, Firefox (no Worker fallback drama).",
  ]}
  canonical="The pipeline runs as: keypress → debounce 100 ms → schedule filter via scheduler.postTask({ priority: 'user-visible' }) → filter runs in chunks of 5 000 items per task, yielding via scheduler.yield() between chunks → after each chunk, dispatch a partial-result update to React via useTransition (low priority) → React reconciles in 5 ms slices, never blocking input. The filter algorithm itself is precomputed: build a lowercased index once at load, then per-keypress do indexOf substring checks (O(N) but cache-friendly). For datasets > 50K, move the filter into a Worker: keypress on main → postMessage to worker → worker filters → postMessage results back → main thread renders. Worker pays one structured-clone cost per result batch (~5 ms for 1K items) but main thread stays free. Verify with DevTools: type fast, confirm no long tasks > 50 ms, INP < 200 ms p75, and the input field always responds within one frame. Add LoAF entries in production telemetry for regression detection."
  keyPoints={[
    "Debounce keystrokes to coalesce work.",
    "Chunk the filter into ≤50 ms tasks with scheduler.yield() between.",
    "Use useTransition so React reconciliation does not block input.",
    "For very large datasets, push the filter onto a Worker — main thread becomes free for input/rendering.",
    "Wire LoAF telemetry so regressions are caught in production, not local dev.",
  ]}
/>

<Quiz
  id="sr-quiz-2"
  pieceSlug="04-microtask-starvation"
  lang="en"
  question="LoAF (PerformanceLongAnimationFrameTiming) differs from PerformanceLongTaskTiming in what key way?"
  choices={[
    { label: "LoAF fires per frame (render time > 50 ms) and includes per-script attribution; Long Tasks fires per task (execution time > 50 ms) with only browsing-context attribution", correct: true },
    { label: "LoAF fires per task; Long Tasks fires per frame — they are inverses", misconception: "It is the opposite. Long Tasks = per task exceeding 50 ms execution; LoAF = per frame exceeding 50 ms render time. LoAF is the newer, more useful signal because it gives script-level attribution." },
    { label: "They are identical — LoAF replaced Long Tasks with the same semantics", misconception: "LoAF has richer attribution (per-script) and fires on frame render time, not task execution time. Long Tasks still exists and is useful for counting, but LoAF is the better debugging tool." },
    { label: "LoAF only fires on frames caused by user input; Long Tasks fires on all tasks", misconception: "LoAF fires for any frame whose render time exceeds 50 ms, regardless of cause. Interaction is only one trigger." },
  ]}
/>

</Explanation>

<KeyTakeaway>A microtask that schedules another microtask before returning permanently starves the render step. In production, LoAF gives script-level attribution to INP regressions; scheduler.yield() breaks long tasks into chunks that let input and rendering run between them.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-microtask-starvation-retrieval"
  lang="en"
  questions={[
    { q: "A search input shows 410 ms INP. LoAF reports a 312 ms script attributed to handleSearch. Walk through how to root-cause this from telemetry to fix.", a: "Step 1: confirm the LoAF entry's scripts array — find the entry with the largest duration and read its sourceURL, sourceFunctionName, and sourceCharPosition. Resolve the position through the production sourcemap to get a file:line in the original source. Step 2: classify the work — common heavy operations are synchronous Redux reducers filtering large datasets, synchronous derived-state computation, JSON.parse of large responses, hash-map rebuilds. Step 3: pick the right fix — for dataset filtering, switch to memoised selectors (reselect) or move to a Worker; for derived state, hoist the computation out of render and memoise; for JSON.parse, stream-parse or compress server-side. Step 4: deploy with INP-as-CI-gate — enforce p75 INP budget on a synthetic test that reproduces the typing sequence." },
    { q: "What is the difference between scheduler.yield() and await Promise.resolve() as yield mechanisms?", a: "scheduler.yield() queues a task — the suspended function resumes after the browser has run rendering and input handlers. await Promise.resolve() queues a microtask — the function resumes before any rendering or input, making it invisible to the render loop. From the user's perspective, scheduler.yield() actually yields; Promise.resolve() does not." },
    { q: "Describe the microtask starvation pattern and give one production scenario where it appears accidentally.", a: "The pattern: a microtask schedules another microtask before returning, creating an infinite chain that holds the microtask checkpoint open forever. The loop never reaches step 4 (rendering). One production scenario: a state management library fires reactive updates inside a reaction handler. If the reaction modifies state that triggers another reaction, and that reaction modifies the original state, you get a cycle of microtasks that never resolves, freezing the page. Detection: continuous yellow scripting bar in DevTools Performance, INP > 1 s, zero frames painted during the block." },
  ]}
/>

<Recap lang="en">
Microtask starvation occurs when a microtask enqueues another microtask before returning — the loop is trapped in the microtask checkpoint and never reaches the render or input steps. It shows in DevTools as a single unbroken yellow bar and in production as INP &gt; 1 s. The Long Tasks API (PerformanceLongTaskTiming, 2017) counts tasks exceeding 50 ms but gives only browsing-context attribution. LoAF (PerformanceLongAnimationFrameTiming, 2023) fires per frame and gives per-script attribution, making it the correct tool for root-causing INP regressions in production. The Scheduler API's `scheduler.yield()` (Chrome 115+) provides a structured task-level yield that preserves queue priority, while `scheduler.postTask()` lets you assign `user-blocking`, `user-visible`, or `background` priority to work so the browser's own scheduler — which has access to thermal and battery signals — can make better decisions than any hand-rolled priority queue. Now when you see that unbroken yellow scripting bar, you know exactly what to look for — and the LoAF pipeline to go from metric to file:line to deploy.
</Recap>
