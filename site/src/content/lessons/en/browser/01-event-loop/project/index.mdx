---
concepts:
- inp-diagnosis
- task-chunking
- scheduler-yield
- loaf-telemetry
- inp-ci-gate
estMin: 240
lang: en
level: senior
order: 10
prereqs: []
slug: project
sources:
- https://web.dev/articles/inp
- https://developer.mozilla.org/en-US/docs/Web/API/PerformanceLongAnimationFrameTiming
- https://developer.chrome.com/blog/introducing-scheduler-yield-origin-trial
status: ready
summary: "Hands-on project — drive a real interaction into a long task, diagnose it with LoAF, chunk it under the 50 ms bar, and prove the INP fix with before/after numbers and a CI gate."
title: 'Event loop: rescue an interaction from INP hell'
track: browser
unit: 01-event-loop
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import ProjectBrief from "~/components/pedagogy/ProjectBrief.astro";

<Hook>
Reading about long tasks is not the same as pulling an interaction out of one. Build a page with a deliberately janky interaction, measure its INP under realistic throttling, diagnose the offending frame with LoAF, and apply the unit's yield discipline until input responds within one frame — with evidence at every step.
</Hook>

<Goal lang="en">
Turn the unit's timing model into a reproducible engineering loop: reproduce a long task, attribute it from telemetry, chunk it under the 50 ms bar with a real task-level yield, defend the win with a CI gate, and verify with before/after INP numbers.
</Goal>

<ProjectBrief
  id="01-event-loop-project"
  lessonSlug="01-event-loop"
  lang="en"
  objective="Take an interaction that blocks the main thread (your own app or the starter below) and bring its INP under 200 ms p75 on throttled hardware — without removing the feature — proving each step with measurements, not estimates."
  requirements={[
    "Build or take a page with one heavy interaction: a search box that synchronously filters and renders 50 000+ client-side items per keystroke, or a sort/group control that recomputes a large derived list on click. The handler must produce a task over 50 ms on throttled hardware.",
    "Reproduce the long task with DevTools Performance under 4x CPU throttling: capture the flame chart, confirm a single task exceeds 50 ms, and note that input during the task is delayed until it returns.",
    "Wire LoAF telemetry: a PerformanceObserver on long-animation-frame entries that records frame duration and the heaviest script's sourceURL, sourceFunctionName, and sourceCharPosition. Log the entry that overlaps the slow interaction.",
    "Measure baseline INP: use the web-vitals library (or a PerformanceObserver on event/first-input) to capture p75 INP for the interaction under throttling. Record the number.",
    "Apply the yield discipline: chunk the handler into slices that each stay under 50 ms, inserting a real task-level yield between them — scheduler.yield() with a setTimeout(0) or MessageChannel fallback. Do NOT use await Promise.resolve(); show in a trace that a microtask yield does not let input run.",
    "If the work is CPU-bound and large, move it to a Worker: keystroke on main → postMessage to worker → worker filters → postMessage results back → main renders. Use transferable buffers where the payload is a typed array to avoid the structured-clone copy.",
    "Re-measure under the identical throttled load and record after numbers next to before: INP p75, longest task duration, and frames painted during typing.",
  ]}
  acceptance={[
    "A before/after table: INP p75, longest main-thread task (ms), and dropped-frame count during a fixed typing sequence — all measured under 4x CPU throttling, not estimated.",
    "No single main-thread task exceeds 50 ms in the after trace; the LoAF stream no longer reports a slow frame for the interaction.",
    "INP p75 holds under 200 ms for the interaction at sustained typing speed.",
    "A one-paragraph write-up naming which yield primitive you used and why a microtask yield (await Promise.resolve()) would not have worked, referencing your trace.",
  ]}
  stretch={[
    "Add an INP CI gate: a Playwright test in headless Chrome with --cpu-throttling-rate=4 that replays the typing sequence, measures INP via the same PerformanceObserver code, and fails the build if p75 crosses the budget. Show it catches a regression you intentionally reintroduce.",
    "Add a starvation guard: write the classic Promise.resolve().then(self) freeze, observe zero frames painted and INP over 1 s in a trace, then break the cycle with one task-level yield and show the page recovers.",
    "Port the same heavy handler to a Node HTTP endpoint, measure event-loop lag with perf_hooks.monitorEventLoopDelay() under load, and show the Worker-thread fix drops p99 lag the same way the browser fix dropped INP.",
    "Wire the LoAF sourceCharPosition through a real sourcemap (source-map library or a Sentry-style resolver) so telemetry reports the original file:line, not the bundle position.",
  ]}
/>

<Recap lang="en">
This is the loop you will run in every real INP incident: reproduce the long task under throttling, attribute the slow frame with LoAF plus a sourcemap, chunk the work under the 50 ms bar with a genuine task-level yield (never a microtask), push CPU-bound work to a Worker when chunking is not enough, and verify with before/after INP numbers under identical load. Gate it in CI with the same throttling so the win cannot silently regress. Doing it once on a toy page makes the production version muscle memory.
</Recap>
