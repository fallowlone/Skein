---
concepts:
- LCP
- preload-scanner
- fetchpriority
- TTFB
- resource-load-delay
- resource-load-time
- render-delay
- lazy-loading
deepensInto:
- 06-tradeoffs-and-observability
estMin: 14
lang: en
lessonType: topic
level: middle
order: 2
prereqs:
- 01-what-vitals-measure
slug: 02-lcp-mechanics
sources:
- https://web.dev/articles/lcp
- https://web.dev/articles/optimize-lcp
- https://developer.chrome.com/docs/devtools/performance
spiral:
- latency
status: ready
summary: Largest Contentful Paint decomposes into TTFB, resource load delay, resource
  load time, and render delay — diagnosing and fixing the right phase is the whole
  game.
title: 'LCP: four phases, one dominant cost'
track: browser
unit: 07-core-web-vitals
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A team adds a CDN, compresses the hero image, and LCP barely moves. The problem was never the image size — the browser was discovering the image late, after a JavaScript bundle ran. The fix was one HTML attribute. LCP fails in four different ways, and only one fix works per phase.
</Hook>

<Crux>LCP splits into four phases — TTFB, load delay, load time, render delay — and a fix only works if it targets the dominant one.</Crux>

<Explanation>

**What LCP measures and when it fires.**

When you diagnose a slow LCP, the first question is not "how do I fix it?" — it is "which of the four phases is slow?" Without that, every fix is a guess. Largest Contentful Paint is the render time of the largest content element visible in the viewport during load. "Content" is a specific list: `<img>`, the poster of a `<video>`, a background image loaded via `url()`, and block-level elements containing text. The browser watches as elements paint, keeps updating its candidate for "largest", and finalises the LCP value at the first user interaction (because once the user is interacting, further "loading" is not load anymore). On most pages the LCP element is the hero image or the main headline — the thing the user perceives as "the page has loaded."

**The four phases.**

LCP time is the sum of four sequential sub-parts. The fix is different for each.

1. **TTFB** — time to the first byte from the server. Causes: slow SSR data fetch, no CDN, slow database query on the render path. Fix: cache, CDN, move slow data off the render path (streaming SSR with a Suspense boundary).

2. **Resource load delay** — the gap between TTFB and the browser *starting* to load the LCP image. Cause: the browser discovers the image late because it is referenced only in CSS or injected by JavaScript, not in the initial HTML — the preload scanner cannot find it. Fix: put the LCP image in the initial HTML as a plain `<img>` with `fetchpriority="high"` or a `<link rel="preload">`.

3. **Resource load time** — how long the LCP image itself takes to download. Cause: oversized file, wrong format, not compressed. Fix: correct format (AVIF, WebP), correctly sized for the viewport via `srcset`, compressed.

4. **Element render delay** — the gap between the image finishing download and it actually painting. Cause: render-blocking CSS or JavaScript still occupying the main thread after the image is ready. Fix: inline critical CSS, defer non-critical scripts.

Together, these four phases are a decision tree: measure each, identify the dominant one, and apply only that fix. Compressing the image when the bottleneck is load delay — or adding a preload when the bottleneck is file size — wastes a sprint and leaves LCP unchanged.

<NumbersCard
  id="card-lcp-phases"
  title="LCP four-phase breakdown"
  stats={[
    { label: "TTFB", value: "Server + CDN + DB" },
    { label: "Resource load delay", value: "Discovery gap in HTML/CSS/JS" },
    { label: "Resource load time", value: "File size × connection speed" },
    { label: "Element render delay", value: "Render-blocking resources" },
  ]}
  data-lesson-visual
/>

<FlowDiagram
  label="LCP time is the sum of four sequential phases in fixed order: TTFB, then resource load delay, then resource load time, then element render delay."
  perRow={4}
  hue="var(--d-frontend)"
  nodes={[
    { id: "ttfb", label: "TTFB", sub: "server byte" },
    { id: "delay", label: "Load delay", sub: "discovery" },
    { id: "load", label: "Load time", sub: "download" },
    { id: "render", label: "Render delay", sub: "paint" },
  ]}
  edges={[
    { from: "ttfb", to: "delay" },
    { from: "delay", to: "load" },
    { from: "load", to: "render" },
  ]}
  caption="The four phases run in this order, and a fix only works if it targets the dominant one: TTFB → server/CDN; load delay → HTML placement + fetchpriority; load time → format + size; render delay → remove blocking resources."
/>

**The preload scanner — why discovery is everything.**

Browsers run a secondary parser called the preload scanner that races ahead of the main HTML parser looking for resources to fetch, even while the main parser is blocked on a script. The preload scanner is why an `<img>` in the initial HTML loads early: the scanner sees it and kicks off the fetch immediately. It is also why an image referenced only in CSS (`background-image`) or injected by JavaScript loads late: the scanner cannot see it — the image is not discovered until the CSS is parsed or the JS runs.

This is the deep reason "put the LCP image in the HTML" works: it is the only place the preload scanner can find it without waiting. `fetchpriority="high"` then tells the browser to raise that fetch above other early resources; `<link rel="preload">` is the heavier hammer for resources the scanner still cannot reach.

**The most common self-inflicted LCP regression.**

Adding `loading="lazy"` to the hero image. Lazy loading exists to defer images that are below the fold — images the user has not scrolled to yet. Applying it to the hero tells the browser to delay loading exactly the image the LCP metric is timing. The result is almost always a large resource load delay that did not exist before. Never lazy-load the LCP element.

<Inset kind="why" lang="en">
A common mistake is compressing the image when the real problem is load delay. If resource load delay is the dominant phase (the browser is discovering the image late), then even a 10× smaller file does not move LCP much — the delay before the download starts is still the bottleneck. The discipline is to read the phase split first (available in DevTools or from the web-vitals library's LCP attribution), then fix the dominant phase. Applying the wrong fix wastes effort and leaves the number poor.
</Inset>

<Quiz
  id="mid-quiz-1"
  pieceSlug="02-lcp-mechanics"
  lang="en"
  question="A hero image is the LCP element and LCP is poor. The image has loading='lazy'. What is the likely problem?"
  choices={[
    { label: "`loading=\"lazy\"` tells the browser to delay loading the very image the LCP metric is timing — the hero must load eagerly", correct: true },
    { label: "Lazy loading makes the image render faster once it appears", misconception: "Lazy loading delays the start of the load; it does nothing to render speed. On the LCP element it directly delays the metric." },
    { label: "Lazy loading is required for good LCP", misconception: "The opposite — lazy-loading the LCP element is a classic LCP regression. Lazy-load below-the-fold images, never the hero." },
    { label: "The problem is unrelated to the lazy attribute", misconception: "It is very much related: `loading=\"lazy\"` on the LCP image adds load delay to exactly the element being measured." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="02-lcp-mechanics"
  lang="en"
  question="LCP time is dominated by 'resource load delay' — the gap between TTFB and the image starting to download. Which fix targets that phase?"
  choices={[
    { label: "Put the image in the initial HTML as a plain `<img>` with `fetchpriority=\"high\"` so the preload scanner finds it immediately", correct: true },
    { label: "Compress the image to a smaller file", misconception: "Compression reduces resource LOAD TIME, not load DELAY. If the delay is the dominant phase, a smaller file does not fix it — the browser still discovers it late." },
    { label: "Add a faster CDN", misconception: "A CDN improves TTFB and load time, not the discovery delay. If the image is referenced late in CSS or JS, the CDN cannot help the browser find it sooner." },
    { label: "Inline critical CSS", misconception: "Inlining CSS targets render delay (render-blocking resources), not the resource load delay phase. Wrong phase." },
  ]}
/>

<DragOrder
  id="mid-drag-1"
  pieceSlug="02-lcp-mechanics"
  lang="en"
  prompt="Order the four phases of LCP time, from the request leaving the browser to the LCP element painting."
  items={[
    { id: "ttfb", label: "TTFB — server produces and sends the first byte" },
    { id: "delay", label: "Resource load delay — browser discovers and starts loading the LCP image" },
    { id: "load", label: "Resource load time — the LCP image downloads" },
    { id: "render", label: "Element render delay — the image paints once render-blocking work clears" },
  ]}
  correctOrder={["ttfb", "delay", "load", "render"]}
/>

</Explanation>

<KeyTakeaway>LCP's four phases each have a different fix: TTFB (server/CDN), load delay (HTML placement + fetchpriority), load time (format + size), render delay (remove blocking resources). Diagnose the dominant phase before applying any fix.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="02-lcp-mechanics-retrieval"
  lang="en"
  questions={[
    { q: "Name the four phases of LCP time and what causes each one to be slow.", a: "TTFB: slow server, missing CDN, slow DB on render path. Resource load delay: image discovered late (in CSS/JS, not initial HTML) — preload scanner cannot find it. Resource load time: oversized or wrong-format image. Element render delay: render-blocking CSS or JS still running after image finishes downloading." },
    { q: "Why does putting the LCP image in the initial HTML help LCP, and what does fetchpriority='high' add?", a: "The browser's preload scanner races ahead of the main parser looking for resources to fetch. An <img> in the initial HTML is visible to the scanner immediately, so the fetch starts before any blocking scripts run. fetchpriority='high' elevates that fetch above other competing early resources (stylesheets, other images)." },
    { q: "A team measures the LCP phase split and finds load delay is 80 ms, but resource load time is 3.5 s. Which phase should they fix first and how?", a: "Resource load time dominates — the image file is too large. Fix: serve in AVIF or WebP format (30–50% smaller than JPEG at equal quality), correctly sized for the displayed dimensions via srcset. Load delay is already small, so adding a preload hint would not move LCP materially." },
  ]}
/>

<Recap lang="en">
LCP measures the render time of the largest content element in the viewport, and it decomposes into four sequential phases: TTFB, resource load delay, resource load time, and element render delay. A fix only works if it targets the dominant phase — compressing the image when load delay is the bottleneck does nothing. The preload scanner is why image placement in the initial HTML matters: only HTML-embedded images are visible to the scanner before blocking scripts run. Never apply `loading="lazy"` to the LCP element; it adds load delay to the exact element being measured. The full phase split is available from the web-vitals library's LCP attribution or from a DevTools performance trace, and reading it before applying any fix is the discipline that separates fast iteration from wasted effort. Now when you see poor LCP in the field, your first move is to pull the four-phase breakdown — not reach for a compression tool.
</Recap>
