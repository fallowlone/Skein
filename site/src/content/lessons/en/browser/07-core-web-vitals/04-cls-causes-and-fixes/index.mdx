---
concepts:
- CLS
- layout-shift
- session-window
- impact-fraction
- distance-fraction
- aspect-ratio
- font-display
- size-adjust
deepensInto:
- 06-tradeoffs-and-observability
estMin: 12
lang: en
lessonType: topic
level: middle
order: 4
prereqs:
- 01-what-vitals-measure
slug: 04-cls-causes-and-fixes
sources:
- https://web.dev/articles/cls
- https://web.dev/articles/optimize-cls
- https://developer.chrome.com/docs/devtools/performance
spiral:
- statefulness
status: ready
summary: 'Cumulative Layout Shift scores the worst burst of unexpected movement —
  un-sized images, late ads, font swaps, dynamically injected banners — and the fix
  is always the same: reserve space before the bytes arrive.'
title: 'CLS: why layout shifts happen and how to stop them'
track: browser
unit: 07-core-web-vitals
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";
import DiagramFrame from "~/components/diagram/DiagramFrame.astro";

<Hook>
An article loads fast. The user starts reading the second paragraph. Then an ad slot above the article fills in and shoves everything down by 300 px. The user was mid-sentence; now they are mid-paragraph-of-the-wrong-article. They did not tap anything. The page moved by itself. That is CLS — and it is the easiest of the three vitals to prevent once you understand the one rule behind all four classic causes.
</Hook>

<Crux>CLS measures unexpected movement — and the fix is always the same: reserve space for anything whose final size is unknown at parse time.</Crux>

<Explanation>

**How the score is calculated.**

Before you can reason about whether a fix will move your CLS score, you need to understand what the browser is actually measuring per shift. Each time a visible element changes position between two frames without a user-initiated cause, the browser records a *layout shift*. One shift's score is:

**impact fraction × distance fraction**

*Impact fraction*: the combined area of all shifted elements, as a fraction of the viewport. If an image that covers half the viewport jumps, impact fraction is 0.5. *Distance fraction*: the largest distance any element moved, as a fraction of the viewport height. A shift of 20% of viewport height gives 0.2.

So a single shift that moves a half-viewport element by 20% of viewport height scores 0.5 × 0.2 = 0.1 — exactly on the "good" border.

CLS is not a lifetime sum of all shifts. It is the sum of shifts in the **worst session window**: a cluster of shifts where each shift is within 1 second of the previous, and the entire cluster spans at most 5 seconds. This change from the original algorithm was deliberate — the old sum unfairly penalised long-lived pages and infinite scroll.

<NumbersCard
  id="card-cls-formula"
  title="CLS scoring rules"
  stats={[
    { label: "Shift score", value: "impact fraction × distance fraction" },
    { label: "CLS reported", value: "worst 5 s session window sum" },
    { label: "Shift exclusion window after input", value: "500 ms" },
    { label: "Good threshold (p75)", value: "≤0.1" },
  ]}
  data-lesson-visual
/>

**The 500 ms exclusion — what CLS does not punish.**

Shifts within 500 ms of a user interaction are excluded. Opening an accordion, expanding a dropdown, clicking a "show more" button — the resulting movement is expected by the user and does not count toward CLS. CLS only punishes movement the user did not cause and did not expect.

**Four classic causes and their fixes.**

1. **Images without dimensions.** An `<img>` with no `width` and `height` attributes has a 0-height box until the bytes arrive. When the image loads, the browser learns its intrinsic size, reruns layout, and every element below jumps down. Fix: always set `width` and `height` HTML attributes (modern browsers derive `aspect-ratio` from them automatically and reserve a correctly-proportioned box). CSS can still make the image fluid with `width: 100%; height: auto` — the attributes supply the ratio, the CSS supplies responsive sizing.

2. **Ads, embeds, and iframes injected into unreserved space.** An ad slot that renders 250 px of content into a container with no reserved height shoves everything below it down. Fix: wrap every ad and embed slot in a container with a `min-height` equal to the largest expected ad creative.

3. **Web font reflow.** A fallback font with different line metrics renders first; when the web font loads, the browser reflows text — characters are wider or narrower, lines rebreak, elements below shift. Fix: use `size-adjust` and the `ascent-override` / `descent-override` font descriptors to make the fallback metrics match the web font; or use `font-display: optional` to only apply the web font on repeat visits.

4. **Dynamically injected content above existing content.** A cookie banner, notification bar, or chat widget inserted above the page body at runtime pushes everything down. Fix: insert it into pre-reserved space (a container with a known height in the layout), or render it as an overlay (fixed/absolute) so it does not participate in flow layout at all.

The unifying principle: **reserve space for anything whose final size is not known at parse time, or ensure it never participates in flow layout**.

<DiagramFrame
  label="Without reserved space, a late-loading image collapses to a zero-height box, so the article text renders high; when the image bytes arrive the browser reruns layout and the text jumps down — a layout shift. With explicit width and height the browser reserves a correctly-proportioned box up front, so the image fills it and nothing moves."
  hue="var(--d-frontend)"
>
  <svg viewBox="0 0 460 230" width="460" height="230" role="presentation" style="display:block;margin:0 auto;max-width:100%;height:auto;font-family:var(--font-body)">
    {/* LEFT: no reserved space */}
    <text x="105" y="16" text-anchor="middle" font-size="12" fill="var(--ink)">No width/height — shift</text>
    {/* before frame */}
    <text x="50" y="38" text-anchor="middle" font-size="9" font-family="var(--font-mono)" fill="var(--faint)">before bytes</text>
    <rect x="14" y="44" width="72" height="150" rx="5" fill="none" stroke="var(--hairline-strong)" stroke-width="0.8" stroke-dasharray="3 3" />
    <rect x="22" y="52" width="56" height="10" rx="2" fill="none" stroke="var(--muted)" stroke-width="0.8" />
    <text x="50" y="60" text-anchor="middle" font-size="6.5" font-family="var(--font-mono)" fill="var(--muted)">img 0px</text>
    <rect x="22" y="68" width="56" height="6" rx="1.5" fill="var(--card-2)" />
    <rect x="22" y="78" width="56" height="6" rx="1.5" fill="var(--card-2)" />
    <rect x="22" y="88" width="40" height="6" rx="1.5" fill="var(--card-2)" />
    {/* after frame */}
    <text x="160" y="38" text-anchor="middle" font-size="9" font-family="var(--font-mono)" fill="var(--faint)">after image loads</text>
    <rect x="124" y="44" width="72" height="150" rx="5" fill="none" stroke="var(--hairline-strong)" stroke-width="0.8" stroke-dasharray="3 3" />
    <rect x="132" y="52" width="56" height="40" rx="2" fill="var(--card-2)" stroke="var(--muted)" stroke-width="0.8" />
    <text x="160" y="75" text-anchor="middle" font-size="7" font-family="var(--font-mono)" fill="var(--muted)">img loaded</text>
    <rect x="132" y="98" width="56" height="6" rx="1.5" fill="var(--card-2)" />
    <rect x="132" y="108" width="56" height="6" rx="1.5" fill="var(--card-2)" />
    <rect x="132" y="118" width="40" height="6" rx="1.5" fill="var(--card-2)" />
    {/* shift arrow */}
    <line x1="206" y1="72" x2="206" y2="104" stroke="var(--ink)" stroke-width="1.2" marker-end="url(#cls-shift-arrow)" />
    <text x="232" y="92" text-anchor="middle" font-size="8" font-family="var(--font-mono)" fill="var(--ink)">text jumps</text>
    {/* divider */}
    <line x1="270" y1="30" x2="270" y2="200" stroke="var(--hairline-2)" stroke-width="0.5" />
    {/* RIGHT: reserved space */}
    <text x="365" y="16" text-anchor="middle" font-size="12" fill="var(--ink)">width + height — no shift</text>
    <text x="320" y="38" text-anchor="middle" font-size="9" font-family="var(--font-mono)" fill="var(--faint)">before bytes</text>
    <rect x="284" y="44" width="72" height="150" rx="5" fill="none" stroke="var(--hairline-strong)" stroke-width="0.8" stroke-dasharray="3 3" />
    <rect x="292" y="52" width="56" height="40" rx="2" fill="none" stroke="var(--muted)" stroke-width="0.8" stroke-dasharray="2 2" />
    <text x="320" y="75" text-anchor="middle" font-size="6.5" font-family="var(--font-mono)" fill="var(--muted)">reserved</text>
    <rect x="292" y="98" width="56" height="6" rx="1.5" fill="var(--card-2)" />
    <rect x="292" y="108" width="56" height="6" rx="1.5" fill="var(--card-2)" />
    <rect x="292" y="118" width="40" height="6" rx="1.5" fill="var(--card-2)" />
    <text x="430" y="38" text-anchor="middle" font-size="9" font-family="var(--font-mono)" fill="var(--faint)">after image loads</text>
    <rect x="394" y="44" width="72" height="150" rx="5" fill="none" stroke="var(--hairline-strong)" stroke-width="0.8" stroke-dasharray="3 3" />
    <rect x="402" y="52" width="56" height="40" rx="2" fill="var(--card-2)" stroke="var(--muted)" stroke-width="0.8" />
    <text x="430" y="75" text-anchor="middle" font-size="7" font-family="var(--font-mono)" fill="var(--muted)">img loaded</text>
    <rect x="402" y="98" width="56" height="6" rx="1.5" fill="var(--card-2)" />
    <rect x="402" y="108" width="56" height="6" rx="1.5" fill="var(--card-2)" />
    <rect x="402" y="118" width="40" height="6" rx="1.5" fill="var(--card-2)" />
    <text x="430" y="138" text-anchor="middle" font-size="8" font-family="var(--font-mono)" fill="var(--muted)">text stays put</text>
    <defs>
      <marker id="cls-shift-arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto-start-reverse">
        <path d="M0 0 L10 5 L0 10 z" fill="var(--ink)" />
      </marker>
    </defs>
  </svg>
</DiagramFrame>

**Animation pitfall.**

Animating layout properties — `top`, `left`, `height`, `width`, `margin` — *does* generate layout shifts and can hurt CLS, even if the movement looks intentional to a developer. The fix is to animate `transform` instead (`translateY`, `scale`), which is composited and never triggers layout. A shift caused by a CSS animation is still a shift unless it follows a user interaction within 500 ms.

<Inset kind="why" lang="en">
The session window model replaced the original "lifetime sum" model because long-lived pages and infinite scroll were penalised for shifts that happened five minutes after load — well outside the user's reading context. The session window focuses CLS on bursts of bad behavior: a cluster of shifts during an ad reload, or a batch of images loading without dimensions, rather than the accumulated cost of a site someone browses for an hour. CLS is now more representative of what a user actually notices.
</Inset>

<FadedExample
  client:visible
  id="sr-faded-1"
  pieceSlug="04-cls-causes-and-fixes"
  lang="en"
  title="Reserve space for a late-loading image to eliminate CLS"
  steps={{
    solved: (
      <pre><code>{`{/* width + height let the browser reserve a
     correctly-proportioned box before the bytes arrive.
     Modern browsers derive aspect-ratio from them. */}
<img
  src="/hero.avif"
  width="1200"
  height="800"
  fetchpriority="high"
  alt="Product hero"
/>`}</code></pre>
    ),
    semi: {
      prompt: (
        <p>Fill in the two attributes that let the browser reserve space for the image before it loads.</p>
      ),
      blanks: [
        { id: "w", expected: /width/i, placeholder: "dimension attribute 1" },
        { id: "h", expected: /height/i, placeholder: "dimension attribute 2" },
      ],
    },
    blank: {
      prompt: (
        <p>An <code>&lt;img&gt;</code> with no dimensions loads in late, jumps from a 0-height box to full size, and pushes the article text down — a CLS shift. Rewrite the tag so the browser reserves the right space before the image bytes arrive.</p>
      ),
      reveal: (
        <p>Add explicit <code>width</code> and <code>height</code> attributes. The browser derives the aspect ratio at parse time and lays out a correctly-proportioned box before the image downloads. CSS can still apply <code>width: 100%; height: auto</code> for responsive sizing — the attributes supply the ratio. When the image arrives it fills the reserved space with no shift.</p>
      ),
    },
  }}
/>

<Quiz
  id="mid-quiz-1"
  pieceSlug="04-cls-causes-and-fixes"
  lang="en"
  question="A user opens an accordion and the content below it moves down. Does this count toward CLS?"
  choices={[
    { label: "No — shifts within 500 ms of a user interaction are excluded; this movement was user-initiated and expected", correct: true },
    { label: "Yes — any layout movement counts toward CLS", misconception: "CLS specifically excludes shifts within 500 ms of a user interaction. It only measures unexpected, un-caused movement." },
    { label: "Yes, but only half the score", misconception: "There is no half-score. User-initiated shifts within the 500 ms window are fully excluded, not partially counted." },
    { label: "Only if the accordion has an image in it", misconception: "The content type is irrelevant. The exclusion is about the shift following a user interaction, regardless of what moved." },
  ]}
/>

<Quiz
  id="mid-quiz-2"
  pieceSlug="04-cls-causes-and-fixes"
  lang="en"
  question="A cookie banner is injected above the article body after the page loads, pushing all content down 60 px. What is the correct CLS fix?"
  choices={[
    { label: "Render the banner as a fixed-position overlay, or pre-reserve a 60 px container in the layout so the banner fills reserved space", correct: true },
    { label: "Delay the banner injection until after 5 seconds", misconception: "Delaying does not remove the shift — it just moves the session window. The layout still moves when the banner appears; CLS still scores it." },
    { label: "Make the banner fade in rather than slide in", misconception: "The visibility animation does not affect layout. The layout shift happens when the banner participates in flow and pushes content — whether it fades or slides is irrelevant to CLS." },
    { label: "Use CSS animation on the banner", misconception: "Animating a layout property (height, margin) on the banner still causes a layout shift. Only using overlay positioning or pre-reserved space prevents the shift." },
  ]}
/>

<Misconception id="mc-cls-images">
**Myth:** Animating an element with CSS is always safe for CLS.

**Reality:** Animating layout properties (`top`, `left`, `height`, `width`, `margin`) triggers layout recalculations and records layout shifts. Animate `transform` (`translateY`, `scale`) instead — it is composited and never affects layout.
</Misconception>

</Explanation>

<KeyTakeaway>CLS punishes unexpected layout movement — score is impact × distance, worst 5 s window. The fix is always reserving space: width/height on images, min-height on ad containers, size-adjust on fonts, overlay positioning for injected banners.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-cls-causes-and-fixes-retrieval"
  lang="en"
  questions={[
    { q: "How is one layout shift's score calculated, and what is the CLS session window?", a: "One shift's score = impact fraction (combined area of shifting elements as fraction of viewport) × distance fraction (largest movement as fraction of viewport height). CLS is the sum of the worst session window: a cluster of shifts where each is within 1 second of the previous and the whole cluster spans ≤5 seconds." },
    { q: "Name the four classic CLS causes and the fix for each.", a: "1. Images without dimensions — add width/height HTML attributes so the browser reserves space. 2. Ads/embeds in unreserved containers — pre-reserve min-height on the container. 3. Web font reflow — use size-adjust/font-override descriptors to match fallback metrics. 4. Dynamically injected content above the fold — insert into pre-reserved space or use overlay positioning." },
    { q: "A CSS animation moves an element using 'top' and the page fails its CLS audit. How do you fix it without removing the animation?", a: "Replace the top animation with transform: translateY(). Animating layout properties (top, left, height, width) triggers layout recalculation between frames and generates layout shifts. transform is composited — it moves the element without touching layout, so no shift is recorded." },
  ]}
/>

<Recap lang="en">
CLS scores the worst burst of unexpected layout movement: one shift's score is impact fraction × distance fraction, and CLS reports the worst 5-second session window. Shifts within 500 ms of a user interaction are excluded — CLS only punishes movement the user did not cause. The four classic causes are images without dimensions, ads and embeds in containers with no reserved height, web font reflow when the fallback metrics differ, and dynamically injected content above existing flow. The fix for all four follows one rule: anything whose final size is unknown at parse time must have space reserved before it arrives, or it must use overlay positioning so it does not participate in flow layout. Animating layout properties (top, left, height) generates shifts — animate transform instead. Now when you see a CLS spike, your first question is not "what moved?" but "was space reserved for it?" — and if not, that is your fix.
</Recap>
