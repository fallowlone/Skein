---
concepts:
- CrUX
- Lighthouse
- TBT
- FCP
- TTFB
- lab-data
- field-data
- DevTools-trace
- p75-percentile
deepensInto:
- 06-tradeoffs-and-observability
estMin: 12
lang: en
lessonType: topic
level: middle
order: 5
prereqs:
- 01-what-vitals-measure
- 02-lcp-mechanics
- 03-inp-mechanics
slug: 05-lab-vs-field
sources:
- https://web.dev/articles/vitals
- https://developer.chrome.com/docs/crux
- https://developer.chrome.com/docs/devtools/performance
spiral:
- latency
status: ready
summary: Lighthouse is a lab tool — reproducible, great for debugging. CrUX is field
  data from real Chrome users — and only the field drives Search ranking. Knowing
  which to trust for which question ends the confusion.
title: 'Lab vs field: why the two disagree and how to use each'
track: browser
unit: 07-core-web-vitals
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumberDrill from "~/components/pedagogy/NumberDrill.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import Misconception from "~/components/layout/Misconception.astro";

<Hook>
PageSpeed Insights shows LCP 1.8 s in the Lighthouse section — green, good. The field data section shows LCP 3.9 s at p75 — red, poor. The team says "the lab says we are fine." They ship nothing. Three months later, Search Console still shows poor LCP. The lab score and the ranking are measuring different things. Knowing which one is the verdict changes everything.
</Hook>

<Crux>Debug in the lab, judge in the field — only CrUX p75 drives Search ranking, and lab tools cannot model real user diversity.</Crux>

<Explanation>

**Lab data vs field data — two different questions.**

Lab data is a synthetic measurement: a tool like Lighthouse or DevTools loads the page once, on a simulated device and a throttled network, in a controlled environment. It is fully reproducible and it can attribute problems precisely — great for debugging. One device, one network, one moment. If you change the page and rerun, the result is comparable.

Field data is real-user monitoring: the actual Core Web Vitals experienced by real visitors on their real devices and networks, aggregated. Google's public field dataset is **CrUX** — the Chrome User Experience Report. It aggregates visits from opted-in Chrome users, and the field p75 from CrUX is what Search ranking uses.

They disagree routinely because they measure different things. A lab run on a simulated fast device shows an LCP that a quarter of real users — on older phones, on congested 4G — never come close to. INP barely exists in the lab at all, because lab tools do not script realistic interaction sequences; the lab proxy is Total Blocking Time (TBT). CLS in the lab is close to real, but dynamic content that appears only with real user sessions can be missed.

<NumbersCard
  id="card-lab-vs-field"
  title="Lab vs field — key differences"
  stats={[
    { label: "Lab (Lighthouse, DevTools)", value: "Synthetic, reproducible, debuggable" },
    { label: "Field (CrUX, web-vitals RUM)", value: "Real users, real devices, real networks" },
    { label: "What drives Search ranking", value: "CrUX field p75" },
    { label: "Lab INP proxy", value: "Total Blocking Time (TBT)" },
    { label: "Rule", value: "Debug in lab; judge in field" },
  ]}
  data-lesson-visual
/>

**The rule: debug in the lab, judge in the field.**

Use the lab to reproduce and attribute a problem (fast, deterministic, easy to bisect commits), and to gate regressions in CI (Lighthouse CI runs on every PR, fast, no real users needed). Use the field p75 as the verdict on whether a page is actually good for users and whether it will rank. A lab improvement that does not move the field p75 did not help your users. Only the field number is the truth.

**The supporting diagnostic metrics.**

The three Core Web Vitals sit on top of a layer of diagnostic metrics that are not scored for ranking but are essential for understanding them:

- **TTFB** (Time to First Byte) — the server-and-network portion. It is the floor under LCP: LCP can never be faster than TTFB.
- **FCP** (First Contentful Paint) — when the *first* pixel of content appears, vs LCP's *largest*. A large FCP-to-LCP gap means the page shows something quickly but the main content lags — a sign the LCP resource has load delay.
- **TBT** (Total Blocking Time) — the sum of the blocking portion of every long task during load. This is the lab proxy for INP: you cannot measure true INP in a lab (it needs real interactions), so CI gates use TBT instead. A high TBT predicts a high INP.

Knowing these is what lets you read a Lighthouse report fully — Lighthouse never just says "LCP is bad", it shows TTFB and FCP so you can see where in the chain the time went.

**Reading a DevTools performance trace.**

Open DevTools, the Performance panel, record a page load and an interaction. The trace shows several tracks:

- *Timings* track: marks FCP, LCP, and DCL with vertical lines. Hovering the LCP marker names the exact element.
- *Frames* track: each rendered frame; layout shifts appear as red markers — click to see which nodes shifted.
- *Main* track: flame chart of main-thread work; long tasks are flagged with a red corner.
- *Interactions* track: click/tap events, broken into input delay, processing, and presentation delay — color-coded — so you can see which part dominates for each interaction without guessing.

The INP workflow: interact during the recording, find the interaction in the Interactions track, read the three-part split. The LCP workflow: find the LCP marker in the Timings track, hover to identify the element, then look at the network waterfall above it to see when the resource was requested vs when it downloaded vs when the element painted.

<Inset kind="why" lang="en">
Why is p75 used rather than the median? Median performance is pulled down by fast outliers — developers on fast Wi-Fi, cached repeat visits. The 75th percentile focuses attention on the slow quarter of real users: those on older phones, mobile networks, or less powerful CPUs. A change that improves the median but not p75 helped the already-fast users and did nothing for the users who needed it most. p75 is the threshold that makes the optimization target the tail of the distribution, not its center.
</Inset>

<TraceScenario
  id="mid-trace-1"
  pieceSlug="05-lab-vs-field"
  lang="en"
  scenario="PageSpeed Insights shows LCP 1.8 s in the Lighthouse (lab) section — good — but the field data section shows LCP 3.9 s at p75 — poor. The team says 'the lab says we are fine'. Who is right?"
  steps={[
    { prompt: "The field data is the real verdict — lab ran on a fast simulated device and connection; real users at p75 are on slower phones and networks, and 3.9 s is what they actually experience", reveal: "Correct. The lab number is one synthetic run on a simulated mid-tier device with a throttled-but-generous network. The field number is aggregated from real Chrome users — and p75 means a quarter of real visits are at or above 3.9 s. Search ranking uses the field (CrUX) data. The team must reproduce the slow conditions (heavier CPU throttle, slower network in DevTools) until the lab shows the problem, then fix it." },
    { prompt: "The lab is right because it is reproducible", reveal: "Reproducible does not mean representative. The lab is reproducible precisely because it fixes one device and network — but real users are not one device. Reproducibility makes the lab good for debugging, not for the verdict." },
    { prompt: "They are both wrong; only TTFB matters", reveal: "TTFB is one phase of LCP, not a replacement for it. Both numbers are valid measurements — they just answer different questions (synthetic vs real-user)." },
    { prompt: "Average the two: 2.85 s", reveal: "Averaging a lab number and a field percentile is meaningless — they are different kinds of measurement. The field p75 stands on its own as the user-experienced and ranking-relevant value." },
  ]}
/>

<MetaphorComplete
  id="mid-metaphor-1"
  pieceSlug="05-lab-vs-field"
  lang="en"
  setup="Google publishes a public dataset of real-user Core Web Vitals, aggregated from actual Chrome visits, and Search ranking is based on it — not on any lab test. What is that field dataset called?"
  accepted={["crux", "chrome user experience report", "the chrome ux report"]}
  canonical="CrUX"
  explanation="CrUX — the Chrome User Experience Report — is Google's public dataset of field metrics, aggregated from real Chrome users who opted into reporting. It is the source of the 'field data' you see in PageSpeed Insights and Search Console, and it is what Search ranking actually uses. This is why a great Lighthouse score does not guarantee a good ranking: Lighthouse is lab, CrUX is field, and only the field counts for the verdict."
/>

<NumberDrill
  id="mid-drill-1"
  pieceSlug="05-lab-vs-field"
  lang="en"
  question="A 'good' INP score requires interaction latency at or below how many milliseconds, at the 75th percentile?"
  expected={200}
  tolerance={0}
  unit="ms"
  explanation="200 ms is the 'good' INP threshold at p75. 200–500 ms is 'needs improvement'; over 500 ms is 'poor'. The chain back to the event loop is direct: keeping every task under the 50 ms long-task cap is what keeps input delay small, which is what keeps INP under 200 ms."
/>

<Misconception id="mc-lab-vs-field">
**Myth:** A fast Lighthouse score means your Core Web Vitals are good for ranking.

**Reality:** Lighthouse is a lab test on one simulated device. Search ranking uses field data (CrUX) — real users at p75 on slower devices. A green lab score and a poor field p75 routinely coexist.
</Misconception>

</Explanation>

<KeyTakeaway>Lab tools (Lighthouse, DevTools) are for debugging and CI gating; CrUX field p75 is the Search ranking verdict. A lab fix that does not move field p75 did not help users.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-lab-vs-field-retrieval"
  lang="en"
  questions={[
    { q: "Explain the lab-vs-field distinction and give the rule for when to use each.", a: "Lab: synthetic test, one device, one network, reproducible — use for debugging (attribute a problem, bisect commits) and CI regression gating (fast, no users needed). Field: real users, real devices, real networks, aggregated at p75 — CrUX is what Search ranking uses. Rule: debug in the lab, judge in the field. A lab improvement that does not move field p75 did not help real users." },
    { q: "Why is Total Blocking Time used as the INP proxy in CI, rather than measuring INP directly?", a: "True INP requires real user interactions — a CI run loads the page synthetically without simulating realistic click/tap sequences. TBT is the sum of the blocking portion of long tasks during load, which correlates with input delay (a busy thread delays inputs). TBT is measurable in a lab, predictive of INP, and can gate CI. It is a proxy, not a perfect substitute — a page can have low TBT but poor INP from slow handlers, which TBT does not see." },
    { q: "In a DevTools performance trace, what does the Interactions track show for a slow interaction, and how do you use it to diagnose INP?", a: "The Interactions track shows each click/tap/keypress event as a bar divided into three color-coded segments: input delay (thread was busy), processing time (handler ran), and presentation delay (style/layout/paint after handler). Find the slow interaction, read which segment dominates, then look at the Main flame chart for that time range to identify the specific long task (input delay) or the heavy render (presentation delay)." },
  ]}
/>

<Recap lang="en">
Lab data — from Lighthouse or DevTools — is synthetic: one device, one network, reproducible, great for debugging and CI regression gates. Field data — CrUX — is from real Chrome users at p75, and it is what Search ranking uses. They disagree routinely because real user diversity (slower phones, congested networks) is not captured in a single simulated run. INP barely exists in the lab because lab tools do not simulate real interactions; the lab proxy is Total Blocking Time. The rule is: debug in the lab, judge in the field. TTFB, FCP, and TBT are the supporting diagnostic metrics under the three Core Web Vitals — knowing them is what lets you read a Lighthouse report and trace where in the chain time was lost. A lab improvement that does not move the field p75 did not help your users; only the field number is the real verdict.
</Recap>
