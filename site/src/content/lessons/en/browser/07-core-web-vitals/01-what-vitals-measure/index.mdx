---
concepts:
- LCP
- INP
- CLS
- field-metrics
- p75
- CrUX
deepensInto:
- 02-lcp-mechanics
- 03-inp-mechanics
- 04-cls-causes-and-fixes
estMin: 10
lang: en
lessonType: topic
level: junior
order: 1
prereqs: []
slug: 01-what-vitals-measure
sources:
- https://web.dev/articles/vitals
- https://web.dev/articles/lcp
- https://developer.chrome.com/docs/crux
spiral:
- latency
- statefulness
status: ready
summary: Three field metrics real Chrome users generate on every visit — and why all
  the other performance work in this pillar ultimately rolls up into these three numbers.
title: 'Core Web Vitals: what LCP, INP, and CLS measure'
track: browser
unit: 07-core-web-vitals
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumberDrill from "~/components/pedagogy/NumberDrill.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook>
A page loads. The hero photo appears. A user taps a button and waits. An ad slams in from above and the text jumps — the user taps the wrong link. Every chapter in this pillar — the event loop, the render pipeline, V8, hydration — produces exactly this kind of experience. Three field metrics measure it, and Google ranks pages by them.
</Hook>

<Crux>Three field metrics: did it show up fast (LCP), does it respond (INP), does it stay still (CLS)?</Crux>

<Explanation>

**The three metrics — one question each.**

Each Core Web Vital answers one question a real user has the moment they land on a page:

- **LCP — Largest Contentful Paint** — did the main content show up fast? Good: ≤2.5 s.
- **INP — Interaction to Next Paint** — does the page respond when I tap or type? Good: ≤200 ms.
- **CLS — Cumulative Layout Shift** — did things stay put, or did a button jump out from under my finger? Good: ≤0.1 (unitless score).

Each threshold is measured at the **75th percentile** of real users. That matters: optimising only for fast devices hides the slow quarter of your audience, and that quarter is exactly what the score reflects.

**The shop metaphor.**

Walking into a physical shop is the clearest frame for these three metrics. *LCP* is how long before you can see what the shop sells — if the shelves are still being stocked, you wait at the door. *INP* is whether the clerk answers right away when you ask a question — a good shop responds immediately, a bad one makes you repeat yourself. *CLS* is whether the floor stays put — imagine reaching for an item on a shelf and the shelf sliding sideways just as your hand arrives, so you grab the wrong thing. A good page is a good shop.

**Where the numbers come from — and why it matters.**

Core Web Vitals as Google scores them come from **real Chrome users in the field**, not from a developer's test run. The public dataset is called **CrUX** — the Chrome User Experience Report. It aggregates visits from opted-in Chrome users, and the field p75 from CrUX is what Search ranking actually uses. A great Lighthouse score on your laptop and poor field vitals are entirely compatible — and the field number is the one that counts.

The practical consequence: every performance fix in this pillar ultimately lives or dies on whether it moves the field p75 for real users. Lab tools (Lighthouse, DevTools) are for debugging and catching regressions; CrUX is the verdict.

<NumbersCard
  id="card-cwv-thresholds"
  title="Core Web Vitals good thresholds (at field p75)"
  stats={[
    { label: "LCP — Largest Contentful Paint", value: "≤2.5 s" },
    { label: "INP — Interaction to Next Paint", value: "≤200 ms" },
    { label: "CLS — Cumulative Layout Shift", value: "≤0.1" },
  ]}
  data-lesson-visual
/>

<Inset kind="why" lang="en">
INP replaced First Input Delay (FID) as a Core Web Vital in March 2024. FID only measured the delay before the *first* input's handler started running — it missed the rest of the page lifecycle. INP measures the full input-to-paint latency for every interaction across the whole session and reports a high percentile of them, which is far closer to what a user actually experiences using the page, not just landing on it.
</Inset>

<DragOrder
  id="jr-drag-1"
  pieceSlug="01-what-vitals-measure"
  lang="en"
  prompt="Match the user complaint to the Core Web Vital — drag into the order LCP, INP, CLS."
  items={[
    { id: "lcp", label: "LCP — 'the page took forever to show the article'" },
    { id: "inp", label: "INP — 'I tapped the button and nothing happened for a second'" },
    { id: "cls", label: "CLS — 'the text jumped and I lost my place / tapped the wrong thing'" },
  ]}
  correctOrder={["lcp", "inp", "cls"]}
/>

<Quiz
  id="jr-quiz-1"
  pieceSlug="01-what-vitals-measure"
  lang="en"
  question="A user taps a button and the page does not visibly respond for almost a second. Which Core Web Vital captures this?"
  choices={[
    { label: "INP — Interaction to Next Paint — the delay from a tap to the screen responding", correct: true },
    { label: "LCP — Largest Contentful Paint", misconception: "LCP measures when the largest content first appears during load. A delayed response to a tap is about interactivity — that is INP." },
    { label: "CLS — Cumulative Layout Shift", misconception: "CLS measures unexpected movement of the layout. A slow response is not movement — it is latency, which is INP." },
    { label: "TTFB — Time to First Byte", misconception: "TTFB is a network metric for the first byte from the server. The button delay happens long after that, in the browser." },
  ]}
/>

<Quiz
  id="jr-quiz-2"
  pieceSlug="01-what-vitals-measure"
  lang="en"
  question="Where do Core Web Vitals numbers come from when Google uses them for Search ranking?"
  choices={[
    { label: "Real Chrome users visiting the page in the field — CrUX, not from a test on a developer's machine", correct: true },
    { label: "A single lab test run once at build time", misconception: "A lab test is one synthetic run. Core Web Vitals as Google scores them are field data — aggregated from real users on real devices and networks." },
    { label: "The developer's local DevTools session", misconception: "Local DevTools is a lab tool. It is useful for debugging, but the scored vitals come from real-user field data." },
    { label: "The server's access logs", misconception: "Server logs do not capture paint timing or interaction latency in the browser. Vitals are measured client-side from real users." },
  ]}
/>

<MetaphorComplete
  id="jr-metaphor-1"
  pieceSlug="01-what-vitals-measure"
  lang="en"
  setup="In the shop metaphor, one vital is about whether the floor stays put — whether a shelf slides sideways just as you reach for an item, making you grab the wrong thing. Which Core Web Vital is that?"
  accepted={["cls", "cumulative layout shift", "layout shift", "the cls"]}
  canonical="CLS"
  explanation="CLS — Cumulative Layout Shift — measures how much the page's layout moves unexpectedly. The classic cause is content loading in late — an image with no reserved space, an ad, a web font swapping — and pushing everything below it down. To the user it feels like the shop floor sliding: they reach for a button and it is suddenly somewhere else, so they tap the wrong thing. A good CLS score means the layout is stable: things load into space that was reserved for them in advance."
/>

<NumberDrill
  id="jr-drill-1"
  pieceSlug="01-what-vitals-measure"
  lang="en"
  question="A 'good' LCP score is achieved when the largest content paints within how many seconds, at the 75th percentile of real users?"
  expected={2.5}
  tolerance={0}
  unit="seconds"
  explanation="2.5 seconds is the 'good' LCP threshold. The largest contentful element — usually a hero image or a headline block — must paint within 2.5 s for 75% of real visits. Between 2.5 s and 4 s is 'needs improvement'; over 4 s is 'poor'. The 75th percentile matters: optimising only for fast devices hides the slow quarter of your users, and that quarter is what the score reflects."
/>

</Explanation>

<KeyTakeaway>LCP, INP, CLS are field metrics at p75 of real Chrome users — not lab scores. Each answers one user question: did it show up, does it respond, does it stay still.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-what-vitals-measure-retrieval"
  lang="en"
  questions={[
    { q: "What does each Core Web Vital measure — in one phrase per metric?", a: "LCP: time until the largest content paints (good ≤2.5 s). INP: latency from a tap to the screen visibly responding (good ≤200 ms). CLS: how much the layout shifts unexpectedly (good ≤0.1)." },
    { q: "Why is the 75th percentile threshold more useful than the mean for these metrics?", a: "The mean is pulled down by fast outliers — developers on fast Wi-Fi, cached repeat visits. The p75 focuses on the slow quarter of real users, which is who the ranking and experience targets." },
    { q: "What is CrUX and why does it matter more than Lighthouse for SEO?", a: "CrUX (Chrome User Experience Report) is Google's public dataset of field metrics from real Chrome users. Search ranking uses CrUX p75 values — not Lighthouse lab scores. A green Lighthouse result and poor CrUX p75 coexist routinely." },
  ]}
/>

<Recap lang="en">
Core Web Vitals are three field metrics that answer what real users notice: LCP (did the main content appear within 2.5 s), INP (did the page respond to a tap within 200 ms), and CLS (did the layout stay still, scoring ≤0.1). They are measured at the 75th percentile of real Chrome users via CrUX — not from a lab test — and CrUX p75 is what Search ranking uses. Every performance technique in this pillar (reducing long tasks for INP, optimising image delivery for LCP, reserving space for CLS) ultimately exists to move these three field numbers. Debug in the lab; judge in the field.
</Recap>
