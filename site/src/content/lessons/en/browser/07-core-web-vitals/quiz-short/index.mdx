---
concepts:
- lcp-phases
- inp-parts
- cls-scoring
- field-vs-lab
- metric-tradeoffs
- rum-attribution
estMin: 14
lang: en
level: senior
order: 8
prereqs: []
slug: quiz-short
sources:
- https://web.dev/articles/vitals
- https://web.dev/articles/inp
- https://web.dev/articles/cls
status: ready
summary: "Free-recall prompts across the Core Web Vitals unit. Answer each in your own words first, then reveal the model answer and compare."
title: 'Core Web Vitals: free-recall review'
track: browser
unit: 07-core-web-vitals
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";

<Hook>
Retrieval beats re-reading. For each prompt, say or write a full answer from memory before you open the model answer — the effort of recall is what makes the material stick.
</Hook>

<Goal lang="en">
Reconstruct the unit's spine — what each vital measures and its threshold, the LCP phase split, the INP parts and the hydration trap, CLS scoring, the field-vs-lab rule, and the shared-budget discipline — without looking back at the lessons.
</Goal>

<RetrievalDrawer
  client:load
  id="07-core-web-vitals-short-retrieval"
  lang="en"
  questions={[
    { q: "What does each Core Web Vital measure, what is its 'good' threshold, and where are they measured?", a: "LCP (Largest Contentful Paint): time until the largest content element paints; good ≤2.5 s. INP (Interaction to Next Paint): latency from a tap/click/keypress to the next visible paint, reported at a high percentile across all interactions; good ≤200 ms. CLS (Cumulative Layout Shift): unitless score for unexpected layout movement; good ≤0.1. All three are measured at the 75th percentile of real Chrome users in the field (CrUX), not from a single lab run — the p75 focuses on the slow quarter of users rather than fast outliers." },
    { q: "Name the four LCP phases and the fix for each, and explain why you must read the split before applying a fix.", a: "TTFB — slow server/CDN/DB on the render path; fix with caching, a CDN, and moving slow data off the render path. Resource load delay — the image is discovered late (referenced in CSS or injected by JS), so the preload scanner cannot find it; fix by placing it in the initial HTML with fetchpriority='high' or a preload link. Resource load time — oversized/wrong-format image; fix with AVIF/WebP sized via srcset. Element render delay — render-blocking CSS/JS still occupying the thread; fix by inlining critical CSS and deferring scripts. You read the split first because a fix only works on the dominant phase — compressing the image when load delay dominates does nothing." },
    { q: "Name the three parts of INP, and explain the early-only INP pattern and what it points to.", a: "Input delay (the thread is busy with a long task when the input arrives), processing time (the event handler runs), and presentation delay (style/layout/paint/composite after the handler). If INP is poor only for interactions in the first few seconds and fine afterward, the cause is hydration: the hydration long task blocks the main thread, so early taps queue behind it (input delay). Once hydration finishes the thread is free. The fix is to shrink what hydrates — Server Components, islands, code-splitting, progressive hydration — not to optimise the handler code, which was already fast." },
    { q: "How is one CLS shift scored, what is the session window, and what does the 500 ms exclusion do?", a: "One shift's score = impact fraction (combined area of shifting elements as a fraction of the viewport) × distance fraction (largest movement as a fraction of viewport height). So a half-viewport element moving 20% of viewport height scores 0.5 × 0.2 = 0.1 — exactly on the good border. CLS is not a lifetime sum; it is the sum of the worst session window — a cluster where each shift is within 1 s of the previous and the cluster spans at most 5 s (this replaced the lifetime sum that unfairly penalised long-lived pages and infinite scroll). Shifts within 500 ms of a user interaction are excluded, because that movement was expected (opening an accordion, expanding a dropdown). CLS only punishes movement the user did not cause." },
    { q: "Explain the lab-vs-field distinction and give the rule for when to use each.", a: "Lab data is a synthetic measurement — one device, one network, reproducible — from tools like Lighthouse or a DevTools trace; great for debugging (attribute a problem, bisect commits) and for CI regression gates (fast, no real users needed). Field data is real-user monitoring aggregated at p75; Google's public field dataset is CrUX, and CrUX p75 is what Search ranking uses. They disagree routinely because a single simulated run cannot model real user diversity (older phones, congested networks). The rule: debug in the lab, judge in the field. A lab improvement that does not move the field p75 did not help real users. INP barely exists in the lab, so CI uses Total Blocking Time as the proxy." },
    { q: "Why must you measure all three vitals before and after any change, and what does complete production observability require?", a: "The three vitals share a performance budget, so a fix for one can break another: inlining critical CSS helps LCP render delay but bloats HTML and raises TTFB; lazy-loading shrinks the bundle for INP but wrecks LCP if it catches the hero; generous CLS space reservation can push the LCP element out of view and change the candidate; a heavy interactive framework adds hydration that spikes early INP. You cannot know the net effect without checking all three. Complete observability needs both halves: RUM (the web-vitals library on PerformanceObserver, sending attributed LCP/INP/CLS — element, phase split, interaction target, shifting nodes — tagged by route, device class, country, release) as the field verdict, plus a throttled CI gate (Lighthouse CI or a Playwright trace budgeting LCP/TBT/CLS per PR) to catch regressions before production. RUM without CI ships regressions to users first; CI without RUM misses device-class and real-interaction failures." },
  ]}
/>

<Recap lang="en">
If you reconstructed each answer from memory, you hold the unit's spine: LCP/INP/CLS are field metrics at p75 with thresholds ≤2.5 s, ≤200 ms, ≤0.1; LCP is read by its four phases and INP by its three parts so you fix the dominant one; the early-only INP pattern is the hydration signature; CLS is impact × distance over the worst 5 s window with a 500 ms post-interaction exclusion; the verdict lives in the field (CrUX), the lab is for debugging and CI; and because all three share a budget, every change is measured against all of them with RUM plus a CI gate.
</Recap>
