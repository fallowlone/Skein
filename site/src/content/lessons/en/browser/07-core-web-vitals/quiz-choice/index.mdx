---
concepts:
- lcp-inp-cls-thresholds
- field-vs-lab
- hydration-inp
- lcp-phases
- cls-session-window
- metric-tradeoffs
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://web.dev/articles/vitals
- https://web.dev/articles/inp
- https://developer.chrome.com/docs/crux
status: ready
summary: "Multiple-choice synthesis across the Core Web Vitals unit — thresholds, the field-vs-lab verdict, LCP phases, the hydration INP trap, CLS scoring, and shared-budget tradeoffs."
title: 'Core Web Vitals: multiple-choice review'
track: browser
unit: 07-core-web-vitals
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each mirrors a call you make reading a real PageSpeed report or a field dashboard — not a definition to recite, but which number to trust and which fix to reach for.
</Hook>

<Goal lang="en">
Confirm you can connect the three thresholds, the field-vs-lab verdict, the phase and part decompositions, and the shared performance budget — the synthesis the individual lessons built toward.
</Goal>

<Quiz
  id="07-core-web-vitals-choice-1"
  lessonSlug="07-core-web-vitals"
  lang="en"
  question="PageSpeed Insights shows LCP 1.8 s in the Lighthouse section (green) and LCP 3.9 s at field p75 (red). The team says 'the lab says we are fine, ship nothing.' What is the correct read?"
  choices={[
    { label: "The field p75 is the verdict — Search ranking uses CrUX field data, and a quarter of real users are at or above 3.9 s. Reproduce the slow conditions in the lab (heavier CPU/network throttle) until the lab shows the problem, then fix it.", correct: true },
    { label: "The lab is right because it is reproducible and deterministic.", misconception: "Reproducible is not representative. The lab fixes one device and network on purpose — that makes it good for debugging, not for the verdict. Ranking uses field p75." },
    { label: "Average them: the true LCP is about 2.85 s, which is fine.", misconception: "A lab number and a field percentile are different kinds of measurement; averaging them is meaningless. The field p75 stands on its own as the user-experienced, ranking-relevant value." },
    { label: "Only TTFB matters; both LCP numbers are noise.", misconception: "TTFB is one phase of LCP, not a replacement. Both LCP numbers are valid — they answer the synthetic-vs-real-user question differently — and the field one is the verdict." },
  ]}
/>

<Quiz
  id="07-core-web-vitals-choice-2"
  lessonSlug="07-core-web-vitals"
  lang="en"
  question="A server-rendered page has poor INP, but only for interactions in the first ~3 seconds; after that every tap is fine. The tap handlers run in 8 ms. Where is the problem and what is the fix?"
  choices={[
    { label: "Hydration. The early taps queue behind the hydration long task (input delay); once hydration finishes the thread is free. Fix: shrink what hydrates — Server Components, islands, code-splitting — not the handlers.", correct: true },
    { label: "The event handlers are too slow; optimise the handler code.", misconception: "If handlers were the cause, INP would be poor for every interaction, not only the early ones. An 8 ms handler cannot produce a time-bounded pattern — that signature points at hydration." },
    { label: "The server is slow; reduce TTFB.", misconception: "Server latency affects TTFB and LCP, not the main-thread interaction latency INP measures — and it would not produce an early-only INP pattern." },
    { label: "The images are too large; compress them.", misconception: "Image size drives LCP load time. It does not block the main thread for interactions; that is a JavaScript/hydration concern, which is what the early-only pattern points to." },
  ]}
/>

<Quiz
  id="07-core-web-vitals-choice-3"
  lessonSlug="07-core-web-vitals"
  lang="en"
  question="The web-vitals LCP attribution shows TTFB 280 ms, load delay 90 ms, load time 3510 ms, render delay 240 ms. The element is a 3.8 MB 4000×3000 JPEG hero. What is the highest-leverage fix?"
  choices={[
    { label: "Load time dominates (~85% of LCP) because the hero is a full-resolution unprocessed JPEG. Serve it sized to the viewport in AVIF or WebP via srcset — that alone cuts load time from 3.5 s to a few hundred ms.", correct: true },
    { label: "Add fetchpriority='high' and a preload hint to fix discovery.", misconception: "Load delay is only 90 ms — discovery is already fast. fetchpriority/preload fix load DELAY, not load TIME. Fixing the wrong phase is the classic LCP mistake." },
    { label: "Lower TTFB with a CDN and edge caching.", misconception: "TTFB is 280 ms — a small slice. Shaving it does almost nothing against a 3510 ms load time. Read the phase split first; fix the dominant phase." },
    { label: "Inline critical CSS to cut render delay.", misconception: "Render delay is 240 ms. Inlining CSS targets render-blocking resources, which are not the bottleneck here. The 4 MB image download is." },
  ]}
/>

<Quiz
  id="07-core-web-vitals-choice-4"
  lessonSlug="07-core-web-vitals"
  lang="en"
  question="A single layout shift moves a half-viewport element down by 20% of viewport height, with no user interaction in the prior 500 ms. What does it score, and is the page's CLS automatically poor?"
  choices={[
    { label: "0.5 × 0.2 = 0.1 — impact fraction times distance fraction, exactly on the 'good' border. CLS is the worst 5 s session-window sum, so one borderline shift is not automatically poor unless it clusters with others.", correct: true },
    { label: "It scores 0.7 — you add impact and distance fractions.", misconception: "The score is the product, not the sum: impact fraction × distance fraction = 0.5 × 0.2 = 0.1. Adding them is the common arithmetic error." },
    { label: "It scores 0 because there was no user interaction.", misconception: "Backwards — the 500 ms exclusion removes shifts that FOLLOW a user interaction. An un-caused shift is exactly what CLS punishes; this one scores 0.1." },
    { label: "It is automatically poor because any single shift over 0.05 fails.", misconception: "There is no per-shift pass/fail threshold. CLS is the summed worst 5 s session window; a lone 0.1 shift sits on the good border, not in 'poor'." },
  ]}
/>

<Quiz
  id="07-core-web-vitals-choice-5"
  lessonSlug="07-core-web-vitals"
  lang="en"
  question="A team inlines the full critical CSS bundle to cut LCP render delay by 400 ms. LCP improves in the lab, but you push back before merge. Why?"
  choices={[
    { label: "The three vitals share a budget — inlining bloats the HTML by tens of KB, raising TTFB, and TTFB is the floor under LCP. Measure all three (and TTFB) before and after; a render-delay win can be eaten by a TTFB regression on slow connections.", correct: true },
    { label: "Inlining CSS always hurts INP, so it should never be done.", misconception: "Inlining critical CSS does not inherently spike INP — it removes a render-blocking request. The real concern is the HTML-size/TTFB tradeoff, whose sign depends on connection speed and bundle size." },
    { label: "CSS has nothing to do with LCP, so the change is pointless.", misconception: "Render-blocking CSS directly delays the LCP element's paint (render delay), so inlining critical CSS can help LCP. The catch is the TTFB cost, not irrelevance." },
    { label: "Lab improvements never transfer to the field, so ignore it.", misconception: "Lab fixes can absolutely move the field — that is the point of debugging in the lab. The issue is verifying the net effect across all three metrics, not dismissing lab results." },
  ]}
/>

<Quiz
  id="07-core-web-vitals-choice-6"
  lessonSlug="07-core-web-vitals"
  lang="en"
  question="A SPA ships green Core Web Vitals on initial load, but users complain that client-side route changes feel sluggish. Why might the metrics miss this, and what is the fix?"
  choices={[
    { label: "Core Web Vitals were designed around full page loads; a soft navigation fires no document load event, so route changes historically had no LCP attribution. Instrument soft navigations explicitly — soft-navigation PerformanceObserver entries or RUM marks on route-change events.", correct: true },
    { label: "Nothing is wrong — green initial-load vitals mean the whole app is fast.", misconception: "Green initial-load numbers do not cover soft navigations. Snappy first load plus sluggish route transitions is a real, historically under-measured failure mode in SPAs." },
    { label: "The CrUX dataset is simply wrong for SPAs.", misconception: "CrUX is not wrong; the gap is that soft navigations emit no standard load event, so vitals were not attributed to them. Chrome's soft-navigation support and explicit RUM instrumentation close the gap." },
    { label: "Switch to multi-page architecture; SPAs cannot have good vitals.", misconception: "SPAs can hit good vitals — the issue is measurement coverage of route changes, not an architectural impossibility. Instrument soft navigations rather than abandoning the SPA." },
  ]}
/>

<Recap lang="en">
The unit's through-line is one diagnostic discipline: LCP/INP/CLS are field metrics at p75 (≤2.5 s, ≤200 ms, ≤0.1), and only CrUX field data is the ranking verdict — debug in the lab, judge in the field. LCP is read by its four-phase split and INP by its three-part split, so you fix the dominant phase, not the convenient one. The early-only INP pattern means hydration, not handlers. CLS is impact × distance summed over the worst 5 s window, with a 500 ms post-interaction exclusion. And all three share a budget — every change must be measured against all of them, never one in isolation.
</Recap>
