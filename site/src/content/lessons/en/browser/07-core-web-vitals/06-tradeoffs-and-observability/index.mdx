---
concepts:
- metric-tradeoffs
- RUM
- web-vitals-library
- PerformanceObserver
- LoAF
- INP-attribution
- LCP-attribution
- CI-gate
- soft-navigation
- SPA-vitals
deepensInto: []
estMin: 16
lang: en
lessonType: topic
level: senior
order: 6
prereqs:
- 02-lcp-mechanics
- 03-inp-mechanics
- 04-cls-causes-and-fixes
- 05-lab-vs-field
slug: 06-tradeoffs-and-observability
sources:
- https://web.dev/articles/vitals
- https://web.dev/articles/optimize-inp
- https://developer.chrome.com/docs/crux
- https://web.dev/articles/optimize-lcp
spiral:
- latency
- statefulness
status: ready
summary: The three vitals share a budget — a fix for one can break another. Complete
  production observability needs web-vitals RUM for the field verdict and a throttled
  CI gate for regressions — neither alone is enough.
title: Metric tradeoffs, RUM attribution, and the CI+field loop
track: browser
unit: 07-core-web-vitals
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DebugLog from "~/components/pedagogy/DebugLog.astro";
import DesignPrompt from "~/components/pedagogy/DesignPrompt.astro";
import RFCQuiz from "~/components/pedagogy/RFCQuiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook>
A team fixes LCP by inlining the full critical CSS bundle — LCP render delay drops 400 ms. But TTFB rises 60 ms because the HTML is now 40 KB larger. A separate team lazy-loads every non-hero image to cut the bundle — LCP stays fine, but the hero was accidentally caught and LCP regresses 800 ms. Another team ships a full SPA and gets green vitals on the first load — but every client-side route change is invisible to the metrics. One budget, three metrics, four teams, and no single knob.
</Hook>

<Crux>LCP, INP, and CLS share a performance budget — every change spends from it; measure all three before and after, never one in isolation.</Crux>

<Explanation>

**The metrics are not independent dials.**

Fixing one Core Web Vital can hurt another, and the senior move is to see the system, not individual metrics. Common tradeoffs:

- **Inlining a large critical CSS block** helps LCP render delay (no render-blocking request) but bloats the HTML, hurting TTFB. The net effect depends on connection speed and HTML size.
- **Lazy-loading everything** to shrink the bundle helps INP (less JS to parse and execute) but, applied accidentally to the hero image, adds a large load delay and wrecks LCP.
- **Reserving generous space for CLS** — large `min-height` containers for ads — can push the LCP element below the fold so it is no longer the LCP candidate. Sometimes that is fine (the new LCP candidate is already fast). Sometimes it is not.
- **Shipping a large interactive framework** for a snappy feel adds hydration — one large long task that spikes INP for early interactions. The same feature simultaneously hurts INP and can hurt LCP (JS blocks the render-blocking parse path).

There is no single knob. The discipline: measure all three before and after any change, in the field where possible, in a throttled lab at minimum. A change that improves one metric at the cost of another may or may not be worth it — but you cannot know unless you look at all three.

**INP attribution in the field: from metric to line of code.**

A field INP number alone is not actionable — it is a latency with no cause attached. The attribution chain:

1. `PerformanceObserver` subscribed to `event` entries gives you, per interaction, the input delay / processing time / presentation delay split.
2. Long Animation Frames (`long-animation-frame` entries via `PerformanceObserver`) give you, for the frame where a slow interaction landed, the array of scripts that ran — with source URLs, function names, and durations.
3. Combine them: high input-delay INP → find the long task in the LoAF entry; high processing-time INP → the handler, attributed by LoAF; high presentation-delay INP → a heavy render or forced synchronous layout.

The whole point of wiring LoAF into RUM is to turn "p75 INP is 340 ms" into "the search filter function at search.js:88 is the dominant script in slow frames." That is actionable.

**LCP attribution in the field.**

The web-vitals library's `onLCP` callback delivers not just the LCP time but the *element* (so you know what the browser picked as LCP), the *URL* of the resource (if it is an image), and the four-phase breakdown: TTFB, load delay, load time, render delay. Logging these to telemetry means a regression from "LCP went from 1.9 s to 3.8 s" immediately surfaces which phase grew — was it TTFB (a slow deploy), load delay (an accidental `loading="lazy"`), load time (an unoptimised image), or render delay (a new render-blocking script)?

<NumbersCard
  id="card-rum-attribution"
  title="Attributed field sample from web-vitals RUM"
  stats={[
    { label: "LCP element", value: "img.hero" },
    { label: "LCP phase: load time", value: "3510 ms (dominant — 4 MB JPEG)" },
    { label: "LCP phase: TTFB + delay + render", value: "610 ms combined" },
    { label: "INP", value: "38 ms — good" },
    { label: "CLS", value: "0.02 — good" },
  ]}
  data-lesson-visual
/>

**The RUM and CI loop — both halves required.**

The complete production observability setup has two halves.

*RUM*: ship the `web-vitals` library (or equivalent), which uses `PerformanceObserver` to capture LCP, INP, and CLS exactly as Chrome scores them, plus attribution (element, phase split, interaction target, shifting nodes). Send them to telemetry tagged by route, device class, country, and release. This is the real verdict — it catches regressions a lab never will, especially device-class regressions and interactions that only real users trigger.

*CI*: a synthetic gate — Lighthouse CI or a Playwright trace — that runs on every PR, throttled to a realistic mid-tier device, asserting budgets on LCP, total blocking time (the lab proxy for INP), and CLS, and failing the build on a regression.

Neither alone is enough. Lab without RUM ships regressions that only real devices reveal. RUM without a lab gate means every regression reaches production before anyone sees it.

**The soft-navigation gap — why SPA vitals need explicit instrumentation.**

Core Web Vitals were designed around full page loads. In a single-page app, the first load has a real LCP — but subsequent client-side route changes ("soft navigations") historically had no LCP measurement at all, because no document load event fires. To a user a soft navigation feels exactly like a page load — they clicked a link and expect new content fast — but the metric did not see it.

Chrome has been shipping soft-navigation support to attribute LCP and other vitals to client-side route changes, but coverage is still maturing and not all frameworks emit the right hints. The consequence: for a SPA, do not assume your vitals story is complete just because the initial-load numbers are green. Snappy first load + sluggish route transitions is a real, common, and historically under-measured failure. Instrument soft navigations explicitly with the `PerformanceObserver` soft-navigation entries or your own RUM marks on `router.beforeEach` / route change events.

<Inset kind="why" lang="en">
The CLS session window (worst 5-second cluster rather than lifetime sum) is one concrete example of the spec evolving to match real user experience rather than pure engineering measurement. The original lifetime-sum CLS unfairly penalised long-lived pages and infinite scroll — a shift that happened four minutes into a session counted the same as one that happened at second 2. The session window focuses the metric on concentrated bad behavior: a burst of shifts during ad reload or a batch of unsized images loading. It is more representative of what a user actually notices in context, and it changed how you reason about CLS: spreading shifts apart across windows can reduce the score even without fixing the root cause, but the root cause (unreserved content) still creates a bad experience.
</Inset>

<DebugLog
  id="sr-debug-1"
  pieceSlug="06-tradeoffs-and-observability"
  lang="en"
  label="web-vitals RUM — attributed field sample"
  output={`[LCP] value: 4120 ms  rating: poor
  element: img.hero
  url: /assets/hero-original.jpg  (3.8 MB, JPEG, 4000x3000)
  phase split: ttfb 280ms | loadDelay 90ms | loadTime 3510ms | renderDelay 240ms

[INP] value: 38 ms  rating: good
[CLS] value: 0.02  rating: good`}
  outputLang="log"
  question="Read the phase split and the element details — which phase dominates, what is the root cause, and what is the fix? What should the team NOT do, and why?"
  hint="Compare the four phase numbers. One is an order of magnitude larger than the others. Then look at the image's size and dimensions."
  answer="The phase split is decisive: loadTime is 3510 ms — ~85% of the 4120 ms LCP — while TTFB, loadDelay, and renderDelay together are under 700 ms. The dominant phase is resource load time: the LCP image itself takes 3.5 seconds to download. The element details show why: hero-original.jpg is 3.8 MB, a raw JPEG at 4000x3000 — a full-resolution photo served unprocessed, far larger than any viewport needs. The fix: serve a correctly sized image in AVIF or WebP (typically 30-50% smaller than JPEG at equal quality), with a srcset of widths for different viewports. That alone should cut loadTime from 3510 ms to a few hundred ms. What NOT to do: do not add a preload hint or fetchpriority='high'. LoadDelay is already only 90 ms — discovery is not the problem. Fixing the wrong phase is the classic LCP mistake; the phase split is what tells you which one to fix."
/>

<RFCQuiz
  id="sr-rfc-1"
  pieceSlug="06-tradeoffs-and-observability"
  lang="en"
  question="Which browser API is the basis for measuring LCP, INP, layout shifts, and Long Animation Frames in real-user monitoring?"
  choices={[
    { rfc: "PerformanceObserver", title: "Subscribes to performance entry types — largest-contentful-paint, event, layout-shift, long-animation-frame", correct: true },
    { rfc: "console.time", title: "A manual stopwatch for debugging — not a source of standardised vitals entries" },
    { rfc: "Date.now", title: "Wall-clock time; cannot observe paint, layout-shift, or interaction entries" },
    { rfc: "ResizeObserver", title: "Observes element size changes — not paint timing or interaction latency" },
  ]}
/>

<DesignPrompt
  id="sr-design-1"
  pieceSlug="06-tradeoffs-and-observability"
  lang="en"
  prompt="Design the Core Web Vitals strategy for a media site: article pages with a hero image, ads, embeds, and a comments section. Hit good LCP, INP, and CLS at p75 in the field, and keep them green over time."
  constraints={[
    "Hero image is the LCP element on every article.",
    "Ads and social embeds load late and into the article body.",
    "Comments section is interactive and below the fold.",
    "The site is server-rendered and hydrates.",
    "Targets: LCP ≤2.5 s, INP ≤200 ms, CLS ≤0.1 — all at field p75.",
    "Regressions must be caught before they reach production.",
  ]}
  canonical="LCP: put the hero image in the initial HTML as a plain <img> with explicit width/height and fetchpriority='high'; never lazy-load it; serve it sized and in AVIF/WebP via srcset; keep TTFB low with a CDN and, if SSR, keep slow data off the render path. CLS: give the hero and every in-body image explicit dimensions or aspect-ratio so space is reserved; wrap every ad and embed slot in a fixed min-height container; neutralise web-font reflow with size-adjust or font-display: optional; never insert content above what the user is reading. INP: the article text itself needs almost no JS — make it mostly static/Server Components so there is little to hydrate; the comments section is the main interactive surface, so make it an island that hydrates on scroll-into-view (it is below the fold) rather than part of eager full-page hydration that blocks early taps. Observability: ship the web-vitals library with attribution, send LCP/INP/CLS to RUM tagged by route, device class, country, and release — that is the field verdict. Add a Lighthouse CI gate on every PR, throttled to a mid-tier device, budgeting LCP, TBT, and CLS so regressions fail the build. Neither alone is enough."
  keyPoints={[
    "Hero LCP: in the HTML, sized, fetchpriority='high', never lazy, modern format.",
    "CLS: reserve space for everything that loads late — images, ads, embeds, fonts.",
    "INP: minimise hydration — static/Server Components for the article, comments as a scroll-hydrated island.",
    "RUM with web-vitals + attribution is the field verdict; a throttled CI gate catches regressions before production.",
    "Measure all three before and after every change — fixing one can break another.",
  ]}
/>

<Quiz
  id="sr-quiz-1"
  pieceSlug="06-tradeoffs-and-observability"
  lang="en"
  question="Why can a page show a great Lighthouse score but still be flagged for poor Core Web Vitals in Search Console?"
  choices={[
    { label: "Lighthouse is a lab test on a simulated device; Search Console reports field data from real users (CrUX) — only the field data drives ranking", correct: true },
    { label: "Search Console uses a stricter version of Lighthouse", misconception: "Search Console does not run Lighthouse at all for the vitals report — it reports CrUX field data. Lab and field are different data sources." },
    { label: "The Lighthouse score is cached and stale", misconception: "The discrepancy is not staleness — it is lab vs field. A fresh Lighthouse run on a fast simulated device can still disagree with real-user p75." },
    { label: "Search Console has a bug", misconception: "It is expected behaviour, not a bug. Lab and field measure different things; the field (CrUX) p75 is the ranking-relevant verdict." },
  ]}
/>

</Explanation>

<KeyTakeaway>LCP, INP, CLS share a budget — fix all three or create hidden regressions. RUM gives the field verdict (PerformanceObserver + web-vitals library); CI throttled Lighthouse catches regressions before production. Neither replaces the other.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-tradeoffs-and-observability-retrieval"
  lang="en"
  questions={[
    { q: "A page has poor LCP. Walk through how you diagnose it using the phase split and why reading the phase split is the key step.", a: "LCP decomposes into four phases: TTFB, resource load delay, resource load time, element render delay. Get the split from the web-vitals library's LCP attribution or a DevTools trace. Read which phase dominates. If TTFB: fix the server path (CDN, caching, move slow data off render path). If load delay: the image is discovered late — put it in the initial HTML with fetchpriority='high'. If load time: the image is too large — serve AVIF/WebP at the correct dimensions via srcset. If render delay: remove render-blocking CSS/JS. Applying the wrong fix (compressing the image when load delay dominates) wastes effort and leaves the number poor." },
    { q: "Explain the complete production observability setup for Core Web Vitals — what RUM provides, what CI provides, and why neither is sufficient alone.", a: "RUM: ship the web-vitals library, use PerformanceObserver to capture LCP, INP, CLS with attribution (element, phase split, interaction target, shifting nodes), send to telemetry tagged by route/device/country/release. This is the real verdict — catches regressions only real users and devices reveal, and confirms a lab fix actually moved the field number. CI: Lighthouse CI or Playwright trace on every PR, throttled to a mid-tier device, asserting budgets on LCP, TBT (lab proxy for INP), CLS, failing on regression. This catches obvious regressions before merge without waiting for real users. Neither alone: lab without RUM ships regressions real devices reveal; RUM without CI means every regression reaches production first." },
    { q: "Why are INP and the SSR/hydration material inseparable, and what does an early-only INP pattern tell you?", a: "Hydration runs as a large long task early in a server-rendered page's life. A user taps a button while hydration is running — the input waits behind the long task (input delay). INP records a large latency. The early-only pattern: INP is poor for interactions in the first few seconds, fine afterward. Once hydration finishes, the thread is free and handlers run fast. This diagnosis points at hydration, not handler code. Every hydration-reducing technique — code-splitting, Server Components, islands, progressive hydration — is also an INP fix." },
  ]}
/>

<Recap lang="en">
The three Core Web Vitals share a performance budget: inlining CSS helps LCP render delay but hurts TTFB; lazy-loading reduces bundle size but can add LCP load delay if accidentally applied to the hero; generous CLS space reservation can change the LCP candidate. Every change must be measured against all three metrics, not one in isolation. Complete production observability requires both RUM (web-vitals library + PerformanceObserver, sending attributed LCP/INP/CLS to telemetry tagged by route and device class) and a throttled CI gate (Lighthouse CI or Playwright, budgeting LCP/TBT/CLS per PR). RUM is the real verdict; CI catches regressions before they reach production. INP in SPAs is further complicated by soft navigations, which historically had no LCP attribution — instrument route changes explicitly. The field p75 from CrUX is the only number that determines ranking; a lab fix that does not move it did not help real users.
</Recap>
