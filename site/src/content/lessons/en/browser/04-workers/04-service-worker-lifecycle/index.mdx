---
concepts:
- service-worker
- install-event
- activate-event
- fetch-event
- cache-api
- skipWaiting
- cache-first
- network-first
- stale-while-revalidate
deepensInto:
- 06-service-worker-edge-cases
estMin: 15
lang: en
lessonType: topic
level: middle
order: 4
prereqs:
- browser/04-workers/01-what-workers-are
slug: 04-service-worker-lifecycle
sources:
- https://developer.mozilla.org/en-US/docs/Web/API/Service_Worker_API
- https://web.dev/articles/service-worker-lifecycle
- https://web.dev/articles/workbox
spiral:
- statefulness
status: ready
summary: 'Register → install → waiting → activate → fetch: the lifecycle that enables
  offline apps, instant repeat loads, and the waiting-state trap that surprises everyone
  the first time.'
title: Service worker lifecycle and cache strategies
track: browser
unit: 04-workers
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
You deploy a new service worker, reload the page, and still get the old behaviour. No error — the new worker installed fine. It is just waiting for you to close every tab.
</Hook>

<Crux>Service worker intercepts fetches; the waiting state blocks a new version from activating over open pages unless you call skipWaiting first.</Crux>

<Explanation>

## Registration and scope

`navigator.serviceWorker.register('sw.js')` starts the lifecycle. The scope defaults to the directory of `sw.js` — a worker at `/app/sw.js` controls all pages under `/app/`. You can narrow it: `register('sw.js', { scope: '/app/checkout/' })`.

A service worker has no DOM and no persistent global state — the browser kills and restarts it freely to save memory. All durable state lives in the **Cache API** or **IndexedDB**. It runs on its own thread, independent of any page — it can receive push notifications and run background sync even when no tab is open.

## The lifecycle

```
register → install → [waiting] → activate → idle ↔ running
```

**install** — fires when the browser parses the new worker for the first time. This is where you pre-cache the app shell:

```js
self.addEventListener('install', event => {
  event.waitUntil(
    caches.open('v1').then(cache => cache.addAll(['/app.js', '/index.html']))
  );
});
```

`event.waitUntil(promise)` tells the browser: do not advance the lifecycle until this promise settles. Forget it and the browser may kill the worker mid-cache-population.

**waiting** — a newly installed worker does not take control of already-open pages by default. It waits until every tab using the old worker closes. This prevents a page from running old HTML with new cached assets.

**activate** — fires after waiting. This is where you delete old caches:

```js
self.addEventListener('activate', event => {
  event.waitUntil(
    caches.keys().then(keys =>
      Promise.all(keys.filter(k => k !== 'v1').map(k => caches.delete(k)))
    )
  );
});
```

**fetch** — intercepts every network request in scope. `event.respondWith(response)` determines what the page receives.

<FlowDiagram
  label="The service worker lifecycle as an ordered state machine: install fires, then the worker sits installed (waiting), then activate fires, then it is activated and handling fetch, and finally it becomes redundant when superseded."
  hue="var(--d-frontend)"
  perRow={5}
  nodes={[
    { id: "install", label: "install", sub: "pre-cache shell" },
    { id: "waiting", label: "installed", sub: "(waiting)" },
    { id: "activate", label: "activate", sub: "clean caches" },
    { id: "activated", label: "activated", sub: "handles fetch" },
    { id: "redundant", label: "redundant", sub: "superseded" },
  ]}
  edges={[
    { from: "install", to: "waiting" },
    { from: "waiting", to: "activate", label: "tabs close / skipWaiting" },
    { from: "activate", to: "activated" },
    { from: "activated", to: "redundant" },
  ]}
  caption="Always this order. The waiting → activate transition is the trap: by default it only fires once every old tab closes — skipWaiting() jumps it. When a newer worker takes over, the old activated worker becomes redundant."
/>

<NumbersCard
  id="card-sw-lifecycle"
  title="Service worker lifecycle states"
  data-lesson-visual
  stats={[
    { label: "install → activate", value: "Only after all old tabs close (without skipWaiting)" },
    { label: "Idle worker killed by browser", value: "Seconds after last event" },
    { label: "State check (DevTools)", value: "Application → Service Workers panel" },
    { label: "sw.js cache by browser (default)", value: "Up to 24 hours — use Cache-Control: no-cache" },
  ]}
/>

## skipWaiting + clients.claim()

To activate immediately without waiting for tabs to close:

```js
// install handler
self.skipWaiting();

// activate handler
self.addEventListener('activate', event => {
  event.waitUntil(clients.claim());
});
```

`skipWaiting()` — new worker activates immediately, even with open pages.
`clients.claim()` — new worker takes control of all open pages in its scope.

Use this deliberately. Without content-hashed asset filenames, `skipWaiting` is how you ship the version-mismatch failure mode.

## Cache strategies

Inside the `fetch` handler you pick a strategy per request type:

**Cache-first** — check cache, fall back to network. Right for immutable, content-hashed static assets (`app.4f3a1c.js`):
```js
event.respondWith(caches.match(event.request).then(r => r || fetch(event.request)));
```

**Network-first** — try network, fall back to cache on failure. Right for API responses that should be fresh but must degrade gracefully offline.

**Stale-while-revalidate** — serve the cached copy immediately, fetch a fresh copy in the background to update the cache for next time. Right for content that can be slightly stale (avatars, feeds).

**Network-only** and **cache-only** are the degenerate ends.

Libraries like Workbox give these strategies declaratively with route matching, but the strategies themselves are ten lines of `fetch`-handler code.

<DragOrder
  id="mid-drag-sw-lifecycle-1"
  lessonSlug="04-service-worker-lifecycle"
  lang="en"
  prompt="Order the service worker lifecycle events from registration to steady state."
  items={[
    { id: "register", label: "register(): browser downloads and parses sw.js" },
    { id: "install", label: "install event: pre-cache the app shell" },
    { id: "wait", label: "waiting: new worker idles until old pages close (unless skipWaiting)" },
    { id: "activate", label: "activate event: clean up old caches" },
    { id: "fetch", label: "fetch events: intercept and respond to requests" },
  ]}
  correctOrder={["register", "install", "wait", "activate", "fetch"]}
/>

<Quiz
  id="mid-quiz-sw-1"
  lessonSlug="04-service-worker-lifecycle"
  lang="en"
  question="You deploy a new service worker, reload the page, and still get the old behaviour. Why?"
  choices={[
    { label: "The new worker is in the 'waiting' state — it won't take control until every tab using the old worker is closed, unless you call skipWaiting()", correct: true },
    { label: "Service workers cache themselves and ignore new versions", misconception: "The browser does fetch and install the new worker. It just does not activate it over open pages by default — that is the waiting state, not a caching bug." },
    { label: "You must unregister the old worker manually every deploy", misconception: "Manual unregister is never required. skipWaiting() + clients.claim() handles the handoff; without them, closing all tabs does too." },
    { label: "Reload does not re-check the service worker", misconception: "The browser re-checks the sw.js file on navigation. The new worker installs — it just waits to activate." },
  ]}
/>

<Quiz
  id="mid-quiz-sw-2"
  lessonSlug="04-service-worker-lifecycle"
  lang="en"
  question="Which job is a service worker the right tool for?"
  choices={[
    { label: "Serving the app shell from cache so a repeat visit loads instantly and works offline", correct: true },
    { label: "Running a CPU-heavy image filter without freezing the page", misconception: "That is a web worker's job — pure compute offload. A service worker is a network proxy, not a compute thread." },
    { label: "Animating a transform smoothly while the main thread is blocked", misconception: "That is an animation worklet (or a compositor-driven CSS animation). Service workers do not touch rendering." },
    { label: "Sharing a memory buffer between two threads", misconception: "That is SharedArrayBuffer + Atomics. Service workers communicate by messages and the Cache API, not shared memory." },
  ]}
/>

<TraceScenario
  id="mid-trace-sw-1"
  lessonSlug="04-service-worker-lifecycle"
  lang="en"
  scenario="A page enables COOP + COEP to use multithreaded WASM. After deploy, the WASM module fails to start and the page is missing its hero image and a third-party analytics script. What happened?"
  steps={[
    { prompt: "COEP: require-corp blocked the cross-origin hero image and analytics script because they do not send Cross-Origin-Resource-Policy headers", reveal: "Correct. COEP requires every cross-origin subresource to opt in via CORP (or CORS). The hero image on a CDN and the third-party analytics script were not configured for it, so the browser blocked them. The WASM failure is a knock-on: if COEP blocks a required asset the page may not reach crossOriginIsolated state cleanly, or the team disabled COEP to unbreak the image and lost SAB with it." },
    { prompt: "The WASM binary is corrupt", reveal: "The WASM binary is fine — the failure is environmental. crossOriginIsolated is false because COEP was either not fully applied or was rolled back, so SharedArrayBuffer is undefined and pthreads cannot initialise." },
    { prompt: "The browser does not support SharedArrayBuffer", reveal: "All modern browsers support SAB — but only under cross-origin isolation. Support is not the issue; the COOP/COEP gate is." },
    { prompt: "The service worker cached a stale WASM file", reveal: "A stale cache would load an old-but-working module, not fail to start. The symptom — missing cross-origin assets — points squarely at COEP." },
  ]}
/>

<Inset kind="why" lang="en">
Why does the waiting state exist? Consider: you deploy version N+1 of your app. An open tab loaded version N's HTML and has version N's worker. If the new worker activated immediately, it would start serving version N+1's cached assets to a page that was expecting version N's assets — a version mismatch that could break the page mid-session. The waiting state prevents this: a user finishing work in an open tab will complete their session with a consistent asset set. `skipWaiting` is the escape hatch when you are confident your assets are content-hashed (old and new can coexist) and you want the new worker active immediately.
</Inset>

</Explanation>

<KeyTakeaway>A service worker intercepts all fetch events in its scope after activation; a new version waits until all old tabs close before activating. skipWaiting + clients.claim activates immediately. Cache strategies — cache-first, network-first, stale-while-revalidate — are ten lines of fetch-handler code.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="04-service-worker-lifecycle-retrieval"
  lang="en"
  questions={[
    { q: "Walk through the service worker lifecycle from register() to handling fetch events.", a: "register() → browser downloads and parses sw.js → install event (pre-cache app shell, waitUntil) → waiting state (new worker idles until old pages close, unless skipWaiting) → activate event (clean up old caches, clients.claim) → idle/running cycle responding to fetch, push, and message events." },
    { q: "What is the waiting state and how do you override it?", a: "A newly installed service worker does not take control of already-open pages — it waits in 'waiting' until all tabs using the old worker close. This prevents version skew mid-session. To override: call self.skipWaiting() in the install handler and clients.claim() in the activate handler. Use deliberately — without content-hashed assets, immediate activation causes module-mismatch bugs." },
    { q: "What are the three main cache strategies and when do you use each?", a: "Cache-first: serve from cache, fall back to network — for immutable content-hashed assets. Network-first: try network, fall back to cache — for API responses that must be fresh but degrade gracefully offline. Stale-while-revalidate: serve cached copy instantly, refresh cache in background — for content that can be slightly stale (avatars, feeds)." },
  ]}
/>

<Recap lang="en">
A service worker intercepts every fetch request in its scope once activated. The lifecycle is: register, install (pre-cache), waiting (new worker idles until old tabs close), activate (clean old caches), then the idle/running fetch loop. `event.waitUntil(promise)` in any handler prevents the browser from killing the worker mid-operation. `skipWaiting()` + `clients.claim()` overrides the waiting state, activating immediately — safe only with content-hashed assets. Service workers have no persistent global state; the browser kills them aggressively and restarts on the next event, so all durable state must live in the Cache API or IndexedDB.
</Recap>
