---
concepts:
- structured-clone
- transferable
- ArrayBuffer
- postMessage
- clone-cost
- ImageBitmap
deepensInto:
- 05-shared-array-buffer-and-atomics
- 07-worker-pools-and-observability
estMin: 12
lang: en
lessonType: topic
level: middle
order: 3
prereqs:
- browser/04-workers/01-what-workers-are
- browser/04-workers/02-web-worker-mechanics
slug: 03-structured-clone-and-transfer
sources:
- https://html.spec.whatwg.org/multipage/structured-data.html
- https://developer.mozilla.org/en-US/docs/Web/API/Web_Workers_API/Transferable_objects
- https://developer.mozilla.org/en-US/docs/Web/API/structuredClone
spiral:
- encapsulation
status: ready
summary: postMessage deep-copies data via structured clone at ~1 ms/MB — transferables
  move ArrayBuffers in O(1) by handing ownership to the receiving thread.
title: Structured clone and transferables
track: browser
unit: 04-workers
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumberDrill from "~/components/pedagogy/NumberDrill.astro";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import RFCQuiz from "~/components/pedagogy/RFCQuiz.astro";

<Hook>
You move a 40 MB image-processing job to a worker — and the main thread still freezes for 40 ms before the worker even starts. The work moved, but the data did not.
</Hook>

<Crux>postMessage copies data via structured clone; transferring an ArrayBuffer moves ownership in O(1) and makes the sender's reference unusable.</Crux>

<Explanation>

## The structured clone algorithm

`postMessage(data)` does not share `data` — it deep-copies it using the **structured clone algorithm**. Structured clone handles objects, arrays, Maps, Sets, Dates, typed arrays, Blobs, and ArrayBuffers. It does **not** handle:
- Functions (dropped silently)
- DOM nodes (throws)
- Class prototypes — methods are lost, only data properties survive

The copy is synchronous on the sending thread and scales with payload size: roughly **1 ms per MB** for plain objects, more for deeply nested structures. Send a 50 MB object and you have blocked the sending thread for ~50 ms — you moved the work, but paid a clone tax to do it.

<NumbersCard
  id="card-clone-cost"
  title="Structured clone cost"
  data-lesson-visual
  stats={[
    { label: "Plain object clone rate", value: "~1 ms / MB" },
    { label: "30 MB object clone", value: "~30 ms sender block" },
    { label: "Transfer (ArrayBuffer)", value: "O(1), microseconds" },
    { label: "Transferables: ArrayBuffer", value: "Detaches sender (byteLength → 0)" },
    { label: "Transferables: ImageBitmap", value: "Transferred, source becomes closed" },
    { label: "Transferables: OffscreenCanvas", value: "Exclusive ownership moves" },
  ]}
/>

## Transferables: O(1) ownership handoff

The escape from clone cost is the **transferable** mechanism. Pass a second argument to `postMessage` — a list of transferable objects:

```js
postMessage({ pixels, width, height }, [pixels.buffer]);
```

Instead of copying the bytes, the browser hands the underlying memory to the receiving thread and **detaches** it from the sender. After the transfer:
- `pixels.buffer.byteLength` is `0` on the sender
- The receiver has a new view with the full data

The cost drops from O(size) to O(1). Transfer is the right move for large binary payloads: image bitmaps, audio buffers, file contents.

**Transferable types:** `ArrayBuffer`, `MessagePort`, `ImageBitmap`, `OffscreenCanvas`, `ReadableStream`, `WritableStream`, `TransformStream`.

## What gets dropped in structured clone

When you postMessage an object that had methods:

```js
class Point {
  constructor(x, y) { this.x = x; this.y = y; }
  distanceTo(other) { ... }
}
const p = new Point(1, 2);
worker.postMessage(p);
// Worker receives: { x: 1, y: 2 } — no distanceTo method
```

Structured clone copies data properties but has no representation for functions or prototype chains. This is by design: functions reference closures that may hold DOM nodes or other main-thread-only state. The rule: serialize data, not behaviour. Reconstruct the class on the receiving side if needed.

<Quiz
  id="mid-quiz-clone-1"
  lessonSlug="03-structured-clone-and-transfer"
  lang="en"
  question="You `postMessage` a 40 MB Float32Array to a worker and the sending thread freezes for ~40 ms. What is the fix?"
  choices={[
    { label: "Transfer the buffer: `postMessage(arr, [arr.buffer])` — ownership moves in O(1) instead of a 40 MB copy", correct: true },
    { label: "Send it as JSON instead", misconception: "JSON.stringify of a 40 MB typed array is far slower than structured clone and produces a much larger string. This makes it worse, not better." },
    { label: "Split it into 40 separate 1 MB postMessage calls", misconception: "Total clone cost is the same 40 MB, now spread across 40 task hops with 40× the scheduling overhead. Transfer eliminates the copy entirely." },
    { label: "Compress it with gzip before sending", misconception: "Compression itself is CPU work on the sending thread, plus decompression on the worker. Transfer is free; compression is not." },
  ]}
/>

<Quiz
  id="mid-quiz-clone-2"
  lessonSlug="03-structured-clone-and-transfer"
  lang="en"
  question="A worker's `onmessage` receives an object that had methods on it before being sent. The methods are gone. Why?"
  choices={[
    { label: "Structured clone copies data only — prototypes and functions are not cloneable, so methods are dropped", correct: true },
    { label: "The worker stripped them for security", misconception: "There is no security stripping. Structured clone simply has no representation for functions or prototype chains — they cannot cross the boundary." },
    { label: "The methods are there but lazily loaded", misconception: "Nothing is lazy here. The clone produced a plain object with the data properties only; the methods never crossed." },
    { label: "postMessage only sends JSON", misconception: "postMessage uses structured clone, which is richer than JSON (it handles Maps, Dates, ArrayBuffers). But functions are outside both." },
  ]}
/>

<MetaphorComplete
  id="mid-metaphor-clone-1"
  lessonSlug="03-structured-clone-and-transfer"
  lang="en"
  setup="When you postMessage a large ArrayBuffer, the default deep-copies it (the structured clone tax). The alternative moves ownership of the buffer to the receiving thread in constant time, leaving it unusable on the sender. What is that alternative called?"
  accepted={["transfer", "transferable", "transferables", "transferable objects", "transfer the buffer", "transferring"]}
  canonical="transfer"
  explanation="Transferring passes the second argument to postMessage — a list of transferable objects: `postMessage(data, [data.buffer])`. Instead of copying the bytes, the browser hands the underlying memory to the receiving thread and detaches it from the sender (byteLength becomes 0). The cost drops from O(size) to O(1). Transferables include ArrayBuffer, MessagePort, ImageBitmap, and OffscreenCanvas — all the heavy binary types where a copy would be expensive."
/>

<NumberDrill
  id="mid-drill-clone-1"
  lessonSlug="03-structured-clone-and-transfer"
  lang="en"
  question="Structured clone of a plain object costs roughly 1 ms per MB. A worker is sent a 30 MB object by value. Roughly how many milliseconds does the sending thread block on the clone?"
  expected={30}
  tolerance={5}
  unit="ms"
  explanation="At ~1 ms/MB, a 30 MB structured clone blocks the sender for ~30 ms — more than half a 60 fps frame budget multiple times over, and itself a near-long-task. This is the hidden cost of 'just move it to a worker': the clone runs synchronously on the thread you were trying to unblock. Transfer the buffer instead, or use SharedArrayBuffer, and the 30 ms disappears."
/>

<FadedExample
  client:visible
  id="sr-faded-clone-1"
  lessonSlug="03-structured-clone-and-transfer"
  lang="en"
  title="Transfer a buffer to a worker instead of cloning it"
  steps={{
    solved: (
      <pre><code>{`// main.js
const worker = new Worker('process.js');
const pixels = new Uint8ClampedArray(width * height * 4);
// Second arg transfers the buffer: O(1), no copy.
// pixels.buffer is detached here afterwards.
worker.postMessage({ pixels, width, height }, [pixels.buffer]);

// process.js
self.onmessage = (e) => {
  const { pixels, width, height } = e.data;
  applyFilter(pixels, width, height);
  // Transfer it back the same way.
  self.postMessage({ pixels }, [pixels.buffer]);
};`}</code></pre>
    ),
    semi: {
      prompt: (
        <p>Fill in the transfer list argument on both postMessage calls so the pixel buffer moves without a copy.</p>
      ),
      blanks: [
        { id: "send", expected: /\[\s*pixels\.buffer\s*\]/i, placeholder: "transfer list for send" },
        { id: "back", expected: /\[\s*pixels\.buffer\s*\]/i, placeholder: "transfer list for return" },
      ],
    },
    blank: {
      prompt: (
        <p>You post a multi-MB <code>Uint8ClampedArray</code> of pixels to a worker by value, and the sending thread blocks for ~25 ms on the structured clone. Rewrite the postMessage calls so the buffer moves in O(1). What is the structural change?</p>
      ),
      reveal: (
        <p>Pass a second argument to <code>postMessage</code> — a transfer list containing the array's underlying <code>ArrayBuffer</code>: <code>postMessage(&#123; pixels &#125;, [pixels.buffer])</code>. The browser hands the memory to the receiving thread instead of copying it; the buffer is detached on the sender (its <code>byteLength</code> becomes 0). Do the same on the worker's reply. The clone cost drops from O(size) to O(1) in both directions. The constraint: the sender must not use the buffer after transferring it — if it still needs the data, transfer is the wrong tool and SharedArrayBuffer is the right one.</p>
      ),
    },
  }}
/>

<RFCQuiz
  id="sr-rfc-clone-1"
  lessonSlug="03-structured-clone-and-transfer"
  lang="en"
  question="Which specification defines the structured clone algorithm used by postMessage, IndexedDB, and the Cache API?"
  choices={[
    { rfc: "HTML Living Standard § structured data", title: "Defines StructuredSerialize / StructuredDeserialize and the transferable-objects mechanism", correct: true },
    { rfc: "ECMA-262 (JavaScript)", title: "Defines objects and the language, but not the cross-realm serialization algorithm" },
    { rfc: "W3C Service Workers", title: "Uses structured clone for messages but does not define the algorithm" },
    { rfc: "WHATWG Streams", title: "Defines ReadableStream / WritableStream, unrelated to structured clone" },
  ]}
/>

<Inset kind="why" lang="en">
Why is structured clone synchronous and why does it block the sender? Because the JavaScript specification guarantees that a postMessage call is a single, atomic operation — the data is fully serialized before the function returns. If serialization were asynchronous, you could mutate the data between the postMessage call and the point where the worker reads it, breaking isolation. The synchronous cost is the price of the isolation guarantee. The transfer mechanism gets around this by moving a memory pointer instead of bytes — the OS can hand ownership of a memory region atomically without copying.
</Inset>

</Explanation>

<KeyTakeaway>Structured clone deep-copies data at ~1 ms/MB — free for small objects, expensive for large buffers. Transfer an ArrayBuffer to move ownership in O(1): the sender's reference is detached, the receiver gets the bytes. Use transfer for all large binary payloads; use SharedArrayBuffer when both threads need concurrent access.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-structured-clone-and-transfer-retrieval"
  lang="en"
  questions={[
    { q: "What does structured clone handle and what does it drop?", a: "Handles: objects, arrays, Maps, Sets, Dates, typed arrays, Blobs, ArrayBuffers. Drops: functions (silently), DOM nodes (throws), class prototype methods (only data properties survive). The algorithm is defined in the HTML Living Standard § structured data." },
    { q: "How do you transfer an ArrayBuffer instead of cloning it, and what happens to the sender's reference?", a: "Pass the buffer in the second argument (transfer list): postMessage({ pixels }, [pixels.buffer]). The browser hands the memory to the receiving thread in O(1). The sender's buffer is detached — its byteLength becomes 0 and any access throws. The receiver has a new view with the full data." },
    { q: "When should you use SharedArrayBuffer instead of transfer?", a: "When both threads need concurrent access to the same data — for example, a WASM thread pool where the main thread and worker threads all read/write the same frame buffer. Transfer moves exclusive ownership; SharedArrayBuffer shares it. SharedArrayBuffer requires cross-origin isolation (COOP + COEP headers)." },
  ]}
/>

<Recap lang="en">
`postMessage` deep-copies data via the structured clone algorithm at ~1 ms per MB — a 30 MB object blocks the sending thread for ~30 ms, erasing the benefit of moving work off the main thread. The fix is the transferable mechanism: pass the ArrayBuffer in postMessage's second argument and the browser hands ownership to the receiver in O(1), detaching the sender's reference. Functions and prototype methods cannot cross the boundary — structured clone is a data serializer, not an object copier. For cases where both threads need concurrent access to the same memory, SharedArrayBuffer is the answer, but it requires cross-origin isolation.
</Recap>
