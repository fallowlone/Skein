---
slug: 01-what-workers-are
lang: en
track: browser
unit: 04-workers
order: 1
title: "What workers are and why they exist"
summary: "Workers run JavaScript off the main thread — web workers compute, service workers proxy the network, each with a hard boundary the DOM cannot cross."
estMin: 10
status: ready
lessonType: topic
level: junior
prereqs: ["browser/01-event-loop/01-loop-model"]
deepensInto: ["02-web-worker-mechanics", "04-service-worker-lifecycle"]
spiral: ["encapsulation"]
concepts: ["web-worker", "service-worker", "main-thread", "postMessage", "DOM-boundary"]
sources:
  - https://developer.mozilla.org/en-US/docs/Web/API/Web_Workers_API
  - https://developer.mozilla.org/en-US/docs/Web/API/Service_Worker_API
  - https://web.dev/articles/service-worker-lifecycle
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumberDrill from "~/components/pedagogy/NumberDrill.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";
import PersonaTag from "~/components/pedagogy/PersonaTag.astro";

<Hook>
Parse a 5 MB JSON file on the main thread and the page freezes for ~150 ms — dropped frames, dead buttons. The browser's answer is not "make the thread faster." It is "give you more threads."
</Hook>

<Crux>Workers run JS off the main thread: web workers compute, service workers proxy the network — neither can touch the DOM.</Crux>

<Explanation>

## The main thread problem

The browser has one thread for JavaScript, layout, paint, and input. While it is busy, nothing else runs. Parse a large file, run a crypto hash, or filter a huge dataset on the main thread and every animation and button response stalls for the duration.

The browser's answer is off-main-thread execution — **workers**. Four kinds exist, each for a different job:

- **Web worker** — a second JS thread for CPU-bound work (parsing, crypto, image processing). No DOM, no `window`, no `document`.
- **Service worker** — a network proxy sitting between the page and the server. Intercepts requests, serves cached responses, survives page close.
- **Worklet** — a tiny hook into the browser's render or audio pipeline (paint worklet, audio worklet). Even more constrained than a worker.
- **SharedArrayBuffer** — shared memory between threads (not a worker itself, but the escape hatch from message-passing). Gated behind cross-origin isolation headers.

<NumbersCard
  id="card-workers-intro-numbers"
  title="Workers at a glance"
  data-lesson-visual
  stats={[
    { label: "Web worker startup", value: "5–20 ms" },
    { label: "5 MB JSON parse, main thread", value: "~150 ms freeze" },
    { label: "5 MB JSON parse, web worker", value: "0 ms jank" },
    { label: "Frame budget at 60 fps", value: "16.67 ms" },
    { label: "Frames dropped by 150 ms parse", value: "~9 frames" },
  ]}
/>

## The office metaphor

The main thread is you at your desk: you answer the phone (input) and update the whiteboard (DOM) yourself. A **web worker** is a colleague in a back room — you send a big calculation by note, they do it without disturbing your desk, they send the answer back by note. They cannot touch your whiteboard. Only you can.

A **service worker** is the mailroom: every letter in or out passes through it first, and it can answer some from its own filing cabinet without going outside. It keeps running even when you leave for the day.

<PersonaTag id="bea" lang="en" /> uploads a photo to edit. <PersonaTag id="sven" lang="en" /> narrates: "Applying the filter is millions of pixel operations — on the main thread, a 600 ms freeze. So we post the image data to a web worker. The worker churns the pixels on its own thread; the main thread stays free. It posts the result back and we paint it."

## The fundamental rule

Workers have **no DOM**. A worker's global is `WorkerGlobalScope` — it has no `document`, no `window`, no elements. This is not a limitation to work around; it is the design. It keeps the DOM single-owner and race-condition-free. A worker can compute *what* to render and post the result back, but the main thread must apply it.

The one exception: `OffscreenCanvas`. A canvas bitmap is not part of the DOM tree — it is just a pixel buffer. You can transfer an `OffscreenCanvas` to a worker, and the worker can draw into it entirely off the main thread. Everything else in the DOM stays main-thread only.

<DragOrder
  id="jr-drag-1"
  lessonSlug="01-what-workers-are"
  lang="en"
  prompt="A page hands a heavy calculation to a web worker. Drag the steps into the order they happen."
  items={[
    { id: "create", label: "Main thread creates the worker: new Worker('calc.js')" },
    { id: "post", label: "Main thread sends input data: worker.postMessage(data)" },
    { id: "compute", label: "Worker runs the calculation on its own thread" },
    { id: "reply", label: "Worker sends the result back: postMessage(result)" },
    { id: "receive", label: "Main thread's onmessage handler receives the result" },
  ]}
  correctOrder={["create", "post", "compute", "reply", "receive"]}
/>

<Quiz
  id="jr-quiz-1"
  lessonSlug="01-what-workers-are"
  lang="en"
  question="Why can't a web worker update the page directly?"
  choices={[
    { label: "Workers have no access to the DOM — only the main thread can touch the document", correct: true },
    { label: "Workers are too slow to update the DOM", misconception: "Speed is not the reason. The DOM is simply not exposed in a worker's global scope by design — to keep it single-owner." },
    { label: "Workers can update the DOM but only once per second", misconception: "There is no rate limit. The DOM is entirely absent from a worker — there is nothing to rate-limit." },
    { label: "Workers can update the DOM after asking permission", misconception: "There is no permission to ask. A worker's global is WorkerGlobalScope, which has no document at all." },
  ]}
/>

<Quiz
  id="jr-quiz-2"
  lessonSlug="01-what-workers-are"
  lang="en"
  question="What makes a service worker different from a web worker?"
  choices={[
    { label: "A service worker sits between the page and the network and can outlive the page; a web worker is just an extra compute thread tied to the page", correct: true },
    { label: "A service worker is faster", misconception: "Speed is not the distinction. They do entirely different jobs — one is a network proxy, the other a compute thread." },
    { label: "A service worker can touch the DOM", misconception: "Neither can touch the DOM. Both run in a worker global scope with no document." },
    { label: "A web worker works offline, a service worker does not", misconception: "It is the reverse. The service worker is the one that enables offline by serving cached responses." },
  ]}
/>

<MetaphorComplete
  id="jr-metaphor-1"
  lessonSlug="01-what-workers-are"
  lang="en"
  setup="In the office metaphor, the colleague in the back room does heavy calculations for you and reports back by note, but cannot touch your whiteboard. Which kind of worker is that?"
  accepted={["web worker", "web workers", "worker", "dedicated worker", "a web worker"]}
  canonical="web worker"
  explanation="A web worker is a second JavaScript thread dedicated to compute. You hand it data through postMessage, it processes on its own thread without blocking yours, and it sends the result back. It has no DOM, no document, no window — that is the 'cannot touch your whiteboard' rule. Only the main thread owns the DOM, which keeps the document single-owner and free of race conditions."
/>

<NumberDrill
  id="jr-drill-1"
  lessonSlug="01-what-workers-are"
  lang="en"
  question="A page parses a 5 MB JSON file on the main thread and it takes roughly 150 ms. The frame budget at 60 fps is 16.67 ms. Roughly how many frames does that one parse drop?"
  expected={9}
  tolerance={1}
  unit="frames"
  explanation="150 ms divided by 16.67 ms per frame ≈ 9 frames. For ~9 frames in a row the page cannot paint or respond — a visible quarter-second freeze. Moving the parse to a web worker keeps all 9 frames on time: the main thread stays free while the worker does the parse on its own thread."
/>

<Inset kind="why" lang="en">
Workers were introduced because JavaScript is single-threaded by design — sharing mutable state across threads requires locks, and locks in a UI context cause deadlocks and priority inversion. The worker model avoids all of that by banning shared state entirely. Each worker has its own heap, its own globals, its own event loop. Data crosses the boundary only through postMessage (which copies it) or through explicit shared-memory primitives (SharedArrayBuffer + Atomics). The constraint is the safety guarantee.
</Inset>

</Explanation>

<KeyTakeaway>Web workers offload CPU work, service workers proxy the network — both are isolated threads with no DOM access. PostMessage is the only bridge, keeping the DOM single-owner and race-free.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-what-workers-are-retrieval"
  lang="en"
  questions={[
    { q: "What is the fundamental difference between a web worker and a service worker?", a: "A web worker is a compute thread tied to the page that created it — it runs CPU-bound JS without blocking the main thread. A service worker is a network proxy that intercepts all fetch requests from pages in its scope, can serve cached responses, and outlives the page." },
    { q: "Why do workers have no DOM access?", a: "The DOM is single-owner by design — only the main thread can mutate it. If workers could write the DOM concurrently, you would need locks and face race conditions. The constraint is the safety guarantee: workers compute and post results; the main thread applies them." },
    { q: "What is the one DOM-related exception for workers?", a: "OffscreenCanvas. A canvas bitmap is a pixel buffer, not part of the DOM tree. You can transfer an OffscreenCanvas to a worker, and the worker draws into it on its own thread — the browser reflects the result into the visible canvas element on the main thread." },
  ]}
/>

<Recap lang="en">
The browser's main thread is the single owner of JS execution, layout, paint, and input — anything blocking it stalls the whole page. Workers provide additional threads: web workers for CPU-bound computation (no DOM, communicates via postMessage), service workers for network proxying (intercept fetch, serve cache, survive page close). Neither can touch the DOM — that constraint is what makes them safe. A 150 ms JSON parse on the main thread drops ~9 frames at 60 fps; moved to a web worker, it costs the main thread nothing.
</Recap>
