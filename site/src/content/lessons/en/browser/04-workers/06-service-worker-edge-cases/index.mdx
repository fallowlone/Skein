---
slug: 06-service-worker-edge-cases
lang: en
track: browser
unit: 04-workers
order: 6
title: "Service worker edge cases: version skew, durability, and navigation traps"
summary: "Version skew from content-unhashed assets, the kill-and-restart durability trap, and why a broken navigation-intercepting service worker is a stop-the-deploy incident."
estMin: 16
status: ready
lessonType: topic
level: senior
prereqs: ["browser/04-workers/04-service-worker-lifecycle"]
deepensInto: []
spiral: ["statefulness"]
concepts: ["version-skew", "service-worker-durability", "navigation-interception", "app-shell", "kill-switch", "event-waitUntil", "cache-versioning"]
sources:
  - https://web.dev/articles/service-worker-lifecycle
  - https://developer.mozilla.org/en-US/docs/Web/API/Service_Worker_API
  - https://web.dev/articles/workbox
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook>
You ship a service worker that serves the app shell cache-first. A week later you push a bug fix. Some users never get it — they keep hitting the broken cached shell on every reload, and there is no recovery button for an ordinary user.
</Hook>

<Crux>A navigation-intercepting service worker is a persistent proxy — a deployed bug stops the release because users cannot reload out of it.</Crux>

<Explanation>

## The update-and-version-skew problem

A service worker's asset cache is versioned to its own code. When you deploy version N+1, an open page may be running version N's worker while N+1's HTML has shipped — or the reverse. If the worker serves cache-first `app.js` from version N while the HTML expects version N+1's `app.js`, you get a runtime error from mismatched modules.

**The robust pattern:**

1. **Content-hash every asset filename** (`app.4f3a1c.js`). Old and new assets coexist in the cache with no collision.
2. **Version-tag cache names** (`cache-v3`, `cache-v4`). Pre-cache each deploy's full asset set under the version tag.
3. **In `activate`, delete only stale caches** — caches whose version tag is not the current one. Do it *after* `clients.claim()` so no controlled page loses assets mid-session.
4. **Serve navigation requests network-first** (or a dedicated app-shell route), so users always land on HTML consistent with the active worker.

The same class of bug applies to `sw.js` itself: browsers cache the worker file for up to 24 hours by default. The modern practice is to serve `sw.js` with `Cache-Control: no-cache` so the browser always re-fetches it on navigation.

## Service workers are not durable

The browser kills an idle service worker aggressively — often within seconds of finishing a `fetch` event — and restarts it on the next event. Any state held in a module-level variable is gone on restart. This is a frequent bug source:

- A counter tracking requests in flight.
- A cache of pending promises.
- A WebSocket connection held in a global.

All evaporate. **Durable state must live in `IndexedDB` or the Cache API.**

Long-running work inside an event handler must be wrapped in `event.waitUntil(promise)` — that tells the browser "do not kill me until this promise settles." Forgetting `waitUntil` means the browser may terminate the worker mid-operation, and background sync, push handling, and cache population silently fail to complete.

<NumbersCard
  id="card-sw-edge-cases"
  title="Service worker durability facts"
  data-lesson-visual
  stats={[
    { label: "Idle worker kill time", value: "Seconds after last event" },
    { label: "sw.js browser cache default", value: "Up to 24 hours" },
    { label: "Recommended sw.js cache header", value: "Cache-Control: no-cache" },
    { label: "Durable state options", value: "Cache API or IndexedDB only" },
    { label: "waitUntil forgets → silent fail", value: "push, sync, cache population" },
  ]}
/>

## Navigation interception and the app-shell danger

The most powerful — and most dangerous — service worker pattern is intercepting navigation requests: the `fetch` handler catches the request for the HTML document itself and returns a cached app shell. This gives instant loads, but creates a class of bug otherwise impossible.

**The trap:** If you ship a bug in the app shell and cache it cache-first, every repeat visit serves the broken shell from cache, bypassing the network where the fix lives. The user cannot escape with an ordinary reload.

**The defence is layered:**

1. **Navigation requests should be network-first** with a short timeout (~3 s, fall back to cache). This ensures a fix reaches users on their first successful load.
2. **Keep a kill switch** — a versioned endpoint the worker checks on activate or periodically. On signal, call `self.registration.unregister()` and delete caches. This lets you remotely detach a broken service worker from all clients.
3. **Never cache navigation cache-only.** Always have a network path.

**A broken service worker shipped widely is a stop-the-deploy incident** because ordinary users have no recovery button — they cannot open DevTools, they cannot clear site data. Your only recourse is the kill switch or a fresh deploy that the old worker fetches on next activation.

<Quiz
  id="sr-quiz-sw-edge-1"
  lessonSlug="06-service-worker-edge-cases"
  lang="en"
  question="A service worker holds an in-flight request map in a module-level `const cache = new Map()`. After a few seconds of inactivity, entries vanish. Why?"
  choices={[
    { label: "The browser terminated the idle service worker; module-level state is lost on restart — durable state must live in IndexedDB or the Cache API", correct: true },
    { label: "The Map has a built-in TTL", misconception: "Map has no TTL. The entries vanish because the entire worker — and its module scope — was torn down and restarted." },
    { label: "Another tab cleared the Map", misconception: "Each page does not share the service worker's module scope, and there is no cross-tab Map clearing. The worker itself was killed." },
    { label: "Garbage collection removed the Map", misconception: "A module-level const is a GC root — it is not collected while the worker lives. The worker stopped living; that is the difference." },
  ]}
/>

<Quiz
  id="sr-quiz-sw-edge-2"
  lessonSlug="06-service-worker-edge-cases"
  lang="en"
  question="You deploy a service worker update and some users report a broken page: scripts fail to load with module-mismatch errors. What is the most likely cause and the robust fix?"
  choices={[
    { label: "Version skew: the worker served cache-first assets from version N against version N+1 HTML. Fix: content-hash all assets, version-tag cache names, use network-first for navigation", correct: true },
    { label: "The service worker cached the HTML with a wrong content-type", misconception: "A content-type issue would fail to parse HTML, not cause module-mismatch errors. Module-mismatch means the wrong JS version loaded." },
    { label: "skipWaiting was not called, so the old worker is still active", misconception: "If the old worker is still active, users get consistent version N — no mismatch. Mismatch happens when old cached assets are served against new HTML or vice versa." },
    { label: "The Cache API does not store JavaScript correctly", misconception: "The Cache API stores responses by URL and handles JS fine. The issue is URL collision between versions — solved by content-hashing." },
  ]}
/>

<Quiz
  id="sr-quiz-sw-edge-3"
  lessonSlug="06-service-worker-edge-cases"
  lang="en"
  question="A user is stuck on a broken cached app shell and a normal reload does not fix it. What is the recovery mechanism you should have built in advance?"
  choices={[
    { label: "A kill switch endpoint the worker checks — on signal it calls self.registration.unregister() and deletes caches, detaching the worker from all clients", correct: true },
    { label: "Tell users to clear browser cache manually", misconception: "Ordinary users cannot or will not do this. A kill switch is the engineering-owned recovery path." },
    { label: "Deploy a new service worker and wait for skipWaiting to propagate", misconception: "If the broken worker is intercepting navigation cache-first, it may serve a cached response before ever checking for updates. The kill switch forces an update check." },
    { label: "The browser will automatically detect the broken worker and unregister it", misconception: "The browser has no concept of a 'broken' worker — it only knows the worker ran and responded. It will keep using it." },
  ]}
/>

<Inset kind="why" lang="en">
Why is a broken service worker so hard to recover from? When a service worker intercepts navigation, it sits between the browser and the server for the HTML document itself — the page cannot load without the service worker responding first. Unlike a broken CDN (where the browser falls back to origin), a broken service worker responds successfully with a broken cached response. The browser has no way to distinguish a correct cached response from a buggy one. This is why the kill switch must be proactive: a URL the worker fetches on every activate, whose response tells the worker whether to unregister itself. If you wait for users to report breakage, you have already shipped.
</Inset>

</Explanation>

<KeyTakeaway>Service workers have no durable module-level state — use waitUntil and IndexedDB/Cache API. Version skew requires content-hashed assets and version-tagged caches. A service worker intercepting navigation must be network-first with a kill switch or a broken deployment traps users permanently.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="06-service-worker-edge-cases-retrieval"
  lang="en"
  questions={[
    { q: "Why does a service worker's module-level state disappear between requests?", a: "The browser kills idle service workers aggressively — often seconds after a fetch event completes — and restarts them on the next event. The module scope is torn down and rebuilt each time. Durable state must live in the Cache API or IndexedDB, and any long-running operation inside an event handler must be wrapped in event.waitUntil(promise) to prevent termination mid-operation." },
    { q: "What is the version-skew failure mode in service workers and how do you prevent it?", a: "Version skew: a service worker serves cached assets from version N against HTML expecting version N+1, causing runtime module-mismatch errors. Prevention: content-hash every asset filename (old and new coexist without collision), version-tag cache names and delete stale ones in activate, serve navigation requests network-first so HTML is always consistent with the active worker." },
    { q: "Why is a broken navigation-intercepting service worker a stop-the-deploy incident, and what is the architectural defence?", a: "When the service worker intercepts navigation and serves the HTML document from cache, users cannot reload their way to the fixed version — the broken cached shell serves on every reload. Ordinary users have no way to clear the worker. The defence: navigation must be network-first (not cache-first), and a kill switch endpoint must exist that the worker checks on activation — if it signals a problem, the worker calls self.registration.unregister() and clears caches." },
  ]}
/>

<Recap lang="en">
Service workers have three major edge-case failure modes. Version skew: serving cached assets from the wrong version — prevented with content-hashed filenames and version-tagged caches. Durability trap: module-level state evaporates between events because the browser kills idle workers; use `event.waitUntil` for long operations and IndexedDB/Cache API for state. Navigation interception: caching the HTML document itself means a broken shell traps users permanently — always use network-first for navigation and build a kill-switch endpoint. All three failures become hard-to-reverse production incidents if deployed without the safeguards.
</Recap>
