---
concepts:
- RSC
- use-client
- per-route-strategy
- ISR
- streaming-SSR
- TTFB
- LCP
- INP
- hydration-mismatch-telemetry
deepensInto: []
estMin: 20
lang: en
lessonType: topic
level: senior
order: 5
prereqs:
- 01-rendering-strategies
- 02-strategy-mechanisms
- 03-hydration-cost
- 04-hydration-mismatch
slug: 05-rsc-and-observability
sources:
- https://react.dev/reference/rsc/server-components
- https://nextjs.org/docs/app/building-your-application/rendering
- https://web.dev/articles/rendering-on-the-web
- https://web.dev/articles/inp
spiral:
- statefulness
- observability
status: ready
summary: How React Server Components reduce the hydration surface, the per-route decision
  frame for picking SSG/ISR/SSR/streaming, and how to observe render strategies in
  production.
title: RSC, per-route strategy, and production observability
track: browser
unit: 06-ssr-vs-ssg
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import DesignPrompt from "~/components/pedagogy/DesignPrompt.astro";
import RFCQuiz from "~/components/pedagogy/RFCQuiz.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A product manager asks: the marketing homepage, the product pages that update pricing a few times a day, the logged-in account dashboard with a slow order-history query, and the checkout flow — should they all use the same rendering strategy? The answer is no, and making the wrong choice costs either TTFB, freshness, INP, or SEO. The correct answer requires a per-route decision, an understanding of RSC's role, and production telemetry that catches problems before users do.
</Hook>

<Crux>RSC limits which components ship client code, orthogonal to SSR. Choose per-route by freshness × interactivity. Gate mismatch and INP in CI.</Crux>

<Explanation>

This lesson gives you the decision frame to walk into that product manager meeting with a concrete answer for each route — and the telemetry setup to know when that answer breaks in production.

**RSC and SSR are orthogonal axes.** React Server Components control which components ever ship JavaScript to the client. A Server Component runs on the server, renders to HTML (or the RSC wire format), and contributes zero bytes to the client bundle — its code never reaches the browser, so it never hydrates. A Client Component (`'use client'` at the top of the file) does ship code to the client, is SSR'd to HTML for the initial render (if SSR is enabled), and then hydrates. SSR controls *when and where the HTML is generated* — at request time on the server (SSR), at build (SSG), or on a schedule with caching (ISR). RSC controls *how much of that HTML needs to become interactive*. A page that is 90% Server Components ships a tiny client bundle and has minimal hydration cost — even with full SSR. The common confusion is thinking RSC replaced SSR; it did not. A well-architected page uses both: Server Components deliver server-computed HTML with zero client cost, and small `'use client'` islands handle the genuinely interactive parts.

<FlowDiagram
  label="Server Components render on the server into a serialised RSC payload that is streamed to the browser, where the client merges it with the Client Component islands that shipped their own code and then hydrates only those islands into the interactive tree."
  hue="var(--d-frontend)"
  nodes={[
    { id: "sc", label: "Server Components", sub: "run on server", col: 0, row: 0 },
    { id: "payload", label: "RSC payload", sub: "serialised", col: 1, row: 0 },
    { id: "stream", label: "Stream", sub: "to browser", col: 2, row: 0 },
    { id: "merge", label: "Client merge", sub: "into tree", col: 3, row: 0 },
    { id: "cc", label: "Client Components", sub: "'use client', ship code", col: 2, row: 1 },
    { id: "tree", label: "Interactive tree", sub: "islands hydrated", col: 4, row: 0 },
  ]}
  edges={[
    { from: "sc", to: "payload" },
    { from: "payload", to: "stream" },
    { from: "stream", to: "merge" },
    { from: "cc", to: "merge" },
    { from: "merge", to: "tree" },
  ]}
  caption="RSC pipeline. Server Components never ship code — they serialise to the RSC payload, which streams to the browser. The client merges that payload with the Client Component islands (the only code that shipped) and hydrates only those islands. Server Components contribute zero client bytes and never hydrate."
/>

<div data-lesson-visual class="my-8 rounded-xl border border-edge bg-surface-2 p-6">
  <p class="text-sm font-semibold text-ink mb-4">RSC vs SSR — two independent axes</p>
  <div class="space-y-3 text-sm">
    <div class="grid grid-cols-2 gap-4">
      <div class="rounded-lg bg-blue-500/10 border border-blue-500/20 px-4 py-3">
        <div class="font-semibold text-blue-700 dark:text-blue-400 mb-1">SSR axis</div>
        <div class="text-ink-2 text-xs">When/where HTML is generated. Options: at build (SSG), at request (SSR), on a schedule+cache (ISR), or client-only (CSR). Controls TTFB and freshness.</div>
      </div>
      <div class="rounded-lg bg-purple-500/10 border border-purple-500/20 px-4 py-3">
        <div class="font-semibold text-purple-700 dark:text-purple-400 mb-1">RSC axis</div>
        <div class="text-ink-2 text-xs">Which components ship JS. Server Components → zero client bytes, zero hydration. Client Components → ship code, SSR'd then hydrated. Controls bundle size and hydration surface.</div>
      </div>
    </div>
    <div class="rounded-lg bg-green-500/10 border border-green-500/20 px-4 py-3">
      <div class="font-semibold text-green-700 dark:text-green-400 mb-1">Both together (Next.js App Router)</div>
      <div class="text-ink-2 text-xs">Server Components SSR'd and streamed → fast TTFB, small bundle. 'use client' leaves hydrated → only interactive parts pay hydration cost. Streaming Suspense boundaries → fast shell, slow data doesn't block.</div>
    </div>
  </div>
</div>

**The per-route decision frame.** The correct approach is never one strategy for the whole application. Every route has a different freshness requirement, interactivity demand, and data latency profile — and those three dimensions determine the strategy:

- **Marketing homepage** — content is identical for everyone, changes on marketing's schedule (days, not minutes). Strategy: SSG. Served directly from the CDN edge, TTFB is tens of milliseconds, it costs zero per-request server work, and a new build on marketing's deploy refreshes it.

- **Product pages** — same content for every user, but prices update a few times a day and SEO requires fast LCP. Strategy: ISR with a revalidation interval (e.g. 4 hours) plus on-demand revalidation triggered by the price feed webhook. The page is a cached static document for the overwhelming majority of requests — excellent LCP and SEO — and freshness is bounded: no request waits more than one interval behind a price update.

- **Account dashboard** — per-user, must be fresh, but has one slow query (order history). Strategy: streaming SSR. Render per request for freshness, but wrap the order-history query in a Suspense boundary. The shell and fast sections stream immediately (~50 ms TTFB); the order-history boundary streams in when its query resolves. The user sees content fast; the slow query does not block it.

- **Checkout** — per-user, correctness is paramount, high interactivity. Strategy: SSR per request. Keep it mostly Server Components with small `'use client'` islands for the form fields and payment inputs. The hydration surface stays minimal — only the interactive form parts hydrate, which keeps INP low even under heavy main-thread pressure.

**Minimising hydration cost across all routes.** The universal principle: push `'use client'` as far down the component tree as possible. A layout component, a card, a data table — these are almost always Server Components. Only the leaf that actually needs `onClick`, `useState`, or browser APIs needs to be a Client Component. When `'use client'` is pushed to leaves, the client bundle shrinks, the hydration pass is tiny, and INP stays comfortably under 200 ms.

<Inset kind="insight" lang="en">
ISR on the edge: each CDN POP holds its own cache entry. On-demand revalidation (Next.js `revalidatePath` / Vercel's purge API) invalidates the entry globally — but there is a brief window (seconds to low tens of seconds) during which some POPs serve the old version and others serve the new one. For price-sensitive pages, the bounded staleness of ISR is a conscious trade: all POPs stale by at most one interval, with on-demand revalidation as the escape valve. If true consistency across all POPs at the moment of an update is required, full SSR per request is the only option.
</Inset>

**Production observability.** A render strategy only works if you know when it breaks. Three categories of signals:

1. **TTFB and LCP per route** — SSG routes should have TTFB under 50 ms from CDN hits; streaming SSR routes should flush the shell in ~50–100 ms. LCP should be under 2.5 s at p75. These are detectable in field data (CrUX) and synthetic monitoring (Lighthouse CI).

2. **INP per page** — the hydration long task is the most common INP regression. If INP at p75 exceeds 200 ms on a page that recently added a Client Component or increased bundle size, the hydration cost grew. Correlate with code changes. Real User Monitoring (RUM) tools segment INP by page.

3. **Hydration mismatch rate** — React logs mismatch warnings in development, but in production they are silent unless you instrument them. Wire `window.addEventListener('error', ...)` to catch React's error boundary reports, or use a framework integration (Next.js `instrumentation.ts`, Sentry's React plugin) that captures hydration errors and sends them to your error tracker. Gate on CI: zero new hydration-mismatch errors per deploy. A mismatch in production means a CLS event and a re-render — they compound when the page has many components.

<RFCQuiz
  id="05-rfc-1"
  pieceSlug="05-rsc-and-observability"
  lang="en"
  question="Which React DOM server API renders a tree as a stream, flushing the shell first and streaming Suspense boundaries as their data resolves?"
  choices={[
    { rfc: "renderToPipeableStream", title: "Node streaming SSR API — flushes the shell, streams Suspense boundaries on resolve", correct: true },
    { rfc: "renderToString", title: "Renders the whole tree to a single string synchronously — no streaming, blocks on the slowest part" },
    { rfc: "hydrateRoot", title: "Client-side API that hydrates server HTML — not a server renderer" },
    { rfc: "createRoot", title: "Client-side API for a fresh client render — does not adopt server HTML" },
  ]}
/>

<Quiz
  id="05-quiz-1"
  lessonSlug="05-rsc-and-observability"
  lang="en"
  question="Why are RSC and SSR described as orthogonal rather than competing?"
  choices={[
    { label: "SSR decides when/where the HTML is generated; RSC decides which components ship client code at all — a page commonly uses both together", correct: true },
    { label: "RSC replaced SSR in React 18", misconception: "RSC did not replace SSR. A modern app server-renders HTML (SSR) and uses RSC to keep server-only components out of the client bundle — both at once." },
    { label: "They are the same thing with different names", misconception: "They are different axes. SSR is about HTML generation timing; RSC is about which component code reaches the client. A page uses each for its own purpose." },
    { label: "RSC only works without SSR", misconception: "RSC composes with SSR — Server Components are rendered to streamed HTML, and Client Components within are SSR'd then hydrated." },
  ]}
/>

<TradeoffMatrix
  id="05-tradeoff-1"
  lessonSlug="05-rsc-and-observability"
  lang="en"
  prompt="An e-commerce product page: same content for every user, prices update 4–6 times a day, must rank in search and load fast. Pick the rendering strategy."
  options={[
    { name: "ISR — static document with a bounded revalidation interval + on-demand revalidation on price updates", summary: "Best TTFB, SEO, and bounded freshness.", correct: true, justification: "The page is identical for all users (no per-user data) so there is no reason to render it per request. ISR serves it from the CDN cache — TTFB in tens of milliseconds, excellent LCP and SEO — while the revalidation interval bounds staleness to at most a few hours, and on-demand revalidation via price-feed webhook ensures the page is refreshed within seconds of a price change. Full SSR would pay server-compute on every request with no benefit for identical content." },
    { name: "Full SSR per request", summary: "Fresh, but pays server-compute on every request.", justification: "For content identical across users, SSR re-renders on every request — unnecessary CPU and TTFB cost with no freshness benefit over ISR. Acceptable for truly unique per-request data; wrong for shared product pages." },
    { name: "SSG — render at build, never revalidate", summary: "Fast, but prices are stale until next build.", justification: "Pure SSG with no revalidation means prices are locked at build time. For products with daily pricing, this causes visible price mismatches. ISR with on-demand revalidation is the right model." },
    { name: "CSR — fetch the product data in the browser", summary: "Empty shell on first load, bad LCP and SEO.", justification: "CSR sends an empty shell and fetches data in the browser. LCP is the data-loaded render, not the initial HTML — terrible for SEO and first paint. Never right for content that must rank in search." },
  ]}
/>

<DesignPrompt
  id="05-design-1"
  pieceSlug="05-rsc-and-observability"
  lang="en"
  prompt="Design the rendering strategy for an e-commerce site: a marketing homepage, category pages, product pages with daily-changing prices, a logged-in account dashboard, and a checkout flow. Optimise first paint, freshness, and interactivity per route."
  constraints={[
    "Marketing homepage: identical for everyone, changes on marketing's schedule.",
    "Product pages: same for everyone, price updates a few times a day, must load fast and rank in search.",
    "Account dashboard: per-user, must be fresh, has one slow 'order history' query.",
    "Checkout: per-user, correctness over everything, interactive.",
    "Hydration cost must not block early interactions — INP under 200 ms.",
    "No hydration-mismatch errors in production.",
  ]}
  canonical="Choose per route. Marketing homepage: SSG — rendered at build, served static from the CDN, instant TTFB; rebuild on marketing's deploy. Product pages: ISR — served static for the overwhelming majority of requests, with a revalidation interval (e.g. every few hours) plus on-demand revalidation when the price feed updates, so freshness is bounded without a full rebuild; this also gives the best LCP and SEO since the page is a cached static document. Account dashboard: streaming SSR — render per request for freshness, but wrap the slow 'order history' query in a Suspense boundary so the dashboard shell and fast widgets stream immediately (~50 ms TTFB) and order history streams in when its query resolves. Checkout: SSR, per request, correctness-first; keep it mostly Server Components with small 'use client' islands for the interactive form fields so the hydration surface is minimal. Across all routes: minimise the client bundle by keeping components as Server Components and pushing 'use client' as far down the tree as possible, so hydration cost stays small and INP stays under 200 ms. Guard determinism: no Date/Math.random/window read during render — defer client-only values to useEffect — and wire hydration-mismatch errors to telemetry with a CI gate. Verify: each route type tested for TTFB, LCP, the FCP-to-interactive gap, and zero hydration warnings."
  keyPoints={[
    "Pick the strategy per route by data freshness and interactivity, never one strategy for the whole app.",
    "SSG for build-frozen content, ISR for bounded staleness, streaming SSR for per-request with a slow dependency.",
    "Wrap slow data in Suspense boundaries so fast content streams without waiting.",
    "Minimise hydration: Server Components by default, 'use client' pushed down to small leaves.",
    "Enforce render determinism and wire hydration-mismatch errors to telemetry + a CI gate.",
  ]}
/>

</Explanation>

<KeyTakeaway>RSC and SSR are independent axes — use both. Apply per-route strategy: SSG for static, ISR for bounded-stale, streaming SSR for per-user with slow dependencies. Push 'use client' to leaves to minimise hydration. Wire TTFB, LCP, INP, and hydration-mismatch rate to telemetry and gate on CI.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="05-retrieval"
  lang="en"
  questions={[
    { q: "A page is server-rendered and paints in 400 ms, but users report that buttons do not respond for another ~2 seconds. Explain the mechanism and name three ways to reduce the dead window.", a: "The 400 ms paint is the server HTML arriving and the browser rendering it — pixels, but inert. The ~2 second dead window is hydration: the browser must download the JavaScript bundle, parse and compile it (V8 work, slow on mobile), execute the component tree, and walk the server DOM attaching event handlers. Until hydration reaches a given button, that button has no handler — taps are dropped or queued behind the hydration long task, which is exactly what spikes INP. Three reductions: 1) Ship less JavaScript — use Server Components so display-only components carry zero client code and never hydrate. 2) Hydrate less of the page — islands architecture makes static content ship no JS at all; progressive hydration delays non-critical islands until idle or visible. 3) Reorder the cost — React 18 selective hydration with Suspense boundaries hydrates the part the user interacted with first and yields to input mid-hydration, so even before the whole page is hydrated the touched part comes alive." },
    { q: "RSC, SSR, and streaming are often conflated. Lay out what each one independently controls, and how a single Next.js App Router page uses all three.", a: "They are three separate axes. SSR controls when and where the HTML is generated — on the server, per request, as opposed to at build (SSG) or in the browser (CSR). Streaming controls how the HTML is delivered — flushed in chunks as Suspense boundaries resolve, rather than held until the whole page is done. RSC controls which components ship code to the client at all — Server Components run only on the server and their code never reaches the browser, while Client Components ('use client') are the interactive parts whose code does ship. A single App Router page uses all three: its Server Components run on the server and are rendered to HTML (SSR), the HTML is flushed progressively as data resolves (streaming), and only the 'use client' leaves contribute to the client bundle and need hydration (RSC limiting the hydration surface). RSC reduces how much hydrates; SSR plus streaming controls how fast the HTML appears. They are complementary: a well-built page is mostly Server Components (small bundle, little to hydrate), server-rendered and streamed (fast TTFB and LCP), with small Client Component islands that hydrate quickly because there are few of them." },
    { q: "What three categories of production signals should you monitor for a rendering strategy, and what is the CI gate for hydration mismatches?", a: "1. TTFB and LCP per route — SSG routes should show TTFB under 50 ms from CDN hits; streaming SSR shell should flush in 50–100 ms; LCP under 2.5 s at p75. Detectable via CrUX and Lighthouse CI. 2. INP per page — the hydration long task is the most common INP regression; if INP at p75 exceeds 200 ms after adding a Client Component or growing the bundle, hydration cost grew. Segment INP by page with RUM tools. 3. Hydration mismatch rate — React logs warnings in dev but is silent in production unless instrumented. Wire window.addEventListener('error', ...) or a framework integration (Next.js instrumentation.ts, Sentry React plugin) to capture hydration errors and send to your error tracker. CI gate: zero new hydration-mismatch errors per deploy — a mismatch means a CLS event plus a recovery re-render." },
  ]}
/>

<Recap lang="en">
RSC and SSR solve different problems on different axes. SSR controls when HTML is generated (build, request, cached interval); RSC controls which components ship JavaScript at all — Server Components contribute zero client bytes and never hydrate. The combination: mostly Server Components keeps the bundle small and hydration cost low, SSR or streaming delivers fast TTFB and LCP, and small `'use client'` leaves hydrate quickly. The per-route decision frame: marketing content → SSG; shared content with bounded staleness → ISR with on-demand revalidation; per-user with a slow dependency → streaming SSR with Suspense around the slow query; per-user high-interactivity → SSR with minimal `'use client'` surface. Push `'use client'` to the smallest leaf that actually needs it. In production: monitor TTFB and LCP per route, INP per page, and hydration-mismatch rate with a CI gate of zero new mismatches per deploy. Hydration is a statefulness question — the server captured a state snapshot that the client must reconstruct identically; every strategy here is a different decision about which state is frozen when and what it costs to carry it across the wire. Now when someone on your team proposes "let's just SSR everything," you know exactly which routes will pay an unnecessary server-compute tax — and which genuinely need it.
</Recap>
