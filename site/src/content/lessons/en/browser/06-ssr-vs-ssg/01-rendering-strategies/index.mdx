---
slug: 01-rendering-strategies
lang: en
track: browser
unit: 06-ssr-vs-ssg
order: 1
title: "Rendering strategies: SSG, SSR, ISR, streaming, and hydration"
summary: "When the HTML is produced — at build, per request, on a schedule, or in chunks — and why hydration is the separate step that makes that HTML come alive."
estMin: 12
status: ready
lessonType: topic
level: junior
prereqs: ["browser/02-render-pipeline/01-the-six-stages"]
deepensInto: ["02-strategy-mechanisms", "03-hydration-cost"]
spiral: ["statefulness", "latency"]
concepts: ["SSG", "SSR", "ISR", "streaming-SSR", "hydration", "time-to-interactive"]
sources:
  - https://web.dev/articles/rendering-on-the-web
  - https://react.dev/reference/react-dom/client/hydrateRoot
  - https://nextjs.org/docs/app/building-your-application/data-fetching/incremental-static-regeneration
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import DragOrder from "~/components/pedagogy/DragOrder.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import NumberDrill from "~/components/pedagogy/NumberDrill.astro";
import NumbersCard from "~/components/layout/NumbersCard.astro";

<Hook>
A user taps a product page. The server sends finished HTML — text, price, photo arrive in under a second, the page looks ready. Then nothing happens when they tap "Add to cart" for another two seconds. The page is painted but not yet alive.
</Hook>

<Crux>SSR, SSG, ISR, streaming — when to render the HTML. Hydration — the price to make that HTML interactive.</Crux>

<Explanation>

The rendering strategies split on timing. **SSG** (Static Site Generation) renders at build time — the HTML is a file on a CDN, served instantly to every visitor. **SSR** (Server-Side Rendering) renders per request — fresh HTML for every hit, but the server works on every request. **ISR** (Incremental Static Regeneration) is SSG that refreshes itself on a schedule or on demand. **Streaming SSR** sends the HTML in chunks as each part is ready, rather than waiting for the whole page.

Cutting across all of them: **hydration** is the client-side step where React (or any framework) re-runs the component tree to attach event handlers and state to the server's HTML. Until hydration finishes, the page looks done but cannot be clicked.

<div data-lesson-visual class="my-8 rounded-xl border border-edge bg-surface-2 p-6">
  <p class="text-sm font-semibold text-ink mb-4">The four strategies and one shared step</p>
  <div class="grid grid-cols-1 gap-3 text-sm">
    <div class="flex items-start gap-3 rounded-lg bg-surface-3 px-4 py-3">
      <span class="font-mono font-bold text-accent-blue w-10 shrink-0">SSG</span>
      <span>HTML rendered <strong>once at build</strong> → file on CDN → instant TTFB for every visitor. Frozen at build time.</span>
    </div>
    <div class="flex items-start gap-3 rounded-lg bg-surface-3 px-4 py-3">
      <span class="font-mono font-bold text-accent-blue w-10 shrink-0">SSR</span>
      <span>HTML rendered <strong>per request</strong> → fresh, per-user data. TTFB includes server render time + data fetch.</span>
    </div>
    <div class="flex items-start gap-3 rounded-lg bg-surface-3 px-4 py-3">
      <span class="font-mono font-bold text-accent-blue w-10 shrink-0">ISR</span>
      <span>SSG + <strong>revalidation rule</strong> → serve stale while regenerating in background. stale-while-revalidate for pages.</span>
    </div>
    <div class="flex items-start gap-3 rounded-lg bg-surface-3 px-4 py-3">
      <span class="font-mono font-bold text-accent-blue w-12 shrink-0">Stream</span>
      <span>Server sends <strong>shell immediately</strong> → slow parts stream in as Suspense boundaries resolve.</span>
    </div>
    <div class="flex items-start gap-3 rounded-lg bg-amber-500/10 border border-amber-500/30 px-4 py-3">
      <span class="font-mono font-bold text-amber-600 dark:text-amber-400 w-10 shrink-0">Hydrat.</span>
      <span>Client downloads JS, re-runs components, <strong>attaches handlers</strong>. Happens after HTML arrives regardless of strategy.</span>
    </div>
  </div>
</div>

**The restaurant metaphor.** SSG is a meal cooked in advance and kept ready — served instantly, but made hours ago. SSR is cooking to order — fresh and exactly right, but the customer waits. ISR is a buffet a cook refreshes every so often — mostly instant, occasionally a tray is swapped. Streaming is bringing out each dish the moment it is ready. And hydration is the part after the plate lands: the food looks perfect, but the customer cannot eat until the cutlery arrives.

**Why the gap matters.** A server-rendered page looks ready almost instantly — text and images are right there in the HTML. But click a button before hydration finishes and nothing happens: the picture is on screen, the app is not running yet. The gap between "looks ready" and "works" is the most misunderstood thing in modern web performance.

<DragOrder
  id="01-drag-1"
  lessonSlug="01-rendering-strategies"
  lang="en"
  prompt="A server-rendered page goes from request to a working button. Drag the steps into order."
  items={[
    { id: "request", label: "Browser requests the page" },
    { id: "html", label: "Server renders components to HTML and sends it" },
    { id: "paint", label: "Browser paints the HTML — page looks ready" },
    { id: "js", label: "Browser downloads and runs the JavaScript bundle" },
    { id: "hydrate", label: "Hydration: framework attaches handlers — buttons now work" },
  ]}
  correctOrder={["request", "html", "paint", "js", "hydrate"]}
/>

<Quiz
  id="01-quiz-1"
  lessonSlug="01-rendering-strategies"
  lang="en"
  question="A server-rendered page looks fully loaded but a button does nothing when clicked. What is the most likely reason?"
  choices={[
    { label: "Hydration has not finished — the HTML is painted but the JavaScript that wires up the button has not run yet", correct: true },
    { label: "The button is broken in the server code", misconception: "The button works once hydration completes. The problem is timing — the JS that activates it has not run yet, not a code bug." },
    { label: "The server forgot to send the button", misconception: "The button is visible, so the HTML for it arrived. What is missing is the client-side JavaScript that makes it interactive." },
    { label: "The CSS has not loaded", misconception: "Missing CSS would make the page look wrong, not make a visible button unresponsive. Unresponsiveness is a hydration-timing problem." },
  ]}
/>

<Quiz
  id="01-quiz-2"
  lessonSlug="01-rendering-strategies"
  lang="en"
  question="Which strategy renders the HTML once, at build time, so every visitor gets a pre-made file?"
  choices={[
    { label: "SSG — Static Site Generation", correct: true },
    { label: "SSR — Server-Side Rendering", misconception: "SSR renders fresh on every request, not once at build time. SSG is the build-time strategy." },
    { label: "Hydration", misconception: "Hydration is not a rendering strategy — it is the client-side step that makes already-rendered HTML interactive." },
    { label: "Streaming", misconception: "Streaming describes how HTML is delivered in chunks, not when it was rendered." },
  ]}
/>

<MetaphorComplete
  id="01-metaphor-1"
  lessonSlug="01-rendering-strategies"
  lang="en"
  setup="In the restaurant metaphor, the plate of food arrives looking perfect, but the customer cannot eat until the cutlery is delivered. The food is the server's HTML. What is the cutlery — the step that makes the page usable?"
  accepted={["hydration", "hydrate", "hydrating", "client hydration"]}
  canonical="hydration"
  explanation="Hydration is the client-side step where the framework downloads its JavaScript, re-runs the components, and attaches event handlers and state to the server-rendered HTML. Before hydration the page is a picture — visible but inert. After hydration it is a live application."
/>

<NumberDrill
  id="01-drill-1"
  lessonSlug="01-rendering-strategies"
  lang="en"
  question="A rough rule of thumb: hydrating a component tree costs about how many times the work of the server's initial render of that same tree?"
  expected={2}
  tolerance={0}
  unit="times"
  explanation="Hydration roughly doubles the render work: the server rendered the tree once to produce the HTML, and the client re-runs the same component tree again to attach interactivity. That is why a heavily server-rendered page can feel fast to appear but slow to respond — the second render runs on the user's possibly-slow device."
/>

<NumbersCard
  id="01-numbers"
  title="Render-strategy numbers"
  stats={[
    { label: "SSG time-to-first-byte", value: "Tens of ms (CDN hit)" },
    { label: "SSR TTFB", value: "Server render + data fetch" },
    { label: "Streaming shell flush", value: "~50 ms" },
    { label: "Hydration cost", value: "≈ 2× the server render" },
    { label: "Good INP (p75)", value: "≤200 ms" },
  ]}
/>

</Explanation>

<KeyTakeaway>SSG renders at build (frozen, fast), SSR per request (fresh, costs server time), ISR refreshes static on a schedule, streaming flushes the shell first. Hydration — ~2× the server render — is the shared last step that wires buttons up.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="01-retrieval"
  lang="en"
  questions={[
    { q: "What does SSG stand for and when is the HTML rendered?", a: "Static Site Generation — HTML is rendered once at build time and served as a static file from a CDN. The content is frozen at build." },
    { q: "What is hydration and why does it happen after the page is already painted?", a: "Hydration is the client-side process where the framework re-runs the component tree to attach event handlers and state to the server's HTML. The HTML arrives first (fast paint), but the JS that wires up interactivity runs after — during hydration." },
    { q: "What is ISR and how does it differ from pure SSG?", a: "Incremental Static Regeneration is SSG with a revalidation rule. Pages are served static from cache, but after a configured time the server regenerates a fresh version in the background — stale-while-revalidate applied to whole pages." },
  ]}
/>

<Recap lang="en">
The four strategies answer when the HTML is produced: SSG at build (instant CDN hit, frozen content), SSR per request (fresh, server-compute cost on every hit), ISR on a revalidation schedule (bounded staleness, mostly static delivery), and streaming SSR in chunks (shell appears at ~50 ms, slow widgets stream in later). Hydration is the separate, ~2× cost step that runs regardless of strategy — the framework re-runs the component tree on the client to attach event handlers to the server's HTML. Until hydration finishes for a given element, that element is visible but dead. The gap between first paint and interactivity is where "I clicked and nothing happened" bugs live.
</Recap>
