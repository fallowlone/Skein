---
concepts:
- hydration-mismatch
- CLS
- determinism
- useEffect
- dehydrated-query-cache
- renderToPipeableStream
- Suspense-boundary
deepensInto:
- 05-rsc-and-observability
estMin: 18
lang: en
lessonType: topic
level: senior
order: 4
prereqs:
- 01-rendering-strategies
- 03-hydration-cost
- browser/05-react-fiber/03-render-and-commit-phases
slug: 04-hydration-mismatch
sources:
- https://react.dev/reference/react-dom/client/hydrateRoot
- https://web.dev/articles/rendering-on-the-web
- https://react.dev/reference/react-dom/server/renderToPipeableStream
- https://web.dev/articles/inp
spiral:
- statefulness
status: ready
summary: How the hydration contract works, what breaks it, how React recovers, and
  the determinism rule that prevents CLS and re-render cost in production.
title: 'Hydration mismatch: causes, detection, and the determinism rule'
track: browser
unit: 06-ssr-vs-ssg
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import DebugLog from "~/components/pedagogy/DebugLog.astro";
import FadedExample from "~/components/pedagogy/FadedExample.tsx";
import TraceScenario from "~/components/pedagogy/TraceScenario.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
A greeting component renders "Good morning" or "Good evening" based on the hour. In development it works. In production, users see a flash: text changes the instant the page loads, and layout shifts by 0.14. The browser console says: "Text content did not match. Server: 'Good evening' Client: 'Good morning'." The cause is a broken contract called a hydration mismatch.
</Hook>

<Crux>Hydration is a contract: server DOM must equal client first-render DOM. Non-determinism breaks it — causing CLS and a recovery re-render.</Crux>

<Explanation>

**The mechanism, precisely.** Hydration is a contract: the DOM the client produces on its first render must be structurally identical to the DOM the server serialised into the HTML. React walks the server DOM and the client's virtual tree in lockstep, adopting each node. When a node disagrees — different text, different attributes, a different element, an extra or missing child — the contract is broken. React's recovery: it logs a mismatch error and, for the affected subtree, throws away the server HTML and client-renders it from scratch. That recovery render is the visible symptom: a flash of changed content and a layout shift that spikes CLS.

<FlowDiagram
  label="React compares the server-serialised DOM with the client's first render node by node: when they match it adopts the existing nodes, but when they diverge it logs a mismatch, discards the server HTML for that subtree, and re-renders it client-side, spiking CLS."
  hue="var(--d-frontend)"
  nodes={[
    { id: "server", label: "Server DOM", sub: "serialised HTML", col: 0, row: 0 },
    { id: "client", label: "Client render", sub: "first pass", col: 0, row: 1 },
    { id: "compare", label: "Compare", sub: "node by node", col: 1, row: 0 },
    { id: "adopt", label: "Adopt nodes", sub: "contract held", col: 2, row: 0 },
    { id: "mismatch", label: "Mismatch", sub: "contract broken", col: 2, row: 1 },
    { id: "discard", label: "Discard HTML", sub: "subtree", col: 3, row: 1 },
    { id: "rerender", label: "Client re-render", sub: "flash + CLS", col: 4, row: 1 },
  ]}
  edges={[
    { from: "server", to: "compare" },
    { from: "client", to: "compare" },
    { from: "compare", to: "adopt", label: "equal" },
    { from: "compare", to: "mismatch", label: "diverge" },
    { from: "mismatch", to: "discard" },
    { from: "discard", to: "rerender" },
  ]}
  caption="The hydration contract in one picture. Equal trees: React adopts the server nodes, cheaply. Divergent trees: React throws the server HTML away for that subtree and renders it from scratch on the client — the visible flash and the CLS spike."
/>

The common causes are all forms of non-determinism between the two environments: `Date.now()`, `Math.random()`, `window`/`document`/`localStorage` read during render (undefined on the server), `Intl` formatting that depends on locale or timezone, `typeof window` branches, and browser extensions that mutate the DOM before React hydrates.

**The determinism rule.** The cure is to make the first client render a pure function of the same inputs the server had. Any value that can differ between server and client must not appear during render — it is deferred to a `useEffect` that runs after hydration, or gated behind a `useState(false)` "mounted" flag flipped in an effect, so both the server render and the first client render produce the placeholder and only a later render introduces the client-specific value. When you see a flicker on load that only appears in production, ask yourself what the component is reading during render that the server and the client compute at different moments.

<div data-lesson-visual class="my-8 rounded-xl border border-edge bg-surface-2 p-6">
  <p class="text-sm font-semibold text-ink mb-4">Hydration mismatch: what breaks and how</p>
  <div class="space-y-2 text-sm">
    <div class="grid grid-cols-3 gap-2 font-semibold text-xs text-ink-2 border-b border-edge pb-2">
      <div>Non-determinism source</div>
      <div>Why it mismatches</div>
      <div>Fix</div>
    </div>
    <div class="grid grid-cols-3 gap-2 py-2 border-b border-edge/50 text-xs">
      <div class="font-mono">new Date() / Date.now()</div>
      <div>Server and client run at different moments</div>
      <div>Defer to useEffect; render null or server value first</div>
    </div>
    <div class="grid grid-cols-3 gap-2 py-2 border-b border-edge/50 text-xs">
      <div class="font-mono">Math.random()</div>
      <div>Different random values each run</div>
      <div>Generate server-side, pass as serialized prop</div>
    </div>
    <div class="grid grid-cols-3 gap-2 py-2 border-b border-edge/50 text-xs">
      <div class="font-mono">window / localStorage</div>
      <div>undefined on server, object on client</div>
      <div>typeof window check deferred to effect</div>
    </div>
    <div class="grid grid-cols-3 gap-2 py-2 border-b border-edge/50 text-xs">
      <div class="font-mono">Intl / timezone</div>
      <div>Server timezone differs from user timezone</div>
      <div>Pass timezone explicitly; format server-side</div>
    </div>
    <div class="grid grid-cols-3 gap-2 py-2 text-xs">
      <div>Browser extension DOM mutation</div>
      <div>Extension adds/removes nodes before hydration</div>
      <div>suppressHydrationWarning on affected nodes</div>
    </div>
  </div>
</div>

**The data mismatch trap.** The same constraint applies to data. If the server renders from one snapshot of an API response and the client refetches a newer one before hydrating, the trees diverge — a hydration mismatch caused by data, not by time or locale. The fix: the server's data must be embedded in the HTML and reused by the client for the first render. This is exactly what frameworks do with the dehydrated query cache (React Query, Apollo, SWR): serialize the server's data into the HTML, rehydrate it on the client, render the first pass from that cache, and only then allow fresh fetches.

<Inset kind="edgecase" lang="en">
The size of the embedded dehydrated cache matters. A very large API response serialized into the HTML inflates the document and parse time — sometimes the right trade is to embed the minimum for the above-the-fold content and refetch the rest post-hydration, accepting one extra background fetch to keep the document small.
</Inset>

**Streaming SSR internals.** `renderToPipeableStream` renders the tree depth-first and flushes the shell — everything outside any pending `Suspense` boundary — as soon as it is ready. For each `Suspense` boundary whose data has not resolved, the server emits the fallback inline and keeps the connection open. When a boundary's data resolves, the server renders that boundary's real content and writes it as a chunk: a hidden `<div>` carrying the content plus a tiny inline `<script>` that relocates it into the right slot and removes the fallback. The browser, parsing HTML progressively, executes that script the moment the chunk arrives. Boundaries stream in completion order, not document order — a fast widget low on the page can arrive before a slow widget above it.

**Streaming and hydration interleave.** As each `Suspense` boundary's HTML streams in, React on the client can hydrate that boundary independently — it does not wait for the whole document. If the user interacts with an already-streamed-but-not-yet-hydrated boundary, React records the event, prioritises hydrating that boundary first, then replays the event. The senior implication: where you place `Suspense` boundaries is a performance design decision, not just an error/loading-state decision. A boundary around a slow, below-the-fold widget lets the rest of the page stream and hydrate without waiting for it; no boundary and that widget blocks the whole document.

<DebugLog
  id="04-debug-1"
  lessonSlug="04-hydration-mismatch"
  lang="en"
  label="Browser console — hydration warning"
  output={`Warning: Text content did not match.
  Server: "Good evening" Client: "Good morning"
    at Greeting (greeting.tsx:6)
    at Header
    at App

Warning: An error occurred during hydration. The server HTML
was replaced with client content in <div id="greeting">.

[CLS] layout shift: 0.14  (source: #greeting subtree)`}
  outputLang="log"
  question="The Greeting component renders 'Good morning' or 'Good evening' based on the hour. It mismatches on hydration and causes a 0.14 CLS shift. Why does it mismatch, and what is the correct fix?"
  hint="The server and the client run the component at different moments — and possibly in different timezones. What does Greeting read during render?"
  answer="Greeting reads the current hour during render. The server rendered it in the server's timezone at the request moment; the client hydrated it seconds later, in the user's timezone — so the two renders compute different greetings, React detects the text mismatch, throws away the server subtree, and re-renders client-side, which shifts layout (CLS 0.14). The root cause is non-determinism: the component's output depends on Date/timezone, which differ between the two environments. The correct fix: make the first client render match the server. Option one: render a neutral, stable placeholder on the first pass, then compute the real time-of-day greeting in a useEffect after hydration and update state — the first client render matches the server exactly and only the post-hydration update changes it, with no mismatch. Option two: decide the greeting on the server from a source both sides agree on (e.g. pass the user's timezone explicitly, compute server-side, pass the result as a serialized prop) so both environments compute from identical inputs. Either way, never read a live, environment-dependent value directly in render of a server-rendered component. Wire hydration-mismatch errors to telemetry so this is caught in CI, not in the field."
/>

<FadedExample
  client:visible
  id="04-faded-1"
  lessonSlug="04-hydration-mismatch"
  lang="en"
  title="Eliminate a hydration mismatch from a client-only value"
  steps={{
    solved: (
      <pre><code>{`function LocalTime() {
  // First render (server AND first client render) shows the
  // placeholder — they match, so no hydration mismatch.
  const [time, setTime] = useState(null);

  useEffect(() => {
    // Runs only on the client, AFTER hydration.
    setTime(new Date().toLocaleTimeString());
  }, []);

  return <span>{time ?? 'Loading…'}</span>;
}`}</code></pre>
    ),
    semi: {
      prompt: (
        <p>Fill in the hook that defers the client-only value, and where the live value is computed so it runs after hydration.</p>
      ),
      blanks: [
        { id: "state", expected: /useState/i, placeholder: "hook holding the deferred value" },
        { id: "effect", expected: /useEffect/i, placeholder: "hook that runs after hydration" },
      ],
    },
    blank: {
      prompt: (
        <p>A component renders <code>new Date().toLocaleTimeString()</code> directly. It throws a hydration-mismatch warning and flickers on load. Rewrite it so the server render and the first client render agree. What is the structural change?</p>
      ),
      reveal: (
        <p>Do not read the live value during render. Initialise state to a stable placeholder (<code>null</code> or the server's value) so the server render and the first client render produce identical DOM — that satisfies the hydration contract and the mismatch disappears. Then compute the real client-only value inside <code>useEffect</code>, which runs only on the client and only after hydration completes, and call <code>setState</code> to update. The user sees the placeholder for one frame, then the live time — no mismatch, no flicker, no CLS. The general rule: any value that differs between server and client (time, locale, <code>window</code>, <code>localStorage</code>, random) must be introduced after hydration, never during render.</p>
      ),
    },
  }}
/>

<TraceScenario
  id="04-trace-1"
  lessonSlug="04-hydration-mismatch"
  lang="en"
  scenario="A server-rendered page shows a console warning 'Text content did not match. Server: 14:32 Client: 14:33' and a timestamp briefly flickers on load. What happened and how is it fixed?"
  steps={[
    { prompt: "The component renders the current time directly — the server rendered it at one second, the client hydrated a second later, so the two renders disagree and React re-rendered the subtree", reveal: "Correct. Rendering a live value like the current time during render guarantees server and client disagree, because they run at different moments. React detects the mismatch, discards the server HTML for that subtree, and re-renders it client-side — the visible flicker. Fix: render a stable placeholder on the first pass (or the server's value), then update to the live time in a useEffect after hydration, so the first client render matches the server exactly." },
    { prompt: "The server clock is wrong", reveal: "The clocks are fine — they are simply read at different instants. Even perfectly synced clocks differ by the seconds between server render and client hydration. The fix is not clock sync; it is not rendering a live value during render." },
    { prompt: "The CDN cached a stale page", reveal: "A stale cache would show an old but internally consistent page, not a server/client mismatch warning. The warning is specifically about the two renders disagreeing." },
    { prompt: "The JavaScript bundle is corrupted", reveal: "A corrupt bundle would fail to hydrate entirely, not produce a precise 'text content did not match' diff. This is a determinism problem in one component." },
  ]}
/>

<Quiz
  id="04-quiz-1"
  lessonSlug="04-hydration-mismatch"
  lang="en"
  question="Why are RSC and SSR described as orthogonal rather than competing?"
  choices={[
    { label: "SSR decides when/where the HTML is generated; RSC decides which components ship client code at all — a page commonly uses both together", correct: true },
    { label: "RSC replaced SSR in React 18", misconception: "RSC did not replace SSR. A modern app server-renders HTML (SSR) and uses RSC to keep server-only components out of the client bundle — both at once." },
    { label: "They are the same thing with different names", misconception: "They are different axes. SSR is about HTML generation timing; RSC is about which component code reaches the client." },
    { label: "RSC only works without SSR", misconception: "RSC composes with SSR — Server Components are rendered to streamed HTML, and Client Components within are SSR'd then hydrated." },
  ]}
/>

</Explanation>

<KeyTakeaway>Hydration is a contract: server DOM must equal client first-render DOM. Any environment-dependent value read during render breaks it, causing a CLS-spiking recovery re-render. Fix: defer client-only values to useEffect; embed server data in the HTML so the client's first pass uses the same snapshot.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="04-retrieval"
  lang="en"
  questions={[
    { q: "What is a hydration mismatch and what are the three most common causes?", a: "A hydration mismatch is when the DOM the client produces on its first render differs from the DOM the server serialized. React walks them in lockstep and when they diverge it discards the server subtree and re-renders client-side — causing a content flash and CLS. Three common causes: 1) reading Date/time during render (server and client run at different moments); 2) reading window/localStorage during render (undefined on server, object on client); 3) Intl formatting with locale or timezone differences between server and user." },
    { q: "Why must the server's API response data be embedded in the HTML for the first client render?", a: "If the client refetches an API endpoint before hydrating and gets a newer response than the server used, the client's first render produces different content than the server's HTML — a data-driven hydration mismatch. Embedding the server's data in the HTML (dehydrated query cache) lets the client's first render use the exact same snapshot, satisfying the hydration contract. Fresh fetches only happen after hydration completes." },
    { q: "How does placing a Suspense boundary around a slow widget affect streaming and hydration?", a: "With a Suspense boundary around the slow widget, the server sends the shell (everything outside the boundary) immediately, streams the slow widget's HTML as a chunk when its data resolves, and React can hydrate each boundary independently as it arrives. Without a boundary, the slow widget's query blocks the entire HTML response and the entire hydration pass. Boundary placement is a performance design decision: it controls which content waits for which data." },
  ]}
/>

<Recap lang="en">
Hydration is a contract: React walks the server DOM and the client's virtual tree in lockstep, adopting each node. When any node disagrees — because the component read `Date.now()`, `Math.random()`, `window`, or a locale-sensitive format during render — React discards the affected subtree and re-renders it on the client. The visible symptoms are a content flash and a CLS spike. The determinism rule: the first client render must be a pure function of the same inputs the server had. Client-only values move to `useEffect` so both environments produce identical DOM on the first pass. The same constraint applies to data: the server's query snapshot must be embedded in the HTML (dehydrated cache) so the client's first pass uses the same data without refetching. Streaming SSR with `renderToPipeableStream` sends the shell immediately and streams Suspense boundaries as their data resolves — boundary placement decides which content pays which latency, making it a performance design choice, not just a loading-state concern. Now when you see a mysterious CLS spike on a page that never shifts visually during normal browsing, check the hydration-mismatch warnings first — the layout shift is React throwing away the server subtree and re-rendering it.
</Recap>
