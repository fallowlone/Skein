---
concepts:
- full-hydration
- selective-hydration
- progressive-hydration
- islands-architecture
- resumability
- INP
- hydrateRoot
deepensInto:
- 04-hydration-mismatch
- 05-rsc-and-observability
estMin: 16
lang: en
lessonType: topic
level: middle
order: 3
prereqs:
- 01-rendering-strategies
- 02-strategy-mechanisms
- browser/05-react-fiber/03-render-and-commit-phases
slug: 03-hydration-cost
sources:
- https://react.dev/reference/react-dom/client/hydrateRoot
- https://docs.astro.build/en/concepts/islands/
- https://qwik.dev/docs/concepts/resumable/
- https://web.dev/articles/inp
spiral:
- statefulness
- latency
status: ready
summary: Why full hydration is expensive, how selective hydration and islands reduce
  what needs to hydrate, and how resumability eliminates the re-render entirely.
title: 'Hydration cost: selective, progressive, islands, resumability'
track: browser
unit: 06-ssr-vs-ssg
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import MetaphorComplete from "~/components/pedagogy/MetaphorComplete.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
A news article page: 2,000 words of text, one comment box, a like button. Classic full-page hydration runs the framework across all 2,000 words of text — making text that has no interactivity "interactive." The bundle is larger than it needs to be and the hydration long task blocks the main thread, spiking INP. There are better options.
</Hook>

<Crux>Full hydration re-runs the whole tree — islands and selective hydration make only the interactive parts pay the cost.</Crux>

<Explanation>

**Why hydration is expensive.** Hydration roughly doubles render work — the tree is rendered once on the server and effectively rendered again on the client. But it is worse than a simple doubling, because the client render runs on the user's device, which may be a mid-range phone an order of magnitude slower than your server. And it is all main-thread JavaScript: downloading the bundle, parsing and compiling it (V8 work), then running the component tree and attaching handlers. On a large app this is hundreds of milliseconds to seconds of main-thread time during which the page looks done but is not. This whole window is the "uncanny valley" of SSR — the page is painted and looks interactive, the user tries to interact, and the input is dropped or queued behind the hydration long task, spiking INP.

**Full vs selective hydration.** Classic React hydration is full and eager: the whole app hydrates in one pass before anything is interactive, and it is blocking. React 18 introduced selective hydration: with `Suspense` boundaries, parts of the tree hydrate independently, and React prioritises hydrating whatever the user just interacted with. If the user clicks a not-yet-hydrated section, React hydrates that section first, ahead of the rest. It also makes hydration interruptible — it can yield to input mid-hydration. Selective hydration does not remove the total cost; it reorders it so the cost lands where the user is not looking and the part they touched comes alive first.

<div data-lesson-visual class="my-8 rounded-xl border border-edge bg-surface-2 p-6">
  <p class="text-sm font-semibold text-ink mb-4">Hydration spectrum — from full to zero</p>
  <div class="space-y-3 text-sm">
    <div class="rounded-lg bg-red-500/10 border border-red-500/20 px-4 py-3">
      <div class="font-semibold text-red-700 dark:text-red-400 mb-1">Full hydration (classic React)</div>
      <div class="text-ink-2">Hydrates the entire tree in one blocking pass. Nothing is interactive until the whole page finishes. Bundle includes every component's code.</div>
    </div>
    <div class="rounded-lg bg-amber-500/10 border border-amber-500/20 px-4 py-3">
      <div class="font-semibold text-amber-700 dark:text-amber-400 mb-1">Selective hydration (React 18 + Suspense)</div>
      <div class="text-ink-2">Parts of the tree hydrate independently. User interaction prioritises that boundary. Interruptible — yields to input. Same total cost, better order.</div>
    </div>
    <div class="rounded-lg bg-blue-500/10 border border-blue-500/20 px-4 py-3">
      <div class="font-semibold text-blue-700 dark:text-blue-400 mb-1">Islands architecture (Astro, Marko)</div>
      <div class="text-ink-2">Page is static HTML by default. Only interactive components ("islands") ship JS and hydrate. Static text: zero JS, never hydrates. Can delay: client:visible, client:idle.</div>
    </div>
    <div class="rounded-lg bg-green-500/10 border border-green-500/20 px-4 py-3">
      <div class="font-semibold text-green-700 dark:text-green-400 mb-1">Resumability (Qwik)</div>
      <div class="text-ink-2">No hydration at all. App state + handler locations serialized into HTML. Client resumes from serialized state, attaches a handler only on first interaction. Startup JS ≈ 0.</div>
    </div>
  </div>
</div>

**Progressive hydration and the islands architecture.** The deeper fix is to hydrate less. The islands architecture (Astro, Marko, Fresh) starts from the opposite default: the page is static HTML, and only the specific interactive components — the "islands" — ship JavaScript and hydrate. A blog post is 95% text: with islands, that 95% ships zero JavaScript and never hydrates, and only the comment box and the like button — the actual islands — carry JS. Each island hydrates independently, and can be told when to (`client:visible` hydrates on scroll into view, `client:idle` when the main thread is free). This is progressive hydration: the page is interactive where it needs to be, inert and free everywhere else.

**Resumability: skipping hydration entirely.** Qwik pushes the idea to its limit with resumability. Instead of re-running the component tree on the client to rebuild state and handlers, Qwik serializes the entire application state — including which handler is attached where — into the HTML itself. The client does not re-render to hydrate; it resumes from the serialized state, attaching a handler only when the user actually interacts with that specific element. Startup JavaScript approaches zero regardless of app size. The trade-off is a more complex serialization format and a framework built around it from the ground up — but conceptually it is the endpoint of the whole progression: from "hydrate everything" to "hydrate what the user touched" to "do not hydrate at all, just resume."

<Quiz
  id="03-quiz-1"
  lessonSlug="03-hydration-cost"
  lang="en"
  question="What does the islands architecture do differently from classic full-page hydration?"
  choices={[
    { label: "The page is static HTML by default; only the specific interactive components ship JavaScript and hydrate — the rest never does", correct: true },
    { label: "It hydrates the whole page faster by using a Web Worker", misconception: "Islands do not move hydration to a worker (hydration touches the DOM). They reduce how much hydrates at all — most of the page never hydrates." },
    { label: "It renders everything on the client instead of the server", misconception: "Islands are server-rendered HTML. Only islands get client JS — the opposite of more client rendering." },
    { label: "It removes the need for a server", misconception: "Islands still need HTML rendered somewhere (build or server). The architecture is about hydration scope, not about eliminating the server." },
  ]}
/>

<Quiz
  id="03-quiz-2"
  lessonSlug="03-hydration-cost"
  lang="en"
  question="A page is fully painted and visually complete, but tapping a button in the first 2 seconds does nothing. Which metric captures this and what is the cause?"
  choices={[
    { label: "INP — the hydration JavaScript is occupying the main thread, so the tap is dropped or queued behind the hydration long task", correct: true },
    { label: "LCP — the largest element has not painted", misconception: "The page is fully painted, so LCP already fired. The problem is interactivity, not paint — that is INP's domain." },
    { label: "TTFB — the server was slow", misconception: "TTFB measures server-to-first-byte. The HTML already arrived and painted; the issue is post-paint interactivity." },
    { label: "CLS — the layout shifted", misconception: "CLS measures layout movement. A dead button is an interactivity-timing problem, captured by INP." },
  ]}
/>

<MetaphorComplete
  id="03-metaphor-1"
  lessonSlug="03-hydration-cost"
  lang="en"
  setup="Qwik avoids re-running the component tree on the client. Instead it serializes the whole app state — including which handler belongs where — into the HTML, and the client picks up exactly where the server left off, attaching a handler only on first interaction. What is this hydration-free approach called?"
  accepted={["resumability", "resumable", "resume", "resumable hydration"]}
  canonical="resumability"
  explanation="Resumability means the client resumes the application from serialized state instead of rebuilding it. Classic hydration re-executes the whole component tree on the client; resumability serializes all of that into the HTML so the client never re-runs the tree — it just attaches a specific handler the instant the user interacts with that specific element. Startup JavaScript stays near zero no matter how large the app is."
/>

<TradeoffMatrix
  id="03-tradeoff-1"
  lessonSlug="03-hydration-cost"
  lang="en"
  prompt="A content site: long articles (mostly text) with a comment box and a few interactive widgets. Pick the rendering and hydration approach."
  options={[
    { name: "Static HTML + islands — only the comment box and widgets ship JS and hydrate", summary: "Minimal JS, minimal hydration.", correct: true, justification: "The article is 95% text — that 95% should ship zero JavaScript and never hydrate. Islands deliver static HTML for the text and hydrate only the genuinely interactive parts. First paint is instant, hydration cost is tiny because there is almost nothing to hydrate, and INP stays low because the main thread is never blocked by a full-page hydration pass." },
    { name: "Full SSR + full-page hydration", summary: "Works, but hydrates the 95% that needs nothing.", justification: "Server-rendering then hydrating all of it pays to make static text 'interactive' when it has no interactivity. The bundle is larger and the hydration long task is longer than needed — a real INP cost for zero benefit on the text." },
    { name: "Pure client-side rendering (SPA)", summary: "Worst first paint for content.", justification: "CSR ships an empty shell and renders in the browser — the worst possible first paint and LCP for a content site, plus SEO costs." },
    { name: "SSR with no Suspense boundaries", summary: "One slow widget blocks the whole article.", justification: "Without Suspense boundaries, a single slow widget's data fetch holds the entire HTML response — the article text waits for a comment count query." },
  ]}
/>

</Explanation>

<KeyTakeaway>Full hydration re-runs the entire component tree — expensive on slow devices and blocks INP. Selective hydration (React 18) reorders cost to prioritise user interaction. Islands hydrate only the interactive parts. Resumability skips the re-run entirely by serializing state into HTML.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="03-retrieval"
  lang="en"
  questions={[
    { q: "What are the four stages of hydration cost on the main thread?", a: "1. Download the JS bundle (network). 2. Parse and compile it (V8 work, brutal on slow mobile CPUs). 3. Execute the module and component tree to rebuild the virtual tree. 4. Attach — walk the server DOM adopting nodes and wiring handlers. All four happen after the page is already painted." },
    { q: "How does selective hydration change the user's experience compared to full hydration?", a: "Selective hydration (React 18 + Suspense) lets each boundary hydrate independently and prioritises whichever boundary the user just interacted with. It makes hydration interruptible — it yields to input mid-pass. The total work is the same, but the part the user touched comes alive first rather than waiting for the whole tree." },
    { q: "What is the islands architecture's default assumption, and how does it differ from full hydration's default?", a: "Full hydration assumes all components need to be interactive — they all hydrate. Islands architecture assumes the page is static HTML by default — only components explicitly marked as islands ship JS and hydrate. Static text, layout, and non-interactive UI carry zero client JS." },
  ]}
/>

<Recap lang="en">
Hydration's cost has four parts — download, parse/compile, execute, attach — all on the main thread, all after the page is painted. A 500 KB bundle is not just 500 KB of network; it is hundreds of milliseconds of parse-compile-execute on a phone. The uncanny valley: the page looks interactive, but input is dropped because the hydration long task owns the main thread — field INP. React 18 selective hydration makes the cost interruptible and priority-ordered but does not reduce the total. The islands architecture attacks the total by hydrating only the interactive parts — a text-heavy page with islands ships zero JS for the text. Resumability (Qwik) eliminates the re-render entirely: state and handler locations are serialized into the HTML, and the client resumes from that serialized state with zero startup JavaScript regardless of app size.
</Recap>
