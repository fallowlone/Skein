---
concepts:
- render-strategy-selection
- isr-revalidation
- streaming-ssr
- hydration-cost
- rsc-vs-ssr
- hydration-mismatch
estMin: 13
lang: en
level: senior
order: 7
prereqs: []
slug: quiz-choice
sources:
- https://web.dev/articles/rendering-on-the-web
- https://react.dev/reference/rsc/server-components
- https://nextjs.org/docs/app/building-your-application/data-fetching/incremental-static-regeneration
status: ready
summary: "Multiple-choice synthesis across the render-strategies unit: choosing SSG/ISR/SSR/streaming per route, the orthogonality of RSC and SSR, and the real cost of hydration."
title: 'Render strategies: multiple-choice review'
track: browser
unit: 06-ssr-vs-ssg
---

import Hook from "~/components/lesson/Hook.astro";
import Goal from "~/components/lesson/Goal.astro";
import Recap from "~/components/lesson/Recap.astro";
import Quiz from "~/components/pedagogy/Quiz.astro";

<Hook>
Six questions that cut across the whole unit. Each one is a decision you make designing a real route — not a definition to recite, but a tradeoff between freshness, server cost, and the dead window before the page can be clicked.
</Hook>

<Goal lang="en">
Confirm you can connect when the HTML is produced (SSG/ISR/SSR/streaming) with how much of it has to become interactive (hydration, RSC) — the synthesis the individual lessons built toward.
</Goal>

<Quiz
  id="06-ssr-vs-ssg-choice-1"
  lessonSlug="06-ssr-vs-ssg"
  lang="en"
  question="An e-commerce product page is identical for every user, its price updates 4–6 times a day, and it must rank in search with a fast LCP. Which strategy fits best, and why?"
  choices={[
    { label: "ISR — serve a cached static document for the overwhelming majority of requests, with a revalidation interval plus on-demand revalidation on the price-feed webhook. Best LCP/SEO, and staleness is bounded to one interval.", correct: true },
    { label: "Pure SSG with no revalidation — render once at build.", misconception: "Pure SSG freezes the price at build time; a price that changes several times a day would be visibly stale until the next full rebuild. ISR adds bounded revalidation without losing static delivery." },
    { label: "Full SSR per request — render fresh every time.", misconception: "The content is identical for all users, so SSR pays server-compute on every request for zero freshness benefit over ISR. SSR is for genuinely per-request or per-user data." },
    { label: "CSR — fetch the product data in the browser.", misconception: "CSR ships an empty shell and renders after a client fetch, so LCP is the data-loaded render — the worst first paint and SEO for content that must rank." },
  ]}
/>

<Quiz
  id="06-ssr-vs-ssg-choice-2"
  lessonSlug="06-ssr-vs-ssg"
  lang="en"
  question="A logged-in dashboard must be per-user fresh but has one slow order-history query (~400 ms). Plain SSR makes every user wait 400 ms for TTFB. What is the right fix?"
  choices={[
    { label: "Streaming SSR — render per request, but wrap the order-history query in a Suspense boundary so the shell and fast widgets flush at ~50 ms and order history streams in when its query resolves.", correct: true },
    { label: "Switch the dashboard to ISR.", misconception: "ISR caches one shared document; a per-user dashboard cannot be served from a shared cache. ISR is for content identical across users." },
    { label: "Move the whole dashboard to SSG.", misconception: "SSG renders once at build with no per-user or per-request data — it cannot reflect a specific logged-in user's order history at all." },
    { label: "Keep plain SSR but raise the server's CPU allocation.", misconception: "More CPU does not shrink a 400 ms query that blocks the whole response. Streaming changes the unit of waiting from the whole page to one Suspense boundary, which is the actual fix." },
  ]}
/>

<Quiz
  id="06-ssr-vs-ssg-choice-3"
  lessonSlug="06-ssr-vs-ssg"
  lang="en"
  question="A teammate says 'we adopted RSC, so we no longer need SSR.' Why is this wrong?"
  choices={[
    { label: "RSC and SSR are orthogonal axes. SSR decides when/where the HTML is generated; RSC decides which components ship client code at all. A modern App Router page uses both — Server Components are SSR'd to HTML, and 'use client' leaves hydrate.", correct: true },
    { label: "Correct — RSC replaced SSR in React 18.", misconception: "RSC did not replace SSR. Server Components are still rendered to HTML on the server (SSR), often streamed; RSC only removes their code from the client bundle." },
    { label: "RSC and SSR are the same mechanism under two names.", misconception: "They are different axes: SSR is about HTML-generation timing and location; RSC is about which component code reaches the browser at all." },
    { label: "Wrong, because RSC requires CSR to function.", misconception: "RSC does not depend on client-side rendering; Server Components produce HTML and the RSC payload on the server. The real point is that RSC composes with SSR, not that it needs CSR." },
  ]}
/>

<Quiz
  id="06-ssr-vs-ssg-choice-4"
  lessonSlug="06-ssr-vs-ssg"
  lang="en"
  question="A news article — 2,000 words of text, one comment box, one like button — is fully SSR'd and fully hydrated. INP at p75 is poor. What is the highest-leverage architectural fix?"
  choices={[
    { label: "Hydrate less. Use islands (or push 'use client' to the comment box and like button only) so the 95% that is text ships zero JS and never hydrates — the hydration long task shrinks and INP drops.", correct: true },
    { label: "Move the hydration work to a Web Worker.", misconception: "Hydration touches the DOM and cannot run off the main thread. The fix is to hydrate less of the page, not relocate the same work." },
    { label: "Switch the article from SSR to CSR.", misconception: "CSR makes first paint and LCP worse for content and still ships JS for everything. It increases, not decreases, the client work that spikes INP." },
    { label: "Raise the server's render budget so hydration finishes faster.", misconception: "Hydration runs on the user's device, not the server. Server budget does not change client-side parse/compile/execute cost; reducing what hydrates does." },
  ]}
/>

<Quiz
  id="06-ssr-vs-ssg-choice-5"
  lessonSlug="06-ssr-vs-ssg"
  lang="en"
  question="A greeting component reads the current hour during render and prints 'Good morning' or 'Good evening'. In production users see a flash and a 0.14 CLS spike, with a console warning that server and client text disagree. What is the root cause and the correct fix?"
  choices={[
    { label: "A hydration mismatch from non-determinism: server and client run the component at different moments and possibly different timezones, so React discards the server subtree and re-renders, shifting layout. Fix: defer the live value to useEffect so the first client render matches the server.", correct: true },
    { label: "The server clock is wrong and needs NTP sync.", misconception: "Even perfectly synced clocks differ by the seconds between server render and client hydration. The cure is not clock sync; it is not reading a live value during render." },
    { label: "The CDN cached a stale version of the page.", misconception: "A stale cache serves an old but internally consistent page — no server/client diff warning. This warning is specifically the two renders disagreeing." },
    { label: "The JavaScript bundle is too large, so hydration ran out of time.", misconception: "Bundle size affects how long hydration takes, not whether the two renders agree. A precise 'text content did not match' diff is a determinism bug in one component." },
  ]}
/>

<Quiz
  id="06-ssr-vs-ssg-choice-6"
  lessonSlug="06-ssr-vs-ssg"
  lang="en"
  question="Across an e-commerce app you want to minimise hydration cost and keep INP under 200 ms on every route. What is the single most reliable rule?"
  choices={[
    { label: "Keep components as Server Components by default and push 'use client' as far down the tree as possible — only the leaf that actually needs onClick/useState/browser APIs ships code, shrinking the bundle and the hydration surface.", correct: true },
    { label: "Mark the top-level layout 'use client' so the whole app hydrates in one predictable pass.", misconception: "A 'use client' boundary high in the tree drags every descendant into the client bundle — the largest possible hydration surface, the opposite of the goal." },
    { label: "Disable streaming so hydration starts only after the full document arrives.", misconception: "Streaming improves TTFB and lets boundaries hydrate as they arrive; disabling it delays interactivity. It does not reduce how much must hydrate." },
    { label: "Render every route with SSG so nothing needs to hydrate.", misconception: "SSG controls when HTML is generated, not whether it hydrates — a fully SSG page with Client Components still hydrates them. Per-user routes also cannot be SSG at all." },
  ]}
/>

<Recap lang="en">
The through-line of the unit is a two-axis decision. Axis one — when the HTML is produced — runs SSG (build, frozen, instant CDN hit) → ISR (bounded staleness via stale-while-revalidate) → SSR (per-request fresh, server cost on the critical path) → streaming SSR (shell first at ~50 ms, slow Suspense boundaries later). Axis two — how much of that HTML must become interactive — is governed by RSC (Server Components ship zero client code) and the hydration strategy (full → selective → islands → resumability). Production failures resolve back to these: stale prices want ISR, slow per-user queries want streaming plus Suspense, poor INP wants less hydration, and the content flash plus CLS spike is a hydration mismatch fixed by render determinism.
</Recap>
