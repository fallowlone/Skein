---
slug: 05-devtools-and-frame-lifecycle
lang: en
track: browser
unit: 02-render-pipeline
order: 5
title: "DevTools flame strip and the frame lifecycle"
summary: "How to read a Performance flame strip, what contain/content-visibility do, the exact order of events inside one frame, and how microtasks differ from tasks."
estMin: 16
status: ready
lessonType: topic
level: middle
prereqs: ["04-compositor-layers"]
deepensInto: ["06-layout-thrash", "08-observability-and-attack-surface"]
spiral: ["statefulness"]
concepts: ["performance-panel", "flame-strip", "frame-lifecycle", "raf", "resize-observer", "intersection-observer", "microtask", "task", "scheduler-yield", "vsync", "contain", "content-visibility"]
sources:
  - https://developer.chrome.com/docs/devtools/performance/reference
  - https://html.spec.whatwg.org/multipage/webappapis.html#event-loops
  - https://developer.chrome.com/blog/renderingng-architecture
---

import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import DragOrder from "~/components/pedagogy/DragOrder.astro";

<Hook>
You open DevTools Performance, record a scroll, and see a sea of coloured bars. Some bars are red. Most are yellow. You know jank is somewhere in there — but you don't know which bar to fix. The flame strip is a map. This lesson teaches you to read it.
</Hook>

<Crux>Flame strip colours encode pipeline stages; rAF fires before style, ResizeObserver after layout, microtasks drain between every step.</Crux>

<Explanation>

## Reading the DevTools Performance flame strip

Open DevTools → Performance, click record, do whatever caused the jank, stop recording.

**Track colours (Main thread):**
- **Yellow** — scripting (JavaScript executing)
- **Purple** — rendering (style recalc + layout)
- **Green** — paint
- **Dark blue** — compositing

**Track colours (Frames):**
- **Green** — on-time frame
- **Yellow** — late frame
- **Red** — dropped frame

The flame strip stacks calls top-down: the top bar is what JS triggered; each bar below is what that called. Find a long bar in red or yellow — that is your bottleneck. Hover to see exact ms.

<div data-lesson-visual class="my-6 rounded-lg border border-border bg-surface-2 p-4 text-sm">
  <p class="font-semibold mb-3">Common DevTools patterns → diagnosis</p>
  <div class="space-y-3">
    <div class="rounded border border-border bg-surface p-2">
      <p class="font-semibold text-yellow-600 dark:text-yellow-400">Yellow JS bar → purple Layout bar</p>
      <p class="text-ink-2 text-xs mt-1">JS triggered a forced synchronous layout. The most common render-performance bug.</p>
    </div>
    <div class="rounded border border-border bg-surface p-2">
      <p class="font-semibold text-purple-600 dark:text-purple-400">Wide purple Recalculate Style after a class change high in the tree</p>
      <p class="text-ink-2 text-xs mt-1">You cascaded style invalidation to too many descendants. Move the class change lower or use contain: style.</p>
    </div>
    <div class="rounded border border-border bg-surface p-2">
      <p class="font-semibold text-green-600 dark:text-green-400">Wide green Paint after filter: blur(...) change</p>
      <p class="text-ink-2 text-xs mt-1">You exceeded paint complexity. filter is paint-heavy because each pixel requires multi-pixel sampling.</p>
    </div>
    <div class="rounded border border-border bg-surface p-2">
      <p class="font-semibold text-blue-600 dark:text-blue-400">Wide dark-blue Composite Layers when nothing else looks wrong</p>
      <p class="text-ink-2 text-xs mt-1">Too many layers, often from will-change abuse. Audit with Layer Borders.</p>
    </div>
  </div>
</div>

## The frame lifecycle: exact order

Inside one frame the browser runs work in a strict order defined by the HTML spec:

1. **Collect input events** (mousemove, keypress)
2. **Run setTimeout / setInterval callbacks** if their time has elapsed
3. **Run microtasks** (promise resolutions) until the queue is empty
4. **Run requestAnimationFrame callbacks**
5. **Run ResizeObserver and IntersectionObserver callbacks**
6. **Compute style and layout**
7. **Paint**
8. **Composite**

After step 8 the browser yields to the OS and waits for the next vsync.

**Crucial property:** rAF runs *before* style/layout, so a write inside rAF is followed by exactly one layout pass for the frame — that is the entire purpose of rAF. ResizeObserver fires *after* layout but *before* paint; you can react to layout changes without triggering another forced layout. IntersectionObserver fires asynchronously across frames, never blocking the current one.

## Microtasks vs tasks: scheduling boundaries

The microtask queue (promises, `queueMicrotask`) **drains to empty** between every step of the event loop, including between rAF callbacks.

A misbehaving promise chain that schedules itself indefinitely starves rendering — the browser cannot reach step 6 until the queue empties. This is why long promise chains and tight `await` loops appear in DevTools as a single uninterrupted yellow scripting bar, not a series of small ones.

Modern code that wants to yield to the renderer mid-task uses:
- `scheduler.yield()` (Scheduler API, Chrome 115+) — designed specifically for this
- `setTimeout(fn, 0)` — drops work onto the macrotask queue and unblocks rendering

## Reactive frameworks and the pipeline

When React re-renders, DOM updates happen on the main thread, then the pipeline runs style and layout on changed nodes. Reconciliation itself is pure JS — it shows up as yellow in DevTools. If your re-render touches a node near the top of the tree, style invalidation cascades down the tree; if it touches an isolated leaf, the invalidation is local.

**Key practice:** keep state local, as close to the leaf as possible. A `useState` in a leaf component invalidates one node; a `useContext` on a top-level provider can invalidate hundreds. The same principle applies to Svelte stores, MobX observables, Vue refs — every reactivity model eventually writes to the DOM, and a DOM write triggers the same pipeline.

<Inset kind="why" lang="en">
Understanding the frame lifecycle order is what separates "scheduling tricks that work" from "scheduling tricks that quietly thrash." A common mistake: writing a CSS property inside `setTimeout(fn, 0)` expecting it to batch with rAF-driven writes. It does not — `setTimeout` fires at step 2, *before* rAF at step 4, so the write happens one step before the rAF writes and cannot be batched together. Always put visual writes inside rAF, not macrotask callbacks.
</Inset>

<DragOrder
  id="05-devtools-and-frame-lifecycle-drag-1"
  lessonSlug="05-devtools-and-frame-lifecycle"
  lang="en"
  prompt="Drag the events into the order they happen inside one frame, according to the HTML spec's rendering steps."
  items={[
    { id: "input", label: "Browser processes input events (mousemove, keypress)" },
    { id: "timer", label: "setTimeout / setInterval callbacks fire if due" },
    { id: "microtasks", label: "Microtasks drain (promise.then handlers)" },
    { id: "raf", label: "requestAnimationFrame callbacks run" },
    { id: "observers", label: "ResizeObserver / IntersectionObserver callbacks fire" },
    { id: "stylelayout", label: "Style and layout are computed" },
    { id: "paint", label: "Paint runs on the main thread" },
    { id: "composite", label: "Compositor thread assembles and ships the frame" },
  ]}
  correctOrder={["input", "timer", "microtasks", "raf", "observers", "stylelayout", "paint", "composite"]}
/>

<Quiz
  id="05-devtools-and-frame-lifecycle-quiz-1"
  lessonSlug="05-devtools-and-frame-lifecycle"
  lang="en"
  question="In the DevTools Performance panel, a yellow JS bar is immediately followed by a purple Layout bar of similar length. What happened?"
  choices={[
    { label: "The JS triggered a forced synchronous layout — it read a geometry property after writing a style", correct: true },
    { label: "The browser scheduled layout after every JS task, by design", misconception: "Layout does not run after every JS task. It runs only when the layout dirty flag is set, and a forced sync layout happens only when JS reads geometry after a style write." },
    { label: "A CSS animation ran and required layout per frame", misconception: "CSS animations on composite-only properties (transform, opacity) would not show a purple Layout bar at all. Layout-triggering animations show layout bars, but they come from the CSS animation engine, not from JS." },
    { label: "The JavaScript parsed a large JSON object", misconception: "JSON.parse shows up as a yellow scripting bar with no purple Layout bar following it. Parsing JS objects does not touch the layout engine." },
  ]}
/>

<Quiz
  id="05-devtools-and-frame-lifecycle-quiz-2"
  lessonSlug="05-devtools-and-frame-lifecycle"
  lang="en"
  question="An infinite `await` loop in a promise chain runs for 200 ms. What does the DevTools Performance panel show?"
  choices={[
    { label: "One long uninterrupted yellow bar — microtasks drain to empty before rendering can proceed, so the renderer is starved", correct: true },
    { label: "Many short yellow bars separated by green paint bars", misconception: "If microtasks yield to rendering, you would see this pattern. But microtasks drain to empty before rendering — an infinite microtask loop never yields." },
    { label: "A purple Recalculate Style bar because promises touch the CSSOM", misconception: "Promises are pure JS machinery with no connection to CSSOM. An infinite promise loop shows as a yellow scripting bar, not purple." },
    { label: "Alternating yellow and blue bars", misconception: "Blue bars are compositing. Compositing cannot interleave with a blocked main thread because the compositor still runs on its own thread — but you would not see alternating bars for a microtask starvation scenario." },
  ]}
/>

<Quiz
  id="05-devtools-and-frame-lifecycle-quiz-3"
  lessonSlug="05-devtools-and-frame-lifecycle"
  lang="en"
  question="You want to read the new size of a resized element and update a sibling's style in response. Which observer fires at the right time to avoid triggering a second layout?"
  choices={[
    { label: "ResizeObserver — fires after layout but before paint, letting you react to size changes without a forced re-layout", correct: true },
    { label: "IntersectionObserver", misconception: "IntersectionObserver fires asynchronously across frames, not synchronously after layout. You cannot use it for same-frame size-reactive updates." },
    { label: "MutationObserver", misconception: "MutationObserver fires when DOM mutations occur — not specifically after layout computes new sizes. Reading geometry in a MutationObserver callback can trigger a forced layout." },
    { label: "requestAnimationFrame", misconception: "rAF runs *before* style and layout (step 4), so reading element sizes inside rAF will force the browser to flush pending layout to answer the read — a forced sync layout." },
  ]}
/>

</Explanation>

<KeyTakeaway>The flame strip maps pipeline stages to colours: yellow=JS, purple=layout, green=paint, blue=composite. rAF runs before layout; ResizeObserver runs after. Microtasks drain to empty between every step — an infinite promise loop starves rendering.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="05-devtools-and-frame-lifecycle-retrieval"
  lang="en"
  questions={[
    { q: "In what order do these fire inside one frame: rAF, setTimeout, microtasks, ResizeObserver, style/layout?", a: "setTimeout → microtasks → rAF → ResizeObserver → style/layout. (Input events come first, then timers, then microtasks drain, then rAF, then observers, then style+layout, then paint, then composite.)" },
    { q: "Why does an infinite promise chain starve rendering?", a: "The microtask queue drains to empty before the browser can advance to step 6 (style/layout). A self-scheduling promise keeps the queue non-empty indefinitely, so rendering never runs." },
    { q: "What colour are layout bars in the DevTools Performance panel, and what colour are scripting bars?", a: "Layout (and style recalc) bars are purple. Scripting (JavaScript) bars are yellow. A yellow bar immediately followed by a purple bar of similar length is the classic forced-sync-layout signature." },
  ]}
/>

<Recap lang="en">
The DevTools Performance panel encodes pipeline stages as colours: yellow for JS, purple for layout/style, green for paint, dark blue for composite. The frame lifecycle is fixed by the HTML spec — rAF fires before style/layout, ResizeObserver after layout before paint. Microtasks drain completely between every step, so an infinite promise loop starves the renderer. To yield mid-task use `scheduler.yield()` (Chrome 115+) or `setTimeout(fn, 0)`. Reactive frameworks trigger the same pipeline on DOM writes; keeping state local to leaf components minimises style-invalidation cascade.
</Recap>
