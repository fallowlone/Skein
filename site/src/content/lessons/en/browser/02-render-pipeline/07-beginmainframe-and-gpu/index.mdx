---
concepts:
- BeginMainFrame
- CommitMainFrame
- compositor-driven-animation
- rAF-animation
- gpu-memory-eviction
- passive-scroll
- will-change-lifecycle
- tradeoff-matrix
deepensInto:
- 08-observability-and-attack-surface
estMin: 14
lang: en
lessonType: topic
level: senior
order: 7
prereqs:
- 06-layout-thrash
slug: 07-beginmainframe-and-gpu
sources:
- https://developer.chrome.com/blog/inside-browser-part3
- https://developer.chrome.com/blog/renderingng-architecture
- https://drafts.csswg.org/css-will-change/
- https://web.dev/articles/rendering-performance
spiral:
- statefulness
status: ready
summary: How the Chromium two-thread handshake works, why CSS animations on transform/opacity
  survive a blocked main thread, and what happens when GPU memory is exhausted.
title: BeginMainFrame, compositor-driven animations, and GPU memory
track: browser
unit: 02-render-pipeline
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import TradeoffMatrix from "~/components/pedagogy/TradeoffMatrix.astro";

<Hook>
A CSS animation keeps running smoothly at 60 fps while the main thread is blocked for 400 ms parsing a JSON blob. A rAF animation on the same element freezes for the same 400 ms. Same hardware, same property, opposite results — because one hands control to the compositor, the other does not.
</Hook>

<Crux>BeginMainFrame fires every 16.67 ms; stale frame ships if main thread is late. Composite-only CSS animations bypass the main thread.</Crux>

<Explanation>

## The BeginMainFrame handshake

In Chromium's renderer process, the **compositor thread is the driver** and the main thread is the worker. The flow each vsync:

1. Compositor sends `BeginMainFrame` to the main thread
2. Main thread runs all per-frame work: rAF callbacks, style, layout, paint setup
3. Main thread sends `CommitMainFrame` back, handing off a new layer tree
4. Compositor asks raster workers to fill any dirty tiles with bitmaps
5. Compositor performs the GPU draw and displays the frame

If the main thread is too slow, **the compositor does not wait**: it ships the previous frame's layer tree again (a "drawn but stale" frame). The user perceives this as a missed input or a stutter.

`BeginMainFrame` arrives every ~16.67 ms whether the main thread is ready or not. This is the deep reason main-thread work cannot exceed the frame budget: the metronome is relentless.

<div data-lesson-visual class="my-6 rounded-lg border border-border bg-surface-2 p-4 text-sm">
  <p class="font-semibold mb-3">BeginMainFrame flow</p>
  <div class="flex flex-col gap-1">
    <div class="flex gap-2 items-center">
      <span class="w-32 text-xs text-ink-2 text-right">Compositor</span>
      <span class="font-mono text-xs px-2 py-1 rounded bg-blue-100 dark:bg-blue-900/30">BeginMainFrame →</span>
    </div>
    <div class="flex gap-2 items-center">
      <span class="w-32 text-xs text-ink-2 text-right">Main thread</span>
      <span class="font-mono text-xs px-2 py-1 rounded bg-yellow-100 dark:bg-yellow-900/30">rAF + style + layout + paint setup</span>
    </div>
    <div class="flex gap-2 items-center">
      <span class="w-32 text-xs text-ink-2 text-right">Main thread</span>
      <span class="font-mono text-xs px-2 py-1 rounded bg-yellow-100 dark:bg-yellow-900/30">← CommitMainFrame</span>
    </div>
    <div class="flex gap-2 items-center">
      <span class="w-32 text-xs text-ink-2 text-right">Compositor</span>
      <span class="font-mono text-xs px-2 py-1 rounded bg-blue-100 dark:bg-blue-900/30">raster workers fill dirty tiles → GPU draw</span>
    </div>
    <div class="mt-2 font-mono text-xs text-ink-2">If main thread misses: compositor ships previous frame ("stale frame")</div>
  </div>
</div>

## Compositor-driven CSS animations

When you write a CSS animation that only changes `transform` or `opacity` on a compositor-promoted element, the browser detects this at animation-start and **hands the animation's timeline directly to the compositor thread**. From that point the main thread is not involved at all — the compositor interpolates the value frame-by-frame and renders the next bitmap directly.

This is why a CSS animation can keep running smoothly even when the main thread is completely blocked (alert dialog, long synchronous parse, debugger pause): the compositor still runs.

The same animation written in JS via rAF **cannot** do this, because rAF runs on the main thread. A blocked main thread freezes the rAF animation.

Compositor-driven CSS animations are the **only way** to guarantee animation continuity under main-thread pressure.

## GPU memory exhaustion and layer eviction

Layers are not free. Each layer is a GPU bitmap costing width × height × 4 bytes. On a phone with 256 MB of GPU memory, fifty 1080p layers exhausts the budget. When the OS starts evicting tiles:

1. Browser detects the evicted tiles
2. Raster workers re-rasterise them on the fly
3. While rasterising, the compositor composites with incomplete tiles
4. The page stutters — often worse than if no layers were requested at all

The `will-change` anti-pattern causes this: `will-change: transform` on every component permanently reserves a layer per instance. A list of 100 cards each with 5 will-change'd sub-elements holds 500 layers.

**The correct pattern:** set `will-change` just before the animation starts (on `mouseenter`, or in a `transitionstart` listener), remove it on `animationend` or `transitionend`. This keeps GPU memory near zero when the user is not actively interacting.

<Inset kind="why" lang="en">
Why does `will-change: transform` cause promotion before the animation even starts? The browser needs time to rasterise the layer into GPU memory before the first frame of the animation. If it waited until the animation started, the first 1–3 frames would be blank while rasterisation ran. `will-change` is the hint that says "prepare now, not at animation-start." The cost is holding the bitmap for the duration of the hint — which is why you want to remove the hint when the animation ends.
</Inset>

<TradeoffMatrix
  id="07-beginmainframe-and-gpu-tradeoff"
  lessonSlug="07-beginmainframe-and-gpu"
  lang="en"
  prompt="Animate a card from y=0 to y=200 over 300 ms at 60 fps. Pick the implementation."
  options={[
    { name: "CSS animation on transform: translateY(...)", summary: "Composite-only, no main thread cost.", correct: true, justification: "Transform changes do not invalidate layout or paint. The element is promoted to a layer once; the compositor moves the GPU bitmap for the next 18 frames. Main thread is free to do anything else. This is the default for almost every UI animation." },
    { name: "CSS animation on top: 0 → 200px", summary: "Layout per frame.", justification: "Top is a flow-affecting property. Each animation tick invalidates layout for the card and any sibling that depends on it. 18 layouts in 300 ms. On a complex page this is the difference between 60 fps and 20 fps." },
    { name: "transform: translateY with will-change: transform set on mount, never removed", summary: "GPU memory leak.", justification: "Will-change reserves a layer for the lifetime of the element. A list of 100 cards permanently holds 100 layers — possibly 200 MB of GPU memory. Removed cards may not free the layer immediately. The fix is to set will-change just before the animation starts and remove it on animationend." },
    { name: "requestAnimationFrame loop setting element.style.top each frame", summary: "Worst of both.", justification: "rAF still flips layout dirty flags via style writes to top. You also lose the browser's animation-aware scheduling. Same cost as the second option, plus you wrote more code." },
  ]}
/>

<Quiz
  id="07-beginmainframe-and-gpu-quiz-1"
  lessonSlug="07-beginmainframe-and-gpu"
  lang="en"
  question="The main thread is blocked for 400 ms by a synchronous JSON.parse. Which kind of animation continues to run smoothly at 60 fps during the block?"
  choices={[
    { label: "A CSS animation on `transform` of an element promoted to its own compositor layer", correct: true },
    { label: "A `requestAnimationFrame` loop incrementing `element.style.transform`", misconception: "rAF runs on the main thread. A blocked main thread cannot dispatch rAF callbacks, so the animation freezes for the full 400 ms." },
    { label: "A CSS animation on `width`", misconception: "Width invalidates layout, which runs on the main thread. Blocked main thread = no layout = no animation." },
    { label: "A setInterval that nudges `element.style.left`", misconception: "setInterval runs on the main thread and writes a layout-invalidating property. Both halves of this approach fail: the timer cannot fire and the property cannot be applied." },
  ]}
/>

<Quiz
  id="07-beginmainframe-and-gpu-quiz-2"
  lessonSlug="07-beginmainframe-and-gpu"
  lang="en"
  question="GPU memory balloons on a mobile device. Profiling shows 500 compositor layers. Most are from cards in a list, each with `will-change: transform` set permanently on mount. What is the targeted fix?"
  choices={[
    { label: "Set will-change on mouseenter, remove it on animationend — layers exist only during active animations", correct: true },
    { label: "Replace will-change with transform: translateZ(0)", misconception: "transform: translateZ(0) also promotes to a compositor layer and has the same permanent memory cost if never removed. It is a hack that suffers the same problem." },
    { label: "Increase the GPU memory budget in browser settings", misconception: "GPU memory budget is set by the OS and device. There is no browser setting to increase it. The fix must be reducing layer count, not increasing the budget." },
    { label: "Add contain: layout to each card to prevent layer creation", misconception: "contain: layout affects layout propagation, not layer promotion. It does not prevent will-change from promoting to a compositor layer." },
  ]}
/>

</Explanation>

<KeyTakeaway>The compositor sends BeginMainFrame every ~16.67 ms; if the main thread misses, the previous frame ships. CSS animations on composite properties bypass the main thread entirely — they survive a 400 ms block. will-change scoped to animation duration is the correct pattern; permanent will-change exhausts mobile GPU memory and causes tile eviction.</KeyTakeaway>

<RetrievalDrawer
  client:load
  id="07-beginmainframe-and-gpu-retrieval"
  lang="en"
  questions={[
    { q: "What does the compositor do if the main thread misses a BeginMainFrame deadline?", a: "It ships the previous frame's layer tree again (a stale frame), keeping the display running at vsync frequency. The user sees a missed input or a stutter — not a blank screen." },
    { q: "Why does a CSS transform animation survive a blocked main thread while a rAF animation does not?", a: "The browser hands a CSS animation on composite properties directly to the compositor thread at animation-start. The compositor interpolates without involving the main thread. rAF callbacks run on the main thread — a blocked main thread freezes them." },
    { q: "What is the will-change anti-pattern and how do you fix it?", a: "Setting `will-change: transform` permanently on every component. Each instance holds a GPU bitmap for the element's lifetime. Fix: set it on mouseenter (or before animation start), remove it on animationend. GPU memory returns to baseline when no animations are active." },
  ]}
/>

<Recap lang="en">
The Chromium compositor sends `BeginMainFrame` every ~16.67 ms. The main thread responds with `CommitMainFrame` if it finishes on time; if not, the compositor ships the previous frame as a stale frame. CSS animations on `transform` or `opacity` of promoted elements are handed to the compositor at animation-start — the main thread is out of the loop, so they survive a blocked main thread at 60 fps. rAF animations run on the main thread and freeze when it is blocked. The `will-change` anti-pattern reserves GPU bitmaps permanently; on a phone with 256 MB GPU memory, 500 layers from a design system exhausts the budget and the OS evicts tiles. Scope `will-change` to the duration of an animation to keep GPU memory near zero at rest.
</Recap>
