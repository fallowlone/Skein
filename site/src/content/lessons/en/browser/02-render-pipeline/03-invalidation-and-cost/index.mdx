---
concepts:
- invalidation
- dirty-bit
- style-propagation
- layout-propagation
- contain
- content-visibility
- css-containment
- csstriggers
- display-locking
deepensInto:
- 04-compositor-layers
- 06-layout-thrash
estMin: 14
lang: en
lessonType: topic
level: middle
order: 3
prereqs:
- 02-stages-and-threads
slug: 03-invalidation-and-cost
sources:
- https://csstriggers.com/
- https://drafts.csswg.org/css-will-change/
- https://web.dev/articles/content-visibility
- https://web.dev/articles/rendering-performance
spiral:
- statefulness
- encapsulation
status: ready
summary: How CSS property changes propagate through the pipeline, what controls the
  blast radius, and how contain/content-visibility isolate the damage.
title: Invalidation, dirty bits, and contain
track: browser
unit: 02-render-pipeline
---



import Hook from "~/components/lesson/Hook.astro";
import Explanation from "~/components/lesson/Explanation.astro";
import Recap from "~/components/lesson/Recap.astro";
import Inset from "~/components/lesson/Inset.astro";
import Crux from "~/components/prose/Crux.astro";
import KeyTakeaway from "~/components/prose/KeyTakeaway.astro";
import RetrievalDrawer from "~/components/pedagogy/RetrievalDrawer.tsx";
import Quiz from "~/components/pedagogy/Quiz.astro";
import FlowDiagram from "~/components/diagram/FlowDiagram.astro";

<Hook>
You change one class on one element and the browser re-paints a thousand elements. You change a different class on the same element and nothing else moves. The difference is which pipeline stage the property invalidates — and whether anything downstream can escape.
</Hook>

<Crux>Each CSS property change invalidates one stage; dirty bits flow downstream from there — unless containment stops them.</Crux>

<Explanation>

## The invalidation rule

Why does changing one property cost a millisecond while changing another costs twenty? The answer is which pipeline stage it invalidates — and the downstream stages it drags along for the ride.

Changing a CSS property invalidates one or more pipeline stages. Everything from that stage down must re-run.

| CSS change | Invalidates | Cost |
| --- | --- | --- |
| `width`, `height`, `top`, `left` | Layout (+ paint + composite) | High |
| `background-color`, `color` | Paint (+ composite) | Medium |
| `transform`, `opacity` (on own layer) | Composite only | Low |
| `filter: blur(...)` | Paint (+ composite) | Medium–High |

[csstriggers.com](https://csstriggers.com/) publishes a per-property map. Learn the cheap ones (transform, opacity, filter on a promoted element) and the expensive ones (anything that affects flow).

<FlowDiagram
  label="Three kinds of CSS change enter the same Style to Composite pipeline at different points: a layout-affecting property enters at Style, a paint-only property enters at Paint, and transform or opacity enters at Composite. The later a change enters, the fewer stages re-run and the cheaper it is."
  perRow={4}
  hue="var(--d-frontend)"
  nodes={[
    { id: "style", label: "Style", sub: "recalc" },
    { id: "layout", label: "Layout", sub: "measure" },
    { id: "paint", label: "Paint", sub: "rasterize" },
    { id: "composite", label: "Composite", sub: "GPU" },
    { id: "e_layout", label: "width / top", sub: "layout prop" },
    { id: "e_paint", label: "background-color", sub: "paint only" },
    { id: "e_comp", label: "transform / opacity", sub: "composite only" },
  ]}
  edges={[
    { from: "style", to: "layout" },
    { from: "layout", to: "paint" },
    { from: "paint", to: "composite" },
    { from: "e_layout", to: "style", label: "enters" },
    { from: "e_paint", to: "paint", label: "enters" },
    { from: "e_comp", to: "composite", label: "enters" },
  ]}
  caption="The pipeline runs left to right. A layout-changing prop enters at Style and pays all four stages; a paint-only prop enters at Paint and pays two; transform/opacity enters at Composite and pays one. Cheaper changes enter later."
/>

## How dirty bits propagate

<div data-lesson-visual class="my-6 rounded-lg border border-border bg-surface-2 p-4 text-sm">
  <p class="font-semibold mb-2">Dirty-bit propagation rules</p>
  <ul class="space-y-2 list-disc list-inside">
    <li><strong>Style flags</strong> flow down the DOM tree — a class change on a parent dirties all descendants whose selectors match.</li>
    <li><strong>Layout flags</strong> flow down the box tree — a width change on a parent may dirty siblings if their position depends on it.</li>
    <li><strong>Paint flags</strong> are scoped to the paint layer — a paint-affecting change on a layer invalidates that layer's bitmap; sibling layers with their own bitmaps are unaffected.</li>
    <li><strong>Composite</strong> is always dirty-free — it works from already-rasterised tiles and only re-runs the GPU draw call.</li>
  </ul>
  <p class="mt-3 font-mono text-xs text-ink-2">Rule: dirty bits flow DOWN the dependency graph, never UP.</p>
</div>

Together these rules explain why a tooltip that changes `background-color` is cheap (paint flag, one layer) while a tooltip that changes `width` can re-layout the entire page (layout flag cascades to all dependents). The blast radius is determined by how high in the dependency graph the flag starts, and how many nodes depend on it below.

If you can isolate your changes to an independent subtree, the invalidation stops at the isolation boundary.

## CSS Containment: capping the blast radius

CSS Containment gives you a knob to limit how far invalidation propagates.

- **`contain: layout`** — "this element's layout cannot affect its ancestors." If a child resizes, the parent's siblings do not need to re-layout.
- **`contain: paint`** — painting is contained inside the box (overflow clipped). The browser can skip painting it entirely when it is off-screen.
- **`contain: strict`** — combines layout, paint, and size containment.

The newer **`content-visibility: auto`** goes further: it tells the browser to skip the element entirely (no style calc, no layout, no paint) when it is off-screen. Combined with `contain-intrinsic-size` (which gives the browser a placeholder size), it lets a page with thousands of below-the-fold elements render in milliseconds because only on-screen elements pay the cost.

These are the modern "free wins" for any page rendering large lists, infinite scroll feeds, or long-form content.

<Inset kind="edgecase" lang="en">
**Fonts and FOIT/FOUT.** A web font is a pipeline input often overlooked. The font starts loading when the CSS parser sees a `font-family` backed by a `@font-face` with a `src`. Until the font arrives, the browser either shows text in a fallback font (FOUT — flash of unstyled text) or hides text (FOIT — flash of invisible text). The `font-display` property controls this: `swap` shows the fallback immediately then swaps; `optional` uses the fallback if the font hasn't arrived within ~100 ms; `block` hides text for up to 3 s. When the loaded font swaps in, the browser re-runs style recalc and layout, because font metrics change text width, which changes box sizes. On a large text block this swap is visible as frame jank 200–500 ms after first paint.

**Images and CLS.** An image loading without explicit `width` and `height` forces a re-layout when it loads: the image box was 0×0, now it is the actual size, and everything around it shifts — the main cause of CLS regressions. The fix is to set explicit `width`/`height` in HTML or use `aspect-ratio` in CSS; the browser reserves the space up front, and the image load becomes only a paint invalidation, not a layout one. Modern `loading="lazy"` loads off-screen images lazily, saving bandwidth, but still requires pre-declared sizes to avoid layout shift when the image enters the viewport.
</Inset>

<Quiz
  id="03-invalidation-and-cost-quiz-1"
  lessonSlug="03-invalidation-and-cost"
  lang="en"
  question="Which property change invalidates only the composite stage (no layout, no paint)?"
  choices={[
    { label: "`opacity` and `transform` on an element that already has its own compositor layer", correct: true },
    { label: "`width` and `height`", misconception: "Width and height are flow-affecting — they invalidate layout, which forces paint and composite to re-run downstream." },
    { label: "`background-color`", misconception: "Background-color invalidates paint (the bitmap must be redrawn) but not layout. It is cheaper than layout-affecting changes but still costs more than composite-only." },
    { label: "`top` and `left` on a position: absolute element", misconception: "Top and left affect flow geometry (where the box sits relative to its container), so they invalidate layout. This is why transform is preferred over top/left for animation." },
  ]}
/>

<Quiz
  id="03-invalidation-and-cost-quiz-2"
  lessonSlug="03-invalidation-and-cost"
  lang="en"
  question="You add `contain: layout` to a card component. A child element inside it resizes. Which elements need to re-layout?"
  choices={[
    { label: "Only elements inside the card — contain: layout prevents invalidation from escaping the boundary", correct: true },
    { label: "The card and all its ancestors up to the root", misconception: "Without contain: layout that would be true. The whole point of contain: layout is to stop the dirty flag at the card boundary." },
    { label: "Nothing — contain: layout prevents all layout", misconception: "contain: layout prevents propagation outward. Layout still runs inside the contained subtree." },
    { label: "Only the card's siblings", misconception: "Siblings of the card are specifically what contain: layout protects. Without containment they could be invalidated; with it they are safe." },
  ]}
/>

<Quiz
  id="03-invalidation-and-cost-quiz-3"
  lessonSlug="03-invalidation-and-cost"
  lang="en"
  question="A page uses `content-visibility: auto` on 10 000 below-the-fold list items. What happens during first paint?"
  choices={[
    { label: "Only on-screen items pay style, layout, and paint cost — off-screen items are locked and skipped", correct: true },
    { label: "All 10 000 items render normally, just off-screen", misconception: "Without content-visibility: auto, yes. With it, off-screen items are display-locked: no style calc, no layout, no paint until they intersect the viewport." },
    { label: "The browser defers JavaScript for off-screen items", misconception: "content-visibility: auto affects rendering, not JavaScript execution. JS for off-screen components still runs normally." },
    { label: "Off-screen items are removed from the DOM", misconception: "They stay in the DOM; they are just not rendered. Removal is what virtualisation does. content-visibility is a rendering optimisation, not a DOM management strategy." },
  ]}
/>

</Explanation>

<KeyTakeaway>Dirty bits flow downstream from the invalidated stage — layout triggers paint and composite; paint triggers composite. CSS Containment (`contain`, `content-visibility`) stops the blast at a boundary, turning a page-wide re-layout into a subtree re-layout.</KeyTakeaway>

<RetrievalDrawer
  client:visible
  id="03-invalidation-and-cost-retrieval"
  lang="en"
  questions={[
    { q: "Which pipeline stages does changing `width` invalidate?", a: "Layout, then paint, then composite — all downstream stages re-run because width is a flow-affecting property." },
    { q: "What does `contain: layout` tell the browser?", a: "That layout inside this element cannot affect its ancestors or siblings. If a child resizes, the invalidation stops at the contain boundary." },
    { q: "What is the mechanism behind `content-visibility: auto`?", a: "Display locking: the browser pauses rendering for off-screen subtrees (no style, no layout, no paint) and uses a placeholder size. When a subtree intersects the viewport, it unlocks and renders on demand." },
  ]}
/>

<Recap lang="en">
Every CSS property change invalidates exactly one or more pipeline stages; dirty bits then flow downstream. `width` invalidates layout, which forces paint and composite to re-run. `opacity` on a promoted element invalidates only composite — which is why it is "free." CSS Containment (`contain: layout`, `contain: strict`) stops the invalidation blast at a boundary. `content-visibility: auto` takes this further: off-screen subtrees are entirely skipped (display-locked), turning a 1200 ms first-paint into 10 ms for long pages. Images without declared dimensions cause layout invalidation on load, which is the root cause of most CLS regressions. Now when you reach for a CSS property to animate or toggle a state, you'll instinctively ask: does this enter the pipeline at layout, paint, or composite? The answer decides whether a style change costs 0.5 ms or 20 ms.
</Recap>
